\chapter{A Concrete Naming Certificate for $\SardRegularValueUp$}
\label{ch:concrete-instances-sard-regular-value-namecert}
\origin{human}

Following Sard and standard differential-topology treatments, the $\SardRegularValueUp$ packet records the finite regular-value consumer surface for displayed manifold chart rows, smooth-map rows, derivative finite-jet rows, the finite Sard jet packet of \autoref{ch:concrete-instances-sard-theorem-finite-jet-namecert}, terminal $\RealUp$ readback through \autoref{ch:concrete-instances-real-namecert}, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ data. It sits beside the charted manifold carrier of \autoref{ch:concrete-instances-manifold-namecert} and consumes only finite window evidence; it does not claim full Sard measure theory, host smooth topology, a transversality theorem, or choice-selected regular values.

\begin{definition}[Sard regular-value carrier]
\label{def:sard-regular-value-carrier}
A $\SardRegularValueUp$ carrier is a finite $\BHist$ packet
$$
R=(M,F,D,J,V,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\ManifoldUp$ chart row naming the source and target chart windows in which the candidate value is inspected.
\item $F$ is the smooth-map observation row, containing only the displayed map values and chart restrictions consumed by the packet.
\item $D$ is the derivative finite-jet row, recording the derivative windows and rank reads used to test regularity.
\item $J$ is the $\SardTheoremFiniteJetUp$ support row that supplies the finite critical-window exclusion surface.
\item $V$ is the candidate value-window row whose regular-value status is being consumed.
\item $E$ is the terminal $\RealUp$ readback row for the displayed value after the finite chart and jet route has been replayed.
\item $H$ records componentwise $\hsame$ transport for the chart, map, derivative, Sard-jet, value-window, readback, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a regular-value request through $M,F,D,J,V,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\SardRegularValueUp$, $\SardTheoremFiniteJetUp$, $\ManifoldUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SardRegularValueUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these ten rows. It has no coordinate for ambient measure-zero data, host smooth-function spaces, selected values, global transversality, or a standard bridge.
\end{definition}

\begin{theorem}[Sard regular-value NameCert obligations]
\label{thm:sard-regular-value-namecert-obligations}
For every accepted $\SardRegularValueUp$ carrier $R=(M,F,D,J,V,E,H,C,P,N)$, the local $\NameCert_{\SardRegularValueUp}$ surface consists exactly of chart admission through $M$, smooth-map observation through $F$, derivative finite-jet admission through $D$, finite critical-window support through $J$, candidate value exposure through $V$, terminal $\RealUp$ readback through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:sard-regular-value-carrier}. Its rows $M,F,D,J,V,E,H,C,P,N$ are the whole accepted packet, so every regular-value read begins as finite $\BHist$ data.

The differential-topology part is confined to $M,F,D,J,V$. The chart row and smooth-map row expose the finite windows, the derivative row records the rank reads, the Sard finite-jet row supplies the critical-window support, and the value row names the candidate window.

The readback and structure rows preserve that same route. The row $E$ seals the displayed value as $\RealUp$, while $H,C,P,N$ transport, replay, source, and name the packet without adding host measure theory, global transversality, selected regular values, or a smooth-function-space quotient.
\end{proof}

\begin{theorem}[Sard regular-value critical-window handoff]
\label{thm:sard-regular-value-critical-window-handoff}
Every consumer of an accepted $\SardRegularValueUp$ packet that asks whether a displayed value window is regular must first read the route
$$
M\longmapsto F\longmapsto D\longmapsto J\longmapsto V\longmapsto E
$$
with structural support $H,C,P,N$. The row $J$ is the only handoff to $\SardTheoremFiniteJetUp$, so the consumer receives finite critical-window exclusion data rather than a full Sard measure theorem, host topology on smooth maps, or a choice-selected regular value.
\end{theorem}
\begin{proof}
Begin with the chart and map rows $M,F$. They name the displayed source and target windows and the finite observations available to the regular-value consumer.

Next read $D,J,V$. The derivative row supplies the rank-facing finite-jet data, the Sard finite-jet row records the critical-window support from \autoref{ch:concrete-instances-sard-theorem-finite-jet-namecert}, and the value row identifies the inspected target window.

Finally replay the terminal value through $E$ and the structural rows. The route stays inside the displayed carrier and therefore cannot export ambient measure-zero reasoning, host smooth topology, global transversality, or a selected regular value.
\end{proof}

\falsifiablePrediction{Every accepted $\SardRegularValueUp$ consumer must expose $\ManifoldUp$, smooth-map finite observations, derivative finite-jet rows, $\SardTheoremFiniteJetUp$, $\RealUp$ readback, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows before any regular-value result is consumed.}

\independenceWitness{sard-theorem-finite-jet, manifold, real: $\SardRegularValueUp$ is not bijective to any listed sibling because it binds a candidate value-window consumer to chart, map, derivative, finite critical-window support, terminal real readback, transport, replay, provenance, and local naming in one carrier.}

\closureat{SardRegularValueUp}{seedStr}

\begin{closurestatus}{\SardRegularValueUp}
  \constructivestory{A $\SardRegularValueUp$ packet is generated from Manifold chart rows, smooth-map observation rows, derivative finite-jet rows, a SardTheoremFiniteJetUp support row, candidate value-window rows, a terminal RealUp readback row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:sard-regular-value-carrier}, \autoref{thm:sard-regular-value-namecert-obligations}, and \autoref{thm:sard-regular-value-critical-window-handoff} seed the finite regular-value certificate over chart, map, derivative, Sard finite-jet, value-window, readback, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \leantarget{BEDC.Derived.SardRegularValueUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim the full Sard measure theorem, transversality, choice-selected regular values, host smooth topology, Lean theorem checking, or standard bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, derivative finite-jet admission, Sard finite-jet handoff, candidate value-window exposure, terminal Real readback, transport stability, replay, provenance, and local naming for the displayed packet.}
\end{closurestatus}
