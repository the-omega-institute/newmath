\chapter{A Concrete Naming Certificate for $\SeparableUltrametricSpaceUp$}
\label{ch:concrete-instances-separable-ultrametric-space-namecert}
\origin{human}

Following Bourbaki and Kelley, the $\SeparableUltrametricSpaceUp$ packet records the finite NameCert data for an ultrametric space whose countable dense surface is available through displayed prefix windows. It sits after the countable-density route of \autoref{ch:concrete-instances-separablemetric-namecert}, the strong-triangle surface of \autoref{ch:concrete-instances-ultrametricspace-namecert}, the prefix geometry of \autoref{ch:concrete-instances-baire-ultrametric-namecert}, the Baire prefix carrier of \autoref{ch:concrete-instances-bairespace-namecert}, and the finite name windows of \autoref{ch:concrete-instances-streamname-namecert}. The packet exposes separability and ultrametric comparison as one finite carrier, not as selected dense points, quotient stream equality, host metric equality, countable choice, or ambient completeness.

\begin{definition}[Separable ultrametric space carrier]
\label{def:separable-ultrametric-space-carrier}
A $\SeparableUltrametricSpaceUp$ carrier is a finite $\BHist$ packet
$$
X=(D,U,B,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the dense schedule row, recording the countable dense-name family and the finite request bounds inherited from the $\SeparableMetricUp$ route.
\item $U$ is the ultrametric comparison row, recording parent metric reads together with the strong max-radius ledger inherited from $\UltrametricSpaceUp$.
\item $B$ is the Baire-prefix witness row, recording finite prefix cylinders and common-prefix witnesses consumed through $\BaireUltrametricUp$ and $\BaireSpaceUp$.
\item $S$ is the $\StreamNameUp$ window row, listing the finite name coordinates inspected before a dense request or prefix comparison is accepted.
\item $H$ records componentwise $\hsame$ transport for the dense schedule, ultrametric comparison, Baire-prefix witnesses, stream windows, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from dense requests to stream windows, from stream windows to ultrametric comparison, and from prefix witnesses to downstream Baire or completion-facing consumers.
\item $P$ is the $\Pkg$ provenance over $\SeparableUltrametricSpaceUp$, $\SeparableMetricUp$, $\UltrametricSpaceUp$, $\BaireUltrametricUp$, $\BaireSpaceUp$, $\StreamNameUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\SeparableUltrametricSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,U,B,S,H,C,P,N$. It contains no coordinate for choosing dense representatives from arbitrary balls, quotienting stream names, replacing the strong-triangle row by host metric equality, or importing ambient completeness.
\end{definition}

\begin{theorem}[Separable ultrametric NameCert obligations]
\label{thm:separable-ultrametric-space-namecert-obligations}
For every accepted $\SeparableUltrametricSpaceUp$ carrier
$$
X=(D,U,B,S,H,C,P,N),
$$
the local $\NameCert_{\SeparableUltrametricSpaceUp}$ surface consists exactly of carrier admission for the dense schedule, ultrametric comparison, Baire-prefix witnesses, finite stream windows, componentwise transport, displayed replay, provenance, and local naming rows. The certificate exports no selected dense point, quotient stream equality, host metric equality, countable choice, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:separable-ultrametric-space-carrier}. The analytic rows are $D,U,B,S$, while $H,C,P,N$ preserve, replay, source, and name the same finite packet.

The dense schedule row $D$ supplies only indexed dense-name data through the separable-metric route. The ultrametric row $U$ supplies only displayed strong-triangle comparisons, and the prefix row $B$ supplies only finite Baire-prefix witnesses.

The stream window row $S$ is the common readback boundary. Every accepted dense request or prefix comparison must expose its inspected coordinates there before any downstream consumer reads the route.

Since the remaining rows only transport, replay, source, and name this packet, the listed rows exhaust the local NameCert surface and the excluded ambient objects have no carrier coordinate.
\end{proof}

\begin{theorem}[Separable ultrametric dependency obligations]
\label{thm:separable-ultrametric-space-dependency-obligations}
Every accepted $\SeparableUltrametricSpaceUp$ packet reads its density, ultrametric, prefix, and stream-window evidence through
$$
\SeparableMetricUp,\quad \UltrametricSpaceUp,\quad \BaireUltrametricUp,\quad \BaireSpaceUp,\quad \StreamNameUp .
$$
The route is finite and ordered by $D,U,B,S,H,C,P,N$: density requests pass through $D$ and $S$, ultrametric comparisons pass through $U$ and $S$, and Baire-prefix witnesses pass through $B$ and $S$ before the structural rows are consumed.
\end{theorem}
\begin{proof}
Read the dense schedule first. The row $D$ is the only countable-density coordinate, so any separable-metric consumer must use the displayed request schedule rather than a selected dense point.

Read the ultrametric comparison next. The row $U$ is the only strong-triangle coordinate, so ball and prefix comparisons cannot bypass the finite max-radius ledger.

Read the Baire-prefix and stream-window rows together. The row $B$ names finite prefix witnesses and $S$ names the inspected stream coordinates; these are the only rows from which a Baire ultrametric or Baire-space consumer can obtain prefix data.

Finally $H,C,P,N$ preserve that dependency route without adding a second source. Thus the dependency surface is exactly the displayed sibling route.
\end{proof}

\begin{theorem}[Separable ultrametric dense-prefix transport]
\label{thm:separable-ultrametric-space-dense-prefix-transport}
Let $X=(D,U,B,S,H,C,P,N)$ and $X'=(D',U',B',S',H',C',P',N')$ be accepted $\SeparableUltrametricSpaceUp$ carriers. If the dense schedules, ultrametric comparison rows, Baire-prefix witness rows, and finite stream windows transport componentwise under $\hsame$ and replay through displayed $\Cont$ routes, then every dense-prefix consumer of $X$ and $X'$ reads the same finite route through $D,U,B,S$ and $D',U',B',S'$ before provenance and local naming are consumed.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:separable-ultrametric-space-namecert-obligations}. They identify the only readable analytic rows as $D,U,B,S$ and their primed counterparts.

Use the dependency obligations of \autoref{thm:separable-ultrametric-space-dependency-obligations}. Density, ultrametric comparison, Baire-prefix witnesses, and stream windows are already tied to the listed sibling routes before any structural row is inspected.

The componentwise $\hsame$ rows in $H,H'$ transport those four analytic rows, and the displayed $\Cont$ rows in $C,C'$ replay the same dense-prefix route. Provenance and local naming in $P,N,P',N'$ then record the transported packet without adding selected representatives, quotient stream equality, host equality, countable choice, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\SeparableUltrametricSpaceUp$ read factors through the dense schedule, ultrametric comparison, Baire-prefix witness, StreamName window, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows before any consumer reads density or prefix geometry.}

\independenceWitness{separablemetric, ultrametricspace, baire-ultrametric, bairespace, streamname: $\SeparableUltrametricSpaceUp$ is not bijective to any listed sibling because it jointly carries countable-density scheduling, strong ultrametric comparison, Baire-prefix witnesses, finite stream windows, transport, replay, provenance, and local naming as one packet.}

\closureat{SeparableUltrametricSpaceUp}{seedStr}

\begin{closurestatus}{\SeparableUltrametricSpaceUp}
  \constructivestory{A $\SeparableUltrametricSpaceUp$ packet is generated from a dense schedule row, an ultrametric comparison row, Baire-prefix witness rows, finite StreamName windows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:separable-ultrametric-space-carrier}, \autoref{thm:separable-ultrametric-space-namecert-obligations}, \autoref{thm:separable-ultrametric-space-dependency-obligations}, and \autoref{thm:separable-ultrametric-space-dense-prefix-transport} seed the finite separable-ultrametric route through dense schedules, strong ultrametric comparisons, Baire-prefix witnesses, and stream windows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected dense representatives, quotient stream equality, host metric equality, countable choice, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dense-schedule exactness, ultrametric comparison exactness, Baire-prefix witness coverage, stream-window readback, transport, replay, provenance, and local naming for BEDC.Derived.SeparableUltrametricSpaceUp.}
\end{closurestatus}
