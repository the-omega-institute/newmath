\chapter{A Concrete Naming Certificate for $\RealOscillationMajorantUp$}
\label{ch:concrete-instances-real-oscillation-majorant-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\RealOscillationMajorantUp$ packet records a finite oscillation-bound ledger for Real-facing estimates without appealing to host suprema or ambient completeness. It sits over the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the convergence-rate packet of \autoref{ch:concrete-instances-real-modulus-of-convergence-namecert}. The carrier is meant for later Cauchy, continuity, and compact-interval estimates that need a displayed oscillation majorant before they read a terminal $\RealUp$ seal.

\begin{definition}[Real oscillation majorant carrier]
\label{def:real-oscillation-majorant-carrier}
A $\RealOscillationMajorantUp$ carrier is a finite $\BHist$ packet
$$
O=(E,Q,S,D,M,A,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the terminal $\RealUp$ seal row whose accepted real-name boundary is being estimated.
\item $Q$ is the $\RegSeqRatUp$ readback row supplying regular rational approximants to the sealed real name.
\item $S$ is the $\StreamNameUp$ finite-window row listing the stream observations used by $Q$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, carrying dyadic radii, comparisons, and halving slack.
\item $M$ is the $\RealModulusOfConvergenceUp$ schedule row that bounds which windows must be inspected at the displayed tolerance.
\item $A$ is the finite oscillation-majorant ledger, recording a dyadic bound above all displayed pairwise oscillations seen through $Q,S,D,M$.
\item $W$ is the finite witness-window family naming the inspected indices and comparisons that justify the ledger row $A$.
\item $H$ records componentwise $\hsame$ transport for $E,Q,S,D,M,A,W$.
\item $C$ records displayed $\Cont$ replay from a tolerance request through the modulus row and finite witness windows to the majorant ledger.
\item $P$ is the $\Pkg$ provenance over $\RealOscillationMajorantUp$, $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealModulusOfConvergenceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealOscillationMajorantUp}$ row.
\end{enumerate}
The carrier has no coordinate for a host supremum, a completed metric-space bound, a selected limit, quotient equality of streams, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Real oscillation majorant NameCert obligations]
\label{thm:real-oscillation-majorant-namecert-obligations}
For every accepted $\RealOscillationMajorantUp$ carrier
$$
O=(E,Q,S,D,M,A,W,H,C,P,N),
$$
the local $\NameCert_{\RealOscillationMajorantUp}$ surface consists exactly of Real seal admission, regular rational readback, finite stream-window exposure, dyadic tolerance arithmetic, convergence-modulus scheduling, oscillation-majorant ledger exactness, witness-window coverage, componentwise transport, displayed replay, package provenance, local naming, and non-escape for host suprema, selected limits, quotient stream equality, countable choice, and ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-oscillation-majorant-carrier}. Its visible rows are precisely the Real seal, RegSeqRat readback, StreamName window, DyadicRatCore tolerance, RealModulusOfConvergence schedule, majorant ledger, witness windows, and structural rows.

The analytic obligations are finite. Row $D$ fixes the dyadic tolerance arithmetic, row $M$ fixes the inspected tail window, rows $Q,S$ expose the regular rational observations, and row $W$ lists the finite comparisons whose dyadic upper bound is recorded by $A$.

The structural obligations then bind the read. Componentwise transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$ keep every oscillation-majorant consumer on the same displayed packet. Since the excluded supremum, selected-limit, quotient, choice, and completeness objects have no coordinate, the listed rows exhaust the certificate surface.
\end{proof}

\begin{theorem}[Real oscillation majorant L10 readback]
\label{thm:real-oscillation-majorant-l10-readback}
Every L10 Real or Completion consumer of an accepted $\RealOscillationMajorantUp$ packet reads an oscillation bound only through the finite route
$$
\DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealModulusOfConvergenceUp \longmapsto \RealUp .
$$
Concretely, the consumer first reads the dyadic tolerance row $D$, the finite stream-window row $S$, the regular rational readback row $Q$, the convergence-modulus row $M$, and the terminal Real seal row $E$ before it may use the majorant ledger $A$ and witness family $W$. The route exports no host supremum, selected Cauchy limit, quotient stream equality, countable-choice schedule, or ambient $\RealUp$ completeness datum.
\end{theorem}
\begin{proof}
Begin with an accepted carrier $O=(E,Q,S,D,M,A,W,H,C,P,N)$. The only tolerance source is $D$, and the only finite observation source is $S$.

Read the Real-facing schedule. The modulus row $M$ bounds which stream windows are inspected, the regular rational row $Q$ supplies the displayed approximants on those windows, and the Real seal row $E$ is reached only after that finite route has been exposed.

The majorant ledger is then read from $A$ together with the witness family $W$. Rows $H,C,P,N$ transport, replay, source, and name the same packet, so the L10 consumer cannot replace the displayed route by a host supremum, selected limit, quotient equality, choice schedule, or completeness theorem.
\end{proof}

\closureat{RealOscillationMajorantUp}{seedStr}

\begin{closurestatus}{\RealOscillationMajorantUp}
  \constructivestory{A $\RealOscillationMajorantUp$ packet is generated from a RealUp seal row, RegSeqRat readback, StreamName finite windows, DyadicRatCore tolerance arithmetic, a RealModulusOfConvergence schedule, a finite oscillation-majorant ledger, witness windows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-oscillation-majorant-carrier}, \autoref{thm:real-oscillation-majorant-namecert-obligations}, and \autoref{thm:real-oscillation-majorant-l10-readback} seed the finite oscillation-majorant route for Real, RegSeqRat, StreamName, DyadicRatCore, and convergence-modulus consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host suprema, selected Cauchy limits, quotient stream equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, tolerance arithmetic, finite-window readback, convergence-modulus scheduling, majorant-ledger exactness, witness-window coverage, structural replay, and non-escape for BEDC.Derived.RealOscillationMajorantUp.}
\end{closurestatus}
