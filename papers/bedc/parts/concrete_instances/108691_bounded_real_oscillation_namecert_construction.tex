\chapter{A Concrete Naming Certificate for $\BoundedRealOscillationUp$}
\label{ch:concrete-instances-bounded-real-oscillation-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BoundedRealOscillationUp$ packet records the finite route by which a continuous real-valued map on a compact metric source receives an oscillation bound from displayed uniform-modulus and finite-partition data. It sits between the compact source surface of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus route of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the finite oscillation partition of \autoref{ch:concrete-instances-finite-oscillation-partition-namecert}, and the $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. The packet names an oscillation-bound witness without importing open-cover compactness, supremum selection, quotient real equality, or choice.

\begin{definition}[BoundedRealOscillation carrier]
\label{def:bounded-real-oscillation-carrier}
A $\BoundedRealOscillationUp$ carrier is a finite $\BHist$ packet
$$
O=(K,F,U,A,B,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, exposing only the inspected compact-metric windows and finite metric ledgers.
\item $F$ is the $\ContinuousMapUp$ row from $K$ to $\RealUp$, with its graph, local continuity data, and real-value readbacks.
\item $U$ is the $\UniformModulusUp$ row translating each requested oscillation tolerance into a displayed metric radius.
\item $A$ is the $\FiniteOscillationPartitionUp$ row, listing the finite cells inspected at that tolerance and the local oscillation ledgers attached to them.
\item $B$ is the bounded-oscillation ledger, recording the displayed real bound certified from the cellwise oscillation rows and the uniform modulus.
\item $R$ is the terminal $\RealUp$ seal for the bound value, reached only after $K,F,U,A,B$ have been read.
\item $H$ records componentwise $\hsame$ transport for the compact source, continuous map, uniform modulus, finite partition, bound ledger, real seal, continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an oscillation request through $K,F,U,A,B,R$ and back to the public certificate boundary.
\item $P$ is the $\Pkg$ provenance over $\BoundedRealOscillationUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\FiniteOscillationPartitionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BoundedRealOscillationUp}$ row.
\end{enumerate}
The classifier accepts exactly packets with these ten rows. It has no coordinate for open-cover compactness, a selected supremum, host function equality, quotient real equality, countable choice, or an ambient completeness principle detached from the displayed $\RealUp$ seal.
\end{definition}

\begin{theorem}[BoundedRealOscillation NameCert obligations]
\label{thm:bounded-real-oscillation-namecert-obligations}
For every accepted $\BoundedRealOscillationUp$ carrier
$$
O=(K,F,U,A,B,R,H,C,P,N),
$$
the local $\NameCert_{\BoundedRealOscillationUp}$ surface consists exactly of carrier admission, compact-source readback, continuous-map readback, uniform-modulus tolerance routing, finite oscillation-partition coverage, bounded-ledger exactness, $\RealUp$ seal nonescape, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and terminal local naming. No oscillation-bound obligation is stored outside those rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bounded-real-oscillation-carrier}. The rows $K,F,U,A,B,R,H,C,P,N$ are the only local coordinates, so carrier admission and local naming are exactly the displayed finite packet.

Read the analytic rows in order. The compact source row $K$ supplies only finite metric windows, the continuous-map row $F$ supplies the real-valued graph readbacks on those windows, and the uniform-modulus row $U$ translates an oscillation tolerance into the metric radius used by the partition row.

The finite partition row $A$ then exposes the cellwise oscillation checks, and the bounded ledger $B$ records the real bound obtained from those checks. The seal row $R$ is downstream of this route, so a real-facing consumer cannot read a bound before the compact, continuous, modulus, partition, and ledger rows have been displayed.

Finally $H$ and $C$ transport and replay the same route componentwise, while $P$ records provenance and $N$ names the packet. Since the carrier has no excluded host-level coordinate, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[BoundedRealOscillation compact-uniform route]
\label{thm:bounded-real-oscillation-compact-uniform-route}
Every compact-uniform-continuity or extreme-value consumer of an accepted $\BoundedRealOscillationUp$ packet receives the oscillation bound only through the route
$$
\CompactMetricUp,\ \ContinuousMapUp,\ \UniformModulusUp,\ \FiniteOscillationPartitionUp,\ \RealUp,\ H,C,P,N .
$$
The route is finite: a requested tolerance opens the uniform modulus, the modulus selects the displayed metric radius, the finite oscillation partition checks the resulting cells, and the bound ledger seals the resulting $\RealUp$ value. It exports no open-cover compactness theorem, no selected supremum, no quotient real equality, and no choice-selected covering family.
\end{theorem}
\begin{proof}
Start from the uniform-modulus row. A tolerance request is answered by $U$, and that answer is tied to the compact source $K$ and continuous map $F$ before any finite partition row is read.

Next use the finite oscillation partition row $A$. It inspects only the displayed cells determined by the compact-metric windows and the modulus radius, so the oscillation estimate is a finite ledger calculation rather than an open-cover argument.

The bound ledger $B$ and Real seal $R$ then expose the bounded value to downstream consumers. Since $B$ is downstream of $K,F,U,A$ and $R$ is downstream of $B$, the consumer receives exactly the compact-uniform route displayed in the statement.

The transport, replay, provenance, and naming rows preserve that order. Therefore the packet can be consumed by extreme-value, uniform-limit, and compact-uniform-continuity chapters without supplying any open-cover, supremum-selection, quotient-equality, or choice coordinate.
\end{proof}

\begin{theorem}[BoundedRealOscillation Real-seal nonescape]
\label{thm:bounded-real-oscillation-real-seal-nonescape}
For every accepted $\BoundedRealOscillationUp$ carrier $O=(K,F,U,A,B,R,H,C,P,N)$, every $\RealUp$-facing read of the oscillation bound factors through $K,F,U,A,B$ before $R$ is visible, and through $H,C,P,N$ before the local certificate is exported. Thus the real seal cannot be used as a bypass around compact-source readback, continuous-map readback, uniform-modulus routing, finite-partition coverage, or the bounded-oscillation ledger.
\end{theorem}
\begin{proof}
Read the seal coordinate in \autoref{def:bounded-real-oscillation-carrier}. The row $R$ is terminal: it records a real bound only after the compact source, continuous map, uniform modulus, finite partition, and bounded ledger have been displayed.

Apply \autoref{thm:bounded-real-oscillation-namecert-obligations}. The NameCert surface requires the same rows for carrier admission, tolerance routing, partition coverage, bounded-ledger exactness, and nonescape, so a Real-facing consumer has no direct path to $R$.

Finally use the structural rows. The $\hsame$ transport row $H$ and the $\Cont$ replay row $C$ preserve the route componentwise, while $P$ and $N$ record provenance and local naming. Hence the real seal remains bound to the displayed finite packet and cannot bypass any of its compact-uniform rows.
\end{proof}

\falsifiablePrediction{Every accepted $\BoundedRealOscillationUp$ consumer must expose a compact metric source row, continuous real-valued map row, uniform modulus row, finite oscillation-partition row, bounded ledger, $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row before an oscillation bound is available.}

\independenceWitness{compactmetric, continuousmap, uniformmodulus, finiteoscillationpartition, real: $\BoundedRealOscillationUp$ is not bijective to any listed sibling because it jointly carries the compact source, continuous map, uniform modulus, finite oscillation partition, bounded ledger, real seal, transport, replay, provenance, and local naming rows in one finite packet.}

\closureat{BoundedRealOscillationUp}{seedStr}

\begin{closurestatus}{\BoundedRealOscillationUp}
  \constructivestory{A $\BoundedRealOscillationUp$ packet is generated from a CompactMetric source row, a ContinuousMap real-valued graph row, a UniformModulus tolerance route, a FiniteOscillationPartition cell ledger, a bounded-oscillation RealUp ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bounded-real-oscillation-carrier}, \autoref{thm:bounded-real-oscillation-namecert-obligations}, \autoref{thm:bounded-real-oscillation-compact-uniform-route}, and \autoref{thm:bounded-real-oscillation-real-seal-nonescape} seed the finite oscillation-bound route over $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\FiniteOscillationPartitionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, open-cover compactness, selected suprema, quotient real equality, countable choice, or ambient completeness outside the displayed real seal.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source readback, continuous-map readback, uniform-modulus routing, finite-partition coverage, bounded-ledger exactness, real-seal nonescape, and TasteGate field faithfulness for BEDC.Derived.BoundedRealOscillationUp.}
\end{closurestatus}
