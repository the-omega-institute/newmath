\begin{definition}[Sum ledger policy]
\label{def:sum-ledger-policy}
Record a raw sum history carrying the selected injection tag and source ledger
together with an $\hsame$ link to the visible history exposed by the ledger.
\end{definition}
\leandef{BEDC.Derived.SumUp.SumHistoryLedgerPolicy}

\begin{theorem}[Sum ledger visible carrier]
\label{thm:sum-ledger-visible-carrier}
If a visible history is linked by $\hsame$ to a raw sum-ledger history, then
the visible history is itself a sum carrier.
\end{theorem}
\begin{proof}
Unpack the raw sum carrier and transport it across the stored $\hsame$ link
by sum-carrier transport.
\end{proof}
\leanchecked{BEDC.Derived.SumUp.SumHistoryLedgerPolicy\_visible\_carrier}

\begin{theorem}[Sum ledger raw-visible classifier]
\label{thm:sum-ledger-raw-visible-classifier}
If $\mathsf{SumHistoryLedgerPolicy}(L,R,\rho,v)$ holds, then
$\mathsf{SumHistoryClassifier}(L,R,\hsame,\hsame,\rho,v)$ holds.
\end{theorem}
\begin{proof}
Unpack the ledger into its raw sum carrier and the stored $\hsame(\rho,v)$
link. The raw carrier determines whether $\rho$ is left-tagged or right-tagged;
transport the tag witness across the stored link and use reflexivity of
$\hsame$ on the selected component to build the matching classifier branch.
\end{proof}
\leanchecked{BEDC.Derived.SumUp.SumHistoryLedgerPolicy\_raw\_visible\_classifier}

\begin{theorem}[Sum ledger history-same transport]
\label{thm:sum-ledger-history-same-transport}
Let $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$ be source carriers and let
$raw,raw',visible,visible':\mathsf{BHist}$ be histories. If
$\mathsf{SumHistoryLedgerPolicy}(Left,Right,raw,visible)$,
$\hsame(raw,raw')$, and $\hsame(visible,visible')$ hold, then
$$
  \mathsf{SumHistoryLedgerPolicy}(Left,Right,raw',visible')
$$
holds.
\end{theorem}
\begin{proof}
Unpack the ledger into the raw $\mathsf{SumHistoryCarrier}$ witness and the
stored link $\hsame(raw,visible)$. Transport the raw carrier along
$\hsame(raw,raw')$ by
Theorem~\ref{thm:sum-history-carrier-hsame-transport}. For the raw-visible
link of the transported ledger, compose symmetry of $\hsame(raw,raw')$, the
stored $\hsame(raw,visible)$ link, and $\hsame(visible,visible')$ to obtain
$\hsame(raw',visible')$.
\end{proof}
\leanchecked{BEDC.Derived.SumUp.SumHistoryLedgerPolicy\_hsame\_transport}

\begin{theorem}[Sum ledger classifier composition]
\label{thm:sum-ledger-classifier-extension}
Let $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$ be source carriers and let
$\rho,v,w:\mathsf{BHist}$ be histories. If
$\mathsf{SumHistoryLedgerPolicy}(Left,Right,\rho,v)$ holds and
$\mathsf{SumHistoryClassifier}(Left,Right,\hsame,\hsame,v,w)$ holds, then
$$
\mathsf{SumHistoryClassifier}(Left,Right,\hsame,\hsame,\rho,w)
$$
holds.
\end{theorem}
\begin{proof}
First apply the raw-visible classifier theorem to the ledger policy, obtaining
$$
\mathsf{SumHistoryClassifier}(Left,Right,\hsame,\hsame,\rho,v).
$$
Then compose this classifier witness with the given visible-target classifier
witness using sum-history classifier transitivity. The branch classifiers are
both $\hsame$, so their required transitivity inputs are the left and right
instances of BEDC history equality transitivity.
\end{proof}
\leanchecked{BEDC.Derived.SumUp.SumHistoryLedgerPolicy\_classifier\_composition}

\begin{theorem}[Sum ledger two-step classifier composition]
\label{thm:sum-ledger-two-step-classifier-composition}
Let $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$ be source carriers and let
$\rho,v,w:\mathsf{BHist}$ be histories. If
$\mathsf{SumHistoryLedgerPolicy}(Left,Right,\rho,v)$ and
$\mathsf{SumHistoryLedgerPolicy}(Left,Right,v,w)$ hold, then
$$
\mathsf{SumHistoryClassifier}(Left,Right,\hsame,\hsame,\rho,w)
$$
holds. The roadmap fit is: traditional target disjoint-sum ledger-chain
closure; BEDC source $\mathsf{BHist}$ with tagged sum carrier; carrier
$\mathsf{SumHistoryCarrier}$; classifier
$\mathsf{SumHistoryClassifier}(Left,Right,\hsame,\hsame)$; stability by two
raw-visible ledger exposures and classifier transitivity; ledger two
composable $\mathsf{SumHistoryLedgerPolicy}$ witnesses; core theorem this
closure; exactness raw-to-middle-to-visible; standard bridge tagged sum ledger
chain; status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:sum-ledger-raw-visible-classifier} to the two ledger
witnesses, obtaining classifiers from $\rho$ to $v$ and from $v$ to $w$.
Compose them by Theorem~\ref{thm:sum-history-classifier-transitivity}; the
branch classifiers are both $\hsame$, so the required transitivity inputs are
the left and right instances of BEDC history equality transitivity.
\end{proof}
\leanchecked{BEDC.Derived.SumUp.SumHistoryLedgerPolicy\_two\_step\_classifier\_composition}

\begin{theorem}[Sum ledger raw-visible branch exactness]
\label{thm:sum-ledger-raw-visible-branch-exactness}
Let $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$ be source carriers and let
$raw,visible:\mathsf{BHist}$ be histories. Then
$$
\mathsf{SumHistoryLedgerPolicy}(Left,Right,raw,visible)
\Longleftrightarrow
\bigl(\exists l:\mathsf{BHist}.\; Left(l)\land\hsame(raw,\Ezero(l))\land\hsame(visible,\Ezero(l))\bigr)
\lor
\bigl(\exists r:\mathsf{BHist}.\; Right(r)\land\hsame(raw,\Eone(r))\land\hsame(visible,\Eone(r))\bigr).
$$
\end{theorem}
\begin{proof}
For the forward direction, unpack the ledger into its raw sum carrier and the
stored raw-visible link. Split the raw carrier branch.

In the left branch, the carrier supplies $Left(l)$ and
$\hsame(raw,\Ezero(l))$. Compose symmetry of the stored raw-visible link with
this raw endpoint witness to obtain $\hsame(visible,\Ezero(l))$, and assemble the
left disjunct.

In the right branch, the same argument uses the witness $Right(r)$ and
$\hsame(raw,\Eone(r))$ to obtain $\hsame(visible,\Eone(r))$ and assemble the right
disjunct.

For the reverse direction, split the displayed branch evidence. In the left
branch, rebuild the raw sum carrier from $Left(l)$ and
$\hsame(raw,\Ezero(l))$. Compose $\hsame(raw,\Ezero(l))$ with symmetry of
$\hsame(visible,\Ezero(l))$ to recover the raw-visible ledger link.

The right branch is identical, rebuilding the raw carrier from $Right(r)$ and
$\hsame(raw,\Eone(r))$, then composing the two endpoint witnesses to recover the
stored raw-visible link.
\end{proof}
\leanchecked{BEDC.Derived.SumUp.SumHistoryLedgerPolicy\_raw\_visible\_branch\_exactness}

\begin{theorem}[Sum ledger two-step branch exactness]
\label{thm:sum-ledger-two-step-branch-exactness}
Let $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$ be source carriers and let
$\rho,v,w:\mathsf{BHist}$ be histories. If
$\mathsf{SumHistoryLedgerPolicy}(Left,Right,\rho,v)$ and
$\mathsf{SumHistoryLedgerPolicy}(Left,Right,v,w)$ hold, then the whole
two-step chain has a visible same-branch payload readback:
$$
\bigl(\exists l\,l':\mathsf{BHist}.\; Left(l)\land Left(l')\land\hsame(\rho,\Ezero(l))\land\hsame(w,\Ezero(l'))\land\hsame(l,l')\bigr)
\lor
\bigl(\exists r\,r':\mathsf{BHist}.\; Right(r)\land Right(r')\land\hsame(\rho,\Eone(r))\land\hsame(w,\Eone(r'))\land\hsame(r,r')\bigr).
$$
The ten-field fit is: traditional target disjoint-sum ledger-chain readback;
BEDC source $\mathsf{BHist}$ with $\hsame$; carrier
$\mathsf{SumHistoryCarrier}$; classifier unused in the final exactness statement
but supplied by Theorem~\ref{thm:sum-ledger-two-step-classifier-composition};
ledger two composable $\mathsf{SumHistoryLedgerPolicy}$ witnesses; core theorem
this branch exactness statement; exactness raw-to-visible same-branch payload
readback; standard bridge tagged sum ledger chains; status
$\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:sum-ledger-raw-visible-branch-exactness} to each ledger
witness. The first application exposes either a left payload visible at
$\rho$ and $v$, or a right payload visible at $\rho$ and $v$; the second exposes
the corresponding alternatives at $v$ and $w$. Split the four branch cases. In
the left-left case, use Theorem~\ref{thm:sum-visible-branch-payload-determinism}
at the shared middle endpoint $v$ to identify the two left payloads and assemble
the left disjunct. The right-right case is identical with the right branch.
The mixed left-right and right-left cases contradict generated-history
no-confusion between $\Ezero$ and $\Eone$ at the shared middle endpoint $v$.
\end{proof}
\leanchecked{BEDC.Derived.SumUp.SumHistoryLedgerPolicy\_two\_step\_branch\_exactness}

\begin{theorem}[Sum ledger two-step endpoint exactness]
\label{thm:sum-ledger-two-step-endpoint-exactness}
Let $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$ be source carriers and let
$\rho,v,w,l,l',r,r':\mathsf{BHist}$ be histories. If
$\mathsf{SumHistoryLedgerPolicy}(Left,Right,\rho,v)$ and
$\mathsf{SumHistoryLedgerPolicy}(Left,Right,v,w)$ hold, then the two-ledger
chain has endpoint exactness from $\rho$ to $w$. First, both mixed endpoint
pairs are impossible:
$$
(\hsame(\rho,\Ezero(l))\land \hsame(w,\Eone(r')))\to\bot
$$
and
$$
(\hsame(\rho,\Eone(r))\land \hsame(w,\Ezero(l')))\to\bot.
$$
Second, same-tag endpoint pairs determine their payloads:
$$
(\hsame(\rho,\Ezero(l))\land\hsame(w,\Ezero(l')))\to\hsame(l,l')
$$
and
$$
(\hsame(\rho,\Eone(r))\land\hsame(w,\Eone(r')))\to\hsame(r,r').
$$
Third, final visible endpoints of $w$ read back source payload evidence:
$$
(\hsame(w,\Ezero(l'))\to\exists a:\mathsf{BHist}.\;Left(a)\land\hsame(l',a))
$$
and
$$
(\hsame(w,\Eone(r'))\to\exists b:\mathsf{BHist}.\;Right(b)\land\hsame(r',b)).
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:sum-ledger-two-step-classifier-composition} to the two
ledger witnesses and obtain
$\mathsf{SumHistoryClassifier}(Left,Right,\hsame,\hsame,\rho,w)$. The two mixed
endpoint conclusions follow from
Theorem~\ref{thm:sum-history-classifier-tag-separation}; for the reversed
displayed order, use classifier symmetry from
Theorem~\ref{thm:sum-history-classifier-hsame-symmetry} before applying the same
tag-separation theorem. The left same-tag conclusion is exactly the left
endpoint inversion supplied by
Theorem~\ref{thm:sum-history-classifier-left-hsame-inversion}, and the right
same-tag conclusion is the corresponding right inversion from
Theorem~\ref{thm:sum-history-classifier-right-hsame-inversion}. For the final
visible readback, apply Theorem~\ref{thm:sum-ledger-visible-carrier} to the
second ledger witness, obtaining a visible carrier for $w$; then apply
Theorem~\ref{thm:sum-history-carrier-visible-tag-exactness} to the displayed
$\Ezero$ or $\Eone$ endpoint of $w$ to extract the matching source payload
evidence.
\end{proof}
\leanchecked{BEDC.Derived.SumUp.SumHistoryLedgerPolicy\_two\_step\_endpoint\_exactness}

\begin{definition}[Sum ledger finite provenance chain]
\label{def:sum-history-ledger-chain}
For source carriers $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$, define
$\mathsf{SumHistoryLedgerChain}_{Left,Right}(\rho,z)$ as the finite provenance
relation generated by the following two clauses. A displayed one-ledger witness
$\mathsf{SumHistoryLedgerPolicy}(Left,Right,\rho,z)$ gives
$\mathsf{SumHistoryLedgerChain}_{Left,Right}(\rho,z)$. If there is a history
$v:\mathsf{BHist}$ such that
$\mathsf{SumHistoryLedgerPolicy}(Left,Right,\rho,v)$ and
$\mathsf{SumHistoryLedgerChain}_{Left,Right}(v,z)$ hold, then
$\mathsf{SumHistoryLedgerChain}_{Left,Right}(\rho,z)$ also holds.
\end{definition}
\leandef{BEDC.Derived.SumUp.SumHistoryLedgerChain}

\begin{theorem}[Sum ledger chain classifier composition]
\label{thm:sum-ledger-chain-classifier-composition}
\label{thm:sum-history-ledger-chain-classifier-composition}
Let $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$ be source carriers and let
$raw,visible:\mathsf{BHist}$ be histories. If
$\mathsf{SumHistoryLedgerChain}_{Left,Right}(raw,visible)$ holds, then
$$
\mathsf{SumHistoryClassifier}(Left,Right,\hsame,\hsame,raw,visible)
$$
holds. The ten-field fit is: traditional target disjoint-sum finite ledger
classifier closure; BEDC source $\mathsf{BHist}$ with $\hsame$; source carriers
$Left$ and $Right$; carrier $\mathsf{SumHistoryCarrier}$; classifier
$\mathsf{SumHistoryClassifier}(Left,Right,\hsame,\hsame)$; stability by
raw-visible classification plus classifier transitivity; ledger
$\mathsf{SumHistoryLedgerChain}_{Left,Right}$; core theorem this finite-chain
classifier closure; exactness finite raw-to-visible classifier reachability;
standard bridge tagged sum finite ledger provenance; status
$\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Induct on the displayed
$\mathsf{SumHistoryLedgerChain}_{Left,Right}(raw,visible)$ witness. In the
one-ledger case, apply
Theorem~\ref{thm:sum-ledger-raw-visible-classifier}. In the cons case, apply
the induction hypothesis to the tail chain to obtain the classifier from the
middle history to $visible$, then compose it with the first ledger by
Theorem~\ref{thm:sum-ledger-classifier-extension}.
\end{proof}
\leanchecked{BEDC.Derived.SumUp.SumHistoryLedgerChain\_classifier\_composition}

\begin{theorem}[Sum ledger chain visible carrier]
\label{thm:sum-ledger-chain-visible-carrier}
Let $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$ be source carriers and let
$\rho,z:\mathsf{BHist}$ be histories. If
$\mathsf{SumHistoryLedgerChain}_{Left,Right}(\rho,z)$ holds, then
$$
\mathsf{SumHistoryCarrier}(Left,Right,z)
$$
holds. The ten-field fit is: traditional target disjoint-sum finite ledger
visible-carrier closure; BEDC source $\mathsf{BHist}$ with $\hsame$; source
carriers $Left$ and $Right$; carrier $\mathsf{SumHistoryCarrier}$; classifier
not needed for this unary closure; ledger
$\mathsf{SumHistoryLedgerChain}_{Left,Right}$; core theorem this carrier
closure; exactness final visible endpoint remains in the sum carrier; standard
bridge tagged sum finite ledger provenance; status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Induct on the displayed
$\mathsf{SumHistoryLedgerChain}_{Left,Right}(\rho,z)$ witness. In the
one-ledger case, apply Theorem~\ref{thm:sum-ledger-visible-carrier}. In the
cons case, use the carrier supplied by the tail induction hypothesis.
\end{proof}

\begin{theorem}[Sum ledger chain endpoint exactness]
\label{thm:sum-ledger-chain-endpoint-exactness}
Let $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$ be source carriers and let
$\rho,z,l,l',r,r':\mathsf{BHist}$ be histories. If
$\mathsf{SumHistoryLedgerChain}_{Left,Right}(\rho,z)$ holds, then the whole
finite ledger provenance from $\rho$ to $z$ has endpoint exactness. First, both
mixed endpoint pairs are impossible:
$$
(\hsame(\rho,\Ezero(l))\land\hsame(z,\Eone(r')))\to\bot
$$
and
$$
(\hsame(\rho,\Eone(r))\land\hsame(z,\Ezero(l')))\to\bot.
$$
Second, same-tag endpoint pairs determine their payloads:
$$
(\hsame(\rho,\Ezero(l))\land\hsame(z,\Ezero(l')))\to\hsame(l,l')
$$
and
$$
(\hsame(\rho,\Eone(r))\land\hsame(z,\Eone(r')))\to\hsame(r,r').
$$
Third, final visible endpoints of $z$ read back matching source payload
evidence:
$$
(\hsame(z,\Ezero(l'))\to\exists a:\mathsf{BHist}.\;Left(a)\land\hsame(l',a))
$$
and
$$
(\hsame(z,\Eone(r'))\to\exists b:\mathsf{BHist}.\;Right(b)\land\hsame(r',b)).
$$
The ten-field fit is: traditional target disjoint-sum finite ledger provenance;
BEDC source $\mathsf{BHist}$ with $\hsame$; source carriers $Left$ and $Right$;
carrier $\mathsf{SumHistoryCarrier}$; classifier
$\mathsf{SumHistoryClassifier}(Left,Right,\hsame,\hsame)$ supplied one ledger at
a time by raw-visible classification; ledger
$\mathsf{SumHistoryLedgerChain}_{Left,Right}$ generated by
$\mathsf{SumHistoryLedgerPolicy}$; core theorem this chain endpoint exactness
package; exactness mixed-tag separation, same-tag payload determinacy, and final
visible source readback; standard bridge tagged sum finite ledger provenance;
status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
First apply Theorem~\ref{thm:sum-ledger-chain-classifier-composition} to the
displayed chain, obtaining
$\mathsf{SumHistoryClassifier}(Left,Right,\hsame,\hsame,\rho,z)$. The two mixed
endpoint clauses follow from
Theorem~\ref{thm:sum-history-classifier-tag-separation}; for the reversed
displayed order, first use classifier symmetry from
Theorem~\ref{thm:sum-history-classifier-hsame-symmetry}. The same-tag endpoint
clauses follow by transporting the classifier along the displayed endpoint
$\hsame$ links and applying
Theorem~\ref{thm:sum-history-classifier-left-hsame-inversion} or
Theorem~\ref{thm:sum-history-classifier-right-hsame-inversion}. For the final
visible readback, apply
Theorem~\ref{thm:sum-ledger-chain-visible-carrier} to the displayed chain to
obtain $\mathsf{SumHistoryCarrier}(Left,Right,z)$. Transport this carrier along
the displayed $\hsame$ endpoint at $z$ and apply the left or right carrier
inversion theorem to extract the matching source payload evidence.
\end{proof}
\leanchecked{BEDC.Derived.SumUp.SumHistoryLedgerChain\_endpoint\_exactness}

\begin{theorem}[Sum ledger chain branch readback soundness]
\label{thm:sum-ledger-chain-branch-readback-soundness}
Let $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$ be source carriers and let
$\rho,z:\mathsf{BHist}$ be histories. The finite ledger-chain relation forces
exact same-branch payload readback:
$$
\mathsf{SumHistoryLedgerChain}_{Left,Right}(\rho,z)
\to
\bigl(\exists l\,l':\mathsf{BHist}.\; Left(l)\land Left(l')\land\hsame(\rho,\Ezero(l))\land\hsame(z,\Ezero(l'))\land\hsame(l,l')\bigr)
\lor
\bigl(\exists r\,r':\mathsf{BHist}.\; Right(r)\land Right(r')\land\hsame(\rho,\Eone(r))\land\hsame(z,\Eone(r'))\land\hsame(r,r')\bigr).
$$
The ten-field fit is: traditional target disjoint-sum finite ledger branch
readback; BEDC source $\mathsf{BHist}$ with $\hsame$; source carriers $Left$
and $Right$; carrier $\mathsf{SumHistoryCarrier}$; classifier supplied by
Theorem~\ref{thm:sum-ledger-chain-classifier-composition}; ledger
$\mathsf{SumHistoryLedgerChain}_{Left,Right}$; core theorem this branch
readback soundness; finite ledger-chain witnesses map into same-branch payload
readback; standard bridge tagged sum finite ledger provenance; status
$\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Induct on the displayed $\mathsf{SumHistoryLedgerChain}_{Left,Right}(\rho,z)$
witness. In the one-ledger case, apply
Theorem~\ref{thm:sum-ledger-raw-visible-branch-exactness} and choose the same
payload at the raw and visible endpoints, with $\hsame$ reflexivity for the
payload link.

In the cons case, combine the raw-visible branch exactness of the first ledger
with the induction hypothesis for the tail chain. The mixed left-right and
right-left cases contradict generated-history no-confusion between $\Ezero$ and
$\Eone$ at the shared middle endpoint. In the same-branch cases, use
Theorem~\ref{thm:sum-visible-branch-payload-determinism} at the middle endpoint
and $\hsame$ transitivity to compose the first payload with the final payload.
\end{proof}
\leanchecked{BEDC.Derived.SumUp.SumHistoryLedgerChain\_branch\_exactness}

\begin{theorem}[Sum ledger chain branch exactness]
\label{thm:sum-ledger-chain-branch-exactness}
Let $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$ be source carriers and let
$\rho,z:\mathsf{BHist}$ be histories. The finite ledger-chain relation is
equivalent to exact same-branch payload readback:
$$
\mathsf{SumHistoryLedgerChain}_{Left,Right}(\rho,z)
\Longleftrightarrow
\bigl(\exists l\,l':\mathsf{BHist}.\; Left(l)\land Left(l')\land\hsame(\rho,\Ezero(l))\land\hsame(z,\Ezero(l'))\land\hsame(l,l')\bigr)
\lor
\bigl(\exists r\,r':\mathsf{BHist}.\; Right(r)\land Right(r')\land\hsame(\rho,\Eone(r))\land\hsame(z,\Eone(r'))\land\hsame(r,r')\bigr).
$$
The ten-field fit is: traditional target disjoint-sum finite ledger branch
readback; BEDC source $\mathsf{BHist}$ with $\hsame$; source carriers $Left$
and $Right$; carrier $\mathsf{SumHistoryCarrier}$; classifier supplied by
Theorem~\ref{thm:sum-ledger-chain-classifier-composition}; ledger
$\mathsf{SumHistoryLedgerChain}_{Left,Right}$; core theorem this branch
exactness; exactness coverage and completeness of the finite ledger-chain
relation by same-branch payload readback; standard bridge tagged sum finite
ledger provenance; status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
For the forward direction, apply
Theorem~\ref{thm:sum-ledger-chain-branch-readback-soundness}.

For the reverse direction, split the displayed disjunction. In the left branch,
the witnesses $Left(l)$, $\hsame(\rho,\Ezero(l))$, $\hsame(z,\Ezero(l'))$, and
$\hsame(l,l')$ determine a one-ledger left readback. Use symmetry of
$\hsame(l,l')$ and constructor congruence for $\Ezero$ to transport
$\hsame(z,\Ezero(l'))$ into $\hsame(z,\Ezero(l))$.

Apply the reverse direction of
Theorem~\ref{thm:sum-ledger-raw-visible-branch-exactness} to obtain
$\mathsf{SumHistoryLedgerPolicy}(Left,Right,\rho,z)$, then use the one-ledger
constructor of $\mathsf{SumHistoryLedgerChain}_{Left,Right}$.

The right branch is identical, using the witnesses $Right(r)$,
$\hsame(\rho,\Eone(r))$, $\hsame(z,\Eone(r'))$, and $\hsame(r,r')$, with
constructor congruence for $\Eone$ before applying the same raw-visible branch
exactness theorem and the one-ledger chain constructor.
\end{proof}

\begin{theorem}[Sum ledger chain raw branch forces final branch readback]
\label{thm:sum-ledger-chain-raw-branch-forces-final-readback}
Let $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$ be source carriers and let
$\rho,z,l,r:\mathsf{BHist}$ be histories. If
$\mathsf{SumHistoryLedgerChain}_{Left,Right}(\rho,z)$ holds, then raw visible
branch evidence forces the matching final visible branch. The left clause is
$$
\begin{array}{l}
Left(l)\to \hsame(\rho,\Ezero(l))\to
\exists l':\mathsf{BHist}.\; Left(l')\land\hsame(z,\Ezero(l'))\land
\hsame(l,l')\land
\neg(\exists r':\mathsf{BHist}.\; Right(r')\land\hsame(z,\Eone(r')))
\end{array}
$$
and the right clause is
$$
\begin{array}{l}
Right(r)\to \hsame(\rho,\Eone(r))\to
\exists r':\mathsf{BHist}.\; Right(r')\land\hsame(z,\Eone(r'))\land
\hsame(r,r')\land
\neg(\exists l':\mathsf{BHist}.\; Left(l')\land\hsame(z,\Ezero(l'))).
\end{array}
$$
The ten-field fit is: traditional target disjoint-sum finite ledger branch
forcing; BEDC source $\mathsf{BHist}$ with $\hsame$; carrier final
$\mathsf{SumHistoryCarrier}$; classifier supplied by the chain classifier
composition theorem; ledger $\mathsf{SumHistoryLedgerChain}_{Left,Right}$; core
theorem this raw-branch forcing readback; exactness same-tag payload determinacy
plus mixed-tag exclusion; standard bridge tagged sum finite ledger provenance;
status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:sum-ledger-chain-visible-carrier} to the chain and then
Theorem~\ref{thm:sum-history-carrier-single-valued-visible-readback} to the
final carrier. This splits the endpoint $z$ into a left visible presentation
with right-presentation exclusion, or a right visible presentation with
left-presentation exclusion.

For the left raw premise, keep the left final presentation from the split. The
same-tag payload clause of
Theorem~\ref{thm:sum-ledger-chain-endpoint-exactness}, applied to
$\hsame(\rho,\Ezero(l))$ and the displayed $\hsame(z,\Ezero(l'))$, gives
$\hsame(l,l')$; combine it with the source witness for $l'$ and the carried
right-presentation exclusion.

The right final presentation cannot occur under the left raw premise, because
the mixed endpoint clause of
Theorem~\ref{thm:sum-ledger-chain-endpoint-exactness} contradicts
$\hsame(\rho,\Ezero(l))$ and $\hsame(z,\Eone(r'))$. The right raw premise is
symmetric: the right final presentation supplies the witness and the
left-presentation exclusion, while the mixed endpoint clause excludes any left
final presentation.
\end{proof}
\leanchecked{BEDC.Derived.SumUp.SumHistoryLedgerChain\_raw\_branch\_final\_readback}

\begin{theorem}[Sum ledger preserves visible tag separation]
\label{thm:sum-ledger-visible-tag-separation}
Let $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$ be source carriers and let
$\rho,v,l,r:\mathsf{BHist}$ be histories. If
$\mathsf{SumHistoryLedgerPolicy}(Left,Right,\rho,v)$ holds, then both visible
mixed-tag endpoint pairs are impossible:
$$
(\hsame(\rho,\Ezero(l))\land \hsame(v,\Eone(r)))\to\bot
$$
and
$$
(\hsame(\rho,\Eone(r))\land \hsame(v,\Ezero(l)))\to\bot.
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:sum-ledger-raw-visible-classifier} to the ledger policy
to obtain
$\mathsf{SumHistoryClassifier}(Left,Right,\hsame,\hsame,\rho,v)$. The
left-to-right mixed endpoint pair is then discharged by
$\mathsf{SumHistoryClassifier\_mixed\_tags\_absurd}$ with the displayed
$\hsame(\rho,\Ezero(l))$ and $\hsame(v,\Eone(r))$ witnesses. For the symmetric
right-to-left pair, use classifier symmetry from
Theorem~\ref{thm:sum-history-classifier-hsame-symmetry} and apply the same mixed-tag
separation theorem to the exchanged endpoints.
\end{proof}
\leanchecked{BEDC.Derived.SumUp.SumHistoryLedgerPolicy\_visible\_tag\_separation}

\begin{theorem}[Sum ledger same-tag payload exactness]
\label{thm:sum-ledger-same-tag-payload-exactness}
Let $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$ be source carriers and let
$\rho,v,l,l',r,r':\mathsf{BHist}$ be histories. If
$\mathsf{SumHistoryLedgerPolicy}(Left,Right,\rho,v)$ holds, then the visible
same-tag endpoint pairs determine their payloads:
$$
(\hsame(\rho,\Ezero(l))\land\hsame(v,\Ezero(l')))\to\hsame(l,l')
$$
and
$$
(\hsame(\rho,\Eone(r))\land\hsame(v,\Eone(r')))\to\hsame(r,r').
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:sum-ledger-raw-visible-classifier} to obtain the
raw-visible $\mathsf{SumHistoryClassifier}(Left,Right,\hsame,\hsame,\rho,v)$
witness. For the left-tagged endpoint pair, transport this witness along the
displayed $\hsame(\rho,\Ezero(l))$ and $\hsame(v,\Ezero(l'))$ links by
Theorem~\ref{thm:sum-history-classifier-hsame-transport}, then apply the
left inversion Theorem~\ref{thm:sum-history-classifier-left-hsame-inversion}
to extract $\hsame(l,l')$. The right-tagged case is identical with
Theorem~\ref{thm:sum-history-classifier-right-hsame-inversion}.
\end{proof}
\leanchecked{BEDC.Derived.SumUp.SumHistoryLedgerPolicy\_same\_tag\_payload\_exactness}

\begin{theorem}[Sum ledger visible payload readback]
\label{thm:sum-ledger-visible-payload-readback}
Let $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$ be source carriers and let
$\rho,v,l,r:\mathsf{BHist}$ be histories. If
$\mathsf{SumHistoryLedgerPolicy}(Left,Right,\rho,v)$ holds, then visible tagged
endpoints read back source payload evidence:
$$
(\hsame(v,\Ezero(l))\to\exists a:\mathsf{BHist}.\;Left(a)\land\hsame(l,a))
$$
and
$$
(\hsame(v,\Eone(r))\to\exists b:\mathsf{BHist}.\;Right(b)\land\hsame(r,b)).
$$
\end{theorem}
\begin{proof}
The traditional target is disjoint-sum ledger readback; the BEDC source is
$\mathsf{BHist}$; the carrier is $\mathsf{SumHistoryCarrier}$; no classifier is
needed for this carrier-readback obligation; the ledger is
$\mathsf{SumHistoryLedgerPolicy}$; the exactness principle is visible-tag
carrier readback; the status is $\mathsf{ProofTarget}$. Apply
Theorem~\ref{thm:sum-ledger-visible-carrier} to the ledger, transport the visible
carrier witness along the displayed $\hsame(v,\Ezero(l))$ or
$\hsame(v,\Eone(r))$ endpoint equality, and then apply
Theorem~\ref{thm:sum-history-carrier-visible-tag-exactness} to extract the left
or right source payload witness.
\end{proof}
\leanchecked{BEDC.Derived.SumUp.SumHistoryLedgerPolicy\_visible\_payload\_readback}

\begin{theorem}[Sum ledger one-step endpoint exactness]
\label{thm:sum-ledger-one-step-endpoint-exactness}
Let $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$ be source carriers and let
$\rho,v,l,l',r,r':\mathsf{BHist}$ be histories. If
$\mathsf{SumHistoryLedgerPolicy}(Left,Right,\rho,v)$ holds, then the one-ledger
link has endpoint exactness from $\rho$ to $v$. First, both mixed endpoint pairs
are impossible:
$$
(\hsame(\rho,\Ezero(l))\land\hsame(v,\Eone(r')))\to\bot
$$
and
$$
(\hsame(\rho,\Eone(r))\land\hsame(v,\Ezero(l')))\to\bot.
$$
Second, same-tag endpoint pairs determine their payloads:
$$
(\hsame(\rho,\Ezero(l))\land\hsame(v,\Ezero(l')))\to\hsame(l,l')
$$
and
$$
(\hsame(\rho,\Eone(r))\land\hsame(v,\Eone(r')))\to\hsame(r,r').
$$
Third, final visible endpoints read back source payload evidence:
$$
(\hsame(v,\Ezero(l'))\to\exists a:\mathsf{BHist}.\;Left(a)\land\hsame(l',a))
$$
and
$$
(\hsame(v,\Eone(r'))\to\exists b:\mathsf{BHist}.\;Right(b)\land\hsame(r',b)).
$$
The roadmap ten-field fit is: traditional target disjoint-sum one-ledger
endpoint audit; BEDC source $\mathsf{BHist}$ with $\hsame$; source carriers
$Left$ and $Right$; carrier $\mathsf{SumHistoryCarrier}$; classifier
$\mathsf{SumHistoryClassifier}(Left,Right,\hsame,\hsame)$; ledger
$\mathsf{SumHistoryLedgerPolicy}$; core theorem this endpoint exactness package;
exactness raw-to-visible tag separation, same-tag payload determinacy, and
visible source readback; standard bridge tagged sum ledger readback; status
$\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:sum-ledger-visible-tag-separation} to the ledger witness
for the two mixed clauses. Apply
Theorem~\ref{thm:sum-ledger-same-tag-payload-exactness} to the same ledger
witness for the two same-tag payload clauses. Apply
Theorem~\ref{thm:sum-ledger-visible-payload-readback} to the ledger witness for
the two final visible readback clauses.
\end{proof}

\begin{theorem}[Concrete $\SumUp$ semantic name certificate]
\label{thm:concrete-sum-namecert}
Let $Left,Right:\mathsf{BHist}\to\mathsf{Prop}$ be source carriers with classifiers
$LeftEq$ and $RightEq$. If $\NameCert(Left,LeftEq)$ and
$\NameCert(Right,RightEq)$ are supplied, then the tagged sum carrier, the tagged
sum classifier, and the carrier-as-pattern and carrier-as-ledger tuple form
$$
\begin{array}{l}
\mathsf{SemanticNameCert}( \\
\quad \mathsf{SumHistoryCarrier}(Left,Right), \\
\quad \mathsf{SumHistoryCarrier}(Left,Right), \\
\quad \mathsf{SumHistoryCarrier}(Left,Right), \\
\quad \mathsf{SumHistoryClassifier}(Left,Right,LeftEq,RightEq)).
\end{array}
$$
\end{theorem}
\begin{proof}
The carrier is represented by $e0$-tagged left histories and $e1$-tagged right histories. Theorem~\ref{thm:sum-classifier-no-source-membership} marks the split: the classifier delegates within matching tags to the corresponding source certificate, while source-carrier evidence is taken from $\mathsf{SumHistoryCarrier}$ when certificate fields require it. Reflexivity is built by tagging the source certificate reflexivity field on the selected component. Symmetry and transitivity invert the tagged classifier witnesses, use the matching source certificate fields inside a left--left or right--right branch, and discharge mixed tags by constructor no-confusion.

The core certificate fields are assembled from
Theorem~\ref{thm:sum-history-classifier-source-certified-stability-fields}.
That theorem supplies carrier-relative reflexivity, source-certified symmetry,
source-certified transitivity, and classifier-induced carrier transport for the
tagged carrier and tagged classifier. The inhabited field is separate: take the
inhabited witness $l$ from the left source certificate, choose the visible
endpoint $\Ezero(l)$, and rebuild
$\mathsf{SumHistoryCarrier}(Left,Right,\Ezero(l))$ by the left tagged
constructor with the reflexive endpoint witness and the stored $Left(l)$
evidence.

Inside the carrier-transport field supplied by the cited theorem, source-carrier
membership is not transported along an arbitrary source $\NameCert$ merely from
$\hsame(l,a)$. In the left branch, constructor inversion first aligns the tagged
payloads and obtains $\hsame(l,a)$; the checked finite-kernel identity
$\mathsf{BEDC.FKernel.Hist.hsame\_iff\_eq}$ converts that history-sameness
witness to kernel equality before the stored $Left(l)$ evidence is rewritten as
$Left(a)$. Only after this equality rewrite does
$\NameCert.carrier\_respects\_equiv$ from the left source certificate apply
along the displayed source classifier witness $LeftEq(a,b)$, yielding
$Left(b)$ and hence the target left tagged carrier. The right branch is the same
with $RightEq(a,b)$ and the right source certificate. Pattern soundness and
ledger soundness are identity implications, since both semantic fields are the
same predicate $\mathsf{SumHistoryCarrier}(Left,Right)$.
\end{proof}
\leanchecked{BEDC.Derived.SumUp.sum\_history\_semantic\_name\_certificate}

\begin{corollary}[$\SumUp$ is licensed, not primitive]
\label{cor:sum-licensed-not-primitive}
$\SumUp$ is licensed as a derived disjoint sum, not as a primitive coproduct.
\end{corollary}

\closureat{\SumUp}{\checkedCertStr}
