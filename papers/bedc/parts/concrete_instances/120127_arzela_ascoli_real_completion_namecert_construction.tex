\chapter{A Concrete Naming Certificate for $\AscoliRealCompletionUp$}
\label{ch:concrete-instances-ascoli-real-completion-namecert}
\origin{human}

Following Arzela, Ascoli, Bishop, and Bridges, the $\AscoliRealCompletionUp$ packet records the finite compact-continuity data that an Arzela--Ascoli style family hands to the L10 Real-and-Completion route. It sits after the finite family and compact-source surface of \autoref{ch:concrete-instances-arzelaascoli-namecert}, the compact-uniform-continuity packet of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-window source of \autoref{ch:concrete-instances-streamname-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only displayed finite-net, equicontinuity, stream, regular-rational, Real, transport, replay, provenance, and local naming rows; it does not assert open-cover compactness, selected subsequences, quotient function equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Ascoli real-completion carrier]
\label{def:ascoli-real-completion-carrier}
An $\AscoliRealCompletionUp$ carrier is a finite $\BHist$ packet
$$
A=(F,K,U,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\ArzelaAscoliUp$ family row, exposing the finite family, source compactness, and pointwise readback data consumed by the Ascoli route.
\item $K$ is the $\CompactUniformContinuityUp$ row, recording the finite-net and modulus handoff for the same compact source.
\item $U$ is the compact-continuity ledger that ties family windows, finite-net witnesses, and tolerance requests to one inspected source surface.
\item $W$ is the $\StreamNameUp$ window row opened by the compact family and modulus data.
\item $R$ is the $\RegSeqRatUp$ regular-rational readback row attached to those finite windows.
\item $E$ is the terminal $\RealUp$ seal reached only after $W$ and $R$ are visible.
\item $H$ records componentwise $\hsame$ transport for the family, compact-uniform, ledger, stream, regular, Real, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from compact family inspection through finite-net continuity, stream windows, regular readback, and Real sealing.
\item $P$ is the $\Pkg$ provenance over $\AscoliRealCompletionUp$, $\ArzelaAscoliUp$, $\CompactUniformContinuityUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\AscoliRealCompletionUp}$ row.
\end{enumerate}
The classifier accepts only packets whose completion-facing read uses the displayed rows $F,K,U,W,R,E,H,C,P,N$. It has no coordinate for selected subsequences, open-cover compactness, quotient equality of functions, host completeness, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Ascoli real-completion NameCert obligations]
\label{thm:ascoli-real-completion-namecert-obligations}
For every accepted $\AscoliRealCompletionUp$ carrier $A=(F,K,U,W,R,E,H,C,P,N)$, the local $\NameCert_{\AscoliRealCompletionUp}$ surface consists exactly of carrier admission, Arzela--Ascoli family exposure through $F$, compact-uniform-continuity exposure through $K$, compact-continuity ledger visibility through $U$, stream windows through $W$, regular rational readback through $R$, terminal Real sealing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:ascoli-real-completion-carrier}. The tuple lists the family row, compact-uniform row, ledger row, stream windows, regular readback, Real seal, and structural rows as the whole accepted surface.

Read the analytic source through $F,K,U$. The family row supplies the Ascoli data, the compact-uniform row supplies the finite-net modulus route, and the ledger row keeps those rows tied to the same inspected compact source before any completion-facing read is available.

Finally route completion-facing output through $W,R,E,H,C,P,N$. The stream and regular rows expose finite windows and rational readback, the Real row seals the terminal value, and the structural rows transport, replay, source, and name the same packet. Since no selected subsequence, open-cover compactness, quotient equality, choice, host completeness, or ambient completeness coordinate appears in the carrier, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Ascoli real-completion modulus handoff]
\label{thm:ascoli-real-completion-modulus-handoff}
Every Real-and-Completion consumer of an accepted $\AscoliRealCompletionUp$ packet receives its completion-facing data through the finite route
$$
\ArzelaAscoliUp
\longmapsto
\CompactUniformContinuityUp
\longmapsto
\StreamNameUp
\longmapsto
\RegSeqRatUp
\longmapsto
\RealUp .
$$
The handoff exposes only the displayed family, compact-continuity, finite-window, regular-readback, Real-seal, transport, replay, provenance, and local naming rows. It exports no open-cover compactness theorem, no selected subsequence, no quotient equality of functions, no host completeness principle, no countable-choice schedule, and no ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the carrier surface. The rows $F,K,U$ force the consumer to read the Ascoli family, compact-uniform-continuity packet, and compact-continuity ledger before it reaches any Real-facing object.

Next consume the completion-facing rows $W,R,E$. The finite stream windows expose only inspected name fragments, the regular rational row supplies the displayed readback, and the Real row seals the terminal value after those rows are visible.

The structural rows $H,C,P,N$ then preserve the same route under transport, replay, provenance, and local naming. Because the packet contains no selected subsequence, open-cover compactness, quotient function equality, host completeness, choice schedule, or ambient Real-completeness coordinate, no such endpoint can be read from the handoff.
\end{proof}

\closureat{AscoliRealCompletionUp}{seedStr}

\begin{closurestatus}{\AscoliRealCompletionUp}
  \constructivestory{An $\AscoliRealCompletionUp$ packet is generated from ArzelaAscoliUp family data, CompactUniformContinuityUp finite-net data, StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:ascoli-real-completion-carrier}, \autoref{thm:ascoli-real-completion-namecert-obligations}, and \autoref{thm:ascoli-real-completion-modulus-handoff} seed the finite Ascoli-to-Real completion route through family, compact-uniform, stream, regular-rational, Real, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subsequences, quotient function equality, countable choice, host completeness, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Ascoli family exposure, compact-uniform-continuity routing, stream-window exposure, regular-rational readback, RealUp sealing, transport, replay, provenance, and local naming for BEDC.Derived.AscoliRealCompletionUp.}
\end{closurestatus}
