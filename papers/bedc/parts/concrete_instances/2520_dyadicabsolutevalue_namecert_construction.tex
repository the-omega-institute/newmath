\chapter{A Concrete Naming Certificate for $\DyadicAbsoluteValueUp$}
\label{ch:concrete-instances-dyadicabsolutevalue-namecert}
\origin{human}

The $\DyadicAbsoluteValueUp$ interface isolates the finite rows needed when a real-completion tolerance ledger reads the nonnegative size of a dyadic rational source. It factors the absolute-value operation through the visible $\DyadicRatCoreUp$ source packet, sign split, common-exponent mantissa row, nonnegative endpoint, and tolerance ledger, so $\RegSeqRatUp$ and $\RealUp$ consumers can cite a named certificate without extending the near-capacity dyadic core chapter.

\begin{definition}[Dyadic absolute-value carrier]
\label{def:dyadic-absolute-value-carrier}
A $\DyadicAbsoluteValueUp$ carrier is a finite $\BHist$ packet
$$
A=(D,S,M,E,V,H,C,P,N)
$$
with the following visible coordinates:
\begin{enumerate}
\item $D$ is the source $\DyadicRatCoreUp$ row whose mantissa, unary exponent, denominator ledger, $\IntUp$ provenance, and $\RatUp$ provenance are read from the dyadic core surface;
\item $S$ is the sign-split row deciding which displayed branch of the scaled mantissa is used for the absolute-value read;
\item $M$ is the common-exponent mantissa row that exposes the source mantissa at the same denominator ledger used by dyadic distance-window observations;
\item $E$ is the nonnegative output endpoint row, recorded as a $\DyadicRatCoreUp$ endpoint whose mantissa is the displayed nonnegative branch of $M$;
\item $V$ is the tolerance and absolute-value ledger recording the radius window consumed by $\CauchyModulusUp$, $\RegSeqRatUp$, and $\RealUp$ routes;
\item $H$ stores componentwise $\hsame$ transports for the source, sign split, mantissa row, nonnegative endpoint, tolerance ledger, provenance, and local name;
\item $C$ stores the displayed $\Cont$ routes from $D$ to $S$ and $M$, from $S$ and $M$ to $E$, and from $E$ to the tolerance ledger $V$;
\item $P$ records $\Pkg$ provenance over $\IntUp$, $\RatUp$, $\DyadicRatCoreUp$, $\CauchyModulusUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\DyadicAbsoluteValueUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient rationals, selected real values, trichotomy, host integer normal forms, or $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Dyadic absolute-value NameCert obligations]
\label{thm:dyadic-absolute-value-namecert-obligations}
The local $\NameCert_{\DyadicAbsoluteValueUp}$ surface for \autoref{def:dyadic-absolute-value-carrier} consists exactly of source $\DyadicRatCoreUp$ admission, sign-split exposure, common-exponent mantissa readback, nonnegative endpoint construction, tolerance-ledger exposure, componentwise $\hsame$ transport, displayed $\Cont$ routes, $\Pkg$ provenance, and the local naming row.
\end{theorem}
\begin{proof}
Carrier admission is projection from the displayed packet $A$. The coordinates list the dyadic source, sign split, common-exponent mantissa, endpoint, tolerance ledger, transport, continuation, provenance, and local name.

The source and mantissa reads are inherited from the visible $\DyadicRatCoreUp$ rows. The sign split chooses the displayed nonnegative branch of the scaled mantissa, and the endpoint row repacks that branch with the same denominator ledger and provenance.

The remaining obligations are routing and naming obligations. The rows $H,C,P,N$ transport the listed coordinates, replay the reads, preserve the $\IntUp$ and $\RatUp$ source ledgers, and name the packet without introducing any hidden quotient or real-completion coordinate.
\end{proof}

\begin{theorem}[Dyadic absolute-value transport stability]
\label{thm:dyadic-absolute-value-transport-stability}
For accepted $\DyadicAbsoluteValueUp$ carriers whose displayed source, sign-split, mantissa, endpoint, tolerance-ledger, provenance, and naming rows are related componentwise by the stored $\hsame$ rows, the absolute-value endpoint and tolerance ledger remain readable after transport. The transported carrier computes the nonnegative endpoint from the transported $\DyadicRatCoreUp$ source through the transported common-exponent mantissa row.
\end{theorem}
\begin{proof}
Project the two accepted carriers. The rows that determine the absolute-value read are exactly the source $D$, sign split $S$, common-exponent mantissa row $M$, endpoint $E$, and tolerance ledger $V$.

Use the componentwise transport entries in $H$ to move those rows to the transported surface. The continuation rows in $C$ then replay the read from $D$ to $S$ and $M$, from $S$ and $M$ to $E$, and from $E$ to $V$.

The dyadic core distance-window stability theorem, \autoref{thm:dyadicratcore-distance-window-stability}, supplies the underlying common-exponent absolute-value stability for the source row. Repacking that stable window with the displayed endpoint and ledger rows gives the claimed transport stability.
\end{proof}

\begin{theorem}[Dyadic absolute-value nonnegative bridge]
\label{thm:dyadic-absolute-value-nonnegative-bridge}
Every accepted $\DyadicAbsoluteValueUp$ carrier exposes a nonnegative dyadic endpoint $E$ whose source is the sign-split common-exponent mantissa row of $D$. Any $\CauchyModulusUp$, $\RegSeqRatUp$, or $\RealUp$ consumer of this endpoint reads only the displayed endpoint row, tolerance ledger, transports, continuation routes, package provenance, and local $\NameCert_{\DyadicAbsoluteValueUp}$ row.
\end{theorem}
\begin{proof}
Start from the accepted carrier and read the sign-split row $S$. The common-exponent mantissa row $M$ places the source mantissa of $D$ on the same denominator ledger used by the dyadic core distance and absolute-value window.

The nonnegative endpoint $E$ is then the displayed branch chosen by $S$ and repacked as a dyadic endpoint with the visible $\IntUp$ and $\RatUp$ provenance. No separate order oracle or trichotomy row is needed, because the branch is already part of the finite packet.

A downstream consumer reaches $E$ through the continuation routes in $C$ and receives the tolerance ledger $V$ as the only real-facing output surface. Thus the bridge exports a nonnegative dyadic endpoint without exporting quotient rationals, selected real values, host integer normal forms, or $\RealUp$ completeness.
\end{proof}

\begin{theorem}[Dyadic absolute-value ledger exactness]
\label{thm:dyadic-absolute-value-ledger-exactness}
For an accepted $\DyadicAbsoluteValueUp$ carrier, the finite ledger is exact for the route from the source $\DyadicRatCoreUp$ row through sign split, common-exponent mantissa readback, nonnegative endpoint construction, and tolerance readback to $\CauchyModulusUp$, $\RegSeqRatUp$, and $\RealUp$ consumers. No ledger entry records a quotient rational, selected real value, trichotomy proof, host integer normal form, or real-completeness witness.
\end{theorem}
\begin{proof}
Inspect the carrier of \autoref{def:dyadic-absolute-value-carrier}. The only absolute-value data are $D,S,M,E,V$ together with their transport, continuation, provenance, and naming rows.

Apply \autoref{thm:dyadic-absolute-value-namecert-obligations} to bind every accepted read to those rows. Transport stability then shows that a transported read cannot replace the common-exponent mantissa window or tolerance ledger with an external absolute-value oracle.

Finally apply \autoref{thm:dyadic-absolute-value-nonnegative-bridge}. Every completion-facing use factors through the same endpoint and tolerance ledger, so the ledger is exact for the displayed finite route and contains no unlisted analytic or host-normalization coordinate.
\end{proof}

\begin{theorem}[Dyadic absolute-value obligation closure package]
\label{thm:dyadic-absolute-value-obligation-closure-package}
The carrier, NameCert obligations, transport stability, nonnegative bridge, and ledger exactness of \autoref{def:dyadic-absolute-value-carrier}, \autoref{thm:dyadic-absolute-value-namecert-obligations}, \autoref{thm:dyadic-absolute-value-transport-stability}, \autoref{thm:dyadic-absolute-value-nonnegative-bridge}, and \autoref{thm:dyadic-absolute-value-ledger-exactness} assemble the finite $\DyadicAbsoluteValueUp$ obligation package over $\DyadicRatCoreUp$, the $\IntUp$ and $\RatUp$ sign rows, $\RegSeqRatUp$ and $\RealUp$ tolerance consumers, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The package exports only the displayed source, sign split, common-exponent mantissa, nonnegative endpoint, tolerance ledger, transport, continuation, provenance, and local naming rows.
\end{theorem}
\begin{proof}
The carrier theorem supplies the finite packet and lists exactly the rows available to the package. The NameCert obligations bind admission, sign-split exposure, common-exponent mantissa readback, endpoint construction, tolerance-ledger exposure, transport, continuation, provenance, and naming to those coordinates.

Transport stability then shows that accepted reads remain inside the same displayed surface after componentwise $\hsame$ movement. The nonnegative bridge identifies the downstream endpoint read consumed by $\CauchyModulusUp$, $\RegSeqRatUp$, and $\RealUp$ routes.

Ledger exactness supplies the final packaging boundary. It rules out any hidden coordinate for a host absolute value, trichotomy, quotient normalization, or analytic completeness, so the five cited rows form one finite $\DyadicAbsoluteValueUp$ obligation package rather than an appeal to an external absolute-value operation.
\end{proof}

\closureat{DyadicAbsoluteValueUp}{seedStr}

\begin{closurestatus}{\DyadicAbsoluteValueUp}
  \constructivestory{A $\DyadicAbsoluteValueUp$ packet is generated from a $\DyadicRatCoreUp$ source row, a sign-split row, a common-exponent mantissa row, a nonnegative endpoint row, a tolerance and absolute-value ledger, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-absolute-value-carrier}, \autoref{thm:dyadic-absolute-value-namecert-obligations}, \autoref{thm:dyadic-absolute-value-transport-stability}, \autoref{thm:dyadic-absolute-value-nonnegative-bridge}, and \autoref{thm:dyadic-absolute-value-ledger-exactness} bind the finite dyadic absolute-value carrier, sign split, nonnegative endpoint, transport surface, and tolerance ledger.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close quotient rationals, selected real values, trichotomy, host integer normal forms, $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, transport stability, nonnegative-endpoint handoff, and ledger exactness for the same finite packet.}
\end{closurestatus}
