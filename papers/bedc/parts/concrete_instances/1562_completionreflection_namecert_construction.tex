\chapter{A Concrete Naming Certificate for $\CompletionReflectionUp$}
\label{ch:concrete-instances-completionreflection-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the completion reflection boundary is read here inside the BEDC mathlib-free substrate: completion data are admitted only as finite named rows, and reflection across the real seal is expressed by displayed transports rather than by host quotient objects. The $\CompletionReflectionUp$ packet ties together the construction surface of $\RealCauchyCompletionUp$, the extension surface of $\CompletionUniversalPropertyUp$, and the separated boundary of $\HausdorffCompletionUp$.

The reflection packet consumes the completion construction of \autoref{ch:concrete-instances-real-cauchy-completion-namecert}, the universal-property extension row from \autoref{ch:concrete-instances-completionuniversalproperty-namecert}, the separated boundary row from \autoref{ch:concrete-instances-hausdorffcompletion-namecert}, the regular-rational source row from \autoref{ch:concrete-instances-regseqrat-namecert}, and the real seal row from \autoref{ch:concrete-instances-real-namecert}. These sibling rows are the source of $C,U,H,Q,A$ in the carrier tuple, and the $\CompletionReflectionUp$ packet only repackages their finite routes through $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

\begin{theorem}[CompletionReflection StreamName-RegSeqRat-Real factorization]
\label{thm:completion-reflection-stream-regseq-real-factor}
Every accepted $\CompletionReflectionUp$ read factors through the $\StreamNameUp$ finite-window surface, the $\RegSeqRatUp$ readback row, the $\RealUp$ seal row, and the local reflection rows
$$
C,U,H,Q,A
$$
before any completion-facing consumer can use the reflected object. The factorization is the finite route supplied by \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-real-namecert}, \autoref{ch:concrete-instances-real-cauchy-completion-namecert}, \autoref{ch:concrete-instances-completionuniversalproperty-namecert}, and \autoref{ch:concrete-instances-hausdorffcompletion-namecert}.
\end{theorem}
\leanchecked{BEDC.Derived.CompletionReflectionUp.CompletionReflectionPacket\_stream\_regseq\_real\_factor}
\begin{proof}
Project the dependency paragraph and the carrier rows. The completion construction row $C$, universal-property row $U$, Hausdorff boundary row $H$, regular-rational source row $Q$, and Real seal row $A$ are exactly the local reflection rows available before transport and provenance are read.

Use the RegSeqRat and Real sibling rows to identify the finite source route. The StreamName finite-window surface feeds the regular rational readback, and the regular readback is the finite row through which the Real seal is consumed.

Now compose the completion-construction, universal-property, and Hausdorff-completion sibling rows. They allow the reflected object to be used only after the same finite $C,U,H,Q,A$ route has been transported, replayed, cited, and named.

Thus a completion-facing consumer receives a reflected finite route, not a quotient stream, selected host limit, host real equality principle, or ambient completeness theorem.
\end{proof}

\begin{definition}[CompletionReflection carrier]
\label{def:completion-reflection-carrier}
A $\CompletionReflectionUp$ carrier is a finite $\mathsf{BHist}$ packet
$$
R=(C,U,H,D,Q,A,S,T,K,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $C$ is the $\RealCauchyCompletionUp$ completion-construction row;
\item $U$ is the $\CompletionUniversalPropertyUp$ extension row attached to the same completion-facing source;
\item $H$ is the $\HausdorffCompletionUp$ separated completion boundary row;
\item $D$ is the $\RealDiagonalCompletionUp$ diagonal real-seal row;
\item $Q$ is the $\RegSeqRatUp$ regular rational source row inspected by the diagonal and completion packets;
\item $A$ is the $\RealUp$ seal row through which the completed object is consumed;
\item $S$ records the componentwise $\hsame$ transports for construction, extension, separatedness, diagonal, regular-sequence, and real-seal rows;
\item $T$ records the displayed $\Cont$ routes through the same rows;
\item $K$ is the $\Pkg$ provenance over $\RealCauchyCompletionUp$, $\CompletionUniversalPropertyUp$, $\HausdorffCompletionUp$, $\RealDiagonalCompletionUp$, $\RegSeqRatUp$, and $\RealUp$;
\item $P$ records the local reflection packet provenance over $\mathsf{BHist}$, $\hsame$, $\Cont$, and $\Pkg$ rows;
\item $N$ is the local $\NameCert_{\CompletionReflectionUp}$ row.
\end{enumerate}
The carrier contains no quotient stream, no selected host limit, no host equality of completed reals, and no ambient completeness theorem.
\end{definition}
\leanchecked{BEDC.Derived.CompletionReflectionUp.TasteGate.CompletionReflectionTasteGate\_single\_carrier\_alignment}

\begin{definition}[CompletionReflection classifier]
\label{def:completion-reflection-classifier}
For accepted $\CompletionReflectionUp$ carriers $R$ and $R'$, the classifier $\mathsf{CompletionReflectionClassifier}(R,R')$ compares exactly the displayed rows of \autoref{def:completion-reflection-carrier}. The construction row compares through $\RealCauchyCompletionUp$, the extension row through $\CompletionUniversalPropertyUp$, the separated boundary through $\HausdorffCompletionUp$, the diagonal row through $\RealDiagonalCompletionUp$, the regular rational source through $\RegSeqRatUp$, and the seal row through $\RealUp$.

The classifier has no component for a quotient of regular streams, a selected host limit, host real equality, or an external complete-space object. Transport is therefore confined to the displayed $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.
\end{definition}
\leandef{BEDC.Derived.CompletionReflectionUp.CompletionReflectionClassifier}

\begin{theorem}[CompletionReflection NameCert obligations]
\label{thm:completion-reflection-namecert-obligations}
The local $\NameCert_{\CompletionReflectionUp}$ surface consists of five finite rows:
\begin{enumerate}
\item carrier admission by the finite packet of \autoref{def:completion-reflection-carrier};
\item classifier transport by the visible comparison surface of \autoref{def:completion-reflection-classifier};
\item diagonal real-seal reflection from $\RealDiagonalCompletionUp$ and $\RegSeqRatUp$ into the $\RealUp$ seal row;
\item universal-property extension reflection from $\CompletionUniversalPropertyUp$ back into the same completion and seal provenance;
\item non-escape from quotient streams, selected host limits, host equality, and ambient completeness.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.CompletionReflectionUp.CompletionReflectionPacket\_namecert\_obligations}
\begin{proof}
Carrier admission is projection of the finite packet. Each row required by the reflection boundary appears as a coordinate before any consumer can read it.

Classifier transport is componentwise. Use the displayed $\hsame$ rows to compare construction, extension, separatedness, diagonal, regular-sequence, and real-seal data, then replay the $\Cont$ routes attached to those same coordinates.

The diagonal reflection row reads $D$, $Q$, and $A$ together. It states that the real-seal observation is reached through regular rational diagonal data and not through a host real representative.

The extension reflection row reads $U$ against the same construction and seal provenance. Since the extension ledger is carried by $U$ and routed through $T$, the reflected extension cannot introduce a separate completion object.

Finally the provenance rows $K$, $P$, and $N$ account for package and local naming data. Because the carrier has no quotient-stream, selected-limit, host-equality, or ambient-completeness coordinate, no such object can be exported.
\end{proof}

\begin{theorem}[CompletionReflection real-seal boundary]
\label{thm:completion-reflection-real-seal-boundary}
Every $\RealUp$ consumer read of an accepted $\CompletionReflectionUp$ packet factors through the $\RealCauchyCompletionUp$ construction row, the $\RealDiagonalCompletionUp$ diagonal row, the $\RegSeqRatUp$ source row, the $\HausdorffCompletionUp$ separated boundary row, and the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ provenance. The read exposes no quotient stream, selected host limit, host real equality, or ambient completeness theorem.
\end{theorem}
\leanchecked{BEDC.Derived.CompletionReflectionUp.CompletionReflectionPacket\_real\_seal\_boundary}
\begin{proof}
Project an accepted carrier $R=(C,U,H,D,Q,A,S,T,K,P,N)$. The only real-facing coordinate is $A$, and it is paired in the packet with the completion construction row $C$, diagonal row $D$, regular source row $Q$, and separated boundary row $H$.

Use the classifier of \autoref{def:completion-reflection-classifier} to move between accepted representatives. It preserves exactly those rows by displayed transport and continuation, so any real-seal read remains attached to the same finite provenance.

Apply the non-escape row of \autoref{thm:completion-reflection-namecert-obligations}. There is no coordinate from which a quotient stream, host limit, host equality, or ambient completeness theorem could be read, so the real-seal boundary is the displayed finite route.
\end{proof}

\begin{theorem}[CompletionReflection extension reflects]
\label{thm:completion-reflection-extension-reflects}
Let $R=(C,U,H,D,Q,A,S,T,K,P,N)$ be an accepted $\CompletionReflectionUp$ carrier. Any completion-facing extension read exported by $U$ reflects back to the same construction row $C$, diagonal row $D$, regular rational source $Q$, real-seal row $A$, separated boundary row $H$, and provenance rows $S,T,K,P,N$. No reflected extension can switch to a quotient stream, selected host limit, host equality principle, or ambient completeness theorem.
\end{theorem}
\leanchecked{BEDC.Derived.CompletionReflectionUp.CompletionReflectionPacket\_extension\_reflects}
\begin{proof}
Read the extension coordinate $U$ inside the carrier. Its source and target provenance are not external: the same packet records the completion construction row $C$, diagonal row $D$, regular source row $Q$, and real-seal row $A$.

The separated boundary row $H$ fixes the Hausdorff-facing route. Transport along $S$ and $T$ keeps the extension ledger attached to that route, while the package rows $K$ and $P$ record the dependency surface consumed by the local certificate.

The obligation surface in \autoref{thm:completion-reflection-namecert-obligations} then repacks the extension read as a $\NameCert_{\CompletionReflectionUp}$ row. Since every exported coordinate is visible in the carrier, the reflected extension has no path to unrecorded quotient, limit, equality, or completeness data.
\end{proof}

\begin{theorem}[CompletionReflection carrier scope]
\label{thm:completionreflection-carrier-scope}
The scoped carrier of $\CompletionReflectionUp$ is exactly the finite packet
$$
(C,U,H,D,Q,A,S,T,K,P,N)
$$
over $\RealCauchyCompletionUp$, $\CompletionUniversalPropertyUp$, $\HausdorffCompletionUp$, $\RealDiagonalCompletionUp$, $\RegSeqRatUp$, $\RealUp$, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. No scoped carrier coordinate names a quotient stream, selected host limit, host real equality, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Project the accepted carrier through \autoref{def:completion-reflection-carrier}. The definition lists the six construction-facing rows $C,U,H,D,Q,A$ and then the five structural rows $S,T,K,P,N$.

The same definition also states the exclusions. Since every carrier read is a projection from this finite tuple, any object outside these coordinates has no scoped carrier slot from which it can be read.
\end{proof}

\begin{theorem}[CompletionReflection classifier scope]
\label{thm:completionreflection-classifier-scope}
The $\CompletionReflectionUp$ classifier transports the scoped rows componentwise along the same $C,U,H,D,Q,A,S,T,K,P,N$ surface. It can compare construction, extension, separated-boundary, diagonal, regular-source, real-seal, transport, continuation, package, packet-provenance, and local naming rows, but it cannot add quotient streams, selected host limits, host real equality, or ambient completeness data.
\end{theorem}
\leanchecked{BEDC.Derived.CompletionReflectionUp.CompletionReflectionPacket\_classifier\_scope}
\begin{proof}
Apply \autoref{def:completion-reflection-classifier}. Its comparison clauses are exactly the rows already displayed in the carrier definition, with $\hsame$ and $\Cont$ supplying componentwise transport for the visible packet.

Because the classifier is defined only on accepted carriers and has no extra field, transport preserves the listed rows without creating a new coordinate. The excluded quotient, limit, equality, and completeness objects therefore remain outside the classifier scope.
\end{proof}

\begin{theorem}[CompletionReflection consumer scope]
\label{thm:completionreflection-consumer-scope}
Every consumer read exported by $\CompletionReflectionUp$ factors through the displayed real-seal and extension rows: the real-facing read passes through $A$ over $C,D,Q,H,S,T,K,P,N$, and the extension-facing read passes through $U$ over the same construction, diagonal, regular-source, real-seal, separated-boundary, transport, continuation, package, and local naming rows.
\end{theorem}
\leanchecked{BEDC.Derived.CompletionReflectionUp.CompletionReflectionPacket\_consumer\_scope}
\begin{proof}
For real-facing consumers, apply \autoref{thm:completion-reflection-real-seal-boundary}. It routes every $\RealUp$ read through the displayed construction, diagonal, regular-source, separated-boundary, transport, continuation, package, and naming coordinates.

For extension-facing consumers, apply \autoref{thm:completion-reflection-extension-reflects}. The extension coordinate $U$ reflects back to the same rows and cannot switch to an unrecorded completion object.

Combining the two routes exhausts the consumer surface: a consumer can read only the displayed real-seal row or the displayed extension row, each with the same finite provenance boundary.
\end{proof}

\begin{theorem}[CompletionReflection public export]
\label{thm:completionreflection-public-export}
Any public consumer of $\NameCert_{\CompletionReflectionUp}$ may read exactly the completion construction row $C$, universal-property extension row $U$, Hausdorff separated boundary row $H$, diagonal regular-sequence reflection row $D$, regular rational source row $Q$, $\RealUp$ seal row $A$, displayed $\hsame$ and $\Cont$ transports $S,T$, $\Pkg$ provenance $K,P$, and the local $\NameCert$ row $N$ from the carrier
$$
(C,U,H,D,Q,A,S,T,K,P,N).
$$
The public export has no coordinate for quotient completion objects, selected host limits, host equality of completed reals, or ambient completeness.
\end{theorem}
\begin{proof}
First apply \autoref{thm:completionreflection-carrier-scope}. It identifies the public carrier surface with the finite tuple $C,U,H,D,Q,A,S,T,K,P,N$ and excludes quotient streams, selected host limits, host real equality, and ambient completeness from the carrier coordinates.

Next apply \autoref{thm:completionreflection-classifier-scope}. Public movement between accepted packets is componentwise along the same construction, extension, separated-boundary, diagonal, regular-source, real-seal, transport, continuation, package, packet-provenance, and local naming rows.

Finally apply \autoref{thm:completionreflection-consumer-scope}. Every public consumer read factors through the real-seal row or the extension row over the same finite provenance surface, so the exported $\NameCert_{\CompletionReflectionUp}$ interface is exactly the displayed tuple and nothing outside it.
\end{proof}
\leanchecked{BEDC.Derived.CompletionReflectionUp.CompletionReflectionPacket\_public\_export}

\begin{theorem}[CompletionReflection universal real-seal factorization]
\label{thm:completionreflection-universal-real-seal-factorization}
Let $R=(C,U,H,D,Q,A,S,T,K,P,N)$ be an accepted $\CompletionReflectionUp$ carrier. Every universal-property extension route exported by $U$ factors through the displayed $\RealUp$ seal row $A$ before any completion-facing consumer can read it. The factorization uses the completion construction row $C$, diagonal row $D$, regular rational source row $Q$, Hausdorff boundary row $H$, componentwise $\hsame$ rows $S$, displayed $\Cont$ routes $T$, package provenance $K,P$, and local $\NameCert$ row $N$.

No universal-property consumer can bypass the real-seal row by reading a quotient completion object, selected host limit, host equality of completed reals, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:completion-reflection-real-seal-boundary}. It identifies the $\RealUp$ seal row $A$ as the real-facing boundary through which completion data become consumable.

Apply \autoref{thm:completion-reflection-extension-reflects}. Any extension route supplied by $U$ reflects back to the same construction row, diagonal row, regular source, real-seal row, separated boundary, and provenance surface.

Use \autoref{thm:completionreflection-consumer-scope} to align the two reads. It states that real-facing and extension-facing consumers share the same finite rows, so the extension route cannot be consumed before the real-seal boundary is present.

Finally apply \autoref{thm:completionreflection-public-export}. The public packet exposes exactly $C,U,H,D,Q,A,S,T,K,P,N$ and no quotient, selected limit, host equality, or ambient completeness coordinate, so the universal route factors through the displayed seal for every completion-facing consumer.
\end{proof}
\leanchecked{BEDC.Derived.CompletionReflectionUp.CompletionReflectionPacket\_universal\_real\_seal\_factorization}

\begin{theorem}[CompletionReflection real-seal extension coherence]
\label{thm:completionreflection-real-seal-extension-coherence}
Let $R=(C,U,H,D,Q,A,S,T,K,P,N)$ be an accepted $\CompletionReflectionUp$ packet. The public real-seal read through $A$ and the universal-property extension read through $U$ are coherent exactly when both reads factor through the same displayed rows
$$
C,D,Q,A,H,S,T,K,P,N.
$$
Neither read may replace those rows with a quotient stream, selected host limit, host equality, or ambient completeness datum.
\end{theorem}
\begin{proof}
For the real-facing direction, apply \autoref{thm:completion-reflection-real-seal-boundary}. Every $\RealUp$ read is routed through the displayed construction, diagonal, regular-source, separated-boundary, transport, continuation, package, and naming rows before it reaches the seal row $A$.

For the extension-facing direction, apply \autoref{thm:completion-reflection-extension-reflects}. The route exported by $U$ reflects back to $C,D,Q,A,H,S,T,K,P,N$, so the extension read cannot introduce a second completion surface.

Now align the two readings by \autoref{thm:completionreflection-consumer-scope}. Its consumer boundary says that the real-facing read and the extension-facing read use the same finite packet surface, with $A$ and $U$ as the two visible public entries.

Finally apply \autoref{thm:completionreflection-public-export}. Since the public export exposes only $C,U,H,D,Q,A,S,T,K,P,N$ and excludes quotient streams, selected host limits, host equality, and ambient completeness, coherence is exactly shared factorization through the displayed rows.
\end{proof}
\leanchecked{BEDC.Derived.CompletionReflectionUp.CompletionReflectionPacket\_real\_seal\_extension\_coherence}

\begin{theorem}[CompletionReflection regular-source tail reflection]
\label{thm:completionreflection-regular-source-tail-reflection}
Let $R=(C,U,H,D,Q,A,S,T,K,P,N)$ be an accepted $\CompletionReflectionUp$ packet whose reflected completion read starts from a regular rational tail window. Then every such read factors through the displayed route
$$
\StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \RealUp \longrightarrow \RealCauchyCompletionUp \longrightarrow \CompletionUniversalPropertyUp \longrightarrow \HausdorffCompletionUp
$$
before it reaches the local $\NameCert_{\CompletionReflectionUp}$ row $N$. The factorization uses the carrier rows $C,D,Q,A,H$, the classifier transport rows $S$, the displayed $\Cont$ routes $T$, package provenance $K,P$, and the local naming row $N$.

No reflected regular-source tail read may replace this route by quotient streams, selected host limits, host real equality, or ambient completeness.
\end{theorem}
\begin{proof}
Start with the accepted carrier. By \autoref{thm:completionreflection-carrier-scope}, the only regular-source data visible to the packet are the diagonal reflection row $D$, the regular rational source row $Q$, the real-seal row $A$, the Hausdorff boundary row $H$, and the completion construction row $C$.

Apply \autoref{thm:completion-reflection-namecert-obligations}. The local NameCert obligations read $Q$ through the same finite packet surface and route it through the completion construction, universal extension, Hausdorff separated boundary, diagonal reflection, real seal, transport, continuation, provenance, and naming rows.

Next use \autoref{thm:completionreflection-classifier-scope}. Componentwise $\hsame$ transport preserves the regular-source, real-seal, completion-construction, universal-property, and Hausdorff-boundary rows, so the reflected tail read cannot branch into an unlisted completion surface.

Finally apply \autoref{thm:completionreflection-public-export}. The exported packet exposes only $C,U,H,D,Q,A,S,T,K,P,N$, which realizes the displayed $\StreamNameUp$ to $\HausdorffCompletionUp$ route before the local $\NameCert_{\CompletionReflectionUp}$ row. Since quotient streams, selected host limits, host real equality, and ambient completeness are absent from that export, no regular-source tail reflection may read them.
\end{proof}

\begin{theorem}[CompletionReflection standard bridge boundary]
\label{thm:completion-reflection-standard-bridge-boundary}
Let $R=(C,U,H,D,Q,A,S,T,K,P,N)$ be an accepted $\CompletionReflectionUp$ packet. If the carrier row, classifier row, local $\NameCert$ obligations, real-seal boundary, extension-reflection row, consumer scope, provenance-route scope, and public export of $R$ are displayed, then $R$ induces a standard bridge boundary read only through the finite rows already named by this chapter: $\RegSeqRatUp$, $\RealUp$, the real-seal row, classifier row, $\hsame$, $\Cont$, $\Pkg$, and the local $\NameCert$ row.

The bridge boundary exports no quotient completion, no host equality of completed reals, no selected host limit, and no ambient completeness claim.
\end{theorem}
\leanchecked{BEDC.Derived.CompletionReflectionUp.CompletionReflectionPacket\_standard\_bridge\_boundary}
\begin{proof}
First read the accepted packet through its displayed local obligations. Apply \autoref{thm:completion-reflection-namecert-obligations} to bind the completion construction, universal extension, separated boundary, diagonal reflection, regular rational source, real-seal, transport, continuation, package, provenance, and naming rows.

Next route the real-facing and extension-facing reads. Apply \autoref{thm:completion-reflection-real-seal-boundary} for the $\RealUp$ seal row and \autoref{thm:completion-reflection-extension-reflects} for the universal-extension row; both routes return to the same displayed construction, regular-source, real-seal, classifier, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ surface.

Now restrict the consumers and provenance routes. Apply \autoref{thm:completionreflection-consumer-scope} and \autoref{thm:completionreflection-classifier-scope} to ensure that every admitted read uses only the rows already visible in the chapter.

Finally apply \autoref{thm:completionreflection-public-export}. The public export exposes exactly the finite completion-reflection tuple and excludes quotient completion objects, host equality, selected host limits, and ambient completeness. This is precisely the standard bridge boundary over the displayed rows, with no additional completion-side coordinate.
\end{proof}

\closureat{CompletionReflectionUp}{seedStr}
\closureat{CompletionReflectionUp}{obligationStr}
\closureat{CompletionReflectionUp}{scopedStr}[BEDC.Derived.CompletionReflectionUp.CompletionReflectionPacket\_real\_seal\_boundary]
\closureat{CompletionReflectionUp}{publicStr}[BEDC.Derived.CompletionReflectionUp.CompletionReflectionPacket\_public\_export]

\begin{closurestatus}{\CompletionReflectionUp}
  \constructivestory{A $\CompletionReflectionUp$ packet is generated from completion construction, universal-property extension, Hausdorff separated boundary, diagonal regular-sequence reflection, real-seal, transport, continuation, package, and local naming rows.}
  \theoryclosure{\publicClosure}
  \scopeclosed{\autoref{thm:completionreflection-carrier-scope}, \autoref{thm:completionreflection-classifier-scope}, and \autoref{thm:completionreflection-consumer-scope} bind the finite carrier, componentwise classifier transport, and consumer readback boundary over $\RealCauchyCompletionUp$, $\CompletionUniversalPropertyUp$, $\HausdorffCompletionUp$, $\RealDiagonalCompletionUp$, $\RegSeqRatUp$, $\RealUp$, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close quotient completion construction, selected host limits, host equality of completed reals, ambient completeness, or bridge checking.}
  \upgradepath{Bridged closure requires a standard-bridge consumer over the public completion-reflection packet.}
\end{closurestatus}
