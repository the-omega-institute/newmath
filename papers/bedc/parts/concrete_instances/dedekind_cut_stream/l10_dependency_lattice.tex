The $\DedekindCutStreamUp$ dependency lattice consumes the finite-window
$\StreamNameUp$ spine of \autoref{ch:concrete-instances-streamname-namecert}
and the $\RegSeqRatUp$ readback route of
\autoref{ch:concrete-instances-regseqrat-namecert} before any
$\RealUp$-facing cut stream is inspected through
\autoref{ch:concrete-instances-real-namecert}. Thus the cut-observation packet
remains a horizontal L10 route through displayed stream windows and regular
rational readback, not a kernel-only pointer to a selected real limit or
quotient cut.
