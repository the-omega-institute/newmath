\chapter{A Concrete Naming Certificate for $\MetaCICResidualNormalFormBudgetRouterUp$}
\label{ch:concrete-instances-metacic-residual-normal-form-budget-router-namecert}
\origin{ai}

The $\MetaCICResidualNormalFormBudgetRouterUp$ packet realizes a residual normal-form budget route for MetaCIC as finite candidate-mediated NameCert data. It sits beside the typed normal-form scheduler of \autoref{ch:concrete-instances-metacic-typed-normal-form-scheduler-namecert}, the residual diamond budget of \autoref{ch:concrete-instances-metacic-residual-diamond-budget-namecert}, the residual join budget of \autoref{ch:concrete-instances-metacic-residual-join-budget-namecert}, the normal-endpoint budget of \autoref{ch:concrete-instances-metacic-normal-endpoint-budget-namecert}, and the candidate-normalization confluence handoff of \autoref{ch:concrete-instances-metacic-candidate-normalization-confluence-handoff-namecert}. The router stores the finite rows by which a residual pair, candidate gate, normal-form scheduler budget, and endpoint seal are read before a confluence-facing handoff is public; it is not a global normalization theorem, subject-reduction theorem, unrestricted residual diamond, quotient syntax equality, evaluator equality, or kernel-consistency certificate.

\begin{definition}[MetaCIC residual normal-form budget router carrier]
\label{def:metacic-residual-normal-form-budget-router-carrier}
A $\MetaCICResidualNormalFormBudgetRouterUp$ carrier is a finite $\BHist$ packet
$$
R=(W,D,J,S,E,Q,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $W$ is the typed residual window row, recording the closed typed source, residual pair, and bounded comparison window.
\item $D$ is the residual-diamond budget row, read through $\MetaCICResidualDiamondBudgetUp$ and retaining the finite local-diamond checker surface.
\item $J$ is the residual-join budget row, read through $\MetaCICResidualJoinBudgetUp$ and recording the candidate join that the checker may inspect.
\item $S$ is the typed normal-form scheduler row, read through $\MetaCICTypedNormalFormSchedulerUp$ and exposing candidate-gate and scheduler-budget data.
\item $E$ is the normal-endpoint budget row, read through $\MetaCICNormalEndpointBudgetUp$ and sealing the finished normal endpoint available to bounded comparison.
\item $Q$ is the candidate-normalization confluence handoff row, read through $\MetaCICCandidateNormalizationConfluenceHandoffUp$ only after $W,D,J,S,E$ have been displayed.
\item $O$ is the obstruction row, retaining failed candidate gates, exhausted scheduler budgets, blocked residual pairs, missing endpoint seals, and failed bounded local joins.
\item $H$ records componentwise $\hsame$ transport for $W,D,J,S,E,Q,O$ and the structural rows.
\item $C$ records displayed $\Cont$ replay from a residual normal-form request through $W,D,J,S,E,Q,O,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\MetaCICResidualNormalFormBudgetRouterUp$, $\MetaCICTypedNormalFormSchedulerUp$, $\MetaCICResidualDiamondBudgetUp$, $\MetaCICResidualJoinBudgetUp$, $\MetaCICNormalEndpointBudgetUp$, $\MetaCICCandidateNormalizationConfluenceHandoffUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetaCICResidualNormalFormBudgetRouterUp}$ row.
\end{enumerate}
The classifier compares accepted router packets only through $W,D,J,S,E,Q,O,H,C,P,N$. It has no coordinate for unrestricted residual substitution, global strong normalization, subject reduction, quotient syntax equality, evaluator equality, or kernel consistency.
\end{definition}

\begin{theorem}[MetaCIC residual normal-form budget router obligations]
\label{thm:metacic-residual-normal-form-budget-router-obligations}
For every accepted $\MetaCICResidualNormalFormBudgetRouterUp$ carrier
$$
R=(W,D,J,S,E,Q,O,H,C,P,N),
$$
the local $\NameCert_{\MetaCICResidualNormalFormBudgetRouterUp}$ surface consists exactly of typed residual-window admission, residual-diamond budget exposure, residual-join budget exposure, typed scheduler budget routing, normal-endpoint sealing, candidate-normalization confluence handoff, obstruction retention, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-residual-normal-form-budget-router-carrier}. The accepted packet displays exactly $W,D,J,S,E,Q,O,H,C,P,N$, so the NameCert surface is exhausted by those rows.

The residual rows $W,D,J$ are read before any normal-form endpoint is public. They bind the typed window, local-diamond budget, and candidate join budget to the same residual pair.

The scheduler and endpoint rows $S,E$ then expose candidate-gate, finite scheduler, and finished endpoint data. The handoff row $Q$ can be consumed only after those rows are visible.

Finally $O,H,C,P,N$ retain the obstruction boundary and preserve the displayed route under transport, replay, provenance, and local naming. Since no further coordinate is present, the packet cannot export unrestricted residual substitution, global normalization, subject reduction, quotient syntax equality, evaluator equality, or kernel consistency.
\end{proof}

\begin{theorem}[MetaCIC residual normal-form router handoff]
\label{thm:metacic-residual-normal-form-budget-router-handoff}
Every candidate-mediated confluence consumer of an accepted $\MetaCICResidualNormalFormBudgetRouterUp$ packet first reads the finite route
$$
W \longmapsto D,J \longmapsto S \longmapsto E \longmapsto Q \longmapsto O,H,C,P,N .
$$
Thus residual diamond and residual join budgets are consumed before the typed normal-form scheduler and normal-endpoint budget, and the candidate-normalization confluence handoff is visible only after those budget rows have been displayed.
\end{theorem}
\begin{proof}
Start with the typed residual window $W$. It fixes the closed typed source, residual pair, and bounded comparison window for the whole route.

Next read $D$ and $J$. These rows expose the residual-diamond and residual-join budgets without turning them into a premise-free confluence theorem.

Then consume $S,E,Q$. The scheduler row supplies candidate-gate and budget data, the endpoint row seals the finished normal endpoint, and the handoff row exports only the candidate-mediated confluence boundary that can read those budgets.

The obstruction and structural rows keep the same boundary public. They record missing gates, exhausted budgets, blocked residuals, failed endpoint seals, failed joins, transport, replay, provenance, and local naming.
\end{proof}

\falsifiablePrediction{Any accepted $\MetaCICResidualNormalFormBudgetRouterUp$ confluence-facing read that exposes $Q$ without first displaying typed residual window $W$, residual-diamond budget $D$, residual-join budget $J$, typed scheduler budget $S$, endpoint seal $E$, obstruction $O$, transport $H$, replay $C$, provenance $P$, and local naming $N$ is outside the carrier surface.}

\independenceWitness{metacic-typed-normal-form-scheduler, metacic-residual-diamond-budget, metacic-residual-join-budget, metacic-normal-endpoint-budget, metacic-candidate-normalization-confluence-handoff: no listed sibling is bijective with $\MetaCICResidualNormalFormBudgetRouterUp$ because this packet jointly carries a typed residual window, residual-diamond budget, residual-join budget, scheduler budget, endpoint seal, candidate-normalization handoff, obstruction, transport, replay, provenance, and local $\NameCert$ rows.}

Lean TasteGate work for $\MetaCICResidualNormalFormBudgetRouterUp$ must supply $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances so the residual window, residual-diamond budget, residual-join budget, scheduler budget, endpoint seal, handoff, obstruction, replay, provenance, and local naming rows remain distinguishable.

\closureat{MetaCICResidualNormalFormBudgetRouterUp}{seedStr}

\begin{closurestatus}{\MetaCICResidualNormalFormBudgetRouterUp}
  \constructivestory{A $\MetaCICResidualNormalFormBudgetRouterUp$ packet is generated from a typed residual window, residual-diamond budget, residual-join budget, typed normal-form scheduler budget, normal-endpoint seal, candidate-normalization confluence handoff, obstruction row, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-residual-normal-form-budget-router-carrier}, \autoref{thm:metacic-residual-normal-form-budget-router-obligations}, and \autoref{thm:metacic-residual-normal-form-budget-router-handoff} seed the finite residual normal-form budget route through typed residual windows, residual-diamond and residual-join budgets, scheduler budgets, endpoint seals, candidate handoff, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close unrestricted residual substitution, global strong normalization, subject reduction, unrestricted residual diamond, quotient syntax equality, evaluator equality, Lean proof obligations, bridge checking, or kernel consistency.}
  \upgradepath{Obligation closure requires checked carrier admission, typed residual-window exposure, residual-diamond budget exposure, residual-join budget exposure, scheduler-budget routing, endpoint sealing, handoff exactness, Nontrivial and FieldFaithful instances, obstruction retention, transport, replay, provenance, and local naming for BEDC.Derived.MetaCICResidualNormalFormBudgetRouterUp.}
\end{closurestatus}
