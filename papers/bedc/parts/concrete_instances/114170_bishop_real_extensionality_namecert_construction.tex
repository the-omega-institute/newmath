\chapter{A Concrete Naming Certificate for $\BishopRealExtensionalityUp$}
\label{ch:concrete-instances-bishop-real-extensionality-namecert}
\origin{human}

Following Bishop and Bridges, equality of constructive reals is read through interchangeable regular presentations rather than through host real equality. The $\BishopRealExtensionalityUp$ packet records the finite NameCert surface by which an accepted $\RealUp$ name may be substituted for an extensionally equal accepted name. It is downstream of the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the comparison boundary of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert}, the tolerance-row discipline of \autoref{ch:concrete-instances-real-modulus-of-convergence-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet supplies Real and Completion consumers with an extensional substitution route, not quotient equality, host real equality, selected representatives, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop real extensionality carrier]
\label{def:bishop-real-extensionality-carrier}
A $\BishopRealExtensionalityUp$ carrier is a finite $\BHist$ packet
$$
E=(S_0,S_1,R_0,R_1,Q,T,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S_0$ and $S_1$ are $\StreamNameUp$ finite-window rows for the source and target real names.
\item $R_0$ and $R_1$ are $\RegSeqRatUp$ readback rows attached to $S_0$ and $S_1$.
\item $Q$ is the $\CauchyEquivalenceSetoidUp$ comparison row relating the two regular readbacks through displayed Cauchy agreement.
\item $T$ is the $\RealModulusOfConvergenceUp$ tolerance ledger used by the comparison and by any substitution request.
\item $A$ is the terminal $\RealUp$ seal row reached only after $S_0,S_1,R_0,R_1,Q,T$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the two stream windows, two regular readbacks, comparison row, tolerance ledger, terminal seal, replay, provenance, and local naming row.
\item $C$ records displayed $\Cont$ replay from an extensional substitution request through finite windows, regular readback, comparison, tolerance, and seal rows.
\item $P$ is the $\Pkg$ provenance over $\BishopRealExtensionalityUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\CauchyEquivalenceSetoidUp$, $\RealModulusOfConvergenceUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopRealExtensionalityUp}$ row.
\end{enumerate}
The classifier compares accepted extensionality packets only through $S_0,S_1,R_0,R_1,Q,T,A,H,C,P,N$. It exposes no quotient equality of streams, host real equality, selected representative, countable-choice schedule, detached limit, or ambient completeness row.
\end{definition}

\begin{theorem}[Bishop real extensionality NameCert obligations]
\label{thm:bishop-real-extensionality-namecert-obligations}
For every accepted $\BishopRealExtensionalityUp$ carrier
$$
E=(S_0,S_1,R_0,R_1,Q,T,A,H,C,P,N),
$$
the local $\NameCert_{\BishopRealExtensionalityUp}$ surface consists exactly of the two $\StreamNameUp$ finite windows, two $\RegSeqRatUp$ readbacks, the $\CauchyEquivalenceSetoidUp$ comparison row, the $\RealModulusOfConvergenceUp$ tolerance ledger, the terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every substitution-facing consumer must expose $S_0,S_1,R_0,R_1,Q,T,A$ before it consumes $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-real-extensionality-carrier}. The analytic rows are exactly $S_0,S_1,R_0,R_1,Q,T,A$, and the remaining rows are structural transport, replay, provenance, and naming.

The two $\StreamNameUp$ rows are finite windows. The two $\RegSeqRatUp$ rows are the only readbacks attached to those windows, so a consumer cannot enter through an infinite stream or a selected representative.

The comparison row $Q$ is displayed before the terminal seal, and it is governed by the tolerance ledger $T$. Thus equality-facing information is read as Cauchy-equivalence evidence over inspected tolerances, not as host real equality or quotient equality.

Finally the seal row $A$ is downstream of the displayed comparison route. Since no carrier coordinate remains outside $S_0,S_1,R_0,R_1,Q,T,A,H,C,P,N$, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Bishop real extensionality substitution route]
\label{thm:bishop-real-extensionality-substitution-route}
Every Real or Completion consumer that substitutes one accepted $\RealUp$ name for another through a $\BishopRealExtensionalityUp$ packet must factor the substitution through
$$
\StreamNameUp
\longmapsto
\RegSeqRatUp
\longmapsto
\CauchyEquivalenceSetoidUp
\longmapsto
\RealModulusOfConvergenceUp
\longmapsto
\RealUp .
$$
The consumer may replay the substitution along componentwise $\hsame$ and displayed $\Cont$ rows, with $\Pkg$ provenance and local $\NameCert$ data, but it receives no quotient stream equality, host real equality, selected representative, countable choice, detached limit, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with a substitution-facing read of $E$. The only source observations are the finite windows $S_0,S_1$, so the route starts in the $\StreamNameUp$ surface.

Read those windows through $R_0,R_1$. These are the $\RegSeqRatUp$ readbacks attached to the displayed observations, and no other readback coordinate occurs in the carrier.

Now inspect the equality-facing row. The comparison $Q$ is a $\CauchyEquivalenceSetoidUp$ row, and the tolerance ledger $T$ records the $\RealModulusOfConvergenceUp$ budget used by the comparison before the terminal seal can be consumed.

The terminal $\RealUp$ row $A$ is therefore reached only after the finite extensionality route is visible. The rows $H,C,P,N$ transport, replay, source, and name that same route, so substitution exports the displayed finite evidence and none of the excluded host principles.
\end{proof}

\closureat{BishopRealExtensionalityUp}{seedStr}

\begin{closurestatus}{\BishopRealExtensionalityUp}
  \constructivestory{A $\BishopRealExtensionalityUp$ packet is generated from two StreamName finite windows, two RegSeqRat readbacks, a CauchyEquivalenceSetoid comparison row, a RealModulusOfConvergence tolerance ledger, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-real-extensionality-carrier}, \autoref{thm:bishop-real-extensionality-namecert-obligations}, and \autoref{thm:bishop-real-extensionality-substitution-route} seed the constructive Real extensional substitution route over finite windows, regular readbacks, comparison, tolerance, seal, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, host real equality, selected representatives, countable choice, detached limits, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-window exposure, regular readback, Cauchy-equivalence comparison, tolerance-ledger exactness, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BishopRealExtensionalityUp.}
\end{closurestatus}
