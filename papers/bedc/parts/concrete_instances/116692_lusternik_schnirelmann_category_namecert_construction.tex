\chapter{A Concrete Naming Certificate for $\LusternikSchnirelmannCategoryUp$}
\label{ch:concrete-instances-lusternik-schnirelmann-category-namecert}
\origin{human}

Following Lusternik and Schnirelmann, the $\LusternikSchnirelmannCategoryUp$ packet records finite categorical-cover data as a BEDC NameCert surface. It sits beside the finite antipodal obstruction route of \autoref{ch:concrete-instances-borsuk-ulam-namecert}, the combinatorial certificates of \autoref{ch:concrete-instances-sperner-lemma-namecert} and \autoref{ch:concrete-instances-tucker-lemma-namecert}, the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the finite face inventory of \autoref{ch:concrete-instances-simplicialcomplex-namecert}, and the compact finite-cover route of \autoref{ch:concrete-instances-finite-cover-star-refinement-modulus-namecert}. The packet names a finite cover by null-homotopy rows and categorical budget ledgers; it does not import a host homotopy category, selected global contractions, quotient sphere equality, arbitrary open-cover compactness, or a classical critical-point theorem.

\begin{definition}[Lusternik--Schnirelmann category carrier]
\label{def:lusternik-schnirelmann-category-carrier}
A $\LusternikSchnirelmannCategoryUp$ carrier is a finite $\BHist$ packet
$$
L=(X,K,U,A,M,S,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the compact source row, read through $\CompactMetricUp$ finite-cover data and the finite source model used by the downstream consumer.
\item $K$ is the $\SimplicialComplexUp$ support row when the source is presented by a finite complex, including the face inventory and incidence rows used to form nerves of covers.
\item $U$ is the finite categorical cover row, listing the displayed cover members and the membership evidence inspected by the packet.
\item $A$ is the null-homotopy admission row for each displayed cover member, recording the finite contraction certificate visible to the same source.
\item $M$ is the multiplicity and budget row, recording the displayed upper bound for the number of categorical pieces used by $U$.
\item $S$ is the sphere or antipodal-obstruction handoff row, reading the Borsuk--Ulam, Sperner, and Tucker sibling packets only as finite obstruction consumers.
\item $O$ is the lower-obstruction row, recording that a requested category reduction is refused by the displayed antipodal or combinatorial obstruction route.
\item $H$ records componentwise $\hsame$ transport for compact source, simplicial support, cover, null-homotopy, budget, obstruction, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from finite-cover inspection through null-homotopy rows and obstruction rows to the category budget.
\item $P$ is the $\Pkg$ provenance over $\LusternikSchnirelmannCategoryUp$, $\BorsukUlamUp$, $\SpernerLemmaUp$, $\TuckerLemmaUp$, $\CompactMetricUp$, $\SimplicialComplexUp$, $\FiniteCoverStarRefinementModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LusternikSchnirelmannCategoryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,K,U,A,M,S,O,H,C,P,N$. It has no coordinate for a completed homotopy category, arbitrary open covers, choice-selected contractions, quotient equality of spheres or maps, unrestricted critical-point theory, or ambient compactness.
\end{definition}

\begin{theorem}[Lusternik--Schnirelmann category NameCert obligations]
\label{thm:lusternik-schnirelmann-category-namecert-obligations}
For every accepted $\LusternikSchnirelmannCategoryUp$ carrier $L=(X,K,U,A,M,S,O,H,C,P,N)$, the local $\NameCert_{\LusternikSchnirelmannCategoryUp}$ surface consists exactly of compact source admission, optional finite simplicial support admission, finite categorical cover exposure, null-homotopy admission for displayed cover members, categorical budget exposure, antipodal or combinatorial obstruction handoff, lower-obstruction refusal, componentwise transport, displayed replay, provenance, and local naming. No local obligation is discharged by an ambient homotopy category, selected global contraction, arbitrary open cover, quotient sphere equality, or classical critical-point theorem.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:lusternik-schnirelmann-category-carrier}. The source-facing rows are $X$ and $K$, the categorical-cover rows are $U,A,M$, and the obstruction-facing rows are $S,O$.

The NameCert surface first admits the source. The compact metric row $X$ supplies the finite cover data, while $K$ supplies the finite simplicial support when a nerve or face inventory is used by the consumer.

The cover route is then rowwise. The finite cover row $U$ lists only displayed members, the row $A$ records the null-homotopy data attached to those members, and the budget row $M$ records the category bound read from the same finite list.

Finally $S,O,H,C,P,N$ bind the obstruction, replay, provenance, and local name to that same packet. Since the excluded host objects are not coordinates of $L$, they cannot become additional local NameCert obligations.
\end{proof}

\begin{theorem}[Finite categorical-cover route]
\label{thm:lusternik-schnirelmann-category-finite-cover-route}
Every accepted $\LusternikSchnirelmannCategoryUp$ consumer that reads a categorical upper bound must factor through
$$
\CompactMetricUp,\quad \SimplicialComplexUp,\quad U,\quad A,\quad M
$$
before the category budget is exposed. The route may reuse the finite-cover star-refinement and compact-metric sibling packets, but it exports no arbitrary open-cover compactness theorem, no selected contraction outside the displayed cover, no quotient equality of maps, and no ambient homotopy-category object.
\end{theorem}
\begin{proof}
Begin with the source rows. The compact metric row $X$ supplies finite cover data, and the simplicial row $K$ supplies only the displayed face or nerve inventory used by the consumer.

Read the cover rows in order. The row $U$ exposes the finite family of categorical pieces, $A$ attaches the null-homotopy admission data to each displayed piece, and $M$ records the resulting budget.

The structural rows $H,C,P,N$ transport, replay, source, and name the same route. Because the carrier lacks arbitrary open-cover, quotient-map, selected-contraction, and ambient homotopy-category coordinates, the upper-bound read remains exactly the finite $X,K,U,A,M,H,C,P,N$ surface.
\end{proof}

\begin{theorem}[Sphere consumer handoff]
\label{thm:lusternik-schnirelmann-category-sphere-consumer-handoff}
When an accepted $\LusternikSchnirelmannCategoryUp$ packet is consumed by a sphere or antipodal-obstruction argument, the handoff reads the sibling packets of \autoref{ch:concrete-instances-borsuk-ulam-namecert}, \autoref{ch:concrete-instances-sperner-lemma-namecert}, and \autoref{ch:concrete-instances-tucker-lemma-namecert} only through the displayed obstruction row $S$ and lower-refusal row $O$. Thus a sphere-facing consumer receives a finite categorical-cover obstruction surface, not a host sphere theorem, quotient antipodal space, selected critical point, or unrestricted LS critical-value theorem.
\end{theorem}
\begin{proof}
Project $S$ and $O$ from the carrier. The handoff row $S$ names the antipodal and combinatorial sibling packets as finite obstruction consumers, while $O$ records the refusal of a smaller categorical cover only inside the same displayed packet.

The Borsuk--Ulam, Sperner, and Tucker chapters contribute routes already expressed as finite NameCert packets. They do not supply quotient sphere equality, selected antipodal points, or unrestricted critical-point data to this carrier.

Consequently the sphere-facing read is constrained by $X,K,U,A,M,S,O,H,C,P,N$. Transport, replay, provenance, and local naming keep the obstruction tied to the finite categorical cover rather than to an ambient topological theorem.
\end{proof}

\falsifiablePrediction{Every accepted $\LusternikSchnirelmannCategoryUp$ consumer that claims a category budget or obstruction must display compact source data, finite cover rows, null-homotopy admission rows, a budget row, obstruction handoff rows, transport, replay, provenance, and local NameCert data before the read is public.}

\independenceWitness{borsuk-ulam, sperner-lemma, tucker-lemma, compactmetric, simplicialcomplex, finite-cover-star-refinement-modulus: $\LusternikSchnirelmannCategoryUp$ is not bijective to any listed sibling because it jointly carries categorical cover rows, null-homotopy admissions, budget data, antipodal or combinatorial obstruction handoff, transport, replay, provenance, and local naming in one finite packet.}

\closureat{LusternikSchnirelmannCategoryUp}{seedStr}

\begin{closurestatus}{\LusternikSchnirelmannCategoryUp}
  \constructivestory{A $\LusternikSchnirelmannCategoryUp$ packet is generated from compact source rows, optional finite simplicial support rows, finite categorical cover rows, null-homotopy admission rows, categorical budget rows, antipodal or combinatorial obstruction handoff, lower-obstruction refusal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:lusternik-schnirelmann-category-carrier}, \autoref{thm:lusternik-schnirelmann-category-namecert-obligations}, \autoref{thm:lusternik-schnirelmann-category-finite-cover-route}, and \autoref{thm:lusternik-schnirelmann-category-sphere-consumer-handoff} seed the finite categorical-cover packet over compact, simplicial, antipodal, combinatorial, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, ambient homotopy categories, arbitrary open-cover compactness, selected global contractions, quotient sphere equality, unrestricted critical-point theory, or classical LS critical-value theorems.}
  \upgradepath{Obligation closure requires checked carrier admission, finite categorical cover exposure, null-homotopy admission, budget exactness, obstruction handoff, lower-refusal stability, transport, replay, provenance, and local naming for BEDC.Derived.LusternikSchnirelmannCategoryUp.}
\end{closurestatus}
