\chapter{A Concrete Naming Certificate for $\BoundaryShadowExpansionCocycleUp$}
\label{ch:concrete-instances-boundary-shadow-expansion-cocycle-namecert}
\origin{ai}

The $\BoundaryShadowExpansionCocycleUp$ packet realizes the boundary-shadow and observer-expansion route of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite cocycle-level NameCert data. It sits after the boundary-shadow route of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the observer-shadow metric of \autoref{ch:concrete-instances-hyperbolic-observer-shadow-metric-namecert}, the Busemann cocycle of \autoref{ch:concrete-instances-hyperbolic-busemann-cocycle-namecert}, the horosphere-flow ledger of \autoref{ch:concrete-instances-horosphere-flow-ledger-namecert}, and the observer-expansion packet of \autoref{ch:concrete-instances-hyperbolic-observer-expansion-namecert}. The chapter names a finite expansion cocycle over displayed boundary and horosphere rows; it is not a completed hyperbolic boundary, quotient geodesic ray space, physical observer dynamics, selected ideal point, or ambient manifold theorem.

\begin{definition}[Boundary-shadow expansion cocycle carrier]
\label{def:boundary-shadow-expansion-cocycle-carrier}
A $\BoundaryShadowExpansionCocycleUp$ carrier is a finite $\BHist$ packet
$$
X=(S,O,B,F,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\HyperbolicBoundaryShadowUp$ row recording the visible boundary shadow through which the cocycle is read.
\item $O$ is the $\HyperbolicObserverShadowMetricUp$ row recording the observer-facing shadow metric comparison for the same boundary read.
\item $B$ is the $\HyperbolicBusemannCocycleUp$ row recording the additive Busemann comparison consumed by the expansion read.
\item $F$ is the $\HorosphereFlowLedgerUp$ row recording the horosphere-flow level through which boundary and Busemann rows are replayed.
\item $E$ is the $\HyperbolicObserverExpansionUp$ row recording the displayed observer-expansion magnitude or direction attached to $S,O,B,F$.
\item $H$ records componentwise $\hsame$ transport for boundary shadow, observer-shadow metric, Busemann cocycle, horosphere-flow ledger, observer expansion, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through boundary-shadow admission, observer-shadow metric readback, Busemann cocycle comparison, horosphere-flow ledger inspection, and observer-expansion read.
\item $P$ is the $\Pkg$ provenance over $\BoundaryShadowExpansionCocycleUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicObserverShadowMetricUp$, $\HyperbolicBusemannCocycleUp$, $\HorosphereFlowLedgerUp$, $\HyperbolicObserverExpansionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\BoundaryShadowExpansionCocycleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,O,B,F,E,H,C,P,N$. It has no coordinate for completed boundary points, quotient geodesic rays, a hidden expansion selector, physical observer dynamics, selected ideal endpoints, or ambient hyperbolic manifold data.
\end{definition}

\begin{theorem}[Boundary-shadow expansion cocycle NameCert obligations]
\label{thm:boundary-shadow-expansion-cocycle-namecert-obligations}
For every accepted $\BoundaryShadowExpansionCocycleUp$ carrier
$$
X=(S,O,B,F,E,H,C,P,N),
$$
the local $\NameCert_{\BoundaryShadowExpansionCocycleUp}$ surface consists exactly of boundary-shadow admission, observer-shadow metric readback, Busemann cocycle comparison, horosphere-flow ledger inspection, observer-expansion read, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Any accepted expansion read that omits either the boundary-shadow row $S$ or the Busemann cocycle row $B$ is outside the packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:boundary-shadow-expansion-cocycle-carrier}. The boundary-facing rows are $S$ and $O$, while the cocycle and horosphere-flow rows are $B$ and $F$.

The expansion row $E$ is downstream from those four rows. It can be read only after the visible boundary shadow, observer-shadow metric, Busemann cocycle, and horosphere-flow ledger have all been displayed.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier has no coordinate for completed boundary data, quotient geodesic rays, hidden selectors, physical observer dynamics, selected endpoints, or ambient manifold structure, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Boundary-shadow expansion cocycle dependency route]
\label{thm:boundary-shadow-expansion-cocycle-dependency-route}
Every accepted $\BoundaryShadowExpansionCocycleUp$ read factors through
$$
\HyperbolicBoundaryShadowUp,\ \HyperbolicObserverShadowMetricUp,\ \HyperbolicBusemannCocycleUp,\ \HorosphereFlowLedgerUp,\ \HyperbolicObserverExpansionUp
$$
before a consumer may inspect the expansion cocycle as a local NameCert row. The route is finite and preserves the boundary-shadow row and Busemann cocycle row as separate coordinates.
\end{theorem}
\begin{proof}
Use \autoref{thm:boundary-shadow-expansion-cocycle-namecert-obligations}. It places the boundary-shadow row, observer-shadow metric row, Busemann cocycle row, and horosphere-flow ledger before the expansion row.

The cocycle dependency is not reducible to one sibling. The boundary-shadow row supplies the boundary face, the observer-shadow metric supplies the observer-facing comparison, the Busemann row supplies the additive cocycle read, and the horosphere-flow row supplies the level ledger.

Only after those rows are present can $E,H,C,P,N$ expose the expansion cocycle as a local packet. Therefore a consumer cannot collapse the route to a single hyperbolic object or bypass the separate boundary-shadow and Busemann coordinates.
\end{proof}

\falsifiablePrediction{Any accepted $\BoundaryShadowExpansionCocycleUp$ packet is refuted if the expansion read omits either the boundary-shadow row $S$ or the Busemann cocycle row $B$.}

\independenceWitness{hyperbolic\_boundary\_shadow, hyperbolic\_observer\_shadow\_metric, hyperbolic\_busemann\_cocycle, horosphere\_flow\_ledger, hyperbolic\_observer\_expansion: none is bijective with $\BoundaryShadowExpansionCocycleUp$ because the packet jointly carries boundary shadow, observer-shadow metric, Busemann cocycle, horosphere-flow level, observer expansion, transport, replay, provenance, and local naming.}

The Lean TasteGate route for this packet must provide \texttt{Nontrivial BoundaryShadowExpansionCocycleUp} and \texttt{FieldFaithful BoundaryShadowExpansionCocycleUp} instances in \texttt{lean4/BEDC/Derived/BoundaryShadowExpansionCocycleUp/TasteGate.lean}. Those instances must preserve $S,O,B,F,E,H,C,P,N$ as distinct fields before a checked carrier-alignment theorem can serve as the TasteGate witness.

\closureat{BoundaryShadowExpansionCocycleUp}{seedStr}

\begin{closurestatus}{\BoundaryShadowExpansionCocycleUp}
  \constructivestory{A $\BoundaryShadowExpansionCocycleUp$ packet is generated from HyperbolicBoundaryShadow rows, HyperbolicObserverShadowMetric rows, HyperbolicBusemannCocycle rows, HorosphereFlowLedger rows, HyperbolicObserverExpansion rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:boundary-shadow-expansion-cocycle-carrier}, \autoref{thm:boundary-shadow-expansion-cocycle-namecert-obligations}, and \autoref{thm:boundary-shadow-expansion-cocycle-dependency-route} seed the finite boundary-shadow expansion cocycle route over boundary, observer-shadow, Busemann, horosphere-flow, expansion, provenance, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed hyperbolic boundaries, quotient geodesic ray spaces, physical observer dynamics, selected ideal endpoints, ambient manifold theorems, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, boundary-shadow exactness, observer-shadow metric readback, Busemann cocycle separation, horosphere-flow routing, observer-expansion exposure, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.BoundaryShadowExpansionCocycleUp.}
\end{closurestatus}
