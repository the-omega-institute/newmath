\chapter{A Concrete Naming Certificate for $\CompactUniformLimitUp$}
\label{ch:concrete-instances-compact-uniform-limit-namecert}
\origin{human}

Following Bishop and Bridges on compact-source uniform convergence, the $\CompactUniformLimitUp$ packet realizes the compact-uniform-continuity program of \autoref{ch:visions-compact-uniform-continuity} as a finite NameCert carrier. It sits over the compact uniform-modulus row of \autoref{ch:concrete-instances-compact-uniform-modulus-namecert}, the continuous-map interface of \autoref{ch:concrete-instances-continuousmap-namecert}, the theorem handoff of \autoref{ch:concrete-instances-uniform-limit-theorem-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names compact-source uniform limits through displayed finite moduli and readback rows; it does not import open-cover compactness, quotient equality of functions, selected host limits, or ambient $\RealUp$ completeness.

\begin{definition}[Compact uniform-limit carrier]
\label{def:compact-uniform-limit-carrier}
A $\CompactUniformLimitUp$ carrier is a finite $\BHist$ packet
$$
U=(M,F,T,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\CompactUniformModulusUp$ row, carrying the compact-source finite modulus used by every tolerance request.
\item $F$ is the $\ContinuousMapUp$ row for the displayed family or limiting map whose graph data are visible to the packet.
\item $T$ is the $\UniformLimitTheoremUp$ handoff row, recording the finite continuity inheritance route available after uniform convergence is displayed.
\item $W$ is the finite observation-window row for compact-source points, function stages, modulus requests, and limit-value reads.
\item $R$ is the $\RegSeqRatUp$ readback row for the rational comparison windows generated from $M,F,T,W$.
\item $E$ is the terminal $\RealUp$ seal row reached only after the compact modulus, continuous-map, theorem, window, and regular-rational rows have been displayed.
\item $H$ records componentwise $\hsame$ transport for the compact-modulus, continuous-map, theorem, window, readback, real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from compact-source uniform-limit requests through $M,F,T,W,R,E,H$.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformLimitUp$, $\CompactUniformModulusUp$, $\ContinuousMapUp$, $\UniformLimitTheoremUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformLimitUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,F,T,W,R,E,H,C,P,N$. It has no coordinate for an open-cover compactness theorem, quotient function equality, selected limit function, host continuity theorem, or ambient Real completeness.
\end{definition}

\begin{theorem}[Compact uniform-limit NameCert obligations]
\label{thm:compact-uniform-limit-namecert-obligations}
For every accepted $\CompactUniformLimitUp$ carrier $U=(M,F,T,W,R,E,H,C,P,N)$, the local $\NameCert_{\CompactUniformLimitUp}$ surface consists exactly of carrier admission, compact uniform-modulus exposure, continuous-map graph visibility, uniform-limit theorem handoff, finite observation-window locality, regular-rational readback, terminal $\RealUp$ seal nonescape, componentwise transport, displayed replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-limit-carrier}. The accepted packet exposes exactly the compact modulus, continuous-map, theorem handoff, finite windows, regular-rational reads, real seal, transport, replay, provenance, and local naming rows.

The compact part is $M$. A tolerance request over the compact source becomes public only as a finite modulus row and is read together with the graph data in $F$ and the inspected windows in $W$.

The continuity handoff is $T$. It can be consumed only after the compact modulus and continuous-map rows have been displayed, and its Real-facing values pass through $R$ before the terminal seal $E$ is read.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since no row stores open-cover compactness, quotient function equality, a selected host limit, host continuity, or ambient Real completeness, the listed obligations exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Compact uniform-limit continuity handoff]
\label{thm:compact-uniform-limit-continuity-handoff}
Every continuity read exported by an accepted $\CompactUniformLimitUp$ carrier factors through
$$
\CompactUniformModulusUp \longmapsto \ContinuousMapUp \longmapsto \UniformLimitTheoremUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
with the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows retained. The handoff exports compact-source uniform convergence as finite modulus, continuous-map, regular-rational readback, and Real-seal data, not as a host compactness theorem, quotient function equality, selected limit, or ambient completeness principle.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:compact-uniform-limit-namecert-obligations}. It forces every accepted read to expose the compact uniform modulus $M$, the continuous-map row $F$, the theorem handoff $T$, finite windows $W$, regular-rational readback $R$, and terminal real seal $E$.

The continuity read is therefore downstream of the compact modulus and the graph row. The theorem handoff $T$ may state the continuity inheritance route, but only as a displayed finite row inside the same packet.

The Real-facing values are sealed last. The regular-rational row $R$ supplies the comparison windows before $E$ is consumed, while $H,C,P,N$ preserve the route under transport, replay, provenance, and local naming. No excluded host compactness, quotient equality, selected limit, or ambient completeness coordinate is available.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactUniformLimitUp$ read exposes a CompactUniformModulus row, ContinuousMap row, UniformLimitTheorem handoff, finite observation windows, RegSeqRat readback, terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row before any consumer reads compact uniform-limit continuity.}

\independenceWitness{compact-uniform-modulus, continuousmap, uniform-limit-theorem, regseqrat, real: no listed sibling is bijective with $\CompactUniformLimitUp$ because this packet jointly binds compact-source finite modulus data, continuous-map graph rows, uniform-limit theorem handoff, finite windows, regular rational readback, terminal Real seal, transport, replay, provenance, and local naming.}

\closureat{CompactUniformLimitUp}{seedStr}

\begin{closurestatus}{\CompactUniformLimitUp}
  \constructivestory{A $\CompactUniformLimitUp$ packet is generated from a CompactUniformModulus row, ContinuousMap graph data, UniformLimitTheorem handoff, finite observation windows, RegSeqRat readback, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-limit-carrier}, \autoref{thm:compact-uniform-limit-namecert-obligations}, and \autoref{thm:compact-uniform-limit-continuity-handoff} seed the compact-source uniform-limit route over CompactUniformModulus, ContinuousMap, UniformLimitTheorem, RegSeqRat, Real, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, quotient equality of functions, selected host limits, host continuity theorems, ambient Real completeness, Lean proof obligations, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact uniform-modulus exposure, continuous-map graph visibility, uniform-limit theorem handoff, window locality, RegSeqRat readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformLimitUp.}
\end{closurestatus}
