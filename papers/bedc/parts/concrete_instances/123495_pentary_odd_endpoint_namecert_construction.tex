\chapter{A Concrete Naming Certificate for $\PentaryOddEndpointUp$}
\label{ch:concrete-instances-pentary-odd-endpoint-namecert}
\origin{ai}

The $\PentaryOddEndpointUp$ packet realizes the odd-endpoint branch of \autoref{ch:visions-innovation-input-pentary-odd-endpoint-exclusion} as finite NameCert data over the anchored pentary three-link obstruction. It sits beside the pentary endpoint gate, the palindromic Hermitian reduction of \autoref{ch:concrete-instances-pentary-palindromic-hermitian-reduction-namecert}, the mirror-deficit carrier of \autoref{ch:concrete-instances-pentary-mirror-deficit-namecert}, the finite RP boundary row of \autoref{ch:concrete-instances-finite-window-rp-boundary-namecert}, and the real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite odd-endpoint exclusion route; it does not assert an even-endpoint exclusion, a completed RP classification, host spectral data, or a Hilbert-space quotient.

\begin{definition}[Pentary odd endpoint carrier]
\label{def:pentary-odd-endpoint-carrier}
A $\PentaryOddEndpointUp$ carrier is a finite $\BHist$ packet
$$
P=(Q,E,H,D,M,R,B,T,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the anchored pentary $\mathcal Q_5$ row naming the five visible unit-phase coordinates and the $L=3$ neutral RP request.
\item $E$ is the odd-endpoint row, recording the displayed branch equation $v_4=-v_0$.
\item $H$ is the Hermitian first-correlation row, recording the finite witness that the relevant first reflected mean is real.
\item $D$ is the middle-link diagonal row, recording the endpoint dichotomy data used before the odd branch is inspected.
\item $M$ is the finite $D_{13}$ positivity-gate row.
\item $R$ is the real-bound readback row, recording that the odd-endpoint first-correlation value is too small for the displayed positivity gate.
\item $B$ is the branch-refusal ledger, recording the rejection of the odd endpoint branch.
\item $T$ records componentwise $\hsame$ transport for the pentary source, endpoint branch, Hermitian row, diagonal row, positivity gate, real-bound readback, refusal ledger, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from every odd-endpoint query through $Q,E,H,D,M,R,B,T,G,N$.
\item $G$ is the $\Pkg$ provenance over $\PentaryOddEndpointUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, $\Nontrivial$, and $\FieldFaithful$ rows.
\item $N$ is the local $\NameCert_{\PentaryOddEndpointUp}$ row.
\end{enumerate}
Future Lean realization of this carrier must provide $\Nontrivial\ \PentaryOddEndpointUp$ and $\FieldFaithful\ \PentaryOddEndpointUp$ TasteGate instances before any checked carrier-alignment theorem can serve as public evidence.
\end{definition}
\leanstmt{BEDC.Derived.PentaryOddEndpointUp}

\begin{theorem}[Pentary odd endpoint NameCert obligations]
\label{thm:pentary-odd-endpoint-namecert-obligations}
For every accepted $\PentaryOddEndpointUp$ carrier
$$
P=(Q,E,H,D,M,R,B,T,C,G,N),
$$
the local $\NameCert_{\PentaryOddEndpointUp}$ surface consists exactly of anchored pentary source admission through $Q$, odd-endpoint branch exposure through $E$, Hermitian realness through $H$, middle-link diagonal exposure through $D$, $D_{13}$ positivity-gate visibility through $M$, real-bound readback through $R$, branch refusal through $B$, componentwise transport through $T$, displayed replay through $C$, provenance through $G$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the finite carrier. The rows $Q,E,H,D,M,R$ display the pentary source, the odd endpoint branch, the Hermitian realness constraint, the middle-link diagonal, the positivity gate, and the real-bound readback before the branch-refusal row is available.

The refusal row $B$ is therefore not a free narrative label. It is downstream of the visible odd endpoint equation and the displayed conflict between Hermitian realness and the $D_{13}$ positivity gate.

The transport, replay, provenance, and naming rows $T,C,G,N$ preserve the same finite route. Because the carrier has no even-endpoint exclusion, global RP classifier, host spectral coordinate, or Hilbert-space quotient field, none of those can be read as a local NameCert obligation.
\end{proof}

\falsifiablePrediction{Any accepted odd-endpoint exclusion read that reaches the refusal ledger without displaying the anchored pentary source, odd endpoint equation, Hermitian first-correlation row, middle-link diagonal, $D_{13}$ positivity gate, and real-bound readback refutes the $\PentaryOddEndpointUp$ carrier surface.}

\independenceWitness{pentary-palindromic-hermitian-reduction, pentary-mirror-deficit, finite-window-rp-boundary: none is bijective to $\PentaryOddEndpointUp$ because this packet binds the odd endpoint equation, Hermitian realness, middle-link diagonal, positivity gate, real-bound readback, branch refusal, TasteGate-facing nontriviality and field-faithfulness requirements, transport, replay, provenance, and local NameCert rows.}

\closureat{PentaryOddEndpointUp}{seedStr}

\begin{closurestatus}{\PentaryOddEndpointUp}
  \constructivestory{A $\PentaryOddEndpointUp$ packet is generated from an anchored pentary source row, an odd endpoint branch row, Hermitian first-correlation realness, a middle-link diagonal row, a finite D13 positivity gate, real-bound readback, branch refusal, TasteGate-facing Nontrivial and FieldFaithful requirements, hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{This chapter seeds the finite carrier and NameCert obligations for the pentary odd endpoint exclusion route.}
  \formalstatus{\formalTargetV}
  \leantarget{BEDC.Derived.PentaryOddEndpointUp}
  \bridgestatus{none}
  \notclaimed{It does not close the even endpoint branch, the full pentary RP class, host spectral data, Hilbert-space quotients, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, branch exposure, Hermitian realness, diagonal exposure, positivity-gate visibility, real-bound readback, refusal-ledger routing, transport, replay, provenance, and local naming for BEDC.Derived.PentaryOddEndpointUp.}
\end{closurestatus}
