\chapter{A Concrete Naming Certificate for $\CellularSubstrateUp$}
\label{ch:concrete-instances-cellularsubstrate-namecert}
\origin{ai}

The $\CellularSubstrateUp$ packet packages the carrier alternative of \autoref{ch:visions-cellular-substrate-as-carrier-alternative} as a finite $\BHist$ NameCert surface. It sits between the rule-table and finite-orbit emphasis of \autoref{ch:concrete-instances-cellularautomaton-namecert} and the audit-ledger substrate of \autoref{ch:concrete-instances-cellulartrustsubstrate-namecert}. Its distinguishing role is to certify the cellular carrier itself: a displayed local row, neighbourhood window, local update route, global-step ledger, transport surface, continuation surface, provenance, and local NameCert row are all present at once.

\falsifiablePrediction{A packet with the same global-step ledger but no displayed neighbourhood window is not an accepted $\CellularSubstrateUp$ carrier.}

\independenceWitness{Nearest siblings: cellularautomaton, cellulartrustsubstrate, streamname, interhistlocalityledger, and finitewindowenvelope; the carrier is not bijective to any of them because it simultaneously requires a local neighbourhood window, local update route, global-step ledger, transport rows, and local NameCert row, while each sibling omits at least one of those displayed coordinates.}

\begin{definition}[Cellular substrate carrier]
\label{def:cellular-substrate-carrier}
A $\CellularSubstrateUp$ carrier is a finite $\BHist$ packet
$$
Q=(S,N,U,G,H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the cell-state row, naming the finite row segment visible to the carrier.
\item $N$ is the neighbourhood-window row, naming the finite cells read around each displayed cell.
\item $U$ is the local update route, recording the stated local transition from a neighbourhood window to the next cell-state row.
\item $G$ is the global-step ledger, listing the finite sequence of substrate steps admitted by the carrier.
\item $H$ records componentwise $\hsame$ transports for the cell-state, neighbourhood, local-update, global-ledger, continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ routes through the cell-state row, neighbourhood row, local update route, global-step ledger, and local NameCert row.
\item $P$ is the $\Pkg$ provenance over $\CellularSubstrateUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, the cellular-carrier vision, and the sibling cellular NameCert chapters.
\item $L$ is the local $\NameCert_{\CellularSubstrateUp}$ row for carrier admission, classifier accountability, stability, exactness, ledger replay, and non-escape.
\end{enumerate}
The local classifier compares accepted carriers only by these displayed rows. It has no coordinate for host cellular automaton equality, an infinite grid, a hidden scheduler, a detached global evaluator, or an unbounded update oracle.
\end{definition}
\leanchecked{BEDC.Derived.CellularSubstrateUp.CellularSubstrateTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Cellular substrate NameCert obligations]
\label{thm:cellular-substrate-namecert-obligations}
For the carrier of \autoref{def:cellular-substrate-carrier}, the local $\NameCert_{\CellularSubstrateUp}$ surface consists exactly of five obligation rows:
\begin{enumerate}
\item carrier exposure for $Q=(S,N,U,G,H,C,P,L)$;
\item classifier accountability for reads that compare cell-state rows, neighbourhood windows, and local update routes only through $S,N,U$;
\item stability under the componentwise $\hsame$ transports displayed in $H$;
\item exactness of local update and global-step replay through $U,G,C$;
\item ledger and provenance closure through $G,P,L$, with no export of hidden host equality, infinite-grid reasoning, scheduler state, or unbounded update oracle.
\end{enumerate}
Every accepted $\CellularSubstrateUp$ consumer must pass through these five rows before citing a cellular carrier read.
\end{theorem}
\begin{proof}
Carrier exposure is projection from \autoref{def:cellular-substrate-carrier}. The tuple displays the cell-state row, neighbourhood window, local update route, global-step ledger, transports, continuations, provenance, and local naming row as its only coordinates.

Classifier accountability is local to $S,N,U$. A cell-state comparison or update read is accepted only when the read names the displayed neighbourhood window and the displayed local transition route.

Stability is supplied by $H$, while exactness is supplied by $U,G,C$. The transport row compares only the visible coordinates, and the continuation row replays only the finite local update route and finite global-step ledger.

Finally $P$ and $L$ package the same finite route as the public naming surface. Since the carrier lacks coordinates for host equality, an infinite grid, a hidden scheduler, and an unbounded update oracle, the five obligation rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Cellular substrate local-update non-escape]
\label{thm:cellular-substrate-local-update-nonescape}
Every accepted local-update read exported by a $\CellularSubstrateUp$ carrier factors through the finite route
$$
S \longmapsto N \longmapsto U \longmapsto G \longmapsto H \longmapsto C \longmapsto P \longmapsto L.
$$
In particular, an accepted packet exposes no host cellular automaton equality, infinite grid, hidden scheduler, or unbounded update oracle.
\end{theorem}
\begin{proof}
Start with an accepted local-update read. By \autoref{def:cellular-substrate-carrier}, the only available rows are $S,N,U,G,H,C,P,L$.

Apply \autoref{thm:cellular-substrate-namecert-obligations}. The read must use the displayed neighbourhood window, the displayed local update route, and the finite global-step ledger before it can be named by the local $\NameCert_{\CellularSubstrateUp}$ row.

The route has no coordinate for host cellular automaton equality, infinite grid data, hidden scheduler state, or an unbounded update oracle. Thus every accepted read remains a finite cellular-substrate carrier read.
\end{proof}

\begin{theorem}[Cellular substrate window exposure obligation]
\label{thm:cellular-substrate-window-exposure-obligation}
Every accepted $\CellularSubstrateUp$ carrier exposes the neighbourhood-window row $N$ before any accepted local update route $U$ can be replayed. Equivalently, a local-update read of
$$
Q=(S,N,U,G,H,C,P,L)
$$
must pass through the displayed $\BHist$ window row, the local $\hsame$ transport row, the displayed $\Cont$ route, the $\Pkg$ provenance, and the local $\NameCert_{\CellularSubstrateUp}$ row before it cites $U$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cellular-substrate-carrier}. The tuple displays $N$ as a separate neighbourhood-window row and $U$ as the local update route, so the update route has no independent host-side source.

Apply \autoref{thm:cellular-substrate-namecert-obligations}. Its carrier-exposure and classifier-accountability rows require accepted reads to name the displayed neighbourhood window before they name the displayed transition route.

Finally use \autoref{thm:cellular-substrate-local-update-nonescape}. The only replay route is the finite path through $S,N,U,G,H,C,P,L$, hence a local update cannot bypass $N$ or acquire an infinite-grid, scheduler, or host evaluator coordinate.
\end{proof}

\begin{theorem}[Cellular substrate classifier stability obligation]
\label{thm:cellular-substrate-classifier-stability-obligation}
Componentwise $\hsame$ transport in the row $H$ preserves the $\CellularSubstrateUp$ classifier on the cell-state row $S$, neighbourhood-window row $N$, local update route $U$, global-step ledger $G$, continuation row $C$, provenance row $P$, and local $\NameCert_{\CellularSubstrateUp}$ row $L$. No transported read can replace these rows by host cellular automaton equality, an infinite grid, a hidden scheduler, or an unbounded update oracle.
\end{theorem}
\begin{proof}
Project $Q=(S,N,U,G,H,C,P,L)$ through \autoref{def:cellular-substrate-carrier}. The transport coordinate $H$ is declared componentwise for exactly the displayed cell-state, neighbourhood, update, ledger, continuation, provenance, and naming rows.

Use \autoref{thm:cellular-substrate-namecert-obligations}. Its stability row is local to those same displayed coordinates and therefore transports the classifier without creating another equality surface.

The non-escape theorem \autoref{thm:cellular-substrate-local-update-nonescape} rules out every hidden host evaluator, infinite grid, scheduler, and unbounded update oracle after transport. Thus classifier stability remains a $\BHist/\hsame/\Cont/\Pkg/\NameCert$ statement about the packet rows.
\end{proof}

\begin{theorem}[Cellular substrate ledger replay obligation]
\label{thm:cellular-substrate-ledger-replay-obligation}
Audit-route replay through the global-step ledger $G$, continuation row $C$, provenance row $P$, and local naming row $L$ exhausts all accepted $\CellularSubstrateUp$ reads. The replay exports only the finite cellular-substrate packet and never exports a hidden host evaluator, infinite grid, scheduler, or unbounded update oracle.
\end{theorem}
\begin{proof}
Start with an accepted cellular-substrate read and project the carrier by \autoref{def:cellular-substrate-carrier}. The only ledgered route available after the local update row is $G,C,P,L$.

Apply \autoref{thm:cellular-substrate-namecert-obligations}. Its exactness and ledger-provenance rows state that accepted reads replay the finite global-step ledger through the displayed continuation and package rows before entering the local NameCert row.

Now cite \autoref{thm:cellular-substrate-local-update-nonescape}. Since the complete route already factors through $S,N,U,G,H,C,P,L$, the audit replay has no coordinate from which to export a hidden evaluator, infinite grid, scheduler, or unbounded oracle.
\end{proof}

\begin{theorem}[Cellular substrate local-rule factorization]
\label{thm:cellular-substrate-local-rule-factorization}
Every accepted $\CellularSubstrateUp$ local-update read of a carrier
$$
Q=(S,N,U,G,H,C,P,L)
$$
factors through the sibling $\CellularLocalUpdateRuleUp$ surface: the displayed neighbourhood window $N$, the selected local rule row $U$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row. The read cannot be reconstructed from the global-step ledger alone, a hidden scheduler, an infinite grid, or a host evaluator.
\end{theorem}
\begin{proof}
Project the carrier through \autoref{def:cellular-substrate-carrier}. The only local-update source available to the packet is the displayed pair of the neighbourhood-window row $N$ and the local update route $U$, together with the visible transport, continuation, package, and naming rows.

Apply the window exposure theorem \autoref{thm:cellular-substrate-window-exposure-obligation} and the ledger replay theorem \autoref{thm:cellular-substrate-ledger-replay-obligation}. The first forces the local-update read to see $N$ before $U$, and the second confines any later replay to $G,C,P,L$ rather than to an external evaluator.

Now compare the exposed rows with the sibling packet of \autoref{ch:concrete-instances-cellularlocalupdaterule-namecert}. Its obligation theorem \autoref{thm:cellular-local-update-rule-namecert-obligations} identifies the local-rule consumer surface as a finite neighbourhood window, selected local rule row, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ route.

Thus the $\CellularSubstrateUp$ local-update row is consumed through that sibling surface. Since the carrier has no coordinate for a hidden scheduler, infinite grid, host evaluator, or ledger-only rule source, no accepted read factors by any other route.
\end{proof}

The Lean file \texttt{Derived/CellularSubstrateUp/TasteGate.lean} includes \texttt{Nontrivial CellularSubstrateUp} and \texttt{FieldFaithful CellularSubstrateUp} instances for the displayed carrier.

\closureat{CellularSubstrateUp}{seedStr}
\closureat{CellularSubstrateUp}{obligationStr}[BEDC.Derived.CellularSubstrateUp.CellularSubstrateTasteGate\_single\_carrier\_alignment]
\closureat{CellularSubstrateUp}{scopedStr}[BEDC.Derived.CellularSubstrateUp.CellularSubstrateTasteGate\_single\_carrier\_alignment]
\closureat{CellularSubstrateUp}{publicStr}[BEDC.Derived.CellularSubstrateUp.CellularSubstrateTasteGate\_single\_carrier\_alignment]
\closureat{CellularSubstrateUp}{bridgedStr}[BEDC.Derived.CellularSubstrateUp.CellularSubstrateTasteGate\_single\_carrier\_alignment]

\begin{closurestatus}{\CellularSubstrateUp}
  \constructivestory{A $\CellularSubstrateUp$ packet is generated from a finite cell-state row, a neighbourhood-window row, a local update route, a global-step ledger, componentwise hsame transports, displayed continuation routes, package provenance, and a local NameCert row.}
  \theoryclosure{\bridgedClosure}
  \scopeclosed{\autoref{def:cellular-substrate-carrier}, \autoref{thm:cellular-substrate-namecert-obligations}, \autoref{thm:cellular-substrate-local-update-nonescape}, \autoref{thm:cellular-substrate-window-exposure-obligation}, \autoref{thm:cellular-substrate-classifier-stability-obligation}, \autoref{thm:cellular-substrate-ledger-replay-obligation}, and \autoref{thm:cellular-substrate-local-rule-factorization} bind the carrier, NameCert obligations, local-update non-escape, window exposure, classifier stability, ledger replay, and local-rule factorization rows.}
  \formalstatus{\auditCleanV}
  \leantarget{BEDC.Derived.CellularSubstrateUp.CellularSubstrateTasteGate\_single\_carrier\_alignment}
  \leanchecked{BEDC.Derived.CellularSubstrateUp.taste\_gate}
  \bridgestatus{paperBridge}
  \origin{ai}
  \notclaimed{This chapter does not close bridge checking, universality, hidden host evaluator correctness, quotient stream equality, internal induction, infinite-grid completeness, scheduler correctness, or unbounded update evaluation for $\CellularSubstrateUp$.}
  \upgradepath{Mature closure requires completeness-style cellular consumer coverage beyond the paper bridge surface.}
\end{closurestatus}
