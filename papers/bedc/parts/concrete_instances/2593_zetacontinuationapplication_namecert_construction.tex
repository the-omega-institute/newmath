\chapter{A Concrete Naming Certificate for $\ZetaContinuationApplicationUp$}
\label{ch:concrete-instances-zetacontinuationapplication-namecert}
\origin{ai}

The $\ZetaContinuationApplicationUp$ packet records the application-level certificate that turns the zeta continuation surface into a finite NameCert object. It reads the eta continuation row, the functional-equation continuation row, pole and zero ledgers, Gamma horizon rows, application routes, transport rows, package provenance, and a local naming row. The packet is an application certificate only: it does not assert RH, complete the constructive $\GammaUp$ chapter, classify nontrivial zeros, or export a continuation branch outside the displayed routes.

The carrier consumes the $\GammaUp$ horizon row from \autoref{ch:concrete-instances-gamma-namecert}, the analytic-operation routing surface from \autoref{ch:concrete-instances-analyticcontinuationoperation-namecert}, and the $\ZetaBasicUp$ source row from \autoref{ch:concrete-instances-zeta-basic-namecert}. These sibling rows are classifier inputs for the finite packet: $G$ bounds Gamma reads, $A$ routes analytic continuation through the operation surface, and $E$ records the eta handoff from the basic zeta source without turning that source row into a global continuation theorem.

\begin{definition}[ZetaContinuationApplication carrier]
\label{def:zeta-continuation-application-carrier}
A $\ZetaContinuationApplicationUp$ carrier is a finite $\BHist$ packet
$$
Z=(E,F,P,T,G,A,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the eta-continuation row, recording the $\eta$ handoff from the $\ZetaBasicUp$ source and the denominator boundary $1-2^{1-s}$;
\item $F$ is the functional-equation continuation row, recording the reflected input $1-s$, the factors $2^s$, $\pi^{s-1}$, $\sin(\pi s/2)$, and their displayed product route;
\item $P$ is the pole ledger row at $s=1$, recording the simple-pole boundary and residue $\Eone(\emp)$ without exporting a regular zeta value there;
\item $T$ is the zero ledger row, recording the trivial-zero inputs witnessed by the sine factor and the absence of any nontrivial-zero-location conclusion;
\item $G$ is the $\GammaUp$ horizon row, recording the domain boundary for $\Gamma(1-s)$, its pole exclusions, and the fact that the full constructive Gamma carrier is not supplied here;
\item $A$ is the application-certificate row, reading $E,F,P,T,G$ as the finite application of analytic continuation to the zeta source;
\item $H$ stores componentwise $\hsame$ transports for $E,F,P,T,G,A$ and for the local naming row;
\item $C$ stores the finite $\Cont$ routes by which consumers read eta continuation, functional-equation continuation, pole and zero ledgers, Gamma horizon rows, application provenance, and local naming;
\item $Q$ is the $\Pkg$ provenance row over the same application packet;
\item $N$ is the local $\NameCert_{\ZetaContinuationApplicationUp}$ row.
\end{enumerate}
The carrier exposes exactly this finite application packet. It has no coordinate for a value at the pole, no complete $\GammaUp$ construction, no branch selector outside the displayed eta and functional-equation routes, and no RH-facing nontrivial-zero classifier.
\end{definition}
\leandef{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationUp}

\begin{theorem}[ZetaContinuationApplication NameCert obligations]
\label{thm:zeta-continuation-application-namecert-obligations}
The local $\NameCert_{\ZetaContinuationApplicationUp}$ surface for \autoref{def:zeta-continuation-application-carrier} consists of carrier admission for $Z$, componentwise classifier stability for $E,F,P,T,G,A$, ledger exactness for pole and zero rows, Gamma-horizon boundary exactness, $\Cont$ replay through $C$, $\Pkg$ provenance through $Q$, and local naming exactness for $N$.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_namecert\_obligations}
\begin{proof}
Carrier admission is projection from the tuple $Z$. Each continuation, ledger, horizon, application, transport, replay, package, and naming coordinate is displayed as a row of the same finite $\BHist$ packet.

Classifier stability is componentwise. The transports in $H$ preserve the eta-continuation row, the functional-equation row, the pole row, the zero ledger, the Gamma horizon row, and the application-certificate row without adding a new analytic object.

The replay and provenance rows then repack the same data. The routes in $C$ expose only the displayed application coordinates, $Q$ records their package provenance, and $N$ names the local certificate surface.
\end{proof}

\begin{theorem}[ZetaContinuationApplication carrier admission]
\label{thm:zeta-continuation-application-carrier-admission}
For the displayed carrier $Z=(E,F,P,T,G,A,H,C,Q,N)$ of \autoref{def:zeta-continuation-application-carrier}, the admissible $\ZetaContinuationApplicationUp$ packet is exactly the finite application certificate whose source locks are the $\GammaUp$ horizon row of \autoref{ch:concrete-instances-gamma-namecert}, the analytic-operation routing surface of \autoref{ch:concrete-instances-analyticcontinuationoperation-namecert}, and the $\ZetaBasicUp$ eta source of \autoref{ch:concrete-instances-zeta-basic-namecert}. The checked TasteGate alignment recorded at this site fixes those rows as the single carrier surface consumed by the chapter.
\end{theorem}
\begin{proof}
Project the tuple $Z$. Its displayed coordinates are precisely the eta row $E$, functional-equation row $F$, pole row $P$, zero row $T$, Gamma horizon row $G$, application row $A$, transport row $H$, replay row $C$, provenance row $Q$, and local naming row $N$.

The sibling source locks identify where those coordinates may be read. The Gamma-facing data enters only through $G$, the analytic-operation data enters only through $A$, and the zeta source data enters only through $E$.

The TasteGate marker attached to the carrier definition records that these rows share one carrier alignment. Hence carrier admission is the displayed finite packet rather than an undisplayed analytic object.
\end{proof}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrierAdmission}

\begin{theorem}[ZetaContinuationApplication componentwise stability]
\label{thm:zeta-continuation-application-componentwise-stability}
For an accepted carrier $Z=(E,F,P,T,G,A,H,C,Q,N)$, every $\hsame$ or $\Cont$ replay that preserves the checked TasteGate alignment preserves the components $E,F,P,T,G,A$ separately. In particular, the $\GammaUp$, analytic-operation, and $\ZetaBasicUp$ sibling locks may be transported only through their own displayed rows and may not be exchanged for a different continuation source.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_componentwise\_stability}
\begin{proof}
Read the transport coordinate $H$ componentwise. It stores preservation data for the eta, functional-equation, pole, zero, Gamma-horizon, application, and naming rows without adding a mixed row between them.

Replay through $C$ keeps the same separation. Eta-side requests return to $E$, functional-equation requests return to $F$, ledger requests return to $P$ or $T$, Gamma-facing requests return to $G$, and application requests return to $A$.

Since the TasteGate marker fixes the single-carrier alignment, these componentwise routes cannot swap the sibling locks. The accepted packet remains stable row by row.
\end{proof}

\begin{theorem}[ZetaContinuationApplication ledger exactness]
\label{thm:zeta-continuation-application-ledger-exactness}
For an accepted carrier $Z=(E,F,P,T,G,A,H,C,Q,N)$, the pole ledger $P$, zero ledger $T$, Gamma horizon row $G$, application provenance $Q$, and local name $N$ are exact ledgers for the displayed application certificate. They record only the pole boundary, trivial-zero surface, Gamma horizon boundary, analytic-operation application route, package provenance, and local naming already present in $Z$, and they export no pole value, completed $\GammaUp$ construction, nontrivial-zero classifier, or continuation branch outside the finite replay routes in $C$.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_ledger\_exactness}
\begin{proof}
Inspect the ledger coordinates. The pole boundary is stored in $P$, the zero surface is stored in $T$, the Gamma boundary is stored in $G$, and provenance and naming are stored in $Q$ and $N$.

Apply the carrier admission and componentwise stability rows. Each ledger read is routed back through the same coordinate of $Z$ and through the same sibling source lock named in the chapter introduction.

The carrier has no coordinate for the positive objects excluded in the statement. Therefore exactness is ledger exactness for the displayed packet, not a hidden continuation theorem.
\end{proof}

\begin{theorem}[Eta factorization route]
\label{thm:zeta-continuation-application-eta-factorization}
For an accepted $\ZetaContinuationApplicationUp$ carrier $Z=(E,F,P,T,G,A,H,C,Q,N)$, every eta-side continuation read factors through $E$, the application row $A$, the transport row $H$, and one of the finite replay routes in $C$. Such a read may use the displayed denominator boundary and overlap agreement with $\ZetaBasicUp$, but it cannot export a value at $s=1$ or a continuation branch outside the eta route.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_eta\_factorization}
\begin{proof}
Read an eta-side request through the finite routes stored in $C$. If the request is inside the eta continuation surface, the route lands in $E$ and then in the application row $A$.

Transport through $H$ preserves the same eta row and denominator boundary. The pole request is not part of $E$ as a regular value; it is routed to the pole ledger $P$.

Thus the eta factorization is a finite application route. The certificate exports the eta handoff and overlap evidence, while pole and outside-route requests remain ledger reads.
\end{proof}

\begin{theorem}[Gamma horizon boundary]
\label{thm:zeta-continuation-application-gamma-horizon-boundary}
For an accepted $\ZetaContinuationApplicationUp$ carrier, every functional-equation read that touches the factor $\Gamma(1-s)$ must pass through the Gamma horizon row $G$. Inputs where the Gamma factor is outside the displayed horizon are recorded in $G$ as boundary data and are not exported as values of the continued zeta function.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_gamma\_horizon\_boundary}
\begin{proof}
Begin with a functional-equation read. The factorization row $F$ records the reflected input and the elementary factors, while the Gamma factor is named only by the horizon row $G$.

If the input is inside the displayed Gamma horizon, the read may continue through $F,G,A,H,C$. If the input is at a Gamma boundary, the route terminates in the ledger part of $G$.

The application row $A$ therefore never converts the Gamma horizon into a completed $\GammaUp$ construction. It records exactly the boundary needed by the zeta continuation application.
\end{proof}

\begin{theorem}[Gamma eta handoff source lock]
\label{thm:zeta-continuation-application-gamma-eta-handoff}
For an accepted $\ZetaContinuationApplicationUp$ carrier $Z=(E,F,P,T,G,A,H,C,Q,N)$, every eta-factorization read that uses a Gamma-facing horizon fact consumes the $\GammaUp$ row through \autoref{ch:concrete-instances-gamma-namecert}. The read must pass through $E$, $G$, $A$, the componentwise $\hsame$ transport row $H$, and a finite route in $C$; it cannot replace the Gamma horizon row by an undisplayed Gamma construction.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_gamma\_eta\_handoff}
\begin{proof}
Start with the carrier definition. The eta side is stored in $E$, while the only Gamma-facing coordinate is the horizon row $G$.

Use \autoref{thm:zeta-continuation-application-eta-factorization} to route the eta read through $E,A,H,C$. If the route also needs a Gamma-facing horizon fact, \autoref{thm:zeta-continuation-application-gamma-horizon-boundary} forces that fact to pass through $G$.

The sibling $\GammaUp$ chapter supplies the source row named by $G$, and the local packet only records that consumption. Since no coordinate of $Z$ stores a completed Gamma construction, the handoff remains a finite source lock.
\end{proof}

\begin{theorem}[Analytic operation boundary]
\label{thm:zeta-continuation-application-analytic-operation-boundary}
For an accepted $\ZetaContinuationApplicationUp$ carrier $Z=(E,F,P,T,G,A,H,C,Q,N)$, every application-level continuation read factors through the analytic-operation surface of \autoref{ch:concrete-instances-analyticcontinuationoperation-namecert}. The row $A$ records the application of that operation to the eta, functional-equation, pole, zero, and Gamma-horizon rows; it does not create a second continuation operation outside $E,F,P,T,G,H,C$.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_analytic\_operation\_boundary}
\begin{proof}
Read an application-level continuation request through $C$. By the carrier definition, the only row that names application of continuation data is $A$, and its inputs are the displayed rows $E,F,P,T,G$.

The analytic-operation sibling supplies the operation boundary consumed by $A$. Componentwise transport through $H$ preserves the inputs as inputs to that operation surface rather than as independent analytic continuations.

Apply \autoref{thm:zeta-continuation-application-non-escape} to the outside cases. A request for an undisplayed branch or another operation row has no carrier coordinate, so the application route remains locked to the finite analytic-operation boundary.
\end{proof}

\begin{theorem}[ZetaBasic source lock]
\label{thm:zeta-continuation-application-zeta-basic-source-lock}
For an accepted $\ZetaContinuationApplicationUp$ carrier $Z=(E,F,P,T,G,A,H,C,Q,N)$, the $\ZetaBasicUp$ row consumed from \autoref{ch:concrete-instances-zeta-basic-namecert} is only the finite source lock for the eta handoff. It licenses the displayed source row and its $\hsame$ transport into $E$, but it does not by itself license analytic continuation, pole removal, Gamma-factor transport, or any branch outside the routes displayed in $C$.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_zeta\_basic\_source\_lock}
\begin{proof}
Project the source dependency from the carrier. The source row enters through $E$, where the eta handoff and denominator boundary are stored.

Use \autoref{thm:zeta-continuation-application-eta-factorization} to read the source through $E,A,H,C$. The transport row $H$ preserves the source handoff componentwise, and the replay row $C$ exposes only the finite eta route.

The remaining analytic content is held in $F,P,T,G,A$, not in the $\ZetaBasicUp$ source row. By \autoref{thm:zeta-continuation-application-non-escape}, pole removal, Gamma transport, and outside-branch requests therefore remain outside the source lock unless the corresponding displayed row supplies them.
\end{proof}

\begin{theorem}[ZetaContinuationApplication route totality]
\label{thm:zeta-continuation-application-route-totality}
For an accepted $\ZetaContinuationApplicationUp$ packet, every eta, functional-equation, Gamma-horizon, pole-ledger, zero-ledger, or application read factors through the displayed rows

$$
E,F,P,T,G,A,H,C,Q,N.
$$

The row $E$ carries eta input, $F$ carries the functional-equation route, $P$ and $T$ carry the pole and trivial-zero ledgers, $G$ carries the Gamma horizon, $A$ carries the application route, $H$ carries componentwise transport, $C$ carries finite replay, $Q$ carries package provenance, and $N$ carries the local $\NameCert$ row.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_route\_totality}
\begin{proof}
Project the carrier of \autoref{def:zeta-continuation-application-carrier}. It lists exactly the rows $E,F,P,T,G,A,H,C,Q,N$, so every application read starts from the finite NameCert packet rather than from an ambient analytic-continuation object.

Use the componentwise and ledger rows next. Theorem~\ref{thm:zeta-continuation-application-componentwise-stability} keeps eta, functional-equation, pole, zero, Gamma, and application rows separate under $H$ and $C$, while \autoref{thm:zeta-continuation-application-ledger-exactness} routes pole, zero, Gamma, provenance, and naming reads through their displayed ledgers.

Finally attach the sibling source locks. The Gamma eta handoff, analytic-operation boundary, and ZetaBasic source lock force source-facing reads through $G$, $A$, and $E$ respectively. Hence every listed read factors through the ten-row carrier surface.
\end{proof}

\begin{theorem}[ZetaContinuationApplication boundary exhaustion]
\label{thm:zeta-continuation-application-boundary-exhaustion}
An accepted $\ZetaContinuationApplicationUp$ packet exports no pole value, completed $\GammaUp$ construction, nontrivial-zero classifier, RH statement, or off-route continuation branch. All boundary requests are exhausted by the pole ledger $P$, trivial-zero ledger $T$, Gamma horizon $G$, application row $A$, transport $H$, replay $C$, provenance $Q$, and local certificate $N$ of the displayed packet.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_boundary\_exhaustion}
\begin{proof}
Start from route totality. Every boundary-facing request must already be a read of $E,F,P,T,G,A,H,C,Q,N$.

The ledger exactness theorem then handles the exceptional analytic surfaces. Pole requests terminate at $P$, trivial-zero requests at $T$, and Gamma-horizon requests at $G$; none of these rows contains a regular pole value, completed Gamma construction, or nontrivial-zero classifier.

It remains to rule out route escape. The analytic-operation boundary and ZetaBasic source lock keep $A$ and $E$ from becoming a second continuation theorem, and the carrier definition has no coordinate for RH or an off-route branch. Therefore the displayed boundary rows exhaust the public application packet.
\end{proof}

\begin{theorem}[ZetaContinuationApplication root boundary triad]
\label{thm:zeta-continuation-application-root-boundary-triad}
For every accepted $\ZetaContinuationApplicationUp$ carrier $Z=(E,F,P,T,G,A,H,C,Q,N)$, the root source locks are the Gamma horizon row $G$, the analytic-operation routing row $A$, and the $\ZetaBasicUp$ source row $E$. Every root read of $E,F,P,T,G,A,H,C,Q,N$ must expose that triad before the application row is consumed, and the support rows $H,C,Q,N$ may only transport, replay, package, and name the same displayed triad.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_root\_boundary\_triad}
\begin{proof}
Start with carrier admission and route totality. The accepted packet is the finite carrier $Z=(E,F,P,T,G,A,H,C,Q,N)$, and every eta, functional-equation, pole, zero, Gamma, or application read factors through those rows.

Now attach the three source locks. The Gamma horizon boundary and Gamma eta handoff place Gamma-facing reads in $G$, the analytic-operation boundary places application routing in $A$, and the ZetaBasic source lock places eta-source reads in $E$.

Finally use ledger exactness and boundary exhaustion. The rows $P,T$ remain pole and trivial-zero ledgers, while $H,C,Q,N$ only preserve the displayed packet. Thus the root source locks are exactly the triad $G,A,E$ inside the carrier.
\end{proof}

\input{parts/concrete_instances/zetacontinuationapplication/root_obligations}

\begin{theorem}[ZetaContinuationApplication public export]
\label{thm:zeta-continuation-application-public-export}
The public application packet consumed by $\GammaUp$, $\AnalyticContinuationOperationUp$, and $\ZetaBasicUp$ is exactly the finite NameCert surface

$$
E,F,P,T,G,A,H,C,Q,N.
$$

Downstream consumers may read the packet only through the displayed carrier admission, componentwise stability, ledger exactness, Gamma eta handoff, analytic-operation boundary, and ZetaBasic source lock; no additional analytic, RH-facing, or completed-Gamma row is exported.
\end{theorem}
\begin{proof}
First combine carrier admission with route totality. The accepted packet is the ten-row carrier of \autoref{def:zeta-continuation-application-carrier}, and every public route factors through those rows.

Next identify the three sibling-facing entry points. The Gamma eta handoff supplies $G$, the analytic-operation boundary supplies $A$, and the ZetaBasic source lock supplies $E$; the remaining rows $F,P,T,H,C,Q,N$ stay inside the same finite application packet.

Finally apply boundary exhaustion. Since pole values, completed Gamma data, nontrivial-zero classifiers, RH statements, and off-route branches have no exported coordinate, the public packet consumed by the sibling chapters is exactly the displayed NameCert surface.
\end{proof}

\begin{theorem}[Application certificate non-escape]
\label{thm:zeta-continuation-application-non-escape}
Every downstream consumer of an accepted $\ZetaContinuationApplicationUp$ packet factors through the displayed rows $E,F,P,T,G,A,H,C,Q,N$. Requests for a pole value, a Gamma value beyond the horizon row, an undisplayed continuation branch, or a nontrivial-zero-location claim have no carrier coordinate and are recorded only as ledger or horizon boundary reads.
\end{theorem}
\begin{proof}
Let a downstream consumer route be given. By the carrier definition, the route can inspect only the eta row, functional-equation row, pole row, zero ledger, Gamma horizon, application row, transport row, continuation routes, package row, and local naming row.

Pole requests land in $P$, Gamma-boundary requests land in $G$, and trivial-zero requests land in $T$. An undisplayed branch or a nontrivial-zero-location claim is absent from the carrier inventory.

Consequently every exported application fact is a read of the finite packet. The missing objects remain outside the certificate surface rather than becoming hidden application outputs.
\end{proof}

\begin{theorem}[ZetaContinuationApplication primitive scope package]
\label{thm:zeta-continuation-application-primitive-scope-package}
Every accepted $\ZetaContinuationApplicationUp$ packet reads the $\GammaUp$, analytic-operation, and $\ZetaBasicUp$ sibling rows only through the displayed carrier
$$
Z=(E,F,P,T,G,A,H,C,Q,N)
$$
and through the BEDC primitives $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. In particular, the packet exposes the Gamma horizon by $G$, the analytic-operation route by $A$, the zeta source by $E$, componentwise transport by $H$, replay by $C$, package provenance by $Q$, and local naming by $N$, without importing analytic continuation as an external theorem.
\end{theorem}
\begin{proof}
Start with the carrier admission of \autoref{thm:zeta-continuation-application-carrier-admission}. It fixes the visible packet as the finite $\BHist$ carrier $Z=(E,F,P,T,G,A,H,C,Q,N)$ and identifies the Gamma, analytic-operation, and zeta-source rows as sibling inputs to that carrier.

Use componentwise stability from \autoref{thm:zeta-continuation-application-componentwise-stability}. The $\hsame$ row $H$ preserves $E,F,P,T,G,A$ separately, so no sibling row can be exchanged for a different continuation source during transport.

Now apply the source-lock theorems. The Gamma eta handoff of \autoref{thm:zeta-continuation-application-gamma-eta-handoff} routes Gamma-facing reads through $G$; the analytic-operation boundary of \autoref{thm:zeta-continuation-application-analytic-operation-boundary} routes operation reads through $A$; and the ZetaBasic source lock of \autoref{thm:zeta-continuation-application-zeta-basic-source-lock} routes zeta-source reads through $E$.

Finally use non-escape from \autoref{thm:zeta-continuation-application-non-escape}. Since every downstream consumer factors through $E,F,P,T,G,A,H,C,Q,N$, the only remaining public structure is $\Cont$ replay through $C$, $\Pkg$ provenance through $Q$, and the local $\NameCert$ row $N$ over the same $\BHist$ packet.
\end{proof}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_primitive\_scope\_package}

\begin{theorem}[ZetaContinuationApplication visible-boundary lock]
\label{thm:zeta-continuation-application-visible-boundary-lock}
Every accepted $\ZetaContinuationApplicationUp$ application read preserving the sibling locks keeps Gamma horizon reads in $G$, analytic-operation reads in $A$, ZetaBasic eta-source reads in $E$, and boundary exclusions in $P,T,G,N$. Such a read cannot merge those rows into a completed Gamma construction, an off-route continuation branch, a pole value, an RH statement, or a nontrivial-zero classifier.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_visible\_boundary\_lock}
\begin{proof}
Start with componentwise stability. Theorem~\ref{thm:zeta-continuation-application-componentwise-stability} preserves $E,F,P,T,G,A,H,C,Q,N$ row by row, so a preserving read cannot exchange the Gamma, analytic-operation, eta-source, pole, trivial-zero, or naming rows.

Next apply the source locks. The Gamma eta handoff routes Gamma-facing reads through $G$, the analytic-operation boundary routes operation reads through $A$, and the ZetaBasic source lock routes eta-source reads through $E$.

Now attach route totality. Every accepted application route stays on the displayed packet and reads sibling data through the primitive scope package rather than through a second analytic continuation object.

Finally use boundary exhaustion. The pole, trivial-zero, Gamma horizon, and local naming boundaries remain in $P,T,G,N$, and the chapter exports no coordinate for completed Gamma data, an off-route continuation branch, a pole value, an RH statement, or a nontrivial-zero classifier.
\end{proof}

\begin{theorem}[ZetaContinuationApplication root NameCert obligation surface]
\label{thm:zeta-continuation-application-root-namecert-obligation-surface}
For an accepted $\ZetaContinuationApplicationUp$ carrier $Z=(E,F,P,T,G,A,H,C,Q,N)$, the root NameCert obligation surface is the local package consisting of carrier admission, componentwise stability, ledger exactness, Gamma horizon boundary, eta factorization, analytic-operation routing, ZetaBasic source locking, primitive scope, and non-escape for the same displayed rows. Every root obligation is therefore a row obligation over $E,F,P,T,G,A,H,C,Q,N$ and the primitives $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_root\_namecert\_obligation\_surface}
\begin{proof}
Start from the carrier and NameCert rows in \autoref{def:zeta-continuation-application-carrier} and \autoref{thm:zeta-continuation-application-namecert-obligations}. These identify the local packet and list the rows whose admission, stability, replay, provenance, and naming must be checked.

Next attach the carrier admission, componentwise stability, and ledger exactness theorems. They keep the accepted packet on the displayed rows and make the pole, zero, Gamma horizon, application, provenance, and naming ledgers exact for that packet.

Finally use the eta factorization, Gamma horizon, analytic-operation boundary, ZetaBasic source lock, primitive scope package, and non-escape theorems. Together they give the root obligation surface as a finite NameCert package rather than an off-table continuation theorem.
\end{proof}

\begin{theorem}[ZetaContinuationApplication source route exactness]
\label{thm:zeta-continuation-application-source-route-exactness}
For an accepted $\ZetaContinuationApplicationUp$ carrier, every read of the $\GammaUp$ horizon, analytic-operation surface, or $\ZetaBasicUp$ source row factors through the displayed source routes $G$, $A$, and $E$ respectively, then through componentwise transport $H$, finite replay $C$, provenance $Q$, and local naming $N$. No source read may replace these routes by a completed Gamma object, a second analytic operation, or an independent zeta continuation branch.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_source\_route\_exactness}
\begin{proof}
Read the three source locks separately. The Gamma eta handoff theorem routes Gamma-facing requests through $G$, the analytic-operation boundary routes operation-facing requests through $A$, and the ZetaBasic source lock routes zeta-source requests through $E$.

Componentwise stability then prevents exchange among those rows. Transport through $H$ preserves the source identities separately, while replay through $C$ returns each request to the corresponding displayed route.

The primitive scope package repacks the same source routes with $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The non-escape theorem excludes completed Gamma data, a second analytic operation, and an undisplayed continuation branch, so the displayed source routes are exact.
\end{proof}

\begin{theorem}[ZetaContinuationApplication ledger non-escape]
\label{thm:zeta-continuation-application-ledger-nonescape}
Every ledger-facing consumer of an accepted $\ZetaContinuationApplicationUp$ packet factors through the pole ledger $P$, trivial-zero ledger $T$, Gamma horizon ledger $G$, application provenance $Q$, and local name $N$ inside $Z=(E,F,P,T,G,A,H,C,Q,N)$. The ledger surface rules out RH, nontrivial-zero classification, completed Gamma construction, and undisplayed continuation branches.
\end{theorem}
\begin{proof}
Begin with ledger exactness. The pole boundary is recorded in $P$, the trivial-zero surface in $T$, the Gamma horizon in $G$, and the package and naming records in $Q$ and $N$.

Use carrier admission and componentwise stability to keep these ledgers attached to the accepted packet. A ledger read transported by $H$ and replayed by $C$ returns to the same coordinate of $Z$.

Finally apply the primitive scope package and non-escape theorem. Since the carrier has no coordinate for RH, nontrivial-zero classification, completed Gamma construction, or an undisplayed continuation branch, no ledger-facing consumer can export one.
\end{proof}

\begin{theorem}[ZetaContinuationApplication public export surface]
\label{thm:zeta-continuation-application-public-export-surface}
For an accepted $\ZetaContinuationApplicationUp$ carrier, the public export surface is exactly the finite packet
$$
Z=(E,F,P,T,G,A,H,C,Q,N).
$$
The row $E$ exports the $\ZetaBasicUp$ eta handoff, $F$ exports the functional-equation route, $P$ exports the pole ledger, $T$ exports the trivial-zero ledger, $G$ exports the $\GammaUp$ horizon row, $A$ exports the analytic-operation application row, $H$ exports componentwise $\hsame$ transport, $C$ exports finite $\Cont$ replay, $Q$ exports $\Pkg$ provenance, and $N$ exports the local $\NameCert_{\ZetaContinuationApplicationUp}$ row. No public export coordinate lies outside those ten displayed rows.
\end{theorem}
\begin{proof}
Project the accepted carrier through \autoref{def:zeta-continuation-application-carrier}. Its complete row inventory is the displayed tuple $E,F,P,T,G,A,H,C,Q,N$.

The source-lock theorems identify the public meaning of the rows. The $\ZetaBasicUp$ source is read through $E$, the Gamma horizon through $G$, and the analytic-operation boundary through $A$; transport, replay, provenance, and local naming are read through $H,C,Q,N$.

Use \autoref{thm:zeta-continuation-application-non-escape} for exclusion. Since every downstream read factors through the displayed inventory, no hidden continuation branch, pole value, completed Gamma construction, or nontrivial-zero classifier becomes part of the export surface.
\end{proof}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_public\_export\_surface}

\begin{theorem}[ZetaContinuationApplication bridge consumer boundary]
\label{thm:zeta-continuation-application-bridge-consumer-boundary}
Every bridge-facing or application-facing consumer of an accepted $\ZetaContinuationApplicationUp$ packet factors through the $\GammaUp$ row $G$, the analytic-operation row $A$, the $\ZetaBasicUp$ eta row $E$, componentwise transport $H$, replay $C$, provenance $Q$, and the local name $N$. The remaining public rows $F,P,T$ may be inspected only as the displayed functional-equation, pole-ledger, and trivial-zero-ledger coordinates of the same packet.
\end{theorem}
\begin{proof}
Begin with a bridge-facing consumer route. By \autoref{thm:zeta-continuation-application-primitive-scope-package}, sibling data enters the packet only through the primitive rows of $Z$ and the BEDC rows $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

The consumer must therefore read the sibling boundaries through their named coordinates. Gamma-facing requests pass through $G$, analytic-operation requests through $A$, and zeta-source eta requests through $E$.

The route then remains public only by using $H$ for componentwise transport, $C$ for replay, $Q$ for package provenance, and $N$ for the local certificate name. The rows $F,P,T$ are available as internal displayed coordinates of the same route, not as independent bridge endpoints.
\end{proof}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_bridge\_consumer\_boundary}

\begin{theorem}[ZetaContinuationApplication public non-escape package]
\label{thm:zeta-continuation-application-public-nonescape-package}
The public $\ZetaContinuationApplicationUp$ package is the conjunction of the non-escape theorem, the primitive scope package, and the sibling source locks for $\GammaUp$, analytic continuation, and $\ZetaBasicUp$. Consequently every public consumer is confined to $E,F,P,T,G,A,H,C,Q,N$ and the package excludes RH, nontrivial-zero classification, a completed $\GammaUp$ construction, and undisplayed continuation branches.
\end{theorem}
\begin{proof}
First combine \autoref{thm:zeta-continuation-application-public-export-surface} with \autoref{thm:zeta-continuation-application-primitive-scope-package}. The public surface is the same finite carrier that the primitive scope theorem reads through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

Next attach the sibling source-lock theorems. The Gamma eta handoff, analytic-operation boundary, and ZetaBasic source lock keep the three external sources behind $G,A,E$ respectively, while $H,C,Q,N$ supply transport, replay, provenance, and naming.

Finally use \autoref{thm:zeta-continuation-application-non-escape}. The excluded objects have no displayed coordinate in the carrier, so the public package cannot export them through a bridge or application consumer.
\end{proof}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_public\_nonescape\_package}

\begin{theorem}[ZetaContinuationApplication root source lock]
\label{thm:zeta-continuation-application-root-source-lock}
For an accepted $\ZetaContinuationApplicationUp$ carrier $Z=(E,F,P,T,G,A,H,C,Q,N)$, every root-level read of the $\GammaUp$ horizon, the analytic-operation application surface, or the $\ZetaBasicUp$ eta source must pass through the displayed rows $G,A,E$ and then through the transport, replay, provenance, and naming rows $H,C,Q,N$. The root packet has no source coordinate outside $G,A,E,H,C,Q,N$.
\end{theorem}
\begin{proof}
Begin with the carrier definition and the NameCert obligation surface. The tuple $Z=(E,F,P,T,G,A,H,C,Q,N)$ displays the Gamma horizon row $G$, analytic-operation row $A$, eta source row $E$, transport row $H$, replay row $C$, provenance row $Q$, and local name $N$ as named coordinates of one finite packet.

Apply the source-lock theorems. The Gamma eta handoff forces Gamma-facing reads through $G$, the analytic-operation boundary forces operation-facing reads through $A$, and the ZetaBasic source lock forces eta-source reads through $E$.

Finally use componentwise stability, primitive scope, and the public non-escape package. Transport and replay preserve the three source rows separately through $H$ and $C$, while $Q$ and $N$ record only the package provenance and local name of the same carrier. Therefore every root source read is locked to $G,A,E,H,C,Q,N$.
\end{proof}

\begin{theorem}[ZetaContinuationApplication root ledger boundary]
\label{thm:zeta-continuation-application-root-ledger-boundary}
For an accepted $\ZetaContinuationApplicationUp$ carrier $Z=(E,F,P,T,G,A,H,C,Q,N)$, the root ledgers for the pole boundary, trivial-zero surface, Gamma horizon, application provenance, and local naming are exhausted by $P,T,G,Q,N$. They export no pole value, completed $\GammaUp$ construction, nontrivial-zero classifier, RH statement, or ledger row outside the displayed packet.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_root\_ledger\_boundary}
\begin{proof}
Start with ledger exactness. The pole ledger is $P$, the trivial-zero ledger is $T$, the Gamma horizon ledger is $G$, and the provenance and local naming ledgers are $Q$ and $N$.

Use carrier admission and componentwise stability to keep these rows attached to the same accepted packet. A root ledger read transported by $H$ and replayed by $C$ returns to the corresponding coordinate of $Z$ rather than to an external analytic object.

Now attach boundary exhaustion and the public non-escape package. The carrier has no coordinate for a regular pole value, completed Gamma construction, nontrivial-zero classifier, RH statement, or additional ledger surface, so the root ledger boundary is exactly $P,T,G,Q,N$.
\end{proof}

\begin{theorem}[ZetaContinuationApplication root replay non-escape]
\label{thm:zeta-continuation-application-root-replay-nonescape}
For an accepted $\ZetaContinuationApplicationUp$ carrier $Z=(E,F,P,T,G,A,H,C,Q,N)$, every root consumer replay after the displayed carrier rows factors through $H,C,Q,N$. Such replay may inspect the source rows $E,G,A$ and the public coordinates $F,P,T$ only through the same finite package, and it exports no RH statement, completed $\GammaUp$ construction, pole value, nontrivial-zero classifier, or off-route continuation branch.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_root\_replay\_nonescape}
\begin{proof}
Begin with route totality and the public export surface. They identify the complete replay-readable inventory as the displayed carrier $Z=(E,F,P,T,G,A,H,C,Q,N)$.

Next apply the primitive scope package and bridge consumer boundary. A root consumer can move across the packet only by using componentwise $\hsame$ transport in $H$, finite $\Cont$ replay in $C$, package provenance in $Q$, and the local $\NameCert$ row $N$.

It remains to exclude replay escape. The public non-escape package combines the sibling source locks with boundary exhaustion, so RH, completed Gamma data, pole values, nontrivial-zero classifiers, and off-route branches have no carrier coordinate. Every root consumer replay therefore factors through $H,C,Q,N$ over the displayed finite packet.
\end{proof}

\begin{theorem}[ZetaContinuationApplication functional-row totality]
\label{thm:zeta-continuation-application-functional-row-totality}
For an accepted $\ZetaContinuationApplicationUp$ carrier
$Z=(E,F,P,T,G,A,H,C,Q,N)$, every functional-equation read of the application
surface remains on the displayed rows
$$
F,G,A,H,C,Q,N .
$$
It may use the Gamma horizon row, analytic-operation row, componentwise
transport, replay route, provenance, and local naming only as they occur in
$Z$; it exports no completed $\GammaUp$ construction, pole value, RH statement,
nontrivial-zero classifier, or off-packet continuation branch.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_functional\_row\_totality}
\begin{proof}
Begin with the local carrier and componentwise stability theorems. They project
the rows $F,G,A,H,C,Q,N$ from the accepted carrier and keep them attached under
the displayed $\hsame$ transports and $\Cont$ replay.

Use ledger exactness and the Gamma horizon boundary next. The functional row
$F$ may read the Gamma horizon only through $G$, and the cited boundary prevents
that read from becoming completed Gamma data or a pole-value computation.

Finally apply the analytic-operation boundary, ZetaBasic source lock, public
non-escape package, and root replay non-escape. These results exhaust the
application source route through $G,A,E,H,C,Q,N$ and leave no coordinate for RH,
nontrivial-zero classification, or an off-packet branch.
\end{proof}

\begin{theorem}[ZetaContinuationApplication pole-zero ledger separation]
\label{thm:zeta-continuation-application-pole-zero-ledger-separation}
For an accepted $\ZetaContinuationApplicationUp$ carrier
$Z=(E,F,P,T,G,A,H,C,Q,N)$, the pole ledger $P$ and the trivial-zero ledger $T$
remain separate public ledgers. They can be transported and replayed only
through $H,C,Q,N$, and they cannot export RH, nontrivial-zero classification,
regular pole values, completed Gamma data, or a ledger row outside $P,T$.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_pole\_zero\_ledger\_separation}
\begin{proof}
First read the exact ledger rows. The carrier admission and ledger exactness
theorems identify $P$ as the pole ledger and $T$ as the trivial-zero ledger,
with no shared coordinate that could merge the two public records.

Next use componentwise stability and root ledger boundary. Transport through
$H$ and replay through $C$ return to the displayed $P,T,Q,N$ rows, so a pole
read cannot become a trivial-zero classifier and a trivial-zero read cannot
become a pole-value computation.

The public non-escape package and root replay non-escape then remove the
off-table conclusions. Since RH, nontrivial-zero classification, completed
Gamma data, regular pole values, and extra ledgers have no carrier coordinate,
neither $P$ nor $T$ can export them.
\end{proof}

\begin{theorem}[ZetaContinuationApplication source-triad exhaustion]
\label{thm:zeta-continuation-application-source-triad-exhaustion}
For an accepted $\ZetaContinuationApplicationUp$ carrier
$Z=(E,F,P,T,G,A,H,C,Q,N)$, the Gamma, analytic-operation, and ZetaBasic source
triad is exhausted by the rows $G,A,E$ before any replay through $H,C,Q,N$.
Every downstream functional-equation, pole-ledger, trivial-zero-ledger, or root
consumer therefore reads the source triad through
$$
G,A,E,H,C,Q,N
$$
and not through completed Gamma data, an ambient meromorphic continuation, RH,
or an unrecorded zero classifier.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_source\_triad\_exhaustion}
\begin{proof}
Start from the source locks. The Gamma eta handoff source lock, analytic
operation boundary, and ZetaBasic source lock identify the only source rows
available to the application packet as $G,A,E$.

Attach route totality and boundary exhaustion. They place each source read
before the replay rows $H,C,Q,N$, so downstream consumers receive the triad
through the same finite carrier rather than through a host analytic object.

Finally use the public non-escape package together with root replay non-escape.
The exhausted triad has no coordinate for completed Gamma data, ambient
meromorphic continuation, RH, or unrecorded zero classification, and every
consumer route remains on $G,A,E,H,C,Q,N$.
\end{proof}

\begin{theorem}[ZetaContinuationApplication row-inventory coverage]
\label{thm:zeta-continuation-application-row-inventory-coverage}
For every accepted $\ZetaContinuationApplicationUp$ carrier
$Z=(E,F,P,T,G,A,H,C,Q,N)$, every public read is covered by the eta source row
$E$, the Gamma source row $G$, the analytic-operation source row $A$, the
functional row $F$, the pole and trivial-zero ledger rows $P,T$, and the support
rows $H,C,Q,N$. No additional analytic coordinate, RH-facing coordinate,
ambient continuation branch, or zero-classifier row is part of the public
inventory.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_row\_inventory\_coverage}
\begin{proof}
Begin with the carrier definition and the NameCert obligations. The carrier is
the displayed ten-row packet $Z=(E,F,P,T,G,A,H,C,Q,N)$, and the obligation
surface assigns those rows to eta source, functional equation, pole ledger,
trivial-zero ledger, Gamma source, analytic-operation source, componentwise
transport, replay, provenance, and local naming.

Apply root source lock, root ledger boundary, and root replay non-escape. These
results exhaust source reads through $E,G,A$, ledger reads through $P,T$, and
root consumer replay through $H,C,Q,N$, while keeping $F$ as the displayed
functional row of the same packet.

The inventory is therefore exactly the displayed carrier inventory. Any
analytic branch, RH-facing conclusion, or zero-classifier output outside those
rows lacks a coordinate in $Z$ and cannot be a public read of the accepted
carrier.
\end{proof}

\begin{theorem}[ZetaContinuationApplication source-lock triad separation]
\label{thm:zeta-continuation-application-source-lock-triad-separation}
For every accepted $\ZetaContinuationApplicationUp$ carrier
$Z=(E,F,P,T,G,A,H,C,Q,N)$, the Gamma, analytic-operation, and ZetaBasic source
reads remain separated through the rows $G,A,E$ under $\hsame$ transport and
$\Cont$ replay. No one source lock can substitute for another, and no replay
route can identify the Gamma row, analytic-operation row, and eta source row as
a single source coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_source\_lock\_triad\_visible\_separation}
\begin{proof}
Use the root source lock to locate the three source reads. Gamma-facing reads
enter through $G$, analytic-operation reads through $A$, and ZetaBasic eta
reads through $E$.

Now apply the NameCert obligations and componentwise stability. The transport
row $H$ carries $\hsame$ information componentwise, while the replay row $C$
records $\Cont$ replay for the displayed packet rather than replacing one
source row by another.

Finally combine source-triad exhaustion with root replay non-escape. Downstream
routes may replay the triad only over $G,A,E,H,C,Q,N$, so substituting a single
source lock for the other two would require an undisplayed coordinate. The
accepted packet has no such coordinate.
\end{proof}

\begin{theorem}[ZetaContinuationApplication root-obligation readiness]
\label{thm:zeta-continuation-application-root-obligation-readiness}
For every accepted $\ZetaContinuationApplicationUp$ carrier
$Z=(E,F,P,T,G,A,H,C,Q,N)$, row-inventory coverage, source-lock triad separation,
root ledger boundary, and root replay non-escape form the root-obligation
packet available to downstream zeta and RH boundary chapters. The packet
certifies only the displayed finite carrier interface and does not assert RH,
nontrivial-zero classification, a completed $\GammaUp$ construction, or bridge
verification.
\end{theorem}
\begin{proof}
First collect the local inventory. Row-inventory coverage supplies the complete
public read table $E,F,P,T,G,A,H,C,Q,N$, and the NameCert obligations attach
those reads to the accepted carrier.

Next attach the separated source route and ledger boundary. Source-lock triad
separation keeps $G,A,E$ distinct under $\hsame$ and $\Cont$, while root ledger
boundary keeps pole, trivial-zero, Gamma, provenance, and naming ledgers on
$P,T,G,Q,N$.

Finally apply root replay non-escape. Any downstream zeta or RH boundary
consumer receives only the displayed root packet through $H,C,Q,N$, so the
packet is ready as a boundary obligation without exporting RH, zero
classification, completed Gamma data, or bridge verification.
\end{proof}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_root\_obligation\_readiness}

\begin{theorem}[ZetaContinuationApplication root functional-row readiness]
\label{thm:zeta-continuation-application-root-functional-row-readiness}
For every accepted $\ZetaContinuationApplicationUp$ carrier
$Z=(E,F,P,T,G,A,H,C,Q,N)$, the functional-equation continuation row $F$ is
root-ready for downstream zeta and RH boundary consumers exactly as a finite
$\BHist$ row transported by the displayed $\hsame$ data and replayed through
$\Cont$, $\Pkg$, and $\NameCert$. The row is read together with the eta source
row $E$, the separated Gamma and analytic-operation rows $G,A$, the pole and
trivial-zero ledgers $P,T$, and the replay support $H,C,Q,N$; it does not
assert RH, classify nontrivial zeros, construct a completed $\GammaUp$ object,
or verify a bridge.
\end{theorem}
\begin{proof}
Start with row-inventory coverage. It places $F$ inside the same accepted
carrier as $E,P,T,G,A,H,C,Q,N$, so a downstream consumer cannot read a
functional-equation continuation row apart from the displayed packet.

Use source-lock triad separation and the root ledger boundary. The source rows
$G,A,E$ remain separated under $\hsame$ and $\Cont$, while $P,T$ carry only the
pole and trivial-zero ledgers needed by the root interface.

Finally apply root replay non-escape and root-obligation readiness. The
functional row is available as a finite replay row of $Z$, but the carrier has
no coordinate for RH, nontrivial-zero classification, completed Gamma data, or
bridge verification.
\end{proof}

\begin{theorem}[ZetaContinuationApplication root Gamma handoff readiness]
\label{thm:zeta-continuation-application-root-gamma-handoff-readiness}
For every accepted $\ZetaContinuationApplicationUp$ carrier
$Z=(E,F,P,T,G,A,H,C,Q,N)$, the Gamma handoff available to root consumers is the
separated source row $G$ together with its finite replay through $H,C,Q,N$ and
its boundary interaction with $F,P,T,A,E$. The handoff is a $\BHist$,
$\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ route of the displayed packet; it is
not a completed $\GammaUp$ construction, an RH assertion, a nontrivial-zero
classifier, or a checked bridge.
\end{theorem}
\begin{proof}
Use the Gamma eta handoff and source-lock triad separation. The Gamma source
row is $G$, and the eta and analytic-operation rows remain $E$ and $A$ rather
than substitutes for $G$.

Apply the root ledger boundary. The pole and trivial-zero rows $P,T$ record the
ledger side of the handoff, while $F$ records the functional row adjacent to the
Gamma source route.

Root replay non-escape then keeps the handoff on $G,F,P,T,A,E,H,C,Q,N$. Since
no completed Gamma coordinate, RH coordinate, zero-classifier row, or bridge
verification row is present, none can be exported by the handoff.
\end{proof}

\begin{theorem}[ZetaContinuationApplication root public-export readiness]
\label{thm:zeta-continuation-application-root-public-export-readiness}
For every accepted $\ZetaContinuationApplicationUp$ carrier
$Z=(E,F,P,T,G,A,H,C,Q,N)$, the public export available to downstream zeta and
RH boundary chapters is exactly the root packet formed by row-inventory
coverage, source-lock triad separation, root ledger boundary, root replay
non-escape, root functional-row readiness, and root Gamma handoff readiness.
The export is anchored in the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$,
and $\NameCert$ rows and refuses RH, nontrivial-zero classification, completed
Gamma construction, and bridge verification.
\end{theorem}
\begin{proof}
First collect the finite rows. Row-inventory coverage gives
$E,F,P,T,G,A,H,C,Q,N$, while source-lock triad separation keeps the three source
rows $G,A,E$ distinct under transport and replay.

Next attach the two root handoffs. Functional-row readiness exposes $F$ as the
functional-equation continuation row of the same packet, and Gamma handoff
readiness exposes $G$ only through its finite replay and ledger boundary.

Finally use root ledger boundary, root replay non-escape, and root-obligation
readiness. The public export is therefore the displayed root packet, with no
coordinate for RH, nontrivial-zero classification, completed Gamma
construction, or bridge verification.
\end{proof}

\begin{theorem}[ZetaContinuationApplication open-phase root readiness]
\label{thm:zeta-continuation-application-open-phase-root-readiness}
Any open-phase consumer of an accepted $\ZetaContinuationApplicationUp$
application packet must expose the eta source lock $E$, Gamma horizon row $G$,
analytic-operation row $A$, pole and trivial-zero ledgers $P,T$,
componentwise transport $H$, replay row $C$, provenance row $Q$, and local
name row $N$ before it can read continuation application output. The readable
output is therefore a finite $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and
$\NameCert$ route of the displayed packet.

The readiness row asserts no RH conclusion, no analytic continuation beyond
the displayed routes, no completed Gamma construction, and no standard bridge.
\end{theorem}
\begin{proof}
First use source route exactness. The application output can be reached only
through the displayed source route, so the eta source lock, Gamma horizon row,
and analytic-operation row must appear as the visible $E,G,A$ portion of the
carrier.

Next apply source-triad exhaustion. It prevents an open-phase consumer from
collapsing $E,G,A$ into an undisplayed source coordinate and keeps the pole and
trivial-zero ledgers $P,T$ attached to the same packet.

Finally invoke root-obligation readiness together with root public-export
readiness. These rows add $H,C,Q,N$ as the componentwise transport, replay,
provenance, and local name surface needed by downstream root consumers. Since
the same readiness package refuses RH, nontrivial-zero classification,
completed Gamma data, and bridge verification, the open-phase root read is
exactly the displayed finite obligation row.
\end{proof}

\begin{theorem}[ZetaContinuationApplication root consumer exactness]
\label{thm:zeta-continuation-application-root-consumer-exactness}
Every root consumer of an accepted $\ZetaContinuationApplicationUp$ packet
$Z=(E,F,P,T,G,A,H,C,Q,N)$ reads exactly the displayed rows
$E,F,P,T,G,A,H,C,Q,N$ through the Gamma horizon lock, analytic-operation lock,
and $\ZetaBasicUp$ source lock. The consumer receives no RH statement,
nontrivial-zero classifier, completed $\GammaUp$ construction, or bridge
verification.
\end{theorem}
\begin{proof}
Use open-phase root readiness to place the consumer on the displayed carrier
rows. Then apply root public-export readiness and source-lock triad separation:
$G$ remains the Gamma-facing row, $A$ remains the analytic-operation row, and
$E$ remains the $\ZetaBasicUp$ source row.

Finally use root replay non-escape. Since the packet has no coordinate for RH,
nontrivial-zero classification, completed Gamma data, or bridge verification,
the root consumer surface is exactly the ten-row packet.
\end{proof}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_root\_consumer\_exactness}

\begin{theorem}[ZetaContinuationApplication root pole-zero consumer readiness]
\label{thm:zeta-continuation-application-root-pole-zero-consumer-readiness}
Every pole- or zero-facing root consumer of an accepted
$\ZetaContinuationApplicationUp$ packet $Z=(E,F,P,T,G,A,H,C,Q,N)$ reads the
pole ledger $P$ and trivial-zero ledger $T$ only inside the same carrier route
that exposes $E,F,G,A,H,C,Q,N$. The read may use the displayed Gamma,
analytic-operation, and $\ZetaBasicUp$ sibling locks, but it exports no pole
value, RH conclusion, nontrivial-zero classification, completed $\GammaUp$
object, or bridge verification.
\end{theorem}
\begin{proof}
Start with root ledger boundary and pole-zero ledger separation. They identify
$P,T$ as ledger rows of the same accepted packet rather than as external
analytic conclusions.

Next attach root consumer exactness. The sibling locks keep Gamma reads in
$G$, analytic-operation reads in $A$, and zeta source reads in $E$, so the
ledger consumer cannot leave $E,F,P,T,G,A,H,C,Q,N$.
\end{proof}

\begin{theorem}[ZetaContinuationApplication root Gamma operation handoff]
\label{thm:zeta-continuation-application-root-gamma-operation-handoff}
For every accepted $\ZetaContinuationApplicationUp$ packet
$Z=(E,F,P,T,G,A,H,C,Q,N)$, the root handoff between the Gamma horizon row and
the analytic-operation row is the displayed pair $G,A$ transported through
$H,C,Q,N$ and read against the eta source row $E$, functional row $F$, and
ledgers $P,T$. This handoff remains a finite NameCert route and does not
complete $\GammaUp$, assert RH, classify nontrivial zeros, or verify a bridge.
\end{theorem}
\leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.ZetaContinuationApplicationCarrier\_root\_gamma\_operation\_handoff}
\begin{proof}
Apply root Gamma handoff readiness and root functional-row readiness. They
expose $G$ and $F$ as finite rows adjacent to the application row $A$.

Then use root consumer exactness and root pole-zero consumer readiness. These
keep the handoff inside $E,F,P,T,G,A,H,C,Q,N$ and leave RH, nontrivial-zero
classification, completed Gamma data, and bridge verification outside the
packet.
\end{proof}

\closureat{ZetaContinuationApplicationUp}{seedStr}
\closureat{ZetaContinuationApplicationUp}{obligationStr}
\closureat{ZetaContinuationApplicationUp}{scopedStr}
\closureat{ZetaContinuationApplicationUp}{publicStr}

\begin{closurestatus}{\ZetaContinuationApplicationUp}
  \constructivestory{A $\ZetaContinuationApplicationUp$ packet is generated from eta continuation, functional-equation continuation, pole and zero ledgers, Gamma horizon rows, application routes, componentwise $\hsame$ transports, $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\publicClosure}
  \scopeclosed{\autoref{def:zeta-continuation-application-carrier}, \autoref{thm:zeta-continuation-application-namecert-obligations}, \autoref{thm:zeta-continuation-application-carrier-admission}, \autoref{thm:zeta-continuation-application-componentwise-stability}, \autoref{thm:zeta-continuation-application-ledger-exactness}, \autoref{thm:zeta-continuation-application-eta-factorization}, \autoref{thm:zeta-continuation-application-gamma-horizon-boundary}, \autoref{thm:zeta-continuation-application-gamma-eta-handoff}, \autoref{thm:zeta-continuation-application-analytic-operation-boundary}, \autoref{thm:zeta-continuation-application-zeta-basic-source-lock}, \autoref{thm:zeta-continuation-application-route-totality}, \autoref{thm:zeta-continuation-application-boundary-exhaustion}, \autoref{thm:zeta-continuation-application-public-export}, \autoref{thm:zeta-continuation-application-non-escape}, \autoref{thm:zeta-continuation-application-primitive-scope-package}, \autoref{thm:zeta-continuation-application-public-export-surface}, \autoref{thm:zeta-continuation-application-bridge-consumer-boundary}, \autoref{thm:zeta-continuation-application-public-nonescape-package}, \autoref{thm:zeta-continuation-application-root-source-lock}, \autoref{thm:zeta-continuation-application-root-ledger-boundary}, \autoref{thm:zeta-continuation-application-root-replay-nonescape}, \autoref{thm:zeta-continuation-application-functional-row-totality}, \autoref{thm:zeta-continuation-application-pole-zero-ledger-separation}, \autoref{thm:zeta-continuation-application-source-triad-exhaustion}, \autoref{thm:zeta-continuation-application-row-inventory-coverage}, \autoref{thm:zeta-continuation-application-source-lock-triad-separation}, and \autoref{thm:zeta-continuation-application-root-obligation-readiness} with \leanchecked{BEDC.Derived.ZetaContinuationApplicationUp.taste\_gate} define the local application carrier, obligation rows, carrier admission, componentwise stability, ledger exactness, eta factorization route, Gamma horizon boundary, sibling source locks, analytic-operation boundary, route totality, boundary exhaustion, public export exactness, non-escape ledger, primitive scope package, public export surface, bridge consumer boundary, public non-escape package, root source lock, root ledger boundary, root replay non-escape, functional-row totality, pole-zero ledger separation, source-triad exhaustion, row-inventory coverage, source-lock triad separation, and root-obligation readiness.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close RH, nontrivial-zero classification, full constructive $\GammaUp$ construction, global meromorphic uniqueness, Lean verification, or bridge checking.}
  \upgradepath{Formal verification and bridge checking remain the next obligations for the displayed public application packet.}
\end{closurestatus}
