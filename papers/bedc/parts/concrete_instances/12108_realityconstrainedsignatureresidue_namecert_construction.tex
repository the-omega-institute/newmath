\chapter{A Concrete Naming Certificate for $\RealityConstrainedSignatureResidueUp$}
\label{ch:concrete-instances-reality-constrained-signature-residue-namecert}
\origin{ai}

The $\RealityConstrainedSignatureResidueUp$ packet realizes the representation-gap row of \autoref{sec:reality-constrained-signatures}, especially \autoref{sec:reality-constrained-signatures}, as a finite NameCert object. It sits downstream of the signature-fit carrier of \autoref{ch:concrete-instances-reality-constrained-signature-fit-namecert}, the open-fit carrier of \autoref{ch:concrete-instances-reality-constrained-open-fit-namecert}, the relational-objectivity carrier of \autoref{ch:concrete-instances-relational-objectivity-namecert}, and the methodology-ledger carrier of \autoref{ch:concrete-instances-reality-constrained-scientific-methodology-namecert}. Those siblings record successful compatibility, admitted open fit routes, objectivity rows, and method provenance; this packet records the remaining reality-facing signature separation after a mathematical classifier has already identified the compared representations.

\begin{definition}[Reality-constrained signature residue carrier]
\label{def:reality-constrained-signature-residue-carrier}
A $\RealityConstrainedSignatureResidueUp$ carrier is a finite $\BHist$ packet
$$
Z=(M,S,G,R,W,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $M$ is the mathematical classifier row, recording that the compared representations are identified by the selected mathematical classifier;
\item $S$ is the physical signature row, recording the reality-facing signature bundle and the exposed physical-fit comparison;
\item $G$ is the GAP and methodology-ledger row, naming the admitted representation gap, the observation scope, and the provenance route through the methodology ledger;
\item $R$ is the residue row, storing the remaining physical-signature separation that survives the mathematical identification in $M$;
\item $W$ is the witness route, displaying the finite observation or audit route that exposes the residue rather than inferring it from host disagreement;
\item $H$ stores componentwise $\hsame$ transport for the classifier row, physical signature row, GAP and ledger row, residue row, witness route, continuation rows, provenance row, and local naming row;
\item $C$ stores displayed $\Cont$ replay for mathematical-classifier inspection, physical-signature inspection, GAP-ledger inspection, residue exposure, witness replay, and NameCert comparison;
\item $P$ is the $\Pkg$ provenance row over $\RealityConstrainedSignatureResidueUp$, $\RealityConstrainedSignatureFitUp$, $\RealityConstrainedOpenFitUp$, $\RelationalObjectivityUp$, $\RealityConstrainedMethodologyLedgerUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\RealityConstrainedSignatureResidueUp}$ row.
\end{enumerate}
The carrier contains no coordinate that turns mathematical classifier agreement alone into physical adequacy. A physical-adequacy read must display $S,G,R,W$ in the same finite packet.
\end{definition}

\begin{theorem}[Reality-constrained signature residue NameCert obligations]
\label{thm:reality-constrained-signature-residue-namecert-obligations}
For the carrier of \autoref{def:reality-constrained-signature-residue-carrier}, the local $\NameCert_{\RealityConstrainedSignatureResidueUp}$ surface consists exactly of five obligation rows:
\begin{enumerate}
\item carrier admission for the finite packet $Z=(M,S,G,R,W,H,C,P,N)$;
\item classifier separation, requiring $M$ to be read as mathematical agreement and $S$ as the physical-signature comparison rather than a consequence of $M$;
\item residue exposure, requiring $G,R,W$ to display the representation gap, remaining signature separation, and finite witness route before any consumer can mention physical inadequacy;
\item transport and replay stability, requiring $H$ and $C$ to preserve the classifier, signature, ledger, residue, witness, provenance, and local naming rows componentwise;
\item provenance and local naming, requiring $P$ and $N$ to cite the signature-fit, open-fit, relational-objectivity, methodology-ledger, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ surfaces used by the packet.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedSignatureResidueUp.TasteGate.RealityConstrainedSignatureResidueTasteGate\_single\_carrier\_alignment}
\begin{proof}
Project the finite packet of \autoref{def:reality-constrained-signature-residue-carrier}. Its coordinates already separate mathematical agreement, physical signature comparison, GAP-ledger data, residue exposure, witness route, transport, replay, provenance, and local naming.

The classifier-separation obligation reads $M$ and $S$ as different rows of the same packet. The residue-exposure obligation then requires $G,R,W$ before any consumer can use the gap as evidence against physical adequacy.

Finally, the rows $H,C,P,N$ keep the packet stable under componentwise transport and displayed replay. Since no further coordinate exists, the five listed obligations exhaust the local $\NameCert_{\RealityConstrainedSignatureResidueUp}$ surface.
\end{proof}
\leanchecked{BEDC.Derived.RealityConstrainedSignatureResidueUp.RealityConstrainedSignatureResidueTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Reality-constrained signature residue consumer boundary]
\label{thm:reality-constrained-signature-residue-consumer-boundary}
Let $Z=(M,S,G,R,W,H,C,P,N)$ be an accepted $\RealityConstrainedSignatureResidueUp$ packet. Every public consumer of the residue packet factors through exactly the displayed carrier route
$$
M,S,G,R,W,H,C,P,N.
$$
It may read mathematical classifier agreement, physical-signature comparison, GAP and methodology-ledger data, residue, witness route, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\RealityConstrainedSignatureResidueUp}$ row. It has no direct consumer coordinate for host physical adequacy, objectivity completion, or physical-fit resolution outside that route.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:reality-constrained-signature-residue-carrier}. Its only visible rows are $M,S,G,R,W,H,C,P,N$, so a consumer of the local packet has no additional carrier coordinate to inspect.

Apply the NameCert obligations of \autoref{thm:reality-constrained-signature-residue-namecert-obligations}. They require classifier separation, residue exposure through $G,R,W$, componentwise transport and replay through $H,C$, and provenance plus local naming through $P,N$.

The sibling anchors in \autoref{ch:concrete-instances-reality-constrained-signature-fit-namecert}, \autoref{ch:concrete-instances-reality-constrained-open-fit-namecert}, \autoref{ch:concrete-instances-relational-objectivity-namecert}, and \autoref{ch:concrete-instances-reality-constrained-scientific-methodology-namecert} are cited only through the provenance route. Hence a public consumer factors through the displayed route and not through a hidden adequacy or objectivity-completion output.
\end{proof}

\begin{theorem}[Reality-constrained signature residue dependency handoff]
\label{thm:reality-constrained-signature-residue-dependency-handoff}
The $\RealityConstrainedSignatureResidueUp$ packet consumes exactly the reality-constrained sibling surfaces
$$
\RealityConstrainedSignatureFitUp,\quad
\RealityConstrainedOpenFitUp,\quad
\RelationalObjectivityUp,\quad
\RealityConstrainedMethodologyLedgerUp
$$
for signature-fit compatibility, open-fit admission, objectivity rows, and methodology provenance. Beyond those consumed sibling surfaces and the ambient $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ structure, the packet contributes only the residual signature-separation row $R$ together with its displayed witness route $W$ and local naming row $N$.
\end{theorem}
\begin{proof}
Read the dependency row in the carrier definition. The row $P$ names $\RealityConstrainedSignatureResidueUp$ together with $\RealityConstrainedSignatureFitUp$, $\RealityConstrainedOpenFitUp$, $\RelationalObjectivityUp$, $\RealityConstrainedMethodologyLedgerUp$, and the structural $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ surfaces.

The four sibling chapters cited in the introduction provide the consumed reality-constrained surfaces: signature fit, open fit, relational objectivity, and methodology ledger. The carrier does not name a fifth reality-constrained sibling dependency.

Apply the NameCert obligations. Their classifier-separation and residue-exposure rows force the new local contribution to be the displayed separation $R$ with its witness route $W$ and name $N$, rather than a duplicate of the consumed sibling packets or an adequacy conclusion.
\end{proof}

\begin{theorem}[Reality-constrained signature residue obligation closure]
\label{thm:reality-constrained-signature-residue-obligation-closure}
The obligation surface of an accepted $\RealityConstrainedSignatureResidueUp$ packet is exactly the finite package consisting of carrier admission, classifier separation, residue exposure, witness-route display, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\RealityConstrainedSignatureResidueUp}$ naming for the route $M,S,G,R,W,H,C,P,N$. This closure is the local TasteGate-facing obligation surface of the packet and does not assert bridge checking or public physical adequacy.
\end{theorem}
\begin{proof}
Start with \autoref{def:reality-constrained-signature-residue-carrier}. It supplies the carrier rows $M,S,G,R,W,H,C,P,N$ and separates mathematical agreement from physical-signature comparison before the residue row can be read.

Apply \autoref{thm:reality-constrained-signature-residue-namecert-obligations}. The five NameCert rows expand to carrier admission, classifier separation, residue exposure, stability under $H,C$, and provenance plus naming under $P,N$.

Use the consumer boundary and dependency handoff theorems above. The first keeps every consumer on the displayed route, while the second fixes the four consumed sibling surfaces and isolates the residual signature-separation contribution.

The TasteGate alignment already displayed at this NameCert site is therefore read only as a local obligation alignment. Since no standard bridge row or public-adequacy row is present in the carrier, the closure reached here is the obligation surface and not a bridge-checked or physical-adequacy claim.
\end{proof}

\begin{theorem}[Reality-constrained signature residue non-escape]
\label{thm:reality-constrained-signature-residue-nonescape}
Let $Z=(M,S,G,R,W,H,C,P,N)$ be an accepted $\RealityConstrainedSignatureResidueUp$ packet. No consumer may export physical adequacy, physical inadequacy, or physical-fit resolution from the mathematical classifier row $M$ alone. Every public physical-fit consumer must first expose the physical signature row $S$, GAP and methodology-ledger row $G$, residue row $R$, witness route $W$, transport row $H$, replay row $C$, provenance row $P$, and local naming row $N$.
\end{theorem}
\begin{proof}
Start from the NameCert obligations of \autoref{thm:reality-constrained-signature-residue-namecert-obligations}. They make classifier separation and residue exposure mandatory before any physical-fit claim can leave the packet.

If a route reads only $M$, it has mathematical classifier agreement but no displayed physical-signature comparison, GAP ledger, residue row, or witness route. Such a route therefore lacks the rows required for a physical adequacy or inadequacy claim.

Reading $S,G,R,W,H,C,P,N$ restores the displayed packet route. The conclusion is a non-escape boundary: the consumer may use the accepted residue packet only through its visible rows, never through a direct export from mathematical classifier agreement.
\end{proof}

\begin{theorem}[Reality-constrained signature residue sibling independence]
\label{thm:reality-constrained-signature-residue-sibling-independence}
The $\RealityConstrainedSignatureResidueUp$ carrier is not bijective to $\RealityConstrainedSignatureFitUp$, $\RealityConstrainedOpenFitUp$, $\RelationalObjectivityUp$, or $\RealityConstrainedMethodologyLedgerUp$. SignatureFit records successful compatibility, OpenFit records admitted open fit routes, RelationalObjectivity records objectivity rows, and MethodologyLedger records method provenance, while the residue packet records the remaining reality-facing signature separation after mathematical identification.
\end{theorem}
\begin{proof}
Compare the displayed coordinates. The signature-fit packet supplies compatibility rows but need not carry a remaining residue row $R$ or a witness route $W$ for a failed physical-signature descent. The open-fit packet supplies continuation and finite refutation surfaces, but it does not by itself bind the mathematical classifier row $M$ to a surviving representation-gap residue.

The relational-objectivity packet supplies objectivity rows, and the methodology ledger supplies method provenance. Neither one alone contains the joint classifier, physical-signature, GAP, residue, witness, transport, replay, provenance, and local naming coordinates of $Z$.

Thus each sibling may be consumed by the residue packet, but none is interchangeable with it. The carrier independence is exactly the remaining row $R$ displayed between mathematical identification and physical adequacy.
\end{proof}

\falsifiablePrediction{If a RealityConstrainedSignatureResidueUp packet is accepted, every public physical-fit consumer must expose the residue row before claiming adequacy; a route from mathematical classifier agreement directly to physical adequacy refutes the packet.}

\independenceWitness{reality-constrained-signature-fit, reality-constrained-open-fit, relational-objectivity, reality-constrained-methodology-ledger: SignatureFit records successful compatibility, OpenFit records admitted open fit routes, RelationalObjectivity records objectivity rows, and MethodologyLedger records method provenance, while the residue packet records the remaining reality-facing signature separation after mathematical identification.}

\closureat{RealityConstrainedSignatureResidueUp}{seedStr}
\closureat{RealityConstrainedSignatureResidueUp}{obligationStr}

\begin{theorem}[Reality-constrained signature residue scope lock]
\label{thm:reality-constrained-signature-residue-scope-lock}
Let $Z=(M,S,G,R,W,H,C,P,N)$ be an accepted $\RealityConstrainedSignatureResidueUp$ packet. The residue scope is locked to the displayed dependencies on $\RealityConstrainedSignatureFitUp$, $\RealityConstrainedOpenFitUp$, $\RelationalObjectivityUp$, and $\RealityConstrainedMethodologyLedgerUp$: $M$ reads the signature-fit classifier, $S$ and $G$ read the open-fit and relational-objectivity comparison surface, $R$ stores only the remaining signature residue, and $W,H,C,P,N$ expose the methodology-ledger witness, transport, replay, provenance, and local naming rows. No consumer may enlarge the scope to a host physical-adequacy judgment or shrink it to mathematical classifier agreement alone.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:reality-constrained-signature-residue-carrier}. The rows $M,S,G,R,W,H,C,P,N$ already list the classifier, physical signature, gap ledger, residue, witness, transport, replay, provenance, and local naming surfaces.

Apply the NameCert obligations of \autoref{thm:reality-constrained-signature-residue-namecert-obligations}. They force classifier separation and residue exposure before any physical-fit consumer can read the packet.

Finally invoke the non-escape theorem, \autoref{thm:reality-constrained-signature-residue-nonescape}. A route from $M$ alone lacks $S,G,R,W$, while a route to host physical adequacy lacks a coordinate in the carrier; hence the displayed sibling dependencies are exactly the locked scope.
\end{proof}

\begin{theorem}[Reality-constrained signature residue witness-route totality]
\label{thm:reality-constrained-signature-residue-witness-route-totality}
For every accepted $\RealityConstrainedSignatureResidueUp$ packet $Z=(M,S,G,R,W,H,C,P,N)$, every public witness of the residue row $R$ factors through the finite route
$$
M,S,G,R,W,H,C,P,N
$$
with $P$ citing $\RealityConstrainedSignatureFitUp$, $\RealityConstrainedOpenFitUp$, $\RelationalObjectivityUp$, and $\RealityConstrainedMethodologyLedgerUp$. Conversely, that displayed route supplies all witness data available to a consumer of the residue packet.
\end{theorem}
\leanchecked{BEDC.Derived.RealityConstrainedSignatureResidueUp.RealityConstrainedSignatureResidueWitnessRouteTotality}
\begin{proof}
First read the witness row in the carrier. The row $W$ is placed after the mathematical classifier, physical signature, gap-ledger, and residue rows, so a witness to $R$ is not detached from $M,S,G$.

The NameCert obligations identify $G,R,W$ as the residue-exposure surface. The transport and replay rows $H,C$ then preserve that same exposure under componentwise movement and displayed continuation replay.

The provenance row $P$ supplies the listed sibling dependencies, and $N$ locally names the same packet. Since the non-escape theorem rules out physical-fit resolution from $M$ alone or from any hidden host coordinate, the displayed route is total for residue witnesses.
\end{proof}

\begin{theorem}[Reality-constrained signature residue consumer adequacy refusal]
\label{thm:reality-constrained-signature-residue-consumer-adequacy-refusal}
An accepted $\RealityConstrainedSignatureResidueUp$ consumer that exposes $M,S,G,R,W,H,C,P,N$ may report the remaining signature residue, but it may not report physical adequacy, physical-fit resolution, or objectivity completion unless a separate packet supplies those claims through $\RealityConstrainedSignatureFitUp$, $\RealityConstrainedOpenFitUp$, $\RelationalObjectivityUp$, or $\RealityConstrainedMethodologyLedgerUp$. The residue packet itself is a refusal boundary for adequacy by mathematical agreement alone.
\end{theorem}
\begin{proof}
Begin with the sibling independence theorem, \autoref{thm:reality-constrained-signature-residue-sibling-independence}. It separates the residue carrier from the signature-fit, open-fit, relational-objectivity, and methodology-ledger carriers.

Next use non-escape, \autoref{thm:reality-constrained-signature-residue-nonescape}. The mathematical classifier row $M$ cannot export physical adequacy, physical inadequacy, or physical-fit resolution without the rows $S,G,R,W,H,C,P,N$.

The accepted residue consumer therefore receives a residue report, not an adequacy closure. Any positive adequacy or objectivity-completion claim must come from a separate accepted sibling packet, while this packet refuses the shortcut from mathematical classifier agreement to physical adequacy.
\end{proof}

\begin{theorem}[Reality-constrained signature residue sibling-route exactness]
\label{thm:reality-constrained-signature-residue-sibling-route-exactness}
For an accepted $\RealityConstrainedSignatureResidueUp$ packet, the sibling route is exactly the displayed dependency on $\RealityConstrainedSignatureFitUp$, $\RealityConstrainedOpenFitUp$, $\RelationalObjectivityUp$, and $\RealityConstrainedMethodologyLedgerUp$ through the rows
$$
M,S,G,R,W,H,C,P,N.
$$
No sibling alone supplies the residue packet, and no residue consumer may replace the displayed route by a host physical-adequacy judgment.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:reality-constrained-signature-residue-sibling-independence}. It separates the residue carrier from the signature-fit, open-fit, relational-objectivity, and methodology-ledger carriers.

Apply \autoref{thm:reality-constrained-signature-residue-witness-route-totality}. Every public witness of the residue row factors through $M,S,G,R,W,H,C,P,N$ with $P$ citing the same sibling packets.

Use \autoref{thm:reality-constrained-signature-residue-consumer-adequacy-refusal}. A host physical-adequacy judgment is not exported by the residue packet, so the sibling route is exactly the displayed finite dependency.
\end{proof}

\begin{theorem}[Reality-constrained signature residue mathematical-agreement refusal]
\label{thm:reality-constrained-signature-residue-mathematical-agreement-refusal}
Mathematical classifier agreement in the row $M$ of an accepted $\RealityConstrainedSignatureResidueUp$ packet does not imply physical adequacy, physical-fit resolution, or objectivity completion. Any such claim must pass through the physical-signature, GAP, residue, witness, transport, replay, provenance, and local naming rows $S,G,R,W,H,C,P,N$ and through a separate accepted sibling packet when the claim is positive.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:reality-constrained-signature-residue-namecert-obligations}. They read $M$ as mathematical agreement and $S$ as the physical-signature comparison, so the two rows cannot be collapsed.

Apply non-escape, \autoref{thm:reality-constrained-signature-residue-nonescape}. A route from $M$ alone cannot export physical adequacy, physical inadequacy, or physical-fit resolution.

Finally use \autoref{thm:reality-constrained-signature-residue-consumer-adequacy-refusal}. Positive adequacy or objectivity-completion claims must come from a separate accepted sibling packet, while this residue packet refuses the shortcut from classifier agreement to physical adequacy.
\end{proof}

\begin{theorem}[Reality-constrained signature residue methodology-ledger totality]
\label{thm:reality-constrained-signature-residue-methodology-ledger-totality}
Every public methodology-ledger read used by an accepted $\RealityConstrainedSignatureResidueUp$ packet is contained in the finite route
$$
G,W,H,C,P,N.
$$
The ledger is exposed as GAP and method provenance for the residue witness, not as a free-standing proof of physical adequacy or objectivity completion.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:reality-constrained-signature-residue-carrier}. The methodology-ledger data enters through $G$, is witnessed through $W$, and is transported, replayed, sourced, and named by $H,C,P,N$.

Apply \autoref{thm:reality-constrained-signature-residue-witness-route-totality}. It makes the witness route for $R$ pass through the same GAP, witness, transport, replay, provenance, and local naming rows.

Finish with \autoref{thm:reality-constrained-signature-residue-consumer-adequacy-refusal}. The residue consumer receives a residue report rather than an adequacy closure, so the methodology ledger is total only for the displayed witness route.
\end{proof}

\begin{theorem}[Reality-constrained signature residue carrier row readback]
\label{thm:reality-constrained-signature-residue-carrier-row-readback}
Every public residue read of an accepted $\RealityConstrainedSignatureResidueUp$
packet projects to the displayed finite route
$$
M,S,G,R,W,H,C,P,N.
$$
The read may expose mathematical classifier agreement, physical signature
comparison, GAP and methodology-ledger data, residue, witness route,
componentwise transport, continuation replay, provenance, and local naming,
but it cannot read physical adequacy or objectivity completion outside those
rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:reality-constrained-signature-residue-carrier}.
Its coordinates are exactly $M,S,G,R,W,H,C,P,N$, so a public residue read has no
other row available.

Apply the NameCert obligations and non-escape theorem,
\autoref{thm:reality-constrained-signature-residue-namecert-obligations} and
\autoref{thm:reality-constrained-signature-residue-nonescape}. They require
classifier separation, residue exposure, transport, replay, provenance, and
local naming before a physical-fit consumer may read the packet.

Use witness-route totality and sibling-route exactness,
\autoref{thm:reality-constrained-signature-residue-witness-route-totality} and
\autoref{thm:reality-constrained-signature-residue-sibling-route-exactness}. The
public residue read is therefore the displayed route and not a hidden adequacy
or objectivity-completion judgment.
\end{proof}

\begin{theorem}[Reality-constrained signature residue public consumer factorization]
\label{thm:reality-constrained-signature-residue-public-consumer-factorization}
Every public physical-fit consumer of an accepted
$\RealityConstrainedSignatureResidueUp$ packet factors first through
$\RealityConstrainedSignatureFitUp$, $\RealityConstrainedOpenFitUp$,
$\RelationalObjectivityUp$, and $\RealityConstrainedMethodologyLedgerUp$ as
recorded by the rows $M,S,G,W,H,C,P,N$, and only then reads the residue row
$R$. In particular, signature-fit, open-fit, relational-objectivity, and
methodology-ledger surfaces must all be visible before the consumer treats the
residue as a physical-fit obstruction.
\end{theorem}
\begin{proof}
Begin with sibling independence,
\autoref{thm:reality-constrained-signature-residue-sibling-independence}. It
separates the residue carrier from the signature-fit, open-fit,
relational-objectivity, and methodology-ledger siblings while allowing the
residue packet to consume them.

Apply witness-route totality and sibling-route exactness,
\autoref{thm:reality-constrained-signature-residue-witness-route-totality} and
\autoref{thm:reality-constrained-signature-residue-sibling-route-exactness}. The
public route to the residue passes through $M,S,G,W,H,C,P,N$ with provenance
citing the sibling packets.

Finally use methodology-ledger totality,
\autoref{thm:reality-constrained-signature-residue-methodology-ledger-totality}.
The methodology row is exposed for the residue witness rather than for a
free-standing adequacy claim, so the public consumer must factor through those
surfaces before reading $R$.
\end{proof}

\begin{theorem}[Reality-constrained signature residue row-order totality]
\label{thm:reality-constrained-signature-residue-row-order-totality}
Every public residue read of an accepted $\RealityConstrainedSignatureResidueUp$
packet exposes the finite rows in the order
$$
M,S,G,R,W,H,C,P,N.
$$
The read may stop at no earlier row as a complete residue report: $M$ supplies
mathematical classifier agreement, $S$ supplies the physical-signature
comparison, $G$ supplies the GAP and methodology-ledger context, $R$ supplies the
displayed residue, $W$ supplies the witness route, $H$ and $C$ supply transport
and replay, $P$ supplies provenance, and $N$ supplies the local NameCert row.
\end{theorem}
\begin{proof}
Project the carrier of
\autoref{def:reality-constrained-signature-residue-carrier}. Its displayed
coordinates are exactly $M,S,G,R,W,H,C,P,N$, and the NameCert obligations of
\autoref{thm:reality-constrained-signature-residue-namecert-obligations} read
those coordinates as carrier admission, classifier separation, residue exposure,
transport and replay stability, provenance, and local naming.

Apply witness-route totality,
\autoref{thm:reality-constrained-signature-residue-witness-route-totality}. A
public witness of $R$ factors through the same ordered route, so no consumer can
present $R$ before $M,S,G$ or detach it from $W,H,C,P,N$.

Finally use public-consumer factorization,
\autoref{thm:reality-constrained-signature-residue-public-consumer-factorization}.
The fit-facing consumer must see the sibling surfaces and methodology ledger
before reading $R$, hence the displayed row order is total for every public
residue read.
\end{proof}

\begin{theorem}[Reality-constrained signature residue audit consumer readiness]
\label{thm:reality-constrained-signature-residue-audit-consumer-readiness}
An audit consumer may treat an accepted $\RealityConstrainedSignatureResidueUp$
packet as actionable only after seeing all nine readiness rows: the classifier
row $M$, the signature row $S$, the GAP and ledger row $G$, the residue row $R$,
the witness route $W$, the transport row $H$, the replay row $C$, the provenance
row $P$, and the local NameCert row $N$. Before those rows are visible, the
consumer has at most a partial inspection route and not an actionable residue
certificate.
\end{theorem}
\begin{proof}
Start with the NameCert obligations,
\autoref{thm:reality-constrained-signature-residue-namecert-obligations}. They
make carrier admission, classifier separation, residue exposure, transport,
replay, provenance, and local naming mandatory parts of the packet rather than
optional annotations.

Use non-escape,
\autoref{thm:reality-constrained-signature-residue-nonescape}. A route from $M$
alone cannot export physical adequacy, physical inadequacy, or physical-fit
resolution; the audit consumer must expose $S,G,R,W,H,C,P,N$ before any
fit-facing use of the residue is available.

Combine row-order totality and public-consumer factorization,
\autoref{thm:reality-constrained-signature-residue-row-order-totality} and
\autoref{thm:reality-constrained-signature-residue-public-consumer-factorization}.
The actionable audit route is therefore exactly the ordered nine-row surface,
with the classifier, signature, GAP, residue, witness, transport, replay,
provenance, and local naming rows all present.
\end{proof}

\begin{theorem}[Reality-constrained signature residue fit-obstruction handoff]
\label{thm:reality-constrained-signature-residue-fit-obstruction-handoff}
Every downstream fit-obstruction consumer of an accepted
$\RealityConstrainedSignatureResidueUp$ packet receives only the displayed
residue obstruction carried by $R$ along the route $M,S,G,R,W,H,C,P,N$. The
handoff does not supply physical adequacy, physical-fit resolution, or
objectivity completion; those claims require separate accepted sibling packets.
\end{theorem}
\begin{proof}
Begin with non-escape,
\autoref{thm:reality-constrained-signature-residue-nonescape}. It blocks any
handoff from exporting adequacy, inadequacy, or fit resolution from mathematical
classifier agreement alone.

Apply witness-route totality and public-consumer factorization,
\autoref{thm:reality-constrained-signature-residue-witness-route-totality} and
\autoref{thm:reality-constrained-signature-residue-public-consumer-factorization}.
The downstream consumer receives the residue only through the displayed route
and through the sibling surfaces named by provenance.

Finally use audit consumer readiness,
\autoref{thm:reality-constrained-signature-residue-audit-consumer-readiness}. The
consumer may act on the residue obstruction once the nine rows are exposed, but
the same readiness surface contains no coordinate for physical adequacy or
objectivity completion.
\end{proof}

\begin{theorem}[Reality-constrained signature residue methodology provenance nonescape]
\label{thm:reality-constrained-signature-residue-methodology-provenance-nonescape}
In an accepted $\RealityConstrainedSignatureResidueUp$ packet, methodology
provenance in $G,W,H,C,P,N$ cannot be exported as physical adequacy,
physical-fit resolution, or objectivity completion. It remains provenance for
the displayed residue route $M,S,G,R,W,H,C,P,N$ and must be paired with a
separate accepted sibling packet before any positive adequacy or objectivity
claim is available.
\end{theorem}
\begin{proof}
Start with methodology-ledger totality,
\autoref{thm:reality-constrained-signature-residue-methodology-ledger-totality}.
The ledger is total for the route $G,W,H,C,P,N$, not for adequacy or objectivity
completion.

Apply consumer adequacy refusal and mathematical-agreement refusal,
\autoref{thm:reality-constrained-signature-residue-consumer-adequacy-refusal} and
\autoref{thm:reality-constrained-signature-residue-mathematical-agreement-refusal}.
They rule out physical adequacy, physical-fit resolution, and objectivity
completion from either $M$ alone or from the residue packet alone.

Finally use the carrier row readback of
\autoref{thm:reality-constrained-signature-residue-carrier-row-readback}. Since
the public read is the same displayed route, methodology provenance cannot
leave the packet as an independent physical-adequacy or objectivity-completion
certificate.
\end{proof}

\begin{theorem}[Reality-constrained signature residue carrier admission]
\label{thm:reality-constrained-signature-residue-carrier-admission}
Every accepted $\RealityConstrainedSignatureResidueUp$ packet exposes the finite rows
$$
M,S,G,R,W,H,C,P,N
$$
as $\BHist$-carrier data before a public residue consumer may read the local name. These rows respectively carry mathematical classifier agreement, physical signature comparison, GAP and methodology-ledger data, residue, witness route, componentwise transport, displayed replay, package provenance, and local $\NameCert_{\RealityConstrainedSignatureResidueUp}$ naming.
\end{theorem}
\begin{proof}
Project the carrier row readback of \autoref{thm:reality-constrained-signature-residue-carrier-row-readback}. It states that every public residue read of an accepted packet has exactly the displayed route through $M,S,G,R,W,H,C,P,N$.

Apply public-consumer factorization, \autoref{thm:reality-constrained-signature-residue-public-consumer-factorization}. The consumer must pass through the signature-fit, open-fit, relational-objectivity, and methodology-ledger surfaces recorded by the same rows before reading $R$ as a physical-fit obstruction.

Finally use methodology-provenance nonescape, \autoref{thm:reality-constrained-signature-residue-methodology-provenance-nonescape}. Since methodology provenance cannot leave the packet as adequacy or objectivity completion, the admitted carrier data are exactly the displayed finite $\BHist$ rows.
\end{proof}

\begin{theorem}[Reality-constrained signature residue classifier exactness]
\label{thm:reality-constrained-signature-residue-classifier-exactness}
Classifier acceptance for $\RealityConstrainedSignatureResidueUp$ is exactly preservation of the rows $M,S,G,R,W,H,C,P,N$ under the local residue route. A transported packet is accepted as the same residue packet precisely when mathematical classifier agreement, signature comparison, methodology ledger, residue, witness, transport, replay, provenance, and local naming remain on that route.
\end{theorem}
\begin{proof}
Begin with carrier admission, \autoref{thm:reality-constrained-signature-residue-carrier-admission}. It identifies the carrier surface that a residue classifier is allowed to compare.

Use public-consumer factorization to fix the route order. The consumer does not read $R$ before the sibling surfaces and methodology row have been displayed, so classifier preservation cannot be tested on $R$ alone or on mathematical agreement alone.

The reverse containment follows from carrier row readback. Once $M,S,G,R,W,H,C,P,N$ are preserved along the local route, there is no additional carrier coordinate for the classifier to inspect. Methodology-provenance nonescape prevents a preserved methodology row from being reclassified as physical adequacy or objectivity completion.
\end{proof}

\begin{theorem}[Reality-constrained signature residue NameCert ledger]
\label{thm:reality-constrained-signature-residue-namecert-ledger}
The $\NameCert_{\RealityConstrainedSignatureResidueUp}$ ledger is exhausted by methodology, signature, residue, witness, transport, replay, provenance, and local name rows. It contains no additional ledger entry for physical adequacy, physical-fit resolution, objectivity completion, or a detached mathematical classifier certificate.
\end{theorem}
\begin{proof}
Start with the methodology-provenance nonescape theorem. It keeps the methodology rows $G,W,H,C,P,N$ inside the residue route rather than allowing them to export adequacy or objectivity completion.

Next combine carrier row readback with public-consumer factorization. The public consumer sees signature comparison and sibling fit surfaces through $M,S,G,W,H,C,P,N$, reads the residue row $R$ only on that route, and then reaches the local name $N$.

It remains to rule out a second ledger row. Classifier exactness shows that acceptance preserves exactly $M,S,G,R,W,H,C,P,N$ under the local residue route. Since the carrier has no coordinate outside those rows, the NameCert ledger is exhausted by methodology, signature, residue, witness, transport, replay, provenance, and local naming.
\end{proof}

\begin{theorem}[Reality-constrained signature residue gap-row exhaustion]
\label{thm:reality-constrained-signature-residue-gap-row-exhaustion}
Let $Z=(M,S,G,R,W,H,C,P,N)$ be an accepted $\RealityConstrainedSignatureResidueUp$ packet. Every public residue read that reaches the local $\NameCert_{\RealityConstrainedSignatureResidueUp}$ row must expose the gap row $G$, the residue row $R$, and the witness route $W$ as $\BHist$-carrier data. In particular, the read cannot collapse the packet to mathematical classifier agreement $M$ alone, nor can it replace $G,R,W$ by a detached adequacy, physical-fit resolution, or objectivity-completion entry.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:reality-constrained-signature-residue-carrier}. Its displayed $\BHist$ packet contains $G,R,W$ as named coordinates between the classifier and the transport-replay rows.

Apply the NameCert obligations and nonescape theorem,
\autoref{thm:reality-constrained-signature-residue-namecert-obligations} and
\autoref{thm:reality-constrained-signature-residue-nonescape}. The obligations make residue exposure a local certificate condition, while nonescape prevents the same packet from exporting adequacy or objectivity completion outside the displayed route.

Use witness-route totality and sibling-route exactness,
\autoref{thm:reality-constrained-signature-residue-witness-route-totality} and
\autoref{thm:reality-constrained-signature-residue-sibling-route-exactness}. They force the public route to pass through the gap, residue, and witness coordinates before a consumer reaches $N$.

Finally combine carrier row readback with the NameCert ledger theorem,
\autoref{thm:reality-constrained-signature-residue-carrier-row-readback} and
\autoref{thm:reality-constrained-signature-residue-namecert-ledger}. Since the ledger has no detached mathematical-classifier certificate and no hidden adequacy row, a public residue read cannot be reduced to $M$ alone.
\end{proof}

\begin{theorem}[Reality-constrained signature residue physical signature separation]
\label{thm:reality-constrained-signature-residue-physical-signature-separation}
In every accepted $\RealityConstrainedSignatureResidueUp$ packet $Z=(M,S,G,R,W,H,C,P,N)$, the physical-signature row $S$ is a visible carrier coordinate independent of mathematical classifier agreement $M$. Any public adequacy-facing or physical-fit-facing claim must display $S$ together with $G,R,W,H,C,P,N$ before it may cite the residue packet; agreement in $M$ alone is not a substitute for the physical-signature comparison.
\end{theorem}
\begin{proof}
Read $M$ and $S$ from the carrier of \autoref{def:reality-constrained-signature-residue-carrier}. The carrier lists them as different $\BHist$ coordinates: $M$ records mathematical classifier agreement, while $S$ records physical signature comparison.

Apply the NameCert obligations, nonescape, and mathematical-agreement refusal,
\autoref{thm:reality-constrained-signature-residue-namecert-obligations},
\autoref{thm:reality-constrained-signature-residue-nonescape}, and
\autoref{thm:reality-constrained-signature-residue-mathematical-agreement-refusal}. These results require classifier separation and prohibit export of adequacy, physical-fit resolution, or objectivity completion from $M$ alone.

Next use witness-route totality and sibling-route exactness,
\autoref{thm:reality-constrained-signature-residue-witness-route-totality} and
\autoref{thm:reality-constrained-signature-residue-sibling-route-exactness}. The accepted route carries the sibling physical-fit surfaces through $S,G,W,H,C,P,N$ before the residue row can be read.

Carrier row readback and public-consumer factorization then identify the visible public surface. Because the same read must expose $S$ before it can use the residue packet as a physical-fit obstruction, mathematical classifier agreement cannot absorb the physical-signature coordinate.
\end{proof}

\begin{theorem}[Reality-constrained signature residue witness replay stability]
\label{thm:reality-constrained-signature-residue-witness-replay-stability}
Let $Z=(M,S,G,R,W,H,C,P,N)$ be an accepted $\RealityConstrainedSignatureResidueUp$ packet. The transport row $H$ and replay row $C$ preserve the residue witness route componentwise: under the recorded $\hsame$ transport in $H$ and displayed $\Cont$ replay in $C$, the rows $M,S,G,R,W,H,C,P,N$ remain on the same public residue route, and the witness component $W$ cannot be replayed as a route omitting $G$, $R$, or the sibling physical-signature surface.
\end{theorem}
\begin{proof}
Begin with the carrier and the NameCert obligations. The carrier supplies the rows $M,S,G,R,W,H,C,P,N$, and the obligations require $H$ and $C$ to preserve the displayed residue exposure, provenance, and local naming rows.

Apply nonescape together with witness-route totality,
\autoref{thm:reality-constrained-signature-residue-nonescape} and
\autoref{thm:reality-constrained-signature-residue-witness-route-totality}. Nonescape keeps the route inside the residue packet, while witness-route totality makes $W$ a total public route through the gap and residue rows.

Use sibling-route exactness to keep the physical-signature, open-fit, relational-objectivity, and methodology-ledger dependencies on the same route. The $\hsame$ data in $H$ can transport only the displayed coordinates, and the $\Cont$ data in $C$ can replay only the displayed continuation route.

Finally appeal to carrier row readback and classifier exactness. They identify the transported and replayed read with preservation of exactly $M,S,G,R,W,H,C,P,N$, so no componentwise replay can remove $G$, remove $R$, detach $W$, or hide the physical-signature surface behind mathematical classifier agreement alone.
\end{proof}

\begin{theorem}[Reality-constrained signature residue fit readback]
\label{thm:reality-constrained-signature-residue-fit-readback}
Every public fit-facing read of an accepted $\RealityConstrainedSignatureResidueUp$ packet is anchored in the $\BHist$ rows
$$
M,S,G,R,W,H,C,P,N.
$$
The read may cite $\RealityConstrainedSignatureFitUp$, $\RealityConstrainedOpenFitUp$, $\RelationalObjectivityUp$, and $\RealityConstrainedMethodologyLedgerUp$ only through the displayed $\Pkg$ row $P$ and the same witness route $W$; it cannot read a physical-fit conclusion from the mathematical classifier row $M$ alone.
\end{theorem}
\begin{proof}
Begin with carrier row readback, \autoref{thm:reality-constrained-signature-residue-carrier-row-readback}. It projects every public residue read to $M,S,G,R,W,H,C,P,N$.

Apply public-consumer factorization and sibling-route exactness, \autoref{thm:reality-constrained-signature-residue-public-consumer-factorization} and \autoref{thm:reality-constrained-signature-residue-sibling-route-exactness}. They force the fit-facing consumer through the sibling surfaces recorded by $M,S,G,W,H,C,P,N$ before the residue row $R$ is read.

Finally use mathematical-agreement refusal, \autoref{thm:reality-constrained-signature-residue-mathematical-agreement-refusal}. Agreement in $M$ is not a physical-fit conclusion, so the readback remains anchored in the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\end{proof}

\begin{theorem}[Reality-constrained signature residue constraint evidence display]
\label{thm:reality-constrained-signature-residue-constraint-evidence-display}
For every accepted $\RealityConstrainedSignatureResidueUp$ packet $Z=(M,S,G,R,W,H,C,P,N)$, the reality-constraint evidence used by the residue row is displayed through $S,G,R,W$ and preserved through $H,C,P,N$. A consumer may not replace those rows by unrecorded observational evidence, host adequacy judgment, or detached methodology provenance.
\end{theorem}
\begin{proof}
Read the physical-signature separation theorem, \autoref{thm:reality-constrained-signature-residue-physical-signature-separation}. It makes $S$ visible independently of $M$ before any adequacy-facing claim can cite the residue packet.

Use gap-row exhaustion and methodology provenance nonescape, \autoref{thm:reality-constrained-signature-residue-gap-row-exhaustion} and \autoref{thm:reality-constrained-signature-residue-methodology-provenance-nonescape}. They require $G,R,W$ and keep methodology provenance inside the residue route.

The transport and replay rows are fixed by witness replay stability. Thus $H$ and $C$ preserve the displayed evidence route, while $P$ and $N$ package and name it without adding an unrecorded observational coordinate.
\end{proof}

\begin{theorem}[Reality-constrained signature residue unrecorded-evidence nonescape]
\label{thm:reality-constrained-signature-residue-unrecorded-evidence-nonescape}
No accepted $\RealityConstrainedSignatureResidueUp$ consumer may export unrecorded reality-constraint evidence, physical adequacy, or objectivity completion from the route $M,S,G,R,W,H,C,P,N$. Any such export must be supplied by a separate accepted sibling packet and must still pass through the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows when it is cited by the residue packet.
\end{theorem}
\begin{proof}
Start with constraint evidence display, \autoref{thm:reality-constrained-signature-residue-constraint-evidence-display}. The reality-facing evidence available to the residue packet is exactly the displayed physical-signature, gap, residue, witness, transport, replay, provenance, and local naming route.

Apply consumer adequacy refusal and methodology provenance nonescape, \autoref{thm:reality-constrained-signature-residue-consumer-adequacy-refusal} and \autoref{thm:reality-constrained-signature-residue-methodology-provenance-nonescape}. They prevent the residue packet from exporting adequacy, fit resolution, objectivity completion, or detached methodology provenance.

Finally use fit readback, \autoref{thm:reality-constrained-signature-residue-fit-readback}. A positive sibling claim may be cited only through the displayed route, so unrecorded evidence cannot leave the packet as if it were part of the local $\NameCert_{\RealityConstrainedSignatureResidueUp}$ surface.
\end{proof}

\begin{theorem}[Reality-constrained signature residue source exactness]
\label{thm:reality-constrained-signature-residue-source-exactness}
Let $Z=(M,S,G,R,W,H,C,P,N)$ be an accepted
$\RealityConstrainedSignatureResidueUp$ packet. Suppose a public residue read
of $Z$ exposes the methodology-ledger, open-fit, signature-fit, and
relational-objectivity sibling rows together with the displayed $\BHist$ carrier,
the local $\hsame$ transport row, the $\Cont$ replay row, the $\Pkg$ provenance
row, and the local $\NameCert_{\RealityConstrainedSignatureResidueUp}$ support.
Then its source surface is exactly the source surface of
$\RealityConstrainedSignatureResidueUp$: no off-ledger physical-signature row,
objectivity row, fit row, or detached adequacy witness can enter the packet.
\end{theorem}
\begin{proof}
Begin with carrier admission and classifier exactness,
\autoref{thm:reality-constrained-signature-residue-carrier-admission} and
\autoref{thm:reality-constrained-signature-residue-classifier-exactness}. They
identify the admitted carrier and the accepted classifier with preservation of
the rows $M,S,G,R,W,H,C,P,N$ under the local residue route.

Next use the NameCert ledger and gap-row exhaustion,
\autoref{thm:reality-constrained-signature-residue-namecert-ledger} and
\autoref{thm:reality-constrained-signature-residue-gap-row-exhaustion}. The
ledger has no extra entry for adequacy, physical-fit resolution, objectivity
completion, or detached mathematical agreement, while the public read must
display $G,R,W$ before it reaches the local name.

Finally combine sibling-route exactness with fit readback. The methodology-ledger,
open-fit, signature-fit, and relational-objectivity siblings can be cited only
through the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$
rows. Since those rows already exhaust the route, no off-ledger physical-signature,
objectivity, or fit source can be added to the residue packet.
\end{proof}

\begin{theorem}[Reality-constrained signature residue transport closure]
\label{thm:reality-constrained-signature-residue-transport-closure}
Let $Z=(M,S,G,R,W,H,C,P,N)$ be an accepted
$\RealityConstrainedSignatureResidueUp$ packet, and let $Z'$ be obtained by an
$\hsame$-compatible transport that preserves the methodology-ledger, open-fit,
signature-fit, and relational-objectivity sibling rows on the displayed residue
route. Then $Z'$ again satisfies carrier admission, classifier exactness, the
$\NameCert_{\RealityConstrainedSignatureResidueUp}$ ledger, and gap-row
exhaustion for the same residue packet.
\end{theorem}
\begin{proof}
Start with witness replay stability. The $\hsame$ row transports only the
displayed residue route, and the $\Cont$ row replays the same continuation
surface through $M,S,G,R,W,H,C,P,N$.

Carrier admission is preserved because the transported packet still exposes the
same finite $\BHist$ rows. Classifier exactness is preserved because the
classifier compares exactly those rows and the assumed transport keeps the four
sibling rows on the route.

The ledger and exhaustion claims then follow from the source-exactness theorem
and the existing NameCert ledger and gap-row exhaustion theorems. Since transport
does not add a carrier coordinate, it cannot create a new physical-signature,
objectivity, fit, adequacy, or detached mathematical-agreement row.
\end{proof}

\begin{theorem}[Reality-constrained signature residue Lean-facing unblock surface]
\label{thm:reality-constrained-signature-residue-lean-unblock-surface}
The local theorem surface consisting of carrier admission, classifier exactness,
the $\NameCert_{\RealityConstrainedSignatureResidueUp}$ ledger, gap-row
exhaustion, source exactness, and transport closure is sufficient as the
Lean-facing root-unblock handoff for
$\RealityConstrainedSignatureResidueUp$. It presents the carrier, classifier,
ledger, gap, source, and transport obligations without adding any off-route
physical-signature, objectivity, fit, adequacy, or detached mathematical
classifier premise.
\end{theorem}
\begin{proof}
Read the first four rows from carrier admission, classifier exactness, NameCert
ledger, and gap-row exhaustion. These theorems give the admitted $\BHist$
carrier, the exact accepted classifier, the exhausted local ledger, and the
mandatory gap-residue-witness route.

Source exactness supplies the boundary condition for the handoff. It says that
the methodology-ledger, open-fit, signature-fit, and relational-objectivity
sibling rows enter only through the displayed $\BHist$, $\hsame$, $\Cont$,
$\Pkg$, and local $\NameCert$ support.

Transport closure supplies the stability condition. Any $\hsame$-compatible
transport preserving those sibling rows preserves the four earlier obligations.
Thus the six named results form a compact local surface for downstream Lean
work on the chapter without importing an off-ledger source or an unrecorded
adequacy premise.
\end{proof}

\begin{closurestatus}{\RealityConstrainedSignatureResidueUp}
  \constructivestory{The reality-constrained signature residue is generated from a $BHist$ ledger that records methodology rows, physical-signature rows, gap rows, residue rows, witness routes, and explicit separation from mathematical agreement alone. The carrier first reads the residue row under the named constraint, then uses $hsame$ to keep that residue stable across equivalent histories. $\Cont$ extensions replay witness and methodology rows only when the same residue carrier remains visible. The chapter develops this packet through non-escape, scope lock, sibling-route exactness, adequacy refusal, methodology totality, carrier admission, classifier exactness, gap-row exhaustion, physical separation, and witness replay, all without converting the residue into host reality.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{This chapter binds the reality-constrained signature-residue carrier and its non-escape, scope, witness-route, methodology, classifier, gap-row, separation, and replay theorems.}
  \formalstatus{\axiomCleanV}
  \leanchecked{BEDC.Derived.RealityConstrainedSignatureResidueUp.taste\_gate}
  \leantarget{BEDC.Derived.RealityConstrainedSignatureResidueUp.RealityConstrainedSignatureResidueTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{It does not claim physical signature identity, mathematical agreement outside the carrier, or consumer adequacy beyond the recorded witness route.}
  \upgradepath{Reaching scopedClosure requires an explicit dependency-scope theorem over the displayed carrier, witness-route, methodology, classifier, gap-row, separation, and replay surface.}
\end{closurestatus}
