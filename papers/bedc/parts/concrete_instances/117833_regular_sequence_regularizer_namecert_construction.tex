\chapter{A Concrete Naming Certificate for $\RegularSequenceRegularizerUp$}
\label{ch:concrete-instances-regular-sequence-regularizer-namecert}
\origin{human}

Following Bishop and Bridges on constructive real numbers, the $\RegularSequenceRegularizerUp$ packet records the finite route that turns a stream-window Cauchy witness into a regular rational sequence before the terminal real-name seal is consumed. It sits between the finite observation interface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the uniform-modulus surface of \autoref{ch:concrete-instances-regular-cauchy-uniform-modulus-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a displayed regularization interface for Real and Completion consumers; it does not choose a limit, quotient streams, import countable choice, or assert ambient $\RealUp$ completeness.

\begin{definition}[Regular sequence regularizer carrier]
\label{def:regular-sequence-regularizer-carrier}
A $\RegularSequenceRegularizerUp$ carrier is a finite $\BHist$ packet
$$
G=(S,D,K,U,Q,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ finite-window row inspected by the Cauchy request;
\item $D$ is the $\DyadicRatCoreUp$ tolerance row recording the dyadic endpoint and comparison budgets used by the regularization;
\item $K$ is the stream-window Cauchy evidence row, read only through $S$ and $D$;
\item $U$ is the $\RegularCauchyUniformModulusUp$ row that supplies one displayed modulus schedule for the regularization route;
\item $Q$ is the $\RegSeqRatUp$ readback row produced from $S,D,K,U$;
\item $R$ is the terminal $\RealUp$ seal reached only after the regular readback row has been displayed;
\item $H$ records componentwise $\hsame$ transport for $S,D,K,U,Q,R,C,P,N$;
\item $C$ records displayed $\Cont$ replay through the stream window, dyadic tolerance row, Cauchy evidence, uniform modulus, regular readback, real seal, transport, provenance, and local naming rows;
\item $P$ is the $\Pkg$ provenance over $\RegularSequenceRegularizerUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RegularCauchyUniformModulusUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\RegularSequenceRegularizerUp}$ row.
\end{enumerate}
The carrier has no coordinate for a chosen limit, quotient stream equality, host real equality, countable-choice schedule, hidden convergence oracle, or ambient completeness principle.
\end{definition}

\begin{theorem}[Regular sequence regularizer NameCert obligations]
\label{thm:regular-sequence-regularizer-namecert-obligations}
For every accepted $\RegularSequenceRegularizerUp$ carrier $G=(S,D,K,U,Q,R,H,C,P,N)$, the local $\NameCert_{\RegularSequenceRegularizerUp}$ surface consists exactly of carrier admission for $G$, stream-window exposure through $S$, dyadic tolerance exposure through $D$, Cauchy evidence exposure through $K$, uniform-modulus exposure through $U$, regular rational readback through $Q$, terminal real-seal handoff through $R$, componentwise $\hsame$ stability through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:regular-sequence-regularizer-carrier}. The only observation data are the finite stream windows in $S$, and the only tolerance arithmetic available to the regularizer is the dyadic ledger $D$.

The row $K$ records that the inspected windows satisfy the Cauchy request, while $U$ supplies the displayed uniform schedule that converts the request into a regular route. Thus the regular rational readback $Q$ is downstream of $S,D,K,U$.

The seal $R$ is reached only after the regular readback is visible. The structural rows $H,C,P,N$ transport, replay, source, and name that same finite route, so the NameCert surface cannot add a selected limit, quotient stream equality, host real equality, countable-choice schedule, hidden convergence oracle, or ambient completeness coordinate.
\end{proof}

\begin{theorem}[Regularizer RegSeqRat readback]
\label{thm:regular-sequence-regularizer-regseqrat-readback}
Every $\RegSeqRatUp$ readback exported by an accepted $\RegularSequenceRegularizerUp$ packet factors through the displayed route
$$
S,D,K,U \longmapsto Q .
$$
Consequently a Real-facing or Completion-facing consumer cannot read the regular sequence as a free stream, a selected limit, or a quotient representative outside the StreamName, DyadicRatCore, Cauchy evidence, and regular uniform-modulus rows.
\end{theorem}
\begin{proof}
Begin with $G=(S,D,K,U,Q,R,H,C,P,N)$. The carrier definition presents $Q$ as the regular rational row produced from the finite-window row $S$, dyadic tolerance row $D$, Cauchy evidence row $K$, and uniform-modulus row $U$.

Use \autoref{thm:regular-sequence-regularizer-namecert-obligations}. Its readback clause exposes $Q$ only after the four source rows have been displayed, and its transport and replay clauses preserve that dependency under $\hsame$ and $\Cont$.

Therefore any consumer of $Q$ receives a regular readback route rather than an independent stream or chosen representative. The packet carries no coordinate that could supply a selected limit, quotient stream equality, host real equality, countable-choice schedule, or hidden convergence oracle.
\end{proof}

\begin{theorem}[Regularizer terminal seal nonescape]
\label{thm:regular-sequence-regularizer-terminal-seal-nonescape}
Every terminal $\RealUp$ seal exported by an accepted $\RegularSequenceRegularizerUp$ packet is downstream of the rows
$$
S,D,K,U,Q,H,C,P,N .
$$
It cannot be consumed as ambient real completeness, host real equality, quotient stream equality, a selected real limit, or a completion seal independent of the displayed regularization route.
\end{theorem}
\begin{proof}
Project the accepted packet. The carrier row $R$ is the only terminal $\RealUp$ seal coordinate, and it appears after the stream window, dyadic tolerance, Cauchy evidence, uniform modulus, and regular readback rows.

Apply \autoref{thm:regular-sequence-regularizer-namecert-obligations}. Its terminal handoff clause binds $R$ to $Q$, while the preceding clauses bind $Q$ to $S,D,K,U$.

The structural rows $H,C,P,N$ keep the same route visible under transport, replay, provenance, and naming. Since no ambient-completeness or quotient coordinate is stored in the carrier, the terminal seal cannot escape the displayed regularization route.
\end{proof}

\falsifiablePrediction{A consumer that claims a $\RegularSequenceRegularizerUp$ read while omitting the $\StreamNameUp$ finite window, $\DyadicRatCoreUp$ tolerance row, Cauchy evidence row, $\RegularCauchyUniformModulusUp$ schedule, $\RegSeqRatUp$ readback, $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, or local $\NameCert$ rows refutes the carrier of \autoref{def:regular-sequence-regularizer-carrier}.}

\independenceWitness{streamname, dyadicratcore, regseqrat, regularcauchyuniformmodulus, real: no listed sibling is bijective to $\RegularSequenceRegularizerUp$ because this packet alone binds finite stream windows, dyadic tolerance data, Cauchy evidence, a regular uniform modulus, regular rational readback, terminal Real seal, transport, replay, provenance, and local naming as one regularization interface.}

\closureat{RegularSequenceRegularizerUp}{seedStr}

\begin{closurestatus}{\RegularSequenceRegularizerUp}
  \constructivestory{A $\RegularSequenceRegularizerUp$ packet is generated from StreamName finite-window rows, DyadicRatCore tolerance rows, stream-window Cauchy evidence, a RegularCauchyUniformModulus schedule, RegSeqRat readback, a terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-sequence-regularizer-carrier}, \autoref{thm:regular-sequence-regularizer-namecert-obligations}, \autoref{thm:regular-sequence-regularizer-regseqrat-readback}, and \autoref{thm:regular-sequence-regularizer-terminal-seal-nonescape} seed the finite regularization route between stream-window Cauchy evidence and RegSeqRat and Real consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, quotient stream equality, selected real limits, host real equality, countable choice, or ambient Real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, stream-window exposure, dyadic tolerance exposure, Cauchy evidence exposure, uniform-modulus exposure, RegSeqRat readback, Real seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.RegularSequenceRegularizerUp.}
\end{closurestatus}
