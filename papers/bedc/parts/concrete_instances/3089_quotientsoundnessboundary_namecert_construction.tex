\chapter{A Concrete Naming Certificate for $\QuotientSoundnessBoundaryUp$}
\label{ch:concrete-instances-quotient-soundness-boundary-namecert}
\origin{human}

The $\QuotientSoundnessBoundaryUp$ interface names the finite boundary at which a quotient-style equality request is refused as an unwitnessed representative-selection step and replaced by classifier-respecting $\psame$ transport. It does not construct a quotient type, select a canonical representative, collapse equivalent source rows, or import $\mathsf{Quot.sound}$. It records the visible source relation rows, requested representative rows, transport replacement, refusal verdict, locality routes, provenance, and naming certificate that downstream quotient-like chapters may consume.

The refusal surface is the quotient-facing specialization of the negative-boundary discipline of \autoref{ch:concrete-instances-negative-name-boundary-namecert}: representative-selection requests are first treated as blocked boundary reads, and only then replaced by local $\psame$ transport.

\begin{definition}[QuotientSoundnessBoundary carrier]
\label{def:quotient-soundness-boundary-carrier}
A $\QuotientSoundnessBoundaryUp$ carrier is a finite $\BHist$ packet
$$
Q=(E,A,T,V,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the finite family of quotient-relation rows, recording the source carrier entries, their classifier relation, and the displayed equivalence-derivation data admitted by the packet;
\item $A$ is the requested representative row family, recording each attempted demand for a canonical representative, section, quotient equality token, or erased derivation tree;
\item $T$ is the $\psame$ transport replacement row, recording the classifier-respecting route by which an operation reads related source rows without selecting a representative;
\item $V$ is the refusal verdict row, recording that the requested representative-selection surface in $A$ is not admitted and that downstream use must pass through $T$;
\item $H$ records componentwise $\hsame$ transport for the relation rows, requested representative rows, transport replacement, refusal verdict, continuation routes, provenance, and local naming row;
\item $C$ records the visible $\Cont$ routes from the relation rows to the refusal verdict, from the relation rows to the $\psame$ transport replacement, and from the transport replacement to the downstream classifier-respecting consumer;
\item $P$ is the $\Pkg$ provenance over $\QuotientSoundnessBoundaryUp$, quotient-relation rows, requested representative rows, $\psame$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\QuotientSoundnessBoundaryUp}$ row.
\end{enumerate}
The carrier exports only a finite refusal-and-transport packet. It contains no coordinate for a quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, or $\mathsf{Quot.sound}$ dependency.
\end{definition}

\begin{theorem}[QuotientSoundnessBoundary NameCert obligations]
\label{thm:quotient-soundness-boundary-namecert-obligations}
For every accepted $\QuotientSoundnessBoundaryUp$ carrier
$$
Q=(E,A,T,V,H,C,P,N),
$$
the local $\NameCert_{\QuotientSoundnessBoundaryUp}$ surface consists exactly of carrier admission, relation-row admission, requested-representative refusal, classifier-respecting $\psame$ transport, componentwise $\hsame$ stability, displayed $\Cont$ locality, $\Pkg$ provenance, and non-admission of quotient carriers, representative selectors, quotient equality eliminators, erased derivation recovery maps, and $\mathsf{Quot.sound}$ dependencies.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:quotient-soundness-boundary-carrier}. The tuple exposes the relation rows, requested representative rows, transport replacement, refusal verdict, transport rows, continuation routes, provenance, and local naming row.

The requested representative rows are read through $A$ and judged by $V$. The verdict attaches the refusal to the displayed demands rather than to an anonymous global prohibition, so every blocked quotient-soundness request remains audit-visible.

The replacement obligation is supplied by $T$. It routes classifier-respecting consumers through $\psame$ transport on the source rows named in $E$, while $H$ and $C$ keep the transported read componentwise stable and local to the displayed packet.

Since no coordinate of $Q$ is a quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, or $\mathsf{Quot.sound}$ dependency, the local certificate is exhausted by the listed finite obligations.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary transport replacement]
\label{thm:quotient-soundness-boundary-transport-replacement}
Every downstream operation accepted by a $\QuotientSoundnessBoundaryUp$ packet that would otherwise request a quotient representative must instead factor through the source relation rows $E$, the classifier-respecting $\psame$ transport row $T$, the componentwise transport row $H$, the continuation routes $C$, and the local naming row $N$. The result is an operation on named source rows, not an operation on a selected representative of a quotient class.
\end{theorem}
\begin{proof}
Begin with a consumer request recorded in $A$. By the refusal verdict $V$, the request is not admitted as a representative selector, quotient carrier, quotient equality eliminator, or derivation eraser.

The only positive route left by the carrier is the transport row $T$. It reads the relevant relation rows in $E$ and supplies the classifier-respecting $\psame$ transport needed by the downstream operation.

The rows $H$ and $C$ then make the replacement stable and local: $H$ moves the relation, refusal, and transport data componentwise, while $C$ displays the route from relation evidence to transport and then to the consumer. Thus the operation proceeds on named source rows and never obtains a hidden quotient representative.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary negative refusal dependency]
\label{thm:quotient-soundness-boundary-negative-refusal-dependency}
Every quotient-style representative request accepted by a $\QuotientSoundnessBoundaryUp$ packet first lands in the negative-boundary refusal discipline of \autoref{ch:concrete-instances-negative-name-boundary-namecert}. This chapter specializes that discipline to the visible relation rows $E$, requested representative rows $A$, classifier-respecting $\psame$ transport row $T$, refusal verdict $V$, componentwise transport $H$, continuation routes $C$, provenance $P$, and local naming row $N$, replacing any appeal to $\mathsf{Quot.sound}$ by displayed transport through $T$.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:quotient-soundness-boundary-carrier}. The requested representative rows $A$ and refusal verdict $V$ record the same kind of blocked boundary read handled by the negative-name boundary.

Apply the transport replacement theorem, \autoref{thm:quotient-soundness-boundary-transport-replacement}. It routes the surviving operation through $E,T,H,C,N$ rather than through a quotient representative or quotient equality eliminator.

Use the sibling refusal surface \autoref{ch:concrete-instances-negative-name-boundary-namecert} to read the representative request as a negative boundary request before the transport replacement is consumed. The local packet then records the specialized quotient-facing rows $E,A,T,V,H,C,P,N$.

Finally cite non-escape, \autoref{thm:quotient-soundness-boundary-non-escape}. Since the excluded quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, and $\mathsf{Quot.sound}$ dependency are absent from the packet, the only exported replacement is the displayed $\psame$ transport route.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary non-escape]
\label{thm:quotient-soundness-boundary-non-escape}
An accepted $\QuotientSoundnessBoundaryUp$ carrier cannot export a quotient carrier, canonical representative, quotient equality eliminator, erased equivalence-derivation recovery map, unrestricted quotient-recursion principle, host-level class selector, or $\mathsf{Quot.sound}$ dependency. Any consumer request for one of these objects leaves the finite packet
$$
E,A,T,V,H,C,P,N
$$
and must be supplied by an explicit extra certificate outside this boundary.
\end{theorem}
\begin{proof}
Inspect the carrier inventory. The rows $E,A,T,V,H,C,P,N$ name relation evidence, representative requests, transport replacement, refusal, stability, locality, provenance, and the local certificate.

None of these rows has the type or role of a quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, unrestricted quotient-recursion principle, host-level class selector, or $\mathsf{Quot.sound}$ dependency. The rows $A$ and $V$ record such demands as refused requests, and $T$ records only the classifier-respecting replacement.

Therefore a downstream consumer cannot project any excluded object from the packet. It may either use the displayed $\psame$ transport route or supply a separate certificate whose boundary is audited independently.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary refusal locality]
\label{thm:quotient-soundness-boundary-refusal-locality}
For every accepted $\QuotientSoundnessBoundaryUp$ carrier
$$
Q=(E,A,T,V,H,C,P,N),
$$
every representative-selection demand is localized to the requested-representative rows $A$ and the refusal verdict rows $V$ before any transport replacement is consumed. No downstream consumer may treat $T$, $H$, $C$, $P$, or $N$ as evidence that a representative selector, quotient equality eliminator, erased derivation recovery map, or $\mathsf{Quot.sound}$ dependency has been admitted.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:quotient-soundness-boundary-carrier}. The only rows that record a demand for a canonical representative, quotient equality token, section, or erased derivation tree are the rows $A$.

The verdict rows $V$ attach the refusal to those displayed demands. Thus the blocked request is not a global prohibition detached from the packet; it is a local row pair $A,V$ that can be audited before the replacement route is used.

Apply \autoref{thm:quotient-soundness-boundary-transport-replacement}. The positive route after refusal is the classifier-respecting $\psame$ transport through $T$, stabilized and routed by $H$ and $C$ and named by $P,N$. Since \autoref{thm:quotient-soundness-boundary-non-escape} excludes the quotient-side objects, the replacement rows cannot be reread as hidden representative selection.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary psame transport exhaustion]
\label{thm:quotient-soundness-boundary-psame-transport-exhaustion}
Every surviving quotient-style operation accepted by a $\QuotientSoundnessBoundaryUp$ packet is exhausted by the displayed route through the source relation rows $E$, the classifier-respecting $\psame$ transport row $T$, the componentwise stability row $H$, the continuation row $C$, the package provenance row $P$, and the local naming row $N$. After the refusal pair $A,V$ has been read, no second route remains through a quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, unrestricted quotient-recursion principle, host-level class selector, or $\mathsf{Quot.sound}$ dependency.
\end{theorem}
\begin{proof}
Begin with a surviving operation. By \autoref{thm:quotient-soundness-boundary-refusal-locality}, its representative-selection demand has already been localized to $A,V$ and refused before any positive transport is consumed.

The carrier then exposes only the source relation rows $E$ and the replacement row $T$ as the operation-facing data. The row $T$ is explicitly a classifier-respecting $\psame$ route on named source rows, not an operation on quotient classes.

The rows $H,C,P,N$ provide stability, locality, provenance, and naming for that same route. Apply \autoref{thm:quotient-soundness-boundary-non-escape} to remove the excluded quotient objects from the exported surface. Therefore the displayed $E,T,H,C,P,N$ route exhausts the surviving operation.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary downstream audit package]
\label{thm:quotient-soundness-boundary-downstream-audit-package}
The refusal locality theorem, the $\psame$ transport exhaustion theorem, and the non-escape theorem assemble a downstream audit package for quotient-like BEDC chapters. A downstream chapter may consume the refusal verdict $A,V$ and the transport route $E,T,H,C,P,N$ as an auditable refusal-and-transport surface, but it receives no quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, unrestricted quotient-recursion principle, host-level class selector, or $\mathsf{Quot.sound}$ dependency from $\QuotientSoundnessBoundaryUp$.
\end{theorem}
\begin{proof}
Use \autoref{thm:quotient-soundness-boundary-refusal-locality} to bind the negative part of the package. It says every representative-selection demand is visible as $A$ and refused by $V$ before replacement.

Use \autoref{thm:quotient-soundness-boundary-psame-transport-exhaustion} for the positive part. It identifies the only surviving operation route as $E,T,H,C,P,N$, with $\psame$ transport carried by $T$ and stabilized through $H,C$.

Finally apply \autoref{thm:quotient-soundness-boundary-non-escape}. The excluded quotient primitives cannot be projected from the packet, so downstream quotient-like chapters receive an audit surface consisting of refusal plus displayed transport, not a quotient kernel extension.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary fixed classifier]
\label{thm:quotient-soundness-boundary-fixed-classifier}
For an accepted $\QuotientSoundnessBoundaryUp$ carrier $Q=(E,A,T,V,H,C,P,N)$, every accepted comparison is exhausted by the relation rows $E$, requested representative rows $A$, replacement row $T$, refusal verdict $V$, and the displayed transport rows $H,C$. No comparison may consult a quotient carrier, canonical representative, quotient equality eliminator, erased derivation recovery map, or $\mathsf{Quot.sound}$ dependency.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:quotient-soundness-boundary-carrier}. The only comparison-facing coordinates are the relation evidence $E$, the recorded representative demands $A$, the replacement row $T$, the verdict $V$, and the locality rows $H,C$.

Apply the transport replacement theorem, \autoref{thm:quotient-soundness-boundary-transport-replacement}. It consumes $E$ and $T$ as the positive classifier-respecting route, while $A$ and $V$ keep representative selection in the refusal ledger.

The non-escape theorem, \autoref{thm:quotient-soundness-boundary-non-escape}, excludes every quotient eliminator and $\mathsf{Quot.sound}$ route from the packet. Hence the classifier is fixed by $E,A,T,V,H,C$ and cannot be widened by an ambient quotient principle.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary kernel non-escape]
\label{thm:quotient-soundness-boundary-kernel-nonescape}
Every kernel-facing consumer of an accepted $\QuotientSoundnessBoundaryUp$ packet remains inside the rows
$$
E,A,T,V,H,C,P,N.
$$
The packet cannot import $\mathsf{Quot.sound}$, a host quotient eliminator, a hidden representative selector, or erased relation-derivation recovery.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:quotient-soundness-boundary-namecert-obligations}. They list carrier admission, relation admission, refused representative requests, classifier-respecting transport, componentwise stability, locality, provenance, and local naming as the full public surface.

Use the fixed classifier theorem above to identify the comparison route. The kernel-facing read is not an arbitrary quotient read; it is the finite path through $E,A,T,V,H,C$ and then through package provenance and the local certificate.

Finally apply \autoref{thm:quotient-soundness-boundary-negative-refusal-dependency} and \autoref{thm:quotient-soundness-boundary-non-escape}. The refusal dependency explains why the blocked request is represented negatively, and non-escape prevents any $\mathsf{Quot.sound}$ or host quotient eliminator from entering the kernel route.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary root request triad]
\label{thm:quotient-soundness-boundary-root-request-triad}
Every root quotient-style request accepted by a $\QuotientSoundnessBoundaryUp$ packet is exhausted, before any positive transport is consumed, by the relation rows $E$, the requested-representative rows $A$, and the refusal verdict rows $V$. The rows $E$ name the visible relation evidence, $A$ records the demanded representative-selection surface, and $V$ refuses that demand as a kernel-facing object.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:quotient-soundness-boundary-carrier}. Its request-facing coordinates include the relation rows $E$, the requested representative rows $A$, and the refusal verdict $V$.

Use the NameCert obligations of \autoref{thm:quotient-soundness-boundary-namecert-obligations}. They identify requested-representative refusal as a named certificate obligation, so the representative demand is read from $A$ and judged by $V$ rather than by an ambient quotient principle.

Finally apply kernel non-escape, \autoref{thm:quotient-soundness-boundary-kernel-nonescape}. Since the kernel route cannot import $\mathsf{Quot.sound}$, a host quotient eliminator, a hidden representative selector, or erased derivation recovery, the root request surface is exactly the finite triad $E,A,V$ before the displayed transport row is used.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary root relation row exposure]
\label{thm:quotient-soundness-boundary-root-relation-row-exposure}
Every root quotient-style read accepted by a $\QuotientSoundnessBoundaryUp$ packet $Q=(E,A,T,V,H,C,P,N)$ must expose the relation rows $E$ before it exposes any representative-demand row in $A$. Thus the packet records visible source relation evidence before a downstream consumer can ask for a canonical representative, section, quotient equality token, or erased derivation tree.
\end{theorem}
\begin{proof}
Project the carrier. The row family $E$ records the source relation evidence, while $A$ records the demanded representative-selection surface.

Use the fixed classifier theorem. It keeps every accepted comparison inside $E,A,T,V,H,C$ and prevents an ambient quotient principle from widening the comparison before relation evidence is visible.

Finally apply the root request triad and kernel non-escape. The triad exhausts the root request by $E,A,V$, and non-escape excludes hidden representative selectors, erased derivation recovery, host quotient eliminators, and $\mathsf{Quot.sound}$.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary root request accountability]
\label{thm:quotient-soundness-boundary-root-request-accountability}
For every accepted $\QuotientSoundnessBoundaryUp$ packet $Q=(E,A,T,V,H,C,P,N)$, relation rows $E$, representative-request rows $A$, and refusal verdict rows $V$ are accountable before any replacement by the transport row $T$. A downstream quotient-style consumer therefore sees the requested relation evidence, the demanded representative surface, and the refusal verdict before it can use $T,H,C,P,N$ as the surviving audit route.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:quotient-soundness-boundary-root-relation-row-exposure}. It places the visible relation rows $E$ before any representative-demand rows in $A$.

Use the root request triad, \autoref{thm:quotient-soundness-boundary-root-request-triad}. The triad binds the representative demand to $A$ and its refusal to $V$, so the negative verdict is part of the root request surface.

After those rows are visible, the transport replacement theorem supplies $T$ only as the classifier-respecting surviving route. The stability, locality, provenance, and naming rows $H,C,P,N$ audit that route and do not erase $E,A,V$.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary transport gate]
\label{thm:quotient-soundness-boundary-transport-gate}
For every accepted $\QuotientSoundnessBoundaryUp$ packet $Q=(E,A,T,V,H,C,P,N)$, any quotient-like operation that survives the refused representative request must pass through $T,H,C,P,N$. The operation may still read the relation rows $E$, requested-representative rows $A$, and refusal verdict rows $V$ as its accountable source surface, but its positive movement is gated by the displayed transport, stability, locality, provenance, and naming rows.
\end{theorem}
\begin{proof}
First read the accountability surface from \autoref{thm:quotient-soundness-boundary-root-request-accountability}. It keeps $E,A,V$ visible before any surviving positive route is consumed.

Apply the transport replacement theorem. The only admitted positive motion after refusal is the $\psame$ transport row $T$ on named source rows.

Finally use the NameCert obligations and non-escape theorem. They force every use of $T$ to remain packaged with $H,C,P,N$ and exclude any hidden positive path outside the displayed packet.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary downstream nonimport]
\label{thm:quotient-soundness-boundary-downstream-nonimport}
Every downstream quotient-like consumer of an accepted $\QuotientSoundnessBoundaryUp$ packet $Q=(E,A,T,V,H,C,P,N)$ receives only the audit surface $E,A,T,V,H,C,P,N$. It receives no $\mathsf{Quot.sound}$ dependency, quotient carrier, representative selector, erased derivation recovery map, or host quotient eliminator.
\end{theorem}
\begin{proof}
Use root request accountability to expose $E,A,V$ as the complete negative request surface. These rows record the visible relation evidence, the representative demand, and the refusal verdict before any surviving operation is available.

Next apply the transport gate. It confines every surviving positive operation to $T,H,C,P,N$, with $T$ carrying classifier-respecting $\psame$ transport and the remaining rows carrying stability, locality, provenance, and local naming.

Close with kernel non-escape. Since the carrier has no coordinate for $\mathsf{Quot.sound}$, quotient carriers, representative selectors, erased derivation recovery, or host quotient eliminators, the downstream consumer imports only the displayed audit surface.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary root transport verdict order]
\label{thm:quotient-soundness-boundary-root-transport-verdict-order}
For every accepted $\QuotientSoundnessBoundaryUp$ packet $Q=(E,A,T,V,H,C,P,N)$, the refusal verdict row $V$ must be read before any positive $\psame$ transport row $T$ is used by a quotient-like consumer. The transport row is therefore a replacement after refusal, not evidence that a representative selector or quotient equality eliminator has been admitted.
\end{theorem}
\begin{proof}
Begin with refusal locality. It localizes every representative-selection demand to $A$ and the refusal verdict $V$ before the transport replacement is consumed.

Apply the transport replacement theorem. The row $T$ is the classifier-respecting $\psame$ route on named source rows after the request has been refused, with $H,C,P,N$ recording stability, locality, provenance, and naming.

Use the $\psame$ transport exhaustion theorem. After $A,V$ are read, the surviving operation is exhausted by $E,T,H,C,P,N$ and cannot recover a quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, or $\mathsf{Quot.sound}$ dependency.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary root downstream unblock surface]
\label{thm:quotient-soundness-boundary-root-downstream-unblock-surface}
The root relation row exposure, the root transport verdict order, the transport replacement theorem, non-escape, and the kernel refusal ledger assemble the downstream unblock surface for quotient-like chapters. The exported surface is exactly $E,A,V,T,H,C,P,N$: relation rows, representative-demand rows, refusal verdict, classifier-respecting $\psame$ transport, componentwise $\hsame$ stability, displayed $\Cont$ locality, $\Pkg$ provenance, and local $\NameCert$ naming. It explicitly excludes $\mathsf{Quot.sound}$ and host quotient eliminators.
\end{theorem}
\begin{proof}
Use \autoref{thm:quotient-soundness-boundary-root-relation-row-exposure}. It forces every root quotient-style read to expose $E$ before any representative-demand row in $A$ can be consumed.

Use \autoref{thm:quotient-soundness-boundary-root-transport-verdict-order}. It places the refusal verdict $V$ before the positive transport row $T$, so downstream use receives replacement transport only after the refused request is visible.

Finally combine \autoref{thm:quotient-soundness-boundary-transport-replacement}, \autoref{thm:quotient-soundness-boundary-non-escape}, and \autoref{thm:quotient-soundness-boundary-kernel-refusal-ledger}. These rows assemble $E,A,V,T,H,C,P,N$ as the finite consumer surface and rule out $\mathsf{Quot.sound}$, host quotient eliminators, hidden representative selectors, and erased derivation recovery.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary psame replacement classifier]
\label{thm:quotient-soundness-boundary-psame-replacement-classifier}
After the refusal pair $A,V$ has been read, the only accepted classifier-respecting route for a quotient-like consumer is the displayed path through $E,T,H,C,P,N$. The row $T$ is the $\psame$ transport replacement on named source rows, and the packet supplies no quotient carrier, selected representative, quotient equality eliminator, erased relation-derivation recovery, or $\mathsf{Quot.sound}$ dependency.
\end{theorem}
\begin{proof}
Begin with the refusal pair supplied by \autoref{thm:quotient-soundness-boundary-root-request-triad}. The pair $A,V$ has already recorded and refused the representative-selection demand before the positive consumer route is read.

Apply transport replacement, \autoref{thm:quotient-soundness-boundary-transport-replacement}, together with the fixed classifier theorem, \autoref{thm:quotient-soundness-boundary-fixed-classifier}. The consumer route is forced through the relation rows $E$, the $\psame$ replacement row $T$, and the stability and locality rows $H,C$.

Use $\psame$ transport exhaustion, \autoref{thm:quotient-soundness-boundary-psame-transport-exhaustion}. It adds the provenance and naming rows $P,N$ to the same displayed route and excludes a second path through quotient carriers or selected representatives.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary root transport package totality]
\label{thm:quotient-soundness-boundary-root-transport-package-totality}
After the root request triad has exposed $E,A,V$ and the verdict-before-transport order has placed $V$ before any positive transport use, every surviving quotient-like consumer route is totally packaged by $E,T,H,C,P,N$. The package reads the displayed relation rows, the classifier-respecting $\psame$ transport row, componentwise $\hsame$ stability, displayed $\Cont$ locality, $\Pkg$ provenance, and local $\NameCert$ naming, and it imports no quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, or $\mathsf{Quot.sound}$ dependency.
\end{theorem}
\begin{proof}
First expose the root request surface. By \autoref{thm:quotient-soundness-boundary-root-request-triad} and \autoref{thm:quotient-soundness-boundary-root-relation-row-exposure}, every root quotient-style request reads the relation rows $E$ together with the representative-demand rows $A$ and refusal verdict $V$.

Next use \autoref{thm:quotient-soundness-boundary-root-transport-verdict-order}. It forces the refusal verdict to be visible before the positive transport row $T$ is consumed.

Once the verdict is fixed, apply \autoref{thm:quotient-soundness-boundary-psame-replacement-classifier}. The surviving classifier-respecting route is exactly $E,T,H,C,P,N$.

Finally combine \autoref{thm:quotient-soundness-boundary-non-escape} with \autoref{thm:quotient-soundness-boundary-psame-transport-exhaustion}. These results rule out the quotient-side primitives and package the whole surviving route in the displayed transport rows.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary transport-refusal normal form]
\label{thm:quotient-soundness-boundary-transport-refusal-normal-form}
After the refusal pair $A,V$ has been read in an accepted
$\QuotientSoundnessBoundaryUp$ packet, every surviving quotient-like operation
is normalised to the displayed $E,T,H,C,P,N$ route: relation rows $E$,
classifier-respecting $\psame$ transport $T$, componentwise $\hsame$ stability
$H$, displayed $\Cont$ locality $C$, $\Pkg$ provenance $P$, and local
$\NameCert$ naming $N$. No surviving read exports a quotient carrier,
representative selector, host quotient eliminator, erased relation-derivation
recovery map, or $\mathsf{Quot.sound}$ dependency.
\end{theorem}
\begin{proof}
Start with the refused request. The root request triad
\autoref{thm:quotient-soundness-boundary-root-request-triad} exposes the
relation rows $E$, the representative-style demand $A$, and the refusal
verdict $V$.

Use \autoref{thm:quotient-soundness-boundary-root-transport-verdict-order} to
place $V$ before any positive transport read. Thus a later transport row cannot
erase or reinterpret the refusal pair.

Now apply \autoref{thm:quotient-soundness-boundary-psame-replacement-classifier}
and \autoref{thm:quotient-soundness-boundary-root-transport-package-totality}.
They identify every surviving classifier-respecting route with the displayed
$E,T,H,C,P,N$ transport package.

Finally use \autoref{thm:quotient-soundness-boundary-kernel-nonescape} and the
public bridge non-escape surface of
\autoref{thm:quotient-soundness-boundary-public-bridge-nonescape}. The
excluded quotient primitives have no exported coordinate, so the displayed
transport route is the normal form.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary consumer ledger coverage]
\label{thm:quotient-soundness-boundary-consumer-ledger-coverage}
Every downstream consumer read of an accepted
$\QuotientSoundnessBoundaryUp$ packet is covered by the finite ledger rows
$E,A,V,T,H,C,P,N$. The rows $A,V$ record the refused representative-style
request, while $E,T,H,C,P,N$ record the surviving classifier-respecting
$\psame$ transport route. The ledger exports no $\mathsf{Quot.sound}$ row and
no host quotient eliminator.
\end{theorem}
\begin{proof}
Read the negative coverage first. By
\autoref{thm:quotient-soundness-boundary-root-request-triad} and
\autoref{thm:quotient-soundness-boundary-root-relation-row-exposure}, each
root quotient-style request is visible through $E,A,V$.

Read the positive coverage next. The psame replacement classifier and root
transport package totality,
\autoref{thm:quotient-soundness-boundary-psame-replacement-classifier} and
\autoref{thm:quotient-soundness-boundary-root-transport-package-totality},
cover every surviving transport consumer by $E,T,H,C,P,N$.

It remains to exclude hidden exits. Apply
\autoref{thm:quotient-soundness-boundary-transport-refusal-normal-form},
\autoref{thm:quotient-soundness-boundary-kernel-nonescape}, and
\autoref{thm:quotient-soundness-boundary-public-bridge-nonescape}. The
combined ledger therefore covers downstream consumers without exporting
$\mathsf{Quot.sound}$ or a host quotient eliminator.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary downstream handoff readiness]
\label{thm:quotient-soundness-boundary-downstream-handoff-readiness}
The transport-refusal normal form and consumer ledger coverage assemble the
exact package that downstream quotient-like chapters may cite. That package is
the finite surface $E,A,V,T,H,C,P,N$ with refusal before transport,
classifier-respecting $\psame$ replacement, componentwise $\hsame$ stability,
displayed $\Cont$ locality, $\Pkg$ provenance, and local $\NameCert$ naming;
it contains no quotient primitive, no host quotient eliminator, no
representative-selection export, and no $\mathsf{Quot.sound}$ dependency.
\end{theorem}
\begin{proof}
First take the ordered refusal surface from
\autoref{thm:quotient-soundness-boundary-root-request-triad} and
\autoref{thm:quotient-soundness-boundary-root-transport-verdict-order}. These
rows make $A,V$ visible before any surviving transport handoff.

Next take the normal transport package from
\autoref{thm:quotient-soundness-boundary-transport-refusal-normal-form}. It
identifies the only surviving quotient-like operation as the displayed
$E,T,H,C,P,N$ route.

Now add ledger coverage by
\autoref{thm:quotient-soundness-boundary-consumer-ledger-coverage}. Every
downstream consumer read is therefore inside $E,A,V,T,H,C,P,N$.

Finally combine kernel non-escape with public bridge non-escape. The handoff
surface is ready for downstream citation precisely because no quotient
primitive, host quotient eliminator, representative-selection export, or
$\mathsf{Quot.sound}$ dependency crosses the package boundary.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary refusal verdict monotonicity]
\label{thm:quotient-soundness-boundary-refusal-verdict-monotonicity}
In an accepted $\QuotientSoundnessBoundaryUp$ packet, adding later transport rows cannot turn a refused representative-selection row $A,V$ into an admitted quotient primitive. The rows $E,T,H,C,P,N$ may supply the classifier-respecting $\psame$ replacement route after refusal, but they cannot reread $A,V$ as a representative selector, quotient equality eliminator, host quotient eliminator, unrestricted quotient recursion principle, or $\mathsf{Quot.sound}$ dependency.
\end{theorem}
\begin{proof}
Start with the negative row. The root request triad, \autoref{thm:quotient-soundness-boundary-root-request-triad}, records representative-selection demand in $A$ and refusal in $V$ before transport is available.

The order statement \autoref{thm:quotient-soundness-boundary-root-transport-verdict-order} separates verdict from transport. Thus later use of $T$ is downstream of the refused request rather than a replacement for the refusal verdict.

Use \autoref{thm:quotient-soundness-boundary-psame-replacement-classifier} for the positive route. It gives only classifier-respecting $\psame$ transport through $E,T,H,C,P,N$.

Finally apply \autoref{thm:quotient-soundness-boundary-non-escape} and \autoref{thm:quotient-soundness-boundary-kernel-refusal-ledger}. The packet has no coordinate by which later transport rows can promote a refused representative request into an admitted quotient primitive.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary downstream factorization exactness]
\label{thm:quotient-soundness-boundary-downstream-factorization-exactness}
Every downstream quotient-like consumer of $\QuotientSoundnessBoundaryUp$ factors through exactly one of the two displayed surfaces: either the refused request pair $A,V$ or the $\psame$ transport package $E,T,H,C,P,N$. There is no third route through a quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, host-level class selector, unrestricted quotient recursion principle, or $\mathsf{Quot.sound}$ dependency.
\end{theorem}
\begin{proof}
Read the negative surface first. By \autoref{thm:quotient-soundness-boundary-root-request-triad}, \autoref{thm:quotient-soundness-boundary-root-relation-row-exposure}, and \autoref{thm:quotient-soundness-boundary-root-transport-verdict-order}, a representative-style request must expose $E$, then read $A,V$, before any surviving transport can be used.

Read the positive surface next. By \autoref{thm:quotient-soundness-boundary-psame-replacement-classifier} and \autoref{thm:quotient-soundness-boundary-root-transport-package-totality}, every surviving classifier-respecting consumer route is packaged by $E,T,H,C,P,N$.

The classifier boundary is exact because \autoref{thm:quotient-soundness-boundary-non-escape} removes the quotient-side primitives, while \autoref{thm:quotient-soundness-boundary-kernel-refusal-ledger} records the exported surface as refusal plus displayed transport.

Finally apply \autoref{thm:quotient-soundness-boundary-psame-replacement-classifier} again to identify the positive side as the named $\psame$ replacement rather than a hidden quotient route. The downstream consumer therefore has only the refused pair or the displayed transport package.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary kernel refusal ledger]
\label{thm:quotient-soundness-boundary-kernel-refusal-ledger}
The root request triad, the $\psame$ replacement classifier, and kernel non-escape assemble the kernel-facing refusal ledger consumed by downstream quotient-like chapters. The exported surface consists of the refused request rows $E,A,V$ together with the classifier-respecting transport rows $E,T,H,C,P,N$, and no $\mathsf{Quot.sound}$ or host quotient eliminator enters that surface.
\end{theorem}
\begin{proof}
Use \autoref{thm:quotient-soundness-boundary-root-request-triad} for the negative part of the ledger. It binds each root quotient-style request to the visible relation evidence $E$, the requested representative rows $A$, and the refusal verdict $V$.

Use \autoref{thm:quotient-soundness-boundary-psame-replacement-classifier} for the positive replacement route. It identifies the accepted classifier-respecting consumer path as $E,T,H,C,P,N$, with $T$ carrying the displayed $\psame$ transport.

Finally apply \autoref{thm:quotient-soundness-boundary-kernel-nonescape}. The kernel-facing packet cannot project $\mathsf{Quot.sound}$, a host quotient eliminator, a hidden representative selector, or erased relation-derivation recovery, so downstream quotient-like chapters receive refusal plus displayed transport rather than a hidden quotient principle.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary NameCert ledger]
\label{thm:quotient-soundness-boundary-namecert-ledger}
The carrier of \autoref{def:quotient-soundness-boundary-carrier}, the fixed classifier of \autoref{thm:quotient-soundness-boundary-fixed-classifier}, the transport replacement of \autoref{thm:quotient-soundness-boundary-transport-replacement}, the negative-refusal dependency of \autoref{thm:quotient-soundness-boundary-negative-refusal-dependency}, and the kernel non-escape theorem of \autoref{thm:quotient-soundness-boundary-kernel-nonescape} assemble the local $\NameCert_{\QuotientSoundnessBoundaryUp}$ ledger.
\end{theorem}
\begin{proof}
The carrier supplies the finite inventory $E,A,T,V,H,C,P,N$. The fixed classifier theorem binds every accepted comparison to the displayed relation, request, replacement, verdict, and transport rows.

The transport replacement theorem supplies the positive operation available to downstream consumers. It routes classifier-respecting reads through named source rows rather than through selected quotient representatives.

The negative-refusal dependency and kernel non-escape theorems complete the ledger. They record the blocked quotient-soundness demand as a negative boundary request and keep every exported consumer inside $E,A,T,V,H,C,P,N$, so the local naming certificate contains exactly the quotient-boundary obligations listed in \autoref{thm:quotient-soundness-boundary-namecert-obligations}.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary public bridge]
\label{thm:quotient-soundness-boundary-public-bridge}
Every downstream quotient-like consumer of an accepted $\QuotientSoundnessBoundaryUp$ packet must read the public boundary through the finite rows
$$
E,A,T,V,H,C,P,N.
$$
The fixed classifier, transport replacement, negative-refusal dependency, kernel non-escape theorem, and NameCert ledger make this the whole public bridge: it admits relation rows, requested-representative refusal, classifier-respecting $\psame$ transport, componentwise stability, continuation, provenance, and local naming, but it excludes quotient carriers, representative selectors, quotient equality eliminators, erased derivation recovery, and $\mathsf{Quot.sound}$.
\end{theorem}
\begin{proof}
Project the carrier and apply the fixed classifier theorem. Every accepted comparison is already read through $E,A,T,V,H,C$, so no downstream consumer can widen the comparison by consulting a quotient carrier or representative selector.

Use transport replacement for the positive route. It routes quotient-like use through the source relation rows $E$, the classifier-respecting transport row $T$, and the structural rows $H,C,P,N$.

Use the negative-refusal dependency for blocked representative requests. The rows $A,V$ record those requests as refused boundary reads before any surviving transport is consumed.

Finally apply kernel non-escape and the NameCert ledger. Kernel non-escape removes quotient equality eliminators, erased derivation recovery, and $\mathsf{Quot.sound}$ from the exported packet, while the ledger assembles the remaining rows as the finite public boundary.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary bridge schema]
\label{thm:quotient-soundness-boundary-bridge-schema}
Every accepted $\QuotientSoundnessBoundaryUp$ carrier presents a bridge from the finite refusal-and-transport packet to the public quotient-refusal schema. The bridge reads the relation rows $E$, requested representative rows $A$, classifier-respecting $\psame$ transport row $T$, refusal verdict $V$, componentwise $\hsame$ transport row $H$, continuation rows $C$, $\Pkg$ provenance row $P$, and local $\NameCert$ row $N$, and it imports no $\mathsf{Quot.sound}$ dependency.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:quotient-soundness-boundary-carrier}. Its public quotient-facing surface is already the finite tuple $E,A,T,V,H,C,P,N$.

Use the NameCert ledger, \autoref{thm:quotient-soundness-boundary-namecert-ledger}, to assemble the refusal row $A,V$ and the transport route $E,T,H,C,P,N$ as one public packet. The fixed classifier theorem keeps comparison reads inside the same rows.

Finally apply non-escape, \autoref{thm:quotient-soundness-boundary-non-escape}. Since the excluded quotient primitives and $\mathsf{Quot.sound}$ have no coordinate in the carrier, the bridge is exactly the displayed refusal-and-transport schema.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary psame bridge handoff]
\label{thm:quotient-soundness-boundary-psame-bridge-handoff}
After the public refusal row $A,V$ has been read, the only bridge handoff available to a quotient-like consumer is the classifier-respecting $\psame$ route $E,T,H,C,P,N$. The handoff supplies named-source transport and does not supply a representative selector, quotient equality eliminator, erased derivation recovery map, or $\mathsf{Quot.sound}$ dependency.
\end{theorem}
\begin{proof}
Start with the refusal-locality theorem, \autoref{thm:quotient-soundness-boundary-refusal-locality}. It places every representative-selection demand in $A,V$ before the positive transport route is consumed.

Then use $\psame$ transport exhaustion, \autoref{thm:quotient-soundness-boundary-psame-transport-exhaustion}. The surviving operation is exhausted by $E,T,H,C,P,N$, with $T$ carrying classifier-respecting transport on named source rows.

The non-escape theorem removes the quotient-side objects from the handoff. Thus the bridge gives downstream consumers a displayed $\psame$ route and no quotient-soundness primitive.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary refusal-transport determinacy]
\label{thm:quotient-soundness-boundary-refusal-transport-determinacy}
For every accepted $\QuotientSoundnessBoundaryUp$ packet
$$
Q=(E,A,T,V,H,C,P,N),
$$
once the representative request recorded in $A$ has been refused by $V$, any accepted quotient-like consumer route is determined by the displayed $\psame$ route through $E,T,H,C,P,N$. The consumer cannot choose a second route through a quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, host-level class selector, unrestricted quotient recursion principle, or $\mathsf{Quot.sound}$ dependency.
\end{theorem}
\begin{proof}
Begin with the carrier of \autoref{def:quotient-soundness-boundary-carrier}. Its rows display the relation evidence $E$, representative requests $A$, $\psame$ transport $T$, refusal verdict $V$, componentwise stability $H$, continuation routes $C$, provenance $P$, and local naming $N$.

Use refusal locality, \autoref{thm:quotient-soundness-boundary-refusal-locality}. It forces the representative-selection demand to be read as the local pair $A,V$ before any positive transport is consumed.

After that refusal, apply $\psame$ transport exhaustion, \autoref{thm:quotient-soundness-boundary-psame-transport-exhaustion}, together with the bridge handoff, \autoref{thm:quotient-soundness-boundary-psame-bridge-handoff}. These results identify $E,T,H,C,P,N$ as the displayed consumer route after refusal.

Finally use kernel non-escape, \autoref{thm:quotient-soundness-boundary-kernel-nonescape}, and the public bridge, \autoref{thm:quotient-soundness-boundary-public-bridge}. Since the excluded quotient primitives have no coordinate in the packet, the accepted consumer has no second quotient route to select.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary source-row consumer totality]
\label{thm:quotient-soundness-boundary-source-row-consumer-totality}
Every surviving quotient-like consumer of an accepted $\QuotientSoundnessBoundaryUp$ packet must expose the source relation rows $E$ before it reads the positive transport row $T$. All consumer-facing data in the packet is exhausted by the finite rows
$$
E,A,T,V,H,C,P,N.
$$
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:quotient-soundness-boundary-carrier}. The row $E$ is the source relation surface, while $T$ is the classifier-respecting $\psame$ transport replacement available only as part of the displayed packet.

Apply root relation row exposure, \autoref{thm:quotient-soundness-boundary-root-relation-row-exposure}, and root transport verdict order, \autoref{thm:quotient-soundness-boundary-root-transport-verdict-order}. These force relation exposure and refusal to be visible before the positive transport row can be consumed.

Then use $\psame$ transport exhaustion and the bridge handoff. The surviving consumer route is the single path through $E,T,H,C,P,N$ after the refusal pair $A,V$ has been read.

Kernel non-escape and the public bridge complete the totality claim. They keep every consumer-facing read inside $E,A,T,V,H,C,P,N$ and exclude quotient carriers, hidden representative selectors, erased derivation recovery, host quotient eliminators, and $\mathsf{Quot.sound}$.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary verdict-transport separation]
\label{thm:quotient-soundness-boundary-verdict-transport-separation}
In every accepted $\QuotientSoundnessBoundaryUp$ packet $Q=(E,A,T,V,H,C,P,N)$, the row $V$ is a negative refusal row for the representative request in $A$, while $T$ is a positive classifier-respecting $\psame$ transport row on the source relation surface $E$. Neither row can be reread as the other, and neither row can be reread as $\mathsf{Quot.sound}$.
\end{theorem}
\begin{proof}
The carrier separates the roles at the level of rows. The row $V$ records the refusal verdict for the requested representative surface in $A$, while $T$ records the positive $\psame$ transport replacement over the relation rows $E$.

Use refusal locality to read $A,V$ as the negative part of the packet before transport. Use $\psame$ transport exhaustion and the bridge handoff to read $E,T,H,C,P,N$ as the positive route made available after that refusal.

The two readings therefore have different positions and different consumer roles inside the finite packet. The verdict row blocks representative selection; the transport row carries classifier-respecting named-source transport.

Finally apply kernel non-escape and the public bridge non-escape, \autoref{thm:quotient-soundness-boundary-public-bridge-nonescape}. Since $\mathsf{Quot.sound}$ and the host quotient primitives are outside the public bridge, neither $V$ nor $T$ can be reinterpreted as a quotient-soundness primitive.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary public bridge non-escape]
\label{thm:quotient-soundness-boundary-public-bridge-nonescape}
The public bridge supplied by $\QuotientSoundnessBoundaryUp$ is non-escaping: every consumer read remains inside $E,A,T,V,H,C,P,N$, and any request for quotient carriers, representative selectors, quotient equality eliminators, erased derivation recovery maps, host-level class selectors, unrestricted quotient recursion, or $\mathsf{Quot.sound}$ lies outside the bridge.
\end{theorem}
\begin{proof}
Apply the bridge schema theorem above to identify the public bridge with the refusal-and-transport tuple $E,A,T,V,H,C,P,N$.

Apply the $\psame$ bridge handoff theorem above for the positive route after refusal. It leaves only named-source transport through $E,T,H,C,P,N$.

Finally apply \autoref{thm:quotient-soundness-boundary-kernel-nonescape}. The kernel-facing route has no quotient primitive coordinate, so any request for such an object must be supplied by a separate certificate outside this chapter.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary erased derivation refusal]
\label{thm:quotient-soundness-boundary-erased-derivation-refusal}
For every accepted $\QuotientSoundnessBoundaryUp$ packet
$$
Q=(E,A,T,V,H,C,P,N),
$$
any request to recover an erased quotient derivation is first routed to the representative-request row $A$ and the refusal-verdict row $V$. The request is refused before the positive transport row $T$ is readable by a downstream consumer.
\end{theorem}
\begin{proof}
The carrier in \autoref{def:quotient-soundness-boundary-carrier} separates representative requests, refusal verdicts, and positive transport into the rows $A,V,T$. A recovery demand for an erased derivation is therefore not a transport demand; it is a representative-side demand.

Apply refusal locality, \autoref{thm:quotient-soundness-boundary-refusal-locality}. It forces the demand through $A,V$ before any consumer can use the positive transport route.

Then use verdict-transport separation, \autoref{thm:quotient-soundness-boundary-verdict-transport-separation}. The refusal row $V$ cannot be reread as the transport row $T$, and $T$ cannot be used to reconstruct a denied representative witness.

Finally apply public bridge non-escape, \autoref{thm:quotient-soundness-boundary-public-bridge-nonescape}. Erased derivation recovery has no bridge coordinate, so the only public response is the local refusal recorded by $A,V$.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary psame-only descent]
\label{thm:quotient-soundness-boundary-psame-only-descent}
After the refusal of representative recovery in an accepted $\QuotientSoundnessBoundaryUp$ packet $Q=(E,A,T,V,H,C,P,N)$, every accepted descent route factors only through $E,T,H,C,P,N$ using classifier-respecting $\psame$ transport. No accepted descent route uses representative selection, quotient equality elimination, erased derivation recovery, host quotient recursion, or $\mathsf{Quot.sound}$.
\end{theorem}
\begin{proof}
Begin after the refusal supplied by \autoref{thm:quotient-soundness-boundary-erased-derivation-refusal}. At that point the consumer may read the positive route only as a route downstream of $A,V$.

Apply $\psame$ transport exhaustion, \autoref{thm:quotient-soundness-boundary-psame-transport-exhaustion}. It identifies the surviving positive descent as classifier-respecting $\psame$ transport through $E,T,H,C,P,N$.

Use the $\psame$ bridge handoff, \autoref{thm:quotient-soundness-boundary-psame-bridge-handoff}, to pass this displayed route to the public bridge. Then apply source-row consumer totality, \autoref{thm:quotient-soundness-boundary-source-row-consumer-totality}, which exhausts the downstream read by the finite rows $E,A,T,V,H,C,P,N$.

The excluded quotient primitives have no coordinate in those rows. Hence an accepted descent route is psame-only and cannot pass through representative selection, quotient equality elimination, erased derivation recovery, host quotient recursion, or $\mathsf{Quot.sound}$.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary consumer non-escape]
\label{thm:quotient-soundness-boundary-consumer-nonescape}
Every downstream quotient-like consumer of an accepted $\QuotientSoundnessBoundaryUp$ packet is exhausted by the finite rows
$$
E,A,T,V,H,C,P,N.
$$
It cannot reinterpret the refusal verdict $V$ or the positive transport row $T$ as quotient equality elimination, erased derivation recovery, host quotient recursion, or $\mathsf{Quot.sound}$.
\end{theorem}
\begin{proof}
First apply erased derivation refusal, \autoref{thm:quotient-soundness-boundary-erased-derivation-refusal}. Any recovery demand is already localized to $A,V$ and refused before $T$ is available.

Next apply psame-only descent, \autoref{thm:quotient-soundness-boundary-psame-only-descent}. The surviving positive route factors through $E,T,H,C,P,N$ as classifier-respecting $\psame$ transport.

Finally use public bridge non-escape, \autoref{thm:quotient-soundness-boundary-public-bridge-nonescape}. Since every consumer read stays inside $E,A,T,V,H,C,P,N$, neither $V$ nor $T$ can be reinterpreted as a quotient-side eliminator, recovery map, recursion principle, or $\mathsf{Quot.sound}$ witness.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary representative request frontier]
\label{thm:quotient-soundness-boundary-representative-request-frontier}
For every accepted $\QuotientSoundnessBoundaryUp$ packet $Q=(E,A,T,V,H,C,P,N)$, any request for a representative, representative selector, erased derivation, or host quotient witness is exhausted by the source row $E$, representative-request row $A$, and refusal-verdict row $V$ before the positive transport row $T$ is readable. The request therefore reaches a visible refusal frontier before any downstream $\psame$ transport is exposed.
\end{theorem}
\begin{proof}
Start with erased derivation refusal, \autoref{thm:quotient-soundness-boundary-erased-derivation-refusal}. It routes recovery demands to $A,V$ before the positive transport row is available.

Apply refusal locality and verdict-transport separation, \autoref{thm:quotient-soundness-boundary-refusal-locality} and \autoref{thm:quotient-soundness-boundary-verdict-transport-separation}. These rows prevent a representative request from being reread as transport and prevent $T$ from reconstructing a denied witness.

Finally use consumer non-escape, \autoref{thm:quotient-soundness-boundary-consumer-nonescape}. The public consumer remains inside $E,A,T,V,H,C,P,N$, so the representative-side request is exhausted at $E,A,V$ before transport can be used.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary psame consumer closure]
\label{thm:quotient-soundness-boundary-psame-consumer-closure}
After the representative frontier refuses the representative-side request, every surviving downstream consumer closes through $E,T,H,C,P,N$ by classifier-respecting $\psame$ transport. The surviving route has no representative selector, quotient equality eliminator, erased derivation recovery map, host quotient recursion, or $\mathsf{Quot.sound}$ coordinate.
\end{theorem}
\begin{proof}
First apply the representative request frontier above. It leaves the positive route available only after $E,A,V$ have already recorded the refusal boundary.

Use psame-only descent, \autoref{thm:quotient-soundness-boundary-psame-only-descent}. The surviving route factors through $E,T,H,C,P,N$ as classifier-respecting $\psame$ transport.

Then apply the public bridge non-escape theorem, \autoref{thm:quotient-soundness-boundary-public-bridge-nonescape}. The bridge has no coordinate for the excluded quotient primitives, so the psame-only consumer route is closed by the displayed finite rows.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary Quot.sound refusal exactness]
\label{thm:quotient-soundness-boundary-quot-sound-refusal-exactness}
In an accepted $\QuotientSoundnessBoundaryUp$ packet, a request for $\mathsf{Quot.sound}$ is refused exactly at the visible boundary rows $E,A,V$ and cannot be served by the positive transport row $T$ or by the consumer rows $H,C,P,N$. Conversely, the admitted positive route through $E,T,H,C,P,N$ is precisely the $\psame$-only route that remains after that refusal.
\end{theorem}
\begin{proof}
For the refusal direction, apply the representative request frontier above. A $\mathsf{Quot.sound}$ demand is a representative-side quotient primitive request, so it is routed to $E,A,V$ before $T$ is readable.

Apply kernel non-escape and public bridge non-escape, \autoref{thm:quotient-soundness-boundary-kernel-nonescape} and \autoref{thm:quotient-soundness-boundary-public-bridge-nonescape}. Neither the kernel-facing route nor the bridge exposes a $\mathsf{Quot.sound}$ coordinate.

For the surviving route, use psame consumer closure above. Once the refusal is recorded, every admitted consumer is exactly the classifier-respecting $\psame$ route through $E,T,H,C,P,N$, so the refusal boundary and the positive route are exact complements inside the finite packet.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary root psame route totality]
\label{thm:quotient-soundness-boundary-root-psame-route-totality}
For every accepted $\QuotientSoundnessBoundaryUp$ packet, every surviving root operation after representative refusal factors through the displayed $\psame$ route $E,T,H,C,P,N$. The packet exports no root operation through a representative selector, quotient equality eliminator, erased derivation recovery map, host quotient recursion, or $\mathsf{Quot.sound}$ witness.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations and fixed classifier, \autoref{thm:quotient-soundness-boundary-namecert-obligations} and \autoref{thm:quotient-soundness-boundary-fixed-classifier}. They identify the relation source and classifier rows read by any accepted packet.

Next use kernel non-escape and psame transport exhaustion, \autoref{thm:quotient-soundness-boundary-kernel-nonescape} and \autoref{thm:quotient-soundness-boundary-psame-transport-exhaustion}. These results leave only the classifier-respecting $\psame$ transport row and the structural rows $H,C,P,N$ for a surviving root operation.

Finally apply \autoref{thm:quotient-soundness-boundary-quot-sound-refusal-exactness}. Once $\mathsf{Quot.sound}$ is refused at the visible boundary, the positive consumer route is exactly $E,T,H,C,P,N$.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary refusal before transport]
\label{thm:quotient-soundness-boundary-refusal-before-transport}
In every accepted $\QuotientSoundnessBoundaryUp$ packet, the representative-request row $A$ and refusal-verdict row $V$ are read before the positive $\psame$ transport row $T$. Thus no surviving use of $T$ can retroactively supply a representative selector, erased derivation, quotient equality eliminator, host quotient recursion, or $\mathsf{Quot.sound}$ witness.
\end{theorem}
\begin{proof}
Apply the root transport verdict order and representative request frontier, \autoref{thm:quotient-soundness-boundary-root-transport-verdict-order} and \autoref{thm:quotient-soundness-boundary-representative-request-frontier}. Together they place $A,V$ at the visible refusal boundary before $T$ is available to a downstream consumer.

Use verdict-transport separation, \autoref{thm:quotient-soundness-boundary-verdict-transport-separation}. The positive transport row cannot reread the refusal verdict as a quotient eliminator or as evidence for a representative witness.

Finally invoke \autoref{thm:quotient-soundness-boundary-quot-sound-refusal-exactness}. The exact complement between refusal and the surviving $\psame$ route gives the required order: refusal first, transport only after that refusal has been displayed.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary kernel consumer audit triad]
\label{thm:quotient-soundness-boundary-kernel-consumer-audit-triad}
The kernel-facing audit surface of an accepted $\QuotientSoundnessBoundaryUp$ packet consists exactly of the relation exposure row, the refusal verdict for representative-side quotient primitives, and the surviving $\psame$ replacement route. Equivalently, every kernel consumer reads the triad
$$
E,A,V \quad\text{and}\quad E,T,H,C,P,N
$$
with the refusal side preceding transport, and no further quotient-soundness coordinate is exported.
\end{theorem}
\begin{proof}
First apply relation row exposure and the fixed classifier, \autoref{thm:quotient-soundness-boundary-root-relation-row-exposure} and \autoref{thm:quotient-soundness-boundary-fixed-classifier}. They expose the source relation and classifier rows without admitting a host quotient witness.

Next use refusal before transport above. The representative-request row and refusal-verdict row are read before the positive transport row, so the audit records the negative verdict before any surviving consumer can proceed.

Finally combine root psame route totality with quotient-sound refusal exactness. The surviving side is exactly the $\psame$ replacement route through $E,T,H,C,P,N$, while the refused side stays at $E,A,V$; kernel non-escape then rules out any additional quotient-soundness coordinate.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary root representative refusal]
\label{thm:quotient-soundness-boundary-root-representative-refusal}
Every root representative-side quotient request accepted by a $\QuotientSoundnessBoundaryUp$ packet is read through the relation row $E$, the requested-representative row $A$, and the refusal-verdict row $V$ before the transport row $T$ becomes available. Thus the root request reaches the visible representative refusal frontier before any surviving $\psame$ replacement route can be consumed.
\end{theorem}
\begin{proof}
Start with the representative request frontier, \autoref{thm:quotient-soundness-boundary-representative-request-frontier}. It exhausts representative, representative-selector, erased-derivation, and host quotient requests by $E,A,V$ before the transport row $T$ is readable.

Apply refusal before transport, \autoref{thm:quotient-soundness-boundary-refusal-before-transport}. The refusal verdict is consumed before any positive transport use, so a downstream root consumer cannot reverse the order and treat $T$ as representative evidence.

Finally use the kernel consumer audit triad, \autoref{thm:quotient-soundness-boundary-kernel-consumer-audit-triad}. The kernel-facing surface keeps the refusal side $E,A,V$ distinct from the surviving transport side $E,T,H,C,P,N$, giving the stated root order.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary root psame transport admission]
\label{thm:quotient-soundness-boundary-root-psame-transport-admission}
Every surviving root operation of an accepted $\QuotientSoundnessBoundaryUp$ packet is admitted only through the classifier-respecting $\psame$ transport route $E,T,H,C,P,N$. The operation may read the refused representative-side rows $E,A,V$ as its accountability surface, but its positive movement is exactly the displayed transport, stability, locality, provenance, and naming route.
\end{theorem}
\begin{proof}
Begin after the refusal surface has been read. By \autoref{thm:quotient-soundness-boundary-root-representative-refusal}, every root representative-side request has already passed through $E,A,V$ before the positive route is available.

Use psame-only descent, \autoref{thm:quotient-soundness-boundary-psame-only-descent}. It states that accepted descent after refusal factors only through $E,T,H,C,P,N$ by classifier-respecting $\psame$ transport.

Now apply root psame route totality, \autoref{thm:quotient-soundness-boundary-root-psame-route-totality}. This makes the same $E,T,H,C,P,N$ route total for surviving root operations and excludes representative selection, quotient equality elimination, erased derivation recovery, host quotient recursion, and $\mathsf{Quot.sound}$.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary root nonimport ledger]
\label{thm:quotient-soundness-boundary-root-nonimport-ledger}
The root kernel ledger exported by a $\QuotientSoundnessBoundaryUp$ packet is exactly the refused representative surface $E,A,V$ together with the admitted $\psame$ transport surface $E,T,H,C,P,N$. It imports no quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, host quotient recursion principle, or $\mathsf{Quot.sound}$ dependency.
\end{theorem}
\begin{proof}
First combine root representative refusal with root $\psame$ transport admission, \autoref{thm:quotient-soundness-boundary-root-representative-refusal} and \autoref{thm:quotient-soundness-boundary-root-psame-transport-admission}. These two rows identify the negative ledger side $E,A,V$ and the surviving positive side $E,T,H,C,P,N$.

Apply the kernel consumer audit triad, \autoref{thm:quotient-soundness-boundary-kernel-consumer-audit-triad}. It packages those two sides as the kernel-facing audit surface and records that no further quotient-soundness coordinate is exported.

Finally cite public bridge non-escape and psame-only descent, \autoref{thm:quotient-soundness-boundary-public-bridge-nonescape} and \autoref{thm:quotient-soundness-boundary-psame-only-descent}. The bridge and descent rows exclude quotient carriers, representative selectors, erased derivation recovery, host quotient recursion, and $\mathsf{Quot.sound}$, so the ledger is a nonimport ledger rather than a quotient extension.
\end{proof}

\closureat{QuotientSoundnessBoundaryUp}{seedStr}
\closureat{QuotientSoundnessBoundaryUp}{obligationStr}
\closureat{QuotientSoundnessBoundaryUp}{scopedStr}
\closureat{QuotientSoundnessBoundaryUp}{publicStr}
\closureat{QuotientSoundnessBoundaryUp}{bridgedStr}
\closureat{QuotientSoundnessBoundaryUp}{matureStr}

\begin{closurestatus}{\QuotientSoundnessBoundaryUp}
  \constructivestory{A $\QuotientSoundnessBoundaryUp$ packet is generated from relation rows, requested representative rows, classifier-respecting $\psame$ transport, refusal verdicts, componentwise transports, displayed continuations, package provenance, and local naming.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{def:quotient-soundness-boundary-carrier}, \autoref{thm:quotient-soundness-boundary-namecert-obligations}, \autoref{thm:quotient-soundness-boundary-transport-replacement}, \autoref{thm:quotient-soundness-boundary-negative-refusal-dependency}, \autoref{thm:quotient-soundness-boundary-fixed-classifier}, \autoref{thm:quotient-soundness-boundary-kernel-nonescape}, \autoref{thm:quotient-soundness-boundary-root-request-triad}, \autoref{thm:quotient-soundness-boundary-root-relation-row-exposure}, \autoref{thm:quotient-soundness-boundary-root-transport-verdict-order}, \autoref{thm:quotient-soundness-boundary-root-downstream-unblock-surface}, \autoref{thm:quotient-soundness-boundary-psame-replacement-classifier}, \autoref{thm:quotient-soundness-boundary-kernel-refusal-ledger}, \autoref{thm:quotient-soundness-boundary-namecert-ledger}, \autoref{thm:quotient-soundness-boundary-bridge-schema}, \autoref{thm:quotient-soundness-boundary-psame-bridge-handoff}, \autoref{thm:quotient-soundness-boundary-refusal-transport-determinacy}, \autoref{thm:quotient-soundness-boundary-source-row-consumer-totality}, \autoref{thm:quotient-soundness-boundary-verdict-transport-separation}, \autoref{thm:quotient-soundness-boundary-public-bridge-nonescape}, \autoref{thm:quotient-soundness-boundary-erased-derivation-refusal}, \autoref{thm:quotient-soundness-boundary-psame-only-descent}, and \autoref{thm:quotient-soundness-boundary-consumer-nonescape} bind the relation rows, representative-refusal rows, refusal-to-transport determinacy, source-row consumer totality, verdict-transport separation, erased derivation refusal, psame-only descent, root relation exposure, verdict-before-transport order, $\psame$ transport row, locality rows, provenance, public bridge, and non-escape boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{paperBridge}
  \origin{human}
  \notclaimed{This chapter does not construct quotient carriers, representative selectors, quotient equality eliminators, erased derivation recovery maps, or any dependency on $\mathsf{Quot.sound}$.}
  \upgradepath{No paper theory-axis grade remains; Lean verification and bridge checking remain independent obligations for the same finite refusal-and-transport packet.}
\end{closurestatus}
