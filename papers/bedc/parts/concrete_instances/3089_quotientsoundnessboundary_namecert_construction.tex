\chapter{A Concrete Naming Certificate for $\QuotientSoundnessBoundaryUp$}
\label{ch:concrete-instances-quotient-soundness-boundary-namecert}
\origin{human}

The $\QuotientSoundnessBoundaryUp$ interface names the finite boundary at which a quotient-style equality request is refused as an unwitnessed representative-selection step and replaced by classifier-respecting $\psame$ transport. It does not construct a quotient type, select a canonical representative, collapse equivalent source rows, or import $\mathsf{Quot.sound}$. It records the visible source relation rows, requested representative rows, transport replacement, refusal verdict, locality routes, provenance, and naming certificate that downstream quotient-like chapters may consume.

The refusal surface is the quotient-facing specialization of the negative-boundary discipline of \autoref{ch:concrete-instances-negative-name-boundary-namecert}: representative-selection requests are first treated as blocked boundary reads, and only then replaced by local $\psame$ transport.

\begin{definition}[QuotientSoundnessBoundary carrier]
\label{def:quotient-soundness-boundary-carrier}
A $\QuotientSoundnessBoundaryUp$ carrier is a finite $\BHist$ packet
$$
Q=(E,A,T,V,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the finite family of quotient-relation rows, recording the source carrier entries, their classifier relation, and the displayed equivalence-derivation data admitted by the packet;
\item $A$ is the requested representative row family, recording each attempted demand for a canonical representative, section, quotient equality token, or erased derivation tree;
\item $T$ is the $\psame$ transport replacement row, recording the classifier-respecting route by which an operation reads related source rows without selecting a representative;
\item $V$ is the refusal verdict row, recording that the requested representative-selection surface in $A$ is not admitted and that downstream use must pass through $T$;
\item $H$ records componentwise $\hsame$ transport for the relation rows, requested representative rows, transport replacement, refusal verdict, continuation routes, provenance, and local naming row;
\item $C$ records the visible $\Cont$ routes from the relation rows to the refusal verdict, from the relation rows to the $\psame$ transport replacement, and from the transport replacement to the downstream classifier-respecting consumer;
\item $P$ is the $\Pkg$ provenance over $\QuotientSoundnessBoundaryUp$, quotient-relation rows, requested representative rows, $\psame$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\QuotientSoundnessBoundaryUp}$ row.
\end{enumerate}
The carrier exports only a finite refusal-and-transport packet. It contains no coordinate for a quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, or $\mathsf{Quot.sound}$ dependency.
\end{definition}

\begin{theorem}[QuotientSoundnessBoundary NameCert obligations]
\label{thm:quotient-soundness-boundary-namecert-obligations}
For every accepted $\QuotientSoundnessBoundaryUp$ carrier
$$
Q=(E,A,T,V,H,C,P,N),
$$
the local $\NameCert_{\QuotientSoundnessBoundaryUp}$ surface consists exactly of carrier admission, relation-row admission, requested-representative refusal, classifier-respecting $\psame$ transport, componentwise $\hsame$ stability, displayed $\Cont$ locality, $\Pkg$ provenance, and non-admission of quotient carriers, representative selectors, quotient equality eliminators, erased derivation recovery maps, and $\mathsf{Quot.sound}$ dependencies.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:quotient-soundness-boundary-carrier}. The tuple exposes the relation rows, requested representative rows, transport replacement, refusal verdict, transport rows, continuation routes, provenance, and local naming row.

The requested representative rows are read through $A$ and judged by $V$. The verdict attaches the refusal to the displayed demands rather than to an anonymous global prohibition, so every blocked quotient-soundness request remains audit-visible.

The replacement obligation is supplied by $T$. It routes classifier-respecting consumers through $\psame$ transport on the source rows named in $E$, while $H$ and $C$ keep the transported read componentwise stable and local to the displayed packet.

Since no coordinate of $Q$ is a quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, or $\mathsf{Quot.sound}$ dependency, the local certificate is exhausted by the listed finite obligations.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary transport replacement]
\label{thm:quotient-soundness-boundary-transport-replacement}
Every downstream operation accepted by a $\QuotientSoundnessBoundaryUp$ packet that would otherwise request a quotient representative must instead factor through the source relation rows $E$, the classifier-respecting $\psame$ transport row $T$, the componentwise transport row $H$, the continuation routes $C$, and the local naming row $N$. The result is an operation on named source rows, not an operation on a selected representative of a quotient class.
\end{theorem}
\begin{proof}
Begin with a consumer request recorded in $A$. By the refusal verdict $V$, the request is not admitted as a representative selector, quotient carrier, quotient equality eliminator, or derivation eraser.

The only positive route left by the carrier is the transport row $T$. It reads the relevant relation rows in $E$ and supplies the classifier-respecting $\psame$ transport needed by the downstream operation.

The rows $H$ and $C$ then make the replacement stable and local: $H$ moves the relation, refusal, and transport data componentwise, while $C$ displays the route from relation evidence to transport and then to the consumer. Thus the operation proceeds on named source rows and never obtains a hidden quotient representative.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary negative refusal dependency]
\label{thm:quotient-soundness-boundary-negative-refusal-dependency}
Every quotient-style representative request accepted by a $\QuotientSoundnessBoundaryUp$ packet first lands in the negative-boundary refusal discipline of \autoref{ch:concrete-instances-negative-name-boundary-namecert}. This chapter specializes that discipline to the visible relation rows $E$, requested representative rows $A$, classifier-respecting $\psame$ transport row $T$, refusal verdict $V$, componentwise transport $H$, continuation routes $C$, provenance $P$, and local naming row $N$, replacing any appeal to $\mathsf{Quot.sound}$ by displayed transport through $T$.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:quotient-soundness-boundary-carrier}. The requested representative rows $A$ and refusal verdict $V$ record the same kind of blocked boundary read handled by the negative-name boundary.

Apply the transport replacement theorem, \autoref{thm:quotient-soundness-boundary-transport-replacement}. It routes the surviving operation through $E,T,H,C,N$ rather than through a quotient representative or quotient equality eliminator.

Use the sibling refusal surface \autoref{ch:concrete-instances-negative-name-boundary-namecert} to read the representative request as a negative boundary request before the transport replacement is consumed. The local packet then records the specialized quotient-facing rows $E,A,T,V,H,C,P,N$.

Finally cite non-escape, \autoref{thm:quotient-soundness-boundary-non-escape}. Since the excluded quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, and $\mathsf{Quot.sound}$ dependency are absent from the packet, the only exported replacement is the displayed $\psame$ transport route.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary non-escape]
\label{thm:quotient-soundness-boundary-non-escape}
An accepted $\QuotientSoundnessBoundaryUp$ carrier cannot export a quotient carrier, canonical representative, quotient equality eliminator, erased equivalence-derivation recovery map, unrestricted quotient-recursion principle, host-level class selector, or $\mathsf{Quot.sound}$ dependency. Any consumer request for one of these objects leaves the finite packet
$$
E,A,T,V,H,C,P,N
$$
and must be supplied by an explicit extra certificate outside this boundary.
\end{theorem}
\begin{proof}
Inspect the carrier inventory. The rows $E,A,T,V,H,C,P,N$ name relation evidence, representative requests, transport replacement, refusal, stability, locality, provenance, and the local certificate.

None of these rows has the type or role of a quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, unrestricted quotient-recursion principle, host-level class selector, or $\mathsf{Quot.sound}$ dependency. The rows $A$ and $V$ record such demands as refused requests, and $T$ records only the classifier-respecting replacement.

Therefore a downstream consumer cannot project any excluded object from the packet. It may either use the displayed $\psame$ transport route or supply a separate certificate whose boundary is audited independently.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary refusal locality]
\label{thm:quotient-soundness-boundary-refusal-locality}
For every accepted $\QuotientSoundnessBoundaryUp$ carrier
$$
Q=(E,A,T,V,H,C,P,N),
$$
every representative-selection demand is localized to the requested-representative rows $A$ and the refusal verdict rows $V$ before any transport replacement is consumed. No downstream consumer may treat $T$, $H$, $C$, $P$, or $N$ as evidence that a representative selector, quotient equality eliminator, erased derivation recovery map, or $\mathsf{Quot.sound}$ dependency has been admitted.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:quotient-soundness-boundary-carrier}. The only rows that record a demand for a canonical representative, quotient equality token, section, or erased derivation tree are the rows $A$.

The verdict rows $V$ attach the refusal to those displayed demands. Thus the blocked request is not a global prohibition detached from the packet; it is a local row pair $A,V$ that can be audited before the replacement route is used.

Apply \autoref{thm:quotient-soundness-boundary-transport-replacement}. The positive route after refusal is the classifier-respecting $\psame$ transport through $T$, stabilized and routed by $H$ and $C$ and named by $P,N$. Since \autoref{thm:quotient-soundness-boundary-non-escape} excludes the quotient-side objects, the replacement rows cannot be reread as hidden representative selection.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary psame transport exhaustion]
\label{thm:quotient-soundness-boundary-psame-transport-exhaustion}
Every surviving quotient-style operation accepted by a $\QuotientSoundnessBoundaryUp$ packet is exhausted by the displayed route through the source relation rows $E$, the classifier-respecting $\psame$ transport row $T$, the componentwise stability row $H$, the continuation row $C$, the package provenance row $P$, and the local naming row $N$. After the refusal pair $A,V$ has been read, no second route remains through a quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, unrestricted quotient-recursion principle, host-level class selector, or $\mathsf{Quot.sound}$ dependency.
\end{theorem}
\begin{proof}
Begin with a surviving operation. By \autoref{thm:quotient-soundness-boundary-refusal-locality}, its representative-selection demand has already been localized to $A,V$ and refused before any positive transport is consumed.

The carrier then exposes only the source relation rows $E$ and the replacement row $T$ as the operation-facing data. The row $T$ is explicitly a classifier-respecting $\psame$ route on named source rows, not an operation on quotient classes.

The rows $H,C,P,N$ provide stability, locality, provenance, and naming for that same route. Apply \autoref{thm:quotient-soundness-boundary-non-escape} to remove the excluded quotient objects from the exported surface. Therefore the displayed $E,T,H,C,P,N$ route exhausts the surviving operation.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary downstream audit package]
\label{thm:quotient-soundness-boundary-downstream-audit-package}
The refusal locality theorem, the $\psame$ transport exhaustion theorem, and the non-escape theorem assemble a downstream audit package for quotient-like BEDC chapters. A downstream chapter may consume the refusal verdict $A,V$ and the transport route $E,T,H,C,P,N$ as an auditable refusal-and-transport surface, but it receives no quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, unrestricted quotient-recursion principle, host-level class selector, or $\mathsf{Quot.sound}$ dependency from $\QuotientSoundnessBoundaryUp$.
\end{theorem}
\begin{proof}
Use \autoref{thm:quotient-soundness-boundary-refusal-locality} to bind the negative part of the package. It says every representative-selection demand is visible as $A$ and refused by $V$ before replacement.

Use \autoref{thm:quotient-soundness-boundary-psame-transport-exhaustion} for the positive part. It identifies the only surviving operation route as $E,T,H,C,P,N$, with $\psame$ transport carried by $T$ and stabilized through $H,C$.

Finally apply \autoref{thm:quotient-soundness-boundary-non-escape}. The excluded quotient primitives cannot be projected from the packet, so downstream quotient-like chapters receive an audit surface consisting of refusal plus displayed transport, not a quotient kernel extension.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary fixed classifier]
\label{thm:quotient-soundness-boundary-fixed-classifier}
For an accepted $\QuotientSoundnessBoundaryUp$ carrier $Q=(E,A,T,V,H,C,P,N)$, every accepted comparison is exhausted by the relation rows $E$, requested representative rows $A$, replacement row $T$, refusal verdict $V$, and the displayed transport rows $H,C$. No comparison may consult a quotient carrier, canonical representative, quotient equality eliminator, erased derivation recovery map, or $\mathsf{Quot.sound}$ dependency.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:quotient-soundness-boundary-carrier}. The only comparison-facing coordinates are the relation evidence $E$, the recorded representative demands $A$, the replacement row $T$, the verdict $V$, and the locality rows $H,C$.

Apply the transport replacement theorem, \autoref{thm:quotient-soundness-boundary-transport-replacement}. It consumes $E$ and $T$ as the positive classifier-respecting route, while $A$ and $V$ keep representative selection in the refusal ledger.

The non-escape theorem, \autoref{thm:quotient-soundness-boundary-non-escape}, excludes every quotient eliminator and $\mathsf{Quot.sound}$ route from the packet. Hence the classifier is fixed by $E,A,T,V,H,C$ and cannot be widened by an ambient quotient principle.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary kernel non-escape]
\label{thm:quotient-soundness-boundary-kernel-nonescape}
Every kernel-facing consumer of an accepted $\QuotientSoundnessBoundaryUp$ packet remains inside the rows
$$
E,A,T,V,H,C,P,N.
$$
The packet cannot import $\mathsf{Quot.sound}$, a host quotient eliminator, a hidden representative selector, or erased relation-derivation recovery.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:quotient-soundness-boundary-namecert-obligations}. They list carrier admission, relation admission, refused representative requests, classifier-respecting transport, componentwise stability, locality, provenance, and local naming as the full public surface.

Use the fixed classifier theorem above to identify the comparison route. The kernel-facing read is not an arbitrary quotient read; it is the finite path through $E,A,T,V,H,C$ and then through package provenance and the local certificate.

Finally apply \autoref{thm:quotient-soundness-boundary-negative-refusal-dependency} and \autoref{thm:quotient-soundness-boundary-non-escape}. The refusal dependency explains why the blocked request is represented negatively, and non-escape prevents any $\mathsf{Quot.sound}$ or host quotient eliminator from entering the kernel route.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary NameCert ledger]
\label{thm:quotient-soundness-boundary-namecert-ledger}
The carrier of \autoref{def:quotient-soundness-boundary-carrier}, the fixed classifier of \autoref{thm:quotient-soundness-boundary-fixed-classifier}, the transport replacement of \autoref{thm:quotient-soundness-boundary-transport-replacement}, the negative-refusal dependency of \autoref{thm:quotient-soundness-boundary-negative-refusal-dependency}, and the kernel non-escape theorem of \autoref{thm:quotient-soundness-boundary-kernel-nonescape} assemble the local $\NameCert_{\QuotientSoundnessBoundaryUp}$ ledger.
\end{theorem}
\begin{proof}
The carrier supplies the finite inventory $E,A,T,V,H,C,P,N$. The fixed classifier theorem binds every accepted comparison to the displayed relation, request, replacement, verdict, and transport rows.

The transport replacement theorem supplies the positive operation available to downstream consumers. It routes classifier-respecting reads through named source rows rather than through selected quotient representatives.

The negative-refusal dependency and kernel non-escape theorems complete the ledger. They record the blocked quotient-soundness demand as a negative boundary request and keep every exported consumer inside $E,A,T,V,H,C,P,N$, so the local naming certificate contains exactly the quotient-boundary obligations listed in \autoref{thm:quotient-soundness-boundary-namecert-obligations}.
\end{proof}

\closureat{QuotientSoundnessBoundaryUp}{seedStr}

\begin{closurestatus}{\QuotientSoundnessBoundaryUp}
  \constructivestory{A $\QuotientSoundnessBoundaryUp$ packet is generated from relation rows, requested representative rows, classifier-respecting $\psame$ transport, refusal verdicts, componentwise transports, displayed continuations, package provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:quotient-soundness-boundary-carrier}, \autoref{thm:quotient-soundness-boundary-namecert-obligations}, \autoref{thm:quotient-soundness-boundary-transport-replacement}, and \autoref{thm:quotient-soundness-boundary-non-escape} display the finite refusal-and-transport boundary for quotient-style requests.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not construct quotient carriers, representative selectors, quotient equality eliminators, erased derivation recovery maps, or any dependency on $\mathsf{Quot.sound}$.}
  \upgradepath{Obligation closure requires checked carrier admission, requested-representative refusal, classifier-respecting $\psame$ transport, locality, provenance, and non-escape for the same refusal-and-transport packet.}
\end{closurestatus}
