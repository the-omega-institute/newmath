\chapter{A Concrete Naming Certificate for $\KoksmaHlawkaFiniteErrorUp$}
\label{ch:concrete-instances-koksma-hlawka-finite-error-namecert}
\origin{human}

Following Koksma and Hlawka, the $\KoksmaHlawkaFiniteErrorUp$ packet records a finite numerical-integration error certificate whose only analytic inputs are a displayed discrepancy ledger and a displayed bounded-variation ledger. It sits after the low-discrepancy sequence surface of \autoref{ch:concrete-instances-low-discrepancy-sequence-namecert}, the bounded-variation family surface of \autoref{ch:concrete-instances-boundedvariation-namecert}, the finite epsilon-net tolerance row of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names a finite error handoff only; it does not import measure completion, selected representatives, quotient equality of functions, host total variation, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Koksma--Hlawka finite-error carrier]
\label{def:koksma-hlawka-finite-error-carrier}
A $\KoksmaHlawkaFiniteErrorUp$ carrier is a finite $\BHist$ packet
$$
K=(S,D,V,E,Q,T,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\LowDiscrepancySequenceUp$ source row, carrying the finite point schedule and the inspected star-discrepancy windows.
\item $D$ is the discrepancy ledger, recording the finite dyadic upper bound read from $S$.
\item $V$ is the $\BoundedVariationUp$ row for the integrand, including the finite partition-variation ledger and endpoint readback surface.
\item $E$ is the $\FiniteEpsilonNetUp$ tolerance row that names the finite comparison scale used by the error request.
\item $Q$ is the $\DyadicRatCoreUp$ product-budget row joining the displayed discrepancy and variation budgets.
\item $T$ is the $\RegSeqRatUp$ readback row for the certified quadrature error bound.
\item $R$ is the terminal $\RealUp$ seal for the finite error estimate.
\item $H$ records componentwise $\hsame$ transport for the sequence, discrepancy, variation, epsilon-net, dyadic-budget, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from discrepancy and variation rows to the dyadic product budget, then to regular-rational readback and the terminal real seal.
\item $P$ is the $\Pkg$ provenance over $\KoksmaHlawkaFiniteErrorUp$, $\LowDiscrepancySequenceUp$, $\BoundedVariationUp$, $\FiniteEpsilonNetUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\KoksmaHlawkaFiniteErrorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,D,V,E,Q,T,R,H,C,P,N$. It has no coordinate for a measure-completion object, a selected representative of the integrand, quotient function equality, a host supremum over all partitions, or an ambient completeness theorem.
\end{definition}

\begin{theorem}[Koksma--Hlawka NameCert obligations]
\label{thm:koksma-hlawka-namecert-obligations}
For every accepted $\KoksmaHlawkaFiniteErrorUp$ carrier $K=(S,D,V,E,Q,T,R,H,C,P,N)$, the local $\NameCert_{\KoksmaHlawkaFiniteErrorUp}$ surface consists exactly of carrier admission for the low-discrepancy source $S$, discrepancy ledger $D$, bounded-variation ledger $V$, finite epsilon-net tolerance row $E$, dyadic product budget $Q$, regular-rational readback $T$, terminal real seal $R$, componentwise transport $H$, displayed replay $C$, package provenance $P$, and local naming row $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:koksma-hlawka-finite-error-carrier}. The analytic rows visible to the certificate are the source schedule, discrepancy ledger, variation ledger, tolerance row, product budget, readback row, and terminal seal.

The sequence side is exhausted by $S,D,E$. The low-discrepancy row supplies the inspected point schedule, the discrepancy ledger supplies the dyadic upper bound, and the finite epsilon-net row supplies the tolerance scale at which the error request is read.

The integrand side is exhausted by $V$. Its bounded-variation ledger exposes only finite partition and endpoint-readback data before the product budget is available.

Finally $Q,T,R,H,C,P,N$ multiply the displayed budgets, read the product as regular-rational data, seal the finite estimate as $\RealUp$, transport the same rows, replay the route, record provenance, and name the packet. Since no excluded coordinate appears in the carrier, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Koksma--Hlawka error handoff]
\label{thm:koksma-hlawka-error-handoff}
Every quadrature-error consumer of an accepted $\KoksmaHlawkaFiniteErrorUp$ packet factors through the finite route
$$
\LowDiscrepancySequenceUp \longmapsto \DyadicRatCoreUp \longmapsto \BoundedVariationUp \longmapsto \FiniteEpsilonNetUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
The route exports only the displayed discrepancy budget, bounded-variation budget, tolerance scale, dyadic product ledger, regular-rational readback, terminal real seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:koksma-hlawka-namecert-obligations}. They force every consumer read to expose the sequence schedule, discrepancy ledger, variation ledger, epsilon-net tolerance, and dyadic product budget before any error estimate can be read.

Next read the dyadic budget row $Q$. It is the only coordinate where discrepancy and variation interact, so the consumer cannot bypass it through a measure-completion object, a hidden partition supremum, or a selected representative of the integrand.

The regular-rational row $T$ and terminal seal $R$ are downstream from $Q$. The structural rows $H,C,P,N$ preserve that order under transport, replay, provenance, and local naming.

Thus the finite error handoff is exactly the displayed Koksma--Hlawka route and carries no additional analytic principle.
\end{proof}

\closureat{KoksmaHlawkaFiniteErrorUp}{seedStr}

\begin{closurestatus}{\KoksmaHlawkaFiniteErrorUp}
  \constructivestory{A $\KoksmaHlawkaFiniteErrorUp$ packet is generated from a LowDiscrepancySequence source, a displayed discrepancy ledger, a BoundedVariation ledger, a FiniteEpsilonNet tolerance row, a DyadicRatCore product budget, RegSeqRat readback, a RealUp error seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:koksma-hlawka-finite-error-carrier}, \autoref{thm:koksma-hlawka-namecert-obligations}, and \autoref{thm:koksma-hlawka-error-handoff} seed the finite Koksma--Hlawka error route over discrepancy, bounded-variation, epsilon-net, dyadic, regular-rational, and real-seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, measure completion, selected integrand representatives, quotient function equality, host total variation, countable choice, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, discrepancy-ledger exactness, bounded-variation ledger exactness, epsilon-net tolerance routing, dyadic product-budget routing, RegSeqRat readback, RealUp error sealing, transport stability, continuation replay, and provenance nonescape for BEDC.Derived.KoksmaHlawkaFiniteErrorUp.}
\end{closurestatus}
