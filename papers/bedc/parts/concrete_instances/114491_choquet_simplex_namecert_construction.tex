\chapter{A Concrete Naming Certificate for $\ChoquetSimplexUp$}
\label{ch:concrete-instances-choquet-simplex-namecert}
\origin{human}

Following Choquet on compact convex sets and the Bishop--Bridges constructive reading of probability measures, the $\ChoquetSimplexUp$ packet records the boundary-facing finite NameCert surface for compact convex probability carriers. It sits after the compact source of \autoref{ch:concrete-instances-compact-namecert}, the convex carrier of \autoref{ch:concrete-instances-convexset-namecert}, the Radon-measure route of \autoref{ch:concrete-instances-radon-measure-namecert}, and the locally convex source of \autoref{ch:concrete-instances-locally-convex-space-namecert}. The chapter names only the carrier rows needed by a compact-convex probability-boundary consumer; it does not assert representing-measure uniqueness, full Krein--Milman strength, selected extreme points, countable choice, quotient measure equality, or ambient locally convex completeness.

\begin{definition}[Choquet simplex carrier surface]
\label{def:choquet-simplex-carrier-surface}
A $\ChoquetSimplexUp$ carrier is a finite $\BHist$ packet
$$
S=(K,V,P,B,R,E,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact source row, read through the compact carrier and compact-metric observation surfaces.
\item $V$ is the convex carrier row, including the displayed affine-combination and convex-subcarrier data visible to the packet.
\item $P$ is the probability row, recording finite probability-window requests over the compact convex source.
\item $B$ is the boundary-window row, naming the inspected extreme-boundary candidates only as finite observations.
\item $R$ is the $\RadonMeasureUp$ readback row available after the compact, convex, probability, and boundary-window rows have been displayed.
\item $E$ is the locally convex exposure row, read through $\LocallyConvexSpaceUp$ seminorm and neighbourhood observations needed by affine consumers.
\item $H$ records componentwise $\hsame$ transport for compact, convex, probability, boundary, Radon, locally convex, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a compact-convex probability-boundary request through the same rows.
\item $Q$ is the $\Pkg$ provenance over $\ChoquetSimplexUp$, $\CompactUp$, $\ConvexSetUp$, $\RadonMeasureUp$, $\LocallyConvexSpaceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\ChoquetSimplexUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,V,P,B,R,E,H,C,Q,N$. It has no coordinate for a host simplex object, selected barycentric measure, representing-measure uniqueness, full extreme-point generation, quotient measure equality, countable choice, or ambient locally convex completeness.
\end{definition}
\leanchecked{BEDC.Derived.ChoquetSimplexUp.ChoquetSimplexTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Choquet simplex certificate surface]
\label{thm:choquet-simplex-certificate-surface}
For every accepted $\ChoquetSimplexUp$ carrier $S=(K,V,P,B,R,E,H,C,Q,N)$, the local $\NameCert_{\ChoquetSimplexUp}$ surface consists exactly of compact-source admission through $K$, convex-carrier admission through $V$, probability-window admission through $P$, boundary-window exposure through $B$, Radon readback through $R$, locally convex exposure through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $Q$, and local naming through $N$. Any probability-boundary consumer must read those rows before using the simplex packet.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:choquet-simplex-carrier-surface}. The compact and convex rows are the only source rows, so a consumer first reads compact observations through $K$ and affine convex data through $V$.

The probability and boundary rows are displayed next. The row $P$ records finite probability-window requests, while $B$ records the inspected boundary candidates as finite observations rather than selected extreme points.

The Radon and locally convex rows are downstream. The readback row $R$ is available only after the compact, convex, probability, and boundary rows are visible, and $E$ exposes the locally convex observation surface used by affine consumers.

Finally the structural rows $H,C,Q,N$ transport, replay, source, and name exactly this route. Since no row stores representing-measure uniqueness, a selected barycentric measure, full extreme-point generation, quotient measure equality, countable choice, or ambient locally convex completeness, the listed rows exhaust the certificate surface.
\end{proof}

\falsifiablePrediction{Every $\ChoquetSimplexUp$ probability-boundary consumer reads compact rows, convex rows, finite probability windows, boundary observations, Radon readback, locally convex exposure, transport, replay, provenance, and local naming before it exports a simplex-facing certificate.}

\independenceWitness{compact, convexset, radon-measure, locally-convex-space: $\ChoquetSimplexUp$ is not bijective to any listed sibling because it jointly carries compact source rows, convex affine rows, probability windows, boundary observations, Radon readback, locally convex exposure, transport, replay, provenance, and local NameCert naming while refusing selected representation data.}

\closureat{ChoquetSimplexUp}{seedStr}

\begin{closurestatus}{\ChoquetSimplexUp}
  \constructivestory{A $\ChoquetSimplexUp$ packet is generated from compact source rows, convex affine rows, finite probability-window rows, boundary observations, Radon readback, locally convex exposure, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:choquet-simplex-carrier-surface} and \autoref{thm:choquet-simplex-certificate-surface} seed the compact-convex probability-boundary certificate over compact, convex, Radon, locally convex, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close representing-measure uniqueness, full Krein--Milman generation, selected extreme points, quotient measure equality, countable choice, ambient locally convex completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source exposure, convex affine exposure, probability-window exactness, boundary-window readback, Radon handoff, locally convex exposure, transport, replay, provenance, and local naming for BEDC.Derived.ChoquetSimplexUp.}
\end{closurestatus}
