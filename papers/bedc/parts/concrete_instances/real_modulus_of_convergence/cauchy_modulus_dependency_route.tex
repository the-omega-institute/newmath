The Cauchy-modulus dependency route for $\RealModulusOfConvergenceUp$ is finite before any Real-facing consumer reads a convergence rate. The carrier consumes modulus rows from \autoref{ch:concrete-instances-regular-cauchy-modulus-extraction-namecert}, \autoref{ch:concrete-instances-bishop-cauchy-modulus-extraction-namecert}, and the Cauchy modulus surface of \autoref{ch:concrete-instances-cauchymodulus-namecert}, then routes the accepted tail data through the $\RegSeqRatUp$ readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}. Thus the convergence-rate row is not a detached selector: it is read as displayed Cauchy-modulus extraction data before the downstream $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert} or any Completion consumer can use it.
