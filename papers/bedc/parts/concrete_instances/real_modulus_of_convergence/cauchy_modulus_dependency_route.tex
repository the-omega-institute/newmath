The Cauchy-modulus dependency route for $\RealModulusOfConvergenceUp$ is finite before any Real-facing consumer reads a convergence rate. The carrier consumes modulus rows from \autoref{ch:concrete-instances-regular-cauchy-modulus-extraction-namecert} and \autoref{ch:concrete-instances-bishop-cauchy-modulus-extraction-namecert}, then routes the accepted tail data through the $\RegSeqRatUp$ readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}. Thus the convergence-rate row is not a detached selector: it is read as displayed Cauchy-modulus extraction data before the downstream $\RealUp$ seal or Completion consumer can use it.
