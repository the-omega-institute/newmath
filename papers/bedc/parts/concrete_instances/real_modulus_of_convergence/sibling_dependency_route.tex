The sibling dependency route for $\RealModulusOfConvergenceUp$ is explicit: the carrier consumes $\CauchyConvergenceModulusUp$ tail schedules from \autoref{ch:concrete-instances-cauchy-convergence-modulus-namecert}, $\StreamNameUp$ finite windows from \autoref{ch:concrete-instances-streamname-namecert}, $\RegSeqRatUp$ readback from \autoref{ch:concrete-instances-regseqrat-namecert}, and terminal $\RealUp$ seal rows from \autoref{ch:concrete-instances-real-namecert}. In the L10 completion lattice, this places the modulus packet beside the Bishop criterion surface of \autoref{ch:concrete-instances-bishop-completion-criterion-namecert}: both routes expose finite schedules, finite windows, regular rational readback, and Real-facing seals before any completion consumer may read a convergence claim.
