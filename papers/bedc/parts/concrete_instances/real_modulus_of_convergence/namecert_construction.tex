\chapter{A Concrete Naming Certificate for $\RealModulusOfConvergenceUp$}
\label{ch:concrete-instances-real-modulus-of-convergence-namecert}
\origin{human}

Following Bishop and Bridges, the $\RealModulusOfConvergenceUp$ packet records the explicit convergence-modulus ledger used by the Real and Completion route. It sits over the dyadic tolerance surface of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite window service of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite NameCert ledger for modulus-controlled convergence; it is not a proof of Real completeness, not a quotient of streams, and not a choice principle.

The uniqueness-facing consumer is the regular Cauchy route of \autoref{ch:concrete-instances-regular-cauchy-uniqueness-namecert}, which reads the same dyadic tolerance, $\StreamNameUp$ window, $\RegSeqRatUp$ handoff, and $\RealUp$ seal rows before comparing representatives.

\begin{definition}[Real modulus of convergence carrier]
\label{def:real-modulus-of-convergence-carrier}
A $\RealModulusOfConvergenceUp$ carrier is a finite $\BHist$ packet
$$
R=(D,W,Q,M,E,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance row naming the dyadic precision request and the comparison ledger used to judge later approximants.
\item $W$ is the $\StreamNameUp$ finite-window row naming the observable segment of the stream family inspected for that tolerance.
\item $Q$ is the $\RegSeqRatUp$ readback row through which the windowed stream data becomes a regular rational sequence request.
\item $M$ is the monotone convergence-modulus ledger, recording that later tolerance requests read a weakly later finite window and never shorten the accepted tail.
\item $E$ is the $\RealUp$ seal handoff exposed only after $D,W,Q,M$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for tolerance, window, readback, modulus ledger, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a convergence request through $D,W,Q,M,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RealModulusOfConvergenceUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealModulusOfConvergenceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,W,Q,M,E,H,C,P,N$. It has no coordinate for quotient stream equality, a selected host limit, countable choice, host real equality, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Real modulus of convergence NameCert obligations]
\label{thm:real-modulus-of-convergence-namecert-obligations}
For every accepted $\RealModulusOfConvergenceUp$ carrier $R=(D,W,Q,M,E,H,C,P,N)$, the local $\NameCert_{\RealModulusOfConvergenceUp}$ surface consists exactly of carrier admission, dyadic tolerance exposure, finite-window readback, regular rational handoff, monotone modulus-ledger exactness, Real seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every Real or Completion consumer must read the displayed route
$$
D,W,Q,M \longmapsto E \longmapsto H,C,P,N
$$
before using the convergence modulus as evidence for a Real-facing seal.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-modulus-of-convergence-carrier}. The dyadic row $D$ supplies the tolerance request, and the window row $W$ is the only finite observation service available to answer it.

The regular rational handoff $Q$ is then read through the monotone ledger $M$. Monotonicity is a row condition inside $M$: a later tolerance request must move to a weakly later accepted window, so the packet cannot introduce a detached tail selector.

The Real seal row $E$ is downstream of $D,W,Q,M$. The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route, so no quotient equality of streams, selected host limit, countable-choice schedule, host real equality, or ambient completeness theorem can be exported by the local classifier.
\end{proof}

\begin{theorem}[Real modulus of convergence CauchyModulus handoff]
\label{thm:real-modulus-convergence-cauchy-modulus-handoff}
Every accepted $\RealModulusOfConvergenceUp$ read factors its modulus request through the displayed $\CauchyModulusUp$ row before it reaches the terminal Real-facing seal. The route is
$$
\CauchyModulusUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp
\longmapsto \RealUp
\longmapsto \hsame,\Cont,\Pkg,\NameCert .
$$
Thus every Real or Completion consumer sees the convergence request as Cauchy-modulus data, regular rational readback, dyadic tolerance accounting, the terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$. It exports no selected tail, quotient real equality, countable choice, or ambient Real completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-modulus-of-convergence-carrier}. The dyadic tolerance row $D$, finite-window row $W$, regular rational readback row $Q$, and monotone ledger $M$ are the displayed data from which the modulus request is read.

Use the existing Cauchy modulus dependency route. It identifies the request as a $\CauchyModulusUp$ handoff before the regular rational row $Q$ and the dyadic row $D$ are consumed by the Real-facing seal.

Apply \autoref{thm:real-modulus-of-convergence-namecert-obligations}. The terminal row $E$ is downstream of the displayed modulus, readback, and tolerance rows, and $H,C,P,N$ only transport, replay, source, and name that same route.

Since the carrier has no coordinate for a selected tail, quotient real equality, countable-choice schedule, or ambient Real completeness, the handoff cannot export any of those objects to a Real or Completion consumer.
\end{proof}

\begin{theorem}[Real modulus of convergence tail stability]
\label{thm:real-modulus-of-convergence-tail-stability}
Let $R=(D,W,Q,M,E,H,C,P,N)$ be an accepted $\RealModulusOfConvergenceUp$ packet. For any later dyadic tolerance request transported through the monotone ledger $M$, the corresponding read uses a weakly later finite $\StreamNameUp$ window, the same $\RegSeqRatUp$ handoff route, and the same downstream $\RealUp$ seal boundary before any Real or Completion consumer may use the convergence claim. The routed tail-stability surface is exactly
$$
D,W,Q,M \longmapsto E \longmapsto H,C,P,N .
$$
It contains no quotient stream equality, selected limit, countable-choice schedule, or ambient $\RealUp$ completeness coordinate.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-modulus-of-convergence-carrier}. The rows $D,W,Q$ expose the tolerance request, the finite stream window, and the regular rational handoff available before any Real-facing seal is consumed.

Use the monotonicity row inside $M$. A later dyadic tolerance request may only move the accepted read to a weakly later finite window, so the packet preserves the tail already admitted by the earlier request and never introduces a separate tail selector.

Finally route the resulting read through $E,H,C,P,N$. The seal row $E$ remains downstream of the displayed tolerance, window, readback, and modulus rows, while $H,C,P,N$ transport, replay, source, and name the same route. Since no carrier coordinate supplies quotient stream equality, selected limits, countable choice, or ambient Real completeness, those objects cannot be exported to the Real or Completion consumer.
\end{proof}

\begin{theorem}[Real modulus of convergence window composition]
\label{thm:real-modulus-of-convergence-window-composition}
For an accepted $\RealModulusOfConvergenceUp$ carrier $R=(D,W,Q,M,E,H,C,P,N)$, each convergence-modulus read composes the dyadic tolerance row $D$, the finite $\StreamNameUp$ window row $W$, and the $\RegSeqRatUp$ readback row $Q$ before any $\RealUp$ seal is consumed. The composed pre-seal route is
$$
D,W,Q \longmapsto M \longmapsto E \longmapsto H,C,P,N .
$$
It exports no quotient equality of streams and makes no choice of a host limit.
\end{theorem}
\begin{proof}
Read the carrier of \autoref{def:real-modulus-of-convergence-carrier}. The dyadic tolerance row $D$, finite-window row $W$, and regular rational readback row $Q$ are displayed before the monotone modulus ledger and before the Real-facing seal.

The composition step is local to those three rows. The tolerance request in $D$ determines the comparison scale, $W$ supplies only a finite observed window at that scale, and $Q$ turns that finite window into the regular rational readback consumed by the modulus ledger.

Only after this pre-seal read may the route pass through $M$ and then to $E,H,C,P,N$. Because the carrier has no coordinate for quotient stream equality or a selected host limit, the composed window route cannot identify stream representatives or choose a limit outside the displayed NameCert data.
\end{proof}

\begin{theorem}[Real modulus of convergence monotone-window refinement]
\label{thm:real-modulus-of-convergence-monotone-window-refinement}
Let $R=(D,W,Q,M,E,H,C,P,N)$ be an accepted $\RealModulusOfConvergenceUp$ carrier, and let two displayed dyadic tolerance requests be related by the monotone ledger $M$. The later request refines the earlier one only through a weakly later $\StreamNameUp$ window, preserves the same $\RegSeqRatUp$ readback route, and reaches the same $\RealUp$ seal boundary before any Real or Completion consumer may use the modulus schedule. The refinement route is
$$
D,W,Q,M \longmapsto E \longmapsto H,C,P,N .
$$
It exports no quotient stream, selected limit, countable-choice schedule, or ambient $\RealUp$ completeness datum.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-modulus-of-convergence-carrier}. The two tolerance requests are displayed through the same dyadic row $D$, so their comparison is available only through the packet's monotone ledger $M$.

Use tail stability for the later request and window composition for the resulting read. Theorem~\ref{thm:real-modulus-of-convergence-tail-stability} forces the later request to use a weakly later finite $\StreamNameUp$ window, while Theorem~\ref{thm:real-modulus-of-convergence-window-composition} keeps the dyadic row, the window row, and the $\RegSeqRatUp$ readback row on the same pre-seal route.

Finally route the refined read through $E,H,C,P,N$. The visible $\RealUp$ seal boundary is reached only after $D,W,Q,M$ have been displayed, and no carrier coordinate supplies quotient streams, selected limits, countable choice, or ambient Real completeness.
\end{proof}

\begin{theorem}[Real modulus RegSeqRat uniqueness handoff]
\label{thm:real-modulus-convergence-regseqrat-uniqueness-handoff}
Let $R=(D,W,Q,M,E,H,C,P,N)$ and $R'=(D',W',Q',M',E',H',C',P',N')$ be accepted $\RealModulusOfConvergenceUp$ packets. If the two packets expose the same $\DyadicRatCoreUp$ tolerance row, the same $\StreamNameUp$ finite-window row, the same $\RegSeqRatUp$ readback row, and the same monotone modulus ledger, then every $\RegSeqRatUp$ uniqueness consumer and every $\RealUp$ seal consumer reads the same finite convergence route
$$
D,W,Q,M \longmapsto E \longmapsto H,C,P,N .
$$
Equivalently, the uniqueness-facing consumer cannot distinguish $R$ from $R'$ except through the displayed handoff rows $D,W,Q,M$ and their downstream seal, transport, replay, provenance, and local naming rows. The handoff exports no quotient stream, selected limit, countable-choice schedule, host Real equality, or ambient $\RealUp$ completeness datum.
\end{theorem}
\begin{proof}
Project the two carriers through \autoref{def:real-modulus-of-convergence-carrier}. The hypotheses identify the tolerance row, finite-window row, regular rational readback row, and monotone ledger, so both packets present the same pre-seal route $D,W,Q,M$ to any uniqueness-facing consumer.

Apply \autoref{thm:real-modulus-of-convergence-tail-stability}. Later tolerance requests must use weakly later finite windows and preserve the same $\RegSeqRatUp$ handoff route, so the identified rows cannot split into two independent tails.

Apply \autoref{thm:real-modulus-of-convergence-window-composition} and \autoref{thm:real-modulus-of-convergence-monotone-window-refinement}. These results keep the dyadic tolerance, finite window, readback row, and monotone ledger on the same route before the Real-facing seal is reached.

The downstream rows $E,H,C,P,N$ are therefore read only as seal, transport, replay, provenance, and local naming for that route. Since the carrier excludes quotient streams, selected limits, countable choice, host Real equality, and ambient completeness, neither a $\RegSeqRatUp$ uniqueness consumer nor a $\RealUp$ seal consumer can read any further distinguishing datum.
\end{proof}

\begin{theorem}[Real modulus of convergence CauchyFilterLimit route]
\label{thm:real-modulus-of-convergence-cauchyfilterlimit-route}
Let $R=(D,W,Q,M,E,H,C,P,N)$ be an accepted $\RealModulusOfConvergenceUp$ packet. Every $\CauchyFilterLimitUp$ consumer of $R$ reads the convergence-modulus schedule only through the shared finite route
$$
\DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
The modulus row supplies the filter-limit tolerance schedule, the finite window row supplies the basis inspected by the filter-limit request, and the regular rational readback row supplies the only pre-seal comparison. The route exports no quotient stream, selected host limit, countable-choice schedule, host Real equality, or ambient $\RealUp$ completeness datum.
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:real-modulus-of-convergence-namecert-obligations}. Carrier admission exposes the dyadic tolerance row $D$, finite window row $W$, regular rational readback row $Q$, monotone modulus ledger $M$, Real seal $E$, and structural rows $H,C,P,N$.

Apply Theorem~\ref{thm:real-modulus-convergence-cauchy-modulus-handoff}. The modulus row is first consumed as the tolerance schedule for the filter-limit request, so the request cannot start from a selected host limit or from ambient completeness.

Use Theorem~\ref{thm:real-modulus-of-convergence-tail-stability} and Theorem~\ref{thm:real-modulus-of-convergence-monotone-window-refinement}. These keep the filter-limit tolerance schedule on a weakly later $\StreamNameUp$ window while preserving the same $\RegSeqRatUp$ readback and $\DyadicRatCoreUp$ ledger.

Finally apply Theorem~\ref{thm:real-modulus-convergence-regseqrat-uniqueness-handoff} and the regular seal handoff of \autoref{thm:cauchy-filter-limit-regular-seal-handoff}. The downstream consumer reaches the $\CauchyFilterLimitUp$ seal only through the same $\RealUp$ boundary and structural rows, and the carrier contains no quotient stream, selected host limit, countable-choice schedule, host Real equality, or ambient completeness coordinate.
\end{proof}

\begin{theorem}[Real modulus of convergence tail comparison route]
\label{thm:real-modulus-of-convergence-tail-comparison-route}
Let $R=(D,W,Q,M,E,H,C,P,N)$ and $R'=(D',W',Q',M',E',H',C',P',N')$ be accepted $\RealModulusOfConvergenceUp$ packets. Suppose their dyadic tolerance rows and monotone ledgers compare through the same $\CauchyModulusUp$ request, and suppose the later-tail comparison is read only after the displayed $\StreamNameUp$ window and $\RegSeqRatUp$ handoff rows are exposed. Then every later-tail comparison between $R$ and $R'$ routes through
$$
D,W,Q,M \longmapsto E \longmapsto H,C,P,N .
$$
The comparison consumes $\CauchyModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ in that order, and it exports no quotient stream equality, selected limit, countable-choice schedule, host Real equality, or ambient $\RealUp$ completeness datum.
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:real-modulus-convergence-cauchy-modulus-handoff}. The shared tolerance and ledger comparison must first be read as a $\CauchyModulusUp$ request before the regular rational row or Real-facing seal can be consumed.

Apply Theorem~\ref{thm:real-modulus-of-convergence-tail-stability}. Once the request is later-tail-facing, the read may only move to weakly later finite $\StreamNameUp$ windows while preserving the same $\RegSeqRatUp$ handoff route.

Apply Theorem~\ref{thm:real-modulus-of-convergence-monotone-window-refinement}. The monotone ledgers keep the dyadic tolerance, finite window, readback, and modulus rows on one route before the Real-facing seal is reached.

Finally apply Theorem~\ref{thm:real-modulus-convergence-regseqrat-uniqueness-handoff}. The identified pre-seal rows force every uniqueness-facing and Real-facing consumer to read the same downstream seal, transport, replay, provenance, and local naming rows. Since the carrier contains none of quotient streams, selected limits, countable choice, host Real equality, or ambient completeness, the later-tail comparison cannot export any such datum.
\end{proof}

\begin{theorem}[Real modulus of convergence Cauchy-tail exhaustion]
\label{thm:real-modulus-of-convergence-cauchy-tail-exhaustion}
Let $R=(D,W,Q,M,E,H,C,P,N)$ be an accepted $\RealModulusOfConvergenceUp$ packet. Every later-tail comparison accepted by $R$ is exhausted by the $\CauchyModulusUp$ request, the $\StreamNameUp$ finite window, the $\RegSeqRatUp$ readback, the $\DyadicRatCoreUp$ tolerance ledger, the $\RealUp$ seal, and the structural rows $H,C,P,N$. No quotient stream equality, selected limit, countable-choice schedule, host Real equality, ambient $\RealUp$ completeness, or extra metric-completion row is exported by the comparison.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-modulus-of-convergence-carrier}. The only pre-seal rows available to a later-tail comparison are the dyadic tolerance row $D$, finite-window row $W$, regular rational readback row $Q$, and monotone ledger $M$.

Apply Theorem~\ref{thm:real-modulus-convergence-cauchy-modulus-handoff}. The comparison must first be read as a $\CauchyModulusUp$ request, so it cannot begin from a selected limit or from an ambient Real equality.

Use Theorem~\ref{thm:real-modulus-of-convergence-tail-stability}. Later requests move only to weakly later $\StreamNameUp$ windows and preserve the same $\RegSeqRatUp$ handoff route.

Apply Theorem~\ref{thm:real-modulus-of-convergence-window-composition} and Theorem~\ref{thm:real-modulus-of-convergence-monotone-window-refinement}. These keep the dyadic tolerance ledger, finite window, readback row, and monotone comparison on one displayed route before the $\RealUp$ seal is consumed.

Finally apply Theorem~\ref{thm:real-modulus-of-convergence-tail-comparison-route}. The downstream rows $E,H,C,P,N$ supply only the Real-facing seal, transport, replay, provenance, and local naming for that route. Since no carrier coordinate records quotient streams, selected limits, countable choice, host Real equality, ambient completeness, or a separate metric-completion row, the accepted later-tail comparison is exhausted by the stated data.
\end{proof}

\begin{theorem}[Real modulus of convergence tail dominance]
\label{thm:real-modulus-of-convergence-tail-dominance}
Let $R=(D,W,Q,M,E,H,C,P,N)$ be an accepted $\RealModulusOfConvergenceUp$ packet. If a later displayed request dominates an earlier convergence-modulus request through the monotone ledger $M$, then every Real or Completion consumer follows the same route
$$
\CauchyModulusUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp
$$
with a weakly later finite window. The dominant request adds no second choice schedule, no selected tail, no quotient stream equality, no host Real equality, and no ambient $\RealUp$ completeness datum.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-modulus-of-convergence-carrier}. The earlier and later requests are both displayed through the dyadic tolerance row $D$ and compared only through the monotone ledger $M$.

Apply Theorem~\ref{thm:real-modulus-convergence-cauchy-modulus-handoff}. The request is read as $\CauchyModulusUp$ data before any regular rational handoff or Real-facing seal is consumed.

Use Theorem~\ref{thm:real-modulus-of-convergence-tail-stability} and Theorem~\ref{thm:real-modulus-of-convergence-monotone-window-refinement}. Since the later request dominates the earlier one, the accepted read moves only to a weakly later $\StreamNameUp$ window and preserves the same $\RegSeqRatUp$ handoff route.

Finally apply Theorem~\ref{thm:real-modulus-of-convergence-tail-comparison-route}. The downstream consumer reaches the same $\RealUp$ seal through $D,W,Q,M,E,H,C,P,N$. Because the carrier has no coordinate for a second choice schedule, selected tails, quotient stream equality, host Real equality, or ambient completeness, dominance cannot export any such datum.
\end{proof}

\falsifiablePrediction{Every accepted $\RealModulusOfConvergenceUp$ Real or Completion consumer factors through one displayed $\DyadicRatCoreUp$ tolerance row, one $\StreamNameUp$ finite-window row, one $\RegSeqRatUp$ readback row, one monotone convergence-modulus ledger, one $\RealUp$ seal handoff, componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ before it may use a convergence-modulus claim.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real: $\RealModulusOfConvergenceUp$ is not bijective to any listed sibling because the siblings separately supply tolerance comparisons, finite windows, regular rational readback, and terminal Real sealing, while this packet uniquely binds them to a monotone convergence-modulus ledger consumed by the L10 Real and Completion route.}

\closureat{RealModulusOfConvergenceUp}{seedStr}

\begin{closurestatus}{\RealModulusOfConvergenceUp}
  \constructivestory{A $\RealModulusOfConvergenceUp$ packet is generated from a DyadicRatCore tolerance row, a StreamName finite-window row, a RegSeqRat readback row, a monotone convergence-modulus ledger, a RealUp seal handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-modulus-of-convergence-carrier}, \autoref{thm:real-modulus-of-convergence-namecert-obligations}, \autoref{thm:real-modulus-of-convergence-tail-stability}, \autoref{thm:real-modulus-of-convergence-window-composition}, and \autoref{thm:real-modulus-of-convergence-monotone-window-refinement} seed the explicit convergence-modulus ledger over DyadicRatCore, StreamName, RegSeqRat, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Real completeness, quotient stream equality, selected host limits, countable choice, host real equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic tolerance exposure, finite-window readback, RegSeqRat handoff, monotone ledger exactness, Real seal handoff, structural transport, and ledger non-escape for BEDC.Derived.RealModulusOfConvergenceUp.}
\end{closurestatus}
