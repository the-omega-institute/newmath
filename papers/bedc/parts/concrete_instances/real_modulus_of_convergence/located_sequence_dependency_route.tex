\begin{theorem}[Real modulus located sequence link]
\label{thm:real-modulus-of-convergence-located-sequence-link}
Any $\RealModulusOfConvergenceUp$ read consumed by a located sequence or Cauchy-modulus chapter must factor through the displayed located Cauchy route of \autoref{thm:real-modulus-of-convergence-located-cauchy-route}, the existing Cauchy modulus dependency route, the regular Cauchy modulus route, and the RegSeqRat consumer route of \autoref{thm:real-modulus-of-convergence-regseqrat-consumer-route}. In particular, a consumer that reaches the located Cauchy sequence surface of \autoref{ch:concrete-instances-located-cauchy-sequence-namecert} must first read the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchyConvergenceModulusUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows already exposed by the $\RealModulusOfConvergenceUp$ carrier.
\end{theorem}
\begin{proof}
Project the accepted $\RealModulusOfConvergenceUp$ carrier from \autoref{def:real-modulus-of-convergence-carrier}. Its convergence-rate row is not available without the dyadic tolerance ledger, finite stream windows, regular rational readback, monotone modulus ledger, terminal Real seal, transport, replay, provenance, and local naming rows.

Apply the located Cauchy route. It places every completion-facing convergence-rate read after the Cauchy source, tail schedule, stream windows, regular readback, dyadic tolerance ledger, Real seal, and Bishop completion criterion surface.

Now use the Cauchy modulus dependency route and the regular Cauchy modulus route. These routes ensure that a Cauchy-modulus consumer reads the displayed modulus extraction rows before the Real-facing seal can treat the convergence rate as evidence.

Finally apply the RegSeqRat consumer route. A located sequence consumer, including the sibling surface of \autoref{ch:concrete-instances-located-cauchy-sequence-namecert}, therefore reaches the modulus only after the same finite RegSeqRat-to-Real path has been displayed, and the carrier exports no selected limit, quotient stream equality, countable-choice schedule, host Real equality, or ambient completeness coordinate.
\end{proof}
