The $\RealModulusOfConvergenceUp$ packet consumes its substantive route through finite stream windows, regular rational readback, dyadic tolerance rows, and the terminal real-name seal. In particular, \autoref{ch:concrete-instances-streamname-namecert} supplies the displayed observation windows, \autoref{ch:concrete-instances-regseqrat-namecert} supplies the regular rational readback attached to those windows, \autoref{ch:concrete-instances-dyadicratcore-namecert} supplies the positive tolerance ledger, and \autoref{ch:concrete-instances-real-namecert} supplies the bounded $\RealUp$ consumer boundary. The modulus chapter therefore reads convergence data from these sibling certificates before exposing any real-facing rate row.
