\begin{theorem}[Real modulus of convergence located Cauchy route]
\label{thm:real-modulus-of-convergence-located-cauchy-route}
Every $\RealModulusOfConvergenceUp$ consumer that reads a convergence-rate row must pass through the displayed located-Cauchy and completion-facing surfaces before the claim is visible to a completion consumer. The route reads the Cauchy source of \autoref{ch:concrete-instances-cauchy-namecert}, the tail schedule of \autoref{ch:concrete-instances-cauchy-convergence-modulus-namecert}, the finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}, and the Bishop completion criterion surface of \autoref{ch:concrete-instances-bishop-completion-criterion-namecert}. It exports no selected limit, quotient Cauchy name, countable-choice schedule, host real equality, or ambient completion theorem.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:real-modulus-of-convergence-carrier}. Its convergence-rate row is available only together with the dyadic tolerance ledger, finite stream window, regular rational handoff, monotone modulus ledger, Real seal, transport, replay, provenance, and local naming rows.

Use the sibling dependency route already displayed for $\RealModulusOfConvergenceUp$. It places the convergence-rate row after $\CauchyConvergenceModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, and the Cauchy source route, so the modulus cannot be read as an isolated local counter.

Finally compose with \autoref{thm:real-modulus-of-convergence-bishop-criterion-forward-link}. A Bishop completion consumer receives the same finite route before any completion-facing claim is visible, and the carrier has no row for selected limits, quotient names, countable choice, host real equality, or ambient completion.
\end{proof}
