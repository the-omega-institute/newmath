The regular-Cauchy modulus route for $\RealModulusOfConvergenceUp$ is read before any terminal Real seal is consumed: a convergence-modulus request first cites the substantive Cauchy modulus sibling of \autoref{ch:concrete-instances-cauchymodulus-namecert}, then passes through the RegSeqRat handoff of \autoref{ch:concrete-instances-regseqrat-namecert} as displayed regular-Cauchy readback. This dependency keeps the packet in the finite L10 lattice, so the later $\RealUp$ seal receives a routed modulus, stream window, and regular-rational witness rather than a detached host limit, quotient stream equality, countable-choice schedule, or ambient real-completeness premise.
