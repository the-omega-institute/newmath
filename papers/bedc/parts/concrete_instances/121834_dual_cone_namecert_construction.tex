\chapter{A Concrete Naming Certificate for $\DualConeUp$}
\label{ch:concrete-instances-dual-cone-namecert}
\origin{human}

Following Rockafellar's finite-dimensional convex analysis and the Boyd--Vandenberghe treatment of convex cones, the $\DualConeUp$ packet records the polar-support route for a displayed cone before any separation theorem or analytic closure principle is read. It sits over the convex membership rows of \autoref{ch:concrete-instances-convexset-namecert}, the vector-space boundary of \autoref{ch:concrete-instances-vecspace-namecert}, the linear functional rows of \autoref{ch:concrete-instances-linearmap-namecert}, the scalar seal of \autoref{ch:concrete-instances-real-namecert}, and the finite primal-dual packet of \autoref{ch:concrete-instances-fenchelduality-namecert}. The packet names the dual cone as finite support-inequality data, not as Hahn--Banach, strong separation, a host quotient space, or analytic closedness.

\begin{definition}[Dual-cone carrier]
\label{def:dual-cone-carrier}
A $\DualConeUp$ carrier is a finite $\BHist$ packet
$$
D=(V,C,L,R,F,H,K,P,N)
$$
whose rows are displayed as follows: $V$ is the $\VecSpaceUp$ source row, $C$ is the $\ConvexSetUp$ cone row inside $V$, $L$ is the $\LinearMapUp$ functional row, $R$ is the $\RealUp$ scalar comparison row for nonnegative support values, $F$ is the $\FenchelDualityUp$ provenance row that records the adjacent polar-support reading, $H$ is componentwise $\hsame$ transport for $V,C,L,R,F$, $K$ is displayed $\Cont$ replay, $P$ is $\Pkg$ provenance over $\DualConeUp$, $\ConvexSetUp$, $\VecSpaceUp$, $\LinearMapUp$, $\RealUp$, $\FenchelDualityUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, and $N$ is the local $\NameCert_{\DualConeUp}$ row. The classifier compares accepted packets only through these rows and has no coordinate for Hahn--Banach extension, strong separation, host quotient spaces, analytic closedness, or an ambient topological dual.
\end{definition}

\begin{theorem}[Dual-cone NameCert obligations]
\label{thm:dual-cone-namecert-obligations}
For every accepted $\DualConeUp$ carrier $D=(V,C,L,R,F,H,K,P,N)$, the local $\NameCert_{\DualConeUp}$ surface consists exactly of vector-space exposure through $V$, cone-membership exposure through $C$, linear-functional exposure through $L$, Real-valued support comparison through $R$, polar-support provenance through $F$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $K$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the rows of \autoref{def:dual-cone-carrier}. The vector, cone, functional, and scalar comparison data are the only mathematical rows stored by the packet.

The polar-support row $F$ is read only as provenance from the finite Fenchel-duality sibling. It does not supply a separating hyperplane theorem, a topological dual completion, or an analytic closure assertion.

The structural rows $H,K,P,N$ transport, replay, source, and name the same displayed data. Since no other coordinate is present, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Dual-cone polar-support route]
\label{thm:dual-cone-polar-support-route}
Every polar-support read of an accepted $\DualConeUp$ packet must first expose the $\ConvexSetUp$ cone row, the $\VecSpaceUp$ source row, the $\LinearMapUp$ functional row, the $\RealUp$ comparison row, and the $\FenchelDualityUp$ provenance row. The route may be consumed by finite primal-dual chapters, but it exports no Hahn--Banach theorem, no strong separation theorem, no host quotient space, no analytic closedness statement, and no ambient topological-dual object.
\end{theorem}
\begin{proof}
Read the carrier rows $C,V,L,R,F$ in order. The cone is inspected as displayed convex data in a displayed vector-space boundary, and a dual read is admitted only through a displayed linear functional with a Real-valued support comparison.

Now compare with \autoref{ch:concrete-instances-fenchelduality-namecert}. The Fenchel row records the finite primal-dual bridge that may consume the support comparison, while the present packet keeps the cone-dual route at the level of named rows.

The carrier has no row for extension, separation, quotienting, analytic closure, or a completed topological dual. Thus every consumer remains on the finite polar-support route.
\end{proof}

\falsifiablePrediction{Any accepted $\DualConeUp$ consumer that invokes Hahn--Banach, strong separation, host quotient spaces, analytic closedness, or an ambient topological dual before reading $V,C,L,R,F,H,K,P,N$ refutes the carrier surface of \autoref{def:dual-cone-carrier}.}

\independenceWitness{convexset, vecspace, linearmap, real, fenchelduality: $\DualConeUp$ is not bijective to any listed sibling because it jointly carries cone membership, vector-space source, linear-functional support, Real comparison, Fenchel polar-support provenance, transport, replay, provenance, and local naming rows.}

\closureat{DualConeUp}{seedStr}

\begin{closurestatus}{\DualConeUp}
  \constructivestory{A $\DualConeUp$ packet is generated from a VecSpace source row, a ConvexSet cone row, a LinearMap functional row, a Real support-comparison row, a FenchelDuality polar-support provenance row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dual-cone-carrier}, \autoref{thm:dual-cone-namecert-obligations}, and \autoref{thm:dual-cone-polar-support-route} seed the finite dual-cone NameCert surface over $V,C,L,R,F,H,K,P,N$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Hahn--Banach, strong separation, host quotient spaces, analytic closedness, ambient topological duality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, cone exposure, functional exposure, support comparison, polar-support provenance, transport, replay, provenance, and local naming for BEDC.Derived.DualConeUp.}
\end{closurestatus}
