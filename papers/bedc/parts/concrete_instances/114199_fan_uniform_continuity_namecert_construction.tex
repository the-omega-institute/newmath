\chapter{A Concrete Naming Certificate for $\FanUniformContinuityUp$}
\label{ch:concrete-instances-fan-uniform-continuity-namecert}
\origin{human}

Following Bishop and Bridges, the $\FanUniformContinuityUp$ packet records the constructive fan-based route from finite bars on Cantor space to uniform-modulus extraction. It consumes the Cantor-prefix surface of \autoref{ch:concrete-instances-cantorspace-namecert}, the finite fan row of \autoref{ch:concrete-instances-fantheorem-namecert}, the tolerance handoff of \autoref{ch:concrete-instances-uniformcontinuity-namecert}, the finite compact metric row of \autoref{ch:concrete-instances-compactmetric-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only displayed finite prefix windows, decidable bar coverage, modulus rows, transport, replay, provenance, and local naming; it does not claim open-cover compactness, a selected branch, countable choice, quotient function equality, or ambient real completeness.

\begin{definition}[FanUniformContinuity carrier]
\label{def:fan-uniform-continuity-carrier}
A $\FanUniformContinuityUp$ carrier is a finite $\BHist$ packet
$$
U=(C,F,T,M,K,E,H,R,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $C$ is the $\CantorSpaceUp$ finite-prefix row, including Boolean prefix observations and retained branch-window data.
\item $F$ is the $\FanTheoremUp$ finite fan row, recording the decidable bar classifier, finite bar-depth ledger, and witness window for the displayed binary spread.
\item $T$ is the target tolerance row for the $\UniformContinuityUp$ consumer, read as displayed precision data rather than as a host real predicate.
\item $M$ is the extracted modulus row, assigning one finite prefix depth sufficient for all retained Cantor histories at tolerance $T$.
\item $K$ is the $\CompactMetricUp$ finite-prefix coverage row used only as a displayed compact metric ledger for the Cantor-prefix surface.
\item $E$ is the terminal $\RealUp$ seal for real-valued reads that consume the same modulus route.
\item $H$ records componentwise $\hsame$ transport for the Cantor, fan, tolerance, modulus, compact metric, Real seal, replay, provenance, and local naming rows.
\item $R$ records displayed $\Cont$ replay of the finite route
$$
C,F,T,K \longmapsto M \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\FanUniformContinuityUp$, $\CantorSpaceUp$, $\FanTheoremUp$, $\UniformContinuityUp$, $\CompactMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FanUniformContinuityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $C,F,T,M,K,E,H,R,P,N$. It has no coordinate for open-cover compactness, selected infinite branches, choice of bar witnesses, quotient equality of functions, selected real limits, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[FanUniformContinuity NameCert obligations]
\label{thm:fan-uniform-continuity-namecert-obligations}
For every accepted $\FanUniformContinuityUp$ carrier $U=(C,F,T,M,K,E,H,R,P,N)$, the local $\NameCert_{\FanUniformContinuityUp}$ surface consists exactly of carrier admission, Cantor-prefix exposure through $C$, finite fan-bar exposure through $F$, tolerance accountability through $T$, modulus extraction through $M$, compact metric ledger exposure through $K$, Real-seal handoff through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $R$, $\Pkg$ provenance through $P$, and local naming through $N$.

Every $\UniformContinuityUp$ or real-valued consumer of the packet must first read the displayed Cantor prefix row, finite fan row, tolerance row, and compact metric ledger before the modulus row or Real seal is exposed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:fan-uniform-continuity-carrier}. The projection lists exactly the Cantor prefix, finite fan, tolerance, modulus, compact metric, Real seal, transport, replay, provenance, and local naming rows.

The fan-based step is finite. The $\FanTheoremUp$ row supplies the decidable bar classifier and the finite bar-depth ledger, while the $\CantorSpaceUp$ row supplies the prefix windows over which the bar is inspected.

The modulus step reads $T,K,M$. The tolerance row fixes the requested precision, the compact metric row records only finite-prefix coverage, and the modulus row is extracted from those displayed rows before any downstream $\UniformContinuityUp$ consumer can read it.

Finally $E,H,R,P,N$ seal, transport, replay, source, and name the same finite route. Since the packet has no coordinate for open-cover compactness, selected branches, countable choice, quotient equality of functions, selected real limits, or ambient completeness, the listed rows exhaust the NameCert obligations.
\end{proof}

\falsifiablePrediction{Every accepted $\FanUniformContinuityUp$ consumer read factors through displayed CantorSpace, FanTheorem, UniformContinuity, CompactMetric, Real, BHist, hsame, Cont, Pkg, and NameCert rows before a uniform-modulus or Real-facing observation is exposed.}

\independenceWitness{cantorspace, fantheorem, uniformcontinuity, compactmetric, real: $\FanUniformContinuityUp$ is not bijective to any listed sibling because it jointly carries Cantor prefix windows, finite fan-bar depth, tolerance accountability, compact metric finite coverage, modulus extraction, Real sealing, transport, replay, provenance, and local naming while refusing open-cover compactness, selected branches, countable choice, quotient function equality, selected real limits, and ambient completeness.}

\closureat{FanUniformContinuityUp}{seedStr}

\begin{closurestatus}{\FanUniformContinuityUp}
  \constructivestory{A $\FanUniformContinuityUp$ packet is generated from CantorSpace finite-prefix rows, FanTheorem finite bar rows, a UniformContinuity tolerance row, a CompactMetric finite-prefix coverage row, an extracted modulus row, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:fan-uniform-continuity-carrier} and \autoref{thm:fan-uniform-continuity-namecert-obligations} seed the finite fan-bar route from Cantor prefix inspection to uniform-modulus extraction and Real sealing.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, countable choice, selected infinite branches, quotient function equality, selected real limits, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Cantor-prefix exposure, finite fan-bar exposure, tolerance accountability, compact metric ledger exposure, modulus extraction, Real-seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.FanUniformContinuityUp.}
\end{closurestatus}
