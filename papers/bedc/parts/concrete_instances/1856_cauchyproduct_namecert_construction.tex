\chapter{A Concrete Naming Certificate for $\CauchyProductUp$}
\label{ch:concrete-instances-cauchyproduct-namecert}
\origin{human}

The $\CauchyProductUp$ interface names the finite product certificate for two regular Cauchy names. It expresses the Bishop--Bridges constructive product of regular Cauchy sequences as displayed $\NameCert$ rows over scheduled finite windows, dyadic observations, regular rational readback, and the $\RealUp$ seal, without adding quotient streams or an ambient completeness operation.

\begin{definition}[CauchyProduct NameCert source]
\label{def:cauchy-product-namecert-source}
A $\CauchyProductUp$ source is a finite $\mathsf{BHist}$ packet
$$
Z=(X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $X$ and $Y$ are the two $\RegularCauchyNameUp$ source rows;
\item $W_X$ and $W_Y$ are the paired $\StreamNameUp$ finite-window schedules for a shared precision request;
\item $M_X$ and $M_Y$ are the paired dyadic-radius rows bounding the two source windows;
\item $Q_X$ and $Q_Y$ are the $\DyadicRatCoreUp$ observation rows read from the scheduled windows;
\item $P$ is the pointwise product observation row formed from $Q_X$ and $Q_Y$ inside the displayed dyadic rational core;
\item $E$ is the $\RegSeqRatUp$ classifier row for the product observation and its finite radius ledger;
\item $H$ stores the displayed $\hsame$ transports for the two source names, both stream windows, both radius rows, both dyadic observations, the product observation, and the regular rational readback;
\item $C$ stores the displayed $\Cont$ routes for reading the source windows, transporting the dyadic product observation, exposing the regular rational classifier, and handing the result to the real seal;
\item $L$ is the finite ledger recording the paired input radii, the product-radius row, the $\RealUp$ seal handoff, and the $\Pkg$ provenance over $\RegularCauchyNameUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\CauchyProductUp}$ row.
\end{enumerate}
The packet stores a finite product construction. It contains no quotient stream, host multiplication primitive, selected limit, trichotomy row, independent real carrier, or ambient completeness coordinate.
\end{definition}

\begin{theorem}[CauchyProduct NameCert obligations]
\label{thm:cauchy-product-namecert-obligations}
The local $\NameCert_{\CauchyProductUp}$ surface for the source packet of \autoref{def:cauchy-product-namecert-source} consists exactly of carrier admission, paired $\RegularCauchyNameUp$ source admission, paired $\StreamNameUp$ window scheduling, dyadic-radius routing, $\DyadicRatCoreUp$ product observation, pointwise $\RegSeqRatUp$ classifier exposure, $\RealUp$ seal handoff, displayed $\hsame$ and $\Cont$ transport, package provenance, finite ledger, and the local naming row.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_namecert\_obligations}
\begin{proof}
Carrier admission is projection from the displayed packet $Z$. Its coordinates list the two regular Cauchy sources, their paired stream windows, the radius rows, dyadic observations, product observation, readback classifier, transport rows, continuation routes, ledger, and local certificate.

The analytic product step is finite. The two window reads expose $Q_X$ and $Q_Y$ inside $\DyadicRatCoreUp$, and the product observation row $P$ is the only row that combines them. The classifier row $E$ then reads this product observation with the radius information stored in the ledger.

The remaining obligations are provenance obligations. The rows $H,C,L,N$ carry transport, continuation, package, seal handoff, and local naming data, so the certificate has no source from which to read host multiplication, quotient equality, selected limits, trichotomy, or completeness.
\end{proof}

\begin{theorem}[CauchyProduct StreamName handoff]
\label{thm:cauchy-product-streamname-handoff}
Every accepted $\CauchyProductUp$ packet routes a product read through the paired $\StreamNameUp$ windows $W_X,W_Y$ before exposing the $\DyadicRatCoreUp$ observations $Q_X,Q_Y$, the dyadic-radius rows $M_X,M_Y$, the product observation $P$, and the pointwise $\RegSeqRatUp$ classifier row $E$. No consumer can read a product row without first reading those finite schedules and their displayed provenance.
\end{theorem}
\begin{proof}
Start with the two source rows $X$ and $Y$. Each is read only through its displayed $\RegularCauchyNameUp$ source row and the matching finite $\StreamNameUp$ schedule.

The dyadic product row is downstream of the schedules. Once $W_X$ and $W_Y$ have exposed $Q_X,Q_Y$ and $M_X,M_Y$, the row $P$ records the finite dyadic product observation and $E$ repacks it as a pointwise regular rational classifier.

The transport and continuation rows preserve this order. Since $H,C,L,N$ are attached to the same paired windows, a consumer has no alternate route to a host sequence, quotient product, selected limit, or ambient real operation.
\end{proof}

\begin{theorem}[CauchyProduct Real seal boundary]
\label{thm:cauchy-product-real-seal-boundary}
Any $\RealUp$ consumer of an accepted $\CauchyProductUp$ packet reads only the $\RegSeqRatUp$ classifier row $E$ built from the paired $\StreamNameUp$ windows, dyadic-radius rows, $\DyadicRatCoreUp$ product observation, displayed $\hsame$ transports, $\Cont$ routes, finite ledger, package provenance, and local $\NameCert_{\CauchyProductUp}$ row. The seal boundary exports no RealUp completeness theorem, quotient stream, selected limit, trichotomy row, independent real carrier, or host multiplication primitive.
\end{theorem}
\begin{proof}
Factor the real-facing read through the pointwise regular classifier. By \autoref{thm:cauchy-product-streamname-handoff}, the classifier is already downstream of the two finite stream windows and their dyadic product observation.

The real seal is recorded in the ledger row rather than supplied by an ambient carrier. Thus a $\RealUp$ consumer receives $E$ together with $H,C,L,N$, which are exactly the displayed transport, continuation, ledger, package, and naming rows.

No other coordinate in \autoref{def:cauchy-product-namecert-source} can supply completeness, quotient equality, a selected limit, trichotomy, or host multiplication. The real-facing boundary is therefore exhausted by the finite product packet.
\end{proof}

\begin{theorem}[CauchyProduct ledger exhaustion]
\label{thm:cauchy-product-ledger-exhaustion}
For every accepted $\CauchyProductUp$ packet, every consumer route is exhausted by the rows named in \autoref{def:cauchy-product-namecert-source} together with the obligations of \autoref{thm:cauchy-product-namecert-obligations}, the $\StreamNameUp$ handoff of \autoref{thm:cauchy-product-streamname-handoff}, and the $\RealUp$ seal boundary of \autoref{thm:cauchy-product-real-seal-boundary}. The ledger contains exactly the paired input radii, product-radius row, regular rational readback route, seal handoff, displayed transports, continuations, package provenance, and local certificate.
\end{theorem}
\begin{proof}
Inspect the source packet. Its only product-facing data are the two regular names, paired windows, paired radius rows, dyadic observations, product observation, classifier row, transports, continuations, ledger, and local naming row.

The NameCert obligations attach every accepted read to those displayed rows. The StreamName handoff fixes the order from source windows to dyadic product observation to regular classifier, while the Real seal boundary places each real-facing read downstream of the same classifier and ledger.

Therefore the listed rows exhaust the certificate. A route seeking quotient streams, host multiplication, selected limits, trichotomy, an independent real carrier, or ambient completeness has no coordinate in the accepted packet.
\end{proof}

\begin{theorem}[CauchyProduct window product stability]
\label{thm:cauchy-product-window-product-stability}
For an accepted $\CauchyProductUp$ packet
$$
Z=(X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N),
$$
the paired $\StreamNameUp$ windows $W_X,W_Y$ stably determine the dyadic product observation $P$. Componentwise $\hsame$ transport of the two source names, both windows, both radius rows, and both dyadic observations preserves the product row and keeps every read inside the displayed window route.
\end{theorem}
\begin{proof}
Project the source packet of \autoref{def:cauchy-product-namecert-source}. The product row $P$ is downstream of the paired window rows and the dyadic observations $Q_X,Q_Y$.

Apply the StreamName handoff theorem. It forces a product read to pass through $W_X,W_Y$ before exposing $Q_X,Q_Y$, the radius rows, and the product observation.

The transport coordinate $H$ compares exactly those rows componentwise, and the continuation coordinate $C$ replays the displayed route from windows to product observation. Hence stable transport preserves the paired-window product route without adding host multiplication or a quotient product.
\end{proof}

\begin{theorem}[CauchyProduct radius ledger exactness]
\label{thm:cauchy-product-radius-ledger-exactness}
The product-radius ledger of an accepted $\CauchyProductUp$ packet is exact for the displayed route from paired input radii $M_X,M_Y$ through the dyadic product observation $P$ to the $\RegSeqRatUp$ classifier row $E$. The ledger records no independent radius oracle, selected limit, trichotomy row, host multiplication primitive, or ambient completeness witness.
\end{theorem}
\begin{proof}
Use window product stability to bind the product observation to the paired windows, input radii, and dyadic observations. This places the product-radius read downstream of $W_X,W_Y,M_X,M_Y,Q_X,Q_Y$.

The NameCert obligations identify $E$ as the pointwise regular rational classifier exposed from the product observation together with its finite radius information. The finite ledger $L$ records the paired input radii, product-radius row, seal handoff, and package provenance.

Finally apply ledger exhaustion. Since every consumer route is exhausted by the source packet and its displayed obligations, the radius ledger is exact for that route and contains no unlisted analytic primitive.
\end{proof}

\begin{theorem}[CauchyProduct Real seal consumer exhaustion]
\label{thm:cauchy-product-real-seal-consumer-exhaustion}
Every $\RealUp$ consumer of an accepted $\CauchyProductUp$ packet factors through the paired windows, dyadic product observation, product-radius ledger, $\RegSeqRatUp$ classifier, displayed transport and continuation rows, finite ledger, package provenance, and local naming row. It has no access to host multiplication, quotient streams, selected limits, trichotomy, independent real carriers, or ambient completeness.
\end{theorem}
\begin{proof}
Start with the Real seal boundary. It places each real-facing read downstream of the classifier row $E$, which is built from the paired windows, dyadic observations, and displayed ledger.

Use radius ledger exactness to identify the classifier input with the product-radius route recorded by $L$. The transport and continuation rows $H,C$ are the only allowed movement from source windows to the product observation and then to the regular rational readback.

Apply ledger exhaustion at the end. The rows named in the source packet exhaust every consumer route, so the real seal cannot import host multiplication, quotient streams, selected limits, trichotomy, independent real carriers, or ambient completeness.
\end{proof}

\begin{theorem}[CauchyProduct paired-window closure]
\label{thm:cauchy-product-paired-window-closure}
For an accepted $\CauchyProductUp$ packet
$$
Z=(X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N),
$$
the two $\RegularCauchyNameUp$ source rows $X,Y$ close their product read only through the paired $\StreamNameUp$ windows $W_X,W_Y$. Those windows expose the $\DyadicRatCoreUp$ observations $Q_X,Q_Y$, the paired radius rows $M_X,M_Y$, the product observation $P$, the product-radius ledger in $L$, and the $\RegSeqRatUp$ classifier row $E$; no quotient stream, selected limit, trichotomy row, ambient completeness witness, or host multiplication primitive is admitted.
\end{theorem}
\begin{proof}
Begin with the StreamName handoff. It forces every product read from $X,Y$ to pass through the paired finite windows before the dyadic observations and radius rows are visible.

Use window product stability next. The product observation $P$ is stably determined by $W_X,W_Y,Q_X,Q_Y,M_X,M_Y$ under componentwise transport.

Finally apply radius ledger exactness and ledger exhaustion. The product-radius row and classifier exposure are exactly the rows recorded in $L$ and $E$, so the paired-window closure has no coordinate for quotient streams, selected limits, trichotomy, ambient completeness, or host multiplication.
\end{proof}

\begin{theorem}[CauchyProduct radius product regularity]
\label{thm:cauchy-product-radius-product-regularity}
For an accepted $\CauchyProductUp$ packet, the product-radius ledger turns the paired $\RegularCauchyNameUp$ source windows, $\DyadicRatCoreUp$ observations, and product observation $P$ into the displayed $\RegSeqRatUp$ classifier row $E$. The regularity evidence is exactly the finite radius route recorded by $L$ and transported by $H,C$; it is not a host multiplication theorem, quotient stream construction, selected limit, trichotomy decision, or ambient completeness argument.
\end{theorem}
\begin{proof}
Use radius ledger exactness to bind the product-radius route to the displayed path from $M_X,M_Y$ and $Q_X,Q_Y$ through $P$ to $E$.

The paired-window closure identifies the source of those rows with the two regular Cauchy names and their finite windows. Window product stability keeps the product observation invariant under the displayed transport rows.

Ledger exhaustion then prevents any further regularity source. The regular sequence classifier is therefore certified by the finite radius ledger alone, not by host multiplication, quotient streams, selected limits, trichotomy, or completeness.
\end{proof}

\begin{theorem}[CauchyProduct Real multiplication seal handoff]
\label{thm:cauchy-product-real-multiplication-seal-handoff}
Every $\RealUp$ handoff of an accepted $\CauchyProductUp$ packet reads multiplication only as the sealed $\RegSeqRatUp$ classifier row $E$ built from paired $\RegularCauchyNameUp$ windows, $\DyadicRatCoreUp$ observations, the product-radius ledger, displayed $\hsame$ transports, $\Cont$ routes, package provenance, and local $\NameCert_{\CauchyProductUp}$ evidence. The handoff exports no host multiplication primitive, quotient stream, selected limit, trichotomy row, independent real carrier, or ambient completeness witness.
\end{theorem}
\begin{proof}
Start with Real seal consumer exhaustion. It already factors every real-facing read through the paired windows, product observation, radius ledger, classifier row, transports, continuations, provenance, and local naming data.

Apply radius product regularity to identify the classifier input. The row $E$ is supplied by the finite product-radius ledger over the dyadic observations and product row, not by an ambient multiplication operation.

Use the Real seal boundary at the end. Since the seal sees only the classifier and its finite ledger, the multiplication handoff cannot expose host multiplication, quotient streams, selected limits, trichotomy, independent real carriers, or completeness.
\end{proof}

\begin{theorem}[CauchyProduct product-observation seal obligations]
\label{thm:cauchy-product-product-observation-seal-obligations}
For an accepted $\CauchyProductUp$ packet, the paired $\RegularCauchyNameUp$ windows, paired $\StreamNameUp$ schedules, dyadic-radius rows, $\DyadicRatCoreUp$ product observation $P$, $\RegSeqRatUp$ classifier $E$, $\RealUp$ seal handoff, displayed $\hsame$ transports, $\Cont$ routes, finite ledger, $\Pkg$ provenance, and local $\NameCert_{\CauchyProductUp}$ row jointly supply the seed-to-obligation package for product observation. The package admits no host multiplication primitive, quotient stream, selected limit, trichotomy row, independent real carrier, or ambient completeness witness.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations. They expose the carrier, paired regular Cauchy sources, finite stream schedules, dyadic-radius routing, product observation $P$, regular rational classifier $E$, seal handoff, transport rows, continuation rows, package provenance, ledger, and local naming row.

Use the StreamName handoff to bind the order of observation. A product read must pass through $W_X,W_Y$ before $Q_X,Q_Y$, the radius rows, $P$, and then $E$ are visible.

The radius ledger exactness theorem identifies the product-radius row with that displayed route, and radius product regularity turns the route into the regular classifier row. Thus the observation supplied to the seal is the finite dyadic product packet, not an external multiplication rule.

Finally apply the Real multiplication seal handoff. Since every real-facing consumer reads multiplication only through $E$ and the recorded ledger, the listed rows form the obligation package and no unlisted analytic primitive can enter.
\end{proof}

\begin{theorem}[CauchyProduct RealCompleteness diagonal seal route]
\label{thm:cauchy-product-real-completeness-diagonal-seal-route}
Let an accepted $\CauchyProductUp$ packet be consumed by a $\RealCompletenessUp$ diagonal-limit packet through the real-facing seal route. The contribution from $\CauchyProductUp$ is only the finite product-observation surface
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N.
$$
These rows supply the paired $\StreamNameUp$ windows, dyadic observations, product-radius ledger, $\RegSeqRatUp$ classifier, componentwise $\hsame$ transports, $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert_{\CauchyProductUp}$ row. The diagonal seal route receives no host multiplication primitive, quotient stream, selected limit, trichotomy row, independent real carrier, or ambient completeness witness from the product packet.
\end{theorem}
\begin{proof}
Start with the StreamName handoff of \autoref{thm:cauchy-product-streamname-handoff}. It places the product observation downstream of the paired finite windows and their dyadic observation rows, so a diagonal-limit consumer first sees $W_X,W_Y,M_X,M_Y,Q_X,Q_Y$ before it can read $P$.

Next apply radius ledger exactness and radius product regularity, \autoref{thm:cauchy-product-radius-ledger-exactness} and \autoref{thm:cauchy-product-radius-product-regularity}. These identify the product-radius ledger as the only route from the dyadic product observation to the $\RegSeqRatUp$ classifier row $E$.

Use the product-observation seal obligations and Real multiplication seal handoff, \autoref{thm:cauchy-product-product-observation-seal-obligations} and \autoref{thm:cauchy-product-real-multiplication-seal-handoff}. They make the real-facing multiplication read consume only the displayed ledger, classifier, transports, continuations, package provenance, and local certificate.

Finally pull this finite surface back along the RealCompleteness scoped seal discipline of \autoref{thm:real-completeness-scoped-finite-seal-pullback}. Since the pullback receives only the displayed product rows, the diagonal seal route cannot acquire host multiplication, quotient streams, selected limits, trichotomy, an independent real carrier, or ambient completeness from $\CauchyProductUp$.
\end{proof}

\begin{theorem}[CauchyProduct constant left factor]
\label{thm:cauchy-product-constant-left-factor}
For an accepted $\CauchyProductUp$ packet whose left source row $X$ is read as a constant regular Cauchy name through the displayed $\RegularCauchyNameUp$ and $\StreamNameUp$ schedules, multiplication by that left factor is certified only by the existing product packet. The consumer route factors through the paired windows, dyadic product observation $P$, product-radius ledger, $\RegSeqRatUp$ classifier $E$, and $\RealUp$ multiplication seal handoff, and it introduces no host scalar multiplication primitive or quotient equality.
\end{theorem}
\begin{proof}
Begin with the StreamName handoff. Even when the left source is constant, the packet still reads $X$ through its displayed $\RegularCauchyNameUp$ row and the window $W_X$ before the dyadic observation $Q_X$ is available.

Use paired-window closure and radius product regularity. These force the product observation $P$ and the classifier row $E$ through the same finite route from $W_X,W_Y,M_X,M_Y,Q_X,Q_Y$ to the product-radius ledger.

Finally apply the Real multiplication seal handoff. The seal receives only $E$ with its recorded ledger and transports, so the constant left factor is a specialization of the displayed product packet rather than a host scalar operation.
\end{proof}

\begin{theorem}[CauchyProduct constant right factor]
\label{thm:cauchy-product-constant-right-factor}
For an accepted $\CauchyProductUp$ packet whose right source row $Y$ is read as a constant regular Cauchy name through the displayed $\RegularCauchyNameUp$ and $\StreamNameUp$ schedules, multiplication by that right factor is certified only by the existing product packet. The consumer route factors through the paired windows, dyadic product observation $P$, product-radius ledger, $\RegSeqRatUp$ classifier $E$, and $\RealUp$ multiplication seal handoff, and it introduces no host scalar multiplication primitive or quotient equality.
\end{theorem}
\begin{proof}
Start with the right source row. The StreamName handoff makes $W_Y$ the only route by which the constant right input exposes its dyadic observation $Q_Y$ and radius row $M_Y$.

Apply paired-window closure and radius product regularity. The right constant read is paired with the left window through the product observation $P$, and the classifier row $E$ is supplied by the finite product-radius ledger.

The Real multiplication seal handoff then exhausts the real-facing route. Since the seal sees only the displayed classifier and ledger, the right constant factor does not add host scalar multiplication, quotient streams, selected limits, or ambient completeness.
\end{proof}

\begin{theorem}[CauchyProduct bilinear tail window]
\label{thm:cauchy-product-bilinear-tail-window}
For an accepted $\CauchyProductUp$ packet and any shared tail read admitted by its paired $\StreamNameUp$ windows, the two one-sided constant-factor routes and the product route are all witnessed by the same finite window surface
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N.
$$
The bilinear tail window is therefore a NameCert obligation over the displayed product-radius ledger and $\RealUp$ seal handoff, not a field law, quotient equality, selected-limit argument, or ambient completeness principle.
\end{theorem}
\begin{proof}
First bind the shared tail read to the paired-window closure. The windows $W_X,W_Y$ expose the two source observations, their radius rows, the dyadic product observation, and the regular rational classifier through one finite route.

Next use the two constant-factor obligations. The left and right constant reads are not new operations; each is a specialization of the same displayed product packet and therefore uses the same product observation $P$ and classifier row $E$.

Finally use radius product regularity and the Real multiplication seal handoff. These identify the product-radius ledger as the only regularity evidence visible to the seal, so the bilinear tail window is exactly the finite NameCert surface and not an external algebraic law.
\end{proof}

\begin{theorem}[CauchyProduct constant-factor bilinear seal]
\label{thm:cauchy-product-constant-factor-bilinear-seal}
For an accepted $\CauchyProductUp$ packet, the left constant-factor route, the right constant-factor route, and the bilinear tail window are all specializations of the same finite product packet. Every $\RealUp$ seal-facing multiplication read therefore factors through the paired windows, dyadic product observation, product-radius ledger, $\RegSeqRatUp$ classifier, displayed $\hsame$ transports, $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row:
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N.
$$
The shared seal boundary asserts only this finite packet route. It asserts no host multiplication, quotient equality, selected limit, trichotomy, independent real carrier, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start with \autoref{thm:cauchy-product-constant-left-factor} and \autoref{thm:cauchy-product-constant-right-factor}. Each constant-factor read passes through the displayed $\RegularCauchyNameUp$ and $\StreamNameUp$ rows before exposing the dyadic observations and product-radius ledger.

Use \autoref{thm:cauchy-product-bilinear-tail-window}. The bilinear tail window states that the two one-sided constant-factor routes and the product route share the same finite surface $W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N$.

Apply radius product regularity to identify the product-radius ledger with the $\RegSeqRatUp$ classifier row $E$. Then apply the $\RealUp$ multiplication seal handoff to make the seal-facing consumer read only $E$ together with the recorded ledger, transports, continuations, provenance, and local naming row.

The assembled route is therefore one product-packet seal boundary. Since none of its rows is a host multiplication primitive, quotient stream, selected limit, trichotomy row, independent real carrier, or completeness witness, the listed exclusions remain in force.
\end{proof}

\begin{theorem}[CauchyProduct left-factor classifier congruence]
\label{thm:cauchy-product-left-factor-classifier-congruence}
For an accepted $\CauchyProductUp$ packet with product route
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N,
$$
replace the left $\RegularCauchyNameUp$ source by another source whose $\RegSeqRatUp$ classifier is equivalent through the displayed $\StreamNameUp$ handoff, $\hsame$ transports, $\Cont$ routes, package provenance, finite radius ledger, and local $\NameCert_{\CauchyProductUp}$ row. Then the product observation route descends to the same displayed classifier row $E$ and the same $\RealUp$ multiplication seal handoff. The descent introduces no host multiplication primitive, quotient stream, selected limit, trichotomy row, independent real carrier, or ambient completeness witness.
\end{theorem}
\begin{proof}
Begin with the left StreamName handoff. Classifier-equivalence of the replacement source is read only through the displayed window $W_X$, radius row $M_X$, dyadic observation $Q_X$, transport row $H$, continuation route $C$, provenance row $L$, and local naming row $N$.

Next pair this transported left read with the unchanged right window. Window product stability keeps the dyadic product observation $P$ on the same finite route, so the product packet has no additional coordinate from which a host product could be read.

Finally apply radius ledger exactness, radius product regularity, and the Real multiplication seal handoff. These identify the classifier row as the same $E$ and make the real-facing seal consume only the recorded ledger, so the left-factor replacement is a classifier descent inside the packet surface.
\end{proof}

\begin{theorem}[CauchyProduct right-factor classifier congruence]
\label{thm:cauchy-product-right-factor-classifier-congruence}
For an accepted $\CauchyProductUp$ packet with product route
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N,
$$
replace the right $\RegularCauchyNameUp$ source by another source whose $\RegSeqRatUp$ classifier is equivalent through the displayed $\StreamNameUp$ handoff, $\hsame$ transports, $\Cont$ routes, package provenance, finite radius ledger, and local $\NameCert_{\CauchyProductUp}$ row. Then the product observation route descends to the same displayed classifier row $E$ and the same $\RealUp$ multiplication seal handoff. The descent introduces no host multiplication primitive, quotient stream, selected limit, trichotomy row, independent real carrier, or ambient completeness witness.
\end{theorem}
\begin{proof}
Start with the right StreamName handoff. The replacement source can expose its classifier only through $W_Y$, $M_Y$, $Q_Y$, the displayed transport and continuation rows, the package row, and the local naming evidence.

Now pair that transported right read with the unchanged left window. Window product stability preserves the dyadic product observation $P$ over the same finite product surface and leaves the packet with the original radius ledger.

Use radius ledger exactness and radius product regularity to recover the same regular rational classifier row $E$. The Real multiplication seal handoff then reads only that row and its ledger, excluding host multiplication, quotient streams, selected limits, trichotomy, independent real carriers, and ambient completeness.
\end{proof}

\begin{theorem}[CauchyProduct two-factor classifier descent]
\label{thm:cauchy-product-two-factor-classifier-descent}
For an accepted $\CauchyProductUp$ packet with product route
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N,
$$
simultaneously replace the left and right $\RegularCauchyNameUp$ sources by classifier-equivalent sources witnessed through the displayed $\StreamNameUp$ handoffs, $\hsame$ transports, $\Cont$ routes, package provenance, finite product-radius ledger, and local $\NameCert_{\CauchyProductUp}$ evidence. The paired replacement descends to the same $\RegSeqRatUp$ classifier row $E$ and therefore to the same $\RealUp$ multiplication seal handoff. The resulting multiplication surface contains no host multiplication primitive, quotient stream, selected limit, trichotomy row, independent real carrier, or ambient completeness witness.
\end{theorem}
\begin{proof}
First transport the two source reads separately. The left congruence supplies the route through $W_X,M_X,Q_X,H,C,L,N$, and the right congruence supplies the route through $W_Y,M_Y,Q_Y,H,C,L,N$.

Next recombine the transported reads at the product observation. Window product stability keeps $P$ on the displayed paired-window surface, while ledger exactness keeps the radius evidence tied to the same finite product-radius row.

It remains to pass from the product row to the real-facing consumer. Radius product regularity recovers the same $\RegSeqRatUp$ classifier row $E$, and the Real multiplication seal handoff reads only $E$ with its recorded ledger. Thus the simultaneous two-factor descent is a classifier-level handoff, not an appeal to host multiplication, quotient streams, selected limits, trichotomy, independent real carriers, or ambient completeness.
\end{proof}

\begin{theorem}[CauchyProduct factor-swap observation surface]
\label{thm:cauchy-product-factor-swap-observation-surface}
For an accepted $\CauchyProductUp$ packet with product route
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N,
$$
swapping the two $\RegularCauchyNameUp$ source rows swaps only the displayed source-window and radius-observation rows
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y.
$$
The dyadic product observation route $P$, classifier row $E$, $\hsame$ transports $H$, $\Cont$ continuations $C$, finite ledger $L$, and local $\NameCert_{\CauchyProductUp}$ row $N$ remain on the same product surface. The swapped read therefore preserves the $\RealUp$ multiplication seal handoff and introduces no host multiplication primitive, quotient stream, selected limit, trichotomy row, independent real carrier, or ambient completeness witness.
\end{theorem}
\begin{proof}
Begin with the two StreamName handoffs for the source rows. Swapping the source order exchanges the displayed finite windows and their radius-observation rows, so the only rows whose names change are $W_X,W_Y,M_X,M_Y,Q_X,Q_Y$.

Next use window product stability. The paired window route still reaches the same dyadic product observation $P$, because the product packet records the observation through the finite paired-window surface rather than through a host multiplication operation.

The radius ledger exactness theorem keeps the ledger $L$ tied to that same product observation route. Radius product regularity then recovers the same $\RegSeqRatUp$ classifier row $E$ from the recorded ledger.

Finally use the two-factor classifier descent together with the Real multiplication seal handoff. The transports $H$, continuations $C$, package provenance, and local naming row $N$ are carried along the displayed packet route, so the factor swap changes only the factor-side rows and not the public product observation surface or any forbidden ambient resource.
\end{proof}

\begin{theorem}[CauchyProduct public multiplication consumer boundary]
\label{thm:cauchy-product-multiplication-public-consumer-boundary}
Every public multiplication consumer supplied by the $\CauchyProductUp$ packet and read through the $\RealUp$ seal can inspect only the finite rows
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N.
$$
It cannot inspect host multiplication, quotient streams, selected limits, trichotomy rows, independent real carriers, or ambient completeness. Thus the public multiplication boundary is exactly the displayed NameCert product packet and not any external algebraic or order-theoretic resource.
\end{theorem}
\begin{proof}
Start from the Real multiplication seal handoff. A seal-facing consumer receives the product only through the recorded $\RegSeqRatUp$ classifier row $E$ and the finite ledger, not through a host product or an independently chosen real representative.

Trace that classifier row backward through the packet. Radius product regularity, radius ledger exactness, and the product-observation seal obligations force every access to $E$ to pass through $P$ and then through the paired window and dyadic observation rows $W_X,W_Y,M_X,M_Y,Q_X,Q_Y$.

The remaining rows are administrative rather than ambient. The transports $H$, continuations $C$, package provenance, ledger $L$, and local naming evidence $N$ exhaust the public consumer interface recorded by the product packet.

Since this exhaustion lists every row visible through the $\RealUp$ seal, there is no remaining coordinate from which a public consumer could read quotient streams, selected limits, trichotomy, an independent carrier, ambient completeness, or host multiplication.
\end{proof}

\begin{theorem}[CauchyProduct RealAlgOrder multiplication export]
\label{thm:cauchy-product-realalgorder-multiplication-export}
The source packet of \autoref{def:cauchy-product-namecert-source}, together with the factor-swap observation surface of \autoref{thm:cauchy-product-factor-swap-observation-surface} and the public multiplication consumer boundary of \autoref{thm:cauchy-product-multiplication-public-consumer-boundary}, supplies the RealAlgOrder-facing multiplication export for $\CauchyProductUp$. The export is the finite product packet
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N
$$
read through the $\RealUp$ seal, with factor congruence, factor swap, and public consumer exhaustion all internal to the displayed $\RegularCauchyNameUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\end{theorem}
\begin{proof}
First assemble the source packet. The carrier source and local NameCert obligations provide the two regular Cauchy source rows, paired StreamName windows, dyadic observations, product observation, classifier row, transports, continuations, ledger, provenance, and naming evidence.

Next add factor congruence and descent. The left-factor, right-factor, and two-factor classifier congruence theorems show that changing either source by an equivalent displayed classifier leaves the product route at the same classifier row $E$ and the same Real multiplication seal handoff.

The factor-swap surface records the symmetric product read visible to downstream algebra consumers. It preserves $P,E,H,C,L,N$ and swaps only the source-side window and radius-observation rows.

Finally use the public multiplication consumer boundary. Since every public consumer reads only the listed packet through the $\RealUp$ seal, the RealAlgOrder-facing export has exactly the finite multiplication surface required here and carries no host multiplication, quotient stream, selected limit, trichotomy row, independent real carrier, or ambient completeness witness.
\end{proof}

\begin{theorem}[CauchyProduct obligation closure]
\label{thm:cauchy-product-obligation-closure}
For every accepted $\CauchyProductUp$ packet, the carrier source of
\autoref{def:cauchy-product-namecert-source}, the StreamName handoff, the
$\RealUp$ seal boundary, ledger exhaustion, radius product regularity, and
product-observation seal obligations jointly supply the full
$\obligationClosure$ surface for $\CauchyProductUp$ over
$\RegularCauchyNameUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$,
$\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

This closure surface preserves the exclusions on quotient streams, selected
limits, trichotomy, host multiplication, independent real carriers, and ambient
completeness.
\end{theorem}
\begin{proof}
First collect the carrier source and NameCert obligations. They list the two
$\RegularCauchyNameUp$ sources, paired $\StreamNameUp$ windows, dyadic-radius
rows, $\DyadicRatCoreUp$ observations, product observation, $\RegSeqRatUp$
classifier, $\RealUp$ seal handoff, transport, continuation, ledger,
provenance, and local naming rows.

Next apply the StreamName handoff and Real seal boundary. These theorems fix
the route from the source names through paired finite windows to the dyadic
product observation and then to the real-facing classifier row.

Use ledger exhaustion and radius product regularity to identify the finite
regularity evidence. The product-radius ledger is the only route turning the
paired observations and product row into the displayed $\RegSeqRatUp$
classifier.

Finally apply the product-observation seal obligations. They assemble the same
rows into the product-observation package and exclude any coordinate for
quotient streams, selected limits, trichotomy, host multiplication, independent
real carriers, or ambient completeness. The named rows therefore supply the
full obligation-closure surface.
\end{proof}

\closureat{CauchyProductUp}{seedStr}
\closureat{CauchyProductUp}{obligationStr}
\closureat{CauchyProductUp}{scopedStr}

\begin{closurestatus}{\CauchyProductUp}
  \constructivestory{A $\CauchyProductUp$ packet is generated from two $\RegularCauchyNameUp$ source rows, paired $\StreamNameUp$ finite-window schedules, dyadic-radius rows, $\DyadicRatCoreUp$ observations, a pointwise product row, a $\RegSeqRatUp$ classifier, displayed $\hsame$ transports, $\Cont$ routes, a finite ledger, $\RealUp$ seal handoff, package provenance, and local $\NameCert$ evidence.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{thm:cauchy-product-obligation-closure} binds the product-observation surface to the displayed $\RegularCauchyNameUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ scope.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close RealUp completeness, quotient streams, selected limits, trichotomy, host multiplication as a primitive, independent real carriers, or bridge checking.}
  \upgradepath{Scoped closure requires binding the product-observation obligation surface to the displayed $\RegularCauchyNameUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
\end{closurestatus}
