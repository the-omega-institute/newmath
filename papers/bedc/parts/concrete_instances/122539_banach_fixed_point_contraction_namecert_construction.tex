\chapter{A Concrete Naming Certificate for $\BanachFixedPointContractionUp$}
\label{ch:concrete-instances-banach-fixed-point-contraction-namecert}
\origin{human}

Following Banach, the $\BanachFixedPointContractionUp$ packet records the contraction-mapping principle as finite BEDC NameCert data over a complete metric source, a displayed Lipschitz self-map, a Picard iterate stream, a regular rational Cauchy handoff, and a Real-facing endpoint boundary. It sits beside the complete metric source of \autoref{ch:concrete-instances-completemetric-namecert}, the metric comparison surface of \autoref{ch:concrete-instances-metric-namecert}, the Lipschitz map packet of \autoref{ch:concrete-instances-lipschitz-function-namecert}, the stream-name windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names a standard contraction fixed-point neighbour for completion and continuous-map consumers; it does not assert host fixed-point equality, quotient stream equality, selected limits, or ambient completeness outside the displayed $\CompleteMetricUp$ row.

\begin{definition}[Banach fixed-point contraction carrier]
\label{def:banach-fixed-point-contraction-carrier}
A $\BanachFixedPointContractionUp$ carrier is a finite $\BHist$ packet
$$
K=(M,d,L,S,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\CompleteMetricUp$ source row, exposing the admitted carrier, the completion boundary, and the finite metric-comparison service.
\item $d$ is the $\MetricUp$ distance row used to compare adjacent Picard iterates and later endpoint windows.
\item $L$ is the $\LipschitzMapUp$ self-map row with a displayed rational contraction ledger $0\le q<1$.
\item $S$ is the finite Picard schedule row, recording the inspected iterates $x_0,\ldots,x_n$ and the application rows $x_{k+1}=L(x_k)$.
\item $W$ is the $\StreamNameUp$ window row for the scheduled iterates.
\item $R$ is the $\RegSeqRatUp$ readback row generated from the contraction ledger and the finite stream windows.
\item $E$ is the $\RealUp$ endpoint seal boundary reached only after $R$ and the carried complete metric route are displayed.
\item $H$ records componentwise $\hsame$ transport for $M,d,L,S,W,R,E$ and the local certificate rows.
\item $C$ records displayed $\Cont$ replay for metric reads, Lipschitz-map application, Picard-window extension, regular rational readback, and endpoint sealing.
\item $P$ is the $\Pkg$ provenance over $\BanachFixedPointContractionUp$, $\CompleteMetricUp$, $\MetricUp$, $\LipschitzMapUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BanachFixedPointContractionUp}$ row.
\end{enumerate}
The classifier accepts a contraction fixed-point read only through these rows. It has no coordinate for an externally selected fixed point, host equality of endpoints, a quotient of Cauchy streams, a non-displayed completeness theorem, or countable choice.
\end{definition}

\begin{theorem}[Banach fixed-point contraction obligation surface]
\label{thm:banach-fixed-point-contraction-obligation-surface}
For every accepted $\BanachFixedPointContractionUp$ carrier $K=(M,d,L,S,W,R,E,H,C,P,N)$, the local $\NameCert_{\BanachFixedPointContractionUp}$ surface consists exactly of complete metric source visibility, metric comparison visibility, Lipschitz self-map visibility, rational contraction ledger visibility, finite Picard schedule visibility, StreamName window visibility, RegSeqRat readback visibility, Real endpoint seal nonescape, componentwise transport, displayed replay, package provenance, and local naming for the packet of \autoref{def:banach-fixed-point-contraction-carrier}.
\end{theorem}
\begin{proof}
Project the carrier. The analytic source rows are $M,d,L$, the iteration rows are $S,W$, the completion-facing rows are $R,E$, and the structural rows are $H,C,P,N$.

The contraction ledger belongs to $L$, so a consumer may spend a shrinkage bound only after the self-map row and metric row are visible. The finite Picard schedule $S$ records each inspected application step, while $W$ repacks those steps as stream windows.

The readback row $R$ and endpoint seal $E$ are downstream of that finite route. Since no excluded object is a coordinate of $K$, the listed rows exhaust the local naming surface.
\end{proof}

\begin{theorem}[Banach fixed-point contraction iterate Cauchy route]
\label{thm:banach-fixed-point-contraction-iterate-cauchy-route}
Every Cauchy read of an accepted $\BanachFixedPointContractionUp$ packet factors through the displayed route
$$
\CompleteMetricUp,\ \MetricUp,\ \LipschitzMapUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp .
$$
The finite Picard schedule first reads adjacent iterate distances through $d$ and the contraction ledger in $L$, then packages the inspected windows through $W$, then exposes the regular rational readback $R$, and only then reaches the endpoint seal $E$. The route does not export host convergence, a selected limit, quotient stream equality, or ambient Real completeness.
\end{theorem}
\begin{proof}
Start with the finite schedule $S$. Each adjacent row is an application of the displayed Lipschitz self-map $L$, and every distance comparison is read through the metric row $d$ over the complete metric source $M$.

The rational contraction ledger in $L$ supplies the tail estimate for the inspected Picard windows. Those windows are recorded by $W$ as a $\StreamNameUp$ row rather than as a completed infinite orbit.

The regular rational readback $R$ is therefore generated from displayed finite data, and the endpoint boundary $E$ is reached only after that readback and the carried complete metric route are visible. The absent host objects cannot be recovered from this route because the carrier has no coordinate for them.
\end{proof}

\begin{theorem}[Banach fixed-point contraction unique fixed-point boundary]
\label{thm:banach-fixed-point-contraction-unique-fixed-point-boundary}
If two accepted $\BanachFixedPointContractionUp$ carriers have the same displayed complete metric row, metric row, Lipschitz self-map row, rational contraction ledger, Picard-window readback, and structural transports, then their $\RealUp$ endpoint seal rows are classifier-determinate. This boundary is the BEDC fixed-point uniqueness surface; it is not a host equality statement about completed real points.
\end{theorem}
\begin{proof}
Compare the two carriers componentwise through their $H$ and $C$ rows. The shared rows $M,d,L,S,W,R$ force both endpoint routes to spend the same contraction ledger and the same regular rational readback.

The usual contraction estimate is used here only as a finite classifier pattern: a rational bound below one identifies two displayed endpoint routes after both are routed through the same Picard windows and completion handoff.

Thus the endpoint rows agree at the $\RealUp$ seal boundary seen by the BEDC classifier. Because the packet contains no host endpoint object, the conclusion cannot be read as external equality.
\end{proof}

\falsifiablePrediction{An accepted $\BanachFixedPointContractionUp$ consumer is refuted if it reads a fixed-point endpoint without displaying the complete metric source, metric comparison row, Lipschitz contraction ledger, Picard schedule, StreamName windows, RegSeqRat readback, transport, replay, provenance, and local NameCert rows.}

\independenceWitness{complete-metric, metric, lipschitz-map, streamname, regseqrat, real: $\BanachFixedPointContractionUp$ is not bijective to any listed sibling because it jointly carries a complete metric source, a contraction self-map, a Picard schedule, finite stream windows, a regular rational readback, a Real endpoint seal, transport, replay, provenance, and local naming.}

\closureat{BanachFixedPointContractionUp}{seedStr}

\begin{closurestatus}{\BanachFixedPointContractionUp}
  \constructivestory{A $\BanachFixedPointContractionUp$ packet is generated from a complete metric source row, metric comparison row, Lipschitz self-map row with rational contraction ledger, finite Picard schedule, StreamName windows, RegSeqRat readback, Real endpoint seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:banach-fixed-point-contraction-carrier}, \autoref{thm:banach-fixed-point-contraction-obligation-surface}, \autoref{thm:banach-fixed-point-contraction-iterate-cauchy-route}, and \autoref{thm:banach-fixed-point-contraction-unique-fixed-point-boundary} seed the finite contraction fixed-point carrier and its completion-facing endpoint boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, host fixed-point equality, selected limits, quotient stream equality, countable choice, or ambient completeness beyond the carried $\CompleteMetricUp$ row.}
  \upgradepath{Obligation closure requires checked carrier admission, contraction-ledger visibility, Picard-window Cauchy readback, endpoint seal nonescape, uniqueness-boundary determinacy, transport, replay, provenance, local naming, and TasteGate support for BEDC.Derived.BanachFixedPointContractionUp.}
\end{closurestatus}
