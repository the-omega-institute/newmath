\chapter{A Concrete Naming Certificate for $\ImplicitFunctionTheoremUp$}
\label{ch:concrete-instances-implicit-function-theorem-namecert}
\origin{human}

Following Krantz and Parks and Bishop--Bridges constructive analysis, the $\ImplicitFunctionTheoremUp$ packet records the finite route by which a local equation solve may be read from displayed derivative, invertibility, Newton, completion, and real-name data. It sits beside the Newton iteration route of \autoref{ch:concrete-instances-newtoniteration-namecert}, the derivative source of \autoref{ch:concrete-instances-derivative-namecert}, the real-name surface of \autoref{ch:concrete-instances-real-namecert}, the completion boundary of \autoref{ch:concrete-instances-completion-namecert}, and the metric context of \autoref{ch:concrete-instances-metric-namecert}. The chapter does not assert a host implicit-function theorem; it names the finite NameCert carrier a later constructive calculus consumer must inspect.

\begin{definition}[Implicit function theorem carrier]
\label{def:implicit-function-theorem-carrier}
An $\ImplicitFunctionTheoremUp$ carrier is a finite $\BHist$ packet
$$
T=(E,D,I,N,K,R,H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the equation packet, recording the finite source variables, target variable, base point, local equation value, and classifier for the read $F(x,y)=0$.
\item $D$ is the $\DerivativeUp$ row for the equation packet at the displayed base point, including the partial derivative history read by the target coordinate.
\item $I$ is the invertibility window, naming the displayed inverse witness, local radius, and tolerance budget in the same $\MetricSpaceUp$ context as $E$ and $D$.
\item $N$ is the $\NewtonIterationUp$ route, recording the correction step, residual budget, nonescape window, and finite stopping schedule used by the local solve.
\item $K$ is the $\CompletionUp$ and $\RealUp$ seal, requiring every limiting or real-facing read to pass through the displayed completion row before the Real name is exposed.
\item $R$ is the transport row from the solved Newton endpoint to the local graph read, keeping the equation packet, derivative row, invertibility window, Newton route, completion seal, and Real seal componentwise aligned.
\item $H$ records componentwise $\hsame$ transport for equation packets, derivative histories, invertibility witnesses, Newton schedules, Completion and Real seals, transport rows, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a local solve request through $E,D,I,N,K,R,H,P,L$.
\item $P$ is the $\Pkg$ provenance over $\ImplicitFunctionTheoremUp$, $\NewtonIterationUp$, $\DerivativeUp$, $\RealUp$, $\CompletionUp$, $\MetricSpaceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $L$ is the local $\NameCert_{\ImplicitFunctionTheoremUp}$ row binding the packet to the displayed route.
\end{enumerate}
The classifier compares accepted packets only through $E,D,I,N,K,R,H,C,P,L$. The carrier has no coordinate for a host implicit function, a selected germ, global inverse theorem, quotient function equality, ambient choice, or an untracked completeness principle.
\end{definition}
\leanstmt{BEDC.Derived.ImplicitFunctionTheoremUp}

\begin{theorem}[Implicit function theorem NameCert obligations]
\label{thm:implicit-function-theorem-namecert-obligations}
For every accepted $\ImplicitFunctionTheoremUp$ carrier
$$
T=(E,D,I,N,K,R,H,C,P,L),
$$
the local $\NameCert_{\ImplicitFunctionTheoremUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the displayed equation packet, derivative row, invertibility window, Newton route, Completion and Real seal, transport, replay, provenance, and local name;
\item derivative-invertibility compatibility, requiring $D$ and $I$ to display the same target-coordinate approximation inside the same $\MetricSpaceUp$ window;
\item Newton residual replay, requiring $N$ and $C$ to keep every correction step downstream of $E,D,I$ and inside the named nonescape window;
\item Completion and Real sealing, requiring $K$ to be the only route from the Newton residual schedule to a real-facing endpoint;
\item graph transport, requiring $R,H$ to preserve the equation packet, derivative row, invertibility window, Newton route, and seal rows componentwise;
\item provenance exactness, requiring $P$ and $L$ to name only the listed rows and no host implicit function theorem or selected germ.
\end{enumerate}
No local NameCert obligation is stored in an ambient function space or external completeness theorem.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:implicit-function-theorem-carrier}. The packet lists exactly the equation, derivative, invertibility, Newton, seal, transport, replay, provenance, and local naming rows.

The analytic obligations are internal to $D,I,N$. The derivative and invertibility rows name the same local approximation, while the Newton row records the displayed correction schedule and residual budget.

The endpoint obligations are downstream of that schedule. The Completion and Real seal row $K$ is reached only after the Newton route has been replayed, and $R,H$ transport the resulting local graph read without replacing it by a host solution object.

Finally the provenance and local naming rows bind the same finite route. Since the carrier contains no selected germ, quotient function equality, global inverse theorem, ambient choice, or untracked completeness coordinate, these rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Implicit function theorem Newton-completion handoff]
\label{thm:implicit-function-theorem-newton-completion-handoff}
Every $\ImplicitFunctionTheoremUp$ local-solve read factors through the displayed finite route
$$
\DerivativeUp,\MetricSpaceUp,\NewtonIterationUp,\CompletionUp,\RealUp,\BHist,\hsame,\Cont,\Pkg,\NameCert .
$$
In the notation of \autoref{def:implicit-function-theorem-carrier}, the consumer must open $E,D,I,N,K,R,H,C,P,L$ before it can export a local graph endpoint.
\end{theorem}
\begin{proof}
Project the accepted carrier. The local-solve request has no available coordinate except the equation packet, derivative row, invertibility window, Newton route, Completion and Real seal, transport, replay, provenance, and local naming rows.

The derivative and metric rows provide the local analytic source. The Newton route consumes that source through the invertibility window and records the finite correction schedule together with the residual nonescape budget.

The handoff to Completion and Real data is then forced through $K$. The transport and replay rows preserve the same displayed endpoint before provenance and local naming expose it as a local graph read, so no host implicit-function theorem or selected germ is read.
\end{proof}

\falsifiablePrediction{Every $\ImplicitFunctionTheoremUp$ local-solve read opens one equation packet, one Derivative row, one invertibility window in a MetricSpace context, one NewtonIteration route, one Completion and Real seal, graph transport, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming before exporting a graph endpoint.}

\independenceWitness{newtoniteration, derivative, real, completion, metricspace: $\ImplicitFunctionTheoremUp$ is not bijective to any listed sibling because it jointly carries equation data, derivative data, an invertibility window, Newton residual replay, Completion and Real sealing, transport, provenance, and local naming as one certificate.}

\closureat{ImplicitFunctionTheoremUp}{seedStr}

\begin{closurestatus}{\ImplicitFunctionTheoremUp}
  \constructivestory{An $\ImplicitFunctionTheoremUp$ packet is generated from an equation packet, derivative row, invertibility window, NewtonIteration route, Completion and Real seal, graph transport, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:implicit-function-theorem-carrier}, \autoref{thm:implicit-function-theorem-namecert-obligations}, and \autoref{thm:implicit-function-theorem-newton-completion-handoff} seed the finite implicit-function-theorem certificate over equation, derivative, invertibility, Newton, Completion, Real, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \leantarget{BEDC.Derived.ImplicitFunctionTheoremUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host implicit functions, selected germs, quotient function equality, global inverse theorem packages, ambient choice, full analytic existence, Lean theorem verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, derivative-invertibility compatibility, Newton residual replay, Completion and Real sealing, graph transport, provenance exactness, and local naming for BEDC.Derived.ImplicitFunctionTheoremUp.}
\end{closurestatus}
