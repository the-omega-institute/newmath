\chapter{A Concrete Naming Certificate for $\ShrinkingNestedIntervalUp$}
\label{ch:concrete-instances-shrinking-nested-interval-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\ShrinkingNestedIntervalUp$ packet records the finite interval-refinement route in which a displayed nested interval chain carries an explicit shrinking-diameter ledger before any $\RegSeqRatUp$ or $\RealUp$ consumer reads a center name. It sits after the dyadic tolerance arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the rational-density witness route of \autoref{ch:concrete-instances-rational-density-real-namecert}, the located refinement route of \autoref{ch:concrete-instances-located-interval-refinement-namecert}, and the finite nested-interval sibling \autoref{ch:concrete-instances-nestedinterval-namecert}. The packet names a constructive shrinking-diameter route; it is not a Heine--Borel theorem, not a quotient-real equality principle, and not an appeal to ambient $\RealUp$ completeness.

\begin{definition}[Shrinking nested interval carrier]\label{def:shrinking-nested-interval-carrier}
A $\ShrinkingNestedIntervalUp$ carrier is a finite $\BHist$ packet
$$
S=(I,D,W,R,E,Q,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the $\NestedIntervalUp$ source row, recording the retained finite interval chain and adjacent containment ledger.
\item $D$ is the $\DyadicRatCoreUp$ endpoint and diameter ledger attached to every visible interval in $I$.
\item $W$ is the $\StreamNameUp$ window row naming the finite stages inspected by the consumer.
\item $R$ is the $\RegSeqRatUp$ rational readback row for endpoints and midpoint candidates read from $W$ and $D$.
\item $E$ is the downstream $\RealUp$ seal reached only after $I,D,W,R$ have been displayed.
\item $Q$ is the $\RationalDensityRealUp$ witness row used when a rational center must be displayed inside the current interval.
\item $L$ is the $\LocatedIntervalRefinementUp$ row recording that each retained interval is a located refinement of the previous displayed interval.
\item $H$ records componentwise $\hsame$ transport for $I,D,W,R,E,Q,L$ and the structural rows.
\item $C$ records displayed $\Cont$ replay through the nested chain, dyadic diameter ledger, finite observation window, regular rational readback, rational-density witness, located refinement, terminal seal, transport, provenance, and local naming rows.
\item $P$ is the $\Pkg$ provenance over $\ShrinkingNestedIntervalUp$, $\NestedIntervalUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\RationalDensityRealUp$, $\LocatedIntervalRefinementUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\ShrinkingNestedIntervalUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,D,W,R,E,Q,L,H,C,P,N$. It has no coordinate for Heine--Borel compactness, trichotomy, quotient equality of real names, countable choice, selected infinite intersections, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Shrinking nested interval NameCert obligations]\label{thm:shrinking-nested-interval-namecert-obligations}
For every accepted $\ShrinkingNestedIntervalUp$ carrier
$$
S=(I,D,W,R,E,Q,L,H,C,P,N),
$$
the local $\NameCert_{\ShrinkingNestedIntervalUp}$ surface consists exactly of nested-interval source admission through $I$, dyadic endpoint and diameter accounting through $D$, finite observation scheduling through $W$, regular rational readback through $R$, terminal real sealing through $E$, rational center witnessing through $Q$, located refinement through $L$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:shrinking-nested-interval-carrier}. The accepted packet displays exactly the nested interval source row, dyadic endpoint and diameter ledger, finite observation window, regular rational readback, real seal, rational-density witness, located refinement row, and structural rows.

The interval data are read first. The row $I$ supplies the finite nested chain, while $D$ records the endpoint comparisons and the shrinking-diameter budget for each visible stage. The row $L$ keeps each retained interval tied to a located refinement rather than to an ambient interval operator.

The consumer-facing rows are downstream. The schedule $W$ determines which stages are inspected, $R$ reads their rational endpoint data, $Q$ supplies any rational center witness inside the displayed interval, and $E$ seals the resulting read as a $\RealUp$-facing boundary.

Finally $H,C,P,N$ preserve, replay, source, and name the same displayed route. Since no carrier row stores Heine--Borel compactness, trichotomy, quotient real equality, countable choice, a selected infinite intersection, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Shrinking nested interval Cauchy-center route]\label{thm:shrinking-nested-interval-cauchy-center-route}
Every $\RegSeqRatUp$ or $\RealUp$ consumer of an accepted $\ShrinkingNestedIntervalUp$ packet must factor through the finite route
$$
\NestedIntervalUp \longmapsto \DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
with the $\RationalDensityRealUp$ center witness and $\LocatedIntervalRefinementUp$ containment row still visible in the same packet. The exported object is a finite Cauchy-center ledger for shrinking displayed intervals; it exports no Heine--Borel compactness, no trichotomy, no quotient equality of real names, no countable choice, and no ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:shrinking-nested-interval-namecert-obligations}. A consumer cannot reach the regular rational readback row $R$ before the nested source $I$, dyadic diameter ledger $D$, and finite schedule $W$ have been displayed.

The rational center row $Q$ is tied to the same displayed interval data. It is read only as a rational-density witness inside a currently retained interval, while $L$ records the located refinement relation from that interval to the preceding window.

The real seal $E$ is therefore downstream of a finite shrinking-diameter route. Transport, replay, provenance, and local naming preserve that route as $H,C,P,N$, so no consumer can detach the center readback from the displayed nested interval, dyadic tolerance, stream-window, rational-readback, rational-density, and located-refinement rows.
\end{proof}

\falsifiablePrediction{A consumer that claims a $\ShrinkingNestedIntervalUp$ center read while omitting the $\NestedIntervalUp$ source row, $\DyadicRatCoreUp$ diameter ledger, $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\RealUp$ seal, $\RationalDensityRealUp$ witness, $\LocatedIntervalRefinementUp$ row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, or local $\NameCert$ row refutes the carrier of \autoref{def:shrinking-nested-interval-carrier}.}

\independenceWitness{nestedinterval, dyadicratcore, streamname, regseqrat, real, rationaldensityreal, locatedintervalrefinement: no listed sibling is bijective to $\ShrinkingNestedIntervalUp$ because this packet alone binds a displayed nested interval chain, a dyadic shrinking-diameter ledger, finite observation windows, regular rational readback, a terminal Real seal, a rational-density center witness, located refinement, transport, replay, provenance, and local naming as one interval-refinement certificate.}

\closureat{ShrinkingNestedIntervalUp}{seedStr}

\begin{closurestatus}{\ShrinkingNestedIntervalUp}
  \constructivestory{A $\ShrinkingNestedIntervalUp$ packet is generated from a NestedIntervalUp source row, DyadicRatCoreUp endpoint and diameter ledgers, StreamNameUp finite windows, RegSeqRatUp readback, a RealUp seal, RationalDensityRealUp center witnessing, LocatedIntervalRefinementUp containment, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:shrinking-nested-interval-carrier}, \autoref{thm:shrinking-nested-interval-namecert-obligations}, and \autoref{thm:shrinking-nested-interval-cauchy-center-route} seed the constructive shrinking-diameter interval-refinement route for L10 Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Heine--Borel compactness, trichotomy, quotient equality of real names, countable choice, selected infinite intersections, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, nested-interval source visibility, dyadic diameter accounting, finite observation scheduling, regular rational readback, rational-density center witnessing, located refinement, real seal routing, transport, replay, provenance, and local naming for BEDC.Derived.ShrinkingNestedIntervalUp.}
\end{closurestatus}
