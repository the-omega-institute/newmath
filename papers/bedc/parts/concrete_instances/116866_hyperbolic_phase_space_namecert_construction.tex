\chapter{A Concrete Naming Certificate for $\HyperbolicPhaseSpaceUp$}
\label{ch:concrete-instances-hyperbolic-phase-space-namecert}
\origin{ai}

The $\HyperbolicPhaseSpaceUp$ packet realizes the phase-space branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite reversible disk-phase transport, boundary refusal, and provenance rows. It sits after the disk carrier of \autoref{ch:concrete-instances-unitdisk-namecert}, the Poincare disk metric of \autoref{ch:concrete-instances-poincare-disk-metric-namecert}, the hyperbolic metric row of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, and the reversible transport route of \autoref{ch:concrete-instances-poincare-disk-phase-transport-namecert}. The packet names a finite phase-space carrier for observer-expansion reads; it does not complete the disk, select boundary representatives, identify a physical observer trajectory, or import a host symplectic phase space.

\begin{definition}[Hyperbolic phase-space carrier]
\label{def:hyperbolic-phase-space-carrier}
A $\HyperbolicPhaseSpaceUp$ carrier is a finite $\BHist$ packet
$$
S=(U,D,M,T,B,G,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ is the $\UnitDiskUp$ source row for the inspected disk observations.
\item $D$ is the $\PoincareDiskMetricUp$ metric row over the same displayed disk points.
\item $M$ is the $\HyperbolicMetricUp$ readback row used to compare phase displacement through hyperbolic distance.
\item $T$ is the $\PoincareDiskPhaseTransportUp$ reversible transport row.
\item $B$ is the boundary-refusal row, recording that interior-center or selected-boundary exits are unavailable to the packet.
\item $G$ is the observer-phase groupoid row: it records finite source, target, inverse, and composition windows for reversible phase moves without asserting a host groupoid of all disk paths.
\item $R$ is the route-stability row, preserving $U,D,M,T,B,G$ under the local classifier.
\item $H$ records componentwise $\hsame$ transport for disk, metric, transport, refusal, groupoid, stability, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a phase-space request through $U,D,M,T,B,G,R,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicPhaseSpaceUp$, $\UnitDiskUp$, $\PoincareDiskMetricUp$, $\HyperbolicMetricUp$, $\PoincareDiskPhaseTransportUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicPhaseSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $U,D,M,T,B,G,R,H,C,P,N$. It has no coordinate for completed disk geometry, selected ideal-boundary points, global geodesic flow, host symplectic form, physical cosmology, or observer substance.
\end{definition}

\begin{theorem}[Hyperbolic phase-space NameCert obligations]
\label{thm:hyperbolic-phase-space-namecert-obligations}
For every accepted $\HyperbolicPhaseSpaceUp$ carrier $S=(U,D,M,T,B,G,R,H,C,P,N)$, the local $\NameCert_{\HyperbolicPhaseSpaceUp}$ surface consists exactly of disk-coordinate admission, Poincare disk metric visibility, hyperbolic metric readback, reversible phase-transport routing, boundary-refusal recording, finite observer-phase groupoid windows, route stability, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-phase-space-carrier}. The accepted packet displays exactly $U,D,M,T,B,G,R,H,C,P,N$.

The disk and metric rows are read first. The rows $U,D,M$ keep the packet inside displayed disk observations and metric readbacks rather than a completed hyperbolic surface.

The reversible phase data are read next. The row $T$ supplies the phase transport, $B$ records the boundary refusal, and $G$ exposes only finite source, target, inverse, and composition windows for observer-phase movement.

Finally $R,H,C,P,N$ preserve, transport, replay, source, and name that same route. Since none of those rows contains completed disk geometry, selected ideal-boundary points, global geodesic flow, a host symplectic form, physical cosmology, or observer substance, the displayed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Hyperbolic phase-space boundary refusal stability]
\label{thm:hyperbolic-phase-space-boundary-refusal-stability}
Every phase-space consumer of an accepted $\HyperbolicPhaseSpaceUp$ packet must pass through
$$
\UnitDiskUp,\ \PoincareDiskMetricUp,\ \HyperbolicMetricUp
\longmapsto
\PoincareDiskPhaseTransportUp
\longmapsto
B,G,R,H,C,P,N .
$$
Thus reversible disk-phase transport and finite observer-phase groupoid windows remain available, while interior-center collapse, selected ideal-boundary representatives, completed disk geometry, and host phase-space structure remain outside the exported surface.
\end{theorem}
\begin{proof}
Begin with a phase-space read. The disk carrier $U$ supplies the inspected disk observations, and the metric rows $D,M$ provide the only distance readbacks available to the route.

Read the reversible phase row $T$. It transports the displayed phase data across the disk surface, but it does not create a selected boundary representative or a global path space.

Use the refusal and groupoid rows $B,G$. The refusal row blocks interior-center collapse and selected-boundary exits, while the groupoid row records only finite source, target, inverse, and composition windows.

The stability and structural rows $R,H,C,P,N$ preserve the same finite route. They therefore export reversible disk-phase transport and provenance without adding completed disk geometry, host symplectic data, physical cosmology, or observer substance.
\end{proof}

\falsifiablePrediction{An accepted $\HyperbolicPhaseSpaceUp$ consumer is refuted if it obtains reversible phase transport, boundary refusal, or observer-phase composition without displaying UnitDisk observations, PoincareDiskMetric and HyperbolicMetric reads, PoincareDiskPhaseTransport routing, componentwise transport, replay, provenance, and local naming.}

\independenceWitness{hyperbolic-metric, poincare-disk-metric, poincare-disk-phase-transport, observer-phase-groupoid: no listed sibling is bijective to $\HyperbolicPhaseSpaceUp$ because this packet jointly carries disk observations, two metric readback rows, reversible phase transport, boundary refusal, finite groupoid windows, route stability, componentwise transport, replay, provenance, and local naming.}

The proposed Lean TasteGate surface for this packet must include explicit $\mathsf{Nontrivial}\ \HyperbolicPhaseSpaceUp$ and $\mathsf{FieldFaithful}\ \HyperbolicPhaseSpaceUp$ instances. Those instances must keep $U,D,M,T,B,G,R,H,C,P,N$ as separate rows before any checked carrier-alignment theorem can serve as the public TasteGate realization.

\closureat{HyperbolicPhaseSpaceUp}{seedStr}

\begin{closurestatus}{\HyperbolicPhaseSpaceUp}
  \constructivestory{A $\HyperbolicPhaseSpaceUp$ packet is generated from UnitDisk observations, PoincareDiskMetric and HyperbolicMetric readbacks, PoincareDiskPhaseTransport rows, a boundary-refusal ledger, finite observer-phase groupoid windows, route stability, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-phase-space-carrier}, \autoref{thm:hyperbolic-phase-space-namecert-obligations}, and \autoref{thm:hyperbolic-phase-space-boundary-refusal-stability} seed the finite hyperbolic phase-space packet over disk observations, metric readbacks, reversible phase transport, boundary refusal, finite groupoid windows, route stability, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed disk geometry, selected ideal-boundary representatives, global geodesic flow, host symplectic structure, physical cosmology, observer-substance readings, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked disk admission, Poincare and hyperbolic metric visibility, reversible phase routing, boundary-refusal exactness, finite observer-phase groupoid windows, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicPhaseSpaceUp.}
\end{closurestatus}
