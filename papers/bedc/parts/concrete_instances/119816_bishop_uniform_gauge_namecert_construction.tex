\chapter{A Concrete Naming Certificate for $\BishopUniformGaugeUp$}
\label{ch:concrete-instances-bishop-uniform-gauge-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopUniformGaugeUp$ packet records the finite gauge-radius object used to pass from Bishop uniform-structure data to finite cover and tagged-partition consumers. It sits after the dyadic arithmetic surface of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name surface of \autoref{ch:concrete-instances-real-namecert}, the real uniform-structure surface of \autoref{ch:concrete-instances-real-uniform-structure-namecert}, the Cousin cover source of \autoref{ch:concrete-instances-cousin-cover-namecert}, and the tagged-partition consumer of \autoref{ch:concrete-instances-tagged-partition-namecert}. The packet names a finite radius gauge carrier only; it is not an open-cover compactness theorem, a selected limiting process, or an ambient completeness principle for $\RealUp$.

\begin{definition}[Bishop uniform gauge carrier]
\label{def:bishop-uniform-gauge-carrier}
A $\BishopUniformGaugeUp$ carrier is a finite $\BHist$ packet
$$
G=(D,R,U,Q,F,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, listing the dyadic radius codes and comparison tests inspected by the gauge.
\item $R$ is the $\RealUp$ source row, exposing the real-name endpoints or centers on which the gauge is read.
\item $U$ is the $\RealUniformStructureUp$ row, carrying entourage and uniform-neighborhood data in a Bishop-readable form.
\item $Q$ is the local gauge-radius row. For each displayed source point and entourage request, it stores a positive dyadic radius history admitted by the uniform-structure row.
\item $F$ is the $\CousinCoverUp$ handoff row, reading the displayed gauge radii as local cover cells without turning them into an open-cover compactness proof.
\item $T$ is the $\TaggedPartitionUp$ consumer row, available only after the finite gauge cells have been named.
\item $H$ records componentwise $\hsame$ transport for dyadic tolerances, real source rows, uniform-structure rows, gauge radii, Cousin cover handoff, tagged-partition consumption, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from an entourage request through dyadic radius choice, Cousin cover handoff, and tagged-partition consumption.
\item $P$ is the $\Pkg$ provenance over $\BishopUniformGaugeUp$, $\DyadicRatCoreUp$, $\RealUp$, $\RealUniformStructureUp$, $\CousinCoverUp$, $\TaggedPartitionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopUniformGaugeUp}$ row.
\end{enumerate}
The classifier compares accepted gauges only through $D,R,U,Q,F,T,H,C,P,N$. It has no coordinate for host open-cover compactness, selected limits, countable choice, quotient cover equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop uniform gauge NameCert obligations]
\label{thm:bishop-uniform-gauge-namecert-obligations}
For every accepted $\BishopUniformGaugeUp$ carrier
$$
G=(D,R,U,Q,F,T,H,C,P,N),
$$
the local $\NameCert_{\BishopUniformGaugeUp}$ surface consists exactly of dyadic tolerance admission through $D$, real source exposure through $R$, Bishop uniform-structure admission through $U$, displayed positive gauge-radius exactness through $Q$, Cousin cover handoff through $F$, tagged-partition consumption through $T$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$. No obligation is discharged by host open-cover compactness, selected limits, countable choice, quotient cover equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-uniform-gauge-carrier}. The tuple displays the dyadic tolerance row, real source row, uniform-structure row, gauge-radius row, Cousin cover handoff, tagged-partition consumer, transport, replay, provenance, and local naming rows.

The gauge-radius row is downstream of the uniform-structure row. The row $D$ supplies dyadic radius codes, $R$ supplies the finite real-name surface being inspected, and $U$ supplies the Bishop uniform-neighborhood request.

The row $Q$ then records the positive radius histories admitted for that displayed request. Because $Q$ is a finite carrier row, it does not select a limiting process, quantify over an ambient open cover, or call a countable-choice schedule.

Finally $F,T,H,C,P,N$ export, consume, transport, replay, source, and name exactly the same finite route. Since the excluded compactness, choice, quotient, limit, and completeness objects are absent from the carrier, they cannot appear in the local NameCert obligations.
\end{proof}

\begin{theorem}[Bishop uniform gauge finite-cover route]
\label{thm:bishop-uniform-gauge-finite-cover-route}
Every $\CousinCoverUp$ or $\TaggedPartitionUp$ consumer of an accepted $\BishopUniformGaugeUp$ packet reads gauge data only through the finite route
$$
\DyadicRatCoreUp,\ \RealUp,\ \RealUniformStructureUp,\ Q,\ \CousinCoverUp,\ \TaggedPartitionUp .
$$
The route exports finite Bishop gauge-radius data for cover and partition consumers; it exports no open-cover compactness proof, selected limiting process, countable-choice selector, quotient cover equality, or ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
Begin with the accepted carrier $G=(D,R,U,Q,F,T,H,C,P,N)$. A cover-facing read must expose the dyadic tolerance row $D$, the real source row $R$, and the Bishop uniform-structure row $U$ before any local gauge radius is visible.

Use the gauge-radius row $Q$. This row stores the displayed positive radius histories for the finite source surface, so a downstream consumer cannot replace the route by an unrecorded open-cover compactness argument or an external selector.

The Cousin cover row $F$ reads those radii as local cover cells, and the tagged-partition row $T$ is downstream of that handoff. Componentwise transport and replay through $H,C$ preserve the same order, while $P,N$ source and name it.

Thus every cover or partition read factors through the finite Bishop uniform-gauge route. The excluded compactness, limit, choice, quotient, and completeness principles have no carrier row from which to enter.
\end{proof}

\closureat{BishopUniformGaugeUp}{seedStr}

\begin{closurestatus}{\BishopUniformGaugeUp}
  \constructivestory{A $\BishopUniformGaugeUp$ packet is generated from dyadic tolerance rows, Real source rows, RealUniformStructure rows, positive gauge-radius rows, CousinCover handoff, TaggedPartition consumption, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-uniform-gauge-carrier}, \autoref{thm:bishop-uniform-gauge-namecert-obligations}, and \autoref{thm:bishop-uniform-gauge-finite-cover-route} seed the finite Bishop uniform-gauge route for cover and tagged-partition consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host open-cover compactness, selected limiting processes, countable choice, quotient cover equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked dyadic tolerance admission, Real source exposure, RealUniformStructure admission, gauge-radius exactness, CousinCover handoff, TaggedPartition consumption, transport, replay, provenance, and local naming for BEDC.Derived.BishopUniformGaugeUp.}
\end{closurestatus}
