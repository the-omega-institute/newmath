\chapter{A Concrete Naming Certificate for $\ExpLogBisectionUp$}
\label{ch:concrete-instances-exp-log-bisection-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\ExpLogBisectionUp$ packet records the finite bisection route that makes logarithm a constructive inverse read for exponential on positive real data. It consumes the exponential packet of \autoref{ch:concrete-instances-real-exponential-namecert}, the logarithm packet of \autoref{ch:concrete-instances-real-logarithm-namecert}, the dyadic bracket ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite window route of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the inverse route by finite brackets and comparison rows, not by a host exponential, host logarithm, quotient real equality, selected infinite bisection branch, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[ExpLogBisection carrier]
\label{def:exp-log-bisection-carrier}
An $\ExpLogBisectionUp$ carrier is a finite $\BHist$ packet
$$
G=(X,I,B,E,L,W,D,R,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the positive $\RealUp$ input row whose logarithm is being named.
\item $I$ is the finite dyadic initial bracket row for the inverse search.
\item $B$ is the retained bisection ledger, recording midpoint comparisons and selected subintervals.
\item $E$ is the $\RealExponentialUp$ comparison row used to evaluate the displayed midpoint endpoints.
\item $L$ is the $\RealLogarithmUp$ endpoint row read only after the bisection ledger and exponential comparisons are displayed.
\item $W$ records the $\StreamNameUp$ finite windows for the retained dyadic endpoints.
\item $D$ records the $\DyadicRatCoreUp$ width, midpoint, and comparison tolerances used by the bisection ledger.
\item $R$ is the $\RegSeqRatUp$ readback row produced by the dyadic endpoint windows.
\item $Q$ is the $\RealUp$ seal row for the inverse endpoint and its exponential comparison with $X$.
\item $H$ records componentwise $\hsame$ transport for all displayed inverse-route rows.
\item $C$ records displayed $\Cont$ replay from positive input and brackets through exponential comparison, regular readback, logarithm row, and real seal.
\item $P$ is the $\Pkg$ provenance over $\ExpLogBisectionUp$, $\RealExponentialUp$, $\RealLogarithmUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ExpLogBisectionUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $X,I,B,E,L,W,D,R,Q,H,C,P,N$. It has no coordinate for host analytic functions, quotient real equality, selected infinite branches, countable choice, or ambient $\RealUp$ completeness.
\end{definition}
\leanstmt{BEDC.Derived.ExpLogBisectionUp}

\begin{theorem}[ExpLogBisection NameCert obligations]
\label{thm:exp-log-bisection-namecert-obligations}
For every accepted $\ExpLogBisectionUp$ carrier $G=(X,I,B,E,L,W,D,R,Q,H,C,P,N)$, the local $\NameCert_{\ExpLogBisectionUp}$ surface consists exactly of positive input exposure, finite initial bracket exposure, retained bisection rows, $\RealExponentialUp$ comparison rows, $\RealLogarithmUp$ endpoint routing, $\StreamNameUp$ window exposure, $\DyadicRatCoreUp$ tolerance accounting, $\RegSeqRatUp$ readback, $\RealUp$ seal, componentwise transport, displayed continuation replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the accepted carrier. The positive input and bracket data are exactly $X,I,B$, so the inverse search begins with a displayed finite bracket ledger.

The comparison and window rows are $E,W,D,R$. Exponential comparisons are evaluated through $E$, while the dyadic widths, midpoint tolerances, finite windows, and regular readback are exposed through $D,W,R$.

The endpoint and seal rows are $L,Q$. They identify the logarithm endpoint and the real equality-facing seal only after the bisection comparison rows have been displayed.

Finally $H,C,P,N$ preserve the same rows under transport, replay, provenance, and naming. Since the carrier has no host analytic-function or selected-branch coordinate, the listed rows exhaust the local certificate.
\end{proof}

\begin{theorem}[ExpLogBisection inverse handoff]
\label{thm:exp-log-bisection-inverse-handoff}
Every public inverse read of an accepted $\ExpLogBisectionUp$ carrier factors through the finite route
$$
X\longmapsto I,B\longmapsto E,D,W\longmapsto R\longmapsto L\longmapsto Q .
$$
Thus the consumer of $\exp(\log(X))\sim_{\RealUp}X$ receives a positive real input, dyadic brackets, exponential comparison rows, StreamName windows, RegSeqRat readback, logarithm endpoint row, and Real seal before any inverse conclusion is inspected.
\end{theorem}
\begin{proof}
Start with the carrier projection. The inverse route has no access to a completed host logarithm; it first displays the positive input $X$ and the finite bracket rows $I,B$.

The retained bracket is justified through $E,D,W,R$. These rows provide the exponential comparisons, dyadic width budgets, finite endpoint windows, and regular rational readback that make the bisection endpoint visible.

The logarithm and real-seal handoff is then $L,Q$. The endpoint row records the logarithm packet, and the real seal records the equality-facing inverse comparison with the original input.

The structural rows transport and replay exactly that route. No quotient equality, selected infinite branch, countable-choice schedule, or ambient completeness theorem is exported by the packet.
\end{proof}

\closureat{ExpLogBisectionUp}{seedStr}

\begin{closurestatus}{\ExpLogBisectionUp}
  \constructivestory{An $\ExpLogBisectionUp$ packet is generated from a positive RealUp input, dyadic initial and retained bracket rows, RealExponential comparison rows, a RealLogarithm endpoint row, StreamName windows, DyadicRatCore tolerances, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:exp-log-bisection-carrier}, \autoref{thm:exp-log-bisection-namecert-obligations}, and \autoref{thm:exp-log-bisection-inverse-handoff} seed the finite bisection inverse route linking logarithm and exponential over positive RealUp data.}
  \formalstatus{\formalTargetV}
  \leantarget{BEDC.Derived.ExpLogBisectionUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host exponential or logarithm functions, quotient real equality, selected infinite bisection branches, countable choice, ambient $\RealUp$ completeness, theorem-level Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, positive-domain exposure, dyadic bracket exactness, exponential comparison routing, StreamName and RegSeqRat readback, logarithm endpoint handoff, Real seal nonescape, transport stability, continuation replay, and provenance nonescape for BEDC.Derived.ExpLogBisectionUp.}
\end{closurestatus}
