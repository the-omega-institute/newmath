\chapter{A Concrete Naming Certificate for $\BishopRealUniformCompletionUp$}
\label{ch:concrete-instances-bishop-real-uniform-completion-namecert}
\origin{human}

Following Bishop and Bridges and the Bourbaki--Kelley uniform-completion viewpoint, the $\BishopRealUniformCompletionUp$ packet records the finite route by which the Real uniformity is consumed by Cauchy completion without turning that consumption into a Real-completeness theorem. It reads the Real-induced entourage surface of \autoref{ch:concrete-instances-real-uniform-structure-namecert}, the reflective completion inclusion of \autoref{ch:concrete-instances-cauchy-completion-reflective-subcategory-namecert}, the triangle route of \autoref{ch:concrete-instances-cauchy-completion-triangle-identity-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names the L10 bridge between uniform Real reads and completion-facing Cauchy routes while keeping quotient streams, selected limits, host real equality, countable choice, ambient completeness, and bridge credentials outside the packet.

\begin{definition}[BishopRealUniformCompletion carrier]
\label{def:bishop-real-uniform-completion-carrier}
A $\BishopRealUniformCompletionUp$ carrier is a finite $\BHist$ packet
$$
B=(R,U,J,T,S,D,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RealUp$ endpoint and seal row, exposing only named Real observations and the local seal consumed by the bridge.
\item $U$ is the $\RealUniformStructureUp$ entourage row, generated from Real metric-ball requests before any completion-facing read is available.
\item $J$ is the reflective completion inclusion row, read through $\CauchyCompletionReflectiveSubcategoryUp$ as the public completion route for the Real-uniform data.
\item $T$ is the $\CauchyCompletionTriangleIdentityUp$ route row, recording that both completion composites reuse the same finite Real-uniform and reflective-inclusion surface.
\item $S$ is the $\StreamNameUp$ finite-window row inspected by Real and completion consumers.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row used by the Real-uniform entourage request and by the completion-side route comparison.
\item $Q$ is the $\RegSeqRatUp$ readback row attached to the stream windows and dyadic tolerances.
\item $H$ records componentwise $\hsame$ transport for the Real seal, Real-uniform entourage, reflective completion inclusion, triangle route, stream, dyadic, regular-rational, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a Real endpoint request through the Real-uniform entourage row, reflective inclusion, triangle route, stream window, dyadic tolerance, regular rational readback, provenance, and local naming rows.
\item $P$ is the $\Pkg$ provenance over $\BishopRealUniformCompletionUp$, $\RealUp$, $\RealUniformStructureUp$, $\CauchyCompletionReflectiveSubcategoryUp$, $\CauchyCompletionTriangleIdentityUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopRealUniformCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $R,U,J,T,S,D,Q,H,C,P,N$. The carrier contains no quotient stream, selected limit, host real equality, countable-choice selector, ambient $\RealUp$ completeness theorem, metric-completeness datum, or bridge credential.
\end{definition}
\leanchecked{BEDC.Derived.BishopRealUniformCompletionUp.BishopRealUniformCompletionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[BishopRealUniformCompletion NameCert obligations]
\label{thm:bishop-real-uniform-completion-namecert-obligations}
For every accepted $\BishopRealUniformCompletionUp$ carrier $B=(R,U,J,T,S,D,Q,H,C,P,N)$, the local $\NameCert_{\BishopRealUniformCompletionUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed Real endpoint and seal row, Real-uniform entourage row, reflective completion inclusion row, triangle route row, StreamName windows, DyadicRatCore tolerances, RegSeqRat readback, transport, replay, provenance, and local naming row;
\item uniform entourage handoff, requiring every completion-facing Real read to consume $U$ after projecting $R$ and before inspecting $J$ or $T$;
\item reflective route exactness, requiring every completion inclusion read to pass through $J$ over the same $R,U,S,D,Q$ coordinates;
\item triangle route exactness, requiring both completion composites to use $T$ after the reflective route has fixed the finite inclusion surface;
\item L10 handoff discipline, requiring StreamName, DyadicRatCore, and RegSeqRat observations to be read only through $S,D,Q$ along the displayed Real-uniform and completion route;
\item non-escape, requiring $H,C,P,N$ to replay and name no row from which quotient streams, selected limits, host real equality, countable choice, ambient Real completeness, metric-completeness data, or bridge credentials can be exported.
\end{enumerate}
No local obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:bishop-real-uniform-completion-carrier}. The packet displays the Real seal, Real-uniform entourage, reflective inclusion, triangle route, finite-window, tolerance, readback, transport, replay, provenance, and local naming rows.

The uniform entourage handoff is ordered by the carrier. A Real-facing request first projects $R$, then reads the Real-induced entourage row $U$, and only then may continue toward completion through $J$ or $T$.

Reflective exactness is the completion-side projection. The row $J$ is the only displayed inclusion surface, so every completion inclusion read is forced to reuse the same $R,U,S,D,Q$ coordinates rather than selecting a host completion object.

Triangle exactness is then downstream of $J$. The row $T$ records both completion composites over the fixed inclusion route, and it has no separate quotient, representative, or host-equality coordinate.

Finally, the L10 handoff and non-escape clauses are exhausted by $S,D,Q,H,C,P,N$. These rows transport, replay, source, and name the displayed route, and none of them contains quotient streams, selected limits, host real equality, countable choice, ambient Real completeness, metric-completeness data, or bridge credentials.
\end{proof}

\begin{theorem}[BishopRealUniformCompletion L10 handoff]
\label{thm:bishop-real-uniform-completion-l10-handoff}
Every completion-facing $\RealUp$ read exported by an accepted $\BishopRealUniformCompletionUp$ carrier factors through the finite rows
$$
R\longmapsto U\longmapsto J\longmapsto T\longmapsto S,D,Q\longmapsto H,C,P,N .
$$
Consequently the packet exports no quotient stream, selected limit, host real equality, countable choice, ambient $\RealUp$ completeness, metric-completeness datum, or bridge credential.
\end{theorem}
\begin{proof}
Start with a completion-facing Real read and project the carrier rows. The Real endpoint and seal data are visible only through $R$, and the uniformity consumed by the read is visible only through $U$.

The reflective completion route then passes through $J$. This fixes the finite inclusion surface before the triangle route is inspected, so the read cannot branch into a host completion carrier or selected limit.

Project $T$ to record the two completion composites over the same route. The finite observation rows $S,D,Q$ supply StreamName windows, dyadic tolerances, and regular rational readback for that route and no other one.

The structural rows $H,C,P,N$ close the handoff by transport, replay, provenance, and local naming. Since these rows exhaust the packet, there is no coordinate from which a quotient stream, selected limit, host real equality, countable choice, ambient Real completeness, metric-completeness datum, or bridge credential could be exported.
\end{proof}

\begin{closurestatus}{\BishopRealUniformCompletionUp}
  \constructivestory{A $\BishopRealUniformCompletionUp$ packet is generated from a Real endpoint and seal row, a RealUniformStructure entourage row, a CauchyCompletionReflectiveSubcategory inclusion row, a CauchyCompletionTriangleIdentity route row, StreamName windows, DyadicRatCore tolerances, RegSeqRat readback, hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-real-uniform-completion-carrier}, \autoref{thm:bishop-real-uniform-completion-namecert-obligations}, and \autoref{thm:bishop-real-uniform-completion-l10-handoff} seed the finite Real-uniform completion bridge for L10 consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient streams, selected limits, host real equality, countable choice, ambient $\RealUp$ completeness, metric-completeness data, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, uniform entourage handoff, reflective route exactness, triangle route exactness, L10 handoff, and non-escape proofs for BEDC.Derived.BishopRealUniformCompletionUp.}
\end{closurestatus}
