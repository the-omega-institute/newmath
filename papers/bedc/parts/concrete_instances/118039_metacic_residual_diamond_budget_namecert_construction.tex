\chapter{A Concrete Naming Certificate for $\MetaCICResidualDiamondBudgetUp$}
\label{ch:concrete-instances-metacic-residual-diamond-budget-namecert}
\origin{ai}

The $\MetaCICResidualDiamondBudgetUp$ packet realizes one finite residual-substitution confluence frontier from \autoref{sec:metacic-open-problems-confluence}. It sits beside the residual-substitution boundary of \autoref{ch:concrete-instances-metacic-residual-substitution-boundary-namecert}, the residual checker of \autoref{ch:concrete-instances-metacic-confluence-residual-checker-namecert}, the residual-diamond scheduler of \autoref{ch:concrete-instances-metacic-residual-diamond-scheduler-namecert}, the residual-diamond witness of \autoref{ch:concrete-instances-metacic-residual-diamond-witness-namecert}, and the residual-join budget of \autoref{ch:concrete-instances-metacic-residual-join-budget-namecert}. The packet records a finite typed window, residual-pair budget, local-diamond candidate ledger, checker fuel, obstruction retention, transport, replay, provenance, and local naming; it does not claim unrestricted residual substitution, arbitrary closed parallel diamond, Church--Rosser, quotient conversion equality, or a host confluence oracle.

\begin{definition}[MetaCIC residual diamond budget carrier]
\label{def:metacic-residual-diamond-budget-carrier}
A $\MetaCICResidualDiamondBudgetUp$ carrier is a finite $\BHist$ packet
$$
B=(T,R,D,J,C,O,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the typed-window row, fixing the closed source term, local context fragment, redex shape, and observation depth inspected by the packet.
\item $R$ is the residual-frontier row, naming the residual substitutions visible inside the typed window.
\item $D$ is the residual-pair budget row, listing the finitely many residual pairs for which the local-diamond question is inspected.
\item $J$ is the join-candidate ledger, recording the proposed common reducts for the residual pairs in $D$.
\item $C$ is the bounded checker row, recording the finite fuel, replay trace, and checker status used to inspect the candidate joins.
\item $O$ is the obstruction-retention row, preserving any residual-substitution or subject-reduction boundary that the bounded checker has not discharged.
\item $H$ records componentwise $\hsame$ transport for $T,R,D,J,C,O,K,P,N$.
\item $K$ records displayed $\Cont$ replay from a residual-diamond request through the typed window, frontier, pair budget, join candidates, checker row, and obstruction row.
\item $P$ is the $\Pkg$ provenance over $\MetaCICResidualDiamondBudgetUp$, $\MetaCICResidualSubstitutionBoundaryUp$, $\MetaCICConfluenceResidualCheckerUp$, $\MetacicResidualDiamondSchedulerUp$, $\MetaCICResidualDiamondWitnessUp$, $\MetaCICResidualJoinBudgetUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetaCICResidualDiamondBudgetUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $T,R,D,J,C,O,H,K,P,N$. It has no coordinate for unrestricted residual substitution, an arbitrary closed parallel diamond, a global Church--Rosser endpoint, quotient conversion equality, unbounded checking, or a host confluence oracle.
\end{definition}

\begin{theorem}[MetaCIC residual diamond budget obligations]
\label{thm:metacic-residual-diamond-budget-obligations}
For every accepted $\MetaCICResidualDiamondBudgetUp$ carrier $B=(T,R,D,J,C,O,H,K,P,N)$, the local $\NameCert_{\MetaCICResidualDiamondBudgetUp}$ surface consists exactly of typed-window admission, residual-frontier exposure, residual-pair budget accounting, join-candidate ledger visibility, bounded-checker exactness, obstruction retention, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-residual-diamond-budget-carrier}. The visible rows are exactly $T,R,D,J,C,O,H,K,P,N$.

The typed window $T$ and residual frontier $R$ fix the local MetaCIC source in which residuals are meaningful. The residual-pair budget $D$ narrows the local-diamond request to finitely many inspected pairs, and $J$ records only the proposed joins for those pairs.

The bounded checker row $C$ evaluates the same finite ledger, while the obstruction row $O$ keeps undisclosed residual-substitution or subject-reduction boundaries visible. Transport, replay, provenance, and naming through $H,K,P,N$ preserve this route and introduce no additional confluence endpoint.
\end{proof}

\begin{theorem}[MetaCIC residual diamond budget nonescape]
\label{thm:metacic-residual-diamond-budget-nonescape}
An accepted $\MetaCICResidualDiamondBudgetUp$ packet exports no unrestricted residual-substitution principle, no arbitrary closed parallel diamond, no global Church--Rosser theorem, no quotient conversion equality, no unbounded checker, and no host confluence oracle. Every confluence-facing read factors through the displayed typed window, residual-frontier row, residual-pair budget, join-candidate ledger, bounded checker, obstruction row, componentwise transport, replay, provenance, and local NameCert row.
\end{theorem}
\begin{proof}
Inspect the carrier coordinates. They list only the finite typed-window, residual-frontier, residual-pair, join-candidate, bounded-checker, obstruction, transport, replay, provenance, and naming rows.

Any diamond-facing read must pass through the pair budget $D$, the candidate ledger $J$, and the bounded checker $C$ before it reaches a consumer. The obstruction row $O$ remains present whenever the bounded check has not discharged a residual-substitution or subject-reduction boundary.

The excluded endpoints require coordinates absent from the carrier. Since no row supplies unrestricted substitution compatibility, arbitrary closed parallel diamonds, quotient conversion equality, unbounded checking, or a host oracle, those objects cannot escape this NameCert packet.
\end{proof}

\begin{theorem}[MetaCIC residual diamond budget confluence frontier]
\label{thm:metacic-residual-diamond-budget-confluence-frontier}
The $\MetaCICResidualDiamondBudgetUp$ packet records the residual-substitution confluence frontier as the finite route
$$
T,R \longmapsto D,J,C,O \longmapsto H,K,P,N .
$$
It turns a candidate local-diamond request into a bounded residual-pair budget with explicit join candidates, checker status, and obstruction retention. It does not remove the residual-substitution boundary named in \autoref{sec:metacic-open-problems-confluence}, and it does not upgrade a finite local budget into arbitrary closed-term confluence.
\end{theorem}
\begin{proof}
Read the route from \autoref{def:metacic-residual-diamond-budget-carrier}. The rows $T,R$ locate the typed residual frontier, while $D,J,C,O$ record the finite pair budget, candidate joins, bounded checking, and retained obstruction.

\autoref{thm:metacic-residual-diamond-budget-obligations} identifies these rows as the local NameCert obligation surface. \autoref{thm:metacic-residual-diamond-budget-nonescape} prevents a downstream confluence consumer from replacing the finite budget by unrestricted residual substitution, arbitrary closed parallel diamonds, or quotient conversion equality.

Thus the packet is a concrete finite frontier object for later Lean work: it makes the residual-diamond budget inspectable without pretending that the open confluence endpoint has already been discharged.
\end{proof}

\falsifiablePrediction{If the residual-pair budget row $D$ is removed, \autoref{thm:metacic-residual-diamond-budget-confluence-frontier} can no longer distinguish a bounded residual-diamond packet from a single local-join witness.}

\independenceWitness{metacic-residual-substitution-boundary, metacic-confluence-residual-checker, metacic-residual-diamond-scheduler, metacic-residual-diamond-witness, metacic-residual-join-budget: $\MetaCICResidualDiamondBudgetUp$ is not bijective to any listed sibling because it jointly carries a typed residual frontier, residual-pair budget, join-candidate ledger, bounded checker, obstruction retention, transport, replay, provenance, and local naming.}

\closureat{MetaCICResidualDiamondBudgetUp}{seedStr}

\begin{closurestatus}{\MetaCICResidualDiamondBudgetUp}
  \constructivestory{A $\MetaCICResidualDiamondBudgetUp$ packet is generated from typed-window rows, residual-frontier rows, residual-pair budgets, join-candidate ledgers, bounded-checker rows, obstruction-retention rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-residual-diamond-budget-carrier}, \autoref{thm:metacic-residual-diamond-budget-obligations}, \autoref{thm:metacic-residual-diamond-budget-nonescape}, and \autoref{thm:metacic-residual-diamond-budget-confluence-frontier} seed the finite residual-diamond budget packet for MetaCIC confluence-frontier consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, unrestricted residual substitution, arbitrary closed parallel diamond, global Church--Rosser, quotient conversion equality, unbounded checking, host confluence oracles, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, typed-window coverage, residual-frontier exposure, residual-pair budget accounting, join-candidate ledger visibility, bounded-checker exactness, obstruction retention, classifier stability, and nonescape for BEDC.Derived.MetaCICResidualDiamondBudgetUp.}
\end{closurestatus}
