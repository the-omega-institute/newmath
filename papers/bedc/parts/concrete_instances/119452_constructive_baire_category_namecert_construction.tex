\chapter{A Concrete Naming Certificate for $\ConstructiveBaireCategoryUp$}
\label{ch:concrete-instances-constructive-baire-category-namecert}
\origin{human}

Following Baire's theorem and Bishop-style constructive analysis, the $\ConstructiveBaireCategoryUp$ packet records the finite dense-open cylinder surface by which a completion-facing consumer may use a Baire-category argument without importing classical countable choice. It sits beside the Baire-space source of \autoref{ch:concrete-instances-bairespace-namecert}, the Cantor-space sibling of \autoref{ch:concrete-instances-cantorspace-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only displayed cylinder rows, dense-open admission rows, finite meet witnesses, metric-window replay, transport, provenance, and local naming.

\begin{definition}[Constructive Baire category carrier]
\label{def:constructive-baire-category-carrier}
A $\ConstructiveBaireCategoryUp$ carrier is a finite $\BHist$ packet
$$
B=(S,C,D,F,M,W,R,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\BaireSpaceUp$ source row, exposing the finite prefix cylinder currently inspected by the packet.
\item $C$ is the $\CantorSpaceUp$ comparison row used when the same finite-prefix argument is read through binary cylinders.
\item $D$ is the dense-open admission row, listing the displayed dense-open cylinder requests visible to the packet.
\item $F$ is the finite meet-witness row, recording a common cylinder refinement for the displayed finite family from $D$.
\item $M$ is the $\MetricSpaceUp$ window row that bounds the cylinder read by a displayed metric observation.
\item $W$ is the $\StreamNameUp$ finite-window row attached to the inspected prefixes and metric observations.
\item $R$ is the $\RealUp$ seal row for scalar radius or tolerance reads obtained only after $S,C,D,F,M,W$ have been exposed.
\item $H$ records componentwise $\hsame$ transport for the displayed source, cylinder, dense-open, finite-meet, metric, stream-window, and real-seal rows.
\item $K$ records displayed $\Cont$ replay from finite cylinder requests through dense-open admission, finite meet witnesses, metric windows, stream windows, real sealing, transport, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\ConstructiveBaireCategoryUp$, $\BaireSpaceUp$, $\CantorSpaceUp$, $\MetricSpaceUp$, $\StreamNameUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ConstructiveBaireCategoryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a classical choice sequence, an arbitrary countable meet of open sets, open-cover compactness, quotient equality of streams, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Constructive Baire category NameCert obligations]
\label{thm:constructive-baire-category-namecert-obligations}
For every accepted $\ConstructiveBaireCategoryUp$ carrier $B=(S,C,D,F,M,W,R,H,K,P,N)$, the local $\NameCert_{\ConstructiveBaireCategoryUp}$ surface consists exactly of Baire-space source exposure, Cantor-space cylinder comparison when present, dense-open cylinder admission, finite meet-witness exposure, metric-window exposure, $\StreamNameUp$ finite-window exposure, $\RealUp$ seal exposure, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. A consumer receives no classical countable-choice principle, no arbitrary countable intersection of open sets, no open-cover compactness theorem, no quotient stream equality, and no ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:constructive-baire-category-carrier}. The source and cylinder rows are $S,C,D,F$, the analytic observation rows are $M,W,R$, and the structural rows are $H,K,P,N$.

The dense-open obligations come from $D$ and $F$. The row $D$ lists only the dense-open cylinder requests already displayed by the packet, while $F$ records a finite common refinement for that listed family.

The analytic handoff comes from $M,W,R$. The metric row bounds the cylinder read, the stream row records the finite prefix window that is actually inspected, and the real row seals scalar radius or tolerance reads only after those rows are visible.

Transport, replay, provenance, and local naming preserve this finite route. Since the carrier contains no coordinate for classical countable choice, arbitrary countable meets, open-cover compactness, quotient stream equality, or ambient $\RealUp$ completeness, none of those objects belongs to the local NameCert surface.
\end{proof}

\begin{theorem}[Constructive Baire category countable-meet route]
\label{thm:constructive-baire-category-countable-meet-route}
Every completion-facing or dense-open consumer of an accepted $\ConstructiveBaireCategoryUp$ packet that reads a countable-meet-shaped claim must factor the read through finite dense-open cylinder windows before reaching $\StreamNameUp$, $\MetricSpaceUp$, and $\RealUp$. The exported route is the finite chain
$$
\BaireSpaceUp,\ \CantorSpaceUp,\ \MetricSpaceUp,\ \StreamNameUp,\ \RealUp
$$
read through $S,C,D,F,M,W,R,H,K,P,N$, not a classical countable intersection theorem or a choice-selected branch.
\end{theorem}
\begin{proof}
Start with the dense-open rows of \autoref{thm:constructive-baire-category-namecert-obligations}. A consumer may inspect only the finite family listed in $D$, and the common refinement it receives is the displayed witness $F$.

Next read the cylinder through the metric and stream windows. The row $M$ bounds the observation and $W$ fixes the finite prefix window, so the route remains a finite cylinder argument rather than a branch selected from an infinite family.

Finally pass through the $\RealUp$ seal and the structural rows. The seal $R$ is downstream of the finite cylinder data, and $H,K,P,N$ replay only the same finite route. Thus the countable-meet-shaped read is available only through finite dense-open cylinder obligations.
\end{proof}

\begin{theorem}[Constructive Baire category choice boundary]
\label{thm:constructive-baire-category-choice-boundary}
An accepted $\ConstructiveBaireCategoryUp$ packet separates finite dense-open cylinder witnesses from classical choice principles. It may export the displayed finite meet witness $F$ and its $\BaireSpaceUp$, $\CantorSpaceUp$, $\MetricSpaceUp$, $\StreamNameUp$, and $\RealUp$ route, but it cannot export a choice function over arbitrary dense-open sequences, a selected generic point, an arbitrary open-cover compactness principle, quotient equality of streams, or ambient completeness of $\RealUp$.
\end{theorem}
\begin{proof}
Read the carrier coordinates. The only witness coordinate is $F$, and $F$ is indexed by the displayed dense-open rows in $D$ together with the finite cylinder sources $S,C$.

The downstream analytic rows do not add a selector. They only bind the cylinder to metric observations, finite stream windows, and the terminal real-name seal.

The structural rows preserve the same packet. Since $B$ has no coordinate for a choice function, selected generic point, arbitrary open-cover compactness, quotient stream equality, or ambient Real completeness, those objects cannot be recovered from the local NameCert route.
\end{proof}

\falsifiablePrediction{Every accepted $\ConstructiveBaireCategoryUp$ read exposes BaireSpace cylinders, CantorSpace comparison cylinders, dense-open admission rows, finite meet witnesses, MetricSpace windows, StreamName finite windows, a RealUp seal, hsame transport, Cont replay, Pkg provenance, and local NameCert rows before any completion-facing consumer uses the packet.}

\independenceWitness{baire-space, cantor-space, streamname, metricspace, real: $\ConstructiveBaireCategoryUp$ is not bijective to any listed sibling because it jointly binds finite dense-open cylinder obligations, finite meet witnesses, metric windows, stream windows, Real sealing, transport, replay, provenance, and local naming while refusing classical countable choice, arbitrary countable meets, open-cover compactness, quotient stream equality, and ambient completeness.}

\closureat{ConstructiveBaireCategoryUp}{seedStr}

\begin{closurestatus}{\ConstructiveBaireCategoryUp}
  \constructivestory{A $\ConstructiveBaireCategoryUp$ packet is generated from displayed BaireSpace cylinders, CantorSpace comparison cylinders, dense-open cylinder rows, finite meet witnesses, MetricSpace windows, StreamName finite windows, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:constructive-baire-category-carrier}, \autoref{thm:constructive-baire-category-namecert-obligations}, \autoref{thm:constructive-baire-category-countable-meet-route}, and \autoref{thm:constructive-baire-category-choice-boundary} seed the finite constructive Baire-category NameCert surface over dense-open cylinder obligations.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim classical countable choice, arbitrary countable meets of open sets, arbitrary open-cover compactness, quotient equality of streams, ambient $\RealUp$ completeness, Lean theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dense-open cylinder admission, finite meet-witness exactness, MetricSpace window routing, StreamName finite-window exposure, Real seal preservation, transport, replay, provenance, and local naming for BEDC.Derived.ConstructiveBaireCategoryUp.}
\end{closurestatus}
