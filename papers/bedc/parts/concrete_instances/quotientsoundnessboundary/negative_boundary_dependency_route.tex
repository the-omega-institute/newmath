\begin{theorem}[QuotientSoundnessBoundary negative-boundary route]
\label{thm:quotient-soundness-boundary-negative-boundary-route}
Every quotient-style representative request exposed to an accepted $\QuotientSoundnessBoundaryUp$ packet is first read as a negative-name boundary request through \autoref{ch:concrete-instances-negative-name-boundary-namecert}. Only after that boundary has displayed the blocked request may the quotient-soundness packet replace the attempted representative read by the local $\psame$ transport row over its visible relation data.
\end{theorem}
\begin{proof}
Read the requested representative rows and refusal verdict from the carrier in \autoref{def:quotient-soundness-boundary-carrier}. These rows place the demand for a representative, quotient equality token, section, erased derivation tree, or $\mathsf{Quot.sound}$ dependency on the public refusal surface before any positive transport row is consumed.

The negative-name boundary of \autoref{ch:concrete-instances-negative-name-boundary-namecert} supplies the sibling route for that refusal. It treats the representative request as a blocked boundary read rather than as generated content.

After this refusal route is visible, apply \autoref{thm:quotient-soundness-boundary-transport-replacement}. The surviving operation is the classifier-respecting $\psame$ route through the displayed relation rows, componentwise $\hsame$ transport, visible $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row. No quotient representative, quotient eliminator, erased derivation recovery map, or $\mathsf{Quot.sound}$ dependency is exported.
\end{proof}
