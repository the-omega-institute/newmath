The quotient-soundness boundary also records a sibling refusal route through \autoref{ch:concrete-instances-quotientstreamrefusal-namecert}. That sibling supplies the quotient-facing refusal discipline consumed by the local $\QuotientSoundnessBoundaryUp$ rows before classifier-respecting $\psame$ transport replaces any representative-selection request.
