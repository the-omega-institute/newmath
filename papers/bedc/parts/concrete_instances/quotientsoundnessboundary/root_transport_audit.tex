\begin{theorem}[QuotientSoundnessBoundary root psame route totality]
\label{thm:quotient-soundness-boundary-root-psame-route-totality}
For every accepted $\QuotientSoundnessBoundaryUp$ packet, every surviving root operation after representative refusal factors through the displayed $\psame$ route $E,T,H,C,P,N$. The packet exports no root operation through a representative selector, quotient equality eliminator, erased derivation recovery map, host quotient recursion, or $\mathsf{Quot.sound}$ witness.
\end{theorem}
\leanchecked{BEDC.Derived.QuotientSoundnessBoundaryUp.QuotientSoundnessBoundary\_root\_psame\_route\_totality}
\begin{proof}
Begin with the NameCert obligations and fixed classifier, \autoref{thm:quotient-soundness-boundary-namecert-obligations} and \autoref{thm:quotient-soundness-boundary-fixed-classifier}. They identify the relation source and classifier rows read by any accepted packet.

Next use kernel non-escape and psame transport exhaustion, \autoref{thm:quotient-soundness-boundary-kernel-nonescape} and \autoref{thm:quotient-soundness-boundary-psame-transport-exhaustion}. These results leave only the classifier-respecting $\psame$ transport row and the structural rows $H,C,P,N$ for a surviving root operation.

Finally apply \autoref{thm:quotient-soundness-boundary-quot-sound-refusal-exactness}. Once $\mathsf{Quot.sound}$ is refused at the visible boundary, the positive consumer route is exactly $E,T,H,C,P,N$.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary root psame source admission]
\label{thm:quotient-soundness-boundary-root-psame-source-admission}
Every root quotient-style operation accepted by a $\QuotientSoundnessBoundaryUp$ packet exposes the source relation rows $E$ before the $\psame$ transport row $T$ is readable. Thus the positive route $E,T,H,C,P,N$ is admitted only after the relation source has been displayed, and no representative, erased derivation, host quotient witness, or $\mathsf{Quot.sound}$ token can stand in for that source admission.
\end{theorem}
\leanchecked{BEDC.Derived.QuotientSoundnessBoundaryUp.QuotientSoundnessBoundary\_root\_psame\_source\_admission}
\begin{proof}
Begin with the root request triad and root relation row exposure, \autoref{thm:quotient-soundness-boundary-root-request-triad} and \autoref{thm:quotient-soundness-boundary-root-relation-row-exposure}. They make the source relation rows $E$ visible at the root boundary before any consumer reads a surviving transport.

Next apply root transport verdict order, \autoref{thm:quotient-soundness-boundary-root-transport-verdict-order}. The refusal verdict is fixed before the positive route is consumed, so the later $\psame$ transport cannot create its own source relation.

Finally use psame transport exhaustion and non-escape, \autoref{thm:quotient-soundness-boundary-psame-transport-exhaustion} and \autoref{thm:quotient-soundness-boundary-kernel-nonescape}. The only surviving transport route is the displayed $E,T,H,C,P,N$ route, and the excluded quotient primitives have no source-admission coordinate.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary root representative refusal totality]
\label{thm:quotient-soundness-boundary-root-representative-refusal-totality}
Every root request for a representative, section, equality token, erased derivation, host quotient witness, or $\mathsf{Quot.sound}$ dependency is exhausted by the representative-request row $A$ and refusal-verdict row $V$ before any positive transport route is consumed. After $A,V$ are read, the only admitted consumer route is the classifier-respecting $\psame$ route through $E,T,H,C,P,N$.
\end{theorem}
\leanchecked{BEDC.Derived.QuotientSoundnessBoundaryUp.QuotientSoundnessBoundary\_root\_representative\_refusal\_totality}
\begin{proof}
Use the root request triad, \autoref{thm:quotient-soundness-boundary-root-request-triad}. It sends representative-style demands to the visible request and refusal rows $A,V$ while keeping the relation source row $E$ explicit.

Apply root transport verdict order and the representative request frontier, \autoref{thm:quotient-soundness-boundary-root-transport-verdict-order} and \autoref{thm:quotient-soundness-boundary-representative-request-frontier}. Together they place the refusal verdict before the transport row is available to a downstream consumer.

Then use psame transport exhaustion and non-escape, \autoref{thm:quotient-soundness-boundary-psame-transport-exhaustion} and \autoref{thm:quotient-soundness-boundary-kernel-nonescape}. They leave only the classifier-respecting route $E,T,H,C,P,N$ after the refused request has been exhausted by $A,V$.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary root Quot.sound exclusion]
\label{thm:quotient-soundness-boundary-root-quotsound-exclusion}
The combined root packet
$$
E,A,T,V,H,C,P,N
$$
has no coordinate for $\mathsf{Quot.sound}$, quotient eliminators, host class selectors, representative recovery, erased derivation recovery, or hidden section choices. Relation exposure is recorded by $E$, representative-style refusal is recorded by $A,V$, and the only positive transport route is the displayed $\psame$ route $E,T,H,C,P,N$.
\end{theorem}
\begin{proof}
First combine the root source and refusal boundaries. By \autoref{thm:quotient-soundness-boundary-root-psame-source-admission} and \autoref{thm:quotient-soundness-boundary-root-representative-refusal-totality}, the packet exposes $E$ before transport and exhausts representative-style requests by $A,V$.

Next apply the root psame route totality, \autoref{thm:quotient-soundness-boundary-root-psame-route-totality}. Every surviving root operation after refusal factors through the displayed $E,T,H,C,P,N$ route.

Finally use psame transport exhaustion and kernel non-escape, \autoref{thm:quotient-soundness-boundary-psame-transport-exhaustion} and \autoref{thm:quotient-soundness-boundary-kernel-nonescape}. The packet contains only the displayed source, refusal, transport, stability, continuation, provenance, and naming rows, so $\mathsf{Quot.sound}$ and the other quotient primitives are excluded from the public surface.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary refusal before transport]
\label{thm:quotient-soundness-boundary-refusal-before-transport}
In every accepted $\QuotientSoundnessBoundaryUp$ packet, the representative-request row $A$ and refusal-verdict row $V$ are read before the positive $\psame$ transport row $T$. Thus no surviving use of $T$ can retroactively supply a representative selector, erased derivation, quotient equality eliminator, host quotient recursion, or $\mathsf{Quot.sound}$ witness.
\end{theorem}
\begin{proof}
Apply the root transport verdict order and representative request frontier, \autoref{thm:quotient-soundness-boundary-root-transport-verdict-order} and \autoref{thm:quotient-soundness-boundary-representative-request-frontier}. Together they place $A,V$ at the visible refusal boundary before $T$ is available to a downstream consumer.

Use verdict-transport separation, \autoref{thm:quotient-soundness-boundary-verdict-transport-separation}. The positive transport row cannot reread the refusal verdict as a quotient eliminator or as evidence for a representative witness.

Finally invoke \autoref{thm:quotient-soundness-boundary-quot-sound-refusal-exactness}. The exact complement between refusal and the surviving $\psame$ route gives the required order: refusal first, transport only after that refusal has been displayed.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary kernel consumer audit triad]
\label{thm:quotient-soundness-boundary-kernel-consumer-audit-triad}
The kernel-facing audit surface of an accepted $\QuotientSoundnessBoundaryUp$ packet consists exactly of the relation exposure row, the refusal verdict for representative-side quotient primitives, and the surviving $\psame$ replacement route. Equivalently, every kernel consumer reads the triad
$$
E,A,V \quad\text{and}\quad E,T,H,C,P,N
$$
with the refusal side preceding transport, and no further quotient-soundness coordinate is exported.
\end{theorem}
\begin{proof}
First apply relation row exposure and the fixed classifier, \autoref{thm:quotient-soundness-boundary-root-relation-row-exposure} and \autoref{thm:quotient-soundness-boundary-fixed-classifier}. They expose the source relation and classifier rows without admitting a host quotient witness.

Next use refusal before transport above. The representative-request row and refusal-verdict row are read before the positive transport row, so the audit records the negative verdict before any surviving consumer can proceed.

Finally combine root psame route totality with quotient-sound refusal exactness. The surviving side is exactly the $\psame$ replacement route through $E,T,H,C,P,N$, while the refused side stays at $E,A,V$; kernel non-escape then rules out any additional quotient-soundness coordinate.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary root representative refusal]
\label{thm:quotient-soundness-boundary-root-representative-refusal}
Every root representative-side quotient request accepted by a $\QuotientSoundnessBoundaryUp$ packet is read through the relation row $E$, the requested-representative row $A$, and the refusal-verdict row $V$ before the transport row $T$ becomes available. Thus the root request reaches the visible representative refusal frontier before any surviving $\psame$ replacement route can be consumed.
\end{theorem}
\begin{proof}
Start with the representative request frontier, \autoref{thm:quotient-soundness-boundary-representative-request-frontier}. It exhausts representative, representative-selector, erased-derivation, and host quotient requests by $E,A,V$ before the transport row $T$ is readable.

Apply refusal before transport, \autoref{thm:quotient-soundness-boundary-refusal-before-transport}. The refusal verdict is consumed before any positive transport use, so a downstream root consumer cannot reverse the order and treat $T$ as representative evidence.

Finally use the kernel consumer audit triad, \autoref{thm:quotient-soundness-boundary-kernel-consumer-audit-triad}. The kernel-facing surface keeps the refusal side $E,A,V$ distinct from the surviving transport side $E,T,H,C,P,N$, giving the stated root order.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary root psame transport admission]
\label{thm:quotient-soundness-boundary-root-psame-transport-admission}
Every surviving root operation of an accepted $\QuotientSoundnessBoundaryUp$ packet is admitted only through the classifier-respecting $\psame$ transport route $E,T,H,C,P,N$. The operation may read the refused representative-side rows $E,A,V$ as its accountability surface, but its positive movement is exactly the displayed transport, stability, locality, provenance, and naming route.
\end{theorem}
\begin{proof}
Begin after the refusal surface has been read. By \autoref{thm:quotient-soundness-boundary-root-representative-refusal}, every root representative-side request has already passed through $E,A,V$ before the positive route is available.

Use psame-only descent, \autoref{thm:quotient-soundness-boundary-psame-only-descent}. It states that accepted descent after refusal factors only through $E,T,H,C,P,N$ by classifier-respecting $\psame$ transport.

Now apply root psame route totality, \autoref{thm:quotient-soundness-boundary-root-psame-route-totality}. This makes the same $E,T,H,C,P,N$ route total for surviving root operations and excludes representative selection, quotient equality elimination, erased derivation recovery, host quotient recursion, and $\mathsf{Quot.sound}$.
\end{proof}

\begin{theorem}[QuotientSoundnessBoundary root nonimport ledger]
\label{thm:quotient-soundness-boundary-root-nonimport-ledger}
The root kernel ledger exported by a $\QuotientSoundnessBoundaryUp$ packet is exactly the refused representative surface $E,A,V$ together with the admitted $\psame$ transport surface $E,T,H,C,P,N$. It imports no quotient carrier, representative selector, quotient equality eliminator, erased derivation recovery map, host quotient recursion principle, or $\mathsf{Quot.sound}$ dependency.
\end{theorem}
\leanchecked{BEDC.Derived.QuotientSoundnessBoundaryUp.QuotientSoundnessBoundary\_root\_nonimport\_ledger}
\begin{proof}
First combine root representative refusal with root $\psame$ transport admission, \autoref{thm:quotient-soundness-boundary-root-representative-refusal} and \autoref{thm:quotient-soundness-boundary-root-psame-transport-admission}. These two rows identify the negative ledger side $E,A,V$ and the surviving positive side $E,T,H,C,P,N$.

Apply the kernel consumer audit triad, \autoref{thm:quotient-soundness-boundary-kernel-consumer-audit-triad}. It packages those two sides as the kernel-facing audit surface and records that no further quotient-soundness coordinate is exported.

Finally cite public bridge non-escape and psame-only descent, \autoref{thm:quotient-soundness-boundary-public-bridge-nonescape} and \autoref{thm:quotient-soundness-boundary-psame-only-descent}. The bridge and descent rows exclude quotient carriers, representative selectors, erased derivation recovery, host quotient recursion, and $\mathsf{Quot.sound}$, so the ledger is a nonimport ledger rather than a quotient extension.
\end{proof}
