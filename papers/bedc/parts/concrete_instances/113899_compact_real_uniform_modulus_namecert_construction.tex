\chapter{A Concrete Naming Certificate for $\CompactRealUniformModulusUp$}
\label{ch:concrete-instances-compact-real-uniform-modulus-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CompactRealUniformModulusUp$ packet records the finite uniform-modulus surface available from compact real data when the compactness evidence is already displayed as metric and finite-net information. It reads metric real distances through \autoref{ch:concrete-instances-real-metric-namecert}, uniform control through \autoref{ch:concrete-instances-uniformmodulus-namecert}, compact metric finite data through \autoref{ch:concrete-instances-compactmetric-namecert}, finite observations through \autoref{ch:concrete-instances-streamname-namecert}, rational readback through \autoref{ch:concrete-instances-regseqrat-namecert}, and terminal real names through \autoref{ch:concrete-instances-real-namecert}. The packet names an L10 adjacent finite-modulus route over compact real sources; it is not an open-cover compactness theorem, a choice-selected finite subcover, quotient real equality, or ambient completeness principle.

\begin{definition}[Compact real uniform modulus carrier]
\label{def:compact-real-uniform-modulus-carrier}
A $\CompactRealUniformModulusUp$ carrier is a finite $\BHist$ packet
$$
Q=(R,M,U,K,W,A,S,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RealUp$ source row, carrying the real names whose finite compact real data are being read.
\item $M$ is the $\RealMetricUp$ row, recording the metric distance comparison surface used by the compact real request.
\item $U$ is the $\UniformModulusUp$ row, exposing the dyadic tolerance budget and response modulus.
\item $K$ is the $\CompactMetricUp$ row, read only as finite compact metric data for the displayed real source.
\item $W$ is the $\StreamNameUp$ finite-window row opened at the compact real observations requested by $R,M,K$.
\item $A$ is the $\RegSeqRatUp$ readback row attached to the same displayed windows and tolerance budget.
\item $S$ is the target $\RealUp$ seal row reached after metric comparison, compact metric data, finite windows, readback, and uniform-modulus control have all been displayed.
\item $T$ records componentwise $\hsame$ transport for $R,M,U,K,W,A,S$.
\item $C$ records displayed $\Cont$ replay through the finite route
$$
R,M,K \longmapsto W,A,U \longmapsto S,T,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\CompactRealUniformModulusUp$, $\RealUp$, $\RealMetricUp$, $\UniformModulusUp$, $\CompactMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactRealUniformModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through the displayed rows $R,M,U,K,W,A,S,T,C,P,N$. It has no coordinate for open-cover compactness, countable choice, selected subcovers, quotient real equality, host real representatives, or ambient Real completeness.
\end{definition}
\leanchecked{BEDC.Derived.CompactRealUniformModulusUp.CompactRealUniformModulusTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Compact real uniform modulus NameCert obligations]
\label{thm:compact-real-uniform-modulus-namecert-obligations}
For every accepted $\CompactRealUniformModulusUp$ carrier $Q=(R,M,U,K,W,A,S,T,C,P,N)$, the local $\NameCert_{\CompactRealUniformModulusUp}$ surface consists exactly of real-source admission, real-metric comparison admission, compact-metric finite-data admission, finite StreamName window exposure, RegSeqRat readback, uniform-modulus tolerance exactness, target Real seal admission, componentwise transport, displayed replay, provenance, and local naming for the same displayed rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-real-uniform-modulus-carrier}. The projection exposes the real source, real metric, uniform modulus, compact metric, finite windows, rational readback, target seal, and structural rows.

The compact real obligations are upstream of every modulus read. A consumer must first display $R,M,K$ before it can open finite windows $W$ or consume the readback row $A$.

The uniform-modulus row $U$ then binds all accepted compact real observations to the same dyadic tolerance budget. The seal $S$ is downstream of those rows, while $T,C,P,N$ transport, replay, source, and name the same finite packet.

Since the carrier has no coordinate for open covers, selected subcovers, quotient equality, countable choice, host real representatives, or ambient Real completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Compact real uniform modulus finite-net extraction]
\label{thm:compact-real-uniform-modulus-finite-net-extraction}
Every finite-net extraction from an accepted $\CompactRealUniformModulusUp$ carrier factors through
$$
R,M,K \longmapsto W,A,U .
$$
The compact metric row $K$ may hand off only the displayed compact real source to finite StreamName windows, RegSeqRat readback, and the exposed uniform-modulus row. It does not export open-cover compactness, a choice-selected subcover, quotient syntax equality for reals, or ambient Real completeness.
\end{theorem}
\begin{proof}
Start from the carrier projection. The extraction request has no source other than the displayed real source, real-metric comparison, and compact-metric finite-data rows $R,M,K$.

The finite windows $W$ and rational readback $A$ are read at the tolerance governed by $U$. Thus the extraction is a local finite route through displayed compact real data and the visible uniform-modulus row.

The structural rows preserve the same route. Because no row stores an open cover, selected subcover, quotient equality proof, choice schedule, or ambient completeness witness, the extraction cannot be read as any of the excluded principles.
\end{proof}

\falsifiablePrediction{Every $\CompactRealUniformModulusUp$ read that exports a uniform-modulus or finite-net result without first exposing real source data, RealMetric comparison, CompactMetric finite data, StreamName windows, RegSeqRat readback, target Real sealing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert is outside the carrier surface.}

\independenceWitness{Nearest siblings: real, real-metric, uniform-modulus, compact-metric, streamname, and regseqrat. $\CompactRealUniformModulusUp$ is not bijective to any listed sibling because it jointly carries compact real source data, metric comparison, finite compact metric evidence, finite windows, rational readback, uniform-modulus control, target Real sealing, transport, replay, provenance, and local naming as one packet.}

\closureat{CompactRealUniformModulusUp}{seedStr}

\begin{closurestatus}{\CompactRealUniformModulusUp}
  \constructivestory{A $\CompactRealUniformModulusUp$ packet is generated from Real source rows, RealMetric comparison rows, a UniformModulus row, CompactMetric finite data, StreamName finite windows, RegSeqRat readback, a Real seal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-real-uniform-modulus-carrier}, \autoref{thm:compact-real-uniform-modulus-namecert-obligations}, and \autoref{thm:compact-real-uniform-modulus-finite-net-extraction} seed the finite compact real uniform-modulus route over metric comparison, compact finite data, windows, readback, seal, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, countable choice, selected finite subcovers, quotient real equality, host real representatives, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, RealMetric comparison exactness, UniformModulus tolerance exactness, CompactMetric finite-data admission, StreamName window replay, RegSeqRat readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactRealUniformModulusUp.}
\end{closurestatus}
