\chapter{A Concrete Naming Certificate for $\RegularCauchySumTailEstimateUp$}
\label{ch:concrete-instances-regular-cauchy-sum-tail-estimate-namecert}
\origin{human}

Following Bishop and Bridges on constructive Cauchy estimates, the $\RegularCauchySumTailEstimateUp$ packet records the finite tail-estimate route by which two regular rational Cauchy sources, their aligned tail windows, and the displayed pointwise-sum budget become a real-facing seal. It sits over the Cauchy carrier of \autoref{ch:concrete-instances-cauchy-namecert}, the regular rational source of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}, the dyadic tolerance arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the binary sum surface of \autoref{ch:concrete-instances-regularcauchysum-namecert}. The packet names finite tail estimates for the sum route; it is not a host limit theorem, quotient stream equality, selected convergence witness, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy sum tail-estimate carrier]
\label{def:regular-cauchy-sum-tail-estimate-carrier}
A $\RegularCauchySumTailEstimateUp$ carrier is a finite $\BHist$ packet
$$
T=(A,B,W_A,W_B,D_A,D_B,S,E,R,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ and $B$ are the two accepted $\RegSeqRatUp$ source rows whose tails are compared on the same precision request.
\item $W_A$ and $W_B$ are the aligned $\StreamNameUp$ tail windows opened for the two sources.
\item $D_A$ and $D_B$ are the $\DyadicRatCoreUp$ tolerance rows attached to the two tail estimates.
\item $S$ is the $\RegularCauchySumUp$ row that combines the two displayed source windows through pointwise dyadic addition.
\item $E$ is the finite error ledger recording the displayed tail bound for the sum after both source bounds have been read.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the sum window and its tail ledger.
\item $Q$ is the $\RealUp$ seal row, exposed only after $A,B,W_A,W_B,D_A,D_B,S,E,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the two sources, windows, dyadic rows, sum row, ledger, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a tail request through the same rows.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchySumTailEstimateUp$, $\CauchyUp$, $\RegSeqRatUp$, $\RegularCauchySumUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchySumTailEstimateUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows. It has no coordinate for quotient streams, selected limits, host real equality, trichotomy, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[Regular Cauchy sum tail-estimate NameCert obligations]
\label{thm:regular-cauchy-sum-tail-estimate-namecert-obligations}
For every accepted $\RegularCauchySumTailEstimateUp$ carrier
$$
T=(A,B,W_A,W_B,D_A,D_B,S,E,R,Q,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchySumTailEstimateUp}$ surface consists exactly of carrier admission for the two regular rational sources, aligned tail windows, dyadic tolerance rows, the $\RegularCauchySumUp$ row, the finite error ledger, regular rational readback, real seal, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-sum-tail-estimate-carrier}. The accepted packet exposes precisely the rows $A,B,W_A,W_B,D_A,D_B,S,E,R,Q,H,C,P,N$, so carrier admission and local naming have no hidden coordinates.

The tail estimate is then read in order. The two source rows $A,B$ are inspected through the aligned windows $W_A,W_B$ and the dyadic tolerance rows $D_A,D_B$ before the pointwise-sum row $S$ is available.

The finite ledger $E$ records the displayed output bound for the summed tail, and the readback row $R$ is downstream of that ledger. The real seal $Q$ is therefore reached only through the displayed regular rational route.

Finally, $H,C,P,N$ transport, replay, source, and name the same finite route. Since no row stores quotient streams, selected limits, host equality, trichotomy, countable choice, or ambient completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy sum tail-estimate real seal route]
\label{thm:regular-cauchy-sum-tail-estimate-real-seal-route}
Every $\RealUp$ consumer of an accepted $\RegularCauchySumTailEstimateUp$ packet reaches the seal row $Q$ only after the two $\RegSeqRatUp$ source rows, aligned $\StreamNameUp$ tail windows, $\DyadicRatCoreUp$ tolerance rows, $\RegularCauchySumUp$ row, finite error ledger, and output $\RegSeqRatUp$ readback row have been displayed. The route exports no quotient stream equality, selected limit, host real equality, trichotomy, countable-choice schedule, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:regular-cauchy-sum-tail-estimate-namecert-obligations}. They force the two source rows and their aligned windows to be visible before the sum row or output readback is consumed.

Read the sum route through \autoref{ch:concrete-instances-regularcauchysum-namecert}. The binary sum packet supplies only the displayed pointwise-addition and finite-budget surface, so the tail estimate remains tied to $D_A,D_B,S,E$.

The output readback $R$ is therefore a regular rational row over the displayed sum ledger. The real seal $Q$ is downstream of $R$, while $H,C,P,N$ preserve the same route under transport, replay, provenance, and local naming.

No carrier coordinate supplies the excluded host or choice principles. Hence every Real-facing read of the packet is the finite route through Cauchy, RegSeqRat, DyadicRatCore, RegularCauchySum, and Real rows.
\end{proof}

\closureat{RegularCauchySumTailEstimateUp}{seedStr}

\begin{closurestatus}{\RegularCauchySumTailEstimateUp}
  \constructivestory{A $\RegularCauchySumTailEstimateUp$ packet is generated from two RegSeqRat source rows, aligned StreamName tail windows, DyadicRatCore tolerance rows, a RegularCauchySum row, a finite error ledger, RegSeqRat readback, a RealUp seal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-sum-tail-estimate-carrier}, \autoref{thm:regular-cauchy-sum-tail-estimate-namecert-obligations}, and \autoref{thm:regular-cauchy-sum-tail-estimate-real-seal-route} seed the finite L10 tail-estimate route for regular Cauchy sums.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient stream equality, selected limits, host real equality, trichotomy, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, aligned tail-window coverage, dyadic tolerance exactness, sum-ledger stability, readback handoff, real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchySumTailEstimateUp.}
\end{closurestatus}
