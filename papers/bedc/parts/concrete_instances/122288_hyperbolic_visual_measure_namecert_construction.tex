\chapter{A Concrete Naming Certificate for $\HyperbolicVisualMeasureUp$}
\label{ch:concrete-instances-hyperbolic-visual-measure-namecert}
\origin{ai}

The $\HyperbolicVisualMeasureUp$ packet realizes the boundary-measure branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as a finite NameCert surface. It sits after the visual-boundary rows of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, the boundary-shadow window of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the metric comparison surface of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, and the terminal scalar seal of \autoref{ch:concrete-instances-real-namecert}. The packet names visual weights only as displayed finite shadow-window data; it does not construct a host sigma-algebra, a completed measure space, a quotient boundary, a global Patterson--Sullivan density, or ambient analytic regularity.

\begin{definition}[Hyperbolic visual measure carrier]
\label{def:hyperbolic-visual-measure-carrier}
A $\HyperbolicVisualMeasureUp$ carrier is a finite $\BHist$ packet
$$
V=(B,S,M,R,W,T,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\HyperbolicVisualBoundaryUp$ row, exposing the boundary face visible to the observer.
\item $S$ is the $\HyperbolicBoundaryShadowUp$ row, listing the finite shadow windows on which visual weights are read.
\item $M$ is the $\HyperbolicMetricUp$ comparison row attached to the same displayed disk or metric source.
\item $R$ is the $\RealUp$ scalar-readback row for finite nonnegative weights and total-window comparisons.
\item $W$ is the visual-weight table, assigning a displayed scalar only to shadow windows listed by $S$.
\item $T$ is the finite-total ledger, recording the finite sum of weights over the current visual window and refusing unlisted boundary regions.
\item $F$ is the TasteGate-facing field-faithfulness row, requiring the same packet to support Nontrivial and FieldFaithful readings before a scalar consumer treats $R$ as a faithful weight surface.
\item $H$ records componentwise $\hsame$ transport for boundary, shadow, metric, scalar, weight, total, TasteGate-facing, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a visual-measure request through $B,S,M,R,W,T,F,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicVisualMeasureUp$, $\HyperbolicVisualBoundaryUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicVisualMeasureUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,S,M,R,W,T,F,H,C,P,N$. It has no coordinate for completed measures, host measurable subsets, quotient ideal boundary equality, selected densities, or ambient analytic regularity.
\end{definition}

\begin{theorem}[Hyperbolic visual measure NameCert obligations]
\label{thm:hyperbolic-visual-measure-namecert-obligations}
For every accepted $\HyperbolicVisualMeasureUp$ carrier
$$
V=(B,S,M,R,W,T,F,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicVisualMeasureUp}$ surface consists exactly of carrier admission for the visual-boundary row, boundary-shadow window, hyperbolic metric comparison, Real scalar readback, visual-weight table, finite-total ledger, TasteGate-facing field-faithfulness row, componentwise transport, displayed replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-visual-measure-carrier}. The boundary, shadow, metric, scalar, weight, total, and TasteGate-facing rows are $B,S,M,R,W,T,F$, while $H,C,P,N$ transport, replay, source, and name the same packet.

The shadow row $S$ bounds the visual windows available to the measure read. The scalar row $R$ and finite-total row $T$ then expose only finite weight comparisons on those displayed windows.

Since the packet has no coordinate for completed measures, host measurable subsets, quotient boundary equality, selected densities, or ambient regularity, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Hyperbolic visual measure shadow support]
\label{thm:hyperbolic-visual-measure-shadow-support}
Every visual-measure read from an accepted $\HyperbolicVisualMeasureUp$ packet is supported on the finite boundary-shadow windows listed by $S$. A consumer may sum weights through $T$ only after displaying the visual-boundary row $B$, the shadow row $S$, the metric row $M$, and the scalar readback row $R$.
\end{theorem}
\begin{proof}
Begin with the displayed boundary and shadow rows. The visual-boundary row names the observer-visible boundary face, and the boundary-shadow row lists the finite windows that may receive weights.

The metric row supplies the comparison data used to index the shadow windows, while the Real row supplies the scalar readback. The visual-weight table $W$ assigns values only to those listed windows.

The finite-total ledger $T$ can therefore sum only the displayed shadow support. Transport and replay preserve this route, so a consumer cannot turn the packet into a completed measure on an unlisted boundary region.
\end{proof}

\begin{theorem}[Hyperbolic visual measure TasteGate route]
\label{thm:hyperbolic-visual-measure-tastegate-route}
The TasteGate-facing obligations for $\HyperbolicVisualMeasureUp$ are bound to the same finite packet
$$
B,S,M,R,W,T,F,H,C,P,N .
$$
In particular, \texttt{lean4/BEDC/Derived/HyperbolicVisualMeasureUp/TasteGate.lean} must provide Nontrivial and FieldFaithful instances for this carrier before a downstream scalar consumer treats the visual-weight table as a faithful measurement row.
\end{theorem}
\begin{proof}
Read the field-faithfulness row $F$ from the carrier. It is displayed beside the boundary, shadow, metric, scalar, weight, and total rows, so it cannot be supplied by a different hyperbolic sibling.

The provenance row $P$ identifies the consumed visual-boundary, boundary-shadow, hyperbolic-metric, and Real rows. The replay row $C$ preserves their order before a scalar consumer can inspect $W$ or $T$.

Thus the Nontrivial and FieldFaithful requirements are obligations on the local $\HyperbolicVisualMeasureUp$ packet. They do not assert a global measure theory, a completed boundary, or an ambient analytic density.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicVisualMeasureUp$ read displays HyperbolicVisualBoundaryUp, HyperbolicBoundaryShadowUp, HyperbolicMetricUp, RealUp scalar readback, finite visual weights, finite-total ledgers, TasteGate-facing field-faithfulness rows, hsame transport, Cont replay, Pkg provenance, and local NameCert data before any visual measure consumer may read the packet.}

\independenceWitness{hyperbolic-visual-boundary, hyperbolic-boundary-shadow, hyperbolic-metric, hyperbolic-observer-expansion, hyperbolic-visual-metric: none of these siblings jointly carries finite visual-weight rows, finite-total ledgers, TasteGate-facing field-faithfulness rows, transport, replay, provenance, and local NameCert data as one visual-measure packet.}

\closureat{HyperbolicVisualMeasureUp}{seedStr}

\begin{closurestatus}{\HyperbolicVisualMeasureUp}
  \constructivestory{A $\HyperbolicVisualMeasureUp$ packet is generated from HyperbolicVisualBoundaryUp boundary rows, HyperbolicBoundaryShadowUp finite shadow windows, HyperbolicMetricUp comparison rows, RealUp scalar readbacks, finite visual-weight rows, finite-total ledgers, TasteGate-facing Nontrivial and FieldFaithful obligations, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-visual-measure-carrier}, \autoref{thm:hyperbolic-visual-measure-namecert-obligations}, \autoref{thm:hyperbolic-visual-measure-shadow-support}, and \autoref{thm:hyperbolic-visual-measure-tastegate-route} seed the finite visual-measure route over boundary, shadow, metric, scalar, field-faithfulness, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host sigma-algebras, completed measure spaces, quotient boundary equality, selected densities, global Patterson--Sullivan theory, ambient analytic regularity, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, shadow support exactness, finite-total exactness, TasteGate Nontrivial and FieldFaithful rows, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicVisualMeasureUp.}
\end{closurestatus}
