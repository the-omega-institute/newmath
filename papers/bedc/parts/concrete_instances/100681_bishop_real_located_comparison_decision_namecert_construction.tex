\chapter{A Concrete Naming Certificate for $\BishopRealLocatedComparisonDecisionUp$}
\label{ch:concrete-instances-bishop-real-located-comparison-decision-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\BishopRealLocatedComparisonDecisionUp$ packet records the finite local decision surface available when two located real presentations are compared through a common Bishop-style Cauchy-name window. It sits between the Bishop comparison packet of \autoref{ch:concrete-instances-bishoprealcomparison-namecert}, the located comparison surface of \autoref{ch:concrete-instances-located-real-comparison-namecert}, the located-real endpoint surface of \autoref{ch:concrete-instances-locatedreal-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a downstream decision interface for order, interval, and apartness consumers without exporting trichotomy, quotient real equality, selected representatives, countable choice, ambient $\RealUp$ completeness, or a standard bridge.

\begin{definition}[Bishop real located comparison decision carrier]
\label{def:bishop-real-located-comparison-decision-carrier}
A $\BishopRealLocatedComparisonDecisionUp$ carrier is a finite $\BHist$ packet
$$
Q=(L_0,L_1,W,R_0,R_1,D_0,D_1,B,J,H,C,P,N)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $L_0$ and $L_1$ are the two $\LocatedRealUp$ endpoint rows read before any comparison decision is exposed;
\item $W$ is the shared $\StreamNameUp$ finite observation window used for both endpoints at the requested tolerance;
\item $R_0$ and $R_1$ are the two $\RegSeqRatUp$ readback rows attached to the same window;
\item $D_0$ and $D_1$ are the two $\DyadicRatCoreUp$ endpoint and tolerance ledgers consumed by the comparison row;
\item $B$ is the $\BishopRealComparisonUp$ row that records the Bishop-style finite comparison request over the same endpoint, readback, and dyadic rows;
\item $J$ records exactly one displayed local decision row at the chosen finite window: a left-gap row, a right-gap row, or an unresolved tolerance-band row;
\item $H$ records componentwise $\hsame$ transport for the endpoint rows, shared window, readbacks, dyadic ledgers, Bishop comparison row, local decision row, continuation replay, provenance, and local naming row;
\item $C$ records displayed $\Cont$ replay from a comparison request through $L_0,L_1,W,R_0,R_1,D_0,D_1,B,J,H,P,N$;
\item $P$ is the $\Pkg$ provenance over $\BishopRealLocatedComparisonDecisionUp$, $\LocatedRealUp$, $\LocatedRealComparisonUp$, $\BishopRealComparisonUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\BishopRealLocatedComparisonDecisionUp}$ row.
\end{enumerate}
The carrier predicate asserts that these rows form one finite local comparison-decision packet. The classifier compares accepted packets only through $L_0,L_1,W,R_0,R_1,D_0,D_1,B,J,H,C,P,N$, so it has no coordinate for global real trichotomy, quotient real equality, selected endpoint representatives, countable choice, ambient $\RealUp$ completeness, or bridge data.
\end{definition}

\begin{theorem}[Bishop real located comparison decision NameCert obligations]
\label{thm:bishop-real-located-comparison-decision-namecert-obligations}
For every accepted $\BishopRealLocatedComparisonDecisionUp$ carrier
$$
Q=(L_0,L_1,W,R_0,R_1,D_0,D_1,B,J,H,C,P,N),
$$
the local $\NameCert_{\BishopRealLocatedComparisonDecisionUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the two located-real endpoints, shared finite window, two regular readbacks, two dyadic endpoint and tolerance ledgers, Bishop comparison row, local decision row, transport, replay, provenance, and local naming rows;
\item shared-window discipline, requiring both endpoints and both readbacks to use the same $W$ before $B$ and $J$ are public;
\item readback exactness, requiring $R_0,R_1,D_0,D_1$ to supply precisely the rational observations, dyadic enclosures, and tolerance budget consumed by $B$ and $J$;
\item decision-row boundedness, requiring $J$ to contain exactly one of the left-gap, right-gap, or unresolved tolerance-band rows at the displayed window;
\item transport and replay, requiring $H$ and $C$ to preserve and replay the same finite endpoint, window, readback, dyadic, Bishop comparison, and decision rows;
\item non-escape, refusing trichotomy, quotient real equality, selected endpoint representatives, countable choice, ambient $\RealUp$ completeness, Lean verification, and bridge checking as local NameCert data.
\end{enumerate}
No comparison-decision obligation is stored outside these six rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:bishop-real-located-comparison-decision-carrier}. The projection exposes exactly the two located endpoints, the shared window, two regular readbacks, two dyadic ledgers, the Bishop comparison row, the local decision row, and the structural rows.

Shared-window discipline is read from $W$. Since both endpoint presentations and both readbacks use the same finite schedule before $B$ and $J$ are public, no consumer can compare one endpoint through a hidden precision or through an unrecorded stream request.

Readback exactness is the role of $R_0,R_1,D_0,D_1$. These rows supply the rational observations, dyadic enclosures, and tolerance budget consumed by the Bishop comparison row and by the local decision row.

Decision-row boundedness follows from the definition of $J$. The packet admits exactly one displayed row among left-gap, right-gap, and unresolved tolerance band at the chosen finite window, so the classifier has a public finite case split rather than a host-level real comparison.

Finally, $H$ and $C$ transport and replay only the displayed rows, while $P$ and $N$ source and name the same packet. Since the carrier has no coordinate for trichotomy, quotient real equality, selected representatives, countable choice, ambient completeness, Lean verification, or bridge checking, no further local obligation can be read from it.
\end{proof}

\begin{theorem}[Bishop real located comparison decision trichotomy refusal]
\label{thm:bishop-real-located-comparison-decision-window-trichotomy-refusal}
The local three-row decision carried by an accepted $\BishopRealLocatedComparisonDecisionUp$ packet is not constructive real trichotomy. The left-gap and right-gap rows are finite positive-gap observations at the displayed window, while the unresolved tolerance-band row is public packet data rather than a failed proof, a hidden search state, or an omitted real-order case.
\end{theorem}
\begin{proof}
Read the decision coordinate $J$ in the carrier. It stores one row at one displayed finite window, and its alternatives are left gap, right gap, and unresolved tolerance band.

The two gap rows are finite observations. They are bounded by the same $\StreamNameUp$ window, the two $\RegSeqRatUp$ readbacks, the two $\DyadicRatCoreUp$ tolerance ledgers, and the $\BishopRealComparisonUp$ row named in the packet.

The unresolved row is also public data. It records that the chosen finite window has not separated the endpoints beyond the displayed tolerance, and it is transported and replayed by $H$ and $C$ just like either gap row.

Constructive real trichotomy would decide a global order alternative for the sealed real endpoints. This packet instead exposes a finite local row before the $\RealUp$ seal and keeps quotient equality, host real order, selected representatives, countable choice, and ambient completeness outside the carrier.
\end{proof}

\begin{theorem}[Bishop real located comparison decision Real seal boundary]
\label{thm:bishop-real-located-comparison-decision-real-seal-boundary}
Every $\RealUp$-facing consumer of an accepted $\BishopRealLocatedComparisonDecisionUp$ packet must pass through the common $\StreamNameUp$ window, the two $\RegSeqRatUp$ readbacks, the $\DyadicRatCoreUp$ tolerance ledgers, the $\BishopRealComparisonUp$ row, and the displayed decision row before reading any terminal real-name seal. No consumer obtains a detached real comparison, quotient equality, selected endpoint representative, countable-choice selector, ambient completeness witness, or bridge row from the packet.
\end{theorem}
\begin{proof}
Project an accepted carrier $Q=(L_0,L_1,W,R_0,R_1,D_0,D_1,B,J,H,C,P,N)$. The only endpoint-facing entries are $L_0,L_1$ together with the shared finite window and readback rows.

A real-facing consumer next reads the common $\StreamNameUp$ window $W$, the two regular readbacks $R_0,R_1$, and the two dyadic tolerance ledgers $D_0,D_1$. These rows are the only finite observations available before the Bishop comparison row $B$ and the decision row $J$ are public.

The comparison and decision rows then determine the visible local surface. The consumer receives either a displayed positive-gap row or a displayed unresolved tolerance-band row, always routed through the same finite data.

Finally, $H,C,P,N$ transport, replay, source, and name that route before any terminal $\RealUp$ seal is consumed. Because the carrier has no coordinate for detached comparison, quotient equality, selected endpoints, countable choice, ambient completeness, or bridge data, every Real-facing consumer remains inside the displayed finite boundary.
\end{proof}

\closureat{BishopRealLocatedComparisonDecisionUp}{seedStr}

\begin{closurestatus}{\BishopRealLocatedComparisonDecisionUp}
  \constructivestory{A $\BishopRealLocatedComparisonDecisionUp$ packet is generated from two displayed $\LocatedRealUp$ endpoint rows, one shared $\StreamNameUp$ window, two $\RegSeqRatUp$ readbacks, two $\DyadicRatCoreUp$ tolerance ledgers, a $\BishopRealComparisonUp$ row, exactly one local decision row, componentwise transport, displayed continuation replay, package provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-real-located-comparison-decision-carrier}, \autoref{thm:bishop-real-located-comparison-decision-namecert-obligations}, \autoref{thm:bishop-real-located-comparison-decision-window-trichotomy-refusal}, and \autoref{thm:bishop-real-located-comparison-decision-real-seal-boundary} seed the finite local decision surface over two $\LocatedRealUp$ endpoints, one shared $\StreamNameUp$ window, two $\RegSeqRatUp$ readbacks, two $\DyadicRatCoreUp$ tolerance ledgers, a $\BishopRealComparisonUp$ row, and exactly one displayed local decision row.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close global real trichotomy, quotient real equality, selected endpoint representatives, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, shared-window discipline, readback exactness, decision-row boundedness, transport and replay, trichotomy refusal, and Real seal boundary for BEDC.Derived.BishopRealLocatedComparisonDecisionUp.}
\end{closurestatus}
