\chapter{A Concrete Naming Certificate for $\HyperbolicObserverBoundaryShellUp$}
\label{ch:concrete-instances-hyperbolic-observer-boundary-shell-namecert}
\origin{ai}

The $\HyperbolicObserverBoundaryShellUp$ packet realizes the observer-expansion route of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as a finite boundary-shell NameCert surface. It sits beside the sector observer nerve of \autoref{ch:concrete-instances-hyperbolic-sector-observer-nerve-namecert}, the horosphere-flow ledger of \autoref{ch:concrete-instances-horosphere-flow-ledger-namecert}, and the boundary-shadow packet of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}. The packet binds sector rows, horosphere rows, boundary-shadow rows, transport rows, replay rows, provenance rows, and local naming into one observer-facing shell; it does not assert a completed hyperbolic space, quotient ideal-boundary equality, a physical observer, a selected geodesic ray, or ambient $\RealUp$ completeness.

\begin{definition}[Hyperbolic observer boundary shell carrier]
\label{def:hyperbolic-observer-boundary-shell-carrier}
A $\HyperbolicObserverBoundaryShellUp$ carrier is a finite $\BHist$ packet
$$
B=(S,F,W,T,R,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\HyperbolicSectorObserverNerveUp$ sector row, recording the finite sector windows seen by the observer.
\item $F$ is the $\HorosphereFlowLedgerUp$ flow row, recording the boundary-facing horosphere provenance attached to the same observer window.
\item $W$ is the $\HyperbolicBoundaryShadowUp$ boundary-shadow row, naming the visible boundary shadow inspected by the shell.
\item $T$ is the transport row joining sector, horosphere, and shadow reads without collapsing them into equality.
\item $R$ is the replay row exposing the finite observer-expansion read through displayed $\Cont$ steps.
\item $Q$ is the refusal row excluding completed boundary, physical observer, selected ray, quotient ideal-boundary equality, and ambient completeness readings.
\item $H$ records componentwise $\hsame$ transport for $S,F,W,T,R,Q,C,P,N$.
\item $C$ records displayed $\Cont$ replay from an observer-boundary-shell request through the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicObserverBoundaryShellUp$, $\HyperbolicSectorObserverNerveUp$, $\HorosphereFlowLedgerUp$, $\HyperbolicBoundaryShadowUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicObserverBoundaryShellUp}$ row.
\end{enumerate}
The Lean TasteGate file for $\HyperbolicObserverBoundaryShellUp$ must supply $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances for this carrier surface before a checked classifier can replace the paper-only packet.
\end{definition}

\begin{theorem}[Hyperbolic observer boundary shell NameCert obligations]
\label{thm:hyperbolic-observer-boundary-shell-namecert-obligations}
For every accepted $\HyperbolicObserverBoundaryShellUp$ carrier
$$
B=(S,F,W,T,R,Q,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicObserverBoundaryShellUp}$ surface consists exactly of sector observer-nerve exposure through $S$, horosphere-flow exposure through $F$, boundary-shadow exposure through $W$, transport exposure through $T$, finite replay through $R,C$, refusal exactness through $Q$, componentwise $\hsame$ transport through $H$, $\Pkg$ provenance through $P$, and local naming through $N$. Every observer-expansion consumer must read the sector, horosphere, and boundary-shadow rows before it treats the shell as accepted.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-observer-boundary-shell-carrier}. The rows $S,F,W,T,R,Q,H,C,P,N$ are the complete visible packet.

The sector, horosphere, and boundary-shadow rows are independent displayed coordinates. The transport row $T$ may relate them, but it cannot identify them as one host boundary object or erase the provenance of any row.

Replay through $R,C$ and refusal through $Q$ keep the observer-expansion read finite. The refusal row excludes exactly the completed boundary, physical observer, selected ray, quotient ideal-boundary equality, and ambient completeness readings.

Finally $H,P,N$ preserve, source, and name the same carrier. Hence the listed rows exhaust the NameCert obligations for the boundary shell.
\end{proof}

\begin{theorem}[Hyperbolic observer boundary shell vision handoff]
\label{thm:hyperbolic-observer-boundary-shell-vision-handoff}
The $\HyperbolicObserverBoundaryShellUp$ packet concretely realizes the hyperbolic observer-expansion vision by the finite route
$$
\HyperbolicSectorObserverNerveUp,\ \HorosphereFlowLedgerUp,\ \HyperbolicBoundaryShadowUp \longmapsto T,R,Q,H,C,P,N .
$$
Any accepted handoff to an observer-expansion consumer must expose sector, horosphere, boundary-shadow, transport, replay, refusal, provenance, and NameCert rows, and it exports no completed hyperbolic boundary, physical observer trajectory, selected geodesic ray, quotient ideal-boundary equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:hyperbolic-observer-boundary-shell-namecert-obligations}. They force the consumer to read $S,F,W$ before it can read transport, replay, refusal, provenance, or naming rows.

The vision handoff is the displayed route from sector windows, horosphere provenance, and boundary shadows into a single shell packet. The rows remain separate, so the shell records observer-facing geometry without converting the boundary side into a completed host object.

The refusal row $Q$ blocks the forbidden readings, while $H,C,P,N$ transport, replay, source, and name the same finite packet. This proves the stated handoff from the vision chapter to the concrete NameCert surface.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicObserverBoundaryShellUp$ read exposes HyperbolicSectorObserverNerve, HorosphereFlowLedger, HyperbolicBoundaryShadow, transport, replay, refusal, hsame, Cont, Pkg, and local NameCert rows before any observer-boundary shell consumer is served.}

\independenceWitness{hyperbolic-sector-observer-nerve, horosphere-flow-ledger, hyperbolic-boundary-shadow, hyperbolic-boundary-shadow-transport, hyperbolic-observer-expansion: $\HyperbolicObserverBoundaryShellUp$ is not bijective to any listed sibling because it jointly binds sector windows, horosphere-flow provenance, boundary-shadow windows, shell transport, replay, refusal, provenance, and local naming in one carrier.}

\closureat{HyperbolicObserverBoundaryShellUp}{seedStr}

\begin{closurestatus}{\HyperbolicObserverBoundaryShellUp}
  \constructivestory{A $\HyperbolicObserverBoundaryShellUp$ packet is generated from HyperbolicSectorObserverNerve rows, HorosphereFlowLedger rows, HyperbolicBoundaryShadow rows, shell transport, replay, refusal exactness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-observer-boundary-shell-carrier}, \autoref{thm:hyperbolic-observer-boundary-shell-namecert-obligations}, and \autoref{thm:hyperbolic-observer-boundary-shell-vision-handoff} seed the finite observer-boundary shell route from the hyperbolic observer-expansion vision.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, completed hyperbolic boundary, physical observer trajectory, selected geodesic ray, quotient ideal-boundary equality, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, sector exposure, horosphere-flow exposure, boundary-shadow exposure, transport exactness, replay, refusal exactness, provenance, Nontrivial and FieldFaithful TasteGate instances, and local naming for BEDC.Derived.HyperbolicObserverBoundaryShellUp.}
\end{closurestatus}
