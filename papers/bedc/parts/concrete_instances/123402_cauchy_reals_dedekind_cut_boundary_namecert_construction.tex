\chapter{A Concrete Naming Certificate for $\CauchyRealsDedekindCutBoundaryUp$}
\label{ch:concrete-instances-cauchy-reals-dedekind-cut-boundary-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CauchyRealsDedekindCutBoundaryUp$ packet records the finite boundary by which a regular Cauchy real name may be read with lower-cut language without constructing $\RealUp$ as a Dedekind cut. It sits over the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-name window of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the Bishop L10 comparison discipline of \autoref{ch:concrete-instances-bishop-l10-comparison-principle-namecert}. The packet is a one-way readout boundary for textbook cut notation; it is not a Dedekind-cut construction of $\RealUp$, not quotient equality of real names, and not an ambient completeness principle.

\begin{definition}[CauchyRealsDedekindCutBoundary carrier]
\label{def:cauchy-reals-dedekind-cut-boundary-carrier}
A $\CauchyRealsDedekindCutBoundaryUp$ carrier is a finite $\BHist$ packet
$$
B=(D,S,R,E,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger carrying the dyadic radius, the inspected rational endpoint, and the located comparison budget.
\item $S$ is the $\StreamNameUp$ finite-window row naming the inspected prefix of the regular Cauchy name.
\item $R$ is the $\RegSeqRatUp$ readback row accepted from $D$ and $S$.
\item $E$ is the terminal $\RealUp$ seal for the same regular Cauchy name, available only after $D,S,R$ have been displayed.
\item $L$ is the $\BishopLTenComparisonPrincipleUp$ comparison row that records the lower-cut readout as displayed dyadic tolerance plus located comparison data.
\item $H$ records componentwise $\hsame$ transport for the dyadic, stream, regular-readback, Real-seal, comparison, continuation, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay of the one-way route from regular Cauchy data to lower-cut readout.
\item $P$ is the $\Pkg$ provenance over $\CauchyRealsDedekindCutBoundaryUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BishopLTenComparisonPrincipleUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CauchyRealsDedekindCutBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,S,R,E,L,H,C,P,N$. It has no coordinate for a Dedekind-cut construction of $\RealUp$, quotient equality, trichotomy, a selected representative, countable choice, or ambient completeness.
\end{definition}

\begin{theorem}[CauchyRealsDedekindCutBoundary NameCert obligations]
\label{thm:cauchy-reals-dedekind-cut-boundary-namecert-obligations}
For every accepted $\CauchyRealsDedekindCutBoundaryUp$ carrier $B=(D,S,R,E,L,H,C,P,N)$, the local $\NameCert_{\CauchyRealsDedekindCutBoundaryUp}$ surface consists exactly of dyadic tolerance admission through $D$, finite stream-window exposure through $S$, regular rational readback through $R$, terminal Real sealing through $E$, Bishop L10 located comparison through $L$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-reals-dedekind-cut-boundary-carrier}. The analytic rows are precisely $D,S,R,E,L$, and the structural rows are precisely $H,C,P,N$.

The dependency route is ordered. The dyadic tolerance ledger and finite stream window are displayed before the regular rational readback; the terminal Real seal is attached to that same readback; and the lower-cut phrase is admitted only through the located comparison row $L$.

The rows $H,C,P,N$ transport, replay, source, and name that same finite route. Since the carrier contains no further coordinate, these rows exhaust the local NameCert obligation surface.
\end{proof}

\begin{theorem}[Regular Cauchy to lower-cut one-way route]
\label{thm:cauchy-reals-dedekind-cut-boundary-one-way-route}
Every accepted regular Cauchy real-name packet consumed through $\CauchyRealsDedekindCutBoundaryUp$ exposes a lower-cut readout only along the displayed chain
$$
\DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp \longmapsto \RealUp \longmapsto \BishopLTenComparisonPrincipleUp .
$$
Equivalently, a downstream lower-cut consumer receives only the dyadic tolerance row $D$, the finite stream window $S$, the regular rational readback $R$, the terminal Real seal $E$, and the located comparison row $L$, transported and replayed through $H,C,P,N$.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:cauchy-reals-dedekind-cut-boundary-namecert-obligations}. They place $D$ and $S$ before $R$, so the regular Cauchy name is public only through displayed dyadic tolerances and inspected finite windows.

The terminal seal $E$ is read after that regular rational row. The comparison row $L$ then records the lower-cut assertion as a Bishop L10 located comparison against the same dyadic data, rather than as an independent cut carrier.

Finally, $H,C,P,N$ preserve the same route under transport, replay, provenance, and local naming. Thus the lower-cut readout is one-way from accepted regular Cauchy data to displayed comparison data.
\end{proof}

\begin{theorem}[CauchyRealsDedekindCutBoundary nonescape]
\label{thm:cauchy-reals-dedekind-cut-boundary-nonescape}
Every lower-cut readout exported by an accepted $\CauchyRealsDedekindCutBoundaryUp$ packet factors through $D,S,R,E,L,H,C,P,N$. The packet exports no Dedekind-cut construction of $\RealUp$, no quotient equality of real names, no trichotomy principle, no selected representative, no countable-choice schedule, and no ambient completeness coordinate.
\end{theorem}
\begin{proof}
Read the packet in dependency order. Rows $D,S,R$ display the dyadic tolerance, finite stream window, and regular rational readback before any Real-facing or cut-facing sentence can be inspected.

Apply the one-way route theorem, \autoref{thm:cauchy-reals-dedekind-cut-boundary-one-way-route}. It places the terminal seal $E$ before the located comparison row $L$ and keeps both rows tied to the same displayed dyadic and stream data.

No excluded object is among the carrier rows. The structural rows $H,C,P,N$ can only transport, replay, source, and name the listed route, so the lower-cut readout cannot escape to a Dedekind construction, quotient equality, trichotomy, selected representative, countable choice, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyRealsDedekindCutBoundaryUp$ lower-cut read factors through one DyadicRatCore tolerance ledger, one StreamName finite window, one RegSeqRat readback, one RealUp seal, one Bishop L10 located comparison row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, bishop-lten-comparison-principle: $\CauchyRealsDedekindCutBoundaryUp$ is not bijective to any listed sibling because it jointly records a regular Cauchy readout and a one-way lower-cut comparison boundary while refusing Dedekind construction, quotient equality, trichotomy, selected representatives, countable choice, and ambient completeness.}

\closureat{CauchyRealsDedekindCutBoundaryUp}{seedStr}

\begin{closurestatus}{\CauchyRealsDedekindCutBoundaryUp}
  \constructivestory{A $\CauchyRealsDedekindCutBoundaryUp$ packet is generated from a DyadicRatCore tolerance ledger, StreamName finite windows, RegSeqRat readback, a terminal RealUp seal, a Bishop L10 located comparison row, hsame transport, Cont replay, Pkg provenance, and local NameCert naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-reals-dedekind-cut-boundary-carrier}, \autoref{thm:cauchy-reals-dedekind-cut-boundary-namecert-obligations}, \autoref{thm:cauchy-reals-dedekind-cut-boundary-one-way-route}, and \autoref{thm:cauchy-reals-dedekind-cut-boundary-nonescape} seed the one-way regular-Cauchy-to-lower-cut boundary over DyadicRatCore, StreamName, RegSeqRat, Real, BishopLTenComparisonPrinciple, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a Dedekind-cut construction of $\RealUp$, quotient equality, trichotomy, selected representatives, countable choice, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic tolerance exactness, finite-window coverage, regular readback, Real seal routing, located comparison nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CauchyRealsDedekindCutBoundaryUp.}
\end{closurestatus}
