\begin{theorem}[CauchyProduct root source window totality]
\label{thm:cauchy-product-root-source-window-totality}
For every accepted $\CauchyProductUp$ packet, each product read first exposes the two $\RegularCauchyNameUp$ source rows through the paired $\StreamNameUp$ windows $W_X,W_Y$. The exposed rows are exactly the finite source-window route named by the carrier, the local $\NameCert_{\CauchyProductUp}$ obligations, the StreamName handoff, the root window obligations, and the shared tail-window surface of \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert}.
\end{theorem}
\begin{proof}
Read the carrier of \autoref{def:cauchy-product-namecert-source}. Its first source coordinates are $X,Y,W_X,W_Y$, with the two source rows displayed before the dyadic observations and before the product row.

Apply the NameCert obligations and the StreamName handoff. These force any consumer of the product packet to enter the two sources through $W_X,W_Y$ before it can inspect $M_X,M_Y,Q_X,Q_Y,P$, or $E$.

Finally use the root window obligations together with \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert}. The sibling tail-meet chapter supplies the same finite tail-window consumption pattern, so the product read has no alternate source route outside the paired StreamName windows.
\end{proof}

\begin{theorem}[CauchyProduct root budget product lock]
\label{thm:cauchy-product-root-budget-product-lock}
For every accepted $\CauchyProductUp$ packet, the dyadic product row $P$ is locked by the two source modulus rows $M_X,M_Y$ and the product budget ledger $L$ before the $\RegSeqRatUp$ classifier row $E$ is read. The lock is the finite route supplied by the carrier, the local NameCert obligations, the StreamName handoff, the root window obligations, the root budget obligations, and the budget triangle package.
\end{theorem}
\begin{proof}
Start from the paired source-window route. By source-window totality, $W_X,W_Y$ expose the two regular Cauchy sources before the dyadic observations and product row are available.

The root budget obligations then read $M_X,M_Y,Q_X,Q_Y,P,L$ as the budget route into the product classifier. The budget triangle package keeps that route compatible with the downstream product and observation-budget consumers.

Thus $E$ is reached only after $P$ has been formed inside the displayed dyadic core and after $L$ has recorded the two source modulus rows. No consumer can read the classifier through a separate product estimate, radius oracle, quotient stream, selected limit, host multiplication row, or ambient completeness coordinate.
\end{proof}

\begin{theorem}[CauchyProduct root source-window unblock certificate]
\label{thm:cauchy-product-root-unblock-certificate}
The source-window totality row, the budget product lock row, and the existing root budget triangle rows form the root-unblock certificate consumed by $\RealUp$-facing product consumers. The certificate is the finite $\CauchyProductUp$ route from $X,Y$ through $W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,L$ to $E$ and then to the displayed $\RealUp$ seal handoff; it exports no quotient stream, host multiplication primitive, selected limit, trichotomy row, independent real carrier, or ambient completeness witness.
\end{theorem}
\begin{proof}
First assemble the source side. The carrier, NameCert obligations, StreamName handoff, and source-window totality theorem expose the two $\RegularCauchyNameUp$ rows only through the paired StreamName windows.

Next assemble the product side. The root window obligations, root budget obligations, and budget product lock route the dyadic product row through the two source modulus rows and the product ledger before the classifier row is read.

Finally hand the joined route to the existing root budget triangle rows and the sibling finite-tail surface of \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert}. A $\RealUp$-facing product consumer therefore receives exactly the displayed finite route and has no coordinate for any excluded analytic object.
\end{proof}

\begin{theorem}[CauchyProduct source-window seal consumer exactness]
\label{thm:cauchy-product-source-window-seal-consumer-exactness}
For every accepted $\CauchyProductUp$ packet, any consumer that exports the product as a $\RealUp$ seal factors through the finite route
$$
X,Y \to W_X,W_Y \to M_X,M_Y,Q_X,Q_Y,P,L \to E .
$$
The factorization is forced by the source-window totality row, the budget product lock row, and the root-unblock certificate. Such a consumer exports no quotient stream, host multiplication primitive, selected limit, trichotomy row, independent real carrier, or ambient completeness witness.
\end{theorem}
\begin{proof}
First apply \autoref{thm:cauchy-product-root-source-window-totality}. Every product read reaches the two source rows only through the paired $\StreamNameUp$ windows $W_X,W_Y$, so the consumer has no direct source coordinate outside the displayed window route.

Next apply \autoref{thm:cauchy-product-root-budget-product-lock}. The dyadic product row is read through $M_X,M_Y,Q_X,Q_Y,P,L$ before the classifier row $E$, so the source modulus rows and product ledger are the only budgeted path into the $\RegSeqRatUp$ classifier.

Finally apply \autoref{thm:cauchy-product-root-unblock-certificate} together with the sibling finite-tail surface of \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert}. The downstream $\RealUp$-facing seal therefore receives exactly the stated finite route, and the excluded analytic objects have no exported coordinate in the accepted packet.
\end{proof}
