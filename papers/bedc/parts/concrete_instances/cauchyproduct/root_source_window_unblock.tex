\begin{theorem}[CauchyProduct root source window totality]
\label{thm:cauchy-product-root-source-window-totality}
For every accepted $\CauchyProductUp$ packet, each product read first exposes the two $\RegularCauchyNameUp$ source rows through the paired $\StreamNameUp$ windows $W_X,W_Y$. The exposed rows are exactly the finite source-window route named by the carrier, the local $\NameCert_{\CauchyProductUp}$ obligations, the StreamName handoff, the root window obligations, and the shared tail-window surface of \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert}.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_root\_source\_window\_totality}
\begin{proof}
Read the carrier of \autoref{def:cauchy-product-namecert-source}. Its first source coordinates are $X,Y,W_X,W_Y$, with the two source rows displayed before the dyadic observations and before the product row.

Apply the NameCert obligations and the StreamName handoff. These force any consumer of the product packet to enter the two sources through $W_X,W_Y$ before it can inspect $M_X,M_Y,Q_X,Q_Y,P$, or $E$.

Finally use the root window obligations together with \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert}. The sibling tail-meet chapter supplies the same finite tail-window consumption pattern, so the product read has no alternate source route outside the paired StreamName windows.
\end{proof}

\begin{theorem}[CauchyProduct root budget product lock]
\label{thm:cauchy-product-root-budget-product-lock}
For every accepted $\CauchyProductUp$ packet, the dyadic product row $P$ is locked by the two source modulus rows $M_X,M_Y$ and the product budget ledger $L$ before the $\RegSeqRatUp$ classifier row $E$ is read. The lock is the finite route supplied by the carrier, the local NameCert obligations, the StreamName handoff, the root window obligations, the root budget obligations, and the budget triangle package.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_root\_budget\_product\_lock}
\begin{proof}
Start from the paired source-window route. By source-window totality, $W_X,W_Y$ expose the two regular Cauchy sources before the dyadic observations and product row are available.

The root budget obligations then read $M_X,M_Y,Q_X,Q_Y,P,L$ as the budget route into the product classifier. The budget triangle package keeps that route compatible with the downstream product and observation-budget consumers.

Thus $E$ is reached only after $P$ has been formed inside the displayed dyadic core and after $L$ has recorded the two source modulus rows. No consumer can read the classifier through a separate product estimate, radius oracle, quotient stream, selected limit, host multiplication row, or ambient completeness coordinate.
\end{proof}

\begin{theorem}[CauchyProduct root source-window unblock certificate]
\label{thm:cauchy-product-root-unblock-certificate}
The source-window totality row, the budget product lock row, and the existing root budget triangle rows form the root-unblock certificate consumed by $\RealUp$-facing product consumers. The certificate is the finite $\CauchyProductUp$ route from $X,Y$ through $W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,L$ to $E$ and then to the displayed $\RealUp$ seal handoff; it exports no quotient stream, host multiplication primitive, selected limit, trichotomy row, independent real carrier, or ambient completeness witness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_root\_unblock\_certificate}
\begin{proof}
First assemble the source side. The carrier, NameCert obligations, StreamName handoff, and source-window totality theorem expose the two $\RegularCauchyNameUp$ rows only through the paired StreamName windows.

Next assemble the product side. The root window obligations, root budget obligations, and budget product lock route the dyadic product row through the two source modulus rows and the product ledger before the classifier row is read.

Finally hand the joined route to the existing root budget triangle rows and the sibling finite-tail surface of \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert}. A $\RealUp$-facing product consumer therefore receives exactly the displayed finite route and has no coordinate for any excluded analytic object.
\end{proof}

\begin{theorem}[CauchyProduct source-window seal consumer exactness]
\label{thm:cauchy-product-source-window-seal-consumer-exactness}
For every accepted $\CauchyProductUp$ packet, any consumer that exports the product as a $\RealUp$ seal factors through the finite route
$$
X,Y \to W_X,W_Y \to M_X,M_Y,Q_X,Q_Y,P,L \to E .
$$
The factorization is forced by the source-window totality row, the budget product lock row, and the root-unblock certificate. Such a consumer exports no quotient stream, host multiplication primitive, selected limit, trichotomy row, independent real carrier, or ambient completeness witness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_source\_window\_seal\_consumer\_exactness}
\begin{proof}
First apply \autoref{thm:cauchy-product-root-source-window-totality}. Every product read reaches the two source rows only through the paired $\StreamNameUp$ windows $W_X,W_Y$, so the consumer has no direct source coordinate outside the displayed window route.

Next apply \autoref{thm:cauchy-product-root-budget-product-lock}. The dyadic product row is read through $M_X,M_Y,Q_X,Q_Y,P,L$ before the classifier row $E$, so the source modulus rows and product ledger are the only budgeted path into the $\RegSeqRatUp$ classifier.

Finally apply \autoref{thm:cauchy-product-root-unblock-certificate} together with the sibling finite-tail surface of \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert}. The downstream $\RealUp$-facing seal therefore receives exactly the stated finite route, and the excluded analytic objects have no exported coordinate in the accepted packet.
\end{proof}

\begin{theorem}[CauchyProduct root source-window admission]
\label{thm:cauchyproduct-root-source-window-admission}
For every accepted $\CauchyProductUp$ packet, every product read admits the two source rows $X,Y$ only through the paired windows $W_X,W_Y$ and the displayed $\StreamNameUp/\DyadicRatCoreUp$ route. The read cannot inspect either source before the window rows, and it cannot replace the window route by a quotient stream, selected product limit, host multiplication primitive, or ambient completeness witness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_root\_source\_window\_admission}
\begin{proof}
First apply \autoref{thm:cauchy-product-root-source-window-totality}. It exposes the two $\RegularCauchyNameUp$ source rows exactly through the paired $\StreamNameUp$ windows $W_X,W_Y$.

Next apply \autoref{thm:cauchy-product-streamname-regseqrat-source-lock}. The route from the source rows to the classifier must pass through the displayed dyadic and regular-rational rows, so no source read can enter after the windows have been skipped.

Finally use \autoref{thm:cauchy-product-root-unblock-certificate}. The root certificate excludes quotient streams, selected limits, host multiplication, and ambient completeness from the product route, leaving the paired source-window admission as the only source entry.
\end{proof}

\begin{theorem}[CauchyProduct window product readback]
\label{thm:cauchyproduct-window-product-readback}
For every accepted $\CauchyProductUp$ packet, the product row $P$ is read back from $M_X,M_Y,Q_X,Q_Y$ before the $\RegSeqRatUp$ classifier row $E$ is consumed. The readback is the finite product route through $X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,L,E$, not a route through host multiplication or a selected product limit.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductWindowProductReadback}
\begin{proof}
Begin with source-window admission. It fixes the source rows behind $W_X,W_Y$ before any modulus, dyadic, product, or classifier row can be read.

Apply \autoref{thm:cauchy-product-root-budget-product-lock}. The dyadic product row $P$ is locked by the source modulus rows $M_X,M_Y$ and the product ledger $L$ before the classifier $E$ is available.

The source-window seal consumer exactness theorem then gives the complete order of the readback into $E$. Thus $P$ is read from $M_X,M_Y,Q_X,Q_Y$ inside the finite BEDC route, with no host product primitive or selected limit coordinate.
\end{proof}

\begin{theorem}[CauchyProduct product seal non-escape]
\label{thm:cauchyproduct-product-seal-nonescape}
Every $\RealUp$ seal consumer of an accepted $\CauchyProductUp$ packet receives only the finite route
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N .
$$
The consumer obtains no quotient stream, host multiplication primitive, selected limit, trichotomy row, independent real carrier, or ambient completeness witness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_product\_seal\_nonescape}
\begin{proof}
First use \autoref{thm:cauchyproduct-root-source-window-admission}. It confines the two source rows to the paired StreamName windows and the displayed dyadic route.

Next use \autoref{thm:cauchyproduct-window-product-readback}. The product row and classifier are read only after the source modulus and dyadic readback rows have been fixed.

Finally apply \autoref{thm:cauchy-product-source-window-seal-consumer-exactness} and \autoref{thm:cauchy-product-root-unblock-certificate}. These results hand the product route to a $\RealUp$-facing seal consumer and exclude quotient streams, host multiplication, selected limits, trichotomy rows, independent real carriers, and ambient completeness from the exported surface.
\end{proof}

\begin{theorem}[CauchyProduct StreamName-RegSeqRat source lock]
\label{thm:cauchy-product-streamname-regseqrat-source-lock}
For every accepted $\CauchyProductUp$ packet, every product read locks both source rows through the paired $\StreamNameUp$ windows $W_X,W_Y$ before any $\RegSeqRatUp$ or dyadic product row is read. The locked route is the finite path
$$
X,Y \to W_X,W_Y \to M_X,M_Y,Q_X,Q_Y,P,L \to E .
$$
It exports no unpaired source row, quotient stream, selected product limit, host multiplication primitive, or ambient completeness witness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_streamname\_regseqrat\_source\_lock}
\begin{proof}
First use \autoref{thm:cauchy-product-root-source-window-totality}. It places the two source rows behind the paired $\StreamNameUp$ windows, so a product read cannot inspect $X$ or $Y$ independently of $W_X,W_Y$.

Next apply \autoref{thm:cauchy-product-root-budget-product-lock}. The dyadic product rows and product ledger are read only after the source modulus and dyadic observation rows, and the $\RegSeqRatUp$ classifier row $E$ is downstream of that product ledger.

Finally combine the root-unblock certificate with the StreamName-RegSeqRat consumer surfaces of \autoref{ch:concrete-instances-streamname-namecert} and \autoref{ch:concrete-instances-regseqrat-namecert}. The route reaching $E$ is exactly the displayed paired-window route, with no coordinate for an alternate source or host product object.
\end{proof}

\begin{theorem}[CauchyProduct RealTailAgreementSeal handoff]
\label{thm:cauchy-product-realtailagreementseal-handoff}
Any $\RealUp$-facing product consumer that compares product tails of an accepted $\CauchyProductUp$ packet must pass through the $\RealTailAgreementSealUp$ route of \autoref{ch:concrete-instances-realtailagreementseal-namecert}. The comparison consumes the paired source-window lock, the product budget lock, and the root-unblock certificate before it reaches a RealUp seal; it cannot replace that route by a selected host limit, quotient stream, host equality, or ambient completeness row.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_real\_tail\_agreement\_handoff}
\begin{proof}
Begin with \autoref{thm:cauchy-product-source-window-seal-consumer-exactness}. A RealUp-facing product seal already factors through the finite route from the two source rows through the paired windows, dyadic observations, product row, ledger, and classifier.

Use \autoref{thm:cauchy-product-streamname-regseqrat-source-lock} to make the source side rigid. Both sources are locked through $W_X,W_Y$ before RegSeqRat readback or product rows can be consumed.

Now read the sibling $\RealTailAgreementSealUp$ chapter. Its paired-window route compares tails only through displayed StreamName windows, dyadic readbacks, agreement rows, and the RealUp handoff. Composing that route with the product root-unblock certificate gives the required product-tail comparison surface.

The excluded alternatives are absent from both packets. Therefore a product-tail consumer reaches the RealUp seal through $\RealTailAgreementSealUp$ and receives no selected limit, quotient stream, host equality, host multiplication primitive, or ambient completeness witness.
\end{proof}

\begin{theorem}[CauchyProduct paired-window tail-agreement exhaustion]
\label{thm:cauchy-product-paired-window-tail-agreement-exhaustion}
For every accepted $\CauchyProductUp$ packet, if a product-tail consumer reaches a $\RealUp$ seal, then its paired tail-agreement comparison is exhausted by the finite route through $W_X,W_Y$, $M_X,M_Y,Q_X,Q_Y,P,L$, the $\RegSeqRatUp$ classifier row $E$, and the sibling $\RealTailAgreementSealUp$ packet. The route is the one supplied by the $\StreamNameUp$ source lock, the product budget lock, the RealTailAgreementSeal handoff, and the sibling surfaces of \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-realtailagreementseal-namecert}. It exports no quotient stream, host multiplication primitive, host equality, selected limit, trichotomy row, or ambient completeness witness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_source\_window\_tail\_agreement\_exhaustion}
\begin{proof}
First apply \autoref{thm:cauchy-product-streamname-regseqrat-source-lock}. This fixes the two source rows behind the paired $\StreamNameUp$ windows $W_X,W_Y$ before any $\RegSeqRatUp$ classifier or product read is available.

Next apply \autoref{thm:cauchy-product-root-budget-product-lock}. The product-tail consumer can reach $E$ only through $M_X,M_Y,Q_X,Q_Y,P,L$, so the product comparison has no separate budget, product, or classifier route.

Now apply \autoref{thm:cauchy-product-realtailagreementseal-handoff}. The Real-facing tail comparison is handed to the sibling $\RealTailAgreementSealUp$ packet, whose chapter supplies the displayed paired-window agreement surface.

Finally read the sibling chapters \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-realtailagreementseal-namecert} as the three consumer boundaries. Their surfaces account for the StreamName windows, the regular-rational classifier row, and the RealTailAgreementSeal packet, leaving no coordinate for quotient streams, host multiplication, host equality, selected limits, trichotomy, or ambient completeness.
\end{proof}

\begin{corollary}[CauchyProduct root source-window strict obstruction]
\label{cor:cauchyproduct-root-source-window-strict-obstruction}
Let a displayed failure, refusal, separation, or countermodel row be cited against a named root source-window converse or reuse route for an accepted $\CauchyProductUp$ packet. Then that row is a strict local obstruction: it blocks only the named route whose consumer-facing read omits, replaces, detaches, or bypasses one of the displayed finite coordinates
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N .
$$
Every permitted consumer-facing use of the obstruction factors first through the paired $\StreamNameUp$ source windows $W_X,W_Y$ and then through the finite product-seal route named in \autoref{thm:cauchyproduct-product-seal-nonescape}. The obstruction has no force as a global impossibility theorem, no force as a host theorem about products or completion, and no force as hidden external validation for any route not displayed by the accepted packet.
\end{corollary}
\begin{proof}
Start with \autoref{thm:cauchy-product-root-source-window-totality}. It makes the two $\RegularCauchyNameUp$ source rows visible only through the paired $\StreamNameUp$ windows $W_X,W_Y$, and it identifies the exposed source rows with the carrier route, the local $\NameCert_{\CauchyProductUp}$ obligations, the StreamName handoff, the root window obligations, and the shared finite tail-window surface. Thus any consumer-facing converse or reuse route for root source-window totality must first ask for those paired windows before it can read any downstream product coordinate.

Now apply \autoref{thm:cauchyproduct-product-seal-nonescape}. A $\RealUp$ seal consumer receives only the finite route
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N .
$$
It receives no quotient stream, host multiplication primitive, selected limit, trichotomy row, independent real carrier, or ambient completeness witness. Therefore every consumer-facing use of a displayed failure, refusal, separation, or countermodel row factors through the same finite route: the source part is forced by $W_X,W_Y$, and the product-seal part is exhausted by $M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N$.

Consider the named converse or reuse route against which the displayed row is cited. If the route omits, replaces, detaches, or bypasses one of the listed coordinates, the row blocks that named route exactly at the missing finite coordinate. If the proposed reading instead asks for a quotient stream, host multiplication theorem, selected product limit, trichotomy row, independent real carrier, ambient completeness witness, or external validation channel, the two cited theorems supply no coordinate through which that request can be consumed. Such a request is rejected because it is unlisted in the accepted packet, not because the displayed row proves a stronger global impossibility.

Hence the obstruction remains local to the named root source-window converse or reuse route. It is a negative certificate for a failed finite packet read, while global impossibility statements, host product theorems, completion principles, and hidden external validation remain outside the exported $\CauchyProductUp$ surface.
\end{proof}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_root\_source\_window\_strict\_obstruction}
