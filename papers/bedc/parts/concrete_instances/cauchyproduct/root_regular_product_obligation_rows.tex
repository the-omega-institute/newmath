\begin{theorem}[CauchyProduct root regular product carrier row]
\label{thm:cauchy-product-root-regular-product-carrier-row}
For every accepted $\CauchyProductUp$ packet, the paired $\RegularCauchyNameUp$ source rows, paired $\StreamNameUp$ finite-window rows, $\DyadicRatCoreUp$ ledger rows, and $\RegSeqRatUp$ product classifier row form one finite $\BHist$ product carrier row. In displayed coordinates the carrier route is
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N,
$$
where $X,Y$ are the two regular Cauchy source rows, $W_X,W_Y$ are the paired finite windows, $M_X,M_Y,Q_X,Q_Y$ are the dyadic radius and observation rows, $P$ is the product row, and $E$ is the regular-rational classifier row. The row is local to the product packet and carries no host multiplication, quotient stream, selected limit, independent real carrier, or ambient completeness witness.
The windows $W_X,W_Y$ are read through \autoref{ch:concrete-instances-streamname-namecert}, the rows $M_X,M_Y,Q_X,Q_Y$ through \autoref{ch:concrete-instances-dyadicratcore-namecert}, the classifier row $E$ through \autoref{ch:concrete-instances-regseqrat-namecert}, and the seal boundary through \autoref{ch:concrete-instances-real-namecert}.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_root\_regular\_product\_carrier\_row}
\begin{proof}
Project the accepted carrier of \autoref{def:cauchy-product-namecert-source}. Its source coordinates expose $X,Y$ before the paired finite windows $W_X,W_Y$, and the dyadic rows $M_X,M_Y,Q_X,Q_Y$ are read before the product row.

Use the StreamName handoff and the product-observation seal obligations. They place the product observation $P$ and the classifier $E$ downstream of the displayed finite windows and dyadic ledger rows.

Finally read the support rows. The componentwise $\hsame$ rows, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row keep the whole product route inside one finite $\BHist$ carrier row, so none of the excluded analytic objects has an exported coordinate.
\end{proof}

\begin{theorem}[CauchyProduct root shared-window product classifier]
\label{thm:cauchy-product-root-shared-window-product-classifier}
For every accepted $\CauchyProductUp$ packet, the product classifier row $E$ is locked to the paired finite windows $W_X,W_Y$ and to the displayed $\hsame$ and $\Cont$ route
$$
W_X,W_Y \to M_X,M_Y,Q_X,Q_Y \to P \to E.
$$
Any consumer that reads $E$ must first read the two $\StreamNameUp$ windows, the $\DyadicRatCoreUp$ radius and observation rows, and the local support rows $H,C,L,N$. It cannot feed the classifier from a host multiplication operation, quotient stream product, selected limit, independent real carrier, or ambient completeness theorem.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_root\_shared\_window\_product\_classifier\_certificate}
\begin{proof}
Begin with the carrier row of \autoref{thm:cauchy-product-root-regular-product-carrier-row}. The row lists $W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E$ in the order needed by the product read.

Apply the root source window totality and root budget product lock. These statements force the two finite windows and the dyadic budget rows to be consumed before the product row $P$ and classifier row $E$ are visible.

The displayed $\hsame$ and $\Cont$ rows then transport the same product route to the local naming surface. Since the accepted packet contains no host multiplication row, quotient stream, selected limit, independent real carrier, or ambient completeness coordinate, the classifier is locked to the finite windows and the displayed route.
\end{proof}

\begin{theorem}[CauchyProduct root Real seal product ledger]
\label{thm:cauchy-product-root-real-seal-product-ledger}
Any $\RealUp$-facing product seal read of an accepted $\CauchyProductUp$ packet consumes the local $\Pkg$ and $\NameCert$ ledger before it reaches the seal. The read factors through
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N,
$$
uses the product classifier route of \autoref{thm:cauchy-product-root-shared-window-product-classifier}, and then reaches the $\RealUp$ seal only through the displayed product ledger. It cannot use host multiplication, quotient streams, selected limits, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_root\_real\_seal\_product\_ledger\_certificate}
\begin{proof}
First build the product carrier row using \autoref{thm:cauchy-product-root-regular-product-carrier-row}. This supplies the two regular Cauchy sources, paired windows, dyadic ledger rows, product row, classifier row, and support rows as one finite inventory.

Next apply the shared-window product classifier theorem. The classifier row $E$ is already downstream of the paired windows, dyadic observations, product row, and displayed $\hsame$ and $\Cont$ route.

Finally route the classifier to the Real-facing seal. The local $\Pkg$ and $\NameCert$ ledger rows $L,N$ are part of the displayed packet before the seal is consumed, and the existing Real seal root boundary excludes host multiplication, quotient streams, selected limits, and ambient completeness from the consumer surface.
\end{proof}

\begin{theorem}[CauchyProduct root regular product budget seal exhaustion]
\label{thm:cauchy-product-root-regular-product-budget-seal-exhaustion}
Any $\RealUp$-facing product consumer that uses the root regular product carrier of an accepted $\CauchyProductUp$ packet must pass through
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N
$$
and through the displayed $\RealObservationBudgetUp$ and $\RegSeqRatUp$ seal route before a $\RealUp$ seal is exposed. The consumer cannot reach the seal from host multiplication, quotient streams, selected limits, an independent real carrier, or ambient completeness.
\end{theorem}
\begin{proof}
Begin with the root regular product carrier row. It places the paired regular Cauchy sources, finite windows, dyadic budget rows, product observation, classifier row, transport rows, route rows, package ledger, and local naming row in one finite inventory.

Apply the shared-window product classifier. The classifier $E$ is visible only after the paired windows $W_X,W_Y$, the dyadic radius and observation rows $M_X,M_Y,Q_X,Q_Y$, and the product row $P$ have been consumed along the displayed route.

Use the Real seal product ledger for the final boundary. It sends the $\RealUp$-facing consumer through the local $\Pkg$ and $\NameCert$ ledger rows $L,N$ before the seal is visible, and it identifies the exposed seal as the displayed $\RealObservationBudgetUp$ and $\RegSeqRatUp$ route rather than an ambient real object.

The excluded sources have no coordinate in the carrier row. Therefore the Real-facing product seal is exhausted by the displayed budgeted product route.
\end{proof}

\begin{lemma}[CauchyProduct root regular product downstream coverage]
\label{lem:cauchy-product-root-regular-product-downstream-coverage}
Every accepted local consumer downstream of the root regular product route of an accepted $\CauchyProductUp$ packet is exhausted by the finite alternatives, ledgers, and coverage rows displayed in this file. Concretely, the consumer can read only the carrier and classifier rows
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N,
$$
the $\RealUp$-facing product ledger through $L,N$, or the displayed $\RealObservationBudgetUp$ and $\RegSeqRatUp$ seal route attached to the same product inventory. Host multiplication, quotient streams, selected product limits, independent regular-sequence or real carriers, and ambient completeness witnesses remain outside the accepted BEDC packet.
\end{lemma}
\begin{proof}
Start with the carrier row of \autoref{thm:cauchy-product-root-regular-product-carrier-row}. It displays the whole root regular product packet as one finite $\BHist$ inventory: the two $\RegularCauchyNameUp$ source rows, the paired $\StreamNameUp$ windows, the dyadic radius and observation rows, the product row, the $\RegSeqRatUp$ classifier, and the support rows $H,C,L,N$. Hence every local consumer first meets one of the listed carrier, window, dyadic, product, classifier, transport, continuation, ledger, or naming coordinates.

The classifier alternatives are then fixed by \autoref{thm:cauchy-product-root-shared-window-product-classifier}. Any read of $E$ must pass through the paired windows, the dyadic rows, the product row $P$, and the displayed $\hsame$ and $\Cont$ route. Thus a downstream consumer cannot introduce a second classifier input, a host multiplication operation, a quotient-stream product, or a selected product limit at the classifier stage.

For a seal-facing read, apply \autoref{thm:cauchy-product-root-real-seal-product-ledger}. The $\RealUp$ boundary consumes the local $\Pkg$ and $\NameCert$ ledger rows before the seal is visible, so the downstream route is still the same finite inventory with $L,N$ as the product ledger.

Finally apply \autoref{thm:cauchy-product-root-regular-product-budget-seal-exhaustion}. It identifies every $\RealUp$-facing product consumer using the root regular product carrier with the displayed $\RealObservationBudgetUp$ and $\RegSeqRatUp$ seal route, and it excludes host multiplication, quotient streams, selected limits, an independent real carrier, and ambient completeness from the seal surface. Combining the carrier, classifier, ledger, and budget-seal rows accounts for all accepted local downstream reads, while the excluded alternatives occur in no coordinate of the accepted BEDC packet.
\end{proof}

\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_root\_regular\_product\_downstream\_coverage}

\begin{theorem}[CauchyProduct regular product window exhaustion]
\label{thm:cauchy-product-regular-product-window-exhaustion}
Any $\RealUp$-facing product consumer of an accepted $\CauchyProductUp$ packet must consume both $\StreamNameUp$ windows, both $\DyadicRatCoreUp$ observation and budget rows, the $\RegSeqRatUp$ product classifier, the local $\Pkg$ and $\NameCert$ ledger, and the displayed $\RealObservationBudgetUp$ and $\RealUp$ seal route before any product read is exported. Equivalently, the consumer factors through
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N
$$
and then through the budgeted Real seal route. The packet provides no host multiplication, quotient stream product, selected limit, independent real carrier, or ambient completeness coordinate from which a product read could be exported earlier.
\end{theorem}
\begin{proof}
First apply the root regular product carrier row. It supplies the two regular Cauchy sources, the two finite windows, the two dyadic observation and budget pairs, the product row, the classifier row, and the local support rows as one finite inventory.

Next apply the shared-window product classifier. The classifier $E$ is downstream of both windows $W_X,W_Y$ and both dyadic rows $M_X,M_Y,Q_X,Q_Y$, so no product read can use $E$ before the displayed finite-window route has been consumed.

Finally apply the budget seal exhaustion theorem. That theorem routes every $\RealUp$-facing product consumer through the local $\Pkg$ and $\NameCert$ ledger and through the displayed $\RealObservationBudgetUp$ and $\RegSeqRatUp$ seal boundary before the Real seal is visible.

The excluded sources have no coordinate in the carrier row. Hence host multiplication, quotient streams, selected limits, independent real carriers, and ambient completeness are unavailable to the consumer, and the regular product window route is exhaustive.
\end{proof}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_regular\_product\_window\_exhaustion}

\begin{theorem}[CauchyProduct root RealSeries handoff exhaustion]
\label{thm:cauchy-product-root-series-handoff-exhaustion}
Every $\CauchyProductUp$ consumer fed by a $\RealSeriesUp$ root series surface consumes paired $\StreamNameUp$ windows, $\DyadicRatCoreUp$ budget rows, $\RegSeqRatUp$ classifier rows, the $\RealSeriesUp$ shared tail threshold, and the local $\Pkg$ and $\NameCert$ ledger before any Real-facing product seal is exported. The visible route factors through the local product inventory
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N
$$
and through the RealSeries rows
$$
T,S,W,Q,D,M,E,H,C,P,N
$$
from \autoref{thm:realseries-root-cauchy-tail-budget}. It has no coordinate for host multiplication, quotient stream product, selected infinite sums, independent real carriers, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_root\_series\_handoff\_exhaustion}
\begin{proof}
Begin with the root regular product carrier row. It supplies $X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N$ as the only product inventory visible to the consumer.

Apply the shared-window product classifier. The classifier row $E$ is downstream of both $\StreamNameUp$ windows, the $\DyadicRatCoreUp$ budget and observation rows, and the product observation row, so a product read cannot be fed by a host multiplication operation or quotient stream product.

Use the budget seal exhaustion theorem for the Real-facing boundary. It sends every seal read through the local $\Pkg$ and $\NameCert$ rows before any $\RealUp$ seal is exported.

Finally apply \autoref{thm:realseries-root-cauchy-tail-budget}. The RealSeries handoff supplies only the finite rows $T,S,W,Q,D,M,E,H,C,P,N$, with the shared threshold row $M$ already fixed before endpoint sealing. Thus the product consumer receives a finite paired-window route, not a selected infinite sum, independent real carrier, or ambient completeness coordinate.
\end{proof}

\begin{theorem}[CauchyProduct root regular product public export]
\label{thm:cauchyproduct-root-regular-product-public-export}
Every public $\CauchyProductUp$ export over the root regular-product packet factors through paired $\RegularCauchyNameUp$ sources, paired $\StreamNameUp$ windows, $\DyadicRatCoreUp$ budget rows, the $\RegSeqRatUp$ product classifier, $\RealSeriesUp$ shared-tail rows, the local $\Pkg$ and $\NameCert$ ledger, and the terminal $\RealUp$ seal route. In displayed coordinates the export consumes
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N
$$
and the $\RealSeriesUp$ rows
$$
T,S,W,Q,D,M,E,H,C,P,N
$$
before any public product read is available. The export has no coordinate for host multiplication, quotient streams, selected product limits, independent real carriers, or ambient completeness.
\end{theorem}
\begin{proof}
First apply the root regular product carrier row. It places the paired $\RegularCauchyNameUp$ sources, paired finite windows, dyadic budget rows, product observation, classifier row, transport, replay, provenance, and local naming in one finite carrier inventory.

Next use the shared-window product classifier. The classifier row $E$ can be read only after the two $\StreamNameUp$ windows and the $\DyadicRatCoreUp$ radius and observation rows have been consumed, so no host multiplication or quotient-stream product can feed the public export.

Then apply the regular product budget seal exhaustion and the Real seal product ledger. Together they route the public read through $L,N$ and through the displayed $\RegSeqRatUp$ and $\RealUp$ seal boundary before any Real-facing product value is exposed.

Finally use the root RealSeries handoff exhaustion. The shared-tail rows $T,S,W,Q,D,M,E,H,C,P,N$ are the only series-facing input to the product packet, and their threshold row is fixed before endpoint sealing. Combining these carrier, classifier, budget, ledger, and handoff rows gives the public export route and excludes the stated analytic alternatives by field exhaustion.
\end{proof}

\begin{theorem}[CauchyProduct root regular product budget separation]
\label{thm:cauchyproduct-root-regular-product-budget-separation}
Any accepted $\CauchyProductUp$ root regular product consumer that reaches a $\RealUp$-facing seal factors first through the paired $\StreamNameUp$ windows, the $\DyadicRatCoreUp$ budget rows, the $\RegSeqRatUp$ classifier row, the $\RealObservationBudgetUp$ rows, the displayed $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ evidence. The Real seal is consumed only after that route has exposed
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N.
$$
No host multiplication, quotient stream product, selected limit, independent real carrier, or ambient completeness coordinate is available to the consumer before the seal is read.
\end{theorem}
\begin{proof}
First apply the root regular product carrier row. It supplies the paired regular Cauchy sources, finite windows, dyadic budget rows, product observation row, classifier row, and structural support rows as one finite carrier inventory.

Next apply the shared-window product classifier. This locks $E$ behind the two $\StreamNameUp$ windows and the $\DyadicRatCoreUp$ radius and observation rows, so the classifier cannot be fed by a host multiplication operation or a quotient stream product.

Then apply the Real seal product ledger. The $\Pkg$ and $\NameCert$ rows are consumed before the seal, and the route passes through the displayed $\RealObservationBudgetUp$ and $\RegSeqRatUp$ handoff rather than through an independent real carrier.

Finally apply budget seal exhaustion. The exhausted route has no coordinate for selected limits or ambient completeness, so every accepted Real-facing product consumer remains separated from the excluded sources until the displayed finite budget route reaches the seal.
\end{proof}
