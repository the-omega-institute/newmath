\begin{theorem}[CauchyProduct root regular product carrier row]
\label{thm:cauchy-product-root-regular-product-carrier-row}
For every accepted $\CauchyProductUp$ packet, the paired $\RegularCauchyNameUp$ source rows, paired $\StreamNameUp$ finite-window rows, $\DyadicRatCoreUp$ ledger rows, and $\RegSeqRatUp$ product classifier row form one finite $\BHist$ product carrier row. In displayed coordinates the carrier route is
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N,
$$
where $X,Y$ are the two regular Cauchy source rows, $W_X,W_Y$ are the paired finite windows, $M_X,M_Y,Q_X,Q_Y$ are the dyadic radius and observation rows, $P$ is the product row, and $E$ is the regular-rational classifier row. The row is local to the product packet and carries no host multiplication, quotient stream, selected limit, independent real carrier, or ambient completeness witness.
The windows $W_X,W_Y$ are read through \autoref{ch:concrete-instances-streamname-namecert}, the rows $M_X,M_Y,Q_X,Q_Y$ through \autoref{ch:concrete-instances-dyadicratcore-namecert}, the classifier row $E$ through \autoref{ch:concrete-instances-regseqrat-namecert}, and the seal boundary through \autoref{ch:concrete-instances-real-namecert}.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_root\_regular\_product\_carrier\_row}
\begin{proof}
Project the accepted carrier of \autoref{def:cauchy-product-namecert-source}. Its source coordinates expose $X,Y$ before the paired finite windows $W_X,W_Y$, and the dyadic rows $M_X,M_Y,Q_X,Q_Y$ are read before the product row.

Use the StreamName handoff and the product-observation seal obligations. They place the product observation $P$ and the classifier $E$ downstream of the displayed finite windows and dyadic ledger rows.

Finally read the support rows. The componentwise $\hsame$ rows, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row keep the whole product route inside one finite $\BHist$ carrier row, so none of the excluded analytic objects has an exported coordinate.
\end{proof}

\begin{theorem}[CauchyProduct root shared-window product classifier]
\label{thm:cauchy-product-root-shared-window-product-classifier}
For every accepted $\CauchyProductUp$ packet, the product classifier row $E$ is locked to the paired finite windows $W_X,W_Y$ and to the displayed $\hsame$ and $\Cont$ route
$$
W_X,W_Y \to M_X,M_Y,Q_X,Q_Y \to P \to E.
$$
Any consumer that reads $E$ must first read the two $\StreamNameUp$ windows, the $\DyadicRatCoreUp$ radius and observation rows, and the local support rows $H,C,L,N$. It cannot feed the classifier from a host multiplication operation, quotient stream product, selected limit, independent real carrier, or ambient completeness theorem.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_root\_shared\_window\_product\_classifier\_certificate}
\begin{proof}
Begin with the carrier row of \autoref{thm:cauchy-product-root-regular-product-carrier-row}. The row lists $W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E$ in the order needed by the product read.

Apply the root source window totality and root budget product lock. These statements force the two finite windows and the dyadic budget rows to be consumed before the product row $P$ and classifier row $E$ are visible.

The displayed $\hsame$ and $\Cont$ rows then transport the same product route to the local naming surface. Since the accepted packet contains no host multiplication row, quotient stream, selected limit, independent real carrier, or ambient completeness coordinate, the classifier is locked to the finite windows and the displayed route.
\end{proof}

\begin{theorem}[CauchyProduct root Real seal product ledger]
\label{thm:cauchy-product-root-real-seal-product-ledger}
Any $\RealUp$-facing product seal read of an accepted $\CauchyProductUp$ packet consumes the local $\Pkg$ and $\NameCert$ ledger before it reaches the seal. The read factors through
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N,
$$
uses the product classifier route of \autoref{thm:cauchy-product-root-shared-window-product-classifier}, and then reaches the $\RealUp$ seal only through the displayed product ledger. It cannot use host multiplication, quotient streams, selected limits, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_root\_real\_seal\_product\_ledger\_certificate}
\begin{proof}
First build the product carrier row using \autoref{thm:cauchy-product-root-regular-product-carrier-row}. This supplies the two regular Cauchy sources, paired windows, dyadic ledger rows, product row, classifier row, and support rows as one finite inventory.

Next apply the shared-window product classifier theorem. The classifier row $E$ is already downstream of the paired windows, dyadic observations, product row, and displayed $\hsame$ and $\Cont$ route.

Finally route the classifier to the Real-facing seal. The local $\Pkg$ and $\NameCert$ ledger rows $L,N$ are part of the displayed packet before the seal is consumed, and the existing Real seal root boundary excludes host multiplication, quotient streams, selected limits, and ambient completeness from the consumer surface.
\end{proof}

\begin{theorem}[CauchyProduct root regular product budget seal exhaustion]
\label{thm:cauchy-product-root-regular-product-budget-seal-exhaustion}
Any $\RealUp$-facing product consumer that uses the root regular product carrier of an accepted $\CauchyProductUp$ packet must pass through
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N
$$
and through the displayed $\RealObservationBudgetUp$ and $\RegSeqRatUp$ seal route before a $\RealUp$ seal is exposed. The consumer cannot reach the seal from host multiplication, quotient streams, selected limits, an independent real carrier, or ambient completeness.
\end{theorem}
\begin{proof}
Begin with the root regular product carrier row. It places the paired regular Cauchy sources, finite windows, dyadic budget rows, product observation, classifier row, transport rows, route rows, package ledger, and local naming row in one finite inventory.

Apply the shared-window product classifier. The classifier $E$ is visible only after the paired windows $W_X,W_Y$, the dyadic radius and observation rows $M_X,M_Y,Q_X,Q_Y$, and the product row $P$ have been consumed along the displayed route.

Use the Real seal product ledger for the final boundary. It sends the $\RealUp$-facing consumer through the local $\Pkg$ and $\NameCert$ ledger rows $L,N$ before the seal is visible, and it identifies the exposed seal as the displayed $\RealObservationBudgetUp$ and $\RegSeqRatUp$ route rather than an ambient real object.

The excluded sources have no coordinate in the carrier row. Therefore the Real-facing product seal is exhausted by the displayed budgeted product route.
\end{proof}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_root\_regular\_product\_budget\_seal\_exhaustion}
