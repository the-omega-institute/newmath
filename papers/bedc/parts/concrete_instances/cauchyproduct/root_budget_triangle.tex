\begin{theorem}[CauchyProduct observation-budget triangle]
\label{thm:cauchy-product-observation-budget-triangle}
Any accepted $\CauchyProductUp$ product observation consumed through a $\RealObservationBudgetUp$ packet factors through the triangle whose sides are the paired $\StreamNameUp$ windows, the $\DyadicRatCoreUp$ observations, the $\RegSeqRatUp$ classifier row, the $\RealUp$ seal row, and the product and budget rows
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N
$$
and
$$
E_B,W_B,D_B,R_B,S_B,H_B,C_B,P_B,N_B.
$$
The source side is the admission route of \autoref{thm:cauchy-product-root-source-admission}; the classifier side is the stability route of \autoref{thm:cauchy-product-root-classifier-stability}; and the budget side is the finite readback boundary of \autoref{thm:cauchy-product-budgeted-readback-root-boundary}.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_observation\_budget\_triangle}
\begin{proof}
First read the product packet through \autoref{thm:cauchy-product-root-source-admission}. The two $\RegularCauchyNameUp$ source rows are visible only through the paired $\StreamNameUp$ windows and the $\DyadicRatCoreUp$ observation rows, so the product row $P$ has no source-facing route outside the displayed finite window surface.

Next transport that surface through \autoref{thm:cauchy-product-root-classifier-stability}. The classifier row $E$, the $\RealUp$ seal row, and the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows preserve the same finite product packet rather than replacing it by a quotient stream or host product.

Finally enter the observation budget through \autoref{thm:cauchy-product-budgeted-readback-root-boundary}. The budget rows $E_B,W_B,D_B,R_B,S_B,H_B,C_B,P_B,N_B$ read the same product packet, so the accepted observation is routed through the stated triangle of source windows, dyadic observations, classifier and seal rows, and budget rows.
\end{proof}

\begin{theorem}[CauchyProduct RealAlgOrder Completion handoff]
\label{thm:cauchy-product-realalgorder-completion-handoff}
Every $\RealAlgOrderUp$ multiplication consumer and every $\RealCompletenessUp$ completion-facing consumer that reads an accepted $\CauchyProductUp$ packet receives the same finite product-budget route. The shared route is the phase handoff of \autoref{thm:cauchy-product-root-phase-handoff} with the ledger obligation of \autoref{thm:cauchy-product-ledger-obligation}, read by the downstream chapters \autoref{ch:concrete-instances-real-alg-order-namecert} and \autoref{ch:concrete-instances-realcompleteness-namecert}.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:cauchy-product-root-phase-handoff}. It places every downstream L10 product consumer behind the paired source windows, dyadic observations, product row, classifier row, real seal row, displayed transports, continuations, package provenance, and local naming evidence.

Now apply \autoref{thm:cauchy-product-ledger-obligation}. The ledger route keeps the radius rows, product-radius accounting, and product classifier visible as finite packet data, so the handoff does not create a second multiplication surface for $\RealAlgOrderUp$ or a selected limit surface for $\RealCompletenessUp$.

The two downstream chapters then consume the same row inventory. The $\RealAlgOrderUp$ chapter may use the packet as its multiplication-facing product route, while the $\RealCompletenessUp$ chapter may use the same route as a completion-facing finite seal input; neither receives any coordinate beyond the displayed product and budget rows.
\end{proof}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_realalgorder\_completion\_handoff}

\begin{theorem}[CauchyProduct finite product-budget exhaustion]
\label{thm:cauchy-product-finite-product-budget-exhaustion}
The union of the product rows
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N
$$
and the budget rows
$$
E_B,W_B,D_B,R_B,S_B,H_B,C_B,P_B,N_B
$$
exhausts the downstream L10 consumer surface of an accepted $\CauchyProductUp$ packet consumed through $\RealObservationBudgetUp$. That surface excludes quotient streams, selected limits, host multiplication primitives, trichotomy rows, independent real carriers, and ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_finite\_product\_budget\_exhaustion}
\begin{proof}
Read the product side first. The root source admission, classifier stability, and phase handoff theorems make every product-facing consumer pass through $W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N$ before any downstream real-facing read is available.

Read the budget side next. The observation-budget boundary supplies exactly $E_B,W_B,D_B,R_B,S_B,H_B,C_B,P_B,N_B$ as the finite consumer rows through which a $\RealObservationBudgetUp$ packet may inspect the product.

Combining the two inventories leaves no unused coordinate in the L10 consumer surface. Since quotient streams, selected limits, host multiplication primitives, trichotomy rows, independent real carriers, and ambient completeness are not rows of either packet, they cannot be exported by the accepted product-budget route.
\end{proof}

\begin{theorem}[CauchyProduct budget-triangle Real seal exhaustion]
\label{thm:cauchy-product-budget-triangle-real-seal-exhaustion}
Any Real-facing consumer that reads an accepted $\CauchyProductUp$ packet through the observation-budget triangle reaches the $\RealUp$ seal only after the paired $\StreamNameUp$ windows, dyadic observations, product row, classifier row, product ledger, and $\RealObservationBudgetUp$ rows have all been displayed. The exhausted route is the finite union
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N
$$
and
$$
E_B,W_B,D_B,R_B,S_B,H_B,C_B,P_B,N_B.
$$
It exports no quotient stream, selected limit, host multiplication primitive, trichotomy row, independent real carrier, or ambient completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_budget\_triangle\_real\_seal\_exhaustion}
\begin{proof}
First compose the triangle of \autoref{thm:cauchy-product-observation-budget-triangle}. It routes the product read through paired source windows, dyadic observations, the product row, classifier row, seal row, and the displayed observation-budget rows.

Next apply the RealAlgOrder and completion handoff of \autoref{thm:cauchy-product-realalgorder-completion-handoff}. The downstream $\RealAlgOrderUp$ and $\RealCompletenessUp$ consumers receive the same finite product-budget route, so the seal is not reached through a separate multiplication or completion surface.

Finally use finite product-budget exhaustion from \autoref{thm:cauchy-product-finite-product-budget-exhaustion}. That row inventory accounts for every product and budget coordinate available to the consumer; because quotient streams, selected limits, host multiplication, trichotomy, independent real carriers, and ambient completeness are absent from the inventory, none can be exported by the Real-facing seal route.
\end{proof}

\begin{theorem}[CauchyProduct budget-triangle selector compatibility]
\label{thm:cauchy-product-budget-triangle-selector-compatibility}
Any Real-facing consumer that reads an accepted $\CauchyProductUp$ packet through the observation-budget triangle and then through a $\DiagonalTailSelectorUp$ route factors through the shared finite product rows
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N,
$$
the $\RealObservationBudgetUp$ rows
$$
E_B,W_B,D_B,R_B,S_B,H_B,C_B,P_B,N_B,
$$
and the selector rows
$$
R,n,\mu,k,W,D,T,S,H,C,P,N.
$$
The common seal route is read through the displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ rows, and it exports no quotient stream, selected host limit, countable-choice row, trichotomy row, host multiplication primitive, or ambient completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_budget\_triangle\_selector\_compatibility}
\begin{proof}
Start with the observation-budget triangle of \autoref{thm:cauchy-product-observation-budget-triangle}. It identifies the product-facing read with the paired $\StreamNameUp$ windows, the $\DyadicRatCoreUp$ observation rows, the $\RegSeqRatUp$ classifier row, the $\RealUp$ seal row, and the displayed product and budget rows.

Next apply finite product-budget exhaustion from \autoref{thm:cauchy-product-finite-product-budget-exhaustion}. This accounts for every product and $\RealObservationBudgetUp$ coordinate visible to the Real-facing consumer before any selector handoff occurs.

Finally pass to the $\DiagonalTailSelectorUp$ route by \autoref{thm:diagonal-tail-selector-limit-handoff}. The selector read contributes only the rows $R,n,\mu,k,W,D,T,S,H,C,P,N$ and uses the same $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ surface already identified by the product-budget triangle.

Combining the three row inventories gives the stated factorization. Since none of the product, budget, or selector rows contains a quotient stream, selected host limit, countable-choice row, trichotomy row, host multiplication primitive, or ambient completeness coordinate, the common Real seal route cannot export any of those objects.
\end{proof}

\begin{theorem}[CauchyProduct shared-tail selector factorization]
\label{thm:cauchy-product-shared-tail-selector-factorization}
Let an accepted $\CauchyProductUp$ packet be consumed by a Real-facing route whose paired source windows are served by an accepted $\CauchySharedTailModulusUp$ packet and whose seal route then passes through a $\DiagonalTailSelectorUp$ packet. Then the consumer factors through the product rows
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N,
$$
the shared-tail rows
$$
R_0,R_1,M_0,M_1,\tau,D,W_0,W_1,Q_0,Q_1,E,H,C,P,N,
$$
and the selector rows
$$
R,n,\mu,k,W,D,T,S,H,C,P,N.
$$
The combined route exports no quotient stream, selected host limit, countable-choice row, trichotomy row, host multiplication primitive, or ambient completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_shared\_tail\_selector\_factorization}
\begin{proof}
Start with the product side. Apply \autoref{thm:cauchy-product-budget-triangle-selector-compatibility}; the Real-facing product read is already confined to the displayed product rows, the observation-budget route, and the $\DiagonalTailSelectorUp$ rows.

Next identify the paired source windows used by that product read. The diagonal-consistency route of \autoref{thm:cauchy-product-root-source-diagonal-consistency} keeps the two source windows on the same finite product packet, so the shared-tail packet may serve the read without replacing it by a host product or a quotient stream.

Now insert the shared-tail modulus surface. By \autoref{thm:cauchy-shared-tail-modulus-real-seal-handoff}, every completion-facing Real seal read factors through $M_0,M_1,\tau,D,W_0,W_1,Q_0,Q_1,E,H,C,P,N$. The paired-window readback and route replay rows keep the sources $R_0,R_1$ on the same displayed route, so the full shared-tail inventory is $R_0,R_1,M_0,M_1,\tau,D,W_0,W_1,Q_0,Q_1,E,H,C,P,N$.

Finally use \autoref{thm:cauchy-shared-tail-modulus-threshold-uniqueness-obligation} together with the diagonal selector boundary \autoref{thm:diagonal-tail-selector-limit-handoff}. The shared threshold cannot be replaced by private thresholds, and the selector contributes only $R,n,\mu,k,W,D,T,S,H,C,P,N$.

Combining the product, shared-tail, and selector inventories gives the stated factorization. Since none of the three finite row lists contains quotient streams, selected host limits, countable-choice rows, trichotomy, host multiplication, or ambient completeness, no Real or completion consumer can obtain such a coordinate from the combined route.
\end{proof}

\begin{lemma}[CauchyProduct observation-budget downstream coverage]
\label{lem:cauchy-product-observation-budget-downstream-coverage}
Every accepted local consumer downstream of an accepted $\CauchyProductUp$ packet read through the $\RealObservationBudgetUp$ observation-budget triangle is exhausted by the finite alternatives, ledgers, and coverage rows already displayed in the triangle file. Concretely, the consumer can read only the product-side rows
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N,
$$
the observation-budget rows
$$
E_B,W_B,D_B,R_B,S_B,H_B,C_B,P_B,N_B,
$$
or the downstream $\RealAlgOrderUp$ and $\RealCompletenessUp$ handoff route carried by the same finite product-budget inventory. Quotient streams, selected product limits, host multiplication primitives, trichotomy rows, independent regular-sequence or real carriers, and ambient completeness witnesses remain outside the accepted BEDC packet.
\end{lemma}
\begin{proof}
Start with the observation-budget triangle of \autoref{thm:cauchy-product-observation-budget-triangle}. It says that any accepted product observation consumed through $\RealObservationBudgetUp$ factors through the paired $\StreamNameUp$ windows, the $\DyadicRatCoreUp$ observations, the $\RegSeqRatUp$ classifier row, the $\RealUp$ seal row, and exactly the displayed product and budget row lists. Thus every local product-facing alternative has already entered one of the listed source-window, dyadic, classifier, seal, product-ledger, or budget-ledger coordinates.

Now pass to downstream consumers by \autoref{thm:cauchy-product-realalgorder-completion-handoff}. A $\RealAlgOrderUp$ multiplication consumer and a $\RealCompletenessUp$ completion-facing consumer receive the same finite product-budget route, namely the phase handoff together with the ledger obligation. Therefore the downstream read does not add a second multiplication surface, a selected completion surface, or an independent real carrier; it only reuses the rows already supplied by the triangle.

Finally apply the finite product-budget exhaustion of \autoref{thm:cauchy-product-finite-product-budget-exhaustion} and the Real seal exhaustion of \autoref{thm:cauchy-product-budget-triangle-real-seal-exhaustion}. These two coverage statements account for every product, budget, and seal-facing coordinate available to a local consumer. Since quotient streams, selected product limits, host multiplication primitives, trichotomy rows, independent regular-sequence or real carriers, and ambient completeness witnesses occur in none of those inventories, there is no row from which such an excluded alternative can be exported by the accepted BEDC packet.
\end{proof}

\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_observation\_budget\_downstream\_coverage}
