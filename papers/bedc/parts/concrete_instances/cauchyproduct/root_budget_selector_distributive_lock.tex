\begin{theorem}[CauchyProduct budget-selector distributive lock]
\label{thm:cauchy-product-budget-selector-distributive-lock}
Let $Z=(X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N)$ be an accepted $\CauchyProductUp$ packet. Suppose the two factor-window reads $W_X$ and $W_Y$ are consumed through the same accepted $\FiniteObservationBudgetSelectorUp$ finite observation-budget selector from \autoref{ch:concrete-instances-finiteobservationbudgetselector-namecert}. Then the product read has a single displayed budget route
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N
$$
together with the selector rows
$$
B,S,W,D,R,E_B,H_B,C_B,P_B,N_B.
$$
The route is distributive only as a finite $\BHist$ product-route certificate: it distributes the same selector window over the two displayed factor windows and then reads the product through the existing $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows. It does not expose host multiplication, quotient stream equality, selected limits, independent real carriers, trichotomy rows, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_budget\_selector\_distributive\_lock}
\begin{proof}
Begin with the two source windows. By \autoref{thm:cauchy-product-window-product-nonescape}, the only route from the source names to the product observation passes through $W_X,W_Y,M_X,M_Y,Q_X,Q_Y$ and the product ledger before $P$ is visible.

Now read both factor windows through the common finite observation-budget selector of \autoref{ch:concrete-instances-finiteobservationbudgetselector-namecert}. The selector contributes one finite schedule, one finite window family, one dyadic ledger, one regular handoff, one seal row, and one set of transport, continuation, provenance, and local naming rows; it does not create separate selector routes for the two factors.

Finally apply \autoref{thm:cauchy-product-budget-product-consumer-boundary}. The product consumer can inspect exactly the displayed product rows together with the displayed budget rows, so the two factor-window reads and the product read are locked to one finite route. The excluded analytic objects are absent from both accepted packets, hence they cannot enter the distributive product-route certificate.
\end{proof}

\begin{theorem}[CauchyProduct commuted-budget route]
\label{thm:cauchy-product-commuted-budget-route}
Let $Z=(X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N)$ be an accepted $\CauchyProductUp$ packet whose two factor-window reads are accepted by the same finite observation-budget selector from \autoref{ch:concrete-instances-finiteobservationbudgetselector-namecert}. If the packet is read after swapping $W_X$ with $W_Y$ and swapping the dyadic product inputs, then the same finite product-budget route reaches the $\RegSeqRatUp$ classifier row and the $\RealUp$ seal. The commuted route is preserved by componentwise $\StreamNameUp$ handoff, $\DyadicRatCoreUp$ product-row symmetry, $\RegSeqRatUp$ handoff, and $\RealUp$ seal non-escape; it does not claim host multiplication or completed-real commutativity.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_commuted\_budget\_route}
\begin{proof}
First expose the original finite route. The root source-window totality and \autoref{thm:cauchy-product-budget-selector-distributive-lock} bind both factor windows to the same displayed selector route before the product row is available.

Next commute the source read. Swapping the two factor windows only exchanges the componentwise $\StreamNameUp$ handoffs, and the dyadic product row is read through the symmetric $\DyadicRatCoreUp$ product observation rather than through host multiplication.

Finally reuse the downstream budget boundary. The root budget product lock and \autoref{thm:cauchy-product-real-budget-multiplication-boundary} keep the $\RegSeqRatUp$ handoff and $\RealUp$ seal downstream of the same finite budget rows. Thus the commuted packet reaches the same classifier and seal route while excluding host multiplication and completed-real commutativity.
\end{proof}

\begin{theorem}[CauchyProduct Real budget multiplication boundary]
\label{thm:cauchy-product-real-budget-multiplication-boundary}
A $\RealUp$ multiplication-facing seal for an accepted $\CauchyProductUp$ packet is reached only after the shared $\StreamNameUp$ factor windows, the $\DyadicRatCoreUp$ radius ledger, the $\RegSeqRatUp$ handoff, and the local $\CauchyProductUp$ $\NameCert$ rows have all been displayed. If the factor windows are read through the finite observation-budget selector of \autoref{ch:concrete-instances-finiteobservationbudgetselector-namecert}, then the multiplication-facing route additionally passes through the selector budget rows before the $\RealUp$ seal is consumed. No seal-facing consumer receives a host multiplication primitive, a quotient-stream product, a selected real value, an independent real carrier, or an ambient completeness witness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_real\_budget\_multiplication\_boundary}
\begin{proof}
First expose the CauchyProduct product route. The non-escape statement \autoref{thm:cauchy-product-window-product-nonescape} fixes the source-window route, and \autoref{thm:cauchy-product-budget-selector-distributive-lock} binds any shared selector read to the same finite product-route certificate.

Next pass to the real-facing seal. By \autoref{thm:cauchy-product-real-seal-budget-gate}, every $\RealUp$ seal read consumed through a budget packet factors through the displayed budget rows before the seal is available.

The boundary follows by joining that gate with \autoref{thm:cauchy-product-budget-product-consumer-boundary}. The consumer sees the shared $\StreamNameUp$ windows, the dyadic radius ledger, the regular-rational handoff, the selector rows, the displayed transports and continuations, the provenance rows, and the local $\NameCert$ rows. There is no remaining coordinate from which a host multiplication primitive or ambient completion principle could be read.
\end{proof}

\begin{theorem}[CauchyProduct RegSeqRat product seal coverage]
\label{thm:cauchy-product-regseqrat-product-seal-coverage}
Every $\RegSeqRatUp$-facing product consumer of an accepted $\CauchyProductUp$ packet whose factor windows are read through the finite observation-budget selector of \autoref{ch:concrete-instances-finiteobservationbudgetselector-namecert} is exhausted by the rows displayed in \autoref{thm:cauchy-product-budget-selector-distributive-lock} and \autoref{thm:cauchy-product-real-budget-multiplication-boundary}. In particular, the consumer reads the same source windows, dyadic radius rows, product row, classifier handoff, selector rows, transports, continuations, provenance, and local naming rows before any product seal is exposed, and it receives no additional regular-sequence carrier, quotient stream, selected limit, host multiplication, trichotomy row, independent real carrier, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_regseqrat\_product\_seal\_coverage}
\begin{proof}
Start with the RegSeqRat side of the product packet. The window product non-escape theorem \autoref{thm:cauchy-product-window-product-nonescape} and the budgeted product consumer boundary \autoref{thm:cauchy-product-budget-product-consumer-boundary} leave only the displayed product rows and budget rows available to a regular-rational consumer.

Now attach the shared selector route. The distributive lock \autoref{thm:cauchy-product-budget-selector-distributive-lock} shows that both factor-window reads pass through one selector route rather than two unrelated budget routes.

Finally use the real budget multiplication boundary of \autoref{thm:cauchy-product-real-budget-multiplication-boundary}. Any product seal read occurs after the same window, dyadic, classifier, selector, transport, continuation, provenance, and local naming rows. Thus the listed rows exhaust every $\RegSeqRatUp$-facing product consumer, and no unlisted analytic or host-level object remains available.
\end{proof}

\begin{lemma}[CauchyProduct budget-selector downstream coverage]
\label{lem:cauchy-product-budget-selector-downstream-coverage}
Let an accepted $\CauchyProductUp$ packet read its factor windows through the same accepted $\FiniteObservationBudgetSelectorUp$ selector. Every accepted local consumer downstream of the budget-selector distributive route is exhausted by the finite product rows
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N
$$
and by the selector and coverage rows already displayed in \autoref{thm:cauchy-product-budget-selector-distributive-lock} and \autoref{thm:cauchy-product-regseqrat-product-seal-coverage}. Thus a downstream consumer may read only a factor-window alternative, the product ledger and classifier route, the selector schedule and budget ledger, or the transport, continuation, provenance, and local $\NameCert$ coverage rows. Quotient streams, selected product limits, host multiplication primitives, trichotomy rows, independent regular-sequence or real carriers, and ambient completeness witnesses remain outside the accepted BEDC packet.
\end{lemma}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_budget\_selector\_downstream\_coverage}
\begin{proof}
Start with the distributive route. The budget-selector distributive lock of \autoref{thm:cauchy-product-budget-selector-distributive-lock} says that the two factor-window reads do not create two independent budget paths. They are consumed through one finite selector route, and the product read is therefore carried by the displayed product rows together with the displayed selector rows.

Now pass to downstream consumers. The RegSeqRat product seal coverage theorem \autoref{thm:cauchy-product-regseqrat-product-seal-coverage} exhausts every $\RegSeqRatUp$-facing product consumer by the same source windows, dyadic radius rows, product row, classifier handoff, selector rows, transports, continuations, provenance, and local naming rows before any product seal is exposed.

These two cited statements cover the local alternatives. A factor-window consumer is already on the shared selector route, a ledger or classifier consumer is on the displayed product route, and a seal-facing consumer is on the coverage route of \autoref{thm:cauchy-product-regseqrat-product-seal-coverage}. No fourth coordinate is exported by either packet inventory. Hence every accepted local consumer is exhausted by the enumerated alternatives, ledgers, and coverage rows, while each excluded analytic alternative remains outside the accepted BEDC packet.
\end{proof}

\begin{theorem}[CauchyProduct root-selector Real-seal exhaustion]
\label{thm:cauchy-product-root-selector-real-seal-exhaustion}
Every $\RealUp$-facing product seal read of an accepted $\CauchyProductUp$ packet whose factor windows pass through the shared finite observation-budget selector factors through the existing distributive lock, real budget multiplication boundary, $\RegSeqRatUp$ product seal coverage, $\DyadicRatCoreUp$ product rows, $\StreamNameUp$ windows, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows. In particular, the seal-facing route is exhausted by the displayed source windows, dyadic product observations, selector budget rows, regular-sequence handoff rows, componentwise transports, continuation replays, provenance rows, and local names; it exposes no host multiplication primitive, quotient stream equality, selected real value, independent real carrier, trichotomy row, or ambient completeness witness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_root\_selector\_real\_seal\_exhaustion}
\begin{proof}
First read the shared selector route. The distributive lock of \autoref{thm:cauchy-product-budget-selector-distributive-lock} binds both factor windows to one finite observation-budget selector before a product-facing consumer sees the product rows.

Next pass through the real-facing boundary. The real budget multiplication boundary of \autoref{thm:cauchy-product-real-budget-multiplication-boundary} forces every seal-facing read through the stream windows, dyadic radius and product rows, selector budget rows, regular handoff, transports, continuations, provenance, and local naming.

Finally use the RegSeqRat product seal coverage of \autoref{thm:cauchy-product-regseqrat-product-seal-coverage}. The regular-sequence side and the real-seal side therefore name the same finite route, so a root-selector consumer has no unlisted coordinate from which to read host multiplication, quotient equality, selected limits, order trichotomy, separate real carriers, or ambient completeness.
\end{proof}

\begin{theorem}[CauchyProduct shared-selector RegSeqRat--Real seal commutation]
\label{thm:cauchy-product-shared-selector-regseqrat-real-seal-commutation}
Let $Z=(X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N)$ be an accepted $\CauchyProductUp$ packet whose two factor windows are read through the same $\FiniteObservationBudgetSelectorUp$ route. Then the $\RegSeqRatUp$ classifier handoff and the $\RealUp$ seal handoff commute through one displayed route
$$
\StreamNameUp,\ \DyadicRatCoreUp,\ \FiniteObservationBudgetSelectorUp,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert.
$$
Equivalently, every product-facing consumer first sees the same stream names, dyadic product rows, selector budget rows, componentwise transports, continuation replays, provenance rows, and local naming row before either the regular-sequence classifier or the real seal is consumed. The commutation exposes no host multiplication, quotient stream equality, selected limits, trichotomy, independent real carriers, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_shared\_selector\_regseqrat\_real\_seal\_commutation}
\begin{proof}
Start from the shared selector route. By \autoref{thm:cauchy-product-budget-selector-distributive-lock}, the two factor windows are not read by unrelated budget packets: they are displayed through the same finite selector rows and the same product-route certificate.

Now pass to the two downstream handoffs. The real-facing side is bounded by \autoref{thm:cauchy-product-real-budget-multiplication-boundary}, while the regular-sequence side is exhausted by \autoref{thm:cauchy-product-regseqrat-product-seal-coverage}. Both statements name the same stream-window, dyadic, selector, transport, continuation, provenance, and local $\NameCert$ rows.

Composing these two readings gives the commutation claim. Whichever handoff a consumer cites first, the other handoff is reached only by replaying the same finite rows, so no unlisted host-level product, quotient equality, selected limit, order trichotomy, separate real carrier, or completeness principle can enter the route.
\end{proof}

\begin{theorem}[CauchyProduct RealSeries shared-selector handoff]
\label{thm:cauchy-product-realseries-shared-selector-handoff}
Let $Z=(X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N)$ be an accepted $\CauchyProductUp$ packet, and let $K=(T,S,W,Q,D,M,E_R,H_R,C_R,P_R,N_R)$ be an accepted $\RealSeriesUp$ packet. Suppose the product factor windows of $Z$ and the series tail windows of $K$ are consumed through the same finite observation-budget selector from \autoref{ch:concrete-instances-finiteobservationbudgetselector-namecert}. Then the product-facing $\RealUp$ seal of $Z$ and the series endpoint seal of $K$ read the same $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance ledger, $\RegSeqRatUp$ readback, selector budget rows, $\hsame$ transports, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows before either handoff is exposed.

The handoff is a finite shared-selector route. It excludes host multiplication, selected infinite sums, quotient stream equality, countable choice, detached real carriers, and ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_realseries\_shared\_selector\_handoff}
\begin{proof}
Start with the CauchyProduct side. The shared-selector commutation theorem \autoref{thm:cauchy-product-shared-selector-regseqrat-real-seal-commutation} places the product-facing regular handoff and real seal on one route through the stream windows, dyadic product rows, selector rows, transports, continuations, provenance, and local names.

Now read the RealSeries side through \autoref{thm:real-series-cauchy-consumer-obligation} and \autoref{thm:realseries-cauchy-consumer-readiness}. These results make the finite-tail consumer surface visible only through partial-sum windows, selected $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, the $\DyadicRatCoreUp$ tolerance ledger, the shared tail threshold, the endpoint seal, and the structural rows.

The common selector hypothesis identifies the selector budget rows used by the product windows with the selector budget rows used by the series tail windows. Apply \autoref{thm:cauchy-product-budget-selector-distributive-lock} to lock the product windows to that selector, \autoref{thm:cauchy-product-regseqrat-product-seal-coverage} to keep the regular product handoff on the same route, and \autoref{thm:cauchy-product-real-budget-multiplication-boundary} to place the product seal after the same budget ledger.

Finally use the RealSeries endpoint route \autoref{thm:realseries-regseqrat-endpoint-seal-route}. The endpoint seal of $K$ is downstream of the same finite tail windows, regular readback, dyadic ledger, threshold, transports, continuations, provenance, and local names. Since neither accepted packet contains host multiplication, an infinite-sum selector, quotient stream equality, countable choice, a detached real carrier, or ambient completeness data, the shared-selector handoff exposes none of those objects.
\end{proof}

\begin{theorem}[CauchyProduct shared-selector budget totality]
\label{thm:cauchy-product-shared-selector-budget-totality}
Let $Z=(X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N)$ be an accepted $\CauchyProductUp$ packet, and let $K=(T,S,W,Q,D,M,E_R,H_R,C_R,P_R,N_R)$ be an accepted $\RealSeriesUp$ packet. Suppose the product factor windows of $Z$ and the series tail windows of $K$ are consumed through one accepted $\FiniteObservationBudgetSelectorUp$ route
$$
B,S_B,W_B,D_B,R_B,E_B,H_B,C_B,P_B,N_B .
$$
Then every product-facing or series-facing consumer that reaches a $\RealUp$ endpoint must read the displayed CauchyProduct rows, the selector rows, the $\StreamNameUp$ window rows, the $\DyadicRatCoreUp$ product and tolerance observations, the $\RegSeqRatUp$ handoff rows, the RealSeries endpoint route, the componentwise $\hsame$ transports, displayed $\Cont$ replays, $\Pkg$ provenance rows, and local $\NameCert$ rows. No remaining endpoint route is available outside this finite budget.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_shared\_selector\_budget\_totality}
\begin{proof}
First use the distributive lock \autoref{thm:cauchy-product-budget-selector-distributive-lock}. It binds the two CauchyProduct factor windows to the single selector route before the dyadic product row and regular handoff can be consumed.

Next use the real budget multiplication boundary \autoref{thm:cauchy-product-real-budget-multiplication-boundary}. This places every product-facing $\RealUp$ seal downstream of the same stream windows, dyadic product rows, selector ledger, regular handoff, transport, replay, provenance, and local naming rows.

Finally apply the RealSeries shared-selector handoff \autoref{thm:cauchy-product-realseries-shared-selector-handoff}. The series endpoint reads the same finite selector route and therefore has no second budget, selected infinite-sum route, quotient stream equality, detached real carrier, or ambient completeness witness. The listed rows exhaust the shared-selector product route.
\end{proof}

\begin{theorem}[CauchyProduct dyadic product-window inversion]
\label{thm:cauchy-product-dyadic-product-window-inversion}
For an accepted $\CauchyProductUp$ packet $Z=(X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N)$ whose factor windows are consumed through a shared $\FiniteObservationBudgetSelectorUp$ route, any accepted read of the $\DyadicRatCoreUp$ product observations $Q_X,Q_Y$ determines the preceding product-window route
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y
$$
inside the same selector budget. The inversion is only the BEDC packet readback: it recovers the displayed $\StreamNameUp$ windows, dyadic product observations, selector rows, $\RegSeqRatUp$ handoff, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows. It does not recover host factors, a quotient stream equality, selected real values, or a parameter-equivalence transport.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_dyadic\_product\_window\_inversion}
\begin{proof}
Start with the window product non-escape theorem used by \autoref{thm:cauchy-product-budget-selector-distributive-lock}. A dyadic product observation in an accepted packet is downstream of the displayed factor windows and radius rows; it is not a detached product token.

Now attach the shared selector. The distributive lock places the factor windows and product observations inside one finite observation-budget selector route. Reading $Q_X,Q_Y$ therefore also reads the selector rows and the same componentwise transports, continuations, provenance, and local names.

Finally use \autoref{thm:cauchy-product-regseqrat-product-seal-coverage}. The regular-sequence product seal is downstream of exactly these rows, so the inversion recovers only the finite BEDC product-window route. No host factorization, quotient equality, selected real value, or parametric carrier transport is part of the recovered data.
\end{proof}

\begin{theorem}[CauchyProduct RealSeries budget exhaustion]
\label{thm:cauchy-product-real-series-budget-exhaustion}
Let $Z=(X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N)$ be an accepted $\CauchyProductUp$ packet and $K=(T,S,W,Q,D,M,E_R,H_R,C_R,P_R,N_R)$ an accepted $\RealSeriesUp$ packet whose windows are read through the same finite observation-budget selector. The combined product-and-series endpoint route is exhausted by the CauchyProduct displayed rows, the RealSeries tail and endpoint rows, the selector rows, $\StreamNameUp$ windows, $\DyadicRatCoreUp$ product and tolerance observations, $\RegSeqRatUp$ readback, $\RealUp$ endpoint seals, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ data. In particular, no consumer can add a second selector, an infinite-sum choice row, host multiplication, quotient stream equality, detached real carrier, trichotomy, or ambient completeness after the shared budget is spent.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_real\_series\_budget\_exhaustion}
\begin{proof}
First build the product side from the shared-selector budget totality of \autoref{thm:cauchy-product-shared-selector-budget-totality}. The product-facing route already contains the displayed CauchyProduct rows, selector rows, stream windows, dyadic product observations, regular handoff, and real seal.

Next read the product-window inversion of \autoref{thm:cauchy-product-dyadic-product-window-inversion}. Any dyadic product observation consumed by the route points back to the same factor-window and selector budget, so there is no hidden second window family.

Finally compose with the RealSeries handoff \autoref{thm:cauchy-product-realseries-shared-selector-handoff}. The series endpoint seal is downstream of the same finite tail windows, dyadic tolerance ledger, regular readback, transports, continuations, provenance, and local names. Thus the combined route has no coordinate left for a second selector, chosen infinite sum, host multiplication, quotient equality, detached real carrier, order trichotomy, or ambient completeness principle.
\end{proof}
