\begin{theorem}[CauchyProduct source-window bilinear synchronization]
\label{thm:cauchy-product-source-window-bilinear-synchronization}
For every accepted $\CauchyProductUp$ packet, the paired source windows from $\StreamNameUp$ synchronize the two $\RegularCauchyNameUp$ inputs before any product row is read. Concretely, every product observation must first factor through the finite source route
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y
$$
over the sibling carriers of \autoref{ch:concrete-instances-streamname-namecert} and \autoref{ch:concrete-instances-dyadicratcore-namecert}. The row $P$ is therefore a bilinear readback from the displayed pair of regular-Cauchy source windows, not a host multiplication primitive, selected limit, quotient stream, or ambient product operation.
\end{theorem}
\begin{proof}
Start with the root source admission theorem \autoref{thm:cauchy-product-root-source-admission}. It places the two $\RegularCauchyNameUp$ source rows and the paired $\StreamNameUp$ windows inside the same finite $\BHist$ surface.

Next use the radius budget conservation theorem \autoref{thm:cauchy-product-radius-budget-conservation}. The dyadic rows from \autoref{ch:concrete-instances-dyadicratcore-namecert} attach the source windows to the product-radius ledger before $P$ can be inspected.

The sibling regular-rational and real chapters, \autoref{ch:concrete-instances-regseqrat-namecert} and \autoref{ch:concrete-instances-real-namecert}, then consume only that displayed route. Since the route has no coordinate for host multiplication or quotient streams, the product row is synchronized by the paired windows before any downstream read.
\end{proof}

\begin{theorem}[CauchyProduct shared budget radius exhaustion]
\label{thm:cauchy-product-shared-budget-radius-exhaustion}
For every accepted $\CauchyProductUp$ packet and every product observation requested by a downstream reader, the shared dyadic radius and budget rows exhaust the product observation before the $\RegSeqRatUp$ classifier and $\RealUp$ seal are consumed. The only available route is the finite ledger
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N
$$
together with the displayed observation-budget rows when such a budget packet is present. No second radius source, hidden selector, selected limit, quotient stream, host multiplication estimate, or ambient completeness row can feed the classifier after this ledger has been consumed.
\end{theorem}
\begin{proof}
Read the conservation surface through \autoref{thm:cauchy-product-radius-budget-conservation}. It identifies the paired source radii, dyadic product observations, product row, and classifier handoff as one finite accounting route.

Apply \autoref{thm:cauchy-product-regseq-seal-nonescape}. The $\RegSeqRatUp$ classifier is reached only after the displayed product route has been read, and the $\RealUp$ seal is downstream of the same classifier boundary.

Finally combine this with the handoff row \autoref{thm:cauchy-product-realalgorder-completion-handoff}. The order and completion consumers receive the same exhausted ledger rather than a fresh analytic source, so the radius and budget rows close the product observation before the classifier and seal are consumed.
\end{proof}

\begin{theorem}[CauchyProduct Real-completion consumer lock]
\label{thm:cauchy-product-real-completion-consumer-lock}
Every $\RealCompletenessUp$ or $\RealAlgOrderUp$ product consumer of an accepted $\CauchyProductUp$ packet is locked to the same finite route
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N
$$
and, when present, to the same finite observation-budget rows. Such a consumer cannot import quotient streams, selected limits, host multiplication, trichotomy, independent real carriers, or ambient completeness as an alternative source for the product seal.
\end{theorem}
\begin{proof}
First use source-window synchronization from \autoref{thm:cauchy-product-source-window-bilinear-synchronization}. It fixes the paired $\RegularCauchyNameUp$ and $\StreamNameUp$ source surface before the product row is read.

Next apply shared budget radius exhaustion from \autoref{thm:cauchy-product-shared-budget-radius-exhaustion}. The $\RegSeqRatUp$ classifier and $\RealUp$ seal are reached only through the conserved dyadic radius and product-budget ledger.

Now read the consumer handoff through \autoref{thm:cauchy-product-realalgorder-completion-handoff} together with the sibling surfaces \autoref{ch:concrete-instances-regseqrat-namecert} and \autoref{ch:concrete-instances-real-namecert}. The $\RealAlgOrderUp$ and $\RealCompletenessUp$ consumers therefore receive exactly the finite route already displayed by the product packet, and no excluded analytic object has a coordinate from which it could be imported.
\end{proof}
