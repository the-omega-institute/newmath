\begin{theorem}[CauchyProduct budgeted root source triple]
\label{thm:cauchy-product-budgeted-root-source-triple}
For every accepted $\CauchyProductUp$ packet, the two $\RegularCauchyNameUp$ source rows, the paired $\StreamNameUp$ finite-window rows, and the $\DyadicRatCoreUp$ radius and observation rows form the admitted root source triple for the product classifier. The triple is the finite $\BHist$ route
$$
(X,Y),\quad (W_X,W_Y),\quad (M_X,M_Y,Q_X,Q_Y)
$$
inside the packet
$$
Z=(X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N),
$$
and it reaches the product row and $\RegSeqRatUp$ classifier only through the displayed $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\CauchyProductUp}$ rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-product-namecert-source}. Its displayed coordinates put the two regular Cauchy source rows, the paired windows, and the dyadic radius and observation rows in one finite packet before the product row $P$ and classifier row $E$ are read.

Apply the root source admission of \autoref{thm:cauchy-product-root-source-admission} and the root source window totality of \autoref{thm:cauchy-product-root-source-window-totality}. These rows force every product read to enter through the two source rows and the paired StreamName windows.

Now use the root budget product lock of \autoref{thm:cauchy-product-root-budget-product-lock}. The dyadic observations and radius rows are the budgeted route into $P$ and $E$, while $H,C,L,N$ carry the displayed transport, continuation, package, and local naming evidence. Coordinate exhaustion leaves no second source route.
\end{proof}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_budgeted\_root\_source\_triple}

\begin{theorem}[CauchyProduct window-budget NameCert ledger]
\label{thm:cauchy-product-window-budget-namecert-ledger}
For an accepted $\CauchyProductUp$ packet feeding a product observation into $\RegSeqRatUp$, the finite window-budget ledger is the only $\NameCert_{\CauchyProductUp}$ ledger available to that observation. It is the displayed inventory
$$
W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,L,H,C,N
$$
together with the $\RegSeqRatUp$ classifier row $E$ and, when a $\RealObservationBudgetUp$ packet consumes the read, the budget rows
$$
E_B,W_B,D_B,R_B,S_B,H_B,C_B,P_B,N_B.
$$
No separate ledger can feed the product observation into $\RegSeqRatUp$.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_window\_budget\_namecert\_ledger}
\begin{proof}
First read the product ledger through \autoref{thm:cauchy-product-ledger-obligation}. It binds the radius rows, product row, transport rows, continuation rows, package provenance, and local naming row as one finite accounting surface.

Next apply the window-budget composition surface of \autoref{thm:cauchy-product-window-budget-composition-surface}. This identifies the path from paired StreamName windows and DyadicRatCore rows to the $\RegSeqRatUp$ classifier $E$ as a single finite route.

Finally use the budgeted product consumer boundary of \autoref{thm:cauchy-product-budget-product-consumer-boundary}. If a budget packet is present, it adds only the displayed budget rows, and those rows read the same product ledger. Thus the NameCert ledger feeding the RegSeqRat observation is exactly the stated inventory.
\end{proof}

\begin{theorem}[CauchyProduct Real seal root consumer boundary]
\label{thm:cauchy-product-real-seal-root-consumer-boundary}
Every $\RealUp$-facing product seal read of an accepted $\CauchyProductUp$ packet factors through the displayed $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of the root package. The consumer boundary is the finite route from the two source rows through the paired windows, dyadic radius and observation rows, product row, ledger, classifier row, and seal handoff; it does not pass through host multiplication, quotient streams, selected limits, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_real\_seal\_root\_consumer\_boundary}
\begin{proof}
Begin with the budgeted root source triple of \autoref{thm:cauchy-product-budgeted-root-source-triple}. It fixes the source, window, radius, and dyadic observation rows before the product row and classifier can be inspected.

Use the window-budget NameCert ledger of \autoref{thm:cauchy-product-window-budget-namecert-ledger}. The ledger identifies the exact route through $P,L,H,C,N$ and, when present, through the observation-budget rows feeding the same classifier.

Finally apply the Real seal root surface of \autoref{thm:cauchy-product-real-seal-root-unblock-surface} together with the budget-triangle Real seal exhaustion of \autoref{thm:cauchy-product-budget-triangle-real-seal-exhaustion}. These theorems place every real-facing product seal behind the same finite product and budget rows, so host multiplication, quotient streams, selected limits, and ambient completeness have no consumer coordinate.
\end{proof}

\begin{theorem}[CauchyProduct root Real seal factorization]
\label{thm:cauchy-product-root-real-seal-factorization}
Every $\RealUp$ seal read that consumes an accepted $\CauchyProductUp$ packet factors through the two $\RegularCauchyNameUp$ source rows, the paired $\StreamNameUp$ finite-window rows, the dyadic radius and readback rows, the product row, the $\RegSeqRatUp$ classifier row, and the local $\NameCert_{\CauchyProductUp}$ rows. In $\BHist$ coordinates the read is routed through
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,E,H,C,L,N,
$$
with each transport witnessed by the displayed $\hsame$ rows, each admissible continuation witnessed by the displayed $\Cont$ rows, and each package handoff witnessed by the displayed $\Pkg$ and $\NameCert$ rows. No $\RealUp$ consumer obtains a separate seal factor outside this finite route.
\end{theorem}
\begin{proof}
Start from the budgeted root source triple of \autoref{thm:cauchy-product-budgeted-root-source-triple}. It supplies the two regular Cauchy sources, the paired finite windows, and the dyadic budget rows before the product and classifier rows can be inspected.

Use the window-budget NameCert ledger of \autoref{thm:cauchy-product-window-budget-namecert-ledger}. The ledger binds $P,E,H,C,L,N$ to the same finite route and records the local NameCert evidence for the product observation.

Finally apply the Real seal root consumer boundary of \autoref{thm:cauchy-product-real-seal-root-consumer-boundary}. Since that boundary admits only the displayed root package rows for real-facing seal reads, the factorization is exactly the stated $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ route.
\end{proof}

\begin{theorem}[CauchyProduct root RegSeqRat classifier lock]
\label{thm:cauchy-product-root-regseqrat-classifier-lock}
For every accepted $\CauchyProductUp$ packet, the $\RegSeqRatUp$ classifier input used by a product consumer is locked to the paired-window and dyadic-budget route
$$
(W_X,W_Y,M_X,M_Y,Q_X,Q_Y)\longrightarrow P\longrightarrow E.
$$
The classifier row $E$ may read the two regular Cauchy source rows only through the displayed paired $\StreamNameUp$ windows, dyadic radius rows, dyadic readback rows, product row, $\hsame$ transports, $\Cont$ continuations, $\Pkg$ provenance, and local $\NameCert_{\CauchyProductUp}$ ledger. It cannot be fed by an alternate product stream, quotient representative, ambient multiplication operation, or selected limit.
\end{theorem}
\begin{proof}
The budgeted root source triple in \autoref{thm:cauchy-product-budgeted-root-source-triple} identifies the only source, window, and dyadic budget coordinates available before $P$ and $E$ are formed.

The window-budget NameCert ledger of \autoref{thm:cauchy-product-window-budget-namecert-ledger} then ties those coordinates to the product row and classifier row as one local accounting surface. Any classifier input that omits this ledger has no $\NameCert_{\CauchyProductUp}$ row in the packet.

The Real seal root consumer boundary of \autoref{thm:cauchy-product-real-seal-root-consumer-boundary} excludes all non-root analytic routes at the consumer edge. Therefore the $\RegSeqRatUp$ classifier input is locked to the displayed paired-window and dyadic-budget route.
\end{proof}

\begin{theorem}[CauchyProduct root completion non-escape]
\label{thm:cauchy-product-root-completion-nonescape}
Every completion-facing product consumer of an accepted $\CauchyProductUp$ packet remains inside the finite root package route. In particular, the consumer may use only the two regular Cauchy source rows, paired windows, dyadic radius and readback rows, product row, $\RegSeqRatUp$ classifier row, $\BHist$ route, $\hsame$ transports, $\Cont$ continuations, $\Pkg$ provenance, and local $\NameCert_{\CauchyProductUp}$ rows displayed in the packet. Host multiplication, quotient streams, selected limits, and ambient completeness are not available as consumer witnesses for the product seal.
\end{theorem}
\begin{proof}
Apply the budgeted root source triple of \autoref{thm:cauchy-product-budgeted-root-source-triple}. It places every product consumer behind the two source rows, paired windows, and dyadic budget rows.

Apply the window-budget NameCert ledger of \autoref{thm:cauchy-product-window-budget-namecert-ledger}. The ledger shows that the product observation and $\RegSeqRatUp$ classifier share the same finite $\BHist$ route, transport rows, continuation rows, package provenance, and local naming row.

Apply the Real seal root consumer boundary of \autoref{thm:cauchy-product-real-seal-root-consumer-boundary}. That boundary already denies host multiplication, quotient streams, selected limits, and ambient completeness at the real-facing seal edge, so a completion-facing consumer has no remaining route by which to escape the displayed root package.
\end{proof}

\begin{theorem}[CauchyProduct root source diagonal consistency]
\label{thm:cauchy-product-root-source-diagonal-consistency}
Every accepted $\CauchyProductUp$ root package presents one diagonal-consistent source route from the two $\RegularCauchyNameUp$ source rows through the paired $\StreamNameUp$ finite windows, $\DyadicRatCoreUp$ observations, $\RegSeqRatUp$ classifier input, and the $\RealUp$ seal. The route is the finite handoff
$$
(X,Y)\to(W_X,W_Y)\to(M_X,M_Y,Q_X,Q_Y)\to P\to E\to \RealUp
$$
inside the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert_{\CauchyProductUp}$ rows. It denies host multiplication, quotient streams, selected limits, and ambient completeness as source witnesses for the product seal.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:cauchy-product-budgeted-root-source-triple}. It fixes the two source rows, paired StreamName windows, and dyadic budget rows as the only root source triple feeding the product classifier.

Use \autoref{thm:cauchy-product-window-budget-namecert-ledger}. The ledger ties the windows, dyadic observations, product row, classifier row, transport rows, continuation rows, package provenance, and local naming row into one finite accounting surface.

Next apply \autoref{thm:cauchy-product-real-seal-root-consumer-boundary} and \autoref{thm:cauchy-product-root-regseqrat-classifier-lock}. These rows place the Real-facing seal behind the same root package and lock the $\RegSeqRatUp$ classifier input to the paired-window and dyadic-budget route.

Finally use \autoref{thm:cauchy-product-root-completion-nonescape}. Since completion-facing consumers cannot leave the displayed root package, the preceding rows form one diagonal-consistent Real/Completion handoff and no host multiplication, quotient stream, selected limit, or ambient completeness coordinate remains available.
\end{proof}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_root\_source\_diagonal\_consistency}

\begin{theorem}[CauchyProduct source-window budget compatibility]
\label{thm:cauchy-product-source-window-budget-compatibility}
For every accepted $\CauchyProductUp$ root package, the source-window route and the product-budget route are compatible on the single finite consumer path
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,L,E,H,C,N.
$$
The compatibility composes the root source-window totality, budget product lock, StreamName-RegSeqRat source lock, RealTailAgreementSeal handoff, and root source diagonal consistency. It leaves no route through quotient streams, host multiplication, selected limits, trichotomy, or ambient completeness.
\end{theorem}
\begin{proof}
Start with \autoref{thm:cauchy-product-root-source-window-totality}. It exposes the two $\RegularCauchyNameUp$ source rows only through the paired $\StreamNameUp$ windows $W_X,W_Y$.

Apply \autoref{thm:cauchy-product-root-budget-product-lock}. This places the dyadic rows $M_X,M_Y,Q_X,Q_Y$, product row $P$, and budget ledger $L$ before the $\RegSeqRatUp$ classifier row $E$ is read.

Now use \autoref{thm:cauchy-product-streamname-regseqrat-source-lock} and \autoref{thm:cauchy-product-root-source-diagonal-consistency}. The source windows and budget rows feed one diagonal-consistent route to $E$ and then to the $\RealUp$ seal.

Finally apply \autoref{thm:cauchy-product-realtailagreementseal-handoff}. The Real-facing handoff consumes the same finite route, so the excluded analytic resources have no coordinate from which to enter the product seal.
\end{proof}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_source\_window\_budget\_compatibility}

\begin{theorem}[CauchyProduct RegSeqRat product budget exhaustion]
\label{thm:cauchy-product-regseqrat-product-budget-exhaustion}
Every $\RegSeqRatUp$ classifier read used by an accepted $\CauchyProductUp$ product seal is exhausted by the displayed rows $X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,L,E,H,C,N$. In particular, the classifier input is obtained from the paired source windows, dyadic radius and observation rows, product row, and budget ledger, rather than from a quotient stream, a host multiplication primitive, a selected product limit, trichotomy, or ambient completeness.
\end{theorem}
\begin{proof}
Read the classifier lock in \autoref{thm:cauchy-product-streamname-regseqrat-source-lock}. It fixes both source rows behind $W_X,W_Y$ before any $\RegSeqRatUp$ or product row can be consumed.

Use \autoref{thm:cauchy-product-root-budget-product-lock}. The lock makes $M_X,M_Y,Q_X,Q_Y,P,L$ the only budget route into $E$.

Apply \autoref{thm:cauchy-product-source-window-budget-compatibility}. The compatibility theorem joins the source windows, budget rows, classifier row, and structural rows into one route.

The non-escape part follows from \autoref{thm:cauchy-product-root-completion-nonescape}. Since completion-facing consumers cannot leave the root package, the classifier read is exhausted by the displayed budget route.
\end{proof}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_regseqrat\_product\_budget\_exhaustion}

\begin{theorem}[CauchyProduct Real seal source-budget non-escape]
\label{thm:cauchy-product-real-seal-source-budget-nonescape}
Any $\RealUp$-facing product seal consumer of an accepted $\CauchyProductUp$ packet must pass through
$$
X,Y,W_X,W_Y,M_X,M_Y,Q_X,Q_Y,P,L,E,H,C,N
$$
before exporting the product seal. The route composes the source-window totality, product-budget exhaustion, StreamName-RegSeqRat lock, RealTailAgreementSeal handoff, and root source diagonal consistency; no quotient stream, host multiplication primitive, selected limit, trichotomy row, or ambient completeness witness is exported with the seal.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyProductUp.CauchyProductPacket\_real\_seal\_source\_budget\_nonescape}
\begin{proof}
Begin with \autoref{thm:cauchy-product-source-window-budget-compatibility}. It gives the single finite source-budget path from the two source rows through the paired windows, dyadic rows, product row, budget ledger, classifier row, and structural rows.

Apply \autoref{thm:cauchy-product-regseqrat-product-budget-exhaustion}. This exhausts the $\RegSeqRatUp$ classifier input by the same product-budget path.

Next use \autoref{thm:cauchy-product-realtailagreementseal-handoff}. The Real-facing seal comparison consumes the paired-window and product-budget route before reaching the sibling $\RealTailAgreementSealUp$ surface.

Finally apply \autoref{thm:cauchy-product-root-source-diagonal-consistency}. The Real/Completion handoff is diagonal-consistent inside the displayed root package, so no excluded analytic object can be repacked as a source-budget witness.
\end{proof}
