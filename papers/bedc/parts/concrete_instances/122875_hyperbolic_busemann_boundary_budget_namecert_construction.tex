\chapter{A Concrete Naming Certificate for $\HyperbolicBusemannBoundaryBudgetUp$}
\label{ch:concrete-instances-hyperbolic-busemann-boundary-budget-namecert}
\origin{ai}

The $\HyperbolicBusemannBoundaryBudgetUp$ packet realizes the boundary-budget branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite BEDC NameCert data. It sits after the hyperbolic metric row of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the Busemann boundary row of \autoref{ch:concrete-instances-hyperbolic-busemann-boundary-namecert}, the boundary-fixed disk transport row of \autoref{ch:concrete-instances-poincare-disk-boundary-transport-namecert}, the horocycle row of \autoref{ch:concrete-instances-hyperbolic-horocycle-namecert}, and the horosphere row of \autoref{ch:concrete-instances-hyperbolic-horosphere-namecert}. The packet names the finite disk-map, metric-read, boundary, classifier-stability, provenance, and refusal rows required by observer-expansion boundary budgets; it is not a global hyperbolic model, a quotient ideal boundary, a selected geodesic ray, or a physical observer.

\begin{definition}[Hyperbolic Busemann-boundary budget carrier]
\label{def:hyperbolic-busemann-boundary-budget-carrier}
A $\HyperbolicBusemannBoundaryBudgetUp$ carrier is a finite $\BHist$ packet
$$
B=(D,M,U,Y,O,S,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the disk-map readout row, keeping the inspected Poincare-disk coordinate and boundary-admission window finite.
\item $M$ is the $\HyperbolicMetricUp$ metric-read row attached to that disk window.
\item $U$ is the $\HyperbolicBusemannBoundaryUp$ boundary row, exposing Busemann-level readback only through displayed metric and boundary data.
\item $Y$ is the boundary-fixed transport row supplied by the Poincare disk boundary-transport sibling.
\item $O$ is the horocycle and horosphere budget row, combining the displayed $\HyperbolicHorocycleUp$ and $\HyperbolicHorosphereUp$ boundary-level reads.
\item $S$ is the classifier-stability row, requiring the same disk, metric, Busemann-boundary, transport, and budget rows before a boundary verdict is public.
\item $R$ is the refusal row excluding interior far-end fixed points, quotient ideal-boundary equality, selected rays, and detached observer states.
\item $H$ records componentwise $\hsame$ transport for $D,M,U,Y,O,S,R,C,P,N$.
\item $C$ records displayed $\Cont$ replay from disk readout through metric read, Busemann boundary, boundary-fixed transport, budget, classifier stability, refusal, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicBusemannBoundaryBudgetUp$, $\HyperbolicMetricUp$, $\HyperbolicBusemannBoundaryUp$, $\HyperbolicHorocycleUp$, $\HyperbolicHorosphereUp$, boundary-fixed transport, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicBusemannBoundaryBudgetUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a host hyperbolic surface, quotient ideal-boundary equality, selected geodesic rays, a physical observer, or ambient completeness.
\end{definition}

\begin{theorem}[Hyperbolic Busemann-boundary budget NameCert obligations]
\label{thm:hyperbolic-busemann-boundary-budget-namecert-obligations}
For every accepted $\HyperbolicBusemannBoundaryBudgetUp$ carrier
$$
B=(D,M,U,Y,O,S,R,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicBusemannBoundaryBudgetUp}$ surface consists exactly of disk-map readout through $D$, hyperbolic metric readback through $M$, Busemann-boundary exposure through $U$, boundary-fixed transport through $Y$, horocycle and horosphere budget exposure through $O$, classifier stability through $S$, refusal through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-busemann-boundary-budget-carrier}. The metric and boundary rows $M,U$ are not public until the disk-map row $D$ is displayed, and the budget row $O$ is not public until the Busemann-boundary and boundary-fixed transport rows are visible.

The classifier-stability row $S$ reads the same disk, metric, boundary, transport, and budget rows before it exports a boundary verdict. The refusal row $R$ then records the nonescape constraints: no interior far-end fixed point, quotient ideal-boundary equality, selected ray, or detached observer state can be projected from the packet.

The structural rows preserve the same route. Transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$ expose the finite NameCert surface and no additional hyperbolic host object.
\end{proof}

\begin{theorem}[Hyperbolic Busemann-boundary budget TasteGate requirements]
\label{thm:hyperbolic-busemann-boundary-budget-tastegate-requirements}
The Lean follow-up for $\HyperbolicBusemannBoundaryBudgetUp$ must provide $\mathsf{Nontrivial}\ \HyperbolicBusemannBoundaryBudgetUp$ and $\mathsf{FieldFaithful}\ \HyperbolicBusemannBoundaryBudgetUp$ instances for the same carrier
$$
B=(D,M,U,Y,O,S,R,H,C,P,N).
$$
Nontriviality must distinguish a packet with the displayed boundary budget from a packet with a missing disk, metric, Busemann-boundary, transport, budget, classifier-stability, or refusal row. Field faithfulness must preserve the row order $D,M,U,Y,O,S,R,H,C,P,N$ without replacing it by a host hyperbolic model or quotient boundary.
\end{theorem}
\begin{proof}
Read the requirement from \autoref{def:hyperbolic-busemann-boundary-budget-carrier}. The packet is nontrivial only when the disk-map, metric, Busemann-boundary, boundary-fixed transport, horocycle and horosphere budget, classifier-stability, refusal, transport, replay, provenance, and local naming rows are all present.

Field faithfulness is the same row-preservation demand. Any classifier that identifies two packets while changing one of $D,M,U,Y,O,S,R,H,C,P,N$ would erase the finite boundary-budget route and would no longer be a faithful TasteGate for the chapter.

The excluded host objects are absent from the carrier and from the NameCert obligations of \autoref{thm:hyperbolic-busemann-boundary-budget-namecert-obligations}. The Lean follow-up therefore has to certify the finite carrier, not a global hyperbolic surface or quotient ideal boundary.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicBusemannBoundaryBudgetUp$ boundary-budget read exposes disk-map, hyperbolic metric, Busemann-boundary, boundary-fixed transport, horocycle and horosphere budget, classifier-stability, refusal, transport, replay, provenance, and local naming rows before any observer-expansion boundary verdict is public.}

\independenceWitness{hyperbolic-metric, hyperbolic-busemann-boundary, poincare-disk-boundary-transport, hyperbolic-horocycle, hyperbolic-horosphere: $\HyperbolicBusemannBoundaryBudgetUp$ is not bijective to any listed sibling because it jointly binds disk readout, metric readback, Busemann-boundary exposure, boundary-fixed transport, horocycle and horosphere budgets, classifier stability, refusal, transport, replay, provenance, and local NameCert rows.}

\closureat{HyperbolicBusemannBoundaryBudgetUp}{seedStr}

\begin{closurestatus}{\HyperbolicBusemannBoundaryBudgetUp}
  \constructivestory{A $\HyperbolicBusemannBoundaryBudgetUp$ packet is generated from disk-map readout, hyperbolic metric readback, Busemann-boundary exposure, boundary-fixed transport, horocycle and horosphere budget rows, classifier stability, refusal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-busemann-boundary-budget-carrier}, \autoref{thm:hyperbolic-busemann-boundary-budget-namecert-obligations}, and \autoref{thm:hyperbolic-busemann-boundary-budget-tastegate-requirements} bind the finite boundary-budget carrier, NameCert surface, and TasteGate follow-up requirements.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close Lean verification, bridge checking, a global hyperbolic model, quotient ideal-boundary equality, selected geodesic rays, physical observers, or ambient completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, disk-map readout, metric readback, Busemann-boundary exposure, boundary-fixed transport, horocycle and horosphere budget exactness, classifier stability, refusal, and TasteGate Nontrivial and FieldFaithful instances for BEDC.Derived.HyperbolicBusemannBoundaryBudgetUp.}
\end{closurestatus}
