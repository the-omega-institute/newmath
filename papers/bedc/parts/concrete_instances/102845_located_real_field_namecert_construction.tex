\chapter{A Concrete Naming Certificate for $\LocatedRealFieldUp$}
\label{ch:concrete-instances-located-real-field-namecert}
\origin{human}

Following Bishop and Bridges, the $\LocatedRealFieldUp$ packet records the finite field interface for located Cauchy real names. It sits after the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational source surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the Cauchy real field route of \autoref{ch:concrete-instances-cauchy-real-field-namecert}, and the positive apartness surface of \autoref{ch:concrete-instances-cauchy-real-apartness-namecert}. The packet gives Real and Completion consumers one constructive field-facing surface over located Cauchy names; it is not order trichotomy, quotient real equality, selected representatives, countable choice, ambient $\RealUp$ completeness, or bridge checking.

\begin{definition}[Located real field carrier]
\label{def:located-real-field-carrier}
A $\LocatedRealFieldUp$ carrier is a finite $\BHist$ packet
$$
L=(E,R,W,D,F,A,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the located real source row. It records the displayed located Cauchy real names and exposes them only through the terminal $\RealUp$ seal and the local locatedness data.
\item $R$ is the $\RegSeqRatUp$ source-window row used to read every located real source through regular rational Cauchy data.
\item $W$ is the synchronized $\StreamNameUp$ finite-window discipline for operation, comparison, and apartness reads.
\item $D$ is the $\DyadicRatCoreUp$ tolerance and arithmetic ledger. It records the dyadic endpoint operations, positive-radius tolerances, comparison slack, and reciprocal-denominator budgets used by the field surface.
\item $F$ is the $\CauchyRealFieldUp$ operation row for zero, one, negation, addition, multiplication, and partial reciprocal reads on the displayed Cauchy real names.
\item $A$ is the $\CauchyRealApartnessUp$ positive-apartness row consumed by reciprocal eligibility and by downstream located-order consumers.
\item $O$ is the located comparison row. It records the order and comparison boundary available from the located real presentation without turning it into trichotomy or quotient equality.
\item $H$ records componentwise $\hsame$ transport for the source, regular readback, window, dyadic, field, apartness, comparison, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from each field request through $E,R,W,D,F,A,O,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\LocatedRealFieldUp$, $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchyRealFieldUp$, $\CauchyRealApartnessUp$, $\LocatedRealComparisonUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedRealFieldUp}$ row.
\end{enumerate}
The carrier predicate asserts exactly that these rows form one finite constructive located-field packet. It has no coordinate for trichotomy, quotient real equality, selected representatives, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Located real field NameCert obligations]
\label{thm:located-real-field-namecert-obligations}
For every accepted $\LocatedRealFieldUp$ carrier
$$
L=(E,R,W,D,F,A,O,H,C,P,N),
$$
the local $\NameCert_{\LocatedRealFieldUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the located real source data, regular rational readback row, synchronized finite windows, dyadic tolerance ledger, Cauchy real field row, apartness row, comparison row, transport row, replay row, provenance row, and local naming row;
\item operation projection, requiring every zero, one, negation, addition, multiplication, or partial reciprocal read to pass through the displayed $\CauchyRealFieldUp$ row $F$ after the source, window, and dyadic rows are visible;
\item apartness and comparison handoff, requiring reciprocal eligibility, positive-gap consumption, and order-facing reads to use the displayed $\CauchyRealApartnessUp$ row $A$ and located comparison row $O$;
\item transport and replay stability, requiring $H$ and $C$ to preserve the same componentwise source, readback, window, dyadic, operation, apartness, comparison, provenance, and local naming rows;
\item ledger exhaustion, excluding trichotomy, quotient real equality, selected representatives, countable choice, ambient $\RealUp$ completeness, and bridge checking from the local certificate.
\end{enumerate}
No located real field obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:located-real-field-carrier}. The accepted packet displays exactly the located real source row, regular rational readback, synchronized windows, dyadic tolerance ledger, Cauchy real field row, apartness row, comparison row, and structural rows.

The operation projection is inherited from \autoref{ch:concrete-instances-cauchy-real-field-namecert}. Its row $F$ is the only place where the packet stores zero, one, negation, addition, multiplication, or reciprocal data, and every such read remains downstream of $E,R,W,D$.

The apartness and comparison handoff is inherited from \autoref{ch:concrete-instances-cauchy-real-apartness-namecert} and the located comparison surface. The positive row $A$ supplies the finite apartness boundary used by reciprocal eligibility, while $O$ supplies comparison data without adding a trichotomy coordinate.

Finally, $H,C,P,N$ transport, replay, source, and name only the displayed rows. Since the carrier contains no coordinate for the excluded principles, none can become a local NameCert obligation.
\end{proof}

\begin{theorem}[Located real field apartness-field handoff]
\label{thm:located-real-field-apartness-field-handoff}
Every accepted $\LocatedRealFieldUp$ carrier routes any reciprocal or positive-gap field read through the finite chain
$$
E,R,W,D\longmapsto A,O\longmapsto F\longmapsto H,C,P,N .
$$
Thus the field surface consumes located apartness and comparison data before exposing a reciprocal branch, while addition and multiplication remain confined to the same regular rational windows and dyadic arithmetic ledger.
\end{theorem}
\begin{proof}
Start with an accepted carrier $L=(E,R,W,D,F,A,O,H,C,P,N)$. The only located real source is $E$, and all field reads first pass through the regular rational row $R$, synchronized window row $W$, and dyadic ledger $D$.

For a reciprocal or positive-gap read, the apartness row $A$ must be available before the field row exposes the reciprocal branch. The comparison row $O$ records the located order boundary used by the same consumer, but it does not decide trichotomy or quotient equality.

For addition and multiplication, the field row $F$ uses the same $R,W,D$ route and does not receive separate host arithmetic. These branches therefore remain tied to the located Cauchy real presentation.

The structural rows are downstream of both cases. They preserve the displayed handoff by componentwise transport, replay, provenance, and local naming, and they do not add a selected representative, countable-choice selector, ambient completeness witness, or bridge credential.
\end{proof}

\closureat{LocatedRealFieldUp}{seedStr}
\begin{closurestatus}{\LocatedRealFieldUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-real-field-carrier}, \autoref{thm:located-real-field-namecert-obligations}, and \autoref{thm:located-real-field-apartness-field-handoff} seed the finite located-field route over located real source data, regular rational windows, dyadic tolerances, Cauchy real field operations, positive apartness, located comparison, componentwise transport, replay, provenance, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \leantarget{BEDC.Derived.LocatedRealFieldUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close trichotomy, quotient real equality, selected representatives, countable choice, ambient $\RealUp$ completeness, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, operation projection, apartness-field handoff, comparison confinement, transport, replay, provenance, and local naming for BEDC.Derived.LocatedRealFieldUp.}
  \constructivestory{A $\LocatedRealFieldUp$ packet is generated from located real source data, RegSeqRat readback windows, StreamName finite windows, DyadicRatCore tolerance rows, a CauchyRealField operation row, a CauchyRealApartness positive row, a LocatedRealComparison boundary row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
