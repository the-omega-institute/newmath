\chapter{A Concrete Naming Certificate for $\ConstructiveCompactUniformFanUp$}
\label{ch:concrete-instances-constructive-compact-uniform-fan-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis and the fan-theorem route for compact metric sources, the $\ConstructiveCompactUniformFanUp$ packet records the finite branch data by which compact-uniform continuity is read as a bounded finite-window induction. It sits between the fan-uniform route of \autoref{ch:concrete-instances-fan-uniform-continuity-namecert}, the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the map graph of \autoref{ch:concrete-instances-continuousmap-namecert}, the downstream modulus surface of \autoref{ch:concrete-instances-uniformmodulus-namecert}, and the real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the constructive fan ledger; it does not assert an ambient open-cover theorem, a choice-selected branch, quotient function equality, or completeness of $\RealUp$.

\begin{definition}[Constructive compact uniform fan carrier]
\label{def:constructive-compact-uniform-fan-carrier}
A $\ConstructiveCompactUniformFanUp$ carrier is a finite $\BHist$ packet
$$
F=(K,B,T,W,D,M,U,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row carrying the compact finite-net and metric-window data inspected by the fan.
\item $B$ is the finite branch ledger, listing the branch prefixes over which the fan induction ranges.
\item $T$ is the terminal-bar row, recording that every inspected branch reaches a displayed terminal prefix.
\item $W$ is the $\StreamNameUp$ finite-window row attached to the inspected branch prefixes.
\item $D$ is the $\DyadicRatCoreUp$ tolerance and diameter row for the finite windows.
\item $M$ is the $\ContinuousMapUp$ pointwise-continuity row whose local data is read at each branch prefix.
\item $U$ is the $\UniformModulusUp$ handoff row produced after the branch ledger and tolerance rows have been exhausted.
\item $E$ is the terminal $\RealUp$ seal row for codomain distance and tolerance comparisons.
\item $H$ records componentwise $\hsame$ transport for $K,B,T,W,D,M,U,E$ and the local packet shape.
\item $C$ records displayed $\Cont$ replay from compact-metric source data through the branch ledger, terminal-bar row, windows, dyadic tolerances, continuous-map reads, uniform-modulus handoff, and real seal.
\item $P$ is the $\Pkg$ provenance over $\ConstructiveCompactUniformFanUp$, $\FanUniformContinuityUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\ConstructiveCompactUniformFanUp}$ row.
\end{enumerate}
The carrier exports no open-cover compactness theorem, infinite bar object, branch selector, quotient equality of functions, global choice coordinate, or ambient $\RealUp$ completeness row.
\end{definition}
\leanchecked{BEDC.Derived.ConstructiveCompactUniformFanUp.ConstructiveCompactUniformFanTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Constructive compact uniform fan NameCert obligations]
\label{thm:constructive-compact-uniform-fan-namecert-obligations}
For every accepted $\ConstructiveCompactUniformFanUp$ carrier
$$
F=(K,B,T,W,D,M,U,E,H,C,P,N),
$$
the local $\NameCert_{\ConstructiveCompactUniformFanUp}$ surface consists exactly of compact-metric source exposure through $K$, finite branch-ledger exposure through $B$, terminal-bar exposure through $T$, finite-window exposure through $W$, dyadic tolerance exposure through $D$, pointwise continuous-map exposure through $M$, uniform-modulus handoff through $U$, real sealing through $E$, componentwise transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:constructive-compact-uniform-fan-carrier}. The finite packet exposes exactly the compact source, branch ledger, terminal bar, windows, dyadic tolerances, continuous-map rows, uniform-modulus row, real seal, and structural rows.

Read the compact and fan rows first. The compact-metric row $K$ supplies the inspected finite-net source, while $B$ and $T$ record the branch prefixes and the terminal-bar stops used by the fan-uniform route of \autoref{ch:concrete-instances-fan-uniform-continuity-namecert}.

The analytic rows are downstream of that finite branch data. The window row $W$ and dyadic row $D$ supply the finite observations and tolerance budgets; the continuous-map row $M$ supplies pointwise continuity data at the displayed prefixes; the uniform-modulus row $U$ is exposed only after those rows have been read.

Finally $E,H,C,P,N$ seal, transport, replay, package, and name the same route. Since no additional coordinate exists, the listed rows exhaust the local NameCert surface and cannot export open-cover compactness, a selected branch, quotient function equality, global choice, or ambient Real completeness.
\end{proof}

\begin{theorem}[Constructive compact uniform fan finite-branch induction]
\label{thm:constructive-compact-uniform-fan-finite-branch-induction}
Let $F=(K,B,T,W,D,M,U,E,H,C,P,N)$ be an accepted $\ConstructiveCompactUniformFanUp$ carrier. Every fan-uniform read accepted by $F$ is exhausted by induction over the finite branch ledger $B$. At each branch prefix, the induction preserves the compact-metric source row $K$, the terminal-bar row $T$, the window row $W$, the dyadic tolerance row $D$, the continuous-map row $M$, the uniform-modulus handoff $U$, the real seal $E$, and the structural rows $H,C,P,N$.

Consequently a downstream $\UniformModulusUp$ consumer receives only the displayed finite route
$$
K,B,T,W,D,M,U,E,H,C,P,N
$$
and receives no branch selector, infinite fan object, host open-cover compactness theorem, quotient function equality, global choice coordinate, or ambient $\RealUp$ completeness witness.
\end{theorem}
\begin{proof}
Induct on the displayed finite branch ledger $B$.

For the empty ledger, no branch prefix is available to inspect. The packet can therefore expose only the compact source and structural rows already present in the carrier, and no dyadic tolerance, continuous-map read, or uniform-modulus handoff can be detached from the displayed route.

For a successor branch prefix, first read the terminal-bar row $T$ and the finite-window row $W$. These rows keep the successor prefix inside the same compact-metric source $K$ and prevent the consumer from replacing it by a selected external branch.

Next consume the dyadic tolerance row $D$ and the pointwise continuous-map row $M$ for that prefix. The row $D$ records the local diameter and tolerance budget, while $M$ supplies only the displayed continuity data attached to the prefix.

The induction hypothesis preserves every earlier prefix with the same rows $K,T,W,D,M,U,E,H,C,P,N$. Combining the earlier route with the successor prefix exhausts the finite branch ledger, and the handoff row $U$ reaches the $\RealUp$ seal $E$ without importing open-cover compactness, quotient function equality, global choice, or ambient real completeness.
\end{proof}

\falsifiablePrediction{A compact-uniform consumer that claims a $\ConstructiveCompactUniformFanUp$ read while omitting the compact-metric source row, finite branch ledger, terminal-bar row, finite-window row, dyadic tolerance row, continuous-map row, uniform-modulus handoff, Real seal, transport, replay, provenance, or local naming row refutes the carrier of \autoref{def:constructive-compact-uniform-fan-carrier}.}

\independenceWitness{fanuniformcontinuity, compactmetric, continuousmap, uniformmodulus, real: $\ConstructiveCompactUniformFanUp$ is not bijective to any listed sibling because it jointly carries compact-metric source data, finite branch prefixes, terminal-bar stops, finite windows, dyadic tolerance budgets, pointwise continuous-map rows, a uniform-modulus handoff, a Real seal, transport, replay, provenance, and local naming.}

\closureat{ConstructiveCompactUniformFanUp}{seedStr}

\begin{closurestatus}{\ConstructiveCompactUniformFanUp}
  \constructivestory{A $\ConstructiveCompactUniformFanUp$ packet is generated from a CompactMetric source row, a finite branch ledger, a terminal-bar row, StreamName finite windows, DyadicRatCore tolerance rows, ContinuousMap pointwise rows, a UniformModulus handoff, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:constructive-compact-uniform-fan-carrier}, \autoref{thm:constructive-compact-uniform-fan-namecert-obligations}, and \autoref{thm:constructive-compact-uniform-fan-finite-branch-induction} seed the L10 compact-uniform fan route over compact-metric, finite-branch, continuous-map, uniform-modulus, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close open-cover compactness, infinite fan objects, branch selectors, quotient function equality, global choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, terminal-bar exposure, finite-window coverage, dyadic tolerance exactness, pointwise continuous-map routing, uniform-modulus handoff, RealUp seal nonescape, and branch-selector refusal for BEDC.Derived.ConstructiveCompactUniformFanUp.}
\end{closurestatus}
