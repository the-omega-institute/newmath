\chapter{A Concrete Naming Certificate for $\NachbinOrderedTopologicalSpaceUp$}
\label{ch:concrete-instances-nachbin-ordered-topological-space-namecert}
\origin{human}

Following Nachbin and Kelley, the $\NachbinOrderedTopologicalSpaceUp$ packet records an ordered topological space by finite BEDC rows: a topology row, a partial-order row, observable-open classifiers, closed-order graph tests, directed quasi-uniform comparison data, structural transport, replay, provenance, and local naming. It sits after the topology carrier of \autoref{ch:concrete-instances-topology-namecert}, the partial-order surface of \autoref{ch:concrete-instances-poset-namecert}, the observable-open truth row of \autoref{ch:concrete-instances-sierpinski-space-namecert}, and the directed entourage surface of \autoref{ch:concrete-instances-quasi-uniform-space-namecert}. The packet names the traditional order-topology interface without importing host products, quotient order equality, selected open neighbourhoods, trichotomy, choice principles, or ambient completeness.

\begin{definition}[Nachbin ordered topological space carrier]\label{def:nachbin-ordered-topological-space-carrier}
A $\NachbinOrderedTopologicalSpaceUp$ carrier is a finite $\BHist$ packet
$$
K=(X,T,Q,S,G,U,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the shared carrier row, exposing only the classifier visible to the packet.
\item $T$ is the $\TopologyUp$ row on $X$, including the indexed open rows and finite neighbourhood operations used by topological consumers.
\item $Q$ is the $\PosetUp$ row on $X$, exposing the reflexive, transitive, and antisymmetric order surface as displayed comparison data.
\item $S$ is the $\SierpinskiSpaceUp$ observable-open row, reading membership in displayed opens as finite truth data rather than as a host proposition.
\item $G$ is the closed-order graph row: a displayed negative-separation test for order-incompatible pairs, expressed through finite topology and observable-open rows.
\item $U$ is the $\QuasiUniformSpaceUp$ comparison row, recording directed entourage evidence compatible with the order and the topology.
\item $B$ is the boundary ledger, refusing host product topologies, quotient order equality, selected neighbourhood systems, trichotomy, open-cover compactness, and ambient completeness.
\item $H$ records componentwise $\hsame$ transport for the carrier, topology, order, observable-open, closed-graph, directed-entourage, boundary, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an ordered-topological-space request through $X,T,Q,S,G,U,B,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\NachbinOrderedTopologicalSpaceUp$, $\TopologyUp$, $\PosetUp$, $\SierpinskiSpaceUp$, $\QuasiUniformSpaceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\NachbinOrderedTopologicalSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,T,Q,S,G,U,B,H,C,P,N$. It has no coordinate for a host ordered space, a product-space graph object, quotient equality of ordered points, selected open neighbourhoods, trichotomy, or a completion principle.
\end{definition}

\begin{theorem}[Nachbin ordered topological space NameCert obligations]\label{thm:nachbin-ordered-topological-space-namecert-obligations}
For every accepted $\NachbinOrderedTopologicalSpaceUp$ carrier $K=(X,T,Q,S,G,U,B,H,C,P,N)$, the local $\NameCert_{\NachbinOrderedTopologicalSpaceUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the shared carrier, topology, order, observable-open, closed-graph, directed-entourage, boundary, transport, replay, provenance, and local naming rows;
\item topology visibility, requiring every open or neighbourhood read to inspect the displayed $\TopologyUp$ row $T$;
\item order visibility, requiring every comparison read to inspect the displayed $\PosetUp$ row $Q$ before any order-topology claim is exported;
\item observable-open accountability, requiring membership in displayed opens to pass through the $\SierpinskiSpaceUp$ row $S$;
\item closed-order exactness, requiring the closed-order graph row $G$ to witness order-incompatible separation only through finite open and observable-open data;
\item directed comparison compatibility, requiring the $\QuasiUniformSpaceUp$ row $U$ to record only order-compatible directed entourage evidence downstream of $T,Q,S,G$;
\item boundary non-escape, structural transport, replay, provenance, and local naming through $B,H,C,P,N$.
\end{enumerate}
No $\NachbinOrderedTopologicalSpaceUp$ obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:nachbin-ordered-topological-space-carrier}. The accepted packet exposes exactly the shared carrier, topology, order, observable-open, closed-graph, directed-entourage, boundary, transport, replay, provenance, and naming rows.

Topology and order visibility determine the first two inspection routes. A neighbourhood or open-set read must inspect $T$, while a comparison read must inspect $Q$. The observable-open row $S$ converts membership in displayed opens into finite truth data before the closed-order graph can be consumed.

The closed-order row $G$ is therefore exact. It witnesses separation of order-incompatible pairs only by the displayed topology and observable-open rows, so it cannot create a host product graph or a selected neighbourhood system.

The directed comparison row $U$ records the quasi-uniform compatibility that remains downstream of $T,Q,S,G$. The boundary row $B$ refuses the excluded host readings, and $H,C,P,N$ transport, replay, source, and name the same packet. Since no excluded datum has a carrier coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Nachbin ordered topological space closed-order scope]\label{thm:nachbin-ordered-topological-space-closed-order-scope}
Every closed-order or directed-comparison read exported by an accepted $\NachbinOrderedTopologicalSpaceUp$ carrier factors through the finite route
$$
\TopologyUp
\longmapsto
\SierpinskiSpaceUp
\longmapsto
\PosetUp
\longmapsto
G
\longmapsto
\QuasiUniformSpaceUp
\longmapsto
B,H,C,P,N .
$$
The route records closed-order separation and order-compatible directed entourage data as finite NameCert evidence. It exports no host product topology, quotient order equality, selected neighbourhood base, trichotomy, choice-selected comparison, or ambient completion theorem.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:nachbin-ordered-topological-space-namecert-obligations}. It places topology reads in $T$, observable-open truth reads in $S$, and comparison reads in $Q$ before any closed-order or directed-entourage claim is exported.

The closed-order graph row $G$ can then be read only as a finite separation ledger over those displayed rows. It records which order-incompatible pairs have the required open evidence, but it does not identify the packet with a host product-space graph.

The directed row $U$ is consumed after the closed-order graph has exposed the order-topology interface. It records order-compatible quasi-uniform evidence rather than a symmetric uniformity, selected completion, or quotient comparison object.

Finally $B,H,C,P,N$ keep the exported read inside the same finite packet. The excluded host structures are refused by $B$ and have no coordinate in the carrier, so every exported read factors through the displayed route.
\end{proof}

\falsifiablePrediction{Every accepted $\NachbinOrderedTopologicalSpaceUp$ closed-order read factors through a displayed TopologyUp row, SierpinskiSpaceUp observable-open row, PosetUp order row, closed-order graph row, QuasiUniformSpaceUp directed-comparison row, boundary ledger, hsame transport, Cont replay, Pkg provenance, and local naming.}

\independenceWitness{topology, poset, sierpinski-space, quasi-uniform-space: $\NachbinOrderedTopologicalSpaceUp$ is not bijective to any listed sibling because it binds the shared carrier, topology row, partial-order row, observable-open truth row, closed-order graph row, directed quasi-uniform comparison row, boundary ledger, transport, replay, provenance, and local NameCert row in one packet.}

\closureat{NachbinOrderedTopologicalSpaceUp}{seedStr}

\begin{closurestatus}{\NachbinOrderedTopologicalSpaceUp}
  \constructivestory{A $\NachbinOrderedTopologicalSpaceUp$ packet is generated from a shared carrier row, a TopologyUp row, a PosetUp row, a SierpinskiSpaceUp observable-open row, a closed-order graph row, a QuasiUniformSpaceUp directed-comparison row, a boundary ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:nachbin-ordered-topological-space-carrier}, \autoref{thm:nachbin-ordered-topological-space-namecert-obligations}, and \autoref{thm:nachbin-ordered-topological-space-closed-order-scope} seed the finite closed-order and directed-comparison interface for ordered topological spaces.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host product topologies, quotient order equality, selected neighbourhood systems, trichotomy, choice principles, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, topology visibility, order visibility, observable-open accountability, closed-order exactness, directed comparison compatibility, boundary non-escape, transport, replay, provenance, and local naming for BEDC.Derived.NachbinOrderedTopologicalSpaceUp.}
\end{closurestatus}
