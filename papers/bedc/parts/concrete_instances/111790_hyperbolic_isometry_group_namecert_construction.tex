\chapter{A Concrete Naming Certificate for $\HyperbolicIsometryGroupUp$}
\label{ch:concrete-instances-hyperbolic-isometry-group-namecert}
\origin{ai}

The $\HyperbolicIsometryGroupUp$ packet realizes the group-facing route of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite disk-map, inverse, hyperbolic-metric preservation, boundary-action, transport, provenance, and NameCert rows. It sits after the disk-coordinate carrier of \autoref{ch:concrete-instances-unitdisk-namecert}, the metric readback of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the visual-boundary surface of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, the Mobius-transform packet of \autoref{ch:concrete-instances-mobius-transform-namecert}, and the finite evolution route of \autoref{ch:concrete-instances-dynsystem-namecert}. The packet names a simultaneous isometry-group surface; it is not a completed hyperbolic plane, quotient ideal-boundary equality, Lie-group manifold, Haar-measure object, or physical observer law.

\begin{definition}[Hyperbolic isometry group carrier]
\label{def:hyperbolic-isometry-group-carrier}
A $\HyperbolicIsometryGroupUp$ carrier is a finite $\BHist$ packet
$$
G=(D,M,F,I,B,A,Q,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\UnitDiskUp$ disk-coordinate row for finite source and target observations.
\item $M$ is the $\HyperbolicMetricUp$ metric-preservation row over those observations.
\item $F$ is the $\MobiusTransformUp$ disk-map row, restricted to maps whose displayed source and target rows remain in the disk carrier.
\item $I$ is the inverse row, recording the displayed inverse disk map and the finite two-sided replay checks.
\item $B$ is the $\HyperbolicVisualBoundaryUp$ boundary-action row, recording the induced action on displayed visual-boundary data.
\item $A$ is the group-law row, displaying identity, composition, and inverse closure for the finite map packet.
\item $Q$ is the $\DynSystemUp$ replay row, used when a finite orbit-style consumer reads repeated action.
\item $T$ records componentwise $\hsame$ transport for disk coordinates, metric preservation, map row, inverse row, boundary action, group law, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a finite disk-map request through inverse checking, metric preservation, boundary action, group-law inspection, and orbit-style replay.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicIsometryGroupUp$, $\UnitDiskUp$, $\HyperbolicMetricUp$, $\HyperbolicVisualBoundaryUp$, $\MobiusTransformUp$, $\DynSystemUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicIsometryGroupUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,M,F,I,B,A,Q,T,C,P,N$. The carrier has no coordinate for a completed hyperbolic plane, quotient ideal endpoints, continuous Lie-group topology, Haar measure, host analytic equality of maps, or physical observer dynamics.
\end{definition}

\begin{theorem}[Hyperbolic isometry group NameCert obligations]
\label{thm:hyperbolic-isometry-group-namecert-obligations}
For every accepted $\HyperbolicIsometryGroupUp$ carrier
$$
G=(D,M,F,I,B,A,Q,T,C,P,N),
$$
the local $\NameCert_{\HyperbolicIsometryGroupUp}$ surface consists exactly of disk-coordinate admission, disk-map admission, inverse closure, hyperbolic-metric preservation, visual-boundary action, identity and composition rows, finite orbit replay, componentwise transport, displayed continuation replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier through \autoref{def:hyperbolic-isometry-group-carrier}. The accepted tuple exposes exactly the disk, metric, map, inverse, boundary, group-law, replay, transport, provenance, and naming rows.

The isometry obligation is read from $M,F,I$. The map row displays the disk action, the inverse row supplies the finite two-sided checks, and the metric row records preservation of the hyperbolic readback on the displayed observations.

The group and boundary obligations are read from $A,B,Q$. Composition, identity, and inverse closure are finite rows in $A$, boundary action is confined to $B$, and repeated action is replayed only through the $\DynSystemUp$ row $Q$.

Transport and replay through $T,C,P,N$ preserve the same packet. Since the carrier stores no completed plane, quotient ideal endpoint, Lie topology, Haar measure, host analytic equality, or physical dynamics, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Hyperbolic isometry group boundary action]
\label{thm:hyperbolic-isometry-group-boundary-action}
For every accepted $\HyperbolicIsometryGroupUp$ carrier $G=(D,M,F,I,B,A,Q,T,C,P,N)$, every boundary-action consumer must read the disk-map row $F$, inverse row $I$, metric-preservation row $M$, and group-law row $A$ before the visual-boundary row $B$ can be exported as stable under the action.
\end{theorem}
\begin{proof}
Start with a boundary-action request. The request is attached to the displayed disk map $F$, so the disk-coordinate row $D$ and the inverse row $I$ are visible before boundary data can be consumed.

Metric preservation is the row $M$. It keeps the boundary read tied to the same hyperbolic metric surface rather than to an external visual compactification.

Finally read the group-law row $A$ and the boundary row $B$. The rows $T,C,P,N$ transport, replay, source, and name this finite route, while $Q$ handles any repeated-action read. Thus boundary stability is a packet fact over $D,M,F,I,B,A,Q,T,C,P,N$, not a quotient-boundary theorem or a completed hyperbolic-plane assertion.
\end{proof}

\falsifiablePrediction{If the metric-preservation row is removed, no current boundary-action consumer can recover both inverse closure and visual-boundary stability from the remaining packet rows.}

\independenceWitness{hyperbolic\_metric, hyperbolic\_visual\_boundary, mobius\_transform, dynsystem: the new carrier has a simultaneous group law, inverse, metric-preservation, and boundary-action obligation surface that no single sibling packet carries.}

The Lean follow-up will provide $\mathsf{Nontrivial}\ \HyperbolicIsometryGroupUp$ and $\mathsf{FieldFaithful}\ \HyperbolicIsometryGroupUp$ instances in \texttt{TasteGate.lean}.

\closureat{HyperbolicIsometryGroupUp}{seedStr}
\begin{closurestatus}{\HyperbolicIsometryGroupUp}
  \constructivestory{A $\HyperbolicIsometryGroupUp$ packet is generated from UnitDisk coordinates, HyperbolicMetric preservation, MobiusTransform disk maps, inverse checks, HyperbolicVisualBoundary action, group-law rows, DynSystem replay, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-isometry-group-carrier}, \autoref{thm:hyperbolic-isometry-group-namecert-obligations}, \autoref{thm:hyperbolic-isometry-group-boundary-action}, the falsifiable prediction, and the independence witness seed the finite hyperbolic isometry group route over disk-map, inverse, metric-preservation, boundary-action, replay, transport, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed hyperbolic geometry, quotient ideal-boundary equality, Lie-group topology, Haar measure, physical dynamics, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked disk-map admission, inverse closure, metric preservation, boundary action, group-law rows, replay, transport, provenance, and local naming for the same finite packet.}
\end{closurestatus}
