\chapter{A Concrete Naming Certificate for $\HyperbolicVisualShadowMetricUp$}
\label{ch:concrete-instances-hyperbolic-visual-shadow-metric-namecert}
\origin{ai}

The $\HyperbolicVisualShadowMetricUp$ packet realizes the boundary-shadow visual metric reading of \autoref{sec:hyperbolic-poincare-disk} as finite NameCert data. It consumes disk coordinates from \autoref{ch:concrete-instances-unitdisk-namecert}, the metric-read surface of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the boundary-shadow packet of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the visual-metric packet of \autoref{ch:concrete-instances-hyperbolic-visual-metric-namecert}, and the Busemann-boundary route of \autoref{ch:concrete-instances-hyperbolic-busemann-boundary-namecert}. Its carrier binds those rows into one finite visual-shadow metric packet rather than a completed disk model, quotient boundary, global field instance, or host hyperbolic geometry.

\begin{definition}[Hyperbolic visual-shadow metric carrier]
\label{def:hyperbolic-visual-shadow-metric-carrier}
A $\HyperbolicVisualShadowMetricUp$ carrier is a finite $\BHist$ packet
$$
V=(U,B,M,S,G,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ is the $\UnitDiskUp$ disk-carrier row for the inspected points and visible disk windows.
\item $B$ is the boundary-shadow row, recording the displayed boundary projection or shadow comparison consumed by the read.
\item $M$ is the $\HyperbolicMetricUp$ metric-read row on the displayed disk data.
\item $S$ is the $\HyperbolicVisualMetricUp$ visual-metric row, recording the finite visual comparison surface.
\item $G$ is the $\HyperbolicBusemannBoundaryUp$ boundary row used to keep the shadow comparison attached to a displayed Busemann endpoint.
\item $T$ is the phase-transport row, recording the displayed observer-expansion transport through which the visual-shadow read is replayed.
\item $H$ records componentwise $\hsame$ transport for disk, boundary-shadow, metric, visual, Busemann, phase-transport, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a visual-shadow metric request through $U,B,M,S,G,T$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicVisualShadowMetricUp$, $\UnitDiskUp$, $\HyperbolicMetricUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicVisualMetricUp$, $\HyperbolicBusemannBoundaryUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HyperbolicVisualShadowMetricUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $U,B,M,S,G,T,H,C,P,N$. It has no coordinate for a host Poincare disk, quotient ideal boundary, global geometry theorem, physical observer trajectory, or implicit field-faithfulness witness.
\end{definition}

\begin{theorem}[Hyperbolic visual-shadow metric NameCert obligations]
\label{thm:hyperbolic-visual-shadow-metric-namecert-obligations}
For every accepted $\HyperbolicVisualShadowMetricUp$ carrier
$$
V=(U,B,M,S,G,T,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicVisualShadowMetricUp}$ surface consists exactly of disk-carrier admission, boundary-shadow exposure, hyperbolic metric readback, visual-metric readback, Busemann-boundary attachment, phase-transport replay, componentwise transport, displayed continuation, package provenance, and local naming through $U,B,M,S,G,T,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-visual-shadow-metric-carrier}. The rows $U,B,M,S,G,T,H,C,P,N$ are the whole visual-shadow metric packet.

The disk and boundary data are read first. The disk carrier $U$ supplies the visible points, the boundary-shadow row $B$ supplies the shadow comparison, and the metric row $M$ supplies the hyperbolic readback attached to those points.

The visual and Busemann rows then bind the comparison to a displayed boundary-facing route. The phase-transport row $T$ records how the observer-expansion reading is replayed without importing a completed geometry.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Therefore the listed rows exhaust the NameCert surface and leave host disk models, quotient boundaries, global geometry, physical trajectories, and implicit field-faithfulness outside the packet.
\end{proof}

\begin{theorem}[Hyperbolic visual-shadow metric factorization]
\label{thm:hyperbolic-visual-shadow-metric-factorization}
Every accepted visual-shadow metric read factors through the disk carrier, boundary-shadow row, $\HyperbolicMetricUp$ row, $\HyperbolicVisualMetricUp$ row, $\HyperbolicBusemannBoundaryUp$ row, phase-transport row, displayed replay, package provenance, and local $\NameCert$ row before use.
\end{theorem}
\begin{proof}
Begin with an accepted carrier $V=(U,B,M,S,G,T,H,C,P,N)$. A read first lands on the disk-carrier row $U$, then consumes the boundary-shadow row $B$ and the hyperbolic metric row $M$.

The visual comparison row $S$ and Busemann boundary row $G$ attach that metric read to a displayed boundary-facing visual surface. The phase-transport row $T$ records the route along which the observer-expansion packet replays the comparison.

Transport, continuation, provenance, and naming are then supplied by $H,C,P,N$. Since no further coordinate is present, every accepted read factors through the displayed rows and cannot bypass them through host geometry, quotient boundary data, or an implicit global field instance.
\end{proof}

\falsifiablePrediction{Every accepted visual-shadow metric read factors through the disk carrier, boundary-shadow row, $\HyperbolicMetricUp$ row, $\HyperbolicVisualMetricUp$ row, $\HyperbolicBusemannBoundaryUp$ row, phase-transport row, displayed replay, provenance, and local $\NameCert$ row.}

\independenceWitness{unitdisk, hyperbolicmetric, hyperbolic-boundary-shadow, hyperbolic-visual-metric, hyperbolic-busemann-boundary: no listed sibling is bijective to $\HyperbolicVisualShadowMetricUp$ because this packet jointly carries disk windows, boundary shadows, metric readback, visual comparison, Busemann boundary attachment, phase transport, replay, provenance, and local naming.}

The future \texttt{lean4/BEDC/Derived/HyperbolicVisualShadowMetricUp/TasteGate.lean} file supplies explicit $\Nontrivial\ \HyperbolicVisualShadowMetricUp$ and $\FieldFaithful\ \HyperbolicVisualShadowMetricUp$ instances. Those instances must preserve $U,B,M,S,G,T,H,C,P,N$ as distinct fields before a checked carrier-alignment theorem can serve as a TasteGate witness.

\closureat{HyperbolicVisualShadowMetricUp}{seedStr}

\begin{closurestatus}{\HyperbolicVisualShadowMetricUp}
  \constructivestory{A $\HyperbolicVisualShadowMetricUp$ packet is generated from UnitDisk carrier rows, boundary-shadow rows, HyperbolicMetric readback, HyperbolicVisualMetric comparison, HyperbolicBusemannBoundary attachment, phase transport, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-visual-shadow-metric-carrier}, \autoref{thm:hyperbolic-visual-shadow-metric-namecert-obligations}, and \autoref{thm:hyperbolic-visual-shadow-metric-factorization} seed the finite boundary-shadow visual metric route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim completed Poincare disk geometry, quotient ideal boundary equality, global field instances, physical observer trajectories, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, disk readback, boundary-shadow exposure, metric readback, visual comparison, Busemann boundary attachment, phase transport, TasteGate nontriviality, field faithfulness, and local naming for BEDC.Derived.HyperbolicVisualShadowMetricUp.}
\end{closurestatus}
