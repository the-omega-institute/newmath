\chapter{A Concrete Naming Certificate for $\GoldenWeilGramContainerUp$}
\label{ch:concrete-instances-golden-weil-gram-container-namecert}
\origin{ai}

The $\GoldenWeilGramContainerUp$ packet realizes the finite side of \autoref{ch:visions-rh-golden-weil-gram-induction} as a bounded NameCert surface. It also consumes the finite reflected zero-ledger and Schur-square route of \autoref{ch:visions-rh-diagonal-schur-square-target} together with the finite pentagon Gram packet of \autoref{ch:concrete-instances-window6-golden-mean-cyclotomic-pentagon} only as sibling finite-window evidence, not as a global positivity theorem. It packages the golden test ruler, a finite Gram matrix, conditional negative or positive semidefinite test rows, Schur residual bookkeeping, and refusal rows as finite data. The packet is a finite container for a Gram test window; it is not a proof of RH, global Weil positivity, analytic continuation, selected zero ledgers, or a completed infinite test space.

\begin{definition}[Golden Weil Gram-container carrier]
\label{def:golden-weil-gram-container-carrier}
A $\GoldenWeilGramContainerUp$ carrier is a finite $\BHist$ packet
$$
G=(R,A,M,Q,S,D,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the finite golden ruler row, recording the selected Zeckendorf-ordered test rows used in the window.
\item $A$ is the finite test-space row $\mathcal A_n$ generated by those displayed test rows.
\item $M$ is the finite Gram matrix row whose entries are the displayed Weil-pairing reads inside the window.
\item $Q$ is the finite conditional-negative or positive-semidefinite test row for $M$, expressed only as a matrix predicate on the displayed coefficients.
\item $S$ is the Schur residual row, including range and residual-energy entries for the next displayed test row when such a row is present.
\item $D$ is the diagonal witness or refusal row that records a finite negative direction when the test fails and records absence only for the displayed window when it does not.
\item $B$ is the boundary row refusing RH, global Weil positivity, analytic continuation, selected zero ledgers, infinite density, and any completed infinite test-space claim.
\item $H$ records componentwise $\hsame$ transport for ruler, test-space, Gram, semidefinite, Schur, refusal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from finite coefficient reads through the Gram and Schur rows to the local decision row.
\item $P$ records $\Pkg$ provenance over \autoref{ch:visions-rh-golden-weil-gram-induction}, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\GoldenWeilGramContainerUp}$ row.
\end{enumerate}
The carrier has no coordinate for an infinite Hilbert completion, analytic continuation, selected zero inventory, global Weil positivity theorem, RH theorem, countable choice, or an external positivity oracle.
\end{definition}

\begin{theorem}[Golden Weil Gram-container NameCert obligations]
\label{thm:golden-weil-gram-container-namecert-obligations}
For every accepted $\GoldenWeilGramContainerUp$ carrier
$$
G=(R,A,M,Q,S,D,B,H,C,P,N),
$$
the local $\NameCert_{\GoldenWeilGramContainerUp}$ surface consists exactly of finite ruler admission, finite test-space admission, finite Gram-matrix exposure, conditional-negative or positive-semidefinite matrix testing, Schur residual bookkeeping, diagonal witness or refusal recording, boundary refusal, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:golden-weil-gram-container-carrier}. The rows $R,A,M,Q,S,D,B,H,C,P,N$ are the full finite container.

The ruler and test-space rows determine the coefficient window. The Gram row then exposes only finitely many Weil-pairing entries, and the matrix test row $Q$ reads only finite coefficient vectors for that window.

The Schur row $S$ and diagonal row $D$ record the finite outcome: a residual ledger, a finite negative direction, or a refusal to infer more than the displayed test has established. Boundary row $B$ keeps global positivity and RH-facing requests outside the container.

Transport, replay, provenance, and local naming repack those rows without adding analytic continuation, an infinite test-space completion, a zero ledger, or a global theorem. Hence the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Golden Weil Gram-container finite semidefinite route]
\label{thm:golden-weil-gram-container-cnd-route}
An accepted $\GoldenWeilGramContainerUp$ carrier supports only the finite route from displayed golden-ruler rows to the finite Gram matrix $M$, then to the conditional-negative or positive-semidefinite test row $Q$, and then to the Schur residual and diagonal witness rows $S,D$. A successful read certifies the displayed finite window only; a failing read exposes a finite witness row only. Neither read claims RH, global Weil positivity, analytic continuation, selected zero ledgers, or a completed infinite test space.
\end{theorem}
\begin{proof}
Use the NameCert obligations. They force every coefficient read to pass through the finite ruler row $R$, the finite test-space row $A$, and the finite Gram row $M$ before the semidefinite row $Q$ is inspected.

If $Q$ succeeds, the result is a finite matrix statement for the displayed window and may be replayed through $S,H,C,P,N$. If $Q$ fails, the diagonal row $D$ records the finite negative direction and does not create a global counterclaim.

The boundary row $B$ handles every request for RH, global Weil positivity, analytic continuation, selected zero data, or an infinite test-space completion. Those requests are not coordinates of the carrier, so the finite semidefinite route remains local to the displayed Gram container.
\end{proof}

\falsifiablePrediction{Every $\GoldenWeilGramContainerUp$ read either exposes a finite golden-ruler Gram window with a displayed semidefinite test row, or exposes a finite diagonal witness row; no accepted read yields RH, global Weil positivity, analytic continuation, selected zero ledgers, or a completed infinite test space.}

\independenceWitness{fixed-half-zero-witness, contourintegraloperation, dyadic-truncation-lp-refutation-window, vision-ledger-route: $\GoldenWeilGramContainerUp$ is not bijective to any listed sibling because it carries finite golden-ruler rows, finite Gram-matrix rows, Schur residual rows, semidefinite test rows, boundary refusal, transport, replay, provenance, and local naming as one packet.}

\closureat{GoldenWeilGramContainerUp}{seedStr}

\begin{closurestatus}{\GoldenWeilGramContainerUp}
  \constructivestory{A $\GoldenWeilGramContainerUp$ packet is generated from finite golden-ruler rows, finite test-space rows, finite Gram-matrix rows, conditional-negative or positive-semidefinite matrix-test rows, Schur residual rows, diagonal witness or refusal rows, boundary refusal rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:golden-weil-gram-container-carrier}, \autoref{thm:golden-weil-gram-container-namecert-obligations}, and \autoref{thm:golden-weil-gram-container-cnd-route} seed the finite Gram-container route for the golden Weil vision.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close RH, global Weil positivity, analytic continuation, selected zero ledgers, a completed infinite test space, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked finite ruler admission, finite Gram-matrix exposure, semidefinite test routing, Schur residual bookkeeping, diagonal witness or refusal exactness, boundary refusal, transport, replay, provenance, and local naming for BEDC.Derived.GoldenWeilGramContainerUp.}
\end{closurestatus}
