\chapter{A Concrete Naming Certificate for $\LocatedRealFastCauchyUp$}
\label{ch:concrete-instances-located-real-fast-cauchy-namecert}
\origin{human}

Following Bishop and Bridges, the $\LocatedRealFastCauchyUp$ packet records the finite bridge by which located-real interval data is read as a fast Cauchy and regular-rational real name before any completion-facing consumer receives it. It sits after the Bishop located-real surface of \autoref{ch:concrete-instances-bishop-located-real-namecert}, the fast sequence surface of \autoref{ch:concrete-instances-fast-cauchy-sequence-namecert}, the fast real-name carrier of \autoref{ch:concrete-instances-fast-cauchy-real-namecert}, and the two-way comparison route of \autoref{ch:concrete-instances-fast-regular-cauchy-equivalence-namecert}. The packet is a finite L10 bridge from located intervals to fast and regular Cauchy readback; it is not quotient real equality, a selected representative, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[LocatedRealFastCauchy carrier]
\label{def:located-real-fast-cauchy-carrier}
A $\LocatedRealFastCauchyUp$ carrier is a finite $\BHist$ packet
$$
K=(L,I,M,R,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the located-real interval data row, carrying the finite interval enclosures, strict-side evidence, and located-cut requests inherited from the $\BishopLocatedRealUp$ surface.
\item $I$ is the interval-refinement witness row, recording the finite refinement steps by which later intervals remain compatible with the earlier located enclosure.
\item $M$ is the fast-Cauchy modulus row, displaying the threshold schedule that sends each requested tolerance to a finite sequence window.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the same interval refinements and fast-Cauchy windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, recording dyadic radii, comparison slack, midpoint arithmetic, and endpoint budgets used by $L,I,M,R$.
\item $E$ is the terminal $\RealUp$ seal handoff reached only after $L,I,M,R,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for located intervals, refinements, modulus rows, readback rows, dyadic tolerances, Real seal handoff, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay for every located-interval request, fast threshold request, regular readback request, and Real seal request through the same finite route.
\item $P$ is the $\Pkg$ provenance over $\LocatedRealFastCauchyUp$, $\BishopLocatedRealUp$, $\FastCauchySequenceUp$, $\FastCauchyRealUp$, $\FastRegularCauchyEquivalenceUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedRealFastCauchyUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,I,M,R,D,E,H,C,P,N$. It has no coordinate for quotient stream equality, selected limits, countable-choice schedules, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[LocatedRealFastCauchy NameCert obligations]
\label{thm:located-real-fast-cauchy-namecert-obligations}
For every accepted $\LocatedRealFastCauchyUp$ carrier $K=(L,I,M,R,D,E,H,C,P,N)$, the local $\NameCert_{\LocatedRealFastCauchyUp}$ surface consists exactly of carrier admission, located-interval exposure, interval-refinement exposure, fast-Cauchy modulus exposure, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance accounting, $\RealUp$ seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming for the displayed rows $L,I,M,R,D,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the accepted packet. The analytic rows are precisely $L,I,M,R,D,E$: located intervals first, refinement evidence next, the fast modulus route next, regular rational readback next, dyadic tolerance accounting alongside those reads, and the terminal Real seal last.

Read the sibling obligations through provenance. The row $L$ is the located-real interface of \autoref{ch:concrete-instances-bishop-located-real-namecert}; the row $M$ is the fast sequence and fast real-name schedule visible through \autoref{ch:concrete-instances-fast-cauchy-sequence-namecert} and \autoref{ch:concrete-instances-fast-cauchy-real-namecert}; and the row $R$ is the regular readback disciplined by \autoref{ch:concrete-instances-fast-regular-cauchy-equivalence-namecert}.

Transport, replay, provenance, and local naming do not add analytic content. The rows $H,C,P,N$ preserve and name the same finite route, so quotient streams, selected limits, countable-choice schedules, trichotomy, host equality, and ambient completeness remain outside the local certificate.
\end{proof}

\begin{theorem}[LocatedRealFastCauchy regular readback]
\label{thm:located-real-fast-cauchy-regular-readback}
Every accepted $\LocatedRealFastCauchyUp$ carrier $K=(L,I,M,R,D,E,H,C,P,N)$ supplies a regular readback route
$$
\BishopLocatedRealUp,\ \DyadicRatCoreUp
\longmapsto
\FastCauchySequenceUp
\longmapsto
\RegSeqRatUp
\longmapsto
\RealUp .
$$
The route reads the located interval row $L$, interval refinements $I$, fast threshold row $M$, dyadic tolerance row $D$, regular readback row $R$, and terminal seal $E$ before any Real-facing or completion-facing consumer may inspect the packet. It supplies no detached uniform limit, quotient stream, selected representative, countable-choice schedule, trichotomy decision, host equality, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Begin with the located row $L$ and refinement row $I$. These rows display the finite interval enclosures and the refinement witnesses that keep later precision requests attached to the same located-real surface.

Use the fast row $M$ and the tolerance row $D$. A requested precision is routed to a finite sequence window and dyadic comparison budget before any regular rational readback can be consumed.

Read $R$ and then $E$. The $\RegSeqRatUp$ row is downstream of the same located, refinement, fast, and dyadic rows, and the $\RealUp$ seal is terminal with respect to that route. Componentwise transport and replay preserve these rows, while provenance and local naming bind the packet without adding any detached limit, quotient stream, selected representative, choice schedule, trichotomy decision, host equality, or ambient completeness coordinate.
\end{proof}

\closureat{LocatedRealFastCauchyUp}{seedStr}

\begin{closurestatus}{\LocatedRealFastCauchyUp}
  \constructivestory{A $\LocatedRealFastCauchyUp$ packet is generated from located-real interval data, interval-refinement witnesses, fast-Cauchy modulus rows, RegSeqRat readback, DyadicRatCore tolerance rows, a RealUp seal handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-real-fast-cauchy-carrier}, \autoref{thm:located-real-fast-cauchy-namecert-obligations}, and \autoref{thm:located-real-fast-cauchy-regular-readback} seed the located-real to fast-Cauchy and regular-readback bridge over finite L10 rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, selected representatives, countable choice, trichotomy, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located-interval exposure, interval-refinement exactness, fast-modulus exposure, RegSeqRat readback, DyadicRatCore tolerance accounting, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.LocatedRealFastCauchyUp.}
\end{closurestatus}
