\chapter{A Concrete Naming Certificate for $\BishopRegularCompletionObserverUp$}
\label{ch:concrete-instances-bishop-regular-completion-observer-namecert}
\origin{human}

Following Bishop and Bridges on constructive real completion, the $\BishopRegularCompletionObserverUp$ packet records the finite observer surface by which a regular Cauchy rational name is read through a displayed modulus, dyadic tolerance, sequence-space row, and terminal Real seal. It sits over the finite window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the sequence-space route of \autoref{ch:concrete-instances-regular-sequence-space-namecert}, and the value-facing boundary of \autoref{ch:concrete-instances-real-namecert}. The observer names only displayed regular-completion rows; it does not add quotient stream equality, selected representatives, countable choice, trichotomy, host completion, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop regular completion observer carrier]
\label{def:bishop-regular-completion-observer-carrier}
A $\BishopRegularCompletionObserverUp$ carrier is a finite $\BHist$ packet
$$
O=(S,R,D,M,W,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ observation-window row, listing the finite windows opened by the observer.
\item $R$ is the $\RegSeqRatUp$ readback row served from those windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row naming the requested precision scale.
\item $M$ is the regular-modulus row, recording the displayed window bound that witnesses the regular Cauchy condition at tolerance $D$.
\item $W$ is the $\RegularSequenceSpaceUp$ membership row tying the observed readback to the sequence-space carrier.
\item $E$ is the terminal $\RealUp$ seal reached only after $S,R,D,M,W$ remain visible together.
\item $H$ records componentwise $\hsame$ transport for observation windows, regular readback, dyadic tolerance, modulus, sequence-space membership, Real seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from an observer request through the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\BishopRegularCompletionObserverUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RegularSequenceSpaceUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopRegularCompletionObserverUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient stream equality, selected representative streams, countable choice, hidden tail selection, trichotomy, host completion equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop regular completion observer NameCert obligations]
\label{thm:bishop-regular-completion-observer-namecert-obligations}
For every accepted $\BishopRegularCompletionObserverUp$ carrier
$$
O=(S,R,D,M,W,E,H,C,P,N),
$$
the local $\NameCert_{\BishopRegularCompletionObserverUp}$ surface consists exactly of carrier admission for the displayed packet, finite $\StreamNameUp$ observation windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance, regular-modulus evidence, $\RegularSequenceSpaceUp$ membership, terminal $\RealUp$ sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No obligation row may replace this observer surface by quotient stream equality, selected representatives, countable choice, trichotomy, host completion, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-regular-completion-observer-carrier}. The rows $S,R,D,M,W,E$ are the observer-facing data, while $H,C,P,N$ preserve transport, replay, provenance, and local naming.

The finite observation row $S$ opens the windows that make $R$ visible as regular rational readback. The dyadic row $D$ fixes the tolerance request, and the modulus row $M$ records the displayed regular Cauchy bound for that request.

The sequence-space row $W$ and terminal seal $E$ keep the readback route attached to $\RegularSequenceSpaceUp$ before any Real-facing consumer reads it. The structural rows $H,C,P,N$ can only transport, replay, source, and name this same packet.

Since no carrier coordinate stores quotient equality, selected representatives, countable choice, hidden tail selection, trichotomy, host completion equality, or ambient completeness, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Bishop regular completion observer regularity window]
\label{thm:bishop-regular-completion-observer-regularity-window}
Every Real-facing consumer of an accepted $\BishopRegularCompletionObserverUp$ carrier must expose the observation row $S$, regular rational readback $R$, dyadic tolerance row $D$, regular-modulus row $M$, sequence-space row $W$, and Real seal $E$ before reading the value-facing boundary. Thus the observer's completion read is a finite regularity-window route through StreamName, RegSeqRat, DyadicRatCore, RegularSequenceSpace, and Real rows, not a quotient or host-completion principle.
\end{theorem}
\begin{proof}
Start with an accepted observer packet. The Real-facing boundary is $E$, but $E$ is downstream of the finite rows $S,R,D,M,W$ in the carrier.

The window row $S$ and regular readback row $R$ display the inspected rational name. The dyadic tolerance row $D$ and modulus row $M$ display the regularity budget at the requested precision scale.

The sequence-space row $W$ attaches this budgeted readback to $\RegularSequenceSpaceUp$ before the terminal $\RealUp$ seal is consumed. Transport, replay, provenance, and local naming preserve that same finite route through $H,C,P,N$.

Therefore any consumer that skips $S,R,D,M,W$ is outside the accepted carrier. The observer exports a regularity-window certificate and no quotient equality, representative selector, choice schedule, hidden tail, trichotomy, host completion, or ambient completeness.
\end{proof}

\falsifiablePrediction{Any accepted $\BishopRegularCompletionObserverUp$ Real-facing read that reaches the terminal seal without displaying a StreamName window, RegSeqRat readback, DyadicRatCore tolerance, regular-modulus row, and RegularSequenceSpace membership refutes the carrier.}

\independenceWitness{streamname, regseqrat, dyadicratcore, regularsequencespace, real: $\BishopRegularCompletionObserverUp$ is not bijective to any listed sibling because it jointly carries observation windows, regular rational readback, dyadic tolerance, modulus evidence, sequence-space membership, Real sealing, transport, replay, provenance, and local naming as one observer packet.}

\closureat{BishopRegularCompletionObserverUp}{seedStr}

\begin{closurestatus}{\BishopRegularCompletionObserverUp}
  \constructivestory{A $\BishopRegularCompletionObserverUp$ packet is generated from finite StreamName observation windows, RegSeqRat readback, DyadicRatCore tolerance, regular-modulus evidence, RegularSequenceSpace membership, Real sealing, hsame transport, Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-regular-completion-observer-carrier}, \autoref{thm:bishop-regular-completion-observer-namecert-obligations}, \autoref{thm:bishop-regular-completion-observer-regularity-window}, the falsifiable prediction, and the independence witness seed the finite regular-completion observer surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, quotient stream equality, selected representatives, countable choice, hidden tail choice, trichotomy, host completion equality, ambient $\RealUp$ completeness, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, observation-window exactness, regular rational readback, dyadic tolerance stability, modulus determinacy, sequence-space membership, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BishopRegularCompletionObserverUp.}
\end{closurestatus}
