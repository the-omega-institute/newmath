\chapter{A Concrete Naming Certificate for $\BishopRegularCauchyFilterUp$}
\label{ch:concrete-instances-bishop-regular-cauchy-filter-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\BishopRegularCauchyFilterUp$ packet records the filter presentation of regular Cauchy completion as finite BEDC data. It sits between the Cauchy-filter basis route of \autoref{ch:concrete-instances-cauchy-filter-basis-namecert}, the sequence-indexed filter route of \autoref{ch:concrete-instances-sequential-cauchy-filter-namecert}, finite stream windows from \autoref{ch:concrete-instances-streamname-namecert}, regular rational readback from \autoref{ch:concrete-instances-regseqrat-namecert}, dyadic tolerance ledgers from \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the L10 filter route for completion without quotienting filters, selecting limits, importing countable choice, using trichotomy, or appealing to ambient $\RealUp$ completeness.

\begin{definition}[Bishop regular Cauchy filter carrier]
\label{def:bishop-regular-cauchy-filter-carrier}
A $\BishopRegularCauchyFilterUp$ carrier is a finite $\BHist$ packet
$$
F=(B,S,W,R,D,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\CauchyFilterBasisUp$ row generated by regular tail requests and common-refinement witnesses.
\item $S$ is the $\SequentialCauchyFilterUp$ row tying the basis to a sequence-indexed Cauchy-filter read.
\item $W$ is the $\StreamNameUp$ finite-window row used by the inspected tail requests.
\item $R$ is the $\RegSeqRatUp$ regular rational readback row exposed from those windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger attached to the same window and readback route.
\item $Q$ is the Cauchy-filter saturation row, recording that every completion-facing filter request factors through the displayed basis route.
\item $E$ is the terminal $\RealUp$ seal row, available only after $B,S,W,R,D,Q$ have been displayed.
\item $H$ is the componentwise $\hsame$ transport row for the basis, sequential filter, windows, readback, dyadic ledger, saturation row, real seal, replay, provenance, and local naming.
\item $C$ is the displayed $\Cont$ replay row for the same finite filter route.
\item $P$ is the $\Pkg$ provenance over $\BishopRegularCauchyFilterUp$, $\CauchyFilterBasisUp$, $\SequentialCauchyFilterUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopRegularCauchyFilterUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,S,W,R,D,Q,E,H,C,P,N$. It has no coordinate for quotient filters, selected limit points, host equality of filters, trichotomy, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop regular Cauchy filter NameCert obligations]
\label{thm:bishop-regular-cauchy-filter-namecert-obligations}
For every accepted $\BishopRegularCauchyFilterUp$ carrier $F=(B,S,W,R,D,Q,E,H,C,P,N)$, the local $\NameCert_{\BishopRegularCauchyFilterUp}$ surface consists exactly of carrier admission for the displayed eleven-row packet; Cauchy-filter basis generation through $B$; sequence-indexed filter routing through $S$; finite stream-window exposure through $W$; regular rational readback through $R$; dyadic tolerance accounting through $D$; filter saturation through $Q$; terminal real-seal boundary through $E$; componentwise transport through $H$; displayed replay through $C$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Carrier admission is tuple projection from \autoref{def:bishop-regular-cauchy-filter-carrier}. The displayed filter data are exactly $B,S,Q$, the completion-facing readback data are $W,R,D,E$, and the structural certificate rows are $H,C,P,N$.

The basis and sequence rows are read first. The row $B$ records the finite common-refinement surface for regular Cauchy tails, while $S$ keeps that basis tied to the sequence-indexed Cauchy-filter route before saturation is consumed.

The completion-facing rows are downstream. Rows $W,R,D$ expose finite observations, regular rational readback, and dyadic tolerance accounting before the real seal $E$ is available, and $Q$ records that the filter request remains attached to the displayed basis.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since no remaining coordinate stores quotient filters, selected limits, host filter equality, trichotomy, countable choice, or ambient completeness, the listed rows exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Bishop regular Cauchy filter Real seal non-escape]
\label{thm:bishop-regular-cauchy-filter-real-seal-nonescape}
Every $\RealUp$ consumer of an accepted $\BishopRegularCauchyFilterUp$ carrier reaches the terminal seal only through
$$
\CauchyFilterBasisUp \longmapsto \SequentialCauchyFilterUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \RealUp
$$
together with the saturation row $Q$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\BishopRegularCauchyFilterUp}$ row. The packet exports no quotient filter, selected limit, host filter equality, trichotomy, countable-choice schedule, or ambient $\RealUp$ completeness witness.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:bishop-regular-cauchy-filter-namecert-obligations}. It forces every accepted read to expose the finite Cauchy-filter basis, the sequence-indexed route, the stream windows, the regular rational readback, and the dyadic tolerance ledger before the terminal seal appears.

The saturation row $Q$ keeps the filter request attached to the displayed basis route. A completion-facing consumer therefore receives the Cauchy-filter presentation as finite refinement data, not as a quotient filter or selected limit object.

Transport, replay, provenance, and local naming preserve only that route. Because the carrier has no coordinate for quotient filters, selected limits, host filter equality, trichotomy, countable choice, or ambient completeness, none of those endpoints can be exported by a $\RealUp$ seal consumer.
\end{proof}

\falsifiablePrediction{A $\BishopRegularCauchyFilterUp$ completion read that exports a terminal $\RealUp$ seal without displaying CauchyFilterBasis, SequentialCauchyFilter, StreamName, RegSeqRat, DyadicRatCore, saturation, transport, replay, provenance, and local NameCert rows refutes the carrier of \autoref{def:bishop-regular-cauchy-filter-carrier}.}

\independenceWitness{cauchy-filter-basis, sequential-cauchy-filter, streamname, regseqrat, dyadicratcore, real: no listed sibling carries the whole Bishop regular Cauchy filter packet because each supplies only a basis generator, sequence-indexed filter route, finite window row, regular readback, dyadic tolerance ledger, or terminal seal while this chapter binds them into one L10 completion-facing filter certificate.}

\closureat{BishopRegularCauchyFilterUp}{seedStr}

\begin{closurestatus}{\BishopRegularCauchyFilterUp}
  \constructivestory{A $\BishopRegularCauchyFilterUp$ packet is generated from CauchyFilterBasis rows, SequentialCauchyFilter routing, StreamName finite windows, RegSeqRat readback, DyadicRatCore tolerance rows, a Cauchy-filter saturation row, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-regular-cauchy-filter-carrier}, \autoref{thm:bishop-regular-cauchy-filter-namecert-obligations}, and \autoref{thm:bishop-regular-cauchy-filter-real-seal-nonescape} seed the finite L10 filter presentation of regular Cauchy completion.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient filters, selected limits, host filter equality, trichotomy, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Cauchy-filter basis generation, sequence-indexed filter routing, finite window exposure, regular rational readback, dyadic tolerance exactness, saturation, Real seal non-escape, transport, replay, provenance, and local naming for BEDC.Derived.BishopRegularCauchyFilterUp.}
\end{closurestatus}
