\chapter{A Concrete Naming Certificate for $\ZeroSignatureFaithfulnessUp$}
\label{ch:concrete-instances-zero-signature-faithfulness-namecert}
\origin{ai}

The $\ZeroSignatureFaithfulnessUp$ packet realizes the mirror-slot faithfulness branch of \autoref{sec:unified-reality-zero-signature-faithfulness} as finite NameCert data. It sits over the critical-strip source of \autoref{ch:concrete-instances-critical-strip-namecert}, the zero-source rows of \autoref{ch:concrete-instances-zeta-zeros-namecert}, the critical-line witness boundary of \autoref{ch:concrete-instances-criticallinewitness-namecert}, the complex-coordinate carrier of \autoref{ch:concrete-instances-complex-namecert}, and the real-coordinate seal of \autoref{ch:concrete-instances-real-namecert}. The packet records a classifier that reads only displayed zero signatures and mirror slots; it does not assert RH, analytic continuation completeness, zero simplicity, Euler product closure, or an unledgered zero observable.

\begin{definition}[Zero signature faithfulness carrier]
\label{def:zero-signature-faithfulness-carrier}
A $\ZeroSignatureFaithfulnessUp$ carrier is a finite $\BHist$ packet
$$
Z=(Z,J,\Sigma,F,H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $Z$ is the accepted zero-source row, read through $\ZetaZerosUp$ inside the $\CritStripUp$ boundary.
\item $J$ is the mirror-slot row, recording the functional-equation companion slot visible to the zero source.
\item $\Sigma$ is the displayed zero signature row, consisting of zero atom type, spectral time, and mirror slot as in \autoref{def:unified-reality-zero-ontic-signature}.
\item $F$ is the packet classifier row: two accepted zero-source rows with equal displayed $\Sigma$ rows are assigned to the same mirror slot.
\item $H$ records componentwise $\hsame$ transport for zero source, mirror slot, signature, classifier, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay through zero-source admission, mirror-slot readback, signature readback, classifier comparison, transport, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\ZeroSignatureFaithfulnessUp$, $\CritStripUp$, $\ZetaZerosUp$, $\CriticalLineWitnessUp$, $\ComplexUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\ZeroSignatureFaithfulnessUp}$ ledger row.
\end{enumerate}
The carrier contains no coordinate for an unledgered fourth zero observable, an RH proof object, a hidden analytic continuation theorem, zero simplicity, Euler product closure, or host-level equality of complex zeros.
\end{definition}

\begin{theorem}[Zero signature faithfulness NameCert obligations]
\label{thm:zero-signature-faithfulness-namecert-obligations}
For every accepted $\ZeroSignatureFaithfulnessUp$ carrier
$$
Q=(Z,J,\Sigma,F,H,C,P,L),
$$
the local $\NameCert_{\ZeroSignatureFaithfulnessUp}$ surface consists exactly of the following rows:
\begin{enumerate}
\item zero-source admission through the $\CritStripUp$ and $\ZetaZerosUp$ sibling rows;
\item mirror-slot readback through $J$ and displayed signature readback through $\Sigma$;
\item packet classifier faithfulness through $F$;
\item componentwise $\hsame$ transport and displayed $\Cont$ replay through $H$ and $C$;
\item $\Pkg$ provenance and local NameCert ledgering through $P$ and $L$.
\end{enumerate}
These rows expose the mirror-slot faithfulness packet only as finite NameCert data. They do not supply RH, analytic continuation completeness, zero simplicity, Euler product closure, hidden zero observables, or host-level equality of complex zeros.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:zero-signature-faithfulness-carrier}. The rows $Z,J,\Sigma,F$ are the zero-source, mirror-slot, signature, and classifier data; the rows $H,C,P,L$ are the structural data.

The zero-source row is bounded by the concrete sibling chapters. It is read through the critical-strip and zeta-zero surfaces before the mirror slot or signature can be inspected.

The classifier row is the faithfulness surface. It compares displayed zero signatures and returns equality of mirror slots inside the packet classifier, without adding a fourth zero observable.

Finally $H,C,P,L$ transport, replay, source, and name the same finite packet. Since no coordinate stores RH, analytic continuation completeness, zero simplicity, Euler product closure, hidden observables, or host-level equality of complex zeros, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Zero signature faithfulness mirror-slot rigidity]
\label{thm:zero-signature-faithfulness-mirror-slot-rigidity}
Let two accepted zero-source rows be read by a $\ZeroSignatureFaithfulnessUp$ carrier. If their displayed zero signatures agree, then the packet classifier row $F$ assigns them to the same mirror slot. Thus equal displayed zero signatures force the same mirror slot under the finite classifier surface of the packet.
\end{theorem}
\begin{proof}
Read the two zero-source rows through $Z$. Each row first passes through the critical-strip and zeta-zero sibling surfaces, so the comparison is internal to the displayed zero-source boundary.

Compare the signature rows through $\Sigma$. Equality of the displayed signatures means equality of the zero atom type, spectral time, and mirror-slot component recorded by the carrier.

Apply the classifier row $F$. By the definition of the packet classifier, equal displayed $\Sigma$ rows are classified into the same mirror slot.

The transport and replay rows $H$ and $C$ preserve this comparison under accepted readback, while $P$ and $L$ keep the result attached to package provenance and local naming. The conclusion is finite mirror-slot rigidity, not an RH theorem or a host equality principle for complex zeros.
\end{proof}

\falsifiablePrediction{If two accepted zero-source rows share the displayed signature but occupy distinct mirror slots, then $\ZeroSignatureFaithfulnessUp$ fails the mirror-slot rigidity theorem.}

\independenceWitness{criticalstripzetazerowitness, criticallinewitness, zetazeros, nontrivialzeroclassifier: $\ZeroSignatureFaithfulnessUp$ is not bijective to any listed sibling because it carries the zero-source row, mirror-slot row, displayed zero signature, packet classifier, transport, replay, provenance, and local NameCert ledger in one packet.}

The Lean follow-up must provide \texttt{Nontrivial ZeroSignatureFaithfulnessUp} and \texttt{FieldFaithful ZeroSignatureFaithfulnessUp} instances for the displayed carrier. The existing RH-route context at \texttt{BEDC.Derived.RHRoute.ZeroSignatureFaithfulness} supplies the zero-signature carrier-map boundary but does not yet define the concrete chapter carrier.

\closureat{ZeroSignatureFaithfulnessUp}{seedStr}

\begin{closurestatus}{\ZeroSignatureFaithfulnessUp}
  \constructivestory{A $\ZeroSignatureFaithfulnessUp$ packet is generated from zero-source, mirror-slot, displayed zero-signature, classifier, hsame transport, Cont replay, Pkg provenance, and local NameCert ledger rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:zero-signature-faithfulness-carrier}, \autoref{thm:zero-signature-faithfulness-namecert-obligations}, \autoref{thm:zero-signature-faithfulness-mirror-slot-rigidity}, the falsifiable prediction, and the independence witness seed the finite mirror-slot faithfulness route for zero-source signatures.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close RH, zero simplicity, analytic continuation completeness, Euler product closure, host-level equality of complex zeros, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, zero-source row exactness, mirror-slot readback, signature comparison, classifier faithfulness, transport, replay, provenance, local ledgering, and TasteGate instances for BEDC.Derived.ZeroSignatureFaithfulnessUp.}
\end{closurestatus}
