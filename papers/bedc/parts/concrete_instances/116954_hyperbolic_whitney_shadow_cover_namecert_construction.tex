\chapter{A Concrete Naming Certificate for $\HyperbolicWhitneyShadowCoverUp$}
\label{ch:concrete-instances-hyperbolic-whitney-shadow-cover-namecert}
\origin{ai}

The $\HyperbolicWhitneyShadowCoverUp$ packet realizes the finite shadow-cover branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as concrete NameCert data. It sits between the compactification surface of \autoref{ch:concrete-instances-hyperbolic-boundary-compactification-namecert}, the Gromov-boundary rows of \autoref{ch:concrete-instances-gromov-boundary-namecert}, the visual-boundary packet of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, the visual-metric rows of \autoref{ch:concrete-instances-hyperbolic-visual-metric-namecert}, and the boundary-shadow route of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}. The packet names a finite boundary shadow cover only; the Lean TasteGate side later needs $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances before it can certify the intended hyperbolic comparison surface.

\begin{definition}[HyperbolicWhitneyShadowCover carrier]
\label{def:hyperbolic-whitney-shadow-cover-carrier}
A $\HyperbolicWhitneyShadowCoverUp$ carrier is a finite $\BHist$ packet
$$
S=(M,V,G,B,T,F,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the hyperbolic metric source row, recording the displayed space, basepoint, coarse distance route, and local metric naming surface.
\item $V$ is the visual-boundary row, read through $\HyperbolicVisualBoundaryUp$ and naming the boundary side visible to the observer.
\item $G$ is the $\GromovBoundaryUp$ row, exposing the ray-equivalence and product data used by the visual boundary.
\item $B$ is the $\HyperbolicBoundaryShadowUp$ row, recording the accepted finite shadow window.
\item $T$ is the visual-metric tolerance row, read through $\HyperbolicVisualMetricUp$ and the $\RealUp$ seal.
\item $F$ is the selected finite shadow family, storing only displayed shadows, centers, apertures, and scale indices.
\item $R$ is the Real seal row, recording the finite real-valued tolerance read and no ambient real-completeness principle.
\item $H$ records componentwise $\hsame$ transport for the metric source, visual boundary, Gromov boundary, boundary shadow, visual metric tolerance, selected shadow family, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a finite hyperbolic shadow-cover request through $M,V,G,B,T,F,R,H$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicWhitneyShadowCoverUp$, $\HyperbolicBoundaryCompactificationUp$, $\GromovBoundaryUp$, $\HyperbolicVisualBoundaryUp$, $\HyperbolicVisualMetricUp$, $\HyperbolicBoundaryShadowUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicWhitneyShadowCoverUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,V,G,B,T,F,R,H,C,P,N$. It has no coordinate for selected infinite shadows, quotient boundary equality, ambient real completeness, measure theory, ergodic density, host group action, or an unsealed visual metric.
\end{definition}

\begin{theorem}[HyperbolicWhitneyShadowCover NameCert obligations]
\label{thm:hyperbolic-whitney-shadow-cover-namecert-obligations}
For every accepted $\HyperbolicWhitneyShadowCoverUp$ carrier
$$
S=(M,V,G,B,T,F,R,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicWhitneyShadowCoverUp}$ surface consists exactly of hyperbolic metric admission through $M$, visual-boundary exposure through $V$, Gromov-boundary exposure through $G$, boundary-shadow routing through $B$, visual-metric tolerance through $T$, selected finite shadow-family admission through $F$, Real sealing through $R$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-whitney-shadow-cover-carrier}. The hyperbolic metric source is $M$, while $V$ and $G$ expose the boundary rows before any selected shadow can be read.

Next read the covering data. The boundary-shadow row $B$ and visual-metric tolerance row $T$ admit the shadow windows and tolerance, and the selected finite shadow family $F$ stores only displayed centers, apertures, and scale indices.

The Real seal is explicit. The row $R$ records the finite tolerance read through $\RealUp$, so the packet has a real-valued comparison row without importing ambient real completeness or a host visual-metric theorem.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier contains no selected infinite-shadow family, quotient-boundary equality, measure-theoretic density, ergodic theorem, host group action, or unsealed metric coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicWhitneyShadowCoverUp$ read exposes the gromov-boundary, hyperbolic-visual-metric, hyperbolic-boundary-shadow, hyperbolic-visual-boundary, and real rows before any finite shadow-cover consumer reads the packet.}

\independenceWitness{gromov-boundary, hyperbolic-visual-metric, hyperbolic-boundary-shadow, hyperbolic-visual-boundary, real: no listed sibling is bijective with $\HyperbolicWhitneyShadowCoverUp$ because this packet jointly stores a hyperbolic metric source, visual boundary, Gromov boundary, boundary shadow, visual-metric tolerance, selected finite shadow family, Real seal, transport, replay, provenance, and local naming.}

\closureat{HyperbolicWhitneyShadowCoverUp}{seedStr}

\begin{closurestatus}{\HyperbolicWhitneyShadowCoverUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-whitney-shadow-cover-carrier} and \autoref{thm:hyperbolic-whitney-shadow-cover-namecert-obligations} seed the finite hyperbolic shadow-cover packet over displayed metric, visual-boundary, Gromov-boundary, boundary-shadow, visual-metric, finite-shadow, Real-seal, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim selected infinite shadows, quotient boundary equality, ambient Real completeness, measure theory, ergodic density, host group action, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure asks for checked carrier admission, boundary exposure, shadow-window routing, visual-metric tolerance, selected finite shadows, Real sealing, transport, replay, provenance, local naming, and TasteGate Nontrivial and FieldFaithful instances for BEDC.Derived.HyperbolicWhitneyShadowCoverUp.}
  \constructivestory{A $\HyperbolicWhitneyShadowCoverUp$ packet is generated from a hyperbolic metric source, visual-boundary row, Gromov-boundary row, boundary-shadow row, visual-metric tolerance row, selected finite shadow family, Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
