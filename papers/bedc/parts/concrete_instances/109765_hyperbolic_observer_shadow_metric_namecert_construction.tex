\chapter{A Concrete Naming Certificate for $\HyperbolicObserverShadowMetricUp$}
\label{ch:concrete-instances-hyperbolic-observer-shadow-metric-namecert}
\origin{ai}

The $\HyperbolicObserverShadowMetricUp$ packet realizes the observer-shadow reading of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It consumes the visual-shadow metric surface of \autoref{ch:concrete-instances-hyperbolic-visual-shadow-metric-namecert}, the general metric rows of \autoref{ch:concrete-instances-metric-namecert}, finite observation windows from \autoref{ch:concrete-instances-streamname-namecert}, regular rational readbacks from \autoref{ch:concrete-instances-regseqrat-namecert}, and terminal real-name seals from \autoref{ch:concrete-instances-real-namecert}. The packet names the finite visual shadow seen by a focused observer route; it is not a global hyperbolic model, a physical observer trajectory, a quotient boundary, or ambient $\RealUp$ completeness.

\begin{definition}[Hyperbolic observer-shadow metric carrier]
\label{def:hyperbolic-observer-shadow-metric-carrier}
A $\HyperbolicObserverShadowMetricUp$ carrier is a finite $\BHist$ packet
$$
O=(S,M,W,R,E,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\HyperbolicVisualShadowMetricUp$ source row, supplying the boundary-shadow visual metric packet.
\item $M$ is the $\MetricUp$ comparison row used when the observer-shadow read is consumed as a metric datum.
\item $W$ is the $\StreamNameUp$ finite-window schedule opened for the visual-shadow and metric reads.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the same finite windows.
\item $E$ is the terminal $\RealUp$ seal row for the displayed shadow distance.
\item $F$ is the focused observer-shadow row, recording which observer-facing finite route is being inspected.
\item $H$ records componentwise $\hsame$ transport for the visual-shadow, metric, window, readback, real, focus, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the focused observer-shadow request through $S,M,W,R,E,F$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicObserverShadowMetricUp$, $\HyperbolicVisualShadowMetricUp$, $\MetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HyperbolicObserverShadowMetricUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,M,W,R,E,F,H,C,P,N$. It has no coordinate for a completed hyperbolic space, quotient boundary equality, physical observer path, hidden global geometry, or ambient real completeness.
\end{definition}

\begin{theorem}[Hyperbolic observer-shadow metric NameCert obligations]
\label{thm:hyperbolic-observer-shadow-metric-namecert-obligations}
For every accepted $\HyperbolicObserverShadowMetricUp$ carrier
$$
O=(S,M,W,R,E,F,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicObserverShadowMetricUp}$ surface consists exactly of visual-shadow admission, metric comparison admission, finite stream-window exposure, regular-rational readback, terminal real sealing, focused observer-shadow routing, componentwise transport, displayed replay, package provenance, and local naming through $S,M,W,R,E,F,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-observer-shadow-metric-carrier}. The listed rows are the whole observer-shadow metric packet.

The visual-shadow row $S$ is read before the metric row $M$. This keeps the metric comparison tied to the finite visual-shadow carrier rather than to a host hyperbolic model.

The window, readback, and real-seal rows $W,R,E$ then expose the numerical shadow distance through the same L10 real-name route. The focused row $F$ records the observer-facing route being inspected.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since no further coordinate is present, the NameCert surface exports no completed hyperbolic space, quotient boundary equality, physical observer trajectory, hidden global geometry, or ambient real completeness.
\end{proof}

\begin{theorem}[Hyperbolic observer-shadow L10 handoff]
\label{thm:hyperbolic-observer-shadow-l10-handoff}
Every $\HyperbolicObserverShadowMetricUp$ consumer that reads a shadow distance factors through $\HyperbolicVisualShadowMetricUp$, $\MetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows before the focused observer-shadow route can be consumed as a finite NameCert datum.
\end{theorem}
\begin{proof}
Begin with an accepted carrier $O=(S,M,W,R,E,F,H,C,P,N)$. The source visual-shadow metric row $S$ supplies the displayed boundary-shadow packet.

The metric comparison row $M$ is then consumed through the finite metric surface, and the L10 numeric read follows the window, readback, and real-seal route $W,R,E$.

Only after these rows are displayed does the focused row $F$ expose the observer-shadow route. Transport, continuation, provenance, and local naming are supplied by $H,C,P,N$, so the handoff remains finite and cannot pass through a host global geometry or quotient boundary.
\end{proof}

\closureat{HyperbolicObserverShadowMetricUp}{seedStr}

\begin{closurestatus}{\HyperbolicObserverShadowMetricUp}
  \constructivestory{A $\HyperbolicObserverShadowMetricUp$ packet is generated from a HyperbolicVisualShadowMetric row, a MetricUp comparison row, StreamName windows, RegSeqRat readback, a RealUp seal, a focused observer-shadow row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-observer-shadow-metric-carrier}, \autoref{thm:hyperbolic-observer-shadow-metric-namecert-obligations}, and \autoref{thm:hyperbolic-observer-shadow-l10-handoff} seed the finite observer-shadow metric route over visual-shadow, metric, stream-window, regular-rational, and real-name rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim completed hyperbolic geometry, quotient boundary equality, physical observer trajectories, hidden global geometry, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, visual-shadow source admission, metric comparison, stream-window readback, real-name sealing, focused observer routing, and local naming for BEDC.Derived.HyperbolicObserverShadowMetricUp.}
\end{closurestatus}
