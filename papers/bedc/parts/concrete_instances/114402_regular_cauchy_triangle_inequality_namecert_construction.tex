\chapter{A Concrete Naming Certificate for $\RegularCauchyTriangleInequalityUp$}
\label{ch:concrete-instances-regular-cauchy-triangle-inequality-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchyTriangleInequalityUp$ packet records the finite route by which three regular Cauchy rational sources present a triangle-inequality estimate before a Real metric consumer reads the terminal seal. It sits after the three-leg bound of \autoref{ch:concrete-instances-regular-cauchy-triangle-bound-namecert}, the metric readback surface of \autoref{ch:concrete-instances-regularcauchymetric-namecert}, the real metric route of \autoref{ch:concrete-instances-real-triangle-inequality-namecert}, the synchronized windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational source rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite handoff surface for Real and Completion consumers; it is not quotient stream equality, a selected limit, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy triangle inequality carrier]
\label{def:regular-cauchy-triangle-inequality-carrier}
A $\RegularCauchyTriangleInequalityUp$ carrier is a finite $\BHist$ packet
$$
T=(X,Y,Z,L_{xy},L_{yz},W,Q,M,R,S,H,C,P,N)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $X,Y,Z$ are the three $\RegSeqRatUp$ source rows whose pairwise metric comparison is requested.
\item $L_{xy}$ and $L_{yz}$ are the two $\RegularCauchyTriangleBoundUp$ leg packets that compare $X$ to $Y$ and $Y$ to $Z$ through finite regular Cauchy estimates.
\item $W$ is the synchronized $\StreamNameUp$ window family used to inspect the three sources and the two leg packets at a common request.
\item $Q$ is the $\DyadicRatCoreUp$ tolerance ledger carrying the two leg budgets, their displayed sum, and the output budget read by the Real metric consumer.
\item $M$ is the $\RegularCauchyMetricUp$ handoff row that turns the synchronized regular Cauchy comparisons into the metric-facing surface.
\item $R$ is the $\RealTriangleInequalityUp$ handoff row that exposes the terminal three-leg Real metric route.
\item $S$ stores the $\RealUp$ seal rows for the three endpoints and the final distance comparison.
\item $H$ stores componentwise $\hsame$ transport for sources, leg packets, windows, dyadic budgets, metric handoff, Real handoff, seal rows, provenance, and the local name.
\item $C$ stores displayed $\Cont$ replay from a Real metric triangle request through $X,Y,Z,L_{xy},L_{yz},W,Q,M,R,S,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyTriangleInequalityUp$, $\RegularCauchyTriangleBoundUp$, $\RegularCauchyMetricUp$, $\RealTriangleInequalityUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RegularCauchyTriangleInequalityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only after transporting every displayed row to the common source, window, tolerance, metric, and Real seal surface. The carrier has no coordinate for quotient stream equality, selected limits, countable choice, trichotomy, host real equality, or ambient completeness.
\end{definition}

\begin{theorem}[Regular Cauchy triangle inequality NameCert obligations]
\label{thm:regular-cauchy-triangle-inequality-obligations}
For an accepted $\RegularCauchyTriangleInequalityUp$ carrier $T=(X,Y,Z,L_{xy},L_{yz},W,Q,M,R,S,H,C,P,N)$, the local $\NameCert_{\RegularCauchyTriangleInequalityUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the displayed three-source packet $T$;
\item source admission for the three $\RegSeqRatUp$ rows $X,Y,Z$;
\item leg admission for the two $\RegularCauchyTriangleBoundUp$ packets $L_{xy}$ and $L_{yz}$;
\item synchronized $\StreamNameUp$ window inspection through $W$;
\item dyadic budget addition and output-budget exactness through $Q$;
\item $\RegularCauchyMetricUp$ handoff through $M$;
\item $\RealTriangleInequalityUp$ handoff through $R$;
\item terminal $\RealUp$ seal reads through $S$;
\item componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local naming row $N$.
\end{enumerate}
No obligation exports quotient stream equality, selected limits, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-triangle-inequality-carrier}. Its displayed rows are exactly the three regular Cauchy sources, the two triangle-bound legs, synchronized stream windows, dyadic tolerance ledger, metric handoff, Real triangle handoff, Real seal rows, transports, replay, provenance, and local naming row.

The NameCert obligations are therefore the admission and replay checks for those coordinates. Source admission reads $X,Y,Z$; leg admission reads $L_{xy}$ and $L_{yz}$; window inspection reads $W$; tolerance exactness reads $Q$; the two handoff obligations read $M$ and $R$; and the terminal seal reads $S$.

The rows $H,C,P,N$ account for componentwise transport, consumer replay, package provenance, and local naming. Since the carrier contains no coordinate for quotient stream equality, selected limits, countable choice, trichotomy, host real equality, or ambient completeness, the displayed list exhausts the local certificate surface.
\end{proof}

\begin{theorem}[Regular Cauchy triangle inequality Real metric route]
\label{thm:regular-cauchy-triangle-inequality-real-metric-route}
Every Real metric triangle consumer of an accepted $\RegularCauchyTriangleInequalityUp$ carrier factors through the displayed finite route
$$
(X,Y,Z) \longrightarrow (L_{xy},L_{yz},W,Q) \longrightarrow M \longrightarrow R \longrightarrow S.
$$
The consumer first reads the three $\RegSeqRatUp$ sources, the two $\RegularCauchyTriangleBoundUp$ legs, the synchronized $\StreamNameUp$ windows, and the $\DyadicRatCoreUp$ tolerance ledger; it then reads the $\RegularCauchyMetricUp$ handoff, the $\RealTriangleInequalityUp$ handoff, and the terminal $\RealUp$ seal rows. No Real metric triangle route out of the packet can bypass this finite carrier through quotient stream equality, selected limits, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with an accepted carrier and project its rows. The only source coordinates are $X,Y,Z$, the only leg coordinates are $L_{xy},L_{yz}$, and the only shared inspection and budget coordinates are $W,Q$.

The continuation row $C$, transported by $H$, replays a Real metric triangle request through those rows before it can reach the metric-facing handoff $M$. Thus the regular Cauchy metric comparison is already tied to the two displayed legs and their dyadic budget ledger.

The Real-facing part of the route then reads $R$ and $S$. Row $R$ is the displayed $\RealTriangleInequalityUp$ handoff, while $S$ stores the terminal $\RealUp$ seal rows for the three endpoints and the final comparison.

Because $P$ and $N$ only record provenance and local naming, and because the carrier has no coordinate for quotient stream equality, selected limits, countable choice, trichotomy, host real equality, or ambient completeness, every Real metric triangle consumer factors through the finite route displayed above.
\end{proof}

\closureat{RegularCauchyTriangleInequalityUp}{seedStr}

\begin{closurestatus}{\RegularCauchyTriangleInequalityUp}
  \constructivestory{A $\RegularCauchyTriangleInequalityUp$ packet is generated from three $\RegSeqRatUp$ source rows, two $\RegularCauchyTriangleBoundUp$ leg packets, synchronized $\StreamNameUp$ windows, a $\DyadicRatCoreUp$ tolerance ledger, $\RegularCauchyMetricUp$ and $\RealTriangleInequalityUp$ handoff rows, terminal $\RealUp$ seal rows, componentwise $\hsame$ transports, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-triangle-inequality-carrier}, \autoref{thm:regular-cauchy-triangle-inequality-obligations}, and \autoref{thm:regular-cauchy-triangle-inequality-real-metric-route} seed the finite triangle-inequality route over regular Cauchy sources, triangle-bound legs, synchronized stream windows, dyadic budgets, metric handoff, Real handoff, Real seals, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected limits, countable choice, trichotomy, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked source admission, leg admission, synchronized window inspection, dyadic budget exactness, RegularCauchyMetric handoff, RealTriangleInequality handoff, RealUp seal reads, and non-escape for BEDC.Derived.RegularCauchyTriangleInequalityUp.}
\end{closurestatus}
