\chapter{A Concrete Naming Certificate for $\RellichKondrachovUp$}
\label{ch:concrete-instances-rellich-kondrachov-namecert}
\origin{human}

Following Rellich and Kondrachov, in the form used by Adams--Fournier for Sobolev spaces, the $\RellichKondrachovUp$ packet records the finite compact-embedding route from a bounded Sobolev window into a compact metric target window. It sits after the weak-derivative and Sobolev embedding surfaces of \autoref{ch:concrete-instances-weak-derivative-namecert} and \autoref{ch:concrete-instances-sobolev-embedding-namecert}, and it cites the Sobolev carrier of \autoref{ch:concrete-instances-sobolev-namecert}, the compact and complete metric witnesses of \autoref{ch:concrete-instances-compactmetric-namecert} and \autoref{ch:concrete-instances-completemetric-namecert}, and the scalar readback of \autoref{ch:concrete-instances-real-namecert}. The chapter names only the finite subsequence-window ledger and the compactness route visible to BEDC consumers.

\begin{definition}[Rellich--Kondrachov carrier]
\label{def:rellich-kondrachov-carrier}
A $\RellichKondrachovUp$ carrier is a finite $\BHist$ packet
$$
R=(D,W,S,E,C,M,T,L,H,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ records the bounded domain, boundary regularity, scalar $\RealUp$ readback, and finite coordinate windows in which the compact-embedding statement is inspected.
\item $W$ is the $\WeakDerivativeUp$ row, read through \autoref{ch:concrete-instances-weak-derivative-namecert}, that fixes the weak-action observations used by the Sobolev source.
\item $S$ is the $\SobolevUp$ source row from \autoref{ch:concrete-instances-sobolev-namecert}, including the derivative-order, integrability, and boundedness ledger visible to the compactness route.
\item $E$ is the $\SobolevEmbeddingUp$ comparison row from \autoref{ch:concrete-instances-sobolev-embedding-namecert}; it records the exponent inequality and the embedding read without asserting compactness by itself.
\item $C$ is the $\CompactMetricUp$ target row from \autoref{ch:concrete-instances-compactmetric-namecert}, restricted to the finite sequential compactness windows named by this packet.
\item $M$ is the $\CompleteMetricUp$ Cauchy-readback row from \autoref{ch:concrete-instances-completemetric-namecert}, used only to read the displayed limit-window tests.
\item $T$ stores the finite tolerance scale and subsequence-window schedule, listing for each requested observation window the bounded Sobolev source window, the target metric tolerance, and the compact-metric witness window.
\item $L$ is the compact-embedding ledger. It records that the route
$$
W,S,E,D \longmapsto C,M,T
$$
is the only available path from bounded Sobolev data to compact target observations.
\item $H$ stores componentwise $\hsame$ transport for $D,W,S,E,C,M,T,L,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RellichKondrachovUp$, $\WeakDerivativeUp$, $\SobolevUp$, $\SobolevEmbeddingUp$, $\CompactMetricUp$, $\CompleteMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RellichKondrachovUp}$ row.
\end{enumerate}
The carrier has no coordinate for a selected subsequence, open-cover compactness, quotient function space, host function representative, countable choice, full PDE regularity, trace theory, or ambient $\RealUp$ completeness beyond the displayed real readbacks.
\end{definition}

\begin{theorem}[Rellich--Kondrachov NameCert obligations]
\label{thm:rellich-kondrachov-namecert-obligations}
For every accepted $\RellichKondrachovUp$ carrier $R=(D,W,S,E,C,M,T,L,H,P,N)$, the local $\NameCert_{\RellichKondrachovUp}$ surface consists exactly of carrier admission for the eleven displayed rows, weak-derivative access through $W$, Sobolev source access through $S$, embedding comparison access through $E$, compact-metric target access through $C$, complete-metric Cauchy readback through $M$, finite subsequence-window scheduling through $T$, compact-route ledger preservation through $L$, componentwise $\hsame$ transport through $H$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Carrier admission is projection from the displayed packet of \autoref{def:rellich-kondrachov-carrier}. The analytic inputs are only the weak-derivative, Sobolev, embedding, compact-metric, complete-metric, and real readback rows named in $D,W,S,E,C,M,T,L,H,P,N$.

The obligation surface is therefore exhausted row by row. The weak action is read through $W$, the Sobolev boundedness data through $S$, the exponent and embedding comparison through $E$, and the compact target windows through $C$ and $M$.

The subsequence evidence is not a chosen subsequence. It is the finite schedule in $T$ together with the ledger $L$, so a consumer may replay a requested compactness window but cannot select an infinite object outside the packet.

Finally, $H,P,N$ transport, source, and name the same rows. Since no remaining coordinate contains open-cover compactness, quotient function spaces, host representatives, choice, trace theory, or ambient real completeness, none of those objects can discharge a local NameCert clause.
\end{proof}

\begin{theorem}[Rellich--Kondrachov finite compact-embedding route]
\label{thm:rellich-kondrachov-finite-compact-embedding-route}
Every consumer read exported by an accepted $\RellichKondrachovUp$ carrier factors through the displayed route
$$
\begin{aligned}
&\WeakDerivativeUp,\SobolevUp,\SobolevEmbeddingUp,\RealUp \\
&\qquad\longmapsto \CompactMetricUp,\CompleteMetricUp \\
&\qquad\longmapsto T,L,H,P,N.
\end{aligned}
$$
The route supplies only finite compactness windows: a bounded Sobolev source row, an embedding comparison row, a compact-metric witness window, a complete-metric readback window, and the tolerance ledger that relates them. It supplies no selected subsequence, open-cover compactness principle, quotient function space, host representative, countable choice, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Begin with a consumer request for compact embedding data. The carrier first exposes the weak-derivative and Sobolev source rows, so the request is restricted to the derivative order, integrability, boundedness, and scalar readbacks displayed in $D,W,S$.

Next the request must pass through the embedding comparison row $E$. This row locates the source and target exponents, but by construction it contains no compactness coordinate beyond the route recorded in this carrier.

The target compactness read is then forced through $C,M,T$. The compact-metric row gives the finite witness window, the complete-metric row gives the Cauchy readback available inside that window, and the schedule $T$ records the tolerance and observation indices used by the route.

It remains only to replay the route. The ledger $L$ states that all compact-embedding observations factor through the displayed rows, while $H,P,N$ transport, source, and name the same finite packet. The absent analytic principles are not recoverable from these rows.
\end{proof}

\falsifiablePrediction{Every $\RellichKondrachovUp$ compact-embedding read factors through displayed WeakDerivativeUp, SobolevUp, SobolevEmbeddingUp, CompactMetricUp, CompleteMetricUp, RealUp, subsequence-window schedule, compact-route ledger, transport, provenance, and local naming rows.}

\independenceWitness{weak-derivative, sobolev, sobolev-embedding, compactmetric, completemetric, real: $\RellichKondrachovUp$ is not bijective to any listed sibling because it jointly carries the finite compact-embedding route, target compactness windows, complete-metric readbacks, subsequence-window schedule, route ledger, transport, provenance, and local naming.}

\closureat{RellichKondrachovUp}{seedStr}

\begin{closurestatus}{\RellichKondrachovUp}
  \constructivestory{A $\RellichKondrachovUp$ packet is generated from bounded Sobolev and weak-derivative rows, an embedding comparison row, compact-metric and complete-metric target windows, scalar readback, a finite subsequence-window schedule, compact-route ledger, componentwise transport, provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:rellich-kondrachov-carrier}, \autoref{thm:rellich-kondrachov-namecert-obligations}, and \autoref{thm:rellich-kondrachov-finite-compact-embedding-route} seed the finite compact-embedding route over WeakDerivative, Sobolev, SobolevEmbedding, CompactMetric, CompleteMetric, and Real neighbours.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected subsequences, open-cover compactness, quotient function spaces, host representatives, trace theory, full PDE regularity, countable choice, bridge checking, or Lean verification.}
  \upgradepath{Obligation closure requires a Lean carrier encoding and checked NameCert obligation theorem for the same finite Rellich--Kondrachov compact-embedding packet.}
\end{closurestatus}
