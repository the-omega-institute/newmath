\chapter{A Concrete Naming Certificate for $\CompactConvergenceTheoremUp$}
\label{ch:concrete-instances-compact-convergence-theorem-namecert}
\origin{human}

Following Kelley and Bishop--Bridges on compact convergence, the $\CompactConvergenceTheoremUp$ packet defines a finite $\BHist$ route over $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformLimitUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. It sits after the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-limit sibling of \autoref{ch:concrete-instances-uniformlimit-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The packet records the compact-source uniform-convergence theorem as finite BEDC data; it does not import open-cover compactness, host function-space completion, selected suprema, or ambient Real completeness.

\begin{definition}[Compact convergence theorem carrier]
\label{def:compact-convergence-theorem-carrier}
A $\CompactConvergenceTheoremUp$ carrier is a finite $\BHist$ packet
$$
\mathsf{CompConv}(K,F,U,W,R,D,S,H,C,P,N)
$$
with the following rows.
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, exposing the compact metric carrier and the finite compactness evidence used by the theorem packet.
\item $F$ is the $\ContinuousMapUp$ family row, recording the displayed continuous maps on the compact source.
\item $U$ is the $\UniformLimitUp$ row, carrying the uniform Cauchy or uniform convergence evidence for the family $F$ over $K$.
\item $W$ is the $\StreamNameUp$ finite-window row used to inspect only bounded prefixes of the convergence data.
\item $R$ is the $\RegSeqRatUp$ readback row, translating the inspected windows into regular rational observations.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, recording the dyadic radius and comparison slack used by the uniform-limit route.
\item $S$ is the terminal $\RealUp$ seal row, reached only after $K,F,U,W,R,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for compact sources, continuous maps, uniform-limit evidence, stream windows, regular rational readbacks, dyadic budgets, Real seals, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from the compact source and map-family rows through $U,W,R,D$ to the Real seal row $S$.
\item $P$ is the $\Pkg$ provenance over $\CompactConvergenceTheoremUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformLimitUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactConvergenceTheoremUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,F,U,W,R,D,S,H,C,P,N$. It has no coordinate for an open-cover compactness proof, a host function-space completion, a selected supremum norm, a quotient of maps, or an ambient completeness theorem for $\RealUp$.
\end{definition}

\begin{theorem}[Compact convergence theorem NameCert obligations]
\label{thm:compact-convergence-theorem-namecert-obligations}
For every accepted $\CompactConvergenceTheoremUp$ carrier $\mathsf{CompConv}(K,F,U,W,R,D,S,H,C,P,N)$, the local $\NameCert_{\CompactConvergenceTheoremUp}$ surface consists exactly of compact-source admission through $K$, continuous-map family exposure through $F$, uniform-limit evidence through $U$, finite $\StreamNameUp$ window inspection through $W$, $\RegSeqRatUp$ readback through $R$, $\DyadicRatCoreUp$ tolerance budgeting through $D$, terminal $\RealUp$ seal handoff through $S$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-convergence-theorem-carrier}. The rows $K,F,U,W,R,D,S,H,C,P,N$ are the only fields available to the local classifier.

The compact source $K$ and continuous-map family $F$ determine the theorem's domain and maps. The uniform-limit row $U$ supplies the displayed convergence evidence, but it must still pass through the finite window row $W$, the regular rational readback row $R$, and the dyadic tolerance row $D$ before any Real-facing seal is visible.

The structural rows $H,C,P,N$ transport, replay, source, and name that same route. Since the carrier contains no open-cover compactness proof, host function-space completion, selected supremum, quotient-map equality, or ambient Real-completeness coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Compact convergence uniform-limit route]
\label{thm:compact-convergence-theorem-uniform-limit-route}
Every accepted $\CompactConvergenceTheoremUp$ consumer that reads a compact-source uniform convergence theorem must factor through the route
$$
\CompactMetricUp,\ \ContinuousMapUp,\ \UniformLimitUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp
\longrightarrow
\RealUp.
$$
In particular, the public read sees a compact metric source, a continuous-map family, uniform-limit evidence, finite observation windows, regular rational readback, and dyadic tolerance data before the terminal Real seal.
\end{theorem}
\begin{proof}
Begin with an accepted carrier $\mathsf{CompConv}(K,F,U,W,R,D,S,H,C,P,N)$. The compact source and continuous-map rows are the only entries that name the domain and map family.

The uniform-limit row $U$ is downstream of those rows. It is not public until the finite $\StreamNameUp$ windows $W$, $\RegSeqRatUp$ readback $R$, and $\DyadicRatCoreUp$ tolerance row $D$ have displayed the inspected convergence data.

Finally the $\RealUp$ seal $S$ and structural rows $H,C,P,N$ preserve the same route. Thus the theorem packet exports a finite uniform-limit path, not a hidden compactness theorem, selected supremum, or host completion object.
\end{proof}

\begin{theorem}[Compact convergence Real-seal boundary]
\label{thm:compact-convergence-theorem-real-seal-boundary}
The $\RealUp$ boundary of an accepted $\CompactConvergenceTheoremUp$ packet is exactly the seal row $S$ reached from $K,F,U,W,R,D$. A downstream Real or Completion consumer may use $S$ only together with the displayed compact source, continuous-map family, uniform-limit row, finite stream windows, regular rational readback, dyadic tolerance budget, transport, replay, provenance, and local naming rows stored in the same packet.
\end{theorem}
\begin{proof}
Read the carrier rows in order. The seal row $S$ has no independent source: it is reached only after the compact metric row, continuous-map family row, uniform-limit row, finite window row, regular rational readback row, and dyadic tolerance row have been projected.

Componentwise transport $H$ and replay $C$ keep that dependency visible, while $P$ and $N$ record provenance and local naming for the same packet. Since there is no further coordinate for open covers, ambient completeness, or selected suprema, any Real or Completion consumer must remain inside this displayed Real-seal boundary.
\end{proof}

\falsifiablePrediction{An accepted $\CompactConvergenceTheoremUp$ consumer that reaches a $\RealUp$ seal without displaying the compact metric source, continuous-map family, uniform-limit row, StreamName window, RegSeqRat readback, and DyadicRatCore tolerance data refutes the carrier surface.}

\independenceWitness{compactmetric, continuousmap, uniformlimit, streamname, regseqrat, dyadicratcore, real: $\CompactConvergenceTheoremUp$ is not bijective to any listed sibling because it jointly binds the compact source, map family, uniform-limit evidence, finite observation windows, regular rational readback, dyadic tolerance budget, Real seal, transport, replay, provenance, and local naming as one theorem packet.}

\closureat{CompactConvergenceTheoremUp}{seedStr}

\begin{closurestatus}{\CompactConvergenceTheoremUp}
  \constructivestory{A $\CompactConvergenceTheoremUp$ packet is generated from CompactMetricUp, ContinuousMapUp, UniformLimitUp, StreamNameUp, RegSeqRatUp, DyadicRatCoreUp, RealUp, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-convergence-theorem-carrier}, \autoref{thm:compact-convergence-theorem-namecert-obligations}, \autoref{thm:compact-convergence-theorem-uniform-limit-route}, and \autoref{thm:compact-convergence-theorem-real-seal-boundary} seed the compact-source uniform-convergence route through finite windows, regular rational readback, dyadic tolerance data, and a Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, host function-space completion, selected suprema, quotient-map equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source compatibility, continuous-map family exposure, uniform-limit route exactness, finite-window readback, dyadic tolerance preservation, Real-seal nonescape, componentwise transport, provenance, and local naming for BEDC.Derived.CompactConvergenceTheoremUp.}
\end{closurestatus}
