\chapter{A Concrete Naming Certificate for $\RegularSequenceModulusUp$}
\label{ch:concrete-instances-regular-sequence-modulus-namecert}
\origin{human}

Following Bishop and Bridges, a regular Cauchy construction needs an explicit tail-modulus packet before a regular rational readback can be handed to a real-name seal. The $\RegularSequenceModulusUp$ packet records that finite tail data over the dyadic arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the stream-window naming surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational route of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name surface of \autoref{ch:concrete-instances-real-namecert}, and the modulus boundary of \autoref{ch:concrete-instances-cauchymodulus-namecert}. It is a displayed modulus object for regular-Cauchy tails, not a quotient of streams, a selected limit, a host real equality, a countable-choice schedule, or ambient $\RealUp$ completeness.

\begin{definition}[Regular sequence modulus carrier]
\label{def:regular-sequence-modulus-carrier}
A $\RegularSequenceModulusUp$ carrier is a finite $\BHist$ packet
$$
R=(S,D,Q,M,T,H,C,P,N)
$$
with the following rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ source row, listing the finite windows of the sequence name inspected by the packet.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, listing the dyadic comparison requests used by the tail test.
\item $Q$ is the $\RegSeqRatUp$ readback row, exposing the regular rational observations that will be consumed downstream.
\item $M$ is the tail-modulus row; for each admitted precision request it displays a finite bound after which all listed readbacks are stable inside the corresponding dyadic tolerance.
\item $T$ is the finite tail ledger pairing stream windows, dyadic tolerances, readback rows, and modulus bounds.
\item $H$ records componentwise $\hsame$ transport for $S,D,Q,M,T$ and the structural rows.
\item $C$ records displayed $\Cont$ replay from a tail request through the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\RegularSequenceModulusUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\CauchyModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularSequenceModulusUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through hsame-stable agreement of $S,D,Q,M,T,H,C,P,N$. Two carriers agree when the same finite windows, dyadic tolerance rows, regular rational readbacks, modulus bounds, tail-ledger entries, transport rows, replay rows, provenance rows, and local naming rows are matched by componentwise $\hsame$. No coordinate records a selected limit, quotient stream equality, countable choice, host real equality, or ambient completion theorem.
\end{definition}
\leanchecked{BEDC.Derived.RegularSequenceModulusUp.TasteGate.RegularSequenceModulusTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Regular sequence modulus carrier admission]
\label{thm:regular-sequence-modulus-carrier-admission}
Every accepted $\RegularSequenceModulusUp$ carrier
$R=(S,D,Q,M,T,H,C,P,N)$ admits exactly the finite NameCert surface displayed in \autoref{def:regular-sequence-modulus-carrier}. A consumer may project stream windows, dyadic tolerances, regular rational readbacks, tail-modulus bounds, tail-ledger entries, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. It may not project a selected limit, quotient stream equality, countable-choice schedule, host real equality, or ambient $\RealUp$ completeness witness.
\end{theorem}
\begin{proof}
Project the carrier definition. The rows $S,D,Q,M,T$ provide the finite data used by tail reads, while $H,C,P,N$ provide the structural transport, replay, provenance, and local name.

The classifier is hsame-stable only on those displayed rows. Therefore an accepted packet can be read through the listed finite coordinates and through no detached completion coordinate.

Finally inspect the carrier inventory. Since selected limits, quotient stream classes, countable-choice schedules, host real equalities, and ambient completeness witnesses are not fields of $R$, they cannot be projected from the NameCert surface.
\end{proof}

\begin{theorem}[Regular sequence modulus tail exactness]
\label{thm:regular-sequence-modulus-tail-exactness}
Let $R=(S,D,Q,M,T,H,C,P,N)$ be an accepted $\RegularSequenceModulusUp$ carrier. For every admitted dyadic precision row in $D$, the modulus row $M$ supplies a finite tail bound, and the ledger $T$ records that all corresponding $\RegSeqRatUp$ readbacks in $Q$ past that bound agree inside the displayed dyadic tolerance. This exactness is internal to the finite packet: it does not choose a limit and does not identify streams by quotient equality.
\end{theorem}
\begin{proof}
Begin with the dyadic precision row in $D$. The carrier requires the same precision request to be paired with a displayed bound in $M$ and with readback entries in $Q$.

Use the tail ledger $T$. It is the finite row that binds the stream windows from $S$, the dyadic comparisons from $D$, the regular rational observations from $Q$, and the modulus bound from $M$ into one tail assertion.

The structural rows $H,C,P,N$ only transport, replay, source, and name that same finite assertion. Because no limit or quotient coordinate is present, the exactness statement remains the displayed tail-bound claim carried by $T$.
\end{proof}

\begin{theorem}[Regular sequence modulus RegSeqRat handoff]
\label{thm:regular-sequence-modulus-regseqrat-handoff}
Every $\RegSeqRatUp$ consumer that receives a tail witness from an accepted $\RegularSequenceModulusUp$ packet must pass through the finite route
$$
\StreamNameUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \CauchyModulusUp,\ \RegularSequenceModulusUp,\ \RealUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
For each admitted unary precision row, the modulus bound in $M$ supplies the closeness witness consumed by the $\RegSeqRatUp$ readback boundary, and the terminal $\RealUp$ seal may only read that displayed witness. The handoff exports no selected limit, quotient stream equality, countable choice, host real equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Apply \autoref{thm:regular-sequence-modulus-carrier-admission}. A downstream read can only project the displayed carrier rows and the structural rows.

Next use \autoref{thm:regular-sequence-modulus-tail-exactness}. The precision row in $D$ and the bound in $M$ are tied by $T$ to the regular rational readback in $Q$, so the $\RegSeqRatUp$ boundary receives a finite closeness witness rather than an external convergence theorem.

The $\RealUp$ seal is downstream of the same readback route. Since $H,C,P,N$ merely preserve and name the finite packet, the terminal seal cannot add quotient stream equality, a selected limit, choice, host equality, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularSequenceModulusUp$ consumer displays $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\CauchyModulusUp$, finite tail-ledger, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows before it reads a terminal $\RealUp$ seal.}

\independenceWitness{streamname, dyadicratcore, regseqrat, real, cauchymodulus: $\RegularSequenceModulusUp$ is not bijective to any listed sibling because it packages the finite tail-modulus row and the regular rational handoff ledger as one hsame-stable NameCert surface.}

\closureat{RegularSequenceModulusUp}{seedStr}

\begin{closurestatus}{\RegularSequenceModulusUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-sequence-modulus-carrier}, \autoref{thm:regular-sequence-modulus-carrier-admission}, \autoref{thm:regular-sequence-modulus-tail-exactness}, and \autoref{thm:regular-sequence-modulus-regseqrat-handoff} seed the finite tail-modulus packet for regular rational readbacks and terminal real-name reads.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected limits, quotient stream equality, countable choice, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier stability, tail-bound exactness, RegSeqRat handoff, transport replay, provenance, and non-escape over the same finite packet.}
  \constructivestory{A $\RegularSequenceModulusUp$ packet is generated from finite stream windows, dyadic tolerance rows, regular rational readbacks, tail-modulus bounds, a finite tail ledger, componentwise transport, displayed replay, package provenance, and local naming evidence.}
\end{closurestatus}
