\chapter{A Concrete Naming Certificate for $\CantorIntersectionMetricUp$}
\label{ch:concrete-instances-cantor-intersection-metric-namecert}
\origin{human}

Following Cantor's intersection theorem and Bishop--Bridges constructive metric completion, the $\CantorIntersectionMetricUp$ packet records the metric nested-closed-set intersection route as finite BEDC NameCert data over the L10 completion surface. It sits after the complete-metric source of \autoref{ch:concrete-instances-completemetric-namecert}, the metric-completion route of \autoref{ch:concrete-instances-metriccompletion-namecert}, the nested closed-ball packet of \autoref{ch:concrete-instances-nestedclosedball-namecert}, the Cantor complete metric route of \autoref{ch:concrete-instances-cantor-complete-metric-namecert}, the Cauchy-filter interface of \autoref{ch:concrete-instances-cauchyfilter-namecert}, and the L10 real-name route through \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The packet names a nested metric intersection handoff; it does not assert open-cover compactness, arbitrary closed-set compactness, quotient stream equality, selected limits, or ambient $\RealUp$ completeness.

\begin{definition}[CantorIntersectionMetric carrier]
\label{def:cantor-intersection-metric-carrier}
A $\CantorIntersectionMetricUp$ carrier is a finite $\BHist$ packet
$$
I_{\mathrm{cim}}=(M,E,B,K,F,D,S,R,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\CompleteMetricUp$ source row whose metric and convergence service are the only complete-metric data available to the packet.
\item $E$ is the $\MetricCompletionUp$ route row naming the surrounding completion request before any intersection point is read.
\item $B$ is the $\NestedClosedBallUp$ row, recording the descending closed metric ball spine and its finite containment witnesses.
\item $K$ is the $\CantorCompleteMetricUp$ row, used only as the displayed nested closed-ball completion handoff.
\item $F$ is the $\CauchyFilterUp$ row generated by the nested metric spine and read only through the displayed filter route.
\item $D$ is the $\DyadicRatCoreUp$ diameter and tolerance ledger, storing the shrink schedule, late-radius comparisons, and requested precision bounds.
\item $S$ is the $\StreamNameUp$ finite-window row exposing the inspected centers, radii, and containment data.
\item $R$ is the $\RegSeqRatUp$ handoff row for the regular rational readback of the displayed center schedule.
\item $L$ is the terminal $\RealUp$ seal reached only after $B,K,F,D,S,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the complete-metric source, completion route, nested-ball row, Cantor handoff, filter, dyadic ledger, stream windows, regular readback, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a nested metric intersection request through $M,E,B,K,F,D,S,R,L$.
\item $P$ is the $\Pkg$ provenance over $\CantorIntersectionMetricUp$, $\CompleteMetricUp$, $\MetricCompletionUp$, $\NestedClosedBallUp$, $\CantorCompleteMetricUp$, $\CauchyFilterUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CantorIntersectionMetricUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,E,B,K,F,D,S,R,L,H,C,P,N$. It has no coordinate for open-cover compactness, arbitrary closed-set compactness, quotient completion equality, a selected limit, host trichotomy, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.CantorIntersectionMetricUp.CantorIntersectionMetricTasteGate\_single\_carrier\_alignment}

\begin{theorem}[CantorIntersectionMetric NameCert obligations]
\label{thm:cantor-intersection-metric-namecert-obligations}
For every accepted $\CantorIntersectionMetricUp$ carrier
$$
I_{\mathrm{cim}}=(M,E,B,K,F,D,S,R,L,H,C,P,N),
$$
the local $\NameCert_{\CantorIntersectionMetricUp}$ surface consists exactly of the following five obligations:
\begin{enumerate}
\item expose the complete-metric and metric-completion source rows $M,E$ before any intersection witness is read;
\item display the nested metric route through $B,K,F$, namely the nested closed-ball spine, Cantor complete-metric handoff, and generated Cauchy-filter row;
\item bind all numerical readback to the L10 rows $D,S,R,L$, namely the dyadic tolerance ledger, finite stream windows, regular rational handoff, and terminal real seal;
\item preserve the route by componentwise $\hsame$ transport and displayed $\Cont$ replay through $H,C$;
\item source and name the same finite packet through $\Pkg$ provenance $P$ and the local $\NameCert$ row $N$.
\end{enumerate}
No obligation row is stored for open-cover compactness, arbitrary closed-set compactness, quotient completions, selected limits, trichotomy, or full $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:cantor-intersection-metric-carrier}. The rows $M,E$ expose the metric and completion sources before the packet can read an intersection point.

The nested metric content is then displayed through $B,K,F$. The nested closed-ball row supplies the descending closed metric spine, the Cantor complete-metric row supplies the existing handoff, and the Cauchy-filter row records the generated filter route.

The L10 readback is downstream. The dyadic ledger $D$, stream windows $S$, regular rational row $R$, and real seal $L$ are the only numerical coordinates available before a completion-facing consumer receives a named output.

Finally $H,C,P,N$ transport, replay, source, and name that same finite route. Since the carrier contains none of the excluded compactness, quotient, selected-limit, trichotomy, or ambient-completeness coordinates, the five displayed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[CantorIntersectionMetric complete handoff]
\label{thm:cantor-intersection-metric-complete-handoff}
Every completion-facing read exported by an accepted $\CantorIntersectionMetricUp$ carrier factors through the displayed nested metric route
$$
\CompleteMetricUp,\ \MetricCompletionUp,\ \NestedClosedBallUp,\ \CantorCompleteMetricUp,\ \CauchyFilterUp,\ \DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert_{\CantorIntersectionMetricUp}.
$$
Thus a consumer receives the complete-metric service, completion request, nested closed-ball spine, Cantor complete-metric handoff, Cauchy-filter row, dyadic tolerance ledger, finite stream windows, regular rational readback, terminal real seal, componentwise transport, replay, provenance, and local naming rows before it may use the intersection output. The handoff exports no $\RealUp$ completeness principle, open-cover compactness theorem, arbitrary closed-set compactness, quotient stream equality, or selected limit.
\end{theorem}
\begin{proof}
Begin with a completion-facing read of the accepted carrier. The first available rows are $M$ and $E$, so the read is grounded in the displayed complete-metric service and the surrounding metric-completion request.

Next pass through the nested route $B,K,F$. The descending closed-ball spine is read through the $\NestedClosedBallUp$ row, the existing Cantor complete-metric handoff is read through $K$, and the generated filter is read through $F$.

The numerical output is then forced through the L10 readback rows $D,S,R,L$. The dyadic ledger supplies tolerance control, the stream row supplies finite windows, the regular rational row supplies readback, and the terminal real seal is reached only after those rows are present.

The structural rows $H,C,P,N$ preserve the same route for downstream consumers. Because no row supplies full real completeness, open-cover compactness, arbitrary closed-set compactness, quotient stream equality, or a selected limit, every completion-facing read factors through the displayed nested metric route.
\end{proof}

\closureat{CantorIntersectionMetricUp}{seedStr}

\begin{closurestatus}{\CantorIntersectionMetricUp}
  \constructivestory{A $\CantorIntersectionMetricUp$ packet is generated from a CompleteMetric source row, a MetricCompletion request row, a NestedClosedBall spine, a CantorCompleteMetric handoff, a CauchyFilter row, a DyadicRatCore tolerance ledger, StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cantor-intersection-metric-carrier}, \autoref{thm:cantor-intersection-metric-namecert-obligations}, and \autoref{thm:cantor-intersection-metric-complete-handoff} seed the metric nested-closed-set intersection route over displayed completion, nested-ball, Cantor handoff, filter, dyadic, stream, regular readback, real seal, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, arbitrary closed-set compactness, quotient completions, selected limits, full $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, nested metric inclusion, Cantor handoff, Cauchy-filter generation, dyadic shrink exactness, finite-window readback, real sealing, transport, replay, provenance, and local naming for BEDC.Derived.CantorIntersectionMetricUp.}
\end{closurestatus}
