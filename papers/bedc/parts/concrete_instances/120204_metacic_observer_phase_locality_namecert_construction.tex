\chapter{A Concrete Naming Certificate for $\MetacicObserverPhaseLocalityUp$}
\label{ch:concrete-instances-metacic-observer-phase-locality-namecert}
\origin{ai}

The $\MetacicObserverPhaseLocalityUp$ packet realizes the observer-phase locality route of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite MetaCIC residual-window data. It sits beside the critical-path packet of \autoref{ch:concrete-instances-metaciccriticalpath-namecert}, the normalization frontier of \autoref{ch:concrete-instances-metacicnormalizationfrontier-namecert}, the confluence residual envelope of \autoref{ch:concrete-instances-metacic-confluence-residual-envelope-namecert}, the decidability boundary of \autoref{ch:concrete-instances-metacicdecidableboundary-namecert}, and the observer phase route of \autoref{ch:concrete-instances-hyperbolic-observer-phase-route-namecert}. The packet names a local observer-phase read over residual windows; it does not assert global confluence, subject reduction, beta-conversion decidability, quotient syntax equality, a selected normal form, or kernel consistency.

\begin{definition}[MetaCIC observer-phase locality carrier]
\label{def:metacic-observer-phase-locality-carrier}
A $\MetacicObserverPhaseLocalityUp$ carrier is a finite $\BHist$ packet
$$
L=(O,R,J,B,K,D,A,E,H,T,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $O$ is the observer-phase row whose read is local to a displayed MetaCIC audit route;
\item $R$ is the finite residual-window row retaining the redex and residual observations that feed the local read;
\item $J$ is the local-join row, recording the candidate join available inside the displayed residual window;
\item $B$ is the candidate-boundary row separating local candidate evidence from any global normalization or confluence assertion;
\item $K$ is the bounded checker row consumed through the decidability boundary;
\item $D$ is the obstruction-retention row that keeps failed subject-reduction, confluence, or conversion reads visible rather than erased;
\item $A$ is the adjacent critical-path row through which candidate-mediated rows, residual budget, bounded checker rows, and L10 faces remain auditable;
\item $E$ stores componentwise $\hsame$ transport for the observer-phase, residual-window, local-join, candidate-boundary, checker, obstruction, and critical-path rows;
\item $H$ records the displayed $\Cont$ replay by which a consumer reads the same local observer-phase route;
\item $T$ is the taste-facing instance slot reserved for later Nontrivial and FieldFaithful evidence;
\item $C$ is the classifier row comparing accepted packets only through the listed local rows;
\item $P$ records $\Pkg$ provenance over $\MetacicObserverPhaseLocalityUp$, $\MetaCICCriticalPathUp$, $\MetaCICNormalizationFrontierUp$, $\MetaCICConfluenceResidualEnvelopeUp$, $\MetaCICDecidableBoundaryUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\MetacicObserverPhaseLocalityUp}$ row.
\end{enumerate}
The classifier has no coordinate for global confluence, subject reduction, beta-conversion decidability, quotient term equality, selected normal forms, or kernel consistency.
\end{definition}

\begin{theorem}[MetaCIC observer-phase locality NameCert obligations]
\label{thm:metacic-observer-phase-locality-namecert-obligations}
For the carrier of \autoref{def:metacic-observer-phase-locality-carrier}, the local $\NameCert_{\MetacicObserverPhaseLocalityUp}$ surface consists exactly of six finite obligations: carrier admission for $L=(O,R,J,B,K,D,A,E,H,T,C,P,N)$, observer-phase and residual-window admission through $O,R$, local-join and candidate-boundary separation through $J,B$, bounded checker and obstruction-retention accounting through $K,D$, critical-path transport and replay through $A,E,H$, and taste-facing instance, classifier, provenance, and local naming through $T,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier rows. The observer-phase row $O$ is local because it can be read only with the residual window $R$, not as an unscoped phase surface.

Next attach the MetaCIC rows. The local join $J$, candidate boundary $B$, bounded checker $K$, and obstruction-retention row $D$ keep the route inside the residual-window envelope while the critical-path row $A$ exposes the candidate-mediated and L10-facing dependencies already present in the surrounding MetaCIC surface.

Finally read $E,H,T,C,P,N$. Transport and replay preserve the same finite route, the taste-facing slot records that later Lean TasteGate work must provide Nontrivial and FieldFaithful instances, and the classifier, provenance, and local NameCert rows add no global confluence, subject reduction, conversion decider, quotient syntax equality, selected normal form, or kernel-consistency theorem.
\end{proof}

\falsifiablePrediction{Any accepted MetacicObserverPhaseLocalityUp packet that exports observer-phase locality without the residual window, local join, candidate boundary, obstruction-retention row, bounded checker, critical-path dependency, transport, replay, provenance, and local NameCert rows refutes the carrier.}

\independenceWitness{metaciccriticalpath, metacicnormalizationfrontier, metacic-confluence-residual-envelope, metacicdecidableboundary: $\MetacicObserverPhaseLocalityUp$ is not bijective to any listed sibling because it jointly carries observer-phase locality, residual windows, local joins, candidate-boundary separation, obstruction retention, bounded checker rows, critical-path dependency, taste-facing instance slots, transport, replay, classifier comparison, provenance, and local NameCert data.}

\closureat{MetacicObserverPhaseLocalityUp}{seedStr}

\begin{closurestatus}{\MetacicObserverPhaseLocalityUp}
  \constructivestory{A $\MetacicObserverPhaseLocalityUp$ packet is generated from observer-phase, residual-window, local-join, candidate-boundary, bounded-checker, obstruction-retention, critical-path, taste-facing instance, transport, replay, classifier, provenance, and local naming rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-observer-phase-locality-carrier} and \autoref{thm:metacic-observer-phase-locality-namecert-obligations} seed the observer-phase locality packet over displayed MetaCIC residual windows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close global confluence, subject reduction, beta-conversion decidability, quotient syntax equality, selected normal forms, Lean verification, bridge checking, or kernel consistency.}
  \upgradepath{Obligation closure requires checked carrier admission, residual-window locality, local-join accounting, candidate-boundary separation, bounded checker routing, obstruction retention, critical-path dependency, Nontrivial and FieldFaithful TasteGate instances, transport, provenance, and local naming for BEDC.Derived.MetacicObserverPhaseLocalityUp.}
\end{closurestatus}
