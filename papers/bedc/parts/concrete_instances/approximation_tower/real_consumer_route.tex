\begin{theorem}[ApproximationTower Real consumer route]
\label{thm:approximation-tower-real-consumer-route}
Every accepted $\ApproximationTowerUp$ consumer that reaches the terminal $\RealUp$ seal must first display the finite level ledger, the $\StreamNameUp$ window rows, the $\DyadicRatCoreUp$ tolerance rows, the $\RegSeqRatUp$ readback row, the $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\ApproximationTowerUp}$ row. The route is the concrete L10 completion path through \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}.
\end{theorem}
\begin{proof}
Project the accepted carrier $A=(L,W,D,R,E,H,C,P,N)$ of \autoref{def:approximation-tower-carrier}. The row $L$ is the only finite level ledger, $W$ is the only displayed stream-window source, $D$ is the only dyadic tolerance surface, $R$ is the only regular rational readback row, and $E$ is the only terminal real seal.

Apply the finite NameCert surface of \autoref{thm:approximation-tower-finite-namecert-surface}. It forces the analytic rows to be read with the structural rows $H,C,P,N$, so a Real-facing consumer receives transport, replay, provenance, and local naming before it can consume $E$.

Finally use the L10 sibling route of \autoref{thm:approximation-tower-l10-sibling-route}. The route anchors the window, tolerance, readback, and seal rows in the StreamName, DyadicRatCore, RegSeqRat, and Real chapters named above. Thus the accepted Real consumer cannot bypass the finite tower route through a hidden source, quotient stream equality, selected limit point, ambient completeness theorem, physical-adequacy classifier, or external approximation oracle.
\end{proof}
