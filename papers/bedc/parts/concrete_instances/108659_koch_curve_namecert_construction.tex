\chapter{A Concrete Naming Certificate for $\KochCurveUp$}
\label{ch:concrete-instances-koch-curve-namecert}
\origin{human}

Following Koch's classical curve construction, the $\KochCurveUp$ packet records the finite polygonal-stage, dyadic-schedule, and uniform-limit handoff data for the standard Koch curve before any completed planar subset is imported. It sits beside the compact binary address discipline of \autoref{ch:concrete-instances-cantorspace-namecert}, the uniform-limit surface of \autoref{ch:concrete-instances-uniformlimit-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the compact-uniform-continuity surface of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}. The packet names only finite stage words, segment replacements, coordinate schedules, tolerance ledgers, and the displayed uniform-limit route; it does not import a host fractal subset, a selected infinite branch, quotient equality of real pairs, ambient compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Koch curve carrier]
\label{def:koch-curve-carrier}
A $\KochCurveUp$ carrier is a finite $\BHist$ packet
$$
K=(A,S,D,W,Q,E,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the finite address row. It records the inspected ternary stage, the finite left-turn and right-turn segment word, and the CantorSpace address window used by branch-facing consumers.
\item $S$ is the polygonal-stage row, naming the directed segment list obtained by replacing each displayed segment by the four Koch generator segments at the next stage.
\item $D$ is the dyadic displacement ledger. It records the rational endpoint deltas and the dyadic tolerance comparisons used to read the finite segment list.
\item $W$ is the coordinate $\StreamNameUp$ window row for the horizontal and vertical endpoint schedules attached to the inspected segment.
\item $Q$ is the $\RegSeqRatUp$ readback row for those coordinate schedules, with all tolerance comparisons routed through $\DyadicRatCoreUp$.
\item $E$ is the pair of terminal $\RealUp$ coordinate seals read from the same scheduled polygonal-stage rows.
\item $L$ is the $\UniformLimitUp$ handoff row, binding the stagewise polygonal approximations, dyadic tolerance ledger, and coordinate seals to one uniform-limit route.
\item $H$ records componentwise $\hsame$ transport for addresses, segment stages, displacement ledgers, coordinate windows, rational readbacks, real seals, uniform-limit handoff, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from each curve-facing request through $A,S,D,W,Q,E,L,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\KochCurveUp$, $\CantorSpaceUp$, $\UniformLimitUp$, $\RealUp$, $\DyadicRatCoreUp$, $\CompactUniformContinuityUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\KochCurveUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,S,D,W,Q,E,L,H,C,P,N$. It has no coordinate for a completed planar fractal, selected infinite branch, quotient equality of real pairs, open-cover compactness, or ambient completeness.
\end{definition}

\begin{theorem}[Koch curve NameCert obligations]
\label{thm:koch-curve-namecert-obligations}
For every accepted $\KochCurveUp$ carrier
$$
K=(A,S,D,W,Q,E,L,H,C,P,N),
$$
the local $\NameCert_{\KochCurveUp}$ surface consists exactly of carrier admission for the displayed packet; finite address exposure through $A$; polygonal-stage exposure through $S$; dyadic displacement and tolerance exposure through $D$; coordinate-window exposure through $W$; regular rational readback through $Q$; terminal real coordinate sealing through $E$; uniform-limit handoff through $L$; componentwise transport through $H$; displayed replay through $C$; provenance through $P$; and local naming through $N$. No obligation is discharged by a host planar powerset, selected infinite address, quotient real-pair equality, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:koch-curve-carrier}. The accepted packet displays exactly the finite address row, polygonal-stage row, displacement ledger, coordinate windows, rational readbacks, real seals, uniform-limit handoff, and structural rows.

The finite combinatorial obligations are exhausted by $A$ and $S$. A consumer cannot inspect a curve branch until the stage address is displayed, and it cannot read a segment replacement except through the directed segment list attached to that same finite stage.

The coordinate obligations are exhausted by $D,W,Q,E$. The dyadic ledger gives endpoint deltas and tolerance comparisons, the coordinate windows schedule the endpoint reads, the regular rational row reads those schedules, and the $\RealUp$ seals remain downstream of the same polygonal-stage data.

Finally $L,H,C,P,N$ transport, replay, source, and name only the displayed route. Since the excluded host principles have no carrier coordinate, they cannot appear as local $\NameCert_{\KochCurveUp}$ obligations.
\end{proof}

\begin{theorem}[Koch curve uniform-limit handoff]
\label{thm:koch-curve-uniform-limit-handoff}
Every accepted $\KochCurveUp$ consumer that reaches a curve point, segment bound, or real-coordinate read must first expose the finite route
$$
\CantorSpaceUp \longmapsto A,S \longmapsto D,W,Q,E \longmapsto \UniformLimitUp \longmapsto H,C,P,N .
$$
The handoff exports only the displayed polygonal-stage approximations, dyadic tolerance ledger, scheduled coordinate readbacks, terminal $\RealUp$ coordinate seals, and uniform-limit route. It exports no completed fractal subset, selected infinite branch, quotient equality of real coordinate pairs, ambient compactness theorem, or ambient completeness principle.
\end{theorem}
\begin{proof}
Begin with a consumer that reaches the Real-facing boundary of the packet. By \autoref{def:koch-curve-carrier}, the only terminal coordinate seals are downstream of the finite address, polygonal-stage, dyadic displacement, coordinate-window, and rational-readback rows.

Apply \autoref{thm:koch-curve-namecert-obligations}. It forces every curve-facing read through $A,S,D,W,Q,E,L,H,C,P,N$ and keeps the uniform-limit handoff on the same displayed packet.

The compact-uniform-continuity and uniform-limit dependencies supply only the finite modulus and convergence route named by $L$. They do not add a second carrier coordinate for a host planar subset, a selected infinite address, quotient equality of real coordinate pairs, or ambient completeness. Therefore every point, segment, or coordinate consumer remains inside the finite NameCert packet.
\end{proof}

\falsifiablePrediction{An accepted $\KochCurveUp$ consumer that reaches a $\RealUp$ coordinate seal or a uniform-limit read without first displaying the finite address row, polygonal-stage row, dyadic displacement ledger, coordinate window, and rational readback refutes the carrier surface.}

\independenceWitness{CantorSpace, UniformLimit, Real, DyadicRatCore, and CompactUniformContinuity: no listed sibling supplies the whole $\KochCurveUp$ packet, because each sibling contributes address windows, convergence handoff, terminal coordinate seals, dyadic tolerance data, or modulus discipline while this chapter binds all rows with transport, replay, provenance, and local naming.}

\closureat{KochCurveUp}{seedStr}

\begin{closurestatus}{\KochCurveUp}
  \constructivestory{A $\KochCurveUp$ packet is generated from finite address rows, polygonal-stage segment rows, dyadic displacement ledgers, coordinate StreamName windows, RegSeqRat readbacks, RealUp coordinate seals, a UniformLimit handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:koch-curve-carrier}, \autoref{thm:koch-curve-namecert-obligations}, and \autoref{thm:koch-curve-uniform-limit-handoff} seed the finite polygonal-stage and uniform-limit certificate surface for the classical Koch curve.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host planar powersets, selected infinite addresses, quotient equality of real coordinate pairs, open-cover compactness, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite address exposure, polygonal-stage exposure, dyadic displacement exactness, coordinate readback, Real coordinate sealing, uniform-limit handoff, transport, replay, provenance, and local naming for BEDC.Derived.KochCurveUp.}
\end{closurestatus}
