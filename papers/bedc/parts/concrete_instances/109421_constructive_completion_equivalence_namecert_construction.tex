\chapter{A Concrete Naming Certificate for $\ConstructiveCompletionEquivalenceUp$}
\label{ch:concrete-instances-constructive-completion-equivalence-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis and Kelley's treatment of completion by universal comparison, the $\ConstructiveCompletionEquivalenceUp$ packet records the finite BEDC surface on which several constructive completion presentations can be read through one comparison certificate. It sits after the Cauchy-comparison route of \autoref{ch:concrete-instances-cauchy-completion-comparison-namecert}, the Bishop completion comparison of \autoref{ch:concrete-instances-bishopcompletioncomparison-namecert}, the separated uniqueness ledger of \autoref{ch:concrete-instances-completion-uniqueness-namecert}, and the metric uniqueness boundary of \autoref{ch:concrete-instances-metric-completion-uniqueness-namecert}. The packet also reads the finite observation spine of \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. It compares displayed constructive completion routes only; it is not quotient-completion equality, not selected representative equality, and not a proof that $\RealUp$ is ambiently complete.

\begin{definition}[Constructive completion equivalence carrier]
\label{def:constructive-completion-equivalence-carrier}
A $\ConstructiveCompletionEquivalenceUp$ carrier is a finite $\BHist$ packet
$$
E=(C,B,U,M,S,D,R,A,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $C$ is the $\CauchyCompletionComparisonUp$ row, comparing unit, universal-property, functorial, and metric-completion presentations by finite observation data.
\item $B$ is the $\BishopCompletionComparisonUp$ row, comparing regular located Cauchy data with the completion-facing real-name seal.
\item $U$ is the $\CompletionUniquenessUp$ row, recording the separated uniqueness ledger available to completion consumers.
\item $M$ is the $\MetricCompletionUniquenessUp$ row, recording the metric completion comparison boundary over a common metric source.
\item $S$ is the $\StreamNameUp$ finite-window row common to the completion observations.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger attached to the same windows and comparison requests.
\item $R$ is the $\RegSeqRatUp$ readback row extracted from the windows and dyadic ledger.
\item $A$ is the terminal $\RealUp$ seal row reached only after the comparison, window, tolerance, and readback rows have been displayed.
\item $H$ records componentwise $\hsame$ transport for $C,B,U,M,S,D,R,A$ and the local naming row.
\item $K$ records displayed $\Cont$ replay through the comparison rows, observation spine, transport, provenance, and local naming rows.
\item $P$ is the $\Pkg$ provenance over $\ConstructiveCompletionEquivalenceUp$, $\CauchyCompletionComparisonUp$, $\BishopCompletionComparisonUp$, $\CompletionUniquenessUp$, $\MetricCompletionUniquenessUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\ConstructiveCompletionEquivalenceUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $C,B,U,M,S,D,R,A,H,K,P,N$. It has no coordinate for quotient completion equality, selected representatives, host equality of completed objects, countable choice, open-cover compactness, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Constructive completion equivalence NameCert obligations]
\label{thm:constructive-completion-equivalence-namecert-obligations}
For every accepted $\ConstructiveCompletionEquivalenceUp$ carrier
$$
E=(C,B,U,M,S,D,R,A,H,K,P,N),
$$
the local $\NameCert_{\ConstructiveCompletionEquivalenceUp}$ surface consists exactly of Cauchy-comparison exposure through $C$, Bishop-comparison exposure through $B$, separated uniqueness exposure through $U$, metric uniqueness exposure through $M$, finite-window exposure through $S$, dyadic tolerance exposure through $D$, regular rational readback through $R$, terminal real-name sealing through $A$, componentwise transport through $H$, displayed replay through $K$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:constructive-completion-equivalence-carrier}. The four comparison rows $C,B,U,M$ are the only completion-presentation coordinates, while $S,D,R,A$ are the shared finite observation spine.

The observation spine is read after the comparison rows have been named. Finite windows, dyadic tolerances, and regular rational readback provide the data by which the terminal $\RealUp$ seal is reached.

Finally $H,K,P,N$ transport, replay, source, and name the same packet. Since no quotient completion equality, selected representative, host equality, countable-choice schedule, open-cover compactness row, or ambient real-completeness theorem occurs in the carrier, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Constructive completion equivalence comparison route]
\label{thm:constructive-completion-equivalence-comparison-route}
Every completion-equivalence consumer of an accepted $\ConstructiveCompletionEquivalenceUp$ carrier factors through
$$
\CauchyCompletionComparisonUp,\ \BishopCompletionComparisonUp,\ \CompletionUniquenessUp,\ \MetricCompletionUniquenessUp,\ \StreamNameUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \RealUp
$$
as displayed rows before it may compare constructive completion presentations. The route exports only the finite comparison certificate $E=(C,B,U,M,S,D,R,A,H,K,P,N)$ and does not identify quotient completions or assert ambient completeness of $\RealUp$.
\end{theorem}
\begin{proof}
Begin with the obligation surface of \autoref{thm:constructive-completion-equivalence-namecert-obligations}. Any consumer must first expose the Cauchy-comparison, Bishop-comparison, separated uniqueness, and metric uniqueness rows.

The shared observation spine is then consumed through $S,D,R,A$. These rows give finite windows, tolerance ledgers, regular rational readback, and a terminal real-name seal, so the comparison remains a displayed certificate rather than a quotient completion object.

The structural rows $H,K,P,N$ preserve the same route under transport and replay. Therefore no consumer can bypass the finite comparison packet by citing selected representatives, host equality of completed points, countable choice, open-cover compactness, or ambient completeness of $\RealUp$.
\end{proof}

\falsifiablePrediction{Every accepted $\ConstructiveCompletionEquivalenceUp$ read exposes the CauchyCompletionComparison, BishopCompletionComparison, CompletionUniqueness, MetricCompletionUniqueness, StreamName, DyadicRatCore, RegSeqRat, RealUp, componentwise hsame, Cont replay, Pkg provenance, and local NameCert rows before any constructive-completion comparison is consumed.}

\independenceWitness{cauchy-completion-comparison, bishopcompletioncomparison, completion-uniqueness, metric-completion-uniqueness, streamname, dyadicratcore, regseqrat, real: none is bijective with $\ConstructiveCompletionEquivalenceUp$ because this carrier alone binds the four comparison surfaces to one shared finite observation spine and one local NameCert row without quotient-completion equality or ambient RealUp completeness.}

\closureat{ConstructiveCompletionEquivalenceUp}{seedStr}

\begin{closurestatus}{\ConstructiveCompletionEquivalenceUp}
  \constructivestory{A $\ConstructiveCompletionEquivalenceUp$ packet is generated from CauchyCompletionComparison, BishopCompletionComparison, CompletionUniqueness, MetricCompletionUniqueness, StreamName, DyadicRatCore, RegSeqRat, RealUp, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:constructive-completion-equivalence-carrier}, \autoref{thm:constructive-completion-equivalence-namecert-obligations}, and \autoref{thm:constructive-completion-equivalence-comparison-route} seed the finite comparison certificate for constructive completion presentations.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient completion equality, selected representatives, host equality of completed objects, countable choice, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, comparison-row exposure, shared-window coverage, dyadic tolerance exactness, regular readback, RealUp seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.ConstructiveCompletionEquivalenceUp.}
\end{closurestatus}
