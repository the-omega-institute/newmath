\chapter{A Concrete Naming Certificate for $\ApartnessCompletionReflectorUp$}
\label{ch:concrete-instances-apartness-completion-reflector-namecert}
\origin{human}

Following Bishop and Bridges, the $\ApartnessCompletionReflectorUp$ packet records the finite route by which a regular Cauchy source carrying displayed apartness evidence is reflected into the separated comparison surface of the Real completion route. It sits after the tail-diameter witness of \autoref{ch:concrete-instances-cauchy-tail-diameter-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a reflector from apartness-stable regular Cauchy data to a separated Real-facing comparison row; it does not select a completed limit, quotient a Cauchy name, assert countable choice, or invoke ambient $\RealUp$ completeness.

\begin{definition}[Apartness completion reflector carrier]
\label{def:apartness-completion-reflector-carrier}
An $\ApartnessCompletionReflectorUp$ carrier is a finite $\BHist$ packet
$$
A=(S,T,W,D,R,X,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the regular Cauchy source row, read through the $\RegSeqRatUp$ discipline and carrying the source observations used by the reflector.
\item $T$ is the $\CauchyTailDiameterUp$ row witnessing that the displayed tail window has the requested dyadic diameter bound.
\item $W$ is the $\StreamNameUp$ window row that exposes the finite observations used by $S$ and $T$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row recording the dyadic request, tail budget, and comparison scale.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the same finite windows and tolerance data.
\item $X$ is the displayed apartness witness row, recording the separated comparison branch that survives the regular Cauchy reflection.
\item $E$ is the $\RealUp$ seal row, available only after $S,T,W,D,R,X$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the source, tail-diameter, window, tolerance, readback, apartness, Real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through the same reflector route.
\item $P$ is the $\Pkg$ provenance over $\ApartnessCompletionReflectorUp$, $\CauchyTailDiameterUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ApartnessCompletionReflectorUp}$ row.
\end{enumerate}
The classifier accepts only packets whose separated completion read is exhausted by $S,T,W,D,R,X,E,H,C,P,N$. It has no coordinate for quotient Cauchy equality, selected representatives, selected limits, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Apartness completion carrier obligation]
\label{thm:apartness-completion-reflector-carrier-obligation}
For every accepted $\ApartnessCompletionReflectorUp$ carrier
$$
A=(S,T,W,D,R,X,E,H,C,P,N),
$$
the local $\NameCert_{\ApartnessCompletionReflectorUp}$ surface consists exactly of carrier admission for the displayed packet; regular Cauchy source exposure through $S$; tail-diameter exposure through $T$; finite StreamName window exposure through $W$; DyadicRatCore tolerance exposure through $D$; RegSeqRat readback through $R$; displayed apartness witness exposure through $X$; terminal Real sealing through $E$; componentwise transport through $H$; displayed replay through $C$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:apartness-completion-reflector-carrier}. The analytic rows are the regular source, tail-diameter row, finite window, dyadic tolerance, regular readback, apartness witness, and Real seal.

The structural rows $H,C,P,N$ transport, replay, source, and name exactly those analytic rows. They do not add an external completion endpoint or a quotient equality.

Thus the local certificate surface is exhausted by $S,T,W,D,R,X,E,H,C,P,N$. The excluded objects are absent from the carrier inventory, so they cannot become local obligations of the reflector.
\end{proof}

\begin{theorem}[Apartness completion separated readback]
\label{thm:apartness-completion-reflector-separated-readback}
Every separated Real-facing read of an accepted $\ApartnessCompletionReflectorUp$ carrier factors through
$$
S \longmapsto T,W,D \longmapsto R,X \longmapsto E \longmapsto H,C,P,N .
$$
The displayed apartness witness $X$ is read after the same tail-diameter, StreamName, DyadicRatCore, and RegSeqRat rows that serve the regular Cauchy source. Hence the separated comparison is a reflected finite route, not a selected completed real or a quotient of Cauchy names.
\end{theorem}
\begin{proof}
Start from the source row $S$. It supplies only the displayed regular Cauchy observations and must be inspected through the finite windows $W$.

Read the control rows next. The tail-diameter row $T$ and dyadic ledger $D$ fix the requested tolerance before the regular readback row $R$ is consumed.

The apartness witness $X$ is then attached to the same finite route. Since $X$ is downstream of $T,W,D,R$, it cannot be replaced by a detached trichotomy decision or host real inequality.

Finally the terminal Real seal $E$ and structural rows $H,C,P,N$ preserve the same route. The separated readback therefore remains finite and displayed.
\end{proof}

\begin{theorem}[Apartness completion reflector nonescape]
\label{thm:apartness-completion-reflector-nonescape}
No accepted $\ApartnessCompletionReflectorUp$ packet exports quotient Cauchy equality, a selected representative, a selected limit, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness. Every consumer that reads the terminal $\RealUp$ seal or the separated apartness witness must first read the rows $S,T,W,D,R,X,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Use the obligation surface of \autoref{thm:apartness-completion-reflector-carrier-obligation}. It lists all rows that a local NameCert read may inspect.

Apply the separated readback theorem \autoref{thm:apartness-completion-reflector-separated-readback}. It places the apartness witness and Real seal after the displayed tail-diameter, window, tolerance, and regular readback rows.

The excluded principles are not rows of the carrier. Since transport, replay, provenance, and local naming preserve only the displayed packet, no consumer can obtain those principles from an accepted reflector read.
\end{proof}

\falsifiablePrediction{Every accepted ApartnessCompletionReflectorUp read that reaches a separated Real-facing comparison displays the regular Cauchy source, tail-diameter row, StreamName window, DyadicRatCore tolerance, RegSeqRat readback, apartness witness, RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert row.}

\independenceWitness{cauchy-tail-diameter, regseqrat, streamname, dyadicratcore, real: no listed sibling alone binds a regular Cauchy source, tail-diameter control, finite stream window, dyadic tolerance, regular readback, displayed apartness witness, Real seal, transport, replay, provenance, and local naming into one completion reflector packet.}

\closureat{ApartnessCompletionReflectorUp}{seedStr}

\begin{closurestatus}{\ApartnessCompletionReflectorUp}
  \constructivestory{An $\ApartnessCompletionReflectorUp$ packet is generated from a regular Cauchy source, CauchyTailDiameter row, StreamName window, DyadicRatCore tolerance, RegSeqRat readback, displayed apartness witness, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:apartness-completion-reflector-carrier}, \autoref{thm:apartness-completion-reflector-carrier-obligation}, \autoref{thm:apartness-completion-reflector-separated-readback}, and \autoref{thm:apartness-completion-reflector-nonescape} seed the L10 route from apartness-stable regular Cauchy data to a separated Real-facing comparison surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient Cauchy equality, selected representatives, selected limits, countable choice, trichotomy, host real equality, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, tail-diameter exactness, finite-window readback, displayed apartness preservation, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.ApartnessCompletionReflectorUp.}
\end{closurestatus}
