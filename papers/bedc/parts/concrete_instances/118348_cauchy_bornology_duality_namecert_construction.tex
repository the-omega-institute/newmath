\chapter{A Concrete Naming Certificate for $\CauchyBornologyDualityUp$}
\label{ch:concrete-instances-cauchy-bornology-duality-namecert}
\origin{human}

Following Bishop and Bridges on constructive Cauchy completion, and Bourbaki and Hogbe-Nlend on bornological control, the $\CauchyBornologyDualityUp$ packet records the finite interface where boundedness tests and Cauchy-completion reads determine one another without passing to quotient completions or selected limits. It sits after the bounded-uniform Cauchy surface of \autoref{ch:concrete-instances-cauchy-bornological-uniformity-namecert}, the Cauchy-filter carrier of \autoref{ch:concrete-instances-cauchyfilter-namecert}, the metric-completion surface of \autoref{ch:concrete-instances-metriccompletion-namecert}, the uniform-space entourage discipline of \autoref{ch:concrete-instances-uniformspace-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the dual passage between bounded Cauchy data and completion-facing reads as finite NameCert evidence, not as ambient Real completeness.

\begin{definition}[Cauchy bornology duality carrier]
\label{def:cauchy-bornology-duality-carrier}
A $\CauchyBornologyDualityUp$ carrier is a finite $\BHist$ packet
$$
D=(B,F,U,M,W,R,A,C,H,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the bornological boundedness row read from bounded subsets or bounded events;
\item $F$ is the $\CauchyFilterUp$ row that turns bounded Cauchy tests into finite eventuality requests;
\item $U$ is the $\UniformSpaceUp$ entourage row that supplies the uniform reading of the same Cauchy data;
\item $M$ is the $\MetricCompletionUp$ row through which completion-facing consumers inspect the resulting route;
\item $W$ is the $\StreamNameUp$ finite-window row used to read Cauchy observations;
\item $R$ is the $\RegSeqRatUp$ readback row attached to the window schedule;
\item $A$ is the terminal $\RealUp$ seal for the displayed tolerance and distance reads;
\item $C$ is the compatibility row asserting that the bornological boundedness tests and completion-facing Cauchy reads replay through the same bounded-uniform route of \autoref{ch:concrete-instances-cauchy-bornological-uniformity-namecert};
\item $H$ stores componentwise $\hsame$ transport for the boundedness, Cauchy-filter, uniform, completion, window, readback, Real-seal, and compatibility rows;
\item $P$ is the $\Pkg$ provenance over $\CauchyBornologyDualityUp$, $\CauchyBornologyUp$, $\CauchyFilterUp$, $\UniformSpaceUp$, $\MetricCompletionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\CauchyBornologyDualityUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient completion, selected limit, countable choice, host equality of filters, trichotomy over distances, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Cauchy bornology duality NameCert obligations]
\label{thm:cauchy-bornology-duality-namecert-obligations}
For every accepted $\CauchyBornologyDualityUp$ carrier
$$
D=(B,F,U,M,W,R,A,C,H,P,N),
$$
the local $\NameCert_{\CauchyBornologyDualityUp}$ surface consists exactly of carrier admission, bornological boundedness, Cauchy-filter eventuality, uniform entourage stability, metric-completion routing, finite StreamName windows, RegSeqRat readback, Real-seal tolerance, bounded-completion compatibility, componentwise $\hsame$ transport, $\Pkg$ provenance, and local naming. A consumer may pass from boundedness tests to completion reads, or from completion reads back to bounded Cauchy tests, only through these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-bornology-duality-carrier}. The packet exposes $B,F,U,M,W,R,A,C,H,P,N$ as the whole local surface, so the boundedness, Cauchy-filter, uniform, completion, finite-window, readback, Real-seal, compatibility, transport, provenance, and naming rows are all displayed finite BEDC data.

The forward direction reads bounded Cauchy tests through $B,F,U$ before any completion-facing row is available. The compatibility row $C$ ties that route to the bounded-uniform Cauchy packet of \autoref{ch:concrete-instances-cauchy-bornological-uniformity-namecert}, so the metric-completion row $M$ can only consume the same displayed bounded-uniform route.

The reverse direction starts from a completion-facing read through $M,W,R,A$. The finite StreamName windows, RegSeqRat readback, and Real seal recover only the bounded Cauchy observations stored in $B,F,U,C$, not a chosen limit point or quotient representative.

Finally inspect $H,P,N$. Componentwise transport, provenance, and local naming preserve the same finite packet. Since the carrier has no quotient-completion, selected-limit, countable-choice, host-filter-equality, trichotomy, or ambient-completeness coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Cauchy bornology duality completion route]
\label{thm:cauchy-bornology-duality-completion-route}
Every completion-facing $\CauchyBornologyDualityUp$ consumer factors through
$$
\CauchyBornologicalUniformityUp,\ \CauchyFilterUp,\ \UniformSpaceUp,\ \MetricCompletionUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp
$$
before it exports a completion seal. The route is dual in the finite BEDC sense: boundedness tests determine exactly the Cauchy-completion reads carried by the packet, and completion reads reflect back only to those bounded Cauchy tests. It does not export quotient completions, selected representatives, countable choice, or ambient Real completeness.
\end{theorem}
\begin{proof}
Use the NameCert obligations of \autoref{thm:cauchy-bornology-duality-namecert-obligations}. They force every accepted consumer to expose boundedness, Cauchy-filter, uniform, completion, finite-window, readback, Real-seal, compatibility, transport, provenance, and local naming rows.

The bounded-uniform route of \autoref{ch:concrete-instances-cauchy-bornological-uniformity-namecert} supplies the common interface. Thus a boundedness-to-completion read and a completion-to-boundedness read share the same $B,F,U,C$ spine before the metric-completion row $M$ reaches the stream, regular rational, and Real-seal rows.

The excluded endpoints are absent by carrier inspection. No row stores a quotient completion, selected representative, countable-choice schedule, or ambient completeness theorem, so the completion seal is only the displayed bounded Cauchy route.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyBornologyDualityUp$ completion consumer displays the boundedness row, $\CauchyFilterUp$ row, $\UniformSpaceUp$ row, $\MetricCompletionUp$ row, $\StreamNameUp$ window row, $\RegSeqRatUp$ readback row, $\RealUp$ seal, compatibility row, transport, provenance, and local $\NameCert$ before a completion seal is exported.}

\independenceWitness{cauchy-bornological-uniformity, cauchyfilter, metriccompletion, uniformspace, streamname, regseqrat, real: $\CauchyBornologyDualityUp$ is not bijective to any listed sibling because it packages the two-way boundedness and completion readback route as one finite NameCert surface.}

\closureat{CauchyBornologyDualityUp}{seedStr}

\begin{closurestatus}{\CauchyBornologyDualityUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-bornology-duality-carrier}, \autoref{thm:cauchy-bornology-duality-namecert-obligations}, and \autoref{thm:cauchy-bornology-duality-completion-route} seed the boundedness and completion readback interface over $\CauchyBornologicalUniformityUp$, $\CauchyFilterUp$, $\UniformSpaceUp$, $\MetricCompletionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Pkg$, and $\NameCert$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close Lean verification, quotient completion, selected limits, countable choice, host filter equality, trichotomy, bridge checking, or ambient Real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, boundedness-to-completion routing, completion-to-boundedness readback, compatibility, transport, provenance, local naming, and ledger non-escape for BEDC.Derived.CauchyBornologyDualityUp.}
  \constructivestory{A $\CauchyBornologyDualityUp$ packet is generated from bornological boundedness rows, Cauchy-filter eventuality rows, uniform entourage rows, metric-completion routing, finite stream windows, regular rational readback, Real-seal tolerance, bounded-completion compatibility, componentwise transport, package provenance, and local naming evidence.}
\end{closurestatus}
