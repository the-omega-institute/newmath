\chapter{A Concrete Naming Certificate for $\FiniteWindowRealSeparationUp$}
\label{ch:concrete-instances-finite-window-real-separation-namecert}
\origin{ai}

The $\FiniteWindowRealSeparationUp$ packet realizes the Real-completeness separation surface of \autoref{ch:visions-real-completeness} as a finite NameCert object. It consumes the dyadic tolerance row of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the StreamName and RegSeqRat readback rows of \autoref{ch:concrete-instances-streamname-namecert} and \autoref{ch:concrete-instances-regseqrat-namecert}, the RealUp seal of \autoref{ch:concrete-instances-real-namecert}, the exact completion boundary of \autoref{ch:concrete-instances-real-completion-exact-boundary-namecert}, the budgeted seal route of \autoref{ch:concrete-instances-budgeted-real-seal-route-namecert}, and the shared threshold route of \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert}. Its role is to make Real separation readable only through a shared finite window, dyadic tolerance, regular-rational readback, componentwise transport, displayed replay, provenance, and local naming packet, not through quotient streams, host real equality, selected representatives, or an ambient completeness oracle.

\falsifiablePrediction{If the shared finite-window row is removed, no accepted $\FiniteWindowRealSeparationUp$ packet can expose RealUp separation readback.}

\independenceWitness{finitewindowrealsealaudit, locatedreal, realcompletionexactboundary, budgetedrealsealroute, regularcauchytailmeet: none is bijective because none simultaneously carries the shared finite-window row $W$, dyadic tolerance row $D$, StreamName and RegSeqRat readback row $S$, RealUp separation readback row $R$, componentwise transport row $H$, displayed replay row $C$, provenance row $P$, and local NameCert row $N$.}

\begin{definition}[Finite-window Real-separation carrier]
\label{def:finite-window-real-separation-carrier}
A $\FiniteWindowRealSeparationUp$ carrier is a finite $\BHist$ packet
$$
F=(W,D,S,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $W$ is the shared finite-window row naming the common observation window for the two Real-facing reads;
\item $D$ is the $\DyadicRatCoreUp$ tolerance row attached to exactly that shared window;
\item $S$ is the combined $\StreamNameUp/\RegSeqRatUp$ readback row, exposing the stream-name schedule and the regular-rational readback reached from $W$ and $D$;
\item $R$ is the $\RealUp$ separation readback row reached only after $W,D,S$ have been displayed;
\item $H$ records componentwise $\hsame$ transport for the shared window, tolerance, readback, separation, and local packet rows;
\item $C$ records the displayed $\Cont$ replay from window admission through dyadic tolerance, StreamName and RegSeqRat readback, RealUp separation readback, and local naming;
\item $P$ is the $\Pkg$ provenance over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\RealCompletionExactBoundaryUp$, $\BudgetedRealSealRouteUp$, $\RegularCauchyTailMeetUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\FiniteWindowRealSeparationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these eight rows. It has no coordinate for host real equality, quotient-stream equality, selected representatives, detached Real separation witnesses, countable choice, or ambient completeness.
\end{definition}
\leanchecked{BEDC.Derived.FiniteWindowRealSeparationUp.FiniteWindowRealSeparationCarrier\_admission}

\begin{theorem}[Shared-window refusal for Real separation]
\label{thm:finite-window-real-separation-window-refusal}
For every accepted $\FiniteWindowRealSeparationUp$ carrier
$$
F=(W,D,S,R,H,C,P,N),
$$
no consumer can expose the RealUp separation readback row $R$ without first preserving the shared finite-window row $W$, the dyadic tolerance row $D$, and the StreamName/RegSeqRat readback row $S$. Any route that asks for host real equality, quotient-stream equality, a selected representative, or a detached separation witness is outside the accepted carrier.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteWindowRealSeparationUp.FiniteWindowRealSeparation\_window\_refusal}
\begin{proof}
Project the carrier of \autoref{def:finite-window-real-separation-carrier}. The only Real-facing coordinate is $R$, and the carrier lists it after the shared window, tolerance, and readback rows.

Read the route in dependency order. The row $W$ fixes the common finite observation window, $D$ fixes the dyadic tolerance for that window, and $S$ fixes the StreamName and RegSeqRat readback that the Real-facing separation row consumes.

The remaining rows $H,C,P,N$ only transport, replay, package, and name the same displayed packet. Since no quotient stream, host equality, selected representative, or detached separation coordinate appears in the carrier, a consumer that omits $W,D,S$ has no accepted path to $R$.
\end{proof}

\begin{theorem}[Finite-window Real-separation NameCert surface]
\label{thm:finite-window-real-separation-namecert-surface}
For every accepted $\FiniteWindowRealSeparationUp$ carrier
$$
F=(W,D,S,R,H,C,P,N),
$$
the local $\NameCert_{\FiniteWindowRealSeparationUp}$ surface consists exactly of shared finite-window exposure through $W$, dyadic-tolerance exposure through $D$, StreamName and RegSeqRat readback through $S$, RealUp separation readback through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteWindowRealSeparationUp.FiniteWindowRealSeparation\_namecert\_surface}
\begin{proof}
Start with the carrier inventory. The rows $W,D,S,R$ are the full Real-separation route, while $H,C,P,N$ are the structural rows that preserve and expose that route.

Attach the Real-completion faces. The dyadic row $D$ supplies the finite tolerance, $S$ supplies the StreamName schedule and RegSeqRat readback, and $R$ supplies the RealUp separation readback under the exact-boundary, budgeted-seal, and shared-tail-meet provenance recorded by $P$.

No further row is available. Thus the local NameCert surface is exhausted by $W,D,S,R,H,C,P,N$, and any read requiring host equality, quotient streams, selected representatives, or ambient completeness is not a read of this packet.
\end{proof}

\begin{theorem}[Finite-window Real-separation obligation surface]
\label{thm:finite-window-real-separation-obligation-surface}
For every accepted $\FiniteWindowRealSeparationUp$ carrier
$$
F=(W,D,S,R,H,C,P,N),
$$
the RealUp separation obligation surface is exposed only through the common finite window $W$, the dyadic tolerance row $D$, the StreamName/RegSeqRat readback row $S$, the RealUp readback row $R$, and the structural rows $H,C,P,N$. No accepted separation consumer can replace these rows by quotient streams, host equality of reals, selected representatives, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteWindowRealSeparationUp.FiniteWindowRealSeparation\_obligation\_surface}
\begin{proof}
Project the carrier of \autoref{def:finite-window-real-separation-carrier}. Its only Real-separation coordinates are $W,D,S,R$, followed by the structural rows $H,C,P,N$.

Apply the NameCert surface of \autoref{thm:finite-window-real-separation-namecert-surface}. It lists the same eight rows as the whole local surface, so no further obligation coordinate is available to a consumer.

Now use the shared-window obligation of \autoref{thm:finite-window-real-separation-shared-window-obligation}. The row $W$ is common to the two Real-facing reads, $D$ fixes the dyadic tolerance for that window, $S$ supplies the StreamName and RegSeqRat readback, and $R$ is the RealUp readback reached only after those rows are displayed.

The refusal clauses follow from absence of carrier coordinates. Quotient streams, host real equality, selected representatives, and ambient completeness are not fields of $F$, and the structural rows $H,C,P,N$ only transport, replay, source, and name the same finite surface.
\end{proof}

\begin{theorem}[Finite-window Real-separation shared-window obligation]
\label{thm:finite-window-real-separation-shared-window-obligation}
Every separation read exported by an accepted $\FiniteWindowRealSeparationUp$ carrier
$$
F=(W,D,S,R,H,C,P,N)
$$
must display one common finite window row $W$ before the dyadic tolerance row $D$, the $\StreamNameUp/\RegSeqRatUp$ readback row $S$, the $\RealUp$ seal row $R$, componentwise transport $H$, displayed replay $C$, provenance $P$, and the local $\NameCert_{\FiniteWindowRealSeparationUp}$ row $N$ are consumed. The obligation is constructive: it supplies no quotient stream equality, no host Real equality, no selected representative, and no ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteWindowRealSeparationUp.FiniteWindowRealSeparation\_shared\_window\_obligation}
\begin{proof}
Project the carrier of \autoref{def:finite-window-real-separation-carrier}. The row $W$ is the only coordinate that names the common observation window for the two Real-facing reads.

Apply the shared-window refusal of \autoref{thm:finite-window-real-separation-window-refusal}. It blocks access to the RealUp separation readback $R$ unless $W,D,S$ have already been displayed in that order.

Now use the NameCert surface of \autoref{thm:finite-window-real-separation-namecert-surface}. The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route; they do not add a second window, a quotient equality, a host representative, or an ambient completeness object.

Thus every separation read has to expose the common finite window before any dyadic, StreamName, RegSeqRat, Real-seal, transport, replay, provenance, or local naming row is consumed.
\end{proof}

\begin{theorem}[Finite-window Real-separation TasteGate obligation]
\label{thm:finite-window-real-separation-tastegate-obligation}
The Lean TasteGate surface for $\FiniteWindowRealSeparationUp$ must provide $\mathsf{Nontrivial}\ \FiniteWindowRealSeparationUp$ and $\mathsf{FieldFaithful}\ \FiniteWindowRealSeparationUp$ instances for the same finite carrier $F=(W,D,S,R,H,C,P,N)$. These instances must keep RealUp separation readback behind the displayed dyadic tolerance and StreamName/RegSeqRat rows and must not introduce host equality, quotient streams, selected representatives, or ambient completeness.
\end{theorem}
\begin{proof}
The obligation is read from \autoref{def:finite-window-real-separation-carrier}. Nontriviality must inhabit the displayed finite packet rather than a detached Real value, because the only admissible carrier is $F=(W,D,S,R,H,C,P,N)$.

Field faithfulness must preserve the same route. By \autoref{thm:finite-window-real-separation-window-refusal}, the separation row $R$ is not public until $W,D,S$ have been displayed, so any TasteGate classifier faithful to the chapter must transport Real separation only through those finite rows.

The excluded coordinates are absent from the carrier and from the NameCert surface of \autoref{thm:finite-window-real-separation-namecert-surface}. The Lean file \texttt{TasteGate.lean} therefore has to expose the instances for the finite route, not for host equality, quotient streams, selected representatives, or ambient completeness.
\end{proof}
\leanchecked{BEDC.Derived.FiniteWindowRealSeparationUp.FiniteWindowRealSeparationTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Finite-window Real-separation obligation closure route]
\label{thm:finite-window-real-separation-obligation-closure-route}
For every accepted $\FiniteWindowRealSeparationUp$ carrier
$$
F=(W,D,S,R,H,C,P,N),
$$
the full obligation route is exposed through the shared finite-window row $W$, dyadic tolerance row $D$, StreamName and RegSeqRat readback row $S$, RealUp separation readback row $R$, componentwise transport $H$, displayed replay $C$, provenance $P$, and local naming $N$ before any Real separation read is consumed. No accepted route may read $R$ before the packet has displayed $W,D,S,H,C,P,N$ as the finite support of that read.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:finite-window-real-separation-carrier}. It lists exactly the rows $W,D,S,R,H,C,P,N$, so every accepted Real-separation consumer is already bounded by those displayed coordinates.

Apply the shared-window refusal of \autoref{thm:finite-window-real-separation-window-refusal}. It prevents access to the RealUp row $R$ until the common finite window $W$, dyadic tolerance $D$, and StreamName/RegSeqRat readback $S$ have been displayed.

Use the NameCert surface and shared-window obligation, \autoref{thm:finite-window-real-separation-namecert-surface} and \autoref{thm:finite-window-real-separation-shared-window-obligation}. These bind the structural rows $H,C,P,N$ to the same finite packet, so transport, replay, provenance, and local naming cannot escape the displayed route.

Finally apply the TasteGate obligation of \autoref{thm:finite-window-real-separation-tastegate-obligation}. It forces any Lean-side gate for the packet to preserve the same finite carrier and refuses host equality, quotient streams, selected representatives, and ambient completeness before the Real separation read.
\end{proof}
\leanchecked{BEDC.Derived.FiniteWindowRealSeparationUp.FiniteWindowRealSeparation\_obligation\_closure\_route}

\begin{theorem}[Finite-window Real-separation window-budget determinacy]
\label{thm:finite-window-real-separation-window-budget-determinacy}
Let
$$
F=(W,D,S,R,H,C,P,N)
\qquad\text{and}\qquad
F'=(W,D,S,R',H,C',P',N')
$$
be accepted $\FiniteWindowRealSeparationUp$ packets with the same shared finite-window row $W$, the same dyadic tolerance row $D$, the same $\StreamNameUp/\RegSeqRatUp$ readback row $S$, and the same componentwise $\hsame$ transport row $H$. Then $F$ and $F'$ expose the same admissible $\RealUp$ separation readback budget: any consumer of $R$ or $R'$ must pass through the identical finite window $W$, dyadic budget $D$, readback row $S$, and transport row $H$ before it can read a Real-separation handoff. The packet has no second budget coordinate, detached Real witness, quotient stream equality, selected representative, host Real equality, or ambient completeness row from which an alternate readback budget could be obtained.
\end{theorem}
\begin{proof}
Project the two eight-row carriers from \autoref{def:finite-window-real-separation-carrier}. The only rows that can govern a Real-separation budget are the shared finite window $W$, dyadic tolerance $D$, StreamName/RegSeqRat readback $S$, Real readback row, transport $H$, replay, provenance, and local naming.

Apply the refusal, NameCert surface, and shared-window obligation of \autoref{thm:finite-window-real-separation-window-refusal}, \autoref{thm:finite-window-real-separation-namecert-surface}, and \autoref{thm:finite-window-real-separation-shared-window-obligation}. They force every admissible Real-separation read to enter through $W,D,S$ and then preserve the same route through $H$ before the Real-facing row is public.

Finally use the obligation closure route \autoref{thm:finite-window-real-separation-obligation-closure-route}. Since the accepted packets agree on $W,D,S,H$, the admissible budget visible to any consumer is the same finite dependency route. The remaining rows $C,P,N$ and $C',P',N'$ replay, source, and name that route, while the excluded coordinates are not fields of the carrier.
\end{proof}
\leanchecked{BEDC.Derived.FiniteWindowRealSeparationUp.FiniteWindowRealSeparation\_window\_budget\_determinacy}

\begin{theorem}[Finite-window Real-separation scoped kernel route]
\label{thm:finite-window-real-separation-scoped-kernel-route}
For every accepted $\FiniteWindowRealSeparationUp$ carrier
$$
F=(W,D,S,R,H,C,P,N),
$$
the scoped kernel route is exactly the finite route over the $\DyadicRatCoreUp$ tolerance row, $\StreamNameUp$ window row, $\RegSeqRatUp$ readback row, $\RealUp$ separation readback row, $\RealCompletionExactBoundaryUp$, $\BudgetedRealSealRouteUp$, $\RegularCauchyTailMeetUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. The carrier exposes no route through quotient streams, host real equality, selected representatives, countable choice, detached separation witnesses, or ambient Real completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-window-real-separation-carrier}. The rows $W,D,S,R,H,C,P,N$ are the only rows available to a consumer, and the provenance row $P$ names the exact-boundary, budgeted-seal, and shared-tail-meet sources of the Real-facing read.

Apply shared-window refusal and the NameCert surface, \autoref{thm:finite-window-real-separation-window-refusal} and \autoref{thm:finite-window-real-separation-namecert-surface}. They force the RealUp separation row to be read only after the shared finite window, dyadic tolerance, and StreamName/RegSeqRat readback rows have been displayed, while $H,C,P,N$ preserve and name the same route.

Use the shared-window obligation and obligation closure route, \autoref{thm:finite-window-real-separation-shared-window-obligation} and \autoref{thm:finite-window-real-separation-obligation-closure-route}. These bind the structural rows to the displayed kernel dependencies and reject every outside coordinate before the Real-separation read is consumed.

Finally apply window-budget determinacy, \autoref{thm:finite-window-real-separation-window-budget-determinacy}. Since the visible budget is determined by the same $W,D,S,H$ route, the scoped kernel surface is exactly the listed dyadic, stream, regular-rational, Real, completion-boundary, seal-route, tail-meet, history, transport, continuation, provenance, and local naming inventory.
\end{proof}
\leanchecked{BEDC.Derived.FiniteWindowRealSeparationUp.FiniteWindowRealSeparation\_scoped\_kernel\_route}

\begin{theorem}[Finite-window Real-separation witness exhaustion]
\label{thm:finite-window-real-separation-witness-exhaustion}
Every Real-separation witness accepted by a $\FiniteWindowRealSeparationUp$ carrier is exhausted by the rows
$$
W,D,S,R,H,C,P,N .
$$
No detached Real separation witness can be consumed, and no witness route may replace the shared finite window, dyadic tolerance, StreamName and RegSeqRat readback, Real readback, transport, replay, provenance, and local naming rows by quotient streams, host equality, selected representatives, or ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-window-real-separation-carrier}. The witness-facing coordinates are exactly $W,D,S,R,H,C,P,N$.

Use shared-window refusal, the NameCert surface, and the shared-window obligation from \autoref{thm:finite-window-real-separation-window-refusal}, \autoref{thm:finite-window-real-separation-namecert-surface}, and \autoref{thm:finite-window-real-separation-shared-window-obligation}. They force every Real-separation read to display the common finite window, tolerance, and readback rows before the Real-facing row is consumed.

Finally apply the obligation-closure route of \autoref{thm:finite-window-real-separation-obligation-closure-route}. The structural rows preserve the same finite packet, and the carrier contains no quotient-stream, host-equality, selected-representative, or ambient-completeness coordinate.
\end{proof}
\leanchecked{BEDC.Derived.FiniteWindowRealSeparationUp.FiniteWindowRealSeparation\_witness\_exhaustion}

\begin{theorem}[Finite-window Real-separation scoped completion route]
\label{thm:finite-window-real-separation-scoped-completion-route}
Every accepted $\FiniteWindowRealSeparationUp$ separation read that reaches a completion-facing consumer factors through the finite route
$$
W \longmapsto D \longmapsto S \longmapsto R \longmapsto
\RealCompletionExactBoundaryUp \longmapsto
\BudgetedRealSealRouteUp \longmapsto
\RegularCauchyTailMeetUp \longmapsto H \longmapsto C \longmapsto P \longmapsto N .
$$
The route first displays the common finite window $W$, dyadic tolerance $D$, $\StreamNameUp/\RegSeqRatUp$ readback $S$, and $\RealUp$ seal $R$; it then reads exact-boundary, budgeted-seal, and tail-meet rows only through the provenance already carried by $P$. It exposes no quotient stream, host real equality, selected representative, detached completion witness, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-window-real-separation-carrier}. The only separation-facing rows are $W,D,S,R,H,C,P,N$, and the provenance row names the exact-boundary, budgeted-seal, and shared-tail-meet siblings consumed by the Real-facing route.

Apply the shared-window refusal and NameCert surface theorems, \autoref{thm:finite-window-real-separation-window-refusal} and \autoref{thm:finite-window-real-separation-namecert-surface}. They force any RealUp read to pass through the same finite window, dyadic tolerance, and StreamName/RegSeqRat readback before $R$ becomes public.

Use the shared-window obligation and obligation-closure route, \autoref{thm:finite-window-real-separation-shared-window-obligation} and \autoref{thm:finite-window-real-separation-obligation-closure-route}. These bind $H,C,P,N$ to the same displayed packet, so completion-facing consumers cannot detach the exact-boundary or seal rows from the carrier.

Finally apply the scoped kernel route and witness exhaustion theorems, \autoref{thm:finite-window-real-separation-scoped-kernel-route} and \autoref{thm:finite-window-real-separation-witness-exhaustion}. The scoped route already lists the dyadic, stream, regular-rational, Real, exact-boundary, budgeted-seal, and tail-meet dependencies, while witness exhaustion blocks every outside completion witness.
\end{proof}
\leanchecked{BEDC.Derived.FiniteWindowRealSeparationUp.FiniteWindowRealSeparation\_scoped\_completion\_route}

\begin{theorem}[Finite-window Real-separation shared-budget normalization]
\label{thm:finite-window-real-separation-shared-budget-normalization}
Let
$$
F=(W,D,S,R,H,C,P,N)
\qquad\text{and}\qquad
F'=(W,D,S,R',H,C',P',N')
$$
be accepted $\FiniteWindowRealSeparationUp$ packets with the same shared finite-window row $W$ and the same dyadic tolerance row $D$. Any $\RealUp$ separation read exposed by either packet factors through one normalized finite budget route
$$
W,D,S,R,H,C,P,N
$$
after replacing the replay, provenance, and local naming rows of the second packet by the displayed route for the same window, tolerance, readback, and transport budget. No normalized route may use quotient streams, selected representatives, or ambient completeness.
\end{theorem}
\begin{proof}
Start with the shared-window refusal of \autoref{thm:finite-window-real-separation-window-refusal}. It blocks any RealUp read until the common window row $W$, dyadic tolerance row $D$, and StreamName/RegSeqRat readback row $S$ have been displayed.

Apply the NameCert surface and shared-window obligation of \autoref{thm:finite-window-real-separation-namecert-surface} and \autoref{thm:finite-window-real-separation-shared-window-obligation}. These theorems bind the readback row, transport row, replay, provenance, and local name to the same finite packet once $W$ and $D$ are fixed.

Use the window-budget determinacy theorem \autoref{thm:finite-window-real-separation-window-budget-determinacy}. Agreement on the shared finite window and dyadic tolerance forces the visible Real-separation budget through the same finite route, so the normalized read can be recorded as $W,D,S,R,H,C,P,N$.

The refusal clauses are structural. Quotient streams, selected representatives, and ambient completeness are not fields of the accepted carrier, and replay or provenance cannot create a second budget outside the displayed finite route.
\end{proof}
\leanchecked{BEDC.Derived.FiniteWindowRealSeparationUp.FiniteWindowRealSeparation\_shared\_budget\_normalization}

\begin{theorem}[Finite-window Real-separation common-window exactness]
\label{thm:finite-window-real-separation-common-window-exactness}
Let
$$
F=(W,D,S,R,H,C,P,N)
\qquad\text{and}\qquad
F'=(W,D,S,R',H',C',P',N')
$$
be accepted $\FiniteWindowRealSeparationUp$ packets with the same shared finite-window row $W$, the same dyadic tolerance row $D$, and the same $\StreamNameUp/\RegSeqRatUp$ readback row $S$. Every admissible $\RealUp$ separation read exposed by either packet factors through the same $W,D,S$ route before reading $R$ or $R'$. Disagreement in replay, provenance, local naming, or the remaining transport rows cannot create a second separation budget outside that common finite window.
\end{theorem}
\begin{proof}
Start with the shared-window refusal of \autoref{thm:finite-window-real-separation-window-refusal}. It blocks every RealUp separation read until the common finite window $W$, dyadic tolerance $D$, and StreamName/RegSeqRat readback row $S$ have been displayed.

Use the NameCert surface and shared-window obligation of \autoref{thm:finite-window-real-separation-namecert-surface} and \autoref{thm:finite-window-real-separation-shared-window-obligation}. These bind the Real-facing row, transport, replay, provenance, and local name to the finite route whose first three rows are $W,D,S$.

Apply window-budget determinacy and shared-budget normalization, \autoref{thm:finite-window-real-separation-window-budget-determinacy} and \autoref{thm:finite-window-real-separation-shared-budget-normalization}. Once the two packets agree on $W,D,S$, any admissible read of $R$ or $R'$ has the same finite budget prefix, while $H,C,P,N$ and $H',C',P',N'$ only preserve, replay, source, and name that displayed route.

The refusal clause follows from the carrier inventory. The packet has no coordinate for a detached budget, quotient stream equality, selected representative, host Real equality, or ambient completeness, so disagreement in replay, provenance, or local naming cannot supply another route to a RealUp separation read.
\end{proof}
\leanchecked{BEDC.Derived.FiniteWindowRealSeparationUp.FiniteWindowRealSeparation\_common\_window\_exactness}

\begin{theorem}[Finite-window Real-separation terminal readback exclusion]
\label{thm:finitewindowrealseparation-terminal-readback-exclusion}
For every accepted $\FiniteWindowRealSeparationUp$ carrier
$$
F=(W,D,S,R,H,C,P,N),
$$
any terminal $\RealUp$ readback exposed by the packet remains downstream of the shared finite window $W$, dyadic tolerance row $D$, $\StreamNameUp/\RegSeqRatUp$ readback row $S$, componentwise transport $H$, displayed replay $C$, provenance $P$, and local $\NameCert_{\FiniteWindowRealSeparationUp}$ row $N$. The terminal readback cannot be replaced by host equality, quotient streams, selected representatives, detached Real witnesses, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteWindowRealSeparationUp.FiniteWindowRealSeparation\_terminal\_readback\_exclusion}
\begin{proof}
Start with the shared-window refusal of \autoref{thm:finite-window-real-separation-window-refusal}. It blocks access to the $\RealUp$ row unless the common finite window, dyadic tolerance, and StreamName/RegSeqRat readback rows have already been displayed.

Apply the NameCert surface of \autoref{thm:finite-window-real-separation-namecert-surface}. The structural rows $H,C,P,N$ transport, replay, source, and name the same eight-row carrier, so a terminal readback has no route outside the displayed packet.

Use the scoped completion route of \autoref{thm:finite-window-real-separation-scoped-completion-route}. Completion-facing consumers see the Real-facing row only after the finite route through $W,D,S,R,H,C,P,N$ and the listed exact-boundary, budgeted-seal, and shared-tail-meet provenance rows.

Finally combine shared-budget normalization and common-window exactness, \autoref{thm:finite-window-real-separation-shared-budget-normalization} and \autoref{thm:finite-window-real-separation-common-window-exactness}. These theorems keep every admissible terminal readback attached to the same finite window and tolerance budget, while the carrier has no coordinate for host equality, quotient streams, selected representatives, detached Real witnesses, or ambient completeness.
\end{proof}

\begin{theorem}[Finite-window Real-separation public route]
\label{thm:finite-window-real-separation-public-route}
Every public $\RealUp$, $\RegSeqRatUp$, or $\StreamNameUp$ consumer of a $\FiniteWindowRealSeparationUp$ separation window factors through the displayed finite route
$$
W \longmapsto D \longmapsto S \longmapsto R \longmapsto H \longmapsto C \longmapsto P \longmapsto N .
$$
The consumer reads the shared-window budget, dyadic tolerance, StreamName and RegSeqRat readback, refusal boundary, and terminal Real readback only through the rows already established by the carrier admission, window refusal, obligation surface, shared-window obligation, scoped kernel route, shared-budget normalization, common-window exactness, and terminal-readback exclusion results. It receives no quotient stream, host Real equality, selected representative, detached Real witness, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteWindowRealSeparationUp.FiniteWindowRealSeparation\_public\_route}
\begin{proof}
Project the accepted carrier $F=(W,D,S,R,H,C,P,N)$ from \autoref{def:finite-window-real-separation-carrier}. The public route starts with the same shared finite-window row $W$, dyadic tolerance row $D$, and StreamName/RegSeqRat readback row $S$ that the carrier admission exposes.

Apply the window refusal, obligation surface, and shared-window obligation results: \autoref{thm:finite-window-real-separation-window-refusal}, \autoref{thm:finite-window-real-separation-obligation-surface}, and \autoref{thm:finite-window-real-separation-shared-window-obligation}. Together they force every admissible $\RealUp$ separation read to pass through $W,D,S$ before the Real-facing row $R$ and the structural rows $H,C,P,N$ become public.

Use the scoped kernel route, shared-budget normalization, and common-window exactness of \autoref{thm:finite-window-real-separation-scoped-kernel-route}, \autoref{thm:finite-window-real-separation-shared-budget-normalization}, and \autoref{thm:finite-window-real-separation-common-window-exactness}. These results identify the same finite budget for every $\RegSeqRatUp$ or $\StreamNameUp$ read that supports the separation window, so the public consumer cannot choose a second readback budget.

Finally apply terminal-readback exclusion from \autoref{thm:finitewindowrealseparation-terminal-readback-exclusion}. The terminal $\RealUp$ row remains downstream of the displayed window, tolerance, readback, transport, replay, provenance, and local naming rows, while quotient streams, host Real equality, selected representatives, detached witnesses, and ambient completeness have no coordinate in the packet.
\end{proof}

\closureat{FiniteWindowRealSeparationUp}{obligationStr}
\closureat{FiniteWindowRealSeparationUp}{scopedStr}

\begin{closurestatus}{\FiniteWindowRealSeparationUp}
  \constructivestory{A $\FiniteWindowRealSeparationUp$ packet is generated from a shared finite-window row, a DyadicRatCore tolerance row, StreamName and RegSeqRat readback, RealUp separation readback, componentwise hsame transport, displayed Cont replay, package provenance, and a local NameCert row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:finite-window-real-separation-carrier}, \autoref{thm:finite-window-real-separation-obligation-surface}, \autoref{thm:finite-window-real-separation-shared-window-obligation}, \autoref{thm:finite-window-real-separation-tastegate-obligation}, \autoref{thm:finite-window-real-separation-scoped-kernel-route}, \autoref{thm:finite-window-real-separation-witness-exhaustion}, \autoref{thm:finite-window-real-separation-shared-budget-normalization}, and \autoref{thm:finite-window-real-separation-common-window-exactness} bind the finite Real-separation carrier, obligation surface, shared-window obligation, TasteGate obligation, scoped kernel route, witness exhaustion, shared-budget normalization, and common-window exactness rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close Lean verification, bridge checking, quotient reals, host real equality, selected representatives, or ambient Real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission and TasteGate-visible Nontrivial and FieldFaithful instances for the same finite Real-separation packet.}
\end{closurestatus}
