\chapter{A Concrete Naming Certificate for $\AxisZeckendorfCarryBudgetUp$}
\label{ch:concrete-instances-axiszeckendorfcarrybudget-namecert}
\origin{human}

The $\AxisZeckendorfCarryBudgetUp$ interface packages the finite carry-window budget behind the displayed AxisZeckendorf carry classifier. It records the carry source, carry target, normal-form target, Fibonacci value-budget ledger, non-$\hsame$ boundary, componentwise transports, displayed continuation routes, package provenance, and a local naming row. The packet is a finite certificate for the carry-budget surface; it is not history sameness, a kernel rewrite, a host arithmetic quotient, or a global normalization algorithm.

The budget carrier consumes the Zeckendorf normal-form rows of \autoref{ch:concrete-instances-zeckendorf-normal-namecert}, the local carry row of \autoref{ch:concrete-instances-zcarry-namecert}, and the classifier boundary of \autoref{ch:concrete-instances-zeckendorf-carry-classifier-namecert} before adding the value-budget ledger and non-$\hsame$ boundary. Thus the packet sits on the concrete carry and normal-form lattice rather than on a kernel-only naming surface.

\begin{definition}[AxisZeckendorf carry-budget carrier]
\label{def:axis-zeckendorf-carry-budget-carrier}
An $\AxisZeckendorfCarryBudgetUp$ carrier is a finite $\BHist$ packet
$$
B=(h,k,G,N_k,V,Q,H,C,P,N)
$$
with the following visible coordinates:
\begin{enumerate}
\item $h$ is the displayed carry-source history row carrying the public window $\mathtt{word\_011}$;
\item $k$ is the displayed carry-target history row carrying the public window $\mathtt{word\_100}$;
\item $G$ is the local $\ZCarryUp$ classifier row reading $\mathsf{ZCarry}(h,k)$ through the displayed carry generator;
\item $N_k$ is the normal-form target row stating that $k$ is admitted by the $\ZeckendorfNormalUp$ normal-form classifier;
\item $V$ is the value-budget ledger row recording the finite Fibonacci value read for $h$, the finite Fibonacci value read for $k$, and the equality of those two displayed sums;
\item $Q$ is the non-$\hsame$ boundary row recording that the carry relation between $h$ and $k$ is not a history-sameness witness;
\item $H$ stores the componentwise $\hsame$ transports that preserve the source row, target row, carry row, normal-form target row, value-budget ledger, boundary row, route row, provenance row, and local naming row;
\item $C$ stores the displayed $\Cont$ routes for carry-source exposure, carry-target exposure, normal-form target reading, value-budget reading, non-$\hsame$ boundary reading, provenance reading, and local naming;
\item $P$ is the $\Pkg$ provenance over $\BHist$, $\ZCarryUp$, $\ZeckendorfNormalUp$, Fibonacci value-budget rows, $\hsame$, $\Cont$, and local $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\AxisZeckendorfCarryBudgetUp}$ row.
\end{enumerate}
The carrier compares accepted packets only through the displayed rows $h,k,G,N_k,V,Q,H,C,P,N$. It exports no source-target $\hsame$ proof, no kernel rewrite, no hidden arithmetic quotient, and no unbounded normalization process.
\end{definition}

\begin{theorem}[AxisZeckendorf carry-budget NameCert obligations]
\label{thm:axis-zeckendorf-carry-budget-namecert-obligations}
The local $\NameCert_{\AxisZeckendorfCarryBudgetUp}$ surface for \autoref{def:axis-zeckendorf-carry-budget-carrier} consists of five finite obligations:
\begin{enumerate}
\item carrier admission for the finite packet $B=(h,k,G,N_k,V,Q,H,C,P,N)$;
\item carry-window coverage, stating that the only admitted carry source and target windows are the displayed $\mathtt{word\_011}$ and $\mathtt{word\_100}$ rows read through $G$;
\item value-budget preservation, stating that every public value-preservation read factors through the finite ledger $V$;
\item non-$\hsame$ boundary, stating that $Q$ keeps carry-relatedness separate from $\hsame(h,k)$ under all displayed transports;
\item ledger non-escape, stating that the rows $H,C,P,N$ route and name exactly this packet and do not export a kernel rewrite, a host arithmetic quotient, or a global normalizer.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.AxisZeckendorfCarryBudgetUp.AxisZeckendorfCarryBudgetNamecertObligations}
\begin{proof}
Carrier admission is the tuple in \autoref{def:axis-zeckendorf-carry-budget-carrier}. It lists the carry source, carry target, carry classifier, normal-form target, value-budget ledger, non-$\hsame$ boundary, transports, routes, provenance, and local naming row inside one finite $\BHist$ packet.

Carry-window coverage is read from $h,k,G,N_k$. The carry row $G$ fixes the public source and target windows, and the normal-form target row $N_k$ records that the target side is the admitted normal-form window.

Value-budget preservation is the role of $V$. Every public value read of the carry step reaches the two displayed Fibonacci sums and their equality only through that finite ledger coordinate.

The boundary and non-escape clauses are the rows $Q,H,C,P,N$. The row $Q$ records that the carry is not $\hsame(h,k)$, while $H$ transports only the displayed components. The route, provenance, and naming rows repack the same finite surface, so no excluded object is available to a consumer.
\end{proof}

\begin{theorem}[AxisZeckendorf carry-budget classifier realization]
\label{thm:axis-zeckendorf-carry-budget-classifier-realization}
Every consumer-visible carry-budget read of an accepted $\AxisZeckendorfCarryBudgetUp$ carrier factors through
$$
h,k,G,N_k,V,Q,H,C,P,N.
$$
Thus the carry classifier can expose finite carry-window coverage, normal-form target reading, value-ledger preservation, non-$\hsame$ separation, transport stability, route replay, provenance, and local naming without treating carry-relatedness as history sameness.
\end{theorem}
\begin{proof}
Start with the finite carrier of \autoref{def:axis-zeckendorf-carry-budget-carrier}. The rows $h,k,G,N_k$ expose exactly the displayed carry source, carry target, carry witness, and normal-form target.

Add the budget and boundary rows. The row $V$ carries the finite value-preservation ledger, while $Q$ keeps the non-$\hsame$ boundary visible for the same source and target.

Finally apply the structural rows $H,C,P,N$. They transport, route, package, and name only the displayed packet. Therefore every consumer-facing carry-budget read remains inside the listed rows and cannot become a source-target $\hsame$ proof or a kernel rewrite.
\end{proof}

\closureat{AxisZeckendorfCarryBudgetUp}{seedStr}

\begin{closurestatus}{\AxisZeckendorfCarryBudgetUp}
  \constructivestory{An $\AxisZeckendorfCarryBudgetUp$ packet is generated from the displayed carry source, carry target, normal-form target row, finite Fibonacci value-budget ledger, non-$\hsame$ boundary, componentwise transports, displayed continuation routes, package provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:axis-zeckendorf-carry-budget-carrier}, \autoref{thm:axis-zeckendorf-carry-budget-namecert-obligations}, and \autoref{thm:axis-zeckendorf-carry-budget-classifier-realization} bind the finite carry-budget carrier, value ledger, non-$\hsame$ boundary, route, provenance, and local naming surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close Lean verification, a bridge certificate, a global Zeckendorf normalizer, a kernel rewrite, host arithmetic quotienting, or source-target $\hsame$.}
  \upgradepath{Obligation closure requires checked carrier admission, carry-window coverage, value-budget preservation, non-$\hsame$ boundary preservation, route stability, provenance, local naming, and ledger non-escape for the same finite packet.}
\end{closurestatus}
