\chapter{A Concrete Naming Certificate for $\RegularCauchyMidpointUp$}
\label{ch:concrete-instances-regular-cauchy-midpoint-namecert}
\origin{human}

Following Bishop and Bridges, the $\RegularCauchyMidpointUp$ packet names midpoint formation for two regular rational Cauchy names by finite dyadic arithmetic and displayed modulus rows before the completed $\RealUp$ seal is read. It sits over the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the stream-window schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the pointwise midpoint ledger of \autoref{ch:concrete-instances-dyadicmidpoint-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet supplies the finite midpoint surface needed by interval bisection and Real-facing finite-window algebra; it does not import host real arithmetic, quotient streams, selected limits, trichotomy, or ambient completeness.

\begin{definition}[Regular Cauchy midpoint carrier]
\label{def:regular-cauchy-midpoint-carrier}
A $\RegularCauchyMidpointUp$ carrier is a finite $\BHist$ packet
$$
M_{\mathrm{mid}}=(R_0,R_1,S_0,S_1,D,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R_0$ is the left $\RegSeqRatUp$ regular rational Cauchy name row.
\item $R_1$ is the right $\RegSeqRatUp$ regular rational Cauchy name row.
\item $S_0$ is the $\StreamNameUp$ finite-window schedule for the left name, recording exactly which rational approximants are inspected for each requested midpoint precision.
\item $S_1$ is the matching $\StreamNameUp$ finite-window schedule for the right name, synchronized with $S_0$ at the common precision ledger.
\item $D$ is the $\DyadicMidpointUp$ pointwise midpoint row, reading the two displayed rational approximants through the dyadic endpoint and common-denominator midpoint calculation.
\item $Q$ is the $\DyadicRatCoreUp$ tolerance ledger recording the dyadic radius, common denominator budget, and regularity allowance used to certify that the pointwise midpoint row is again regular Cauchy.
\item $E$ is the $\RealUp$ seal row reached only after the two regular names, synchronized schedules, dyadic midpoint row, and tolerance ledger have been displayed.
\item $H$ records componentwise $\hsame$ transport for the two regular rows, two stream schedules, pointwise midpoint row, tolerance ledger, Real seal row, replay row, provenance row, and local naming row.
\item $C$ records displayed $\Cont$ routes from every midpoint-facing read through $R_0,R_1,S_0,S_1,D,Q,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyMidpointUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicMidpointUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyMidpointUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $R_0,R_1,S_0,S_1,D,Q,E,H,C,P,N$. The carrier has no coordinate for quotient stream equality, host real midpoint arithmetic, selected limits, trichotomy, ambient completeness, or a detached completed-real midpoint.
\end{definition}
\leanchecked{BEDC.Derived.RegularCauchyMidpointUp.RegularCauchyMidpointTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Regular Cauchy midpoint NameCert obligations]
\label{thm:regular-cauchy-midpoint-namecert-obligations}
For every accepted $\RegularCauchyMidpointUp$ carrier
$$
M_{\mathrm{mid}}=(R_0,R_1,S_0,S_1,D,Q,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyMidpointUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the two $\RegSeqRatUp$ source rows $R_0,R_1$, the synchronized $\StreamNameUp$ schedules $S_0,S_1$, the $\DyadicMidpointUp$ pointwise midpoint row $D$, the $\DyadicRatCoreUp$ tolerance ledger $Q$, the $\RealUp$ seal row $E$, and the structural rows $H,C,P,N$;
\item synchronized-window exposure, requiring every midpoint-facing read to inspect the two stream schedules at a common finite precision before the dyadic midpoint row is read;
\item pointwise midpoint exactness, requiring $D$ to be obtained through the displayed $\DyadicMidpointUp$ common-denominator calculation on the two rational approximants exposed by $S_0$ and $S_1$;
\item regularity tolerance accounting, requiring $Q$ to record the dyadic radius and the allowance that turns the two input regularity rows into the output regularity row;
\item Real seal boundary, requiring $E$ to be consumed only after the route through $R_0,R_1,S_0,S_1,D,Q$ has been displayed;
\item transport and replay stability, requiring $H$ and $C$ to preserve the same two regular names, synchronized schedules, midpoint row, tolerance ledger, Real seal boundary, provenance, and local naming row;
\item package provenance and local naming, requiring $P$ and $N$ to name exactly the listed sibling certificates and the same finite midpoint packet;
\item refusal of quotient streams, host real midpoint arithmetic, selected limits, trichotomy, and ambient completeness as NameCert data.
\end{enumerate}
No NameCert obligation is stored outside these finite rows.
\end{theorem}
\begin{proof}
Project the accepted carrier. The analytic coordinates are the two regular rational names $R_0,R_1$, the synchronized schedules $S_0,S_1$, the dyadic midpoint row $D$, the tolerance ledger $Q$, and the Real seal row $E$; the remaining rows transport, replay, source, and name the same finite packet.

Read the midpoint route in dependency order. A precision request first opens the paired finite schedules $S_0$ and $S_1$ from the two $\RegSeqRatUp$ rows. The displayed rational approximants then enter the $\DyadicMidpointUp$ row $D$, where the common-denominator midpoint calculation is the only pointwise midpoint source.

The regularity ledger is the next step. The row $Q$ records the dyadic radius and the allowance inherited from the two input regular Cauchy rows, so the midpoint readback remains a regular Cauchy name before the $\RealUp$ seal is reached.

Finally apply componentwise transport and replay. The rows $H$ and $C$ preserve the same displayed route, while $P$ and $N$ name exactly the sibling certificate dependencies. Since no carrier coordinate stores quotient stream equality, host real arithmetic, selected limits, trichotomy, or ambient completeness, none of those principles can be exported by the local NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy midpoint Real seal handoff]
\label{thm:regular-cauchy-midpoint-real-seal-handoff}
Every accepted $\RegularCauchyMidpointUp$ carrier factors its Real-facing midpoint read through the finite route
$$
(R_0,R_1)\longmapsto(S_0,S_1)\longmapsto D\longmapsto Q\longmapsto E
$$
before it may hand the result to a $\RealUp$ consumer. Thus the consumer receives two regular rational source rows, synchronized finite schedules, a pointwise $\DyadicMidpointUp$ row, a $\DyadicRatCoreUp$ tolerance ledger, and a Real seal row; it receives no quotient stream equality, host real midpoint operation, selected limit, trichotomy, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Start with an accepted carrier $M_{\mathrm{mid}}=(R_0,R_1,S_0,S_1,D,Q,E,H,C,P,N)$. The only source rows for the midpoint read are the two regular rational names $R_0$ and $R_1$, and the only observation schedules attached to them are $S_0$ and $S_1$.

The pointwise midpoint then passes through $D$. That row consumes the displayed rational approximants through the same finite common-denominator midpoint calculation named by \autoref{ch:concrete-instances-dyadicmidpoint-namecert}; no second midpoint row is present.

The tolerance and seal rows finish the handoff. The row $Q$ records the dyadic budget that witnesses regularity of the midpoint name, and $E$ is reached only after this finite ledger has been displayed.

Replay the route through $C$ and transport it through $H$. The provenance row $P$ points to \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-dyadicmidpoint-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. Together with $N$, it packages the same finite route and adds no coordinate for quotient stream equality, host arithmetic, selected limits, trichotomy, or completeness.
\end{proof}

\begin{closurestatus}{\RegularCauchyMidpointUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-midpoint-carrier}, \autoref{thm:regular-cauchy-midpoint-namecert-obligations}, and \autoref{thm:regular-cauchy-midpoint-real-seal-handoff} seed the finite midpoint surface for two $\RegSeqRatUp$ rows over synchronized $\StreamNameUp$ schedules, $\DyadicMidpointUp$ pointwise midpoint rows, $\DyadicRatCoreUp$ tolerance ledgers, and $\RealUp$ seal data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, host real arithmetic, selected limits, trichotomy, ambient completeness, Lean verification, or a standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, synchronized-window exposure, pointwise midpoint exactness, regularity tolerance accounting, Real seal boundary, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyMidpointUp.}
  \constructivestory{A $\RegularCauchyMidpointUp$ packet is generated from two $\RegSeqRatUp$ rows, synchronized $\StreamNameUp$ schedules, a $\DyadicMidpointUp$ pointwise midpoint row, a $\DyadicRatCoreUp$ tolerance ledger, a $\RealUp$ seal row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
\end{closurestatus}
