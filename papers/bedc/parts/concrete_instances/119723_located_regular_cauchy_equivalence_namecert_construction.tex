\chapter{A Concrete Naming Certificate for $\LocatedRegularCauchyEquivalenceUp$}
\label{ch:concrete-instances-located-regular-cauchy-equivalence-namecert}
\origin{human}

Following Bishop and Bridges, the $\LocatedRegularCauchyEquivalenceUp$ packet records the finite comparison surface by which two regular Cauchy real names are judged equivalent through located windows and dyadic tolerances. It sits after the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the Bishop L10 comparison route of \autoref{ch:concrete-instances-bishop-l10-comparison-principle-namecert}. The packet names located equivalence as displayed finite data only; it does not quotient regular names, import host equality, assert trichotomy, choose representatives, or use ambient $\RealUp$ completeness.

\begin{definition}[Located regular Cauchy equivalence carrier]
\label{def:located-regular-cauchy-equivalence-carrier}
A $\LocatedRegularCauchyEquivalenceUp$ carrier is a finite $\BHist$ packet
$$
E=(L,R,W_L,W_R,D,C,V,H,A,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ and $R$ are the left and right regular-name rows read through the $\RegSeqRatUp$ surface;
\item $W_L$ and $W_R$ are the finite $\StreamNameUp$ windows inspected on the two sides;
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used by the located comparison;
\item $C$ is the finite comparison row licensed by the Bishop L10 comparison surface;
\item $V$ is the located verdict row saying that the inspected windows agree within the displayed dyadic budget;
\item $H$ records componentwise $\hsame$ transport for the two names, windows, tolerance, comparison, verdict, replay, provenance, and local naming rows;
\item $A$ records displayed $\Cont$ replay through the same finite comparison route;
\item $P$ is the $\Pkg$ provenance over $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BishopLTenComparisonPrincipleUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\LocatedRegularCauchyEquivalenceUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient equality of regular names, host equality of completed reals, trichotomy, selected representatives, selected limits, countable choice, or ambient $\RealUp$ completeness.
\end{definition}
\leanstmt{BEDC.Derived.LocatedRegularCauchyEquivalenceUp}

\begin{theorem}[Located regular Cauchy equivalence NameCert obligations]
\label{thm:located-regular-cauchy-equivalence-namecert-obligations}
For every accepted $\LocatedRegularCauchyEquivalenceUp$ carrier $E=(L,R,W_L,W_R,D,C,V,H,A,P,N)$, the local $\NameCert_{\LocatedRegularCauchyEquivalenceUp}$ surface consists exactly of carrier admission for the displayed rows, two regular-name exposures through $L,R$, finite window exposure through $W_L,W_R$, dyadic tolerance exposure through $D$, Bishop L10 comparison exposure through $C$, located verdict exposure through $V$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $A$, provenance through $P$, and local naming through $N$.

Consequently, an accepted consumer may read equivalence only as the finite located comparison of the two displayed regular Cauchy names. It cannot detach the verdict from the windows and tolerance ledger, convert the verdict into quotient equality, choose a representative, decide trichotomy, or appeal to ambient completion of $\RealUp$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-regular-cauchy-equivalence-carrier}. The only exposed rows are $L,R,W_L,W_R,D,C,V,H,A,P,N$, so the naming surface begins with two regular-name rows, their inspected windows, and the dyadic budget used by the comparison.

Read the comparison row next. The Bishop L10 comparison route licenses $C$ only after the displayed $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ data have been inspected, so the verdict $V$ remains a located finite-window statement.

Finally inspect the structural rows. Componentwise transport, displayed replay, provenance, and local naming preserve the same rows and add no quotient equality, selected representative, host equality, trichotomy, countable-choice schedule, selected limit, or ambient completeness coordinate.
\end{proof}

\begin{theorem}[Located regular Cauchy equivalence Real handoff]
\label{thm:located-regular-cauchy-equivalence-real-handoff}
Every $\RealUp$ consumer that reads an accepted $\LocatedRegularCauchyEquivalenceUp$ verdict must first display the two $\RegSeqRatUp$ rows $L,R$, the $\StreamNameUp$ windows $W_L,W_R$, the $\DyadicRatCoreUp$ tolerance ledger $D$, the Bishop L10 comparison row $C$, the located verdict row $V$, and the structural rows $H,A,P,N$. The handoff reaches the terminal $\RealUp$ seal only through those rows and does not assert quotient real equality, selected limits, trichotomy, host equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with an accepted located-equivalence packet. By \autoref{thm:located-regular-cauchy-equivalence-namecert-obligations}, a verdict is accepted only with the two regular names, their finite windows, the dyadic tolerance ledger, and the comparison row still present.

Now read the terminal seal. A $\RealUp$ consumer may use the verdict as a displayed handoff because $P$ ties the packet to the real-name seal and $A$ replays the same finite route.

No excluded object is produced by this route. Since the carrier lacks quotient equality, selected representatives, trichotomy, host equality, selected limits, and ambient completeness rows, the Real handoff remains a finite located comparison rather than a completed equality principle.
\end{proof}

\falsifiablePrediction{A located regular Cauchy equivalence read is rejected if the verdict row is accepted without both StreamName windows, both RegSeqRat rows, the DyadicRatCore tolerance ledger, the Bishop L10 comparison row, transport, replay, provenance, and local naming.}

\begin{closurestatus}{\LocatedRegularCauchyEquivalenceUp}
  \constructivestory{A $\LocatedRegularCauchyEquivalenceUp$ packet is generated from two regular Cauchy name rows, two finite StreamName windows, a DyadicRatCore tolerance ledger, a Bishop L10 comparison row, a located verdict, componentwise hsame transport, displayed Cont replay, provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-regular-cauchy-equivalence-carrier}, \autoref{thm:located-regular-cauchy-equivalence-namecert-obligations}, and \autoref{thm:located-regular-cauchy-equivalence-real-handoff} seed the finite located equivalence route over StreamNameUp, RegSeqRatUp, DyadicRatCoreUp, RealUp, BishopLTenComparisonPrincipleUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient equality of regular names, host real equality, trichotomy, selected representatives, selected limits, countable choice, ambient RealUp completeness, Lean verification of the theorem surface, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located verdict stability, tolerance-ledger nonescape, Real handoff ordering, transport, replay, provenance, and local naming for BEDC.Derived.LocatedRegularCauchyEquivalenceUp.}
\end{closurestatus}
