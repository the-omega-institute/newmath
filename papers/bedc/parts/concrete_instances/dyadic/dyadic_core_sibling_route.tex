\begin{theorem}[Dyadic core sibling route]
\label{thm:dyadic-core-sibling-route}
Every dyadic hub route serving a $\DyadicCauchyTailEnvelopeUp$ packet factors through the displayed sibling chain in the following order: \autoref{ch:concrete-instances-dyadic-cauchy-tail-envelope-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}.
The exported surface is only the finite tail-envelope read, dyadic tolerance ledger, finite stream window, regular rational readback, and terminal real-name seal, with transport, replay, provenance, and local naming rows attached. It exports no quotient stream, selected host limit, trichotomy, host real equality, or $\RealUp$ completeness coordinate.
\end{theorem}
\begin{proof}
Start with the hub input \autoref{ch:concrete-instances-dyadic-cauchy-tail-envelope-namecert}. Its carrier exposes the finite stream window, regular rational readback, dyadic tolerance row, tail-envelope ledger, reindexing route, transport, replay, provenance, and local naming rows of an accepted $\DyadicCauchyTailEnvelopeUp$ packet.

Next read the sibling sources named by that packet. The $\DyadicRatCoreUp$ chapter supplies the tolerance ledger, $\StreamNameUp$ supplies the finite window, $\RegSeqRatUp$ supplies the regular rational readback, and $\RealUp$ supplies the terminal seal consumed by downstream Real and Completion readers.

The route is therefore exhausted by the five displayed chapter surfaces and the structural rows already present in the packet. Since neither the hub nor the tail-envelope carrier contains a quotient stream, selected host limit, trichotomy row, host real equality row, or $\RealUp$ completeness coordinate, none of those objects can be exported by the dyadic hub route.
\end{proof}
