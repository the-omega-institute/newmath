\chapter{A Concrete Naming Certificate for $\BishopCompactOpenAscoliUp$}
\label{ch:concrete-instances-bishop-compact-open-ascoli-namecert}
\origin{human}

Following Bishop and Bridges on compactness through explicit finite nets and the Ascoli principle through equicontinuity data, the $\BishopCompactOpenAscoliUp$ packet records the compact-open Ascoli route as finite BEDC rows rather than as a host theorem about function spaces. It sits beside the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus ledger of \autoref{ch:concrete-instances-uniformmodulus-namecert}, and the Ascoli packet of \autoref{ch:concrete-instances-arzelaascoli-namecert}. The chapter names the compact-open extraction surface used by downstream compact-function and completion consumers.

\begin{definition}[Bishop compact-open Ascoli carrier]
\label{def:bishop-compact-open-ascoli-carrier}
A $\BishopCompactOpenAscoliUp$ carrier is a finite $\BHist$ packet
$$
A=(K,\Omega,E,F,R,S,H,C,P,N).
$$
Here $K$ is the compact-source finite-net row, $\Omega$ is the compact-open window ledger listing finite source windows and target inspection radii, $E$ is the equicontinuity-modulus row, and $F$ is the finite subfamily row selected inside the displayed compact-open window. The row $R$ is the $\RegSeqRatUp$ readback for all rational estimates, $S$ is the terminal $\RealUp$ seal for target-distance bounds, $H$ is componentwise $\hsame$ transport, $C$ is displayed $\Cont$ replay for the compact-open route, $P$ is $\Pkg$ provenance over $\BishopCompactOpenAscoliUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, and $\Cont$, and $N$ is the local $\NameCert_{\BishopCompactOpenAscoliUp}$ row.
\end{definition}

\begin{theorem}[Bishop compact-open Ascoli NameCert obligations]
\label{thm:bishop-compact-open-ascoli-namecert-obligations}
For every accepted carrier $A=(K,\Omega,E,F,R,S,H,C,P,N)$, the local $\NameCert_{\BishopCompactOpenAscoliUp}$ surface is exhausted by the following five rows:
\begin{enumerate}
\item compact-source finite nets $K$ over the displayed $\CompactMetricUp$ source;
\item compact-open window rows $\Omega$ recording only finite source windows and target tolerances;
\item equicontinuity moduli $E$ supplied before any finite subfamily row is read;
\item finite subfamily rows $F$ with $\RegSeqRatUp$ readback $R$ and terminal $\RealUp$ seal $S$;
\item componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local naming row $N$.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the accepted packet onto its compact source, compact-open window, equicontinuity, finite-subfamily, readback, seal, transport, replay, provenance, and naming coordinates.

The carrier definition leaves no coordinate for a host compact-open topology, an unrecorded open cover, a choice-selected infinite subfamily, or quotient function equality. Thus every public NameCert read factors through the five displayed rows.
\end{proof}

\begin{theorem}[Bishop compact-open Ascoli consumer route]
\label{thm:bishop-compact-open-ascoli-compact-open-route}
Every compact-open consumer of an accepted $\BishopCompactOpenAscoliUp$ packet must read the compact-source finite nets, compact-open window rows, equicontinuity moduli, finite subfamily rows, $\RegSeqRatUp$ readback, $\RealUp$ seal, transport, replay, provenance, and local NameCert row before treating the packet as an Ascoli handoff.
\end{theorem}
\begin{proof}
The consumer begins at the compact source row $K$ and window ledger $\Omega$, so it sees only the displayed finite compact-open windows.

The equicontinuity row $E$ and finite subfamily row $F$ then provide the Ascoli-facing extraction data, while $R$ and $S$ keep the numerical bounds on the regular-rational and real-seal route. Transport $H$, replay $C$, provenance $P$, and local name $N$ attach those rows to the same packet. No additional compactness, equality, or completeness coordinate is available.
\end{proof}

\closureat{BishopCompactOpenAscoliUp}{seedStr}

\begin{closurestatus}{\BishopCompactOpenAscoliUp}
  \constructivestory{A $\BishopCompactOpenAscoliUp$ packet is generated from compact-source finite nets, compact-open window rows, equicontinuity moduli, finite subfamily rows, regular-rational readback, a Real seal, transport, replay, provenance, and local NameCert evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{The seed surface is the finite compact-open Ascoli carrier and two-row consumer route through $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\RegSeqRatUp$, and $\RealUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close sequential compactness without data, open-cover compactness as a general theorem, host function equality, quotient real equality, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, NameCert row coverage, compact-open window stability, and consumer non-escape for $\BishopCompactOpenAscoliUp$.}
\end{closurestatus}
