\chapter{A Concrete Naming Certificate for $\HyperbolicPhaseLedgerUp$}
\label{ch:concrete-instances-hyperbolic-phase-ledger-namecert}
\origin{ai}

The $\HyperbolicPhaseLedgerUp$ packet realizes the phase-ledger branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite observer-facing geometry data. It sits after the boundary-shadow route of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the observer-expansion packet of \autoref{ch:concrete-instances-hyperbolic-observer-expansion-namecert}, the phase-shell packet of \autoref{ch:concrete-instances-hyperbolic-observer-phase-shell-namecert}, and the finite phase-transport row of \autoref{ch:concrete-instances-poincare-disk-phase-transport-namecert}. The packet names a finite ledger for hyperbolic phase readout; it is not a completed hyperbolic space, a physical observer, a quotient ideal boundary, or an ambient dynamical theorem.

\begin{definition}[Hyperbolic phase ledger carrier]
\label{def:hyperbolic-phase-ledger-carrier}
A $\HyperbolicPhaseLedgerUp$ carrier is a finite $\BHist$ packet
$$
Q=(B,O,S,T,R,H,C,P,N)
$$
with $B$ the $\HyperbolicBoundaryShadowUp$ boundary-shadow row, $O$ the $\HyperbolicObserverExpansionUp$ observer-expansion row, $S$ the $\HyperbolicObserverPhaseShellUp$ shell row, $T$ the $\PoincareDiskPhaseTransportUp$ transport row, $R$ the refusal row excluding completed boundary and physical-observer readings, $H$ componentwise $\hsame$ transport, $C$ displayed $\Cont$ replay, $P$ the $\Pkg$ provenance, and $N$ the local $\NameCert_{\HyperbolicPhaseLedgerUp}$ row.
\end{definition}

\begin{theorem}[Hyperbolic phase ledger NameCert obligations]
\label{thm:hyperbolic-phase-ledger-namecert-obligations}
For every accepted $\HyperbolicPhaseLedgerUp$ carrier $Q=(B,O,S,T,R,H,C,P,N)$, the local $\NameCert_{\HyperbolicPhaseLedgerUp}$ surface is exhausted by boundary-shadow admission, observer-expansion admission, phase-shell admission, disk phase-transport replay, refusal exactness, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier onto $B,O,S,T$. These four rows are the only geometric and observer-facing data available to the packet, and each is already a finite NameCert surface rather than a host geometry.

The refusal row $R$ is checked against those same displayed rows. It prevents a consumer from replacing boundary-shadow, observer-expansion, shell, or phase-transport data by a completed hyperbolic space, quotient ideal-boundary equality, physical observer trajectory, or global dynamical theorem.

Transport and replay are componentwise. The row $H$ preserves the displayed packet under $\hsame$, while $C$ replays the same finite route under $\Cont$. The provenance and local naming rows $P,N$ then package exactly the exposed ledger and no additional analytic object.
\end{proof}

\falsifiablePrediction{An accepted $\HyperbolicPhaseLedgerUp$ read is refuted if a consumer obtains a completed hyperbolic space, quotient ideal-boundary equality, physical observer trajectory, or global dynamical theorem without displaying HyperbolicBoundaryShadow, HyperbolicObserverExpansion, HyperbolicObserverPhaseShell, PoincareDiskPhaseTransport, refusal, transport, replay, provenance, and local naming rows.}

\independenceWitness{hyperbolic-boundary-shadow, hyperbolic-observer-expansion, hyperbolic-observer-phase-shell, poincare-disk-phase-transport: none is bijective to $\HyperbolicPhaseLedgerUp$ because the packet jointly carries boundary shadow, observer expansion, phase shell, disk phase transport, refusal exactness, transport, replay, provenance, and local NameCert rows.}

The Lean TasteGate route must provide explicit $\Nontrivial\ \HyperbolicPhaseLedgerUp$ and $\FieldFaithful\ \HyperbolicPhaseLedgerUp$ instances before the packet can serve as a checked carrier-alignment witness. Those instances must preserve $B,O,S,T,R,H,C,P,N$ as separate rows.

\closureat{HyperbolicPhaseLedgerUp}{seedStr}

\begin{closurestatus}{\HyperbolicPhaseLedgerUp}
  \constructivestory{A $\HyperbolicPhaseLedgerUp$ packet is generated from HyperbolicBoundaryShadow rows, HyperbolicObserverExpansion rows, HyperbolicObserverPhaseShell rows, PoincareDiskPhaseTransport rows, refusal exactness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-phase-ledger-carrier}, \autoref{thm:hyperbolic-phase-ledger-namecert-obligations}, the falsifiable prediction, and the independence witness seed the finite hyperbolic phase ledger over boundary-shadow, observer-expansion, phase-shell, disk-transport, refusal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed hyperbolic spaces, quotient ideal-boundary equality, physical observer trajectories, global dynamical theorems, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, boundary-shadow exposure, observer-expansion exposure, phase-shell exposure, disk phase-transport replay, TasteGate nontriviality, field faithfulness, refusal exactness, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicPhaseLedgerUp.}
\end{closurestatus}
