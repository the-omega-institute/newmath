The $\SubspaceTopologyUp$ carrier exports a subspace surface only after reading the $\TopologyUp$ open-set classifier rows of \autoref{ch:concrete-instances-topology-namecert} and the $\ContinuousUp$ restriction and continuity rows of \autoref{ch:concrete-instances-continuous-namecert}. The dependency route keeps the inherited open-set test, restricted source row, and displayed continuity replay inside the same finite $\BHist$ packet before any subspace consumer can read the local $\NameCert_{\SubspaceTopologyUp}$ surface.
