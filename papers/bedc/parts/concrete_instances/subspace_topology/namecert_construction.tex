\chapter{A Concrete Naming Certificate for $\SubspaceTopologyUp$}
\label{ch:concrete-instances-subspace-topology-namecert}
\origin{human}

Following Bourbaki and Kelley, the $\SubspaceTopologyUp$ packet records the induced topology on a displayed carrier inside an ambient topological space as finite BEDC naming data. It sits between the indexed-open topology surface of \autoref{ch:concrete-instances-topology-namecert}, the metric-subspace restriction packet of \autoref{ch:concrete-instances-metric-subspace-namecert}, and the existing $\mathsf{BHist}$ subspace-open surface in \autoref{def:topology-bhist-subspace-open-surface}. The packet gives compact-subspace, restriction-of-continuity, and relative-open consumers a general topology-level carrier, without forcing them through metric distance rows, host powersets, quotient subset equality, or selected ambient points.

Before a subspace open is exported, the carrier consumes the ambient $\TopologyUp$ open-row discipline of \autoref{ch:concrete-instances-topology-namecert} and the inclusion-facing admission classifier carried by its own row $A$. Thus every relative-open read is already anchored in the ambient indexed-open ledger and the displayed inclusion surface, rather than being a free-standing host subset or an independent topology replacement.

\begin{definition}[SubspaceTopology carrier]
\label{def:subspace-topology-carrier}
A $\SubspaceTopologyUp$ carrier is a finite $\BHist$ packet
$$
S=(X,A,O,R,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the ambient $\TopologyUp$ row, exposing the point classifier, indexed-open ledger, finite-intersection rows, arbitrary-union rows, boundary opens, and visible open-membership stability.
\item $A$ is the displayed admission row for the subspace carrier. A point is available to the subspace packet only when it passes the ambient point classifier of $X$ and the admission row $A$.
\item $O$ is the relative-open row. Each accepted subspace open is displayed as an ambient open row of $X$ together with restriction to admitted points.
\item $R$ is the restriction-exactness row, recording that membership in a relative open is read only as simultaneous admission by $A$ and ambient membership in the corresponding open row of $X$.
\item $B$ is the boundary and closure ledger, recording empty, whole-of-subspace, finite-intersection, and arbitrary-union rows obtained by restricting the corresponding ambient topology rows.
\item $H$ records componentwise $\hsame$ transport for the ambient topology, admission row, relative-open row, restriction exactness, boundary and closure ledger, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a relative-open request through $X,A,O,R,B,H$.
\item $P$ is the $\Pkg$ provenance over $\SubspaceTopologyUp$, $\TopologyUp$, $\MetricSubspaceUp$, \autoref{def:topology-bhist-subspace-open-surface}, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\SubspaceTopologyUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,A,O,R,B,H,C,P,N$. It has no coordinate for host powersets, quotient subset equality, selected ambient representatives, independent open selectors, metric distance data, or a replacement topology.
\end{definition}

\begin{theorem}[SubspaceTopology NameCert obligations]
\label{thm:subspace-topology-namecert-obligations}
For every accepted $\SubspaceTopologyUp$ carrier $S=(X,A,O,R,B,H,C,P,N)$, the local $\NameCert_{\SubspaceTopologyUp}$ surface consists exactly of carrier admission for the ambient topology and admission rows, relative-open exposure, restriction exactness, boundary and closure rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every relative-open consumer must read the route
$$
\TopologyUp,A,O,R,B,H,C,P,N
$$
before treating a subspace open as public.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:subspace-topology-carrier}. The available rows are the ambient topology $X$, admission predicate $A$, relative-open row $O$, restriction exactness $R$, boundary and closure ledger $B$, and the structural rows $H,C,P,N$.

Relative-open exposure is the role of $O$ and $R$. A public subspace membership read is not a host subset membership; it is the displayed conjunction of the admission row $A$ and an ambient open-membership row of $X$.

The boundary and closure rows are inherited from the ambient topology. Empty and whole-of-subspace opens are obtained by restricting the corresponding boundary opens of $X$, finite intersections restrict the ambient meet row, and arbitrary unions restrict the ambient union row while keeping the same admission predicate.

Transport and replay preserve the same route. The row $H$ aligns the ambient classifier and admission predicate, $C$ replays relative-open requests through the displayed rows, and $P,N$ source and name the packet.

Since host powersets, quotient subset equality, selected representatives, independent open selectors, metric distance data, and replacement topologies are absent from the carrier, no local NameCert obligation can export them.
\end{proof}

\begin{theorem}[SubspaceTopology neighbour compatibility]
\label{thm:subspace-topology-neighbour-compatibility}
Every $\SubspaceTopologyUp$ packet refines the ambient $\TopologyUp$ row by restriction to admitted points, and every $\MetricSubspaceUp$ topology consumer may read the same admission row through this topology-level packet before invoking metric distance data. In particular, the relative-open route factors as
$$
X,A \longmapsto O,R,B \longmapsto H,C,P,N ,
$$
so compact subspace, continuity restriction, and metric-subspace consumers can share the same displayed subspace carrier while keeping metric distances outside the general topology certificate.
\end{theorem}
\begin{proof}
Start with an accepted $\SubspaceTopologyUp$ carrier and project the ambient topology row $X$ and admission row $A$. These two rows determine the carrier on which all relative-open reads take place.

Use \autoref{thm:subspace-topology-namecert-obligations}. It forces every relative-open read through $O,R,B$ after the admission row has been displayed, and it keeps transport, replay, provenance, and local naming in $H,C,P,N$.

A $\MetricSubspaceUp$ consumer supplies a compatible admission row and an inherited distance ledger, but those metric rows are not needed to form the relative opens. The topology-level packet can therefore be read first, and metric distance data can be invoked only by consumers that explicitly require it.

Thus the packet is the common neighbour of the ambient topology and metric-subspace surfaces: it restricts opens by admission, names the induced topology, and does not add an independent metric or host subset coordinate.
\end{proof}

\begin{theorem}[SubspaceTopology continuity restriction]
\label{thm:subspace-topology-continuity-restriction}
Let $S=(X,A,O,R,B,H,C,P,N)$ be an accepted $\SubspaceTopologyUp$ carrier. Any $\ContinuousMapUp$ route whose source is the ambient $\TopologyUp$ row $X$ restricts to admitted subspace points only after the consumer reads $X,A,O,R,B,H,C,P,N$ in order: ambient opens from $X$, admission from $A$, relative opens from $O$, restriction exactness from $R$, inherited boundary and closure rows from $B$, transport from $H$, replay from $C$, provenance from $P$, and local naming from $N$.

The restricted continuity read is therefore a relative-open route through the displayed subspace packet. It introduces no metric rows, host subset equality, selected ambient representatives, independent open selectors, or replacement topology.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:subspace-topology-carrier}. A subspace point is available only when the ambient point classifier in $X$ and the admission row $A$ have both been displayed.

Apply the neighbour compatibility theorem \autoref{thm:subspace-topology-neighbour-compatibility}. It factors every relative-open read through $X,A$ before $O,R,B$, so the preimage test for a restricted continuous route can inspect only admitted points and ambient open rows.

The restriction-exactness row $R$ supplies the needed membership reading: a point belongs to a relative open exactly through the displayed admission row and the corresponding ambient open-membership row. Thus continuity on the subspace is checked by pulling the ambient open request back through the same relative-open route.

Finally $H,C,P,N$ preserve, replay, source, and name that route. Since no metric distance row, host subset equality, selected representative, independent selector, or replacement topology is present in the carrier, none can be exported by the restricted continuity read.
\end{proof}

\begin{theorem}[SubspaceTopology downstream Kelleyfication route]
\label{thm:subspace-topology-downstream-kelleyfication-route}
Let $S=(X,A,O,R,B,H,C,P,N)$ be an accepted $\SubspaceTopologyUp$ carrier. Any $\KelleyficationUp$ or compactly generated reflection consumer that reads the topology induced on the displayed subspace must first pass through the ambient $\TopologyUp$ row $X$, the admission row $A$, the relative-open row $O$, the restriction-exactness row $R$, and the transport and replay rows $H,C$ before the compact-window tests of \autoref{ch:concrete-instances-kelleyfication-namecert} may consume the result.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:subspace-topology-carrier}. The rows $X,A,O,R,H,C$ are the only displayed route by which an accepted subspace packet exposes its ambient topology, admitted carrier, relative opens, restriction exactness, componentwise transport, and replay.

Apply \autoref{thm:subspace-topology-neighbour-compatibility}. It factors every relative-open route through $X,A$ before $O,R,B$ and then through $H,C,P,N$, keeping metric distance data and host subset coordinates outside the general topology certificate.

A $\KelleyficationUp$ consumer reads compact-window tests from a topology source. Therefore its downstream request must receive the induced topology only after the subspace packet has exposed the ambient topology, admission, relative-open, restriction-exactness, transport, and replay rows. This is exactly the route displayed by $S$, so the compactly generated reflection gains no independent topology replacement.
\end{proof}

\begin{theorem}[SubspaceTopology forward SorgenfreyPlane route]
\label{thm:subspace-topology-forward-sorgenfrey-plane-route}
Every $\SorgenfreyPlaneUp$ lower-limit product consumer that reads a restricted product topology must first read the ambient $\TopologyUp$ row $X$ and the $\SubspaceTopologyUp$ restriction row $A,O,R$ before product lower-limit data from \autoref{ch:concrete-instances-sorgenfrey-plane-namecert} is consumed. The route is
$$
X,A,O,R,B,H,C,P,N \longmapsto \SorgenfreyPlaneUp ,
$$
so the Sorgenfrey-plane read receives the displayed ambient topology and subspace restriction before it inspects product lower-limit coordinates. It does not receive an independent host product topology, quotient subset equality, selected ambient representative, or detached lower-limit basis.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:subspace-topology-carrier}. The ambient row $X$ and admission row $A$ are the only coordinates that expose the ambient topology and the subspace carrier.

Apply the topology-forward route of \autoref{thm:subspace-topology-topology-forward-route} and the topology sibling route of \autoref{thm:subspace-topology-topology-sibling-route}. These routes make every relative-open read pass through the ambient $\TopologyUp$ ledger and the displayed subspace restriction before downstream topology consumers use the result.

Now read the Sorgenfrey-plane consumer of \autoref{ch:concrete-instances-sorgenfrey-plane-namecert}. Its lower-limit product surface can consume the induced topology only after the subspace packet has exposed $X,A,O,R$ and preserved the read through $B,H,C,P,N$.

Since the carrier has no host product topology, quotient subset equality, selected representative, or detached lower-limit basis coordinate, the downstream route is exactly the displayed topology-to-subspace-to-product spine.
\end{proof}

\closureat{SubspaceTopologyUp}{seedStr}

\begin{closurestatus}{\SubspaceTopologyUp}
  \constructivestory{A $\SubspaceTopologyUp$ packet is generated from an ambient TopologyUp row, displayed admission row, relative-open row, restriction-exactness row, boundary and closure ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:subspace-topology-carrier}, \autoref{thm:subspace-topology-namecert-obligations}, and \autoref{thm:subspace-topology-neighbour-compatibility} seed the finite induced-topology interface over ambient topology, admission, relative opens, restriction exactness, closure rows, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, host powerset topologies, quotient subset equality, selected ambient representatives, independent open selectors, metric distance construction, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, relative-open exposure, restriction exactness, inherited boundary and closure rows, neighbour compatibility with MetricSubspaceUp, transport, replay, provenance, and local naming for BEDC.Derived.SubspaceTopologyUp.}
\end{closurestatus}
