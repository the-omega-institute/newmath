\chapter{A Concrete Naming Certificate for $\RegularCauchyDiagonalArgumentUp$}
\label{ch:concrete-instances-regular-cauchy-diagonal-argument-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchyDiagonalArgumentUp$ packet records the finite diagonal extraction route that turns a displayed regular Cauchy family of rational names into the regular rational readback consumed by a $\RealUp$ seal. It sits between the $\RegSeqRatUp$ source surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite $\StreamNameUp$ window surface of \autoref{ch:concrete-instances-streamname-namecert}, the $\DyadicRatCoreUp$ tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the $\RealUp$ boundary of \autoref{ch:concrete-instances-real-namecert}, and the sibling diagonalization packet of \autoref{ch:concrete-instances-regular-cauchy-diagonalization-namecert}. The packet names the L10 RegSeqRat-to-Real handoff without importing quotient streams, selected limits, choice, or ambient Real completeness.

\begin{definition}[Regular Cauchy diagonal argument carrier]
\label{def:regular-cauchy-diagonal-argument-carrier}
A $\RegularCauchyDiagonalArgumentUp$ carrier is a finite $\BHist$ packet
$$
A=(F,W,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the source $\RegSeqRatUp$ family row. It displays the finite collection of rational-name approximants and the precision requests inspected by the diagonal extraction.
\item $W$ is the $\StreamNameUp$ window row. It records the finite source windows selected for each inspected precision and exposes no quotient stream equality.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger. It records the dyadic radius, split budget, common scale, and comparison cells used to certify the diagonal readback.
\item $R$ is the diagonal $\RegSeqRatUp$ readback row. It consumes only the displayed windows $W$ and tolerance ledger $D$ as one regular rational sequence.
\item $E$ is the terminal $\RealUp$ seal row. It is visible only after $F,W,D,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the source family, stream windows, tolerance ledger, regular readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a diagonal extraction request through the source family, finite stream windows, dyadic ledger, regular rational readback, Real seal, transports, provenance, and local name.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyDiagonalArgumentUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyDiagonalArgumentUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $F,W,D,R,E,H,C,P,N$. It has no coordinate for quotient streams, selected limits, choice, ambient $\RealUp$ completeness, Lean verification, or a checked bridge.
\end{definition}

\begin{theorem}[Regular Cauchy diagonal argument NameCert obligations]
\label{thm:regular-cauchy-diagonal-argument-namecert-obligations}
For every accepted $\RegularCauchyDiagonalArgumentUp$ carrier
$$
A=(F,W,D,R,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyDiagonalArgumentUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed $\RegSeqRatUp$ family, $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance ledger, diagonal $\RegSeqRatUp$ readback, $\RealUp$ seal, transport, replay, provenance, and local naming rows;
\item window exactness, requiring $W$ to expose only the finite source windows named by the diagonal extraction request;
\item dyadic tolerance exactness, requiring $D$ to supply the radius, split budget, common scale, and comparison cells used by the extraction;
\item regular readback exactness, requiring $R$ to consume precisely the windows $W$ and dyadic ledger $D$ before any Real-facing seal is visible;
\item Real-seal nonescape, requiring $E$ to appear only after $F,W,D,R$ have been transported by $H$ and replayed by $C$;
\item componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming for exactly the rows $F,W,D,R,E,H,C,P,N$;
\item refusal of quotient streams, selected limits, choice, ambient $\RealUp$ completeness, Lean verification, and bridge checking as local certificate data.
\end{enumerate}
No local diagonal-argument obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-diagonal-argument-carrier}. The accepted inventory is exactly the source $\RegSeqRatUp$ family $F$, finite $\StreamNameUp$ windows $W$, dyadic tolerance ledger $D$, diagonal $\RegSeqRatUp$ readback $R$, terminal $\RealUp$ seal $E$, and structural rows $H,C,P,N$.

Window and tolerance exactness are read from $W$ and $D$. The stream row exposes only finite inspected windows, and the dyadic row supplies the radius, split budget, common scale, and comparison cells needed to compare those windows.

The regular readback row $R$ is downstream of those two rows. It cannot name a quotient stream or a selected limit, because the carrier gives it only $W$ and $D$ as inputs.

Finally, $E,H,C,P,N$ seal, transport, replay, source, and name the same packet. Since the carrier inventory contains no quotient streams, selected limits, choice operator, ambient completeness principle, Lean verification row, or bridge credential, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy diagonal argument Real handoff]
\label{thm:regular-cauchy-diagonal-argument-real-handoff}
Every $\RealUp$ or Completion consumer of an accepted $\RegularCauchyDiagonalArgumentUp$ carrier factors through the finite route
$$
F \longmapsto W \longmapsto D \longmapsto R \longmapsto E \longmapsto H,C,P,N .
$$
Thus a diagonal extraction is available to the Real boundary only as a displayed $\RegSeqRatUp$ family row, finite $\StreamNameUp$ windows, one $\DyadicRatCoreUp$ tolerance ledger, one diagonal $\RegSeqRatUp$ readback, one terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ evidence.
\end{theorem}
\begin{proof}
Begin with the obligation surface of \autoref{thm:regular-cauchy-diagonal-argument-namecert-obligations}. It confines every accepted packet to the displayed family, window, tolerance, readback, Real-seal, and structural rows.

The consumer first reads $F,W,D$. These rows expose the source regular rational names, the finite stream windows inspected by the diagonal argument, and the dyadic comparison budget.

The consumer then reads $R$ and $E$. The regular rational readback is formed from the same finite windows and dyadic ledger, and the terminal Real seal is visible only after that readback has been displayed.

The rows $H,C,P,N$ preserve this route under transport, replay, provenance, and naming. Because the carrier has no coordinate for quotient streams, selected limits, choice, ambient Real completeness, Lean verification, or bridge checking, none of those objects can enter the Real-facing handoff.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyDiagonalArgumentUp$ Real or Completion read must expose a RegSeqRat family row, finite StreamName windows, one DyadicRatCore tolerance ledger, one diagonal RegSeqRat readback, one RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}

\independenceWitness{regseqrat, streamname, dyadicratcore, real, regular-cauchy-diagonalization: $\RegularCauchyDiagonalArgumentUp$ is not bijective to any listed sibling because it jointly binds source RegSeqRat family data, finite StreamName windows, dyadic tolerance exactness, diagonal RegSeqRat readback, and RealUp handoff in one displayed packet.}

\closureat{RegularCauchyDiagonalArgumentUp}{seedStr}

\begin{closurestatus}{\RegularCauchyDiagonalArgumentUp}
  \constructivestory{A $\RegularCauchyDiagonalArgumentUp$ packet is generated from a RegSeqRat family row, finite StreamName windows, one DyadicRatCore tolerance ledger, one diagonal RegSeqRat readback, one RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-diagonal-argument-carrier}, \autoref{thm:regular-cauchy-diagonal-argument-namecert-obligations}, and \autoref{thm:regular-cauchy-diagonal-argument-real-handoff} seed the finite diagonal extraction packet used by L10 RegSeqRat-to-Real consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient streams, selected limits, choice, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-window exposure, dyadic tolerance exactness, regular readback exactness, Real-seal exposure, transport stability, replay stability, provenance exhaustion, and local naming for BEDC.Derived.RegularCauchyDiagonalArgumentUp.}
\end{closurestatus}
