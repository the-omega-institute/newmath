\chapter{A Concrete Naming Certificate for $\LocatedRealSpreadUp$}
\label{ch:concrete-instances-located-real-spread-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\LocatedRealSpreadUp$ packet realizes the Real-completeness direction of \autoref{ch:visions-real-completeness} as a finite located-window NameCert object. It binds the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the finite-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the tail-budget siblings used by the completion route. The packet records finite located reads for a spread of Real-facing windows; it does not assert full $\RealUp$ completeness, quotient stream equality, selected limits, countable choice, or an ambient Cauchy completion theorem.

\begin{definition}[Located real spread carrier]
\label{def:located-real-spread-carrier}
A $\LocatedRealSpreadUp$ carrier is a finite $\BHist$ packet
$$
S=(W,L,D,R,E,H,C,P,N)
$$
with the following rows:
\begin{enumerate}
\item $W$ is the finite spread-window ledger, listing the StreamName requests and local indices at which Real-facing reads are inspected.
\item $L$ is the located read row, carrying lower and upper dyadic bracketing data for every displayed window together with apartness refusals outside the inspected spread.
\item $D$ is the dyadic tail-budget row over $\DyadicRatCoreUp$, recording precision thresholds, common-scale tolerances, and finite tail comparisons.
\item $R$ is the $\RegSeqRatUp$ readback row, repacking the located windows and dyadic tail budgets as regular rational finite data.
\item $E$ is the terminal $\RealUp$ seal row for the displayed located reads and their regular rational witnesses.
\item $H$ records componentwise $\hsame$ transport for spread windows, located Real reads, dyadic tail budgets, regular rational readbacks, Real seals, replay rows, provenance rows, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a located-spread request through $W,L,D,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\LocatedRealSpreadUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, the Cauchy-tail budget siblings, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedRealSpreadUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $W,L,D,R,E,H,C,P,N$. It has no coordinate for quotient stream equality, selected limits, countable choice, full Real completeness, or ambient completion.
\end{definition}

\begin{theorem}[Located real spread NameCert obligations]
\label{thm:located-real-spread-namecert-obligations}
For every accepted $\LocatedRealSpreadUp$ carrier
$$
S=(W,L,D,R,E,H,C,P,N),
$$
the local $\NameCert_{\LocatedRealSpreadUp}$ surface consists exactly of finite spread-window admission for $W$, located read admission for $L$, dyadic tail-budget admission for $D$, regular rational readback for $R$, terminal Real sealing for $E$, componentwise $\hsame$ transport for $H$, displayed replay for $C$, provenance for $P$, and local naming for $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-real-spread-carrier}. The row $W$ lists the finite windows being inspected, and $L$ supplies the located bracketing reads attached to those windows.

Read the tail-budget row $D$. It records precision thresholds, common dyadic scales, and finite tail comparisons before any regular rational or Real-facing row is consumed.

The readback row $R$ repacks the located windows and dyadic budgets as $\RegSeqRatUp$ data, and $E$ seals exactly those finite reads as $\RealUp$ data.

The structural rows $H,C,P,N$ then transport, replay, source, and locally name the same packet. Since the carrier contains no quotient stream equality, selected-limit operator, countable-choice schedule, full completeness theorem, or ambient completion coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Located real spread regular readback]
\label{thm:located-real-spread-regular-readback}
Every completion-facing consumer of an accepted $\LocatedRealSpreadUp$ packet must read the finite route
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp
$$
through $W,L,D,R,E,H,C,P,N$ before using the located spread as Real-completeness data. The route exports finite located-window reads, dyadic tail budgets, regular rational readbacks, terminal Real seals, transport, replay, provenance, and local naming, but not quotient stream equality, selected limits, countable choice, full $\RealUp$ completeness, or an ambient completion operator.
\end{theorem}
\begin{proof}
Start with the finite window ledger $W$ and located read row $L$. These rows expose only the inspected spread windows and their dyadic bracketing data.

Use the dyadic tail-budget row $D$ next. It attaches each located read to a displayed precision threshold and common-scale comparison before the readback row is available.

The regular readback row $R$ is the only handoff to $\RegSeqRatUp$, and the terminal row $E$ is the only handoff to $\RealUp$. Thus a completion-facing reader receives regular rational evidence before it reads a Real seal.

Transport, replay, provenance, and local naming are supplied by $H,C,P,N$. None of these rows adds quotient stream equality, a selected limit, countable choice, full Real completeness, or an ambient completion operator, so every accepted read remains inside the displayed finite route.
\end{proof}

\begin{theorem}[Located real spread obligation closure]
\label{thm:located-real-spread-obligation-closure}
The carrier of \autoref{def:located-real-spread-carrier}, the NameCert surface of \autoref{thm:located-real-spread-namecert-obligations}, and the regular readback route of \autoref{thm:located-real-spread-regular-readback} form the obligation surface for $\LocatedRealSpreadUp$. Every local obligation is one of the displayed rows $W,L,D,R,E,H,C,P,N$, and every completion-facing read factors through those rows before it can be used as located Real-spread data.
\end{theorem}
\begin{proof}
Use the carrier projection. The rows $W,L,D,R,E$ provide the finite spread windows, located bracketing reads, dyadic tail budgets, regular rational readbacks, and Real seals.

Apply the NameCert obligations to identify the structural rows $H,C,P,N$ as transport, replay, provenance, and local naming for the same packet.

Finally use the regular readback theorem. It forces every completion-facing consumer through $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ before the located spread is exported, so no additional local obligation remains outside $W,L,D,R,E,H,C,P,N$.
\end{proof}

\begin{theorem}[Located real spread L10 scope]
\label{thm:located-real-spread-l10-scope}
The $\LocatedRealSpreadUp$ packet is scoped to the L10 Real and Completion substrate consisting of $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, displayed $\Cont$ replay, componentwise $\hsame$ transport, and finite located-window rows. It exports no obligation that reads a quotient stream, selected limit, countable-choice schedule, full $\RealUp$ completeness theorem, or ambient completion operator.
\end{theorem}
\begin{proof}
Read the finite located-window rows first. The window ledger $W$ and located row $L$ determine the inspected spread requests and their dyadic bracketing data.

The dyadic row $D$, regular readback row $R$, and Real seal row $E$ place those requests inside $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$.

The structural rows $H$ and $C$ provide exactly componentwise $\hsame$ transport and displayed $\Cont$ replay, while $P,N$ record provenance and local naming. Since the carrier has no coordinate for quotient streams, selected limits, countable choice, full Real completeness, or ambient completion, the L10 scope is precisely the listed substrate.
\end{proof}

\falsifiablePrediction{Every $\LocatedRealSpreadUp$ read factors through finite spread windows, located Real reads, dyadic tail budgets, $\RegSeqRatUp$ readback, terminal $\RealUp$ seals, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and a local $\NameCert_{\LocatedRealSpreadUp}$ row.}

\independenceWitness{streamname, regseqrat, dyadicratcore, real, tailbudget: $\LocatedRealSpreadUp$ is not bijective to any listed sibling because it jointly carries finite spread windows, located bracketing reads, dyadic tail-budget rows, regular rational readbacks, Real seals, transport, replay, provenance, and local naming.}

\closureat{LocatedRealSpreadUp}{seedStr}

\begin{closurestatus}{\LocatedRealSpreadUp}
  \constructivestory{A $\LocatedRealSpreadUp$ packet is generated from finite spread windows, located Real reads, dyadic tail budgets, RegSeqRat readback, Real seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:located-real-spread-carrier}, \autoref{thm:located-real-spread-namecert-obligations}, \autoref{thm:located-real-spread-regular-readback}, \autoref{thm:located-real-spread-obligation-closure}, and \autoref{thm:located-real-spread-l10-scope} bind the finite located-window, dyadic-tail, RegSeqRat readback, StreamName, Real seal, transport, replay, provenance, and local naming obligation packet.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected limits, countable choice, full $\RealUp$ completeness, ambient completion, Lean verification, or bridge checking for $\LocatedRealSpreadUp$.}
  \upgradepath{Obligation closure requires checked spread-window admission, located read stability, dyadic tail-budget exactness, RegSeqRat readback, Real sealing, replay stability, and nonescape for BEDC.Derived.LocatedRealSpreadUp.}
\end{closurestatus}
