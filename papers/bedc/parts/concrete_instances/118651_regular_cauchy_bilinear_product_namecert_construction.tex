\chapter{A Concrete Naming Certificate for $\RegularCauchyBilinearProductUp$}
\label{ch:concrete-instances-regular-cauchy-bilinear-product-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchyBilinearProductUp$ packet records finite bilinear product-window data over $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, and $\CauchyProductUp$. It sits beside the product source of \autoref{ch:concrete-instances-cauchyproduct-namecert}, the product-stability route of \autoref{ch:concrete-instances-real-uniform-cauchy-product-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic product ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite bilinear product surface for later Real algebra; it does not use quotient streams, host multiplication, trichotomy, selected limits, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy bilinear product carrier]
\label{def:regular-cauchy-bilinear-product-carrier}
A $\RegularCauchyBilinearProductUp$ carrier is a finite $\BHist$ packet
$$
B=(X,Y,W,D,Q,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ and $Y$ are the two source $\RegSeqRatUp$ rows, each read through finite $\StreamNameUp$ windows before a product request is admitted.
\item $W$ is the paired product-window row, recording the synchronized finite observations used for the two inputs.
\item $D$ is the $\DyadicRatCoreUp$ product-budget row, displaying the requested tolerance, endpoint bounds, cross-error budget, and output tolerance for the paired window.
\item $Q$ is the $\CauchyProductUp$ route row that consumes $X,Y,W,D$ as finite product data.
\item $R$ is the regular readback row asserting that the product route remains a $\RegSeqRatUp$ read rather than a host multiplication result.
\item $E$ is the terminal $\RealUp$ seal reached only after the product window, dyadic budget, Cauchy-product route, and regular readback have been displayed.
\item $H$ records componentwise $\hsame$ transport for the two source rows, product window, dyadic budget, product route, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a bilinear product request through $X,Y,W,D,Q,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyBilinearProductUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\CauchyProductUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyBilinearProductUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $X,Y,W,D,Q,R,E,H,C,P,N$ and has no coordinate for quotient streams, host multiplication, trichotomy, selected limits, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Regular Cauchy bilinear product NameCert obligations]
\label{thm:regular-cauchy-bilinear-product-namecert-obligations}
For every accepted $\RegularCauchyBilinearProductUp$ carrier $B=(X,Y,W,D,Q,R,E,H,C,P,N)$, the local $\NameCert_{\RegularCauchyBilinearProductUp}$ surface consists exactly of carrier admission for the two regular rational source rows, paired product-window exposure, dyadic product-budget exactness, Cauchy-product route admission, regular readback preservation, Real-seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-bilinear-product-carrier}. The analytic rows are exactly the two source rows $X,Y$, the paired window $W$, the dyadic budget $D$, the product route $Q$, the readback row $R$, and the seal row $E$.

Read the product-window side first. The row $W$ exposes only finite observations of the two source names, and $D$ records the product tolerance budget used by that window.

The route row $Q$ consumes those displayed rows as a $\CauchyProductUp$ handoff. The readback row $R$ keeps the result inside the regular rational surface, and the seal row $E$ is available only after that readback.

Finally $H,C,P,N$ transport, replay, source, and name the same packet. Since no refused object appears as a coordinate, the listed obligations exhaust the local certificate.
\end{proof}

\begin{theorem}[Regular Cauchy bilinear product window bilinearity]
\label{thm:regular-cauchy-bilinear-product-window-bilinearity}
Every accepted $\RegularCauchyBilinearProductUp$ packet reads bilinearity only through the finite route
$$
X,Y,W,D \longmapsto Q \longmapsto R,E,H,C,P,N .
$$
Thus a downstream algebra consumer may vary either displayed source row inside the same paired window and dyadic budget only by replaying the corresponding $\CauchyProductUp$ route and regular readback; it receives no host multiplication operation, quotient-stream equality, trichotomy branch, selected limit, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start from the carrier $B=(X,Y,W,D,Q,R,E,H,C,P,N)$. The only product-facing source rows are $X$ and $Y$, and the only joint observation row is $W$.

The dyadic budget $D$ records the finite tolerance accounting for the displayed window. Therefore any left-source or right-source product read must pass through the same visible budget before the product route can be consumed.

The row $Q$ is the sole $\CauchyProductUp$ handoff, while $R$ and $E$ are downstream of that handoff. Transport and replay through $H$ and $C$ preserve this order, and $P,N$ only record provenance and local naming.

Since the carrier has no coordinate for host multiplication or quotient equality, the bilinear behavior available to consumers is exactly the window-level replay route described above.
\end{proof}

\begin{theorem}[Regular Cauchy bilinear product Real-seal boundary]
\label{thm:regular-cauchy-bilinear-product-real-seal-boundary}
Every $\RealUp$ consumer of an accepted $\RegularCauchyBilinearProductUp$ packet factors through the two $\RegSeqRatUp$ source rows, the paired $\StreamNameUp$ product window, the $\DyadicRatCoreUp$ product-budget row, the $\CauchyProductUp$ route, and the regular readback row before the terminal seal $E$ is exposed. The packet therefore supplies a non-quotient bilinear product certificate and cannot export quotient streams, host multiplication, trichotomy, selected limits, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-bilinear-product-carrier}. The terminal seal $E$ is downstream of $X,Y,W,D,Q,R$ in the displayed tuple.

Apply \autoref{thm:regular-cauchy-bilinear-product-namecert-obligations}. The local certificate requires paired window exposure, dyadic product-budget exactness, Cauchy-product route admission, and regular readback preservation before any seal handoff.

Use \autoref{thm:regular-cauchy-bilinear-product-window-bilinearity}. A downstream algebra consumer can replay only the finite window route through $Q$ and $R$, so it cannot replace the packet by host multiplication or a selected completed product.

The structural rows $H,C,P,N$ preserve the same route. Row exhaustion blocks quotient streams, trichotomy, selected limits, and ambient completeness from crossing the seal boundary.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyBilinearProductUp$ read reaching a $\RealUp$ seal must display both $\RegSeqRatUp$ source rows, the paired $\StreamNameUp$ window, the $\DyadicRatCoreUp$ product budget, the $\CauchyProductUp$ route, the regular readback row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows.}

\independenceWitness{streamname, dyadicratcore, regseqrat, cauchyproduct, real: $\RegularCauchyBilinearProductUp$ is not bijective to any listed sibling because it jointly carries two source regular rational rows, a paired product window, dyadic product-budget accounting, a Cauchy-product route, regular readback, terminal Real sealing, transport, replay, provenance, and local naming as one bilinear product packet.}

\closureat{RegularCauchyBilinearProductUp}{seedStr}

\begin{closurestatus}{\RegularCauchyBilinearProductUp}
  \constructivestory{A $\RegularCauchyBilinearProductUp$ packet is generated from two RegSeqRat source rows, paired StreamName product windows, a DyadicRatCore product budget, a CauchyProduct route, a regular readback row, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-bilinear-product-carrier}, \autoref{thm:regular-cauchy-bilinear-product-namecert-obligations}, \autoref{thm:regular-cauchy-bilinear-product-window-bilinearity}, and \autoref{thm:regular-cauchy-bilinear-product-real-seal-boundary} seed the finite bilinear product-window route from regular rational sources to product readback and Real seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient streams, host multiplication, trichotomy, selected limits, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, paired product-window exposure, dyadic product-budget exactness, CauchyProduct route admission, regular readback preservation, Real-seal boundary, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyBilinearProductUp.}
\end{closurestatus}
