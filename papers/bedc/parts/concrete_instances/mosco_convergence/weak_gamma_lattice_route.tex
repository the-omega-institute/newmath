The $\MoscoConvergenceUp$ lattice route reads the $\GammaConvergenceUp$ sibling of \autoref{ch:concrete-instances-gamma-convergence-namecert} for liminf and recovery rows, while the weak side is supplied by the weak-topology route of \autoref{ch:concrete-instances-weak-star-topology-namecert} and the weak Hilbert convergence route of \autoref{ch:concrete-instances-weak-convergence-hilbert-namecert}. Those rows enter the carrier before the strong recovery route is read, so a downstream variational consumer receives $F,W,L,R,C,H,T,P,N$ as one finite dependency lattice rather than as separate host claims about weak topology, $\Gamma$-convergence, or strong approximation.
