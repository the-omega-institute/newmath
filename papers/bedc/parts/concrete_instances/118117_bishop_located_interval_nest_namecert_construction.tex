\chapter{A Concrete Naming Certificate for $\BishopLocatedIntervalNestUp$}
\label{ch:concrete-instances-bishop-located-interval-nest-namecert}
\origin{human}

Following Bishop and Bridges on constructive interval arguments, the $\BishopLocatedIntervalNestUp$ packet records a located nested-interval route as finite BEDC data. It sits between the located interval carrier of \autoref{ch:concrete-instances-locatedinterval-namecert}, the nested-chain surface of \autoref{ch:concrete-instances-nestedinterval-namecert}, the dyadic tolerance arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the finite route by which a nested located interval argument can feed Real and Completion consumers; it does not assert open-cover compactness, quotient real equality, selected limits, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop located interval nest carrier]
\label{def:bishop-located-interval-nest-carrier}
A $\BishopLocatedIntervalNestUp$ carrier is a finite $\BHist$ packet
$$
L=(I,D,K,S,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the finite family of $\LocatedIntervalUp$ rows inspected by the consumer.
\item $D$ is the $\DyadicRatCoreUp$ endpoint-margin ledger recording positive dyadic margins for the displayed intervals.
\item $K$ is the $\NestedIntervalUp$ containment spine, stating which displayed interval row is retained at each inspected stage.
\item $S$ is the $\StreamNameUp$ window surface used to read the retained endpoints and margin requests.
\item $Q$ is the $\RegSeqRatUp$ readback row for the finite endpoint windows selected by $S$.
\item $E$ is the terminal $\RealUp$ seal boundary available only after $I,D,K,S,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for located intervals, dyadic margins, containment-spine rows, stream windows, readbacks, seal rows, provenance, replay, and local naming.
\item $C$ records displayed $\Cont$ replay from located-interval reads through endpoint margins, nested containment, stream windows, regular readbacks, and the terminal seal boundary.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedIntervalNestUp$, $\LocatedIntervalUp$, $\NestedIntervalUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopLocatedIntervalNestUp}$ row naming exactly this finite located-nest packet.
\end{enumerate}
The local classifier compares accepted packets only through these rows. It has no coordinate for an infinite host intersection, a chosen real point, quotient equality of real names, open-cover compactness, countable choice, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Bishop located interval nest NameCert obligations]
\label{thm:bishop-located-interval-nest-namecert-obligations}
For every accepted $\BishopLocatedIntervalNestUp$ carrier
$$
L=(I,D,K,S,Q,E,H,C,P,N),
$$
the local $\NameCert_{\BishopLocatedIntervalNestUp}$ surface consists exactly of located-interval exposure through $I$, dyadic endpoint-margin accounting through $D$, nested-containment spine exposure through $K$, finite window selection through $S$, regular rational readback through $Q$, terminal $\RealUp$ seal boundary through $E$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$. A consumer cannot read an interval-nest convergence route without first exposing those rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-located-interval-nest-carrier}. The located intervals, dyadic endpoint margins, nested containment spine, stream windows, regular readbacks, Real seal boundary, and structural rows are the complete visible packet.

The analytic route is componentwise. The located interval rows in $I$ provide endpoint and interval-cell data; the dyadic margin ledger $D$ records positive endpoint margins; and the containment spine $K$ states the finite nesting relations inspected by the consumer.

Next read $S,Q,E$. The stream row selects the finite endpoint windows, the regular rational row supplies their readback, and the Real seal row is exposed only after those windows and margins have been displayed.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier lacks any coordinate for host interval compactness, quotient real equality, selected limits, countable choice, or ambient Real completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Bishop located interval nest regular Cauchy consumer]
\label{thm:bishop-located-interval-nest-regular-cauchy-consumer}
Every $\RegSeqRatUp$ or $\RealUp$ consumer of an accepted $\BishopLocatedIntervalNestUp$ packet must factor through the displayed route
$$
\LocatedIntervalUp \longmapsto \NestedIntervalUp \longmapsto \DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
using the finite rows $I,D,K,S,Q,E,H,C,P,N$. The route may be transported and replayed, but it cannot export a selected point of the infinite nest, quotient real equality, countable-choice data, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the located interval and nesting rows. A consumer first reads $I$ and $K$, so the retained interval stages and their containment spine are visible before any regular Cauchy or Real-facing handoff is available.

Then read the dyadic margin and window rows. The ledger $D$ supplies the positive endpoint-margin data, while $S$ determines the finite windows on which those margins and endpoint reads are inspected.

Now pass to regular rational data and the Real seal. The row $Q$ gives the $\RegSeqRatUp$ readback for the same finite windows, and $E$ is downstream of that readback. Transport, replay, provenance, and local naming through $H,C,P,N$ preserve the same route.

No row remains for a host-selected nest point, quotient equality of real names, countable choice, open-cover compactness, or ambient completeness. The consumer therefore receives only the displayed regular Cauchy handoff.
\end{proof}

\closureat{BishopLocatedIntervalNestUp}{seedStr}

\begin{closurestatus}{\BishopLocatedIntervalNestUp}
  \constructivestory{A $\BishopLocatedIntervalNestUp$ packet is generated from displayed located interval rows, dyadic endpoint-margin rows, a nested containment spine, StreamName windows, RegSeqRat readback, a Real seal boundary, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-located-interval-nest-carrier}, \autoref{thm:bishop-located-interval-nest-namecert-obligations}, and \autoref{thm:bishop-located-interval-nest-regular-cauchy-consumer} seed the finite located-interval-nest route for Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, quotient real equality, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located-interval exposure, dyadic endpoint-margin accounting, nested containment, StreamName window selection, RegSeqRat readback, Real seal boundary, transport, replay, provenance, and local naming for BEDC.Derived.BishopLocatedIntervalNestUp.}
\end{closurestatus}
