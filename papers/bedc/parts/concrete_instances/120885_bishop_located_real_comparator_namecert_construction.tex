\chapter{A Concrete Naming Certificate for $\BishopLocatedRealComparatorUp$}
\label{ch:concrete-instances-bishop-located-real-comparator-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopLocatedRealComparatorUp$ packet records a constructive located comparison witness for displayed $\RealUp$ names as finite NameCert data. It sits beside the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the located-real row of \autoref{ch:concrete-instances-locatedreal-namecert}, and the order-and-apartness surface of \autoref{ch:concrete-instances-real-apartness-field-namecert}. The packet names the finite route by which two real names expose enough located window data for a comparison-facing consumer; it is not host trichotomy, quotient real equality, selected limits, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop located real comparator carrier]
\label{def:bishop-located-real-comparator-carrier}
A $\BishopLocatedRealComparatorUp$ carrier is a finite $\BHist$ packet
$$
K=(X,Y,W_X,W_Y,D_X,D_Y,R_X,R_Y,L,A,J,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ and $Y$ are the two source $\RealUp$ name rows.
\item $W_X$ and $W_Y$ are finite $\StreamNameUp$ windows inspected for the two names.
\item $D_X$ and $D_Y$ are the $\DyadicRatCoreUp$ endpoint-and-tolerance rows read from those windows.
\item $R_X$ and $R_Y$ are the $\RegSeqRatUp$ readback rows that expose the regular rational names behind the two $\RealUp$ seals.
\item $L$ is the $\LocatedRealUp$ locatedness row, displaying the positive-radius or separation window used by the comparison request.
\item $A$ is the $\ApartnessRealUp$ or real-apartness-field row recording the apartness-facing handoff when the displayed windows separate.
\item $J$ is the finite comparator witness row. Its alternatives are window-left, window-right, and overlap-pending, each tied to the displayed dyadic and regular rows rather than to a host order on completed reals.
\item $H$ records componentwise $\hsame$ transport for the two real rows, windows, dyadic ledgers, readbacks, locatedness row, apartness handoff, comparator witness, replay, provenance, and local name.
\item $C$ records displayed $\Cont$ replay from a comparison request through $X,Y,W_X,W_Y,D_X,D_Y,R_X,R_Y,L,A,J$ and onward to provenance and naming.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedRealComparatorUp$, $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\LocatedRealUp$, $\ApartnessRealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopLocatedRealComparatorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows. It has no coordinate for host trichotomy, quotient real equality, selected representatives, countable choice, or an ambient completeness theorem.
\end{definition}

\begin{theorem}[Bishop located real comparator NameCert obligations]
\label{thm:bishop-located-real-comparator-namecert-obligations}
For every accepted $\BishopLocatedRealComparatorUp$ carrier $K=(X,Y,W_X,W_Y,D_X,D_Y,R_X,R_Y,L,A,J,H,C,P,N)$, the local $\NameCert_{\BishopLocatedRealComparatorUp}$ surface consists exactly of two $\RealUp$ source admissions, finite $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance rows, $\RegSeqRatUp$ readbacks, a $\LocatedRealUp$ locatedness row, an $\ApartnessRealUp$ handoff row, the finite comparator witness $J$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier. The analytic rows are precisely the two source real names, their finite observation windows, their dyadic tolerance ledgers, their regular rational readbacks, the locatedness row, the apartness handoff, and the comparator witness.

Read the comparator witness only after those rows are displayed. In a window-left branch, $J$ records that the inspected $X$ window is separated below the inspected $Y$ window by the dyadic and regular rows. In a window-right branch, the same finite evidence is read with the two sources exchanged. In an overlap-pending branch, $J$ records that the displayed data do not license a strict comparison at this tolerance.

The structural rows $H,C,P,N$ transport, replay, source, and name exactly that finite surface. Since no carrier coordinate names host trichotomy, quotient equality, selected representatives, countable choice, or completeness, none of those objects can be exported by the local NameCert row.
\end{proof}

\begin{theorem}[Bishop located real comparator stability]
\label{thm:bishop-located-real-comparator-stability}
Let $K=(X,Y,W_X,W_Y,D_X,D_Y,R_X,R_Y,L,A,J,H,C,P,N)$ be an accepted $\BishopLocatedRealComparatorUp$ carrier. Any componentwise $\hsame$ transport recorded in $H$ preserves the same comparator alternative in $J$ together with the same $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\LocatedRealUp$, $\ApartnessRealUp$, $\Pkg$, and $\NameCert$ rows.
\end{theorem}
\begin{proof}
Inspect the transport row $H$. It is componentwise over the displayed source, window, dyadic, readback, locatedness, apartness, comparator, replay, provenance, and naming rows.

Case-split on $J$. In the window-left branch, transport preserves the two windows and their dyadic separation witness before the left comparison is read. In the window-right branch, the same argument preserves the exchanged separation witness. In the overlap-pending branch, transport preserves the absence of a licensed strict comparison at the displayed tolerance.

The continuation row $C$ replays the transported rows in the same order, and $P,N$ only record provenance and local naming. Thus stability is local to the carrier and does not introduce a host order, quotient equality, or completeness principle.
\end{proof}

\begin{theorem}[Bishop located real comparator non-escape]
\label{thm:bishop-located-real-comparator-nonescape}
Every $\BishopLocatedRealComparatorUp$ consumer read factors through the finite route
$$
X,Y,W_X,W_Y,D_X,D_Y,R_X,R_Y,L,A,J,H,C,P,N.
$$
The packet exports no host trichotomy for $\RealUp$, no quotient real equality test, no selected representative, no countable-choice schedule, and no ambient completeness witness.
\end{theorem}
\begin{proof}
Start from the carrier inventory. Its only comparison row is $J$, and $J$ is downstream of the displayed real names, finite stream windows, dyadic tolerance rows, regular rational readbacks, locatedness row, and apartness handoff.

Any consumer route must then pass through $H$ and $C$ to transport and replay the same displayed rows. The provenance and local naming rows record where the finite packet came from, but they do not add comparison data.

Therefore the listed route is exhaustive. The refused host objects are absent from the carrier and cannot be projected from an accepted packet.
\end{proof}

\closureat{BishopLocatedRealComparatorUp}{seedStr}

\begin{closurestatus}{\BishopLocatedRealComparatorUp}
  \constructivestory{A $\BishopLocatedRealComparatorUp$ packet is generated from two RealUp source rows, finite StreamName windows, DyadicRatCore tolerance ledgers, RegSeqRat readbacks, a LocatedReal row, an ApartnessReal handoff, a finite comparator witness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-located-real-comparator-carrier}, \autoref{thm:bishop-located-real-comparator-namecert-obligations}, \autoref{thm:bishop-located-real-comparator-stability}, and \autoref{thm:bishop-located-real-comparator-nonescape} seed the finite located comparison witness for displayed RealUp names.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host trichotomy, quotient real equality, selected representatives, countable choice, ambient Real completeness, Lean verification, or bridge checking for $\BishopLocatedRealComparatorUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-window comparison exactness, locatedness and apartness handoff, transport stability, provenance, local naming, and non-escape for BEDC.Derived.BishopLocatedRealComparatorUp.}
\end{closurestatus}
