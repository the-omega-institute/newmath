\chapter{A Concrete Naming Certificate for $\ModelSelectionBoundaryUp$}
\label{ch:concrete-instances-model-selection-boundary-namecert}
\origin{ai}

The $\ModelSelectionBoundaryUp$ packet realizes the comparative-selection boundary of \autoref{sec:reality-constrained-synthesis} as a finite BEDC NameCert carrier for reality-constrained physical truth. It sits beside the open-fit packet of \autoref{ch:concrete-instances-open-fit-packet-namecert}, the model-audit packet of \autoref{ch:concrete-instances-reality-constrained-model-audit-namecert}, the signature-fit packet of \autoref{ch:concrete-instances-reality-constrained-signature-fit-namecert}, and the synthesis seal of \autoref{ch:concrete-instances-realityconstrainedsynthesisseal-namecert}. It also consumes the truth-certificate discipline of \autoref{ch:concrete-instances-physical-truth-certificate-namecert}: those siblings expose fit, audit, signature, synthesis, and truth-certificate faces, while this boundary records which finite candidate currently wins and which displayed defeats keep that selection provisional.

\begin{definition}[ModelSelectionBoundary carrier]
\label{def:model-selection-boundary-carrier}
A $\ModelSelectionBoundaryUp$ carrier is a finite $\BHist$ packet
$$
B=(M,S,F,D,L,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the finite model-family row listing the accepted candidate packets under comparison.
\item $S$ is the observed-signature row shared by the comparison.
\item $F$ is the comparative-fit row recording the selected packet, its displayed fit score, and the losing rows that remain accepted but not selected.
\item $D$ is the defeat-surface row naming finite observations, signature mismatches, audit failures, domain exits, and ledger conflicts that overturn the displayed selection.
\item $L$ is the selection-ledger row recording admissibility, tie policy, defeat visibility, provenance, and refused final-oracle demands.
\item $T$ is the componentwise transport row preserving the candidate family, observed signatures, fit comparison, defeat surface, and ledger under displayed $\hsame$ routes.
\item $P$ is the $\Pkg$ provenance row over $\ModelSelectionBoundaryUp$, $\OpenFitPacketUp$, $\RealityConstrainedModelAuditUp$, $\RealityConstrainedSignatureFitUp$, $\RealityConstrainedSynthesisSealUp$, $\PhysicalTruthCertificateUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\ModelSelectionBoundaryUp}$ row naming the whole boundary.
\end{enumerate}
The carrier has no coordinate for model--reality identity, total confirmation, a hidden winner outside $M$, an erased losing row, an unledgered defeat, or a final oracle deciding all future observations.
\end{definition}

\begin{theorem}[ModelSelectionBoundary fit-ledger separation]
\label{thm:model-selection-boundary-fit-ledger-separation}
For an accepted $\ModelSelectionBoundaryUp$ carrier $B=(M,S,F,D,L,T,P,N)$, the selected row in $F$ is a finite ledgered selection from the displayed candidate family $M$, not a truth identity between the selected model and physical reality. Every public selection read must expose the observed signatures $S$, the comparative fit row $F$, the losing accepted rows recorded by $F$, the selection ledger $L$, the transport row $T$, provenance $P$, and local name $N$ before it can be consumed as a reality-constrained physical-truth certificate.
\end{theorem}
\leanchecked{BEDC.Derived.ModelSelectionBoundaryUp.TasteGate.ModelSelectionBoundaryTasteGate\_single\_carrier\_alignment}
\begin{proof}
Carrier admission exposes exactly $B=(M,S,F,D,L,T,P,N)$. The only row that selects a candidate is $F$, and $F$ is indexed by the finite family $M$ and the displayed signature row $S$.

The ledger row $L$ separates acceptance from selection. A losing row may remain an accepted packet while failing to be the displayed winner; conversely, the winning row is selected only under the current finite comparison and ledger policy.

The transport and provenance rows keep the comparison public. Since $T,P,N$ preserve the same candidate family, signature, fit, ledger, and local name, no consumer can replace the selected row by hidden model identity or final physical truth.
\end{proof}

\begin{theorem}[ModelSelectionBoundary defeat surface]
\label{thm:model-selection-boundary-defeat-surface}
An accepted $\ModelSelectionBoundaryUp$ packet is locally refuted whenever a competing accepted packet in $M$ beats the displayed selected row of $F$ while the relevant finite defeat is hidden from $D$ or erased from $L$. A selection that keeps $D$ and $L$ visible remains a current finite winner; a selection that suppresses the defeat surface is outside the boundary.
\end{theorem}
\leanchecked{BEDC.Derived.ModelSelectionBoundaryUp.TasteGate.ModelSelectionBoundary\_defeat\_surface}
\begin{proof}
Read the carrier rows componentwise. The family row $M$ supplies the competing accepted packets, the signature row $S$ supplies the shared observation surface, and the comparative-fit row $F$ records the selected and losing rows.

If a competing accepted packet beats the selected row, the defeat must be visible in $D$ and ledgered in $L$. Those two rows are the only coordinates that expose overturned fit, mismatch, audit failure, domain exit, or policy conflict.

When the defeat is hidden or erased, the claimed selection asks the packet to export a winning status without the displayed boundary rows that make the comparison auditable. Such a route is not an accepted $\ModelSelectionBoundaryUp$ read.
\end{proof}

\begin{definition}[ModelSelectionBoundary obligation carrier]
\label{def:modelselectionboundary-obligation-carrier}
A $\ModelSelectionBoundaryUp$ obligation carrier is a finite $\BHist$ packet
$$
O=(B,F,D,L,A,S,T,P,N)
$$
where $B$ is an accepted $\ModelSelectionBoundaryUp$ carrier, $F$ is its comparative fit ledger, $D$ is its defeat surface, $L$ is its selection ledger, $A$ is the audit route through \autoref{ch:concrete-instances-open-fit-packet-namecert}, \autoref{ch:concrete-instances-reality-constrained-model-audit-namecert}, \autoref{ch:concrete-instances-reality-constrained-signature-fit-namecert}, \autoref{ch:concrete-instances-realityconstrainedsynthesisseal-namecert}, and \autoref{ch:concrete-instances-physical-truth-certificate-namecert}, $S$ is the corresponding TasteGate alignment row, $T$ is componentwise $\hsame$ transport, $P$ is $\Pkg$ provenance, and $N$ is the local $\NameCert$ row. The carrier has no coordinate for a hidden model oracle, probability semantics, host truth predicate, or final winner outside the displayed finite family.
\end{definition}

\begin{theorem}[ModelSelectionBoundary NameCert obligations]
\label{thm:modelselectionboundary-namecert-obligations}
For every accepted obligation carrier $O=(B,F,D,L,A,S,T,P,N)$, the $\ModelSelectionBoundaryUp$ NameCert obligation surface consists exactly of the fit ledger, defeat surface, selection ledger, sibling audit route, TasteGate alignment row, componentwise transport, package provenance, and local naming displayed in $O$. The selected model remains a current finite winner only relative to those rows.
\end{theorem}
\begin{proof}
Project the obligation carrier. The rows $F,D,L$ are inherited from the existing fit-ledger and defeat-surface theorems, so selection is exposed as a ledgered finite comparison rather than as an oracle.

The sibling audit route $A$ forces every selection read through open-fit, model-audit, signature-fit, synthesis-seal, and truth-certificate surfaces. The TasteGate row $S$ promotes that finite route only as single-carrier alignment, not as probability semantics or host truth.

Finally $T,P,N$ preserve, source, and name the same displayed rows. Since the carrier contains no hidden oracle, final winner outside the candidate family, probability space, or host truth predicate, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[ModelSelectionBoundary audit scope]
\label{thm:modelselectionboundary-audit-scope}
Every audit of a $\ModelSelectionBoundaryUp$ selection is limited to the displayed candidate family, observed signatures, fit ledger, defeat surface, selection ledger, sibling audit route, and TasteGate alignment row. A later observation may defeat the current winner by adding a visible row to $D$ and $L$, but the packet never exports a host-level final model order, probability semantics, or truth predicate.
\end{theorem}
\begin{proof}
Read the audit rows in order. The candidate family and observed signatures determine which finite rows are under comparison, while $F$ records the current selection and losing accepted rows.

The defeat and ledger rows $D,L$ are the only places where a later observation, mismatch, audit failure, domain exit, or ledger conflict can overturn the selection. If such a row is absent, the packet records only the current finite winner; if it is present, the winner is visibly defeated.

The sibling audit route and TasteGate alignment row keep the promotion local to displayed BEDC data. They do not add probability semantics, a host truth predicate, or a final model order, so the audit scope is exactly the finite surface stated above.
\end{proof}

\falsifiablePrediction{A selected model packet is refuted whenever its displayed fit row is beaten by a competing accepted packet while the defeat surface is hidden or erased.}

\independenceWitness{openfitpacket, realityconstrainedmodelaudit, realityconstrainedsignaturefit, realityconstrainedsynthesisseal, physicaltruthcertificate: none is bijective because each supplies one fit, audit, signature, synthesis, or truth-certificate face, while ModelSelectionBoundaryUp carries the comparative candidate family, selected row, losing rows, finite defeat surface, ledger, transport, and no-final-oracle refusal together.}

The Lean follow-up in \texttt{lean4/BEDC/Derived/ModelSelectionBoundaryUp/TasteGate.lean} provides explicit $\mathsf{Nontrivial}\ \ModelSelectionBoundaryUp$ and $\mathsf{FieldFaithful}\ \ModelSelectionBoundaryUp$ instances preserving $M,S,F,D,L,T,P,N$, together with the target \texttt{BEDC.Derived.ModelSelectionBoundaryUp.TasteGate.ModelSelectionBoundaryTasteGate\_single\_carrier\_alignment}.

\closureat{ModelSelectionBoundaryUp}{seedStr}
\closureat{ModelSelectionBoundaryUp}{obligationStr}

\begin{closurestatus}{\ModelSelectionBoundaryUp}
  \constructivestory{A $\ModelSelectionBoundaryUp$ packet is generated from a finite model-family row, observed signatures, comparative fit, finite defeat surface, selection ledger, componentwise transport, package provenance, and local NameCert row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:model-selection-boundary-carrier}, \autoref{thm:model-selection-boundary-fit-ledger-separation}, \autoref{thm:model-selection-boundary-defeat-surface}, \autoref{def:modelselectionboundary-obligation-carrier}, \autoref{thm:modelselectionboundary-namecert-obligations}, \autoref{thm:modelselectionboundary-audit-scope}, and \leanchecked{BEDC.Derived.ModelSelectionBoundaryUp.taste\_gate} bind comparative model selection to finite fit, ledgered defeat, obligation carrier rows, NameCert obligations, audit scope, TasteGate instance evidence, and no-final-oracle refusal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not assert final physical truth, model--reality identity, total confirmation, host-level model ordering, Lean verification, or bridge checking for $\ModelSelectionBoundaryUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, comparative-fit ledger separation, defeat-surface visibility, TasteGate nontriviality, and field faithfulness for $M,S,F,D,L,T,P,N$.}
\end{closurestatus}
