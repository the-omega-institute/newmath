\chapter{A Concrete Naming Certificate for $\FiniteRegularStepFunctionEnvelopeUp$}
\label{ch:concrete-instances-finite-regular-step-function-envelope-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\FiniteRegularStepFunctionEnvelopeUp$ packet records the finite step-envelope data used before a regulated-function approximation is read as a regular rational sequence and then sealed as a real name. It sits beside the regulated-function carrier of \autoref{ch:concrete-instances-regulated-function-namecert}, the dyadic step-function surface of \autoref{ch:concrete-instances-dyadicstepfunction-namecert}, the dyadic step-approximation route of \autoref{ch:concrete-instances-dyadic-step-approximation-namecert}, the uniform partition carrier of \autoref{ch:concrete-instances-uniform-partition-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite envelope interface; it is not a quotient of functions, a selected limiting process, a measure-completion object, or an ambient $\RealUp$ completeness theorem.

\begin{definition}[Finite regular step-function envelope carrier]
\label{def:finite-regular-step-function-envelope-carrier}
A $\FiniteRegularStepFunctionEnvelopeUp$ carrier is a finite $\BHist$ packet
$$
E=(G,U,D,A,Q,R,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $G$ is the $\RegulatedFunctionUp$ source row, exposing the finite regulated-function request and its local oscillation schedule.
\item $U$ is the $\UniformPartitionUp$ row, naming the finite partition cells on which the envelope is inspected.
\item $D$ is the $\DyadicStepFunctionUp$ row attached to the displayed partition cells.
\item $A$ is the $\DyadicStepApproximationUp$ row recording the step approximation and its oscillation ledger.
\item $Q$ is the finite envelope row, listing the lower and upper step bounds on every displayed cell.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the finite step values and envelope bounds.
\item $S$ is the terminal $\RealUp$ seal route, available only after $G,U,D,A,Q,R$ have all been displayed.
\item $H$ records componentwise $\hsame$ transport for the source, partition, step, approximation, envelope, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a regulated-function request through the finite envelope route.
\item $P$ is the $\Pkg$ provenance over $\FiniteRegularStepFunctionEnvelopeUp$, $\RegulatedFunctionUp$, $\DyadicStepFunctionUp$, $\DyadicStepApproximationUp$, $\UniformPartitionUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteRegularStepFunctionEnvelopeUp}$ row.
\end{enumerate}
The classifier compares packets only through $G,U,D,A,Q,R,S,H,C,P,N$. It has no coordinate for host function equality, selected mesh limits, measure completion, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[Finite regular step-function envelope obligations]
\label{thm:finite-regular-step-function-envelope-obligations}
For every accepted $\FiniteRegularStepFunctionEnvelopeUp$ carrier
$$
E=(G,U,D,A,Q,R,S,H,C,P,N),
$$
the local $\NameCert_{\FiniteRegularStepFunctionEnvelopeUp}$ surface consists exactly of carrier admission for the regulated-function source $G$, uniform partition $U$, dyadic step row $D$, dyadic step-approximation row $A$, finite envelope row $Q$, regular rational readback $R$, terminal real seal $S$, componentwise transport $H$, displayed replay $C$, package provenance $P$, and local naming $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-regular-step-function-envelope-carrier}. The analytic rows are exactly the regulated-function source, the displayed uniform partition, the dyadic step row, the step-approximation row, the envelope bounds, the regular rational readback, and the terminal real seal.

The finite envelope row $Q$ is read after the partition and step rows. It records lower and upper step bounds on the displayed cells, so a consumer cannot replace it by an ambient supremum, an unrecorded mesh limit, or a quotient class of functions.

The completion-facing route is downstream. The row $R$ records the regular rational readback obtained from the finite step data, and $S$ seals the real read only after that readback is displayed. Transport, replay, provenance, and local naming preserve the same packet, so the listed rows exhaust the NameCert surface.
\end{proof}

\falsifiablePrediction{A $\FiniteRegularStepFunctionEnvelopeUp$ classifier is refuted by any accepted regulated-function request whose displayed finite partition and dyadic step rows agree but whose envelope row omits a cellwise lower or upper bound needed by the $\RegSeqRatUp$ readback.}

\independenceWitness{regulated-function, dyadic-step-function, dyadic-step-approximation, uniform-partition, regseqrat, real: none is bijective to $\FiniteRegularStepFunctionEnvelopeUp$ because this packet jointly carries the regulated source, partition cells, dyadic step data, approximation ledger, envelope bounds, regular rational readback, terminal real seal, transport, replay, provenance, and local NameCert rows.}

The Lean follow-up namespace is \texttt{BEDC.Derived.FiniteRegularStepFunctionEnvelopeUp}; it should provide the carrier and obligation surface without importing a host quotient of regulated functions or an ambient completeness principle.

\closureat{FiniteRegularStepFunctionEnvelopeUp}{seedStr}

\begin{closurestatus}{\FiniteRegularStepFunctionEnvelopeUp}
  \constructivestory{A $\FiniteRegularStepFunctionEnvelopeUp$ packet is generated from a regulated-function source, a uniform partition, dyadic step data, a dyadic step-approximation ledger, finite envelope bounds, a RegSeqRat readback, a Real seal, componentwise hsame transport, Cont replay, Pkg provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-regular-step-function-envelope-carrier} and \autoref{thm:finite-regular-step-function-envelope-obligations} seed the finite route from regulated-function step envelopes through RegSeqRat readback to the Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, host equality of regulated functions, selected limiting meshes, measure completion, countable choice, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, cellwise envelope exactness, regular rational readback, real-seal routing, transport stability, replay stability, provenance coverage, and local naming for BEDC.Derived.FiniteRegularStepFunctionEnvelopeUp.}
\end{closurestatus}
