\chapter{A Concrete Naming Certificate for $\HyperbolicGeodesicTriangleUp$}
\label{ch:concrete-instances-hyperbolic-geodesic-triangle-namecert}
\origin{human}

Following classical hyperbolic geometry, the $\HyperbolicGeodesicTriangleUp$ packet records a three-side geodesic triangle as finite BEDC NameCert data inside the hyperbolic observer-expansion vision of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion}. It sits between the boundary quadruple route of \autoref{ch:concrete-instances-hyperbolic-ideal-quadrilateral-namecert}, the disk-geodesic triangle route of \autoref{ch:concrete-instances-poincare-disk-geodesic-triangle-namecert}, the hyperbolic metric packet of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the geodesic boundary packet of \autoref{ch:concrete-instances-hyperbolic-geodesic-boundary-namecert}, the cross-ratio packet of \autoref{ch:concrete-instances-hyperbolic-cross-ratio-namecert}, and the disk carrier of \autoref{ch:concrete-instances-unitdisk-namecert}. The chapter names the triangle-level carrier needed between ideal quadrilateral data and later phase-geometry consumers; it does not complete a hyperbolic plane, quotient boundary endpoints, select global geodesic representatives, or identify the observer-expansion reading with physical spacetime.

\begin{definition}[Hyperbolic geodesic triangle carrier]
\label{def:hyperbolic-geodesic-triangle-carrier}
A $\HyperbolicGeodesicTriangleUp$ carrier is a finite $\BHist$ packet
$$
T=(U,M,V,G,I,A,X,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ is the $\UnitDiskUp$ coordinate row for the inspected disk window and its boundary reads.
\item $M$ is the $\HyperbolicMetricUp$ metric-read row over the same displayed disk data.
\item $V$ is the three-boundary-vertex row, naming the ordered boundary or disk-facing vertices visible to the packet.
\item $G$ records the three displayed $\HyperbolicGeodesicBoundaryUp$ side rows joining the vertex pairs.
\item $I$ is the incidence and cyclic-order ledger, checking that the side rows meet the displayed vertices in the stated order and that no fourth side is introduced.
\item $A$ is the angle or orientation budget row, recording the finite comparison data available for triangle reads without importing an ambient smooth angle measure.
\item $X$ is the $\HyperbolicCrossRatioUp$ readback row attached to the same ordered triple and to an auxiliary displayed comparison end when such a read is requested.
\item $H$ records componentwise $\hsame$ transport for disk coordinates, metric reads, vertices, sides, incidence, angle or order data, cross-ratio readback, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a hyperbolic triangle request through $U,M,V,G,I,A,X,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicGeodesicTriangleUp$, $\UnitDiskUp$, $\HyperbolicMetricUp$, $\HyperbolicGeodesicBoundaryUp$, $\HyperbolicCrossRatioUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HyperbolicGeodesicTriangleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $U,M,V,G,I,A,X,H,C,P,N$. It has no coordinate for completed hyperbolic planes, quotient ideal-boundary equality, selected global geodesics, ambient smooth angle structures, host cross-ratio fields, or physical observer dynamics.
\end{definition}

\begin{theorem}[Hyperbolic geodesic triangle NameCert obligations]
\label{thm:hyperbolic-geodesic-triangle-namecert-obligations}
For every accepted $\HyperbolicGeodesicTriangleUp$ carrier
$$
T=(U,M,V,G,I,A,X,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicGeodesicTriangleUp}$ surface consists exactly of disk-coordinate admission for $U$, hyperbolic-metric visibility for $M$, three-vertex admission for $V$, three geodesic-side admissions for $G$, incidence and cyclic-order ledger exposure for $I$, finite angle or orientation budget exposure for $A$, finite cross-ratio readback for $X$, componentwise $\hsame$ transport for $H$, displayed $\Cont$ replay for $C$, $\Pkg$ provenance for $P$, and local naming for $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-geodesic-triangle-carrier}. The disk and metric rows $U,M$ are the only geometric source rows, and the vertex row $V$ fixes the three displayed ends or disk-facing vertices.

Read the side and incidence data next. The row $G$ lists exactly three geodesic sides, while $I$ records their endpoint incidence and cyclic order, so a fourth side or detached boundary representative cannot be inserted.

The comparison rows are downstream. The row $A$ stores only the finite angle or orientation budget available to the packet, and $X$ stores only the finite cross-ratio readback attached to the same displayed triple and auxiliary comparison end.

Rows $H,C,P,N$ transport, replay, source, and locally name the same packet. Since no carrier coordinate stores completed planes, quotient boundary equality, selected geodesics, ambient smooth angle structures, host cross-ratio fields, or physical observer dynamics, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Hyperbolic geodesic triangle finite readback]
\label{thm:hyperbolic-geodesic-triangle-finite-readback}
Every triangle-level consumer of an accepted $\HyperbolicGeodesicTriangleUp$ packet must read
$$
\UnitDiskUp,\HyperbolicMetricUp,\HyperbolicGeodesicBoundaryUp,\HyperbolicCrossRatioUp
$$
through the displayed rows $U,M,V,G,I,A,X,H,C,P,N$ before exporting a metric, incidence, angle-budget, or cross-ratio comparison. The export is a finite NameCert route between ideal quadrilateral data and later hyperbolic phase-geometry consumers; it is not a completed hyperbolic model, quotient boundary construction, selected geodesic representative, smooth angle theory, host field theorem, or physical observer trajectory.
\end{theorem}
\begin{proof}
Begin with the source rows $U,M,V$. They name the disk window, metric read, and three visible vertices used by the packet.

Use $G,I$ to form the triangle spine. These rows display the three geodesic sides and their endpoint incidence before any comparison row is read.

Apply the finite comparison rows $A,X$. The angle or order budget and cross-ratio readback are attached to the same displayed triangle data and cannot be replaced by a host smooth angle measure or global cross-ratio field.

Finally replay the structural rows $H,C,P,N$. They preserve the finite source, side, incidence, comparison, provenance, and local naming data, so the exported triangle read remains inside the NameCert route stated above.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicGeodesicTriangleUp$ read must expose UnitDisk coordinates, HyperbolicMetric reads, three boundary vertices, three HyperbolicGeodesicBoundary side rows, incidence or cyclic-order data, finite angle or orientation budget rows, HyperbolicCrossRatio readback, hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}

\independenceWitness{unitdisk, hyperbolic-metric, hyperbolic-geodesic-boundary, hyperbolic-cross-ratio, hyperbolic-ideal-quadrilateral, poincare-disk-geodesic-triangle: $\HyperbolicGeodesicTriangleUp$ is not bijective to any listed sibling because it jointly binds three displayed vertices, three side rows, incidence or order data, a finite angle or orientation budget, cross-ratio readback, transport, replay, provenance, and local naming in one triangle packet.}

\closureat{HyperbolicGeodesicTriangleUp}{seedStr}

\begin{closurestatus}{\HyperbolicGeodesicTriangleUp}
  \constructivestory{A $\HyperbolicGeodesicTriangleUp$ packet is generated from UnitDisk coordinate rows, HyperbolicMetric reads, three displayed boundary vertices, three HyperbolicGeodesicBoundary side rows, incidence or cyclic-order ledgers, finite angle or orientation budget rows, HyperbolicCrossRatio readback, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-geodesic-triangle-carrier}, \autoref{thm:hyperbolic-geodesic-triangle-namecert-obligations}, and \autoref{thm:hyperbolic-geodesic-triangle-finite-readback} seed the finite triangle route over disk coordinates, hyperbolic metric rows, displayed geodesic sides, incidence ledgers, comparison budgets, cross-ratio readback, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed hyperbolic planes, quotient ideal-boundary equality, selected global geodesics, ambient smooth angle structures, host cross-ratio fields, physical observer dynamics, Lean verification, or bridge checking for $\HyperbolicGeodesicTriangleUp$.}
  \upgradepath{Obligation closure requires checked disk-coordinate admission, hyperbolic-metric visibility, three-vertex admission, geodesic-side exactness, incidence and cyclic-order stability, angle or orientation budget exposure, cross-ratio readback, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicGeodesicTriangleUp.}
\end{closurestatus}
