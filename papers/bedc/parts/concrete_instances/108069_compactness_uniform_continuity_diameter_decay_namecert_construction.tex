\chapter{A Concrete Naming Certificate for $\CompactnessUniformContinuityDiameterDecayUp$}
\label{ch:concrete-instances-compactness-uniform-continuity-diameter-decay-namecert}
\origin{human}

Following Bishop and Bridges, the $\CompactnessUniformContinuityDiameterDecayUp$ packet realizes the compact finite-net route from continuous pointwise control to a diameter-decay uniform-modulus certificate. It sits over the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the triangle estimate of \autoref{ch:concrete-instances-metric-triangle-uniform-estimate-namecert}, and the real endpoint seal of \autoref{ch:concrete-instances-real-namecert}.

The packet records diameter decay as finite certificate data: compact-net windows, local continuous-map oscillation bounds, a common uniform-modulus handoff, source and target metric triangle rows, and a terminal $\RealUp$ seal. It does not appeal to open-cover compactness, a selected subsequence, a choice-selected center outside the bundle, semantic continuity, or ambient real completeness.

\begin{definition}[Compact uniform-continuity diameter-decay carrier]
\label{def:compactness-uniform-continuity-diameter-decay}
A $\CompactnessUniformContinuityDiameterDecayUp$ carrier is a finite $\BHist$ packet
$$
D=(K,F,M,T,E,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row carrying finite-net centers, located source-distance reads, and compact bundle coverage;
\item $F$ is the $\ContinuousMapUp$ row exposing the graph and pointwise modulus data for the displayed map into a $\RealUp$ target;
\item $M$ is the $\UniformModulusUp$ handoff row obtained from the compact finite-net fold and consumed by downstream uniform-continuity readers;
\item $T$ is the $\MetricTriangleUniformEstimateUp$ row joining the selected compact-net center, two source-distance bounds, two target estimates, and the final target triangle estimate;
\item $E$ is the $\RealUp$ endpoint seal through which the target-diameter and oscillation reads must pass;
\item $H$ records componentwise $\hsame$ transport for the compact source, continuous-map row, uniform-modulus row, triangle estimate, and real seal;
\item $C$ records displayed $\Cont$ replay of the finite-net coverage, pointwise-modulus read, uniform-modulus handoff, and triangle-estimate route;
\item $P$ is the $\Pkg$ provenance over $\CompactnessUniformContinuityDiameterDecayUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\MetricTriangleUniformEstimateUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\CompactnessUniformContinuityDiameterDecayUp}$ row.
\end{enumerate}
The classifier compares two packets only through the displayed rows $K,F,M,T,E,H,C,P,N$. It has no coordinate for host compactness, selected subsequences, choice outside the finite bundle, semantic continuity, quotient equality, or real completeness.
\end{definition}

\begin{theorem}[Diameter-decay NameCert obligations]
\label{thm:compactness-uniform-continuity-diameter-decay-namecert-obligations}
For every accepted $\CompactnessUniformContinuityDiameterDecayUp$ carrier $D=(K,F,M,T,E,H,C,P,N)$, the local $\NameCert$ obligation surface consists exactly of compact-metric admission through $K$, continuous-map admission through $F$, uniform-modulus handoff through $M$, metric-triangle uniform estimate through $T$, real-seal admission through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compactness-uniform-continuity-diameter-decay}. The finite packet exposes exactly $K,F,M,T,E,H,C,P,N$.

Read the compact-continuity rows first. The compact metric row supplies the finite-net centers and located source-distance reads, while the continuous-map row supplies the pointwise target estimates at those centers.

Next read the diameter-decay handoff. The uniform-modulus row receives a precision chosen from the finite compact-net fold, and the metric-triangle row converts the selected center route into the displayed target-diameter estimate.

The real seal, transport, replay, provenance, and local naming rows close the obligation surface. Since host compactness, selected subsequences, choice outside the finite bundle, semantic continuity, quotient equality, and real completeness are absent from the carrier, they cannot be exported by the local certificate.
\end{proof}

\begin{theorem}[Compact diameter-decay uniform-modulus route]
\label{thm:compactness-uniform-continuity-diameter-decay-route}
Every $\UniformModulusUp$ consumer of an accepted $\CompactnessUniformContinuityDiameterDecayUp$ packet receives the compact finite-net row, the continuous-map pointwise rows, the metric-triangle uniform estimate, and the terminal $\RealUp$ seal as one finite route:
$$
\CompactMetricUp \longmapsto \ContinuousMapUp \longmapsto \MetricTriangleUniformEstimateUp \longmapsto \UniformModulusUp \longmapsto \RealUp .
$$
Thus the diameter-decay certificate is a finite compact-continuous image packet, not a semantic compactness theorem or an external modulus selector.
\end{theorem}
\begin{proof}
Start from the compact metric row $K$. It supplies the finite centers and source-distance reads used to measure the diameter of each displayed image window.

Read the continuous-map row $F$ at the selected centers. The pointwise modulus data are tied to the same source rows that $K$ exposes, so a consumer cannot choose a detached center or a detached target estimate.

Apply the triangle-estimate row $T$ and then the uniform-modulus handoff $M$. The source and target triangle rows convert the local estimates into a single diameter-decay precision consumed by $\UniformModulusUp$.

Finally consume the real seal $E$ and replay the route through $H,C,P,N$. The consumer obtains only the finite packet data, so open-cover compactness, selected subsequences, choice outside the bundle, semantic continuity, and real completeness remain unavailable.
\end{proof}

\closureat{CompactnessUniformContinuityDiameterDecayUp}{seedStr}

\begin{closurestatus}{\CompactnessUniformContinuityDiameterDecayUp}
  \constructivestory{A $\CompactnessUniformContinuityDiameterDecayUp$ packet is generated from $\CompactMetricUp$ finite-net rows, $\ContinuousMapUp$ pointwise modulus data, a $\UniformModulusUp$ handoff, a $\MetricTriangleUniformEstimateUp$ row, a $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ data.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compactness-uniform-continuity-diameter-decay}, \autoref{thm:compactness-uniform-continuity-diameter-decay-namecert-obligations}, and \autoref{thm:compactness-uniform-continuity-diameter-decay-route} seed the finite diameter-decay route from compact metric finite nets and continuous-map pointwise rows to a uniform-modulus consumer.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, semantic compactness, selected subsequences, choice outside the finite bundle, quotient equality, open-cover compactness, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires binding the compact finite-net, continuous-map, uniform-modulus, triangle-estimate, real-seal, transport, replay, provenance, and local naming rows to exact BEDC primitives and a Lean theorem surface.}
\end{closurestatus}
