\chapter{A Concrete Naming Certificate for $\FiniteDyadicBracketingModulusUp$}
\label{ch:concrete-instances-finite-dyadic-bracketing-modulus-namecert}
\origin{human}

Bishop--Bridges grounding for constructive real analysis treats real approximation by finite rational brackets and explicit moduli before any completed real is consumed. The $\FiniteDyadicBracketingModulusUp$ packet records that finite means as BEDC NameCert data: it sits over the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the metric-facing distance route of \autoref{ch:concrete-instances-real-metric-namecert}. The packet supplies bracket evidence for Real-and-Completion consumers without asserting $\RealUp$ completeness, quotient streams, selected limits, trichotomy, or countable choice.

\begin{definition}[Finite dyadic bracketing-modulus carrier]
\label{def:finite-dyadic-bracketing-modulus-carrier}
A $\FiniteDyadicBracketingModulusUp$ carrier is a finite $\BHist$ packet
$$
B=(D,S,R,L,U,M,E,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ ledger containing dyadic denominators, positive radii, midpoint arithmetic, endpoint comparisons, and slack inequalities for the bracket.
\item $S$ is the $\StreamNameUp$ finite-window schedule naming exactly which observation windows are inspected.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the same stream windows and dyadic tolerances.
\item $L$ and $U$ are the lower and upper dyadic bracket rows, with $L \leq U$ witnessed inside $D$.
\item $M$ is the finite modulus row assigning each admitted precision request to a displayed window in $S$ and a displayed dyadic bracket in $D,L,U$.
\item $E$ is the terminal $\RealUp$ seal exposed only after $D,S,R,L,U,M$ have been displayed.
\item $Q$ is the $\RealMetricUp$ distance-read row available to metric consumers only through the same dyadic bracket and readback route.
\item $H$ stores componentwise $\hsame$ transport for the dyadic ledger, window schedule, regular readback, bracket endpoints, modulus row, Real seal, metric read, provenance, and local name.
\item $C$ stores displayed $\Cont$ replay from a precision request through $M,S,D,L,U,R,E,Q,H$.
\item $P$ is the $\Pkg$ provenance over $\FiniteDyadicBracketingModulusUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\RealMetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteDyadicBracketingModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for $\RealUp$ completeness, quotient streams, selected limits, trichotomy, countable choice, host real equality, or an unbounded completion selector.
\end{definition}

\begin{theorem}[Finite dyadic bracketing-modulus NameCert obligations]
\label{thm:finite-dyadic-bracketing-modulus-namecert-obligations}
For every accepted $\FiniteDyadicBracketingModulusUp$ carrier $B=(D,S,R,L,U,M,E,Q,H,C,P,N)$, the local $\NameCert_{\FiniteDyadicBracketingModulusUp}$ surface consists exactly of carrier admission, dyadic ledger exposure, finite-window scheduling, regular rational readback, lower and upper bracket exposure, modulus exactness, Real seal handoff, metric-read handoff, componentwise transport, displayed replay, package provenance, and local naming.

Modulus exactness means that every precision request accepted by $M$ names a finite row of $S$ and a finite dyadic bracket in $D,L,U$ before $R,E$, or $Q$ can be consumed.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:finite-dyadic-bracketing-modulus-carrier}. The accepted packet displays the dyadic ledger, stream windows, regular readback, bracket endpoints, modulus row, Real seal, metric-read row, transport, replay, provenance, and local name.

The modulus row is finite because every request in $M$ points to a displayed window in $S$ and a displayed dyadic comparison in $D$. The bracket rows $L,U$ are not independent real-order decisions; they are named only after the dyadic ledger has supplied the endpoint arithmetic and slack comparisons.

The Real and metric-facing rows are downstream. A consumer reaches $E$ or $Q$ only after $D,S,R,L,U,M$ have been read, and the structural rows $H,C,P,N$ transport, replay, source, and name that same route. No omitted coordinate can supply quotient streams, selected limits, trichotomy, countable choice, or ambient completeness.
\end{proof}

\begin{theorem}[Finite dyadic bracketing-modulus Real handoff]
\label{thm:finite-dyadic-bracketing-modulus-real-handoff}
Every $\RealUp$ or $\RealMetricUp$ consumer of an accepted $\FiniteDyadicBracketingModulusUp$ packet factors through the finite route
$$
\DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp \longmapsto \RealMetricUp
$$
as displayed by $D,S,R,E,Q,H,C,P,N$, with the bracket endpoints $L,U$ and modulus row $M$ read before the terminal seal or metric distance row is exposed. The handoff gives bracket evidence and a finite precision schedule, not $\RealUp$ completeness, quotient streams, selected limits, trichotomy, countable choice, or host real equality.
\end{theorem}
\begin{proof}
Start from the NameCert obligations. They force every accepted request to expose $D,S,R,L,U,M$ before a Real-facing row is read.

The dyadic stage reads $D,L,U$ as one bracket ledger. The window and readback stage then reads $S$ and $R$, so the bracket is attached to displayed finite observations rather than to an ambient completed real.

Only after those rows are present may the terminal seal $E$ and metric read $Q$ be consumed. The provenance row points to the sibling surfaces of \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-real-namecert}, and \autoref{ch:concrete-instances-real-metric-namecert}. Thus the handoff remains a finite route through displayed BEDC rows.
\end{proof}

\begin{theorem}[Finite dyadic bracketing-modulus non-escape]
\label{thm:finite-dyadic-bracketing-modulus-nonescape}
No accepted $\FiniteDyadicBracketingModulusUp$ packet exports $\RealUp$ completeness, quotient streams, selected limits, trichotomy, countable choice, host real equality, or an unbounded completion selector. Every consumer read factors through $D,S,R,L,U,M,E,Q,H,C,P,N$ and through the sibling rows named in \autoref{thm:finite-dyadic-bracketing-modulus-real-handoff}.
\end{theorem}
\begin{proof}
Inspect the carrier of \autoref{def:finite-dyadic-bracketing-modulus-carrier}. Its analytic rows are the dyadic ledger, finite windows, regular readback, bracket endpoints, modulus row, Real seal, and metric-read row.

Apply the Real handoff theorem. The terminal rows $E$ and $Q$ are available only after the finite bracket and modulus route has been displayed.

The excluded principles require data absent from the tuple: an ambient completeness witness, quotient-stream equality, a selected limit object, a trichotomy branch, a countable-choice selector, host real equality, or unbounded completion selection. Since the packet contains none of those rows, every read remains inside the finite bracketing-modulus certificate.
\end{proof}

\falsifiablePrediction{An accepted $\FiniteDyadicBracketingModulusUp$ consumer that reaches a $\RealUp$ seal or $\RealMetricUp$ distance read without exposing $D,S,R,L,U,M,E,Q,H,C,P,N$ refutes the carrier surface.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, realmetric: no listed sibling supplies the joint packet containing a DyadicRatCore ledger, finite StreamName windows, RegSeqRat readback, lower and upper dyadic brackets, a finite modulus row, a Real seal, a RealMetric read, transport, replay, provenance, and a local NameCert row.}

\closureat{FiniteDyadicBracketingModulusUp}{seedStr}

\begin{closurestatus}{\FiniteDyadicBracketingModulusUp}
  \constructivestory{A $\FiniteDyadicBracketingModulusUp$ packet is generated from a DyadicRatCore ledger, finite StreamName windows, RegSeqRat readback, lower and upper dyadic bracket rows, a finite modulus row, a RealUp seal, a RealMetricUp read, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-dyadic-bracketing-modulus-carrier}, \autoref{thm:finite-dyadic-bracketing-modulus-namecert-obligations}, \autoref{thm:finite-dyadic-bracketing-modulus-real-handoff}, and \autoref{thm:finite-dyadic-bracketing-modulus-nonescape} seed the finite bracketing-modulus certificate for Real-and-Completion consumers.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim $\RealUp$ completeness, quotient streams, selected limits, trichotomy, countable choice, host real equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic ledger exposure, finite-window scheduling, regular rational readback, bracket exactness, modulus exactness, Real seal handoff, metric-read handoff, transport, replay, provenance, and local naming for BEDC.Derived.FiniteDyadicBracketingModulusUp.}
\end{closurestatus}
