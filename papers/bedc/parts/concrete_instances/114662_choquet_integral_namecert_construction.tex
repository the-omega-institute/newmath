\chapter{A Concrete Naming Certificate for $\ChoquetIntegralUp$}
\label{ch:concrete-instances-choquet-integral-namecert}
\origin{human}

Following Choquet's capacity integral and Bishop--Bridges constructive measure theory, the $\ChoquetIntegralUp$ packet records the finite NameCert surface for integrating nonnegative simple-function envelopes against a displayed finite capacity. It consumes the measure carrier of \autoref{ch:concrete-instances-measure-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the compact-convex probability boundary of \autoref{ch:concrete-instances-choquet-simplex-namecert}. The chapter names only capacity rows, simple-function envelope rows, finite cut-level ledgers, Real readback, transport, replay, provenance, and local naming; it does not assert countable additivity, a monotone-convergence theorem, quotient equality of functions, selected measurable representatives, or ambient $\RealUp$ completeness.

\begin{definition}[Choquet integral carrier]
\label{def:choquet-integral-carrier}
A $\ChoquetIntegralUp$ carrier is a finite $\BHist$ packet
$$
I=(M,A,S,L,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MeasureUp$ source row naming the measurable carrier, measurable-set requests, and finite measure-read interface visible to the packet.
\item $A$ is the finite capacity row, recording monotone set-capacity observations over the displayed measurable rows without adding countable additivity.
\item $S$ is the simple-function envelope row, a finite list of level values and measurable support rows used to bound the integrand from below and above.
\item $L$ is the cut-level ledger, recording the inspected threshold rows, support inclusions, and finite envelope comparisons used by the Choquet read.
\item $R$ is the $\RealUp$ handoff row, sealing finite sums, capacity weights, and envelope bounds as displayed real-name data.
\item $H$ records componentwise $\hsame$ transport for measure, capacity, simple envelope, cut-level, Real handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a capacity-integral request through the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\ChoquetIntegralUp$, $\MeasureUp$, $\RealUp$, $\ChoquetSimplexUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\ChoquetIntegralUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,A,S,L,R,H,C,P,N$. It has no coordinate for an ambient integral value, countable additivity, selected measurable representatives, host almost-everywhere equality, quotient simple functions, or completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Choquet integral NameCert obligations]
\label{thm:choquet-integral-namecert-obligations}
For every accepted $\ChoquetIntegralUp$ carrier $I=(M,A,S,L,R,H,C,P,N)$, the local $\NameCert_{\ChoquetIntegralUp}$ surface consists exactly of measure-source admission through $M$, finite capacity admission through $A$, simple-function envelope admission through $S$, cut-level ledger exactness through $L$, Real handoff through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$. Any Choquet-integral consumer must display those rows before exporting an integral-facing certificate.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:choquet-integral-carrier}. The measure row $M$ is the only source for measurable-set requests, and the capacity row $A$ is the only source for monotone finite set-capacity observations.

Read the simple-function envelope next. The row $S$ supplies the finite level values and support rows, while $L$ records the inspected cut levels and support-inclusion comparisons used by the Choquet read.

The Real handoff is downstream of those finite rows. The row $R$ seals capacity weights, finite sums, and envelope bounds only after $M,A,S,L$ have been displayed.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier contains no ambient integral value, countable-additivity theorem, selected measurable representative, host almost-everywhere equality, quotient simple-function equality, or $\RealUp$ completeness row, the listed rows exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Choquet integral capacity handoff]
\label{thm:choquet-integral-capacity-handoff}
Every $\RealUp$ value exported by an accepted $\ChoquetIntegralUp$ carrier factors through the finite route
$$
\MeasureUp,\ChoquetSimplexUp \longmapsto \ChoquetIntegralUp \longmapsto \RealUp .
$$
The route first displays the measure source $M$, finite capacity row $A$, simple-function envelope row $S$, and cut-level ledger $L$; only then may the Real handoff row $R$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row export a real-name boundary. The handoff exports no monotone-convergence theorem, countable-additive extension, selected representative, quotient equality, or ambient completeness claim.
\end{theorem}
\begin{proof}
Start with the carrier inventory of \autoref{def:choquet-integral-carrier}. The only analytic rows before the real-name boundary are $M,A,S,L$: measure-source observations, finite capacity observations, simple-function envelopes, and cut-level ledgers.

Use the NameCert obligations of \autoref{thm:choquet-integral-namecert-obligations}. They force Real readback through $R$ only after the displayed finite capacity and envelope route is available.

The Choquet simplex sibling supplies the compact-convex probability boundary cited by the provenance row, while the measure and Real siblings supply the measurable-source and terminal real-name rows. Transport, replay, provenance, and local naming preserve that same route, so a consumer cannot replace it by host measure theory or by an unrecorded real completeness principle.
\end{proof}

\falsifiablePrediction{A consumer that claims a $\ChoquetIntegralUp$ real-name output while omitting the finite capacity row, simple-function envelope row, cut-level ledger, $\RealUp$ handoff, $\hsame$, $\Cont$, $\Pkg$, or $\NameCert$ rows refutes the carrier of \autoref{def:choquet-integral-carrier}.}

\independenceWitness{measure, real, choquet-simplex: $\ChoquetIntegralUp$ is not bijective to any listed sibling because it jointly carries measure-source rows, finite capacity rows, simple-function envelope rows, cut-level ledgers, Real handoff, transport, replay, provenance, and local naming while refusing countable additivity, selected representatives, quotient simple functions, and ambient real completeness.}

\closureat{ChoquetIntegralUp}{seedStr}

\begin{closurestatus}{\ChoquetIntegralUp}
  \constructivestory{A $\ChoquetIntegralUp$ packet is generated from a MeasureUp source, a finite capacity row, a simple-function envelope row, a cut-level ledger, a RealUp handoff row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:choquet-integral-carrier}, \autoref{thm:choquet-integral-namecert-obligations}, and \autoref{thm:choquet-integral-capacity-handoff} seed the capacity-integral certificate over measure, capacity, simple-envelope, cut-level, Real handoff, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close countable additivity, monotone convergence, selected measurable representatives, quotient equality of simple functions, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, measure-source exposure, finite capacity monotonicity, simple-function envelope exactness, cut-level ledger exactness, Real handoff, transport, replay, provenance, and local naming for BEDC.Derived.ChoquetIntegralUp.}
\end{closurestatus}
