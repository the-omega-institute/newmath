\chapter{A Concrete Naming Certificate for $\FinitePolarGaugeUp$}
\label{ch:concrete-instances-finite-polar-gauge-namecert}
\origin{human}

Following Bourbaki and Rockafellar finite convex analysis, the $\FinitePolarGaugeUp$ packet records the finite polar-gauge surface for a displayed bounded convex row in a normed vector space. It sits after the vector-space carrier of \autoref{ch:concrete-instances-vecspace-namecert}, the norm row of \autoref{ch:concrete-instances-norm-namecert}, the convex-set carrier of \autoref{ch:concrete-instances-convexset-namecert}, the bounded-set surface of \autoref{ch:concrete-instances-boundedset-namecert}, and the polar-polar boundary of \autoref{ch:concrete-instances-bipolar-theorem-namecert}. The packet names finite support probes, polar membership rows, and gauge comparison data without invoking Hahn--Banach separation, a completed dual space, quotient polar equality, selected functionals, or ambient local convexity.

\begin{definition}[Finite polar gauge carrier]
\label{def:finite-polar-gauge-carrier}
A $\FinitePolarGaugeUp$ carrier is a finite $\BHist$ packet
$$
G=(V,N,C,B,P,Q,S,E,H,R,K,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $V$ is the $\VecSpaceUp$ source row, exposing the vector addition, scalar action, zero vector, and finite linear-combination windows consumed by the gauge request.
\item $N$ is the $\NormUp$ row, giving the displayed norm comparisons that bound the finite polar probes.
\item $C$ is the $\ConvexSetUp$ row for the displayed convex body whose polar gauge is being read.
\item $B$ is the $\BoundedSetUp$ row, recording the finite radius and membership bounds used before a polar probe can be exposed.
\item $P$ is the finite polar-probe row: a displayed $\mathsf{ProbeBundle}$ of scalar readbacks and functional-window names, each tied to $V,N,C,B$.
\item $Q$ is the gauge-threshold row, pairing each inspected vector window with the finite set of polar inequalities that certify its gauge comparison.
\item $S$ is the support-budget row, recording that each polar inequality is read through a displayed finite support budget rather than through an ambient dual-space quantifier.
\item $E$ is the bipolar boundary row, linking the finite polar-polar return to the $\BipolarTheoremUp$ surface without asserting a separate separating-functional theorem.
\item $H$ records componentwise $\hsame$ transport for the vector, norm, convex, bounded, polar-probe, threshold, support, bipolar, replay, provenance, and local naming rows.
\item $R$ records displayed $\Cont$ replay from a finite polar-gauge request through $V,N,C,B,P,Q,S,E,H,K,L$.
\item $K$ is the $\Pkg$ provenance over $\FinitePolarGaugeUp$, $\VecSpaceUp$, $\NormUp$, $\ConvexSetUp$, $\BoundedSetUp$, $\BipolarTheoremUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $L$ is the local $\NameCert_{\FinitePolarGaugeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $V,N,C,B,P,Q,S,E,H,R,K,L$. No coordinate stores a host dual space, a selected separating functional, a Hahn--Banach extension, quotient polar equality, arbitrary local convexity, countable choice, or ambient completeness.
\end{definition}

\begin{theorem}[Finite polar gauge NameCert obligations]
\label{thm:finite-polar-gauge-namecert-obligations}
For every accepted $\FinitePolarGaugeUp$ carrier
$$
G=(V,N,C,B,P,Q,S,E,H,R,K,L),
$$
the local $\NameCert_{\FinitePolarGaugeUp}$ surface consists exactly of vector-space admission, norm comparison, convex-set admission, bounded-set admission, finite polar-probe exposure, gauge-threshold comparison, support-budget display, bipolar boundary handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-polar-gauge-carrier}. The rows $V,N,C,B$ identify the vector source, norm budget, convex membership surface, and boundedness evidence available to the polar-gauge request.

Read the finite polar rows next. The probe bundle $P$, threshold row $Q$, support-budget row $S$, and bipolar boundary row $E$ are the only coordinates through which polar inequalities and polar-polar returns can be consumed.

Finally read $H,R,K,L$. Transport, replay, provenance, and local naming preserve exactly those displayed rows, so the NameCert surface has no place for Hahn--Banach separation, completed dual spaces, selected functionals, quotient polar equality, countable choice, or ambient local convexity.
\end{proof}

\begin{theorem}[Finite polar gauge bipolar boundary]
\label{thm:finite-polar-gauge-bipolar-boundary}
Every bipolar-facing read of an accepted $\FinitePolarGaugeUp$ packet factors through
$$
\VecSpaceUp \longmapsto \NormUp \longmapsto \ConvexSetUp \longmapsto \BoundedSetUp \longmapsto \FinitePolarGaugeUp \longmapsto \BipolarTheoremUp .
$$
The boundary exposes only finite polar probes, displayed gauge-threshold comparisons, and the already named $\BipolarTheoremUp$ polar-polar return before any convex-duality consumer may read the packet.
\end{theorem}
\begin{proof}
Start with the source rows $V,N,C,B$. They determine the vector windows, norm comparisons, convex membership rows, and bounded radius data used by the finite polar-gauge packet.

The polar-probe row $P$ and threshold row $Q$ then display the actual finite inequalities inspected by the consumer. The support-budget row $S$ keeps those inequalities inside finite support data, not inside an ambient dual-space quantifier.

The bipolar boundary row $E$ is read only after those finite rows are visible. It hands the same displayed data to the $\BipolarTheoremUp$ route, while $H,R,K,L$ preserve transport, replay, provenance, and local naming.

Thus the boundary exports a finite convex-duality packet over the sibling rows of \autoref{ch:concrete-instances-vecspace-namecert}, \autoref{ch:concrete-instances-norm-namecert}, \autoref{ch:concrete-instances-convexset-namecert}, \autoref{ch:concrete-instances-boundedset-namecert}, and \autoref{ch:concrete-instances-bipolar-theorem-namecert}. It exports no Hahn--Banach separation, selected support functional, completed dual space, quotient polar equality, countable choice, or ambient local convexity.
\end{proof}

\closureat{FinitePolarGaugeUp}{seedStr}

\begin{closurestatus}{\FinitePolarGaugeUp}
  \constructivestory{A $\FinitePolarGaugeUp$ packet is generated from VecSpaceUp rows, NormUp comparison rows, ConvexSetUp membership rows, BoundedSetUp radius rows, finite polar ProbeBundle rows, gauge-threshold rows, support-budget rows, a BipolarTheoremUp boundary row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-polar-gauge-carrier}, \autoref{thm:finite-polar-gauge-namecert-obligations}, and \autoref{thm:finite-polar-gauge-bipolar-boundary} seed the finite polar-gauge carrier, local NameCert surface, and bipolar boundary route for convex-duality consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Hahn--Banach separation, selected supporting functionals, completed dual spaces, quotient polar equality, arbitrary local convexity, countable choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite polar-probe exposure, gauge-threshold comparison, support-budget display, bipolar boundary handoff, structural transport, replay, provenance, and local naming for BEDC.Derived.FinitePolarGaugeUp.}
\end{closurestatus}
