\chapter{A Concrete Naming Certificate for $\DenseRationalEmbeddingUp$}
\label{ch:concrete-instances-dense-rational-embedding-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\DenseRationalEmbeddingUp$ packet records the finite handoff by which rational certificates are read as dense real-name data before completion consumers inspect a $\RealUp$ seal. It sits after the rational carrier of \autoref{ch:concrete-instances-rat-namecert}, the dyadic comparison core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational sequence route of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-window interface of \autoref{ch:concrete-instances-streamname-namecert}, and the terminal real-name packet of \autoref{ch:concrete-instances-real-namecert}. The packet names a displayed density route from rational witnesses to real-name seals; it is not quotient real equality, trichotomy, selected limits, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Dense rational embedding carrier]
\label{def:dense-rational-embedding-carrier}
A $\DenseRationalEmbeddingUp$ carrier is a finite $\BHist$ packet
$$
D=(Q,Y,S,R,E,A,H,C,P,N)
$$
with the following rows:
\begin{enumerate}
\item $Q$ is the $\RatUp$ source row naming the rational witness to be embedded.
\item $Y$ is the $\DyadicRatCoreUp$ comparison row, recording the dyadic tolerance and order checks used to locate $Q$ inside a real-name window.
\item $S$ is the $\StreamNameUp$ finite-window row that exposes the inspected real-name prefixes.
\item $R$ is the $\RegSeqRatUp$ regular rational sequence row used to transport the rational witness through completion-facing Cauchy data.
\item $E$ is the $\RealUp$ seal row for the terminal real name reached by the displayed embedding route.
\item $A$ is the density-approximation row, recording that every inspected real-name window admits a rational witness read through $Q,Y,S,R,E$ at the displayed tolerance.
\item $H$ records componentwise $\hsame$ transport for the rational, dyadic, stream, regular-sequence, real-seal, density, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a density request through the same rows.
\item $P$ is the $\Pkg$ provenance over $\DenseRationalEmbeddingUp$, $\RatUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DenseRationalEmbeddingUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $Q,Y,S,R,E,A,H,C,P,N$. It has no coordinate for quotient real equality, trichotomy, selected limit objects, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[Dense rational embedding NameCert obligations]
\label{thm:dense-rational-embedding-namecert-obligations}
For every accepted $\DenseRationalEmbeddingUp$ carrier $D=(Q,Y,S,R,E,A,H,C,P,N)$, the local $\NameCert_{\DenseRationalEmbeddingUp}$ surface consists exactly of rational source admission, dyadic tolerance comparison, finite stream-window exposure, regular rational sequence readback, terminal real sealing, density-approximation exposure, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every density read is therefore confined to the finite route
$$
Q,Y,S,R,E,A \longmapsto H,C,P,N .
$$
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:dense-rational-embedding-carrier}. The rows $Q,Y,S,R,E,A$ are exactly the rational source, dyadic tolerance, stream-window, regular-sequence, real-seal, and density-approximation data available before structural replay.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite packet. They add no quotient-real equality, no trichotomy branch, no selected limit, and no choice principle.

Thus the local NameCert surface is exhausted by the displayed rows. A completion-facing consumer can read density only through this finite route.
\end{proof}

\begin{theorem}[Dense rational embedding density window]
\label{thm:dense-rational-embedding-density-window}
Every real-name density request accepted by a $\DenseRationalEmbeddingUp$ packet must display a finite stream window $S$, a regular rational readback row $R$, a dyadic tolerance row $Y$, and a rational witness row $Q$ before the terminal $\RealUp$ seal $E$ is consumed. The packet therefore supplies density as a finite window statement over rational certificates, not as an ambient completeness or selected-limit principle.
\end{theorem}
\begin{proof}
Read the density-approximation row $A$. By the carrier definition, $A$ is attached to the rows $Q,Y,S,R,E$ and has no independent coordinate outside them.

The stream and regular-sequence rows determine what part of the real name is inspected. The dyadic row records the tolerance comparison, and the rational row supplies the witness used inside that tolerance.

Finally the real seal $E$ authenticates the same inspected window. Since selected limits, quotient equality, trichotomy, choice, and ambient completeness are absent from the carrier, the density statement remains a finite rational-window handoff.
\end{proof}

\begin{theorem}[Dense rational embedding real seal]
\label{thm:dense-rational-embedding-real-seal}
Every $\RealUp$ consumer of an accepted $\DenseRationalEmbeddingUp$ packet receives the real-name seal only after the route
$$
\RatUp,\DyadicRatCoreUp,\StreamNameUp,\RegSeqRatUp
\longmapsto
\RealUp
$$
has been displayed and authenticated by $A,H,C,P,N$. Thus the handoff from rational certificates to real-name seals is a NameCert route for completion proofs, not a claim of quotient real equality, trichotomy, selected limits, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the rows of \autoref{def:dense-rational-embedding-carrier}. The real seal $E$ is downstream of the rational witness $Q$, dyadic comparison $Y$, stream window $S$, and regular rational readback $R$.

The density row $A$ records that this route supplies the rational witness required by the inspected window. The transport and replay rows $H,C$ preserve that route componentwise, and $P,N$ source and name it.

No remaining coordinate can expose quotient equality of real names, a trichotomy decision, a selected limit object, choice, or ambient completeness. Hence the seal is exactly the displayed finite handoff from rational certificates into the real-name boundary.
\end{proof}

\falsifiablePrediction{Every accepted $\DenseRationalEmbeddingUp$ completion-facing read must display a $\RatUp$ source row, $\DyadicRatCoreUp$ tolerance row, $\StreamNameUp$ finite-window row, $\RegSeqRatUp$ readback row, $\RealUp$ seal row, density-approximation row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row before the density handoff is consumed.}

\independenceWitness{rat, dyadicratcore, regseqrat, streamname, real: $\DenseRationalEmbeddingUp$ is not bijective to any listed sibling because it binds the rational witness, dyadic tolerance comparison, finite stream window, regular rational readback, terminal real seal, density row, transport, replay, provenance, and local naming into one completion-facing handoff.}

\closureat{DenseRationalEmbeddingUp}{seedStr}

\begin{closurestatus}{\DenseRationalEmbeddingUp}
  \constructivestory{A $\DenseRationalEmbeddingUp$ packet is generated from a RatUp source row, DyadicRatCoreUp tolerance row, StreamNameUp finite-window row, RegSeqRatUp readback row, RealUp seal row, density-approximation row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dense-rational-embedding-carrier}, \autoref{thm:dense-rational-embedding-namecert-obligations}, \autoref{thm:dense-rational-embedding-density-window}, and \autoref{thm:dense-rational-embedding-real-seal} seed the finite rational-to-real density handoff used by completion-facing consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, trichotomy, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, rational source exposure, dyadic tolerance compatibility, stream-window exposure, regular rational readback, real sealing, density-row exactness, transport, replay, provenance, and local naming for BEDC.Derived.DenseRationalEmbeddingUp.}
\end{closurestatus}
