\chapter{A Concrete Naming Certificate for $\CaratheodoryKernelConvergenceUp$}
\label{ch:concrete-instances-caratheodory-kernel-convergence-namecert}
\origin{human}

Following Caratheodory, Koebe, and Ahlfors on kernel convergence of simply connected planar domains, the $\CaratheodoryKernelConvergenceUp$ packet records the finite NameCert route by which pointed domain windows, compact-kernel exhaustions, holomorphic chart reads, normal-family compactness, and Riemann-map normalization are read as one carrier. It sits between the normal-family packet of \autoref{ch:concrete-instances-normal-family-namecert}, the Riemann mapping packet of \autoref{ch:concrete-instances-riemann-mapping-namecert}, the holomorphic chart surface of \autoref{ch:concrete-instances-holomorphic-namecert}, the unit-disk source of \autoref{ch:concrete-instances-unitdisk-namecert}, and the compact-open topology surface of \autoref{ch:concrete-instances-compact-open-topology-namecert}. The packet names the displayed kernel route without importing prime ends, quotient equality of functions, host domain convergence, or a selected boundary correspondence.

\begin{definition}[Caratheodory kernel convergence carrier]
\label{def:caratheodory-kernel-convergence-carrier}
A $\CaratheodoryKernelConvergenceUp$ carrier is a finite $\BHist$ packet
$$
K=(D,E,H,N,R,T,C,P,L)
$$
with the following displayed rows.
\begin{enumerate}
\item $D$ is the pointed planar-domain window, recording the visible base point, the inspected planar open rows, and the admitted source windows for the kernel read.
\item $E$ is the compact-kernel exhaustion row, recording the finite compact windows whose eventual containment witnesses the kernel interior and whose escape rows witness points outside the kernel.
\item $H$ is the holomorphic chart-read row, exposing the finite $\HolomorphicUp$ chart observations used to compare maps on the displayed compact windows.
\item $N$ is the normal-family compactness handoff, read through $\NormalFamilyUp$ and the compact-window discipline of $\CompactOpenTopologyUp$ before any subsequential holomorphic limit is public.
\item $R$ is the Riemann-map route, recording the normalized $\UnitDiskUp$ source, base-point normalization, derivative-orientation row, and $\RiemannMappingUp$ handoff used by simply connected kernel consumers.
\item $T$ records componentwise $\hsame$ transport for the pointed window, compact-kernel exhaustion, holomorphic chart reads, normal-family handoff, Riemann-map route, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a kernel-convergence request through $D,E,H,N,R,T$.
\item $P$ is the $\Pkg$ provenance over $\CaratheodoryKernelConvergenceUp$, $\NormalFamilyUp$, $\RiemannMappingUp$, $\HolomorphicUp$, $\UnitDiskUp$, $\CompactOpenTopologyUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\CaratheodoryKernelConvergenceUp}$ row.
\end{enumerate}
The carrier has no coordinate for prime ends, quotient function equality, host domain convergence, selected boundary correspondence, ambient compactness beyond the displayed normal-family handoff, or an unrestricted domain topology.
\end{definition}
\leanchecked{BEDC.Derived.CaratheodoryKernelConvergenceUp.CaratheodoryKernelConvergenceTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Caratheodory kernel convergence NameCert obligations]
\label{thm:caratheodory-kernel-convergence-namecert-obligations}
For every accepted $\CaratheodoryKernelConvergenceUp$ carrier $K=(D,E,H,N,R,T,C,P,L)$, the local $\NameCert_{\CaratheodoryKernelConvergenceUp}$ surface consists exactly of carrier admission for the pointed planar-domain window, compact-kernel exhaustion visibility, holomorphic chart-read visibility, normal-family compactness handoff, Riemann-map route visibility, componentwise transport, continuation replay, provenance, and local naming. A kernel-convergence consumer must read these rows before using the packet as a Caratheodory kernel route.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:caratheodory-kernel-convergence-carrier}. The pointed window $D$, compact exhaustion $E$, holomorphic chart row $H$, normal-family handoff $N$, and Riemann-map route $R$ are visible coordinates of the same finite packet.

The compact-kernel row $E$ supplies both interior containment windows and exterior escape rows. The holomorphic row $H$ is read only on those displayed compact windows, so chart convergence is a compact-open read rather than host function equality.

The normal-family handoff $N$ and Riemann-map route $R$ remain downstream. They expose the compactness and normalized disk-map rows needed by consumers, but they do not add prime ends, boundary selectors, quotient equality of maps, or host domain convergence.

Finally $T,C,P,L$ transport, replay, source, and name exactly these rows. Hence the listed rows exhaust the local NameCert obligations for the kernel packet.
\end{proof}

\begin{theorem}[Caratheodory kernel convergence normal-family handoff]
\label{thm:caratheodory-kernel-convergence-normal-family-handoff}
Every accepted $\CaratheodoryKernelConvergenceUp$ read that reaches a Riemann-map consumer first exposes the pointed planar-domain window $D$, compact-kernel exhaustion $E$, holomorphic chart reads $H$, and the $\NormalFamilyUp$ compactness handoff $N$ through $\CompactOpenTopologyUp$. Only then may the normalized $\UnitDiskUp$ source and $\RiemannMappingUp$ route $R$ be consumed. The handoff supplies no prime-end compactification, quotient function equality, host domain convergence, selected boundary correspondence, or ambient compactness principle.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:caratheodory-kernel-convergence-namecert-obligations}. They force every accepted kernel read to expose the pointed domain window, compact exhaustion, holomorphic chart reads, normal-family handoff, and Riemann-map route as rows of one packet.

The normal-family row is read before the Riemann-map route. It consumes the compact-open windows from \autoref{ch:concrete-instances-compact-open-topology-namecert} and the holomorphic chart reads from \autoref{ch:concrete-instances-holomorphic-namecert}, then hands a compactness boundary to \autoref{ch:concrete-instances-normal-family-namecert}.

The Riemann-map row is therefore downstream of the same finite handoff. Its normalized $\UnitDiskUp$ source and $\RiemannMappingUp$ data may be used only after the compact-kernel and normal-family rows are visible.

Since the carrier contains no prime-end coordinate, quotient equality of functions, host domain convergence row, boundary selector, or ambient compactness principle, the handoff is exactly the displayed normal-family route into Riemann mapping.
\end{proof}
\leanchecked{BEDC.Derived.CaratheodoryKernelConvergenceUp.CaratheodoryKernelConvergenceCarrier\_normal\_family\_handoff}

\falsifiablePrediction{Every accepted $\CaratheodoryKernelConvergenceUp$ read displays one pointed planar-domain window, one compact-kernel exhaustion, one holomorphic chart-read row, one NormalFamily compactness handoff, one RiemannMapping route, hsame transport, Cont replay, Pkg provenance, and local NameCert row before a kernel-convergence or Riemann-map consumer may use it.}

\independenceWitness{normalfamily, riemannmapping, holomorphic, unitdisk, compactopentopology: none is bijective with $\CaratheodoryKernelConvergenceUp$ because the packet jointly stores the pointed domain window, compact-kernel exhaustion, holomorphic chart reads, normal-family handoff, Riemann-map route, transport, replay, provenance, and local naming.}

\closureat{CaratheodoryKernelConvergenceUp}{seedStr}

\begin{closurestatus}{\CaratheodoryKernelConvergenceUp}
  \constructivestory{A $\CaratheodoryKernelConvergenceUp$ packet is generated from a pointed planar-domain window, compact-kernel exhaustion, holomorphic chart reads, NormalFamily compactness handoff, RiemannMapping route, hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:caratheodory-kernel-convergence-carrier}, \autoref{thm:caratheodory-kernel-convergence-namecert-obligations}, and \autoref{thm:caratheodory-kernel-convergence-normal-family-handoff} seed the finite kernel-convergence route from compact-open holomorphic windows through normal-family compactness into normalized Riemann mapping.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, prime-end compactification, quotient equality of functions, host domain convergence, selected boundary correspondences, or ambient compactness beyond the displayed normal-family handoff.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-kernel exhaustion, holomorphic chart-read visibility, normal-family handoff, Riemann-map route visibility, transport, replay, provenance, and local naming for BEDC.Derived.CaratheodoryKernelConvergenceUp.}
\end{closurestatus}
