\chapter{A Concrete Naming Certificate for $\HyperbolicObserverPhaseTransportUp$}
\label{ch:concrete-instances-hyperbolic-observer-phase-transport-namecert}
\origin{ai}

The $\HyperbolicObserverPhaseTransportUp$ packet realizes the reversible observer-phase transport branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It sits over the disk-metric readout of \autoref{ch:concrete-instances-poincare-disk-metric-namecert}, the visual-boundary surface of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, the observer-expansion route of \autoref{ch:concrete-instances-hyperbolic-observer-expansion-namecert}, the observer-phase packet of \autoref{ch:concrete-instances-hyperbolic-observer-phase-namecert}, the phase-transport budget of \autoref{ch:concrete-instances-hyperbolic-phase-transport-budget-namecert}, the boundary-shadow packet of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, and the complex phase-fiber row of \autoref{ch:concrete-instances-complex-phase-surface-fiber-namecert}. The packet names reversible disk-phase, boundary-shadow, visual-boundary, provenance, and refusal rows; it is not a completed hyperbolic disk, a physical observer, a quotient boundary, a chosen global phase, or a cosmological transport law.

\begin{definition}[HyperbolicObserverPhaseTransport carrier]
\label{def:hyperbolic-observer-phase-transport-carrier}
A $\HyperbolicObserverPhaseTransportUp$ carrier is a finite $\BHist$ packet
$$
T=(D,M,V,O,G,A,B,F,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the Poincare disk-coordinate row read through $\PoincareDiskMetricUp$.
\item $M$ is the hyperbolic metric comparison row over the same displayed disk data.
\item $V$ is the $\HyperbolicVisualBoundaryUp$ row displaying the visible boundary face.
\item $O$ is the $\HyperbolicObserverExpansionUp$ row exposing the observer-expansion window.
\item $G$ is the $\ObserverPhaseGroupoidUp$ row recording reversible composition of observer-phase arrows.
\item $A$ is the $\HyperbolicObserverPhaseUp$ phase-read row consumed by the transport.
\item $B$ is the $\HyperbolicBoundaryShadowUp$ row binding the transport to a finite boundary shadow.
\item $F$ is the $\ComplexPhaseSurfaceFiberUp$ provenance-fiber row that prevents the phase read from becoming a hidden source equality.
\item $E$ is the $\HyperbolicPhaseTransportBudgetUp$ energy and curvature-budget row for the accepted transport.
\item $H$ records componentwise $\hsame$ transport for every displayed object row, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay through disk metric, visual boundary, observer expansion, groupoid composition, phase read, boundary shadow, fiber provenance, and transport budget.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicObserverPhaseTransportUp$, $\PoincareDiskMetricUp$, $\HyperbolicVisualBoundaryUp$, $\HyperbolicObserverExpansionUp$, $\ObserverPhaseGroupoidUp$, $\HyperbolicObserverPhaseUp$, $\HyperbolicBoundaryShadowUp$, $\ComplexPhaseSurfaceFiberUp$, $\HyperbolicPhaseTransportBudgetUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HyperbolicObserverPhaseTransportUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,M,V,O,G,A,B,F,E,H,C,P,N$. It exports no completed disk, quotient visual boundary, selected global phase, physical observer, cosmological transport law, or hidden fiber equality.
\end{definition}

\begin{theorem}[HyperbolicObserverPhaseTransport NameCert obligations]
\label{thm:hyperbolic-observer-phase-transport-namecert-obligations}
For every accepted $\HyperbolicObserverPhaseTransportUp$ carrier
$$
T=(D,M,V,O,G,A,B,F,E,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicObserverPhaseTransportUp}$ surface consists exactly of disk-coordinate admission through $D$, metric comparison through $M$, visual-boundary exposure through $V$, observer-expansion exposure through $O$, reversible observer-phase groupoid composition through $G$, phase readback through $A$, boundary-shadow binding through $B$, provenance-fiber exposure through $F$, phase-transport budget accounting through $E$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-observer-phase-transport-carrier}. The rows $D,M,V,O$ are the geometric source rows: disk coordinates, metric comparison, visual boundary, and observer expansion must all be displayed before transport is consumed.

The phase rows are $G,A,B,F,E$. The groupoid row records reversible observer-phase composition, the phase-read row is the local phase surface, the boundary-shadow row ties it to the far boundary, the fiber row records provenance, and the budget row accounts for the accepted transport.

The structural rows are $H,C,P,N$. Transport, replay, provenance, and local naming preserve exactly the displayed rows. Since no excluded host object appears as a coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[HyperbolicObserverPhaseTransport boundary coherence]
\label{thm:hyperbolic-observer-phase-transport-boundary-coherence}
Every accepted $\HyperbolicObserverPhaseTransportUp$ read exposes boundary-shadow and provenance rows before phase transport is consumed. In particular, the route
$$
D,M,V,O,G,A \longmapsto B,F,E
$$
is coherent only when $B$ records the finite boundary shadow, $F$ records the provenance fiber, and $E$ records the transport budget before any downstream consumer reads the transported observer phase.
\end{theorem}
\begin{proof}
Start with the disk and visual rows. The disk coordinate, metric comparison, visual-boundary, and observer-expansion rows determine the visible surface on which the phase read is meaningful.

Next consume the reversible phase rows. The observer-phase groupoid row and phase-read row give a transport candidate, but they do not yet license a boundary-level consumer.

Finally read $B,F,E$. The boundary-shadow row binds the candidate to the visual boundary, the fiber row displays provenance, and the budget row records the accepted transport cost. A consumer that skips one of these rows has no displayed boundary-shadow or provenance coordinate, so it cannot consume the transported phase as an accepted packet.
\end{proof}

\begin{theorem}[HyperbolicObserverPhaseTransport non-escape]
\label{thm:hyperbolic-observer-phase-transport-nonescape}
The public information exported by a $\HyperbolicObserverPhaseTransportUp$ carrier is exactly
$$
D,M,V,O,G,A,B,F,E,H,C,P,N .
$$
Thus a consumer cannot extract a completed hyperbolic disk, quotient visual boundary, selected global phase, physical observer, cosmological transport law, or hidden fiber equality from the packet.
\end{theorem}
\begin{proof}
Inspect the carrier rows. The object rows are exactly the disk, metric, visual-boundary, observer-expansion, groupoid, phase-read, boundary-shadow, fiber, and budget rows.

The structural rows preserve the same inventory. Componentwise transport, displayed replay, provenance, and local naming do not add a completed disk, quotient boundary, selected phase, physical observer, cosmological law, or hidden fiber equality.

Therefore every public read factors through the displayed packet and the excluded objects remain outside the local NameCert surface.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicObserverPhaseTransportUp$ read must expose boundary-shadow and provenance rows before phase transport is consumed.}

\independenceWitness{poincare-disk-metric, hyperbolic-visual-boundary, observer-phase-groupoid, hyperbolic-observer-expansion, complex-phase-surface-fiber: no listed sibling is bijective to $\HyperbolicObserverPhaseTransportUp$ because this packet jointly carries disk metric, visual-boundary, observer-expansion, reversible observer-phase groupoid, phase read, boundary-shadow, provenance-fiber, budget, transport, replay, provenance, and local NameCert rows.}

The Lean TasteGate for this packet will require explicit $\mathsf{Nontrivial}\ \HyperbolicObserverPhaseTransportUp$ and $\mathsf{FieldFaithful}\ \HyperbolicObserverPhaseTransportUp$ instances before a checked carrier-alignment theorem can serve as a public TasteGate realization.

\closureat{HyperbolicObserverPhaseTransportUp}{seedStr}

\begin{closurestatus}{\HyperbolicObserverPhaseTransportUp}
  \constructivestory{A $\HyperbolicObserverPhaseTransportUp$ packet is generated from disk-coordinate, metric, visual-boundary, observer-expansion, observer-phase groupoid, phase-read, boundary-shadow, provenance-fiber, transport-budget, hsame, Cont, Pkg, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-observer-phase-transport-carrier}, \autoref{thm:hyperbolic-observer-phase-transport-namecert-obligations}, \autoref{thm:hyperbolic-observer-phase-transport-boundary-coherence}, \autoref{thm:hyperbolic-observer-phase-transport-nonescape}, the falsifiable prediction, and the independence witness seed the finite observer-phase transport surface over disk, boundary, phase, provenance, and refusal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed hyperbolic disks, quotient visual boundaries, selected global phases, physical observers, cosmological transport laws, hidden fiber equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked disk and metric visibility, visual-boundary exposure, observer-expansion exposure, groupoid composition, phase readback, boundary-shadow binding, fiber provenance, transport-budget exactness, TasteGate nontriviality, field faithfulness, replay stability, provenance exhaustion, and local naming for BEDC.Derived.HyperbolicObserverPhaseTransportUp.}
\end{closurestatus}
