\begin{theorem}[RealCauchyCompletion witness-ledger classifier lock]
\label{thm:real-cauchy-completion-witness-ledger-classifier-lock}
Every accepted $\RealCauchyCompletionUp$ carrier whose completion-facing consumer also reads an accepted $\CauchyWitnessLedgerUp$ packet $L=(Q,B,S,K,H,C,P,N)$ reaches the public $\RealUp$ seal through the completion-seal classifier only by the shared finite route
$$
Q \longmapsto B \longmapsto S \longmapsto K .
$$
The shared route is read together with the $\RealCauchyCompletionUp$ regular-family row, unary modulus, diagonal packet, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The consumer-side lock is relative to the witness-ledger surface of \autoref{ch:concrete-instances-cauchy-witness-ledger-namecert} and the completion-seal classifier surface of \autoref{ch:concrete-instances-real-completion-seal-classifier-namecert}. It exports no quotient stream equality, host real equality, selected external limit, countable-choice row, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Start with the accepted $\RealCauchyCompletionUp$ carrier. The public real-name export theorem \autoref{thm:real-cauchy-completion-public-real-name-export} keeps the public $\RealUp$ read on the displayed completion route, and \autoref{thm:real-cauchy-completion-uniform-criterion-seal-factorization} factors that route through the regular-family row, unary modulus, diagonal packet, finite windows, regular-rational readback, dyadic ledger, transports, continuations, provenance, and local naming rows.

Now read the accepted $\CauchyWitnessLedgerUp$ packet. The Real-completion handoff \autoref{thm:cauchy-witness-ledger-real-completion-handoff} places every Real-facing ledger consumer on the displayed completion route. The synchronizer-classifier lock \autoref{thm:cauchy-witness-ledger-synchronizer-classifier-lock} orders that route as $Q \longmapsto B \longmapsto S \longmapsto K$, and the ledger-exhaustion theorem \autoref{thm:cauchy-witness-ledger-ledger-exhaustion} removes any public coordinate outside $Q,B,S,K,H,C,P,N$ and the structural rows.

Finally identify the two consumer paths at the shared classifier row $K$. The RealCauchyCompletion path supplies the completion-facing public $\RealUp$ seal, while the witness ledger supplies the same finite budget, observation, synchronizer, and classifier route. Since both cited chapters expose only the displayed BEDC rows, the combined consumer cannot export quotient stream equality, host real equality, a selected external limit, a countable-choice row, or an ambient completeness theorem.
\end{proof}

\begin{theorem}[RealCauchyCompletion witness-ledger uniform seal route]
\label{thm:real-cauchy-completion-witness-ledger-uniform-seal-route}
Every accepted $\RealCauchyCompletionUp$ carrier whose completion-facing route is also read through an accepted $\CauchyWitnessLedgerUp$ packet and an accepted $\UniformCauchyCriterionUp$ packet factors through one finite seal route. The route consists exactly of the regular-family row, unary modulus, diagonal packet, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\CauchyWitnessLedgerUp$ budget, synchronizer, and classifier rows, $\UniformCauchyCriterionUp$ shared-threshold rows, the public $\RealUp$ seal, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The combined route exports no quotient stream, selected host limit, host real equality, countable-choice row, second family-limit route, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with the completion-facing seal. The uniform criterion seal factorization theorem \autoref{thm:real-cauchy-completion-uniform-criterion-seal-factorization} places the accepted $\RealCauchyCompletionUp$ carrier on the finite regular-family, unary-modulus, diagonal-packet, window, readback, dyadic-ledger, transport, continuation, package, naming, and public $\RealUp$ seal route.

Next read the witness-ledger side. The witness-ledger classifier lock \autoref{thm:real-cauchy-completion-witness-ledger-classifier-lock} identifies the completion-seal classifier with the shared finite route through the ledger budget, observation, synchronizer, and classifier rows, and \autoref{thm:cauchy-witness-ledger-root-consumer-exhaustion} exhausts downstream completion consumers at those displayed ledger rows.

Finally attach the uniform criterion consumer surface. The downstream Real consumer export \autoref{thm:uniform-cauchy-criterion-downstream-real-consumer-export} and the Real-completion budget lock \autoref{thm:uniform-cauchy-criterion-real-completion-budget-lock} keep every $\RealCauchyCompletionUp$ read behind the same shared-threshold and finite-window rows. Intersecting the three non-escape clauses leaves no coordinate for quotient streams, selected host limits, host real equality, countable choice, a second family-limit route, or ambient $\RealUp$ completeness, so the sole exposed route is the finite BEDC packet named in the statement.
\end{proof}

\begin{theorem}[RealCauchyCompletion shared-tail modulus consumer route]
\label{thm:real-cauchy-completion-shared-tail-modulus-consumer-route}
Every accepted $\RealCauchyCompletionUp$ carrier whose diagonal packet consumes an accepted $\CauchySharedTailModulusUp$ row reaches the public $\RealUp$ seal through a single finite route. The route consists of the regular-family row, unary modulus, shared-tail threshold, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The route is relative to the shared-tail modulus surface of \autoref{ch:concrete-instances-cauchy-shared-tail-modulus-namecert}. It exports no quotient streams, selected limits, host real equality, countable-choice row, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the accepted completion carrier. The public real-name export theorem, \autoref{thm:real-cauchy-completion-public-real-name-export}, keeps the $\RealUp$ seal on the displayed completion path, while the diagonal handoff and downstream consumer handoff theorems bind that path to the regular-family row, unary modulus, finite windows, regular-rational readback, dyadic ledger, transports, continuations, provenance, and local naming rows.

Now attach the accepted $\CauchySharedTailModulusUp$ packet. Its consumer contribution is the shared-tail threshold for the same tail comparison, not a separate limit selector or quotient-stream equality.

Combining the two projections gives one displayed route from the completion diagonal packet to the public seal. The exclusions in the completion and shared-tail surfaces remove quotient streams, selected limits, host real equality, countable choice, and ambient completeness from the exported carrier.
\end{proof}

\begin{theorem}[RealCauchyCompletion dyadic tail-radius ledger route]
\label{thm:real-cauchy-completion-dyadic-tail-radius-ledger-route}
Every accepted $\RealCauchyCompletionUp$ carrier whose diagonal packet consumes an accepted $\DyadicTailRadiusLedgerUp$ row reaches the public $\RealUp$ seal only after the dyadic tail-radius ledger has been displayed. The finite route is the regular-family row, unary modulus, shared tail request, dyadic tail-radius ledger, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The route is relative to the dyadic ledger surface of \autoref{ch:concrete-instances-dyadic-tail-radius-ledger-namecert}. It exports no quotient-stream representative, selected limit, host real equality, countable-choice row, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Start from the displayed completion route supplied by \autoref{thm:real-cauchy-completion-tail-schedule-budget-exhaustion}. This already places the public seal after the regular-family row, unary modulus, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, diagonal packet, transports, continuations, provenance, and local naming rows.

Insert the $\DyadicTailRadiusLedgerUp$ packet at the tail-radius step. Its ledger row records the dyadic tolerance and radius accounting used by the same finite windows, so the completion consumer reads a displayed dyadic budget rather than an external completeness witness.

The resulting path is still the completion path. Since neither the completion route nor the dyadic ledger surface contains a coordinate for quotient representatives, selected limits, host real equality, countable choice, or ambient completeness, the public seal can only be reached through the rows named in the statement.
\end{proof}

\begin{theorem}[RealCauchyCompletion CauchyCriterion sibling route]
\label{thm:real-cauchy-completion-cauchycriterion-sibling-route}
Every accepted $\RealCauchyCompletionUp$ carrier whose diagonal packet consumes accepted $\CauchySharedTailModulusUp$, $\DyadicTailRadiusLedgerUp$, and $\CauchyCriterionUp$ rows reaches the public $\RealUp$ seal only through the combined finite rows of the regular-family row, unary modulus, shared-tail threshold, dyadic tail-radius ledger, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

The combined route is relative to \autoref{ch:concrete-instances-cauchy-shared-tail-modulus-namecert}, \autoref{ch:concrete-instances-dyadic-tail-radius-ledger-namecert}, and \autoref{ch:concrete-instances-cauchycriterion-namecert}. It exports no quotient streams, selected limits, host real equality, countable-choice row, or ambient completeness principle.
\end{theorem}
\begin{proof}
First apply the shared-tail modulus route above. It identifies the completion seal path with the regular-family row, unary modulus, shared-tail threshold, scheduled windows, readback, dyadic ledger, structural transport, provenance, and local naming rows.

Next apply the dyadic tail-radius ledger route above. It shows that the same scheduled windows must expose their dyadic radius accounting before the public $\RealUp$ seal is consumed.

Finally attach the sibling $\CauchyCriterionUp$ packet. Its role is to certify the Cauchy-condition route for the displayed shared threshold and dyadic ledger, not to import a selected limit or ambient completeness theorem.

The three sibling routes therefore assemble one completion-seal consumer packet. Because each component route is confined to $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows and excludes quotient streams, selected limits, host real equality, countable choice, and ambient completeness, the carrier reaches the public seal only through the finite rows listed in the statement.
\end{proof}

\begin{theorem}[RealCauchyCompletion diagonal seal uniformity]
\label{thm:real-cauchy-completion-diagonal-seal-uniformity}
Every accepted $\RealCauchyCompletionUp$ carrier whose diagonal packet consumes the same displayed $\CauchyCriterionUp$ row and the same singleton or fixed finite-family $\UniformCauchyCriterionUp$ rows factors through one finite completion route. The route consists of the regular-family row, shared unary modulus, dyadic tolerance, tail ledger, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\RealUp$ seal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The diagonal seal therefore exports no quotient streams, selected limits, host real equality, countable choice, ambient completeness principle, or second family-limit route. A downstream $\CompletionUp$ or $\RealCauchyCompletionUp$ consumer reads only the displayed finite seal path.
\end{theorem}
\begin{proof}
First apply \autoref{thm:real-cauchy-completion-cauchycriterion-sibling-route}. It places the completion seal behind the regular-family row, unary modulus, finite stream windows, regular-rational readback, dyadic ledger, $\RealUp$ seal, and structural $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Next use \autoref{thm:cauchycriterion-uniformcriterion-finite-family-reduction}. The displayed $\CauchyCriterionUp$ packet and the singleton or fixed finite-family $\UniformCauchyCriterionUp$ packet share the same modulus, dyadic tolerance, tail ledger, stream-name source, regular-rational tail, and real-seal rows, so the family route restricts to the same finite CauchyCriterion path.

Finally apply \autoref{thm:uniform-cauchy-criterion-downstream-real-consumer-export}. Its downstream consumer clause keeps the $\RealUp$ seal on the uniform finite-family route. Intersecting the non-escape clauses from the three cited routes removes quotient streams, selected limits, host real equality, countable choice, ambient completeness, and any second family-limit route from the diagonal seal.
\end{proof}

\begin{theorem}[RealCauchyCompletion StreamName RegSeqRat Real seal correspondence]
\label{thm:real-cauchy-completion-streamname-regseqrat-real-seal-correspondence}
Every accepted $\RealCauchyCompletionUp$ packet whose regular family, unary modulus, diagonal packet, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, and $\RealUp$ seal rows are displayed determines exactly the same public finite-window seal boundary as the composed handoff
$$
\StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$
Conversely, any consumer that uses this composed handoff must factor through the displayed $\RealCauchyCompletionUp$ regular-family, diagonal, stream-window, readback, dyadic-ledger, and real-seal rows before it reads a limit-seal boundary.

The correspondence is relative to the finite-name, regular-sequence, dyadic, and real-name surfaces of \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert}. It exports no quotient stream, selected limit, host real equality, countable-choice row, or ambient completeness principle.
\end{theorem}
\begin{proof}
First use \autoref{thm:real-cauchy-completion-stream-regseq-tail-seal-factorization}. It places the finite $\StreamNameUp$ windows and $\RegSeqRatUp$ readback on the same tail-seal route as the accepted completion packet, so the stream-to-regular handoff is not a separate public boundary.

Next apply \autoref{thm:real-cauchy-completion-public-real-name-export}. This exports the public $\RealUp$ seal only through the displayed completion packet and the finite rows carried by the sibling $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ chapters.

Use \autoref{thm:real-cauchy-completion-regular-family-input-interface} to identify the input side of the route with the regular-family row and unary modulus. Then apply \autoref{thm:real-cauchy-completion-diagonal-seal-uniformity}, which keeps the diagonal packet and the real-seal consumer on one finite completion path.

For the converse, take any consumer of the composed $\StreamNameUp \to \RegSeqRatUp \to \RealUp$ handoff. The cited factorization and export theorems force its stream windows, regular-rational readback, dyadic ledger, and real seal to pass through the displayed completion rows before a limit-seal boundary is available. The sibling chapters supply exactly those finite public surfaces, so no consumer can bypass the $\RealCauchyCompletionUp$ rows while still reading the advertised boundary.
\end{proof}

\begin{theorem}[RealCauchyCompletion four-face exit uniqueness]
\label{thm:real-cauchy-completion-four-face-exit-uniqueness}
Every accepted $\RealCauchyCompletionUp$ public completion read that exports a $\RealUp$ seal through the L10 phase route has a unique finite support spine, up to displayed $\hsame$ transport and common-refinement comparison of finite windows. The spine consists of
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp
$$
together with the local $\BHist$, $\Cont$, $\Pkg$, and $\NameCert$ rows carried by the accepted completion packet.

Any purported public route that omits one of the four faces, duplicates a second seal boundary, or replaces a face by quotient stream equality, selected limits, host real equality, countable choice, or ambient completeness is not classifier-equivalent to the accepted public $\RealCauchyCompletionUp$ route.
\end{theorem}
\begin{proof}
Begin with the StreamName-RegSeqRat-Real seal correspondence, \autoref{thm:real-cauchy-completion-streamname-regseqrat-real-seal-correspondence}. It identifies the public seal boundary with the composed $\StreamNameUp \to \RegSeqRatUp \to \RealUp$ handoff and keeps the $\DyadicRatCoreUp$ ledger on the same displayed completion path.

Next apply current four-face phase support, \autoref{thm:real-cauchy-completion-current-phase-four-face-support-minimality}, and terminal support minimality, \autoref{thm:real-cauchy-completion-terminal-support-minimality}. These results force every accepted L10 phase read to display the four faces and the local $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows; removing or replacing any one of them collapses the route rather than producing another public completion witness.

Now compare two accepted presentations. Pull both to a certified finite common refinement of their stream windows and use \autoref{thm:real-cauchy-completion-terminal-support-common-refinement-comparison}. The pulled-back routes determine the same BEDC completion witness, so the only variation left is the displayed $\hsame$ transport and the finite-window cover used to present the same support spine.

It remains to exclude alternate exits. Terminal support defect localization, \autoref{thm:real-cauchy-completion-terminal-support-defect-localization}, places any missing face, missing support row, duplicated seal boundary, or substituted quotient, selected-limit, host-equality, choice, or completeness coordinate at a named defect. Such a presentation is therefore not classifier-equivalent to the accepted public route.
\end{proof}

\begin{theorem}[RealCauchyCompletion tail-budget uniform seal boundary]
\label{thm:real-cauchy-completion-tail-budget-uniform-seal-boundary}
Every accepted $\RealCauchyCompletionUp$ packet whose finite $\StreamNameUp$ windows, $\RegSeqRatUp$ tail selector, dyadic budget row, and $\RealUp$ endpoint seal satisfy the StreamName-RegSeqRat tail-budget uniformity hypotheses has a uniform completion-facing seal boundary. Every later finite window accepted by the same pointwise $\StreamNameUp$ classifier factors through the same displayed completion route.

Consequently the completion seal exports no new limit selector, quotient stream, host equality, countable-choice row, ambient completeness datum, or second completion-facing boundary beyond the displayed regular-family row, unary modulus, diagonal packet, finite stream windows, regular-rational readback, dyadic budget ledger, $\RealUp$ endpoint seal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\end{theorem}
\begin{proof}
Start with \autoref{thm:real-cauchy-completion-streamname-regseqrat-real-seal-correspondence}. It identifies the public completion seal boundary with the composed $\StreamNameUp \to \RegSeqRatUp \to \RealUp$ handoff, relative to the displayed regular-family, diagonal, stream-window, readback, dyadic-ledger, and real-seal rows.

Next apply \autoref{thm:streamname-regseqrat-tail-budget-uniformity}. For every later finite window accepted by the same pointwise StreamName classifier, the RegSeqRat tail selector and the RealUp budget consumer read the same tail-budget certificate. Thus enlarging the finite window changes only the visible window ledger.

Transport this uniform handoff back through \autoref{thm:real-cauchy-completion-stream-regseq-tail-seal-factorization} and \autoref{thm:real-cauchy-completion-public-real-name-export}. The first theorem keeps the StreamName windows and RegSeqRat readback on the completion tail-seal route, and the second theorem exposes the public RealUp seal only through the displayed completion rows.

The non-escape clauses of the cited routes now intersect at the same boundary: the later windows share one completion-facing route and cannot introduce a fresh limit selector, quotient stream, host equality, countable-choice row, ambient completeness datum, or second seal boundary.
\end{proof}

\begin{theorem}[RealCauchyCompletion witness-ledger observation-budget route]
\label{thm:real-cauchy-completion-witness-ledger-observation-budget-route}
Every accepted $\RealCauchyCompletionUp$ carrier whose completion-facing consumer is read through both an accepted $\CauchyWitnessLedgerUp$ packet and an accepted $\RealObservationBudgetUp$ packet reaches the public $\RealUp$ seal only through one shared finite route. The route consists exactly of the regular-family row, unary modulus, diagonal packet, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, witness-ledger budget, synchronizer, and classifier rows, observation-budget precision, window, and disclosure rows, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

The route exports no quotient equality of regular streams, host real equality, selected limit, hidden unbounded observation schedule, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
First apply the witness-ledger classifier lock \autoref{thm:real-cauchy-completion-witness-ledger-classifier-lock}. It places the completion-facing public seal behind the regular-family row, unary modulus, diagonal packet, finite windows, regular-rational readback, dyadic ledger, and witness-ledger budget, synchronizer, and classifier rows. The uniform seal route \autoref{thm:real-cauchy-completion-witness-ledger-uniform-seal-route} keeps this consumer path on the same finite seal boundary.

Next apply the budgeted common-refinement theorem \autoref{thm:real-cauchy-completion-budgeted-common-refinement}. The accepted $\RealObservationBudgetUp$ packet supplies the precision rows, finite $\StreamNameUp$ windows, dyadic readbacks, regular handoffs, seal boundary, $\hsame$ transports, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows for the same request surface.

Finally intersect the observation-budget window coverage, real-seal boundary, and non-escape rows from \autoref{thm:real-observation-budget-window-coverage}, \autoref{thm:real-observation-budget-real-seal-boundary}, and \autoref{thm:real-observation-budget-nonescape}. The two readings therefore share one finite completion-facing route, and all omitted objects stay outside the public $\RealUp$ seal.
\end{proof}

\begin{theorem}[RealCauchyCompletion finite-window completion coverage]
\label{thm:real-cauchy-completion-finite-window-completion-coverage}
For every accepted $\RealCauchyCompletionUp$ packet and every finite $\mathsf{ProbeBundle}$ of unary precision requests, the completion-facing $\RealUp$ seal read is exhausted by the displayed finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback rows, $\DyadicRatCoreUp$ observation ledger, diagonal packet, $\RealUp$ seal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The exhausted route is the same finite-name path from $\StreamNameUp$ through $\RegSeqRatUp$ and $\DyadicRatCoreUp$ into $\RealUp$. It exports no quotient stream, selected limit, host real equality, countable-choice row, ambient completeness coordinate, or completion principle beyond those displayed certificate rows.
\end{theorem}
\begin{proof}
First apply \autoref{thm:real-cauchy-completion-probebundle-window-coverage}. It turns the finite $\mathsf{ProbeBundle}$ into one consumer surface whose pointwise requests pass through the displayed modulus rows, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback rows, $\DyadicRatCoreUp$ ledgers, and the structural $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ data.

Next use the StreamName handoff and the sibling finite-window coverage rows for regular sequences, dyadic observations, and real-name seals. These identify each window read as the same finite route from $\StreamNameUp$ to $\RegSeqRatUp$, then through $\DyadicRatCoreUp$ observation data into the public $\RealUp$ seal.

Finally apply \autoref{thm:real-cauchy-completion-streamname-regseqrat-real-seal-correspondence} together with the non-escape boundary of \autoref{thm:real-cauchy-completion-non-escape-boundary}. The composed route is therefore the only completion-facing seal read available to the accepted packet, and quotient streams, selected limits, host real equality, countable choice, ambient completeness, and any hidden completion coordinate remain outside the exported surface.
\end{proof}

\begin{theorem}[RealCauchyCompletion monadic four-face exit correspondence]
\label{thm:real-cauchy-completion-monadic-four-face-exit-correspondence}
For every accepted $\RealCauchyCompletionUp$ packet whose visible rows are the regular-family row, unary modulus, diagonal packet, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\RealUp$ seal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, the public L10 four-face exit coincides with the finite completion-transformer route read through $\CauchyCompletionMonadUp$.

Both readings expose exactly the same four roadmap objects $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. Neither reading adds quotient stream equality, selected limits, host real equality, countable choice, ambient completeness, or bridge-axis evidence.
\end{theorem}
\begin{proof}
Start with \autoref{thm:real-cauchy-completion-streamname-regseqrat-real-seal-correspondence}. It identifies the public completion seal with the finite handoff through $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, while keeping the $\DyadicRatCoreUp$ ledger on the same displayed completion route.

Apply \autoref{thm:real-cauchy-completion-finite-window-completion-coverage}. Every finite precision request is exhausted by the displayed windows, readbacks, dyadic ledger, diagonal packet, real seal, and structural $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, so the L10 exit has no additional coordinate.

Now read the same boundary through \autoref{ch:concrete-instances-cauchycompletionmonad-namecert}. Its unit, bind, public seal, finite-window exhaustion, and standard completion-boundary rows use the same four roadmap objects as the completion transformer. Intersecting the non-escape clauses on both sides gives one shared four-face exit and excludes quotient streams, selected limits, host real equality, countable choice, ambient completeness, and bridge-axis evidence.
\end{proof}

\begin{theorem}[RealCauchyCompletion monadic unit-bind locality]
\label{thm:real-cauchy-completion-monadic-unit-bind-locality}
For every accepted $\RealCauchyCompletionUp$ packet whose public read is compared with an accepted $\CauchyCompletionMonadUp$ unit-and-bind route, the unit packet, bind packet, and downstream completion consumer factor through the same displayed L10 four-face route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$
The shared route carries the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows as the direct $\RealCauchyCompletionUp$ read.

The comparison is local to that route. It introduces no second completion source, quotient stream, selected limit, host equality, countable-choice row, ambient completeness principle, or bridge-axis evidence.
\end{theorem}
\begin{proof}
Begin with the monadic four-face exit correspondence of \autoref{thm:real-cauchy-completion-monadic-four-face-exit-correspondence}. It identifies the direct $\RealCauchyCompletionUp$ public read with the $\CauchyCompletionMonadUp$ completion-transformer read on the same four roadmap objects.

Next apply the Kleisli source triangle \autoref{thm:cauchy-completion-monad-kleisli-source-triangle}. The unit source, bind source, and downstream consumer source are forced through the displayed finite route, so the monadic interface does not add another completion origin.

Now use the finite-route correspondence of \autoref{thm:cauchy-completion-monad-l10-exit-finite-route-correspondence}. The unit and bind packets inherit the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows already visible on the direct completion read, and the downstream consumer reads those rows rather than a separate quotient or selected-limit coordinate.

Finally intersect this monadic route with the terminal support minimality and non-escape rows local to this chapter. The only available unit-bind support is the displayed L10 four-face route, while second completion sources, quotient streams, selected limits, host equality, countable choice, ambient completeness, and bridge-axis evidence remain outside the accepted public surface.
\end{proof}

\begin{theorem}[RealCauchyCompletion terminal support minimality]
\label{thm:real-cauchy-completion-terminal-support-minimality}
Every accepted public L10 completion read through $\RealCauchyCompletionUp$ retains the four displayed faces
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp .
$$
It also retains the visible $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows carried by the completion packet. Deleting or replacing any one of these four faces, or deleting any one of the displayed structural rows, collapses the public completion route rather than producing another BEDC completion witness.

The minimality claim is internal to the public L10 completion surface. It does not identify quotient streams, choose limiting representatives, import host real equality, assume countable choice, assert ambient completeness, or add bridge-axis evidence.
\end{theorem}
\begin{proof}
Start with the composed public seal handoff of \autoref{thm:real-cauchy-completion-streamname-regseqrat-real-seal-correspondence}. That result identifies the completion boundary with the displayed $\StreamNameUp \to \RegSeqRatUp \to \RealUp$ route, while the dyadic ledger remains the finite observation support for the same read. Thus the four faces appear as part of one route, not as replaceable decorations around the carrier.

Next apply \autoref{thm:real-cauchy-completion-finite-window-completion-coverage}. Every finite precision request is exhausted by the displayed windows, readbacks, dyadic ledger, diagonal packet, real seal, and the structural $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. Removing one of these rows removes a named step used by the public request surface.

Now compare the same terminal read with the monadic four-face exit correspondence of \autoref{thm:real-cauchy-completion-monadic-four-face-exit-correspondence}. The $\CauchyCompletionMonadUp$ reading and the direct $\RealCauchyCompletionUp$ reading expose the same four roadmap objects, so replacing a face does not yield another advertised completion exit; it changes the route outside the correspondence.

Finally use the non-escape boundary of \autoref{thm:real-cauchy-completion-non-escape-boundary}. Quotient streams, selected limits, host real equality, countable choice, ambient completeness, and bridge-axis evidence are excluded from the carrier surface. With no excluded object available as substitute support, any deletion or replacement collapses the public L10 completion route rather than yielding a second BEDC completion witness.
\end{proof}

\begin{theorem}[RealCauchyCompletion current four-face phase support]
\label{thm:real-cauchy-completion-current-phase-four-face-support-minimality}
A Phase Real and Completion exit read through $\RealCauchyCompletionUp$ is valid only when the four faces
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp
$$
are all present as the displayed completion route. The same read must also display the $\BHist$ histories, componentwise $\hsame$ transports, $\Cont$ replay rows, $\Pkg$ provenance row, and local $\NameCert_{\RealCauchyCompletionUp}$ support rows carried by the accepted completion packet.

Thus $\RealCauchyCompletionUp$ supplies the roadmap-facing Phase Real and Completion exit exactly as a four-face BEDC support condition. It supplies no quotient stream equality, host real equality, selected limit, countable-choice row, ambient completeness principle, or bridge-axis evidence in place of any one of the four faces or support rows.
\end{theorem}
\begin{proof}
First apply \autoref{thm:real-cauchy-completion-terminal-support-minimality}. It says that every accepted public L10 completion read through $\RealCauchyCompletionUp$ retains the four displayed faces and the visible $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows, and that deleting or replacing any one of them collapses the completion route.

Next use \autoref{thm:real-cauchy-completion-monadic-four-face-exit-correspondence}. The direct $\RealCauchyCompletionUp$ reading and the $\CauchyCompletionMonadUp$ completion-transformer reading expose the same four roadmap objects, so the local completion route is already the Phase Real and Completion route rather than a separate completion-only support condition.

Finally compose with the Real-side exit criterion \autoref{thm:real-l10-four-face-exit-criterion}. That criterion accepts a Phase Real and Completion endpoint read exactly when $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and the local $\RealUp$ seal appear in order with their local records and the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The excluded coordinates are removed by the non-escape clauses in the three cited results. Quotient streams, host real equality, selected limits, countable choice, ambient completeness, and bridge-axis evidence are not support rows of the four-face packet, so none can serve as substitute evidence for the phase exit read.
\end{proof}

\begin{theorem}[RealCauchyCompletion four-face status noncollapse]
\label{thm:real-cauchy-completion-four-face-status-noncollapse}
A Phase Real and Completion exit read through $\RealCauchyCompletionUp$ whose route displays
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp
$$
and the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows reads $\mathsf{closurestatus}$, $\mathsf{formalstatus}$, $\mathsf{leantarget}$, and $\mathsf{bridgestatus}$ only from the four local face records.

The same route has no aggregate formalstatus row, replacement leantarget, route-level bridge credential, standard bridge row, selected limit, quotient stream equality, host real equality, countable-choice row, metric-completeness datum, ambient completeness principle, or $\RealUp$ completeness coordinate.
\end{theorem}
\begin{proof}
First apply \autoref{thm:real-cauchy-completion-current-phase-four-face-support-minimality}. It identifies the Phase Real and Completion read through $\RealCauchyCompletionUp$ with the displayed four faces and with the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows.

Next use \autoref{thm:real-l10-four-face-exit-criterion} and \autoref{thm:real-l10-objectwise-face-status-exit}. These results read the roadmap exit objectwise from the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and local $\RealUp$ face records. Therefore the closure, verification, Lean-target, and bridge-status rows remain face-local reports.

It remains to rule out aggregate status substitutes. Apply \autoref{thm:real-l10-face-status-noncollapse} and \autoref{thm:real-l10-face-leantarget-noncollapse}. They block a route-level formalstatus row, replacement leantarget, aggregate bridge credential, or standard bridge row from replacing the four local face reports.

Finally apply the non-escape boundary of \autoref{thm:real-cauchy-completion-non-escape-boundary}. Quotient stream equality, selected limits, host real equality, countable choice, ambient completeness, and bridge-axis evidence are outside the accepted completion carrier. Together with the face-local status reading, this also excludes metric-completeness data and a separate $\RealUp$ completeness coordinate from the current four-face route.
\end{proof}

\begin{theorem}[RealCauchyCompletion terminal support defect localization]
\label{thm:real-cauchy-completion-terminal-support-defect-localization}
Let a Phase Real and Completion exit read through $\RealCauchyCompletionUp$ be presented as a public BEDC completion route. If that presentation omits or replaces any one of the four faces
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp
$$
or omits or replaces any visible $\BHist$, $\hsame$, $\Cont$, $\Pkg$, or $\NameCert$ support row carried by the accepted completion packet, then the failure is witnessed at that named face or support row. The presentation is therefore not an accepted BEDC completion route through $\RealCauchyCompletionUp$.

No quotient stream equality, selected limit, host real equality, countable-choice row, ambient completeness principle, or bridge-axis evidence can repair the failed presentation or move the failure away from the omitted or replaced face or support row.
\end{theorem}
\begin{proof}
Apply the terminal support minimality theorem, \autoref{thm:real-cauchy-completion-terminal-support-minimality}. It states that every accepted public L10 completion read retains the four displayed faces and the visible $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows, and that deleting or replacing any one of them collapses the route.

Next apply the monadic four-face exit correspondence of \autoref{thm:real-cauchy-completion-monadic-four-face-exit-correspondence}. The direct $\RealCauchyCompletionUp$ read and the $\CauchyCompletionMonadUp$ read expose the same four roadmap objects, so a defect in one of those faces cannot be transferred to a different completion-transformer coordinate.

Now use \autoref{thm:real-cauchy-completion-current-phase-four-face-support-minimality} together with the Real-side criterion \autoref{thm:real-l10-four-face-exit-criterion}. These results identify the Phase Real and Completion endpoint with the ordered $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ route and with the same local $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. Hence the failed presentation has a named local witness: exactly the face or support row that is absent or replaced.

Finally apply the non-escape boundary of \autoref{thm:real-cauchy-completion-non-escape-boundary}. Quotient streams, selected limits, host equality, countable choice, ambient completeness, and bridge-axis evidence are outside the accepted carrier surface. Since no excluded coordinate is available as substitute support, the malformed presentation is rejected at the located face or row and is not a BEDC completion route through $\RealCauchyCompletionUp$.
\end{proof}

\begin{theorem}[RealCauchyCompletion terminal support refinement invariance]
\label{thm:real-cauchy-completion-terminal-support-refinement-invariance}
Let $\mathcal P$ be a coarse public L10 completion route through $\RealCauchyCompletionUp$, and let $\mathcal Q$ be a finite certified refinement of $\mathcal P$. Suppose both presentations display the same four faces
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp
$$
and suppose every refined member, refined overlap, and comparison map preserves the same visible $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows carried by the accepted completion packet. Then the route accepted from $\mathcal P$ and the route accepted from $\mathcal Q$ determine the same BEDC completion witness through $\RealCauchyCompletionUp$.

This comparison changes only the finite cover presentation of the terminal four-face route. It introduces no quotient stream, selected limit, host real equality, countable-choice row, ambient completeness principle, metric-completeness datum, aggregate $\formalstatus$ row, aggregate bridge row, or bridge-axis evidence.
\end{theorem}
\begin{proof}
First apply terminal support minimality, \autoref{thm:real-cauchy-completion-terminal-support-minimality}, and current four-face phase support, \autoref{thm:real-cauchy-completion-current-phase-four-face-support-minimality}. Both the coarse route and the refined route must therefore be read from the displayed $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ faces together with the same visible $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows.

Next localize possible differences by \autoref{thm:real-cauchy-completion-terminal-support-defect-localization}. Since every refined member, overlap, and comparison map preserves the named faces and support rows, no defect can be moved to a new completion coordinate. The only remaining difference is the finite cover by which the same terminal route is presented.

Apply the DyadicRatCore finite-cover comparison, \autoref{thm:dyadicratcore-l10-exit-ledger-finite-cover-refinement-invariance}. It identifies the dyadic ledger and the four-face exit packet obtained from the coarse cover with the one obtained from the refined cover, because both presentations preserve the displayed DyadicRatCore, StreamName, RegSeqRat, Real, and support rows.

Finally use RegSeqRat common-tail cocycle descent, in particular \autoref{thm:regseqrat-common-tail-atlas-refinement-invariance}. The common-tail members, overlaps, and cocycle rows glue to the same Real seal-visible boundary and the same Phase $\RealUp$ and Completion audit-threshold coordinate. Thus the refined presentation changes only the finite support cover, while the accepted public completion witness through $\RealCauchyCompletionUp$ remains the same.
\end{proof}

\begin{theorem}[RealCauchyCompletion terminal support common-refinement comparison]
\label{thm:real-cauchy-completion-terminal-support-common-refinement-comparison}
Let $\mathcal P$ and $\mathcal Q$ be accepted public L10 completion routes through $\RealCauchyCompletionUp$. Suppose there is a certified finite common refinement $\mathcal R$ of the two presentations such that, after pullback to $\mathcal R$, both routes display the same four faces
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp
$$
and preserve the same visible $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows carried by the accepted completion packet. Then $\mathcal P$ and $\mathcal Q$ determine the same BEDC completion witness through $\RealCauchyCompletionUp$.

The comparison is entirely internal to the certified common finite support. It introduces no quotient stream, selected limit, host real equality, countable-choice row, ambient completeness principle, metric-completeness datum, aggregate $\formalstatus$ row, aggregate bridge row, standard bridge row, or bridge-axis evidence.
\end{theorem}
\begin{proof}
First pull the two accepted presentations back to the common refinement $\mathcal R$. By terminal support minimality, \autoref{thm:real-cauchy-completion-terminal-support-minimality}, both pulled-back routes are still read from the displayed $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ faces and from the same visible $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Next apply terminal support defect localization, \autoref{thm:real-cauchy-completion-terminal-support-defect-localization}. Since the hypotheses preserve every named face and every support row after pullback to $\mathcal R$, a disagreement between the two accepted presentations cannot be relocated to a hidden completion coordinate.

Use terminal support refinement invariance, \autoref{thm:real-cauchy-completion-terminal-support-refinement-invariance}, on the refinement from $\mathcal P$ to $\mathcal R$ and again on the refinement from $\mathcal Q$ to $\mathcal R$. Thus each original route determines the same BEDC completion witness as its pulled-back route on the certified common support.

It remains to identify the pulled-back terminal support itself. Apply \autoref{thm:streamname-tail-window-cover-common-refinement} to form the shared StreamName finite window, then \autoref{thm:streamname-real-regseqrat-seal-common-refinement} to read the common terminal Real seal over that window. Finally use \autoref{thm:regseqrat-common-tail-atlas-refinement-invariance} to keep the RegSeqRat common-tail atlas and dyadic ledger invariant over the same pullback.

The non-escape clauses in the preceding results rule out all substitute coordinates. Quotient streams, selected limits, host equality, countable choice, ambient completeness, metric-completeness data, aggregate status rows, bridge rows, and bridge-axis evidence are not part of the visible terminal support, so they cannot distinguish the two accepted completion witnesses.
\end{proof}

\begin{theorem}[RealCauchyCompletion terminal support refinement composition]
\label{thm:real-cauchy-completion-terminal-support-refinement-composition}
Let $\mathcal P$, $\mathcal Q$, and $\mathcal R$ be public L10 completion routes through $\RealCauchyCompletionUp$. Suppose $\mathcal Q$ is a finite certified refinement of $\mathcal P$, and $\mathcal R$ is a finite certified refinement of $\mathcal Q$. Suppose also that every refined member, overlap, and comparison map in both refinements preserves the same four faces
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp
$$
and the same visible $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows carried by the accepted completion packet. Then $\mathcal P$ and $\mathcal R$ determine the same BEDC completion witness through $\RealCauchyCompletionUp$.

The successive comparison remains internal to the terminal four-face route. It introduces no quotient stream, selected limit, host real equality, countable-choice row, ambient completeness principle, metric-completeness datum, aggregate $\formalstatus$ row, aggregate bridge row, standard bridge row, or bridge-axis evidence.
\end{theorem}
\begin{proof}
First apply terminal support refinement invariance, \autoref{thm:real-cauchy-completion-terminal-support-refinement-invariance}, to the certified refinement $\mathcal P \to \mathcal Q$. The coarse route $\mathcal P$ and the intermediate route $\mathcal Q$ therefore read the same accepted BEDC completion witness from the displayed $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ faces and the same visible support rows.

Apply the same theorem again to the certified refinement $\mathcal Q \to \mathcal R$. Since the second refinement preserves the same faces, overlaps, comparison maps, and $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, the intermediate route $\mathcal Q$ and the final route $\mathcal R$ also determine one completion witness.

It remains to exclude a new coordinate appearing only at the intermediate stage. Use terminal support defect localization, \autoref{thm:real-cauchy-completion-terminal-support-defect-localization}. Any proposed difference between the two comparisons would have to be witnessed by an omitted or replaced face or support row, but the hypotheses preserve every named face, overlap, comparison map, and support row across both refinements.

Finally apply the non-escape boundary of \autoref{thm:real-cauchy-completion-non-escape-boundary}. Quotient streams, selected limits, host equality, countable choice, ambient completeness, and bridge-axis evidence are outside the accepted carrier surface, and the status and bridge rows cannot aggregate outside the four local face records. Thus the two certified refinement steps compose without changing the BEDC completion witness determined through $\RealCauchyCompletionUp$.
\end{proof}

\begin{theorem}[RealCauchyCompletion terminal support refinement cocycle]
\label{thm:real-cauchy-completion-terminal-support-refinement-cocycle}
Let $\mathcal P$ and $\mathcal Q$ be two finite certified terminal-support refinements of one public L10 completion route through $\RealCauchyCompletionUp$. Suppose both refinements preserve the same four faces
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp
$$
and the same visible $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows carried by the accepted completion packet. Suppose moreover that the two refinements carry the same $\RegSeqRatUp$ common-tail pairwise packets on every retained overlap, and that their triple-overlap rows satisfy the same common-tail cocycle over the terminal four-face support. Then $\mathcal P$ and $\mathcal Q$ determine the same BEDC completion witness through $\RealCauchyCompletionUp$.

The path through a finite refinement diagram is therefore independent of the certified terminal-support cover. The comparison introduces no quotient stream, selected limit, host real equality, countable-choice row, ambient completeness principle, metric-completeness datum, aggregate $\formalstatus$ row, aggregate bridge row, standard bridge row, or bridge-axis evidence.
\end{theorem}
\begin{proof}
First restrict both refinements to the displayed terminal support. By terminal support minimality, \autoref{thm:real-cauchy-completion-terminal-support-minimality}, and terminal support defect localization, \autoref{thm:real-cauchy-completion-terminal-support-defect-localization}, every accepted comparison must be witnessed on the named $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ faces and on the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Next compare the dyadic and four-face ledgers. The finite-cover refinement invariance theorem \autoref{thm:dyadicratcore-l10-exit-ledger-finite-cover-refinement-invariance} identifies the DyadicRatCore exit ledger obtained from either certified support cover, since the endpoint, radius, stream-window, regular-sequence, Real, and support rows are the same.

Now descend the common-tail data. Apply \autoref{thm:regseqrat-common-tail-cocycle-descent} to the shared pairwise packets and triple-overlap rows, and then use \autoref{thm:regseqrat-common-tail-terminal-four-face-cocycle-uniqueness}. These two results identify the two refined RegSeqRat common-tail presentations as one terminal four-face cocycle boundary.

Finally pass from the common terminal boundary to the completion witness. The Real L10 terminal four-face correspondence, \autoref{thm:real-l10-terminal-four-face-correspondence}, repacks the DyadicRatCore, StreamName, RegSeqRat, and Real rows as the accepted terminal four-face read, and \autoref{thm:real-l10-terminal-pullback-uniqueness} forbids a second pullback over the same visible rows. Together with terminal support refinement composition, \autoref{thm:real-cauchy-completion-terminal-support-refinement-composition}, this shows that both finite paths determine the same BEDC completion witness through $\RealCauchyCompletionUp$.

The exclusion clauses follow from the cited support and terminality results. None of the comparison data carries a quotient stream, selected limit, host real equality, countable-choice row, ambient completeness principle, metric-completeness datum, aggregate $\formalstatus$ row, aggregate bridge row, standard bridge row, or bridge-axis evidence, so no such coordinate can distinguish the two certified refinement paths.
\end{proof}

\begin{theorem}[RealCauchyCompletion request-section terminal exhaustion]
\label{thm:real-cauchy-completion-request-section-terminal-exhaustion}
For every accepted $\RealCauchyCompletionUp$ carrier and every finite $\mathsf{ProbeBundle}$ of unary precision requests, the terminal completion-facing read is exhausted by one finite request-section route through
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp
$$
and through the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows. Any other request-section route that agrees on these four faces and support rows is classifier-equivalent to it after finite common refinement.

The exhausted route exports no quotient stream, selected limit, host real equality, countable-choice row, ambient completeness principle, metric-completeness datum, aggregate $\formalstatus$ row, aggregate bridge row, or bridge-axis evidence.
\end{theorem}
\begin{proof}
Start with the finite probe bundle. The probe-bundle window coverage theorem, \autoref{thm:real-cauchy-completion-probebundle-window-coverage}, replaces the unary precision requests by a finite completion window without adding any completion coordinate beyond the accepted carrier surface.

Apply finite-window completion coverage, \autoref{thm:real-cauchy-completion-finite-window-completion-coverage}. This identifies the window with the four-face request-section route through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, together with the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.

Now compare another route agreeing on those rows. Use terminal support common-refinement comparison, \autoref{thm:real-cauchy-completion-terminal-support-common-refinement-comparison}, to pull the two request-section presentations to a certified finite common refinement. Then terminal support refinement cocycle, \autoref{thm:real-cauchy-completion-terminal-support-refinement-cocycle}, makes the resulting classifier independent of the finite support cover chosen along the way.

It remains to exclude hidden support. Terminal support defect localization, \autoref{thm:real-cauchy-completion-terminal-support-defect-localization}, places any missing or substituted face or support row at the named local defect. The non-escape boundary, \autoref{thm:real-cauchy-completion-non-escape-boundary}, prevents quotient streams, selected limits, host real equality, countable choice, ambient completeness, metric-completeness data, aggregate status rows, aggregate bridge rows, and bridge-axis evidence from serving as replacement output. Thus the finite request-section route exhausts the accepted terminal completion read.
\end{proof}

\begin{theorem}[RealCauchyCompletion terminal exit locality]
\label{thm:real-cauchy-completion-terminal-exit-locality}
Every accepted terminal exit read through $\RealCauchyCompletionUp$ for the Phase Real and Completion boundary factors through exactly the four local faces
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp
$$
together with the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows carried by the accepted completion packet. The locality assertion is facewise: the dyadic ledger, finite stream windows, regular-rational readback, and local real seal remain separate rows, and no route-level status or bridge coordinate replaces any one of them.

The terminal read therefore has no quotient-stream row, selected-limit row, host real equality row, countable-choice row, metric-completeness datum, aggregate $\formalstatus$ row, aggregate bridge row, standard bridge row, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Start with the request-section terminal exhaustion theorem, \autoref{thm:real-cauchy-completion-request-section-terminal-exhaustion}. It exhausts every finite unary request bundle by a route through the displayed $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ faces and through the listed support rows.

Apply terminal support minimality, \autoref{thm:real-cauchy-completion-terminal-support-minimality}, and current four-face phase support, \autoref{thm:real-cauchy-completion-current-phase-four-face-support-minimality}. These results force the same four faces to remain present for any accepted terminal read and prevent a replacement face from presenting the same completion witness.

The face-local status reading is supplied by \autoref{thm:real-cauchy-completion-four-face-status-noncollapse} together with \autoref{thm:real-l10-objectwise-face-status-exit}. The closure, verification, Lean-target, and bridge-status information is read from the four component records rather than from an aggregate route row.

Finally use the sibling route inventory. The CauchyCriterion route, \autoref{thm:real-cauchy-completion-cauchycriterion-sibling-route}, the Real-side terminal correspondence \autoref{thm:real-l10-terminal-four-face-correspondence}, and the RegSeqRat consumer normal form \autoref{thm:regseqrat-four-face-exit-consumer-normal-form} all expose only the same finite rows. Their non-escape clauses exclude quotient streams, selected limits, host equality, countable choice, metric-completeness data, aggregate status rows, aggregate bridge rows, standard bridge rows, and ambient $\RealUp$ completeness from the terminal exit surface.
\end{proof}

\begin{theorem}[RealCauchyCompletion terminal exit normal form]
\label{thm:real-cauchy-completion-terminal-exit-normal-form}
For an accepted $\RealCauchyCompletionUp$ carrier, the terminal Phase Real and Completion exit certificate has the following normal form. It is a finite BEDC packet whose four visible faces are exactly
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp
$$
and whose only shared support rows are $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. A consumer reads the dyadic observation ledger, then the finite stream-window schedule, then the regular-rational readback and common-tail rows, and finally the local $\RealUp$ seal through those support rows.

Conversely, any accepted terminal packet exposing precisely those four faces with the same displayed support rows determines the same Phase Real and Completion exit certificate. The normal form excludes quotient streams, selected limits, host real equality, countable-choice rows, metric-completeness data, aggregate $\formalstatus$ rows, aggregate bridge rows, standard bridge rows, and $\RealUp$ completeness.
\end{theorem}
\begin{proof}
For the forward direction, apply terminal exit locality, \autoref{thm:real-cauchy-completion-terminal-exit-locality}. Every accepted terminal read has already been reduced to the four named faces and the shared support rows.

Next identify the order of the rows. The request-section terminal exhaustion theorem, \autoref{thm:real-cauchy-completion-request-section-terminal-exhaustion}, supplies the finite request route, while \autoref{thm:streamname-four-object-exit-exactness} and \autoref{thm:dyadicratcore-phase-real-completion-objectwise-exit-equivalence} align the stream-window, dyadic-ledger, RegSeqRat, and Real rows with the Phase Real and Completion exit surface.

For the reverse direction, suppose a terminal packet exposes exactly the four faces and support rows listed in the statement. Apply the Real L10 terminal four-face correspondence, \autoref{thm:real-l10-terminal-four-face-correspondence}, to repack the displayed rows as the accepted terminal $\RealUp$ read. Then use terminal pullback uniqueness, \autoref{thm:real-l10-terminal-pullback-uniqueness}, and the RegSeqRat four-face terminal pullback, \autoref{thm:regseqrat-four-face-terminal-pullback}, to identify the same completion-facing boundary.

It remains to rule out additional coordinates in the normal form. Terminal support defect localization, \autoref{thm:real-cauchy-completion-terminal-support-defect-localization}, makes any missing, duplicated, or substituted face a named local defect. The four-face status noncollapse theorem, \autoref{thm:real-cauchy-completion-four-face-status-noncollapse}, and the non-escape boundary, \autoref{thm:real-cauchy-completion-non-escape-boundary}, remove quotient streams, selected limits, host equality, countable choice, metric-completeness data, aggregate status rows, aggregate bridge rows, standard bridge rows, and ambient $\RealUp$ completeness from the accepted packet.
\end{proof}

\begin{theorem}[RealCauchyCompletion terminal exit support invariance]
\label{thm:real-cauchy-completion-terminal-exit-support-invariance}
Let $\mathcal P$ and $\mathcal Q$ be accepted terminal exit presentations of the same $\RealCauchyCompletionUp$ carrier. Suppose both presentations expose the four local faces
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp
$$
and preserve the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows after passage to a certified finite common refinement. Then $\mathcal P$ and $\mathcal Q$ determine the same terminal Phase Real and Completion exit certificate.

The invariant certificate is exactly the normal form of \autoref{thm:real-cauchy-completion-terminal-exit-normal-form}. It is unaffected by the choice of finite support cover and contains no quotient streams, selected limits, host real equality, countable-choice rows, metric-completeness data, aggregate $\formalstatus$ rows, aggregate bridge rows, standard bridge rows, or $\RealUp$ completeness.
\end{theorem}
\begin{proof}
First pass both presentations to the certified finite common refinement. Terminal support common-refinement comparison, \autoref{thm:real-cauchy-completion-terminal-support-common-refinement-comparison}, identifies the pulled-back routes whenever the four faces and the shared support rows agree there.

Next remove dependence on the chosen finite support cover. Terminal support refinement invariance, \autoref{thm:real-cauchy-completion-terminal-support-refinement-invariance}, and terminal support refinement cocycle, \autoref{thm:real-cauchy-completion-terminal-support-refinement-cocycle}, show that the path through the refinement diagram changes only the cover presentation, not the BEDC completion witness.

Now repack the common refined support by the terminal exit normal form, \autoref{thm:real-cauchy-completion-terminal-exit-normal-form}. The repacked packet has exactly the DyadicRatCore, StreamName, RegSeqRat, Real, and support rows listed in the statement, so both original presentations read the same Phase Real and Completion certificate.

The exclusion clauses are inherited from the same normal form and from terminal support defect localization, \autoref{thm:real-cauchy-completion-terminal-support-defect-localization}. Any alleged difference outside the displayed rows would have to be a quotient stream, selected limit, host equality, choice row, metric-completeness datum, aggregate status row, aggregate bridge row, standard bridge row, or ambient $\RealUp$ completeness coordinate, and none of these is part of the accepted terminal support.
\end{proof}
