\chapter{A Concrete Naming Certificate for $\BerryEsseenFiniteWindowUp$}
\label{ch:concrete-instances-berry-esseen-finite-window-namecert}
\origin{human}

Following Berry and Esseen on finite normal-approximation error bounds, the $\BerryEsseenFiniteWindowUp$ packet records the BEDC surface that attaches an explicit finite error-bound row to a central-limit-facing window. It sits after the finite central-limit carrier of \autoref{ch:concrete-instances-central-limit-finite-window-namecert}, the Lindeberg replacement surface of \autoref{ch:concrete-instances-lindeberg-condition-namecert}, the distribution readback of \autoref{ch:concrete-instances-distribution-namecert}, the regular rational comparison window of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only a finite normal-window error certificate; it does not import an asymptotic central limit theorem, a host distribution metric, countable choice, quotient law equality, or ambient $\RealUp$ completeness.

\begin{definition}[Berry-Esseen finite-window carrier]
\label{def:berry-esseen-finite-window-carrier}
A $\BerryEsseenFiniteWindowUp$ carrier is a finite $\BHist$ packet
$$
B=(C,L,D,R,E,G,H,K,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $C$ is the $\CentralLimitFiniteWindowUp$ source row naming the displayed centered and scaled finite-sum window.
\item $L$ is the $\LindebergConditionUp$ row naming the finite truncation, variance, and replacement-condition data used by the bound.
\item $D$ is the $\DistributionUp$ readback row for the normalized finite sum and the displayed Gaussian comparison target.
\item $R$ is the $\RegSeqRatUp$ comparison row carrying rational intervals, tolerances, and the finite third-moment or tail-budget samples read by the error estimate.
\item $E$ is the $\RealUp$ error-bound seal row, recording the displayed constant token, scale token, sample-count token, and terminal inequality request.
\item $G$ is the Gaussian-window row, naming the finite normal comparison window exposed to downstream consumers.
\item $H$ records componentwise $\hsame$ transport across the central-limit, Lindeberg, distribution, RegSeqRat, Real error-bound, Gaussian-window, replay, provenance, and local naming rows.
\item $K$ records displayed $\Cont$ replay from a normal-approximation request through $C,L,D,R,E,G$ and then through the structural rows.
\item $P$ is the $\Pkg$ provenance row over $\BerryEsseenFiniteWindowUp$, $\CentralLimitFiniteWindowUp$, $\LindebergConditionUp$, $\DistributionUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BerryEsseenFiniteWindowUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these ten rows.
\end{definition}
\leandef{BEDC.Derived.BerryEsseenFiniteWindowUp.BerryEsseenFiniteWindowUp}
\leanchecked{BEDC.Derived.BerryEsseenFiniteWindowUp.BerryEsseenFiniteWindowTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Berry-Esseen finite-window NameCert obligations]
\label{thm:berry-esseen-finite-window-namecert-obligations}
For every accepted $\BerryEsseenFiniteWindowUp$ carrier $B=(C,L,D,R,E,G,H,K,P,N)$, the local $\NameCert_{\BerryEsseenFiniteWindowUp}$ surface consists exactly of carrier admission for the central-limit window $C$, Lindeberg row $L$, distribution readback $D$, RegSeqRat comparison row $R$, Real error-bound seal $E$, Gaussian-window row $G$, componentwise transport $H$, displayed replay $K$, provenance $P$, and local naming $N$. The certificate exports no asymptotic central limit theorem, host distribution metric, unrestricted replacement principle, countable-choice schedule, quotient distribution equality, or ambient $\RealUp$ completeness witness.
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:berry-esseen-finite-window-carrier}. The rows $C$ and $L$ are the only central-limit and Lindeberg inputs visible to the packet.

Read the comparison rows next. The distribution row $D$ supplies the finite law readback, the RegSeqRat row $R$ supplies rational comparison windows and tolerance data, and the Real row $E$ seals the displayed error-bound request.

The Gaussian-window row $G$ is consumed only after those rows are visible. The structural rows $H,K,P,N$ transport, replay, source, and name the same finite packet without adding an asymptotic theorem or host equality principle.
\end{proof}

\begin{theorem}[Berry-Esseen error-bound handoff]
\label{thm:berry-esseen-error-bound-handoff}
Every error-bound read exported by an accepted $\BerryEsseenFiniteWindowUp$ carrier factors through the finite route
$$
\CentralLimitFiniteWindowUp,\LindebergConditionUp \longmapsto \DistributionUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
before the Gaussian-window, transport, replay, provenance, and local naming rows are consumed. Thus the handoff supplies a finite normal-approximation error row and not an asymptotic CLT, a host probability-limit operator, quotient distribution equality, countable choice, or ambient real completeness.
\end{theorem}
\leanchecked{BEDC.Derived.BerryEsseenFiniteWindowUp.BerryEsseenFiniteWindowErrorBoundHandoff}
\begin{proof}
Start with a downstream request for a Berry-Esseen error bound. The request must first expose the central-limit finite window $C$ and the Lindeberg condition row $L$, because these are the only rows that name the normalized finite sum and the displayed replacement data.

The request then moves through $D,R,E$. The distribution row names the finite law readback, the RegSeqRat row names rational tolerances and finite samples, and the Real seal row records the terminal inequality request.

Finally $G,H,K,P,N$ present the Gaussian comparison window and preserve the same finite route. Since no other row appears in the carrier, the handoff cannot supply an asymptotic theorem, a host probability-limit operator, quotient distribution equality, countable choice, or ambient real completeness.
\end{proof}

\begin{theorem}[Berry-Esseen Gaussian-window nonescape]
\label{thm:berry-esseen-gaussian-window-nonescape}
Every Gaussian-window comparison read from an accepted $\BerryEsseenFiniteWindowUp$ carrier must retain the central-limit row $C$, Lindeberg row $L$, distribution row $D$, RegSeqRat row $R$, Real error-bound seal $E$, and Gaussian-window row $G$ before a consumer may use the comparison. The packet refuses hidden normal laws, selected limiting distributions, unrestricted characteristic-function convergence, quotient equality of laws, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Use the NameCert obligations of \autoref{thm:berry-esseen-finite-window-namecert-obligations}. They force any accepted comparison read to stay inside the displayed rows of the carrier.

Apply the error-bound handoff of \autoref{thm:berry-esseen-error-bound-handoff}. It places the finite central-limit and Lindeberg rows before the distribution, RegSeqRat, and Real error-bound rows.

The Gaussian-window row $G$ is therefore read as part of that same finite route. The structural rows $H,K,P,N$ preserve the route but do not add hidden normal laws, selected limiting distributions, unrestricted characteristic-function convergence, quotient equality of laws, or ambient completeness.
\end{proof}

\closureat{BerryEsseenFiniteWindowUp}{seedStr}

\begin{closurestatus}{\BerryEsseenFiniteWindowUp}
  \constructivestory{A $\BerryEsseenFiniteWindowUp$ packet is generated from a central-limit finite-window row, Lindeberg row, distribution readback, RegSeqRat comparison row, Real error-bound seal, Gaussian-window row, componentwise hsame transport, continuation replay, package provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:berry-esseen-finite-window-carrier}, \autoref{thm:berry-esseen-finite-window-namecert-obligations}, \autoref{thm:berry-esseen-error-bound-handoff}, and \autoref{thm:berry-esseen-gaussian-window-nonescape} seed the finite Berry-Esseen normal-approximation error-bound carrier through central-limit, Lindeberg, Distribution, RegSeqRat, Real, Gaussian-window, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close an asymptotic central limit theorem, a host distribution metric, unrestricted characteristic-function convergence, selected limiting distributions, quotient law equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite error-bound routing, distribution-to-RegSeqRat handoff, Real error-bound sealing, Gaussian-window nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BerryEsseenFiniteWindowUp.}
\end{closurestatus}
