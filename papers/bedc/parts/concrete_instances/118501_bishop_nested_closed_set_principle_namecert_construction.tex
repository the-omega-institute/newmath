\chapter{A Concrete Naming Certificate for $\BishopNestedClosedSetPrincipleUp$}
\label{ch:concrete-instances-bishop-nested-closed-set-principle-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopNestedClosedSetPrincipleUp$ packet records located nested closed-set completeness over $\RealUp$ as finite BEDC NameCert data. It sits after the located-real comparison surface of \autoref{ch:concrete-instances-locatedreal-namecert}, the nested-interval route of \autoref{ch:concrete-instances-nestedinterval-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the interval nesting route of \autoref{ch:concrete-instances-bishop-interval-cantor-namecert}, and the metric-completion handoff of \autoref{ch:concrete-instances-metriccompletion-namecert}. The packet names a shrinking inhabited located closed family and its completion-facing handoff; it is not open-cover compactness, sequential compactness, a selected point principle, quotient real equality, countable choice, or a host closed-set intersection theorem.

\begin{definition}[Bishop nested closed-set carrier]
\label{def:bishop-nested-closed-set-carrier}
A $\BishopNestedClosedSetPrincipleUp$ carrier is a finite $\BHist$ packet
$$
K=(F,I,D,Q,R,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the located closed-set row, listing the inspected $\RealUp$ predicates together with located distance or apartness witnesses read through $\LocatedRealUp$.
\item $I$ is the inhabited witness row, giving for each inspected depth a displayed finite witness row rather than a chosen global point.
\item $D$ is the diameter-modulus row, assigning every requested dyadic radius a displayed depth after which the inspected closed-set row has that diameter bound.
\item $Q$ is the $\DyadicRatCoreUp$ tolerance row used by the diameter and locatedness reads.
\item $R$ is the $\RegSeqRatUp$ readback row extracted from the nested witness windows and the dyadic diameter schedule.
\item $A$ is the terminal $\RealUp$ seal available only after $F,I,D,Q,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for located closed-set rows, inhabited witnesses, diameter moduli, dyadic tolerances, regular rational readback, Real sealing, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from nested located closed-set inspection through witness windows, diameter-modulus comparison, regular rational readback, and Real sealing.
\item $P$ is the $\Pkg$ provenance over $\BishopNestedClosedSetPrincipleUp$, $\RealUp$, $\LocatedRealUp$, $\NestedIntervalUp$, $\BishopIntervalCantorUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\MetricCompletionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopNestedClosedSetPrincipleUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $F,I,D,Q,R,A,H,C,P,N$. It has no coordinate for open-cover compactness, sequential compactness, an unbounded choice schedule, quotient equality of real names, a host closed-set intersection, or ambient $\RealUp$ completeness.
\end{definition}
\leanstmt{BEDC.Derived.BishopNestedClosedSetPrincipleUp}

\begin{theorem}[Bishop nested closed-set NameCert obligations]
\label{thm:bishop-nested-closed-set-namecert-obligations}
For every accepted $\BishopNestedClosedSetPrincipleUp$ carrier
$$
K=(F,I,D,Q,R,A,H,C,P,N),
$$
the local $\NameCert_{\BishopNestedClosedSetPrincipleUp}$ surface consists exactly of located closed-set admission, inhabited finite witness exposure, diameter-modulus admission, dyadic tolerance readback, regular rational readback, terminal Real seal admission, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No obligation is stored outside these displayed rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-nested-closed-set-carrier}. The located closed-set row $F$, witness row $I$, diameter-modulus row $D$, dyadic row $Q$, readback row $R$, and seal row $A$ are the only mathematical coordinates.

The locatedness and inhabitedness obligations are finite because the packet records inspected rows rather than a host family of all closed subsets. The witness row gives depthwise evidence, and the diameter row supplies a displayed modulus for the requested precision.

The structural rows $H,C,P,N$ preserve the same packet under transport, replay, provenance, and local naming. Since the carrier contains no open-cover compactness, sequential compactness, global choice schedule, quotient equality, host intersection, or ambient completeness coordinate, none can become a local NameCert obligation.
\end{proof}

\begin{theorem}[Bishop nested closed-set diameter handoff]
\label{thm:bishop-nested-closed-set-diameter-handoff}
Every completion-facing consumer of an accepted $\BishopNestedClosedSetPrincipleUp$ carrier must expose
$$
\LocatedRealUp,\quad \NestedIntervalUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp
$$
through the displayed rows $F,I,D,Q,R,A,H,C,P,N$ before it reads diameter-modulus data as a completed Real-facing witness. The handoff may cite \autoref{ch:concrete-instances-bishop-interval-cantor-namecert} and \autoref{ch:concrete-instances-metriccompletion-namecert}; it does not claim open-cover compactness, sequential compactness, countable choice, quotient real equality, or host closed-set completeness.
\end{theorem}
\begin{proof}
Start with the located closed-set and witness rows. The consumer reads $F$ and $I$ as finite displayed rows, so the nested family is available only through inspected located data and depthwise inhabitants.

Next read the diameter modulus. The row $D$ is measured by the dyadic tolerance row $Q$, and therefore any precision request reaches the nested family through $\DyadicRatCoreUp$ rather than through a host compactness argument.

The regular rational readback row $R$ is the only route into the terminal $\RealUp$ seal $A$. The same route is preserved by $H,C,P,N$, so completion consumers receive a finite diameter handoff compatible with the interval nesting and metric-completion siblings.

No additional coordinate remains from which a selected point, open-cover compactness theorem, sequential compactness theorem, countable-choice schedule, quotient equality, or ambient completeness claim could be read.
\end{proof}

\closureat{BishopNestedClosedSetPrincipleUp}{seedStr}

\begin{closurestatus}{\BishopNestedClosedSetPrincipleUp}
  \constructivestory{A $\BishopNestedClosedSetPrincipleUp$ packet is generated from nested located closed Real rows, inhabited witness rows, diameter-modulus rows, DyadicRatCore tolerance rows, RegSeqRat readback, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-nested-closed-set-carrier}, \autoref{thm:bishop-nested-closed-set-namecert-obligations}, and \autoref{thm:bishop-nested-closed-set-diameter-handoff} seed the finite located nested closed-set route over Real, LocatedReal, NestedInterval, DyadicRatCore, RegSeqRat, MetricCompletion, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, sequential compactness, selected intersection points, countable choice, quotient real equality, ambient $\RealUp$ completeness, Lean theorem verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located closed-set exposure, inhabited witness exposure, diameter-modulus exactness, DyadicRatCore tolerance readback, RegSeqRat extraction, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BishopNestedClosedSetPrincipleUp.}
\end{closurestatus}
