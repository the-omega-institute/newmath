\chapter{A Concrete Naming Certificate for $\DirichletFunctionUp$}
\label{ch:concrete-instances-dirichlet-function-namecert}
\origin{human}

Following Dirichlet and Bishop--Bridges constructive analysis, the $\DirichletFunctionUp$ packet records the classical rational-versus-irrational boundary example as finite BEDC data. It sits after the rational carrier of \autoref{ch:concrete-instances-rat-namecert}, the rational-density surface of \autoref{ch:concrete-instances-rational-density-real-namecert}, the stream naming surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational-sequence readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the real seal of \autoref{ch:concrete-instances-real-namecert}, the continuous-map packet of \autoref{ch:concrete-instances-continuousmap-namecert}, and the boundary comparison supplied by \autoref{ch:concrete-instances-thomae-function-namecert}. Its role is not to assert a host function equality or a complete classical discontinuity theorem, but to expose the finite NameCert route by which a Real-facing consumer sees rational density, irrational-boundary refusal, stream windows, regular readback, and a terminal $\RealUp$ seal.

\falsifiablePrediction{An accepted $\DirichletFunctionUp$ packet that omits rational-density evidence, treats the irrational-boundary refusal as a continuity proof, or reads the terminal seal without a displayed StreamName window refutes this carrier.}

\independenceWitness{rat, rationaldensityreal, streamname, regseqrat, real, continuous-map, thomae-function: none is bijective because DirichletFunctionUp binds rational-density branching, irrational-boundary refusal, finite stream windows, regular rational readback, Real-seal delivery, transport, continuation replay, provenance, and local naming in one packet.}

\begin{definition}[Dirichlet-function carrier]
\label{def:dirichlet-function-carrier}
A $\DirichletFunctionUp$ carrier is a finite $\BHist$ packet
$$
D=(R,\Delta,I,W,G,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RatUp$ source row for rational probes and rational values;
\item $\Delta$ is the rational-density evidence row over the surrounding $\RealUp$ boundary;
\item $I$ is the irrational-boundary refusal row, recording that a non-rational boundary read is not converted into a rational witness;
\item $W$ is the finite $\StreamNameUp$ window through which the boundary read is opened;
\item $G$ is the $\RegSeqRatUp$ readback row attached to the same window;
\item $A$ is the terminal $\RealUp$ seal row reached only after $R,\Delta,I,W,G$ are displayed;
\item $H$ records componentwise $\hsame$ transport for the rational, density, refusal, window, readback, seal, provenance, and naming rows;
\item $C$ records displayed $\Cont$ replay of the packet as a syntactic continuation route, not as a continuity claim;
\item $P$ is the $\Pkg$ provenance over $\RatUp$, $\RationalDensityRealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\ContinuousMapUp$, $\ThomaeFunctionUp$, $\BHist$, $\hsame$, $\Cont$, and local certificate rows;
\item $N$ is the local $\NameCert_{\DirichletFunctionUp}$ row.
\end{enumerate}
The local classifier compares accepted carriers only through the visible rows $R,\Delta,I,W,G,A,H,C,P,N$. It has no coordinate for quotient real equality, host function extensionality, a continuity proof, a complete discontinuity-set classifier, or an omniscient rationality decision.
\end{definition}
\leanchecked{BEDC.Derived.DirichletFunctionUp.TasteGate.DirichletFunctionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Dirichlet-function NameCert obligations]
\label{thm:dirichlet-function-namecert-obligations}
For the carrier of \autoref{def:dirichlet-function-carrier}, the local $\NameCert_{\DirichletFunctionUp}$ surface consists of carrier admission for $D=(R,\Delta,I,W,G,A,H,C,P,N)$, rational-probe exposure through $R$, rational-density evidence through $\Delta$, irrational-boundary refusal through $I$, finite $\StreamNameUp$ window exposure through $W$, $\RegSeqRatUp$ readback through $G$, terminal $\RealUp$ seal delivery through $A$, componentwise $\hsame$ stability through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Carrier admission is projection from the displayed tuple in \autoref{def:dirichlet-function-carrier}. The rational row, density evidence, refusal row, stream window, regular readback, seal, transport, replay, provenance, and naming row are all visible coordinates of one finite packet.

The obligations are then read in order. The rational row supplies the rational branch, the density row records the nearby rational evidence, and the refusal row prevents an irrational-boundary read from being reclassified as a rational witness.

The window and readback rows open the finite StreamName and RegSeqRat route before the Real seal is delivered. The remaining rows transport, replay, source, and name the same packet, so there is no hidden coordinate for extensional equality, continuity, complete discontinuity classification, or omniscient rationality.
\end{proof}

\begin{theorem}[Dirichlet-function rational and irrational boundary]
\label{thm:dirichlet-function-rational-irrational-boundary}
Every accepted $\DirichletFunctionUp$ read keeps the rational-density branch and the irrational-boundary refusal simultaneously visible. The rational side may be consumed only through $R,\Delta,W,G,A,H,C,P,N$, while the non-rational boundary side remains a refusal row $I$ and cannot be upgraded into a host decision procedure, quotient real equality, or a continuity statement.
\end{theorem}
\begin{proof}
Use the obligation surface of \autoref{thm:dirichlet-function-namecert-obligations}. It exposes both the rational-density evidence row $\Delta$ and the irrational-boundary refusal row $I$ before any Real-facing seal can be read.

The StreamName and RegSeqRat rows keep the boundary read finite. They supply a window and regular rational readback attached to the displayed packet, not an ambient equality of real functions or a host-level decision of rationality.

Since $I$ is a refusal row and no additional classifier coordinate exists, a non-rational boundary read cannot be repackaged as continuity, quotient real equality, or a complete discontinuity-set theorem. The packet therefore preserves the rational and non-rational boundary sides as distinct displayed obligations.
\end{proof}

\begin{theorem}[Dirichlet-function Real-seal factorization]
\label{thm:dirichlet-function-real-seal-factorization}
For every accepted $\DirichletFunctionUp$ carrier
$$
D=(R,\Delta,I,W,G,A,H,C,P,N),
$$
each completion-facing read of the terminal $\RealUp$ seal row $A$ factors through the displayed rational row $R$, rational-density row $\Delta$, irrational-boundary refusal row $I$, finite $\StreamNameUp$ window $W$, and $\RegSeqRatUp$ readback $G$. The factorization uses $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows only for the same finite packet, and it exports no continuity theorem, classical discontinuity-set completeness, host function extensionality, quotient real equality, or countable-choice selector.
\end{theorem}
\begin{proof}
Project the carrier. The seal row $A$ appears only after the rational, density, refusal, window, and readback rows in the displayed tuple.

Apply \autoref{thm:dirichlet-function-namecert-obligations}. Any accepted seal read must preserve the same rational evidence, refusal row, StreamName window, RegSeqRat readback, transport, replay, provenance, and local naming.

Now use the boundary theorem \autoref{thm:dirichlet-function-rational-irrational-boundary}. It prevents the refusal row from becoming a host rationality decision and keeps the rational-density branch separate from continuity or discontinuity-set claims. Thus the Real seal factors through exactly the displayed BEDC rows and no stronger analytic principle.
\end{proof}

\closureat{DirichletFunctionUp}{seedStr}

\begin{closurestatus}{\DirichletFunctionUp}
  \constructivestory{A $\DirichletFunctionUp$ packet is generated from rational probes, rational-density evidence, an irrational-boundary refusal row, finite StreamName windows, RegSeqRat readback, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, package provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{thm:dirichlet-function-namecert-obligations}, \autoref{thm:dirichlet-function-rational-irrational-boundary}, and \autoref{thm:dirichlet-function-real-seal-factorization} expose the seed carrier surface for rational-density branching and irrational-boundary refusal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not claim continuity, classical discontinuity-set completeness, host function extensionality, quotient real equality, countable choice, or Lean verification.}
  \upgradepath{Scoped closure requires checked local obligations tying the carrier rows to the concrete RatUp, StreamNameUp, RegSeqRatUp, RealUp, ContinuousMapUp, and ThomaeFunctionUp sibling surfaces.}
\end{closurestatus}
