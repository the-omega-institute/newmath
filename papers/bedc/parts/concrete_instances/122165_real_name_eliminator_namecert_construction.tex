\chapter{A Concrete Naming Certificate for $\RealNameEliminatorUp$}
\label{ch:concrete-instances-real-name-eliminator-namecert}
\origin{human}

Following Bishop--Bridges constructive real-number practice, the $\RealNameEliminatorUp$ packet records the finite route by which a consumer may use a $\RealUp$ seal only through certified regular rational representatives. It sits over the dyadic arithmetic surface of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular-sequence readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the completed real seal of \autoref{ch:concrete-instances-real-namecert}, and the modulus witness of \autoref{ch:concrete-instances-cauchymodulus-namecert}. The packet supplies a representative-consuming eliminator, not quotient equality, host real primitives, selected limits, or ambient real completeness.

\falsifiablePrediction{A $\RealUp$ consumer that reads a completed real without displaying a certified $\RegSeqRatUp$ representative refutes the $\RealNameEliminatorUp$ carrier surface.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, cauchymodulus: $\RealNameEliminatorUp$ is not bijective to any one sibling because it binds dyadic tolerance rows, finite stream windows, regular-sequence representatives, the Real seal, and modulus transport into one consumer-facing elimination packet.}

\begin{definition}[Real-name eliminator carrier]
\label{def:real-name-eliminator-carrier}
A $\RealNameEliminatorUp$ carrier is a finite $\BHist$ packet
$$
E=(D,S,Q,R,M,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance and comparison ledger used by the representative;
\item $S$ is the $\StreamNameUp$ finite-window row listing the observations a consumer may inspect;
\item $Q$ is the certified $\RegSeqRatUp$ representative readback served on those windows;
\item $R$ is the terminal $\RealUp$ seal row, available only after $D,S,Q$ and the modulus data have been displayed;
\item $M$ is the $\CauchyModulusUp$ row transporting precision requests back to finite representative windows;
\item $H$ records componentwise $\hsame$ transport for the analytic and structural rows;
\item $C$ records displayed $\Cont$ replay from representative admission to Real consumption;
\item $P$ is the $\Pkg$ provenance over $\RealNameEliminatorUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\CauchyModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RealNameEliminatorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,S,Q,R,M,H,C,P,N$. It has no coordinate for host real equality, quotient stream equality, selected representatives outside $Q$, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Real-name eliminator NameCert obligations]
\label{thm:real-name-eliminator-namecert-obligations}
For every accepted $\RealNameEliminatorUp$ carrier
$$
E=(D,S,Q,R,M,H,C,P,N),
$$
the local $\NameCert_{\RealNameEliminatorUp}$ surface consists exactly of carrier exposure for $E$, dyadic tolerance and comparison exposure through $D$, finite window exposure through $S$, certified regular-rational representative readback through $Q$, Real seal exposure through $R$, modulus transport through $M$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-name-eliminator-carrier}. The displayed tuple lists precisely the dyadic ledger, stream window, regular representative, Real seal, modulus row, and structural rows.

Read the analytic rows in dependency order. The representative row $Q$ is attached to the finite windows $S$ and the dyadic ledger $D$, while the modulus row $M$ transports later precision requests back to those displayed windows before the Real seal row $R$ is consumed.

Finally inspect the structural rows. The rows $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier has no further coordinate, the listed rows exhaust the local naming surface.
\end{proof}

\begin{theorem}[Real-name eliminator RegSeqRat determinacy]
\label{thm:real-name-eliminator-regseqrat-determinacy}
Every accepted $\RealUp$ consumer of a $\RealNameEliminatorUp$ packet must factor through the certified $\RegSeqRatUp$ representative row $Q$ and the finite route
$$
D,S,Q,R,M,H,C,P,N.
$$
Thus two accepted consumers that agree on $D,S,Q,M,H,C,P,N$ have the same local Real-name elimination surface, and any consumer that claims host real equality, quotient stream equality, a selected representative outside $Q$, countable choice, or ambient $\RealUp$ completeness has left the packet.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:real-name-eliminator-namecert-obligations}. They place the regular representative row $Q$ between the finite stream windows $S$ and the terminal Real seal row $R$.

Use the modulus row $M$ to replay every later precision request through the same representative windows. Agreement on $D,S,Q,M$ therefore fixes the representative-consuming part of the eliminator before any structural transport is read.

The structural rows $H,C,P,N$ preserve that same packet under componentwise transport, continuation replay, provenance, and naming. The excluded objects are absent from the carrier, so a read of any such object cannot be an accepted $\RealNameEliminatorUp$ consumption.
\end{proof}

\begin{closurestatus}{\RealNameEliminatorUp}
  \constructivestory{A $\RealNameEliminatorUp$ packet is generated from DyadicRatCore tolerance rows, StreamName finite windows, RegSeqRat representative readback, a Real seal row, CauchyModulus precision transport, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-name-eliminator-carrier}, \autoref{thm:real-name-eliminator-namecert-obligations}, and \autoref{thm:real-name-eliminator-regseqrat-determinacy} declare the representative-consuming Real-name eliminator surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host real equality, quotient stream equality, selected representatives outside the displayed RegSeqRat row, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, representative determinacy, modulus transport, Real-seal nonescape, and structural transport for BEDC.Derived.RealNameEliminatorUp.}
\end{closurestatus}
