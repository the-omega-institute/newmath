\chapter{A Concrete Naming Certificate for $\BishopCompleteMetricUp$}
\label{ch:concrete-instances-bishop-complete-metric-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis and Kelley completion language, the $\BishopCompleteMetricUp$ packet records the complete metric object reached when displayed Cauchy-filter data, regular rational readback, separated metric identity, and terminal real sealing are assembled as one finite L10 NameCert route. It sits after the metric-completion carrier of \autoref{ch:concrete-instances-metriccompletion-namecert}, the complete-metric service of \autoref{ch:concrete-instances-completemetric-namecert}, the Cauchy-filter evidence of \autoref{ch:concrete-instances-cauchyfilter-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the Real classifier seal of \autoref{ch:concrete-instances-real-namecert}, and the separated metric classifier of \autoref{ch:concrete-instances-separatedmetric-namecert}. The packet exposes a named constructive complete metric surface; it does not assert host quotient completion equality, selected representatives, countable choice, trichotomy, or ambient real completeness outside the displayed rows.

\begin{definition}[Bishop complete metric carrier]
\label{def:bishop-complete-metric-carrier}
A $\BishopCompleteMetricUp$ carrier is a finite $\BHist$ packet
$$
B=(M,F,R,S,C,E,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricCompletionUp$ source row, including the displayed metric carrier, completion handoff, and finite tolerance readback surface.
\item $F$ is the $\CauchyFilterUp$ evidence row, recording the Cauchy-filter route from which completion-facing point names are admitted.
\item $R$ is the $\RegSeqRatUp$ readback row, exposing the regular rational windows used by Real-facing consumers of the same completion data.
\item $S$ is the $\SeparatedMetricUp$ evidence row, recording the zero-distance classifier boundary available for completed point equality.
\item $C$ is the $\CompleteMetricUp$ service row, recording the displayed limit, modulus, and convergence-distance ledger for accepted regular Cauchy point names.
\item $E$ is the terminal $\RealUp$ seal, reached only after $M,F,R,S,C$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the metric-completion, Cauchy-filter, regular-readback, separatedness, complete-metric, Real-seal, continuation, provenance, and local naming rows.
\item $K$ records displayed $\Cont$ replay from a complete-metric consumer through $M,F,R,S,C,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\BishopCompleteMetricUp$, $\MetricCompletionUp$, $\CauchyFilterUp$, $\RegSeqRatUp$, $\SeparatedMetricUp$, $\CompleteMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\BishopCompleteMetricUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,F,R,S,C,E,H,K,P,N$. It has no coordinate for quotient completion equality, selected representatives, host metric equality, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.BishopCompleteMetricUp.BishopCompleteMetricTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Bishop complete metric NameCert obligations]
\label{thm:bishop-complete-metric-namecert-obligations}
For every accepted $\BishopCompleteMetricUp$ carrier $B=(M,F,R,S,C,E,H,K,P,N)$, the local $\NameCert_{\BishopCompleteMetricUp}$ surface consists exactly of carrier admission for the metric-completion source, Cauchy-filter evidence, regular rational readback, separated metric classifier, complete-metric service, terminal Real seal, componentwise transport, displayed continuation replay, provenance, and local naming rows. Every complete-metric read is therefore routed through $M,F,R,S,C,E,H,K,P,N$, and no Bishop complete metric obligation is stored outside those rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-complete-metric-carrier}. The visible packet consists exactly of $M,F,R,S,C,E,H,K,P,N$, so carrier admission, transport, continuation replay, provenance, and local naming are exhausted by those displayed rows.

Read the analytic dependencies in dependency order. The metric-completion source $M$ admits the completion route, $F$ records the Cauchy-filter evidence feeding that route, $R$ supplies regular rational readback for Real-facing observations, $S$ supplies the separated zero-distance classifier, and $C$ supplies the complete-metric limit service.

The terminal seal $E$ is downstream of those rows, while $H,K,P,N$ preserve the same route by transport, replay, provenance, and naming. Since quotient completion equality, representatives, host equality, countable choice, trichotomy, and ambient real completeness have no carrier coordinate, they cannot appear as local obligations.
\end{proof}

\begin{theorem}[Bishop complete metric completion handoff]
\label{thm:bishop-complete-metric-completion-handoff}
Every completion-facing or $\RealUp$-facing consumer of an accepted $\BishopCompleteMetricUp$ carrier must pass through the ordered route
$$
M,F,R,S,C,E,H,K,P,N
$$
before reading the terminal complete metric seal. The handoff exposes metric-completion data, Cauchy-filter evidence, regular rational readback, separated metric identity, complete-metric limit service, Real sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming, but it exports no quotient completion equality, selected representative, countable-choice selector, host metric equality, trichotomy, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Start from a completion-facing consumer of the accepted carrier. The only completion source row is $M$, and the only Cauchy evidence row feeding it is $F$.

Move next through the readback and classifier boundary. The row $R$ exposes the regular rational windows used by Real-facing reads, while $S$ is the separated metric route by which zero-distance comparisons are admitted.

Finally read $C$ and $E$, then replay the structural rows $H,K,P,N$. The absence of quotient, representative, choice, host-equality, trichotomy, and ambient-completeness coordinates in \autoref{def:bishop-complete-metric-carrier} forces every accepted handoff to remain on the displayed finite route.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopCompleteMetricUp$ completion or Real read factors through $\MetricCompletionUp$, $\CauchyFilterUp$, $\RegSeqRatUp$, $\SeparatedMetricUp$, $\CompleteMetricUp$, terminal $\RealUp$ sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{metriccompletion, completemetric, cauchyfilter, regseqrat, real, separatedmetric: $\BishopCompleteMetricUp$ is not bijective to any listed sibling because it jointly carries the metric-completion source, Cauchy-filter evidence, regular rational readback, separated metric classifier, complete-metric service, terminal Real seal, transport, continuation, provenance, and local naming as one finite complete-metric packet.}

\closureat{BishopCompleteMetricUp}{seedStr}

\begin{closurestatus}{\BishopCompleteMetricUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-complete-metric-carrier}, \autoref{thm:bishop-complete-metric-namecert-obligations}, and \autoref{thm:bishop-complete-metric-completion-handoff} seed the complete-metric surface over $\MetricCompletionUp$, $\CauchyFilterUp$, $\RegSeqRatUp$, $\SeparatedMetricUp$, $\CompleteMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion equality, selected representatives, host metric equality, countable choice, trichotomy, bridge checking, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, Cauchy-filter exposure, regular rational readback, separated classifier exactness, complete-metric service compatibility, and completion-handoff nonescape over the same displayed packet.}
  \constructivestory{The construction starts from a metric-completion source, records Cauchy-filter evidence and regular rational readback, passes through separated metric identity and complete-metric limit service, and exposes the terminal Real seal only along the same finite NameCert route.}
\end{closurestatus}
