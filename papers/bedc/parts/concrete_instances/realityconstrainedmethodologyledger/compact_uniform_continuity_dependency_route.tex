The $\RealityConstrainedMethodologyLedgerUp$ carrier consumes the finite-cover methodology exemplified by \autoref{ch:concrete-instances-compactuniformcontinuity-namecert} as a sibling route before public methodology reads are exported. This dependency keeps the ledger's scope, audit, refusal, and failure rows tied to a concrete finite-cover method pattern rather than to a detached methodology slogan.
