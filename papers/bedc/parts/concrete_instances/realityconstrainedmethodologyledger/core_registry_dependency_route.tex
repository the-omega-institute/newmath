The $\RealityConstrainedMethodologyLedgerUp$ carrier reads the core registry index of \autoref{ch:concrete-instances-core-registry-index-namecert} before methodology authority, route unblock, and export-surface rows can be treated as ledger entries. This dependency keeps the methodology ledger inside the shared registry lattice: the authority row, root-route row, open-fit row, and public export row are not free-standing methodology claims, but indexed entries whose provenance is visible before any downstream consumer reads them.
