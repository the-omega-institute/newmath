\chapter{A Concrete Naming Certificate for $\RealityConstrainedMethodologyLedgerUp$}
\label{ch:concrete-instances-reality-constrained-methodology-ledger-namecert}
\origin{ai}

The $\RealityConstrainedMethodologyLedgerUp$ packet realizes the scientific-methodology discipline of \autoref{sec:scientific-methodology-consequences} as a finite BEDC NameCert carrier. It consumes the explanation rows of \autoref{ch:concrete-instances-reality-constrained-explanation-namecert}, the model-audit rows of \autoref{ch:concrete-instances-reality-constrained-model-audit-namecert}, the multi-observer rows of \autoref{ch:concrete-instances-reality-constrained-objectivity-namecert}, the tower-compression rows of \autoref{ch:concrete-instances-reality-constrained-tower-compression-namecert}, and the open-fit rows of \autoref{ch:concrete-instances-open-fit-packet-namecert}. Those siblings separately expose explanation scope, fit audit, observer symmetry, compression residue, and open refutation surfaces; this packet binds them into the finite ledger by which a scientific-methodology claim is admitted only with effective-region bounds, idealization residue, domain-leakage refusal, upgrade routes, provenance, and local naming rows visible.

\begin{definition}[Reality-constrained methodology-ledger carrier]
\label{def:reality-constrained-methodology-ledger-carrier}
A $\RealityConstrainedMethodologyLedgerUp$ carrier is a finite $\BHist$ packet
$$
L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the explanation row, recording the observation, model, phenomenon, signature-comparison, descent, residue, and failure-surface material inherited from $\RealityConstrainedExplanationUp$;
\item $A$ is the audit row, recording source traces, observed signatures, model-generated signatures, classifier comparisons, ledgers, failure surfaces, and strength rows inherited from $\RealityConstrainedModelAuditUp$;
\item $O$ is the objectivity row, recording multi-observer histories, anchor-change schedules, invariant packets, and no-global-observer refusals inherited from $\RealityConstrainedObjectivityUp$;
\item $T$ is the tower-compression row, recording source, indexed tower, observer tower, physical-fit tower, endpoint, composite ledger, replay, and provenance data inherited from $\RealityConstrainedTowerCompressionUp$;
\item $I$ is the open-fit row, recording finite observation traces, admissible observation bundles, current fit, open continuation, and finite refutation surface inherited from $\OpenFitPacketUp$;
\item $S$ is the scope row, naming the domain, observation budget, admissible consumers, and physical-signature interface for the methodology claim;
\item $D$ is the domain-leakage refusal row, naming attempted exports beyond $S$ and the finite reason each export is refused;
\item $U$ is the upgrade-route row, naming the additional observations, classifier refinements, bridge rows, or ledger entries needed to extend the claim without deleting the present carrier;
\item $F$ is the methodology failure-surface row, collecting finite tests, signature mismatches, observer-anchor failures, tower-residue conflicts, and open-fit defeats;
\item $H$ records componentwise $\hsame$ transport for the explanation, audit, objectivity, tower-compression, open-fit, scope, refusal, upgrade, and failure rows;
\item $C$ records the accepted consumer route through the displayed methodology ledger;
\item $Q$ records $\Pkg$ provenance over the sibling carriers, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\RealityConstrainedMethodologyLedgerUp}$ row.
\end{enumerate}
The carrier has no coordinate for unbounded explanation, final model truth, hidden observer privilege, endpoint/source identity, closed empirical fit, or methodological export outside the displayed packet.
\end{definition}

\begin{definition}[Reality-constrained methodology-ledger certificate]
\label{def:reality-constrained-methodology-ledger-certificate}
For accepted packets
$$
L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)
\quad\hbox{and}\quad
L'=(X',A',O',T',I',S',D',U',F',H',C',Q',N'),
$$
the certificate classifier $\mathsf{RealityConstrainedMethodologyLedgerClassifier}(L,L')$ holds exactly when the explanation, audit, objectivity, tower-compression, and open-fit rows transport componentwise, the scope rows name the same admissible domain after $\hsame$ transport, the domain-leakage refusal rows preserve the refused exports, the upgrade-route rows preserve the admitted extension paths, the methodology failure surfaces preserve the defeating finite tests, and the consumer, provenance, and local naming rows transport through the same displayed route.

Thus certificate agreement is not sameness of theories, models, or observers. It is preservation of the finite methodology ledger that records where the claim explains, where it fits, where it is objective, where it is compressed, where it remains open, where it refuses export, and how it may be extended.
\end{definition}

\begin{theorem}[Reality-constrained methodology-ledger NameCert obligations]
\label{thm:reality-constrained-methodology-ledger-namecert-obligations}
For the carrier of \autoref{def:reality-constrained-methodology-ledger-carrier}, the local $\NameCert_{\RealityConstrainedMethodologyLedgerUp}$ surface consists exactly of six obligation rows:
\begin{enumerate}
\item carrier admission: the accepted object is exactly $L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)$;
\item sibling accountability: explanation, audit, objectivity, tower-compression, and open-fit reads must pass through $X,A,O,T,I$;
\item scope exactness: every methodology claim must display the scope row $S$ before it is exported;
\item domain-leakage refusal: every attempted export beyond $S$ must be recorded in $D$ as refused or routed through $U$ as a declared extension path;
\item failure-surface exposure: finite defeats from explanation, audit, objectivity, tower compression, and open fit must appear in $F$ before the consumer route $C$ is accepted;
\item ledger non-escape: no consumer may infer scientific-methodology authority from rows outside $X,A,O,T,I,S,D,U,F,H,C,Q,N$.
\end{enumerate}
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:reality-constrained-methodology-ledger-carrier}. The packet lists the only rows available to the local certificate.

Sibling accountability follows from the distinguished roles of $X,A,O,T,I$. Explanation scope, model audit, objectivity, tower compression, and open fit are not reconstructed by host prose; they enter through the five displayed sibling rows.

Scope exactness and domain-leakage refusal are forced by $S$ and $D$. A methodology claim is admitted only after its domain and budget are named, and an attempted export beyond that boundary is either refused in $D$ or sent to the declared extension route $U$.

Failure exposure is forced by $F$. The consumer route may read the methodology ledger only after the finite tests, signature mismatches, anchor failures, tower-residue conflicts, and open-fit defeats are visible.

Ledger non-escape follows because the carrier contains no coordinate outside $X,A,O,T,I,S,D,U,F,H,C,Q,N$. A route depending on an absent row is not a route in the accepted local naming certificate.
\end{proof}

\begin{theorem}[Reality-constrained methodology-ledger domain-leakage refusal]
\label{thm:reality-constrained-methodology-ledger-domain-leakage-refusal}
Let $L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)$ be an accepted $\RealityConstrainedMethodologyLedgerUp$ packet. A claim that uses the explanation, audit, objectivity, tower-compression, or open-fit rows while exporting beyond the displayed scope row $S$ is rejected unless the same packet records the attempted export in $D$ and supplies an upgrade route in $U$.
\end{theorem}
\begin{proof}
Start with the scope-exactness and domain-leakage obligations of \autoref{thm:reality-constrained-methodology-ledger-namecert-obligations}. They require the scope row before export and require every export beyond that scope to appear in the refusal or upgrade surface.

If a claim exports beyond $S$ without a row in $D$, the packet lacks the displayed refusal record. If it also lacks a route in $U$, the packet gives no accepted extension path.

The carrier has no remaining coordinate through which the export could be licensed. The claim may remain a local explanation, audit read, objectivity read, tower-compression read, or open-fit read, but it is not a $\RealityConstrainedMethodologyLedgerUp$ methodology export.
\end{proof}

\begin{theorem}[Reality-constrained methodology-ledger scope-export exactness]
\label{thm:reality-constrained-methodology-ledger-scope-export-exactness}
Let $L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)$ be an accepted $\RealityConstrainedMethodologyLedgerUp$ packet. Every methodology export must display the scope row $S$ and either remain inside that displayed scope or route the attempted export through the refusal and upgrade rows $D,U$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:reality-constrained-methodology-ledger-carrier}. The packet contains a distinguished scope row $S$, a domain-leakage refusal row $D$, and an upgrade-route row $U$; it contains no other export-authorizing coordinate.

Apply the NameCert obligations of \autoref{thm:reality-constrained-methodology-ledger-namecert-obligations}. Scope exactness requires $S$ before export, while domain-leakage refusal sends every attempted export beyond $S$ through $D$ or through the declared extension path $U$.

Finally use \autoref{thm:reality-constrained-methodology-ledger-domain-leakage-refusal}. A methodology claim that crosses the displayed scope without those rows is rejected as an export outside the accepted finite ledger. Hence the only accepted export routes are the in-scope route through $S$ and the displayed beyond-scope route through $D,U$.
\end{proof}

\begin{theorem}[Reality-constrained methodology-ledger sibling independence]
\label{thm:reality-constrained-methodology-ledger-sibling-independence}
The $\RealityConstrainedMethodologyLedgerUp$ carrier is not bijective to any one of $\RealityConstrainedExplanationUp$, $\RealityConstrainedModelAuditUp$, $\RealityConstrainedObjectivityUp$, $\RealityConstrainedTowerCompressionUp$, or $\OpenFitPacketUp$. Each sibling lacks at least one of the joined effective-region, idealization-residue, domain-leakage, upgrade-route, and methodology-ledger rows required by \autoref{def:reality-constrained-methodology-ledger-carrier}.
\end{theorem}
\begin{proof}
Compare the carrier fields. $\RealityConstrainedExplanationUp$ supplies explanation scope and residue but lacks the joined audit, objectivity, tower-compression, open-fit, domain-leakage, and upgrade-route rows.

$\RealityConstrainedModelAuditUp$ supplies audit and failure rows but lacks the explanation, no-global-observer, tower-compression endpoint, open-continuation, methodology scope, and upgrade-route rows. $\RealityConstrainedObjectivityUp$ supplies anchor-change and no-global-observer refusal but lacks the effective-region, idealization-residue, open-fit, and methodology-upgrade rows.

$\RealityConstrainedTowerCompressionUp$ supplies compression residue and endpoint/source separation but lacks the methodology domain-leakage refusal and open-fit refutation surface. $\OpenFitPacketUp$ supplies finite fit and open continuation but lacks the joined explanation, audit, objectivity, tower-compression, and methodology-ledger rows.

Since each sibling omits at least one required field of $L$, no bijection can preserve all carrier fields of \autoref{def:reality-constrained-methodology-ledger-carrier}.
\end{proof}

\begin{theorem}[Reality-constrained methodology-ledger failure-surface exhaustion]
\label{thm:reality-constrained-methodology-ledger-failure-surface-exhaustion}
Let $L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)$ be an accepted $\RealityConstrainedMethodologyLedgerUp$ packet. Every accepted methodology consumer routed by $C$ must expose in $F$ each finite defeating test available from the explanation, audit, objectivity, tower-compression, open-fit, scope, refusal, and upgrade rows before the route is accepted.
\end{theorem}
\begin{proof}
The carrier of \autoref{def:reality-constrained-methodology-ledger-carrier} displays $F$ as the row collecting finite tests, signature mismatches, observer-anchor failures, tower-residue conflicts, and open-fit defeats. The certificate of \autoref{def:reality-constrained-methodology-ledger-certificate} requires preservation of that row when two accepted packets agree as methodology ledgers.

Apply the failure-surface exposure clause of \autoref{thm:reality-constrained-methodology-ledger-namecert-obligations}. It states that finite defeats from the sibling rows must appear in $F$ before $C$ is accepted.

The refusal theorem \autoref{thm:reality-constrained-methodology-ledger-domain-leakage-refusal} closes the remaining route. A consumer that attempts to use an unexposed defeating test as if it were harmless either exports beyond the displayed scope or reads outside the admitted ledger, and neither route is licensed by $D$ and $U$.
\end{proof}

\begin{theorem}[Reality-constrained methodology-ledger consumer-route exactness]
\label{thm:reality-constrained-methodology-ledger-consumer-route-exactness}
For any accepted $\RealityConstrainedMethodologyLedgerUp$ packet $L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)$, the accepted consumer route factors exactly through the displayed rows $X,A,O,T,I,S,D,U,F,H,C,Q,N$. In particular, it cannot read hidden observer privilege, unbounded explanation, final model truth, closed empirical fit, or any other coordinate absent from the carrier.
\end{theorem}
\begin{proof}
By \autoref{def:reality-constrained-methodology-ledger-carrier}, the packet has exactly the displayed coordinates and explicitly omits unbounded explanation, final model truth, hidden observer privilege, endpoint/source identity, closed empirical fit, and export outside the packet. Thus a consumer route that uses one of those omitted coordinates is not a route through $L$.

The certificate in \autoref{def:reality-constrained-methodology-ledger-certificate} makes the same restriction invariant under accepted comparison: agreement preserves the displayed route through the sibling, scope, refusal, upgrade, failure, transport, provenance, and local naming rows. The NameCert obligation theorem \autoref{thm:reality-constrained-methodology-ledger-namecert-obligations} records this as ledger non-escape.

Finally apply \autoref{thm:reality-constrained-methodology-ledger-domain-leakage-refusal}. Any alleged consumer authority that depends on a hidden observer, an unbounded explanation, a final truth coordinate, or a closed fit is an export beyond the displayed scope and must be refused unless it is rerouted through the admitted upgrade surface; without such a displayed row it is not accepted by $C$.
\end{proof}

\begin{theorem}[Reality-constrained methodology-ledger upgrade-route determinacy]
\label{thm:reality-constrained-methodology-ledger-upgrade-route-determinacy}
Let $L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)$ be an accepted $\RealityConstrainedMethodologyLedgerUp$ packet. Every accepted export beyond the scope row $S$ is determined by the displayed refusal row $D$ and upgrade row $U$; it is not determined by deleting, replacing, or bypassing the present carrier.
\end{theorem}
\begin{proof}
The carrier definition \autoref{def:reality-constrained-methodology-ledger-carrier} assigns attempted exports beyond $S$ to $D$ and admissible extension paths to $U$. No separate coordinate records an export by erasing the carrier or by appealing to an outside methodological authority.

The classifier in \autoref{def:reality-constrained-methodology-ledger-certificate} preserves both rows. Therefore accepted comparison of methodology-ledger packets keeps the refused exports and admitted extension paths visible rather than allowing them to be reconstructed from hidden data.

Use the domain-leakage clause of \autoref{thm:reality-constrained-methodology-ledger-namecert-obligations} and the refusal theorem \autoref{thm:reality-constrained-methodology-ledger-domain-leakage-refusal}. Together they force every beyond-$S$ export to be either refused in $D$ or supplied with a route in $U$, so bypassing the present carrier cannot determine an accepted upgrade route.
\end{proof}

\begin{theorem}[Reality-constrained methodology-ledger root scope carrier admission]
\label{thm:reality-constrained-methodology-ledger-root-scope-carrier-admission}
Every accepted $\RealityConstrainedMethodologyLedgerUp$ root read is admitted only through the displayed carrier rows
$$
X,A,O,T,I,S,D,U,F,H,C,Q,N.
$$
No root consumer may replace those rows by an unbounded explanation, final model truth coordinate, hidden observer privilege, closed empirical fit, endpoint identity, or off-ledger methodology export.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:reality-constrained-methodology-ledger-carrier}. It lists exactly the explanation, audit, objectivity, tower-compression, open-fit, scope, refusal, upgrade, failure, transport, consumer, provenance, and local naming rows.

Apply the carrier-admission and ledger-non-escape clauses of \autoref{thm:reality-constrained-methodology-ledger-namecert-obligations}. The accepted root read must therefore expose those rows before it is a local $\NameCert_{\RealityConstrainedMethodologyLedgerUp}$ read.

The certificate of \autoref{def:reality-constrained-methodology-ledger-certificate} preserves the same displayed rows under comparison. Hence any route using an absent coordinate is not a root read of the accepted methodology-ledger carrier.
\end{proof}

\begin{theorem}[Reality-constrained methodology-ledger root refusal upgrade coverage]
\label{thm:reality-constrained-methodology-ledger-root-refusal-upgrade-coverage}
Let $L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)$ be an accepted $\RealityConstrainedMethodologyLedgerUp$ packet. Every beyond-scope methodology export from the root read is covered by the domain-leakage refusal row $D$ or by the upgrade-route row $U$.
\end{theorem}
\begin{proof}
Start with scope-export exactness, \autoref{thm:reality-constrained-methodology-ledger-scope-export-exactness}. It requires the scope row $S$ before any methodology export and sends every export beyond $S$ through the displayed refusal and upgrade rows.

The domain-leakage refusal theorem, \autoref{thm:reality-constrained-methodology-ledger-domain-leakage-refusal}, gives the local rejection clause. A claim that crosses the displayed scope without a refusal record in $D$ and an extension route in $U$ is not accepted as a methodology export.

Finally apply upgrade-route determinacy, \autoref{thm:reality-constrained-methodology-ledger-upgrade-route-determinacy}. It rules out deleting, replacing, or bypassing the carrier as a way to determine an accepted beyond-scope export, leaving only the $D$ and $U$ coverage rows.
\end{proof}

\begin{theorem}[Reality-constrained methodology-ledger root failure consumer exhaustion]
\label{thm:reality-constrained-methodology-ledger-root-failure-consumer-exhaustion}
For any accepted $\RealityConstrainedMethodologyLedgerUp$ packet $L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)$, every accepted consumer route must expose the failure row $F$ before using the consumer route $C$.
\end{theorem}
\begin{proof}
Use the failure-surface exposure clause of \autoref{thm:reality-constrained-methodology-ledger-namecert-obligations}. It places the finite defeats from the explanation, audit, objectivity, tower-compression, and open-fit rows in $F$ before the consumer route is accepted.

Apply failure-surface exhaustion, \autoref{thm:reality-constrained-methodology-ledger-failure-surface-exhaustion}. The same exhaustion requirement covers defeating tests available from the scope, refusal, and upgrade rows, so the root consumer cannot postpone $F$ until after $C$.

Finally use consumer-route exactness, \autoref{thm:reality-constrained-methodology-ledger-consumer-route-exactness}. Since $C$ factors through the displayed carrier rows and no hidden failure surface exists outside them, every accepted consumer route must expose $F$ before it is read through $C$.
\end{proof}

\begin{theorem}[Reality-constrained methodology-ledger source-refusal boundary]
\label{thm:reality-constrained-methodology-ledger-source-refusal-boundary}
Let $L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)$ be an accepted $\RealityConstrainedMethodologyLedgerUp$ packet. Any export that reads one of the sibling source rows $X,A,O,T,I$ beyond the displayed scope row $S$ must be witnessed in the refusal row $D$ before the consumer route $C$ may accept it.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:reality-constrained-methodology-ledger-carrier}. The sibling source rows, the scope row, the refusal row, and the consumer route are all displayed in the same finite packet, and there is no off-ledger coordinate from which a consumer can authorize a beyond-scope read.

Apply the scope exactness and domain-leakage clauses of \autoref{thm:reality-constrained-methodology-ledger-namecert-obligations}. They force every methodology export to display $S$ and force every attempted export beyond $S$ into the refusal or extension surface.

Now use \autoref{thm:reality-constrained-methodology-ledger-domain-leakage-refusal}. A beyond-scope export that lacks its witness in $D$ is rejected before it reaches $C$. Finally, \autoref{thm:reality-constrained-methodology-ledger-upgrade-route-determinacy} rules out deleting, replacing, or bypassing the carrier as a competing source for the same accepted export.
\end{proof}

\begin{theorem}[Reality-constrained methodology-ledger root audit source lock]
\label{thm:reality-constrained-methodology-ledger-root-audit-source-lock}
Let $L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)$ be an accepted
$\RealityConstrainedMethodologyLedgerUp$ packet. Every root audit read of the
methodology ledger must expose the sibling rows $X,A,O,T,I$ and the kernel
support rows $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ before the
consumer route $C$ can treat the read as methodology authority.
\end{theorem}
\begin{proof}
Begin with the carrier and certificate,
\autoref{def:reality-constrained-methodology-ledger-carrier} and
\autoref{def:reality-constrained-methodology-ledger-certificate}. They place
the explanation, audit, objectivity, tower-compression, and open-fit siblings
in $X,A,O,T,I$ and preserve them through the displayed comparison surface.

Apply the NameCert obligations of
\autoref{thm:reality-constrained-methodology-ledger-namecert-obligations}. The
sibling-accountability and carrier-admission rows require those five sibling
sources inside the same finite $\BHist$ packet, while the transport,
continuation, provenance, and local naming rows are read through
$H,C,Q,N$ as $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

Finally use scope-export exactness and source-refusal boundary,
\autoref{thm:reality-constrained-methodology-ledger-scope-export-exactness} and
\autoref{thm:reality-constrained-methodology-ledger-source-refusal-boundary}.
They prevent a root audit read from exporting sibling authority beyond $S$
unless the same packet records the refusal and route rows. Thus the audit source
is locked to $X,A,O,T,I$ over the displayed kernel support.
\end{proof}

\begin{theorem}[Reality-constrained methodology-ledger root open-fit refusal route]
\label{thm:reality-constrained-methodology-ledger-root-open-fit-refusal-route}
Let $L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)$ be an accepted
$\RealityConstrainedMethodologyLedgerUp$ packet. Any root read that uses the
open-fit sibling row $I$ as a public methodology refusal must route the read
through the displayed sibling rows $X,A,O,T,I$, the refusal and failure rows
$D,F$, and the $\BHist/\hsame/\Cont/\Pkg/\NameCert$ support of $H,C,Q,N$.
\end{theorem}
\begin{proof}
Project the carrier. The open-fit material is one sibling row $I$ of the same
packet $L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)$, not a separate public refusal oracle.

Use the NameCert obligations, scope-export exactness, and source-refusal
boundary. The obligations require sibling accountability and failure-surface
exposure, scope-export exactness requires $S$ before public methodology use, and
the source-refusal boundary sends any beyond-scope sibling read through the
displayed refusal row $D$ before $C$ accepts it.

Now apply root open-fit refusal package and failure-route locality,
\autoref{thm:reality-constrained-methodology-ledger-root-open-fit-refusal-package}
and
\autoref{thm:reality-constrained-methodology-ledger-failure-route-locality}.
They keep the open-fit defeat in $F$ and the refusal route in $D$ inside the
same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support, so the route
cannot become an off-ledger empirical-fit authority.
\end{proof}

\begin{theorem}[Reality-constrained methodology-ledger root upgrade readiness]
\label{thm:reality-constrained-methodology-ledger-root-upgrade-readiness}
For every accepted $\RealityConstrainedMethodologyLedgerUp$ packet
$L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)$, the root upgrade surface is ready exactly when
the sibling rows $X,A,O,T,I$, the scope/refusal/failure rows $S,D,F$, the
upgrade row $U$, and the $\BHist/\hsame/\Cont/\Pkg/\NameCert$ support
$H,C,Q,N$ are displayed together. No upgrade route may delete the present
carrier or replace it by an external methodology source.
\end{theorem}
\begin{proof}
First assemble the displayed packet from
\autoref{def:reality-constrained-methodology-ledger-carrier}. The upgrade row
$U$ is part of $L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)$, so it is read together with the
sibling, scope, refusal, failure, transport, consumer, provenance, and local
naming rows.

Apply upgrade-route determinacy and upgrade scope lock,
\autoref{thm:reality-constrained-methodology-ledger-upgrade-route-determinacy}
and
\autoref{thm:reality-constrained-methodology-ledger-upgrade-scope-lock}. These
results make every accepted extension a route through the displayed $D,U$
surface and prevent the upgrade row from replacing the carrier.

Finally combine
\autoref{thm:reality-constrained-methodology-ledger-root-audit-source-lock},
\autoref{thm:reality-constrained-methodology-ledger-root-open-fit-refusal-route},
and
\autoref{thm:reality-constrained-methodology-ledger-obligation-closure-package}.
The audit lock keeps $X,A,O,T,I$ visible, the open-fit refusal route keeps
$D,F$ visible for empirical defeats, and the closure package binds $H,C,Q,N$ to
$\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. Hence upgrade readiness is exactly
the displayed root surface and not an external methodology authority.
\end{proof}

\begin{theorem}[Reality-constrained methodology-ledger upgrade scope lock]
\label{thm:reality-constrained-methodology-ledger-upgrade-scope-lock}
Let $L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)$ be an accepted $\RealityConstrainedMethodologyLedgerUp$ packet. An accepted upgrade row $U$ preserves the same displayed packet
$$
X,A,O,T,I,S,D,U,F,H,C,Q,N
$$
by constraining $U$ to the declared extension route; it does not replace the carrier, erase $S$ or $D$, or create a second consumer packet for the methodology claim.
\end{theorem}
\begin{proof}
The carrier definition \autoref{def:reality-constrained-methodology-ledger-carrier} makes $U$ one row of the displayed packet rather than a substitute carrier. Thus an upgrade route is read together with the explanation, audit, objectivity, tower-compression, open-fit, scope, refusal, failure, transport, consumer, provenance, and local naming rows.

Apply \autoref{thm:reality-constrained-methodology-ledger-namecert-obligations}. The obligation surface demands carrier admission, sibling accountability, scope exactness, domain-leakage refusal, failure exposure, and ledger non-escape for the same packet whenever a methodology claim is accepted.

The refusal theorem \autoref{thm:reality-constrained-methodology-ledger-domain-leakage-refusal} and upgrade-route determinacy \autoref{thm:reality-constrained-methodology-ledger-upgrade-route-determinacy} then close the route. They allow $U$ to declare how the claim may be extended, but they do not allow $U$ to delete the existing scope, refusal, failure, or consumer rows.
\end{proof}

\begin{theorem}[Reality-constrained methodology-ledger failure-route locality]
\label{thm:reality-constrained-methodology-ledger-failure-route-locality}
Let $L=(X,A,O,T,I,S,D,U,F,H,C,Q,N)$ be an accepted $\RealityConstrainedMethodologyLedgerUp$ packet. Defeating tests recorded in the failure row $F$ remain local to the displayed sibling, scope, refusal, upgrade, transport, consumer, provenance, and local naming rows. They cannot be converted by $C$ into unbounded model truth, hidden observer privilege, final explanation authority, or a closed empirical oracle.
\end{theorem}
\begin{proof}
Read the carrier of \autoref{def:reality-constrained-methodology-ledger-carrier}. The failure row $F$ is one component of the finite methodology ledger, and the omitted coordinates include unbounded explanation, final model truth, hidden observer privilege, and closed empirical fit.

Apply \autoref{thm:reality-constrained-methodology-ledger-namecert-obligations}. The failure-surface exposure clause requires defeating tests to appear in $F$ before $C$ is accepted, while ledger non-escape prevents $C$ from reading authority outside the displayed carrier.

Use \autoref{thm:reality-constrained-methodology-ledger-domain-leakage-refusal} for any attempt to export those defeating tests beyond the displayed scope. Finally, \autoref{thm:reality-constrained-methodology-ledger-upgrade-route-determinacy} confines accepted beyond-scope movement to the displayed refusal and upgrade rows, so no defeating test in $F$ becomes an unbounded truth or hidden-observer privilege.
\end{proof}

\begin{theorem}[Reality-constrained methodology-ledger route normal form]
\label{thm:reality-constrained-methodology-ledger-route-normal-form}
Every accepted $\RealityConstrainedMethodologyLedgerUp$ consumer route factors
through exactly
$$
X,A,O,T,I,S,D,U,F,H,C,Q,N
$$
before any methodology export is read.
\end{theorem}
\begin{proof}
Project the carrier of
\autoref{def:reality-constrained-methodology-ledger-carrier}. It lists exactly
the explanation, audit, objectivity, tower-compression, open-fit, scope,
refusal, upgrade, failure, transport, consumer, provenance, and local naming
rows.

Apply the NameCert obligations of
\autoref{thm:reality-constrained-methodology-ledger-namecert-obligations}. The
carrier-admission, sibling-accountability, scope-exactness, failure-exposure,
and ledger-non-escape clauses require the same rows before the local
$\NameCert_{\RealityConstrainedMethodologyLedgerUp}$ route accepts a methodology
export.

Finally use consumer-route exactness,
\autoref{thm:reality-constrained-methodology-ledger-consumer-route-exactness}.
Since the accepted consumer route has no coordinate outside the displayed
carrier, every methodology export factors through the listed route and no
shorter host route is available.

The failure and upgrade coordinates are fixed by
\autoref{thm:reality-constrained-methodology-ledger-failure-surface-exhaustion}
and
\autoref{thm:reality-constrained-methodology-ledger-upgrade-route-determinacy}.
Thus the normal form does not hide defeating tests after $C$ and does not move
beyond $S$ except through the displayed $D,U$ rows.
\end{proof}
