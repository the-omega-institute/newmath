\begin{theorem}[Reality-constrained methodology audit-membrane dependency]
\label{thm:reality-constrained-methodology-ledger-audit-membrane-dependency}
Every accepted $\RealityConstrainedMethodologyLedgerUp$ packet that is exported as public methodology must consume the audit/export boundary of \autoref{ch:concrete-instances-auditgateboundary-namecert} and the membrane-local routing discipline of \autoref{ch:concrete-instances-operationalmembrane-namecert} before its local $\NameCert$ row is read. The exported route keeps methodology scope, audit evidence, membrane admission, failure exposure, transport, replay, provenance, and naming in one finite $\BHist$ packet, and it cannot be read as a kernel-only methodology island.
\end{theorem}
\begin{proof}
Use the sibling lattice route. It already places public methodology export inside the displayed explanation, audit, objectivity, tower-compression, open-fit, truth-certificate, and scientific-methodology dependency family.

The audit-gate boundary supplies the finite pass, refusal, ancestry, and replay rows that make a public methodology claim inspectable. The operational membrane supplies the export-side membrane row that keeps those audit rows attached to a concrete consumer boundary rather than to an ambient host predicate.

Combining these rows with the methodology-ledger carrier forces every public export through the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ surface. Thus the methodology ledger records a substantive horizontal dependency on the audit and membrane boundary chapters before its local name is consumed.
\end{proof}
