\input{parts/concrete_instances/cyclotomic/carrier_definition}
\input{parts/concrete_instances/cyclotomic/certificate}
\input{parts/concrete_instances/cyclotomic/root_downstream_unblock_package}
\input{parts/concrete_instances/cyclotomic/root_obligation_inventory}
\input{parts/concrete_instances/cyclotomic/root_threshold_obligations}
\input{parts/concrete_instances/cyclotomic/root_obligation_closure_package}
\input{parts/concrete_instances/cyclotomic/scoped_root_obligation_package}
\input{parts/concrete_instances/cyclotomic/iwasawa_classgroup_consumer_packet}

\closureat{CyclotomicUp}{bridgedStr}[BEDC.Derived.CyclotomicUp.CyclotomicRootCarrier\_root\_layer\_classifier\_transport]
\begin{closurestatus}{\CyclotomicUp}
  \constructivestory{A cyclotomic root layer is generated as the finite packet $\mathcal{C}_{\zeta}=(\mathcal{N},n,p,\mathcal{S},\zeta,\alpha,\lambda,\rho)$: a $\NumFieldUp$ source row, a positive unary exponent, the rational polynomial ledger for $X^n-1$, a $\SplittingFieldUp$ source over that ledger, a primitive-root endpoint, an acceptance and factorization $\Cont$ row, a base $\hsame$ comparison, and $\mathsf{Pkg}$ provenance. The carrier accepts exactly that displayed tuple and excludes quotient roots, host field equality, valuation rows, class-field reciprocity data, automorphism groups, completions, and regulator rows. Two accepted packets classify together when the NumField source, exponent, polynomial ledger, splitting-field source, and primitive endpoint are transported by componentwise $\hsame$ with support from the displayed acceptance and provenance rows. The source specification is the same root-layer tuple, the pattern records source coverage, polynomial-ledger exactness, primitive endpoint determinacy, factorization coverage, and downstream consumer exhaustion, and the ledger keeps the exponent, polynomial, root, factorization, base comparison, and provenance rows visible as the same packet. The five naming-certificate obligations are supplied by the accepted root tuple, reflexive, symmetric, and transitive component transports, and the root-layer classifier transport theorem that repacks transported rows as another accepted carrier. A history $h$ lies in the signature gap of a $\CyclotomicUp$ token $p$ when the source domain accepts the root-layer packet, the signature relation pairs it with an answer packet and evidence ledger, and token introduction names that answer as $p$; here that means the positive exponent, polynomial $X^n-1$ ledger, splitting-field root rows, primitive endpoint, and provenance of $h$ are the rows named by $p$. The chapter registers this scoped root-obligation construction under $\CyclotomicUp$; it gives downstream number-theoretic consumers a finite source package, but no public cyclotomic-field quotient, Galois bridge, class-field bridge, or standard splitting-field model.}
  \theoryclosure{\bridgedClosure}
  \scopeclosed{\autoref{thm:cyclotomic-root-scoped-closure-boundary} binds the scoped root package to the displayed $\mathsf{BHist}$, $\hsame$, $\Cont$, $\NameCert$, and $\Pkg$ primitives.}
  \formalstatus{\auditCleanV}
  \leantarget{BEDC.Derived.CyclotomicUp.CyclotomicRootCarrier\_root\_layer\_classifier\_transport}
  \bridgestatus{none}
  \notclaimed{No standard cyclotomic field, class-field bridge, Galois bridge, or public cyclotomic-field quotient is claimed here.}
  \upgradepath{Mature treatment requires downstream $\IwasawaUp$, $\ClassFieldUp$, and $\mathsf{ClassGroupUp}$ examples together with a later standard bridge verification target.}
\end{closurestatus}
