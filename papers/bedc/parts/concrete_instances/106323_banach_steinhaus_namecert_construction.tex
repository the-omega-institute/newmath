\chapter{A Concrete Naming Certificate for $\BanachSteinhausUp$}
\label{ch:concrete-instances-banach-steinhaus-namecert}
\origin{human}

Following Banach and Steinhaus, the $\BanachSteinhausUp$ packet records the traditional theorem-level surface behind the uniform boundedness principle as finite NameCert data. It is the neighbour of the Baire handoff in \autoref{ch:concrete-instances-baire-steinhaus-namecert} and the operator-family certificate in \autoref{ch:concrete-instances-uniform-boundedness-principle-namecert}: the packet names the theorem route from Baire-category completeness data to a uniform boundedness read without importing a host Banach-space theorem, countable choice, quotient equality of function spaces, or an unrecorded operator-family supremum.

\begin{definition}[Banach--Steinhaus carrier]
\label{def:banach-steinhaus-carrier}
A $\BanachSteinhausUp$ carrier is a finite $\BHist$ packet
$$
T=(B,M,N,F,U,L,W,H,C,P,K)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\BaireCategoryUp$ row that supplies the category witness used by the theorem-level route.
\item $M$ is the $\CompleteMetricUp$ row over the displayed source, keeping the Baire read tied to a complete metric carrier.
\item $N$ is the $\NormUp$ row through which pointwise and uniform boundedness are read as norm estimates.
\item $F$ is the $\FunctionalAnalysisUp$ row naming the linear-probe family and the finite observation windows visible to the packet.
\item $U$ is the $\UniformBoundednessPrincipleUp$ row that receives the theorem-level uniform-bound read after the Baire, completeness, norm, and functional-analysis rows have been displayed.
\item $L$ is the $\BoundedLinearOperatorUp$ family row over the same source and target surfaces.
\item $W$ is the pointwise-boundedness-to-uniform-bound ledger, recording the inspected source windows, operator rows, norm bounds, and the single uniform operator-bound output row.
\item $H$ records componentwise $\hsame$ transport for $B,M,N,F,U,L,W$.
\item $C$ records displayed $\Cont$ replay from pointwise boundedness requests through the Baire-category, complete-metric, norm, functional-analysis, bounded-operator, and uniform-boundedness rows.
\item $P$ is the $\Pkg$ provenance over $\BanachSteinhausUp$, $\BaireSteinhausUp$, $\UniformBoundednessPrincipleUp$, $\BaireCategoryUp$, $\CompleteMetricUp$, $\NormUp$, $\FunctionalAnalysisUp$, $\BoundedLinearOperatorUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $K$ is the local $\NameCert_{\BanachSteinhausUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows. It has no coordinate for host-level Banach-space completeness, countable choice, quotient equality of maps, an unrestricted operator-family selector, or a theorem object outside the finite route.
\end{definition}

\begin{theorem}[Banach--Steinhaus NameCert obligations]
\label{thm:banach-steinhaus-namecert-obligations}
For every accepted $\BanachSteinhausUp$ carrier $T=(B,M,N,F,U,L,W,H,C,P,K)$, the local $\NameCert_{\BanachSteinhausUp}$ surface consists exactly of carrier admission for the Baire-category row, complete-metric row, norm row, functional-analysis row, uniform-boundedness-principle row, bounded-linear-operator family row, pointwise-to-uniform ledger, componentwise transport, displayed replay, provenance, and local naming; Baire-source compatibility between $B$ and $M$; operator-family compatibility between $F$ and $L$; uniform-bound exactness through $W$ and $U$; and non-escape from host completeness, countable choice, quotient equality of maps, unrestricted operator-family selectors, and theorem objects outside the displayed finite packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:banach-steinhaus-carrier}. The tuple $B,M,N,F,U,L,W,H,C,P,K$ is the whole accepted packet, so carrier admission and the named local rows are direct reads from the finite history.

The Baire-source compatibility is the shared route through $B$ and $M$. The category row is interpreted only over the complete metric row displayed in the same packet, so a consumer cannot detach the Baire read from the carried source.

The operator-family compatibility is the shared route through $F$ and $L$. The functional-analysis row names the finite probe windows, and the bounded-linear-operator row records the operator-family data over those same windows.

The uniform-bound exactness is the ledger $W$ feeding the uniform-boundedness-principle row $U$. The transport, replay, provenance, and naming rows $H,C,P,K$ certify that the same displayed route is preserved.

Since no coordinate names the excluded host-level objects, the local NameCert surface exports only the finite Banach--Steinhaus route.
\end{proof}

\begin{theorem}[Banach--Steinhaus Baire handoff]
\label{thm:banach-steinhaus-baire-handoff}
Every theorem-level read of an accepted $\BanachSteinhausUp$ carrier factors through the ordered handoff
$$
\BaireCategoryUp,\ \CompleteMetricUp,\ \NormUp,\ \FunctionalAnalysisUp,\ \BoundedLinearOperatorUp \longmapsto \UniformBoundednessPrincipleUp .
$$
The $\UniformBoundednessPrincipleUp$ row receives the ledger $W$ only after the Baire and complete-metric rows have been exposed through the same $\BHist$ packet. Thus the packet is a theorem-level neighbour of \autoref{ch:concrete-instances-baire-steinhaus-namecert} and \autoref{ch:concrete-instances-uniform-boundedness-principle-namecert}, not a replacement for either sibling and not a host Banach--Steinhaus theorem outside BEDC data.
\end{theorem}
\begin{proof}
Read the pointwise-boundedness request through the continuation row $C$. The first part of the route exposes the Baire-category row $B$ over the complete-metric row $M$, matching the Baire side of the handoff named in \autoref{ch:concrete-instances-baire-steinhaus-namecert}.

Next read the norm row $N$, the functional-analysis row $F$, and the bounded-linear-operator family row $L$. These rows provide the finite operator windows and norm estimates that can be assembled into the pointwise-to-uniform ledger $W$.

Finally feed $W$ to the uniform-boundedness-principle row $U$, the same theorem-level consumer named in \autoref{ch:concrete-instances-uniform-boundedness-principle-namecert}. The rows $H,P,K$ transport, source, and name this route without adding an external theorem object.

The exported handoff is therefore exactly the displayed finite route, with no host completeness principle, countable-choice selector, quotient equality of function spaces, or unrestricted family supremum.
\end{proof}

\falsifiablePrediction{Every accepted $\BanachSteinhausUp$ packet must expose $\BaireCategoryUp$, $\CompleteMetricUp$, $\NormUp$, $\FunctionalAnalysisUp$, $\UniformBoundednessPrincipleUp$, $\BoundedLinearOperatorUp$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows before any theorem-level uniform-bound read is public.}

\independenceWitness{baire-steinhaus, uniform-boundedness-principle, boundedlinearoperator, norm, complete-metric: $\BanachSteinhausUp$ is not bijective to any listed sibling because it jointly binds the Baire-category source, complete-metric source, norm read, functional-analysis probe family, bounded-operator family, pointwise-to-uniform ledger, theorem-level uniform-boundedness consumer, transport, replay, provenance, and local naming rows.}

\closureat{BanachSteinhausUp}{seedStr}

\begin{closurestatus}{\BanachSteinhausUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:banach-steinhaus-carrier}, \autoref{thm:banach-steinhaus-namecert-obligations}, and \autoref{thm:banach-steinhaus-baire-handoff} seed the finite theorem-level Banach--Steinhaus route over BaireCategory, CompleteMetric, Norm, FunctionalAnalysis, UniformBoundednessPrinciple, BoundedLinearOperator, hsame, Cont, Pkg, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host Banach-space completeness, countable choice, quotient equality of maps, unrestricted operator-family selection, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Baire-source compatibility, operator-family compatibility, uniform-bound ledger exactness, Baire handoff stability, provenance exhaustion, and TasteGate field faithfulness for BEDC.Derived.BanachSteinhausUp.}
  \constructivestory{A $\BanachSteinhausUp$ packet is generated from BaireCategory, CompleteMetric, Norm, FunctionalAnalysis, UniformBoundednessPrinciple, BoundedLinearOperator, pointwise-to-uniform ledger, componentwise hsame, displayed Cont replay, Pkg provenance, and local NameCert rows.}
\end{closurestatus}
