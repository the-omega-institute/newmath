\chapter{A Concrete Naming Certificate for $\CompleteSeparatedMetricReflectorUp$}
\label{ch:concrete-instances-complete-separated-metric-reflector-namecert}
\origin{human}

Following Bishop--Bridges constructive completion and Kelley-style separated reflection, the $\CompleteSeparatedMetricReflectorUp$ packet records the standard reflector landing surface that sends a displayed metric-completion request to a complete separated metric object. It sits after the complete-metric service of \autoref{ch:concrete-instances-completemetric-namecert}, the separated-metric classifier of \autoref{ch:concrete-instances-separatedmetric-namecert}, the metric-completion comparison route of \autoref{ch:concrete-instances-metriccompletion-namecert}, the finite stream and regular rational readback stack of \autoref{ch:concrete-instances-streamname-namecert} and \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only the finite reflector certificate; it does not assert quotient completion equality, selected limits, countable choice, host point equality, or ambient $\RealUp$ completeness.

\begin{definition}[CompleteSeparatedMetricReflector carrier]
\label{def:complete-separated-metric-reflector-carrier}
A $\CompleteSeparatedMetricReflectorUp$ carrier is a finite $\BHist$ packet
$$
R=(C,S,M,W,Q,E,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $C$ is the $\CompleteMetricUp$ source row that exposes the limit-service surface available to completion consumers;
\item $S$ is the $\SeparatedMetricUp$ classifier row, read only after zero-distance evidence has been displayed;
\item $M$ is the $\MetricCompletionUp$ comparison row, carrying the metric-completion handoff and its reflector-facing comparison data;
\item $W$ is the $\StreamNameUp$ finite-window row for inspected Cauchy names and tolerance schedules;
\item $Q$ is the $\RegSeqRatUp$ readback row obtained from the displayed stream windows and rational tolerance data;
\item $E$ is the terminal $\RealUp$ seal row used for zero-distance and convergence-distance reads;
\item $H$ records componentwise $\hsame$ transport for the complete-metric, separated-classifier, metric-completion, stream-window, readback, Real-seal, provenance, and local naming rows;
\item $K$ records displayed $\Cont$ replay from the metric-completion request through complete-metric service, stream windows, regular rational readback, Real sealing, and separated reflection;
\item $P$ is the $\Pkg$ provenance over $\CompleteSeparatedMetricReflectorUp$, $\CompleteMetricUp$, $\SeparatedMetricUp$, $\MetricCompletionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\CompleteSeparatedMetricReflectorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows and their structural transport and replay data.
\end{definition}

\begin{theorem}[CompleteSeparatedMetricReflector NameCert obligations]
\label{thm:complete-separated-metric-reflector-namecert-obligations}
For every accepted $\CompleteSeparatedMetricReflectorUp$ carrier $R=(C,S,M,W,Q,E,H,K,P,N)$, the local $\NameCert_{\CompleteSeparatedMetricReflectorUp}$ surface consists exactly of carrier admission for the displayed tuple, complete-metric source exposure through $C$, separated classifier exposure through $S$, metric-completion comparison through $M$, StreamName finite-window exposure through $W$, RegSeqRat readback through $Q$, Real sealing through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $K$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier. Its coordinates display the complete-metric source, separated classifier, metric-completion comparison, stream windows, regular rational readback, Real seal, transport, replay, provenance, and local name.

Read the completion-facing rows first. The metric-completion row $M$ supplies the handoff under inspection, while $C$ supplies the complete-metric service row consumed by the reflector.

Read the finite name rows next. The stream-window row $W$ precedes the regular rational readback $Q$, and the terminal Real seal $E$ is downstream of those displayed windows and readbacks.

Finally apply the separated classifier row $S$ and the structural rows $H,K,P,N$. They preserve, replay, source, and name the same finite reflector route, so no local NameCert obligation remains outside the listed rows.
\end{proof}

\begin{theorem}[CompleteSeparatedMetricReflector adjunction route]
\label{thm:complete-separated-metric-reflector-adjunction-route}
Every reflector-facing consumer of an accepted $\CompleteSeparatedMetricReflectorUp$ packet reads the finite route
$$
\MetricCompletionUp \longmapsto \CompleteMetricUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp \longmapsto \SeparatedMetricUp .
$$
The route is the displayed packet $R=(C,S,M,W,Q,E,H,K,P,N)$ with $M$ opening the metric-completion comparison, $C$ supplying the complete-metric service, $W,Q,E$ supplying the finite real-name readback, and $S$ supplying the separated classifier.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:complete-separated-metric-reflector-namecert-obligations}. They force every accepted reflector read to expose $M,C,W,Q,E,S$ before the local name is consumed.

The reflector source is $M$ followed by $C$. This is the finite metric-completion request together with the complete-metric service row, not a quotient completion object.

The readback stack is $W,Q,E$. The stream windows and regular rational readback are displayed before the Real seal, so the zero-distance or convergence-distance observation is reached only through finite name data.

The separated classifier $S$ is terminal. Rows $H,K,P,N$ transport, replay, source, and name this same route, so the consumer receives a reflector certificate rather than selected limits, countable choice, host point equality, or ambient Real completeness.
\end{proof}

\begin{theorem}[CompleteSeparatedMetricReflector real-seal handoff]
\label{thm:complete-separated-metric-reflector-real-seal-handoff}
Any $\RealUp$-facing read from an accepted $\CompleteSeparatedMetricReflectorUp$ carrier must expose the $\StreamNameUp$ finite-window row $W$ and the $\RegSeqRatUp$ readback row $Q$ before the terminal seal $E$ is public, and must expose the $\CompleteMetricUp$ source row $C$, $\MetricCompletionUp$ comparison row $M$, and $\SeparatedMetricUp$ classifier row $S$ before the reflector handoff is public.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:complete-separated-metric-reflector-carrier}. The Real-facing row is $E$, and the only rows feeding it are the displayed stream-window and regular-readback rows $W,Q$.

Apply the adjunction route of \autoref{thm:complete-separated-metric-reflector-adjunction-route}. It places the metric-completion comparison and complete-metric service before the real-name readback and places the separated classifier after the Real seal.

The structural rows $H,K,P,N$ preserve that order under transport, replay, provenance, and local naming. Therefore a Real-facing consumer cannot bypass $W,Q$, and a reflector-facing consumer cannot bypass $M,C,S$.
\end{proof}

\falsifiablePrediction{Every accepted $\CompleteSeparatedMetricReflectorUp$ consumer read factors through $\MetricCompletionUp$, $\CompleteMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\SeparatedMetricUp$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming before it can use the reflector handoff.}

\independenceWitness{complete-metric, separated-metric, metric-completion, streamname, regseqrat, real: $\CompleteSeparatedMetricReflectorUp$ is not bijective to any listed sibling because it jointly carries the complete service, separated classifier, metric-completion comparison, finite windows, regular rational readback, Real seal, transport, replay, provenance, and local NameCert rows in one packet.}

\closureat{CompleteSeparatedMetricReflectorUp}{seedStr}

\begin{closurestatus}{\CompleteSeparatedMetricReflectorUp}
  \constructivestory{A $\CompleteSeparatedMetricReflectorUp$ packet is generated from a CompleteMetricUp source row, a SeparatedMetricUp classifier row, a MetricCompletionUp comparison row, StreamName windows, RegSeqRat readback, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:complete-separated-metric-reflector-carrier}, \autoref{thm:complete-separated-metric-reflector-namecert-obligations}, \autoref{thm:complete-separated-metric-reflector-adjunction-route}, and \autoref{thm:complete-separated-metric-reflector-real-seal-handoff} seed the finite complete separated metric reflector route over complete-metric, separated, metric-completion, stream, readback, Real-seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient completion equality, selected limits, countable choice, host point equality, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, complete-metric source exposure, separated classifier exposure, metric-completion comparison, finite-window readback, Real-seal handoff, transport stability, replay stability, provenance non-escape, and local naming for BEDC.Derived.CompleteSeparatedMetricReflectorUp.}
\end{closurestatus}
