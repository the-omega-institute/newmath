\chapter{A Concrete Naming Certificate for $\BioRealityQSixLeaveOneCodonBoundaryUp$}
\label{ch:concrete-instances-bioreality_q_six_leave_one_codon_boundary-namecert}
\origin{ai}

The $\BioRealityQSixLeaveOneCodonBoundaryUp$ packet names the finite boundary behavior of the claim h1.leave.one.codon.out. Its recorded panel has $0$ anchor rows, $5$ boundary rows, $8$ predictable rows, and a high-coverage closure reading of $8$. The recovery reading is exact on the displayed panel: the recovery rate is $1.0$, and the recovery rate above threshold is also $1.0$.

This chapter is self-contained. It does not appeal to a sibling chapter, an external codon list, a biological stochastic law, or a hidden enumeration oracle. The carrier only exposes the seven rows described below, and every NameCert obligation is read from those rows.

\begin{definition}[Leave-one codon boundary carrier]
\label{def:bioreality-q-six-leave-one-codon-boundary-carrier}
A $\BioRealityQSixLeaveOneCodonBoundaryUp$ carrier is a finite tuple
$$
\begin{gathered}
B=(A,D,P,H,R,T,N),
\end{gathered}
$$
where the rows have the following meanings.
\begin{itemize}
\item $A$ is the anchor row. For this packet it records anchor count $0$, so no anchor is permitted to serve as an unobserved support source.
\item $D$ is the boundary row. It records boundary count $5$ and is the only place from which a boundary witness may be read.
\item $P$ is the predictable-row ledger. It records predictable count $8$.
\item $H$ is the high-coverage closure row. It records closure coverage $8$ on the same finite panel as $P$.
\item $R$ is the recovery row. It records recovery rate $1.0$.
\item $T$ is the threshold recovery row. It records recovery rate above threshold $1.0$.
\item $N$ is the local naming row for the carrier name $\BioRealityQSixLeaveOneCodonBoundaryUp$.
\end{itemize}
The tuple has no coordinate for an omitted codon table, a p-value, a probability model, an empirical fitness surface, or a biological mechanism. If a consumer asks for such data, the request is outside the carrier.
\end{definition}

\begin{definition}[Local NameCert surface]
\label{def:bioreality-q-six-leave-one-codon-boundary-namecert-surface}
For an accepted carrier $B=(A,D,P,H,R,T,N)$, the local $\NameCert_{\BioRealityQSixLeaveOneCodonBoundaryUp}$ surface consists exactly of anchor exclusion through $A$, boundary admission through $D$, predictable-row admission through $P$, high-coverage closure readback through $H$, recovery readback through $R$, threshold recovery readback through $T$, and local naming through $N$.
\end{definition}

\begin{lemma}[No anchor leakage]
\label{lem:bioreality-q-six-leave-one-codon-boundary-no-anchor-leakage}
Every accepted $\BioRealityQSixLeaveOneCodonBoundaryUp$ carrier exposes no anchor-supported boundary route.
\end{lemma}

\begin{proof}
The only anchor row is $A$, and $A$ records anchor count $0$. The boundary row $D$ records the $5$ displayed boundary rows independently of any anchor. Since the carrier has no further coordinate from which an anchor can be projected, every accepted read is anchor-free.
\end{proof}

\begin{lemma}[Boundary inventory is finite]
\label{lem:bioreality-q-six-leave-one-codon-boundary-finite-inventory}
Every accepted $\BioRealityQSixLeaveOneCodonBoundaryUp$ carrier presents a finite boundary inventory with $5$ boundary rows and $8$ predictable rows.
\end{lemma}

\begin{proof}
The boundary count is read only from $D$, and $D$ records $5$. The predictable count is read only from $P$, and $P$ records $8$. No row in the carrier provides an unbounded enumerator or a hidden codon source. Therefore the inventory exposed by the carrier is finite and has the displayed counts.
\end{proof}

\begin{theorem}[Leave-one codon boundary recovery certificate]
\label{thm:bioreality-q-six-leave-one-codon-boundary-recovery-certificate}
For every accepted $\BioRealityQSixLeaveOneCodonBoundaryUp$ carrier, the boundary ledger closes the displayed high-coverage panel with coverage count $8$, recovery rate $1.0$, and recovery rate above threshold $1.0$.
\end{theorem}

\begin{proof}
The high-coverage closure row $H$ records closure coverage $8$. The recovery row $R$ records recovery rate $1.0$, and the threshold row $T$ records recovery rate above threshold $1.0$. These three rows are part of the displayed tuple $B=(A,D,P,H,R,T,N)$, and the local NameCert surface permits recovery reads only through them. Hence any accepted carrier returns exactly those three readings and no stronger biological or probabilistic conclusion.
\end{proof}

\falsifiablePrediction{Recomputing the same finite leave-one codon boundary panel must return anchor count $0$, boundary count $5$, predictable count $8$, closure coverage $8$, recovery rate $1.0$, and recovery rate above threshold $1.0$. A mismatch in any of these six displayed quantities falsifies this $\BioRealityQSixLeaveOneCodonBoundaryUp$ instance.}

\independenceWitness{The $\BioRealityQSixLeaveOneCodonBoundaryUp$ packet is not interchangeable with a generic codon, boundary, or recovery carrier because it jointly requires zero anchors, five boundary rows, eight predictable rows, high-coverage closure count eight, exact recovery, exact above-threshold recovery, and the local carrier name. Removing any one of these rows changes the available NameCert reads.}

\section{Provenance row}
\label{sec:bioreality-q-six-leave-one-codon-boundary-provenance}

The provenance row records that the numerical panel was verified at 2026-06-05T15:28:01+00:00. This timestamp is not an additional mathematical hypothesis; it only identifies the observation time for the displayed finite counts and rates. The mathematical content of the chapter is exactly the tuple inventory and recovery readings stated above.

\section{Closure status}
\label{sec:bioreality-q-six-leave-one-codon-boundary-closure-status}

\closureat{BioRealityQSixLeaveOneCodonBoundaryUp}{seedStr}
\begin{closurestatus}{\BioRealityQSixLeaveOneCodonBoundaryUp}
  \constructivestory{A $\BioRealityQSixLeaveOneCodonBoundaryUp$ packet is generated from a finite leave-one codon boundary tuple with anchor count $0$, boundary count $5$, predictable count $8$, closure coverage $8$, recovery rate $1.0$, recovery rate above threshold $1.0$, and a local naming row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{Closed for the named finite leave-one codon boundary inventory and its displayed recovery rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a biological mechanism, stochastic fitness law, empirical codon table, p-value, external codon list, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires a checked carrier implementation for the seven displayed rows and a bridge that validates the finite counts and rates against the recorded observation panel.}
\end{closurestatus}
