\chapter{BioReality Q-Six Leave-One-Codon Boundary NameCert Construction}
\label{ch:concrete-instances-bioreality_q_six_leave_one_codon_boundary-namecert}
\origin{ai}

The $\BioRealityQSixLeaveOneCodonBoundaryUp$ packet records a finite BioReality boundary claim for the leave-one-codon-out probe family at the Q-six scale.  The carrier is not a biological universality statement: it is a bounded NameCert for the claim identifier $\mathsf{h1.leave.one.codon.out}$, with all numerical content restricted to the recorded witness table.  In that table the anchor count is $0$, the boundary count is $5$, the predictable count is $8$, and the high-coverage closure count is $8$.  The recovery rate is exactly $1.0$, and the recorded value above the recovery threshold is also exactly $1.0$.

\falsifiablePrediction{The packet predicts that any rerun admitted as the same bounded Q-six leave-one-codon-out witness must preserve the finite boundary profile $\mathrm{anchor\_count}=0$, $\mathrm{boundary\_count}=5$, $\mathrm{predictable\_count}=8$, and $\mathrm{closure\_coverage\_high}=8$ under the same carrier interpretation.  A rerun with a positive anchor count, a boundary count other than $5$, or a recovery rate below $1.0$ is a falsifying boundary witness for this NameCert packet.}

\independenceWitness{The independence witness is the empty anchor side together with the nonempty boundary side: $\mathrm{anchor\_count}=0$ while $\mathrm{boundary\_count}=5$.  This separates the boundary claim from any argument that derives the packet merely from anchored codon coverage.  The recovery certificates are carried by the eight predictable entries and the eight high-coverage closure entries, not by anchor mass.}

\begin{definition}[Q-six leave-one-codon boundary carrier]
A $\BioRealityQSixLeaveOneCodonBoundaryUp$ carrier is a finite record consisting of a claim tag, an anchor count, a boundary count, a predictable count, a high-coverage closure count, a recovery rate, and a recovery-threshold flag.  The carrier is admitted for the present packet exactly when these fields satisfy
$$
\begin{aligned}
\mathrm{claim} &= \mathsf{h1.leave.one.codon.out},\\
\mathrm{anchor\_count} &= 0,\\
\mathrm{boundary\_count} &= 5,\\
\mathrm{predictable\_count} &= 8,\\
\mathrm{closure\_coverage\_high} &= 8,\\
\mathrm{recovery\_rate} &= 1.0,\\
\mathrm{recovery\_rate\_above\_threshold} &= 1.0.
\end{aligned}
$$
The definition is intentionally finite: it does not infer unlisted codon behavior, unrecorded anchors, or a probability model beyond the displayed fields.
\end{definition}

\begin{lemma}[Boundary profile separates from anchors]
For the admitted $\BioRealityQSixLeaveOneCodonBoundaryUp$ carrier, the boundary side is nonempty while the anchor side is empty.
\end{lemma}

\begin{proof}
The carrier equations give $\mathrm{anchor\_count}=0$ and $\mathrm{boundary\_count}=5$.  Since $5$ is nonzero, the boundary side is nonempty, while the anchor side is empty by the displayed anchor equation.
\end{proof}

\begin{theorem}[Finite recovery closure for the Q-six boundary packet]
The admitted $\BioRealityQSixLeaveOneCodonBoundaryUp$ carrier closes the finite recovery boundary for $\mathsf{h1.leave.one.codon.out}$ at the recorded threshold.
\end{theorem}

\begin{proof}
The closure claim has only the finite fields displayed in the carrier definition.  The predictable count is $8$, and the high-coverage closure count is also $8$, so every recorded predictable entry is accounted for by the high-coverage closure side of the packet.  The recovery rate is $1.0$, and the recovery-threshold flag is $1.0$, hence the recorded recovery condition is met at the threshold tracked by the carrier.  The theorem asserts no extension beyond these finite fields.
\end{proof}

\begin{closurestatus}{\BioRealityQSixLeaveOneCodonBoundaryUp}
  \constructivestory{The carrier is a finite NameCert record for $\mathsf{h1.leave.one.codon.out}$ whose boundary evidence is the displayed tuple of six recorded numerical fields.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{Closed only for the bounded Q-six leave-one-codon-out carrier with anchor count $0$, boundary count $5$, predictable count $8$, high-coverage closure count $8$, recovery rate $1.0$, and recovery-threshold flag $1.0$.}
  \formalstatus{\unformalized}
  \bridgestatus{none}
  \notclaimed{No claim is made about unlisted codons, external biological mechanisms, statistical significance, or any rerun whose finite fields differ from the displayed carrier equations.}
  \upgradepath{A higher closure level requires an explicit bridge from the finite carrier equations to an independently checked generator for the leave-one-codon-out witness table.}
\end{closurestatus}
