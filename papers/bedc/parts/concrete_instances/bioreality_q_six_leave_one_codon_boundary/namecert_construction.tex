\chapter{BioReality Q-Six Leave-One-Codon Boundary NameCert Construction}
\label{ch:concrete-instances-bioreality_q_six_leave_one_codon_boundary-namecert}
\origin{ai}

The BioReality Q-six leave-one-codon boundary packet certifies a finite boundary phenomenon for the claim family \texttt{h1.leave.one.codon.out}. The carrier named \texttt{BioRealityQSixLeaveOneCodonBoundary} records a closed observation in which the leave-one-codon probe has no anchor rows, has five boundary rows, and still preserves a full recovery reading on the measured recovery axis. This is not a generic biological plausibility assertion: the packet is pinned to the numerical shape
$$
\begin{gathered}
A=0,\qquad B=5,\qquad P=8,\\
C_{\mathrm{high}}=8,\qquad R=1.0,\qquad R_{\mathrm{threshold}}=1.0.
\end{gathered}
$$
Here $A$ is the anchor count, $B$ is the boundary count, $P$ is the predictable count, $C_{\mathrm{high}}$ is the high closure coverage count, $R$ is the observed recovery rate, and $R_{\mathrm{threshold}}$ is the threshold-side recovery certificate. The NameCert content therefore asserts a boundary certificate rather than an anchor certificate: the observed anchor side is empty, while the boundary side contains five certified rows.

\falsifiablePrediction{Repeating the same leave-one-codon boundary extraction on the certified carrier must again return $A=0$, $B=5$, $P=8$, $C_{\mathrm{high}}=8$, and recovery values $R=1.0$ and $R_{\mathrm{threshold}}=1.0$. Any nonzero anchor count, any boundary count other than five, or any recovery rate below $1.0$ falsifies this packet as stated.}

\independenceWitness{The packet separates the boundary claim from an anchor-backed explanation: the certified anchor count is $0$ while the certified boundary count is $5$. Thus the recovery reading is witnessed without importing an anchor row, and the high closure coverage count $8$ is recorded as a finite coverage fact rather than as evidence for an omitted anchor population.}

\begin{definition}[Leave-one-codon boundary carrier]
A \emph{BioReality Q-six leave-one-codon boundary carrier} is a finite certificate tuple
$$
\begin{aligned}
\mathcal{B}=(A,B,P,C_{\mathrm{high}},R,R_{\mathrm{threshold}})
\end{aligned}
$$
with the certified values
$$
\begin{aligned}
A&=0, & B&=5, & P&=8,\\
C_{\mathrm{high}}&=8, & R&=1.0, & R_{\mathrm{threshold}}&=1.0.
\end{aligned}
$$
The carrier is read as a finite NameCert boundary witness for the leave-one-codon claim: no anchor row is present, five boundary rows are present, eight predictable rows are present, and all eight high-coverage rows remain inside the certified closure account.
\end{definition}

\begin{theorem}[Boundary without anchor support]
For the BioReality Q-six leave-one-codon boundary carrier, the boundary side is nonempty while the anchor side is empty.
\end{theorem}

\begin{proof}
By the carrier definition, $A=0$ and $B=5$. Since $5$ is nonzero, the boundary side is nonempty. Since the anchor count is exactly $0$, no anchor row is present. These two equalities establish the asserted separation.
\end{proof}

\begin{theorem}[Full recovery is recorded on the certified boundary packet]
The carrier records full recovery on both the observed recovery coordinate and the threshold-side recovery coordinate.
\end{theorem}

\begin{proof}
The defining tuple contains $R=1.0$ and $R_{\mathrm{threshold}}=1.0$. Hence the recovery reading and the threshold-side recovery certificate are both full in the finite record.
\end{proof}

\begin{theorem}[Predictable rows match high closure coverage]
The predictable count and the high closure coverage count agree on the carrier.
\end{theorem}

\begin{proof}
The carrier definition gives $P=8$ and $C_{\mathrm{high}}=8$. Therefore $P=C_{\mathrm{high}}$, so the predictable rows match the high closure coverage rows in the certified finite account.
\end{proof}

The certified shape is intentionally asymmetric. The packet does not claim that anchors are missing because of biological impossibility, sampling design, or codon-level causal structure. It only records that this NameCert instance has anchor count $0$ and boundary count $5$ under the certified extraction. The equal recovery values $R=1.0$ and $R_{\mathrm{threshold}}=1.0$ are therefore part of the boundary certificate itself, not a license to infer an unobserved anchor mechanism.

\begin{closurestatus}{BioRealityQSixLeaveOneCodonBoundary}
  \constructivestory{The chapter constructs a finite boundary NameCert carrier from the certified tuple with anchor count $0$, boundary count $5$, predictable count $8$, high closure coverage $8$, recovery rate $1.0$, and threshold-side recovery rate $1.0$.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{Closed only for the finite leave-one-codon boundary certificate encoded by the displayed six-coordinate carrier.}
  \formalstatus{\unformalized}
  \bridgestatus{none}
  \notclaimed{No codon list, p-value, causal biological mechanism, external replication claim, or Lean-side theorem is asserted here.}
  \upgradepath{A stronger closure level would require a separately supplied codon-level table, an explicit bridge from the extraction protocol to the carrier fields, and a formal proof target for the finite certificate.}
\end{closurestatus}
