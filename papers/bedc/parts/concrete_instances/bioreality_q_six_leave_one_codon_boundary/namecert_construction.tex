\chapter{BioReality Q Six Leave-One-Codon Boundary NameCert}
\label{ch:concrete-instances-bioreality_q_six_leave_one_codon_boundary-namecert}
\origin{ai}

The $\BioRealityQSixLeaveOneCodonBoundaryUp$ packet records a finite biological boundary test in which a six-codon observation surface is probed by leaving one codon out at a time. The accepted evidence has $5$ boundary rows, $0$ anchor rows, $8$ predictable rows, and high-coverage closure on $8$ rows. The observed recovery rate is $1.0$, and the above-threshold recovery coordinate is also $1.0$.

The carrier is therefore not a generic witness for a claim name. It is a boundary packet whose public surface is exactly the leave-one-codon comparison ledger: boundary rows are present, anchor rows are absent, and the only quantitative recovery claim admitted by the packet is perfect recovery at the displayed threshold coordinate.

\begin{definition}[Leave-one-codon boundary packet]
A $\BioRealityQSixLeaveOneCodonBoundaryUp$ packet is a finite tuple
$$
\begin{gathered}
Q=(B,A,P,C,R,T,H,K,N),
\end{gathered}
$$
where $B$ is the displayed boundary ledger, $A$ is the anchor ledger, $P$ is the predictable-row ledger, $C$ is the high-coverage closure ledger, $R$ is the recovery-rate row, $T$ is the above-threshold recovery row, $H$ is the componentwise history-transport row, $K$ is the displayed continuation route, and $N$ is the local naming row. The packet is accepted only when the exposed counts satisfy
$$
\begin{aligned}
|A|&=0,\\
|B|&=5,\\
|P|&=8,\\
|C|&=8,\\
R&=1.0,\\
T&=1.0.
\end{aligned}
$$
\end{definition}

The absence of anchors is part of the boundary surface, not missing data. A consumer cannot turn the packet into an anchored codon certificate, because the anchor coordinate is explicitly recorded as $0$. Likewise, the boundary count is fixed at $5$ rows, so the carrier does not license a six-boundary reading or a collapsed single-boundary reading.

\begin{definition}[Classifier]
Two accepted $\BioRealityQSixLeaveOneCodonBoundaryUp$ packets classify together when their boundary ledgers, anchor ledgers, predictable-row ledgers, high-coverage ledgers, recovery rows, threshold rows, continuation routes, and local naming rows are related component by component by the displayed history-sameness relation. No host-level biological equivalence relation is added outside these finite rows.
\end{definition}

\begin{theorem}[NameCert surface for the leave-one-codon boundary]
The accepted $\BioRealityQSixLeaveOneCodonBoundaryUp$ packets form a finite NameCert surface for the claim $h1.leave.one.codon.out$ at the displayed quantitative boundary.
\end{theorem}

\begin{proof}
The source row is the finite tuple $Q$. Nonemptiness is supplied by the displayed packet whose public coordinates have $0$ anchor rows, $5$ boundary rows, $8$ predictable rows, $8$ high-coverage closure rows, recovery rate $1.0$, and above-threshold recovery $1.0$. The pattern row is the leave-one-codon boundary comparison: it preserves the boundary ledger and refuses any anchor-dependent reading because $|A|=0$ is part of the source. The classifier is componentwise, so reflexivity, symmetry, and transitivity follow from the corresponding history-sameness rows on $B,A,P,C,R,T,K,N$. Carrier closure follows because transporting an accepted packet componentwise preserves the displayed equalities and row counts. Thus the finite source, pattern, classifier, stability, and naming obligations are all witnessed inside the packet.
\end{proof}

\falsifiablePrediction{Any accepted $\BioRealityQSixLeaveOneCodonBoundaryUp$ consumer that reports an anchored codon boundary, changes the $5$ boundary-row count, drops the $8$ predictable rows, drops the $8$ high-coverage closure rows, or reports recovery below $1.0$ refutes this carrier surface.}

\independenceWitness{codon-boundary, anchor-codon-certificate, predictable-row-ledger, closure-coverage-ledger, recovery-rate-audit: $\BioRealityQSixLeaveOneCodonBoundaryUp$ is not bijective to any listed sibling because it jointly binds an explicit $0$-anchor ledger, a $5$-row leave-one-codon boundary ledger, $8$ predictable rows, $8$ high-coverage closure rows, recovery rate $1.0$, above-threshold recovery $1.0$, transport, continuation, and local naming as one finite packet.}

\begin{closurestatus}{\BioRealityQSixLeaveOneCodonBoundaryUp}
  \constructivestory{A $\BioRealityQSixLeaveOneCodonBoundaryUp$ packet is generated from the finite leave-one-codon boundary ledger with $0$ anchor rows, $5$ boundary rows, $8$ predictable rows, $8$ high-coverage closure rows, recovery rate $1.0$, above-threshold recovery $1.0$, componentwise transport, displayed continuation replay, and local NameCert naming.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{The chapter closes the finite NameCert surface for the displayed leave-one-codon boundary counts and recovery coordinates only.}
  \formalstatus{\unformalized}
  \bridgestatus{none}
  \notclaimed{The chapter does not claim a biological mechanism, a codon list, a p-value, a genome-wide extrapolation, or any anchor-dependent boundary certificate.}
  \upgradepath{A stronger bridge would require an external formal carrier for the codon rows, the leave-one-codon operation, and the recovery threshold comparison.}
\end{closurestatus}