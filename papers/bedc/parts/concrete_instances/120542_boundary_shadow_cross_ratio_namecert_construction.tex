\chapter{A Concrete Naming Certificate for $\BoundaryShadowCrossRatioUp$}
\label{ch:concrete-instances-boundary-shadow-cross-ratio-namecert}
\origin{ai}

The $\BoundaryShadowCrossRatioUp$ packet realizes the hyperbolic observer-expansion route in \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as one finite NameCert surface joining boundary shadows, cross-ratio readback, visual metrics, Busemann boundary data, and observer-expansion ledgers. It sits after the boundary-shadow packet of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the cross-ratio packet of \autoref{ch:concrete-instances-hyperbolic-cross-ratio-namecert}, the visual-metric packet of \autoref{ch:concrete-instances-hyperbolic-visual-metric-namecert}, the Busemann boundary packet of \autoref{ch:concrete-instances-hyperbolic-busemann-boundary-namecert}, and the observer-expansion packet of \autoref{ch:concrete-instances-hyperbolic-observer-expansion-namecert}. The packet is a finite boundary-shadow cross-ratio ledger; it is not a host hyperbolic boundary, a selected geodesic endpoint, a global compactification theorem, or ambient real-valued geometry.

\falsifiablePrediction{An accepted $\BoundaryShadowCrossRatioUp$ read is refuted if a consumer exports a cross-ratio comparison without first displaying boundary-shadow rows, visual-metric rows, Busemann boundary rows, observer-expansion ledger rows, transport, replay, provenance, and local naming.}

\independenceWitness{hyperbolic-boundary-shadow, hyperbolic-cross-ratio, hyperbolic-visual-metric, hyperbolic-busemann-boundary, hyperbolic-observer-expansion: $\BoundaryShadowCrossRatioUp$ is not bijective to any listed sibling because only this packet jointly records boundary-shadow incidence, four-point cross-ratio readback, visual-metric comparison, Busemann boundary calibration, observer-expansion routing, transport, replay, provenance, and local NameCert rows.}

\begin{definition}[Boundary-shadow cross-ratio carrier]
\label{def:boundary-shadow-cross-ratio-carrier}
A $\BoundaryShadowCrossRatioUp$ carrier is a finite $\BHist$ packet
$$
X=(S,Q,V,B,O,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\HyperbolicBoundaryShadowUp$ row, naming the boundary shadows and incidence data visible to the packet.
\item $Q$ is the $\HyperbolicCrossRatioUp$ row, recording the finite four-boundary-point comparison readback.
\item $V$ is the $\HyperbolicVisualMetricUp$ row, supplying the visual-metric comparison surface used by the same boundary data.
\item $B$ is the $\HyperbolicBusemannBoundaryUp$ row, calibrating the shadow and visual-metric reads through Busemann boundary data.
\item $O$ is the $\HyperbolicObserverExpansionUp$ row, recording the observer-expansion ledger that consumes the boundary-shadow and metric comparison data.
\item $R$ is the unification ledger, binding shadow incidence, cross-ratio order, visual-metric scale, Busemann calibration, and observer-expansion replay to the same displayed boundary packet.
\item $H$ records componentwise $\hsame$ transport for $S,Q,V,B,O,R,P,N$.
\item $C$ records displayed $\Cont$ replay from a boundary-shadow request through cross-ratio readback, visual-metric comparison, Busemann calibration, observer-expansion routing, and the unification ledger.
\item $P$ is the $\Pkg$ provenance over $\BoundaryShadowCrossRatioUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicCrossRatioUp$, $\HyperbolicVisualMetricUp$, $\HyperbolicBusemannBoundaryUp$, $\HyperbolicObserverExpansionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\BoundaryShadowCrossRatioUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,Q,V,B,O,R,H,C,P,N$. It has no coordinate for selected boundary endpoints, global hyperbolic compactification, ambient real-valued geometry, quotient boundary equality, or host geodesic equality.
\end{definition}

\begin{theorem}[Boundary-shadow cross-ratio NameCert obligations]
\label{thm:boundary-shadow-cross-ratio-namecert-obligations}
For the carrier of \autoref{def:boundary-shadow-cross-ratio-carrier}, the local $\NameCert_{\BoundaryShadowCrossRatioUp}$ surface consists exactly of carrier admission for
$$
X=(S,Q,V,B,O,R,H,C,P,N),
$$
boundary-shadow exposure through $S$, cross-ratio readback through $Q$, visual-metric comparison through $V$, Busemann boundary calibration through $B$, observer-expansion routing through $O$, unification-ledger exactness through $R$, componentwise transport through $H$, displayed continuation replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:boundary-shadow-cross-ratio-carrier}. Its visible rows are exactly $S,Q,V,B,O,R,H,C,P,N$.

The geometric comparison side is read through $S,Q,V,B$. Boundary shadows provide the incidence surface, cross-ratio readback supplies the four-point comparison, the visual metric supplies scale, and the Busemann boundary row calibrates the same boundary data.

The observer side is read through $O,R$. The observer-expansion row consumes the boundary-shadow and comparison data, while the unification ledger records that all reads use the same displayed packet.

Finally $H,C,P,N$ preserve, replay, source, and name the route. Since the carrier has no coordinate for selected endpoints, global compactification, ambient real-valued geometry, quotient boundary equality, or host geodesic equality, the listed rows exhaust the obligations.
\end{proof}

\begin{theorem}[Boundary-shadow cross-ratio observer handoff]
\label{thm:boundary-shadow-cross-ratio-observer-handoff}
Every accepted $\BoundaryShadowCrossRatioUp$ packet hands an observer-expansion consumer the finite route
$$
\HyperbolicBoundaryShadowUp \longmapsto \HyperbolicCrossRatioUp,\HyperbolicVisualMetricUp,\HyperbolicBusemannBoundaryUp \longmapsto \HyperbolicObserverExpansionUp .
$$
The handoff must pass through $S,Q,V,B,O,R,H,C,P,N$ before any boundary-shadow cross-ratio comparison is consumed.
\end{theorem}
\begin{proof}
Start with the boundary-shadow row $S$. It fixes the displayed boundary incidence data before any comparison row is read.

Next read the comparison rows $Q,V,B$. The cross-ratio, visual-metric, and Busemann boundary rows provide compatible comparison surfaces for the same displayed shadows.

The ledger $R$ then binds those surfaces to the observer-expansion row $O$. This is the step that prevents a cross-ratio read from floating free of the boundary-shadow and observer-expansion data.

Transport, replay, provenance, and local naming are carried by $H,C,P,N$. Therefore a consumer cannot replace the route by selected boundary endpoints, quotient boundary equality, host geodesic equality, global compactification, or ambient real-valued geometry.
\end{proof}

The Lean \texttt{TasteGate.lean} development for $\BoundaryShadowCrossRatioUp$ must include explicit $\mathsf{Nontrivial}\ \BoundaryShadowCrossRatioUp$ and $\mathsf{FieldFaithful}\ \BoundaryShadowCrossRatioUp$ instances. These instances must preserve the boundary-shadow, cross-ratio, visual-metric, Busemann boundary, observer-expansion, unification-ledger, transport, replay, provenance, and local naming rows.

\begin{closurestatus}{\BoundaryShadowCrossRatioUp}
  \constructivestory{A $\BoundaryShadowCrossRatioUp$ packet is generated from boundary-shadow rows, cross-ratio readback, visual-metric comparison, Busemann boundary calibration, observer-expansion routing, a unification ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:boundary-shadow-cross-ratio-carrier}, \autoref{thm:boundary-shadow-cross-ratio-namecert-obligations}, and \autoref{thm:boundary-shadow-cross-ratio-observer-handoff} seed the finite boundary-shadow cross-ratio route for observer-expansion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, selected boundary endpoints, global compactification, quotient boundary equality, host geodesic equality, or ambient real-valued geometry.}
  \upgradepath{Obligation closure requires checked carrier admission, boundary-shadow exposure, cross-ratio readback, visual-metric comparison, Busemann boundary calibration, observer-expansion routing, unification-ledger exactness, nontriviality, and field faithfulness for BEDC.Derived.BoundaryShadowCrossRatioUp.}
\end{closurestatus}
