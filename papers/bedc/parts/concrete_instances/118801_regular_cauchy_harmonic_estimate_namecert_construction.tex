\chapter{A Concrete Naming Certificate for $\RegularCauchyHarmonicEstimateUp$}
\label{ch:concrete-instances-regular-cauchy-harmonic-estimate-namecert}
\origin{human}

Following Bishop and Bridges' constructive analysis, the $\RegularCauchyHarmonicEstimateUp$ packet records the finite BEDC surface by which the harmonic reciprocal schedule $n \mapsto 1/(n+1)$ is read as a regular Cauchy tail estimate before a $\RealUp$ seal is consumed. It sits over the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the tail-estimate route of \autoref{ch:concrete-instances-regularcauchytailestimate-namecert}. The packet names only the displayed harmonic threshold, the inspected finite window, the dyadic tolerance, the regular readback, the real seal, transport, replay, provenance, and local naming rows; it does not import quotient streams, selected limits, host real equality, choice, trichotomy, open-cover compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy harmonic estimate carrier]
\label{def:regular-cauchy-harmonic-estimate-carrier}
A $\RegularCauchyHarmonicEstimateUp$ carrier is a finite $\BHist$ packet
$$
H=(S,W,D,R,E,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
  \item $S$ is the harmonic reciprocal schedule row. It stores the finite threshold calculation by which a requested dyadic tolerance is met by the displayed tail bound $1/(n+1)$.
  \item $W$ is the finite $\StreamNameUp$ window row inspected at and beyond the threshold exposed by $S$.
  \item $D$ is the $\DyadicRatCoreUp$ tolerance row naming the dyadic error budget used for the harmonic tail read.
  \item $R$ is the $\RegSeqRatUp$ regular readback row reached from the window $W$ under the schedule and tolerance rows $S,D$.
  \item $E$ is the $\RealUp$ seal row that is available only after the harmonic schedule, window, tolerance, and regular readback rows have been displayed.
  \item $T$ records componentwise $\hsame$ transport for $S,W,D,R,E,C,P,N$.
  \item $C$ records displayed $\Cont$ replay from a tolerance request through the harmonic threshold, finite window, dyadic tolerance, regular readback, and real seal route.
  \item $P$ is the $\Pkg$ provenance over $\RegularCauchyTailEstimateUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
  \item $N$ is the local $\NameCert_{\RegularCauchyHarmonicEstimateUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through those finite rows. It has no coordinate for quotient stream equality, a chosen limit, host real equality, trichotomy, open-cover compactness, or an ambient completeness principle.
\end{definition}

\begin{theorem}[Regular Cauchy harmonic estimate NameCert obligations]
\label{thm:regular-cauchy-harmonic-estimate-obligations}
For every accepted $\RegularCauchyHarmonicEstimateUp$ carrier
$H=(S,W,D,R,E,T,C,P,N)$, the local $\NameCert_{\RegularCauchyHarmonicEstimateUp}$ surface consists exactly of five obligation rows:
\begin{enumerate}
  \item carrier admission for the harmonic reciprocal schedule, finite $\StreamNameUp$ window, $\DyadicRatCoreUp$ tolerance, $\RegSeqRatUp$ readback, $\RealUp$ seal, transport, replay, provenance, and local naming rows;
  \item classifier transport to the common $\RegularCauchyTailEstimateUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ finite surface;
  \item stability of $S,W,D,R,E$ under the componentwise $\hsame$ rows recorded in $T$;
  \item route exactness requiring every harmonic tail read to pass through $S,W,D,R,E$ in that order;
  \item refusal rows excluding quotient streams, selected limits, host real equality, choice, trichotomy, open-cover compactness, and ambient $\RealUp$ completeness.
\end{enumerate}
No local NameCert obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier $H=(S,W,D,R,E,T,C,P,N)$. The displayed analytic inventory is exactly the harmonic schedule, the finite stream window, the dyadic tolerance, the regular readback, and the terminal real seal.

Classifier transport is componentwise. The $T$ row moves $S,W,D,R,E$ and the administrative rows $C,P,N$ to the same finite comparison surface, so any accepted comparison is already a comparison of the listed rows.

Route exactness is supplied by the replay row $C$. A tolerance request first reads the harmonic threshold in $S$, then the finite observations in $W$, then the dyadic tolerance ledger $D$, then the regular readback $R$, and only then the seal $E$.

The refusal row follows from the carrier inventory. Since no row stores quotient streams, selected limits, host real equality, choice, trichotomy, open-cover compactness, or ambient completeness, the local name cannot export any of those principles.
\end{proof}

\begin{theorem}[Regular Cauchy harmonic tail window factorization]
\label{thm:regular-cauchy-harmonic-estimate-tail-window}
For every accepted $\RegularCauchyHarmonicEstimateUp$ carrier
$H=(S,W,D,R,E,T,C,P,N)$, every harmonic tail read factors through the finite threshold, window, tolerance, readback, and seal route
$$
S \longrightarrow W \longrightarrow D \longrightarrow R \longrightarrow E
$$
before any consumer may inspect the transport, replay, provenance, or local naming rows $T,C,P,N$. In particular, the read cannot bypass the finite $\StreamNameUp$ window, cannot replace the dyadic tolerance by a host real comparison, and cannot obtain a selected limit or quotient-stream equality.
\end{theorem}
\begin{proof}
Start with a harmonic tail read of an accepted carrier. The carrier exposes the harmonic reciprocal schedule only through $S$, so the first available threshold is the finite threshold row stored there.

Replay the route recorded in $C$. After $S$ has exposed the threshold, the read is sent to the finite observations in $W$, then to the dyadic tolerance ledger $D$, then to the regular rational readback $R$, and finally to the real seal $E$.

Transport through $T$ preserves this displayed order. The provenance row $P$ and local name $N$ bind the same route to $\RegularCauchyHarmonicEstimateUp$; they do not introduce another value-facing coordinate.

Thus every harmonic tail read factors through $S,W,D,R,E$ before reaching $T,C,P,N$. The carrier has no row from which a bypass could read host real comparison, selected limits, quotient streams, choice, trichotomy, open-cover compactness, or ambient completeness.
\end{proof}

\closureat{RegularCauchyHarmonicEstimateUp}{seedStr}

\begin{closurestatus}{\RegularCauchyHarmonicEstimateUp}
  \constructivestory{A $\RegularCauchyHarmonicEstimateUp$ packet is generated from a harmonic reciprocal schedule, a finite $\StreamNameUp$ window, a $\DyadicRatCoreUp$ tolerance, a $\RegSeqRatUp$ readback, a $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-harmonic-estimate-carrier}, \autoref{thm:regular-cauchy-harmonic-estimate-obligations}, and \autoref{thm:regular-cauchy-harmonic-estimate-tail-window} seed the finite harmonic-tail estimate surface for the Real and Completion route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient streams, selected limits, host real equality, choice, trichotomy, open-cover compactness, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier transport, hsame stability, harmonic tail-window exactness, refusal rows, and provenance faithfulness for $S,W,D,R,E,T,C,P,N$.}
\end{closurestatus}
