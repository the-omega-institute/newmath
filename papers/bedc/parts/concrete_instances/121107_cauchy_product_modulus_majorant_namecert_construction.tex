\chapter{A Concrete Naming Certificate for $\CauchyProductModulusMajorantUp$}
\label{ch:concrete-instances-cauchy-product-modulus-majorant-namecert}
\origin{human}

Following Hardy, Knopp, and Bishop--Bridges on constructive products of series, the $\CauchyProductModulusMajorantUp$ packet records the finite modulus-majorant route behind Cauchy products. It sits between the product-series surface of \autoref{ch:concrete-instances-cauchy-series-product-namecert}, the majorant tail ledger of \autoref{ch:concrete-instances-cauchy-majorant-series-namecert}, the shared uniform-tail route of \autoref{ch:concrete-instances-uniform-cauchy-series-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. It also supplies the product-tail control consumed by the Mertens packet of \autoref{ch:concrete-instances-mertens-cauchy-product-theorem-namecert}. The packet names a finite L10 route for product tails; it does not assert a host theorem on infinite Cauchy products, select a product sum, identify quotient series representatives, invoke countable choice, or import ambient $\RealUp$ completeness.

\begin{definition}[Cauchy product modulus majorant carrier]
\label{def:cauchy-product-modulus-majorant-carrier}
A $\CauchyProductModulusMajorantUp$ carrier is a finite $\BHist$ packet
$$
M=(A,B,W,Q,T,U,R,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ and $B$ are the two source-series rows. They expose finite term windows, partial-sum windows, and source tail requests already admitted by the neighboring Cauchy-series product route.
\item $W$ is the convolution-window row. It lists the inspected product diagonals, the finite rectangular and triangular windows used to compare product partial sums, and the omitted tail cells.
\item $Q$ is the product-modulus row. It records the requested output tolerance together with the finite source-tail indices that make every displayed product-window comparison small enough.
\item $T$ is the majorant tail ledger. It records the nonnegative majorant schedules and tail bounds that dominate the omitted convolution cells.
\item $U$ is the $\UniformCauchySeriesUp$ threshold row reached only after $A,B,W,Q,T$ have all been displayed.
\item $R$ is the $\RegSeqRatUp$ readback row for the source windows, product partial sums, and modulus thresholds.
\item $D$ is the $\DyadicRatCoreUp$ budget row, recording the dyadic tolerances, product-radius comparisons, and finite addition slack used by $Q$ and $T$.
\item $E$ is the terminal $\RealUp$ seal row reached only after the product modulus, majorant ledger, uniform tail threshold, regular readback, and dyadic budget rows have been inspected.
\item $H$ records componentwise $\hsame$ transport for the source series, convolution windows, modulus row, majorant ledger, uniform threshold, readback, dyadic budget, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a product-tail request through
$$
A,B,W \longmapsto Q,T \longmapsto U,R,D \longmapsto E,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\CauchyProductModulusMajorantUp$, $\CauchySeriesProductUp$, $\CauchyMajorantSeriesUp$, $\UniformCauchySeriesUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyProductModulusMajorantUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,B,W,Q,T,U,R,D,E,H,C,P,N$. It has no coordinate for a host product theorem, selected infinite convolution sum, quotient equality of series, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Cauchy product modulus majorant NameCert obligations]
\label{thm:cauchy-product-modulus-majorant-namecert-obligations}
For every accepted $\CauchyProductModulusMajorantUp$ carrier
$$
M=(A,B,W,Q,T,U,R,D,E,H,C,P,N),
$$
the local $\NameCert_{\CauchyProductModulusMajorantUp}$ surface consists exactly of carrier admission for the two source-series rows, convolution-window exposure, product-modulus exactness, majorant tail control, uniform-tail thresholding, regular rational readback, dyadic budget accounting, Real seal nonescape, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.

No Cauchy product modulus-majorant obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-product-modulus-majorant-carrier}. The source rows $A$ and $B$ and the convolution row $W$ are the only product-facing data visible before a tail request is answered.

The modulus and majorant obligations are the rows $Q$ and $T$. The modulus row stores the finite source-tail indices for the requested tolerance, while the majorant row supplies the displayed domination data for omitted convolution cells.

The endpoint-facing rows are downstream. The uniform threshold $U$, regular readback $R$, and dyadic budget $D$ must be displayed before the terminal Real seal $E$ is available.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier has no coordinate for a host product theorem, selected convolution sum, quotient equality, countable choice, trichotomy, or ambient completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Cauchy product modulus majorant uniform tail route]
\label{thm:cauchy-product-modulus-majorant-uniform-tail-route}
Every Real or Completion consumer of an accepted $\CauchyProductModulusMajorantUp$ packet factors its constructive Cauchy-product tail read through the displayed route
$$
\CauchySeriesProductUp \longmapsto \CauchyMajorantSeriesUp \longmapsto \UniformCauchySeriesUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \RealUp .
$$
In carrier notation, the consumer first reads the source rows and convolution window $A,B,W$, then the product modulus and majorant ledger $Q,T$, then the uniform threshold, readback, and dyadic budget rows $U,R,D$, and only then the Real seal $E$ and structural rows $H,C,P,N$. The route exports finite tail control for constructive Cauchy products without exporting a host infinite product theorem, selected product endpoint, quotient stream equality, countable-choice schedule, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from the accepted carrier. The Cauchy-series product surface supplies the two source rows and the convolution window, so the product tail is first a displayed finite-window request through $A,B,W$.

Next read the modulus-majorant pair. The row $Q$ gives the finite source-tail thresholds for the requested product tolerance, and the row $T$ gives the majorant tail ledger that dominates the omitted convolution cells.

The handoff then enters the uniform-tail route. The $\UniformCauchySeriesUp$ row $U$ consumes the same finite threshold data, while $R$ and $D$ record the regular rational readback and dyadic tolerance budget needed before any endpoint-facing read is public.

Finally the Real seal $E$ and structural rows $H,C,P,N$ preserve that route. The consumer therefore receives exactly the finite modulus-majorant handoff and cannot replace it by a host product theorem, a selected infinite convolution sum, quotient equality, countable choice, or ambient completeness.
\end{proof}

\closureat{CauchyProductModulusMajorantUp}{seedStr}

\begin{closurestatus}{\CauchyProductModulusMajorantUp}
  \constructivestory{A $\CauchyProductModulusMajorantUp$ packet is generated from two source-series rows, a convolution-window row, a product-modulus row, a majorant tail ledger, a UniformCauchySeries threshold row, RegSeqRat readback, a DyadicRatCore budget, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-product-modulus-majorant-carrier}, \autoref{thm:cauchy-product-modulus-majorant-namecert-obligations}, and \autoref{thm:cauchy-product-modulus-majorant-uniform-tail-route} seed the finite product-tail modulus route over CauchySeriesProduct, CauchyMajorantSeries, UniformCauchySeries, RegSeqRat, DyadicRatCore, Real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host Cauchy product theorems, selected infinite convolution sums, quotient equality of series, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, convolution-window exactness, product-modulus exactness, majorant tail control, uniform-tail thresholding, regular readback, dyadic budget accounting, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CauchyProductModulusMajorantUp.}
\end{closurestatus}
