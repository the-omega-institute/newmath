\input{parts/concrete_instances/real/13_real_constant_core.tex}
\input{parts/concrete_instances/real/13_real_stream_prefix.tex}

\begin{theorem}[Independent reindexed constant rational embedding certificate]
\label{thm:real-independent-reindexed-constant-rational-embedding-certificate}
For histories $d,e:\Hist$ and observation maps $r,q:\Hist\to\Hist$, the two
concrete endpoint assignments
$$
d\longmapsto (\mathsf{RatConstStream}_d\circ r,\Eone(d)),
\qquad
e\longmapsto (\mathsf{RatConstStream}_e\circ q,\Eone(e))
$$
supply the independently scheduled constant rational embedding certificate into
RealUp. The first endpoint has carrier exactness
$$
\mathsf{RatHistoryCarrier}(d)
\quad\Longleftrightarrow\quad
\mathsf{RatStreamNameCarrier}(\mathsf{RatConstStream}_d\circ r)
\quad\Longleftrightarrow\quad
\mathsf{RealConstantHistoryCarrier}(\Eone(d)),
$$
and the second endpoint has carrier exactness
$$
\mathsf{RatHistoryCarrier}(e)
\quad\Longleftrightarrow\quad
\mathsf{RatStreamNameCarrier}(\mathsf{RatConstStream}_e\circ q)
\quad\Longleftrightarrow\quad
\mathsf{RealConstantHistoryCarrier}(\Eone(e)).
$$
Its classifier exactness is the three-row chain displayed in
\autoref{thm:real-constant-independent-reindexed-stream-bridge}: the RatUp
classifier of $d$ and $e$, the StreamNameUp classifier of
$\mathsf{RatConstStream}_d\circ r$ and $\mathsf{RatConstStream}_e\circ q$, the
RealUp unary-observation stream classifier of those same streams, and the
RealUp constant-history classifier of $\Eone(d)$ and $\Eone(e)$ are equivalent.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-constant-stream-carrier-reindexed-streamname-bridge}
to $d$ with observation map $r$. This gives the first carrier row, identifying
the RatUp carrier of $d$, the StreamNameUp carrier of
$\mathsf{RatConstStream}_d\circ r$, and the RealUp constant carrier of
$\Eone(d)$.

Apply the same theorem separately to $e$ with observation map $q$. This gives
the second carrier row, identifying the RatUp carrier of $e$, the StreamNameUp
carrier of $\mathsf{RatConstStream}_e\circ q$, and the RealUp constant carrier
of $\Eone(e)$.

For classifier exactness, apply
\autoref{thm:real-constant-independent-reindexed-stream-bridge}. Its three
equivalence rows are precisely the classifier rows required for independently
scheduled constant streams and their paired e-one seals.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealIndependentReindexedConstant\_rational\_embedding\_certificate}

\begin{theorem}[Real stream classifier finite-prefix stability package]
\label{thm:real-stream-classifier-finite-prefix-stability-package}
For rational-observation streams $x,y,z:\mathbb{N}\to\Hist$ and prefix index
$n$, if
$$
\forall i,\ \mathsf{RatHistoryCarrier}(x(i)),\quad
\mathsf{RealStreamClassifier}(x,y),\quad
\mathsf{RealStreamClassifier}(y,z),
$$
then
$$
\mathsf{RealStreamPrefixClassifier}(x,x,n)
\land
\mathsf{RealStreamPrefixClassifier}(y,x,n)
\land
\mathsf{RealStreamPrefixClassifier}(x,z,n)
\land
\mathsf{RatHistoryClassifier}(x(n),z(n)).
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-stream-classifier-prefix} to
$\mathsf{RealStreamClassifier}(x,y)$ at the selected prefix index $n$, and
apply the same theorem to $\mathsf{RealStreamClassifier}(y,z)$ at $n$. These
give the two finite-prefix classifier witnesses for $x$ with $y$ and for $y$
with $z$.

Use \autoref{thm:real-stream-prefix-classifier-refl} with the displayed
rational-history carrier row for $x$ to obtain
$\mathsf{RealStreamPrefixClassifier}(x,x,n)$. Apply
\autoref{thm:real-stream-prefix-classifier-symm} to the $x,y$ prefix witness
to obtain $\mathsf{RealStreamPrefixClassifier}(y,x,n)$.

Compose the $x,y$ and $y,z$ prefix witnesses by
\autoref{thm:real-stream-prefix-classifier-trans}, yielding
$\mathsf{RealStreamPrefixClassifier}(x,z,n)$. Finally apply
\autoref{thm:real-stream-prefix-classifier-endpoint} to this composite prefix
to recover $\mathsf{RatHistoryClassifier}(x(n),z(n))$.
\end{proof}

Fit. Traditional target: finite-prefix stability for RealUp streams. BEDC
source: RatUp plus RealUp stream prefixes. Carrier: rational-observation
streams. Classifier: $\mathsf{RealStreamClassifier}$ and
$\mathsf{RealStreamPrefixClassifier}$. Stability: prefix reflexivity, symmetry,
transitivity, and endpoint projection. Ledger: the selected prefix index $n$.
Core theorem: \autoref{thm:real-stream-classifier-finite-prefix-stability-package}.
Exactness: the displayed four-part conjunction. Standard bridge: finite
rational prefix agreement. Status: $\mathsf{ProofTarget}$.

\begin{theorem}[Real unary StreamNameUp stability package]
\label{thm:real-unary-streamname-stability-package}
For certified unary-observation maps $s,t,u,s',t':\Hist\to\Hist$, suppose
$$
\mathsf{RatStreamNameCarrier}(s),\quad
\mathsf{RatStreamNameCarrier}(t),\quad
\mathsf{RatStreamNameCarrier}(u),
$$
$$
\mathsf{RealUnaryStreamClassifier}(s,t),\quad
\mathsf{RealUnaryStreamClassifier}(t,u),
$$
and suppose the transported endpoints satisfy
$$
\forall n,\ \mathsf{UnaryHistory}(n)\to\hsame(s(n),s'(n)),
\quad
\forall n,\ \mathsf{UnaryHistory}(n)\to\hsame(t(n),t'(n)).
$$
Then the same BEDC unary observation rows supply the following public
certificate fields:
\begin{enumerate}
  \item $\mathsf{RatStreamNameClassifier}(s,t)$.
  \item $\mathsf{RealUnaryStreamClassifier}(s,s)$,
  $\mathsf{RealUnaryStreamClassifier}(t,s)$, and
  $\mathsf{RealUnaryStreamClassifier}(s,u)$.
  \item For every certified unary observation $n$, both
  $\mathsf{RatHistoryCarrier}(s(n))$ and $\mathsf{RatHistoryCarrier}(t(n))$.
  \item $\mathsf{RealUnaryStreamClassifier}(s',t')$.
\end{enumerate}
\end{theorem}
\begin{proof}
Unfold Definition~\ref{def:rat-stream-name-carrier},
Definition~\ref{def:rat-stream-name-classifier}, and
Definition~\ref{def:real-unary-observation-stream-classifier}. After this
unfolding, both StreamNameUp and RealUp classifier rows are the same pointwise
RatUp classifier over histories $n$ equipped with
$\mathsf{UnaryHistory}(n)$.

The first conclusion is therefore just the displayed
$\mathsf{RealUnaryStreamClassifier}(s,t)$ read under the StreamNameUp
classifier name. For $\mathsf{RealUnaryStreamClassifier}(s,s)$, fix a
certified observation $n$; the carrier row for $s$ gives
$\mathsf{RatHistoryCarrier}(s(n))$, and RatUp classifier reflexivity gives the
required point classifier at that observation.

For the reversed classifier $\mathsf{RealUnaryStreamClassifier}(t,s)$, read the
point classifier from $\mathsf{RealUnaryStreamClassifier}(s,t)$ and apply RatUp
classifier symmetry. For $\mathsf{RealUnaryStreamClassifier}(s,u)$, read the
two point classifiers from the assumptions at the same certified observation
and compose them by RatUp classifier transitivity.

For endpoint carriers, fix a certified unary observation $n$ and unpack the
RatUp classifier obtained from $\mathsf{RealUnaryStreamClassifier}(s,t)$ at
that observation. Its endpoint data are precisely
$\mathsf{RatHistoryCarrier}(s(n))$ and $\mathsf{RatHistoryCarrier}(t(n))$.

For transport, fix the same certified observation $n$. Apply
$\mathsf{RatHistoryClassifier\_hsame\_transport}$ to the point classifier from
$\mathsf{RealUnaryStreamClassifier}(s,t)$ and to the two pointwise $\hsame$
witnesses from $s,t$ to $s',t'$. Repacking the transported point classifier
over all certified unary observations gives
$\mathsf{RealUnaryStreamClassifier}(s',t')$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealUnaryStreamClassifier\_streamName\_stability\_package}
\leanchecked{BEDC.Derived.RealUp.RealUnaryStreamClassifier\_stability\_package}

\begin{theorem}[Real stream classifier selected e-one pair readback]
\label{thm:real-stream-classifier-selected-e1-pair-readback}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$, every selected
observation index $n$, and histories $a,b:\Hist$, if
$$
\mathsf{RealStreamClassifier}(x,y),\quad
\hsame(x(n),\Eone(a)),\quad
\hsame(y(n),\Eone(b)),
$$
then
$$
\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land \hsame(a,b).
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-stream-classifier-prefix} to the stream classifier at
the selected index $n$. This produces the finite-prefix classifier witness
needed at that index.

Apply \autoref{thm:real-stream-prefix-e1-pair-readback} to that prefix witness
and to the two displayed e-one endpoint equalities. The readback theorem returns
the two unary tail witnesses and the internal sameness relation
$\hsame(a,b)$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_selected\_e1\_pair\_readback}

\begin{theorem}[Real stream classifier transported selected e-one rational classifier readback]
\label{thm:real-stream-classifier-transported-selected-e1-rat-classifier-readback}
For rational-observation streams $x,x',y,y':\mathbb{N}\to\Hist$, every selected
observation index $n$, and histories $a,b:\Hist$, if
$$
\forall i,\ \hsame(x(i),x'(i)),\quad
\forall i,\ \hsame(y(i),y'(i)),\quad
\mathsf{RealStreamClassifier}(x,y),\quad
\hsame(x'(n),\Eone(a)),\quad
\hsame(y'(n),\Eone(b)),
$$
then
$$
\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b)).
$$
\end{theorem}
\begin{proof}
Use the forward direction of
\autoref{thm:real-stream-finite-prefix-exactness} on
$\mathsf{RealStreamClassifier}(x,y)$ and select the finite prefix at $n$.
Transport this prefix witness along the two pointwise $\hsame$ hypotheses by
\autoref{thm:real-stream-prefix-classifier-hsame-transport}, obtaining a
classified prefix for $x'$ and $y'$ at the same selected index.

Project the selected endpoint classifier with
\autoref{thm:real-stream-prefix-classifier-endpoint}. It gives
$\mathsf{RatHistoryClassifier}(x'(n),y'(n))$.

Finally apply \autoref{thm:rat-history-classifier-hsame-transport} to move this
endpoint classifier along $\hsame(x'(n),\Eone(a))$ and
$\hsame(y'(n),\Eone(b))$, producing
$\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_transported\_selected\_e1\_rat\_classifier\_readback}

Fit: traditional target selected rational endpoint readback for RealUp streams;
BEDC source RatUp plus RealUp; carrier rational-observation streams; classifier
RealStreamClassifier and RatHistoryClassifier; stability prefix exactness,
hsame transport, and endpoint projection; ledger selected observation index and
displayed e-one seals; core theorem
\autoref{thm:real-stream-classifier-transported-selected-e1-rat-classifier-readback};
exactness the displayed implication; standard bridge rational equality read from
equal real observations; status $\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier transported selected e-one pair readback]
\label{thm:real-stream-classifier-transported-selected-e1-pair-readback}
For rational-observation streams $x,x',y,y':\mathbb{N}\to\Hist$, every selected
observation index $n$, and histories $a,b:\Hist$, if
$$
\forall i,\ \hsame(x(i),x'(i)),\quad
\forall i,\ \hsame(y(i),y'(i)),\quad
\mathsf{RealStreamClassifier}(x,y),\quad
\hsame(x'(n),\Eone(a)),\quad
\hsame(y'(n),\Eone(b)),
$$
then
$$
\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land \hsame(a,b).
$$
\end{theorem}
\begin{proof}
Apply
\autoref{thm:real-stream-classifier-transported-selected-e1-rat-classifier-readback}
to obtain the endpoint classifier
$\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$.

Apply \autoref{thm:rat-history-classifier-e1-tail-unary-iff} in the forward
direction. The one-tail exactness theorem returns
$\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land \hsame(a,b)$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_transported\_e1\_pair\_readback}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_transported\_selected\_e1\_pair\_readback}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_transport\_selected\_e1\_pair\_readback}

\begin{theorem}[Real stream classifier transported selected e-one full readback package]
\label{thm:real-stream-classifier-transported-selected-e1-full-readback-package}
For rational-observation streams $x,x',y,y':\mathbb{N}\to\Hist$, every selected
observation index $n$, and histories $a,b:\Hist$, if
$$
\forall i,\ \hsame(x(i),x'(i)),\quad
\forall i,\ \hsame(y(i),y'(i)),\quad
\mathsf{RealStreamClassifier}(x,y),\quad
\hsame(x'(n),\Eone(a)),\quad
\hsame(y'(n),\Eone(b)),
$$
then
$$
\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))\land
\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land \hsame(a,b).
$$
\end{theorem}
\begin{proof}
Apply
\autoref{thm:real-stream-classifier-transported-selected-e1-rat-classifier-readback}
under the displayed transport, classifier, and e-one seal hypotheses. This gives
the first conjunct
$\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$.

Apply
\autoref{thm:real-stream-classifier-transported-selected-e1-pair-readback}
under the same hypotheses. This gives
$\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land \hsame(a,b)$.

Package the endpoint classifier together with the unary-tail readback triple.
The resulting conjunction is exactly the selected transported e-one readback
package displayed in the statement.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_transported\_selected\_e1\_full\_readback\_package}

Fit: traditional target selected sealed rational endpoint package for RealUp
streams; BEDC source RatUp plus RealUp finite-prefix exactness; carrier
rational-observation streams; classifier RealStreamClassifier and
RatHistoryClassifier; stability prefix hsame transport and RatUp one-tail
exactness; ledger selected index and two e-one seals; core theorem this block;
exactness the displayed conjunction; standard bridge rational equality read from
equal transported real observations; status $\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier transported selected positive denominator package]
\label{thm:real-stream-classifier-transported-selected-positive-denominator-package}
For rational-observation streams $x,x',y,y':\mathbb{N}\to\Hist$ and every
selected observation index $n$, if
$$
\forall i,\ \hsame(x(i),x'(i)),\quad
\forall i,\ \hsame(y(i),y'(i)),\quad
\mathsf{RealStreamClassifier}(x,y),
$$
then
$$
\mathsf{RatHistoryClassifier}(x'(n),y'(n))\land
\mathsf{PositiveUnaryDenominator}(x'(n))\land
\mathsf{PositiveUnaryDenominator}(y'(n)).
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-stream-classifier-transport-with-endpoint-readback}
to the two pointwise $\hsame$ hypotheses and the source stream classifier. The
endpoint field of that package gives
$\mathsf{RatHistoryClassifier}(x'(n),y'(n))$ at the selected index $n$.

Apply \autoref{thm:rat-history-classifier-positive-denominators} to this
selected endpoint classifier. It exposes
$\mathsf{PositiveUnaryDenominator}(x'(n))$ and
$\mathsf{PositiveUnaryDenominator}(y'(n))$.

Package the endpoint classifier with the two positive-denominator witnesses.
The result is exactly the displayed transported selected denominator readback
conjunction.
\end{proof}

\begin{theorem}[Real stream classifier transported selected positive unary nonempty package]
\label{thm:real-stream-classifier-transport-selected-positive-unary-nonempty-package}
For rational-observation streams $x,x',y,y':\mathbb{N}\to\Hist$ and a selected
observation index $n$, assume
$$
\forall i,\ \hsame(x(i),x'(i)),\quad
\forall i,\ \hsame(y(i),y'(i)),\quad
\mathsf{RealStreamClassifier}(x,y).
$$
Then the transported selected endpoints carry the complete rational endpoint
package
$$
\begin{aligned}
&\mathsf{RatHistoryClassifier}(x'(n),y'(n))\land
\mathsf{PositiveUnaryDenominator}(x'(n))\land
\mathsf{PositiveUnaryDenominator}(y'(n))\\
&\land\ \mathsf{UnaryHistory}(x'(n))\land
\mathsf{UnaryHistory}(y'(n))\land
(\hsame(x'(n),\emp)\to\bot)\land
(\hsame(y'(n),\emp)\to\bot).
\end{aligned}
$$
\end{theorem}
\begin{proof}
Transport the selected rational-history classifier from
$\mathsf{RatHistoryClassifier}(x(n),y(n))$ to
$\mathsf{RatHistoryClassifier}(x'(n),y'(n))$ along the two pointwise
$\hsame$ hypotheses.

Rational classifier positivity exposes positive unary denominator witnesses at
both transported endpoints. Each positive unary denominator witness gives a
unary-history row and separates that endpoint from $\emp$. Packaging these rows
with the transported classifier gives the displayed conjunction.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_transport\_selected\_positive\_unary\_nonempty\_package}

Fit: traditional target selected positive denominator readback for transported
RealUp rational observations; BEDC source RealUp plus RatUp; carrier
rational-observation streams; classifier RealStreamClassifier and
RatHistoryClassifier; stability prefix hsame transport plus RatUp denominator
exposure; ledger selected observation index and transported endpoint displays;
core theorem this block; exactness the displayed conjunction; standard bridge
positivity of rational denominators inside equal transported real observations;
status $\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier selected positive unary nonempty package]
\label{thm:real-stream-classifier-selected-positive-unary-nonempty-package}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$ and a selected
index $n$, if
$$
\mathsf{RealStreamClassifier}(x,y),
$$
then the selected endpoints carry positive unary denominator witnesses, unary
history witnesses, and nonempty boundary exclusions:
$$
\begin{array}{l}
\mathsf{PositiveUnaryDenominator}(x(n))\land
\mathsf{PositiveUnaryDenominator}(y(n))\land
\mathsf{UnaryHistory}(x(n))\land
\mathsf{UnaryHistory}(y(n))\land\\
(\hsame(x(n),\emp)\to\mathsf{False})\land
(\hsame(y(n),\emp)\to\mathsf{False}).
\end{array}
$$
\end{theorem}
\begin{proof}
Select the point classifier
$\mathsf{RatHistoryClassifier}(x(n),y(n))$ from the stream classifier. RatUp
positive-denominator projection gives the two positive denominator witnesses.
Apply positive unary denominator readback to each endpoint, yielding the unary
history witness and the empty-boundary contradiction for that endpoint.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_selected\_positive\_unary\_nonempty\_package}

\begin{theorem}[Real stream classifier transported selected e-one denominator package]
\label{thm:real-stream-classifier-transported-selected-e1-denominator-package}
For rational-observation streams $x,x',y,y':\mathbb{N}\to\Hist$, every selected
observation index $n$, and histories $a,b:\Hist$, if
$$
\forall i,\ \hsame(x(i),x'(i)),\quad
\forall i,\ \hsame(y(i),y'(i)),\quad
\mathsf{RealStreamClassifier}(x,y),\quad
\hsame(x'(n),\Eone(a)),\quad
\hsame(y'(n),\Eone(b)),
$$
then
$$
\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))\land
\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land \hsame(a,b)\land
\mathsf{PositiveUnaryDenominator}(x'(n))\land
\mathsf{PositiveUnaryDenominator}(y'(n)).
$$
\end{theorem}
\begin{proof}
Apply
\autoref{thm:real-stream-classifier-transported-selected-e1-full-readback-package}
under the two pointwise transport rows, the source RealUp stream classifier, and
the two selected e-one seals. This gives the endpoint classifier
$\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$ together with the unary tail
readback
$\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land \hsame(a,b)$.

Apply
\autoref{thm:real-stream-classifier-transported-selected-positive-denominator-package}
under the same two pointwise transport rows and the same source RealUp stream
classifier. This gives the selected transported endpoint classifier together
with the positive-denominator witnesses for $x'(n)$ and $y'(n)$.

Keep the endpoint classifier and unary-tail readback from the e-one package, and
keep the two denominator witnesses from the denominator package. Combining these
conjuncts gives exactly the displayed selected transported rational observation
package.
\end{proof}

Fit: traditional target selected transported rational observation package for
RealUp; BEDC source RatUp plus StreamNameUp-shaped RealUp streams; carrier
rational-observation streams; classifiers RealStreamClassifier and
RatHistoryClassifier; stability transported prefix readback plus
positive-denominator exposure; ledger selected observation index, two transport
rows, and two e-one seals; core theorem this block; exactness the displayed
conjunction; downstream use by the RealUp dyadic embedding certificate; status
$\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier unary denominator context closure]
\label{thm:real-stream-classifier-unary-denominator-context-closed}
For rational-observation streams
$x,y,p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$, if
$$
\mathsf{RealStreamClassifier}(x,y),
$$
and for every observation index $n$ the contexts and continuation outputs
satisfy
$$
\mathsf{UnaryHistory}(p_X(n)),\quad \mathsf{UnaryHistory}(t_X(n)),\quad
\hsame(p_X(n),p_Y(n)),\quad \hsame(t_X(n),t_Y(n)),
$$
$$
\mathsf{Cont}(p_X(n),x(n),m_X(n)),\quad
\mathsf{Cont}(m_X(n),t_X(n),o_X(n)),
$$
$$
\mathsf{Cont}(p_Y(n),y(n),m_Y(n)),\quad
\mathsf{Cont}(m_Y(n),t_Y(n),o_Y(n)),
$$
then
$$
\mathsf{RealStreamClassifier}(o_X,o_Y).
$$
\end{theorem}
\begin{proof}
Fix an observation index $n$. The stream classifier supplies
$\mathsf{RatHistoryClassifier}(x(n),y(n))$ at that index.

Apply \autoref{thm:rat-history-classifier-unary-denominator-context-closed} to
this point classifier, the two unary-context hypotheses on $p_X(n)$ and
$t_X(n)$, and the two displayed context comparisons
$\hsame(p_X(n),p_Y(n))$ and $\hsame(t_X(n),t_Y(n))$. This gives the rational
history classifier on the associativity-normal context endpoints.

The four continuation witnesses identify those normal endpoints with $o_X(n)$
and $o_Y(n)$, using associativity of append as in
\autoref{thm:real-constant-history-classifier-cont-unary-denominator-context-closed}.
Transport the point classifier along these endpoint identifications.

Since $n$ was arbitrary, repack the transported pointwise witnesses as
$\mathsf{RealStreamClassifier}(o_X,o_Y)$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_unary\_denominator\_context\_closed}

\begin{theorem}[Real stream classifier selected continuation e-one pair readback]
\label{thm:real-stream-classifier-selected-continuation-e1-pair-readback}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$, a selected index
$n$, a shared unary suffix $q$, continuation outputs $xq,yq$, and histories
$a,b:\Hist$, if
$$
\mathsf{RealStreamClassifier}(x,y),\quad
\mathsf{UnaryHistory}(q),\quad
\mathsf{Cont}(x(n),q,xq),\quad
\mathsf{Cont}(y(n),q,yq),
$$
and
$$
\hsame(xq,\Eone(a)),\quad \hsame(yq,\Eone(b)),
$$
then
$$
\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land \hsame(a,b).
$$
\end{theorem}
\begin{proof}
At the selected index, the stream classifier gives
$\mathsf{RatHistoryClassifier}(x(n),y(n))$. Close this rational classifier
under the shared unary suffix $q$ by rational unary-denominator continuation
closure, using reflexive sameness of the suffix on the two sides. The two
continuation displays identify the closed endpoints with $xq$ and $yq$.
Transport the closed rational classifier across those displays and then across
the two e-one endpoint seals. Rational one-tail classifier exactness reads back
the unary tails and their $\hsame$ relation.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_selected\_continuation\_e1\_pair\_readback}

Fit: traditional target streamwise rational-denominator context stability; BEDC
source RealUp streams plus RatUp classifier plus $\mathsf{Cont}$; carrier
rational-observation streams; classifier $\mathsf{RealStreamClassifier}$;
stability pointwise unary context closure; ledger pointwise prefix and tail
context witnesses with continuation outputs; core theorem
\autoref{thm:real-stream-classifier-unary-denominator-context-closed};
exactness pointwise $\mathsf{RealStreamClassifier}$ preservation; standard
bridge denominator/context stability of rational real names; status
$\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier unary context closure]
\label{thm:real-stream-classifier-unary-context-closed}
For rational-observation streams $x,y$ and pointwise unary contexts
$pref_X,pref_Y,tail_X,tail_Y$, if $\mathsf{RealStreamClassifier}(x,y)$ and
each prefix and tail is matched by $\hsame$, then the stream obtained by
placing every observation inside its matched prefix and tail context is again
classified by $\mathsf{RealStreamClassifier}$.
\end{theorem}
\begin{proof}
At each index, apply rational history unary-denominator context closure to the
point classifier supplied by the real stream classifier and to the matched
unary prefix and tail histories. These pointwise classifiers assemble into the
contextual stream classifier.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_unary\_context\_closed}

\begin{theorem}[Real stream classifier unary denominator context selected one-tail pair readback]
\label{thm:real-stream-classifier-unary-denominator-context-selected-e1-pair-readback}
Under the hypotheses of
\autoref{thm:real-stream-classifier-unary-denominator-context-closed}, if the
selected outputs $o_X(n)$ and $o_Y(n)$ are one-extension histories, then the
displayed tails are unary and internally the same.
\end{theorem}
\begin{proof}
First apply streamwise unary denominator context closure to obtain
$\mathsf{RealStreamClassifier}(o_X,o_Y)$. Then apply selected one-tail pair
readback at the chosen index $n$ to the two displayed endpoint witnesses.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_unary\_denominator\_context\_selected\_e1\_pair\_readback}

\begin{theorem}[Real stream classifier unary denominator context selected positive denominators]
\label{thm:real-stream-classifier-unary-denominator-context-selected-positive-denominators}
For rational-observation streams
$x,y,p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$ and a selected
index $n$, assume
$$
\mathsf{RealStreamClassifier}(x,y),
$$
and for every observation index the unary contexts, context comparisons, and
continuation displays satisfy the hypotheses of
\autoref{thm:real-stream-classifier-unary-denominator-context-closed}. Then
$$
\mathsf{PositiveUnaryDenominator}(o_X(n))\land
\mathsf{PositiveUnaryDenominator}(o_Y(n)).
$$
\end{theorem}
\begin{proof}
At the selected index $n$, the stream classifier gives
$\mathsf{RatHistoryClassifier}(x(n),y(n))$. Apply the rational unary
denominator context closure to the two unary contexts and their $\hsame$
comparisons. The continuation displays identify the normalized contextual
endpoints with $o_X(n)$ and $o_Y(n)$ by associativity of continuation append.

Transport the rational classifier to these displayed outputs and apply
\autoref{thm:rat-history-classifier-positive-denominators}. This yields the two
positive-denominator witnesses in the displayed conclusion.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_unary\_denominator\_context\_selected\_positive\_denominators}

\begin{theorem}[Real stream classifier unary denominator context one-tail pair readback]
\label{thm:real-stream-classifier-unary-denominator-context-e1-pair-readback}
For rational-observation streams
$x,y,p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$ and a selected
index $n$, assume $\mathsf{RealStreamClassifier}(x,y)$ and the pointwise
unary-context and continuation hypotheses of
\autoref{thm:real-stream-classifier-unary-denominator-context-closed}. If the
displayed contextual outputs satisfy
$$
\hsame(o_X(n),\Eone(a))\quad\text{and}\quad \hsame(o_Y(n),\Eone(b)),
$$
then
$$
\mathsf{UnaryHistory}(a)\land\mathsf{UnaryHistory}(b)\land\hsame(a,b).
$$
\end{theorem}
\begin{proof}
First apply
\autoref{thm:real-stream-classifier-unary-denominator-context-closed} to obtain
$\mathsf{RealStreamClassifier}(o_X,o_Y)$. At the selected index, transport the
point classifier to the displayed one-extension endpoints and apply the
selected RealUp one-tail pair readback theorem.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_unary\_denominator\_context\_e1\_pair\_readback}
