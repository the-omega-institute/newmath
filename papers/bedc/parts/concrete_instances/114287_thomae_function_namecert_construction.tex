\chapter{A Concrete Naming Certificate for $\ThomaeFunctionUp$}
\label{ch:concrete-instances-thomae-function-namecert}
\origin{human}

Following Thomae and Bishop--Bridges constructive analysis, the $\ThomaeFunctionUp$ packet records the finite rational-spike data by which the popcorn-function example is read through rational density, denominator ledgers, regular rational windows, and the terminal Real seal. It sits between the rational input surface of RationalDensityRealUp, the $\RatUp$ denominator arithmetic, the $\StreamNameUp$ and $\RegSeqRatUp$ readback route, the $\RealUp$ completion seal, and the local continuity-facing boundary used by $\ContinuousMapUp$. The packet names the constructive rational-spike function as finite BEDC data; it is not a host function, a quotient-real extensionality principle, a global continuity theorem, a selected enumeration of rationals, or a classical discontinuity set.

\begin{definition}[Thomae function carrier]
\label{def:thomae-function-carrier}
A $\ThomaeFunctionUp$ carrier is a finite $\BHist$ packet
$$
T=(Q,D,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the rational-density input row, recording the finite rational sample by which the spike query is opened.
\item $D$ is the denominator spike ledger, recording the denominator bound and the spike height assigned to the visible rational input.
\item $S$ is the $\StreamNameUp$ window row used to inspect the finite name data around the input.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from that window.
\item $E$ is the terminal $\RealUp$ seal row that receives the rational-spike value as a Real-facing output.
\item $H$ records componentwise $\hsame$ transport for rational input, denominator ledger, stream window, regular-rational readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a rational-density input through $Q,D,S,R,E$.
\item $P$ is the $\Pkg$ provenance over $\ThomaeFunctionUp$, RationalDensityRealUp, $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\ContinuousMapUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\ThomaeFunctionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $Q,D,S,R,E,H,C,P,N$. It has no coordinate for host function extensionality, quotient real equality, selected rational enumerations, global continuity, or a classical discontinuity set.
\end{definition}

\begin{theorem}[Thomae function NameCert obligations]
\label{thm:thomae-function-namecert-obligations}
For every accepted $\ThomaeFunctionUp$ carrier
$$
T=(Q,D,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\ThomaeFunctionUp}$ surface consists exactly of carrier admission, rational-density input, denominator spike ledger stability, StreamName window stability, regular-rational readback, Real-facing seal stability, componentwise transport, displayed replay, provenance, and ledger non-escape through $Q,D,S,R,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:thomae-function-carrier}. The rational input row $Q$, denominator ledger $D$, stream window $S$, regular-rational readback $R$, Real seal $E$, and structural rows $H,C,P,N$ are the whole packet.

Rational-density input and denominator spike stability are read from $Q,D$. These rows record the finite input and spike height before any Real-facing consumer can inspect the output.

Readback stability is supplied by $S,R,E$. The stream window exposes the finite observations, the regular rational row replays them as readback data, and the terminal Real row seals the same finite route.

Finally $H,C,P,N$ transport, replay, source, and name the displayed rows. Since no further coordinate is present, host function extensionality, quotient real equality, selected rational enumerations, global continuity, and classical discontinuity sets remain outside the ledger.
\end{proof}

\begin{theorem}[Thomae function rational-spike locality]
\label{thm:thomae-function-rational-spike-locality}
Every rational-spike read of an accepted $\ThomaeFunctionUp$ carrier factors through RationalDensityRealUp, $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows before it exposes a value to a $\ContinuousMapUp$-facing consumer. The read may display the local denominator spike and the finite Real seal, but it cannot use host function extensionality, quotient real equality, a selected rational enumeration, or a global continuity theorem.
\end{theorem}
\begin{proof}
Let
$$
T=(Q,D,S,R,E,H,C,P,N)
$$
be an accepted carrier. The rational-density row $Q$ opens the finite input request, and the denominator ledger $D$ determines the visible spike height using only rational arithmetic recorded in the packet.

The stream and regular-rational rows $S,R$ then expose the finite window and readback route. The Real seal $E$ is downstream of the same rows, so a Real-facing consumer receives the spike value only after the denominator ledger and readback route have both been displayed.

The continuity-facing boundary can read the result only through the structural rows $H,C,P,N$. Therefore the route is local to the displayed rational-density, $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ data, and the excluded host and quotient principles never enter the packet.
\end{proof}

\closureat{ThomaeFunctionUp}{seedStr}

\begin{closurestatus}{\ThomaeFunctionUp}
  \constructivestory{A $\ThomaeFunctionUp$ packet is generated from a rational-density input row, denominator spike ledger, StreamName window, RegSeqRat readback, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:thomae-function-carrier}, \autoref{thm:thomae-function-namecert-obligations}, and \autoref{thm:thomae-function-rational-spike-locality} seed the finite rational-spike route from rational input to Real-facing readback.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host function extensionality, quotient real equality, selected rational enumeration, global continuity, classical discontinuity sets, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, rational-density input stability, denominator spike ledger stability, readback stability, Real-facing seal stability, transport, replay, provenance, and ledger non-escape for BEDC.Derived.ThomaeFunctionUp.}
\end{closurestatus}
