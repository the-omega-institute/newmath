\chapter{A Concrete Naming Certificate for $\BishopFixedPointIntervalUp$}
\label{ch:concrete-instances-bishop-fixed-point-interval-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopFixedPointIntervalUp$ packet records the constructive interval fixed-point route as finite NameCert data. It sits after the located interval source of \autoref{ch:concrete-instances-locatedinterval-namecert}, the bisection discipline of \autoref{ch:concrete-instances-intervalbisection-namecert}, the finite Cauchy-modulus rows used by \autoref{ch:concrete-instances-cauchymodulus-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite name windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the fixed-point certificate for a displayed interval self-map; it does not import a host compactness theorem, quotient real equality, choice-selected limit, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop fixed-point interval carrier]
\label{def:bishop-fixed-point-interval-carrier}
A $\BishopFixedPointIntervalUp$ carrier is a finite $\BHist$ packet
$$
F=(I,T,M,B,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the $\LocatedIntervalUp$ row naming the closed located interval, its endpoint reads, and its dyadic radius ledger.
\item $T$ is the contraction self-map row over $I$, recording that every admitted interval point is sent back into the same located interval.
\item $M$ is an explicit $\CauchyModulusUp$ or Lipschitz modulus row that turns each tolerance request into a finite contraction budget.
\item $B$ is the $\IntervalBisectionUp$ or nested located interval row that records the displayed shrinking interval chain.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the rational midpoint, endpoint, and radius observations.
\item $E$ is the terminal $\RealUp$ fixed-point seal reached only after the interval, contraction, modulus, bisection, and readback rows have been exposed.
\item $H$ records componentwise $\hsame$ transport for $I,T,M,B,R,E$ and for the structural rows.
\item $C$ records displayed $\Cont$ replay for interval requests, contraction reads, modulus reads, nested interval reads, rational readback, and real-seal requests.
\item $P$ is the $\Pkg$ provenance over $\BishopFixedPointIntervalUp$, $\LocatedIntervalUp$, $\IntervalBisectionUp$, $\CauchyModulusUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopFixedPointIntervalUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,T,M,B,R,E,H,C,P,N$. It has no coordinate for a host fixed-point theorem, quotient completion, selected limit, trichotomy, countable choice, or ambient compactness principle.
\end{definition}
\leanchecked{BEDC.Derived.BishopFixedPointIntervalUp.BishopFixedPointIntervalTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Bishop fixed-point interval NameCert obligations]
\label{thm:bishop-fixed-point-interval-namecert-obligations}
For every accepted $\BishopFixedPointIntervalUp$ carrier
$$
F=(I,T,M,B,R,E,H,C,P,N),
$$
the local $\NameCert_{\BishopFixedPointIntervalUp}$ surface consists exactly of carrier admission for the located interval row $I$, contraction self-map row $T$, explicit modulus row $M$, nested bisection row $B$, regular rational readback row $R$, terminal real seal $E$, componentwise transport row $H$, displayed replay row $C$, provenance row $P$, and local naming row $N$. Every fixed-point consumer must read these rows in the displayed order and cannot replace them by host interval compactness, quotient real equality, a selected limit, or an unrecorded convergence principle.
\end{theorem}
\begin{proof}
Carrier admission is the finite packet of \autoref{def:bishop-fixed-point-interval-carrier}. The rows $I,T,M,B,R,E,H,C,P,N$ are the only coordinates available to the local certificate.

The positive interval route is then forced. The interval source is $I$, the self-map evidence is $T$, the contraction budget is $M$, and the nested interval chain is $B$; each row is compared only through the componentwise transport row $H$ and replayed only through $C$.

The readback and seal are downstream. Rational observations pass through $R$ and the dyadic and stream-name data named in $P$ before the terminal $\RealUp$ row $E$ is exposed. Since no host compactness, quotient equality, selected limit, or external convergence coordinate occurs in the carrier, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Bishop fixed-point interval Cauchy-modulus route]
\label{thm:bishop-fixed-point-interval-cauchy-modulus-route}
Let $F=(I,T,M,B,R,E,H,C,P,N)$ be an accepted $\BishopFixedPointIntervalUp$ packet. Each tolerance request for the fixed-point name is answered by first reading the $\LocatedIntervalUp$ row $I$, then the contraction self-map row $T$, then the explicit $\CauchyModulusUp$ or Lipschitz row $M$, and only then the $\IntervalBisectionUp$ or nested located interval row $B$. The resulting finite observations are read through $\StreamNameUp$, $\DyadicRatCoreUp$, and $\RegSeqRatUp$ as recorded by $P$, so no consumer can infer convergence from an unbounded host iteration or a choice-selected subsequence.
\end{theorem}
\begin{proof}
Start with a tolerance request. The packet has no direct convergence row, so the request must project $I$ to know the interval source and $T$ to know that the displayed map remains inside the interval.

Next read the modulus row $M$. This row converts the tolerance into a finite contraction or Lipschitz budget. The nested row $B$ can then expose only the finite bisection or located-interval observations licensed by that budget.

Finally use the readback rows listed in $P$. Stream windows, dyadic tolerances, and regular rational observations feed $R$, and $H,C,N$ transport and name the same route. Thus the Cauchy route is explicit and finite, with no unbounded host iteration or selected subsequence.
\end{proof}

\begin{theorem}[Bishop fixed-point interval real-seal nonescape]
\label{thm:bishop-fixed-point-interval-real-seal-nonescape}
For every accepted $\BishopFixedPointIntervalUp$ packet, the terminal row $E$ is exposed only as the $\RealUp$ seal of the displayed nested interval and Cauchy-modulus route
$$
I,T,M,B,R \longmapsto E \longmapsto H,C,P,N.
$$
The seal may be consumed as the named fixed-point endpoint of the packet, but it cannot escape as quotient real equality, a proof of ambient $\RealUp$ completeness, a host fixed-point theorem, an unrestricted interval compactness principle, or a selected representative outside the displayed rows.
\end{theorem}
\begin{proof}
Read $E$ from the carrier. It is terminal in the packet: the interval, self-map, modulus, nested interval, and rational readback rows precede it, and the structural rows only transport, replay, source, and name the resulting read.

Apply \autoref{thm:bishop-fixed-point-interval-cauchy-modulus-route}. Every tolerance request reaching $E$ has already passed through the finite interval, contraction, modulus, bisection, stream, dyadic, and regular rational observations recorded by $I,T,M,B,R,P$.

It remains to rule out escape. The carrier has no coordinate for quotient real equality, ambient completeness, a host fixed-point theorem, unrestricted compactness, or selected representatives. Therefore $E$ seals only the displayed fixed-point name of this packet.
\end{proof}

\falsifiablePrediction{An accepted $\BishopFixedPointIntervalUp$ consumer that reaches the terminal $\RealUp$ row $E$ without first reading $I,T,M,B,R,H,C,P,N$ refutes the carrier surface.}

\independenceWitness{locatedinterval, intervalbisection, cauchymodulus, regseqrat, streamname, dyadicratcore, real: no listed sibling supplies the full interval self-map packet $F=(I,T,M,B,R,E,H,C,P,N)$, because each sibling contributes only a located interval source, a bisection route, a modulus row, a readback row, a finite name window, a dyadic ledger, or a terminal real seal while this chapter binds them into one fixed-point NameCert surface.}

\closureat{BishopFixedPointIntervalUp}{seedStr}

\begin{closurestatus}{\BishopFixedPointIntervalUp}
  \constructivestory{A $\BishopFixedPointIntervalUp$ packet is generated from a located interval row, contraction self-map row, explicit Cauchy or Lipschitz modulus row, nested bisection row, regular rational readback row, terminal Real seal, componentwise transport, displayed replay, provenance, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-fixed-point-interval-carrier}, \autoref{thm:bishop-fixed-point-interval-namecert-obligations}, \autoref{thm:bishop-fixed-point-interval-cauchy-modulus-route}, and \autoref{thm:bishop-fixed-point-interval-real-seal-nonescape} seed the finite constructive interval fixed-point route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host fixed-point compactness, quotient real equality, selected limits, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, contraction self-map admission, explicit modulus routing, nested interval readback, terminal Real seal nonescape, and a Lean carrier/obligation formalization for BEDC.Derived.BishopFixedPointIntervalUp.}
\end{closurestatus}
