\chapter{A Concrete Naming Certificate for $\RegularCauchyVariationBoundUp$}
\label{ch:concrete-instances-regular-cauchy-variation-bound-namecert}
\origin{human}

Following Jordan's finite variation ledgers and the Bishop--Bridges treatment of regular Cauchy real names, the $\RegularCauchyVariationBoundUp$ packet records a displayed variation budget for a regular rational Cauchy stream before the terminal real-name seal is consumed. It sits after the regular sequence surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window interface of \autoref{ch:concrete-instances-streamname-namecert}, the Cauchy modulus route of \autoref{ch:concrete-instances-cauchymodulus-namecert}, and the real-name boundary of \autoref{ch:concrete-instances-real-namecert}. The packet names only a finite variation-budget route; it does not import host sequence equality, a selected limit representative, quotient real equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy variation-bound carrier]
\label{def:regular-cauchy-variation-bound-carrier}
A $\RegularCauchyVariationBoundUp$ carrier is a finite $\BHist$ packet
$$
V=(S,Q,D,B,M,E,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ row of finite windows inspected from the regular Cauchy name.
\item $Q$ is the $\RegSeqRatUp$ row reading the same windows as regular rational observations.
\item $D$ is the $\DyadicRatCoreUp$ row of adjacent dyadic difference cells and finite partial variation sums.
\item $B$ is the displayed dyadic variation-budget row bounding every inspected partial variation sum.
\item $M$ is the $\CauchyModulusUp$ handoff row extracted from the displayed variation budget and regularity data.
\item $E$ is the $\RealUp$ seal reached only after $S,Q,D,B,M$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the window, readback, dyadic, budget, modulus, seal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from stream windows to regular rational readbacks, dyadic variation sums, the modulus handoff, and the terminal seal.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyVariationBoundUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchyModulusUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RegularCauchyVariationBoundUp}$ row.
\end{enumerate}
The classifier compares carriers only through $S,Q,D,B,M,E,H,C,P,N$. It has no coordinate for host sequence equality, quotient real equality, a chosen limit representative, unbounded variation search, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[Regular Cauchy variation-bound NameCert obligations]
\label{thm:regular-cauchy-variation-bound-namecert-obligations}
For every accepted $\RegularCauchyVariationBoundUp$ carrier $V=(S,Q,D,B,M,E,H,C,P,N)$, the local $\NameCert_{\RegularCauchyVariationBoundUp}$ surface consists exactly of stream-window exposure, regular rational readback, dyadic adjacent-difference exposure, finite variation-budget exposure, Cauchy-modulus handoff, real-seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-variation-bound-carrier}. The rows $S,Q,D,B,M,E$ are the only mathematical rows visible before the structural transport, replay, provenance, and naming rows.

The stream and regular rational rows determine which finite observations are being compared. The dyadic row $D$ records adjacent differences and finite partial variation sums, while $B$ records the displayed budget bounding those sums.

The modulus row $M$ is downstream of the same data, and the real seal $E$ is downstream of $M$. The rows $H,C,P,N$ preserve, replay, source, and name that finite route.

Since the carrier has no host equality, quotient equality, selected-limit, unbounded-search, choice, or ambient-completeness coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Regular Cauchy variation-bound Cauchy handoff]
\label{thm:regular-cauchy-variation-bound-cauchy-handoff}
Every $\CauchyModulusUp$ consumer of an accepted $\RegularCauchyVariationBoundUp$ packet must first read the route
$$
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \CauchyModulusUp
$$
through the same displayed rows $S,Q,D,B,M,H,C,P,N$. The handoff is a finite tail-control obligation extracted from the variation budget, not a selected limit, quotient equality of names, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:regular-cauchy-variation-bound-namecert-obligations}. It places stream windows, regular rational readbacks, dyadic adjacent-difference rows, and the displayed variation budget before the modulus row.

The row $M$ is therefore read only after $S,Q,D,B$ have been displayed. The replay row $C$ records the same order, and the transport row $H$ keeps the handoff componentwise stable.

The rows $P,N$ source and name the route. Because no carrier row stores a chosen limit, quotient equality, or ambient completeness claim, the consumer receives only the finite Cauchy-modulus handoff.
\end{proof}

\begin{theorem}[Regular Cauchy variation-bound Real seal]
\label{thm:regular-cauchy-variation-bound-real-seal}
Every $\RealUp$ consumer of an accepted $\RegularCauchyVariationBoundUp$ packet must read the stream windows, regular rational readbacks, dyadic variation sums, displayed variation budget, and $\CauchyModulusUp$ handoff before the seal row $E$ is available. Thus the real-facing boundary is the finite variation-budget packet, not host real equality, quotient real equality, selected representatives, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier $V=(S,Q,D,B,M,E,H,C,P,N)$. The seal row $E$ appears only after the stream, regular rational, dyadic, budget, and modulus rows in the displayed packet.

Apply \autoref{thm:regular-cauchy-variation-bound-cauchy-handoff}. A completion-facing read of the modulus already factors through the finite variation-budget route before it can reach a real-name consumer.

Finally inspect $H,C,P,N$. They transport, replay, source, and name the same route without adding another real equality or completion coordinate. The stated exclusions are therefore unavailable inside the accepted packet.
\end{proof}

\falsifiablePrediction{A $\RegularCauchyVariationBoundUp$ consumer is refuted if it reaches a $\CauchyModulusUp$ handoff or $\RealUp$ seal without displaying the stream windows, regular rational readback, dyadic adjacent-difference ledger, finite variation budget, transport, replay, provenance, and local $\NameCert$ rows.}

\independenceWitness{regseqrat, dyadicratcore, streamname, cauchymodulus, real: no listed sibling is bijective to $\RegularCauchyVariationBoundUp$ because this packet jointly binds finite windows, regular rational observations, dyadic variation sums, a displayed budget, Cauchy-modulus handoff, Real seal, transport, replay, provenance, and local naming.}

\closureat{RegularCauchyVariationBoundUp}{seedStr}

\begin{closurestatus}{\RegularCauchyVariationBoundUp}
  \constructivestory{A $\RegularCauchyVariationBoundUp$ packet is generated from StreamName finite windows, RegSeqRat regular rational readbacks, DyadicRatCore adjacent-difference ledgers, a displayed dyadic variation budget, a CauchyModulus handoff, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-variation-bound-carrier}, \autoref{thm:regular-cauchy-variation-bound-namecert-obligations}, \autoref{thm:regular-cauchy-variation-bound-cauchy-handoff}, and \autoref{thm:regular-cauchy-variation-bound-real-seal} seed the finite variation-budget route over RegSeqRat, DyadicRatCore, StreamName, CauchyModulus, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host sequence equality, quotient real equality, selected limit representatives, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, variation-budget exactness, Cauchy-modulus handoff, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyVariationBoundUp.}
\end{closurestatus}
