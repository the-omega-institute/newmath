\chapter{A Concrete Naming Certificate for $\HyperbolicVisualFlowShadowUp$}
\label{ch:concrete-instances-hyperbolic-visual-flow-shadow-namecert}
\origin{ai}

The $\HyperbolicVisualFlowShadowUp$ packet realizes the joint visual-metric, geodesic-flow, boundary-shadow, observer-expansion, and Real-seal route of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It sits after the visual-metric packet of \autoref{ch:concrete-instances-hyperbolic-visual-metric-namecert}, the geodesic-flow packet of \autoref{ch:concrete-instances-hyperbolic-geodesic-flow-namecert}, the boundary-shadow route of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the observer-expansion packet of \autoref{ch:concrete-instances-hyperbolic-observer-expansion-namecert}, and the terminal scalar seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite visual-flow shadow surface; it is not a quotient boundary, a chosen geodesic ray, a global hyperbolic model, a physical observer, or an ambient Real-completeness claim.

\begin{definition}[Hyperbolic visual-flow shadow carrier]
\label{def:hyperbolic-visual-flow-shadow-carrier}
A $\HyperbolicVisualFlowShadowUp$ carrier is a finite $\BHist$ packet
$$
S=(M,F,B,O,R,T,Q,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\HyperbolicVisualMetricUp$ row, recording the visible metric comparison and basepoint window under inspection.
\item $F$ is the $\HyperbolicGeodesicFlowUp$ row, storing the finite flow segment and its endpoint ledger.
\item $B$ is the $\HyperbolicBoundaryShadowUp$ row, exposing only the displayed boundary-shadow witness attached to the same metric and flow window.
\item $O$ is the $\HyperbolicObserverExpansionUp$ row, recording the observer-expansion readout that consumes the visual metric, flow, and boundary-shadow data.
\item $R$ is the $\RealUp$ seal for the visual-flow comparison and the expansion budget.
\item $T$ stores componentwise $\hsame$ transport for visual metric, geodesic-flow, boundary-shadow, observer-expansion, Real-seal, replay, provenance, and local naming rows.
\item $Q$ stores displayed $\Cont$ replay from a visual-flow request through $M,F,B,O,R,T$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicVisualFlowShadowUp$, $\HyperbolicVisualMetricUp$, $\HyperbolicGeodesicFlowUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicObserverExpansionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicVisualFlowShadowUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a quotient ideal boundary, selected global ray, completed hyperbolic model, physical observer, or ambient Real completion theorem.
\end{definition}

\begin{theorem}[Hyperbolic visual-flow shadow obligation]
\label{thm:hyperbolic-visual-flow-shadow-obligation}
For every accepted $\HyperbolicVisualFlowShadowUp$ carrier $S=(M,F,B,O,R,T,Q,P,N)$, the local $\NameCert_{\HyperbolicVisualFlowShadowUp}$ surface consists exactly of visual-metric admission, geodesic-flow exposure, boundary-shadow exposure, observer-expansion exposure, Real-seal ordering, componentwise transport, displayed replay, provenance, local naming, and nonescape for quotient boundaries, selected rays, global hyperbolic models, physical observers, and ambient Real completeness.
\end{theorem}
\begin{proof}
Project \autoref{def:hyperbolic-visual-flow-shadow-carrier}. The metric, flow, boundary, observer, and Real rows are the only analytic coordinates in the carrier.

The visual route starts with $M$ and $F$. The boundary-shadow row $B$ is accepted only as the displayed shadow attached to that same visual metric and finite geodesic-flow segment, so it cannot introduce an independent quotient boundary or selected ray.

The observer-expansion and Real-facing rows are downstream. Row $O$ reads the joint visual metric, flow, and shadow surface, while $R$ seals only the displayed comparison. The rows $T,Q,P,N$ transport, replay, source, and name the same finite packet, so no global hyperbolic model, physical observer, or ambient Real-completeness theorem can be exported.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicVisualFlowShadowUp$ consumer must display visual-metric, geodesic-flow, boundary-shadow, observer-expansion, Real-seal, transport, replay, provenance, and local naming rows before reading a visual-flow shadow.}

\independenceWitness{hyperbolic-visual-metric, hyperbolic-geodesic-flow, hyperbolic-boundary-shadow, hyperbolic-observer-expansion: $\HyperbolicVisualFlowShadowUp$ is not bijective to any listed sibling because it jointly carries visual metric, geodesic flow, boundary shadow, observer expansion, Real sealing, structural replay, provenance, and local naming.}

The later Lean TasteGate route for \texttt{BEDC.Derived.HyperbolicVisualFlowShadowUp.TasteGate} must include $\mathsf{Nontrivial}\ \HyperbolicVisualFlowShadowUp$ and $\mathsf{FieldFaithful}\ \HyperbolicVisualFlowShadowUp$ instances preserving $M,F,B,O,R,T,Q,P,N$ as separate displayed fields.

\closureat{HyperbolicVisualFlowShadowUp}{seedStr}

\begin{closurestatus}{\HyperbolicVisualFlowShadowUp}
  \constructivestory{A $\HyperbolicVisualFlowShadowUp$ packet is generated from a HyperbolicVisualMetricUp row, a HyperbolicGeodesicFlowUp row, a HyperbolicBoundaryShadowUp row, a HyperbolicObserverExpansionUp row, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-visual-flow-shadow-carrier} and \autoref{thm:hyperbolic-visual-flow-shadow-obligation} seed the finite visual-flow shadow packet over visual metric, geodesic-flow, boundary-shadow, observer-expansion, and Real-seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient ideal boundaries, selected global rays, physical observer semantics, global hyperbolic models, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, visual-metric exposure, geodesic-flow exposure, boundary-shadow exposure, observer-expansion exposure, Real-seal ordering, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicVisualFlowShadowUp.}
\end{closurestatus}
