\chapter{A Concrete Naming Certificate for $\BishopBarInductionModulusUp$}
\label{ch:concrete-instances-bishop-bar-induction-modulus-namecert}
\origin{human}

Following Brouwer's barred-spread analysis, Troelstra--van Dalen's treatment of bar induction, and Bishop--Bridges constructive Real practice, the $\BishopBarInductionModulusUp$ packet records the finite modulus surface that Real and Completion consumers may read from a barred spread. It sits after the barred-spread packet of \autoref{ch:concrete-instances-brouwer-bar-induction-namecert}, the finite fan surface of \autoref{ch:concrete-instances-fantheorem-namecert}, the finite stream-window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a displayed modulus handoff; it is not countable choice, open-cover compactness, quotient stream equality, selected branch data, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop bar induction modulus carrier]
\label{def:bishop-bar-induction-modulus-carrier}
A $\BishopBarInductionModulusUp$ carrier is a finite barred-spread modulus packet
$$
K=(B,T,I,W,R,M,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the barred-spread row inherited from $\BrouwerBarInductionUp$. It records the finite bar decisions, monotone coverage, and local backwards induction replay visible in the current prefix tree.
\item $T$ is the finite tree-depth ledger. It records the displayed root, visible extensions, retained prefix levels, and the finite fan boundary consumed by $\FanTheoremUp$.
\item $I$ is the induction replay row. It records the local step converting displayed barred children into the displayed parent prefix, without selecting an infinite branch.
\item $W$ is the $\StreamNameUp$ window row. It schedules the finite prefix observations requested by Real and Completion consumers.
\item $R$ is the $\RegSeqRatUp$ readback row attached to those windows, including the rational tolerances used to read the visible prefix depth.
\item $M$ is the modulus output row. It records the displayed natural bound after which every accepted window read is determined by the barred-spread replay.
\item $E$ is the terminal $\RealUp$ seal row reached only after $B,T,I,W,R,M$ have been displayed.
\item $H$ stores componentwise $\hsame$ transport for barred-spread data, tree-depth ledgers, induction replay, StreamName windows, RegSeqRat readback, modulus output, Real sealing, continuation, provenance, and local naming.
\item $C$ stores displayed $\Cont$ replay from any finite-window request through the same $B,T,I,W,R,M,E,H,P,N$ route.
\item $P$ is the $\Pkg$ provenance over $\BishopBarInductionModulusUp$, $\BrouwerBarInductionUp$, $\FanTheoremUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopBarInductionModulusUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $B,T,I,W,R,M,E,H,C,P,N$. It has no coordinate for countable choice, arbitrary open covers, quotient stream equality, a selected infinite branch, a host continuity theorem, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop bar induction modulus NameCert obligations]
\label{thm:bishop-bar-induction-modulus-namecert-obligations}
For every accepted $\BishopBarInductionModulusUp$ carrier
$$
K=(B,T,I,W,R,M,E,H,C,P,N),
$$
the local $\NameCert_{\BishopBarInductionModulusUp}$ surface consists exactly of the following finite rows:
\begin{enumerate}
\item carrier admission for barred-spread data, finite tree-depth ledgers, induction replay, StreamName windows, RegSeqRat readback, modulus output, Real sealing, transport, continuation, provenance, and local naming;
\item barred-spread ancestry, requiring every modulus read to expose the $\BrouwerBarInductionUp$ row $B$ before using $M$;
\item fan-boundary exactness, requiring the displayed depth ledger $T$ to be the only finite fan surface used in the modulus computation;
\item readback ordering, requiring every $\RegSeqRatUp$ read to pass through the $\StreamNameUp$ window row before reaching $M$ and $E$;
\item analytic nonescape, requiring every Real or Completion-facing consumer to use the route $B,T,I,W,R,M,E,H,C,P,N$ and no hidden choice, cover, branch-selection, quotient, or ambient-completeness coordinate.
\end{enumerate}
No local obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-bar-induction-modulus-carrier}. The carrier admission row is exactly the tuple $B,T,I,W,R,M,E,H,C,P,N$, so the first obligation is a coordinate projection.

The barred-spread ancestry clause is forced by the placement of $M$ after $B$ and $I$. A consumer cannot read a modulus output without first displaying the bar decisions, monotone coverage, and local backwards replay already named by $\BrouwerBarInductionUp$.

The fan-boundary exactness clause is the role of $T$. The packet records only a finite depth ledger and visible prefix levels; it does not add an infinite branch or a separate compactness principle.

For readback ordering, the StreamName row $W$ schedules the requested finite observations, the RegSeqRat row $R$ reads them with rational tolerances, and only then does the modulus row $M$ report the displayed bound. The Real seal $E$ is downstream of that same route.

Finally $H,C,P,N$ transport, replay, package, and name no further data. Since hidden choice, arbitrary covers, selected branches, quotient stream equality, and ambient Real completeness are not carrier coordinates, they cannot be local NameCert obligations.
\end{proof}

\begin{theorem}[Bishop bar induction modulus window boundary]
\label{thm:bishop-bar-induction-modulus-window-boundary}
Every $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, or Completion-facing read of an accepted $\BishopBarInductionModulusUp$ carrier factors through the displayed finite route
$$
\BrouwerBarInductionUp,\FanTheoremUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \BishopBarInductionModulusUp \longmapsto \RealUp .
$$
Equivalently, the exported modulus is determined by the rows $B,T,I,W,R,M,E,H,C,P,N$ and cannot expose countable choice, open-cover compactness, quotient stream equality, selected infinite branches, host continuity, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with an analytic-facing read of $K$. The only window interface in the carrier is $W$, so the request must first become a displayed finite $\StreamNameUp$ observation.

The readback row $R$ is attached to that same finite observation. It supplies regular rational tolerances for the visible prefix depth, not a quotient stream representative or a completed branch.

The modulus row $M$ is downstream of $B,T,I,W,R$. Thus its bound is read only after the barred-spread row, finite fan-depth ledger, induction replay, window schedule, and rational readback have all been displayed.

The terminal seal $E$ can therefore carry only the same finite route to Real or Completion consumers. Componentwise transport, continuation replay, provenance, and local naming preserve this route through $H,C,P,N$, and the carrier has no additional coordinate from which the excluded principles could be recovered.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopBarInductionModulusUp$ Real or Completion-facing read exposes barred-spread data, a finite tree-depth ledger, induction replay, StreamName windows, RegSeqRat readback, a modulus output, a Real seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming before any modulus boundary is visible.}

\independenceWitness{brouwer-bar-induction, fantheorem, streamname, regseqrat, real: $\BishopBarInductionModulusUp$ is not bijective to any listed sibling because it jointly carries barred-spread ancestry, finite fan-depth data, induction replay, finite windows, regular rational readback, a modulus output, Real sealing, transport, replay, provenance, and local naming.}

\closureat{BishopBarInductionModulusUp}{seedStr}

\begin{closurestatus}{\BishopBarInductionModulusUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-bar-induction-modulus-carrier}, \autoref{thm:bishop-bar-induction-modulus-namecert-obligations}, and \autoref{thm:bishop-bar-induction-modulus-window-boundary} seed the finite barred-spread modulus route from Brouwer bar induction and fan-depth rows through StreamName windows, RegSeqRat readback, terminal Real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close countable choice, open-cover compactness, quotient stream equality, selected infinite branches, host continuity, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, barred-spread ancestry, fan-boundary exactness, readback ordering, modulus-window boundary, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BishopBarInductionModulusUp.}
  \constructivestory{A $\BishopBarInductionModulusUp$ packet is generated from BrouwerBarInductionUp barred-spread rows, FanTheoremUp finite depth ledgers, induction replay, StreamName windows, RegSeqRat readback, a displayed modulus output, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
