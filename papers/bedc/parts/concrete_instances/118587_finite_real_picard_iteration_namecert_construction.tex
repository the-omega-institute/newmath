\chapter{A Concrete Naming Certificate for $\FiniteRealPicardIterationUp$}
\label{ch:concrete-instances-finite-real-picard-iteration-namecert}
\origin{human}

Following Picard and Bishop--Bridges constructive metric iteration, the $\FiniteRealPicardIterationUp$ packet records the finite Real-facing Picard window used before a contraction, completion, or fixed-point consumer may read a terminal real-name seal. It sits over the broader Picard iteration surface of \autoref{ch:concrete-instances-picard-iteration-namecert}, the contraction-real modulus row of \autoref{ch:concrete-instances-contraction-real-modulus-namecert}, the finite completion handoff of \autoref{ch:concrete-instances-metriccompletionfinite-namecert}, the Bishop comparison route of \autoref{ch:concrete-instances-bishopcompletioncomparison-namecert}, and the real-completion spine of \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The packet is a finite iteration certificate; it is not a proof of Banach fixed-point existence, quotient equality of Picard streams, a selected limit, countable choice, or ambient completeness of $\RealUp$.

\begin{definition}[FiniteRealPicardIteration carrier]
\label{def:finite-real-picard-iteration-carrier}
A $\FiniteRealPicardIterationUp$ carrier is a finite $\BHist$ packet
$$
P=(X,F,D,W,S,R,E,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the source $\RealUp$ seal row naming the initial real or real-window datum from which the finite Picard run starts.
\item $F$ is the contractive self-map row, carrying the displayed graph reads and the contraction witness consumed by $\ContractionRealModulusUp$ or the Picard sibling route.
\item $D$ is the $\DyadicRatCoreUp$ contraction budget row, recording the dyadic radius, contraction slack, and finite request schedule used for the window.
\item $W$ is the finite iterate-window row. It lists the displayed iterates from the source row through the chosen finite height and records the successor reads of $F$.
\item $S$ is the $\StreamNameUp$ observation row for the source, iterate, and terminal-name windows opened by $W$.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the same observations and dyadic budgets.
\item $E$ is the terminal $\RealUp$ seal row reached only after $X,F,D,W,S,R$ have all been displayed.
\item $H$ records componentwise $\hsame$ transport for the source seal, contractive map, dyadic budget, iterate window, stream observations, regular readback, terminal seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a Picard-window request through
$$
X,F,D,W,S \longmapsto R \longmapsto E \longmapsto H,Q,N .
$$
\item $Q$ is the $\Pkg$ provenance over $\FiniteRealPicardIterationUp$, $\PicardIterationUp$, $\ContractionRealModulusUp$, $\MetricCompletionFiniteUp$, $\BishopCompletionComparisonUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteRealPicardIterationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,F,D,W,S,R,E,H,C,Q,N$. It has no coordinate for a completed Picard limit, fixed-point selection, quotient stream equality, countable-choice schedule, host function extensionality, ambient completeness of $\RealUp$, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[FiniteRealPicardIteration NameCert obligations]
\label{thm:finite-real-picard-iteration-namecert-obligations}
For every accepted $\FiniteRealPicardIterationUp$ carrier
$$
P=(X,F,D,W,S,R,E,H,C,Q,N),
$$
the local $\NameCert_{\FiniteRealPicardIterationUp}$ surface consists exactly of source Real-seal admission, contractive-map exposure, dyadic contraction-budget exposure, finite iterate-window exposure, stream-window readback, regular rational readback, terminal Real-seal nonescape, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier coordinates. The analytic rows are exactly the source seal $X$, contractive map row $F$, dyadic budget $D$, finite iterate window $W$, stream observations $S$, regular readback $R$, and terminal seal $E$.

The structural rows then preserve the same finite route. The transport row $H$ moves each coordinate componentwise, while $C$ replays only the displayed request path from the source seal through the iterate window and readback.

The provenance and local name rows $Q,N$ source and name this packet inside the cited Picard, contraction, completion, dyadic, stream, regular-readback, and Real chapters. Since no other coordinate is present, the listed rows exhaust the NameCert obligation surface and the excluded limit, fixed-point, quotient, choice, extensionality, completeness, verification, and bridge data have no export path.
\end{proof}

\begin{theorem}[FiniteRealPicardIteration contractive window]
\label{thm:finite-real-picard-iteration-contractive-window}
Every contraction-facing or completion-facing consumer of an accepted $\FiniteRealPicardIterationUp$ carrier must read the finite route
$$
X \longrightarrow F \longrightarrow D \longrightarrow W \longrightarrow S \longrightarrow R \longrightarrow E
$$
before it may consume the terminal $\RealUp$ seal. The route exports a finite Picard-window certificate only; it does not export a completed fixed point, quotient equality of iterate streams, countable choice, host equality of maps, or an ambient completeness theorem.
\end{theorem}
\begin{proof}
Start with the terminal seal request. The carrier definition places $E$ after the source seal, contractive map, dyadic budget, finite iterate window, stream observations, and regular rational readback.

The contraction data are finite. The row $F$ is read only against the displayed source row and the row $D$ supplies the dyadic contraction budget for the finite height recorded by $W$.

The Real-facing handoff is downstream. The rows $S$ and $R$ expose the observed finite windows and regular rational readback before $E$ is available, while $H,C,Q,N$ transport, replay, source, and name the same route.

Thus every consumer receives only the finite Picard-window certificate. The absent fixed point, quotient stream equality, choice schedule, host map equality, and ambient completeness theorem cannot be recovered from the displayed packet.
\end{proof}

\begin{theorem}[FiniteRealPicardIteration Real seal nonescape]
\label{thm:finite-real-picard-iteration-real-seal-nonescape}
In every accepted $\FiniteRealPicardIterationUp$ carrier, the terminal $\RealUp$ seal row $E$ is a seal for the displayed finite route
$$
X,F,D,W,S,R,H,C,Q,N .
$$
Any downstream Real or Completion consumer may cite $E$ only as the endpoint of that route, and cannot detach it into ambient $\RealUp$ completeness, host equality of completed reals, a selected Picard limit, or quotient equality of iterate streams.
\end{theorem}
\begin{proof}
Read $E$ from the carrier. It appears only after the finite Picard data $X,F,D,W,S,R$ and is transported only through the componentwise row $H$.

Replay is equally local. The continuation row $C$ repeats the same finite order of exposure, and the provenance and local naming rows $Q,N$ record no further Real object.

Therefore the seal is terminal for the displayed finite packet. Since the carrier contains no coordinate for completeness, host equality, selected limits, or quotient stream equality, no consumer can detach $E$ into one of those claims.
\end{proof}

\closureat{FiniteRealPicardIterationUp}{seedStr}

\begin{closurestatus}{\FiniteRealPicardIterationUp}
  \constructivestory{A $\FiniteRealPicardIterationUp$ packet is generated from a source Real seal, a contractive map row, a dyadic contraction budget, a finite Picard iterate window, StreamName observations, RegSeqRat readback, a terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-real-picard-iteration-carrier}, \autoref{thm:finite-real-picard-iteration-namecert-obligations}, \autoref{thm:finite-real-picard-iteration-contractive-window}, and \autoref{thm:finite-real-picard-iteration-real-seal-nonescape} seed the finite Picard-window route over Real, contraction, dyadic budget, StreamName, RegSeqRat, completion comparison, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Banach fixed-point existence, completed Picard limits, quotient equality of iterate streams, countable choice, host function extensionality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, contractive-map exposure, dyadic budget exactness, finite iterate-window replay, StreamName observation, RegSeqRat readback, Real-seal preservation, transport, replay, provenance, and local naming for BEDC.Derived.FiniteRealPicardIterationUp.}
\end{closurestatus}
