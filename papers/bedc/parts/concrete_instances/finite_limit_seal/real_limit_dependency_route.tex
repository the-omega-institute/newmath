\begin{theorem}[Finite limit seal real dependency route]
\label{thm:finite-limit-seal-real-dependency-route}
Every public $\FiniteLimitSealUp$ seal consumes its real-facing rows in the displayed dependency order
$$
\RealUp,\ \RegSeqRatUp,\ \StreamNameUp,\ \MetricCompletionUp
$$
only through the finite modulus, window, readback, tolerance, and completion-envelope rows of \autoref{def:finite-limit-seal-carrier}. A limit consumer must first read the $\StreamNameUp$ observation windows, then the $\RegSeqRatUp$ rational readback, then the $\RealUp$ seal and $\MetricCompletionUp$ envelope route before the local $\NameCert_{\FiniteLimitSealUp}$ row is public; it receives no selected limit, quotient stream equality, countable-choice schedule, host real equality, trichotomy branch, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Project the accepted carrier from \autoref{def:finite-limit-seal-carrier}. The only real-facing path is the finite route from the modulus schedule through stream windows, regular rational readback, dyadic tolerance, and terminal Real seal.

Apply \autoref{thm:finite-limit-seal-route-nonescape} and \autoref{thm:finite-limit-seal-category-consumer-route}. They force completion-facing consumers to remain behind the displayed $M,W,R,D,E$ rows before any categorical or completion envelope can read the seal.

The added dependency order records the same lattice edge: $\StreamNameUp$ supplies the finite windows, $\RegSeqRatUp$ supplies the rational readback, $\RealUp$ supplies the terminal seal, and $\MetricCompletionUp$ consumes that seal only through the completion route. Since the local packet has no coordinate for a selected limit or ambient completeness theorem, the dependency route is exactly the finite readback route already named by the carrier.
\end{proof}

\begin{theorem}[FiniteLimitSeal MetricCompletionFinite forward link]
\label{thm:finite-limit-seal-metriccompletionfinite-forward-link}
Every finite-limit seal consumed by a completion-facing route must expose its $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows before entering $\MetricCompletionFiniteUp$. The forward link into \autoref{ch:concrete-instances-metriccompletionfinite-namecert} is a finite readback route, not a selected limit, quotient stream equality, countable-choice schedule, host real equality, trichotomy branch, or ambient Real completeness theorem.
\end{theorem}
\begin{proof}
Start with the real dependency route above. It already forces a public $\FiniteLimitSealUp$ consumer to read finite stream windows, regular rational readback, terminal Real sealing, and the completion envelope before any local naming row is public.

Next project the structural rows of \autoref{def:finite-limit-seal-carrier}. The same route carries $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ data, so a completion-facing consumer cannot bypass the finite seal packet.

Finally compose this route with \autoref{ch:concrete-instances-metriccompletionfinite-namecert}. A $\MetricCompletionFiniteUp$ consumer therefore receives the finite-limit seal only after the StreamName, RegSeqRat, DyadicRatCore, Real, transport, replay, provenance, and naming rows are exposed, with no selected limit, quotient stream equality, countable-choice schedule, host real equality, trichotomy branch, or ambient Real completeness.
\end{proof}

\begin{theorem}[FiniteLimitSeal Real-completion route]
\label{thm:finite-limit-seal-real-completion-route}
Every accepted $\FiniteLimitSealUp$ completion-facing consumer factors through the route
$$
M,W,R,D,E,H,C,P,N.
$$
Here $W$ is supplied by $\StreamNameUp$, $R$ by $\RegSeqRatUp$, $D$ by $\DyadicRatCoreUp$, $E$ by $\RealUp$, and downstream completion is read only through $\MetricCompletionUp$ or $\MetricCompletionFiniteUp$. No selected limit, quotient stream equality, countable-choice schedule, host real equality, trichotomy branch, or ambient Real completeness enters the route.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-limit-seal-carrier}. The completion-facing rows available to a consumer are the displayed modulus, window, readback, dyadic tolerance, Real seal, transport, replay, provenance, and local naming rows.

Use route nonescape, \autoref{thm:finite-limit-seal-route-nonescape}. It prevents a completion consumer from bypassing $M,W,R,D,E$ or replacing them by a selected limit, quotient stream equality, host equality, trichotomy branch, or ambient completeness theorem.

Apply the real dependency route above. It identifies $W$ as the $\StreamNameUp$ window row, $R$ as the $\RegSeqRatUp$ readback row, $D$ as the dyadic tolerance row, and $E$ as the terminal $\RealUp$ seal.

Finally use the $\MetricCompletionFiniteUp$ forward link, \autoref{thm:finite-limit-seal-metriccompletionfinite-forward-link}. It records the finite completion handoff and leaves downstream completion only through $\MetricCompletionUp$ or $\MetricCompletionFiniteUp$, so the displayed route is exhausted by $M,W,R,D,E,H,C,P,N$.
\end{proof}

\begin{theorem}[FiniteLimitSeal PointwiseStreamMetric reverse route]
\label{thm:finite-limit-seal-pointwise-stream-metric-reverse-route}
Any $\PointwiseStreamMetricUp$ consumer entering $\FiniteLimitSealUp$ must expose $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance, $\RegSeqRatUp$ readback, $\RealUp$ seal, $\MetricSpaceUp$ row, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ before the finite seal is read. The route from \autoref{ch:concrete-instances-pointwise-stream-metric-namecert} is therefore the reverse completion-facing edge to the finite-limit seal routes above, not a selected limit, quotient stream equality, countable-choice schedule, host real equality, trichotomy branch, or ambient Real completeness theorem.
\end{theorem}
\begin{proof}
Project the completion-facing route of \autoref{thm:finite-limit-seal-real-completion-route}. It requires the finite seal to expose $M,W,R,D,E,H,C,P,N$ before downstream completion data can be read.

Now read the $\PointwiseStreamMetricUp$ consumer through \autoref{ch:concrete-instances-pointwise-stream-metric-namecert}. Its stream metric input already carries a displayed $\MetricSpaceUp$ row and pointwise StreamName observations, so entry into $\FiniteLimitSealUp$ must align those observations with the seal rows $W,R,D,E$.

Finally apply the real dependency route and the $\MetricCompletionFiniteUp$ forward link. They identify $W$ as the $\StreamNameUp$ window row, $R$ as the $\RegSeqRatUp$ readback row, $D$ as the $\DyadicRatCoreUp$ tolerance row, and $E$ as the $\RealUp$ seal, while $H,C,P,N$ keep transport, replay, provenance, and naming visible. Thus the reverse route contributes only the displayed finite rows and no selected limit, quotient stream equality, choice schedule, host equality, trichotomy branch, or ambient Real completeness.
\end{proof}
