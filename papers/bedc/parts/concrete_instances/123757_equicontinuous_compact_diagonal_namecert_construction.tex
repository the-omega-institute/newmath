\chapter{A Concrete Naming Certificate for $\EquicontinuousCompactDiagonalUp$}
\label{ch:concrete-instances-equicontinuous-compact-diagonal-namecert}
\origin{human}

Following Arzela--Ascoli and Bishop--Bridges constructive compact analysis, the $\EquicontinuousCompactDiagonalUp$ packet records the finite diagonal route by which an equicontinuous family over a compact metric source presents one Real-facing diagonal readback. It sits over the compact finite-net surface of \autoref{ch:concrete-instances-compactmetric-namecert}, the displayed map rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus output of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the finite observation interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite diagonal carrier; it does not rely on open-cover compactness, quotient equality of functions, countable choice, selected subsequences outside the displayed rows, or ambient $\RealUp$ completeness.

\begin{definition}[Equicontinuous compact diagonal carrier]
\label{def:equicontinuous-compact-diagonal-carrier}
An $\EquicontinuousCompactDiagonalUp$ carrier is a finite $\BHist$ packet
$$
D=(K,F,U,S,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net row, recording the inspected centers, mesh budget, and cell-membership evidence for the compact source.
\item $F$ is the $\ContinuousMapUp$ family row, naming the displayed maps and the source and target windows on which they are read.
\item $U$ is the $\UniformModulusUp$ row shared by the accepted family over the displayed compact-net cells.
\item $S$ is the finite diagonal schedule row, ordering the net centers and precision requests consumed by the diagonal readback.
\item $W$ is the $\StreamNameUp$ window row opened by the schedule $S$.
\item $R$ is the $\RegSeqRatUp$ regular rational readback row attached to the scheduled windows.
\item $E$ is the terminal $\RealUp$ seal exposed only after $K,F,U,S,W,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for compact nets, continuous-map rows, uniform moduli, diagonal schedules, stream windows, regular rational readback, Real seals, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from the compact request through
$$
K\longrightarrow F\longrightarrow U\longrightarrow S\longrightarrow W\longrightarrow R\longrightarrow E .
$$
\item $P$ is the $\Pkg$ provenance over $\EquicontinuousCompactDiagonalUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\EquicontinuousCompactDiagonalUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,F,U,S,W,R,E,H,C,P,N$. It has no coordinate for open-cover compactness, quotient function equality, countable choice, selected host subsequences, hidden limits, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Equicontinuous compact diagonal NameCert obligations]
\label{thm:equicontinuous-compact-diagonal-namecert-obligations}
For every accepted $\EquicontinuousCompactDiagonalUp$ carrier
$$
D=(K,F,U,S,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\EquicontinuousCompactDiagonalUp}$ surface consists exactly of carrier admission, compact finite-net exposure, continuous-map family exposure, shared uniform-modulus exposure, finite diagonal-schedule exactness, $\StreamNameUp$ window exposure, $\RegSeqRatUp$ readback, terminal $\RealUp$ sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the accepted carrier to the tuple of \autoref{def:equicontinuous-compact-diagonal-carrier}. The analytic rows before transport and naming are precisely $K,F,U,S,W,R,E$.

The compact row $K$ supplies only finite-net data. The continuous-map row $F$ names the displayed family, and the uniform-modulus row $U$ is the only row allowed to propagate observations from centers to compact cells.

The diagonal information is finite. The schedule $S$ orders the inspected centers and precision requests, $W$ opens the corresponding stream windows, and $R$ reads those windows as regular rational data before the terminal Real seal $E$ is available.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since no carrier coordinate stores an open-cover compactness theorem, function quotient, choice schedule, hidden limit, host subsequence, or ambient completeness principle, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Equicontinuous compact finite-subcover diagonal route]
\label{thm:equicontinuous-compact-diagonal-finite-subcover-route}
Every Real or Completion consumer of an accepted $\EquicontinuousCompactDiagonalUp$ packet factors through
$$
\CompactMetricUp
\longmapsto
\ContinuousMapUp
\longmapsto
\UniformModulusUp
\longmapsto
\StreamNameUp
\longmapsto
\RegSeqRatUp
\longmapsto
\RealUp .
$$
The finite subcover role is supplied only by the displayed compact-net centers in $K$ together with the shared modulus $U$ and diagonal schedule $S$; the route exports no open-cover compactness object, no quotient equality of functions, no countable-choice schedule, no selected host subsequence, and no ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the carrier $D=(K,F,U,S,W,R,E,H,C,P,N)$. The compact source is read through the finite-net row $K$, not through an open-cover object.

The family and modulus rows then fix the local-to-global propagation. The row $F$ supplies the displayed continuous maps, while $U$ is the shared $\UniformModulusUp$ row used on the compact-net cells.

The schedule row $S$ supplies the diagonal route. It opens only the finite $\StreamNameUp$ windows recorded in $W$, and $R$ converts those windows to $\RegSeqRatUp$ readback before $E$ is sealed as a $\RealUp$ row.

The structural rows $H,C,P,N$ preserve this ordering under transport, replay, provenance, and local naming. Thus every consumer reads a finite compact diagonal route and cannot project any excluded compactness, equality, choice, subsequence, or completeness principle from the packet.
\end{proof}

\falsifiablePrediction{Every accepted $\EquicontinuousCompactDiagonalUp$ Real-facing read displays one compact finite-net row, one continuous-map family row, one shared UniformModulus row, one finite diagonal schedule, StreamName windows, RegSeqRat readback, a RealUp seal, hsame transport, Cont replay, Pkg provenance, and a local NameCert row before exporting diagonal data.}

\independenceWitness{compactmetric, continuousmap, uniformmodulus, streamname, regseqrat, real: $\EquicontinuousCompactDiagonalUp$ is not bijective to any listed sibling because it jointly carries compact finite-net data, continuous-family rows, shared uniform-modulus data, a finite diagonal schedule, stream windows, regular rational readback, Real sealing, transport, replay, provenance, and local naming.}

\closureat{EquicontinuousCompactDiagonalUp}{seedStr}

\begin{closurestatus}{\EquicontinuousCompactDiagonalUp}
  \constructivestory{An $\EquicontinuousCompactDiagonalUp$ packet is generated from compact finite-net rows, continuous-map family rows, a shared uniform modulus, a finite diagonal schedule, StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:equicontinuous-compact-diagonal-carrier}, \autoref{thm:equicontinuous-compact-diagonal-namecert-obligations}, and \autoref{thm:equicontinuous-compact-diagonal-finite-subcover-route} seed the finite diagonal route from compact finite-net and equicontinuous modulus data to a Real-facing readback.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, quotient function equality, countable choice, selected host subsequences, hidden limits, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net exposure, continuous-map family exposure, uniform-modulus exposure, diagonal-schedule exactness, StreamName window exposure, RegSeqRat readback, RealUp seal non-escape, transport, replay, provenance, and local naming for BEDC.Derived.EquicontinuousCompactDiagonalUp.}
\end{closurestatus}
