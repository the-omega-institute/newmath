The $\CauchyCompletionMonadLawUp$ packet consumes its sibling completion lattice through displayed finite rows. The unit, multiplication, and composition reads are routed through \autoref{ch:concrete-instances-cauchy-namecert}, \autoref{ch:concrete-instances-metriccompletion-namecert}, \autoref{ch:concrete-instances-completionuniversalproperty-namecert}, and \autoref{ch:concrete-instances-regseqrat-namecert}: Cauchy rows name the input streams, MetricCompletion rows expose the completion carrier, the universal-property rows supply the comparison route, and RegSeqRat rows give the regular rational windows used by the readbacks. No step appeals to a host completion monad outside the displayed Cauchy completion and regular-rational certificate rows.
