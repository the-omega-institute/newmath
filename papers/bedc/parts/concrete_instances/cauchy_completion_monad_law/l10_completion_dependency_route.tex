\begin{lemma}[CauchyCompletionMonadLaw completion dependency route]
\label{lem:cauchy-completion-monad-law-l10-completion-dependency-route}
Every accepted $\CauchyCompletionMonadLawUp$ law read consumes the completion-route carrier of \autoref{ch:concrete-instances-constructive-metric-completion-namecert}, the criterion surface of \autoref{ch:concrete-instances-cauchycompletioncriterion-namecert}, and the finite $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ route before its unit and multiplication laws can be read as monad-law data. The packet therefore binds the unit, idempotence, bind, and associativity rows to the displayed constructive completion route rather than to quotient completion equality, selected limits, countable choice, trichotomy, host equality, or an ambient $\RealUp$ completeness principle.
\end{lemma}
\begin{proof}
Project the carrier of \autoref{def:cauchy-completion-monad-law-carrier}. Its law rows $M,U,I,B,S,D,R,H,C,P,N$ are completion-facing rows, so the source completion route must be visible before a client can read them as unit or multiplication data.

Read the constructive metric completion carrier and the Cauchy-completion criterion surface through the cited sibling chapters. They supply the completion admissibility and criterion rows consumed by the monad transformer before the law schedule is replayed.

Finally apply \autoref{thm:cauchy-completion-monad-law-namecert-obligations} and \autoref{thm:cauchy-completion-monad-law-unit-associativity-boundary}. These force the law read through the finite StreamName, RegSeqRat, and DyadicRatCore rows, leaving no coordinate for quotient completion equality, selected limits, countable choice, trichotomy, host equality, or ambient real completeness.
\end{proof}
