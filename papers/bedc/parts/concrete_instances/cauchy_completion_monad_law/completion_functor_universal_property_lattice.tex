The $\CauchyCompletionMonadLawUp$ packet reads the completion functor, universal-property, metric-completion, and Real-completion sibling rows before the monad multiplication and unit laws are available as law data. The completion functor route of \autoref{ch:concrete-instances-cauchycompletionfunctor-namecert} supplies the displayed functorial transformer, the universal-property route of \autoref{ch:concrete-instances-completionuniversalproperty-namecert} supplies the extension and uniqueness rows, the metric-completion packet of \autoref{ch:concrete-instances-metriccompletion-namecert} supplies the completion carrier, and the Real-completion handoff of \autoref{ch:concrete-instances-real-cauchy-completion-namecert} supplies the terminal completion-facing seal. Only after those rows are visible may the law packet expose multiplication, unit, idempotence, and associativity through its local carrier.
