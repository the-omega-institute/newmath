\begin{theorem}[CauchyCompletionMonadLaw functor forward anchor]
\label{thm:cauchy-completion-monad-law-functor-forward-anchor}
Every public monad-law read from an accepted $\CauchyCompletionMonadLawUp$ packet factors through the $\CauchyCompletionFunctorUp$ dependency route before unit, bind, idempotence, or associativity rows are exposed. The consumer must first read the functor dependency route of \autoref{thm:cauchy-completion-monad-law-functor-dependency-route} and the local NameCert construction of \autoref{thm:cauchy-completion-monad-law-namecert-obligations}; only then may it inspect $M,U,I,B,S,D,R,H,C,P,N$ as law rows. The route is a forward anchor into the public functorial completion surface, not a quotient-completion equality, selected representative, selected limit, countable-choice principle, or ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
Read the functor dependency route first. The theorem \autoref{thm:cauchy-completion-monad-law-functor-dependency-route} places the $\CauchyCompletionFunctorUp$ source-metric, regular-Cauchy, completion-seal, unit-and-bind, universal-property, classifier, transport, provenance, and local naming rows before any monad-law consumer reads law data.

Then read the local construction through \autoref{thm:cauchy-completion-monad-law-namecert-obligations}. It exposes $M,U,I,B,S,D,R,H,C,P,N$ as the finite monad, unit, idempotence, bind, stream, dyadic, regular-rational, transport, replay, provenance, and local naming surface.

With both reads in place, the public law route may expose unit, bind, idempotence, and associativity as rows attached to the functorial completion dependency. Since the carrier has no quotient-completion, selected-representative, selected-limit, countable-choice, or ambient-completeness coordinate, the anchor is only the displayed forward dependency.
\end{proof}
