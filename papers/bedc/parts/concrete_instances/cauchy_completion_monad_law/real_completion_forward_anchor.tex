The $\CauchyCompletionMonadLawUp$ route consumes the Real and Completion siblings before any unit or associativity row is exported: $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ provide the L10 real-name readback through \autoref{ch:concrete-instances-real-namecert}, while $\CauchyCompletionFunctorUp$ and $\CompletionUniversalPropertyUp$ provide the completion-facing handoff through \autoref{ch:concrete-instances-completionuniversalproperty-namecert}. Thus a monad-law consumer sees the finite real-name route and the completion universal-property route as displayed dependencies, not as hidden quotient completion data.
