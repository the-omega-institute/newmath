\begin{theorem}[Cauchy completion monad law completion dependency lattice]
\label{thm:cauchy-completion-monad-law-sibling-route}
Every accepted $\CauchyCompletionMonadLawUp$ packet consumes its completion-facing law rows through the displayed sibling lattice: $\MetricCompletionUp$ supplies the completion carrier of \autoref{ch:concrete-instances-metriccompletion-namecert}, $\CompletionUp$ and the $\RealUp$ completion seal rows supply the real-name boundary of \autoref{ch:concrete-instances-real-namecert}, and $\CauchyCompletionFunctorUp$ supplies the functorial route before unit, bind, multiplication, or associativity data are exported. The route is a finite dependency projection; it is not a quotient-completion equality, selected representative, selected limit, host equality of completed objects, countable-choice principle, or ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
Project the monad-law carrier and the existing sibling routes. The law rows are already carried through the Cauchy completion monad, unit, bind, stream, dyadic, regular-rational, transport, replay, provenance, and local naming coordinates before any Real-facing read is available.

Read the metric-completion boundary next. The $\MetricCompletionUp$ sibling fixes the completion carrier as a displayed route through \autoref{ch:concrete-instances-metriccompletion-namecert}, so the monad-law packet cannot obtain a hidden host completion object.

Finally compose with the Real-facing route and the functor dependency route. The $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert} is reached only after the $\StreamNameUp$, $\DyadicRatCoreUp$, and $\RegSeqRatUp$ rows, and the $\CauchyCompletionFunctorUp$ route is consumed before unit, bind, multiplication, or associativity rows are exported. Thus the dependency lattice is horizontal and finite, with none of the refused quotient, selector, choice, equality, or completeness endpoints.
\end{proof}
