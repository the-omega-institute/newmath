\begin{theorem}[CauchyCompletionMonadLaw real completion unit route]
\label{thm:cauchy-completion-monad-law-real-completion-unit-route}
Every accepted $\CauchyCompletionMonadLawUp$ unit, multiplication, or associativity read that is consumed as completion-facing law data first passes through the $\RealCompletionUnitUp$ route of \autoref{ch:concrete-instances-real-completion-unit-namecert}. The read exposes the $\StreamNameUp$ window row, $\DyadicRatCoreUp$ tolerance row, $\RegSeqRatUp$ readback row, terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows before the law packet $L=(M,U,I,B,S,D,R,H,C,P,N)$ is exported.

Thus the monad-law consumer receives the real-completion unit as a displayed finite route. It does not receive quotient-completion equality, a selected Cauchy representative, countable-choice schedule, host equality of completions, hidden real completeness, or a detached $\RealUp$ endpoint.
\end{theorem}

\begin{proof}
Project the carrier of \autoref{def:cauchy-completion-monad-law-carrier}. The law rows $M,U,I,B$ can be consumed only together with the finite stream, dyadic, regular-rational, transport, replay, provenance, and naming rows $S,D,R,H,C,P,N$.

Apply the real completion unit surface of \autoref{ch:concrete-instances-real-completion-unit-namecert}. Its carrier and nonescape theorems place every real-completion unit read behind the dyadic source, stream-window, regular readback, terminal real seal, transport, replay, provenance, and local naming rows.

Finally use \autoref{thm:cauchy-completion-monad-law-namecert-obligations} and \autoref{thm:cauchy-completion-monad-law-unit-associativity-boundary}. They bind unit, multiplication, idempotence, and associativity reads to the same finite carrier before any completion-facing consumer sees law data, so the exported route cannot import quotient completions, selected representatives, countable choice, host equality, hidden completeness, or a detached real endpoint.
\end{proof}
