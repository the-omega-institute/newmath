The $\CauchyCompletionMonadLawUp$ packet exports its multiplication-law rows toward \autoref{ch:concrete-instances-cauchycompletionmultiplication-namecert} as a concrete completion-monad sibling route. The multiplication consumer reads the displayed unit, bind, idempotence, associativity, stream-window, dyadic-tolerance, regular-readback, transport, replay, provenance, and local naming rows of the law packet before it flattens a completion of completions; the edge is finite NameCert data, not quotient-completion equality, selected representatives, countable choice, or ambient $\RealUp$ completeness.
