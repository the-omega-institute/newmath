The $\CauchyCompletionMonadLawUp$ packet reads the completion functor route of \autoref{ch:concrete-instances-cauchycompletionfunctor-namecert}, the completion universal-property surface of \autoref{ch:concrete-instances-completionuniversalproperty-namecert}, and the metric-completion dependency lattice of \autoref{ch:concrete-instances-metriccompletion-namecert} before any $\RealUp$ or $\CompletionUp$ consumer treats the monad boundary as available law data. Thus unit, multiplication, idempotence, and associativity rows are exposed only after the displayed completion-facing dependencies are visible, not as isolated quotient-completion equality, selected limits, or ambient real completeness.
