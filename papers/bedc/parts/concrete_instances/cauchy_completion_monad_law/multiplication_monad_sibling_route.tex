The $\CauchyCompletionMonadLawUp$ packet reads the multiplication sibling of \autoref{ch:concrete-instances-cauchycompletionmultiplication-namecert} together with the monad sibling of \autoref{ch:concrete-instances-cauchycompletionmonad-namecert} before its associativity and unit-law rows are exposed. The multiplication row supplies the displayed bind-reassociation surface, while the monad row supplies the unit-and-bind schedule consumed by the law carrier; the route therefore remains a finite NameCert dependency through those sibling packets rather than a host-level equality of completed objects.
