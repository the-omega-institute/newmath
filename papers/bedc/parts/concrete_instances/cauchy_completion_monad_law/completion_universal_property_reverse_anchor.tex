The $\CauchyCompletionMonadLawUp$ packet reads \autoref{ch:concrete-instances-completionuniversalproperty-namecert} and \autoref{ch:concrete-instances-cauchycompletionfunctor-namecert} before associativity law rows are public. This is a finite completion-law dependency through the universal-property and functor rows, not quotient completion equality, selected limits, or host equality.
