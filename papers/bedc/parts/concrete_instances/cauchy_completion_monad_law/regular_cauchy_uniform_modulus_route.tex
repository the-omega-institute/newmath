\begin{lemma}[CauchyCompletionMonadLaw regular Cauchy uniform-modulus route]
\label{lem:cauchy-completion-monad-law-regular-cauchy-uniform-modulus-route}
Every accepted $\CauchyCompletionMonadLawUp$ consumer that compares completion unit and bind windows must first read the $\RegularCauchyUniformModulusUp$ budget of \autoref{ch:concrete-instances-regular-cauchy-uniform-modulus-namecert}. The modulus row fixes the common regular-Cauchy tolerance budget before the monad-law consumer may identify two completion routes, and the idempotent metric-monad route of \autoref{lem:cauchy-completion-monad-law-idempotent-metric-monad-route} keeps the resulting comparison inside the displayed unit, bind, multiplication, and associativity packet. The route exports no quotient-completion equality, selected representative, countable-choice schedule, host equality of completions, or ambient $\RealUp$ completeness principle.
\end{lemma}

The regular-modulus sibling is not a decorative cross-reference: it supplies the uniform tolerance row that lets the local carrier of \autoref{def:cauchy-completion-monad-law-carrier} compare unit and bind reads over one shared regular-Cauchy budget. Without that row, the monad-law packet would still display law coordinates, but it would not expose the finite modulus through which completion consumers synchronize their Cauchy windows.

\begin{theorem}[CauchyCompletionMonadLaw regular-modulus unit route]
\label{thm:cauchy-completion-monad-law-regular-modulus-unit}
Every accepted $\CauchyCompletionMonadLawUp$ carrier $L=(M,U,I,B,S,D,R,H,C,P,N)$ that exports a unit read to a completion-facing consumer first consumes the $\RegularCauchyUniformModulusUp$ sibling of \autoref{ch:concrete-instances-regular-cauchy-uniform-modulus-namecert}. The sibling supplies the common regular-Cauchy modulus for the stream row $S$, dyadic tolerance row $D$, and regular-rational row $R$ before the unit row $U$ is replayed through $\Cont$ and recorded by the local $\NameCert_{\CauchyCompletionMonadLawUp}$ row. Hence the unit route is a finite modulus-controlled read of the local carrier, not a quotient-completion equality, selected representative, countable-choice schedule, host equality of completions, or ambient $\RealUp$ completeness principle.
\end{theorem}

\begin{proof}
Project the carrier through \autoref{def:cauchy-completion-monad-law-carrier}. Its rows $S,D,R$ are the finite stream, dyadic tolerance, and regular-rational readback data used by unit and bind comparisons.

Apply \autoref{lem:cauchy-completion-monad-law-regular-cauchy-uniform-modulus-route}. It places the $\RegularCauchyUniformModulusUp$ budget before the monad-law consumer identifies unit and bind windows.

Finally apply \autoref{thm:cauchy-completion-monad-law-namecert-obligations}. The unit row $U$, replay row $C$, provenance row $P$, and local naming row $N$ are consumed only after the displayed carrier rows are available, so the route remains inside the finite monad-law packet and cannot import any excluded completion principle.
\end{proof}
