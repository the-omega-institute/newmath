The $\CauchyCompletionMonadLawUp$ packet also sits horizontally over the completion-functor sibling of \autoref{ch:concrete-instances-cauchycompletionfunctor-namecert}. Its unit, multiplication, idempotence, and associativity rows consume the functorial completion route before they are read as monad-law data, so the completion monad/functor law lattice is visible as a sibling dependency rather than a kernel-only float.
