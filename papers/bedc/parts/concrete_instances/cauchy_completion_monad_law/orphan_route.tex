$\CauchyCompletionMonadLawUp$ consumes the completion functor unit and multiplication route of \autoref{ch:concrete-instances-cauchycompletionfunctor-namecert} together with the finite readback rows of the Cauchy carrier in \autoref{ch:concrete-instances-cauchy-namecert}. The dependency is only the displayed unit, multiplication, stream-window, dyadic-tolerance, regular-readback, transport, replay, provenance, and local naming surface; it is not a quotient completion or a host universal property.

\begin{theorem}[CauchyCompletionMonadLaw completion-adjunction route]
\label{thm:cauchy-completion-monad-law-orphan-route}
Every accepted $\CauchyCompletionMonadLawUp$ unit or multiplication read consumes the same completion-adjunction route used by \autoref{ch:concrete-instances-metric-completion-adjunction-namecert}. The unit row, bind row, idempotence row, stream window, dyadic tolerance row, regular-rational readback, transport, replay, provenance, and local naming rows of $L=(M,U,I,B,S,D,R,H,C,P,N)$ therefore sit over the displayed completion-adjunction surface rather than over an isolated kernel-only packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-completion-monad-law-carrier}. The rows $M,U,I,B,S,D,R,H,C,P,N$ expose the law surface, but a completion-facing consumer still needs the adjunction route that identifies how completion reads are admitted and returned.

Read \autoref{ch:concrete-instances-metric-completion-adjunction-namecert}. Its source, dense embedding, completion seal, extension handoff, uniqueness, transport, replay, provenance, and local naming rows are the completion-adjunction surface through which the monad-law unit and multiplication rows are consumed.

Now apply \autoref{thm:cauchy-completion-monad-law-namecert-obligations} and \autoref{thm:cauchy-completion-monad-law-unit-associativity-boundary}. They force unit, idempotence, bind, and associativity reads through the same finite stream, dyadic, regular-rational, transport, replay, provenance, and naming rows, so the monad-law packet is routed through the completion-adjunction sibling before it can serve a Real-facing law read.
\end{proof}
