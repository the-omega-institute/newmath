The $\CauchyCompletionMonadLawUp$ packet also reads the completion universal-property and metric-completion siblings before its law rows are exposed to completion consumers. The extension-uniqueness side is supplied by \autoref{ch:concrete-instances-completionuniversalproperty-namecert}, while the metric-completion handoff is supplied by \autoref{ch:concrete-instances-metriccompletion-namecert}; hence the unit, multiplication, idempotence, and associativity rows remain attached to displayed completion data rather than to an isolated law ledger.
