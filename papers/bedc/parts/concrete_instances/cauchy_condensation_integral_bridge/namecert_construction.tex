\chapter{A Concrete Naming Certificate for $\CauchyCondensationIntegralBridgeUp$}
\label{ch:concrete-instances-cauchy-condensation-integral-bridge-namecert}
\origin{human}

Following Cauchy and Bishop--Bridges constructive analysis, the $\CauchyCondensationIntegralBridgeUp$ packet records the finite comparison bridge between dyadic condensation tails and integral-test windows. It sits after the theorem-level condensation surface of \autoref{ch:concrete-instances-cauchy-condensation-theorem-namecert}, the integral-window route of \autoref{ch:concrete-instances-cauchy-integral-test-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback corridor of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite handoff surface for Real and Completion consumers; it is not a quotient stream, a selected infinite sum, a host function equality, an ambient integral, countable choice, or a completeness principle for $\RealUp$.

\begin{definition}[CauchyCondensationIntegralBridge carrier]
\label{def:cauchy-condensation-integral-bridge-carrier}
A $\CauchyCondensationIntegralBridgeUp$ carrier is a finite $\BHist$ packet
$$
J=(K,I,D,W,Q,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the accepted $\CauchyCondensationTheoremUp$ row, exposing the monotone source tail, dyadic schedule, original block sums, condensed dyadic terms, and Cauchy-criterion handoff.
\item $I$ is the accepted $\CauchyIntegralTestUp$ row, exposing the nonnegative monotone observation, the Darboux or Riemann integral tail-window row, and the regular Cauchy series-tail row.
\item $D$ is the $\DyadicRatCoreUp$ ledger that compares the dyadic block endpoints used by $K$ with the finite integral-window endpoints used by $I$.
\item $W$ is the common finite window schedule. It lists the block indices, interval endpoints, tolerances, and comparison rows inspected before any regular readback is exported.
\item $Q$ is the bridge comparison row, recording that the condensation tail and the integral window are read over the same displayed tolerance budget.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from $D,W,Q$.
\item $E$ is the $\RealUp$ seal row attached to the regular readback.
\item $H$ records componentwise $\hsame$ transports for the condensation row, integral row, dyadic ledger, window schedule, comparison row, readback, real seal, replay, provenance, and local name.
\item $C$ records displayed $\Cont$ routes from each finite precision request through the dyadic and integral windows to $R$ and $E$.
\item $P$ is the $\Pkg$ provenance over $\CauchyCondensationIntegralBridgeUp$, $\CauchyCondensationTheoremUp$, $\CauchyIntegralTestUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyCondensationIntegralBridgeUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these rows. It has no coordinate for quotient streams, host functions, selected sums, ambient integrals, arbitrary real equality, countable choice, or $\RealUp$ completeness.
\end{definition}

\begin{theorem}[CauchyCondensationIntegralBridge NameCert obligations]
\label{thm:cauchy-condensation-integral-bridge-namecert-obligations}
For every accepted $\CauchyCondensationIntegralBridgeUp$ carrier $J=(K,I,D,W,Q,R,E,H,C,P,N)$, the local $\NameCert_{\CauchyCondensationIntegralBridgeUp}$ surface consists exactly of carrier admission for the displayed eleven-row packet; condensation-row exposure through $K$; integral-window exposure through $I$; dyadic endpoint and tolerance comparison through $D$; finite schedule coverage through $W$; bridge comparison exposure through $Q$; $\RegSeqRatUp$ readback through $R$; $\RealUp$ sealing through $E$; componentwise $\hsame$ transport through $H$; displayed $\Cont$ replay through $C$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted packet $J=(K,I,D,W,Q,R,E,H,C,P,N)$. The projection exposes exactly the condensation theorem row, integral-test row, dyadic ledger, finite window schedule, bridge comparison, regular readback, real seal, transport, replay, provenance, and local name.

Read a finite tail request through $K$ and $I$. The condensation side supplies dyadic block endpoints and condensed terms, while the integral side supplies the matching tail-window row. The dyadic ledger $D$ is the only comparison surface between those two rows.

Now inspect $W$ and $Q$. They bind the finite block indices, integral endpoints, and tolerance budget before $R$ can be produced, so neither side can export a separate quotient stream, host function, selected sum, or ambient integral.

The structural rows $H,C,P,N$ transport, replay, source, and name that same route. Since the carrier has no further coordinate, the listed rows exhaust the local NameCert obligation surface.
\end{proof}

\begin{theorem}[CauchyCondensationIntegralBridge dyadic tail exactness]
\label{thm:cauchy-condensation-integral-bridge-dyadic-tail-exactness}
Every accepted $\CauchyCondensationIntegralBridgeUp$ carrier preserves the dyadic tail route of $\CauchyCondensationTheoremUp$ before it enters an integral window. Concretely, a consumer can read the bridge comparison only through the finite path
$$
K,D,W,Q \longmapsto R,E,H,C,P,N,
$$
where $K$ supplies the condensation tail rows, $D$ supplies the dyadic tolerance ledger, and $W,Q$ record the common comparison window.
\end{theorem}
\begin{proof}
Begin with an accepted bridge carrier and project $K$. By the condensation theorem packet, the monotone source, dyadic schedule, block sums, condensed terms, and Cauchy handoff are already displayed before the bridge reads them.

Read $D$ against that displayed dyadic schedule. The tolerance ledger compares only finite endpoints and finite dyadic budgets, so the bridge does not select a new tail, quotient a stream, or appeal to an ambient real limit.

Finally use $W,Q,R,E,H,C,P,N$. The window and comparison rows attach the dyadic tail to the regular readback and real seal along the same displayed route, while the structural rows preserve that route componentwise.
\end{proof}

\begin{theorem}[CauchyCondensationIntegralBridge integral-window handoff]
\label{thm:cauchy-condensation-integral-bridge-integral-window-handoff}
Every accepted $\CauchyCondensationIntegralBridgeUp$ carrier hands an integral-test window to Real and Completion consumers only after the condensation-side dyadic tail and the integral-side tail window have been matched by $D,W,Q$. The handoff exports the $\RegSeqRatUp$ readback row $R$ and the $\RealUp$ seal row $E$, but exports no host function equality, ambient integral object, selected infinite sum, quotient stream, countable-choice schedule, or completeness principle.
\end{theorem}
\begin{proof}
Project the integral-test row $I$. Its finite tail-window data is the only integral-facing input available to the bridge, and it is read beside the nonnegative monotone observation and regular Cauchy series-tail row already named by $\CauchyIntegralTestUp$.

Use the dyadic exactness theorem above to align the same request with $K,D,W,Q$. The bridge comparison cannot reach $R$ until both the condensation tail and the integral window have been displayed under the shared tolerance budget.

Read the seal row $E$ through $R$ and the structural rows. Since the carrier contains no host function equality, ambient integral coordinate, selected sum, quotient stream, countable-choice schedule, or completeness field, the Real and Completion handoff is exactly the finite regular readback route.
\end{proof}

\falsifiablePrediction{An accepted $\CauchyCondensationIntegralBridgeUp$ consumer that reaches a $\RealUp$ seal without reading the displayed rows $K,I,D,W,Q,R$ refutes the carrier surface.}

\independenceWitness{cauchy-condensation-theorem, cauchy-integral-test, dyadicratcore, regseqrat, real: no listed sibling supplies the whole bridge packet $J=(K,I,D,W,Q,R,E,H,C,P,N)$, because each sibling contributes only a condensation theorem row, an integral-window row, a dyadic ledger, a regular readback, or a terminal seal while this chapter binds them to one finite comparison certificate.}

\closureat{CauchyCondensationIntegralBridgeUp}{seedStr}

\begin{closurestatus}{\CauchyCondensationIntegralBridgeUp}
  \constructivestory{A $\CauchyCondensationIntegralBridgeUp$ packet is generated from a CauchyCondensationTheorem row, a CauchyIntegralTest row, a DyadicRatCore endpoint and tolerance ledger, a finite shared window schedule, a bridge comparison row, a RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-condensation-integral-bridge-carrier}, \autoref{thm:cauchy-condensation-integral-bridge-namecert-obligations}, \autoref{thm:cauchy-condensation-integral-bridge-dyadic-tail-exactness}, and \autoref{thm:cauchy-condensation-integral-bridge-integral-window-handoff} seed the finite comparison surface between condensation tails and integral windows over $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host summability, quotient stream equality, selected infinite sums, ambient integrals, countable choice, RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, condensation-row exposure, integral-window exposure, dyadic endpoint comparison, finite schedule coverage, readback routing, Real seal routing, transport stability, replay stability, and non-escape for BEDC.Derived.CauchyCondensationIntegralBridgeUp.}
\end{closurestatus}
