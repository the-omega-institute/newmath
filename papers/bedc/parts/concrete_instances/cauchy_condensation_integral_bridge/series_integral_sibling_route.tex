\begin{theorem}[CauchyCondensationIntegralBridge sibling route]
\label{thm:cauchy-condensation-integral-bridge-series-integral-sibling-route}
Every accepted $\CauchyCondensationIntegralBridgeUp$ carrier reads the $\CauchyCondensationTheoremUp$ series-side row of \autoref{ch:concrete-instances-cauchy-condensation-theorem-namecert} and the $\CauchyIntegralTestUp$ integral comparison row of \autoref{ch:concrete-instances-cauchy-integral-test-namecert} before any integral-bound consumer is exposed. The route factors through the displayed bridge rows
$$
K,I,D,W,Q \longmapsto R,E,H,C,P,N,
$$
so an integral-bound consumer may receive only the finite comparison, $\RegSeqRatUp$ readback, $\RealUp$ seal, transport, replay, provenance, and local naming rows, not a selected subseries, ambient integral object, quotient stream equality, countable choice schedule, or Real completeness principle.
\end{theorem}
\begin{proof}
Project the accepted bridge carrier of \autoref{def:cauchy-condensation-integral-bridge-carrier}. Its source-facing rows are exactly $K$ and $I$: $K$ names the condensation theorem surface and $I$ names the integral-test surface.

Apply the obligation handoff of \autoref{thm:cauchy-condensation-integral-bridge-obligation-handoff}. The dyadic ledger, finite window schedule, and bridge comparison row must be read before the regular readback and Real seal are visible.

Thus the sibling route is finite and ordered. Since the carrier has no coordinate for a selected subseries, host integral object, quotient stream equality, countable choice schedule, or Real completeness theorem, the integral-bound consumer cannot escape the displayed comparison rows.
\end{proof}
