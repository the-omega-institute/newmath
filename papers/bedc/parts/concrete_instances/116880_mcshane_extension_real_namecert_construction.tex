\chapter{A Concrete Naming Certificate for $\McShaneExtensionRealUp$}
\label{ch:concrete-instances-mcshane-extension-real-namecert}
\origin{human}

Following McShane and Bishop--Bridges constructive real analysis, the $\McShaneExtensionRealUp$ packet records the finite BEDC surface for reading a real-valued Lipschitz map on a displayed subset of a displayed metric carrier as an ambient extension candidate. It sits after the metric source of \autoref{ch:concrete-instances-metric-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the reusable Lipschitz extension interface of \autoref{ch:concrete-instances-lipschitz-extension-namecert}, the uniformly continuous extension handoff of \autoref{ch:concrete-instances-uniform-continuous-extension-namecert}, and the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The chapter isolates the Real-valued McShane handoff as a finite NameCert packet rather than a host supremum formula, quotient function, selected family of extrema, countable-choice construction, or ambient completeness theorem.

\begin{definition}[McShane real-valued extension carrier]
\label{def:mcshane-extension-real-carrier}
A $\McShaneExtensionRealUp$ carrier is a finite $\BHist$ packet
$$
M=(A,d,G,L,E,S,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the subset-source row, recording admitted source points, inclusion into the ambient metric carrier, and the finite membership evidence used by source-value reads.
\item $d$ is the $\MetricUp$ distance row for the ambient carrier and the source subset, exposing only displayed distance observations.
\item $G$ is the Real-valued source graph row, read through $\RealUp$ seals and carrying the displayed source values without a host function object.
\item $L$ is the Lipschitz-constant budget row, recording the finite bound ledger consumed by $\LipschitzExtensionUp$ and the dyadic tolerance requests mediated by $\DyadicRatCoreUp$.
\item $E$ is the ambient extension graph row, recording the finite McShane envelope comparisons available at ambient inputs without selecting a supremum or infimum.
\item $S$ is the subset-agreement row, requiring every source-admitted ambient read to return through the same $A,d,G,L,E$ route before it can be identified with the source graph.
\item $R$ is the Real seal row for source values, envelope comparisons, and ambient extension reads.
\item $H$ records componentwise $\hsame$ transport for the subset source, metric distance, graph, budget, extension graph, agreement, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a precision request through $A,d,G,L,E,S,R,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\McShaneExtensionRealUp$, $\MetricUp$, $\RealUp$, $\LipschitzExtensionUp$, $\UniformContinuousExtensionUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\McShaneExtensionRealUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these eleven rows. It has no coordinate for host function equality, selected suprema, selected infima, quotient functions, countable choice, trichotomy, ambient $\RealUp$ completeness, or a public bridge credential.
\end{definition}

\begin{theorem}[McShane real-valued NameCert obligations]
\label{thm:mcshane-extension-real-namecert-obligations}
For every accepted $\McShaneExtensionRealUp$ carrier
$$
M=(A,d,G,L,E,S,R,H,C,P,N),
$$
the local $\NameCert_{\McShaneExtensionRealUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the subset source, metric distance, Real-valued graph, Lipschitz-constant budget, extension graph, subset agreement, Real seal, transport, replay, provenance, and local naming rows;
\item source-domain compatibility, requiring every source-value read to pass through $A$ and $d$ before it is compared with an ambient extension read;
\item Real graph admission, requiring $G$ and $R$ to be the only rows that expose source values and ambient extension values;
\item Lipschitz-budget transport, requiring $L$ and $\DyadicRatCoreUp$ to mediate every tolerance transfer used by the extension graph row;
\item extension-graph exactness, requiring $E$ to store only finite McShane comparison data and no selected supremum, selected infimum, quotient function, or host function object;
\item subset-agreement exactness, requiring $S$ to identify the extension graph with the source graph only through the same displayed $A,d,G,L,E,R$ route;
\item ledger non-escape, requiring $H,C,P,N$ to preserve and replay the listed rows without adding host Real equality, selected extrema, quotient functions, countable choice, or ambient Real completeness.
\end{enumerate}
No local obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:mcshane-extension-real-carrier}. The accepted packet displays exactly the subset source, metric distance, Real-valued graph, Lipschitz budget, extension graph, subset agreement, Real seal, and structural rows.

Read the source-domain and graph clauses next. The row $A$ admits source points, the distance row $d$ supplies the metric observations, and $G$ is the only Real-valued source graph row. Hence a source comparison cannot be formed before the packet has opened $A,d,G,R$.

The Lipschitz-budget clause is forced by $L$ together with the dyadic tolerance core. The reusable interface of \autoref{ch:concrete-instances-lipschitz-extension-namecert} and the dyadic route of \autoref{ch:concrete-instances-dyadicratcore-namecert} keep every tolerance transfer finite and visible.

Project the extension and agreement rows $E$ and $S$. The extension row stores finite McShane envelope comparisons, while the agreement row checks those comparisons on inputs admitted by $A$. Neither row selects an extremum, forms a quotient function, or imports a host equality of functions.

Finally inspect $H,C,P,N$. Transport is componentwise, replay follows the displayed request route, and provenance cites only the packet and its sibling dependencies, including \autoref{ch:concrete-instances-uniform-continuous-extension-namecert}. Therefore the local NameCert obligations are exactly the listed finite rows.
\end{proof}

\begin{theorem}[McShane real-valued Lipschitz handoff]
\label{thm:mcshane-extension-real-lipschitz-handoff}
Let $M=(A,d,G,L,E,S,R,H,C,P,N)$ be an accepted $\McShaneExtensionRealUp$ carrier. Every $\LipschitzExtensionUp$, $\UniformContinuousExtensionUp$, or $\RealUp$ consumer of the ambient extension read must first traverse the finite route
$$
A,d\longmapsto G\longmapsto L\longmapsto \DyadicRatCoreUp\longmapsto E\longmapsto S\longmapsto R\longmapsto H,C,P,N .
$$
Along that route the displayed Lipschitz budget remains the only bound used for the Real-valued ambient extension graph. The handoff exports no host function equality, selected supremum, selected infimum, quotient function, countable-choice selector, trichotomy branch, or ambient $\RealUp$ completeness witness.
\end{theorem}
\begin{proof}
Start with an ambient extension read. The subset row $A$ and distance row $d$ provide the only source-domain and metric observations available to the consumer, while $G$ supplies the source Real-valued graph.

Read the Lipschitz budget and dyadic tolerance rows. The row $L$ records the displayed bound, and the $\DyadicRatCoreUp$ route turns that bound into finite precision data before any ambient comparison is inspected.

The extension graph row $E$ is then opened as finite McShane comparison data, and the subset-agreement row $S$ checks that the same route restricts back to $G$ on inputs admitted by $A$. The Real seal $R$ is therefore reached only after the source graph, budget, extension graph, and agreement rows are visible.

Transport, replay, provenance, and local naming preserve the route through $H,C,P,N$. Since no listed row contains host function equality, selected extrema, quotient functions, countable choice, trichotomy, or ambient Real completeness, the $\LipschitzExtensionUp$, $\UniformContinuousExtensionUp$, and $\RealUp$ handoff cannot export any such object.
\end{proof}

\falsifiablePrediction{An accepted $\McShaneExtensionRealUp$ consumer that reaches an ambient Real-valued extension read without first reading the subset source, metric distance, source graph, Lipschitz budget, dyadic tolerance, extension graph, subset agreement, and Real seal rows refutes the carrier surface.}

\independenceWitness{metric, real, lipschitz-extension, uniform-continuous-extension, dyadicratcore: no listed sibling supplies the full eleven-row packet $M=(A,d,G,L,E,S,R,H,C,P,N)$, because each sibling contributes only a metric source, Real seal, reusable extension interface, continuity handoff, or dyadic tolerance route while this chapter binds the finite Real-valued McShane extension certificate into one local NameCert surface.}

\closureat{McShaneExtensionRealUp}{seedStr}

\begin{closurestatus}{\McShaneExtensionRealUp}
  \constructivestory{A $\McShaneExtensionRealUp$ packet is generated from a subset-source row, a MetricUp distance row, a Real-valued graph row, a Lipschitz-constant budget, an ambient extension graph, a subset-agreement row, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:mcshane-extension-real-carrier}, \autoref{thm:mcshane-extension-real-namecert-obligations}, and \autoref{thm:mcshane-extension-real-lipschitz-handoff} seed the finite Real-valued McShane extension certificate over displayed subset, metric, Lipschitz-budget, dyadic-tolerance, and Real-seal data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host function equality, selected suprema, selected infima, quotient functions, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source-domain compatibility, Real graph admission, Lipschitz-budget transport, extension-graph exactness, subset-agreement exactness, non-escape, and a Lean carrier/obligation formalization for BEDC.Derived.McShaneExtensionRealUp.}
\end{closurestatus}
