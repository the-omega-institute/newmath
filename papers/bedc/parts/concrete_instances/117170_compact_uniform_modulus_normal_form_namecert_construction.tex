\chapter{A Concrete Naming Certificate for $\CompactUniformModulusNormalFormUp$}
\label{ch:concrete-instances-compact-uniform-modulus-normal-form-namecert}
\origin{human}

Following the Bishop--Bridges finite-net proof of compact uniform continuity, the $\CompactUniformModulusNormalFormUp$ packet records the normal-form route that turns compact coverage, local continuity rows, and a rational lower-bound fold into a tolerance-only uniform-modulus handoff. It realizes the compact-uniform-continuity route of \autoref{ch:visions-compact-uniform-continuity} through the metric source of \autoref{ch:concrete-instances-metric-namecert}, the compact carrier of \autoref{ch:concrete-instances-compact-namecert}, the continuous-map rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the rational fold substrate of \autoref{ch:concrete-instances-rat-namecert}, and the consumer handoff of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The certificate is finite NameCert data; it is not an open-cover compactness theorem, a host Heine--Cantor principle, a selected global minimum outside the fold, quotient equality of functions, or a hidden modulus function.

\begin{definition}[Compact uniform modulus normal-form carrier]
\label{def:compact-uniform-modulus-normal-form-carrier}
A $\CompactUniformModulusNormalFormUp$ carrier is a finite $\BHist$ packet
$$
F=(M,K,G,N,L,R,E,U,H,C,P,A)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricUp$ source row for the displayed domain and codomain distances.
\item $K$ is the $\CompactUp$ finite-net row, exposing a finite $\ProbeBundle$ coverage window.
\item $G$ is the $\ContinuousMapUp$ row, read only through local pointwise-modulus data at the bundled centers.
\item $N$ is the finite-net normal-form row, pairing each bundled center with a local radius and target tolerance row.
\item $L$ is the local-radius ledger indexed by the same finite net.
\item $R$ is the $\RatUp$ lower-bound fold row over the nonzero local-radius ledger.
\item $E$ is the metric-triangle error-budget row that moves from an arbitrary point to its displayed center.
\item $U$ is the $\UniformModulusUp$ handoff row produced by $R$ and $E$.
\item $H$ stores componentwise $\hsame$ transport for metric, compact, continuous, finite-net, local-radius, rational-fold, error-budget, uniform-modulus, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from a compact-uniform request through $M,K,G,N,L,R,E,U,H,P,A$.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformModulusNormalFormUp$, $\MetricUp$, $\CompactUp$, $\ContinuousMapUp$, $\RatUp$, $\UniformModulusUp$, $\BHist$, $\ProbeBundle$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $A$ is the local $\NameCert_{\CompactUniformModulusNormalFormUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for an open-cover proof, countable choice, a host-selected minimum, quotient function equality, an ambient compactness predicate, or a hidden modulus.
\end{definition}

\begin{theorem}[Compact uniform modulus normal-form obligations]
\label{thm:compact-uniform-modulus-normal-form-namecert-obligations}
For every accepted $\CompactUniformModulusNormalFormUp$ carrier
$$
F=(M,K,G,N,L,R,E,U,H,C,P,A),
$$
the local $\NameCert_{\CompactUniformModulusNormalFormUp}$ surface consists exactly of metric admission through $M$, compact finite-net admission through $K$, continuous-map local-modulus exposure through $G$, finite-net normal-form alignment through $N,L$, rational lower-bound exactness through $R$, metric error-budget transport through $E$, uniform-modulus export through $U$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $A$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-modulus-normal-form-carrier}. The metric and compact rows $M,K$ supply the finite net, while the continuous-map row $G$ supplies only pointwise local-modulus data at the displayed centers.

The normal-form rows are $N,L,R$. They align the centers with local radii, keep the radius ledger finite, and fold the displayed positive rational radii to one global lower-bound row. No selected minimum outside the fold is present.

The consumer handoff is then forced through $E$ and $U$. The error-budget row transports the center estimates to arbitrary points, and the uniform-modulus row records the tolerance-only output. Rows $H,C,P,A$ only transport, replay, source, and name the same packet, so the listed obligations exhaust the certificate.
\end{proof}

\begin{theorem}[Compact uniform finite-net normal-form exhaustion]
\label{thm:compact-uniform-modulus-normal-form-exhaustion}
Every $\UniformModulusUp$ read exported by an accepted $\CompactUniformModulusNormalFormUp$ packet factors through the finite normal-form route
$$
\MetricUp,\CompactUp \longmapsto
\ContinuousMapUp \longmapsto
\RatUp \longmapsto
\UniformModulusUp .
$$
The route exposes a finite net, local pointwise moduli at the net centers, a rational lower-bound fold, metric-triangle transport, and a tolerance-only uniform-modulus row before any consumer may read the output.
\end{theorem}
\begin{proof}
Begin with a compact-uniform request. The metric row $M$ fixes the distance comparisons, and the compact row $K$ exposes the finite $\ProbeBundle$ coverage window.

Read the continuous-map row $G$ only at the bundled centers. The normal-form rows $N$ and $L$ pair those center reads with local-radius data, so every local modulus request has a visible finite-net index.

Apply the rational fold row $R$. It is the only row that turns the finite list of local radii into a global lower bound, and it is still a $\RatUp$ ledger rather than a host minimum.

Finally use the error-budget row $E$ and the uniform-modulus row $U$. The structural rows $H,C,P,A$ preserve this route, so no open-cover compactness, selected minimum, quotient equality, ambient compactness theorem, or hidden modulus can be exported.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactUniformModulusNormalFormUp$ uniform-modulus read factors through displayed Metric, Compact, ContinuousMap, Rat lower-bound fold, UniformModulus, ProbeBundle, hsame, Cont, Pkg, and local NameCert rows.}

\independenceWitness{metric, compact, continuousmap, rat, uniformmodulus: none is bijective with $\CompactUniformModulusNormalFormUp$ because this packet binds finite-net coverage, center-local modulus rows, rational lower-bound folding, metric error-budget transport, uniform-modulus export, replay, provenance, and local naming as one normal-form certificate.}

\closureat{CompactUniformModulusNormalFormUp}{seedStr}

\begin{closurestatus}{\CompactUniformModulusNormalFormUp}
  \constructivestory{A $\CompactUniformModulusNormalFormUp$ packet is generated from Metric source rows, Compact finite-net rows, ContinuousMap local-modulus rows, finite-net normal-form ledgers, Rat lower-bound folds, metric error-budget transport, UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-modulus-normal-form-carrier}, \autoref{thm:compact-uniform-modulus-normal-form-namecert-obligations}, and \autoref{thm:compact-uniform-modulus-normal-form-exhaustion} seed the finite-net lower-bound fold normal form for compact uniform modulus extraction.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close open-cover compactness, host Heine--Cantor semantics, selected minima outside the fold, quotient function equality, ambient compactness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-net normal-form alignment, local-modulus exposure, rational lower-bound fold exactness, metric error-budget transport, uniform-modulus export, replay, provenance, and local naming for BEDC.Derived.CompactUniformModulusNormalFormUp.}
\end{closurestatus}
