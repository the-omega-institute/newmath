\chapter{A Concrete Naming Certificate for $\ArithmeticalHierarchyUp$}
\label{ch:concrete-instances-arithmetical-hierarchy-namecert}
\origin{human}

Following Kleene's recursion-theory hierarchy, the $\ArithmeticalHierarchyUp$ packet records the finite BEDC route by which bounded first-order syntax, computability evidence, and oracle-free quantifier alternation data are named before a Real or Completion consumer may cite arithmetical complexity. It sits beside the syntactic carrier of \autoref{ch:concrete-instances-firstorder-namecert}, the computability interface of \autoref{ch:concrete-instances-computable-namecert}, the set-membership discipline of \autoref{ch:concrete-instances-setlike-namecert}, the lambda-calculus syntax route of \autoref{ch:concrete-instances-lambdacalc-namecert}, and the constructive-real boundary of \autoref{ch:concrete-instances-computablereal-namecert}. The downstream use is the same finite obstruction surface read by \autoref{ch:concrete-instances-specker-sequence-boundary-namecert}: a Specker or computable-real boundary may cite a displayed arithmetical class, but it cannot import a host predicate hierarchy, quotient equality of predicates, excluded middle, countable choice, or an ambient completion theorem.

\begin{definition}[ArithmeticalHierarchy carrier]
\label{def:arithmetical-hierarchy-carrier}
An $\ArithmeticalHierarchyUp$ carrier is a finite $\BHist$ packet
$$
A=(S,E,Q,B,R,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\FirstOrderUp$ syntax row containing the bounded formula spine, free-variable ledger, and term source read by the classifier.
\item $E$ is the $\ComputableUp$ effective-evaluation row for atomic and bounded subformula reads.
\item $Q$ is the quantifier-prefix row listing a finite alternation word over existential and universal blocks, with its initial polarity and displayed block boundaries.
\item $B$ is the bound ledger recording which quantifiers are bounded and which finite rows expose the requested numeric or finite-set search windows.
\item $R$ is the rank row naming the local $\Sigma^0_n$, $\Pi^0_n$, or $\Delta^0_n$ class as finite data rather than as a host predicate family.
\item $L$ is the stability ledger asserting that prefix padding, bounded-quantifier expansion, and componentwise formula transport preserve the displayed rank row.
\item $H$ records componentwise $\hsame$ transport for $S,E,Q,B,R,L,C,P,N$.
\item $C$ records displayed $\Cont$ replay from the syntax and computability rows through prefix, bound, rank, and stability reads.
\item $P$ is the $\Pkg$ provenance over $\ArithmeticalHierarchyUp$, $\FirstOrderUp$, $\ComputableUp$, $\SetLikeUp$, $\LambdaCalcUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\ArithmeticalHierarchyUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,E,Q,B,R,L,H,C,P,N$. It has no coordinate for a host subset of $\mathbb{N}$, truth in the standard model, quotient equality of predicates, oracle jumps, countable choice, excluded middle, Real completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[ArithmeticalHierarchy NameCert obligations]
\label{thm:arithmetical-hierarchy-namecert-obligations}
For every accepted $\ArithmeticalHierarchyUp$ carrier
$$
A=(S,E,Q,B,R,L,H,C,P,N),
$$
the local $\NameCert_{\ArithmeticalHierarchyUp}$ surface consists exactly of carrier admission for the displayed rows, syntax visibility through $S$, effective atomic and bounded reads through $E$, quantifier-prefix admission through $Q$, bound-ledger exactness through $B$, rank-row exactness through $R$, stability under displayed prefix padding and bounded expansion through $L$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$.

These obligations bind the arithmetical-class route without adding host predicate semantics, quotient predicate equality, oracle jumps, unrestricted comprehension, excluded middle, countable choice, or an ambient $\RealUp$ completion claim.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:arithmetical-hierarchy-carrier}. The rows $S,E,Q,B,R,L,H,C,P,N$ are the whole local surface, so carrier admission is exactly admission of those rows.

The syntactic and effective data are read first. The row $S$ fixes the first-order formula spine and free-variable ledger, while $E$ supplies only the effective atomic and bounded reads available to the packet.

The hierarchy data are then forced by the displayed prefix. The row $Q$ lists the finite alternation word, $B$ records the bounded search windows, and $R$ names the resulting $\Sigma^0_n$, $\Pi^0_n$, or $\Delta^0_n$ rank. The stability row $L$ is the only place where padding or bounded expansion may be replayed.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier has no coordinate for host truth, predicate quotients, oracle jumps, unrestricted comprehension, choice, excluded middle, or Real completeness, the listed clauses exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[ArithmeticalHierarchy computable-real boundary handoff]
\label{thm:arithmetical-hierarchy-computable-real-boundary-handoff}
Every computable-real, Specker-sequence, or completion-boundary consumer of an accepted $\ArithmeticalHierarchyUp$ packet must read the finite route
$$
\FirstOrderUp,\ \ComputableUp,\ Q,\ B,\ R,\ L,\ \SetLikeUp
$$
as displayed by \autoref{def:arithmetical-hierarchy-carrier}. The route exports a named arithmetical class for the downstream boundary read; it does not export host truth, a predicate quotient, an oracle jump, a selected completion point, countable choice, excluded middle, or an ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Start with a Real-facing or Specker-facing boundary read. The only syntax source available in the carrier is $S$, and the only effective-evaluation row is $E$, so the consumer is confined to the displayed first-order and computability data.

Read the classification surface next. The prefix row $Q$, bound row $B$, rank row $R$, and stability row $L$ together expose the finite arithmetical-class route, while the set-like provenance can be cited only through $P$ and the displayed replay $C$.

The structural rows do not add a hidden semantic object. Because no host predicate hierarchy, quotient equality, oracle jump, selected Real point, countable-choice schedule, excluded-middle principle, or ambient completeness theorem appears as a carrier coordinate, the consumer receives only the stated finite route.
\end{proof}

\falsifiablePrediction{Every accepted $\ArithmeticalHierarchyUp$ boundary read that reaches a $\ComputableRealUp$, $\SpeckerSequenceBoundaryUp$, or completion-facing consumer displays the rows $S,E,Q,B,R,L,H,C,P,N$ first.}

\independenceWitness{firstorder, computable, setlike, lambdacalc, computablereal, specker-sequence-boundary: no listed sibling supplies the joint packet containing bounded first-order syntax, effective atomic and bounded reads, quantifier-prefix data, bound ledgers, rank rows, stability transport, displayed replay, provenance, and a local NameCert row.}

\closureat{ArithmeticalHierarchyUp}{seedStr}

\begin{closurestatus}{\ArithmeticalHierarchyUp}
  \constructivestory{An $\ArithmeticalHierarchyUp$ packet is generated from a FirstOrderUp syntax row, a ComputableUp effective-evaluation row, finite quantifier-prefix data, a bounded-search ledger, a displayed rank row, stability under prefix padding and bounded expansion, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:arithmetical-hierarchy-carrier}, \autoref{thm:arithmetical-hierarchy-namecert-obligations}, and \autoref{thm:arithmetical-hierarchy-computable-real-boundary-handoff} seed the finite arithmetical-class route from displayed first-order and computability rows through prefix, bound, rank, stability, and boundary-consumer reads.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean theorem checking, bridge checking, host predicate semantics, quotient predicate equality, oracle jumps, unrestricted comprehension, countable choice, excluded middle, selected Real points, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, syntax visibility, effective readback, quantifier-prefix admission, bound-ledger exactness, rank-row exactness, stability transport, displayed replay, provenance, and local naming for BEDC.Derived.ArithmeticalHierarchyUp.}
\end{closurestatus}
