\chapter{A Concrete Naming Certificate for $\UpperDiniDerivativeEnvelopeUp$}
\label{ch:concrete-instances-upper-dini-derivative-envelope-namecert}
\origin{human}

Following Dini and Bishop--Bridges constructive analysis, the $\UpperDiniDerivativeEnvelopeUp$ packet records the finite upper one-sided Dini derivative envelope through which displayed function windows expose upper secant comparison rows before any semicontinuity, regular-readback, or real-name consumer can inspect them. It sits beside the one-sided Dini carrier of \autoref{ch:concrete-instances-dini-derivative-namecert}, the lower envelope sibling of \autoref{ch:concrete-instances-dini-derivative-lower-envelope-namecert}, the regular Cauchy majorant of \autoref{ch:concrete-instances-regular-cauchy-majorant-namecert}, the finite stream-name schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names only finite upper-envelope windows and readback rows; it does not claim classical limsup existence, selected derivative values, quotient real equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[UpperDiniDerivativeEnvelope carrier]
\label{def:upper-dini-derivative-envelope-carrier}
An $\UpperDiniDerivativeEnvelopeUp$ carrier is a finite $\BHist$ packet
$$
U=(F,W,UQ,R,S,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the displayed real-valued function row, read through the same finite observation surface used by the sibling $\DiniDerivativeUp$ and $\UpperDiniDerivativeUp$ packets.
\item $W$ is the upper-envelope window row, listing the finite one-sided neighborhoods and upper secant comparisons available to the packet.
\item $UQ$ is the $\DyadicRatCoreUp$ tolerance row for radii, slope upper bounds, and comparison slack.
\item $R$ is the $\RegSeqRatUp$ readback row for endpoint values, secant numerators, step sizes, and dyadic upper-bound approximants.
\item $S$ is the $\StreamNameUp$ schedule row that records which finite function and base-point observations are available before a readback is public.
\item $E$ is the terminal $\RealUp$ seal reached only after $F,W,UQ,R,S$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the function, upper-envelope window, dyadic, readback, stream, Real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay for refinements of the same finite upper-envelope window.
\item $P$ is the $\Pkg$ provenance over $\UpperDiniDerivativeEnvelopeUp$, $\DiniDerivativeUp$, $\DiniDerivativeLowerEnvelopeUp$, $\UpperDiniDerivativeUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\UpperDiniDerivativeEnvelopeUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $F,W,UQ,R,S,E,H,C,P,N$. It has no coordinate for a lower-envelope row, two-sided derivative, selected limiting slope, classical limsup object, quotient real equality, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[UpperDiniDerivativeEnvelope NameCert obligations]
\label{thm:upper-dini-derivative-envelope-namecert-obligations}
For every accepted $\UpperDiniDerivativeEnvelopeUp$ carrier
$$
U=(F,W,UQ,R,S,E,H,C,P,N),
$$
the local $\NameCert_{\UpperDiniDerivativeEnvelopeUp}$ surface consists exactly of carrier admission for the displayed function row, upper-envelope windows, DyadicRatCore tolerance row, RegSeqRat readback, StreamName schedule, RealUp seal, componentwise transport, continuation replay, package provenance, and local naming; upper-envelope exactness requiring every upper-bound read to pass through $W,UQ,R,S$; sibling compatibility requiring the Dini and lower-envelope packets to remain cited only as comparison surfaces; and nonescape from lower-envelope export, two-sided derivative data, selected limiting slopes, classical limsup objects, quotient real equality, countable choice, and ambient Real completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:upper-dini-derivative-envelope-carrier}. The accepted packet exposes only the displayed function row $F$, upper-envelope windows $W$, dyadic tolerance row $UQ$, regular readback row $R$, stream schedule $S$, Real seal $E$, and structural rows $H,C,P,N$.

Upper-envelope exactness is finite. An upper-bound request must first read the inspected window in $W$, then the dyadic tolerance and slack data in $UQ$, then the regular rational readback in $R$, and then the scheduled observation row in $S$ before the Real seal can be consumed.

The Dini and lower-envelope siblings are comparison anchors, not exported coordinates. They identify the surrounding one-sided derivative surface while the present packet keeps only the upper-envelope route visible.

Since no carrier coordinate stores a lower-envelope export, two-sided derivative, selected limiting slope, classical limsup object, quotient equality, choice schedule, or ambient completeness theorem, the listed rows exhaust the local NameCert obligations.
\end{proof}

\falsifiablePrediction{Every accepted $\UpperDiniDerivativeEnvelopeUp$ consumer reaching an upper-envelope read without the displayed function row, upper-envelope windows, DyadicRatCore tolerance row, RegSeqRat readback, StreamName schedule, RealUp seal, hsame transport, Cont replay, Pkg provenance, and local naming refutes the carrier.}

\independenceWitness{dini-derivative, dini-derivative-lower-envelope, upper-dini-derivative, regseqrat, streamname, real: no listed sibling supplies the whole upper-envelope packet $U=(F,W,UQ,R,S,E,H,C,P,N)$, because each sibling contributes only one-sided Dini windows, lower rows, upper derivative rows, rational readback, observation schedules, or a terminal real seal while this chapter binds the upper-envelope route in one finite surface.}

\closureat{UpperDiniDerivativeEnvelopeUp}{seedStr}

\begin{closurestatus}{\UpperDiniDerivativeEnvelopeUp}
  \constructivestory{An $\UpperDiniDerivativeEnvelopeUp$ packet is generated from displayed function data, upper-envelope windows, DyadicRatCore tolerance rows, RegSeqRat readback, StreamName schedules, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:upper-dini-derivative-envelope-carrier} and \autoref{thm:upper-dini-derivative-envelope-namecert-obligations} seed the finite upper-envelope packet from Dini derivative windows to regular-readback and real-name consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim classical limsup existence, lower-envelope export, two-sided derivative existence, selected derivative values, quotient real equality, countable choice, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, upper-envelope exactness, sibling compatibility, DyadicRatCore tolerance exposure, RegSeqRat readback, StreamName scheduling, RealUp seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.UpperDiniDerivativeEnvelopeUp.}
\end{closurestatus}
