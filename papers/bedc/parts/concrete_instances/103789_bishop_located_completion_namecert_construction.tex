\chapter{A Concrete Naming Certificate for $\BishopLocatedCompletionUp$}
\label{ch:concrete-instances-bishop-located-completion-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopLocatedCompletionUp$ packet records the located completion landing object reached when displayed Cauchy data carries an explicit locatedness service before the L10 Real and Completion routes consume it. It sits after the located Cauchy evidence of \autoref{ch:concrete-instances-cauchy-locatedness-namecert}, the located completion seal of \autoref{ch:concrete-instances-located-completion-seal-namecert}, the metric-completion interface of \autoref{ch:concrete-instances-metriccompletion-namecert}, and the real Cauchy modulus boundary of \autoref{ch:concrete-instances-realcauchymodulus-namecert}. The packet supplies a named constructive completion surface for located Cauchy data; it does not assert quotient equality of Cauchy names, select representatives, use trichotomy, invoke countable choice, or claim ambient $\RealUp$ completeness.

\begin{definition}[Bishop located completion carrier]
\label{def:bishop-located-completion-carrier}
A $\BishopLocatedCompletionUp$ carrier is a finite $\BHist$ packet
$$
B=(L,S,M,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the $\CauchyLocatednessUp$ row. It records the located Cauchy evidence available before any completion-facing consumer reads a seal.
\item $S$ is the $\LocatedCompletionSealUp$ row. It seals the located Cauchy data as a completion-facing object without quotienting names.
\item $M$ is the $\MetricCompletionUp$ route. It records the displayed metric-completion source through which the located packet can be consumed by L10 completion readers.
\item $R$ is the $\RealCauchyModulusUp$ row. It records the real-facing modulus or threshold discipline attached to the same Cauchy data.
\item $E$ is the terminal $\RealUp$ or completion seal exposed only after $L,S,M,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for locatedness, seal, metric-completion, real-modulus, terminal seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a completion request through $L,S,M,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedCompletionUp$, $\CauchyLocatednessUp$, $\LocatedCompletionSealUp$, $\MetricCompletionUp$, $\RealCauchyModulusUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\BishopLocatedCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,S,M,R,E,H,C,P,N$. It has no coordinate for quotient completion equality, selected representatives, host real equality, trichotomy, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop located completion NameCert obligations]
\label{thm:bishop-located-completion-namecert}
For every accepted $\BishopLocatedCompletionUp$ carrier $B=(L,S,M,R,E,H,C,P,N)$, the local $\NameCert_{\BishopLocatedCompletionUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed located Cauchy row, located completion seal, metric-completion route, real Cauchy modulus row, terminal seal, and structural rows $H,C,P,N$;
\item locatedness exposure, requiring every completion-facing read to pass through $L$ before the located seal or metric-completion route is inspected;
\item seal exactness, requiring $S$ to package the same located Cauchy data without adding quotient names or representative selection;
\item completion compatibility, requiring $M$ to consume only the displayed metric-completion route already available from the located Cauchy data;
\item real-modulus compatibility, requiring $R$ to expose the same real Cauchy modulus or threshold discipline before the terminal $\RealUp$ or completion seal $E$ is read;
\item componentwise transport, displayed replay, provenance, and local naming for the same route through $H,C,P,N$.
\end{enumerate}
No Bishop located completion obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-located-completion-carrier}. The visible packet consists exactly of the located Cauchy row $L$, located seal $S$, metric-completion route $M$, real-modulus row $R$, terminal seal $E$, and structural rows $H,C,P,N$.

The locatedness row is read first. The chapter \autoref{ch:concrete-instances-cauchy-locatedness-namecert} supplies the sibling surface for located Cauchy evidence, and the carrier gives no route to the seal before that row is displayed.

The seal and completion rows then keep the same data on the L10 route. The seal row $S$ is the located-completion boundary of \autoref{ch:concrete-instances-located-completion-seal-namecert}, while $M$ routes the packet through the metric-completion interface of \autoref{ch:concrete-instances-metriccompletion-namecert}.

Finally the real-modulus and structural rows close the local certificate. The row $R$ records the threshold discipline named in \autoref{ch:concrete-instances-realcauchymodulus-namecert}, and $E,H,C,P,N$ expose, transport, replay, source, and name the same route. Since the excluded quotient, selection, trichotomy, choice, host-equality, and ambient-completeness objects are absent from the carrier, the listed rows exhaust the local $\NameCert$ surface.
\end{proof}

\begin{theorem}[Bishop located completion nonescape]
\label{thm:bishop-located-completion-nonescape}
Every accepted $\BishopLocatedCompletionUp$ Real or completion read factors through the displayed rows $L,S,M,R,E,H,C,P,N$ of \autoref{def:bishop-located-completion-carrier}. The read may inspect the $\CauchyLocatednessUp$ row, the $\LocatedCompletionSealUp$ row, the $\MetricCompletionUp$ route, the $\RealCauchyModulusUp$ threshold row, the terminal seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row, but it cannot project quotient completion equality, selected representatives, host equality, trichotomy, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-located-completion-carrier}. The only visible analytic rows are $L,S,M,R,E$, and the only structural rows are $H,C,P,N$.

Use \autoref{thm:bishop-located-completion-namecert}. It requires completion-facing reads to pass through locatedness, located sealing, metric-completion compatibility, real-modulus compatibility, terminal sealing, componentwise transport, replay, provenance, and local naming in the displayed order.

The refused objects have no row in the carrier. Hence quotient completion equality, selected representatives, host equality, trichotomy, countable choice, and ambient $\RealUp$ completeness cannot be projected from an accepted Bishop located completion read.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopLocatedCompletionUp$ Real or Completion read factors through $\CauchyLocatednessUp$, $\LocatedCompletionSealUp$, $\MetricCompletionUp$, $\RealCauchyModulusUp$, terminal sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{cauchylocatedness, locatedcompletionseal, metriccompletion, realcauchymodulus: $\BishopLocatedCompletionUp$ is not bijective to any listed sibling because it jointly carries located Cauchy evidence, located completion sealing, metric-completion routing, real-modulus threshold discipline, terminal sealing, transport, continuation, provenance, and local naming as one finite completion packet.}

\closureat{BishopLocatedCompletionUp}{seedStr}

\begin{closurestatus}{\BishopLocatedCompletionUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-located-completion-carrier}, \autoref{thm:bishop-located-completion-namecert}, and \autoref{thm:bishop-located-completion-nonescape} seed the finite located-completion surface over $\CauchyLocatednessUp$, $\LocatedCompletionSealUp$, $\MetricCompletionUp$, $\RealCauchyModulusUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completions, selected representatives, host real equality, trichotomy, countable choice, public bridge checking, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, locatedness exposure, seal exactness, metric-completion compatibility, real-modulus compatibility, and ledger non-escape over the same displayed packet.}
  \constructivestory{The construction starts from located Cauchy data, seals it as a located completion object, routes it through the L10 metric-completion interface, and exposes the real Cauchy modulus only along the same finite NameCert path.}
\end{closurestatus}
