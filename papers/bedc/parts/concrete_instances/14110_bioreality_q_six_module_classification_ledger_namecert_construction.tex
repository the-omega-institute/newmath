The $\BioRealityQSixModuleClassificationLedgerUp$ packet records the query-six leave-one-codon module-classification ledger as a finite NameCert carrier. It exposes only the six named module rows, the thirteen-codon classification scope, the complete assignment counts, the empty residual lists, and provenance needed to name this empirical surface.

The packet consumes the q-six leave-one code boundary ledger of \autoref{ch:concrete-instances-bioreality_q_six_leave_one_code_boundary_ledger-namecert} before its child file records the module rows, so the classification ledger is read against a concrete boundary sibling rather than as a kernel-only note.

\input{parts/concrete_instances/bioreality_q_six_module_classification_ledger/namecert_construction}
