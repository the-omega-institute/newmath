\begin{theorem}[$\ZNormalUp$ root downstream totality]
\label{thm:znormal-root-downstream-totality}
Every downstream normal-form consumer of an accepted $\ZNormalUp$ packet factors through the root carrier rows
$$
T,F,V,K,R,H,C,P,N.
$$
The consumer may read terminal normality through $T,F,V,R$, finite normal-word evidence through $K$, transport through $H$, continuation replay through $C$, package provenance through $P$, and local naming through $N$; it receives no normal-form row outside that displayed packet.
\end{theorem}
\leanchecked{BEDC.Derived.ZnormalUp.ZnormalPacket\_downstream\_totality}
\begin{proof}
Project the carrier from \autoref{def:znormal-carrier}. It displays exactly $T,F,V,K,R,H,C,P,N$ as the accepted packet and gives a downstream consumer no separate carrier coordinate.

Use \autoref{thm:znormal-namecert-obligations} to bind those rows to the local $\NameCert_{\ZNormalUp}$ surface. Then apply \autoref{thm:znormal-root-visible-source-package}, which places every normal-output read after the same visible source package has been exposed.

The routing clauses are supplied by \autoref{thm:znormal-root-normal-word-route-totality} and \autoref{thm:znormal-root-continuation-terminal-exhaustion}. The first theorem fixes the finite normal-word route through $K$, and the second fixes continuation-terminal reads through $T,F,V,R,H,C,P,N$. Hence every downstream normal-form consumer factors through the displayed packet.
\end{proof}

\begin{theorem}[$\ZNormalUp$ root sibling normal-word consumption]
\label{thm:znormal-root-sibling-normalword-consumption}
For every accepted $\ZNormalUp$ packet, the $K$ row is consumed only through the finite sibling surfaces of \autoref{ch:concrete-instances-zeckendorf-normal-namecert}, \autoref{ch:concrete-instances-zcarry-namecert}, and \autoref{ch:concrete-instances-continuationtracenormalform-namecert}. No downstream normal-form consumer may read $K$ as host conversion, quotient normalization, unrestricted evaluation, divergence detection, substrate halting, or global normalization.
\end{theorem}
\leanchecked{BEDC.Derived.ZnormalUp.ZnormalRootSiblingNormalWordConsumption}
\begin{proof}
Begin with \autoref{def:znormal-carrier}. The row $K$ appears inside the same finite packet as $T,F,V,R,H,C,P,N$ and is not a separate host-facing row.

Apply \autoref{thm:znormal-root-normal-word-route-totality}. It routes the normal-word evidence through the ZeckendorfNormal and ZCarry sibling surfaces before any root consumer reads the local $K$ row.

Next use \autoref{thm:znormal-root-continuation-terminal-exhaustion}. Continuation-trace normal-form reads remain terminal continuation reads through $T,F,V,R,H,C,P,N$, so the same downstream consumer cannot detach $K$ from the terminal route.

Finally apply \autoref{thm:znormal-root-downstream-totality}. Since every downstream normal-form consumer factors through the displayed packet, the sibling normal-word row is consumed only through those finite sibling surfaces and not through any excluded host or global-normalization object.
\end{proof}

\begin{theorem}[$\ZNormalUp$ root total-host refusal]
\label{thm:znormal-root-total-host-refusal}
The root interface of an accepted $\ZNormalUp$ packet exports no host conversion, quotient normalizer, unrestricted evaluator, divergence oracle, substrate-halting row, or global normalizer. Any total-host or downstream normal-form use must remain inside
$$
T,F,V,K,R,H,C,P,N
$$
and the finite sibling consumption route of \autoref{thm:znormal-root-sibling-normalword-consumption}.
\end{theorem}
\leanchecked{BEDC.Derived.ZnormalUp.ZnormalRootTotalHostRefusal}
\begin{proof}
Project the visible source package using \autoref{thm:znormal-root-visible-source-package}. It exposes the typed endpoint, finite fuel, terminal-normality, normal-word, continuation, transport, replay, package, and naming rows before any normal-output consumer reads the packet.

Use \autoref{thm:znormal-root-downstream-totality} to cover downstream normal-form consumers, and use \autoref{thm:znormal-root-sibling-normalword-consumption} to cover the finite sibling route through $K$.

It remains to exclude total-host escape. Apply \autoref{thm:znormal-root-continuation-terminal-exhaustion}; continuation reads are terminal route reads through the displayed packet and expose no unrestricted evaluator, divergence oracle, host conversion, quotient normalizer, substrate-halting row, or global normalizer. Therefore the root interface refuses exactly those host and global-normalization objects.
\end{proof}

\begin{theorem}[$\ZNormalUp$ total-host readback factorization]
\label{thm:znormal-root-total-host-readback-factorization}
Every total-host readback of an accepted $\ZNormalUp$ packet factors through the root packet
$$
T,F,V,K,R,H,C,P,N
$$
and through the finite sibling route of \autoref{thm:znormal-root-sibling-normalword-consumption}. In particular, a total-host consumer may read terminal normality, finite normal-word evidence, transport, continuation replay, provenance, and local naming only by first passing through the displayed carrier rows and then through the ZeckendorfNormal, ZCarry, and ContinuationTraceNormalForm sibling surfaces where the normal-word route exposes them.
\end{theorem}
\leanchecked{BEDC.Derived.ZnormalUp.ZnormalPacket\_root\_total\_host\_readback\_factorization}
\begin{proof}
Project the accepted carrier from \autoref{def:znormal-carrier}. It lists $T,F,V,K,R,H,C,P,N$ as the complete root packet, so a total-host readback has no separate coordinate from which to obtain normal-form data.

Apply \autoref{thm:znormal-root-downstream-totality}. This places every downstream normal-form consumer inside the displayed packet. Then apply \autoref{thm:znormal-root-sibling-normalword-consumption}; the only consumption path for $K$ is the finite sibling route through ZeckendorfNormal, ZCarry, and ContinuationTraceNormalForm surfaces.

Finally use \autoref{thm:znormal-root-total-host-refusal}. Since the root interface refuses host conversion, quotient normalization, unrestricted evaluation, divergence detection, substrate halting, and global normalization, the total-host readback can only be the packet-and-sibling factorization just described.
\end{proof}

\begin{theorem}[$\ZNormalUp$ normal-form consumer boundary]
\label{thm:znormal-root-normal-form-consumer-boundary}
Every normal-form consumer of an accepted $\ZNormalUp$ packet receives only terminal normality through $T,V,R$, finite normal-word evidence through $K$, componentwise transport through $H$, continuation replay through $C$, provenance through $P$, and local $\NameCert_{\ZNormalUp}$ rows through $N$. The consumer receives no host conversion row, quotient normalizer, unrestricted evaluator, divergence detector, substrate-halting row, or global normalizer.
\end{theorem}
\leanchecked{BEDC.Derived.ZnormalUp.ZnormalPacket\_normal\_form\_consumer\_boundary}
\leanvariant{BEDC.Derived.ZnormalUp.ZnormalPacket\_root\_normal\_form\_consumer\_boundary}
\begin{proof}
Start from \autoref{def:znormal-carrier}. The carrier exposes the terminal, word, transport, replay, provenance, and naming rows as coordinates of the same finite packet and lists no host-facing conversion or global-normalization row.

Use \autoref{thm:znormal-root-downstream-totality} to bind any normal-form consumer to that packet. The terminal and continuation readbacks are then exhausted by \autoref{thm:znormal-root-continuation-terminal-exhaustion}, so terminal normality and continuation replay remain inside $T,V,R,H,C,P,N$.

For finite normal-word evidence, apply \autoref{thm:znormal-root-sibling-normalword-consumption}. It keeps $K$ on the finite sibling route. The refusal clause of \autoref{thm:znormal-root-total-host-refusal} excludes every listed host and global-normalization object, giving exactly the stated consumer boundary.
\end{proof}

\begin{theorem}[$\ZNormalUp$ sibling-refusal exhaustion]
\label{thm:znormal-root-sibling-refusal-exhaustion}
For an accepted $\ZNormalUp$ packet, the ZeckendorfNormal, ZCarry, and ContinuationTraceNormalForm sibling surfaces exhaust the permitted normal-word route. Outside those sibling surfaces and the root packet
$$
T,F,V,K,R,H,C,P,N,
$$
the interface excludes host conversion, quotient normalization, unrestricted evaluation, divergence detection, substrate halting, and global normalization.
\end{theorem}
\leanchecked{BEDC.Derived.ZnormalUp.ZnormalRootSiblingRefusalExhaustion}
\begin{proof}
Project the carrier with \autoref{def:znormal-carrier}. The normal-word coordinate $K$ is one row of the finite root packet, and the carrier supplies no independent route for host conversion or global normalization.

Apply \autoref{thm:znormal-root-sibling-normalword-consumption}. It identifies the permitted normal-word route with the ZeckendorfNormal, ZCarry, and ContinuationTraceNormalForm sibling surfaces and denies the listed host and global-normalization readings of $K$.

It remains to see that no total-host path reintroduces an excluded surface. Use \autoref{thm:znormal-root-downstream-totality} to confine downstream consumers to the displayed packet, and then use \autoref{thm:znormal-root-total-host-refusal} to refuse host conversion, quotient normalization, unrestricted evaluation, divergence detection, substrate halting, and global normalization. These clauses exhaust the sibling route and the refusal boundary together.
\end{proof}
