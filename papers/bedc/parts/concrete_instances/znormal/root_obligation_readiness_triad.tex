\begin{theorem}[$\ZNormalUp$ componentwise NameCert transport]
\label{thm:znormal-componentwise-namecert-transport}
Let
$$
Z=(T,F,V,K,R,H,C,P,N)
$$
and
$$
Z'=(T',F',V',K',R',H',C',P',N')
$$
be accepted $\ZNormalUp$ root packets over displayed $\BHist$ rows. If the componentwise $\hsame$ transport row $H$ relates $T,F,V,K,R,C,P,N$ to $T',F',V',K',R',C',P',N'$, then the local $\NameCert_{\ZNormalUp}$ surface transports from $Z$ to $Z'$ without adding a host normalization row, quotient conversion row, or divergence oracle. The transported surface remains the same displayed terminal typed endpoint, bounded fuel, terminal-normality, total-host refusal, continuation readback, $\Cont$ replay, $\Pkg$ provenance, and local naming packet.
\end{theorem}
\begin{proof}
Project both packets through \autoref{def:znormal-root-carrier-boundary}. The only readable coordinates are the displayed $\BHist$ rows $T,F,V,K,R,H,C,P,N$ and their primed counterparts.

Apply \autoref{thm:znormal-namecert-root-obligations}. The certificate surface is carrier admission, classifier exposure, terminal-normality evidence, bounded-fuel replay, non-escape, $\Pkg$ provenance, and local naming over exactly those rows.

The sibling chapters provide the component witnesses rather than host normalization. The typed endpoint row is read through \autoref{ch:concrete-instances-typedreductionnormalform-namecert}, the normal-word row through \autoref{ch:concrete-instances-zeckendorf-normal-namecert}, and the terminal continuation row through \autoref{ch:concrete-instances-continuationtracenormalform-namecert}.

Transport along $H$ therefore moves each visible component and then rereads the same route through $C$. Since no host normalization, quotient conversion, or divergence-oracle coordinate occurs in the carrier, the transported NameCert remains kernel-scoped.
\end{proof}
\leanchecked{BEDC.Derived.ZnormalUp.ZnormalPacket\_componentwise\_namecert\_transport}

\begin{theorem}[$\ZNormalUp$ total-host refusal boundary]
\label{thm:znormal-total-host-refusal-boundary}
For every accepted $\ZNormalUp$ root packet
$$
Z=(T,F,V,K,R,H,C,P,N),
$$
the total-host refusal row $K$ is part of the kernel-scoped $\NameCert_{\ZNormalUp}$ boundary. Any consumer that cites terminal normality from $V$ must also display $K$ together with the bounded fuel row $F$, continuation readback row $R$, $\hsame$ transport row $H$, $\Cont$ replay row $C$, $\Pkg$ provenance row $P$, and local $\NameCert$ row $N$. A consumer that omits $K$ has not crossed the total-host boundary and cannot export host conversion, quotient normalization, divergence detection, unrestricted evaluation, or global normalization as a $\ZNormalUp$ consequence.
\end{theorem}
\leanchecked{BEDC.Derived.ZnormalUp.ZnormalPacket\_total\_host\_refusal\_boundary}
\begin{proof}
Start with the root carrier boundary. The row $K$ is a coordinate of the displayed $\BHist$ packet, not a host-side comment about normalization.

Use \autoref{thm:znormal-namecert-root-obligations}. Terminal-normality evidence is accepted only after the bounded fuel row $F$ and the total-host refusal row $K$ have been displayed, and replay is restricted to the visible $\Cont$ route $C$.

Now read the sibling inputs. The typed reduction normal-form packet supplies a typed endpoint, the Zeckendorf normal packet supplies finite normal-word evidence, and the continuation trace normal-form packet supplies terminal continuation readback. None of these siblings supplies a host conversion, quotient normalization, divergence oracle, unrestricted evaluator, or global normalizer.

Therefore any consumer omitting $K$ lacks one of the boundary rows required by $\NameCert_{\ZNormalUp}$. The terminal-normality citation remains inside the finite kernel packet and cannot be promoted to a total-host export.
\end{proof}

\begin{theorem}[$\ZNormalUp$ terminal route exhaustion]
\label{thm:znormal-terminal-route-exhaustion}
Every terminal route exported by an accepted $\ZNormalUp$ root packet factors through
$$
T,F,V,K,R,H,C,P,N.
$$
After the terminal typed endpoint row $T$, bounded fuel row $F$, terminal-normality row $V$, total-host refusal row $K$, continuation readback row $R$, componentwise $\hsame$ transport row $H$, displayed $\Cont$ replay row $C$, $\Pkg$ provenance row $P$, and local $\NameCert_{\ZNormalUp}$ row $N$ have been read, no additional kernel row remains for host normalization, quotient conversion, divergence detection, unrestricted evaluation, global normalization, or a second terminal route.
\end{theorem}
\leanchecked{BEDC.Derived.ZnormalUp.ZnormalPacket\_terminal\_route\_exhaustion}
\begin{proof}
Apply \autoref{thm:znormal-source-route-totality}. A root consumer that cites terminal normality must expose the complete source route $T,F,V,K,R,H,C,P,N$.

Apply \autoref{thm:znormal-namecert-ledger-exhaustion}. The local ledger is exhausted by the displayed $\Pkg$ and $\NameCert$ rows over that same packet, so there is no second provenance or naming coordinate for a hidden terminal route.

Finally use \autoref{thm:znormal-terminal-boundary-nonescape}. It rules out host conversion, quotient normalization, divergence oracles, unrestricted evaluation, and global normalization from the terminal boundary. Thus the displayed route is exhausted once the nine kernel rows have been read.
\end{proof}

\begin{theorem}[$\ZNormalUp$ total-host field readiness]
\label{thm:znormal-total-host-field-readiness}
Before any total-host normality read is accepted from a $\ZNormalUp$ root packet, the rows
$$
T,F,V,K,R,H,C,P,N
$$
must be displayed fieldwise: terminal typed endpoint, bounded fuel, terminal-normality evidence, total-host refusal, continuation readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\ZNormalUp}$ naming.
\end{theorem}
\leanchecked{BEDC.Derived.ZnormalUp.ZnormalPacket\_total\_host\_field\_readiness}
\begin{proof}
Apply \autoref{thm:znormal-namecert-root-obligations}. The root certificate surface requires carrier admission, classifier exposure, terminal-normality evidence, bounded-fuel replay, non-escape, provenance, and local naming over the displayed packet.

Use \autoref{thm:znormal-source-route-totality}. A consumer that cites terminal normality must expose the full source route $T,F,V,K,R,H,C,P,N$.

Finally apply \autoref{thm:znormal-total-host-refusal-boundary}. It places $K$ inside the accepted boundary, so a total-host read cannot consume $V$ without also displaying the refusal row and the structural rows that keep the route kernel-scoped.
\end{proof}

\begin{theorem}[$\ZNormalUp$ terminal TasteGate readiness]
\label{thm:znormal-terminal-tastegate-readiness}
The displayed terminal endpoint, bounded fuel, terminal-normality row, total-host refusal row, continuation readback, componentwise transport, replay, provenance, and local NameCert rows are exactly the paper surface expected by later $\Nontrivial\ \ZNormalUp$ and $\FieldFaithful\ \ZNormalUp$ Lean instances. No such instance may read host conversion, quotient normalization, divergence detection, unrestricted evaluation, global normalization, or a second terminal route.
\end{theorem}
\begin{proof}
Begin with total-host field readiness from \autoref{thm:znormal-total-host-field-readiness}. It fixes the candidate TasteGate fields as $T,F,V,K,R,H,C,P,N$.

Use \autoref{thm:znormal-componentwise-namecert-transport}. Componentwise transport preserves the same terminal typed endpoint, bounded fuel, terminal-normality, refusal, replay, provenance, and naming packet without adding a host normalization row.

Then use \autoref{thm:znormal-terminal-route-exhaustion}. Once the nine rows have been read, no additional kernel row remains for a second terminal route or an excluded host operation.

Thus the later Lean instances have the exact field-faithful surface they need: nontriviality is witnessed by the presence of the nine distinct rows, and field faithfulness is the demand that consumers preserve those rows.
\end{proof}
\leanchecked{BEDC.Derived.ZnormalUp.ZnormalPacket\_terminal\_tastegate\_readiness}

\begin{theorem}[$\ZNormalUp$ root readiness export package]
\label{thm:znormal-root-readiness-export-package}
The componentwise transport, refusal boundary, terminal route exhaustion, and TasteGate readiness results assemble the $\ZNormalUp$ root unblock package. A downstream root consumer receives exactly the terminal typed endpoint, bounded fuel, terminal-normality evidence, total-host refusal, continuation readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\ZNormalUp}$ naming rows.
\end{theorem}
\leanchecked{BEDC.Derived.ZnormalUp.ZnormalPacket\_root\_readiness\_export\_certificate}
\begin{proof}
First apply componentwise transport from \autoref{thm:znormal-componentwise-namecert-transport}. It identifies the rowwise transport route and keeps it inside the displayed root packet.

Next apply the total-host refusal boundary, \autoref{thm:znormal-total-host-refusal-boundary}, together with total-host field readiness from \autoref{thm:znormal-total-host-field-readiness}. These results force $K$ and all structural rows to be visible before any terminal-normality export is consumed.

Use terminal route exhaustion, \autoref{thm:znormal-terminal-route-exhaustion}. It excludes host conversion, quotient normalization, divergence detection, unrestricted evaluation, global normalization, and a second terminal route after $T,F,V,K,R,H,C,P,N$ have been read.

Finally apply terminal TasteGate readiness from \autoref{thm:znormal-terminal-tastegate-readiness}. The same nine rows are the expected Nontrivial and FieldFaithful surface, so the root consumer receives a complete root unblock package rather than a host-level normalization theorem.
\end{proof}
