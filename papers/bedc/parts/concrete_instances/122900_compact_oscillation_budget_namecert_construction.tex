\chapter{A Concrete Naming Certificate for $\CompactOscillationBudgetUp$}
\label{ch:concrete-instances-compact-oscillation-budget-namecert}
\origin{human}

Following Bishop and Bridges on compact metric spaces and constructive uniform continuity, the $\CompactOscillationBudgetUp$ packet records the finite-net oscillation rows used before a continuous map on a compact source may expose a uniform-modulus or Real-facing readback. It sits beside the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map row of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus handoff of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the dyadic tolerance arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite compact-net oscillation budget; it does not assert host suprema, open-cover compactness, selected infinite subcovers, quotient equality of functions, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Compact oscillation budget carrier]
\label{def:compact-oscillation-budget-carrier}
A $\CompactOscillationBudgetUp$ carrier is a finite $\BHist$ packet
$$
B=(K,F,N,O,D,Q,R,U,H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row exposing the compact finite-net cells inspected by the packet.
\item $F$ is the $\ContinuousMapUp$ row whose local graph data are being tested on those cells.
\item $N$ is the finite compact-net row, listing the displayed centers, radii, and cell-membership witnesses.
\item $O$ is the finite oscillation row, assigning to each displayed cell the pointwise oscillation bound read from $F$ over $K$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row used to compare the requested precision with the cellwise oscillation rows.
\item $Q$ is the finite budget ledger folding the cellwise oscillation rows into one displayed compact budget.
\item $R$ is the $\RegSeqRatUp$ readback row for the folded rational budget.
\item $U$ is the $\UniformModulusUp$ or $\RealUp$ handoff row reached only after $K,F,N,O,D,Q,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the compact source, map, net, oscillation, dyadic, budget, readback, handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from compact-net cells and continuous-map oscillation rows through dyadic comparison, regular readback, and the terminal handoff.
\item $P$ is the $\Pkg$ provenance over $\CompactOscillationBudgetUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\CompactOscillationBudgetUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,F,N,O,D,Q,R,U,H,C,P,L$. It has no coordinate for host suprema of oscillation, open-cover compactness, selected infinite subcovers, quotient function equality, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[Compact oscillation budget NameCert obligations]
\label{thm:compact-oscillation-budget-namecert-obligations}
For every accepted $\CompactOscillationBudgetUp$ carrier
$$
B=(K,F,N,O,D,Q,R,U,H,C,P,L),
$$
the local $\NameCert_{\CompactOscillationBudgetUp}$ surface consists exactly of compact-source admission through $K$, continuous-map admission through $F$, finite compact-net exposure through $N$, pointwise cell oscillation rows through $O$, dyadic tolerance comparison through $D$, folded budget exposure through $Q$, regular rational readback through $R$, terminal uniform-modulus or Real handoff through $U$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, and local naming through $L$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-oscillation-budget-carrier}. Its analytic rows are exactly the compact source $K$, the continuous-map row $F$, the finite net $N$, the cellwise oscillation row $O$, the dyadic tolerance $D$, the folded budget $Q$, the regular readback $R$, and the terminal handoff $U$.

Read the finite-net route in order. The row $N$ lists only displayed cells over $K$, and $O$ records only the pointwise oscillation rows supplied by $F$ on those cells. The dyadic row $D$ and budget ledger $Q$ compare those finitely many rows without taking a host supremum.

The readback and handoff rows $R,U$ are downstream of that finite ledger. The structural rows $H,C,P,L$ transport, replay, source, and locally name the same packet, so the excluded open-cover, selected-subcover, quotient, choice, and ambient-completeness data are not part of the NameCert surface.
\end{proof}

\begin{theorem}[Compact oscillation budget finite-net route]
\label{thm:compact-oscillation-budget-finite-net-route}
Every $\UniformModulusUp$ or $\RealUp$ consumer of an accepted $\CompactOscillationBudgetUp$ packet must read the finite route
$$
\CompactMetricUp,\ContinuousMapUp,N,O,\DyadicRatCoreUp,Q,\RegSeqRatUp,U,H,C,P,L
$$
before using the exported compact oscillation budget. Thus the route is determined by compact-net cells, pointwise oscillation rows, dyadic tolerance comparison, regular rational readback, and the terminal handoff, rather than by host suprema or an ambient compactness theorem.
\end{theorem}
\begin{proof}
Start from the NameCert obligations of \autoref{thm:compact-oscillation-budget-namecert-obligations}. They force every consumer through the compact source, continuous-map row, finite net, oscillation ledger, dyadic comparison, budget fold, and regular readback before $U$ is public.

The compact finite-net cells are the only coverage data in the carrier. A consumer that asks for a different cover, a selected infinite subcover, or a host supremum of oscillation would need a coordinate absent from $B$.

Finally, transport and replay preserve the same finite dependency route through $H,C,P,L$. Therefore the exported budget is a BEDC finite NameCert route to $\UniformModulusUp$ or $\RealUp$, not an external compactness or supremum principle.
\end{proof}

\falsifiablePrediction{An accepted $\CompactOscillationBudgetUp$ consumer is refuted if it reaches $\UniformModulusUp$ or $\RealUp$ without displaying the compact metric source, continuous-map row, finite compact-net cells, pointwise oscillation rows, dyadic tolerance row, folded budget ledger, regular rational readback, transport, replay, provenance, and local NameCert row.}

\independenceWitness{compactmetric, continuousmap, uniformmodulus, dyadicratcore, regseqrat, real: none is bijective to $\CompactOscillationBudgetUp$ because this packet jointly binds compact finite-net cells, pointwise oscillation rows, dyadic tolerance comparison, a folded finite budget, regular rational readback, terminal handoff, transport, replay, provenance, and local naming.}

\closureat{CompactOscillationBudgetUp}{seedStr}

\begin{closurestatus}{\CompactOscillationBudgetUp}
  \constructivestory{A $\CompactOscillationBudgetUp$ packet is generated from CompactMetric finite-net cells, a ContinuousMap row, pointwise oscillation rows, DyadicRatCore tolerance comparison, a folded finite budget ledger, RegSeqRat readback, UniformModulus or RealUp handoff data, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-oscillation-budget-carrier}, \autoref{thm:compact-oscillation-budget-namecert-obligations}, and \autoref{thm:compact-oscillation-budget-finite-net-route} seed the finite compact-net oscillation route from compact and continuous rows into uniform-modulus and Real-facing consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, host suprema of oscillation, open-cover compactness, selected infinite subcovers, quotient function equality, countable choice, or ambient Real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source exposure, continuous-map exposure, finite-net exposure, oscillation-row exactness, dyadic budget comparison, regular readback, terminal handoff nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactOscillationBudgetUp.}
\end{closurestatus}
