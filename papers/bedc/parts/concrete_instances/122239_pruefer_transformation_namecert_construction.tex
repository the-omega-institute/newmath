\chapter{A Concrete Naming Certificate for $\PrueferTransformationUp$}
\label{ch:concrete-instances-pruefer-transformation-namecert}
\origin{human}

Following the Sturm--Liouville and Pruefer phase-transform tradition, the $\PrueferTransformationUp$ packet records the finite phase-angle read by which a displayed second-order real differential system is routed from amplitude and derivative rows into a Sturm-facing oscillation surface. It sits after the Wronskian determinant corridor of \autoref{ch:concrete-instances-wronskian-namecert}, the separation consumer of \autoref{ch:concrete-instances-sturm-separation-namecert}, the root-counting consumer of \autoref{ch:concrete-instances-sturm-root-count-namecert}, the derivative-admission route of \autoref{ch:concrete-instances-derivative-namecert}, the continuous-map admission route of \autoref{ch:concrete-instances-continuousmap-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only a displayed finite phase-angle carrier; it does not import full spectral theory, selected eigenfunctions, host phase equality, quotient real equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Pruefer transformation carrier]
\label{def:pruefer-transformation-carrier}
A $\PrueferTransformationUp$ carrier is a finite $\BHist$ packet
$$
T=(F,D,A,\Theta,W,S,Z,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the source function-family row, read through $\ContinuousMapUp$ before any phase request is inspected.
\item $D$ is the derivative-admission row, exposing the first derivative and second-order source data only through $\DerivativeUp$.
\item $A$ is the amplitude ledger, recording the displayed radius-squared and nonzero-window requests used before a phase angle is named.
\item $\Theta$ is the phase-angle row, listing the finite angle windows produced from the displayed amplitude and derivative rows.
\item $W$ is the $\WronskianUp$ determinant handoff row, keeping zero and nonzero separation reads attached to the finite determinant ledger.
\item $S$ is the Sturm consumer row, available only through the $\SturmSeparationUp$ and $\SturmRootCountUp$ sibling routes.
\item $Z$ is the zero-window discipline row, recording which displayed windows may be treated as phase-crossing candidates.
\item $R$ is the regular rational readback row through $\RegSeqRatUp$.
\item $E$ is the terminal $\RealUp$ seal reached only after the $\DyadicRatCoreUp$ tolerance ledger and $R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the source, derivative, amplitude, phase, Wronskian, Sturm, zero-window, readback, seal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a phase request through source admission, derivative admission, amplitude readback, Wronskian handoff, Sturm consumer routing, dyadic tolerance, regular rational readback, Real sealing, transport, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\PrueferTransformationUp$, $\WronskianUp$, $\SturmSeparationUp$, $\SturmRootCountUp$, $\DerivativeUp$, $\ContinuousMapUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PrueferTransformationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a host phase function, a selected eigenfunction, quotient real equality, countable choice, full spectral decomposition, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Pruefer transformation NameCert obligations]
\label{thm:pruefer-transformation-namecert-obligations}
For every accepted $\PrueferTransformationUp$ carrier
$$
T=(F,D,A,\Theta,W,S,Z,R,E,H,C,P,N),
$$
the local $\NameCert_{\PrueferTransformationUp}$ surface consists exactly of carrier admission for the displayed packet, continuous source admission through $F$, derivative admission through $D$, amplitude and phase-window exposure through $A,\Theta,Z$, Wronskian determinant handoff through $W$, Sturm separation and root-count consumer routing through $S$, dyadic tolerance and regular rational readback through $R,E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, local naming through $N$, and nonescape from full spectral theory, selected eigenfunctions, host phase equality, quotient real equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:pruefer-transformation-carrier}. The tuple displays source functions, derivative rows, amplitude windows, phase windows, Wronskian handoff, Sturm consumer routing, zero-window discipline, regular rational readback, Real sealing, transport, replay, provenance, and local naming as separate rows.

The source and differentiability data are read first. The rows $F$ and $D$ force every phase request through $\ContinuousMapUp$ and $\DerivativeUp$, so the carrier cannot begin from an unnamed host solution or an external differentiability witness.

Next inspect $A,\Theta,Z,W,S$. These rows expose only finite amplitude and phase windows, route determinant separation through $\WronskianUp$, and send Sturm-facing reads through the displayed $\SturmSeparationUp$ and $\SturmRootCountUp$ sibling surfaces.

Finally pass through $R,E,H,C,P,N$. The regular rational readback, dyadic tolerance, terminal Real seal, transport, replay, provenance, and naming rows preserve the same finite route. Since no row supplies spectral decomposition, selected eigenfunctions, host phase equality, quotient real equality, countable choice, or ambient $\RealUp$ completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Pruefer transformation Wronskian--Sturm handoff]
\label{thm:pruefer-transformation-wronskian-sturm-handoff}
Every $\SturmSeparationUp$ or $\SturmRootCountUp$ consumer that reads a $\PrueferTransformationUp$ phase window must first expose the Wronskian row $W$, the source and derivative rows $F,D$, the amplitude and phase rows $A,\Theta,Z$, and the Real-facing readback rows $R,E$. The consumer receives the handoff only as a displayed route through $\WronskianUp$, $\DerivativeUp$, $\ContinuousMapUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$, with no host zero theorem, selected eigenfunction, host phase equality, quotient real equality, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Start with a Sturm-facing phase request. The carrier makes the phase row $\Theta$ downstream of the source row $F$, derivative row $D$, and amplitude ledger $A$, so no phase read is available before the continuous and derivative admissions have been displayed.

Use the Wronskian row $W$ to route zero and nonzero separation through the determinant corridor. The consumer therefore reads the same finite Wronskian surface before it can treat a phase crossing as a separation or root-count input.

Now inspect the Sturm row $S$. It sends the read to the $\SturmSeparationUp$ and $\SturmRootCountUp$ sibling chapters as consumers of the displayed finite route, not as new coordinates inside the transformation packet.

Finally apply the readback and seal rows $R,E$ together with $H,C,P,N$. They preserve the handoff under regular rational readback, dyadic tolerance, Real sealing, componentwise transport, replay, provenance, and local naming. The refused endpoints are absent because none of these rows names a host zero theorem, selected eigenfunction, host phase equality, quotient real equality, countable-choice schedule, or ambient completeness principle.
\end{proof}

\falsifiablePrediction{Every accepted $\PrueferTransformationUp$ read displays continuous source rows, derivative rows, amplitude and phase windows, Wronskian determinant handoff, Sturm separation and root-count consumer routing, dyadic tolerance, regular rational readback, terminal Real sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{wronskian, sturm-separation, sturm-root-count, derivative, continuousmap, dyadicratcore, regseqrat, real: $\PrueferTransformationUp$ is not bijective to any listed sibling because it binds amplitude and phase windows, Wronskian determinant handoff, zero-window discipline, Sturm consumer routing, and Real-facing readback into one finite NameCert packet.}

\closureat{PrueferTransformationUp}{seedStr}

\begin{closurestatus}{\PrueferTransformationUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:pruefer-transformation-carrier}, \autoref{thm:pruefer-transformation-namecert-obligations}, and \autoref{thm:pruefer-transformation-wronskian-sturm-handoff} seed the finite source, derivative, amplitude, phase-window, Wronskian, Sturm-consumer, dyadic, readback, Real-seal, transport, replay, provenance, and local naming route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim full spectral theory, selected eigenfunctions, host phase equality, quotient real equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, continuous and derivative admission, amplitude and phase-window exactness, Wronskian determinant handoff, Sturm consumer routing, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.PrueferTransformationUp.}
  \constructivestory{A $\PrueferTransformationUp$ packet is generated from displayed source rows, derivative rows, amplitude and phase windows, a Wronskian determinant handoff, Sturm separation and root-count consumer routing, dyadic tolerance, regular rational readback, a terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
