\chapter{A Concrete Naming Certificate for $\LowerSemicontinuousRegularizationUp$}
\label{ch:concrete-instances-lower-semicontinuous-regularization-namecert}
\origin{human}

Following Bourbaki and Bishop-style constructive analysis, the $\LowerSemicontinuousRegularizationUp$ packet records the finite BEDC surface by which a real-valued metric datum is replaced by its displayed lower-semicontinuous envelope. It sits after the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the regular-rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the lower-semicontinuity packet of \autoref{ch:concrete-instances-lowersemicontinuous-namecert}. The packet names only displayed finite windows and refusals; it does not import host infima, selected limits, quotient functions, countable choice, or ambient completion.

\begin{definition}[Semicontinuous envelope carrier]
\label{def:semicontinuous-envelope-carrier}
A $\LowerSemicontinuousRegularizationUp$ carrier is a finite $\BHist$ packet
$$
E=(S,M,R,D,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source $\RealUp$ row naming the real-valued datum whose lower envelope is being read.
\item $M$ is the $\MetricUp$ row naming the displayed neighbourhood or ball requests over which the envelope is inspected.
\item $R$ is the $\RegSeqRatUp$ readback row for rational approximants attached to those metric windows.
\item $D$ is the dyadic lower-bound window row, carrying the finite lower estimates that a consumer may inspect.
\item $L$ is the $\LowerSemicontinuousUp$ row stating that accepted reads preserve lower-window stability under displayed metric refinement.
\item $H$ records componentwise $\hsame$ transport for the source, metric, readback, dyadic-window, lower-stability, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from source data through metric windows, regular-rational readback, dyadic lower bounds, and the lower-semicontinuity row.
\item $P$ is the $\Pkg$ provenance over $\LowerSemicontinuousRegularizationUp$, $\RealUp$, $\MetricUp$, $\RegSeqRatUp$, $\LowerSemicontinuousUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LowerSemicontinuousRegularizationUp}$ row.
\end{enumerate}
The classifier compares accepted envelopes only through these nine rows. It has no coordinate for hidden infima, selected minimizing sequences, quotient functions, host real equality, countable choice, or ambient lower-completion principles.
\end{definition}

\begin{theorem}[Semicontinuous envelope NameCert obligations]
\label{thm:semicontinuous-envelope-namecert-obligations}
For every accepted $\LowerSemicontinuousRegularizationUp$ carrier
$$
E=(S,M,R,D,L,H,C,P,N),
$$
the local $\NameCert_{\LowerSemicontinuousRegularizationUp}$ surface consists exactly of source-real exposure through $S$, metric-window exposure through $M$, regular-rational readback through $R$, dyadic lower-bound exposure through $D$, lower-semicontinuity stability through $L$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:semicontinuous-envelope-carrier}. The source row, metric row, readback row, dyadic lower-window row, and lower-semicontinuity row are all displayed before the local name is admitted.

Read the analytic route in dependency order. The source real datum is inspected only through $S$; metric refinement requests are named by $M$; regular-rational witnesses are read back through $R$; and the dyadic lower-bound ledger $D$ records the finite estimates available to consumers.

Finally use $L,H,C,P,N$. Lower-semicontinuity is a displayed stability row, and transport, replay, provenance, and local naming preserve the same packet. Since the carrier has no hidden infimum or selected-limit coordinate, the listed rows exhaust the NameCert obligation surface.
\end{proof}

\begin{theorem}[Semicontinuous envelope lower-upper stability]
\label{thm:semicontinuous-envelope-lower-upper-stability}
Every accepted $\LowerSemicontinuousRegularizationUp$ consumer must preserve the dyadic lower-bound windows $D$ together with the source, metric, readback, and lower-semicontinuity rows $S,M,R,L$. The packet may export lower-envelope stability under displayed metric refinement, but it cannot be consumed as a selected pointwise infimum, a hidden upper regularity theorem, a quotient-function equality, or a final real-name oracle.
\end{theorem}
\begin{proof}
Start from the NameCert obligations of \autoref{thm:semicontinuous-envelope-namecert-obligations}. They place $D$ in the same packet as $S,M,R,L$, so lower bounds are not detachable annotations.

Apply the classifier clause in \autoref{def:semicontinuous-envelope-carrier}. Accepted comparison sees the dyadic window row and the lower-semicontinuity row together with componentwise transport and displayed replay. A consumer that drops $D$ or $L$ therefore leaves the accepted envelope surface.

The exclusions follow from the absent coordinates. The carrier contains no selected infimum, no hidden upper envelope, no quotient-function equality, and no final real-name oracle, while $H,C,P,N$ only transport, replay, source, and name the displayed rows already present.
\end{proof}

\falsifiablePrediction{A lower-envelope consumer that asks for a selected infimum or a quotient-function representative without displaying the dyadic lower-bound window row is rejected by the LowerSemicontinuousRegularizationUp classifier.}

\independenceWitness{real, metric, regseqrat, lowersemicontinuous: none is bijective because the regularization packet binds source real data, metric windows, regular-rational readback, dyadic lower bounds, lower-semicontinuity stability, transport, replay, provenance, and local NameCert rows in one carrier.}

\closureat{LowerSemicontinuousRegularizationUp}{seedStr}

\begin{closurestatus}{\LowerSemicontinuousRegularizationUp}
  \constructivestory{A $\LowerSemicontinuousRegularizationUp$ packet is generated from source Real rows, Metric window rows, RegSeqRat readback rows, dyadic lower-bound windows, a LowerSemicontinuous stability row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:semicontinuous-envelope-carrier}, \autoref{thm:semicontinuous-envelope-namecert-obligations}, and \autoref{thm:semicontinuous-envelope-lower-upper-stability} seed the finite lower-semicontinuous regularization route over $\RealUp$, $\MetricUp$, $\RegSeqRatUp$, $\LowerSemicontinuousUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, selected infima, quotient functions, countable choice, ambient completion, hidden upper regularity, or final real-name oracle behavior.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic lower-window exposure, lower-semicontinuity stability, transport, replay, provenance, and local naming for BEDC.Derived.LowerSemicontinuousRegularizationUp.}
\end{closurestatus}
