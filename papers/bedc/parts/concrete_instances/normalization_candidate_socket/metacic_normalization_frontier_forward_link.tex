The $\NormalizationCandidateSocketUp$ route is consumed by \autoref{ch:concrete-instances-metacicnormalizationfrontier-namecert} and \autoref{ch:concrete-instances-metacic-candidate-normalization-witness-namecert} only after the candidate evidence rows, endpoint obstruction row, and socket rows remain visible in the same finite packet. The frontier read therefore passes through displayed candidate evidence rather than treating the socket as a theorem-free normalization endpoint.
