The $\NormalizationCandidateSocketUp$ packet reads the subject-reduction discharge surface through \autoref{ch:concrete-instances-metaciccriticalpath-namecert} and the normalization-frontier sibling of \autoref{ch:concrete-instances-metacicnormalizationfrontier-namecert}. A candidate-normalization consumer must therefore expose the critical-path subject-reduction budget, the displayed normalization frontier rows, and the socket-local candidate evidence before treating the socket as a closed proof. The route is a forward dependency edge for MetaCIC normalization discharge, not an independent strong-normalization theorem or a replacement for the subject-reduction proof surface.
