\begin{theorem}[NormalizationCandidateSocket frontier cross-reference]
\label{thm:normalization-candidate-socket-frontier-crossref}
Every $\MetaCICNormalizationFrontierUp$ consumer of a $\NormalizationCandidateSocketUp$ packet must first pass through the candidate evidence rows, endpoint obstruction rows, socket rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row before any normalization endpoint is read. The route reaches the frontier consumer of \autoref{ch:concrete-instances-metacicnormalizationfrontier-namecert} and the candidate witness surface of \autoref{ch:concrete-instances-metacic-candidate-normalization-witness-namecert} only after those rows remain visible in the same finite packet.
\end{theorem}
\begin{proof}
Project the accepted socket packet. Its public coordinates already contain the candidate evidence rows, endpoint obstruction rows, socket rows, transport, replay, provenance, and local naming.

The frontier route then consumes those displayed rows through the sibling dependency named in \autoref{ch:concrete-instances-metacicnormalizationfrontier-namecert}. The witness route of \autoref{ch:concrete-instances-metacic-candidate-normalization-witness-namecert} is available only as part of the same displayed candidate evidence surface.

Since no carrier coordinate stores an already-read endpoint or an external normalization result, any endpoint-facing read must traverse the listed rows before it reaches the frontier consumer.
\end{proof}

\begin{theorem}[NormalizationCandidateSocket frontier dependency]
\label{thm:normalization-candidate-socket-frontier-dependency}
Every endpoint-facing $\NormalizationCandidateSocketUp$ read must expose candidate evidence rows, frontier refusal rows, $\MetaCICNormalizationFrontierUp$, $\MetaCICCriticalPathUp$, and candidate-normalization witness rows before it exports a normalization endpoint. The conclusion is conditional on the displayed socket packet and claims no global strong normalization, subject reduction, confluence, evaluator equality, or kernel consistency.
\end{theorem}
\begin{proof}
Start with the frontier cross-reference of \autoref{thm:normalization-candidate-socket-frontier-crossref}. It already routes a frontier consumer through candidate evidence, endpoint obstruction, socket, transport, replay, provenance, and local naming rows before any normalization endpoint is read.

Use the socket boundary theorem from the parent chapter. The endpoint-facing surface of $\NormalizationCandidateSocketUp$ is therefore not available until the finite packet has displayed the candidate evidence and refusal rows that keep the endpoint conditional.

The $\MetaCICNormalizationFrontierUp$ row supplies the frontier side of that refusal, while the $\MetaCICCriticalPathUp$ row records the critical-path dependency that must remain visible to endpoint consumers. The candidate-normalization witness rows are read as part of the same finite packet, not as an external evaluator equality or kernel theorem.

Thus an endpoint-facing read can export only the conditional endpoint licensed by those displayed rows. It cannot turn the socket into global strong normalization, subject reduction, confluence, evaluator equality, or kernel consistency.
\end{proof}
