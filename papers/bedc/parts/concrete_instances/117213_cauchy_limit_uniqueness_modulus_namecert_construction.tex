\chapter{A Concrete Naming Certificate for $\CauchyLimitUniquenessModulusUp$}
\label{ch:concrete-instances-cauchy-limit-uniqueness-modulus-namecert}
\origin{human}

Following Bishop and Bridges, the $\CauchyLimitUniquenessModulusUp$ packet records the finite modulus data used to compare two regular Cauchy real-limit routes without quotienting streams or appealing to host real equality. It sits after the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular Cauchy limit uniqueness packet of \autoref{ch:concrete-instances-regular-cauchy-limit-uniqueness-namecert}, and the real-limit uniqueness seal of \autoref{ch:concrete-instances-real-limit-uniqueness-seal-namecert}. The packet names a displayed finite-means uniqueness modulus for Real and Completion consumers; it is not a completed-real equality theorem, a quotient-stream principle, a selected limit, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Cauchy limit uniqueness modulus carrier]
\label{def:cauchy-limit-uniqueness-modulus-carrier}
A $\CauchyLimitUniquenessModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(L_0,L_1,B,W,R,U,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L_0$ and $L_1$ are the two regular Cauchy limit source rows whose terminal reads are being compared.
\item $B$ is the $\DyadicRatCoreUp$ tail-budget row naming the requested dyadic radius and its common comparison scale.
\item $W$ is the paired $\StreamNameUp$ finite tail-window row, including the two observations used after the displayed unary threshold.
\item $R$ is the $\RegSeqRatUp$ readback row for the two scheduled tails.
\item $U$ is the $\RegularCauchyLimitUniquenessUp$ route row binding the paired tails to one uniqueness read.
\item $E$ is the terminal $\RealUp$ seal row, available only after $B,W,R,U$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for $L_0,L_1,B,W,R,U,E,C,P,N$.
\item $C$ records displayed $\Cont$ replay through the tail-budget, window, readback, uniqueness, and terminal seal rows.
\item $P$ is the $\Pkg$ provenance over $\CauchyLimitUniquenessModulusUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RegularCauchyLimitUniquenessUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CauchyLimitUniquenessModulusUp}$ row.
\end{enumerate}
The carrier has no coordinate for host real equality, quotient stream equality, a selected limiting representative, a hidden modulus oracle, countable choice, trichotomy, or ambient completion.
\end{definition}

\begin{definition}[Cauchy limit uniqueness modulus classifier]
\label{def:cauchy-limit-uniqueness-modulus-classifier}
The $\CauchyLimitUniquenessModulusUp$ classifier identifies accepted packets only through displayed agreement of $L_0,L_1,B,W,R,U,E,H,C,P,N$. In particular, the classifier refuses equality that preserves the two source rows but changes the dyadic budget, the finite StreamName observations, the RegSeqRat readback, the uniqueness route, the terminal Real seal, or any structural transport, replay, provenance, or local naming row.
\end{definition}

\begin{theorem}[Cauchy limit uniqueness modulus NameCert obligations]
\label{thm:cauchy-limit-uniqueness-modulus-namecert}
For every accepted $\CauchyLimitUniquenessModulusUp$ carrier $M=(L_0,L_1,B,W,R,U,E,H,C,P,N)$, the local $\NameCert_{\CauchyLimitUniquenessModulusUp}$ surface consists exactly of carrier admission, classifier exactness, dyadic tail-budget stability, finite StreamName window stability, RegSeqRat readback compatibility, regular Cauchy uniqueness routing, terminal Real-seal exposure, componentwise transport, displayed replay, package provenance, and local naming. Every uniqueness read for a requested dyadic radius factors through these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-limit-uniqueness-modulus-carrier}. The analytic rows are the paired source rows $L_0,L_1$, dyadic tail budget $B$, finite window $W$, regular readback $R$, uniqueness route $U$, and terminal seal $E$.

Apply the classifier of \autoref{def:cauchy-limit-uniqueness-modulus-classifier}. It makes the five NameCert obligations visible: carrier admission for the displayed tuple, exact comparison through the classifier row, stability of the dyadic and StreamName windows, ledger compatibility for the RegSeqRat and Real-seal route, and nonescape from the structural rows $H,C,P,N$.

The regular Cauchy uniqueness route is read through \autoref{ch:concrete-instances-regular-cauchy-limit-uniqueness-namecert} and the terminal seal is read through \autoref{ch:concrete-instances-real-limit-uniqueness-seal-namecert}. Since neither route supplies host real equality, quotient stream equality, a selected limit, or ambient completion, every requested dyadic radius remains inside the displayed finite modulus ledger.
\end{proof}

\falsifiablePrediction{A Real-facing uniqueness consumer that identifies two regular Cauchy limit reads while omitting the dyadic budget row, the paired StreamName observations, the RegSeqRat readback, or the regular Cauchy uniqueness route is rejected by the $\CauchyLimitUniquenessModulusUp$ classifier.}

\independenceWitness{dyadicratcore, streamname, regseqrat, regular-cauchy-limit-uniqueness, real-limit-uniqueness-seal, real: no listed sibling is bijective to $\CauchyLimitUniquenessModulusUp$ because this packet jointly carries two source rows, one dyadic tail budget, paired finite observations, regular readback, uniqueness routing, terminal Real sealing, transport, replay, provenance, and local NameCert naming.}

\closureat{CauchyLimitUniquenessModulusUp}{seedStr}
\begin{closurestatus}{\CauchyLimitUniquenessModulusUp}
  \constructivestory{A $\CauchyLimitUniquenessModulusUp$ packet is generated from paired regular Cauchy limit rows, a DyadicRatCore tail budget, finite StreamName observations, RegSeqRat readback, RegularCauchyLimitUniqueness routing, a RealUp terminal seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-limit-uniqueness-modulus-carrier}, \autoref{def:cauchy-limit-uniqueness-modulus-classifier}, and \autoref{thm:cauchy-limit-uniqueness-modulus-namecert} seed the finite uniqueness-modulus route over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RegularCauchyLimitUniquenessUp$, and $\RealUp$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, quotient stream equality, host real equality, selected limits, countable choice, trichotomy, or ambient Real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic budget, paired finite observations, RegSeqRat readback, uniqueness route, terminal Real seal, transport, replay, provenance, and local naming for BEDC.Derived.CauchyLimitUniquenessModulusUp.}
\end{closurestatus}
