\chapter{A Concrete Naming Certificate for $\CompleteRealModulusUp$}
\label{ch:concrete-instances-complete-real-modulus-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\CompleteRealModulusUp$ packet records the finite modulus data by which a completion-facing real read is accepted without appealing to ambient completeness. It sits after the Cauchy-filter limit route of \autoref{ch:concrete-instances-cauchyfilterlimit-namecert}, the regular-rational sequence discipline of \autoref{ch:concrete-instances-regseqrat-namecert}, the stream-window naming surface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. It also shares its modulus-facing boundary with \autoref{ch:concrete-instances-realcauchymodulus-namecert}: the point is not to add a new completion principle, but to name the displayed modulus route that lets completion consumers read a Real-facing limit through finite windows and tolerances.

\begin{definition}[CompleteRealModulus carrier]
\label{def:complete-real-modulus-carrier}
A $\CompleteRealModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(F,S,D,R,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\CauchyFilterLimitUp$ source row naming the Cauchy-filter data whose completion read is being inspected.
\item $S$ is the $\StreamNameUp$ finite-window row used to expose the inspected stream segments.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row recording the displayed dyadic budget for the read.
\item $R$ is the $\RegSeqRatUp$ regular-rational readback row reached after the stream windows and dyadic tolerances are displayed.
\item $Q$ is the complete-modulus row, recording the finite rule that sends each requested output tolerance to the displayed filter window, stream segment, and regular-rational comparison needed for the read.
\item $E$ is the terminal $\RealUp$ seal row. It is visible only after $F,S,D,R,Q$ have supplied the displayed route.
\item $H$ records componentwise $\hsame$ transport for the Cauchy-filter source, stream windows, dyadic tolerances, regular readback, modulus row, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a completion-modulus request through $F,S,D,R,Q,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CompleteRealModulusUp$, $\CauchyFilterLimitUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CompleteRealModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $F,S,D,R,Q,E,H,C,P,N$. It has no coordinate for a selected limit, quotient filter, host equality of reals, countable choice, or an ambient completeness theorem.
\end{definition}

\begin{theorem}[CompleteRealModulus NameCert obligations]
\label{thm:complete-real-modulus-namecert-obligations}
For every accepted $\CompleteRealModulusUp$ carrier
$$
M=(F,S,D,R,Q,E,H,C,P,N),
$$
the local $\NameCert_{\CompleteRealModulusUp}$ surface consists exactly of carrier admission for $M$, Cauchy-filter source exposure through $F$, stream-window exposure through $S$, dyadic tolerance exposure through $D$, regular-rational readback through $R$, complete-modulus accountability through $Q$, terminal Real sealing through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:complete-real-modulus-carrier}. The analytic source is not an ambient completion object; it is the displayed Cauchy-filter row $F$ together with finite stream windows $S$ and dyadic tolerances $D$.

The readback rows are ordered. The regular-rational row $R$ is reached only after the relevant stream windows and dyadic comparisons are present, and the modulus row $Q$ records how each requested tolerance is answered by those displayed rows.

The Real-facing row $E$ is terminal for this packet. It seals the displayed completion read after $F,S,D,R,Q$ have been exposed, while $H,C,P,N$ transport, replay, source, and name the same route.

Since no row stores a selected limit, quotient filter, host equality, countable choice schedule, or ambient completeness theorem, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[CompleteRealModulus threshold seal route]
\label{thm:complete-real-modulus-threshold-seal-route}
Every Real-facing consumer of an accepted $\CompleteRealModulusUp$ packet must read a completion threshold through the finite route
$$
\CauchyFilterLimitUp \longmapsto \StreamNameUp \longmapsto \DyadicRatCoreUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
as instantiated by $F,S,D,R,Q,E$. In particular, the terminal seal is available only after the Cauchy-filter source, stream windows, dyadic tolerance row, regular-rational readback, complete-modulus row, componentwise transport, continuation replay, provenance, and local NameCert row have been displayed.
\end{theorem}
\begin{proof}
Start with an accepted carrier $M=(F,S,D,R,Q,E,H,C,P,N)$. The source row $F$ names the Cauchy-filter limit request, so the consumer has no detached completion carrier to inspect.

The finite observation route then passes through $S,D,R$. Stream windows expose the inspected name segments, dyadic tolerances record the requested threshold, and regular-rational readback presents the comparison data in a form that can face the Real seal.

The complete-modulus row $Q$ binds these observations to every requested output tolerance. The terminal row $E$ can therefore be read only after the threshold route is displayed, and the structural rows $H,C,P,N$ preserve that same route under transport, replay, provenance, and local naming.

Thus a Real-facing completion consumer receives a finite threshold-seal packet, not a selected limit, quotient filter, host equality, countable choice principle, or ambient completeness claim.
\end{proof}

\falsifiablePrediction{Every accepted $\CompleteRealModulusUp$ completion-threshold read factors through one CauchyFilterLimitUp source row, one StreamNameUp finite-window row, one DyadicRatCoreUp tolerance row, one RegSeqRatUp readback row, one complete-modulus row, one RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{realcauchymodulus, cauchyfilterlimit, regseqrat, streamname, dyadicratcore, real: $\CompleteRealModulusUp$ is not bijective to any listed sibling because it packages the completion-threshold rule, source filter row, finite windows, dyadic tolerances, regular readback, terminal Real seal, transport, replay, provenance, and local NameCert as one certificate.}

\closureat{CompleteRealModulusUp}{seedStr}

\begin{closurestatus}{\CompleteRealModulusUp}
  \constructivestory{A $\CompleteRealModulusUp$ packet is generated from a CauchyFilterLimitUp source row, StreamNameUp finite windows, DyadicRatCoreUp tolerances, RegSeqRatUp readback, a complete-modulus row, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:complete-real-modulus-carrier}, \autoref{thm:complete-real-modulus-namecert-obligations}, and \autoref{thm:complete-real-modulus-threshold-seal-route} seed the finite completion-threshold route from Cauchy-filter source data to regular-rational readback and the Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient Real completeness, quotient completion, selected limits, host equality, countable choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Cauchy-filter source exposure, stream-window exposure, dyadic tolerance exposure, regular-rational readback, modulus accountability, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompleteRealModulusUp.}
\end{closurestatus}
