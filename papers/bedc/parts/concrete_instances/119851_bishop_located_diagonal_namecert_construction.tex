\chapter{A Concrete Naming Certificate for $\BishopLocatedDiagonalUp$}
\label{ch:concrete-instances-bishop-located-diagonal-namecert}
\origin{human}

Following Bishop and Bridges on constructive analysis, the $\BishopLocatedDiagonalUp$ packet records the finite route by which a located diagonal subsequence is read from displayed rational and stream data. It sits over the dyadic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the stream naming surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational sequence carrier of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The packet also consumes diagonal-extraction sibling rows such as $\RegularCauchyDiagonalExtractionUp$ and $\RegularCauchyDiagonalizationUp$ before it exports a located diagonal read. It is a finite naming certificate, not countable choice, quotient stream equality, selected host subsequences, or ambient $\RealUp$ completeness.

\section{Carrier definition}
\label{sec:bishop-located-diagonal-carrier}

\begin{definition}[BishopLocatedDiagonal carrier]
\label{def:bishop-located-diagonal-carrier}
A $\BishopLocatedDiagonalUp$ carrier is a finite $\BHist$ packet
$$
D=(Q,W,R,E,X,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the $\DyadicRatCoreUp$ tolerance ledger for the diagonal error budgets, comparison cells, and slack conversions.
\item $W$ is the $\StreamNameUp$ finite-window row exposing the inspected source stream entries and their chosen diagonal indices.
\item $R$ is the $\RegSeqRatUp$ readback row reached after the dyadic tolerances and stream windows have been displayed.
\item $E$ is the terminal $\RealUp$ seal for the located real endpoint read from the regular rational diagonal.
\item $X$ is the diagonal-extraction sibling row, read through $\RegularCauchyDiagonalExtractionUp$ and $\RegularCauchyDiagonalizationUp$, that records the finite extraction handoff.
\item $L$ is the locatedness ledger, recording the displayed lower and upper rational comparisons used by the diagonal request.
\item $H$ records componentwise $\hsame$ transport for $Q,W,R,E,X,L$.
\item $C$ records displayed $\Cont$ replay of the same finite diagonal route.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedDiagonalUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, the diagonal-extraction siblings, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopLocatedDiagonalUp}$ row.
\end{enumerate}
The classifier accepts exactly packets exposing these rows and compares them componentwise through the inherited classifiers. It has no coordinate for countable choice, quotient stream equality, host-level subsequence identity, hidden real completeness, or an unrecorded diagonal selector.
\end{definition}
\leanchecked{BEDC.Derived.BishopLocatedDiagonalUp.TasteGate.BishopLocatedDiagonalTasteGate\_single\_carrier\_alignment}

\section{Certificate obligations}
\label{sec:bishop-located-diagonal-obligations}

\begin{theorem}[BishopLocatedDiagonal NameCert obligations]
\label{thm:bishop-located-diagonal-namecert-obligations}
For every accepted $\BishopLocatedDiagonalUp$ carrier $D=(Q,W,R,E,X,L,H,C,P,N)$, the local $\NameCert_{\BishopLocatedDiagonalUp}$ surface consists exactly of dyadic tolerance exposure through $Q$, finite stream-window exposure through $W$, regular rational readback through $R$, real seal non-escape through $E$, diagonal-extraction handoff through $X$, locatedness comparison through $L$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The rows $Q,W,R,E,X,L,H,C,P,N$ are the full visible inventory, so any consumer of the diagonal read must begin with the dyadic tolerance ledger, finite stream windows, regular rational readback, terminal real seal, extraction row, and locatedness ledger.

The inherited dependencies fix the numeric route. The dyadic row is read through \autoref{ch:concrete-instances-dyadicratcore-namecert}, the stream windows through \autoref{ch:concrete-instances-streamname-namecert}, the regular readback through \autoref{ch:concrete-instances-regseqrat-namecert}, and the final seal through \autoref{ch:concrete-instances-real-namecert}.

The sibling row $X$ then records which finite diagonal-extraction handoff is being consumed. Since the structural rows $H,C,P,N$ only transport, replay, source, and name the same displayed route, no countable choice principle, quotient stream equality, selected host subsequence, or ambient completeness row is introduced.
\end{proof}

\begin{theorem}[BishopLocatedDiagonal window handoff]
\label{thm:bishop-located-diagonal-window-handoff}
Every $\BishopLocatedDiagonalUp$ consumer that reads a located diagonal endpoint must pass through the finite window route
$$
\DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp,\ \RegularCauchyDiagonalExtractionUp,\ \RegularCauchyDiagonalizationUp
\longmapsto
Q,W,R,E,X,L,H,C,P,N .
$$
The handoff exports a located diagonal ledger over finite windows and dyadic budgets; it does not export a global selector, countable choice, quotient stream equality, or a real-completeness theorem.
\end{theorem}
\begin{proof}
Read the carrier of \autoref{def:bishop-located-diagonal-carrier}. The dyadic row $Q$ and stream-window row $W$ are exposed before any regular rational readback can be consumed.

Next consume the regular sequence and real seal rows $R,E$. These rows only state that the displayed finite window and tolerance route reaches the named real boundary; they do not supply host equality of streams or selected representatives.

Finally read the extraction sibling row $X$ together with the locatedness row $L$ and structural rows $H,C,P,N$. The consumer therefore receives only the finite BEDC handoff shown in the theorem.
\end{proof}

\begin{theorem}[BishopLocatedDiagonal real seal non-escape]
\label{thm:bishop-located-diagonal-real-seal-nonescape}
In an accepted $\BishopLocatedDiagonalUp$ packet, the terminal $\RealUp$ seal $E$ can be consumed only after the $\DyadicRatCoreUp$ tolerance row $Q$, $\StreamNameUp$ window row $W$, $\RegSeqRatUp$ readback row $R$, diagonal-extraction row $X$, and locatedness ledger $L$ have all been displayed. No consumer may bypass these rows to obtain ambient $\RealUp$ completeness, quotient equality of real names, or a hidden limit selector.
\end{theorem}
\begin{proof}
The carrier places $E$ after the tolerance, window, readback, extraction, and locatedness rows. Projection therefore exposes $E$ only as the endpoint of that finite route.

The window handoff of \autoref{thm:bishop-located-diagonal-window-handoff} records the same dependency order for consumers. Since the accepted packet contains no coordinate for ambient completeness, quotient real equality, or hidden selection, the real seal cannot be read as any of those stronger objects.
\end{proof}

\begin{theorem}[BishopLocatedDiagonal transport stability]
\label{thm:bishop-located-diagonal-transport-stability}
If an accepted $\BishopLocatedDiagonalUp$ packet is transported by $\hsame$ or replayed by $\Cont$, then the rows $Q,W,R,E,X,L$ remain attached to the same local $\NameCert_{\BishopLocatedDiagonalUp}$ route. Thus transport preserves the finite diagonal ledger while refusing countable choice, quotient stream equality, host subsequence identity, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Use the transport row $H$ and replay row $C$ of the carrier. They act componentwise on the dyadic tolerance, stream-window, regular readback, real seal, extraction, and locatedness rows.

The provenance row $P$ and local name row $N$ record the same dependency anchors after transport. Hence a transported packet still factors through the four dependency certificates and the diagonal-extraction sibling rows before exposing the diagonal ledger.
\end{proof}

\closureat{BishopLocatedDiagonalUp}{seedStr}

\begin{closurestatus}{\BishopLocatedDiagonalUp}
  \constructivestory{A $\BishopLocatedDiagonalUp$ packet is generated from DyadicRatCore tolerance rows, StreamName finite windows, RegSeqRat readback, a RealUp seal, diagonal-extraction sibling rows, locatedness comparisons, componentwise transport, displayed replay, provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-located-diagonal-carrier}, \autoref{thm:bishop-located-diagonal-namecert-obligations}, \autoref{thm:bishop-located-diagonal-window-handoff}, \autoref{thm:bishop-located-diagonal-real-seal-nonescape}, and \autoref{thm:bishop-located-diagonal-transport-stability} seed the finite Bishop-style located diagonal packet over dyadic, stream, regular rational, real, and diagonal-extraction rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, countable choice, quotient stream equality, selected host subsequences, hidden real completeness, or global diagonal compactness.}
  \upgradepath{Obligation closure requires a Lean carrier for BEDC.Derived.BishopLocatedDiagonalUp and checked rows for tolerance exposure, window handoff, real-seal non-escape, transport, replay, provenance, and local naming.}
\end{closurestatus}
