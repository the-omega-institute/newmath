\chapter{A Concrete Naming Certificate for $\CompactRadiusChainUp$}
\label{ch:concrete-instances-compact-radius-chain-namecert}
\origin{human}

Following the Bishop--Heine--Cantor finite-cover proof, the $\CompactRadiusChainUp$ packet records the radius ledger that is read before a compact-continuity consumer reaches a $\UniformModulusUp$ row. It sits after the compact metric finite data of \autoref{ch:concrete-instances-compactmetric-namecert}, the ProbeBundle lower-bound fold of \autoref{ch:concrete-instances-probe-bundle-finite-minimum-fold-namecert}, the continuous-map pointwise radius route of \autoref{ch:concrete-instances-continuousmap-namecert}, the rational comparison surface of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet is the finite radius-chain component of the compact-uniform route; it is not an external Lebesgue-number selector, open-cover compactness, quotient map equality, countable choice, or ambient Real completeness.

\begin{definition}[Compact radius-chain carrier]
\label{def:compact-radius-chain-carrier}
A $\CompactRadiusChainUp$ carrier is a finite $\BHist$ packet
$$
R=(K,G,B,A,F,D,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-data row, exposing the compact source only as displayed ProbeBundle cover information.
\item $G$ is the $\ContinuousMapUp$ pointwise-radius row for the map whose compact-uniform handoff is being read.
\item $B$ is the finite ProbeBundle cell row, carrying centers, local membership witnesses, and the radius candidates attached to each cell.
\item $A$ is the positive-radius admission row, requiring every local radius candidate to be visible as finite rational or dyadic comparison data.
\item $F$ is the finite lower-bound fold row over the displayed radius candidates.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used to compare the folded radius with the requested tolerance budget.
\item $U$ is the downstream $\UniformModulusUp$ placeholder row, available only as a handoff after $K,G,B,A,F,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for compact source rows, pointwise-radius rows, bundle cells, radius admissions, folds, dyadic comparisons, and the handoff row.
\item $C$ records displayed $\Cont$ replay from compact finite data through pointwise radii, ProbeBundle folding, dyadic comparison, and uniform-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\CompactRadiusChainUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\DyadicRatCoreUp$, $\mathsf{ProbeBundle}$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactRadiusChainUp}$ row.
\end{enumerate}
Accepted packets are compared only through $K,G,B,A,F,D,U,H,C,P,N$ and never through an external radius selector, an open cover, a quotient map, countable choice, or ambient completeness.
\end{definition}

\begin{theorem}[Compact radius-chain NameCert obligations]
\label{thm:compact-radius-chain-namecert-obligations}
For every accepted $\CompactRadiusChainUp$ carrier $R=(K,G,B,A,F,D,U,H,C,P,N)$, the local $\NameCert_{\CompactRadiusChainUp}$ surface consists exactly of compact finite-data admission, continuous-map pointwise-radius admission, ProbeBundle cell exposure, positive-radius admission, finite lower-bound folding, DyadicRatCore tolerance accounting, UniformModulus handoff admission, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-radius-chain-carrier}. The compact source and pointwise-radius rows $K,G$ are visible before any radius fold can be read.

The finite chain is then displayed by $B,A,F,D$. The ProbeBundle cells name the finite family, the radius admission row exposes each positive candidate, the fold row records the lower-bound construction, and the dyadic ledger records the tolerance comparison.

Only after those rows are visible may the handoff row $U$ be consumed by a uniform-modulus reader. The rows $H,C,P,N$ transport, replay, source, and name the same packet. Since no row stores an external selector, open cover, quotient equality, choice schedule, or ambient completeness witness, these rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Compact radius-chain uniform-modulus handoff]
\label{thm:compact-radius-chain-uniform-modulus-handoff}
Every compact-uniform-continuity consumer that reads a $\CompactRadiusChainUp$ packet reaches $\UniformModulusUp$ only through
$$
K,G \longmapsto B,A,F,D \longmapsto U,H,C,P,N .
$$
The handoff exposes a finite ProbeBundle lower-bound chain before the uniform-modulus row is consumed; it does not export a selected Lebesgue number, open-cover compactness, quotient map equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the compact and pointwise rows first. The consumer receives compact finite data from $K$ and local radius information from $G$, not an ambient compactness theorem.

Next read the ProbeBundle radius chain. The cells $B$, admissions $A$, fold $F$, and dyadic ledger $D$ display the lower-bound route that binds all local radii to one finite tolerance comparison.

Finally consume the handoff row $U$ and the structural rows $H,C,P,N$. Because the uniform-modulus read is downstream of the displayed chain, it cannot be replaced by a selected Lebesgue number, an open-cover proof, quotient equality, choice, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactRadiusChainUp$ reader that claims a compact uniform-modulus handoff while omitting compact finite data, pointwise-radius data, ProbeBundle cells, positive-radius admission, lower-bound folding, DyadicRatCore comparison, hsame transport, Cont replay, Pkg provenance, or local NameCert has left the packet.}

\independenceWitness{compactmetric, continuousmap, uniformmodulus, dyadicratcore, probe-bundle-finite-minimum-fold: $\CompactRadiusChainUp$ is not bijective with any listed sibling because it joins compact finite data, pointwise local radii, finite bundle cells, positive-radius admission, lower-bound folding, dyadic tolerance comparison, uniform-modulus handoff, transport, replay, provenance, and local naming as one finite packet.}

\closureat{CompactRadiusChainUp}{seedStr}

\begin{closurestatus}{\CompactRadiusChainUp}
  \constructivestory{A $\CompactRadiusChainUp$ packet is generated from CompactMetric finite data, ContinuousMap pointwise-radius rows, ProbeBundle cell rows, positive-radius admissions, a finite lower-bound fold, DyadicRatCore tolerance comparison, a UniformModulus handoff row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-radius-chain-carrier}, \autoref{thm:compact-radius-chain-namecert-obligations}, and \autoref{thm:compact-radius-chain-uniform-modulus-handoff} seed the finite radius-chain surface for compact-uniform-modulus consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim an external Lebesgue-number selector, open-cover compactness, quotient map equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked compact finite-data admission, pointwise-radius admission, ProbeBundle cell exposure, positive-radius admission, lower-bound folding, dyadic tolerance accounting, uniform-modulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.CompactRadiusChainUp.}
\end{closurestatus}
