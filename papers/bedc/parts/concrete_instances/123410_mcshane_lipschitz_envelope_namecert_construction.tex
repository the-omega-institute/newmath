\chapter{A Concrete Naming Certificate for $\McshaneLipschitzEnvelopeUp$}
\label{ch:concrete-instances-mcshane-lipschitz-envelope-namecert}
\origin{human}

Following McShane's extension theorem, the $\McshaneLipschitzEnvelopeUp$ packet records the finite envelope surface by which a displayed Lipschitz source map on a displayed metric domain is read as a Real-valued ambient extension candidate. It sits beside the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the Lipschitz-map surface of \autoref{ch:concrete-instances-lipschitzmap-namecert}, the continuous-map route of \autoref{ch:concrete-instances-continuousmap-namecert}, the finite dyadic mesh of \autoref{ch:concrete-instances-finite-dyadic-mesh-namecert}, and the real-valued McShane handoff of \autoref{ch:concrete-instances-mcshane-extension-real-namecert}. The packet names only finite metric-domain rows, value rows, Lipschitz-bound rows, dyadic envelope rows, transport, replay, provenance, and local naming; it is not a host supremum construction, a selected extremum, a quotient function, or an appeal to ambient $\RealUp$ completeness.

\begin{definition}[McShane Lipschitz-envelope carrier]
\label{def:mcshane-lipschitz-envelope-carrier}
A $\McshaneLipschitzEnvelopeUp$ carrier is a finite $\BHist$ packet
$$
E=(D,M,V,L,Y,G,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the finite metric-domain row, listing the source points, ambient target points, and inclusion evidence read through $\MetricUp$.
\item $M$ is the $\MetricUp$ distance row for the displayed source and ambient points.
\item $V$ is the $\RealUp$ value row for the source map values exposed on $D$.
\item $L$ is the Lipschitz-bound row, recording the displayed rational or dyadic bound consumed by $\LipschitzMapUp$.
\item $Y$ is the finite dyadic envelope row, recording the lower and upper McShane comparison values over the displayed $\FiniteDyadicMeshUp$ cells.
\item $G$ is the ambient graph row, exposing the envelope as a $\ContinuousMapUp$ or $\LipschitzMapUp$ candidate only after $D,M,V,L,Y$ have been displayed.
\item $A$ is the source-agreement row, requiring the envelope graph to return the displayed source value on every admitted source point.
\item $H$ records componentwise $\hsame$ transport for the domain, metric, value, bound, envelope, graph, agreement, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an extension request through $D,M,V,L,Y,G,A,H$.
\item $P$ is the $\Pkg$ provenance over $\McshaneLipschitzEnvelopeUp$, $\MetricUp$, $\RealUp$, $\LipschitzMapUp$, $\ContinuousMapUp$, $\FiniteDyadicMeshUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\McshaneLipschitzEnvelopeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a host supremum, selected minimizer, selected maximizer, quotient equality of functions, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[McShane extension Lipschitz-envelope obligations]
\label{thm:mcshane-extension-lipschitz-envelope-obligations}
For every accepted $\McshaneLipschitzEnvelopeUp$ carrier
$$
E=(D,M,V,L,Y,G,A,H,C,P,N),
$$
the local $\NameCert_{\McshaneLipschitzEnvelopeUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the finite metric-domain row, metric row, Real value row, Lipschitz-bound row, dyadic envelope row, ambient graph row, source-agreement row, transport, replay, provenance, and local naming rows;
\item domain inclusion, requiring every source and ambient read to pass through $D$ and $M$ before a value or envelope comparison is public;
\item Lipschitz bound preservation, requiring $L$ to be the only displayed bound used by $\LipschitzMapUp$ and by the envelope comparisons in $Y$;
\item envelope exactness, requiring $Y$ to store only finite dyadic McShane lower and upper comparison rows over the displayed $\FiniteDyadicMeshUp$ cells before $G$ is exposed;
\item transport and provenance, requiring $H,C,P,N$ to preserve, replay, source, and name the same rows without adding host function equality, selected extrema, quotient functions, countable choice, trichotomy, or ambient Real completeness.
\end{enumerate}
No local obligation is stored outside $D,M,V,L,Y,G,A,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:mcshane-lipschitz-envelope-carrier}. The accepted packet displays exactly the finite metric-domain row, metric row, Real value row, Lipschitz-bound row, dyadic envelope row, graph row, agreement row, and structural rows.

The domain-inclusion clause is read first. Rows $D$ and $M$ are the only source of admitted points, ambient points, inclusion evidence, and displayed metric distances, so neither source values nor ambient envelope comparisons can be inspected before those rows are open.

Next read the Lipschitz and envelope clauses. The bound row $L$ is the only displayed Lipschitz budget, and $Y$ records the finite lower and upper McShane comparison rows over the dyadic mesh. Thus the graph row $G$ is an envelope handoff from finite rows, not a selected supremum or quotient function.

Finally inspect $A,H,C,P,N$. The agreement row restricts the graph back to the displayed source values, while transport, replay, provenance, and local naming keep the same packet route. Since the carrier contains no host function equality, selected extrema, quotient function, countable choice, trichotomy, or ambient Real-completeness coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\falsifiablePrediction{Every accepted $\McshaneLipschitzEnvelopeUp$ consumer that reads an ambient envelope graph while omitting the finite metric-domain row, MetricUp distance row, RealUp value row, Lipschitz-bound row, dyadic envelope row, source-agreement row, transport, replay, provenance, or local naming row refutes the carrier surface.}

\independenceWitness{metric, real, lipschitzmap, continuousmap, finite-dyadic-mesh, and mcshane-extension-real: no listed sibling is bijective to $\McshaneLipschitzEnvelopeUp$ because this packet alone binds finite domain inclusion, source values, one Lipschitz-bound ledger, dyadic McShane envelope rows, source agreement, transport, replay, provenance, and local NameCert as one envelope surface.}

\closureat{McshaneLipschitzEnvelopeUp}{seedStr}

\begin{closurestatus}{\McshaneLipschitzEnvelopeUp}
  \constructivestory{A $\McshaneLipschitzEnvelopeUp$ packet is generated from finite metric-domain rows, MetricUp distance rows, RealUp value rows, Lipschitz-bound rows, dyadic envelope rows, an ambient graph row, a source-agreement row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:mcshane-lipschitz-envelope-carrier} and \autoref{thm:mcshane-extension-lipschitz-envelope-obligations} seed the finite McShane Lipschitz-envelope surface over displayed MetricUp, RealUp, LipschitzMapUp, ContinuousMapUp, FiniteDyadicMeshUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host suprema, selected extrema, quotient function equality, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, domain inclusion, Lipschitz bound preservation, envelope exactness, source agreement, transport, replay, provenance, and local naming for BEDC.Derived.McshaneLipschitzEnvelopeUp.}
\end{closurestatus}
