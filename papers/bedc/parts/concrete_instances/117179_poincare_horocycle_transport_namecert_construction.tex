\chapter{A Concrete Naming Certificate for $\PoincareHorocycleTransportUp$}
\label{ch:concrete-instances-poincare-horocycle-transport-namecert}
\origin{ai}

The $\PoincareHorocycleTransportUp$ packet realizes the horocycle transport branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite disk, boundary-shadow, horocycle, transport, Real-seal, provenance, and NameCert rows. It sits after the disk automorphism surface of \autoref{ch:concrete-instances-poincare-disk-automorphism-namecert}, the horocycle carrier of \autoref{ch:concrete-instances-hyperbolic-horocycle-namecert}, the boundary-shadow route of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the visual-boundary readout of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, and the observer-expansion packet of \autoref{ch:concrete-instances-hyperbolic-observer-expansion-namecert}. The packet names finite horocycle transport; it is not a completed hyperbolic plane, quotient ideal boundary, selected geodesic ray, physical observer trajectory, or ambient $\RealUp$ completeness principle.

\begin{definition}[Poincare horocycle transport carrier]
\label{def:poincare-horocycle-transport-carrier}
A $\PoincareHorocycleTransportUp$ carrier is a finite $\BHist$ packet
$$
T=(D,B,O,Y,M,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\PoincareDiskAutomorphismUp$ disk-transport row.
\item $B$ is the boundary-shadow row, read through $\HyperbolicBoundaryShadowUp$ and the visible boundary packet.
\item $O$ is the $\HyperbolicHorocycleUp$ row, recording the boundary-tangent horocycle being transported.
\item $Y$ is the observer-expansion row that records the finite observer-facing read of the transported horocycle.
\item $M$ is the metric or visual-shadow comparison row that binds the disk transport, boundary shadow, and horocycle rows to one finite budget.
\item $R$ is the refusal row excluding quotient boundary equality, selected rays, unlogged physical observer trajectories, host isometry semantics, and hidden completeness.
\item $E$ is the terminal $\RealUp$ seal row for the displayed comparison budget.
\item $H$ records componentwise $\hsame$ transport for disk, boundary-shadow, horocycle, observer-expansion, comparison, refusal, Real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through
$$
D,B,O,Y,M,R \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\PoincareHorocycleTransportUp$, $\PoincareDiskAutomorphismUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicVisualBoundaryUp$, $\HyperbolicHorocycleUp$, $\HyperbolicObserverExpansionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PoincareHorocycleTransportUp}$ row.
\end{enumerate}
The classifier accepts only packets whose rows $D,B,O,Y,M,R,E,H,C,P,N$ remain visible on the same finite route.
\end{definition}

\begin{theorem}[Poincare horocycle transport NameCert obligations]
\label{thm:poincare-horocycle-transport-namecert-obligations}
For every accepted $\PoincareHorocycleTransportUp$ carrier $T=(D,B,O,Y,M,R,E,H,C,P,N)$, the local $\NameCert_{\PoincareHorocycleTransportUp}$ surface consists exactly of disk-transport admission, boundary-shadow exposure, horocycle exposure, observer-expansion exposure, comparison-budget exposure, refusal exactness, terminal Real sealing, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:poincare-horocycle-transport-carrier}. The rows $D,B,O,Y,M,R,E,H,C,P,N$ are the whole local inventory.

The geometric source rows are $D,B,O,Y,M$. They bind the Poincare disk transport, boundary shadow, horocycle, observer-facing read, and comparison budget before the Real-facing seal can be inspected.

The refusal row $R$ blocks quotient boundary equality, selected rays, physical observer trajectories, host isometry semantics, and hidden completeness. The structural rows $H,C,P,N$ preserve the same finite route, so the listed obligations exhaust the certificate.
\end{proof}

\begin{theorem}[Poincare horocycle transport boundary-shadow handoff]
\label{thm:poincare-horocycle-transport-boundary-shadow-handoff}
Every accepted $\PoincareHorocycleTransportUp$ Real-facing read factors through
$$
D,B,O,Y,M,R \longmapsto E \longmapsto H,C,P,N .
$$
The boundary-shadow row $B$ and horocycle row $O$ must be displayed before the comparison budget $M$ and the seal $E$ are read. A consumer missing either row is outside the accepted horocycle transport packet.
\end{theorem}
\begin{proof}
Start from the carrier. The disk row $D$ supplies the Poincare transport, but it does not by itself identify the boundary-shadow or horocycle data.

Read the rows $B$ and $O$ before $M$. These rows attach the visible boundary shadow and the boundary-tangent horocycle to the same transport budget, while $Y$ records the observer-facing read.

The refusal and Real-seal rows are downstream. Transport, replay, provenance, and local naming preserve the same route, so omitting $B$ or $O$ leaves the carrier rather than producing a weaker accepted packet.
\end{proof}

\begin{theorem}[Poincare horocycle transport nonescape]
\label{thm:poincare-horocycle-transport-nonescape}
No accepted $\PoincareHorocycleTransportUp$ packet exports a completed hyperbolic plane, quotient ideal boundary, selected geodesic ray, physical observer trajectory, host isometry semantics, or ambient $\RealUp$ completeness. Every exported read is confined to $D,B,O,Y,M,R,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. Its rows are exactly the disk transport, boundary shadow, horocycle, observer expansion, comparison budget, refusal, Real seal, transport, replay, provenance, and local naming rows.

Apply the boundary-shadow handoff theorem. It forces every Real-facing consumer through the displayed finite route before the seal is read, so the refused host objects have no carrier coordinate and cannot be exported.
\end{proof}

\falsifiablePrediction{A claimed $\PoincareHorocycleTransportUp$ packet that reaches the $\RealUp$ seal without displaying both the boundary-shadow row $B$ and the horocycle row $O$ is rejected as a horocycle transport packet.}

\independenceWitness{poincare-disk-automorphism, hyperbolic-horocycle, hyperbolic-boundary-shadow, hyperbolic-visual-boundary, hyperbolic-observer-expansion: none is bijective to $\PoincareHorocycleTransportUp$ because the transport packet binds disk transport, boundary shadow, horocycle, observer-expansion read, comparison budget, refusal, Real seal, transport, replay, provenance, and local NameCert rows as one finite route.}

The Lean TasteGate route must provide explicit $\Nontrivial\ \PoincareHorocycleTransportUp$ and $\FieldFaithful\ \PoincareHorocycleTransportUp$ instances before the chapter can be treated as a checked discovery-facing surface. The nontrivial witness separates a packet with both $B$ and $O$ from a refused disk-only transport, and field faithfulness compares exactly $D,B,O,Y,M,R,E,H,C,P,N$.

\closureat{PoincareHorocycleTransportUp}{seedStr}

\begin{closurestatus}{\PoincareHorocycleTransportUp}
  \constructivestory{A $\PoincareHorocycleTransportUp$ packet is generated from Poincare disk transport, boundary-shadow rows, horocycle rows, observer-expansion rows, comparison-budget rows, refusal exactness, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:poincare-horocycle-transport-carrier}, \autoref{thm:poincare-horocycle-transport-namecert-obligations}, \autoref{thm:poincare-horocycle-transport-boundary-shadow-handoff}, and \autoref{thm:poincare-horocycle-transport-nonescape} seed the finite Poincare horocycle transport route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed hyperbolic planes, quotient ideal boundaries, selected geodesic rays, physical observer trajectories, host isometry semantics, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked disk-transport admission, boundary-shadow exposure, horocycle exposure, observer-expansion exposure, comparison-budget exactness, refusal, RealUp sealing, TasteGate nontriviality, field faithfulness, replay, provenance, and local naming for BEDC.Derived.PoincareHorocycleTransportUp.}
\end{closurestatus}
