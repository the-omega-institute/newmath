\chapter{A Concrete Naming Certificate for $\MooreAronszajnKernelUp$}
\label{ch:concrete-instances-moore-aronszajn-kernel-namecert}
\origin{human}

Following Moore and Aronszajn on reproducing kernels, the $\MooreAronszajnKernelUp$ packet records the finite positive-kernel route as BEDC NameCert data. It sits after the Hilbert-space packet of \autoref{ch:concrete-instances-hilbert-namecert}, the vector-space rows of \autoref{ch:concrete-instances-vecspace-namecert}, the finite matrix readback of \autoref{ch:concrete-instances-matrix-namecert}, the functional-analysis handoff of \autoref{ch:concrete-instances-functionalanalysis-namecert}, and the real scalar seal of \autoref{ch:concrete-instances-real-namecert}. The packet names finite Gram-matrix reproducing-kernel evidence without selecting an infinite feature map, quotienting functions, completing an ambient RKHS, importing a host Hilbert space, assuming countable choice, or asserting positive-kernel analysis beyond the displayed rows.

\begin{definition}[Moore--Aronszajn kernel carrier]
\label{def:moore-aronszajn-kernel-carrier}
A $\MooreAronszajnKernelUp$ carrier is a finite $\BHist$ packet
$$
K=(K_0,G,V,H,M,R,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K_0$ is the kernel-value row. It records the finite list of source points, displayed kernel evaluations, and symmetry requests inspected by the finite packet.
\item $G$ is the positive finite Gram row. It records the finite Gram matrix over the displayed source points, the diagonal and symmetry witnesses, and the finite positivity ledger for every displayed coefficient window.
\item $V$ is the $\VecSpaceUp$ source row. It records the finite vector expressions, scalar multiplication requests, and linear-combination windows from which Gram reads are formed.
\item $H$ is the $\HilbertUp$ source row. It records the inner-product readout surface and norm-facing rows through which the reproducing-kernel handoff may be interpreted.
\item $M$ is the $\MatrixUp$ readback row. It records the finite row-column indexing, entry readbacks, Gram-row projections, and matrix-vector products read before any RKHS-style consumer can use $G$.
\item $R$ is the $\RealUp$ scalar seal. It records real scalar values, positive-radius comparisons, and the terminal real-name boundary for every displayed Gram and norm read.
\item $C$ records componentwise $\hsame$ transport together with displayed $\Cont$ replay for the route
$$
K_0,G,V,H,M \longmapsto R .
$$
\item $P$ is the $\Pkg$ provenance over $\MooreAronszajnKernelUp$, $\HilbertUp$, $\VecSpaceUp$, $\MatrixUp$, $\FunctionalAnalysisUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MooreAronszajnKernelUp}$ row.
\end{enumerate}
The carrier predicate admits exactly these nine rows. Its classifier compares accepted packets only through $K_0,G,V,H,M,R,C,P,N$ and has no coordinate for an infinite RKHS completion, selected feature map, quotient function space, countable-choice schedule, ambient Hilbert completion, or uninspected positive-kernel theorem.
\end{definition}
\leanchecked{BEDC.Derived.MooreAronszajnKernelUp.MooreAronszajnKernelTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Moore--Aronszajn kernel NameCert obligations]
\label{thm:moore-aronszajn-kernel-namecert-obligations}
For the carrier of \autoref{def:moore-aronszajn-kernel-carrier}, the local $\NameCert_{\MooreAronszajnKernelUp}$ surface consists exactly of carrier admission, finite kernel-value exposure, positive Gram-row exposure, $\VecSpaceUp$ source compatibility, $\HilbertUp$ inner-product handoff, $\MatrixUp$ readback compatibility, $\RealUp$ scalar sealing, componentwise $\hsame$ transport with displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. The surface exports no selected feature map, quotient function space, infinite completion, countable choice, host Hilbert space, or ambient RKHS theorem.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:moore-aronszajn-kernel-carrier}. The packet displays exactly the finite kernel row, Gram row, vector-space row, Hilbert row, matrix readback, real scalar seal, structural replay, provenance, and local naming row.

The positive-kernel obligation is finite. The row $G$ records only displayed Gram windows and their positivity ledgers, while $M$ supplies the row-column readback and $R$ supplies the real scalar comparison boundary.

The analytic handoff remains local to the displayed siblings. The rows $V$ and $H$ expose only the $\VecSpaceUp$ and $\HilbertUp$ surfaces named by $P$, and the functional-analysis route is read as provenance rather than as an ambient completion theorem.

Finally $C$ preserves the same rows under $\hsame$ and $\Cont$ replay, and $N$ names no off-packet coordinate. Therefore the listed obligations exhaust the local NameCert surface and none of the excluded RKHS-side objects can be read from the packet.
\end{proof}

\begin{theorem}[Moore--Aronszajn kernel Hilbert handoff]
\label{thm:moore-aronszajn-kernel-hilbert-handoff}
Every Hilbert-facing or functional-analysis consumer of an accepted $\MooreAronszajnKernelUp$ packet must first read the finite kernel row $K_0$, the positive finite Gram row $G$, the $\VecSpaceUp$ source row $V$, the $\HilbertUp$ inner-product source row $H$, the $\MatrixUp$ readback row $M$, the $\RealUp$ scalar seal $R$, and the structural rows $C,P,N$. The handoff reaches the sibling surfaces of \autoref{ch:concrete-instances-hilbert-namecert}, \autoref{ch:concrete-instances-vecspace-namecert}, \autoref{ch:concrete-instances-matrix-namecert}, \autoref{ch:concrete-instances-functionalanalysis-namecert}, and \autoref{ch:concrete-instances-real-namecert} only through those displayed rows.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:moore-aronszajn-kernel-namecert-obligations}. They force every consumer to inspect $K_0,G,V,H,M,R,C,P,N$ before a Hilbert-facing read is available.

The Hilbert handoff is routed through displayed finite data. The vector-space row supplies the linear-combination windows, the matrix row supplies the finite Gram readback, and the real row supplies scalar comparison before the Hilbert row may be consumed.

The provenance row names the five sibling chapters, and the replay row orders the read through the finite Gram route. Since no row stores a selected feature map, quotient function space, or completed RKHS carrier, the Hilbert-facing consumer receives only the displayed finite handoff.
\end{proof}

\begin{theorem}[Moore--Aronszajn kernel finite Gram nonescape]
\label{thm:moore-aronszajn-kernel-finite-gram-nonescape}
Every reproducing-kernel read of an accepted $\MooreAronszajnKernelUp$ packet factors through the finite Gram row $G$, the $\MatrixUp$ readback row $M$, the $\VecSpaceUp$ source row $V$, the $\HilbertUp$ source row $H$, the $\RealUp$ scalar seal $R$, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row. The packet does not export selected RKHS points, quotient function equality, infinite Gram operators, countable choice, ambient Hilbert completion, or full Moore--Aronszajn existence.
\end{theorem}
\begin{proof}
Project the carrier rows. The only route from kernel values to an analytic consumer is the displayed replay
$$
K_0,G,V,H,M \longmapsto R .
$$

The finite Gram row is therefore unavoidable. Matrix readback exposes only the finite rows and columns displayed by $M$, vector expressions are those displayed by $V$, and scalar comparisons end at $R$.

Transport and replay cannot create new analytic objects. They preserve the same finite rows under $\hsame$ and $\Cont$, while $P,N$ name only the listed packet surface. Hence every reproducing-kernel read remains inside the finite Gram route and none of the excluded RKHS-side data can escape the packet.
\end{proof}

\leanstmt{BEDC.Derived.MooreAronszajnKernelUp}

\closureat{MooreAronszajnKernelUp}{seedStr}

\begin{closurestatus}{\MooreAronszajnKernelUp}
  \constructivestory{A $\MooreAronszajnKernelUp$ packet is generated from finite kernel-value rows, positive finite Gram rows, VecSpace and Hilbert source rows, Matrix readback, Real scalar sealing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:moore-aronszajn-kernel-carrier}, \autoref{thm:moore-aronszajn-kernel-namecert-obligations}, \autoref{thm:moore-aronszajn-kernel-hilbert-handoff}, and \autoref{thm:moore-aronszajn-kernel-finite-gram-nonescape} seed the finite Gram-matrix reproducing-kernel carrier and its Hilbert-facing handoff boundary.}
  \formalstatus{\formalTargetV}
  \leantarget{BEDC.Derived.MooreAronszajnKernelUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected feature maps, quotient function spaces, infinite RKHS completion, countable choice, ambient Hilbert completeness, full Moore--Aronszajn existence, checked Lean obligations, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite Gram positivity, Matrix readback, VecSpace and Hilbert handoff, Real scalar sealing, transport, replay, provenance, and local naming for BEDC.Derived.MooreAronszajnKernelUp.}
\end{closurestatus}
