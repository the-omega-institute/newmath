\chapter{A Concrete Naming Certificate for $\SolenoidPhaseUp$}
\label{ch:concrete-instances-solenoid-phase-namecert}
\origin{human}

Following Pontryagin duality and the classical theory of solenoids, the $\SolenoidPhaseUp$ packet records the finite phase carrier associated to an $S$-localized integer row and its compact inverse-limit circle presentation. It realizes the solenoid carrier described in \autoref{sec:unified-reality-solenoid-lee-yang-resonance-boundary} as concrete NameCert data, reading prime rows from \autoref{ch:concrete-instances-prime-namecert}, visible circle phase rows from \autoref{ch:concrete-instances-circle-namecert} and \autoref{ch:concrete-instances-s1-namecert}, and compact group operation rows from \autoref{ch:concrete-instances-topgroup-namecert}. The packet names only displayed localization, circle projection, compact fibre, resonance phase, transport, replay, provenance, and local naming rows; it does not assert a zeta zero theorem, a hidden Pontryagin-dual object, quotient equality of characters, or an ambient compactness principle.

\begin{definition}[SolenoidPhase carrier]
\label{def:solenoid-phase-carrier}
A $\SolenoidPhaseUp$ carrier is a finite $\BHist$ packet
$$
S=(P,L,Q,V,F,R,H,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $P$ is the displayed prime-family row, read through $\PrimeUp$, naming the finite prime set or prime-window source whose elements are made invertible.
\item $L$ is the $S$-localization row for the integer carrier, recording the denominators whose prime divisors are constrained by $P$.
\item $Q$ is the compact solenoid phase row, read as the dual or inverse-limit phase carrier generated by $L$.
\item $V$ is the visible circle projection row, routed through the $\CircleUp$ and $\SOneUp$ phase surfaces.
\item $F$ is the compact fibre row, recording the kernel of the visible projection without naming a hidden quotient space.
\item $R$ is the resonance phase row, carrying the displayed prime-local phase coordinates consumed by the solenoid Lee--Yang route.
\item $H$ records componentwise $\hsame$ transport for prime-family, localization, compact phase, circle projection, fibre, resonance, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from prime-family selection through localization, compact solenoid phase, visible circle projection, fibre, resonance row, provenance, and local naming.
\item $G$ is the $\Pkg$ provenance over $\SolenoidPhaseUp$, $\PrimeUp$, $\CircleUp$, $\SOneUp$, $\TopGroupUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SolenoidPhaseUp}$ row.
\end{enumerate}
The carrier has no coordinate for a host Pontryagin-dual construction, an undisplayed character group, quotient equality of phase points, a zeta-zero locator, or a noncompact positive scale.
\end{definition}

\begin{theorem}[SolenoidPhase NameCert obligations]
\label{thm:solenoid-phase-namecert-obligations}
For every accepted $\SolenoidPhaseUp$ carrier $S=(P,L,Q,V,F,R,H,C,G,N)$, the local $\NameCert_{\SolenoidPhaseUp}$ surface consists exactly of prime-family admission through $P$, $S$-localization exposure through $L$, compact solenoid phase admission through $Q$, visible circle projection through $V$, compact fibre exposure through $F$, resonance phase exposure through $R$, componentwise transport through $H$, displayed replay through $C$, provenance through $G$, and local naming through $N$.

Every solenoid-facing resonance consumer must read the localization and visible circle projection before the resonance phase row is public. The packet exports no hidden Pontryagin-dual carrier, quotient character equality, zeta-zero theorem, ambient compactness principle, or noncompact scale coordinate.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:solenoid-phase-carrier}. The rows $P,L,Q,V,F,R,H,C,G,N$ are the only rows available to a consumer.

The prime-family row $P$ and localization row $L$ supply the arithmetic source. The compact solenoid phase row $Q$, visible projection row $V$, and compact fibre row $F$ are then displayed before the resonance row $R$ can be consumed.

The structural rows $H,C,G,N$ transport, replay, source, and name the same finite packet. Since no coordinate stores a hidden character group, quotient phase equality, zeta-zero locator, or noncompact scale, the listed rows exhaust the local NameCert obligation surface.
\end{proof}

\begin{theorem}[SolenoidPhase resonance handoff]
\label{thm:solenoid-phase-resonance-handoff}
Every accepted $\SolenoidPhaseUp$ packet hands a resonance consumer the route
$$
P,L \longmapsto Q,V,F \longmapsto R,H,C,G,N
$$
where $P$ is read through \autoref{ch:concrete-instances-prime-namecert}, $V$ is read through the circle phase rows of \autoref{ch:concrete-instances-circle-namecert} and \autoref{ch:concrete-instances-s1-namecert}, and $Q,V,F$ are compatible with the topological-group operation surface of \autoref{ch:concrete-instances-topgroup-namecert}. Thus the resonance phase row is public only after the displayed localization, compact phase, visible circle, and compact fibre rows have been exposed.
\end{theorem}
\begin{proof}
Start with the arithmetic source. The prime-family row $P$ fixes the admitted prime directions, and the localization row $L$ records exactly which denominator primes can appear.

Next expose the compact phase carrier. The solenoid row $Q$ is read together with its visible circle projection $V$ and compact fibre $F$, so a resonance consumer cannot treat the phase as a noncompact positive scale or as an undisplayed character group.

Finally read $R,H,C,G,N$. The resonance row $R$ is downstream from the localization and compact phase rows, while transport, replay, provenance, and local naming preserve that order. Hence the handoff is a finite phase certificate, not a proof of the zeta-specific dominance theorem.
\end{proof}

\falsifiablePrediction{A $\SolenoidPhaseUp$ consumer that reads a resonance phase row $R$ without first exposing the prime-family row $P$, localization row $L$, visible circle projection $V$, and compact fibre row $F$ refutes the carrier surface.}

\independenceWitness{prime, circle, s1, topgroup: no listed sibling supplies the full ten-row packet $S=(P,L,Q,V,F,R,H,C,G,N)$, because each sibling contributes only prime admission, visible phase, group operation, or compactness-facing data while this chapter binds the solenoid phase route with transport, replay, provenance, and local naming.}

\closureat{SolenoidPhaseUp}{seedStr}

\begin{closurestatus}{\SolenoidPhaseUp}
  \constructivestory{A $\SolenoidPhaseUp$ packet is generated from prime-family rows, localization rows, compact solenoid phase rows, visible circle projection rows, compact fibre rows, resonance phase rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:solenoid-phase-carrier}, \autoref{thm:solenoid-phase-namecert-obligations}, and \autoref{thm:solenoid-phase-resonance-handoff} seed the finite solenoid phase route over prime-family, localization, compact phase, visible circle, fibre, resonance, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close the zeta solenoid Lee--Yang row, zeta-zero classification, host Pontryagin duality, quotient character equality, noncompact positive scale exclusion, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, localization exposure, compact phase admission, visible circle projection, compact fibre exposure, resonance handoff, transport, replay, provenance, and local naming for BEDC.Derived.SolenoidPhaseUp.}
\end{closurestatus}
