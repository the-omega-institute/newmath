\chapter{A Concrete Naming Certificate for $\SubordinateModulusCoverUp$}
\label{ch:concrete-instances-subordinatemoduluscover-namecert}
\origin{ai}

The $\SubordinateModulusCoverUp$ packet names the finite cover returned before the compact-uniform-continuity rational lower-bound fold. It sits between the compact finite-net carrier and the $\UniformModulusUp$ consumer: the packet records bundled centers, pointwise-modulus precision rows, subordinate positive-radius rows, half-radius coverage witnesses, rational comparison ledgers, and the local provenance needed to hand the bundle to the uniform-modulus fold.

The packet consumes the finite-net coverage surface of \autoref{ch:concrete-instances-compact-namecert}, the pointwise-modulus data supplied by \autoref{ch:concrete-instances-continuousmap-namecert}, and the metric and rational comparison rows of \autoref{ch:concrete-instances-metric-namecert} and \autoref{ch:concrete-instances-rat-namecert}. Its output is the finite subordinate bundle handed to \autoref{ch:concrete-instances-uniformmodulus-namecert}, before any uniform precision row is chosen.

\begin{definition}[SubordinateModulusCover carrier]
\label{def:subordinate-modulus-cover-carrier}
A $\SubordinateModulusCoverUp$ carrier is a finite $\BHist$ packet
$$
S=(E,\mathcal{P},C,R,D,M,H,F,L,Q,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $E$ is the positive $\RatUp$ tolerance history requested by the compact-continuous consumer;
\item $\mathcal{P}$ is the nonempty $\mathsf{ProbeBundle}$ spine returned by the compact finite-net certificate;
\item $C$ assigns to each bundled probe its displayed center history;
\item $R$ assigns to each bundled probe a positive subordinate radius history;
\item $D$ assigns to each bundled probe the pointwise-modulus precision history supplied at its center;
\item $M$ records the accepted pointwise-modulus row for the tolerance fraction read from $E$;
\item $H$ records the half-radius coverage row saying that every source history is covered by a bundled center at the displayed subordinate radius;
\item $F$ records the rational comparison ledger witnessing that every subordinate radius row is below its pointwise precision row;
\item $L$ records componentwise $\hsame$ stability for the tolerance, bundle, centers, radius rows, precision rows, pointwise rows, coverage rows, and rational comparisons;
\item $Q$ records the displayed $\Cont$ routes through which compact and continuous consumers read the packet;
\item $P$ is the $\Pkg$ provenance over $\CompactUp$, $\ContinuousMapUp$, $\MetricUp$, $\RatUp$, $\mathsf{ProbeBundle}$, $\BHist$, $\hsame$, $\Cont$, and the local naming rows;
\item $N$ is the local $\NameCert_{\SubordinateModulusCoverUp}$ row.
\end{enumerate}
The carrier exports no open cover, no external radius selector, no host minimum, and no uniform precision row. Its role is exactly the finite subordinate bundle consumed before the $\UniformModulusUp$ lower-bound fold.
\end{definition}
\leandef{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverCarrier}

\begin{theorem}[SubordinateModulusCover NameCert obligations]
\label{thm:subordinate-modulus-cover-namecert-obligations}
The local $\NameCert_{\SubordinateModulusCoverUp}$ surface for \autoref{def:subordinate-modulus-cover-carrier} consists of carrier admission for $S=(E,\mathcal{P},C,R,D,M,H,F,L,Q,P,N)$, classifier stability under the componentwise $\hsame$ rows in $L$, compact coverage exactness for the half-radius rows in $H$, pointwise-modulus compatibility for the rows in $M$ and $D$, rational subordination exactness for the comparison rows in $F$, and consumer non-escape through the displayed $\Cont$, $\Pkg$, and local naming rows.
\end{theorem}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverTasteGate\_single\_carrier\_alignment}
\begin{proof}
Carrier admission is the displayed finite packet. The tolerance row, nonempty bundle, centers, subordinate radii, pointwise precision rows, pointwise-modulus rows, coverage witnesses, rational comparisons, transport ledger, continuation routes, package provenance, and local naming row are all visible coordinates.

Classifier stability is componentwise over those coordinates. The transport ledger $L$ can rename bundled probes, centers, radius histories, precision histories, coverage rows, and comparison rows only through the displayed $\hsame$ entries, and the routes in $Q$ preserve the compact and continuous reads attached to the same bundle.

Coverage exactness is the half-radius row $H$. Pointwise compatibility is the row $M$ at each center together with the precision row in $D$. Rational subordination is the row $F$, which binds each radius row in $R$ below the corresponding precision row in $D$.

These rows exhaust the consumer surface. Since no coordinate stores an open cover, an external selector, a host minimum, or a uniform precision history, the local naming certificate cannot export any of those objects.
\end{proof}

\begin{theorem}[SubordinateModulusCover uniform-modulus handoff]
\label{thm:subordinate-modulus-cover-uniform-handoff}
Every accepted $\SubordinateModulusCoverUp$ packet hands a $\UniformModulusUp$ consumer exactly the nonempty $\mathsf{ProbeBundle}$ spine, bundled centers, positive subordinate radius rows, pointwise-modulus precision rows, half-radius coverage rows, and rational subordination comparisons needed by the finite lower-bound fold. The handoff does not yet choose the uniform precision; it supplies the finite input ledger from which the $\UniformModulusUp$ fold computes that precision.
\end{theorem}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverUniformHandoff}
\begin{proof}
Project the accepted packet through \autoref{def:subordinate-modulus-cover-carrier}. The consumer receives $\mathcal{P}$, $C$, $R$, $D$, $M$, $H$, and $F$ through the displayed routes in $Q$ and the provenance rows $P,N$.

The nonempty bundle and coverage rows are the compact side of the handoff. The pointwise-modulus and precision rows are the continuous side. The rational comparison ledger records the subordination that permits the later lower-bound fold to stay below every bundled half-radius row.

No fold output is present in the carrier. Therefore the handoff is precisely the finite subordinate-cover input to the $\UniformModulusUp$ rational fold, with no external modulus selector and no host-level compactness object.
\end{proof}

\begin{theorem}[SubordinateModulusCover carrier obligation]
\label{thm:subordinate-modulus-cover-carrier-obligation}
Every accepted $\SubordinateModulusCoverUp$ read is admitted through the single carrier
$$
S=(E,\mathcal{P},C,R,D,M,H,F,L,Q,P,N)
$$
of \autoref{def:subordinate-modulus-cover-carrier}. The TasteGate alignment of \autoref{thm:subordinate-modulus-cover-namecert-obligations} binds the positive tolerance row, nonempty probe bundle, centers, subordinate radii, pointwise precision rows, pointwise-modulus row, half-radius coverage row, rational comparison ledger, transport rows, continuation routes, package provenance, and local naming row to this one packet.
\end{theorem}
\begin{proof}
Project an accepted read through the carrier definition. The rows available to the consumer are exactly $E,\mathcal{P},C,R,D,M,H,F,L,Q,P,N$.

Apply \autoref{thm:subordinate-modulus-cover-namecert-obligations}. Its checked TasteGate row states the local NameCert obligations on the same displayed carrier, including carrier admission and consumer non-escape.

Since the carrier exports no open cover, external radius selector, host minimum, or uniform precision row, admission cannot be transferred to a second packet. The accepted read is therefore a read of the single displayed subordinate-modulus-cover carrier.
\end{proof}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverCarrierObligation}

\begin{theorem}[SubordinateModulusCover classifier stability obligation]
\label{thm:subordinate-modulus-cover-classifier-stability-obligation}
Classifier transport for an accepted $\SubordinateModulusCoverUp$ packet is componentwise on the rows of
$$
S=(E,\mathcal{P},C,R,D,M,H,F,L,Q,P,N).
$$
The TasteGate alignment of \autoref{thm:subordinate-modulus-cover-namecert-obligations} permits transport only through the $\hsame$ ledger $L$ and the displayed $\Cont$ routes $Q$, preserving the tolerance, bundle, centers, radius rows, precision rows, pointwise-modulus rows, coverage rows, rational comparisons, package provenance, and local naming row.
\end{theorem}
\begin{proof}
Start with the single carrier $S$. The only classifier-stability coordinate is the componentwise transport ledger $L$, and the only consumer routes are the displayed continuations in $Q$.

Use \autoref{thm:subordinate-modulus-cover-namecert-obligations}. The TasteGate row records classifier stability under $L$ together with compact coverage exactness, pointwise-modulus compatibility, rational subordination exactness, and consumer non-escape for the same packet.

Thus a transported packet preserves each visible coordinate rather than replacing the carrier or changing the bundle. No parameter-transport equivalence, open-cover selector, host minimum, or uniform precision row is introduced by classifier transport.
\end{proof}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverClassifierStabilityObligation}

\begin{theorem}[SubordinateModulusCover ledger handoff obligation]
\label{thm:subordinate-modulus-cover-ledger-handoff-obligation}
The compact, continuous, and uniform-modulus handoff ledger of an accepted $\SubordinateModulusCoverUp$ packet is exactly the route through
$$
\mathcal{P},C,R,D,M,H,F,L,Q,P,N
$$
inside the single carrier $S=(E,\mathcal{P},C,R,D,M,H,F,L,Q,P,N)$. The handoff exposes the finite nonempty bundle, bundled centers, subordinate radius rows, pointwise-modulus precision rows, half-radius coverage witnesses, and rational subordination comparisons needed before the $\UniformModulusUp$ lower-bound fold, and it exposes no fold output.
\end{theorem}
\begin{proof}
Read the handoff rows from the carrier definition. The compact side supplies $\mathcal{P}$, $C$, and $H$; the continuous side supplies $D$ and $M$; the rational subordination ledger supplies $R$ and $F$; and $L,Q,P,N$ transport, route, and record the public read.

Apply \autoref{thm:subordinate-modulus-cover-namecert-obligations}. Its TasteGate row binds coverage exactness, pointwise-modulus compatibility, rational subordination exactness, and non-escape to the same carrier $S$.

Finally use \autoref{thm:subordinate-modulus-cover-uniform-handoff}. The $\UniformModulusUp$ consumer receives precisely the finite input ledger for the later lower-bound fold, while the uniform precision chosen by that fold remains outside the subordinate cover packet.
\end{proof}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverLedgerHandoffObligation}

\begin{theorem}[SubordinateModulusCover scope dependency]
\label{thm:subordinate-modulus-cover-scope-dependency}
Every compact-uniform handoff row of an accepted $\SubordinateModulusCoverUp$ packet factors through $\CompactUp$ finite-net coverage, $\ContinuousMapUp$ pointwise-modulus data, $\MetricUp$ distance witnesses, $\RatUp$ comparison rows, $\mathsf{ProbeBundle}$ induction, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, and the $\UniformModulusUp$ consumer. The factorization stays inside
$$
S=(E,\mathcal{P},C,R,D,M,H,F,L,Q,P,N),
$$
and it contains no open-cover compactness, host minimum, or external selector.
\end{theorem}
\begin{proof}
Project the carrier $S=(E,\mathcal{P},C,R,D,M,H,F,L,Q,P,N)$. The bundle spine $\mathcal{P}$ and center rows $C$ supply the finite-net side, while $R,D,M,H,F$ record subordinate radii, pointwise precision, pointwise-modulus data, half-radius coverage, and rational comparison.

Apply \autoref{thm:subordinate-modulus-cover-namecert-obligations}. The local $\NameCert_{\SubordinateModulusCoverUp}$ surface binds those rows to $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local naming, so the dependency cannot leave the displayed packet.

Use \autoref{thm:subordinate-modulus-cover-uniform-handoff} and \autoref{thm:subordinate-modulus-cover-ledger-handoff-obligation}. They expose the compact, continuous, metric, rational, and finite-bundle rows to the $\UniformModulusUp$ consumer before the lower-bound fold is read.

Thus the scoped dependency is exhausted by the displayed carrier and its existing handoff obligations. Since $S$ has no coordinate for open-cover compactness, host minima, or external selectors, none of those objects can enter the compact-uniform handoff.
\end{proof}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverScopeDependency}

\begin{theorem}[SubordinateModulusCover compact coverage exactness]
\label{thm:subordinate-modulus-cover-compact-coverage-exactness}
Let
$$
S=(E,\mathcal{P},C,R,D,M,H,F,L,Q,P,N)
$$
be an accepted $\SubordinateModulusCoverUp$ carrier. Every source history covered by the packet is covered by a bundled center from the nonempty $\mathsf{ProbeBundle}$ spine $\mathcal{P}$, through the half-radius coverage rows $H$ and the displayed consumer routes $Q$. The coverage row is internal to the carrier and uses no host open-cover compactness, external radius selector, host minimum, or uniform precision output.
\end{theorem}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverCompactCoverageExactness}
\begin{proof}
Project the carrier by \autoref{def:subordinate-modulus-cover-carrier}. The nonempty bundle spine $\mathcal{P}$, center row $C$, subordinate radius row $R$, half-radius coverage row $H$, and continuation route row $Q$ are all visible coordinates of the same packet.

Apply \autoref{thm:subordinate-modulus-cover-namecert-obligations}. Its TasteGate alignment binds compact coverage exactness to the half-radius rows in $H$ for the displayed carrier $S$.

The public coverage read is therefore the route through $\mathcal{P},C,R,H,Q,P,N$. Since the carrier definition contains no open-cover object, external radius selector, host minimum, or uniform precision row, none of those objects can supply the coverage witness.
\end{proof}

\begin{theorem}[SubordinateModulusCover pointwise compatibility]
\label{thm:subordinate-modulus-cover-pointwise-compatibility}
Let
$$
S=(E,\mathcal{P},C,R,D,M,H,F,L,Q,P,N)
$$
be an accepted $\SubordinateModulusCoverUp$ carrier. Every precision row in $D$ is supplied by the pointwise-modulus row $M$ at the corresponding bundled center from $\mathcal{P}$ and is transported only by the componentwise $\hsame$ ledger $L$. The compatibility row does not choose an external modulus, host minimum, or uniform precision output.
\end{theorem}
\begin{proof}
Read the relevant coordinates from \autoref{def:subordinate-modulus-cover-carrier}. The center row is $C$, the precision row is $D$, the pointwise-modulus row is $M$, and the only componentwise transport ledger is $L$.

Use \autoref{thm:subordinate-modulus-cover-namecert-obligations}. The TasteGate alignment lists pointwise-modulus compatibility for the rows in $M$ and $D$ and classifier stability only through $L$.

Thus each precision read is attached to the corresponding bundled center and its pointwise-modulus row. The carrier has no coordinate for an external modulus selector, a host minimum, or a uniform precision result, so compatibility cannot be sourced from any of those objects.
\end{proof}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverPointwiseCompatibility}

\begin{theorem}[SubordinateModulusCover rational subordination exactness]
\label{thm:subordinate-modulus-cover-rational-subordination-exactness}
Let
$$
S=(E,\mathcal{P},C,R,D,M,H,F,L,Q,P,N)
$$
be an accepted $\SubordinateModulusCoverUp$ carrier. Each subordinate radius row in $R$ is below its matching pointwise precision row in $D$ through the rational comparison ledger $F$. This exactness statement is a finite ledger row of the packet and does not assert compact host minima, external radius selection, or a completed uniform-modulus output.
\end{theorem}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverRationalSubordinationExactness}
\begin{proof}
Start with the carrier definition. It displays subordinate radius rows $R$, pointwise precision rows $D$, and the rational comparison ledger $F$ as separate coordinates of the same finite packet.

Apply \autoref{thm:subordinate-modulus-cover-namecert-obligations}. Its TasteGate row names rational subordination exactness for the comparison rows in $F$, binding each radius row in $R$ below the corresponding precision row in $D$.

The handoff remains finite by \autoref{thm:subordinate-modulus-cover-ledger-handoff-obligation}. The comparison is consumed as part of the subordinate-cover ledger before the $\UniformModulusUp$ lower-bound fold, so no host minimum, external radius selector, or fold output is introduced here.
\end{proof}

\begin{theorem}[SubordinateModulusCover carrier admission]
\label{thm:subordinate-modulus-cover-carrier-admission}
Carrier admission for $\SubordinateModulusCoverUp$ is exactly the displayed packet
$$
S=(E,\mathcal{P},C,R,D,M,H,F,L,Q,P,N).
$$
It consumes the $\CompactUp$, $\ContinuousMapUp$, $\MetricUp$, $\RatUp$, $\mathsf{ProbeBundle}$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows shown in \autoref{def:subordinate-modulus-cover-carrier}, and it admits no host open cover, external selector, host minimum, or uniform-modulus fold output.
\end{theorem}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverCarrierAdmission}
\begin{proof}
Project the carrier definition. The listed packet contains the tolerance row, nonempty probe bundle, centers, subordinate radii, pointwise precision rows, pointwise-modulus row, half-radius coverage row, rational comparison ledger, transport, replay, provenance, and local naming.

Apply the NameCert obligations of \autoref{thm:subordinate-modulus-cover-namecert-obligations}. They bind carrier admission, classifier stability, coverage exactness, pointwise compatibility, rational subordination exactness, and consumer non-escape to that same packet.

Use compact coverage exactness, pointwise compatibility, rational subordination exactness, and the uniform handoff theorem. These results account for every analytic row needed by the L10 compact-uniform route, so admission is exhausted by $S$ and cannot come from any host object outside the displayed carrier.
\end{proof}

\begin{theorem}[SubordinateModulusCover radius stability]
\label{thm:subordinate-modulus-cover-radius-stability}
Transport through the $\hsame$ ledger $L$ preserves the subordinate radius rows $R$, pointwise precision rows $D$, half-radius coverage rows $H$, and rational comparison rows $F$ without changing the $\mathsf{ProbeBundle}$ spine $\mathcal{P}$. Thus a transported $\SubordinateModulusCoverUp$ packet presents the same finite subordinate bundle to every $\UniformModulusUp$ consumer.
\end{theorem}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverRadiusStability}
\begin{proof}
Read the transport coordinate $L$ from \autoref{def:subordinate-modulus-cover-carrier}. It is componentwise over the tolerance, bundle, centers, radius histories, precision histories, pointwise rows, coverage rows, and rational comparison rows.

Apply the NameCert obligations and the classifier stability obligation. They restrict transport to the displayed rows and preserve the continuation routes $Q$ through which compact and continuous consumers read the packet.

Now use compact coverage exactness, pointwise compatibility, rational subordination exactness, and the uniform handoff theorem. Since the bundle spine $\mathcal{P}$ remains the same displayed spine and the rows $R,D,H,F$ are transported componentwise, the handoff ledger remains stable without introducing a new bundle, host minimum, or fold output.
\end{proof}

\begin{theorem}[SubordinateModulusCover ledger exactness]
\label{thm:subordinate-modulus-cover-ledger-exactness}
The handoff ledger exposed from an accepted $\SubordinateModulusCoverUp$ packet to $\UniformModulusUp$ is exhausted by the finite bundle $\mathcal{P}$, bundled centers $C$, subordinate radii $R$, pointwise precision rows $D$, half-radius coverage rows $H$, and rational comparison rows $F$, together with the displayed transport, replay, provenance, and local naming rows. It contains no host minimum, no external radius selector, and no fold output.
\end{theorem}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverLedgerExactness}
\begin{proof}
Project the handoff rows from \autoref{def:subordinate-modulus-cover-carrier}. The compact side is $\mathcal{P},C,H$, the pointwise side is $D,M$, and the rational subordination side is $R,F$.

Apply the NameCert obligations, compact coverage exactness, pointwise compatibility, and rational subordination exactness. These theorems attach each ledger row to the same accepted packet and prevent any read from bypassing $L,Q,P,N$.

Finally use the uniform handoff theorem and the carrier admission theorem above. The $\UniformModulusUp$ consumer receives precisely the finite subordinate-cover input ledger before the lower-bound fold, while the fold result and any host minimum remain outside the packet.
\end{proof}

\begin{theorem}[SubordinateModulusCover TasteGate Stage-F discipline]
\label{thm:subordinate-modulus-cover-tastegate-stagef}
The single carrier
$$
S=(E,\mathcal{P},C,R,D,M,H,F,L,Q,P,N)
$$
supplies the Stage-F discipline for $\SubordinateModulusCoverUp$: the finite $\mathsf{ProbeBundle}$ spine is nontrivial, the visible fields are faithful to the carrier, and every $\UniformModulusUp$ consumer must pass through compact coverage, pointwise-modulus rows, rational subordination, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows before any uniform-modulus fold is read.
\end{theorem}
\begin{proof}
Start with carrier admission from \autoref{thm:subordinate-modulus-cover-carrier-admission}. It identifies $S$ as the single accepted packet and keeps the nonempty $\mathsf{ProbeBundle}$ spine, center rows, radius rows, precision rows, pointwise rows, coverage rows, comparison rows, transport, replay, provenance, and local naming visible.

Apply the NameCert obligations of \autoref{thm:subordinate-modulus-cover-namecert-obligations}. They bind the carrier fields to the same TasteGate surface and prevent consumer reads from bypassing the displayed $\hsame$, $\Cont$, $\Pkg$, and local naming rows.

Use radius stability and ledger exactness from \autoref{thm:subordinate-modulus-cover-radius-stability} and \autoref{thm:subordinate-modulus-cover-ledger-exactness}. The compact coverage row, pointwise-modulus row, and rational subordination row remain faithful to $S$ through transport, and the $\UniformModulusUp$ consumer sees only the finite subordinate-cover ledger before the fold output exists.
\end{proof}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverTasteGate\_stageF\_discipline}

\begin{theorem}[SubordinateModulusCover compact-uniform consumer boundary]
\label{thm:subordinate-modulus-cover-compact-uniform-consumer-boundary}
Every $\CompactUp$, $\ContinuousMapUp$, or $\MetricUp$ consumer that reads an accepted $\SubordinateModulusCoverUp$ packet must expose the finite $\mathsf{ProbeBundle}$ spine, pointwise-modulus rows, rational subordination ledger, half-radius coverage row, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ before any $\UniformModulusUp$ fold is read. The consumer boundary is exactly the displayed route through the carrier
$$
S=(E,\mathcal{P},C,R,D,M,H,F,L,Q,P,N),
$$
and it exports no host compact minimum, open-cover selector, external modulus choice, or completed uniform precision.
\end{theorem}
\begin{proof}
Carrier admission identifies the only packet available to the consumer. The rows $\mathcal{P},C,R,D,M,H,F,L,Q,P,N$ are displayed coordinates of the single carrier, and the tolerance row $E$ only records the compact-uniform request that the packet answers.

Apply radius stability, ledger exactness, pointwise compatibility, and rational subordination exactness. These results keep the finite bundle spine, centerwise precision rows, subordinate radii, half-radius coverage row, rational comparison ledger, and transport rows attached to the same accepted packet through $\hsame$ and $\Cont$ replay.

Finally apply the Stage-F discipline. It forces every $\UniformModulusUp$ consumer to read compact coverage, pointwise-modulus data, rational subordination, provenance, replay, and the local naming row before the lower-bound fold is available. Hence a compact, continuous, or metric consumer reaches the fold only through the finite subordinate-cover boundary, with no host compact minimum, selector, external modulus choice, or completed uniform precision.
\end{proof}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverCompactUniformConsumerBoundary}

\begin{theorem}[SubordinateModulusCover choice-free handoff]
\label{thm:subordinate-modulus-cover-choice-free-handoff}
Every accepted $\SubordinateModulusCoverUp$ packet exposes the finite rows $\mathcal{P},C,R,D,M,H,F,L,Q,P,N$ to a $\UniformModulusUp$ consumer before any lower-bound fold is read. No row may be retyped as an external radius selector, host minimum, open-cover compactness witness, or completed uniform precision.
\end{theorem}
\begin{proof}
Start from carrier admission in \autoref{def:subordinate-modulus-cover-carrier}. The rows $\mathcal{P},C,R,D,M,H,F,L,Q,P,N$ are displayed coordinates of the same finite packet, and the tolerance row $E$ only supplies the request read by that packet.

Compact coverage exactness, pointwise compatibility, and rational subordination exactness attach the bundle, centers, subordinate radii, precision rows, pointwise-modulus row, coverage row, and comparison ledger to the same carrier. The uniform handoff theorem then exposes those finite rows to the $\UniformModulusUp$ consumer before the lower-bound fold produces any output.

Ledger exactness rules out the remaining escape routes. Since the carrier contains no selector coordinate, no host minimum coordinate, no open-cover compactness row, and no completed uniform precision row, none of the displayed rows can be retyped as one of those objects.
\end{proof}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverChoiceFreeHandoff}

\begin{theorem}[SubordinateModulusCover finite-selection scope]
\label{thm:subordinate-modulus-cover-finite-selection-scope}
Every accepted $\SubordinateModulusCoverUp$ packet exposes the nonempty $\mathsf{ProbeBundle}$ spine, bundled centers, subordinate positive-radius rows, pointwise precision rows, half-radius coverage rows, and rational comparison ledger as the full finite-selection scope consumed by $\UniformModulusUp$. No host compact minimum, open-cover selector, or completed uniform precision row is present in that scope.
\end{theorem}
\begin{proof}
Project the accepted packet through \autoref{def:subordinate-modulus-cover-carrier}. The finite-selection data are exactly the bundle $\mathcal{P}$, centers $C$, subordinate radii $R$, pointwise precision rows $D$, half-radius coverage rows $H$, and rational comparison ledger $F$, with $L,Q,P,N$ supplying transport, replay, provenance, and local naming.

Apply \autoref{thm:subordinate-modulus-cover-namecert-obligations}. Its carrier admission and consumer non-escape clauses bind those rows to the same local $\NameCert_{\SubordinateModulusCoverUp}$ surface, while compact coverage exactness, pointwise compatibility, and rational subordination exactness account for the three analytic faces of the packet.

Use \autoref{thm:subordinate-modulus-cover-compact-coverage-exactness}, \autoref{thm:subordinate-modulus-cover-pointwise-compatibility}, and \autoref{thm:subordinate-modulus-cover-rational-subordination-exactness}. These theorems attach coverage, centerwise precision, and radius comparison to the displayed finite carrier rather than to an ambient compact object.

Finally apply \autoref{thm:subordinate-modulus-cover-uniform-handoff} and \autoref{thm:subordinate-modulus-cover-choice-free-handoff}. The $\UniformModulusUp$ consumer receives the finite subordinate-cover ledger before the lower-bound fold is read, so the scope contains neither a host compact minimum nor an open-cover selector nor a completed uniform precision row.
\end{proof}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverFiniteSelectionScope}

\begin{theorem}[SubordinateModulusCover obligation-closure route]
\label{thm:subordinate-modulus-cover-obligation-closure-route}
The displayed carrier, TasteGate single-carrier alignment, carrier admission, classifier stability, compact coverage exactness, pointwise compatibility, rational subordination exactness, ledger exactness, choice-free handoff, and finite-selection scope form the obligation surface for $\SubordinateModulusCoverUp$. These rows remain on the single carrier
$$
S=(E,\mathcal{P},C,R,D,M,H,F,L,Q,P,N),
$$
and they expose no host compact minimum, open-cover selector, external modulus choice, completed uniform precision, or carrier outside the packet.
\end{theorem}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverObligationClosureRoute}
\begin{proof}
Start with the carrier of \autoref{def:subordinate-modulus-cover-carrier} and the TasteGate row of \autoref{thm:subordinate-modulus-cover-namecert-obligations}. Together they bind the tolerance, nonempty probe bundle, centers, subordinate radii, pointwise precision rows, pointwise-modulus row, half-radius coverage row, rational comparison ledger, transport, replay, provenance, and local naming to the same packet $S$.

Use carrier admission and classifier stability from \autoref{thm:subordinate-modulus-cover-carrier-admission} and \autoref{thm:subordinate-modulus-cover-classifier-stability-obligation}. These results keep accepted reads and transported reads on the displayed carrier and prevent a consumer from replacing $S$ by an external compact or modulus object.

Apply compact coverage exactness, pointwise compatibility, and rational subordination exactness. The theorems \autoref{thm:subordinate-modulus-cover-compact-coverage-exactness}, \autoref{thm:subordinate-modulus-cover-pointwise-compatibility}, and \autoref{thm:subordinate-modulus-cover-rational-subordination-exactness} account for the coverage, pointwise-modulus, and radius-comparison rows without leaving the packet.

Finally combine ledger exactness, choice-free handoff, and finite-selection scope from \autoref{thm:subordinate-modulus-cover-ledger-exactness}, \autoref{thm:subordinate-modulus-cover-choice-free-handoff}, and \autoref{thm:subordinate-modulus-cover-finite-selection-scope}. The $\UniformModulusUp$ consumer receives exactly the finite subordinate-cover ledger before the fold output, so the named rows form the obligation-closure route and no hidden selection or completed uniform precision is exported.
\end{proof}

\falsifiablePrediction{Every accepted $\SubordinateModulusCoverUp$ consumer reaches $\UniformModulusUp$ only after displaying compact coverage, continuousmap pointwise-modulus rows, metric radius rows, rat comparison rows, rational subordination, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{compact, continuousmap, metric, rat, uniformmodulus: no listed sibling is bijective to $\SubordinateModulusCoverUp$ because this packet jointly carries a nonempty probe bundle, bundled centers, subordinate radius rows, pointwise precision rows, half-radius coverage, rational comparison, transport, replay, provenance, and local naming before the uniform-modulus fold.}

\closureat{SubordinateModulusCoverUp}{seedStr}

\begin{theorem}[Subordinate modulus cover public export]
\label{thm:subordinate-modulus-cover-public-export}
Every accepted $\SubordinateModulusCoverUp$ carrier exports exactly the public finite compact-source spine, $\ContinuousMapUp$ pointwise-modulus rows, $\RatUp$ lower-bound comparison ledger, $\UniformModulusUp$ handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\SubordinateModulusCoverUp}$ surface. The exported route is the packet
$$
S=(E,\mathcal{P},C,R,D,M,H,F,L,Q,P,N),
$$
read through carrier admission, classifier stability, compact coverage exactness, pointwise compatibility, rational subordination exactness, ledger exactness, and the finite-selection scope. It exports no open-cover compactness theorem, selected subcover, countable-choice row, ambient $\RealUp$ completeness, host compact minimum, or completed uniform precision outside the displayed packet.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:subordinate-modulus-cover-carrier}. The rows $\mathcal{P},C,R,D,M,H,F,L,Q,P,N$ are the only compact-source, center, radius, pointwise-modulus, coverage, rational-comparison, transport, replay, provenance, and naming coordinates available to a public consumer.

Use the current cover and modulus obligations. Carrier admission and classifier stability keep the read on the same packet; compact coverage exactness, pointwise compatibility, and rational subordination exactness identify the finite source, pointwise-modulus, and rational lower-bound rows; ledger exactness prevents a consumer from replacing them by a host compactness or selector object.

Finally compose the handoff results. The uniform handoff, choice-free handoff, finite-selection scope, and scoped package route expose the $\UniformModulusUp$ consumer only after the finite subordinate-cover rows, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local naming surface are displayed. Since the carrier has no coordinate for open-cover compactness, selected subcovers, countable choice, ambient $\RealUp$ completeness, host compact minima, or completed uniform precision, none of those objects is exported.
\end{proof}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverPublicExport}

\begin{theorem}[SubordinateModulusCover scoped package]
\label{thm:subordinate-modulus-cover-scoped-package}
The carrier admission, classifier stability, compact coverage exactness, pointwise compatibility, rational subordination exactness, and uniform-modulus handoff theorems form the complete scoped package for $\SubordinateModulusCoverUp$. Every accepted packet remains on the displayed carrier over $\CompactUp$, $\ContinuousMapUp$, $\MetricUp$, $\RatUp$, $\mathsf{ProbeBundle}$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, and $\UniformModulusUp$; no external compact minimum, open-cover selector, ambient modulus choice, completed uniform precision, or carrier outside the packet is exported.
\end{theorem}
\begin{proof}
Begin with carrier admission and classifier stability. The theorems \autoref{thm:subordinate-modulus-cover-carrier-admission} and \autoref{thm:subordinate-modulus-cover-classifier-stability-obligation} keep every accepted and transported read on the displayed carrier $S=(E,\mathcal{P},C,R,D,M,H,F,L,Q,P,N)$.

Read the analytic rows through the exactness theorems. Compact coverage exactness, pointwise compatibility, and rational subordination exactness are supplied by \autoref{thm:subordinate-modulus-cover-compact-coverage-exactness}, \autoref{thm:subordinate-modulus-cover-pointwise-compatibility}, and \autoref{thm:subordinate-modulus-cover-rational-subordination-exactness}; each theorem consumes the same probe bundle, center rows, radius rows, pointwise-modulus rows, half-radius coverage rows, and rational ledger.

Finally apply \autoref{thm:subordinate-modulus-cover-uniform-handoff}. The $\UniformModulusUp$ consumer receives the finite subordinate-cover packet before any lower-bound fold is read, while $L,Q,P,N$ preserve transport, replay, provenance, and local naming.

The remaining refusal follows from the absence of extra carrier coordinates. The scoped package contains neither an external compact minimum nor an open-cover selector nor an ambient modulus-choice row nor a completed uniform-precision row.
\end{proof}
\leanchecked{BEDC.Derived.SubordinateModulusCoverUp.SubordinateModulusCoverScopedPackage}

\begin{closurestatus}{\SubordinateModulusCoverUp}
  \constructivestory{A $\SubordinateModulusCoverUp$ packet is generated from a positive tolerance row, a nonempty $\mathsf{ProbeBundle}$ spine, bundled centers, subordinate positive-radius rows, pointwise-modulus precision rows, half-radius coverage rows, rational comparison ledgers, transport rows, continuation routes, package provenance, and a local $\NameCert$ row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{thm:subordinate-modulus-cover-scoped-package} binds carrier admission, classifier stability, compact coverage exactness, pointwise compatibility, rational subordination exactness, and the uniform-modulus handoff to the same displayed packet.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close the rational lower-bound fold, the final uniform-modulus ledger, compactness of host open covers, external modulus selection, or bridge checking.}
  \upgradepath{Public closure requires a public consumer theorem for the same displayed subordinate-cover packet and checked Lean obligations for the route.}
\end{closurestatus}
