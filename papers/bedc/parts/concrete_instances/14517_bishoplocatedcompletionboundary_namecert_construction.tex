\chapter{A Concrete Naming Certificate for $\BishopLocatedCompletionBoundaryUp$}
\label{ch:concrete-instances-bishoplocatedcompletionboundary-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopLocatedCompletionBoundaryUp$ packet names the finite boundary at which a regular Cauchy rational source, locatedness evidence, and the displayed limit route are all visible before a $\RealUp$ completion-facing seal is read. It sits after the regular located Cauchy surface of \autoref{ch:concrete-instances-regular-located-cauchy-real-namecert}, the located-limit route of \autoref{ch:concrete-instances-locatedlimit-namecert}, the located-real source of \autoref{ch:concrete-instances-locatedreal-namecert}, the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational source of \autoref{ch:concrete-instances-regseqrat-namecert}, the stream-window source of \autoref{ch:concrete-instances-streamname-namecert}, and the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The chapter records only the NameCert boundary for located completion data; it does not assert that $\RealUp$ is complete.

\begin{definition}[Bishop located completion boundary carrier]
\label{def:bishop-located-completion-boundary-carrier}
A $\BishopLocatedCompletionBoundaryUp$ carrier is a finite $\BHist$ packet
$$
B=(S,Q,D,R,L,E,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ window family naming the finite observations of the rational Cauchy source.
\item $Q$ is the $\RegSeqRatUp$ regular rational source row read only through the windows in $S$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance, endpoint, and radius ledger attached to those windows.
\item $R$ is the $\RegularLocatedCauchyRealUp$ row that binds the regular source, dyadic ledger, locatedness evidence, and terminal real-name seal as one visible route.
\item $L$ is the $\LocatedLimitUp$ route by which the same displayed source is read as a located limit candidate.
\item $E$ is the $\LocatedRealUp$ enclosure row used at the location boundary before any terminal real-name consumer is allowed to inspect a seal.
\item $T$ is the $\RealUp$ seal row reached only after $S,Q,D,R,L,E$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the stream, regular-source, dyadic, regular-located, located-limit, located-real, Real-seal, continuation, provenance, and local naming rows.
\item $C$ records the displayed $\Cont$ replay for every completion-facing request through the same rows.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedCompletionBoundaryUp$, $\RegularLocatedCauchyRealUp$, $\LocatedLimitUp$, $\LocatedRealUp$, $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopLocatedCompletionBoundaryUp}$ row.
\end{enumerate}
The carrier has no coordinate for a proof of $\RealUp$ completeness, quotient equality of Cauchy names, real trichotomy, countable choice, open-cover compactness, or a host-selected limit.
\end{definition}
\leanchecked{BEDC.Derived.BishopLocatedCompletionBoundaryUp.TasteGate.BishopLocatedCompletionBoundaryTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Regular Cauchy extraction at the located boundary]
\label{thm:bishop-located-completion-boundary-regular-cauchy-extraction}
Every accepted $\BishopLocatedCompletionBoundaryUp$ packet $B=(S,Q,D,R,L,E,T,H,C,P,N)$ exposes its regular Cauchy source before any located completion read. The extracted source surface is exactly the finite route
$$
S,\quad Q,\quad D,\quad R
$$
with the visible transport, replay, provenance, and naming rows $H,C,P,N$. No consumer can obtain a completion-facing $\RealUp$ seal from $B$ without first reading the displayed StreamName windows, RegSeqRat source, DyadicRatCore tolerance ledger, and RegularLocatedCauchyReal route.
\end{theorem}
\leanchecked{BEDC.Derived.BishopLocatedCompletionBoundaryUp.BishopLocatedCompletionBoundaryRegularCauchyExtraction}
\begin{proof}
Project the carrier of \autoref{def:bishop-located-completion-boundary-carrier}. The only regular Cauchy data present are the stream windows $S$, regular rational source $Q$, dyadic tolerance ledger $D$, and regular located Cauchy row $R$.

Now read any completion-facing request. The row $T$ is stored after the located-limit and located-real rows, and those rows are themselves attached to $S,Q,D,R$. Thus the request must first pass through the extracted regular Cauchy source route.

The transport, replay, provenance, and local naming rows preserve this same route. Since the carrier contains no independent completion coordinate, the displayed extraction exhausts the regular Cauchy surface available to the consumer.
\end{proof}

\begin{theorem}[Located limit route at the completion boundary]
\label{thm:bishop-located-completion-boundary-located-limit-route}
For every accepted $\BishopLocatedCompletionBoundaryUp$ packet $B=(S,Q,D,R,L,E,T,H,C,P,N)$, every located completion read factors through
$$
S\longmapsto Q\longmapsto D\longmapsto R\longmapsto L\longmapsto E\longmapsto T .
$$
The route uses the located-limit row $L$ and located-real enclosure row $E$ as visible finite data before the $\RealUp$ seal row $T$ is read, and it exports no separate host limit, quotient equality row, or choice-selected subsequence.
\end{theorem}
\leanchecked{BEDC.Derived.BishopLocatedCompletionBoundaryUp.BishopLocatedCompletionBoundaryLocatedLimitRoute}
\begin{proof}
Begin with the extraction theorem \autoref{thm:bishop-located-completion-boundary-regular-cauchy-extraction}. It places the stream, regular rational, dyadic tolerance, and regular located Cauchy rows before any completion-facing seal.

The carrier definition then places $L$ and $E$ between that extracted source and the row $T$. Hence a located completion read must first display the located-limit route and the located-real enclosure row attached to the same finite source.

Finally apply the structural rows. Componentwise transport and replay keep the consumer on $S,Q,D,R,L,E,T$, while provenance and local naming record the same packet. No coordinate outside this route can supply a host limit or a quotient equality proof.
\end{proof}

\begin{theorem}[Bishop located completion boundary NameCert obligations]
\label{thm:bishop-located-completion-boundary-namecert-obligations}
The seed $\NameCert_{\BishopLocatedCompletionBoundaryUp}$ surface for \autoref{def:bishop-located-completion-boundary-carrier} consists exactly of carrier exposure for $B$, regular Cauchy extraction through $S,Q,D,R$, located-limit routing through $L$, located-real enclosure through $E$, terminal $\RealUp$ seal exposure through $T$, componentwise transport through $H$, displayed continuation replay through $C$, package provenance through $P$, and local naming through $N$. These obligations bind the finite located completion boundary without adding $\RealUp$ completeness, quotient equality, trichotomy, countable choice, open-cover compactness, or a host-selected limit.
\end{theorem}
\begin{proof}
Carrier exposure is projection from the displayed tuple $B=(S,Q,D,R,L,E,T,H,C,P,N)$. The analytic rows are exactly the stream, regular rational, dyadic, regular located Cauchy, located-limit, located-real, and Real-seal rows named in the carrier.

Regular Cauchy extraction is supplied by \autoref{thm:bishop-located-completion-boundary-regular-cauchy-extraction}. Located routing is supplied by \autoref{thm:bishop-located-completion-boundary-located-limit-route}. Together they put every completion-facing read on one finite route through $S,Q,D,R,L,E,T$.

The remaining rows are structural. The $\hsame$ transports and $\Cont$ replay preserve the same finite route, while $\Pkg$ provenance and the local $\NameCert$ row name the packet. Since no excluded principle appears as a carrier coordinate, the listed rows exhaust the seed obligation surface.
\end{proof}
\leanchecked{BEDC.Derived.BishopLocatedCompletionBoundaryUp.BishopLocatedCompletionBoundaryNameCertObligations}

\begin{theorem}[Bishop located completion boundary located Real-seal handoff]
\label{thm:bishoplocatedcompletionboundary-located-real-seal-handoff}
Every accepted $\BishopLocatedCompletionBoundaryUp$ packet reaches its $\RealUp$ seal $T$ only through the displayed route
$$
S\longmapsto Q\longmapsto D\longmapsto R\longmapsto L\longmapsto E\longmapsto T .
$$
The handoff first reads the $\DyadicRatCoreUp$ tolerance rows $D$, the finite $\StreamNameUp$ windows $S$, the $\RegSeqRatUp$ regularity and readback row $Q$, and the located-real comparison row $E$. It preserves the same route through componentwise $\hsame$, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\BishopLocatedCompletionBoundaryUp}$ row, and it supplies no ambient $\RealUp$ completeness theorem or quotient equality of real names.
\end{theorem}
\begin{proof}
Unpack the carrier of \autoref{def:bishop-located-completion-boundary-carrier}. Its analytic rows are exactly $S,Q,D,R,L,E,T$, with $T$ placed after the dyadic tolerance ledger, stream windows, regular rational source, regular located Cauchy route, located-limit route, and located-real enclosure row.

Apply regular Cauchy extraction, \autoref{thm:bishop-located-completion-boundary-regular-cauchy-extraction}. It forces every completion-facing read to expose $S,Q,D,R$ before a terminal seal can be inspected, so the dyadic tolerance rows, finite windows, and regularity/readback row are already displayed.

Next apply located-limit routing, \autoref{thm:bishop-located-completion-boundary-located-limit-route}. The row $E$ is the located-real comparison row immediately before $T$, so the Real-seal handoff cannot bypass locatedness data or replace it by a host limit.

Finally use the obligation surface. Componentwise $\hsame$ transport and $\Cont$ replay keep the same route, while $\Pkg$ provenance and the local $\NameCert$ row name it as one packet. Since the carrier has no coordinate for $\RealUp$ completeness or quotient equality, the handoff exports only the displayed seal row $T$ over the finite located boundary.
\end{proof}
\leanchecked{BEDC.Derived.BishopLocatedCompletionBoundaryUp.BishopLocatedCompletionBoundaryLocatedRealSealHandoff}

\begin{theorem}[Bishop located completion boundary regular route]
\label{thm:bishop-located-completion-boundary-regular-route}
Any accepted $\BishopLocatedCompletionBoundaryUp$ read that reaches the terminal seal $T$ must first expose the rows
$$
S,Q,D,R,L,E
$$
in that order. The $\RegularLocatedCauchyRealUp$ row $R$, the $\LocatedLimitUp$ row $L$, and the $\LocatedRealUp$ row $E$ are therefore mandatory boundary rows, not replaceable by a host completion, quotient equality, selected limit, ambient $\RealUp$ completeness theorem, trichotomy, countable-choice schedule, or open-cover compactness principle.
\end{theorem}
\begin{proof}
Apply regular Cauchy extraction, \autoref{thm:bishop-located-completion-boundary-regular-cauchy-extraction}. It exposes $S,Q,D,R$ before any terminal seal can be read, so the regular located Cauchy row cannot be replaced by a host completion row.

Next apply the located-limit route, \autoref{thm:bishop-located-completion-boundary-located-limit-route}. This places $L$ after the regular route and before any located Real enclosure can be consumed.

Now use the NameCert obligations of \autoref{thm:bishop-located-completion-boundary-namecert-obligations} and the located Real-seal handoff of \autoref{thm:bishoplocatedcompletionboundary-located-real-seal-handoff}. These keep $E$ immediately before $T$ on the same finite route and preserve it under transport, replay, provenance, and local naming.

Since the carrier has no coordinate for host completion, quotient equality, selected limits, completeness, trichotomy, countable choice, or compactness, every terminal read reaches $T$ only through the displayed regular route.
\end{proof}
\leanchecked{BEDC.Derived.BishopLocatedCompletionBoundaryUp.BishopLocatedCompletionBoundaryRegularRoute}

\begin{theorem}[Bishop located completion boundary window exhaustion]
\label{thm:bishop-located-completion-boundary-window-exhaustion}
Every accepted $\BishopLocatedCompletionBoundaryUp$ packet $B=(S,Q,D,R,L,E,T,H,C,P,N)$ exposes all completion-facing finite windows through $S$ and all tolerance and readback data through $D,Q,R,L,E$ before $T$ is consumed. No completion-facing consumer can add a second $\StreamNameUp$ schedule, quotient equality, selected limit, countable choice, trichotomy, open-cover compactness, or $\RealUp$ completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.BishopLocatedCompletionBoundaryUp.BishopLocatedCompletionBoundaryWindowExhaustion}
\begin{proof}
Project the carrier of \autoref{def:bishop-located-completion-boundary-carrier}. The only finite-window row is $S$, and the only tolerance, regular readback, regular-located, located-limit, and located-real rows are $D,Q,R,L,E$.

Apply regular Cauchy extraction, \autoref{thm:bishop-located-completion-boundary-regular-cauchy-extraction}. It forces every completion-facing read to display $S,Q,D,R$ before the terminal seal row can be inspected.

Apply located-limit routing, \autoref{thm:bishop-located-completion-boundary-located-limit-route}, and then the located Real-seal handoff, \autoref{thm:bishoplocatedcompletionboundary-located-real-seal-handoff}. These two results place $L,E$ after the extracted source and before $T$, so the consumer cannot bypass locatedness or replace the route by a host-selected limit.

Finally use $H,C,P,N$. Componentwise $\hsame$ transport, displayed $\Cont$ replay, package provenance, and local naming preserve the same route $S,Q,D,R,L,E,T$. Since none of those structural rows contains a second stream schedule, quotient equality, choice principle, trichotomy, compactness principle, or completeness coordinate, the displayed finite windows are exhausted before $T$ is consumed.
\end{proof}

\begin{theorem}[Bishop located completion boundary inductive route]
\label{thm:bishop-located-completion-boundary-inductive-route}
Every accepted terminal read of a $\BishopLocatedCompletionBoundaryUp$ carrier
$$
B=(S,Q,D,R,L,E,T,H,C,P,N)
$$
is generated by induction on the displayed route: first the $\StreamNameUp$ windows $S$, then the $\RegSeqRatUp$ source $Q$, the $\DyadicRatCoreUp$ ledger $D$, the $\RegularLocatedCauchyRealUp$ row $R$, the $\LocatedLimitUp$ row $L$, the $\LocatedRealUp$ row $E$, and the terminal $\RealUp$ seal $T$; only after those analytic rows are present may the structural rows $H,C,P,N$ transport, replay, source, and name the same read. The induction exposes no quotient equality, selected limit, countable choice, trichotomy, open-cover compactness, or $\RealUp$ completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.BishopLocatedCompletionBoundaryUp.BishopLocatedCompletionBoundaryInductiveRoute}
\begin{proof}
The base of the route is the carrier projection in \autoref{def:bishop-located-completion-boundary-carrier}. It lists $S$ as the only stream-window row and places the regular source, dyadic ledger, located rows, terminal seal, and structural rows in the displayed order.

For the first analytic steps, apply regular Cauchy extraction and the located-limit route. The extraction theorem forces $S,Q,D,R$ to be visible before any completion-facing seal, while the located-limit theorem extends the route through $L$ and $E$ before $T$ can be read.

The Real-seal handoff and the regular-route theorem provide the terminal step. They identify $T$ as downstream of $S,Q,D,R,L,E$ and rule out replacement by a host completion, quotient equality, selected limit, countable choice, trichotomy, open-cover compactness, or ambient Real completeness.

It remains to account for the structural rows. The NameCert obligations and window-exhaustion theorem preserve the same finite route under $H,C,P,N$ and show that no second window schedule or hidden completion coordinate is available. Thus induction over the displayed rows generates every accepted terminal read.
\end{proof}

\begin{theorem}[Bishop located completion boundary obligation carrier]
\label{thm:bishop-located-completion-boundary-obligation-carrier}
Every accepted $\BishopLocatedCompletionBoundaryUp$ packet is admitted to the obligation surface only as the eleven-row carrier
$$
B=(S,Q,D,R,L,E,T,H,C,P,N)
$$
of \autoref{def:bishop-located-completion-boundary-carrier}. The admitted rows are exactly the $\StreamNameUp$ window family, $\RegSeqRatUp$ source row, $\DyadicRatCoreUp$ tolerance ledger, $\RegularLocatedCauchyRealUp$ route, $\LocatedLimitUp$ route, $\LocatedRealUp$ enclosure row, terminal $\RealUp$ seal, componentwise transport, displayed replay, provenance, and local naming. No additional carrier coordinate may supply $\RealUp$ completeness, quotient equality, trichotomy, countable choice, open-cover compactness, or a host-selected limit.
\end{theorem}
\leanchecked{BEDC.Derived.BishopLocatedCompletionBoundaryUp.BishopLocatedCompletionBoundaryObligationCarrier}
\begin{proof}
Project the carrier definition. It lists precisely $S,Q,D,R,L,E,T,H,C,P,N$ and places all completion-facing analytic data before the terminal seal.

Apply regular Cauchy extraction, located-limit routing, the located Real-seal handoff, and window exhaustion. These results show that the stream windows, regular rational source, dyadic ledger, located route, enclosure row, and seal are consumed as one visible boundary.

The structural rows only preserve that boundary. Since provenance and local naming record the same packet and no excluded principle is present as a row, the obligation carrier is exactly the displayed eleven-row surface.
\end{proof}

\begin{theorem}[Bishop located completion boundary scope]
\label{thm:bishop-located-completion-boundary-scope}
Every scoped route exposed by a $\BishopLocatedCompletionBoundaryUp$ packet passes through
$$
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \RegularLocatedCauchyRealUp \longmapsto \LocatedLimitUp \longmapsto \LocatedRealUp \longmapsto \RealUp .
$$
The route is read through the displayed rows $S,Q,D,R,L,E,T$ and the structural rows $H,C,P,N$ before any completion-facing consumer can cite it. It does not expose a host completion, quotient real equality, trichotomy, countable-choice schedule, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.BishopLocatedCompletionBoundaryUp.BishopLocatedCompletionBoundaryScope}
\begin{proof}
Start with \autoref{thm:bishop-located-completion-boundary-regular-route}. It forces the rows $S,Q,D,R,L,E$ to appear before the terminal seal $T$ can be read.

Use window exhaustion and the NameCert obligations. They identify $S$ as the only finite-window schedule, $Q$ as the regular rational source, $D$ as the dyadic ledger, and $R,L,E$ as the located completion boundary before the $\RealUp$ seal.

Finally apply componentwise transport and displayed replay. The scope is preserved under $H$ and $C$, while $P$ and $N$ name the same packet, so no outside completion, equality, order, choice, compactness, or completeness datum enters the route.
\end{proof}

\begin{theorem}[Bishop located completion boundary scoped route]
\label{thm:bishop-located-completion-boundary-scoped-route}
Every scoped completion-boundary read from an accepted $\BishopLocatedCompletionBoundaryUp$ packet must pass through
$$
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \RegularLocatedCauchyRealUp \longmapsto \LocatedLimitUp \longmapsto \LocatedRealUp \longmapsto \RealUp
$$
together with the componentwise $\hsame$ row, displayed $\Cont$ replay row, $\Pkg$ provenance row, and local $\NameCert$ row. The route is exactly the eleven-row carrier
$$
B=(S,Q,D,R,L,E,T,H,C,P,N)
$$
and admits no host completion, quotient equality, selected limit, trichotomy, countable-choice schedule, open-cover compactness, ambient $\RealUp$ completeness, or bridge-checking row.
\end{theorem}
\leanchecked{BEDC.Derived.BishopLocatedCompletionBoundaryUp.BishopLocatedCompletionBoundaryScopedRoute}
\begin{proof}
Project the obligation carrier of \autoref{thm:bishop-located-completion-boundary-obligation-carrier}. It admits only $S,Q,D,R,L,E,T,H,C,P,N$, so every scoped read begins inside the eleven displayed rows.

Apply regular Cauchy extraction and the located-limit route. These place the $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RegularLocatedCauchyRealUp$, $\LocatedLimitUp$, and $\LocatedRealUp$ rows before the terminal $\RealUp$ seal can be read.

Next use the located Real-seal handoff and the scope theorem. The seal row $T$ is reachable only through the displayed located route, and the scope theorem records the same ordered kernel path as a boundary for completion-facing consumers.

Finally use the ledger theorem. The structural rows $H,C,P,N$ transport, replay, source, and name the same route, while the ledger has no coordinate for host completion, quotient equality, selected limits, order principles, choice schedules, compactness, ambient completeness, or bridge checking. Thus every scoped read is confined to the stated kernel route.
\end{proof}

\begin{theorem}[Bishop located completion boundary ledger]
\label{thm:bishop-located-completion-boundary-ledger}
The ledger of an accepted $\BishopLocatedCompletionBoundaryUp$ packet is non-escaping: every completion-facing read is accounted for by the stream windows $S$, regular source $Q$, dyadic ledger $D$, regular located route $R$, located-limit route $L$, located-real enclosure $E$, terminal seal $T$, and structural rows $H,C,P,N$. The ledger has no row for $\RealUp$ completeness, quotient equality of Cauchy names, real trichotomy, countable choice, open-cover compactness, a host-selected limit, or bridge checking.
\end{theorem}
\begin{proof}
Read the obligation carrier theorem. It admits only the displayed eleven rows and refuses every extra completion, equality, order, choice, compactness, selected-limit, or bridge coordinate.

Apply the scope theorem. Any consumer that reaches the terminal seal has already passed through the StreamName, RegSeqRat, DyadicRatCore, RegularLocatedCauchyReal, LocatedLimit, LocatedReal, and Real route.

The remaining rows are bookkeeping inside the same packet: $H$ transports components, $C$ replays displayed continuations, $P$ records provenance, and $N$ gives local naming. Therefore the ledger cannot export any refused endpoint.
\end{proof}
\leanchecked{BEDC.Derived.BishopLocatedCompletionBoundaryUp.BishopLocatedCompletionBoundaryLedger}

\begin{theorem}[Bishop located completion boundary public interface]
\label{thm:bishop-located-completion-boundary-public-interface}
Every public read of an accepted $\BishopLocatedCompletionBoundaryUp$ packet factors through the displayed route
$$
\StreamNameUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp,\ \RegularLocatedCauchyRealUp,\ \LocatedLimitUp,\ \LocatedRealUp,\ \RealUp
$$
together with the componentwise $\hsame$ row, displayed $\Cont$ replay row, $\Pkg$ provenance row, and local $\NameCert_{\BishopLocatedCompletionBoundaryUp}$ row. The public interface exposes only those finite rows and explicitly refuses $\RealUp$ completeness, quotient equality of Cauchy names, selected limits, countable choice, real trichotomy, open-cover compactness, and bridge checking.
\end{theorem}
\leanchecked{BEDC.Derived.BishopLocatedCompletionBoundaryUp.BishopLocatedCompletionBoundaryPublicInterface}
\begin{proof}
Project the scoped route theorem and the ledger theorem. They identify every completion-facing read with the eleven-row carrier $S,Q,D,R,L,E,T,H,C,P,N$ and place the StreamName, RegSeqRat, DyadicRatCore, RegularLocatedCauchyReal, LocatedLimit, LocatedReal, and Real rows before any terminal seal can be consumed.

The structural rows do not enlarge the interface. Componentwise $\hsame$ transport and displayed $\Cont$ replay preserve the same route, while $\Pkg$ provenance and the local $\NameCert$ row name the packet.

Since the ledger has no coordinate for completeness, quotient equality, selected limits, choice, trichotomy, compactness, or bridge checking, the public read is exactly the displayed finite boundary and none of the refused endpoints is exported.
\end{proof}

\begin{closurestatus}{\BishopLocatedCompletionBoundaryUp}
  \constructivestory{A $\BishopLocatedCompletionBoundaryUp$ packet is generated from finite $\StreamNameUp$ windows, a $\RegSeqRatUp$ regular rational source, a $\DyadicRatCoreUp$ tolerance ledger, a $\RegularLocatedCauchyRealUp$ route, a $\LocatedLimitUp$ route, a $\LocatedRealUp$ enclosure row, a terminal $\RealUp$ seal, componentwise $\hsame$ transports, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:bishop-located-completion-boundary-carrier}, \autoref{thm:bishop-located-completion-boundary-regular-cauchy-extraction}, \autoref{thm:bishop-located-completion-boundary-located-limit-route}, \autoref{thm:bishop-located-completion-boundary-obligation-carrier}, \autoref{thm:bishop-located-completion-boundary-scope}, and \autoref{thm:bishop-located-completion-boundary-ledger} bind the finite located completion boundary over $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RegularLocatedCauchyRealUp$, $\LocatedLimitUp$, $\LocatedRealUp$, and $\RealUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim $\RealUp$ completeness, quotient equality, trichotomy, countable choice, open-cover compactness, a host-selected limit, or bridge checking.}
  \upgradepath{The next paper obligation is public closure for the same located completion boundary, with formal verification still pending.}
\end{closurestatus}
