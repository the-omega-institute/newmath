\chapter{A Concrete Naming Certificate for $\AbelUniformLimitUp$}
\label{ch:concrete-instances-abel-uniform-limit-namecert}
\origin{human}

Following Abel and Bishop--Bridges constructive analysis, the $\AbelUniformLimitUp$ packet records the finite route by which a displayed real power series is read uniformly on a closed radial window before a boundary Real consumer sees a seal. It sits beside the majorant route of \autoref{ch:concrete-instances-weierstrassmtest-namecert}, the uniform-limit handoff of \autoref{ch:concrete-instances-uniformlimit-namecert}, and the real-series source of \autoref{ch:concrete-instances-realseries-namecert}. The packet names only finite coefficient windows, radial parameter ledgers, majorant-tail budgets, regular rational readback, and Real sealing; it does not import host function equality, selected sums, open-cover compactness, choice, or ambient $\RealUp$ completeness.

\begin{definition}[Abel uniform-limit carrier]
\label{def:abel-uniform-limit-carrier}
An $\AbelUniformLimitUp$ carrier is a finite $\BHist$ packet
$$
A=(S,W,J,M,T,U,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\RealSeriesUp$ source row, exposing coefficient and partial-sum data only through the real-series packet.
\item $W$ is the finite power-series window row, recording the inspected coefficient indices, argument samples, partial sums, and endpoint-tolerance requests.
\item $J$ is the radial parameter ledger, recording the center, displayed radius, inner closed-window bound, and the finite slack that keeps every sample inside the admitted radial window.
\item $M$ is the $\WeierstrassMTestUp$ majorant row used on the same radial window.
\item $T$ is the majorant-tail row, storing the finite tail budget that the M-test row supplies for the displayed window.
\item $U$ is the $\UniformLimitUp$ handoff row that turns $W,J,M,T$ into a uniform Cauchy route on the closed radial window.
\item $R$ is the $\RegSeqRatUp$ readback row reached only after the uniform-limit handoff has exposed the same windows and tail budget.
\item $E$ is the $\RealUp$ seal row consumed after the regular rational readback.
\item $H$ records componentwise $\hsame$ transport for source, window, radial, majorant, tail, handoff, readback, seal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay for the route from radial window data through majorant tails to the uniform-limit readback.
\item $P$ is the $\Pkg$ provenance over $\AbelUniformLimitUp$, $\RealSeriesUp$, $\WeierstrassMTestUp$, $\UniformLimitUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\AbelUniformLimitUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for quotient function equality, a selected limiting sum, open-cover compactness, choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Abel uniform-limit NameCert obligations]
\label{thm:abel-uniform-limit-namecert-obligations}
For the carrier of \autoref{def:abel-uniform-limit-carrier}, the local $\NameCert_{\AbelUniformLimitUp}$ surface consists exactly of carrier admission for $A=(S,W,J,M,T,U,R,E,H,C,P,N)$; $\RealSeriesUp$ source exposure; finite power-series window exposure; radial parameter ledger exactness; $\WeierstrassMTestUp$ majorant-tail admission; $\UniformLimitUp$ handoff; $\RegSeqRatUp$ readback; $\RealUp$ seal nonescape; componentwise $\hsame$ transport; displayed $\Cont$ replay; $\Pkg$ provenance; and local naming.
\end{theorem}
\begin{proof}
Project the finite carrier of \autoref{def:abel-uniform-limit-carrier}. Rows $S,W,J$ display the coefficient source, the inspected power-series windows, and the radial ledger before any convergence read is available.

The majorant part is $M,T$. It is read through \autoref{ch:concrete-instances-weierstrassmtest-namecert}, so domination and tail control are finite displayed rows rather than a host convergence theorem.

The handoff part is $U,R,E$. The uniform-limit row is the only route from the radial majorant data to a regular rational readback, and the Real seal is downstream of that readback.

Finally $H,C,P,N$ transport, replay, source, and name the same packet. Since no carrier coordinate stores quotient function equality, a selected sum, compactness by open covers, choice data, or ambient completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Abel uniform-limit Real seal route]
\label{thm:abel-uniform-limit-real-seal-route}
Every $\RealUp$ seal read of an accepted $\AbelUniformLimitUp$ packet factors through the finite route
$$
\RealSeriesUp \longmapsto \WeierstrassMTestUp \longmapsto \UniformLimitUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
The route first displays power-series windows and a radial ledger, then reads the majorant tail, then hands the same data to the uniform-limit row, and only then exposes the regular rational readback and terminal Real seal.
\end{theorem}
\begin{proof}
Start with $S,W,J$. These rows make the coefficient source, finite windows, and closed radial window visible to the consumer.

Next read $M,T$. The Weierstrass M-test sibling supplies majorant domination and the finite tail budget on the same radial window, so the packet has no need for an unrecorded host summation object.

Apply the uniform-limit row $U$. It consumes the displayed windows, radial ledger, and tail budget before any $\RegSeqRatUp$ readback $R$ is exposed.

The Real seal $E$ is therefore downstream of $R$, with $H,C,P,N$ preserving and naming the same dependency order. No quotient function equality, selected limiting sum, open-cover compactness, choice principle, or ambient completeness row is exported by this route.
\end{proof}

\falsifiablePrediction{Every accepted Abel uniform-limit endpoint read factors through displayed RealSeries source rows, finite power-series windows, radial parameter ledger, WeierstrassMTest majorant-tail data, UniformLimit handoff, RegSeqRat readback, Real seal, transport, replay, provenance, and local NameCert rows.}

\independenceWitness{weierstrassmtest, uniformlimit, realseries, regseqrat, real: no listed sibling supplies the whole packet $A=(S,W,J,M,T,U,R,E,H,C,P,N)$, because each sibling contributes only source series data, majorant tails, uniform-limit handoff, regular readback, or terminal Real sealing while this chapter binds them to the same radial power-series window.}

\closureat{AbelUniformLimitUp}{seedStr}

\begin{closurestatus}{\AbelUniformLimitUp}
  \constructivestory{An $\AbelUniformLimitUp$ packet is generated from RealSeries source rows, finite power-series windows, a radial parameter ledger, WeierstrassMTest majorant-tail data, a UniformLimit handoff, RegSeqRat readback, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:abel-uniform-limit-carrier}, \autoref{thm:abel-uniform-limit-namecert-obligations}, and \autoref{thm:abel-uniform-limit-real-seal-route} seed the constructive L10 power-series uniform-limit route from finite radial windows to regular rational readback and the Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient function equality, a selected limiting sum, open-cover compactness, choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, radial ledger exactness, majorant-tail coverage, UniformLimit handoff, RegSeqRat readback, Real seal nonescape, transport, replay, provenance, and local NameCert registration for BEDC.Derived.AbelUniformLimitUp.}
\end{closurestatus}
