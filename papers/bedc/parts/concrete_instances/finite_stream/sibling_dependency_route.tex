Every $\FiniteStreamUp$ read consumed by \autoref{ch:concrete-instances-regseqrat-namecert} or \autoref{ch:concrete-instances-real-namecert} first passes through the finite-observation surface of \autoref{ch:concrete-instances-streamname-namecert}. The packet therefore exposes its request row, list spine, and StreamName window before the RegSeqRat readback boundary or the Real seal can be inspected; no consumer receives an ambient infinite stream or a selected real limit outside the displayed finite route.

\begin{theorem}[FiniteStream sibling dependency route]
\label{thm:finite-stream-sibling-dependency-route}
For every accepted $\FiniteStreamUp$ carrier, any downstream finite-stream consumer that reads a prefix window toward $\RegSeqRatUp$ or $\RealUp$ first consumes the same finite observation lattice
$$
\StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \RealUp
$$
through the StreamName finite-window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the Real seal of \autoref{ch:concrete-instances-real-namecert}. The dependency is sibling-visible because each stage is named by the carrier's $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\FiniteStreamUp}$ rows before the prefix window is exported to any later finite-stream consumer.
\end{theorem}

The downstream packets $\RegularRealTailEquivalenceUp$ and $\RegularSequenceClusterWitnessUp$ consume the same finite request/list/window route before any $\RegSeqRatUp$ or $\RealUp$ seal read; for instance, \autoref{ch:concrete-instances-regular-real-tail-equivalence-namecert} reads equality of regular real names through that displayed finite tail data.
