Every $\FiniteStreamUp$ read consumed by \autoref{ch:concrete-instances-regseqrat-namecert} or \autoref{ch:concrete-instances-real-namecert} first passes through the finite-observation surface of \autoref{ch:concrete-instances-streamname-namecert}. The packet therefore exposes its request row, list spine, and StreamName window before the RegSeqRat readback boundary or the Real seal can be inspected; no consumer receives an ambient infinite stream or a selected real limit outside the displayed finite route.

\begin{lemma}[FiniteStream sibling dependency route]
\label{lem:finite-stream-sibling-dependency-route}
For every accepted $\FiniteStreamUp$ carrier, any $\RegSeqRatUp$ or $\RealUp$ consumer reads the same finite observation lattice
$$
\StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \RealUp
$$
through the StreamName finite-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the Real seal of \autoref{ch:concrete-instances-real-namecert}. The dependency is sibling-visible because each stage is named by the carrier's $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert_{\FiniteStreamUp}$ rows.
\end{lemma}
