Finite $\FiniteStreamUp$ windows are consumed by the $\RegSeqRatUp$ readback of \autoref{ch:concrete-instances-regseqrat-namecert} before the downstream $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert} is available. The dependency route is the displayed finite observation row $W$ together with the carrier rows $Q,L,R,D,H,C,P,N$ of \autoref{def:finite-stream-carrier}; it records no completed stream, quotient stream equality, selected real limit, or ambient real-completeness datum.
