The $\FiniteStreamUp$ dependency route reads finite-window names through \autoref{ch:concrete-instances-streamname-namecert}, the regular-sequence rational route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the real-name seal of \autoref{ch:concrete-instances-real-namecert} before any Real-facing consumer inspects a displayed finite window. Thus a finite stream is visible to downstream L10 consumers only after its stream-name row, rational regular-sequence row, and $\RealUp$ readback route have all been exposed inside the same dependency lattice.
