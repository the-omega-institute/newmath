\chapter{A Concrete Naming Certificate for $\FiniteStreamUp$}
\label{ch:concrete-instances-finite-stream-namecert}
\origin{human}

Following Bishop and Bridges on finite sequences and finite observation records, the $\FiniteStreamUp$ packet records the finite schedule object that sits between $\ListUp$ spine data and the countable-name windows of $\StreamNameUp$. It packages only the finitely requested observations needed by downstream regular-rational and real-name consumers. The chapter is a finite observation-schedule neighbour of \autoref{ch:concrete-instances-list-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert}; it does not export an infinite stream, quotient stream equality, a selected limit, countable choice, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[FiniteStream carrier]
\label{def:finite-stream-carrier}
A $\FiniteStreamUp$ carrier is a finite $\BHist$ packet
$$
F=(Q,L,W,R,D,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the finite request row in $\BHist$, recording the ordered observation indices and payload requests actually inspected by the consumer.
\item $L$ is the $\ListUp$ prefix spine that stores those requests as a finite constructor-generated list rather than as a host array or hidden stream segment.
\item $W$ is the $\StreamNameUp$ finite-window handoff row, exposing exactly the window generated from $Q$ and $L$.
\item $R$ is the $\RegSeqRatUp$ readback boundary available to regular-rational consumers only after the finite window $W$ has been displayed.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, recording the displayed dyadic radii, comparison cells, and finite precision budget attached to the window.
\item $H$ records componentwise $\hsame$ transport for the request row, list prefix spine, StreamName window, RegSeqRat readback boundary, DyadicRatCore tolerance row, replay row, provenance row, and local naming row.
\item $C$ records displayed $\Cont$ replay from a finite request through $Q,L,W,R,D,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\FiniteStreamUp$, $\ListUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteStreamUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through the rows $Q,L,W,R,D,H,C,P,N$. No coordinate stores a completed stream, host stream equality, selected limit, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[FiniteStream NameCert obligations]
\label{thm:finite-stream-namecert-obligations}
For every accepted $\FiniteStreamUp$ carrier
$$
F=(Q,L,W,R,D,H,C,P,N),
$$
the local $\NameCert_{\FiniteStreamUp}$ surface consists exactly of carrier admission for the finite request row, $\ListUp$ prefix spine, $\StreamNameUp$ finite-window handoff, $\RegSeqRatUp$ readback boundary, $\DyadicRatCoreUp$ tolerance row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. The finite-window exactness clause requires $W$ to expose only the observations listed by $Q$ and represented by $L$. The StreamName handoff clause requires every downstream countable-name read to pass through $W$ before it can reach $R$ or $D$. The RegSeqRat and Real nonescape clause requires every regular-rational or $\RealUp$-facing consumer to remain downstream from $Q,L,W,R,D,H,C,P,N$. The provenance exhaustion clause requires $P$ and $N$ to name only this finite route.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-stream-carrier}. Its displayed inventory is exactly the finite request row, the finite list spine, the StreamName window, the regular-rational readback boundary, the dyadic tolerance row, and the structural rows $H,C,P,N$.

Finite-window exactness is read from $Q,L,W$. The request row lists the observations, the list spine represents them as finite constructor data, and the StreamName handoff exposes precisely that finite window.

The regular-rational and dyadic rows are downstream. A consumer may reach the $\RegSeqRatUp$ readback boundary $R$ or the $\DyadicRatCoreUp$ tolerance row $D$ only after the finite window $W$ has already been displayed.

Transport, replay, and provenance preserve the same route. Since the carrier contains no completed stream, quotient stream equality, selected limit, countable-choice row, trichotomy branch, or ambient $\RealUp$ completeness coordinate, no Real-facing consumer can escape the finite packet.
\end{proof}

\begin{theorem}[FiniteStream section sibling lock]
\label{thm:finite-stream-section-sibling-lock}
Every $\RealUp$-facing consumer of an accepted $\FiniteStreamUp$ packet reads the $\StreamNameUp$ window $W$, $\RegSeqRatUp$ boundary $R$, $\DyadicRatCoreUp$ tolerance row $D$, and downstream $\RealUp$ seal rows only over the same finite request row $Q$ and $\ListUp$ spine $L$. In particular, no sibling packet can replace $Q$ or $L$ while preserving the displayed route
$$
Q,L,W,R,D,H,C,P,N.
$$
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-stream-carrier}. The rows $W,R,D$ are not free-standing analytic objects; they are displayed after the finite request row $Q$ and the finite list spine $L$.

The displayed $\Cont$ replay row $C$ then fixes the request coordinate used by the downstream consumer. Replaying the packet with a different finite request row would leave the listed carrier rows and hence would leave the accepted $\FiniteStreamUp$ packet.

The componentwise $\hsame$ transport row $H$ preserves the same coordinate rather than identifying it with an unlisted sibling. Thus any $\RealUp$-facing read that reaches $W,R,D$ or a Real seal remains locked to the original $Q,L$ route named by $P,N$.
\end{proof}

\begin{theorem}[FiniteStream StreamName dependency route]
\label{thm:finite-stream-streamname-dependency-route}
Every accepted $\FiniteStreamUp$ packet consumed by a $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, or $\RealUp$ reader must expose the finite request row $Q$, the $\ListUp$ prefix spine $L$, and the $\StreamNameUp$ window $W$ before the read can reach the downstream rows
$$
R,D,H,C,P,N.
$$
The substantive dependency edge is therefore the finite request/list route into the StreamName window of \autoref{ch:concrete-instances-streamname-namecert}, as displayed by the carrier of \autoref{def:finite-stream-carrier}.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-stream-carrier}. Its first exposed consumer rows are $Q,L,W$, and the remaining rows $R,D,H,C,P,N$ are displayed only as downstream readback, tolerance, transport, replay, provenance, and naming data.

Use \autoref{thm:finite-stream-section-sibling-lock}. A Real-facing read that reaches $W,R,D$ or a Real seal remains locked to the original $Q,L$ route, so a StreamName, RegSeqRat, DyadicRatCore, or Real reader cannot replace the finite request/list surface with an unlisted sibling.

Thus the dependency is not an ambient infinite-stream dependency. It is the displayed finite route from $Q$ and $L$ into the $\StreamNameUp$ window $W$, after which the downstream rows may be consumed.
\end{proof}

\begin{theorem}[FiniteStream StreamName route]
\label{thm:finite-stream-streamname-route}
Every $\StreamNameUp$ consumer of an accepted $\FiniteStreamUp$ packet must display the finite request row $Q$, the $\ListUp$ prefix spine $L$, and the finite-window row $W$ before it can reach any $\RegSeqRatUp$, $\DyadicRatCoreUp$, or $\RealUp$ row. The only admissible route is
$$
Q,L,W \longmapsto R,D,H,C,P,N,
$$
so the downstream regular-rational, dyadic-tolerance, and real-seal rows remain locked to the same finite request/list packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-stream-carrier}. The rows $Q,L,W$ are the finite request, list-spine, and StreamName-window data; the rows $R,D,H,C,P,N$ are downstream readback, tolerance, transport, replay, provenance, and naming data.

Apply \autoref{thm:finite-stream-section-sibling-lock}. It prevents a downstream $\RealUp$-facing read from replacing $Q$ or $L$ while keeping the displayed route.

Now apply \autoref{thm:finite-stream-streamname-dependency-route}. It forces any StreamName, RegSeqRat, DyadicRatCore, or Real reader to pass through $Q,L,W$ before the downstream rows are consumed. Therefore every StreamName consumer sees the finite route rather than an ambient infinite stream or an unlisted sibling packet.
\end{proof}

\begin{theorem}[FiniteStream product window]
\label{thm:finite-stream-product-window}
Given two accepted $\FiniteStreamUp$ packets over displayed rows
$$
F_i=(Q_i,L_i,W_i,R_i,D_i,H_i,C_i,P_i,N_i)
$$
for $i=0,1$, a product finite-window read is admissible only by exposing both request rows $Q_0,Q_1$, both $\ListUp$ spines $L_0,L_1$, and the componentwise $\StreamNameUp$ product window $W_0 \times W_1$ before any downstream row is consumed. The product route then reads the corresponding $\RegSeqRatUp$ readbacks, $\DyadicRatCoreUp$ tolerance rows, componentwise $\hsame$ transports, displayed $\Cont$ replays, $\Pkg$ provenance rows, and local $\NameCert$ rows for the same two finite packets. It exports no host product stream, no quotient stream equality, no selected limit, and no ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the two carriers from \autoref{def:finite-stream-carrier}. Each carrier exposes its own finite request row, list spine, StreamName window, regular-rational readback, dyadic tolerance row, transport, replay, provenance, and local naming.

Apply \autoref{thm:finite-stream-section-sibling-lock} separately to the two packets. The lock prevents either downstream read from replacing $Q_i$ or $L_i$ while keeping the displayed StreamName, RegSeqRat, DyadicRatCore, or Real-facing route.

Next use \autoref{thm:finite-stream-streamname-dependency-route} and \autoref{thm:finite-stream-streamname-route}. They require each component window to pass through $Q_i,L_i,W_i$ before its readback and tolerance rows are consumed.

The product window is therefore formed only after both component routes are visible. Pair the two displayed StreamName windows componentwise, then read $R_0,R_1$, $D_0,D_1$, $H_0,H_1$, $C_0,C_1$, $P_0,P_1$, and $N_0,N_1$ along the same finite request/list routes. Since neither carrier contains a host product stream, quotient stream equality, selected limit, or ambient completeness coordinate, the paired read remains a finite product-window route.
\end{proof}

\falsifiablePrediction{Every accepted $\FiniteStreamUp$ read is refuted if it reaches a $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, or $\RealUp$ consumer without displaying the finite request row, $\ListUp$ prefix spine, finite-window handoff, regular-rational readback boundary, dyadic tolerance row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ evidence.}

\independenceWitness{list, streamname, regseqrat, dyadicratcore, real: $\FiniteStreamUp$ is not bijective to any listed sibling because it jointly carries a finite request row, a List prefix spine, a StreamName window handoff, RegSeqRat readback boundary, DyadicRatCore tolerance row, transport, replay, provenance, and local naming as one finite schedule packet.}

\closureat{FiniteStreamUp}{seedStr}

\begin{closurestatus}{\FiniteStreamUp}
  \constructivestory{A $\FiniteStreamUp$ packet is generated from finite BHist request rows, a ListUp prefix spine, a StreamName finite-window handoff, a RegSeqRat readback boundary, a DyadicRatCore tolerance row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-stream-carrier}, \autoref{thm:finite-stream-namecert-obligations}, \autoref{thm:finite-stream-section-sibling-lock}, and \autoref{thm:finite-stream-streamname-dependency-route} seed the finite observation-schedule route between List spines and StreamName finite windows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close infinite stream equality, selected limits, countable choice, trichotomy, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked finite request admission, List prefix exactness, StreamName handoff exactness, RegSeqRat and DyadicRatCore boundary nonescape, transport, replay, provenance, and local naming for BEDC.Derived.FiniteStreamUp.}
\end{closurestatus}
