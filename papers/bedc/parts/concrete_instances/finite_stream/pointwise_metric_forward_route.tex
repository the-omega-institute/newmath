\begin{theorem}[FiniteStream pointwise metric forward route]
\label{thm:finite-stream-pointwise-metric-forward-route}
Every $\PointwiseStreamMetricUp$ consumer that reads a $\FiniteStreamUp$ packet must first consume the finite request row, list prefix spine, and finite-window handoff of \autoref{def:finite-stream-carrier}. The route then reaches \autoref{ch:concrete-instances-pointwise-stream-metric-namecert} only through the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows. Thus the pointwise metric read receives finite stream windows as finite BEDC data and does not import an infinite stream, quotient stream equality, selected representative, countable choice, or ambient Real completeness.
\end{theorem}
\begin{proof}
Project the finite stream carrier of \autoref{def:finite-stream-carrier}. The rows $Q,L,W,R,D,H,C,P,N$ display the finite request, list prefix, StreamName window, regular-rational readback, dyadic tolerance row, and structural evidence before any downstream consumer is visible.

Use the section sibling lock of \autoref{thm:finite-stream-section-sibling-lock}. It keeps every Real-facing read attached to the same finite request row and list spine.

The pointwise metric chapter consumes exactly that finite-window route. Since the accepted packet has no coordinate for an infinite stream, quotient stream equality, selected representative, choice schedule, or ambient completeness, the forward route exports only the finite window surface.
\end{proof}
