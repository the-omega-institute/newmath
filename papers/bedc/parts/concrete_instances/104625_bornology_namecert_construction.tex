\chapter{A Concrete Naming Certificate for $\BornologyUp$}
\label{ch:concrete-instances-bornology-namecert}
\origin{human}

Following Bourbaki and Kelley on bornologies, the $\BornologyUp$ packet records the finite bounded-family interface that sits between the bounded-subset certificate of \autoref{ch:concrete-instances-boundedset-namecert}, the directed base surface of \autoref{ch:concrete-instances-filterbase-namecert}, the uniform entourage surface of \autoref{ch:concrete-instances-uniformspace-namecert}, and the Cauchy-family specialization of \autoref{ch:concrete-instances-cauchy-bornology-namecert}. It names bounded families by displayed hereditary and finite-union rows before any completion, uniform, or Real-facing consumer can ask for a selected bound, a quotient family, countable choice, host suprema, or ambient completeness.

\begin{definition}[Bornology carrier]
\label{def:bornology-carrier}
A $\BornologyUp$ carrier is a finite $\BHist$ packet
$$
B=(F,S,U,E,D,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the bounded-family source row. It lists the finite family requests that may be read as bounded data.
\item $S$ is the subset-heredity ledger, recording that an accepted subfamily request is read only through a displayed parent family already named by $F$.
\item $U$ is the finite-union ledger, recording binary and finite union requests as displayed rows over previously accepted bounded families.
\item $E$ is the singleton and source-exposure row, naming the basic bounded packets available before hereditary or union closure is inspected.
\item $D$ is the consumer handoff row for $\FilterBaseUp$ or $\UniformSpaceUp$ readers, and for the $\CauchyBornologyUp$ specialization of the same bounded-family surface.
\item $H$ records componentwise $\hsame$ transport for $F,S,U,E,D,C,P,N$.
\item $C$ records displayed $\Cont$ replay from bounded-family admission through subset heredity, finite union, source exposure, consumer handoff, transport, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\BornologyUp$, $\BoundedSetUp$, $\FilterBaseUp$, $\UniformSpaceUp$, $\CauchyBornologyUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BornologyUp}$ row.
\end{enumerate}
The carrier compares accepted packets only through these rows. It has no coordinate for host suprema, selected bounds, quotient families, countable choice, open-cover compactness, or ambient completeness.
\end{definition}
\leanchecked{BEDC.Derived.BornologyUp.BornologyTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Bornology NameCert obligations]
\label{thm:bornology-namecert-obligations}
For every accepted $\BornologyUp$ carrier $B=(F,S,U,E,D,H,C,P,N)$, the local $\NameCert_{\BornologyUp}$ surface consists exactly of carrier admission, bounded-family source exposure, subset-heredity ledger visibility, finite-union ledger visibility, singleton and source exposure, $\FilterBaseUp$ or $\UniformSpaceUp$ consumer handoff, $\CauchyBornologyUp$ specialization handoff, componentwise transport, displayed continuation replay, provenance, and local naming.
\end{theorem}
\leanchecked{BEDC.Derived.BornologyUp.BornologyNameCertObligations}
\begin{proof}
Carrier admission projects the displayed packet. The source row $F$ and singleton/source row $E$ expose the basic bounded-family entries before any closure rule is read.

Subset heredity is confined to $S$, and finite union is confined to $U$. These ledgers are rows of the same packet, so a consumer cannot replace them by a host powerset, a selected bound, or a quotient family.

The handoff row $D$ then presents the same bounded-family surface to $\FilterBaseUp$, $\UniformSpaceUp$, and $\CauchyBornologyUp$ consumers. The structural rows $H,C,P,N$ transport, replay, source, and name only the displayed packet.
\end{proof}

\begin{theorem}[Bornology bounded-family union stability]
\label{thm:bornology-bounded-family-union-stability}
Let $B=(F,S,U,E,D,H,C,P,N)$ be an accepted $\BornologyUp$ carrier. Every accepted finite union of displayed bounded-family reads is certified by the finite-union ledger $U$ after the source row $F$ and singleton/source row $E$ have been exposed, and every accepted subfamily of such a union is routed back through the subset-heredity ledger $S$. The route exports no host supremum, selected bound, quotient family, countable-choice selector, open-cover compactness principle, or ambient completeness premise.
\end{theorem}
\leanchecked{BEDC.Derived.BornologyUp.BornologyBoundedFamilyUnionStability}
\begin{proof}
Read the carrier rows. Source and singleton exposure are the only entry points for basic bounded-family data, so a union request must first cite displayed rows from $F$ and $E$.

The finite-union ledger $U$ records the admitted union row. A later subfamily request cannot inspect that union through a host family predicate; it must pass through $S$, whose parent row is already displayed inside the packet.

Transport and replay through $H$ and $C$ preserve the same ledgers, while $P$ and $N$ package only the named dependencies and local certificate. Therefore the union-stability route remains finite and contains none of the excluded host principles.
\end{proof}

\begin{theorem}[Bornology CauchyBornology handoff]
\label{thm:bornology-cauchy-bornology-handoff}
Every $\CauchyBornologyUp$ consumer of an accepted $\BornologyUp$ carrier reads the general bounded-family surface through
$$
F,E \longmapsto S,U \longmapsto D \longmapsto H,C,P,N .
$$
Thus the Cauchy specialization receives bounded-family source rows, singleton exposure, subset heredity, finite-union stability, consumer handoff, transport, replay, provenance, and local naming before it may open rate budgets, regular rational readback, Real seals, or completion-facing data.
\end{theorem}
\leanchecked{BEDC.Derived.BornologyUp.BornologyCauchyBornologyHandoff}
\begin{proof}
Start with a $\CauchyBornologyUp$ consumer. The only general bornology data available to it are the displayed source and singleton rows $F,E$.

The closure properties are read next. Subset heredity is supplied by $S$, and finite union is supplied by $U$, so the consumer receives the ordinary bornological closure rows before any Cauchy-specific budget or Real-facing row can be inspected.

The handoff is then the row $D$, preserved by $H$ and replayed by $C$. Since $P$ and $N$ name only the general bounded-family packet and its Cauchy specialization, the route adds no selected bound, quotient family, countable-choice selector, host supremum, or ambient completeness premise.
\end{proof}
\leanchecked{BEDC.Derived.BornologyUp.BornologyCarrier\_cauchy\_bornology\_handoff}

\begin{theorem}[Bornology obligation closure route]
\label{thm:bornology-obligation-closure-route}
Every accepted $\BornologyUp$ obligation read exposes exactly the route
$$
F,S,U,E,D,H,C,P,N.
$$
The rows are the bounded-family source, subset-heredity ledger, finite-union ledger, singleton and source exposure, $\FilterBaseUp$ or $\UniformSpaceUp$ and $\CauchyBornologyUp$ handoff, componentwise transport, displayed replay, provenance, and local $\NameCert_{\BornologyUp}$ evidence. The route exports no host suprema, selected bounds, quotient families, countable choice, open-cover compactness, or ambient completeness premise.
\end{theorem}
\leanchecked{BEDC.Derived.BornologyUp.BornologyObligationClosureRoute}
\begin{proof}
The carrier of \autoref{def:bornology-carrier} supplies the visible rows $F,S,U,E,D,H,C,P,N$. The NameCert obligations of \autoref{thm:bornology-namecert-obligations} identify those rows as carrier admission, bounded-family source exposure, heredity, finite union, source exposure, consumer handoff, transport, replay, provenance, and local naming.

The bounded-family union stability theorem \autoref{thm:bornology-bounded-family-union-stability} keeps union and subfamily reads inside $U$ and $S$ after the source rows $F,E$ have been exposed. The CauchyBornology handoff of \autoref{thm:bornology-cauchy-bornology-handoff} routes every Cauchy consumer through the same displayed surface before any Cauchy-specific budget can be opened.

Together these results exhaust the finite obligation route. Since none of the carrier rows names host suprema, selected bounds, quotient families, countable choice, open-cover compactness, or ambient completeness, no accepted obligation read can export those omitted principles.
\end{proof}

\begin{theorem}[Bornology scoped bounded-family route]
\label{thm:bornology-scoped-bounded-family-route}
Every scoped bounded-family consumer of an accepted $\BornologyUp$ carrier
$$
B=(F,S,U,E,D,H,C,P,N)
$$
must read the route
$$
F,S,U,E,D,H,C,P,N
$$
through the displayed $\BoundedSetUp$, $\FilterBaseUp$, $\UniformSpaceUp$, $\CauchyBornologyUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows before using the bounded-family surface. The route exports no host supremum, selected bound, quotient family, countable-choice selector, open-cover compactness principle, or ambient completeness premise.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:bornology-namecert-obligations}. They expose the bounded-family source row $F$, the subset-heredity ledger $S$, the finite-union ledger $U$, the singleton/source row $E$, the consumer handoff row $D$, and the structural rows $H,C,P,N$ as the complete local surface.

Use bounded-family union stability, \autoref{thm:bornology-bounded-family-union-stability}. It keeps every finite union and subfamily read inside $U$ and $S$ after $F,E$ have been displayed, so the bounded-family consumer cannot replace the packet by a host family or selected bound.

Next apply the CauchyBornology handoff of \autoref{thm:bornology-cauchy-bornology-handoff}. It forces completion-facing bounded-family reads through the same $F,E,D,H,C,P,N$ surface before any Cauchy-specific budget is opened.

Finally compose with the obligation closure route \autoref{thm:bornology-obligation-closure-route}. The scoped consumer therefore reads exactly the displayed carrier route through $\BoundedSetUp$, $\FilterBaseUp$, $\UniformSpaceUp$, $\CauchyBornologyUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, with no additional supremum, choice, quotient, compactness, or completeness coordinate.
\end{proof}

\begin{theorem}[Bornology scope kernel grounding]
\label{thm:bornology-scope-kernel-grounding}
The bounded-family source, subset-heredity, finite-union, consumer handoff, transport, replay, provenance, and local NameCert rows of an accepted $\BornologyUp$ carrier are scoped exactly over $\BoundedSetUp$, $\FilterBaseUp$, $\UniformSpaceUp$, $\CauchyBornologyUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. Every scoped consumer must read the displayed route
$$
F,S,U,E,D,H,C,P,N
$$
through those kernel and sibling rows, and the route adds no host supremum, selected bound, quotient family, countable-choice selector, compactness principle, or ambient completeness premise.
\end{theorem}
\leanchecked{BEDC.Derived.BornologyUp.BornologyScopeKernelGrounding}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:bornology-namecert-obligations}. They expose carrier admission, bounded-family source rows, subset heredity, finite union, singleton/source exposure, consumer handoff, componentwise transport, displayed replay, provenance, and local naming as the local $\BornologyUp$ surface.

Use bounded-family union stability, \autoref{thm:bornology-bounded-family-union-stability}. It keeps heredity and finite-union reads inside the displayed $S$ and $U$ ledgers after $F$ and $E$ have been exposed, so a scoped consumer cannot replace the packet by host suprema, selected bounds, quotient families, or choice data.

Apply the CauchyBornology handoff of \autoref{thm:bornology-cauchy-bornology-handoff}. The handoff row $D$ presents the same bounded-family surface to $\FilterBaseUp$, $\UniformSpaceUp$, and $\CauchyBornologyUp$ consumers before any Cauchy-specific or completion-facing data are opened.

Finally compose the obligation closure route \autoref{thm:bornology-obligation-closure-route} with the scoped bounded-family route \autoref{thm:bornology-scoped-bounded-family-route}. The resulting kernel grounding is exactly the route through $\BoundedSetUp$, $\FilterBaseUp$, $\UniformSpaceUp$, $\CauchyBornologyUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, with no extra compactness or completeness coordinate.
\end{proof}

\begin{theorem}[Bornology public bounded-family export]
\label{thm:bornology-public-bounded-family-export}
Every public bounded-family consumer of an accepted $\BornologyUp$ carrier reads the displayed rows
$$
F,S,U,E,D,H,C,P,N
$$
as the exported $\NameCert_{\BornologyUp}$ surface. The export is public exactly for bounded-family readers: it exposes source families, subset heredity, finite unions, singleton source rows, handoff to $\FilterBaseUp$, $\UniformSpaceUp$, and $\CauchyBornologyUp$, componentwise transport, replay, provenance, and local naming, while exporting no selected bound, host supremum, quotient family, countable-choice selector, open-cover compactness theorem, or ambient completeness premise.
\end{theorem}
\begin{proof}
Read the scoped kernel grounding of \autoref{thm:bornology-scope-kernel-grounding}. It identifies the displayed bounded-family route over the sibling rows and the kernel rows.

Use the obligation closure route of \autoref{thm:bornology-obligation-closure-route}. It shows that an accepted obligation read has no coordinate outside $F,S,U,E,D,H,C,P,N$.

The public bounded-family export is therefore the same finite route made available to bounded-family consumers. Since the carrier has no row for selected bounds, host suprema, quotient families, choice, compactness, or ambient completeness, the public export cannot expose any of those objects.
\end{proof}

\closureat{BornologyUp}{seedStr}

\begin{closurestatus}{\BornologyUp}
  \theoryclosure{\publicClosure}
  \scopeclosed{\autoref{def:bornology-carrier}, \autoref{thm:bornology-namecert-obligations}, \autoref{thm:bornology-bounded-family-union-stability}, \autoref{thm:bornology-cauchy-bornology-handoff}, \autoref{thm:bornology-obligation-closure-route}, \autoref{thm:bornology-scoped-bounded-family-route}, \autoref{thm:bornology-scope-kernel-grounding}, and \autoref{thm:bornology-public-bounded-family-export} bind the finite bounded-family NameCert surface over bounded-set, filter-base, uniform-space, and Cauchy-bornology consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host suprema, selected bounds, quotient families, countable choice, open-cover compactness, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Further verification requires Lean carrier admission, bounded-family route exactness, public export coverage, and bridge checking for BEDC.Derived.BornologyUp.}
  \constructivestory{A $\BornologyUp$ packet is generated from bounded-family source rows, subset-heredity ledgers, finite-union ledgers, singleton and source exposure, FilterBaseUp or UniformSpaceUp handoff rows, CauchyBornologyUp specialization, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
