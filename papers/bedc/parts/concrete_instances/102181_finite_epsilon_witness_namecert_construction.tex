\chapter{A Concrete Naming Certificate for $\FiniteEpsilonWitnessUp$}
\label{ch:concrete-instances-finite-epsilon-witness-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\FiniteEpsilonWitnessUp$ packet isolates the finite row that answers one inspected epsilon request before any completed real, quotient stream, or ambient choice principle is consulted. It sits on the L10 Real and Completion route through the $\DyadicRatCoreUp$ tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite $\StreamNameUp$ observation surface of \autoref{ch:concrete-instances-streamname-namecert}, the $\RegSeqRatUp$ rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the threshold-selection interface of \autoref{ch:concrete-instances-cauchy-modulus-selection-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. The packet records one explicit epsilon-witness row; it is not a global modulus, a selected limit, quotient equality, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.

\begin{definition}[Finite epsilon-witness carrier]
\label{def:finite-epsilon-witness-carrier}
A $\FiniteEpsilonWitnessUp$ carrier is a finite $\BHist$ packet
$$
X=(E,D,W,M,R,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the inspected epsilon request row, including the displayed request token and the local comparison budget that the packet is required to answer.
\item $D$ is a positive $\DyadicRatCoreUp$ tolerance row refining $E$ into a dyadic radius, scale, slack, and positivity ledger.
\item $W$ is the $\StreamNameUp$ finite-window row whose inspected coordinates are sufficient for the displayed request.
\item $M$ is the $\CauchyModulusUp$ threshold row, recording the finite index bound used for the single epsilon request.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the source stream inside the window $W$ and threshold $M$.
\item $S$ is the $\RealUp$ seal boundary, exposed only after $E,D,W,M,R$ have been displayed and never as an independent completeness or quotient-equality witness.
\item $H$ records componentwise $\hsame$ transport for the epsilon request, dyadic tolerance, finite windows, threshold, readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the inspected epsilon request through $D,W,M,R,S,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\FiniteEpsilonWitnessUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\CauchyModulusUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteEpsilonWitnessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $E,D,W,M,R,S,H,C,P,N$. It has no coordinate for a family of epsilon requests, a global modulus, a selected limit, quotient stream equality, trichotomy, ambient completeness, Lean verification, or standard bridge data.
\end{definition}

\begin{theorem}[Finite epsilon-witness NameCert obligations]
\label{thm:finite-epsilon-witness-namecert-obligations}
For every accepted $\FiniteEpsilonWitnessUp$ carrier
$$
X=(E,D,W,M,R,S,H,C,P,N),
$$
the local $\NameCert_{\FiniteEpsilonWitnessUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the inspected epsilon request, positive dyadic tolerance row, finite StreamName window, CauchyModulus threshold, RegSeqRat readback, Real seal boundary, transport, replay, provenance, and local naming;
\item request positivity, requiring $D$ to record a positive dyadic tolerance refining the inspected request $E$;
\item finite-window sufficiency, requiring every coordinate used to answer $E$ to occur inside the displayed window row $W$;
\item threshold exactness, requiring $M$ to bind the request $E$ and tolerance row $D$ to a single finite bound before readback;
\item rational readback, requiring $R$ to be obtained from the $\RegSeqRatUp$ source through $W$ and $M$;
\item $\RealUp$ seal non-escape, requiring $S$ to remain downstream of $E,D,W,M,R$ and to add no selected limit, quotient equality, trichotomy, or ambient completeness principle;
\item componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming through $H,C,P,N$.
\end{enumerate}
No local finite epsilon-witness obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-epsilon-witness-carrier}. The analytic rows are exactly the inspected request $E$, the positive dyadic tolerance $D$, the finite windows $W$, the threshold row $M$, the rational readback $R$, and the Real seal boundary $S$.

Request positivity is the role of $D$. Since the carrier has no second tolerance coordinate, the radius, scale, slack, and positivity ledger answering $E$ must be read from $D$.

The finite computation is forced through $W,M,R$. The window row $W$ lists the inspected StreamName coordinates, the threshold row $M$ binds those coordinates to the single request $E$ and tolerance $D$, and $R$ records the RegSeqRat readback available after that threshold has been displayed.

The seal row is terminal. The row $S$ is downstream of $E,D,W,M,R$, while $H,C,P,N$ transport, replay, source, and name the same finite route. Because the classifier has no coordinate for a family of requests, a global modulus, selected limits, quotient stream equality, trichotomy, ambient completeness, Lean verification, or bridge data, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Finite epsilon-witness Real route]
\label{thm:finite-epsilon-witness-real-route}
Every $\RealUp$-facing answer accepted by a $\FiniteEpsilonWitnessUp$ carrier
$$
X=(E,D,W,M,R,S,H,C,P,N)
$$
factors through the finite route
$$
\DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \CauchyModulusUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
as recorded by the displayed rows $D,W,M,R,S$, with the inspected epsilon request $E$ visible at the start of the route.
\end{theorem}
\begin{proof}
Begin with a Real-facing answer admitted by the carrier. By definition, its seal coordinate is $S$, and $S$ is exposed only after the request $E$, tolerance row $D$, finite windows $W$, threshold row $M$, and rational readback $R$ have been displayed.

Read the route from the request side. The inspected epsilon row $E$ is refined by the positive $\DyadicRatCoreUp$ row $D$. The finite observation surface is the $\StreamNameUp$ row $W$, and the bound used for that observation is the $\CauchyModulusUp$ row $M$.

The readback into rational sequence data is $R$. Only after this readback is visible may the terminal $\RealUp$ seal boundary $S$ be inspected.

Finally, $H,C,P,N$ preserve and name the displayed route. Hence every accepted Real-facing answer factors through $\DyadicRatCoreUp$, $\StreamNameUp$, $\CauchyModulusUp$, $\RegSeqRatUp$, and $\RealUp$ in that order, with no bypass through a global modulus, selected limit, quotient stream equality, trichotomy, ambient completeness, Lean verification, or bridge checking.
\end{proof}

\closureat{FiniteEpsilonWitnessUp}{seedStr}

\begin{closurestatus}{\FiniteEpsilonWitnessUp}
  \constructivestory{A $\FiniteEpsilonWitnessUp$ packet is generated from one inspected epsilon request, a positive DyadicRatCore tolerance row, a StreamName finite-window row, a CauchyModulus threshold row, a RegSeqRat readback row, a RealUp seal boundary, hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-epsilon-witness-carrier}, \autoref{thm:finite-epsilon-witness-namecert-obligations}, and \autoref{thm:finite-epsilon-witness-real-route} seed the finite single-request epsilon-witness certificate on the L10 Real and Completion route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close global moduli, families of epsilon requests, selected limits, quotient stream equality, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, request positivity, finite-window sufficiency, threshold exactness, RegSeqRat readback, RealUp seal non-escape, transport, replay, provenance, and local naming for BEDC.Derived.FiniteEpsilonWitnessUp.}
\end{closurestatus}
