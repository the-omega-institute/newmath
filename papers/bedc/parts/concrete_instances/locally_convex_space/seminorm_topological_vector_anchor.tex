The $\LocallyConvexSpaceUp$ packet reads its neighbourhood basis only after the finite seminorm-family row and the $\TopVecSpaceUp$ linear-topology row have been displayed together. The seminorm ledger is anchored by \autoref{ch:concrete-instances-seminormed-space-namecert}, while the ambient additive-topological continuity source is anchored by \autoref{ch:concrete-instances-topvecspace-namecert}; downstream functional-analysis consumers therefore use the locally convex surface through $V,T,S,B,M,H,C,P,N$, not through a detached host predicate.
