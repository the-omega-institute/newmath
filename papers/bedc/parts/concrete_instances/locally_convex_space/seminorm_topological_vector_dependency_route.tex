The $\LocallyConvexSpaceUp$ carrier reads its separating seminorm family through the finite family ledger of \autoref{ch:concrete-instances-seminormed-space-namecert} and its ambient additive-topological structure through the $\TopVecSpaceUp$ source of \autoref{ch:concrete-instances-topvecspace-namecert}. A local-convexity consumer therefore reaches the $S$ seminorm row only after the topological-vector operations and continuity rows have been displayed, and the resulting packet remains the finite route $V,T,S,B,M,H,C,P,N$ rather than a host locally convex predicate.
