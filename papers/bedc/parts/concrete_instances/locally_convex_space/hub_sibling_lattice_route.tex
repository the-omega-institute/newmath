The top-level $\LocallyConvexSpaceUp$ hub exposes its sibling lattice through \autoref{ch:concrete-instances-topvecspace-namecert}, \autoref{ch:concrete-instances-convexset-namecert}, \autoref{ch:concrete-instances-seminormed-space-namecert}, and \autoref{ch:concrete-instances-functionalanalysis-namecert}. A locally convex read therefore enters the dossier through displayed topological-vector operations, convex-neighbourhood evidence, finite seminorm-family rows, and functional-analysis consumers before any downstream packet treats $V,T,S,B,M,H,C,P,N$ as a reusable source surface.
