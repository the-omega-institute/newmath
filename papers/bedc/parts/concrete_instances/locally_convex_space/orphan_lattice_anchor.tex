The $\LocallyConvexSpaceUp$ seminorm and convex-neighbourhood ledgers consume the sibling lattice directly: $\VecSpaceUp$ supplies the linear operations through \autoref{ch:concrete-instances-vecspace-namecert}, $\TopVecSpaceUp$ supplies continuity of addition and scalar multiplication through \autoref{ch:concrete-instances-topvecspace-namecert}, $\NormUp$ supplies scalar-bound readbacks through \autoref{ch:concrete-instances-norm-namecert}, and $\FunctionalAnalysisUp$ supplies downstream replay through \autoref{ch:concrete-instances-functionalanalysis-namecert}. A local-convexity consumer therefore reads those four sibling rows as part of the finite ledger route before citing the packet.
