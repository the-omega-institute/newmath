The top-level $\LocallyConvexSpaceUp$ route consumes the sibling lattice through \autoref{ch:concrete-instances-vecspace-namecert}, \autoref{ch:concrete-instances-topvecspace-namecert}, \autoref{ch:concrete-instances-norm-namecert}, and \autoref{ch:concrete-instances-functionalanalysis-namecert}. Its finite seminorm and convex-neighbourhood ledgers read those $\VecSpaceUp$, $\TopVecSpaceUp$, $\NormUp$, and $\FunctionalAnalysisUp$ rows before any downstream local-convexity consumer can cite the packet.
