\paragraph{Frechet dependency route.}
The $\LocallyConvexSpaceUp$ packet supplies the finite seminorm-family rows and convex balanced neighbourhood-filter rows consumed by the $\FrechetSpaceUp$ carrier of \autoref{ch:concrete-instances-frechet-space-namecert}. The same read also consumes the $\TopVecSpaceUp$ operation-continuity surface of \autoref{ch:concrete-instances-topvecspace-namecert} and the $\MetricCompletionUp$ handoff of \autoref{ch:concrete-instances-metriccompletion-namecert} before complete locally convex replay is available. Thus the dependency is horizontal: $\LocallyConvexSpaceUp$ provides the local convexity evidence, while $\FrechetSpaceUp$ repacks that evidence with topological-vector operations, metrizability, and completion-facing data without importing an ambient complete locally convex host, a selected metric, quotient completion equality, countable choice, or an infinite seminorm enumeration.

\paragraph{Frechet and topological-vector sibling lattice.}
The sibling lattice around $\LocallyConvexSpaceUp$ has two mandatory dependency edges. A $\FrechetSpaceUp$ read cites \autoref{ch:concrete-instances-frechet-space-namecert} for the metric-completion and metrizability rows, while a $\TopVecSpaceUp$ read cites \autoref{ch:concrete-instances-topvecspace-namecert} for the vector-space topology, addition-continuity, and scalar-continuity rows. A locally convex consumer may therefore export seminorm-family evidence only after those sibling rows have displayed the finite seminorm ledger, metric completion handoff, and vector-space topology surface; it does not bypass either sibling into an ambient locally convex host.
