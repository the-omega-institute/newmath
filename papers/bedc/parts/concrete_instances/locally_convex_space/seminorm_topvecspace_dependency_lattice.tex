The $\LocallyConvexSpaceUp$ dependency lattice reads the $\SeminormUp$ family through the finite family route of \autoref{ch:concrete-instances-seminormed-space-namecert} and the $\TopVecSpaceUp$ carrier of \autoref{ch:concrete-instances-topvecspace-namecert} before any convex-neighbourhood route is available. Thus the local-convexity packet exposes seminorm-control rows and topological-vector continuity rows as finite sibling data, then passes them to $S,B,H,C,P,N$ for transport, replay, provenance, and local naming rather than treating local convexity as a kernel-only predicate.
