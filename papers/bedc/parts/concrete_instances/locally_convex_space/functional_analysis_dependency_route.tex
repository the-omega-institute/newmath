\begin{theorem}[Locally convex functional-analysis dependency route]
\label{thm:locally-convex-space-functional-analysis-route}
Every $\FunctionalAnalysisUp$ consumer of an accepted $\LocallyConvexSpaceUp$ packet must first read the seminorm family row $S$, the $\TopVecSpaceUp$ transport row $T$, the finite convex-balanced neighbourhood ledger $B$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and the local $\NameCert_{\LocallyConvexSpaceUp}$ row $N$. The consumer boundary of \autoref{ch:concrete-instances-functionalanalysis-namecert} may then cite the packet only through those visible rows, never through a selected dual, a host locally convex topology predicate, a Hahn--Banach principle, quotient equality of functionals, or an ambient completeness theorem.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:locally-convex-space-carrier}. Its locally convex data are the seminorm row $S$ and the convex-balanced neighbourhood ledger $B$, while the linear-topology compatibility is supplied by the $\TopVecSpaceUp$ row $T$.

The downstream $\FunctionalAnalysisUp$ consumer can therefore replay local convexity only after $S,T,B$ are visible, and only with the transport, replay, provenance, and naming rows $H,C,P,N$ attached. The functional-analysis chapter of \autoref{ch:concrete-instances-functionalanalysis-namecert} receives that finite route as a dependency surface, so no selected dual, host topology predicate, quotient equality, or completeness principle is introduced.
\end{proof}
