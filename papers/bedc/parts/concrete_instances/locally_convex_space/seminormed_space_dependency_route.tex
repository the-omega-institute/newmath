The $\LocallyConvexSpaceUp$ carrier reads its separating seminorm family through the finite family-seminorm packet of \autoref{ch:concrete-instances-seminormed-space-namecert} and reads topology compatibility through the $\TopVecSpaceUp$ source of \autoref{ch:concrete-instances-topvecspace-namecert} together with the complete-metrizable consumer route of \autoref{ch:concrete-instances-frechet-space-namecert}. The seminorm rows generate the displayed locally convex neighbourhood basis before the $B$ convex-balanced neighbourhood ledger can be used, so a downstream consumer reaches local convexity through $S$ and $B$ as BEDC NameCert rows rather than through an ambient locally convex predicate.
