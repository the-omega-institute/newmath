The $\LocallyConvexSpaceUp$ carrier consumes the finite seminorm-family ledger only after the topological-vector classifier surface of \autoref{ch:concrete-instances-topvecspace-namecert} is visible. The same read is then available to the functional-analysis route of \autoref{ch:concrete-instances-functionalanalysis-namecert}: consumers inspect the $S$ seminorm row and the $B$ convex-balanced neighbourhood row as displayed NameCert data, not as a host locally convex predicate or an unbounded family of seminorms.
