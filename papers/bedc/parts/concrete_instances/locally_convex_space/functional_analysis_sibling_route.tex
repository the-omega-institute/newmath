Every $\LocallyConvexSpaceUp$ read that is consumed by $\FunctionalAnalysisUp$ or $\FrechetSpaceUp$ must expose the convex-neighbourhood and seminorm-window surface before any downstream linear-topology or complete-metrizable row is read. The route passes from the local carrier of \autoref{ch:concrete-instances-locally-convex-space-namecert} through the $\FrechetSpaceUp$ dependency of \autoref{ch:concrete-instances-frechet-space-namecert} and the functional-analysis consumers cited by \autoref{ch:concrete-instances-functionalanalysis-namecert}; it exports no host locally convex space, selected infinite seminorm family, quotient completion, or ambient completeness coordinate.
