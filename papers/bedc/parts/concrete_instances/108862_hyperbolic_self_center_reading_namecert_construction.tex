\chapter{A Concrete Naming Certificate for $\HyperbolicSelfCenterReadingUp$}
\label{ch:concrete-instances-hyperbolic-self-center-reading-namecert}
\origin{ai}

The $\HyperbolicSelfCenterReadingUp$ packet realizes \autoref{def:hyperbolic-self-center-reading} as a finite NameCert carrier for the three-role discipline of focused disk readout, inscription-side self-center, and far-end boundary reading. It sits beside $\PoincareBoundaryFixedTransportUp$ of \autoref{ch:concrete-instances-poincare-boundary-fixed-transport-namecert}, $\LocalSelfCenterInscriptionSealUp$ of \autoref{ch:concrete-instances-local-self-center-inscription-seal-namecert}, $\HyperbolicExpansionLedgerUp$ of \autoref{ch:concrete-instances-hyperbolic-expansion-ledger-namecert}, and $\UnitDiskUp$ of \autoref{ch:concrete-instances-unitdisk-namecert}; those siblings supply boundary-fixed transport, local inscription sealing, observer-expansion ledger data, and disk-coordinate rows, while this packet binds the self-center reading itself.

\begin{definition}[Hyperbolic self-center reading carrier]
\label{def:hyperbolic-self-center-reading-carrier}
A $\HyperbolicSelfCenterReadingUp$ carrier is a finite $\BHist$ packet
$$
R=(D,I,B,T,V,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the disk-readout row, recording the finite $\UnitDiskUp$ or Poincare-disk coordinate read of the focused ledger event.
\item $I$ is the inscription row, recording the $\LocalSelfCenterInscriptionSealUp$ reading of the local self-center.
\item $B$ is the far-end boundary row, recording the boundary-side $\mathsf{T}$ reading used by the hyperbolic vision.
\item $T$ is the boundary-fixed transport row, recording the $\PoincareBoundaryFixedTransportUp$ route that preserves the boundary reading while disk data move.
\item $V$ is the noncollapse verdict row, recording that $D$, $I$, and $B$ are three roles and not three hidden objects or one collapsed object.
\item $H$ records componentwise $\hsame$ transport for disk readout, inscription, boundary, boundary-fixed transport, noncollapse verdict, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay for reading $D,I,B,T,V$ in the same finite packet.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicSelfCenterReadingUp$, $\UnitDiskUp$, $\PoincareBoundaryFixedTransportUp$, $\LocalSelfCenterInscriptionSealUp$, $\HyperbolicExpansionLedgerUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HyperbolicSelfCenterReadingUp}$ row.
\end{enumerate}
The carrier has no coordinate for a hidden subject, a global observer point, a selected geodesic center, a physical constant, or an identification of disk readout with boundary far end.
\end{definition}

\begin{theorem}[Hyperbolic self-center reading NameCert obligations]
\label{thm:hyperbolic-self-center-reading-namecert-obligations}
For every accepted $\HyperbolicSelfCenterReadingUp$ carrier
$$
R=(D,I,B,T,V,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicSelfCenterReadingUp}$ surface consists exactly of disk-readout admission, inscription admission, far-end boundary admission, boundary-fixed transport, noncollapse verdict, componentwise transport, displayed replay, provenance, and local naming. No obligation may identify the records-side disk row $D$, the inscription-side row $I$, and the far-end boundary row $B$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:hyperbolic-self-center-reading-carrier}. The three geometric roles are displayed separately as $D,I,B$.

The transport row $T$ moves disk-facing data while preserving the boundary reading, and the verdict row $V$ records the refusal to collapse the three roles. These rows are part of the same finite packet and cannot be replaced by prose analogy.

Finally, $H,C,P,N$ transport, replay, source, and name the packet. Since the carrier has no hidden subject, global observer point, selected geodesic center, physical constant, or identification coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Hyperbolic self-center reading noncollapse]
\label{thm:hyperbolic-self-center-reading-noncollapse}
Every accepted $\HyperbolicSelfCenterReadingUp$ read keeps disk readout $D$, inscription row $I$, and far-end boundary row $B$ distinct under the boundary-fixed transport row $T$ and the noncollapse verdict row $V$. A consumer may cite their relation only through $R=(D,I,B,T,V,H,C,P,N)$, and may not export a hidden subject, a single collapsed self-center object, or a physical constant from the geometry alone.
\end{theorem}
\begin{proof}
Begin with the three displayed roles. The disk-readout row $D$ is records-side data, the inscription row $I$ is the local self-center seal, and the boundary row $B$ is the far-end reading.

The transport row $T$ relates disk and boundary readings without erasing that distinction. The verdict row $V$ records the noncollapse condition before any downstream consumer can package the read.

The structural rows $H,C,P,N$ preserve the same finite inventory. Because no hidden-subject, collapsed-object, or physical-constant coordinate appears in the carrier, every accepted reading remains inside the displayed packet.
\end{proof}

\begin{theorem}[Hyperbolic self-center reading provenance route]
\label{thm:hyperbolic-self-center-reading-provenance-route}
The $\HyperbolicSelfCenterReadingUp$ packet routes its focused-ledger context through $\HyperbolicExpansionLedgerUp$, disk coordinates through $\UnitDiskUp$, local inscription through $\LocalSelfCenterInscriptionSealUp$, and boundary-fixed movement through $\PoincareBoundaryFixedTransportUp$. These sibling routes are consumed as finite rows of one carrier and do not supply an ambient hyperbolic model, a global observer, or a host-geometric self.
\end{theorem}
\begin{proof}
Read the provenance row $P$ together with the displayed rows $D,I,B,T,V$. It names the focused-ledger, disk-coordinate, inscription, and boundary-fixed transport sources that the carrier is allowed to consume.

The replay row $C$ then orders those reads inside one finite packet. A consumer can replay the disk readout, local inscription, far-end boundary reading, transport, and noncollapse verdict, but it receives no external geometric model.

Finally, $H$ and $N$ preserve and name the same packet. The absence of a global observer or host-geometric self coordinate gives the stated boundary.
\end{proof}

\closureat{HyperbolicSelfCenterReadingUp}{seedStr}

\begin{closurestatus}{\HyperbolicSelfCenterReadingUp}
  \constructivestory{A $\HyperbolicSelfCenterReadingUp$ packet is generated from disk-readout, local inscription, far-end boundary, boundary-fixed transport, noncollapse verdict, componentwise hsame transport, displayed replay, provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-self-center-reading-carrier}, \autoref{thm:hyperbolic-self-center-reading-namecert-obligations}, \autoref{thm:hyperbolic-self-center-reading-noncollapse}, and \autoref{thm:hyperbolic-self-center-reading-provenance-route} seed the finite carrier for the hyperbolic self-center reading.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, a global observer, hidden subject, physical constant, or ambient hyperbolic model.}
  \upgradepath{Obligation closure requires checked carrier admission, disk-readout stability, inscription and boundary noncollapse, boundary-fixed transport, replay, provenance, and local naming for the same finite packet.}
\end{closurestatus}
