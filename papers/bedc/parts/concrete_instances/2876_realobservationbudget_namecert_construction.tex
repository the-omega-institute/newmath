\chapter{A Concrete Naming Certificate for $\RealObservationBudgetUp$}
\label{ch:concrete-instances-real-observation-budget-namecert}
\origin{ai}

The $\RealObservationBudgetUp$ interface records the finite observation packet that a completion-facing consumer may inspect before a $\RealUp$ seal is reached. It keeps the precision request, stream windows, dyadic readbacks and radii, regular-sequence handoff rows, real-seal rows, transports, routes, provenance, and local naming row inside one $\BHist$ surface. It does not export quotient stream equality, host real equality, ambient completeness, hidden limit selection, or an unbounded observation schedule.

\begin{definition}[Real observation budget carrier]
\label{def:real-observation-budget-carrier}
A $\RealObservationBudgetUp$ carrier is a finite $\BHist$ packet
$$
B=(E,W,D,R,S,H,C,P,N)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $E$ is the unary precision request row naming the finite tolerance level visible to the consumer;
\item $W$ is the finite family of $\StreamNameUp$ window rows selected for that request;
\item $D$ records the $\DyadicRatCoreUp$ readbacks together with the displayed dyadic radii attached to the windows in $W$;
\item $R$ stores the $\RegSeqRatUp$ handoff rows that expose the regular rational sources feeding the observation budget;
\item $S$ stores the $\RealUp$ seal rows that mark the boundary at which completion-facing consumers receive the finite packet;
\item $H$ stores componentwise $\hsame$ transports for the precision request, windows, dyadic readbacks and radii, regular-sequence rows, seal rows, provenance, and local name;
\item $C$ stores the displayed $\Cont$ routes from $E$ to $W$, from $W$ to $D$, from $D$ to $R$, and from $R$ to $S$;
\item $P$ records $\Pkg$ provenance over $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RealObservationBudgetUp}$ row.
\end{enumerate}
The carrier is finite. It has no coordinate for quotient stream equality, host real equality, ambient completeness, hidden limit selection, or an unbounded observation schedule.
\end{definition}

\begin{theorem}[Real observation budget NameCert obligations]
\label{thm:real-observation-budget-namecert-obligations}
The local $\NameCert_{\RealObservationBudgetUp}$ surface for \autoref{def:real-observation-budget-carrier} consists exactly of carrier admission, unary precision exposure, finite $\StreamNameUp$ window coverage, $\DyadicRatCoreUp$ readback and radius exposure, $\RegSeqRatUp$ handoff routing, $\RealUp$ seal-boundary exposure, componentwise $\hsame$ transport, displayed $\Cont$ route replay, $\Pkg$ provenance, and the local naming row.
\end{theorem}
\begin{proof}
Carrier admission is projection from the displayed packet $B$. The rows $E,W,D,R,S$ provide the precision request, finite windows, dyadic readbacks and radii, regular-sequence handoffs, and real-seal boundary.

The window and dyadic obligations are read directly from $W$ and $D$. The regular-sequence handoff rows in $R$ expose only the rational-source data already routed through those finite observations.

The remaining obligations are the transport, route, provenance, and naming rows $H,C,P,N$. Since the carrier has no coordinate for quotient stream equality, host real equality, ambient completeness, hidden limit selection, or an unbounded schedule, the listed rows exhaust the local naming certificate.
\end{proof}

\begin{theorem}[Real observation budget window coverage]
\label{thm:real-observation-budget-window-coverage}
For an accepted $\RealObservationBudgetUp$ carrier, every dyadic readback and displayed radius exported by $D$ is covered by one of the finite $\StreamNameUp$ windows in $W$, and every such window is selected by the unary precision request row $E$ through the displayed routes in $C$.
\end{theorem}
\begin{proof}
Project the accepted packet $B$ and read the precision row $E$, window family $W$, dyadic ledger $D$, transports $H$, and continuation routes $C$.

The route from $E$ to $W$ selects the finite windows visible to the budget. The route from $W$ to $D$ is the only displayed path by which a dyadic readback or radius enters the packet.

Transport rows in $H$ preserve the same precision, window, readback, and radius coordinates when the packet is repacked for a consumer. Therefore every exported dyadic observation is covered by $W$ and no observation can be imported from an unbounded stream schedule.
\end{proof}

\begin{theorem}[Real observation budget regular-sequence handoff]
\label{thm:real-observation-budget-regseq-handoff}
For an accepted $\RealObservationBudgetUp$ carrier, each $\RegSeqRatUp$ handoff row in $R$ is reached only after the unary precision request, finite stream windows, and dyadic readbacks and radii have been read. A downstream $\RegSeqRatUp$ or $\RealUp$ consumer receives the handoff through the finite route
$$
E,W,D,H,C,P,N.
$$
\end{theorem}
\begin{proof}
Begin with the carrier projection. The displayed order of the observation route places $R$ after the precision request, stream windows, and dyadic ledger.

Use window coverage to bind every dyadic readback and radius to the finite windows selected from $E$. The continuation route from $D$ to $R$ then supplies the only path from the observation budget to the regular rational source rows.

The transport and provenance rows repack the same route for consumers. No handoff row can bypass $E,W,D,H,C,P,N$ or appeal to quotient stream equality, hidden limit choice, or host real comparison.
\end{proof}

\begin{theorem}[Real observation budget seal boundary]
\label{thm:real-observation-budget-real-seal-boundary}
Every $\RealUp$ completion-facing consumer of an accepted $\RealObservationBudgetUp$ carrier factors through the seal rows $S$. The seal rows may read only the precision request $E$, finite windows $W$, dyadic readbacks and radii $D$, regular-sequence handoffs $R$, transports $H$, routes $C$, provenance $P$, and local naming row $N$ supplied by the packet.
\end{theorem}
\begin{proof}
Project the accepted packet and inspect the consumer-facing coordinates. The carrier contains a single real-facing boundary coordinate, namely the seal family $S$.

Apply the regular-sequence handoff theorem to force any route into $S$ through $E,W,D,R,H,C,P,N$. The seal rows therefore receive only the finite observations, their radii, the regular rational handoffs, and the bookkeeping rows displayed by the packet.

Since $S$ is reached after those rows and no other real-facing coordinate exists, a completion consumer cannot read a selected completed real, a host equality test, an ambient completeness principle, or a hidden infinite schedule from the budget.
\end{proof}

\begin{theorem}[Real observation budget non-escape]
\label{thm:real-observation-budget-nonescape}
Every downstream consumer of an accepted $\RealObservationBudgetUp$ carrier factors through the finite rows
$$
E,W,D,R,S,H,C,P,N.
$$
The consumer may inspect exactly the unary precision request, finite $\StreamNameUp$ windows, $\DyadicRatCoreUp$ readbacks and radii, $\RegSeqRatUp$ handoff rows, $\RealUp$ seal rows, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row. It cannot inspect quotient stream equality, host real equality, ambient completeness, hidden limit selection, or an unbounded observation schedule.
\end{theorem}
\begin{proof}
Start from the finite carrier of \autoref{def:real-observation-budget-carrier}. The displayed rows of $B$ are exactly $E,W,D,R,S,H,C,P,N$.

Apply the local NameCert obligations to identify those rows as the complete naming surface. Window coverage binds the dyadic readbacks and radii to the finite windows selected by the precision request.

The regular-sequence handoff theorem routes source-facing access through the same finite observations. The seal-boundary theorem then routes every completion-facing consumer through $S$ without exposing any unlisted analytic object.

Thus the observation budget is a finite BEDC packet. Its consumers can use the displayed rows, but the carrier gives them no route to quotient streams, host real equality, ambient completeness, hidden limit selection, or unbounded observation schedules.
\end{proof}

\begin{theorem}[Real observation budget obligation package]
\label{thm:real-observation-budget-obligation-package}
The $\RealObservationBudgetUp$ obligation package is the finite surface
$$
B=(E,W,D,R,S,H,C,P,N)
$$
over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. It packages carrier admission, unary precision exposure, finite window coverage, dyadic readback and radius exposure, $\RegSeqRatUp$ handoff routing, $\RealUp$ seal-boundary exposure, componentwise transport, displayed route replay, provenance, and the local naming row, with no quotient stream equality, host real equality, ambient completeness, hidden limit selection, or unbounded observation schedule.
\end{theorem}
\begin{proof}
Start from the carrier and the local NameCert obligation theorem. \autoref{def:real-observation-budget-carrier} supplies exactly $E,W,D,R,S,H,C,P,N$, and \autoref{thm:real-observation-budget-namecert-obligations} reads those rows as the local naming surface.

The window, dyadic, regular-rational, and real-facing clauses are already separated by the existing obligation rows. \autoref{thm:real-observation-budget-window-coverage} binds $D$ to the finite windows $W$, \autoref{thm:real-observation-budget-regseq-handoff} routes $R$ through the finite observation route, and \autoref{thm:real-observation-budget-real-seal-boundary} marks $S$ as the completion-facing boundary.

Finally apply non-escape. \autoref{thm:real-observation-budget-nonescape} rules out every unlisted analytic object, so the obligation package is exhausted by the finite BEDC surface displayed above.
\end{proof}

\begin{theorem}[Real observation budget transport stability]
\label{thm:real-observation-budget-transport-stability}
Classifier transport for the $\RealObservationBudgetUp$ obligation package preserves the same precision request, finite window family, dyadic readbacks and radii, regular-sequence handoff rows, real-seal boundary rows, provenance, and local name. Thus any transported package remains a finite observation-budget surface over the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\end{theorem}
\begin{proof}
Project the two classified budget packets to their displayed coordinates. The transport ledger $H$ is componentwise: it transports the precision request, window rows, dyadic ledger, handoff rows, seal rows, provenance, and local name without changing the shape of the packet.

Use the displayed routes $C$ to replay the observation order after transport. The route from $E$ to $W$, from $W$ to $D$, from $D$ to $R$, and from $R$ to $S$ is part of the same finite package, so transported consumers still read the same obligation surface.

The non-escape theorem excludes hidden replacement data during transport. No transported package can insert quotient stream equality, host real equality, ambient completeness, hidden limit selection, or an unbounded schedule because none of those rows exists in the source package.
\end{proof}

\begin{theorem}[Real observation budget consumer exactness]
\label{thm:real-observation-budget-consumer-exactness}
Every $\RegSeqRatUp$ or $\RealUp$ consumer of a $\RealObservationBudgetUp$ obligation package reads exactly the finite packet rows required by the observation budget: $E,W,D,R,S,H,C,P,N$. The $\RegSeqRatUp$ side factors through the handoff rows $R$, the $\RealUp$ side factors through the seal rows $S$, and both sides remain inside the obligation package of \autoref{thm:real-observation-budget-obligation-package}.
\end{theorem}
\begin{proof}
First read the source-facing route. \autoref{thm:real-observation-budget-regseq-handoff} forces every regular-sequence handoff through the precision request, finite windows, dyadic readbacks and radii, transports, routes, provenance, and local naming row.

Next read the completion-facing route. \autoref{thm:real-observation-budget-real-seal-boundary} sends every $\RealUp$ consumer through the seal rows $S$, and those seal rows are reached only after $E,W,D,R,H,C,P,N$ have been displayed.

Combine these routes with transport stability and non-escape. The transported package keeps the same finite rows, and \autoref{thm:real-observation-budget-nonescape} blocks any consumer from reading quotient stream equality, host real equality, ambient completeness, hidden limit selection, or an unbounded observation schedule outside the package.
\end{proof}

\begin{theorem}[Real observation budget TasteGate ground-compiler alignment]
\label{thm:real-observation-budget-tastegate-ground-compiler-alignment}
For an AI-origin $\RealObservationBudgetUp$ packet
$$
B=(E,W,D,R,S,H,C,P,N),
$$
the rows $E,W,D,R,S,H,C,P,N$ are exactly the single-carrier $\ChapterTasteGate$ surface available to the chapter. The precision request, finite windows, dyadic readbacks and radii, regular-sequence handoff rows, real-seal rows, transports, continuation routes, provenance, and local name must be consumed through the ground-compiler event-flow and no-hidden-input discipline of \autoref{ch:ground-compiler-event-flow}. In particular, a consumer may not replace those rows by hidden prompt state, a host-side limit selector, an unlisted compiler fact, quotient stream equality, host real equality, ambient completeness, or an unbounded observation schedule.
\end{theorem}
\begin{proof}
Start from the carrier definition and the local NameCert surface. \autoref{def:real-observation-budget-carrier} exposes exactly $E,W,D,R,S,H,C,P,N$, while \autoref{thm:real-observation-budget-namecert-obligations} reads those coordinates as the whole local certificate.

The ground-compiler boundary is then a consumption rule for the same finite packet. The event-flow and no-hidden-input discipline of \autoref{ch:ground-compiler-event-flow} permits only displayed, recognized rows to enter the compiler-facing read, so the $\ChapterTasteGate$ row cannot import an extra semantic channel.

Finally apply non-escape. \autoref{thm:real-observation-budget-nonescape} already rules out quotient stream equality, host real equality, ambient completeness, hidden limit selection, and unbounded schedules; the ground-compiler reading adds that hidden prompt state and unnamed compiler facts are not admissible input rows either.
\end{proof}

\begin{theorem}[Real observation budget finite-window seal-route totality]
\label{thm:real-observation-budget-finite-window-seal-route-totality}
Every admitted $\RealUp$ seal read of an accepted $\RealObservationBudgetUp$ packet is reached by the finite route
$$
E \longmapsto W \longmapsto D \longmapsto R \longmapsto S.
$$
The route is carried with componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\RealObservationBudgetUp}$ row. There is no admitted seal read that bypasses the precision request, finite window coverage, dyadic ledger, regular-sequence handoff, or displayed provenance rows.
\end{theorem}
\begin{proof}
First use window coverage. \autoref{thm:real-observation-budget-window-coverage} sends every exported dyadic readback and radius through the precision request $E$, the finite window family $W$, and the dyadic ledger $D$.

Next use the regular-sequence handoff theorem. \autoref{thm:real-observation-budget-regseq-handoff} places the handoff rows $R$ downstream of $E,W,D$ and carries the same transport, route, provenance, and naming data.

Finally apply the real-seal boundary theorem. \autoref{thm:real-observation-budget-real-seal-boundary} makes $S$ the only completion-facing boundary, and transport stability keeps the same $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows attached to the route.
\end{proof}

\begin{theorem}[Real observation budget obligation closure readiness]
\label{thm:real-observation-budget-obligation-closure-readiness}
The $\RealObservationBudgetUp$ paper-side obligation package consists of the carrier admission of $B=(E,W,D,R,S,H,C,P,N)$, the local $\NameCert$ obligations, finite window coverage, $\RegSeqRatUp$ handoff routing, $\RealUp$ seal-boundary routing, non-escape, transport stability, consumer exactness, $\ChapterTasteGate$ ground-compiler alignment, and finite-window seal-route totality. This package is precisely the obligation surface required before a later closure claim may promote the AI-origin observation budget beyond seed status.
\end{theorem}
\begin{proof}
Begin with the carrier and NameCert rows. \autoref{thm:real-observation-budget-obligation-package} assembles carrier admission, local naming, window, handoff, seal, transport, route, provenance, and non-escape obligations into one finite surface.

Add the consumer-facing rows. \autoref{thm:real-observation-budget-transport-stability} preserves the same packet under classifier transport, and \autoref{thm:real-observation-budget-consumer-exactness} says the $\RegSeqRatUp$ and $\RealUp$ consumers read exactly that packet.

Add the AI-origin readiness rows. \autoref{thm:real-observation-budget-tastegate-ground-compiler-alignment} ties the single-carrier $\ChapterTasteGate$ surface to ground-compiler event-flow and no-hidden-input consumption, while \autoref{thm:real-observation-budget-finite-window-seal-route-totality} makes every admitted seal read pass through $E,W,D,R,S$ with the displayed transport and provenance rows.
\end{proof}

\closureat{RealObservationBudgetUp}{seedStr}

\begin{closurestatus}{\RealObservationBudgetUp}
  \constructivestory{A $\RealObservationBudgetUp$ packet is generated from a unary precision request, finite $\StreamNameUp$ windows, $\DyadicRatCoreUp$ readbacks and radii, $\RegSeqRatUp$ handoff rows, $\RealUp$ seal rows, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-observation-budget-carrier}, \autoref{thm:real-observation-budget-namecert-obligations}, \autoref{thm:real-observation-budget-window-coverage}, \autoref{thm:real-observation-budget-regseq-handoff}, \autoref{thm:real-observation-budget-real-seal-boundary}, and \autoref{thm:real-observation-budget-nonescape} bind the finite observation-budget carrier, precision-indexed windows, dyadic readbacks and radii, regular-sequence handoffs, and RealUp seal boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close quotient stream equality, host real equality, ambient completeness, hidden limit selection, unbounded observation schedules, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, window coverage, regular-sequence handoff routing, RealUp seal-boundary routing, and non-escape for the same finite observation budget.}
\end{closurestatus}
