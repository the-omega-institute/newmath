\chapter{A Concrete Naming Certificate for $\BanachContractionModulusUp$}
\label{ch:concrete-instances-banach-contraction-modulus-namecert}
\origin{human}

Following Banach's fixed-point theorem and Bishop--Bridges constructive metric analysis, the $\BanachContractionModulusUp$ packet isolates the finite contraction-budget ledger before any endpoint or fixed-point equality is read. It sits after the contraction-map surface of \autoref{ch:concrete-instances-banach-contraction-namecert}, the constructive contraction modulus of \autoref{ch:concrete-instances-constructive-contraction-modulus-namecert}, the Cauchy orbit seal of \autoref{ch:concrete-instances-cauchy-fixed-point-orbit-seal-namecert}, the complete-metric endpoint boundary of \autoref{ch:concrete-instances-completemetric-namecert}, the Cauchy-modulus route of \autoref{ch:concrete-instances-cauchymodulus-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the downstream fixed-point corridor of \autoref{ch:concrete-instances-lipschitz-fixed-point-namecert}. The packet names the modulus surface only; it is not a completed fixed point, uniqueness theorem, selected limit, quotient Cauchy equality, host map equality, countable-choice schedule, or ambient completeness claim.

\begin{definition}[Banach contraction modulus carrier]
\label{def:banach-contraction-modulus-carrier}
A $\BanachContractionModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(B,K,O,C,Q,R,E,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\BanachContractionUp$ source row, exposing the self-map, contraction constant, and one-step distance comparison.
\item $K$ is the $\ConstructiveContractionModulusUp$ row, storing the finite budget that turns a target tolerance into an iteration-depth request.
\item $O$ is the $\CauchyFixedPointOrbitSealUp$ row that reads the Picard-orbit residuals without selecting a completed endpoint.
\item $C$ is the $\CauchyModulusUp$ row generated from the contraction budget and orbit residuals.
\item $Q$ is the $\RegSeqRatUp$ readback row for the resulting regular Cauchy schedule.
\item $R$ is the $\CompleteMetricUp$ admission row that permits completion-facing consumers to inspect the same Cauchy data.
\item $E$ is the terminal $\RealUp$ seal row available after the regular rational readback has been displayed.
\item $H$ records componentwise $\hsame$ transport for the contraction, budget, orbit, Cauchy, completion, Real, replay, provenance, and local naming rows.
\item $T$ records displayed $\Cont$ replay along the same route.
\item $P$ is the $\Pkg$ provenance row over $\BanachContractionModulusUp$, $\BanachContractionUp$, $\ConstructiveContractionModulusUp$, $\CauchyFixedPointOrbitSealUp$, $\CompleteMetricUp$, $\CauchyModulusUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BanachContractionModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows and inherited transports. It has no coordinate for a fixed point, endpoint equality, representative selection, quotient completion equality, host map equality, or ambient completeness.
\end{definition}

\begin{theorem}[Banach contraction modulus NameCert obligations]
\label{thm:banach-contraction-modulus-namecert-obligations}
For every accepted $\BanachContractionModulusUp$ carrier
$$
M=(B,K,O,C,Q,R,E,H,T,P,N),
$$
the local $\NameCert_{\BanachContractionModulusUp}$ surface consists exactly of contraction-source visibility, finite contraction-budget visibility, Picard-orbit residual visibility, Cauchy-modulus extraction, $\RegSeqRatUp$ readback, $\CompleteMetricUp$ admission, terminal $\RealUp$ sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier. The analytic rows are $B,K,O,C,Q,R,E$, and the structural rows are $H,T,P,N$.

Read $B,K,O$ first. The contraction source supplies the self-map and contraction bound, the modulus row turns tolerances into finite iteration requests, and the orbit seal records the displayed residuals without selecting an endpoint.

Read $C,Q,R,E$ next. The Cauchy modulus is extracted from the contraction budget, the regular rational readback packages the schedule, the complete-metric row admits that schedule as endpoint-facing input, and the Real seal is downstream of the regular readback.

The transport, replay, provenance, and naming rows preserve only this displayed route. Since no excluded endpoint or choice coordinate appears in the carrier, the local NameCert cannot export one.
\end{proof}

\begin{theorem}[Banach contraction modulus complete-metric handoff]
\label{thm:banach-contraction-modulus-complete-metric-handoff}
Every complete-metric or fixed-point consumer of an accepted $\BanachContractionModulusUp$ packet must first pass through the finite route
$$
\BanachContractionUp \longmapsto \ConstructiveContractionModulusUp \longmapsto \CauchyFixedPointOrbitSealUp \longmapsto \CauchyModulusUp \longmapsto \RegSeqRatUp \longmapsto \CompleteMetricUp \longmapsto \RealUp .
$$
The route exports the contraction budget and Cauchy handoff needed by \autoref{ch:concrete-instances-lipschitz-fixed-point-namecert}; it does not export a fixed point, selected limit, quotient stream equality, host equality of maps, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the contraction and budget rows. They determine only finite iteration-depth requests and residual comparisons, so the consumer receives a displayed modulus rather than an endpoint.

Apply the obligation theorem. It places the orbit seal, Cauchy-modulus row, regular rational readback, complete-metric admission, and Real seal in one route before any fixed-point corridor may inspect the data.

The $\LipschitzFixedPointUp$ consumer may therefore read a named contraction-budget handoff. It must supply any endpoint existence, uniqueness, or equality claim in its own packet, because none of those rows occurs in the modulus carrier.
\end{proof}

\closureat{BanachContractionModulusUp}{seedStr}

\begin{closurestatus}{\BanachContractionModulusUp}
  \constructivestory{A $\BanachContractionModulusUp$ packet is generated from a BanachContraction source row, a ConstructiveContractionModulus budget row, a CauchyFixedPointOrbitSeal row, a CauchyModulus row, a RegSeqRat readback, a CompleteMetric admission boundary, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:banach-contraction-modulus-carrier}, \autoref{thm:banach-contraction-modulus-namecert-obligations}, and \autoref{thm:banach-contraction-modulus-complete-metric-handoff} seed the finite contraction-modulus route before fixed-point endpoint reads.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a fixed point, uniqueness, selected limit, quotient Cauchy equality, host map equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, contraction-source visibility, finite budget extraction, orbit-seal visibility, Cauchy-modulus readback, CompleteMetric handoff, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BanachContractionModulusUp.}
\end{closurestatus}
