\chapter{A Concrete Naming Certificate for $\PermutationUp$}
\label{ch:concrete-instances-permutation-namecert}

The permutation interface $\PermutationUp$ packages the carrier of bijections of a $\NameCert_{\FinSetUp}$ (\autoref{ch:concrete-instances-finset-namecert}) onto itself. It supplies the underlying carrier for $\NameCert_{\SymGroupUp}$ (\autoref{ch:concrete-instances-symgroup-namecert}) and the action data for combinatorial group-theoretic certificates.

\section{Carrier definition}
\label{sec:permutation-carrier}

\section{The certificate}
\label{sec:permutation-certificate}

\begin{closurestatus}{\PermutationUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{The seed binds the $\FinSetUp$ bijection carrier, classifier row, composition row, inverse row, action row, and ledger boundary as obligations not yet supplied by this chapter.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a PermutationUp carrier, classifier, composition law, inverse law, action law, exactness row, ledger row, standard bridge, or Lean verification target.}
  \upgradepath{Obligation closure requires explicit bijection carrier data over $\NameCert_{\FinSetUp}$, a classifier, composition and inverse stability, action compatibility, exact ledger entries, and the downstream $\SymGroupUp$ carrier interface.}
\end{closurestatus}
