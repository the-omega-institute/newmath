\chapter{A Concrete Naming Certificate for $\TypeLikeUp$}
\label{ch:concrete-instances-typelike-namecert}
\origin{human}

Following Martin-Lof type theory, categorical families, and displayed fibrations, the $\TypeLikeUp$ packet records the carrier-plus-classifier-plus-transport interface used when BEDC data are read as typed families rather than as an unindexed host universe. It sits beside the membership discipline of \autoref{ch:concrete-instances-setlike-namecert}, the syntactic carrier of \autoref{ch:concrete-instances-firstorder-namecert}, and the satisfaction carrier of \autoref{ch:concrete-instances-modeltheory-namecert}. The packet names only displayed classifier families, fiber rows, classifier transport, componentwise history transport, continuation replay, package provenance, and local naming; it does not introduce a universe hierarchy, quotient type equality, impredicative comprehension, host isomorphism of types, or an external category of families.

\begin{definition}[TypeLike carrier]
\label{def:typelike-carrier}
A $\TypeLikeUp$ carrier is a finite $\BHist$ packet
$$
T=(B,F,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the displayed base classifier family row, read as BEDC classifier data rather than as a host universe;
\item $F$ is the fiber-carrier row assigning to each accepted base entry a finite displayed fiber packet;
\item $Q$ is the classifier row for fiber membership, endpoint equality, and family-compatible reads;
\item $E$ is the extensionality and endpoint-exactness ledger, recording which displayed fiber endpoints may be identified under the classifier;
\item $H$ records componentwise $\hsame$ transport for $B,F,Q,E$ together with replay, provenance, and local naming rows;
\item $C$ records displayed $\Cont$ replay through which a consumer reads a base classifier, a fiber carrier, a classifier row, and an endpoint-exactness ledger;
\item $P$ is the $\Pkg$ provenance over $\TypeLikeUp$, $\SetLikeUp$, $\FirstOrderUp$, $\ModelTheoryUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\TypeLikeUp}$ row.
\end{enumerate}
The carrier has no coordinate for an ambient type universe, quotient identity, selected representative, host-level equivalence of categories, or a comprehension rule not already displayed by $B,F,Q,E,H,C,P,N$.
\end{definition}
\leanchecked{BEDC.Derived.TypeLikeUp.TypeLikeTasteGate\_single\_carrier\_alignment}

\begin{theorem}[TypeLike NameCert obligations]
\label{thm:typelike-namecert-obligations}
For every accepted $\TypeLikeUp$ carrier
$$
T=(B,F,Q,E,H,C,P,N),
$$
the local $\NameCert_{\TypeLikeUp}$ surface consists exactly of carrier admission for $T$, base-family classifier admission through $B$, fiber-carrier admission through $F$, classifier exposure through $Q$, endpoint-exactness exposure through $E$, componentwise $\hsame$ stability through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:typelike-carrier}. Its base side is the displayed classifier family $B$, while its fiber side is the finite carrier row $F$ indexed by accepted base entries.

The classifier row $Q$ supplies the only family-compatible membership and endpoint reads. The exactness row $E$ records when two displayed endpoints may be identified by that classifier, so endpoint equality is a carried ledger row rather than host type equality.

The structural rows $H,C,P,N$ preserve and expose the same packet. Since no further coordinate is present, the NameCert obligations cannot ask for an ambient universe, quotient identity, selected representative, categorical equivalence, or external comprehension rule.
\end{proof}
\leanchecked{BEDC.Derived.TypeLikeUp.TypeLikeNameCert\_obligations}
\leanvariant{BEDC.Derived.TypeLikeUp.TypeLikeNameCertObligations}

\begin{theorem}[TypeLike classifier transport]
\label{thm:typelike-classifier-transport}
Every downstream consumer of an accepted $\TypeLikeUp$ packet that transports a fiber endpoint must factor through the finite route
$$
B,F,Q,E \longmapsto H,C,P,N .
$$
Consequently the transported endpoint remains inside the displayed classifier family and cannot be replaced by host equality of types, an untracked equivalence of fibers, or a SetLike, FirstOrder, or ModelTheory read that bypasses the carried classifier row.
\end{theorem}
\leanchecked{BEDC.Derived.TypeLikeUp.TypeLikeClassifierTransport}
\begin{proof}
Begin with an accepted carrier $T=(B,F,Q,E,H,C,P,N)$. A consumer can reach a fiber endpoint only after reading the base classifier $B$ and the indexed fiber carrier $F$.

The classifier row $Q$ fixes the admissible endpoint comparison, and $E$ records the exact endpoint ledger for that comparison. Transport therefore has to preserve both the base-family row and the fiber classifier row.

The rows $H,C,P,N$ then move, replay, source, and name the same finite route. Reads inherited from $\SetLikeUp$, $\FirstOrderUp$, or $\ModelTheoryUp$ may be cited only through the package provenance $P$ and the displayed continuation $C$, so no sibling can supply a bypass around $B,F,Q,E$.
\end{proof}

\begin{theorem}[TypeLike displayed-family consumer exhaustion]
\label{thm:typelike-displayed-family-consumer-exhaustion}
Every downstream $\SetLikeUp$, $\FirstOrderUp$, or $\ModelTheoryUp$ consumer of an accepted $\TypeLikeUp$ endpoint factors through the displayed base classifier $B$, fiber carrier $F$, classifier row $Q$, endpoint-exactness ledger $E$, componentwise transport $H$, displayed replay $C$, provenance row $P$, and local $\NameCert_{\TypeLikeUp}$ row $N$. No such consumer may read the endpoint as unindexed membership, bare syntax, or satisfaction data before the displayed family route has been exposed.
\end{theorem}
\leanchecked{BEDC.Derived.TypeLikeUp.TypeLikeDisplayedFamilyConsumerExhaustion}
\begin{proof}
Begin with the accepted carrier $T=(B,F,Q,E,H,C,P,N)$ of \autoref{def:typelike-carrier}. Its first four rows are the only displayed family data: base classifier, indexed fiber carrier, family-compatible classifier, and endpoint-exactness ledger.

Apply \autoref{thm:typelike-classifier-transport}. Any endpoint transport used by a sibling consumer must preserve those four rows before the structural rows are available.

Finally inspect the sibling reads. $\SetLikeUp$ can supply membership only through $Q$, $\FirstOrderUp$ can supply syntax only through the provenance row $P$ and replay row $C$, and $\ModelTheoryUp$ can supply satisfaction only after the same displayed classifier route has been named locally by $N$. Thus the three consumer families are exhausted by $B,F,Q,E,H,C,P,N$ and cannot add an ambient universe, quotient type equality, or external category of families.
\end{proof}

\begin{theorem}[TypeLike SetLike FirstOrder ModelTheory carrier boundary]
\label{thm:typelike-setlike-firstorder-modeltheory-carrier-boundary}
Every accepted $\TypeLikeUp$ endpoint consumed by \autoref{ch:concrete-instances-setlike-namecert}, \autoref{ch:concrete-instances-firstorder-namecert}, or \autoref{ch:concrete-instances-modeltheory-namecert} must expose the finite displayed-family route
$$
B,F,Q,E \longmapsto H,C,P,N
$$
before the sibling read. The consumer may then interpret the exposed row as membership, syntax, or satisfaction-facing data, but it cannot replace the TypeLike carrier by unindexed SetLike membership, bare FirstOrder syntax, external ModelTheory satisfaction, quotient type equality, or an ambient host family.
\end{theorem}
\begin{proof}
Start with classifier transport, \autoref{thm:typelike-classifier-transport}. It forces endpoint movement to preserve the displayed base classifier, fiber carrier, classifier row, and endpoint-exactness ledger.

Then apply consumer exhaustion, \autoref{thm:typelike-displayed-family-consumer-exhaustion}. The SetLike, FirstOrder, and ModelTheory consumers enter only after the displayed route has reached the transport, replay, provenance, and local naming rows.

Thus the sibling chapters can consume the endpoint only through $B,F,Q,E,H,C,P,N$. Since the carrier contains no unindexed membership source, bare syntax row, external satisfaction predicate, quotient equality, or ambient family coordinate, none of those can serve as a replacement boundary.
\end{proof}
\leanchecked{BEDC.Derived.TypeLikeUp.TypeLikeSetLikeFirstOrderModelTheoryCarrierBoundary}

\begin{theorem}[TypeLike displayed-family inversion]
\label{thm:typelike-displayed-family-inversion}
Let $T=(B,F,Q,E,H,C,P,N)$ be an accepted $\TypeLikeUp$ carrier. Any endpoint read classified as $\TypeLikeUp$ data determines the displayed base classifier row $B$, the fiber-carrier row $F$, the classifier row $Q$, and the endpoint-exactness ledger $E$ before any structural row $H,C,P,N$ is used. Thus endpoint readback is inverted through the concrete displayed-family spine
$$
B,F,Q,E \longmapsto H,C,P,N
$$
and cannot be read first as host type equality, unindexed $\SetLikeUp$ membership, bare $\FirstOrderUp$ syntax, or $\ModelTheoryUp$ satisfaction.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:typelike-carrier}. The endpoint-facing data appear in the first four rows: base classifier, indexed fiber carrier, family-compatible classifier, and endpoint-exactness ledger.

Use classifier transport, \autoref{thm:typelike-classifier-transport}. It keeps any transported endpoint inside the displayed classifier family and prevents a downstream consumer from replacing the route by host type equality or an untracked fiber equivalence.

Finally apply consumer exhaustion, \autoref{thm:typelike-displayed-family-consumer-exhaustion}. The SetLike, FirstOrder, and ModelTheory consumers may enter only after $B,F,Q,E$ have been exposed and carried through $H,C,P,N$. Hence an accepted endpoint read determines those four displayed rows before the structural rows are consumed.
\end{proof}
\leanchecked{BEDC.Derived.TypeLikeUp.TypeLikeDisplayedFamilyInversion}

\falsifiablePrediction{A consumer that claims a $\TypeLikeUp$ endpoint transport while omitting the base classifier row, fiber-carrier row, classifier row, endpoint-exactness ledger, $\hsame$, $\Cont$, $\Pkg$, or local $\NameCert$ rows refutes the carrier of \autoref{def:typelike-carrier}.}

\independenceWitness{setlike, firstorder, modeltheory: $\TypeLikeUp$ is not bijective to any listed sibling because it jointly carries a displayed base classifier family, indexed fiber carrier, classifier row, endpoint-exactness ledger, componentwise transport, continuation replay, provenance, and local NameCert row. SetLikeUp has membership without indexed fiber transport; FirstOrderUp has syntax and deduction rows without the carrier family; ModelTheoryUp has satisfaction rows over FirstOrderUp without a general classifier-family transport interface.}

\closureat{TypeLikeUp}{seedStr}
\closureat{TypeLikeUp}{obligationStr}
\closureat{TypeLikeUp}{scopedStr}[BEDC.Derived.TypeLikeUp.TypeLikeClassifierTransport]
\closureat{TypeLikeUp}{publicStr}[BEDC.Derived.TypeLikeUp.TypeLikeClassifierTransport]
\closureat{TypeLikeUp}{bridgedStr}[BEDC.Derived.TypeLikeUp.TypeLikeClassifierTransport]
\closureat{TypeLikeUp}{matureStr}[BEDC.Derived.TypeLikeUp.TypeLikeClassifierTransport]

\begin{closurestatus}{\TypeLikeUp}
  \constructivestory{A $\TypeLikeUp$ packet is generated from a displayed base classifier family, indexed fiber carrier rows, a family-compatible classifier row, endpoint-exactness ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{def:typelike-carrier}, \autoref{thm:typelike-namecert-obligations}, \autoref{thm:typelike-classifier-transport}, and \autoref{thm:typelike-displayed-family-consumer-exhaustion} bind the displayed carrier-plus-classifier-plus-transport scope and exhaust the SetLikeUp, FirstOrderUp, and ModelTheoryUp sibling consumers over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, and the local family rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a universe hierarchy, quotient type equality, impredicative comprehension, host isomorphism of types, Lean verification, bridge checking, or an external category of families.}
  \upgradepath{No theory-axis upgrade remains for this displayed classifier-family transport surface; Lean verification and bridge status are recorded separately.}
\end{closurestatus}
