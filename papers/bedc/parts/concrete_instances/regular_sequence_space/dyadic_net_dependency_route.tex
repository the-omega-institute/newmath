The $\RegularSequenceSpaceUp$ route consumes \autoref{ch:concrete-instances-dyadic-interval-net-namecert} as a finite dyadic tolerance-window child before $\RegSeqRatUp$ readback and the terminal $\RealUp$ seal are public. This child route records the dyadic interval net as a genuine L10 sibling dependency of regular sequence finite-window reads, not as a self-contained completion principle.

\begin{proposition}[RegularSequenceSpace dyadic net dependency]
\label{thm:regular-sequence-space-dyadic-net-dependency}
Every accepted $\RegularSequenceSpaceUp$ carrier read by a completion-facing consumer must expose its $\StreamNameUp$ finite window, $\DyadicRatCoreUp$ tolerance row, the finite-cell route of \autoref{ch:concrete-instances-dyadic-interval-net-namecert}, the $\RegSeqRatUp$ readback row, and only then the terminal $\RealUp$ seal. The dependency exports no quotient stream equality, selected limit, countable-choice schedule, host real equality, or ambient completeness theorem.
\end{proposition}
\begin{proof}
Project the displayed regular-sequence carrier rows. The schedule, dyadic tolerance, readback, seal, transport, replay, provenance, and local naming coordinates are the only rows available to a completion-facing consumer.

Insert the dyadic interval net after the tolerance row and before readback reaches the terminal value seal. The finite-cell route supplies dyadic endpoint and incidence data for the visible tolerance window while leaving RegSeqRat and Real readback downstream.

The route therefore records a sibling dependency through finite rows. Since no carrier coordinate contains a quotient of streams, a selected limit, a choice schedule, host real equality, or ambient completeness data, none of those endpoints can be exported by the dependency.
\end{proof}
