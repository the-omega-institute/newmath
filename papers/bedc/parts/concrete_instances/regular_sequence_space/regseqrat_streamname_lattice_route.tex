The $\RegularSequenceSpaceUp$ lattice route consumes the finite scheduling surface of \autoref{ch:concrete-instances-streamname-namecert} together with the regular readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}. The StreamName row fixes the visible observation order, while the RegSeqRat row reads those observations as regular rational data before the local sequence-space certificate is exposed.
