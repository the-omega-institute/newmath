The $\RegularSequenceSpaceUp$ route consumes \autoref{ch:concrete-instances-finite-tail-filter-namecert} as its finite-tail filter carrier before the regular-sequence-space schedule and tail rows are inspected. The schedule/tail handoff therefore remains tied to the finite cofinal-tail filter surface rather than to a detached sequence-space island.
