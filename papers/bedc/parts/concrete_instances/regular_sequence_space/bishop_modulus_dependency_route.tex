The $\RegularSequenceSpaceUp$ route consumes \autoref{ch:concrete-instances-bishop-regular-cauchy-modulus-namecert} whenever a regular sequence is read with an explicit dyadic modulus budget. The packet is reached through the $\RegSeqRatUp$ readback and $\StreamNameUp$ scheduling routes of \autoref{thm:regular-sequence-space-sibling-route} and \autoref{thm:regular-sequence-space-regseqrat-streamname-real-route}, so the local sequence-space carrier exposes the modulus budget as finite sibling data rather than as quotient selection, countable choice, or ambient completion structure.
