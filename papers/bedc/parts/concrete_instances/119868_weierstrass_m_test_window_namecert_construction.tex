\chapter{A Concrete Naming Certificate for $\WeierstrassMTestWindowUp$}
\label{ch:concrete-instances-weierstrass-m-test-window-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis and the standard Weierstrass M-test, the $\WeierstrassMTestWindowUp$ packet records the finite majorant-window data needed before a function-series consumer may read a regular Cauchy handoff or terminal real-name seal. It sits beside the M-test carrier of \autoref{ch:concrete-instances-weierstrassmtest-namecert}, the real-series source of \autoref{ch:concrete-instances-realseries-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the constructive majorant-window route as finite BEDC rows; it does not import countable choice, arbitrary infinite sums, quotient real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Weierstrass M-test finite-window carrier]
\label{def:weierstrass-m-test-window-carrier}
A $\WeierstrassMTestWindowUp$ carrier is a finite $\BHist$ packet
$$
W=(F,M,B,T,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the finite function-series window, listing only the summand windows and source indices inspected by this packet.
\item $M$ is the displayed positive majorant sequence for the same inspected indices, written as a finite read of positive comparison rows rather than as an ambient infinite sum.
\item $B$ is the finite domination ledger matching each inspected summand window in $F$ to its corresponding positive majorant row in $M$.
\item $T$ is the finite partial-tail budget obtained from $B$ and the displayed majorant tail route.
\item $S$ is the $\StreamNameUp$ window surface through which the source summand windows and partial-tail requests are observed.
\item $R$ is the $\RegSeqRatUp$ readback row produced after the partial-tail budget has been exposed.
\item $E$ is the $\RealUp$ seal row reached only through $R$.
\item $H$ records componentwise $\hsame$ transport for source windows, majorant rows, domination ledgers, partial-tail budgets, stream windows, regular readbacks, real seals, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay of the same finite route.
\item $P$ is the $\Pkg$ provenance over $\WeierstrassMTestWindowUp$, $\WeierstrassMTestUp$, $\RealSeriesUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\WeierstrassMTestWindowUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $F,M,B,T,S,R,E,H,C,P,N$. It has no coordinate for countable choice, an arbitrary infinite sum object, quotient real equality, selected limits, host equality of functions, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Weierstrass M-test finite-window NameCert obligations]
\label{thm:weierstrass-m-test-window-namecert-obligations}
For every accepted $\WeierstrassMTestWindowUp$ carrier
$$
W=(F,M,B,T,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\WeierstrassMTestWindowUp}$ surface consists exactly of finite function-series window exposure through $F$, positive majorant-row exposure through $M$, domination ledger exposure through $B$, finite partial-tail budgeting through $T$, StreamName observation through $S$, RegSeqRat readback through $R$, RealUp sealing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:weierstrass-m-test-window-carrier}. The rows $F,M,B,T$ expose the source windows, positive majorant sequence rows, domination ledger, and finite partial-tail budget before any convergence-facing read can be served.

Use the sibling surfaces in order. The source series is the real-series surface of \autoref{ch:concrete-instances-realseries-namecert}; the majorant route is the M-test packet of \autoref{ch:concrete-instances-weierstrassmtest-namecert}; the finite observations are StreamName windows from \autoref{ch:concrete-instances-streamname-namecert}; and the regular readback is the $\RegSeqRatUp$ surface of \autoref{ch:concrete-instances-regseqrat-namecert}.

Finally $E,H,C,P,N$ seal, transport, replay, source, and name the same finite route. Since the carrier has no coordinate for countable choice, arbitrary infinite sums, quotient real equality, selected limits, host function equality, or ambient Real completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Weierstrass M-test finite-window Cauchy-bound route]
\label{thm:weierstrass-m-test-window-cauchy-bound-route}
Every Cauchy-bound or Real-facing consumer of an accepted $\WeierstrassMTestWindowUp$ packet factors through the finite route
$$
\RealSeriesUp,\ \WeierstrassMTestUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp,\ \RealUp .
$$
The partial-tail budget is therefore read as a finite majorant-window certificate before the terminal real seal is consumed. The route exports no countable-choice schedule, arbitrary infinite sum, quotient real equality, selected limit, host function equality, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with \autoref{thm:weierstrass-m-test-window-namecert-obligations}. It forces every consumer read through the source window, positive majorant rows, domination ledger, finite partial-tail budget, StreamName observation, RegSeqRat readback, and RealUp seal before the local certificate can be inspected.

The $\WeierstrassMTestUp$ sibling supplies the majorant-tail route, while $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ keep each observation and tolerance request finite. Thus the Cauchy-bound read is a displayed finite-window route, not an appeal to an unrecorded infinite summation theorem.

The terminal row $E$ is only the $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. The absence of countable-choice, arbitrary-sum, quotient-real, selector, host-equality, and ambient-completeness coordinates prevents any such object from entering the exported route.
\end{proof}

\falsifiablePrediction{Every accepted $\WeierstrassMTestWindowUp$ read exposes finite function-series windows, positive majorant rows, finite domination ledgers, partial-tail budgets, StreamName windows, RegSeqRat readback, RealUp seal, componentwise hsame, Cont replay, Pkg provenance, and local NameCert rows before any Cauchy-bound or Real-facing consumer is served.}

\independenceWitness{realseries, weierstrassmtest, streamname, regseqrat, dyadicratcore, real: none alone is bijective with $\WeierstrassMTestWindowUp$ because this packet alone binds the finite source window, positive majorant window, domination ledger, partial-tail budget, observation route, readback, seal, transport, replay, provenance, and local naming as one certificate.}

\closureat{WeierstrassMTestWindowUp}{seedStr}

\begin{closurestatus}{\WeierstrassMTestWindowUp}
  \constructivestory{A $\WeierstrassMTestWindowUp$ packet is generated from finite function-series windows, displayed positive majorant rows, a finite domination ledger, a partial-tail budget, StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:weierstrass-m-test-window-carrier}, \autoref{thm:weierstrass-m-test-window-namecert-obligations}, and \autoref{thm:weierstrass-m-test-window-cauchy-bound-route} seed the finite majorant-window route for the Weierstrass M-test.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close countable choice, arbitrary infinite sums, quotient real equality, selected limits, host function equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite source-window exposure, positive majorant-row exposure, domination-ledger exactness, partial-tail budgeting, StreamName observation, RegSeqRat readback, RealUp seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.WeierstrassMTestWindowUp.}
\end{closurestatus}
