\chapter{A Concrete Naming Certificate for $\BishopApartnessMorphismUp$}
\label{ch:concrete-instances-bishop-apartness-morphism-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopApartnessMorphismUp$ packet records the constructive interface for strongly extensional maps that preserve apartness between displayed real-name and completion-facing apartness carriers. It sits beside the strongly extensional real-map surface of \autoref{ch:concrete-instances-strongly-extensional-real-map-namecert}, the located-apartness route of \autoref{ch:concrete-instances-located-apartness-namecert}, the constructive apartness-space packet of \autoref{ch:concrete-instances-constructive-apartness-space-namecert}, the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite morphism certificate only: it does not introduce host functions, quotient equality of real names, trichotomy, selected witnesses, countable choice, Real completeness, or bridge checking.

\begin{definition}[BishopApartnessMorphism carrier]
\label{def:bishop-apartness-morphism-carrier}
A $\BishopApartnessMorphismUp$ carrier is a finite $\BHist$ packet
$$
A=(S,T,G,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source apartness row, read through $\LocatedApartnessUp$ or $\ConstructiveApartnessSpaceUp$ before any morphism claim is consumed.
\item $T$ is the target apartness row, read through the same apartness-space discipline and kept separate from the source row.
\item $G$ is the graph-and-continuity row, combining the strongly extensional graph route of $\StronglyExtensionalRealMapUp$ with the finite continuity surface of $\ContinuousMapUp$.
\item $R$ is the $\RegSeqRatUp$ readback row for the regular rational observations used by the source, target, and graph rows.
\item $E$ is the terminal $\RealUp$ seal row reached only after $S,T,G,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for source apartness, target apartness, graph continuity, regular readback, Real seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay of the route
$$
S,T,G \longmapsto R \longmapsto E \longmapsto H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\BishopApartnessMorphismUp$, $\StronglyExtensionalRealMapUp$, $\LocatedApartnessUp$, $\ConstructiveApartnessSpaceUp$, $\ContinuousMapUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopApartnessMorphismUp}$ row.
\end{enumerate}
The accepted carrier is exactly the displayed packet $A$. It has no coordinate for a host function, quotient equality of real names, trichotomy, a selected apartness witness, countable choice, ambient $\RealUp$ completeness, or a bridge row.
\end{definition}

\begin{theorem}[BishopApartnessMorphism NameCert obligations]
\label{thm:bishop-apartness-morphism-namecert-obligations}
For every accepted $\BishopApartnessMorphismUp$ carrier $A=(S,T,G,R,E,H,C,P,N)$, the local $\NameCert_{\BishopApartnessMorphismUp}$ obligations are exhausted by source-apartness exposure through $S$, target-apartness exposure through $T$, graph and continuity exposure through $G$, regular rational readback through $R$, terminal Real sealing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$. A consumer may read the packet as an apartness-preserving strongly extensional morphism only through that finite route, not through host functions, quotient equality, trichotomy, selected apartness witnesses, countable choice, ambient Real completeness, or bridge checking.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-apartness-morphism-carrier}. The only visible rows are $S,T,G,R,E,H,C,P,N$, so the source and target apartness data remain displayed before any graph or continuity read is available.

Read the morphism route next. The graph row $G$ is accepted only as the finite $\StronglyExtensionalRealMapUp$ and $\ContinuousMapUp$ handoff over the already displayed apartness rows, while $R$ and $E$ keep the regular rational readback and terminal Real seal on the same route.

Finally transport and replay the packet through $H,C,P,N$. These structural rows preserve the source, target, graph, readback, and seal coordinates without adding a host function, quotient-equality coordinate, trichotomy principle, selected witness, countable-choice schedule, Real-completeness row, or bridge row.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopApartnessMorphismUp$ consumer exposes one source apartness row, one target apartness row, one strongly extensional graph-and-continuity row, one RegSeqRat readback row, one Real seal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming before using the morphism as apartness-preserving data.}

\independenceWitness{strongly-extensional-real-map, located-apartness, constructive-apartness-space, continuousmap, regseqrat, real: none is bijective to $\BishopApartnessMorphismUp$ because the morphism packet jointly carries source and target apartness rows, graph continuity, regular readback, Real sealing, transport, replay, provenance, and local naming in one finite certificate.}

\closureat{BishopApartnessMorphismUp}{seedStr}

\begin{closurestatus}{\BishopApartnessMorphismUp}
  \constructivestory{A $\BishopApartnessMorphismUp$ packet is generated from source and target apartness rows, a strongly extensional graph-and-continuity row, RegSeqRat readback, Real sealing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-apartness-morphism-carrier} and \autoref{thm:bishop-apartness-morphism-namecert-obligations} seed the strongly extensional apartness-preserving morphism surface over the displayed Real and Completion corridor rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host functions, quotient equality of real names, trichotomy, selected apartness witnesses, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source and target apartness exposure, graph-continuity exactness, RegSeqRat readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BishopApartnessMorphismUp.}
\end{closurestatus}
