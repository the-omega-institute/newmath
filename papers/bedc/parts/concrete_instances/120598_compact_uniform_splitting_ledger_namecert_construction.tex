\chapter{A Concrete Naming Certificate for $\CompactUniformSplittingLedgerUp$}
\label{ch:concrete-instances-compact-uniform-splitting-ledger-namecert}
\origin{ai}

The $\CompactUniformSplittingLedgerUp$ packet realizes the compact-uniform-continuity route as one finite split ledger: compact finite windows and pointwise continuous-map rows are folded into a tolerance-only $\UniformModulusUp$ handoff before any $\RealUp$ consumer reads the target estimate. It sits over the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the graph and pointwise-modulus surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the tolerance-only handoff of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the dyadic precision rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The carrier records the finite $\ProbeBundle$ split from pointwise compact-cover moduli to one uniform-modulus read; it is not a host Heine--Cantor theorem, a selected Lebesgue number, an infinite cover, countable choice, or ambient completeness of $\RealUp$.

\begin{definition}[Compact-uniform splitting ledger carrier]
\label{def:compact-uniform-splitting-ledger-carrier}
A $\CompactUniformSplittingLedgerUp$ carrier is a finite $\BHist$ packet
$$
S=(K,F,U,B,D,Q,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, listing the finite compact windows and cover probes inspected by the ledger.
\item $F$ is the $\ContinuousMapUp$ pointwise row, recording the local modulus entries attached to the compact windows in $K$.
\item $U$ is the $\UniformModulusUp$ tolerance-only output row.
\item $B$ is the finite $\ProbeBundle$ split ledger, pairing each compact probe with its local radius, target tolerance request, and selected finite comparison row.
\item $D$ is the $\DyadicRatCoreUp$ precision row for the dyadic tolerance cuts used by the split.
\item $Q$ is the rational lower-bound and triangle-budget row that chooses the displayed minimum budget over the finite probe bundle.
\item $R$ is the $\RegSeqRatUp$ readback row for rational and dyadic estimates consumed by downstream Real-facing readers.
\item $E$ is the terminal $\RealUp$ seal row for the source and target metric estimates.
\item $H$ records componentwise $\hsame$ transport for compact, continuous-map, uniform-modulus, probe-bundle, dyadic, rational-budget, regular-readback, Real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the finite split
$$
K,F,B,D,Q \longmapsto U,R,E,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\CompactUniformSplittingLedgerUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CompactUniformSplittingLedgerUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,F,U,B,D,Q,R,E,H,C,P,N$. It has no coordinate for an infinite cover, selected Lebesgue number, host compactness theorem, countable choice, external modulus oracle, quotient real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact-uniform splitting ledger NameCert obligations]
\label{thm:compact-uniform-splitting-ledger-namecert-obligations}
For every accepted $\CompactUniformSplittingLedgerUp$ carrier
$$
S=(K,F,U,B,D,Q,R,E,H,C,P,N),
$$
the local $\NameCert_{\CompactUniformSplittingLedgerUp}$ surface consists exactly of compact-source exposure through $K$, continuous-map pointwise exposure through $F$, finite $\ProbeBundle$ split exposure through $B$, dyadic precision exposure through $D$, rational lower-bound and metric-triangle budget exposure through $Q$, tolerance-only uniform-modulus output through $U$, regular-rational readback through $R$, terminal Real sealing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-splitting-ledger-carrier}. The compact-continuous source rows are $K,F$, the finite splitting rows are $B,D,Q$, the output rows are $U,R,E$, and the structural rows are $H,C,P,N$.

The compact source and continuous-map rows determine the finite cover probes and local pointwise moduli. The $\ProbeBundle$ row then records the split from those local rows to a single tolerance request.

The dyadic and rational-budget rows record the precision and lower-bound comparisons used in the metric-triangle estimate. The regular readback and Real seal are downstream of those rows, while the uniform-modulus row is the only tolerance-only output.

Finally, transport, replay, provenance, and local naming preserve that displayed route. Since the carrier has no infinite cover, selected Lebesgue number, host compactness theorem, countable-choice row, external modulus oracle, quotient real equality, or ambient Real-completeness coordinate, those objects cannot discharge a local NameCert obligation.
\end{proof}

\begin{theorem}[Compact-uniform split nonescape]
\label{thm:compact-uniform-splitting-ledger-nonescape}
Every $\UniformModulusUp$ or $\RealUp$ consumer of an accepted $\CompactUniformSplittingLedgerUp$ packet must read the finite route
$$
\CompactMetricUp,\ \ContinuousMapUp,\ \ProbeBundle,\ \DyadicRatCoreUp
\longmapsto Q,\ \UniformModulusUp,\ \RegSeqRatUp,\ \RealUp
\longmapsto H,C,P,N .
$$
Thus the tolerance-only uniform-modulus handoff and the Real-facing metric estimate cannot be justified by an infinite cover, a selected Lebesgue number, countable choice, quotient real equality, or ambient completeness.
\end{theorem}
\begin{proof}
Start with \autoref{thm:compact-uniform-splitting-ledger-namecert-obligations}. It forces the compact metric source, continuous-map pointwise row, finite ProbeBundle split, dyadic precision row, and rational budget row to be displayed before a uniform-modulus or Real-facing read is available.

The uniform-modulus output is the row $U$. It is tolerance-only because the source point, local modulus, lower-bound fold, and triangle estimate have already been folded through $B,D,Q$.

The Real-facing read then passes through $R$ and $E$. These rows read regular rational and Real seals downstream from the same split ledger, so they cannot introduce a selected cover, quotient real equality, or completeness theorem.

The structural rows keep the route finite and replayable. Hence every downstream consumer remains inside the displayed compact-uniform splitting ledger.
\end{proof}

\begin{theorem}[Compact-uniform splitting ledger obligation exhaustion]
\label{thm:compact-uniform-splitting-ledger-obligation-exhaustion}
For every accepted $\CompactUniformSplittingLedgerUp$ carrier
$$
S=(K,F,U,B,D,Q,R,E,H,C,P,N),
$$
the carrier admission, compact-source exposure, pointwise $\ContinuousMapUp$ exposure, finite $\ProbeBundle$ split exactness, $\DyadicRatCoreUp$ precision, rational lower-bound and triangle budget, $\UniformModulusUp$ handoff, $\RegSeqRatUp$ readback, $\RealUp$ seal ordering, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local naming rows exhaust the local $\NameCert_{\CompactUniformSplittingLedgerUp}$ surface. No further local obligation remains outside the displayed L10 rows of $S$.
\end{theorem}
\begin{proof}
Start with the carrier inventory of \autoref{def:compact-uniform-splitting-ledger-carrier}. It admits exactly the rows $K,F,U,B,D,Q,R,E,H,C,P,N$ and gives no coordinate for an infinite cover, selected Lebesgue number, host compactness theorem, countable choice, external modulus oracle, quotient real equality, or ambient Real-completeness principle.

Apply the obligation surface of \autoref{thm:compact-uniform-splitting-ledger-namecert-obligations}. It identifies the compact source, pointwise continuous-map data, finite ProbeBundle split, dyadic precision, rational budget, uniform-modulus output, regular readback, Real seal, transport, replay, provenance, and local naming rows as the complete local NameCert surface.

Finally use the nonescape theorem \autoref{thm:compact-uniform-splitting-ledger-nonescape}. Every downstream $\UniformModulusUp$ or $\RealUp$ consumer must read the same route through those rows, so there is no second local discharge site. Hence the displayed L10 rows exhaust the local obligations.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactUniformSplittingLedgerUp$ read is refuted if it exports a $\UniformModulusUp$ or $\RealUp$ result without displaying the $\CompactMetricUp$, $\ContinuousMapUp$, finite $\ProbeBundle$, $\DyadicRatCoreUp$, rational lower-bound, $\RegSeqRatUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.}

\independenceWitness{compactmetric, continuousmap, uniformmodulus, dyadicratcore, real: $\CompactUniformSplittingLedgerUp$ is not identical to any listed sibling because it binds compact finite-window data, pointwise continuous-map moduli, a finite ProbeBundle split, dyadic precision, rational lower-bound readback, a tolerance-only UniformModulus handoff, and a Real seal as one NameCert packet.}

\closureat{CompactUniformSplittingLedgerUp}{seedStr}

\begin{closurestatus}{\CompactUniformSplittingLedgerUp}
  \constructivestory{A $\CompactUniformSplittingLedgerUp$ packet is generated from CompactMetric finite-window rows, ContinuousMap pointwise-modulus rows, a UniformModulus output row, finite ProbeBundle split rows, DyadicRatCore precision rows, rational lower-bound and triangle-budget rows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-splitting-ledger-carrier}, \autoref{thm:compact-uniform-splitting-ledger-namecert-obligations}, and \autoref{thm:compact-uniform-splitting-ledger-nonescape} seed the finite split from pointwise compact-cover moduli to one tolerance-only uniform-modulus handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim an infinite cover, selected Lebesgue number, host compactness theorem, countable choice, external modulus oracle, quotient real equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source exposure, pointwise ContinuousMap exposure, ProbeBundle split exactness, dyadic precision, rational lower-bound and triangle-budget exactness, UniformModulus handoff, RegSeqRat readback, Real seal ordering, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformSplittingLedgerUp.}
\end{closurestatus}
