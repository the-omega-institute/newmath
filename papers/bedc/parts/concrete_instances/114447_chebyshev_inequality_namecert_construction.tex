\chapter{A Concrete Naming Certificate for $\ChebyshevInequalityUp$}
\label{ch:concrete-instances-chebyshev-inequality-namecert}
\origin{human}

Following Chebyshev and Kolmogorov, the $\ChebyshevInequalityUp$ packet records the finite probability tail-bound surface used before weak-law consumers read a variance-controlled estimate. It sits after the normalized event rows of \autoref{ch:concrete-instances-probspace-namecert}, the measurable random-variable surface of \autoref{ch:concrete-instances-randomvar-namecert}, the finite measure ledger of \autoref{ch:concrete-instances-finite-measure-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. It is the standalone tail-bound carrier consumed by \autoref{ch:concrete-instances-weak-law-large-numbers-namecert}, not a limit theorem, countable-choice selector, almost-sure convergence result, or ambient $\RealUp$ completeness principle.

\begin{definition}[Chebyshev inequality carrier]\label{def:chebyshev-inequality-carrier}
A $\ChebyshevInequalityUp$ carrier is a finite $\BHist$ packet
$$
C=(P,M,X,\mu,V,\epsilon,T,R,H,K,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $P$ is the $\ProbSpaceUp$ source row carrying the event classifier and total-mass normalization.
\item $M$ is the $\FiniteMeasureUp$ measure row for the exceptional event and its complement inside the displayed probability source.
\item $X$ is the $\RandomVarUp$ row whose finite observation windows are inspected over $P$.
\item $\mu$ is the displayed center row for the same random-variable surface.
\item $V$ is the finite variance or second-moment bound row consumed by the tail estimate.
\item $\epsilon$ is the positive $\RealUp$ tolerance seal naming the requested separation from the center.
\item $T$ is the displayed tail-event row, recording the event where the random-variable observation differs from $\mu$ by more than $\epsilon$.
\item $R$ is the $\RealUp$ probability-bound row reached only after $P,M,X,\mu,V,\epsilon,T$ have been displayed.
\item $H$ stores componentwise $\hsame$ transport for the source, measure, random-variable, center, variance, tolerance, tail-event, probability-bound, replay, provenance, and local naming rows.
\item $K$ records displayed $\Cont$ replay of the finite tail-bound route.
\item $Q$ is the $\Pkg$ provenance over $\ChebyshevInequalityUp$, $\ProbSpaceUp$, $\FiniteMeasureUp$, $\RandomVarUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ChebyshevInequalityUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $P,M,X,\mu,V,\epsilon,T,R,H,K,Q,N$. It has no coordinate for an infinite sequence of variables, almost-sure convergence, selected subsequence, central-limit normalization, host measure algebra, or ambient real completeness.
\end{definition}

\begin{theorem}[Chebyshev inequality NameCert obligations]\label{thm:chebyshev-inequality-namecert-obligations}
For every accepted $\ChebyshevInequalityUp$ carrier $C=(P,M,X,\mu,V,\epsilon,T,R,H,K,Q,N)$, the local $\NameCert_{\ChebyshevInequalityUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the displayed probability source, finite-measure row, random-variable row, center row, variance-bound row, positive tolerance seal, tail-event row, probability-bound row, transport, replay, provenance, and local naming;
\item source and event stability, requiring $M$ and $T$ to remain attached to the event classifier displayed by $P$;
\item random-variable stability, requiring every tail-event read to consume the same $\RandomVarUp$ row $X$ and center row $\mu$;
\item variance-budget stability, requiring $V$ to be displayed before the tail probability bound is read;
\item tolerance seal stability, requiring $\epsilon$ to be a displayed $\RealUp$ positive tolerance before $T$ and $R$ are consumed;
\item probability-bound nonescape, requiring $R$ to seal only the displayed Chebyshev tail estimate and not an infinite-limit, selected-subsequence, host-measure, or ambient-completeness claim;
\item componentwise transport, displayed replay, package provenance, and local naming through $H,K,Q,N$.
\end{enumerate}
These obligations certify only the finite probability tail-bound route. They do not close a weak law, almost-sure convergence, a central limit theorem, countable choice, host probability limits, or Lean verification.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:chebyshev-inequality-carrier}. The accepted rows are exactly the probability source, finite-measure row, random-variable row, center row, variance-bound row, tolerance seal, tail-event row, probability-bound row, and the structural rows.

Source and event stability are read from $P,M,T$. The event classifier comes from $P$, the finite measure row $M$ attaches measure data to that source, and $T$ names only the displayed exceptional event.

Random-variable and variance-budget stability are read from $X,\mu,V$. A tail estimate can be consumed only after the variable, center, and variance-bound rows have been displayed.

The endpoint $R$ is downstream from the positive tolerance seal $\epsilon$ and from the tail-event row. Since the carrier has no coordinate for infinite limits, selected subsequences, host measure algebra, or ambient completeness, $R$ cannot export any of those objects.

The rows $H,K,Q,N$ preserve, replay, source, and name the same finite route. Hence the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Chebyshev tail-bound nonescape]\label{thm:chebyshev-inequality-tail-bound-nonescape}
Every accepted $\ChebyshevInequalityUp$ tail-bound read factors through
$$
P\longmapsto M\longmapsto X\longmapsto \mu\longmapsto V\longmapsto \epsilon\longmapsto T\longmapsto R\longmapsto H,K,Q,N
$$
before any downstream probability consumer receives the estimate. The route exposes a finite probability source, finite-measure event row, random-variable row, center, variance budget, positive tolerance, tail event, real probability bound, transport, replay, provenance, and local naming. It cannot be replaced by a host law of large numbers, almost-sure convergence, central-limit normalization, countable-choice subsequence selection, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with a tail-bound request from an accepted carrier. Projection gives the source rows $P,M$ and no independent host probability-space coordinate.

The request next reads $X,\mu,V,\epsilon$. These rows display the random variable, center, variance budget, and positive real tolerance before the exceptional event can be measured.

Only after those rows are visible can $T$ and $R$ be consumed as the finite Chebyshev tail estimate. The structural rows $H,K,Q,N$ then transport, replay, package, and name the same route for downstream consumers.

Since every readable coordinate has already been listed, the route has no path to almost-sure convergence, a central-limit theorem, selected subsequences, host probability limits, or ambient real completeness.
\end{proof}

\falsifiablePrediction{Every $\ChebyshevInequalityUp$ tail-bound read factors through a ProbSpace source row, a FiniteMeasure event row, a RandomVar observation row, a displayed center, a variance-bound row, a positive Real tolerance, a tail-event row, a Real probability-bound row, hsame transport, Cont replay, Pkg provenance, and local naming.}

\independenceWitness{probspace, finite-measure, randomvar, real, weak-law-large-numbers: $\ChebyshevInequalityUp$ is not bijective to any listed sibling because it jointly carries a probability source, finite-measure event row, random-variable observation, center, variance budget, positive tolerance, tail event, probability-bound seal, transport, replay, provenance, and local naming.}

\closureat{ChebyshevInequalityUp}{seedStr}

\begin{closurestatus}{\ChebyshevInequalityUp}
  \constructivestory{A $\ChebyshevInequalityUp$ packet is generated from a probability source row, a finite-measure event row, a random-variable row, a displayed center, a variance or second-moment bound row, a positive RealUp tolerance, a tail-event row, a RealUp probability-bound row, componentwise hsame transport, continuation replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:chebyshev-inequality-carrier}, \autoref{thm:chebyshev-inequality-namecert-obligations}, and \autoref{thm:chebyshev-inequality-tail-bound-nonescape} seed the finite probability tail-bound route through ProbSpace, FiniteMeasure, RandomVar, RealUp, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close weak laws, almost-sure convergence, central-limit normalization, countable-choice subsequence selection, host probability limits, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source and event stability, random-variable stability, variance-budget stability, tolerance seal stability, probability-bound nonescape, transport, replay, provenance, and local naming for BEDC.Derived.ChebyshevInequalityUp.}
\end{closurestatus}
