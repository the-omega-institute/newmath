\chapter{A Concrete Naming Certificate for $\RieszMarkovKakutaniUp$}
\label{ch:concrete-instances-riesz-markov-kakutani-namecert}
\origin{human}

Following Riesz, Markov, and Kakutani, the $\RieszMarkovKakutaniUp$ packet records the finite BEDC surface that routes a positive linear functional on displayed continuous functions over a compact source to a Radon-measure handoff. It sits over the compact source rows of \autoref{ch:concrete-instances-compactmetric-namecert}, the function rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the measure rows of \autoref{ch:concrete-instances-radon-measure-namecert}. The packet names the carrier, positivity ledger, linearity ledger, compact-support restriction, finite approximation route, measure handoff, transport, replay, provenance, and local naming; it does not import host dual spaces, selected representing measures, open-cover compactness, quotient function equality, or countable choice.

\begin{definition}[Riesz--Markov--Kakutani carrier]
\label{def:riesz-markov-kakutani-carrier}
A $\RieszMarkovKakutaniUp$ carrier is a finite $\BHist$ packet
$$
R=(K,F,V,P,L,A,M,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row carrying the compact domain surface that every continuous-function read must inspect.
\item $F$ is the $\ContinuousMapUp$ function-family row over $K$, with target observations sealed through $\RealUp$ rows.
\item $V$ is the real-valued functional row, recording the finite evaluation requests and real-name outputs read by the positive functional.
\item $P$ is the positivity ledger: nonnegative function rows in $F$ are sent to nonnegative $\RealUp$ output rows.
\item $L$ is the linearity ledger, recording displayed addition and scalar-multiplication compatibility for the finite function rows read by the packet.
\item $A$ is the approximation row, routing compact finite probes and continuous-function modulus data to the finite test family used by the measure handoff.
\item $M$ is the $\RadonMeasureUp$ handoff row, exposing the measure candidate only after $K,F,V,P,L,A$ are visible.
\item $H$ records componentwise $\hsame$ transport for the compact source, function family, real-valued functional, positivity, linearity, approximation, measure handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through the same rows.
\item $Q$ is the $\Pkg$ provenance over $\RieszMarkovKakutaniUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\RadonMeasureUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RieszMarkovKakutaniUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for an ambient dual Banach space, a selected representing measure, quotient equality of continuous functions, arbitrary open-cover compactness, countable choice, or a completed integration theory.
\end{definition}

\begin{theorem}[Riesz--Markov--Kakutani NameCert obligations]
\label{thm:riesz-markov-kakutani-namecert-obligations}
For every accepted $\RieszMarkovKakutaniUp$ carrier
$$
R=(K,F,V,P,L,A,M,H,C,Q,N),
$$
the local $\NameCert_{\RieszMarkovKakutaniUp}$ surface consists exactly of compact-source admission through $K$, continuous-function admission through $F$, real-valued functional exposure through $V$, positivity through $P$, linearity through $L$, finite approximation through $A$, Radon-measure handoff through $M$, componentwise transport through $H$, displayed replay through $C$, package provenance through $Q$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:riesz-markov-kakutani-carrier}. The rows $K,F,V$ display the compact domain, continuous real-valued function data, and finite functional evaluations before any measure handoff is public.

Read the positivity and linearity ledgers next. They record exactly the functional laws needed by the handoff and do not introduce a host dual space or quotient equality of functions.

The approximation row $A$ then ties compact finite probes to the continuous-function rows, and the measure row $M$ is exposed only after that route is visible. Finally $H,C,Q,N$ preserve, replay, source, and name the same packet, so the listed rows exhaust the local certificate obligations.
\end{proof}

\begin{theorem}[Riesz--Markov--Kakutani positive functional measure route]
\label{thm:riesz-markov-kakutani-positive-functional-measure-route}
Every downstream $\RadonMeasureUp$ consumer of an accepted $\RieszMarkovKakutaniUp$ packet must first read the compact source row $K$, the continuous-function row $F$, the real-valued functional row $V$, the positivity ledger $P$, the linearity ledger $L$, and the finite approximation row $A$. The measure handoff is therefore a displayed route
$$
\CompactMetricUp \longmapsto \ContinuousMapUp \longmapsto \RealUp
\longmapsto (P,L,A) \longmapsto \RadonMeasureUp
$$
inside the packet, not a selected representing-measure theorem in the host metatheory.
\end{theorem}
\begin{proof}
Start from the obligation theorem \autoref{thm:riesz-markov-kakutani-namecert-obligations}. It places the compact source, function family, functional row, positivity row, linearity row, and approximation row before the measure handoff.

The $\CompactMetricUp$ and $\ContinuousMapUp$ rows provide the finite domain and function observations, while $\RealUp$ seals the functional outputs. Positivity and linearity remain displayed ledgers attached to those observations.

Only after these rows are visible may a $\RadonMeasureUp$ consumer inspect $M$. Since the carrier has no selected measure coordinate, no quotient function equality, and no countable-choice coordinate, the exported measure read is exactly the displayed handoff route.
\end{proof}

\closureat{RieszMarkovKakutaniUp}{seedStr}

\begin{closurestatus}{\RieszMarkovKakutaniUp}
  \constructivestory{A $\RieszMarkovKakutaniUp$ packet is generated from a CompactMetric source row, ContinuousMap real-valued function rows, RealUp functional outputs, positivity and linearity ledgers, finite approximation rows, a RadonMeasure handoff, componentwise hsame transport, displayed Cont replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:riesz-markov-kakutani-carrier}, \autoref{thm:riesz-markov-kakutani-namecert-obligations}, and \autoref{thm:riesz-markov-kakutani-positive-functional-measure-route} seed the positive-functional-to-measure NameCert surface over compact metric, continuous map, real-name, and Radon measure rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, a selected representing measure, host dual-space equality, quotient equality of continuous functions, open-cover compactness, countable choice, or a completed integration theory.}
  \upgradepath{Obligation closure requires checked carrier admission, positivity exposure, linearity exposure, compact approximation routing, RadonMeasure handoff stability, replay stability, transport stability, provenance exhaustion, and local naming for BEDC.Derived.RieszMarkovKakutaniUp.}
\end{closurestatus}
