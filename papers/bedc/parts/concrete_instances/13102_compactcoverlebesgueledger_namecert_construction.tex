\chapter{A Concrete Naming Certificate for $\CompactCoverLebesgueLedgerUp$}
\label{ch:concrete-instances-compact-cover-lebesgue-ledger-namecert}
\origin{ai}

The $\CompactCoverLebesgueLedgerUp$ packet materializes the finite-cover lower-bound ledger used by \autoref{ch:visions-compact-uniform-continuity}. It sits after the compact-uniform-continuity packet of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, the finite rational lower-bound fold of \autoref{ch:concrete-instances-finitenetminimumfold-namecert}, the compact-metric finite-net surface, the continuous-map pointwise-radius rows, and the uniform-modulus consumer. Its purpose is narrower than the cover-shrinkage ledger: it records the concrete Lebesgue-number-style row that joins a finite $\mathsf{ProbeBundle}$ cover, positive $\RatUp$ radius rows, and the final $\UniformModulusUp$ handoff.

\begin{definition}[Compact cover Lebesgue ledger carrier]
\label{def:compact-cover-lebesgue-ledger-carrier}
A $\CompactCoverLebesgueLedgerUp$ carrier is a finite $\BHist$ packet
$$
L=(K,C,R,F,U,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net row, exposing the nonempty finite $\mathsf{ProbeBundle}$ of centers and the cover-membership rows;
\item $C$ is the $\ContinuousMapUp$ pointwise-radius ledger, assigning to each displayed center a positive local radius for the requested target tolerance;
\item $R$ is the positive $\RatUp$ radius row, recording the half-radius comparisons and the lower-bound candidates consumed by the fold;
\item $F$ is the $\FiniteNetMinimumFoldUp$ lower-bound row over the same finite $\mathsf{ProbeBundle}$ and the same positive radius ledger $R$;
\item $U$ is the $\UniformModulusUp$ consumer row that receives the precision exported by the finite lower-bound fold;
\item $H$ records componentwise $\hsame$ transport for the compact net, pointwise-radius ledger, rational rows, finite fold, uniform-modulus row, routes, provenance, and local naming;
\item $T$ records the displayed $\Cont$ routes through which compact finite nets, continuous pointwise radii, rational comparisons, fold readback, and uniform-modulus consumers inspect the packet;
\item $P$ is the $\Pkg$ provenance over $\CompactMetricUp$, $\ContinuousMapUp$, $\RatUp$, $\FiniteNetMinimumFoldUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and local naming rows;
\item $N$ is the local $\NameCert_{\CompactCoverLebesgueLedgerUp}$ row.
\end{enumerate}
The carrier has no coordinate for an open-cover compactness oracle, host minimum over radii, selected center outside the finite bundle, quotient metric equality, or external modulus function.
\end{definition}

\begin{theorem}[Compact cover Lebesgue ledger classifier surface]
\label{thm:compact-cover-lebesgue-ledger-classifier-surface}
The classifier of an accepted $\CompactCoverLebesgueLedgerUp$ packet is determined by exactly the rows $K,C,R,F,U,H,T,P,N$. Two presentations with the same compact finite-net row, pointwise-radius row, positive rational ledger, lower-bound fold, uniform-modulus output, componentwise transports, displayed routes, provenance, and local naming row determine the same Lebesgue-ledger classifier, and no presentation may refine the classifier by a host minimum, open-cover oracle, external center selector, quotient metric equality, or external modulus function.
\end{theorem}
\begin{proof}
Project both presentations to the carrier of \autoref{def:compact-cover-lebesgue-ledger-carrier}. Equality of the nine displayed rows identifies the compact source, local radius ledger, rational lower-bound candidates, fold output, and uniform-modulus consumer.

The transport, route, provenance, and naming rows are also shared. They replay the same reads and cannot introduce a new classifier coordinate.

Every excluded object is absent from the carrier inventory. Therefore the classifier is exhausted by the displayed finite packet.
\end{proof}

\begin{theorem}[Compact cover Lebesgue ledger NameCert obligations]
\label{thm:compact-cover-lebesgue-ledger-namecert-obligations}
For every accepted $\CompactCoverLebesgueLedgerUp$ packet
$$
L=(K,C,R,F,U,H,T,P,N),
$$
the local $\NameCert_{\CompactCoverLebesgueLedgerUp}$ surface consists exactly of carrier admission for $L$, compact finite-net admission through $K$, continuous pointwise-radius admission through $C$, positive $\RatUp$ lower-bound candidates through $R$, finite lower-bound fold exactness through $F$, uniform-modulus output exactness through $U$, componentwise $\hsame$ stability through $H$, displayed $\Cont$ replay through $T$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-cover-lebesgue-ledger-carrier}. The rows $K,C,R,F,U,H,T,P,N$ are the complete displayed packet, and the first five rows name the compact net, pointwise radii, rational candidates, finite fold, and uniform-modulus consumer.

The compact finite-net and continuous pointwise-radius obligations are read before the fold. Thus every lower-bound candidate in $R$ is attached to a center already visible in $K$ and to a pointwise-radius row already visible in $C$.

The fold row $F$ is exact over that same finite bundle and radius ledger. Its exported precision is the only row that $U$ may read as a $\UniformModulusUp$ precision.

Finally $H,T,P,N$ transport, replay, package, and name the same finite surface. Since no excluded coordinate occurs in the carrier, no local NameCert obligation can appeal to an open-cover oracle, host minimum, external selected center, quotient metric equality, or external modulus function.
\end{proof}
\leanchecked{BEDC.Derived.CompactCoverLebesgueLedgerUp.CompactCoverLebesgueLedgerCarrier\_namecert\_obligations}

\begin{theorem}[Compact cover Lebesgue ledger uniform-modulus handoff]
\label{thm:compact-cover-lebesgue-ledger-uniform-modulus-handoff}
Every accepted $\CompactCoverLebesgueLedgerUp$ consumer that exports a $\UniformModulusUp$ precision must factor through the compact finite-net row $K$, the continuous pointwise-radius row $C$, the positive $\RatUp$ lower-bound ledger $R$, the $\FiniteNetMinimumFoldUp$ row $F$, and the structural rows $H,T,P,N$. A consumer cannot export the uniform modulus without the compact finite-net row, continuous pointwise-radius row, positive lower-bound fold, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\end{theorem}
\begin{proof}
Begin with the compact finite-net row $K$. It supplies the finite $\mathsf{ProbeBundle}$ of centers used by the compact-uniform-continuity construction of \autoref{ch:visions-compact-uniform-continuity}.

Read the continuous pointwise-radius row $C$ at those centers. The positive rational row $R$ records the half-radius candidates whose comparisons make the local pointwise estimates usable by a common precision.

Apply the lower-bound fold $F$. Because $F$ is a $\FiniteNetMinimumFoldUp$ row over the same finite bundle and the same rational ledger, its exported precision is below every displayed local radius and is available as a single tolerance-only row.

The $\UniformModulusUp$ row $U$ can read that precision only through $H,T,P,N$. These structural rows preserve and route the finite ledger; they do not add a direct pointwise-to-uniform selector or a hidden compactness theorem.
\end{proof}

\falsifiablePrediction{A consumer that exports a $\UniformModulusUp$ precision from a compact-continuous packet without reading the compact finite-net row, continuous pointwise-radius row, positive $\RatUp$ lower-bound fold, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows refutes the $\CompactCoverLebesgueLedgerUp$ carrier.}

\independenceWitness{compactuniformcontinuity, finitenetminimumfold, compactnetmodulusselector, compactmoduluscover, uniformmodulus: $\CompactCoverLebesgueLedgerUp$ binds the compact finite-net row, continuous pointwise-radius row, positive rational lower-bound fold, uniform-modulus output, and structural transport rows in one finite NameCert packet; each named sibling lacks at least one of these rows as a local carrier obligation.}

Lean follow-up for this surface must provide $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances in \texttt{BEDC.Derived.CompactCoverLebesgueLedgerUp.TasteGate}.

\begin{closurestatus}{\CompactCoverLebesgueLedgerUp}
  \constructivestory{A $\CompactCoverLebesgueLedgerUp$ packet is generated from a compact finite-net row, continuous pointwise-radius rows, positive rational lower-bound candidates, a finite minimum-fold row, the resulting uniform-modulus output, componentwise $\hsame$ transport, displayed $\Cont$ replay, package provenance, and local naming. The classifier is exhausted by these rows: it reads the finite ProbeBundle centers, their local radii, and the lower-bound fold before any uniform consumer may inspect a precision. The ledger story is a finite Lebesgue-number account, where the cover evidence is not an oracle but a displayed list whose fold and replay routes can be audited in the packet itself.}
  \theoryclosure{\seedClosure}
  \scopeclosed{This chapter binds the carrier definition, classifier-surface, NameCert-obligation, uniform-modulus-handoff theorem, falsifiable prediction, independence witness, and Lean-follow-up boundary for $\CompactCoverLebesgueLedgerUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{It does not claim an open-cover compactness oracle, host minimum over radii, selected center outside the finite bundle, quotient metric equality, or external modulus function.}
  \upgradepath{To reach obligationClosure, the finite lower-bound fold and uniform-modulus handoff must be encoded with checked carrier and classifier obligations.}
\end{closurestatus}
