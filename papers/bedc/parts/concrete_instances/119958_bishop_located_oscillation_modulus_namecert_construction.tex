\chapter{A Concrete Naming Certificate for $\BishopLocatedOscillationModulusUp$}
\label{ch:concrete-instances-bishop-located-oscillation-modulus-namecert}
\origin{human}

Bishop and Bridges constructive analysis treats an oscillation modulus as finite located data: a tolerance request is answered by an inspected window and a displayed rational bound, not by a selected limiting object. The $\BishopLocatedOscillationModulusUp$ packet records this L10 corridor as a NameCert carrier over the pointwise oscillation surface of \autoref{ch:concrete-instances-real-oscillation-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}. It refines the neighboring located oscillation certificate by naming the modulus row consumed before Real or Completion consumers turn oscillation bounds into Cauchy-facing evidence.

\begin{definition}[Bishop located oscillation-modulus carrier]
\label{def:bishop-located-oscillation-modulus-carrier}
A $\BishopLocatedOscillationModulusUp$ carrier is a finite $\BHist$ packet
$$
M_{\mathrm{locosc}}=(O,D,S,Q,R,W,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $O$ is the $\RealOscillationUp$ source row whose inspected values and local difference bounds are read from the existing real-oscillation packet.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger carrying the requested oscillation radius, endpoint slack, and comparison budget.
\item $S$ is the $\StreamNameUp$ finite-window row naming the sampled source windows used for the request.
\item $Q$ is the $\RegSeqRatUp$ readback row attached to the same windows and dyadic budget.
\item $R$ is the terminal $\RealUp$ seal available only after $O,D,S,Q$ have been displayed.
\item $W$ is the located-window ledger recording which finite source window answers the request and which rational bound it returns.
\item $T$ is the tolerance-to-window modulus row: for every displayed dyadic tolerance in $D$, it points to one inspected row of $W$ and to the corresponding regular readback through $Q$ and $R$.
\item $H$ records componentwise $\hsame$ transport for $O,D,S,Q,R,W,T$ and the local naming data.
\item $C$ records displayed $\Cont$ replay from a tolerance request through the dyadic ledger, window row, regular readback, Real seal, located-window ledger, and modulus row.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedOscillationModulusUp$, $\RealOscillationUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopLocatedOscillationModulusUp}$ row.
\end{enumerate}
The classifier accepts only packets whose modulus row $T$ is downstream of the displayed oscillation row $O$ and the same dyadic-window-readback route $D,S,Q,R,W$. It has no coordinate for ambient $\RealUp$ completeness, quotient stream equality, selected limits, trichotomy, countable choice, host supremum, or a detached modulus oracle.
\end{definition}

\begin{theorem}[Bishop located oscillation-modulus NameCert obligations]
\label{thm:bishop-located-oscillation-modulus-namecert-obligations}
For an accepted $\BishopLocatedOscillationModulusUp$ carrier $M_{\mathrm{locosc}}=(O,D,S,Q,R,W,T,H,C,P,N)$, the local $\NameCert_{\BishopLocatedOscillationModulusUp}$ surface consists exactly of:
\begin{enumerate}
\item carrier admission for the real-oscillation row, dyadic tolerance ledger, stream-window row, regular rational readback, Real seal, located-window ledger, tolerance-to-window modulus row, transport, replay, provenance, and local naming rows;
\item oscillation-source exposure, requiring $O$ to be read before any modulus row answers a tolerance request;
\item dyadic-window coverage, requiring every accepted tolerance in $D$ to name one displayed finite window in $S$ and one regular readback in $Q$ before the terminal seal $R$ is consumed;
\item located-modulus exactness, requiring $T$ to point only to rows already present in $W$ and to export no answer outside the displayed dyadic-window route;
\item non-escape, requiring $H,C,P,N$ to transport, replay, source, and name no ambient completeness theorem, quotient stream equality, selected limit, trichotomy, countable-choice schedule, host supremum, or detached oracle.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the accepted carrier. The source row $O$, tolerance ledger $D$, window row $S$, regular readback $Q$, Real seal $R$, located ledger $W$, and modulus row $T$ are all displayed before the structural rows $H,C,P,N$ can expose the packet.

The source-exposure and dyadic-window clauses are componentwise projections. A tolerance request is first read in $D$, then replayed through $S$ and $Q$, and only then reaches the terminal seal $R$.

Located-modulus exactness follows from the shape of $T$: every answer points back into $W$ and to the same $D,S,Q,R$ route. Since the carrier stores no separate oracle row, $T$ cannot answer from outside the packet.

Finally, $H,C,P,N$ preserve and name only these displayed coordinates. None contains ambient real completeness, quotient stream equality, selected limits, trichotomy, countable choice, host suprema, or detached modulus data, so the NameCert surface has no exit to those objects.
\end{proof}

\begin{theorem}[Bishop located oscillation-modulus window route]
\label{thm:bishop-located-oscillation-modulus-window-route}
Every Real-facing or Completion-facing consumer of an accepted $\BishopLocatedOscillationModulusUp$ packet must read the finite route
$$
\RealOscillationUp,\ \DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp,\ W,\ T,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
Thus oscillation-to-Cauchy or limit-facing use receives a displayed located modulus over finite windows, not ambient $\RealUp$ completeness, quotient stream equality, selected limits, trichotomy, countable choice, a host supremum, or a detached modulus oracle.
\end{theorem}
\begin{proof}
Start with the accepted carrier of \autoref{def:bishop-located-oscillation-modulus-carrier}. Its only source of oscillation information is $O$, and its only tolerance budget is the dyadic row $D$.

The window route is forced by the readback rows. The stream row $S$ names the inspected finite windows, $Q$ gives regular rational readback for those windows, and $R$ seals the Real-facing endpoint only after those rows are visible.

The modulus row $T$ then reads through the located-window ledger $W$ and cannot bypass it. Applying the NameCert obligations of \autoref{thm:bishop-located-oscillation-modulus-namecert-obligations} shows that every consumer-visible route is exhausted by $O,D,S,Q,R,W,T,H,C,P,N$.

The excluded objects are absent from those coordinates. Therefore an oscillation-to-Cauchy or limit-facing consumer receives exactly the displayed finite-window modulus route.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopLocatedOscillationModulusUp$ consumer read factors through RealOscillation, DyadicRatCore, StreamName, RegSeqRat, RealUp, located-window, tolerance-to-window modulus, hsame, Cont, Pkg, and NameCert rows.}

\independenceWitness{real-oscillation, dyadicratcore, streamname, regseqrat, real: $\BishopLocatedOscillationModulusUp$ is not bijective to any listed sibling because it jointly carries an oscillation source row, dyadic tolerance budget, finite observation windows, regular rational readback, Real seal, located-window ledger, tolerance-to-window modulus row, transport, replay, provenance, and local naming.}

\closureat{BishopLocatedOscillationModulusUp}{seedStr}

\begin{closurestatus}{\BishopLocatedOscillationModulusUp}
  \constructivestory{A $\BishopLocatedOscillationModulusUp$ packet is generated from a RealOscillation source row, a DyadicRatCore tolerance ledger, StreamName finite windows, RegSeqRat readback, a RealUp seal, a located-window ledger, a tolerance-to-window modulus row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-located-oscillation-modulus-carrier}, \autoref{thm:bishop-located-oscillation-modulus-namecert-obligations}, and \autoref{thm:bishop-located-oscillation-modulus-window-route} seed the finite located oscillation-modulus route over RealOscillation, DyadicRatCore, StreamName, RegSeqRat, and RealUp rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim ambient $\RealUp$ completeness, quotient stream equality, selected limits, trichotomy, countable choice, host suprema, detached modulus oracles, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, oscillation-source exposure, dyadic-window coverage, located-modulus exactness, non-escape, transport, replay, provenance, and local naming for BEDC.Derived.BishopLocatedOscillationModulusUp.}
\end{closurestatus}
