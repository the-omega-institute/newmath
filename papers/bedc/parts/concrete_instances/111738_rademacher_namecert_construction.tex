\chapter{A Concrete Naming Certificate for $\RademacherUp$}
\label{ch:concrete-instances-rademacher-namecert}
\origin{human}

Following Rademacher's differentiability theorem for Lipschitz maps, the $\RademacherUp$ packet records only the finite dyadic-window derivative-candidate data that a BEDC consumer may inspect before any measure-theoretic almost-everywhere conclusion is visible. It sits over the Lipschitz carrier of \autoref{ch:concrete-instances-lipschitz-function-namecert}, the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the metric-space carrier of \autoref{ch:concrete-instances-metric-namecert}, and the absolute-continuity route of \autoref{ch:concrete-instances-absolute-continuous-function-namecert}. The packet names the finite frontier at which difference quotients, local Lipschitz bounds, dyadic radii, metric neighborhoods, and absolute-continuity control are displayed; it does not claim Lebesgue almost-everywhere differentiability, a measurable exceptional set, a selected derivative, an ambient measure algebra, or completion of a derivative space.

\begin{definition}[Rademacher carrier]
\label{def:rademacher-carrier}
A $\RademacherUp$ carrier is a finite $\BHist$ packet
$$
R=(L,E,D,M,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the $\LipschitzFunctionUp$ row, exposing the source and target metric rows, graph ledger, Lipschitz bound, and distance-inequality route inspected by the packet.
\item $E$ is the $\RealUp$ seal row for the base point, output windows, and finite difference-quotient values read by downstream Real-facing consumers.
\item $D$ is the $\DyadicRatCoreUp$ endpoint-radius and tolerance ledger. It records the dyadic radii, halved-radius refinements, and comparison budgets used to build the displayed derivative-candidate windows.
\item $M$ is the $\MetricSpaceUp$ neighborhood row for the source and target distance requests used by the Lipschitz and difference-quotient checks.
\item $A$ is the $\AbsolutelyContinuousFunctionUp$ control row, recording only the finite interval or curve-window budget that bounds the visible variation consumed by the derivative-candidate packet.
\item $H$ records componentwise $\hsame$ transport for the Lipschitz row, Real seals, dyadic ledger, metric neighborhoods, absolute-continuity control, replay row, provenance row, and local naming row.
\item $C$ records displayed $\Cont$ replay from a Rademacher-facing finite request through $D,M,L,A$, and finally into the terminal Real seal $E$.
\item $P$ is the $\Pkg$ provenance over $\RademacherUp$, $\LipschitzFunctionUp$, $\RealUp$, $\DyadicRatCoreUp$, $\MetricSpaceUp$, $\AbsolutelyContinuousFunctionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RademacherUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,E,D,M,A,H,C,P,N$. It has no coordinate for Lebesgue almost-everywhere differentiability, measurable null sets, host derivative functions, quotient maps, selected pointwise derivatives, countable-choice schedules, ambient measure completion, or completed function spaces.
\end{definition}

\begin{theorem}[Rademacher NameCert obligations]
\label{thm:rademacher-namecert-obligations}
For every accepted $\RademacherUp$ carrier
$$
R=(L,E,D,M,A,H,C,P,N),
$$
the local $\NameCert_{\RademacherUp}$ surface consists exactly of carrier admission, Lipschitz-row exposure, Real seal routing, dyadic endpoint-radius accounting, metric-neighborhood inspection, absolute-continuity control, componentwise transport, displayed replay, provenance, local naming, and non-escape from the displayed rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:rademacher-carrier}. The visible analytic rows are $L,E,D,M,A$, and the visible support rows are $H,C,P,N$.

The derivative-candidate route is finite. The Lipschitz row supplies the graph and bound data, the dyadic ledger supplies the inspected radii and tolerances, and the metric row supplies the neighborhoods in which those finite requests are read.

The Real and absolute-continuity rows do not add a measure theorem. The Real row seals only the displayed numerical windows, while the absolute-continuity row records the finite variation-control budget used by those windows.

Finally $H$ and $C$ transport and replay the same coordinates, and $P,N$ source and name the packet. Since no other coordinate occurs in the carrier, the NameCert surface exports none of the excluded measurable, derivative-selection, quotient, choice, completion, or ambient almost-everywhere data.
\end{proof}
\leanchecked{BEDC.Derived.RademacherUp.TasteGate.RademacherTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Rademacher Lipschitz metric scope]
\label{thm:rademacher-lipschitz-metric-scope}
Every accepted $\RademacherUp$ carrier $R=(L,E,D,M,A,H,C,P,N)$ exposes the $\LipschitzFunctionUp$ row $L$ and the $\MetricSpaceUp$ row $M$ before any derivative-candidate read is admitted. The exposed scope includes the source and target distance requests, the Lipschitz graph ledger, the finite bound comparison, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\RademacherUp}$ row. It does not include a measure algebra, an almost-everywhere differentiability claim, a host derivative, quotient-point equality, or a completed function space.
\end{theorem}
\begin{proof}
Project the accepted carrier through \autoref{def:rademacher-carrier}. The only rows that can supply metric data are the Lipschitz row $L$ and the metric-neighborhood row $M$.

Apply \autoref{thm:rademacher-namecert-obligations}. It lists carrier admission, Lipschitz-row exposure, metric-neighborhood inspection, transport, replay, provenance, and local naming as visible obligations, so every derivative-candidate request must pass through those rows before it reaches any Real-facing seal.

The structural rows $H,C,P,N$ preserve and name the same finite scope. Since the carrier has no coordinate for measure algebras, almost-everywhere assertions, host derivatives, quotient equality, or completed function spaces, none of those objects can be imported through the Lipschitz and metric rows.
\end{proof}
\leanchecked{BEDC.Derived.RademacherUp.RademacherCarrier\_lipschitz\_metric\_scope}

\begin{theorem}[Rademacher dyadic difference-quotient obligation]
\label{thm:rademacher-dyadic-difference-quotient-obligation}
Every accepted $\RademacherUp$ carrier exposes its difference-quotient windows through the $\DyadicRatCoreUp$ row $D$ before any $\RegSeqRatUp$ or $\RealUp$ readback is consumed. The displayed window records endpoint radii, halved-radius refinements, dyadic comparison budgets, the matching Lipschitz and metric rows, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local naming. No consumer may replace that finite window by an unbounded radius schedule, a selected derivative function, a measurable exceptional set, or host real equality.
\end{theorem}
\begin{proof}
Start with the dyadic row $D$ in \autoref{def:rademacher-carrier}. It records the endpoint-radius and tolerance ledger used to build the finite derivative-candidate windows.

Use \autoref{thm:rademacher-namecert-obligations}. The obligation surface makes dyadic endpoint-radius accounting visible alongside Lipschitz-row exposure, metric-neighborhood inspection, Real seal routing, transport, replay, provenance, and local naming.

The finite-window induction is over the displayed dyadic ledger: each inspected radius and halved-radius refinement is replayed through $C$ and transported through $H$ before the terminal readback row is used. The provenance row $P$ and local certificate $N$ keep the same finite request attached to $\BHist$.

Because no other row appears in the carrier, a consumer cannot obtain an unbounded schedule, selected derivative, exceptional-set algebra, or host real equality from the difference-quotient window.
\end{proof}
\leanchecked{BEDC.Derived.RademacherUp.RademacherCarrier\_dyadic\_difference\_quotient\_obligation}

\begin{theorem}[Rademacher Real derivative handoff]
\label{thm:rademacher-real-derivative-handoff}
Every Real-facing derivative-candidate read of an accepted $\RademacherUp$ carrier reaches $\RegSeqRatUp$ or $\RealUp$ only after the finite route through $D,M,L,A,H,C,P,N$ has been displayed. The handoff gives downstream consumers regular rational and Real readback for the visible derivative-candidate window, not a Lebesgue almost-everywhere theorem, not a selected derivative, not quotient equality, and not an ambient completeness statement.
\end{theorem}
\begin{proof}
Project the carrier as $R=(L,E,D,M,A,H,C,P,N)$. The terminal Real seal row is $E$, and the rows feeding it are the dyadic ledger $D$, metric row $M$, Lipschitz row $L$, and absolute-continuity control row $A$.

Apply the NameCert obligations from \autoref{thm:rademacher-namecert-obligations}. They force each Real-facing request to expose carrier admission, Real seal routing, dyadic accounting, metric inspection, Lipschitz exposure, absolute-continuity control, and the structural rows.

The readback route is finite. Replay through $C$ and transport through $H$ carry the displayed derivative-candidate window to $P$ and $N$, so the $\RegSeqRatUp$ or $\RealUp$ consumer receives only the named window data.

Thus the handoff is a regular rational and Real readback of a displayed finite packet. It does not assert an almost-everywhere differentiability theorem, select a derivative outside the inspected windows, identify quotient representatives, or complete an ambient derivative space.
\end{proof}
\leanchecked{BEDC.Derived.RademacherUp.RademacherCarrier\_real\_derivative\_handoff}

\begin{theorem}[Rademacher finite derivative-candidate packet]
\label{thm:rademacher-finite-derivative-candidate-packet}
Every Real-facing read exported by an accepted $\RademacherUp$ carrier factors through the finite route
$$
D,M,L,A \longmapsto E
$$
before any derivative-candidate value is consumed. The displayed packet records dyadic radii, metric neighborhoods, Lipschitz distance bounds, absolute-continuity variation control, and terminal Real seals, but it does not assert that the derivative candidate exists on a Lebesgue almost-everywhere set or that the candidate is selected outside the displayed finite windows.
\end{theorem}
\begin{proof}
Begin with an accepted carrier $R=(L,E,D,M,A,H,C,P,N)$. By \autoref{thm:rademacher-namecert-obligations}, the only Real-facing row is $E$, and it is downstream of the dyadic, metric, Lipschitz, and absolute-continuity rows.

The dyadic and metric rows are indispensable because the finite difference-quotient request needs displayed endpoint radii and the source and target neighborhoods in which the Lipschitz comparison is read. The Lipschitz row is indispensable because it supplies the graph and bound ledger for the same finite request.

The absolute-continuity row records the finite variation-control budget attached to the inspected windows. It is not a measure-completion row and does not select derivatives outside those windows.

Transport, replay, provenance, and local naming preserve the same route. Thus every Real-facing derivative-candidate read reaches the seal only through $D,M,L,A$, with no Lebesgue almost-everywhere theorem, exceptional-set algebra, selected derivative, quotient stream, countable-choice schedule, or ambient completeness coordinate added.
\end{proof}
\leanchecked{BEDC.Derived.RademacherUp.RademacherCarrier\_finite\_derivative\_candidate\_packet}

\begin{theorem}[Rademacher difference-window obligation]
\label{thm:rademacher-difference-window-obligation}
Every accepted $\RademacherUp$ carrier exposes derivative-candidate reads only through the finite dyadic-radius row $D$, metric-neighborhood row $M$, Lipschitz-bound row $L$, absolute-continuity control row $A$, terminal Real-seal row $E$, and the structural rows $H,C,P,N$ already listed in \autoref{def:rademacher-carrier}. No derivative-candidate read bypasses those rows through a host derivative, a measurable exceptional set, a quotient representative, or an unbounded radius schedule.
\end{theorem}
\begin{proof}
Project the accepted carrier as $R=(L,E,D,M,A,H,C,P,N)$. The carrier definition lists the Lipschitz row, Real seal, dyadic ledger, metric neighborhoods, absolute-continuity control, componentwise transport, replay, provenance, and local naming as the only visible coordinates.

Apply \autoref{thm:rademacher-namecert-obligations}. It makes the local $\NameCert_{\RademacherUp}$ surface exactly those rows, so a derivative-candidate request has no admissible public coordinate outside $L,E,D,M,A,H,C,P,N$.

Finally use \autoref{thm:rademacher-finite-derivative-candidate-packet}. The Real-facing endpoint of the route is reached only after $D,M,L,A$ have supplied the displayed dyadic radii, metric neighborhoods, Lipschitz bounds, and finite variation budget. Thus every candidate read remains inside the finite window and cannot import any excluded host, quotient, measurable, or unbounded schedule datum.
\end{proof}
\leanchecked{BEDC.Derived.RademacherUp.RademacherCarrier\_difference\_window\_obligation}

\begin{theorem}[Rademacher difference-quotient window stability]
\label{thm:rademacher-difference-quotient-window-stability}
Let
$$
R=(L,E,D,M,A,H,C,P,N)
$$
and
$$
R'=(L',E',D',M',A',H',C',P',N')
$$
be accepted $\RademacherUp$ carriers. If $D,D'$, $M,M'$, $L,L'$, $A,A'$, and $E,E'$ are componentwise $\hsame$ as dyadic endpoint-radius ledgers, metric-neighborhood rows, Lipschitz-bound rows, absolute-continuity control rows, and Real seal rows, then any finite derivative-candidate read exported through $H,C,P,N$ and through $H',C',P',N'$ remains the same displayed difference-quotient window.
\end{theorem}
\leanchecked{BEDC.Derived.RademacherUp.RademacherCarrier\_difference\_quotient\_window\_stability}
\begin{proof}
Project the two accepted carriers. The only rows that can feed a derivative-candidate read are $D,M,L,A$ before the terminal Real seal $E$, together with the structural rows $H,C,P,N$.

Use \autoref{thm:rademacher-namecert-obligations} and \autoref{thm:rademacher-finite-derivative-candidate-packet}. These force every finite read through the dyadic radius ledger, the metric neighborhood request, the Lipschitz-bound ledger, and the absolute-continuity control before any Real-facing value is sealed.

Now apply the componentwise $\hsame$ assumptions. Since the two packets display the same $D,M,L,A$ rows and the same $E$ seal row, replay through $C$ and $C'$ follows the same finite row inventory. Transport through $H$ and $H'$ preserves that inventory, and $P,N$ and $P',N'$ only source and name the displayed packets.

Therefore the exported difference-quotient window is the same displayed window. The theorem asserts no Lebesgue almost-everywhere differentiability, measurable exceptional set, selected derivative, quotient map, countable choice, ambient measure completion, or completed derivative-space equality.
\end{proof}

\begin{theorem}[Rademacher absolute-continuity boundary]
\label{thm:rademacher-absolute-continuity-boundary}
For every accepted $\RademacherUp$ carrier $R=(L,E,D,M,A,H,C,P,N)$, the absolute-continuity row $A$ may bound the displayed finite variation window used by the difference-quotient route, but it cannot be read as a measurable exceptional-set row, a Lebesgue almost-everywhere derivative theorem, a selected derivative function, a Lebesgue algebra, quotient equality, countable choice, or host real equality.
\end{theorem}
\begin{proof}
Begin with the row inventory of \autoref{def:rademacher-carrier}. Row $A$ is the absolute-continuity control row, and the definition assigns it only the finite interval or curve-window budget consumed by the displayed derivative-candidate packet.

The NameCert obligations keep that interpretation fixed. By \autoref{thm:rademacher-namecert-obligations}, $A$ is one of the exposed analytic rows, while $H,C,P,N$ transport, replay, source, and name the same packet without adding coordinates.

Now apply \autoref{thm:rademacher-finite-derivative-candidate-packet} and \autoref{thm:rademacher-difference-window-obligation}. Both routes place $A$ before the terminal Real seal and inside the finite dyadic and metric window, so it bounds the visible variation request only. Since the carrier contains no measurable exceptional-set row, a.e. derivative row, selected derivative function, Lebesgue algebra, quotient-equality row, choice row, or host equality row, none can be read from $A$.
\end{proof}
\leanchecked{BEDC.Derived.RademacherUp.RademacherCarrier\_absolute\_continuity\_boundary}

\begin{theorem}[Rademacher finite derivative-candidate kernel scope]
\label{thm:rademacher-kernel-scope}
Every accepted $\RademacherUp$ packet exposes its Lipschitz row $L$, metric-neighborhood row $M$, dyadic difference-quotient row $D$, $\RegSeqRatUp$ or $\RealUp$ derivative handoff row $E$, and absolute-continuity boundary row $A$ before any derivative claim is read. The scoped kernel is exactly the displayed carrier route
$$
L,D,M,A \longmapsto E \longmapsto H,C,P,N,
$$
with the finite dyadic windows, metric requests, Lipschitz comparisons, Real-facing readback, absolute-continuity variation budget, componentwise transport, replay, provenance, and local naming all visible in the packet. It exports no Lebesgue almost-everywhere theorem, selected derivative, measurable exceptional set, quotient map, countable-choice schedule, host real equality, or completed derivative space.
\end{theorem}
\begin{proof}
Start from the accepted carrier $R=(L,E,D,M,A,H,C,P,N)$ of \autoref{def:rademacher-carrier}. The carrier projection exposes the only rows available to a derivative-candidate consumer.

Apply the Lipschitz and metric scope theorem \autoref{thm:rademacher-lipschitz-metric-scope}. It places $L$ and $M$ before any derivative-candidate read and keeps the metric requests inside the displayed NameCert route.

Next apply the dyadic difference-quotient obligation \autoref{thm:rademacher-dyadic-difference-quotient-obligation}. It forces the finite endpoint-radius ledger, halved-radius refinements, and comparison budgets through $D$ before any regular rational or Real readback is consumed.

Use the Real derivative handoff \autoref{thm:rademacher-real-derivative-handoff} and the absolute-continuity boundary \autoref{thm:rademacher-absolute-continuity-boundary}. These place the Real-facing derivative-candidate seal downstream of $D,M,L,A$ and restrict $A$ to finite variation-window control.

Finally replay the structural rows $H,C,P,N$. The same route is the paper-side counterpart of the existing Lean namespace around \texttt{BEDC.Derived.RademacherUp.RademacherCarrier\_lipschitz\_metric\_scope}, together with its dyadic, Real-handoff, and absolute-continuity neighbors. Since no carrier coordinate stores the excluded measurable, quotient, selected-derivative, choice, host-equality, or completion data, no derivative claim can be read before the scoped kernel rows are displayed.
\end{proof}
\leanchecked{BEDC.Derived.RademacherUp.RademacherCarrier\_kernel\_scope}

\closureat{RademacherUp}{obligationStr}

\begin{closurestatus}{\RademacherUp}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:rademacher-carrier}, \autoref{thm:rademacher-namecert-obligations}, \autoref{thm:rademacher-finite-derivative-candidate-packet}, \autoref{thm:rademacher-difference-window-obligation}, \autoref{thm:rademacher-difference-quotient-window-stability}, and \autoref{thm:rademacher-absolute-continuity-boundary} close the finite derivative-candidate obligation surface over Lipschitz, Real, DyadicRatCore, MetricSpace, absolute-continuity, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lebesgue almost-everywhere differentiability, measurable exceptional sets, selected derivatives, quotient maps, countable choice, ambient measure completion, completed derivative spaces, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Lipschitz-row exposure, Real seal routing, dyadic endpoint-radius accounting, metric-neighborhood inspection, absolute-continuity control, transport stability, replay stability, provenance, local naming, and non-escape for BEDC.Derived.RademacherUp.}
  \constructivestory{A $\RademacherUp$ packet is generated from a LipschitzFunction row, RealUp seal row, DyadicRatCore endpoint-radius ledger, MetricSpace neighborhood row, AbsolutelyContinuousFunction control row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
