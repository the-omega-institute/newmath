\chapter{A Concrete Naming Certificate for $\McNaughtonPiecewiseLinearUp$}
\label{ch:concrete-instances-mcnaughton-piecewise-linear-namecert}
\origin{human}

Following McNaughton and standard finite MV-algebra representation theory, the $\McNaughtonPiecewiseLinearUp$ packet records a finite rational polyhedral presentation of an integer-slope piecewise-linear map before any real-valued or algebraic consumer reads it. It sits beside the finite interval and partition routes of \autoref{ch:concrete-instances-rational-interval-hull-namecert} and \autoref{ch:concrete-instances-finite-cover-mesh-namecert}, the rational ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream window of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names finite-cell McNaughton data only; it does not assert an ambient real-function extensionality theorem, an infinite subdivision search, a hidden fan, or quotient equality of completed functions.

\begin{definition}[McNaughton piecewise-linear carrier]
\label{def:mcnaughton-piecewise-linear-carrier}
A $\McNaughtonPiecewiseLinearUp$ carrier is a finite $\BHist$ packet
$$
M=(C,A,R,D,S,Q,E,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $C$ is a finite rational polyhedral cell list, with each cell described by finitely many rational half-space inequalities and with overlaps recorded only through the displayed shared-refinement ledger.
\item $A$ assigns to each cell an integer-slope affine expression with rational intercept data.
\item $R$ is the shared refinement ledger, recording the common subdivision on which adjacent cell expressions agree on the displayed boundary faces.
\item $D$ is the $\DyadicRatCoreUp$ and $\RatUp$ coefficient readback row for rational vertices, half-space constants, slopes, intercepts, and boundary comparisons.
\item $S$ is the $\StreamNameUp$ finite inspection row for requested cells, faces, and coefficient windows.
\item $Q$ is the $\RegSeqRatUp$ regular rational readback row attached to the same windows.
\item $E$ is the $\RealUp$ seal row reached only after $C,A,R,D,S,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for cell lists, affine pieces, shared-refinement rows, rational readbacks, stream windows, regular rational tails, and the real seal.
\item $T$ records displayed $\Cont$ replay from the finite cell table through rational readback and regular-tail rows to the real-facing seal.
\item $P$ is the $\Pkg$ provenance over $\McNaughtonPiecewiseLinearUp$, $\DyadicRatCoreUp$, $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\McNaughtonPiecewiseLinearUp}$ row.
\end{enumerate}
The carrier has no coordinate for an infinite partition, hidden real-function equality, externally chosen refinement, quotient completed-function equality, non-rational coefficient oracle, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[McNaughton piecewise-linear NameCert obligations]
\label{thm:mcnaughton-piecewise-linear-namecert-obligations}
For every accepted $\McNaughtonPiecewiseLinearUp$ carrier $M=(C,A,R,D,S,Q,E,H,T,P,N)$, the local $\NameCert_{\McNaughtonPiecewiseLinearUp}$ surface consists exactly of finite cell admission, integer-slope affine-piece admission, shared-refinement agreement, rational coefficient readback, finite-window exposure, regular rational tail readback, real-seal nonescape, componentwise transport, displayed replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:mcnaughton-piecewise-linear-carrier}. The rows $C,A,R$ are the finite McNaughton table: cells, affine expressions, and the common refinement on which boundary agreement is witnessed.

Next read $D,S,Q,E$. Rational coefficient data is exposed through $\DyadicRatCoreUp$ and $\RatUp$, finite requests pass through $\StreamNameUp$, regular rational readback is supplied by $\RegSeqRatUp$, and the $\RealUp$ seal is available only after those rows have been displayed.

Finally inspect $H,T,P,N$. Transport, replay, provenance, and local naming preserve the same finite row inventory. Since no coordinate remains, an accepted certificate cannot add an infinite subdivision, a hidden real-function equality, or an ambient completeness claim.
\end{proof}

\begin{theorem}[McNaughton finite-cell handoff]
\label{thm:mcnaughton-piecewise-linear-finite-cell-handoff}
Every $\RealUp$-facing or $\RatUp$-facing consumer of an accepted $\McNaughtonPiecewiseLinearUp$ carrier must factor through the finite route
$$
C,A,R \longmapsto \DyadicRatCoreUp,\RatUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
using the displayed rows $D,S,Q,E,H,T,P,N$. The handoff exports the finite-cell piecewise-linear table, not host extensional equality of real functions, an infinite refinement procedure, quotient completed-function equality, or a hidden coefficient oracle.
\end{theorem}
\begin{proof}
Begin with the finite cell and affine-piece rows. The shared-refinement ledger $R$ is part of the same packet as $C$ and $A$, so a consumer cannot read an affine expression independently of the cell subdivision on which it is certified.

The rational readback row $D$ then exposes vertices, inequalities, slopes, intercepts, and boundary comparisons through $\DyadicRatCoreUp$ and $\RatUp$. The window row $S$ and regular readback row $Q$ make the same finite data available to stream and regular-rational consumers before the terminal real seal $E$ is reached.

The structural rows $H,T,P,N$ replay and name exactly this route. Therefore every accepted handoff is finite-cell data transported through rational and regular readback rows, with no additional real-function extensionality, infinite search, or hidden refinement surface.
\end{proof}

\closureat{McNaughtonPiecewiseLinearUp}{seedStr}

The Lean follow-up is expected under \texttt{BEDC.Derived.McNaughtonPiecewiseLinearUp}; it must provide explicit finite-cell carrier admission, shared-refinement agreement, and nontrivial field-faithful TasteGate rows before any checked marker is cited.

\begin{closurestatus}{\McNaughtonPiecewiseLinearUp}
  \constructivestory{A $\McNaughtonPiecewiseLinearUp$ packet is generated from finite rational polyhedral cells, integer-slope affine pieces, shared-refinement agreement, DyadicRatCore and Rat readback, StreamName windows, RegSeqRat tails, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:mcnaughton-piecewise-linear-carrier}, \autoref{thm:mcnaughton-piecewise-linear-namecert-obligations}, and \autoref{thm:mcnaughton-piecewise-linear-finite-cell-handoff} seed the finite-cell McNaughton packet over rational polyhedral cells, integer-slope affine pieces, shared refinement, rational readback, stream windows, regular rational tails, real seal, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient real-function extensionality, infinite subdivision search, hidden fan construction, quotient completed-function equality, non-rational coefficient oracles, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked finite-cell admission, integer-slope affine-piece admission, shared-refinement agreement, rational coefficient readback, finite-window exposure, regular rational tail readback, real-seal nonescape, transport, replay, provenance, and local naming.}
\end{closurestatus}
