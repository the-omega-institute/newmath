\chapter{A Concrete Naming Certificate for $\RealIntervalUp$}
\label{ch:concrete-instances-realinterval-namecert}
\origin{human}

Following Bishop and Bridges, the $\RealIntervalUp$ packet records displayed real interval endpoints and finite enclosure rows for the L10 Real and Completion route. It sits after the located interval surface of \autoref{ch:concrete-instances-locatedinterval-namecert}, the rational interval surface of \autoref{ch:concrete-instances-rationalinterval-namecert}, and the dyadic-to-real spine of \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The chapter gives interval-completion and real-enclosure consumers one named endpoint-and-enclosure packet; it is not a host closed interval, selected endpoint representatives, trichotomy, compactness, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[RealInterval carrier]
\label{def:real-interval-carrier}
A $\RealIntervalUp$ carrier is a finite $\BHist$ packet
$$
I=(L,U,E,D,W,R,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the lower endpoint $\RealUp$ row, read through the displayed $\StreamNameUp$ and $\RegSeqRatUp$ seal discipline.
\item $U$ is the upper endpoint $\RealUp$ row, read through the same finite real-name seal discipline.
\item $E$ is the enclosure and order row, binding the lower endpoint, upper endpoint, rational interval enclosure, and endpoint-order evidence without replacing the endpoints by host representatives.
\item $D$ is the dyadic tolerance row supplied by $\DyadicRatCoreUp$; it records positive precision requests, outward rounding budgets, and finite endpoint comparison tolerances.
\item $W$ is the $\StreamNameUp$ window row for the two endpoint streams and the interval enclosure reads visible at the requested tolerance.
\item $R$ is the $\RegSeqRatUp$ readback row that turns the endpoint windows and dyadic tolerance budget into regular rational observations.
\item $S$ is the terminal $\RealUp$ seal row for endpoint and enclosure consumers; every Real-facing output must pass through this row after $D,W,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for endpoints, enclosure and order data, dyadic tolerance, windows, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from interval membership, enclosure, refinement, or completion-facing requests through $L,U,E,D,W,R,S,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RealIntervalUp$, $\LocatedIntervalUp$, $\RationalIntervalUp$, $\RealIntervalEnclosureUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealIntervalUp}$ row.
\end{enumerate}
The carrier predicate asserts exactly that these rows form one displayed real interval endpoint-and-enclosure history. It has no coordinate for host interval equality, selected quotient representatives, real-order trichotomy, countable choice, compactness, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.RealIntervalUp.RealIntervalTasteGate\_single\_carrier\_alignment}

\begin{theorem}[RealInterval NameCert obligations]
\label{thm:real-interval-namecert-obligations}
For every accepted $\RealIntervalUp$ carrier $I=(L,U,E,D,W,R,S,H,C,P,N)$, the local $\NameCert_{\RealIntervalUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the lower endpoint, upper endpoint, enclosure and order row, dyadic tolerance row, stream windows, regular rational readback, Real seal, transport, replay, provenance, and local name;
\item endpoint order, requiring $E$ to bind $L$ and $U$ before any interval read can be treated as an enclosure;
\item enclosure stability, requiring every containment, refinement, and endpoint-bound read to pass through $E$ together with the displayed tolerance and window rows;
\item dyadic tolerance handoff, requiring $D$ to precede the $\StreamNameUp$ windows $W$ and $\RegSeqRatUp$ readback $R$;
\item Real seal non-escape, requiring every Real-facing endpoint or enclosure output to reach $S$ only after $L,U,E,D,W,R$ have been displayed;
\item ledger refusal, excluding host interval equality, selected endpoint representatives, real-order trichotomy, countable choice, compactness, ambient $\RealUp$ completeness, Lean verification, and bridge checking.
\end{enumerate}
No real interval obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:real-interval-carrier}. The projection exposes exactly the two endpoint rows, enclosure and order row, dyadic tolerance row, stream windows, regular rational readback, Real seal, and structural rows.

Endpoint order and enclosure stability are read from $E$. That row is the only coordinate joining $L$ and $U$ into an interval boundary, so containment, refinement, and endpoint-bound requests cannot bypass it.

The dyadic tolerance handoff is the route $D\mapsto W\mapsto R$. A positive precision request first enters the dyadic budget, then selects finite stream windows, and only then produces regular rational readback.

The Real seal row $S$ is downstream of that route. The remaining rows $H,C,P,N$ transport, replay, package, and name the same endpoint-and-enclosure history, while the excluded host interval, choice, compactness, and completeness objects have no coordinate in the carrier.
\end{proof}
\leanchecked{BEDC.Derived.RealIntervalUp.RealIntervalCarrier\_namecert\_obligations}

\begin{theorem}[RealInterval seal enclosure route]
\label{thm:real-interval-seal-enclosure-route}
Every endpoint, enclosure, refinement, or completion-facing read of an accepted $\RealIntervalUp$ carrier reaches the $\RealUp$ seal only through the displayed route
$$
L,U,E \longmapsto D,W,R \longmapsto S,H,C,P,N .
$$
Consequently every interval consumer receives endpoint order, finite enclosure data, dyadic tolerance, stream windows, and regular rational readback before any Real-facing seal is visible. The route exports no host interval equality, selected endpoint representative, real-order trichotomy, countable-choice schedule, compactness theorem, hidden complete space, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with an accepted carrier $I=(L,U,E,D,W,R,S,H,C,P,N)$. The endpoint rows $L$ and $U$ are the only Real endpoints in the packet, and $E$ is the only row that binds them as an ordered enclosure.

Read the dyadic tolerance handoff next. The row $D$ supplies the finite precision and outward-rounding budget, $W$ exposes the endpoint stream windows visible at that budget, and $R$ turns those windows into regular rational readback.

Only after this handoff may the Real-facing seal $S$ be consumed. Componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row preserve the same route. Since none of the excluded host interval or completeness objects is a carrier coordinate, every interval read reaches the seal only through the displayed rows.
\end{proof}
\leanchecked{BEDC.Derived.RealIntervalUp.RealIntervalSealEnclosureRoute}

\begin{theorem}[RealInterval endpoint-order obligation]
\label{thm:real-interval-endpoint-order-obligation}
Let $I=(L,U,E,D,W,R,S,H,C,P,N)$ be an accepted $\RealIntervalUp$ carrier. Any consumer that treats $L$ and $U$ as an interval must first read the endpoint-order row $E$; the two endpoint rows alone do not authorize an interval enclosure.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-interval-carrier}. The rows $L$ and $U$ name the two endpoint Real rows, while $E$ is the only displayed row that binds them as an ordered enclosure.

Now apply the local obligations of \autoref{thm:real-interval-namecert-obligations}. The endpoint-order obligation explicitly requires $E$ to bind $L$ and $U$ before any interval read can be treated as an enclosure.

The seal route of \autoref{thm:real-interval-seal-enclosure-route} preserves the same order, since every endpoint or enclosure read begins with $L,U,E$ before entering the dyadic handoff. Thus an interval consumer cannot bypass $E$ by reading the endpoint rows alone.
\end{proof}
\leanchecked{BEDC.Derived.RealIntervalUp.RealIntervalEndpointOrderObligation}

\begin{theorem}[RealInterval classifier transport]
\label{thm:real-interval-classifier-transport}
Let $I=(L,U,E,D,W,R,S,H,C,P,N)$ and $I'=(L',U',E',D',W',R',S',H',C',P',N')$ be accepted $\RealIntervalUp$ carriers. If the accepted interval classifier identifies the endpoint and enclosure rows of $I$ with those of $I'$, then carrier admission transports from $I$ to $I'$ only through the displayed endpoint, locatedness, dyadic-window, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. In particular, classifier equality cannot replace the rows supplied by \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-locatedinterval-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, or \autoref{ch:concrete-instances-real-namecert}.
\end{theorem}
\begin{proof}
Project both carriers through \autoref{def:real-interval-carrier}. The endpoint rows $L,U,L',U'$ and enclosure rows $E,E'$ are the only rows on which the interval classifier can act.

Use the endpoint-order obligation \autoref{thm:real-interval-endpoint-order-obligation}. It forces every interval read to keep the endpoint order row visible, so classifier equality must transport the ordered enclosure rather than only the two endpoints.

The seal route of \autoref{thm:real-interval-seal-enclosure-route} then carries the transported enclosure through the dyadic tolerance, stream-window, regular-rational, and Real-seal rows. The structural rows $H',C',P',N'$ name the transported packet, so carrier admission moves only along the displayed BEDC rows.
\end{proof}
\leanchecked{BEDC.Derived.RealIntervalUp.RealIntervalClassifierTransport}

\begin{theorem}[RealInterval endpoint refinement stability]
\label{thm:real-interval-endpoint-refinement-stability}
For every accepted $\RealIntervalUp$ carrier $I=(L,U,E,D,W,R,S,H,C,P,N)$, any dyadic refinement of the endpoint enclosure that preserves the displayed endpoint order and locatedness rows preserves the $\RealIntervalUp$ NameCert surface before the terminal $\RealUp$ seal is read. The refinement may change the finite dyadic tolerance and window rows $D,W,R$, but it must keep the endpoint, locatedness, transport, replay, provenance, and local naming rows visible before $S$ is consumed.
\end{theorem}
\leanchecked{BEDC.Derived.RealIntervalUp.RealIntervalEndpointRefinementStability}
\begin{proof}
Read the carrier rows of \autoref{def:real-interval-carrier}. A dyadic endpoint refinement changes only the tolerance, window, and regular-rational observations recorded in $D,W,R$.

Apply \autoref{thm:real-interval-namecert-obligations}. Its endpoint order and enclosure stability clauses keep $L,U,E$ visible, while its dyadic tolerance handoff clause keeps every refinement inside the $D\mapsto W\mapsto R$ route.

Finally use the seal enclosure route \autoref{thm:real-interval-seal-enclosure-route}. Since $S$ is terminal, the refined endpoint surface must already have passed through $\DyadicRatCoreUp$, $\LocatedIntervalUp$, $\StreamNameUp$, $\RegSeqRatUp$, and the displayed structural rows before any $\RealUp$ seal is available.
\end{proof}

\begin{theorem}[RealInterval dyadic-window handoff]
\label{thm:real-interval-dyadic-window-handoff}
For every accepted $\RealIntervalUp$ carrier $I=(L,U,E,D,W,R,S,H,C,P,N)$, the displayed route
$$
D \longmapsto W \longmapsto R
$$
is the only tolerance-to-readback handoff supplied by the packet.
\end{theorem}
\begin{proof}
Project \autoref{def:real-interval-carrier}. The tolerance coordinate is $D$, the finite endpoint-window coordinate is $W$, and the regular rational readback coordinate is $R$.

Use \autoref{thm:real-interval-namecert-obligations}. Its dyadic tolerance handoff row requires $D$ to precede the $\StreamNameUp$ windows $W$ and the $\RegSeqRatUp$ readback $R$.

Finally apply \autoref{thm:real-interval-seal-enclosure-route}. The route displays no second tolerance-to-readback path; every enclosure read moves from $D$ to $W$ to $R$ before any seal or structural row is public.
\end{proof}
\leanchecked{BEDC.Derived.RealIntervalUp.RealIntervalDyadicWindowHandoff}

\begin{theorem}[RealInterval Real-seal nonescape]
\label{thm:real-interval-real-seal-nonescape}
Let $I=(L,U,E,D,W,R,S,H,C,P,N)$ be an accepted $\RealIntervalUp$ carrier. Every Real-facing enclosure read reaches the seal row $S$ only after the endpoint rows, endpoint-order row, dyadic tolerance row, stream-window row, and regular readback row $L,U,E,D,W,R$ have all been displayed.
\end{theorem}
\begin{proof}
Start from the carrier of \autoref{def:real-interval-carrier}. The seal row $S$ is not an independent endpoint; it is the terminal Real-facing row for endpoint and enclosure consumers.

The obligations of \autoref{thm:real-interval-namecert-obligations} make Real seal non-escape explicit. They require every Real-facing endpoint or enclosure output to reach $S$ only after $L,U,E,D,W,R$ have been displayed.

The route theorem \autoref{thm:real-interval-seal-enclosure-route} gives the same ordering as a displayed path. Therefore a Real-facing enclosure read cannot reach $S$ while omitting any of the endpoint, order, tolerance, window, or readback rows.
\end{proof}
\leanchecked{BEDC.Derived.RealIntervalUp.RealIntervalRealSealNonescape}

\begin{theorem}[RealInterval obligation-closure route]
\label{thm:real-interval-obligation-closure-route}
Every accepted $\RealIntervalUp$ carrier $I=(L,U,E,D,W,R,S,H,C,P,N)$ routes endpoint admission, endpoint order, enclosure stability, dyadic-window handoff, Real-seal nonescape, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ through the displayed rows $L,U,E,D,W,R,S,H,C,P,N$. The route supplies no host interval equality, selected endpoint representative, trichotomy, countable choice, compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RealIntervalUp.RealIntervalCarrier\_obligation\_closure\_route}
\begin{proof}
First read the local obligation surface from \autoref{thm:real-interval-namecert-obligations}. It exposes carrier admission, endpoint order, enclosure stability, dyadic tolerance handoff, Real seal non-escape, transport, replay, provenance, and local naming as finite rows of the accepted carrier.

Next use the route theorems that keep these rows in order. The seal enclosure route of \autoref{thm:real-interval-seal-enclosure-route}, the endpoint-order obligation of \autoref{thm:real-interval-endpoint-order-obligation}, and the dyadic-window handoff of \autoref{thm:real-interval-dyadic-window-handoff} force every interval read through $L,U,E,D,W,R,S$ before the structural rows are consumed.

Finally apply Real-seal nonescape from \autoref{thm:real-interval-real-seal-nonescape}. Since every Real-facing enclosure read reaches $S$ only after $L,U,E,D,W,R$ have been displayed, and since $H,C,P,N$ only transport, replay, package, and name that same route, the carrier closes exactly the obligation route stated above while leaving the refused global principles outside the packet.
\end{proof}

\begin{theorem}[RealInterval public NameCert export]
\label{thm:realinterval-public-namecert}
The public $\RealIntervalUp$ export is exactly the endpoint-pair carrier together with endpoint order, classifier transport, endpoint-refinement stability, dyadic-window handoff, and Real-seal nonescape rows. A public interval consumer may cite the ordered surface
$$
L,U,E,D,W,R,S,H,C,P,N
$$
only as the endpoint-and-enclosure packet of \autoref{def:real-interval-carrier}; it may not add host interval equality, selected endpoint representatives, trichotomy, countable choice, compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RealIntervalUp.RealIntervalPublicNameCertExport}
\begin{proof}
Start from the carrier and obligation route. Theorems~\ref{def:real-interval-carrier} and~\ref{thm:real-interval-obligation-closure-route} identify the complete endpoint-and-enclosure packet as the rows $L,U,E,D,W,R,S,H,C,P,N$.

The public surface is determined by the interval-facing route theorems. Endpoint order is supplied by \autoref{thm:real-interval-endpoint-order-obligation}, classifier transport by \autoref{thm:real-interval-classifier-transport}, endpoint-refinement stability by \autoref{thm:real-interval-endpoint-refinement-stability}, dyadic-window handoff by \autoref{thm:real-interval-dyadic-window-handoff}, and Real-seal nonescape by \autoref{thm:real-interval-real-seal-nonescape}.

These theorems cite the same displayed rows and no additional host object. Therefore the reusable public export is precisely the endpoint-pair carrier with its order, transport, refinement, dyadic-window, and seal rows, while the refused global principles remain outside the packet.
\end{proof}

\begin{theorem}[RealInterval finite-net consumer]
\label{thm:real-interval-finite-net-consumer}
Every finite-net or density-net consumer of an accepted $\RealIntervalUp$ packet must read the endpoint order row $E$, dyadic tolerance row $D$, $\StreamNameUp$ windows $W$, $\RegSeqRatUp$ readback row $R$, and terminal $\RealUp$ seal row $S$ before passing interval data into \autoref{ch:concrete-instances-dyadic-interval-net-namecert} or \autoref{ch:concrete-instances-finite-cauchy-density-net-namecert}. The consumer route is the displayed packet
$$
L,U,E,D,W,R,S,H,C,P,N,
$$
not a host interval equality, selected endpoint representative, compactness theorem, countable-choice net, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.RealIntervalUp.RealIntervalFiniteNetConsumer}
\begin{proof}
Start with endpoint order. By \autoref{thm:real-interval-endpoint-order-obligation}, a consumer cannot treat $L$ and $U$ as an interval until it has read the order and enclosure row $E$.

Next apply the dyadic-window handoff of \autoref{thm:real-interval-dyadic-window-handoff}. It forces the requested tolerance through $D$, then the finite StreamName windows $W$, and only then the regular rational readback row $R$ used by dyadic interval cells or finite density rows.

Now use Real-seal nonescape and the public NameCert export, \autoref{thm:real-interval-real-seal-nonescape} and \autoref{thm:realinterval-public-namecert}. The seal row $S$ is available only after $L,U,E,D,W,R$ have been displayed, and the public export keeps $H,C,P,N$ attached to that same endpoint-and-enclosure packet.

Finally route the displayed interval data to the dyadic interval-net and finite density-net sibling chapters. Since \autoref{def:real-interval-carrier} contains no coordinate for host interval equality, selected endpoint representatives, compactness, countable choice, or ambient completeness, those siblings receive only the finite displayed route stated above.
\end{proof}

\begin{theorem}[RealInterval bridged NameCert route]
\label{thm:realinterval-bridged-namecert-route}
Every bridge-facing $\RealIntervalUp$ consumer routes through the displayed endpoint-and-enclosure packet
$$
L,U,E,D,W,R,S,H,C,P,N.
$$
The route first reads the $\DyadicRatCoreUp$ endpoint ledger $D$, then the $\LocatedIntervalUp$ and $\RealIntervalEnclosureUp$ rows carried by $E$, then the $\StreamNameUp$ windows $W$, the $\RegSeqRatUp$ readback $R$, and finally the $\RealUp$ seal $S$ with transport, replay, provenance, and local naming still visible.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:real-interval-carrier}. The endpoint rows, enclosure row, tolerance ledger, windows, readback, seal, and structural rows are all coordinates of one packet.

Use the NameCert obligations and public export, \autoref{thm:real-interval-namecert-obligations} and \autoref{thm:realinterval-public-namecert}. They keep endpoint order, enclosure stability, dyadic-window handoff, Real-seal nonescape, transport, replay, provenance, and local naming attached to the displayed rows.

Finally apply the finite-net consumer route of \autoref{thm:real-interval-finite-net-consumer}. It shows that downstream interval data reaches finite cell and density consumers only through the dyadic, located-enclosure, StreamName, RegSeqRat, and Real-seal route. Hence the bridge-facing NameCert route is the displayed packet above.
\end{proof}
\leanchecked{BEDC.Derived.RealIntervalUp.RealIntervalBridgedNameCertRoute}

\begin{theorem}[RealInterval finite endpoint-envelope route]
\label{thm:realinterval-finite-endpoint-envelope-route}
Every $\RealIntervalUp$ endpoint consumer that exports interval membership must
first factor through the displayed finite endpoint envelope
$$
\DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto
\RegSeqRatUp \longmapsto \RealIntervalEnclosureUp \longmapsto \RealUp .
$$
The dyadic endpoint arithmetic is read from $D$, the finite endpoint windows
from $W$, the regular readback from $R$, the enclosure row from $E$, and the
terminal value seal from $S$ before membership is public. The envelope exports
no quotient stream equality, selected endpoints, host interval equality,
ambient $\RealUp$ completeness, trichotomy, compactness, or countable-choice
schedule.
\end{theorem}
\begin{proof}
Start with the endpoint-order obligation of
\autoref{thm:real-interval-endpoint-order-obligation}. It keeps the enclosure
and order row $E$ visible whenever a consumer treats $L$ and $U$ as interval
endpoints.

Next apply the dyadic-window handoff
\autoref{thm:real-interval-dyadic-window-handoff}. The endpoint arithmetic
enters through the displayed $\DyadicRatCoreUp$ row $D$, then exposes only
finite $\StreamNameUp$ windows $W$, and only then produces the $\RegSeqRatUp$
readback row $R$.

Use the bridged NameCert route
\autoref{thm:realinterval-bridged-namecert-route}. It inserts the
$\LocatedIntervalUp$ and $\RealIntervalEnclosureUp$ rows carried by $E$ between
the endpoint ledger and the terminal $\RealUp$ seal $S$, while $H,C,P,N$
transport, replay, package, and name the same displayed route.

Finally read Real-seal nonescape from
\autoref{thm:real-interval-real-seal-nonescape}. Since $S$ is available only
after $L,U,E,D,W,R$ have been displayed, interval membership cannot be exported
from quotient stream equality, selected endpoints, host interval equality,
ambient completeness, trichotomy, compactness, or countable choice.
\end{proof}

\begin{theorem}[RealInterval endpoint enclosure transport]
\label{thm:realinterval-endpoint-enclosure-transport}
For accepted $\RealIntervalUp$ carriers $I=(L,U,E,D,W,R,S,H,C,P,N)$ and $I'=(L',U',E',D',W',R',S',H',C',P',N')$, componentwise $\hsame$ transport preserves both endpoint enclosure rows before interval membership is public. In particular, a consumer that transports $L$ to $L'$ and $U$ to $U'$ must also transport the enclosure-and-order row $E$ to $E'$ and the dyadic-window-readback route $D,W,R$ to $D',W',R'$ before it may read the terminal seal.
\end{theorem}
\begin{proof}
Start with classifier transport, \autoref{thm:real-interval-classifier-transport}. It allows carrier admission to move only through the displayed endpoint, enclosure, dyadic-window, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Endpoint order then fixes the enclosure boundary. By \autoref{thm:real-interval-endpoint-order-obligation}, the two endpoint rows alone do not authorize interval membership; the row $E$ must remain visible as the ordered enclosure.

Finally use endpoint-refinement stability and the dyadic-window handoff, \autoref{thm:real-interval-endpoint-refinement-stability} and \autoref{thm:real-interval-dyadic-window-handoff}. These rows keep $D,W,R$ attached to the transported enclosure before $S$ is consumed, so componentwise transport preserves both endpoint enclosure rows before membership becomes public.
\end{proof}

\begin{theorem}[RealInterval StreamName Real-seal nonescape]
\label{thm:realinterval-stream-real-seal-nonescape}
No $\RealIntervalUp$ read that passes through $\StreamNameUp$ windows and a $\RealUp$ seal exports open-cover compactness, quotient real equality, choice-selected endpoints, trichotomy, or $\RealUp$ completeness. The only public route is
$$
L,U,E,D,W,R,S,H,C,P,N,
$$
with $W$ and $R$ displayed before $S$ and with the refusal boundary preserved by the local NameCert rows.
\end{theorem}
\begin{proof}
Read the Real-seal nonescape theorem \autoref{thm:real-interval-real-seal-nonescape}. It places the seal row $S$ strictly after the endpoint, order, tolerance, window, and readback rows $L,U,E,D,W,R$.

Use the obligation-closure route and public NameCert export, \autoref{thm:real-interval-obligation-closure-route} and \autoref{thm:realinterval-public-namecert}. They state that $H,C,P,N$ only transport, replay, package, and name the same displayed packet.

The finite-net consumer theorem \autoref{thm:real-interval-finite-net-consumer} keeps downstream interval reads inside the same route. Since none of the carrier coordinates names open-cover compactness, quotient equality, selected endpoints, trichotomy, or ambient completeness, the StreamName-to-Real-seal read cannot export those objects.
\end{proof}

\begin{theorem}[RealInterval bridge scope]
\label{thm:real-interval-bridge-scope}
Every bridge-facing $\RealIntervalUp$ read is exhausted by the displayed rows
$$
L,U,E,D,W,R,S,H,C,P,N
$$
and by the sibling surfaces $\DyadicRatCoreUp$, $\LocatedIntervalUp$, $\RationalIntervalUp$, $\RealIntervalEnclosureUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. The bridge-scope packet exports no host interval equality, selected endpoint representative, trichotomy, countable choice, compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from the public export of \autoref{thm:realinterval-public-namecert}. It binds public interval reads to the endpoint, enclosure, tolerance, window, readback, seal, transport, replay, provenance, and local naming rows of the displayed carrier.

Next use the bridged route and endpoint-enclosure transport, \autoref{thm:realinterval-bridged-namecert-route} and \autoref{thm:realinterval-endpoint-enclosure-transport}. These results keep the bridge-facing read on the $\DyadicRatCoreUp$, $\LocatedIntervalUp$, $\RationalIntervalUp$, $\RealIntervalEnclosureUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ sibling surfaces before any interval membership is public.

Finally apply the StreamName Real-seal nonescape theorem, \autoref{thm:realinterval-stream-real-seal-nonescape}. The seal row $S$ remains downstream of $L,U,E,D,W,R$ and upstream of no additional host principle, so the bridge scope is exactly the displayed packet and none of the excluded objects is exported.
\end{proof}

\closureat{RealIntervalUp}{seedStr}
\closureat{RealIntervalUp}{obligationStr}

\begin{closurestatus}{\RealIntervalUp}
  \theoryclosure{\publicClosure}
  \scopeclosed{\autoref{def:real-interval-carrier}, \autoref{thm:real-interval-namecert-obligations}, \autoref{thm:real-interval-seal-enclosure-route}, \autoref{thm:real-interval-endpoint-order-obligation}, \autoref{thm:real-interval-dyadic-window-handoff}, \autoref{thm:real-interval-real-seal-nonescape}, \autoref{thm:real-interval-obligation-closure-route}, and \autoref{thm:realinterval-public-namecert} bind endpoint admission, endpoint order, enclosure stability, dyadic-window handoff, Real-seal nonescape, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ to the displayed public $\RealIntervalUp$ rows over $\LocatedIntervalUp$, $\RationalIntervalUp$, $\RealIntervalEnclosureUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host interval equality, selected endpoint representatives, trichotomy, countable choice, compactness, ambient RealUp completeness, Lean verification, or a public standard bridge.}
  \upgradepath{Bridged closure requires a standard bridge that transports the public $\RealIntervalUp$ surface into the acceptance schema without adding host interval equality, selected endpoint representatives, trichotomy, countable choice, compactness, or ambient RealUp completeness.}
  \constructivestory{A $\RealIntervalUp$ packet is generated from lower and upper RealUp endpoint rows, an enclosure and order row, a DyadicRatCoreUp tolerance row, StreamNameUp finite windows, RegSeqRatUp readback, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
