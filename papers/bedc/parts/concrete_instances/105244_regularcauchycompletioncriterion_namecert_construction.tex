\chapter{A Concrete Naming Certificate for $\RegularCauchyCompletionCriterionUp$}
\label{ch:concrete-instances-regular-cauchy-completion-criterion-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\RegularCauchyCompletionCriterionUp$ packet records the finite criterion by which a regular rational name is admitted to the completion-facing Real seal. It sits between the $\RegSeqRatUp$ rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite $\StreamNameUp$ windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the modulus schedule of \autoref{ch:concrete-instances-cauchy-convergence-modulus-namecert}, the sequence-limit handoff of \autoref{ch:concrete-instances-real-sequence-limit-namecert}, the completion-admission boundary of \autoref{ch:concrete-instances-cauchycompletioncriterion-namecert}, the functorial completion surface of \autoref{ch:concrete-instances-bishop-cauchy-completion-functor-namecert}, the recursor route of \autoref{ch:concrete-instances-cauchy-completion-recursor-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a displayed NameCert criterion for L10 completion consumers, not a quotient completion, selected representative, countable-choice schedule, host sequence theorem, trichotomy split, or ambient $\RealUp$ completeness assertion.

\begin{theorem}[RegularCauchyCompletionCriterion carrier]
\label{thm:regular-cauchy-completion-criterion-carrier}
A $\RegularCauchyCompletionCriterionUp$ carrier is a finite $\BHist$ packet
$$
K=(R,W,D,M,L,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RegSeqRatUp$ row naming the regular rational readback inspected by the completion criterion.
\item $W$ is the $\StreamNameUp$ finite-window schedule through which the source name is observed.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger recording the requested dyadic radius, slack cells, and comparison budget used by the regular readback.
\item $M$ is the $\CauchyConvergenceModulusUp$ schedule row naming the displayed tail threshold.
\item $L$ is the $\RealSequenceLimitUp$ handoff row reached only after $R,W,D,M$ have been displayed.
\item $Q$ is the $\CauchyCompletionCriterionUp$ boundary row that admits the regular Cauchy route to the completion-facing $\RealUp$ seal.
\item $H$ records componentwise $\hsame$ transport for the regular readback, finite windows, dyadic ledger, modulus schedule, sequence-limit handoff, completion boundary, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from window requests through dyadic tolerance, regular readback, modulus schedule, sequence-limit handoff, and completion boundary.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyCompletionCriterionUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchyConvergenceModulusUp$, $\RealSequenceLimitUp$, $\CauchyCompletionCriterionUp$, $\BishopCauchyCompletionFunctorUp$, $\CauchyCompletionRecursorUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyCompletionCriterionUp}$ row naming the displayed criterion and its permitted completion-facing reads.
\end{enumerate}
The classifier compares accepted packets only through $R,W,D,M,L,Q,H,C,P,N$. It has no coordinate for quotient stream equality, a selected completion representative, countable choice over tail indices, host convergence, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
The carrier is the displayed L10 completion-neighbour packet. The first six rows record the analytic route from regular rational readback through finite StreamName windows, dyadic tolerance, modulus data, sequence-limit handoff, and completion admission.

The remaining rows are structural. They transport, replay, source, and name exactly that route, with the Bishop completion functor and completion recursor recorded only as provenance-facing consumers. No field remains for quotient completion, selected representatives, countable choice, host convergence, trichotomy, host equality, or ambient completeness.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyCompletionCriterionUp.RegularCauchyCompletionCriterionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[RegularCauchyCompletionCriterion NameCert obligations]
\label{thm:regular-cauchy-completion-criterion-classifier}
For every accepted $\RegularCauchyCompletionCriterionUp$ carrier $K=(R,W,D,M,L,Q,H,C,P,N)$, the local $\NameCert_{\RegularCauchyCompletionCriterionUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the displayed $\RegSeqRatUp$ readback, $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance ledger, $\CauchyConvergenceModulusUp$ schedule, $\RealSequenceLimitUp$ handoff, $\CauchyCompletionCriterionUp$ boundary, transport, replay, provenance, and local naming row;
\item finite-window exactness, requiring every regular Cauchy observation to read $W$ before $R$ or $M$ is available;
\item dyadic tolerance exactness, requiring the comparison budget $D$ to be read before the modulus schedule $M$, sequence-limit handoff $L$, or completion boundary $Q$ is exposed;
\item completion-boundary handoff, requiring $Q$ to remain downstream of $R,W,D,M,L$ and upstream only of the structural rows $H,C,P,N$;
\item ledger refusal, excluding quotient completion, selected representatives, countable choice over thresholds, host sequence convergence, trichotomy, host real equality, and ambient $\RealUp$ completeness.
\end{enumerate}
No local NameCert obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is tuple projection from \autoref{thm:regular-cauchy-completion-criterion-carrier}. The accepted packet displays exactly the regular readback $R$, finite windows $W$, dyadic ledger $D$, modulus schedule $M$, sequence-limit handoff $L$, completion boundary $Q$, and the structural rows $H,C,P,N$.

The analytic route first reads the finite windows $W$ and the tolerance ledger $D$. The regular rational readback $R$ and the convergence-modulus schedule $M$ are therefore exposed only as displayed finite data.

The handoff rows $L$ and $Q$ are downstream of that finite route. The $\RealSequenceLimitUp$ row receives the displayed sequence-limit comparison, and the $\CauchyCompletionCriterionUp$ row records the completion-admission boundary before any $\RealUp$-facing consumer can read the seal.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since the carrier has no row for quotient completion, selected representatives, countable choice over thresholds, host convergence, trichotomy, host real equality, or ambient $\RealUp$ completeness, the listed obligations exhaust the local NameCert surface.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyCompletionCriterionUp.RegularCauchyCompletionCriterionNameCertObligations}
\leanvariant{BEDC.Derived.RegularCauchyCompletionCriterionUp.RegularCauchyCompletionCriterionCarrier\_namecert\_obligations}

\begin{theorem}[RegularCauchyCompletionCriterion Real-seal handoff]
\label{thm:regular-cauchy-completion-criterion-real-seal-handoff}
Every $\RealUp$-facing consumer of an accepted $\RegularCauchyCompletionCriterionUp$ carrier $K=(R,W,D,M,L,Q,H,C,P,N)$ must first read the route
$$
\StreamNameUp \longrightarrow \DyadicRatCoreUp \longrightarrow \RegSeqRatUp \longrightarrow \CauchyConvergenceModulusUp \longrightarrow \RealSequenceLimitUp \longrightarrow \CauchyCompletionCriterionUp \longrightarrow \BishopCauchyCompletionFunctorUp \longrightarrow \CauchyCompletionRecursorUp \longrightarrow \RealUp .
$$
The handoff exposes only finite windows, dyadic tolerances, regular readback, modulus schedule, sequence-limit comparison, completion-admission boundary, componentwise transport, replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{thm:regular-cauchy-completion-criterion-carrier}. Its $\StreamNameUp$ and $\DyadicRatCoreUp$ rows are the only source of inspected windows and tolerance data.

The regular readback and modulus rows consume those displayed data. Thus the $\RegSeqRatUp$ row $R$ and the $\CauchyConvergenceModulusUp$ row $M$ cannot be replaced by a host sequence predicate or by an unbounded tail search.

Next the $\RealSequenceLimitUp$ handoff $L$ and $\CauchyCompletionCriterionUp$ boundary $Q$ are read in order. The $\BishopCauchyCompletionFunctorUp$ and $\CauchyCompletionRecursorUp$ rows can consume this boundary only as displayed completion data, so the completion-facing consumer reaches the $\RealUp$ seal only after the finite route has been displayed.

The structural rows $H,C,P,N$ preserve and name the same handoff. Hence the exported surface is exactly the finite route in the statement.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyCompletionCriterionUp.RegularCauchyCompletionCriterionRealSealHandoff}

\begin{theorem}[RegularCauchyCompletionCriterion ledger refusal]
\label{thm:regular-cauchy-completion-criterion-ledger}
For every accepted $\RegularCauchyCompletionCriterionUp$ packet, the completion-facing ledger refuses quotient stream equality, quotient completion, selected representatives, countable choice over tail thresholds, host sequence convergence, trichotomy, host real equality, and ambient $\RealUp$ completeness. Any consumer requesting one of those objects must leave the displayed carrier route $R,W,D,M,L,Q,H,C,P,N$.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyCompletionCriterionUp.RegularCauchyCompletionCriterionLedgerRefusal}
\begin{proof}
Inspect the carrier fields in \autoref{thm:regular-cauchy-completion-criterion-carrier}. They record only the regular readback, finite windows, dyadic tolerance ledger, modulus schedule, sequence-limit handoff, completion boundary, completion functor and recursor provenance, transport, replay, provenance, and local naming rows.

The NameCert obligations of \autoref{thm:regular-cauchy-completion-criterion-classifier} make those rows exhaustive. No additional row stores a quotient, selected representative, choice schedule, host convergence predicate, trichotomy split, host equality proof, or ambient completeness witness.

Therefore any such request cannot be answered by the packet. It is refused at the ledger boundary before a $\RealUp$-facing consumer can treat the displayed completion criterion as a quotient completion theorem.
\end{proof}

\begin{theorem}[RegularCauchyCompletionCriterion transport stability]
\label{thm:regular-cauchy-completion-criterion-transport-stability}
Let $K=(R,W,D,M,L,Q,H,C,P,N)$ and $K'=(R',W',D',M',L',Q',H',C',P',N')$ be accepted $\RegularCauchyCompletionCriterionUp$ packets. If componentwise $\hsame$ transport identifies the rows $R,W,D,M,L,Q,H,C,P,N$ with $R',W',D',M',L',Q',H',C',P',N'$, then the finite-window route, dyadic tolerance ledger, modulus schedule, $\RealSequenceLimitUp$ handoff, $\CauchyCompletionCriterionUp$ boundary, replay, provenance, and local $\NameCert$ row are preserved. The transported packet still refuses quotient completion, selected representatives, countable choice over thresholds, trichotomy, host equality, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project both carriers through \autoref{thm:regular-cauchy-completion-criterion-carrier}. The assumed componentwise $\hsame$ transport aligns exactly the displayed rows $R,W,D,M,L,Q,H,C,P,N$ and their primed counterparts.

Apply the NameCert obligations of \autoref{thm:regular-cauchy-completion-criterion-classifier}. Finite-window exactness, dyadic tolerance exactness, and completion-boundary handoff are all phrased in those rows, so the transport cannot move the route outside the displayed packet.

Finally use the Real-seal handoff and ledger refusal theorems, \autoref{thm:regular-cauchy-completion-criterion-real-seal-handoff} and \autoref{thm:regular-cauchy-completion-criterion-ledger}. They identify the terminal read and the refused coordinates, so the transported packet preserves both the completion-facing route and the same boundary exclusions.
\end{proof}

\begin{theorem}[RegularCauchyCompletionCriterion obligation closure package]
\label{thm:regular-cauchy-completion-criterion-obligation-closure}
The carrier, NameCert obligations, Real-seal handoff, ledger refusal, and transport stability results for $\RegularCauchyCompletionCriterionUp$ assemble the obligation-closure surface over $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchyConvergenceModulusUp$, $\RealSequenceLimitUp$, $\CauchyCompletionCriterionUp$, $\BishopCauchyCompletionFunctorUp$, $\CauchyCompletionRecursorUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The surface closes exactly the finite criterion rows $R,W,D,M,L,Q,H,C,P,N$ and does not claim quotient completion, selected representatives, countable choice, trichotomy, host equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the carrier and obligation rows in \autoref{thm:regular-cauchy-completion-criterion-carrier} and \autoref{thm:regular-cauchy-completion-criterion-classifier}. They expose every analytic and structural coordinate of the packet and make the local $\NameCert$ surface exhaustive.

Next apply the Real-seal handoff and ledger refusal theorems. The handoff theorem routes any completion-facing read through the displayed StreamName, DyadicRatCore, RegSeqRat, modulus, sequence-limit, and completion-boundary rows, while the refusal theorem removes quotient, choice, host, and ambient-completeness exits from the package.

Finally apply transport stability from \autoref{thm:regular-cauchy-completion-criterion-transport-stability}. Since componentwise $\hsame$ preserves the displayed rows and their replay and provenance data, the package is stable as an obligation-closure surface over the named BEDC primitives.
\end{proof}

\begin{theorem}[RegularCauchyCompletionCriterion scoped factorization]
\label{thm:regular-cauchy-completion-criterion-scope-factorization}
Every completion-facing read of an accepted $\RegularCauchyCompletionCriterionUp$ packet factors through the displayed rows of \autoref{thm:regular-cauchy-completion-criterion-carrier} before it may export a local $\NameCert$ surface. Concretely, the read must pass through $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchyConvergenceModulusUp$, $\RealSequenceLimitUp$, $\CauchyCompletionCriterionUp$, $\BishopCauchyCompletionFunctorUp$, $\CauchyCompletionRecursorUp$, $\RealUp$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\RegularCauchyCompletionCriterionUp}$ row.

The factorization is scoped to those rows. It does not supply quotient completion, selected representatives, countable choice over thresholds, host sequence convergence, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from the carrier and NameCert obligations of \autoref{thm:regular-cauchy-completion-criterion-carrier} and \autoref{thm:regular-cauchy-completion-criterion-classifier}. They make the rows $R,W,D,M,L,Q,H,C,P,N$ exhaustive for any accepted packet.

Apply the Real-seal handoff of \autoref{thm:regular-cauchy-completion-criterion-real-seal-handoff}. It orders the completion-facing route through StreamName windows, DyadicRatCore tolerance, RegSeqRat readback, convergence modulus, sequence-limit handoff, completion boundary, completion functor, completion recursor, and the terminal Real seal before a consumer can read completion data.

The ledger refusal of \autoref{thm:regular-cauchy-completion-criterion-ledger} removes quotient, choice, host, trichotomy, and ambient-completeness exits from that route. Transport stability in \autoref{thm:regular-cauchy-completion-criterion-transport-stability} then preserves the same finite rows under componentwise $\hsame$ and displayed $\Cont$ replay.

Finally use the obligation-closure package of \autoref{thm:regular-cauchy-completion-criterion-obligation-closure}. It repacks the displayed route with $\Pkg$ provenance and the local $\NameCert$ row, so every completion-facing read factors through exactly the named scope.
\end{proof}

\closureat{RegularCauchyCompletionCriterionUp}{seedStr}

\begin{closurestatus}{\RegularCauchyCompletionCriterionUp}
  \constructivestory{A $\RegularCauchyCompletionCriterionUp$ packet is generated from a RegSeqRat readback row, StreamName finite windows, a DyadicRatCore tolerance ledger, a CauchyConvergenceModulus schedule, a RealSequenceLimit handoff, a CauchyCompletionCriterion boundary, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{thm:regular-cauchy-completion-criterion-carrier}, \autoref{thm:regular-cauchy-completion-criterion-classifier}, \autoref{thm:regular-cauchy-completion-criterion-real-seal-handoff}, \autoref{thm:regular-cauchy-completion-criterion-ledger}, \autoref{thm:regular-cauchy-completion-criterion-transport-stability}, \autoref{thm:regular-cauchy-completion-criterion-obligation-closure}, and \autoref{thm:regular-cauchy-completion-criterion-scope-factorization} bind the finite regular-Cauchy completion criterion route through RegSeqRat, StreamName, DyadicRatCore, CauchyConvergenceModulus, RealSequenceLimit, CauchyCompletionCriterion, BishopCauchyCompletionFunctor, CauchyCompletionRecursor, Real rows, hsame transport, Cont replay, Pkg provenance, and local NameCert transport before any completion-facing consumer reads the Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient completion, selected representatives, countable choice, host sequence convergence, trichotomy, host real equality, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Public closure requires a checked export theorem for the same RegSeqRat, StreamName, DyadicRatCore, modulus, sequence-limit, completion-boundary, recursor, Real, transport, replay, provenance, and local NameCert primitives.}
\end{closurestatus}
