\begin{theorem}[LowerSemicontinuous obligation closure package]
\label{thm:lower-semicontinuous-obligation-closure-package}
The $\LowerSemicontinuousUp$ root packet carries the paper-side obligation package through the finite route
$$
X,F,W,R,E,O,H,C,P,N .
$$
Carrier admission, epigraph-window exactness, lower-real classifier stability, located readback compatibility, transport replay, provenance, and Real-seal nonescape are all read from the displayed rows of \autoref{def:lower-semicontinuous-carrier}, \autoref{thm:lower-semicontinuous-namecert-obligation-surface}, \autoref{thm:lower-semicontinuous-lowerreal-epigraph-route}, and the root epigraph obligations of \autoref{thm:lowersemicontinuous-root-epigraph-closure-package}. The package does not add host topology, semantic liminf data, quotient real equality, selected approximating sequences, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousObligationClosurePackage}
\begin{proof}
Project the carrier and NameCert obligation surface. They list the source, graph, epigraph, schedule, readback, comparison, transport, replay, provenance, and local naming rows as the complete obligation inventory.

Use the lower-real epigraph route and the root epigraph obligation package. These results place every lower-bound read on $X,F,W,R,E,O,H,C,P,N$ and account for exactness, located comparison, finite thresholds, and root nonescape within that route.

The remaining rows are structural. Componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming preserve the same finite packet, so no refused analytic principle is introduced by packaging the obligations together.
\end{proof}

\begin{theorem}[LowerSemicontinuous public epigraph export]
\label{thm:lowersemicontinuous-public-epigraph-export}
Every public epigraph consumer of an accepted $\LowerSemicontinuousUp$ packet must read the finite route
$$
X,F,W,R,E,O,H,C,P,N .
$$
The window row $W$ is read through $\StreamNameUp$, the value row $R$ through $\RegSeqRatUp$, the epigraph and comparison rows $E,O$ through $\LowerRealUp$, $\UpperRealUp$, and $\LocatedRealUp$, and the terminal $\RealUp$ seal only after the transport, replay, provenance, and local naming rows $H,C,P,N$ remain visible. The export provides no semantic liminf operator, host topology, selected approximants, quotient equality, choice schedule, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousPublicEpigraphExport}
\begin{proof}
Start with the obligation closure package. It identifies $X,F,W,R,E,O,H,C,P,N$ as the complete obligation inventory and fixes carrier admission, epigraph-window exactness, located readback, transport replay, provenance, and Real-seal nonescape on those rows.

Apply the located epigraph route and located-real obligation route. These force a public epigraph reader to expose $W$ through $\StreamNameUp$, $R$ through $\RegSeqRatUp$, and $E,O$ through the lower-real, upper-real, and located-real surfaces before the lower-bound claim can be used.

Use Real-seal nonescape and the scoped route next. The terminal $\RealUp$ seal is downstream of the same displayed rows, while $H,C,P,N$ preserve only componentwise transport, displayed replay, provenance, and local naming.

Finally apply public route export. Since that export is already the public composite of the displayed lower-bound route and none of the refused analytic principles is a carrier coordinate, every public epigraph consumer receives exactly the finite route above.
\end{proof}

\begin{theorem}[LowerSemicontinuous located epigraph route]
\label{thm:lower-semicontinuous-located-epigraph-route}
Every located epigraph read from an accepted $\LowerSemicontinuousUp$ packet must expose the finite schedule row $W$, the $\RegSeqRatUp$ readback row $R$, the lower-real epigraph row $E$, and the comparison ledger $O$ before the located consumer can use the lower-bound evidence. The read is therefore routed through $\StreamNameUp$, $\RegSeqRatUp$, $\LowerRealUp$, $\UpperRealUp$, $\LocatedRealUp$, and the terminal $\RealUp$ seal only as displayed rows, not as a host topology or semantic liminf operator.
\end{theorem}
\begin{proof}
Start with the root epigraph carrier. It orders the lower-bound read as $X,F,W,R,E,O,H,C,P,N$, so the located consumer cannot inspect $O$ before the scheduled window and regular rational value row are visible.

Apply located readback stability and located epigraph transport induction. They keep the lower-bound witness in $E$ attached to the same comparison ledger $O$ after transport through the located and lower-real surfaces.

Finally use the Real handoff. The terminal seal is reached only downstream of the same displayed route, and the carrier has no coordinate for host topology, liminf semantics, quotient equality, selected sequences, choice, compactness, or completeness.
\end{proof}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousLocatedEpigraphRoute}

\begin{theorem}[LowerSemicontinuous located-real obligation route]
\label{thm:lower-semicontinuous-located-real-obligation-route}
Every $\LocatedRealUp$-facing read of an accepted $\LowerSemicontinuousUp$ packet must expose the full finite route
$$
X,F,W,R,E,O,H,C,P,N
$$
before any lower-bound claim is used. The route opens the $\StreamNameUp$ window $W$, reads the $\RegSeqRatUp$ value row $R$, compares the $\LowerRealUp$ and $\UpperRealUp$ epigraph rows through $E$ and $O$, exposes the $\LocatedRealUp$ comparison data, and reaches the terminal $\RealUp$ seal only after transport, replay, provenance, and local naming remain visible.
\end{theorem}
\begin{proof}
Project the carrier and obligation closure package. They make $X,F,W,R,E,O,H,C,P,N$ the complete displayed route for lower-bound reads, with the schedule, readback, epigraph row, comparison ledger, transport, replay, provenance, and local naming all present before the claim is consumed.

Use the located epigraph route above. It places every located lower-bound read through $W,R,E,O$ and accounts for the $\StreamNameUp$, $\RegSeqRatUp$, $\LowerRealUp$, $\UpperRealUp$, and $\LocatedRealUp$ rows as displayed data rather than as a host topology.

Finally apply Real-seal nonescape. The terminal seal cannot be reached by semantic liminf data, selected approximants, quotient equality, countable choice, open-cover compactness, or ambient Real completeness, because none of those principles is a row of the accepted packet.
\end{proof}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousLocatedRealObligationRoute}

\begin{theorem}[LowerSemicontinuous Real-seal nonescape]
\label{thm:lower-semicontinuous-realseal-nonescape}
Every $\RealUp$-facing read exported by the $\LowerSemicontinuousUp$ obligation package remains downstream of the finite route
$$
X,F,W,R,E,O,H,C,P,N .
$$
The route first opens the $\StreamNameUp$ finite window $W$ and the $\RegSeqRatUp$ readback row $R$, then reads the $\LocatedRealUp$ threshold rows carried by the comparison ledger $O$, then consumes the one-sided $\LowerRealUp$ and $\UpperRealUp$ ledgers attached to the epigraph row $E$ and comparison row $O$, and only then reaches the terminal $\RealUp$ seal through $H,C,P,N$. The seal can certify only this displayed L10 source route; it cannot export a semantic liminf operator, quotient real equality, selected approximating sequence, countable-choice schedule, open-cover compactness theorem, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the Real-seal window exhaustion theorem. It confines every Real-facing read to the displayed route through the scheduled $\StreamNameUp$ window $W$, $\RegSeqRatUp$ readback $R$, epigraph row $E$, and comparison ledger $O$.

Use the located epigraph route and the located-real obligation route to bind that route to its threshold rows. These theorems place the $\LocatedRealUp$ comparison data inside $O$ and force the $\LowerRealUp$ and $\UpperRealUp$ one-sided ledgers in $E,O$ to remain visible before a lower-bound claim is consumed.

Use the obligation closure package above to account for the same rows as the complete obligation inventory. The package adds no analytic coordinate beyond $X,F,W,R,E,O,H,C,P,N$.

Transport, replay, provenance, and local naming preserve that inventory. Since the refused principles are absent from the carrier and from the Real-seal handoff, the endpoint cannot escape the displayed finite route.
\end{proof}

\begin{theorem}[LowerSemicontinuous scoped route]
\label{thm:lower-semicontinuous-scoped-route}
Every scoped consumer of an accepted $\LowerSemicontinuousUp$ packet must traverse the finite route
$$
X,F,W,R,E,O,H,C,P,N
$$
before any lower-bound claim is consumed. The route exposes the source row $X$, the graph row $F$, the $\StreamNameUp$ window $W$, the $\RegSeqRatUp$ readback $R$, the $\LowerRealUp$ and $\UpperRealUp$ epigraph rows $E,O$, the $\LocatedRealUp$ comparison data, and the terminal $\RealUp$ seal with componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ still visible.

The scoped route exports no semantic liminf operator, host topology, selected approximants, countable-choice schedule, quotient equality, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousScopedRoute}
\begin{proof}
Start with the obligation closure package. It identifies $X,F,W,R,E,O,H,C,P,N$ as the complete obligation inventory and refuses host topology, semantic liminf data, quotient equality, selected approximating sequences, countable choice, open-cover compactness, and ambient $\RealUp$ completeness at the package boundary.

Apply the located epigraph route and the located-real obligation route. These place every located lower-bound read through $W,R,E,O$ and keep the $\StreamNameUp$, $\RegSeqRatUp$, $\LowerRealUp$, $\UpperRealUp$, and $\LocatedRealUp$ rows visible before the lower-bound claim is used.

Finally apply Real-seal nonescape. The terminal $\RealUp$ seal remains downstream of the same displayed route, while $H,C,P,N$ preserve transport, replay, provenance, and naming. Since no refused principle is a coordinate of the accepted packet, a scoped consumer receives only the finite route stated above.
\end{proof}

\begin{theorem}[LowerSemicontinuous public route export]
\label{thm:lower-semicontinuous-public-route-export}
Every public consumer of an accepted $\LowerSemicontinuousUp$ packet receives the finite route
$$
X,F,W,R,E,O,H,C,P,N
$$
as the exported lower-bound route. The route factors through $\StreamNameUp$, $\RegSeqRatUp$, $\LowerRealUp$, $\UpperRealUp$, $\LocatedRealUp$, and $\RealUp$ before any lower-bound claim is consumed, and exports no semantic liminf operator, host topology, selected approximants, quotient equality, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousPublicRouteExport}
\begin{proof}
Start with the obligation closure package. It identifies $X,F,W,R,E,O,H,C,P,N$ as the complete lower-bound obligation inventory and adds no analytic coordinate beyond those displayed rows.

Use the located epigraph route and the located-real obligation route. Together they force every lower-bound read through the finite window $W$, the regular rational readback $R$, the lower and upper epigraph rows $E,O$, and the $\LocatedRealUp$ comparison data before the claim is available to a consumer.

Finally apply Real-seal nonescape and the scoped route. The terminal $\RealUp$ seal remains downstream of the same displayed route, and $H,C,P,N$ preserve transport, replay, provenance, and local naming. The public export is therefore the composite of the existing route theorems and contributes no semantic liminf operator, host topology, selected approximant, quotient equality, countable-choice schedule, open-cover compactness theorem, or ambient completeness principle.
\end{proof}

\begin{theorem}[LowerSemicontinuous public-route exactness triad]
\label{thm:lower-semicontinuous-public-route-exactness-triad}
Every public $\LowerSemicontinuousUp$ consumer factors through exactly three visible row groups. The first group is the $\StreamNameUp$ and $\RegSeqRatUp$ window-readback surface $W,R$. The second group is the $\LowerRealUp$, $\UpperRealUp$, and $\LocatedRealUp$ epigraph-comparison surface $E,O$. The third group is the terminal $\RealUp$ seal together with the structural rows $H,C,P,N$ for componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming. No public route exports a semantic liminf operator, host topology, selected approximants, quotient equality, countable-choice schedule, open-cover compactness, or ambient $\RealUp$ completeness outside this triad.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousPublicRouteExactnessTriad}
\begin{proof}
Start with the obligation closure package. It fixes $X,F,W,R,E,O,H,C,P,N$ as the complete public inventory, so the source and graph rows only feed the three consumer-facing groups named in the statement.

Use the located epigraph route and the located-real obligation route. They place every lower-bound read through the window-readback rows $W,R$ and then through the epigraph-comparison rows $E,O$, with $\StreamNameUp$, $\RegSeqRatUp$, $\LowerRealUp$, $\UpperRealUp$, and $\LocatedRealUp$ all visible before the claim is consumed.

Apply Real-seal nonescape, the scoped route, and the public route export. These theorems put the terminal $\RealUp$ seal downstream of the same displayed route and keep $H,C,P,N$ attached as transport, replay, provenance, and local naming rows.

The three groups therefore exhaust the public route. Since the carrier has no coordinate for semantic liminf data, host topology, selected approximants, quotient equality, countable choice, open-cover compactness, or ambient completeness, no public consumer can obtain such data outside the triad.
\end{proof}
