\begin{theorem}[LowerSemicontinuous root epigraph carrier]
\label{thm:lowersemicontinuous-root-epigraph-carrier}
For an accepted $\LowerSemicontinuousUp$ root packet
$$
L_{\mathrm{root}}=(X,F,E,W,R,O,H,C,P,N),
$$
there is a finite root epigraph subcarrier
$$
L_{\mathrm{epi}}=(X,F,W,R,E,O,H,C,P,N)
$$
whose rows are read in that order. The row $W$ displays the finite $\StreamNameUp$ inspection window, $R$ displays the $\RegSeqRatUp$ value readback, $E$ displays the lower-real epigraph ledger, $O$ displays the $\LowerRealUp$--$\UpperRealUp$ comparison and located $\RealUp$ seal boundary, $H$ records componentwise $\hsame$ transport, $C$ records displayed $\Cont$ replay, $P$ records $\Pkg$ provenance, and $N$ is the local $\NameCert_{\LowerSemicontinuousUp}$ row. The carrier has no host liminf predicate, quotient real equality, selected approximating sequence, countable choice, open-cover compactness, or ambient completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootEpigraphCarrier}
\leanvariant{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRoot\_epigraph\_carrier}
\begin{proof}
Project the rows of the root packet. The carrier definition already displays $X,F,E,W,R,O,H,C,P,N$, and the reordered subcarrier only makes the consumer route explicit: source and graph first, then finite window, rational readback, epigraph evidence, comparison, transport, replay, provenance, and local name.

The finite epigraph window is obtained by induction on the displayed window rows. The base read sees the source and graph rows; the successor read adds one $\StreamNameUp$ schedule row, one $\RegSeqRatUp$ value row, and the corresponding epigraph witness in $E$.

Transport and replay do not add analytic data. They carry the same rows through $H$ and $C$, with $P$ and $N$ sourcing and naming the accepted packet.

The exclusions follow from the carrier inventory. Since no coordinate contains a host liminf predicate, quotient equality, selector, choice row, compactness theorem, or completeness witness, no consumer can extract one from the subcarrier.
\end{proof}

\begin{theorem}[LowerSemicontinuous regular readback handoff]
\label{thm:lowersemicontinuous-root-regular-readback-handoff}
Every $\StreamNameUp$ and $\RegSeqRatUp$ readback consumed from $L_{\mathrm{epi}}$ reaches a $\LocatedRealUp$ handoff only through the displayed route
$$
W \longrightarrow R \longrightarrow E \longrightarrow O \longrightarrow H \longrightarrow C \longrightarrow P \longrightarrow N .
$$
The comparison row $O$ keeps the lower-real and upper-real boundary attached to the same finite readback, so the handoff exports located comparison data and not semantic topology, hidden lower envelopes, quotient $\RealUp$ equality, countable choice, or a detached completion principle.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootRegularReadbackHandoff}
\begin{proof}
Read the finite schedule row $W$ and the regular rational value row $R$. These are the only readback rows displayed before the epigraph ledger is inspected.

Apply the lower-real epigraph route and located readback stability already present in the chapter. The epigraph row $E$ is reached from the scheduled value read, and $O$ then compares the same lower-real and upper-real boundary before the located handoff is visible.

The remaining rows preserve that route. Componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and the local $\NameCert$ row $N$ keep the handoff attached to the same readback.

Thus the consumer receives only the finite located comparison surface. The absent semantic topology, envelope, quotient equality, choice, and completion coordinates cannot be supplied by this handoff.
\end{proof}

\begin{theorem}[LowerSemicontinuous Real seal boundary]
\label{thm:lowersemicontinuous-root-real-seal-boundary}
For every accepted root epigraph subcarrier $L_{\mathrm{epi}}$, a terminal $\RealUp$ seal can be read only after the finite epigraph window, $\LowerRealUp$--$\UpperRealUp$ comparison row, $\LocatedRealUp$ handoff, $\BHist$ transport, $\hsame$ ledger, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row have all been displayed. The seal boundary exports no Baire-one pointwise limit, quotient real equality, selected representative, countable choice schedule, open-cover compactness proof, or ambient Real-completeness theorem.
\end{theorem}
\begin{proof}
Start from the subcarrier of \autoref{thm:lowersemicontinuous-root-epigraph-carrier}. Its Real-facing route is finite because every row is already part of the accepted $\BHist$ packet.

Use the regular readback handoff of \autoref{thm:lowersemicontinuous-root-regular-readback-handoff}. It places the terminal seal after the finite window $W$, rational readback $R$, epigraph row $E$, comparison row $O$, transport row $H$, replay row $C$, provenance row $P$, and local name $N$.

The seal is therefore a boundary row, not an analytic limit principle. It confirms that the displayed located comparison is ready for Real-facing consumers while refusing the absent pointwise-limit, quotient-equality, selection, choice, compactness, and completeness data.
\end{proof}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootRealSealBoundary}

\begin{theorem}[LowerSemicontinuous root epigraph obligation rows]
\label{thm:lowersemicontinuous-root-epigraph-rows}
Every accepted $\LowerSemicontinuousUp$ root epigraph packet exposes, as one finite surface, the lower-real row, epigraph row, located comparison row, $\RegSeqRatUp$ handoff, and terminal $\RealUp$ seal. Concretely, the consumer route is
$$
X,F,W,R,E,O,H,C,P,N,
$$
where $R$ is the regular readback, $E$ is the lower-real epigraph ledger, $O$ is the located comparison boundary, and the terminal Real seal is downstream of those rows. No row in this surface is a host epigraph closure theorem, quotient real equality, selected limit, countable-choice schedule, open-cover compactness proof, or ambient completeness witness.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootEpigraphRouteSurface}
\begin{proof}
Project the root epigraph carrier of \autoref{thm:lowersemicontinuous-root-epigraph-carrier}. It already displays the source, graph, finite window, regular readback, epigraph ledger, comparison row, transport, replay, provenance, and local naming rows.

Apply the regular readback handoff of \autoref{thm:lowersemicontinuous-root-regular-readback-handoff}. This places the $\RegSeqRatUp$ row before the epigraph and located comparison rows are consumed.

Use the Real seal boundary of \autoref{thm:lowersemicontinuous-root-real-seal-boundary}. The terminal seal is visible only after the lower-real epigraph and located comparison rows, so the listed exclusions cannot be exported by the accepted packet.
\end{proof}

\begin{theorem}[LowerSemicontinuous root Real-seal route]
\label{thm:lowersemicontinuous-root-realseal-route}
Any $\RealUp$-facing read of an accepted $\LowerSemicontinuousUp$ root packet passes through the regular readback row and epigraph rows before the Real seal is visible. Equivalently, the read must expose $W,R,E,O,H,C,P,N$ in that order, and the terminal seal cannot be consumed directly from the source row, graph row, or an ambient completion principle.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootRealSealRoute}
\begin{proof}
Begin with the regular readback handoff \autoref{thm:lowersemicontinuous-root-regular-readback-handoff}. It forces the finite window $W$ and regular rational readback $R$ to appear before the epigraph ledger $E$ and located comparison row $O$.

Apply the obligation rows theorem \autoref{thm:lowersemicontinuous-root-epigraph-rows}. The lower-real and epigraph rows are part of the same finite surface as the located comparison row.

Finally use the Real seal boundary \autoref{thm:lowersemicontinuous-root-real-seal-boundary}. Since the seal is terminal for this route, a Real-facing consumer cannot bypass the regular readback and epigraph rows by appealing to the source row, graph row, or an ambient completion object.
\end{proof}

\begin{theorem}[LowerSemicontinuous root epigraph nonescape]
\label{thm:lowersemicontinuous-root-epigraph-nonescape}
No accepted $\LowerSemicontinuousUp$ root epigraph packet exports host epigraph closure, quotient real equality, selected limits, countable choice, or ambient completeness. Every export is exhausted by the finite rows $X,F,W,R,E,O,H,C,P,N$ and by the downstream Real seal boundary already attached to those rows.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootEpigraphNonescape}
\begin{proof}
Project the finite row surface of \autoref{thm:lowersemicontinuous-root-epigraph-rows}. The only analytic-facing data available to a consumer are the scheduled window, regular readback, epigraph ledger, located comparison row, and terminal seal boundary.

Use the Real-seal route of \autoref{thm:lowersemicontinuous-root-realseal-route}. It prevents a Real-facing read from detaching from $W,R,E,O,H,C,P,N$.

The carrier exclusions in \autoref{thm:lowersemicontinuous-root-epigraph-carrier} then finish the nonescape claim. The packet has no coordinate for host closure, quotient equality, selected limit, countable choice, or ambient completeness, so those objects cannot be returned as $\LowerSemicontinuousUp$ evidence.
\end{proof}

\begin{theorem}[LowerSemicontinuous root epigraph basis]
\label{thm:lowersemicontinuous-root-epigraph-basis}
Every accepted $\LowerSemicontinuousUp$ root packet carries its lower-real epigraph basis through the finite rows
$$
X,F,W,R,E,O,H,C,P,N .
$$
The basis row is generated by scheduled source windows, regular rational readbacks, and displayed lower-bound witnesses; it is not a host liminf predicate, quotient real equality, selected approximating sequence, countable choice schedule, open-cover compactness proof, or ambient completeness witness.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootEpigraphBasis}
\begin{proof}
Project the root epigraph carrier of \autoref{thm:lowersemicontinuous-root-epigraph-carrier}. The only rows before the comparison ledger are the source, graph, finite window, regular rational readback, and lower-real epigraph rows.

Apply \autoref{thm:lowersemicontinuous-root-regular-readback-handoff}. It keeps each readback attached to the same epigraph witness before the located comparison row is consumed.

Finally use \autoref{thm:lowersemicontinuous-root-real-seal-boundary}. The terminal seal is downstream of the displayed basis, so no excluded host analytic object can be exported by the root packet.
\end{proof}

\begin{theorem}[LowerSemicontinuous root located readback]
\label{thm:lowersemicontinuous-root-located-readback}
For an accepted $\LowerSemicontinuousUp$ root packet, every located readback of a lower-bound row factors through the same scheduled window $W$, regular rational value row $R$, epigraph ledger $E$, comparison row $O$, transport row $H$, replay row $C$, provenance row $P$, and local name $N$. The readback cannot detach into a semantic topology, quotient $\RealUp$ equality, selected representative, countable choice, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootLocatedReadback}
\begin{proof}
Start with the regular readback handoff of \autoref{thm:lowersemicontinuous-root-regular-readback-handoff}. It places $W,R,E,O,H,C,P,N$ in the exact order consumed by a located readback.

The comparison row $O$ is reached only after the lower-bound witness in $E$. Transport through $H$ and replay through $C$ preserve that same finite route rather than creating a second located-real carrier.

The Real seal boundary of \autoref{thm:lowersemicontinuous-root-real-seal-boundary} excludes semantic topology, quotient equality, selection, choice, and ambient completeness from the endpoint.
\end{proof}

\begin{theorem}[Lower semicontinuous epigraph source scope]
\label{thm:lower-semicontinuous-epigraph-source-scope}
Every accepted root epigraph consumer of a $\LowerSemicontinuousUp$ packet factors through the concrete rows
$$
X,F,W,R,E,O,H,C,P,N .
$$
Here $W$ is the finite $\StreamNameUp$ inspection window, $R$ is the $\RegSeqRatUp$ readback row, $E$ is the lower-real epigraph evidence, $O$ is the $\LowerRealUp$--$\UpperRealUp$ located comparison and terminal $\RealUp$ seal boundary, and $H,C,P,N$ are the componentwise transport, replay, provenance, and local NameCert rows. Thus the consumer is source-scoped by the displayed stream-name and rational-readback route; it has no coordinate for semantic topology, host liminf, quotient real equality, selected representatives, countable choice, open-cover compactness, or ambient completion.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousEpigraphSourceScope}
\begin{proof}
Begin with the root epigraph carrier of \autoref{thm:lowersemicontinuous-root-epigraph-carrier}. It displays the only source rows available to an epigraph consumer: the source row $X$, graph-facing row $F$, finite window $W$, rational readback $R$, lower-real epigraph row $E$, comparison row $O$, and structural rows $H,C,P,N$.

Next use the regular readback handoff of \autoref{thm:lowersemicontinuous-root-regular-readback-handoff}. This keeps the $\StreamNameUp$ window and $\RegSeqRatUp$ readback attached to the same lower-bound evidence before any located endpoint is visible.

Apply the Real seal boundary of \autoref{thm:lowersemicontinuous-root-real-seal-boundary}. The row $O$ is therefore a displayed $\LowerRealUp$--$\UpperRealUp$ comparison with a terminal $\RealUp$ boundary, not a hidden analytic completion principle.

Finally use the located readback theorem of \autoref{thm:lowersemicontinuous-root-located-readback}. Every accepted located read of the epigraph endpoint has already passed through $W,R,E,O,H,C,P,N$, so the root epigraph consumer factors through the listed source-scope route and cannot export any of the excluded host objects.
\end{proof}

\begin{theorem}[LowerSemicontinuous epigraph to LocatedSet handoff]
\label{thm:lowersemicontinuous-epigraph-locatedset-handoff}
Any accepted $\LowerSemicontinuousUp$ epigraph read that is consumed as a $\LocatedSetUp$ packet must pass through the terminal $\RealUp$ seal boundary, the root epigraph basis, the $\LocatedSetUp$ route, $\RegSeqRatUp$ readback, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows. The handoff exports no host topology, semantic liminf operator, quotient $\RealUp$ equality, selected approximating sequence, countable choice schedule, open-cover compactness proof, or ambient Real-completeness theorem.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousEpigraphLocatedSetHandoff}
\begin{proof}
Start with the root epigraph carrier of \autoref{thm:lowersemicontinuous-root-epigraph-carrier}. It displays the finite source, graph, window, readback, epigraph, comparison, transport, replay, provenance, and local naming rows.

Use the regular readback handoff of \autoref{thm:lowersemicontinuous-root-regular-readback-handoff} and the root epigraph basis of \autoref{thm:lowersemicontinuous-root-epigraph-basis}. Together they keep every $\RegSeqRatUp$ read attached to the same lower-bound row before any located consumer sees the endpoint.

Apply the Real seal boundary of \autoref{thm:lowersemicontinuous-root-real-seal-boundary}. The terminal seal is visible only after the epigraph basis and located comparison rows have been displayed.

Finally use the located readback theorem above. A $\LocatedSetUp$ consumer therefore receives the same finite route through $\RealUp$, $\RegSeqRatUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, and the excluded host analytic objects remain outside the packet.
\end{proof}

\begin{theorem}[LowerSemicontinuous epigraph to LocatedSet exactness]
\label{thm:lowersemicontinuous-epigraph-locatedset-exactness}
An accepted $\LowerSemicontinuousUp$ epigraph read becomes a $\LocatedSetUp$ packet exactly when the scheduled $\StreamNameUp$ window $W$, $\RegSeqRatUp$ value row $R$, lower-real epigraph ledger $E$, upper/lower comparison row $O$, $\LocatedRealUp$ handoff, terminal $\RealUp$ seal boundary, $\hsame$ transport row $H$, displayed $\Cont$ replay row $C$, $\Pkg$ provenance row $P$, and local $\NameCert$ row $N$ are all displayed in the same finite route. The exactness route exports no host topology, semantic liminf operator, quotient $\RealUp$ equality, selected approximating sequence, countable choice schedule, open-cover compactness proof, or ambient Real-completeness theorem.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousEpigraphLocatedSetExactness}
\begin{proof}
Start with the root epigraph carrier of \autoref{thm:lowersemicontinuous-root-epigraph-carrier}. It supplies the scheduled window, regular rational value row, epigraph ledger, comparison row, transport, replay, provenance, and local name as one finite packet.

Use the regular readback handoff and the root located readback theorem. These force the $\RegSeqRatUp$ value row and every $\LocatedRealUp$ endpoint to pass through the same $W,R,E,O,H,C,P,N$ route before a located consumer can read it.

Apply the Real seal boundary of \autoref{thm:lowersemicontinuous-root-real-seal-boundary}. The seal is terminal for the displayed epigraph comparison, so quotient equality, selected representatives, countable choice, compactness, and ambient completeness have no export coordinate.

Finally use the epigraph-to-$\LocatedSetUp$ handoff of \autoref{thm:lowersemicontinuous-epigraph-locatedset-handoff}. The forward direction reads the listed rows before forming the located-set packet; the reverse direction projects those same rows from any accepted located-set consumption of the epigraph read. Thus the handoff is exact for the displayed finite route and for no hidden host object.
\end{proof}

\begin{theorem}[LowerSemicontinuous root nonescape]
\label{thm:lowersemicontinuous-root-nonescape}
Every root-level consumer of the $\LowerSemicontinuousUp$ epigraph packet is exhausted by the displayed source, graph, finite-window, readback, epigraph, located-comparison, transport, replay, provenance, and local naming rows. The packet exports no host topology, semantic liminf operator, quotient real equality, selected sequence, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Use the root epigraph basis theorem to identify the consumer-visible rows. They are all part of the finite $\BHist$ packet and all pass through the local NameCert route.

The located readback theorem then accounts for every Real-facing endpoint. A consumer reaches the endpoint only after $W,R,E,O,H,C,P,N$ have been displayed.

Since the excluded host objects have no coordinate in the carrier and no replay row, the root packet cannot export them.
\end{proof}

\begin{theorem}[LowerSemicontinuous finite epigraph threshold exhaustion]
\label{thm:lowersemicontinuous-finite-epigraph-threshold-exhaustion}
Every accepted $\LowerSemicontinuousUp$ root epigraph packet exhausts all displayed finite threshold reads through
$$
X,F,W,R,E,O,H,C,P,N .
$$
Any lower-bound threshold visible to a $\LowerRealUp$, $\UpperRealUp$, $\LocatedRealUp$, or $\RealUp$ consumer must appear in the scheduled $\StreamNameUp$ and $\RegSeqRatUp$ window rows before the epigraph ledger is consumed. The packet exports no semantic liminf predicate, quotient $\RealUp$ equality, selected approximating sequence, countable choice, open-cover compactness, or ambient Real completeness.
\end{theorem}
\begin{proof}
Start with the root epigraph carrier of \autoref{thm:lowersemicontinuous-root-epigraph-carrier}. Its visible threshold route is exactly the source row $X$, graph row $F$, scheduled window row $W$, regular rational readback $R$, epigraph ledger $E$, comparison row $O$, transport $H$, replay $C$, provenance $P$, and local name $N$.

Apply the regular readback handoff of \autoref{thm:lowersemicontinuous-root-regular-readback-handoff}. A threshold cannot reach a lower-real, upper-real, located-real, or Real-facing consumer until the scheduled $\StreamNameUp$ window and matching $\RegSeqRatUp$ value row have already been displayed.

Use the Real seal boundary of \autoref{thm:lowersemicontinuous-root-real-seal-boundary}. The terminal $\RealUp$ seal is downstream of $W,R,E,O,H,C,P,N$, so any visible lower-bound threshold must already be present in those finite rows before the seal is read.

Finally apply root nonescape. Since the carrier has no semantic liminf coordinate, quotient Real equality, selected approximating sequence, countable-choice schedule, open-cover compactness proof, or ambient completeness primitive, the displayed finite threshold rows exhaust every accepted epigraph read.
\end{proof}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousFiniteEpigraphThresholdExhaustion}

\begin{theorem}[LowerSemicontinuous root epigraph closure package]
\label{thm:lowersemicontinuous-root-epigraph-closure-package}
The root epigraph carrier, regular readback handoff, $\RealUp$ seal boundary, epigraph basis, located readback, $\LocatedSetUp$ handoff, exactness, nonescape, finite threshold exhaustion, and $\LocatedSetUp$ threshold route assemble the $\obligationClosure$ surface for $\LowerSemicontinuousUp$ over $\StreamNameUp$, $\RegSeqRatUp$, $\LowerRealUp$, $\UpperRealUp$, $\LocatedRealUp$, $\LocatedSetUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. Every accepted root epigraph consumer reads the finite route
$$
X,F,W,R,E,O,H,C,P,N
$$
or its threshold subroute
$$
W,R,E,O,H,C,P,N
$$
before consuming the lower-real, upper-real, located-real, located-set, or Real-facing endpoint. The package exports no semantic liminf predicate, quotient $\RealUp$ equality, selected approximating sequence, countable choice, open-cover compactness, or ambient Real completeness.
\end{theorem}
\begin{proof}
First assemble the carrier and readback spine. The root epigraph carrier of \autoref{thm:lowersemicontinuous-root-epigraph-carrier} displays the finite rows $X,F,W,R,E,O,H,C,P,N$, and the regular readback handoff of \autoref{thm:lowersemicontinuous-root-regular-readback-handoff} forces every $\StreamNameUp$ and $\RegSeqRatUp$ read to pass through $W,R,E,O,H,C,P,N$ before any located endpoint is consumed.

Next attach the Real-facing and epigraph-facing boundaries. The Real seal boundary of \autoref{thm:lowersemicontinuous-root-real-seal-boundary} places the terminal $\RealUp$ seal after the same finite rows, while the root epigraph basis of \autoref{thm:lowersemicontinuous-root-epigraph-basis} identifies the lower-real epigraph rows generated by the scheduled windows and regular rational readbacks.

Now pass to located consumption. The root located readback theorem \autoref{thm:lowersemicontinuous-root-located-readback} keeps every located endpoint on the displayed route, the $\LocatedSetUp$ handoff of \autoref{thm:lowersemicontinuous-epigraph-locatedset-handoff} gives the located-set consumer, and exactness from \autoref{thm:lowersemicontinuous-epigraph-locatedset-exactness} says that consuming the located-set packet is equivalent to displaying those rows in one finite route.

Finally close the obligation package. Root nonescape from \autoref{thm:lowersemicontinuous-root-nonescape} excludes all absent analytic coordinates, finite threshold exhaustion from \autoref{thm:lowersemicontinuous-finite-epigraph-threshold-exhaustion} accounts for every displayed lower-bound threshold, and the $\LocatedSetUp$ threshold route of \autoref{thm:lowersemicontinuous-root-locatedset-threshold-route} places located-set threshold consumption downstream of the same rows. Thus the listed results form the paper-side $\obligationClosure$ surface and do not export semantic liminf data, quotient equality, selected sequences, choice, compactness, or ambient completeness.
\end{proof}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootEpigraphClosurePackage}

\begin{theorem}[LowerSemicontinuous root LocatedSet threshold route]
\label{thm:lowersemicontinuous-root-locatedset-threshold-route}
Every $\LocatedSetUp$ consumer of a $\LowerSemicontinuousUp$ root epigraph threshold must read the scheduled $\StreamNameUp$ window $W$, the $\RegSeqRatUp$ value row $R$, the lower-real epigraph ledger $E$, the comparison row $O$, the $\LocatedRealUp$ handoff, the terminal $\RealUp$ seal boundary, the $\hsame$ transport row $H$, the displayed $\Cont$ replay row $C$, the $\Pkg$ provenance row $P$, and the local $\NameCert$ row $N$ before it consumes the located-set packet. Thus the threshold route is the finite route
$$
W,R,E,O,H,C,P,N
$$
with the located-real and Real-seal boundary rows displayed at the comparison endpoint, not a host topology, semantic liminf operator, quotient $\RealUp$ equality, selected approximating sequence, countable choice schedule, open-cover compactness proof, or ambient Real-completeness theorem.
\end{theorem}
\begin{proof}
First build the root epigraph basis. Apply \autoref{thm:lowersemicontinuous-root-epigraph-basis} to expose the scheduled source window, regular rational value row, lower-real epigraph ledger, comparison row, transport, replay, provenance, and local name.

Next read the located endpoint. The located readback theorem \autoref{thm:lowersemicontinuous-root-located-readback} keeps every lower-bound read attached to the same $W,R,E,O,H,C,P,N$ route, and the exactness theorem \autoref{thm:lowersemicontinuous-epigraph-locatedset-exactness} identifies exactly when that route becomes consumable as a $\LocatedSetUp$ packet.

Finally apply finite threshold exhaustion from \autoref{thm:lowersemicontinuous-finite-epigraph-threshold-exhaustion}. It says that every displayed lower-bound threshold visible to lower-real, upper-real, located-real, or Real-facing consumers has already appeared in the same finite rows. Hence the located-set consumer must read the listed rows before consumption, and the excluded host analytic objects have no route into the packet.
\end{proof}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootLocatedSetThresholdRoute}

\begin{theorem}[LowerSemicontinuous root epigraph specialization]
\label{thm:lower-semicontinuous-root-epigraph-specialization}
Let an accepted $\LowerSemicontinuousUp$ root epigraph packet expose a concrete lower-bound threshold read. Then that read specializes the root route
$$
W,R,E,O,H,C,P,N
$$
to the scheduled $\StreamNameUp$ window, the matching $\RegSeqRatUp$ value row, the lower-real epigraph ledger, the comparison endpoint, transport, replay, provenance, and local naming rows already displayed by the packet. The specialization exports no semantic liminf predicate, selected infimum, host topology, quotient $\RealUp$ equality, countable-choice schedule, open-cover compactness proof, or ambient completeness theorem.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootEpigraphSpecialization}
\begin{proof}
Start with the root epigraph carrier and the regular readback handoff. They place the scheduled window $W$, value row $R$, epigraph ledger $E$, comparison row $O$, and structural rows $H,C,P,N$ before any concrete threshold read reaches a consumer.

Apply finite threshold exhaustion. Every lower-bound threshold visible to lower-real, upper-real, located-real, located-set, or Real-facing consumers has already appeared in the same scheduled rows, so specializing to one concrete threshold is just projecting that finite ledger.

Finally use the Real seal boundary and the root LocatedSet threshold route. The terminal seal and located-set consumer both remain downstream of $W,R,E,O,H,C,P,N$, and the excluded analytic objects have no carrier coordinate from which to enter the specialized read.
\end{proof}

\begin{theorem}[LowerSemicontinuous root LowerReal projection]
\label{thm:lower-semicontinuous-root-lowerreal-projection}
Before the terminal $\LocatedRealUp$ or $\RealUp$ seal of an accepted $\LowerSemicontinuousUp$ root epigraph packet is consumed, the comparison row $O$ projects the $\LowerRealUp$ and $\UpperRealUp$ boundary reads through the displayed $W,R,E$ threshold ledger. Thus a lower-real projection reaches the located endpoint only after the scheduled $\StreamNameUp$ and $\RegSeqRatUp$ rows have been displayed, and not by host real order, trichotomy, selected infimum, quotient equality, choice, compactness, or ambient completeness.
\end{theorem}
\begin{proof}
Project the comparison row from the root epigraph carrier. The row $O$ is downstream of the scheduled window, regular rational readback, and epigraph ledger, so its lower-real and upper-real reads are not independent endpoints.

Use the regular readback handoff and Real seal boundary. They force every lower or upper boundary value to pass through $W,R,E,O$ before the $\LocatedRealUp$ handoff or terminal $\RealUp$ seal can be consumed.

Finite threshold exhaustion supplies the nonescape clause. Since every visible threshold is already in the finite scheduled rows, the projection cannot be replaced by host order, trichotomy, selected infimum, quotient equality, choice, compactness, or ambient completeness.
\end{proof}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootLowerRealProjection}

\begin{theorem}[LowerSemicontinuous root LocatedSet nonescape]
\label{thm:lower-semicontinuous-root-locatedset-nonescape}
No $\LocatedSetUp$ consumer of an accepted $\LowerSemicontinuousUp$ root epigraph packet can bypass the finite scheduled $\StreamNameUp$ and $\RegSeqRatUp$ rows. Any such consumer must read the displayed route
$$
W,R,E,O,H,C,P,N
$$
including the lower-real epigraph ledger, located comparison endpoint, terminal Real-seal boundary, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row. The consumer receives no host topology, semantic liminf operator, selected approximating sequence, quotient $\RealUp$ equality, countable-choice schedule, open-cover compactness proof, or ambient Real-completeness theorem.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootLocatedSetNonescape}
\begin{proof}
Begin with the epigraph-to-$\LocatedSetUp$ handoff and exactness theorems. They identify located-set consumption with the scheduled window, regular rational value row, epigraph ledger, comparison endpoint, transport, replay, provenance, and local name.

Apply the root LocatedSet threshold route. It requires the located-set consumer to pass through the same $W,R,E,O,H,C,P,N$ rows before the located-set packet is consumed.

Finally use finite threshold exhaustion together with the Real seal boundary. The finite scheduled rows already contain every visible lower-bound threshold, and the terminal seal is downstream of those rows. Hence a located-set consumer has no route around the finite schedule and no coordinate for the excluded host analytic principles.
\end{proof}

\begin{theorem}[LowerSemicontinuous epigraph threshold monotonicity]
\label{thm:lower-semicontinuous-epigraph-threshold-monotonicity}
Let two displayed $\RealUp$ threshold rows of an accepted $\LowerSemicontinuousUp$ root epigraph packet be ordered by the dyadic tolerance ledger before the terminal seal is consumed. The later epigraph read refines the earlier read through the same finite route
$$
\StreamNameUp,\RegSeqRatUp,\LocatedRealUp,\LowerRealUp,\UpperRealUp,\LocatedSetUp
$$
and through the same $W,R,E,O,H,C,P,N$ rows. The refinement is therefore a located-set routing fact internal to the displayed packet, not a host real-order comparison, trichotomy principle, selected supremum, countable-choice schedule, or ambient completeness argument.
\end{theorem}
\begin{proof}
Start with finite threshold exhaustion from \autoref{thm:lowersemicontinuous-finite-epigraph-threshold-exhaustion}. It places every visible lower-bound threshold in the scheduled $\StreamNameUp$ and $\RegSeqRatUp$ rows before the epigraph ledger and comparison endpoint are read.

Next use the root epigraph basis and the regular readback handoff. The basis theorem \autoref{thm:lowersemicontinuous-root-epigraph-basis} keeps both threshold rows in the same $W,R,E,O,H,C,P,N$ carrier, while \autoref{thm:lowersemicontinuous-root-regular-readback-handoff} attaches each read to the corresponding located endpoint before any $\RealUp$ seal is exported.

Finally apply the LocatedSet threshold route of \autoref{thm:lowersemicontinuous-root-locatedset-threshold-route}. Since the dyadic tolerance ledger orders the displayed threshold rows before the located-set consumer reads them, the later row can only refine the earlier one along that finite route.

The exclusions follow by route exhaustion. No coordinate of the carrier supplies host real order, trichotomy, selected suprema, countable choice, or ambient completeness, so monotonicity is witnessed only by the displayed epigraph and located-set rows.
\end{proof}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousEpigraphThresholdMonotonicity}

\begin{theorem}[LowerSemicontinuous root epigraph carrier scope]
\label{thm:lower-semicontinuous-root-epigraph-carrier-scope}
Every accepted $\LowerSemicontinuousUp$ root epigraph packet reads its located-set predicate, $\LowerRealUp$ and $\UpperRealUp$ cut rows, regular rational stream witness, and terminal $\RealUp$ seal only through the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of the root epigraph carrier. The packet introduces no open-cover compactness proof, selected infimum, semantic liminf operator, host real order, quotient real equality, or countable-choice schedule.
\end{theorem}
\begin{proof}
Start with the root epigraph carrier of \autoref{thm:lowersemicontinuous-root-epigraph-carrier}. Its rows expose the source, graph, scheduled finite window, regular rational readback, lower-real epigraph ledger, comparison row, transport, replay, provenance, and local naming data.

The located-set predicate is reached through the handoff of \autoref{thm:lowersemicontinuous-epigraph-locatedset-handoff}. The lower and upper cuts are read through the regular readback handoff of \autoref{thm:lowersemicontinuous-root-regular-readback-handoff} and the root epigraph basis of \autoref{thm:lowersemicontinuous-root-epigraph-basis}.

The terminal $\RealUp$ seal is downstream of those same rows by \autoref{thm:lowersemicontinuous-root-real-seal-boundary}. Componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row keep the located-set and real-cut reads attached to the accepted $\BHist$ packet.

The exclusions are coordinate exhaustion. Since the carrier contains no open-cover compactness proof, selected infimum, host real order, semantic liminf operator, quotient real equality, or countable-choice schedule, the scoped root epigraph read cannot export any of them.
\end{proof}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootEpigraphCarrierScope}

\begin{theorem}[LowerSemicontinuous root epigraph coverage]
\label{thm:lowersemicontinuous-root-epigraph-coverage}
Every accepted lower-bound threshold of a $\LowerSemicontinuousUp$ root epigraph packet is covered by the displayed route
$$
W,R,E
$$
before any $\LocatedRealUp$ or $\RealUp$-facing consumer reads it. The finite window row $W$, regular rational readback $R$, and epigraph ledger $E$ therefore exhaust threshold coverage before the comparison, transport, replay, provenance, and local NameCert rows are consumed.
\end{theorem}
\begin{proof}
Start with the root epigraph carrier of \autoref{thm:lowersemicontinuous-root-epigraph-carrier}. It displays the scheduled finite window, regular rational value row, and lower-real epigraph ledger before the comparison endpoint.

Apply the regular readback handoff of \autoref{thm:lowersemicontinuous-root-regular-readback-handoff}. A threshold can reach a located or Real-facing consumer only after the $W,R,E$ route has already attached the scheduled value to its epigraph witness.

Finally use the root epigraph carrier scope of \autoref{thm:lower-semicontinuous-root-epigraph-carrier-scope}. The remaining rows $O,H,C,P,N$ preserve and name the covered threshold; they do not create a hidden threshold outside the displayed finite window.
\end{proof}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootEpigraphCoverage}

\begin{theorem}[LowerSemicontinuous root epigraph coverage exactness]
\label{thm:lowersemicontinuous-root-epigraph-coverage-exactness}
An accepted lower-bound threshold of a $\LowerSemicontinuousUp$ root epigraph packet is covered exactly when the scheduled $\StreamNameUp$ window $W$, $\RegSeqRatUp$ value row $R$, epigraph ledger $E$, located comparison endpoint $O$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert_{\LowerSemicontinuousUp}$ row $N$ are all displayed in one route. The exactness boundary exports no semantic liminf predicate, quotient $\RealUp$ equality, selected infimum, countable choice, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootEpigraphCoverageExactness}
\begin{proof}
For the forward direction, start with the root epigraph coverage theorem. It places every accepted lower-bound threshold in the scheduled window, regular rational value row, and epigraph ledger before any located or Real-facing consumer reads the threshold.

Apply finite epigraph threshold exhaustion. The theorem forces any displayed threshold visible to lower-real, upper-real, located-real, or Real-facing consumers to pass through the same $W,R,E,O,H,C,P,N$ route.

Use the LocatedSet threshold route to identify the comparison endpoint. The located-set consumer can read the threshold only after $O$ and the structural rows $H,C,P,N$ are displayed with the same scheduled value.

For the reverse direction, project an accepted route with all listed rows. The scheduled window and regular value row provide the finite threshold read, the epigraph ledger covers it, and the located comparison endpoint keeps the lower-bound threshold attached to the same packet.

Finally apply the root epigraph carrier scope. Since semantic liminf, quotient equality, selected infimum, countable choice, and ambient completeness have no carrier coordinate, the exactness statement is only the displayed finite coverage boundary.
\end{proof}

\begin{theorem}[LowerSemicontinuous root located comparison]
\label{thm:lowersemicontinuous-root-located-comparison}
The comparison row $O$ of an accepted $\LowerSemicontinuousUp$ root epigraph packet compares $\LowerRealUp$, $\UpperRealUp$, and $\LocatedRealUp$ endpoints only after the epigraph ledger $E$ has been displayed from the scheduled $W,R$ readback. Thus located comparison is downstream of finite epigraph evidence, not a host order comparison, trichotomy principle, selected infimum, or quotient real equality.
\end{theorem}
\begin{proof}
Project the rows of \autoref{thm:lowersemicontinuous-root-epigraph-carrier}. The row order places $W$ and $R$ before $E$, and places $O$ after the displayed epigraph ledger.

Use \autoref{thm:lowersemicontinuous-root-regular-readback-handoff}. It keeps every $\RegSeqRatUp$ readback attached to the same lower-bound witness before $O$ may compare the lower, upper, and located endpoints.

The scope theorem \autoref{thm:lower-semicontinuous-root-epigraph-carrier-scope} supplies the boundary. Since host order, trichotomy, selected infimum, and quotient equality have no coordinate in the carrier, the comparison row can only read the displayed epigraph ledger.
\end{proof}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootLocatedComparison}

\begin{theorem}[LowerSemicontinuous root Real seal refusal]
\label{thm:lowersemicontinuous-root-real-seal-refusal}
The terminal $\RealUp$ seal of a $\LowerSemicontinuousUp$ root epigraph packet exports no semantic liminf operator, selected infimum, host topology, quotient equality, countable-choice schedule, or ambient completeness theorem. It seals only the displayed $W,R,E,O,H,C,P,N$ route supplied by the finite epigraph carrier.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootRealSealRefusal}
\begin{proof}
Begin with the Real seal boundary of \autoref{thm:lowersemicontinuous-root-real-seal-boundary}. It places the terminal seal after the finite epigraph window, comparison row, transport, replay, provenance, and local naming rows.

Apply root epigraph coverage and located comparison above. Coverage accounts for every accepted lower-bound threshold through $W,R,E$, while the comparison row $O$ reads lower, upper, and located endpoints only after that ledger is displayed.

Finally use \autoref{thm:lower-semicontinuous-root-epigraph-carrier-scope}. The refused liminf, selected infimum, topology, quotient equality, choice, and completeness objects have no carrier coordinate, so the terminal seal cannot export them.
\end{proof}

\begin{theorem}[LowerSemicontinuous root epigraph finite infimum handoff]
\label{thm:lowersemicontinuous-root-epigraph-finite-infimum-handoff}
Any finite lower-bound infimum read consumed by a $\LowerSemicontinuousUp$ root epigraph packet must pass through the scheduled $\StreamNameUp$ window, the $\RegSeqRatUp$ value row, the lower-real epigraph ledger, the located comparison row, the terminal $\RealUp$ seal boundary, componentwise transport, displayed replay, provenance, and local $\NameCert$ rows. The packet exports no selected infimum, semantic liminf operator, host topology, quotient $\RealUp$ equality, countable-choice schedule, open-cover compactness proof, or ambient completeness theorem.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootEpigraphFiniteInfimumHandoff}
\begin{proof}
Start with the root epigraph carrier of \autoref{thm:lowersemicontinuous-root-epigraph-carrier}. Its finite route displays the scheduled window, regular rational value row, lower-real epigraph ledger, comparison row, transport, replay, provenance, and local naming before any real-facing endpoint is consumed.

Apply finite threshold exhaustion through \autoref{thm:lowersemicontinuous-finite-epigraph-threshold-exhaustion}. Every lower-bound threshold read is accounted for inside the scheduled $\StreamNameUp$ window and the $\RegSeqRatUp$ value row, so an infimum-facing finite read cannot skip the epigraph ledger.

Use located comparison from \autoref{thm:lowersemicontinuous-root-located-comparison}. The comparison row reads lower, upper, and located endpoints only after the epigraph ledger has been displayed, so the lower-bound infimum route remains a finite BEDC comparison route rather than a host-order selection.

Finally apply the Real seal refusal of \autoref{thm:lowersemicontinuous-root-real-seal-refusal}. The terminal seal refuses semantic liminf data, selected infima, host topology, quotient equality, choice schedules, compactness proofs, and ambient completeness. Hence the finite infimum handoff exports only the displayed $W,R,E,O,H,C,P,N$ route.
\end{proof}

\begin{theorem}[LowerSemicontinuous epigraph threshold refinement]
\label{thm:lowersemicontinuous-epigraph-threshold-refinement}
Let two displayed $\RealUp$ threshold rows of an accepted $\LowerSemicontinuousUp$ root epigraph packet be ordered before the terminal seal is consumed. The refined row reaches every $\LowerRealUp$, $\UpperRealUp$, $\LocatedRealUp$, $\LocatedSetUp$, and $\RealUp$ consumer through the same
$$
W,R,E,O,H,C,P,N
$$
route as the coarser row, with the refinement witnessed before the terminal seal and not by a host real-order comparison, selected infimum, semantic liminf operator, countable-choice schedule, open-cover compactness proof, or ambient completeness theorem.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousEpigraphThresholdRefinement}
\begin{proof}
Start with finite epigraph threshold exhaustion. It places both displayed threshold rows in the scheduled $\StreamNameUp$ window and $\RegSeqRatUp$ readback rows before the epigraph ledger is consumed.

Use the LocatedSet threshold route and epigraph threshold monotonicity. The located-set consumer reads the same $W,R,E,O,H,C,P,N$ rows, and the ordered threshold ledger lets the refined row sharpen the coarser read only along that finite route.

Finally apply the root epigraph carrier scope. The terminal $\RealUp$ seal is downstream of the displayed transport, replay, provenance, and local naming rows, so the refinement cannot import host order, selected infima, semantic liminf data, choice, compactness, or ambient completeness.
\end{proof}

\begin{theorem}[LowerSemicontinuous located epigraph filter stability]
\label{thm:lowersemicontinuous-located-epigraph-filter-stability}
Every located-set filter consumer of an accepted $\LowerSemicontinuousUp$ root epigraph packet reads the same finite epigraph threshold rows under componentwise $\hsame$ transport and displayed $\Cont$ replay. Thus a filter-stable located epigraph read is carried by
$$
W,R,E,O,H,C,P,N
$$
and exports no detached topology, quotient $\RealUp$ equality, selected approximating sequence, countable-choice schedule, open-cover compactness proof, or ambient Real-completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousLocatedEpigraphFilterStability}
\begin{proof}
Begin with the epigraph-to-$\LocatedSetUp$ exactness theorem. It identifies located-set consumption with the scheduled window, regular rational value row, epigraph ledger, comparison endpoint, transport, replay, provenance, and local name.

Apply finite threshold exhaustion and threshold refinement. Every lower-bound threshold visible to the located filter is already displayed in the same finite rows, and ordered threshold reads refine before the terminal seal.

Transport stability is then carrier-local. The $\hsame$ row preserves the displayed threshold rows componentwise, and the $\Cont$ row replays the same route for the filter consumer. The carrier scope theorem excludes detached topology, quotient equality, selection, choice, compactness, and ambient completeness from the endpoint.
\end{proof}

\begin{theorem}[LowerSemicontinuous epigraph filter route]
\label{thm:lowersemicontinuous-epigraph-filter-route}
Let an accepted $\LowerSemicontinuousUp$ root epigraph packet expose threshold rows that are stable under located comparison. Then every filter-facing read of its epigraph factors through $\LocatedSetUp$ rows, $\LowerRealUp$ and $\UpperRealUp$ boundary rows, $\RegSeqRatUp$ readback, $\StreamNameUp$ windows, and the terminal $\RealUp$ seal along the same
$$
W,R,E,O,H,C,P,N
$$
route. The exported route contains no selected minimizer, quotient function equality, detached topology, selected approximating sequence, countable-choice schedule, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousEpigraphFilterRoute}
\begin{proof}
Start with the root epigraph carrier. The rows $W,R,E,O,H,C,P,N$ place the stream window, regular rational readback, epigraph ledger, located comparison endpoint, transport, replay, provenance, and local name before any filter consumer may inspect the packet.

Use the located readback and epigraph-to-$\LocatedSetUp$ handoff. These rows identify the filter-facing epigraph as a located-set read whose lower and upper real boundary data are still tied to the same displayed threshold window.

Apply finite threshold exhaustion and threshold refinement. Every threshold requested by the filter is already present in the finite ledger, and each ordered located comparison refines through the displayed lower, upper, and located endpoint rows.

Finally use located epigraph filter stability. Componentwise $\hsame$ transport and displayed $\Cont$ replay preserve the same route for the filter consumer, while the root carrier scope excludes minimizer selection, quotient function equality, detached topology, selected sequences, choice schedules, and ambient completeness.
\end{proof}

\begin{theorem}[LowerSemicontinuous root epigraph filter directedness]
\label{thm:lowersemicontinuous-root-epigraph-filter-directedness}
Let an accepted $\LowerSemicontinuousUp$ root packet expose two epigraph threshold/filter rows over the same $\RealUp$ seal and $\RegSeqRatUp$ readback source. Then the displayed located comparison, epigraph threshold refinement, and epigraph filter route admit a common refined threshold row carried by the same finite route
$$
W,R,E,O,H,C,P,N .
$$
The common row is a $\BHist$ and $\hsame$-stable filter refinement whose $\Cont$ replay and $\NameCert$ provenance remain internal to the accepted packet. It exports no host liminf theorem, open-sublevel theorem, detached topology, selected approximating sequence, quotient $\RealUp$ equality, countable-choice schedule, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootEpigraphFilterDirectedness}
\begin{proof}
Start with the two displayed threshold/filter rows. Since they share the same $\RealUp$ seal and $\RegSeqRatUp$ readback source, both rows are already visible through the scheduled window $W$, regular value row $R$, epigraph ledger $E$, and located comparison endpoint $O$.

Apply the root located comparison theorem and the epigraph threshold refinement theorem. Located comparison keeps both lower-bound reads attached to the same $W,R,E,O$ route, while threshold refinement supplies a row below the two displayed reads before the terminal seal is consumed.

Now use the epigraph filter route. The filter-facing read of each threshold factors through the same located-set, lower-real, upper-real, regular-readback, stream-window, and Real-seal rows, so the refined threshold is a common filter row rather than a second host filter object.

Finally repack through the structural rows. Componentwise $\hsame$ transport keeps the common row in the same $\BHist$ packet, $\Cont$ replay preserves the route for consumers, and $P,N$ supply provenance and local naming. The excluded host liminf, open-sublevel, topology, selector, quotient, choice, and completeness objects have no coordinate in this directedness witness.
\end{proof}

\begin{theorem}[LowerSemicontinuous root epigraph filter basis closure]
\label{thm:lowersemicontinuous-root-epigraph-filter-basis-closure}
Every accepted $\LowerSemicontinuousUp$ root epigraph filter basis over the same $\RealUp$ seal and $\RegSeqRatUp$ readback source is closed under displayed threshold refinement and common filter rows along
$$
W,R,E,O,H,C,P,N .
$$
The closure read is a $\BHist$-routed basis package: lower thresholds refine through the epigraph ledger, common rows remain under the same located comparison endpoint, and componentwise $\hsame$ transport plus displayed $\Cont$ replay keep provenance and local naming internal to the accepted packet. It exports no host liminf predicate, detached topology, selected approximating sequence, quotient $\RealUp$ equality, countable choice schedule, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootEpigraphFilterBasisClosure}
\begin{proof}
Begin with finite threshold exhaustion. \autoref{thm:lowersemicontinuous-finite-epigraph-threshold-exhaustion} places every displayed lower-bound request inside the scheduled $\StreamNameUp$ window, $\RegSeqRatUp$ readback, epigraph ledger, comparison endpoint, transport, replay, provenance, and local naming route.

Next apply epigraph threshold refinement and located epigraph filter stability. \autoref{thm:lowersemicontinuous-epigraph-threshold-refinement} supplies refined threshold rows before the terminal seal is consumed, while \autoref{thm:lowersemicontinuous-located-epigraph-filter-stability} keeps those rows stable under componentwise transport and replay.

Now use the epigraph filter route and Real threshold boundary. \autoref{thm:lowersemicontinuous-epigraph-filter-route} forces every filter-facing read through the same located-set, lower-real, upper-real, regular-readback, stream-window, and Real-seal rows, and \autoref{thm:lowersemicontinuous-root-filter-real-threshold} keeps any Real-facing threshold downstream of the displayed finite route.

Finally repack with directedness. \autoref{thm:lowersemicontinuous-root-epigraph-filter-directedness} supplies the common refined filter row over the shared $\RealUp$ seal and $\RegSeqRatUp$ source; the structural rows $H,C,P,N$ name that row as a basis closure witness. Since the carrier has no coordinate for host liminf, detached topology, selectors, quotient equality, choice, or ambient completeness, none of those objects is exported by the closure package.
\end{proof}

\begin{theorem}[LowerSemicontinuous root epigraph filter-basis exactness]
\label{thm:lowersemicontinuous-root-epigraph-filter-basis-exactness}
An accepted $\LowerSemicontinuousUp$ root epigraph filter-basis read is exact precisely when the displayed route
$$
W,R,E,O,H,C,P,N
$$
is present and every common refinement is obtained from epigraph threshold refinement followed by root epigraph filter directedness. Thus the exact basis has no hidden host topology, selected infimum, semantic liminf predicate, quotient $\RealUp$ equality, countable-choice schedule, or ambient completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootEpigraphFilterBasisExactness}
\begin{proof}
For the forward read, apply \autoref{thm:lowersemicontinuous-root-epigraph-filter-basis-closure}. It keeps the basis inside the displayed scheduled-window, regular-readback, epigraph-ledger, located-comparison, transport, replay, provenance, and local naming route.

The refinement witness is forced by the finite threshold route. Use \autoref{thm:lowersemicontinuous-epigraph-threshold-refinement} to refine displayed lower-bound thresholds before the terminal $\RealUp$ seal is consumed, and then apply \autoref{thm:lowersemicontinuous-root-epigraph-filter-directedness} to obtain the common filter row over the same $\RegSeqRatUp$ source and $\RealUp$ seal.

Conversely, suppose the route $W,R,E,O,H,C,P,N$ is present and the common refinement is supplied by threshold refinement plus filter directedness. The Real-facing threshold is already downstream of the finite packet by \autoref{thm:lowersemicontinuous-root-filter-real-threshold}, so no outside topology, infimum selector, liminf predicate, quotient equality, choice schedule, or completeness principle can enter the basis read. This is exactly the finite epigraph basis advertised by the accepted root packet.
\end{proof}

\begin{theorem}[LowerSemicontinuous root filter-basis directed refinement]
\label{thm:lowersemicontinuous-root-filter-basis-directed-refinement}
Let two displayed $\LowerSemicontinuousUp$ root epigraph filter-basis rows lie over the same $W,R,E,O,H,C,P,N$ route. They admit a common displayed refinement only through epigraph threshold refinement, located epigraph filter stability, and root epigraph filter directedness. The refinement exports no host liminf theorem, open-sublevel theorem, detached topology, selected approximating sequence, quotient $\RealUp$ equality, countable choice, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootFilterBasisDirectedRefinement}
\begin{proof}
Begin with the filter-basis closure package. \autoref{thm:lowersemicontinuous-root-epigraph-filter-basis-closure} keeps both basis rows inside the same scheduled window, regular readback, epigraph ledger, located comparison, transport, replay, provenance, and local naming route.

Use epigraph threshold refinement next. \autoref{thm:lowersemicontinuous-epigraph-threshold-refinement} supplies the only permitted way to refine the displayed lower-bound thresholds before the terminal $\RealUp$ seal is consumed.

Finally apply filter directedness. \autoref{thm:lowersemicontinuous-root-epigraph-filter-directedness} provides the common refined filter row over the shared $\RealUp$ seal and $\RegSeqRatUp$ source, while located epigraph filter stability keeps componentwise transport and replay inside the accepted packet. Since none of the refused host objects appears as a carrier coordinate, the directed refinement is exactly the displayed finite witness.
\end{proof}

\begin{theorem}[LowerSemicontinuous root filter real threshold]
\label{thm:lowersemicontinuous-root-filter-real-threshold}
Every accepted $\LowerSemicontinuousUp$ root epigraph filter read that reaches a $\RealUp$-facing threshold must first expose the $\LowerRealUp$, $\UpperRealUp$, $\LocatedRealUp$, $\LocatedSetUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows already named by the root carrier. In carrier notation the threshold route is the finite read
$$
W,R,E,O,H,C,P,N
$$
with the scheduled window $W$, regular rational value row $R$, epigraph ledger $E$, located comparison endpoint $O$, transport row $H$, displayed replay row $C$, provenance row $P$, and local naming row $N$ visible before the Real-facing threshold is public. The route exports no semantic liminf operator, quotient $\RealUp$ equality, selected approximating sequence, countable choice schedule, open-cover compactness proof, or ambient Real-completeness theorem.
\end{theorem}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootFilterRealThreshold}
\begin{proof}
Start with the epigraph filter route above. It forces every filter-facing read to pass through the located-set handoff, the lower and upper real boundary rows, the scheduled stream window, the regular rational readback, and the terminal Real seal on the same displayed route.

Use located comparison and located readback. The comparison row $O$ is the only endpoint at which $\LowerRealUp$, $\UpperRealUp$, and $\LocatedRealUp$ data meet, and it is downstream of the finite $W,R,E$ threshold ledger rather than a host order predicate.

Finally apply the Real seal refusal block. Since the terminal $\RealUp$ seal can only seal the already displayed $W,R,E,O,H,C,P,N$ route, a Real-facing threshold cannot be read before those rows are exposed. The refused liminf, quotient-equality, selection, choice, compactness, and completeness objects have no carrier coordinate from which to enter the threshold read.
\end{proof}

\begin{theorem}[LowerSemicontinuous root epigraph threshold continuity]
\label{thm:lowersemicontinuous-root-epigraph-threshold-continuity}
Every accepted $\LowerSemicontinuousUp$ root epigraph filter read preserves the same threshold row when transported through the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\LowerRealUp$, $\UpperRealUp$, located-comparison, $\LocatedSetUp$, $\RealUp$ seal, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. In carrier notation, if the threshold is read along
$$
W,R,E,O,H,C,P,N ,
$$
then every transported filter-facing threshold read of the same accepted packet reads that same row through the same finite route. The transport exports no host liminf predicate, quotient $\RealUp$ equality, selected approximating sequence, countable-choice schedule, open-cover compactness proof, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Begin with the root epigraph carrier and the regular readback handoff. The carrier fixes $W,R,E,O,H,C,P,N$ as the scheduled window, regular rational value row, epigraph ledger, located comparison endpoint, transport, replay, provenance, and local naming route, while the readback handoff keeps the threshold downstream of the $\StreamNameUp$ and $\RegSeqRatUp$ rows.

Use located comparison next. The $\LowerRealUp$ and $\UpperRealUp$ rows meet only at the located comparison endpoint $O$, and the $\LocatedSetUp$ read is attached to that endpoint before the terminal $\RealUp$ seal is exposed.

Apply filter directedness and filter-basis exactness. Directedness supplies common displayed threshold rows for filter-facing reads over the same source, and exactness says those rows are obtained by epigraph threshold refinement rather than by an external topological or liminf object.

Finally use \autoref{thm:lowersemicontinuous-root-filter-real-threshold}. It places every Real-facing threshold downstream of the same $W,R,E,O,H,C,P,N$ route, so $\hsame$ transport and $\Cont$ replay preserve the displayed row without introducing host liminf, quotient equality, selectors, countable choice, open-cover compactness, or ambient completeness.
\end{proof}
\leanchecked{BEDC.Derived.LowerSemicontinuousUp.LowerSemicontinuousRootEpigraphThresholdContinuity}
