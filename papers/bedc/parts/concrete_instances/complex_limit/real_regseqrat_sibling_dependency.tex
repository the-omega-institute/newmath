The $\ComplexLimitUp$ coordinatewise limit surface reads its real-coordinate and regular-rational evidence through \autoref{ch:concrete-instances-real-namecert} and \autoref{ch:concrete-instances-regseqrat-namecert} before any complex limit consumer receives a seal. This dependency keeps complex convergence on the displayed $\RealUp$ and $\RegSeqRatUp$ route rather than on host complex completeness, quotient stream equality, or selected limit data.
