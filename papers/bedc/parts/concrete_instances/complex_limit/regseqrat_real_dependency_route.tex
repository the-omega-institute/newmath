Before any $\ComplexLimitUp$ seal is read, the packet consumes the real-coordinate sibling of \autoref{ch:concrete-instances-real-namecert} and the regular-rational sibling of \autoref{ch:concrete-instances-regseqrat-namecert}. This route keeps complex-limit convergence tied to displayed $\RealUp$ and $\RegSeqRatUp$ carrier rows rather than to host complex completeness, quotient stream equality, or selected coordinate limits.
