\chapter{A Concrete Naming Certificate for $\PrimeShiftAnalyticCompatibilityUp$}
\label{ch:concrete-instances-prime-shift-analytic-compatibility-namecert}
\origin{ai}

The $\PrimeShiftAnalyticCompatibilityUp$ packet realizes the compatibility surface of \autoref{ch:visions-rh-prime-shift-analytic-compatibility} as finite NameCert data. It records nested prime-cell rows, digit-congruence rows, cocycle-transition rows, character-flow rows, compact normal-convergence rows, logarithmic-derivative windows, and no-radial-anomaly refusals as one named carrier rather than as an ambient analytic-continuation theorem. The packet is RH-facing only as finite interface data; it does not close analytic continuation, the Euler product, the functional equation, zero exclusion, a Hilbert--Polya route, or the Riemann hypothesis.

\begin{definition}[Prime-shift analytic compatibility carrier]
\label{def:prime-shift-analytic-compatibility-carrier}
A $\PrimeShiftAnalyticCompatibilityUp$ carrier is a finite $\BHist$ packet
$$
A=(C,D,K,F,U,L,Z,H,R,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $C$ is the nested prime-cell row, listing the displayed clopen cells and their finite refinement order.
\item $D$ is the digit-congruence row for the $p$-adic address windows named by $C$.
\item $K$ is the cocycle-transition ledger stating how two-step refinements are replayed through the same finite transition rows.
\item $F$ is the character-flow row recording the displayed frequency and the finite character readouts consumed by the tower.
\item $U$ is the compact normal-convergence row for the finite holomorphic readouts into $\ComplexUp$.
\item $L$ is the logarithmic-derivative window row on zero-free compact windows named by the packet.
\item $Z$ is the no-radial-anomaly refusal row, separating unit phase and compact prime-fibre data from any unledgered positive-real scale.
\item $H$ records componentwise $\hsame$ transport for $C,D,K,F,U,L,Z,R,P,N$.
\item $R$ records displayed $\Cont$ replay from a compatibility request through the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\PrimeShiftAnalyticCompatibilityUp$, $\PrimeUp$, $\ComplexUp$, $\ZetaUp$, prime-shift frontier rows, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PrimeShiftAnalyticCompatibilityUp}$ row.
\end{enumerate}
The carrier predicate compares accepted packets only through these rows. It has no coordinate for analytic continuation, Euler-product convergence, a completed zero theorem, a selected prime-fibre limit outside the ledger, or an ambient complex-analytic oracle.
\end{definition}

\begin{theorem}[Prime-shift analytic compatibility NameCert obligations]
\label{thm:prime-shift-analytic-compatibility-namecert-obligations}
For every accepted $\PrimeShiftAnalyticCompatibilityUp$ carrier $A=(C,D,K,F,U,L,Z,H,R,P,N)$, the local $\NameCert_{\PrimeShiftAnalyticCompatibilityUp}$ surface consists exactly of nested cell exposure through $C$, digit congruence through $D$, cocycle replay through $K$, character-flow exposure through $F$, compact normal-convergence exposure through $U$, logarithmic-derivative window exposure through $L$, no-radial-anomaly refusal through $Z$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $R$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The rows $C,D,K,F$ are the prime-shift skeleton: nested cells, displayed address congruences, coherent refinements, and character-flow readouts.

The analytic rows are finite window data. Normal convergence is read through the compact row $U$, logarithmic derivatives are read through the zero-free windows in $L$, and the refusal row $Z$ prevents a positive-real scale from being absorbed into the prime-shift fibre.

The structural rows preserve the same carrier. Transport through $H$, replay through $R$, provenance through $P$, and local naming through $N$ do not add analytic continuation or a zero theorem, so the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Prime-shift analytic compatibility RH handoff]
\label{thm:prime-shift-analytic-compatibility-rh-handoff}
Every RH-facing consumer of an accepted $\PrimeShiftAnalyticCompatibilityUp$ packet receives only the finite route
$$
C\longmapsto D\longmapsto K\longmapsto F\longmapsto U\longmapsto L\longmapsto Z\longmapsto H\longmapsto R\longmapsto P\longmapsto N .
$$
Thus the packet can hand compatible prime-shift tower data to later zeta or zero-packet interfaces, but it does not assert analytic continuation, Euler-product convergence, functional-equation closure, zero exclusion, a Hilbert--Polya route, or the Riemann hypothesis.
\end{theorem}
\begin{proof}
Use \autoref{thm:prime-shift-analytic-compatibility-namecert-obligations}. The prime-shift skeleton is read through the nested-cell, digit, cocycle, and character-flow rows $C,D,K,F$.

Read the analytic compatibility rows next. The compact normal-convergence row $U$, logarithmic-derivative row $L$, and no-radial-anomaly row $Z$ are finite windows and refusals, not a completed continuation theorem.

Finally replay the structural rows $H,R,P,N$. They transport, replay, source, and name the compatibility packet for later RH-facing consumers while preserving the absence of a zero theorem, Euler-product theorem, functional-equation theorem, Hilbert--Polya route, and Riemann-hypothesis conclusion.
\end{proof}

The Lean \texttt{TasteGate.lean} surface for this packet must include \texttt{Nontrivial} and \texttt{FieldFaithful} instances so the prime-cell, digit, cocycle, character-flow, compact-window, logarithmic-window, and no-radial-anomaly rows cannot collapse into a smoke carrier.

\falsifiablePrediction{An accepted $\PrimeShiftAnalyticCompatibilityUp$ consumer that reaches an RH-facing handoff without the nested prime-cell row, digit-congruence row, cocycle-transition ledger, character-flow row, compact normal-convergence row, logarithmic-derivative window row, no-radial-anomaly refusal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row refutes the packet.}

\independenceWitness{prime, padic, complex, zeta, prime-shift-frontier: no listed sibling supplies the joint packet $A=(C,D,K,F,U,L,Z,H,R,P,N)$, because prime rows, $p$-adic address rows, complex readout rows, zeta-facing rows, and frontier refusals become RH-facing only when the carrier binds them with transport, replay, provenance, and local naming.}

\closureat{PrimeShiftAnalyticCompatibilityUp}{seedStr}

\begin{closurestatus}{\PrimeShiftAnalyticCompatibilityUp}
  \constructivestory{A $\PrimeShiftAnalyticCompatibilityUp$ packet is generated from nested prime-cell rows, digit-congruence rows, cocycle-transition rows, character-flow rows, compact normal-convergence rows, logarithmic-derivative windows, no-radial-anomaly refusals, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:prime-shift-analytic-compatibility-carrier}, \autoref{thm:prime-shift-analytic-compatibility-namecert-obligations}, and \autoref{thm:prime-shift-analytic-compatibility-rh-handoff} seed the finite prime-shift compatibility route for RH-facing consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim analytic continuation, Euler-product convergence, functional-equation closure, zero exclusion, a Hilbert--Polya route, the Riemann hypothesis, checked Lean obligations, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, nested-cell exposure, digit congruence, cocycle replay, character-flow exposure, compact-window exactness, logarithmic-window exactness, no-radial-anomaly nonescape, transport, replay, provenance, and local naming for BEDC.Derived.PrimeShiftAnalyticCompatibilityUp.}
\end{closurestatus}
