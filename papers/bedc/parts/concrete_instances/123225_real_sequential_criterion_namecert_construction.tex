\chapter{A Concrete Naming Certificate for $\RealSequentialCriterionUp$}
\label{ch:concrete-instances-real-sequential-criterion-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RealSequentialCriterionUp$ packet records the finite criterion surface where displayed convergence data, late Cauchy readiness, and the terminal $\RealUp$ seal are read in one sequential route. It sits after the real-name boundary of \autoref{ch:concrete-instances-real-namecert}, the finite window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the convergence-to-Cauchy handoff of \autoref{ch:concrete-instances-convergent-real-sequence-cauchy-namecert}, and the Real Cauchy criterion surface of \autoref{ch:concrete-instances-real-cauchy-criterion-namecert}. The packet names the constructive sequential criterion as finite NameCert data; it does not assert ambient completeness, choose limits for arbitrary streams, quotient sequence names, or import host real equality.

\begin{definition}[Real sequential criterion carrier]
\label{def:real-sequential-criterion-carrier}
A $\RealSequentialCriterionUp$ carrier is a finite $\BHist$ packet
$$
S=(X,W,R,D,L,K,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the displayed real-sequence source row whose terms are read only through $\RealUp$ names.
\item $W$ is the $\StreamNameUp$ finite-window row scheduling the inspected sequence entries and comparison windows.
\item $R$ is the $\RegSeqRatUp$ readback row for the rational names used inside the displayed windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger splitting late comparison budgets into finite dyadic cells.
\item $L$ is the displayed convergence row, consumed through the convergence-to-Cauchy handoff of $\ConvergentRealSequenceCauchyUp$.
\item $K$ is the $\RealCauchyCriterionUp$ row naming the corresponding late-pair Cauchy readiness surface.
\item $E$ is the terminal $\RealUp$ seal row for sequence values and displayed limit reads.
\item $H$ records componentwise $\hsame$ transport for the sequence, window, readback, dyadic, convergence, Cauchy, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from convergence windows to the late-pair Cauchy test and then to the terminal real-name seal.
\item $P$ is the $\Pkg$ provenance over $\RealSequentialCriterionUp$, $\RealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\ConvergentRealSequenceCauchyUp$, $\RealCauchyCriterionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RealSequentialCriterionUp}$ row.
\end{enumerate}
The carrier has no coordinate for selecting a limit from an arbitrary Cauchy stream, quotienting real sequences, invoking trichotomy, using countable choice, or asserting ambient completeness of $\RealUp$.
\end{definition}
\leanchecked{BEDC.Derived.RealSequentialCriterionUp.RealSequentialCriterionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[RealSequentialCriterion NameCert obligations]
\label{thm:real-sequential-criterion-namecert-obligations}
For every accepted $\RealSequentialCriterionUp$ carrier
$$
S=(X,W,R,D,L,K,E,H,C,P,N),
$$
the local $\NameCert_{\RealSequentialCriterionUp}$ surface consists exactly of carrier admission for the displayed real-sequence source, finite StreamName windows, regular rational readback, dyadic tolerance ledger, displayed convergence row, Real Cauchy criterion row, terminal Real seal, componentwise transport, displayed replay, provenance, and local naming. Every obligation is read through the sibling routes named in \autoref{ch:concrete-instances-real-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-convergent-real-sequence-cauchy-namecert}, and \autoref{ch:concrete-instances-real-cauchy-criterion-namecert}.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-sequential-criterion-carrier}. The source row $X$ is visible only together with the finite windows $W$, regular readback $R$, and dyadic tolerance ledger $D$.

Read the two criterion rows next. The convergence row $L$ supplies the displayed-limit side through \autoref{ch:concrete-instances-convergent-real-sequence-cauchy-namecert}, while the Cauchy row $K$ supplies the late-pair readiness side through \autoref{ch:concrete-instances-real-cauchy-criterion-namecert}.

Finally the seal, transport, replay, provenance, and naming rows $E,H,C,P,N$ preserve the same route. Since the carrier lists no additional coordinate, those rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[RealSequentialCriterion Cauchy equivalence route]
\label{thm:real-sequential-criterion-cauchy-equivalence}
Every accepted $\RealSequentialCriterionUp$ packet exposes convergence-facing and Cauchy-facing reads over the same finite sequence route. A consumer may pass from the displayed convergence row $L$ to the Cauchy criterion row $K$ only after $X,W,R,D$ have been read, and may pass from $K$ back to a displayed sequential criterion read only through the same terminal seal $E$ and structural rows $H,C,P,N$. The exported equivalence is a route equality of finite NameCert data, not an ambient completeness theorem for $\RealUp$.
\end{theorem}
\begin{proof}
For the convergence-to-Cauchy direction, first expose the displayed sequence source $X$, the finite StreamName windows $W$, the regular rational readback $R$, and the dyadic tolerance ledger $D$. The convergence row $L$ can then be consumed by the convergence-to-Cauchy handoff, producing the late-pair criterion row $K$ on the same finite windows.

For the reverse read, start from the accepted Cauchy criterion row $K$ inside the same packet. The row is not free-standing: the carrier keeps it attached to $X,W,R,D,E,H,C,P,N$, so any sequential-criterion consumer receives the same real-name seal and the same replay route.

Thus the criterion compares two public reads of one finite packet. It identifies the convergence-facing and Cauchy-facing routes only after their shared rows remain visible, and it exports no selected limit, quotient stream equality, countable-choice schedule, trichotomy branch, or ambient real completeness.
\end{proof}

\falsifiablePrediction{A packet that omits either the displayed convergence row $L$ or the Cauchy criterion row $K$ can still name a real-sequence window, but it cannot expose the RealSequentialCriterion route.}

\independenceWitness{real, streamname, regseqrat, dyadicratcore, convergent-real-sequence-cauchy, real-cauchy-criterion: no listed sibling is bijective with $\RealSequentialCriterionUp$ because this packet jointly stores the displayed sequence source, finite observation windows, regular rational readback, dyadic tolerance split, convergence-facing row, Cauchy-facing row, terminal Real seal, transport, replay, provenance, and local naming.}

\closureat{RealSequentialCriterionUp}{seedStr}

\begin{closurestatus}{\RealSequentialCriterionUp}
  \constructivestory{A $\RealSequentialCriterionUp$ packet is generated from a displayed Real sequence source, StreamName finite windows, RegSeqRat readback, DyadicRatCore tolerance rows, a displayed convergence row, a RealCauchyCriterion row, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-sequential-criterion-carrier}, \autoref{thm:real-sequential-criterion-namecert-obligations}, and \autoref{thm:real-sequential-criterion-cauchy-equivalence} seed the finite sequential criterion packet over shared convergence-facing and Cauchy-facing Real routes.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient Real completeness, selected limits for arbitrary streams, quotient sequence equality, trichotomy, countable choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, shared-window exactness, convergence-to-Cauchy route exposure, Cauchy-to-sequential readback, transport, replay, provenance, and local naming for BEDC.Derived.RealSequentialCriterionUp.}
\end{closurestatus}
