\chapter{A Concrete Naming Certificate for $\RealTailAgreementSealUp$}
\label{ch:concrete-instances-realtailagreementseal-namecert}
\origin{ai}

The $\RealTailAgreementSealUp$ packet binds the paired tail-agreement route used by Real and Completion diagonal-limit consumers. It receives two regular rational source rows, the finite StreamName tail windows selected for both sides, the DyadicRatCore readback rows that compare those windows, the displayed agreement row, and the RealUp handoff row as one local NameCert surface. The packet therefore certifies a finite paired tail-window agreement route, not a quotient equality of streams, selected real values, host metric convergence, open-cover compactness, or choice-selected tails.

The agreement row is consumed as the paired-tail counterpart of the finite cofinal-tail route in \autoref{ch:concrete-instances-regularcauchytailfusion-namecert}, with the shared-threshold discipline supplied by \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert}.

\begin{definition}[Real tail-agreement seal carrier]
\label{def:real-tail-agreement-seal-carrier}
A $\RealTailAgreementSealUp$ carrier is a finite $\BHist$ packet
$$
Q=(R,S,W,D,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ and $S$ are the paired $\RegSeqRat$ source rows whose tails are being compared;
\item $W$ is the finite paired $\StreamNameUp$ tail-window row, naming the left and right windows read at the shared tail threshold;
\item $D$ is the paired $\DyadicRatCoreUp$ readback row obtained from the two windows in $W$;
\item $A$ is the displayed tail-agreement row, recording that the two dyadic readbacks lie inside the requested finite tolerance on the named windows;
\item $H$ records componentwise $\hsame$ transport for $R,S,W,D,A,C,P,N$ and for the local packet shape;
\item $C$ records the $\Cont$ routes by which a consumer reads the paired sources, finite windows, dyadic readbacks, agreement row, package provenance, and local naming row;
\item $P$ is the $\Pkg$ provenance over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, finite tolerance rows, and non-escape ledgers;
\item $N$ is the local $\NameCert_{\RealTailAgreementSealUp}$ row.
\end{enumerate}
The carrier exports only the finite packet $R,S,W,D,A,H,C,P,N$ and the RealUp-facing handoff read through it.
\end{definition}

\begin{theorem}[Real tail-agreement seal NameCert obligations]
\label{thm:real-tail-agreement-seal-namecert-obligations}
The local $\NameCert_{\RealTailAgreementSealUp}$ surface for \autoref{def:real-tail-agreement-seal-carrier} consists of five finite obligation rows:
\begin{enumerate}
\item carrier admission for the packet $Q=(R,S,W,D,A,H,C,P,N)$ over paired $\RegSeqRat$ sources, finite $\StreamNameUp$ windows, dyadic readback, agreement, transport, continuation, provenance, and local naming rows;
\item tail-window coverage stating that every public agreement read uses the paired windows listed in $W$ at the shared finite threshold;
\item dyadic readback exactness stating that the agreement row $A$ is computed only from the paired $\DyadicRatCoreUp$ readbacks recorded in $D$;
\item RealUp handoff stating that a downstream Real or Completion consumer reaches the seal only through $R,S,W,D,A,H,C,P,N$;
\item non-escape stating that the packet exports no quotient stream equality, selected real value, host metric convergence, open-cover compactness, choice-selected tail, or hidden equality oracle.
\end{enumerate}
\end{theorem}
\begin{proof}
Carrier admission is projection from the displayed packet. The two regular rational sources, the finite paired windows, the dyadic readbacks, the agreement row, and the structural rows are all coordinates of $Q$.

Tail-window coverage is read from $W$ through the continuation row $C$. A public agreement read must first select one of the paired finite windows already listed in the packet, so it cannot call an unrecorded tail search.

Dyadic readback exactness is the role of $D$ and $A$. The agreement row is downstream of the paired dyadic readbacks, and the componentwise $\hsame$ transport in $H$ preserves the same source-window-readback pairing.

The RealUp handoff and non-escape rows are then package clauses. The provenance $P$ and local naming row $N$ expose only $R,S,W,D,A,H,C,P,N$, so any downstream Real or Completion consumer receives the finite tail-agreement route and no external analytic object.
\end{proof}

\begin{theorem}[Real tail-agreement seal left--right threshold synchrony]
\label{thm:real-tail-agreement-seal-left-right-threshold-synchrony}
For an accepted $\RealTailAgreementSealUp$ packet $Q=(R,S,W,D,A,H,C,P,N)$, the two $\RegSeqRat$ source rows $R$ and $S$ read the same finite $\StreamNameUp$ tail-window threshold through the displayed row $W$. Any public read of the left tail and any public read of the right tail reaches the paired threshold through $W$ before $D$ or $A$ is available, and the route is the shared-threshold surface supplied by \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert}.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-tail-agreement-seal-carrier}. The paired source rows $R,S$ and the paired finite tail-window row $W$ are coordinates of one $\BHist$ packet, so a public left or right tail read has no carrier coordinate outside the displayed window row.

Apply the carrier-admission and tail-window coverage clauses of \autoref{thm:real-tail-agreement-seal-namecert-obligations}. Carrier admission places $R,S,W$ in the same local $\NameCert_{\RealTailAgreementSealUp}$ surface, while tail-window coverage says that every public agreement read uses the paired windows listed in $W$ at the shared finite threshold.

Finally use the shared-threshold discipline of \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert}. The present seal consumes that finite threshold surface before its dyadic readback and agreement rows, so the two $\RegSeqRat$ sources synchronize through $W$ rather than through a hidden tail selector or quotient stream equality.
\end{proof}

\begin{theorem}[Real tail-agreement seal dyadic-window exactness]
\label{thm:real-tail-agreement-seal-dyadic-window-exactness}
For an accepted $\RealTailAgreementSealUp$ packet $Q=(R,S,W,D,A,H,C,P,N)$, the rows $D$ and $A$ are computed only from the paired $\DyadicRatCoreUp$ readbacks displayed by $D$ after the left and right finite windows have been read through $W$. No quotient stream equality, host real equality, selected limit, ambient convergence predicate, or unrecorded dyadic sample can determine the agreement row.
\end{theorem}
\begin{proof}
Start from the carrier in \autoref{def:real-tail-agreement-seal-carrier}. The only dyadic observation coordinate in the packet is $D$, and the only agreement coordinate downstream of it is $A$.

Apply the dyadic readback exactness row of \autoref{thm:real-tail-agreement-seal-namecert-obligations}. That row makes $A$ depend on the paired $\DyadicRatCoreUp$ readbacks recorded in $D$, while the tail-window coverage row fixes those readbacks to the windows already named by $W$.

The sibling finite cofinal-tail route in \autoref{ch:concrete-instances-regularcauchytailfusion-namecert} supplies the comparison pattern: a finite tail read is reused as displayed finite evidence, not as an ambient completed object. Hence $D$ and $A$ are exact readback rows of the local packet and cannot be replaced by quotient equality, host equality, a selected limit, a convergence predicate, or an unrecorded sample.
\end{proof}

\begin{theorem}[Real tail-agreement seal Real--Completion handoff]
\label{thm:real-tail-agreement-seal-real-completion-handoff}
Every $\RealUp$ or $\CompletionUp$ consumer of an accepted $\RealTailAgreementSealUp$ packet reaches the seal through the displayed route
$$
R,S,W,D,A,H,C,P,N.
$$
The handoff imports no quotient stream equality, selected limit, host metric convergence, open-cover compactness, or choice-selected tail; it only reuses the paired threshold and finite cofinal-tail surfaces provided by \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert} and \autoref{ch:concrete-instances-regularcauchytailfusion-namecert}.
\end{theorem}
\begin{proof}
Project the packet in \autoref{def:real-tail-agreement-seal-carrier}. Its Real-facing boundary is exactly the source, window, readback, agreement, transport, continuation, provenance, and local naming rows $R,S,W,D,A,H,C,P,N$.

Apply the RealUp handoff and non-escape clauses of \autoref{thm:real-tail-agreement-seal-namecert-obligations}. The handoff clause forces every downstream Real or Completion consumer through the displayed rows, and the non-escape clause rules out quotient stream equality, selected real values, host convergence, open-cover compactness, choice-selected tails, and hidden equality oracles.

It remains to identify the sibling surfaces consumed by the route. The regular Cauchy tail-meet chapter supplies the shared threshold used by $W$, and the regular Cauchy tail-fusion chapter supplies the finite cofinal-tail pattern used when the agreement route is exposed to downstream consumers. Thus the handoff is finite and packet-local all the way through $R,S,W,D,A,H,C,P,N$.
\end{proof}

\begin{theorem}[Real tail-agreement seal TasteGate obligation route]
\label{thm:real-tail-agreement-seal-tastegate-obligation-route}
The paired source, window, readback, and agreement rows used by \autoref{thm:real-tail-agreement-seal-namecert-obligations} are the same single-carrier rows certified by the checked TasteGate marker recorded for this chapter. In particular, the TasteGate read does not introduce a second carrier, a second window ledger, or a second agreement classifier beyond
$$
R,S,W,D,A,H,C,P,N.
$$
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-tail-agreement-seal-carrier}. The rows consumed by the NameCert obligations are exactly the paired source rows $R,S$, the finite paired window row $W$, the dyadic readback row $D$, and the agreement row $A$ with the structural rows $H,C,P,N$.

Read the checked TasteGate marker already attached to the chapter as a single-carrier alignment for that same packet. Since the marker has only the displayed carrier surface available, its field-faithful read cannot select a second packet or a hidden agreement row.

Apply the NameCert obligation theorem. Its carrier-admission, tail-window coverage, dyadic exactness, RealUp handoff, and non-escape rows are therefore the concrete paper rows that the TasteGate marker preserves, not a parallel list of parameters.
\end{proof}

\begin{theorem}[Real tail-agreement seal paired-window transport]
\label{thm:real-tail-agreement-seal-paired-window-transport}
For an accepted $\RealTailAgreementSealUp$ packet $Q=(R,S,W,D,A,H,C,P,N)$, componentwise $\hsame$ transport preserves the paired $\StreamNameUp$ windows in $W$, the paired $\DyadicRatCoreUp$ readbacks in $D$, and the displayed agreement row $A$. A transported consumer still reads the route
$$
W \to D \to A
$$
through the same paired sources $R,S$ and the same structural rows $H,C,P,N$.
\end{theorem}
\begin{proof}
Project $H$ from the carrier. By definition, this row records componentwise transport for the paired sources, paired windows, dyadic readbacks, agreement row, continuation row, provenance row, local name, and packet shape.

Use tail-window coverage and dyadic readback exactness from \autoref{thm:real-tail-agreement-seal-namecert-obligations}. They force any transported agreement read to encounter the displayed window row before the dyadic readback row and the agreement row.

The continuation, provenance, and local naming rows only replay and name that route. Hence componentwise transport preserves the paired-window, readback, and agreement surface rather than replacing it by a host equality or an unrecorded tail selector.
\end{proof}

\begin{theorem}[Real tail-agreement seal RealUp consumer threshold factorization]
\label{thm:real-tail-agreement-seal-realup-consumer-threshold-factorization}
Every $\RealUp$ or $\CompletionUp$ consumer of an accepted $\RealTailAgreementSealUp$ packet first reads the shared-threshold route through \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert} and the cofinal-tail route through \autoref{ch:concrete-instances-regularcauchytailfusion-namecert} before reaching the RealUp handoff row of \autoref{thm:real-tail-agreement-seal-namecert-obligations}. The seal exports only the finite route
$$
R,S,W,D,A,H,C,P,N.
$$
\end{theorem}
\begin{proof}
Begin with the consumer read. The shared threshold is not an ambient choice of tail; it is the finite threshold surface named by the regular Cauchy tail-meet chapter and then displayed locally as the paired window row $W$.

Next use the cofinal-tail fusion chapter. It supplies the sibling finite tail route that turns a named cofinal window into a reusable tail-read surface, and the present packet applies that pattern to the two source rows $R,S$.

Finally apply the RealUp handoff and non-escape clauses of \autoref{thm:real-tail-agreement-seal-namecert-obligations}. The consumer can reach the seal only after the shared threshold, paired windows, dyadic readbacks, and agreement row have been displayed, and it receives no quotient stream equality, selected limit, host convergence, or choice-selected tail.
\end{proof}

\begin{theorem}[Real tail-agreement seal budget source lock]
\label{thm:real-tail-agreement-seal-budget-source-lock}
Every paired tail-seal read of an accepted $\RealTailAgreementSealUp$ packet reads its budget source only through the displayed $\RegularCauchyTailBudgetUp$ route of \autoref{ch:concrete-instances-regular-cauchy-tail-budget-namecert} and the $\ObservationBudgetLimiterUp$ route of \autoref{ch:concrete-instances-observationbudgetlimiter-namecert}. The local rows $R,S,W,D,A,H,C,P,N$ may replay that budgeted service, but they do not contain an independent tail-budget source, uncapped observation request, selected tail, or ambient convergence coordinate.
\end{theorem}
\begin{proof}
Begin with the threshold factorization of \autoref{thm:real-tail-agreement-seal-realup-consumer-threshold-factorization}. It places every Real or Completion consumer on the shared finite threshold before the local paired windows are read.

Next use the budget-window coherence row \autoref{thm:real-tail-agreement-seal-budget-window-coherence}. The finite request source for the paired tail windows is the $\RegularCauchyTailBudgetUp$ budget row, and the observation service behind that row is the limiter route named by \autoref{ch:concrete-instances-observationbudgetlimiter-namecert}.

Finally apply paired-window transport from \autoref{thm:real-tail-agreement-seal-paired-window-transport} and non-escape from \autoref{thm:real-tail-agreement-seal-nonescape}. Transport may replay the displayed budgeted route through $H,C,P,N$, but the packet has no coordinate for a second budget source, uncapped request, choice-selected tail, or host convergence proof.
\end{proof}

\begin{theorem}[Real tail-agreement seal window-meet coverage]
\label{thm:real-tail-agreement-seal-window-meet-coverage}
For an accepted $\RealTailAgreementSealUp$ packet, the left and right tail windows named by $W$ are covered by one finite $\StreamNameUp$ lattice meet surface before any $\DyadicRatCoreUp$ readback row $D$ or agreement row $A$ is consumed. The meet surface is the finite packet of \autoref{thm:streamname-finite-window-lattice-meet-exhaustion}; the local seal then reads the same paired-window evidence through $W \to D \to A$.
\end{theorem}
\begin{proof}
Use paired finite tail-window coverage from \autoref{def:real-tail-agreement-seal-window-coverage}. It identifies the left and right source windows through the single local row $W$ before the dyadic readback and agreement rows are available.

Apply the StreamName lattice-meet exhaustion theorem \autoref{thm:streamname-finite-window-lattice-meet-exhaustion}. The two accepted finite StreamName windows are first repacked as one meet surface over the common classifier, probe bundle, membership rows, unary histories, dyadic ledger, transports, routes, provenance, and local names.

Now return to the local packet using \autoref{thm:real-tail-agreement-seal-paired-window-source-lock}. Once the meet has supplied the shared finite window surface, every public agreement read proceeds through $W \to D \to A$ and cannot replace the meet by quotient stream equality, host real equality, selected limits, or a hidden window selector.
\end{proof}

\begin{theorem}[Real tail-agreement seal downstream route exhaustion]
\label{thm:real-tail-agreement-seal-downstream-route-exhaustion}
Every $\RealUp$ or $\CompletionUp$ consumer of an accepted $\RealTailAgreementSealUp$ packet receives only the route
$$
R,S,W,D,A,H,C,P,N
$$
together with the budget source lock of \autoref{thm:real-tail-agreement-seal-budget-source-lock} and the finite-window meet coverage of \autoref{thm:real-tail-agreement-seal-window-meet-coverage}. No downstream consumer can add a quotient stream, selected host limit, host convergence row, external completion selector, second budget gate, or second paired-window meet.
\end{theorem}
\begin{proof}
First apply the RealUp consumer threshold factorization \autoref{thm:real-tail-agreement-seal-realup-consumer-threshold-factorization}. It puts both Real and Completion consumers on the displayed threshold route before the local seal handoff.

Next combine the budget source lock with the window-meet coverage theorem. The former fixes the only budget route through $\RegularCauchyTailBudgetUp$ and $\ObservationBudgetLimiterUp$; the latter fixes the only finite StreamName meet surface feeding $W,D,A$.

Finally use \autoref{thm:real-tail-agreement-seal-completion-consumer-ledger} and \autoref{thm:real-tail-agreement-seal-real-completion-boundary}. These rows exhaust the downstream route by $R,S,W,D,A,H,C,P,N$, so the cited budget and meet surfaces are consumed as finite predecessors of the same displayed packet rather than as hidden quotient, choice, convergence, or completion data.
\end{proof}

\begin{definition}[Real tail-agreement seal window coverage]
\label{def:real-tail-agreement-seal-window-coverage}
For a $\RealTailAgreementSealUp$ carrier $Q=(R,S,W,D,A,H,C,P,N)$, paired finite tail-window coverage means that every public agreement read first selects the same displayed threshold in the $\StreamNameUp$ row $W$, then reads the left and right finite windows of the paired source rows $R$ and $S$, and only then reaches the $\DyadicRatCoreUp$ readback row $D$ and the agreement row $A$. The coverage predicate is transported componentwise by $H$, replayed only through $C$, recorded in provenance $P$, and named locally by $N$.
\end{definition}

\begin{theorem}[Real tail-agreement seal dyadic exactness]
\label{thm:real-tail-agreement-seal-dyadic-exactness}
For an accepted $\RealTailAgreementSealUp$ packet $Q=(R,S,W,D,A,H,C,P,N)$, the agreement row $A$ is determined only by the $\DyadicRatCoreUp$ readback row $D$ selected by the paired tail-window row $W$. No separate quotient-stream equality, selected real equality, host metric limit, or hidden tail oracle can determine $A$.
\end{theorem}
\begin{proof}
Project the displayed rows $W,D,A,H,C,P,N$ from the carrier. The window row $W$ selects the paired finite tail windows, and the readback row $D$ is the only dyadic observation row attached to those windows.

Apply the tail-window coverage and dyadic readback exactness clauses of \autoref{thm:real-tail-agreement-seal-namecert-obligations}. They force every public agreement read through $W$ and $D$ before $A$ is available.

The structural rows $H,C,P,N$ preserve and expose that same dependency. Since none of them contains quotient stream equality, selected real equality, a host limit, or a tail oracle, the agreement row is determined exactly by the displayed dyadic readback route.
\end{proof}
\leanchecked{BEDC.Derived.RealTailAgreementSealUp.RealTailAgreementSealWindowRoute\_result\_exact}

\begin{theorem}[Real tail-agreement seal shared-threshold non-escape]
\label{thm:real-tail-agreement-seal-shared-threshold-nonescape}
Any downstream $\RealUp$ or $\CompletionUp$ consumer that uses the shared tail threshold of an accepted $\RealTailAgreementSealUp$ packet must factor through $W$, then $D$, then $A$. The consumer cannot import quotient stream equality, selected limits, host convergence, or choice-selected tails into the seal route.
\end{theorem}
\begin{proof}
Begin with a downstream Real or Completion read. By \autoref{def:real-tail-agreement-seal-window-coverage}, the read first reaches the paired finite windows selected by $W$.

Next apply \autoref{thm:real-tail-agreement-seal-dyadic-exactness}. The selected windows determine the dyadic readback row $D$, and $D$ determines the displayed agreement row $A$.

Finally use the RealUp handoff and non-escape clauses of \autoref{thm:real-tail-agreement-seal-namecert-obligations}. They expose the agreement only through the finite packet rows and exclude quotient stream equality, selected limits, host convergence, and choice-selected tails from the consumer route.
\end{proof}

\begin{theorem}[Real tail-agreement seal non-escape]
\label{thm:real-tail-agreement-seal-nonescape}
An accepted $\RealTailAgreementSealUp$ packet cannot be used to export quotient stream equality, selected real values, host metric convergence, open-cover compactness, or choice-selected tails. Any attempted read of one of those objects is outside the local packet unless it factors through the finite rows
$$
R,S,W,D,A,H,C,P,N.
$$
\end{theorem}
\begin{proof}
Project the carrier. Its source rows are the paired $\RegSeqRat$ inputs, its observation rows are the finite $\StreamNameUp$ windows and $\DyadicRatCoreUp$ readbacks, and its comparison row is the displayed finite agreement row.

The structural rows only transport, route, package, and name those coordinates. They do not add a quotient of streams, an ambient real carrier, a host metric space, an open cover, or a choice function selecting tails.

Therefore a read that asks for any of those external objects has no coordinate in the packet. The only Real-facing information available to downstream consumers is the finite agreement route through $R,S,W,D,A,H,C,P,N$.
\end{proof}

\begin{theorem}[Real tail-agreement seal tail classifier]
\label{thm:real-tail-agreement-seal-tail-classifier}
For a $\RealTailAgreementSealUp$ carrier $Q=(R,S,W,D,A,H,C,P,N)$, the displayed agreement row $A$ is classifier-readable only through the exposed rows $R,S,W,D$. In particular, a public classifier for the finite tail agreement must first read the paired $\RegSeqRat$ sources $R,S$, the paired $\StreamNameUp$ tail-window row $W$, and the paired $\DyadicRatCoreUp$ readback row $D$; the row $A$ carries no separate tail oracle, quotient-stream equality, or ambient real equality.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:real-tail-agreement-seal-carrier}. The agreement coordinate $A$ is displayed only after the source rows $R,S$, the window row $W$, and the readback row $D$ have been named.

Apply the tail-window and dyadic-readback clauses of \autoref{thm:real-tail-agreement-seal-namecert-obligations}. They force every public agreement read to pass through $W$ and $D$, so the classifier sees the same finite rows that produced $A$.

Finally apply \autoref{thm:real-tail-agreement-seal-nonescape}. Since the packet exports no quotient equality or hidden tail selector, no classifier can bypass $R,S,W,D$ while still reading the displayed agreement row.
\end{proof}

\begin{theorem}[Real tail-agreement seal Real handoff]
\label{thm:real-tail-agreement-seal-real-classifier-handoff}
Every Real or Completion consumer of a $\RealTailAgreementSealUp$ packet receives the displayed agreement through
$$
R,S,W,D,A,H,C,P,N
$$
together with the shared-threshold surface supplied by \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert}. The handoff is therefore the paired-tail analogue of the finite cofinal-tail route in \autoref{ch:concrete-instances-regularcauchytailfusion-namecert}, not a direct assertion of Real equality or completion convergence.
\end{theorem}
\begin{proof}
The carrier supplies the local rows $R,S,W,D,A,H,C,P,N$. The RealUp handoff clause in \autoref{thm:real-tail-agreement-seal-namecert-obligations} says that a downstream consumer reaches the seal only through those rows.

The agreement row is finite because its windows and readbacks are finite. The cofinal-tail route of \autoref{ch:concrete-instances-regularcauchytailfusion-namecert} gives the sibling pattern for exposing finite tail evidence, while \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert} supplies the shared-threshold discipline needed for a paired comparison.

The non-escape theorem then fixes the boundary of the handoff. A Real or Completion consumer may consume the displayed paired-tail agreement route, but it does not receive quotient stream equality, a selected Real value, host metric convergence, open-cover compactness, or a choice-selected tail.
\end{proof}

\begin{theorem}[Real tail-agreement seal ledger exhaustion]
\label{thm:real-tail-agreement-seal-ledger-exhaustion}
The rows
$$
R,S,W,D,A,H,C,P,N
$$
exhaust the local $\RealTailAgreementSealUp$ ledger. Any valid local obligation is one of carrier admission, tail-window coverage, dyadic readback exactness, RealUp handoff, or non-escape; no local row remains for quotient stream equality, selected reals, host metric convergence, open-cover compactness, choice-selected tails, or hidden tail oracles.
\end{theorem}
\begin{proof}
Carrier admission in \autoref{thm:real-tail-agreement-seal-namecert-obligations} lists the whole packet. The only displayed data rows are the paired sources, windows, readbacks, agreement, transport, continuation, provenance, and local naming rows.

The middle obligations account for the classifier surface. Tail-window coverage accounts for $W$, dyadic readback exactness accounts for $D$ and $A$, and the RealUp handoff accounts for the consumer route through $H,C,P,N$.

It remains to rule out extra ledger rows. Apply \autoref{thm:real-tail-agreement-seal-nonescape}: the packet contains no coordinate for quotient stream equality, selected reals, host metric convergence, open-cover compactness, choice-selected tails, or hidden tail oracles. Hence the five displayed obligations exhaust the local ledger.
\end{proof}

\begin{theorem}[Real tail-agreement seal source separation obligation]
\label{thm:real-tail-agreement-seal-source-separation-obligation}
For an accepted $\RealTailAgreementSealUp$ packet $Q=(R,S,W,D,A,H,C,P,N)$, the rows $R$ and $S$ are the only paired $\RegSeqRat$ source rows, $W$ is the only paired $\StreamNameUp$ tail-window row, $D$ is the only paired $\DyadicRatCoreUp$ readback row, and $A$ is the only displayed agreement row. The structural rows $H,C,P,N$ transport, route, package, and name these fields, but they do not introduce a second source, a second tail window, a second dyadic readback, or a second agreement classifier.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:real-tail-agreement-seal-carrier}. Its displayed data coordinates are exactly the paired source rows $R,S$, the paired window row $W$, the readback row $D$, and the agreement row $A$.

Apply the carrier-admission and non-escape clauses of \autoref{thm:real-tail-agreement-seal-namecert-obligations}. Carrier admission lists the rows admitted to the local $\NameCert_{\RealTailAgreementSealUp}$ surface, while non-escape excludes hidden tail selectors, quotient equalities, and unrecorded analytic objects.

The remaining structural rows cannot manufacture another source of agreement. The componentwise transport row $H$ preserves the same coordinates, $C$ only routes reads through them, $P$ records their provenance, and $N$ names the local packet.
\end{proof}

\begin{theorem}[Real tail-agreement seal paired-window source lock]
\label{thm:real-tail-agreement-seal-paired-window-source-lock}
Let a $\RealUp$ or $\CompletionUp$ consumer agree with an accepted $\RealTailAgreementSealUp$ packet on the rows $R,S,W,H,C,P,N$. Then every public agreement read by that consumer uses the same route
$$
W \to D \to A.
$$
In particular, the consumer cannot replace the paired tail-window selector by a second selector, and it cannot replace the dyadic readback route by host equality of completed real values.
\end{theorem}
\begin{proof}
The agreement on $R,S,W,H,C,P,N$ fixes the public carrier boundary through which the consumer reads the seal. By \autoref{def:real-tail-agreement-seal-window-coverage}, the read first reaches the paired finite tail windows selected by $W$.

Apply \autoref{thm:real-tail-agreement-seal-dyadic-exactness}. Once $W$ has selected the paired windows, the only readback row available for the agreement is $D$, and the displayed agreement row is $A$.

Finally use \autoref{thm:real-tail-agreement-seal-source-separation-obligation}. Since no second window selector, second readback row, or host equality row is present in the local ledger, every Real or Completion consumer that stays on the displayed boundary must follow $W \to D \to A$.
\end{proof}
\leanchecked{BEDC.Derived.RealTailAgreementSealUp.RealTailAgreementSealWindowRoute\_result\_deterministic}

\begin{theorem}[Real tail-agreement seal terminal consumer factorization]
\label{thm:real-tail-agreement-seal-terminal-consumer-factorization}
Let $Q=(R,S,W,D,A,H,C,P,N)$ be an accepted $\RealTailAgreementSealUp$ packet. Every terminal $\RealUp$ or $\CompletionUp$ consumer of $Q$ factors its public read through the displayed route
$$
R,S,W,D,A,H,C,P,N.
$$
In particular, the consumer first agrees with the paired source and tail-window rows, then reads the dyadic agreement route $W \to D \to A$, and only then uses the structural rows $H,C,P,N$ for transport, continuation, provenance, and local naming.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-tail-agreement-seal-realup-consumer-threshold-factorization}. It identifies the Real-facing threshold route through the carrier rows and prevents the consumer from entering the packet through an unlisted analytic boundary.

Apply \autoref{thm:real-tail-agreement-seal-paired-window-source-lock}. Once the consumer agrees with $R,S,W,H,C,P,N$, every public agreement read follows the finite route $W \to D \to A$.

Finally apply \autoref{thm:real-tail-agreement-seal-source-separation-obligation}. The only source, window, readback, agreement, transport, continuation, package, and name rows available to a terminal Real or Completion consumer are the displayed coordinates of $Q$.
\end{proof}

\begin{theorem}[Real tail-agreement seal dyadic terminal non-escape]
\label{thm:real-tail-agreement-seal-dyadic-terminal-nonescape}
Let $Q=(R,S,W,D,A,H,C,P,N)$ be an accepted $\RealTailAgreementSealUp$ packet. A terminal $\RealUp$ or $\CompletionUp$ consumer of $Q$ cannot replace the route
$$
W \to D \to A
$$
by quotient stream equality, selected real equality, host metric convergence, open-cover compactness, or choice-selected tails.
\end{theorem}
\begin{proof}
By \autoref{def:real-tail-agreement-seal-window-coverage}, the public read reaches the finite paired tail windows selected by $W$ before it reaches any agreement row.

Apply \autoref{thm:real-tail-agreement-seal-dyadic-exactness}. The selected windows are read back through the dyadic row $D$, and the agreement visible to the consumer is the displayed row $A$.

Apply \autoref{thm:real-tail-agreement-seal-shared-threshold-nonescape}. The shared threshold is not a quotient-stream equality, a selected real, a host metric convergence proof, an open-cover compactness argument, or a choice-selected tail. Hence no terminal consumer can substitute any of those objects for $W \to D \to A$.
\end{proof}

\begin{theorem}[Real tail-agreement seal Completion consumer exactness]
\label{thm:real-tail-agreement-seal-completion-consumer-exactness}
Every $\CompletionUp$-facing consumer of an accepted $\RealTailAgreementSealUp$ packet reads the same paired tail-window agreement surface before any completion seal read. The only rows it receives are the displayed BEDC rows over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$:
$$
R,S,W,D,A,H,C,P,N.
$$
\end{theorem}
\begin{proof}
Use \autoref{thm:real-tail-agreement-seal-terminal-consumer-factorization} to place the Completion-facing consumer on the terminal packet route. Its public read factors through the displayed rows before it can consume a completion seal.

Use \autoref{thm:real-tail-agreement-seal-dyadic-terminal-nonescape} to fix the agreement surface. The consumer must read $W \to D \to A$, and it cannot replace that dyadic route by quotient streams, selected limits, host convergence, compactness, or choice-selected tails.

It remains to identify the received rows. Apply \autoref{thm:real-tail-agreement-seal-nonescape} together with the chapter closure scope: the local packet exposes only the finite BEDC carrier rows over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\end{proof}

\begin{theorem}[Real tail-agreement seal Completion consumer ledger]
\label{thm:real-tail-agreement-seal-completion-consumer-ledger}
Every $\CompletionUp$ consumer of an accepted $\RealTailAgreementSealUp$ packet reaches the seal through the displayed ledger
$$
R,S,W,D,A,H,C,P,N.
$$
The only sibling routes it may use are the $\CauchyLimitSealUp$ route of \autoref{ch:concrete-instances-cauchylimitseal-namecert} and the $\CauchyDiagonalBudgetUp$ route of \autoref{ch:concrete-instances-cauchydiagonalbudget-namecert}; both consume the same finite paired tail-agreement boundary rather than a quotient stream, selected limit, host convergence proof, or external completion object.
\end{theorem}
\begin{proof}
Start with \autoref{thm:real-tail-agreement-seal-realup-consumer-threshold-factorization}. It places the downstream consumer on the displayed tail-agreement carrier rows before any Real or Completion handoff can be read.

Apply \autoref{thm:real-tail-agreement-seal-paired-window-source-lock}. Once the consumer is on $R,S,W,H,C,P,N$, the agreement surface is forced through $W \to D \to A$.

The sibling routes cited in the statement consume this same boundary. The $\CauchyLimitSealUp$ chapter receives a finite seal row from its Cauchy-facing source, and the $\CauchyDiagonalBudgetUp$ chapter receives the finite budget-selector packet before its Real-facing seal handoff; neither route adds a new coordinate to $Q=(R,S,W,D,A,H,C,P,N)$.
\end{proof}

\begin{theorem}[Real tail-agreement seal budget-window coherence]
\label{thm:real-tail-agreement-seal-budget-window-coherence}
Every $\RealTailAgreementSealUp$ completion consumer must first read the $\RegularCauchyTailBudgetUp$ budget row from \autoref{ch:concrete-instances-regular-cauchy-tail-budget-namecert} before it can consume the shared paired-window route of the accepted packet $Q=(R,S,W,D,A,H,C,P,N)$. The budget row fixes the finite tail request that the seal later displays through $W,D,A$.
\end{theorem}
\begin{proof}
Start with \autoref{thm:real-tail-agreement-seal-completion-consumer-ledger}. It puts every completion consumer on the displayed ledger $R,S,W,D,A,H,C,P,N$ before any seal read.

The budget row is the finite request source for the paired tail windows. Reading it from \autoref{ch:concrete-instances-regular-cauchy-tail-budget-namecert} determines which tail request the local $W$ row can display.

Since the packet has no quotient stream, host real equality, selected limit, or ambient completeness coordinate, the consumer cannot bypass the budget row and later recover the same finite paired-window route.
\end{proof}

\begin{theorem}[Real tail-agreement seal tail-meet factorization]
\label{thm:real-tail-agreement-seal-tail-meet-factorization}
After the $\RegularCauchyTailBudgetUp$ budget row has been read, every $\RealTailAgreementSealUp$ completion consumer factors through the shared-threshold route of \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert} before reaching any $\CauchyLimitSealUp$ or $\CauchyDiagonalBudgetUp$ sibling route. The factorization exposes the same finite $W \to D \to A$ agreement surface used by the local seal packet.
\end{theorem}
\begin{proof}
Read the budget-window coherence row. It places the consumer on the finite tail request before the paired windows of $Q$ are available.

Now use the shared-threshold sibling. The $\RegularCauchyTailMeetUp$ route supplies the common threshold consumed by the same $W$ row, and \autoref{thm:real-tail-agreement-seal-paired-window-source-lock} forces any public agreement read through $W \to D \to A$.

The $\CauchyLimitSealUp$ and $\CauchyDiagonalBudgetUp$ routes consume that agreement surface after the shared threshold has been displayed. They do not add a second tail source, quotient stream equality, host real equality, or selected limit to the local seal packet.
\end{proof}

\begin{theorem}[Real tail-agreement seal completion route non-escape]
\label{thm:real-tail-agreement-seal-completion-route-nonescape}
Every $\RealTailAgreementSealUp$ completion route to $\CauchyLimitSealUp$ or $\CauchyDiagonalBudgetUp$ is confined to the sequence: $\RegularCauchyTailBudgetUp$ budget row, $\RegularCauchyTailMeetUp$ shared-threshold row, and the local $W \to D \to A$ agreement surface. No route may replace that sequence by quotient stream equality, host real equality, selected limit, or ambient completeness.
\end{theorem}
\begin{proof}
Use budget-window coherence for the first step and tail-meet factorization for the second step. Together they force the consumer to read the budget row, then the shared-threshold route, and then the displayed local agreement surface.

Apply the completion consumer ledger. Once the route reaches $Q$, the only local rows available are $R,S,W,D,A,H,C,P,N$, and the only agreement route is $W \to D \to A$.

The excluded analytic objects have no coordinate in either the budget row, the shared-threshold sibling, or the local seal packet. Hence they cannot serve as alternate completion routes.
\end{proof}

\begin{theorem}[Real tail-agreement seal completion budget-threshold exhaustion]
\label{thm:real-tail-agreement-seal-completion-budget-threshold-exhaustion}
Let $Q=(R,S,W,D,A,H,C,P,N)$ be an accepted $\RealTailAgreementSealUp$ packet. Every $\CompletionUp$-facing consumer of $Q$ factors through the finite sequence consisting of the $\RegularCauchyTailBudgetUp$ budget row, the $\RegularCauchyTailMeetUp$ shared-threshold row, the local $W \to D \to A$ agreement surface, and then the $\CauchyLimitSealUp$ or $\CauchyDiagonalBudgetUp$ consumer route. No quotient stream equality, host real equality, selected limit, hidden tail selector, or ambient completeness row can appear in that factorization.
\end{theorem}
\begin{proof}
Start with \autoref{thm:real-tail-agreement-seal-budget-window-coherence}. It places the Completion-facing consumer on the $\RegularCauchyTailBudgetUp$ budget row before the paired window route of $Q$ is read.

Next apply \autoref{thm:real-tail-agreement-seal-tail-meet-factorization}. The budget row is carried through the $\RegularCauchyTailMeetUp$ shared threshold, and the local public agreement surface is forced to be $W \to D \to A$.

Finally use \autoref{thm:real-tail-agreement-seal-completion-route-nonescape}. The only sibling consumers available after this surface are the $\CauchyLimitSealUp$ route of \autoref{ch:concrete-instances-cauchylimitseal-namecert} and the $\CauchyDiagonalBudgetUp$ route of \autoref{ch:concrete-instances-cauchydiagonalbudget-namecert}. Since none of the listed rows carries quotient stream equality, host real equality, a selected limit, a hidden tail selector, or an ambient completeness row, the displayed sequence exhausts the Completion-facing handoff.
\end{proof}

\begin{theorem}[Real tail-agreement seal dual-source window exhaustion]
\label{thm:real-tail-agreement-seal-dual-source-window-exhaustion}
For an accepted $\RealTailAgreementSealUp$ packet $Q=(R,S,W,D,A,H,C,P,N)$, the two source rows $R,S$ and the paired $\StreamNameUp$ window row $W$ exhaust the left/right tail evidence visible to Real and Completion consumers. The dyadic row $D$ and agreement row $A$ read that evidence, while $H,C,P,N$ transport, route, package, and name it without adding another left tail, right tail, or paired-window selector.
\end{theorem}
\begin{proof}
Use \autoref{def:real-tail-agreement-seal-window-coverage}. The public read reaches the paired finite windows through the single window row $W$ after the two source rows $R,S$ have been displayed.

Apply \autoref{thm:real-tail-agreement-seal-dyadic-exactness}. The selected windows are read back through $D$, and the visible agreement is recorded by $A$.

Finally apply \autoref{thm:real-tail-agreement-seal-paired-window-source-lock}. A Real or Completion consumer that agrees on $R,S,W,H,C,P,N$ must read $W \to D \to A$, so there is no remaining carrier coordinate that can serve as another left/right source or another paired-window selector.
\end{proof}

\begin{theorem}[Real tail-agreement seal Real--Completion boundary]
\label{thm:real-tail-agreement-seal-real-completion-boundary}
$\RealUp$ and $\CompletionUp$ consumers of an accepted $\RealTailAgreementSealUp$ packet receive the same finite paired agreement boundary
$$
R,S,W,D,A,H,C,P,N.
$$
The boundary supplies source rows, paired windows, dyadic readback, displayed agreement, transport, routes, package provenance, and local naming; it supplies no quotient stream, selected limit, host convergence predicate, or hidden completion selector.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-tail-agreement-seal-realup-consumer-threshold-factorization} to put the Real-facing read on the carrier boundary, and apply \autoref{thm:real-tail-agreement-seal-completion-consumer-ledger} to put the Completion-facing read on the same displayed ledger.

The common agreement row is determined by the window and dyadic readback. This is exactly the route supplied by \autoref{def:real-tail-agreement-seal-window-coverage}, \autoref{thm:real-tail-agreement-seal-dyadic-exactness}, and \autoref{thm:real-tail-agreement-seal-paired-window-source-lock}.

The non-escape part is finite. Since the packet has no coordinate for quotient streams, selected limits, host convergence, or hidden completion selectors, both kinds of consumer receive only the same paired agreement boundary.
\end{proof}

\begin{theorem}[Real tail-agreement seal selector-budget handoff]
\label{thm:real-tail-agreement-seal-selector-budget-handoff}
Let $Q=(R,S,W,D,A,H,C,P,N)$ be an accepted $\RealTailAgreementSealUp$ packet. If $Q$ is consumed through a finite observation-budget selector, then the consumer first factors through \autoref{ch:concrete-instances-finiteobservationbudgetselector-namecert} and its finite route
$$
B \to S \to W \to D \to R \to E.
$$
Only after that public selector route has been displayed can the consumer reach the local paired agreement route $W \to D \to A$. The handoff exports no quotient stream, selected limit, host convergence row, hidden completion selector, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Apply \autoref{thm:finite-observation-budget-selector-public-export}. It makes the finite selector route $B \to S \to W \to D \to R \to E$ the public route seen by Real-completion consumers before the local seal is read.

Now use budget-window coherence for the present packet. The selector route supplies the finite budget and window service consumed by the local $W$ row, so the seal cannot be reached through a separate unbounded observation request.

Finally apply \autoref{thm:real-tail-agreement-seal-paired-window-source-lock}. Once the local rows $R,S,W,H,C,P,N$ have been matched, every displayed agreement read proceeds through $W \to D \to A$, with no additional analytic coordinate exported by the handoff.
\end{proof}

\begin{theorem}[Real tail-agreement seal finite-approximation consumer boundary]
\label{thm:real-tail-agreement-seal-finite-approximation-consumer-boundary}
Every finite approximation consumer reaches an accepted $\RealTailAgreementSealUp$ packet only after the $\FiniteCauchyApproximationUp$ surface of \autoref{ch:concrete-instances-finite-cauchy-approximation-namecert} has displayed the finite rows
$$
R,n,k,W,D,E,S,H,C,P,N.
$$
The consumer then reads the local paired agreement boundary $R,S,W,D,A,H,C,P,N$ through $W \to D \to A$. The boundary contains no selected real value, quotient stream equality, infinite tail, host metric, hidden completion selector, or ambient completeness row.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:finite-cauchy-approximation-obligation-closure}. It packages the finite approximation rows $R,n,k,W,D,E,S,H,C,P,N$ as the local $\NameCert_{\FiniteCauchyApproximationUp}$ obligation surface before any real-facing or completion-facing consumer can use the approximation.

Next apply \autoref{thm:finite-cauchy-approximation-budget-route-exactness}. The precision, finite-window, dyadic-ledger, error-row, and handoff data are consumed through the same finite budget route as the backing budget packets, so the approximation contributes no hidden completion choice.

Finally use the local completion route non-escape theorem. After the finite approximation rows have supplied the window and dyadic readback surface, the present seal can only read $W \to D \to A$ inside $R,S,W,D,A,H,C,P,N$, and none of the excluded analytic objects has a carrier coordinate.
\end{proof}

\begin{theorem}[Real tail-agreement seal Real--Completion selector chain]
\label{thm:real-tail-agreement-seal-real-completion-selector-chain}
For an accepted $\RealTailAgreementSealUp$ packet $Q=(R,S,W,D,A,H,C,P,N)$, every $\RealUp$ or $\CompletionUp$ consumer that passes through a finite observation-budget selector and a finite Cauchy approximation sees one finite chain: the selector route $B \to S \to W \to D \to R \to E$, the finite approximation rows $R,n,k,W,D,E,S,H,C,P,N$, the shared threshold route, and the local paired agreement route $W \to D \to A$. The chain exports the same Real--Completion boundary $R,S,W,D,A,H,C,P,N$ and no quotient stream, selected limit, host convergence, hidden completion selector, or ambient completeness row.
\end{theorem}
\begin{proof}
Use \autoref{thm:real-tail-agreement-seal-selector-budget-handoff} for the budget-selector side. It forces the consumer through the displayed finite selector route before the local paired windows are available.

Use \autoref{thm:real-tail-agreement-seal-finite-approximation-consumer-boundary} for the finite approximation side. It forces the same consumer through the approximation rows and then returns it to the local $W \to D \to A$ boundary.

Now apply \autoref{thm:real-tail-agreement-seal-real-completion-boundary}. The Real-facing and Completion-facing reads receive the same finite local packet, so the selector route, approximation rows, shared threshold, and local paired agreement route form one displayed chain rather than a route through quotient streams, selected limits, host convergence, hidden completion selectors, or ambient completeness.
\end{proof}

\falsifiablePrediction{Deleting any one of $R,S,W,D,A$ breaks a different public read of the seal: source membership for $R,S$, shared tail-window selection for $W$, dyadic exactness for $D$, and displayed agreement for $A$.}

\independenceWitness{Nearest Real and Completion siblings share some source or handoff vocabulary, but none is bijective to this carrier while preserving the five distinct rows $R,S,W,D,A$: a single-source regularity packet lacks the paired source split, a tail-selector packet lacks dyadic agreement, a dyadic readback packet lacks the paired window lock, and a Real handoff packet lacks the displayed agreement boundary.}

\begin{theorem}[Real tail-agreement seal TasteGate independence boundary]
\label{thm:real-tail-agreement-seal-tastegate-independence-boundary}
The checked single-carrier TasteGate marker for $\RealTailAgreementSealUp$ is read together with the displayed carrier rows, the falsifiable prediction, and the independence witness as preserving the distinct fields $R,S,W,D,A$. Thus the current \texttt{FieldFaithful} witness and any later \texttt{Nontrivial} witness have a non-vacuous paper boundary: they must respect source separation, paired-window source lock, dyadic readback, displayed agreement, and the Real-facing handoff instead of collapsing the packet to a single undifferentiated carrier row.
\end{theorem}
\begin{proof}
The checked TasteGate marker recorded for the chapter supplies the single-carrier alignment for the carrier in \autoref{def:real-tail-agreement-seal-carrier}. It does not by itself identify the internal roles of the rows, so the paper boundary records the roles explicitly.

The falsifiable prediction separates the rows by deletion behavior: removing $R,S,W,D$, or $A$ destroys a different public read. The independence witness separates the carrier from nearby Real and Completion packets by requiring the paired source split, the window lock, the dyadic readback, and the agreement row to be preserved together.

Apply \autoref{thm:real-tail-agreement-seal-source-separation-obligation} and \autoref{thm:real-tail-agreement-seal-paired-window-source-lock}. These theorems bind the row distinction and the consumer route $W \to D \to A$, so any faithful Lean-side field witness for this carrier has a concrete paper boundary to preserve.
\end{proof}

\closureat{RealTailAgreementSealUp}{obligationStr}
\closureat{RealTailAgreementSealUp}{scopedStr}[BEDC.Derived.RealTailAgreementSealUp.taste\_gate]
\closureat{RealTailAgreementSealUp}{publicStr}[BEDC.Derived.RealTailAgreementSealUp.taste\_gate]
\closureat{RealTailAgreementSealUp}{bridgedStr}[BEDC.Derived.RealTailAgreementSealUp.taste\_gate]
\closureat{RealTailAgreementSealUp}{matureStr}[BEDC.Derived.RealTailAgreementSealUp.taste\_gate]

\begin{closurestatus}{\RealTailAgreementSealUp}
  \constructivestory{A $\RealTailAgreementSealUp$ packet is generated from paired RegSeqRat source rows, finite paired StreamName tail windows, paired DyadicRatCore readbacks, a displayed tail-agreement row, componentwise hsame transports, Cont routes, Pkg provenance, and a local NameCert row.}
  \leanchecked{BEDC.Derived.RealTailAgreementSealUp.taste\_gate}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{def:real-tail-agreement-seal-carrier}, \autoref{thm:real-tail-agreement-seal-namecert-obligations}, \autoref{thm:real-tail-agreement-seal-left-right-threshold-synchrony}, \autoref{thm:real-tail-agreement-seal-dyadic-window-exactness}, \autoref{thm:real-tail-agreement-seal-real-completion-handoff}, \autoref{thm:real-tail-agreement-seal-nonescape}, \autoref{thm:real-tail-agreement-seal-tail-classifier}, \autoref{thm:real-tail-agreement-seal-real-classifier-handoff}, \autoref{thm:real-tail-agreement-seal-ledger-exhaustion}, \autoref{thm:real-tail-agreement-seal-source-separation-obligation}, \autoref{thm:real-tail-agreement-seal-paired-window-source-lock}, \autoref{thm:real-tail-agreement-seal-terminal-consumer-factorization}, \autoref{thm:real-tail-agreement-seal-dyadic-terminal-nonescape}, \autoref{thm:real-tail-agreement-seal-completion-consumer-exactness}, \autoref{thm:real-tail-agreement-seal-completion-consumer-ledger}, \autoref{thm:real-tail-agreement-seal-completion-budget-threshold-exhaustion}, \autoref{thm:real-tail-agreement-seal-dual-source-window-exhaustion}, \autoref{thm:real-tail-agreement-seal-real-completion-boundary}, \autoref{thm:real-tail-agreement-seal-selector-budget-handoff}, \autoref{thm:real-tail-agreement-seal-finite-approximation-consumer-boundary}, \autoref{thm:real-tail-agreement-seal-real-completion-selector-chain}, and \autoref{thm:real-tail-agreement-seal-tastegate-independence-boundary} bind the finite paired tail-window agreement packet, source separation, left--right threshold synchrony, paired-window route, dyadic readback, Real--Completion handoff, terminal consumer factorization, Completion ledger, completion budget-threshold exhaustion, dual-source window exhaustion, shared Real--Completion boundary, selector-budget handoff, finite approximation consumer boundary, Real--Completion selector chain, and TasteGate independence boundary used by Real and Completion handoff consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close Lean verification, quotient stream equality, selected real values, host metric convergence, open-cover compactness, choice-selected tails, or a public standard bridge.}
  \upgradepath{The paper closure axis is complete for the finite paired tail-agreement packet; Lean verification and bridge checking remain independent axes.}
\end{closurestatus}
