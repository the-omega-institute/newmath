\chapter{A Concrete Naming Certificate for $\FirstExitChannelUp$}
\label{ch:concrete-instances-first-exit-channel-namecert}
\origin{ai}

The $\FirstExitChannelUp$ packet realizes the first-exit channel geometry of \autoref{def:philosophy-first-exit-channel} as finite BEDC NameCert data. It reads a killed bit-flip walk state through origin state, flipped bit, outside target, target class, transition ledger, transport, replay, provenance, and local naming before any first-exit geometry claim is exported. The packet is a finite science-facing certificate; it is not a bijection with the six-boundary-tile coverage, not a host biological law, not an optimizer for unseen codon tables, and not an unrestricted stochastic-process oracle.

The transition ledger is consumed beside \autoref{ch:concrete-instances-six-boundary-tile-coverage-namecert}, \autoref{ch:concrete-instances-window-coverage-trigger-algebra-namecert}, \autoref{ch:concrete-instances-punctured-cube-skeleton-namecert}, and \autoref{ch:concrete-instances-post-median-gate-shell-namecert} because those chapters supply the codon-window partition, trigger activation, cube adjacency, and post-median shell consumer rows.

\falsifiablePrediction{Every accepted $\FirstExitChannelUp$ read must display origin state, flipped bit, outside target, target class, transition ledger, transport, replay, provenance, and local naming before any first-exit geometry claim is exported.}

\independenceWitness{six-boundary-tile-coverage, window-coverage-trigger-algebra, post-median-gate-shell, punctured-cube-skeleton: six-boundary-tile coverage names the codon-window partition, window-coverage trigger algebra names gate activation, post-median gate shell names the downstream shell consumer, and the punctured-cube skeleton names cube adjacency. None is bijective to $\FirstExitChannelUp$ because this packet binds origin row, flipped-bit row, outside-target row, target-class row, transition ledger, transport, replay, provenance, and local naming as one accepted first-exit surface.}

\begin{definition}[First-exit channel carrier]
\label{def:first-exit-channel-carrier}
A $\FirstExitChannelUp$ carrier is a finite $\BHist$ packet
$$
F=(O,B,T,K,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $O$ is the origin-state row inside the killed median-closure domain.
\item $B$ is the flipped-bit row naming the bit coordinate through which the walk exits.
\item $T$ is the outside-target row naming the codon reached after the bit flip.
\item $K$ is the target-class row recording the standard genetic-code class of $T$.
\item $L$ is the transition ledger binding $O$, $B$, $T$, and $K$ to the displayed killed-walk boundary half-edge.
\item $H$ stores componentwise $\hsame$ transport for origin, bit, outside target, target class, transition ledger, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from the first-exit request through $O,B,T,K,L,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\FirstExitChannelUp$, the six-boundary-tile coverage, the window-coverage trigger algebra, the post-median gate shell, the punctured-cube skeleton, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FirstExitChannelUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $O,B,T,K,L,H,C,P,N$. It has no coordinate for global codon-table optimality, unseen-window extrapolation, host biochemical causality, an unrestricted Markov-chain oracle, or bridge checking.
\end{definition}

\begin{theorem}[First-exit channel NameCert obligations]
\label{thm:first-exit-channel-namecert-obligations}
For every accepted $\FirstExitChannelUp$ carrier
$$
F=(O,B,T,K,L,H,C,P,N),
$$
the local $\NameCert_{\FirstExitChannelUp}$ surface consists exactly of carrier admission for the origin-state row $O$, flipped-bit row $B$, outside-target row $T$, target-class row $K$, transition ledger $L$, componentwise transport $H$, continuation replay $C$, package provenance $P$, and local naming row $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:first-exit-channel-carrier}. The projection exposes origin, bit, outside target, target class, and transition rows before it exposes the structural rows.

The transition ledger $L$ is the only row that binds the four observational rows into a first-exit boundary half-edge. Thus a consumer cannot replace the displayed exit by an unrelated codon adjacency or by an unrestricted stochastic-process read.

The rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier has no coordinate for global codon-table optimality, unseen-window extrapolation, or host biochemical causality, the displayed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[First-exit channel geometry readback]
\label{thm:first-exit-channel-geometry-readback}
Every accepted first-exit geometry read through $\FirstExitChannelUp$ factors through
$$
O,B,T,K\longmapsto L\longmapsto H,C,P,N .
$$
Consequently the read may export origin-state distribution rows, flipped-bit rows, outside-target rows, and target-class rows only after the transition ledger and structural NameCert rows are displayed.
\end{theorem}
\begin{proof}
Start with the four observational rows $O,B,T,K$. They name the origin state, flipped bit, outside target, and target class, but they do not by themselves assert first-exit geometry.

The transition ledger $L$ binds those rows to the killed-walk boundary half-edge. This is the point where the packet distinguishes a first exit from an arbitrary bit flip or a free codon-class lookup.

Finally $H,C,P,N$ preserve the same finite route under transport, replay, provenance, and local naming. Any geometry claim that bypasses one of these rows, or that asserts an unseen-window optimizer or host biochemical cause, is outside the accepted packet.
\end{proof}

\closureat{FirstExitChannelUp}{seedStr}

\begin{closurestatus}{\FirstExitChannelUp}
  \constructivestory{A $\FirstExitChannelUp$ packet is generated from origin-state rows, flipped-bit rows, outside-target rows, target-class rows, a transition ledger, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:first-exit-channel-carrier}, \autoref{thm:first-exit-channel-namecert-obligations}, and \autoref{thm:first-exit-channel-geometry-readback} seed the finite first-exit channel carrier, NameCert obligations, and geometry readback route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close global codon-table optimality, unseen-window extrapolation, host biochemical causality, unrestricted Markov-chain semantics, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, origin-bit-target-class ledger exactness, transition replay, provenance, local naming, and Nontrivial and FieldFaithful TasteGate instances for BEDC.Derived.FirstExitChannelUp.TasteGate.}
\end{closurestatus}
