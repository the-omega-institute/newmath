\begin{theorem}[ClosedTermSubstitutionBoundary normalization-router dependency route]
\label{thm:closed-term-substitution-boundary-normalization-router-dependency-route}
Every $\ClosedTermSubstitutionBoundaryUp$ consumer that sends closed substitution data toward MetaCIC normalization must expose the candidate-mediated router of \autoref{ch:concrete-instances-metacicnormalizationcertificaterouter-namecert}. The boundary supplies the closed projection row, while the router supplies candidate evidence, Pi and application adequacy, authorized generator, compiler recognizer, kernel acceptance, replay, provenance, and local naming rows before any normalization-facing read is consumed. Thus the dependency is horizontal and finite: it records a displayed route from closed-term substitution into the normalization router, not a host substitution theorem, evidence-free normalization theorem, subject-reduction theorem, or quotient reduction equality.
\end{theorem}
\leanchecked{BEDC.Derived.ClosedTermSubstitutionBoundaryUp.ClosedTermSubstitutionBoundaryNormalizationRouterDependencyRoute}
