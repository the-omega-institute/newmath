\begin{theorem}[ClosedTermSubstitutionBoundary sibling lattice route]
\label{thm:closed-term-substitution-boundary-sibling-lattice-route}
Every $\ClosedTermSubstitutionBoundaryUp$ consumer that treats the boundary as a closed substitution lattice must read four sibling rows before the local boundary is exported. The substitution operation surface is supplied by \autoref{ch:concrete-instances-closedtermsubstitution-namecert}; the MetaCIC socket and critical-path readback are supplied by \autoref{ch:concrete-instances-metaciccriticalpath-namecert}; the finite binder budget is supplied by \autoref{ch:concrete-instances-binderbudgetseal-namecert}; and the permitted generator boundary is supplied by \autoref{ch:concrete-instances-authorizedgeneratorrecursor-namecert}. The route therefore exposes a finite lattice of operation, socket, budget, generator, transport, replay, provenance, and local naming rows, without exporting quotient term equality, unrestricted substitution, host normalization, or a hidden subject-reduction theorem.
\end{theorem}
\begin{proof}
Read the existing sibling dependency route and the normalization-router dependency route. They already force the boundary carrier to pass through the closed substitution operation row, MetaCIC critical-path socket, binder-budget seal, and authorized-generator boundary before any normalization-facing consumer reads the substitution data.

The present route repacks those sibling reads as the local lattice boundary. Since each cited chapter contributes a displayed NameCert row and none contributes quotient syntax equality, unrestricted substitution, host normalization, or erased subject reduction, the lattice exports exactly the finite operation, socket, budget, generator, transport, replay, provenance, and local naming rows.
\end{proof}

\begin{theorem}[ClosedTermSubstitutionBoundary MetaCIC sibling lattice route]
\label{thm:closed-term-substitution-boundary-metacic-sibling-lattice-route}
Every MetaCIC critical-path consumer of $\ClosedTermSubstitutionBoundaryUp$ keeps the closed-substitution operation row, the binder-budget seal, the authorized-generator recursor row, the residual socket, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming visible before it exports the boundary to a critical-path read. The route refuses quotient term equality, unrestricted substitution, host normalization, hidden subject reduction, and kernel consistency.
\end{theorem}
\begin{proof}
Apply \autoref{thm:closed-term-substitution-boundary-sibling-lattice-route}. It already exposes the closed-substitution operation row, MetaCIC socket, binder-budget seal, authorized-generator boundary, transport, replay, provenance, and local naming.

Now read the MetaCIC critical-path dependency through \autoref{ch:concrete-instances-metaciccriticalpath-namecert}. The critical-path consumer can only reuse the displayed residual socket and the rows supplied by the sibling lattice; no new quotient term equality, unrestricted substitution, host normalization, hidden subject-reduction theorem, or kernel-consistency assertion is present in the packet.
\end{proof}
