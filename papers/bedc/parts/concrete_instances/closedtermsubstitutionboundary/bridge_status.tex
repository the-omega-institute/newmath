The $\ClosedTermSubstitutionBoundaryUp$ dossier records its bridged-closure route as a paper-side ledger row: the bridge route is read through the displayed closed-source, shift, substitution, audit, transport, replay, provenance, and local $\NameCert$ rows, while the verification axis remains bound only by the closurestatus block in \texttt{namecert\_construction\_core.tex}. This ledger row does not add a subject-reduction theorem, host syntax quotient, compiler-correctness theorem, or hidden substitution semantics.
