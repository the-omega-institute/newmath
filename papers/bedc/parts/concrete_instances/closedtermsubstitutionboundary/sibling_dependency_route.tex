\begin{theorem}[ClosedTermSubstitutionBoundary sibling dependency route]
\label{thm:closed-term-substitution-boundary-sibling-dependency-route}
Every $\ClosedTermSubstitutionBoundaryUp$ consumer that reads the boundary as MetaCIC substitution data must first expose the sibling substitution surface of \autoref{ch:concrete-instances-closedtermsubstitution-namecert} and the critical-path readback boundary of \autoref{ch:concrete-instances-metaciccriticalpath-namecert}. The $\BinderBudgetSealUp$ row of \autoref{ch:concrete-instances-binderbudgetseal-namecert} supplies the finite binder budget, and the $\AuthorizedGeneratorRecursorUp$ row of \autoref{ch:concrete-instances-authorizedgeneratorrecursor-namecert} supplies the permitted generator boundary before the substitution readback is consumed. The $\ClosedTermSubstitutionUp$ row supplies the operation-facing substitution surface, while the $\MetaCICCriticalPathUp$ row supplies the critical-path socket where binder budgets, closedness ledgers, and substitution reads remain audit-visible. The route is finite: it records only the displayed substitution operation row, closedness boundary row, transport, replay, provenance, and local naming data. It does not claim beta-conversion, quotient term equality, unrestricted normalization, or a hidden subject-reduction theorem.
\end{theorem}
\leanchecked{BEDC.Derived.ClosedTermSubstitutionBoundaryUp.ClosedTermSubstitutionBoundarySiblingDependencyRoute}
\begin{proof}
Project the closed-term substitution boundary carrier from the core construction. Its operation row is available only through the displayed source and value closedness witnesses, ledger row, audit row, transport, replay, provenance, and local naming certificate.

Read the sibling anchors as route boundaries. The $\ClosedTermSubstitutionUp$ chapter names the substitution operation surface consumed by the boundary, the $\BinderBudgetSealUp$ chapter supplies the finite binder budget, the $\AuthorizedGeneratorRecursorUp$ chapter supplies the permitted generator boundary, and the $\MetaCICCriticalPathUp$ chapter names the critical-path socket in which the readback remains visible to MetaCIC consumers.

Since the carrier and the two sibling routes expose no beta-conversion theorem, quotient syntax equality, unrestricted normalization theorem, or hidden subject-reduction certificate, the dependency route remains exactly the displayed finite handoff.
\end{proof}
