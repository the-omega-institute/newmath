\begin{theorem}[Closed-term substitution boundary MetaCIC consumer nonescape]
\label{thm:closed-term-substitution-boundary-metacic-consumer-nonescape}
Every MetaCIC confluence or subject-reduction consumer of an accepted $\ClosedTermSubstitutionBoundaryUp$ packet must read the source closedness row, value closedness row, shift boundary row, substitution boundary row, finite ledger, audit row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row before citing the boundary. The consumer-facing route is confined to
$$
T,V,d,C_{\mathsf{src}},C_{\mathsf{val}},Q,R,L,A,H,C,P,N.
$$
It supplies no full subject-reduction theorem, compiler correctness theorem, host substitution semantics, selected term representative, or unregistered free-variable data.
\end{theorem}
\leanchecked{BEDC.Derived.ClosedtermsubstitutionboundaryUp.ClosedTermSubstitutionBoundaryMetacicConsumerNonescape}
\begin{proof}
Start from source and value closedness admission. By \autoref{thm:closed-term-substitution-boundary-source-closedness-admission} and \autoref{thm:closed-term-substitution-boundary-value-closedness-admission}, the rows $C_{\mathsf{src}}$ and $C_{\mathsf{val}}$ are displayed before either operation row can serve a downstream consumer.

Next apply substitution-row exactness. The theorem \autoref{thm:closed-term-substitution-boundary-substitution-row-exactness} forces the substitution boundary row $R$ to remain attached to the same source row, value row, depth row, and closedness witnesses, while the shift row $Q$ stays inside the same operation surface.

Finally use ledger non-escape, together with the route-saturation package. The rows $L,A,H,C,P,N$ record the closedness and operation reads, transport them componentwise, replay them through displayed continuation, and name the local boundary. By \autoref{thm:closed-term-substitution-boundary-ledger-non-escape} and \autoref{thm:closed-term-substitution-boundary-route-saturation}, a MetaCIC confluence or subject-reduction consumer receives only that finite packet and no host substitution semantics, compiler correctness theorem, full subject-reduction theorem, selected representative, or unregistered free-variable data.
\end{proof}

\begin{theorem}[Closed-term substitution boundary MetaCIC dependency]
\label{thm:closed-term-substitution-boundary-metacic-dependency}
Every MetaCIC consumer of an accepted $\ClosedTermSubstitutionBoundaryUp$ packet must route through the closed source row, closed value row, shift boundary row, substitution boundary row, ledger row, audit row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row before the packet can feed a normalization or confluence surface. In particular, any handoff to the critical-path consumer surface of \autoref{ch:concrete-instances-metaciccriticalpath-namecert} and to a confluence-facing sibling such as \autoref{ch:concrete-instances-metacic-local-confluence-frontier-namecert} is mediated by the existing closed rows; it does not export subject reduction, compiler correctness, host substitution semantics, a selected representative, or unregistered free-variable data.
\end{theorem}
\begin{proof}
Start with \autoref{thm:closed-term-substitution-boundary-metacic-consumer-nonescape}. It already forces every MetaCIC confluence or subject-reduction consumer to expose the source closedness, value closedness, shift boundary, substitution boundary, finite ledger, audit, transport, replay, provenance, and local naming rows.

Next read the MetaCIC critical-path surface. The critical-path packet of \autoref{ch:concrete-instances-metaciccriticalpath-namecert} consumes closed substitution support only as a displayed dependency row before normalization or confluence traffic can continue; it does not turn that dependency into subject reduction or compiler correctness.

Finally pass to the confluence-facing sibling. A local confluence frontier such as \autoref{ch:concrete-instances-metacic-local-confluence-frontier-namecert} may receive the closed substitution boundary as a premise row, but the nonescape theorem keeps the route attached to the same closed source and value rows. Thus the dependency is horizontal and finite, not a new exported metatheorem.
\end{proof}
