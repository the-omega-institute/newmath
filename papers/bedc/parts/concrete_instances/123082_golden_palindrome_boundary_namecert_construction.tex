\chapter{A Concrete Naming Certificate for $\GoldenPalindromeBoundaryUp$}
\label{ch:concrete-instances-golden-palindrome-boundary-namecert}
\origin{ai}

The $\GoldenPalindromeBoundaryUp$ packet realizes the finite boundary rows of \autoref{ch:visions-innovation-input-anchored-rp-golden-palindrome-boundary} as NameCert data. It packages the anchored finite-window RP test for the pentary golden-palindrome branches, the window-six golden-mean Hankel--Turan certificate of \autoref{ch:concrete-instances-window6-golden-mean-hankel-turan}, the finite RP boundary of \autoref{ch:concrete-instances-finite-window-rp-boundary-namecert}, the golden-mean shift equivalence of \autoref{ch:concrete-instances-window6-golden-mean-shift-equivalence}, and the quadratic capstone route of \autoref{ch:concrete-instances-window6-golden-mean-zeta-program-hub}. The packet is a finite boundary carrier; it does not assert a global OS/RP theorem, a spectral theorem, a host positivity principle, or an infinite symbolic-dynamics object.

\begin{definition}[Golden palindrome boundary carrier]
\label{def:golden-palindrome-boundary-carrier}
A $\GoldenPalindromeBoundaryUp$ carrier is a finite $\BHist$ packet
$$
G=(W,A,B,T,S,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $W$ is the finite pentary palindrome window row, naming the two golden branches and their anchored observation sites.
\item $A$ is the anchored RP boundary row, recording the reflected future-block matrix before any translation-invariant simplification is read.
\item $B$ is the finite-window failure row, exposing the blocked neutral $L=2$ boundary for the golden-palindrome branches.
\item $T$ is the window-six golden-mean Hankel--Turan row available as a finite determinant witness.
\item $S$ is the golden-mean shift-equivalence row used only as finite symbolic provenance.
\item $Q$ is the golden-mean quadratic capstone row that records the nearby finite algebraic witness surface.
\item $H$ records componentwise $\hsame$ transport for palindrome windows, anchored RP rows, boundary failures, golden-mean witnesses, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from $W$ through $A,B,T,S,Q$.
\item $P$ is the $\Pkg$ provenance over $\GoldenPalindromeBoundaryUp$, $\FiniteWindowRpBoundaryUp$, window-six golden-mean certificates, $\GoldenMeanShiftUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\GoldenPalindromeBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $W,A,B,T,S,Q,H,C,P,N$. It has no coordinate for an infinite RP matrix, a global symbolic shift, host positivity, spectral completeness, or a hidden phase-space quotient.
\end{definition}
\leanstmt{BEDC.Derived.GoldenPalindromeBoundaryUp}

\begin{theorem}[Golden palindrome boundary NameCert obligations]
\label{thm:golden-palindrome-boundary-namecert-obligations}
For the carrier of \autoref{def:golden-palindrome-boundary-carrier}, the local $\NameCert_{\GoldenPalindromeBoundaryUp}$ surface consists exactly of carrier admission for $G=(W,A,B,T,S,Q,H,C,P,N)$, pentary palindrome window exposure through $W$, anchored RP boundary exposure through $A$, finite boundary failure through $B$, window-six Hankel--Turan witness exposure through $T$, finite golden-mean shift-equivalence provenance through $S$, quadratic capstone provenance through $Q$, and transport, replay, provenance, and naming stability through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier. The rows $W,A,B,T,S,Q,H,C,P,N$ are the complete local inventory.

The boundary rows are read first. The pentary palindrome row $W$ fixes the finite branches, the anchored RP row $A$ fixes the reflection workflow, and the failure row $B$ records the blocked neutral window.

The golden-mean witness rows $T,S,Q$ are then read as finite provenance and algebraic support. They do not replace the anchored boundary row by an infinite shift or a host positivity theorem.

Rows $H,C,P,N$ preserve the same inventory under transport, replay, provenance, and naming. Since the carrier has no further coordinate, the listed rows exhaust the NameCert obligation surface.
\end{proof}

\begin{theorem}[Golden palindrome boundary anchored nonescape]
\label{thm:golden-palindrome-boundary-anchored-nonescape}
Every accepted $\GoldenPalindromeBoundaryUp$ read of a pentary golden-palindrome branch factors through the finite route
$$
W,A,B,T,S,Q,H,C,P,N.
$$
The read remains anchored: it may consume the finite-window RP boundary, window-six Hankel--Turan witness, golden-mean shift-equivalence row, and quadratic capstone row, but it cannot replace them by an infinite RP positivity claim, a host spectral theorem, or a quotient symbolic-dynamics object.
\end{theorem}
\begin{proof}
Start from an accepted packet $G=(W,A,B,T,S,Q,H,C,P,N)$. The only branch data are the finite palindrome window row $W$ and the anchored RP row $A$.

The boundary failure row $B$ is displayed before any golden-mean support row is available. Thus a consumer cannot bypass the anchored matrix by appealing directly to shift equivalence or algebraic provenance.

The witness rows $T,S,Q$ remain finite rows transported by $H$ and replayed by $C$. Provenance $P$ and local naming $N$ name the same finite carrier, so no infinite positivity, spectral, or quotient coordinate can be exported.
\end{proof}

\falsifiablePrediction{If a purported golden-palindrome boundary certificate omits the anchored RP row, finite boundary failure row, or finite golden-mean witness rows, it cannot satisfy the GoldenPalindromeBoundaryUp NameCert obligations.}

\independenceWitness{window6-golden-mean-hankel-turan, finite-window-rp-boundary, golden-mean-shift-equivalence, golden-mean-quadratic-capstone: the Hankel--Turan row supplies a determinant witness, the finite RP boundary supplies the anchored test, the shift-equivalence row supplies finite symbolic provenance, and the quadratic capstone supplies algebraic support. None alone carries the pentary palindrome window, anchored RP boundary, failure row, three witness rows, transport, replay, provenance, and local NameCert row in one packet.}

\closureat{GoldenPalindromeBoundaryUp}{seedStr}

\begin{closurestatus}{\GoldenPalindromeBoundaryUp}
  \constructivestory{A $\GoldenPalindromeBoundaryUp$ packet is generated from finite pentary palindrome windows, anchored RP boundary rows, finite boundary-failure rows, window-six golden-mean Hankel--Turan witnesses, shift-equivalence provenance, quadratic capstone rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:golden-palindrome-boundary-carrier}, \autoref{thm:golden-palindrome-boundary-namecert-obligations}, and \autoref{thm:golden-palindrome-boundary-anchored-nonescape} bind the finite anchored golden-palindrome boundary packet.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, a global OS/RP theorem, infinite symbolic dynamics, host positivity, spectral completeness, or quotient phase-space equality.}
  \upgradepath{Lean closure requires checked carrier admission and checked anchored nonescape for the finite golden-palindrome boundary route.}
\end{closurestatus}
