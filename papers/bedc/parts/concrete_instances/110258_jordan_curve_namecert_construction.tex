\chapter{A Concrete Naming Certificate for $\JordanCurveUp$}
\label{ch:concrete-instances-jordan-curve-namecert}
\origin{human}

Following Jordan and constructive topology, the $\JordanCurveUp$ packet records the finite polygonal and winding-ledger route by which a displayed simple closed curve in the real plane may be consumed as a separation boundary. It sits after the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the circle carrier of \autoref{ch:concrete-instances-s1-namecert}, the graph and modulus rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the finite compact-metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, and the simplex inventory of \autoref{ch:concrete-instances-simplicialcomplex-namecert}. The packet names a constructive finite obligation surface; it does not import classical open-cover compactness, selected complementary components, quotient path equality, or a choice-dependent separation theorem.

\begin{definition}[Jordan curve carrier]
\label{def:jordan-curve-carrier}
A $\JordanCurveUp$ carrier is a finite $\BHist$ packet
$$
J=(S,F,P,W,O,I,E,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\SOneUp$ source row, recording the circle parameter ledger and its local classifier.
\item $F$ is the $\ContinuousMapUp$ row into the displayed $\RealUp\times\RealUp$ plane, with graph and modulus observations visible.
\item $P$ is the finite polygonal approximation row, read through a $\SimplicialComplexUp$ subdivision and its vertex-image ledger.
\item $W$ is the winding ledger, recording the finite edge-crossing and sign-change data used to compare test points with the polygonal boundary.
\item $O$ is the orientation row, recording the compatible cyclic order inherited from the $\SOneUp$ row and the polygonal subdivision.
\item $I$ is the injectivity-apartness row, stating that distinct inspected arcs carry displayed positive separation windows before they are used by the boundary read.
\item $E$ is the exterior-interior ledger, recording only the finite side classification certified by the current polygonal and winding rows.
\item $H$ records componentwise $\hsame$ transport for $S,F,P,W,O,I,E,C,Q,N$.
\item $C$ records displayed $\Cont$ replay from circle, map, polygonal, winding, orientation, and apartness rows to the side-classification row.
\item $Q$ is the $\Pkg$ provenance over $\JordanCurveUp$, $\RealUp$, $\SOneUp$, $\ContinuousMapUp$, $\CompactMetricUp$, $\SimplicialComplexUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\JordanCurveUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,F,P,W,O,I,E,H,C,Q,N$. It has no coordinate for a host Jordan curve theorem, selected complementary components, open-cover compactness, arbitrary path lifting, quotient path equality, or countable-choice separation.
\end{definition}

\begin{theorem}[Jordan curve orientation ledger]
\label{thm:jordan-curve-orientation-ledger}
For every accepted $\JordanCurveUp$ carrier
$$
J=(S,F,P,W,O,I,E,H,C,Q,N),
$$
the orientation row $O$ is determined by the finite $\SOneUp$ cyclic order, the $\SimplicialComplexUp$ subdivision in $P$, and the displayed $\ContinuousMapUp$ edge-image ledger in $F$. The winding ledger $W$ can read orientation only through those rows, transported by $H$ and replayed by $C$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The only circle-facing coordinate is $S$, and the only finite boundary skeleton is the polygonal row $P$ together with the displayed edge images in $F$.

The orientation row $O$ is therefore read by following the cyclic order of $S$ across the subdivision cells of $P$. Each edge-image comparison is a finite real-plane observation supplied by $F$, not by an ambient topological orientation principle.

The winding ledger $W$ consumes that ordered edge list. Componentwise transport $H$ and replay $C$ preserve the same ordered rows, while $Q$ and $N$ record provenance and local naming. Since the carrier contains no host orientation object or quotient path coordinate, the finite ledger is the entire orientation source.
\end{proof}

\begin{theorem}[Jordan curve winding separation boundary]
\label{thm:jordan-curve-winding-separation}
Every side-classification read from an accepted $\JordanCurveUp$ packet factors through the finite route
$$
S,F,P,O,I,W\longmapsto E\longmapsto H,C,Q,N .
$$
Thus the packet exposes polygonal approximation, injectivity-apartness, orientation, and winding ledgers before it names an interior or exterior row. It does not assert a blanket classical Jordan separation theorem, selected complementary components, open-cover compactness, quotient path equality, or choice-dependent planar separation.
\end{theorem}
\begin{proof}
Read the carrier of \autoref{def:jordan-curve-carrier}. The curve source is $S$, the displayed map is $F$, and the finite polygonal boundary is $P$.

Before a side row is exported, the packet requires the orientation ledger $O$, injectivity-apartness row $I$, and winding ledger $W$. These rows are finite $\BHist$ data: the winding read uses edge crossings and signs already attached to the polygonal boundary.

The exterior-interior row $E$ is downstream of that route. Transport, replay, provenance, and naming through $H,C,Q,N$ preserve the same finite packet. The excluded host theorem, selected components, open covers, quotient paths, and choice rows are not coordinates of the carrier, so they cannot be read from the local NameCert surface.
\end{proof}

\falsifiablePrediction{A $\JordanCurveUp$ consumer that exports side separation without reading $P,W,O,I$ refutes the carrier surface.}

\independenceWitness{real, sone, continuousmap, compactmetric, simplicialcomplex: the sibling chapters supply real-plane coordinates, circle source data, continuous graph and modulus rows, compact-metric finite inspection, and subdivision cells separately, while $\JordanCurveUp$ binds them into the finite packet $J=(S,F,P,W,O,I,E,H,C,Q,N)$.}

\closureat{JordanCurveUp}{seedStr}

\begin{closurestatus}{\JordanCurveUp}
  \constructivestory{A $\JordanCurveUp$ packet is generated from a circle source row, continuous real-plane map row, finite polygonal subdivision, winding ledger, orientation row, injectivity-apartness row, side-classification ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:jordan-curve-carrier}, \autoref{thm:jordan-curve-orientation-ledger}, and \autoref{thm:jordan-curve-winding-separation} seed the finite polygonal, winding, orientation, apartness, side-classification, transport, replay, provenance, and local naming surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, the classical Jordan curve theorem, selected complementary components, open-cover compactness, arbitrary path lifting, quotient path equality, or choice-dependent separation.}
  \upgradepath{Obligation closure requires checked carrier admission, polygonal approximation exactness, winding-ledger stability, orientation transport, injectivity-apartness handoff, side-classification non-escape, replay, provenance, and local naming for BEDC.Derived.JordanCurveUp.}
\end{closurestatus}
