\chapter{A Concrete Naming Certificate for $\MetricCompletionReflectionComparisonUp$}
\label{ch:concrete-instances-metric-completion-reflection-comparison-namecert}
\origin{human}

Following Bishop and Bridges on constructive completion and Kelley reflection terminology, the $\MetricCompletionReflectionComparisonUp$ packet compares the finite completion presentations already named by \autoref{ch:concrete-instances-metriccompletion-namecert}, \autoref{ch:concrete-instances-cauchy-completion-reflection-namecert}, \autoref{ch:concrete-instances-separated-completion-reflection-namecert}, and \autoref{ch:concrete-instances-bishop-metric-completion-namecert}. It records a comparison table over displayed stream, regular-rational, dyadic, and Real-facing rows. The packet names only a finite reflection-comparison surface, not a quotient completion, selected representative, countable-choice selector, host equality relation, or ambient Real-completeness theorem.

\begin{definition}[Metric completion reflection comparison carrier]
\label{def:metric-completion-reflection-comparison-carrier}
A $\MetricCompletionReflectionComparisonUp$ carrier is a finite $\BHist$ packet
$$
Q=(M,C,S,B,W,G,D,R,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricCompletionUp$ row, recording the baseline completion witness and its visible metric-source route.
\item $C$ is the $\CauchyCompletionReflectionUp$ row, recording the reflection-facing Cauchy presentation without quotienting Cauchy names.
\item $S$ is the $\SeparatedCompletionReflectionUp$ row, recording the separated zero-distance comparison route before endpoint equality is public.
\item $B$ is the $\BishopMetricCompletionUp$ row, recording the constructive finite-window completion presentation.
\item $W$ is the $\StreamNameUp$ window row shared by the completion presentations.
\item $G$ is the $\RegSeqRatUp$ regular rational readback row for the displayed Cauchy windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance and comparison row used to align finite windows.
\item $R$ is the $\RealUp$ seal row for the displayed distance and agreement comparisons.
\item $H$ records componentwise $\hsame$ transport for all completion, reflection, window, readback, seal, provenance, and naming rows.
\item $T$ records displayed $\Cont$ replay from a comparison request through $M,C,S,B,W,G,D,R,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\MetricCompletionReflectionComparisonUp$, $\MetricCompletionUp$, $\CauchyCompletionReflectionUp$, $\SeparatedCompletionReflectionUp$, $\BishopMetricCompletionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetricCompletionReflectionComparisonUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,C,S,B,W,G,D,R,H,T,P,N$. It has no coordinate for quotient completions, selected representatives, countable choice, host equality of completed names, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Metric completion reflection comparison obligations]
\label{thm:metric-completion-reflection-comparison-obligations}
For every accepted $\MetricCompletionReflectionComparisonUp$ carrier
$$
Q=(M,C,S,B,W,G,D,R,H,T,P,N),
$$
the local $\NameCert_{\MetricCompletionReflectionComparisonUp}$ surface consists exactly of baseline completion admission, Cauchy-reflection exposure, separated-reflection exposure, Bishop finite-window exposure, StreamName window readback, RegSeqRat regular readback, DyadicRatCore tolerance comparison, Real seal discipline, componentwise transport, displayed replay, package provenance, and local naming. Every comparison between the four completion presentations must display $W,G,D,R$ before the structural rows $H,T,P,N$ may export the comparison.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metric-completion-reflection-comparison-carrier}. The completion rows $M,C,S,B$ are all visible before a comparison request can be replayed.

Read the numerical rows next. The StreamName window $W$, regular rational readback $G$, dyadic tolerance row $D$, and Real seal $R$ are the only rows that align the four completion presentations.

The structural rows then preserve that comparison. Componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $T$, provenance $P$, and local naming $N$ keep the route inside the same finite packet.

Since quotient completions, selected representatives, countable-choice schedules, host equality, and ambient Real completeness are absent from the carrier, the displayed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Metric completion reflection comparison nonescape]
\label{thm:metric-completion-reflection-comparison-nonescape}
Every accepted $\MetricCompletionReflectionComparisonUp$ comparison factors through the finite route
$$
M,C,S,B,W,G,D,R,H,T,P,N.
$$
The route compares the MetricCompletion, CauchyCompletionReflection, SeparatedCompletionReflection, and BishopMetricCompletion presentations only by displayed StreamName windows, RegSeqRat readbacks, DyadicRatCore tolerance rows, and Real seal rows. It cannot export quotient completion objects, selected representatives, countable choice, host equality of completed names, or ambient Real completeness.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:metric-completion-reflection-comparison-obligations}. It places every accepted comparison on the route $M,C,S,B,W,G,D,R,H,T,P,N$.

Use the sibling completion chapters only through their public rows. The baseline completion, Cauchy reflection, separated reflection, and Bishop finite-window presentations contribute the rows $M,C,S,B$, while the shared numerical comparison is mediated by $W,G,D,R$.

Transport, replay, provenance, and local naming do not add another completion object. They only preserve the displayed comparison route.

The excluded quotient, representative, choice, host-equality, and ambient-completeness data require coordinates that the carrier omits. Hence no accepted comparison can escape the finite NameCert packet.
\end{proof}

\begin{closurestatus}{\MetricCompletionReflectionComparisonUp}
  \constructivestory{A $\MetricCompletionReflectionComparisonUp$ packet is generated from MetricCompletion, CauchyCompletionReflection, SeparatedCompletionReflection, BishopMetricCompletion, StreamName, RegSeqRat, DyadicRatCore, Real, hsame, Cont, Pkg, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metric-completion-reflection-comparison-carrier}, \autoref{thm:metric-completion-reflection-comparison-obligations}, and \autoref{thm:metric-completion-reflection-comparison-nonescape} seed the finite comparison surface between constructive completion presentations.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient completion objects, selected representatives, countable choice, host equality of completed names, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, completion-row exposure, numerical readback alignment, Real seal discipline, transport, replay, provenance, and nonescape for BEDC.Derived.MetricCompletionReflectionComparisonUp.}
\end{closurestatus}
