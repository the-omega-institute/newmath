\chapter{A Concrete Naming Certificate for $\MinimalTriggerHomologyCoreUp$}
\label{ch:concrete-instances-minimal-trigger-homology-core-namecert}
\origin{ai}

The $\MinimalTriggerHomologyCoreUp$ packet realizes the zero-marker support-nerve sphere core of \autoref{thm:philosophy-minimal-trigger-sphere} and the finite count table of \autoref{thm:philosophy-minimal-trigger-homology-core-counts} as finite BEDC NameCert data. It sits beside the support-nerve realization of \autoref{ch:concrete-instances-support-nerve-realization-namecert}, the support-nerve census of \autoref{ch:concrete-instances-support-nerve-census-namecert}, the codon lift packet of \autoref{ch:concrete-instances-minimal-trigger-lift-bundle-namecert}, and the boundary-tile packet of \autoref{ch:concrete-instances-dna-window-six-boundary-tile-namecert}. The packet names the quotient trigger, complement simplex cover, boundary-simplex nerve, reduced homology degree, finite count table, TasteGate admission boundary, transport, replay, provenance, and local naming rows; it is not a biological mechanism, stochastic selection claim, or unbounded enumeration oracle.

\begin{definition}[Minimal trigger homology-core carrier]
\label{def:minimal-trigger-homology-core-carrier}
A $\MinimalTriggerHomologyCoreUp$ carrier is a finite $\BHist$ packet
$$
M=(Q,F,N,R,B,K,T,H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the quotient-level minimal full-trigger row, displaying $A=\{S_{1},\ldots,S_{m}\}$ and its finite support cells.
\item $F$ is the complement-simplex row, recording $F_i=\Omega\setminus S_i$ and the finite simplices $2^{F_i}$.
\item $N$ is the support-nerve row, identifying the union $\bigcup_i 2^{F_i}$ as the displayed support nerve.
\item $R$ is the boundary-simplex route row, recording that the cover nerve is $\partial\Delta^{m-1}$ because every proper complement intersection is inhabited and the full intersection is empty.
\item $B$ is the reduced-homology row, naming the sphere type $S^{m-2}$ and the corresponding one-dimensional reduced homology witness.
\item $K$ is the finite count row, displaying the quotient and codon count table for trigger sizes $1,2,3,4$.
\item $T$ is the TasteGate obligation row, requiring later $\mathsf{Nontrivial}\ \MinimalTriggerHomologyCoreUp$ and $\mathsf{FieldFaithful}\ \MinimalTriggerHomologyCoreUp$ instances to preserve $Q,F,N,R,B,K$ before downstream consumers may use the packet as a stable AI-origin chapter surface.
\item $H$ records componentwise $\hsame$ transport for quotient triggers, complement simplices, support nerves, boundary-simplex routes, homology rows, count rows, TasteGate rows, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a quotient trigger through complement simplices, support nerves, boundary-simplex routing, homology readback, and finite count exposure.
\item $P$ is the $\Pkg$ provenance over $\MinimalTriggerHomologyCoreUp$, \autoref{thm:philosophy-minimal-trigger-sphere}, \autoref{thm:philosophy-minimal-trigger-homology-core-counts}, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\MinimalTriggerHomologyCoreUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $Q,F,N,R,B,K,T,H,C,P,L$. It has no coordinate for empirical biology, stochastic fitness, a host simplicial-complex library, or an unrestricted search over supports.
\end{definition}

\begin{theorem}[Minimal trigger homology-core NameCert obligations]
\label{thm:minimal-trigger-homology-core-namecert-obligations}
For every accepted $\MinimalTriggerHomologyCoreUp$ carrier $M=(Q,F,N,R,B,K,T,H,C,P,L)$, the local $\NameCert_{\MinimalTriggerHomologyCoreUp}$ surface consists exactly of quotient-trigger admission through $Q$, complement-simplex admission through $F$, support-nerve construction through $N$, boundary-simplex routing through $R$, reduced-homology readback through $B$, finite count preservation through $K$, TasteGate obligation exposure through $T$, componentwise transport through $H$, replay through $C$, provenance through $P$, and local naming through $L$.
\end{theorem}
\begin{proof}
Project the carrier. The quotient trigger row $Q$ fixes the finite support family, and the complement row $F$ records the finite simplices that generate the support nerve.

The support-nerve row $N$ and boundary-simplex route $R$ encode the nerve-theorem calculation from \autoref{thm:philosophy-minimal-trigger-sphere}. The reduced-homology row $B$ reads the displayed sphere degree, and the count row $K$ records the quotient and codon tables from \autoref{thm:philosophy-minimal-trigger-homology-core-counts}.

The TasteGate row $T$ is an explicit downstream obligation: later $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances must preserve the carrier rows, but those instances are not hidden assumptions in the present packet. The rows $H,C,P,L$ transport, replay, source, and name the same finite data.
\end{proof}

\begin{theorem}[Minimal trigger homology-core support-nerve route]
\label{thm:minimal-trigger-homology-core-support-nerve-route}
Every homology-core read exported by an accepted $\MinimalTriggerHomologyCoreUp$ carrier factors through the finite route
$$
Q,F \longmapsto N,R \longmapsto B,K \longmapsto T,H,C,P,L .
$$
The route requires the quotient minimal-trigger row and complement-simplex row before the support nerve, boundary-simplex route, homology witness, count table, TasteGate obligation, transport, replay, provenance, and local naming rows can be consumed.
\end{theorem}
\begin{proof}
Start with a homology-core read. The row $Q$ displays the quotient-level minimal full trigger, while $F$ displays its complement simplices; neither row by itself stores a homology conclusion.

The read must pass through $N$ and $R$. These rows expose the support nerve and the boundary-simplex route that turns minimal full-trigger data into the sphere core.

The rows $B$ and $K$ then read the reduced homology degree and the finite count table. The TasteGate row $T$ keeps the future nontriviality and field-faithfulness requirements attached to the same carrier rather than replacing the packet by a detached classifier.

Finally $H,C,P,L$ preserve the route by componentwise transport, replay, provenance, and local naming. Since the carrier has no row for a host simplicial-complex library, stochastic fitness model, or unbounded support search, every exported read is exhausted by the displayed finite route.
\end{proof}

\falsifiablePrediction{Every accepted $\MinimalTriggerHomologyCoreUp$ packet exposes quotient-trigger data, complement-simplex data, support-nerve data, boundary-simplex routing, reduced homology readback, finite count rows, TasteGate obligations, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert evidence before any downstream trigger-lattice consumer may cite the sphere-core result.}

\independenceWitness{support-nerve-realization, support-nerve-census, minimal-trigger-lift-bundle, dna-window-six-boundary-tile: none is bijective with $\MinimalTriggerHomologyCoreUp$ because this packet jointly carries quotient minimal-trigger rows, complement simplices, support-nerve routing, reduced homology, finite counts, TasteGate obligations, transport, replay, provenance, and local naming.}

\closureat{MinimalTriggerHomologyCoreUp}{seedStr}

\begin{closurestatus}{\MinimalTriggerHomologyCoreUp}
  \constructivestory{A $\MinimalTriggerHomologyCoreUp$ packet is generated from quotient minimal-trigger rows, complement-simplex rows, support-nerve rows, boundary-simplex route rows, reduced-homology rows, finite count rows, TasteGate obligation rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:minimal-trigger-homology-core-carrier}, \autoref{thm:minimal-trigger-homology-core-namecert-obligations}, and \autoref{thm:minimal-trigger-homology-core-support-nerve-route} seed the finite support-nerve sphere-core packet over quotient triggers, complement simplices, boundary-simplex routes, reduced homology, count rows, TasteGate obligations, transport, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close biological mechanism, stochastic fitness, host simplicial-complex libraries, unrestricted trigger search, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked quotient-trigger admission, complement-simplex admission, support-nerve construction, boundary-simplex route exactness, homology readback, finite count preservation, and TasteGate Nontrivial and FieldFaithful instances for BEDC.Derived.MinimalTriggerHomologyCoreUp.}
\end{closurestatus}
