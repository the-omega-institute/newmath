The $\BanachUp$ lattice reads its complete normed source through the $\NormedSpaceUp$ rows of \autoref{ch:concrete-instances-normedspace-namecert} together with the completion-facing metric rows of \autoref{ch:concrete-instances-completemetric-namecert} and \autoref{ch:concrete-instances-metric-namecert}. Thus a Banach consumer receives bounded-operator or limit data only after the normed vector carrier, norm-induced distance, regular Cauchy stream, complete-metric limit witness, transport rows, replay rows, provenance, and local naming row have been displayed.

\begin{lemma}[Banach normed complete-metric lattice anchor]
\label{lem:banach-normed-complete-metric-lattice-anchor}
Every $\BanachUp$ bounded-operator, limit, or functional-analysis export first factors through a $\NormedSpaceUp$ source row and the $\CompleteMetricUp/\MetricUp$ completion rows before it exposes any Banach-specific conclusion. The route supplies the normed carrier, scalar and additive rows, norm-induced distance, regular Cauchy observation, complete-metric limit witness, transport, replay, provenance, and local $\NameCert$ row. It supplies no detached host normed space, anonymous complete metric, selected limit outside the completion row, quotient carrier, or ambient completeness theorem.
\end{lemma}
\begin{proof}
Read the Banach hub through its normed carrier and completion children. The $\NormedSpaceUp$ source supplies the vector, scalar, additive, norm, and classifier rows used by the Banach carrier, while the $\MetricUp$ row turns the displayed norm difference into the distance surface consumed by completion.

Next use the $\CompleteMetricUp$ completion interface. Its regular Cauchy stream, modulus, limit, and uniqueness rows are the only route by which the Banach packet can export completed-limit data to later bounded-operator or functional-analysis consumers.

The two source families are therefore jointly load-bearing. A downstream Banach read that omits either the normed-space rows or the complete-metric rows has no displayed coordinate from which to obtain a Banach limit, bounded-operator input, or completion-facing handoff.
\end{proof}
