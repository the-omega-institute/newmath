\chapter{A Concrete Naming Certificate for $\FiniteModulusRegularCauchyCompletionUp$}
\label{ch:concrete-instances-finite-modulus-regular-cauchy-completion-namecert}
\origin{human}

Following Bishop and Bridges, the $\FiniteModulusRegularCauchyCompletionUp$ packet records the finite modulus surface by which a regular rational Cauchy name is read into the Real and Completion route without claiming ambient completeness. It sits after the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet packages the displayed modulus ledger only; it does not quotient regular streams, select a completed limit, assert host real equality, import countable choice, or promote $\RealUp$ to an ambient completeness theorem.

\begin{definition}[Finite modulus regular Cauchy completion carrier]
\label{def:finite-modulus-regular-cauchy-completion-carrier}
A $\FiniteModulusRegularCauchyCompletionUp$ carrier is a finite $\BHist$ packet
$$
F=(D,S,R,M,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ window row, carrying the dyadic precision scale, comparison budget, and tolerance ledger used by the completion-facing read.
\item $S$ is the $\StreamNameUp$ schedule row, listing the finite observations inspected before a modulus claim is exposed.
\item $R$ is the $\RegSeqRatUp$ regularity row, recording the rational-name regularity predicate on the displayed schedule.
\item $M$ is the tail-modulus row, binding each requested dyadic precision to a displayed tail window in $S$ and a regularity witness in $R$.
\item $E$ is the terminal $\RealUp$ seal row, available only after $D,S,R,M$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the dyadic window, stream schedule, regularity, tail modulus, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the dyadic precision request through the stream schedule, regular rational readback, tail modulus, real seal, transport, provenance, and local naming rows.
\item $P$ is the $\Pkg$ provenance over $\FiniteModulusRegularCauchyCompletionUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteModulusRegularCauchyCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,S,R,M,E,H,C,P,N$. It has no coordinate for quotient stream equality, selected limits, hidden convergence witnesses, host real equality, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Finite modulus regular Cauchy completion NameCert obligations]
\label{thm:finite-modulus-regular-cauchy-completion-obligations}
For every accepted $\FiniteModulusRegularCauchyCompletionUp$ carrier $F=(D,S,R,M,E,H,C,P,N)$, the local $\NameCert_{\FiniteModulusRegularCauchyCompletionUp}$ surface consists exactly of dyadic-window admission, finite stream scheduling, regular rational readback, tail-modulus exposure, terminal Real seal, componentwise transport, displayed replay, package provenance, and local naming. Every completion-facing consumer must read the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ sibling surfaces through those displayed rows before citing the finite modulus route.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-modulus-regular-cauchy-completion-carrier}. The rows $D,S,R,M$ expose the dyadic precision window, the inspected stream schedule, the regular rational readback, and the tail-modulus ledger.

The terminal seal $E$ is downstream from those rows. It can be read only after the displayed regular Cauchy route has opened the dyadic budget and finite schedule, so the seal is not a selected host limit or a quotient of streams.

The structural rows $H,C,P,N$ preserve, replay, source, and name the same finite evidence. Since the carrier has no coordinate for hidden convergence, countable choice, quotient equality, host real equality, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Finite modulus completion route boundary]
\label{thm:finite-modulus-regular-cauchy-completion-route-boundary}
Any $\RealUp$-facing read of an accepted $\FiniteModulusRegularCauchyCompletionUp$ packet factors through the dyadic window row $D$, stream schedule row $S$, regularity row $R$, tail-modulus row $M$, terminal seal row $E$, transport row $H$, replay row $C$, provenance row $P$, and local naming row $N$. A consumer that reaches a real seal without the displayed finite modulus leaves the packet.
\end{theorem}
\begin{proof}
Apply the obligations theorem. It lists $D,S,R,M,E,H,C,P,N$ as the complete local surface, with the $\RealUp$ seal downstream from dyadic, stream, regularity, and modulus rows.

If a consumer omits $M$, then the requested precision has no displayed tail window. If it omits $D,S,$ or $R$, then the modulus is detached from the dyadic budget, finite observations, or regular rational readback.

Transport, replay, provenance, and local naming do not create missing analytic data. Therefore every accepted completion read must pass through the full finite route, and a detached seal is outside the carrier.
\end{proof}

\falsifiablePrediction{A Real or Completion consumer that cites a terminal RealUp seal without a displayed dyadic window, stream schedule, regular rational readback, and tail-modulus row refutes the FiniteModulusRegularCauchyCompletionUp carrier.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real: $\FiniteModulusRegularCauchyCompletionUp$ is not bijective to any listed sibling because it binds dyadic tolerance, finite observation schedule, regular rational readback, tail modulus, terminal seal, transport, replay, provenance, and local naming as one completion-facing packet.}

\closureat{FiniteModulusRegularCauchyCompletionUp}{seedStr}

\begin{closurestatus}{\FiniteModulusRegularCauchyCompletionUp}
  \constructivestory{A $\FiniteModulusRegularCauchyCompletionUp$ packet is generated from a DyadicRatCore precision window, StreamName observation schedule, RegSeqRat regularity row, tail-modulus ledger, Real seal, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-modulus-regular-cauchy-completion-carrier}, \autoref{thm:finite-modulus-regular-cauchy-completion-obligations}, and \autoref{thm:finite-modulus-regular-cauchy-completion-route-boundary} seed the finite regular-Cauchy completion route over DyadicRatCore, StreamName, RegSeqRat, Real, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected limits, host real equality, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked dyadic-window admission, stream scheduling, regularity readback, tail-modulus exposure, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.FiniteModulusRegularCauchyCompletionUp.}
\end{closurestatus}
