\chapter{A Concrete Naming Certificate for $\MetaCICConfluenceBudgetWitnessUp$}
\label{ch:concrete-instances-metacic-confluence-budget-witness-namecert}
\origin{ai}

The $\MetaCICConfluenceBudgetWitnessUp$ packet realizes the finite confluence-budget witness route of \autoref{ch:metacic-confluence-audit-map}. It sits beside the local diamond witness of \autoref{ch:concrete-instances-metacic-local-diamond-witness-namecert}, the residual envelope of \autoref{ch:concrete-instances-metacic-confluence-residual-envelope-namecert}, the substitution-boundary packet of \autoref{ch:concrete-instances-metacic-confluence-substitution-boundary-namecert}, the closed-premise ledger of \autoref{ch:concrete-instances-closed-parallel-diamond-premise-ledger-namecert}, and the residual-budget packet of \autoref{ch:concrete-instances-metacic-diamond-residual-budget-namecert}. The witness binds finite budget rows used by confluence-facing readers while keeping the boundary against full confluence explicit.

\begin{definition}[MetaCIC confluence-budget witness carrier]
\label{def:metacic-confluence-budget-witness-carrier}
A $\MetaCICConfluenceBudgetWitnessUp$ carrier is a finite $\BHist$ packet
$$
W=(T,F,R,J,E,S,L,D,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the typed-window row naming the closed MetaCIC terms and finite context fragments inspected by the witness.
\item $F$ is the redex-frontier row listing the beta and congruence faces admitted to the budget.
\item $R$ is the residual-budget row that bounds residual addresses, replay fuel, and candidate-join inspection.
\item $J$ is the candidate-join row, recording only the joins accepted inside the displayed budget.
\item $E$ is the residual-envelope row read from the confluence residual-envelope packet.
\item $S$ is the substitution-boundary row that keeps residual-substitution compatibility visible as a boundary condition.
\item $L$ is the closed parallel-diamond premise ledger row.
\item $D$ is the local-diamond witness row consumed as finite local geometry.
\item $O$ is the obstruction-retention row, recording that full parallel diamond and arbitrary closed Church--Rosser are not supplied by the packet.
\item $H$ records componentwise $\hsame$ transport for typed windows, frontiers, budgets, joins, envelopes, substitution boundaries, premise ledgers, local diamonds, obstruction rows, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a confluence-budget request through $T,F,R,J,E,S,L,D,O,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\MetaCICConfluenceBudgetWitnessUp$, $\MetaCICDiamondResidualBudgetUp$, $\MetaCICLocalDiamondWitnessUp$, $\MetaCICConfluenceResidualEnvelopeUp$, $\MetaCICConfluenceSubstitutionBoundaryUp$, $\ClosedParallelDiamondPremiseLedgerUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetaCICConfluenceBudgetWitnessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $T,F,R,J,E,S,L,D,O,H,C,P,N$. It has no coordinate for full parallel diamond, arbitrary closed Church--Rosser, global strong normalization, substitution compatibility, subject reduction, or host reduction semantics.
\end{definition}

\begin{theorem}[MetaCIC confluence-budget witness NameCert obligations]
\label{thm:metacic-confluence-budget-witness-namecert-obligations}
For every accepted $\MetaCICConfluenceBudgetWitnessUp$ carrier $W=(T,F,R,J,E,S,L,D,O,H,C,P,N)$, the local $\NameCert_{\MetaCICConfluenceBudgetWitnessUp}$ surface consists exactly of typed-window admission, redex-frontier exposure, residual-budget accounting, candidate-join recording, residual-envelope handoff, substitution-boundary visibility, closed-premise-ledger handoff, local-diamond handoff, obstruction retention, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-confluence-budget-witness-carrier}. The mathematical rows are precisely $T,F,R,J,E,S,L,D,O$, followed by the structural rows $H,C,P,N$.

The budget is finite before any join is consumed. Rows $T,F,R$ bind the typed window, redex frontier, and residual fuel, while $J$ records only the candidate joins accepted inside that displayed budget.

The sibling handoffs remain visible. The residual envelope $E$, substitution boundary $S$, closed-premise ledger $L$, and local diamond row $D$ are not collapsed into an endpoint theorem; the obstruction row $O$ records the refused endpoint claims.

Rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since no excluded endpoint is a carrier coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[MetaCIC confluence-budget witness endpoint boundary]
\label{thm:metacic-confluence-budget-witness-endpoint-boundary}
Every confluence-facing consumer of an accepted $\MetaCICConfluenceBudgetWitnessUp$ packet reads only the finite route
$$
T,F,R \longmapsto J \longmapsto E,S,L,D,O \longmapsto H,C,P,N .
$$
The route may expose candidate joins under a residual budget, but it does not assert full parallel diamond, arbitrary closed Church--Rosser, global strong normalization, substitution compatibility, subject reduction, or host reduction semantics.
\end{theorem}
\begin{proof}
Begin with the displayed witness route. The typed window, redex frontier, and residual budget are read before the candidate-join row, so the join is budget-mediated rather than global.

The downstream rows keep the audit boundary intact. Residual envelope, substitution boundary, closed-premise ledger, and local diamond data are exposed as sibling handoffs, while the obstruction row remains in the same packet.

The structural rows preserve the route without adding endpoint proof data. Thus a confluence-facing consumer receives finite budget evidence and no coordinate for the refused global claims.
\end{proof}

\begin{theorem}[MetaCIC confluence-budget residual coverage]
\label{thm:metacic-confluence-budget-witness-residual-coverage}
Every accepted $\MetaCICConfluenceBudgetWitnessUp$ packet that is consumed as residual-substitution evidence factors through the residual-budget row $R$, candidate-join row $J$, residual-envelope row $E$, substitution-boundary row $S$, closed-premise ledger $L$, local-diamond row $D$, and obstruction row $O$. It cannot be read as full parallel diamond, arbitrary closed Church--Rosser, or unguarded substitution compatibility; residual-substitution consumers must keep the guarded input shape of \autoref{ch:concrete-instances-metacic-residual-substitution-compatibility-namecert} visible.
\end{theorem}
\begin{proof}
Project the carrier $W=(T,F,R,J,E,S,L,D,O,H,C,P,N)$ from \autoref{def:metacic-confluence-budget-witness-carrier}. The residual-facing coordinates are displayed as $R,J,E,S,L,D,O$, with transport, replay, provenance, and naming downstream.

Apply \autoref{thm:metacic-confluence-budget-witness-namecert-obligations}. The NameCert surface requires residual-budget accounting, candidate-join recording, residual-envelope handoff, substitution-boundary visibility, closed-premise-ledger handoff, local-diamond handoff, and obstruction retention as separate rows.

The refused endpoints are fixed by \autoref{thm:metacic-confluence-budget-witness-endpoint-boundary}. That theorem prevents the same packet from being read as full parallel diamond, arbitrary closed Church--Rosser, or an unguarded substitution theorem.

Finally, the guarded input shape is the one recorded by \autoref{ch:concrete-instances-metacic-residual-substitution-compatibility-namecert}. Thus a residual-substitution consumer receives only the displayed budgeted rows and the explicit boundary, not an endpoint confluence claim.
\end{proof}

\falsifiablePrediction{A packet omitting either the residual-budget row $R$ or the obstruction-retention row $O$ is rejected as a $\MetaCICConfluenceBudgetWitnessUp$ packet, even when it carries typed-window, local-diamond, or residual-envelope rows.}

\independenceWitness{metacic-local-diamond-witness, metacic-confluence-residual-envelope, metacic-confluence-substitution-boundary, closed-parallel-diamond-premise-ledger, metacic-diamond-residual-budget: $\MetaCICConfluenceBudgetWitnessUp$ is not bijective to any listed sibling because it jointly binds typed windows, redex frontiers, residual budgets, candidate joins, residual envelopes, substitution boundaries, closed-premise ledgers, local diamonds, obstruction retention, transport, replay, provenance, and local NameCert data.}

The Lean-side hardening surface requires $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances for the confluence-budget witness packet. The nontrivial witness separates an accepted packet with displayed residual-budget and obstruction rows from a refused packet missing one of those rows; field faithfulness compares $T,F,R,J,E,S,L,D,O,H,C,P,N$ componentwise so that equal displayed fields are indistinguishable to the local NameCert classifier.

\closureat{MetaCICConfluenceBudgetWitnessUp}{seedStr}

\begin{closurestatus}{\MetaCICConfluenceBudgetWitnessUp}
  \constructivestory{A $\MetaCICConfluenceBudgetWitnessUp$ packet is generated from typed-window rows, redex-frontier rows, residual budgets, candidate joins, residual envelopes, substitution-boundary rows, closed-premise ledgers, local diamonds, obstruction retention, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-confluence-budget-witness-carrier}, \autoref{thm:metacic-confluence-budget-witness-namecert-obligations}, and \autoref{thm:metacic-confluence-budget-witness-endpoint-boundary} seed the finite confluence-budget witness for MetaCIC confluence-audit consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim full parallel diamond, arbitrary closed Church--Rosser, global strong normalization, substitution compatibility, subject reduction, host reduction semantics, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, budget accounting, candidate-join exposure, residual-envelope handoff, substitution-boundary visibility, premise-ledger handoff, local-diamond handoff, obstruction retention, and Nontrivial and FieldFaithful TasteGate instances for BEDC.Derived.MetaCICConfluenceBudgetWitnessUp.}
\end{closurestatus}
