\chapter{A Concrete Naming Certificate for $\ClosedSetUp$ Metric Boundary}
\label{ch:concrete-instances-closedset-namecert}
\origin{human}

Following Kelley and Munkres on closed subsets as complements of open neighborhoods and as metric limit boundaries, the $\ClosedSetUp$ packet records the finite BEDC naming surface for closed-set reads. It sits between the topological carrier of \autoref{ch:concrete-instances-topology-namecert}, the metric and separated-metric routes of \autoref{ch:concrete-instances-metric-namecert} and \autoref{ch:concrete-instances-separatedmetric-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the compact-image consumer of \autoref{ch:concrete-instances-compact-image-closed-namecert}. The packet names a displayed closed-boundary ledger; it does not import a host powerset, a detached open-set lattice, selected limit points, quotient equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[ClosedSet carrier]\label{def:closedset-carrier}
A $\ClosedSetUp$ carrier is a finite $\BHist$ packet
$$
K=(T,M,S,F,O,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the $\TopologyUp$ carrier row containing the finite neighborhood and complement observations used by the closed-set read.
\item $M$ is the $\MetricUp$ row used when the closed boundary is inspected by distance or ball data.
\item $S$ is the $\SeparatedMetricUp$ row that turns metric zero-distance comparisons into separated boundary evidence.
\item $F$ is the finite membership classifier for the named subset boundary.
\item $O$ is the open-neighborhood complement ledger, recording the displayed route from nonmembership to a neighborhood outside the set.
\item $B$ is the metric complement boundary row, recording the finite ball and real-distance readbacks through $\RealUp$ that witness the same complement behavior.
\item $H$ records componentwise $\hsame$ transport for $T,M,S,F,O,B,C,P,N$.
\item $C$ records displayed $\Cont$ replay from topology reads through metric and separated-metric observations, open-neighborhood complement rows, metric boundary rows, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\ClosedSetUp$, $\TopologyUp$, $\MetricUp$, $\SeparatedMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ClosedSetUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $T,M,S,F,O,B,H,C,P,N$. It has no coordinate for host powersets, detached open-set lattices, selected limit points, quotient equality of names, countable choice, ambient Real completeness, or compact-image closedness beyond the displayed consumer route.
\end{definition}
\leandef{BEDC.Derived.ClosedSetUp.TasteGate.ClosedSetUp}

\begin{theorem}[ClosedSet NameCert obligations]\label{thm:closedset-namecert-obligations}
For every accepted $\ClosedSetUp$ carrier
$$
K=(T,M,S,F,O,B,H,C,P,N),
$$
the local $\NameCert_{\ClosedSetUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the topological row, metric row, separated-metric row, membership classifier, open-neighborhood complement ledger, metric complement boundary, transport, replay, provenance, and local naming;
\item topology compatibility, requiring every closed-set read to expose the $\TopologyUp$ neighborhood and complement rows before the membership classifier is consumed;
\item metric compatibility, requiring any ball or distance read to pass through $\MetricUp$, $\SeparatedMetricUp$, and the $\RealUp$ distance seal before it is treated as boundary evidence;
\item complement exactness, requiring nonmembership evidence to be routed through $O$ and metric boundary evidence to be routed through $B$ over the same classifier $F$;
\item transport replay, requiring $H$ and $C$ to preserve the ordered route componentwise before $P$ and $N$ are read;
\item nonescape, excluding host powersets, detached open-set lattices, selected limit points, quotient equality of names, countable choice, ambient $\RealUp$ completeness, and a proof of compact-image closedness not routed through \autoref{ch:concrete-instances-compact-image-closed-namecert}.
\end{enumerate}
No local ClosedSet NameCert obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:closedset-carrier}. The tuple displays the topology, metric, separated-metric, membership, complement, metric-boundary, transport, replay, provenance, and local naming rows.

Topology compatibility is forced by the placement of $T$ and $F$. A closed-set read first opens the finite neighborhood and complement observations, and only then consumes the classifier for the named subset boundary.

Metric compatibility is forced by $M,S,B$. Any distance or ball evidence passes through the metric row, the separated zero-distance row, and the $\RealUp$ distance seal before it becomes boundary evidence.

The remaining rows preserve and name the same route. Since the carrier has no coordinate for host powersets, detached open-set lattices, selected limit points, quotient equality, choice, or ambient completeness, the listed rows exhaust the local NameCert surface.
\end{proof}
\leanchecked{BEDC.Derived.ClosedSetUp.TasteGate.ClosedSetTasteGate\_single\_carrier\_alignment}

\begin{theorem}[ClosedSet metric complement boundary]\label{thm:closedset-metric-complement-boundary}
Every accepted $\ClosedSetUp$ carrier routes a metric complement request through
$$
\TopologyUp
\longmapsto
\MetricUp,\SeparatedMetricUp,\RealUp
\longmapsto
F,O,B,H,C,P,N .
$$
Thus a consumer such as $\CompactImageClosedUp$ may read a named closed boundary only after the topological complement ledger, metric distance rows, separated classifier, real-distance seal, transport, replay, provenance, and local naming rows are displayed. A request bypassing one of those rows is outside the accepted $\ClosedSetUp$ packet.
\end{theorem}
\begin{proof}
Read the accepted carrier $K=(T,M,S,F,O,B,H,C,P,N)$. The topological boundary is $T$, and the metric boundary is read only through $M,S$ and the real-distance data stored in $B$.

The classifier $F$ is shared by the open-neighborhood complement ledger $O$ and the metric complement boundary $B$. Therefore the two complement presentations cannot diverge into a host powerset or detached open-set lattice.

Transport and replay are then supplied by $H$ and $C$, while $P$ and $N$ record provenance and local naming. Since the compact-image consumer of \autoref{ch:concrete-instances-compact-image-closed-namecert} receives only that finite boundary row, it cannot obtain selected limit points, quotient equality, countable choice, or ambient Real completeness from the closed-set packet.
\end{proof}
\leanchecked{BEDC.Derived.ClosedSetUp.TasteGate.ClosedSetCarrier\_metric\_complement\_boundary}

\begin{theorem}[ClosedSet scope basis]\label{thm:closedset-scope-basis}
The $\ClosedSetUp$ scope is fixed by the carrier and metric-complement boundary rows over $\MetricUp$, $\TopologyUp$, $\hsame$, $\Cont$, and the local $\NameCert_{\ClosedSetUp}$ surface. A consumer may read closed-boundary data only after the topology row, metric row, separated-metric row, classifier row, open-neighborhood complement ledger, metric complement boundary, componentwise transport, displayed replay, provenance, and local naming rows have been exposed. The scope refuses host powerset semantics, detached open-set lattices, quotient equality of names, selected limit points, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Use the carrier of \autoref{def:closedset-carrier}. Its displayed rows contain exactly the topology, metric, separated-metric, classifier, complement, metric-boundary, transport, replay, provenance, and local naming data.

Apply the NameCert obligations of \autoref{thm:closedset-namecert-obligations} and the metric complement boundary route of \autoref{thm:closedset-metric-complement-boundary}. These two results force every closed-boundary read through $\TopologyUp$, $\MetricUp$, $\hsame$, $\Cont$, and $\NameCert$ before any consumer can inspect the boundary.

Because the accepted packet has no coordinate for a host powerset, detached open-set lattice, quotient equality, selected limit point, or ambient completeness theorem, the stated rows exhaust the ClosedSet scope basis.
\end{proof}
\leanchecked{BEDC.Derived.ClosedSetUp.TasteGate.ClosedSetScopeBasis}

\begin{theorem}[ClosedSet public boundary export]\label{thm:closedset-public-boundary-export}
The public $\ClosedSetUp$ export is exactly the topology row, metric row, separated-metric row, classifier row, open-neighborhood complement ledger, metric complement boundary, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\ClosedSetUp}$ row of the accepted carrier. A public consumer may cite the closed-boundary packet only through those rows and through the scope basis; it receives no host powerset semantics, detached open-set lattice, selected limit point, quotient equality of names, countable choice, ambient $\RealUp$ completeness, or compact-image closedness outside the displayed consumer route.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:closedset-carrier}. It exposes precisely the topology, metric, separated-metric, classifier, complement ledger, metric-boundary, transport, replay, provenance, and local naming rows.

Apply the NameCert obligations, metric complement boundary, and scope basis from \autoref{thm:closedset-namecert-obligations}, \autoref{thm:closedset-metric-complement-boundary}, and \autoref{thm:closedset-scope-basis}. These results force every public closed-boundary read through the same finite row inventory and block any route through host powersets, detached open-set lattices, quotient equality, selected limit points, choice, or ambient completeness.

Thus the public export is not a larger closure theorem; it is the named boundary surface already displayed by the carrier and its metric complement route.
\end{proof}
\leanchecked{BEDC.Derived.ClosedSetUp.TasteGate.ClosedSetPublicBoundaryExport}

\begin{theorem}[ClosedSet bridged boundary export]\label{thm:closedset-bridged-boundary-export}
Any future standard bridge for $\ClosedSetUp$ must read exactly the topology row, metric row, separated-metric row, classifier row, open-neighborhood complement ledger, metric complement boundary, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\ClosedSetUp}$ row already exported by \autoref{thm:closedset-public-boundary-export}. A bridge consumer may replay those rows as the closed-boundary surface, but it receives no host powerset semantics, detached open-set lattice, selected limit point, quotient equality of names, countable choice, ambient $\RealUp$ completeness, or compact-image closedness outside that displayed consumer route.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:closedset-carrier}. Its coordinates are exactly $T,M,S,F,O,B,H,C,P,N$, so a bridge has no additional coordinate from which to read a host powerset, detached open-set lattice, selected point, quotient equality witness, choice principle, or ambient completeness theorem.

Apply the NameCert obligations of \autoref{thm:closedset-namecert-obligations} and the metric complement boundary of \autoref{thm:closedset-metric-complement-boundary}. These force topology exposure through $T$, metric exposure through $M,S,B$, complement compatibility through $F,O,B$, and replay through $H,C,P,N$ before the closed-boundary row can be consumed.

The scope basis of \autoref{thm:closedset-scope-basis} and the public export of \autoref{thm:closedset-public-boundary-export} then identify the bridge-facing surface with the already public row inventory. Thus the bridge reads the displayed consumer route and nothing stronger, including no compact-image closedness theorem beyond the named downstream route.
\end{proof}
\leanchecked{BEDC.Derived.ClosedSetUp.TasteGate.ClosedSetBridgedBoundaryExport}

\closureat{ClosedSetUp}{seedStr}

\begin{closurestatus}{\ClosedSetUp}
  \constructivestory{A $\ClosedSetUp$ packet is generated from topological, metric, separated-metric, membership-classifier, open-neighborhood complement, metric complement boundary, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\bridgedClosure}
  \scopeclosed{\autoref{def:closedset-carrier}, \autoref{thm:closedset-namecert-obligations}, \autoref{thm:closedset-metric-complement-boundary}, \autoref{thm:closedset-scope-basis}, \autoref{thm:closedset-public-boundary-export}, and \autoref{thm:closedset-bridged-boundary-export} enumerate the finite closed-set carrier, local NameCert obligation surface, metric complement boundary route, scope basis, public boundary export, and bridge-facing export over the displayed topology, metric, separated-metric, complement, transport, replay, provenance, local naming, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, host powerset semantics, detached open-set lattices, selected limit points, quotient equality of names, countable choice, ambient $\RealUp$ completeness, or compact-image closedness outside the displayed consumer route.}
  \upgradepath{Bridged closure requires a checked standard bridge for the public boundary export and its carrier admission, topology compatibility, metric compatibility, complement exactness, complement-boundary transport, metric-boundary replay, transport replay, nonescape, provenance exposure, local naming, and TasteGate field faithfulness for BEDC.Derived.ClosedSetUp.}
\end{closurestatus}
