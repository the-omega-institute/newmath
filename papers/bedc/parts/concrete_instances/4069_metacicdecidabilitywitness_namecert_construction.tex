\chapter{A Concrete Naming Certificate for $\MetacicDecidabilityWitnessUp$}
\label{ch:concrete-instances-metacicdecidabilitywitness-namecert}
\origin{ai}

The $\MetacicDecidabilityWitnessUp$ packet realizes the decidability audit map of \autoref{ch:metacic-decidable-audit-map} as a finite NameCert witness. It sits beside the normal false exclusion packet of \autoref{ch:concrete-instances-normalfalseexclusion-namecert} and the confluence normal-fragment packet of \autoref{ch:concrete-instances-metacicconfluencenormalfragment-namecert}: those siblings keep normal-form and confluence boundaries visible, while this packet separates typing-checker exactness from bounded beta-conversion evidence.

\begin{definition}[MetaCIC decidability witness carrier]
\label{def:metacic-decidability-witness-carrier}
A $\MetacicDecidabilityWitnessUp$ carrier is a finite $\BHist$ packet
$$
W=(T,S,B,F,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the $\mathsf{inferTypeCtx}$ exactness row, containing the soundness and completeness surfaces for the encoded $\mathsf{HasType}$ relation;
\item $S$ is the $\mathsf{checkSameTerm}$ structural equality row used by the typing checker;
\item $B$ is the bounded beta-comparison row for $\mathsf{normalizeBounded}$ and $\mathsf{decideClosedBetaConv}$;
\item $F$ is the finished-normal-form side-condition row consumed when a bounded comparison result is read as equality or beta-convertibility of named normal forms;
\item $R$ records refusal of a standalone $\mathsf{ClosedAt}$ decision theorem, unrestricted closed beta-conversion decidability, and fuel-erased normalisation;
\item $H$ records componentwise $\hsame$ transport for $T,S,B,F,R,P,N$;
\item $C$ records displayed $\Cont$ routes for typing exactness, same-term checking, bounded comparison, finished-normal side conditions, and refusal reads;
\item $P$ is the $\Pkg$ provenance row over the decidability audit map and its normal-form and confluence siblings;
\item $N$ is the local $\NameCert_{\MetacicDecidabilityWitnessUp}$ row.
\end{enumerate}
The local classifier compares accepted carriers only through the rows $T,S,B,F,R,H,C,P,N$.
\end{definition}

\begin{theorem}[MetaCIC decidability witness NameCert obligations]
\label{thm:metacic-decidability-witness-namecert-obligations}
Every accepted $\MetacicDecidabilityWitnessUp$ packet binds exactly the finite NameCert obligation surface of \autoref{def:metacic-decidability-witness-carrier}: typing-checker exactness through $T$, same-term structural equality through $S$, bounded beta comparison through $B$, finished-normal side conditions through $F$, refusal boundaries through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier. The positive checker rows are $T$ and $S$: typing exactness is read through the inferTypeCtx row, while structural equality is read through the same-term checker row used by that typing surface.

The bounded conversion rows are $B$ and $F$. The comparison row exposes only bounded normalizer output comparison, and the finished-normal side-condition row supplies the additional hypotheses under which the audit map permits equality or beta-convertibility reads for named normal forms.

The refusal row $R$ remains part of the same packet. It records that no coordinate exports a standalone closedness decider, an unrestricted closed beta-conversion decider, or fuel-erased normalisation.

Finally $H,C,P,N$ transport, replay, cite, and name only those displayed rows. Since the carrier has no further coordinate, the local NameCert surface is exhausted by the finite obligations listed above.
\end{proof}
\leanchecked{BEDC.Derived.MetacicDecidabilityWitnessUp.MetacicDecidabilityWitnessTasteGate\_single\_carrier\_alignment}

\begin{theorem}[MetaCIC decidability witness bounded conversion non-escape]
\label{thm:metacic-decidability-witness-bounded-conversion-nonescape}
Any consumer of an accepted $\MetacicDecidabilityWitnessUp$ packet may read conversion evidence only through $B$ together with $F$. The read can expose bounded-normal output equality and the finished-normal consequences named by the audit map, but it cannot export a global closed beta-conversion decision procedure, a fuel-erased normalisation theorem, a quotient equality over represented terms, or a host evaluator.
\end{theorem}
\begin{proof}
Begin with the conversion coordinates. The carrier separates the bounded comparison row $B$ from the finished-normal side-condition row $F$, so a consumer route that asks for conversion evidence must pass through both rows.

The refusal row $R$ records the missing endpoints. It prevents the bounded comparison route from being reread as a global closed beta-conversion decider, fuel-erased normalisation theorem, quotient equality, or host evaluator.

Transport and replay do not add data. The rows $H$ and $C$ preserve the same displayed comparison and side-condition routes, while $P$ and $N$ only record provenance and local naming. Thus the conversion read stays inside the bounded witness packet.
\end{proof}
\leanchecked{BEDC.Derived.MetacicDecidabilityWitnessUp.MetacicDecidabilityWitnessBoundedConversionNonescape}
\leanvariant{BEDC.Derived.MetacicDecidabilityWitnessUp.MetacicDecidabilityWitnessCarrier\_bounded\_conversion\_nonescape}

\begin{theorem}[MetaCIC decidability witness bounded normal-form split]
\label{thm:metacic-decidability-witness-bounded-normal-form-split}
An accepted $\MetacicDecidabilityWitnessUp$ packet may expose bounded checker output and normal-form endpoint rows only through its displayed candidate row $T$, structural comparison row $S$, fuel and window row $B$, normal-form side-condition row $F$, refusal row $R$, componentwise transport $H$, displayed replay $C$, provenance $P$, and local $\NameCert_{\MetacicDecidabilityWitnessUp}$ row $N$. A later confluence or decidability consumer may inspect those rows as bounded normal-form evidence, but the packet does not expose global closed-term conversion decidability, unrestricted subject reduction, quotient syntax equality, or consistency.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-decidability-witness-carrier}. The checker candidate, structural comparison, bounded fuel and window, normal-form side condition, refusal, transport, replay, provenance, and naming rows are exactly $T,S,B,F,R,H,C,P,N$.

Invoke the NameCert obligations of \autoref{thm:metacic-decidability-witness-namecert-obligations}. They identify the admissible checker and normal-form reads with the displayed rows and leave no additional coordinate for a global conversion decider or host syntax equality.

Use the bounded-conversion non-escape theorem \autoref{thm:metacic-decidability-witness-bounded-conversion-nonescape}. It confines conversion evidence to $B$ together with $F$, while $R$ records the missing global endpoints. Thus a later consumer may inspect bounded normal-form evidence through the named rows only, and cannot reread it as unrestricted conversion decidability, subject reduction, quotient syntax equality, or consistency.
\end{proof}
\leanchecked{BEDC.Derived.MetacicDecidabilityWitnessUp.MetacicDecidabilityWitnessBoundedNormalFormSplit}
\leanvariant{BEDC.Derived.MetacicDecidabilityWitnessUp.MetacicDecidabilityWitness\_bounded\_normal\_form\_split}

\begin{theorem}[MetaCIC decidability witness typing-exactness route]
\label{thm:metacic-decidability-witness-typing-exactness-route}
Every accepted $\MetacicDecidabilityWitnessUp$ packet routes typing-checker exactness through the inferTypeCtx row $T$ before any consumer may combine structural equality $S$, bounded beta comparison $B$, and finished-normal row $F$. A request that skips $T$ cannot be read as $\mathsf{HasType}$ exactness or as closed conversion evidence for the packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-decidability-witness-carrier}. The row $T$ is the unique displayed coordinate that names inferTypeCtx soundness and completeness for the encoded $\mathsf{HasType}$ relation, while $S,B,F$ record structural equality, bounded comparison, and finished-normal side conditions.

Apply the NameCert obligations of \autoref{thm:metacic-decidability-witness-namecert-obligations}. They bind typing-checker exactness to $T$ and leave $S,B,F$ as later consumer rows, so the structural and bounded-normal data cannot be promoted into a typing-exactness witness without first exposing $T$.

Use the bounded normal-form split of \autoref{thm:metacic-decidability-witness-bounded-normal-form-split}. It permits bounded normal-form inspection only through the displayed rows and keeps the refusal row $R$ visible. Together with \autoref{thm:metacic-decidability-witness-bounded-conversion-nonescape}, this excludes a global $\mathsf{ClosedAt}$ decision theorem, unrestricted beta conversion, quotient syntax equality, and evaluator rows.

Thus any route skipping $T$ lacks the only row that carries inferTypeCtx exactness. Such a route may inspect bounded comparison evidence after the displayed side conditions, but it is not a $\mathsf{HasType}$ exactness route and does not close conversion for the packet.
\end{proof}
\leanchecked{BEDC.Derived.MetacicDecidabilityWitnessUp.MetacicDecidabilityWitnessCarrier\_typing\_exactness\_route}

\begin{theorem}[MetaCIC decidability witness bounded-search exhaustion]
\label{thm:metacic-decidability-witness-bounded-search-exhaustion}
For every accepted $\MetacicDecidabilityWitnessUp$ packet
$W=(T,S,B,F,R,H,C,P,N)$, every bounded conversion or typing decision requested
inside the displayed finite depth bound is exhausted by the rows already
visible in the packet: the syntax window in $T$, the bounded conversion ledger
$B$, the normal-form split through $F$, the typing-exactness route through
$T$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay
through $C$, $\Pkg$ provenance through $P$, and the local
$\NameCert_{\MetacicDecidabilityWitnessUp}$ row $N$.

The packet exports no unbounded strong-normalisation theorem, global
confluence theorem, or ambient decision oracle.
\end{theorem}
\leanchecked{BEDC.Derived.MetacicDecidabilityWitnessUp.MetacicDecidabilityWitnessBoundedSearchExhaustion}
\leanvariant{BEDC.Derived.MetacicDecidabilityWitnessUp.MetacicDecidabilityWitness\_bounded\_search\_exhaustion}
\begin{proof}
Proceed by induction over the displayed finite depth bound in the carrier
row. At depth zero, the syntax window in $T$ and the structural row $S$ expose
only the immediate inferTypeCtx and checkSameTerm reads, so the decision is
already inside the displayed packet.

For the successor depth, the induction hypothesis exhausts all smaller
conversion and typing requests through the same finite rows. A bounded
conversion request then reads $B$ and the finished-normal side-condition row
$F$; a typing request reads the inferTypeCtx exactness row $T$ before it may
combine $S,B,F$.

Apply \autoref{thm:metacic-decidability-witness-bounded-normal-form-split} and
\autoref{thm:metacic-decidability-witness-typing-exactness-route}. These
results keep bounded normal-form evidence and typing exactness inside
$T,S,B,F,R,H,C,P,N$.

The remaining structural rows preserve the same finite search. Transport
through $H$, replay through $C$, provenance through $P$, and local naming
through $N$ do not add a decision oracle. The refusal row $R$ therefore keeps
unbounded strong normalisation, global confluence, and ambient decidability
outside the exported surface.
\end{proof}

\begin{definition}[MetaCIC parallel-diamond fuel window]
\label{def:metacic-parallel-diamond-fuel-window}
A $\MetacicDecidabilityWitnessUp$ parallel-diamond fuel-window packet is a finite $\BHist$ row
$$
V=(T,S,B,F,R,H,C,P,N,D)
$$
where $T,S,B,F,R,H,C,P,N$ are the displayed rows of the carrier in \autoref{def:metacic-decidability-witness-carrier}, and $D$ is the confluence normal-fragment sibling row from \autoref{ch:concrete-instances-metacicconfluencenormalfragment-namecert}. The row $D$ is read only inside the same bounded fuel window as $B$ and $F$: it records bounded parallel-diamond evidence for the displayed normal fragments, not a global confluence theorem, unrestricted beta-conversion decision, quotient syntax equality, consistency theorem, or host evaluator.
\end{definition}

\begin{theorem}[MetaCIC parallel-diamond fuel monotonicity]
\label{thm:metacic-parallel-diamond-fuel-monotonicity}
Let $V=(T,S,B,F,R,H,C,P,N,D)$ be an accepted $\MetacicDecidabilityWitnessUp$ parallel-diamond fuel-window packet. Increasing the displayed bounded fuel window preserves only the bounded-normal evidence already routed through $B,F,D$ together with the typing, same-term, refusal, transport, replay, provenance, and naming rows $T,S,R,H,C,P,N$. The enlarged window does not export global $\mathsf{ClosedAt}$ decidability, unrestricted beta conversion, quotient syntax equality, consistency, fuel-erased normalisation, or a host evaluator.
\end{theorem}
\begin{proof}
Start with the TasteGate single-carrier marker recorded for the chapter. It fixes the MetaCIC decidability witness boundary at the displayed carrier rows $T,S,B,F,R,H,C,P,N$, so adding fuel cannot introduce a new decision coordinate.

Use the bounded-search exhaustion theorem, \autoref{thm:metacic-decidability-witness-bounded-search-exhaustion}. It says that each request inside a finite depth bound is exhausted by the visible syntax window, bounded conversion ledger, normal-form split, typing-exactness route, transport, replay, provenance, and local naming rows.

Now enlarge the finite fuel window. The bounded-conversion non-escape theorem, \autoref{thm:metacic-decidability-witness-bounded-conversion-nonescape}, keeps conversion evidence inside $B$ together with $F$, while the sibling row $D$ contributes only bounded confluence-normal-fragment evidence at the displayed window.

Transport through $H$ and replay through $C$ preserve the same row inventory, and $P,N$ record provenance and naming. The refusal row $R$ therefore remains active after the enlargement, preventing the bounded parallel-diamond evidence from being reread as global $\mathsf{ClosedAt}$ decidability, unrestricted beta conversion, quotient syntax equality, consistency, fuel-erased normalisation, or a host evaluator.
\end{proof}

\begin{theorem}[MetaCIC decidability witness fuel-window exhaustion]
\label{thm:metacic-decidability-witness-fuel-window-exhaustion}
Let $V=(T,S,B,F,R,H,C,P,N,D)$ be an accepted $\MetacicDecidabilityWitnessUp$ parallel-diamond fuel-window packet. Every bounded conversion, typing, or candidate-mediated parallel-diamond request inside the displayed finite fuel window is exhausted by the rows $T,S,B,F,D,R,H,C,P,N$: typing exactness routes through $T$, structural same-term comparison through $S$, bounded beta comparison through $B$, finished-normal side conditions through $F$, bounded parallel-diamond evidence through $D$, refusal through $R$, transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$. The packet exports no unbounded strong normalization, global confluence, subject reduction, quotient syntax equality, consistency, fuel-erased normalization, or host evaluator.
\end{theorem}
\begin{proof}
Start with the TasteGate single-carrier alignment marker recorded after the NameCert obligations. It fixes the accepted MetaCIC decidability witness at the displayed carrier rows, so a bounded request has no hidden coordinate outside $T,S,B,F,R,H,C,P,N$ and the parallel-diamond row $D$.

Apply bounded-search exhaustion, \autoref{thm:metacic-decidability-witness-bounded-search-exhaustion}. It accounts for every finite conversion or typing read by the visible syntax window, bounded conversion row, finished-normal row, typing route, refusal row, transport, replay, provenance, and local naming.

Use bounded-conversion nonescape, \autoref{thm:metacic-decidability-witness-bounded-conversion-nonescape}, and the typing-exactness route, \autoref{thm:metacic-decidability-witness-typing-exactness-route}. These force conversion reads through $B,F$ and typing reads through $T$ before any candidate-mediated request can consume the packet.

Finally invoke the parallel-diamond fuel monotonicity theorem, \autoref{thm:metacic-parallel-diamond-fuel-monotonicity}. The row $D$ contributes only bounded normal-fragment evidence inside the displayed fuel window, and the refusal row $R$ prevents that evidence from being read as unbounded strong normalization, global confluence, subject reduction, quotient syntax equality, consistency, fuel-erased normalization, or a host evaluator.
\end{proof}

\begin{theorem}[MetaCIC decidability witness obligation-closure package]
\label{thm:metacic-decidability-witness-obligation-closure-package}
For the carrier of \autoref{def:metacic-decidability-witness-carrier}, the $\MetacicDecidabilityWitnessUp$ obligation surface is closed by the carrier row, the NameCert obligations, bounded-conversion nonescape, bounded normal-form split, and typing-exactness route. Equivalently, every accepted packet $W=(T,S,B,F,R,H,C,P,N)$ exposes exactly inferTypeCtx exactness through $T$, checkSameTerm structural equality through $S$, bounded beta comparison through $B$, finished-normal side conditions through $F$, refusal rows through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and the local $\NameCert_{\MetacicDecidabilityWitnessUp}$ row $N$.
\end{theorem}
\begin{proof}
Carrier admission supplies the displayed tuple $T,S,B,F,R,H,C,P,N$, and the existing TasteGate marker above fixes the AI chapter boundary at that single carrier. The NameCert obligations of \autoref{thm:metacic-decidability-witness-namecert-obligations} then assign typing exactness, structural equality, bounded comparison, finished-normal side conditions, refusal reads, transport, replay, provenance, and naming to those rows.

The bounded-conversion theorem \autoref{thm:metacic-decidability-witness-bounded-conversion-nonescape} blocks escape from $B$ and $F$ to global conversion, fuel-erased normalisation, quotient equality, or a host evaluator. The bounded normal-form split of \autoref{thm:metacic-decidability-witness-bounded-normal-form-split} keeps every bounded checker and normal-form read inside $T,S,B,F,R,H,C,P,N$.

Finally, the typing-exactness route of \autoref{thm:metacic-decidability-witness-typing-exactness-route} forces every $\mathsf{HasType}$ exactness read through $T$ before structural or bounded-normal evidence is combined. These cited rows cover every obligation named by the carrier and leave no additional coordinate for a separate decidability, transport, replay, provenance, or local naming obligation.
\end{proof}
\leanchecked{BEDC.Derived.MetacicDecidabilityWitnessUp.MetacicDecidabilityWitnessCarrier\_obligation\_closure\_package}

\begin{theorem}[MetaCIC decidability witness scope assignment]
\label{thm:metacic-decidability-witness-scope-assignment}
For every accepted $\MetacicDecidabilityWitnessUp$ packet $W=(T,S,B,F,R,H,C,P,N)$, the rows are scoped as follows: $T$ is the $\mathsf{inferTypeCtx}$ exactness row, $S$ is the $\mathsf{checkSameTerm}$ structural-equality row, $B$ is the bounded beta-comparison row, $F$ is the finished-normal side-condition row, $R$ is the refusal-boundary row, $H$ is componentwise $\hsame$ transport, $C$ is displayed $\Cont$ replay, $P$ is package provenance, and $N$ is local $\NameCert_{\MetacicDecidabilityWitnessUp}$ naming. No consumer may read these rows as $\mathsf{ClosedAt}$ decidability, unrestricted beta-conversion, quotient syntax equality, consistency, fuel-erased normalisation, or a host evaluator.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-decidability-witness-carrier}. The displayed tuple is exactly $T,S,B,F,R,H,C,P,N$, so every scoped assignment must be read from one of those coordinates.

Apply the NameCert obligations of \autoref{thm:metacic-decidability-witness-namecert-obligations}. They assign typing exactness to $T$, structural equality to $S$, bounded comparison to $B$, finished-normal side conditions to $F$, refusal boundaries to $R$, transport to $H$, replay to $C$, provenance to $P$, and naming to $N$.

Use bounded-conversion non-escape, \autoref{thm:metacic-decidability-witness-bounded-conversion-nonescape}, together with the bounded normal-form split of \autoref{thm:metacic-decidability-witness-bounded-normal-form-split}. These keep conversion reads inside $B,F$ and keep bounded normal-form evidence inside the displayed packet.

Finally apply the typing-exactness route of \autoref{thm:metacic-decidability-witness-typing-exactness-route} and the TasteGate single-carrier alignment marker already recorded for the chapter. The accepted packet therefore has one finite scope assignment for the nine rows, with no coordinate from which a consumer can extract $\mathsf{ClosedAt}$ decidability, unrestricted beta-conversion, quotient syntax equality, consistency, or a host evaluator.
\end{proof}
\leanchecked{BEDC.Derived.MetacicDecidabilityWitnessUp.MetacicDecidabilityWitnessCarrier\_scope\_assignment}

\begin{theorem}[MetaCIC decidability witness candidate-normalization scope]
\label{thm:metacic-decidability-witness-candidate-normalization-scope}
Every candidate-normalization consumer of an accepted $\MetacicDecidabilityWitnessUp$ packet $W=(T,S,B,F,R,H,C,P,N)$ may inspect only the typing exactness row $T$, same-term row $S$, bounded beta row $B$, finished-normal side condition $F$, refusal row $R$, and structural rows $H,C,P,N$. The packet keeps strong normalization, global confluence, subject reduction, evaluator equality, quotient syntax equality, and consistency outside the candidate-normalization scope.
\end{theorem}
\begin{proof}
Start with the TasteGate single-carrier alignment marker recorded after the NameCert obligations. It fixes the packet boundary at the carrier $W=(T,S,B,F,R,H,C,P,N)$, so a candidate-normalization consumer has no row beyond those nine displayed coordinates.

Apply the bounded normal-form split of \autoref{thm:metacic-decidability-witness-bounded-normal-form-split}. It allows bounded normal-form inspection only through the typing, same-term, bounded beta, finished-normal, refusal, and structural rows already visible in the carrier.

Use the typing-exactness route of \autoref{thm:metacic-decidability-witness-typing-exactness-route} and the bounded-search exhaustion theorem \autoref{thm:metacic-decidability-witness-bounded-search-exhaustion}. These force typing reads through $T$ and bounded conversion reads through $B,F$ before any finite candidate-normalization request can be exhausted.

Finally apply the scope assignment of \autoref{thm:metacic-decidability-witness-scope-assignment}. It identifies $S,R,H,C,P,N$ as structural equality, refusal, transport, replay, provenance, and local naming rows, and it leaves no coordinate for strong normalization, global confluence, subject reduction, evaluator equality, quotient syntax equality, or consistency.
\end{proof}
\leanchecked{BEDC.Derived.MetacicDecidabilityWitnessUp.MetacicDecidabilityWitnessCarrier\_candidate\_normalization\_scope}

\begin{theorem}[MetaCIC decidability witness scope routes]
\label{thm:metacic-decidability-witness-scope-routes}
For every accepted $\MetacicDecidabilityWitnessUp$ packet $W=(T,S,B,F,R,H,C,P,N)$, each consumer route is assigned to the displayed row that owns it: typing-checker exactness routes through $T$, structural same-term equality routes through $S$, bounded conversion routes through $B$ together with the finished-normal side condition $F$, refusal routes through $R$, and transport, replay, provenance, and local naming route through $H,C,P,N$. No route exports $\mathsf{ClosedAt}$ decidability, unrestricted beta conversion, quotient syntax equality, consistency, fuel-erased normalisation, or a host evaluator.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-decidability-witness-carrier}. The displayed tuple has exactly the rows $T,S,B,F,R,H,C,P,N$, so every accepted route must be assigned to one of those coordinates.

Apply the NameCert obligations of \autoref{thm:metacic-decidability-witness-namecert-obligations} and the scope assignment of \autoref{thm:metacic-decidability-witness-scope-assignment}. These identify $T$ with typing exactness, $S$ with same-term structural equality, $B$ with bounded beta comparison, $F$ with the finished-normal side condition, $R$ with refusal, and $H,C,P,N$ with transport, replay, provenance, and local naming.

Use bounded-conversion non-escape, \autoref{thm:metacic-decidability-witness-bounded-conversion-nonescape}, together with the bounded normal-form split of \autoref{thm:metacic-decidability-witness-bounded-normal-form-split}. Conversion can therefore be read only through $B$ and $F$, and bounded normal-form evidence remains inside the displayed packet.

Finally invoke the typing-exactness route of \autoref{thm:metacic-decidability-witness-typing-exactness-route} and the candidate-normalization scope of \autoref{thm:metacic-decidability-witness-candidate-normalization-scope}. They force typing reads through $T$ and keep candidate-normalization consumers inside the same finite rows. Since the carrier has no additional coordinate, none of the listed routes can export $\mathsf{ClosedAt}$ decidability, unrestricted beta conversion, quotient syntax equality, consistency, fuel-erased normalisation, or a host evaluator.
\end{proof}

\begin{theorem}[MetaCIC decidability witness scoped closure package]
\label{thm:metacic-decidability-witness-scoped-closure-package}
For every accepted $\MetacicDecidabilityWitnessUp$ packet $W=(T,S,B,F,R,H,C,P,N)$, the scope routes assign all nine rows to one finite MetaCIC decidability witness: $T$ to typing exactness, $S$ to same-term structural equality, $B$ to bounded beta comparison, $F$ to finished-normal side conditions, $R$ to refusal, $H$ to transport, $C$ to replay, $P$ to provenance, and $N$ to local naming. The existing TasteGate marker
$$
\mathsf{BEDC.Derived.MetacicDecidabilityWitnessUp.MetacicDecidabilityWitnessTasteGate\_single\_carrier\_alignment}
$$
fixes the AI chapter boundary at that single carrier surface.

Thus the scoped closure package is the union of the NameCert obligations, bounded-conversion non-escape, bounded normal-form split, typing-exactness route, candidate-normalization scope, scope routes, and TasteGate single-carrier alignment. It exports no standalone $\mathsf{ClosedAt}$ decision theorem, unrestricted closed beta-conversion decider, quotient syntax equality, consistency theorem, fuel-erased normalisation theorem, or host evaluator.
\end{theorem}
\begin{proof}
Begin with the TasteGate marker already recorded after \autoref{thm:metacic-decidability-witness-namecert-obligations}. It aligns the chapter with the single displayed carrier $W=(T,S,B,F,R,H,C,P,N)$, so any scoped package must be read through those rows.

The NameCert obligations and scope routes assign the rows. They place typing exactness in $T$, same-term equality in $S$, bounded beta comparison in $B$, finished-normal side conditions in $F$, refusal in $R$, and the structural transport, replay, provenance, and local naming data in $H,C,P,N$.

Use bounded-conversion non-escape and the bounded normal-form split to confine conversion and normal-form reads to $B,F$ together with the visible packet rows. Then use the typing-exactness route and candidate-normalization scope to force typing and finite candidate-normalization consumers through $T,S,B,F,R,H,C,P,N$.

These cited routes exhaust the accepted carrier surface fixed by TasteGate. Since the carrier has no further coordinate, the scoped closure package cannot export any of the refused global decidability, conversion, equality, consistency, normalisation, or evaluator objects.
\end{proof}
\leanchecked{BEDC.Derived.MetacicDecidabilityWitnessUp.MetacicDecidabilityWitnessScopedClosurePackage}

\begin{theorem}[MetaCIC decidability witness public export]
\label{thm:metacic-decidability-witness-public-export}
Every public consumer of an accepted $\MetacicDecidabilityWitnessUp$ packet must read the same typing, same-term, bounded-conversion, finished-normal, refusal, transport, replay, provenance, and local naming rows before treating the packet as a decidability witness. Thus a public export over $W=(T,S,B,F,R,H,C,P,N)$ is valid only when $T$ supplies typing exactness, $S$ supplies structural same-term equality, $B$ and $F$ supply bounded conversion and finished-normal inspection, $R$ supplies refusal, and $H,C,P,N$ supply componentwise transport, displayed replay, provenance, and local naming. It exports no standalone $\mathsf{ClosedAt}$ decision theorem, unrestricted beta-conversion decider, quotient syntax equality, consistency theorem, fuel-erased normalisation theorem, or host evaluator.
\end{theorem}
\begin{proof}
Begin with the TasteGate single-carrier marker already recorded after \autoref{thm:metacic-decidability-witness-namecert-obligations}. It fixes the public packet to the displayed carrier $W=(T,S,B,F,R,H,C,P,N)$.

The bounded-conversion non-escape theorem and bounded normal-form split force conversion and finished-normal reads through $B,F$ together with the visible refusal and structural rows. The typing-exactness route forces typing reads through $T$, while bounded-search exhaustion keeps finite candidate search inside the displayed packet.

The scope assignment, candidate-normalization scope, scope routes, and scoped-closure package then identify the remaining public rows. They place same-term checking in $S$, refusal in $R$, and transport, replay, provenance, and local naming in $H,C,P,N$.

These cited surfaces exhaust the accepted public export. Since no carrier coordinate names global decidability, unrestricted conversion, quotient equality, consistency, fuel-erased normalisation, or a host evaluator, none of those objects can be exported by a public consumer of the witness.
\end{proof}

\closureat{MetacicDecidabilityWitnessUp}{seedStr}
\closureat{MetacicDecidabilityWitnessUp}{obligationStr}
\closureat{MetacicDecidabilityWitnessUp}{scopedStr}

\begin{closurestatus}{\MetacicDecidabilityWitnessUp}
  \constructivestory{A $\MetacicDecidabilityWitnessUp$ packet is generated from inferTypeCtx soundness and completeness, checkSameTerm structural equality, bounded beta comparison, finished-normal side conditions, refusal rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:metacic-decidability-witness-carrier}, \autoref{thm:metacic-decidability-witness-namecert-obligations}, \autoref{thm:metacic-decidability-witness-bounded-conversion-nonescape}, \autoref{thm:metacic-decidability-witness-bounded-normal-form-split}, \autoref{thm:metacic-decidability-witness-typing-exactness-route}, \autoref{thm:metacic-decidability-witness-obligation-closure-package}, \autoref{thm:metacic-decidability-witness-scope-assignment}, \autoref{thm:metacic-decidability-witness-scope-routes}, \autoref{thm:metacic-decidability-witness-scoped-closure-package}, and \autoref{thm:metacic-decidability-witness-public-export} bind typing exactness, same-term checking, bounded conversion, finished-normal exposure, refusal rows, transport, replay, provenance, and local naming as one scoped finite NameCert witness with a public consumer route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close standalone ClosedAt decidability, unrestricted closed beta-conversion decidability, fuel-erased normalisation, quotient equality over represented terms, Lean-side verification, or bridge checking.}
  \upgradepath{Public closure requires Lean verification over the consumer-facing export for the same typing, same-term, bounded-conversion, finished-normal, refusal, transport, replay, provenance, and local naming rows.}
\end{closurestatus}
