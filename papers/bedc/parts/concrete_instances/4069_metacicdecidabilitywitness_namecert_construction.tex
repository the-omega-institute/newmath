\chapter{A Concrete Naming Certificate for $\MetacicDecidabilityWitnessUp$}
\label{ch:concrete-instances-metacicdecidabilitywitness-namecert}
\origin{ai}

The $\MetacicDecidabilityWitnessUp$ packet realizes the decidability audit map of \autoref{ch:metacic-decidable-audit-map} as a finite NameCert witness. It sits beside the normal false exclusion packet of \autoref{ch:concrete-instances-normalfalseexclusion-namecert} and the confluence normal-fragment packet of \autoref{ch:concrete-instances-metacicconfluencenormalfragment-namecert}: those siblings keep normal-form and confluence boundaries visible, while this packet separates typing-checker exactness from bounded beta-conversion evidence.

\begin{definition}[MetaCIC decidability witness carrier]
\label{def:metacic-decidability-witness-carrier}
A $\MetacicDecidabilityWitnessUp$ carrier is a finite $\BHist$ packet
$$
W=(T,S,B,F,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the $\mathsf{inferTypeCtx}$ exactness row, containing the soundness and completeness surfaces for the encoded $\mathsf{HasType}$ relation;
\item $S$ is the $\mathsf{checkSameTerm}$ structural equality row used by the typing checker;
\item $B$ is the bounded beta-comparison row for $\mathsf{normalizeBounded}$ and $\mathsf{decideClosedBetaConv}$;
\item $F$ is the finished-normal-form side-condition row consumed when a bounded comparison result is read as equality or beta-convertibility of named normal forms;
\item $R$ records refusal of a standalone $\mathsf{ClosedAt}$ decision theorem, unrestricted closed beta-conversion decidability, and fuel-erased normalisation;
\item $H$ records componentwise $\hsame$ transport for $T,S,B,F,R,P,N$;
\item $C$ records displayed $\Cont$ routes for typing exactness, same-term checking, bounded comparison, finished-normal side conditions, and refusal reads;
\item $P$ is the $\Pkg$ provenance row over the decidability audit map and its normal-form and confluence siblings;
\item $N$ is the local $\NameCert_{\MetacicDecidabilityWitnessUp}$ row.
\end{enumerate}
The local classifier compares accepted carriers only through the rows $T,S,B,F,R,H,C,P,N$.
\end{definition}

\begin{theorem}[MetaCIC decidability witness NameCert obligations]
\label{thm:metacic-decidability-witness-namecert-obligations}
Every accepted $\MetacicDecidabilityWitnessUp$ packet binds exactly the finite NameCert obligation surface of \autoref{def:metacic-decidability-witness-carrier}: typing-checker exactness through $T$, same-term structural equality through $S$, bounded beta comparison through $B$, finished-normal side conditions through $F$, refusal boundaries through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier. The positive checker rows are $T$ and $S$: typing exactness is read through the inferTypeCtx row, while structural equality is read through the same-term checker row used by that typing surface.

The bounded conversion rows are $B$ and $F$. The comparison row exposes only bounded normalizer output comparison, and the finished-normal side-condition row supplies the additional hypotheses under which the audit map permits equality or beta-convertibility reads for named normal forms.

The refusal row $R$ remains part of the same packet. It records that no coordinate exports a standalone closedness decider, an unrestricted closed beta-conversion decider, or fuel-erased normalisation.

Finally $H,C,P,N$ transport, replay, cite, and name only those displayed rows. Since the carrier has no further coordinate, the local NameCert surface is exhausted by the finite obligations listed above.
\end{proof}
\leanchecked{BEDC.Derived.MetacicDecidabilityWitnessUp.MetacicDecidabilityWitnessTasteGate\_single\_carrier\_alignment}

\begin{theorem}[MetaCIC decidability witness bounded conversion non-escape]
\label{thm:metacic-decidability-witness-bounded-conversion-nonescape}
Any consumer of an accepted $\MetacicDecidabilityWitnessUp$ packet may read conversion evidence only through $B$ together with $F$. The read can expose bounded-normal output equality and the finished-normal consequences named by the audit map, but it cannot export a global closed beta-conversion decision procedure, a fuel-erased normalisation theorem, a quotient equality over represented terms, or a host evaluator.
\end{theorem}
\begin{proof}
Begin with the conversion coordinates. The carrier separates the bounded comparison row $B$ from the finished-normal side-condition row $F$, so a consumer route that asks for conversion evidence must pass through both rows.

The refusal row $R$ records the missing endpoints. It prevents the bounded comparison route from being reread as a global closed beta-conversion decider, fuel-erased normalisation theorem, quotient equality, or host evaluator.

Transport and replay do not add data. The rows $H$ and $C$ preserve the same displayed comparison and side-condition routes, while $P$ and $N$ only record provenance and local naming. Thus the conversion read stays inside the bounded witness packet.
\end{proof}
\leanchecked{BEDC.Derived.MetacicDecidabilityWitnessUp.MetacicDecidabilityWitnessBoundedConversionNonescape}
\leanvariant{BEDC.Derived.MetacicDecidabilityWitnessUp.MetacicDecidabilityWitnessCarrier\_bounded\_conversion\_nonescape}

\begin{theorem}[MetaCIC decidability witness bounded normal-form split]
\label{thm:metacic-decidability-witness-bounded-normal-form-split}
An accepted $\MetacicDecidabilityWitnessUp$ packet may expose bounded checker output and normal-form endpoint rows only through its displayed candidate row $T$, structural comparison row $S$, fuel and window row $B$, normal-form side-condition row $F$, refusal row $R$, componentwise transport $H$, displayed replay $C$, provenance $P$, and local $\NameCert_{\MetacicDecidabilityWitnessUp}$ row $N$. A later confluence or decidability consumer may inspect those rows as bounded normal-form evidence, but the packet does not expose global closed-term conversion decidability, unrestricted subject reduction, quotient syntax equality, or consistency.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-decidability-witness-carrier}. The checker candidate, structural comparison, bounded fuel and window, normal-form side condition, refusal, transport, replay, provenance, and naming rows are exactly $T,S,B,F,R,H,C,P,N$.

Invoke the NameCert obligations of \autoref{thm:metacic-decidability-witness-namecert-obligations}. They identify the admissible checker and normal-form reads with the displayed rows and leave no additional coordinate for a global conversion decider or host syntax equality.

Use the bounded-conversion non-escape theorem \autoref{thm:metacic-decidability-witness-bounded-conversion-nonescape}. It confines conversion evidence to $B$ together with $F$, while $R$ records the missing global endpoints. Thus a later consumer may inspect bounded normal-form evidence through the named rows only, and cannot reread it as unrestricted conversion decidability, subject reduction, quotient syntax equality, or consistency.
\end{proof}
\leanchecked{BEDC.Derived.MetacicDecidabilityWitnessUp.MetacicDecidabilityWitness\_bounded\_normal\_form\_split}

\begin{theorem}[MetaCIC decidability witness typing-exactness route]
\label{thm:metacic-decidability-witness-typing-exactness-route}
Every accepted $\MetacicDecidabilityWitnessUp$ packet routes typing-checker exactness through the inferTypeCtx row $T$ before any consumer may combine structural equality $S$, bounded beta comparison $B$, and finished-normal row $F$. A request that skips $T$ cannot be read as $\mathsf{HasType}$ exactness or as closed conversion evidence for the packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-decidability-witness-carrier}. The row $T$ is the unique displayed coordinate that names inferTypeCtx soundness and completeness for the encoded $\mathsf{HasType}$ relation, while $S,B,F$ record structural equality, bounded comparison, and finished-normal side conditions.

Apply the NameCert obligations of \autoref{thm:metacic-decidability-witness-namecert-obligations}. They bind typing-checker exactness to $T$ and leave $S,B,F$ as later consumer rows, so the structural and bounded-normal data cannot be promoted into a typing-exactness witness without first exposing $T$.

Use the bounded normal-form split of \autoref{thm:metacic-decidability-witness-bounded-normal-form-split}. It permits bounded normal-form inspection only through the displayed rows and keeps the refusal row $R$ visible. Together with \autoref{thm:metacic-decidability-witness-bounded-conversion-nonescape}, this excludes a global $\mathsf{ClosedAt}$ decision theorem, unrestricted beta conversion, quotient syntax equality, and evaluator rows.

Thus any route skipping $T$ lacks the only row that carries inferTypeCtx exactness. Such a route may inspect bounded comparison evidence after the displayed side conditions, but it is not a $\mathsf{HasType}$ exactness route and does not close conversion for the packet.
\end{proof}

\begin{theorem}[MetaCIC decidability witness obligation-closure package]
\label{thm:metacic-decidability-witness-obligation-closure-package}
For the carrier of \autoref{def:metacic-decidability-witness-carrier}, the $\MetacicDecidabilityWitnessUp$ obligation surface is closed by the carrier row, the NameCert obligations, bounded-conversion nonescape, bounded normal-form split, and typing-exactness route. Equivalently, every accepted packet $W=(T,S,B,F,R,H,C,P,N)$ exposes exactly inferTypeCtx exactness through $T$, checkSameTerm structural equality through $S$, bounded beta comparison through $B$, finished-normal side conditions through $F$, refusal rows through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and the local $\NameCert_{\MetacicDecidabilityWitnessUp}$ row $N$.
\end{theorem}
\begin{proof}
Carrier admission supplies the displayed tuple $T,S,B,F,R,H,C,P,N$, and the existing TasteGate marker above fixes the AI chapter boundary at that single carrier. The NameCert obligations of \autoref{thm:metacic-decidability-witness-namecert-obligations} then assign typing exactness, structural equality, bounded comparison, finished-normal side conditions, refusal reads, transport, replay, provenance, and naming to those rows.

The bounded-conversion theorem \autoref{thm:metacic-decidability-witness-bounded-conversion-nonescape} blocks escape from $B$ and $F$ to global conversion, fuel-erased normalisation, quotient equality, or a host evaluator. The bounded normal-form split of \autoref{thm:metacic-decidability-witness-bounded-normal-form-split} keeps every bounded checker and normal-form read inside $T,S,B,F,R,H,C,P,N$.

Finally, the typing-exactness route of \autoref{thm:metacic-decidability-witness-typing-exactness-route} forces every $\mathsf{HasType}$ exactness read through $T$ before structural or bounded-normal evidence is combined. These cited rows cover every obligation named by the carrier and leave no additional coordinate for a separate decidability, transport, replay, provenance, or local naming obligation.
\end{proof}

\closureat{MetacicDecidabilityWitnessUp}{seedStr}
\closureat{MetacicDecidabilityWitnessUp}{obligationStr}

\begin{closurestatus}{\MetacicDecidabilityWitnessUp}
  \constructivestory{A $\MetacicDecidabilityWitnessUp$ packet is generated from inferTypeCtx soundness and completeness, checkSameTerm structural equality, bounded beta comparison, finished-normal side conditions, refusal rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:metacic-decidability-witness-carrier}, \autoref{thm:metacic-decidability-witness-namecert-obligations}, \autoref{thm:metacic-decidability-witness-bounded-conversion-nonescape}, \autoref{thm:metacic-decidability-witness-bounded-normal-form-split}, \autoref{thm:metacic-decidability-witness-typing-exactness-route}, and \autoref{thm:metacic-decidability-witness-obligation-closure-package} bind typing exactness, bounded conversion non-escape, bounded normal-form exposure, refusal rows, transport, replay, provenance, and local naming as one finite NameCert witness.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close standalone ClosedAt decidability, unrestricted closed beta-conversion decidability, fuel-erased normalisation, quotient equality over represented terms, Lean-side verification, or bridge checking.}
  \upgradepath{Scoped closure requires assigning each obligation row to the MetaCIC typing, same-term, bounded-conversion, finished-normal, refusal, transport, replay, provenance, and local naming scope.}
\end{closurestatus}
