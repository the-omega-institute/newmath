\begin{theorem}[AuthorizedGeneratorRecursor Real stream route]
\label{thm:authorized-generator-recursor-real-stream-route}
For every accepted $\AuthorizedGeneratorRecursorUp$ packet that is read as an L10 source route, the public generator output may depend on $\RealUp$, $\RegSeqRatUp$, and $\StreamNameUp$ source rows only after those rows are carried through the same finite route
$$
I,E,M,B,D,O,A,H,C,P,G,N.
$$
The route exposes source rows as $\BHist$, $\hsame$, $\Cont$, $\Pkg$, boundary, and local $\NameCert$ data, and it exports no selected Real limit, quotient stream equality, host evaluation, or detached generator state.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursor\_real\_stream\_route}
\begin{proof}
Begin with the closed-substitution handoff of \autoref{thm:authorized-generator-recursor-closed-substitution-handoff}. It keeps every output-facing consumer inside the displayed packet before the row $O$ is public.

Use the ground-compiler authorization theorem, \autoref{thm:authorized-generator-recursor-ground-compiler-authorization}. It forces the same retained route for any consumer that compiles the generator output into a ground-facing surface.

Finally apply \autoref{thm:authorized-generator-recursor-terminal-authorized-output-cover}. Terminal authorized output is read only from the finite packet rows, so the Real, regular-rational, and stream-name source rows remain packet data rather than external completion objects.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor substitution membrane lock]
\label{thm:authorized-generator-recursor-substitution-membrane-lock}
The L10 route of $\AuthorizedGeneratorRecursorUp$ locks closed-substitution and operational-membrane rows before any generator output is public. A consumer may read substitution, branch, descent, audit, transport, continuation, package, boundary, and local naming rows from the packet, but it cannot cross the membrane through unrestricted substitution, unchecked recursion, external proof generation, host equality, or a private audit channel.
\end{theorem}
\begin{proof}
Read the output boundary through \autoref{thm:authorized-generator-recursor-closed-substitution-handoff}. The handoff places closed-substitution consumption behind the displayed carrier route rather than behind a general substitution calculus.

Next use \autoref{thm:authorized-generator-recursor-ground-compiler-authorization}. The operational side of the membrane retains signature, eliminator, motive, branch, descent, audit, transport, continuation, provenance, boundary, and local naming rows together.

The terminal cover theorem, \autoref{thm:authorized-generator-recursor-terminal-authorized-output-cover}, closes the membrane at the output row. It leaves no route for unchecked recursion, host equality, external proof generation, or a private audit channel outside the displayed packet.
\end{proof}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursor\_substitution\_membrane\_lock}

\begin{theorem}[AuthorizedGeneratorRecursor generator normalization readiness]
\label{thm:authorized-generator-recursor-generator-normalization-readiness}
Generator-normalization readiness for the current L10 $\AuthorizedGeneratorRecursorUp$ packet is available only after the Real, regular-rational, stream-name, closed-substitution, operational-membrane, audit, continuation, package, boundary, and local NameCert rows have been retained in one displayed route. The readiness claim does not publicize any output through host normalization, unchecked recursion, selected Real limits, quotient stream equality, or an off-packet generator certificate.
\end{theorem}
\begin{proof}
Start from \autoref{thm:authorized-generator-recursor-closed-substitution-handoff}. It makes the output row readable only through the retained closed-substitution and generator-audit route.

Use \autoref{thm:authorized-generator-recursor-ground-compiler-authorization} to attach the normalization-facing route to the same accepted signature, emitted recursor, motive, branch, descent, audit, transport, continuation, provenance, boundary, and local naming rows.

Finally apply \autoref{thm:authorized-generator-recursor-terminal-authorized-output-cover}. Since terminal output remains covered by the displayed packet, generator normalization is ready only as a packet-local consumer surface and not as a host-normalizer or selected-limit interface.
\end{proof}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursor\_generator\_normalization\_readiness}
