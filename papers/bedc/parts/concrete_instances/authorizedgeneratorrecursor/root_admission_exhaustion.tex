\begin{theorem}[AuthorizedGeneratorRecursor admission exhaustion]
\label{thm:authorized-generator-recursor-admission-exhaustion}
Every accepted $\AuthorizedGeneratorRecursorUp$ root packet is exhausted by the displayed $\BHist$ carrier rows
$$
I,E,M,B,D,O,A,H,C,P,G,N
$$
together with the readiness view
$$
U,J,K,O,L,H,C,P,N.
$$
An output-facing read is admitted only after the accepted signature, emitted recursor, checked motive, branch package, structural descent, audit row, boundary row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, refusal ledger, and local $\NameCert$ row have all been retained. No route into \autoref{ch:concrete-instances-closed-substitution-boundary-namecert}, \autoref{ch:concrete-instances-metacicnormalizationfrontier-namecert}, or \autoref{ch:concrete-instances-recursorgenerator-namecert} may read a detached generator output outside those rows.
\end{theorem}
\begin{proof}
Start with carrier admission and root carrier totality. Theorems~\ref{thm:authorized-generator-recursor-root-carrier-admission} and~\ref{thm:authorized-generator-recursor-root-carrier-totality} display the admitted root packet and leave no carrier coordinate outside $I,E,M,B,D,O,A,H,C,P,G,N$.

Next pass to the readiness view. The definition of the readiness route, \autoref{def:authorized-generator-recursor-readiness-route}, repackages the same admitted data as $U,J,K,O,L,H,C,P,N$, while \autoref{thm:authorized-generator-recursor-namecert-obligation-exhaustion} keeps carrier, classifier, stability, ledger, and non-escape obligations on that packet.

Finally attach the sibling consumers. The closed-substitution gate, terminal consumer readiness, and root output route saturation theorems force every closed-substitution, MetaCIC normalization frontier, and recursor-generator read to retain the boundary, replay, provenance, refusal, and local naming rows before reading $O$. Thus admission is exhausted by the two displayed finite views.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor route refusal]
\label{thm:authorized-generator-recursor-route-refusal}
Let an accepted $\AuthorizedGeneratorRecursorUp$ consumer request a generator output not listed in the authorized signature, branch, descent, and audit rows of the packet. Then the request remains in the refusal and boundary rows $L,G,A,N$ and is not admitted as an output row $O$. In particular, no downstream closed-substitution, MetaCIC normalization frontier, or recursor-generator consumer may reinterpret an unlisted generator result as an authorized branch, recursive call, host induction principle, proof producer, or off-packet substitution channel.
\end{theorem}
\begin{proof}
Read the authorization side first. The accepted signature route, branch budget exhaustion, and root signature totality theorems place every listed generator route behind $I,E,M,B,D$ and the audit row $A$.

Now apply output refusal. Theorem~\ref{thm:authorized-generator-recursor-output-refusal} sends any request missing the authorization row or refusal ledger to the $\mathsf{GAP}$ boundary, and \autoref{thm:authorized-generator-recursor-ground-route-refusal-surface} keeps ground-facing refusals on the displayed packet.

It remains to block downstream reinterpretation. The closed-substitution budget lock, MetaCIC normalization boundary route, and RecursorGenerator authorization obligations only consume output through the displayed finite route; terminal reader nonescape then removes hidden generator state, host evaluation, unchecked recursion, proof-producing branches, and off-packet substitution channels.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor downstream NameCert package exactness]
\label{thm:authorized-generator-recursor-downstream-namecert-package}
For every accepted $\AuthorizedGeneratorRecursorUp$ packet, the downstream NameCert package is exactly the finite package consisting of the carrier rows $I,E,M,B,D,O,A,H,C,P,G,N$, the readiness classifier rows $U,J,K,O,L,H,C,P,N$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local $\NameCert$ rows through $N$. Downstream closed-substitution, MetaCIC normalization frontier, and recursor-generator consumers may specialize this package, but may not add a carrier, classifier, continuation, provenance, or naming row outside it.
\end{theorem}
\begin{proof}
Begin with the downstream consumer surface. The root consumer factorization and public output coverage theorems force public output consumers to factor through the displayed carrier and readiness packet before a specialization is read.

Next identify the structural rows. Componentwise transport is carried by $H$, replay by $C$, provenance by $P$, and local naming by $N$; the NameCert obligation exhaustion theorem states that these rows discharge the carrier, classifier, stability, refusal-ledger, and non-escape clauses for the same packet.

Finally use the sibling locks. Closed-substitution handoff keeps substitution consumers inside the boundary rows, MetaCIC normalization frontier reads the output only as finite boundary data, and RecursorGenerator consumes the result as authorized generator provenance rather than as a second generator. Therefore the downstream NameCert package is exact.
\end{proof}
