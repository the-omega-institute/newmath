\chapter{A Concrete Naming Certificate for $\AuthorizedGeneratorRecursorUp$}
\label{ch:concrete-instances-authorizedgeneratorrecursor-namecert}
\origin{ai}

The $\AuthorizedGeneratorRecursorUp$ surface turns the recursor authorization principle of \autoref{ch:recursor-as-authorized-generator} into a finite NameCert packet. The packet records an accepted constructor signature, a legal eliminator name, complete branch data, structural descent evidence, output naming, audit provenance, and the visible boundary at which BEDC relies on the host kernel's acceptance of the recursor call. It does not add a new eliminator, a new induction principle, or a hidden source of proof terms outside the accepted signature.

The audit-provenance row $A$ is the local consumer of the $\OperationalMembraneUp$ audit carrier in \autoref{ch:concrete-instances-operationalmembrane-namecert}. It reads source-axiom refusal, axiom-purity ancestry, and import-separation rows before the recursor output row $O$ becomes public.

\begin{definition}[AuthorizedGeneratorRecursor carrier]
\label{def:authorized-generator-recursor-carrier}
An $\AuthorizedGeneratorRecursorUp$ carrier is a finite $\BHist$ packet
$$
R=(I,E,M,B,D,O,A,H,C,P,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the accepted constructor-signature row, naming the finite constructors of the inductive carrier and the strict-positivity gate that admits them;
\item $E$ is the emitted eliminator row, naming the corresponding recursor $I.\mathrm{rec}$ as the only generator admitted by this packet;
\item $M$ is the checked motive row, recording the output family or target judgment selected for the recursor call;
\item $B$ is the branch-package row, containing exactly one accepted branch for each constructor listed by $I$;
\item $D$ is the structural-descent row, recording the recursive arguments and the kernel-visible decrease or constructor subterm relation used by the call;
\item $O$ is the output-name row, naming the term, proof object, or definition produced by the recursor call;
\item $A$ is the audit-provenance row, recording the build check, source-axiom refusal, and transitive dependency refusal relevant to the generated output;
\item $H$ records componentwise $\hsame$ transport for $I,E,M,B,D,O,A$, package provenance, boundary data, and the local naming row;
\item $C$ records the displayed $\Cont$ routes by which downstream consumers read the signature, eliminator, motive, branch package, descent row, output name, and audit provenance;
\item $P$ is the $\Pkg$ provenance over $\BHist$, constructor signatures, recursor names, motive rows, branch packages, structural-descent rows, output names, audit reports, $\hsame$, $\Cont$, $\mathsf{GAP}$, and $\NameCert$;
\item $G$ is the $\mathsf{GAP}$ boundary row recording that the packet relies on host-kernel acceptance of the displayed signature and recursor call, while refusing any unlisted branch, non-structural recursion channel, external axiom, or hidden proof-producing generator;
\item $N$ is the local $\NameCert_{\AuthorizedGeneratorRecursorUp}$ row.
\end{enumerate}
The carrier contains no coordinate for an extra constructor, a missing branch, an unchecked motive, a non-decreasing recursive call, an output without a name, an unaudited dependency channel, or a proof term generated outside $I.\mathrm{rec}$ and the accepted constructor signature.
\end{definition}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorTasteGate\_single\_carrier\_alignment}

\begin{theorem}[AuthorizedGeneratorRecursor branch exhaustion]
\label{thm:authorized-generator-recursor-branch-exhaustion}
For an accepted $\AuthorizedGeneratorRecursorUp$ packet, the branch-package row $B$ exhausts the generator surface named by $E$. Every public use of the packet reads one branch for each constructor displayed in $I$, transports those branches only through the componentwise row $H$, and cannot recover any additional branch or constructor case from the packet.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorBranchExhaustion}
\begin{proof}
Project the carrier of \autoref{def:authorized-generator-recursor-carrier}. The constructor signature $I$ and emitted eliminator $E$ are displayed together, and $B$ is declared to contain exactly one accepted branch for each constructor listed by $I$.

Read downstream access through $C$. A consumer reaches the branch package only by the displayed continuation route, so the public surface is the finite list already paired with $I$.

Transport through $H$ preserves the same component rows. Since the tuple has no coordinate for an extra constructor or unlisted branch, the branch surface is exhausted by the entries already recorded in $B$.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor NameCert surface]
\label{thm:authorized-generator-recursor-namecert-surface}
The local $\NameCert_{\AuthorizedGeneratorRecursorUp}$ surface for the carrier of \autoref{def:authorized-generator-recursor-carrier} consists exactly of signature admission, recursor naming, motive checking, branch exhaustion, structural-descent visibility, output naming, audit provenance, componentwise transport, continuation locality, package provenance, and the displayed $\mathsf{GAP}$ boundary. A downstream consumer may treat $O$ as a recursor-generated named output only through the packet
$$
(I,E,M,B,D,O,A,H,C,P,G,N).
$$
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorNameCertSurface}
\begin{proof}
Carrier exposure supplies the finite tuple. The rows $I,E,M,B,D,O,A,H,C,P,G,N$ are the only rows admitted to the local NameCert surface.

Signature admission and recursor naming are read from $I$ and $E$. Motive checking, branch exhaustion, and structural-descent visibility are read from $M,B,D$, with branch exhaustion supplied by \autoref{thm:authorized-generator-recursor-branch-exhaustion}.

The output becomes public through $O$ only after the audit row $A$, transport row $H$, continuation row $C$, package row $P$, boundary row $G$, and local certificate row $N$ are retained. No coordinate in the packet can replace these rows with an unnamed proof term or an external generator.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor audit boundary]
\label{thm:authorized-generator-recursor-audit-boundary}
An accepted $\AuthorizedGeneratorRecursorUp$ packet binds the audit boundary for recursor-authorized proof generation. The packet certifies that the displayed output is public only as the named result of the accepted signature, emitted eliminator, checked motive, complete branch package, structural-descent row, and audit-provenance row. It does not certify kernel completeness, global termination theory, semantic conservativity for all inductive declarations, or any output whose dependency chain escapes the displayed audit provenance.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorAuditBoundary}
\begin{proof}
Start with the NameCert surface of \autoref{thm:authorized-generator-recursor-namecert-surface}. It forces the output row $O$ to be read together with the constructor signature, eliminator, motive, branch package, descent row, and audit row.

The boundary row $G$ records the host-kernel handoff. It names the reliance on kernel acceptance of the signature and recursor call while refusing unlisted branches, non-structural recursion, external axioms, and hidden proof generators.

The excluded claims have no coordinate in the carrier. Therefore the packet gives a seed certificate for the displayed recursor-generated output and does not export global kernel completeness, unrestricted termination, general semantic conservativity, or an unaudited dependency surface.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor structural descent locality]
\label{thm:authorized-generator-recursor-structural-descent-locality}
Every public recursive read of an accepted $\AuthorizedGeneratorRecursorUp$ packet factors through the displayed constructor signature $I$, emitted eliminator $E$, checked motive $M$, branch package $B$, and structural-descent row $D$. The packet admits no public recursive call whose descent is not recorded in $D$, and it admits no branch read outside the constructor list displayed by $I$ and exhausted by $B$.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorUp\_structural\_descent\_locality}
\begin{proof}
Project the carrier
$$
R=(I,E,M,B,D,O,A,H,C,P,G,N).
$$
The only recursive generator named by the packet is the emitted eliminator row $E$, and it is paired with the constructor signature $I$, motive row $M$, branch package $B$, and descent row $D$.

Read access through $C$ exposes those rows as the local public interface. By \autoref{thm:authorized-generator-recursor-branch-exhaustion}, the branch package already contains exactly the public branches available for the constructors displayed in $I$.

The boundary row $G$ refuses non-structural recursion and unlisted branches. Since the tuple has no coordinate for a second recursion channel, a non-decreasing recursive call, or an extra branch source, every public recursive read is local to $I,E,M,B,D$.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor audit provenance factorization]
\label{thm:authorized-generator-recursor-audit-provenance-factorization}
For an accepted $\AuthorizedGeneratorRecursorUp$ packet, the output row $O$ becomes public only after the audit-provenance row $A$, componentwise transports $H$, continuation routes $C$, package row $P$, boundary row $G$, and local name row $N$ are retained. Thus public output exposure factors through the audit boundary of \autoref{thm:authorized-generator-recursor-audit-boundary} and cannot be read as an unaudited recursor result.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorAuditProvenanceFactorization}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursor\_audit\_provenance\_factorization}
\begin{proof}
Project the carrier and retain the rows $O,A,H,C,P,G,N$. The NameCert surface of \autoref{thm:authorized-generator-recursor-namecert-surface} states that $O$ is part of the local certificate only inside the displayed packet.

Apply \autoref{thm:authorized-generator-recursor-audit-boundary}. It binds the public output to the accepted signature, emitted eliminator, checked motive, complete branch package, structural-descent row, and audit-provenance row.

The rows $H,C,P,G,N$ keep the transports, public routes, package provenance, boundary refusal, and local naming certificate attached to that exposure. No coordinate remains from which a consumer can read $O$ apart from $A,H,C,P,G,N$, so the output factors through the displayed audit provenance.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor ground-compiler handoff]
\label{thm:authorized-generator-recursor-ground-compiler-handoff}
The \texttt{\textbackslash origin\{ai\}} TasteGate marker for this chapter is consumed only as the single-carrier alignment witness for the finite $\AuthorizedGeneratorRecursorUp$ packet. It does not replace the paper-side obligations for branch exhaustion, structural-descent locality, audit provenance factorization, or the visible handoff to the host kernel at $G$.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorGroundCompilerHandoff}
\begin{proof}
The checked TasteGate marker attached to \autoref{def:authorized-generator-recursor-carrier} identifies the chapter's finite carrier as the aligned object used by the recursor packet. It does not add a new theorem row to the carrier and does not alter the closure status of any paper-side claim.

Branch exhaustion is supplied by \autoref{thm:authorized-generator-recursor-branch-exhaustion}. Structural-descent locality is supplied by \autoref{thm:authorized-generator-recursor-structural-descent-locality}, and audit exposure is supplied by \autoref{thm:authorized-generator-recursor-audit-provenance-factorization}.

The handoff to the ground compiler is therefore explicit: the TasteGate witness aligns the finite carrier, while the displayed rows $I,E,M,B,D,O,A,H,C,P,G,N$ continue to carry the branch, descent, provenance, and boundary obligations inside the paper.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor host-kernel handoff]
\label{thm:authorized-generator-recursor-host-kernel-handoff}
Every accepted $\AuthorizedGeneratorRecursorUp$ packet that reaches the host-kernel boundary row $G$ first exposes the authorized generator branch row $B$, structural-descent row $D$, audit-provenance row $A$, visible $\Cont$ route $C$, and local $\NameCert_{\AuthorizedGeneratorRecursorUp}$ row $N$. The handoff exports no free induction principle, hidden branch source, or proof-producing generator outside the displayed constructor signature and accepted generator row.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorHostKernelHandoff}
\begin{proof}
Start with branch exhaustion and branch-descent totality, \autoref{thm:authorized-generator-recursor-branch-exhaustion} and \autoref{thm:authorized-generator-recursor-branch-descent-totality}. They force public recursive reads to stay on the accepted constructor signature, branch package, and displayed descent row.

Use audit provenance factorization from \autoref{thm:authorized-generator-recursor-audit-provenance-factorization}. It keeps public output attached to the audit row, continuation route, package provenance, boundary row, and local name row.

Finally cite the TasteGate handoff only as the single-carrier alignment witness, through \autoref{thm:authorized-generator-recursor-ground-compiler-handoff}. Since the aligned carrier has no coordinate for a free induction principle or hidden generator, the host-kernel row $G$ receives exactly the displayed branch, descent, audit, route, and local NameCert surface.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor kernel-boundary exhaustion]
\label{thm:authorized-generator-recursor-kernel-boundary-exhaustion}
Every public recursor read of an accepted $\AuthorizedGeneratorRecursorUp$ packet factors through exactly
$$
I,E,M,B,D,O,A,H,C,P,G,N.
$$
The read cannot introduce an unlisted constructor, a missing branch, a non-structural recursive call, an external axiom, or a hidden proof generator beyond the host-kernel boundary recorded by $G$.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorKernelBoundaryExhaustion}
\begin{proof}
Start from the carrier definition in \autoref{def:authorized-generator-recursor-carrier}. It displays the full packet and leaves no coordinate outside $I,E,M,B,D,O,A,H,C,P,G,N$.

Branch coverage is exhausted by \autoref{thm:authorized-generator-recursor-branch-exhaustion}, while the local $\NameCert$ surface is fixed by \autoref{thm:authorized-generator-recursor-namecert-surface}. Thus a consumer may read only the listed constructor, eliminator, motive, branch, output, transport, route, package, boundary, and naming rows.

Structural recursion is confined by \autoref{thm:authorized-generator-recursor-structural-descent-locality}. Audit exposure is confined by \autoref{thm:authorized-generator-recursor-audit-boundary} and \autoref{thm:authorized-generator-recursor-audit-provenance-factorization}.

Finally apply \autoref{thm:authorized-generator-recursor-ground-compiler-handoff}. The TasteGate handoff aligns the same finite carrier and does not add a constructor, branch, recursion channel, axiom row, or proof-producing generator outside the displayed kernel boundary.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor output-name stability]
\label{thm:authorized-generator-recursor-output-name-stability}
For an accepted $\AuthorizedGeneratorRecursorUp$ packet, the output-name row $O$ is stable only when the accepted signature $I$, eliminator $E$, motive $M$, branch package $B$, descent row $D$, audit row $A$, componentwise transports $H$, displayed routes $C$, boundary row $G$, and local name row $N$ are transported together. A consumer cannot keep the public output name while replacing any one of these supporting rows by an unlisted row.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorOutputNameStability}
\begin{proof}
Project the carrier of \autoref{def:authorized-generator-recursor-carrier}. The row $O$ is not a standalone output; it is one coordinate of the displayed packet with $I,E,M,B,D,A,H,C,P,G,N$.

Use \autoref{thm:authorized-generator-recursor-namecert-surface} to read the output through the local certificate, and use \autoref{thm:authorized-generator-recursor-branch-exhaustion} with \autoref{thm:authorized-generator-recursor-structural-descent-locality} to keep the signature, branch package, and descent row attached to that read.

Now apply \autoref{thm:authorized-generator-recursor-audit-boundary} and \autoref{thm:authorized-generator-recursor-audit-provenance-factorization}. They keep the audit row, transports, routes, package provenance, boundary row, and naming row attached before $O$ becomes public.

The TasteGate handoff of \autoref{thm:authorized-generator-recursor-ground-compiler-handoff} consumes the same carrier. Therefore any transported public output name preserves the whole displayed packet surface, not a detached name row.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor audit-row completeness]
\label{thm:authorized-generator-recursor-audit-row-completeness}
The audit row $A$ is complete for the accepted $\AuthorizedGeneratorRecursorUp$ packet in the following finite sense: before $O$ is public, $A$ must expose the build check, source-axiom refusal, dependency refusal, and operational-membrane provenance consumed by the packet. This audit completeness does not prove kernel completeness, global termination, or a proof generator outside the accepted recursor boundary.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorAuditRowCompleteness}
\begin{proof}
Begin with the carrier definition and its audit-provenance row. The displayed packet places $A$ before public output exposure and ties it to $I,E,M,B,D,O,H,C,P,G,N$.

Apply \autoref{thm:authorized-generator-recursor-audit-boundary}. It states that public output is certified only as the named result of the accepted signature, emitted eliminator, checked motive, complete branch package, structural-descent row, and audit-provenance row.

Next use \autoref{thm:authorized-generator-recursor-audit-provenance-factorization}. The build check, source-axiom refusal, dependency refusal, operational provenance, transports, routes, boundary, and local name remain in the same packet before $O$ is exposed.

Branch exhaustion, structural descent locality, and the TasteGate handoff are supplied by \autoref{thm:authorized-generator-recursor-branch-exhaustion}, \autoref{thm:authorized-generator-recursor-structural-descent-locality}, and \autoref{thm:authorized-generator-recursor-ground-compiler-handoff}. Since the carrier has no coordinate for kernel completeness or global termination, the audit row is complete only for the finite packet boundary stated above.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor operational membrane audit handoff]
\label{thm:authorized-generator-recursor-operational-membrane-audit-handoff}
For an accepted $\AuthorizedGeneratorRecursorUp$ packet, the audit row $A$ must consume the $\OperationalMembraneUp$ provenance before the output row $O$ becomes public. The public output route therefore factors through the displayed build check, source-axiom refusal, dependency refusal, operational-membrane provenance, output-name row, and local $\NameCert$ row, all inside
$$
R=(I,E,M,B,D,O,A,H,C,P,G,N).
$$
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorOperationalMembraneAuditHandoff}
\begin{proof}
Project the carrier of \autoref{def:authorized-generator-recursor-carrier}. The row $A$ records the build check, source-axiom refusal, dependency refusal, and operational-membrane provenance, while $O$ is only the displayed output-name row of the same packet.

Apply \autoref{thm:authorized-generator-recursor-audit-boundary} and \autoref{thm:authorized-generator-recursor-audit-provenance-factorization}. These rows place public exposure of $O$ behind the audit boundary and keep $A,H,C,P,G,N$ attached to that exposure.

Use \autoref{thm:authorized-generator-recursor-output-name-stability} with \autoref{thm:authorized-generator-recursor-audit-row-completeness}. The output name cannot be transported apart from the audit row, and the audit row is complete only after it exposes the operational-membrane provenance consumed by the packet.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor branch descent totality]
\label{thm:authorized-generator-recursor-branch-descent-totality}
For an accepted $\AuthorizedGeneratorRecursorUp$ packet, the accepted branch package $B$ and structural-descent row $D$ jointly exhaust all public recursive reads. A public read may reach the constructor signature, emitted eliminator, checked motive, accepted branches, descent evidence, output name, audit row, transports, routes, package provenance, boundary row, and local certificate, but it cannot read a recursive branch outside $B$ or a recursive call whose descent is absent from $D$.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorBranchDescentTotality}
\begin{proof}
Start from the carrier definition. The only public recursive generator named by the packet is the emitted eliminator $E$, and the rows adjacent to it are the accepted signature $I$, checked motive $M$, branch package $B$, and descent row $D$.

Apply \autoref{thm:authorized-generator-recursor-branch-exhaustion}. It says that $B$ contains exactly the public branch surface associated with the constructors listed by $I$.

Apply \autoref{thm:authorized-generator-recursor-structural-descent-locality}. It confines every public recursive read to the displayed signature, eliminator, motive, branch package, and structural-descent row.

The audit boundary and output-name stability, supplied by \autoref{thm:authorized-generator-recursor-audit-boundary} and \autoref{thm:authorized-generator-recursor-output-name-stability}, keep the resulting public output attached to the same packet. Thus $B$ and $D$ jointly exhaust the recursive read surface.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor hidden generator exclusion]
\label{thm:authorized-generator-recursor-hidden-generator-exclusion}
No public route out of an accepted $\AuthorizedGeneratorRecursorUp$ packet can recover an unlisted constructor, unchecked branch, non-structural recursion channel, external axiom, or hidden proof generator. Every public route is a projection of the carrier rows $I,E,M,B,D,O,A,H,C,P,G,N$ together with the displayed $\mathsf{GAP}$ boundary.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorHiddenGeneratorExclusion}
\begin{proof}
Read the carrier definition and the boundary row $G$. The tuple has no coordinate for an extra constructor, unchecked branch, non-structural recursion channel, external axiom, or hidden proof-producing generator.

Use branch exhaustion and branch-descent totality. By \autoref{thm:authorized-generator-recursor-branch-exhaustion} and \autoref{thm:authorized-generator-recursor-branch-descent-totality}, the public recursive surface is exhausted by the displayed constructors, accepted branches, and structural descent row.

Use audit provenance and output-name stability. By \autoref{thm:authorized-generator-recursor-audit-provenance-factorization}, \autoref{thm:authorized-generator-recursor-audit-row-completeness}, and \autoref{thm:authorized-generator-recursor-output-name-stability}, public output remains attached to the audit row and cannot detach into an unaudited generator route.

Finally apply \autoref{thm:authorized-generator-recursor-ground-compiler-handoff}. The checked carrier alignment is consumed only for the same finite packet, so it cannot add an external axiom or hidden generator beyond $G$.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor scoped packet]
\label{thm:authorized-generator-recursor-scope-package}
The branch exhaustion row, local $\NameCert$ surface, structural-descent locality row, audit-provenance factorization row, operational-membrane audit handoff, branch-descent totality row, hidden-generator exclusion row, $\mathsf{GAP}$ boundary row, and checked TasteGate single-carrier handoff form the scoped finite packet for $\AuthorizedGeneratorRecursorUp$. The packet exposes only the accepted constructor signature, emitted eliminator, checked motive, complete branch package, structural-descent row, named output, audit provenance, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, $\mathsf{GAP}$ boundary, and local $\NameCert$ row.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorScopePackage}
\begin{proof}
Start with \autoref{thm:authorized-generator-recursor-branch-exhaustion} and \autoref{thm:authorized-generator-recursor-namecert-surface}. They bind the constructor signature, emitted eliminator, motive, branch package, output name, transport, route, provenance, boundary, and local naming rows to the same finite carrier.

Next apply \autoref{thm:authorized-generator-recursor-structural-descent-locality}. It keeps every public recursive read on the displayed signature, eliminator, motive, branch package, and structural-descent row.

Use \autoref{thm:authorized-generator-recursor-audit-boundary} and \autoref{thm:authorized-generator-recursor-audit-provenance-factorization} to attach the audit-provenance row and $\mathsf{GAP}$ boundary to the public output route.

Use \autoref{thm:authorized-generator-recursor-operational-membrane-audit-handoff}, \autoref{thm:authorized-generator-recursor-branch-descent-totality}, and \autoref{thm:authorized-generator-recursor-hidden-generator-exclusion}. These rows make the operational audit handoff, the exhaustive recursive-read surface, and the refusal of hidden generators explicit inside the same packet.

Finally apply \autoref{thm:authorized-generator-recursor-ground-compiler-handoff}. The checked TasteGate alignment row is read only as the existing single-carrier handoff, so the scoped packet remains the displayed finite tuple and does not export an unlisted branch, non-structural recursion channel, unaudited output, or hidden proof-producing generator.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor signature route totality]
\label{thm:authorized-generator-recursor-signature-route-totality}
Every public read of an accepted $\AuthorizedGeneratorRecursorUp$ packet is
total only as a route through the accepted signature $I$, eliminator $E$,
motive $M$, branch package $B$, descent row $D$, output name $O$, audit row
$A$, componentwise transports $H$, displayed routes $C$, provenance row $P$,
$\mathsf{GAP}$ row $G$, and local name $N$. No downstream recursor consumer can
detach the public output from the accepted signature route.
\end{theorem}
\begin{proof}
Start with \autoref{thm:authorized-generator-recursor-branch-exhaustion} and
\autoref{thm:authorized-generator-recursor-namecert-surface}. They bind the
constructor signature, eliminator, motive, branch package, output name,
transport, route, provenance, boundary, and local naming rows to one finite
packet.

Next apply \autoref{thm:authorized-generator-recursor-structural-descent-locality}.
Every recursive read stays on the displayed signature, eliminator, motive,
branch package, and descent row.

Finally use \autoref{thm:authorized-generator-recursor-output-name-stability}
and \autoref{thm:authorized-generator-recursor-hidden-generator-exclusion}. The
output name cannot be transported apart from the signature, descent, audit, and
boundary rows, and the packet has no coordinate for a hidden generator.
\end{proof}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorSignatureRouteTotality}

\begin{theorem}[AuthorizedGeneratorRecursor output audit ledger]
\label{thm:authorized-generator-recursor-output-audit-ledger}
For an accepted $\AuthorizedGeneratorRecursorUp$ packet, the output name $O$ is
public only together with the audit row $A$, transports $H$, routes $C$,
provenance $P$, boundary $G$, and local name $N$ over the same
$I,E,M,B,D$ recursor route. The ledger refuses any public output whose
dependency or audit provenance escapes the displayed packet.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorOutputAuditLedger}
\begin{proof}
Begin with \autoref{thm:authorized-generator-recursor-audit-boundary}. It
places the public output behind the accepted signature, emitted eliminator,
checked motive, complete branch package, structural-descent row, and audit row.

Apply \autoref{thm:authorized-generator-recursor-audit-provenance-factorization}
and \autoref{thm:authorized-generator-recursor-operational-membrane-audit-handoff}.
These attach build checking, source-axiom refusal, dependency refusal,
operational provenance, transports, routes, package provenance, boundary, and
local naming before $O$ is public.

Use \autoref{thm:authorized-generator-recursor-output-name-stability}. It keeps
$O$ tied to the same $I,E,M,B,D,A,H,C,P,G,N$ rows, so the audit ledger cannot
be bypassed by an unaudited output route.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor public consumer boundary]
\label{thm:authorized-generator-recursor-public-consumer-boundary}
Every downstream consumer of an accepted $\AuthorizedGeneratorRecursorUp$
packet reads the public boundary through $I,E,M,B,D,O,A,H,C,P,G,N$. The boundary
contains branch coverage, structural descent, audit provenance, operational
membrane handoff, output-name stability, and hidden-generator refusal, and it
contains no unlisted branch, detached output name, non-structural recursion
channel, external axiom, or proof-producing generator outside $G$.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorPublicConsumerBoundary}
\begin{proof}
First combine
\autoref{thm:authorized-generator-recursor-signature-route-totality} and
\autoref{thm:authorized-generator-recursor-output-audit-ledger}. The public
consumer receives the accepted signature route and the audit ledger as one
packet.

Next apply \autoref{thm:authorized-generator-recursor-branch-descent-totality}
with \autoref{thm:authorized-generator-recursor-operational-membrane-audit-handoff}.
These rows exhaust recursive branch reads and attach the operational membrane
audit surface before the output is exposed.

Finally use \autoref{thm:authorized-generator-recursor-hidden-generator-exclusion}
and \autoref{thm:authorized-generator-recursor-ground-compiler-handoff}. The
checked carrier alignment is only the handoff for the same finite packet, so no
unlisted branch, detached output, external axiom, or hidden proof generator can
cross the public boundary.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor root branch row totality]
\label{thm:authorized-generator-recursor-root-branch-row-totality}
For an accepted $\AuthorizedGeneratorRecursorUp$ packet
$$
(I,E,M,B,D,O,A,H,C,P,G,N),
$$
every branch row used by the recursor is exposed in the displayed branch
packet $B$ before any output row $O$ is public. The public output cannot be
supplied by a hidden generator, an omitted branch, or a route outside the
displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorRootBranchRowTotality}
\begin{proof}
Start from the carrier definition and
\autoref{thm:authorized-generator-recursor-branch-exhaustion}. The accepted
packet has one displayed constructor signature, emitted eliminator, checked
motive, and complete branch package, so every recursive branch read is already
accounted for in $B$.

Apply \autoref{thm:authorized-generator-recursor-audit-boundary} and
\autoref{thm:authorized-generator-recursor-output-audit-ledger}. These results
place the output row behind the same signature, branch, descent, audit,
transport, continuation, provenance, boundary, and local-name rows.

Finally use
\autoref{thm:authorized-generator-recursor-hidden-generator-exclusion} and
\autoref{thm:authorized-generator-recursor-public-consumer-boundary}. They rule
out a proof-producing generator or omitted branch coordinate outside the
displayed packet, so $O$ can become public only after branch totality has been
read through $B$.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor audit descent row]
\label{thm:authorized-generator-recursor-audit-descent-row}
For an accepted $\AuthorizedGeneratorRecursorUp$ packet
$(I,E,M,B,D,O,A,H,C,P,G,N)$, the structural descent row $D$ and audit row $A$
are read through the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and
$\NameCert$ packet. Audit provenance therefore cannot detach from the recursor
branch route, and no descent certificate may be consumed without the displayed
audit boundary.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorAuditDescentRow}
\begin{proof}
Use the carrier definition together with
\autoref{thm:authorized-generator-recursor-structural-descent-locality}. The
descent row belongs to the same finite tuple as the signature, eliminator,
motive, and branch package, and every recursive read stays on those displayed
coordinates.

Next apply \autoref{thm:authorized-generator-recursor-audit-boundary} and
\autoref{thm:authorized-generator-recursor-output-audit-ledger}. The audit row
is exposed only through the continuation, transport, package, boundary, and
local naming data attached to the same packet.

The non-detachment claim follows from
\autoref{thm:authorized-generator-recursor-hidden-generator-exclusion} and
\autoref{thm:authorized-generator-recursor-public-consumer-boundary}. Since no
hidden generator, omitted branch, or outside output route can cross the public
boundary, the audit row and descent row remain one packet-local route.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor NameCert root unblock package]
\label{thm:authorized-generator-recursor-namecert-root-unblock-package}
The carrier row, branch-totality row, audit-descent row, hidden-generator
exclusion, output audit ledger, and public consumer boundary form the root
$\NameCert_{\AuthorizedGeneratorRecursorUp}$ obligation package for downstream
consumers of the displayed packet
$(I,E,M,B,D,O,A,H,C,P,G,N)$. A consumer may use the authorized recursor output
only through that package.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorNameCertRootUnblockPackage}
\begin{proof}
First combine the carrier definition with
\autoref{thm:authorized-generator-recursor-root-branch-row-totality}. The root
package begins with the displayed packet and the complete branch surface that
precedes public output.

Next use \autoref{thm:authorized-generator-recursor-audit-descent-row} and
\autoref{thm:authorized-generator-recursor-output-audit-ledger}. These bind
structural descent, audit provenance, output exposure, transport, route, and
package provenance into one $\BHist$-local NameCert object.

Finally apply
\autoref{thm:authorized-generator-recursor-hidden-generator-exclusion} and
\autoref{thm:authorized-generator-recursor-public-consumer-boundary}. The
downstream consumer receives exactly the displayed root package, with no
unlisted branch, hidden generator, detached audit row, or outside public-output
route.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor accepted signature cover]
\label{thm:authorized-generator-recursor-accepted-signature-cover}
Every public route exposing the emitted eliminator $E$, checked motive $M$,
branch package $B$, structural descent row $D$, or output name $O$ of an
accepted $\AuthorizedGeneratorRecursorUp$ packet first exposes the accepted
constructor-signature row $I$. Outside the $I,E$ surface, the packet provides
no unlisted constructor signature and no alternate eliminator.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorAcceptedSignatureCover}
\begin{proof}
Project the public carrier through
\autoref{thm:authorized-generator-recursor-namecert-surface}. The displayed
surface lists $I,E,M,B,D,O,A,H,C,P,G,N$ as one finite packet, so the eliminator,
motive, branch, descent, and output rows are not independent public entries.

Apply \autoref{thm:authorized-generator-recursor-branch-exhaustion}. It keeps
all branch reads on the accepted constructor signature and the emitted
eliminator, so a consumer cannot obtain $M,B,D$, or $O$ before the $I,E$ route
has been read.

Finally use
\autoref{thm:authorized-generator-recursor-hidden-generator-exclusion}. The
carrier has no coordinate for a hidden proof-producing generator, unlisted
constructor, or alternate eliminator, so the accepted signature cover exhausts
the public recursor route.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor motive branch totality]
\label{thm:authorized-generator-recursor-motive-branch-totality}
For every accepted $\AuthorizedGeneratorRecursorUp$ packet, the checked motive
row $M$ and branch package row $B$ are consumed together with the structural
descent row $D$. No branch can become public without the motive that types it
and the descent row that authorizes its recursive reads.
\end{theorem}
\begin{proof}
Start with
\autoref{thm:authorized-generator-recursor-structural-descent-locality}. It
places every public recursive read on the same signature, eliminator, motive,
branch package, and structural-descent row.

Next apply
\autoref{thm:authorized-generator-recursor-branch-descent-totality}. The branch
surface is exhaustive only as a package over the checked motive and the descent
row, so no single branch can be projected as a public route apart from $M$ and
$D$.

Use \autoref{thm:authorized-generator-recursor-output-name-stability}. The
output name remains tied to the same $I,E,M,B,D$ route, which prevents a branch
consumer from exporting a detached branch result under a stable public name.
\end{proof}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorMotiveBranchTotality}

\begin{theorem}[AuthorizedGeneratorRecursor GAP boundary refusal]
\label{thm:authorized-generator-recursor-gap-boundary-refusal}
For every accepted $\AuthorizedGeneratorRecursorUp$ packet, the $\mathsf{GAP}$
boundary row $G$ refuses external axioms, hidden proof generators,
non-structural recursion, and unlisted branches. The output name $O$ becomes
public only through the audit row $A$, transports $H$, routes $C$, provenance
$P$, boundary $G$, and local name $N$.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:authorized-generator-recursor-audit-boundary}. It
places public output behind the accepted signature route, checked motive,
complete branch package, structural descent, and audit row.

Apply
\autoref{thm:authorized-generator-recursor-audit-provenance-factorization}. The
audit provenance factors through the displayed transport, route, package, and
boundary rows, so $O$ has no public path that bypasses $A,H,C,P,G,N$.

Next use
\autoref{thm:authorized-generator-recursor-operational-membrane-audit-handoff}.
The operational membrane reads the same finite packet and does not add a
non-structural recursion channel or an unlisted branch.

Finally apply
\autoref{thm:authorized-generator-recursor-hidden-generator-exclusion}. Since
the carrier has no hidden proof-generator coordinate, the $\mathsf{GAP}$ row is
the public refusal boundary for external axioms, hidden generators,
non-structural recursion, and branch escape.
\end{proof}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorGapBoundaryRefusal}

\begin{theorem}[AuthorizedGeneratorRecursor output boundary non-escape]
\label{thm:authorized-generator-recursor-output-boundary-nonescape}
For an accepted $\AuthorizedGeneratorRecursorUp$ packet, the output row $O$ cannot be read apart from the accepted signature $I$, emitted eliminator $E$, checked motive $M$, branch package $B$, structural-descent row $D$, audit row $A$, transports $H$, routes $C$, provenance $P$, boundary row $G$, and local name $N$. The packet exports no detached output, hidden generator, external proof term, or output route outside these displayed rows.
\end{theorem}
\begin{proof}
Start from branch exhaustion and the local NameCert surface. \autoref{thm:authorized-generator-recursor-branch-exhaustion} and \autoref{thm:authorized-generator-recursor-namecert-surface} force the output read to retain the accepted signature, eliminator, motive, complete branch package, descent row, output naming row, audit provenance, routes, package provenance, boundary row, and local certificate.

Next use audit boundary, audit provenance, and output-name stability. \autoref{thm:authorized-generator-recursor-audit-boundary}, \autoref{thm:authorized-generator-recursor-audit-provenance-factorization}, and \autoref{thm:authorized-generator-recursor-output-name-stability} keep $O$ attached to $A,H,C,P,G,N$ before it is exposed.

Finally apply audit-row completeness, branch descent totality, hidden-generator exclusion, and the TasteGate handoff. \autoref{thm:authorized-generator-recursor-audit-row-completeness}, \autoref{thm:authorized-generator-recursor-branch-descent-totality}, \autoref{thm:authorized-generator-recursor-hidden-generator-exclusion}, and \autoref{thm:authorized-generator-recursor-ground-compiler-handoff} leave no coordinate for a detached output or proof-producing generator.
\end{proof}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorOutputBoundaryNonescape}

\begin{theorem}[AuthorizedGeneratorRecursor GAP boundary totality]
\label{thm:authorized-generator-recursor-gap-boundary-totality}
Every host-kernel handoff through the boundary row $G$ of an accepted $\AuthorizedGeneratorRecursorUp$ packet is preceded by the signature row $I$, branch package $B$, structural-descent row $D$, audit row $A$, displayed route row $C$, package row $P$, and local $\NameCert$ row $N$. The $\mathsf{GAP}$ boundary receives only the displayed recursor packet and never receives a hidden branch source, unchecked descent claim, unaudited dependency channel, or proof-producing generator outside the packet.
\end{theorem}
\begin{proof}
Begin with the host-kernel boundary rows. \autoref{thm:authorized-generator-recursor-host-kernel-handoff} sends any $G$-read through the branch, descent, audit, route, and local certificate rows, while \autoref{thm:authorized-generator-recursor-signature-route-totality} attaches the signature route to the same packet.

Use the audit and branch synchronization supplied by the chapter. \autoref{thm:authorized-generator-recursor-audit-provenance-factorization}, \autoref{thm:authorized-generator-recursor-audit-row-completeness}, and \autoref{thm:authorized-generator-recursor-branch-descent-totality} place the audit, package, branch, and descent rows before any boundary exposure.

Finally use hidden-generator exclusion and the TasteGate handoff. \autoref{thm:authorized-generator-recursor-hidden-generator-exclusion} blocks external generators, and \autoref{thm:authorized-generator-recursor-ground-compiler-handoff} reads the checked carrier alignment only as the finite packet handoff. Thus the $G$ boundary is total for the displayed signature, branch, descent, audit, route, package, and naming rows.
\end{proof}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorGapBoundaryTotality}

\begin{theorem}[AuthorizedGeneratorRecursor audit branch synchronization]
\label{thm:authorized-generator-recursor-audit-branch-synchronization}
For every accepted $\AuthorizedGeneratorRecursorUp$ packet, branch exhaustion and audit provenance are synchronized before any public output exposure. A consumer that reads $O$ must first read the complete branch package $B$, structural-descent row $D$, audit row $A$, operational audit handoff, output-name row, transports, routes, provenance, boundary row, and local name together, and it cannot separate the audit ledger from the public branch descent route.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorAuditBranchSynchronization}
\begin{proof}
First synchronize the branch rows. \autoref{thm:authorized-generator-recursor-branch-exhaustion} and \autoref{thm:authorized-generator-recursor-branch-descent-totality} exhaust public recursive reads through the accepted branch package and structural descent row.

Next synchronize the audit rows. \autoref{thm:authorized-generator-recursor-audit-boundary}, \autoref{thm:authorized-generator-recursor-audit-provenance-factorization}, \autoref{thm:authorized-generator-recursor-audit-row-completeness}, and \autoref{thm:authorized-generator-recursor-operational-membrane-audit-handoff} place audit provenance before public exposure of $O$.

Finally attach output naming and the public handoff. \autoref{thm:authorized-generator-recursor-output-name-stability}, \autoref{thm:authorized-generator-recursor-hidden-generator-exclusion}, and \autoref{thm:authorized-generator-recursor-ground-compiler-handoff} prevent a detached output name, hidden generator, or carrier-alignment read from bypassing the synchronized branch and audit rows.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor kernel acceptance scope]
\label{thm:authorized-generator-recursor-kernel-acceptance-scope}
For every accepted $\AuthorizedGeneratorRecursorUp$ packet, any public
host-kernel handoff through the boundary row $G$ is scoped by the displayed
rows $I,E,M,B,D,O,A,H,C,P,N$. The handoff authorizes only the finite carrier
tuple of \autoref{def:authorized-generator-recursor-carrier}; it cannot be read
as an unbounded eliminator principle, a global induction rule, or a public
recursion channel outside those rows.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorKernelAcceptanceScope}
\begin{proof}
Project the carrier tuple
$(I,E,M,B,D,O,A,H,C,P,G,N)$. The rows $I,E,M,B,D$ give the accepted signature,
emitted eliminator, checked motive, branch package, and structural descent
surface, while $O,A,H,C,P,N$ give the named output, audit row, transports,
routes, provenance, and local certificate.

Use branch exhaustion and structural locality. By
\autoref{thm:authorized-generator-recursor-branch-exhaustion} and
\autoref{thm:authorized-generator-recursor-structural-descent-locality}, every
public recursive read stays on the accepted constructor signature, the checked
motive, the branch package, and the structural-descent row.

Attach the audit and output rows. Apply
\autoref{thm:authorized-generator-recursor-audit-boundary} and
\autoref{thm:authorized-generator-recursor-output-name-stability}; public
exposure of $O$ remains behind the audit surface and cannot keep a stable name
after any supporting row is detached.

Finally use hidden-generator exclusion and the ground-compiler handoff.
\autoref{thm:authorized-generator-recursor-hidden-generator-exclusion} and
\autoref{thm:authorized-generator-recursor-ground-compiler-handoff} leave no
carrier coordinate for an unbounded eliminator, global induction rule, or
external recursion source beyond the displayed $G$ handoff.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor branch motive pairing]
\label{thm:authorized-generator-recursor-branch-motive-pairing}
For every accepted $\AuthorizedGeneratorRecursorUp$ packet, the motive row $M$
and branch row $B$ are paired constructor-by-constructor against the accepted
signature row $I$. A downstream recursor consumer may not detach a branch from
the motive that types it or from the constructor case in $I$ that it covers.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorBranchMotivePairing}
\begin{proof}
Project the same carrier tuple and read the $I,M,B,D$ coordinates together.
The accepted signature supplies the constructor cases, the motive supplies the
target family for those cases, the branch row supplies the case data, and the
descent row supplies the legal recursive reads.

Apply branch exhaustion and structural descent locality. By
\autoref{thm:authorized-generator-recursor-branch-exhaustion} and
\autoref{thm:authorized-generator-recursor-structural-descent-locality}, branch
coverage is exhausted only as a package over the accepted constructors and the
checked motive, with recursive reads confined to the displayed descent row.

Use the audit boundary and output-name stability to make the pairing public.
\autoref{thm:authorized-generator-recursor-audit-boundary} places output
exposure behind the same $I,M,B,D,A$ packet, and
\autoref{thm:authorized-generator-recursor-output-name-stability} prevents a
stable public output from retaining $B$ while dropping $M$ or $I$.

Finally cite hidden-generator exclusion and the ground-compiler handoff.
\autoref{thm:authorized-generator-recursor-hidden-generator-exclusion} blocks a
replacement branch source, and
\autoref{thm:authorized-generator-recursor-ground-compiler-handoff} reads the
TasteGate alignment only for the finite packet. Hence each branch remains paired
with its motive and constructor case.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor audit dependency refusal]
\label{thm:authorized-generator-recursor-audit-dependency-refusal}
For every accepted $\AuthorizedGeneratorRecursorUp$ packet, the audit row $A$
refuses external axiom channels and hidden dependency surfaces before the
output row $O$ becomes public. The refusal is the same operational-membrane
route used by \autoref{thm:authorized-generator-recursor-operational-membrane-audit-handoff}
and the same audit boundary used by the displayed packet.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorAuditDependencyRefusal}
\begin{proof}
Project $(I,E,M,B,D,O,A,H,C,P,G,N)$. The audit row is not a detached label: it
is packaged with the accepted signature, emitted eliminator, motive, branch
data, structural descent, transports, routes, provenance, boundary, and local
certificate before $O$ is exposed.

Use branch exhaustion and structural locality first. By
\autoref{thm:authorized-generator-recursor-branch-exhaustion} and
\autoref{thm:authorized-generator-recursor-structural-descent-locality}, any
dependency surface visible to a public branch read is already confined to the
accepted constructors, motive, branch package, and descent row.

Now apply the audit boundary and the operational membrane handoff.
\autoref{thm:authorized-generator-recursor-audit-boundary} and
\autoref{thm:authorized-generator-recursor-operational-membrane-audit-handoff}
place public exposure of $O$ behind the build check, source-axiom refusal,
dependency refusal, and operational-membrane provenance carried by $A$.

Close the dependency refusal with output-name stability, hidden-generator
exclusion, and the ground-compiler handoff.
\autoref{thm:authorized-generator-recursor-output-name-stability},
\autoref{thm:authorized-generator-recursor-hidden-generator-exclusion}, and
\autoref{thm:authorized-generator-recursor-ground-compiler-handoff} prevent a
stable output name, hidden generator, or host-kernel handoff from bypassing the
audited dependency surface.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor branch-ledger disjointness]
\label{thm:authorized-generator-recursor-branch-ledger-disjointness}
For every accepted $\AuthorizedGeneratorRecursorUp$ packet, a row consumed as
branch data in $B$ cannot also be consumed as an audit row in $A$ or as a
structural-descent row in $D$. The public recursor surface keeps branch
coverage, descent reads, and audit provenance as disjoint displayed ledger
roles inside the single carrier.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorBranchLedgerDisjointness}
\begin{proof}
Project the carrier
$(I,E,M,B,D,O,A,H,C,P,G,N)$ of
\autoref{def:authorized-generator-recursor-carrier}. The branch, descent, and
audit coordinates are separately named rows of the finite packet, and their
consumer roles are introduced by the local $\NameCert$ surface rather than by a
shared ambient row.

Use \autoref{thm:authorized-generator-recursor-branch-exhaustion} for the
branch side and
\autoref{thm:authorized-generator-recursor-structural-descent-locality} for
the descent side. These theorems confine branch coverage to the accepted
constructor and motive rows, while descent reads remain tied to the displayed
structural-decrease row.

For the audit side, apply
\autoref{thm:authorized-generator-recursor-audit-boundary} and
\autoref{thm:authorized-generator-recursor-audit-provenance-factorization}.
They expose $A$ only as the provenance and refusal surface behind public
output, not as branch payload or recursive descent data.

Finally
\autoref{thm:authorized-generator-recursor-ground-compiler-handoff} reads the
same separated carrier coordinates at the TasteGate handoff. Thus no public
consumer can reclassify a branch row as audit or descent while retaining an
accepted $\AuthorizedGeneratorRecursorUp$ packet.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor descent-output lock]
\label{thm:authorized-generator-recursor-descent-output-lock}
For every accepted $\AuthorizedGeneratorRecursorUp$ packet, the public output
row $O$ remains locked to the displayed structural-descent row $D$. A consumer
may expose $O$ only through the same finite descent ledger that licensed the
recursive reads used by the packet.
\end{theorem}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorDescentOutputLock}
\begin{proof}
Read $O$ and $D$ from the carrier of
\autoref{def:authorized-generator-recursor-carrier}. The output row is not a
detached result name: it is packaged with the accepted signature, motive,
branch data, structural descent, audit surface, transports, routes,
provenance, and local certificate.

Apply
\autoref{thm:authorized-generator-recursor-structural-descent-locality}. Every
recursive read used by the packet is confined to the displayed structural
descent row $D$.

Next combine
\autoref{thm:authorized-generator-recursor-branch-descent-totality} with
\autoref{thm:authorized-generator-recursor-output-name-stability}. Branch
coverage supplies public output only together with its descent obligations,
and the output name loses stability if the supporting descent row is detached.

The audit boundary and ground-compiler handoff preserve the same lock.
\autoref{thm:authorized-generator-recursor-audit-boundary} keeps public
exposure behind the finite packet, and
\autoref{thm:authorized-generator-recursor-ground-compiler-handoff} transfers
only that packet to the TasteGate surface. Hence $O$ cannot be exposed apart
from the structural descent row $D$.
\end{proof}

\begin{theorem}[AuthorizedGeneratorRecursor audit-provenance cover]
\label{thm:authorized-generator-recursor-audit-provenance-cover}
For every accepted $\AuthorizedGeneratorRecursorUp$ packet, each public
exposure of the output row $O$ consumes the audit provenance rows already
listed in the carrier. No output exposure may cite an external audit source or
omit the displayed provenance factorization.
\end{theorem}
\begin{proof}
Begin with the carrier inventory in
\autoref{def:authorized-generator-recursor-carrier}. The audit row $A$ and
provenance row $P$ are displayed before any public route exposes $O$, and the
local certificate names them as part of the same finite packet.

Apply \autoref{thm:authorized-generator-recursor-audit-boundary}. Public
output exposure is behind the audit boundary, so it must pass through the
displayed audit surface.

Use
\autoref{thm:authorized-generator-recursor-audit-provenance-factorization} to
resolve that surface into the provenance rows already carried by $P$ and $A$.
The operational route of
\autoref{thm:authorized-generator-recursor-operational-membrane-audit-handoff}
then presents the same provenance to the consumer side.

Finally
\autoref{thm:authorized-generator-recursor-ground-compiler-handoff} permits
the ground compiler to read only the finite carrier packet. Therefore every
public exposure of $O$ consumes the listed audit provenance rows and cannot
draw authority from an external audit channel.
\end{proof}
\leanchecked{BEDC.Derived.AuthorizedGeneratorRecursorUp.AuthorizedGeneratorRecursorAuditProvenanceCover}
