\chapter{A Concrete Naming Certificate for $\SierpinskiUniversalCurveUp$}
\label{ch:concrete-instances-sierpinski-universal-curve-namecert}
\origin{human}

Following Sierpinski and continuum theory, the $\SierpinskiUniversalCurveUp$ packet records the planar universal-curve boundary as finite compact-metric and square-refinement NameCert data. It sits after the compact-metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the Peano-continuum interface of \autoref{ch:concrete-instances-peano-continuum-namecert}, the planar retained-square surface of \autoref{ch:concrete-instances-sierpinski-carpet-namecert}, the universal one-dimensional comparison packet of \autoref{ch:concrete-instances-menger-curve-namecert}, and the terminal real readback of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite planar curve boundary for downstream compact-continuum consumers; it does not assert a host hyperspace equality, open-cover compactness, selected branch points, quotient continua, choice, or ambient $\RealUp$ completeness.

\begin{definition}[Sierpinski universal curve carrier]
\label{def:sierpinski-universal-curve-carrier}
A $\SierpinskiUniversalCurveUp$ carrier is a finite $\BHist$ packet
$$
U=(K,P,S,M,G,W,R,H,C,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row supplying compact metric coverage before any planar boundary row is consumed;
\item $P$ is the $\PeanoContinuumUp$ row recording compact connected locally connected continuum data;
\item $S$ is the $\SierpinskiCarpetUp$ planar retained-square sibling row;
\item $M$ is the $\MengerCurveUp$ universal one-dimensional comparison row;
\item $G$ is the finite square-refinement row naming the retained and deleted planar-window stages;
\item $W$ is the planar-window row exposing finite boundary arcs and hole-adjacency reads;
\item $R$ is the $\RealUp$ readback row for the finite coordinate windows used by $G$ and $W$;
\item $H$ records componentwise $\hsame$ transports for the compact, continuum, planar, square-refinement, readback, replay, provenance, and local naming rows;
\item $C$ is the displayed $\Cont$ replay route for extending the finite square-refinement ledger;
\item $Q$ is the $\Pkg$ provenance over $\SierpinskiUniversalCurveUp$, $\CompactMetricUp$, $\PeanoContinuumUp$, $\SierpinskiCarpetUp$, $\MengerCurveUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\SierpinskiUniversalCurveUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,P,S,M,G,W,R,H,C,Q,N$. It has no coordinate for host hyperspace equality, open-cover compactness, selected planar continua, quotient point equality, choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Sierpinski universal curve NameCert obligations]
\label{thm:sierpinski-universal-curve-namecert-obligations}
For every accepted $\SierpinskiUniversalCurveUp$ carrier
$$
U=(K,P,S,M,G,W,R,H,C,Q,N),
$$
the local $\NameCert_{\SierpinskiUniversalCurveUp}$ surface consists exactly of compact-metric exposure through $K$, Peano-continuum exposure through $P$, Sierpinski-carpet planar comparison through $S$, Menger-curve universal comparison through $M$, finite square-refinement exposure through $G$, planar-window boundary exposure through $W$, real readback through $R$, componentwise transport through $H$, displayed continuation replay through $C$, provenance through $Q$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier through \autoref{def:sierpinski-universal-curve-carrier}. The rows $K,P,S,M$ expose the compact, continuum, planar-carpet, and universal-curve sibling data that a consumer may cite.

The finite planar boundary data are held by $G,W,R$. The square-refinement row records retained and deleted stages, the planar-window row records boundary arcs and adjacency reads, and the real readback row seals only those finite coordinate windows.

Finally use $H,C,Q,N$ for transport, replay, source, and local naming. Since no carrier coordinate supplies host hyperspace equality, open-cover compactness, selected branch points, quotient continua, choice, or ambient completeness, those obligations cannot enter the local NameCert surface.
\end{proof}

\begin{theorem}[Sierpinski universal curve planar curve boundary]
\label{thm:sierpinski-universal-curve-planar-curve-boundary}
Every compact-planar-continuum consumer of an accepted $\SierpinskiUniversalCurveUp$ carrier must read the finite route
$$
\CompactMetricUp,\ \PeanoContinuumUp,\ \SierpinskiCarpetUp,\ \MengerCurveUp
\longmapsto
G,W,R
\longmapsto
H,C,Q,N .
$$
The route may use the Sierpinski-carpet sibling as a planar retained-square boundary and the Menger-curve sibling as a universal one-dimensional comparison surface, but it exports no host hyperspace equality, no open-cover compactness theorem, no selected universal embedding, no quotient equality of continua, no choice principle, and no ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Start with the carrier rows $K,P,S,M$. These rows place the packet inside the existing compact metric, Peano continuum, Sierpinski carpet, and Menger curve surfaces without identifying the new packet with any sibling.

Read the planar boundary through $G,W,R$. The square-refinement and planar-window rows are finite $\BHist$ data, and the $\RealUp$ row is only the coordinate readback for those displayed windows.

Transport and replay are then forced through $H,C,Q,N$. The packet has no row for a host hyperspace, an open-cover proof, a selected embedding, quotient continuum equality, choice, or ambient completeness, so a consumer cannot extract any of those principles from the boundary route.
\end{proof}

\falsifiablePrediction{Every accepted $\SierpinskiUniversalCurveUp$ compact-planar-continuum read factors through $\CompactMetricUp$, $\PeanoContinuumUp$, $\SierpinskiCarpetUp$, $\MengerCurveUp$, finite square-refinement rows, planar-window rows, $\RealUp$ readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ before using the curve boundary.}

\independenceWitness{sierpinski-carpet, menger-curve, peano-continuum, compactmetric, real: $\SierpinskiUniversalCurveUp$ is not bijective to any listed sibling because it binds compact metric, continuum, planar retained-square, universal-curve comparison, square-refinement, planar-window, real readback, transport, replay, provenance, and local naming rows into one planar universal-curve certificate.}

\closureat{SierpinskiUniversalCurveUp}{seedStr}

\begin{closurestatus}{\SierpinskiUniversalCurveUp}
  \constructivestory{A $\SierpinskiUniversalCurveUp$ packet is generated from a CompactMetricUp source row, a PeanoContinuumUp row, a SierpinskiCarpetUp planar sibling row, a MengerCurveUp universal comparison row, finite square-refinement rows, planar-window boundary rows, RealUp readback, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:sierpinski-universal-curve-carrier}, \autoref{thm:sierpinski-universal-curve-namecert-obligations}, and \autoref{thm:sierpinski-universal-curve-planar-curve-boundary} seed the compact-planar-continuum route over CompactMetricUp, PeanoContinuumUp, SierpinskiCarpetUp, MengerCurveUp, finite square-refinement, planar-window, RealUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close host hyperspace equality, open-cover compactness, selected universal embeddings, quotient equality of continua, choice, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-metric exposure, continuum exposure, planar sibling exposure, universal-curve comparison, finite square-refinement exposure, planar-window readback, transport, replay, provenance, and local naming for BEDC.Derived.SierpinskiUniversalCurveUp.}
\end{closurestatus}
