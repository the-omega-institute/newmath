\chapter{A Concrete Naming Certificate for $\EffectiveModulusCompactFamilyUp$}
\label{ch:concrete-instances-effective-modulus-compact-family-namecert}
\origin{human}

Following Bishop-style compact metric uniform-continuity arguments, the $\EffectiveModulusCompactFamilyUp$ packet records the finite-net, tolerance-request, covering-number, and explicit modulus rows for a compact family of maps as one NameCert surface. It sits over the compact finite-net source of \autoref{ch:concrete-instances-compactmetric-namecert}, the uniform-continuity request surface of \autoref{ch:concrete-instances-uniformcontinuity-namecert}, the tolerance-only output of \autoref{ch:concrete-instances-uniformmodulus-namecert}, and the covering-budget ledger of \autoref{ch:concrete-instances-finite-metric-entropy-namecert}. The packet is a finite data surface; it is not an open-cover compactness principle, a selected center family, a host function-space compactness theorem, or a choice-selected modulus oracle.

\begin{definition}[Effective modulus compact family carrier]
\label{def:effective-modulus-compact-family-carrier}
An $\EffectiveModulusCompactFamilyUp$ carrier is a finite $\BHist$ packet
$$
E=(K,U,M,Q,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net data row, including displayed centers, cover radii, and located membership witnesses for the compact metric source.
\item $U$ is the $\UniformContinuityUp$ tolerance-request row, listing the target tolerances and the local continuity requests visible to the family consumer.
\item $M$ is the $\UniformModulusUp$ explicit modulus row extracted from the finite compact source and the displayed tolerance requests.
\item $Q$ is the $\FiniteMetricEntropyUp$ covering-number row, recording the finite covering budget used by the same net and tolerance data.
\item $W$ is the family-window row, naming the inspected family members and the finite source windows over which $K,U,M,Q$ are read.
\item $H$ records componentwise $\hsame$ transport for compact finite nets, tolerance requests, explicit modulus rows, covering-number rows, family windows, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a tolerance request through $K,U,Q,W,H,P,N$ to the modulus row $M$.
\item $P$ is the $\Pkg$ provenance over $\EffectiveModulusCompactFamilyUp$, $\CompactMetricUp$, $\UniformContinuityUp$, $\UniformModulusUp$, $\FiniteMetricEntropyUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\EffectiveModulusCompactFamilyUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,U,M,Q,W,H,C,P,N$. It has no coordinate for open-cover compactness, selected centers outside the displayed net, host equality of maps, countable choice over the family, ambient completeness, or a modulus chosen outside the packet.
\end{definition}

\begin{theorem}[Effective modulus compact family NameCert obligations]
\label{thm:effective-modulus-compact-family-namecert-obligations}
For every accepted $\EffectiveModulusCompactFamilyUp$ carrier
$$
E=(K,U,M,Q,W,H,C,P,N),
$$
the local $\NameCert_{\EffectiveModulusCompactFamilyUp}$ surface consists exactly of compact finite-net admission through $K$, uniform-continuity tolerance-request admission through $U$, explicit modulus-row exposure through $M$, finite covering-budget exposure through $Q$, family-window discipline through $W$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:effective-modulus-compact-family-carrier}. Its accepted coordinates are precisely the compact finite-net row, uniform-continuity tolerance row, explicit modulus row, covering-budget row, family-window row, and structural rows.

The analytic rows are read in dependency order. The compact source $K$ supplies the finite-net support, $U$ supplies the tolerance request, $Q$ supplies the finite covering budget attached to the same net, and $M$ records the explicit output row read by the uniform-modulus consumer.

The structural rows $W,H,C,P,N$ keep the family members, transport, replay, provenance, and local name attached to the same finite route. Since the carrier has no coordinate for open-cover compactness, selected centers, host map equality, countable choice, ambient completeness, or an external modulus selector, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Effective modulus compact family finite-net handoff]
\label{thm:effective-modulus-compact-family-finite-net-handoff}
Every downstream $\UniformModulusUp$ read from an accepted $\EffectiveModulusCompactFamilyUp$ carrier factors through the finite route
$$
\CompactMetricUp
\longmapsto
\UniformContinuityUp
\longmapsto
\FiniteMetricEntropyUp
\longmapsto
\UniformModulusUp .
$$
Concretely, the consumer reads the compact finite-net support $K$, the tolerance requests $U$, the covering-number row $Q$, the family-window row $W$, and the structural rows $H,C,P,N$ before it reads the explicit modulus row $M$. The handoff exports no open-cover compactness proof, no selected subcover, no host function-space compactness, no countable-choice schedule, and no modulus outside the displayed packet.
\end{theorem}
\begin{proof}
Begin with an accepted carrier $E=(K,U,M,Q,W,H,C,P,N)$. The compact source $K$ is the only row that supplies finite centers and cover radii, so the handoff starts with displayed finite-net support rather than an open-cover compactness proof.

Read the tolerance and covering rows next. The row $U$ names the target requests, and $Q$ records the finite covering budget for the same source windows; together they determine the finite data from which the explicit modulus row can be consumed.

Finally replay the route through $W,H,C,P,N$ and read $M$. The family-window, transport, replay, provenance, and naming rows preserve the same packet, so a downstream consumer cannot replace the displayed handoff by selected centers, host map equality, a choice schedule, ambient completeness, or an external modulus oracle.
\end{proof}

\closureat{EffectiveModulusCompactFamilyUp}{seedStr}

\begin{closurestatus}{\EffectiveModulusCompactFamilyUp}
  \constructivestory{An $\EffectiveModulusCompactFamilyUp$ packet is generated from CompactMetricUp finite-net data, UniformContinuityUp tolerance requests, UniformModulusUp explicit modulus rows, FiniteMetricEntropyUp covering-number rows, a family-window row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:effective-modulus-compact-family-carrier}, \autoref{thm:effective-modulus-compact-family-namecert-obligations}, and \autoref{thm:effective-modulus-compact-family-finite-net-handoff} seed the compact-family finite-net handoff over explicit modulus and covering-budget rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subcovers, host function-space compactness, countable choice over the family, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net exactness, tolerance-request exactness, explicit modulus-row exposure, covering-budget visibility, family-window discipline, transport, replay, provenance, and local naming for BEDC.Derived.EffectiveModulusCompactFamilyUp.}
\end{closurestatus}
