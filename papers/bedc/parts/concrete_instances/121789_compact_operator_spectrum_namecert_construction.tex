\chapter{A Concrete Naming Certificate for $\CompactOperatorSpectrumUp$}
\label{ch:concrete-instances-compact-operator-spectrum-namecert}
\origin{human}

Following Riesz and Schauder compact-operator spectral theory, the $\CompactOperatorSpectrumUp$ packet records the finite BEDC route by which compact-operator spectral reads are routed through displayed Banach, bounded-linear-operator, compact-operator, spectral-radius, and Fredholm-alternative certificates. It sits over the Banach carrier of \autoref{ch:concrete-instances-banach-namecert}, the bounded operator row of \autoref{ch:concrete-instances-boundedlinearoperator-namecert}, the compactness route of \autoref{ch:concrete-instances-compactoperator-namecert}, the radius ledger of \autoref{ch:concrete-instances-spectral-radius-namecert}, and the Fredholm alternative surface of \autoref{ch:concrete-instances-fredholm-alternative-namecert}. The chapter names the finite spectral-route interface only; it does not close host spectra, selected eigenbases, quotient operator algebras, closed-range complements, or analytic Fredholm theory.

\begin{definition}[Compact-operator spectrum carrier]
\label{def:compact-operator-spectrum-carrier}
A $\CompactOperatorSpectrumUp$ carrier is a finite $\BHist$ packet
$$
S=(B,L,K,R,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\BanachUp$ source row naming the ambient complete normed carrier used by the operator read.
\item $L$ is the $\BoundedLinearOperatorUp$ row naming the bounded operator action and its displayed norm control.
\item $K$ is the $\CompactOperatorUp$ row naming the compact-image or finite-approximation surface for the same operator.
\item $R$ is the $\SpectralRadiusUp$ ledger row recording the finite radius comparison data consumed by spectral-route readers.
\item $F$ is the $\FredholmAlternativeUp$ route row recording the kernel-range and alternative-facing readbacks available to downstream consumers.
\item $H$ records componentwise $\hsame$ transport for Banach, bounded-operator, compact-operator, spectral-radius, Fredholm-route, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
B,L,K,R\longmapsto F .
$$
\item $P$ is the $\Pkg$ provenance over $\CompactOperatorSpectrumUp$, $\BanachUp$, $\BoundedLinearOperatorUp$, $\CompactOperatorUp$, $\SpectralRadiusUp$, $\FredholmAlternativeUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactOperatorSpectrumUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,L,K,R,F,H,C,P,N$. It has no coordinate for a host spectrum, an eigenbasis selector, a quotient operator algebra, a closed-range complement, or an analytic Fredholm theorem.
\end{definition}

\begin{theorem}[Compact-operator spectrum NameCert obligations]
\label{thm:compact-operator-spectrum-namecert-obligations}
For every accepted $\CompactOperatorSpectrumUp$ carrier
$$
S=(B,L,K,R,F,H,C,P,N),
$$
the local $\NameCert_{\CompactOperatorSpectrumUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed Banach source, bounded-linear-operator row, compact-operator row, spectral-radius ledger, Fredholm-route row, transport, replay, provenance, and local naming rows;
\item Banach-source exposure, requiring every operator read to cite the same $\BanachUp$ row $B$ before the bounded action is consumed;
\item bounded-action exactness, requiring every operator application and norm-control read to pass through the $\BoundedLinearOperatorUp$ row $L$;
\item compactness exposure, requiring spectral-route consumers to use the displayed $\CompactOperatorUp$ row $K$ rather than a host compactness predicate;
\item radius-ledger exactness, requiring every spectral-radius comparison to read the $\SpectralRadiusUp$ row $R$;
\item Fredholm-route nonescape, requiring the $\FredholmAlternativeUp$ row $F$ to be consumed only after $B,L,K,R$ have been displayed;
\item componentwise transport, continuation replay, package provenance, and local naming through $H,C,P,N$.
\end{enumerate}
No obligation is discharged by host spectra, selected eigenbases, quotient operator algebras, closed-range complements, or analytic Fredholm theory.
\end{theorem}
\begin{proof}
Project the accepted carrier $S=(B,L,K,R,F,H,C,P,N)$. The visible rows are exactly the Banach source, bounded operator, compact-operator route, spectral-radius ledger, Fredholm-route row, transport, replay, provenance, and local naming data.

The first operator-facing reads are forced through $B$ and $L$. A consumer has no independent bounded action unless it first cites the displayed Banach row and the bounded-linear-operator row.

The compact spectral route is then forced through $K$ and $R$. The compact-operator row supplies the finite compactness surface, while the spectral-radius row supplies the radius comparison ledger.

Finally $F$ is downstream from those rows, and $H,C,P,N$ preserve, replay, source, and name the same finite packet. Since no excluded host object is a coordinate of $S$, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Compact-operator spectrum Fredholm route]
\label{thm:compact-operator-spectrum-fredholm-route}
Every Fredholm-facing consumer of an accepted $\CompactOperatorSpectrumUp$ packet reads the finite route
$$
\BanachUp \longmapsto \BoundedLinearOperatorUp \longmapsto \CompactOperatorUp \longmapsto \SpectralRadiusUp \longmapsto \FredholmAlternativeUp .
$$
The route supplies a displayed compact-operator spectral interface over finite carrier rows; it does not select eigenbases, construct quotient operator algebras, choose closed-range complements, compute host spectra, or invoke analytic Fredholm theory.
\end{theorem}
\begin{proof}
Start with the obligation theorem \autoref{thm:compact-operator-spectrum-namecert-obligations}. It places the Banach row $B$, bounded-linear-operator row $L$, compact-operator row $K$, and spectral-radius ledger $R$ before the Fredholm route $F$.

The sibling certificates provide the stated handoff surface. The Banach chapter supplies the source carrier, the bounded-linear-operator chapter supplies the action row, the compact-operator chapter supplies compactness exposure, and the spectral-radius chapter supplies finite comparison data.

The FredholmAlternative consumer then reads only the displayed route row $F$. Transport, replay, provenance, and local naming preserve the same packet, so the Fredholm-facing surface receives the finite compact-operator spectral interface and none of the excluded analytic endpoints.
\end{proof}

\falsifiablePrediction{A $\CompactOperatorSpectrumUp$ consumer that reaches a Fredholm-facing spectral read without displaying the Banach source, bounded-linear-operator action, compact-operator row, spectral-radius ledger, Fredholm-route row, transport, replay, provenance, and local naming exits the accepted carrier.}

\independenceWitness{banach, bounded-linear-operator, compact-operator, spectral-radius, fredholm-alternative: no listed sibling is bijective with $\CompactOperatorSpectrumUp$ because this packet jointly binds Banach source, bounded action, compactness exposure, spectral-radius comparison, Fredholm route, transport, replay, provenance, and local naming.}

\closureat{CompactOperatorSpectrumUp}{seedStr}

\begin{closurestatus}{\CompactOperatorSpectrumUp}
  \constructivestory{A $\CompactOperatorSpectrumUp$ packet is generated from a Banach source row, BoundedLinearOperator action row, CompactOperator compactness row, SpectralRadius comparison ledger, FredholmAlternative route row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-operator-spectrum-carrier}, \autoref{thm:compact-operator-spectrum-namecert-obligations}, and \autoref{thm:compact-operator-spectrum-fredholm-route} seed the finite compact-operator spectral route over displayed Banach, bounded-operator, compact-operator, spectral-radius, Fredholm, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, host spectra, selected eigenbases, quotient operator algebras, closed-range complements, or analytic Fredholm theory.}
  \upgradepath{Obligation closure requires checked carrier admission, Banach-source exposure, bounded-action exactness, compactness exposure, spectral-radius exactness, Fredholm-route nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactOperatorSpectrumUp.}
\end{closurestatus}
