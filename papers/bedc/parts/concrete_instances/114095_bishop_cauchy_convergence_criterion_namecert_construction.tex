\chapter{A Concrete Naming Certificate for $\BishopCauchyConvergenceCriterionUp$}
\label{ch:concrete-instances-bishop-cauchy-convergence-criterion-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopCauchyConvergenceCriterionUp$ packet records the finite criterion by which a regular rational Cauchy presentation may be read as convergence-ready data before any real-name seal is consumed. It sits over the finite window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a Bishop-style convergence criterion for the RegSeqRat-to-Real route; it is not a quotient class of Cauchy streams, a selected limit, countable choice, host real completeness, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop Cauchy convergence criterion carrier]
\label{def:bishop-cauchy-convergence-criterion-carrier}
A $\BishopCauchyConvergenceCriterionUp$ carrier is a finite $\BHist$ packet
$$
K=(S,Q,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ window row, carrying the inspected finite observation windows and precision requests.
\item $Q$ is the $\RegSeqRatUp$ readback row, naming the regular rational data read from the windows in $S$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, displaying requested radii, split tolerances, comparison budgets, and endpoint observations.
\item $R$ is the convergence-criterion row, recording that the $Q$ readback is controlled by the finite dyadic tolerance surface in $D$ along the windows in $S$.
\item $E$ is the terminal $\RealUp$ seal row, reachable only after $S,Q,D,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the window, readback, tolerance, criterion, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
S \longmapsto Q \longmapsto D \longmapsto R \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\BishopCauchyConvergenceCriterionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopCauchyConvergenceCriterionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,Q,D,R,E,H,C,P,N$. It has no coordinate for quotient stream equality, a selected real limit, countable choice, host completeness, detached convergence, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Bishop Cauchy convergence criterion NameCert obligations]
\label{thm:bishop-cauchy-convergence-criterion-namecert-obligations}
For every accepted $\BishopCauchyConvergenceCriterionUp$ carrier
$$
K=(S,Q,D,R,E,H,C,P,N),
$$
the local $\NameCert_{\BishopCauchyConvergenceCriterionUp}$ surface consists exactly of five finite obligation families:
\begin{enumerate}
\item carrier admission for the StreamName windows, RegSeqRat readback, DyadicRatCore tolerance ledger, convergence-criterion row, Real seal, transport, replay, provenance, and local naming row;
\item window-readback exactness, requiring $Q$ to read only the finite windows displayed by $S$;
\item dyadic tolerance coverage, requiring $D$ to display the requested radii, split tolerances, comparison budgets, and endpoint observations before $R$ is consumed;
\item convergence nonescape, requiring $R$ to bind the same $S,Q,D$ rows rather than a quotient Cauchy class, selected limit, countable-choice functional, or host convergence theorem;
\item Real seal discipline, requiring $E$ to be downstream of $S,Q,D,R$ while $H,C,P,N$ transport, replay, source, and name only the displayed route.
\end{enumerate}
No Bishop Cauchy convergence criterion obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:bishop-cauchy-convergence-criterion-carrier}. The accepted packet displays exactly the window row, regular readback, dyadic tolerance ledger, convergence row, Real seal, and structural rows.

Window-readback exactness is read from $S$ and $Q$. The regular rational row is attached to the finite windows, so no quotient stream representative is introduced.

Dyadic tolerance coverage is the role of $D$. Its requested radii, split tolerances, comparison budgets, and endpoint observations are displayed before the convergence row can be consumed.

The convergence condition is local to $R$. Since $R$ binds the same $S,Q,D$ route, a consumer receives finite criterion data rather than a selected limit, external choice function, or host convergence theorem.

Finally, $E,H,C,P,N$ add only the terminal seal, transport, replay, provenance, and local naming. The carrier has no coordinate for quotient stream equality, countable choice, detached convergence, host completeness, or ambient completeness, so the listed obligation families exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Bishop Cauchy convergence criterion Real handoff]
\label{thm:bishop-cauchy-convergence-criterion-real-handoff}
Every $\RealUp$ or completion-facing consumer of an accepted $\BishopCauchyConvergenceCriterionUp$ packet factors through the finite route
$$
S \longmapsto Q \longmapsto D \longmapsto R \longmapsto E \longmapsto H,C,P,N .
$$
The handoff reads StreamName windows, RegSeqRat regular rational data, DyadicRatCore tolerance coverage, the convergence-criterion row, and the terminal Real seal in that order. It exports no selected limit, quotient equality of Cauchy streams, countable choice, detached convergence theorem, host real completeness, ambient $\RealUp$ completeness, Lean verification, or standard bridge.
\end{theorem}
\begin{proof}
Begin with a real-facing or completion-facing read of an accepted packet. Projection exposes the ordered carrier rows $S,Q,D,R,E,H,C,P,N$.

The readback row $Q$ is downstream of the finite windows $S$, and the dyadic tolerance row $D$ is downstream of the same readback. Thus the consumer cannot start from an unbounded stream representative or a quotient equality class.

The NameCert obligation theorem places the convergence row $R$ after the displayed windows, regular readback, and dyadic tolerance coverage. Hence the Cauchy-to-real route receives convergence criterion data before it can reach $E$.

The terminal row $E$ is the only $\RealUp$ seal coordinate in the packet. The structural rows preserve and name the same finite route, and the excluded principles are absent from the carrier. Therefore every real-facing or completion-facing consumer factors through the displayed handoff and receives none of the listed escape objects.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopCauchyConvergenceCriterionUp$ real-facing read factors through $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance coverage, a convergence-criterion row, a terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{streamname, regseqrat, dyadicratcore, real: $\BishopCauchyConvergenceCriterionUp$ is not bijective to any listed sibling because it jointly carries finite observation windows, regular rational readback, dyadic tolerance coverage, an explicit convergence-criterion row, terminal real sealing, transport, replay, provenance, and local NameCert rows.}

\closureat{BishopCauchyConvergenceCriterionUp}{seedStr}

\begin{closurestatus}{\BishopCauchyConvergenceCriterionUp}
  \constructivestory{A $\BishopCauchyConvergenceCriterionUp$ packet is generated from StreamName windows, RegSeqRat readback, DyadicRatCore tolerance rows, a convergence criterion row, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-cauchy-convergence-criterion-carrier}, \autoref{thm:bishop-cauchy-convergence-criterion-namecert-obligations}, and \autoref{thm:bishop-cauchy-convergence-criterion-real-handoff} seed the finite convergence route through StreamName windows, RegSeqRat readback, DyadicRatCore tolerances, the convergence row, Real seal, transport, continuation, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected limits, quotient equality of Cauchy streams, countable choice, detached convergence, host real completeness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, window-readback exactness, dyadic tolerance coverage, convergence nonescape, Real seal discipline, transport, replay, provenance, and local naming for BEDC.Derived.BishopCauchyConvergenceCriterionUp.}
\end{closurestatus}
