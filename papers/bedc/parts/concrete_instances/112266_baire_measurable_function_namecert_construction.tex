\chapter{A Concrete Naming Certificate for $\BaireMeasurableFunctionUp$}
\label{ch:concrete-instances-baire-measurable-function-namecert}
\origin{human}

Following classical descriptive set theory, the $\BaireMeasurableFunctionUp$ packet records a real-facing Baire-measurable map by finite inverse-image ledgers for Baire-cylinder generators. It sits between the cylinder rows of \autoref{ch:concrete-instances-real-baire-cylinder-namecert}, the pointwise-limit ledger of \autoref{ch:concrete-instances-baireonefunction-namecert}, the prefix source of \autoref{ch:concrete-instances-bairespace-namecert}, and the measurable preimage discipline of \autoref{ch:concrete-instances-measurable-function-namecert}. The packet names generator-level Baire measurability only through displayed preimage rows; it does not add sigma-algebra completion, quotient equality of functions, a selected representative, or a choice principle.

\begin{definition}[BaireMeasurableFunction carrier]
\label{def:baire-measurable-function-carrier}
A $\BaireMeasurableFunctionUp$ carrier is a finite $\BHist$ packet
$$
F=(X,Y,B,G,R,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the source-context row, read through $\BaireSpaceUp$ prefixes or through a real-facing Baire source supplied by $\RealBaireCylinderUp$.
\item $Y$ is the target real-observation row, exposing only displayed real-value windows and Baire-cylinder target events.
\item $B$ is the Baire-cylinder generator ledger, listing the target $\RealBaireCylinderUp$ rows whose inverse images are inspected.
\item $G$ is the graph-observation row tying displayed source windows to displayed target real windows.
\item $R$ is the inverse-image ledger assigning each displayed generator in $B$ to a displayed source event row over $X$.
\item $O$ is the Baire-one handoff row, recording when the same finite preimage route is supplied by a $\BaireOneFunctionUp$ approximation ledger.
\item $H$ records componentwise $\hsame$ transport for the source, target, generator, graph, inverse-image, handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a target generator request through $B,R,X,G,O$.
\item $P$ is the $\Pkg$ provenance over $\BaireMeasurableFunctionUp$, $\RealBaireCylinderUp$, $\BaireOneFunctionUp$, $\BaireSpaceUp$, $\MeasurableFunctionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\BaireMeasurableFunctionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these ten rows. It has no coordinate for a completed sigma algebra, countable union closure beyond displayed generator ledgers, almost-everywhere equality, selected representatives, or ambient function-space equality.
\end{definition}

\begin{theorem}[BaireMeasurableFunction NameCert obligations]
\label{thm:baire-measurable-function-namecert-obligations}
For every accepted $\BaireMeasurableFunctionUp$ carrier $F=(X,Y,B,G,R,O,H,C,P,N)$, the local $\NameCert_{\BaireMeasurableFunctionUp}$ surface consists exactly of carrier admission for $F$, target Baire-cylinder generator admission through $B$, inverse-image ledger exposure through $R$, source-event readback through $X$, graph-observation compatibility through $G$, Baire-one handoff compatibility through $O$, componentwise $\hsame$ stability through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$. Every measurable-map sibling read must factor through the displayed generator-to-preimage route before it may cite $\MeasurableFunctionUp$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:baire-measurable-function-carrier}. The target generators available to the packet are exactly the rows in $B$, and each such row is a finite $\RealBaireCylinderUp$ read rather than an ambient Borel family.

Read the inverse-image ledger $R$. It assigns each displayed target generator to a displayed source event over $X$, while the graph row $G$ supplies the finite observation compatibility between source windows and target real windows.

The Baire-one row $O$ is only a handoff for the same displayed route. It can replay a $\BaireOneFunctionUp$ approximation ledger, but it cannot add a separate function-space representative or a sigma-algebra completion.

Finally replay $H,C,P,N$. These structural rows transport, replay, source, and name the same finite generator ledger. Since no other coordinate is present, the NameCert surface exports no host inverse-image operation, no completion principle, no quotient of functions, and no choice of representatives.
\end{proof}

\begin{theorem}[Baire measurable generator route]
\label{thm:baire-measurable-function-generator-route}
Every accepted target-generator read of a $\BaireMeasurableFunctionUp$ packet factors through the finite route
$$
B \longmapsto R \longmapsto X,G,O \longmapsto H,C,P,N .
$$
Thus the public Baire-measurability claim is an inverse-image ledger for displayed Baire-cylinder generators, not a sigma-algebra completion theorem and not an ambient measurable-function quotient.
\end{theorem}
\begin{proof}
Start with a target request. By \autoref{def:baire-measurable-function-carrier}, the request enters through the displayed generator ledger $B$ and not through a host collection of all Baire sets.

The inverse-image row $R$ then supplies the source-side event row. The source context $X$ and graph row $G$ keep the source and target observations paired, while $O$ records only the Baire-one handoff cases that use the same finite route.

The structural rows $H,C,P,N$ preserve and replay this route. Therefore every public read remains a finite generator-ledger read and cannot export sigma-algebra completion, choice of representatives, almost-everywhere equality, or ambient function equality.
\end{proof}

\falsifiablePrediction{A $\BaireMeasurableFunctionUp$ consumer that claims Baire measurability without displaying target Baire-cylinder generators, inverse-image rows, source-event readback, graph compatibility, componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows refutes the carrier of \autoref{def:baire-measurable-function-carrier}.}

\independenceWitness{realbairecylinder, baireonefunction, bairespace, measurablefunction: $\BaireMeasurableFunctionUp$ is not bijective to any listed sibling because it binds target Baire-cylinder generators, inverse-image ledgers, source prefix rows, graph observations, Baire-one handoff rows, transport, replay, provenance, and local naming in one packet.}

\closureat{BaireMeasurableFunctionUp}{seedStr}

\begin{closurestatus}{\BaireMeasurableFunctionUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:baire-measurable-function-carrier}, \autoref{thm:baire-measurable-function-namecert-obligations}, and \autoref{thm:baire-measurable-function-generator-route} seed the finite Baire-measurable inverse-image ledger over displayed Baire-cylinder generators.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close sigma-algebra completion, quotient equality of functions, almost-everywhere equality, selected representatives, full Borel measurability, bridge checking, or Lean verification.}
  \upgradepath{Obligation closure requires checked carrier admission, Baire-cylinder generator exposure, inverse-image exactness, graph compatibility, Baire-one handoff compatibility, componentwise transport, and measurable-function sibling nonescape for BEDC.Derived.BaireMeasurableFunctionUp.}
  \constructivestory{A $\BaireMeasurableFunctionUp$ packet is generated from source-context, target-observation, Baire-cylinder generator, graph-observation, inverse-image, Baire-one handoff, componentwise hsame, Cont replay, Pkg provenance, and local NameCert rows.}
\end{closurestatus}
