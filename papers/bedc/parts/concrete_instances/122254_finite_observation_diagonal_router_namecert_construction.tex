\chapter{A Concrete Naming Certificate for $\FiniteObservationDiagonalRouterUp$}
\label{ch:concrete-instances-finite-observation-diagonal-router-namecert}
\origin{ai}

The $\FiniteObservationDiagonalRouterUp$ packet realizes the transported stationary diagonal route as finite NameCert data rather than as a completeness principle. It reads a displayed observation window through \autoref{ch:concrete-instances-streamname-namecert}, the regular rational boundary of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the diagonal seal of \autoref{ch:concrete-instances-regular-cauchy-diagonal-seal-namecert}, and the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}. The router records only the finite path by which a diagonal row is inspected; it does not select limits, quotient streams, host real representatives, countable-choice schedules, or ambient $\RealUp$ completeness.

\begin{definition}[Finite observation diagonal router carrier]
\label{def:finite-observation-diagonal-router-carrier}
A $\FiniteObservationDiagonalRouterUp$ carrier is a finite $\BHist$ packet
$$
R=(W,Q,D,\Delta,E,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $W$ is the $\StreamNameUp$ finite observation window for the transported diagonal row.
\item $Q$ is the $\RegSeqRatUp$ readback surface consumed at that window.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger attached to the same observation request.
\item $\Delta$ is the diagonal-routing row, recording the finite transport from the displayed window to the selected diagonal read.
\item $E$ is the $\RegularCauchyDiagonalSealUp$ row that seals the diagonal read before the Real-facing boundary is public.
\item $S$ is the terminal $\RealUp$ seal read only after $W,Q,D,\Delta,E$ remain visible.
\item $H$ records componentwise $\hsame$ transport for the window, readback, dyadic ledger, diagonal route, seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from the observation request through the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\FiniteObservationDiagonalRouterUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RegularCauchyDiagonalSealUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteObservationDiagonalRouterUp}$ row.
\end{enumerate}
The carrier has no coordinate for an infinite diagonal selector, quotient stream equality, selected representatives, countable choice, host real equality, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Finite observation diagonal router NameCert obligations]
\label{thm:finite-observation-diagonal-router-namecert-obligations}
For every accepted $\FiniteObservationDiagonalRouterUp$ carrier
$$
R=(W,Q,D,\Delta,E,S,H,C,P,N),
$$
the local $\NameCert_{\FiniteObservationDiagonalRouterUp}$ surface consists exactly of StreamName window admission through $W$, RegSeqRat readback through $Q$, dyadic tolerance exposure through $D$, finite diagonal routing through $\Delta$, regular Cauchy diagonal sealing through $E$, terminal Real sealing through $S$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, local naming through $N$, and nonescape from infinite diagonal selection, quotient stream equality, selected representatives, countable choice, host real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-observation-diagonal-router-carrier}. The analytic rows are exactly the finite observation window, regular rational readback, dyadic tolerance ledger, diagonal route, diagonal seal, and Real-facing seal.

The diagonal route is downstream of the displayed window. Row $\Delta$ can be inspected only after $W,Q,D$ have been exposed, and row $E$ seals that finite route through the regular Cauchy diagonal seal rather than through an infinite selector.

Finally read $S,H,C,P,N$. The terminal seal, transport, replay, provenance, and local naming preserve the same finite path. Because no carrier coordinate stores quotient stream equality, selected representatives, countable choice, host real equality, or ambient completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Finite observation diagonal router Real handoff]
\label{thm:finite-observation-diagonal-router-real-handoff}
Every Real-facing read from an accepted $\FiniteObservationDiagonalRouterUp$ carrier must expose the $\StreamNameUp$ window $W$, $\RegSeqRatUp$ readback $Q$, $\DyadicRatCoreUp$ tolerance ledger $D$, finite diagonal route $\Delta$, and $\RegularCauchyDiagonalSealUp$ row $E$ before it may consume the terminal $\RealUp$ seal $S$. The handoff is therefore a finite observation route through StreamName, RegSeqRat, DyadicRatCore, RegularCauchyDiagonalSeal, and Real rows, not a proof of Real completeness.
\end{theorem}
\begin{proof}
Start with the observation window. The regular rational readback $Q$ and dyadic ledger $D$ are attached to the same finite request $W$, so no Real-facing row is public before those three rows are visible.

Use the diagonal route $\Delta$ and seal $E$. They route the displayed finite window through the regular Cauchy diagonal seal, keeping the diagonal read as packet data rather than as an ambient limit choice.

The terminal seal $S$ is then consumed with $H,C,P,N$ still attached. Transport, replay, provenance, and local naming preserve the finite route and add no coordinate for quotient stream equality, selected representatives, countable choice, host real equality, or ambient $\RealUp$ completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\FiniteObservationDiagonalRouterUp$ read displays a $\StreamNameUp$ finite window, a $\RegSeqRatUp$ readback row, a $\DyadicRatCoreUp$ tolerance ledger, a finite diagonal-route row, a $\RegularCauchyDiagonalSealUp$ row, a terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{streamname, regseqrat, dyadicratcore, real, regular-cauchy-diagonal-seal: $\FiniteObservationDiagonalRouterUp$ is not bijective to any listed sibling because it jointly carries the finite observation window, diagonal-routing row, diagonal seal, terminal Real seal, and structural NameCert rows in one packet.}

\closureat{FiniteObservationDiagonalRouterUp}{seedStr}

\begin{closurestatus}{\FiniteObservationDiagonalRouterUp}
  \constructivestory{A $\FiniteObservationDiagonalRouterUp$ packet is generated from a StreamName finite observation window, RegSeqRat readback, DyadicRatCore tolerance ledger, finite diagonal-routing row, RegularCauchyDiagonalSeal row, terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-observation-diagonal-router-carrier}, \autoref{thm:finite-observation-diagonal-router-namecert-obligations}, and \autoref{thm:finite-observation-diagonal-router-real-handoff} seed the finite diagonal observation route over StreamName, RegSeqRat, DyadicRatCore, RegularCauchyDiagonalSeal, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, infinite diagonal selection, quotient stream equality, selected representatives, countable choice, host real equality, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, finite observation routing, diagonal seal exactness, Real handoff non-escape, transport stability, replay stability, provenance non-escape, and local naming for BEDC.Derived.FiniteObservationDiagonalRouterUp.}
\end{closurestatus}
