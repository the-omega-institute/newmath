\chapter{A Concrete Naming Certificate for $\CompactPolishSpaceUp$}
\label{ch:concrete-instances-compact-polish-space-namecert}
\origin{human}

Following Alexandrov and Urysohn on Polish spaces and Kelley on compact metric spaces, the $\CompactPolishSpaceUp$ packet records the finite BEDC surface where compact-metric data and complete separable metric data are read as one named object. It sits over the $\mathsf{CompactMetric}^{\uparrow}$ chapter of \autoref{ch:concrete-instances-compactmetric-namecert}, the $\PolishSpaceUp$ chapter of \autoref{ch:concrete-instances-polishspace-namecert}, the $\mathsf{CompleteMetric}^{\uparrow}$ route of \autoref{ch:concrete-instances-completemetric-namecert}, the $\SeparableMetricUp$ route of \autoref{ch:concrete-instances-separablemetric-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, and the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}. The packet is a synthesis node for compact-completion and continuous-map consumers; it is not an open-cover compactness theorem, a host completion, a countable-choice selector, or a quotient of dense names.

\begin{definition}[Compact Polish space carrier]
\label{def:compact-polish-space-carrier}
A $\CompactPolishSpaceUp$ carrier is a finite $\BHist$ packet
$$
X=(K,P,C,S,W,R,H,T,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\mathsf{CompactMetric}^{\uparrow}$ row carrying the finite compact-metric witness, including the metric row, total-boundedness window, and compact-test handoff.
\item $P$ is the $\PolishSpaceUp$ row naming the same metric carrier as a complete separable metric surface.
\item $C$ is the $\mathsf{CompleteMetric}^{\uparrow}$ row through which Cauchy-facing consumers read completeness data before any Real or completion seal is inspected.
\item $S$ is the $\SeparableMetricUp$ row displaying the finite dense-name schedule available to separability consumers.
\item $W$ is the $\StreamNameUp$ observation-window row used by compact-net, dense-name, and completion-facing reads.
\item $R$ is the $\RegSeqRatUp$ regular-rational readback row consumed after the finite stream window has been displayed.
\item $H$ records componentwise $\hsame$ transport for the compact-metric, Polish-space, complete-metric, separable-metric, stream-window, regular-readback, package, and local naming rows.
\item $T$ records displayed $\Cont$ replay from compact-metric witnesses and dense schedules through complete separable readback.
\item $Q$ is the $\Pkg$ provenance over $\CompactPolishSpaceUp$, $\mathsf{CompactMetric}^{\uparrow}$, $\PolishSpaceUp$, $\mathsf{CompleteMetric}^{\uparrow}$, $\SeparableMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactPolishSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for arbitrary open covers, host compactness, host completion, selected dense enumerations, quotient Cauchy classes, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[Compact Polish space NameCert obligations]
\label{thm:compact-polish-space-namecert-obligations}
For every accepted $\CompactPolishSpaceUp$ carrier $X=(K,P,C,S,W,R,H,T,Q,N)$, the local $\NameCert_{\CompactPolishSpaceUp}$ surface consists exactly of compact-metric admission, Polish-space admission, complete-metric handoff, separable-metric handoff, finite stream-window exposure, regular-rational readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every consumer must read the compact side through $K$, the Polish side through $P,C,S$, and the finite observation side through $W,R$ before using the packet as a compact Polish space.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-polish-space-carrier}. The tuple lists the only rows visible to the local classifier, so carrier admission is confined to $K,P,C,S,W,R,H,T,Q,N$.

The compact side is supplied by $K$. It gives finite compact-metric evidence, but no open-cover compactness principle or ambient compact object.

The Polish side is supplied by $P$ through the complete and separable rows $C$ and $S$. Completeness-facing reads therefore use the displayed complete-metric handoff, and separability-facing reads use the displayed dense-name schedule.

The observation and readback rows $W$ and $R$ bind those two sides to the same finite stream window and regular-rational surface. Transport, replay, provenance, and local naming are then componentwise through $H,T,Q,N$, leaving no coordinate for a host completion, countable-choice selector, or quotient dense-name equality.
\end{proof}

\begin{theorem}[Compact Polish finite-cover readback]
\label{thm:compact-polish-space-finite-cover-readback}
Every compact-completion or continuous-map consumer of an accepted $\CompactPolishSpaceUp$ carrier receives the finite route
$$
\mathsf{CompactMetric}^{\uparrow} \longmapsto \PolishSpaceUp \longmapsto \mathsf{CompleteMetric}^{\uparrow},\SeparableMetricUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp
$$
through the displayed rows $K,P,C,S,W,R,H,T,Q,N$. The exported readback is the compact-metric witness together with complete separable metric data and finite regular-name observations; it exports no open-cover compactness theorem, selected host completion, countable-choice dense enumeration, quotient Cauchy class, or ambient completeness row.
\end{theorem}
\begin{proof}
Start from the obligation surface of \autoref{thm:compact-polish-space-namecert-obligations}. A consumer first reads the compact-metric row $K$ and the Polish-space row $P$ as the two named views of the same carrier.

The complete and separable handoffs are then forced through $C$ and $S$. Thus completion-facing reads see a complete-metric row, while dense-name reads see a separable-metric row, and neither read can introduce a host completion or selected dense enumeration.

Finally the finite observation route passes through $W$ and $R$. The structural rows $H,T,Q,N$ preserve the same route by transport, replay, provenance, and local naming, so the displayed rows exhaust the handoff available to compact-completion and continuous-map consumers.
\end{proof}

\begin{theorem}[Compact Polish compact-complete-separable synthesis]
\label{thm:compact-polish-space-compact-complete-separable-synthesis}
Every accepted $\CompactPolishSpaceUp$ consumer that asks for compact-completion or continuous-map data must factor through the single finite route
$$
K,P,C,S,W,R,H,T,Q,N .
$$
Thus compactness, completeness, separability, stream windows, and regular-rational readback are consumed as one carrier surface and cannot be read independently. The route exports no open-cover compactness, host completion, selected dense enumeration, quotient Cauchy class, countable choice, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Project the carrier rows in \autoref{def:compact-polish-space-carrier}. The compact-metric row $K$, Polish-space row $P$, complete-metric row $C$, separable-metric row $S$, stream row $W$, and regular-rational row $R$ are displayed before any structural replay can be used.

Apply the obligation surface of \autoref{thm:compact-polish-space-namecert-obligations}. It identifies the local NameCert read as exactly $K,P,C,S,W,R,H,T,Q,N$, so compactness, completeness, separability, finite observation, and readback are bound to the same packet.

Finally use finite-cover readback from \autoref{thm:compact-polish-space-finite-cover-readback}. Compact-completion and continuous-map consumers therefore follow the displayed route, and no coordinate exists for open covers, host completions, selected dense enumerations, quotient Cauchy classes, countable choice, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactPolishSpaceUp$ consumer read factors through $\mathsf{CompactMetric}^{\uparrow}$, $\PolishSpaceUp$, $\mathsf{CompleteMetric}^{\uparrow}$, $\SeparableMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows before it is used as compact Polish data.}

\independenceWitness{compactmetric, polishspace, completemetric, separablemetric, streamname, regseqrat: $\CompactPolishSpaceUp$ is not bijective to any listed sibling because it binds compact-metric witnesses and complete separable metric rows through one finite observation and local NameCert surface.}

\closureat{CompactPolishSpaceUp}{seedStr}

\begin{closurestatus}{\CompactPolishSpaceUp}
  \constructivestory{A $\CompactPolishSpaceUp$ packet is generated from $\mathsf{CompactMetric}^{\uparrow}$ compact-metric rows, PolishSpaceUp complete separable rows, $\mathsf{CompleteMetric}^{\uparrow}$ handoff rows, SeparableMetricUp dense-name rows, StreamNameUp windows, RegSeqRatUp readback, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-polish-space-carrier}, \autoref{thm:compact-polish-space-namecert-obligations}, and \autoref{thm:compact-polish-space-finite-cover-readback} seed the finite compact Polish synthesis node over compact-metric and complete separable metric data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim arbitrary open-cover compactness, host completion, selected dense enumerations, quotient Cauchy classes, countable choice, ambient real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-metric admission, Polish-space admission, complete-metric handoff, separable-metric handoff, finite observation readback, transport, replay, provenance, and local naming for BEDC.Derived.CompactPolishSpaceUp.}
\end{closurestatus}
