\chapter{A Concrete Naming Certificate for $\GromovCompactnessUp$}
\label{ch:concrete-instances-gromov-compactness-namecert}
\origin{human}

Following Gromov's compactness theorem for families of compact metric spaces, the $\GromovCompactnessUp$ packet records the finite epsilon-net compactness route as NameCert data. It sits beside the compact metric carrier of \autoref{ch:concrete-instances-compactmetric-namecert}, the Gromov--Hausdorff naming surface of \autoref{ch:concrete-instances-gromov-hausdorff-namecert}, and the Gromov--Hausdorff distance packet of \autoref{ch:concrete-instances-gromov-hausdorff-distance-namecert}. The packet packages finite radius ledgers and finite net comparisons; it does not import open-cover compactness, selected subsequences, quotient metric spaces, or a host compactness theorem.

\begin{definition}[Gromov compactness carrier]
\label{def:gromov-compactness-carrier}
A $\GromovCompactnessUp$ carrier is a finite $\BHist$ packet
$$
K=(F,E,N,D,G,H,C,P,L)
$$
whose rows are the family index row $F$, the finite epsilon-scale row $E$, the finite net row $N$, the distance-comparison row $D$, the $\GromovHausdorffUp$ handoff row $G$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and the local $\NameCert_{\GromovCompactnessUp}$ row $L$. The classifier compares accepted packets only through these displayed rows. It has no coordinate for an open-cover compactness theorem, a chosen subsequence, a quotient metric-space construction, a host hyperspace compactness object, or ambient completeness.
\end{definition}

\begin{theorem}[Gromov compactness NameCert obligations]
\label{thm:gromov-compactness-namecert-obligations}
For every accepted $\GromovCompactnessUp$ carrier $K=(F,E,N,D,G,H,C,P,L)$, the local $\NameCert_{\GromovCompactnessUp}$ surface consists exactly of family-index admission through $F$, finite epsilon-scale admission through $E$, finite net admission through $N$, distance-comparison admission through $D$, Gromov--Hausdorff handoff through $G$, transport through $H$, replay through $C$, provenance through $P$, and local naming through $L$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:gromov-compactness-carrier}. The compactness-facing rows are $F,E,N,D,G$, and the structural rows are $H,C,P,L$.

The finite epsilon-scale row $E$ is read before the finite net row $N$ and distance-comparison row $D$. This keeps the compactness route inside inspected finite nets instead of replacing it by an open-cover compactness theorem.

Finally $G,H,C,P,L$ hand the same finite route to the Gromov--Hausdorff and compact metric neighbours, transport it, replay it, source it, and name it. Since no carrier coordinate contains a selected subsequence, quotient metric space, host hyperspace compactness theorem, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Gromov compactness finite epsilon-net route]
\label{thm:gromov-compactness-finite-epsilon-net-route}
Every compactness-facing consumer of an accepted $\GromovCompactnessUp$ carrier receives only the finite route
$$
F,E,N,D,G,H,C,P,L .
$$
The route may be handed to $\CompactMetricUp$, $\GromovHausdorffUp$, or $\PointedGromovHausdorffUp$ consumers as finite epsilon-net evidence with displayed distance comparisons, but it does not export open-cover compactness, selected subsequences, quotient metric spaces, host hyperspace compactness, or ambient completeness.
\end{theorem}
\begin{proof}
Start with the family and epsilon rows $F,E$. They name the inspected family slice and the finite scale ledger before any compactness-facing read is public.

Read the finite net and distance-comparison rows $N,D$ next. These rows provide the finite witnesses consumed by compact metric and Gromov--Hausdorff neighbours, while keeping the route within displayed comparisons.

The handoff and structural rows $G,H,C,P,L$ preserve the same finite route. Therefore a downstream consumer receives finite epsilon-net compactness data, not an open-cover theorem, a selected subsequence, a quotient metric-space object, or an ambient completeness principle.
\end{proof}

\falsifiablePrediction{A $\GromovCompactnessUp$ compactness-facing read that exposes a public conclusion without the displayed family row $F$, epsilon-scale row $E$, finite-net row $N$, distance-comparison row $D$, Gromov--Hausdorff handoff row $G$, transport, replay, provenance, and local naming refutes the carrier surface.}

\independenceWitness{compactmetric, gromov-hausdorff, gromov-hausdorff-distance: no listed sibling is bijective to $\GromovCompactnessUp$ because this packet jointly carries family indices, finite epsilon scales, finite net ledgers, distance comparisons, Gromov--Hausdorff handoff, transport, replay, provenance, and local naming while refusing open-cover compactness and selected subsequences.}

\begin{closurestatus}{\GromovCompactnessUp}
  \constructivestory{A $\GromovCompactnessUp$ packet is generated from family indices, finite epsilon-scale rows, finite net ledgers, distance comparisons, Gromov--Hausdorff handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:gromov-compactness-carrier}, \autoref{thm:gromov-compactness-namecert-obligations}, and \autoref{thm:gromov-compactness-finite-epsilon-net-route} seed the finite epsilon-net compactness route for metric-family consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, subsequence selection, quotient metric-space construction, host hyperspace compactness, Lean verification, or bridge checking for $\GromovCompactnessUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, finite epsilon-scale admission, finite net admission, distance-comparison admission, Gromov--Hausdorff handoff, transport, replay, provenance, and local naming for BEDC.Derived.GromovCompactnessUp.}
\end{closurestatus}
