\chapter{A Concrete Naming Certificate for $\RegularLocatedCutUp$}
\label{ch:concrete-instances-regular-located-cut-namecert}
\origin{human}

Following Bishop and Bridges, the $\RegularLocatedCutUp$ packet records located-cut boundary data as a finite comparison surface for the regular-sequence route to constructive real numbers. It reads dyadic comparison rows through \autoref{ch:concrete-instances-dyadicratcore-namecert}, regular rational representatives through \autoref{ch:concrete-instances-regseqrat-namecert}, located-real boundary language through \autoref{ch:concrete-instances-locatedreal-namecert}, and the completed Real seal through \autoref{ch:concrete-instances-real-namecert}. The packet is a comparison object for the Real and Completion phase, not a construction of $\RealUp$ by Dedekind cuts.

\falsifiablePrediction{A located-cut consumer that compares against $\RegSeqRatUp$ without displaying dyadic brackets and regularity-modulus data is not an accepted $\RegularLocatedCutUp$ packet.}

\independenceWitness{dyadicratcore, regseqrat, locatedreal, real: $\RegularLocatedCutUp$ is not bijective to any one sibling because it binds lower and upper cut rows, dyadic brackets, regularity modulus data, transport, continuation replay, provenance, and local naming into one comparison packet.}

\begin{definition}[Regular located-cut carrier]
\label{def:regular-located-cut-carrier}
A $\RegularLocatedCutUp$ carrier is a finite $\BHist$ packet
$$
L=(A,B,D,M,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the lower cut row, listing the accepted rational lower approximants;
\item $B$ is the upper cut row, listing the accepted rational upper approximants;
\item $D$ is the $\DyadicRatCoreUp$ bracket row that records finite dyadic comparisons between the lower and upper sides;
\item $M$ is the regularity modulus row that routes precision requests to a displayed bracket refinement;
\item $H$ records componentwise $\hsame$ transport for the lower row, upper row, dyadic bracket row, modulus row, replay row, provenance row, and naming row;
\item $C$ records displayed $\Cont$ replay for extending a bracket comparison without changing the finite carrier surface;
\item $P$ is the $\Pkg$ provenance over $\RegularLocatedCutUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\LocatedRealUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RegularLocatedCutUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,B,D,M,H,C,P,N$. It has no coordinate for a host Dedekind cut, quotient equality of cuts, a selected Real representative, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Regular located-cut NameCert obligations]
\label{thm:regular-located-cut-namecert-obligations}
For every accepted $\RegularLocatedCutUp$ carrier
$$
L=(A,B,D,M,H,C,P,N),
$$
the local $\NameCert_{\RegularLocatedCutUp}$ surface consists exactly of lower cut exposure through $A$, upper cut exposure through $B$, dyadic bracket exposure through $D$, regularity-modulus exposure through $M$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-located-cut-carrier}. The displayed tuple lists exactly the lower side, upper side, dyadic bracket, regularity modulus, and structural rows.

Read the analytic rows in comparison order. The lower and upper rows $A,B$ are related only through the finite dyadic brackets $D$, and the modulus row $M$ sends later precision requests back to displayed bracket refinements.

Inspect the structural rows. The rows $H,C,P,N$ transport, replay, source, and name the same finite packet, so no additional cut object or Real-completeness principle can enter the local naming surface.
\end{proof}

\begin{theorem}[Regular located-cut RegSeqRat comparison boundary]
\label{thm:regular-located-cut-regseqrat-boundary}
Every comparison between an accepted $\RegularLocatedCutUp$ packet and the regular-sequence route to $\RealUp$ factors through the finite rows
$$
A,B,D,M,H,C,P,N.
$$
The rows $A$ and $B$ provide located lower and upper approximants, $D$ supplies dyadic brackets, and $M$ supplies the regularity modulus needed to compare with $\RegSeqRatUp$ representatives. This is a comparison boundary between located-cut language and the $\RegSeqRatUp/\RealUp$ route, not a Dedekind-cut construction of $\RealUp$.
\end{theorem}
\begin{proof}
Start from the NameCert obligations of \autoref{thm:regular-located-cut-namecert-obligations}. They place every located lower and upper read inside the rows $A,B$ and require dyadic bracket evidence through $D$.

Use the modulus row $M$ to align a precision request with a finite bracket refinement. A comparison with a $\RegSeqRatUp$ representative therefore reads the displayed bracket data rather than selecting an ambient cut or a host limit.

The structural rows $H,C,P,N$ preserve only that comparison route under transport, continuation replay, provenance, and naming. Since the carrier has no coordinate for a Dedekind construction of $\RealUp$, the theorem records a boundary between languages rather than an alternate construction of the Real seal.
\end{proof}

\begin{closurestatus}{\RegularLocatedCutUp}
  \constructivestory{A $\RegularLocatedCutUp$ packet is generated from lower cut rows, upper cut rows, dyadic bracket rows, regularity-modulus rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-located-cut-carrier}, \autoref{thm:regular-located-cut-namecert-obligations}, and \autoref{thm:regular-located-cut-regseqrat-boundary} declare the located-cut comparison boundary for the RegSeqRat and Real route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a Dedekind-cut construction of $\RealUp$, quotient equality of cuts, selected Real representatives, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, lower-upper bracket obligations, regularity-modulus transport, RegSeqRat comparison boundary, and structural transport for BEDC.Derived.RegularLocatedCutUp.}
\end{closurestatus}
