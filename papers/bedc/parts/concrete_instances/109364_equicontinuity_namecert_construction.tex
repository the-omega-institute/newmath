\chapter{A Concrete Naming Certificate for $\EquicontinuityUp$}
\label{ch:concrete-instances-equicontinuity-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis and Kelley-style compact-metric reasoning, the $\EquicontinuityUp$ packet records the finite family-modulus surface by which a compact source, a finite continuous-family ledger, and a shared tolerance-radius schedule are presented before any finite-net selector or Arzela--Ascoli consumer may read the family. It sits over the compact source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous graph and pointwise-modulus rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the tolerance-only modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}, and the finite-net family route of \autoref{ch:concrete-instances-finite-net-equicontinuity-namecert}.

The packet exposes a shared family modulus. It does not assert compactness, select subsequences, provide a finite net, choose an Arzela--Ascoli limit, or import host-level continuity.

\begin{definition}[Equicontinuity carrier]
\label{def:equicontinuity-carrier}
An $\EquicontinuityUp$ carrier is a finite $\BHist$ packet
$$
E=(K,F,\epsilon,\rho,M,T,R,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact metric source row whose finite-cover and metric inspection data are read through $\CompactMetricUp$.
\item $F$ is the finite continuous-family row, each member of which is presented through $\ContinuousMapUp$ graph and pointwise-modulus data over the same source.
\item $\epsilon$ is the tolerance row consumed by every family member before a radius is exported.
\item $\rho$ is the shared radius row, read as a single family modulus rather than as one radius per function.
\item $M$ is the $\UniformEquicontinuityModulusUp$ or $\UniformModulusUp$ consumer row that records the shared tolerance-radius handoff.
\item $T$ records componentwise $\hsame$ transport for $K,F,\epsilon,\rho,M$.
\item $R$ records displayed $\Cont$ replay from compact-source observations through continuous-family rows into the shared-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformEquicontinuityModulusUp$, $\UniformModulusUp$, $\FiniteNetEquicontinuityUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\EquicontinuityUp}$ row.
\end{enumerate}
The carrier has no coordinate for a selected finite net, selected subsequence, compactness theorem, host continuity predicate, pointwise-only modulus family, or ambient completeness principle.
\end{definition}

\begin{theorem}[Equicontinuity NameCert obligations]
\label{thm:equicontinuity-namecert-obligations}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, the local $\NameCert_{\EquicontinuityUp}$ surface consists exactly of carrier admission for the compact metric source, continuous-family ledger, tolerance row, shared radius row, modulus handoff, transport, replay, provenance, and local naming; shared-modulus exactness requiring the same $\rho$ to serve every member of $F$ at tolerance $\epsilon$; compact-source compatibility requiring any finite-net consumer to read $K$ before using the modulus row; and nonescape excluding compactness assertions, selected finite nets, selected subsequences, host continuity, and ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.EquicontinuityUp.EquicontinuityCarrier\_namecert\_obligations}
\begin{proof}
Carrier admission is projection from \autoref{def:equicontinuity-carrier}. The tuple displays exactly $K,F,\epsilon,\rho,M,T,R,P,N$.

Shared-modulus exactness is the role of $\rho$ and $M$. The radius row is a single displayed row for the family, so a consumer cannot replace it by independently chosen radii for different members of $F$.

Compact-source compatibility is the route through $K$. A finite-net or compact-uniform consumer must read the compact metric source before it can consume the shared family modulus.

Finally, $T,R,P,N$ transport, replay, source, and name the same displayed rows. Since the carrier has no selected net, selected subsequence, compactness theorem, host-continuity predicate, or ambient-completeness coordinate, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Equicontinuity compact-metric route]
\label{thm:equicontinuity-compact-metric-route}
Every compact-uniform or finite-net consumer of an accepted $\EquicontinuityUp$ carrier factors through
$$
K,F,\epsilon,\rho,M \longmapsto T,R,P,N .
$$
The route first reads the compact metric source $K$, then the continuous-family ledger $F$, then the shared tolerance-radius handoff $\epsilon,\rho,M$. It may be consumed by the finite-net equicontinuity surface of \autoref{ch:concrete-instances-finite-net-equicontinuity-namecert}, but it does not itself supply a finite net or compactness proof.
\end{theorem}
\begin{proof}
Project the accepted carrier. The only source coordinate is $K$, so any compact-uniform consumer starts from the displayed compact metric row.

Read the family row $F$ next. Its entries are continuous-map rows over the same source, so the family cannot change source data while the modulus is being consumed.

The shared handoff is $\epsilon,\rho,M$. It records a tolerance and one family radius before a downstream finite-net selector reads the packet. Transport and replay through $T,R$ preserve this order, while $P,N$ merely package and name it.
\end{proof}

\begin{theorem}[Equicontinuity nonescape]
\label{thm:equicontinuity-nonescape}
An accepted $\EquicontinuityUp$ carrier exports only the finite family-modulus rows $K,F,\epsilon,\rho,M,T,R,P,N$. It cannot export compactness, a selected finite net, a selected subsequence, Arzela--Ascoli convergence, host-level continuity, pointwise-only radius choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
The carrier definition lists the entire public inventory. The analytic rows are the compact source, continuous family, tolerance, shared radius, and modulus handoff.

The structural rows transport and replay those same rows. They do not add a selector, a compactness principle, a subsequence object, a convergence row, or an external continuity predicate.

Thus every downstream route must obtain finite nets, compactness conclusions, and convergence statements from separate consumer chapters. The $\EquicontinuityUp$ packet supplies only the displayed family-modulus boundary.
\end{proof}

\begin{theorem}[Equicontinuity shared-radius stability]
\label{thm:equicontinuity-shared-radius-stability}
Let $E=(K,F,\epsilon,\rho,M,T,R,P,N)$ be an accepted $\EquicontinuityUp$ carrier. Any transport of the compact source $K$, finite family row $F$, tolerance $\epsilon$, and modulus handoff $M$ through $T$ preserves the same shared radius row $\rho$. Thus a consumer reading $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, or $\FiniteNetEquicontinuityUp$ through $R,P,N$ cannot replace $\rho$ by pointwise radii depending on members of $F$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:equicontinuity-carrier}. The row $\rho$ is displayed once, between the tolerance row $\epsilon$ and the modulus handoff row $M$.

Transport through $T$ is componentwise on the displayed rows. It may identify the compact source, the family row, the tolerance row, and the handoff row with transported copies, but it has no coordinate for a family-indexed list of radii.

Replay through $R$ and provenance through $P,N$ therefore reread the same shared-radius row. The compact metric, continuous-map, uniform-modulus, and finite-net consumers inherit the single $\rho$ boundary rather than a pointwise-radius surface.
\end{proof}

\begin{theorem}[Equicontinuity compact-source root admission]
\label{thm:equicontinuity-root-unblock-compact-source-admission}
Every accepted $\EquicontinuityUp$ carrier first exposes the compact metric source row $K$ before any shared-radius, finite-net, or Arzela--Ascoli-facing consumer reads $F,\epsilon,\rho,M,T,R,P,N$. The exposed row is the displayed $\CompactMetricUp$ source of \autoref{def:equicontinuity-carrier}, transported by $\hsame$, replayed by $\Cont$, and sourced by the same $\Pkg$ provenance and local $\NameCert$ row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:equicontinuity-carrier}. Its first analytic coordinate is the compact metric source row $K$, and every later row is attached to that source.

Apply the NameCert obligation surface and compact-metric route, \autoref{thm:equicontinuity-namecert-obligations} and \autoref{thm:equicontinuity-compact-metric-route}. They force the finite family, tolerance, shared radius, and modulus handoff to be read only after $K$ has been admitted.

Finally use shared-radius stability. Transport and replay preserve the same $K$ row while $\Pkg$ provenance and local naming expose the packet, so no downstream consumer can bypass the displayed $\CompactMetricUp$ source.
\end{proof}

\begin{theorem}[Equicontinuity family-modulus exactness root admission]
\label{thm:equicontinuity-root-unblock-family-modulus-exactness}
For every accepted $\EquicontinuityUp$ carrier, the shared radius row $\rho$ is the unique family-radius handoff visible to finite-net consumers. It is read after the compact source $K$, finite $\ContinuousMapUp$ family $F$, and tolerance row $\epsilon$, and before the $\UniformModulusUp$ or $\FiniteNetEquicontinuityUp$ consumer surface; no memberwise radius schedule or second modulus row is exported.
\end{theorem}
\begin{proof}
Read $K,F,\epsilon,\rho,M$ from the carrier. The row $\rho$ appears once, between the tolerance request and the modulus handoff.

Apply shared-radius stability and modulus-ledger exactness, \autoref{thm:equicontinuity-shared-radius-stability} and \autoref{thm:equicontinuity-modulus-ledger-exactness}. They identify $\rho$ as the transported shared radius and make the handoff exact for the finite family ledger.

Use finite-net readiness to locate the downstream boundary. A $\FiniteNetEquicontinuityUp$ consumer sees the same $\rho$ through $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, and the nonescape theorem excludes memberwise radius replacement.
\end{proof}

\begin{theorem}[Equicontinuity finite-net consumer boundary root admission]
\label{thm:equicontinuity-root-unblock-finite-net-consumer-boundary}
The $\EquicontinuityUp$ carrier supplies only compact-source, finite-family, tolerance, shared-radius, modulus-handoff, transport, replay, provenance, and local naming rows. Any finite net, compactness output, Arzela--Ascoli convergence, or selected subsequence must be obtained from downstream consumer chapters after reading this boundary, not from $\EquicontinuityUp$ itself.
\end{theorem}
\begin{proof}
Project the carrier and the NameCert obligations. The analytic rows are $K,F,\epsilon,\rho,M$, and the structural rows are $T,R,P,N$.

Apply finite-net readiness and the nonescape theorem, \autoref{thm:equicontinuity-finite-net-readiness} and \autoref{thm:equicontinuity-nonescape}. They place finite-net access after the compact-source and shared-modulus rows while excluding a selected net, subsequence, compactness theorem, or convergence row from the carrier.

Thus finite-net and Arzela--Ascoli consumers may depend on the explicit carrier boundary, but their nets and convergence conclusions must come from downstream chapters.
\end{proof}

\begin{theorem}[Equicontinuity shared-radius carrier root row]
\label{thm:equicontinuity-root-unblock-shared-radius-carrier}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, the shared radius row $\rho$ is exposed as a finite $\BHist$ row between the tolerance request $\epsilon$ and the modulus handoff $M$. The same packet displays the compact source $K$, finite $\ContinuousMapUp$ family $F$, componentwise $\hsame$ transport $T$, $\Cont$ replay $R$, $\Pkg$ provenance $P$, and local $\NameCert$ row $N$, so any $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, or $\FiniteNetEquicontinuityUp$ consumer reads one shared-radius carrier route rather than memberwise radius data.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:equicontinuity-carrier}. The tuple displays exactly $K,F,\epsilon,\rho,M,T,R,P,N$, and $\rho$ appears as one row before the modulus handoff.

Apply the finite-net consumer boundary of \autoref{thm:equicontinuity-root-unblock-finite-net-consumer-boundary}. It keeps every consumer on the compact-source, finite-family, tolerance, shared-radius, and handoff rows before any finite-net-facing read begins.

The support rows $T,R,P,N$ only transport, replay, source, and name the displayed $\BHist$ route. Since no memberwise radius row is present, the root carrier exposes the shared $\rho$ boundary alone.
\end{proof}

\begin{theorem}[Equicontinuity compact-modulus consumer root row]
\label{thm:equicontinuity-root-unblock-compact-modulus-consumer}
Let $E=(K,F,\epsilon,\rho,M,T,R,P,N)$ be an accepted $\EquicontinuityUp$ carrier. A compact-modulus consumer must first read $K$ as the $\CompactMetricUp$ source, then $F$ as the finite $\ContinuousMapUp$ ledger over that source, then $\epsilon,\rho,M$ as the shared $\UniformModulusUp$ handoff, with the route preserved by $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support. The consumer cannot treat $M$ as detached from the compact source or as a selected finite net.
\end{theorem}
\begin{proof}
Read $K$ and $F$ from the accepted carrier. The compact source row precedes the continuous-family ledger, and the family is displayed over that same source.

Use compact-source admission and finite-net readiness, \autoref{thm:equicontinuity-root-unblock-compact-source-admission} and \autoref{thm:equicontinuity-finite-net-readiness}. They order the compact and family reads before $\epsilon,\rho,M$ can be consumed by a uniform-modulus or finite-net-facing reader.

Finally apply nonescape. The support rows preserve the displayed route, while selected finite nets, detached compactness output, subsequences, and convergence rows are absent from the $\EquicontinuityUp$ packet.
\end{proof}

\begin{theorem}[Equicontinuity finite-net handoff root row]
\label{thm:equicontinuity-root-unblock-finite-net-handoff}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, the finite-net handoff is the ordered $\BHist$ route from the $\CompactMetricUp$ source $K$ through the $\ContinuousMapUp$ family $F$ and the shared tolerance-radius rows $\epsilon,\rho,M$ into the $\FiniteNetEquicontinuityUp$ consumer boundary. The $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows $T,R,P,N$ preserve that route and do not add compactness output, selected nets, selected subsequences, or an Arzela--Ascoli limit.
\end{theorem}
\begin{proof}
Project the rows $K,F,\epsilon,\rho,M,T,R,P,N$. The analytic part of the packet already orders the compact source, continuous family, tolerance request, shared radius, and modulus handoff.

Apply the shared-radius carrier row above and the compact-modulus consumer row above. Together they make the finite-net read pass through the same compact source and shared radius before the downstream $\FiniteNetEquicontinuityUp$ boundary is available.

The structural rows transport and replay only this displayed handoff. Because finite nets and convergence objects are not carrier coordinates, the handoff remains a root boundary rather than a selected-net construction.
\end{proof}

\begin{theorem}[Equicontinuity shared-radius obligation]
\label{thm:equicontinuity-shared-radius-obligation}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, the local $\NameCert_{\EquicontinuityUp}$ obligation requires the single radius row $\rho$ to serve every displayed member of $F$ at tolerance $\epsilon$ before any $\FiniteNetEquicontinuityUp$ or Arzela--Ascoli consumer may read the packet. No consumer may replace $\rho$ by memberwise radii, a pointwise-only modulus family, or a selected finite-net radius outside the carrier.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:equicontinuity-carrier}. The family row $F$ is finite and displayed over the compact metric source $K$, while $\rho$ appears as one row between the tolerance $\epsilon$ and the modulus handoff $M$.

Use the compact-metric route of \autoref{thm:equicontinuity-compact-metric-route}. It forces consumers to read $K,F,\epsilon,\rho,M$ before transport, replay, provenance, and local naming can expose the packet.

Apply shared-radius stability and finite-net readiness, \autoref{thm:equicontinuity-shared-radius-stability} and \autoref{thm:equicontinuity-finite-net-readiness}. Together they keep $\rho$ stable under transport and make it available before any finite-net or Arzela--Ascoli-facing read, so pointwise-radius leakage is outside the accepted carrier.
\end{proof}

\begin{theorem}[Equicontinuity finite-net readiness]
\label{thm:equicontinuity-finite-net-readiness}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, a $\FiniteNetEquicontinuityUp$ consumer may begin only after the compact source $K$, the continuous family ledger $F$, the tolerance row $\epsilon$, the shared radius row $\rho$, and the modulus handoff $M$ have all been exposed. The rows $T,R,P,N$ preserve this order and do not themselves select a finite net.
\end{theorem}
\begin{proof}
Read the carrier in the order stated by \autoref{thm:equicontinuity-compact-metric-route}. The compact source $K$ is the only source row, and the family ledger $F$ is the only row naming the continuous maps over that source.

The finite-net consumer then reads $\epsilon,\rho,M$. These rows record a tolerance, a shared radius, and the handoff to the uniform-modulus surface before any finite-net selector can inspect the packet.

The structural rows $T,R,P,N$ transport, replay, source, and name the same displayed route. Since \autoref{def:equicontinuity-carrier} has no finite-net coordinate, readiness for $\FiniteNetEquicontinuityUp$ is a boundary condition, not a selected net.
\end{proof}

\begin{theorem}[Equicontinuity shared-modulus sibling route]
\label{thm:equicontinuity-root-unblock-shared-modulus-scope}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, any downstream consumer of the equicontinuity packet must read the compact source through \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous family through \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform modulus handoff through \autoref{ch:concrete-instances-uniformmodulus-namecert}, and the finite-net boundary through \autoref{ch:concrete-instances-finite-net-equicontinuity-namecert}. The route preserves the same shared radius $\rho$ and exports no selected finite net, subsequence, host continuity theorem, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:equicontinuity-carrier}. The row $K$ is the compact source, $F$ is the finite continuous-family ledger over that source, and $\epsilon,\rho,M$ are the shared tolerance-radius and modulus handoff rows.

Use the compact-source admission and continuous-family admission theorems, \autoref{thm:equicontinuity-compact-source-admission} and \autoref{thm:equicontinuity-continuous-family-admission}. They identify the source and family reads with the sibling surfaces named by \autoref{ch:concrete-instances-compactmetric-namecert} and \autoref{ch:concrete-instances-continuousmap-namecert}.

Apply shared-radius stability, modulus-handoff admission, and finite-net readiness, \autoref{thm:equicontinuity-shared-radius-stability}, \autoref{thm:equicontinuity-modulus-handoff-admission}, and \autoref{thm:equicontinuity-finite-net-readiness}. These rows keep the same $\rho$ attached to the handoff into the $\UniformModulusUp$ and $\FiniteNetEquicontinuityUp$ consumers.

Finally use \autoref{thm:equicontinuity-nonescape}. Since the selected-net, subsequence, host-continuity, and ambient-completeness coordinates are absent from the carrier, the sibling route just listed is the whole public dependency surface.
\end{proof}

\begin{theorem}[Equicontinuity modulus-ledger exactness]
\label{thm:equicontinuity-modulus-ledger-exactness}
Let $E=(K,F,\epsilon,\rho,M,T,R,P,N)$ be accepted. The modulus ledger exported by $\EquicontinuityUp$ is exactly the handoff from the compact source $K$ and finite family $F$ at tolerance $\epsilon$ to the shared radius $\rho$ recorded by $M$, transported by $T$, replayed by $R$, sourced by $P$, and named by $N$. It has no additional ledger row for host continuity, ambient $\RealUp$ completeness, selected covers, or an Arzela--Ascoli limit.
\end{theorem}
\begin{proof}
The analytic part of the carrier is $K,F,\epsilon,\rho,M$. The compact-metric row supplies the source, the continuous-map row supplies the finite family, and the uniform-modulus row records the tolerance-to-radius handoff.

The ledger is exact because $T,R,P,N$ refer back to those same rows. They cannot add a second modulus, a selected cover, a limit candidate, or a host-level continuity predicate; they only preserve and name the displayed handoff.

The exclusions are also forced by \autoref{thm:equicontinuity-nonescape}. Any consumer needing compactness output, selected nets, subsequences, or completeness must obtain those data from a separate chapter after reading the exact modulus ledger here.
\end{proof}

\begin{definition}[Equicontinuity finite-modulus subcarrier]
\label{def:equicontinuity-finite-modulus-carrier}
An $\EquicontinuityUp$ finite-modulus subcarrier is the finite row packet
$$
E_{\mathrm{fin}}=(K,F,\epsilon,\rho,M,G,T,R,P,N)
$$
projected from an accepted carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$. The rows are, in order, the compact metric source $K$, finite continuous-family ledger $F$, shared tolerance request $\epsilon$, shared radius row $\rho$, uniform-modulus handoff $M$, finite-net consumer boundary $G$, componentwise $\hsame$ transport $T$, displayed $\Cont$ replay $R$, $\Pkg$ provenance $P$, and local $\NameCert_{\EquicontinuityUp}$ row $N$. The boundary row $G$ is readable only as readiness for $\FiniteNetEquicontinuityUp$ over the displayed $K,F,\epsilon,\rho,M$ route; it is not a selected net, subsequence, compactness conclusion, or Arzela--Ascoli limit.
\end{definition}

\begin{theorem}[Equicontinuity compact-metric finite-modulus handoff]
\label{thm:equicontinuity-compactmetric-handoff}
Every accepted $\EquicontinuityUp$ finite-modulus subcarrier $E_{\mathrm{fin}}=(K,F,\epsilon,\rho,M,G,T,R,P,N)$ hands downstream consumers a compact-source route only after $K$ has been read as the compact metric source and before $F,\epsilon,\rho,M,G$ are inspected. The rows $T,R,P,N$ preserve this compact-source handoff and cannot replace it by an ambient compactness assertion or selected finite net.
\end{theorem}
\begin{proof}
Project $E_{\mathrm{fin}}$ from \autoref{def:equicontinuity-finite-modulus-carrier}. Its first row is the same compact source $K$ already displayed in \autoref{def:equicontinuity-carrier}.

Apply \autoref{thm:equicontinuity-compact-source-admission} and \autoref{thm:equicontinuity-compact-metric-route}. They force every continuous-family, uniform-modulus, and finite-net-facing read to pass through $K$ before $F,\epsilon,\rho,M$ are consumed.

The boundary row $G$ is therefore only a consumer boundary for the existing route. Transport, replay, provenance, and naming through $T,R,P,N$ preserve that route and add no selected net or ambient compactness coordinate.
\end{proof}

\begin{theorem}[Equicontinuity continuous-map finite-modulus route]
\label{thm:equicontinuity-continuousmap-modulus-route}
For every accepted finite-modulus subcarrier $E_{\mathrm{fin}}=(K,F,\epsilon,\rho,M,G,T,R,P,N)$, the family row $F$ is read only as a finite $\ContinuousMapUp$ ledger over the already displayed compact source $K$. The shared tolerance request $\epsilon$, radius row $\rho$, and modulus handoff $M$ are attached to that same ledger, so no consumer may detach the modulus route into host-level continuity, a source-changing family, or memberwise radii.
\end{theorem}
\begin{proof}
Read the finite family row $F$ after the compact source $K$. By \autoref{def:equicontinuity-finite-modulus-carrier}, these are the same rows projected from the accepted $\EquicontinuityUp$ carrier.

Apply \autoref{thm:equicontinuity-continuous-family-admission} and \autoref{thm:equicontinuity-continuous-family-ledger-admission}. They identify $F$ as the finite $\ContinuousMapUp$ ledger over $K$ and exclude host continuity, source-changing maps, and infinite-family replacement.

Use \autoref{thm:equicontinuity-shared-radius-stability}. The shared radius row $\rho$ is transported with $K,F,\epsilon,M$, so the modulus route remains attached to the same finite family ledger.
\end{proof}

\begin{theorem}[Equicontinuity uniform-modulus consumer boundary]
\label{thm:equicontinuity-uniform-modulus-consumer}
For every accepted finite-modulus subcarrier $E_{\mathrm{fin}}=(K,F,\epsilon,\rho,M,G,T,R,P,N)$, a $\UniformModulusUp$, $\UniformEquicontinuityModulusUp$, or $\FiniteNetEquicontinuityUp$ consumer may read the boundary $G$ only after the shared handoff
$$
K,F,\epsilon,\rho,M
$$
has been displayed. The boundary exports readiness for the finite-net consumer and no selected finite net, subsequence, compactness proof, Arzela--Ascoli limit, quotient equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the handoff rows $K,F,\epsilon,\rho,M$ and the boundary row $G$ from \autoref{def:equicontinuity-finite-modulus-carrier}. The row $G$ appears downstream of the shared tolerance-radius handoff.

Apply \autoref{thm:equicontinuity-finite-net-readiness} and \autoref{thm:equicontinuity-shared-modulus-handoff-preservation}. They require every finite-net-facing consumer to preserve the compact source, family ledger, tolerance request, shared radius, and modulus handoff before reading any consumer boundary.

Finally use \autoref{thm:equicontinuity-modulus-ledger-exactness} and \autoref{thm:equicontinuity-nonescape}. The displayed handoff is the whole exported modulus ledger, and the excluded selected nets, subsequences, compactness conclusions, limits, quotient equalities, and completeness principles are absent from the packet.
\end{proof}

\begin{theorem}[Equicontinuity compact-source admission]
\label{thm:equicontinuity-compact-source-admission}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, the compact source row $K$ is admitted through $\CompactMetricUp$ before any finite-net, shared-radius, or modulus consumer may inspect $F,\epsilon,\rho,M$. No downstream $\FiniteNetEquicontinuityUp$, $\UniformModulusUp$, or Arzela--Ascoli-facing read can bypass $K$ or replace it by an ambient compactness assertion.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:equicontinuity-carrier}. The row $K$ is the only compact-source coordinate, and the introduction fixes it as the same compact metric surface used by \autoref{ch:concrete-instances-compactmetric-namecert}.

Use the compact-metric route and finite-net readiness theorems, \autoref{thm:equicontinuity-compact-metric-route} and \autoref{thm:equicontinuity-finite-net-readiness}. They order every consumer read through $K$ before the family ledger, tolerance, shared radius, and handoff rows.

The nonescape theorem then excludes an ambient compactness replacement. Since selected finite nets and compactness conclusions are absent from the carrier, the admitted compact source is precisely the displayed $\CompactMetricUp$ row.
\end{proof}

\begin{theorem}[Equicontinuity continuous-family admission]
\label{thm:equicontinuity-continuous-family-admission}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, the family row $F$ is a finite ledger of $\ContinuousMapUp$ graph and pointwise-modulus rows over the same source $K$. Transport through $T$ and replay through $R$ preserve this common source, so a consumer cannot read the family as host-level continuity, source-changing maps, or pointwise-only radius data detached from $K$.
\end{theorem}
\begin{proof}
Read $F$ from \autoref{def:equicontinuity-carrier}. Its entries are continuous-map rows over the compact source row $K$, matching the continuous-map sibling cited in the chapter introduction.

Apply shared-radius stability. The theorem keeps $\rho$ attached to the transported compact source, finite family, tolerance, and modulus handoff, so $F$ cannot be split into unrelated pointwise-radius ledgers.

Finally apply nonescape. Host-level continuity and source-changing maps are not coordinates of $E$, and $T,R,P,N$ only transport, replay, source, and name the displayed family ledger.
\end{proof}

\begin{theorem}[Equicontinuity modulus-handoff admission]
\label{thm:equicontinuity-modulus-handoff-admission}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, the shared tolerance-radius row $\epsilon,\rho,M$ is the only $\UniformModulusUp$ or $\UniformEquicontinuityModulusUp$ handoff available to downstream finite-net consumers. A consumer that reads $\FiniteNetEquicontinuityUp$ must receive the handoff through this row and cannot add a second modulus ledger, a memberwise radius schedule, or a selected finite-net radius outside the carrier.
\end{theorem}
\begin{proof}
Project the handoff rows $\epsilon,\rho,M$. The carrier displays one tolerance, one shared radius, and one modulus consumer row.

Use the shared-radius obligation and finite-net readiness theorems. They force every finite-net or Arzela--Ascoli-facing consumer to read $\epsilon,\rho,M$ after $K,F$ and before any finite-net action can begin.

The modulus-ledger exactness theorem identifies this handoff as the whole exported modulus ledger. Since there is no coordinate for a second modulus, a memberwise radius family, or an outside selected radius, the downstream read is confined to the displayed $\UniformModulusUp$ or $\UniformEquicontinuityModulusUp$ handoff.
\end{proof}

\begin{theorem}[Equicontinuity compact-source root lock]
\label{thm:equicontinuity-root-compact-source-lock}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, the root source lock is the compact metric row $K$ together with the transported route $T,R,P,N$. Every $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, or $\FiniteNetEquicontinuityUp$ consumer must read $K$ before the family ledger $F$, tolerance $\epsilon$, shared radius $\rho$, and modulus handoff $M$ become available. The lock exports no selected finite net, subsequence, or Arzela--Ascoli limit.
\end{theorem}
\begin{proof}
Project the carrier. The only source row is $K$, and the rows $F,\epsilon,\rho,M$ are displayed downstream of that source in the finite packet.

Apply compact-source admission and the compact-metric route. These place every continuous-family, uniform-modulus, and finite-net-facing read after $K$ and before the structural rows $T,R,P,N$ can expose the named packet.

Finally use nonescape. Since the carrier has no selected-net, subsequence, or convergence-limit coordinate, the root lock is exactly the displayed compact-source route.
\end{proof}

\begin{theorem}[Equicontinuity family-ledger root lock]
\label{thm:equicontinuity-root-family-ledger-lock}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, the root family ledger is the finite $\ContinuousMapUp$ row $F$ over the already-read compact source $K$. Transport, replay, provenance, and local naming through $T,R,P,N$ preserve that ledger before $\epsilon,\rho,M$ are consumed by $\UniformModulusUp$ or $\FiniteNetEquicontinuityUp$. No consumer may replace $F$ by an infinite family, host-level continuity, selected finite net, subsequence, or Arzela--Ascoli limit.
\end{theorem}
\begin{proof}
Read $K$ and $F$ from the carrier. The row $F$ is finite and its entries are continuous-map rows over the same compact source.

Use continuous-family admission and continuous-family ledger admission. They force the consumer route through the displayed $\ContinuousMapUp$ ledger rather than through host continuity or a source-changing family.

The rows $T,R,P,N$ only preserve, replay, source, and name the same ledger. Nonescape excludes the listed outside objects, so the root lock is the finite family ledger itself.
\end{proof}

\begin{theorem}[Equicontinuity root family modulus ledger]
\label{thm:equicontinuity-root-family-modulus-ledger}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, the root family-modulus ledger exposed to finite-net consumers is exactly the compact-source row $K$, finite continuous-family row $F$, shared tolerance-radius rows $\epsilon,\rho$, modulus handoff $M$, componentwise $\hsame$ transport $T$, displayed $\Cont$ replay $R$, $\Pkg$ provenance $P$, and local $\NameCert_{\EquicontinuityUp}$ row $N$. No consumer may replace this ledger by selected nets, selected subsequences, host compactness, host continuity, a second modulus ledger, or Arzela--Ascoli convergence.
\end{theorem}
\begin{proof}
Project the carrier. The compact-source lock and family-ledger root lock identify $K$ and $F$ as the only source and family rows available before any modulus-facing read.

Apply shared-radius stability, modulus-handoff admission, and modulus-ledger exactness. These place $\epsilon,\rho,M$ after $K,F$ and identify the shared handoff as the whole exported modulus ledger for finite-net consumers.

Finally use transport replay provenance and nonescape. The rows $T,R,P,N$ preserve the displayed ledger without adding a selected net, subsequence, host compactness proof, host continuity theorem, second modulus ledger, or Arzela--Ascoli convergence row.
\end{proof}

\begin{theorem}[Equicontinuity modulus-consumer root lock]
\label{thm:equicontinuity-root-modulus-consumer-lock}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, the root modulus-consumer lock is the shared handoff $\epsilon,\rho,M$ after the compact source $K$ and family ledger $F$ have been displayed. Any $\UniformModulusUp$ or $\FiniteNetEquicontinuityUp$ consumer must preserve this handoff through $T,R,P,N$ and cannot add a selected finite net, memberwise radius schedule, subsequence, or Arzela--Ascoli limit.
\end{theorem}
\begin{proof}
Project the handoff rows. The packet contains one tolerance row, one shared radius row, and one modulus consumer row.

Apply modulus-handoff admission and shared-modulus handoff preservation. These theorems make $\epsilon,\rho,M$ the only modulus handoff available after $K$ and $F$ have been read.

Since the carrier has no coordinate for a selected finite net, memberwise radii, subsequences, or Arzela--Ascoli limits, transport and replay through $T,R,P,N$ preserve only the displayed shared-modulus consumer lock.
\end{proof}

\begin{theorem}[Equicontinuity continuous-family ledger admission]
\label{thm:equicontinuity-continuous-family-ledger-admission}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, the family row $F$ is admitted only as a finite $\ContinuousMapUp$ ledger over the compact source $K$. No consumer may reread $F$ as host-level continuity, a source-changing family, or an infinite family outside the displayed compact-source route.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:equicontinuity-carrier}. It displays $K$ as the compact metric source and $F$ as the finite continuous-family row over that same source.

Use the compact-metric route and continuous-family admission theorems, \autoref{thm:equicontinuity-compact-metric-route} and \autoref{thm:equicontinuity-continuous-family-admission}. They force every consumer to read $K$ before $F$ and keep the family inside the displayed $\ContinuousMapUp$ ledger.

Finally apply \autoref{thm:equicontinuity-nonescape}. The excluded host-continuity, source-changing, and infinite-family coordinates are absent from $E$.
\end{proof}

\begin{theorem}[Equicontinuity shared-modulus handoff preservation]
\label{thm:equicontinuity-shared-modulus-handoff-preservation}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, any $\FiniteNetEquicontinuityUp$ or $\UniformModulusUp$ consumer must preserve the shared tolerance-radius rows $\epsilon,\rho,M$ before reading a finite-net selector. The consumer cannot replace them by memberwise radii, a second modulus ledger, or a selected net radius outside the packet.
\end{theorem}
\begin{proof}
Read the shared-radius stability and finite-net readiness rows, \autoref{thm:equicontinuity-shared-radius-stability} and \autoref{thm:equicontinuity-finite-net-readiness}. They place $\epsilon,\rho,M$ after the compact source and family rows, and before any finite-net-facing read.

Apply modulus-handoff admission and modulus-ledger exactness, \autoref{thm:equicontinuity-modulus-handoff-admission} and \autoref{thm:equicontinuity-modulus-ledger-exactness}. These identify the shared tolerance-radius handoff as the whole exported modulus ledger.

Since the carrier has no selected-net coordinate, preservation of $\epsilon,\rho,M$ is forced before a finite-net selector can be read.
\end{proof}

\begin{theorem}[Equicontinuity transport replay provenance]
\label{thm:equicontinuity-transport-replay-provenance}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, the transport row $T$, replay row $R$, provenance row $P$, and naming row $N$ preserve only the displayed compact-source, continuous-family, tolerance-radius, and modulus-handoff rows. They cannot add pointwise radius choice, selected nets, subsequences, or Real completeness.
\end{theorem}
\begin{proof}
Project $T,R,P,N$ from \autoref{def:equicontinuity-carrier}. Their only analytic inputs are $K,F,\epsilon,\rho,M$.

Use shared-radius stability and modulus-ledger exactness. Transport and replay preserve the same shared radius row and the same modulus ledger rather than a pointwise-radius family.

The nonescape theorem excludes selected nets, subsequences, and ambient $\RealUp$ completeness. Provenance and local naming therefore record the displayed route only.
\end{proof}

\begin{theorem}[Equicontinuity shared-radius family-cover readiness]
\label{thm:equicontinuity-shared-radius-family-cover-readiness}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, the shared-radius family-cover boundary is the displayed route
$$
K,F,\epsilon \longmapsto \rho,M \longmapsto T,R,P,N .
$$
The row $\rho$ is readable only after the compact source $K$, finite continuous-family ledger $F$, and tolerance row $\epsilon$ have been exposed, and every family-cover consumer must keep $M,T,R,P,N$ visible. The route cannot be replaced by selected nets, selected subsequences, host continuity, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier. The rows $K,F,\epsilon$ are the only source, family, and tolerance data available before the shared radius is read.

Use shared-radius stability and shared-modulus handoff preservation. These results place $\rho$ and $M$ after the displayed source-family-tolerance rows and before structural transport and replay.

Finally apply transport replay provenance and nonescape. The rows $T,R,P,N$ preserve the same boundary, and the carrier lacks coordinates for selected nets, selected subsequences, host continuity, and ambient real completeness.
\end{proof}

\begin{theorem}[Equicontinuity compact family-net readiness]
\label{thm:equicontinuity-compact-family-net-readiness}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, a finite-net consumer may read the family only through the compact-source route
$$
K,F,\epsilon,\rho,M \longmapsto T,R,P,N .
$$
The compact family-net boundary consists of the compact metric source $K$, the finite $\ContinuousMapUp$ family row $F$, the tolerance-radius handoff $\epsilon,\rho,M$, and the transport, replay, provenance, and naming rows $T,R,P,N$. It does not choose a net, choose a subsequence, supply host continuity, or invoke ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with continuous-family ledger admission. It admits $F$ only as a finite $\ContinuousMapUp$ ledger over the already displayed compact source $K$.

Next use finite-net readiness and modulus-consumer root lock. They force any finite-net-facing read to pass through $\epsilon,\rho,M$ before the structural rows are replayed.

The rows $T,R,P,N$ do not add a selector. By transport replay provenance and nonescape, the accepted family-net read remains a compact-source ledger route, excluding selected nets, selected subsequences, host continuity, and ambient real completeness.
\end{proof}

\begin{theorem}[Equicontinuity Arzela--Ascoli boundary readiness]
\label{thm:equicontinuity-arzela-ascoli-boundary-readiness}
For every accepted $\EquicontinuityUp$ carrier $E=(K,F,\epsilon,\rho,M,T,R,P,N)$, an Arzela--Ascoli-facing consumer may cite $\EquicontinuityUp$ only as the displayed boundary
$$
K,F,\epsilon,\rho,M,T,R,P,N .
$$
This boundary supplies compact-source admission, finite continuous-family admission, shared tolerance-radius handoff, modulus-consumer readiness, transport, replay, provenance, and local naming. It does not supply a selected subsequence, a selected limiting map, host continuity of the limit, selected nets, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the root family ledger and modulus-consumer root lock. Together they make $K,F,\epsilon,\rho,M$ the whole analytic boundary exposed by the packet.

Use compact family-net readiness. Any later compactness-facing consumer receives only the routed family-net boundary and not a selected net or subsequence.

Finally replay transport, provenance, and nonescape through $T,R,P,N$. Since no coordinate carries a limiting map, host continuity proof, selected subsequence, selected net, or ambient real-completeness proof, an Arzela--Ascoli-facing consumer can cite only the displayed boundary.
\end{proof}

\closureat{EquicontinuityUp}{seedStr}

\begin{closurestatus}{\EquicontinuityUp}
  \constructivestory{An $\EquicontinuityUp$ packet is generated from a compact metric source row, a finite continuous-family ledger, shared tolerance and radius rows, a $\UniformEquicontinuityModulusUp$ or $\UniformModulusUp$ handoff row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:equicontinuity-carrier}, \autoref{thm:equicontinuity-namecert-obligations}, \autoref{thm:equicontinuity-compact-metric-route}, \autoref{thm:equicontinuity-nonescape}, \autoref{thm:equicontinuity-shared-radius-stability}, \autoref{thm:equicontinuity-shared-radius-obligation}, \autoref{thm:equicontinuity-finite-net-readiness}, and \autoref{thm:equicontinuity-modulus-ledger-exactness} seed the compact-source finite-family shared-modulus route over compact-source, continuous-family, tolerance, shared-radius, modulus-handoff, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close compactness, selected finite nets, selected subsequences, Arzela--Ascoli convergence, host-level continuity, pointwise-only radius choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking for $\EquicontinuityUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source admission, continuous-family ledger admission, shared tolerance-radius exactness, modulus handoff, compact-source compatibility, finite-net handoff preservation, transport, replay, provenance, and local naming for BEDC.Derived.EquicontinuityUp.}
\end{closurestatus}
