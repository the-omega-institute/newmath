\chapter{A Concrete Naming Certificate for $\PhysicalTowerRealizationUp$}
\label{ch:concrete-instances-physical-tower-realization-namecert}
\origin{ai}

The $\PhysicalTowerRealizationUp$ packet realizes the approximation-tower and ledgered-compression route of \autoref{sec:approximation-towers-ledgered-compression} as finite NameCert data. It turns approximation levels, compression arrows, visible endpoints, composite tower ledgers, and endpoint descent checks into one displayed carrier, sitting beside the tower-compression packet of \autoref{ch:concrete-instances-reality-constrained-tower-compression-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance surface of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the calculus consumer of \autoref{ch:concrete-instances-calculus-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet records a finite physical approximation tower as a BEDC-native object; it does not identify the physical source with the endpoint, erase hidden ledgers, choose a preferred model, add host physics, or assert ambient $\RealUp$ completeness.

\begin{definition}[Physical tower realization carrier]
\label{def:physical-tower-realization-carrier}
A $\PhysicalTowerRealizationUp$ carrier is a finite $\BHist$ packet
$$
T=(S,L,A,E,D,G,R,V,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source row, naming the finite physical observation history or finite source window from which the tower is read.
\item $L$ is the level row, listing the displayed approximation levels and the classifier bundle visible at each level.
\item $A$ is the compression-arrow row, recording source, pattern, classifier, stability, and local ledger fields for each displayed arrow.
\item $E$ is the endpoint row, naming the final visible object without identifying it with the source row.
\item $D$ is the descent-audit row, recording which endpoint operations descend through the displayed compression classifiers.
\item $G$ is the composite-ledger row, pulling every local ledger in $A$ back to the source row $S$.
\item $R$ is the $\RegSeqRatUp$ readback row for quantitative tolerance, prediction, or comparison entries carried by the tower.
\item $V$ is the visible-result row, exposing only the endpoint, descent, ledger, and readback data that the tower makes public.
\item $H$ records componentwise $\hsame$ transport for source, level, arrow, endpoint, descent, ledger, readback, result, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the finite route
$$
S,L,A \longmapsto E,D,G \longmapsto R \longmapsto V .
$$
\item $P$ is the $\Pkg$ provenance over $\PhysicalTowerRealizationUp$, $\RealityConstrainedTowerCompressionUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CalculusUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\PhysicalTowerRealizationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,L,A,E,D,G,R,V,H,C,P,N$. It has no coordinate for endpoint-source identity, hidden-ledger erasure, a selected best model, host physical law, quotient equality of models, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Physical tower realization NameCert obligations]
\label{thm:physical-tower-realization-namecert-obligations}
For every accepted $\PhysicalTowerRealizationUp$ carrier $T=(S,L,A,E,D,G,R,V,H,C,P,N)$, the local $\NameCert_{\PhysicalTowerRealizationUp}$ surface consists exactly of carrier admission for the source, level, compression-arrow, endpoint, descent-audit, composite-ledger, regular readback, visible-result, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming rows. Every public tower read follows the finite route
$$
S,L,A \longmapsto E,D,G \longmapsto R \longmapsto V
$$
together with $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:physical-tower-realization-carrier}. The source row, level row, and compression-arrow row provide the displayed tower skeleton.

The endpoint row $E$ is downstream from that skeleton. It names the visible endpoint but does not identify it with $S$, because the arrow row $A$ keeps the classifiers and local ledgers visible.

The descent-audit row $D$ and composite-ledger row $G$ are the certification rows. They record which endpoint operations descend through the tower and which source-side residues remain after all compression arrows have been composed.

The readback row $R$ and visible-result row $V$ expose only the finite data already carried by $S,L,A,E,D,G$. Transport, replay, provenance, and local naming preserve that route, so the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Physical tower realization vision handoff]
\label{thm:physical-tower-realization-vision-handoff}
The finite definitions of approximation level, compression arrow, physical approximation tower, visible endpoint, and tower ledger in \autoref{sec:approximation-towers-ledgered-compression} are realized by the rows $L,A,S,E,G$ of any accepted $\PhysicalTowerRealizationUp$ carrier. Endpoint claims are public only after the descent-audit row $D$, composite-ledger row $G$, regular readback row $R$, and visible-result row $V$ have been displayed.
\end{theorem}
\begin{proof}
Read the vision-side tower vocabulary through the carrier rows. Approximation levels are recorded by $L$, compression arrows by $A$, the physical source by $S$, the visible endpoint by $E$, and the tower ledger by $G$.

The endpoint row is not self-certifying. The descent-audit row $D$ records which endpoint operations are licensed by the compression classifiers, and the composite-ledger row $G$ keeps the hidden source residues visible.

The public result is then exposed through $R$ and $V$, with $H,C,P,N$ preserving the same route. Hence the vision route becomes a finite NameCert packet rather than a prose-only modelling claim.
\end{proof}

\begin{theorem}[Physical tower realization nonescape]
\label{thm:physical-tower-realization-nonescape}
The $\PhysicalTowerRealizationUp$ packet exports no endpoint-source identity, no hidden-ledger erasure, no selected preferred model, no host physical law, no quotient equality of models, and no ambient $\RealUp$ completeness. Every public read remains inside the displayed source, level, compression-arrow, endpoint, descent-audit, composite-ledger, regular-readback, visible-result, transport, replay, provenance, and local naming rows.
\end{theorem}
\begin{proof}
Compare the refused objects with the carrier rows of \autoref{def:physical-tower-realization-carrier}. None is a coordinate of $S,L,A,E,D,G,R,V,H,C,P,N$.

The positive public route is supplied by \autoref{thm:physical-tower-realization-vision-handoff}. It keeps endpoint claims downstream from the source, level, arrow, descent-audit, composite-ledger, and readback rows.

The structural rows $H,C,P,N$ preserve only that finite route. Therefore a consumer cannot obtain an erased ledger, privileged model selector, host physics law, quotient model equality, or ambient real-completeness principle from the packet.
\end{proof}

\falsifiablePrediction{An accepted $\PhysicalTowerRealizationUp$ read that exposes an endpoint result without first displaying source, level, compression-arrow, descent-audit, composite-ledger, $\RegSeqRatUp$ readback, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows refutes the carrier of \autoref{def:physical-tower-realization-carrier}.}

\independenceWitness{reality-constrained-tower-compression, compression-ledger-functor, compression-descent-audit, towerendpointreflection, calculus: $\PhysicalTowerRealizationUp$ is not bijective with any listed sibling because it jointly carries a physical source row, approximation levels, compression arrows, endpoint, descent audit, composite ledger, quantitative readback, visible result, transport, replay, provenance, and local naming in one finite packet.}

The later Lean TasteGate file for this route must include explicit $\mathsf{Nontrivial}\ \PhysicalTowerRealizationUp$ and $\mathsf{FieldFaithful}\ \PhysicalTowerRealizationUp$ instances whose fields project $S,L,A,E,D,G,R,V,H,C,P,N$ separately.

\closureat{PhysicalTowerRealizationUp}{seedStr}

\begin{closurestatus}{\PhysicalTowerRealizationUp}
  \constructivestory{A $\PhysicalTowerRealizationUp$ packet is generated from finite source, approximation-level, compression-arrow, endpoint, descent-audit, composite-ledger, RegSeqRat readback, visible-result, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:physical-tower-realization-carrier}, \autoref{thm:physical-tower-realization-namecert-obligations}, \autoref{thm:physical-tower-realization-vision-handoff}, and \autoref{thm:physical-tower-realization-nonescape} seed the finite approximation-tower realization over source, level, compression-arrow, endpoint, descent, ledger, readback, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close endpoint-source identity, hidden-ledger erasure, preferred-model selection, host physical law, quotient model equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, approximation-level exposure, compression-arrow ledger exactness, endpoint nonidentity, descent-audit routing, composite-ledger readback, visible-result nonescape, transport, replay, provenance, local naming, and TasteGate Nontrivial and FieldFaithful instances for BEDC.Derived.PhysicalTowerRealizationUp.}
\end{closurestatus}
