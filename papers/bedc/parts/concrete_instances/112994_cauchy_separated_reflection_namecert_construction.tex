\chapter{A Concrete Naming Certificate for $\CauchySeparatedReflectionUp$}
\label{ch:concrete-instances-cauchy-separated-reflection-namecert}
\origin{human}

Following Bishop and Bridges on constructive Cauchy presentations, the $\CauchySeparatedReflectionUp$ packet records the finite separated-reflection surface needed before regular Cauchy names are consumed as separated real data. It sits over the precompletion source of \autoref{ch:concrete-instances-cauchy-precompletion-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the metric source of \autoref{ch:concrete-instances-metric-namecert}, the separated-reflection boundary of \autoref{ch:concrete-instances-separated-reflection-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite NameCert route from Cauchy precompletion data to separated real reads; it is not quotient representative data, countable choice, host equality, or ambient $\RealUp$ completeness.

\begin{definition}[Cauchy separated-reflection carrier]
\label{def:cauchy-separated-reflection-carrier}
A $\CauchySeparatedReflectionUp$ carrier is a finite $\BHist$ packet
$$
S=(A,R,M,Z,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the $\CauchyPrecompletionUp$ source row, carrying the finite Cauchy presentation, precompletion admission, and tolerance requests visible before reflection.
\item $R$ is the $\RegSeqRatUp$ readback row assembled from the same Cauchy windows and tolerance ledger.
\item $M$ is the $\MetricSpaceUp$ source row through which the distance comparisons used by the reflection request are read.
\item $Z$ is the $\SeparatedReflectionUp$ row, recording the zero-distance classifier boundary before any separated real read is public.
\item $E$ is the $\RealUp$ seal reached only after $A,R,M,Z$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the precompletion source, regular readback, metric source, separated-reflection boundary, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
\CauchyPrecompletionUp,\ \RegSeqRatUp,\ \MetricSpaceUp,\ \SeparatedReflectionUp \longmapsto \RealUp .
$$
\item $P$ is the $\Pkg$ provenance over $\CauchySeparatedReflectionUp$, $\CauchyPrecompletionUp$, $\RegSeqRatUp$, $\MetricSpaceUp$, $\SeparatedReflectionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchySeparatedReflectionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,R,M,Z,E,H,C,P,N$. It has no coordinate for quotient representatives, countable choice, host equality, selected limits, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Cauchy separated-reflection classifier stability]
\label{thm:cauchy-separated-reflection-classifier-stability}
For every accepted $\CauchySeparatedReflectionUp$ carrier $S=(A,R,M,Z,E,H,C,P,N)$, classifier transport along the displayed separated-reflection route preserves the finite Cauchy-to-real surface. A consumer may read the separated $\RealUp$ boundary only after the $\CauchyPrecompletionUp$ source $A$, $\RegSeqRatUp$ readback $R$, $\MetricSpaceUp$ distance row $M$, $\SeparatedReflectionUp$ classifier row $Z$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert$ row $N$ have all been exposed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-separated-reflection-carrier}. The visible data are exactly $A,R,M,Z,E,H,C,P,N$, so every reflected Cauchy read starts from the displayed precompletion source and regular rational readback.

The separated boundary is then read through $M$ and $Z$. The metric row supplies the distance comparisons used by the zero-distance classifier, while the separated-reflection row records the only route by which indistinguishable Cauchy data can reach a separated consumer.

Transport and replay preserve this same route. The row $H$ compares the source, readback, metric, reflection, and real-seal rows componentwise, and $C$ repeats the path from Cauchy precompletion data through separated reflection to the $\RealUp$ seal.

Because quotient representatives, countable choice, host equality, selected limits, and ambient real completeness have no carrier coordinate, classifier stability is exhausted by the displayed rows. No consumer can read a separated real boundary outside $A,R,M,Z,E,H,C,P,N$.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchySeparatedReflectionUp$ consumer that reaches a separated $\RealUp$ read while omitting the CauchyPrecompletion source, RegSeqRat readback, MetricSpace distance row, SeparatedReflection classifier row, hsame transport, Cont replay, Pkg provenance, or local NameCert row refutes the packet.}

\independenceWitness{cauchyprecompletion, regseqrat, metricspace, separated-reflection, real: $\CauchySeparatedReflectionUp$ is not bijective to any listed sibling because it jointly carries the Cauchy source, regular readback, metric source, separated-reflection boundary, real seal, transport, replay, provenance, and local naming in one finite packet.}

\closureat{CauchySeparatedReflectionUp}{seedStr}

\begin{closurestatus}{\CauchySeparatedReflectionUp}
  \constructivestory{A $\CauchySeparatedReflectionUp$ packet is generated from a CauchyPrecompletion source row, RegSeqRat readback, MetricSpace distance row, SeparatedReflection classifier boundary, Real seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-separated-reflection-carrier} and \autoref{thm:cauchy-separated-reflection-classifier-stability} seed the finite separated-reflection route from Cauchy presentation data to separated Real reads.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient representatives, countable choice, host equality, selected limits, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, regular readback exactness, metric-source exposure, separated-reflection stability, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CauchySeparatedReflectionUp.}
\end{closurestatus}
