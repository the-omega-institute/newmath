\chapter{A Concrete Naming Certificate for $\SequentialBolzanoChoiceFreeSubsequenceUp$}
\label{ch:concrete-instances-sequential-bolzano-choice-free-subsequence-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\SequentialBolzanoChoiceFreeSubsequenceUp$ packet records the finite subsequence-extraction surface used by sequential Bolzano arguments before Real and Completion consumers read a regular Cauchy subsequence. It sits after the dyadic tolerance carrier of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-name schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, and the diagonal selector route of \autoref{ch:concrete-instances-choicefreediagonalselector-namecert}. The packet names a displayed subsequence route; it is not open-cover compactness, countable choice, a selected host limit, a quotient stream, or ambient $\RealUp$ completeness.

\begin{definition}[Sequential Bolzano choice-free subsequence carrier]
\label{def:sequential-bolzano-choice-free-subsequence-carrier}
A $\SequentialBolzanoChoiceFreeSubsequenceUp$ carrier is a finite $\BHist$ packet
$$
B=(S,Q,D,W,R,E,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source sequence row, exposed only through inspected finite $\StreamNameUp$ windows.
\item $Q$ is the finite subsequence schedule row, listing the inspected source positions and their monotone order inside those windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used to compare source windows, selected windows, and Cauchy-tail requests.
\item $W$ is the $\StreamNameUp$ window family opened by the selected schedule.
\item $R$ is the $\RegSeqRatUp$ readback row assembled from the selected windows and tolerance ledger.
\item $E$ is the terminal $\RealUp$ seal row available only after $Q,D,W,R$ have been displayed.
\item $A$ is the $\ChoiceFreeDiagonalSelectorUp$ selector row that binds the precision request to the displayed subsequence window rather than to a host choice function.
\item $H$ records componentwise $\hsame$ transport for the source, schedule, tolerance, window, readback, seal, selector, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a sequential Bolzano request through $S,Q,D,W,R,E,A,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\SequentialBolzanoChoiceFreeSubsequenceUp$, $\ChoiceFreeDiagonalSelectorUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SequentialBolzanoChoiceFreeSubsequenceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows. It has no coordinate for open-cover compactness, countable choice, a selected host limit, quotient stream equality, host sequence equality, or ambient Real completeness.
\end{definition}

\begin{theorem}[Sequential Bolzano choice-free subsequence NameCert obligations]
\label{thm:sequential-bolzano-choice-free-subsequence-namecert-obligations}
For every accepted $\SequentialBolzanoChoiceFreeSubsequenceUp$ carrier
$$
B=(S,Q,D,W,R,E,A,H,C,P,N),
$$
the local $\NameCert_{\SequentialBolzanoChoiceFreeSubsequenceUp}$ surface consists exactly of carrier admission, finite source-window exposure, subsequence schedule visibility, dyadic tolerance accounting, StreamName window replay, RegSeqRat readback, Real seal handoff, ChoiceFreeDiagonalSelector routing, componentwise transport, displayed continuation replay, package provenance, and local naming. Every sequential Bolzano or Real-completion read must pass through the displayed rows before it consumes any subsequence output.
\end{theorem}
\begin{proof}
Project the carrier. The analytic rows are $S,Q,D,W,R,E,A$, and the structural rows are $H,C,P,N$.

The source row $S$ is exposed only by finite StreamName windows. The schedule row $Q$ selects inspected positions inside those windows, while $D$ records the dyadic tolerance budget and $R$ records the regular rational readback.

The selector row $A$ imports the choice-free diagonal selector boundary of \autoref{ch:concrete-instances-choicefreediagonalselector-namecert}. It binds precision requests to displayed finite windows rather than to an external choice function.

Finally $E$ is downstream of $Q,D,W,R,A$, and $H,C,P,N$ only transport, replay, source, and name the same route. Since no carrier coordinate stores open-cover compactness, countable choice, selected host limits, quotient streams, host sequence equality, or ambient completeness, the displayed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Sequential Bolzano choice-free real route]
\label{thm:sequential-bolzano-choice-free-subsequence-real-route}
Every Real and Completion consumer of an accepted $\SequentialBolzanoChoiceFreeSubsequenceUp$ packet factors through
$$
\ChoiceFreeDiagonalSelectorUp\longmapsto\StreamNameUp\longmapsto\DyadicRatCoreUp\longmapsto\RegSeqRatUp\longmapsto\RealUp
$$
as displayed carrier rows before any subsequence output is public. The route exposes the selector, finite windows, dyadic tolerance ledger, regular rational readback, Real seal, transport, replay, provenance, and local naming; it exports no open-cover compactness, countable-choice schedule, selected host limit, quotient stream, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:sequential-bolzano-choice-free-subsequence-namecert-obligations}. They force the selected subsequence schedule and tolerance ledger to be displayed before the readback and seal rows can be consumed.

Use the sibling selector route of \autoref{ch:concrete-instances-choicefreediagonalselector-namecert}. The selector row sends each precision request to a finite StreamName window, so the consumer reads $A,W,D,R,E$ as one displayed route.

The RegSeqRat and Real rows are downstream of that route. The readback row $R$ is assembled from the selected windows and dyadic tolerance ledger, and the seal row $E$ is available only after $R$ has been displayed.

Thus a Real or Completion consumer receives only the finite route through $\ChoiceFreeDiagonalSelectorUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$. The excluded compactness, choice, host-limit, quotient-stream, and ambient-completeness objects are absent from the carrier.
\end{proof}

\closureat{SequentialBolzanoChoiceFreeSubsequenceUp}{seedStr}

\begin{closurestatus}{\SequentialBolzanoChoiceFreeSubsequenceUp}
  \constructivestory{A $\SequentialBolzanoChoiceFreeSubsequenceUp$ packet is generated from source sequence rows, finite subsequence schedules, DyadicRatCore tolerance ledgers, StreamName windows, RegSeqRat readbacks, RealUp seals, a ChoiceFreeDiagonalSelector row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:sequential-bolzano-choice-free-subsequence-carrier}, \autoref{thm:sequential-bolzano-choice-free-subsequence-namecert-obligations}, and \autoref{thm:sequential-bolzano-choice-free-subsequence-real-route} seed the choice-free sequential Bolzano subsequence route for Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, countable choice, selected host limits, quotient streams, ambient $\RealUp$ completeness, Lean theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite subsequence scheduling, dyadic tolerance accounting, StreamName window replay, RegSeqRat readback, Real seal routing, ChoiceFreeDiagonalSelector handoff, transport, replay, provenance, and local naming for BEDC.Derived.SequentialBolzanoChoiceFreeSubsequenceUp.}
\end{closurestatus}
