\chapter{A Concrete Naming Certificate for $\ChernoffFiniteExponentialBoundUp$}
\label{ch:concrete-instances-chernoff-finite-exponential-bound-namecert}
\origin{human}

Following Chernoff's finite exponential-bound method, the $\ChernoffFiniteExponentialBoundUp$ packet records the finite moment-generating-function route by which an exponential Markov row bounds a displayed tail event. It sits after the probability source of \autoref{ch:concrete-instances-probspace-namecert}, the random-variable rows of \autoref{ch:concrete-instances-randomvar-namecert}, the law and range summaries of \autoref{ch:concrete-instances-distribution-namecert}, the finite independence ledger of \autoref{ch:concrete-instances-independence-namecert}, the dyadic exponential arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, and the bounded finite-sum concentration surface of \autoref{ch:concrete-instances-hoeffding-finite-sum-namecert}. The packet names only the finite MGF and exponential Markov route; it is not an asymptotic optimizer, countable-choice scheme, ambient measure-completion theorem, quotient event-equality principle, or ambient $\RealUp$ completeness result.

\begin{definition}[Chernoff finite exponential-bound carrier]
\label{def:chernoff-finite-exponential-bound-carrier}
A $\ChernoffFiniteExponentialBoundUp$ carrier is a finite $\BHist$ packet
$$
C=(P,X,D,I,\lambda,M,A,E,R,H,T,Q,N)
$$
with the following visible rows:
\begin{enumerate}
\item $P$ is the source $\ProbSpaceUp$ row carrying the event classifier and total-mass normalization;
\item $X$ is the finite $\RandomVarUp$ family whose displayed tail event is inspected;
\item $D$ is the $\DistributionUp$ row exposing the finite law or range summary needed for each exponential moment;
\item $I$ is the finite $\IndependenceUp$ ledger used only when the MGF row is factorized over the displayed family;
\item $\lambda$ is the positive dyadic or real parameter row admitted for the exponential Markov read;
\item $M$ is the finite MGF row, recording each displayed expectation of $\exp(\lambda X_i)$ or the single displayed expectation used by the packet;
\item $A$ is the exponential Markov comparison row identifying the tail event with the exponential superlevel event;
\item $E$ is the $\DyadicRatCoreUp$ arithmetic row naming the exponential numerator, denominator, product, and slack comparison used in the bound;
\item $R$ is the terminal $\RealUp$ seal for the resulting probability bound;
\item $H$ records componentwise $\hsame$ transport for the probability, random-variable, distribution, independence, MGF, dyadic, and real rows;
\item $T$ records the displayed $\Cont$ replay of the finite exponential-bound route;
\item $Q$ is the $\Pkg$ provenance over $\ProbSpaceUp$, $\RandomVarUp$, $\DistributionUp$, $\IndependenceUp$, $\DyadicRatCoreUp$, $\RealUp$, $\HoeffdingFiniteSumUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\ChernoffFiniteExponentialBoundUp}$ row.
\end{enumerate}
The local classifier compares accepted carriers only through these displayed rows. It has no coordinate for an asymptotic limiting parameter, a selected optimizing $\lambda$, countable products, completed measure algebras, quotient-level event equality, or an ambient real-completeness oracle.
\end{definition}

\begin{theorem}[Chernoff finite exponential-bound NameCert obligations]
\label{thm:chernoff-finite-exponential-bound-namecert-obligations}
For every accepted $\ChernoffFiniteExponentialBoundUp$ carrier $C=(P,X,D,I,\lambda,M,A,E,R,H,T,Q,N)$, the local $\NameCert_{\ChernoffFiniteExponentialBoundUp}$ surface consists exactly of the finite carrier rows of \autoref{def:chernoff-finite-exponential-bound-carrier}. Carrier admission displays the probability source, finite random-variable family, distribution summary, finite independence ledger when present, positive parameter row, finite MGF row, exponential Markov comparison, dyadic exponential arithmetic, terminal real probability seal, componentwise transports, continuation replay, package provenance, and local naming row.
\end{theorem}
\begin{proof}
Project the accepted carrier. Each row named by the certificate is one of the coordinates of $C$, so carrier formation is finite and visible.

The classifier clause is componentwise. It transports the probability source, each random-variable row, the distribution summaries, the independence ledger when the MGF is factorized, the parameter row, the MGF comparison, the dyadic arithmetic row, and the terminal real seal along the displayed $\hsame$ coordinates.

Ledger exactness follows from the same projection. Since no coordinate of $C$ names an optimizing search, countable product, measure completion, quotient event equality, or ambient real-completeness principle, none of those objects can enter the local certificate surface.
\end{proof}

\begin{theorem}[Chernoff finite MGF handoff]
\label{thm:chernoff-finite-exponential-bound-mgf-handoff}
Every tail-bound consumer of an accepted $\ChernoffFiniteExponentialBoundUp$ packet must read the finite route
$$
\ProbSpaceUp,\RandomVarUp,\DistributionUp,\IndependenceUp,\lambda,M,A,\DyadicRatCoreUp,\RealUp,H,T,Q,N .
$$
The handoff first exposes the finite probability and random-variable rows, then the displayed MGF row and exponential Markov comparison, and only then the dyadic arithmetic and terminal real bound. The route may reuse bounded finite-sum data from $\HoeffdingFiniteSumUp$ as an input range ledger, but it does not export Hoeffding's bounded-sum surface as a Chernoff optimizer or as an ambient limiting theorem.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:chernoff-finite-exponential-bound-namecert-obligations}. It places all probability, random-variable, distribution, independence, parameter, MGF, dyadic, real, transport, replay, provenance, and naming rows inside the accepted carrier.

The exponential Markov step reads only the displayed tail event, the positive parameter row, and the MGF row. If independence is used, it is the finite ledger $I$ already present in the packet, so the factorization has no countable or choice-selected component.

Finally the dyadic arithmetic row $E$ and the real seal $R$ translate the MGF comparison into the exported probability bound. Because the route terminates at those displayed rows, downstream consumers receive a finite exponential-bound packet and not an asymptotic optimization principle, measure-completion object, quotient event equality, or ambient real-completeness witness.
\end{proof}

\closureat{ChernoffFiniteExponentialBoundUp}{obligationStr}

\begin{closurestatus}{\ChernoffFiniteExponentialBoundUp}
  \constructivestory{A $\ChernoffFiniteExponentialBoundUp$ packet is generated from a probability source row, finite random-variable family, distribution summary, finite independence ledger when used, positive parameter row, finite MGF row, exponential Markov comparison, dyadic exponential arithmetic, RealUp probability seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:chernoff-finite-exponential-bound-carrier}, \autoref{thm:chernoff-finite-exponential-bound-namecert-obligations}, and \autoref{thm:chernoff-finite-exponential-bound-mgf-handoff} seed the finite MGF and exponential Markov route through displayed probability, random-variable, distribution, independence, dyadic, real, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close asymptotic optimization, countable choice, ambient measure completion, quotient event equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite MGF exposure, exponential Markov comparison, dyadic arithmetic exactness, terminal real sealing, transport, replay, provenance, and local naming for BEDC.Derived.ChernoffFiniteExponentialBoundUp.}
\end{closurestatus}
