\chapter{A Concrete Naming Certificate for $\BoundaryPairingUp$}
\label{ch:concrete-instances-boundary-pairing-namecert}
\origin{ai}

The $\BoundaryPairingUp$ packet realizes the paired-boundary handoff surface of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite BEDC data. It sits after the Gromov-boundary carrier of \autoref{ch:concrete-instances-gromov-boundary-namecert}, the cross-ratio packet of \autoref{ch:concrete-instances-hyperbolic-cross-ratio-namecert}, the geodesic-flow route of \autoref{ch:concrete-instances-hyperbolic-geodesic-flow-namecert}, the Busemann-cocycle row of \autoref{ch:concrete-instances-hyperbolic-busemann-cocycle-namecert}, and the Poincare-disk boundary transport route of \autoref{ch:concrete-instances-poincare-disk-boundary-transport-namecert}. The packet records a displayed ordered pair of boundary rows before cross-ratio or geodesic-flow consumers may inspect a handoff; it is not quotient ideal-boundary equality, not a selected geodesic, not a completed hyperbolic disk, and not a host compactification theorem.

\begin{definition}[Boundary pairing carrier]
\label{def:boundary-pairing-carrier}
A $\BoundaryPairingUp$ carrier is a finite $\BHist$ packet
$$
Q=(L,R,G,X,F,B,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the left $\GromovBoundaryUp$ boundary row.
\item $R$ is the right $\GromovBoundaryUp$ boundary row.
\item $G$ is the pair-guard row, recording that $L$ and $R$ are exposed as two boundary rows before any downstream consumer reads a cross-ratio or flow handoff.
\item $X$ is the $\HyperbolicCrossRatioUp$ handoff row, available only after the ordered pair $L,R$ and the guard $G$ have been displayed.
\item $F$ is the $\HyperbolicGeodesicFlowUp$ handoff row, recording the boundary-paired flow route consumed by geodesic-facing readers.
\item $B$ is the $\HyperbolicBusemannCocycleUp$ comparison row tying the same pair to Busemann-level boundary transport.
\item $T$ is the $\PoincareDiskBoundaryTransportUp$ row, recording disk-boundary movement that preserves the displayed pair.
\item $H$ records componentwise $\hsame$ transport for the two boundary rows, pair guard, cross-ratio handoff, geodesic-flow handoff, Busemann comparison, disk-boundary transport, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a paired-boundary request through $L,R,G,X,F,B,T,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\BoundaryPairingUp$, $\GromovBoundaryUp$, $\HyperbolicCrossRatioUp$, $\HyperbolicGeodesicFlowUp$, $\HyperbolicBusemannCocycleUp$, $\PoincareDiskBoundaryTransportUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BoundaryPairingUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,R,G,X,F,B,T,H,C,P,N$. It has no coordinate for quotient ideal-boundary equality, selected geodesic representatives, completed disks, unlogged cross-ratio shortcuts, physical observer laws, or host compactification data.
\end{definition}

\begin{theorem}[Boundary pairing NameCert obligations]
\label{thm:boundary-pairing-namecert-obligations}
For every accepted $\BoundaryPairingUp$ carrier $Q=(L,R,G,X,F,B,T,H,C,P,N)$, the local $\NameCert_{\BoundaryPairingUp}$ surface consists exactly of carrier admission, left-boundary exposure, right-boundary exposure, pair-guard exactness, cross-ratio handoff exposure, geodesic-flow handoff exposure, Busemann-cocycle comparison, Poincare-disk boundary-transport exposure, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, local naming, and nonescape of quotient ideal-boundary equality, selected geodesics, completed disks, unlogged cross-ratio shortcuts, physical observer laws, and host compactification data.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:boundary-pairing-carrier}. The rows $L,R,G,X,F,B,T,H,C,P,N$ are the whole accepted packet, so carrier admission and the two boundary exposures are direct projections.

The guard row $G$ supplies pair exactness. It records that both boundary rows are visible as a pair before any cross-ratio, geodesic-flow, Busemann, or disk-transport row can be consumed.

The downstream handoffs are the rows $X,F,B,T$. Each handoff is attached to the same displayed pair, so no consumer can replace the pair by a hidden boundary quotient or by a selected geodesic representative.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite surface. Since the carrier has no coordinate for quotient ideal-boundary equality, selected geodesics, completed disks, unlogged shortcuts, physical observer laws, or host compactification data, the nonescape clause follows by exhaustion.
\end{proof}

\begin{theorem}[Boundary pairing cross-ratio handoff]
\label{thm:boundary-pairing-cross-ratio-handoff}
Every cross-ratio, geodesic-flow, or Busemann-facing consumer of an accepted $\BoundaryPairingUp$ packet first reads the ordered boundary pair
$$
L \longmapsto R \longmapsto G
$$
and only then reads the handoff route
$$
\HyperbolicCrossRatioUp,\ \HyperbolicGeodesicFlowUp,\ \HyperbolicBusemannCocycleUp,\ \PoincareDiskBoundaryTransportUp .
$$
The handoff exports paired boundary rows and their finite transport provenance; it exports no quotient ideal-boundary equality, selected global geodesic, completed hyperbolic disk, unlogged cross-ratio shortcut, physical observer law, or host compactification theorem.
\end{theorem}
\begin{proof}
Start with an accepted carrier $Q=(L,R,G,X,F,B,T,H,C,P,N)$. Projection exposes the two boundary rows and the pair guard before any handoff row is available.

The cross-ratio handoff $X$ reads the pair only after $G$ has certified that both boundary rows are visible. The geodesic-flow row $F$ and Busemann row $B$ consume the same pair, while $T$ records the disk-boundary transport route that preserves it.

Componentwise transport through $H$ and displayed replay through $C$ keep the order fixed for transported packets. Provenance $P$ names the sibling boundary, cross-ratio, flow, Busemann, and Poincare-disk routes, and $N$ names the local certificate.

Because none of the excluded objects appears as a carrier coordinate, every listed consumer receives only the displayed ordered pair and its finite handoff rows.
\end{proof}

\falsifiablePrediction{Every accepted $\BoundaryPairingUp$ read exposes two boundary rows before any cross-ratio handoff can be consumed.}

\independenceWitness{gromov-boundary, hyperbolic-cross-ratio, hyperbolic-geodesic-flow, hyperbolic-busemann-cocycle, poincare-disk-boundary-transport: none is bijective to $\BoundaryPairingUp$ because this packet alone binds two displayed $\GromovBoundaryUp$ rows, a pair guard, cross-ratio handoff, geodesic-flow handoff, Busemann-cocycle comparison, disk-boundary transport, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

The Lean TasteGate surface for this packet must keep the two boundary rows and the pair guard as separate fields before Nontrivial and FieldFaithful instances for $\BoundaryPairingUp$ can serve downstream cross-ratio or geodesic-flow consumers.

\closureat{BoundaryPairingUp}{seedStr}

\begin{closurestatus}{\BoundaryPairingUp}
  \constructivestory{A $\BoundaryPairingUp$ packet is generated from two GromovBoundary rows, a pair-guard row, HyperbolicCrossRatio handoff, HyperbolicGeodesicFlow handoff, HyperbolicBusemannCocycle comparison, PoincareDiskBoundaryTransport, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:boundary-pairing-carrier}, \autoref{thm:boundary-pairing-namecert-obligations}, and \autoref{thm:boundary-pairing-cross-ratio-handoff} seed the finite paired-boundary route consumed before cross-ratio and geodesic-flow handoffs.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient ideal-boundary equality, selected geodesics, completed disks, physical observer laws, host compactification data, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked boundary exposure, pair-guard exactness, cross-ratio handoff exposure, geodesic-flow handoff exposure, Busemann comparison, disk-boundary transport, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.BoundaryPairingUp.}
\end{closurestatus}
