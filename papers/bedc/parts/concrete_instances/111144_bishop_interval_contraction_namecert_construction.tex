\chapter{A Concrete Naming Certificate for $\BishopIntervalContractionUp$}
\label{ch:concrete-instances-bishop-interval-contraction-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopIntervalContractionUp$ packet records a finite interval-contraction handoff from located bisection data to regular Cauchy names. It consumes the located bisection route of \autoref{ch:concrete-instances-located-interval-bisection-namecert}, the dyadic arithmetic ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite L10 neighbor of interval bisection and regular Cauchy handoff; it is not a quotient real construction, a selected infinite branch, a host fixed-point theorem, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop interval contraction carrier]
\label{def:bishop-interval-contraction-carrier}
A $\BishopIntervalContractionUp$ carrier is a finite $\BHist$ packet
$$
B=(I,F,L,D,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the located source interval row, carrying endpoints, locator evidence, and the admitted interval boundary as BEDC data.
\item $F$ is the contraction self-map row, recording the displayed map on the interval together with its contraction witness.
\item $L$ is the Lipschitz or shrinkage ledger, exposing the dyadic contraction budget and the requested depth.
\item $D$ is the $\DyadicRatCoreUp$ midpoint and error ledger used by the retained interval windows.
\item $W$ is the $\StreamNameUp$ finite-window row recording which contraction stages are inspected.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the same windows and dyadic tolerances.
\item $E$ is the terminal $\RealUp$ seal reached only after $I,F,L,D,W,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the interval, contraction, dyadic, stream, regular-rational, real-seal, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the interval-contraction request through $I,F,L,D,W,R,E,H$.
\item $P$ is the $\Pkg$ provenance over $\BishopIntervalContractionUp$, $\LocatedIntervalBisectionUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopIntervalContractionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for quotient real equality, a chosen infinite branch, a host contraction theorem, selected limits, countable choice, or ambient real completeness.
\end{definition}
\leanchecked{BEDC.Derived.BishopIntervalContractionUp.BishopIntervalContractionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Bishop interval contraction NameCert obligations]
\label{thm:bishop-interval-contraction-namecert-obligations}
For every accepted $\BishopIntervalContractionUp$ carrier
$$
B=(I,F,L,D,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\BishopIntervalContractionUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed located interval, contraction self-map, dyadic shrinkage ledger, finite windows, regular rational readback, terminal real seal, transport, replay, provenance, and local naming rows;
\item contraction stability, requiring every replayed self-map read to preserve the located source interval through $I$ and to expose its shrinkage budget through $L,D$;
\item bisection compatibility, requiring the retained interval windows to factor through the located interval bisection route before any $\StreamNameUp$ window or $\RegSeqRatUp$ readback is consumed;
\item dyadic budget exactness, requiring every tolerance request to read the same $\DyadicRatCoreUp$ midpoint and error ledger $D$ before it reaches $W,R,E$;
\item Cauchy handoff, requiring the completion-facing consumer to pass through $W,R,E,H,C,P,N$ after the interval and contraction rows have been displayed.
\end{enumerate}
No NameCert obligation is stored outside $I,F,L,D,W,R,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:bishop-interval-contraction-carrier}. The accepted packet displays the located interval, contraction, dyadic ledger, finite windows, regular readback, real seal, transport, replay, provenance, and local naming rows.

Contraction stability is read through $I,F,L,D$. The self-map row $F$ is not a host function detached from the interval; it is paired with the located interval row $I$ and with the shrinkage ledger $L,D$.

Bisection compatibility follows from the dependency route. A retained interval window must be displayed before a stream or regular-rational readback can be consumed, so the packet reaches $W$ and $R$ only after the located bisection and dyadic rows have been read.

Finally, the handoff rows $E,H,C,P,N$ preserve the same finite route. Since the carrier contains no quotient equality, selected branch, host contraction theorem, selected limit, countable-choice row, or ambient completeness coordinate, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Bishop interval contraction Cauchy handoff]
\label{thm:bishop-interval-contraction-cauchy-handoff}
Every completion-facing consumer of an accepted $\BishopIntervalContractionUp$ packet must read the finite route
$$
I,F,L,D \longmapsto W,R,E \longmapsto H,C,P,N
$$
before it can treat the contraction stages as a regular Cauchy real name. The consumer receives located interval data, a displayed contraction self-map, dyadic shrinkage budgets, finite stream windows, regular rational readback, a terminal real seal, componentwise transport, continuation replay, provenance, and local naming. It receives no quotient completion, selected limit, chosen infinite branch, countable choice, host fixed-point object, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with the interval-contraction request. The source interval is read through $I$, while the self-map and contraction budget are read through $F,L,D$.

The finite windows then expose the Cauchy route. The row $W$ records the inspected contraction stages, and $R$ reads the corresponding regular rational approximants against the same dyadic tolerance ledger.

The terminal seal $E$ is reached only after those rows are present. The structural rows $H,C,P,N$ transport, replay, source, and name the same route for downstream completion consumers.

Thus the packet hands off a regular Cauchy name through displayed BEDC rows. No quotient completion, selected limit, chosen infinite branch, countable-choice row, host fixed-point object, or ambient real-completeness coordinate is present in the carrier.
\end{proof}

\falsifiablePrediction{An accepted $\BishopIntervalContractionUp$ consumer that reaches a $\RegSeqRatUp$ readback or $\RealUp$ seal without reading the displayed rows $I,F,L,D,W,R,E,H,C,P,N$ refutes the carrier surface.}

\independenceWitness{located-interval-bisection, dyadicratcore, streamname, regseqrat, real, bishop-fixed-point-interval: no listed sibling supplies the joint packet containing a located interval source, contraction self-map, dyadic shrinkage ledger, finite windows, regular rational readback, terminal Real seal, transport, replay, provenance, and local NameCert row.}

\closureat{BishopIntervalContractionUp}{seedStr}

\begin{closurestatus}{\BishopIntervalContractionUp}
  \constructivestory{A $\BishopIntervalContractionUp$ packet is generated from a located interval row, a contraction self-map row, a dyadic shrinkage ledger, StreamName finite windows, RegSeqRat readback, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-interval-contraction-carrier}, \autoref{thm:bishop-interval-contraction-namecert-obligations}, and \autoref{thm:bishop-interval-contraction-cauchy-handoff} seed the finite interval-contraction route from located bisection and dyadic shrinkage data to regular Cauchy and Real-seal consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, quotient real equality, selected limits, chosen infinite branches, countable choice, a host fixed-point theorem, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, contraction stability, bisection compatibility, dyadic budget exactness, and Cauchy handoff for BEDC.Derived.BishopIntervalContractionUp.}
\end{closurestatus}
