\chapter{A Concrete Naming Certificate for $\RealLineSegmentUp$}
\label{ch:concrete-instances-real-line-segment-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis of located intervals, the $\RealLineSegmentUp$ packet records a bounded located real-line segment as finite NameCert data rather than as a completed compact subset. It sits over the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the located interval route of \autoref{ch:concrete-instances-located-real-interval-namecert}, the dyadic enclosure arithmetic of \autoref{ch:concrete-instances-dyadic-interval-arithmetic-namecert}, and the closed interval object of \autoref{ch:concrete-instances-closed-interval-object-namecert}. The packet is a Real and Completion carrier for endpoint, membership, and enclosure reads; it does not import Heine--Borel compactness, trichotomy, quotient representatives, or ambient completeness of $\RealUp$.

\begin{definition}[Real-line-segment carrier]
\label{def:real-line-segment-carrier}
A $\RealLineSegmentUp$ carrier is a finite $\BHist$ packet
$$
S=(A,B,L,D,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the left endpoint row, a located $\RealUp$ name exposed through the terminal real-name boundary;
\item $B$ is the right endpoint row, another located $\RealUp$ name on the same real-line substrate;
\item $L$ is the interval-membership row, recording that a displayed point read lies between the two endpoint rows by located real comparison data;
\item $D$ is the dyadic enclosure row, recording rational lower and upper windows that enclose the segment for finite Real and Completion consumers;
\item $H$ records componentwise $\hsame$ transport for endpoint, membership, enclosure, replay, provenance, and local naming rows;
\item $C$ records displayed $\Cont$ replay of endpoint exposure, membership read, and dyadic-enclosure refinement;
\item $P$ is the $\Pkg$ provenance over $\RealLineSegmentUp$, $\RealUp$, $\LocatedRealIntervalUp$, $\DyadicIntervalArithmeticUp$, $\ClosedIntervalObjectUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RealLineSegmentUp}$ row.
\end{enumerate}
The classifier compares accepted segment packets only through $A,B,L,D,H,C,P,N$. It has no coordinate for open-cover compactness, a selected midpoint oracle, quotient real representatives, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Real-line-segment NameCert obligations]
\label{thm:real-line-segment-namecert-obligations}
For the carrier of \autoref{def:real-line-segment-carrier}, the local $\NameCert_{\RealLineSegmentUp}$ surface consists exactly of carrier admission for $S=(A,B,L,D,H,C,P,N)$, endpoint exposure through $A,B$, located interval-membership exposure through $L$, dyadic enclosure exposure through $D$, componentwise transport through $H$, displayed replay through $C$, sibling provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the finite packet of \autoref{def:real-line-segment-carrier}. The endpoint rows $A,B$ are the only real-name endpoints, and the membership row $L$ is the only route by which a point is read as lying on the displayed segment.

The dyadic row $D$ supplies finite enclosure windows for downstream Real and Completion consumers. The transport and replay rows $H,C$ preserve the same endpoint, membership, and enclosure reads without introducing a second carrier.

Finally $P$ and $N$ record provenance and local naming over the cited sibling chapters. Since the packet contains no further coordinate, these rows exhaust the local $\NameCert_{\RealLineSegmentUp}$ obligation surface.
\end{proof}

\begin{theorem}[Real-line-segment located interval route]
\label{thm:real-line-segment-located-interval-route}
Every accepted $\RealLineSegmentUp$ consumer that reads a bounded located real-line segment must expose the endpoint rows $A,B$, the interval-membership row $L$, the dyadic enclosure row $D$, transport row $H$, replay row $C$, provenance row $P$, and local naming row $N$. The route factors through \autoref{ch:concrete-instances-real-namecert}, \autoref{ch:concrete-instances-located-real-interval-namecert}, \autoref{ch:concrete-instances-dyadic-interval-arithmetic-namecert}, and \autoref{ch:concrete-instances-closed-interval-object-namecert}, and exports no compactness theorem or ambient real-completeness theorem.
\end{theorem}
\begin{proof}
Start from the carrier rows. Endpoint exposure is read first through $A$ and $B$, so the segment is attached to two displayed located real names rather than to a host real interval.

Next read the interval and enclosure rows. The row $L$ supplies the located membership route, while $D$ supplies the dyadic windows consumed by finite approximation and completion-facing arguments.

The structural rows $H,C,P,N$ transport, replay, source, and locally name exactly that route. Their provenance cites the real-name, located-interval, dyadic-arithmetic, and closed-interval sibling chapters, but none of them adds Heine--Borel compactness, trichotomy, quotient representatives, or ambient completeness of $\RealUp$.
\end{proof}

\falsifiablePrediction{If a RealLineSegmentUp packet exports a segment membership or enclosure read while deleting either endpoint row, the carrier is refuted.}

\independenceWitness{real, locatedrealinterval, dyadicintervalarithmetic, closedintervalobject: none is bijective because RealLineSegmentUp binds two endpoint rows, one membership row, one dyadic enclosure row, transport, replay, provenance, and local naming in one finite packet.}

\closureat{RealLineSegmentUp}{seedStr}

\begin{closurestatus}{\RealLineSegmentUp}
  \constructivestory{A $\RealLineSegmentUp$ packet is generated from two located Real endpoint rows, an interval-membership row, a dyadic enclosure row, componentwise hsame transport, displayed Cont replay, package provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{This chapter binds the carrier definition, NameCert-obligation surface, and located-interval route for bounded located real-line segments.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close Heine--Borel, general compactness, trichotomy, quotient representatives, selected endpoints beyond the displayed rows, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires binding endpoint exposure, located membership, dyadic enclosure, transport, replay, provenance, and local naming to their displayed kernel rows.}
\end{closurestatus}
