\chapter{A Concrete Naming Certificate for $\HypothesisTestUp$}
\label{ch:concrete-instances-hypothesis-namecert}
\origin{human}

The hypothesis-testing interface $\HypothesisTestUp$ packages a decision procedure between null and alternative distributional hypotheses certified by Type I and Type II error bounds. It depends on $\NameCert_{\DistributionUp}$ (\autoref{ch:concrete-instances-distribution-namecert}) and $\NameCert_{\ProbSpaceUp}$ (\autoref{ch:concrete-instances-probspace-namecert}).

The null and alternative rows consumed from $\NameCert_{\DistributionUp}$ (\autoref{ch:concrete-instances-distribution-namecert}) supply the classifier surface for hypothesis alternatives, while the event rows consumed from $\NameCert_{\ProbSpaceUp}$ (\autoref{ch:concrete-instances-probspace-namecert}) supply the reject and accept-null event surfaces read by the Type I and Type II ledgers. The $\HypothesisTestUp$ packet adds only the decision row, threshold comparison row, and finite error-budget rows; it does not add probability completion, asymptotic optimality, or external statistical semantics.

\section{Carrier definition}
\label{sec:hypothesis-carrier}

\begin{definition}[HypothesisTestUp decision carrier]
\label{def:hypothesistest-decision-carrier}
A $\HypothesisTestUp$ decision carrier is a finite $\mathsf{BHist}$ decision
row over a displayed null hypothesis, a displayed alternative hypothesis, and a
finite threshold ledger supplied by $\NameCert_{\DistributionUp}$ and
$\NameCert_{\ProbSpaceUp}$. The carrier records the sample row, the statistic
row, the threshold comparison row, the accept/reject decision row, and the
finite Type I and Type II error-budget rows. It does not contain an asymptotic
limit, optimality principle, or classical probability-completion field.
\end{definition}

\section{The certificate}
\label{sec:hypothesis-certificate}

\begin{theorem}[HypothesisTestUp Type I error ledger]
\label{thm:hypothesistest-type-one-error-ledger}
For a $\HypothesisTestUp$ carrier, the Type I error row is a visible
$\ProbSpaceUp/\DistributionUp$ ledger entry: under the displayed null
distribution row, the reject-decision event is bounded by the finite threshold
budget recorded in \autoref{def:hypothesistest-decision-carrier}. The theorem
asserts only this finite ledger bound and no optimality or asymptotic claim.
\end{theorem}
\leanchecked{BEDC.Derived.HypothesisUp.HypothesisDecisionCarrier\_type\_one\_error\_ledger}
\begin{proof}
Project the null distribution row, sample row, statistic row, and threshold
comparison row from the decision carrier. The reject-decision event is a
finite $\mathsf{BHist}$ event row in the consumed $\ProbSpaceUp$ surface.

The Type I budget is one ledger coordinate of the same carrier. Reading the
event through the displayed $\DistributionUp$ row gives exactly the recorded
finite bound, with no appeal to external completeness or limiting frequency
data.
\end{proof}
\leanchecked{BEDC.Derived.HypothesisUp.HypothesisTestDecisionCarrier\_type\_one\_error\_ledger}

\begin{theorem}[HypothesisTestUp Type II error ledger]
\label{thm:hypothesistest-type-two-error-ledger}
For a $\HypothesisTestUp$ carrier, the Type II error row is a visible
$\ProbSpaceUp/\DistributionUp$ ledger entry: under the displayed alternative
distribution row, the accept-null decision event is bounded by the finite
threshold budget recorded in
\autoref{def:hypothesistest-decision-carrier}. The theorem asserts only this
finite ledger bound and no power optimality, asymptotic consistency, or
classical probability-completion claim.
\end{theorem}
\begin{proof}
Project the alternative distribution row together with the sample, statistic,
threshold, and accept-null decision rows. These are all finite rows in the
decision carrier, so the consumed $\ProbSpaceUp$ event is the displayed
accept-null event.

The Type II budget row is recorded beside that event in the finite threshold
ledger. Evaluation through the displayed $\DistributionUp$ row returns the
listed bound and nothing stronger than the finite BEDC evidence row.
\end{proof}
\leanchecked{BEDC.Derived.HypothesisUp.HypothesisTestDecisionCarrier\_type\_two\_error\_ledger}

\begin{theorem}[HypothesisTestUp distribution consumer boundary]
\label{thm:hypothesistest-distribution-consumer-boundary}
The $\HypothesisTestUp$ consumer boundary reads only the displayed
$\DistributionUp$ null and alternative rows, the $\ProbSpaceUp$ event rows, the
sample and statistic rows, and the finite threshold ledger. It exports no
statistical optimality theorem, asymptotic decision principle, or classical
probability-completeness row.
\end{theorem}
\leanchecked{BEDC.Derived.HypothesisTestUp.HypothesisTestDecisionCarrier\_distribution\_consumer\_boundary}
\begin{proof}
Unpack the consumer side of the decision carrier. Every probability statement
is routed through a displayed $\ProbSpaceUp$ event row and a displayed
$\DistributionUp$ hypothesis row, and every decision statement is routed
through the sample, statistic, and threshold comparison rows.

Constructor inversion on the carrier leaves no additional consumer coordinate.
The boundary can therefore repack the finite error ledgers, but it cannot
export an optimality, asymptotic, or completion field because no such field is
present in the carrier.
\end{proof}
\leanchecked{BEDC.Derived.HypothesisUp.HypothesisTestDecisionCarrier\_distribution\_boundary}

\begin{theorem}[HypothesisTestUp NameCert obligation surface]
\label{thm:hypothesistest-namecert-obligation-surface}
The public $\NameCert_{\HypothesisTestUp}$ obligation surface is the repacking
of the decision carrier, the null and alternative distribution classifiers,
decision stability under $\hsame$ transport, Type I and Type II error ledger
exactness, and the finite threshold ledger. Its claims remain finite-row
$\mathsf{BHist}$ claims over $\DistributionUp$ and $\ProbSpaceUp$.
\end{theorem}
\begin{proof}
First take the carrier from
\autoref{def:hypothesistest-decision-carrier}. It supplies the hypotheses,
sample, statistic, threshold comparison, decision row, and two finite
error-budget rows.

Next use \autoref{thm:hypothesistest-type-one-error-ledger} and
\autoref{thm:hypothesistest-type-two-error-ledger} for the two error exactness
fields. The distribution consumer boundary of
\autoref{thm:hypothesistest-distribution-consumer-boundary} fixes the allowed
source rows and prevents external probability-completion data from entering the
certificate.

Finally repack these rows with the visible classifier and $\hsame$ transport
fields. Since every component is a finite row from the displayed decision
carrier or its two consumed distribution certificates, the resulting
$\NameCert_{\HypothesisTestUp}$ surface contains no statistical optimality,
asymptotic, or classical completeness assertion.
\end{proof}
\leanchecked{BEDC.Derived.HypothesisUp.HypothesisTestDecisionCarrier\_namecert\_obligation\_surface}

\begin{theorem}[HypothesisTestUp finite-risk boundary]
\label{thm:hypothesis-test-finite-risk-boundary}
For every accepted $\HypothesisTestUp$ carrier, any finite risk read factors
through the displayed null and alternative $\DistributionUp$ rows, the
consumed $\ProbSpaceUp$ event rows, the sample and statistic rows, the
statistic-threshold comparison row, the decision row, and the Type I and Type
II error-budget rows. The boundary exports only these finite ledgers and no
asymptotic consistency, optimality principle, or probability-completion datum.
\end{theorem}
\begin{proof}
Project the decision carrier of
\autoref{def:hypothesistest-decision-carrier}. The carrier displays the null
and alternative hypotheses, the sample row, statistic row,
statistic-threshold comparison, decision row, and the two finite
error-budget rows.

Apply the Type I and Type II ledger theorems,
\autoref{thm:hypothesistest-type-one-error-ledger} and
\autoref{thm:hypothesistest-type-two-error-ledger}. They identify the only
reject-under-null and accept-under-alternative risk reads with the displayed
$\ProbSpaceUp/\DistributionUp$ event ledgers.

Finally use the distribution consumer boundary and the NameCert obligation
surface. Theorems~\ref{thm:hypothesistest-distribution-consumer-boundary}
and~\ref{thm:hypothesistest-namecert-obligation-surface} keep every public
risk read inside the displayed decision rows, so no hidden asymptotic,
optimality, or probability-completion field can be exported.
\end{proof}

\begin{theorem}[HypothesisTestUp decision stability under transport]
\label{thm:hypothesis-test-decision-stability-under-transport}
Let two accepted $\HypothesisTestUp$ carriers be related componentwise by
$\hsame$ on their null and alternative $\DistributionUp$ rows,
$\ProbSpaceUp$ event rows, statistic-threshold ledgers, decision rows, and
Type I/Type II budget rows. Then the transported carrier preserves the
decision row and both error-ledger reads through the same finite
$\NameCert_{\HypothesisTestUp}$ surface. The transport adds no asymptotic
consistency claim, optimality datum, or probability-completion row.
\end{theorem}
\begin{proof}
Start from the componentwise $\hsame$ relation. The carrier projection gives
matching null and alternative distribution rows, event rows, statistic and
threshold comparisons, decision rows, and finite Type I/Type II budget rows.

Transport the error reads through the already checked ledgers. Theorem~\ref{thm:hypothesistest-type-one-error-ledger}
preserves the reject-under-null row, and
\autoref{thm:hypothesistest-type-two-error-ledger} preserves the
accept-under-alternative row, because each is read from the displayed carrier
coordinates.

Repack the transported rows using
\autoref{thm:hypothesistest-namecert-obligation-surface}. Since the
NameCert surface contains only the decision carrier, visible classifiers,
transport field, and finite ledgers, the transported decision cannot acquire
an optimality, asymptotic, or probability-completion coordinate.
\end{proof}

\begin{theorem}[HypothesisTestUp finite-risk public route]
\label{thm:hypothesis-seed-public-route}
For every accepted $\HypothesisTestUp$ carrier, any public finite-risk read
factors through the displayed $\DistributionUp$ null and alternative rows, the
consumed $\ProbSpaceUp$ event rows, the statistic-threshold comparison row, the
decision row, the Type I/II error ledgers, and componentwise $\hsame$ transport.
The route exports a finite decision surface over the named carrier and no
statistical optimality, asymptotic consistency, probability-completion datum, or
standard statistical bridge.
\end{theorem}
\begin{proof}
Start with the finite-risk boundary of
\autoref{thm:hypothesis-test-finite-risk-boundary}. It routes every public risk
read through the displayed null and alternative distributions, consumed
probability events, statistic row, threshold comparison, decision row, and both
finite error budgets.

Use \autoref{thm:hypothesis-test-decision-stability-under-transport} to make the
same route stable under componentwise $\hsame$ transport. The transported read
still sees only the displayed carrier coordinates and the two Type I/II ledgers.

Finally repack the read through
\autoref{thm:hypothesistest-namecert-obligation-surface}. The public surface is
therefore exactly the finite carrier route through $\DistributionUp$,
$\ProbSpaceUp$, statistic-threshold comparison, decision rows, error ledgers, and
$\hsame$ transport, with no bridge or asymptotic field added.
\end{proof}

\closureat{HypothesisTestUp}{matureStr}[BEDC.Derived.HypothesisUp.HypothesisTestDecisionCarrier\_namecert\_obligation\_surface]

\begin{closurestatus}{\HypothesisTestUp}
  \theoryclosure{\matureClosure}
  \scopeclosed{The bridge-facing scope names the finite decision carrier, Type I/II ledgers, distribution consumer boundary, decision transport, and public finite-risk route over displayed DistributionUp and ProbSpaceUp rows, with the checked decision-carrier obligation surface supplying the packet boundary.}
  \constructivestory{A $\HypothesisTestUp$ token names a finite decision carrier with null and alternative $\DistributionUp$ inputs, consumed $\ProbSpaceUp$ event rows, a displayed statistic-threshold comparison, and two visible finite error-budget ledgers. The classifier is $\hsame$ on the decision history, transport repacks the statistic, threshold, decision row, and error ledgers through that history comparison, and the source covers only the displayed distribution, event, threshold, and finite budget rows. A history $h$ lies in the signature gap of a $\HypothesisTestUp$ token $p$ when the finite carrier accepts $h$, the signature relation pairs it with the decision verdict and error-budget rows, and token introduction names that finite verdict as $p$.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.HypothesisUp.HypothesisTestDecisionCarrier\_namecert\_obligation\_surface}
  \bridgestatus{none}
  \notclaimed{The chapter does not claim statistical optimality, asymptotic consistency, classical probability completion, or a standard statistical bridge.}
  \upgradepath{The remaining bridge work is standard statistical bridge verification for the same decision-carrier obligation surface and its Type I/II ledger route, not additional paper closure.}
\end{closurestatus}

\closureat{HypothesisUp}{seedStr}

\begin{closurestatus}{\HypothesisUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hypothesistest-decision-carrier}, \autoref{thm:hypothesistest-namecert-obligation-surface}, and \autoref{thm:hypothesis-seed-public-route} expose the finite carrier, decision obligation rows, Type I/II ledgers, and public finite-risk route for the hypothesis-testing surface.}
  \constructivestory{A $\HypothesisUp$ token is recorded here through the displayed hypothesis-testing decision carrier: null and alternative distribution rows, probability-event rows, a finite statistic-threshold comparison, a decision row, Type I/II budget rows, componentwise transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This seed record does not claim Lean verification, bridge checking, statistical optimality, asymptotic consistency, probability completion, or any global hypothesis-selection principle.}
  \upgradepath{Obligation closure requires a local HypothesisUp carrier boundary that separates the general hypothesis token from the displayed HypothesisTestUp decision carrier and its finite risk ledgers.}
\end{closurestatus}
