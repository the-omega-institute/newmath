\chapter{A Concrete Naming Certificate for $\UniformlyConvexSpaceUp$}
\label{ch:concrete-instances-uniformly-convex-space-namecert}
\origin{human}

Following Clarkson's uniformly convex Banach-space reading and Bishop--Bridges constructive normed-space practice, the $\UniformlyConvexSpaceUp$ packet records a finite modulus-of-convexity surface over the normed-space carrier of \autoref{ch:concrete-instances-normedspace-namecert}. It consumes the $\NormedSpaceUp$ row, the real-valued norm and midpoint reads through \autoref{ch:concrete-instances-real-namecert}, the metric handoff of \autoref{ch:concrete-instances-metric-namecert}, and the completion-facing route of \autoref{ch:concrete-instances-completion-namecert} and \autoref{ch:concrete-instances-cauchy-namecert}. The packet gives Banach, Hilbert, fixed-point, and convexity consumers a named uniform-convexity certificate without importing host Banach-space equality, selected minimizers, quotient completions, or choice.

\begin{definition}[UniformlyConvexSpace carrier]
\label{def:uniformly-convex-space-carrier}
A $\UniformlyConvexSpaceUp$ carrier is a finite $\BHist$ packet
$$
U=(N,R,A,M,\Delta,Q,H,T,P,C)
$$
with the following displayed rows:
\begin{enumerate}
\item $N$ is the $\NormedSpaceUp$ row, exposing vector operations, the norm read, and the induced metric only through the normed-space NameCert surface.
\item $R$ is the $\RealUp$ row used by norm values, radius comparisons, midpoint estimates, and positive modulus outputs.
\item $A$ is the midpoint row, reading $(x+y)/2$ through the vector addition, scalar action, and real scalar rows already visible in $N$ and $R$.
\item $M$ is the metric handoff row, routing distance and unit-ball comparisons through the $\MetricUp$ read induced by $N$.
\item $\Delta$ is the convexity-modulus row: for each displayed positive separation request it supplies a positive real margin forcing the midpoint norm below the unit bound whenever the two admitted unit vectors are separated by that request.
\item $Q$ is the completion-facing row, allowing Cauchy and completion consumers to read the same norm, metric, midpoint, and modulus data without selecting a completed point.
\item $H$ records componentwise $\hsame$ transport for $N,R,A,M,\Delta,Q$ and for the local structural rows.
\item $T$ records displayed $\Cont$ replay from a uniform-convexity request through $N,R,A,M,\Delta,Q,H,P,C$.
\item $P$ is the $\Pkg$ provenance over $\UniformlyConvexSpaceUp$, $\NormedSpaceUp$, $\RealUp$, $\MetricUp$, $\CompletionUp$, $\CauchyUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $C$ is the local $\NameCert_{\UniformlyConvexSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these ten rows. It has no coordinate for host Banach-space equality, strict-convexity as a pointwise substitute, quotient completion, selected closest points, compactness, reflexivity, or a choice principle.
\end{definition}

\begin{theorem}[UniformlyConvexSpace NameCert obligations]
\label{thm:uniformly-convex-space-namecert-obligations}
For every accepted $\UniformlyConvexSpaceUp$ carrier $U=(N,R,A,M,\Delta,Q,H,T,P,C)$, the local $\NameCert_{\UniformlyConvexSpaceUp}$ surface consists exactly of carrier admission, normed-space visibility, real-valued norm and midpoint readback, metric separation handoff, positive modulus admission, midpoint contraction under the displayed separation request, completion-facing nonescape, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No obligation is stored outside $N,R,A,M,\Delta,Q,H,T,P,C$.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:uniformly-convex-space-carrier}. The rows $N,R,A,M,\Delta,Q,H,T,P,C$ are the whole accepted packet.

The normed-space, Real, midpoint, and metric obligations are read first. The row $N$ supplies the normed vector carrier, $R$ supplies real-valued comparisons, $A$ supplies midpoint formation, and $M$ supplies the induced metric separation route.

The uniform-convexity content is isolated in $\Delta$. For a displayed positive separation request, $\Delta$ supplies a positive margin and forces the midpoint norm read through $A,N,R$ below the unit bound. Since $Q,H,T,P,C$ only hand off, transport, replay, source, and name the same route, no host Banach-space equality, quotient completion, selected point, compactness, reflexivity, or choice coordinate can enter.
\end{proof}

\begin{theorem}[UniformlyConvexSpace modulus convexity route]
\label{thm:uniformly-convex-space-modulus-convexity-route}
Every accepted $\UniformlyConvexSpaceUp$ consumer that asks for uniform convexity factors through
$$
\NormedSpaceUp,\RealUp,\MetricUp,\CompletionUp,\CauchyUp \longmapsto H,T,P,C .
$$
The modulus row $\Delta$ may be read only after the normed-space carrier, real norm values, midpoint operation, and metric separation row have been displayed. The route exports a finite modulus-of-convexity NameCert surface, not host strict convexity, a selected minimizer, quotient completion equality, compactness, reflexivity, or countable choice.
\end{theorem}
\begin{proof}
Begin with a uniform-convexity request. The normed-space row $N$ supplies the only vector and norm data, while $R$ supplies the real-valued unit-ball and positivity comparisons.

Read midpoint and separation next. The midpoint row $A$ is formed from the displayed operations of $N$ and $R$, and the metric handoff $M$ reads separation through the induced metric of the same normed-space packet.

Only then can the modulus row $\Delta$ answer the request. Its output is tied to the displayed positive separation and midpoint norm route, and the completion-facing row $Q$ sends Cauchy or completion consumers through the same route rather than through a selected completed point.

Finally $H,T,P,C$ transport, replay, source, and name the packet. The excluded host strict-convexity, selected-minimizer, quotient-completion, compactness, reflexivity, and choice objects are absent from the carrier, so the finite modulus route is the whole exported surface.
\end{proof}

\falsifiablePrediction{An accepted $\UniformlyConvexSpaceUp$ read is refuted if a consumer obtains a uniform-convexity margin without first displaying the $\NormedSpaceUp$, $\RealUp$, midpoint, $\MetricUp$, completion-facing, transport, replay, provenance, and local $\NameCert$ rows.}

\independenceWitness{normedspace, real, metric, completion, cauchy: no listed sibling is bijective to $\UniformlyConvexSpaceUp$ because the packet jointly carries normed-space data, real norm comparisons, midpoint formation, metric separation, a positive convexity modulus, completion-facing handoff, transport, replay, package provenance, and local naming.}

\closureat{UniformlyConvexSpaceUp}{seedStr}

\begin{closurestatus}{\UniformlyConvexSpaceUp}
  \constructivestory{A $\UniformlyConvexSpaceUp$ packet is generated from a NormedSpace row, Real norm-comparison rows, a midpoint row, an induced Metric separation row, a convexity-modulus row, a Completion and Cauchy handoff row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:uniformly-convex-space-carrier}, \autoref{thm:uniformly-convex-space-namecert-obligations}, and \autoref{thm:uniformly-convex-space-modulus-convexity-route} seed the finite modulus-of-convexity route over normed-space, real, metric, completion, and Cauchy rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host Banach-space equality, strict-convexity substitution, selected minimizers, quotient completions, compactness, reflexivity, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, midpoint formation, metric separation, positive modulus exactness, completion-facing nonescape, and local naming for BEDC.Derived.UniformlyConvexSpaceUp.}
\end{closurestatus}
