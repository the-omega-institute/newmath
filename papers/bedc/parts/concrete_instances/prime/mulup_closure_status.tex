\closureat{MulUp}{matureStr}[BEDC.Derived.PrimeUp.NatMul\_result\_semanticNameCert]

\begin{closurestatus}{\MulUp}
  \constructivestory{Unary multiplication is generated on the same spine used for natural numbers: the carried inputs and result are unary histories, the unit is the one-step spine $\Eone(\emp)$, and the multiplication relation is accepted only when the displayed continuation ledger witnesses the repeated append pattern named by $\mathsf{NatMul}$. Two multiplication presentations are classified by $\hsame$ on the unary endpoints and by functionality of the result ledger, so the construction identifies transported factor, multiplier, and product rows without importing a semiring carrier or a host multiplication table. The source specification is the unary-history carrier together with the factor, multiplier, and result rows, the pattern specification records totality, result closure, functionality, unit laws, commutativity, associativity, additive distribution, order monotonicity, and divisibility-order rows, and the ledger policy keeps the raw factors, multiplier splits, appended result, unit endpoint, and continuation evidence visible. The full natural-arithmetic mature boundary is the paper-side consumer boundary of \autoref{thm:mulup-full-nat-arithmetic-mature-boundary}.}
  \theoryclosure{\matureClosure}
  \scopeclosed{The mature Full Nat Arithmetic boundary is the $\mathsf{BHist}$ unary-history multiplication package of \autoref{thm:mulup-full-nat-arithmetic-mature-boundary}, including the $\mathsf{NatMul}$ semantic exit, checked law audit package, $\AddUp$ distributivity rows, $\OrderUp$ monotonicity row, and divisibility-order consumer rows.}
  \formalstatus{\auditCleanV}
  \leantarget{BEDC.Derived.PrimeUp.NatMul\_result\_semanticNameCert}
  \bridgestatus{paperBridge}
  \notclaimed{This chapter does not close semiring completion, quotient carriers, host multiplication, or cancellation laws outside the displayed unary-history rows.}
  \upgradepath{Further closure requires a checked standard bridge for the mature Full Nat Arithmetic surface.}
\end{closurestatus}
