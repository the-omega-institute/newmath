\chapter{A Concrete Naming Certificate for $\CompactOscillationModulusUp$}
\label{ch:concrete-instances-compact-oscillation-modulus-namecert}
\origin{human}

Following Bishop--Bridges compact metric analysis and the finite-net proof of uniform continuity in \autoref{ch:visions-compact-uniform-continuity}, the $\CompactOscillationModulusUp$ packet records compact oscillation control as finite NameCert data. It sits over the compact source of \autoref{ch:concrete-instances-compact-namecert}, the metric inspection rows of \autoref{ch:concrete-instances-metric-namecert}, the compact-metric finite-net surface of \autoref{ch:concrete-instances-compactmetric-namecert}, the finite cover nerve of \autoref{ch:concrete-instances-finite-cover-nerve-namecert}, and the downstream $\UniformModulusUp$ consumer. The packet names finite compact-net oscillation bounds and the handoff from local oscillation ledgers to a tolerance-only uniform modulus; it is not an external modulus selector, an arbitrary open-cover argument, a host minimum over all points, or an ambient completeness theorem.

\begin{definition}[Compact oscillation-modulus carrier]
\label{def:compact-oscillation-modulus-carrier}
A $\CompactOscillationModulusUp$ carrier is a finite $\BHist$ packet
$$
O=(K,M,V,L,F,R,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact source row, carrying finite compact-net coverage data.
\item $M$ is the metric observation row used to read source distances and finite ball membership.
\item $V$ is the finite cover-nerve row recording the displayed compact-net cover and overlap incidence.
\item $L$ is the local oscillation row, assigning to each displayed center a target oscillation bound.
\item $F$ is the finite oscillation fold row, computing a compact-net-wide bound from the listed local rows.
\item $R$ is the $\RatUp$ comparison row for the finite radii, tolerances, and oscillation bounds.
\item $U$ is the $\UniformModulusUp$ handoff row exposed only after $K,M,V,L,F,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the compact, metric, cover-nerve, oscillation, fold, rational, handoff, and structural rows.
\item $C$ records displayed $\Cont$ replay from compact-net oscillation requests through $K,M,V,L,F,R,U,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CompactOscillationModulusUp$, $\CompactUp$, $\CompactMetricUp$, $\MetricUp$, $\FiniteCoverNerveUp$, $\OscillationModulusUp$, $\UniformModulusUp$, $\RatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactOscillationModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,M,V,L,F,R,U,H,C,P,N$. It has no coordinate for arbitrary open covers, selected subcovers, a host supremum of oscillations, quotient equality of covers, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact oscillation-modulus NameCert obligations]
\label{thm:compact-oscillation-modulus-namecert-obligations}
For every accepted $\CompactOscillationModulusUp$ carrier
$$
O=(K,M,V,L,F,R,U,H,C,P,N),
$$
the local $\NameCert_{\CompactOscillationModulusUp}$ surface consists exactly of compact finite-net admission through $K$, metric readback through $M$, finite cover-nerve exposure through $V$, local oscillation-bound exposure through $L$, finite oscillation folding through $F$, rational comparison through $R$, uniform-modulus handoff through $U$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:compact-oscillation-modulus-carrier}. The accepted packet displays the compact source, metric observations, finite cover nerve, local oscillation rows, finite fold, rational comparisons, uniform-modulus handoff, and structural rows.

The compact and cover obligations are read through $K,M,V$. These rows expose finite compact-net coverage and overlap incidence, not an arbitrary open-cover compactness theorem.

The oscillation obligations are read through $L,F,R$. Local oscillation bounds are folded over the displayed finite net and compared by the rational row, so no host supremum or selected global minimum is available.

Finally $U,H,C,P,N$ expose only the downstream handoff and structural replay of the same packet. This exhausts the local NameCert surface.
\end{proof}

\begin{theorem}[Compact oscillation-modulus uniform handoff]
\label{thm:compact-oscillation-modulus-uniform-handoff}
Every $\UniformModulusUp$ consumer of an accepted $\CompactOscillationModulusUp$ carrier factors through the finite route
$$
K,M,V \longmapsto L,F,R \longmapsto U,H,C,P,N .
$$
The route first exposes compact-net and metric cover data, then folds local oscillation bounds over the finite cover-nerve rows, and only then exports the tolerance-only uniform-modulus handoff. It exports no arbitrary open-cover compactness theorem, selected subcover, host oscillation supremum, quotient cover equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with $K,M,V$. The compact source and metric rows identify the finite compact-net observations, and the finite cover nerve records the cover and overlap rows that control which local oscillation bounds can be compared.

Next read $L,F,R$. The local oscillation row supplies the displayed bounds, the fold row combines only the finite list, and the rational row records the tolerance and radius comparisons used by the fold.

The handoff row $U$ is downstream of those finite rows. Componentwise transport, replay, provenance, and local naming through $H,C,P,N$ therefore keep every accepted uniform-modulus read inside the same compact oscillation packet.
\end{proof}

\closureat{CompactOscillationModulusUp}{seedStr}

\begin{closurestatus}{\CompactOscillationModulusUp}
  \constructivestory{A $\CompactOscillationModulusUp$ packet is generated from compact finite-net rows, metric observation rows, finite cover-nerve rows, local oscillation-bound rows, finite oscillation folds, RatUp comparisons, UniformModulus handoff rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-oscillation-modulus-carrier}, \autoref{thm:compact-oscillation-modulus-namecert-obligations}, and \autoref{thm:compact-oscillation-modulus-uniform-handoff} seed the finite compact-net oscillation route through CompactUp, CompactMetricUp, MetricUp, FiniteCoverNerveUp, OscillationModulusUp, UniformModulusUp, RatUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close arbitrary open-cover compactness, selected subcovers, host oscillation suprema, quotient cover equality, countable choice, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-net exposure, cover-nerve exactness, local oscillation-bound exposure, finite fold stability, uniform-modulus handoff, structural replay, provenance exhaustion, and local naming for BEDC.Derived.CompactOscillationModulusUp.}
\end{closurestatus}
