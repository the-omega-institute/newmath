\chapter{A Concrete Naming Certificate for $\PartitionCoreIncidenceCertificateUp$}
\label{ch:concrete-instances-partition-core-incidence-certificate-namecert}
\origin{ai}

The $\PartitionCoreIncidenceCertificateUp$ packet realizes the partition-core incidence kernel of \autoref{thm:philosophy-partition-core-incidence-kernel-theorem} as finite NameCert data. It sits over the partition carrier of \autoref{ch:concrete-instances-partition-namecert}, the Boolean row discipline of \autoref{ch:concrete-instances-booleanalgebra-namecert}, the simplicial support surface of \autoref{ch:concrete-instances-simplicialcomplex-namecert}, the cellular update route of \autoref{ch:concrete-instances-cellularautomaton-namecert}, and the matroid rank boundary of \autoref{ch:concrete-instances-matroid-namecert}. The packet records finite block, quotient, lift, spectrum, and incidence rows; it is not a biological stochastic model, host graph oracle, quotient codon semantics, unrestricted spectral theorem, or hidden global scheduler.

\falsifiablePrediction{Every accepted $\PartitionCoreIncidenceCertificateUp$ read exposes the partition block row, quotient-size row, incidence-count row, lift-count row, kernel-spectrum row, Boolean support row, and local $\NameCert$ row before a partition-core consumer may cite the finite incidence certificate.}

\independenceWitness{partition, booleanalgebra, simplicialcomplex, cellularautomaton, matroid: none alone is bijective with $\PartitionCoreIncidenceCertificateUp$ because the packet jointly carries partition blocks, Boolean support tests, support-nerve incidence, cellular replay, rank-separation boundaries, quotient spectra, lift counts, transport, replay, provenance, and local naming.}

\begin{definition}[Partition-core incidence certificate carrier]
\label{def:partition-core-incidence-certificate-carrier}
A $\PartitionCoreIncidenceCertificateUp$ carrier is a finite $\BHist$ packet
$$
I=(B,Q,A,L,S,E,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the finite partition-block row, recording the displayed block classes and their support names.
\item $Q$ is the quotient-size row, recording the size-graded partition-core windows used by the incidence kernel.
\item $A$ is the incidence-count row, recording finite adjacency or boundary incidences between displayed classes.
\item $L$ is the lift-count row, recording codon-level or support-level lifts of each quotient incidence.
\item $S$ is the kernel-spectrum row, recording the finite spectra exposed by the displayed quotient kernels.
\item $E$ is the Boolean and simplicial support row, recording complement tests, face membership, and support-nerve incidence.
\item $R$ is the rank and replay row, recording the matroid rank boundary and cellular replay needed by downstream partition-core consumers.
\item $H$ records componentwise $\hsame$ transport for block, quotient, incidence, lift, spectrum, support, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from partition-core requests through $B,Q,A,L,S,E,R,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\PartitionCoreIncidenceCertificateUp$, $\PartitionUp$, $\BooleanAlgebraUp$, $\SimplicialComplexUp$, $\CellularAutomatonUp$, $\MatroidUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PartitionCoreIncidenceCertificateUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,Q,A,L,S,E,R,H,C,P,N$. It has no coordinate for host graph reachability, biological stochastic semantics, quotient codon identity, unrestricted homology, hidden scheduler state, or an external spectral oracle.
\end{definition}

\begin{theorem}[Partition-core incidence certificate obligations]
\label{thm:partition-core-incidence-certificate-obligations}
For every accepted $\PartitionCoreIncidenceCertificateUp$ carrier
$$
I=(B,Q,A,L,S,E,R,H,C,P,N),
$$
the local $\NameCert_{\PartitionCoreIncidenceCertificateUp}$ surface consists exactly of partition-block admission through $B$, quotient-size exposure through $Q$, incidence-count exposure through $A$, lift-count exposure through $L$, finite kernel-spectrum exposure through $S$, Boolean and simplicial support exposure through $E$, rank and cellular replay exposure through $R$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:partition-core-incidence-certificate-carrier}. The rows $B,Q,A,L,S,E,R$ are the finite incidence data, and $H,C,P,N$ are the structural certificate rows.

The incidence route first reads partition blocks and quotient sizes. Only then can a consumer inspect incidence counts, lift counts, and finite kernel spectra.

Support and replay data are also displayed. The Boolean and simplicial row $E$ exposes the support tests, while $R$ records rank separation and cellular replay for the same finite partition-core request.

The structural rows preserve exactly those coordinates. Since the excluded host semantics and unrestricted spectral or reachability principles have no carrier row, they cannot be obligations of the local NameCert surface.
\end{proof}

\begin{theorem}[Partition-core incidence TasteGate boundary]
\label{thm:partition-core-incidence-certificate-tastegate-boundary}
Any TasteGate realization for $\PartitionCoreIncidenceCertificateUp$ must provide $\mathsf{Nontrivial}\ \PartitionCoreIncidenceCertificateUp$ and $\mathsf{FieldFaithful}\ \PartitionCoreIncidenceCertificateUp$ instances preserving $B,Q,A,L,S,E,R,H,C,P,N$ as separate displayed fields. In particular, the gate must distinguish a changed partition block, quotient-size row, incidence-count row, lift-count row, kernel-spectrum row, support row, or replay row before it admits two packets as the same incidence certificate.
\end{theorem}
\begin{proof}
Use the carrier of \autoref{def:partition-core-incidence-certificate-carrier}. The rows $B,Q,A,L,S,E,R$ play different roles: source blocks, quotient windows, incidences, lifts, spectra, support tests, and replay boundaries.

The obligations of \autoref{thm:partition-core-incidence-certificate-obligations} consume those rows in order. Collapsing two of them would erase either the source partition, a counted incidence, a lifted incidence, a spectral claim, or the support and replay boundary.

Nontriviality is witnessed at the paper surface by packets that vary one displayed row while keeping the others fixed. Field faithfulness must therefore preserve the row list as a visible TasteGate boundary.
\end{proof}

\closureat{PartitionCoreIncidenceCertificateUp}{seedStr}

\begin{closurestatus}{\PartitionCoreIncidenceCertificateUp}
  \constructivestory{A $\PartitionCoreIncidenceCertificateUp$ packet is generated from partition-block rows, quotient-size rows, incidence-count rows, lift-count rows, finite kernel-spectrum rows, Boolean and simplicial support rows, rank and cellular replay rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:partition-core-incidence-certificate-carrier}, \autoref{thm:partition-core-incidence-certificate-obligations}, and \autoref{thm:partition-core-incidence-certificate-tastegate-boundary} seed the finite partition-core incidence certificate surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, biological stochastic semantics, quotient codon identity, host graph reachability, unrestricted homology, an external spectral oracle, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, incidence-count exposure, lift-count exposure, finite spectrum exposure, support replay, Nontrivial PartitionCoreIncidenceCertificateUp, FieldFaithful PartitionCoreIncidenceCertificateUp, transport, provenance, and local naming for BEDC.Derived.PartitionCoreIncidenceCertificateUp.}
\end{closurestatus}
