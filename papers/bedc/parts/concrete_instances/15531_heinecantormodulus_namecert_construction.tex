\chapter{A Concrete Naming Certificate for $\HeineCantorModulusUp$}
\label{ch:concrete-instances-heine-cantor-modulus-namecert}
\origin{human}

Following Bishop and Bridges, the $\HeineCantorModulusUp$ packet records the
finite-net form of Heine--Cantor as explicit BEDC modulus data. It reads the
compact-metric finite net of
\autoref{ch:concrete-instances-compactmetric-namecert}, the finite
compact-uniform-continuity route of
\autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, the
pointwise rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the
consumer modulus surface of
\autoref{ch:concrete-instances-uniformmodulus-namecert}, and the final
triangle handoff of
\autoref{ch:concrete-instances-metric-triangle-uniform-estimate-namecert}. The
chapter names the finite certificate route that turns local pointwise modulus
rows on a displayed compact net into one tolerance-indexed uniform modulus.

\begin{definition}[Heine-Cantor modulus carrier]
\label{def:heine-cantor-modulus-carrier}
A $\HeineCantorModulusUp$ carrier is a finite BEDC packet
$$
H=(X,Y,F,\varepsilon,P,C,R,M,T,Q,S,K,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the compact metric source row, read through
$\NameCert_{\CompactMetricUp}$, with its finite $\mathsf{ProbeBundle}$ net
family at the target precision scale.
\item $Y$ is the target metric row whose triangle ledger receives the final
estimate.
\item $F$ is the continuous-map row from $X$ to $Y$, read through
$\NameCert_{\ContinuousMapUp}$, with pointwise modulus rows at the listed net
centers.
\item $\varepsilon$ is the positive target dyadic precision history whose
uniform modulus is being named.
\item $P$ is the finite $\mathsf{ProbeBundle}$ net selected from the compact
metric certificate for the source scale required by $\varepsilon$.
\item $C$ lists the center histories in $P$, together with their membership
constructors and coverage rows.
\item $R$ lists the pointwise continuity radii attached to those centers and
the rational lower-bound rows used to fold them into one positive source
precision.
\item $M$ is the exported source precision history, classified componentwise
by the $\RatUp$ and source-metric classifiers.
\item $T$ is the metric triangle handoff row, read through
\autoref{ch:concrete-instances-metric-triangle-uniform-estimate-namecert},
which carries the compact-net source estimates and the two pointwise target
estimates to the final $\varepsilon$ bound.
\item $Q$ records componentwise $\hsame$ transport for $X,Y,F,\varepsilon,P,C,R,M,T$.
\item $S$ records the displayed $\Cont$ routes from compact metric data,
pointwise continuity data, finite-fold data, and triangle data to the
$\UniformModulusUp$ consumer.
\item $K$ records $\Pkg$ provenance over $\CompactMetricUp$,
$\CompactUniformContinuityUp$, $\ContinuousMapUp$, $\UniformModulusUp$,
$\MetricTriangleUniformEstimateUp$, $\RatUp$, $\BHist$, $\hsame$, and
$\Cont$ rows.
\item $N$ is the local $\NameCert_{\HeineCantorModulusUp}$ row.
\end{enumerate}
The carrier accepts only these finite rows. Its displayed continuity condition
is the continuation assertion
$$
\Cont(H,\NameCert_{\UniformModulusUp})
$$
through the rows $P,C,R,M,T,Q,S,K,N$; it has no coordinate for open-cover
compactness, sequential compactness, choice-selected centers, trichotomy, or
host real equality.
\end{definition}
\leanchecked{BEDC.Derived.HeineCantorModulusUp.HeineCantorModulusTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Heine-Cantor modulus NameCert obligations]
\label{thm:heine-cantor-modulus-namecert-obligations}
The local $\NameCert_{\HeineCantorModulusUp}$ obligation surface consists
exactly of the finite carrier exposure of
\autoref{def:heine-cantor-modulus-carrier}, componentwise $\hsame$ transport
for all displayed rows, the finite-net extraction row, the folded positive
source-precision row, the metric triangle handoff row, and the
$\UniformModulusUp$ consumer route. No accepted obligation may cite an
open-cover compactness theorem, a sequential compactness principle, a choice
operation, a trichotomy split on host reals, or host equality of real points.
\end{theorem}
\begin{proof}
Project the carrier packet $H$. The source, target, continuous-map,
precision, net, center, radius, folded-modulus, triangle, transport,
continuation, provenance, and local naming rows are the whole displayed
surface.

Classifier stability is componentwise. The row $Q$ transports each displayed
history by the corresponding source, target, rational, finite-bundle, and
triangle classifier, while $S$ replays the same $\Cont$ route after transport.

The remaining obligations are the three constructive readings: extract the
finite net from $\CompactMetricUp$, fold the pointwise radii in the finite
list to obtain $M$, and apply the metric triangle handoff to deliver the
target $\varepsilon$ estimate. Since every reading uses a named coordinate of
$H$, the excluded compactness and choice-theoretic routes have no readable
slot in the local $\NameCert$ packet.
\end{proof}

\begin{theorem}[Heine-Cantor finite-net extraction]
\label{thm:heine-cantor-modulus-finite-net-extraction}
For an accepted $\HeineCantorModulusUp$ carrier $H$, the net rows
$P$ and $C$ determine the finite list of centers used by the modulus
construction. Each listed center is a source history carried by $X$, each
membership row is a displayed $\mathsf{ProbeBundle}$ constructor, and each
coverage row is a compact-metric distance ledger at the precision scale read
from $\varepsilon$. The extraction is by finite-row projection and induction
over the displayed net spine.
\end{theorem}
\leanchecked{BEDC.Derived.HeineCantorModulusUp.HeineCantorModulusCarrier\_finite\_net\_extraction}
\begin{proof}
Read $P$ from the carrier. It is already a finite $\mathsf{ProbeBundle}$ row
from the compact-metric certificate, so the only available centers are the
histories listed in its finite spine.

Induct over that spine. At the empty boundary no center is produced and no
coverage row is exported. At a successor row, project the displayed center
history, its membership constructor, and the attached compact-metric coverage
ledger, then append those rows to the extracted list.

The induction uses no cover refinement or external compactness principle.
Every step reads the next stored row of $P$ and $C$, and componentwise
$\hsame$ transport in $Q$ carries the extracted center and coverage rows to
classified representatives.
\end{proof}

\begin{theorem}[Heine-Cantor triangle handoff]
\label{thm:heine-cantor-modulus-triangle-handoff}
For an accepted $\HeineCantorModulusUp$ carrier $H$, the folded source
precision $M$, the compact-net center rows $C$, and the pointwise continuity
rows $R$ determine the triangle handoff row $T$. Consequently the
$\UniformModulusUp$ consumer receives a single positive source precision whose
validity for the target tolerance $\varepsilon$ is witnessed by finite-net
coverage, two pointwise target estimates at the selected center, and the
target metric triangle ledger.
\end{theorem}
\leanchecked{BEDC.Derived.HeineCantorModulusUp.HeineCantorModulusCarrier\_triangle\_handoff}
\begin{proof}
Begin with the finite-net extraction theorem. It gives the selected center
route and compact-metric source coverage rows from the displayed net packet.

Next read the folded precision $M$ from the rows $R$. The fold is finite:
each pointwise radius attached to a center is compared through the rational
ledger, and the accumulator remains a positive history classified by the
source and rational classifiers.

Finally apply the triangle handoff row $T$. The source metric ledger places
both source points inside the pointwise radius of the selected center, the
continuous-map row supplies the two target estimates at that center, and the
target metric triangle ledger returns the displayed $\varepsilon$ estimate.
The continuation row $S$ sends exactly this finite package to
$\UniformModulusUp$.
\end{proof}

\begin{theorem}[Heine-Cantor radius fold]
\label{thm:heine-cantor-modulus-radius-fold}
For an accepted $\HeineCantorModulusUp$ carrier $H$, the folded source
precision $M$ is determined only by the finite center rows $C$, the pointwise
radius rows $R$, and the rational lower-bound fold displayed in those rows.
The extraction has no coordinate for open-cover compactness, a host minimum
operator, a selector for centers, trichotomy on host reals, or host equality of
real points.
\end{theorem}
\leanchecked{BEDC.Derived.HeineCantorModulusUp.HeineCantorModulusCarrier\_radius\_fold}
\begin{proof}
Project $C$, $R$, and $M$ from the carrier. The finite-net extraction theorem
identifies the center spine, so each source radius available to the fold is
already attached to a displayed center row.

Induct over that finite center spine. At the empty boundary the fold reads the
displayed positive source-precision base row. At a successor row, project the
next center, its pointwise continuity radius, and its rational lower-bound
certificate, then combine it with the accumulated positive history by the
finite rational fold.

Componentwise $\hsame$ transport in $Q$ carries the center row, radius row, and
fold accumulator through the source-metric and rational classifiers. Thus the
exported $M$ is a finite-row object of $C$ and $R$ alone, with no appeal to
open-cover data or host-side order case splits.
\end{proof}

\begin{theorem}[Heine-Cantor pointwise radius fold obligation]
\label{thm:heine-cantor-modulus-pointwise-radius-fold}
For an accepted $\HeineCantorModulusUp$ carrier, the rows $C,R,M,Q,S,K,N$ form the complete finite obligation turning pointwise continuity radii at compact-net centers into the $\UniformModulusUp$ source precision. The route reads compact-net centers, pointwise radius rows, the folded precision, componentwise transport, displayed replay, provenance, and local naming; it has no coordinate for open-cover compactness, choice-selected centers, a host minimum, trichotomy on host reals, or host equality of source points.
\end{theorem}
\begin{proof}
Project the carrier rows $C,R,M,Q,S,K,N$. The center rows $C$ and pointwise radius rows $R$ are the only analytic inputs to the source-precision fold, while $Q,S,K,N$ carry transport, replay, provenance, and local naming.

Apply \autoref{thm:heine-cantor-modulus-finite-net-extraction}. It displays the compact-net center spine and coverage rows before any pointwise radius can be consumed by a uniform consumer.

Apply \autoref{thm:heine-cantor-modulus-radius-fold}. The folded precision $M$ is determined by the finite center spine and the pointwise radius rows, with the rational lower-bound fold kept inside the displayed packet.

Finally replay the result through componentwise $\hsame$ transport and $\Cont$ continuation. The $\UniformModulusUp$ source precision therefore comes from the finite rows $C,R,M,Q,S,K,N$ alone, and the excluded open-cover, choice, host-minimum, trichotomy, and host-equality routes remain absent.
\end{proof}

\begin{theorem}[Heine-Cantor UniformModulus export]
\label{thm:heine-cantor-modulus-uniformmodulus-export}
Every $\UniformModulusUp$ consumer accepted by the
$\HeineCantorModulusUp$ chapter factors through the finite rows
$P,C,R,M,T,Q,S,K,N$. The route is finite-net extraction, radius fold, and
metric triangle handoff, rather than an open-cover compactness argument,
choice-selected center data, or host-side real comparison.
\end{theorem}
\begin{proof}
First project the carrier rows. The finite-net extraction theorem reads
$P$ and $C$ as the complete center and coverage surface for the source
precision scale determined by $\varepsilon$.

Next apply the radius-fold theorem. It turns the pointwise rows $R$ into the
single positive source precision $M$ by a finite rational lower-bound fold, and
$Q$ transports the resulting history componentwise across classified
representatives.

Finally apply the triangle handoff theorem. The row $T$ combines compact-net
coverage, the two pointwise target estimates, and the target metric triangle
ledger; the continuation row $S$ presents exactly this package to the
$\UniformModulusUp$ consumer. The provenance row $K$ and local row $N$ record
that no further compactness, selector, or host comparison input is part of the
accepted route.
\end{proof}

\begin{theorem}[Heine-Cantor modulus obligation closure]
\label{thm:heine-cantor-modulus-obligation-closure}
For an accepted $\HeineCantorModulusUp$ carrier $H$, the NameCert obligations
of the chapter are exhausted by the carrier exposure of
\autoref{def:heine-cantor-modulus-carrier}, finite-net extraction
\autoref{thm:heine-cantor-modulus-finite-net-extraction}, positive radius
fold \autoref{thm:heine-cantor-modulus-radius-fold}, metric triangle handoff
\autoref{thm:heine-cantor-modulus-triangle-handoff}, and the
$\UniformModulusUp$ consumer route of
\autoref{thm:heine-cantor-modulus-uniformmodulus-export}. Thus every consumer
read is componentwise over the displayed $\hsame$ row $Q$, replays the same
$\Cont$ route $S$, keeps $\Pkg$ provenance in $K$, and remains under the local
$\NameCert_{\HeineCantorModulusUp}$ row $N$. The package has no coordinate for
open-cover compactness, choice-selected centers, trichotomy, host real
comparison, or a non-displayed compactness principle.
\end{theorem}
\begin{proof}
First project the carrier row by row. The source, target, continuous-map,
precision, finite net, center list, radius list, folded precision, triangle
handoff, transport, continuation, provenance, and local naming rows are exactly
the rows listed in \autoref{def:heine-cantor-modulus-carrier}.

Next read the four constructive routes. Finite-net extraction supplies $P,C$,
the radius fold supplies $M$ from $C,R$, the triangle handoff supplies $T$, and
the UniformModulus export packages $P,C,R,M,T,Q,S,K,N$ for the consumer.

Finally keep the structural rows visible. Componentwise transport is the row
$Q$, replay is the row $S$, provenance is the row $K$, and local naming is the
row $N$. Since all four routes are row projections from the same carrier, no
open-cover, selector, trichotomy, host comparison, or hidden compactness
principle can enter the obligation surface.
\end{proof}

\begin{theorem}[Heine-Cantor net-ledger totality]
\label{thm:heine-cantor-modulus-net-ledger-totality}
Every accepted $\HeineCantorModulusUp$ carrier exposing the compact-metric
source row $X$, finite-net extraction rows $P,C$, continuous-map row $F$,
dyadic radius-fold rows $R,M$, triangle handoff row $T$, and
$\UniformModulusUp$ export rows $Q,S,K,N$ has a complete finite ledger for the
modulus readback. The ledger is exactly the route from compact metric source
data to finite centers, from centers to folded source precision, from folded
precision to triangle handoff, and from the handoff to the
$\UniformModulusUp$ consumer.
\end{theorem}
\begin{proof}
First apply finite-net extraction. The rows $P,C$ expose the complete compact
metric center spine and coverage ledgers, so the readback has a displayed
finite source for every center consumed by the modulus route.

Next apply the radius fold. The rows $R,M$ fold the pointwise continuity
radii attached to that finite center spine into one positive source precision,
using only the displayed dyadic and rational lower-bound data.

Now apply the triangle handoff. The row $T$ combines finite-net coverage with
the pointwise estimates from $F$ and returns the target tolerance estimate for
the folded precision.

Finally use the $\UniformModulusUp$ export rows $Q,S,K,N$. They transport,
replay, record provenance, and locally name the same finite package, so the
modulus readback ledger is complete without invoking open-cover compactness,
sequential compactness, choice-selected centers, or host real comparison.
\end{proof}

\begin{theorem}[Heine-Cantor folded radius positivity]
\label{thm:heine-cantor-modulus-folded-radius-positive}
For an accepted $\HeineCantorModulusUp$ carrier $H$, the finite list of
pointwise radii in row $R$ folds to one positive source precision $M$. The
fold reads the center list $C$, the $\RatUp$ lower-bound rows in $R$, the
finite $\mathsf{ProbeBundle}$ spine supplied by $P$, and the componentwise
transport row $Q$; it does not read an open-cover refinement, a host minimum,
a trichotomy split, or a choice-selected center.
\end{theorem}
\begin{proof}
First project the displayed center spine. By
\autoref{thm:heine-cantor-modulus-finite-net-extraction}, the rows $P,C$
present a finite $\mathsf{ProbeBundle}$ list of centers together with their
membership and coverage ledgers.

Induct over that finite spine. The empty boundary reads the positive base
precision stored in the radius-fold row. At a successor row, project the next
center, its pointwise radius, and its $\RatUp$ lower-bound certificate from
$R$, then fold that positive rational lower bound into the accumulator.

Finally transport the accumulator through $Q$. Since each induction step reads
only the displayed radius and rational rows, the resulting $M$ is positive as a
source precision and no hidden compactness or host-order case split is
introduced.
\end{proof}

\begin{theorem}[Heine-Cantor uniform-consumer projection]
\label{thm:heine-cantor-modulus-uniform-consumer-projection}
Every $\UniformModulusUp$ consumer of an accepted $\HeineCantorModulusUp$
packet must read the rows $X,Y,F,\varepsilon,P,C,R,M,T,Q,S,K,N$ before the
exported modulus is public. The consumer receives the compact source, target
metric, continuous-map data, target precision, finite net, pointwise radii,
folded precision, triangle handoff, componentwise $\hsame$ transport,
displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows as one
finite packet.
\end{theorem}
\begin{proof}
Begin with carrier projection. The carrier definition lists exactly the rows
$X,Y,F,\varepsilon,P,C,R,M,T,Q,S,K,N$, and the local $\NameCert$ row names this
complete packet rather than a detached modulus value.

Next apply the finite constructive routes. Finite-net extraction displays
$P,C$, folded radius positivity displays $R,M$, and the triangle handoff
displays $T$ over the source and target metric rows.

Now apply the export theorem. The $\UniformModulusUp$ consumer can see the
modulus only through the replay row $S$, with $Q,K,N$ preserving transport,
provenance, and local naming for the same packet. Thus no consumer-visible
modulus is public before all listed rows have been read.
\end{proof}

\begin{theorem}[Heine-Cantor triangle-fold nonescape]
\label{thm:heine-cantor-modulus-triangle-fold-nonescape}
For an accepted $\HeineCantorModulusUp$ carrier, the triangle handoff row $T$
consumes only compact-net source estimates, the folded precision $M$, and the
pointwise target estimates supplied through $F$ and $R$. It cannot import
open-cover compactness, sequential compactness, choice-selected centers,
trichotomy, host real equality, or any non-displayed compactness principle.
\end{theorem}
\begin{proof}
First read the compact-net side. The rows $P,C$ supply the finite source
coverage estimates, and
\autoref{thm:heine-cantor-modulus-folded-radius-positive} supplies the folded
positive precision $M$ from the displayed radii.

Next read the target side. The continuous-map row $F$ supplies the two
pointwise target estimates at the selected displayed center, and the target
metric row $Y$ supplies the triangle ledger used by $T$.

Finally compare the available coordinates with the excluded routes. The
carrier has no row for open-cover compactness, sequential compactness, choice,
trichotomy, host real equality, or a hidden compactness principle. Therefore
$T$ remains a finite handoff from compact-net estimates and pointwise target
estimates to the $\UniformModulusUp$ route.
\end{proof}

\begin{theorem}[Heine-Cantor scoped route]
\label{thm:heine-cantor-modulus-scoped-route}
For an accepted $\HeineCantorModulusUp$ carrier, the folded-radius projection, the $\UniformModulusUp$ consumer projection, and the triangle-handoff projection are scoped exactly to $\CompactMetricUp$ finite nets, $\CompactUniformContinuityUp$ pointwise rows, $\ContinuousMapUp$ graph rows, $\UniformModulusUp$ consumer rows, $\MetricTriangleUniformEstimateUp$, $\RatUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The route exports no open-cover compactness, sequential compactness, choice-selected centers, host real equality, trichotomy, or non-displayed compactness principle.
\end{theorem}
\begin{proof}
Project the folded-radius theorem. Its construction reads only the compact-net centers, pointwise-radius data, rational lower-bound fold, and displayed transport rows, so its scope is bounded by the $\CompactMetricUp$, $\CompactUniformContinuityUp$, $\ContinuousMapUp$, $\RatUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ coordinates.

Next project the uniform-consumer theorem. The consumer receives the exported modulus only after the compact source, target metric, continuous-map data, folded precision, replay, provenance, and local naming rows have been read, so the public consumer scope is the displayed $\UniformModulusUp$ row over the same packet.

Finally project the triangle-fold nonescape theorem. The triangle handoff reads the metric triangle estimate and the same displayed finite rows; because the carrier has no coordinates for open-cover compactness, sequential compactness, choice-selected centers, host equality, trichotomy, or hidden compactness, the scoped route is exactly the listed BEDC surface.
\end{proof}

\closureat{HeineCantorModulusUp}{scopedStr}[thm:heine-cantor-modulus-scoped-route; BEDC.Derived.HeineCantorModulusUp.HeineCantorModulusTasteGate\_single\_carrier\_alignment]

\begin{closurestatus}{\HeineCantorModulusUp}
  \constructivestory{A $\HeineCantorModulusUp$ packet is generated from a compact metric finite-net row, pointwise continuous-map modulus rows at the displayed centers, a positive target dyadic precision, a finite rational lower-bound fold, metric triangle handoff data, componentwise $\hsame$ transport, displayed $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{The scoped route records the finite-net carrier, displayed pointwise-radius fold, uniform consumer projection, and triangle handoff over exactly the listed BEDC rows.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.HeineCantorModulusUp.HeineCantorModulusTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, sequential compactness, choice, trichotomy, host real equality, or a non-displayed compactness principle.}
  \upgradepath{Public closure requires a public consumer surface beyond the scoped finite-net and triangle-handoff packet.}
\end{closurestatus}
