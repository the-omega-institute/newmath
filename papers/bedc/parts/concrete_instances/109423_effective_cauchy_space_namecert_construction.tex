\chapter{A Concrete Naming Certificate for $\EffectiveCauchySpaceUp$}
\label{ch:concrete-instances-effective-cauchy-space-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis and represented-space computable analysis, the $\EffectiveCauchySpaceUp$ packet records a represented metric source whose Cauchy access is mediated by displayed moduli and finite observation windows. It sits after the represented-space source of \autoref{ch:concrete-instances-representedspace-namecert}, the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the effective modulus surface of \autoref{ch:concrete-instances-effective-cauchy-modulus-namecert}, the finite stream schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the unit handoff of \autoref{ch:concrete-instances-cauchycompletionunit-namecert}. The chapter gives Real and Completion consumers a finite route from represented names and effective Cauchy moduli to the current L10 completion boundary; it is not quotient completion equality, a selected limit, countable choice, host equality, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.

\begin{definition}[Effective Cauchy space carrier]\label{def:effective-cauchy-space-carrier}
An $\EffectiveCauchySpaceUp$ carrier is a finite $\BHist$ packet
$$
X=(A,M,E,S,R,D,L,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the $\RepresentedSpaceUp$ source row, naming the represented input surface and the representation-mediated observations available to the packet.
\item $M$ is the $\MetricSpaceUp$ row, recording the point classifier, distance requests, and metric comparison data read over the same represented source.
\item $E$ is the $\EffectiveCauchyModulusUp$ row, supplying the finite stage schedule by which metric tolerance requests become Cauchy-window requests.
\item $S$ is the $\StreamNameUp$ schedule row, displaying the finite observation windows opened by the represented source and the effective modulus.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the displayed windows after the metric and modulus rows have fixed the tolerance request.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, storing dyadic radii, slack, comparison budgets, and tolerance conversions used by $E,S,R$.
\item $L$ is the terminal $\RealUp$ seal reached only after the represented source, metric row, effective modulus, stream schedule, regular readback, and dyadic ledger have been displayed.
\item $U$ is the $\CauchyCompletionUnitUp$ handoff row admitting the same represented Cauchy data into the completion-facing unit route.
\item $H$ records componentwise $\hsame$ transport for $A,M,E,S,R,D,L,U$ and the local naming row.
\item $C$ records displayed $\Cont$ replay from a Real or Completion request through the represented source, metric row, modulus, finite windows, readback, tolerance ledger, seal, unit handoff, transport, provenance, and local naming rows.
\item $P$ is the $\Pkg$ provenance over $\EffectiveCauchySpaceUp$, $\RepresentedSpaceUp$, $\MetricSpaceUp$, $\EffectiveCauchyModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\CauchyCompletionUnitUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\EffectiveCauchySpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,M,E,S,R,D,L,U,H,C,P,N$. It has no coordinate for quotient completion equality, selected representatives, countable choice, trichotomy, host equality of completed objects, ambient completeness of $\RealUp$, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Effective Cauchy space NameCert obligations]\label{thm:effective-cauchy-space-namecert-obligations}
For every accepted $\EffectiveCauchySpaceUp$ carrier
$$
X=(A,M,E,S,R,D,L,U,H,C,P,N),
$$
the local $\NameCert_{\EffectiveCauchySpaceUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item represented-source admission through $A$ and metric-source admission through $M$ over the same displayed carrier;
\item effective-modulus exposure through $E$ before any finite stream window is opened;
\item StreamName schedule exactness through $S$, requiring every inspected name window to be one of the displayed finite windows;
\item regular rational readback through $R$ only after the represented source, metric row, modulus, stream schedule, and dyadic ledger have been displayed;
\item dyadic tolerance accounting through $D$ for every radius, slack, comparison budget, and tolerance conversion consumed by $E,S,R$;
\item terminal Real sealing through $L$ and completion-unit handoff through $U$, both downstream of $A,M,E,S,R,D$;
\item transport, replay, provenance, and local naming through $H,C,P,N$ without adding a second classifier or hidden completion coordinate.
\end{enumerate}
No effective-Cauchy-space obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:effective-cauchy-space-carrier}. The represented source row $A$ and the metric row $M$ are the only source coordinates, so every downstream read is tied to the same represented metric carrier.

The effective modulus row $E$ mediates every tolerance request before the finite stream schedule $S$ is opened. The dyadic ledger $D$ supplies the radius and slack accounting needed by the modulus and schedule rows, and the regular rational readback $R$ is downstream of those displayed data.

The terminal routes $L$ and $U$ are read only after $A,M,E,S,R,D$ have been exposed. Thus the Real seal and the CauchyCompletionUnit handoff consume the same finite represented-Cauchy route rather than a quotient completion, selected limit, or host equality datum.

Finally $H,C,P,N$ transport, replay, source, and name the same packet. Since the carrier has no row for countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Effective Cauchy space completion handoff]\label{thm:effective-cauchy-space-completion-handoff}
Every Real-facing or completion-facing consumer of an accepted $\EffectiveCauchySpaceUp$ carrier
$$
X=(A,M,E,S,R,D,L,U,H,C,P,N)
$$
factors through the displayed route
$$
\RepresentedSpaceUp,\ \MetricSpaceUp,\ \EffectiveCauchyModulusUp,\ \StreamNameUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \RealUp,\ \CauchyCompletionUnitUp
$$
with transport, replay, provenance, and local naming supplied only by $H,C,P,N$. The consumer receives no quotient completion equality, selected limit, countable-choice selector, host equality, trichotomy branch, or ambient $\RealUp$ completeness witness.
\end{theorem}
\begin{proof}
Fix a Real-facing or completion-facing consumer of $X$. The carrier exposes the represented source, metric row, effective modulus, stream schedule, dyadic ledger, regular readback, Real seal, and completion-unit handoff as the only analytic coordinates.

The consumer first reads $A$ and $M$, then obtains the tolerance stage from $E$. The corresponding finite windows are displayed in $S$, with all radius and slack conversions accounted for in $D$.

Only after that finite observation route is fixed can the readback row $R$ be consumed. The Real seal $L$ and the CauchyCompletionUnit handoff $U$ are downstream of the same readback and ledger, so both terminal routes inherit the represented Cauchy data rather than selecting an external limit.

The structural rows $H,C,P,N$ preserve the route and introduce no additional analytic coordinate. Therefore every allowed consumer factors through the displayed completion handoff and cannot extract any of the excluded quotient, choice, equality, trichotomy, or ambient-completeness data.
\end{proof}

\falsifiablePrediction{Every accepted $\EffectiveCauchySpaceUp$ Real or Completion read exposes the RepresentedSpace source, MetricSpace row, EffectiveCauchyModulus row, StreamName schedule, DyadicRatCore tolerance ledger, RegSeqRat readback, RealUp seal, CauchyCompletionUnit handoff, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert rows before the terminal handoff is consumed.}

\independenceWitness{representedspace, metric, effective-cauchy-modulus, streamname, regseqrat, dyadicratcore, real, cauchycompletionunit: $\EffectiveCauchySpaceUp$ is not bijective to any listed sibling because it binds a represented metric source, effective Cauchy modulus, finite schedule, tolerance ledger, regular readback, Real seal, completion-unit handoff, transport, replay, provenance, and local naming in one packet.}

\closureat{EffectiveCauchySpaceUp}{seedStr}

\begin{closurestatus}{\EffectiveCauchySpaceUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:effective-cauchy-space-carrier}, \autoref{thm:effective-cauchy-space-namecert-obligations}, and \autoref{thm:effective-cauchy-space-completion-handoff} seed the finite represented-metric Cauchy route from effective modulus data to Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion equality, selected limits, countable choice, host equality, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, represented-source and metric-source agreement, effective-modulus exposure, finite-window exactness, dyadic tolerance accounting, regular readback, Real seal boundary, completion-unit handoff, transport, replay, provenance, and local naming for BEDC.Derived.EffectiveCauchySpaceUp.}
  \constructivestory{An $\EffectiveCauchySpaceUp$ packet is generated from a represented-space source, a metric row, an effective Cauchy modulus, StreamName windows, RegSeqRat readback, a DyadicRatCore tolerance ledger, a RealUp seal, a CauchyCompletionUnit handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
