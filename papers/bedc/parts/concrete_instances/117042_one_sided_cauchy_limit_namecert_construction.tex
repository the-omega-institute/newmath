\chapter{A Concrete Naming Certificate for $\OneSidedCauchyLimitUp$}
\label{ch:concrete-instances-one-sided-cauchy-limit-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\OneSidedCauchyLimitUp$ packet records the one-sided limit surface at which side-tagged approach data is also certified by cofinal Cauchy-window evidence. It refines the side classifier and approach-window rows of \autoref{ch:concrete-instances-one-sided-limit-namecert}, reads Cauchy-space compatibility through $\CauchySpaceUp$ from \autoref{ch:concrete-instances-cauchyspace-namecert}, consumes $\StreamNameUp$ windows from \autoref{ch:concrete-instances-streamname-namecert}, records $\RegSeqRatUp$ readback by \autoref{ch:concrete-instances-regseqrat-namecert}, measures tolerances through $\DyadicRatCoreUp$ from \autoref{ch:concrete-instances-dyadicratcore-namecert}, and reaches the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. The packet is an L10 one-sided Cauchy seal route: it names finite cofinal-window data before any Real-facing consumer may treat a side approach as a completed limit.

\begin{definition}[One-sided Cauchy limit carrier]\label{def:one-sided-cauchy-limit-carrier}
A $\OneSidedCauchyLimitUp$ carrier is a finite $\BHist$ packet
$$
K=(A,L,W,J,S,R,D,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the approach-anchor row inherited from the one-sided source.
\item $L$ is the left-hand or right-hand side classifier read before any Cauchy row is inspected.
\item $W$ is the finite row of side-compatible approach windows.
\item $J$ is the cofinal-window ledger, recording for each displayed tolerance request a later side-compatible window in $W$.
\item $S$ is the $\StreamNameUp$ observation schedule opened only along the cofinal windows in $J$.
\item $R$ is the $\RegSeqRatUp$ readback row for the regular rational observations exposed by $S$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used to compare side windows and regular-rational reads.
\item $Q$ is the $\CauchySpaceUp$ compatibility row asserting that the displayed side-window schedule is Cauchy at the named tolerances.
\item $E$ is the terminal $\RealUp$ seal row reached after $A,L,W,J,S,R,D,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for all carrier rows.
\item $C$ records displayed $\Cont$ replay from the side-tagged approach data through the cofinal Cauchy-window route.
\item $P$ is the $\Pkg$ provenance over $\OneSidedCauchyLimitUp$, $\OneSidedLimitUp$, $\CauchySpaceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\OneSidedCauchyLimitUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,L,W,J,S,R,D,Q,E,H,C,P,N$. It has no coordinate for a two-sided limit theorem, quotient real equality, selected tail, countable-choice schedule, host order trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[One-sided Cauchy limit NameCert obligations]\label{thm:one-sided-cauchy-limit-namecert-obligations}
For every accepted $\OneSidedCauchyLimitUp$ carrier $K=(A,L,W,J,S,R,D,Q,E,H,C,P,N)$, the local $\NameCert_{\OneSidedCauchyLimitUp}$ surface consists exactly of carrier admission for the displayed rows; side classifier stability before Cauchy inspection; cofinal-window coverage for each dyadic tolerance request; $\StreamNameUp$ and $\RegSeqRatUp$ readback along those windows; $\DyadicRatCoreUp$ tolerance exactness; $\CauchySpaceUp$ compatibility for the same schedule; terminal $\RealUp$ sealing; componentwise transport; replay; provenance; and local naming.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:one-sided-cauchy-limit-carrier}. The accepted packet displays the approach anchor, side classifier, finite windows, cofinal-window ledger, stream schedule, regular readback, dyadic tolerance ledger, Cauchy-space row, terminal Real seal, and structural rows.

The side classifier is inspected before the Cauchy row. Thus every cofinal-window entry in $J$ is read as a left-hand or right-hand approach window from $W$, and transport through $H$ preserves that tag while the stream schedule and readback rows are replayed.

The Cauchy obligations are exactly the finite rows $J,S,R,D,Q$. The cofinal ledger $J$ chooses the displayed later windows, $S$ opens only their observations, $R$ records the regular rational data, $D$ records the demanded tolerances, and $Q$ confirms Cauchy-space compatibility for that same visible schedule.

The Real seal $E$ is downstream of the whole route. Since no row stores a two-sided limit theorem, quotient equality, selected tail, countable-choice schedule, trichotomy, or ambient Real completeness, those objects cannot become local obligations of the packet.
\end{proof}

\begin{theorem}[One-sided Cauchy L10 seal route]\label{thm:one-sided-cauchy-limit-l10-seal-route}
Every $\RealUp$ or $\CauchySpaceUp$ consumer of an accepted $\OneSidedCauchyLimitUp$ packet reads the one-sided Cauchy limit only through the finite route
$$
\OneSidedLimitUp,\ \CauchySpaceUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp,\ \RealUp .
$$
In carrier coordinates this route is $A,L,W,J,S,R,D,Q,E,H,C,P,N$. The handoff exports no two-sided limiting principle, quotient stream equality, selected Cauchy tail, countable-choice functional, host real equality, order trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier through \autoref{def:one-sided-cauchy-limit-carrier}. A consumer cannot reach the $\CauchySpaceUp$ row $Q$ before reading the side classifier $L$, the compatible windows $W$, the cofinal-window ledger $J$, the stream schedule $S$, the regular readback $R$, and the dyadic tolerance ledger $D$.

The terminal $\RealUp$ row $E$ is even later in the displayed route. It is sealed only after the same Cauchy-space compatibility row has been exposed, so a Real-facing read cannot bypass the one-sided Cauchy schedule.

The structural rows $H,C,P,N$ preserve, replay, package, and name this finite route. Because the carrier has no coordinate for the excluded host or choice objects, the L10 handoff remains a one-sided Cauchy seal route rather than a two-sided completion or quotient-real construction.
\end{proof}

\falsifiablePrediction{Every $\OneSidedCauchyLimitUp$ Real-facing read factors through a one-sided anchor, side classifier, finite approach windows, cofinal Cauchy-window ledger, StreamName schedule, RegSeqRat readback, DyadicRatCore tolerance ledger, CauchySpace compatibility row, terminal RealUp seal, hsame transport, Cont replay, Pkg provenance, and local naming.}

\independenceWitness{one-sided-limit, cauchyspace, streamname, regseqrat, dyadicratcore, real: $\OneSidedCauchyLimitUp$ is not bijective to any listed sibling because it jointly carries side-tagged approach data and cofinal Cauchy-window certification before the terminal Real seal is exposed.}

\closureat{OneSidedCauchyLimitUp}{seedStr}

\begin{closurestatus}{\OneSidedCauchyLimitUp}
  \constructivestory{A $\OneSidedCauchyLimitUp$ packet is generated from a one-sided approach anchor, a left/right side classifier, finite compatible windows, a cofinal Cauchy-window ledger, StreamName schedules, RegSeqRat readbacks, DyadicRatCore tolerances, CauchySpace compatibility, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:one-sided-cauchy-limit-carrier}, \autoref{thm:one-sided-cauchy-limit-namecert-obligations}, and \autoref{thm:one-sided-cauchy-limit-l10-seal-route} seed the finite one-sided Cauchy route from side-tagged approach windows to a terminal Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close two-sided limits, quotient stream equality, selected Cauchy tails, countable choice, host real equality, order trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, side classifier stability, cofinal-window coverage, StreamName and RegSeqRat readback, DyadicRatCore tolerance exactness, CauchySpace compatibility, Real seal nonescape, transport, replay, provenance, and local naming for $\OneSidedCauchyLimitUp$.}
\end{closurestatus}
