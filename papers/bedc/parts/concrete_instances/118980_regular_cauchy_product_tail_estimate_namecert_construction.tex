\chapter{A Concrete Naming Certificate for $\RegularCauchyProductTailEstimateUp$}
\label{ch:concrete-instances-regular-cauchy-product-tail-estimate-namecert}
\origin{human}

Following Cauchy and Hardy, the $\RegularCauchyProductTailEstimateUp$ packet records the finite estimate surface needed before a product of regular Cauchy names can expose a Real-facing tail bound. It refines the product-tail budget route of \autoref{ch:concrete-instances-cauchy-product-tail-budget-namecert}, consumes the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and remains attached to the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the product convergence and metric routes of \autoref{ch:concrete-instances-cauchy-product-convergence-namecert} and \autoref{ch:concrete-instances-cauchy-product-metric-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite NameCert surface for product-tail estimates; it does not assert a selected infinite product, quotient equality of series, choice, trichotomy, host completed multiplication, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy product-tail estimate carrier]
\label{def:regular-cauchy-product-tail-estimate-carrier}
A $\RegularCauchyProductTailEstimateUp$ carrier is a finite $\BHist$ packet
$$
E=(A,B,J,L,R,M,D,Q,Z,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ and $B$ are the two $\RegSeqRatUp$ source rows whose finite rational windows are inspected.
\item $J$ is the $\CauchyProductTailBudgetUp$ schedule naming the left tail, right tail, and mixed finite convolution windows allowed for the requested precision.
\item $L$ and $R$ are the left and right regular-tail ledgers read from $A$ and $B$ under the schedule $J$.
\item $M$ is the mixed-window convolution ledger, containing only finitely many rational products and finite partial sums.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger assigning dyadic radii to $L$, $R$, and $M$.
\item $Q$ is the $\RegSeqRatUp$ readback row produced from the displayed tail estimate.
\item $Z$ is the terminal $\RealUp$ seal row, exposed only after $A,B,J,L,R,M,D,Q$ are visible.
\item $H$ records componentwise $\hsame$ transport for the source rows, schedule, tail ledgers, mixed window, dyadic ledger, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the same finite product-tail estimate route.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyProductTailEstimateUp$, $\CauchyProductTailBudgetUp$, $\CauchyProductUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RegularCauchyProductTailEstimateUp}$ row.
\end{enumerate}
The classifier reads only these rows. It has no coordinate for a chosen infinite product, quotient equality of regular names, countable choice, trichotomy, host multiplication on completed reals, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Regular Cauchy product-tail estimate NameCert obligations]
\label{thm:regular-cauchy-product-tail-estimate-namecert-obligations}
For every accepted $\RegularCauchyProductTailEstimateUp$ carrier $E=(A,B,J,L,R,M,D,Q,Z,H,C,P,N)$, the local $\NameCert_{\RegularCauchyProductTailEstimateUp}$ surface consists exactly of carrier admission, two $\RegSeqRatUp$ source exposures, product-tail budget scheduling, left-tail and right-tail ledger exposure, mixed finite-window convolution, $\DyadicRatCoreUp$ tolerance accounting, $\RegSeqRatUp$ readback, terminal $\RealUp$ seal nonescape, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-product-tail-estimate-carrier}. The two source rows $A,B$, the budget row $J$, the tail ledgers $L,R$, the mixed window $M$, and the dyadic ledger $D$ are the only numerical rows available before readback.

The readback row $Q$ is downstream of those finite rows. It repacks the displayed source windows and product-tail estimate as regular rational data rather than as quotient equality of regular names or a chosen infinite product.

Finally inspect $Z,H,C,P,N$. The Real seal $Z$ is reached only after the source, schedule, tail-ledger, mixed-window, dyadic, and readback rows have been displayed, while the structural rows preserve the same packet. Hence the listed rows exhaust the local NameCert obligation surface.
\end{proof}

\begin{theorem}[Regular Cauchy product-tail estimate Real-seal route]
\label{thm:regular-cauchy-product-tail-estimate-real-seal-route}
Every Real-facing read of an accepted $\RegularCauchyProductTailEstimateUp$ carrier factors through the finite route
$$
\RegSeqRatUp,\ \CauchyProductTailBudgetUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp \longmapsto \RealUp
$$
with the displayed left-tail ledger, right-tail ledger, mixed finite convolution window, componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows still attached. The route exports no selected infinite product, quotient equality of regular names, countable choice, trichotomy row, host completed multiplication, or ambient $\RealUp$ completeness witness.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:regular-cauchy-product-tail-estimate-namecert-obligations}. It makes the two regular Cauchy sources, product-tail schedule, left and right tail ledgers, mixed finite convolution row, dyadic tolerance ledger, and regular readback mandatory before the Real seal can be consumed.

The finite route through $J,L,R,M,D,Q$ controls the estimate request. A consumer may inspect only the displayed source windows, tail rows, mixed products, and dyadic tolerances assigned to the requested precision; no coordinate supplies an external selector or host infinite product.

The structural rows $H,C,P,N$ preserve this same route under transport, replay, provenance, and naming. Therefore any Real-facing read reaches $Z$ only through the displayed BEDC rows and cannot escape to quotient equality, choice, trichotomy, host multiplication, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyProductTailEstimateUp$ endpoint read exposes two RegSeqRat source rows, a CauchyProductTailBudget schedule, left and right finite tail ledgers, a mixed finite convolution window, a DyadicRatCore tolerance ledger, RegSeqRat readback, a Real seal, hsame transport, Cont replay, Pkg provenance, and local NameCert evidence before any product-tail estimate is public.}

\independenceWitness{cauchy-product-tail-budget, regseqrat, dyadicratcore, real: $\RegularCauchyProductTailEstimateUp$ is not bijective to any listed sibling because it jointly carries two regular Cauchy sources, a product-tail schedule, two tail ledgers, a mixed finite convolution row, dyadic tolerance accounting, regular readback, terminal Real sealing, transport, replay, provenance, and local naming in one estimate packet.}

\closureat{RegularCauchyProductTailEstimateUp}{seedStr}

\begin{closurestatus}{\RegularCauchyProductTailEstimateUp}
  \constructivestory{A $\RegularCauchyProductTailEstimateUp$ packet is generated from two RegSeqRat source rows, a CauchyProductTailBudget schedule, finite tail ledgers, a mixed convolution window, a DyadicRatCore tolerance ledger, RegSeqRat readback, a terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-product-tail-estimate-carrier}, \autoref{thm:regular-cauchy-product-tail-estimate-namecert-obligations}, and \autoref{thm:regular-cauchy-product-tail-estimate-real-seal-route} seed the finite Cauchy-product tail-estimate route over displayed regular sources, product-tail budgets, dyadic tolerance ledgers, readback, Real-seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, selected infinite products, quotient equality of regular names, countable choice, trichotomy, host completed multiplication, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, product-tail scheduling, left and right tail-ledger exposure, mixed finite-window convolution, dyadic tolerance accounting, RegSeqRat readback, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyProductTailEstimateUp.}
\end{closurestatus}
