\chapter{A Concrete Naming Certificate for $\LocatedBolzanoWeierstrassUp$}
\label{ch:concrete-instances-located-bolzano-weierstrass-namecert}
\origin{human}

Following Bishop and Bridges on compact intervals and located reals, the $\LocatedBolzanoWeierstrassUp$ packet records the finite subsequence-extraction surface available for bounded located $\RealUp$ sequences. It sits over located membership data from \autoref{ch:concrete-instances-locatedset-namecert}, the sequential completeness route of \autoref{ch:concrete-instances-sequentiallycompletemetric-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite window schedule of \autoref{ch:concrete-instances-streamname-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names located finite windows and extracted regular subsequence reads; it does not claim open-cover compactness, choice-selected infinite subsequences, quotient stream equality, excluded-middle trichotomy, or ambient classical compactness.

\begin{definition}[Located Bolzano-Weierstrass carrier]
\label{def:located-bolzano-weierstrass-carrier}
A $\LocatedBolzanoWeierstrassUp$ carrier is a finite $\BHist$ packet
$$
B=(S,L,M,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the bounded source sequence row, presenting the finite $\StreamNameUp$ windows of a located $\RealUp$ sequence together with its displayed lower and upper bounds.
\item $L$ is the $\LocatedSetUp$ row, recording the located interval or located subset surface in which the sequence terms are read.
\item $M$ is the $\SequentiallyCompleteMetricUp$ row, exposing only the sequential metric-completeness interface needed by the extracted regular reads.
\item $W$ is the finite window row of candidate indices and rational brackets inspected before the subsequence read is named.
\item $R$ is the $\RegSeqRatUp$ readback row generated from the accepted windows and bounds.
\item $E$ is the terminal $\RealUp$ seal row for the extracted finite-window read.
\item $H$ records componentwise $\hsame$ transport across source sequence, located-set, completeness, window, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the extraction route
$$
S,L,M,W \longmapsto R \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\LocatedBolzanoWeierstrassUp$, $\LocatedSetUp$, $\SequentiallyCompleteMetricUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedBolzanoWeierstrassUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,L,M,W,R,E,H,C,P,N$. It has no coordinate for open-cover compactness, selected infinite subsequences, quotient equality of streams, countable choice, host real equality, or ambient classical compactness.
\end{definition}

\begin{theorem}[Located Bolzano-Weierstrass carrier obligations]
\label{thm:located-bolzano-weierstrass-carrier-obligations}
For every accepted $\LocatedBolzanoWeierstrassUp$ carrier
$$
B=(S,L,M,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\LocatedBolzanoWeierstrassUp}$ surface consists exactly of carrier admission for the bounded source sequence, $\LocatedSetUp$ row, $\SequentiallyCompleteMetricUp$ row, finite candidate windows, $\RegSeqRatUp$ readback, $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every bounded located sequence consumer must pass through these rows before using the packet as a finite subsequence-extraction certificate.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-bolzano-weierstrass-carrier}. The accepted packet exposes exactly the bounded sequence, located-set, sequential-completeness, finite-window, readback, seal, transport, replay, provenance, and local naming rows.

The located-set row supplies the finite located membership and distance surface. The sequential-completeness row supplies only the metric route needed for regular readbacks, while $W$ records the inspected candidate windows.

The readback row $R$ and Real seal $E$ are downstream of those finite observations. Transport through $H$ and replay through $C$ preserve the same displayed route, and $P,N$ source and name it.

Since the carrier has no coordinate for the excluded compactness, selection, quotient, choice, or host equality data, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Located Bolzano-Weierstrass finite-window subsequence]
\label{thm:located-bolzano-weierstrass-finite-window-subsequence}
Let $B=(S,L,M,W,R,E,H,C,P,N)$ be an accepted $\LocatedBolzanoWeierstrassUp$ carrier. Every finite-window subsequence read exported by $B$ factors through
$$
S,L,M,W \longmapsto R \longmapsto E \longmapsto H,C,P,N .
$$
Consequently downstream compactness, completion, or limit consumers receive a bounded located source row, a $\LocatedSetUp$ route, a $\SequentiallyCompleteMetricUp$ route, finite candidate windows, $\RegSeqRatUp$ readback, a terminal $\RealUp$ seal, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ data, but no open-cover compactness proof, choice-selected infinite subsequence, quotient stream equality, or ambient classical compactness principle.
\end{theorem}
\begin{proof}
Begin with an exported finite-window subsequence read. Projection exposes the ordered carrier rows $S,L,M,W,R,E,H,C,P,N$.

The read starts from the bounded source sequence row $S$ and located-set row $L$. The sequential-completeness row $M$ is read only as a metric route, not as a global compactness assertion.

The finite candidate window $W$ determines the regular rational readback $R$, and the terminal Real seal $E$ is visible only after that readback. The structural rows $H,C,P,N$ then transport, replay, source, and name the same route.

Thus every downstream consumer receives the displayed finite subsequence-extraction surface and none of the excluded selection, quotient, choice, compactness, or host-equality data.
\end{proof}

\closureat{LocatedBolzanoWeierstrassUp}{seedStr}

\begin{closurestatus}{\LocatedBolzanoWeierstrassUp}
  \constructivestory{A $\LocatedBolzanoWeierstrassUp$ packet is generated from bounded located Real sequence windows, LocatedSet membership data, SequentiallyCompleteMetric route data, finite candidate windows, RegSeqRat readback, a RealUp seal, hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-bolzano-weierstrass-carrier}, \autoref{thm:located-bolzano-weierstrass-carrier-obligations}, and \autoref{thm:located-bolzano-weierstrass-finite-window-subsequence} seed the finite located subsequence-extraction surface for bounded located Real sequences.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, choice-selected infinite subsequences, quotient stream equality, countable choice, host real equality, ambient classical compactness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located-window stability, sequential-completeness route stability, regular readback exactness, Real seal handoff, and downstream nonescape for BEDC.Derived.LocatedBolzanoWeierstrassUp.}
\end{closurestatus}
