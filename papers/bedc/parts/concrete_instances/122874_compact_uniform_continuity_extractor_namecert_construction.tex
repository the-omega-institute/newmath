\chapter{A Concrete Naming Certificate for $\CompactUniformContinuityExtractorUp$}
\label{ch:concrete-instances-compact-uniform-continuity-extractor-namecert}
\origin{human}

Following Bishop and Bridges, the $\CompactUniformContinuityExtractorUp$ packet records the finite-net extraction route by which a displayed continuous map on a compact metric source yields a uniform-continuity budget only through inspected compact, metric, rational, and real-name rows. It sits after the compact carrier of \autoref{ch:concrete-instances-compact-namecert}, the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, the rational carrier of \autoref{ch:concrete-instances-rat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a constructive finite-net extractor; it is not a host Heine--Cantor theorem, a selected infinite cover, quotient function equality, countable choice, or ambient completeness for $\RealUp$.

\begin{definition}[Compact uniform-continuity extractor carrier]
\label{def:compact-uniform-continuity-extractor-carrier}
A $\CompactUniformContinuityExtractorUp$ carrier is a finite $\BHist$ packet
$$
E=(K,M,F,Q,R,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactUp$ source row, carrying the finite cover or finite-net admission surface available to the extractor.
\item $M$ is the $\MetricUp$ source row on the same displayed carrier as $K$.
\item $F$ is the $\ContinuousMapUp$ row whose local continuity reads are inspected only over the compact-metric source.
\item $Q$ is the $\RatUp$ radius and tolerance ledger used to compare finite net cells.
\item $R$ is the $\RealUp$ distance and value-readback seal reached from the rational ledger.
\item $W$ is the finite-net witness row: it records the chosen inspected net cells, the local continuity budgets attached to them, and the common modulus bound exported to consumers.
\item $H$ records componentwise $\hsame$ transport for $K,M,F,Q,R,W,C,P,N$.
\item $C$ records displayed $\Cont$ replay from compact-metric admission through local continuity, finite-net comparison, real readback, and exported uniform budget.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformContinuityExtractorUp$, $\CompactUp$, $\MetricUp$, $\ContinuousMapUp$, $\RatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformContinuityExtractorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for an ambient compactness oracle, a host function space, an infinite cover selector, quotient equality of maps, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact uniform-continuity extractor NameCert obligations]
\label{thm:compact-uniform-continuity-extractor-namecert-obligations}
For every accepted $\CompactUniformContinuityExtractorUp$ carrier
$$
E=(K,M,F,Q,R,W,H,C,P,N),
$$
the local $\NameCert_{\CompactUniformContinuityExtractorUp}$ surface consists exactly of compact-source admission through $K$, metric-source admission through $M$, continuous-map local reads through $F$, rational radius and tolerance comparison through $Q$, real-name distance and value seals through $R$, finite-net extraction through $W$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-continuity-extractor-carrier}. The source rows $K,M,F$ make compactness, metric distance, and local continuity visible before any uniform budget can be read.

The extraction row is $W$. It is reached only after the rational ledger $Q$ and the real-name readback row $R$ have displayed the finite radii, tolerance comparisons, and value-distance seals needed by the finite-net argument. Thus a consumer cannot replace the extractor by an ambient compactness theorem or by a selected infinite cover.

The structural rows preserve the same route. Transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$ export the finite packet named above. Since the carrier has no coordinate for host function equality, countable choice, or ambient $\RealUp$ completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactUniformContinuityExtractorUp$ read exposes $\CompactUp$, $\MetricUp$, $\ContinuousMapUp$, $\RatUp$, $\RealUp$, finite-net extraction, componentwise transport, displayed replay, provenance, and local naming before any uniform-continuity budget is public.}

\independenceWitness{compact, metric, continuousmap, rat, real: $\CompactUniformContinuityExtractorUp$ is not bijective to any listed sibling because it jointly binds compact finite-net admission, metric distance, continuous-map local reads, rational tolerance comparison, Real readback, finite-net extraction, transport, replay, provenance, and local NameCert rows.}

\closureat{CompactUniformContinuityExtractorUp}{seedStr}

\begin{closurestatus}{\CompactUniformContinuityExtractorUp}
  \constructivestory{A $\CompactUniformContinuityExtractorUp$ packet is generated from a compact source row, a metric source row, a continuous-map row, rational radius and tolerance rows, real-name distance and value seals, finite-net extraction, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-continuity-extractor-carrier} and \autoref{thm:compact-uniform-continuity-extractor-namecert-obligations} bind the finite-net extractor surface over compact, metric, continuous-map, rational, real-name, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close Lean verification, bridge checking, a host Heine--Cantor theorem, quotient equality of maps, selected infinite covers, countable choice, or ambient Real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source visibility, metric-source visibility, continuous-map local-read routing, rational tolerance comparison, real-name readback, finite-net extraction, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformContinuityExtractorUp.}
\end{closurestatus}
