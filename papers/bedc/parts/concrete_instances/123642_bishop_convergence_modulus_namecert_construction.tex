\chapter{A Concrete Naming Certificate for $\BishopConvergenceModulusUp$}
\label{ch:concrete-instances-bishop-convergence-modulus-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopConvergenceModulusUp$ packet records explicit convergence-modulus evidence for a regular rational name before the completed real is consumed. It sits over the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the completion-facing boundary of \autoref{ch:concrete-instances-completion-namecert}. The packet names a constructive modulus rather than a selected limit, quotient of streams, countable-choice witness, trichotomy argument, or ambient completeness principle.

\begin{definition}[Bishop convergence modulus carrier]
\label{def:bishop-convergence-modulus-carrier}
A $\BishopConvergenceModulusUp$ carrier is a finite $\BHist$ packet
$$
B=(D,W,R,M,E,K,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for the requested precision.
\item $W$ is the $\StreamNameUp$ finite-window row naming the tail windows inspected for that tolerance.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from $W$.
\item $M$ is the displayed convergence-modulus row that sends each tolerance request to a late enough window.
\item $E$ is the terminal $\RealUp$ seal reached only after $D,W,R,M$ have been displayed.
\item $K$ is the $\CompletionUp$ consumer boundary that reads the same displayed modulus route.
\item $H$ records componentwise $\hsame$ transport of the tolerance, window, readback, modulus, real-seal, and completion-boundary rows.
\item $C$ records displayed $\Cont$ replay from dyadic tolerances through stream windows and regular readback to the Real and Completion rows.
\item $P$ records $\Pkg$ provenance over $\BishopConvergenceModulusUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\CompletionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopConvergenceModulusUp}$ row.
\end{enumerate}
The carrier has no coordinate for host limits, quotient stream equality, countable choice, trichotomy, or ambient completeness.
\end{definition}

\begin{theorem}[Bishop convergence modulus NameCert obligations]
\label{thm:bishop-convergence-modulus-namecert-obligations}
For every accepted $\BishopConvergenceModulusUp$ carrier $B=(D,W,R,M,E,K,H,C,P,N)$, the local $\NameCert_{\BishopConvergenceModulusUp}$ surface consists exactly of dyadic tolerance admission, finite-window exposure, regular rational readback, displayed modulus evidence, terminal Real seal handoff, Completion consumer boundary, componentwise transport, displayed replay, provenance, and local naming. Every Real or Completion consumer must read the route
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp,\quad \CompletionUp,\quad \BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert
$$
through these rows and cannot replace them by a selected limit, quotient stream, countable-choice modulus, trichotomy argument, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-convergence-modulus-carrier}. Its visible rows are precisely $D,W,R,M,E,K,H,C,P,N$.

The tolerance row $D$ determines which dyadic request is being answered, and the finite-window row $W$ displays the tail region inspected for that request. The readback row $R$ is downstream of those windows, while $M$ records the convergence-modulus evidence that the displayed tail is late enough.

Only after $D,W,R,M$ have been displayed may the terminal Real seal $E$ and the Completion boundary $K$ be read. The rows $H,C,P,N$ transport, replay, source, and name the same route.

Since the carrier contains no coordinate for host limits, quotient streams, countable choice, trichotomy, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Bishop convergence modulus dependency route]
\label{thm:bishop-convergence-modulus-dependency-route}
Every accepted $\BishopConvergenceModulusUp$ packet exposes convergence evidence only through the ordered dependency route
$$
\DyadicRatCoreUp\longmapsto\StreamNameUp\longmapsto\RegSeqRatUp\longmapsto\RealUp\longmapsto\CompletionUp .
$$
The route keeps the modulus row $M$ between regular rational readback and terminal Real or Completion consumption, so no consumer can read a completed real first and recover a modulus afterward.
\end{theorem}
\begin{proof}
Read $D,W,R,M,E,K$ from the carrier. The row $D$ supplies the dyadic tolerance, $W$ supplies the inspected window, and $R$ supplies the regular rational readback visible on that window.

The modulus row $M$ is placed before $E$ and $K$ in the same packet. Therefore a consumer that reaches $\RealUp$ or $\CompletionUp$ has already passed through the displayed modulus evidence.

The structural rows $H,C,P,N$ preserve this order under transport, replay, provenance, and local naming. There is no row that permits a later selected limit or quotient representative to manufacture the modulus.
\end{proof}

\begin{theorem}[Bishop convergence modulus regular Cauchy readback]
\label{thm:bishop-convergence-modulus-regular-cauchy-readback}
For an accepted $\BishopConvergenceModulusUp$ carrier $B=(D,W,R,M,E,K,H,C,P,N)$, the regular Cauchy readback used by the modulus is exactly the $\RegSeqRatUp$ row $R$ displayed after the dyadic tolerance row $D$ and the finite $\StreamNameUp$ window row $W$. The modulus cannot be read from $\RealUp$ equality, a completion operator, a selected representative, quotient stream equality, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Project $B$ and inspect the readback row $R$. By the carrier definition, $R$ is obtained from the displayed stream window $W$ under the dyadic tolerance ledger $D$.

Apply the NameCert obligations of \autoref{thm:bishop-convergence-modulus-namecert-obligations}. They force $M$ to be read as explicit convergence-modulus evidence before the Real seal $E$ or Completion boundary $K$ is public.

Thus the regular Cauchy readback remains a visible $\RegSeqRatUp$ row inside the packet. Any route through completed-real equality, a selected representative, quotient stream equality, countable choice, or ambient completeness would require a coordinate absent from the carrier.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopConvergenceModulusUp$ Real or Completion consumer reads DyadicRatCore tolerance, StreamName finite-window, RegSeqRat readback, displayed modulus, Real seal, Completion boundary, hsame transport, Cont replay, Pkg provenance, and local NameCert rows before using convergence evidence.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, completion: no listed sibling carries the full packet $B=(D,W,R,M,E,K,H,C,P,N)$, because the siblings separately supply tolerance arithmetic, finite windows, regular rational readback, terminal Real sealing, and Completion consumption while this packet binds them into one explicit convergence-modulus route.}

\closureat{BishopConvergenceModulusUp}{seedStr}

\begin{closurestatus}{\BishopConvergenceModulusUp}
  \constructivestory{A $\BishopConvergenceModulusUp$ packet is generated from DyadicRatCore tolerance rows, StreamName finite windows, RegSeqRat readback, a displayed convergence-modulus row, RealUp sealing, CompletionUp consumption, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-convergence-modulus-carrier}, \autoref{thm:bishop-convergence-modulus-namecert-obligations}, \autoref{thm:bishop-convergence-modulus-dependency-route}, and \autoref{thm:bishop-convergence-modulus-regular-cauchy-readback} seed the explicit convergence-modulus route over DyadicRatCore, StreamName, RegSeqRat, Real, and Completion rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected limits, quotient stream equality, countable choice, trichotomy, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic tolerance exposure, finite-window exposure, regular readback, displayed modulus exactness, Real seal handoff, Completion consumer boundary, transport, replay, provenance, and local naming for BEDC.Derived.BishopConvergenceModulusUp.}
\end{closurestatus}
