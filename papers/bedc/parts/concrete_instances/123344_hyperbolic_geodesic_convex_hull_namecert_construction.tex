\chapter{A Concrete Naming Certificate for $\HyperbolicGeodesicConvexHullUp$}
\label{ch:concrete-instances-hyperbolic-geodesic-convex-hull-namecert}
\origin{human}

Following the Poincare disk model and Gromov hyperbolic geometry, the $\HyperbolicGeodesicConvexHullUp$ packet records finite geodesic-convex-hull membership as BEDC NameCert data. It sits over the finite metric readback of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the boundary-geodesic route of \autoref{ch:concrete-instances-hyperbolic-geodesic-boundary-namecert}, the disk projection row of \autoref{ch:concrete-instances-poincare-disk-geodesic-projection-namecert}, the segment-inside-target service of \autoref{ch:concrete-instances-finite-geodesic-convexity-namecert}, and the visual boundary and Gromov-product surfaces of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert} and \autoref{ch:concrete-instances-hyperbolic-gromov-product-namecert}. The packet names a finite hull-membership route for displayed endpoints and geodesic sides; it does not select a global hull, quotient ideal-boundary equality, host path equality, projection existence, or ambient hyperbolic completion.

\begin{definition}[Hyperbolic geodesic convex-hull packet]
\label{def:hyperbolic-geodesic-convex-hull-packet}
A $\HyperbolicGeodesicConvexHullUp$ carrier is a finite $\BHist$ packet
$$
H=(M,B,G,K,V,Q,R,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\HyperbolicMetricUp$ metric-read row, exposing only the finite distance comparisons inspected by the hull request.
\item $B$ is the $\HyperbolicGeodesicBoundaryUp$ endpoint row, recording the displayed boundary or disk endpoints whose geodesic sides generate the hull candidate.
\item $G$ is the finite geodesic-side ledger, read through the $\PoincareDiskGeodesicProjectionUp$ and $\PoincareDiskMetricUp$ surfaces.
\item $K$ is the candidate membership row, naming the inspected point or finite family of points whose membership in the geodesic hull is tested.
\item $V$ is the $\FiniteGeodesicConvexityUp$ row proving that the displayed segment reads remain inside the named convex target.
\item $Q$ is the boundary-readback row through $\HyperbolicVisualBoundaryUp$ and $\HyperbolicGromovProductUp$.
\item $R$ is the terminal $\RealUp$ readback row for distance, cross-ratio, visual-radius, or comparison budgets consumed by the route.
\item $T$ records componentwise $\hsame$ transport for metric, boundary, geodesic-side, membership, convexity, boundary-readback, Real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a hull-membership request through $M,B,G,K,V,Q,R,T,P,N$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicGeodesicConvexHullUp$, $\HyperbolicMetricUp$, $\HyperbolicGeodesicBoundaryUp$, $\PoincareDiskGeodesicProjectionUp$, $\PoincareDiskMetricUp$, $\FiniteGeodesicConvexityUp$, $\HyperbolicVisualBoundaryUp$, $\HyperbolicGromovProductUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HyperbolicGeodesicConvexHullUp}$ row.
\end{enumerate}
The carrier compares accepted packets only through $M,B,G,K,V,Q,R,T,C,P,N$. It has no coordinate for a host hyperbolic plane, quotient ideal-boundary equality, selected global geodesics, host path equality, projection selectors, or ambient metric completion.
\end{definition}

\begin{theorem}[Hyperbolic geodesic convex-hull route]
\label{thm:hyperbolic-geodesic-convex-hull-route}
Every accepted $\HyperbolicGeodesicConvexHullUp$ hull-membership read first exposes the displayed route
$$
\HyperbolicMetricUp,\ \HyperbolicGeodesicBoundaryUp,\ \PoincareDiskGeodesicProjectionUp,\ \FiniteGeodesicConvexityUp
$$
inside the rows $M,B,G,V$ before the membership row $K$ can be consumed as geodesic convex-hull evidence. The route exports a finite metric and geodesic-boundary certificate, not a selected global hull, quotient boundary object, host geodesic path, projection selector, or ambient hyperbolic completion.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-geodesic-convex-hull-packet}. The metric row $M$, boundary endpoint row $B$, geodesic-side row $G$, candidate row $K$, and finite convexity row $V$ are the only analytic rows before boundary readback and Real sealing.

Read $M$ through \autoref{ch:concrete-instances-hyperbolic-metric-namecert} and $B$ through \autoref{ch:concrete-instances-hyperbolic-geodesic-boundary-namecert}. These rows fix the displayed metric observations and endpoint incidence before a hull side can be named.

Next consume the Poincare disk geodesic projection of \autoref{ch:concrete-instances-poincare-disk-geodesic-projection-namecert} and the finite geodesic convexity row of \autoref{ch:concrete-instances-finite-geodesic-convexity-namecert}. They certify only the inspected sides and segment-inside-target route.

Finally use $T,C,P,N$ to transport, replay, source, and name the same rows. Since the carrier has no row for host geodesics, quotient boundary equality, selected global hulls, projection selectors, or ambient completion, the displayed route is exhaustive.
\end{proof}

\begin{theorem}[Hyperbolic geodesic convex-hull boundary readback]
\label{thm:hyperbolic-geodesic-convex-hull-boundary-readback}
Every boundary or visual readback exported by an accepted $\HyperbolicGeodesicConvexHullUp$ packet factors through $Q$ after the metric, boundary-geodesic, geodesic-side, candidate, and finite-convexity rows $M,B,G,K,V$ have been displayed. The readback may use $\HyperbolicVisualBoundaryUp$ and $\HyperbolicGromovProductUp$, but it remains a finite boundary comparison route and does not create an ideal-boundary quotient, selected endpoint at infinity, host cross-ratio equality, or completed hyperbolic surface.
\end{theorem}
\begin{proof}
Start with the hull route of \autoref{thm:hyperbolic-geodesic-convex-hull-route}. It fixes $M,B,G,V$ and places the candidate membership row $K$ downstream of the displayed geodesic sides.

Now read the visual boundary and Gromov-product rows through \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert} and \autoref{ch:concrete-instances-hyperbolic-gromov-product-namecert}. These rows provide only finite boundary comparisons and basepointed metric readbacks.

The structural rows $T,C,P,N$ preserve that route under transport, continuation replay, provenance, and local naming. No carrier coordinate names quotient boundary equality, a selected endpoint at infinity, host cross-ratio equality, or a completed hyperbolic surface, so the boundary readback cannot escape the finite packet.
\end{proof}

\begin{theorem}[Hyperbolic geodesic convex-hull nonescape]
\label{thm:hyperbolic-geodesic-convex-hull-nonescape}
Every $\RealUp$ or metric-facing consumer of an accepted $\HyperbolicGeodesicConvexHullUp$ packet must read the displayed rows
$$
M,B,G,K,V,Q,R,T,C,P,N
$$
before using the packet as geodesic convex-hull data. The terminal row $R$ seals only the finite metric, boundary, geodesic-side, convexity, and visual-readback route; it exports no host hyperbolic plane, selected geodesic family, quotient path equality, projection theorem, global hull operator, or ambient completion.
\end{theorem}
\begin{proof}
Apply the boundary readback theorem \autoref{thm:hyperbolic-geodesic-convex-hull-boundary-readback}. It places $Q$ after the metric, boundary, side, candidate, and convexity rows.

Read the terminal Real-facing row $R$ only after that route has been displayed. The scalar comparisons consumed by the hull request are therefore attached to $M,B,G,K,V,Q$ and not to a host hyperbolic surface.

Finally replay $T,C,P,N$. Componentwise transport, displayed continuation, package provenance, and local naming preserve the same finite packet. Since the carrier has no coordinate for a selected global hull, quotient path equality, global geodesic family, projection theorem, or ambient completion, no consumer can extract those objects from the seal.
\end{proof}

\closureat{HyperbolicGeodesicConvexHullUp}{seedStr}

\begin{closurestatus}{\HyperbolicGeodesicConvexHullUp}
  \constructivestory{A $\HyperbolicGeodesicConvexHullUp$ packet is generated from HyperbolicMetric reads, HyperbolicGeodesicBoundary endpoint rows, Poincare disk geodesic-side projections, candidate membership, FiniteGeodesicConvexity routing, visual-boundary and Gromov-product readback, terminal Real sealing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-geodesic-convex-hull-packet}, \autoref{thm:hyperbolic-geodesic-convex-hull-route}, \autoref{thm:hyperbolic-geodesic-convex-hull-boundary-readback}, and \autoref{thm:hyperbolic-geodesic-convex-hull-nonescape} seed the finite geodesic convex-hull route over metric, boundary, projection, convexity, visual-readback, Real-seal, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host hyperbolic planes, quotient ideal-boundary equality, selected global geodesics, quotient path equality, projection existence, global hull operators, ambient completion, Lean verification, or bridge checking for $\HyperbolicGeodesicConvexHullUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, endpoint exposure, geodesic-side exactness, candidate membership routing, finite convexity handoff, visual-boundary readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicGeodesicConvexHullUp.}
\end{closurestatus}
