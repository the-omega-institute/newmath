\chapter{A Concrete Naming Certificate for $\ComputableUniformContinuityUp$}
\label{ch:concrete-instances-computable-uniform-continuity-namecert}
\origin{human}

Following Bishop--Bridges and Weihrauch computable analysis, the $\ComputableUniformContinuityUp$ packet records the finite BEDC surface by which a represented-space map exposes one computable modulus route before any real-valued completion consumer reads it as an ambient host function. It sits over the CauchyRepresentation decoding of \autoref{ch:concrete-instances-cauchy-representation-namecert}, the source and target represented-space rows of \autoref{ch:concrete-instances-representedspace-namecert}, the finite name-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, the modulus row of \autoref{ch:concrete-instances-uniformmodulus-namecert}, and the continuous-map route of \autoref{ch:concrete-instances-continuousmap-namecert}. The packet names a computable-analysis uniform-continuity boundary; it is not a host function, not a choice-selected modulus, not a quotient representation, not full $\RealUp$ completeness, and not a checked public bridge.

\begin{definition}[Computable uniform continuity carrier]
\label{def:computable-uniform-continuity-carrier}
A $\ComputableUniformContinuityUp$ carrier is a finite $\BHist$ packet
$$
Q=(X,Y,K,R,M,W,T,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the represented-space source row, exposing only the source carrier, admissible names, and finite observation cells used by the packet.
\item $Y$ is the represented-space target row, exposing the matching target carrier, target names, and target observation cells.
\item $K$ is the $\CauchyRepresentationUp$ decoding row, relating source and target names to regular Cauchy information before any real-facing seal is inspected.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the decoded Cauchy data.
\item $M$ is the $\UniformModulusUp$ realizer row, giving the finite rule that turns a requested target tolerance into a source-name window.
\item $W$ is the $\StreamNameUp$ finite-window ledger for the source and target name segments inspected by $M$ and $K$.
\item $T$ is the $\DyadicRatCoreUp$ tolerance ledger, recording the dyadic budgets used by the source window, target window, and regular rational readback.
\item $E$ is the terminal $\RealUp$ seal row, reachable only after $X,Y,K,R,M,W,T$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for represented-space rows, decoding, readback, modulus, windows, tolerance ledger, seal, replay, provenance, and naming.
\item $C$ records displayed $\Cont$ replay from a represented-space consumer through the decoding, modulus, finite-window, tolerance, readback, and seal rows.
\item $P$ is the $\Pkg$ provenance over $\ComputableUniformContinuityUp$, $\RepresentedSpaceUp$, $\CauchyRepresentationUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\UniformModulusUp$, $\ContinuousMapUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ComputableUniformContinuityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,Y,K,R,M,W,T,E,H,C,P,N$. The carrier has no coordinate for an external host function, a selected modulus outside $M$, quotient equality of representations, full real completeness, countable choice over names, or a detached continuity proof.
\end{definition}

\begin{theorem}[Computable uniform continuity NameCert obligations]
\label{thm:computable-uniform-continuity-namecert-obligations}
For every accepted $\ComputableUniformContinuityUp$ carrier $Q=(X,Y,K,R,M,W,T,E,H,C,P,N)$, the local $\NameCert_{\ComputableUniformContinuityUp}$ surface is exhausted by the following obligations:
\begin{enumerate}
\item carrier admission: the accepted object is exactly the displayed packet $X,Y,K,R,M,W,T,E,H,C,P,N$;
\item modulus visibility: every exported continuity read passes through the single $\UniformModulusUp$ row $M$ before a target tolerance is accepted;
\item represented-space compatibility: the source row $X$, target row $Y$, and decoding row $K$ are read together, so the modulus never acts on a detached host carrier;
\item window transport: the $\StreamNameUp$ row $W$ and $\hsame$ row $H$ carry the same finite source and target name windows through every accepted replay;
\item tolerance stability: the $\DyadicRatCoreUp$ ledger $T$, the $\RegSeqRatUp$ readback $R$, and the terminal $\RealUp$ seal $E$ use the same dyadic budgets exposed by $M$ and $W$;
\item ledger nonescape: $H,C,P,N$ export no host function, choice-selected modulus, quotient representation, full $\RealUp$ completeness theorem, countable choice over names, or bridge certificate.
\end{enumerate}
No local NameCert obligation is stored outside those rows.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:computable-uniform-continuity-carrier}. Its accepted rows are exactly $X,Y,K,R,M,W,T,E,H,C,P,N$, so carrier admission exposes represented-space data, decoding, readback, modulus, windows, tolerance ledger, real seal, and structural rows only.

The modulus visibility and represented-space compatibility clauses are forced by the row order. A continuity consumer must first read the source and target represented-space rows $X,Y$, then the decoding row $K$, and then the displayed modulus realizer $M$ before it can request a target tolerance.

The window and tolerance clauses read the finite ledgers. The row $W$ lists the inspected source and target name windows, while $T$ records the dyadic budgets that make the $\RegSeqRatUp$ readback $R$ and terminal $\RealUp$ seal $E$ available.

Finally inspect the structural rows. Componentwise transport $H$, replay $C$, provenance $P$, and local naming $N$ preserve the same displayed route and contain no coordinate for the excluded host function, selected modulus, quotient representation, full real completeness theorem, countable choice, or bridge certificate.
\end{proof}

\falsifiablePrediction{An accepted $\ComputableUniformContinuityUp$ read is refuted if it exports a uniform-continuity consumer without displaying represented-space source and target rows, CauchyRepresentation decoding, RegSeqRat readback, UniformModulus realizer, StreamName windows, DyadicRatCore tolerance ledger, RealUp seal, transport, replay, provenance, and local naming.}

\independenceWitness{cauchy-representation, representedspace, streamname, regseqrat, dyadicratcore, real, uniformmodulus, continuousmap: none alone is bijective with $\ComputableUniformContinuityUp$ because the packet jointly records represented source and target rows, Cauchy decoding, a computable modulus realizer, finite name windows, rational readback, dyadic tolerance stability, terminal Real sealing, transport, replay, provenance, and local NameCert.}

\closureat{ComputableUniformContinuityUp}{seedStr}

\begin{closurestatus}{\ComputableUniformContinuityUp}
  \constructivestory{A $\ComputableUniformContinuityUp$ packet is generated from represented-space source and target rows, CauchyRepresentation decoding, RegSeqRat readback, a UniformModulus realizer, StreamName finite windows, a DyadicRatCore tolerance ledger, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:computable-uniform-continuity-carrier} and \autoref{thm:computable-uniform-continuity-namecert-obligations} seed the finite computable-analysis route from represented-space names to a uniform-modulus Real consumer.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host functions, choice-selected moduli, quotient representations, full RealUp completeness, countable choice over names, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, modulus visibility, represented-space compatibility, window transport, tolerance stability, ledger nonescape, and local naming for BEDC.Derived.ComputableUniformContinuityUp.}
\end{closurestatus}
