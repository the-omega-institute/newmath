The compact-metric consumer reads $\UniformlyContinuousSequenceUp$ through \autoref{ch:concrete-instances-compactmetric-namecert} and \autoref{ch:concrete-instances-uniformmodulus-namecert} before using the shared sequence modulus. This keeps the sequence ledger attached to compact finite-net data and the finite modulus row, rather than to an external pointwise continuity family, open-cover compactness theorem, selected limit, or ambient $\RealUp$ completeness claim.
