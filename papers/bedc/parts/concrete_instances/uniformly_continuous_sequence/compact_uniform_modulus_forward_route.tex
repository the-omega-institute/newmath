\begin{theorem}[Compact uniform-modulus route for uniformly continuous sequences]
\label{thm:uniformly-continuous-sequence-compact-uniform-modulus-forward-route}
Every Real-facing read of an accepted $\UniformlyContinuousSequenceUp$ carrier whose source is displayed through the compact route must consume the shared $\UniformModulusUp$ row through the compact-uniform modulus packet of \autoref{ch:concrete-instances-compact-uniform-modulus-namecert}. The read factors through the visible route
$$
\CompactUniformModulusUp,\ \UniformModulusUp,\ \ContinuousMapUp,\ \MetricSpaceUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
It exports no detached pointwise continuity family, host compactness theorem, selected limit, quotient stream equality, countable-choice schedule, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:uniformly-continuous-sequence-carrier}. The sequence entries are available only as $\ContinuousMapUp$ rows, the inspected indices only through $\StreamNameUp$ windows, and the target values only after $\RegSeqRatUp$ readback and the terminal $\RealUp$ seal.

When the source row is compact, the shared modulus row is not read as an external theorem. It is consumed through the compact-uniform modulus route of \autoref{ch:concrete-instances-compact-uniform-modulus-namecert}, which packages compact source coverage and the downstream $\UniformModulusUp$ handoff before any Real-facing sequence read is public.

The transport, replay, provenance, and local naming rows then preserve the same route. Since the carrier has no coordinate for detached pointwise continuity, host compactness, selected limits, quotient stream equality, countable choice, or ambient real completeness, a Real-facing compact-source read must pass through the displayed compact-uniform modulus dependency.
\end{proof}
