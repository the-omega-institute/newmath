Every public sequence-level uniform-continuity read from $\UniformlyContinuousSequenceUp$ must first pass through the sibling lattice formed by compact finite-net rows from \autoref{ch:concrete-instances-compactmetric-namecert}, the shared $\UniformModulusUp$ row of \autoref{ch:concrete-instances-uniformmodulus-namecert}, pointwise stream-metric rows from \autoref{ch:concrete-instances-pointwise-stream-metric-namecert}, $\StreamNameUp$ window rows, $\RegSeqRatUp$ readback, and terminal $\RealUp$ seals. The lattice gives the consumer finite compact coverage, one reusable modulus, stream-local metric observations, windowed rational reads, and sealed target values before any sequence-level uniform continuity claim is public; it exports no detached pointwise-continuity family, selected limit, quotient stream equality, countable-choice schedule, or ambient $\RealUp$ completeness.
