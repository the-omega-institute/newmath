\chapter{A Concrete Naming Certificate for $\TypeLevelSocketExposureUp$}
\label{ch:concrete-instances-typelevelsocketexposure-namecert}
\origin{human}

The $\TypeLevelSocketExposureUp$ packet realizes type-level apophatic exposure as a finite NameCert object. It records the setup-field surface where an outside supply is requested, the carrier and classifier rows that make the request visible, the provenance and refusal-ledger rows that prevent hidden internalization, and the local naming row that downstream audit gates can cite.

The refusal-ledger row $R$ is the type-level specialization of the negative-name boundary in \autoref{ch:concrete-instances-negative-name-boundary-namecert}. This chapter keeps the setup-field row, carrier-shape row, and classifier rows visible so that the same refusal discipline is read as a typed socket surface rather than as an anonymous external-supply boundary.

\begin{definition}[Type-level socket exposure carrier]
\label{def:type-level-socket-exposure-carrier}
A $\TypeLevelSocketExposureUp$ carrier is a finite $\BHist$ packet
$$
E=(S,A,K,L,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the setup-field row naming the type-level boundary at which an outside supply is requested;
\item $A$ is the admitted carrier row for the requested supply shape;
\item $K$ is the classifier row distinguishing visible socket use from generated substrate content;
\item $L$ is the ledger row recording whether the requested supply is exposed at the setup field, carrier, classifier, provenance, or $\mathsf{GAP}$ position;
\item $R$ is the refusal-ledger row excluding hidden axiom channels, forbidden host dependencies, and unmarked premise traffic;
\item $H$ records componentwise $\hsame$ transports for the setup field, carrier, classifier, ledger, refusal row, provenance row, and local naming row;
\item $C$ records the displayed $\Cont$ routes by which consumers read the setup field, carrier, classifier, ledger, refusal, provenance, and naming coordinates;
\item $P$ is the $\Pkg$ provenance over $\BHist$, $\hsame$, $\Cont$, setup fields, carriers, classifiers, $\mathsf{GAP}$ ledgers, axiom-purity gates, and \NameCert{} rows;
\item $N$ is the local $\NameCert_{\TypeLevelSocketExposureUp}$ row.
\end{enumerate}
The carrier contains no hidden axiom, no theorem-valued outside premise, no forbidden host dependency, no unlisted setup field, and no route that turns the requested supply into generated substrate content.
\end{definition}

\begin{theorem}[Type-level socket exposure NameCert obligations]
\label{thm:type-level-socket-exposure-namecert-obligations}
For the carrier of \autoref{def:type-level-socket-exposure-carrier}, the local $\NameCert_{\TypeLevelSocketExposureUp}$ surface consists exactly of carrier admission for
$$
E=(S,A,K,L,R,H,C,P,N),
$$
setup-field exposure through $S$, carrier-shape exposure through $A$, classifier visibility through $K$, ledger placement through $L$, refusal-ledger exactness through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ route replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\leanchecked{BEDC.Derived.TypeLevelSocketExposureUp.TypeLevelSocketExposureNamecertObligations}
\begin{proof}
Carrier admission is projection from the displayed packet $E$. The rows $S,A,K,L,R,H,C,P,N$ list the setup field, admitted carrier shape, classifier, ledger, refusal row, transports, continuation routes, provenance, and local name.

The exposure obligations are finite. A consumer first reads the setup-field row $S$, then the carrier row $A$, then the classifier row $K$ that distinguishes socket use from generated substrate content. The ledger row $L$ records the visible placement of the request.

The refusal row $R$ excludes the channels that would hide the request: axiom admission, forbidden host dependency, and unmarked premise traffic. The rows $H,C,P,N$ account for transport, route replay, provenance, and local naming, so the local surface is exhausted by the displayed packet.
\end{proof}

\begin{theorem}[Type-level socket exposure ledger non-escape]
\label{thm:type-level-socket-exposure-ledger-nonescape}
Every consumer read of an accepted $\TypeLevelSocketExposureUp$ packet factors through
$$
S,A,K,L,R,H,C,P,N.
$$
It may inspect the setup-field exposure, carrier shape, classifier, ledger placement, refusal row, componentwise transports, displayed continuations, provenance, and local \NameCert{} row, and it cannot read a hidden axiom, a theorem-valued outside premise, a forbidden host dependency, an unlisted setup field, or an unmarked supply channel.
\end{theorem}
\leanchecked{BEDC.Derived.TypeLevelSocketExposureUp.TypeLevelSocketExposureLedgerNonescape}
\begin{proof}
Start with the obligation theorem. It identifies the accepted packet with the displayed rows $S,A,K,L,R,H,C,P,N$ and names those rows as the complete local NameCert inventory.

For type-level exposure, inspect $S$, $A$, and $K$. The setup field names the boundary, the carrier row records the requested supply shape, and the classifier row keeps visible socket use separate from generated substrate content.

For ledger non-escape, inspect $L$ and $R$. The ledger row records the visible placement of the request, while the refusal row excludes axiom admission, forbidden host dependency, hidden premise traffic, and unlisted setup fields.

Finally inspect $H,C,P,N$. They provide the only transport, continuation, provenance, and naming routes available to downstream consumers. Since the carrier contains no additional row beyond those coordinates, no consumer can obtain a hidden supply channel from the packet.
\end{proof}
\leanchecked{BEDC.Derived.TypeLevelSocketExposureUp.TypeLevelSocketExposureLedgerHostReturnExclusion}

\begin{theorem}[Type-level socket exposure refusal exactness]
\label{thm:type-level-socket-exposure-refusal-exactness}
For every accepted $\TypeLevelSocketExposureUp$ carrier $E=(S,A,K,L,R,H,C,P,N)$, the refusal row $R$ excludes hidden axiom channels, theorem-valued outside premises, forbidden host dependencies, unlisted setup fields, and unmarked supply channels exactly because every consumer route factors through $S,A,K,L,R,H,C,P,N$. No refused channel can be reconstructed from the structural rows.
\end{theorem}
\leanchecked{BEDC.Derived.TypeLevelSocketExposureUp.TypeLevelSocketExposureRefusalExactness}
\begin{proof}
Start with the carrier of \autoref{def:type-level-socket-exposure-carrier}. The packet exposes the setup field, admitted carrier, classifier, ledger placement, refusal row, transport, continuation, provenance, and local naming as the finite tuple $S,A,K,L,R,H,C,P,N$.

Apply the NameCert obligations of \autoref{thm:type-level-socket-exposure-namecert-obligations}. They make refusal-ledger exactness through $R$ one of the local rows rather than an external convention.

Finally use ledger non-escape from \autoref{thm:type-level-socket-exposure-ledger-nonescape}. Since every consumer read factors through the displayed tuple, a hidden axiom channel, theorem-valued outside premise, forbidden host dependency, unlisted setup field, or unmarked supply channel has no carrier coordinate and no replay route through $H,C,P,N$.
\end{proof}

\begin{theorem}[Type-level socket exposure scope exactness]
\label{thm:type-level-socket-exposure-scope-exactness}
For every accepted $\TypeLevelSocketExposureUp$ packet $E=(S,A,K,L,R,H,C,P,N)$, the complete consumption scope for type-level socket exposure is the displayed setup-field row $S$, carrier-shape row $A$, classifier row $K$, ledger-placement row $L$, refusal row $R$, componentwise $\hsame$ transport row $H$, displayed $\Cont$ replay row $C$, $\Pkg$ provenance row $P$, and local $\NameCert_{\TypeLevelSocketExposureUp}$ row $N$. No hidden axiom channel, theorem-valued outside premise, host dependency, unlisted setup field, or unmarked supply channel has a scope coordinate outside those rows.
\end{theorem}
\leanchecked{BEDC.Derived.TypeLevelSocketExposureUp.TypeLevelSocketExposureScopeExactness}
\begin{proof}
Project the carrier of \autoref{def:type-level-socket-exposure-carrier}. It displays exactly $S,A,K,L,R,H,C,P,N$ as setup, carrier, classifier, ledger, refusal, transport, replay, provenance, and naming coordinates.

Apply the NameCert obligations of \autoref{thm:type-level-socket-exposure-namecert-obligations}. They identify setup-field exposure, carrier-shape exposure, classifier visibility, ledger placement, refusal-ledger exactness, transport, replay, provenance, and local naming as the local obligation surface.

Use ledger non-escape from \autoref{thm:type-level-socket-exposure-ledger-nonescape} and refusal exactness from \autoref{thm:type-level-socket-exposure-refusal-exactness}. Since every consumer read factors through the displayed tuple and the refusal row excludes the hidden channels, no excluded premise can acquire a separate scope coordinate.
\end{proof}

\begin{theorem}[TypeLevelSocketExposure kernel grounding]
\label{thm:type-level-socket-exposure-scope-kernel-grounding}
Every accepted $\TypeLevelSocketExposureUp$ read is grounded by the displayed $\BHist$ carrier rows $S,A,K,L,R,H,C,P,N$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, the refusal ledger $R$, and the local $\NameCert_{\TypeLevelSocketExposureUp}$ row $N$. Consequently the read cannot import outside supply, host type equality, hidden setup fields, theorem-valued outside premises, or global kernel soundness.
\end{theorem}
\leanchecked{BEDC.Derived.TypeLevelSocketExposureUp.TypeLevelSocketExposureScopeKernelGrounding}
\leanchecked{BEDC.Derived.TypeLevelSocketExposureUp.TypeLevelSocketExposureKernelGrounding}
\begin{proof}
Project the carrier of \autoref{def:type-level-socket-exposure-carrier}. The accepted read starts with the setup-field row $S$, admitted carrier row $A$, classifier row $K$, ledger row $L$, refusal row $R$, transport row $H$, replay row $C$, provenance row $P$, and local naming row $N$.

Apply the NameCert obligations of \autoref{thm:type-level-socket-exposure-namecert-obligations}. Those obligations make the carrier rows, refusal ledger, transport, replay, provenance, and local naming the complete local certificate surface.

Use ledger non-escape, refusal exactness, and scope exactness from \autoref{thm:type-level-socket-exposure-ledger-nonescape}, \autoref{thm:type-level-socket-exposure-refusal-exactness}, and \autoref{thm:type-level-socket-exposure-scope-exactness}. Since every consumer route is already confined to $S,A,K,L,R,H,C,P,N$, no outside supply, host type equality, hidden setup field, theorem-valued premise, or global kernel soundness can enter through a separate coordinate.
\end{proof}

\begin{theorem}[Type-level socket exposure public export]
\label{thm:type-level-socket-exposure-public-export}
The public $\TypeLevelSocketExposureUp$ export is exactly the setup-field surface $S$, carrier-shape row $A$, classifier row $K$, ledger-placement row $L$, refusal row $R$, componentwise $\hsame$ transport row $H$, displayed $\Cont$ replay row $C$, $\Pkg$ provenance row $P$, and local $\NameCert_{\TypeLevelSocketExposureUp}$ row $N$. It exports no outside supply, theorem-valued premise, host dependency, unlisted setup field, unmarked supply channel, or global kernel-soundness premise.
\end{theorem}
\begin{proof}
Project the carrier and apply the NameCert obligations of \autoref{thm:type-level-socket-exposure-namecert-obligations}. The public rows are precisely $S,A,K,L,R,H,C,P,N$.

Use ledger non-escape from \autoref{thm:type-level-socket-exposure-ledger-nonescape} and refusal exactness from \autoref{thm:type-level-socket-exposure-refusal-exactness}. These results exclude hidden axiom channels, theorem-valued outside premises, host dependencies, unlisted setup fields, and unmarked supply channels from the exported surface.

Finally apply scope exactness and kernel grounding from \autoref{thm:type-level-socket-exposure-scope-exactness} and \autoref{thm:type-level-socket-exposure-scope-kernel-grounding}. The exported object is the displayed finite surface itself, not an external supply or a theorem-valued premise attached to it.
\end{proof}

\closureat{TypeLevelSocketExposureUp}{publicStr}

\begin{closurestatus}{\TypeLevelSocketExposureUp}
  \constructivestory{A $\TypeLevelSocketExposureUp$ packet is generated from a setup-field exposure row, admitted carrier-shape row, classifier row, ledger-placement row, refusal row, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\publicClosure}
  \scopeclosed{\autoref{def:type-level-socket-exposure-carrier}, \autoref{thm:type-level-socket-exposure-namecert-obligations}, \autoref{thm:type-level-socket-exposure-ledger-nonescape}, \autoref{thm:type-level-socket-exposure-refusal-exactness}, \autoref{thm:type-level-socket-exposure-scope-exactness}, \autoref{thm:type-level-socket-exposure-scope-kernel-grounding}, and \autoref{thm:type-level-socket-exposure-public-export} bind the public finite surface for type-level outside-supply exposure.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, existence of any outside supply, global kernel soundness, all setup-field designs, or any hidden host principle.}
  \upgradepath{Bridged closure requires a standard bridge theorem connecting the public finite export to the acceptance-bridge schema.}
\end{closurestatus}
