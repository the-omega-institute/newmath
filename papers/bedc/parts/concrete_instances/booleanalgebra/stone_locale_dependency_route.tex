The $\BooleanAlgebraUp$ carrier supplies the complemented distributive-lattice source read by \autoref{ch:concrete-instances-stoneduality-namecert} and the Boolean endpoint discipline read by \autoref{ch:concrete-instances-locale-space-namecert}. The horizontal route is the same finite join, meet, complement, endpoint, order, transport, replay, provenance, and local $\NameCert$ surface; it does not add a host powerset algebra, selected ultrafilter, ambient locale frame, quotient lattice equality, or topology object outside the displayed Boolean rows.
