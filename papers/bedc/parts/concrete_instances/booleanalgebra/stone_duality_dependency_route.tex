The $\BooleanAlgebraUp$ carrier exposes the join, meet, complement, endpoint, order, transport, replay, provenance, and local $\NameCert$ rows that a $\StoneDualityUp$ consumer later reads as its Boolean-source side. Thus the dependency into \autoref{ch:concrete-instances-stoneduality-namecert} and the lattice-source handoff of \autoref{ch:concrete-instances-lattice-namecert} is a finite classifier route through $J,M,K,Z,O,R,H,C,P,N$, not a hidden import of a host Boolean algebra, ultrafilter space, powerset representation, or ambient topology.

\leanchecked{BEDC.Derived.BooleanalgebraUp.BooleanAlgebraStoneSourceLatticeForwardConsumer}

The same outgoing route also records the handoff row: after the Stone-source row is read from order and local naming, the provenance row transports it to the concrete downstream handoff row while the forward Stone-source certificate remains available to the same consumer.

\leanchecked{BEDC.Derived.BooleanalgebraUp.BooleanAlgebraStoneHandoffForwardConsumer}
