\chapter{A Concrete Naming Certificate for $\FiniteDiniModulusUp$}
\label{ch:concrete-instances-finite-dini-modulus-namecert}
\origin{human}

Following Dini and Bishop--Bridges constructive analysis, the $\FiniteDiniModulusUp$ packet records the finite NameCert route from compact finite-net data and monotone pointwise descent to one uniform modulus row over $\RealUp$. It consumes the compact finite-net surface of \autoref{ch:concrete-instances-compact-uniform-finite-net-namecert}, the monotone Dini route of \autoref{ch:concrete-instances-dini-monotone-compact-uniform-namecert}, the continuous-map graph rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite minimum-budget handoff of \autoref{ch:concrete-instances-dini-finite-minimum-budget-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names only the finite compact-window modulus route; it does not import arbitrary open-cover compactness, quotient real equality, selected limits, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Finite Dini modulus carrier]
\label{def:finite-dini-modulus-carrier}
A $\FiniteDiniModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(K,G,E,R,T,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact finite-net row, exposing the inspected net centers, radius witnesses, and the finite cover ledger consumed by the modulus extraction.
\item $G$ is the $\ContinuousMapUp$ graph row for the monotone family and the displayed limit row over the same compact source.
\item $E$ is the terminal $\RealUp$ seal row for every function value, limit value, and comparison value read by the packet.
\item $R$ is the monotone pointwise descent row, recording the displayed order comparisons and pointwise tail windows used at the finite net centers.
\item $T$ is the $\DyadicRatCoreUp$ tolerance row, listing the requested tolerance, comparison slack, and finite dyadic descent used by the route.
\item $B$ is the finite minimum-budget row, extracting one displayed index budget from the centerwise Dini comparisons before the uniform-modulus export is available.
\item $H$ records componentwise $\hsame$ transport for the compact net, graph, real seal, monotone descent, tolerance, budget, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a compact finite-net request through $K,G,E,R,T,B,H$ to the exported modulus route.
\item $P$ is the $\Pkg$ provenance over $\FiniteDiniModulusUp$, $\CompactUniformFiniteNetUp$, $\DiniMonotoneCompactUniformUp$, $\ContinuousMapUp$, $\RealUp$, $\DyadicRatCoreUp$, $\DiniFiniteMinimumBudgetUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteDiniModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,G,E,R,T,B,H,C,P,N$. It has no coordinate for arbitrary open covers, selected finite subcovers, quotient real equality, hidden limiting values, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Finite Dini modulus obligations]
\label{thm:finite-dini-modulus-obligations}
For every accepted $\FiniteDiniModulusUp$ carrier
$$
M=(K,G,E,R,T,B,H,C,P,N),
$$
the local $\NameCert_{\FiniteDiniModulusUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item expose the compact finite-net row $K$ before any modulus consumer reads a centerwise comparison;
\item display the $\ContinuousMapUp$ graph row $G$ and the terminal $\RealUp$ seal row $E$ before any function-value comparison is used;
\item route monotone pointwise descent through $R$ and dyadic tolerance descent through $T$;
\item extract the uniform index budget only through the finite minimum-budget row $B$;
\item preserve the same route by componentwise $\hsame$ transport and displayed $\Cont$ replay through $H,C$;
\item source and name the packet through $\Pkg$ provenance $P$ and the local $\NameCert$ row $N$.
\end{enumerate}
No obligation row states arbitrary open-cover compactness, quotient real equality, selected limits, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-dini-modulus-carrier}. The compact data are exactly the finite net centers and radius witnesses in $K$, so a modulus consumer cannot inspect an arbitrary open cover or a selected subcover.

Next read the analytic rows. The graph row $G$ supplies the displayed $\ContinuousMapUp$ family and limit graph, while $E$ is the only $\RealUp$ seal for the values compared by the packet.

The Dini part is split into visible finite rows. Monotone pointwise descent is recorded in $R$, and the requested tolerance plus slack comparisons are recorded in the dyadic row $T$.

The exported budget is downstream of those rows. The finite minimum-budget row $B$ folds the centerwise comparisons into one displayed index budget, and $H,C,P,N$ transport, replay, source, and name the same packet. Since no excluded coordinate occurs in the carrier, the local obligation surface is exhausted by the listed rows.
\end{proof}

\begin{theorem}[Finite Dini uniform modulus route]
\label{thm:finite-dini-modulus-uniform-modulus-route}
Every uniform-modulus consumer of an accepted $\FiniteDiniModulusUp$ carrier must factor through the displayed route
$$
\CompactUniformFiniteNetUp,\ \DiniMonotoneCompactUniformUp,\ \ContinuousMapUp,\ \RealUp,\ \DyadicRatCoreUp,\ \DiniFiniteMinimumBudgetUp,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert_{\FiniteDiniModulusUp}.
$$
Thus the packet exports a uniform modulus route for compact finite-net and monotone pointwise-descent data. It does not claim arbitrary open-cover compactness, quotient real equality, selected limits, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the accepted carrier. The finite source row $K$ names the compact net inspected by the route, and the graph plus real-seal rows $G,E$ name the continuous-map values and limit values that can be compared.

Apply \autoref{thm:finite-dini-modulus-obligations}. The monotone descent row $R$, dyadic tolerance row $T$, and finite minimum-budget row $B$ are the only rows from which a uniform index budget can be read.

The structural rows then keep the consumer on the same packet. Componentwise transport in $H$ and replay in $C$ preserve the displayed dependency order, while $P,N$ provide provenance and local naming. Because the carrier has no field for arbitrary open covers, quotient real equality, selected limits, countable choice, or ambient completeness, the exported object is exactly the finite uniform-modulus route.
\end{proof}

\falsifiablePrediction{Every accepted $\FiniteDiniModulusUp$ uniform-modulus read first exposes compact finite-net rows, ContinuousMap graph rows, RealUp seals, monotone pointwise descent, DyadicRatCore tolerance cells, a finite minimum budget, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}

\independenceWitness{compact-uniform-finite-net, dini-monotone-compact-uniform, continuousmap, real, dyadicratcore, dini-finite-minimum-budget: $\FiniteDiniModulusUp$ is not bijective to any listed sibling because it binds finite compact-net Dini descent to one uniform-modulus budget route at the RealUp seal.}

\closureat{FiniteDiniModulusUp}{seedStr}

\begin{closurestatus}{\FiniteDiniModulusUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-dini-modulus-carrier}, \autoref{thm:finite-dini-modulus-obligations}, and \autoref{thm:finite-dini-modulus-uniform-modulus-route} seed the finite compact-net Dini modulus route over ContinuousMap, Real, DyadicRatCore, finite budget, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close arbitrary open-cover compactness, quotient real equality, selected limits, countable choice, Lean verification, bridge checking, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net exposure, ContinuousMap graph visibility, Real seal nonescape, monotone pointwise descent, dyadic tolerance exactness, finite budget extraction, transport, replay, provenance, and local naming for BEDC.Derived.FiniteDiniModulusUp.}
  \constructivestory{A $\FiniteDiniModulusUp$ packet is generated from compact finite-net rows, ContinuousMap graph rows, RealUp seal rows, monotone pointwise descent, DyadicRatCore tolerance rows, a finite minimum-budget row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
