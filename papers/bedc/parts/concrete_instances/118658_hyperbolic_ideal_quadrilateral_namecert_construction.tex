\chapter{A Concrete Naming Certificate for $\HyperbolicIdealQuadrilateralUp$}
\label{ch:concrete-instances-hyperbolic-ideal-quadrilateral-namecert}
\origin{ai}

The $\HyperbolicIdealQuadrilateralUp$ packet realizes the boundary-quadruple branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It sits beside the disk carrier of \autoref{ch:concrete-instances-unitdisk-namecert}, the metric row of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the boundary row of \autoref{ch:concrete-instances-hyperbolic-geodesic-boundary-namecert}, the cross-ratio packet of \autoref{ch:concrete-instances-hyperbolic-cross-ratio-namecert}, and the ideal-triangle packet of \autoref{ch:concrete-instances-hyperbolic-ideal-triangle-namecert}. The packet names four displayed boundary ends, their side and diagonal incidence, a finite cross-ratio read, transport, replay, provenance, and local naming; it does not complete the disk, quotient boundary endpoints, select global geodesic representatives, or identify observer expansion with physical spacetime.

\begin{definition}[Hyperbolic ideal quadrilateral carrier]
\label{def:hyperbolic-ideal-quadrilateral-carrier}
A $\HyperbolicIdealQuadrilateralUp$ carrier is a finite $\BHist$ packet
$$
Q=(U,M,B,E,D,X,T,J,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ is the $\UnitDiskUp$ coordinate row for the inspected disk probes and four boundary-end reads.
\item $M$ is the $\HyperbolicMetricUp$ metric-read row over the same displayed disk data.
\item $B$ is the $\HyperbolicGeodesicBoundaryUp$ row naming four cyclically ordered boundary ends and the finite incidence data used by the packet.
\item $E$ records the four boundary-to-boundary side rows and the two diagonal rows as displayed geodesic-boundary data.
\item $D$ is the diagonal-splitting row that reads the two ideal triangles cut from the quadrilateral through the sibling $\HyperbolicIdealTriangleUp$ surface.
\item $X$ is the $\HyperbolicCrossRatioUp$ row carrying the finite boundary cross-ratio comparison attached to the same four-end boundary surface.
\item $T$ is the transport row preserving disk coordinates, metric reads, boundary incidence, side rows, diagonal rows, triangle-splitting data, and cross-ratio data.
\item $J$ is the classifier-stability row for cyclic order, side incidence, diagonal incidence, cross-ratio readback, and triangle-splitting reads.
\item $H$ records componentwise $\hsame$ transport for disk, metric, boundary, side, diagonal, cross-ratio, classifier, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an ideal-quadrilateral request through $U,M,B,E,D,X,T,J,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicIdealQuadrilateralUp$, $\UnitDiskUp$, $\HyperbolicMetricUp$, $\HyperbolicGeodesicBoundaryUp$, $\HyperbolicCrossRatioUp$, $\HyperbolicIdealTriangleUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HyperbolicIdealQuadrilateralUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for completed hyperbolic disks, quotient ideal-boundary equality, selected global geodesics, host analytic flow, or physical cosmology.
\end{definition}

\begin{theorem}[Hyperbolic ideal quadrilateral NameCert obligations]
\label{thm:hyperbolic-ideal-quadrilateral-namecert-obligations}
For every accepted $\HyperbolicIdealQuadrilateralUp$ carrier $Q=(U,M,B,E,D,X,T,J,H,C,P,N)$, the local $\NameCert_{\HyperbolicIdealQuadrilateralUp}$ surface is exhausted by disk-coordinate admission, hyperbolic metric visibility, four-end boundary incidence, side and diagonal visibility, triangle-splitting readback, finite cross-ratio readback, transport, classifier stability, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-ideal-quadrilateral-carrier}. The rows $U,M,B,E,D,X,T,J,H,C,P,N$ are the only displayed coordinates.

The geometric read begins with $U,M,B,E$. Disk coordinates and metric reads are visible before the four boundary ends, side rows, and diagonal rows are inspected.

The finite boundary-quadruple structure is carried by $D,X,T,J$. Triangle splitting reads the two diagonal pieces through the ideal-triangle sibling, while the cross-ratio row attaches one finite four-end comparison to the same boundary surface. Transport and classifier stability keep these rows on the same packet.

Rows $H,C,P,N$ preserve, replay, source, and name the finite carrier. Since no row stores the excluded completed or host-level objects, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Hyperbolic ideal quadrilateral boundary-quadruple handoff]
\label{thm:hyperbolic-ideal-quadrilateral-boundary-quadruple-handoff}
Every boundary-quadruple consumer of an accepted $\HyperbolicIdealQuadrilateralUp$ packet factors through
$$
\UnitDiskUp \longmapsto \HyperbolicMetricUp \longmapsto \HyperbolicGeodesicBoundaryUp \longmapsto \HyperbolicCrossRatioUp \longmapsto \HyperbolicIdealTriangleUp .
$$
The consumer receives four displayed boundary ends, side and diagonal incidence, the finite cross-ratio row, triangle-splitting readback, transport, classifier stability, componentwise transport, replay, provenance, and local naming. It obtains no completed disk, quotient ideal-boundary equality, selected global geodesic, host analytic flow, physical cosmology, or observer substance.
\end{theorem}
\begin{proof}
Begin with a boundary-quadruple request. The disk-coordinate row $U$ and metric row $M$ provide the only geometric source data available to the request.

Read $B$ and $E$ next. The four boundary ends, four sides, and two diagonals are displayed as finite incidence and geodesic-boundary rows, not as quotient endpoints or selected global geodesics.

Use $X,D,T,J$ for the handoff. The cross-ratio row records the finite four-end comparison, the diagonal-splitting row exposes the two ideal-triangle reads, and transport plus classifier stability preserve the same boundary commitments.

Finally replay the structural rows $H,C,P,N$. They preserve the same finite packet, and the carrier has no coordinate for the excluded completed or host-level objects, so the boundary-quadruple handoff cannot leave the displayed route.
\end{proof}

\falsifiablePrediction{Any accepted $\HyperbolicIdealQuadrilateralUp$ consumer that exports a boundary-quadruple comparison without the four-end boundary row $B$ and the cross-ratio row $X$ refutes the carrier surface.}

\independenceWitness{unitdisk, hyperbolic-metric, hyperbolic-geodesic-boundary, hyperbolic-cross-ratio, hyperbolic-ideal-triangle: none is bijective to $\HyperbolicIdealQuadrilateralUp$ because this packet alone binds four boundary ends, side and diagonal incidence, triangle-splitting readback, cross-ratio comparison, transport, classifier stability, replay, provenance, and local NameCert rows.}

\closureat{HyperbolicIdealQuadrilateralUp}{seedStr}

\begin{closurestatus}{\HyperbolicIdealQuadrilateralUp}
  \constructivestory{A $\HyperbolicIdealQuadrilateralUp$ packet is generated from UnitDiskUp coordinate rows, HyperbolicMetricUp metric reads, HyperbolicGeodesicBoundaryUp four-end boundary incidence, side and diagonal rows, HyperbolicIdealTriangleUp splitting readback, HyperbolicCrossRatioUp comparison, transport, classifier stability, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-ideal-quadrilateral-carrier}, \autoref{thm:hyperbolic-ideal-quadrilateral-namecert-obligations}, and \autoref{thm:hyperbolic-ideal-quadrilateral-boundary-quadruple-handoff} seed the finite boundary-quadruple route over disk, metric, boundary, side, diagonal, triangle-splitting, cross-ratio, transport, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed hyperbolic disks, quotient ideal-boundary equality, selected global geodesics, host analytic flows, physical cosmology, observer-substance readings, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked disk visibility, metric-read visibility, four-end boundary incidence, side and diagonal visibility, triangle splitting, cross-ratio readback, transport, classifier stability, ledger nonescape, and local naming for BEDC.Derived.HyperbolicIdealQuadrilateralUp.}
\end{closurestatus}
