\chapter{A Concrete Naming Certificate for $\HyperbolicSectorObserverNerveUp$}
\label{ch:concrete-instances-hyperbolic-sector-observer-nerve-namecert}
\origin{ai}

The $\HyperbolicSectorObserverNerveUp$ packet realizes the sector-neighbor route of the hyperbolic observer-expansion vision as finite NameCert data. It sits after the sector cover of \autoref{ch:concrete-instances-hyperbolic-sector-cover-namecert}, the boundary-shadow surface of \autoref{ch:concrete-instances-hyperbolic-shadow-boundary-namecert}, the Poincare disk metric of \autoref{ch:concrete-instances-poincare-disk-metric-namecert}, the sector-refinement packet of \autoref{ch:concrete-instances-hyperbolic-sector-refinement-namecert}, and the phase-distance consumer of \autoref{ch:concrete-instances-phase-distance-observer-expansion-namecert}. The packet records which finite observer sectors overlap and refine one another before any phase-distance read is exported; it does not claim a global hyperbolic manifold, completed boundary measure, selected geodesic fan, or host metric completeness.

\begin{definition}[Hyperbolic sector observer nerve carrier]
\label{def:hyperbolic-sector-observer-nerve-carrier}
A $\HyperbolicSectorObserverNerveUp$ carrier is a finite $\BHist$ packet
$$
V=(S,D,O,R,F,M,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the finite $\HyperbolicSectorCoverUp$ sector row listing the observer sectors under inspection.
\item $D$ is the $\HyperbolicShadowBoundaryUp$ shadow row naming the boundary-shadow windows attached to those sectors.
\item $O$ is the overlap ledger, recording exactly which displayed sector windows meet through their shared boundary-shadow rows.
\item $R$ is the $\HyperbolicSectorRefinementUp$ row recording refinement witnesses between displayed sectors and their smaller observer windows.
\item $F$ is the refusal ledger excluding unlogged sectors, selected geodesic fans, completed boundary measures, and host global geometry.
\item $M$ is the $\PoincareDiskMetricUp$ readback row used only after $S,D,O,R,F$ have exposed the sector and overlap surface.
\item $H$ records componentwise $\hsame$ transport for sector, shadow, overlap, refinement, refusal, metric-readback, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an observer-sector request through $S,D,O,R,F,M,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicSectorObserverNerveUp$, $\HyperbolicSectorCoverUp$, $\HyperbolicShadowBoundaryUp$, $\PoincareDiskMetricUp$, $\HyperbolicSectorRefinementUp$, $\PhaseDistanceObserverExpansionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicSectorObserverNerveUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It stores no ambient hyperbolic surface, no completed nerve realization, no selected ideal boundary, no analytic phase-distance theorem, and no host metric object outside the displayed packet.
\end{definition}

\begin{theorem}[HyperbolicSectorObserverNerve NameCert obligations]
\label{thm:hyperbolic-sector-observer-nerve-namecert-obligations}
For every accepted $\HyperbolicSectorObserverNerveUp$ carrier
$$
V=(S,D,O,R,F,M,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicSectorObserverNerveUp}$ surface consists exactly of sector admission, shadow-boundary visibility, overlap-ledger exactness, refinement-row exactness, refusal-ledger non-escape, Poincare disk metric readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No obligation is stored outside these finite rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:hyperbolic-sector-observer-nerve-carrier}. The accepted tuple displays the sector cover, shadow-boundary row, overlap ledger, refinement row, refusal ledger, metric readback, transport, replay, provenance, and local naming.

Shadow-boundary visibility reads $D$ only after the sector row $S$ is visible. The overlap ledger $O$ then records which displayed sectors meet, and the refinement row $R$ records the permitted sector refinements.

The refusal row $F$ is opened before the metric readback $M$, so any phase-distance consumer receives a visible boundary on unlogged sectors and host global geometry. The rows $H,C,P,N$ then preserve and name the same finite packet.
\end{proof}

\begin{theorem}[HyperbolicSectorObserverNerve phase-distance route]
\label{thm:hyperbolic-sector-observer-nerve-phase-distance-route}
Every phase-distance consumer of an accepted $\HyperbolicSectorObserverNerveUp$ carrier must first expose the finite route
$$
\HyperbolicSectorCoverUp,\ \HyperbolicShadowBoundaryUp,\ O,\ \HyperbolicSectorRefinementUp,\ F,\ \PoincareDiskMetricUp,\ H,C,P,N.
$$
In particular, the overlap and refinement rows are visible before any $\PhaseDistanceObserverExpansionUp$ read is consumed. The route does not export a global hyperbolic manifold, completed boundary measure, selected geodesic fan, or host metric completeness.
\end{theorem}
\begin{proof}
Start with a phase-distance request. The only sector source is $S$, and the only boundary read for that sector source is the displayed shadow row $D$.

Open the overlap and refinement rows next. The ledger $O$ records which displayed sectors meet, while $R$ records the permitted refinement relation before any metric readback can be consumed.

The refusal row $F$ remains between those finite sector rows and the metric readback $M$. Therefore a later $\PhaseDistanceObserverExpansionUp$ consumer sees the overlap and refinement boundary before it sees the metric-facing route.

Transport, replay, provenance, and local naming through $H,C,P,N$ preserve this route. Since the carrier has no row for a global hyperbolic manifold, completed boundary measure, selected geodesic fan, or host metric completeness, none of those claims can be exported.
\end{proof}

\falsifiablePrediction{Every accepted observer-sector read exposes the overlap and refinement rows before any phase-distance consumer can read a metric or expansion-facing output.}

\independenceWitness{hyperbolic-sector-cover, hyperbolic-shadow-boundary, poincare-disk-metric, hyperbolic-sector-refinement: $\HyperbolicSectorObserverNerveUp$ is not bijective to any listed sibling because it binds sector cover, boundary shadow, overlap, refinement, refusal, metric readback, transport, replay, provenance, and local naming into one observer-nerve packet.}

Future Lean work in $\mathsf{TasteGate}$ supplies the Nontrivial and FieldFaithful instances used by metric-facing consumers; this paper packet records only the finite sector, shadow, overlap, refinement, refusal, provenance, and NameCert rows that those instances may later consume.

\closureat{HyperbolicSectorObserverNerveUp}{seedStr}

\begin{closurestatus}{\HyperbolicSectorObserverNerveUp}
  \constructivestory{A $\HyperbolicSectorObserverNerveUp$ packet is generated from a HyperbolicSectorCover row, HyperbolicShadowBoundary row, overlap ledger, HyperbolicSectorRefinement row, refusal ledger, PoincareDiskMetric readback, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-sector-observer-nerve-carrier}, \autoref{thm:hyperbolic-sector-observer-nerve-namecert-obligations}, and \autoref{thm:hyperbolic-sector-observer-nerve-phase-distance-route} seed the finite observer-sector nerve surface for the hyperbolic observer-expansion route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a global hyperbolic manifold, completed boundary measure, selected geodesic fan, host metric completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked sector admission, shadow-boundary visibility, overlap-ledger exactness, refinement exactness, refusal non-escape, metric readback, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicSectorObserverNerveUp.}
\end{closurestatus}
