\chapter{A Concrete Naming Certificate for $\CauchyApproximationCategoryUp$}
\label{ch:concrete-instances-cauchy-approximation-category-namecert}
\origin{human}

Following Bishop and Bridges, the $\CauchyApproximationCategoryUp$ packet records the category-facing side of constructive completion as finite BEDC naming data. It sits beside the Cauchy approximation system of \autoref{ch:concrete-instances-cauchy-approximation-system-namecert}, the Cauchy approximation space of \autoref{ch:concrete-instances-cauchy-approximation-space-namecert}, the Bishop metric completion packet of \autoref{ch:concrete-instances-bishop-metric-completion-namecert}, the finite stream interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names objects and approximation-preserving arrows by displayed rows only; it does not introduce quotient completions, selected limits, ambient $\RealUp$ completeness, or a host category of all Cauchy objects.

\begin{definition}[Cauchy approximation category carrier]
\label{def:cauchy-approximation-category-carrier}
A $\CauchyApproximationCategoryUp$ carrier is a finite $\BHist$ packet
$$
K=(O,M,I,Q,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $O$ is the object row. Each object is a named finite Cauchy approximation packet whose visible coordinates are sourced from $\CauchyApproximationSystemUp$ or $\CauchyApproximationSpaceUp$ rows.
\item $M$ is the morphism approximation data row. A morphism carries displayed source and target object names, a finite window transformer, dyadic tolerance transport, and a regular-readback compatibility ledger.
\item $I$ is the identity route. It records the reflexive window transformer, unchanged dyadic tolerance rows, unchanged $\RegSeqRatUp$ readback, and unchanged $\RealUp$ seal for each displayed object.
\item $Q$ is the composition route. It records the finite pasting of two morphism approximation rows, including the composed window transformer, composed tolerance ledger, composed readback comparison, and Real-seal replay.
\item $W$ is the finite-window row, reading every object and morphism request through $\StreamNameUp$ windows before any regular readback or Real seal is opened.
\item $R$ is the regular readback row, exposing the $\RegSeqRatUp$ observations used by objects, identities, and composites.
\item $E$ is the $\RealUp$ seal row, available only after the finite windows, dyadic tolerances, regular readbacks, identity checks, and composition checks have been displayed.
\item $H$ records componentwise $\hsame$ transport for $O,M,I,Q,W,R,E,C,P,N$ and for the displayed approximation comparisons.
\item $C$ records displayed $\Cont$ replay for object admission, morphism approximation, identity replay, composition replay, finite-window access, regular readback, Real sealing, and transport.
\item $P$ is the $\Pkg$ provenance over $\CauchyApproximationCategoryUp$, $\CauchyApproximationSystemUp$, $\CauchyApproximationSpaceUp$, $\BishopMetricCompletionUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyApproximationCategoryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $O,M,I,Q,W,R,E,H,C,P,N$. It has no coordinate for quotient Cauchy equality, selected representatives, chosen limits, large host category structure, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Cauchy approximation category NameCert obligations]
\label{thm:cauchy-approximation-category-obligations}
For every accepted $\CauchyApproximationCategoryUp$ carrier
$$
K=(O,M,I,Q,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\CauchyApproximationCategoryUp}$ surface consists exactly of object admission through $O$; morphism approximation admission through $M$; identity-route exactness through $I$; composition-route exactness through $Q$; finite-window exposure through $W$; regular readback through $R$; $\RealUp$ sealing through $E$; componentwise $\hsame$ transport through $H$; displayed $\Cont$ replay through $C$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-approximation-category-carrier}. The object and morphism coordinates are $O$ and $M$, and the only category-structure coordinates are the displayed identity and composition rows $I$ and $Q$.

The finite analytic access is forced through $W,R,E$. A reader first opens StreamName windows, then the RegSeqRat readback tied to those windows, and only then the Real seal. The dyadic tolerance data are the displayed comparison rows carried by $M,I,Q$ and packaged by $P$.

Identity exactness is constructive because $I$ is a reflexive replay row. It preserves the same object, the same finite windows, the same dyadic tolerance rows, the same regular readback, and the same Real seal, so it introduces no representative choice or quotient equality.

Composition exactness is constructive because $Q$ is a finite pasting row. Given two displayed morphism approximation rows, the composed window transformer and tolerance ledger are written as coordinates of the packet before any downstream readback is consumed.

Finally $H,C,P,N$ transport, replay, source, and name the same finite sequence. Since the carrier contains no coordinate for selected limits, quotient completions, ambient Real completeness, countable choice, or host category structure, the listed rows exhaust the local NameCert obligations.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyApproximationCategoryUp$ read exposes object rows, morphism approximation rows, identity and composition routes, StreamName finite windows, RegSeqRat readback, RealUp seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{cauchy-approximation-system, cauchy-approximation-space, bishop-metric-completion, streamname, regseqrat, dyadicratcore, real: $\CauchyApproximationCategoryUp$ is not bijective to any listed sibling because it binds approximation-preserving arrows with identity and composition rows before any completion-facing consumer reads a Real seal.}

\closureat{CauchyApproximationCategoryUp}{seedStr}

\begin{closurestatus}{\CauchyApproximationCategoryUp}
  \constructivestory{A $\CauchyApproximationCategoryUp$ packet is generated from object rows, morphism approximation rows, identity and composition routes, finite StreamName windows, RegSeqRat readback, RealUp seal rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-approximation-category-carrier} and \autoref{thm:cauchy-approximation-category-obligations} seed the category-facing Cauchy approximation carrier over objects, approximation-preserving morphisms, identity and composition routes, finite windows, regular readback, Real seals, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completions, selected limits, host category completeness, large-category universality, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, object and morphism approximation exactness, identity and composition route exactness, finite-window access, regular readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CauchyApproximationCategoryUp.}
\end{closurestatus}
