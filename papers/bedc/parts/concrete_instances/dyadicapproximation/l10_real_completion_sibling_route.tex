DyadicApproximation terminal finite packets are consumed through the $\DyadicRatCoreUp$ observation ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the $\StreamNameUp$ finite-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the $\RegSeqRatUp$ readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, and the $\RealCauchyCompletionUp$ seal rows of \autoref{ch:concrete-instances-real-cauchy-completion-namecert}. This records the horizontal route from terminal dyadic observations to the Real-completion consumer as a finite sibling dependency rather than an ambient completion assumption.

\begin{theorem}[DyadicApproximation L10 Real-completion sibling route]
\label{thm:dyadic-approximation-l10-real-completion-sibling-route}
Let $T$ be an accepted terminal $\DyadicApproximationUp$ finite packet, and let a $\RealCauchyCompletionUp$ consumer read the terminal real-facing row of $T$. Suppose the terminal packet is matched with the $\DyadicRatCoreUp$ ledger rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite $\StreamNameUp$ windows of \autoref{ch:concrete-instances-streamname-namecert}, the $\RegSeqRatUp$ readback rows of \autoref{ch:concrete-instances-regseqrat-namecert}, and the $\RealCauchyCompletionUp$ seal rows of \autoref{ch:concrete-instances-real-cauchy-completion-namecert} by the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of the terminal packet.

Then every accepted terminal read consumed by that $\RealCauchyCompletionUp$ surface factors through the sibling chain
$$
\DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealCauchyCompletionUp
$$
before the completion consumer may inspect the terminal seal. The exported surface is exactly the finite packet route where DyadicRatCore supplies the finite observation and ledger rows, StreamName schedules finite windows over those rows, RegSeqRat supplies regular readback, and RealCauchyCompletion consumes the resulting seal rows together with $\hsame$ transports, $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ provenance. It exports no quotient stream, selected host limit, host real equality, solver oracle, ambient completeness principle, or completion object outside the displayed sibling rows.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicApproximationUp.DyadicApproximationCarrier\_l10\_real\_completion\_sibling\_route}
\begin{proof}
First normalize the terminal Real-completion route. The terminal tail-budget theorem \autoref{thm:dyadic-approximation-terminal-tail-budget-real-completion-route} places the accepted terminal packet on the displayed budget-to-seal route through terminal precision, transported endpoint, interval cell, mesh selector, validated enclosure, finite tail ledger, and $\RealUp$ seal.

Next attach the diagonal handoff. The theorem \autoref{thm:dyadicapproximation-terminal-diagonal-completion-handoff} identifies the terminal route with the diagonal completion packet on the same $\DyadicRatCoreUp$ observation ledger, scheduled $\StreamNameUp$ window, $\RegSeqRatUp$ readback, seal ledger, transport rows, continuation rows, package provenance, and local certificate rows.

Now read the completion consumer through the sibling completion surface. Apply \autoref{thm:real-cauchy-completion-diagonal-handoff}, \autoref{thm:real-cauchy-completion-modulus-diagonal-exactness}, and \autoref{thm:real-cauchy-completion-real-seal-readback-scope}. These RealCauchyCompletion rows force the completion read through the modulus-selected diagonal packet, dyadic observation ledger, finite stream window, regular-rational readback, and real seal.

Finally apply \autoref{thm:real-cauchy-completion-public-real-name-export}. Repacking the dyadic terminal route with the RealCauchyCompletion export yields exactly the sibling chain through \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-cauchy-completion-namecert}. The non-escape clauses in the cited terminal, diagonal, and completion theorems exclude quotient streams, selected host limits, host real equality, solver oracles, ambient completeness, and any completion object outside the displayed finite packet rows.
\end{proof}

\begin{theorem}[DyadicApproximation top-hub L10 sibling route]
\label{thm:dyadic-approximation-top-hub-l10-sibling-route}
Every top-level $\DyadicApproximationUp$ read exported through the hub of \autoref{ch:concrete-instances-dyadicapproximation-namecert} factors through the same four sibling faces
$$
\DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealCauchyCompletionUp
$$
named by \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-cauchy-completion-namecert}. The hub-visible export is exhausted by the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of those sibling faces together with the local terminal $\DyadicApproximationUp$ rows. It exports no quotient stream, selected host limit, host real equality, solver oracle, ambient completeness principle, or hidden completion object.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicApproximationUp.DyadicApproximationCarrier\_top\_hub\_l10\_sibling\_route}
\begin{proof}
Begin with the sibling-route theorem \autoref{thm:dyadic-approximation-l10-real-completion-sibling-route}. It already places every terminal $\RealCauchyCompletionUp$ read of an accepted $\DyadicApproximationUp$ packet on the four-face chain through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealCauchyCompletionUp$.

The terminal tail-budget Real-completion route of \autoref{thm:dyadic-approximation-terminal-tail-budget-real-completion-route} supplies the terminal budget-to-seal route consumed by the hub. The terminal diagonal completion handoff of \autoref{thm:dyadicapproximation-terminal-diagonal-completion-handoff} identifies that terminal route with the same diagonal packet, scheduled window, regular-rational readback, seal ledger, visible $\hsame$ transports, visible $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ rows.

Finally use the RealCauchyCompletion readback and export rows: \autoref{thm:real-cauchy-completion-diagonal-handoff}, \autoref{thm:real-cauchy-completion-modulus-diagonal-exactness}, \autoref{thm:real-cauchy-completion-real-seal-readback-scope}, and \autoref{thm:real-cauchy-completion-public-real-name-export}. These rows force the top-level hub read through the same finite completion route and leave no coordinate for a quotient stream, selected host limit, host real equality, solver oracle, ambient completeness principle, or hidden completion object.
\end{proof}

\begin{theorem}[DyadicApproximation terminal Real-completion exit criterion]
\label{thm:dyadicapproximation-terminal-real-completion-exit-criterion}
Let $T$ be an accepted terminal $\DyadicApproximationUp$ finite packet. A terminal read of $T$ is admitted for consumption by the $\RealCauchyCompletionUp$ surface exactly when the read displays the $\DyadicRatCoreUp$ observation ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, a finite $\StreamNameUp$ window from \autoref{ch:concrete-instances-streamname-namecert}, the $\RegSeqRatUp$ regular readback rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the $\RealCauchyCompletionUp$ seal rows of \autoref{ch:concrete-instances-real-cauchy-completion-namecert}, and the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows carried by the terminal packet.

In the forward direction, every admitted terminal $\RealCauchyCompletionUp$ read must expose those five families of displayed rows. In the reverse direction, any terminal read displaying those rows is consumable by the $\RealCauchyCompletionUp$ surface through the four-face route
$$
\DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealCauchyCompletionUp .
$$
The criterion has no substitute coordinate: quotient stream equality, selected host limits, host real equality, solver oracles, ambient completeness, and hidden completion objects cannot replace any displayed face or support row.
\end{theorem}
\begin{proof}
For the forward direction, apply \autoref{thm:dyadic-approximation-l10-real-completion-sibling-route} to the admitted terminal consumer read. Its conclusion already factors the read through the $\DyadicRatCoreUp$ ledger, finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, and $\RealCauchyCompletionUp$ seal rows, with the visible $\hsame$ transports, $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ provenance on the same terminal packet. The terminal packet itself supplies the shared $\BHist$ support.

For the reverse direction, apply \autoref{thm:dyadic-approximation-top-hub-l10-sibling-route} to the displayed terminal route. The hub factorization repacks the displayed ledger, window, readback, seal, transport, continuation, package, history, and certificate rows into the same four-face sibling route consumed by the $\RealCauchyCompletionUp$ surface.

Finally use the RealCauchyCompletion readback and export rows cited in the top-hub theorem: \autoref{thm:real-cauchy-completion-diagonal-handoff}, \autoref{thm:real-cauchy-completion-modulus-diagonal-exactness}, \autoref{thm:real-cauchy-completion-real-seal-readback-scope}, and \autoref{thm:real-cauchy-completion-public-real-name-export}. These rows leave the completion consumer with only the displayed finite faces and support rows, so none of quotient stream equality, selected host limits, host real equality, solver oracles, ambient completeness, or hidden completion objects can witness terminal admission.
\end{proof}
