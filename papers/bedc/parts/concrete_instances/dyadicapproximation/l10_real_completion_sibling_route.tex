\begin{theorem}[DyadicApproximation L10 Real-completion sibling route]
\label{thm:dyadic-approximation-l10-real-completion-sibling-route}
Let $T$ be an accepted terminal $\DyadicApproximationUp$ finite packet, and let a $\RealCauchyCompletionUp$ consumer read the terminal real-facing row of $T$. Suppose the terminal packet is matched with the finite $\StreamNameUp$ windows of \autoref{ch:concrete-instances-streamname-namecert}, the $\RegSeqRatUp$ readback rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the $\DyadicRatCoreUp$ ledger rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the $\RealCauchyCompletionUp$ seal rows of \autoref{ch:concrete-instances-real-cauchy-completion-namecert} by the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of the terminal packet.

Then every accepted terminal read consumed by that $\RealCauchyCompletionUp$ surface factors through the sibling chain
$$
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \RealCauchyCompletionUp
$$
before the completion consumer may inspect the terminal seal. The exported surface is exactly the finite packet route consisting of scheduled StreamName windows, RegSeqRat readback, DyadicRatCore ledger rows, RealUp seal rows, $\hsame$ transports, $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ provenance. It exports no quotient stream, selected host limit, host real equality, solver oracle, ambient completeness principle, or completion object outside the displayed sibling rows.
\end{theorem}
\leanchecked{BEDC.Derived.DyadicApproximationUp.DyadicApproximationCarrier\_l10\_real\_completion\_sibling\_route}
\begin{proof}
First normalize the terminal Real-completion route. The terminal tail-budget theorem \autoref{thm:dyadic-approximation-terminal-tail-budget-real-completion-route} places the accepted terminal packet on the displayed budget-to-seal route through terminal precision, transported endpoint, interval cell, mesh selector, validated enclosure, finite tail ledger, and $\RealUp$ seal.

Next attach the diagonal handoff. The theorem \autoref{thm:dyadicapproximation-terminal-diagonal-completion-handoff} identifies the terminal route with the diagonal completion packet on the same scheduled $\StreamNameUp$ window, $\RegSeqRatUp$ readback, seal ledger, transport rows, continuation rows, package provenance, and local certificate rows.

Now read the completion consumer through the sibling completion surface. Apply \autoref{thm:real-cauchy-completion-diagonal-handoff}, \autoref{thm:real-cauchy-completion-modulus-diagonal-exactness}, and \autoref{thm:real-cauchy-completion-real-seal-readback-scope}. These RealCauchyCompletion rows force the completion read through the modulus-selected diagonal packet, finite stream window, regular-rational readback, dyadic observation ledger, and real seal.

Finally apply \autoref{thm:real-cauchy-completion-public-real-name-export}. Repacking the dyadic terminal route with the RealCauchyCompletion export yields exactly the sibling chain through \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-cauchy-completion-namecert}. The non-escape clauses in the cited terminal, diagonal, and completion theorems exclude quotient streams, selected host limits, host real equality, solver oracles, ambient completeness, and any completion object outside the displayed finite packet rows.
\end{proof}
