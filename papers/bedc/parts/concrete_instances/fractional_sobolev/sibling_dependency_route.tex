The horizontal dependency route for $\FractionalSobolevUp$ is the finite non-integer regularity edge from \autoref{ch:concrete-instances-sobolev-namecert} through the normed and Hilbert magnitude rows of \autoref{ch:concrete-instances-norm-namecert} and \autoref{ch:concrete-instances-hilbert-namecert}, with distribution-style and derivative-style observation supplied by \autoref{ch:concrete-instances-distribution-namecert}, \autoref{ch:concrete-instances-derivative-namecert}, and \autoref{ch:concrete-instances-weak-derivative-namecert}. Thus the packet consumes existing concrete-instance regularity rows as sibling data before exposing its fractional seminorm ledger; it does not create an ambient quotient function space or a completed interpolation theorem.
