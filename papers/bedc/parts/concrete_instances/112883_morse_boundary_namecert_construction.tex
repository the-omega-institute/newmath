\chapter{A Concrete Naming Certificate for $\MorseBoundaryUp$}
\label{ch:concrete-instances-morse-boundary-namecert}
\origin{human}

Following Gromov's boundary viewpoint and Bridson--Haefliger's treatment of hyperbolic and CAT(0) geometry, the $\MorseBoundaryUp$ packet records stable geodesic directions as finite BEDC NameCert data. It sits after the finite boundary-shadow route of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the horofunction boundary comparison of \autoref{ch:concrete-instances-hyperbolic-horofunction-boundary-namecert}, the metric-space carrier of \autoref{ch:concrete-instances-metric-space-namecert}, and the geodesic metric surface of \autoref{ch:concrete-instances-geodesic-metric-namecert}. The packet realizes the hyperbolic observer-expansion route of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite geodesic stability and fellow-traveller NameCert data; it does not complete an ideal boundary, quotient geodesic rays, select global representatives, or import host hyperbolic geometry.

\begin{definition}[Morse boundary carrier]
\label{def:morse-boundary-carrier}
A $\MorseBoundaryUp$ carrier is a finite $\BHist$ packet
$$
M=(X,G,S,F,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\MetricSpaceUp$ source row, naming the finite metric comparison surface used by the packet.
\item $G$ is the geodesic row, read through $\GeodesicUp$ and the $\GeodesicMetricUp$ surface, exposing finite segment incidence and endpoint data.
\item $S$ is the Morse-stability row, recording the displayed stability gauge for geodesic or quasi-geodesic detours.
\item $F$ is the fellow-traveller row, recording finite comparison windows in which stable representatives remain visibly close.
\item $B$ is the boundary-read row, connecting the stable geodesic route to $\HyperbolicBoundaryShadowUp$ and $\HyperbolicHorofunctionBoundaryUp$ consumers.
\item $H$ records componentwise $\hsame$ transport for metric, geodesic, stability, fellow-traveller, boundary-read, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from metric source through geodesic incidence, stability windows, fellow-traveller comparison, boundary readback, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\MorseBoundaryUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicHorofunctionBoundaryUp$, $\MetricSpaceUp$, $\GeodesicUp$, $\GeodesicMetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MorseBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,G,S,F,B,H,C,P,N$. It has no coordinate for a completed ideal boundary, quotient equality of rays, selected global geodesics, host compactification, or ambient hyperbolic model.
\end{definition}

\begin{theorem}[Morse boundary NameCert obligations]
\label{thm:morse-boundary-namecert-obligations}
For every accepted $\MorseBoundaryUp$ carrier $M=(X,G,S,F,B,H,C,P,N)$, the local $\NameCert_{\MorseBoundaryUp}$ surface consists exactly of metric-source admission, geodesic incidence exposure, Morse-stability gauge display, fellow-traveller window comparison, boundary readback, componentwise transport, displayed continuation replay, package provenance, and local naming. Every boundary consumer must inspect these rows before it treats the packet as a Morse boundary read.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:morse-boundary-carrier}. The metric row $X$ fixes the finite source surface, and the geodesic row $G$ exposes the displayed segment and endpoint data consumed by the packet.

Read $S$ and $F$ next. The stability row records the Morse gauge, while the fellow-traveller row records the finite windows in which representative routes remain visibly close.

The boundary row $B$ then hands the stable route to boundary-shadow and horofunction-boundary consumers. The structural rows $H,C,P,N$ transport, replay, source, and name the same finite data, so the packet exports no quotient ray, selected representative, host compactification, or ambient hyperbolic model.
\end{proof}

\begin{theorem}[Morse boundary geodesic stability]
\label{thm:morse-boundary-geodesic-stability}
Every accepted $\MorseBoundaryUp$ read exposes geodesic stability as the finite route
$$
\MetricSpaceUp \longmapsto \GeodesicUp \longmapsto S \longmapsto F \longmapsto B .
$$
The route first displays metric and geodesic rows, then records a Morse-stability gauge, then records fellow-traveller windows, and only then exports a boundary read to $\HyperbolicBoundaryShadowUp$ or $\HyperbolicHorofunctionBoundaryUp$ consumers.
\end{theorem}
\begin{proof}
Start with $X$ and $G$. These rows identify the metric source and the geodesic incidence data before any stability claim is public.

The stability row $S$ binds the detour-control gauge to that displayed geodesic route. The fellow-traveller row $F$ then records the finite comparison windows that make the stable route readable by downstream boundary consumers.

Finally $B,H,C,P,N$ export the boundary read, transport it componentwise, replay the displayed route, source it by package provenance, and name it locally. Since the export is built only from these rows, it is a finite geodesic-stability surface rather than a completed boundary or quotient of rays.
\end{proof}

\falsifiablePrediction{Every accepted $\MorseBoundaryUp$ packet must expose metric-source, geodesic, Morse-stability, fellow-traveller, boundary-read, transport, replay, provenance, and NameCert rows before a boundary consumer may read stable geodesic direction data.}

\independenceWitness{hyperbolic-boundary-shadow, hyperbolic-horofunction-boundary, metric-space, geodesic: $\MorseBoundaryUp$ is not bijective to any listed sibling because it jointly carries metric-source admission, geodesic incidence, stability gauge, fellow-traveller windows, boundary readback, transport, replay, provenance, and local naming.}

\closureat{MorseBoundaryUp}{seedStr}

\begin{closurestatus}{\MorseBoundaryUp}
  \constructivestory{A $\MorseBoundaryUp$ packet is generated from MetricSpace source rows, Geodesic and GeodesicMetric incidence rows, a Morse-stability gauge, fellow-traveller comparison windows, boundary-shadow and horofunction-boundary readback, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:morse-boundary-carrier}, \autoref{thm:morse-boundary-namecert-obligations}, and \autoref{thm:morse-boundary-geodesic-stability} seed the finite geodesic-stability and fellow-traveller route for Morse boundary consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim completed ideal boundaries, quotient equality of rays, selected global geodesic representatives, host compactification, ambient hyperbolic geometry, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, metric-source routing, geodesic incidence, stability-gauge exactness, fellow-traveller comparison, boundary readback, transport, replay, provenance, and local naming for BEDC.Derived.MorseBoundaryUp.}
\end{closurestatus}
