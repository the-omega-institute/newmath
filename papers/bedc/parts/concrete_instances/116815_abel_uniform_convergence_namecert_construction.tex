\chapter{A Concrete Naming Certificate for $\AbelUniformConvergenceUp$}
\label{ch:concrete-instances-abel-uniform-convergence-namecert}
\origin{human}

Following Abel, Hardy, and Bishop--Bridges constructive analysis, the $\AbelUniformConvergenceUp$ packet records the finite interface by which Abel radial convergence is read uniformly over displayed radius windows. It sits after the Abel theorem route of \autoref{ch:concrete-instances-abel-limit-theorem-namecert}, the uniform limit packet of \autoref{ch:concrete-instances-uniformlimit-namecert}, the finite stream-name surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter packages traditional Abel uniform convergence as finite BEDC rows; it does not assert ambient real completeness, countable choice, quotient stream equality, or selected infinite sums.

\begin{definition}[AbelUniformConvergence carrier]
\label{def:abel-uniform-convergence-carrier}
An $\AbelUniformConvergenceUp$ carrier is a finite $\BHist$ packet
$$
A=(R,W,S,U,T,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the finite radius-window row listing the dyadic subradius cells and boundary-approach windows inspected by the Abel consumer.
\item $W$ is the $\StreamNameUp$ window row exposing only the finite coefficient and partial-sum prefixes requested by $R$.
\item $S$ is the Abel partial-sum row whose observations are read through the same finite windows.
\item $U$ is the $\UniformLimitUp$ ledger row, assigning one displayed tail threshold to every radius cell in $R$.
\item $T$ is the $\AbelTheoremUp$ boundary row recording the radial handoff after $R,W,S,U$ have been accepted.
\item $Q$ is the $\RegSeqRatUp$ readback row obtained from the synchronized Abel windows.
\item $E$ is the terminal $\RealUp$ seal row available only after the Abel boundary and regular rational readback have been displayed.
\item $H$ records componentwise $\hsame$ transport for radius windows, stream windows, partial sums, uniform-tail data, Abel boundary data, readback, seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from each Abel uniform convergence request through $R,W,S,U,T,Q,E$.
\item $P$ is the $\Pkg$ provenance over $\AbelUniformConvergenceUp$, $\AbelTheoremUp$, $\UniformLimitUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\AbelUniformConvergenceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $R,W,S,U,T,Q,E,H,C,P,N$. It has no coordinate for ambient real completeness, countable choice, quotient stream equality, selected infinite sums, or a Real seal detached from the displayed Abel uniform route.
\end{definition}

\begin{theorem}[AbelUniformConvergence NameCert obligations]
\label{thm:abel-uniform-convergence-namecert-obligations}
For every accepted $\AbelUniformConvergenceUp$ carrier
$$
A=(R,W,S,U,T,Q,E,H,C,P,N),
$$
the local $\NameCert_{\AbelUniformConvergenceUp}$ surface consists exactly of carrier admission for the displayed rows, radius-window admission through $R$, finite prefix exposure through $W$, Abel partial-sum alignment through $S$, uniform tail exactness through $U$, Abel boundary handoff through $T$, regular rational readback and Real seal nonescape through $Q$ and $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$. No obligation row supplies ambient real completeness, countable choice, quotient stream equality, selected infinite sums, host analytic equality, or an unbounded stream selector.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:abel-uniform-convergence-carrier}. Its mathematical rows are the radius-window row $R$, the stream window row $W$, the partial-sum row $S$, the uniform-tail ledger $U$, the Abel boundary row $T$, the regular rational readback $Q$, and the Real seal $E$.

The radius and stream rows fix the finite inspection surface. Every coefficient or partial-sum observation is read through $W$ only after a displayed radius cell in $R$ has been selected.

The uniform row supplies the common tail budget. Because $U$ is a $\UniformLimitUp$ ledger over the same windows, the Abel boundary read in $T$ cannot choose a different threshold for each point of the displayed radius window.

The readback and seal rows are downstream. The row $Q$ repacks the synchronized Abel windows as regular rational data, and $E$ exposes the terminal Real seal only after $R,W,S,U,T,Q$ have been read.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier has no coordinate for ambient real completeness, countable choice, quotient stream equality, selected infinite sums, host analytic equality, or an unbounded selector, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[AbelUniformConvergence Real seal route]
\label{thm:abel-uniform-convergence-real-seal-route}
Every $\RealUp$ or completion-facing consumer of an accepted $\AbelUniformConvergenceUp$ packet reads the endpoint only through the finite route
$$
\AbelTheoremUp,\ \UniformLimitUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp .
$$
The consumer first receives displayed radius windows, finite stream prefixes, one uniform tail ledger, and the Abel boundary handoff; only then may it consume the regular rational readback and terminal Real seal. The route exports no ambient real completeness theorem, countable-choice schedule, quotient stream equality, selected infinite sum, or host equality of boundary values.
\end{theorem}
\begin{proof}
Start from the accepted packet and read the displayed radius and stream rows $R,W$. These rows expose only finite windows, so the consumer has no independent stream quotient or unbounded selector.

Apply the uniform-tail row $U$. It supplies one threshold discipline for the whole displayed radius window, and therefore binds the Abel boundary read to the same finite window surface.

Read the Abel boundary row $T$ through the sibling theorem route. The row records the radial handoff only after $R,W,S,U$ have fixed the finite convergence surface.

Finally pass through $\RegSeqRatUp$ readback $Q$ and terminal $\RealUp$ seal $E$, with $H,C,P,N$ preserving the same route. The refused objects are absent from the carrier, so every Real-facing read remains inside the displayed finite package.
\end{proof}

\falsifiablePrediction{Every accepted AbelUniformConvergenceUp read exposes a finite radius-window row, StreamNameUp prefix row, Abel partial-sum row, UniformLimitUp tail ledger, AbelTheoremUp boundary row, RegSeqRatUp readback, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert before a Real-facing consumer can inspect the endpoint.}

\independenceWitness{abel-theorem, uniformlimit, streamname, regseqrat, real: $\AbelUniformConvergenceUp$ is not bijective to any listed sibling because it jointly carries radius-window admission, finite prefix exposure, Abel partial-sum alignment, one uniform tail ledger, Abel boundary handoff, regular rational readback, terminal Real sealing, transport, replay, provenance, and local naming.}

\closureat{AbelUniformConvergenceUp}{seedStr}

\begin{closurestatus}{\AbelUniformConvergenceUp}
  \constructivestory{An $\AbelUniformConvergenceUp$ packet is generated from radius-window rows, StreamName finite prefixes, Abel partial sums, a UniformLimit tail ledger, an AbelTheorem boundary row, RegSeqRat readback, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:abel-uniform-convergence-carrier}, \autoref{thm:abel-uniform-convergence-namecert-obligations}, and \autoref{thm:abel-uniform-convergence-real-seal-route} seed the finite Abel uniform convergence route over AbelTheorem, UniformLimit, StreamName, RegSeqRat, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient real completeness, countable choice, quotient stream equality, selected infinite sums, host analytic equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires a Lean declaration for BEDC.Derived.AbelUniformConvergenceUp covering carrier admission, radius-window admission, finite prefix exposure, uniform-tail exactness, Abel boundary handoff, readback, Real seal nonescape, transport, replay, provenance, and local naming.}
\end{closurestatus}
