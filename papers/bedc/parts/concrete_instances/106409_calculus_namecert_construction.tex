\chapter{A Concrete Naming Certificate for $\CalculusUp$}
\label{ch:concrete-instances-calculus-namecert}
\origin{human}

Following Cartan's local differential calculus and the Bishop--Bridges constructive treatment of limits, derivatives, and integrals, the $\CalculusUp$ packet records the finite analysis root used by later derivative and integration chapters. It sits beside the real-name boundary of \autoref{ch:concrete-instances-real-namecert}, the generic limit route of \autoref{ch:concrete-instances-limit-namecert}, the continuous-map surface used throughout the analysis region, the derivative carrier of \autoref{ch:concrete-instances-derivative-namecert}, the integral carrier, and the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}. The packet names the common calculus substrate without importing a host function space, a global limit operator, quotient equality of functions, or an ambient completeness theorem.

\begin{definition}[Calculus local-linearization carrier]
\label{def:calculus-local-linearization-carrier}
A $\CalculusUp$ local-linearization carrier is a finite $\BHist$ packet
$$
\Lambda=(E,R,D,L,J,H,C,P,N)
$$
whose rows are the terminal $\RealUp$ seal $E$, the $\RegSeqRatUp$ readback
row $R$, the $\DyadicRatCoreUp$ tolerance ledger $D$, the $\DerivativeUp$
finite difference-quotient row $L$, the $\RiemannIntegralUp$ finite
tagged-sum row $J$, componentwise $\hsame$ transport $H$, displayed $\Cont$
replay $C$, $\Pkg$ provenance $P$, and the local
$\NameCert_{\CalculusUp}$ row $N$. The classifier compares accepted packets
only through these rows. It has no coordinate for a host differentiability
theorem, selected antiderivative, quotient real equality, or ambient
$\RealUp$ completeness.
\end{definition}

\begin{theorem}[Calculus derivative continuation obligation]
\label{thm:calculus-derivative-continuation-obligation}
Every accepted $\CalculusUp$ local-linearization carrier
$\Lambda=(E,R,D,L,J,H,C,P,N)$ exposes a derivative-facing continuation only
after the $\RealUp$ seal $E$, $\RegSeqRatUp$ readback $R$,
$\DyadicRatCoreUp$ tolerance ledger $D$, $\DerivativeUp$ difference-quotient
row $L$, $\hsame$ transport $H$, $\Cont$ replay $C$, provenance $P$, and local
NameCert row $N$ have been displayed. The read cannot export a host
differentiability theorem, quotient real equality, or ambient real
completeness.
\end{theorem}
\begin{proof}
Project the carrier of
\autoref{def:calculus-local-linearization-carrier}. The derivative-facing row
is $L$, and its tolerance and value data are visible only together with the
real seal $E$, regular-rational readback $R$, and dyadic ledger $D$.

The structural rows $H,C,P,N$ transport, replay, source, and name that same
finite packet. Since none of those rows contains a host differentiability
theorem or quotient real equality, the continuation remains a displayed
finite difference-quotient route.

The Riemann row $J$ may coexist in the packet, but it does not supply
derivative data. Thus every derivative consumer must read through
$E,R,D,L,H,C,P,N$ before any derivative-facing conclusion is public.
\end{proof}

\begin{theorem}[Calculus integral finite-sum obligation]
\label{thm:calculus-integral-finite-sum-obligation}
Every accepted $\CalculusUp$ local-linearization carrier
$\Lambda=(E,R,D,L,J,H,C,P,N)$ exposes an integral-facing read only through the
$\RiemannIntegralUp$ finite tagged-sum row $J$ together with the $\RealUp$
seal $E$, $\RegSeqRatUp$ readback $R$, $\DyadicRatCoreUp$ tolerance ledger
$D$, componentwise transport, replay, provenance, and local naming. The read
does not select an antiderivative and does not import quotient real equality
or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read $J$ from the carrier. It records the partition, tags, finite sum, and
endpoint handoff as displayed calculus data, so an integral consumer cannot
bypass the finite-sum route.

The rows $E,R,D$ provide the real-facing seal, regular-rational comparison,
and dyadic tolerance ledger used to interpret that sum. The structural rows
$H,C,P,N$ preserve the same finite route under accepted transport and replay.

The derivative row $L$ remains separate. Because no coordinate of
$\Lambda$ contains a selected antiderivative, host integration functional,
quotient real equality, or ambient completeness theorem, the integral
obligation is exactly the finite-sum route through $J$ and the displayed
support rows.
\end{proof}

\begin{theorem}[Calculus limit real-seal obligation]
\label{thm:calculus-limit-real-seal-obligation}
Every $\RealUp$-facing limit read exported by an accepted $\CalculusUp$
local-linearization carrier factors through the displayed $\RegSeqRatUp$
readback row $R$, $\DyadicRatCoreUp$ tolerance ledger $D$, derivative row $L$
or Riemann finite-sum row $J$, terminal $\RealUp$ seal $E$, $\hsame$
transport, $\Cont$ replay, provenance, and local NameCert rows. No such read
may use a host limit theorem, quotient real equality, selected antiderivative,
or ambient $\RealUp$ completeness as local data.
\end{theorem}
\begin{proof}
Start from a Real-facing calculus read. The carrier exposes $E$ as the only
terminal real seal, so every such read must arrive at $E$ through the displayed
regular-rational and dyadic rows $R,D$.

There are two calculus sources for that seal. A derivative read uses $L$ and
the finite difference-quotient route; an integral read uses $J$ and the
finite tagged-sum route. Both sources are transported and replayed only by
$H$ and $C$, with provenance and local naming recorded by $P$ and $N$.

Since the carrier has no coordinate for host limits, quotient real equality,
selected antiderivatives, or ambient completeness, the real-seal obligation is
confined to the displayed BEDC rows.
\end{proof}

\begin{theorem}[Calculus RegSeqRat dyadic error route]
\label{thm:calculus-regseqrat-dyadic-error-route}
Every $\CalculusUp$ Real-facing error estimate exposed by an accepted local-linearization carrier $\Lambda=(E,R,D,L,J,H,C,P,N)$ passes through the $\RegSeqRatUp$ readback row $R$ and the $\DyadicRatCoreUp$ tolerance ledger $D$ before the terminal $\RealUp$ seal $E$ is consumed. The route is finite packet readback and not a host analytic estimate, quotient real equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the local-linearization carrier. The rows $R$ and $D$ are the only coordinates that record rational readback and dyadic tolerance data for a Real-facing calculus estimate.

The derivative source row $L$ and the Riemann finite-sum source row $J$ both sit before the terminal seal $E$. Their estimates can be transported and replayed only by the displayed structural rows $H,C,P,N$.

Thus any accepted Real-facing error estimate must read $R,D$ before $E$. Since the carrier has no coordinate for host analysis, quotient real equality, or ambient completeness, no other route can supply the estimate.
\end{proof}

\begin{theorem}[Calculus ContinuousMap limit route]
\label{thm:calculus-continuousmap-limit-route}
Every continuous-map limit read exposed by an accepted $\CalculusUp$ local-linearization carrier must display the graph and pointwise replay data of $\ContinuousMapUp$ before any $\LimitUp$ or $\RealUp$ seal is consumed. In the local-linearization packet this means the derivative or Riemann source row is read only after its graph data has been routed through $R,D,H,C,P,N$ toward $E$.
\end{theorem}
\begin{proof}
Read the carrier rows in order. The local-linearization packet has no separate host function-space coordinate; graph and pointwise replay data can appear only through the displayed calculus source rows and their replay row $C$.

The real-seal obligation already confines limit-facing reads to the regular-rational row $R$, dyadic tolerance row $D$, one of the calculus source rows $L,J$, and the seal $E$. Therefore a continuous-map consumer cannot reach the Limit or Real seal without first displaying the graph data attached to that source row.

The transport, provenance, and local naming rows preserve the same dependency order. Hence the route is a finite ContinuousMap-to-Limit-to-Real read, not an ambient continuity theorem or a hidden limit operator.
\end{proof}

\begin{theorem}[Calculus Riemann derivative separation]
\label{thm:calculus-riemann-derivative-separation}
In every accepted $\CalculusUp$ local-linearization carrier $\Lambda=(E,R,D,L,J,H,C,P,N)$, the $\DerivativeUp$ row $L$ and the $\RiemannIntegralUp$ finite-sum row $J$ remain separate consumers of the common displayed support $E,R,D,H,C,P,N$. The shared support does not identify derivative and integral conclusions and does not supply a host fundamental theorem between them.
\end{theorem}
\begin{proof}
Project the carrier. The derivative data is stored in $L$, and the finite tagged-sum data is stored in $J$; these are distinct coordinates of the finite packet.

Both rows may use the same real seal, rational readback, dyadic tolerance, transport, replay, provenance, and local naming support. Sharing those support rows only records common BEDC infrastructure; it does not turn one source row into the other.

Since the carrier contains no coordinate for a host fundamental theorem, selected antiderivative, quotient function equality, or ambient real completeness, derivative and Riemann finite-sum consumers remain separated after projection.
\end{proof}

\begin{definition}[Calculus carrier]
\label{def:calculus-carrier}
A $\CalculusUp$ carrier is a finite $\BHist$ packet
$$
K=(R,L,C,D,I,Q,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RealUp$ scalar boundary used by all endpoint values, tolerances, finite sums, and error estimates.
\item $L$ is the $\LimitUp$ route for displayed convergence windows and downstream real seals.
\item $C$ is the $\ContinuousMapUp$ row naming the finite graph, pointwise continuity, and replay windows available to calculus consumers.
\item $D$ is the $\DerivativeUp$ local difference-quotient row, including the source point, target read, and remainder ledger.
\item $I$ is the $\IntegralUp$ finite-sum row, exposing the partition, tagged-read, and endpoint handoff data used by integration consumers.
\item $Q$ is the $\RegSeqRatUp$ readback row shared by the limit, derivative, and integral ledgers.
\item $H$ records componentwise $\hsame$ transport for real, limit, continuous-map, derivative, integral, regular-rational, replay, provenance, and local naming rows.
\item $T$ records displayed $\Cont$ replay from real reads to limits, from limits to continuous maps, from finite difference quotients to derivative reads, and from finite sums to integral handoffs.
\item $P$ is the $\Pkg$ provenance over $\CalculusUp$, $\RealUp$, $\LimitUp$, $\ContinuousMapUp$, $\DerivativeUp$, $\IntegralUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CalculusUp}$ row.
\end{enumerate}
The classifier reads only $R,L,C,D,I,Q,H,T,P,N$. It has no coordinate for a host function space, a selected global derivative operator, an unbounded integration functional, quotient equality of functions, an open-cover compactness principle, or ambient real completeness.
\end{definition}

\begin{theorem}[Calculus NameCert obligations]
\label{thm:calculus-namecert-obligations}
For every accepted $\CalculusUp$ carrier $K=(R,L,C,D,I,Q,H,T,P,N)$, the local $\NameCert_{\CalculusUp}$ surface consists exactly of carrier admission, real-boundary exposure, limit-window readback, continuous-map replay, local-linear approximation routing, finite difference quotient readback, integral finite-sum handoff, regular-rational readback, componentwise transport, displayed replay, package provenance, and local naming.
\end{theorem}
\leanchecked{BEDC.Derived.CalculusUp.CalculusTasteGate\_single\_carrier\_alignment}
\begin{proof}
Project the carrier of \autoref{def:calculus-carrier}. The rows $R,L,C,D,I,Q,H,T,P,N$ are the full finite inventory available to a calculus consumer.

The local-linear approximation route is carried by $D$ and is read only after the real boundary $R$, limit route $L$, continuous-map row $C$, and regular-rational readback $Q$ have supplied the displayed point, tolerance, and remainder data. Thus a derivative-facing consumer sees a finite difference quotient route rather than a hidden global derivative operator.

The integral handoff is carried by $I$. Its partition, finite-sum, and endpoint rows are exposed before any real-facing value is consumed, and the same $Q$ row supplies the rational readback needed to compare finite sums with limit and derivative windows.

Finally $H,T,P,N$ transport, replay, source, and name exactly the displayed rows. Since no coordinate contains host function-space equality, ambient completeness, a selected derivative operator, or an unbounded integral functional, every accepted read remains inside the finite calculus packet.
\end{proof}

\begin{theorem}[Calculus continuous-map graph replay obligation]
\label{thm:calculus-continuous-map-graph-replay-obligation}
For every accepted $\CalculusUp$ carrier $K=(R,L,C,D,I,Q,H,T,P,N)$, a continuous-map read is public only after the consumer has displayed the $\RealUp$ boundary $R$, the $\LimitUp$ window route $L$, the $\ContinuousMapUp$ graph row $C$, the $\RegSeqRatUp$ readback row $Q$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $T$, provenance $P$, and the local $\NameCert_{\CalculusUp}$ row $N$. A derivative or integral consumer using that continuous-map read must therefore factor through $R,L,C,Q,H,T,P,N$ before reading $D$ or $I$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:calculus-carrier}. The only row that names a continuous-map graph is $C$, and its endpoint values are typed by the real boundary $R$ and compared through the regular-rational readback $Q$.

The limit route $L$ supplies the finite convergence windows through which the graph row is replayed. The rows $H,T,P,N$ transport, replay, source, and name the same displayed graph data.

Now apply \autoref{thm:calculus-namecert-obligations}. Since the derivative row $D$ and integral row $I$ are later consumers of the continuous-map graph, neither can use $C$ until the route through $R,L,C,Q,H,T,P,N$ is visible.
\end{proof}

\begin{theorem}[Calculus derivative-integral separation obligation]
\label{thm:calculus-derivative-integral-separation-obligation}
For every accepted $\CalculusUp$ carrier $K=(R,L,C,D,I,Q,H,T,P,N)$, the derivative row $D$ and the integral row $I$ are separate finite rows. The row $D$ may expose local difference-quotient and remainder data only through $R,L,C,Q,H,T,P,N$, while the row $I$ may expose finite-sum and endpoint handoff data only through those same support rows. Neither row supplies the other's conclusion, and the carrier contains no host fundamental theorem connecting them.
\end{theorem}
\begin{proof}
Project \autoref{def:calculus-carrier}. The derivative data is stored in $D$, and the integral data is stored in $I$; they are distinct coordinates of the finite packet.

Use \autoref{thm:calculus-derivative-continuation-obligation} for the derivative-facing route and \autoref{thm:calculus-integral-finite-sum-obligation} for the integral-facing route. Each theorem reads its own calculus row together with the real, limit, continuous-map, regular-rational, transport, replay, provenance, and naming support.

\autoref{thm:calculus-namecert-obligations} lists the full local surface and includes no coordinate for a host fundamental theorem. Hence a consumer cannot derive an integral conclusion from $D$, cannot derive a derivative conclusion from $I$, and cannot import a global theorem to identify the two reads.
\end{proof}

\begin{theorem}[Calculus RegSeqRat readback root obligation]
\label{thm:calculus-regseqrat-readback-root-obligation}
Every real-facing value exported by an accepted $\CalculusUp$ carrier $K=(R,L,C,D,I,Q,H,T,P,N)$ must display the $\RegSeqRatUp$ readback row $Q$ together with the $\RealUp$ boundary $R$ and the dyadic tolerance route inherited by the regular-rational readback. This readback is required before any limit, derivative, or integral read is exported from $L$, $D$, or $I$.
\end{theorem}
\begin{proof}
Begin with the carrier of \autoref{def:calculus-carrier}. The terminal real boundary is $R$, and the common rational readback row is $Q$; no other coordinate converts limit, derivative, or integral data into a real-facing value.

The earlier real-seal theorem \autoref{thm:calculus-limit-real-seal-obligation} already forces Real-facing limit reads through regular-rational and dyadic tolerance data. The derivative and integral obligations similarly keep their finite quotient and finite-sum reads attached to the displayed real and rational support rows.

Finally use \autoref{thm:calculus-namecert-obligations}. The local NameCert surface lists regular-rational readback as a root obligation, so every export from $L,D,I$ must first pass through $Q$ with $R$ and the underlying dyadic tolerance route visible.
\end{proof}

\begin{theorem}[Calculus root regular readback obligation]
\label{thm:calculus-root-obligation-regular-readback}
Every derivative, integral, or limit export from an accepted $\CalculusUp$ carrier $K=(R,L,C,D,I,Q,H,T,P,N)$ must expose the $\RegSeqRatUp$ readback row $Q$ and the inherited $\DyadicRatCoreUp$ tolerance route before the terminal $\RealUp$ seal $R$ is consumed. The export is a displayed calculus packet read and not a host analytic estimate, quotient real equality, or ambient Real completeness principle.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:calculus-carrier}. The limit row is $L$, the derivative row is $D$, the integral row is $I$, the common regular-rational readback row is $Q$, and the terminal Real boundary is $R$.

Apply \autoref{thm:calculus-regseqrat-readback-root-obligation}. It already requires every real-facing value exported from $L,D,I$ to display $Q$ together with $R$ and the dyadic tolerance route inherited by the regular-rational readback.

Apply \autoref{thm:calculus-continuous-limit-carrier-budget}. It forces a continuous-map graph read and a limit-window read to remain visible before any Real seal is exported, with $Q$ supplying the readback used by $R$.

Thus no derivative, integral, or limit consumer can replace the displayed readback by a host analytic estimate, quotient real equality, or ambient Real completeness. The root obligation is exactly the route through $Q$ and its dyadic tolerance data before $R$ is consumed.
\end{proof}

\begin{theorem}[Calculus continuous-limit carrier budget]
\label{thm:calculus-continuous-limit-carrier-budget}
For every accepted $\CalculusUp$ carrier $K=(R,L,C,D,I,Q,H,T,P,N)$, a continuous-map graph read and a limit-window read must both be displayed before any $\RealUp$ seal is exported. The graph row $C$ supplies the named finite graph and pointwise replay windows, the limit row $L$ supplies the convergence windows, and the regular-rational row $Q$ supplies the $\RegSeqRatUp$ readback used by the terminal real boundary $R$. Thus a real-facing calculus export cannot bypass $C,L,Q$ or treat Real completeness as an ambient carrier.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:calculus-carrier}. The only continuous-map graph coordinate is $C$, the only limit-window coordinate is $L$, and the only regular-rational readback coordinate is $Q$.

Use \autoref{thm:calculus-continuous-map-graph-replay-obligation}. It requires $R,L,C,Q,H,T,P,N$ before a derivative or integral consumer can read the continuous-map graph.

Use \autoref{thm:calculus-regseqrat-readback-root-obligation}. Every real-facing export from $L,D,I$ must expose $Q$ together with $R$ and the dyadic tolerance route. Hence the Real seal is downstream of the displayed graph, limit, and readback rows rather than an ambient completeness principle.
\end{proof}

\begin{theorem}[Calculus derivative-integral source separation]
\label{thm:calculus-derivative-integral-source-separation}
For every accepted $\CalculusUp$ carrier $K=(R,L,C,D,I,Q,H,T,P,N)$, the derivative row $D$ and integral row $I$ remain separated even when they share the source rows $R,L,C,Q,H,T,P,N$. A derivative consumer must read local difference-quotient and remainder data from $D$, while an integral consumer must read partition, tagged-sum, and endpoint handoff data from $I$; no shared support row identifies the two sources or exports a host fundamental theorem.
\end{theorem}
\begin{proof}
Project \autoref{def:calculus-carrier}. The derivative source is the coordinate $D$, and the integral source is the coordinate $I$.

Apply \autoref{thm:calculus-derivative-integral-separation-obligation}. That theorem already records that the two rows use the same finite support but do not supply each other's conclusions.

The carrier list contains no coordinate for quotient function equality, a selected antiderivative, or a host fundamental theorem. Therefore shared support through $R,L,C,Q,H,T,P,N$ preserves source separation rather than collapsing $D$ and $I$.
\end{proof}

\begin{theorem}[Calculus real RegSeqRat error handoff]
\label{thm:calculus-real-regseqrat-error-handoff}
Every real-facing error estimate exported by an accepted $\CalculusUp$ carrier $K=(R,L,C,D,I,Q,H,T,P,N)$ must first display the $\RegSeqRatUp$ row $Q$ and the dyadic rational tolerance route inherited by that readback. Limit, derivative, and integral consumers may then hand the estimate to the $\RealUp$ boundary $R$, but only after the regular-rational and dyadic data have been read inside the finite packet.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:calculus-regseqrat-readback-root-obligation}. It makes $Q$ mandatory before any real-facing value is exported from $L,D,I$.

The derivative and integral routes use the same readback discipline. Their finite quotient, remainder, partition, tagged-sum, and endpoint rows are compared through regular-rational data before a Real-facing estimate is named.

Finally apply \autoref{thm:calculus-namecert-obligations}. The local NameCert surface contains $R,L,C,D,I,Q,H,T,P,N$ and no ambient Real-completeness or host error functional. Thus the handoff to $R$ is downstream of $\RegSeqRatUp$ and dyadic readback data in $Q$.
\end{proof}

\begin{theorem}[Calculus derivative-limit root obligation]
\label{thm:calculus-derivative-limit-obligation}
For every accepted $\CalculusUp$ carrier $K=(R,L,C,D,I,Q,H,T,P,N)$, a derivative-limit read is public only after the $\RealUp$ seal row $R$, the $\LimitUp$ route $L$, the $\ContinuousMapUp$ replay row $C$, the $\DerivativeUp$ quotient row $D$, the $\RegSeqRatUp$ readback row $Q$, the dyadic tolerance ledger inherited by $Q$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $T$, $\Pkg$ provenance $P$, and the local $\NameCert_{\CalculusUp}$ row $N$ have all been displayed. The read exports a finite BEDC derivative-limit route and not a host function space, quotient equality of functions, selected derivative operator, selected antiderivative, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:calculus-carrier}. The derivative source is the row $D$, while the limit-facing route is $L$ and the graph replay available to a derivative consumer is $C$.

Use \autoref{thm:calculus-continuous-map-graph-replay-obligation}. It places every derivative consumer behind $R,L,C,Q,H,T,P,N$, so the graph and pointwise replay are visible before the derivative row is consumed.

Use \autoref{thm:calculus-regseqrat-readback-root-obligation}. The regular-rational readback $Q$ carries the dyadic tolerance route required before any Real-facing value reaches $R$.

The remaining rows $H,T,P,N$ transport, replay, source, and name the same finite packet. Since the carrier has no coordinate for a host function space, quotient function equality, selected derivative operator, selected antiderivative, or ambient completeness, the derivative-limit obligation is exactly this displayed route.
\end{proof}

\begin{theorem}[Calculus integral-sum root obligation]
\label{thm:calculus-integral-sum-obligation}
For every accepted $\CalculusUp$ carrier $K=(R,L,C,D,I,Q,H,T,P,N)$, an integral read may consume the $\IntegralUp$ finite-sum row $I$ only after the $\RealUp$ boundary $R$, $\LimitUp$ route $L$, $\ContinuousMapUp$ replay row $C$, $\RegSeqRatUp$ readback row $Q$, dyadic tolerance ledger, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $T$, $\Pkg$ provenance $P$, and local $\NameCert_{\CalculusUp}$ row $N$ are visible. The finite-sum row records partition, tag, sum, and endpoint handoff data; it does not export a host integration functional, quotient equality of functions, a selected antiderivative, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the carrier $K$ and isolate the integral source row $I$. The statement of \autoref{thm:calculus-namecert-obligations} lists integral finite-sum handoff as one local NameCert obligation, so $I$ is a displayed row rather than a host integral object.

Apply \autoref{thm:calculus-integral-finite-sum-obligation}. It keeps the partition, tag, finite-sum, and endpoint data attached to the real boundary, regular-rational readback, dyadic tolerance ledger, transport, replay, provenance, and local naming rows.

Apply \autoref{thm:calculus-continuous-map-graph-replay-obligation}. If the integral consumer reads graph data, it reaches that graph through $R,L,C,Q,H,T,P,N$ before the finite-sum handoff is public.

The derivative row $D$ remains separate by \autoref{thm:calculus-derivative-integral-source-separation}. Thus the integral-sum obligation is the finite route through $I$ and the displayed support rows, with no selected antiderivative, host function space, quotient equality, or ambient Real-completeness coordinate.
\end{proof}

\begin{theorem}[Calculus continuity real-readback root obligation]
\label{thm:calculus-continuity-real-readback-obligation}
For every accepted $\CalculusUp$ carrier $K=(R,L,C,D,I,Q,H,T,P,N)$, a continuity-shaped Real read must display the $\ContinuousMapUp$ replay row $C$, the $\LimitUp$ route $L$, the $\RegSeqRatUp$ readback row $Q$, the dyadic tolerance ledger inherited by $Q$, and the terminal $\RealUp$ boundary $R$ before derivative or integral data is consumed. The structural rows $H,T,P,N$ preserve this same route through $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, and no host continuity theorem, function-space equality, quotient real equality, selected antiderivative, or ambient $\RealUp$ completeness is exported.
\end{theorem}
\begin{proof}
Start with \autoref{thm:calculus-continuous-limit-carrier-budget}. It identifies $C,L,Q$ as mandatory displayed rows before any $\RealUp$ seal is exported from the calculus carrier.

Use \autoref{thm:calculus-real-regseqrat-error-handoff}. The real-facing estimate must pass through $Q$ and the dyadic rational tolerance route before it reaches $R$.

Derivative and integral reads remain downstream consumers. The row $D$ supplies only finite difference-quotient data, and the row $I$ supplies only finite-sum data, so neither can replace the continuity-to-limit-to-readback route.

Finally read $H,T,P,N$. These rows give componentwise transport, displayed continuation replay, package provenance, and local naming for the same packet. The carrier therefore exports a finite continuity real-readback route and no host continuity theorem, function-space equality, quotient real equality, selected antiderivative, or ambient completeness principle.
\end{proof}

\begin{definition}[Calculus root derivative-integral carrier]
\label{def:calculus-root-derivative-integral-carrier}
A $\CalculusUp$ root derivative-integral carrier is the carrier
$$
K=(R,L,C,D,I,Q,H,T,P,N)
$$
of \autoref{def:calculus-carrier} read with the following root dependency rows singled out:
\begin{enumerate}
\item $R$ is the $\RealUp$ boundary for endpoint, value, error, derivative, integral, and limit readback.
\item $Q$ is the $\RegSeqRatUp$ readback row carrying the rational comparison and error schedule used before $R$ is consumed.
\item the dyadic tolerance data inherited by $Q$ is the $\DyadicRatCoreUp$ dependency row for denominator, radius, and comparison reads;
\item $C$ is the $\ContinuousMapUp$ graph and pointwise replay row used before derivative, integral, or limit consumers read function data;
\item $D,I,L,H,T,P,N$ are respectively the derivative source row, integral finite-sum row, limit route, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\CalculusUp}$ row.
\end{enumerate}
The root classifier compares accepted packets only through these displayed $\BHist$, $\hsame$, $\Cont$, $\NameCert$, $\RealUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\ContinuousMapUp$ rows. It has no coordinate for a host function space, quotient equality of functions, a selected derivative operator, a selected antiderivative, or ambient real completeness.
\end{definition}

\begin{theorem}[Calculus root derivative real handoff]
\label{thm:calculus-root-derivative-real-handoff}
For every accepted $\CalculusUp$ root derivative-integral carrier $K=(R,L,C,D,I,Q,H,T,P,N)$, a derivative-facing real read must expose the $\ContinuousMapUp$ row $C$, the $\DerivativeUp$ row $D$, the $\RegSeqRatUp$ row $Q$, the $\DyadicRatCoreUp$ tolerance data inherited by $Q$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $T$, $\Pkg$ provenance $P$, and the local $\NameCert_{\CalculusUp}$ row $N$ before the $\RealUp$ boundary $R$ is consumed.
\end{theorem}
\begin{proof}
Project the root carrier of \autoref{def:calculus-root-derivative-integral-carrier}. The derivative source is $D$, but $D$ is public only as part of the same $\BHist$ packet as the graph row $C$, readback row $Q$, transport row $H$, replay row $T$, provenance row $P$, and local naming row $N$.

Use \autoref{thm:calculus-derivative-limit-obligation}. It already places every derivative-limit read behind the $\ContinuousMapUp$ graph row, the regular-rational readback, the dyadic tolerance route, and the structural rows.

The terminal real boundary is $R$. Since no coordinate of the root carrier contains quotient function equality, a selected derivative operator, or ambient real completeness, the derivative-facing handoff reaches $R$ only through the displayed finite route.
\end{proof}

\begin{theorem}[Calculus root integral RegSeqRat handoff]
\label{thm:calculus-root-integral-regseqrat-handoff}
For every accepted $\CalculusUp$ root derivative-integral carrier $K=(R,L,C,D,I,Q,H,T,P,N)$, an integral-facing read through the $\IntegralUp$ row $I$ must first display the $\ContinuousMapUp$ row $C$, the finite-sum and endpoint handoff data in $I$, the $\RegSeqRatUp$ readback row $Q$, the $\DyadicRatCoreUp$ tolerance route inherited by $Q$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $T$, $\Pkg$ provenance $P$, and the local $\NameCert_{\CalculusUp}$ row $N$. Only after this readback may the consumer reach the $\RealUp$ boundary $R$.
\end{theorem}
\begin{proof}
Read the integral source row $I$ from \autoref{def:calculus-root-derivative-integral-carrier}. Its partition, tag, finite-sum, and endpoint data are displayed inside the same finite $\BHist$ packet as $C,Q,H,T,P,N$.

Apply \autoref{thm:calculus-integral-sum-obligation}. That obligation forces the integral row through the real boundary, limit route, continuous-map replay, regular-rational readback, dyadic tolerance ledger, transport, replay, provenance, and local naming rows.

The $\RegSeqRatUp$ handoff is therefore not optional. The consumer cannot replace $Q$ by a host integration functional, selected antiderivative, quotient function equality, or ambient completeness theorem before reading $R$.
\end{proof}

\begin{theorem}[Calculus root limit nonescape]
\label{thm:calculus-root-limit-nonescape}
For every accepted $\CalculusUp$ root derivative-integral carrier $K=(R,L,C,D,I,Q,H,T,P,N)$, each limit-shaped read exported by $L$ must remain inside the route through the $\ContinuousMapUp$ graph row $C$, the $\RegSeqRatUp$ readback row $Q$, the $\DyadicRatCoreUp$ tolerance route inherited by $Q$, the terminal $\RealUp$ boundary $R$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $T$, $\Pkg$ provenance $P$, and local $\NameCert_{\CalculusUp}$ row $N$. It cannot escape to a host limit operator, quotient equality of functions, selected derivative operator, selected antiderivative, or ambient real completeness.
\end{theorem}
\begin{proof}
Project the limit row $L$ from the root carrier. By construction, $L$ is not a standalone host limit object; it is stored together with $C,Q,R,H,T,P,N$ in the displayed calculus packet.

Use \autoref{thm:calculus-continuous-limit-carrier-budget}. A continuous-map graph read and a limit-window read must both be displayed before any real-facing seal is exported, and the regular-rational row $Q$ supplies the readback used by $R$.

The structural rows preserve the same route through $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. Since the carrier has no coordinate for the excluded host principles, every limit-shaped read remains confined to the listed BEDC dependency rows.
\end{proof}

\closureat{CalculusUp}{seedStr}

\begin{closurestatus}{\CalculusUp}
  \constructivestory{A $\CalculusUp$ packet is generated from Real, Limit, ContinuousMap, Derivative, Integral, RegSeqRat, componentwise hsame, displayed Cont, Pkg, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:calculus-carrier} and \autoref{thm:calculus-namecert-obligations} seed the finite calculus root over real, limit, continuous-map, derivative, integral, regular-rational, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, a host function space, global differentiability, an ambient integration operator, quotient function equality, or real completeness beyond the displayed Real row.}
  \upgradepath{Obligation closure requires checked carrier admission, limit readback, continuous-map replay, derivative quotient routing, integral finite-sum handoff, regular-rational readback, transport, replay, provenance, and local naming for BEDC.Derived.CalculusUp.}
\end{closurestatus}
