\chapter{A Concrete Naming Certificate for $\CauchyRealApartnessUp$}
\label{ch:concrete-instances-cauchy-real-apartness-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CauchyRealApartnessUp$ packet records the positive finite boundary by which two displayed real names are certified apart before any order trichotomy, quotient stream equality, selected limit, or ambient completeness principle can be read. It sits over the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite window discipline of \autoref{ch:concrete-instances-streamname-namecert}, and the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The packet names a constructive positive-apartness surface for later order and metric consumers; it does not decide equality or total order on $\RealUp$.

\begin{definition}[Cauchy real apartness carrier]
\label{def:cauchy-real-apartness-carrier}
A $\CauchyRealApartnessUp$ carrier is a finite $\BHist$ packet
$$
A=(E_0,E_1,R_0,R_1,W_0,W_1,D,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E_0$ and $E_1$ are the two $\RealUp$ seal rows. They are terminal classifier rows only; they expose no host real equality, quotient representative, trichotomy branch, or completeness theorem.
\item $R_0$ and $R_1$ are the $\RegSeqRatUp$ witness rows. Each reads the corresponding real name through displayed regular rational data before the apartness tolerance is interpreted.
\item $W_0$ and $W_1$ are finite $\StreamNameUp$ window rows for the same precision request. They record the inspected windows and no infinite stream equality.
\item $D$ is the positive $\DyadicRatCoreUp$ tolerance row. It stores a displayed positive dyadic gap, the comparison ledger, and the slack budget used to separate the two inspected windows.
\item $T$ is the apartness transport row. It records componentwise $\hsame$ transport for the two real seals, the regular readbacks, the finite windows, the positive tolerance ledger, replay, provenance, and local naming.
\item $H$ is the displayed $\Cont$ continuation row. It replays a consumer request through $E_0,E_1,R_0,R_1,W_0,W_1,D,T,P,N$ without adding a selected limit or an equality decision.
\item $P$ is the $\Pkg$ provenance over $\CauchyRealApartnessUp$, $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyRealApartnessUp}$ row.
\end{enumerate}
The carrier predicate asserts exactly that these rows form one finite positive-apartness packet. It has no coordinate for trichotomy, quotient stream equality, selected limits, countable choice, metric-space completeness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Cauchy real apartness NameCert obligations]
\label{thm:cauchy-real-apartness-namecert-obligations}
For every accepted $\CauchyRealApartnessUp$ carrier
$$
A=(E_0,E_1,R_0,R_1,W_0,W_1,D,T,H,C,P,N),
$$
the local $\NameCert_{\CauchyRealApartnessUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the two Real seals, two regular rational readbacks, two finite StreamName windows, positive dyadic tolerance ledger, transport row, continuation row, provenance row, and local naming row;
\item positive tolerance stability, requiring the positive dyadic gap in $D$ to remain positive under the displayed comparison ledger and the componentwise transports in $T$;
\item RegSeqRat handoff, requiring every apartness read to pass from the two finite windows $W_0,W_1$ through $R_0,R_1$ before the Real seal rows $E_0,E_1$ are consumed;
\item Real seal nonescape, requiring $E_0$ and $E_1$ to be terminal seals rather than carriers of equality, trichotomy, selected representatives, selected limits, or completeness;
\item ledger rows, requiring $T,H,P,N$ to transport, replay, source, and name only the displayed rows of the packet.
\end{enumerate}
No local obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:cauchy-real-apartness-carrier}. The accepted packet displays exactly the two terminal real seals, two regular rational readbacks, two finite window rows, one positive dyadic tolerance ledger, and the structural rows.

Positive tolerance stability is finite. The row $D$ records the displayed positive gap and its slack comparisons, while $T$ transports only the named rows componentwise, so no transport step can replace the gap with an undecided order comparison.

The handoff obligation follows the row order. The finite windows $W_0,W_1$ are read first, the regular rational witnesses $R_0,R_1$ interpret those windows, and only then may the real seals $E_0,E_1$ be consumed by an apartness reader.

Finally, the seal and ledger obligations are exhausted by $E_0,E_1,T,H,P,N$. Those rows preserve and name the displayed route; they do not add trichotomy, quotient stream equality, selected limits, countable choice, metric completeness, ambient Real completeness, Lean verification, or bridge data.
\end{proof}

\begin{theorem}[Cauchy real apartness RegSeqRat handoff]
\label{thm:cauchy-real-apartness-regseqrat-handoff}
Every accepted $\CauchyRealApartnessUp$ read reaching a $\RealUp$ apartness consumer factors through the finite route
$$
W_0,W_1\longmapsto R_0,R_1\longmapsto D\longmapsto E_0,E_1\longmapsto (T,H,P,N).
$$
Thus the apartness witness reaches $\RealUp$ only through $\StreamNameUp$ windows, $\RegSeqRatUp$ readback rows, and the positive $\DyadicRatCoreUp$ tolerance ledger.
\end{theorem}
\begin{proof}
Start with a Real-facing apartness read of an accepted carrier. By \autoref{def:cauchy-real-apartness-carrier}, the only window data available to that read are $W_0$ and $W_1$.

The regular rational stage is forced next. The rows $R_0$ and $R_1$ read the two finite StreamName windows as regular rational witnesses, so the consumer receives no quotient stream equality or infinite stream comparison.

The positive gap is then read from $D$. This dyadic ledger supplies the finite positive tolerance and the displayed slack comparisons used by the apartness request.

Only after these rows are displayed can the read consume $E_0$ and $E_1$ as terminal Real seals. The structural rows $T,H,P,N$ preserve the same route by transport, continuation, provenance, and local naming.
\end{proof}

\begin{theorem}[Cauchy real apartness nonescape]
\label{thm:cauchy-real-apartness-nonescape}
An accepted $\CauchyRealApartnessUp$ carrier exports no row for order trichotomy, quotient stream equality, selected limits, countable choice, metric-space completeness, or ambient $\RealUp$ completeness. Any consumer asking for one of those principles must leave the local packet and cannot obtain it from the carrier rows
$$
E_0,E_1,R_0,R_1,W_0,W_1,D,T,H,C,P,N .
$$
\end{theorem}
\begin{proof}
Inspect the carrier rows. The StreamName rows are finite windows, the RegSeqRat rows are displayed readbacks, and the DyadicRatCore row is a positive tolerance ledger; none of these rows carries quotient stream equality or a selected limit.

The two Real rows are seals only. They are consumed after the regular rational and dyadic rows have been displayed, and their role is terminal naming rather than equality decision, order trichotomy, metric completeness, or ambient completeness.

The remaining rows are structural. Transport, continuation, provenance, and local naming preserve the displayed route, so countable choice, selected representatives, quotient equality, trichotomy, metric-space completeness, and $\RealUp$ completeness remain outside the local certificate.
\end{proof}

\closureat{CauchyRealApartnessUp}{seedStr}

\begin{closurestatus}{\CauchyRealApartnessUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-real-apartness-carrier}, \autoref{thm:cauchy-real-apartness-namecert-obligations}, \autoref{thm:cauchy-real-apartness-regseqrat-handoff}, and \autoref{thm:cauchy-real-apartness-nonescape} seed the finite positive-apartness route over two Real seals, regular rational readbacks, StreamName windows, and a positive DyadicRatCore tolerance ledger.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{The chapter does not claim $\RealUp$ completeness, full order trichotomy, metric-space completeness, or a standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, positive tolerance stability, RegSeqRat handoff, Real seal nonescape, ledger exhaustion, and TasteGate field faithfulness for BEDC.Derived.CauchyRealApartnessUp.}
  \constructivestory{A $\CauchyRealApartnessUp$ packet is generated from two RealUp seals, two RegSeqRat readbacks, two StreamName windows, a positive DyadicRatCore tolerance row, componentwise hsame transport, displayed Cont continuation, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
