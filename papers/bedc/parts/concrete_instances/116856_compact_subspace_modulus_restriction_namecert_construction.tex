\chapter{A Concrete Naming Certificate for $\CompactSubspaceModulusRestrictionUp$}
\label{ch:concrete-instances-compact-subspace-modulus-restriction-namecert}
\origin{human}

Following Heine--Cantor and Bishop compact metric practice, the $\CompactSubspaceModulusRestrictionUp$ packet records the finite route by which a uniform modulus over a compact metric source restricts to a displayed compact subspace. It concretizes the compact-uniform-continuity route of \autoref{ch:visions-compact-uniform-continuity} and sits over the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map graph row of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus handoff of \autoref{ch:concrete-instances-uniformmodulus-namecert}, and the subspace admission boundary of \autoref{ch:concrete-instances-metric-subspace-namecert}. The packet exposes inherited modulus data only after compact-source, subspace, graph, and tolerance rows have been displayed; it does not assert host compactness, arbitrary subset continuity, selected finite covers, quotient subspace equality, or ambient $\RealUp$ completeness.

\begin{definition}[Compact subspace modulus-restriction carrier]
\label{def:compact-subspace-modulus-restriction-carrier}
A $\CompactSubspaceModulusRestrictionUp$ carrier is a finite $\BHist$ packet
$$
R=(K,S,F,M,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, carrying the compact metric finite-net and located-distance data available before restriction.
\item $S$ is the $\MetricSubspaceUp$ admission row, naming the displayed subspace predicate, inherited metric row, and inclusion route consumed by the packet.
\item $F$ is the $\ContinuousMapUp$ graph row, exposing the pointwise continuity data of the map before the restricted read reaches a modulus.
\item $M$ is the subspace metric-comparison row, recording that source distances used by the restricted consumer are inherited from $K$ through $S$.
\item $U$ is the $\UniformModulusUp$ row whose tolerance-to-precision ledger is read only after $K,S,F,M$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for compact source, subspace admission, continuous graph, metric comparison, uniform modulus, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the same compact-subspace, graph, metric, and modulus route.
\item $P$ is the $\Pkg$ provenance over $\CompactSubspaceModulusRestrictionUp$, $\CompactMetricUp$, $\MetricSubspaceUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactSubspaceModulusRestrictionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,S,F,M,U,H,C,P,N$. It has no coordinate for an external compactness theorem, unrestricted subspace equality, selected cover refinement, host function extensionality, arbitrary Real completeness, Lean verification, or bridge checking.
\end{definition}
\leanchecked{BEDC.Derived.CompactSubspaceModulusRestrictionUp.CompactSubspaceModulusRestrictionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Compact subspace modulus-restriction NameCert obligations]
\label{thm:compact-subspace-modulus-restriction-namecert-obligations}
For every accepted $\CompactSubspaceModulusRestrictionUp$ carrier
$$
R=(K,S,F,M,U,H,C,P,N),
$$
the local $\NameCert_{\CompactSubspaceModulusRestrictionUp}$ surface consists exactly of carrier admission for the compact metric source, metric subspace admission, continuous-map graph row, inherited metric-comparison row, uniform-modulus handoff, componentwise transport, displayed replay, provenance, and local naming row; restriction exactness requiring every subspace read to pass through $S$ before $M$; modulus exactness requiring $U$ to consume only the displayed graph and inherited metric rows; and nonescape from the named rows.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:compact-subspace-modulus-restriction-carrier}. The compact source, subspace admission, graph, metric comparison, and modulus rows are the only analytic rows present.

The restricted consumer first reads the subspace admission row $S$ against the compact source row $K$. Thus each source-distance comparison visible in the subspace route is inherited through $M$ rather than supplied by a separate host metric.

The continuous-map row $F$ then feeds the uniform-modulus row $U$. Since $U$ is downstream from $K,S,F,M$, the packet restricts an already displayed modulus surface and does not choose a new compact cover or use arbitrary subspace equality.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. The excluded compactness, equality, choice, and completeness endpoints have no carrier coordinate, so the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Compact subspace modulus-restriction uniform handoff]
\label{thm:compact-subspace-modulus-restriction-uniform-handoff}
Every $\UniformModulusUp$ consumer of an accepted $\CompactSubspaceModulusRestrictionUp$ carrier
$$
R=(K,S,F,M,U,H,C,P,N)
$$
must factor its restricted read through the finite route
$$
K \longmapsto S \longmapsto F,M \longmapsto U \longmapsto H,C,P,N .
$$
The consumer may export the compact metric source, displayed subspace admission, continuous-map graph row, inherited metric comparison, uniform-modulus ledger, transport, replay, provenance, and local naming. It may not export host compactness, unrestricted subspace equality, selected finite covers, host function extensionality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{theorem}
\begin{proof}
Start with a uniform-modulus consumer. The compact metric source $K$ and subspace admission row $S$ determine which source points and inherited distances may be inspected.

The graph and metric rows are then read together. The continuous-map row $F$ supplies the displayed pointwise graph data, while $M$ records that each restricted distance comparison is inherited from the parent compact metric source through $S$.

Only after those rows are visible may the consumer read $U$. The resulting modulus is therefore a restriction of the displayed compact-source modulus route, not a new external selector or cover-refinement theorem.

The structural rows $H,C,P,N$ preserve the same route. Since no other coordinates are present, the packet exports exactly the compact-subspace uniform handoff named in the statement.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactSubspaceModulusRestrictionUp$ uniform-modulus read factors through a CompactMetric source row, MetricSubspace admission row, ContinuousMap graph row, inherited metric-comparison row, UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row.}

\independenceWitness{compactmetric, continuousmap, uniformmodulus, metric-subspace: $\CompactSubspaceModulusRestrictionUp$ is not bijective to any listed sibling because it jointly carries compact-source data, subspace admission, graph data, inherited metric comparisons, a uniform-modulus handoff, transport, replay, provenance, and local naming while refusing host compactness, unrestricted subspace equality, selected cover refinement, and ambient Real completeness.}

\closureat{CompactSubspaceModulusRestrictionUp}{seedStr}

\begin{closurestatus}{\CompactSubspaceModulusRestrictionUp}
  \constructivestory{A $\CompactSubspaceModulusRestrictionUp$ packet is generated from a CompactMetric source row, MetricSubspace admission row, ContinuousMap graph row, inherited metric-comparison row, UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-subspace-modulus-restriction-carrier}, \autoref{thm:compact-subspace-modulus-restriction-namecert-obligations}, and \autoref{thm:compact-subspace-modulus-restriction-uniform-handoff} seed the compact-subspace uniform-modulus restriction surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host compactness, unrestricted subspace equality, selected finite covers, host function extensionality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, subspace admission exactness, inherited metric-comparison exactness, ContinuousMap graph exposure, UniformModulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.CompactSubspaceModulusRestrictionUp.}
\end{closurestatus}
