\chapter{A Concrete Naming Certificate for $\RealCompletionExactBoundaryUp$}
\label{ch:concrete-instances-real-completion-exact-boundary-namecert}
\origin{ai}

The $\RealCompletionExactBoundaryUp$ packet is the finite boundary surface for the Real completion route of \autoref{ch:visions-real-completeness}. It sits after the seal route of \autoref{ch:concrete-instances-cauchylimitseal-namecert}, the completion-seal classifier of \autoref{ch:concrete-instances-real-completion-seal-classifier-namecert}, the witness ledger of \autoref{ch:concrete-instances-cauchy-witness-ledger-namecert}, the synchronizer of \autoref{ch:concrete-instances-cauchy-seal-budget-synchronizer-namecert}, the finite stream interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic endpoint ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. Its role is to bind those existing rows into one exact finite NameCert packet, not to add quotient streams, selected host limits, countable choice, host real equality, or ambient completeness.

\falsifiablePrediction{If a Real completion boundary read bypasses either the displayed seal/classifier rows or the displayed witness/budget rows, the packet is refuted.}

\independenceWitness{cauchylimitseal, realcompletionsealclassifier, cauchywitnessledger, cauchysealbudgetsynchronizer, real: no listed sibling contains the exact joint seal, classifier, witness-ledger, synchronizer, StreamName, RegSeqRat, DyadicRatCore, and RealUp packet surface.}

\begin{definition}[Real completion exact-boundary carrier]
\label{def:real-completion-exact-boundary-carrier}
A $\RealCompletionExactBoundaryUp$ carrier is a finite $\BHist$ packet
$$
B=(L,K,J,S,W,R,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the $\CauchyLimitSealUp$ seal row consumed by the completion route;
\item $K$ is the $\RealCompletionSealClassifierUp$ classifier row for the same displayed seal surface;
\item $J$ is the $\CauchyWitnessLedgerUp$ row naming the finite witness evidence for the route;
\item $S$ is the $\CauchySealBudgetSynchronizerUp$ row synchronizing the seal budget with the witness ledger;
\item $W$ is the $\StreamNameUp$ finite-window row read by the same route;
\item $R$ is the $\RegSeqRatUp$ regular-rational readback row attached to $W$;
\item $D$ is the $\DyadicRatCoreUp$ dyadic tolerance and endpoint row used by the readback;
\item $E$ is the terminal $\RealUp$ seal row reached only after $L,K,J,S,W,R,D$ have been displayed;
\item $H$ records componentwise $\hsame$ transports for all displayed rows;
\item $C$ records the displayed $\Cont$ replay from seal and witness admission to the terminal Real seal;
\item $P$ is the $\Pkg$ provenance over the sibling certificates, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\RealCompletionExactBoundaryUp}$ row.
\end{enumerate}
The classifier compares carriers only through these rows. It has no coordinate for quotient stream equality, host real equality, a hidden selector, a second seal route, a detached witness ledger, or ambient completeness.
\end{definition}
\leanchecked{BEDC.Derived.RealCompletionExactBoundaryUp.RealCompletionExactBoundaryTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Real completion exact-boundary NameCert obligations]
\label{thm:real-completion-exact-boundary-namecert-obligations}
For every accepted $\RealCompletionExactBoundaryUp$ carrier
$$
B=(L,K,J,S,W,R,D,E,H,C,P,N),
$$
the local $\NameCert_{\RealCompletionExactBoundaryUp}$ surface consists exactly of carrier exposure for $B$, completion-seal exposure through $L$, classifier exposure through $K$, witness-ledger exposure through $J$, synchronized budget exposure through $S$, finite-window exposure through $W$, regular-rational readback through $R$, dyadic tolerance readback through $D$, terminal $\RealUp$ seal exposure through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-completion-exact-boundary-carrier}. Its displayed packet lists the seal row, classifier row, witness ledger, budget synchronizer, finite window, regular-rational readback, dyadic ledger, terminal Real seal, and structural rows.

Read the sibling dependencies in order. The completion seal $L$ and classifier $K$ fix the seal-facing side, while the witness ledger $J$ and synchronizer $S$ fix the evidence and budget side. The rows $W,R,D,E$ are then the StreamName, RegSeqRat, DyadicRatCore, and RealUp projections of that same route.

The structural rows $H,C,P,N$ only transport, replay, record provenance for, and name the displayed packet. Since no other coordinate is present in the carrier, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Real completion exact-boundary non-escape]
\label{thm:real-completion-exact-boundary-nonescape}
Every accepted $\RealCompletionExactBoundaryUp$ consumer factors through the finite route
$$
L,K,J,S,W,R,D,E,H,C,P,N.
$$
The consumer may inspect the completion seal, seal classifier, witness ledger, synchronized budget, StreamName window, RegSeqRat readback, DyadicRatCore tolerance ledger, RealUp seal, componentwise transports, continuations, provenance, and local NameCert row. It receives no quotient stream equality, host real equality, hidden selector, second seal route, detached witness ledger, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Start from the accepted packet $B=(L,K,J,S,W,R,D,E,H,C,P,N)$. The seal/classifier side and the witness/budget side are both displayed before the StreamName, RegSeqRat, DyadicRatCore, and RealUp rows can be consumed as boundary projections.

Apply \autoref{thm:real-completion-exact-boundary-namecert-obligations}. The local certificate exports exactly the rows $L,K,J,S,W,R,D,E,H,C,P,N$ and treats $H,C,P,N$ as structural support for that same finite packet.

Finally inspect the excluded objects. Quotient stream equality, host real equality, hidden selectors, second seal routes, detached witness ledgers, and ambient completeness are absent from the carrier and classifier. A consumer that reads any of them has left the accepted exact-boundary packet.
\end{proof}

\begin{theorem}[Real completion StreamName--RegSeq route]
\label{thm:real-completion-exact-boundary-streamname-regseq-route}
Every accepted $\RealCompletionExactBoundaryUp$ packet
$$
B=(L,K,J,S,W,R,D,E,H,C,P,N)
$$
routes the completion read through the finite chain $W,R,D,E,H,C,P,N$ after the seal, classifier, witness, and synchronizer rows $L,K,J,S$ have been displayed. In that route, $W$ supplies the $\StreamNameUp$ finite-window row, $R$ supplies the $\RegSeqRatUp$ readback row for the same window, $D$ supplies the $\DyadicRatCoreUp$ tolerance and endpoint row consumed by that readback, and $E$ supplies the terminal $\RealUp$ seal reached by the displayed completion packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-completion-exact-boundary-carrier}. The coordinates $W,R,D,E$ occur as named rows of the same accepted packet as $L,K,J,S,H,C,P,N$.

Apply \autoref{thm:real-completion-exact-boundary-namecert-obligations}. The local NameCert obligations expose the StreamName finite window, RegSeqRat readback, DyadicRatCore tolerance ledger, and RealUp seal as part of one certificate surface, with $H,C,P,N$ transporting, replaying, packaging, and naming that surface.

Use \autoref{thm:real-completion-exact-boundary-nonescape}. Any completion consumer that attempts to read $W,R,D$, or $E$ outside the displayed route would require an absent coordinate, so the completion read is forced through the finite packet $W,R,D,E,H,C,P,N$ under the already displayed rows $L,K,J,S$.
\end{proof}

\begin{theorem}[Real completion terminal seal non-escape]
\label{thm:real-completion-exact-boundary-real-seal-nonescape}
No terminal $\RealUp$ seal read accepted by a $\RealCompletionExactBoundaryUp$ packet can bypass the displayed route
$$
L,K,J,S,W,R,D,E,H,C,P,N
$$
by using quotient stream equality, host real equality, a hidden selector, a second seal route, a detached witness ledger, or ambient completeness. The only accepted terminal seal read is the row $E$ reached through the carrier, NameCert obligations, and non-escape surface of the same finite packet.
\end{theorem}
\begin{proof}
Begin with the carrier. The row $E$ is displayed as the terminal $\RealUp$ seal only after the seal row $L$, classifier row $K$, witness ledger $J$, synchronizer row $S$, StreamName row $W$, RegSeqRat row $R$, and DyadicRatCore row $D$ are present.

Apply \autoref{thm:real-completion-exact-boundary-namecert-obligations}. The certificate exports no equality quotient on streams, no host real equality coordinate, no selector coordinate, no alternate seal route, no detached ledger, and no ambient completeness witness.

Finally apply \autoref{thm:real-completion-exact-boundary-nonescape}. Since every accepted consumer factors through $L,K,J,S,W,R,D,E,H,C,P,N$, any terminal seal read that uses one of the excluded routes is outside the packet rather than an accepted Real seal read.
\end{proof}

\begin{theorem}[Real completion exact-boundary four-face exactness]
\label{thm:real-completion-exact-boundary-four-face-exactness}
For every accepted $\RealCompletionExactBoundaryUp$ packet
$$
B=(L,K,J,S,W,R,D,E,H,C,P,N),
$$
the accepted completion boundary exposes exactly the four Phase Real and Completion faces $\StreamNameUp(W)$, $\RegSeqRatUp(R)$, $\DyadicRatCoreUp(D)$, and $\RealUp(E)$ after the seal, classifier, witness-ledger, and budget rows $L,K,J,S$. Every completion-facing consumer and every terminal Real-seal consumer factors through the structural rows $H,C,P,N$ of the same packet. No quotient stream row, host real equality row, selected limit row, countable-choice row, ambient-completeness row, metric-completeness row, or bridge row is exported by the boundary.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-completion-exact-boundary-carrier}. Its displayed rows place $L,K,J,S$ first, followed by the four boundary faces $W,R,D,E$ and the structural rows $H,C,P,N$.

Apply \autoref{thm:real-completion-exact-boundary-namecert-obligations}. The local certificate identifies the complete row inventory of the accepted packet, so the only Phase Real and Completion face rows available after $L,K,J,S$ are $W$, $R$, $D$, and $E$.

Apply \autoref{thm:real-completion-exact-boundary-streamname-regseq-route}. The ordered route reads $W$ as the $\StreamNameUp$ finite-window face, $R$ as the $\RegSeqRatUp$ readback face, $D$ as the $\DyadicRatCoreUp$ tolerance face, and $E$ as the terminal $\RealUp$ seal face.

Finally use \autoref{thm:real-completion-exact-boundary-real-seal-nonescape} together with \autoref{thm:real-completion-exact-boundary-nonescape}. The first theorem blocks terminal Real-seal escape, and the second blocks completion-boundary escape, so any consumer of the accepted boundary must pass through $H,C,P,N$ and receives none of the excluded quotient, host-equality, selected-limit, choice, completeness, or bridge coordinates.
\end{proof}

\begin{theorem}[Real completion exact-boundary finite-tail-filter handoff]
\label{thm:real-completion-exact-boundary-finite-tail-filter-handoff}
Every accepted $\RealCompletionExactBoundaryUp$ packet
$$
B=(L,K,J,S,W,R,D,E,H,C,P,N)
$$
whose completion boundary is read through a finite cofinal-tail filter must consume the displayed $\FiniteTailFilterUp$ packet of \autoref{ch:concrete-instances-finite-tail-filter-namecert} before any completion-facing or terminal Real-seal consumer reads the boundary. Equivalently, the four faces $\StreamNameUp(W)$, $\RegSeqRatUp(R)$, $\DyadicRatCoreUp(D)$, and $\RealUp(E)$ factor through that finite-tail-filter packet, with no quotient stream row, host real equality row, selected limit row, countable-choice row, ambient-completeness row, metric-completeness row, or bridge row inserted between the cofinal-tail handoff and the accepted boundary.
\end{theorem}
\begin{proof}
Start with \autoref{thm:real-completion-exact-boundary-four-face-exactness}. It identifies the only completion and Real faces exported by the accepted boundary as $\StreamNameUp(W)$, $\RegSeqRatUp(R)$, $\DyadicRatCoreUp(D)$, and $\RealUp(E)$, all after the rows $L,K,J,S$ and before the structural rows $H,C,P,N$ are consumed.

Apply \autoref{thm:real-completion-exact-boundary-streamname-regseq-route}. The displayed route orders the same four faces as a StreamName window, regular-rational readback, dyadic tolerance ledger, and terminal Real seal, so a completion-boundary read through a finite cofinal-tail filter has no separate route on which to read those faces.

Now use \autoref{thm:finite-tail-filter-sibling-tail-handoff}. The finite-tail-filter packet is exactly the sibling handoff surface for StreamName windows, DyadicRatCore tolerances, RegSeqRat readback, and the Real seal. Hence the four displayed faces of the exact boundary must pass through that packet whenever the boundary is presented through a finite cofinal-tail filter.

Finally apply \autoref{thm:finite-tail-filter-real-seal-handoff} and \autoref{thm:real-completion-exact-boundary-real-seal-nonescape}. The first theorem carries the same finite cofinal-tail route to Real and Completion consumers, and the second prevents the terminal $\RealUp(E)$ seal from bypassing the exact-boundary route. The excluded quotient, host-equality, selected-limit, choice, completeness, and bridge coordinates therefore cannot be inserted between the finite-tail-filter handoff and the accepted boundary read.
\end{proof}

\begin{theorem}[Real completion terminal-route pullback]
\label{thm:real-completion-exact-boundary-terminal-route-pullback}
For every accepted $\RealCompletionExactBoundaryUp$ packet
$$
B=(L,K,J,S,W,R,D,E,H,C,P,N),
$$
the terminal $\RealUp(E)$ read is the pullback of the same displayed route
$$
\StreamNameUp(W),\quad \RegSeqRatUp(R),\quad \DyadicRatCoreUp(D)
$$
over the seal, classifier, witness-ledger, and budget rows $L,K,J,S$. If two accepted terminal reads display the same rows $L,K,J,S,W,R,D,H,C,P,N$, then completion-facing consumers cannot distinguish their $\RealUp$ terminal rows except through the displayed terminal coordinate of that finite packet. No quotient stream row, host real equality row, selected-limit row, countable-choice row, metric-completeness row, or bridge row participates in the pullback.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-completion-exact-boundary-carrier}. The terminal row $E$ is not a detached Real object; it appears only as the $\RealUp$ coordinate of the same finite packet that already displays $L,K,J,S,W,R,D,H,C,P,N$.

Apply \autoref{thm:real-completion-exact-boundary-namecert-obligations}. The NameCert obligations expose the seal, classifier, witness, budget, StreamName, RegSeqRat, DyadicRatCore, terminal Real, transport, replay, provenance, and naming rows as one carrier surface. Hence the displayed terminal read is determined at the completion boundary by the same route data, not by an external selector.

Apply \autoref{thm:real-completion-exact-boundary-streamname-regseq-route}. The route first reads $\StreamNameUp(W)$, then $\RegSeqRatUp(R)$, then $\DyadicRatCoreUp(D)$, and reaches $\RealUp(E)$ over $L,K,J,S$. This is the advertised pullback square at the consumer boundary: the terminal Real face is the fourth face attached to the preceding three displayed faces over the four governing rows.

Finally apply \autoref{thm:real-completion-exact-boundary-nonescape} and \autoref{thm:real-completion-exact-boundary-four-face-exactness}. Two accepted reads with the same displayed nonterminal route and the same structural rows can be compared only through the four-face inventory already exported by the packet. The theorem explicitly refuses quotient streams, host real equality, selected limits, countable choice, metric completeness, and bridge rows, so those absent objects cannot distinguish the two completion-facing reads.
\end{proof}

\closureat{RealCompletionExactBoundaryUp}{seedStr}[BEDC.Derived.RealCompletionExactBoundaryUp.RealCompletionExactBoundaryTasteGate\_single\_carrier\_alignment]

\begin{closurestatus}{\RealCompletionExactBoundaryUp}
  \constructivestory{A $\RealCompletionExactBoundaryUp$ packet is generated from displayed seal, classifier, witness-ledger, synchronizer, StreamName, RegSeqRat, DyadicRatCore, terminal Real, transport, continuation, package, and local naming rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{The seed scope binds the carrier, NameCert obligations, non-escape surface, StreamName--RegSeq route, four-face exactness, finite-tail-filter handoff, and terminal-route pullback for one finite $\RealCompletionExactBoundaryUp$ packet.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.RealCompletionExactBoundaryUp.RealCompletionExactBoundaryTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close quotient stream equality, host real equality, selected limits, countable choice, ambient completeness, metric completeness, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier transport, four-face exactness, finite-tail-filter handoff, terminal-route pullback, and the TasteGate field-faithful and nontrivial rows.}
\end{closurestatus}

Lean development for this route must provide $\mathsf{Nontrivial}\ \RealCompletionExactBoundaryUp$ and $\mathsf{FieldFaithful}\ \RealCompletionExactBoundaryUp$ instances in \texttt{lean4/BEDC/Derived/RealCompletionExactBoundaryUp/TasteGate.lean}.
