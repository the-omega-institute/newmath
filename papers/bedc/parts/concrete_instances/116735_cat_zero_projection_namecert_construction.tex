\chapter{A Concrete Naming Certificate for $\CatZeroProjectionUp$}
\label{ch:concrete-instances-cat-zero-projection-namecert}
\origin{human}

Following Bridson--Haefliger on complete CAT(0) spaces and Bacak on projection methods in Hadamard geometry, the $\CatZeroProjectionUp$ packet records the finite BEDC surface for projecting a displayed point onto a displayed closed convex subset of a complete CAT(0) carrier. It sits after the CAT(0) comparison packet of \autoref{ch:concrete-instances-cat-zero-metric-namecert}, the Hadamard-space carrier of \autoref{ch:concrete-instances-hadamard-space-namecert}, the convex-set rows of \autoref{ch:concrete-instances-convexset-namecert}, the existing projection endpoint surface of \autoref{ch:concrete-instances-metricprojection-namecert}, the complete-metric service of \autoref{ch:concrete-instances-completemetric-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the closest-point route as finite NameCert data; it does not import a host projection map, selected minimizer, quotient point equality, Hilbert embedding, local compactness, or ambient $\RealUp$ completeness.

\begin{definition}[CatZeroProjection carrier]
\label{def:cat-zero-projection-carrier}
A $\CatZeroProjectionUp$ carrier is a finite $\BHist$ packet
$$
Q=(Z,H,V,M,C,R,A,T,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Z$ is the $\CatZeroMetricUp$ comparison row, carrying the finite nonpositive-curvature inequality ledger read before projection convexity is used.
\item $H$ is the $\HadamardSpaceUp$ complete geodesic source row over the same displayed metric carrier.
\item $V$ is the $\ConvexSetUp$ row naming the closed convex target subset and its admitted geodesic-combination reads.
\item $M$ is the $\MetricProjectionUp$ endpoint row, recording the closest-point candidate only after the source point, convex target, and distance ledger have been displayed.
\item $C$ is the $\CompleteMetricUp$ service row used by minimizing-window and Cauchy-readback consumers.
\item $R$ is the $\RegSeqRatUp$ readback row for finite distance budgets and approximation windows.
\item $A$ is the $\RealUp$ seal row for distance values, squared-distance comparisons, and endpoint-error budgets.
\item $T$ records componentwise $\hsame$ transport for $Z,H,V,M,C,R,A,P,N$.
\item $K$ records displayed $\Cont$ replay from CAT(0) comparison and convexity rows through the projection endpoint, complete-metric service, rational readback, and Real seal.
\item $P$ is the $\Pkg$ provenance over $\CatZeroProjectionUp$, $\CatZeroMetricUp$, $\HadamardSpaceUp$, $\ConvexSetUp$, $\MetricProjectionUp$, $\CompleteMetricUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CatZeroProjectionUp}$ row.
\end{enumerate}
The classifier compares accepted projection packets only through these rows. It has no coordinate for a host nearest-point map, selected minimizer, quotient point equality, Hilbert-space replacement, local compactness, or ambient real completeness.
\end{definition}

\begin{theorem}[CatZeroProjection NameCert obligations]
\label{thm:cat-zero-projection-namecert-obligations}
For every accepted $\CatZeroProjectionUp$ carrier
$$
Q=(Z,H,V,M,C,R,A,T,K,P,N),
$$
the local $\NameCert_{\CatZeroProjectionUp}$ surface consists exactly of carrier admission for the CAT(0) comparison row $Z$, Hadamard source row $H$, convex target row $V$, metric-projection endpoint row $M$, complete-metric service row $C$, regular rational readback $R$, Real seal $A$, componentwise transport $T$, displayed replay $K$, provenance $P$, and local naming $N$. Every projection-facing consumer must first display the CAT(0) and Hadamard rows, then the convex target, then the projection endpoint and completion/readback rows, before the local naming row is available.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cat-zero-projection-carrier}. Its geometric source data are exactly $Z,H,V$: the CAT(0) comparison row, the complete geodesic Hadamard row, and the closed convex target row.

The endpoint stage is $M,C,R,A$. The projection endpoint is read only through the existing $\MetricProjectionUp$ surface, while complete-metric service, regular rational readback, and the terminal Real seal expose the finite approximation and distance-budget rows needed by projection consumers.

Finally $T,K,P,N$ transport, replay, source, and name the same displayed route. Since no other carrier coordinate exists, the local NameCert surface cannot export a host projection map, selected minimizer, quotient equality, Hilbert embedding, local compactness, or ambient real-completeness theorem.
\end{proof}

\begin{theorem}[CatZeroProjection nonescape]
\label{thm:cat-zero-projection-nonescape}
Every convex-projection or completion-facing read exported by an accepted $\CatZeroProjectionUp$ packet factors through
$$
\CatZeroMetricUp,\ \HadamardSpaceUp,\ \ConvexSetUp,\ \MetricProjectionUp,\ \CompleteMetricUp,\ \RegSeqRatUp,\ \RealUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
The read exposes a displayed CAT(0) closest-point route and no host nearest-point selector, quotient point equality, selected minimizing sequence, Hilbert-space embedding, local compactness theorem, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:cat-zero-projection-namecert-obligations}. Any consumer must first read the CAT(0) comparison row $Z$, Hadamard source row $H$, and convex target row $V$ before it can inspect the projection endpoint row $M$.

The endpoint row is then routed through completion and readback data. The rows $C,R,A$ keep the minimizing-window and distance-budget evidence inside displayed complete-metric, regular-rational, and Real-seal surfaces.

Transport, replay, provenance, and local naming are restricted to $T,K,P,N$. Therefore every exported projection read remains inside the finite route listed above, and the excluded host selector, quotient equality, chosen minimizing sequence, Hilbert embedding, local compactness, and ambient-completeness data have no projection from the packet.
\end{proof}

\falsifiablePrediction{Every accepted $\CatZeroProjectionUp$ consumer that reaches a closest-point or completion-facing projection read while omitting the CAT(0) comparison row, Hadamard source row, convex target row, MetricProjection endpoint row, complete-metric service, regular rational readback, Real seal, transport, replay, provenance, or local naming refutes the packet.}

\independenceWitness{cat-zero-metric, hadamard-space, convexset, metricprojection, completemetric, regseqrat, real: $\CatZeroProjectionUp$ is not bijective to any listed sibling because it jointly carries CAT(0) comparison, complete geodesic source, convex target, projection endpoint, completion service, rational readback, Real sealing, transport, replay, provenance, and local naming as one packet.}

\closureat{CatZeroProjectionUp}{seedStr}

\begin{closurestatus}{\CatZeroProjectionUp}
  \constructivestory{A $\CatZeroProjectionUp$ packet is generated from CatZeroMetric comparison rows, HadamardSpace complete geodesic rows, ConvexSet target rows, MetricProjection endpoint rows, CompleteMetric service rows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cat-zero-projection-carrier}, \autoref{thm:cat-zero-projection-namecert-obligations}, and \autoref{thm:cat-zero-projection-nonescape} seed the finite CAT(0) closest-point projection route from displayed comparison and convexity data through completion, rational readback, and Real sealing.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a host projection map, selected minimizer, quotient point equality, Hilbert embedding, local compactness, Lean verification, standard bridge closure, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, CAT(0) comparison exposure, Hadamard source exposure, convex target routing, MetricProjection endpoint admission, completion/readback exactness, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.CatZeroProjectionUp.}
\end{closurestatus}
