\chapter{A Concrete Naming Certificate for $\BaireClassTwoRealFunctionUp$}
\label{ch:concrete-instances-baire-class-two-real-function-namecert}
\origin{human}

Following Baire and Lebesgue on pointwise hierarchies of real functions, the $\BaireClassTwoRealFunctionUp$ packet records a real-valued function as a displayed pointwise limit of $\BaireClassOneRealFunctionUp$ rows. It sits after the Baire class one carrier of \autoref{ch:concrete-instances-baire-class-one-real-function-namecert}, the pointwise-limit packet of \autoref{ch:concrete-instances-pointwise-limit-namecert}, the stream-name schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet gives later descriptive-analysis consumers a second pointwise layer without quotient function equality, selected representatives, countable choice, ambient measurability, or completeness of a host function space.

\begin{definition}[Baire class two real-function carrier]
\label{def:baire-class-two-real-function-carrier}
A $\BaireClassTwoRealFunctionUp$ carrier is a finite $\BHist$ packet
$$
F=(X,S,B,L,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the source real-domain row whose point requests are read through $\RealUp$ names.
\item $S$ is the $\StreamNameUp$ schedule listing the inspected stages of the outer pointwise sequence.
\item $B$ assigns each inspected outer stage to a displayed $\BaireClassOneRealFunctionUp$ row.
\item $L$ is the $\PointwiseLimitUp$ handoff that compares the stage outputs only through finite pointwise windows.
\item $R$ is the $\RegSeqRatUp$ readback row attached to each inspected output window.
\item $E$ is the terminal $\RealUp$ seal for the displayed second-level pointwise value.
\item $H$ records componentwise $\hsame$ transport for source, schedule, Baire-one stage, pointwise-limit, readback, Real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a point request through $X,S,B,L,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\BaireClassTwoRealFunctionUp$, $\BaireClassOneRealFunctionUp$, $\PointwiseLimitUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\BaireClassTwoRealFunctionUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these rows. It has no coordinate for host equality of functions, quotient function spaces, selected representatives, countable choice, arbitrary measurability, or an ambient theorem identifying all Baire class two maps.
\end{definition}

\begin{theorem}[Baire class two real-function NameCert obligations]
\label{thm:baire-class-two-real-function-namecert-obligations}
For every accepted $\BaireClassTwoRealFunctionUp$ carrier $F=(X,S,B,L,R,E,H,C,P,N)$, the local $\NameCert_{\BaireClassTwoRealFunctionUp}$ surface consists exactly of carrier admission for the source real-domain row, the $\StreamNameUp$ outer stage schedule, the $\BaireClassOneRealFunctionUp$ stage rows, the $\PointwiseLimitUp$ handoff, the $\RegSeqRatUp$ readback, the terminal $\RealUp$ seal, componentwise transport, displayed replay, package provenance, and local naming. Every downstream descriptive-analysis consumer must consume those rows before treating the packet as a Baire class two real-valued function.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:baire-class-two-real-function-carrier}. The accepted packet exposes only the source row, outer stage schedule, Baire-one stage rows, pointwise-limit handoff, regular-rational readback, Real seal, transport, replay, provenance, and local naming.

The second pointwise layer starts with $X$ and $S$. A consumer first names the point request and the inspected outer stages, then reads the displayed $\BaireClassOneRealFunctionUp$ row at each inspected stage.

The output route is then forced through $L,R,E$. The pointwise-limit row records the finite comparison windows, the regular-rational row records the numerical readback, and the Real seal records the terminal pointwise value.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since no coordinate stores host function equality, a quotient function space, selected representatives, countable choice, arbitrary measurability, or an ambient classification theorem, the listed obligations exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Baire class two pointwise handoff]
\label{thm:baire-class-two-real-function-pointwise-handoff}
Every $\RealUp$, pointwise-limit, or descriptive-analysis consumer of an accepted $\BaireClassTwoRealFunctionUp$ carrier must read the finite route
$$
\StreamNameUp
\longmapsto
\BaireClassOneRealFunctionUp
\longmapsto
\PointwiseLimitUp
\longmapsto
\RegSeqRatUp
\longmapsto
\RealUp .
$$
Thus the Baire class two row is a displayed pointwise sequence of Baire class one real-valued maps with finite readback, not a host function equality class, a selected second-level limit map, or an imported measurable-function hierarchy.
\end{theorem}
\begin{proof}
Begin with a downstream consumer. Carrier admission forces the consumer to read the $\StreamNameUp$ schedule before any stage of the outer approximating sequence can be inspected.

The inspected stage is then a $\BaireClassOneRealFunctionUp$ row. Its output is not exposed as a host function value; it passes through the $\PointwiseLimitUp$ handoff, the $\RegSeqRatUp$ readback, and the terminal $\RealUp$ seal.

Transport and replay are supplied by $H$ and $C$, while $P$ and $N$ keep the provenance and local name attached to the same finite tuple. Consequently the consumer receives only the displayed pointwise route over named sibling packets.

The carrier inventory excludes host function equality, quotient function spaces, selected pointwise limits, countable choice, arbitrary measurability, and imported hierarchy theorems, so the handoff is exactly the finite BEDC route stated above.
\end{proof}

\falsifiablePrediction{An accepted $\BaireClassTwoRealFunctionUp$ consumer is refuted if it treats a second-level pointwise limit as public before displaying the StreamName outer schedule, BaireClassOne stage rows, PointwiseLimit handoff, RegSeqRat readback, Real seal, transport, replay, provenance, and local NameCert rows.}

\independenceWitness{baireclassone, pointwiselimit, streamname, regseqrat, real: none is bijective because $\BaireClassTwoRealFunctionUp$ binds a real-domain source, explicit outer stage schedule, Baire-one stage rows, pointwise-limit handoff, numerical readback, Real seal, transport, replay, provenance, and local NameCert rows in one packet. Each sibling supplies one constituent surface, while only this packet names the second pointwise layer.}

\closureat{BaireClassTwoRealFunctionUp}{seedStr}

\begin{closurestatus}{\BaireClassTwoRealFunctionUp}
  \constructivestory{A $\BaireClassTwoRealFunctionUp$ packet is generated from a RealUp source row, a StreamName outer stage schedule, BaireClassOneRealFunction stage rows, a PointwiseLimit handoff, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:baire-class-two-real-function-carrier}, \autoref{thm:baire-class-two-real-function-namecert-obligations}, and \autoref{thm:baire-class-two-real-function-pointwise-handoff} seed the explicit pointwise-sequence-of-Baire-one-functions route over BaireClassOneRealFunctionUp, PointwiseLimitUp, StreamNameUp, RegSeqRatUp, RealUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, host function equality, quotient function spaces, selected pointwise limits, countable choice, arbitrary measurability, or a full Baire hierarchy.}
  \upgradepath{Obligation closure requires checked carrier admission, outer stage schedule exactness, Baire-one stage exposure, pointwise-limit handoff, pointwise readback, Real seal nonescape, componentwise transport, displayed replay, provenance, and local naming for BEDC.Derived.BaireClassTwoRealFunctionUp.}
\end{closurestatus}
