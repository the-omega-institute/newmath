\chapter{A Concrete Naming Certificate for $\BishopCompletionWitnessUp$}
\label{ch:concrete-instances-bishop-completion-witness-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\BishopCompletionWitnessUp$ packet records the finite witness data that a Real and Completion consumer must expose before treating a regular Cauchy route as completion-facing. It sits after the dyadic arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the diagonal classifier boundary of \autoref{ch:concrete-instances-regularcauchylimitclassifier-namecert}, and the complete-real interface of \autoref{ch:concrete-instances-bishop-complete-real-namecert}. The packet names a displayed witness surface only; it is not $\RealUp$ completeness, quotient Cauchy equality, countable choice, or a selected completed real.

\begin{definition}[Bishop completion witness carrier]
\label{def:bishop-completion-witness-carrier}
A $\BishopCompletionWitnessUp$ carrier is a finite $\BHist$ packet
$$
W=(D,S,Q,R,L,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger recording the dyadic endpoint, radius, tail-comparison, and error-budget rows used by the witness.
\item $S$ is the $\StreamNameUp$ window row listing exactly the finite observations and requested precisions inspected by the completion read.
\item $Q$ is the $\RegSeqRatUp$ readback row obtained from those displayed windows and tolerances.
\item $R$ is the terminal $\RealUp$ seal row reached only after $D,S,Q$ have appeared.
\item $L$ is the accepted $\RegularCauchyLimitClassifierUp$ row that classifies the displayed regular-Cauchy handoff for a completion consumer.
\item $B$ is the $\BishopCompleteRealUp$ boundary row, recording only the finite witness route by which the complete-real interface may inspect $D,S,Q,R,L$.
\item $H$ records componentwise $\hsame$ transport for dyadic ledgers, stream windows, regular readbacks, Real seals, classifier rows, complete-real boundary rows, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay of the same finite witness route.
\item $P$ is the $\Pkg$ provenance over $\BishopCompletionWitnessUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\RegularCauchyLimitClassifierUp$, $\BishopCompleteRealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopCompletionWitnessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,S,Q,R,L,B,H,C,P,N$. It has no coordinate for quotient equality of Cauchy streams, selected limits, countable-choice schedules, ambient $\RealUp$ completeness, or an unbounded completion operator.
\end{definition}

\begin{theorem}[Bishop completion witness NameCert obligations]
\label{thm:bishop-completion-witness-namecert-obligations}
For every accepted $\BishopCompletionWitnessUp$ carrier
$$
W=(D,S,Q,R,L,B,H,C,P,N),
$$
the local $\NameCert_{\BishopCompletionWitnessUp}$ surface consists exactly of dyadic tolerance-ledger exposure, finite stream-window exposure, regular rational readback, terminal Real sealing, regular-Cauchy limit-classifier admission, Bishop complete-real boundary exposure, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. A consumer may read $B$ as a completion-facing witness only after $D,S,Q,R,L$ are displayed in the same finite packet.
\end{theorem}
\begin{proof}
Project the carrier $W=(D,S,Q,R,L,B,H,C,P,N)$. The analytic rows $D,S,Q,R,L,B$ list the tolerance ledger, inspected windows, regular readback, Real seal, classifier row, and complete-real boundary row.

The completion-facing row $B$ is downstream of the classifier row $L$. By the public boundary of \autoref{ch:concrete-instances-regularcauchylimitclassifier-namecert} and the complete-real interface of \autoref{ch:concrete-instances-bishop-complete-real-namecert}, the consumer sees only the displayed dyadic, stream, regular-readback, and Real-seal route before the witness is offered.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since the excluded principles are not carrier coordinates, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Bishop completion witness Real-completion handoff]
\label{thm:bishop-completion-witness-real-completion-handoff}
Every Real and Completion consumer of an accepted $\BishopCompletionWitnessUp$ packet must factor its read through
$$
\DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp \longmapsto \RegularCauchyLimitClassifierUp \longmapsto \BishopCompleteRealUp .
$$
Thus the exported completion witness is the finite route $D,S,Q,R,L,B,H,C,P,N$ over the displayed packet. It exports no quotient Cauchy class, selected limit, countable-choice schedule, host real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the dyadic and stream rows. The ledger $D$ and window $S$ determine the finite tolerance and observation data that can be inspected by a completion-facing read.

Next read the regular rational and Real rows. The row $Q$ is the only regular readback in the carrier, and $R$ is the only terminal Real seal available after that readback.

Now route through the classifier and complete-real boundary. The row $L$ makes the displayed regular-Cauchy handoff visible to the limit-classifier surface, and $B$ lets the Bishop complete-real interface consume only that classified finite witness.

Finally the rows $H,C,P,N$ preserve the same route under transport, replay, provenance, and naming. Because no excluded endpoint is a row of $W$, the consumer cannot reread the witness as quotient Cauchy equality, selected completion, countable choice, host real equality, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every $\BishopCompletionWitnessUp$ consumer reaches $\BishopCompleteRealUp$ only after exposing $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\RegularCauchyLimitClassifierUp$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, regularcauchylimitclassifier, bishop-complete-real: $\BishopCompletionWitnessUp$ is not bijective to any listed sibling because it jointly carries dyadic tolerance data, finite stream windows, regular rational readback, a Real seal, a limit-classifier row, a complete-real boundary row, transport, replay, provenance, and local naming in one finite witness packet.}

\closureat{BishopCompletionWitnessUp}{seedStr}

\begin{closurestatus}{\BishopCompletionWitnessUp}
  \constructivestory{A $\BishopCompletionWitnessUp$ packet is generated from a DyadicRatCore tolerance ledger, finite StreamName windows, RegSeqRat readback, a RealUp seal, a RegularCauchyLimitClassifier row, a BishopCompleteReal boundary row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-completion-witness-carrier}, \autoref{thm:bishop-completion-witness-namecert-obligations}, and \autoref{thm:bishop-completion-witness-real-completion-handoff} seed the finite witness route for the open Real and Completion phase.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim $\RealUp$ completeness, quotient Cauchy equality, selected limits, countable choice, host real equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic tolerance exactness, stream-window exposure, RegSeqRat readback, Real seal ordering, classifier admission, complete-real boundary routing, transport, replay, provenance, and local naming for BEDC.Derived.BishopCompletionWitnessUp.}
\end{closurestatus}
