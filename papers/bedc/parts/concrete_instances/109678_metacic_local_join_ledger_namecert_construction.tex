\chapter{A Concrete Naming Certificate for $\MetaCICLocalJoinLedgerUp$}
\label{ch:concrete-instances-metacic-local-join-ledger-namecert}
\origin{ai}

The $\MetaCICLocalJoinLedgerUp$ packet realizes the residual-substitution boundary of \autoref{ch:metacic-confluence-audit-map} as finite local-join data. It sits beside the local diamond witness of \autoref{ch:concrete-instances-metacic-local-diamond-witness-namecert}, the residual checker of \autoref{ch:concrete-instances-metacic-confluence-residual-checker-namecert}, the confluence substitution boundary of \autoref{ch:concrete-instances-metacic-confluence-substitution-boundary-namecert}, the confluence diamond boundary of \autoref{ch:concrete-instances-metacic-confluence-diamond-boundary-namecert}, and the subject-reduction discharge socket of \autoref{ch:concrete-instances-subject-reduction-discharge-socket-namecert}. The carrier records local peaks, candidate joins, bounded checker handoffs, residual-substitution frontiers, obstruction-preserving readback, and TasteGate readiness without claiming full confluence, unrestricted substitution compatibility, subject reduction, evaluator equality, or kernel consistency.

\begin{definition}[MetaCIC local join ledger carrier]
\label{def:metacic-local-join-ledger-carrier}
A $\MetaCICLocalJoinLedgerUp$ carrier is a finite $\BHist$ packet
$$
J=(K,A,B,R,S,O,G,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the local peak row, naming the source term and the two outgoing reduction observations.
\item $A$ is the candidate join row, recording the proposed common reduct and the two finite routes into it.
\item $B$ is the bounded checker handoff row, exposing the closedness, support, branch compatibility, and variable-escape checks applied to the peak.
\item $R$ is the residual frontier row, listing residual descendants and binder-depth data retained by the local join attempt.
\item $S$ is the substitution-boundary row, recording the residual-substitution input that remains visible whenever the local join is conditional.
\item $O$ is the obstruction readback row, naming the endpoints still outside the packet: unrestricted substitution compatibility, full closed Church--Rosser, arbitrary subject reduction, evaluator equality, and kernel consistency.
\item $G$ is the $\TasteGate$ readiness row, reserving the later $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instance obligations for this finite carrier surface.
\item $H$ records componentwise $\hsame$ transport for local peaks, candidate joins, bounded checks, residual frontiers, substitution boundaries, obstruction rows, TasteGate readiness, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a confluence-boundary query through $K,A,B,R,S,O,G,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\MetaCICLocalJoinLedgerUp$, $\MetaCICLocalDiamondWitnessUp$, $\MetaCICConfluenceResidualCheckerUp$, $\MetaCICConfluenceSubstitutionBoundaryUp$, $\MetaCICConfluenceDiamondBoundaryUp$, $\SubjectReductionDischargeSocketUp$, $\BHist$, $\hsame$, $\Cont$, $\TasteGate$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetaCICLocalJoinLedgerUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $K,A,B,R,S,O,G,H,C,P,N$. It contains no coordinate for a global confluence theorem, unrestricted substitution compatibility, subject reduction, quotient equality of terms, evaluator equality, or kernel consistency.
\end{definition}

\begin{theorem}[MetaCIC local join ledger obligations]
\label{thm:metacic-local-join-ledger-obligations}
For every accepted $\MetaCICLocalJoinLedgerUp$ carrier
$$
J=(K,A,B,R,S,O,G,H,C,P,N),
$$
the local $\NameCert_{\MetaCICLocalJoinLedgerUp}$ surface consists exactly of local-peak exposure through $K$, candidate-join exposure through $A$, bounded-checker handoff through $B$, residual-frontier exposure through $R$, substitution-boundary retention through $S$, obstruction readback through $O$, TasteGate readiness through $G$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-local-join-ledger-carrier}. Rows $K,A,B,R,S,O$ are the confluence-facing ledger data, and rows $G,H,C,P,N$ are the structural certificate data.

The local join attempt first reads the peak and candidate join. Row $K$ gives the finite source and branch observations, while $A$ records only the proposed common reduct and its two routes.

The bounded checker and residual rows remain visible before the join can be consumed. Row $B$ records the finite checks actually performed, $R$ keeps residual descendants and binder-depth data, and $S$ keeps the substitution-sensitive input that prevents the ledger from being read as an unconditional diamond.

Finally $O$ preserves the open endpoints, while $G,H,C,P,N$ reserve the TasteGate instance surface, transport the rows, replay the query, source the packet, and name it. Since no carrier coordinate contains full confluence, unrestricted substitution compatibility, subject reduction, evaluator equality, quotient equality, or kernel consistency, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[MetaCIC local join ledger bounded checker handoff]
\label{thm:metacic-local-join-ledger-bounded-checker-handoff}
Every checker-facing consumer of an accepted $\MetaCICLocalJoinLedgerUp$ carrier factors through
$$
K,A,B,R,S,O,G,H,C,P,N
$$
before it may consume the local join ledger. The bounded checker row $B$ is a finite handoff over the displayed peak and residual rows; it is not a global confluence checker, not a subject-reduction proof, and not an evaluator-equality decision.
\end{theorem}
\begin{proof}
Apply the obligation surface of \autoref{thm:metacic-local-join-ledger-obligations}. It forces a checker-facing read through the local peak, candidate join, bounded-checker handoff, residual frontier, substitution boundary, and obstruction row.

The handoff is therefore scoped by $K,A,R,S,O$. It can report the checks listed in $B$, but it has no coordinate from which to manufacture an unrestricted substitution theorem, a subject-reduction theorem, or a global Church--Rosser result.

The structural rows $G,H,C,P,N$ preserve the same finite route under TasteGate readiness, transport, replay, provenance, and local naming. Hence the checker handoff remains bounded local-join data.
\end{proof}

\begin{theorem}[MetaCIC local join ledger obstruction-preserving readback]
\label{thm:metacic-local-join-ledger-obstruction-preserving-readback}
Every confluence-audit read exported by an accepted $\MetaCICLocalJoinLedgerUp$ carrier preserves the obstruction row $O$ beside the candidate join. A consumer may pass the local ledger to the local-diamond witness, residual checker, substitution-boundary packet, diamond-boundary packet, or subject-reduction discharge socket named above, but it cannot erase the residual-substitution boundary or reinterpret the ledger as full MetaCIC confluence.
\end{theorem}
\begin{proof}
Start from the bounded checker handoff of \autoref{thm:metacic-local-join-ledger-bounded-checker-handoff}. The local join is consumed only after the peak, join, checker, residual, substitution-boundary, and obstruction rows have been displayed together.

The sibling handoffs preserve that pairing. The local-diamond witness may read $K$ and $A$, the residual checker may read $B,R,S,O$, the substitution-boundary and diamond-boundary packets may read the same residual-substitution interface, and the subject-reduction discharge socket may read the obstruction as a boundary rather than as a proof.

Since $O$ is a carrier coordinate and no sibling handoff adds a hidden theorem row, every readback keeps the obstruction visible. The exported ledger is therefore finite local-join data, not unrestricted substitution compatibility, full confluence, subject reduction, evaluator equality, or kernel consistency.
\end{proof}

\begin{theorem}[MetaCIC local join ledger TasteGate readiness]
\label{thm:metacic-local-join-ledger-tastegate-readiness}
The later Lean side must provide $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ $\TasteGate$ instances for $\MetaCICLocalJoinLedgerUp$ at the displayed finite carrier surface
$$
K,A,B,R,S,O,G,H,C,P,N .
$$
Those instances must certify that the packet is neither empty nor field-forgetting: every accepted carrier visibly retains the local peak, candidate join, bounded checker handoff, residual frontier, substitution boundary, obstruction readback, transport, replay, provenance, and local NameCert data.
\end{theorem}
\begin{proof}
The readiness row $G$ in \autoref{def:metacic-local-join-ledger-carrier} is tied to the whole local-join ledger rather than to a single obstruction field.

Nontriviality must exhibit an accepted packet with the displayed finite rows present. FieldFaithfulness must show that deleting any of $K,A,B,R,S,O,H,C,P,N$ changes the readable carrier surface. Thus the TasteGate burden remains at the same finite packet boundary as the NameCert obligations.
\end{proof}

\falsifiablePrediction{A $\MetaCICLocalJoinLedgerUp$ confluence-audit read factors through displayed local-peak rows, candidate-join rows, bounded-checker handoff rows, residual-frontier rows, substitution-boundary rows, obstruction readback rows, Nontrivial and FieldFaithful TasteGate readiness, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert data.}

\independenceWitness{metacic-local-diamond-witness, metacic-confluence-residual-checker, metacic-confluence-substitution-boundary, metacic-confluence-diamond-boundary, subject-reduction-discharge-socket: none is bijective with $\MetaCICLocalJoinLedgerUp$ because this packet jointly carries a local peak, candidate join, bounded checker handoff, residual frontier, substitution boundary, obstruction-preserving readback, TasteGate readiness, replay, transport, provenance, and local naming as one ledger surface.}

\closureat{MetaCICLocalJoinLedgerUp}{seedStr}

\begin{closurestatus}{\MetaCICLocalJoinLedgerUp}
  \constructivestory{A $\MetaCICLocalJoinLedgerUp$ packet is generated from local-peak rows, candidate-join rows, bounded-checker handoff rows, residual-frontier rows, substitution-boundary rows, obstruction readback rows, Nontrivial and FieldFaithful TasteGate readiness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-local-join-ledger-carrier}, \autoref{thm:metacic-local-join-ledger-obligations}, \autoref{thm:metacic-local-join-ledger-bounded-checker-handoff}, \autoref{thm:metacic-local-join-ledger-obstruction-preserving-readback}, and \autoref{thm:metacic-local-join-ledger-tastegate-readiness} seed the finite local-join ledger for MetaCIC confluence audit consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close full confluence, unrestricted substitution compatibility, arbitrary subject reduction, evaluator equality, quotient equality of terms, kernel consistency, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, local-peak exposure, candidate-join exactness, bounded-checker handoff, residual-frontier coverage, substitution-boundary retention, obstruction-preserving readback, TasteGate nontriviality, FieldFaithful coverage, transport, replay, provenance, and local naming for BEDC.Derived.MetaCICLocalJoinLedgerUp.}
\end{closurestatus}
