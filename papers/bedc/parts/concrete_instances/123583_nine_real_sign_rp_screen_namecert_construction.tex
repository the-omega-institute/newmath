\chapter{A Concrete Naming Certificate for $\NineRealSignRPScreenUp$}
\label{ch:concrete-instances-nine-real-sign-rp-screen-namecert}
\origin{ai}

The $\NineRealSignRPScreenUp$ packet realizes the finite nine-ary real-sign screen of \autoref{ch:visions-innovation-input-nine-real-sign-rp-screen} as NameCert data. It packages the Q3 row, three-negative-count row, anchored $L=2$ witness row, anchored $L=3$ refusal row, finite reflection-positivity matrix row, transport, replay, provenance, and local naming as one finite carrier. The packet names the displayed finite screen; it does not assert an infinite reflection-positivity theorem, a host positivity oracle, an unrestricted real-sign classifier, or a selected family beyond the displayed rows.

\begin{definition}[Nine-real-sign RP-screen carrier]
\label{def:nine-real-sign-rp-screen-carrier}
A $\NineRealSignRPScreenUp$ carrier is a finite $\BHist$ packet
$$
R=(Q,K,W,U,M,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the nine-ary Q3 row over $\{+1,-1\}^9$, recording the square-realness compatibility filter.
\item $K$ is the three-negative-count row, restricting the inspected signs to the displayed cardinality surface.
\item $W$ is the anchored $L=2$ witness row, recording the surviving finite screens.
\item $U$ is the anchored $L=3$ refusal row, recording the complete finite exclusion of real-sign RP rows at that level.
\item $M$ is the finite reflection-positivity matrix row, including the displayed symmetry and principal-minor checks used by the screen.
\item $H$ records componentwise $\hsame$ transport for Q3, count, witness, refusal, matrix, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a candidate sign vector through $Q,K,W,U,M$ to the local decision row.
\item $P$ records $\Pkg$ provenance over \autoref{ch:visions-innovation-input-nine-real-sign-rp-screen}, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\NineRealSignRPScreenUp}$ row.
\end{enumerate}
The carrier has no coordinate for an infinite RP hierarchy, a non-finite positivity oracle, quotient equality of sign families, hidden real-field completeness, or an unrestricted classifier outside the nine displayed signs.
\end{definition}

\begin{theorem}[Nine-real-sign RP-screen NameCert obligations]
\label{thm:nine-real-sign-rp-screen-namecert-obligations}
For every accepted $\NineRealSignRPScreenUp$ carrier
$$
R=(Q,K,W,U,M,H,C,P,N),
$$
the local $\NameCert_{\NineRealSignRPScreenUp}$ surface consists exactly of Q3 admission through $Q$, three-negative-count exposure through $K$, anchored $L=2$ witness exposure through $W$, anchored $L=3$ refusal exposure through $U$, finite RP matrix checking through $M$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:nine-real-sign-rp-screen-carrier}. The rows $Q,K,W,U,M,H,C,P,N$ are the complete finite screen.

The candidate sign vector is first filtered by Q3 compatibility and the three-negative-count row. The $L=2$ row records the finite witness surface that survives this filtering.

The $L=3$ row is a refusal row. It is read together with the finite RP matrix row $M$, whose symmetry and principal-minor checks provide the displayed finite obstruction.

Transport, replay, provenance, and local naming preserve this same finite screen. Since no infinite RP hierarchy, host positivity oracle, quotient sign-family equality, or hidden real-completeness row is present, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Nine-real-sign RP-screen finite route]
\label{thm:nine-real-sign-rp-screen-finite-route}
Every accepted $\NineRealSignRPScreenUp$ consumer reading the finite screen must pass through the Q3 row $Q$, the three-negative-count row $K$, the $L=2$ witness row $W$, the $L=3$ refusal row $U$, and the finite RP matrix row $M$ before the structural rows $H,C,P,N$ can export a local name. The route never replaces the finite matrix evidence by an infinite positivity theorem or an ambient real-field completeness claim.
\end{theorem}
\begin{proof}
Use the NameCert obligations. A consumer cannot reach $N$ before the classifier rows $Q,K,W,U,M$ have been displayed.

The $L=2$ and $L=3$ rows have different roles. The former records a finite survivor surface, while the latter records the finite refusal produced by the displayed matrix checks.

The structural rows replay and transport those results without adding a new positivity source. Therefore every accepted read remains a finite nine-real-sign RP-screen route.
\end{proof}

The $L=2$ witness and $L=3$ refusal rows are read as consumers of \autoref{ch:concrete-instances-primitive-rp-root-namecert} and \autoref{ch:concrete-instances-finite-rp-deficit-window-namecert}. The primitive-root row supplies the scale-order/root filter, and the finite deficit-window row supplies the obstruction ledger before the nine-sign screen exports its local NameCert row.

\falsifiablePrediction{A claimed $\NineRealSignRPScreenUp$ packet is rejected if it displays a Q3 row and a three-negative-count row but omits either the $L=2$ witness row, the $L=3$ refusal row, or the finite RP matrix row used to separate them.}

\independenceWitness{innovation-input-nine-real-sign-rp-screen, finite-rp-deficit-window, primitive-rp-root: $\NineRealSignRPScreenUp$ is not bijective with those surfaces because it binds the Q3 filter, three-negative-count filter, anchored $L=2$ witness, anchored $L=3$ refusal, finite RP matrix checks, transport, replay, provenance, and local naming in one finite packet.}

The later Lean file \texttt{lean4/BEDC/Derived/NineRealSignRPScreenUp/TasteGate.lean} must include explicit $\Nontrivial\ \NineRealSignRPScreenUp$ and $\FieldFaithful\ \NineRealSignRPScreenUp$ instances preserving $Q,K,W,U,M,H,C,P,N$ as separate rows before any checked carrier-alignment theorem can serve as a TasteGate witness.

\closureat{NineRealSignRPScreenUp}{seedStr}

\begin{closurestatus}{\NineRealSignRPScreenUp}
  \constructivestory{A $\NineRealSignRPScreenUp$ packet is generated from a Q3 row, a three-negative-count row, an anchored L=2 witness row, an anchored L=3 refusal row, a finite reflection-positivity matrix row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:nine-real-sign-rp-screen-carrier}, \autoref{thm:nine-real-sign-rp-screen-namecert-obligations}, and \autoref{thm:nine-real-sign-rp-screen-finite-route} seed the finite nine-real-sign RP-screen packet.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close an infinite RP hierarchy, host positivity, unrestricted real-sign classification, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked Q3 admission, count exposure, L=2 witness exposure, L=3 refusal exposure, finite RP matrix checking, transport, replay, provenance, local naming, and TasteGate row separation for BEDC.Derived.NineRealSignRPScreenUp.}
\end{closurestatus}
