\chapter{A Concrete Naming Certificate for $\WindowSixDecorationDefectUp$}
\label{ch:concrete-instances-window-six-decoration-defect-namecert}
\origin{ai}

The $\WindowSixDecorationDefectUp$ packet realizes the defect-refined decoration polynomial of \autoref{thm:philosophy-defect-refined-decoration-polynomial} inside the DNA window-six private/shared boundary-tile route. It consumes the private/shared decoration packet of \autoref{ch:concrete-instances-window-six-private-shared-decoration-namecert}, the private/shared model packet of \autoref{ch:concrete-instances-private-shared-decoration-model-namecert}, and the Alexander-dual trigger algebra of \autoref{ch:concrete-instances-alexander-dual-trigger-algebra-namecert}. The packet records private/shared decomposition, shared row-set arity, defect exponents, polynomial coefficients, trigger coverage, transport, replay, provenance, and local naming as finite rows; it is not a biological mechanism, stochastic model, hidden enumeration oracle, or quotient codon semantics.

\begin{definition}[Window-six decoration defect carrier]\label{def:window-six-decoration-defect-carrier}
A $\WindowSixDecorationDefectUp$ carrier is a finite $\BHist$ packet
$$
W=(B,S,A,E,K,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the private/shared decomposition row, read through the displayed private-block and shared-coordinate split.
\item $S$ is the shared row-set arity row, assigning each shared coordinate to a finite row set of size at least two.
\item $A$ is the defect exponent ledger, recording the contribution $r-1$ attached to a shared row set of arity $r$.
\item $E$ is the decoration-polynomial coefficient row, recording the finite coefficient data used by the defect-refined polynomial.
\item $K$ is the trigger-coverage row, tying the decomposition to accepted window-six boundary-trigger coverage.
\item $T$ records componentwise $\hsame$ transport for decomposition, arity, exponent, coefficient, trigger, replay, provenance, and local naming rows.
\item $H$ records displayed $\Cont$ replay from a defect-polynomial request through $B,S,A,E,K,T,P,N$.
\item $C$ is the $\Pkg$ provenance over $\WindowSixDecorationDefectUp$, $\WindowSixPrivateSharedDecorationUp$, $\PrivateSharedDecorationModelUp$, $\AlexanderDualTriggerAlgebraUp$, the source theorem of \autoref{thm:philosophy-defect-refined-decoration-polynomial}, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $P$ is the trigger-polynomial readback row that keeps coefficient reads attached to displayed trigger coverage rather than to a hidden enumeration oracle.
\item $N$ is the local $\NameCert_{\WindowSixDecorationDefectUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,S,A,E,K,T,H,C,P,N$. It has no coordinate for biological semantics, stochastic fitness, quotient codon equality, hidden global enumeration, or unrecorded trigger search.
\end{definition}

\begin{theorem}[Window-six decoration defect NameCert obligations]\label{thm:window-six-decoration-defect-namecert-obligations}
For every accepted $\WindowSixDecorationDefectUp$ carrier $W=(B,S,A,E,K,T,H,C,P,N)$, the local $\NameCert_{\WindowSixDecorationDefectUp}$ surface consists exactly of private/shared decomposition exposure, shared row-set arity exposure, defect-exponent exposure, decoration-polynomial coefficient exposure, trigger-coverage exposure, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, trigger-polynomial readback, and local naming.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:window-six-decoration-defect-carrier}. The decomposition, arity, exponent, coefficient, trigger, transport, replay, provenance, readback, and local naming data are precisely $B,S,A,E,K,T,H,C,P,N$.

The rows $B,S$ expose the private/shared split and the finite arities of shared row sets. The row $A$ records the defect contribution for each shared coordinate, and $E$ records the coefficient ledger read by the polynomial.

The trigger row $K$ keeps the polynomial attached to the accepted window-six coverage route. Finally $T,H,C,P,N$ transport, replay, source, read back, and name the same finite packet, leaving no carrier coordinate for biological semantics, stochastic fitness, quotient codon equality, hidden enumeration, or unrecorded trigger search.
\end{proof}

\begin{theorem}[Window-six decoration defect polynomial handoff]\label{thm:window-six-decoration-defect-polynomial-handoff}
Every accepted $\WindowSixDecorationDefectUp$ consumer that reads the defect-refined decoration polynomial must first expose
$$
B,S,A,E,K,T,H,C,P,N .
$$
The handoff realizes \autoref{thm:philosophy-defect-refined-decoration-polynomial} as finite private/shared row-set arity, defect-exponent, coefficient, trigger-coverage, transport, replay, provenance, readback, and local NameCert rows.
\end{theorem}
\begin{proof}
Read the private/shared decomposition through $B$ and the shared row-set arities through $S$. These rows determine which shared coordinates contribute to the defect ledger.

Use $A$ to expose the exponent contribution attached to each shared arity and $E$ to expose the corresponding coefficient row. The trigger row $K$ ensures that the coefficient read remains tied to the window-six boundary-trigger surface.

The remaining rows $T,H,C,P,N$ transport, replay, source, read back, and locally name the same finite route. Hence the consumer receives the finite polynomial handoff described by the theorem rather than a hidden enumeration oracle or external biological semantics.
\end{proof}

\falsifiablePrediction{Every accepted $\WindowSixDecorationDefectUp$ consumer must display private/shared decomposition rows, shared row-set arity rows, defect-exponent ledger rows, coefficient rows, trigger-coverage rows, hsame transport, Cont replay, Pkg provenance, trigger-polynomial readback, and local NameCert rows before the defect-refined decoration polynomial is read.}

\independenceWitness{window-six-private-shared-decoration, private-shared-decoration-model, alexander-dual-trigger-algebra, dna-window-six-boundary-tile, dna-boundary-trigger-closure: $\WindowSixDecorationDefectUp$ is not bijective to any listed sibling because it jointly carries arity, exponent, coefficient, trigger, transport, replay, provenance, readback, and local naming rows for the defect-refined polynomial.}

The Lean TasteGate route for \texttt{BEDC.Derived.WindowSixDecorationDefectUp} must provide explicit $\Nontrivial\ \WindowSixDecorationDefectUp$ and $\FieldFaithful\ \WindowSixDecorationDefectUp$ instances preserving $B,S,A,E,K,T,H,C,P,N$ as separate displayed fields.

\closureat{WindowSixDecorationDefectUp}{seedStr}

\begin{closurestatus}{\WindowSixDecorationDefectUp}
  \constructivestory{A $\WindowSixDecorationDefectUp$ packet is generated from private/shared decomposition rows, shared row-set arity rows, defect-exponent ledgers, decoration-polynomial coefficient rows, trigger-coverage rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, trigger-polynomial readback, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:window-six-decoration-defect-carrier}, \autoref{thm:window-six-decoration-defect-namecert-obligations}, and \autoref{thm:window-six-decoration-defect-polynomial-handoff} seed the finite defect-refined decoration polynomial packet over displayed private/shared, trigger, transport, replay, provenance, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close biological mechanism, stochastic fitness, quotient codon equality, hidden global enumeration, unrestricted trigger search, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, private/shared decomposition, shared row-set arity, defect-exponent ledger, coefficient exposure, trigger-coverage routing, TasteGate nontriviality, field faithfulness, transport, replay, provenance, readback, and local naming for BEDC.Derived.WindowSixDecorationDefectUp.}
\end{closurestatus}
