\chapter{A Concrete Naming Certificate for $\LorentzFrameRateUp$}
\label{ch:concrete-instances-lorentz-frame-rate-namecert}
\origin{ai}

The $\LorentzFrameRateUp$ interface records the finite packet at the boundary where $\MaxCausalRateUp$ becomes an observer-symmetric frame-rate ledger. It does not introduce Lorentz geometry, a metric tensor, synchronized clocks, global frames, or host real-valued speed. It names only the BEDC rows by which the maximum-rate surface can be read uniformly across the displayed histories of a multi-Hist configuration.

\begin{definition}[LorentzFrameRate carrier]
\label{def:lorentz-frame-rate-carrier}
A $\LorentzFrameRateUp$ carrier is a finite $\BHist$ packet
$$
L=(M,X,R,S,H,C,P,N)
$$
with the following visible coordinates:
\begin{enumerate}
\item $M$ is an accepted $\MultiHistConfigUp$ row displaying the finite observer-history configuration and its no-global-sync boundary;
\item $X$ is the finite family of accepted $\CrossHistCausalUp$ witness rows over selected history pairs from $M$;
\item $R$ is an accepted $\MaxCausalRateUp$ row whose witness family is the displayed family $X$ over the same configuration row $M$;
\item $S$ is the observer-symmetry classifier row, recording that each selected observer read of the maximum-rate surface is the same finite unary-bound row of $R$ after componentwise transport;
\item $H$ records componentwise $\hsame$ transport for $M$, $X$, $R$, $S$, the continuation routes, the package row, and the local naming row;
\item $C$ records the visible $\Cont$ routes from the displayed multi-Hist, causal, maximum-rate, and symmetry rows into the local certificate;
\item $P$ is the $\Pkg$ provenance over $\LorentzFrameRateUp$, $\MultiHistConfigUp$, $\CrossHistCausalUp$, $\MaxCausalRateUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\LorentzFrameRateUp}$ row.
\end{enumerate}
The carrier exports a finite observer-symmetric maximum-rate packet. Its classifier preserves the displayed maximum-rate row under accepted observer transport while excluding global frames, synchronized clocks, metric tensors, host real-valued speed, and Lorentz geometry as primitive input.
\end{definition}
\leandef{BEDC.Derived.LorentzFrameRateUp.LorentzFrameRateUp}
\leanchecked{BEDC.Derived.LorentzFrameRateUp.taste\_gate}

\begin{theorem}[LorentzFrameRate boundary]
\label{thm:lorentz-frame-rate-boundary}
Every accepted $\LorentzFrameRateUp$ carrier presents the next inter-Hist locality boundary as the finite packet
$$
L=(M,X,R,S,H,C,P,N).
$$
A consumer may read the multi-Hist configuration row, the selected cross-Hist causal witnesses, the maximum-rate row, the observer-symmetry classifier, componentwise $\hsame$ transport, visible $\Cont$ routes, package provenance, and the local $\NameCert_{\LorentzFrameRateUp}$ row. No other frame-rate surface is public through this certificate.
\end{theorem}
\begin{proof}
Project the accepted carrier. The rows $M,X,R,S,H,C,P,N$ are exactly the admitted coordinates of \autoref{def:lorentz-frame-rate-carrier}.

Read $M$ and $X$ first. These rows keep the boundary tied to the displayed multi-Hist configuration and the selected cross-Hist causal witnesses.

Next read $R$ and $S$. The maximum-rate row supplies the finite unary-bound surface, while the observer-symmetry classifier records that transported observer reads preserve that same displayed row.

Finally route all consumer access through $H,C,P,N$. The resulting boundary is exhausted by the listed rows, so no additional frame-rate surface is exported.
\end{proof}

\begin{theorem}[LorentzFrameRate non-escape]
\label{thm:lorentz-frame-rate-non-escape}
An accepted $\LorentzFrameRateUp$ carrier cannot export a global frame, synchronized clock, metric tensor, host real-valued speed, or Lorentz geometry as a primitive input. Any consumer request for those objects leaves the finite rows
$$
M,X,R,S,H,C,P,N
$$
rather than projecting from the local $\NameCert_{\LorentzFrameRateUp}$ surface.
\end{theorem}
\begin{proof}
The carrier inventory contains only the displayed multi-Hist row, cross-Hist witness family, maximum-rate row, symmetry classifier, transport rows, continuation routes, provenance, and local naming row.

Apply the non-escape clauses of the upstream $\MultiHistConfigUp$, $\CrossHistCausalUp$, and $\MaxCausalRateUp$ certificates. Those rows already exclude global synchronization, unlisted causal witnesses, metric geometry, and ambient real-valued rates from their local surfaces.

The symmetry classifier $S$ adds no coordinate of the excluded kind. It records preservation of the finite maximum-rate row under accepted observer transport, so it cannot become a frame, a clock, a tensor, a host speed, or Lorentz geometry.
\end{proof}

\begin{theorem}[LorentzFrameRate NameCert obligations]
\label{thm:lorentz-frame-rate-namecert-obligations}
For the carrier of \autoref{def:lorentz-frame-rate-carrier}, the local $\NameCert_{\LorentzFrameRateUp}$ surface consists exactly of carrier admission, configuration inheritance from $\MultiHistConfigUp$, causal-witness inheritance from $\CrossHistCausalUp$, maximum-rate inheritance from $\MaxCausalRateUp$, observer-symmetry preservation of the shared unary-bound row, componentwise $\hsame$ stability, displayed $\Cont$ locality, $\Pkg$ provenance, and non-escape for global frames, synchronized clocks, metric tensors, host real-valued speed, and Lorentz geometry as primitive input.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:lorentz-frame-rate-carrier}. The finite packet supplies every row named by the local certificate.

The inherited rows are read in dependency order. The configuration row $M$ supplies the displayed histories, the family $X$ supplies the selected cross-Hist causal witnesses over those histories, and the maximum-rate row $R$ supplies the shared unary-bound surface over the same witnesses.

Observer-symmetry preservation is the role of $S$. It states that the selected observer reads of $R$ remain the same displayed maximum-rate row after componentwise transport.

The rows $H,C,P,N$ provide the remaining stability, locality, provenance, and local naming data. Since no coordinate of the carrier is a global frame, synchronized clock, metric tensor, host real-valued speed, or Lorentz geometry, the non-escape obligation is the displayed inventory boundary.
\end{proof}

\begin{theorem}[Frame-rate observer-symmetry transport]
\label{thm:frame-rate-observer-symmetry-transport}
Let
$$
L=(M,X,R,S,H,C,P,N)
$$
be an accepted $\LorentzFrameRateUp$ carrier. The observer-symmetry row $S$ transports the displayed maximum-rate row $R$ componentwise under the $\hsame$ data in $H$: every selected observer read named by $S$ has the same $\MaxCausalRateUp$ unary-bound surface as $R$ after applying the corresponding component of $H$, and the transported read remains routed through the same finite continuation packet $C$.
\end{theorem}
\begin{proof}
Project the accepted carrier and read the symmetry coordinate $S$. By \autoref{def:lorentz-frame-rate-carrier}, $S$ records exactly the observer reads whose maximum-rate surface is compared with the displayed row $R$.

Next read the transport coordinate $H$. Its $R$- and $S$-components move the maximum-rate row and each observer-symmetry read together, so the comparison is componentwise rather than a global frame comparison.

Finally use the continuation coordinate $C$. It keeps every transported observer read on the displayed route from $M$ and $X$ to $R$ and then to the local certificate, so the shared unary-bound surface is preserved as a finite $\MaxCausalRateUp$ row.
\end{proof}

\begin{theorem}[Frame-rate rate-row inheritance]
\label{thm:frame-rate-rate-row-inheritance}
For an accepted $\LorentzFrameRateUp$ carrier
$$
L=(M,X,R,S,H,C,P,N),
$$
the maximum-rate row $R$ is inherited only from the same upstream $\MultiHistConfigUp$ row $M$ and $\CrossHistCausalUp$ witness family $X$ displayed by the carrier. No consumer of $L$ may read $R$ as a rate row over a different configuration, an unlisted causal-witness family, or an ambient host geometry.
\end{theorem}
\begin{proof}
The carrier definition binds $R$ to $M$ and $X$: $R$ is the accepted $\MaxCausalRateUp$ row whose witness family is the displayed family $X$ over the same configuration row $M$.

The continuation routes preserve this dependency. A consumer reaches $R$ only through the visible route from the multi-Hist configuration row to the selected cross-Hist causal witnesses and then to the maximum-rate row.

The provenance row $P$ records the same dependency surface. Since neither the carrier nor the provenance row contains a different configuration, an unlisted witness family, or an ambient geometry coordinate, the inheritance of $R$ is limited to the displayed $M$ and $X$ rows.
\end{proof}

\begin{theorem}[Frame-rate locality non-escape]
\label{thm:frame-rate-locality-nonescape}
The local packet
$$
M,X,R,S,H,C,P,N
$$
does not recover a global frame, synchronized clock, metric tensor, host real speed, or Lorentz geometry. These objects are not hidden consequences of observer-symmetry transport, rate-row inheritance, or the local $\NameCert_{\LorentzFrameRateUp}$ surface.
\end{theorem}
\begin{proof}
Start with the non-escape theorem for the chapter. It excludes global frames, synchronized clocks, metric tensors, host real-valued speed, and Lorentz geometry as primitive inputs of the carrier.

Observer-symmetry transport adds no excluded coordinate. It moves the displayed maximum-rate row componentwise under $\hsame$ and keeps the read on the visible continuation route.

Rate-row inheritance also adds no excluded coordinate. It ties $R$ only to the displayed multi-Hist configuration $M$ and the selected cross-Hist witness family $X$.

Thus the local certificate remains a finite observer-symmetric maximum-rate packet. Recovering any excluded structure would require data outside $M,X,R,S,H,C,P,N$.
\end{proof}

\begin{theorem}[Frame-rate configuration causal inheritance]
\label{thm:lorentz-frame-rate-configuration-causal-inheritance}
For an accepted $\LorentzFrameRateUp$ carrier
$$
L=(M,X,R,S,H,C,P,N),
$$
the coordinates $M,X,R$ are consumed in dependency order. The multi-Hist row $M$ supplies the displayed observer-history configuration, the family $X$ supplies only $\CrossHistCausalUp$ witness rows over selected history pairs from $M$, and the maximum-rate row $R$ supplies only the $\MaxCausalRateUp$ row whose witness family is that displayed $X$ over the same $M$.
\end{theorem}
\begin{proof}
Project the carrier and read the first three computational coordinates. The definition of the carrier names $M$ before $X$, because the selected causal witnesses are meaningful only over the displayed multi-Hist configuration.

Read $X$ next. Each row in the family is a $\CrossHistCausalUp$ witness row for selected history pairs from $M$, so the family cannot select a history outside the configuration already displayed by $M$.

Finally read $R$. The maximum-rate row is the accepted $\MaxCausalRateUp$ row over the same configuration and the same selected witness family. Thus consumer access to $R$ factors through $M$ and then $X$, and no alternate configuration or witness family is available through the local packet.
\end{proof}

\begin{theorem}[Frame-rate symmetry classifier exactness]
\label{thm:lorentz-frame-rate-symmetry-classifier-exactness}
For an accepted $\LorentzFrameRateUp$ carrier
$$
L=(M,X,R,S,H,C,P,N),
$$
the rows $S,H,C$ compare only transported maximum-rate rows. The observer-symmetry classifier $S$ may identify selected observer reads of the displayed $\MaxCausalRateUp$ row $R$ after componentwise $\hsame$ transport in $H$ and visible routing in $C$; it cannot introduce a global frame, synchronized clock, metric tensor, host real-valued speed, Lorentz geometry, or a rate row not inherited from $M,X,R$.
\end{theorem}
\begin{proof}
Start with the role of $S$. By \autoref{def:lorentz-frame-rate-carrier}, the classifier records equality of selected observer reads only as preservation of the displayed maximum-rate surface of $R$ after componentwise transport.

Now apply the transport and route coordinates. The $H$ row moves the $R$- and $S$-components together by $\hsame$, while $C$ keeps the transported read on the visible continuation route through the same local certificate.

The excluded structures are absent from every compared coordinate. Since $S,H,C$ mention only the transported $\MaxCausalRateUp$ row and the local route that reaches it, the classifier has no coordinate from which to manufacture a global frame, synchronized clock, metric tensor, host real-valued speed, Lorentz geometry, or an unrelated rate row.
\end{proof}

\begin{theorem}[LorentzFrameRate TasteGate obligation package]
\label{thm:lorentz-frame-rate-tastegate-obligation-package}
The $\LorentzFrameRateUp$ chapter presents the obligation surface
$$
M,X,R,S,H,C,P,N
$$
as a single finite package: carrier admission is \autoref{def:lorentz-frame-rate-carrier}; inherited-row exactness is given by \autoref{thm:lorentz-frame-rate-configuration-causal-inheritance}; observer-symmetry transport is given by \autoref{thm:frame-rate-observer-symmetry-transport} and \autoref{thm:lorentz-frame-rate-symmetry-classifier-exactness}; provenance is the row $P$; locality is the row $C$; non-escape is \autoref{thm:frame-rate-locality-nonescape}; and the existing TasteGate marker attached to \autoref{def:lorentz-frame-rate-carrier} records that this AI-origin carrier is admitted only at the displayed single-carrier surface.
\end{theorem}
\begin{proof}
Carrier admission is the finite packet definition. It exposes precisely the eight coordinates $M,X,R,S,H,C,P,N$ and no ambient Lorentz-geometric structure.

The inherited-row obligations are discharged by reading the dependency chain in order. The configuration row $M$ feeds the causal witness family $X$, and those two rows feed the maximum-rate row $R$.

The observer-symmetry obligations are discharged by the two classifier theorems. They compare selected observer reads only after componentwise $\hsame$ transport and visible continuation routing, so the comparison remains a statement about the same finite maximum-rate row.

The remaining obligations are local bookkeeping. The row $P$ records provenance, $C$ records the visible continuation routes, \autoref{thm:frame-rate-locality-nonescape} supplies non-escape, and the TasteGate marker already attached to the carrier records the AI-origin admission boundary without adding a second marker at this theorem site.
\end{proof}

\begin{theorem}[LorentzFrameRate upstream row coherence]
\label{thm:lorentz-frame-rate-upstream-row-coherence}
For an accepted $\LorentzFrameRateUp$ carrier
$$
L=(M,X,R,S,H,C,P,N),
$$
the rows $M$, $X$, and $R$ form one coherent upstream surface. Every row of the family $X$ is a $\CrossHistCausalUp$ witness over the displayed $\MultiHistConfigUp$ row $M$, and the row $R$ is the $\MaxCausalRateUp$ maximum-rate row over exactly that displayed $X$-family and the same configuration row $M$.
\end{theorem}
\begin{proof}
Project the carrier $L=(M,X,R,S,H,C,P,N)$. The first three coordinates are not independent imports: the definition binds $X$ to selected history pairs from $M$ and binds $R$ to the same displayed witness family.

Apply \autoref{thm:frame-rate-rate-row-inheritance}. It states that a consumer may read $R$ only over the same upstream $\MultiHistConfigUp$ row $M$ and $\CrossHistCausalUp$ witness family $X$, and not over a different configuration, unlisted witness family, or ambient host geometry.

Thus the upstream surface available through $L$ is the single displayed chain $M$ to $X$ to $R$.
\end{proof}

\begin{theorem}[LorentzFrameRate symmetry classifier totality]
\label{thm:lorentz-frame-rate-symmetry-classifier-totality}
For an accepted $\LorentzFrameRateUp$ carrier
$$
L=(M,X,R,S,H,C,P,N),
$$
every observer-symmetry read factors through the classifier row $S$ together with the componentwise transport row $H$ and the displayed continuation row $C$. No observer-symmetry read exports a hidden global frame, synchronized clock, metric tensor, host real-valued speed, Lorentz geometry, or rate row outside the displayed upstream surface.
\end{theorem}
\begin{proof}
Read the classifier coordinate first. By \autoref{thm:frame-rate-observer-symmetry-transport}, $S$ compares selected observer reads of the displayed maximum-rate row $R$ only after componentwise $\hsame$ transport in $H$ and along the same finite continuation packet $C$.

Then apply \autoref{thm:frame-rate-locality-nonescape}. The transported comparison has no route to a global frame, synchronized clock, metric tensor, host real speed, or Lorentz geometry.

Finally combine this with \autoref{thm:lorentz-frame-rate-upstream-row-coherence}. Since the rate row being compared is still the displayed $R$ over $M$ and $X$, the classifier is total only over the local packet surface and not over an ambient observer frame or clock.
\end{proof}

\begin{theorem}[LorentzFrameRate obligation closure package]
\label{thm:lorentz-frame-rate-obligation-closure-package}
The $\LorentzFrameRateUp$ chapter closes the local $\NameCert_{\LorentzFrameRateUp}$ obligation surface for
$$
M,X,R,S,H,C,P,N.
$$
Carrier admission is \autoref{def:lorentz-frame-rate-carrier}; upstream row coherence is \autoref{thm:lorentz-frame-rate-upstream-row-coherence}; symmetry classifier totality is \autoref{thm:lorentz-frame-rate-symmetry-classifier-totality}; transport locality is \autoref{thm:frame-rate-observer-symmetry-transport}; provenance is the row $P$; locality is the row $C$; non-escape is \autoref{thm:frame-rate-locality-nonescape}; and the existing TasteGate marker on the AI-origin carrier records the single-carrier admission boundary.
\end{theorem}
\begin{proof}
Carrier admission is the finite packet definition. It displays exactly the rows $M,X,R,S,H,C,P,N$ and supplies the local $\NameCert_{\LorentzFrameRateUp}$ row without adding global geometric input.

The upstream and classifier obligations are now explicit. The upstream coherence theorem binds $M$, $X$, and $R$ as one dependency surface, while the classifier totality theorem forces observer-symmetry reads through $S,H,C$ over that same surface.

The remaining rows complete the certificate boundary. Transport locality is supplied by the observer-symmetry transport theorem, provenance is recorded by $P$, visible continuation by $C$, and non-escape by the locality theorem. The TasteGate marker already attached to the carrier remains the Lean-side admission marker for this AI-origin single-carrier surface.
\end{proof}

\closureat{LorentzFrameRateUp}{seedStr}
\closureat{LorentzFrameRateUp}{obligationStr}[BEDC.Derived.LorentzFrameRateUp.LorentzFrameRateTasteGate\_single\_carrier\_alignment]

\begin{closurestatus}{\LorentzFrameRateUp}
  \constructivestory{A $\LorentzFrameRateUp$ packet is generated from a $\MultiHistConfigUp$ row, finitely many $\CrossHistCausalUp$ witness rows, one $\MaxCausalRateUp$ maximum-rate row over the same configuration, an observer-symmetry classifier, finite $\hsame$ and $\Cont$ transports, package provenance, and a local $\NameCert$ row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:lorentz-frame-rate-carrier}, \autoref{thm:lorentz-frame-rate-upstream-row-coherence}, \autoref{thm:lorentz-frame-rate-symmetry-classifier-totality}, \autoref{thm:lorentz-frame-rate-obligation-closure-package}, \autoref{thm:frame-rate-observer-symmetry-transport}, and \autoref{thm:frame-rate-locality-nonescape} bind the finite observer-symmetric maximum-rate packet over MultiHistConfig, CrossHistCausal, MaxCausalRate, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \leantarget{BEDC.Derived.LorentzFrameRateUp.taste\_gate}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close Lorentz geometry, metric tensors, light-cone distance, synchronized clocks, global frames, host real-valued speed, curved spacetime, or Lean-side verification.}
  \upgradepath{The next grade requires scoped consumer exactness and Lean verification for the same displayed finite frame-rate packet.}
\end{closurestatus}
