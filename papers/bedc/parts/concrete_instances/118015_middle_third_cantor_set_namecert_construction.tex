\chapter{A Concrete Naming Certificate for $\MiddleThirdCantorSetUp$}
\label{ch:concrete-instances-middle-third-cantor-set-namecert}
\origin{human}

Following Cantor and Bishop--Bridges constructive analysis, the $\MiddleThirdCantorSetUp$ packet records the middle-third Cantor subset of the real line as finite ternary-stage NameCert data. It reads the binary prefix discipline of $\CantorSpaceUp$ through \autoref{ch:concrete-instances-cantorspace-namecert}, the compact finite-prefix surface of $\CantorSpaceCompactnessUp$ through \autoref{ch:concrete-instances-cantor-space-compactness-namecert}, the rational endpoint arithmetic of $\RatUp$ through \autoref{ch:concrete-instances-rat-namecert}, the finite observation schedule of $\StreamNameUp$ through \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of $\RegSeqRatUp$ through \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of $\RealUp$ through \autoref{ch:concrete-instances-real-namecert}. The carrier names only finite retained-third prefixes, finite deleted-gap ledgers, regular rational endpoints, stream windows, transport, replay, provenance, and local naming; it does not import host subsets, quotient real equality, selected infinite branches, open-cover compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Middle-third Cantor set carrier]
\label{def:middle-third-cantor-set-carrier}
A $\MiddleThirdCantorSetUp$ carrier is a finite $\BHist$ packet
$$
C=(B,T,G,Q,S,R,E,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\CantorSpaceUp$ prefix row, recording the finite binary choices that are translated into retained left-third and right-third ternary stages.
\item $T$ is the finite ternary-stage ledger, listing the retained closed intervals visible at the inspected depths.
\item $G$ is the deleted-middle gap ledger, listing exactly the middle-third intervals removed before the displayed retained stages are read.
\item $Q$ is the $\RatUp$ endpoint row, giving rational endpoints for every retained interval and deleted gap named by $T$ and $G$.
\item $S$ is the $\StreamNameUp$ observation schedule that bounds which stage numbers, prefix positions, and endpoint rows a consumer may inspect.
\item $R$ is the $\RegSeqRatUp$ readback row carrying the displayed nested endpoint approximations toward a real-name consumer.
\item $E$ is the $\RealUp$ seal row reached only after the finite prefix, gap, endpoint, stream, and regular-rational rows have been displayed.
\item $H$ stores componentwise $\hsame$ transport for the Cantor prefix row, ternary-stage ledger, deleted-gap ledger, rational endpoints, stream schedule, regular-rational readback, Real seal, replay route, provenance, and local naming.
\item $K$ is the displayed $\Cont$ replay route from finite Cantor prefixes to retained ternary stages, from retained stages to endpoint rows, and from endpoint rows to the regular-rational and Real-facing handoff.
\item $P$ is the $\Pkg$ provenance over $\MiddleThirdCantorSetUp$, $\CantorSpaceUp$, $\CantorSpaceCompactnessUp$, $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MiddleThirdCantorSetUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these finite rows. It has no coordinate for a host subset of the real line, quotient equality of real names, an infinite branch selector, an open-cover compactness theorem, or ambient real completeness.
\end{definition}

\begin{theorem}[Middle-third Cantor set NameCert obligations]
\label{thm:middle-third-cantor-set-namecert-obligations}
For every accepted $\MiddleThirdCantorSetUp$ carrier
$$
C=(B,T,G,Q,S,R,E,H,K,P,N),
$$
the local $\NameCert_{\MiddleThirdCantorSetUp}$ surface consists exactly of carrier admission, finite Cantor-prefix translation, retained ternary-stage visibility, deleted-middle gap exclusion, rational endpoint exactness, StreamName schedule discipline, regular-rational readback, Real seal handoff, componentwise transport, continuation replay, provenance, and finite-stage nonescape. Every obligation is read through
$$
B,T,G,Q,S,R,E,H,K,P,N
$$
and through no additional host-level membership or compactness coordinate.
\end{theorem}
\begin{proof}
Carrier admission is projection of \autoref{def:middle-third-cantor-set-carrier}. The rows $B,T,G$ expose the finite Cantor prefix, the retained ternary-stage ledger, and the deleted-middle ledger before any endpoint or Real-facing read is available.

Endpoint exactness is then forced by $Q$. Each retained interval and each deleted gap named by $T$ or $G$ receives rational endpoints in the same displayed packet, so the consumer cannot replace the finite ternary-stage route by a host subset of $\RealUp$.

The observation and readback obligations are $S,R,E$. The StreamName row $S$ bounds the visible stages, the regular-rational row $R$ records the endpoint approximations, and the Real row $E$ is reached only after those finite rows have been consumed.

Finally $H,K,P,N$ preserve, replay, source, and locally name the same route. Since no displayed row contains quotient real equality, selected infinite branches, open-cover compactness, or ambient completeness, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Middle-third Cantor set finite-stage nonescape]
\label{thm:middle-third-cantor-set-finite-stage-nonescape}
Every Real-facing read of an accepted $\MiddleThirdCantorSetUp$ carrier factors through the finite route
$$
B,S \longmapsto T \longmapsto G \longmapsto Q \longmapsto R \longmapsto E \longmapsto H,K,P,N .
$$
The route exports a finite retained-stage and deleted-gap certificate over rational endpoint rows. It exports no host subset membership predicate, no quotient equality of real names, no selected infinite branch, no open-cover compactness principle, and no ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
Start with a Real-facing consumer. The only available stage request is the $\StreamNameUp$ schedule $S$ applied to the finite $\CantorSpaceUp$ prefix row $B$, so the consumer sees finitely many binary choices rather than an infinite branch.

Read the ternary-stage rows next. The retained intervals in $T$ and the deleted gaps in $G$ are displayed together, and the rational endpoint row $Q$ gives the finite endpoint data attached to those rows.

Only after that endpoint data is visible may the packet expose $R$ and $E$. The $\RegSeqRatUp$ readback $R$ carries the displayed endpoint approximations, and the $\RealUp$ seal $E$ is the terminal row of that same finite route.

The structural rows $H,K,P,N$ cannot add omitted host principles; they transport, replay, source, and name the route already displayed. Thus the Real-facing handoff remains a finite-stage middle-third Cantor certificate.
\end{proof}

\falsifiablePrediction{Every accepted $\MiddleThirdCantorSetUp$ Real-facing read exposes finite Cantor prefixes, retained ternary stages, deleted-middle gaps, rational endpoints, StreamName schedules, RegSeqRat readbacks, Real seals, hsame transport, Cont replay, Pkg provenance, and local NameCert rows before any membership-shaped output is available.}

\independenceWitness{cantorspace, cantor-space-compactness, rat, streamname, regseqrat, real: $\MiddleThirdCantorSetUp$ is not bijective to any listed sibling because it jointly carries Cantor-prefix translation, retained ternary-stage ledgers, deleted-middle gap rows, rational endpoint exactness, finite observation schedules, regular-rational readback, Real sealing, transport, replay, provenance, and local naming.}

\closureat{MiddleThirdCantorSetUp}{seedStr}

\begin{closurestatus}{\MiddleThirdCantorSetUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:middle-third-cantor-set-carrier}, \autoref{thm:middle-third-cantor-set-namecert-obligations}, and \autoref{thm:middle-third-cantor-set-finite-stage-nonescape} seed the finite ternary-stage route over $\CantorSpaceUp$, $\CantorSpaceCompactnessUp$, $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{The chapter does not claim Lean verification, bridge checking, host subset membership, quotient equality of real names, selected infinite branches, open-cover compactness, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, finite prefix translation, retained-stage visibility, deleted-gap exclusion, rational endpoint exactness, StreamName schedule discipline, regular-rational readback, Real seal handoff, and nonescape under BEDC.Derived.MiddleThirdCantorSetUp.}
  \constructivestory{A $\MiddleThirdCantorSetUp$ packet is generated from finite Cantor-prefix choices, retained ternary-stage rows, deleted-middle gap rows, rational endpoints, StreamName schedules, RegSeqRat readbacks, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data.}
\end{closurestatus}
