\chapter{A Concrete Naming Certificate for $\CompactUniformModulusFoldUp$}
\label{ch:concrete-instances-compact-uniform-modulus-fold-namecert}
\origin{human}

Following Bishop--Bridges constructive compactness and the finite-fold construction of \autoref{ch:visions-compact-uniform-continuity}, the $\CompactUniformModulusFoldUp$ packet records the concrete NameCert object behind the compact uniform-continuity modulus ledger. It sits after the compact source of \autoref{ch:concrete-instances-compact-namecert}, the continuous-map graph surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the metric distance rows of \autoref{ch:concrete-instances-metric-namecert}, the rational-order readback of \autoref{ch:concrete-instances-rat-namecert}, and the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet names a finite $\mathsf{ProbeBundle}$ fold; it does not select an external modulus, invoke open-cover compactness outside the displayed carrier, or import host function equality.

\begin{definition}[Compact uniform modulus fold carrier]
\label{def:compact-uniform-modulus-fold-carrier}
A $\CompactUniformModulusFoldUp$ carrier is a finite $\BHist$ packet
$$
U=(K,G,M,Q,F,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactUp$ source row exposing the located finite $\mathsf{ProbeBundle}$ net used by the fold.
\item $G$ is the $\ContinuousMapUp$ graph and pointwise-modulus row over the same metric source.
\item $M$ is the $\MetricUp$ source-and-target distance row, including the triangle and symmetry data consumed by the final estimate.
\item $Q$ is the $\RatUp$ positive-radius and finite lower-bound comparison row.
\item $F$ is the finite fold ledger over the $\mathsf{ProbeBundle}$ radius family, recording the accumulator, successor comparisons, and certified lower-bound output.
\item $L$ is the $\UniformModulusUp$ handoff row, consuming the folded lower bound as a tolerance-only precision ledger.
\item $H$ records componentwise $\hsame$ transport for compact source, continuous graph, metric distances, rational comparisons, fold ledger, uniform-modulus handoff, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a compact-uniform request through $K,G,M,Q,F,L,H$.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformModulusFoldUp$, $\CompactUp$, $\ContinuousMapUp$, $\MetricUp$, $\RatUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformModulusFoldUp}$ row.
\end{enumerate}
Accepted packets are compared only through $K,G,M,Q,F,L,H,C,P,N$ and have no coordinate for an external choice of modulus, arbitrary open-cover compactness, quotient function equality, completed function space, or ambient real completeness.
\end{definition}

\begin{theorem}[Compact uniform modulus fold NameCert obligations]
\label{thm:compact-uniform-modulus-fold-namecert-obligations}
For every accepted $\CompactUniformModulusFoldUp$ carrier $U=(K,G,M,Q,F,L,H,C,P,N)$, the local $\NameCert_{\CompactUniformModulusFoldUp}$ surface consists exactly of compact finite-net exposure, continuous-map pointwise-modulus exposure, metric source-and-target distance exposure, rational lower-bound readback, finite $\mathsf{ProbeBundle}$ fold replay, uniform-modulus handoff, componentwise transport, displayed replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-modulus-fold-carrier}. The compact source is $K$, the continuous graph is $G$, the metric distance support is $M$, and the rational comparison support is $Q$.

The fold row $F$ is downstream of those sources. It records primitive recursion over the displayed $\mathsf{ProbeBundle}$ radius family and keeps only certified lower-bound comparisons visible through $Q$.

The uniform-modulus row $L$ is then a consumer of the fold, not an external selector. The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route, so no arbitrary cover, host function equality, or completed function-space coordinate is exported.
\end{proof}

\begin{theorem}[Compact uniform modulus finite-fold handoff]
\label{thm:compact-uniform-modulus-fold-handoff}
Every $\UniformModulusUp$ consumer of an accepted $\CompactUniformModulusFoldUp$ packet must first read the compact finite-net row $K$, the continuous-map pointwise-modulus row $G$, the metric row $M$, the rational comparison row $Q$, and the finite fold ledger $F$. The handoff to $L$ therefore realizes the finite $\mathsf{ProbeBundle}$ construction of \autoref{thm:compact-uniform-finite-minimum-certificate} as NameCert data.
\end{theorem}
\begin{proof}
Begin with the carrier $U=(K,G,M,Q,F,L,H,C,P,N)$. The only finite cover data is in $K$, and the only pointwise modulus data is in $G$.

The rational fold cannot be read without metric and rational support. The row $M$ supplies the distance comparisons used by the compact-uniform triangle estimate, while $Q$ supplies the positive-radius order comparisons used by the fold.

Now read $F$. It is the finite ledger that lowers all bundled local radii to one precision history, matching the construction cited in \autoref{thm:compact-uniform-finite-minimum-certificate}. Only after that finite ledger is visible can $L$ export the tolerance-only $\UniformModulusUp$ row.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactUniformModulusFoldUp$ uniform-modulus read must expose Compact, ContinuousMap, Metric, Rat, finite ProbeBundle fold, UniformModulus, hsame, Cont, Pkg, and local NameCert rows.}

\independenceWitness{compact, continuousmap, metric, rat, uniformmodulus: $\CompactUniformModulusFoldUp$ is not bijective with any listed sibling because it jointly carries compact finite-net rows, continuous pointwise-modulus rows, metric triangle support, rational lower-bound fold data, UniformModulus handoff, transport, replay, provenance, and local naming.}

\closureat{CompactUniformModulusFoldUp}{seedStr}

\begin{closurestatus}{\CompactUniformModulusFoldUp}
  \constructivestory{A $\CompactUniformModulusFoldUp$ packet is generated from a Compact finite-net row, a ContinuousMap pointwise-modulus row, Metric distance rows, Rat positive-radius comparisons, a finite ProbeBundle fold ledger, a UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-modulus-fold-carrier}, \autoref{thm:compact-uniform-modulus-fold-namecert-obligations}, and \autoref{thm:compact-uniform-modulus-fold-handoff} seed the finite compact-uniform modulus fold route over Compact, ContinuousMap, Metric, Rat, ProbeBundle fold, and UniformModulus rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, arbitrary open-cover compactness, external modulus selection, host function equality, completed function spaces, ambient Real completeness, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net exposure, continuous pointwise-modulus exposure, metric-triangle compatibility, Rat lower-bound fold exactness, UniformModulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformModulusFoldUp.}
\end{closurestatus}
