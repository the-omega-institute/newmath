\chapter{A Concrete Naming Certificate for $\RecursiveFnUp$}
\label{ch:concrete-instances-recursivefn-namecert}

The partial-recursive interface $\RecursiveFnUp$ packages partial functions $\NatUp \to \NatUp$ generated by zero, successor, projection, composition, primitive recursion, and bounded minimisation. It depends on $\NameCert_{\NatUp}$ (\autoref{ch:concrete-instances-nat-namecert}), $\NameCert_{\ListUp}$ (\autoref{ch:concrete-instances-list-namecert}), and $\NameCert_{\ComputableUp}$ (\autoref{ch:concrete-instances-computable-namecert}).

\section{Carrier definition}
\label{sec:recursivefn-carrier}

A $\RecursiveFnUp$ token carries an inductive constructor history of generation steps: zero, successor, projection $\pi^k_i$, composition row, primitive-recursion row with explicit base and step subterms, and bounded-minimisation row with an explicit unary bound. The carrier accepts a partial-graph value $F(n,m)$ when the constructor-history evaluation on the $\NatUp$-input row $n$ returns $\NatUp$-output $m$ within the displayed bound, and the evaluation trace is a $\SeqUp$ object. The classifier is $\hsame$ on output rows after the constructor history is fixed; the ledger keeps the constructor sequence, intermediate evaluation rows, and bounded-minimisation witness visible.

\begin{closurestatus}{\RecursiveFnUp}
  \constructivestory{A $\RecursiveFnUp$ token names a finite constructor history built from zero, successor, projection $\pi^k_i$, composition, primitive recursion with base/step subterms, and bounded minimisation with a displayed unary bound. The carrier accepts a graph row $F(n,m)$ when evaluating the constructor history on the $\NatUp$-input row $n$ over a $\SeqUp$ trace yields $\NatUp$-output $m$ within the displayed bound. Classifier control is $\hsame$ on outputs once the constructor history is fixed; the source covers $\NatUp$ inputs and outputs, constructor histories, and $\SeqUp$ evaluation traces; the pattern records constructor-history admissibility, composition row, primitive-recursion base/step row, and bounded-minimisation row; the ledger keeps the constructor sequence, intermediate evaluation rows, and minimisation witness visible. The five name-certificate obligations follow from the inductive constructor-history evaluation: the inhabited identity certificate is the zero constructor; classifier stability is reflexive, symmetric, and transitive $\hsame$ on outputs; transport repacks evaluations through composition and recursion; ledger exactness identifies the graph row with the displayed evaluation; the extension boundary fixes the bounded-minimisation rule. A history $h$ lies in the signature gap of a $\RecursiveFnUp$ token $p$ when the source-domain accepts a constructor-history evaluation, the signature relation pairs it with the graph output, and token introduction names that output as $p$. The chapter registers this finite-constructor-history surface under the name $\RecursiveFnUp$; it is a partial-recursive function object with no general $\mu$-recursion totality claim, no oracle, no Church-Turing bridge, and no standard recursion-theory library.}
  \theoryclosure{\seedClosure}
  \scopeclosed{The seed scope binds the constructor history of zero, successor, projection, composition, primitive recursion, and bounded minimisation; the $\SeqUp$ evaluation trace; and the $\NatUp$-typed input/output rows. Unbounded $\mu$-recursion and oracle queries are excluded.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{The chapter does not close unbounded $\mu$-recursion totality, the Church-Turing thesis, oracle recursive functions, or a standard recursion-theory bridge.}
  \upgradepath{Obligation closure requires a checked $\RecursiveFnUp$ constructor-history evaluation obligation theorem covering composition and primitive-recursion compatibility on $\NatUp$ inputs.}
\end{closurestatus}
