\chapter{A Concrete Naming Certificate for $\StepIndexedTotalHostUp$}
\label{ch:concrete-instances-step-indexed-total-host-namecert}
\origin{ai}

The $\StepIndexedTotalHostUp$ packet is the concrete NameCert object for the boundary between a total host and a universal substrate in \autoref{ch:visions-strong-normalization-vs-turing-completeness}. It sits beside the Turing-machine substrate of \autoref{ch:concrete-instances-turingmachine-namecert}, the bounded universal trace of \autoref{ch:concrete-instances-bounded-universal-trace-namecert}, the normal-form host packet of \autoref{ch:concrete-instances-znormal-namecert}, and the closed-normal consistency boundary of \autoref{ch:concrete-instances-closed-normal-consistency-boundary-namecert}. Its distinguishing datum is the simultaneous presence of a total host row, a unary fuel row, a finite universal-substrate trace row, and a refusal ledger preventing an unbounded substrate run from being reread as host computation.

\falsifiablePrediction{Deleting the fuel row permits an unbounded substrate trace to masquerade as host computation; the local classifier must reject that packet because the host-side read no longer passes through a displayed finite fuel index.}

\independenceWitness{turingmachine, boundeduniversaltrace, znormal, closednormalconsistency: none is bijective because each sibling omits at least one of the fuel-indexed host boundary, the substrate trace, or the refusal ledger.}

\begin{definition}[StepIndexedTotalHost carrier]
\label{def:step-indexed-total-host-carrier}
A $\StepIndexedTotalHostUp$ carrier is a finite $\BHist$ packet
$$
H=(A,F,R,N,Z,B,Hs,C,P,K)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the total host term or evaluator row, read as a BEDC host computation whose reduction is accepted only through finite data;
\item $F$ is the unary fuel row naming the finite host-visible step index;
\item $R$ is the finite substrate trace row, such as a Rule~110 row produced by \texttt{r110\_run\_n\_steps} or a Turing-machine trace row with the same displayed bound;
\item $N$ is the host normal-form boundary row, read through the finite normal-form discipline of $\ZNormalUp$;
\item $Z$ is the bounded universal trace witness row, read through $\BoundedUniversalTraceUp$ and tied to the same fuel row $F$;
\item $B$ is the refusal ledger for halting transfer, partial evaluator transfer, missing fuel, detached substrate trace, and host-equality claims not carried by the packet;
\item $Hs$ records componentwise $\hsame$ transports for $A,F,R,N,Z,B,P,K$;
\item $C$ records displayed $\Cont$ routes from host row to fuel, from fuel to finite substrate trace, from trace to bounded readback, from bounded readback to normal-form boundary, and from each absent semantic claim to the refusal ledger;
\item $P$ is the $\Pkg$ provenance over $\StepIndexedTotalHostUp$, $\TuringMachineUp$, $\BoundedUniversalTraceUp$, $\ZNormalUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $K$ is the local $\NameCert_{\StepIndexedTotalHostUp}$ row.
\end{enumerate}
The local classifier compares accepted packets only through $A,F,R,N,Z,B,Hs,C,P,K$. It has no coordinate for unbounded halting, an infinite Rule~110 orbit, host evaluator equality, quotient behaviour, or a proof that substrate universality transfers to the total host.
\end{definition}
\leanchecked{BEDC.Derived.StepIndexedTotalHostUp.StepIndexedTotalHostTasteGate\_single\_carrier\_alignment}

\begin{theorem}[StepIndexedTotalHost NameCert obligations]
\label{thm:step-indexed-total-host-namecert-obligations}
For the carrier of \autoref{def:step-indexed-total-host-carrier}, the local $\NameCert_{\StepIndexedTotalHostUp}$ surface consists exactly of carrier admission for
$$
H=(A,F,R,N,Z,B,Hs,C,P,K),
$$
total-host exposure through $A$, unary fuel exposure through $F$, finite substrate-trace exposure through $R$, host normal-form boundary exposure through $N$, bounded universal trace witness exposure through $Z$, refusal-ledger exactness through $B$, componentwise transport through $Hs$, displayed continuation replay through $C$, package provenance through $P$, and local naming through $K$.
\end{theorem}
\leanchecked{BEDC.Derived.StepIndexedTotalHostUp.StepIndexedTotalHostCarrier\_namecert\_obligations}
\begin{proof}
Project the carrier of \autoref{def:step-indexed-total-host-carrier}. Its visible rows are exactly $A,F,R,N,Z,B,Hs,C,P,K$, so every public read must be a projection from those rows or a route replaying them.

The host-facing side is read through $A,F,N$. The row $A$ names the total host term or evaluator row, $F$ supplies the finite fuel index, and $N$ records the normal-form boundary available to the host.

The substrate-facing side is read through $R,Z$. The trace row $R$ records a finite Rule~110 or Turing-machine prefix, while $Z$ ties that prefix to the bounded universal trace witness at the same fuel index.

Finally $B,Hs,C,P,K$ bind the refusal and NameCert surface. Since the carrier has no coordinate for unbounded halting, infinite orbit data, host evaluator equality, quotient behaviour, or transfer of substrate universality to host totality, the listed rows exhaust the obligations.
\end{proof}

\begin{theorem}[StepIndexedTotalHost separation]
\label{thm:step-indexed-total-host-separation}
Every accepted $\StepIndexedTotalHostUp$ packet separates the total-host read from the universal-substrate run by the finite route
$$
A,F,R,Z,N,B,Hs,C,P,K .
$$
A consumer may read a host computation at fuel $F$, a finite substrate trace $R$, a bounded witness $Z$, and a normal-form boundary $N$. It may not read an unbounded substrate trace as host computation, transfer halting or partial evaluator behaviour across the boundary, or replace the refusal ledger $B$ by a semantic equality between the host and the substrate.
\end{theorem}
\begin{proof}
Begin with the finite route row $C$. Its admissible paths pass from the host row $A$ to the fuel row $F$, then to the displayed substrate trace $R$ and bounded witness $Z$, and only then to the host normal-form boundary $N$.

The fuel row is essential. If $F$ is absent, the packet still may display a substrate trace schema, but the host has no finite step index under which to read it as a total computation. The refusal ledger $B$ therefore rejects the packet rather than accepting an unbounded run as host data.

The same ledger blocks semantic transfer. Halting transfer, partial evaluator transfer, infinite orbit claims, host evaluator equality, and quotient behaviour require coordinates absent from $A,F,R,N,Z,B,Hs,C,P,K$.

Thus the packet preserves both sides of \autoref{ch:visions-strong-normalization-vs-turing-completeness}: the substrate may carry universal finite prefixes, while the host reads only fuel-indexed finite data.
\end{proof}
\leanchecked{BEDC.Derived.StepIndexedTotalHostUp.StepIndexedTotalHostCarrier\_separation}

\begin{theorem}[StepIndexedTotalHost sibling boundary]
\label{thm:step-indexed-total-host-sibling-boundary}
The siblings $\TuringMachineUp$, $\BoundedUniversalTraceUp$, $\ZNormalUp$, and the closed-normal consistency boundary are comparison anchors for $\StepIndexedTotalHostUp$, but none is bijective to it. $\TuringMachineUp$ supplies machine substrate rows without the host normal-form boundary; $\BoundedUniversalTraceUp$ supplies a bounded trace without the full total-host refusal surface; $\ZNormalUp$ supplies host normality without the universal-substrate trace; and the closed-normal consistency boundary supplies a normal-form consistency node without the step-indexed substrate witness.
\end{theorem}
\begin{proof}
Read the siblings one at a time. The Turing-machine surface names finite machine data, but it does not carry the host row $A$, the normal-form boundary $N$, or the refusal ledger $B$ for total-host transfer.

The bounded universal trace supplies the rows closest to $F,R,Z$. It still lacks the full packet route from host term to normal-form boundary and the refusal ledger blocking host-substrate semantic identification.

The normal-form and closed-normal consistency siblings point in the other direction. They account for finite normality and consistency-facing boundaries, but they do not carry the Rule~110 or Turing-machine substrate trace tied to the same fuel row.

Consequently each sibling can be cited as a component or comparison anchor, while the $\StepIndexedTotalHostUp$ packet remains the concrete boundary object that stores all rows together.
\end{proof}
\leanchecked{BEDC.Derived.StepIndexedTotalHostUp.StepIndexedTotalHostCarrier\_bounded\_sibling\_boundary}

\begin{theorem}[StepIndexedTotalHost obligation surface]
\label{thm:step-indexed-total-host-obligation-surface}
The fuel-indexed total-host packet has exactly the obligations of total-host row exposure through $A$, unary fuel exposure through $F$, finite substrate trace exposure through $R$, normal-form boundary exposure through $N$, bounded universal trace witness exposure through $Z$, refusal-ledger exactness through $B$, componentwise $\hsame$ transport through $Hs$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local $\NameCert_{\StepIndexedTotalHostUp}$ through $K$. Every host/substrate comparison must pass through the same fuel row $F$ before any normal-form read or bounded universal trace read is accepted.
\end{theorem}
\leanchecked{BEDC.Derived.StepIndexedTotalHostUp.StepIndexedTotalHostCarrier\_obligation\_surface}
\begin{proof}
Start with the carrier and NameCert obligations, \autoref{def:step-indexed-total-host-carrier} and \autoref{thm:step-indexed-total-host-namecert-obligations}. They list the total-host row, unary fuel row, finite substrate trace, normal-form boundary, bounded universal trace witness, refusal ledger, transport, replay, provenance, and local name as the complete obligation surface.

Use the separation theorem. It routes every host-facing and substrate-facing read through $A,F,R,Z,N,B,Hs,C,P,K$, so the fuel row $F$ is the shared boundary before either the normal-form row $N$ or bounded universal trace row $Z$ can be inspected.

Apply the sibling boundary theorem. The Turing-machine, bounded universal trace, normal-form, and closed-normal consistency siblings can each provide comparison data, but none replaces the displayed fuel row or the refusal ledger that keeps host and substrate readings separated.
\end{proof}

The Lean \texttt{TasteGate.lean} development for $\StepIndexedTotalHostUp$ must include explicit $\mathsf{Nontrivial}\ \StepIndexedTotalHostUp$ and $\mathsf{FieldFaithful}\ \StepIndexedTotalHostUp$ instances. These instances must preserve the total host row, unary fuel row, finite substrate trace, normal-form boundary, bounded universal trace witness, refusal ledger, componentwise transports, displayed routes, package provenance, and local naming row.

\begin{theorem}[StepIndexedTotalHost scoped-kernel route]
\label{thm:step-indexed-total-host-scoped-kernel-route}
Every accepted $\StepIndexedTotalHostUp$ scoped-kernel read factors through $\BHist$, $\hsame$, $\Cont$, $\BoundedUniversalTraceUp$, $\TypedReductionNormalFormUp$, the displayed $\StepIndexedTotalHostUp$ carrier rows, $\Pkg$ provenance, and $\NameCert$. The route is finite: it exposes carrier admission, fuel-indexed trace rows, host normal-form boundary, bounded universal trace witness, refusal-ledger exactness, nontriviality, and field faithfulness for the same packet, but it does not assert host evaluator equality, kernel consistency, unbounded halting transfer, or substrate universality for the total host.
\end{theorem}
\leanchecked{BEDC.Derived.StepIndexedTotalHostUp.StepIndexedTotalHostCarrier\_scoped\_kernel\_route}
\begin{proof}
Start with the carrier and NameCert obligations, \autoref{def:step-indexed-total-host-carrier} and \autoref{thm:step-indexed-total-host-namecert-obligations}. They expose the total host row, unary fuel row, finite substrate trace, normal-form boundary, bounded universal trace witness, refusal ledger, componentwise transports, route replay, provenance, and local naming row.

Apply separation and the sibling boundary theorem, \autoref{thm:step-indexed-total-host-separation} and \autoref{thm:step-indexed-total-host-sibling-boundary}. These route the substrate side through $\BoundedUniversalTraceUp$ and the host boundary through $\TypedReductionNormalFormUp$ before either side can be compared.

Finally use the obligation surface theorem. It keeps the packet inside the same finite $A,F,R,N,Z,B,Hs,C,P,K$ rows, with nontriviality and field faithfulness read as obligations of that displayed carrier rather than as ambient semantic equalities. Hence the scoped route has no coordinate for host evaluator equality, kernel consistency, unbounded halting transfer, or total-host universality.
\end{proof}

\begin{theorem}[StepIndexedTotalHost public interface]
\label{thm:step-indexed-total-host-public-interface}
The exported public interface of an accepted $\StepIndexedTotalHostUp$ packet is exactly the fuel-indexed host/substrate boundary carried by $A,F,R,N,Z,B,Hs,C,P,K$. A public consumer may read the total-host row, the shared unary fuel row, the finite substrate trace, the host normal-form boundary, the bounded universal trace witness, the refusal ledger, and the displayed transport, replay, provenance, and local naming rows. The interface does not export host evaluator equality, kernel consistency, unbounded halting transfer, partial evaluator semantics, quotient behaviour, or substrate universality for the total host.
\end{theorem}
\leanchecked{BEDC.Derived.StepIndexedTotalHostUp.StepIndexedTotalHostCarrier\_public\_interface}
\begin{proof}
Start with the scoped-kernel route of \autoref{thm:step-indexed-total-host-scoped-kernel-route}. It already factors every accepted read through the carrier rows, the bounded universal trace boundary, the normal-form host boundary, and the local NameCert surface.

Apply the obligation surface and separation theorems, \autoref{thm:step-indexed-total-host-obligation-surface} and \autoref{thm:step-indexed-total-host-separation}. These identify $A,F,R,N,Z,B,Hs,C,P,K$ as the only rows through which a host/substrate comparison may pass, with $F$ as the shared finite boundary.

Finally use the sibling boundary of \autoref{thm:step-indexed-total-host-sibling-boundary}. The Turing-machine, bounded-trace, normal-form, and closed-normal siblings can be cited as anchors, but none supplies the missing semantic endpoints. Hence the public interface exports the displayed finite packet and nothing beyond it.
\end{proof}

\begin{theorem}[StepIndexedTotalHost bridge interface]
\label{thm:step-indexed-total-host-bridge-interface}
The public fuel-indexed $\StepIndexedTotalHostUp$ interface bridges the total-host read and the bounded universal trace read exactly through
$$
A,F,R,N,Z,B,Hs,C,P,K .
$$
A consumer may transport the host row $A$ to the shared fuel row $F$, replay the finite substrate trace $R$, read the bounded universal witness $Z$, and return to the normal-form host boundary $N$. The bridge does not export unbounded halting, partial evaluator transfer, host evaluator equality, quotient behaviour, or a proof that substrate universality is a total-host property.
\end{theorem}
\leanchecked{BEDC.Derived.StepIndexedTotalHostUp.StepIndexedTotalHostCarrier\_bridge\_interface}
\begin{proof}
Start with the public interface theorem, \autoref{thm:step-indexed-total-host-public-interface}. It exposes exactly the rows $A,F,R,N,Z,B,Hs,C,P,K$ and no semantic endpoint outside the finite packet.

Use the separation and obligation-surface theorems, \autoref{thm:step-indexed-total-host-separation} and \autoref{thm:step-indexed-total-host-obligation-surface}. They force host/substrate comparison through the shared fuel row $F$ before either the bounded universal trace witness $Z$ or the normal-form boundary $N$ is read.

Finally apply the sibling boundary. The Turing-machine, bounded universal trace, normal-form, and closed-normal consistency chapters provide bridge anchors, while the refusal ledger $B$ blocks the absent unbounded and equality claims. Hence the public interface is a bridge between the displayed finite reads, not an export of stronger host or substrate semantics.
\end{proof}

\closureat{StepIndexedTotalHostUp}{seedStr}
\closureat{StepIndexedTotalHostUp}{obligationStr}[BEDC.Derived.StepIndexedTotalHostUp.StepIndexedTotalHostTasteGate\_single\_carrier\_alignment]
\closureat{StepIndexedTotalHostUp}{scopedStr}[BEDC.Derived.StepIndexedTotalHostUp.StepIndexedTotalHostTasteGate\_single\_carrier\_alignment]
\closureat{StepIndexedTotalHostUp}{publicStr}[BEDC.Derived.StepIndexedTotalHostUp.StepIndexedTotalHostTasteGate\_single\_carrier\_alignment]
\closureat{StepIndexedTotalHostUp}{bridgedStr}
\closureat{StepIndexedTotalHostUp}{matureStr}[BEDC.Derived.StepIndexedTotalHostUp.StepIndexedTotalHostCarrier\_bridge\_interface]

\begin{closurestatus}{\StepIndexedTotalHostUp}
  \constructivestory{A $\StepIndexedTotalHostUp$ packet is generated from a total host term or evaluator row, unary fuel row, finite substrate trace row, normal-form boundary row, bounded universal trace witness row, refusal ledger, componentwise hsame transports, displayed Cont routes, package provenance, and a local NameCert row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{def:step-indexed-total-host-carrier}, \autoref{thm:step-indexed-total-host-namecert-obligations}, \autoref{thm:step-indexed-total-host-separation}, \autoref{thm:step-indexed-total-host-sibling-boundary}, \autoref{thm:step-indexed-total-host-obligation-surface}, \autoref{thm:step-indexed-total-host-scoped-kernel-route}, \autoref{thm:step-indexed-total-host-public-interface}, and \autoref{thm:step-indexed-total-host-bridge-interface} specify the mature fuel-indexed total-host and universal-substrate boundary through the checked bridge-interface theorem.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not prove Lean verification, bridge checking, unbounded halting, partial evaluator semantics, host evaluator equality, quotient behaviour, or transfer of substrate universality to the total host.}
  \upgradepath{The mature paper scope is the fuel-indexed total-host/substrate boundary through the checked bridge interface; Lean verification remains an independent formal-axis obligation.}
\end{closurestatus}
