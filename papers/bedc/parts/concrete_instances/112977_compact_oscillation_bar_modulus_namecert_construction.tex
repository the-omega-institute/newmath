\chapter{A Concrete Naming Certificate for $\CompactOscillationBarModulusUp$}
\label{ch:concrete-instances-compact-oscillation-bar-modulus-namecert}
\origin{human}

Following Bishop-style compact metric finite-net arguments, the $\CompactOscillationBarModulusUp$ packet realizes the compact-uniform-continuity route of \autoref{ch:visions-compact-uniform-continuity} as a finite bar-modulus NameCert carrier. It reads compact metric finite nets from \autoref{ch:concrete-instances-compactmetric-namecert}, pointwise oscillation rows from \autoref{ch:concrete-instances-continuousmap-namecert}, dyadic tolerances from \autoref{ch:concrete-instances-dyadicratcore-namecert}, rational lower-bound folds from \autoref{ch:concrete-instances-rat-namecert}, and the downstream uniform-modulus ledger of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet records the bar-modulus handoff without importing open-cover compactness, semantic continuity, selected centers outside the finite bundle, or ambient completeness.

\begin{definition}[Compact fan-bar route]
\label{def:compact-fan-bar-route}
A $\CompactOscillationBarModulusUp$ carrier is a finite $\BHist$ packet
$$
O=(K,F,D,R,B,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net row, carrying the bundled centers and located coverage histories inspected by the compact request.
\item $F$ is the $\ContinuousMapUp$ pointwise oscillation row, recording the local radius at each bundled center and the target oscillation estimate used there.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, exposing the dyadic scale of the requested oscillation bound and the source-radius cuts read by the local estimates.
\item $R$ is the $\RatUp$ lower-bound fold row, recording the positive finite minimum over the displayed local radius histories.
\item $B$ is the compact bar row, the finite $\mathsf{ProbeBundle}$ recursion surface that orders the centers, local radii, dyadic cuts, and rational fold.
\item $U$ is the $\UniformModulusUp$ handoff row, exported only after $K,F,D,R,B$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the compact, oscillation, dyadic, rational-fold, bar, uniform-modulus, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a tolerance request through the compact net, pointwise oscillation rows, dyadic tolerance, rational lower-bound fold, compact bar, and uniform-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\CompactOscillationBarModulusUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\DyadicRatCoreUp$, $\RatUp$, $\UniformModulusUp$, $\mathsf{ProbeBundle}$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactOscillationBarModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,F,D,R,B,U,H,C,P,N$. It has no coordinate for host open-cover compactness, a semantic continuity theorem, quotient point equality, a choice-selected center outside the displayed bar, or an external modulus selector.
\end{definition}

\begin{theorem}[Compact fan-bar modulus extraction]
\label{thm:compact-fan-bar-modulus-extraction}
For every accepted $\CompactOscillationBarModulusUp$ carrier
$$
O=(K,F,D,R,B,U,H,C,P,N),
$$
the local $\NameCert_{\CompactOscillationBarModulusUp}$ surface extracts a uniform-modulus row exactly through compact finite-net admission for $K$, pointwise oscillation admission for $F$, dyadic tolerance visibility for $D$, rational lower-bound fold exactness for $R$, finite bar recursion for $B$, downstream handoff for $U$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, and local naming through $N$.

Consequently every $\UniformModulusUp$ consumer receives a tolerance-only precision row through the finite route
$$
\CompactMetricUp,\ \ContinuousMapUp,\ \DyadicRatCoreUp,\ \RatUp,\ \CompactOscillationBarModulusUp,\ \UniformModulusUp .
$$
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-fan-bar-route}. The rows $K,F,D,R,B,U,H,C,P,N$ are the whole accepted packet, so every exported uniform-modulus read must pass through them.

The compact row $K$ and the bar row $B$ supply the finite $\mathsf{ProbeBundle}$ of centers. The pointwise oscillation row $F$ attaches a local source radius and a target oscillation estimate to each displayed center.

The dyadic row $D$ fixes the tolerance scale. The rational fold row $R$ then records a positive lower bound over the finitely many local radius histories, producing a displayed precision history rather than an external selector.

The handoff row $U$ receives only that folded precision and the visible oscillation estimates. Transport, replay, provenance, and local naming through $H,C,P,N$ preserve the same finite route.

Since the carrier has no coordinate for host open-cover compactness, semantic continuity, quotient point equality, selected centers outside the bar, or ambient completeness, the extracted uniform-modulus row cannot import those objects.
\end{proof}

\begin{theorem}[Compact oscillation bar consumer boundary]
\label{thm:compact-oscillation-bar-consumer-boundary}
Every consumer of an accepted $\CompactOscillationBarModulusUp$ packet reads compact-uniform-continuity data as a finite oscillation bar. The consumer may inspect the compact finite net, pointwise oscillation rows, dyadic tolerance ledger, rational lower-bound fold, compact bar recursion, uniform-modulus handoff, transport, replay, provenance, and local naming rows, and nothing else.
\end{theorem}
\begin{proof}
The consumer begins with the accepted carrier. By \autoref{def:compact-fan-bar-route}, the only analytic rows are $K,F,D,R,B,U$ and the only structural rows are $H,C,P,N$.

Reading $K$ and $B$ exposes the finite bar. Reading $F$ attaches local oscillation control to each center, while $D$ and $R$ convert the requested dyadic tolerance into a displayed lower-bound precision.

The row $U$ is downstream of those data and is the only exported uniform-modulus coordinate. The structural rows do not add compactness, continuity, quotient, choice, or completeness objects; they only transport, replay, source, and name the same packet.
\end{proof}

\closureat{CompactOscillationBarModulusUp}{seedStr}

\begin{closurestatus}{\CompactOscillationBarModulusUp}
  \constructivestory{A $\CompactOscillationBarModulusUp$ packet is generated from CompactMetric finite-net data, ContinuousMap pointwise oscillation rows, a DyadicRatCore tolerance row, a RatUp lower-bound fold, finite ProbeBundle bar recursion, a UniformModulusUp handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-fan-bar-route}, \autoref{thm:compact-fan-bar-modulus-extraction}, and \autoref{thm:compact-oscillation-bar-consumer-boundary} seed the compact oscillation bar route from finite compact nets and local oscillation rows to UniformModulusUp consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close host open-cover compactness, semantic continuity, quotient point equality, choice-selected centers outside the displayed bar, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked compact finite-net admission, pointwise oscillation admission, dyadic tolerance visibility, rational lower-bound fold exactness, finite bar recursion, uniform-modulus handoff, transport stability, replay stability, and non-escape for BEDC.Derived.CompactOscillationBarModulusUp.}
\end{closurestatus}
