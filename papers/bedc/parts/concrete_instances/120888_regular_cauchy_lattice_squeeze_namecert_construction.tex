\chapter{A Concrete Naming Certificate for $\RegularCauchyLatticeSqueezeUp$}
\label{ch:concrete-instances-regular-cauchy-lattice-squeeze-namecert}
\origin{human}

Following Bishop and Bridges, the $\RegularCauchyLatticeSqueezeUp$ packet records the finite squeeze route that ties regular Cauchy lattice operations to the terminal $\RealUp$ seal without using host trichotomy, quotient streams, or a selected limit. It sits beside the minimum and maximum surfaces of \autoref{ch:concrete-instances-regularcauchymin-namecert} and \autoref{ch:concrete-instances-regularcauchymax-namecert}, the lattice-operation surface of \autoref{ch:concrete-instances-regular-cauchy-lattice-operations-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation surface of \autoref{ch:concrete-instances-streamname-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a constructive squeeze certificate, not a host order theorem, ambient completeness claim, or quotient-real comparison.

\begin{definition}[RegularCauchyLatticeSqueeze carrier]
\label{def:regular-cauchy-lattice-squeeze-carrier}
A $\RegularCauchyLatticeSqueezeUp$ carrier is a finite $\BHist$ packet
$$
S=(L,U,W,D,\ell,u,R,E,H,C,P,N)
$$
with the following rows:
\begin{enumerate}
\item $L$ is the lower-window row, carrying the displayed minimum route read from the accepted regular Cauchy lattice surface.
\item $U$ is the upper-window row, carrying the displayed maximum route read from the same finite surface.
\item $W$ is the shared $\StreamNameUp$ observation row exposing the finite windows used by both lattice routes.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger recording the displayed radius budget and comparison scale.
\item $\ell$ is the lower-route exactness row, forcing the lower window to pass through the same finite squeeze route as the minimum surface.
\item $u$ is the upper-route exactness row, forcing the upper window to pass through the same finite squeeze route as the maximum surface.
\item $R$ is the terminal $\RegSeqRatUp$ readback row for the common squeeze route.
\item $E$ is the terminal $\RealUp$ seal row, available only after $R$ has been displayed.
\item $H$ records componentwise $\hsame$ transport for the lower and upper routes, the shared observation row, dyadic tolerance, exactness rows, readback, seal, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from the squeeze request through $L,U,W,D,\ell,u,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyLatticeSqueezeUp$, $\RegularCauchyMinimumUp$, $\RegularCauchyMaximumUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyLatticeSqueezeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,U,W,D,\ell,u,R,E,H,C,P,N$. It has no coordinate for host trichotomy, quotient stream equality, selected limits, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[RegularCauchyLatticeSqueeze NameCert obligations]
\label{thm:regular-cauchy-lattice-squeeze-namecert-obligations}
For every accepted $\RegularCauchyLatticeSqueezeUp$ carrier $S=(L,U,W,D,\ell,u,R,E,H,C,P,N)$, the local $\NameCert_{\RegularCauchyLatticeSqueezeUp}$ surface consists exactly of carrier admission for the lower-window row, upper-window row, shared StreamName observation row, DyadicRatCore tolerance ledger, lower-route exactness row, upper-route exactness row, RegSeqRat readback row, Real seal row, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the accepted carrier. The rows $L,U,W,D,\ell,u,R,E,H,C,P,N$ are the entire public inventory of the squeeze packet.

The lower and upper routes are displayed first. The lower route row $\ell$ and the upper route row $u$ force the shared observation row $W$ and dyadic tolerance row $D$ to be read before the common readback row $R$ is available.

The readback row $R$ is downstream of the same displayed squeeze route. The structural rows $H,C,P,N$ transport, replay, source, and name exactly that finite route. Since the carrier contains no coordinate for trichotomy, quotient stream equality, selected limits, or ambient completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[RegularCauchyLatticeSqueeze lower-window route]
\label{thm:regular-cauchy-lattice-squeeze-lower-window-route}
Let $S=(L,U,W,D,\ell,u,R,E,H,C,P,N)$ be an accepted $\RegularCauchyLatticeSqueezeUp$ carrier. Any lower-window consumer must read the finite route
$$
L \longmapsto \ell \longmapsto W \longmapsto D \longmapsto R \longmapsto E .
$$
Thus the lower-window route exports only the displayed lower-window row, the lower-route exactness row, the shared observation row, the dyadic tolerance ledger, the regular readback, and the terminal real seal.
\end{theorem}
\begin{proof}
Start with \autoref{thm:regular-cauchy-lattice-squeeze-namecert-obligations}. The lower-window row is available only after $L$ has been displayed, and the exactness row $\ell$ is the next lower-route commitment.

The shared observation row $W$ and dyadic ledger $D$ are then consumed before the readback row $R$ appears. The seal row $E$ remains downstream of that lower route, so no host trichotomy, quotient comparison, or selected limit enters the packet.
\end{proof}

\begin{theorem}[RegularCauchyLatticeSqueeze upper-window route]
\label{thm:regular-cauchy-lattice-squeeze-upper-window-route}
Let $S=(L,U,W,D,\ell,u,R,E,H,C,P,N)$ be an accepted $\RegularCauchyLatticeSqueezeUp$ carrier. Any upper-window consumer must read the finite route
$$
U \longmapsto u \longmapsto W \longmapsto D \longmapsto R \longmapsto E .
$$
Thus the upper-window route exports only the displayed upper-window row, the upper-route exactness row, the shared observation row, the dyadic tolerance ledger, the regular readback, and the terminal real seal.
\end{theorem}
\begin{proof}
Start with the same NameCert obligations. The upper-window row is available only after $U$ has been displayed, and the exactness row $u$ is the next upper-route commitment.

The shared observation row $W$ and dyadic ledger $D$ are then consumed before the readback row $R$ appears. The seal row $E$ remains downstream of that upper route, so no quotient stream equality or ambient completeness is exported by the packet.
\end{proof}

\begin{theorem}[RegularCauchyLatticeSqueeze RealUp-seal nonescape]
\label{thm:regular-cauchy-lattice-squeeze-realup-seal-nonescape}
Let $S=(L,U,W,D,\ell,u,R,E,H,C,P,N)$ be an accepted $\RegularCauchyLatticeSqueezeUp$ carrier. Every $\RealUp$ consumer reaches the seal $E$ only through the displayed lower-window route and upper-window route together with the common readback row $R$. The consumer therefore receives no host trichotomy, quotient stream equality, selected limit, or ambient $\RealUp$ completeness coordinate.
\end{theorem}
\begin{proof}
Apply the lower-window route theorem and the upper-window route theorem. Both force the consumer through the same shared observation row, dyadic tolerance ledger, and regular readback before the seal row is available.

The structural rows $H,C,P,N$ preserve and name that shared route. Since the carrier has no excluded completeness coordinate, the terminal seal is a finite readback boundary rather than a host-order or quotient argument.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyLatticeSqueezeUp$ read displays one lower-window row, one upper-window row, one shared $\StreamNameUp$ observation row, one $\DyadicRatCoreUp$ tolerance ledger, lower-route exactness, upper-route exactness, one $\RegSeqRatUp$ readback row, one $\RealUp$ seal row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ evidence before a minimum, maximum, or Real-facing consumer may read the squeeze route.}

\independenceWitness{regularcauchymin, regularcauchymax, regseqrat, dyadicratcore, streamname, real: $\RegularCauchyLatticeSqueezeUp$ is not bijective to any listed sibling because it jointly carries the lower-window, upper-window, shared observation, dyadic tolerance, lower exactness, upper exactness, regular readback, real sealing, transport, replay, provenance, and local naming rows.}

\closureat{RegularCauchyLatticeSqueezeUp}{seedStr}

\begin{closurestatus}{\RegularCauchyLatticeSqueezeUp}
  \constructivestory{A $\RegularCauchyLatticeSqueezeUp$ packet is generated from lower-window and upper-window rows, a shared StreamName observation row, a DyadicRatCore tolerance ledger, lower-route and upper-route exactness rows, a RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-lattice-squeeze-carrier}, \autoref{thm:regular-cauchy-lattice-squeeze-namecert-obligations}, \autoref{thm:regular-cauchy-lattice-squeeze-lower-window-route}, \autoref{thm:regular-cauchy-lattice-squeeze-upper-window-route}, and \autoref{thm:regular-cauchy-lattice-squeeze-realup-seal-nonescape} seed the finite squeeze route over minimum, maximum, StreamName, DyadicRatCore, RegSeqRat, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host trichotomy, quotient stream equality, selected limits, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, lower-route exactness, upper-route exactness, route stability, provenance, and non-escape for the regular squeeze TasteGate target.}
\end{closurestatus}
