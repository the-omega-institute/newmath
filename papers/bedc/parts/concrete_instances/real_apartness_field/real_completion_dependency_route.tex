Every $\RealApartnessFieldUp$ carrier reads its terminal real-name dependency through the finite L10 completion route, not through an ambient field of host reals. The route passes through the $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}, the $\RegSeqRatUp$ regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the $\DyadicRatCoreUp$ tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the $\StreamNameUp$ finite-window surface of \autoref{ch:concrete-instances-streamname-namecert}.

\begin{proposition}[Real apartness field finite completion dependency route]
\label{prop:real-apartness-field-finite-completion-dependency-route}
For every accepted $\RealApartnessFieldUp$ carrier $F=(R,A,O,K,D,H,C,P,N)$, any field-facing read of $R,A,O,K$, or $D$ first exposes the finite-window $\StreamNameUp$ rows, the $\RegSeqRatUp$ readback rows, the $\DyadicRatCoreUp$ tolerance rows, and the terminal $\RealUp$ seal named above. Hence the apartness-field carrier consumes $\RealUp$ only through that displayed finite L10 dependency lattice, with $H,C,P,N$ preserving transport, replay, provenance, and local naming for the same route.
\end{proposition}
\begin{proof}
Project the carrier of \autoref{def:real-apartness-field-carrier}. The only Real-facing row is $R$, and the apartness, located-order, field, and dyadic rows $A,O,K,D$ are stored as downstream rows of the same finite packet.

Apply the StreamName and regular-rational dependency routes already present in the chapter. They force a comparison or inverse read to expose finite windows and regular-rational readback before the terminal $\RealUp$ seal is consumed.

Finally read the dyadic ledger. The tolerance and reciprocal-budget information in $D$ is displayed before the field-facing row $K$ is exported, while $H,C,P,N$ carry only the same finite dependency route. Therefore no read imports quotient real equality, selected representatives, countable choice, or ambient $\RealUp$ completeness.
\end{proof}
