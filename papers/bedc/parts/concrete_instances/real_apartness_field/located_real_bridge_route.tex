\begin{theorem}[Real apartness field located real route]
\label{thm:real-apartness-field-located-real-route}
Every accepted $\RealApartnessFieldUp$ inverse read or order-facing read must pass through the $\LocatedRealUp$ source route of \autoref{ch:concrete-instances-located-real-namecert} and the apartness-order bridge of \autoref{ch:concrete-instances-real-apartness-order-bridge-namecert} before the $\FieldUp$ reciprocal branch is exposed. In carrier notation, for
$$
F=(R,A,O,K,D,H,C,P,N),
$$
the real seal row $R$, apartness row $A$, located-order row $O$, field row $K$, dyadic ledger $D$, componentwise transport $H$, displayed replay $C$, provenance $P$, and local naming row $N$ are consumed in the same finite route. The route exports no trichotomy, quotient real equality, selected representative, countable-choice schedule, or ambient $\RealUp$ completeness witness.
\end{theorem}
\begin{proof}
Project the accepted carrier from \autoref{def:real-apartness-field-carrier}. The reciprocal branch is stored in the $\FieldUp$ row $K$, while $R,A,O,D,H,C,P,N$ remain visible as separate rows of the same finite packet.

Read the located-real source route first. The $\LocatedRealUp$ chapter supplies the finite comparison boundary consumed before the terminal real seal is used, so the field consumer receives located real evidence rather than a quotient representative or host real value.

Next read the apartness-order bridge. The bridge of \autoref{ch:concrete-instances-real-apartness-order-bridge-namecert} ties the apartness row $A$ to the located-order row $O$ over the same real seal $R$, and the dyadic ledger $D$ records the reciprocal budget attached to that positive evidence.

Finally replay the consumer through $H,C,P,N$. Since those rows only transport, replay, source, and name the already displayed route, they cannot add trichotomy, quotient real equality, selected representatives, countable choice, or ambient completeness. Thus every inverse or order-facing read reaches the $\FieldUp$ branch only after the located-real and apartness-order routes have both been displayed.
\end{proof}
