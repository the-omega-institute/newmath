Every $\RealApartnessFieldUp$ inverse read consumes the apartness-order bridge of \autoref{ch:concrete-instances-real-apartness-order-bridge-namecert} before the field carrier of \autoref{ch:concrete-instances-field-namecert} can export the reciprocal branch. The dyadic tolerance row inherited by the same route prevents trichotomy or quotient-real equality from entering the packet, so the field operation is read only after constructive apartness-order evidence has been displayed.
