The $\RealApartnessFieldUp$ lattice consumes the regular Cauchy criterion surface of \autoref{ch:concrete-instances-regularcauchycriterion-namecert}, the located real order route of \autoref{ch:concrete-instances-located-real-order-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert} before it exports apartness-field reads. Thus an inverse or comparison read from the field-facing row is routed through displayed criterion checks, order evidence, and Real sealing, rather than through a detached field oracle.

\begin{proposition}[Real apartness field regular Cauchy criterion route]
\label{thm:real-apartness-field-regular-cauchy-criterion-forward-route}
Every accepted $\RealApartnessFieldUp$ packet that exports an apartness-field read must first expose the $\RegularCauchyCriterionUp$ criterion dependency, the $\LocatedRealOrderUp$ order dependency, and the $\RealUp$ seal dependency named by its carrier rows. The exported read is therefore a finite L10 route through regular Cauchy criterion data, located order evidence, terminal Real sealing, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ data.
\end{proposition}
\begin{proof}
Project the accepted $\RealApartnessFieldUp$ carrier. Its apartness-field row is downstream from the displayed Real seal, located-order row, dyadic tolerance ledger, transport, replay, provenance, and local naming rows already named by the chapter.

The regular Cauchy criterion dependency supplies the finite criterion checks consumed before the Real seal and order-facing rows are read. Since the carrier has no coordinate for trichotomy, quotient real equality, selected representatives, countable choice, or ambient Real completeness, the exported apartness-field read remains inside the displayed finite route.
\end{proof}
