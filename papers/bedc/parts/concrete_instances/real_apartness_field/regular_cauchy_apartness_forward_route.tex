The $\RealApartnessFieldUp$ chapter reads its field-facing apartness rows
through the regular-Cauchy apartness lattice before exporting any arithmetic
surface. In particular, the route first exposes
\autoref{ch:concrete-instances-regular-cauchy-apartness-order-namecert}, then
the located-real surface of \autoref{ch:concrete-instances-located-real-namecert},
and then the located order rows of
\autoref{ch:concrete-instances-real-located-order-namecert}. Only after these
three sibling dependencies are visible may the chapter present the
$\RealApartnessFieldUp$ field-facing apartness rows; the route exports no host
real equality, detached order completion, selected located witness, or ambient
field law outside the displayed BEDC dossier lattice.
