\begin{theorem}[Real apartness field Real--RegSeqRat--Dyadic route]
\label{thm:real-apartness-field-real-regseq-dyadic-route}
Every $\RealApartnessFieldUp$ apartness-field comparison read exposes the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}, the $\RegSeqRatUp$ regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the $\DyadicRatCoreUp$ tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert} before the field-facing comparison row is exported. The route is a displayed dependency through real sealing, regular-rational readback, and dyadic tolerance control; it does not add trichotomy, quotient real equality, selected representatives, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:real-apartness-field-carrier}. Its comparison-facing data are stored with the real seal row, apartness row, located-order row, dyadic ledger, transport, replay, provenance, and local naming rows.

Read the $\RegSeqRatUp$ source before the terminal $\RealUp$ seal. The existing regular-Cauchy source route forces the comparison to pass through regular-rational readback rather than through an untracked real representative.

Then expose the $\DyadicRatCoreUp$ tolerance ledger. The apartness guard and reciprocal budget are consumed only with the displayed dyadic tolerance data, so the field-facing row is downstream of the Real, RegSeqRat, and DyadicRatCore dependencies named above.
\end{proof}

\begin{theorem}[Real apartness field inverse-guard dependency route]
\label{thm:real-apartness-field-real-regseq-dyadic-dependency-route}
Every inverse-guard export from an accepted $\RealApartnessFieldUp$ carrier factors through the $\RealUp$ seal, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance ledger, $\CauchyRealApartnessUp$ positive-apartness row, $\LocatedRealOrderUp$ boundary row, and $\FieldUp$ operation row before a field inverse is public. The route composes the Real--RegSeqRat--Dyadic dependency with inverse-guard stability and the regular-Cauchy apartness/order dependency, and it refuses trichotomy, quotient equality, selected representatives, countable choice, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:real-apartness-field-real-regseq-dyadic-route}. It places every field-facing comparison behind the terminal $\RealUp$ seal, the regular rational readback, and the dyadic tolerance ledger.

Next apply the inverse-guard stability surface in the parent chapter. The inverse row is public only when the positive-apartness guard remains attached to the displayed carrier, so the guard cannot be replaced by quotient equality or by a selected representative.

Finally read the regular-Cauchy apartness/order dependency. The positive-apartness row is checked together with the located-order boundary before the $\FieldUp$ operation row exposes an inverse. Hence the inverse export remains a finite route through Real, RegSeqRat, DyadicRatCore, CauchyRealApartness, LocatedRealOrder, and Field data, with trichotomy, countable choice, quotient equality, selected representatives, and ambient Real completeness outside the packet.
\end{proof}
