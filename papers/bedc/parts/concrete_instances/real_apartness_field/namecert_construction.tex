\chapter{A Concrete Naming Certificate for $\RealApartnessFieldUp$}
\label{ch:concrete-instances-real-apartness-field-namecert}
\origin{human}

Following Bishop and Bridges, the $\RealApartnessFieldUp$ packet records the constructive field-facing surface for real names whose inverse operation is guarded by displayed apartness evidence. It sits after the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the positive apartness route of \autoref{ch:concrete-instances-cauchy-real-apartness-namecert}, the located order boundary of $\LocatedRealOrderUp$, the algebraic source rows of \autoref{ch:concrete-instances-field-namecert}, and the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The packet joins $\RealUp$, $\CauchyRealApartnessUp$, $\LocatedRealOrderUp$, $\FieldUp$, and $\DyadicRatCoreUp$ as one finite NameCert surface; it does not import trichotomy, quotient real equality, selected representatives, countable choice, or ambient $\RealUp$ completeness.

Every apartness-field read passes through the $\RealUp$ seal and real readback rows of \autoref{ch:concrete-instances-real-namecert}, the apartness/order bridge of \autoref{ch:concrete-instances-real-apartness-order-bridge-namecert}, the specialization order surface of \autoref{ch:concrete-instances-order-namecert}, and the field operation rows of \autoref{ch:concrete-instances-field-namecert} before it exports an apartness algebra surface.

\begin{definition}[Real apartness field carrier]
\label{def:real-apartness-field-carrier}
A $\RealApartnessFieldUp$ carrier is a finite $\BHist$ packet
$$
F=(R,A,O,K,D,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RealUp$ seal row for the real-name surface exposed to field consumers.
\item $A$ is the $\CauchyRealApartnessUp$ positive-apartness row used to certify that a denominator is separated from zero before an inverse read is accepted.
\item $O$ is the $\LocatedRealOrderUp$ located order row that routes order-facing comparisons through displayed finite windows and dyadic cells.
\item $K$ is the $\FieldUp$ algebraic operation row for addition, multiplication, negation, zero, one, and the inverse request boundary.
\item $D$ is the $\DyadicRatCoreUp$ tolerance and positivity ledger controlling arithmetic error, gap size, and reciprocal budget.
\item $H$ records componentwise $\hsame$ transport for the real seal, apartness row, located order row, field row, dyadic ledger, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from each field-facing request through $R,A,O,K,D,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RealApartnessFieldUp$, $\RealUp$, $\CauchyRealApartnessUp$, $\LocatedRealOrderUp$, $\FieldUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealApartnessFieldUp}$ row.
\end{enumerate}
The carrier predicate asserts exactly that these rows form one constructive real field packet whose inverse reads are admitted only through apartness evidence. It has no coordinate for trichotomy, quotient real equality, selected representatives, countable choice, host real arithmetic, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Real apartness field NameCert obligations]
\label{thm:real-apartness-field-namecert-obligations}
For every accepted $\RealApartnessFieldUp$ carrier
$$
F=(R,A,O,K,D,H,C,P,N),
$$
the local $\NameCert_{\RealApartnessFieldUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the real seal, positive apartness row, located order row, field operation row, dyadic ledger, transport, replay, provenance, and local naming row;
\item algebraic projection, requiring every addition, multiplication, negation, zero, and one read to pass through the displayed $\FieldUp$ row $K$ and the real seal row $R$;
\item inverse admission, requiring an inverse read to consume the $\CauchyRealApartnessUp$ row $A$ and the positive dyadic gap data in $D$ before the field row $K$ exports the reciprocal branch;
\item located order compatibility, requiring the $\LocatedRealOrderUp$ row $O$ to read the same real-name and dyadic comparison data used by the apartness row;
\item transport and replay, requiring $H$ and $C$ to preserve the real, apartness, order, field, dyadic, provenance, and local naming rows together;
\item ledger exhaustion, excluding trichotomy, quotient real equality, selected representatives, countable choice, host real arithmetic, ambient $\RealUp$ completeness, Lean verification, and bridge checking.
\end{enumerate}
No real apartness field obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:real-apartness-field-carrier}. The accepted packet displays exactly the real seal, apartness row, located order row, field row, dyadic ledger, and structural rows.

The algebraic projection reads the $\FieldUp$ row $K$ only over the displayed real seal $R$. Thus the operation surface is the carried field row, not host real arithmetic or a quotient-real operation.

The inverse projection first reads $A$ and $D$. The apartness row supplies positive separation from zero, and the dyadic row supplies the gap and reciprocal budget; without those rows the inverse request remains at the boundary and cannot be consumed as a field operation.

Finally, $O,H,C,P,N$ keep comparison, transport, replay, provenance, and naming inside the same finite packet. Since none of the carrier coordinates stores a trichotomy decision, quotient equality, choice schedule, selected representative, completeness witness, Lean theorem, or standard bridge, none becomes a local NameCert obligation.
\end{proof}

\begin{theorem}[Real apartness field inverse-guard stability]\label{thm:real-apartness-field-inverse-guard-stability}
Let
$$
F=(R,A,O,K,D,H,C,P,N)
$$
be an accepted $\RealApartnessFieldUp$ carrier. An inverse read from the field row $K$ may be exported to a downstream consumer only when the displayed apartness guard in $A$ survives componentwise $\hsame$ transport through the $\RealUp$ seal $R$, the $\CauchyRealApartnessUp$ row $A$, the $\LocatedRealOrderUp$ boundary $O$, the $\FieldUp$ operation row $K$, the $\DyadicRatCoreUp$ tolerance and reciprocal-budget ledger $D$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and the local $\NameCert$ row $N$.

Consequently, the inverse branch is stable under the carried transport rows exactly when its apartness evidence remains displayed in the same finite packet. It is not stable by trichotomy, quotient real equality, selected representatives, countable choice, host real arithmetic, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier. The inverse request sits in the field row $K$, but the NameCert obligations require the request to consume the positive-apartness row $A$ and the positive dyadic gap data in $D$ before the reciprocal branch is exported.

Transport preserves the guard componentwise. The real seal $R$, apartness row $A$, located order boundary $O$, field row $K$, and dyadic ledger $D$ are carried through the same $\hsame$ route, while $C,P,N$ replay, source, and name that route.

Therefore the inverse operation remains downstream of the apartness guard after transport. If the displayed guard is absent, the transported packet has no row from which an inverse read can be exported.

The carrier stores no trichotomy decision, quotient equality, selected representative, choice schedule, host inverse, or completeness witness. Stability of the inverse branch is exactly stability of the displayed apartness guard inside the finite $\RealApartnessFieldUp$ packet.
\end{proof}

\begin{theorem}[Real apartness field multiplicative inverse boundary]
\label{thm:real-apartness-field-apartness-multiplicative-inverse}
Let $F=(R,A,O,K,D,H,C,P,N)$ be an accepted $\RealApartnessFieldUp$ carrier. A multiplicative inverse read from $K$ is available to a downstream real-field consumer only after $A$ displays positive apartness of the denominator from zero and $D$ displays the corresponding positive dyadic gap and reciprocal budget. Therefore the inverse operation is controlled by constructive apartness evidence rather than by trichotomy, quotient equality, selected representatives, or choice.
\end{theorem}
\begin{proof}
Begin with the accepted carrier. The inverse request is a branch of the field row $K$, but the carrier definition makes that branch depend on the same apartness and dyadic rows named by $A$ and $D$.

Read the apartness row. The row $A$ supplies a positive separation witness for the denominator source, so the consumer does not ask whether the denominator is equal to zero by trichotomy or quotient equality.

Read the dyadic ledger next. The row $D$ records the positive gap and reciprocal budget used by the inverse branch, so the reciprocal calculation is bounded by displayed finite arithmetic rather than by an unrecorded host inverse.

The remaining rows only preserve the route. Transport, replay, provenance, and local naming cannot add a selected representative, choice operator, or ambient completeness datum, so the inverse boundary is exactly the apartness-controlled field read stated above.
\end{proof}

\closureat{RealApartnessFieldUp}{seedStr}

\begin{closurestatus}{\RealApartnessFieldUp}
  \constructivestory{A $\RealApartnessFieldUp$ packet is generated from a $\RealUp$ seal, a $\CauchyRealApartnessUp$ positive-apartness row, a $\LocatedRealOrderUp$ located order row, a $\FieldUp$ operation row, a $\DyadicRatCoreUp$ tolerance and reciprocal-budget ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-apartness-field-carrier}, \autoref{thm:real-apartness-field-namecert-obligations}, \autoref{thm:real-apartness-field-inverse-guard-stability}, and \autoref{thm:real-apartness-field-apartness-multiplicative-inverse} seed the constructive real field boundary whose inverse branch is guarded by positive apartness evidence.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close trichotomy, quotient real equality, selected representatives, countable choice, host real arithmetic, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, algebraic projection, inverse admission, located order compatibility, transport, replay, and ledger exhaustion for BEDC.Derived.RealApartnessFieldUp.}
\end{closurestatus}
