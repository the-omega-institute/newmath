\begin{theorem}[Real apartness field Real/order forward link]
\label{thm:real-apartness-field-real-order-forward-link}
Every accepted $\RealApartnessFieldUp$ packet hands its apartness-field row to $\RealUp$ and $\RealApartnessOrderBridgeUp$ consumers only through the displayed $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$ seal, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. In carrier notation, a read of
$$
F=(R,A,O,K,D,H,C,P,N)
$$
must first expose the real seal route of \autoref{ch:concrete-instances-real-namecert} and the apartness/order bridge of \autoref{ch:concrete-instances-real-apartness-order-bridge-namecert} before the field row $K$ can export an inverse or order-facing operation.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:real-apartness-field-carrier}. The field row $K$ is not stored as host real arithmetic; it is carried together with the real seal $R$, apartness row $A$, located-order row $O$, dyadic ledger $D$, and the structural rows $H,C,P,N$.

Read the Real route first. The chapter introduction and StreamName dependency theorem route every Real-facing comparison through finite regular-rational and stream-window data before $R$ is consumed, so the field consumer receives a displayed $\RealUp$ seal rather than a selected representative or quotient real.

Now apply the apartness/order route. The bridge cited in \autoref{ch:concrete-instances-real-apartness-order-bridge-namecert} consumes the apartness row $A$, located-order row $O$, and dyadic ledger $D$ before any inverse or order-facing field operation is read.

Finally $H,C,P,N$ preserve the same route under componentwise transport, replay, provenance, and local naming. Thus every forward handoff to Real and apartness-order consumers factors through the displayed rows and adds no trichotomy, quotient real equality, selected representative, countable choice, or ambient $\RealUp$ completeness.
\end{proof}
