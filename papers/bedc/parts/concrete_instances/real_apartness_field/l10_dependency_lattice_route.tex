Every $\RealApartnessFieldUp$ apartness read is field-facing only after it has passed through the L10 completion lattice: the $\RegSeqRatUp$ readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}, the $\RealAlgOrderUp$ comparison rows of \autoref{ch:concrete-instances-real-alg-order-namecert}, and the located comparison surface of \autoref{ch:concrete-instances-located-real-comparison-namecert}. Thus the packet is consumed as a classifier over displayed regular-rational, real-seal, algebraic-order, and located-comparison evidence, not as a detached field oracle.
