\chapter{A Concrete Naming Certificate for $\RegularCauchyWindowRefinementUp$}
\label{ch:concrete-instances-regular-cauchy-window-refinement-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchyWindowRefinementUp$ packet records finite refinement of regular-Cauchy observation windows. It sits next to the cofinal-window route of \autoref{ch:concrete-instances-regular-cauchy-window-cofinality-namecert}, the finite stream schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet gives Real and Completion consumers a displayed way to pass from a coarse accepted window schedule to a finer schedule without quotient streams, selected limits, countable choice, or ambient completeness.

\begin{definition}[Regular Cauchy window refinement carrier]
\label{def:regular-cauchy-window-refinement-carrier}
A $\RegularCauchyWindowRefinementUp$ carrier is a finite $\BHist$ packet
$$
F=(S,W,W',D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the regular source row whose admissible names are read as $\RegSeqRatUp$ data.
\item $W$ is the original finite $\StreamNameUp$ window schedule exposed to the consumer.
\item $W'$ is the refined finite $\StreamNameUp$ window schedule, displayed as a later or thinner observation window over the same source route.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger comparing the original and refined windows.
\item $R$ is the $\RegSeqRatUp$ readback row reached after both schedules and the dyadic tolerance ledger have been displayed.
\item $E$ is the terminal $\RealUp$ seal read from the refined regular-Cauchy route.
\item $H$ records componentwise $\hsame$ transport for source, original window, refined window, dyadic tolerance, readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
S,W,W',D \longmapsto R \longmapsto E \longmapsto H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyWindowRefinementUp$, $\RegularCauchyWindowCofinalityUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyWindowRefinementUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,W,W',D,R,E,H,C,P,N$. It has no coordinate for quotient stream equality, a selected representative, countable-choice extraction, host real equality, or ambient completeness.
\end{definition}

\begin{theorem}[Regular Cauchy window refinement NameCert obligations]
\label{thm:regular-cauchy-window-refinement-namecert-obligations}
For every accepted $\RegularCauchyWindowRefinementUp$ carrier $F=(S,W,W',D,R,E,H,C,P,N)$, the local $\NameCert_{\RegularCauchyWindowRefinementUp}$ surface consists exactly of source admission through $S$, original finite-window exposure through $W$, refined finite-window exposure through $W'$, dyadic tolerance comparison through $D$, regular rational readback through $R$, terminal Real sealing through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-window-refinement-carrier}. Its displayed rows are exactly $S,W,W',D,R,E,H,C,P,N$, so the original and refined window schedules are both visible before any Real-facing read is exported.

The dyadic ledger $D$ is read after the two schedules have been displayed. It is therefore a comparison between finite window rows, not a quotient comparison between external Cauchy streams.

The readback row $R$ and seal row $E$ are downstream of the same finite data. Transport, replay, provenance, and local naming preserve that route, so the NameCert obligations are exhausted by the listed coordinates.
\end{proof}

\begin{theorem}[Regular Cauchy window refinement cofinal handoff]
\label{thm:regular-cauchy-window-refinement-cofinal-handoff}
Every Real or Completion consumer of an accepted $\RegularCauchyWindowRefinementUp$ packet factors its read through the finite handoff
$$
\RegularCauchyWindowCofinalityUp,\StreamNameUp,\DyadicRatCoreUp
\longmapsto \RegSeqRatUp
\longmapsto \RealUp .
$$
The consumer may replace the original finite window $W$ by the refined finite window $W'$ only through the displayed dyadic tolerance ledger $D$ and the regular rational readback row $R$.
\end{theorem}
\begin{proof}
Start with the source row $S$ and the original schedule $W$. The cofinal-window route of \autoref{ch:concrete-instances-regular-cauchy-window-cofinality-namecert} supplies the admissible tail boundary for replacing a coarse finite observation by a finer one.

Use finite-window induction along the displayed refinement from $W$ to $W'$. At the base window the consumer reads the same $\StreamNameUp$ schedule, and at each successor window the ledger $D$ records the dyadic tolerance comparison needed to preserve the regular-Cauchy readback.

After the refined schedule has been reached, row $R$ supplies the $\RegSeqRatUp$ readback and row $E$ supplies the terminal $\RealUp$ seal. The structural rows $H,C,P,N$ replay and name the same route, so no selected limit, countable-choice selector, quotient stream equality, or ambient completeness theorem can enter the handoff.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyWindowRefinementUp$ read displays one source row, one original $\StreamNameUp$ window row, one refined $\StreamNameUp$ window row, one $\DyadicRatCoreUp$ tolerance ledger, one $\RegSeqRatUp$ readback row, one $\RealUp$ seal, and local transport, replay, provenance, and naming rows before it is consumed by a Real or Completion route.}

\independenceWitness{regular-cauchy-window-cofinality, streamname, regseqrat, dyadicratcore, real: $\RegularCauchyWindowRefinementUp$ is not bijective to any listed sibling because it binds the original and refined finite windows, dyadic comparison, regular readback, Real seal, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ into one accepted refinement packet.}

\closureat{RegularCauchyWindowRefinementUp}{seedStr}

\begin{closurestatus}{\RegularCauchyWindowRefinementUp}
  \constructivestory{A $\RegularCauchyWindowRefinementUp$ packet is generated from a regular source row, original and refined finite StreamName windows, a DyadicRatCore tolerance ledger, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-window-refinement-carrier}, \autoref{thm:regular-cauchy-window-refinement-namecert-obligations}, and \autoref{thm:regular-cauchy-window-refinement-cofinal-handoff} seed the finite refinement route from cofinal windows to Real-facing readback.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient Cauchy stream equality, selected limits, countable choice, host real equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked source admission, original and refined window exposure, dyadic tolerance comparison, RegSeqRat readback, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyWindowRefinementUp.}
\end{closurestatus}
