\chapter{A Concrete Naming Certificate for $\CantorDiagonalRealUncountabilityUp$}
\label{ch:concrete-instances-cantor-diagonal-real-uncountability-namecert}
\origin{human}

Following Cantor and Bishop--Bridges constructive analysis, the $\CantorDiagonalRealUncountabilityUp$ packet records the finite obstruction met by any displayed attempt to enumerate Real-facing name surfaces through unary stream rows. It reads Boolean disagreement from \autoref{ch:concrete-instances-cantorspace-namecert}, finite stream windows from \autoref{ch:concrete-instances-streamname-namecert}, regular rational readback from \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal Real seal from \autoref{ch:concrete-instances-real-namecert}, and the middle-third set surface from \autoref{ch:concrete-instances-cantorset-namecert}. The packet is a finite diagonal certificate, not a host-cardinality theorem.

\begin{definition}[Cantor diagonal real-uncountability carrier]
\label{def:cantor-diagonal-real-uncountability-carrier}
A $\CantorDiagonalRealUncountabilityUp$ carrier is a finite $\BHist$ packet
$$
D=(E,W,B,Q,R,S,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is an attempted enumeration row, exposing only the finite $\StreamNameUp$-indexed candidate surface currently inspected.
\item $W$ is the requested diagonal window row, naming the finite list of indices and coordinates along which the diagonal read is performed.
\item $B$ is the $\BoolUp$ and $\CantorSpaceUp$ disagreement row, recording for each displayed diagonal coordinate the Boolean value read from the enumerated candidate and the opposite Boolean value assigned to the diagonal name.
\item $Q$ is the $\RegSeqRatUp$ readback row converting the displayed Boolean or CantorSpace disagreement window into regular rational observations.
\item $R$ is the terminal $\RealUp$ seal row reached only after $E,W,B,Q$ have been displayed.
\item $S$ is the $\CantorSetUp$ routing row, recording that the same finite disagreement window may be viewed through the real subset surface without adding a host powerset.
\item $H$ records componentwise $\hsame$ transport for the enumeration row, diagonal window, Boolean disagreement, regular rational readback, Real seal, CantorSet routing, continuation replay, provenance, and local naming row.
\item $K$ records displayed $\Cont$ replay from an enumeration request through the window, disagreement, readback, Real seal, and CantorSet routing rows.
\item $P$ is the $\Pkg$ provenance over $\CantorDiagonalRealUncountabilityUp$, $\StreamNameUp$, $\BoolUp$, $\CantorSpaceUp$, $\RegSeqRatUp$, $\RealUp$, $\CantorSetUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CantorDiagonalRealUncountabilityUp}$ row.
\end{enumerate}
The classifier accepts a packet only when the diagonal window is finite, every disagreement row is read through the displayed $\BoolUp$ and $\CantorSpaceUp$ surfaces, regular rational readback factors through $Q$, and the Real-facing seal is downstream of the same $E,W,B,Q$ route. It has no coordinate for host powersets, quotient real equality, actual uncountable-cardinal arithmetic, countable choice, selected infinite branches, or a completed classical surjection proof.
\end{definition}

\begin{theorem}[Cantor diagonal real-uncountability NameCert obligations]
\label{thm:cantor-diagonal-real-uncountability-namecert-obligations}
For every accepted $\CantorDiagonalRealUncountabilityUp$ carrier $D=(E,W,B,Q,R,S,H,K,P,N)$, the local $\NameCert_{\CantorDiagonalRealUncountabilityUp}$ surface consists exactly of the following five finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed enumeration row, diagonal window, disagreement ledger, regular rational readback, Real seal, CantorSet routing, transport, continuation, provenance, and local naming row;
\item finite-window locality, requiring each diagonal inspection to pass through $W$ before it can read any candidate row of $E$;
\item Boolean diagonal exactness, requiring $B$ to display a $\BoolUp$ disagreement between the enumerated candidate at the inspected coordinate and the diagonal value routed through $\CantorSpaceUp$;
\item regular-readback and Real-seal compatibility, requiring $Q$ and $R$ to reuse the same disagreement window rather than selecting a second real representative;
\item non-escape, requiring $S,H,K,P,N$ to route, transport, replay, source, and name no host powerset, quotient real equality, uncountable-cardinal arithmetic, countable-choice selector, selected infinite branch, or completed classical surjection proof.
\end{enumerate}
No local obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:cantor-diagonal-real-uncountability-carrier}. The accepted packet displays exactly the attempted enumeration surface, the finite diagonal window, the disagreement row, the regular rational readback, the Real seal, the CantorSet route, and the structural rows.

Finite-window locality follows from the order of the displayed route. A consumer must name a row of $W$ before it may inspect the matching candidate entry in $E$, so the argument never ranges over all indices or all coordinates at once.

Boolean diagonal exactness is the componentwise content of $B$. At every displayed diagonal coordinate, the candidate bit is read through the $\BoolUp$ surface and the diagonal bit is the opposite displayed row, transported through the $\CantorSpaceUp$ prefix surface.

The readback and seal clauses are then forced by $Q$ and $R$. The regular rational observations and the Real-facing seal both reuse the same finite disagreement window, so they cannot introduce quotient equality, a detached representative, or a selected infinite branch.

Finally, the routing and structural rows $S,H,K,P,N$ preserve the same finite packet. Since none of these rows contains a host powerset, cardinal arithmetic datum, countable-choice selector, or completed surjection proof, the local NameCert obligations are exhausted by the five displayed rows.
\end{proof}

\begin{theorem}[Cantor diagonal real-name separation]
\label{thm:cantor-diagonal-real-uncountability-diagonal-separation}
Every accepted $\CantorDiagonalRealUncountabilityUp$ carrier $D=(E,W,B,Q,R,S,H,K,P,N)$ exhibits a finite window on which the diagonal Real-facing name disagrees with the enumerated candidate routed through that window. More precisely, for each displayed row of $W$, the route
$$
E,W \longmapsto B \longmapsto Q \longmapsto R
$$
contains a $\BoolUp$ disagreement read through $\CantorSpaceUp$, transported by $H$, replayed by $K$, sourced by $P$, and named by $N$; the same window may be routed through $S$ to the $\CantorSetUp$ surface without changing the disagreement row.
\end{theorem}
\begin{proof}
Choose a displayed diagonal window row of $W$. Carrier admission gives the matching finite enumeration read in $E$ and exposes no other source for candidate data.

Read the disagreement row $B$ at that same window. By the classifier condition, the candidate bit is a $\BoolUp$ row coming from the inspected $\StreamNameUp$ coordinate, while the diagonal bit is the opposite displayed $\BoolUp$ row transported through the $\CantorSpaceUp$ prefix surface.

Pass this disagreement through $Q$ and then $R$. The regular rational readback and the Real seal are downstream of the same finite window, so the Real-facing name inherits the displayed disagreement rather than replacing it by quotient real equality or an external representative.

The optional CantorSet routing row $S$ only views the same finite disagreement through the subset surface. Componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local naming through $H,K,P,N$ preserve the row and add no host powerset, countable choice, selected branch, cardinal arithmetic, or completed surjection proof.
\end{proof}

\closureat{CantorDiagonalRealUncountabilityUp}{seedStr}

\begin{closurestatus}{\CantorDiagonalRealUncountabilityUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cantor-diagonal-real-uncountability-carrier}, \autoref{thm:cantor-diagonal-real-uncountability-namecert-obligations}, and \autoref{thm:cantor-diagonal-real-uncountability-diagonal-separation} seed the finite diagonal obstruction over $\StreamNameUp$, $\BoolUp$, $\CantorSpaceUp$, $\RegSeqRatUp$, $\RealUp$, and $\CantorSetUp$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host powersets, quotient real equality, actual uncountable-cardinal arithmetic, countable choice, selected infinite branches, a completed classical surjection proof, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-window locality, Boolean diagonal exactness, regular-readback and Real-seal compatibility, CantorSet routing, and non-escape proofs for BEDC.Derived.CantorDiagonalRealUncountabilityUp.}
  \constructivestory{A $\CantorDiagonalRealUncountabilityUp$ packet is generated from an attempted StreamName-indexed enumeration row, a finite diagonal window, a Bool and CantorSpace disagreement ledger, RegSeqRat readback, a RealUp seal, CantorSet routing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
