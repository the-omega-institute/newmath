\chapter{A Concrete Naming Certificate for $\CompactNetModulusStabilityUp$}
\label{ch:concrete-instances-compact-net-modulus-stability-namecert}
\origin{human}

Following Bishop and Bridges compact metric analysis, the $\CompactNetModulusStabilityUp$ packet realizes the compact-net direction of \autoref{ch:visions-compact-uniform-continuity} as finite NameCert data. It reads the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, continuous-map rows from \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus output surface of \autoref{ch:concrete-instances-uniformmodulus-namecert}, metric triangle rows from \autoref{ch:concrete-instances-metric-namecert}, and the stream, regular-rational, and Real seals of \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The packet names stability of the finite compact-net modulus route; it is not a selected Lebesgue number, an external modulus function, sequential compactness, countable choice, quotient metric equality, or ambient completeness.

\begin{definition}[Compact-net modulus stability carrier]
\label{def:compact-net-modulus-stability-carrier}
A $\CompactNetModulusStabilityUp$ carrier is a finite $\BHist$ packet
$$
K=(X,F,U,M,W,Q,R,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\CompactMetricUp$ finite-net source row.
\item $F$ is the $\ContinuousMapUp$ pointwise-modulus row over the same source.
\item $U$ is the $\UniformModulusUp$ output row produced by the finite compact-net route.
\item $M$ is the $\MetricUp$ triangle and distance-comparison row used to transport estimates across a bundled center.
\item $W$ is the $\StreamNameUp$ finite observation row for the source and target names read by the map.
\item $Q$ is the $\RegSeqRatUp$ regular rational readback row obtained from those windows.
\item $R$ is the terminal $\RealUp$ seal row for the displayed distance estimates.
\item $T$ is the compact-net stability ledger binding $X,F,U,M,W,Q,R$ to one finite modulus route.
\item $H$ stores componentwise $\hsame$ transport for compact-net, continuous-map, uniform-modulus, metric, stream, readback, Real-seal, stability, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay for the same compact-net modulus route.
\item $P$ is the $\Pkg$ provenance over $\CompactNetModulusStabilityUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\MetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactNetModulusStabilityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,F,U,M,W,Q,R,T,H,C,P,N$. It has no coordinate for a selected subcover, selected Lebesgue number, host modulus function, sequential compactness, countable choice, quotient metric equality, or ambient completeness.
\end{definition}

\begin{theorem}[Compact-net modulus finite route stability]
\label{thm:compact-net-modulus-stability-finite-route}
Every accepted $\CompactNetModulusStabilityUp$ carrier $K=(X,F,U,M,W,Q,R,T,H,C,P,N)$ exports a uniform-modulus read only through the finite route
$$
X,F,M \longmapsto W,Q,R \longmapsto U,T,H,C,P,N .
$$
Thus compact-net coverage, continuous pointwise-modulus rows, metric triangle estimates, stream windows, regular rational readback, Real seals, and the uniform-modulus output remain bound to one displayed NameCert packet.
\end{theorem}
\begin{proof}
Project the carrier and read the source rows $X,F,M$. The compact metric net, continuous-map modulus row, and metric triangle row are displayed over the same source before any output modulus is available.

Replay the finite observation layer through $C$. The stream window $W$ is read first, the regular rational row $Q$ reads back the inspected names, and the Real seal $R$ records the distance estimates used by the metric row.

The output stage $U,T,H,C,P,N$ exposes the uniform-modulus row only after the stability ledger has replayed the same compact-net route. Row exhaustion prevents selected subcovers, selected Lebesgue numbers, host modulus functions, sequence extraction, choice, quotient metric equality, or ambient completeness from entering the read.
\end{proof}

\begin{theorem}[Compact-net modulus Real-seal nonescape]
\label{thm:compact-net-modulus-stability-real-seal-nonescape}
Every $\RealUp$ distance estimate consumed by an accepted $\CompactNetModulusStabilityUp$ packet factors through the displayed $\StreamNameUp$ and $\RegSeqRatUp$ rows before it reaches the stability ledger $T$ and the $\UniformModulusUp$ output row $U$. Consequently the packet cannot export a Real-completeness principle, quotient equality of metric points, a selected compact subcover, or a host modulus selector.
\end{theorem}
\begin{proof}
Begin with a Real-facing distance estimate in the accepted packet. The only Real row is $R$, and it is displayed as downstream of the finite windows $W$ and regular rational readback $Q$.

Use the metric row $M$ and the stability ledger $T$ to transport the estimate across the compact-net center selected inside the finite packet. This transport is a replayed NameCert route, not an external triangle theorem detached from the carrier.

Finally expose the uniform-modulus row $U$ through $H,C,P,N$. Since the packet has no row for completeness, quotient point equality, selected subcovers, or host modulus functions, none of those objects can escape from the Real-seal route.
\end{proof}

\closureat{CompactNetModulusStabilityUp}{seedStr}

\begin{closurestatus}{\CompactNetModulusStabilityUp}
  \constructivestory{A $\CompactNetModulusStabilityUp$ packet is generated from CompactMetric finite-net rows, ContinuousMap pointwise-modulus rows, a UniformModulus output, Metric triangle estimates, StreamName windows, RegSeqRat readback, RealUp distance seals, a stability ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-net-modulus-stability-carrier}, \autoref{thm:compact-net-modulus-stability-finite-route}, and \autoref{thm:compact-net-modulus-stability-real-seal-nonescape} seed the compact-net modulus stability route realized from \autoref{ch:visions-compact-uniform-continuity}.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected Lebesgue numbers, selected subcovers, host modulus functions, sequential compactness, countable choice, quotient metric equality, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-net source lock, pointwise-modulus binding, metric-triangle stability, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactNetModulusStabilityUp.}
\end{closurestatus}
