The subsequence extractor also consumes the regular-Cauchy minimum-modulus packet of \autoref{ch:concrete-instances-regular-cauchy-min-modulus-namecert} when it selects a finite subsequence whose tail budget must remain visible. The extraction row $E$ may therefore read the source window $W$ and dyadic ledger $D$ only through the same minimum-modulus witness surface before the terminal $\RealUp$ or $\RealSeparabilityUp$ rows are exposed.
