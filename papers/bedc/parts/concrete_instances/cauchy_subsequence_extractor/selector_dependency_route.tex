Every $\CauchySubsequenceExtractorUp$ read first consumes the selector schedule of \autoref{ch:concrete-instances-cauchy-subsequence-selector-namecert}. Only after that selector route is visible may the extractor read the regular rational sequence surface of \autoref{ch:concrete-instances-regseqrat-namecert} and the finite window rows of \autoref{ch:concrete-instances-streamname-namecert}. Thus the extractor reaches any $\RealUp$ or completion-facing consumer through selector, RegSeqRat, and StreamName rows rather than through a free subsequence choice, quotient stream equality, countable choice, or ambient completeness.
