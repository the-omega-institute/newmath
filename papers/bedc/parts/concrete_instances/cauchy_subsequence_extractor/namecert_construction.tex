\chapter{A Concrete Naming Certificate for $\CauchySubsequenceExtractorUp$}
\label{ch:concrete-instances-cauchy-subsequence-extractor-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CauchySubsequenceExtractorUp$ packet records the finite extraction data used when a regular Cauchy source is read through subsequence windows before reaching a $\RealUp$ consumer. It sits after the Cauchy route of \autoref{ch:concrete-instances-cauchy-namecert}, the dyadic rational arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational sequence source of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the countable dense readback surface of \autoref{ch:concrete-instances-real-separability-namecert}. The packet records only displayed finite subsequence extraction data; it is not countable choice, quotient real equality, ambient completeness, or a selector for arbitrary Cauchy data.

\begin{definition}[CauchySubsequenceExtractor carrier]
\label{def:cauchy-subsequence-extractor-carrier}
A $\CauchySubsequenceExtractorUp$ carrier is a finite $\BHist$ packet
$$
S=(C,Q,W,D,E,R,L,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $C$ is the accepted $\CauchyUp$ source row whose regularity ledger is being inspected.
\item $Q$ is the $\RegSeqRatUp$ readback row naming the rational regular sequence extracted from $C$.
\item $W$ is the $\StreamNameUp$ window row listing the finite source indices, target indices, and requested precisions used by the extraction.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger recording the dyadic bounds that certify the extracted window remains inside the requested Cauchy budget.
\item $E$ is the extraction row. It records the monotone finite index map, the selected source observations, and the target-window schedule read from $W$.
\item $R$ is the terminal $\RealUp$ seal row reached only after $C,Q,W,D,E$ have been displayed.
\item $L$ is the $\RealSeparabilityUp$ readback row recording the finite dense real-name inspection used by downstream separability consumers.
\item $H$ records componentwise $\hsame$ transport for source, regular readback, stream windows, dyadic tolerances, extraction data, Real seal, separability readback, replay, provenance, and local naming.
\item $T$ records displayed $\Cont$ replay from a finite subsequence request through the same $C,Q,W,D,E,R,L,H,P,N$ rows.
\item $P$ is the $\Pkg$ provenance over $\CauchySubsequenceExtractorUp$, $\CauchyUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\RealSeparabilityUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchySubsequenceExtractorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $C,Q,W,D,E,R,L,H,T,P,N$. It has no coordinate for arbitrary subsequence choice, quotient equality of real names, countable choice, a global selector, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[CauchySubsequenceExtractor NameCert obligations]
\label{thm:cauchy-subsequence-extractor-namecert-obligations}
For every accepted $\CauchySubsequenceExtractorUp$ carrier
$$
S=(C,Q,W,D,E,R,L,H,T,P,N),
$$
the local $\NameCert_{\CauchySubsequenceExtractorUp}$ surface consists exactly of Cauchy source exposure, $\RegSeqRatUp$ readback, finite $\StreamNameUp$ window exposure, $\DyadicRatCoreUp$ tolerance ledger exposure, finite monotone extraction data, terminal $\RealUp$ sealing, $\RealSeparabilityUp$ readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every consumer must expose the finite window $W$, dyadic ledger $D$, and extraction row $E$ before it can read the Real seal $R$ or the separability row $L$.
\end{theorem}
\begin{proof}
Project the carrier $S=(C,Q,W,D,E,R,L,H,T,P,N)$. The analytic rows list the Cauchy source, regular rational readback, stream window, dyadic tolerance ledger, extraction row, Real seal, and separability readback.

The extraction row is downstream of the finite window and tolerance ledger. It can therefore name only the displayed monotone finite index map and selected observations, not an arbitrary subsequence selector.

The structural rows $H,T,P,N$ transport, replay, source, and name the same finite route. Since no listed exclusion is a carrier coordinate, the NameCert surface is exhausted by the displayed packet.
\end{proof}

\begin{theorem}[CauchySubsequenceExtractor finite-window route]
\label{thm:cauchy-subsequence-extractor-finite-window-route}
Every accepted $\CauchySubsequenceExtractorUp$ extraction read factors through
$$
\CauchyUp \longmapsto \RegSeqRatUp \longmapsto \StreamNameUp,\DyadicRatCoreUp \longmapsto \RealUp,\RealSeparabilityUp .
$$
Thus the exported subsequence data are finite regular-Cauchy windows with dyadic tolerance control, not countable choice, quotient real equality, or a global subsequence functional.
\end{theorem}
\begin{proof}
Start from the source rows $C$ and $Q$. They provide the Cauchy source and the regular rational readback being inspected.

The finite data are $W,D,E$. The window $W$ supplies the displayed indices and precisions, the dyadic ledger $D$ supplies the tolerance checks, and $E$ records the monotone finite extraction map.

Finally read $R,L,H,T,P,N$. These rows seal the same finite extraction route for Real and separability consumers, transport it componentwise, replay it under displayed continuation, and name it locally. No row stores a choice principle or quotient equality witness.
\end{proof}

\falsifiablePrediction{Every $\CauchySubsequenceExtractorUp$ consumer reaches $\RealUp$ or $\RealSeparabilityUp$ only after exposing $\CauchyUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, finite extraction data, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{cauchy, regseqrat, streamname, dyadicratcore, real, real-separability: $\CauchySubsequenceExtractorUp$ is not bijective to any listed sibling because it jointly carries source rows, regular readback, finite windows, dyadic tolerance ledgers, monotone extraction data, terminal Real sealing, separability readback, transport, replay, provenance, and local naming.}

\closureat{CauchySubsequenceExtractorUp}{seedStr}

\begin{closurestatus}{\CauchySubsequenceExtractorUp}
  \constructivestory{A $\CauchySubsequenceExtractorUp$ packet is generated from a $\CauchyUp$ source row, $\RegSeqRatUp$ readback, finite $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance ledgers, monotone extraction data, a $\RealUp$ seal, $\RealSeparabilityUp$ readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-subsequence-extractor-carrier}, \autoref{thm:cauchy-subsequence-extractor-namecert-obligations}, and \autoref{thm:cauchy-subsequence-extractor-finite-window-route} seed the finite subsequence extraction route from Cauchy sources through regular rational readback, stream windows, dyadic tolerance ledgers, Real sealing, separability readback, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim countable choice, arbitrary subsequence selection, quotient real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-window routing, dyadic tolerance exactness, monotone extraction exposure, Real seal nonescape, separability readback ordering, transport, replay, provenance, and local naming for BEDC.Derived.CauchySubsequenceExtractorUp.}
\end{closurestatus}
