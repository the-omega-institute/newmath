\begin{theorem}[Dominated convergence measure-integral sibling lattice]\label{thm:dominated-convergence-measure-integral-lattice-link}
Every $\DominatedConvergenceUp$ consumer must pass through the displayed sibling lattice before any dominated-convergence conclusion is read:
\begin{enumerate}
\item the $\MeasureUp$ row of \autoref{ch:concrete-instances-measure-namecert} supplies the finite measurable-window source;
\item the $\IntegralUp$ row of \autoref{ch:concrete-instances-integral-namecert} supplies the integral readback over that same source;
\item the $\FatouLemmaUp$ row supplies only the nonnegative lower-bound boundary;
\item the $\TanneryTheoremUp$ row supplies only the bounded finite-exchange handoff;
\item the $\RegSeqRatUp$ and $\RealUp$ rows supply the regular rational readback and terminal Real seal.
\end{enumerate}
Thus the route records measure, integral, Fatou, Tannery, regular-readback, and Real-seal dependencies as visible sibling data. It does not export ambient measure completion, quotient function equality, selected representatives, host almost-everywhere equality, or countable choice.
\end{theorem}
