\chapter{A Concrete Naming Certificate for $\MobiusTransformUp$}
\label{ch:concrete-instances-mobius-transform-namecert}
\origin{human}

Following Ahlfors on fractional linear transformations, the $\MobiusTransformUp$ packet records the finite coefficient and action rows by which a transformation $z \mapsto (Az+B)/(Cz+D)$ may be read before complex, projective, holomorphic, or hyperbolic cross-ratio consumers inspect it. It sits over the complex arithmetic carrier of \autoref{ch:concrete-instances-complex-namecert}, the homogeneous-line action surface of \autoref{ch:concrete-instances-projectivespace-namecert}, the disk-local analytic surface of \autoref{ch:concrete-instances-holomorphic-namecert}, and the four-boundary comparison packet of \autoref{ch:concrete-instances-hyperbolic-cross-ratio-namecert}. The packet names the fractional-linear carrier as BEDC data; it does not import a host Riemann sphere, quotient projective equality, selected inverse matrix, global automorphism group, or unrecorded cross-ratio theorem.

\begin{definition}[Mobius transform carrier]
\label{def:mobius-transform-carrier}
A $\MobiusTransformUp$ carrier is a finite $\BHist$ packet
$$
M=(A,B,C,D,G,PJ,HD,XR,H,K,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A,B,C,D$ are $\ComplexUp$ coefficient rows for the fractional-linear numerator and denominator.
\item $G$ is the determinant guard row, recording that the displayed complex determinant $AD-BC$ is apart from zero in the accepted complex-history reading.
\item $PJ$ is the $\ProjectiveSpaceUp$ action row, exposing the homogeneous coordinate transformation $[z:w]\mapsto [Az+Bw:Cz+Dw]$ only through the displayed projective carrier.
\item $HD$ is the $\HolomorphicUp$ disk-action row, recording the local disk source, denominator-apartness ledger, and holomorphic readback for every admitted disk on which the denominator guard is available.
\item $XR$ is the $\HyperbolicCrossRatioUp$ preservation row, requiring any four-boundary comparison to enter through the disk row, metric row, four boundary rows, boundary-fixed transport row, cross-ratio row, refusal row, Schwarz--Pick provenance, and structural rows of \autoref{ch:concrete-instances-hyperbolic-cross-ratio-namecert}.
\item $H$ records componentwise $\hsame$ transport for the four coefficient rows, determinant guard, projective action, holomorphic disk action, cross-ratio preservation, package provenance, classifier replay, and local naming.
\item $K$ records displayed $\Cont$ replay from coefficient reads through determinant guard, projective action, holomorphic disk action, and cross-ratio preservation.
\item $Q$ is the $\Pkg$ provenance over $\MobiusTransformUp$, $\ComplexUp$, $\ProjectiveSpaceUp$, $\HolomorphicUp$, $\HyperbolicCrossRatioUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MobiusTransformUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,B,C,D,G,PJ,HD,XR,H,K,Q,N$. It has no coordinate for host matrix equality, quotient projective equality, a selected inverse, an ambient automorphism group, or a cross-ratio statement not routed through the displayed four-boundary packet.
\end{definition}

\begin{theorem}[Mobius transform NameCert obligations]
\label{thm:mobius-transform-namecert-obligations}
For every accepted $\MobiusTransformUp$ carrier $M=(A,B,C,D,G,PJ,HD,XR,H,K,Q,N)$, the local $\NameCert_{\MobiusTransformUp}$ surface consists exactly of complex coefficient rows, the determinant guard, projective action, holomorphic disk action, hyperbolic cross-ratio preservation routed through $\HyperbolicCrossRatioUp$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every accepted consumer must inspect those rows before treating the packet as a fractional-linear transformation.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:mobius-transform-carrier}. The four coefficient rows $A,B,C,D$ are the only complex entries of the packet, and the determinant guard $G$ is the only accepted witness that the displayed matrix is usable.

The action rows $PJ$ and $HD$ then expose the same coefficient data through two controlled surfaces. The projective action is read through $\ProjectiveSpaceUp$, while the holomorphic disk action is read through $\HolomorphicUp$ with the denominator-apartness ledger in scope.

The cross-ratio row $XR$ is not a free theorem about a host sphere. It is admitted only as a consumer of the $\HyperbolicCrossRatioUp$ four-boundary packet, so preservation claims inherit the disk, metric, boundary, refusal, Schwarz--Pick, transport, replay, provenance, and local naming rows of that packet.

Finally $H,K,Q,N$ transport, replay, source, and name the same finite carrier. Since no other row is present, a consumer cannot replace the determinant guard by host invertibility, replace projective action by quotient equality, or bypass the displayed cross-ratio route.
\end{proof}

\begin{theorem}[Mobius transform cross-ratio preservation route]
\label{thm:mobius-transform-cross-ratio-preservation}
Every accepted $\MobiusTransformUp$ cross-ratio consumer factors through the finite route
$$
\ComplexUp,\ \ProjectiveSpaceUp,\ \HolomorphicUp,\ \HyperbolicCrossRatioUp .
$$
The route first reads the complex coefficient rows and determinant guard, then the projective action row, then the holomorphic disk-action row, and finally the $\HyperbolicCrossRatioUp$ preservation row with its disk, metric, four boundary rows, boundary-fixed transport, cross-ratio comparison, refusal, Schwarz--Pick provenance, transport, replay, package provenance, and local naming. It exports no host Riemann sphere, quotient projective equality, selected inverse matrix, global automorphism group, or unrecorded boundary invariant.
\end{theorem}
\begin{proof}
Let $M=(A,B,C,D,G,PJ,HD,XR,H,K,Q,N)$ be accepted. The coefficient rows and determinant guard are read first, so the fractional-linear numerator, denominator, and apartness condition are fixed as finite $\ComplexUp$ histories.

The projective row $PJ$ packages the same coefficients as a homogeneous action through $\ProjectiveSpaceUp$. The holomorphic row $HD$ packages the same coefficients as a disk-local map through $\HolomorphicUp$, with denominator-apartness retained before any analytic readback is exposed.

The row $XR$ is the only coordinate where cross-ratio preservation can be claimed. By construction it points to the $\HyperbolicCrossRatioUp$ four-boundary packet, so it must read the disk, metric, boundary, transport, comparison, refusal, Schwarz--Pick, and structural rows before preservation is visible.

The structural rows $H,K,Q,N$ keep the same carrier under transport, replay, provenance, and naming. Thus the preservation route remains finite and local to the displayed NameCert data.
\end{proof}

\falsifiablePrediction{Any accepted $\MobiusTransformUp$ consumer omitting one of the four $\ComplexUp$ coefficient rows, the determinant guard, the $\ProjectiveSpaceUp$ action row, the $\HolomorphicUp$ disk-action row, the $\HyperbolicCrossRatioUp$ preservation row, $\Pkg$ provenance, or the local $\NameCert$ row refutes \autoref{thm:mobius-transform-namecert-obligations}.}

\independenceWitness{complex, projectivespace, holomorphic, hyperbolic-cross-ratio: $\MobiusTransformUp$ is not bijective to any listed sibling because it jointly carries coefficient rows, determinant guard, projective action, holomorphic disk action, cross-ratio preservation, transport, replay, package provenance, and local naming for a fractional-linear transformation.}

\closureat{MobiusTransformUp}{seedStr}

\begin{closurestatus}{\MobiusTransformUp}
  \constructivestory{A $\MobiusTransformUp$ packet is generated from four $\ComplexUp$ coefficient rows, a determinant guard, a $\ProjectiveSpaceUp$ homogeneous action row, a $\HolomorphicUp$ disk-action row, a $\HyperbolicCrossRatioUp$ preservation row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:mobius-transform-carrier}, \autoref{thm:mobius-transform-namecert-obligations}, and \autoref{thm:mobius-transform-cross-ratio-preservation} seed the finite fractional-linear carrier over complex coefficients, determinant guard, projective action, holomorphic disk action, cross-ratio preservation, and structural rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host Riemann-sphere equality, quotient projective equality, selected inverse matrices, global automorphism groups, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, determinant guard transport, projective action replay, holomorphic disk-action replay, cross-ratio consumer routing, TasteGate nontriviality, field faithfulness, provenance, transport, replay, and local naming for BEDC.Derived.MobiusTransformUp.}
\end{closurestatus}
