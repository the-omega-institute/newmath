\chapter{A Concrete Naming Certificate for $\SignChangeBisectionUp$}
\label{ch:concrete-instances-sign-change-bisection-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\SignChangeBisectionUp$ packet records the finite bisection route attached to an interval whose endpoint reads have opposite signs. It sits after the located endpoint-plus-radius interval of \autoref{ch:concrete-instances-located-real-interval-namecert}, the retained-branch bisection surface of \autoref{ch:concrete-instances-located-interval-bisection-namecert}, the located-zero witness of \autoref{ch:concrete-instances-bishop-located-zero-namecert}, and the theorem-object route of \autoref{ch:concrete-instances-intermediate-value-theorem-namecert}. The chapter names the finite sign-change ledger consumed by later located-zero and interval-search rows; it does not select a root, decide trichotomy, identify quotient real names, invoke open-cover compactness, or assert ambient $\RealUp$ completeness.

\begin{definition}[Sign-change bisection NameCert surface]
\label{def:sign-change-bisection-namecert-surface}
A $\SignChangeBisectionUp$ carrier is a finite $\BHist$ packet
$$
B=(I,L,S,Z,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the source $\LocatedRealIntervalUp$ row, read through \autoref{def:located-real-interval-carrier} and carrying endpoint names, radius data, locator reads, locatedness moduli, and metric bracket data.
\item $L$ is the $\LocatedIntervalBisectionUp$ row, read through \autoref{def:located-interval-bisection-carrier}, recording the finite retained-branch history, midpoint rows, retained windows, and regular readback route.
\item $S$ is the endpoint and midpoint sign-change ledger. It records only displayed finite sign reads and the retained sign boundary transported along $L$.
\item $Z$ is the $\BishopLocatedZeroUp$ consumer row, read through \autoref{def:bishop-located-zero-carrier}, which may inspect the bracket, modulus, bisection schedule, readback, and Real seal after $I,L,S$ have been displayed.
\item $T$ is the $\IntermediateValueTheoremUp$ theorem-object row, read through \autoref{def:intermediate-value-theorem-carrier}, supplying the finite interval, map, sign, window, readback, and Real-seal route used by theorem-facing consumers.
\item $H$ records componentwise $\hsame$ transport for the interval, bisection, sign ledger, located-zero, theorem-object, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a sign-change bisection request through $I,L,S,Z,T,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\SignChangeBisectionUp$, $\LocatedRealIntervalUp$, $\LocatedIntervalBisectionUp$, $\BishopLocatedZeroUp$, $\IntermediateValueTheoremUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SignChangeBisectionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a selected root, host interval equality, quotient real equality, trichotomy, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Sign-change bisection located-zero route]
\label{thm:sign-change-bisection-located-zero-route}
Every accepted $\SignChangeBisectionUp$ packet routes a located-zero read through
$$
\LocatedRealIntervalUp \longmapsto \LocatedIntervalBisectionUp \longmapsto S \longmapsto \BishopLocatedZeroUp
$$
before any theorem-object consumer may cite the $\IntermediateValueTheoremUp$ row. The route exposes the interval source of \autoref{thm:located-real-interval-exactness-route}, the retained-window bisection route of \autoref{thm:located-interval-bisection-nested-window-route}, and the zero-witness bisection discipline of \autoref{thm:bishop-located-zero-bisection-soundness}; it exports no selected global root or ambient intermediate-value principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:sign-change-bisection-namecert-surface}. The source interval row $I$ is the only coordinate carrying endpoint and locatedness data, and the bisection row $L$ is the only coordinate carrying retained midpoint and window data.

Apply the located interval exactness route and the located interval bisection nested-window route. These force every interval-search read to pass first through the displayed endpoint-radius data and then through the finite retained branch route.

The sign ledger $S$ is attached to that branch route, and the located-zero row $Z$ may read it only as part of the same finite packet. Bishop located zero bisection soundness then places the zero-witness read behind the bracket, modulus, schedule, regular readback, and Real-seal discipline.

Finally, the theorem-object row $T$ remains a downstream consumer. Componentwise transport, replay, provenance, and local naming preserve the same route, while the carrier contains no coordinate for root selection, trichotomy, quotient equality, open-cover compactness, countable choice, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\SignChangeBisectionUp$ located-zero read displays a LocatedRealIntervalUp source row, a LocatedIntervalBisectionUp retained-branch row, a finite sign-change ledger, a BishopLocatedZeroUp consumer row, hsame transport, Cont replay, Pkg provenance, and local NameCert data.}

\independenceWitness{located-real-interval, located-interval-bisection, bishop-located-zero, intermediate-value-theorem: $\SignChangeBisectionUp$ is not bijective with any listed sibling because it joins the source located interval, retained bisection route, transported sign-change ledger, located-zero consumer, theorem-object consumer, transport, replay, provenance, and local naming in one packet.}

\closureat{SignChangeBisectionUp}{seedStr}

\begin{closurestatus}{\SignChangeBisectionUp}
  \constructivestory{A $\SignChangeBisectionUp$ packet is generated from a $\LocatedRealIntervalUp$ source row, a $\LocatedIntervalBisectionUp$ retained-branch row, a finite sign-change ledger, a $\BishopLocatedZeroUp$ consumer row, an $\IntermediateValueTheoremUp$ theorem-object row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:sign-change-bisection-namecert-surface} and \autoref{thm:sign-change-bisection-located-zero-route} seed the finite sign-change bisection route over located intervals, retained bisection windows, located-zero consumers, theorem-object consumers, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected roots, host interval equality, quotient real equality, trichotomy, countable choice, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, sign-ledger visibility, located-zero routing, theorem-object routing, transport, replay, provenance, and local naming for BEDC.Derived.SignChangeBisectionUp.}
\end{closurestatus}
