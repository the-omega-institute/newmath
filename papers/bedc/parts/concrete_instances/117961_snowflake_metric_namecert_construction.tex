\chapter{A Concrete Naming Certificate for $\SnowflakeMetricUp$}
\label{ch:concrete-instances-snowflake-metric-namecert}
\origin{human}

Following Assouad and Heinonen on snowflake metrics, the $\SnowflakeMetricUp$ packet records the finite BEDC route that transforms a displayed metric distance by a dyadic exponent $0<q\leq 1$. It sits over the metric source of \autoref{ch:concrete-instances-metric-namecert}, the dyadic rational core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the uniform-continuity route of \autoref{ch:concrete-instances-uniformcontinuity-namecert}, and the Lipschitz transport discipline of \autoref{ch:concrete-instances-lipschitz-function-namecert} and \autoref{ch:concrete-instances-lipschitz-modulus-namecert}. The packet names a finite snowflake-distance readback; it does not import host real powers, quotient metric spaces, choice-selected representatives, or ambient completeness.

\begin{definition}[Snowflake metric carrier]
\label{def:snowflake-metric-carrier}
A $\SnowflakeMetricUp$ carrier is a finite $\BHist$ packet
$$
S=(M,q,D,T,L,H,C,P,N)
$$
with the following rows:
\begin{enumerate}
\item $M$ is the source $\MetricUp$ row, including the displayed point pair and distance read;
\item $q$ is the $\DyadicRatCoreUp$ exponent row, carrying the finite inequalities $0<q$ and $q\leq 1$;
\item $D$ is the $\RealUp$ distance readback row for the snowflaked distance produced from the displayed source distance and $q$;
\item $T$ is the Holder/snowflake transport row, read through the uniform-continuity surface of \autoref{ch:concrete-instances-uniformcontinuity-namecert} and the Lipschitz siblings \autoref{ch:concrete-instances-lipschitz-function-namecert} and \autoref{ch:concrete-instances-lipschitz-modulus-namecert};
\item $L$ is the triangle ledger, recording the finite dyadic comparison that keeps the snowflake distance subadditive for $0<q\leq 1$;
\item $H$ records componentwise $\hsame$ transport of $M,q,D,T,L$;
\item $C$ records displayed $\Cont$ replay of the same source metric, exponent, readback, transport, and triangle-ledger rows;
\item $P$ is the $\Pkg$ provenance over $\SnowflakeMetricUp$, $\MetricUp$, $\DyadicRatCoreUp$, $\RealUp$, $\UniformContinuityUp$, $\LipschitzMapUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\SnowflakeMetricUp}$ row.
\end{enumerate}
The carrier has no coordinate for host real exponentiation, quotient equality of metric spaces, selected completions, or choice principles.
\end{definition}

\begin{theorem}[Snowflake metric NameCert obligations]
\label{thm:snowflake-metric-namecert-obligations}
For every accepted $\SnowflakeMetricUp$ carrier $S=(M,q,D,T,L,H,C,P,N)$, the local $\NameCert_{\SnowflakeMetricUp}$ surface consists exactly of source $\MetricUp$ admission through $M$, dyadic exponent admission through $q$ with $0<q\leq 1$, terminal $\RealUp$ distance readback through $D$, Holder/snowflake transport through $T$, triangle-ledger accountability through $L$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:snowflake-metric-carrier}. The source metric row, dyadic exponent row, readback row, transport row, triangle ledger, and structural rows are the whole packet.

Read the analytic rows in order. The $\MetricUp$ source supplies the distance being transformed, $q$ supplies the positive dyadic exponent bounded by $1$, and $D$ records the resulting $\RealUp$ distance readback.

The transport and triangle rows then bind the construction to the existing continuity surface. The rows $H,C,P,N$ transport, replay, source, and name the same finite route, so no host real-power operation, quotient metric-space equality, selected completion, or choice principle is exported.
\end{proof}

\begin{theorem}[Snowflake metric Holder route]
\label{thm:snowflake-metric-holder-route}
Every continuity consumer of an accepted $\SnowflakeMetricUp$ packet reads the transformed metric only through the displayed route
$$
\MetricUp,\quad \DyadicRatCoreUp,\quad \RealUp,\quad \UniformContinuityUp,\quad \LipschitzMapUp,\quad \BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert .
$$
The Holder-facing row $T$ consumes the dyadic exponent and the source metric distance before exporting uniform-continuity or Lipschitz-style data; it exports no ambient real powers, quotient metric spaces, selected representatives, or untracked completeness facts.
\end{theorem}
\begin{proof}
Begin with the source metric and exponent rows. The consumer can inspect only the displayed $\MetricUp$ distance and the $\DyadicRatCoreUp$ proof that $0<q\leq 1$.

Next read the terminal distance readback and the triangle ledger. These rows attach the snowflaked distance to a $\RealUp$ seal and to the finite subadditivity comparison needed by metric consumers.

Finally pass through the uniform-continuity and Lipschitz sibling routes. They consume the Holder/snowflake transport row $T$ only after $M,q,D,L$ are visible, and the structural rows $H,C,P,N$ keep the handoff inside the same finite NameCert packet.
\end{proof}

\falsifiablePrediction{An accepted $\SnowflakeMetricUp$ consumer that obtains a snowflaked distance without reading the source $\MetricUp$ row, the positive dyadic exponent row, the $\RealUp$ readback row, the Holder/snowflake transport row, the triangle ledger, and the structural $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows refutes the carrier surface.}

\independenceWitness{metric, dyadicratcore, real, uniformcontinuity, lipschitz-function, and lipschitz-modulus: no listed sibling jointly carries the source metric, positive dyadic exponent, snowflaked real readback, Holder transport, triangle ledger, transport, replay, provenance, and local naming rows of $S=(M,q,D,T,L,H,C,P,N)$.}

\closureat{SnowflakeMetricUp}{seedStr}

\begin{closurestatus}{\SnowflakeMetricUp}
  \constructivestory{A $\SnowflakeMetricUp$ packet is generated from a source MetricUp row, a positive DyadicRatCoreUp exponent bounded by one, a RealUp distance readback, a Holder/snowflake transport row, a triangle ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:snowflake-metric-carrier}, \autoref{thm:snowflake-metric-namecert-obligations}, and \autoref{thm:snowflake-metric-holder-route} seed the finite snowflake-metric transformation surface over $\MetricUp$, $\DyadicRatCoreUp$, $\RealUp$, $\UniformContinuityUp$, and $\LipschitzMapUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host real powers, quotient metric spaces, selected completions, Lean verification, or bridge checking for $\SnowflakeMetricUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic exponent bounds, Real distance readback, triangle-ledger accountability, Holder transport, replay, provenance, and local naming for BEDC.Derived.SnowflakeMetricUp.}
\end{closurestatus}
