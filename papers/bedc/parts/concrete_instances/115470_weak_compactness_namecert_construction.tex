\chapter{A Concrete Naming Certificate for $\WeakCompactnessUp$}
\label{ch:concrete-instances-weak-compactness-namecert}
\origin{human}

Following Bourbaki and Schaefer on locally convex functional analysis, the $\WeakCompactnessUp$ packet records the finite weak-topology compactness interface that a functional-analysis consumer may inspect before any host weak compactness theorem is available. It sits after the finite weak-star dual-ball route of \autoref{ch:concrete-instances-banach-alaoglu-namecert} and \autoref{ch:concrete-instances-weak-star-compactness-namecert}, beside the compact carrier of \autoref{ch:concrete-instances-compact-namecert}, and over bounded functional-analysis source data. The chapter names weak compactness only as finite NameCert data: a source row, weak-observation window, bounded-set row, compactness evidence, scalar readback, transport, replay, provenance, and local naming.

\begin{definition}[Weak compactness carrier]
\label{def:weak-compactness-carrier}
A $\WeakCompactnessUp$ carrier is a finite $\BHist$ packet
$$
W=(F,T,B,A,C,R,H,Q,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the functional-analysis source row, read through the finite vector, topology, continuity, and bounded-linear-functional surfaces made available to the consumer.
\item $T$ is the weak-topology observation window, listing only the finitely many bounded-linear-functional probes and scalar tolerances visible in the present read.
\item $B$ is the bounded set row, exposing the displayed boundedness ledger before any compactness-facing read is allowed.
\item $A$ is the Banach--Alaoglu and weak-star compactness handoff row, consumed only through the finite windows of \autoref{ch:concrete-instances-banach-alaoglu-namecert} and \autoref{ch:concrete-instances-weak-star-compactness-namecert}.
\item $C$ is the $\CompactUp$ compactness evidence row for the displayed weak-observation window, not an unrestricted open-cover compactness theorem over a host weak topology.
\item $R$ is the $\RealUp$ scalar readback row for functional values, bounds, and comparison tolerances.
\item $H$ records componentwise $\hsame$ transport for the functional-analysis source, weak observation window, bounded row, handoff row, compactness evidence, scalar readback, replay, provenance, and local naming rows.
\item $Q$ records displayed $\Cont$ replay from a weak compactness request through $F,T,B,A,C,R,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\WeakCompactnessUp$, $\FunctionalAnalysisUp$, $\BoundedSetUp$, $\BanachAlaogluUp$, $\WeakStarCompactnessUp$, $\CompactUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\WeakCompactnessUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $F,T,B,A,C,R,H,Q,P,N$. It has no coordinate for a selected weak cluster point, ultrafilter argument, unrestricted product compactness, quotient equality of functionals, host weak topology, or ambient reflexivity theorem.
\end{definition}
\leanstmt{BEDC.Derived.WeakCompactnessUp}

\begin{theorem}[Weak compactness NameCert obligations]
\label{thm:weak-compactness-namecert-obligations}
For every accepted $\WeakCompactnessUp$ carrier
$$
W=(F,T,B,A,C,R,H,Q,P,N),
$$
the local $\NameCert_{\WeakCompactnessUp}$ surface consists exactly of functional-analysis source admission through $F$, finite weak-observation admission through $T$, bounded-set admission through $B$, Banach--Alaoglu and weak-star compactness handoff through $A$, compactness evidence through $C$, scalar readback through $R$, componentwise transport through $H$, displayed replay through $Q$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier in \autoref{def:weak-compactness-carrier}. The displayed rows $F,T,B,A,C,R,H,Q,P,N$ are the complete inventory available to the certificate.

The analytic source is read first. Row $F$ fixes the functional-analysis surface, row $T$ restricts the weak topology to a finite observation window, and row $B$ supplies the boundedness ledger before compactness evidence is inspected.

The compactness-facing part is then routed through $A,C,R$. The handoff row $A$ cites only the finite Banach--Alaoglu and weak-star compactness windows, $C$ records the compact evidence for the displayed window, and $R$ keeps scalar values and tolerances inside the Real readback surface.

Finally the structural rows $H,Q,P,N$ transport, replay, source, and name the same finite packet. Since no selected cluster point, ultrafilter, quotient functional equality, unrestricted product compactness, host weak topology, or reflexivity row is present, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Weak compactness functional-analysis handoff]
\label{thm:weak-compactness-functional-analysis-handoff}
Every functional-analysis consumer of an accepted $\WeakCompactnessUp$ packet must read the finite route
$$
F,T,B,A,C,R,H,Q,P,N
$$
before exporting weak compactness data. Thus the consumer sees a functional-analysis source, a finite weak-topology observation window, a bounded-set row, the Banach--Alaoglu and weak-star compactness handoff, $\CompactUp$ evidence, $\RealUp$ scalar readback, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows. The export carries no selected weak cluster point, ultrafilter argument, unrestricted product compactness, quotient functional equality, host weak topology, ambient reflexivity theorem, or open-cover compactness principle.
\end{theorem}
\begin{proof}
Begin with a functional-analysis consumer request. By \autoref{thm:weak-compactness-namecert-obligations}, the request must expose $F,T,B$ before any compactness-facing data can be read.

Next use the handoff row $A$. It points only to the finite dual-ball and weak-star compactness windows already displayed by the sibling chapters, so the consumer cannot replace the handoff with an unrestricted compactness theorem.

The compactness and scalar data then pass through $C$ and $R$. Row $C$ is the local $\CompactUp$ evidence for the displayed window, while $R$ keeps functional values, bounds, and tolerances inside the scalar readback surface.

The structural rows $H,Q,P,N$ preserve the same route for transport, replay, provenance, and naming. Since the carrier has no row for any refused host principle, the exported functional-analysis handoff is exactly the finite weak compactness route stated above.
\end{proof}

\falsifiablePrediction{Every public $\WeakCompactnessUp$ read factors through a functional-analysis source row, a finite weak-topology observation window, a bounded-set row, a Banach--Alaoglu and $\WeakStarCompactnessUp$ handoff, $\CompactUp$ evidence, $\RealUp$ scalar readback, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ naming. A read that exports selected weak cluster points, ultrafilters, quotient functional equality, unrestricted product compactness, host weak topology, ambient reflexivity, or open-cover compactness from this packet refutes the carrier boundary.}

\independenceWitness{banachalaoglu, weakstarcompactness, compact: none is bijective with $\WeakCompactnessUp$ because this carrier binds the functional-analysis source, weak-observation window, bounded-set row, finite dual-ball handoff, compactness evidence, scalar readback, transport, replay, provenance, and local NameCert rows while refusing selection and unrestricted compactness coordinates.}

\closureat{WeakCompactnessUp}{seedStr}

\begin{closurestatus}{\WeakCompactnessUp}
  \constructivestory{A $\WeakCompactnessUp$ packet is generated from a functional-analysis source row, a finite weak-topology observation window, a bounded-set row, a Banach--Alaoglu and weak-star compactness handoff, compactness evidence, scalar readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:weak-compactness-carrier}, \autoref{thm:weak-compactness-namecert-obligations}, and \autoref{thm:weak-compactness-functional-analysis-handoff} seed the finite weak compactness interface.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected weak cluster points, ultrafilter arguments, quotient functional equality, unrestricted product compactness, host weak topology, ambient reflexivity, open-cover compactness, Lean theorem verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, weak-observation-window admission, bounded-set admission, finite dual-ball handoff exactness, compactness-row handoff, scalar readback, transport, replay, provenance, and local naming for BEDC.Derived.WeakCompactnessUp.}
\end{closurestatus}
