\chapter{A Concrete Naming Certificate for \(\PolynomialUp\)}
\label{ch:concrete-instances-polynomial-namecert}

The polynomial interface \(\PolynomialUp\) is licensed over a scalar ring from \autoref{ch:concrete-instances-ring-namecert} as finite coefficient sequences from \autoref{ch:concrete-instances-list-namecert}, normalized to remove trailing zeros so that the highest-index entry is nonzero (or the sequence is empty for the zero polynomial). The classifier is structural sameness on the normalized representative. Polynomial addition and multiplication are derived operations licensed by the ring's certificate fields.

\section{Carrier definition}

\section{The certificate}
