\chapter{A Concrete Naming Certificate for $\HyperbolicLinearizedPhaseDistanceUp$}
\label{ch:concrete-instances-hyperbolic-linearized-phase-distance-namecert}
\origin{ai}

The $\HyperbolicLinearizedPhaseDistanceUp$ packet realizes the linearized phase-distance reading of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite BEDC NameCert data. It sits after the boundary-shadow packet of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the visual-metric packet of \autoref{ch:concrete-instances-hyperbolic-visual-metric-namecert}, the observer-expansion packet of \autoref{ch:concrete-instances-hyperbolic-observer-expansion-namecert}, the phase-distance observer-expansion packet of \autoref{ch:concrete-instances-phase-distance-observer-expansion-namecert}, the radius-ladder packet of \autoref{ch:concrete-instances-poincare-visual-radius-ladder-namecert}, and the complex phase-surface fiber of \autoref{ch:concrete-instances-complex-phase-surface-fiber-namecert}. The packet names a finite boundary-shadow, visual-metric, Real-seal, transport, replay, provenance, and local naming surface; it does not assert a host hyperbolic metric space, a global visual boundary, differentiable phase flow, quotient geodesic equality, or an observer object outside the displayed rows.

\begin{definition}[Hyperbolic linearized phase-distance carrier]
\label{def:hyperbolic-linearized-phase-distance-carrier}
A $\HyperbolicLinearizedPhaseDistanceUp$ carrier is a finite $\BHist$ packet
$$
L=(B,V,O,\Phi,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\HyperbolicBoundaryShadowUp$ row naming the finite boundary-shadow read.
\item $V$ is the $\HyperbolicVisualMetricUp$ row naming the visual-metric comparison consumed by the phase distance.
\item $O$ is the $\HyperbolicObserverExpansionUp$ row anchoring the observer-side expansion route.
\item $\Phi$ is the phase-distance row, read through $\PhaseDistanceObserverExpansionUp$ and the complex phase-surface fiber.
\item $R$ is the regular real-readback row, displaying the $\RegSeqRatUp$ and $\DyadicRatCoreUp$ tolerance windows used by the linearized comparison.
\item $E$ is the $\RealUp$ seal row for the displayed distance value.
\item $H$ records componentwise $\hsame$ transport for boundary shadow, visual metric, observer expansion, phase distance, real readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from boundary-shadow admission through visual-metric comparison and phase-distance readback to the Real seal.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicLinearizedPhaseDistanceUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicVisualMetricUp$, $\HyperbolicObserverExpansionUp$, $\PhaseDistanceObserverExpansionUp$, $\ComplexPhaseSurfaceFiberUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicLinearizedPhaseDistanceUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $B,V,O,\Phi,R,E,H,C,P,N$. It has no coordinate for a host hyperbolic metric space, global visual boundary, differentiable phase flow, quotient geodesic equality, selected observer point, or hidden real-distance oracle.
\end{definition}

\begin{theorem}[Hyperbolic linearized phase-distance NameCert obligations]
\label{thm:hyperbolic-linearized-phase-distance-namecert-obligations}
For every accepted $\HyperbolicLinearizedPhaseDistanceUp$ carrier $L=(B,V,O,\Phi,R,E,H,C,P,N)$, the local $\NameCert_{\HyperbolicLinearizedPhaseDistanceUp}$ surface consists exactly of boundary-shadow exposure through $B$, visual-metric exposure through $V$, observer-expansion anchoring through $O$, phase-distance readback through $\Phi$, regular real readback through $R$, Real-seal handoff through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-linearized-phase-distance-carrier}. The geometric rows are exactly the boundary-shadow, visual-metric, observer-expansion, phase-distance, real-readback, and Real-seal rows.

The distance read is linearized only as a finite route. Row $B$ supplies the boundary shadow, $V$ supplies the visual comparison, $O$ anchors the observer-expansion route, $\Phi$ records the phase-distance readback, and $R,E$ expose the regular rational and Real-seal interface.

The structural rows $H,C,P,N$ preserve the same route under transport, replay, provenance, and local naming. Since the carrier has no coordinate for a host hyperbolic metric space, global visual boundary, differentiable phase flow, quotient geodesic equality, selected observer point, or hidden real-distance oracle, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Hyperbolic linearized phase-distance observer route]
\label{thm:hyperbolic-linearized-phase-distance-observer-route}
Every observer-expansion consumer of an accepted $\HyperbolicLinearizedPhaseDistanceUp$ packet reads the distance route through $\HyperbolicBoundaryShadowUp$, $\HyperbolicVisualMetricUp$, $\HyperbolicObserverExpansionUp$, $\PhaseDistanceObserverExpansionUp$, $\ComplexPhaseSurfaceFiberUp$, and $\RealUp$ rows before it receives a public comparison value. The handoff exports no host hyperbolic space, global visual boundary, quotient geodesic equality, selected observer point, or differentiable phase-flow theorem.
\end{theorem}
\begin{proof}
Start with the boundary and visual rows. The rows $B$ and $V$ are the only boundary-shadow and visual-metric data admitted by the carrier, so an observer-expansion consumer cannot read a distance value before those rows are displayed.

Next read the observer and phase rows. The row $O$ anchors the focused expansion route, while $\Phi$ records the phase-distance readback against the complex phase-surface fiber rather than against a hidden geometric object.

Finally pass through $R,E,H,C,P,N$. The regular readback and Real seal expose the public comparison value, and the structural rows preserve the route. Thus the observer route remains finite NameCert data and not any excluded host-level geometry.
\end{proof}

\falsifiablePrediction{A $\HyperbolicLinearizedPhaseDistanceUp$ packet is rejected whenever it cannot display boundary-shadow, visual-metric, observer-expansion, phase-distance, regular readback, Real-seal, hsame, Cont, Pkg, and local NameCert rows before exporting a comparison value.}

\independenceWitness{hyperbolic-boundary-shadow, hyperbolic-visual-metric, hyperbolic-observer-expansion, phase-distance-observer-expansion, complex-phase-surface-fiber: no listed sibling is bijective to $\HyperbolicLinearizedPhaseDistanceUp$ because this packet jointly carries boundary shadow, visual metric, observer expansion, phase-distance readback, complex phase-fiber anchoring, Real seal, transport, replay, provenance, and local naming rows.}

The Lean TasteGate route for this packet is planned to expose Nontrivial and FieldFaithful obligations for \texttt{BEDC.Derived.HyperbolicLinearizedPhaseDistanceUp}; those obligations must preserve the displayed boundary-shadow, visual-metric, Real-seal, $\Cont$, $\Pkg$, and local $\NameCert$ rows.

\closureat{HyperbolicLinearizedPhaseDistanceUp}{seedStr}

\begin{closurestatus}{\HyperbolicLinearizedPhaseDistanceUp}
  \constructivestory{A $\HyperbolicLinearizedPhaseDistanceUp$ packet is generated from a boundary-shadow row, visual-metric row, observer-expansion row, phase-distance readback, regular real-readback row, Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-linearized-phase-distance-carrier}, \autoref{thm:hyperbolic-linearized-phase-distance-namecert-obligations}, and \autoref{thm:hyperbolic-linearized-phase-distance-observer-route} seed the finite linearized phase-distance packet over boundary-shadow, visual-metric, observer-expansion, phase-distance, Real-seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host hyperbolic metric spaces, global visual boundaries, differentiable phase flows, quotient geodesic equality, selected observer points, hidden real-distance oracles, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked boundary-shadow exposure, visual-metric exposure, observer-expansion anchoring, phase-distance readback, Real-seal nonescape, transport, replay, provenance, local naming, and TasteGate field faithfulness for BEDC.Derived.HyperbolicLinearizedPhaseDistanceUp.}
\end{closurestatus}
