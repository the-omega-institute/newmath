\chapter{A Concrete Naming Certificate for $\MetaCICResidualConfluenceFuelUp$}
\label{ch:concrete-instances-metacic-residual-confluence-fuel-namecert}
\origin{ai}

The $\MetaCICResidualConfluenceFuelUp$ packet realizes the residual-fuel inspection boundary named by the MetaCIC confluence audit map in \autoref{ch:metacic-confluence-audit-map}. It sits beside the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}, the critical-path packet of \autoref{ch:concrete-instances-metaciccriticalpath-namecert}, the normalization frontier of \autoref{ch:concrete-instances-metacicnormalizationfrontier-namecert}, the residual-substitution boundary of \autoref{ch:concrete-instances-metacic-residual-substitution-boundary-namecert}, and the confluence audit packet of \autoref{ch:concrete-instances-metacic-confluence-audit-packet-namecert}. The carrier records bounded critical-pair and residual-substitution inspection data without claiming arbitrary closed Church--Rosser, subject reduction, quotient equality of terms, evaluator equality, or kernel consistency.

\begin{definition}[MetaCIC residual confluence fuel carrier]
\label{def:metacic-residual-confluence-fuel-carrier}
A $\MetaCICResidualConfluenceFuelUp$ carrier is a finite $\BHist$ packet
$$
F=(T,K,R,J,B,S,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the typed-window row naming the closed source and the bounded syntax fragment under inspection;
\item $K$ is the critical-pair row naming the local peak or overlap to be checked;
\item $R$ is the residual-substitution boundary row retained from the conditional confluence interface;
\item $J$ is the candidate local-join row, available only under the displayed residual boundary;
\item $B$ is the finite fuel budget for residual, substitution, and critical-pair inspection;
\item $S$ is the closed checker or normal-fragment sibling row consumed by the bounded inspection;
\item $O$ is the obstruction row retaining refused endpoints when the residual boundary is not discharged;
\item $H$ records componentwise $\hsame$ transport for typed-window, critical-pair, residual, join, budget, checker, obstruction, replay, provenance, and local naming rows;
\item $C$ records displayed $\Cont$ replay through the same rows;
\item $P$ is the $\Pkg$ provenance over $\MetaCICResidualConfluenceFuelUp$, the five cited MetaCIC sibling packets, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\MetaCICResidualConfluenceFuelUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $T,K,R,J,B,S,O,H,C,P,N$. It does not identify a bounded inspection with a full parallel diamond, closed Church--Rosser theorem, subject-reduction theorem, evaluator equality, quotient equality, or kernel-consistency claim.
\end{definition}

\begin{theorem}[MetaCIC residual fuel critical-pair boundary]
\label{thm:metacic-residual-confluence-fuel-critical-pair}
Every accepted $\MetaCICResidualConfluenceFuelUp$ carrier exposes the critical-pair row $K$, residual-substitution boundary $R$, candidate local-join row $J$, and finite fuel row $B$ before any confluence-facing consumer may read the checker row $S$ or obstruction row $O$. The inspection is therefore bounded by
$$
T,K,R,J,B,S,O,H,C,P,N
$$
rather than by an ambient closed Church--Rosser endpoint.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-residual-confluence-fuel-carrier}. The typed source, local peak, residual boundary, candidate join, and fuel budget are the only rows that can authorize a bounded residual inspection.

The checker row $S$ is downstream of those rows. It can certify a bounded inspection result for the displayed source and fuel, but it cannot erase the residual-substitution boundary $R$ or the obstruction row $O$.

Transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$ preserve that finite boundary. Hence a confluence-facing consumer receives a fuel-bounded inspection surface, not a premise-free Church--Rosser theorem.
\end{proof}

\begin{theorem}[MetaCIC residual fuel non-escape]
\label{thm:metacic-residual-confluence-fuel-non-escape}
An accepted $\MetaCICResidualConfluenceFuelUp$ packet cannot export arbitrary closed Church--Rosser, full parallel diamond, subject reduction, evaluator equality, quotient equality of terms, or kernel consistency. Any such read must first supply a separate discharge for the residual-substitution boundary and the typed critical-pair frontier named by $R$ and $K$.
\end{theorem}
\begin{proof}
Use the critical-pair boundary theorem. It forces every confluence-facing read through the typed window, critical-pair row, residual-substitution row, candidate join, finite fuel, checker row, and obstruction row before structural replay is available.

The refused endpoints require data that are not stored in the carrier. A full parallel diamond needs a discharged residual-substitution compatibility input; subject reduction needs its own typing-preservation surface; evaluator equality, quotient equality, and kernel consistency require separate public commitments.

Since $H,C,P,N$ only transport, replay, source, and name the same finite inspection packet, none of those stronger endpoints can be recovered from $\MetaCICResidualConfluenceFuelUp$ alone.
\end{proof}

The Lean TasteGate follow-up path for $\MetaCICResidualConfluenceFuelUp$ must include explicit $\mathsf{Nontrivial}\ \MetaCICResidualConfluenceFuelUp$ and $\mathsf{FieldFaithful}\ \MetaCICResidualConfluenceFuelUp$ instances. These instances must distinguish the typed window, critical-pair, residual-substitution boundary, candidate join, fuel budget, checker, obstruction, transport, replay, provenance, and local NameCert rows.

\falsifiablePrediction{If the residual-substitution boundary or critical-pair row is absent, a bounded MetaCIC residual-fuel inspection cannot be cited as a confluence endpoint.}

\independenceWitness{metacic-parallel-diamond-frontier, metaciccriticalpath, metacicnormalizationfrontier, metacic-residual-substitution-boundary, metacicconfluenceauditpacket: none is bijective because $\MetaCICResidualConfluenceFuelUp$ jointly carries typed-window, critical-pair, residual-boundary, local-join, finite-fuel, checker, obstruction, transport, replay, provenance, and local NameCert rows as one bounded inspection packet.}

\closureat{MetaCICResidualConfluenceFuelUp}{seedStr}

\begin{closurestatus}{\MetaCICResidualConfluenceFuelUp}
  \constructivestory{A $\MetaCICResidualConfluenceFuelUp$ packet is generated from a typed-window row, critical-pair row, residual-substitution boundary, candidate local-join row, finite fuel budget, closed checker row, obstruction row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-residual-confluence-fuel-carrier}, \autoref{thm:metacic-residual-confluence-fuel-critical-pair}, and \autoref{thm:metacic-residual-confluence-fuel-non-escape} seed the bounded critical-pair and residual-substitution inspection surface for the MetaCIC confluence boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close arbitrary closed Church--Rosser, full parallel diamond, subject reduction, evaluator equality, quotient equality of terms, kernel consistency, Lean verification, or a public standard bridge.}
  \upgradepath{Formal closure requires checked carrier admission, critical-pair exactness, residual-boundary retention, local-join fuel accounting, checker replay, obstruction preservation, TasteGate nontriviality, and field faithfulness.}
\end{closurestatus}
