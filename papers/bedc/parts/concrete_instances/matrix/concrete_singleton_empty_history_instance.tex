\section{Concrete singleton empty-history matrix instance}
\label{sec:matrix-concrete-singleton-instance}

\begin{definition}[Concrete singleton empty-history matrix instance]
\label{def:singleton-empty-history-matrix-instance}
Let $A_{\emp}$ be the singleton history source of
Definition~\ref{def:list-singleton-history-source} at $a_0=\emp$. Use the
classifier
$$
\rho_{\emp}(h,k)\Longleftrightarrow
A_{\emp}(h)\land A_{\emp}(k)\land\hsame(h,k).
$$
The row-dimension witness and column-dimension witness are both represented by
the empty unary history $\emp$. The scalar side is the singleton empty-history
ring of Definition~\ref{def:singleton-empty-history-ring-instance}, and the
row and column vector layers are read through the singleton empty-history
vector-space instance of
Definition~\ref{def:singleton-empty-history-vecspace-instance}.

The matrix carrier is the same singleton source $A_{\emp}$ on
$\mathsf{BHist}$ codes. Define the displayed matrix operations by
$$
0_M:=\emp,\qquad I_M:=\emp,\qquad
M\oplus N:=\mathsf{append}(M,N),\qquad
M\odot N:=\mathsf{append}(M,N).
$$
The certificate fit has ten fields:
\begin{enumerate}
  \item Traditional target: the terminal singleton matrix algebra.
  \item BEDC source: $\mathsf{BHist}$ together with the singleton ring and
  $\VecSpaceUp$ layers.
  \item Carrier choice: $A_{\emp}$.
  \item Classifier choice: $\rho_{\emp}$.
  \item Stability obligations: closure, zero laws, identity laws,
  associativity, and congruence for the displayed operations.
  \item Ledger: the singleton endpoint together with row and column witnesses
  represented by $\emp$.
  \item Core theorem list:
  Theorem~\ref{thm:singleton-empty-history-matrix-laws}.
  \item Exactness predicate: singleton endpoint classification.
  \item Standard bridge: the terminal matrix object.
  \item Status: $\mathsf{ProofTarget}$.
\end{enumerate}
\end{definition}

\begin{definition}[Matrix singleton power]
\label{def:matrix-singleton-pow}
For histories $M$ and $e$, $\mathsf{MatrixSingletonPow}(M,e)$ is the concrete
power ledger obtained by recursion over the exponent history: the empty
exponent returns $I_M$, a one-headed exponent multiplies the previous ledger
by $M$, and the zero-headed branch returns the singleton zero endpoint.
\end{definition}
\leandef{BEDC.Derived.MatrixUp.MatrixSingletonPow}

\begin{theorem}[Matrix singleton power carrier closure]
\label{thm:matrix-singleton-pow-carrier-closed}
If $M$ is carried by the singleton matrix carrier and $e$ is unary, then the
power ledger is again carried by the singleton matrix carrier:
$$
A_{\emp}(M)\land \Unary(e)
\quad\Longrightarrow\quad
A_{\emp}(\mathsf{MatrixSingletonPow}(M,e)).
$$
\end{theorem}
\begin{proof}
Proceed by induction on the unary exponent. The empty exponent returns
$I_M=\emp$, hence is carried by $A_{\emp}$. In the one-headed case, the
induction hypothesis carries the previous power ledger, while the hypothesis
carries $M$; the displayed multiplication is append, so append empty-result
exactness carries the product ledger.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_carrier\_closed}

\begin{theorem}[Matrix singleton power nonempty unary input carrier exactness]
\label{thm:matrix-singleton-pow-carrier-nonempty-unary-input}
If the exponent $e$ is unary and not history-same as $\emp$, then the singleton
carrier condition on the power ledger is exactly the singleton carrier
condition on the input matrix:
$$
\Unary(e)\land(\hsame(e,\emp)\to\bot)
\quad\Longrightarrow\quad
\bigl(
A_{\emp}(\mathsf{MatrixSingletonPow}(M,e))
\Longleftrightarrow
A_{\emp}(M)
\bigr).
$$
\end{theorem}
\begin{proof}
The unary, nonempty exponent has a one-headed form. In that branch the power
ledger is the displayed append product of the previous power ledger and $M$.
Append empty-result exactness therefore recovers $A_{\emp}(M)$ from the carried
power ledger. Conversely, carrier closure for singleton powers carries the
power ledger from $A_{\emp}(M)$ and $\Unary(e)$.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_carrier\_nonempty\_unary\_input\_iff}

\begin{theorem}[Matrix singleton power nonempty unary suffix base carrier]
\label{thm:matrix-singleton-pow-nonempty-unary-suffix-base-carrier}
If $tail$ is unary and not history-same as $\emp$, then a singleton carrier
witness for the power ledger over $\mathsf{append}(e,tail)$ recovers the base
matrix carrier:
$$
\Unary(tail)\land(\hsame(tail,\emp)\to\bot)\land
A_{\emp}(\mathsf{MatrixSingletonPow}(M,\mathsf{append}(e,tail)))
\quad\Longrightarrow\quad
A_{\emp}(M).
$$
\end{theorem}
\begin{proof}
The unary, nonempty suffix cannot be $\emp$ and cannot start with $\Ezero$.
Thus it has the form $\Eone(t)$, so the appended exponent also exposes a
one-headed power branch. In that branch the power ledger is the append product
of the previous power ledger and $M$, and append empty-result inversion
projects the right factor carrier $A_{\emp}(M)$.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_nonempty\_unary\_suffix\_base\_carrier}

\begin{theorem}[Matrix singleton positive-exponent continuation readback]
\label{thm:matrix-singleton-pow-positive-exponent-cont-readback}
If the exponent $e$ is unary and not history-same as $\emp$, then its
one-headed tail exposes the concrete successor continuation for the power
ledger:
$$
\Unary(e)\land(\hsame(e,\emp)\to\bot)
\quad\Longrightarrow\quad
\exists t.\ \Unary(t)\land
\ContR(\mathsf{MatrixSingletonPow}(M,t),M,\mathsf{MatrixSingletonPow}(M,e)).
$$
\end{theorem}
\begin{proof}
Unary nonempty histories have a one-headed tail. In that branch the recursive
definition of $\mathsf{MatrixSingletonPow}$ is the append product of the
predecessor power ledger with $M$, so the continuation is the displayed append
step.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_positive\_exponent\_cont\_readback}

\begin{theorem}[Matrix singleton positive-exponent empty continuation base carrier]
\label{thm:matrix-singleton-pow-positive-exponent-empty-continuation-base-carrier}
If \(e\) is unary and not history-same as \(\emp\), and a continuation from
\(\mathsf{MatrixSingletonPow}(M,e)\) has result history-same to \(\emp\), then
the base matrix \(M\) lies in the singleton matrix carrier:
$$
A_{\emp}(M).
$$
\end{theorem}
\begin{proof}
Transport the continuation result to the concrete empty endpoint and invert
the empty-result continuation. This carries the power ledger itself by
\(A_{\emp}\). The nonempty unary power carrier exactness theorem then reads
the carriedness of the base matrix \(M\) back from the carried power ledger.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_positive\_exponent\_empty\_continuation\_base\_carrier}

\begin{theorem}[Matrix singleton power append positive suffix continuation readback]
\label{thm:matrix-singleton-pow-append-positive-suffix-cont-readback}
If $suffix$ is unary and not history-same as $\emp$, then the power ledger
over $\mathsf{append}(pref,suffix)$ exposes the successor continuation at the
same prefix:
$$
\exists tail.\ \Unary(tail)\land
\ContR(\mathsf{MatrixSingletonPow}(M,\mathsf{append}(pref,tail)),M,
\mathsf{MatrixSingletonPow}(M,\mathsf{append}(pref,suffix))).
$$
\end{theorem}
\begin{proof}
Unary nonempty suffixes have a one-headed tail. After substituting
$suffix=\Eone(tail)$, the appended exponent is itself one-headed with payload
$\mathsf{append}(pref,tail)$. The recursive power ledger therefore unfolds to
the displayed continuation by $M$.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_append\_positive\_suffix\_cont\_readback}

\begin{theorem}[Matrix singleton power append positive suffix classifier readback]
\label{thm:matrix-singleton-pow-append-positive-suffix-classifier-readback}
If $M$ is carried by the singleton matrix carrier, $pref$ and $suffix$ are
unary, and $suffix$ is not history-same as $\emp$, then the append-positive
suffix readback supplies a unary tail whose successor multiplication classifies
the original appended power ledger:
$$
\exists tail.\ \Unary(tail)\land
\rho_{\emp}\bigl(\mathsf{MatrixSingletonPow}(M,\mathsf{append}(pref,suffix)),
\mathsf{MatrixSingletonMul}(\mathsf{MatrixSingletonPow}(M,\mathsf{append}(pref,tail)),M)\bigr).
$$
\end{theorem}
\begin{proof}
The positive-suffix readback exposes a unary tail and the continuation from
$\mathsf{MatrixSingletonPow}(M,\mathsf{append}(pref,tail))$ by $M$ to the
original appended power ledger. Carrier closure gives the source power carrier,
and append empty-result exactness carries the displayed multiplication endpoint.
The continuation equation gives the classifier comparison.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_append\_positive\_suffix\_classifier\_readback}

\begin{theorem}[Matrix singleton power successor classifier]
\label{thm:matrix-singleton-pow-succ-classifier}
For histories $M,e:\mathsf{BHist}$, if $M$ is carried by the singleton matrix
carrier and $e$ is unary, then the one-headed power ledger is classified
against the displayed successor multiplication:
$$
A_{\emp}(M)\land\Unary(e)
\quad\Longrightarrow\quad
\rho_{\emp}\bigl(\mathsf{MatrixSingletonPow}(M,\Eone(e)),
\mathsf{MatrixSingletonMul}(\mathsf{MatrixSingletonPow}(M,e),M)\bigr).
$$
\end{theorem}
\begin{proof}
The one-headed power branch unfolds to the displayed multiplication endpoint.
The carrier closure theorem carries the predecessor power ledger, and the
input hypothesis carries $M$. Since multiplication is append, append
empty-result exactness carries the displayed multiplication endpoint, and
reflexivity of $\hsame$ supplies the classifier comparison.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_succ\_classifier}

\begin{theorem}[Matrix singleton power successor continuation classifier]
\label{thm:matrix-singleton-pow-succ-continuation-classifier}
For histories $M,e,r:\mathsf{BHist}$, if $M$ is carried by the singleton matrix
carrier, $e$ is unary, and
$$
\ContR(\mathsf{MatrixSingletonPow}(M,e),M,r),
$$
then the one-headed power ledger is classified against $r$:
$$
\rho_{\emp}\bigl(\mathsf{MatrixSingletonPow}(M,\Eone(e)),r\bigr).
$$
\end{theorem}
\begin{proof}
Carrier closure carries the predecessor power ledger. Together with the carrier
of $M$, append empty-result exactness carries the one-headed power endpoint.
The displayed continuation identifies $r$ with the same append endpoint, so
$r$ is carried by the singleton matrix carrier and is $\hsame$ to the
one-headed power ledger.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_succ\_continuation\_classifier}

\begin{theorem}[Matrix singleton power visible-base successor continuation empty-result absurd]
\label{thm:matrix-singleton-pow-visible-base-succ-continuation-empty-result-absurd}
For any singleton-carried continuation right input $y$, a successor power whose
base has a visible head cannot continue to the empty result:
$$
\ContR(\mathsf{MatrixSingletonPow}(\Ezero(m),\Eone(e)),y,r)
\land r\hsame\emp\Longrightarrow\bot,
$$
and the same statement holds with $\Eone(m)$ in place of $\Ezero(m)$.
\end{theorem}
\begin{proof}
Transport the displayed continuation along $r\hsame\emp$ and invert the empty
continuation result. The source side is the successor power endpoint, which
unfolds to an append whose right factor is the visible base. Append
empty-result inversion would identify $\Ezero(m)$, respectively $\Eone(m)$,
with $\emp$, contradicting BEDC constructor separation.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_visible\_base\_succ\_continuation\_empty\_result\_absurd}

\begin{theorem}[Matrix singleton power positive-exponent visible-base empty-result exclusion]
\label{thm:matrix-singleton-pow-positive-exponent-visible-base-continuation-empty-result-absurd}
For unary nonempty exponent $e$, a singleton matrix power whose base has a
visible head cannot continue to an empty result:
$$
\ContR(\mathsf{MatrixSingletonPow}(\Ezero(m),e),y,r)
\land r\hsame\emp\Longrightarrow\bot,
$$
and the same statement holds with $\Eone(m)$ in place of $\Ezero(m)$.
\end{theorem}
\begin{proof}
Transport the continuation along $r\hsame\emp$ and invert the empty result.
The nonempty-unary power carrier exactness theorem then turns the empty power
endpoint into a singleton carrier witness for the visible base, contradicting
constructor separation.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_positive\_exponent\_visible\_base\_continuation\_empty\_result\_absurd}

\begin{theorem}[Matrix singleton power nonempty-base empty-result exclusion]
\label{thm:matrix-singleton-pow-nonempty-base-continuation-result-absurd}
For unary nonempty exponent $e$, if the base matrix $M$ is not singleton-carried, then
$$
\ContR(\mathsf{MatrixSingletonPow}(M,e),y,r)\land r\hsame\emp\Longrightarrow\bot.
$$
\end{theorem}
\begin{proof}
Transport the continuation result to $\emp$ and invert the empty continuation. The source carrier for $\mathsf{MatrixSingletonPow}(M,e)$ transfers through positive-unary power exactness to a singleton carrier witness for $M$, contradicting the base non-carrier hypothesis.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_nonempty\_base\_continuation\_result\_absurd}

\begin{theorem}[Matrix singleton power append exponent classifier]
\label{thm:matrix-singleton-pow-append-exponent-classifier}
If $M$ is carried by the singleton matrix carrier and $w,q$ are unary
histories, then the power ledger over the appended exponent is classified with
the product of the two factor power ledgers:
$$
A_{\emp}(M)\land\Unary(w)\land\Unary(q)
\quad\Longrightarrow\quad
\rho_{\emp}\bigl(\mathsf{MatrixSingletonPow}(M,\mathsf{append}(w,q)),
\mathsf{MatrixSingletonMul}(\mathsf{MatrixSingletonPow}(M,w),
\mathsf{MatrixSingletonPow}(M,q))\bigr).
$$
\end{theorem}
\begin{proof}
Append closure makes $\mathsf{append}(w,q)$ unary, so power carrier closure
carries the left ledger. The same closure carries each factor ledger; append
empty-result exactness then carries their displayed product. Since both
endpoints are carried by $A_{\emp}$, transitivity through $\emp$ supplies the
classifier comparison.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_append\_exponent\_classifier}

\begin{theorem}[Matrix singleton power append successor right exponent classifier]
\label{thm:matrix-singleton-pow-append-succ-right-exponent-classifier}
If $M$ is carried by the singleton matrix carrier and $w,q$ are unary histories,
then the append exponent with right successor is classified by the left power
and the nested right multiplication endpoint:
$$
\rho_{\emp}\bigl(\mathsf{MatrixSingletonPow}(M,\mathsf{append}(w,\Eone(q))),
\mathsf{MatrixSingletonMul}(\mathsf{MatrixSingletonPow}(M,w),
\mathsf{MatrixSingletonMul}(\mathsf{MatrixSingletonPow}(M,q),M))\bigr).
$$
\end{theorem}
\begin{proof}
Unary closure carries $\mathsf{append}(w,\Eone(q))$. Singleton power closure
carries the composite power, the left power, and the right predecessor power.
Append empty-result exactness carries the inner multiplication by $M$ and then
the outer multiplication by the left power. Since both endpoints are carried by
$A_{\emp}$, transitivity through $\emp$ supplies the classifier comparison.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_append\_succ\_right\_exponent\_classifier}

\begin{theorem}[Matrix singleton power append exponent commuted classifier]
\label{thm:matrix-singleton-pow-append-exponent-comm-classifier}
If $M$ is carried by the singleton matrix carrier and $w,q$ are unary
histories, then the two power ledgers obtained by the two append orders of the
exponent histories are singleton-classified:
$$
A_{\emp}(M)\land\Unary(w)\land\Unary(q)
\quad\Longrightarrow\quad
\rho_{\emp}\bigl(\mathsf{MatrixSingletonPow}(M,\mathsf{append}(w,q)),
\mathsf{MatrixSingletonPow}(M,\mathsf{append}(q,w))\bigr).
$$
\end{theorem}
\begin{proof}
Unary append closure makes both $\mathsf{append}(w,q)$ and
$\mathsf{append}(q,w)$ unary. Singleton power carrier closure carries both
power ledgers from $A_{\emp}(M)$. Since both endpoints are carried by
$A_{\emp}$, transitivity through $\emp$ supplies the classifier comparison.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_append\_exponent\_comm\_classifier}

\begin{theorem}[Matrix singleton power append exponent classifier iff]
\label{thm:matrix-singleton-pow-append-exponent-classifier-iff}
If $M$ is carried by the singleton matrix carrier and $a,b$ are unary
histories, then the classifier of the append-exponent power ledger against
$h$ is exactly the singleton carrier condition on $h$:
$$
\mathsf{MatrixSingletonClassifier}
  (\mathsf{MatrixSingletonPow}(M,\mathsf{append}(a,b)),h)
\Longleftrightarrow
\mathsf{MatrixSingletonCarrier}(h).
$$
\end{theorem}
\begin{proof}
Unary closure under append makes $\mathsf{append}(a,b)$ a unary exponent. The
power carrier theorem therefore carries
$\mathsf{MatrixSingletonPow}(M,\mathsf{append}(a,b))$. The forward direction
reads the right carrier field from the classifier. In the reverse direction,
that carried power endpoint and the carrier witness for $h$ assemble the
restricted singleton classifier.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_append\_exponent\_classifier\_iff}

\begin{theorem}[Matrix singleton power successor endpoint exactness]
\label{thm:matrix-singleton-pow-successor-endpoint-exactness}
For histories $M,e:\mathsf{BHist}$, if $A_{\emp}(M)$ and $\Unary(e)$, then
the successor power is carried:
$$
A_{\emp}(\mathsf{MatrixSingletonPow}(M,\Eone(e))).
$$
Moreover, the successor power is exactly classified with both displayed
singleton multiplication endpoints:
$$
\rho_{\emp}(\mathsf{MatrixSingletonPow}(M,\Eone(e)),
\mathsf{MatrixSingletonPow}(M,e)\odot M)
\quad\text{and}\quad
\rho_{\emp}(\mathsf{MatrixSingletonPow}(M,\Eone(e)),
M\odot\mathsf{MatrixSingletonPow}(M,e)).
$$
\end{theorem}
\begin{proof}
Unfold the one-headed exponent clause of $\mathsf{MatrixSingletonPow}$. The
successor ledger is the displayed singleton product
$\mathsf{MatrixSingletonPow}(M,e)\odot M$.

The carrier closure theorem for powers gives
$A_{\emp}(\mathsf{MatrixSingletonPow}(M,e))$ from $A_{\emp}(M)$ and
$\Unary(e)$. Combining this carrier with $A_{\emp}(M)$, the singleton
multiplication closure in
Theorem~\ref{thm:singleton-empty-history-matrix-laws} carries both append
endpoints
$\mathsf{MatrixSingletonPow}(M,e)\odot M$ and
$M\odot\mathsf{MatrixSingletonPow}(M,e)$.

For the first classifier, the unfolded successor ledger is the same append
endpoint, so the singleton classifier closes by reflexivity and the carried
endpoint witness. For the second classifier, both append endpoints classify as
empty singleton endpoints; transitivity through $\emp$ compares them and
supplies $\rho_{\emp}$.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_succ\_endpoint\_exactness}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonPow\_successor\_endpoint\_exactness}

\begin{theorem}[Matrix singleton classifier append split empty]
\label{thm:matrix-singleton-classifier-append-split-empty}
For $M,N,h:\mathsf{BHist}$,
$$
\rho_{\emp}(\mathsf{append}(M,N),h)
\quad\Longleftrightarrow\quad
M\hsame\emp\land N\hsame\emp\land A_{\emp}(h).
$$
\end{theorem}
\begin{proof}
The forward direction reads the left carrier component of
$\rho_{\emp}(\mathsf{append}(M,N),h)$ as
$\mathsf{append}(M,N)\hsame\emp$. Empty-result inversion for append splits this
endpoint witness into $M\hsame\emp$ and $N\hsame\emp$, while the right carrier
component supplies $A_{\emp}(h)$.

Conversely, $M\hsame\emp$ and $N\hsame\emp$ reconstruct the singleton carrier
witness for $\mathsf{append}(M,N)$. Together with $A_{\emp}(h)$, transitivity of
$\hsame$ through $\emp$ gives the classifier comparison.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonClassifier\_append\_split\_empty\_iff}

\begin{theorem}[Matrix singleton add-mul classifier iff]
\label{thm:matrix-singleton-add-mul-classifier-iff}
For $M,N:\mathsf{BHist}$, the singleton matrix classifier relates the displayed
addition and multiplication results exactly when both inputs are singleton
empty endpoints:
$$
\rho_{\emp}(M\oplus N,M\odot N)
\quad\Longleftrightarrow\quad
M\hsame\emp\land N\hsame\emp.
$$
\end{theorem}
\begin{proof}
Both displayed operations are the same BEDC append spine
$\mathsf{append}(M,N)$. In the forward direction, the left carrier component
of $\rho_{\emp}(M\oplus N,M\odot N)$ says that this append spine is
$\hsame$ to $\emp$. Empty-result inversion for append splits it into the two
endpoint witnesses $M\hsame\emp$ and $N\hsame\emp$.

Conversely, the two endpoint witnesses reconstruct the singleton carrier for
$\mathsf{append}(M,N)$. Since $M\oplus N$ and $M\odot N$ unfold to that same
append spine, reflexivity of $\hsame$ supplies the classifier relation between
the displayed operation results.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonAddMul\_classifier\_iff}

\begin{theorem}[Matrix singleton add-mul continuation result iff]
\label{thm:matrix-singleton-addmul-continuation-result-iff}
For singleton-carrier inputs $M$ and $N$, a continuation from the displayed
addition result to the displayed multiplication result has result $R$ exactly
when $R$ is itself a singleton carrier:
$$
\ContR(M\oplus N,M\odot N,R)
\quad\Longleftrightarrow\quad
A_{\emp}(R).
$$
\end{theorem}
\begin{proof}
The two displayed operations both unfold to the same append spine
$\mathsf{append}(M,N)$. The singleton witnesses for $M$ and $N$ collapse that
spine to $\emp$. The forward direction compares the displayed continuation
with the right-unit continuation at $\emp$; the reverse direction transports
the result carrier back to that same empty continuation.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonAddMul\_continuation\_result\_iff}

\begin{theorem}[Matrix singleton add-mul continuation empty-result factors]
\label{thm:matrix-singleton-addmul-continuation-empty-result-factors}
For histories $M,N,R:\mathsf{BHist}$, if
$\ContR(M\oplus N,M\odot N,R)$, then the displayed continuation result is
empty exactly when both matrix inputs are empty:
$$
R\hsame\emp
\quad\Longleftrightarrow\quad
M\hsame\emp\land N\hsame\emp.
$$
\end{theorem}
\begin{proof}
For the forward direction, transport the displayed continuation along
$R\hsame\emp$ and invert the empty continuation result. The source factor is
$M\oplus N$, which unfolds to $\mathsf{append}(M,N)$, so append empty-result
inversion gives $M\hsame\emp$ and $N\hsame\emp$.

Conversely, the two input endpoint witnesses collapse both displayed
operations to the empty history. Continuation determinacy compares the given
continuation with the BEDC right-unit continuation at $\emp$, identifying the
result with $\emp$.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonAddMul\_continuation\_empty\_result\_factors\_iff}

\begin{theorem}[Matrix singleton add-mul visible-left continuation result nonempty]
\label{thm:matrix-singleton-addmul-visible-left-continuation-result-nonempty}
For any histories $m,N,R$, if the shared left input of the displayed singleton
addition and multiplication has a visible head, then a continuation from the
addition endpoint to the multiplication endpoint cannot have empty result:
$$
\ContR(\Ezero(m)\oplus N,\Ezero(m)\odot N,R)\land R\hsame\emp
\Longrightarrow\bot,
$$
and the same statement holds with $\Eone(m)$ in place of $\Ezero(m)$.
\end{theorem}
\begin{proof}
Apply the continuation empty-result factor theorem for displayed singleton
addition and multiplication. An empty result forces the left input factor to
be empty. This contradicts constructor separation for $\Ezero(m)$ in the first
case and for $\Eone(m)$ in the second case.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonAddMul\_visible\_left\_continuation\_result\_nonempty}

\begin{theorem}[Matrix singleton classifier visible right append exclusion]
\label{thm:matrix-singleton-classifier-append-visible-right-absurd}
For histories $h,p,q:\mathsf{BHist}$, a singleton matrix classifier witness
cannot target an append whose right factor has a visible head:
$$
\rho_{\emp}(h,\mathsf{append}(p,\mathsf{e0}(q)))\to\bot
\qquad\text{and}\qquad
\rho_{\emp}(h,\mathsf{append}(p,\mathsf{e1}(q)))\to\bot.
$$
\end{theorem}
\begin{proof}
Unfold the singleton matrix classifier. A classifier witness supplies the
target carrier endpoint
$\mathsf{append}(p,\mathsf{e0}(q))\hsame\emp$ in the first case, or
$\mathsf{append}(p,\mathsf{e1}(q))\hsame\emp$ in the second case. Empty-result
inversion for append forces the visible right factor itself to be $\emp$,
contradicting the disjointness of each visible constructor from $\emp$.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonClassifier\_append\_visible\_right\_absurd}

\begin{theorem}[Matrix singleton classifier append pair carrier]
\label{thm:matrix-singleton-classifier-append-pair-carrier}
For $M,N,P,Q:\mathsf{BHist}$,
$$
\rho_{\emp}(\mathsf{append}(M,N),\mathsf{append}(P,Q))
\quad\Longleftrightarrow\quad
A_{\emp}(M)\land A_{\emp}(N)\land A_{\emp}(P)\land A_{\emp}(Q).
$$
\end{theorem}
\begin{proof}
The classifier exposes singleton carrier witnesses for both append endpoints.
Empty-result inversion for append splits these witnesses into the four displayed
endpoint carriers. Conversely, the four endpoint carriers rebuild the two empty
append endpoints, and transitivity of $\hsame$ through $\emp$ supplies the
classifier comparison between them.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonClassifier\_append\_pair\_carrier\_iff}

\begin{theorem}[Matrix singleton classifier self-context split]
\label{thm:matrix-singleton-classifier-self-context-split}
For histories $a,x,b:\mathsf{BHist}$,
$$
\rho_{\emp}(\mathsf{append}(a,\mathsf{append}(x,a)),b)
\Longleftrightarrow
A_{\emp}(a)\land\rho_{\emp}(x,b).
$$
\end{theorem}
\begin{proof}
The forward implication splits the source carrier of the classifier twice by
empty-result inversion for append. This gives the carrier witness for the
repeated context $a$ and the carrier witness for the middle history $x$; the
target carrier field and transitivity through $\emp$ rebuild
$\rho_{\emp}(x,b)$.

Conversely, the carrier for $a$ and the source carrier inside
$\rho_{\emp}(x,b)$ rebuild the inner and outer empty append endpoints. The
target carrier is inherited from $\rho_{\emp}(x,b)$, and both endpoints compare
through the empty history.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonClassifier\_append\_self\_context\_split\_iff}

\begin{theorem}[Matrix singleton classifier append-left cancellation]
\label{thm:matrix-singleton-classifier-append-left-cancellation}
For histories $P,Q,R:\mathsf{BHist}$,
$$
\rho_{\emp}(\mathsf{append}(R,P),\mathsf{append}(R,Q))
\Longleftrightarrow
A_{\emp}(R)\land \rho_{\emp}(P,Q).
$$
\end{theorem}
\begin{proof}
In the forward direction, the two carrier fields say that
$\mathsf{append}(R,P)$ and $\mathsf{append}(R,Q)$ are empty. Empty-result
inversion for append splits these witnesses into $R\hsame\emp$,
$P\hsame\emp$, and $Q\hsame\emp$. The shared left endpoint gives
$A_{\emp}(R)$, and the two remaining endpoint witnesses rebuild
$\rho_{\emp}(P,Q)$.

Conversely, unpack $A_{\emp}(R)$ and $\rho_{\emp}(P,Q)$. The shared left
endpoint and each singleton classified endpoint rebuild both empty append
endpoints. Transitivity of $\hsame$ through $\emp$ then supplies the classifier
comparison between the two appended histories.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonClassifier\_append\_left\_cancel\_iff}

\begin{theorem}[Matrix singleton classifier append-right cancellation]
\label{thm:matrix-singleton-classifier-append-right-cancellation}
For histories $P,Q,R:\mathsf{BHist}$,
$$
\rho_{\emp}(\mathsf{append}(P,R),\mathsf{append}(Q,R))
\Longleftrightarrow
\rho_{\emp}(P,Q)\land A_{\emp}(R).
$$
\end{theorem}
\begin{proof}
In the forward direction, the two carrier fields say that
$\mathsf{append}(P,R)$ and $\mathsf{append}(Q,R)$ are empty. Empty-result
inversion for append splits these witnesses into $P\hsame\emp$,
$Q\hsame\emp$, and $R\hsame\emp$. The first two endpoint witnesses rebuild
$\rho_{\emp}(P,Q)$, and the shared right endpoint gives $A_{\emp}(R)$.

Conversely, unpack $\rho_{\emp}(P,Q)$ and $A_{\emp}(R)$. All three displayed
histories are singleton endpoints, so both appended histories reduce to
$\emp$, and the singleton classifier follows by reflexivity of $\hsame$.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonClassifier\_append\_right\_cancel\_iff}

\begin{theorem}[Matrix singleton classifier append common context]
\label{thm:matrix-singleton-classifier-append-common-context}
For histories $L,P,Q,R:\mathsf{BHist}$,
$$
\rho_{\emp}(\mathsf{append}(L,\mathsf{append}(P,R)),
  \mathsf{append}(L,\mathsf{append}(Q,R)))
\Longleftrightarrow
A_{\emp}(L)\land \rho_{\emp}(P,Q)\land A_{\emp}(R).
$$
\end{theorem}
\begin{proof}
In the forward direction, the two classifier carrier fields say that both
common-context append endpoints are empty. Empty-result inversion first splits
off the shared left context $L$, then splits the middle append endpoints. This
yields $A_{\emp}(L)$, $A_{\emp}(P)$, $A_{\emp}(Q)$, and $A_{\emp}(R)$; the
middle comparison $\rho_{\emp}(P,Q)$ follows by transitivity through $\emp$.

Conversely, $A_{\emp}(L)$, $\rho_{\emp}(P,Q)$, and $A_{\emp}(R)$ reconstruct
both common-context append endpoints as empty. Transitivity through $\emp$
then supplies the singleton classifier relation between the displayed
endpoints.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonClassifier\_append\_common\_context\_iff}

\begin{theorem}[Singleton empty-history matrix laws]
\label{thm:singleton-empty-history-matrix-laws}
For the singleton instance of
Definition~\ref{def:singleton-empty-history-matrix-instance}, the displayed
matrix carrier and operations satisfy the concrete $\MatrixUp$ law package:
\begin{enumerate}
  \item $0_M$ and $I_M$ are carried by $A_{\emp}$.
  \item If $A_{\emp}(M)$ and $A_{\emp}(N)$, then
  $A_{\emp}(M\oplus N)$ and $A_{\emp}(M\odot N)$.
  \item $0_M$ is a left and right unit for $\oplus$ under $\rho_{\emp}$.
  \item $I_M$ is a left and right identity for $\odot$ under $\rho_{\emp}$.
  \item $\oplus$ and $\odot$ are associative under $\rho_{\emp}$.
  \item $\rho_{\emp}$ is reflexive, symmetric, and transitive on carried
  matrix codes, and it is respected by $\oplus$ and $\odot$.
\end{enumerate}
\end{theorem}
\begin{proof}
The carrier endpoint is supplied by the singleton source witness from
Definition~\ref{def:list-singleton-history-source} at $a_0=\emp$. The row and
column witnesses use the same empty unary history, so the dimension ledger is
closed at the singleton endpoint.

For operation closure, carried inputs are classified as singleton endpoints.
The displayed operations are either $\emp$ or $\mathsf{append}$ applied to
singleton endpoints. The BEDC continuation unit laws cited in
Theorem~\ref{thm:concrete-history-monoid-laws} identify the resulting append
endpoint with $\emp$, so the singleton source again carries the result.

The zero laws for $\oplus$ and the identity laws for $\odot$ are the same
unit calculation. On the left and on the right, the operation compares
$\mathsf{append}(\emp,M)$ or $\mathsf{append}(M,\emp)$ with $M$ under
$\rho_{\emp}$, and the continuation unit laws provide the $\hsame$ component.
The endpoint components come from $A_{\emp}(M)$ and $A_{\emp}(\emp)$.

Associativity of $\oplus$ and $\odot$ uses the continuation associativity law
already used by the singleton monoid, ring, and vector-space chapters. Both
operation symbols are represented by $\mathsf{append}$, so the two
parenthesized products share a single BEDC append spine up to $\hsame$.

For classifier congruence, unpack the two $\rho_{\emp}$ hypotheses. Closure
keeps both composite endpoints in $A_{\emp}$, and the relation components are
combined by reflexivity, symmetry, and transitivity of $\hsame$ after the
append unit and associativity reductions. This gives the required
$\rho_{\emp}$ comparison for both $\oplus$ and $\odot$.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonEmptyHistory\_laws}

\begin{theorem}[Singleton empty-history matrix endpoint collapse]
\label{thm:singleton-empty-history-matrix-endpoint-collapse}
With the notation of
Definition~\ref{def:singleton-empty-history-matrix-instance}, if
$A_{\emp}(M)$ and $A_{\emp}(N)$, then
\begin{enumerate}
  \item $\rho_{\emp}(M\oplus N,\emp)$.
  \item $\rho_{\emp}(M\odot N,\emp)$.
  \item $\rho_{\emp}(M\oplus N,M\odot N)$.
\end{enumerate}
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:singleton-empty-history-matrix-laws} to the carried
inputs $M$ and $N$. Its closure clause puts both $M\oplus N$ and $M\odot N$
back in $A_{\emp}$, while the singleton carrier witnesses identify $M$ and
$N$ with the endpoint $\emp$.

By the displayed definitions, both operations are
$\mathsf{append}(M,N)$. Replace the carried inputs by their singleton endpoint
witnesses, then use the left and right continuation unit laws cited in
Theorem~\ref{thm:concrete-history-monoid-laws} to reduce the resulting append
history to $\emp$.

For $\rho_{\emp}(M\oplus N,\emp)$ and
$\rho_{\emp}(M\odot N,\emp)$, combine the closure witnesses with the
corresponding $\hsame$ endpoint calculation. Symmetry is used when the unit
witness is oriented from $\emp$ to the displayed operation rather than from
the displayed operation to $\emp$.

For $\rho_{\emp}(M\oplus N,M\odot N)$, compose the first endpoint comparison
with the symmetry of the second by transitivity of $\hsame$. The two carrier
witnesses are the closure witnesses already supplied by the law package, so
the classifier definition gives the required comparison.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonEmptyHistory\_endpoint\_collapse}

The $\MatrixUp$ schema is instantiated by this concrete $\mathsf{BHist}$
carrier and operations, so formalization can target the endpoint-collapse and
law package rather than an abstract operator schema.

\begin{theorem}[Singleton empty-history matrix endpoint exactness]
\label{thm:singleton-empty-history-matrix-endpoint-exactness}
For the singleton instance of
Definition~\ref{def:singleton-empty-history-matrix-instance}, if
$A_{\emp}(M)$ and $A_{\emp}(N)$, then all displayed matrix endpoints are
classified by the singleton empty history:
\begin{enumerate}
  \item $\rho_{\emp}(M,\emp)$.
  \item $\rho_{\emp}(0_M,\emp)$.
  \item $\rho_{\emp}(I_M,\emp)$.
  \item $\rho_{\emp}(M\oplus N,\emp)$.
  \item $\rho_{\emp}(M\odot N,\emp)$.
\end{enumerate}
\end{theorem}
\begin{proof}
Unfold the singleton carrier $A_{\emp}$ and the classifier
$\rho_{\emp}$. The hypotheses say that $M$ and $N$ are carried by the
singleton endpoint, so the first classification is obtained by pairing the
two carrier witnesses with the corresponding singleton endpoint witness in
$\hsame$.

Apply Theorem~\ref{thm:singleton-empty-history-matrix-laws} to obtain closure
of $0_M$, $I_M$, $M\oplus N$, and $M\odot N$ in $A_{\emp}$. These closure
witnesses supply the carrier components required by the classifier for the
four remaining endpoints.

For the $\hsame$ components, compose the singleton endpoint witnesses with
symmetry and transitivity of $\hsame$. Thus each displayed endpoint is
identified with $\emp$, and repacking the carrier witnesses with the
$\hsame$ witness gives the five asserted instances of $\rho_{\emp}$.
\end{proof}

\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonEmptyHistory\_endpoint\_exactness}

\begin{theorem}[Matrix singleton classifier continuation commutativity closure]
\label{thm:matrix-singleton-classifier-continuation-comm-closed}
Let $M,N:\mathsf{BHist}$ be carried by the singleton matrix carrier
$A_{\emp}$. If $\ContR(M,N,L)$ and $\ContR(N,M,R)$, then both continuation
results are again carried by $A_{\emp}$, and the singleton matrix classifier
relates the two results:
$$
A_{\emp}(L)\land A_{\emp}(R)\land \rho_{\emp}(L,R).
$$
\end{theorem}
\begin{proof}
The two carrier hypotheses identify $M$ and $N$ with $\emp$. After these
identifications, the two continuation premises identify both $L$ and $R$ with
the BEDC append endpoint $\mathsf{append}(\emp,\emp)$, which is $\emp$. The
singleton carrier witnesses for $L$ and $R$ are therefore reflexive empty
history witnesses, and the classifier is rebuilt from those two carrier
components and $\hsame(\emp,\emp)$.
\end{proof}
\leanchecked{BEDC.Derived.MatrixUp.MatrixSingletonClassifier\_continuation\_comm\_closed}

\input{parts/concrete_instances/matrix/singleton_carrier_continuation_visible_result_absurd.tex}
