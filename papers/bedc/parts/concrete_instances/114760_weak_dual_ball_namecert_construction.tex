\chapter{A Concrete Naming Certificate for $\WeakDualBallUp$}
\label{ch:concrete-instances-weak-dual-ball-namecert}
\origin{human}

Following Banach--Alaoglu, Kelley, and Bourbaki on weak-star dual balls, the $\WeakDualBallUp$ packet records the finite dual-ball observation surface that compactness and weak-star consumers may inspect. It is read over the Banach source of \autoref{ch:concrete-instances-banach-namecert}, the scalar-functional surface of \autoref{ch:concrete-instances-bounded-linear-functional-namecert}, the bounded-operator readback of \autoref{ch:concrete-instances-boundedlinearoperator-namecert}, the scalar endpoint seal of \autoref{ch:concrete-instances-real-namecert}, the finite Banach--Alaoglu window of \autoref{ch:concrete-instances-banach-alaoglu-namecert}, and the weak-star compactness route of \autoref{ch:concrete-instances-weak-star-compactness-namecert}. The packet names only finite source probes, bounded scalar-functional rows, scalar bounds, and weak-star observation windows; it does not identify a host dual ball, choose cluster points, invoke ultrafilters, or export unrestricted product compactness.

\begin{definition}[Weak dual-ball carrier]
\label{def:weak-dual-ball-carrier}
A $\WeakDualBallUp$ carrier is a finite $\BHist$ packet
$$
D=(B,L,W,S,R,A,K,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\BanachUp$ source row whose carried endpoints supply the probes inspected by the weak-star window.
\item $L$ is the finite $\BoundedLinearFunctionalUp$ row, read through scalar-valued bounded-operator data and never as an ambient dual-space element.
\item $W$ is the finite weak-star observation row, listing the source probes, scalar tolerances, and functional coordinates available to the packet.
\item $S$ is the scalar-bound row for the displayed functionals, with all bounds and values read through $\RealUp$.
\item $R$ is the scalar readback row, recording the Real comparison data used by the finite window.
\item $A$ is the $\BanachAlaogluUp$ finite-window handoff row, used only as the displayed dual-ball observation route.
\item $K$ is the $\WeakStarCompactnessUp$ compactness-facing row, recording the downstream consumer that reads the same finite dual-ball window.
\item $H$ records componentwise $\hsame$ transport for source, functional, window, bound, readback, Banach--Alaoglu, weak-star compactness, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a dual-ball observation request through $B,L,W,S,R,A,K,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\WeakDualBallUp$, $\BanachUp$, $\BoundedLinearFunctionalUp$, $\BoundedLinearOperatorUp$, $\RealUp$, $\BanachAlaogluUp$, $\WeakStarCompactnessUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\WeakDualBallUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $B,L,W,S,R,A,K,H,C,P,N$. It has no coordinate for a host dual-ball object, quotient functional equality, selected representatives, cluster-point choice, ultrafilter extension, unrestricted Tychonoff compactness, ambient Real completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Weak dual-ball NameCert obligations]
\label{thm:weak-dual-ball-namecert-obligations}
For every accepted $\WeakDualBallUp$ carrier $D=(B,L,W,S,R,A,K,H,C,P,N)$, the local $\NameCert_{\WeakDualBallUp}$ surface consists exactly of Banach source admission through $B$, bounded scalar-functional admission through $L$, finite weak-star observation through $W$, scalar-bound and Real readback through $S,R$, Banach--Alaoglu finite-window handoff through $A$, weak-star compactness consumer routing through $K$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier rows. The analytic inventory is exactly the Banach source, bounded scalar-functional family, finite observation window, scalar-bound row, Real readback row, and the two compactness-facing handoff rows.

The functional data are read before any compactness consumer is reached. Row $L$ supplies only finite scalar-functional observations, while $W,S,R$ specify the probes, tolerances, bounds, and Real comparisons that make the observation visible.

The compactness-facing rows do not add hidden objects. Row $A$ cites the Banach--Alaoglu finite-window route, and row $K$ records the weak-star compactness consumer that reads that same finite window.

Finally $H,C,P,N$ transport, replay, source, and locally name the same packet. Since no host dual ball, quotient functional equality, selected representative, cluster point, ultrafilter, unrestricted product, or ambient completeness row is present, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Weak dual-ball finite observation route]
\label{thm:weak-dual-ball-finite-observation-route}
Every $\BanachAlaogluUp$ or $\WeakStarCompactnessUp$ consumer of an accepted $\WeakDualBallUp$ packet must read the finite route
$$
B,L,W,S,R,A,K,H,C,P,N .
$$
Thus the consumer receives a displayed Banach source, finite bounded-linear-functional rows, finite weak-star probes and scalar tolerances, Real scalar readbacks, the Banach--Alaoglu finite-window handoff, and the weak-star compactness consumer row before any compactness-facing claim is inspected. The route exports no host dual ball, no selected dual element, no quotient functional equality, no cluster-point choice, no ultrafilter extension, no unrestricted Tychonoff compactness, and no ambient Real completeness theorem.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:weak-dual-ball-namecert-obligations}. It forces every public read through the Banach source row $B$, bounded scalar-functional row $L$, finite observation row $W$, scalar-bound row $S$, and Real readback row $R$.

Next inspect the downstream handoff. The Banach--Alaoglu row $A$ and weak-star compactness row $K$ are present only after the finite observation rows have been exposed, so neither consumer can replace the route by an ambient dual-space object.

The structural rows $H,C,P,N$ preserve the same finite packet. Because the carrier contains no selector, quotient equality, ultrafilter, unrestricted product, cluster-point, or ambient-completeness coordinate, every compactness-facing read remains inside the displayed dual-ball observation route.
\end{proof}

\falsifiablePrediction{Every accepted $\WeakDualBallUp$ compactness-facing read exposes a displayed $\BanachUp$ source, finite $\BoundedLinearFunctionalUp$ rows, finite weak-star probes, scalar bounds, $\RealUp$ readbacks, $\BanachAlaogluUp$ and $\WeakStarCompactnessUp$ handoff rows, $\BHist$ transport, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ naming before any dual-ball compactness consumer is public.}

\independenceWitness{banach, bounded-linear-functional, real, banach-alaoglu, weak-star-compactness: none is bijective with $\WeakDualBallUp$ because this packet jointly carries the finite dual-ball observation route consumed by both Banach--Alaoglu and weak-star compactness while refusing host dual balls, selections, quotient functional equality, ultrafilters, unrestricted products, and ambient completeness.}

\closureat{WeakDualBallUp}{seedStr}

\begin{closurestatus}{\WeakDualBallUp}
  \constructivestory{A $\WeakDualBallUp$ packet is generated from a displayed Banach source, finite bounded scalar-functional rows, finite weak-star probes and scalar tolerances, Real scalar readbacks, Banach--Alaoglu and weak-star compactness handoff rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:weak-dual-ball-carrier}, \autoref{thm:weak-dual-ball-namecert-obligations}, and \autoref{thm:weak-dual-ball-finite-observation-route} seed the finite dual-ball observation carrier consumed by Banach--Alaoglu and weak-star compactness routes.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host dual-ball objects, selected dual elements, quotient functional equality, cluster-point choice, ultrafilter extension, unrestricted Tychonoff compactness, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, bounded-functional admission, finite weak-star observation exactness, scalar-bound readback, Banach--Alaoglu handoff, weak-star compactness consumer routing, transport, replay, provenance, and local naming for BEDC.Derived.WeakDualBallUp.}
\end{closurestatus}
