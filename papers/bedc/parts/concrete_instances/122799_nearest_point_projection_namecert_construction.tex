\chapter{A Concrete Naming Certificate for $\NearestPointProjectionUp$}
\label{ch:concrete-instances-nearest-point-projection-namecert}
\origin{human}

Following Bishop--Bridges metric constructions and Kelley-style metric neighbourhoods, the $\NearestPointProjectionUp$ packet records a displayed nearest-point projection witness only when the metric source, located candidate surface, finite distance comparisons, endpoint row, dyadic comparison ledger, and $\RealUp$ distance seal are all present. It sits after the metric source of \autoref{ch:concrete-instances-metric-namecert}, the terminal real-distance naming discipline of \autoref{ch:concrete-instances-real-namecert}, the finite projection table of \autoref{ch:concrete-instances-finite-metric-projection-namecert}, and the finite selector boundary of \autoref{ch:concrete-instances-finite-metric-projection-selector-namecert}. The packet is a finite displayed projection certificate; it does not assert a global nearest-point choice principle, hidden minimizers, quotient subset equality, compactness, or ambient completion.

\begin{definition}[Nearest-point projection carrier]
\label{def:nearest-point-projection-carrier}
A $\NearestPointProjectionUp$ carrier is a finite $\BHist$ packet
$$
P=(M,S,T,Q,E,R,H,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the ambient $\MetricUp$ source row naming the metric space in which the projection question is posed.
\item $S$ is the located candidate-surface row naming the finite visible candidate family and its membership ledger.
\item $T$ is the consumed $\FiniteMetricProjectionUp$ table row, recording the displayed distances from the query point to the visible candidates.
\item $Q$ is the $\DyadicRatCoreUp$ comparison ledger used to compare all distance entries in $T$.
\item $E$ is the endpoint row naming the candidate whose distance row is certified minimal by $Q$.
\item $R$ is the terminal $\RealUp$ distance seal attached to the endpoint row after the finite comparisons have been replayed.
\item $H$ records componentwise $\hsame$ transport for the metric source, candidate surface, finite table, comparison ledger, endpoint, distance seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a projection request through the finite table, comparison ledger, endpoint row, and terminal distance seal.
\item $G$ is the $\Pkg$ provenance over $\NearestPointProjectionUp$, $\MetricUp$, $\RealUp$, $\FiniteMetricProjectionUp$, $\FiniteMetricProjectionSelectorUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\NearestPointProjectionUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these rows. It has no coordinate for a host projection function, selected minimizer outside the displayed table, quotient subset equality, compactness theorem, countable selection schedule, or ambient completion.
\end{definition}

\begin{theorem}[Nearest-point projection NameCert obligations]
\label{thm:nearest-point-projection-namecert-obligations}
For every accepted $\NearestPointProjectionUp$ carrier $P=(M,S,T,Q,E,R,H,C,G,N)$, the local $\NameCert_{\NearestPointProjectionUp}$ surface consists exactly of carrier admission, metric-source exposure, located candidate-surface exposure, finite projection-table exposure, dyadic comparison admission, endpoint handoff, terminal distance sealing, componentwise stability transport, displayed replay, provenance, local naming, and refusal of global nearest-point choice, hidden minimizers, quotient subset equality, compactness, and ambient completion.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:nearest-point-projection-carrier}. Its public rows are exactly $M,S,T,Q,E,R,H,C,G,N$, so admission exposes only the displayed metric source, candidate surface, finite distance table, comparison ledger, endpoint row, distance seal, and structural rows.

The finite projection-table obligation is inherited from \autoref{ch:concrete-instances-finite-metric-projection-namecert}. The row $T$ supplies displayed candidate-distance entries, while $Q$ supplies the dyadic comparison route that can certify one endpoint row against the visible table.

The endpoint and refusal obligations follow from the absence of any additional coordinate. Since the packet stores no host projection map, no quotient subset object, no compactness theorem, and no completion row, the local NameCert surface can export only the displayed endpoint and terminal $\RealUp$ distance seal.
\end{proof}

\begin{theorem}[Nearest-point projection distance minimality]
\label{thm:nearest-point-projection-distance-minimality}
Let $P=(M,S,T,Q,E,R,H,C,G,N)$ be an accepted $\NearestPointProjectionUp$ packet. Every visible candidate row $s$ in the located surface $S$ is compared with the endpoint row $E$ by the finite route
$$
M,S,T,Q \longmapsto E,R \longmapsto H,C,G,N .
$$
The terminal read therefore certifies the displayed inequality
$$
d_M(x,E) \leq d_M(x,s)
$$
only for candidates whose distance rows occur in $T$.
\end{theorem}
\begin{proof}
Start with the visible candidate surface $S$ and the finite table $T$. The table contains exactly the distance rows available to the packet, so a candidate $s$ can be compared with $E$ only when its distance entry is displayed in $T$.

Read the dyadic ledger $Q$. It performs the finite comparison between the endpoint entry and each displayed candidate entry, and the endpoint row $E$ is accepted only after those comparisons have been replayed.

Finally read the terminal seal $R$ and structural rows $H,C,G,N$. They preserve the comparison route as BEDC data and expose the inequality as a finite displayed fact, not as an ambient minimization principle over an unbounded subset.
\end{proof}

\begin{theorem}[Nearest-point projection completion handoff]
\label{thm:nearest-point-projection-completion-handoff}
Every completion-facing consumer that reads an accepted $\NearestPointProjectionUp$ packet must first expose the metric row $M$, candidate surface $S$, finite projection table $T$, dyadic comparison ledger $Q$, endpoint row $E$, terminal $\RealUp$ seal $R$, and structural rows $H,C,G,N$. The handoff supplies a finite endpoint witness and its displayed distance; it does not import ambient completion, quotient subset equality, compactness, or a global projection selector.
\end{theorem}
\begin{proof}
Begin with the metric and real-distance dependencies. The rows $M$ and $R$ place the packet inside the metric and $\RealUp$ naming surfaces referenced by \autoref{ch:concrete-instances-metric-namecert} and \autoref{ch:concrete-instances-real-namecert}.

Next read the finite projection table and selector boundary. The row $T$ is the finite projection table admitted by \autoref{ch:concrete-instances-finite-metric-projection-namecert}, and the endpoint read is constrained by the finite selector discipline of \autoref{ch:concrete-instances-finite-metric-projection-selector-namecert}.

It remains to pass the endpoint to the consumer. The continuation row $C$ replays $M,S,T,Q,E,R$, while $H,G,N$ preserve transport, provenance, and local naming. Because no completion carrier or quotient subset coordinate is present, the consumer receives only the displayed endpoint witness and distance seal.
\end{proof}

\closureat{NearestPointProjectionUp}{seedStr}

\begin{closurestatus}{\NearestPointProjectionUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:nearest-point-projection-carrier}, \autoref{thm:nearest-point-projection-namecert-obligations}, \autoref{thm:nearest-point-projection-distance-minimality}, and \autoref{thm:nearest-point-projection-completion-handoff} seed the finite displayed nearest-point projection certificate over MetricUp, RealUp, FiniteMetricProjectionUp, FiniteMetricProjectionSelectorUp, DyadicRatCoreUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close global nearest-point choice, hidden minimizers, quotient subset equality, compactness, ambient completion, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located candidate-surface exposure, finite comparison exactness, endpoint handoff, Real distance sealing, transport, replay, provenance, and local naming for BEDC.Derived.NearestPointProjectionUp.}
  \constructivestory{A $\NearestPointProjectionUp$ packet is generated from a Metric row, a located candidate-surface row, a FiniteMetricProjection table row, a DyadicRatCore comparison ledger, an endpoint row, a Real distance seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
