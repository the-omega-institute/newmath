\chapter{A Concrete Naming Certificate for $\IntervalConnectednessUp$}
\label{ch:concrete-instances-interval-connectedness-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\IntervalConnectednessUp$ packet records the finite constructive route by which a located real interval is inspected as one connected interval surface before intermediate-value, zero-envelope, or completion-facing consumers use it. It sits after the endpoint seal of \autoref{ch:concrete-instances-real-namecert}, the located interval carrier of \autoref{ch:concrete-instances-located-real-interval-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the sandwich consumer surface of \autoref{ch:concrete-instances-squeeze-theorem-namecert}. The packet names interval connectedness as displayed finite BEDC data; it does not import host topology, open-cover compactness, trichotomy, quotient real equality, selected roots, or ambient $\RealUp$ completeness.

\begin{definition}[Interval connectedness carrier]
\label{def:interval-connectedness-carrier}
An $\IntervalConnectednessUp$ carrier is a finite $\BHist$ packet
$$
I=(E,L,D,S,R,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the $\RealUp$ endpoint-pair row, storing the lower and upper endpoint names through their displayed real seals.
\item $L$ is the $\LocatedRealIntervalUp$ witness row, carrying the located interval budget, locator reads, and endpoint-radius checks attached to $E$.
\item $D$ is the finite $\DyadicRatCoreUp$ subdivision-chain ledger, recording the dyadic cells, adjacent-cell overlaps, and tolerance requests inspected inside $L$.
\item $S$ is the $\StreamNameUp$ window row through which endpoint, cell, and witness observations are scheduled.
\item $R$ is the $\RegSeqRatUp$ readback row for the scheduled endpoint and subdivision observations.
\item $T$ records componentwise $\hsame$ transport for endpoint pairs, located interval witnesses, dyadic subdivision chains, stream windows, regular readbacks, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from endpoint pairs through located interval exposure, dyadic subdivision, stream-window inspection, regular readback, and the interval-connectedness handoff.
\item $P$ is the $\Pkg$ provenance over $\IntervalConnectednessUp$, $\RealUp$, $\LocatedRealIntervalUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\SqueezeTheoremUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\IntervalConnectednessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $E,L,D,S,R,T,C,P,N$. It has no coordinate for host topological connectedness, open-cover compactness, trichotomy, quotient real equality, selected intermediate points, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Interval connectedness NameCert obligations]
\label{thm:interval-connectedness-namecert-obligations}
For every accepted $\IntervalConnectednessUp$ carrier
$$
I=(E,L,D,S,R,T,C,P,N),
$$
the local $\NameCert_{\IntervalConnectednessUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the endpoint pair, located interval witness, dyadic subdivision chain, stream windows, regular readbacks, transport, replay, provenance, and local naming rows;
\item classifier transport, requiring endpoint, locatedness, subdivision, window, readback, replay, provenance, and naming data to move componentwise through $T$;
\item dyadic chain exactness, requiring every inspected subinterval and adjacent-cell overlap to be recorded in $D$ before a connectedness consumer reads the packet;
\item endpoint handoff nonescape, requiring every consumer of interval endpoints to pass through $E,L,D,S,R$ before using any $\RealUp$ seal;
\item ledger exhaustion, requiring the packet to expose no host topological connectedness theorem, open-cover compactness, trichotomy, quotient real equality, selected intermediate point, or ambient $\RealUp$ completeness principle.
\end{enumerate}
The sibling routes through $\RealUp$, $\LocatedRealIntervalUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\SqueezeTheoremUp$ are the only named consumers of this local certificate surface.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:interval-connectedness-carrier}. The endpoint pair is visible only in $E$, and the located interval evidence is visible only in $L$.

The subdivision data are then forced through $D$. Each finite cell, overlap, and tolerance request is read as dyadic data before any stream-window or regular-readback consumer can inspect it.

Transport and replay preserve the same route. The rows $T$ and $C$ move endpoint, locatedness, subdivision, window, and readback data componentwise, while $P$ and $N$ source and name exactly that displayed packet.

Since the carrier has no coordinate for the excluded host principles, the local certificate cannot export them.
\end{proof}

\falsifiablePrediction{Every accepted $\IntervalConnectednessUp$ consumer reads endpoint pairs, located interval witnesses, dyadic subdivision chains, StreamName windows, RegSeqRat readbacks, hsame transport, Cont replay, Pkg provenance, and local naming before any interval-connectedness handoff is visible.}

\independenceWitness{real, locatedrealinterval, dyadicratcore, streamname, regseqrat, squeezetheorem: $\IntervalConnectednessUp$ is not bijective to any listed sibling because it jointly carries endpoint pairs, located interval witnesses, finite dyadic subdivision chains, scheduled stream windows, regular rational readbacks, transport, replay, provenance, and local naming.}

\closureat{IntervalConnectednessUp}{seedStr}

\begin{closurestatus}{\IntervalConnectednessUp}
  \constructivestory{An $\IntervalConnectednessUp$ packet is generated from Real endpoint pairs, a LocatedRealInterval witness, a finite DyadicRatCore subdivision chain, StreamName windows, RegSeqRat readbacks, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:interval-connectedness-carrier} and \autoref{thm:interval-connectedness-namecert-obligations} seed the finite located-interval connectedness route over $\RealUp$, $\LocatedRealIntervalUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\SqueezeTheoremUp$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, host topology, open-cover compactness, trichotomy, quotient real equality, selected intermediate points, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier transport, dyadic chain exactness, endpoint handoff nonescape, ledger exhaustion, and downstream intermediate-value consumer alignment for BEDC.Derived.IntervalConnectednessUp.}
\end{closurestatus}
