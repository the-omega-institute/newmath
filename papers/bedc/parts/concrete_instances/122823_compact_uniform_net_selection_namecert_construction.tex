\chapter{A Concrete Naming Certificate for $\CompactUniformNetSelectionUp$}
\label{ch:concrete-instances-compact-uniform-net-selection-namecert}
\origin{human}

Following Bishop and Bridges on compact finite nets and uniform continuity, the $\CompactUniformNetSelectionUp$ packet records the finite selected-net handoff by which compact metric source data and continuous-map local moduli are turned into a displayed uniform-modulus consumer row. It sits beside the finite cover mesh carrier of \autoref{ch:concrete-instances-finite-cover-mesh-namecert}, reads compact source rows through \autoref{ch:concrete-instances-compactmetric-namecert}, continuous-map rows through \autoref{ch:concrete-instances-continuousmap-namecert}, terminal seals through \autoref{ch:concrete-instances-real-namecert}, regular rational readbacks through \autoref{ch:concrete-instances-regseqrat-namecert}, and dyadic tolerance arithmetic through \autoref{ch:concrete-instances-dyadicratcore-namecert}. The packet supplies a finite selected-net ledger for compact-uniform-continuity consumers; it does not assert open-cover compactness, an external point selector, quotient Real equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Compact uniform net selection carrier]
\label{def:compact-uniform-net-selection-carrier}
A $\CompactUniformNetSelectionUp$ carrier is a finite $\BHist$ packet
$$
S=(K,F,M,Q,R,D,L,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the accepted $\CompactMetricUp$ source row whose finite compact-net data are inspected.
\item $F$ is the $\ContinuousMapUp$ row carrying the pointwise modulus data for the displayed graph.
\item $M$ is the finite selected-net row, listing the compact probes and their local-radius witnesses.
\item $Q$ is the finite lower-bound fold row over the selected probe radii.
\item $R$ is the $\RegSeqRatUp$ readback row for the folded rational tolerance data.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row used to compare the requested precision with the selected radius.
\item $L$ is the local coverage ledger saying which displayed probe covers each inspected source point.
\item $U$ is the compact-uniform handoff row exposed to $\UniformModulusUp$ and compact-uniform-continuity consumers.
\item $H$ records componentwise $\hsame$ transport for the compact, continuous, net, fold, readback, dyadic, coverage, and handoff rows.
\item $C$ records displayed $\Cont$ replay from compact finite-net reads and continuous-map local moduli into the uniform handoff.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformNetSelectionUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\RealUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\FiniteCoverMeshUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformNetSelectionUp}$ registration row.
\end{enumerate}
The classifier compares accepted carriers only through these rows. It has no coordinate for an open-cover compactness principle, an external center selector, hidden Lebesgue-number existence, quotient Real equality, countable choice, or ambient completion.
\end{definition}
\leandef{BEDC.Derived.CompactUniformNetSelectionUp.CompactUniformNetSelectionUp}

\begin{theorem}[Compact uniform net selection NameCert obligations]
\label{thm:compact-uniform-net-selection-namecert-obligations}
For every accepted $\CompactUniformNetSelectionUp$ carrier $S=(K,F,M,Q,R,D,L,U,H,C,P,N)$, the local $\NameCert_{\CompactUniformNetSelectionUp}$ obligations are exactly compact-metric source admission, continuous-map local-modulus admission, selected finite-net exposure, finite lower-bound fold exposure, regular rational readback, dyadic tolerance comparison, coverage-ledger exactness, compact-uniform handoff exposure, componentwise stability transport, displayed replay, provenance, and local naming. The obligations do not include open-cover compactness, hidden center choice, quotient Real equality, countable choice, an ambient completion theorem, or a host Lebesgue-number principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-net-selection-carrier}. The compact and continuity source rows are $K$ and $F$, so selected probes and local moduli must be read before any uniform handoff is available.

Read the finite selection route next. The selected-net row $M$ supplies only displayed probes, the fold row $Q$ records the finite lower-bound comparison, and the $\RegSeqRatUp$ and $\DyadicRatCoreUp$ rows $R,D$ expose the rational tolerance data used by the coverage ledger $L$.

The handoff row $U$ is downstream of that ledger. Componentwise transport $H$, replay $C$, provenance $P$, and local naming $N$ preserve the same finite route, so none of the excluded compactness, choice, quotient, completion, or host Lebesgue-number principles has a carrier coordinate.
\end{proof}

\begin{theorem}[Compact uniform net selection vision handoff]
\label{thm:compact-uniform-net-selection-vision-handoff}
Every compact-uniform-continuity consumer of an accepted $\CompactUniformNetSelectionUp$ packet must read the route
$$
K,F,M,Q,R,D,L,U,H,C,P,N
$$
before using the exported uniform-modulus handoff. Thus the compact finite-net source, continuous-map local-modulus row, regular rational readback, dyadic tolerance comparison, and finite cover mesh relation are exposed as concrete NameCert data; the consumer receives no open-cover compactness theorem, external selector, quotient Real equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with \autoref{thm:compact-uniform-net-selection-namecert-obligations}. It identifies the local surface of an accepted selected-net packet and forces every consumer through the compact source, continuous-map source, selected-net, fold, readback, dyadic, coverage, and handoff rows.

The compact side is read through $K,M,L$. These rows display the compact finite-net probes and the local coverage ledger, matching the finite mesh discipline of \autoref{ch:concrete-instances-finite-cover-mesh-namecert}.

The analytic side is read through $F,Q,R,D,U$. Continuous-map local moduli feed the finite lower-bound fold, the regular rational and dyadic rows expose the tolerance comparison, and the uniform handoff is available only after those rows have been displayed.

Finally $H,C,P,N$ transport, replay, package, and name the same route. Since the packet exports only this finite selected-net path, the compact-uniform-continuity vision receives a concrete NameCert handoff and no hidden compactness or choice principle.
\end{proof}

\falsifiablePrediction{A $\CompactUniformNetSelectionUp$ consumer is refuted whenever a compact-uniform handoff bypasses the compact-metric source, continuous-map local-modulus row, selected finite-net row, lower-bound fold, regular rational readback, dyadic tolerance comparison, coverage ledger, transport, replay, provenance, or local NameCert row.}

\closureat{CompactUniformNetSelectionUp}{seedStr}

\begin{closurestatus}{\CompactUniformNetSelectionUp}
  \constructivestory{A $\CompactUniformNetSelectionUp$ packet is generated from CompactMetric finite-net data, ContinuousMap local-modulus rows, selected probe data, a finite lower-bound fold, RegSeqRat readback, DyadicRatCore tolerance comparison, a coverage ledger, compact-uniform handoff data, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-net-selection-carrier}, \autoref{thm:compact-uniform-net-selection-namecert-obligations}, and \autoref{thm:compact-uniform-net-selection-vision-handoff} seed the finite selected-net handoff from compact and continuous rows into compact-uniform-continuity consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, hidden center choice, host Lebesgue-number existence, quotient Real equality, countable choice, ambient completion, theorem-level Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, selected-net exactness, finite lower-bound fold compatibility, compact-uniform handoff nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformNetSelectionUp.}
\end{closurestatus}
