\chapter{A Concrete Naming Certificate for $\MetaCICResidualNormalizationObstructionUp$}
\label{ch:concrete-instances-metacic-residual-normalization-obstruction-namecert}
\origin{ai}

The $\MetaCICResidualNormalizationObstructionUp$ packet realizes the normalization boundary of \autoref{sec:metacic-open-problems-normalization} as finite BEDC obstruction data. It sits beside the candidate closed-projection boundary of \autoref{ch:concrete-instances-metacic-candidate-closed-projection-boundary-namecert}, the normal-endpoint cut of \autoref{ch:concrete-instances-metacic-normal-endpoint-cut-namecert}, the candidate-normalization discharge route of \autoref{ch:concrete-instances-candidate-normalization-discharge-namecert}, and the typed normal-form scheduler of \autoref{ch:concrete-instances-metacic-typed-normal-form-scheduler-namecert}. The packet names the residual gap between arbitrary closed typed MetaCIC terms and the candidate-mediated normalization interface; it does not assert blanket strong normalization, closed consistency, or a subject-reduction theorem.

\begin{definition}[MetaCIC residual normalization obstruction carrier]
\label{def:metacic-residual-normalization-obstruction-carrier}
A $\MetaCICResidualNormalizationObstructionUp$ carrier is a finite $\BHist$ packet
$$
R=(T,C,E,B,O,L,A,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the closed typed MetaCIC term row, recording the empty-context term, its displayed type, and the finite typing window under inspection.
\item $C$ is the candidate-interface request row, asking whether the term can be placed into the candidate evidence surface already used by the normalization map.
\item $E$ is the endpoint row, recording the bounded normal endpoint or the absence of such an endpoint inside the displayed candidate interface.
\item $B$ is the residual beta-frontier row, exposing unreduced or unchecked residual fragments that remain after the bounded scheduler window.
\item $O$ is the obstruction row, recording that candidate admission, endpoint preservation, or false-type preservation is not yet available for the inspected closed term.
\item $L$ is the Lean TasteGate obligation row, requiring future $\mathsf{Nontrivial}\ \MetaCICResidualNormalizationObstructionUp$ and $\mathsf{FieldFaithful}\ \MetaCICResidualNormalizationObstructionUp$ instances before the obstruction classifier can serve as checked evidence.
\item $A$ is the audit handoff row to subject-reduction and closed-consistency consumers; it exports the obstruction without discharging those consumers.
\item $H$ records componentwise $\hsame$ transport for $T,C,E,B,O,L,A,K,P,N$.
\item $K$ records displayed $\Cont$ replay from the closed-term request through candidate admission, endpoint, residual frontier, obstruction, TasteGate obligation, audit handoff, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\MetaCICResidualNormalizationObstructionUp$, $\MetaCICCandidateClosedProjectionBoundaryUp$, $\MetaCICNormalEndpointCutUp$, $\CandidateNormalizationDischargeUp$, $\MetaCICTypedNormalFormSchedulerUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetaCICResidualNormalizationObstructionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $T,C,E,B,O,L,A,H,K,P,N$. It has no coordinate for blanket strong normalization of arbitrary closed terms, closed consistency, hidden semantic candidates, host proof search, selected normal forms, or subject reduction.
\end{definition}

\begin{theorem}[MetaCIC residual normalization obstruction NameCert obligations]
\label{thm:metacic-residual-normalization-obstruction-namecert-obligations}
For every accepted $\MetaCICResidualNormalizationObstructionUp$ carrier $R=(T,C,E,B,O,L,A,H,K,P,N)$, the local $\NameCert_{\MetaCICResidualNormalizationObstructionUp}$ surface consists exactly of closed typed-term admission through $T$, candidate-interface request through $C$, bounded normal endpoint accounting through $E$, residual beta-frontier exposure through $B$, obstruction recording through $O$, Lean $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ obligation exposure through $L$, audit handoff through $A$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $K$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-residual-normalization-obstruction-carrier}. The closed term, candidate request, endpoint, residual frontier, obstruction, TasteGate obligation, and audit handoff are displayed as $T,C,E,B,O,L,A$.

The obstruction row $O$ is not a proof of non-normalization. It records that the inspected closed typed term has not been placed into the candidate-mediated normalization interface with endpoint and false-type preservation visible in the same finite packet.

The TasteGate row $L$ keeps the Lean obligations explicit: a future checked surface must provide both a nontrivial accepted/refused boundary and field-faithful preservation of the displayed rows. Transport, replay, provenance, and naming are then exhausted by $H,K,P,N$, so the listed rows are exactly the local NameCert obligations.
\end{proof}

\begin{theorem}[MetaCIC residual normalization obstruction boundary]
\label{thm:metacic-residual-normalization-obstruction-boundary}
Every closed-consistency or normalization consumer that reads an accepted $\MetaCICResidualNormalizationObstructionUp$ packet receives an obstruction ledger, not a discharge of the open normalization question. The consumer must still provide candidate admission, bounded normal endpoint preservation, residual-frontier elimination, subject-reduction compatibility, and the Lean $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances before the inspected closed term can be treated as normalized inside the public interface.
\end{theorem}
\begin{proof}
Begin with the closed typed-term row $T$ and the candidate request row $C$. These rows expose the term and the intended candidate interface, but they do not themselves provide candidate evidence.

Read the endpoint and residual rows next. The row $E$ records the bounded endpoint status visible to the packet, while $B$ records the residual beta frontier that remains under the scheduler window.

The obstruction row $O$ preserves the gap rather than erasing it. The audit row $A$ may be consumed by subject-reduction or closed-consistency routes, but it exports the same obstruction ledger. The TasteGate row $L$ records the remaining Lean nontriviality and field-faithfulness obligations, and the structural rows $H,K,P,N$ only transport, replay, source, and name the same finite packet.
\end{proof}

\falsifiablePrediction{Every accepted $\MetaCICResidualNormalizationObstructionUp$ read exposes a closed typed-term row, candidate-interface request, bounded normal endpoint accounting, residual beta-frontier row, obstruction row, Lean TasteGate obligation row, audit handoff, hsame transport, Cont replay, Pkg provenance, and local NameCert row. A read that exports blanket strong normalization, closed consistency, selected normal forms, hidden semantic candidates, or subject reduction from this packet refutes the carrier boundary.}

\independenceWitness{metacic-candidate-closed-projection-boundary, metacic-normal-endpoint-cut, candidate-normalization-discharge, metacic-typed-normal-form-scheduler, metacic-sn-candidate-interface: $\MetaCICResidualNormalizationObstructionUp$ is not bijective to any listed sibling because it keeps the residual frontier, candidate-admission request, endpoint accounting, obstruction row, Lean TasteGate obligation row, and audit handoff in one finite packet.}

\closureat{MetaCICResidualNormalizationObstructionUp}{seedStr}

\begin{closurestatus}{\MetaCICResidualNormalizationObstructionUp}
  \constructivestory{A $\MetaCICResidualNormalizationObstructionUp$ packet is generated from a closed typed MetaCIC term row, candidate-interface request, bounded normal endpoint accounting, residual beta-frontier row, obstruction row, Lean TasteGate obligation row, audit handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-residual-normalization-obstruction-carrier}, \autoref{thm:metacic-residual-normalization-obstruction-namecert-obligations}, and \autoref{thm:metacic-residual-normalization-obstruction-boundary} seed the finite obstruction packet for arbitrary closed typed MetaCIC terms outside the currently displayed candidate normalization interface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close blanket strong normalization, closed consistency, subject reduction, selected normal forms, hidden semantic candidates, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, candidate-interface request exactness, endpoint accounting, residual-frontier exposure, obstruction preservation, Nontrivial and FieldFaithful instances, audit handoff, transport, replay, provenance, and local naming for BEDC.Derived.MetaCICResidualNormalizationObstructionUp.}
\end{closurestatus}
