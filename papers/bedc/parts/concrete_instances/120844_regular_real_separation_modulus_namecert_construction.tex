\chapter{A Concrete Naming Certificate for $\RegularRealSeparationModulusUp$}
\label{ch:concrete-instances-regular-real-separation-modulus-namecert}
\origin{human}

Following Bishop--Bridges constructive real analysis, the $\RegularRealSeparationModulusUp$ packet records the finite positive-gap data by which two regular rational real names are separated before a Real or Completion consumer reads an apartness conclusion. It sits after the regular tail-equivalence route of \autoref{ch:concrete-instances-regular-real-tail-equivalence-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the positive-separation boundary of \autoref{ch:concrete-instances-apartnessreal-namecert}. The packet names a displayed modulus for a strict finite gap; it is not quotient real equality, trichotomy, a selected limit, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Regular real separation modulus carrier]
\label{def:regular-real-separation-modulus-carrier}
A $\RegularRealSeparationModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(L,R,W,D,T,E,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ and $R$ are the two $\RegSeqRatUp$ readback rows whose regular rational windows are compared.
\item $W$ is the shared $\StreamNameUp$ finite-window row exposing the inspected tail request for both readbacks.
\item $D$ is the $\DyadicRatCoreUp$ positive-radius ledger. It records the radius $\rho$ and denominator budget used by the comparison.
\item $T$ is the $\RegularRealTailEquivalenceUp$ refusal row. It states that the inspected tails do not collapse through the sibling tail-equivalence route at the displayed radius.
\item $E$ is the pair of terminal $\RealUp$ seal rows made available only after $L,R,W,D,T$ have been displayed.
\item $A$ is the apartness handoff row to the sibling apartness-real certificate of \autoref{ch:concrete-instances-apartnessreal-namecert}.
\item $H$ records componentwise $\hsame$ transport for $L,R,W,D,T,E,A,C,P,N$.
\item $C$ records displayed $\Cont$ replay of the route
$$
L,R,W,D \longmapsto T \longmapsto E,A \longmapsto H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\RegularRealSeparationModulusUp$, $\RegularRealTailEquivalenceUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RegularRealSeparationModulusUp}$ row.
\end{enumerate}
The classifier reads only these rows. It has no coordinate for quotient real equality, host trichotomy, a selected real representative, countable-choice tail selection, or an ambient completeness witness.
\end{definition}

\begin{theorem}[Regular real separation modulus NameCert obligations]
\label{thm:regular-real-separation-modulus-namecert-obligations}
For every accepted $\RegularRealSeparationModulusUp$ carrier $M=(L,R,W,D,T,E,A,H,C,P,N)$, the local $\NameCert_{\RegularRealSeparationModulusUp}$ surface consists exactly of regular rational readback admission for $L,R$, shared finite-window exposure for $W$, positive dyadic radius and denominator-budget exactness for $D$, tail-equivalence refusal through $T$, delayed real-seal access through $E$, apartness handoff through $A$, componentwise transport, displayed continuation replay, package provenance, and local naming through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-real-separation-modulus-carrier}. The only source rows are the two regular rational readbacks, the shared finite stream window, and the dyadic positive-radius ledger.

The row $T$ is downstream of those source rows. It records that the displayed tails do not collapse through the regular tail-equivalence route at the radius supplied by $D$.

Only after $T$ is displayed may the terminal real seals $E$ and the apartness handoff $A$ be read. The structural rows $H,C,P,N$ transport, replay, source, and name exactly that finite route, so the certificate exports no quotient equality, trichotomy, selected representative, choice schedule, or ambient completeness datum.
\end{proof}

\begin{theorem}[Regular real separation modulus finite-gap route]
\label{thm:regular-real-separation-modulus-finite-gap-route}
Every Real or Completion consumer that reads an accepted $\RegularRealSeparationModulusUp$ carrier must pass through the finite route
$$
\RegSeqRatUp,\StreamNameUp,\DyadicRatCoreUp
\longmapsto
\RegularRealTailEquivalenceUp
\longmapsto
\RealUp,\NameCert .
$$
The resulting apartness read is therefore a positive-gap conclusion over displayed regular rational windows, not a quotient-real comparison, a trichotomy branch, a selected-limit argument, or an ambient completeness principle.
\end{theorem}
\begin{proof}
Start from an accepted carrier $M=(L,R,W,D,T,E,A,H,C,P,N)$. The finite comparison begins with the two regular rational readbacks $L,R$, the shared window $W$, and the dyadic radius row $D$.

Read the tail-equivalence refusal next. Because $T$ is the only row between the source windows and the real seals, any downstream apartness consumer must use the positive radius and denominator budget already displayed in $D$.

Finally, the terminal seals and apartness handoff are transported and replayed by $H,C,P,N$. Since the carrier contains no stronger host-side principle, the consumer receives only the finite positive-gap route named by the local certificate.
\end{proof}

\closureat{RegularRealSeparationModulusUp}{seedStr}

\begin{closurestatus}{\RegularRealSeparationModulusUp}
  \constructivestory{A $\RegularRealSeparationModulusUp$ packet is generated from two RegSeqRat readback rows, one shared StreamName finite-window row, a DyadicRatCore positive-radius ledger, a RegularRealTailEquivalence refusal row, terminal Real seal rows, an apartness handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-real-separation-modulus-carrier}, \autoref{thm:regular-real-separation-modulus-namecert-obligations}, and \autoref{thm:regular-real-separation-modulus-finite-gap-route} seed the finite positive-gap route for regular rational real names.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient real equality, trichotomy, selected representatives, selected limits, countable choice, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, positive-radius exactness, tail-equivalence refusal, real-seal delay, apartness handoff, transport, replay, provenance, and local naming for BEDC.Derived.RegularRealSeparationModulusUp.}
\end{closurestatus}
