\chapter{A Concrete Naming Certificate for $\UniformOscillationModulusUp$}
\label{ch:concrete-instances-uniform-oscillation-modulus-namecert}
\origin{human}

Bishop--Bridges constructive analysis treats uniform control as finite tolerance data rather than as an ambient compactness theorem. The $\UniformOscillationModulusUp$ packet records the finite route by which displayed oscillation windows become a uniform oscillation-control row before any compact-uniform, regular-real, or continuity-modulus consumer reads a seal. It sits beside the oscillation-modulus carrier of \autoref{ch:concrete-instances-oscillation-modulus-namecert}, the finite partition surface of \autoref{ch:concrete-instances-finite-oscillation-partition-namecert}, the finite stream names of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only finite oscillation rows, partition cells, stream windows, dyadic budgets, regular readback, transport, replay, provenance, and local naming data.

\begin{definition}[Uniform oscillation modulus carrier]
\label{def:uniform-oscillation-modulus-carrier}
A $\UniformOscillationModulusUp$ carrier is a finite $\BHist$ packet
$$
U=(O,F,S,R,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $O$ is the $\OscillationModulusUp$ source row, recording tolerance requests and displayed oscillation bounds.
\item $F$ is the $\FiniteOscillationPartitionUp$ row, recording the finite cells on which the oscillation request is checked.
\item $S$ is the $\StreamNameUp$ observation-window row for the cell endpoints and sample names.
\item $R$ is the $\RegSeqRatUp$ readback row for the regular rational bounds produced by the displayed windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance-budget row, recording dyadic precision requests, local comparison margins, and finite budget transfers.
\item $E$ is the terminal $\RealUp$ seal row, available only after the displayed oscillation, partition, stream, regular-rational, and dyadic rows have been read.
\item $H$ records componentwise $\hsame$ transport for the oscillation, partition, stream, readback, dyadic, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from each uniform oscillation request through
$$
O,F,S,R,D,E \longmapsto H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\UniformOscillationModulusUp$, $\OscillationModulusUp$, $\FiniteOscillationPartitionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\UniformOscillationModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $O,F,S,R,D,E,H,C,P,N$. It has no coordinate for open-cover compactness, selected limits, quotient streams, host real equality, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Uniform oscillation modulus NameCert obligations]
\label{thm:uniform-oscillation-modulus-namecert-obligations}
For every accepted $\UniformOscillationModulusUp$ carrier
$$
U=(O,F,S,R,D,E,H,C,P,N),
$$
the local $\NameCert_{\UniformOscillationModulusUp}$ surface consists exactly of oscillation-modulus admission through $O$, finite partition exposure through $F$, stream-window observation through $S$, regular rational readback through $R$, dyadic tolerance budgeting through $D$, terminal real-name sealing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:uniform-oscillation-modulus-carrier}. The analytic rows are exactly the oscillation source, finite partition, stream observations, regular rational readback, dyadic budget, and terminal Real seal.

Read $O$ and $F$ first. The source oscillation row supplies the tolerance request, while the finite partition row supplies the cells on which the request is witnessed.

Next read $S,R,D,E$. Stream windows expose the finite observations, regular rational readback records the numeric bound, the dyadic row stores the budget used by the comparison, and the terminal Real seal is available only after those displayed rows.

Finally read $H,C,P,N$. These rows transport, replay, source, and name the same finite route. Since the carrier has no coordinate for open-cover compactness, selected limits, quotient streams, host real equality, countable choice, or ambient Real completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Uniform oscillation modulus finite-partition readback]
\label{thm:uniform-oscillation-modulus-finite-partition-readback}
Every compact-uniform, regular-real, or continuity-modulus consumer read accepted by a $\UniformOscillationModulusUp$ packet must factor through
$$
\OscillationModulusUp,\ \FiniteOscillationPartitionUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp,\ \RealUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
Equivalently, the consumer may inspect $O,F,S,R,D,E,H,C,P,N$, but it cannot bypass the finite partition and dyadic readback by invoking open-cover compactness, selected limits, quotient streams, host real equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Apply \autoref{thm:uniform-oscillation-modulus-namecert-obligations}. It forces every accepted consumer read to open the oscillation source $O$, finite partition $F$, stream observations $S$, regular rational readback $R$, dyadic budget $D$, and terminal seal $E$ before the structural rows are available.

The finite-partition step is carried by $F$ and $S$. These rows display the cells and observations used by the uniform request, so no consumer can replace them by an open-cover compactness argument or a selected external limit.

The numeric readback is carried by $R,D,E$. Regular rational bounds, dyadic budgets, and the terminal Real seal provide the only exported route to downstream real-facing packets.

Transport, replay, provenance, and naming through $H,C,P,N$ preserve exactly that displayed route. Therefore compact-uniform, regular-real, and continuity-modulus consumers receive a finite partition readback and no quotient-stream, choice, host-equality, or ambient-completeness principle.
\end{proof}

\falsifiablePrediction{An accepted $\UniformOscillationModulusUp$ read is refuted if a compact-uniform, regular-real, or continuity-modulus consumer receives an oscillation-control output without first displaying an OscillationModulus row, a FiniteOscillationPartition row, StreamName observations, RegSeqRat readback, a DyadicRatCore budget, a Real seal, transport, replay, provenance, and local naming rows.}

\independenceWitness{oscillation-modulus, finite-oscillation-partition, streamname, regseqrat, dyadicratcore, real: $\UniformOscillationModulusUp$ is not bijective to any listed sibling because it carries the finite partition readback, dyadic tolerance budget, terminal seal, and uniform consumer handoff in one packet.}

\closureat{UniformOscillationModulusUp}{seedStr}

\begin{closurestatus}{\UniformOscillationModulusUp}
  \constructivestory{A $\UniformOscillationModulusUp$ packet is generated from an OscillationModulus source row, a FiniteOscillationPartition row, StreamName finite observations, RegSeqRat readback, a DyadicRatCore tolerance budget, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:uniform-oscillation-modulus-carrier}, \autoref{thm:uniform-oscillation-modulus-namecert-obligations}, and \autoref{thm:uniform-oscillation-modulus-finite-partition-readback} seed the finite uniform oscillation-control packet for compact-uniform, regular-real, and continuity-modulus consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, selected limits, quotient streams, host real equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, oscillation-modulus admission, finite partition exposure, stream-window observation, regular rational readback, dyadic budgeting, Real seal handoff, transport stability, replay stability, provenance nonescape, and local naming for BEDC.Derived.UniformOscillationModulusUp.}
\end{closurestatus}
