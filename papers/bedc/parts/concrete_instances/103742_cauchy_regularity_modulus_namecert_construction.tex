\chapter{A Concrete Naming Certificate for $\CauchyRegularityModulusUp$}
\label{ch:concrete-instances-cauchy-regularity-modulus-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\CauchyRegularityModulusUp$ packet records a displayed regularity modulus for Cauchy criteria before a $\RealUp$ or Completion consumer is allowed to read a terminal seal. It sits over the Cauchy surface of \autoref{ch:concrete-instances-cauchycriterion-namecert}, the uniform criterion of \autoref{ch:concrete-instances-uniformcauchycriterion-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational sequence route of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic arithmetic ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the finite modulus route from criterion data to regular sequence handoff; it does not select a limit or quotient stream representative.

\begin{definition}[CauchyRegularityModulus carrier]
\label{def:cauchy-regularity-modulus-carrier}
A $\CauchyRegularityModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(K,U,W,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CauchyCriterionUp$ row naming the local Cauchy criterion request.
\item $U$ is the $\UniformCauchyCriterionUp$ row that records the shared criterion surface consumed by the same request.
\item $W$ is the $\StreamNameUp$ window row opened by the criterion request.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, containing the displayed dyadic radius, comparison rows, and denominator discipline used by the modulus.
\item $R$ is the regularity-modulus row. It records the finite threshold assignment that makes the criterion readable as a regular sequence route.
\item $E$ is the $\RegSeqRatUp$ handoff row available only after $K,U,W,D,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the criterion, uniform criterion, window, dyadic ledger, modulus, handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the criterion request through $K,U,W,D,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CauchyRegularityModulusUp$, $\CauchyCriterionUp$, $\UniformCauchyCriterionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyRegularityModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,U,W,D,R,E,H,C,P,N$. It has no coordinate for a selected limit, quotient stream equality, countable choice, host real equality, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[CauchyRegularityModulus NameCert obligations]
\label{thm:cauchy-regularity-modulus-namecert-obligations}
For every accepted $\CauchyRegularityModulusUp$ carrier $M=(K,U,W,D,R,E,H,C,P,N)$, the local $\NameCert_{\CauchyRegularityModulusUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed Cauchy criterion, uniform criterion, stream window, dyadic ledger, regularity modulus, regular rational handoff, transport, replay, provenance, and local naming rows;
\item criterion exactness, requiring $K$ and $U$ to be exposed before any modulus or regular rational handoff is available;
\item window accountability, requiring $W$ to list only the finite observations admitted by the criterion request;
\item dyadic tolerance accountability, requiring $D$ to provide the radius, comparison, slack, and denominator data used by the regularity row;
\item modulus exactness, requiring $R$ to name the finite threshold assignment that turns the Cauchy and uniform criterion rows into a regular sequence route;
\item $\RegSeqRatUp$ handoff, requiring $E$ to consume the same $K,U,W,D,R$ rows before any $\RealUp$ seal may be read;
\item componentwise transport, displayed replay, provenance, and local naming for the same route through $H,C,P,N$.
\end{enumerate}
No CauchyRegularityModulus obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the accepted packet $M=(K,U,W,D,R,E,H,C,P,N)$. The carrier definition exposes exactly the criterion, uniform criterion, window, dyadic ledger, regularity modulus, handoff, and structural rows.

The criterion rows are read first. The row $K$ names the Cauchy request, while $U$ records the uniform criterion surface available to the same request.

The observation and arithmetic rows then determine the finite modulus. The row $W$ lists the admitted finite windows, and $D$ carries the dyadic tolerance data used by the threshold assignment.

Finally $R,E,H,C,P,N$ record the modulus, handoff, transport, replay, source, and local name for the same displayed route. Since the excluded selected-limit, quotient, choice, host-equality, trichotomy, and ambient-completeness objects are absent from the carrier, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[CauchyRegularityModulus RegSeqRat handoff]
\label{thm:cauchy-regularity-modulus-regseqrat-handoff}
Let $M=(K,U,W,D,R,E,H,C,P,N)$ be an accepted $\CauchyRegularityModulusUp$ carrier. Every $\RegSeqRatUp$, $\RealUp$, or Completion-facing consumer of the packet factors through the displayed regularity route
$$
K \longmapsto U \longmapsto W \longmapsto D \longmapsto R \longmapsto E \longmapsto H,C,P,N .
$$
Consequently the consumer receives Cauchy criterion data, uniform criterion data, finite stream observations, dyadic tolerance arithmetic, an explicit regularity modulus, regular rational readback, transport, replay, provenance, and local naming before any Real-facing seal is available.
\end{theorem}
\begin{proof}
Begin with a $\RegSeqRatUp$, $\RealUp$, or Completion-facing read of $M$. By \autoref{def:cauchy-regularity-modulus-carrier}, the read has no direct access to a selected limit, quotient stream equality, or ambient completeness object.

The criterion part of the route is fixed by $K$ and $U$. These rows display the local Cauchy request and the uniform criterion surface before any finite modulus row can be consumed.

The finite data part is fixed by $W$ and $D$. The stream window supplies the observed entries, and the dyadic ledger supplies the tolerance arithmetic that the modulus row must use.

The row $R$ gives the explicit regularity modulus, after which $E$ provides the regular rational handoff. The rows $H,C,P,N$ transport, replay, source, and name the same route, so the consumer has no path to selected limits, quotient equality, choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{proof}

\falsifiablePrediction{Every $\CauchyRegularityModulusUp$ Real or Completion read factors through CauchyCriterion, UniformCauchyCriterion, StreamName, DyadicRatCore, explicit regularity modulus, RegSeqRat handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming rows.}

\independenceWitness{cauchycriterion, uniformcauchycriterion, streamname, regseqrat, dyadicratcore, real: $\CauchyRegularityModulusUp$ is not bijective to any listed sibling because it jointly carries the criterion, uniform criterion, finite-window, dyadic-tolerance, explicit-modulus, regular-handoff, transport, continuation, provenance, and local naming rows as one finite packet.}

\closureat{CauchyRegularityModulusUp}{seedStr}

\begin{closurestatus}{\CauchyRegularityModulusUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-regularity-modulus-carrier}, \autoref{thm:cauchy-regularity-modulus-namecert-obligations}, and \autoref{thm:cauchy-regularity-modulus-regseqrat-handoff} seed the finite regularity-modulus route from Cauchy criteria to regular rational handoff and Real-facing seals.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected limits, quotient stream equality, countable choice, trichotomy, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, criterion exactness, StreamName window accountability, DyadicRatCore tolerance accountability, regularity-modulus exactness, RegSeqRat handoff, terminal seal nonescape, and structural transport for BEDC.Derived.CauchyRegularityModulusUp.}
  \constructivestory{A $\CauchyRegularityModulusUp$ packet is generated from CauchyCriterion and UniformCauchyCriterion rows, StreamName finite windows, a DyadicRatCore tolerance ledger, an explicit regularity-modulus row, a RegSeqRat handoff, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
