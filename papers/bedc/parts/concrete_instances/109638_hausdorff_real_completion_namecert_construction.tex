\chapter{A Concrete Naming Certificate for $\HausdorffRealCompletionUp$}
\label{ch:concrete-instances-hausdorff-real-completion-namecert}
\origin{human}

Following Bishop--Bridges constructive real analysis and Bourbaki's separated completion discipline, the $\HausdorffRealCompletionUp$ packet records the route by which the separated Hausdorff completion surface is read through the displayed regular real-name faces. It sits after the separated completion carrier of \autoref{ch:concrete-instances-hausdorffcompletion-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}. The packet gives completion-facing consumers a named separated Real-completion certificate without asserting quotient completion equality, selected representatives, host real equality, or ambient completeness of $\RealUp$.

\begin{definition}[Hausdorff real completion carrier]
\label{def:hausdorff-real-completion-carrier}
A $\HausdorffRealCompletionUp$ carrier is a finite $\BHist$ packet
$$
Q=(A,U,S,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the $\HausdorffCompletionUp$ separated-completion row that exposes the separated carrier and its local equality boundary.
\item $U$ is the $\MetricCompletionUniquenessUp$ uniqueness row read only as separated limit uniqueness inside the displayed completion route.
\item $S$ is the $\StreamNameUp$ finite-window schedule supplying the observable Cauchy-name requests.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger controlling all finite radii and comparison slacks.
\item $R$ is the $\RegSeqRatUp$ regular rational readback row obtained from $S$ and $D$.
\item $E$ is the terminal $\RealUp$ seal reached from the regular readback route.
\item $H$ records componentwise $\hsame$ transport for the separated-completion, uniqueness, stream, dyadic, regular-rational, real-seal, replay, provenance, and local-name rows.
\item $C$ records displayed $\Cont$ replay from a separated Real-completion request through $A,U,S,D,R,E,H,P,N$.
\item $P$ records $\Pkg$ provenance over $\HausdorffRealCompletionUp$, $\HausdorffCompletionUp$, $\MetricCompletionUniquenessUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HausdorffRealCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $A,U,S,D,R,E,H,C,P,N$. It stores no quotient completion equality, selected completion point, host real equality, countable-choice schedule, hidden Cauchy filter, or ambient $\RealUp$ completeness coordinate.
\end{definition}

\begin{theorem}[Hausdorff real completion NameCert obligations]
\label{thm:hausdorff-real-completion-namecert-obligations}
For every accepted $\HausdorffRealCompletionUp$ carrier
$$
Q=(A,U,S,D,R,E,H,C,P,N),
$$
the local $\NameCert_{\HausdorffRealCompletionUp}$ surface consists exactly of carrier admission, $\HausdorffCompletionUp$ separated-completion exposure through $A$, $\MetricCompletionUniquenessUp$ separated uniqueness exposure through $U$, finite $\StreamNameUp$ window inspection through $S$, $\DyadicRatCoreUp$ tolerance accounting through $D$, $\RegSeqRatUp$ readback through $R$, $\RealUp$ seal discipline through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hausdorff-real-completion-carrier}. The packet displays exactly the separated-completion row, uniqueness row, finite windows, dyadic tolerance ledger, regular rational readback, terminal real seal, and structural rows.

Read the separated-completion route first. The row $A$ supplies only the Hausdorff completion surface, while $U$ records the uniqueness boundary available inside that separated route.

Read the Real-name route next. The finite windows $S$ are measured by the dyadic ledger $D$, the regular rational row $R$ is read from those displayed requests, and the real seal $E$ is reached only after the regular readback appears.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier has no further analytic coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Hausdorff real completion separated limit uniqueness]
\label{thm:hausdorff-real-completion-separated-limit-uniqueness}
Every completion-facing consumer of an accepted $\HausdorffRealCompletionUp$ packet receives separated limit uniqueness only through the finite route
$$
\HausdorffCompletionUp \longmapsto \MetricCompletionUniquenessUp \longmapsto \StreamNameUp \longmapsto \DyadicRatCoreUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
The route is a separated Real-completion readback certificate, not a quotient completion equality theorem, a selected representative, a hidden Cauchy-filter construction, host real equality, or ambient completeness of $\RealUp$.
\end{theorem}
\begin{proof}
Apply \autoref{thm:hausdorff-real-completion-namecert-obligations}. It orders every public completion-facing read through $A,U,S,D,R,E$ before any separated uniqueness claim is visible.

The separated side is local to $A$ and $U$. The Hausdorff completion row supplies the separated carrier boundary, and the metric-completion uniqueness row supplies only the uniqueness read available inside that displayed boundary.

The real-name side is local to $S,D,R,E$. Stream windows, dyadic tolerances, and regular rational readback must be displayed before the terminal $\RealUp$ seal can be consumed by the same route.

Thus a consumer cannot replace the route by quotient completion equality, selected representatives, hidden filters, host equality, or ambient completeness. The packet records separated limit uniqueness only as the displayed finite NameCert route.
\end{proof}

\begin{theorem}[Hausdorff real completion nonescape]
\label{thm:hausdorff-real-completion-nonescape}
No accepted $\HausdorffRealCompletionUp$ packet exports quotient completion equality, a selected completion representative, hidden Cauchy-filter data, countable choice, host real equality, or ambient completeness of $\RealUp$. Every exported separated-completion or Real-facing read is confined to the rows $A,U,S,D,R,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Inspect the carrier definition. Its analytic coordinates are exactly the separated-completion row $A$, uniqueness row $U$, stream windows $S$, dyadic tolerance ledger $D$, regular rational readback $R$, and real seal $E$.

The structural rows add no new analytic principle. Transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$ preserve the displayed packet rather than introducing quotient equalities, representative choices, hidden filters, host equality, or completeness.

Use the separated uniqueness theorem, \autoref{thm:hausdorff-real-completion-separated-limit-uniqueness}. Every separated-completion or Real-facing consumer must pass through the displayed finite route, so none of the excluded data can escape from the packet.
\end{proof}

\falsifiablePrediction{Every accepted $\HausdorffRealCompletionUp$ consumer that claims separated limit uniqueness without reading $\HausdorffCompletionUp$, $\MetricCompletionUniquenessUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row refutes the packet surface.}

\independenceWitness{hausdorffcompletion, metriccompletionuniqueness, streamname, dyadicratcore, regseqrat, real: no listed sibling is bijective to $\HausdorffRealCompletionUp$ because this packet binds separated completion, separated limit uniqueness, finite windows, tolerance accounting, regular readback, Real seal, transport, replay, provenance, and local naming as one L10 route.}

\closureat{HausdorffRealCompletionUp}{seedStr}

\begin{closurestatus}{\HausdorffRealCompletionUp}
  \constructivestory{A $\HausdorffRealCompletionUp$ packet is generated from a HausdorffCompletion separated-completion row, MetricCompletionUniqueness separated uniqueness row, StreamName finite windows, a DyadicRatCore tolerance ledger, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hausdorff-real-completion-carrier}, \autoref{thm:hausdorff-real-completion-namecert-obligations}, \autoref{thm:hausdorff-real-completion-separated-limit-uniqueness}, and \autoref{thm:hausdorff-real-completion-nonescape} seed the separated Real-completion route over HausdorffCompletionUp, MetricCompletionUniquenessUp, StreamNameUp, DyadicRatCoreUp, RegSeqRatUp, RealUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient completion equality, selected representatives, hidden Cauchy filters, countable choice, host real equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{A formal route can encode the displayed carrier and prove carrier admission, separated-completion exposure, uniqueness exposure, window exactness, tolerance exactness, RegSeqRat readback, Real seal handoff, transport, replay, provenance, and nonescape for BEDC.Derived.HausdorffRealCompletionUp.}
\end{closurestatus}
