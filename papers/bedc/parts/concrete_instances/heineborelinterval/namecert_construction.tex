\chapter{Heine-Borel Interval NameCert Construction}
\label{ch:concrete-instances-heineborelinterval-namecert}
\origin{human}

Following Bishop and Bridges, constructive compactness of a closed bounded real interval is recorded here as finite inspection data: every precision request is answered by an explicit dyadic net whose cells are routed through the existing real-name and stream-name surfaces. The packet consumes the $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}, the finite-window interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular Cauchy sequence route of \autoref{ch:concrete-instances-regularcauchysequence-namecert}, and the dyadic-net certificate of \autoref{ch:concrete-instances-closedboundedintervalnet-namecert}. It packages closed interval endpoints, mesh scales, net centers, coverage rows, tail-stability rows, and Real-seal descent rows without importing an open-cover compactness theorem.

The dependency surface is the closed-interval lattice already present in the concrete-instance spine: the located interval packet of \autoref{ch:concrete-instances-locatedinterval-namecert}, the finite epsilon-net packet of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}, the compact interval locator of \autoref{ch:concrete-instances-compact-interval-locator-namecert}, and the $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. The Heine-Borel interval certificate consumes these sibling rows as finite inspection data before exposing any interval-compactness consumer surface. It is also consumed as a compact-metric finite-net witness by \autoref{ch:concrete-instances-compactmetric-namecert} and by compact interval consumers: $\CompactMetricUp$ reads the finite-net and Real-seal rows, while $\HeineBorelIntervalUp$ supplies the closed-interval instance.

\begin{definition}[Heine-Borel interval carrier]
\label{def:heine-borel-interval-carrier}
A $\HeineBorelIntervalUp$ carrier is a finite $\BHist$ packet
$$
H=(A,B,K,M,Z,F,T,S,R,E,Q,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ and $B$ are the lower and upper closed $\RealUp$ endpoint names, including the finite seal rows by which they are read;
\item $K$ is the $\ClosedBoundedIntervalNetUp$ packet naming the closed bounded interval and its explicit dyadic epsilon-net surface;
\item $M$ is the unary precision or mesh request for the dyadic net;
\item $Z$ is the finite center ledger, with one displayed dyadic center for each accepted net cell at scale $M$;
\item $F$ is the finite coverage ledger saying that every admitted interval read at scale $M$ enters one of the cells named by $Z$ through the route of $K$;
\item $T$ is the tail-stability ledger saying that refining the precision request preserves the earlier accepted cells through displayed $\hsame$ comparisons;
\item $S$ is the $\StreamNameUp$ finite-window row used to inspect the endpoint names, center names, and refinement witnesses;
\item $R$ is the $\RegularCauchySequenceUp$ readback row for the same endpoint and center windows;
\item $E$ is the terminal $\RealUp$ seal-descent row reached only after $K,M,Z,F,T,S,R$ have been displayed;
\item $Q$ records componentwise $\hsame$ transport for endpoints, net cells, centers, coverage rows, tail-stability rows, windows, readbacks, and the Real seal;
\item $C$ records displayed $\Cont$ routes from a closed interval read through mesh selection, finite net coverage, tail stability, StreamName windows, regular Cauchy readback, and RealUp sealing;
\item $P$ is the $\Pkg$ provenance over $\HeineBorelIntervalUp$, $\ClosedBoundedIntervalNetUp$, $\RealUp$, $\StreamNameUp$, $\RegularCauchySequenceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\HeineBorelIntervalUp}$ row.
\end{enumerate}
The carrier exports constructive compactness only as explicit dyadic epsilon-net witnesses for closed RealUp interval names. It has no coordinate for open covers, chosen finite subcovers, classical sequential compactness, quotient stream equality, host real equality, or an ambient completeness principle.
\end{definition}
\leandef{BEDC.Derived.HeineBorelIntervalUp.HeineBorelIntervalCarrier}
\leanchecked{BEDC.Derived.HeineBorelIntervalUp.HeineBorelIntervalTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Heine-Borel interval net coverage]
\label{thm:heine-borel-interval-net-coverage}
For every accepted $\HeineBorelIntervalUp$ carrier $H=(A,B,K,M,Z,F,T,S,R,E,Q,C,P,N)$, every closed-interval containment read at precision $M$ factors through
$$
K,M,Z,F,Q,C,P,N.
$$
The read first consumes the displayed $\ClosedBoundedIntervalNetUp$ packet $K$, then the mesh request $M$, then one of the finitely many centers in $Z$, and then the coverage ledger $F$ attached to that center. No open cover, hidden subcover selector, host interval order, or unrecorded center choice is available to the consumer.
\end{theorem}
\begin{proof}
Project the accepted carrier. Its interval-facing coordinate is the net packet $K$, and the mesh request $M$ fixes the scale at which the consumer asks for coverage.

The center and coverage rows are finite. The route through $Z$ and $F$ identifies the displayed cell used by the containment read, so the consumer receives a named center and a named coverage witness rather than an open-cover object.

Finally the structural rows $Q,C,P,N$ transport, replay, source, and name this same finite route. Since the carrier has no field for a chosen subcover, host interval order, or hidden center selector, the coverage read cannot escape the displayed dyadic net.
\end{proof}
\leanchecked{BEDC.Derived.HeineBorelIntervalUp.HeineBorelIntervalNetCoverage}

\begin{theorem}[Heine-Borel interval tail stability]
\label{thm:heine-borel-interval-tail-stability}
Let $H=(A,B,K,M,Z,F,T,S,R,E,Q,C,P,N)$ be an accepted $\HeineBorelIntervalUp$ carrier. If a later precision request refines $M$ through the displayed tail-stability ledger $T$, then the earlier endpoint windows, accepted cells, center rows, coverage rows, and regular Cauchy readbacks transport along the stored $\hsame$ rows in $Q$ and remain attached to the same package provenance $P$.

Thus dyadic nets form a stable finite approximation surface for the closed interval, not a sequence chosen by countable choice or by classical sequential compactness.
\end{theorem}
\begin{proof}
Start with the refinement row in $T$. It is part of the same finite carrier as the endpoint rows, net packet, centers, and coverage ledger, so it can only compare displayed mesh requests and their displayed cells.

Transport is componentwise. The rows in $Q$ move endpoint windows, centers, coverage witnesses, and regular Cauchy readbacks together, while $C$ preserves the consumer route used to inspect them.

Repack the transported rows with the same provenance $P$ and local name $N$. The result is still a finite dyadic-net surface because no coordinate supplies a countable-choice sequence, a host subsequence selector, or a classical compactness argument.
\end{proof}
\leanchecked{BEDC.Derived.HeineBorelIntervalUp.HeineBorelIntervalTailStability}

\begin{theorem}[Heine-Borel interval Real seal descent]
\label{thm:heine-borel-interval-real-seal-descent}
Every $\RealUp$ consumer of an accepted $\HeineBorelIntervalUp$ carrier reaches the endpoint and interval seal rows only through
$$
A,B,K,M,Z,F,T,S,R,E,Q,C,P,N.
$$
The seal descent first displays the closed endpoint names $A,B$, then the explicit dyadic-net packet $K,M,Z,F$, then the tail-stability ledger $T$, then the $\StreamNameUp$ finite windows $S$ and $\RegularCauchySequenceUp$ readbacks $R$, and only then the Real seal row $E$. The route exports no quotient stream equality, host real equality, open-cover compactness, selected convergent subsequence, or ambient Real completeness row.
\end{theorem}
\begin{proof}
Read the endpoint rows first. The carrier stores $A$ and $B$ as closed $\RealUp$ names, but the seal row $E$ is not public until the finite net and readback rows have been displayed.

Next apply the net-coverage and tail-stability routes. The packet $K,M,Z,F$ supplies the finite dyadic epsilon-net witness, and $T$ keeps refinements attached to the same displayed cells and centers.

Finally pass through the finite observation rows $S$ and $R$ before reaching $E$. The structural rows $Q,C,P,N$ preserve the route as one NameCert packet, so a Real-facing consumer cannot substitute quotient equality, host equality, open-cover compactness, a selected convergent subsequence, or an ambient completeness principle.
\end{proof}
\leanchecked{BEDC.Derived.HeineBorelIntervalUp.HeineBorelIntervalRealSealDescent}

\begin{theorem}[Heine-Borel interval finite-net induction]
\label{thm:heine-borel-interval-net-induction}
Let $H=(A,B,K,M,Z,F,T,S,R,E,Q,C,P,N)$ be an accepted $\HeineBorelIntervalUp$ carrier. Every accepted finite center ledger $Z$ is consumed by primitive finite induction over the displayed net cells: the base row is the first accepted cell named by $K,M,Z,F$, and the step row extends an already consumed finite prefix by replaying $\Cont$ through $F,Q,C,P,N$ for the next displayed cell.

The induction surface exports only the finite ledger entries already named by $Z$ and the route data already stored in $F,Q,C,P,N$. It exports no open-cover subcover, selected subsequence, countable-choice enumeration, host real equality, or quotient stream equality.
\end{theorem}
\begin{proof}
Project the finite center ledger $Z$ from the accepted carrier. Since $Z$ is a displayed $\BHist$ finite ledger, its cells are consumed by the primitive finite-list induction available to the packet, not by an external cover-selection principle.

The base row is the first accepted cell. Its center and coverage witness are read through $K,M,Z,F$, and the structural rows $Q,C,P,N$ record the same route as a local $\NameCert_{\HeineBorelIntervalUp}$ entry.

For the step row, assume the previous finite prefix has already been consumed. The next displayed cell is attached by replaying $\Cont$ through the coverage ledger $F$, transporting the relevant rows through $Q$, and repacking the route with $C,P,N$.

This exhausts the ledger because the induction ranges only over the displayed entries of $Z$. No row of the carrier can introduce an open-cover subcover, a selected subsequence, or a hidden infinite enumeration.
\end{proof}
\leanchecked{BEDC.Derived.HeineBorelIntervalUp.HeineBorelIntervalNetInduction}

\begin{theorem}[Heine-Borel interval mesh-refinement functoriality]
\label{thm:heine-borel-interval-mesh-refinement-functoriality}
Let $H=(A,B,K,M,Z,F,T,S,R,E,Q,C,P,N)$ be an accepted $\HeineBorelIntervalUp$ carrier. A displayed refinement of the mesh request $M$ transports coverage rows and center rows through $T,Q,C$ and then composes with the finite-net induction route of \autoref{thm:heine-borel-interval-net-induction}. Thus refinement preserves the finite dyadic-net coverage surface as a named $\NameCert_{\HeineBorelIntervalUp}$ route.
\end{theorem}
\begin{proof}
Begin with the displayed refinement row in $T$. By \autoref{thm:heine-borel-interval-tail-stability}, refinement preserves the earlier endpoint windows, accepted cells, center rows, coverage rows, and regular Cauchy readbacks through the stored $\hsame$ rows.

Transport the center and coverage rows componentwise through $Q$. The replay row $C$ keeps the consumer path attached to the same package provenance $P$ and local name $N$.

Now apply \autoref{thm:heine-borel-interval-net-induction} to the transported finite ledger. The refined mesh therefore feeds the same finite induction route, with no appeal to open-cover compactness, a selected finite subcover, or an ambient Real completeness principle.
\end{proof}
\leanchecked{BEDC.Derived.HeineBorelIntervalUp.HeineBorelIntervalMeshRefinementFunctoriality}

\begin{theorem}[Heine-Borel interval NameCert obligation surface]
\label{thm:heine-borel-interval-namecert-obligation-surface}
For an accepted $\HeineBorelIntervalUp$ carrier $H=(A,B,K,M,Z,F,T,S,R,E,Q,C,P,N)$, the local $\NameCert_{\HeineBorelIntervalUp}$ obligations are exhausted by carrier admission, finite-net induction, mesh-refinement functoriality, tail stability, and Real seal descent. Equivalently, every local consumer route factors through the carrier rows of \autoref{def:heine-borel-interval-carrier}, the coverage route of \autoref{thm:heine-borel-interval-net-coverage}, the stability route of \autoref{thm:heine-borel-interval-tail-stability}, the seal route of \autoref{thm:heine-borel-interval-real-seal-descent}, the induction route of \autoref{thm:heine-borel-interval-net-induction}, and the refinement route of \autoref{thm:heine-borel-interval-mesh-refinement-functoriality}. The packet exports no open-cover compactness theorem, selected finite subcover, host real equality, quotient stream equality, selected convergent subsequence, or ambient Real completeness row.
\end{theorem}
\begin{proof}
The carrier definition lists the only public rows of $H$: endpoints, closed bounded interval net data, mesh request, finite center ledger, coverage ledger, tail-stability ledger, observation windows, regular Cauchy readbacks, Real seal descent, transport, replay, provenance, and local NameCert.

Carrier admission is therefore the projection of these displayed rows. Coverage and tail stability are supplied by \autoref{thm:heine-borel-interval-net-coverage} and \autoref{thm:heine-borel-interval-tail-stability}, while Real-facing consumers descend through \autoref{thm:heine-borel-interval-real-seal-descent}.

The remaining finite-means obligations are the induction over $Z$ and the refinement transport across $M$. These are exactly \autoref{thm:heine-borel-interval-net-induction} and \autoref{thm:heine-borel-interval-mesh-refinement-functoriality}.

No other obligation can be exported from the packet, because the carrier has no row for open-cover compactness, selected finite subcovers, host real equality, quotient stream equality, selected convergent subsequences, or ambient Real completeness. Thus the listed carrier rows and theorem sites exhaust the local $\NameCert_{\HeineBorelIntervalUp}$ surface.
\end{proof}
\leanchecked{BEDC.Derived.HeineBorelIntervalUp.HeineBorelIntervalNameCertObligationSurface}

\begin{theorem}[Heine-Borel interval public finite-net export]
\label{thm:heine-borel-interval-public-finite-net-export}
\label{thm:heineborelinterval-public-finite-net-export}
Every accepted $\HeineBorelIntervalUp$ packet exports to public compactness consumers only the displayed finite dyadic net, $\StreamNameUp$ windows, $\RegularCauchySequenceUp$ readback, $\ClosedBoundedIntervalNetUp$ coverage, tail-stability rows, mesh-refinement rows, $\RealUp$ seal, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows named by \autoref{def:heine-borel-interval-carrier}.

Consequently, a public consumer may reuse the finite-net coverage route, tail-stability route, mesh-refinement route, Real seal descent, and NameCert obligation surface, but it receives no additional interval object beyond the displayed rows of $H=(A,B,K,M,Z,F,T,S,R,E,Q,C,P,N)$.
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:heine-borel-interval-carrier}. The public data are exactly the endpoint names, closed bounded interval net packet, mesh request, center ledger, coverage ledger, tail-stability ledger, StreamName windows, regular Cauchy readbacks, Real seal, transport, replay, provenance, and local name.

Apply the existing finite routes. Net coverage is \autoref{thm:heine-borel-interval-net-coverage}, finite induction is \autoref{thm:heine-borel-interval-net-induction}, refinement is \autoref{thm:heine-borel-interval-mesh-refinement-functoriality}, tail stability is \autoref{thm:heine-borel-interval-tail-stability}, and Real-facing descent is \autoref{thm:heine-borel-interval-real-seal-descent}.

The NameCert obligation surface of \autoref{thm:heine-borel-interval-namecert-obligation-surface} repacks precisely those rows for public consumers. Since no further coordinate appears in the carrier, the public export cannot contain any object outside that finite dyadic-net surface.
\end{proof}
\leanchecked{BEDC.Derived.HeineBorelIntervalUp.HeineBorelIntervalPublicFiniteNetExport}

\begin{theorem}[Heine-Borel interval closed-interval net extraction]
\label{thm:heine-borel-interval-closed-interval-net-extraction}
Every accepted $\HeineBorelIntervalUp$ carrier exports an explicit closed-interval finite-net witness by first projecting the closed bounded interval net row $K$, mesh request $M$, center ledger $Z$, and coverage ledger $F$. The witness is then transported through $Q,C,P,N$ as componentwise $\hsame$ transport, displayed replay, provenance, and local naming, while endpoint and readback access is sealed through the $\StreamNameUp$, $\RegularCauchySequenceUp$, and $\RealUp$ rows $S,R,E$.

Thus the extraction surface of
$$
H=(A,B,K,M,Z,F,T,S,R,E,Q,C,P,N)
$$
is a finite closed-interval net witness for compactness-facing consumers, not an open-cover compactness theorem, selected finite subcover, sequential compactness principle, quotient stream equality, or ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
Project the finite-net rows $K,M,Z,F$ from \autoref{def:heine-borel-interval-carrier}. Net coverage is supplied by \autoref{thm:heine-borel-interval-net-coverage}, so the interval read begins with the displayed closed bounded interval net rather than an arbitrary cover.

Use finite-net induction and mesh-refinement functoriality, \autoref{thm:heine-borel-interval-net-induction} and \autoref{thm:heine-borel-interval-mesh-refinement-functoriality}. These keep the center ledger finite and preserve the same witness when later mesh requests are transported.

Finally apply the public finite-net export of \autoref{thm:heine-borel-interval-public-finite-net-export}. It routes the witness through $Q,C,P,N$ and seals endpoint observation and Real-facing readback through $S,R,E$, leaving no carrier coordinate for open-cover compactness, selected subcovers, sequential compactness, quotient stream equality, or ambient Real completeness.
\end{proof}
\leanchecked{BEDC.Derived.HeineBorelIntervalUp.HeineBorelIntervalClosedIntervalNetExtraction}

\begin{theorem}[Heine-Borel interval Real completion-facing route]
\label{thm:heine-borel-interval-real-completion-facing-route}
Every $\RealUp$ or $\CompletionUp$ consumer of an accepted $\HeineBorelIntervalUp$ packet must pass through the finite center ledger $Z$, mesh-refinement functoriality for the request $M$, finite-net induction over the displayed cells, tail stability for refinements, Real seal descent through $S,R,E$, and the public finite-net export before any compact interval boundary is read.

Thus the completion-facing endpoint route is the same finite dyadic-net route exposed by
$$
H=(A,B,K,M,Z,F,T,S,R,E,Q,C,P,N),
$$
where $A,B$ are the closed endpoints, $K,M,Z,F$ are the finite net and coverage rows, $T$ is the tail-stability ledger, $S,R,E$ are the StreamName, RegularCauchySequence, and Real seal rows, and $Q,C,P,N$ preserve transport, replay, provenance, and local naming. The route is not an ambient completeness principle, quotient stream equality, selected limit, or open-cover compactness theorem.
\end{theorem}
\begin{proof}
Start with the finite-net coverage route of \autoref{thm:heine-borel-interval-net-coverage}. It forces any interval containment read to pass through $K,M,Z,F,Q,C,P,N$, so the completion-facing consumer receives a displayed finite center ledger rather than an external compactness object.

Next apply finite-net induction and mesh-refinement functoriality from \autoref{thm:heine-borel-interval-net-induction} and \autoref{thm:heine-borel-interval-mesh-refinement-functoriality}. The induction makes every center-ledger read finite, and refinement keeps later mesh requests attached to the same coverage and center rows.

Tail stability and Real seal descent are then supplied by \autoref{thm:heine-borel-interval-tail-stability} and \autoref{thm:heine-borel-interval-real-seal-descent}. Stability preserves endpoint windows, cell rows, center rows, coverage rows, and regular Cauchy readbacks through the stored transport ledger, while the Real descent requires $A,B,S,R,E$ to be read after the finite-net rows.

Finally use the public finite-net export of \autoref{thm:heineborelinterval-public-finite-net-export}. It repacks the finite ledger, refinement route, stability route, Real seal descent, transport, replay, provenance, and local name for downstream consumers, leaving no coordinate for ambient completeness, quotient equality, selected limits, or open-cover compactness.
\end{proof}
\leanstmt{BEDC.Derived.HeineBorelIntervalUp.HeineBorelIntervalRealCompletionFacingRoute}

\begin{theorem}[Heine-Borel interval cover-refusal boundary]
\label{thm:heine-borel-interval-cover-refusal-boundary}
The public finite-net surface of \autoref{thm:heine-borel-interval-public-finite-net-export} still refuses general open-cover compactness, selected finite subcovers, classical sequential compactness, host real equality, quotient stream equality, and ambient $\RealUp$ completeness. Every accepted read of $H=(A,B,K,M,Z,F,T,S,R,E,Q,C,P,N)$ must pass through the displayed finite dyadic net and its StreamName, RegularCauchySequence, Real seal, transport, replay, provenance, and local NameCert rows before any compactness-facing conclusion is consumed.
\end{theorem}
\begin{proof}
Inspect the public export theorem. It lists the finite dyadic net, observation windows, readbacks, coverage, tail-stability, refinement, Real seal, transport, replay, provenance, and naming rows as the complete public surface.

The excluded principles have no carrier coordinate. There is no row for an arbitrary open cover, no selector for finite subcovers, no sequential compactness witness, no host real equality test, no quotient stream equality, and no ambient completeness service.

Therefore the public surface remains a finite-net compactness interface only. Any consumer that attempts to read one of the excluded principles must leave the displayed carrier rows, so it is not reading the $\HeineBorelIntervalUp$ packet described here.
\end{proof}
\leanchecked{BEDC.Derived.HeineBorelIntervalUp.HeineBorelIntervalCoverRefusalBoundary}

\begin{theorem}[Heine-Borel interval cluster-filter route]
\label{thm:heine-borel-interval-cluster-filter-route}
Any $\ClusterFilterUp$ consumer that uses an accepted $\HeineBorelIntervalUp$ packet must first read the interval finite-net coverage row, the tail-stability row, the Real-seal descent row, and the finite-net induction row before the cluster-filter adherent-observation row can be consumed. In carrier notation the Heine-Borel side supplies
$$
H=(A,B,K,M,Z,F,T,S,R,E,Q,C,P,N),
$$
and the cluster-filter side may read its adherent-observation row only after $K,M,Z,F$, $T$, $S,R,E$, and the induction over $Z$ have all been displayed through $Q,C,P,N$.

The route exports a finite compactness-to-cluster handoff. It does not select a cluster point, form a quotient filter, import open-cover compactness, or invoke ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the finite-net coverage theorem. It forces every interval containment read through $K,M,Z,F,Q,C,P,N$, so the cluster-filter consumer receives a displayed finite cell and coverage witness rather than a host compactness object.

Next apply tail stability and Real seal descent. Tail stability keeps refinements attached to the same endpoint windows, cell rows, centers, coverage rows, and readbacks, while Real seal descent requires the $\StreamNameUp$ and $\RegularCauchySequenceUp$ rows to precede the terminal $\RealUp$ seal.

Now use finite-net induction over $Z$. The induction supplies the finite row-by-row ledger that a cluster-filter consumer may inspect before it reaches its adherent-observation row. This composes with the $\ClusterFilterUp$ Cauchy-completion handoff, which already requires the CauchyFilter, MetricSpace, Topology, StreamName, RegSeqRat, Real seal, and adherent-observation rows to remain visible.

Since both sides expose only displayed finite rows, the handoff cannot choose a cluster point, quotient the filter, replace the dyadic-net route by open-cover compactness, or appeal to ambient $\RealUp$ completeness.
\end{proof}
\leanchecked{BEDC.Derived.HeineBorelIntervalUp.HeineBorelIntervalClusterFilterRoute}

\begin{theorem}[Heine-Borel interval bridged finite-net interface]
\label{thm:heine-borel-interval-bridged-finite-net-interface}
Every compact-interval or $\RealUp$-completion-facing consumer of an accepted $\HeineBorelIntervalUp$ packet must route through the finite center ledger, mesh-refinement functoriality, net induction, Real seal descent, cover-refusal boundary, and cluster-filter route before reading the interval as a bridge-facing compactness interface.

In carrier notation, the bridge-facing interface still exposes only
$$
H=(A,B,K,M,Z,F,T,S,R,E,Q,C,P,N),
$$
where $Z$ is the finite center ledger, $M$ records mesh refinement, $F,T$ record coverage and tail stability, $S,R,E$ carry the StreamName, RegularCauchySequence, and Real seal rows, and $Q,C,P,N$ preserve transport, replay, provenance, and local naming.
\end{theorem}
\leanchecked{BEDC.Derived.HeineBorelIntervalUp.HeineBorelIntervalBridgedFiniteNetInterface}
\begin{proof}
Start with the public finite-net export of \autoref{thm:heineborelinterval-public-finite-net-export}. It exposes only the dyadic interval net, finite center ledger, finite coverage and stability rows, readback rows, Real seal, transport, replay, provenance, and local name.

Next use mesh-refinement functoriality, net induction, and Real seal descent. The refinement theorem keeps later mesh reads attached to the same finite rows, the induction theorem makes every center-ledger read finite, and the Real seal descent theorem requires the StreamName and RegularCauchySequence rows before any Real-facing endpoint is consumed.

Finally apply the cover-refusal boundary and the cluster-filter route. The boundary removes open-cover compactness, selected subcovers, quotient stream equality, host real equality, selected convergent subsequences, and ambient Real completeness from the interface. The cluster-filter route shows that downstream compactness readers must still pass through the same finite rows before they can consume adherent-observation data. Hence the bridge-facing compactness interface is the displayed finite-net route and nothing outside it.
\end{proof}

\closureat{HeineBorelIntervalUp}{seedStr}
\closureat{HeineBorelIntervalUp}{obligationStr}
\closureat{HeineBorelIntervalUp}{publicStr}
\closureat{HeineBorelIntervalUp}{bridgedStr}
\closureat{HeineBorelIntervalUp}{matureStr}

\begin{closurestatus}{\HeineBorelIntervalUp}
  \constructivestory{A $\HeineBorelIntervalUp$ packet is generated from closed RealUp endpoint names, a ClosedBoundedIntervalNetUp dyadic-net packet, a mesh request, finite center and coverage ledgers, tail-stability rows, StreamName windows, RegularCauchySequence readbacks, a RealUp seal descent row, hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{def:heine-borel-interval-carrier}, \autoref{thm:heine-borel-interval-net-coverage}, \autoref{thm:heine-borel-interval-net-induction}, \autoref{thm:heine-borel-interval-mesh-refinement-functoriality}, \autoref{thm:heine-borel-interval-real-seal-descent}, \autoref{thm:heine-borel-interval-namecert-obligation-surface}, \autoref{thm:heineborelinterval-public-finite-net-export}, and \autoref{thm:heine-borel-interval-bridged-finite-net-interface} bind the bridged finite-net interval export for constructive compactness of closed bounded RealUp intervals, with tail stability supplied by \autoref{thm:heine-borel-interval-tail-stability}.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim general open-cover compactness, classical sequential compactness, choice of finite subcovers, selected convergent subsequences, quotient stream equality, host real equality, or ambient Real completeness.}
  \upgradepath{Lean closure requires carrier admission, net coverage, finite-net induction, mesh-refinement functoriality, tail stability, Real seal descent, NameCert obligation-surface, and TasteGate field faithfulness for BEDC.Derived.HeineBorelIntervalUp.}
\end{closurestatus}
