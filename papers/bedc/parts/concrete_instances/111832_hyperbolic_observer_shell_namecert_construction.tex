\chapter{A Concrete Naming Certificate for $\HyperbolicObserverShellUp$}
\label{ch:concrete-instances-hyperbolic-observer-shell-namecert}
\origin{ai}

The $\HyperbolicObserverShellUp$ packet realizes the boundary-shell reading of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as one finite NameCert surface. It binds the boundary-shadow carrier of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the visual-boundary carrier of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, the observer-expansion carrier of \autoref{ch:concrete-instances-hyperbolic-observer-expansion-namecert}, the Busemann-boundary route of \autoref{ch:concrete-instances-hyperbolic-busemann-boundary-namecert}, and the metric readback of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}. The packet names only a finite observer-facing shell over displayed boundary rows; it is not a completed ideal boundary, a physical observer law, a global hyperbolic space, or a cosmological field model.

\begin{definition}[Hyperbolic observer shell carrier]
\label{def:hyperbolic-observer-shell-carrier}
A $\HyperbolicObserverShellUp$ carrier is a finite $\BHist$ packet
$$
O=(B,V,E,U,M,T,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\HyperbolicBoundaryShadowUp$ row that supplies the visible boundary-shadow face.
\item $V$ is the $\HyperbolicVisualBoundaryUp$ row that reads the shadow as a visual boundary datum.
\item $E$ is the $\HyperbolicObserverExpansionUp$ row that records the focused observer-expansion route carried by the shell.
\item $U$ is the $\HyperbolicBusemannBoundaryUp$ row attaching the shell to Busemann-style boundary comparison.
\item $M$ is the $\HyperbolicMetricUp$ readback row used by the shell when boundary shadows are compared.
\item $T$ is the shell-coherence row, requiring the boundary-shadow, visual-boundary, observer-expansion, Busemann, and metric rows to refer to the same displayed finite source surface.
\item $F$ is the TasteGate-facing row for this AI-origin packet: the Lean development in \texttt{TasteGate.lean} must provide $\mathsf{Nontrivial}\ \HyperbolicObserverShellUp$ and $\mathsf{FieldFaithful}\ \HyperbolicObserverShellUp$ instances preserving $B,V,E,U,M,T,H,C,P,N$ as distinct fields.
\item $H$ records componentwise $\hsame$ transport for boundary shadow, visual boundary, observer expansion, Busemann row, metric readback, shell coherence, TasteGate-facing row, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a shell request through $B,V,E,U,M,T,F,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicObserverShellUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicVisualBoundaryUp$, $\HyperbolicObserverExpansionUp$, $\HyperbolicBusemannBoundaryUp$, $\HyperbolicMetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicObserverShellUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,V,E,U,M,T,F,H,C,P,N$. It has no coordinate for completed ideal-boundary equality, physical observer dynamics, global hyperbolic geometry, a host compactification, or unlogged cosmological expansion.
\end{definition}

\begin{theorem}[Hyperbolic observer shell NameCert obligations]
\label{thm:hyperbolic-observer-shell-namecert-obligations}
For every accepted $\HyperbolicObserverShellUp$ carrier
$$
O=(B,V,E,U,M,T,F,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicObserverShellUp}$ surface consists exactly of boundary-shadow admission, visual-boundary admission, observer-expansion admission, Busemann-boundary attachment, hyperbolic-metric readback, shell-coherence checking, TasteGate-facing nontriviality and field-faithfulness requirements, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-observer-shell-carrier}. The rows $B,V,E,U,M,T,F,H,C,P,N$ are the entire public packet.

The boundary side is carried by $B,V,U,M$. Boundary shadow is read first, visual boundary and Busemann data attach the boundary interpretation, and the metric row supplies the displayed comparison surface.

The observer side is carried by $E$ and $T$. The observer-expansion row records the focused route, while the shell-coherence row requires that route to remain tied to the same displayed boundary and metric surface.

The AI-origin gate is carried by $F$. It records the obligation that the Lean \texttt{TasteGate.lean} development provide Nontrivial and FieldFaithful instances for the same field list rather than for a collapsed shadow token.

Finally $H,C,P,N$ transport, replay, source, and name this finite route. Since no further coordinate is present, the certificate exports no completed ideal boundary, physical observer law, global hyperbolic geometry, host compactification, or unlogged cosmological expansion.
\end{proof}

\begin{theorem}[Hyperbolic observer shell boundary handoff]
\label{thm:hyperbolic-observer-shell-boundary-handoff}
Every accepted $\HyperbolicObserverShellUp$ read factors through
$$
\HyperbolicBoundaryShadowUp,\ \HyperbolicVisualBoundaryUp,\ \HyperbolicObserverExpansionUp,\ \HyperbolicBusemannBoundaryUp,\ \HyperbolicMetricUp
$$
before it can be consumed as an observer-facing boundary shell. The handoff preserves the displayed shell-coherence row and the TasteGate-facing field-faithfulness requirement.
\end{theorem}
\begin{proof}
Start with an accepted shell read. By the carrier definition, the read first displays $B,V,E,U,M$ and cannot reach shell coherence without those rows.

The coherence row $T$ then binds the boundary-shadow, visual-boundary, observer-expansion, Busemann, and metric data to one source surface. This prevents a consumer from mixing a boundary shadow from one packet with an observer-expansion row from another.

The TasteGate-facing row $F$ keeps the AI-origin shell readable as a field-faithful carrier. The required Nontrivial and FieldFaithful instances must preserve the named rows rather than collapse them into an unstructured boundary token.

Transport, replay, provenance, and naming through $H,C,P,N$ preserve the same route. The accepted handoff is therefore finite and cannot export completed boundary equality, physical dynamics, global geometry, host compactification, or unlogged cosmological expansion.
\end{proof}

\falsifiablePrediction{An accepted $\HyperbolicObserverShellUp$ read is refuted if a consumer obtains an observer-facing boundary shell without displaying $\HyperbolicBoundaryShadowUp$, $\HyperbolicVisualBoundaryUp$, $\HyperbolicObserverExpansionUp$, $\HyperbolicBusemannBoundaryUp$, $\HyperbolicMetricUp$, shell coherence, TasteGate-facing nontriviality and field-faithfulness requirements, componentwise transport, replay, provenance, and local naming rows.}

\independenceWitness{hyperbolic-visual-boundary, hyperbolic-observer-expansion, hyperbolic-busemann-boundary, hyperbolic-metric: the visual-boundary packet reads boundary data without owning the observer-expansion route, the observer-expansion packet records focused shadow growth without the complete visual-boundary shell, the Busemann packet supplies boundary comparison without shell coherence, and the metric packet supplies readback without observer-facing boundary packaging. None is bijective to $\HyperbolicObserverShellUp$.}

\closureat{HyperbolicObserverShellUp}{seedStr}

\begin{closurestatus}{\HyperbolicObserverShellUp}
  \constructivestory{A $\HyperbolicObserverShellUp$ packet is generated from HyperbolicBoundaryShadow rows, HyperbolicVisualBoundary rows, HyperbolicObserverExpansion rows, HyperbolicBusemannBoundary rows, HyperbolicMetric rows, shell coherence, TasteGate-facing field obligations, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-observer-shell-carrier}, \autoref{thm:hyperbolic-observer-shell-namecert-obligations}, and \autoref{thm:hyperbolic-observer-shell-boundary-handoff} seed the finite boundary-shadow, visual-boundary, and observer-expansion shell surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed ideal-boundary equality, physical observer dynamics, global hyperbolic geometry, host compactification, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked boundary-shadow admission, visual-boundary admission, observer-expansion admission, Busemann attachment, metric readback, shell coherence, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicObserverShellUp.}
\end{closurestatus}
