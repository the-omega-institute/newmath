\chapter{A Concrete Naming Certificate for $\PositiveSeriesCauchyProductUp$}
\label{ch:concrete-instances-positive-series-cauchy-product-namecert}
\origin{human}

Following Hardy and Knopp on products of positive convergent series, the $\PositiveSeriesCauchyProductUp$ packet records the finite NameCert consumer that reads two positive real-series rows, forms their Cauchy product through bounded convolution windows, and hands the resulting product row to the constructive real-series surface. It sits over the real-series carrier of \autoref{ch:concrete-instances-realseries-namecert}, the positivity route of \autoref{ch:concrete-instances-pringsheim-positive-real-series-namecert}, the convolution carrier of \autoref{ch:concrete-instances-cauchy-product-series-namecert}, the majorant discipline of \autoref{ch:concrete-instances-absolute-convergence-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a consumer of displayed finite Real and completion rows; it is not an ambient summation operator, a quotient equality of infinite series, or a host completeness theorem for $\RealUp$.

\begin{definition}[Positive series Cauchy product carrier]
\label{def:positive-series-cauchy-product-carrier}
A $\PositiveSeriesCauchyProductUp$ carrier is a finite $\BHist$ packet
$$
P=(A,B,W,M,T,S,D,E,H,C,Q,N)
$$
with the following visible rows:
\begin{enumerate}
\item $A$ and $B$ are positive $\RealSeriesUp$ source rows, each exposing finite coefficient windows, partial-sum windows, and the positivity ledgers read by $\PringsheimPositiveRealSeriesUp$ consumers.
\item $W$ is the bounded convolution-window row. For a requested product coefficient or partial product sum it lists only the finitely many pairs $i+j=n$ and the finite rectangular windows needed for the requested tail budget.
\item $M$ is the absolute or majorant tail-control row. It records the positive majorant comparison that bounds the remaining convolution tail before a product read is exposed.
\item $T$ is the $\StreamNameUp$ replay row for the two source streams and the product stream windows.
\item $S$ is the $\RegSeqRatUp$ readback row attached to the partial sums, majorant bounds, and product-tail estimates.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row used to compare source budgets, convolution-window budgets, and the final product tolerance.
\item $E$ is the terminal $\RealUp$ seal for the product value, available only after $S$ and $D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the two source series, convolution window, majorant tail row, stream replay, rational readback, dyadic tolerance, Real seal, provenance, and local name.
\item $C$ records displayed $\Cont$ replay for reading source coefficients, convolution windows, tail bounds, rational tolerances, dyadic comparisons, and the terminal Real seal.
\item $Q$ is the $\Pkg$ provenance over $\PositiveSeriesCauchyProductUp$, $\RealSeriesUp$, $\PringsheimPositiveRealSeriesUp$, $\CauchyProductSeriesUp$, $\AbsoluteConvergenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PositiveSeriesCauchyProductUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through the displayed finite rows. It has no coordinate for an external infinite sum, quotient equality of series, selected completion representative, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[Positive series Cauchy product NameCert obligations]
\label{thm:positive-series-cauchy-product-namecert-obligations}
For every accepted $\PositiveSeriesCauchyProductUp$ carrier $P=(A,B,W,M,T,S,D,E,H,C,Q,N)$, the local $\NameCert_{\PositiveSeriesCauchyProductUp}$ surface consists exactly of carrier admission for the two positive real-series rows, convolution-window row, majorant tail-control row, StreamName replay row, RegSeqRat readback row, DyadicRatCore tolerance row, terminal Real seal, componentwise transport, displayed replay, provenance, and local naming. Every product-facing consumer must read $A,B,W,M,T,S,D,E,H,C,Q,N$ in that finite route before it may consume the product row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:positive-series-cauchy-product-carrier}. The only source series available to the packet are the positive real-series rows $A$ and $B$, and the only product coefficients available to a consumer are those exposed by the bounded convolution window $W$.

The tail step is finite. The row $M$ records the absolute or positive majorant comparison, while $T,S,D$ replay the stream windows, rational readback, and dyadic tolerance comparisons needed to bound the requested product tail.

The terminal product value is then sealed by $E$. Transport, replay, provenance, and local naming are exactly $H,C,Q,N$, so the packet cannot export an external infinite sum, quotient equality of series, selected completion representative, countable choice, or ambient real completeness.
\end{proof}

\begin{theorem}[Positive series Cauchy product Real seal handoff]
\label{thm:positive-series-cauchy-product-real-seal-handoff}
Every $\RealUp$ or completion-facing consumer of an accepted $\PositiveSeriesCauchyProductUp$ packet reaches the product Real seal only through the displayed route
$$
\RealSeriesUp,\ \PringsheimPositiveRealSeriesUp,\ \CauchyProductSeriesUp,\ \AbsoluteConvergenceUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp,\ \RealUp .
$$
The handoff supplies a finite product-series readback over convolution windows and majorant tails. It does not supply ambient Real completeness, a host infinite-sum operator, quotient real equality, selected limit representatives, or a completion object outside the packet.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:positive-series-cauchy-product-namecert-obligations}. They force any product-facing read to expose the two positive source rows and the convolution-window row before the product is read.

Next read the majorant and tolerance rows. The absolute or positive majorant row $M$ bounds the remaining convolution tail, the StreamName row $T$ fixes the finite windows, and the RegSeqRat and DyadicRatCore rows $S,D$ provide the tolerance comparison consumed by the product request.

Finally consume the Real seal $E$. Since every completion-facing read has already passed through $A,B,W,M,T,S,D$, the handoff is a displayed finite route to $\RealUp$, not an ambient summation or completeness principle.
\end{proof}

\closureat{PositiveSeriesCauchyProductUp}{seedStr}

\begin{closurestatus}{\PositiveSeriesCauchyProductUp}
  \constructivestory{A $\PositiveSeriesCauchyProductUp$ packet is generated from two positive RealSeries rows, bounded convolution windows, absolute or majorant tail-control rows, StreamName windows, RegSeqRat readback, DyadicRatCore tolerance comparison, a terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:positive-series-cauchy-product-carrier}, \autoref{thm:positive-series-cauchy-product-namecert-obligations}, and \autoref{thm:positive-series-cauchy-product-real-seal-handoff} seed the finite product-series route from positive source rows to the terminal Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim ambient summation, quotient equality of series, selected completion representatives, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, convolution-window exactness, majorant tail control, tolerance readback, Real-seal nonescape, transport, provenance, refusal, and local naming for BEDC.Derived.PositiveSeriesCauchyProductUp.}
\end{closurestatus}
