\chapter{A Concrete Naming Certificate for $\CompactUniformContinuityFiniteModulusUp$}
\label{ch:concrete-instances-compact-uniform-continuity-finite-modulus-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis and the Heine--Cantor theorem, the $\CompactUniformContinuityFiniteModulusUp$ packet names the finite modulus route that turns compact finite-net data and continuous pointwise-modulus data into a single tolerance-only uniform-modulus row. It sits after the compact carrier of \autoref{ch:concrete-instances-compact-namecert}, the continuous-map surface of \autoref{ch:concrete-instances-continuous-namecert}, the metric comparison rows of \autoref{ch:concrete-instances-metric-namecert}, the rational and dyadic tolerance ledgers of \autoref{ch:concrete-instances-rat-namecert} and \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the finite fold described in \autoref{ch:visions-compact-uniform-continuity}. The packet records a finite ProbeBundle rational-fold handoff only; it does not assert open-cover compactness, choose a pointwise modulus from a host theorem, import $\RealUp$ completeness, or identify quotient functions.

\begin{definition}[Compact uniform continuity finite-modulus carrier]
\label{def:compact-uniform-continuity-finite-modulus-carrier}
A $\CompactUniformContinuityFiniteModulusUp$ carrier is a finite $\BHist$ packet
$$
U=(K,F,M,E,B,R,D,Q,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact finite-net row read through $\CompactUp$;
\item $F$ is the continuous pointwise-modulus row read through $\ContinuousUp$;
\item $M$ is the $\MetricUp$ distance-comparison row used by the finite-net and pointwise-modulus reads;
\item $E$ is the positive $\RatUp$ tolerance request;
\item $B$ is the finite $\mathsf{ProbeBundle}$ of centers, radii, and subordinate pointwise-modulus witnesses;
\item $R$ is the $\RatUp$ lower-bound fold over the displayed bundle radii;
\item $D$ is the $\DyadicRatCoreUp$ tolerance row that converts the rational lower bound into the requested dyadic budget;
\item $Q$ is the $\RegSeqRatUp$ readback row used when the modulus is consumed by regular rational names;
\item $S$ is the $\RealUp$ seal reached only after the finite compact, continuous, metric, rational-fold, dyadic, and regular-readback rows are visible;
\item $H$ records componentwise $\hsame$ transport for $K,F,M,E,B,R,D,Q,S,C,P,N$;
\item $C$ records displayed $\Cont$ replay through the compact source, continuous pointwise-modulus source, metric comparison, rational fold, dyadic tolerance row, regular readback, real seal, transport, provenance, and local naming rows;
\item $P$ is the $\Pkg$ provenance over $\CompactUniformContinuityFiniteModulusUp$, $\CompactUp$, $\ContinuousUp$, $\MetricUp$, $\RatUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\CompactUniformContinuityFiniteModulusUp}$ row.
\end{enumerate}
The carrier has no coordinate for an open-cover compactness theorem, a selected global modulus, ambient $\RealUp$ completeness, quotient equality of functions, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Compact uniform continuity finite-modulus obligations]
\label{thm:compact-uniform-continuity-finite-modulus-obligations}
For every accepted $\CompactUniformContinuityFiniteModulusUp$ carrier $U=(K,F,M,E,B,R,D,Q,S,H,C,P,N)$, the local $\NameCert_{\CompactUniformContinuityFiniteModulusUp}$ surface consists exactly of compact finite-net exposure, continuous pointwise-modulus exposure, metric comparison, positive rational tolerance exposure, finite $\mathsf{ProbeBundle}$ exposure, rational lower-bound fold exposure, dyadic tolerance handoff, regular rational readback, real seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-continuity-finite-modulus-carrier}. The compact source row $K$, continuous row $F$, metric row $M$, tolerance row $E$, and finite bundle row $B$ are the only data from which the lower-bound fold can be read.

The rational fold row $R$ is computed over the displayed bundle; the dyadic row $D$ and regular-readback row $Q$ only transport that finite lower bound to the consumer side. The real seal $S$ is therefore downstream of the same finite compact-continuous route.

The structural rows $H,C,P,N$ preserve, replay, source, and name the packet. Since the carrier stores no open-cover compactness theorem, selected global modulus, ambient completeness claim, quotient function equality, Lean theorem, or bridge row, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Compact uniform continuity finite-modulus nonescape]
\label{thm:compact-uniform-continuity-finite-modulus-nonescape}
Every $\UniformModulusUp$, $\RegSeqRatUp$, or $\RealUp$ consumer of an accepted $\CompactUniformContinuityFiniteModulusUp$ packet must first read
$$
K,F,M,E,B,R,D,Q,H,C,P,N
$$
before it can consume the real-seal row $S$. The finite modulus is therefore the ProbeBundle rational-fold route of the compact-uniform-continuity construction, not an open-cover compactness principle, selected host modulus, quotient-function equality, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Start with $U=(K,F,M,E,B,R,D,Q,S,H,C,P,N)$. Carrier admission makes the compact finite net, continuous pointwise-modulus row, metric comparison, positive rational tolerance request, finite ProbeBundle, rational lower-bound fold, dyadic tolerance handoff, and regular rational readback visible before the seal is available.

Use \autoref{thm:compact-uniform-continuity-finite-modulus-obligations}. Its handoff clauses bind the consumer route to the displayed $K,F,M,E,B,R,D,Q$ rows, while the transport and replay clauses keep the route componentwise stable.

The packet has no coordinate for open-cover compactness, a selected host modulus, quotient equality of functions, or ambient completeness of $\RealUp$. Thus no downstream consumer can detach the seal from the finite ProbeBundle rational-fold route.
\end{proof}

\falsifiablePrediction{A consumer that claims a $\CompactUniformContinuityFiniteModulusUp$ read while omitting the $\CompactUp$ finite-net row, $\ContinuousUp$ pointwise-modulus row, $\MetricUp$ comparison row, positive $\RatUp$ tolerance row, finite $\mathsf{ProbeBundle}$, rational lower-bound fold, $\DyadicRatCoreUp$ tolerance handoff, $\RegSeqRatUp$ readback, $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, or local $\NameCert$ row refutes the carrier of \autoref{def:compact-uniform-continuity-finite-modulus-carrier}.}

\independenceWitness{compact, continuous, metric, rat, dyadicratcore, regseqrat, real: no listed sibling is bijective to $\CompactUniformContinuityFiniteModulusUp$ because this packet alone binds compact finite-net data, continuous pointwise-modulus data, metric comparison, rational ProbeBundle folding, dyadic tolerance routing, regular readback, real sealing, transport, replay, provenance, and local naming as one finite modulus certificate.}

\closureat{CompactUniformContinuityFiniteModulusUp}{seedStr}

\begin{closurestatus}{\CompactUniformContinuityFiniteModulusUp}
  \constructivestory{A $\CompactUniformContinuityFiniteModulusUp$ packet is generated from compact finite-net rows, continuous pointwise-modulus rows, metric comparison rows, positive rational tolerance rows, finite ProbeBundle rows, rational lower-bound fold rows, dyadic tolerance rows, regular rational readback rows, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-continuity-finite-modulus-carrier}, \autoref{thm:compact-uniform-continuity-finite-modulus-obligations}, and \autoref{thm:compact-uniform-continuity-finite-modulus-nonescape} seed the finite ProbeBundle rational-fold modulus route for compact-uniform-continuity consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, a selected global modulus, quotient equality of functions, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net exposure, continuous pointwise-modulus exposure, metric comparison, positive rational tolerance exposure, finite ProbeBundle exposure, rational lower-bound fold exactness, dyadic tolerance handoff, regular rational readback, real seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformContinuityFiniteModulusUp.}
\end{closurestatus}
