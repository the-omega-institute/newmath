\chapter{A Concrete Naming Certificate for $\RegularVariationUp$}
\label{ch:concrete-instances-regular-variation-namecert}
\origin{human}

Following Karamata and Bingham--Goldie--Teugels, the $\RegularVariationUp$ packet records regular variation as finite asymptotic-comparison data rather than as a completed function space. It sits after the $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the slowly varying sibling row of \autoref{ch:concrete-instances-slowly-varying-sequence-namecert}, and the dyadic tolerance discipline of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The packet gives later asymptotic comparison chapters a named BEDC carrier while refusing ambient real completeness, quotient equality of real-valued streams, and host-selected limits.

\begin{definition}[Regular variation carrier]
\label{def:regular-variation-carrier}
A $\RegularVariationUp$ carrier is a finite $\BHist$ packet
$$
Q=(S,L,R,M,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the positive $\RealUp$ scale stream whose inspected windows are exposed before any ratio comparison is read;
\item $L$ is the slowly varying sibling row inherited from $\NameCert_{\SlowlyVaryingSequenceUp}$;
\item $R$ is the ratio-window readback row comparing the displayed values of $S$ against the slowly varying component over the same finite window;
\item $M$ records multiplication and division provenance through the visible $\RealUp$ algebra rows and the regular-rational readback supplied by $\RegSeqRatUp$;
\item $O$ is the eventual order and tolerance comparison row, measured through dyadic and rational windows rather than by an ambient order oracle;
\item $H$ records componentwise $\hsame$ transport for scale, slowly varying, ratio, algebra, order, replay, provenance, and local naming rows;
\item $C$ records displayed $\Cont$ replay from the scale stream through the ratio and order windows;
\item $P$ is the $\Pkg$ provenance over $\RegularVariationUp$, $\RealUp$, $\RegSeqRatUp$, $\SlowlyVaryingSequenceUp$, $\RatUp$, $\OrderUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RegularVariationUp}$ row.
\end{enumerate}
The local classifier compares accepted packets only through $S,L,R,M,O,H,C,P,N$.
\end{definition}

\begin{theorem}[Regular variation NameCert obligations]
\label{thm:regular-variation-namecert}
The local $\NameCert_{\RegularVariationUp}$ surface consists exactly of carrier admission for $Q=(S,L,R,M,O,H,C,P,N)$, positive $\RealUp$ scale exposure through $S$, slowly varying sibling exposure through $L$, ratio-window readback through $R$, visible real algebra provenance through $M$, eventual order and tolerance comparison through $O$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.

Every Real or regular-sequence consumer must read the sibling routes of \autoref{ch:concrete-instances-slowly-varying-sequence-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert} before it treats the packet as a regular-variation comparison. The certificate exports no host function space, no quotient stream equality, no selected real limit, no countable-choice selection of eventual bounds, and no ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted packet. The scale stream, slowly varying row, ratio readback, algebra provenance, order comparison, transport, replay, provenance, and local name are the only visible coordinates.

Read the dependency rows in order. The slowly varying component is supplied by the sibling certificate, the regular rational windows provide finite readback, and the terminal $\RealUp$ seal is reached only after those displayed rows and the algebra provenance have been exposed.

Since the carrier has no coordinate for host functions, quotient stream equality, selected limits, countable choice, or ambient Real completeness, the listed rows exhaust the local NameCert obligation surface.
\end{proof}

\falsifiablePrediction{A purported $\RegularVariationUp$ consumer is refuted if it reads a regular-variation ratio without displaying the positive RealUp scale stream, slowly varying row, ratio-window readback, real-algebra provenance, eventual order comparison, transport, replay, provenance, and local NameCert rows.}
\independenceWitness{slowly-varying-sequence, regseqrat, real, dyadicratcore: the slowly varying sibling lacks the positive scale and ratio-window rows, RegSeqRatUp supplies regular rational readback without the asymptotic classifier, RealUp supplies the terminal seal without eventual comparison data, and DyadicRatCoreUp supplies tolerance rows without the regular-variation carrier. None is bijective to $\RegularVariationUp$.}

\closureat{RegularVariationUp}{seedStr}

\begin{closurestatus}{\RegularVariationUp}
  \constructivestory{A $\RegularVariationUp$ packet is generated from a positive RealUp scale stream, a slowly varying sibling row, ratio-window readback, real-algebra provenance, eventual order and tolerance comparison, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-variation-carrier} and \autoref{thm:regular-variation-namecert} seed the finite regular-variation NameCert surface over RealUp, RegSeqRatUp, SlowlyVaryingSequenceUp, RatUp, OrderUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close Lean verification, ambient RealUp completeness, quotient equality of real-valued streams, selected host limits, countable-choice eventual bounds, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, ratio-window exactness, real-algebra provenance, eventual order comparison, transport, replay, provenance, and local naming for BEDC.Derived.RegularVariationUp.}
\end{closurestatus}
