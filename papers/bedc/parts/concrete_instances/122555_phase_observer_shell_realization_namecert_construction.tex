\chapter{A Concrete Naming Certificate for $\PhaseObserverShellRealizationUp$}
\label{ch:concrete-instances-phase-observer-shell-realization-namecert}
\origin{ai}

The $\PhaseObserverShellRealizationUp$ packet realizes the shell-like observer-expansion branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It sits after the boundary-shadow row of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the visual-boundary row of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, the phase-distance observer-expansion route of \autoref{ch:concrete-instances-phase-distance-observer-expansion-namecert}, the reversible disk-boundary transport of \autoref{ch:concrete-instances-poincare-disk-boundary-transport-namecert}, the reversible phase route of \autoref{ch:concrete-instances-hyperbolic-observer-phase-route-namecert}, and the shell packet of \autoref{ch:concrete-instances-hyperbolic-observer-phase-shell-namecert}. The packet is a concrete realization surface for finite observer-phase shells; it is not a physical observer law, a completed visual boundary, a selected far-end point, or an ambient hyperbolic manifold.

\begin{definition}[Phase observer shell realization carrier]
\label{def:phase-observer-shell-realization-carrier}
A $\PhaseObserverShellRealizationUp$ carrier is a finite $\BHist$ packet
$$
S=(B,V,D,T,R,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\HyperbolicBoundaryShadowUp$ boundary-shadow row.
\item $V$ is the $\HyperbolicVisualBoundaryUp$ visual-boundary readback row.
\item $D$ is the $\PhaseDistanceObserverExpansionUp$ phase-distance comparison row.
\item $T$ is the reversible transport row, read through $\PoincareDiskBoundaryTransportUp$ and $\PoincareDiskPhaseTransportUp$.
\item $R$ is the $\HyperbolicObserverPhaseRouteUp$ route row binding boundary-shadow, visual-boundary, phase-distance, and reversible-transport data.
\item $O$ is the $\HyperbolicObserverPhaseShellUp$ shell row that keeps the observer-facing packet boundary-facing.
\item $H$ records componentwise $\hsame$ transport for $B,V,D,T,R,O,C,P,N$.
\item $C$ records displayed $\Cont$ replay through boundary shadow, visual boundary, phase distance, transport, route, shell, provenance, and local naming.
\item $P$ records
$$
\Pkg(S)=\Pkg(\PhaseObserverShellRealizationUp,\HyperbolicBoundaryShadowUp,\HyperbolicVisualBoundaryUp,\PhaseDistanceObserverExpansionUp,\PoincareDiskBoundaryTransportUp,\PoincareDiskPhaseTransportUp,\HyperbolicObserverPhaseRouteUp,\HyperbolicObserverPhaseShellUp,\BHist,\hsame,\Cont,\NameCert).
$$
\item $N$ is the local $\NameCert_{\PhaseObserverShellRealizationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,V,D,T,R,O,H,C,P,N$. It has no coordinate for global hyperbolic space, completed visual boundary, selected observer center, physical observer dynamics, host phase equality, or unlogged transport.
\end{definition}

\begin{theorem}[Phase observer shell realization NameCert obligations]
\label{thm:phase-observer-shell-realization-namecert-obligations}
For every accepted $\PhaseObserverShellRealizationUp$ carrier
$$
S=(B,V,D,T,R,O,H,C,P,N),
$$
the local $\NameCert_{\PhaseObserverShellRealizationUp}$ surface consists exactly of boundary-shadow admission through $B$, visual-boundary readback through $V$, phase-distance comparison through $D$, reversible-transport admission through $T$, route admission through $R$, shell admission through $O$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:phase-observer-shell-realization-carrier}. Its observable shell data are precisely the boundary-shadow, visual-boundary, phase-distance, reversible-transport, route, and shell rows.

Read the rows in dependency order. Boundary shadow and visual boundary make the shell boundary-facing, phase distance supplies the comparison row, reversible transport records how the surface moves, and the observer phase route binds those data before the shell row is accepted.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier has no coordinate for a completed boundary, selected center, physical dynamics, host phase equality, or unlogged transport, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Phase observer shell realization nonescape]
\label{thm:phase-observer-shell-realization-nonescape}
Every observer-expansion read from an accepted $\PhaseObserverShellRealizationUp$ carrier must factor through
$$
\HyperbolicBoundaryShadowUp,\quad
\HyperbolicVisualBoundaryUp,\quad
\PhaseDistanceObserverExpansionUp,\quad
\PoincareDiskBoundaryTransportUp,\quad
\PoincareDiskPhaseTransportUp,\quad
\HyperbolicObserverPhaseRouteUp,\quad
\HyperbolicObserverPhaseShellUp .
$$
It cannot replace those rows by a global hyperbolic space, a completed visual boundary, a selected far-end point, a physical observer law, host phase equality, or an unlogged reversible transport.
\end{theorem}
\begin{proof}
Apply the NameCert obligations of \autoref{thm:phase-observer-shell-realization-namecert-obligations}. Any accepted read must consume $B,V,D,T,R,O$ before the structural rows are available.

The boundary-shadow and visual-boundary rows make the reading boundary-facing. The phase-distance and reversible-transport rows make the reading phase-facing. The route and shell rows bind those data as observer-expansion evidence.

Finally, $H,C,P,N$ preserve only that finite route. Because the excluded objects are not carrier coordinates, no observer-expansion read can escape to them through the realization packet.
\end{proof}

\falsifiablePrediction{An accepted $\PhaseObserverShellRealizationUp$ read is refuted if it exports an observer-phase shell without displaying boundary-shadow, visual-boundary, phase-distance, reversible-transport, route, shell, hsame, Cont, Pkg, and local NameCert rows.}

\independenceWitness{hyperbolic-boundary-shadow, hyperbolic-visual-boundary, phase-distance-observer-expansion, poincare-disk-boundary-transport, hyperbolic-observer-phase-route: no listed sibling is bijective to $\PhaseObserverShellRealizationUp$ because this packet jointly carries boundary-shadow, visual-boundary, phase-distance, reversible-transport, route, shell, replay, provenance, and local naming rows as one realization surface.}

\closureat{PhaseObserverShellRealizationUp}{seedStr}

\begin{closurestatus}{\PhaseObserverShellRealizationUp}
  \constructivestory{A $\PhaseObserverShellRealizationUp$ packet is generated from boundary-shadow, visual-boundary, phase-distance, reversible-transport, observer-phase route, observer-phase shell, componentwise hsame, Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:phase-observer-shell-realization-carrier}, \autoref{thm:phase-observer-shell-realization-namecert-obligations}, \autoref{thm:phase-observer-shell-realization-nonescape}, the falsifiable prediction, and the independence witness seed the finite observer-phase shell realization over boundary-shadow, visual-boundary, phase-distance, reversible-transport, route, shell, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close global hyperbolic spaces, completed visual boundaries, selected far-end points, physical observer laws, host phase equality, Lean verification, axiom-clean audit, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, boundary-shadow admission, visual-boundary readback, phase-distance comparison, reversible-transport admission, route admission, shell admission, transport, replay, provenance, and local naming for BEDC.Derived.PhaseObserverShellRealizationUp.}
\end{closurestatus}
