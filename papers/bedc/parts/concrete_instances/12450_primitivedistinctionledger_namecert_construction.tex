\chapter{A Concrete Naming Certificate for $\PrimitiveDistinctionLedgerUp$}
\label{ch:concrete-instances-primitive-distinction-ledger-namecert}
\origin{ai}

The $\PrimitiveDistinctionLedgerUp$ packet realizes the primitive mark distinction of \autoref{ch:visions-philosophy-auditable-generativity} before any Boolean, unary, observer, halting, or categorical interpretation is available. It sits beside the observer-local trace of \autoref{ch:concrete-instances-observer-distinction-trace-namecert}, the diagonal finite-trace packet of \autoref{ch:concrete-instances-haltingdistinctiontrace-namecert}, the mature two-endpoint interface of \autoref{ch:concrete-instances-bool-namecert}, the unary-history carrier of \autoref{ch:concrete-instances-nat-namecert}, and the finite kernel category surface of \autoref{ch:concrete-instances-finitekernelcategory-namecert}. Those siblings expose later readings of distinction; this carrier binds the mark-level separation itself as finite NameCert data, before observer, halting, Boolean, unary, or categorical consumers are licensed.

\falsifiablePrediction{If a PrimitiveDistinctionLedgerUp packet is accepted, no consumer can read primitive mark distinction as BoolUp data or host equality unless an explicit BoolUp NameCert bridge row is present.}

\independenceWitness{observerdistinctiontrace, haltingdistinctiontrace, bool, nat, finitekernelcategory: none is bijective because this packet binds primitive bzero/bone mark rows, no-cross msame evidence, trace exposure, anti-Boolean refusal, transport, replay, provenance, and local naming before observer, halting, Boolean, unary, or category readings are available.}

\begin{definition}[Primitive distinction ledger carrier]
\label{def:primitive-distinction-ledger-carrier}
A $\PrimitiveDistinctionLedgerUp$ carrier is a finite $\BHist$ packet
$$
L=(Z,O,D,T,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Z$ is the primitive zero-mark row, exposing the $\bzero$ mark as a mark-level row and not as false, zero, an object identifier, or a host datum.
\item $O$ is the primitive one-mark row, exposing the $\bone$ mark as a mark-level row and not as true, successor, an object identifier, or a host datum.
\item $D$ is the no-cross $\msame$ row, recording the diagonal mark sameness judgments and the absence of generated cross-mark sameness evidence between $\bzero$ and $\bone$.
\item $T$ is the finite trace-exposure row, recording the history windows in which the two primitive mark rows may be observed without turning them into a completed data interface.
\item $R$ is the anti-Boolean refusal row, recording that direct $\BoolUp$ readback, host equality, host disequality, truth-value import, and set-element comparison are unavailable unless a separate $\BoolUp$ NameCert bridge row is displayed.
\item $H$ records componentwise $\hsame$ transport for $Z,O,D,T,R$ and the local naming row.
\item $C$ records displayed $\Cont$ replay from primitive mark exposure through no-cross distinction, trace exposure, refusal, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\PrimitiveDistinctionLedgerUp$, $\ObserverDistinctionTraceUp$, $\HaltingDistinctionTraceUp$, $\BoolUp$, $\NatUp$, $\FiniteKernelCategoryUp$, $\BHist$, $\msame$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\PrimitiveDistinctionLedgerUp}$ row.
\end{enumerate}
The carrier exports only these rows. It has no coordinate for direct Boolean readback, host equality, quotient collapse, observer-local ownership, halting classification, unary counting, or categorical objecthood.
\end{definition}
\leandef{BEDC.Derived.PrimitiveDistinctionLedgerUp.PrimitiveDistinctionLedgerUp}

\begin{theorem}[Primitive distinction ledger NameCert obligations]
\label{thm:primitive-distinction-ledger-namecert-obligations}
For the carrier of \autoref{def:primitive-distinction-ledger-carrier}, the local $\NameCert_{\PrimitiveDistinctionLedgerUp}$ surface consists exactly of carrier admission for $L=(Z,O,D,T,R,H,C,P,N)$, primitive $\bzero$ exposure through $Z$, primitive $\bone$ exposure through $O$, no-cross $\msame$ distinction through $D$, finite trace exposure through $T$, anti-Boolean and anti-host-equality refusal through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\leanchecked{BEDC.Derived.PrimitiveDistinctionLedgerUp.PrimitiveDistinctionLedger\_namecert\_obligations}
\begin{proof}
Project the carrier of \autoref{def:primitive-distinction-ledger-carrier}. The first available rows are $Z$ and $O$, and they expose the two primitive marks only as mark-level rows.

Next read the distinction and trace rows. The row $D$ records the diagonal $\msame$ evidence together with the absence of cross-mark sameness evidence, while $T$ records the finite history windows in which these mark rows are exposed.

Finally attach the refusal and structural rows. The row $R$ blocks direct $\BoolUp$ readback and host equality, and the rows $H,C,P,N$ transport, replay, source, and locally name the same packet. Since the tuple has no further coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Primitive distinction ledger no Boolean collapse]
\label{thm:primitive-distinction-ledger-no-boolean-collapse}
Every accepted $\PrimitiveDistinctionLedgerUp$ consumer route that reads the two primitive mark rows must pass through the refusal row $R$ before any $\BoolUp$ or host-equality interpretation is available. A direct route from $Z,O,D$ to Boolean data, truth values, host equality, or host disequality is not an accepted $\PrimitiveDistinctionLedgerUp$ route.
\end{theorem}
\begin{proof}
Start from the carrier rows in \autoref{def:primitive-distinction-ledger-carrier}. The primitive mark rows $Z,O$ and the no-cross row $D$ are separate from the refusal row $R$ and from any later $\BoolUp$ bridge row.

Apply \autoref{thm:primitive-distinction-ledger-namecert-obligations}. The local NameCert surface requires the anti-Boolean refusal row before the packet is read as a named primitive distinction ledger.

The excluded route has no coordinate in the carrier. Transport, replay, provenance, and local naming preserve $Z,O,D,T,R$ as the same mark-level packet, so they cannot manufacture Boolean data or host equality. A consumer that obtains such a reading has used an additional bridge outside $\PrimitiveDistinctionLedgerUp$.
\end{proof}

\begin{theorem}[Primitive distinction ledger field coverage]
\label{thm:primitive-distinction-ledger-field-coverage}
Every accepted $\PrimitiveDistinctionLedgerUp$ export factors through
$$
Z,O,D,T,R,H,C,P,N.
$$
The consumer receives the primitive zero-mark row, primitive one-mark row, no-cross $\msame$ row, finite trace-exposure row, anti-Boolean refusal row, componentwise transport, displayed replay, package provenance, and local NameCert naming. It receives no direct $\BoolUp$ endpoint, no host equality proof, no observer-local owner, no halting classifier, no unary count, and no categorical object row.
\end{theorem}
\begin{proof}
Use \autoref{thm:primitive-distinction-ledger-namecert-obligations} to identify the complete local certificate surface. Every accepted consumer must expose the five content rows and four structural rows listed there.

Then apply \autoref{thm:primitive-distinction-ledger-no-boolean-collapse}. Any attempt to read the mark rows as Boolean data or host equality must leave the primitive distinction ledger or display a separate bridge row.

The sibling dependencies remain surrounding consumers rather than replacements. $\ObserverDistinctionTraceUp$, $\HaltingDistinctionTraceUp$, $\BoolUp$, $\NatUp$, and $\FiniteKernelCategoryUp$ may be cited through provenance, but none supplies a hidden coordinate outside $Z,O,D,T,R,H,C,P,N$. Therefore the displayed tuple covers every accepted field of the primitive distinction ledger.
\end{proof}

The Lean follow-up file \texttt{lean4/BEDC/Derived/PrimitiveDistinctionLedgerUp/TasteGate.lean} will provide explicit $\mathsf{Nontrivial}\ \PrimitiveDistinctionLedgerUp$ and $\mathsf{FieldFaithful}\ \PrimitiveDistinctionLedgerUp$ instances over the same rows. The instances preserve $Z,O,D,T,R,H,C,P,N$ and keep primitive mark exposure, no-cross $\msame$ evidence, trace exposure, and anti-Boolean refusal as separate fields.

\closureat{PrimitiveDistinctionLedgerUp}{seedStr}

\begin{closurestatus}{\PrimitiveDistinctionLedgerUp}
  \constructivestory{A $\PrimitiveDistinctionLedgerUp$ packet is generated from primitive bzero and bone mark rows, no-cross msame evidence, finite trace exposure, anti-Boolean refusal, hsame transport, Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:primitive-distinction-ledger-carrier}, \autoref{thm:primitive-distinction-ledger-namecert-obligations}, \autoref{thm:primitive-distinction-ledger-no-boolean-collapse}, and \autoref{thm:primitive-distinction-ledger-field-coverage} bind primitive mark distinction as a finite NameCert packet before Boolean, host-equality, observer, halting, unary, or categorical interpretation.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, BoolUp endpoint readback, host equality, observer-local trace ownership, halting classification, unary counting, or categorical objecthood.}
  \upgradepath{Lean closure requires Nontrivial PrimitiveDistinctionLedgerUp and FieldFaithful PrimitiveDistinctionLedgerUp instances in \texttt{lean4/BEDC/Derived/PrimitiveDistinctionLedgerUp/TasteGate.lean}.}
\end{closurestatus}
