\chapter{A Concrete Naming Certificate for $\EquicontinuityFiniteNetSelectorUp$}
\label{ch:concrete-instances-equicontinuity-finite-net-selector-namecert}
\origin{human}

Following the Arzela--Ascoli finite-net reading in Bishop constructive analysis, the $\EquicontinuityFiniteNetSelectorUp$ packet records the finite selector surface that turns an equicontinuous family over a compact metric carrier into a displayed net-indexed modulus handoff. It sits over the family ledger of \autoref{ch:concrete-instances-equicontinuityfamily-namecert}, the compact source of \autoref{ch:concrete-instances-compactmetric-namecert}, the pointwise graph rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the finite net surface of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}, and the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet selects from finite displayed rows only; it is not an open-cover compactness principle, not a host function-space topology, not a choice operator, and not a selector over external points.

\begin{definition}[Equicontinuity finite-net selector carrier]
\label{def:equicontinuity-finite-net-selector-carrier}
An $\EquicontinuityFiniteNetSelectorUp$ carrier is a finite $\BHist$ packet
$$
E=(F,K,G,N,R,L,U,H,C,P,A)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\EquicontinuityFamilyUp$ source row, exposing the shared family ledger and its pointwise tolerance-radius data.
\item $K$ is the $\CompactMetricUp$ source row, exposing compact finite-net and located coverage data for the same source metric.
\item $G$ is the $\ContinuousMapUp$ graph row family whose pointwise modulus rows are inspected by the selector.
\item $N$ is the $\FiniteEpsilonNetUp$ row, carrying the displayed $\mathsf{ProbeBundle}$ of centers and the finite coverage ledger for the requested tolerance scale.
\item $R$ is the positive rational radius ledger attached to the selected centers and family members.
\item $L$ is the finite lower-bound row that compares the selected radii and exports the precision candidate to the modulus consumer.
\item $U$ is the $\UniformModulusUp$ handoff row, available only after $F,K,G,N,R,L$ have displayed the finite selector route.
\item $H$ records componentwise $\hsame$ transport for the family, compact metric, continuous-map, finite-net, radius, lower-bound, uniform-modulus, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a compact finite-net request through the family modulus rows and the lower-bound selector to the uniform-modulus consumer.
\item $P$ is the $\Pkg$ provenance over $\EquicontinuityFiniteNetSelectorUp$, $\EquicontinuityFamilyUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\FiniteEpsilonNetUp$, $\UniformModulusUp$, $\RatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $A$ is the local $\NameCert_{\EquicontinuityFiniteNetSelectorUp}$ row.
\end{enumerate}
The classifier accepts exactly packets whose family, compact metric, continuous graph, finite net, radius ledger, lower-bound row, uniform-modulus handoff, transport, replay, provenance, and local naming are displayed in these coordinates. It has no coordinate for a host open cover, a global choice function, a function-space topology, an external finite subset, or a selected point outside the $\mathsf{ProbeBundle}$ row.
\end{definition}

\begin{theorem}[Equicontinuity finite-net selector NameCert obligations]
\label{thm:equicontinuity-finite-net-selector-namecert-obligations}
For every accepted $\EquicontinuityFiniteNetSelectorUp$ carrier $E=(F,K,G,N,R,L,U,H,C,P,A)$, the local $\NameCert_{\EquicontinuityFiniteNetSelectorUp}$ surface consists exactly of carrier admission for the equicontinuity-family row, compact metric row, continuous-map graph family, finite epsilon-net row, radius ledger, finite lower-bound row, uniform-modulus handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. The selector is valid only when the finite-net centers and the family modulus rows share the same compact source metric before the uniform-modulus row is consumed.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:equicontinuity-finite-net-selector-carrier}. The displayed source rows are $F,K,G,N$, the selector rows are $R,L,U$, and the structural rows are $H,C,P,A$.

The finite centers are read only from $N$. The family modulus information is read only from $F$ and $G$, with $K$ supplying the compact metric source used by the coverage ledger.

The lower-bound row $L$ then compares the displayed radius rows $R$ and exports a precision candidate to $U$. Transport through $H$ and replay through $C$ preserve the same row alignment, while $P$ and $A$ record provenance and local naming. No remaining coordinate can supply a host cover, a choice function, a function-space topology, or an external finite subset.
\end{proof}

\begin{theorem}[Equicontinuity finite-net selector uniform-modulus handoff]
\label{thm:equicontinuity-finite-net-selector-uniform-modulus-handoff}
Every $\UniformModulusUp$ consumer of an accepted $\EquicontinuityFiniteNetSelectorUp$ packet reads the selector through
$$
\EquicontinuityFamilyUp,\quad \CompactMetricUp,\quad \ContinuousMapUp,\quad \FiniteEpsilonNetUp,\quad \UniformModulusUp .
$$
The exported precision is the finite lower-bound row over the displayed net centers and family radius ledger; it is not an externally chosen modulus and not an open-cover compactness theorem.
\end{theorem}
\begin{proof}
Start with the uniform-modulus request. The carrier exposes the consumer boundary only as $U$, so the request must first inspect the equicontinuity family $F$, the compact metric source $K$, the continuous graph family $G$, and the finite epsilon-net row $N$.

The radius ledger $R$ is then read at the displayed net centers and family members. The lower-bound selector $L$ is a finite row over those displayed radii, so the precision candidate handed to $U$ remains inside the packet.

The transport, replay, provenance, and naming rows $H,C,P,A$ preserve this finite route. Thus every consumer sees the selector as a compact finite-net and equicontinuity-family handoff, not as a host-level selector.
\end{proof}

\falsifiablePrediction{Every accepted $\EquicontinuityFiniteNetSelectorUp$ uniform-modulus read must expose an $\EquicontinuityFamilyUp$ source row, a $\CompactMetricUp$ compact source row, $\ContinuousMapUp$ graph rows, a $\FiniteEpsilonNetUp$ ProbeBundle coverage row, finite radius and lower-bound rows, a $\UniformModulusUp$ handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{equicontinuityfamily, compactmetric, continuousmap, finite-epsilon-net, uniformmodulus: $\EquicontinuityFiniteNetSelectorUp$ is not bijective to any listed sibling because it binds the family modulus rows to a compact finite-net lower-bound selector before a UniformModulus consumer can read a tolerance-only precision row.}

\closureat{EquicontinuityFiniteNetSelectorUp}{seedStr}

\begin{closurestatus}{\EquicontinuityFiniteNetSelectorUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:equicontinuity-finite-net-selector-carrier}, \autoref{thm:equicontinuity-finite-net-selector-namecert-obligations}, and \autoref{thm:equicontinuity-finite-net-selector-uniform-modulus-handoff} seed the finite selector route from equicontinuity-family and compact-net rows to a uniform-modulus consumer.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Arzela--Ascoli compactness, function-space topology, host finite subsets, choice of representatives, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source lock, finite-net center visibility, family radius exactness, lower-bound selector stability, uniform-modulus handoff, provenance exhaustion, and TasteGate field faithfulness for BEDC.Derived.EquicontinuityFiniteNetSelectorUp.}
  \constructivestory{An $\EquicontinuityFiniteNetSelectorUp$ packet is generated from an EquicontinuityFamily source row, a CompactMetric source row, ContinuousMap graph rows, a FiniteEpsilonNet ProbeBundle row, finite radius and lower-bound rows, a UniformModulus handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
