\chapter{A Concrete Naming Certificate for $\HyperbolicIdealTriangleUp$}
\label{ch:concrete-instances-hyperbolic-ideal-triangle-namecert}
\origin{human}

Following the classical Poincare-disk treatment of ideal triangles in hyperbolic geometry, the $\HyperbolicIdealTriangleUp$ packet records the finite BEDC surface for three displayed boundary ends, their geodesic sides, the disk metric route, and the boundary-flow handoff. It concretizes the fixed-end route of \autoref{sec:hyperbolic-disk-dynamics-fixed-end} in \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion}. The packet sits after $\HyperbolicMetricUp$ in \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, $\HyperbolicGeodesicBoundaryUp$ in \autoref{ch:concrete-instances-hyperbolic-geodesic-boundary-namecert}, $\UnitDiskUp$ in \autoref{ch:concrete-instances-unitdisk-namecert}, $\PoincareDiskPhaseTransportUp$ in \autoref{ch:concrete-instances-poincare-disk-phase-transport-namecert}, and $\DynSystemUp$ in \autoref{ch:concrete-instances-dynsystem-namecert}. It does not complete the disk, quotient ideal endpoints, select global geodesic representatives, import host analytic flow, or identify the observer-expansion reading with physical spacetime.

\begin{definition}[Hyperbolic ideal triangle carrier]
\label{def:hyperbolic-ideal-triangle-carrier}
A $\HyperbolicIdealTriangleUp$ carrier is a finite $\BHist$ packet
$$
I=(U,M,B,G,T,D,F,J,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ is the $\UnitDiskUp$ disk-coordinate row for the finite interior probes and boundary-end reads inspected by the packet.
\item $M$ is the $\HyperbolicMetricUp$ metric-read row over the same displayed disk data.
\item $B$ is the $\HyperbolicGeodesicBoundaryUp$ row naming three distinct boundary-end positions and the finite incidence data used to form ideal sides.
\item $G$ is the geodesic-side row, recording the three boundary-to-boundary side reads as displayed boundary geodesic data.
\item $T$ is the $\PoincareDiskPhaseTransportUp$ transport row carrying disk-surface movement and boundary provenance.
\item $D$ is the $\DynSystemUp$ finite flow row used when a boundary-flow consumer replays movement along the displayed sides.
\item $F$ is the fixed-end row stating that the three boundary commitments remain the same displayed far-end rows along the routed flow.
\item $J$ is the classifier-stability row for disk coordinates, metric reads, boundary incidence, side rows, phase transport, routed flow, and fixed-end data.
\item $H$ records componentwise $\hsame$ transport for disk, metric, boundary, side, transport, flow, fixed-end, classifier-stability, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an ideal-triangle or boundary-flow request through $U,M,B,G,T,D,F,J,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicIdealTriangleUp$, $\HyperbolicMetricUp$, $\HyperbolicGeodesicBoundaryUp$, $\UnitDiskUp$, $\PoincareDiskPhaseTransportUp$, $\DynSystemUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HyperbolicIdealTriangleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a completed hyperbolic plane, quotient ideal-boundary equality, selected global geodesics, host analytic flow, physical cosmology, or observer substance.
\end{definition}

\begin{theorem}[Hyperbolic ideal triangle NameCert obligations]
\label{thm:hyperbolic-ideal-triangle-namecert-obligations}
For every accepted $\HyperbolicIdealTriangleUp$ carrier $I=(U,M,B,G,T,D,F,J,H,C,P,N)$, the local $\NameCert_{\HyperbolicIdealTriangleUp}$ surface is exhausted by disk-coordinate admission, hyperbolic metric visibility, boundary-end incidence for three displayed ends, geodesic-side visibility, Poincare disk phase transport, finite dynamic-flow replay, fixed-end stability, classifier stability, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-ideal-triangle-carrier}. The rows $U,M,B,G,T,D,F,J,H,C,P,N$ are the only displayed coordinates.

The geometric read begins with $U,M,B,G$. Disk coordinates and metric reads are visible before boundary incidence is used, and the three side rows are built only from the displayed boundary data.

The boundary-flow read uses $T,D,F,J$. Phase transport and finite flow replay carry the same boundary commitments, while classifier stability keeps disk, metric, boundary, side, transport, flow, and fixed-end rows attached to the same packet.

Rows $H,C,P,N$ transport, replay, source, and name the finite carrier. Since no row stores the excluded completed or host-level objects, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Hyperbolic ideal triangle boundary-flow handoff]
\label{thm:hyperbolic-ideal-triangle-boundary-flow-handoff}
Every boundary-flow consumer of an accepted $\HyperbolicIdealTriangleUp$ packet factors through
$$
\UnitDiskUp \longmapsto \HyperbolicMetricUp \longmapsto \HyperbolicGeodesicBoundaryUp \longmapsto \PoincareDiskPhaseTransportUp \longmapsto \DynSystemUp .
$$
The consumer receives three displayed boundary ends, their geodesic-side rows, phase transport, finite routed flow, fixed-end stability, classifier stability, transport, replay, provenance, and local naming. It obtains no completed disk, quotient ideal-boundary equality, selected global geodesic, host analytic flow, physical cosmology, or observer substance.
\end{theorem}
\begin{proof}
Begin with a boundary-flow request. The disk-coordinate row $U$ and metric row $M$ provide the only geometric source data available to the request.

Read $B$ and $G$ next. The three boundary ends and the three ideal sides are displayed as finite incidence and geodesic rows, not as quotient endpoints or selected global geodesics.

Use $T,D,F,J$ for the handoff. The Poincare disk transport row and finite dynamic-flow row replay movement while preserving the fixed-end commitments and classifier readings.

Finally replay the structural rows $H,C,P,N$. They preserve the same finite packet, and the carrier has no coordinate for the excluded completed or host-level objects, so the boundary-flow handoff cannot leave the displayed route.
\end{proof}

\closureat{HyperbolicIdealTriangleUp}{seedStr}

\begin{closurestatus}{\HyperbolicIdealTriangleUp}
  \constructivestory{A $\HyperbolicIdealTriangleUp$ packet is generated from UnitDiskUp coordinate rows, HyperbolicMetricUp metric reads, HyperbolicGeodesicBoundaryUp boundary incidence, three geodesic-side rows, PoincareDiskPhaseTransportUp transport, DynSystemUp finite flow replay, fixed-end stability, classifier stability, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-ideal-triangle-carrier}, \autoref{thm:hyperbolic-ideal-triangle-namecert-obligations}, and \autoref{thm:hyperbolic-ideal-triangle-boundary-flow-handoff} seed the finite ideal-triangle route over disk, metric, boundary, geodesic-side, phase-transport, flow, fixed-end, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed hyperbolic disks, quotient ideal-boundary equality, selected global geodesics, host analytic flows, physical cosmology, observer-substance readings, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked disk visibility, metric-read visibility, boundary incidence, geodesic-side visibility, phase-flow routing, fixed-end stability, classifier stability, ledger nonescape, and local naming for BEDC.Derived.HyperbolicIdealTriangleUp.}
\end{closurestatus}
