\chapter{A Concrete Naming Certificate for $\CompactMetricSequentialNetUp$}
\label{ch:concrete-instances-compact-metric-sequential-net-namecert}
\origin{human}

Following Bishop and Bridges constructive compactness, the $\CompactMetricSequentialNetUp$ packet joins compact metric finite-net data with sequential extraction as a finite L10 neighbour of \autoref{ch:concrete-instances-compact-namecert}, \autoref{ch:concrete-instances-compactmetric-namecert}, \autoref{ch:concrete-instances-metric-namecert}, and \autoref{ch:concrete-instances-streamname-namecert}. It reads total-bounded net schedules, complete-metric Cauchy routes, finite stream windows, regular rational readback, and terminal Real seals as displayed NameCert rows. The packet supports Bolzano--Weierstrass and sequential compactness consumers while refusing open-cover compactness, countable choice, host subsequence selection, quotient stream equality, and detached real completeness.

\begin{definition}[CompactMetricSequentialNet carrier]
\label{def:compact-metric-sequential-net-carrier}
A $\CompactMetricSequentialNetUp$ carrier is a finite $\BHist$ packet
$$
S=(K,T,L,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact metric source row, read through the $\CompactMetricUp$ surface and its compact and metric dependencies.
\item $T$ is the total-bounded net schedule, recording finite centers, mesh radii, coverage witnesses, and refinement rows for the requested tolerances.
\item $L$ is the complete-metric Cauchy route that receives the extracted Cauchy window after the finite-net schedule has stabilized.
\item $W$ is the $\StreamNameUp$ window ledger for the source sequence, selected indices, selected source windows, and monotonicity evidence.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the selected windows and dyadic tail budgets.
\item $E$ is the terminal $\RealUp$ seal reached by the readback and complete-metric route.
\item $H$ records componentwise $\hsame$ transport for the compact metric source, net schedule, Cauchy route, stream windows, readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from compact metric finite nets through sequential windows, regular rational readback, and the terminal seal.
\item $P$ is the $\Pkg$ provenance over $\CompactMetricSequentialNetUp$, $\CompactMetricUp$, $\CompactUp$, $\MetricSpaceUp$, $\TotallyBoundedUp$, $\CompleteMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactMetricSequentialNetUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,T,L,W,R,E,H,C,P,N$. It has no coordinate for an externally chosen subsequence, open-cover compactness, quotient stream equality, countable choice, host equality, or detached $\RealUp$ completeness.
\end{definition}

\begin{theorem}[CompactMetricSequentialNet NameCert obligations]
\label{thm:compact-metric-sequential-net-namecert-obligations}
For every accepted $\CompactMetricSequentialNetUp$ carrier
$$
S=(K,T,L,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\CompactMetricSequentialNetUp}$ surface consists exactly of compact metric source admission through $K$, total-bounded finite-net scheduling through $T$, complete-metric Cauchy routing through $L$, finite stream and subsequence windows through $W$, regular rational readback through $R$, terminal Real sealing through $E$, and structural transport, replay, provenance, and local naming through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-metric-sequential-net-carrier}. The accepted packet exposes exactly the rows $K,T,L,W,R,E,H,C,P,N$.

The compact metric side is finite. The source row $K$ supplies the metric and compact data, while $T$ supplies the total-bounded finite-net schedule used to choose inspected windows at each displayed tolerance.

The sequential side is the row $W$. It records only finite source windows, selected indices, selected windows, and monotonicity evidence, so no host subsequence selector or countable-choice schedule is available.

The completion-facing side is $L,R,E$. The selected windows are read through the regular rational row $R$, then through the complete-metric route $L$, and finally through the terminal Real seal $E$.

The rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier has no coordinate outside these rows, the listed non-escape clauses are part of the NameCert surface.
\end{proof}

\begin{theorem}[CompactMetricSequentialNet subsequence handoff]
\label{thm:compact-metric-sequential-net-subsequence-handoff}
Every compact-metric or Bolzano--Weierstrass consumer of an accepted $\CompactMetricSequentialNetUp$ packet reads the subsequence handoff through the finite route
$$
\CompactMetricUp \longmapsto \TotallyBoundedUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \CompleteMetricUp \longmapsto \RealUp .
$$
The handoff exports the displayed finite-net schedule, selected source windows, regular rational readback, complete-metric Cauchy route, Real seal, and structural rows only; it exports no open-cover compactness theorem, selected infinite subsequence, quotient stream equality, countable choice, host equality, or detached real-completeness principle.
\end{theorem}
\begin{proof}
Start from \autoref{thm:compact-metric-sequential-net-namecert-obligations}. The compact metric source row and the total-bounded schedule must be displayed before a sequence consumer may inspect any selected window.

Next read the stream row $W$. The row contains the finite source windows and the selected-window evidence consumed by the downstream regular rational readback, but it does not contain an independent infinite subsequence object.

Then apply $R,L,E$ in order. The regular rational readback is attached to the displayed windows, the complete-metric route receives that Cauchy data, and the Real seal is reached only at the end of the displayed route.

Finally $H,C,P,N$ preserve the same packet. Therefore every public handoff is confined to the route above and cannot export the excluded open-cover, choice, quotient, host-equality, or detached-completeness data.
\end{proof}

\closureat{CompactMetricSequentialNetUp}{seedStr}

\begin{closurestatus}{\CompactMetricSequentialNetUp}
  \constructivestory{A $\CompactMetricSequentialNetUp$ packet is generated from compact metric source rows, total-bounded finite-net schedules, complete-metric Cauchy routes, StreamName windows, RegSeqRat readback, Real seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-metric-sequential-net-carrier}, \autoref{thm:compact-metric-sequential-net-namecert-obligations}, and \autoref{thm:compact-metric-sequential-net-subsequence-handoff} seed the finite compact-metric sequential extraction route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected infinite subsequences, quotient stream equality, countable choice, host equality, detached Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-net scheduling, sequential-window monotonicity, RegSeqRat readback, complete-metric routing, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactMetricSequentialNetUp.}
\end{closurestatus}
