The $\CauchyEquivalenceSetoidUp$ row feeds the Cauchy--Dedekind gap-router comparison of \autoref{ch:concrete-instances-cauchy-dedekind-gap-router-namecert} only after the finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, and $\RealUp$ classifier rows have been displayed. This keeps the equality-facing setoid as an L10 dependency route into the router rather than a quotient stream, selected representative, host real equality, or ambient completeness surface.
