The horizontal dependency route for $\CauchyEquivalenceSetoidUp$ consumes the finite comparison surface of $\CauchySequenceEquivalenceUp$ from \autoref{ch:concrete-instances-cauchysequenceequivalence-namecert} together with the $\RegSeqRatUp$ readback rows as the equality-facing window before the $\RealUp$ seal is exposed. Thus the setoid packet is read as a sibling dependency in the L10 lattice, not as an isolated quotient object or a detached host equality relation.
