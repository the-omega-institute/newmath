\begin{theorem}[CauchyEquivalenceSetoid RegSeqRat sibling route]
\label{thm:cauchy-equivalence-setoid-regseqrat-sibling-route}
Every accepted $\CauchyEquivalenceSetoidUp$ equality-facing read consumes the finite comparison surface of $\CauchySequenceEquivalenceUp$ from \autoref{ch:concrete-instances-cauchysequenceequivalence-namecert} and the $\RegSeqRatUp$ readback rows of \autoref{ch:concrete-instances-regseqrat-namecert} before exposing the $\RealUp$ seal. Hence the setoid packet is horizontally linked to those sibling certificates rather than being a detached equality object, a quotient stream, or a host real-equality relation.
\end{theorem}
\begin{proof}
Begin with the carrier of \autoref{def:cauchy-equivalence-setoid-carrier}. Its equality-facing route starts with stream-window rows and regular rational readback rows before the dyadic tolerance, classifier-law, and Real seal rows are visible.

Apply the classifier laws and the NameCert surface. These theorems force reflexivity, symmetry, transitivity, readback exactness, and tolerance exactness to remain finite rows over the same comparison packet.

Use the downstream equality dependency theorem. It identifies the route through the Cauchy comparison surface, $\RegSeqRatUp$ readback, dyadic tolerance ledger, classifier row, and terminal $\RealUp$ seal before any completion-facing consumer can read the result.

Therefore the sibling route is substantive and non-quotient: the setoid packet exposes equality only after the RegSeqRat and CauchySequenceEquivalence dependencies have been consumed, and no detached host equality coordinate is present.
\end{proof}
