The $\CauchyEquivalenceSetoidUp$ route also supplies a child dependency toward real separability through explicit Cauchy-modulus schedules. A consumer that reads the dense $\RealSeparabilityUp$ surface of \autoref{ch:concrete-instances-real-separability-namecert} from equivalence classes must first pass the representative tail request through the $\CauchyModulusBridgeUp$ route of \autoref{ch:concrete-instances-cauchy-modulus-bridge-namecert}; only after that finite schedule is displayed may the route seal a $\RealUp$ readback. Thus the setoid is not a quotient-only sink: it records the finite path by which separability consumers inherit explicit Cauchy tail schedules before real-name sealing.
