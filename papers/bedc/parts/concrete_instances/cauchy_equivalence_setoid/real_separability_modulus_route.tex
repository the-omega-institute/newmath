\begin{theorem}[CauchyEquivalenceSetoid RealSeparability forward scope]
\label{thm:cauchy-equivalence-setoid-real-separability-forward-scope}
Every $\RealSeparabilityUp$ dense-modulus consumer that reads through an accepted $\CauchyEquivalenceSetoidUp$ packet must first pass through $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, the $\DyadicRatCoreUp$ tolerance ledger, the $\CauchyModulusBridgeUp$ schedule of \autoref{ch:concrete-instances-cauchy-modulus-bridge-namecert}, and the terminal $\RealUp$ seal before exporting density through \autoref{ch:concrete-instances-real-separability-namecert}. The route exports no quotient equality, selected representative, countable choice, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the local CauchyEquivalenceSetoid carrier of \autoref{def:cauchy-equivalence-setoid-carrier}. Its visible rows begin with two $\StreamNameUp$ windows, two $\RegSeqRatUp$ readbacks, the $\DyadicRatCoreUp$ tolerance ledger, the setoid-test row, and the terminal $\RealUp$ seal.

Next route the dense-modulus request through \autoref{ch:concrete-instances-cauchy-modulus-bridge-namecert}. The bridge supplies the explicit Cauchy-modulus schedule before the RealSeparability consumer may export density.

Finally read the separability endpoint of \autoref{ch:concrete-instances-real-separability-namecert}. Because the carrier and bridge expose only the displayed finite rows, the route cannot appeal to quotient equality, selected representatives, countable choice, or ambient completeness.
\end{proof}
