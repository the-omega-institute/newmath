Every $\CauchyEquivalenceSetoidUp$ classifier consumer that serves $\MetricCompletionUp$ or separated-completion readers first reads the regular-rational comparison route of \autoref{ch:concrete-instances-regseqrat-namecert} and the terminal Real seal route of \autoref{ch:concrete-instances-real-namecert}. The setoid classifier is therefore consumed only through displayed regular-rational windows, finite $\StreamNameUp$ observations, the $\DyadicRatCoreUp$ tolerance ledger, and the $\RealUp$ seal rows before completion-facing data is public. Quotient stream equality and selected representatives are excluded from the route, since no completion consumer receives a classifier except through those displayed rows and their $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ handoff.
