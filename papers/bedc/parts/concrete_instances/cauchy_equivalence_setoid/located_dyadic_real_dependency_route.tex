The $\CauchyEquivalenceSetoidUp$ lattice reads RegSeqRat comparison rows through \autoref{ch:concrete-instances-regseqrat-namecert} and terminal Real classifier rows through \autoref{ch:concrete-instances-real-namecert}. When the located dyadic real route of \autoref{ch:concrete-instances-located-dyadic-real-namecert} is present, it supplies the finite located tolerance handoff between those rows without adding quotient stream equality, selected limits, or ambient Real completeness.
