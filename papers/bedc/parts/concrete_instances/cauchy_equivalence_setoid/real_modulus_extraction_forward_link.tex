\begin{theorem}[CauchyEquivalenceSetoid RealModulusExtraction forward link]
\label{thm:cauchy-equivalence-setoid-real-modulus-extraction-forward-link}
Every $\RealModulusExtractionUp$ consumer that treats a regular-Cauchy
witness as a stable real-facing modulus must first read the
$\CauchyEquivalenceSetoidUp$ classifier rows of
\autoref{def:cauchy-equivalence-setoid-carrier}. The required route exposes
two $\StreamNameUp$ windows, two $\RegSeqRatUp$ readbacks, the
$\DyadicRatCoreUp$ tolerance ledger, the setoid-test row, the $\RealUp$ seal
boundary, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and the local
$\NameCert$ row before the downstream
\autoref{ch:concrete-instances-real-modulus-extraction-namecert} consumer may
name the modulus as stable.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-equivalence-setoid-carrier}. Its
displayed coordinates include the paired stream windows and paired regular
rational readbacks whose comparison is measured by the dyadic tolerance
ledger and the setoid-test row.

The $\RealModulusExtractionUp$ consumer reads a regular-Cauchy witness only as
real-facing data. Therefore the route must pass through the same $\RealUp$
seal boundary and preserve the comparison by the componentwise $\hsame$
transport and displayed $\Cont$ replay rows.

Finally, the provenance and local naming rows keep this as a
$\CauchyEquivalenceSetoidUp$ classifier dependency rather than a host equality
or selected representative. The downstream
\autoref{ch:concrete-instances-real-modulus-extraction-namecert} chapter can
then consume a stable modulus route only after the listed classifier rows have
been displayed.
\end{proof}
