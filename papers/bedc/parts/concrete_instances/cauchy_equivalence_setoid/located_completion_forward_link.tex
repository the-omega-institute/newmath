\begin{theorem}[CauchyEquivalenceSetoid located completion forward link]
\label{thm:cauchy-equivalence-setoid-located-completion-forward-link}
Every $\LocatedCompactCompletionUp$ or completion-facing consumer that reads a regular-Cauchy stream as a stable located real route must first consume the $\CauchyEquivalenceSetoidUp$ classifier side of \autoref{def:cauchy-equivalence-setoid-carrier}. The route passes through the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, and the located compact completion handoff of \autoref{ch:concrete-instances-located-compact-completion-namecert}. It exposes paired stream windows, paired $\RegSeqRatUp$ tails, the $\RealUp$ seal row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row before the downstream completion packet may treat the classifier as stable.
\end{theorem}
\begin{proof}
Project the classifier rows from \autoref{def:cauchy-equivalence-setoid-carrier}. The setoid side compares two regular-Cauchy names by paired stream windows and paired regular rational tails, so a located completion consumer cannot read a single representative without first displaying the comparison rows.

The RegSeqRat and Real sibling chapters provide the forward handoff. The regular rational rows are read through \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal seal is read through \autoref{ch:concrete-instances-real-namecert}; the located compact completion consumer then receives the same classifier route through \autoref{ch:concrete-instances-located-compact-completion-namecert}.

Transport, replay, provenance, and local naming keep the route inside the displayed $\CauchyEquivalenceSetoidUp$ classifier. Thus the forward link supplies the classifier side for located completion consumers without selecting representatives, identifying quotient completions, choosing limits, or asserting host real equality.
\end{proof}
