The compact-metric continuous-image route reads $\CauchyEquivalenceSetoidUp$ only through the displayed $\RegSeqRatUp$ and $\RealUp$ classifier rows before any image-net or compactness consumer is reached. In particular, the finite image-net handoff of \autoref{ch:concrete-instances-compact-metric-continuous-image-namecert} and the total-bounded image carrier of \autoref{ch:concrete-instances-compact-continuous-image-total-bounded-namecert} consume Cauchy equivalence as a regular-rational comparison surface with Real-seal readback, not as quotient stream equality, selected limits, host real equality, or ambient completeness.
