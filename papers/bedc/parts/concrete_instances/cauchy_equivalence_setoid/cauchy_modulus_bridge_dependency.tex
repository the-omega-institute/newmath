Every $\CauchyEquivalenceSetoidUp$ consumer that needs explicit tail schedules must route representative-tail reads through \autoref{ch:concrete-instances-cauchy-modulus-bridge-namecert} before it asks $\RegSeqRatUp$ or $\RealUp$ for readback. This dependency keeps the setoid interface as a finite schedule route: representative tails are first mediated by the $\CauchyModulusBridgeUp$ ledger, then exposed through regular rational windows and the terminal Real seal, rather than being treated as a quotient-only sink.
