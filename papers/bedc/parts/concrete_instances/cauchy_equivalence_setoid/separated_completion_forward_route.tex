\begin{theorem}[CauchyEquivalenceSetoid separated-completion forward route]
\label{thm:cauchy-equivalence-setoid-separated-completion-forward-route}
Every $\SeparatedCompletionUp$ consumer that reads an accepted $\CauchyEquivalenceSetoidUp$ packet consumes the setoid boundary through \autoref{ch:concrete-instances-separatedcompletion-namecert} after the displayed $\CauchySequenceEquivalenceUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows have been exposed. The route is a finite comparison handoff into separated completion, not quotient completion equality, selected representatives, host real equality, countable choice, or ambient Real completeness.
\end{theorem}
\begin{proof}
Start with the CauchyEquivalenceSetoid carrier of \autoref{def:cauchy-equivalence-setoid-carrier}. It exposes two finite StreamName windows, two RegSeqRat readbacks, the DyadicRatCore tolerance ledger, the setoid-test row, and the Real seal boundary.

Use the downstream equality dependency and completion dependency routes already displayed in this chapter. They place completion-facing consumers behind the same finite comparison data rather than behind a quotient of Cauchy names.

Now read the separated-completion carrier. Its Cauchy-equivalence source row consumes the displayed CauchySequenceEquivalence, RegSeqRat, StreamName, DyadicRatCore, and Real rows before separated comparison is available. Hence the forward route into $\SeparatedCompletionUp$ is exactly the finite setoid boundary and not a quotient-completion equality.
\end{proof}
