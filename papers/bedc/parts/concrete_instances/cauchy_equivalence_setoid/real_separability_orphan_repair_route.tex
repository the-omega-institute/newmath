The top-level $\CauchyEquivalenceSetoidUp$ route supplies the finite classifier consumed by \autoref{ch:concrete-instances-regseqrat-namecert} before the dense-readback schedules of \autoref{ch:concrete-instances-real-separability-namecert} inspect any $\RealUp$ endpoint. The consumer lattice is therefore visible at the hub: Cauchy-equivalence rows expose StreamName windows, RegSeqRat readbacks, dyadic tolerances, the displayed setoid classifier, transport, replay, provenance, and local naming; RealSeparability reads those rows as finite dense-readback data rather than as quotient stream equality, selected representatives, host real equality, countable choice, or ambient completeness.
