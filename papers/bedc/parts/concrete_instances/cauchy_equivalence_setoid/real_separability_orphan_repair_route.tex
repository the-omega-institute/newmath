\begin{theorem}[CauchyEquivalenceSetoid RealSeparability route]
\label{thm:cauchy-equivalence-setoid-real-separability-orphan-repair}
Every $\RealSeparabilityUp$ dense-readback consumer of $\CauchyEquivalenceSetoidUp$ first reads the finite $\StreamNameUp$ windows, the $\RegSeqRatUp$ readbacks of \autoref{ch:concrete-instances-regseqrat-namecert}, dyadic tolerance data, the displayed setoid classifier, the $\RealUp$ seal, componentwise transport, replay, provenance, and local naming rows. Thus the downstream route to \autoref{ch:concrete-instances-real-separability-namecert} is a finite dependency row through the Cauchy-equivalence carrier, not quotient stream equality, selected representatives, host real equality, countable choice, or ambient completeness.
\end{theorem}
