The forward equality route for $\CauchyEquivalenceSetoidUp$ is a finite dependency path through \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert}. It keeps the sibling edge outside the hub and records that equality-facing reads are downstream of regular rational and dyadic tolerance data.

\begin{theorem}[Cauchy equivalence setoid RegSeqRat--Real forward route]
\label{thm:cauchy-equivalence-setoid-regseqrat-real-forward}
Every equality-facing read exported by $\CauchyEquivalenceSetoidUp$ first consumes finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, and $\DyadicRatCoreUp$ tolerance data, and only then reaches the terminal $\RealUp$ seal. Thus the setoid route exposes regular rational observations and dyadic comparison rows before any Real-facing equality handoff, and it exports no selected representatives, quotient stream object, host real equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the accepted Cauchy-equivalence carrier from \autoref{def:cauchy-equivalence-setoid-carrier}. Its comparison surface begins with finite stream windows and regular rational readback rows.

Apply the dyadic tolerance route. The tolerance ledger is read before the Real seal, so a Real-facing equality handoff cannot bypass the dyadic comparison cells.

Finally apply the existing Real classifier dependency route for $\CauchyEquivalenceSetoidUp$. It reaches $\RealUp$ only after the regular rational and dyadic rows have been displayed, which gives the stated forward route without selected representatives, quotient stream objects, host real equality, or ambient completeness.
\end{proof}
