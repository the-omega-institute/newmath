The completion-facing dependency lattice for $\CauchyEquivalenceSetoidUp$ routes equality reads through the regular-Cauchy classifier before any metric-completion consumer sees them. In particular, \autoref{ch:concrete-instances-regseqrat-namecert} supplies the regular rational readback rows that make the setoid comparison visible as finite stream data, and \autoref{ch:concrete-instances-metriccompletion-namecert} consumes that comparison only after the metric-completion carrier has exposed its tolerance and completion rows. Thus the setoid packet is a child dependency of the regular-Cauchy and metric-completion route, not a free-standing quotient equality or detached host equivalence relation.
