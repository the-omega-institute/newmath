The $\CauchyEquivalenceSetoidUp$ completion reads consume the finite Cauchy-filter route of \autoref{ch:concrete-instances-cauchyfilter-namecert} before the regular-sequence and real-name consumers identify tail-equivalent regular Cauchy names. In this route, the filter base fixes the displayed tail packets and Cauchy-modulus rows, \autoref{ch:concrete-instances-regseqrat-namecert} reads them as regular rational sequence evidence, and \autoref{ch:concrete-instances-real-namecert} seals the same data as a terminal real-name consumer without adding quotient filters, selected limits, or ambient completeness.
