\begin{theorem}[Cauchy equivalence setoid seed closure for Real separability]
\label{thm:cauchy-equivalence-setoid-seed-closure}
Every $\RealSeparabilityUp$ dense-read packet that consumes a Cauchy-name classifier reads the $\CauchyEquivalenceSetoidUp$ route before exporting dense real rows. The consumed data are the displayed stream comparison, regular rational readback, metric-completion boundary, componentwise $\hsame$ transport, $\Cont$ replay, provenance, and local $\NameCert$ rows of this hub; the export does not acquire quotient real equality, selected representatives, countable choice, or ambient $\RealUp$ completeness from the setoid classifier.
\end{theorem}
\begin{proof}
Read the hub through its regular-rational and Real-separability sibling routes. They expose the Cauchy classifier as finite comparison data before the dense real consumer receives any exported row.

The dense read therefore factors through $\StreamNameUp$, $\RegSeqRatUp$, $\MetricCompletionUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support already displayed by the setoid packet. Since none of those rows stores quotient real equality, a chosen representative, countable choice, or ambient completeness, the RealSeparability consumer cannot export such data from this dependency.
\end{proof}
