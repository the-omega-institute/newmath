The $\CauchyEquivalenceSetoidUp$ classifier is consumed only after the $\RegSeqRatUp$ readback route has exposed the regular rational observation and before the $\RealUp$ seal comparison is read, so the setoid row remains an L10 dependency between \autoref{ch:concrete-instances-regseqrat-namecert} and \autoref{ch:concrete-instances-real-namecert} rather than an isolated quotient or host-equality surface.
