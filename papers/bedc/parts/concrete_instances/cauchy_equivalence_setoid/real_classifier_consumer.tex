\begin{theorem}[Cauchy equivalence setoid Real classifier consumer]
\label{thm:cauchy-equivalence-setoid-real-classifier-consumer}
Every $\RealUp$ classifier consumer of the $\CauchyEquivalenceSetoidUp$ route must pass through the Cauchy equivalence carrier of \autoref{def:cauchy-equivalence-setoid-carrier}, the $\RegSeqRatUp$ readback rows, and the Real classifier dependency route before it can read the terminal Real classifier boundary of \autoref{ch:concrete-instances-real-namecert}. The consumer receives the two finite $\StreamNameUp$ windows, regular rational readbacks, $\DyadicRatCoreUp$ tolerance ledger, setoid-test row, Real seal boundary, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row, but it receives no quotient stream equality, host real equality, selected limit, countable choice, trichotomy, ambient completeness theorem, or detached Real classifier oracle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-equivalence-setoid-carrier}. Any equality-facing consumer first sees the displayed stream windows, regular rational readbacks, dyadic tolerance ledger, setoid-test row, Real seal boundary, and structural rows.

Use the local Real classifier dependency route. That route sends downstream Real classifier reads through the finite comparison surface and the regular rational readback rows before \autoref{ch:concrete-instances-real-namecert} can be consumed.

The structural rows then transport, replay, source, and name the same path. Since the carrier has no quotient, host-equality, selected-limit, choice, trichotomy, ambient-completeness, or detached-classifier coordinate, every $\RealUp$ classifier consumer remains on the displayed setoid route.
\end{proof}
