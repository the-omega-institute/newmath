\begin{theorem}[SeparatedCompletion displayed setoid dependency]
\label{thm:cauchy-equivalence-setoid-separated-completion-reverse-dependency}
Every $\SeparatedCompletionUp$ read that cites the Cauchy-equivalence component of its carrier reads it only as displayed classifier evidence from the $\CauchyEquivalenceSetoidUp$ surface of \autoref{def:cauchy-equivalence-setoid-carrier}. Thus the route through \autoref{ch:concrete-instances-separatedcompletion-namecert} consumes the finite setoid rows $S_0,S_1,R_0,R_1,D,T,E,H,C,P,N$ before separated-completion equality is exported, and it does not replace those rows by quotient classes, selected representatives, host real equality, or ambient completeness.
\end{theorem}
\begin{proof}
Project the separated-completion carrier through its Cauchy-equivalence source row. The row is a consumer of displayed classifier evidence, so it must read the local $\CauchyEquivalenceSetoidUp$ carrier before the separated-completion export is available.

The carrier of \autoref{def:cauchy-equivalence-setoid-carrier} lists exactly the two stream windows, two regular-rational readbacks, dyadic tolerance ledger, setoid-test row, real seal boundary, componentwise transport, replay, provenance, and local naming rows. These are the rows read by the separated-completion route.

Since no quotient class, selected representative, host real equality, or ambient-completeness witness appears in that carrier, the reverse dependency records only the displayed classifier route into \autoref{ch:concrete-instances-separatedcompletion-namecert}.
\end{proof}
