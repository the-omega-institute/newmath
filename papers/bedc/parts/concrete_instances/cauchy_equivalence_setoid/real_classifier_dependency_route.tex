The downstream $\RealClassifierUp$ consumer of \autoref{ch:concrete-instances-realclassifier-namecert} reads $\CauchyEquivalenceSetoidUp$ only after the finite comparison surface of \autoref{ch:concrete-instances-cauchysequenceequivalence-namecert} and the regular rational readback rows of \autoref{ch:concrete-instances-regseqrat-namecert} have exposed their visible classifier data. Thus the setoid packet supplies the equality-facing sibling lattice route into the Real classifier without becoming a detached quotient equality, a selected limit, or a host real-equality oracle.
