The $\CauchyEquivalenceSetoidUp$ packet is also read in reverse from the regular-Cauchy and Real completion side: consumers in \autoref{ch:concrete-instances-regseqrat-namecert} and \autoref{ch:concrete-instances-real-namecert} cite its equality-facing classifier only after the displayed regular rational readback, dyadic tolerance ledger, and terminal Real seal have all been exposed. This records the horizontal dependency edge without turning the setoid into quotient stream equality, selected representatives, host real equality, or ambient completeness.
