The $\CauchySpaceUp$ consumers of \autoref{ch:concrete-instances-cauchyspace-namecert} read $\CauchyEquivalenceSetoidUp$ from \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} only after the $\StreamNameUp$ finite windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance row, setoid-test row, terminal $\RealUp$ seal, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows are visible. Thus the setoid packet reaches Cauchy-space as a routed finite comparison surface, not as a detached quotient representative or host equality selector.
