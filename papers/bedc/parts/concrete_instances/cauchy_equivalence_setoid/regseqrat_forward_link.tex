The $\RegSeqRatUp$ consumer edge of \autoref{ch:concrete-instances-regseqrat-namecert} reads $\CauchyEquivalenceSetoidUp$ only as a displayed finite classifier. The route is forced through the setoid carrier, the open-tail classifier, and the regular-Cauchy uniqueness boundary before any regular rational readback is exported.

\begin{theorem}[RegSeqRat forward link from Cauchy equivalence setoid]
\label{thm:cauchy-equivalence-setoid-regseqrat-forward-link}
Every accepted $\CauchyEquivalenceSetoidUp$ classifier read used by a $\RegSeqRatUp$ consumer factors through the finite classifier of \autoref{def:cauchy-equivalence-setoid-carrier}, the open-tail route of \autoref{thm:cauchy-equivalence-setoid-open-tail-classifier}, and the regular-Cauchy uniqueness route of \autoref{thm:regular-cauchy-uniqueness-cauchy-equivalence-setoid-route}. Thus a $\RegSeqRatUp$ consumer receives stream windows, regular rational readback rows, the dyadic tolerance ledger, the setoid-test row, the terminal $\RealUp$ seal, transport, replay, provenance, and local naming as one finite route, not as quotient stream equality, selected representatives, host real equality, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier $Q=(S_0,S_1,R_0,R_1,D,T,E,H,C,P,N)$ from \autoref{def:cauchy-equivalence-setoid-carrier}. The only visible comparison surface begins with the two $\StreamNameUp$ windows and their two $\RegSeqRatUp$ readback rows.

Apply the open-tail classifier route. It places the dyadic tolerance ledger and setoid-test row after those regular rational readbacks and before the terminal $\RealUp$ seal, so the consumer cannot replace the route by a detached equality of streams.

Finally apply the regular-Cauchy uniqueness route. That route consumes the same displayed classifier rows before exporting any uniqueness-facing regular Cauchy readback, and therefore gives the forward link into \autoref{ch:concrete-instances-regseqrat-namecert} without selected representatives, host equality, or an ambient completion principle.
\end{proof}
