\begin{theorem}[Cauchy equivalence setoid Real-separability reverse route]
\label{thm:cauchy-equivalence-setoid-real-separability-reverse-route}
Every $\RealSeparabilityUp$ dense read that uses regular Cauchy representatives must preserve the same $\CauchyEquivalenceSetoidUp$ classifier before any dense-seal comparison is exported. Thus a consumer returning from \autoref{ch:concrete-instances-real-separability-namecert} to the Cauchy-equivalence surface must keep the two StreamName windows, RegSeqRat readbacks, DyadicRatCore tolerance ledger, setoid-test row, terminal Real seal, transport, replay, provenance, and local naming rows visible.
\end{theorem}
\begin{proof}
Start with the accepted Cauchy-equivalence carrier and local classifier route. The route in \autoref{thm:cauchy-equivalence-setoid-real-separability-route} sends a Real-separability consumer through the displayed StreamName, RegSeqRat, DyadicRatCore, setoid-test, and Real-seal rows before density is public.

Now read the dense-seal comparison back through \autoref{ch:concrete-instances-real-separability-namecert}. A Real-separability dense read supplies a countable dense surface only after its regular readback, dyadic tolerance, Real seal, transport, replay, provenance, and local naming rows have been displayed.

The reverse route therefore preserves the original classifier row. If the consumer dropped it, the returned dense-seal comparison would be a quotient stream equality, selected representative, countable-choice selector, host real equality, or ambient completeness claim, none of which is present in the accepted $\CauchyEquivalenceSetoidUp$ carrier.
\end{proof}
