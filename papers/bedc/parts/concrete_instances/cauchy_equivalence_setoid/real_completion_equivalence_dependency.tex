\noindent
The $\CauchyEquivalenceSetoidUp$ route is consumed by the $\RealUp$ completion classifier of \autoref{ch:concrete-instances-real-namecert}. A regular Cauchy name routed through $\RegSeqRatUp$ reaches the Real seal only after equality of regular Cauchy names has been read through the displayed setoid; the classifier exported by the Real completion surface therefore depends on this sibling row rather than on host quotient equality or a selected representative.
