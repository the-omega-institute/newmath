\begin{theorem}[CauchyEquivalenceSetoid tail stability]
\label{thm:cauchy-equivalence-setoid-tail-stability}
Let $Q=(S_0,S_1,R_0,R_1,D,T,E,H,C,P,N)$ be an accepted $\CauchyEquivalenceSetoidUp$ carrier. Replacing both compared regular rational windows by later displayed tails preserves the local $\CauchyEquivalenceSetoidUp$ classifier whenever the later windows are read through the same $\StreamNameUp$ schedules, $\RegSeqRatUp$ readbacks, dyadic tolerance ledger, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows.

The preserved classifier is still the finite row $T$ over the displayed tails. It exports no quotient stream equality, selected representatives, host real equality, countable choice, hidden cofinal tail, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-equivalence-setoid-carrier}. The classifier can inspect only the two $\StreamNameUp$ finite windows, the two $\RegSeqRatUp$ readbacks, the $\DyadicRatCoreUp$ tolerance ledger, the setoid-test row, the Real seal boundary, and the structural rows.

Pass to later displayed tails by finite tail projection on $S_0$ and $S_1$. The corresponding readbacks remain rows of $R_0$ and $R_1$, and every admitted comparison still uses the same dyadic tolerance ledger $D$.

Transport the projected windows componentwise through $H$ and replay the read order through $C$. The rows $P$ and $N$ keep the tail comparison attached to the same package provenance and local NameCert surface.

Thus the setoid-test row $T$ is preserved over the later displayed tails. Since no carrier row names quotient stream equality, selected representatives, host real equality, countable choice, hidden cofinal tails, or ambient completeness, tail stability is exactly the displayed finite classifier route.
\end{proof}
