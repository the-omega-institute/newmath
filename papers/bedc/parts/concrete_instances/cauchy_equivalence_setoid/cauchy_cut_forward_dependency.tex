The equality-facing reads of $\CauchyEquivalenceSetoidUp$ are also consumed by the cut-to-stream comparison route of \autoref{ch:concrete-instances-cauchy-cut-namecert}. Before the setoid route is compared with a located lower--upper cut packet, the exposed cut rows must display $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerances, and the $\RealUp$ seal boundary, so the comparison remains a finite L10 dependency rather than quotient equality or a selected limit.
