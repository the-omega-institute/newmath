\begin{theorem}[RegSeqRat completion classifier route]
\label{thm:cauchy-equivalence-setoid-regseqrat-completion-classifier}
Every completion-facing $\CauchyEquivalenceSetoidUp$ equality read first factors through the $\RegSeqRatUp$ classifier rows of \autoref{ch:concrete-instances-regseqrat-namecert} and only then through the $\MetricCompletionUp$ tolerance and completion rows of \autoref{ch:concrete-instances-metriccompletion-namecert}. Thus a downstream completion consumer receives the stream-window comparison, regular rational readback, dyadic tolerance ledger, setoid test, and completion-facing handoff in that order; it receives no detached quotient equality, selected representative, host real equality, or independent completion oracle.
\end{theorem}
\begin{proof}
Start with an accepted $\CauchyEquivalenceSetoidUp$ carrier. Its equality-facing rows expose two stream windows, their regular rational readbacks, the dyadic tolerance ledger, the setoid-test row, and the structural transport, replay, provenance, and naming rows.

The first completion-facing classifier available to a consumer is the $\RegSeqRatUp$ readback route. Only after those regular rational rows and the dyadic tolerance ledger have been displayed can the $\MetricCompletionUp$ tolerance and completion rows read the comparison.

Since the carrier contains no quotient equality coordinate, selected representative, host real equality, or independent completion oracle, every completion-facing equality read follows the stated route.
\end{proof}
