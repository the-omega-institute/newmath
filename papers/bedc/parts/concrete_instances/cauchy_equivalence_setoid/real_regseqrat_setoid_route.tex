The $\CauchyEquivalenceSetoidUp$ packet is consumed by the regular-rational to real-name seal route: a $\RegSeqRatUp$ readback supplies the displayed Cauchy representative boundary of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal $\RealUp$ classifier of \autoref{ch:concrete-instances-real-namecert} can then read equality only through that setoid boundary. This route keeps the setoid as a finite dependency row for the Real seal rather than as host quotient equality, selected representatives, countable choice, or ambient real completeness.
