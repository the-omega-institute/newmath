\begin{theorem}[Cauchy equivalence setoid Real-separability backlink]
\label{thm:cauchy-equivalence-setoid-real-separability-backlink}
The $\RealSeparabilityUp$ dense surface consumes the $\CauchyEquivalenceSetoidUp$ classifier through the regular Cauchy and real-name routes. Concretely, any citation from \autoref{ch:concrete-instances-real-separability-namecert} back to the setoid packet must first display the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the Cauchy equivalence classifier row, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}; only then may the dense surface read the setoid route as a horizontal dependency.
\end{theorem}
\begin{proof}
Start with the accepted $\CauchyEquivalenceSetoidUp$ carrier. Its classifier row is not a free equality principle; it is read through StreamName windows, RegSeqRat readbacks, a DyadicRatCore tolerance ledger, the setoid test row, and the terminal Real seal.

The RealSeparability surface can therefore consume the setoid packet only after those same rows have been displayed. The regular Cauchy route supplies the readback discipline, the RealUp route supplies the name seal, and the dense surface records the resulting edge without selecting representatives or quotienting completed streams.
\end{proof}
