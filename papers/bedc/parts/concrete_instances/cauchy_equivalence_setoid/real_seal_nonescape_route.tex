\begin{theorem}[Cauchy equivalence setoid Real seal nonescape]
\label{thm:cauchy-equivalence-setoid-real-seal-nonescape}
Every $\RealUp$-facing consumer of the $\CauchyEquivalenceSetoidUp$ lattice must route through the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The setoid boundary may identify regular Cauchy names only after those sibling rows are displayed, and it exports no isolated quotient token that can bypass the L10 lattice.
\end{theorem}
\begin{proof}
Start from the existing Cauchy-equivalence setoid packet and read its classifier route through the sibling dependency lattice. The displayed equality surface is attached to finite stream observations, regular rational readback, and dyadic tolerance comparisons before any real-facing consumer can inspect a seal.

The $\StreamNameUp$ windows determine which finite observations are compared. The $\RegSeqRatUp$ row records the regular rational source names, and the $\DyadicRatCoreUp$ row supplies the tolerance ledger that makes the Cauchy comparison meaningful.

Only after those rows have been retained may the $\RealUp$ seal be read. Therefore the setoid cannot export a detached quotient credential, selected representative, host real equality, or ambient completeness row; every Real-facing read stays on the displayed StreamName, RegSeqRat, DyadicRatCore, and Real route.
\end{proof}
