\begin{theorem}[CauchyEquivalenceSetoid RegSeqRat--Real dependency anchor]
\label{thm:cauchy-equivalence-setoid-regseqrat-real-dependency-anchor}
Every accepted $\CauchyEquivalenceSetoidUp$ classifier read consumed by \autoref{ch:concrete-instances-regseqrat-namecert} or \autoref{ch:concrete-instances-real-namecert} routes through finite $\StreamNameUp$ windows and dyadic tolerance ledgers before any terminal Real-facing equality boundary is exposed. Thus the setoid surface supplies a displayed dependency anchor for regular rational readback and Real sealing, not a quotient-stream object, selected representative, host equality relation, or ambient completeness principle.
\end{theorem}
