\begin{theorem}[CauchyEquivalenceSetoid RegSeqRat Real classifier bridge]
\label{thm:cauchy-equivalence-setoid-regseqrat-real-classifier-bridge}
Every equality-facing comparison accepted by $\CauchyEquivalenceSetoidUp$ displays two $\StreamNameUp$ windows, two $\RegSeqRatUp$ readbacks, the $\DyadicRatCoreUp$ tolerance ledger, and the $\CauchyEquivalenceSetoidUp$ classifier before any $\RealUp$ seal or separated-completion consumer may read equality. Thus the bridge from regular rational readback to Real-facing and completion-facing equality is a finite classifier route, not quotient stream equality, selected representatives, host real equality, countable choice, or ambient Real completeness.
\end{theorem}
\begin{proof}
Start with the carrier and local surface of \autoref{def:cauchy-equivalence-setoid-carrier} and \autoref{thm:cauchy-equivalence-setoid-namecert-surface}. They expose exactly the two stream windows, two regular-rational readback rows, dyadic tolerance ledger, setoid-test row, Real seal boundary, transport, replay, provenance, and local naming.

Apply the RegSeqRat--Real route of \autoref{thm:cauchy-equivalence-setoid-regseqrat-real-forward}. It orders finite $\StreamNameUp$ observations before $\RegSeqRatUp$ readback and places the dyadic comparison ledger before the terminal $\RealUp$ seal.

Use the separated-completion route of \autoref{thm:cauchy-equivalence-setoid-separated-completion-forward-route}. A completion-facing reader receives equality only after the same displayed classifier rows have been consumed.

Since the carrier contains no quotient stream, selected representative, host equality, choice, or ambient-completeness coordinate, the composed bridge exports only the finite classifier route.
\end{proof}
