The $\CauchyEquivalenceSetoidUp$ family also supplies the forward classifier route consumed by $\RealSeparabilityUp$ dense-read and setoid-modulus schedules. The route in \autoref{thm:cauchy-equivalence-setoid-real-separability-sibling-route} keeps the two $\StreamNameUp$ windows, the two $\RegSeqRatUp$ readback rows, the $\DyadicRatCoreUp$ tolerance ledger, the Cauchy-equivalence classifier, and the terminal $\RealUp$ seal visible before the dense consumer reaches \autoref{ch:concrete-instances-real-separability-namecert}. Thus the separability lattice receives a finite classifier boundary rather than quotient stream equality, selected representatives, countable choice, host real equality, or ambient completeness.
