The $\RegularCauchyUniquenessUp$ seal route of \autoref{ch:concrete-instances-regular-cauchy-uniqueness-namecert} may consume the finite classifier supplied by $\CauchyEquivalenceSetoidUp$ before it reaches the terminal $\RealUp$ seal. This makes the setoid packet a horizontal dependency for regular Cauchy uniqueness rather than an isolated equality-facing classifier.

\begin{theorem}[RegularCauchyUniqueness CauchyEquivalenceSetoid route]
\label{thm:regular-cauchy-uniqueness-cauchy-equivalence-setoid-route}
Every $\RegularCauchyUniquenessUp$ seal read that consumes a $\CauchyEquivalenceSetoidUp$ packet first traverses the finite classifier route of \autoref{def:cauchy-equivalence-setoid-carrier}, the classifier laws of \autoref{thm:cauchy-equivalence-setoid-classifier-laws}, and the NameCert surface of \autoref{thm:cauchy-equivalence-setoid-namecert-surface}. Thus the uniqueness consumer reads $\StreamNameUp$ windows, $\RegSeqRatUp$ readback rows, the $\DyadicRatCoreUp$ tolerance ledger, the setoid-test row, the $\RealUp$ seal boundary, transport, replay, provenance, and local naming before it treats the regular Cauchy seal as independent of representative choice.
\end{theorem}
\begin{proof}
Project the local $\CauchyEquivalenceSetoidUp$ carrier. Its visible route is the pair of stream windows, pair of regular readback rows, dyadic tolerance ledger, setoid-test row, Real seal boundary, and structural rows.

Apply the classifier laws and NameCert surface. Reflexivity, symmetry, transitivity, tolerance exactness, seal nonescape, transport, replay, provenance, and local naming are all supplied by the same finite rows, so the uniqueness consumer receives classifier data rather than quotient stream equality.

Finally compare with \autoref{ch:concrete-instances-regular-cauchy-uniqueness-namecert}. The $\RegularCauchyUniquenessUp$ carrier already reads paired $\RegSeqRatUp$ names, $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance data, a comparison row, and a terminal $\RealUp$ seal. The setoid packet supplies that comparison row through the displayed finite classifier route, with no selected representative, host real equality, countable choice, or ambient completeness.
\end{proof}
