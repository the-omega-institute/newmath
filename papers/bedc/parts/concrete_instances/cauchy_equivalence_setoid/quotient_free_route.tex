\begin{theorem}[Cauchy equivalence setoid seed closure route]
\label{thm:cauchy-equivalence-setoid-seed-closure-route}
Every consumer of an accepted $\CauchyEquivalenceSetoidUp$ packet reads a pair of regular rational sequence windows, dyadic tolerance witnesses, componentwise $\hsame$ transport, displayed replay, $\Pkg$ provenance, and local $\NameCert$ support before it consumes the setoid boundary. The consumer route never reads a quotient class, a selected representative, or host equality of sequences.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:cauchy-equivalence-setoid-carrier}. Its visible equality-facing rows are the two $\StreamNameUp$ windows, the two $\RegSeqRatUp$ readbacks, the $\DyadicRatCoreUp$ tolerance ledger, the classifier row, the $\RealUp$ seal boundary, and the structural rows.

Use the RegSeqRat route, \autoref{thm:cauchy-equivalence-setoid-regseqrat-route}. It places regular rational readback before any completion-tail comparison can be exported, so the setoid boundary is consumed through displayed sequence windows rather than through a detached equality object.

Next use the Real separability route, \autoref{thm:cauchy-equivalence-setoid-real-separability-route}. It sends density-facing consumers through the same finite classifier surface before any $\RealSeparabilityUp$ read is public.

Finally apply transport, replay, provenance, and local naming from the carrier surface. Since no carrier coordinate is a quotient class, selected representative, or host sequence equality, every consumer of the seed route remains quotient-free.
\end{proof}
