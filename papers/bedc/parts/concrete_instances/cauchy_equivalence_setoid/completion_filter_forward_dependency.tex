The $\CauchyEquivalenceSetoidUp$ equality surface is consumed by \autoref{ch:concrete-instances-cauchyfiltercompletion-namecert} and \autoref{ch:concrete-instances-metriccompletion-namecert} only through the completion-filter route that first exposes $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, the $\DyadicRatCoreUp$ tolerance ledger, the terminal $\RealUp$ seal, and then the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. The route supplies equality inspection for completion consumers without exporting quotient representatives, selected limits, ambient metric completeness, countable choice, or host real equality.
