\begin{theorem}[CauchyEquivalenceSetoid forward dependency]
\label{thm:cauchy-equivalence-setoid-forward-dependency}
Every $\RealUp$ or completion-facing equality consumer of an accepted $\CauchyEquivalenceSetoidUp$ classifier must expose the $\StreamNameUp$ finite windows, the $\RegSeqRatUp$ readback rows, the $\DyadicRatCoreUp$ tolerance rows, and the terminal $\RealUp$ seal before it consumes the setoid classifier. The route is the sibling-facing dependency surface of \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert}; it exports no quotient stream equality, selected representative, countable-choice schedule, host real equality, or ambient completion principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-equivalence-setoid-carrier}. Its equality-facing rows begin with two StreamName windows, two RegSeqRat readbacks, and the DyadicRatCore tolerance ledger before the setoid-test row is available.

Apply the classifier-law and NameCert-surface theorems, \autoref{thm:cauchy-equivalence-setoid-classifier-laws} and \autoref{thm:cauchy-equivalence-setoid-namecert-surface}. They keep reflexivity, symmetry, transitivity, tolerance exactness, transport, replay, provenance, and local naming inside the displayed packet.

Use the Real seal forward route of \autoref{thm:cauchy-equivalence-setoid-real-seal-forward-route}. It places the terminal $\RealUp$ seal after the visible stream, readback, tolerance, classifier, transport, replay, provenance, and naming rows.

Thus any Real or completion-facing equality consumer reads the classifier only after those sibling rows have been exposed. Since the carrier has no quotient, selected-representative, choice, host-equality, or ambient-completion coordinate, none of those objects can replace the displayed dependency route.
\end{proof}
