The backward dependency from $\RealSeparabilityUp$ to $\CauchyEquivalenceSetoidUp$ is the classifier-facing half of the finite density route. Before a separability packet exports a countable dense display as in \autoref{ch:concrete-instances-real-separability-namecert}, it reads the Cauchy-equivalence classifier to keep equivalent regular names on the same displayed density window. The dependency is therefore a finite comparison and density handoff, not quotient stream equality, selected representatives, countable choice, host real equality, or ambient completeness.

\begin{proposition}[RealSeparability backward dependency]
\label{prop:cauchy-equivalence-setoid-real-separability-backward-dependency}
Every $\RealSeparabilityUp$ consumer that exports a countable dense display through \autoref{ch:concrete-instances-real-separability-namecert} reads the accepted $\CauchyEquivalenceSetoidUp$ classifier before the dense display is exposed. Consequently the exported density row is stable under the displayed Cauchy-equivalence comparison, and the route exports no quotient stream, selected representative, countable-choice selector, host real equality, or ambient completeness row.
\end{proposition}
