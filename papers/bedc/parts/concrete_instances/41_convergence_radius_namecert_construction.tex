\chapter{A Concrete Naming Certificate for \(\ConvergenceRadiusUp\)}
\label{ch:concrete-instances-convergence-radius-namecert}

This chapter gives a concrete naming-certificate construction for the convergence radius of a complex power series \(\sum a_n z^n\) at the paper-proof level. The certificate sits above \(\ComplexLimitUp\) (\autoref{ch:concrete-instances-complex-limit-namecert}) and the formal power series chapter (\autoref{ch:concrete-instances-fps-namecert}), and gives an explicit unary-history witness for the radius rather than a real-number supremum. Absolute convergence inside the disk is proved by geometric majorant; the Cauchy-Hadamard characterization is given in constructive form using only explicit unary suprema. The construction is fully constructive and uses no \(\mathsf{Classical.choice}\), no \(\mathsf{Quot.sound}\), and no \(\mathsf{propext}\), so the certificate's Lean targets fall under the project's axiom-purity gate.

\section{Power series carrier and partial sums}

\begin{definition}[Power series carrier]
\label{def:power-series-carrier}
A complex power series carrier is a unary-indexed family \(a : \Unary \to \ComplexUp\)-history of complex coefficient histories together with a complex history \(z_0\) marking the origin of the expansion.
\end{definition}
\leandef{BEDC.Derived.ConvergenceRadiusUp.PowerSeriesCarrier}

\begin{lemma}[Power series origin and coefficient transport]
\label{lem:power-series-carrier-origin-coefficient-transport}
If the origin history is transported along $\hsame$ and every displayed
coefficient is classified by the complex-history classifier, then the target
origin is unary and the transported coefficient family is again a power series
carrier at that target origin.
\end{lemma}
\begin{proof}
The origin carrier is transported by unary transport. For each coefficient,
the target-carrier component of the complex-history classifier supplies the
complex coefficient carrier, so the transported family satisfies the power
series carrier predicate.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.PowerSeriesCarrier\_origin\_coefficient\_transport}

\begin{lemma}[Power series carrier append-unary closure]
\label{thm:power-series-carrier-append-unary-closed}
If $(a,z_0)$ is a power series carrier and $q$ is unary, then appending $q$ to
the origin and to every coefficient gives another power series carrier:
$$
\mathsf{PowerSeriesCarrier}(\lambda n.\mathsf{append}(a_n,q),\mathsf{append}(z_0,q)).
$$
\end{lemma}
\begin{proof}
Unpack the power-series carrier into the unary origin, coefficient-wise
complex-history carrier, and complex-history origin carrier. Unary histories
are closed under append, and complex-history carriers are closed under append
of a unary suffix; apply the latter pointwise to every coefficient and once to
the origin.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.PowerSeriesCarrier\_append\_unary\_closed}

\begin{lemma}[Power series carrier prepend-unary closure]
\label{thm:power-series-carrier-prepend-unary-closed}
If $(a,z_0)$ is a power series carrier and $q$ is unary, then prepending $q$ to
the origin and to every coefficient gives another power series carrier:
$$
\mathsf{PowerSeriesCarrier}(\lambda n.\mathsf{append}(q,a_n),\mathsf{append}(q,z_0)).
$$
\end{lemma}
\begin{proof}
Unpack the power-series carrier into the unary origin, coefficient-wise
complex-history carrier, and complex-history origin carrier. Unary histories
are closed under append, and complex-history carriers are closed under prepend
by a unary history; apply the latter pointwise to every coefficient and once
to the origin.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.PowerSeriesCarrier\_prepend\_unary\_closed}

\begin{definition}[Partial sum at a point]
\label{def:part-sum-power-series}
For a power series \((a, z_0)\) and a complex history \(z\), the partial sum predicate \(\mathsf{PartSum}(a, z_0, z, n, S_n)\) is the unary-indexed family of complex histories obtained by summing the first \(n\) terms \(a_k (z - z_0)^k\) via explicit unary recursion, using the complex multiplication and addition of \autoref{ch:concrete-instances-complex-namecert}.
\end{definition}
\leandef{BEDC.Derived.ConvergenceRadiusUp.PowerSeriesPartSum}

\begin{lemma}[Partial sums are complex histories]
\label{lem:part-sum-complex}
For every power series \((a, z_0)\), every complex history \(z\), and every unary \(n\), there is a unique complex history \(S_n\) with \(\mathsf{PartSum}(a, z_0, z, n, S_n)\), up to \(\hsame\) on both real components.
\end{lemma}
\begin{proof}
By unary induction on \(n\), using totality and functionality of complex multiplication and addition.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.PowerSeriesCarrier\_constant\_coeff\_partSum\_exists\_unique}

\section{Geometric bound predicate}

\begin{definition}[Geometric bound]
\label{def:geom-bound}
For coefficients \(a\), unary radius history \(r\), and unary constant \(K\), the geometric bound \(\mathsf{GeomBound}(a, r, K)\) holds when for every unary \(n\), the modulus comparison \(|a_n| \cdot r^n \leq K\) is decidably true on the rational representation.
\end{definition}
\leandef{BEDC.Derived.ConvergenceRadiusUp.GeomBound}

\begin{lemma}[Geometric bound visible radius endpoint package]
\label{lem:geom-bound-visible-radius-endpoint-package}
For coefficients $a$, constant history $K$, result history $R$, and tail
history $tail$, if
$$
\mathsf{GeomBound}(a,\Eone(tail),K)
\qquad\text{and}\qquad
\ContR(\Eone(tail),K,R),
$$
then $tail$ is unary and the continuation result is not internally empty:
$$
\mathsf{UnaryHistory}(tail)\land \neg\HSame(R,\emp).
$$
\end{lemma}
\begin{proof}
The geometric-bound witness contains the unary carrier for the visible radius
$\Eone(tail)$, so unary inversion gives the payload carrier for $tail$. If the
continuation result were empty, empty-result inversion would identify the
visible radius endpoint $\Eone(tail)$ with $\emp$, contradicting constructor
separation.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.GeomBound\_visible\_radius\_endpoint\_package}

\begin{theorem}[Geometric bound visible constant endpoint package]
\label{thm:geom-bound-visible-constant-endpoint-package}
For coefficients $a$, radius history $r$, result history $R$, and tail
history $tail$, if
$$
\mathsf{GeomBound}(a,r,\Eone(tail))
\qquad\text{and}\qquad
\ContR(r,\Eone(tail),R),
$$
then $tail$ is unary and the continuation result is not internally empty:
$$
\mathsf{UnaryHistory}(tail)\land \neg\HSame(R,\emp).
$$
\end{theorem}
\begin{proof}
The geometric-bound witness contains the unary carrier for the visible constant
endpoint $\Eone(tail)$, so unary inversion gives the payload carrier for
$tail$. If the continuation result were empty, empty-result inversion would
identify the visible constant endpoint $\Eone(tail)$ with $\emp$,
contradicting constructor separation.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.GeomBound\_visible\_constant\_endpoint\_package}

\begin{theorem}[Geometric bound radius semantic naming certificate]
\label{thm:geom-bound-radius-semantic-namecert}
For fixed coefficients \(a\) and unary bound constant \(K\), any witness of \(\mathsf{GeomBound}(a,r,K)\) yields a semantic naming certificate whose carrier, pattern, and ledger are the same radius-indexed geometric-bound predicate and whose classifier is \(\hsame\) on the displayed radius.
\end{theorem}
\begin{proof}
The witnessed radius \(r\) inhabits the carrier. Reflexivity, symmetry, and transitivity are the finite-kernel \(\hsame\) laws. Carrier transport moves only the unary radius component across the displayed \(\hsame\) comparison and leaves the fixed coefficient and bound data unchanged.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.GeomBound\_radius\_semanticNameCert}

\begin{theorem}[Geometric bound semantic naming certificate]
\label{thm:geom-bound-semantic-namecert}
If \(\mathsf{GeomBound}(a,r,K)\) holds, then the radius-indexed carrier
\(x\mapsto\mathsf{GeomBound}(a,x,K)\), with classifier \(\hsame\), forms a
semantic naming certificate whose source, pattern, and ledger predicates are
all this same concrete geometric-bound carrier.
\end{theorem}
\begin{proof}
The displayed bound supplies the inhabited carrier witness. Reflexivity,
symmetry, and transitivity are the kernel laws for \(\hsame\). Carrier
transport along \(\hsame\) transports only the unary radius field; the constant
\(K\) and the coefficient carrier rows are preserved unchanged. Pattern and
ledger soundness are direct because all three semantic predicates are the same
geometric-bound carrier.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.GeomBound\_semanticNameCert}

\begin{lemma}[Geometric bound supplies the power series carrier]
\label{lem:geom-bound-power-series-carrier}
If \(\mathsf{GeomBound}(a,r,K)\) holds and the expansion center \(z_0\) is a
complex history, then \((a,z_0)\) is a power series carrier, and both \(r\)
and \(K\) are unary histories.
\end{lemma}
\begin{proof}
Unpack the geometric-bound predicate. Its fields give the unary radius, the
unary constant, and the coefficient-wise complex carrier; combine the latter
with the displayed carrier of \(z_0\).
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.GeomBound\_powerSeriesCarrier}

\begin{lemma}[Geometric bound radius and constant continuation closure]
\label{lem:geom-bound-radius-constant-continuation-closed}
For coefficients $a$, radius histories $r,R$, constant histories $K,K'$, and
unary perturbations $dr,dK$, if
$$
\mathsf{GeomBound}(a,r,K),\qquad
\mathsf{UnaryHistory}(dr),\qquad
\mathsf{UnaryHistory}(dK),
$$
and the continuation ledgers $\ContR(r,dr,R)$ and $\ContR(K,dK,K')$ hold,
then the geometric bound persists at the transported radius and constant:
$$
\mathsf{GeomBound}(a,R,K').
$$
\end{lemma}
\begin{proof}
The geometric-bound witness contains unary carrier data for both $r$ and $K$.
Unary continuation closure transports those carriers along the two displayed
continuation ledgers, while the coefficient carrier clause is unchanged.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.GeomBound\_radius\_constant\_continuation\_closed}

\begin{theorem}[Geometric bound radius and constant hsame transport]
\label{thm:geom-bound-radius-constant-hsame-transport-unary}
For coefficients $a$, radius histories $r,r'$, and constant histories $K,K'$,
if
$$
r\hsame r',
\qquad
K\hsame K',
\qquad
\mathsf{GeomBound}(a,r,K),
$$
then $r'$ and $K'$ are unary histories and
$$
\mathsf{GeomBound}(a,r',K').
$$
\end{theorem}
\begin{proof}
Unpack the geometric-bound witness into its unary radius field, unary constant
field, and coefficient carrier family. Transport the two unary fields along
the displayed $\hsame$ classifiers, and reuse the unchanged coefficient
carrier family.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.GeomBound\_radius\_constant\_hsame\_transport\_unary}

\begin{theorem}[Geometric bound continuation visible radius endpoint package]
\label{thm:geom-bound-continuation-visible-radius-endpoint-package}
For coefficients $a$, radius histories $r,R$, constant histories $K,K'$,
unary perturbations $dr,dK$, and visible tail $tail$, if
$$
\mathsf{GeomBound}(a,r,K),\qquad
\mathsf{UnaryHistory}(dr),\qquad
\mathsf{UnaryHistory}(dK),
$$
and
$$
\ContR(r,dr,\Eone(tail)),\qquad
\ContR(K,dK,K'),\qquad
\ContR(\Eone(tail),K',R),
$$
then the visible tail is unary and the final endpoint is not internally empty:
$$
\mathsf{UnaryHistory}(tail)\land \neg\HSame(R,\emp).
$$
\end{theorem}
\begin{proof}
First apply radius and constant continuation closure to transport the geometric
bound from $(r,K)$ to $(\Eone(tail),K')$. The visible-radius endpoint package
then gives unary readback for $tail$ and excludes an empty final endpoint.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.GeomBound\_continuation\_visible\_radius\_endpoint\_package}

\begin{lemma}[Geometric majorant inside the disk]
\label{lem:geom-bound-majorant}
If \(\mathsf{GeomBound}(a, r, K)\) holds and \(z\) satisfies \(|z - z_0| \leq r' < r\) (decidable strict inequality on rational representations), then the partial sums of \(a\) at \(z\) form a regular complex Cauchy sequence with explicit modulus \(N(k) = \lceil k / \log_2(r/r') \rceil\) computed as a unary function of the precision.
\end{lemma}
\begin{proof}[Sketch]
Each term is bounded above by \(K (r'/r)^n\); apply the geometric series formula with the explicit ratio.
\end{proof}

\section{Convergence radius predicate}

\begin{definition}[Convergence radius]
\label{def:conv-rad}
For coefficients \(a\), the convergence radius predicate \(\mathsf{ConvRad}(a, R)\) holds when there is a constant-witness function \(K : \Unary \to \Unary\) such that for every unary radius \(r\) with \(\mathsf{NatUnaryStrictPrefix}(r, R)\), \(\mathsf{GeomBound}(a, r, K(r))\) is satisfied.
\end{definition}
\leandef{BEDC.Derived.ConvergenceRadiusUp.ConvRad}

\begin{lemma}[Convergence radius supplies power series carriers along witnesses]
\label{lem:conv-rad-power-series-carrier-witness}
If \(\mathsf{ConvRad}(a,R)\) holds and \(z_0\) is a complex history, then the
constant-witness function \(K\) can be chosen so that every admitted radius row
simultaneously gives \(\mathsf{PowerSeriesCarrier}(a,z_0)\) and
\(\mathsf{GeomBound}(a,r,K(r))\).
\end{lemma}
\begin{proof}
Open the witness function in the convergence-radius predicate. For each
admitted radius row, apply
\autoref{lem:geom-bound-power-series-carrier} to the resulting geometric
bound and the carrier of \(z_0\).
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRad\_powerSeriesCarrier\_witness}

\begin{lemma}[Convergence radius append-unary power-series witness]
\label{thm:convrad-power-series-carrier-append-unary-witness}
If $\mathsf{ConvRad}(a,R)$ holds, $z_0$ is a complex history, and $q$ is unary,
then the convergence-radius witness function $K$ can be chosen so that every
admitted radius row simultaneously gives
$$
\mathsf{PowerSeriesCarrier}(\lambda n.\mathsf{append}(a_n,q),\mathsf{append}(z_0,q))
$$
and
$$
\mathsf{GeomBound}(\lambda n.\mathsf{append}(a_n,q),r,K(r)).
$$
\end{lemma}
\begin{proof}
Choose the witness function supplied by
\autoref{lem:conv-rad-power-series-carrier-witness}. For each admitted radius
row, apply append-unary closure to the supplied power-series carrier and
apply geometric-bound append-unary coefficient closure to the supplied
geometric bound. The same $K$ remains the witness function.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRad\_powerSeriesCarrier\_append\_unary\_witness}

\begin{lemma}[Convergence radius prepend-unary power-series witness]
\label{thm:conv-rad-power-series-carrier-prepend-unary-witness}
If $\mathsf{ConvRad}(a,R)$ holds, $z_0$ is a complex history, and $q$ is unary,
then the convergence-radius witness function $K$ can be chosen so that every
admitted radius row simultaneously gives
$$
\mathsf{PowerSeriesCarrier}(\lambda n.\mathsf{append}(q,a_n),\mathsf{append}(q,z_0))
$$
and
$$
\mathsf{GeomBound}(\lambda n.\mathsf{append}(q,a_n),r,K(r)).
$$
\end{lemma}
\begin{proof}
Choose the witness function supplied by
\autoref{lem:conv-rad-power-series-carrier-witness}. For each admitted radius
row, apply prepend-unary closure to the supplied power-series carrier and
apply geometric-bound prepend-unary coefficient closure to the supplied
geometric bound. The same $K$ remains the witness function.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRad\_powerSeriesCarrier\_prepend\_unary\_witness}

\begin{theorem}[Convergence radius visible radius witness endpoint package]
\label{thm:convrad-visible-radius-witness-endpoint-package}
If $\mathsf{ConvRad}(a,R)$ holds and $\Eone(tail)$ is an admitted unary radius
endpoint, then the convergence-radius witness function can be chosen so that
any continuation
$$
\ContR(\Eone(tail),K(\Eone(tail)),R)
$$
exposes the payload carrier and non-empty result boundary:
$$
\mathsf{UnaryHistory}(tail)\land \neg\hsame(R,\emp).
$$
\end{theorem}
\begin{proof}
Open the witness function in $\mathsf{ConvRad}(a,R)$ and specialize it to the
visible radius endpoint $\Eone(tail)$. The resulting geometric-bound row,
together with the displayed continuation, is exactly the visible-radius endpoint
package for geometric bounds.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRad\_visible\_radius\_witness\_endpoint\_package}

\begin{theorem}[Convergence radius classifier-visible radius witness endpoint package]
\label{thm:conv-rad-classifier-visible-radius-witness-endpoint-package}
If $\hsame(R,R')$, $R'$ is unary, the coefficient streams $a$ and $b$ are
complex-history classified pointwise, $\mathsf{ConvRad}(a,R)$ holds, and
$\Eone(tail)$ is an admitted unary radius endpoint, then the transported
convergence-radius witness function can be chosen so that every continuation
$$
\ContR(\Eone(tail),K(\Eone(tail)),R')
$$
exposes
$$
\mathsf{UnaryHistory}(tail)\land \neg\hsame(R',\emp).
$$
\end{theorem}
\begin{proof}
Transport the convergence radius along $\hsame(R,R')$ and the pointwise
complex-history classifier for the coefficients. The visible-radius endpoint
package then applies at the transported radius $R'$.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRad\_classifier\_visible\_radius\_witness\_endpoint\_package}

\begin{lemma}[Convergence radius transport]
\label{lem:conv-rad-radius-transport}
If \(\hsame(R, R')\), \(\mathsf{ConvRad}(a, R)\), and \(R'\) is unary, then the same geometric-bound witness function supplies \(\mathsf{ConvRad}(a, R')\).
\end{lemma}
\begin{proof}
Transport the radius endpoint along \(\hsame\). The unary carrier for the target radius is explicit, and the existing constant-witness function continues to provide the same geometric-bound data.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRad\_radius\_transport}

\begin{lemma}[Convergence radius coefficient classifier transport]
\label{lem:conv-rad-radius-coefficient-classifier-transport}
If \(\hsame(R,R')\), \(R'\) is unary, every coefficient \(a_n\) is classified
with \(b_n\) by the complex-history classifier, and \(\mathsf{ConvRad}(a,R)\)
holds, then \(R'\) is unary and \(\mathsf{ConvRad}(b,R')\) holds.
\end{lemma}
\begin{proof}
First transport the radius endpoint from \(R\) to \(R'\), retaining the same
constant-witness function for geometric bounds. For each supplied geometric
bound, keep the radius and constant unary carriers, and use the target-carrier
component of the complex-history classifier at every coefficient index to
rebuild the coefficient side of the bound for \(b\).
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRad\_radius\_coefficient\_classifier\_transport}

\begin{theorem}[Convergence radius is unique]
\label{thm:conv-rad-unique}
If \(\mathsf{ConvRad}(a, R)\) and \(\mathsf{ConvRad}(a, R')\) both hold, then \(\hsame(R, R')\) on the rational representation.
\end{theorem}
\begin{proof}[Sketch]
Suppose \(R \neq R'\). Pick a rational \(r\) strictly between them. \(\mathsf{GeomBound}\) at \(r\) follows from one side and fails from the other, contradicting consistency of the witness.
\end{proof}

\section{Cauchy-Hadamard characterization}

\begin{theorem}[Cauchy-Hadamard]
\label{thm:cauchy-hadamard}
The convergence radius is the explicit witness given as the supremum of \(r\) for which \(\mathsf{GeomBound}(a, r, K)\) admits a finite \(K\). Equivalently, \(R = 1 / \mathsf{LimSup}_n |a_n|^{1/n}\), where the limsup is computed by an explicit unary diagonal extraction.
\end{theorem}
\begin{proof}[Sketch]
The supremum side is by definition. The limsup side is matched via the elementary inequality \(|a_n|^{1/n} \to 1/R\), shown constructively by unary diagonal majorant.
\end{proof}

\begin{theorem}[Absolute convergence inside the disk]
\label{thm:abs-conv-inside}
If \(\mathsf{ConvRad}(a, R)\) and \(z\) satisfies \(|z - z_0| < R\) (strict inequality with explicit witness), then the power series \(\sum a_n (z - z_0)^n\) converges absolutely with explicit modulus.
\end{theorem}
\begin{proof}[Sketch]
By \autoref{lem:geom-bound-majorant}: pick a rational \(r\) with \(|z - z_0| < r < R\); the series is bounded by a geometric majorant.
\end{proof}

\section{Closure laws}

\begin{lemma}[Geometric bound classifier append-unary closure]
\label{thm:geom-bound-coeff-classifier-append-unary-closed}
If coefficient families $a$ and $b$ are pointwise classified as complex
histories, $\mathsf{GeomBound}(a,r,K)$ holds, and $q$ is unary, then appending
$q$ to every displayed $b_n$ preserves the same geometric-bound radius and
constant:
$$
\mathsf{GeomBound}(\lambda n.\mathsf{append}(b_n,q),r,K).
$$
\end{lemma}
\begin{proof}
Each classifier row exposes the target coefficient $b_n$ as a complex-history
carrier. Complex-history carriers are closed under append of a unary suffix, so
each appended coefficient remains in the carrier. The radius and constant
unary witnesses are the ones already carried by $\mathsf{GeomBound}(a,r,K)$.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.GeomBound\_coeff\_classifier\_append\_unary\_closed}

\begin{lemma}[Geometric bound append-unary coefficient closure]
\label{thm:geom-bound-append-unary-coeff-closed}
If $\mathsf{GeomBound}(a,r,K)$ holds and $q$ is unary, then appending $q$ to
every coefficient preserves the same radius and constant:
$$
\mathsf{GeomBound}(\lambda n.\mathsf{append}(a_n,q),r,K).
$$
\end{lemma}
\begin{proof}
Unpack $\mathsf{GeomBound}(a,r,K)$ into its unary radius witness, unary constant
witness, and coefficient carrier family. Apply complex-history carrier
append-unary closure pointwise to each coefficient and keep the same radius and
constant witnesses.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.GeomBound\_append\_unary\_coeff\_closed}

\begin{lemma}[Geometric bound prepend-unary coefficient closure]
\label{thm:geom-bound-prepend-unary-coeff-closed}
If $q$ is unary and $\mathsf{GeomBound}(a,r,K)$ holds, then prepending $q$ to
every coefficient preserves the same radius and constant:
$$
\mathsf{GeomBound}(\lambda n.\mathsf{append}(q,a_n),r,K).
$$
\end{lemma}
\begin{proof}
Unpack $\mathsf{GeomBound}(a,r,K)$ into its unary radius witness, unary constant
witness, and coefficient carrier family. Apply complex-history carrier
prepend-unary closure pointwise to each coefficient and keep the same radius
and constant witnesses.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.GeomBound\_prepend\_unary\_coeff\_closed}

\begin{theorem}[Convergence radius append-unary coefficient closure]
\label{thm:conv-rad-append-unary-coeff-closed}
If $\mathsf{ConvRad}(a,R)$ holds and $q$ is unary, then appending $q$ to every
coefficient preserves the same convergence radius:
$$
\mathsf{ConvRad}(\lambda n.\mathsf{append}(a_n,q),R).
$$
\end{theorem}
\begin{proof}
Unfold $\mathsf{ConvRad}(a,R)$ and keep its constant-witness function $K$ as the
candidate witness for the appended coefficient family.

Take an arbitrary admitted radius row, together with its continuation ledger
$\ContR(r,K(r),R)$. The original convergence-radius witness gives
$\mathsf{GeomBound}(a,r,K(r))$ for that row. Apply
\autoref{thm:geom-bound-append-unary-coeff-closed} to this geometric bound and
the unary suffix $q$, yielding
$\mathsf{GeomBound}(\lambda n.\mathsf{append}(a_n,q),r,K(r))$. Since the row was
arbitrary and the witness function is unchanged, the displayed predicate is
$\mathsf{ConvRad}(\lambda n.\mathsf{append}(a_n,q),R)$.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRad\_append\_unary\_coeff\_closed}

\begin{theorem}[Convergence radius prepend-unary coefficient closure]
\label{thm:conv-rad-prepend-unary-coeff-closed}
If $\mathsf{ConvRad}(a,R)$ holds and $q$ is unary, then prepending $q$ to every
coefficient preserves the same convergence radius:
$$
\mathsf{ConvRad}(\lambda n.\mathsf{append}(q,a_n),R).
$$
\end{theorem}
\begin{proof}
Unfold $\mathsf{ConvRad}(a,R)$ and keep its constant-witness function $K$ as the
candidate witness for the prepended coefficient family. For any admitted row
$r$ with continuation ledger $\ContR(r,K(r),R)$, the original witness gives
$\mathsf{GeomBound}(a,r,K(r))$. Apply
\autoref{thm:geom-bound-prepend-unary-coeff-closed} with the unary prefix $q$,
and leave the radius witness, the continuation row, and the constant witness
unchanged.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRad\_prepend\_unary\_coeff\_closed}

\begin{lemma}[Differentiation preserves radius]
\label{lem:diff-preserves-radius}
If \(\mathsf{ConvRad}(a, R)\), then the term-wise derivative coefficients \(b_n = (n+1) \cdot a_{n+1}\) satisfy \(\mathsf{ConvRad}(b, R)\) with the same radius and an explicit constant adjustment.
\end{lemma}
\begin{proof}[Sketch]
For \(r' < r < R\), \(|b_n| r'^n \leq (n+1) |a_{n+1}| r'^n\); pull out a factor of \((r'/r)^n\) using \(\mathsf{GeomBound}(a, r, K)\) and bound \(n (r'/r)^n\) by an explicit polynomial-times-geometric majorant.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRad\_successor\_coefficients\_closed}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRad\_coefficient\_tail\_closed}

\begin{lemma}[Cauchy product radius]
\label{lem:cauchy-product-radius}
If \(\mathsf{ConvRad}(a, R_a)\) and \(\mathsf{ConvRad}(b, R_b)\), then the Cauchy product \(c_n = \sum_{k=0}^n a_k b_{n-k}\) satisfies \(\mathsf{ConvRad}(c, R)\) with \(R \geq \min(R_a, R_b)\) and explicit \(K(r)\) computed from \(K_a, K_b\).
\end{lemma}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.GeomBound\_cauchy\_product\_row\_closed}
\begin{proof}[Sketch]
Triangular pasting of geometric bounds; constant inflation by a factor of \((n+1)\) absorbed into the geometric majorant.
\end{proof}

\section{The certificate}

\begin{definition}[Convergence radius source specification]
\label{def:conv-rad-source-spec}
The source specification is the predicate \(\mathsf{ConvRad}(a, R)\) on power series carriers paired with a unary radius candidate.
\end{definition}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRadSourceSpec\_powerSeries\_append\_prepend\_closed}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRadSourceSpec\_powerSeries\_geomBound\_readback}

\begin{definition}[Convergence radius pattern specification]
\label{def:conv-rad-pattern-spec}
The pattern is the supremum-of-geometric-bound predicate, computed through \(\mathsf{GeomBound}\).
\end{definition}
\leandef{BEDC.Derived.ConvergenceRadiusUp.ConvRadPatternSpec}

\begin{definition}[Convergence radius classifier specification]
\label{def:conv-rad-classifier-spec}
Two convergence radii are classified together when they agree under \(\hsame\) on the rational representation of the radius.
\end{definition}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRadRadiusClassifierSpec\_powerSeries\_witness}
\leandef{BEDC.Derived.ConvergenceRadiusUp.ConvRadClassifierSpec}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRadClassifierSpec\_radius\_transport}

\begin{definition}[Convergence radius stability certificate]
\label{def:conv-rad-stability-cert}
The stability certificate consists of \(\hsame\)-respect of \(\mathsf{ConvRad}\) on coefficient input, transport of constant-witness functions, and compatibility with the geometric-bound predicate under \(\hsame\) on radii.
\end{definition}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRad\_stability\_certificate\_fields}

\begin{definition}[Convergence radius ledger policy]
\label{def:conv-rad-ledger-policy}
The ledger policy records the analytic obligations for term-by-term
differentiation, Cauchy product, absolute convergence inside the open disk, and
the Cauchy-Hadamard limsup characterization.
\end{definition}
\leandef{BEDC.Derived.ConvergenceRadiusUp.ConvRadLedgerPolicy}

\begin{theorem}[Concrete $\ConvergenceRadiusUp$ certificate-obligation package]
\label{thm:conv-rad-namecert}
The convergence-radius construction supplies a concrete certificate-obligation
package over $\Hist$ for $\ConvergenceRadiusUp$. Its checked rows are the
$\mathsf{ConvRad}$ carrier, $\hsame$ classifier, radius transport, coefficient
classifier transport, extraction of power-series carrier data from geometric
bounds, the convergence-radius power-series carrier witness, and append-unary
coefficient closure. The analytic ledger laws of
\autoref{def:conv-rad-ledger-policy} remain policy obligations for this
interface.
\end{theorem}
\begin{proof}[Sketch]
The carrier and classifier are the displayed $\mathsf{ConvRad}$ predicate and
$\hsame$ on radius histories.

The checked transport rows are
\autoref{lem:conv-rad-radius-transport} and
\autoref{lem:conv-rad-radius-coefficient-classifier-transport}. The carrier
witness rows are \autoref{lem:geom-bound-power-series-carrier} and
\autoref{lem:conv-rad-power-series-carrier-witness}. The checked closure row is
\autoref{thm:conv-rad-append-unary-coeff-closed}.

These rows give the concrete proof obligations available to the semantic
certificate target. The differentiation, Cauchy-product, absolute-convergence,
and Cauchy-Hadamard rows are ledger-policy obligations in this chapter, not
premises of the checked semantic NameCert target.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.conv\_rad\_name\_certificate}

\begin{theorem}[Semantic $\ConvergenceRadiusUp$ naming certificate]
\label{thm:conv-rad-semantic-namecert}
The concrete package of \autoref{thm:conv-rad-namecert} supplies the checked
semantic naming-certificate schema of $\NameCert$
(\autoref{ch:core-typed-naming-certificates}) exactly for the
$\mathsf{ConvRad}$ carrier, the $\hsame$ radius classifier, radius transport,
coefficient classifier transport, the power-series carrier witness, and
append-unary coefficient closure.
\end{theorem}
\begin{proof}[Sketch]
The witnessed radius inhabits the $\mathsf{ConvRad}$ carrier. The equivalence
fields are the finite-kernel $\hsame$ laws.

Carrier transport is the radius-transport row, with unary transport of the
displayed radius. Coefficient-family transport uses the checked classifier
transport row, while the carrier witness and append-unary closure are supplied
by their corresponding checked rows in this chapter.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRad\_semanticNameCert}

\begin{theorem}[Geometric bound is downward closed in the radius]
\label{thm:geom-bound-radius-shrink-closed}
For coefficients $a$, radius histories $r'$ and $r$, and constant history $K$, if
$$
\mathsf{NatUnaryStrictPrefix}(r',r)
\qquad\text{and}\qquad
\mathsf{GeomBound}(a,r,K),
$$
then
$$
\mathsf{GeomBound}(a,r',K).
$$
\end{theorem}
\begin{proof}
By \autoref{def:nat-unary-strict-prefix}, choose a unary tail $t$ such that $t$ is not $\emp$ and $\ContR(r',t,r)$ holds. By \autoref{def:geom-bound}, the bound at $r$ supplies the unary carrier for $r$, the unary carrier for $K$, the coefficient carrier rows, and the decidable comparison rows $|a_n|\cdot r^n\leq K$ for every unary index $n$. The strict-prefix witness is in particular a prefix witness from $r'$ to $r$; applying \autoref{thm:preorder-prefix-source-carrier-of-target-carrier} to that witness and to the unary carrier of $r$ gives the unary carrier of $r'$.

It remains to transport the comparison rows. In the rational representation of unary histories, the continuation ledger $\ContR(r',t,r)$ reads $r=r'+t$ with $t\geq 0$, hence $0\leq r'\leq r$. Fix a unary index $n$. Unary induction on $n$ gives $(r')^n\leq r^n$: the empty exponent gives equality of units, and the one-extension step multiplies the induction comparison by the nonnegative factor $r'$ and then uses $r'\leq r$ with the nonnegative factor $r^n$. Since complex moduli are nonnegative, rational-history multiplication gives
$$
|a_n|\cdot (r')^n\leq |a_n|\cdot r^n.
$$
Composing this inequality with the comparison row supplied by $\mathsf{GeomBound}(a,r,K)$ yields $|a_n|\cdot (r')^n\leq K$. The constant carrier and coefficient carrier rows are unchanged, so \autoref{def:geom-bound} repacks these data as $\mathsf{GeomBound}(a,r',K)$.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.GeomBound\_radius\_shrink\_closed}

\begin{definition}[Convergence-radius compatible coefficient-ring inclusion]
\label{def:conv-rad-coefficient-ring-inclusion}
Let $R$ and $R'$ be coefficient sources equipped with $\CommRingUp$ certificates as in \autoref{def:commring-certificate-obligations}. Write their carriers and classifiers as $C_R,\sim_R$ and $C_{R'},\sim_{R'}$, their operations as $0_R,1_R,+_R,-_R,\cdot_R$ and $0_{R'},1_{R'},+_{R'},-_{R'},\cdot_{R'}$, and let $\gamma_R$ and $\gamma_{R'}$ be complex-realization maps into $\ComplexUp$ histories. A convergence-radius compatible coefficient-ring inclusion
$$
\iota:R\hookrightarrow R'
$$
consists of the following data:
\begin{enumerate}
  \item Carrier and classifier rows: $C_R(x)$ implies $C_{R'}(\iota x)$; $x\sim_R y$ implies $\iota x\sim_{R'}\iota y$; and, for carried $x,y$, $\iota x\sim_{R'}\iota y$ implies $x\sim_R y$.
  \item Operation rows: for carried inputs, $\iota(0_R)\sim_{R'}0_{R'}$, $\iota(1_R)\sim_{R'}1_{R'}$, $\iota(x+_R y)\sim_{R'}\iota x+_{R'}\iota y$, $\iota(-_R x)\sim_{R'}-_{R'}\iota x$, and $\iota(x\cdot_R y)\sim_{R'}\iota x\cdot_{R'}\iota y$.
  \item Complex-realization rows: $\gamma_R(x)$ and $\gamma_{R'}(\iota x)$ are complex histories, and every carried $x$ satisfies
$$
\mathsf{ComplexHistoryClassifier}(\gamma_R(x),\gamma_{R'}(\iota x)).
$$
\end{enumerate}
\end{definition}
\leandef{BEDC.Derived.ConvergenceRadiusUp.ConvRadCoefficientRingInclusion}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRadCoefficientRingInclusion\_pointwise\_classifier}

\begin{theorem}[Convergence radius monotonicity under coefficient-ring inclusion]
\label{thm:conv-rad-coefficient-ring-inclusion-monotone}
Let $R$ and $R'$ be $\CommRingUp$ coefficient sources and let $\iota:R\hookrightarrow R'$ be a convergence-radius compatible coefficient-ring inclusion from \autoref{def:conv-rad-coefficient-ring-inclusion}. For a pointwise carried formal coefficient sequence $f:\NatUp\to R$ as in \autoref{def:fps-carrier}, put
$$
a_n:=\gamma_R(f_n),\qquad a'_n:=\gamma_{R'}(\iota(f_n)).
$$
If $\rho$ is a unary radius history and $\mathsf{ConvRad}(a,\rho)$ holds, then
$$
\mathsf{ConvRad}(a',\rho).
$$
Thus every convergence-radius witness admitted by the $R$-view is also admitted by the $R'$-view.
\end{theorem}
\begin{proof}
For each unary index $n$, pointwise carriedness gives $C_R(f_n)$. The complex-realization row in \autoref{def:conv-rad-coefficient-ring-inclusion} therefore supplies
$$
\mathsf{ComplexHistoryClassifier}(a_n,a'_n).
$$
The operation rows in the inclusion identify $a'$ as the coefficient sequence obtained by reading the same formal series in the larger coefficient source; these are the finite algebraic rows used by the partial-sum recursion of \autoref{def:part-sum-power-series}. The convergence-radius argument only needs the resulting pointwise complex classifier family.

The radius endpoint is unchanged. Hence the radius-classifier input for \autoref{lem:conv-rad-radius-coefficient-classifier-transport} is the reflexive history-sameness witness $\hsame(\rho,\rho)$, and the target-radius carrier input is the displayed unary witness for $\rho$. Applying that lemma to the coefficient families $a$ and $a'$ gives $\mathsf{ConvRad}(a',\rho)$.

Equivalently, unpacking \autoref{def:conv-rad}, the same constant-witness function $K$ can be retained. For every strict subradius $r$ of $\rho$, the source row gives $\mathsf{GeomBound}(a,r,K(r))$; the coefficient-classifier transport in \autoref{lem:conv-rad-radius-coefficient-classifier-transport} sends this geometric-bound row to $\mathsf{GeomBound}(a',r,K(r))$, while the radius and constant histories are unchanged as in \autoref{def:geom-bound}. These rows reassemble the convergence-radius predicate for $a'$ at $\rho$.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRad\_coefficient\_ring\_inclusion\_monotone}

\begin{definition}[Cauchy--Hadamard exactness row]
\label{def:convrad-cauchy-hadamard-exactness-row}
For a coefficient family $a$ and a unary radius history $R$, let
$\mathsf{CHExact}(a,R)$ denote the analytic ledger row asserting that the
$\mathsf{ConvRad}(a,R)$ endpoint is exact for the Cauchy--Hadamard limsup
expression,
$$
R = 1/\mathsf{LimSup}_n |a_n|^{1/n}.
$$
This is the Cauchy--Hadamard item in \autoref{def:conv-rad-ledger-policy}. It
is a ledger-policy row, not one of the checked carrier, classifier, transport,
or closure rows of \autoref{thm:conv-rad-namecert}.
\end{definition}
\leandef{BEDC.Derived.ConvergenceRadiusUp.ConvRadCauchyHadamardExactnessRow}

\begin{definition}[Convergence-radius checked-row reduct]
\label{def:convrad-checked-row-reduct}
The checked-row reduct of the $\ConvergenceRadiusUp$ package is the finite row
set $\mathcal Q_{\mathrm{chk}}$ consisting of the following data:
\begin{enumerate}
  \item the carrier row $\mathsf{ConvRad}(a,R)$ and the $\hsame$ classifier on
  radius histories;
  \item radius transport, as in \autoref{lem:conv-rad-radius-transport};
  \item coefficient-family classifier transport, as in
  \autoref{lem:conv-rad-radius-coefficient-classifier-transport};
  \item extraction of power-series carrier data from geometric bounds and from
  convergence-radius witnesses, as in \autoref{lem:geom-bound-power-series-carrier}
  and \autoref{lem:conv-rad-power-series-carrier-witness};
  \item append-unary coefficient closure, as in
  \autoref{thm:conv-rad-append-unary-coeff-closed}.
\end{enumerate}
A policy interpretation over this reduct is a row assignment $\Pi$ with
witnesses for every row in $\mathcal Q_{\mathrm{chk}}$. The assignment is
constrained only on $\mathcal Q_{\mathrm{chk}}$; analytic ledger rows such as
$\mathsf{CHExact}(a,R)$ are not constrained unless the ledger policy of
\autoref{def:conv-rad-ledger-policy} is also supplied.
\end{definition}
\leandef{BEDC.Derived.ConvergenceRadiusUp.ConvRadCheckedRowReduct}

\begin{theorem}[Checked convergence-radius rows do not force Cauchy--Hadamard]
\label{thm:convrad-checked-rows-do-not-force-cauchy-hadamard}
Fix a coefficient family $a$ and a unary radius history $R$ for which the
checked-row reduct of \autoref{def:convrad-checked-row-reduct} has the displayed
carrier row $\mathsf{ConvRad}(a,R)$. In the $\ConvergenceRadiusUp$ setup reduced
to that checked row set, there are two policy interpretations $\Pi_{+}$ and
$\Pi_{-}$ over the same history carrier and the same radius classifier such
that
$$
\forall q\in\mathcal Q_{\mathrm{chk}},\quad \Pi_{+}(q)=\Pi_{-}(q),
$$
but
$$
\Pi_{+}(\mathsf{CHExact}(a,R))=\mathsf{accept}
\qquad\text{and}\qquad
\Pi_{-}(\mathsf{CHExact}(a,R))=\mathsf{reject}.
$$
Consequently, no finite derivation whose nonlogical inputs are only the checked
rows of \autoref{def:convrad-checked-row-reduct} can prove
$\mathsf{CHExact}(a,R)$.
\end{theorem}
\begin{proof}
By \autoref{def:typed-naming-certificate}, the naming-certificate template
separates source, pattern, classifier, stability, and ledger fields. The
concrete convergence-radius package in \autoref{thm:conv-rad-namecert} lists
the checked carrier, classifier, transport, witness, and append-unary closure
rows, and states that the analytic laws of \autoref{def:conv-rad-ledger-policy}
remain ledger-policy obligations. The semantic certificate of
\autoref{thm:conv-rad-semantic-namecert} repacks exactly those checked rows for
the semantic naming-certificate schema.

Let $\chi_{a,R}$ be the row name represented by
$\mathsf{CHExact}(a,R)$. Since $\chi_{a,R}$ is not an element of
$\mathcal Q_{\mathrm{chk}}$, define $\Pi_{+}$ and $\Pi_{-}$ to use the same
witness for every checked row $q\in\mathcal Q_{\mathrm{chk}}$. On the
Cauchy--Hadamard row, set $\Pi_{+}(\chi_{a,R})=\mathsf{accept}$ and
$\Pi_{-}(\chi_{a,R})=\mathsf{reject}$; all other analytic row names may be
assigned identically in the two interpretations.

Every premise and conclusion of a checked row is read from
$\mathcal Q_{\mathrm{chk}}$, so the two interpretations validate exactly the
same checked carrier, classifier, transport, witness, and closure data. They
therefore share the full checked-row reduct. They disagree only on the analytic
ledger row $\mathsf{CHExact}(a,R)$, which the reduct does not mention. If the
checked rows alone forced $\mathsf{CHExact}(a,R)$, any two interpretations that
agree on all rows used by the derivation would agree on that conclusion.
Applying this to $\Pi_{+}$ and $\Pi_{-}$ gives the displayed contradiction.
\end{proof}
\leanchecked{BEDC.Derived.ConvergenceRadiusUp.ConvRad\_checked\_rows\_do\_not\_force\_cauchy\_hadamard}
