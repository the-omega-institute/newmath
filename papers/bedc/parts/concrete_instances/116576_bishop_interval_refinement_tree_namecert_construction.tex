\chapter{A Concrete Naming Certificate for $\BishopIntervalRefinementTreeUp$}
\label{ch:concrete-instances-bishop-interval-refinement-tree-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopIntervalRefinementTreeUp$ packet records the finite interval-refinement tree used when compact interval work branches a cover or modulus request over nested dyadic subintervals. It sits after the compact interval modulus route of \autoref{ch:concrete-instances-compact-interval-modulus-bridge-namecert}, the dyadic interval arithmetic of \autoref{ch:concrete-instances-dyadic-interval-arithmetic-namecert}, and the linear interval-chain boundary of \autoref{ch:concrete-instances-bishop-finite-interval-chain-namecert}. The packet names a finite tree carrier for refinement reasoning; it is not open-cover compactness, a selected subcover, a fan theorem, countable choice, quotient endpoint equality, or ambient completeness of $\RealUp$.

\begin{definition}[Bishop interval-refinement tree carrier]
\label{def:bishop-interval-refinement-tree-carrier}
A $\BishopIntervalRefinementTreeUp$ carrier is a finite $\BHist$ packet
$$
T=(R,E,D,B,L,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the root compact-interval request read through $\CompactIntervalModulusBridgeUp$.
\item $E$ stores the dyadic endpoint enclosure and containment data supplied by $\DyadicIntervalArithmeticUp$.
\item $D$ is the finite descendant ledger, listing each split interval, its parent, and its endpoint refinement witnesses.
\item $B$ is the branch-frontier row naming the currently inspected leaves and their cover or modulus obligations.
\item $L$ is the sibling $\BishopFiniteIntervalChainUp$ row obtained by reading any chosen level of leaves as a finite adjacent chain.
\item $A$ records acceptance of each refinement step by local interval containment and leaf-frontier coverage.
\item $H$ stores componentwise $\hsame$ transport for $R,E,D,B,L,A$ and the structural rows.
\item $C$ stores displayed $\Cont$ replay from the compact request through dyadic splits, branch frontiers, and level-chain reads.
\item $P$ is the $\Pkg$ provenance over $\BishopIntervalRefinementTreeUp$, $\CompactIntervalModulusBridgeUp$, $\DyadicIntervalArithmeticUp$, $\BishopFiniteIntervalChainUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopIntervalRefinementTreeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these ten rows. It has no coordinate for an infinite refinement tree, selected branch, open-cover subcover, fan principle, quotient endpoint equality, countable choice, or host compactness theorem.
\end{definition}

\begin{theorem}[Bishop interval-refinement tree NameCert obligations]
\label{thm:bishop-interval-refinement-tree-namecert-obligations}
For every accepted $\BishopIntervalRefinementTreeUp$ carrier $T=(R,E,D,B,L,A,H,C,P,N)$, the local $\NameCert_{\BishopIntervalRefinementTreeUp}$ surface consists exactly of carrier admission, compact-root exposure, dyadic endpoint-refinement exactness, finite descendant-ledger stability, leaf-frontier coverage, level-chain readback through $\BishopFiniteIntervalChainUp$, refinement-step acceptance, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the accepted carrier. The root row $R$ is the only compact-interval request, the endpoint row $E$ is the only dyadic arithmetic source, and the descendant ledger $D$ stores every split relation inspected by the packet.

The branch-frontier and chain rows give the consumer-facing tree shape. The row $B$ names the visible leaves, while $L$ reads any displayed level as the adjacent finite-chain surface already provided by \autoref{ch:concrete-instances-bishop-finite-interval-chain-namecert}.

The acceptance and structural rows then exhaust the certificate. The row $A$ records local containment and coverage for each refinement step, and $H,C,P,N$ transport, replay, source, and name the same finite packet. Since no other coordinate is present, the listed obligations are exactly the local NameCert surface.
\end{proof}

\begin{theorem}[Bishop interval-refinement tree refinement non-escape]
\label{thm:bishop-interval-refinement-tree-refinement-nonescape}
Every accepted $\BishopIntervalRefinementTreeUp$ refinement read factors through
$$
\CompactIntervalModulusBridgeUp,\DyadicIntervalArithmeticUp,\BishopFiniteIntervalChainUp,\BHist,\hsame,\Cont,\Pkg,\NameCert .
$$
For a carrier $T=(R,E,D,B,L,A,H,C,P,N)$, a consumer may inspect only the compact root request, dyadic endpoint refinements, finite descendant ledger, visible leaf frontier, level-chain readback, refinement-step acceptance, transport, replay, provenance, and local name. No consumer receives an infinite tree, selected branch, open-cover subcover, fan theorem, countable-choice schedule, quotient endpoint equality, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Begin with the carrier inventory of \autoref{def:bishop-interval-refinement-tree-carrier}. The rows $R,E,D,B,L,A$ are the complete finite refinement data, and the rows $H,C,P,N$ are the complete structural data.

Apply the obligation surface of \autoref{thm:bishop-interval-refinement-tree-namecert-obligations}. It forces every refinement read through the compact root row, dyadic endpoint refinements, finite descendant ledger, leaf frontier, level-chain readback, and local acceptance row before the packet can be consumed.

The three cited sibling chapters supply only the displayed services: compact modulus bridging, dyadic interval arithmetic, and finite interval chains. None supplies an infinite branch selector, an open-cover subcover, or a host compactness object.

Thus the accepted refinement read remains inside $R,E,D,B,L,A,H,C,P,N$. Transport and replay preserve this finite route, provenance records it, and the local name does not add any unrecorded source.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopIntervalRefinementTreeUp$ read factors through a compact interval modulus root, dyadic endpoint-refinement rows, a finite descendant ledger, a leaf-frontier row, a finite interval-chain readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{compact-interval-modulus-bridge, dyadic-interval-arithmetic, bishop-finite-interval-chain: none is bijective because $\BishopIntervalRefinementTreeUp$ binds the compact request, dyadic endpoint refinements, finite descendant tree ledger, leaf-frontier coverage, level-chain readback, refinement acceptance, transport, replay, provenance, and local NameCert rows in one carrier.}

\closureat{BishopIntervalRefinementTreeUp}{seedStr}

\begin{closurestatus}{\BishopIntervalRefinementTreeUp}
  \constructivestory{A $\BishopIntervalRefinementTreeUp$ packet is generated from a compact interval modulus root, dyadic endpoint-refinement rows, a finite descendant ledger, leaf-frontier rows, finite interval-chain readback, refinement-step acceptance, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-interval-refinement-tree-carrier}, \autoref{thm:bishop-interval-refinement-tree-namecert-obligations}, and \autoref{thm:bishop-interval-refinement-tree-refinement-nonescape} seed the finite interval-refinement tree carrier and its non-escape route through compact modulus, dyadic interval arithmetic, finite interval chains, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not assert open-cover compactness, a selected subcover, a fan theorem, countable choice, quotient endpoint equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic endpoint-refinement exactness, descendant-ledger stability, leaf-frontier coverage, level-chain readback, refinement-step acceptance, transport, replay, provenance, and local naming for BEDC.Derived.BishopIntervalRefinementTreeUp.}
\end{closurestatus}
