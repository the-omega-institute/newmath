\begin{theorem}[Covering dimension real-separability sampling]
\label{thm:covering-dimension-real-separability-sampling}
Every finite-cover dimension consumer may use $\RealSeparabilityUp$ only through displayed rational dense windows before compact metric cover checks reduce to finite sampled rows. The $\CoveringDimensionUp$ carrier exports only the finite cover row, refinement map, order-bound witness, compact Lebesgue ledger, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of \autoref{def:covering-dimension-carrier}.
\end{theorem}
\begin{proof}
Read the dense-window evidence from \autoref{ch:concrete-instances-real-separability-namecert}. It supplies sampled rational names for inspected balls and mesh witnesses before any compact metric cover check is evaluated.

This is the downstream consumer edge for the $\RealSeparabilityUp$ packet: \autoref{ch:concrete-instances-real-separability-namecert} supplies rational dense windows that \autoref{ch:concrete-instances-coveringdimension-namecert} consumes before finite-cover order checks.

Now project the $\CoveringDimensionUp$ carrier of \autoref{def:covering-dimension-carrier}. The only exported rows are the finite cover row, refinement map, order-bound witness, compact Lebesgue ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row.

Thus the dependency is a finite-data sampling interface. It does not add arbitrary open-cover compactness, a selected infinite refinement, or any host dimension theorem.
\end{proof}
\leanchecked{BEDC.Derived.CoveringdimensionUp.CoveringDimensionRealSeparabilitySampling}

\begin{theorem}[Covering dimension real-separability route scope]
\label{thm:coveringdimension-real-separability-route-scope}
Every accepted $\CoveringDimensionUp$ packet that uses the $\RealSeparabilityUp$ density route must read finite dense-window data through
$$
\RealSeparabilityUp,\quad \MetricUp,\quad \CompactMetricUp,\quad \FiniteEpsilonNetUp,\quad \RegSeqRatUp
$$
before it exposes any covering-dimension classifier row. The exposed classifier is therefore bounded by displayed dense samples, metric balls, compact-metric finite-net rows, and regular rational readback; it does not supply open-cover compactness, choice-selected covers, selected limits, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the $\CoveringDimensionUp$ carrier of \autoref{def:covering-dimension-carrier}. A density-dependent read first reaches the $\RealSeparabilityUp$ row, then the metric and compact-metric rows that turn sampled balls into finite cover windows.

Next read the $\FiniteEpsilonNetUp$ and $\RegSeqRatUp$ rows. They provide the finite net and regular rational window data consumed by the dimension route before the classifier row is visible.

The carrier has no hidden coordinate for arbitrary open covers or a choice-selected refinement. Since the Real-facing dependency is already exhausted by the displayed finite rows, the route cannot import ambient $\RealUp$ completeness or a host compactness theorem.
\end{proof}
