\begin{definition}[CoveringDimension finite cover carrier]
\label{def:coveringdimension-finite-cover-carrier}
For an accepted $\CoveringDimensionUp$ packet, the finite cover carrier is the displayed $\BHist$ subpacket
$$
F=(K,E,M,C,R,O,S,A,H,T,P,N)
$$
cut out of the local carrier of \autoref{def:covering-dimension-carrier}. Here $K$ is the $\CompactMetricUp$ source row, $E$ is the finite-net row, $M$ is the $\MetricUp$ distance-read ledger used by the cover tests, $C$ is the finite $\BHist$ cover row, $R$ is the refinement membership row, $O$ is the order-bound ledger, $S$ is the $\SimplicialComplexUp$ nerve row, $A$ records any $\RegSeqRatUp$ or $\RealUp$ seal rows consumed by a metric read, and $H,T,P,N$ are componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ data.

The carrier is finite at every exposed face. It contains no arbitrary open cover, selected subcover, host multiplicity oracle, quotient completion point, or ambient compactness theorem.
\end{definition}

\begin{theorem}[CoveringDimension finite cover admission]
\label{thm:coveringdimension-finite-cover-admission}
Every root $\CoveringDimensionUp$ read that admits finite cover data must first expose the finite cover carrier of \autoref{def:coveringdimension-finite-cover-carrier}. In particular, the $\CompactMetricUp$ finite-net rows $K,E$, the $\MetricUp$ distance reads $M$, the finite $\BHist$ cover row $C$, the refinement row $R$, the order ledger $O$, any $\RegSeqRatUp$ or $\RealUp$ seal rows $A$, and the structural rows $H,T,P,N$ are all visible before a dimension-bound consumer may read an order conclusion.
\end{theorem}
\leanchecked{BEDC.Derived.CoveringdimensionUp.CoveringDimensionCarrier\_finite\_cover\_admission}
\begin{proof}
Project the accepted carrier through \autoref{def:covering-dimension-carrier}. The compact source, finite-net row, cover row, refinement row, order ledger, and structural rows are already part of the displayed CoveringDimension packet.

Insert the metric and Real-facing reads only as rows of the same finite carrier. The $\MetricUp$ ledger records distance tests for cover membership, while the $\RegSeqRatUp$ and $\RealUp$ seal rows are read only when those tests are Real-facing.

Since no arbitrary open-cover coordinate occurs in $F$, admission is a finite row projection. The dimension-bound consumer receives the displayed finite cover route and none of the excluded host principles.
\end{proof}

\begin{theorem}[CoveringDimension order refinement route]
\label{thm:coveringdimension-order-refinement-route}
For a finite cover carrier $F=(K,E,M,C,R,O,S,A,H,T,P,N)$, the order-bound row $O$ is readable only after each member counted by $O$ has been traced through the finite cover row $C$ and the refinement membership row $R$. The route uses the same $\CompactMetricUp$ finite-net rows, $\MetricUp$ distance reads, and $\BHist$ cover entries that admitted the carrier, and it exports no selected refinement or host overlap oracle.
\end{theorem}
\leanchecked{BEDC.Derived.CoveringdimensionUp.CoveringDimensionOrderRefinementRoute}
\begin{proof}
Start with the finite cover admission theorem. It places $K,E,M,C,R$ before $O$ in the public route.

For every refined member, the row $R$ names a parent in the finite cover row $C$ and records the containment witness used by the distance-read ledger $M$. Thus the overlap count in $O$ is computed over displayed $\BHist$ rows.

The structural rows $H,T,P,N$ transport, replay, source, and name that finite computation. They do not add a second order source, so the order refinement route remains a finite CoveringDimension obligation.
\end{proof}

\begin{theorem}[CoveringDimension simplicial nerve handoff]
\label{thm:coveringdimension-simplicial-nerve-handoff}
Every $\SimplicialComplexUp$ nerve read consumed by a $\CoveringDimensionUp$ finite cover carrier must use the same finite cover and refinement data
$$
C,R,O
\longmapsto
S
$$
after the compact finite-net rows $K,E$ and metric distance ledger $M$ have been displayed. The nerve row $S$ receives vertices and incidence rows from the finite cover carrier; it receives no classical nerve theorem, selected locally finite cover, or ambient topological realization.
\end{theorem}
\begin{proof}
Read the carrier $F$. The cover row $C$ supplies the finite vertex list for the nerve, while $R$ supplies the refinement memberships that determine incidence.

The order ledger $O$ bounds the displayed overlap count before the nerve row is consumed. Therefore the $\SimplicialComplexUp$ row $S$ is downstream of the same finite cover-refinement route, not a detached simplicial object.

The compact finite-net and metric rows $K,E,M$ remain attached to the cover tests. Hence the handoff to the nerve surface is constructive row replay over the accepted carrier.
\end{proof}

\begin{theorem}[CoveringDimension Cauchy metric scope]
\label{thm:coveringdimension-cauchy-metric-scope}
When a $\CoveringDimensionUp$ finite cover carrier is read by a Cauchy, RegSeqRat, or Real-facing metric consumer, the consumer may use only the displayed route
$$
\CompactMetricUp
\longmapsto
\MetricUp
\longmapsto
\RegSeqRatUp
\longmapsto
\RealUp
$$
inside the carrier rows $K,E,M,A$. The finite cover, refinement, order, and nerve rows $C,R,O,S$ remain the only dimension-facing rows, and no quotient completion point, selected limit, host metric equality, or ambient $\RealUp$ completeness is exposed.
\end{theorem}
\begin{proof}
Project $K,E,M,A$ from \autoref{def:coveringdimension-finite-cover-carrier}. These are the only rows through which metric or Real-facing reads enter the finite cover route.

The regular rational and Real seal data in $A$ are downstream of the metric distance ledger $M$. They can certify the numerical side of a cover test, but they do not replace the finite cover row $C$ or refinement row $R$.

Finally use the order refinement and nerve handoff routes. They keep $C,R,O,S$ as the dimension-facing surface, so completion or Real-facing metric data remain scoped to the displayed finite carrier.
\end{proof}
\leanchecked{BEDC.Derived.CoveringdimensionUp.CoveringDimensionCauchyMetricScope}

\begin{theorem}[CoveringDimension finite cover root metric-completion route]
\label{thm:coveringdimension-finite-cover-root-metric-completion-route}
Every metric-completion read of a $\CoveringDimensionUp$ finite cover carrier
$$
F=(K,E,M,C,R,O,S,A,H,T,P,N)
$$
factors through the displayed finite-cover route $K,E,M,C,R,O,S,A,H,T,P,N$. The completion-facing rows may inspect the compact source $K$, finite-net row $E$, metric distance ledger $M$, and regular or Real seal row $A$, but the dimension conclusion remains routed through the finite cover row $C$, refinement row $R$, order ledger $O$, nerve row $S$, and structural rows $H,T,P,N$. No quotient completion point, selected Cauchy limit, arbitrary open cover, selected subcover, host metric equality, or ambient compactness principle is exported.
\end{theorem}
\leanchecked{BEDC.Derived.CoveringdimensionUp.CoveringDimensionFiniteCoverRootMetricCompletionRoute}
\begin{proof}
Start with \autoref{thm:coveringdimension-cauchy-metric-scope}. It places every completion-facing metric read inside the rows $K,E,M,A$ of the finite cover carrier.

Then apply finite cover admission and order refinement. The rows $C,R,O,S$ remain the only dimension-facing route, so the completion read cannot replace them by a quotient point, a selected limit, or an external open-cover family.

The transport, replay, provenance, and naming rows $H,T,P,N$ keep both readings attached to the same displayed carrier. Thus metric completion consumes the finite-cover root rather than an ambient completion construction.
\end{proof}

\begin{theorem}[CoveringDimension finite cover root Real separability route]
\label{thm:coveringdimension-finite-cover-root-real-separability-route}
Every $\RealUp$ separability read that consumes a $\CoveringDimensionUp$ finite cover carrier
$$
F=(K,E,M,C,R,O,S,A,H,T,P,N)
$$
must pass through the same finite-cover root $K,E,M,C,R,O,S,A,H,T,P,N$. The separability consumer may read rational or regular Real-facing data only through the metric ledger $M$ and the seal row $A$, while cover membership, refinement, order, and nerve data remain the displayed rows $C,R,O,S$. The route exports no arbitrary dense sequence, selected real representative, quotient completion point, host open ball, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier and read the finite-net and metric rows $K,E,M$. These rows are the only source from which a Real-facing separability consumer can obtain numerical cover tests.

The seal row $A$ is downstream of those tests and does not introduce a separate dense sequence or selected Real representative. By the finite cover admission theorem, cover data still enter through $C$ and refinement through $R$.

Finally the order and nerve rows $O,S$ are replayed through $H,T,P,N$. Hence Real separability reads factor through the displayed finite-cover root and do not expose any ambient dense-set or completeness principle.
\end{proof}
\leanchecked{BEDC.Derived.CoveringdimensionUp.CoveringDimensionFiniteCoverRootRealSeparabilityRoute}

\begin{theorem}[CoveringDimension finite cover root Lebesgue ledger route]
\label{thm:coveringdimension-finite-cover-root-lebesgue-ledger-route}
Every finite Lebesgue-ledger read attached to a $\CoveringDimensionUp$ finite cover carrier
$$
F=(K,E,M,C,R,O,S,A,H,T,P,N)
$$
is computed from the displayed route $K,E,M,C,R,O,S,A,H,T,P,N$. The ledger may consume the compact finite-net rows $K,E$, the metric test row $M$, and any regular or Real seal row $A$ only to certify finite cover membership; its covering, refinement, order, and nerve obligations are exactly $C,R,O,S$ transported and replayed by $H,T,P,N$. It exports no arbitrary open cover, selected Lebesgue number, host compactness theorem, chosen finite subcover, quotient completion point, or ambient Real-completeness witness.
\end{theorem}
\begin{proof}
Use finite cover admission to display the rows $K,E,M,C,R,O,S,A,H,T,P,N$. A finite Lebesgue ledger can read $K,E,M,A$ only as the numerical side of membership tests.

Order refinement then forces every counted overlap through $C$ and $R$, and the simplicial nerve handoff routes incidence through $S$. Thus the ledger consumes the same finite cover, refinement, order, and nerve rows as the dimension packet.

The structural rows $H,T,P,N$ transport, replay, source, and name that computation. Since no selected Lebesgue number, chosen subcover, host compactness theorem, quotient completion point, or ambient Real-completeness witness is present in the carrier, the ledger factors through the finite-cover root.
\end{proof}
\leanchecked{BEDC.Derived.CoveringdimensionUp.CoveringDimensionFiniteCoverRootLebesgueLedgerRoute}

\begin{theorem}[CoveringDimension finite cover root Cauchy selection route]
\label{thm:coveringdimension-finite-cover-root-cauchy-selection}
Every Cauchy or metric-completion cover-selection read attached to a $\CoveringDimensionUp$ finite cover carrier
$$
F=(K,E,M,C,R,O,S,A,H,T,P,N)
$$
factors through the displayed finite-cover route. The compact metric and metric rows $K,E,M$ are displayed before any Cauchy read, the regular and Real-facing row $A$ is displayed before any completion-facing numerical seal, and the finite cover, refinement, order, and nerve rows $C,R,O,S$ are displayed before selection data may be inspected. The route exports no quotient completion point, selected Cauchy limit, arbitrary open cover, host metric equality, or ambient compactness principle.
\end{theorem}
\begin{proof}
Use finite cover admission to expose $K,E,M,C,R,O,S,A,H,T,P,N$. This places compact metric data, metric distance tests, finite cover membership, refinement rows, order bounds, and nerve rows in the finite carrier before any selection read.

Apply the Cauchy metric scope and metric-completion route. They force completion-facing reads to remain inside $K,E,M,A$ and prevent a quotient completion point or selected Cauchy limit from replacing the displayed finite-cover rows.

Use the Real separability route for the numerical seal side. It keeps regular rational and Real-facing data downstream of the metric ledger, so arbitrary open covers, host metric equality, and ambient compactness never enter the exported CoveringDimension root.
\end{proof}
\leanchecked{BEDC.Derived.CoveringdimensionUp.CoveringDimensionFiniteCoverRootCauchySelection}

\begin{theorem}[CoveringDimension finite-cover scope grounding]
\label{thm:covering-dimension-finite-cover-scope-grounding}
Each $\CoveringDimensionUp$ finite-cover root obligation must expose the $\MetricUp$ ball row, $\CompactMetricUp$ finite-net row, $\RealUp$ radius seal, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance row, and $\RealSeparabilityUp$ dense-window row before any covering-dimension consumer reads a refinement conclusion. The refinement remains a finite-cover root route over the displayed rows $K,E,M,C,R,O,S,A,H,T,P,N$.
\end{theorem}
\begin{proof}
Project the finite-cover carrier of \autoref{def:coveringdimension-finite-cover-carrier} and the admission route of \autoref{thm:coveringdimension-finite-cover-admission}. These expose the compact source, finite-net row, metric distance ledger, finite cover row, refinement row, order ledger, and structural rows before the dimension-facing conclusion is read.

Use the metric-completion, Real-separability, and Lebesgue-ledger routes, \autoref{thm:coveringdimension-finite-cover-root-metric-completion-route}, \autoref{thm:coveringdimension-finite-cover-root-real-separability-route}, and \autoref{thm:coveringdimension-finite-cover-root-lebesgue-ledger-route}. Together they force radius seals, regular readback, dyadic tolerances, and dense-window data to remain inside the same finite-cover root.

The dependency routes in this subdirectory keep metric, compact-metric, Real, RealSeparability, RegSeqRat, and DyadicRatCore reads explicit. Since no row names open-cover compactness, a selected subcover, a host dimension theorem, or quotient real equality, every refinement consumer is grounded in the displayed finite BEDC scope.
\end{proof}
