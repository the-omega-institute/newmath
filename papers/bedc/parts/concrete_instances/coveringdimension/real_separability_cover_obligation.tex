Every $\RealSeparabilityUp$ density-window use of $\CoveringDimensionUp$ must consume the finite cover-refinement rows of \autoref{def:covering-dimension-carrier} through displayed metric and naming data rather than through an ambient open-cover principle.

\begin{theorem}[Covering dimension real-separability cover obligation]
\label{thm:coveringdimension-real-separability-cover-obligation}
Every $\CoveringDimensionUp$ cover-refinement row consumed by a $\RealSeparabilityUp$ density-window argument must expose finite cover rows, dyadic or rational density windows, metric carrier rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and $\NameCert$ rows on the same finite BEDC surface. The consumer receives no detached host cover, choice-selected refinement, external density oracle, quotient point equality, or ambient separability theorem.
\end{theorem}
\leanchecked{BEDC.Derived.CoveringdimensionUp.CoveringDimensionRealSeparabilityCoverObligation}
\begin{proof}
Project the covering-dimension carrier of \autoref{def:covering-dimension-carrier}. Its cover and refinement rows are finite rows attached to the compact metric source, finite epsilon-net schedule, order-bound row, Lebesgue ledger, transport, replay, provenance, and local naming.

Use the separability cover-refinement route, \autoref{lem:covering-dimension-separability-cover-refinement-route}. It already forces any $\RealSeparabilityUp$ consumer to pass through the compact metric row, finite epsilon-net row, cover row, refinement row, order-bound row, compact-cover Lebesgue ledger, and structural rows before the separability-facing read is available.

The density-window requirement is therefore the same finite obligation surface read from the separability side. Dyadic or rational windows may be consumed only as displayed finite rows over the metric carrier and structural rows, while detached host covers, choice-selected refinements, external density oracles, quotient point equality, and ambient separability are not carrier coordinates.
\end{proof}

\begin{theorem}[Covering dimension real-separability cover scope]
\label{thm:coveringdimension-real-separability-cover-obligation-scope}
Every $\RealSeparabilityUp$ density-window consumer of $\CoveringDimensionUp$ must cite $\CompactMetricUp$, $\FiniteEpsilonNetUp$, $\CompactCoverLebesgueLedgerUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows before reading a cover-refinement row. The consumer receives the finite L10 cover route through the displayed carrier, not through open-cover compactness, quotient equality, or external separability.
\end{theorem}
\begin{proof}
Project the $\CoveringDimensionUp$ carrier of \autoref{def:covering-dimension-carrier}. Its finite cover-refinement rows expose compact metric source data, finite epsilon-net witnesses, compact-cover Lebesgue ledger data, order rows, transport, replay, provenance, and local naming before a separability-facing consumer can read a refinement.

Apply the cover-obligation theorem \autoref{thm:coveringdimension-real-separability-cover-obligation}. It already requires finite cover rows, dyadic or rational density windows, metric carrier rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and $\NameCert$ rows on the same BEDC surface.

The scope condition is therefore just the explicit consumer boundary for that route. A $\RealSeparabilityUp$ density-window read must display $\CompactMetricUp$, $\FiniteEpsilonNetUp$, $\CompactCoverLebesgueLedgerUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ before it reads the cover-refinement row. Since none of those rows is open-cover compactness, quotient equality, or an external separability theorem, those excluded objects remain outside the accepted packet.
\end{proof}
