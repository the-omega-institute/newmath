\chapter{A Concrete Naming Certificate for \(\POSetUp\)}
\label{ch:concrete-instances-poset-namecert}

The partially ordered set interface \(\POSetUp\) extends \autoref{ch:concrete-instances-preorder-namecert} with the antisymmetry field: if \(a \le b\) and \(b \le a\) then \(a\) and \(b\) are classified the same by the carrier. The integer order from \autoref{ch:concrete-instances-int-namecert} is the canonical concrete instance.

\begin{remark}[\(\POSetUp\) interface scope]
\label{rem:concrete-instances-poset-namecert-scope}
This chapter records the \(\POSetUp\) licensing target. The concrete prefix
antisymmetry theorem is linked to the unary preorder classifier, and the
certificate fields are specified below.
\end{remark}

\section{Carrier definition}

\begin{definition}[POSet carrier]
\label{def:poset-carrier}
Use a preorder setup extended with antisymmetry under the carrier classifier.
\end{definition}

\section{Concrete unary prefix POSet instance}
\label{sec:poset-concrete-unary-prefix-instance}

\begin{definition}[Concrete unary prefix POSet instance]
\label{def:concrete-unary-prefix-poset-instance}
The concrete unary prefix POSet instance is the following ten-field tuple.
\begin{enumerate}
  \item The traditional target is the finite unary-word prefix partial order.
  \item The BEDC source is $\mathsf{BHist}$ restricted to unary histories.
  \item The carrier is $\mathsf{PreorderCarrier}=\mathsf{UnaryHistory}$.
  \item The classifier is the carrier-aware specification $\mathsf{PreorderCarrierClassifierSpec}(\mathsf{PreorderCarrier})$, whose equality content is $\hsame$.
  \item The relation is $\mathsf{PreorderPrefixLE}$.
  \item Stability consists of classifier reflexivity, classifier transitivity, relation reflexivity, relation transitivity, relation congruence, and antisymmetry under the classifier.
  \item The ledger records both comparison-tail witnesses used for $h\le k$ and $k\le h$.
  \item The core theorem list is the concrete prefix stability certificate together with prefix antisymmetry.
  \item Exactness is the bidirectional prefix collapse: two opposing prefix comparisons collapse exactly to the classifier relation.
  \item The standard bridge identifies $\mathsf{PreorderPrefixLE}$ with the initial-segment order on finite unary words, and the status is $\mathsf{ProofTarget}$.
\end{enumerate}
\end{definition}

\begin{theorem}[Concrete unary prefix POSet laws]
\label{thm:concrete-unary-prefix-poset-laws}
The tuple in \autoref{def:concrete-unary-prefix-poset-instance} satisfies the $\POSetUp$ carrier field, the preorder fields for $\mathsf{PreorderPrefixLE}$, classifier congruence for $\hsame$, and antisymmetry under the carrier classifier.
\end{theorem}
\begin{proof}
The carrier and classifier fields are the concrete preorder fields: the carrier is $\mathsf{PreorderCarrier}=\mathsf{UnaryHistory}$, and the classifier is $\mathsf{PreorderCarrierClassifierSpec}(\mathsf{PreorderCarrier})$.

The preorder and classifier-congruence fields are supplied by the concrete prefix stability certificate in \autoref{def:preorder-prefix-stability-certificate}. Equivalently, \autoref{thm:preorder-prefix-order-laws-hsame} records the reflexive and transitive prefix laws together with the history-sameness classifier boundary.

It remains to supply the partial-order field. Apply \autoref{thm:poset-prefix-antisymmetry}: opposing $\mathsf{PreorderPrefixLE}$ witnesses provide the two comparison tails, and the theorem collapses them to $\hsame(h,k)$, which is exactly antisymmetry under the carrier classifier.
\end{proof}

\begin{definition}[Concrete singleton empty POSet instance]
\label{def:concrete-singleton-empty-poset-instance}
Let $A_{\emp}$ be the singleton history source of
Definition~\ref{def:list-singleton-history-source} at $a_0=\emp$. Define the
restricted classifier by
$$
\rho_{\emp}(h,k):=
A_{\emp}(h)\land A_{\emp}(k)\land\hsame(h,k).
$$
Define the carrier-guarded restricted order by
$$
h\le_{\emp}k:=
A_{\emp}(h)\land A_{\emp}(k)\land \mathsf{PreorderPrefixLE}(h,k).
$$
The concrete POSet package is the fixed $\mathsf{BHist}$ tuple
$(A_{\emp},\rho_{\emp},\le_{\emp})$.
\end{definition}

\begin{theorem}[Singleton empty POSet laws]
\label{thm:singleton-empty-poset-laws}
For the concrete package of
\autoref{def:concrete-singleton-empty-poset-instance}, the carrier is
inhabited by $\emp$, carrier membership transports along $\hsame$, and
$\le_{\emp}$ has preorder reflexivity, transitivity, and endpoint congruence
inherited from the singleton empty preorder laws. The package also satisfies
antisymmetry under $\rho_{\emp}$. For all histories $h,k:\mathsf{BHist}$, the
classifier is exact:
$$
\rho_{\emp}(h,k)\Longleftrightarrow
(h\le_{\emp}k\land k\le_{\emp}h).
$$
\end{theorem}
\begin{proof}
The carrier and preorder fields are those of
\autoref{thm:singleton-empty-preorder-laws}. Inhabitance is witnessed by
$\emp$, carrier transport is transport along $\hsame$, and the reflexive,
transitive, and endpoint-congruent behavior of $\le_{\emp}$ is the restricted
$\mathsf{PreorderPrefixLE}$ package on the singleton source.

For antisymmetry, suppose $h$ and $k$ are carried and that
$h\le_{\emp}k$ and $k\le_{\emp}h$. Project the opposing
$\mathsf{PreorderPrefixLE}$ witnesses from the guarded comparisons, so
$\mathsf{PreorderPrefixLE\_antisymm\_hsame}$ gives $\hsame(h,k)$. Pair this
with the carrier witnesses for $h$ and $k$ to obtain $\rho_{\emp}(h,k)$.

For exactness, first assume $\rho_{\emp}(h,k)$. The classifier supplies the
two carrier witnesses and the endpoint witness $\hsame(h,k)$. Apply
$\mathsf{PreorderPrefixLE\_of\_hsame}$ to this witness and to its symmetric
orientation, then repack the two carrier witnesses with the resulting prefix
comparisons to obtain $h\le_{\emp}k$ and $k\le_{\emp}h$.

Conversely, assume the two guarded comparisons. Project the carrier witnesses
and the opposing prefix witnesses from them. Apply
$\mathsf{PreorderPrefixLE\_antisymm\_hsame}$ to the prefix witnesses to recover
$\hsame(h,k)$, and recombine this endpoint witness with the carrier witnesses
to get $\rho_{\emp}(h,k)$.
\end{proof}

\begin{theorem}[Singleton empty POSet terminal interval collapse]
\label{thm:poset-singleton-empty-terminal-interval-collapse}
For the concrete package of
\autoref{def:concrete-singleton-empty-poset-instance}, let
$h,k,r:\mathsf{BHist}$ be carried by $A_{\emp}$. If the displayed terminal
chain supplies $h\le_{\emp}k$ and $k\le_{\emp}r$, then all three endpoints
collapse inside the singleton interval:
$$
\rho_{\emp}(h,k)\land \rho_{\emp}(k,r)\land \rho_{\emp}(h,r)\land
h\le_{\emp}r\land r\le_{\emp}h.
$$
The ten-field fit is: traditional target one-point partial order interval
collapse; BEDC source singleton $\mathsf{BHist}$; carrier $A_{\emp}$;
classifier $\rho_{\emp}$; relation $\le_{\emp}$; stability from singleton
transport, preorder transitivity, and POSet antisymmetry; ledger the two chain
comparisons plus carried endpoints; core theorem this block; exactness
terminal classifier collapse; standard bridge one-point poset; status
$\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Unfold the singleton carrier $A_{\emp}$. Each carried endpoint transports
through the empty history $\emp$, so the endpoint classifier witnesses
$\rho_{\emp}(h,k)$, $\rho_{\emp}(k,r)$, and $\rho_{\emp}(h,r)$ are obtained by
pairing the two relevant carrier witnesses with the corresponding
$\hsame$ endpoint comparison.

The comparison $h\le_{\emp}r$ is inherited from preorder transitivity for the
restricted prefix relation: compose the displayed witnesses
$h\le_{\emp}k$ and $k\le_{\emp}r$.

For the reverse comparison, the same singleton endpoint comparison identifies
$r$ with $h$ inside $A_{\emp}$. Apply the exactness direction of
\autoref{thm:singleton-empty-poset-laws} to the classifier witness
$\rho_{\emp}(h,r)$, then read off $r\le_{\emp}h$ from the opposing comparison.
The five displayed conjuncts therefore repack the terminal interval collapse.
\end{proof}

This concrete $\mathsf{BHist}$ tuple is the $\POSetUp$ schema instance used by
machine checking.

\begin{theorem}[Concrete singleton empty $\POSetUp$ naming certificate]
\label{thm:concrete-singleton-empty-poset-namecert}
The tuple $(A_{\emp},\rho_{\emp},\le_{\emp})$ of
\autoref{def:concrete-singleton-empty-poset-instance} supplies the source,
pattern, classifier, stability, ledger, and exactness rows for a concrete
$\NameCert$ licensing $\POSetUp$ over $\mathsf{BHist}$. On carried histories,
the exactness row is
$$
\rho_{\emp}(h,k)\Longleftrightarrow(h\le_{\emp}k\land k\le_{\emp}h).
$$
\end{theorem}
\begin{proof}
The source row is the singleton history source $A_{\emp}$ anchored at $\emp$,
and the classifier row is the restricted carrier classifier $\rho_{\emp}$ from
\autoref{def:concrete-singleton-empty-poset-instance}. The pattern row is the
restricted prefix comparison $h\le_{\emp}k$ on carried histories.

The preorder, antisymmetry, and stability rows are exactly the concrete law
package of \autoref{thm:singleton-empty-poset-laws}. Its preorder fields give
reflexivity, transitivity, and endpoint congruence for the restricted prefix
relation, while its antisymmetry field collapses opposing comparisons to
$\rho_{\emp}$.

The ledger records the two comparison witnesses used by antisymmetry,
$h\le_{\emp}k$ and $k\le_{\emp}h$, together with the carried endpoints. The
displayed equivalence is the exactness row supplied by the same law package, so
the concrete tuple repacks as the public $\NameCert$ for $\POSetUp$.
\end{proof}

\begin{theorem}[Singleton empty $\POSetUp$ schema instantiation]
\label{thm:poset-singleton-schema-instantiated}
The tuple $(A_{\emp},\rho_{\emp},\le_{\emp})$ of
\autoref{def:concrete-singleton-empty-poset-instance} instantiates the
$\POSetUp$ schema over the fixed BEDC history carrier $\mathsf{BHist}$. Its
carrier row is $A_{\emp}$, its classifier row is $\rho_{\emp}$, and its
relation row is $\le_{\emp}$. Its preorder reflexivity, preorder
transitivity, relation congruence, antisymmetry, and exactness rows are exactly
the rows certified in \autoref{thm:singleton-empty-poset-laws}. Its concrete
$\NameCert$ packaging is the certificate of
\autoref{thm:concrete-singleton-empty-poset-namecert}.
\end{theorem}
\begin{proof}
Unfold \autoref{def:concrete-singleton-empty-poset-instance}. The carrier is
the singleton history source $A_{\emp}$ inside $\mathsf{BHist}$, the classifier
is the restricted history classifier $\rho_{\emp}$, and the relation is the
carrier-guarded restricted prefix comparison $\le_{\emp}$.

The law rows are supplied without changing carrier. Apply
\autoref{thm:singleton-empty-poset-laws}: it gives the singleton inhabitance
and carrier transport, the preorder reflexivity and transitivity rows for
$\le_{\emp}$, endpoint congruence for the guarded relation, antisymmetry under
$\rho_{\emp}$, and the displayed exactness between $\rho_{\emp}$ and the two
opposing guarded comparisons.

Finally apply
\autoref{thm:concrete-singleton-empty-poset-namecert}. That theorem packages
the same source, pattern, classifier, stability, ledger, and exactness fields
as a concrete $\NameCert$ licensing $\POSetUp$ over $\mathsf{BHist}$. Therefore
the schema instance is pinned to the concrete history carrier and not obtained
by transporting an abstract parameter schema.
\end{proof}

\section{The certificate}

\begin{definition}[POSet source specification]
\label{def:poset-source-specification}
The source is a certified preorder carrier plus an antisymmetry field.
\end{definition}

\begin{definition}[POSet pattern specification]
\label{def:poset-pattern-specification}
The pattern is preorder relation formation with classifier collapse from bidirectional comparison.
\end{definition}

\begin{definition}[POSet classifier specification]
\label{def:poset-classifier-specification}
The classifier is the preorder carrier classifier.
\end{definition}

\begin{theorem}[Prefix antisymmetry]
\label{thm:poset-prefix-antisymmetry}
For the concrete unary history prefix preorder, if \(h\le k\) and \(k\le h\),
then \(h\) and \(k\) are classified the same by the preorder classifier.
\end{theorem}
\leanvariant{BEDC.Derived.PreorderUp.PreorderPrefixLE\_antisymm\_hsame}
\begin{proof}
Unpack the two prefix witnesses. Substituting the first continuation into the
second gives a loop from \(h\) through the appended forward and backward tails.
Right-unit uniqueness and append-empty inversion force the forward tail to be
\(\emp\), so the first continuation reduces to \(\hsame(h,k)\), which is the
classifier specification.
\end{proof}

\begin{theorem}[Prefix classifier exactness for concrete POSet]
\label{thm:poset-prefix-classifier-exactness}
For carrier histories $h,k$ in $\mathsf{PreorderCarrier}$, the carrier-aware
classifier relation
$\mathsf{PreorderCarrierClassifierSpec}(\mathsf{PreorderCarrier})(h,k)$ holds
exactly when $\mathsf{PreorderPrefixLE}(h,k)$ and
$\mathsf{PreorderPrefixLE}(k,h)$ both hold.
\end{theorem}
\begin{proof}
Assume first that the carrier-aware classifier holds. Its equality content is
$\hsame(h,k)$, and symmetry gives $\hsame(k,h)$. Applying
\autoref{thm:preorder-prefix-le-of-hsame} in the two orientations gives
$\mathsf{PreorderPrefixLE}(h,k)$ and $\mathsf{PreorderPrefixLE}(k,h)$.

Conversely, assume both prefix comparisons and keep the carrier hypotheses for
$h$ and $k$. By \autoref{thm:poset-prefix-antisymmetry}, the opposing
comparisons collapse to $\hsame(h,k)$. Combining this equality content with the
two carrier witnesses reassembles
$\mathsf{PreorderCarrierClassifierSpec}(\mathsf{PreorderCarrier})(h,k)$.
\end{proof}

\begin{theorem}[Prefix interval classifier collapse for concrete POSet]
\label{thm:poset-prefix-interval-classifier-collapse}
For carrier histories $h,k,r$ in $\mathsf{PreorderCarrier}$, if
$\mathsf{PreorderPrefixLE}(h,k)$, $\mathsf{PreorderPrefixLE}(k,r)$, and
$\mathsf{PreorderCarrierClassifierSpec}(\mathsf{PreorderCarrier})(h,r)$ hold,
then both
$\mathsf{PreorderCarrierClassifierSpec}(\mathsf{PreorderCarrier})(h,k)$ and
$\mathsf{PreorderCarrierClassifierSpec}(\mathsf{PreorderCarrier})(k,r)$ hold.

The per-object fields are as follows: the traditional target is interval
collapse for order-convex consumers; the BEDC source is the unary
$\mathsf{BHist}$ prefix order; the carrier is $\mathsf{PreorderCarrier}$; the
classifier is $\mathsf{PreorderCarrierClassifierSpec}$; stability comes from
prefix transitivity and antisymmetry; the ledger records the chains
$h\le k\le r$ and $r\le h\le k$; the core theorem is this interval classifier
collapse; exactness is inherited from
\autoref{thm:poset-prefix-classifier-exactness}; the standard bridge is the
finite unary prefix order; and the status is $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Unpack the endpoint classifier
$\mathsf{PreorderCarrierClassifierSpec}(\mathsf{PreorderCarrier})(h,r)$. It
provides carrier data for $h$ and $r$ together with $\hsame(h,r)$. The displayed
comparison $\mathsf{PreorderPrefixLE}(h,k)$ and
\autoref{thm:preorder-prefix-preserves-carrier} give the carrier witness for
$k$.

Symmetry of $\hsame(h,r)$ and \autoref{thm:preorder-prefix-le-of-hsame} give
$\mathsf{PreorderPrefixLE}(r,h)$. Compose this with
$\mathsf{PreorderPrefixLE}(k,r)$ by
\autoref{thm:preorder-prefix-transitivity} to obtain
$\mathsf{PreorderPrefixLE}(k,h)$. Now
\autoref{thm:poset-prefix-antisymmetry} applies to
$\mathsf{PreorderPrefixLE}(h,k)$ and $\mathsf{PreorderPrefixLE}(k,h)$, yielding
$\hsame(h,k)$.

For the right subinterval, compose
$\mathsf{PreorderPrefixLE}(r,h)$ with the displayed comparison
$\mathsf{PreorderPrefixLE}(h,k)$ to get $\mathsf{PreorderPrefixLE}(r,k)$.
$\mathsf{PreorderPrefixLE}(k,r)$ is already available, so
\autoref{thm:poset-prefix-antisymmetry} gives $\hsame(k,r)$.

Finally repackage the two classifier witnesses. Combine the carrier witnesses
for $h$ and $k$ with $\hsame(h,k)$ to obtain
$\mathsf{PreorderCarrierClassifierSpec}(\mathsf{PreorderCarrier})(h,k)$, and
combine the carrier witnesses for $k$ and $r$ with $\hsame(k,r)$ to obtain
$\mathsf{PreorderCarrierClassifierSpec}(\mathsf{PreorderCarrier})(k,r)$.
\end{proof}

Thus \autoref{thm:poset-prefix-classifier-exactness} supplies the exactness
predicate named in item 9 of
\autoref{def:concrete-unary-prefix-poset-instance}.

\begin{theorem}[Concrete $\POSetUp$ naming certificate]
\label{thm:concrete-unary-prefix-poset-namecert}
The tuple of \autoref{def:concrete-unary-prefix-poset-instance} supplies the
source, pattern, classifier, stability, and ledger data of a concrete
$\NameCert$ licensing $\POSetUp$ over
$\mathsf{PreorderCarrier}=\mathsf{UnaryHistory}$, classifier
$\mathsf{PreorderCarrierClassifierSpec}(\mathsf{PreorderCarrier})$, and
relation $\mathsf{PreorderPrefixLE}$.
\end{theorem}
\begin{proof}
The carrier, preorder, relation-congruence, and antisymmetry fields are exactly
the fields certified in \autoref{thm:concrete-unary-prefix-poset-laws}. They
identify the carrier with unary histories, use the carrier-aware preorder
classifier, and close the partial-order law by prefix antisymmetry.

The classifier exactness field is
\autoref{thm:poset-prefix-classifier-exactness}. It identifies the carrier
classifier with the two opposing prefix comparisons required by the concrete
partial-order presentation.

The ledger component records the two comparison witnesses, as specified by
\autoref{def:poset-ledger-policy}. Repacking these source, pattern,
classifier, stability, and ledger components gives the displayed concrete
$\NameCert$ for $\POSetUp$.
\end{proof}

This theorem fits the per-object template as follows: the traditional target is
the finite unary-word prefix partial order; the BEDC source is $\mathsf{BHist}$
restricted to unary histories; the carrier is
$\mathsf{PreorderCarrier}=\mathsf{UnaryHistory}$; the classifier is
$\mathsf{PreorderCarrierClassifierSpec}(\mathsf{PreorderCarrier})$; the
stability obligations are the concrete preorder laws, classifier congruence,
and prefix antisymmetry; the ledger records the two comparison-tail witnesses;
the core theorem is \autoref{thm:concrete-unary-prefix-poset-laws}; the
exactness predicate is \autoref{thm:poset-prefix-classifier-exactness}; the
standard bridge is the finite unary-word initial-segment order; and the status
is $\mathsf{ProofTarget}$. This concrete tuple is the schema instance consumed
by the POSet certificate, so Lean can target the BHist prefix construction
rather than an abstract relation schema.

\begin{theorem}[POSet public prefix closure boundary]
\label{thm:poset-public-prefix-closure-boundary}
For the concrete unary-prefix POSet tuple of
\autoref{def:concrete-unary-prefix-poset-instance}, the public surface is
exactly the following $\mathsf{BHist}$-anchored data:
\begin{enumerate}
  \item carrier $\mathsf{PreorderCarrier}=\mathsf{UnaryHistory}$;
  \item classifier
  $\mathsf{PreorderCarrierClassifierSpec}(\mathsf{PreorderCarrier})$;
  \item relation $\mathsf{PreorderPrefixLE}$.
\end{enumerate}
Its preorder laws, endpoint congruence, antisymmetry, exactness, and ledger
rows are supplied by
\autoref{thm:concrete-unary-prefix-poset-laws},
\autoref{thm:poset-prefix-classifier-exactness}, and
\autoref{thm:concrete-unary-prefix-poset-namecert}. The package introduces no
abstract carrier, abstract classifier, quotient, host equality, or
parameterized order relation.
\end{theorem}
\begin{proof}
Unfold the tuple of
\autoref{def:concrete-unary-prefix-poset-instance}. Its carrier coordinate is
$\mathsf{PreorderCarrier}$, which is identified there with
$\mathsf{UnaryHistory}$ inside $\mathsf{BHist}$; its classifier coordinate is
$\mathsf{PreorderCarrierClassifierSpec}(\mathsf{PreorderCarrier})$; and its
relation coordinate is $\mathsf{PreorderPrefixLE}$.

The stability rows come from
\autoref{thm:concrete-unary-prefix-poset-laws}. That theorem supplies the
preorder fields for $\mathsf{PreorderPrefixLE}$, endpoint congruence for the
carrier-aware classifier, and the antisymmetry collapse under that classifier.

The bidirectional-collapse row is
\autoref{thm:poset-prefix-classifier-exactness}. It states that the
carrier-aware classifier is exact for the two opposing prefix comparisons, so
the public exactness field uses no quotient or external equality relation.

Finally, \autoref{thm:concrete-unary-prefix-poset-namecert} repacks the source,
pattern, classifier, stability, and ledger rows into the concrete $\NameCert$.
Since each repacked row names the same carrier, classifier, and prefix
relation, the public $\POSetUp$ surface is anchored in $\mathsf{BHist}$ rather
than in an abstract carrier or parameterized order schema.
\end{proof}

\begin{definition}[POSet stability certificate]
\label{def:poset-stability-certificate}
The stability certificate consists of:
\begin{enumerate}
  \item all preorder stability fields
  \item antisymmetry: \(a\le b\) and \(b\le a\) imply \(a\sim_C b\)
  \item compatibility of antisymmetry with relation congruence
\end{enumerate}
\end{definition}

\begin{definition}[POSet ledger policy]
\label{def:poset-ledger-policy}
Record both comparison derivations when antisymmetry is used.
\end{definition}

\begin{definition}[\(\POSetUp\) certificate obligations]
\label{def:poset-certificate-obligations}
The carrier, source, pattern, classifier, stability, and ledger data listed above are the obligations required for the interface name \(\POSetUp\).
\end{definition}

\paragraph{Primitive boundary.}
\(\POSetUp\) is not a BEDC primitive. It is admitted only when those obligations are supplied.

\begin{proposition}[$\POSetUp$ forgets to $\PreorderUp$ certificate]
\label{prop:poset-forgets-preorder-certificate}
Let $C$ carry a $\POSetUp(C)$ certificate with classifier $\sim_C$ and relation $\le_C$. If the antisymmetry field and the compatibility data whose only role is to transport that antisymmetry field across relation congruence are omitted, the retained carrier, source, pattern, classifier, preorder stability fields, and ledger form a $\PreorderUp(C)$ certificate. The carrier $C$, classifier $\sim_C$, and relation $\le_C$ are unchanged.
\end{proposition}
\begin{proof}
By \autoref{def:poset-certificate-obligations}, the given certificate supplies carrier, source, pattern, classifier, stability, and ledger data for $\POSetUp$. The construction keeps exactly the coordinates that do not require the partial-order antisymmetry clause.

By \autoref{def:poset-carrier}, a $\POSetUp$ carrier is a preorder setup together with antisymmetry under the carrier classifier. Removing the antisymmetry coordinate leaves the carrier, classifier, and relation data required by \autoref{def:preorder-carrier}. Hence $C$, $\sim_C$, and $\le_C$ are copied without replacement.

By \autoref{def:poset-source-specification}, the source is a certified preorder carrier plus an antisymmetry field. Omitting only the antisymmetry field leaves the source described in \autoref{def:preorder-source-specification}. By \autoref{def:poset-classifier-specification}, the classifier is the preorder carrier classifier, so it is exactly the classifier specified in \autoref{def:preorder-classifier-specification}.

By \autoref{def:poset-pattern-specification}, the pattern is preorder relation formation together with classifier collapse from bidirectional comparison. Retaining the one-direction judgment formation $a\le_C b$ and suppressing only the bidirectional-collapse rule leaves the relational judgment formation of \autoref{def:preorder-pattern-specification}.

By \autoref{def:poset-stability-certificate}, the first stability component is all preorder stability fields. These are precisely the classifier reflexivity, classifier transitivity, preorder reflexivity, preorder transitivity, and relation-congruence rows listed in \autoref{def:preorder-stability-certificate}. The remaining $\POSetUp$ rows, namely antisymmetry and compatibility of antisymmetry with relation congruence, are not obligations for $\PreorderUp$. Relation congruence itself is retained because it belongs to the preorder stability component.

Finally, \autoref{def:preorder-ledger-policy} asks for the compared carrier witnesses and the relation derivation. The $\POSetUp$ ledger of \autoref{def:poset-ledger-policy} adds the paired comparison record only when antisymmetry is used. With that antisymmetry use omitted, the remaining comparison provenance is the preorder ledger for the same relation $\le_C$.

The retained carrier, source, pattern, classifier, stability, and ledger fields are therefore exactly the obligations named in \autoref{def:preorder-certificate-obligations}, giving a $\PreorderUp(C)$ certificate with unchanged carrier, classifier, and relation.
\end{proof}

\begin{definition}[Preorder quotient presentation]
\label{def:preorder-quotient-presentation}
Let $(C,\sim_C,\le_C)$ be a $\PreorderUp$ package satisfying the carrier and stability fields of \autoref{def:preorder-carrier} and \autoref{def:preorder-stability-certificate}. On carried representatives define
$$
x\approx_C y :\Longleftrightarrow x\le_C y\land y\le_C x.
$$
Let $q:C\to C_{\!\approx}$ be the class map for $\approx_C$, and write $\overline{x}:=q(x)$. The quotient carrier is $C_{\!\approx}$. Its induced order and quotient classifier are
$$
\overline{x}\le_Q\overline{y} :\Longleftrightarrow x\le_C y,
\qquad
\overline{x}\sim_Q\overline{y} :\Longleftrightarrow x\approx_C y.
$$
\end{definition}

\begin{theorem}[Preorder quotient yields $\PosetUp$]
\label{thm:preorder-quotient-yields-poset}
Let $(C,\sim_C,\le_C)$ supply the carrier, classifier, relation, stability, and ledger fields of a $\PreorderUp$ certificate as in \autoref{def:preorder-certificate-obligations}. For the quotient presentation of \autoref{def:preorder-quotient-presentation}, $\approx_C$ is an equivalence relation on carried representatives, the induced order $\le_Q$ is independent of representatives, and $(C_{\!\approx},\sim_Q,\le_Q)$ supplies the $\POSetUp$ certificate obligations for the quotient partial order. Its classifier is exact on classes:
$$
\overline{x}\sim_Q\overline{y}
\Longleftrightarrow
x\approx_C y
\Longleftrightarrow
(x\le_C y\land y\le_C x).
$$
\end{theorem}
\begin{proof}
The preorder stability fields in \autoref{def:preorder-stability-certificate} give reflexivity and transitivity of $\le_C$ on carried representatives. Reflexivity of $\approx_C$ follows by pairing two copies of $x\le_C x$. Symmetry follows by exchanging the two comparison witnesses. For transitivity, assume $x\approx_C y$ and $y\approx_C z$. The forward comparison $x\le_C z$ is obtained by composing $x\le_C y$ with $y\le_C z$, and the reverse comparison $z\le_C x$ is obtained by composing $z\le_C y$ with $y\le_C x$. Hence $\approx_C$ is an equivalence relation.

The induced order is representative-independent. Suppose $x\approx_C x'$, $y\approx_C y'$, and $x\le_C y$. The reverse half of $x\approx_C x'$ gives $x'\le_C x$, so transitivity gives $x'\le_C y$. The forward half of $y\approx_C y'$ gives $y\le_C y'$, so another use of transitivity gives $x'\le_C y'$. Thus $\overline{x}\le_Q\overline{y}$ implies $\overline{x'}\le_Q\overline{y'}$ under replacement of representatives. Taking one representative fixed gives the two one-sided relation-congruence rows.

The quotient classifier is also representative-independent because $\approx_C$ is an equivalence relation: from $x\approx_C x'$ and $y\approx_C y'$, transitivity and symmetry give $x\approx_C y$ exactly when $x'\approx_C y'$. Therefore $\sim_Q$ is a well-defined classifier on $C_{\!\approx}$, and unfolding its definition gives the displayed exactness equivalence.

The $\POSetUp$ laws follow directly. Reflexivity of $\le_Q$ is the reflexivity of $\le_C$ on any representative. Transitivity of $\le_Q$ is the transitivity of $\le_C$ after unfolding the two induced comparisons. For antisymmetry, if $\overline{x}\le_Q\overline{y}$ and $\overline{y}\le_Q\overline{x}$, then $x\le_C y$ and $y\le_C x$, hence $x\approx_C y$, and therefore $\overline{x}\sim_Q\overline{y}$ by the quotient classifier.

The source row is the quotient carrier $C_{\!\approx}$, the pattern row is $\le_Q$ as in \autoref{def:poset-pattern-specification}, and the classifier row is $\sim_Q$ as in \autoref{def:poset-classifier-specification}. The stability rows required by \autoref{def:poset-stability-certificate} are the quotient reflexivity, transitivity, relation congruence, and antisymmetry just proved. The ledger of \autoref{def:poset-ledger-policy} records the two representatives and, when antisymmetry is invoked, the two induced comparisons. These are exactly the fields listed in \autoref{def:poset-certificate-obligations}, so the quotient presentation supplies the stated $\POSetUp$ certificate and hence the $\PosetUp$ quotient presentation.
\end{proof}

\begin{definition}[Opposite POSet certificate data]
\label{def:opposite-poset-certificate-data}
Let a $\POSetUp$ certificate be presented by a carrier predicate $\mathcal C$, classifier $\sim_C$, and relation $\le_C$. Define the opposite relation by
$$
a\le_C^{\mathrm{op}} b:=b\le_C a.
$$
The opposite POSet data $C^{\mathrm{op}}$ have the same carrier predicate $\mathcal C$, the same classifier $\sim_C$, and relation $\le_C^{\mathrm{op}}$. An antisymmetry ledger for $a\le_C^{\mathrm{op}} b$ and $b\le_C^{\mathrm{op}} a$ is the same pair of original comparison derivations, read as $b\le_C a$ and $a\le_C b$.
\end{definition}

\begin{theorem}[Opposite POSet certificate]
\label{thm:opposite-poset-certificate}
If $(\mathcal C,\sim_C,\le_C)$ satisfies the $\POSetUp$ certificate obligations of \autoref{def:poset-certificate-obligations}, then the data of \autoref{def:opposite-poset-certificate-data} also satisfy the $\POSetUp$ certificate obligations. Thus the same carrier and classifier carry a POSet certificate with relation $\le_C^{\mathrm{op}}$.
\end{theorem}
\begin{proof}
Unpack the given package through \autoref{def:poset-certificate-obligations}. By \autoref{prop:poset-forgets-preorder-certificate}, it has a $\PreorderUp$ reduct with the same carrier predicate $\mathcal C$, classifier $\sim_C$, and relation $\le_C$. Applying \autoref{thm:opposite-preorder-certificate} to that reduct supplies the carrier, source, preorder pattern, classifier, preorder stability rows, and preorder ledger for the relation $\le_C^{\mathrm{op}}$ of \autoref{def:opposite-poset-certificate-data}.

It remains to supply the partial-order rows of \autoref{def:poset-stability-certificate}. Let $a$ and $b$ be carried endpoints and suppose
$$
a\le_C^{\mathrm{op}} b
\qquad\text{and}\qquad
b\le_C^{\mathrm{op}} a.
$$
Unfolding the opposite relation gives $b\le_C a$ and $a\le_C b$. Applying the original antisymmetry field to the ordered pair $a\le_C b$ and $b\le_C a$ yields $a\sim_C b$, which is exactly the antisymmetry conclusion required for $\le_C^{\mathrm{op}}$.

The compatibility of antisymmetry with relation congruence is inherited in the same exchange of endpoints. Given classifier replacements $a\sim_C a'$ and $b\sim_C b'$, the opposite preorder relation-congruence row transports the two opposite comparisons to $a'\le_C^{\mathrm{op}} b'$ and $b'\le_C^{\mathrm{op}} a'$. Unfolding gives $b'\le_C a'$ and $a'\le_C b'$, and the original antisymmetry compatibility row applies to these transported original comparisons to produce $a'\sim_C b'$.

The source row of \autoref{def:poset-source-specification} is therefore a certified preorder carrier plus the antisymmetry field just proved. The pattern row of \autoref{def:poset-pattern-specification} is still preorder relation formation with classifier collapse from bidirectional comparison: a displayed pair of opposite comparisons is read as the same original pair with the two comparison derivations exchanged. The classifier row of \autoref{def:poset-classifier-specification} is unchanged because endpoint classification remains $\sim_C$.

Finally, the ledger row is \autoref{def:poset-ledger-policy}. It records both comparison derivations used by antisymmetry; under \autoref{def:opposite-poset-certificate-data}, those are exactly the original derivations $b\le_C a$ and $a\le_C b$ with the displayed slots exchanged. Hence the carrier, source, pattern, classifier, stability, and ledger rows satisfy \autoref{def:poset-certificate-obligations}, so $C^{\mathrm{op}}$ carries a $\POSetUp$ certificate.
\end{proof}

\begin{theorem}[Double opposite POSet certificate data identity]
\label{thm:double-opposite-poset-certificate-data-identity}
Let $D$ be a $\POSetUp$ certificate satisfying the obligations of
\autoref{def:poset-certificate-obligations}. Present its POSet data by a
carrier predicate $\mathcal C$, classifier $\sim_C$, order relation $\le_C$,
and antisymmetry ledger row $\mathcal L_C$ as in
\autoref{def:poset-ledger-policy}. Let
$D^{\mathrm{opop}}=(D^{\mathrm{op}})^{\mathrm{op}}$ be obtained by applying
\autoref{def:opposite-poset-certificate-data} twice. Then
$D^{\mathrm{opop}}$ is a $\POSetUp$ certificate, and its carrier predicate,
classifier, order relation, and antisymmetry ledger row are componentwise
identical to those of $D$: for carried endpoints $a,b$ and comparison
witnesses $p:a\le_C b$ and $q:b\le_C a$,
$$
\begin{aligned}
\mathcal C^{\mathrm{opop}}(a)&=\mathcal C(a),&
a\sim_C^{\mathrm{opop}}b&=a\sim_C b,\\
a\le_C^{\mathrm{opop}}b&=a\le_C b,&
\mathcal L_C^{\mathrm{opop}}(a,b;p,q)&=\mathcal L_C(a,b;p,q).
\end{aligned}
$$
Here $\mathcal L_C(a,b;p,q)$ denotes the ledger entry that records the
ordered antisymmetry pair $p,q$.
\end{theorem}
\begin{proof}
Because $D$ satisfies the $\POSetUp$ obligations,
\autoref{thm:opposite-poset-certificate} makes $D^{\mathrm{op}}$ a certified
$\POSetUp$ instance. Applying the same theorem to that certified opposite data
makes $D^{\mathrm{opop}}$ a certified $\POSetUp$ instance.

It remains to compare the displayed data. For the carrier predicate and
classifier, two unfoldings of \autoref{def:opposite-poset-certificate-data}
give
$$
\mathcal C^{\mathrm{opop}}(a)=\mathcal C^{\mathrm{op}}(a)=\mathcal C(a)
$$
and
$$
a\sim_C^{\mathrm{opop}}b=a\sim_C^{\mathrm{op}}b=a\sim_C b.
$$

For the order relation, the two reversals cancel:
$$
a\le_C^{\mathrm{opop}}b
=b\le_C^{\mathrm{op}}a
=a\le_C b.
$$
Thus the double opposite order field is exactly the original order field.

For the ledger row, \autoref{def:opposite-poset-certificate-data} says one
opposite keeps the same two antisymmetry derivations and exchanges their
displayed source and target slots. Hence the first opposite reads the ordered
ledger entry for $(a,b;p,q)$ in the exchanged slots, and the second opposite
performs the same exchange again. The endpoints and the two comparison slots
are restored to $(a,b;p,q)$, so
$\mathcal L_C^{\mathrm{opop}}(a,b;p,q)=\mathcal L_C(a,b;p,q)$.

The certified status and the four displayed component identities therefore
identify the double opposite POSet data with the original POSet data.
\end{proof}

\begin{theorem}[POSet finite-chain carrier closure]
\label{thm:poset-finite-chain-carrier-closure}
Let
$$
h=h_0,\ h_1,\ldots,h_n=k
$$
be a finite chain of carried unary-prefix histories in the concrete
$\POSetUp$ source of \autoref{def:concrete-unary-prefix-poset-instance}. If
each adjacent comparison $h_i\leq h_{i+1}$ is recorded by
$\mathsf{PreorderPrefixLE}$ in the finite $\Cont$ ledger, then every endpoint
$h_i$ in the chain remains inside
$\mathsf{PreorderCarrier}=\mathsf{UnaryHistory}$, and the composite comparison
from $h$ to $k$ is again a carried unary-prefix comparison recorded by that
finite ledger.
\end{theorem}
\begin{proof}
Argue by induction on the finite chain. The base chain has only the carried
endpoint $h$, so carrier closure is immediate from the displayed source row.

For the step, assume the prefix subchain from $h$ to $h_i$ has already stayed
inside $\mathsf{UnaryHistory}$ and has produced a carried composite comparison.
The adjacent row $h_i\leq h_{i+1}$ is a displayed
$\mathsf{PreorderPrefixLE}$ comparison between carried histories, so the
prefix transitivity row cited in
\autoref{thm:concrete-unary-prefix-poset-namecert} composes it with the
subchain comparison.

The same finite $\Cont$ ledger records the old subchain, the new adjacent
comparison, and their composite. Thus the next endpoint remains in
$\mathsf{PreorderCarrier}$ and the chain closes inside the concrete unary
prefix carrier.
\end{proof}

\begin{theorem}[POSet finite-chain antisymmetry collapse]
\label{thm:poset-finite-chain-antisymmetry-collapse}
Let $h$ and $k$ be carried unary-prefix histories. If the finite $\Cont$ ledger
records a finite $\mathsf{PreorderPrefixLE}$ chain from $h$ to $k$ and another
finite $\mathsf{PreorderPrefixLE}$ chain from $k$ to $h$, then the two endpoints
collapse to the carrier classifier:
$$
\mathsf{PreorderCarrier}(h)\wedge\mathsf{PreorderCarrier}(k)
\wedge h\leq k\wedge k\leq h
\Longrightarrow \hsame(h,k).
$$
\end{theorem}
\begin{proof}
First apply \autoref{thm:poset-finite-chain-carrier-closure} to the chain from
$h$ to $k$. It gives a carried composite comparison $h\leq k$ recorded by the
finite ledger.

Apply the same closure theorem to the reverse chain from $k$ to $h$. This
gives the carried composite comparison $k\leq h$ over the same unary-prefix
carrier.

Now apply \autoref{thm:poset-prefix-antisymmetry} to the opposing composite
prefix comparisons. The result is $\hsame(h,k)$, and
\autoref{thm:poset-prefix-classifier-exactness} identifies this history
sameness with the carrier classifier on the displayed unary-prefix endpoints.
\end{proof}

\begin{theorem}[POSet finite-chain NameCert surface]
\label{thm:poset-finite-chain-namecert-surface}
The finite-chain carrier closure of
\autoref{thm:poset-finite-chain-carrier-closure} and the finite-chain
antisymmetry collapse of
\autoref{thm:poset-finite-chain-antisymmetry-collapse} package as a
$\POSetUp$ $\NameCert$ obligation surface. Its rows are exactly the concrete
unary-prefix carrier, the $\hsame$ classifier, the
$\mathsf{PreorderPrefixLE}$ relation, prefix stability under finite
composition, the finite $\Cont$ ledger for adjacent and composite comparisons,
and the exactness row of
\autoref{thm:poset-prefix-classifier-exactness}. It adds no quotient carrier,
opposite presentation, totality field, lattice operation, or standard bridge.
\end{theorem}
\begin{proof}
Project the concrete unary-prefix certificate of
\autoref{thm:concrete-unary-prefix-poset-namecert}. It supplies the carrier,
classifier, relation, stability, ledger, and exactness rows for the
$\POSetUp$ surface.

The finite-chain carrier row is supplied by
\autoref{thm:poset-finite-chain-carrier-closure}. Its induction uses only
prefix transitivity and finite $\Cont$ composition already present in the
certificate ledger.

The finite-chain antisymmetry row is supplied by
\autoref{thm:poset-finite-chain-antisymmetry-collapse}. It reduces two finite
chains to opposing composite prefix comparisons, applies
\autoref{thm:poset-prefix-antisymmetry}, and then reads the result through
\autoref{thm:poset-prefix-classifier-exactness}.

Repacking these rows with the source certificate gives a single
$\POSetUp$ NameCert obligation surface. Since every row is drawn from the
concrete unary-prefix packet and its finite ledger, no additional order
structure or bridge datum is exported.
\end{proof}
