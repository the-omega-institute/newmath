\chapter{A Concrete Naming Certificate for $\BishopRealNormalizerUp$}
\label{ch:concrete-instances-bishop-real-normalizer-namecert}
\origin{human}

Following Bishop and Bridges' constructive real-number presentation, the $\BishopRealNormalizerUp$ packet records the finite boundary where an arbitrary regular Cauchy rational name is normalized before a terminal $\RealUp$ seal may be read. It sits over the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the completion boundary of \autoref{ch:concrete-instances-bishop-regular-cauchy-completion-namecert}, the effective comparison packet of \autoref{ch:concrete-instances-grzegorczyk-real-namecert}, and the modulus-neutral seal discipline of \autoref{ch:concrete-instances-regular-cauchy-modulus-independence-namecert}. The packet names a canonical normalization route for L10 Real and Completion consumers; it is not quotient stream equality, host real equality, a selected limit, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.

\begin{definition}[Bishop real normalizer carrier]
\label{def:bishop-real-normalizer-carrier}
A $\BishopRealNormalizerUp$ carrier is a finite $\BHist$ packet
$$
Z=(S,M,D,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source regular Cauchy name row, exposing the rational approximants and finite precision requests before normalization.
\item $M$ is the monotone normalized tail-modulus row, recording the displayed tail schedule whose requested indices are nondecreasing and whose tolerance budget refines the source requests.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, storing dyadic radii, slack comparisons, and finite error budgets consumed by the source and normalized rows.
\item $Q$ is the normalized $\RegSeqRatUp$ output row obtained from $S$ through $M$ and $D$.
\item $E$ is the terminal $\RealUp$ seal row reached only after the source name, monotone tail modulus, dyadic tolerance ledger, and normalized regular output have been displayed.
\item $H$ records componentwise $\hsame$ transport for $S,M,D,Q,E$ and for the replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the normalization route
$$
S\longmapsto M\longmapsto D\longmapsto Q\longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\BishopRealNormalizerUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BishopRegularCauchyCompletionUp$, $\GrzegorczykRealUp$, $\RegularCauchyModulusIndependenceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopRealNormalizerUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,M,D,Q,E,H,C,P,N$. It has no coordinate for quotient stream equality, host real equality, selected limits, countable choice, ambient $\RealUp$ completeness, Lean evidence, or a standard bridge.
\end{definition}

\begin{theorem}[Bishop real normalizer NameCert obligations]
\label{thm:bishop-real-normalizer-namecert-obligations}
For every accepted $\BishopRealNormalizerUp$ carrier
$$
Z=(S,M,D,Q,E,H,C,P,N),
$$
the local $\NameCert_{\BishopRealNormalizerUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the source regular Cauchy name, monotone normalized tail modulus, dyadic tolerance ledger, normalized $\RegSeqRatUp$ output, terminal $\RealUp$ seal, and structural rows $H,C,P,N$;
\item source exposure, requiring every normalization request to begin with the displayed source row $S$ rather than with a detached stream representative;
\item monotone tail-modulus normalization, requiring $M$ to present a nondecreasing finite tail schedule whose tolerance budget refines the requests exported by $S$;
\item dyadic accounting, requiring $D$ to store exactly the dyadic radii and slack comparisons used to justify the normalized output row $Q$;
\item regular-output handoff, requiring $Q$ to be the only route by which the normalized finite windows become a $\RegSeqRatUp$ real name;
\item Real-seal factorization, requiring every downstream consumer of $E$ to pass through $S,M,D,Q$ and the structural rows before reading the terminal seal;
\item transport, replay, and provenance non-escape, requiring $H,C,P,N$ to preserve and name only the displayed normalization rows, with no quotient stream equality, host real equality, selected limit, countable-choice schedule, ambient $\RealUp$ completeness, Lean verification, or bridge credential.
\end{enumerate}
No Bishop real normalizer obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the accepted packet $Z=(S,M,D,Q,E,H,C,P,N)$. Carrier admission exposes exactly the source regular name, monotone tail-modulus row, dyadic tolerance ledger, normalized regular output, terminal Real seal, and the four structural rows.

The source exposure clause is first in the route. A normalization request cannot start from an unnamed stream representative, because the only source coordinate in the carrier is the displayed row $S$.

The monotone tail-modulus clause is the row $M$. It records the finite nondecreasing schedule used to choose tails and ties each chosen tail to the source requests that it refines.

Dyadic accounting is then forced by $D$. The ledger stores the radii and slack comparisons consumed when the selected tails are read as the normalized row $Q$, so the regular output has no tolerance data outside the packet.

Finally, the Real seal and structural rows exhaust the boundary. The seal $E$ is downstream of $S,M,D,Q$, while $H,C,P,N$ transport, replay, source, and name only those displayed rows. Hence no excluded quotient, host equality, selected limit, countable-choice object, ambient-completeness principle, Lean evidence, or bridge credential is exported.
\end{proof}

\begin{theorem}[Bishop real normalizer Real-seal factorization]
\label{thm:bishop-real-normalizer-real-seal-factorization}
For every accepted $\BishopRealNormalizerUp$ carrier
$$
Z=(S,M,D,Q,E,H,C,P,N),
$$
every $\RealUp$ consumer of the terminal seal $E$ factors through the displayed normalization route
$$
S\longmapsto M\longmapsto D\longmapsto Q\longmapsto E
$$
together with componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row. In particular, the consumer cannot replace $S$ by quotient stream equality, cannot replace $Q$ by a selected limit, cannot compare host real values, cannot invoke countable choice, and cannot read $E$ as an ambient completeness witness.
\end{theorem}
\begin{proof}
Begin with a seal-facing consumer. Carrier projection exposes $E$ only as the terminal coordinate of the normalized regular-Cauchy route, not as an independently completed host real.

Trace the route backward from $E$. The seal is downstream of $Q$, the normalized $\RegSeqRatUp$ output is downstream of the dyadic tolerance ledger $D$, and the ledger is controlled by the monotone tail schedule $M$ applied to the source row $S$.

The structural coordinates preserve the same route. Componentwise $\hsame$ in $H$ transports the source, modulus, tolerance, output, and seal rows; displayed $\Cont$ in $C$ replays their order; and $P,N$ name the packet without adding a second real-name surface.

Thus every Real-facing use factors through $S,M,D,Q,E,H,C,P,N$. The carrier contains no coordinate from which quotient stream equality, selected limits, host real equality, countable choice, ambient Real completeness, Lean verification, or bridge checking could be read.
\end{proof}

\falsifiablePrediction{Every $\BishopRealNormalizerUp$ Real-facing read factors through a source regular Cauchy row, a monotone normalized tail modulus, a DyadicRatCore tolerance ledger, a normalized RegSeqRat output, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, bishop-regular-cauchy-completion, grzegorczyk-real, regular-cauchy-modulus-independence: $\BishopRealNormalizerUp$ is not bijective to any listed sibling because it jointly carries source exposure, monotone tail-modulus normalization, dyadic tolerance accounting, normalized regular output, terminal Real seal factorization, componentwise transport, replay, provenance, and local naming.}

\closureat{BishopRealNormalizerUp}{seedStr}

\begin{closurestatus}{\BishopRealNormalizerUp}
  \constructivestory{A $\BishopRealNormalizerUp$ packet is generated from a source regular Cauchy name, a monotone normalized tail modulus, DyadicRatCore tolerance rows, a normalized RegSeqRat output, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-real-normalizer-carrier}, \autoref{thm:bishop-real-normalizer-namecert-obligations}, and \autoref{thm:bishop-real-normalizer-real-seal-factorization} seed the canonical normalization boundary for regular-Cauchy real names before a Real or Completion consumer reads the terminal seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, host real equality, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source exposure, monotone tail-modulus normalization, dyadic accounting, regular-output handoff, Real-seal factorization, transport/replay/provenance coverage, and non-escape for BEDC.Derived.BishopRealNormalizerUp.}
\end{closurestatus}
