\chapter{A Concrete Naming Certificate for $\ResearchProgrammeAuditLoopUp$}
\label{ch:concrete-instances-research-programme-audit-loop-namecert}
\origin{ai}

The $\ResearchProgrammeAuditLoopUp$ packet realizes the research-programme route of \autoref{sec:visions-philosophy-research-programme} as a finite audit loop. It sits after the writing discipline of \autoref{ch:concrete-instances-audit-writing-discipline-namecert}, the registry consistency audit of \autoref{ch:concrete-instances-registry-consistency-audit-namecert}, the machine-readable audit interface of \autoref{ch:concrete-instances-machine-readable-audit-interface-namecert}, the package export discipline of \autoref{ch:concrete-instances-package-export-discipline-namecert}, and the programme-strength gradient ledger of \autoref{ch:concrete-instances-programme-strength-gradient-ledger-namecert}. Those packets regulate public prose, registry agreement, machine-readable export, package-level release, and strength reporting; this packet binds them into one loop before a research programme may treat a proposal as accepted programme motion.

\begin{definition}[Research-programme audit-loop carrier]
\label{def:research-programme-audit-loop-carrier}
A $\ResearchProgrammeAuditLoopUp$ carrier is a finite $\BHist$ packet
$$
L=(S,Q,G,F,K,A,X,J,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the phase row, recording the current programme phase and the admitted transition boundary from $\PhaseGovernedCertificateRouteUp$;
\item $Q$ is the proposal-and-revise row, recording proposal intake, review acceptance, refusal, and permitted rewrite routes;
\item $G$ is the gate row, recording the public gate that must be passed before a proposal is read as programme motion;
\item $F$ is the failure-feedback row, recording typed failure, refusal, and retry data without promoting failure into theorem evidence;
\item $K$ is the calibration row, recording how reported strength is read against $\ProgrammeStrengthGradientLedgerUp$;
\item $A$ is the audit-axis row, recording separate theory, verification, bridge, and machine-interface axes;
\item $X$ is the label-claim row, recording claim labels, registry entries, and their consistency checks;
\item $J$ is the trace row, recording machine-readable audit traces and replay data;
\item $T$ records componentwise $\hsame$ transport for all displayed rows;
\item $P$ is the $\Pkg$ provenance over $\ResearchProgrammeAuditLoopUp$, $\AuditWritingDisciplineUp$, $\RegistryConsistencyAuditUp$, $\MachineReadableAuditInterfaceUp$, $\PackageExportDisciplineUp$, $\ProgrammeStrengthGradientLedgerUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\ResearchProgrammeAuditLoopUp}$ row.
\end{enumerate}
The carrier has no coordinate for private programme approval, unregistered label motion, hidden machine evidence, host-side gate passage, or strength promotion outside the displayed audit loop.
\end{definition}

\begin{theorem}[Research-programme audit-loop obligation surface]
\label{thm:research-programme-audit-loop-obligation-surface}
For the carrier of \autoref{def:research-programme-audit-loop-carrier}, the local $\NameCert_{\ResearchProgrammeAuditLoopUp}$ surface consists exactly of phase admission through $S$, proposal and review motion through $Q$, gate passage through $G$, failure-feedback routing through $F$, calibration through $K$, axis separation through $A$, label-claim consistency through $X$, trace readback through $J$, componentwise transport through $T$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the finite packet of \autoref{def:research-programme-audit-loop-carrier}. Its displayed rows are precisely $S,Q,G,F,K,A,X,J,T,P,N$.

The phase, proposal, gate, failure-feedback, calibration, axis, label-claim, and trace rows are the programme-facing obligations. They respectively consume the phase-governed route, review acceptance, writing discipline, failure record, strength gradient, audit axes, registry consistency, and machine-readable audit interface named in the carrier.

The support rows $T,P,N$ transport, source, and locally name the same displayed packet. Since no further coordinate is present, the listed rows exhaust the local $\NameCert_{\ResearchProgrammeAuditLoopUp}$ surface.
\end{proof}

\begin{theorem}[Research-programme audit-loop nonescape]
\label{thm:research-programme-audit-loop-nonescape}
Every accepted $\ResearchProgrammeAuditLoopUp$ loop exposes phase, proposal-and-revise, gate, failure-feedback, calibration, audit-axis, label-claim, trace, provenance, and local $\NameCert$ rows. It cannot export accepted programme motion through private approval, prose emphasis, unregistered labels, hidden trace evidence, collapsed theory-and-verification status, or strength promotion outside those rows.
\end{theorem}
\begin{proof}
Read any accepted loop through the carrier. The rows $S,Q,G,F,K,A,X,J$ determine the programme phase, proposal route, admission gate, failure feedback, calibration, axis separation, label claim, and trace readback before the loop is public.

The writing, registry, interface, export, and strength-gradient packet dependencies named in $P$ supply the sibling surfaces that each row consumes. The transport row $T$ and local naming row $N$ keep the loop on that finite packet.

The excluded readings are not coordinates of the carrier. A consumer that asks for them has left the displayed $\NameCert_{\ResearchProgrammeAuditLoopUp}$ surface rather than reading an accepted research-programme audit loop.
\end{proof}

\falsifiablePrediction{Any accepted ResearchProgrammeAuditLoopUp loop must expose phase, proposal/revise, gate, failure-feedback, calibration, axis, label-claim, trace, provenance, and local NameCert rows.}

\independenceWitness{audit-writing-discipline, registry-consistency-audit, machine-readable-audit-interface, package-export-discipline, programme-strength-gradient-ledger: none is bijective with $\ResearchProgrammeAuditLoopUp$ because the audit loop simultaneously carries phase governance, proposal review, gate passage, failure feedback, calibration, axis separation, label-claim consistency, machine-readable trace, provenance, and local NameCert rows.}

Lean TasteGate formalization must provide $\Nontrivial\ \ResearchProgrammeAuditLoopUp$ and $\FieldFaithful\ \ResearchProgrammeAuditLoopUp$ instances in \texttt{lean4/BEDC/Derived/ResearchProgrammeAuditLoopUp/TasteGate.lean}. The target \texttt{BEDC.Derived.ResearchProgrammeAuditLoopUp.taste\_gate} must preserve $S,Q,G,F,K,A,X,J,T,P,N$ as distinct rows before a checked paper marker may cite it.

\closureat{ResearchProgrammeAuditLoopUp}{seedStr}
\closureat{ResearchProgrammeAuditLoopUp}{obligationStr}
