\chapter{A Concrete Naming Certificate for $\CompleteMetricSequentialClosureUp$}
\label{ch:concrete-instances-complete-metric-sequential-closure-namecert}
\origin{human}

Following Bourbaki and Kelley on complete metric spaces and Bishop--Bridges on explicit Cauchy convergence, the $\CompleteMetricSequentialClosureUp$ packet records the finite route by which displayed complete-metric Cauchy data may be consumed as $\SequentialClosureUp$ evidence. It sits after the metric-space surface of \autoref{ch:concrete-instances-metric-space-namecert}, the Cauchy witness rows of \autoref{ch:concrete-instances-cauchyfilter-namecert}, the finite sequence windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the sequential-closure packet of \autoref{ch:concrete-instances-sequentialclosure-namecert}. The chapter names a finite bridge from complete metric convergence data to sequential closure; it is not host completeness, quotient stream equality, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Complete metric sequential-closure carrier]
\label{def:complete-metric-sequential-closure-carrier}
A $\CompleteMetricSequentialClosureUp$ carrier is a finite $\BHist$ packet
$$
G=(M,C,S,Q,L,W,R,E,J,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the source $\MetricSpaceUp$ row, including the visible distance, tolerance, and neighbourhood comparison data.
\item $C$ is the complete-metric Cauchy row, naming the Cauchy requests, finite modulus windows, and convergence-readiness data admitted by the source metric presentation.
\item $S$ is the displayed subset or source row whose admitted sequence entries form the sequential-closure source.
\item $Q$ is the $\StreamNameUp$ sequence row, carrying the finite observation windows consumed by closure requests.
\item $L$ is the candidate limit row supplied by the complete-metric Cauchy data, without asserting host completion or quotient equality.
\item $W$ is the finite window ledger aligning Cauchy requests in $C$ with sequence entries in $Q$.
\item $R$ is the $\RegSeqRatUp$ readback row for tolerance and distance data visible in $M,C,Q,W$.
\item $E$ is the $\RealUp$ seal row reached only after the regular readback row $R$ is displayed.
\item $J$ is the $\SequentialClosureUp$ evidence row obtained from $S,Q,L,W,R,E$.
\item $H$ records componentwise $\hsame$ transport for the metric, Cauchy, source, sequence, limit, window, readback, Real seal, sequential-closure evidence, replay, provenance, and local naming rows.
\item $K$ records displayed $\Cont$ replay from a closure consumer through $M,C,S,Q,L,W,R,E,J,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CompleteMetricSequentialClosureUp$, $\MetricSpaceUp$, $\CauchyFilterUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\SequentialClosureUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompleteMetricSequentialClosureUp}$ row.
\end{enumerate}
The carrier has no coordinate for host completeness of the metric space, choice of arbitrary subsequences, quotient stream equality, open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Complete metric sequential-closure NameCert obligations]
\label{thm:complete-metric-sequential-closure-namecert-obligations}
For every accepted $\CompleteMetricSequentialClosureUp$ carrier $G=(M,C,S,Q,L,W,R,E,J,H,K,P,N)$, the local $\NameCert_{\CompleteMetricSequentialClosureUp}$ surface consists exactly of carrier admission, complete-metric Cauchy exposure, sequence-window coverage, limit-row alignment, RegSeqRat readback, Real seal admission, $\SequentialClosureUp$ handoff, componentwise transport, displayed replay, package provenance, local naming, and nonescape from host completeness, choice, quotient stream equality, and compactness.
\end{theorem}
\begin{proof}
Project the carrier. Rows $M,C,S,Q,L,W$ expose the metric presentation, complete-metric Cauchy data, source row, sequence windows, candidate limit, and window alignment ledger.

The analytic handoff is downstream of those rows. The readback row $R$ records the regular rational tolerance data, the Real row $E$ seals the accepted distance comparison, and the sequential-closure row $J$ repackages $S,Q,L,W,R,E$ as $\SequentialClosureUp$ evidence.

The structural rows $H,K,P,N$ then transport, replay, source, and locally name the same finite route. Since the carrier includes no host completeness, selected subsequence, quotient stream equality, open-cover compactness, or ambient Real completeness coordinate, the displayed rows exhaust the NameCert obligation surface.
\end{proof}

\begin{theorem}[Complete metric sequential-closure limit seal]
\label{thm:complete-metric-sequential-closure-limit-seal}
Every closure-facing read of an accepted $\CompleteMetricSequentialClosureUp$ packet factors through
$$
M,C,S,Q,L,W \longmapsto R,E \longmapsto J \longmapsto H,K,P,N .
$$
Thus the candidate limit row is sealed only after the complete-metric Cauchy windows have been aligned with displayed sequence windows, translated through $\RegSeqRatUp$, sealed by $\RealUp$, and repacked as $\SequentialClosureUp$ evidence.
\end{theorem}
\begin{proof}
Start with a closure-facing read of $L$. The carrier admits $L$ beside the metric row $M$, complete-metric Cauchy row $C$, source row $S$, sequence row $Q$, and finite window ledger $W$.

The limit seal is then read through $R$ and $E$. The regular readback row records the tolerance data visible in the metric and Cauchy windows, and the Real seal row certifies the accepted distance comparison only after those windows have been displayed.

Finally the row $J$ repackages the resulting limit evidence as $\SequentialClosureUp$ data. Transport and replay through $H,K,P,N$ preserve this order, so the consumer receives only the displayed finite seal route and not host completeness or a quotient completion.
\end{proof}

\begin{theorem}[Complete metric sequential-closure source nonescape]
\label{thm:complete-metric-sequential-closure-source-nonescape}
An accepted $\CompleteMetricSequentialClosureUp$ packet exports no sequential-closure evidence unless the source row $S$, sequence row $Q$, candidate limit row $L$, finite window ledger $W$, regular readback row $R$, Real seal row $E$, and $\SequentialClosureUp$ row $J$ are all displayed in the same carrier. In particular, complete-metric Cauchy data alone cannot escape as host closure, selected limit, quotient stream equality, or open-cover compactness.
\end{theorem}
\begin{proof}
Read the carrier rows. The complete-metric Cauchy row $C$ is not an exported closure object by itself; it is tied to $S,Q,L,W$ before any Real-facing or closure-facing evidence is available.

Apply the limit-seal route of \autoref{thm:complete-metric-sequential-closure-limit-seal}. It forces every closure read through $R,E,J$ after the metric and sequence windows have been exposed.

The nonescape claim follows from absence of coordinates. There is no row for host closure, selected limits, quotient stream equality, compactness, or ambient completeness, and $H,K,P,N$ only preserve the displayed finite packet. Therefore source data cannot leave the carrier except through the stated sequential-closure route.
\end{proof}

\falsifiablePrediction{Every accepted CompleteMetricSequentialClosureUp closure-facing read factors through a MetricSpace row, complete-metric Cauchy row, source row, StreamName sequence row, candidate limit row, finite window ledger, RegSeqRat readback, Real seal, SequentialClosure evidence row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{metricspace, cauchyfilter, streamname, regseqrat, real, sequentialclosure: $\CompleteMetricSequentialClosureUp$ is not bijective to any listed sibling because it binds complete-metric Cauchy exposure, sequence-window alignment, limit sealing, regular readback, Real sealing, SequentialClosure handoff, transport, replay, provenance, and local naming as one finite certificate.}

\closureat{CompleteMetricSequentialClosureUp}{seedStr}

\begin{closurestatus}{\CompleteMetricSequentialClosureUp}
  \constructivestory{A $\CompleteMetricSequentialClosureUp$ packet is generated from a MetricSpace row, complete-metric Cauchy row, source row, StreamName sequence row, candidate limit row, finite window ledger, RegSeqRat readback, Real seal, SequentialClosure evidence row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:complete-metric-sequential-closure-carrier}, \autoref{thm:complete-metric-sequential-closure-namecert-obligations}, \autoref{thm:complete-metric-sequential-closure-limit-seal}, and \autoref{thm:complete-metric-sequential-closure-source-nonescape} seed the finite route from complete metric Cauchy data to sequential-closure evidence.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host completeness, choice, quotient stream equality, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, complete-metric Cauchy exposure, sequence-window coverage, RegSeqRat readback, Real seal admission, SequentialClosure handoff, transport, replay, provenance, and local naming for BEDC.Derived.CompleteMetricSequentialClosureUp.}
\end{closurestatus}
