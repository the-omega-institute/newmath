\chapter{A Concrete Naming Certificate for $\BishopChoiceBarUp$}
\label{ch:concrete-instances-bishop-choice-bar-namecert}
\origin{human}

Following Bishop and Bridges on constructive choice principles under explicit finite information, the $\BishopChoiceBarUp$ packet records the bar-controlled boundary at which a completion-facing consumer may use inspected finite prefixes and a local selection ledger without acquiring countable choice. It sits on the L10 Real and Completion route beside the finite stream-name interface of \autoref{ch:concrete-instances-streamname-namecert}, the Cauchy-modulus schedule discipline of \autoref{ch:concrete-instances-cauchymodulus-namecert}, the choice-free limit schedule of \autoref{ch:concrete-instances-choice-free-limit-schedule-namecert}, the fan theorem boundary of \autoref{ch:concrete-instances-fantheorem-namecert}, and the lawless sequence prefix surface of \autoref{ch:concrete-instances-lawlesssequence-namecert}. The packet names only the displayed bar coverage, inspected windows, selected-window ledger, and structural rows; it does not claim countable choice, a full fan theorem, $\RealUp$ completeness, quotient stream equality, or selected global limits.

\begin{definition}[Bishop choice bar carrier]
\label{def:bishop-choice-bar-carrier}
A $\BishopChoiceBarUp$ carrier is a finite $\BHist$ packet
$$
B=(S,M,A,W,L,D,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ source row naming the finite prefix windows that the completion-facing request is allowed to inspect.
\item $M$ is the $\CauchyModulusUp$ request row, recording the displayed precision threshold and the finite bound at which the bar is queried.
\item $A$ is the bar-admission row. It records finite bar coverage for the inspected prefix family and cites the fan-boundary sibling only as a displayed finite coverage discipline.
\item $W$ is the selected-window row. It lists the windows actually read after $S,M,A$ have been exposed.
\item $L$ is the selected-window ledger. It records that every selected window in $W$ is justified by a visible bar hit and remains inside the choice-free limit schedule.
\item $D$ is the lawless-prefix compatibility row, ensuring that prefix reads use only displayed finite observations and no global law for the stream.
\item $H$ records componentwise $\hsame$ transport for source, modulus, bar-admission, selected-window, ledger, lawless-prefix, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from completion-facing requests through $S,M,A,W,L,D,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\BishopChoiceBarUp$, $\StreamNameUp$, $\CauchyModulusUp$, $\ChoiceFreeLimitScheduleUp$, $\FanTheoremUp$, $\LawlessSequenceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\BishopChoiceBarUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $S,M,A,W,L,D,H,C,P,N$. It has no coordinate for countable choice, an arbitrary fan theorem, quotient stream equality, selected global limits, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop choice bar NameCert obligations]
\label{thm:bishop-choice-bar-namecert-obligations}
For every accepted $\BishopChoiceBarUp$ carrier $B=(S,M,A,W,L,D,H,C,P,N)$, the local $\NameCert_{\BishopChoiceBarUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed stream source, Cauchy-modulus request, bar-admission row, selected-window row, selected-window ledger, lawless-prefix compatibility row, transport, replay, provenance, and local naming rows;
\item finite bar coverage, requiring $A$ to cover only the inspected prefix family visible through $S$ and the precision threshold visible through $M$;
\item selected-window ledger exactness, requiring every row of $W$ to cite a visible bar hit in $A$ and a schedule edge in the choice-free limit route before any completion-facing read is consumed;
\item lawless-prefix compatibility, requiring $D$ to keep prefix observations finite and to forbid replacement by a global stream law;
\item structural stability, requiring $H,C,P,N$ to transport, replay, source, and name only the rows already listed;
\item non-escape, forbidding countable choice, a full fan theorem, quotient stream equality, selected global limits, host real equality, and ambient $\RealUp$ completeness from the local surface.
\end{enumerate}
No $\BishopChoiceBarUp$ obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-choice-bar-carrier}. The accepted inventory is exactly $S,M,A,W,L,D,H,C,P,N$, so every local naming read must pass through the displayed stream source, modulus request, finite bar row, selected windows, ledger, and lawless-prefix compatibility row.

The bar row is finite. It covers only the inspected prefix family exposed by $S$ at the threshold exposed by $M$, and its citation of the fan-theorem sibling is only the displayed finite coverage discipline needed for those prefixes.

The selected-window ledger is then forced. Each selected window in $W$ is justified by a visible hit in $A$ and is replayed through the choice-free limit schedule, so the packet records a bounded local selection rather than a selector over all requests.

The lawless-prefix and structural rows close the route. The row $D$ forbids replacing finite observations by a global stream law, while $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier has no coordinate for countable choice, a full fan theorem, quotient stream equality, selected global limits, host real equality, or ambient real completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Bishop choice bar L10 dependency route]
\label{thm:bishop-choice-bar-l10-dependency-route}
Every completion-facing consumer of an accepted $\BishopChoiceBarUp$ carrier reads bar-controlled choice data only through the finite route
$$
\StreamNameUp,\CauchyModulusUp \longmapsto A \longmapsto W,L \longmapsto \ChoiceFreeLimitScheduleUp .
$$
The route may expose finite bar coverage and selected-window ledgers for inspected prefixes. It does not export countable choice, a full fan theorem, $\RealUp$ completeness, quotient stream equality, host stream equality, or a selected global limit.
\end{theorem}
\begin{proof}
Start with an accepted packet $B=(S,M,A,W,L,D,H,C,P,N)$. A completion-facing request first reads the stream source $S$ and the Cauchy-modulus threshold $M$, so the inspected prefix family and precision bound are fixed before any selection row is visible.

Next read the bar row $A$ and the selected-window data $W,L$. The bar row supplies finite coverage for those inspected prefixes, and the ledger records only the windows justified by visible bar hits. This is the point where local bar-controlled choice is available.

Finally replay the selected windows through the choice-free limit schedule and the structural rows $D,H,C,P,N$. The lawless-prefix row keeps the stream access finite, and the replay/provenance/name rows preserve the same packet. None of these rows contains a coordinate for countable choice, a full fan theorem, quotient stream equality, host equality, selected global limits, or ambient $\RealUp$ completeness, so no completion-facing consumer can export them.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopChoiceBarUp$ completion-facing read factors through a StreamNameUp source row, CauchyModulusUp request row, finite bar-admission row, selected-window row, selected-window ledger, lawless-prefix compatibility row, hsame transport, Cont replay, Pkg provenance, and local naming.}

\independenceWitness{streamname, cauchymodulus, choice-free-limit-schedule, fantheorem, lawlesssequence: $\BishopChoiceBarUp$ is not bijective to any listed sibling because it jointly carries finite bar coverage, selected-window ledgers, lawless-prefix compatibility, transport, replay, provenance, and local NameCert rows for the L10 completion route.}

\closureat{BishopChoiceBarUp}{seedStr}

\begin{closurestatus}{\BishopChoiceBarUp}
  \constructivestory{A $\BishopChoiceBarUp$ packet is generated from a StreamNameUp source row, CauchyModulusUp request row, finite bar-admission row, selected-window row, selected-window ledger, lawless-prefix compatibility row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-choice-bar-carrier}, \autoref{thm:bishop-choice-bar-namecert-obligations}, and \autoref{thm:bishop-choice-bar-l10-dependency-route} seed the finite bar-controlled local choice surface for L10 completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close countable choice, the full fan theorem, $\RealUp$ completeness, quotient stream equality, selected global limits, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite bar coverage, selected-window ledger exactness, lawless-prefix compatibility, transport, replay, provenance, and local naming for BEDC.Derived.BishopChoiceBarUp.}
\end{closurestatus}
