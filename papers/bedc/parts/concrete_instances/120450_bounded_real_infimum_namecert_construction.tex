\chapter{A Concrete Naming Certificate for $\BoundedRealInfimumUp$}
\label{ch:concrete-instances-bounded-real-infimum-namecert}
\origin{human}

Following Bishop and Bridges, the $\BoundedRealInfimumUp$ packet records the lower-cut side of a bounded real sequence as finite BEDC naming data. It sits beside the bounded sequence surface of \autoref{ch:concrete-instances-boundedrealsequence-namecert}, the bounded lower-cut observation discipline of \autoref{ch:concrete-instances-bounded-real-lower-cut-namecert}, the dyadic rational ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name surface of \autoref{ch:concrete-instances-real-namecert}. The packet gives L10 Real-and-Completion consumers a reusable lower-bound certificate without importing a host infimum, quotient stream equality, choice of subsequence, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Bounded real infimum carrier]
\label{def:bounded-real-infimum-carrier}
A $\BoundedRealInfimumUp$ carrier is a finite $\BHist$ packet
$$
I=(S,L,D,Q,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\BoundedRealSequenceUp$ source row whose finite windows, bound ledger, and displayed $\RealUp$ entries are inspected.
\item $L$ is the $\BoundedRealLowerCutUp$ lower-cut schedule, recording finite lower observations for the bounded source.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used to compare lower-cut observations with displayed rational budgets.
\item $Q$ is the $\RegSeqRatUp$ readback row for the regular rational windows consumed by the lower-cut route.
\item $T$ is the terminal $\RealUp$ seal naming the candidate lower-bound value only after $S,L,D,Q$ have been replayed.
\item $H$ records componentwise $\hsame$ transport for the bounded source, lower-cut schedule, dyadic ledger, rational readback, terminal seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ routes by which consumers replay the bounded sequence row, lower-cut schedule, dyadic ledger, regular readback, terminal real seal, transports, provenance, and local naming row.
\item $P$ is the $\Pkg$ provenance over $\BoundedRealInfimumUp$, $\BoundedRealSequenceUp$, $\BoundedRealLowerCutUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BoundedRealInfimumUp}$ row.
\end{enumerate}
The carrier stores a finite lower-bound route for bounded real sequences. It has no coordinate for a host infimum, quotient equality of streams, a selected subsequence, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{definition}[Bounded real infimum classifier]
\label{def:bounded-real-infimum-classifier}
Two $\BoundedRealInfimumUp$ carriers
$$
I=(S,L,D,Q,T,H,C,P,N)
\qquad\text{and}\qquad
I'=(S',L',D',Q',T',H',C',P',N')
$$
are classified as the same displayed infimum packet exactly when the bounded sequence rows expose the same finite source windows, the lower-cut schedules expose the same accepted lower observations, the dyadic ledgers carry the same rational comparison budgets, the regular readback rows expose the same finite rational windows, the terminal $\RealUp$ seals are transported by the displayed $\hsame$ data, the continuation routes replay the same lower-bound path, and the provenance and local NameCert rows name the same $\BoundedRealInfimumUp$ surface. The classifier is rowwise over $S,L,D,Q,T,H,C,P,N$ and does not identify packets by host infima, quotient stream equality, selected subsequences, trichotomy, or Real completeness.
\end{definition}

\begin{theorem}[Bounded real infimum NameCert obligations]
\label{thm:bounded-real-infimum-namecert-obligations}
For every accepted $\BoundedRealInfimumUp$ carrier $I=(S,L,D,Q,T,H,C,P,N)$, the local $\NameCert_{\BoundedRealInfimumUp}$ surface consists of carrier formation for the nine displayed rows, classifier stability for \autoref{def:bounded-real-infimum-classifier}, lower-cut schedule exactness through $\BoundedRealLowerCutUp$, dyadic tolerance accounting through $\DyadicRatCoreUp$, regular rational readback through $\RegSeqRatUp$, terminal $\RealUp$ sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bounded-real-infimum-carrier}. Its rows are exactly the bounded source, lower-cut schedule, dyadic ledger, regular readback, terminal seal, transport, replay, provenance, and local naming data.

Use the classifier of \autoref{def:bounded-real-infimum-classifier}. It compares accepted packets only by those displayed rows, so a consumer cannot replace the packet by a host-level infimum or by an equality of ambient streams.

The lower-cut, dyadic, regular-readback, and terminal-seal obligations are then row projections. The structural rows $H,C,P,N$ preserve and name the same finite route, and no carrier coordinate contains subsequence choice, trichotomy, or ambient completeness.
\end{proof}

\begin{theorem}[Bounded real infimum lower-bound scope]
\label{thm:bounded-real-infimum-lower-bound-scope}
Every consumer of an accepted $\BoundedRealInfimumUp$ packet receives a lower-bound claim only after the bounded sequence row $S$, bounded lower-cut schedule $L$, dyadic tolerance ledger $D$, regular rational readback $Q$, and terminal $\RealUp$ seal $T$ have been displayed. The lower-bound read is therefore the finite route
$$
S \longmapsto L \longmapsto D \longmapsto Q \longmapsto T \longmapsto H,C,P,N
$$
rather than a host infimum, selected subsequence, quotient stream equality, trichotomy, or Real completeness principle.
\end{theorem}
\begin{proof}
Begin with $S$. The bounded source row supplies the only sequence-facing data available to the packet, and it is read through the bounded-sequence surface.

Next read $L,D,Q$. The lower-cut schedule exposes the accepted lower observations, the dyadic ledger records finite comparison budgets, and the regular readback row exposes the rational windows consumed by those budgets.

Only after those rows are visible can a consumer reach $T$. The structural rows transport, replay, source, and name the same displayed route, so the lower-bound claim cannot escape to any excluded host-level principle.
\end{proof}

\begin{theorem}[Bounded real infimum greatest lower-cut scope]
\label{thm:bounded-real-infimum-greatest-lower-cut-scope}
The greatest-lower-cut information exported by an accepted $\BoundedRealInfimumUp$ carrier is scoped to the displayed lower-cut route $S,L,D,Q,T,H,C,P,N$. A consumer may compare lower observations through $\BoundedRealLowerCutUp$, spend dyadic budgets through $\DyadicRatCoreUp$, read regular rational windows through $\RegSeqRatUp$, and reach the terminal $\RealUp$ seal, but it may not replace that route by a completed host infimum or by a quotient-level stream argument.
\end{theorem}
\begin{proof}
Apply the NameCert obligation surface of \autoref{thm:bounded-real-infimum-namecert-obligations}. It exposes the lower-cut schedule, dyadic ledger, regular readback row, and terminal seal as separate obligations of the accepted packet.

Use the lower-bound scope theorem, \autoref{thm:bounded-real-infimum-lower-bound-scope}. It orders the consumer route from the bounded source through the lower-cut and rational rows before the terminal seal is available.

Finally inspect the carrier inventory. Since host infima, quotient stream equality, selected subsequences, trichotomy, and ambient $\RealUp$ completeness are absent from the packet, the exported greatest-lower-cut surface remains the finite displayed route.
\end{proof}

\falsifiablePrediction{Every accepted $\BoundedRealInfimumUp$ lower-bound read factors through $\BoundedRealSequenceUp$, $\BoundedRealLowerCutUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows before any consumer can inspect the candidate lower-bound seal.}

\independenceWitness{boundedrealsequence, bounded-real-lower-cut, dyadicratcore, regseqrat, real: $\BoundedRealInfimumUp$ is not bijective to any listed sibling because it binds the bounded source, lower-cut schedule, dyadic budget, regular rational readback, terminal Real seal, transport, replay, provenance, and local NameCert rows into one lower-bound packet.}

\closureat{BoundedRealInfimumUp}{seedStr}

\begin{closurestatus}{\BoundedRealInfimumUp}
  \constructivestory{A $\BoundedRealInfimumUp$ packet is generated from a bounded real sequence source, a bounded lower-cut schedule, a DyadicRatCore tolerance ledger, RegSeqRat readback, a terminal RealUp seal, componentwise hsame transports, displayed Cont replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bounded-real-infimum-carrier}, \autoref{def:bounded-real-infimum-classifier}, \autoref{thm:bounded-real-infimum-namecert-obligations}, \autoref{thm:bounded-real-infimum-lower-bound-scope}, and \autoref{thm:bounded-real-infimum-greatest-lower-cut-scope} seed the finite lower-bound surface for bounded real sequences.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a host infimum, quotient stream equality, selected subsequences, trichotomy, Real completeness, Lean verification, or a standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier stability, lower-cut schedule exactness, dyadic tolerance accounting, RegSeqRat readback, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.BoundedRealInfimumUp.}
\end{closurestatus}
