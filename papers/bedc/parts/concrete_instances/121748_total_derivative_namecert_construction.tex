\chapter{A Concrete Naming Certificate for $\TotalDerivativeUp$}
\label{ch:concrete-instances-total-derivative-namecert}
\origin{human}

Following the standard multivariable calculus tradition and its Fréchet-style presentation of linear approximation, the $\TotalDerivativeUp$ packet joins the existing derivative fragments into one finite certificate surface. It reads the linear approximation branch through \autoref{ch:concrete-instances-frechet-derivative-namecert}, the directional and one-parameter probes through \autoref{ch:concrete-instances-gateaux-derivative-namecert} and \autoref{ch:concrete-instances-hadamard-directional-derivative-namecert}, the linear operator row through \autoref{ch:concrete-instances-linearmap-namecert}, the norm and error rows through \autoref{ch:concrete-instances-norm-namecert}, and the terminal seal through \autoref{ch:concrete-instances-real-namecert}. The packet names a local total-linear-approximation route; it is not an inverse-function theorem, not an implicit-function theorem, not a smooth manifold atlas, not a selected ambient limit, and not a hidden completeness principle.

\begin{definition}[Total derivative carrier]
\label{def:total-derivative-carrier}
A $\TotalDerivativeUp$ carrier is a finite $\BHist$ packet
$$
D=(X,Y,L,M,Q,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the source chart row, recording the inspected point, local chart window, and admissible perturbation rows.
\item $Y$ is the target normed row, recording the codomain chart, norm readback, and comparison window consumed by the approximation.
\item $L$ is the $\LinearMapUp$ row naming the candidate linear part.
\item $M$ is the approximation-modulus row, recording the finite tolerance schedule for the remainder.
\item $Q$ is the $\RegSeqRatUp$ readback row for the quotient-style error checks used by the displayed approximation.
\item $R$ is the terminal $\RealUp$ seal row for the inspected error and norm comparisons.
\item $E$ is the remainder ledger, stating that the residual after subtracting $L$ is bounded by the displayed modulus at the requested windows.
\item $H$ records componentwise $\hsame$ transport for source chart, target normed row, linear map, modulus, readback, Real seal, remainder ledger, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from chart exposure to linear-map readback, norm comparison, remainder-modulus inspection, regular rational readback, and Real seal delivery.
\item $P$ is the $\Pkg$ provenance over $\TotalDerivativeUp$, $\FrechetDerivativeUp$, $\GateauxDerivativeUp$, $\HadamardDirectionalDerivativeUp$, $\LinearMapUp$, $\NormUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\TotalDerivativeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,Y,L,M,Q,R,E,H,C,P,N$. It has no coordinate for a global differentiability class, a selected host limit, an inverse map, an implicit solution branch, an ambient Banach-space theorem, or unrecorded directional probes.
\end{definition}

\begin{theorem}[Total derivative NameCert obligations]
\label{thm:total-derivative-namecert-obligations}
For every accepted $\TotalDerivativeUp$ carrier
$$
D=(X,Y,L,M,Q,R,E,H,C,P,N),
$$
the local $\NameCert_{\TotalDerivativeUp}$ surface consists exactly of source-chart exposure, target normed-row exposure, linear-map exposure, remainder-modulus exposure, regular rational readback, Real seal delivery, remainder-ledger exactness, componentwise transport, displayed replay, provenance, and local naming. A consumer may read the packet as a total derivative only after the rows for $\FrechetDerivativeUp$, $\GateauxDerivativeUp$, $\HadamardDirectionalDerivativeUp$, $\LinearMapUp$, $\NormUp$, $\RegSeqRatUp$, and $\RealUp$ have been consumed through the displayed route.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:total-derivative-carrier}. The source chart, target normed row, linear map, modulus, readback, seal, and remainder ledger are all explicit rows of one finite packet.

Read the derivative fragments in their dependency order. The Fréchet row supplies the total linear-approximation shape, while the Gateaux and directional rows record the probe-level faces that the total packet refuses to leave detached.

Read the analytic rows next. The linear-map row $L$ is supplied through $\LinearMapUp$, the norm comparisons through $\NormUp$, the regular rational readback through $Q$, and the Real seal through $R$.

Finally $H,C,P,N$ transport, replay, source, and name the same displayed packet. Because no row stores a global differentiability class, selected host limit, inverse map, implicit branch, ambient Banach theorem, or unrecorded probe, the local NameCert obligations stop at the finite total-linear-approximation route.
\end{proof}

\begin{theorem}[Total derivative linear approximation route]
\label{thm:total-derivative-linear-approximation-route}
Every accepted $\TotalDerivativeUp$ consumer that cites the total derivative must traverse the ordered route
$$
X,Y,L,M,Q,R,E,H,C,P,N.
$$
The route first exposes the source chart and target normed row, then the linear approximation, then the remainder modulus, then the regular rational readback and Real seal, and only then the remainder ledger, transport, replay, provenance, and local naming rows.
\end{theorem}
\begin{proof}
Start with the carrier and NameCert obligations. They identify $X,Y,L,M,Q,R,E,H,C,P,N$ as the complete finite surface and place the linear approximation downstream of the source and target rows.

Use the sibling derivative surfaces to control the route. The Fréchet packet supplies the total-linear approximation shape, while the Gateaux and directional packets keep one-parameter and direction-specific probes visible as dependencies rather than as replacements for $L$.

Use the norm and readback rows to inspect the residual. The row $M$ gives the displayed modulus, $Q$ gives the $\RegSeqRatUp$ readback, $R$ gives the $\RealUp$ seal, and $E$ records the residual bound only after those rows have been exposed.

Thus a consumer cannot cite $\TotalDerivativeUp$ as an unlogged differentiability assertion. The route is exactly $X,Y,L,M,Q,R,E,H,C,P,N$, with no selected host limit or hidden completeness row.
\end{proof}

\falsifiablePrediction{Every accepted $\TotalDerivativeUp$ read must expose source chart, target normed row, linear map, remainder modulus, RegSeqRat readback, Real seal, remainder ledger, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert data before a total-linear-approximation consumer is served.}

\independenceWitness{frechet-derivative, gateaux-derivative, directional-derivative, linear-map, norm, real: $\TotalDerivativeUp$ is not bijective to any listed sibling because it joins chart exposure, normed target data, linear approximation, remainder modulus, readback, seal, transport, replay, provenance, and local naming in one displayed packet.}

\closureat{TotalDerivativeUp}{seedStr}

\begin{closurestatus}{\TotalDerivativeUp}
  \constructivestory{A $\TotalDerivativeUp$ packet is generated from source chart rows, target normed rows, a LinearMap row, a remainder modulus, RegSeqRat readback, a Real seal, a residual ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:total-derivative-carrier}, \autoref{thm:total-derivative-namecert-obligations}, and \autoref{thm:total-derivative-linear-approximation-route} seed the finite total-linear-approximation packet over existing derivative, linear, norm, readback, and Real-seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, a standard bridge, inverse-function or implicit-function theorems, global differentiability classes, selected host limits, ambient completeness, or manifold-atlas compatibility.}
  \upgradepath{Obligation closure requires checked carrier admission, derivative-fragment dependency, LinearMap exposure, Norm modulus accounting, RegSeqRat readback, Real-seal routing, remainder-ledger exactness, transport, replay, provenance, and local naming for BEDC.Derived.TotalDerivativeUp.}
\end{closurestatus}
