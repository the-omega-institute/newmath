\chapter{A Concrete Naming Certificate for $\BishopLocatedInverseFunctionUp$}
\label{ch:concrete-instances-bishop-located-inverse-function-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\BishopLocatedInverseFunctionUp$ packet records the finite local inverse read for a located continuous real map after interval location, apartness separation, regular rational readback, dyadic tolerance accounting, and the terminal real-name seal have all been displayed. It sits beside the monotone inverse route of \autoref{ch:concrete-instances-monotone-inverse-namecert}, the local analytic inverse route of \autoref{ch:concrete-instances-inverse-function-namecert}, the continuous-map surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the located apartness bridge of \autoref{ch:concrete-instances-located-apartness-real-bridge-namecert}, and the L10 real-name lattice of \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The chapter names a located local inverse certificate, not a host inverse function, quotient real equality, trichotomy, selected representative, ambient completeness principle, or bridge verification.

\begin{definition}[Bishop located inverse function carrier]\label{def:bishops-located-inverse-function-carrier}
A $\BishopLocatedInverseFunctionUp$ carrier is a finite $\BHist$ packet
$$
L=(F,A,B,U,V,S,Q,D,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\ContinuousMapUp$ row naming the displayed map and the finite continuity modulus cells used by the local read.
\item $A$ is the $\LocatedApartnessRealBridgeUp$ row. It records the located separation data that keeps candidate preimage windows apart before any inverse request is admitted.
\item $B$ is the source and target interval row, carrying the displayed source window, target window, endpoint images, and located target request.
\item $U$ is the uniqueness-window row. It records the finite apartness comparison showing that two accepted source windows for the same target request cannot remain distinct under the displayed bridge data.
\item $V$ is the existence-window row. It records the finite bisection or refinement schedule that locates a source window for the displayed target request.
\item $S$ is the $\StreamNameUp$ finite-window row opened for source endpoints, target requests, function values, and retained source windows.
\item $Q$ is the $\RegSeqRatUp$ readback row attached to the same stream windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row recording dyadic radii, branch budgets, comparison slack, and endpoint-readback tolerances.
\item $R$ is the terminal $\RealUp$ seal row reached only after the continuous-map, located-apartness, interval, uniqueness, existence, stream, regular rational, and dyadic rows have been displayed.
\item $H$ records componentwise $\hsame$ transport for all rows listed above, together with replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a located inverse request through $F,A,B,U,V,S,Q,D,R,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedInverseFunctionUp$, $\ContinuousMapUp$, $\LocatedApartnessRealBridgeUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopLocatedInverseFunctionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $F,A,B,U,V,S,Q,D,R,H,C,P,N$. It has no coordinate for a host inverse function, quotient real equality, real trichotomy, selected representative, ambient $\RealUp$ completeness, or an unrecorded equality of functions.
\end{definition}

\begin{theorem}[Bishop located inverse function NameCert obligations]\label{thm:bishops-located-inverse-function-namecert-obligations}
For every accepted $\BishopLocatedInverseFunctionUp$ carrier
$$
L=(F,A,B,U,V,S,Q,D,R,H,C,P,N),
$$
the local $\NameCert_{\BishopLocatedInverseFunctionUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the $\ContinuousMapUp$ row, $\LocatedApartnessRealBridgeUp$ row, interval row, uniqueness-window row, existence-window row, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance ledger, $\RealUp$ seal, transport, replay, provenance, and local naming;
\item continuity exposure, requiring every inverse read to open the displayed $\ContinuousMapUp$ modulus cells before endpoint-image or retained-window comparisons are consumed;
\item located apartness exposure, requiring the row $A$ to be read before uniqueness of the source window is inspected;
\item existence-window discipline, requiring $V,S,Q,D$ to display the refinement schedule, finite windows, regular rational readbacks, and dyadic tolerance budget before the terminal $\RealUp$ seal is available;
\item uniqueness-window discipline, requiring $U$ to use the same located apartness and dyadic comparison rows rather than a host equality of real values;
\item structural nonescape, requiring $H,C,P,N$ to preserve only the displayed route and no host inverse function, quotient real equality, real trichotomy, selected representative, ambient Real completeness, or bridge credential.
\end{enumerate}
No Bishop located inverse-function obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:bishops-located-inverse-function-carrier}. The accepted packet displays exactly the continuous-map row, located-apartness bridge row, interval row, uniqueness and existence windows, stream windows, regular rational readback, dyadic tolerance ledger, Real seal, and structural rows.

The local inverse read begins with continuity and location. The row $F$ supplies the finite modulus cells used for endpoint-image and retained-window comparisons, while $A$ supplies the located apartness data that separates candidate source windows.

The existence route is finite. The row $V$ records the refinement schedule, $S$ exposes the relevant finite windows, $Q$ reads those windows as regular rational data, and $D$ records the dyadic comparison budget before the terminal seal $R$ is inspected.

The uniqueness route uses the same displayed data. The row $U$ can compare two source windows only through the already opened located apartness and dyadic tolerance rows, so it cannot import trichotomy or host equality of completed reals.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since no carrier coordinate stores a host inverse function, quotient real equality, selected representative, ambient completeness principle, or bridge credential, the local NameCert surface is exhausted by the listed rows.
\end{proof}

\closureat{BishopLocatedInverseFunctionUp}{seedStr}

\begin{closurestatus}{\BishopLocatedInverseFunctionUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishops-located-inverse-function-carrier} and \autoref{thm:bishops-located-inverse-function-namecert-obligations} seed the finite located inverse-function certificate over $\ContinuousMapUp$, $\LocatedApartnessRealBridgeUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host inverse functions, quotient real equality, real trichotomy, selected representatives, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, continuity exposure, located apartness exposure, existence-window discipline, uniqueness-window discipline, structural nonescape, and local naming for BEDC.Derived.BishopLocatedInverseFunctionUp.}
  \constructivestory{A $\BishopLocatedInverseFunctionUp$ packet is generated from a $\ContinuousMapUp$ row, a $\LocatedApartnessRealBridgeUp$ row, interval rows, uniqueness and existence windows, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerances, a $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
\end{closurestatus}
