\chapter{A Concrete Naming Certificate for $\CofinalStreamTailSelectorUp$}
\label{ch:concrete-instances-cofinalstreamtailselector-namecert}
\origin{ai}

The $\CofinalStreamTailSelectorUp$ packet records the finite cofinal-window route used by the Real and Completion construction of \autoref{ch:visions-real-completeness}. It starts from a precision request, opens a displayed $\StreamNameUp$ tail window, hands that window through $\RegSeqRatUp$ and $\DyadicRatCoreUp$ readback rows, reaches the $\RealUp$ seal, and records selector provenance, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming in one finite $\BHist$ carrier. The packet is a concrete NameCert surface for the L10 construction order of \autoref{sec:roadmap-l10-construction-order}; it is not a quotient stream, host-selected limit, ambient completeness principle, countable-choice rule, or hidden selector.

The one-source tail witness of \autoref{ch:concrete-instances-reg-cauchy-tail-sel-namecert}, the dyadic cofinal route of \autoref{ch:concrete-instances-dyadic-cofinal-tail-selector-namecert}, and the diagonal consumer route of \autoref{ch:concrete-instances-diagonal-tail-selector-namecert} are sibling surfaces. The present packet binds their shared cofinal-window role into one StreamName-tail selector carrier before any $\RealUp$ consumer can read the completion-facing seal.

\begin{definition}[Cofinal StreamName tail selector carrier]
\label{def:cofinal-stream-tail-selector-carrier}
A $\CofinalStreamTailSelectorUp$ carrier is a finite $\BHist$ packet
$$
Q=(\epsilon,W,R,D,A,\sigma,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $\epsilon$ is the positive precision request made by the completion-facing consumer;
\item $W$ is the finite cofinal $\StreamNameUp$ tail window opened at that request;
\item $R$ is the $\RegSeqRatUp$ handoff row read from the displayed window;
\item $D$ is the $\DyadicRatCoreUp$ readback row that records the finite dyadic bounds consumed by the handoff;
\item $A$ is the $\RealUp$ seal row reached only after $W,R,D$ are displayed;
\item $\sigma$ records selector provenance: the request, cofinal window index, selected tail segment, and sibling selector rows used by the packet;
\item $H$ records componentwise $\hsame$ transports for the request, window, regular handoff, dyadic readback, seal, selector provenance, package provenance, and local naming rows;
\item $C$ records the displayed $\Cont$ route from $\epsilon$ through $W,R,D,\sigma$ into $A$;
\item $P$ is the $\Pkg$ provenance over $\CofinalStreamTailSelectorUp$, $\RegularCauchyTailSelectorUp$, $\DyadicCofinalTailSelectorUp$, $\DiagonalTailSelectorUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and local certificate rows;
\item $N$ is the local $\NameCert_{\CofinalStreamTailSelectorUp}$ row.
\end{enumerate}
The local classifier compares accepted carriers only through the visible rows $\epsilon,W,R,D,A,\sigma,H,C,P,N$.
\end{definition}

\begin{theorem}[Cofinal StreamName tail selector NameCert obligations]
\label{thm:cofinal-stream-tail-selector-namecert-obligations}
The local $\NameCert_{\CofinalStreamTailSelectorUp}$ surface for \autoref{def:cofinal-stream-tail-selector-carrier} consists of carrier admission for $Q=(\epsilon,W,R,D,A,\sigma,H,C,P,N)$, precision-request exposure through $\epsilon$, cofinal $\StreamNameUp$ tail-window exposure through $W$, $\RegSeqRatUp$ handoff through $R$, $\DyadicRatCoreUp$ readback through $D$, $\RealUp$ seal delivery through $A$, selector provenance through $\sigma$, componentwise $\hsame$ stability through $H$, displayed $\Cont$ route replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Carrier admission is projection from the displayed tuple in \autoref{def:cofinal-stream-tail-selector-carrier}. The request, cofinal window, regular handoff, dyadic readback, seal, selector provenance, transports, routes, provenance, and naming row are all visible coordinates of one finite packet.

The selector obligations are read in order. The precision request $\epsilon$ opens only the displayed StreamName tail window $W$, the RegSeqRat handoff $R$ and dyadic readback $D$ are both downstream of that finite window, and the selector provenance row $\sigma$ records the sibling selector rows used by the same route.

The RealUp seal row $A$ is downstream of $W,R,D,\sigma$, while $H,C,P,N$ provide transport, route replay, provenance, and local naming. Since the carrier has no coordinate for quotient streams, host-selected limits, countable choice, hidden tail selection, or ambient completeness, these rows exhaust the local NameCert obligations.
\end{proof}
\leanchecked{BEDC.Derived.CofinalStreamTailSelectorUp.CofinalStreamTailSelectorCarrier\_namecert\_obligations}

\begin{theorem}[Cofinal StreamName tail selector seal order]
\label{thm:cofinal-stream-tail-selector-seal-order}
For an accepted $\CofinalStreamTailSelectorUp$ carrier
$$
Q=(\epsilon,W,R,D,A,\sigma,H,C,P,N),
$$
every completion-facing read of the $\RealUp$ seal row $A$ first passes through the finite cofinal $\StreamNameUp$ tail window $W$, the $\RegSeqRatUp$ handoff row $R$, the $\DyadicRatCoreUp$ readback row $D$, and the selector provenance row $\sigma$. The seal route cannot skip directly from the precision request to a host-selected limit.
\end{theorem}
\leanchecked{BEDC.Derived.CofinalStreamTailSelectorUp.CofinalStreamTailSelectorSealOrder}
\begin{proof}
Project the carrier. The seal row $A$ appears only after the window, handoff, dyadic readback, and selector provenance rows in the displayed tuple.

Use the NameCert obligations of \autoref{thm:cofinal-stream-tail-selector-namecert-obligations}. They force any accepted read of the seal to preserve the same request, finite window, regular handoff, dyadic readback, selector provenance, transports, continuation route, package provenance, and local naming row.

Thus every completion-facing seal read factors through $W,R,D,\sigma$ before $A$ is available. There is no coordinate in the carrier for a direct host limit or an ambient completeness argument.
\end{proof}

\begin{theorem}[Cofinal StreamName tail selector TasteGate obligation]
\label{thm:cofinal-stream-tail-selector-tastegate-obligation}
The taste gate for $\CofinalStreamTailSelectorUp$ requires Lean to provide the local $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances in $\mathsf{lean4/BEDC/Derived/CofinalStreamTailSelectorUp/TasteGate.lean}$. The intended instances must read the same finite carrier rows $\epsilon,W,R,D,A,\sigma,H,C,P,N$ and may not add a quotient stream, host limit, ambient completeness theorem, countable-choice selector, or hidden tail oracle.
\end{theorem}
\leanchecked{BEDC.Derived.CofinalStreamTailSelectorUp.CofinalStreamTailSelectorTasteGate\_single\_carrier\_alignment}
\begin{proof}
The carrier of \autoref{def:cofinal-stream-tail-selector-carrier} supplies the finite support on which the taste gate is allowed to act. Nontriviality is witnessed only by the presence of the displayed request-to-window-to-seal route, and field faithfulness is witnessed only by the listed rows and their componentwise transports.

Any Lean instance for the taste gate must therefore be local to the same carrier surface. Adding quotient streams, host limits, ambient completeness, countable choice, or hidden selector rows would create data not present in $Q$ and would fail the NameCert obligation surface of \autoref{thm:cofinal-stream-tail-selector-namecert-obligations}.
\end{proof}

\begin{theorem}[Cofinal StreamName tail selector obligation surface]
\label{thm:cofinal-stream-tail-selector-obligation-surface}
For an accepted $\CofinalStreamTailSelectorUp$ carrier $Q=(\epsilon,W,R,D,A,\sigma,H,C,P,N)$, the obligation surface exposes the precision request $\epsilon$, the displayed $\StreamNameUp$ tail window $W$, the $\RegSeqRatUp$ handoff $R$, the $\DyadicRatCoreUp$ readback $D$, the $\RealUp$ seal $A$, the selector provenance row $\sigma$, the componentwise $\hsame$ row $H$, the displayed $\Cont$ route $C$, the package provenance $P$, and the local naming row $N$ in one finite packet. The surface is read through the sibling routes of \autoref{ch:concrete-instances-reg-cauchy-tail-sel-namecert}, \autoref{ch:concrete-instances-dyadic-cofinal-tail-selector-namecert}, \autoref{ch:concrete-instances-diagonal-tail-selector-namecert}, and \autoref{ch:concrete-instances-real-namecert}.
\end{theorem}
\leanchecked{BEDC.Derived.CofinalStreamTailSelectorUp.CofinalStreamTailSelectorObligationSurface}
\begin{proof}
Project the carrier of \autoref{def:cofinal-stream-tail-selector-carrier}. The request, tail window, regular handoff, dyadic readback, seal, provenance, transport, replay, package, and name are all visible coordinates of one finite packet.

The proof-route order is already fixed by the carrier: the precision request opens the displayed StreamName tail window, the RegSeqRat and DyadicRatCore rows follow from that finite window, and the Real seal is downstream of the same route.

The sibling citations identify the shared tail surface, while $H,C,P,N$ preserve the same route componentwise. Since the carrier has no coordinate for quotient streams, host-selected limits, countable choice, hidden tail selection, or ambient completeness, the displayed rows exhaust the obligation surface.
\end{proof}

\begin{theorem}[Cofinal StreamName tail selector stream route]
\label{thm:cofinal-stream-tail-selector-stream-route}
For every accepted $\CofinalStreamTailSelectorUp$ carrier $Q=(\epsilon,W,R,D,A,\sigma,H,C,P,N)$, the stream route factors through $W,R,D,\sigma$ before the $\RealUp$ seal row $A$ is public. A consumer may cite the route only after the shared sibling surfaces of \autoref{ch:concrete-instances-reg-cauchy-tail-sel-namecert}, \autoref{ch:concrete-instances-dyadic-cofinal-tail-selector-namecert}, \autoref{ch:concrete-instances-diagonal-tail-selector-namecert}, and \autoref{ch:concrete-instances-real-namecert} have already displayed the same finite route.
\end{theorem}
\begin{proof}
Use the obligation surface theorem above. It fixes the displayed rows and places the Real seal downstream of the cofinal StreamName tail window, the regular handoff, and the dyadic readback.

The sibling routes supply the same cofinal-window role in different presentations. Since $H,C,P,N$ only transport, replay, package, and name that route, a consumer cannot bypass the shared finite packet before reading $A$.
\end{proof}
\leanchecked{BEDC.Derived.CofinalStreamTailSelectorUp.CofinalStreamTailSelectorStreamRoute}

\begin{theorem}[Cofinal StreamName tail selector real seal nonescape]
\label{thm:cofinal-stream-tail-selector-real-seal-nonescape}
For an accepted $\CofinalStreamTailSelectorUp$ carrier $Q=(\epsilon,W,R,D,A,\sigma,H,C,P,N)$, the $\RealUp$ seal row $A$ does not export quotient stream identity, host real equality, host-selected limits, countable-choice selection, hidden family limits, or an ambient completeness principle. The seal remains a finite boundary for the same cofinal StreamName route displayed in the sibling tail-selector chapters.
\end{theorem}
\leanchecked{BEDC.Derived.CofinalStreamTailSelectorUp.CofinalStreamTailSelectorRealSealNonescape}
\begin{proof}
Project the carrier. The seal row $A$ is downstream of the finite cofinal tail window $W$, the regular handoff $R$, the dyadic readback $D$, and the selector provenance row $\sigma$.

Apply the seal-order theorem of \autoref{thm:cofinal-stream-tail-selector-seal-order}. It forces every Real-facing read through the same finite cofinal route before $A$ is available.

Since no extra coordinate is present in the carrier, the seal row cannot export quotient stream identity, host equality, selected limits, countable choice, hidden family limits, or ambient completeness.
\end{proof}

\begin{theorem}[Cofinal StreamName tail selector non-escape]
\label{thm:cofinal-stream-tail-selector-nonescape}
An accepted $\CofinalStreamTailSelectorUp$ carrier cannot be used to recover data outside the displayed tuple $\epsilon,W,R,D,A,\sigma,H,C,P,N$. In particular, it exports no quotient stream identity, host real equality, host-selected limit, unbounded tail search, countable-choice selector, hidden family limit, or ambient RealUp completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.CofinalStreamTailSelectorUp.CofinalStreamTailSelectorNonescape}
\begin{proof}
The NameCert obligations identify every row available to a downstream consumer. The precision request, finite StreamName tail window, RegSeqRat handoff, dyadic readback, RealUp seal, and selector provenance rows are the only content rows, and $H,C,P,N$ only transport, replay, package, and name those rows.

Apply \autoref{thm:cofinal-stream-tail-selector-seal-order} to any real-facing read. It forces the read through the finite cofinal window, regular handoff, dyadic readback, and selector provenance before the seal row can be consumed.

Since no additional coordinate is present, the packet has no source for quotient stream identity, host real equality, host-selected limits, unbounded tail search, countable choice, hidden family limits, or an ambient RealUp completeness principle.
\end{proof}

\begin{theorem}[Cofinal StreamName tail selector obligation package]
\label{thm:cofinal-stream-tail-selector-obligation-package}
The $\CofinalStreamTailSelectorUp$ carrier, local $\NameCert$ obligations, seal-order theorem, TasteGate obligation, and non-escape theorem assemble the L10 handoff surface over
$$
\StreamNameUp,\RegSeqRatUp,\DyadicRatCoreUp,\RealUp,\BHist,\hsame,\Cont,\Pkg,\NameCert.
$$
Every completion-facing seal read is therefore routed through the displayed cofinal tail window, regular-sequence handoff, dyadic readback, selector provenance, componentwise transport, continuation route, package provenance, and local naming row. The assembled package gives no bypass through quotient stream equality, host real equality, host-selected limits, countable choice, hidden tail selection, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CofinalStreamTailSelectorUp.CofinalStreamTailSelectorObligationPackage}
\begin{proof}
Use the carrier of \autoref{def:cofinal-stream-tail-selector-carrier} and the obligations of \autoref{thm:cofinal-stream-tail-selector-namecert-obligations}. These identify the finite rows $\epsilon,W,R,D,A,\sigma,H,C,P,N$ and bind them over the StreamName, RegSeqRat, DyadicRatCore, RealUp, history, transport, continuation, package, and naming surfaces.

Apply \autoref{thm:cofinal-stream-tail-selector-seal-order}. It forces every $\RealUp$ seal read through $W,R,D,\sigma$ before $A$ is available, so the handoff is a finite cofinal-window route rather than a direct host-limit read.

Finally combine the TasteGate obligation of \autoref{thm:cofinal-stream-tail-selector-tastegate-obligation} with non-escape from \autoref{thm:cofinal-stream-tail-selector-nonescape}. The taste gate may only read the displayed carrier rows, and the non-escape theorem excludes quotient stream equality, countable choice, hidden selectors, and ambient completeness from the exported surface.
\end{proof}

\begin{theorem}[Cofinal StreamName tail selector public export]
\label{thm:cofinal-stream-tail-selector-public-export}
Every accepted $\CofinalStreamTailSelectorUp$ public consumer receives only the precision request, displayed $\StreamNameUp$ tail window, $\RegSeqRatUp$ handoff, $\DyadicRatCoreUp$ readback, $\RealUp$ seal, selector provenance, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows of the carrier. The public export is routed through the obligation package, seal order, and non-escape boundary, and it uses the TasteGate site of \autoref{thm:cofinal-stream-tail-selector-tastegate-obligation} only as the local finite-carrier discipline.
\end{theorem}
\begin{proof}
Start with the obligation package of \autoref{thm:cofinal-stream-tail-selector-obligation-package}. It assembles the carrier, local NameCert obligations, seal-order theorem, TasteGate obligation, and non-escape theorem over the finite rows $\epsilon,W,R,D,A,\sigma,H,C,P,N$.

Apply the seal-order theorem \autoref{thm:cofinal-stream-tail-selector-seal-order}. It forces every public read of the $\RealUp$ seal through the precision request, StreamName tail window, RegSeqRat handoff, DyadicRatCore readback, and selector provenance before the seal is visible.

Apply the non-escape theorem \autoref{thm:cofinal-stream-tail-selector-nonescape}. It removes quotient stream identity, host real equality, host-selected limits, unbounded tail search, countable choice, hidden family limits, and ambient RealUp completeness from the public surface.

The TasteGate obligation, already recorded at \autoref{thm:cofinal-stream-tail-selector-tastegate-obligation}, supplies only the local finite-carrier discipline for the same rows. Therefore the accepted public consumer receives exactly the listed BEDC rows and no additional tail-selection or completion principle.
\end{proof}
\leanchecked{BEDC.Derived.CofinalStreamTailSelectorUp.CofinalStreamTailSelectorCarrier\_public\_export}

\begin{theorem}[Cofinal StreamName tail selector scoped route]
\label{thm:cofinal-stream-tail-selector-scoped-route}
For every accepted $\CofinalStreamTailSelectorUp$ carrier
$$
Q=(\epsilon,W,R,D,A,\sigma,H,C,P,N),
$$
the route
$$
\epsilon,W,R,D,A,\sigma,H,C,P,N
$$
is scoped over $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The precision request $\epsilon$ opens only the displayed StreamName tail window $W$; $R$ and $D$ read regular-sequence and dyadic rows from that window; $A$ is the terminal Real seal; $\sigma,H,C,P,N$ record selector provenance, componentwise transport, continuation replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Start from the carrier and obligation package of \autoref{thm:cofinal-stream-tail-selector-obligation-package}. They expose exactly the rows $\epsilon,W,R,D,A,\sigma,H,C,P,N$ and list the BEDC surfaces on which those rows may be read.

The carrier order fixes the scope. The request row opens the finite $\StreamNameUp$ window; the $\RegSeqRatUp$ and $\DyadicRatCoreUp$ rows are read from that same displayed window; and the $\RealUp$ seal is reached only after these rows and selector provenance are present.

The remaining rows do not introduce new analytic data. They transport, replay, source, and name the same finite route through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, so the accepted packet is scoped by the listed kernel rows.
\end{proof}

\begin{theorem}[Cofinal StreamName tail selector window readback scope]
\label{thm:cofinal-stream-tail-selector-window-readback-scope}
In an accepted $\CofinalStreamTailSelectorUp$ carrier $Q=(\epsilon,W,R,D,A,\sigma,H,C,P,N)$, the cofinal window row $W$ and readback rows $R,D,\sigma$ remain ordered before any consumer read of the $\RealUp$ seal row $A$. No accepted route can inspect $A$ before the finite window, regular handoff, dyadic readback, and selector provenance have been displayed.
\end{theorem}
\leanchecked{BEDC.Derived.CofinalStreamTailSelectorUp.CofinalStreamTailSelectorWindowReadbackScope}
\begin{proof}
Apply the seal-order theorem \autoref{thm:cofinal-stream-tail-selector-seal-order}. It places the Real seal downstream of $W,R,D,\sigma$ for every completion-facing read.

Use the scoped-route theorem \autoref{thm:cofinal-stream-tail-selector-scoped-route} to keep the same ordering inside the BEDC row classes. The rows $H,C,P,N$ only preserve the already displayed route and cannot move the seal ahead of the cofinal window or readback rows.
\end{proof}

\begin{theorem}[Cofinal StreamName tail selector public certificate]
\label{thm:cofinal-stream-tail-selector-public-certificate}
The carrier of \autoref{def:cofinal-stream-tail-selector-carrier}, the obligation package of \autoref{thm:cofinal-stream-tail-selector-obligation-package}, the seal order of \autoref{thm:cofinal-stream-tail-selector-seal-order}, the scoped route of \autoref{thm:cofinal-stream-tail-selector-scoped-route}, and the window readback scope of \autoref{thm:cofinal-stream-tail-selector-window-readback-scope} export a public certificate for the cofinal-window route over $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The certificate exposes only the displayed rows
$$
\epsilon,W,R,D,A,\sigma,H,C,P,N
$$
and exports no quotient stream identity, host-selected limit, countable-choice selector, hidden tail oracle, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the carrier and obligation package. They identify the accepted packet as the finite row tuple $\epsilon,W,R,D,A,\sigma,H,C,P,N$ and bind those rows to StreamName, RegSeqRat, DyadicRatCore, RealUp, history, transport, continuation, package, and naming surfaces.

Apply the seal-order theorem to the Real-facing boundary. It places $A$ downstream of the finite cofinal window $W$, the regular handoff $R$, the dyadic readback $D$, and selector provenance $\sigma$.

Use the scoped-route theorem and the window-readback scope theorem to make the same ordering public. These results keep the route inside $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ and prevent the seal from moving ahead of the displayed readback rows.

The carrier has no coordinate for quotient stream identity, host-selected limits, countable choice, hidden tail oracles, or ambient Real completeness. Therefore the assembled route is exactly the stated public certificate.
\end{proof}
\leanchecked{BEDC.Derived.CofinalStreamTailSelectorUp.CofinalStreamTailSelectorPublicCertificate}

\falsifiablePrediction{A selector packet that lacks a displayed cofinal StreamName tail window is not an accepted CofinalStreamTailSelectorUp carrier.}
\independenceWitness{regularcauchytailselector, dyadiccofinaltailselector, diagonaltailselector, realcompletionselectorseal: these siblings expose tail, dyadic, diagonal, or seal rows separately, while CofinalStreamTailSelectorUp binds all of them with StreamName cofinal-window selection and RealUp seal handoff, so no row-preserving bijection exists.}

\closureat{CofinalStreamTailSelectorUp}{scopedStr}

\begin{closurestatus}{\CofinalStreamTailSelectorUp}
  \constructivestory{A $\CofinalStreamTailSelectorUp$ packet is generated from a precision request, a displayed cofinal StreamName tail window, RegSeqRat handoff, DyadicRatCore readback, RealUp seal delivery, selector provenance, componentwise hsame transports, displayed Cont routes, package provenance, and a local naming row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{thm:cofinal-stream-tail-selector-scoped-route} and \autoref{thm:cofinal-stream-tail-selector-window-readback-scope} scope the finite route over StreamName, RegSeqRat, DyadicRatCore, Real, BHist, hsame, Cont, Pkg, and NameCert rows before any Real seal read.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close Lean verification, RealUp completeness, quotient stream equality, host real limits, countable choice, hidden tail selection, or a public standard bridge.}
  \upgradepath{Public closure requires a downstream public-object surface for the scoped cofinal-window route without quotient streams, host limits, countable choice, hidden selectors, or ambient RealUp completeness.}
\end{closurestatus}
