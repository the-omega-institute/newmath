\chapter{A Concrete Naming Certificate for $\RegularRealOscillationBoundaryUp$}
\label{ch:concrete-instances-regular-real-oscillation-boundary-namecert}
\origin{human}

Following Bishop and Bridges constructive real analysis, the $\RegularRealOscillationBoundaryUp$ packet records the finite oscillation boundary where a regular rational Cauchy name is allowed to hand a Real-facing consumer only the displayed tail-variation data. It sits over the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}, and the Cauchy oscillation ledger of \autoref{ch:concrete-instances-cauchyoscillation-namecert}. The packet is a boundary object for L10 oscillation consumers, not Real completeness, quotient stream equality, host real equality, or a selected limit.

\begin{definition}[Regular real oscillation boundary carrier]
\label{def:regular-real-oscillation-boundary-carrier}
A $\RegularRealOscillationBoundaryUp$ carrier is a finite $\BHist$ packet
$$
B=(D,S,R,O,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance row naming the finite dyadic radius and comparison budget used by the oscillation request.
\item $S$ is the $\StreamNameUp$ window row exposing only the inspected finite prefix and the later finite refinements admitted for the same name.
\item $R$ is the $\RegSeqRatUp$ regular rational readback row obtained from the displayed stream windows.
\item $O$ is the $\CauchyOscillationUp$ ledger row recording the finite tail-variation comparisons read from $D,S,R$.
\item $L$ is the $\RealUp$ seal reached only after the dyadic tolerance, stream window, regular readback, and oscillation ledger have all been displayed.
\item $H$ records componentwise $\hsame$ transport for the tolerance, window, readback, oscillation, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an oscillation-boundary request through $D,S,R,O$ and then to the terminal seal $L$.
\item $P$ is the $\Pkg$ provenance over $\RegularRealOscillationBoundaryUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\CauchyOscillationUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularRealOscillationBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $D,S,R,O,L,H,C,P,N$. It has no coordinate for quotient stream equality, selected limits, host real equality, countable choice, Real completeness, or an ambient completeness theorem.
\end{definition}

\begin{theorem}[Regular real oscillation boundary NameCert obligations]
\label{thm:regular-real-oscillation-boundary-namecert-obligations}
For every accepted $\RegularRealOscillationBoundaryUp$ carrier $B=(D,S,R,O,L,H,C,P,N)$, the local $\NameCert_{\RegularRealOscillationBoundaryUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed dyadic tolerance, stream window, regular rational readback, oscillation ledger, Real seal, transport, replay, provenance, and local naming rows;
\item tolerance-window compatibility, requiring $D$ and $S$ to be displayed before any regular rational readback is consumed;
\item regular-readback exposure, requiring $R$ to be read only as the finite RegSeqRat route produced from $S$;
\item oscillation-boundary admission, requiring $O$ to consume the same $D,S,R$ rows before any Real-facing seal is read;
\item Real seal non-escape, requiring $L$ to seal only the displayed oscillation comparison and not a completed real object, quotient stream class, selected limit, or host equality;
\item componentwise transport, displayed replay, package provenance, and local naming through $H,C,P,N$.
\end{enumerate}
No obligation is stored outside those rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-real-oscillation-boundary-carrier}. The projection exposes exactly the dyadic tolerance row, finite stream window, regular rational readback, oscillation ledger, Real seal, transport, replay, provenance, and local naming rows.

Read the numerical boundary in order. The dyadic tolerance $D$ and stream window $S$ determine the finite comparison budget; the regular readback $R$ is then the only RegSeqRat row available to the oscillation ledger $O$.

The seal $L$ is downstream of $D,S,R,O$. Since the carrier has no quotient stream, selected limit, host equality, choice, or completeness coordinate, the seal records only the displayed oscillation comparison.

Finally, $H,C,P,N$ transport, replay, source, and name the same finite route. Thus the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Regular real oscillation boundary L10 handoff]
\label{thm:regular-real-oscillation-boundary-l10-handoff}
Every $\RegSeqRatUp$ or $\RealUp$ consumer of an accepted $\RegularRealOscillationBoundaryUp$ carrier must factor through the finite route
$$
D \longmapsto S \longmapsto R \longmapsto O \longmapsto L \longmapsto H,C,P,N .
$$
The handoff gives the consumer a displayed oscillation boundary for regular rational Cauchy names, not Real completeness, quotient equality of streams, host real equality, selected limits, or countable choice.
\end{theorem}
\begin{proof}
Let $B=(D,S,R,O,L,H,C,P,N)$ be accepted. The only tolerance source is $D$, and the only observation source is the finite window $S$.

The regular rational readback $R$ is downstream of those rows, and the Cauchy oscillation ledger $O$ reads the same finite data before the Real seal $L$ is available. A consumer therefore reaches $L$ only through the displayed L10 source route.

Componentwise transport and replay through $H$ and $C$ preserve that route, while $P$ and $N$ record the provenance and local name. Since no other coordinate exists, the consumer cannot obtain Real completeness, quotient stream equality, host equality, a selected limit, or countable choice from this packet.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularRealOscillationBoundaryUp$ reader that claims a Real-facing oscillation handoff while omitting DyadicRatCore tolerance, StreamName windows, RegSeqRat readback, CauchyOscillation ledger, Real seal, hsame transport, Cont replay, Pkg provenance, or local naming refutes the packet.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, cauchyoscillation: $\RegularRealOscillationBoundaryUp$ is not bijective to any listed sibling because it jointly carries the dyadic tolerance, stream window, regular readback, oscillation ledger, terminal seal, transport, replay, provenance, and local naming as one boundary packet.}

\closureat{RegularRealOscillationBoundaryUp}{seedStr}

\begin{closurestatus}{\RegularRealOscillationBoundaryUp}
  \constructivestory{A $\RegularRealOscillationBoundaryUp$ packet is generated from a DyadicRatCore tolerance row, StreamName windows, RegSeqRat readback, a CauchyOscillation ledger, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-real-oscillation-boundary-carrier}, \autoref{thm:regular-real-oscillation-boundary-namecert-obligations}, and \autoref{thm:regular-real-oscillation-boundary-l10-handoff} seed the finite oscillation boundary for RegSeqRat and Real consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, Real completeness, quotient stream equality, host real equality, selected limits, countable choice, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, tolerance-window compatibility, regular readback exposure, oscillation-boundary admission, Real seal non-escape, transport, replay, provenance, and local naming for BEDC.Derived.RegularRealOscillationBoundaryUp.}
\end{closurestatus}
