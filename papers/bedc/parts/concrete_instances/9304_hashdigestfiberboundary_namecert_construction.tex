\chapter{A Concrete Naming Certificate for $\HashDigestFiberBoundaryUp$}
\label{ch:concrete-instances-hash-digest-fiber-boundary-namecert}
\origin{ai}

The $\HashDigestFiberBoundaryUp$ packet realizes the digest/fiber boundary isolated in \autoref{ch:visions-hash-like-apophatic-fixed-point} as a finite NameCert surface. It sits beside the hash-apophatic seal of \autoref{ch:concrete-instances-hashapophaticseal-namecert}, the digest-fiber ledger of \autoref{ch:concrete-instances-digestfiberledger-namecert}, and the hash-like fiber boundary of \autoref{ch:concrete-instances-hashlikefiberboundary-namecert}. Its role is narrower than those siblings: it names the boundary at which visible digest sameness, source-fiber membership, GAP provenance, and refusal of source equality must be read together.

\begin{definition}[HashDigestFiberBoundary carrier]
\label{def:hash-digest-fiber-boundary-carrier}
A $\HashDigestFiberBoundaryUp$ carrier is a finite $\BHist$ packet
$$
B=(D,E,F,G,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the visible digest row read through the $\HashUp$ surface;
\item $E$ is the digest-sameness row, recording classifier-level sameness of visible digest tokens;
\item $F$ is the fiber-membership row, recording the source-side membership behind $D$ without identifying members of the fiber;
\item $G$ is the $\mathsf{GAP}$ provenance row for the same fiber boundary;
\item $R$ is the refusal row blocking any consumer from turning $E$ into source equality or selected preimage recovery;
\item $H$ records componentwise $\hsame$ transports for $D,E,F,G,R,C,P,N$;
\item $C$ records displayed $\Cont$ routes for digest, digest-sameness, fiber, GAP, refusal, transport, provenance, and local naming reads;
\item $P$ is the $\Pkg$ provenance over $\HashUp$, $\DigestFiberLedgerUp$, $\HashLikeFiberBoundaryUp$, $\HashApophaticSealUp$, $\ApophaticFixedPointUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\HashDigestFiberBoundaryUp}$ row.
\end{enumerate}
The carrier contains no coordinate for source equality obtained from digest sameness, no hidden preimage selector, no quotient collapse of the fiber, no global digest, and no unledgered source read.
\end{definition}

\begin{theorem}[HashDigestFiberBoundary NameCert obligations]
\label{thm:hash-digest-fiber-boundary-namecert-obligations}
For the carrier of \autoref{def:hash-digest-fiber-boundary-carrier}, the local $\NameCert_{\HashDigestFiberBoundaryUp}$ surface consists exactly of carrier admission for
$$
B=(D,E,F,G,R,H,C,P,N),
$$
visible digest exposure through $D$, digest-sameness exposure through $E$, fiber membership through $F$, GAP provenance through $G$, source-equality refusal through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.

Every accepted consumer of the packet must preserve the boundary order: digest sameness is read before source equality is even meaningful, and source equality remains unavailable unless a separate exactness certificate is displayed outside the $\HashDigestFiberBoundaryUp$ packet.
\end{theorem}
\leanchecked{BEDC.Derived.HashDigestFiberBoundaryUp.HashDigestFiberBoundaryTasteGate\_single\_carrier\_alignment}
\begin{proof}
Project the displayed carrier in \autoref{def:hash-digest-fiber-boundary-carrier}. The rows $D,E,F,G,R$ are the complete digest/fiber boundary content, while $H,C,P,N$ transport, replay, package, and name the same finite packet.

Read the sibling sources through their local roles. The $\HashUp$ surface supplies the visible digest, $\DigestFiberLedgerUp$ supplies the fiber and GAP rows, $\HashLikeFiberBoundaryUp$ supplies the consumer boundary discipline, $\HashApophaticSealUp$ supplies the digest/fiber/far-end seal pattern, and $\ApophaticFixedPointUp$ supplies the refusal of internalized far-end objects.

The NameCert surface is exhausted by these rows. Since the carrier has no coordinate for source equality from digest sameness, selected preimage recovery, quotient collapse, global digest, or unledgered source read, an accepted consumer cannot obtain any of those readings from the packet.
\end{proof}

\begin{theorem}[HashDigestFiberBoundary nonescape]
\label{thm:hash-digest-fiber-boundary-nonescape}
Every accepted $\HashDigestFiberBoundaryUp$ consumer that reads digest sameness through $E$ must also keep the visible digest row $D$, fiber row $F$, GAP provenance row $G$, refusal row $R$, componentwise transport $H$, continuation routes $C$, package provenance $P$, and local naming row $N$. Removing the fiber and GAP provenance rows while keeping digest sameness collapses the packet's ability to distinguish digest equality from source equality.

Consequently the packet exports no source-equality reflection, selected preimage, quotient fiber, global universe digest, hidden source payload, or unledgered source read.
\end{theorem}
\leanchecked{BEDC.Derived.HashDigestFiberBoundaryUp.HashDigestFiberBoundary\_nonescape}
\begin{proof}
Start from an accepted packet $B=(D,E,F,G,R,H,C,P,N)$. The digest-sameness row $E$ is only a classifier-facing comparison of visible digest rows, while $F$ and $G$ record the source-side fiber membership and provenance that keep the boundary auditable.

Apply the NameCert obligations of \autoref{thm:hash-digest-fiber-boundary-namecert-obligations}. They force any accepted read of $E$ to remain packaged with the digest, fiber, GAP, refusal, transport, continuation, provenance, and local naming rows.

If $F$ and $G$ are removed while $E$ remains, the remaining carrier does not record which source-side fiber is being compared. The packet therefore has no displayed way to separate digest equality from source equality. Since source-equality reflection, selected preimage recovery, quotient collapse, global digest, hidden source payload, and unledgered source reads have no carrier coordinate, none can escape the displayed packet.
\end{proof}

\begin{theorem}[HashDigestFiberBoundary field-faithful obligation]
\label{thm:hash-digest-fiber-boundary-field-faithful-obligation}
For an accepted $\HashDigestFiberBoundaryUp$ packet
$$
B=(D,E,F,G,R,H,C,P,N),
$$
the field-faithful obligation surface is visible in the displayed rows. The digest row $D$, digest-sameness row $E$, fiber row $F$, GAP row $G$, refusal row $R$, transport row $H$, continuation row $C$, provenance row $P$, and naming row $N$ are the fields whose carrier alignment is cited by the checked TasteGate marker attached to \autoref{thm:hash-digest-fiber-boundary-namecert-obligations}.
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:hash-digest-fiber-boundary-carrier}. Each field named in the obligation is one coordinate of $B$, and no row is supplied by ambient digest theory or by a hidden source read.

The checked TasteGate marker attached to \autoref{thm:hash-digest-fiber-boundary-namecert-obligations} records the same single-carrier alignment. Hence field-faithfulness is a paper obligation over the concrete rows $D,E,F,G,R,H,C,P,N$, not a prose property outside the packet.
\end{proof}

\begin{theorem}[HashDigestFiberBoundary nontrivial obligation]
\label{thm:hash-digest-fiber-boundary-nontrivial-obligation}
The nontriviality obligation for $\HashDigestFiberBoundaryUp$ is the joint presence of
$$
D,E,F,G,R,H,C,P,N.
$$
A consumer may not retain only digest sameness $E$: the visible digest $D$, fiber membership $F$, GAP provenance $G$, refusal $R$, componentwise transport $H$, continuation replay $C$, package provenance $P$, and local naming $N$ are the concrete rows that make the TasteGate carrier nontrivial.
\end{theorem}
\begin{proof}
Use \autoref{thm:hash-digest-fiber-boundary-nonescape}. It already shows that a read of $E$ must keep the visible digest, fiber, GAP, refusal, transport, continuation, provenance, and naming rows.

The TasteGate marker attached to the NameCert obligations therefore has a visible nontrivial carrier to check: it is precisely the row tuple $D,E,F,G,R,H,C,P,N$. Removing any of $F,G,R$ would collapse the boundary into digest-sameness prose, while removing $H,C,P,N$ would leave no transported, replayable, packaged, and named certificate.
\end{proof}

\begin{theorem}[HashDigestFiberBoundary obligation scope]
\label{thm:hash-digest-fiber-boundary-obligation-scope}
The scoped carrier obligations of $\HashDigestFiberBoundaryUp$ are exactly the obligations carried by
$$
B=(D,E,F,G,R,H,C,P,N).
$$
They include field-faithful visibility of the displayed rows, nontrivial joint presence of digest, fiber, GAP, and refusal data, componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming; they do not include source-equality reflection, preimage selection, quotient collapse, a global digest, or an unledgered source read.
\end{theorem}
\begin{proof}
Combine the row projection in \autoref{thm:hash-digest-fiber-boundary-field-faithful-obligation} with the nontriviality projection in \autoref{thm:hash-digest-fiber-boundary-nontrivial-obligation}. Both are statements about the same accepted carrier $B$.

The checked TasteGate marker cited at the NameCert obligation site aligns the single carrier with those rows. The scope is therefore the finite digest/fiber boundary packet itself, and the excluded readings remain outside the obligation surface because no coordinate of $B$ names them.
\end{proof}

\falsifiablePrediction{Removing the fiber and GAP provenance rows while keeping digest sameness collapses the chapter's ability to distinguish digest equality from source equality.}

\independenceWitness{Nearest siblings: hashapophaticseal, digestfiberledger, hashlikefiberboundary, hash, and apophaticfixedpoint. No bijection exists because each sibling omits at least one row among explicit digest-sameness exposure, fiber membership, GAP provenance, source-equality refusal, and local NameCert packaging for the digest/fiber boundary.}

The Lean boundary \texttt{lean4/BEDC/Derived/HashDigestFiberBoundaryUp/TasteGate.lean} must provide \texttt{Nontrivial HashDigestFiberBoundaryUp} and \texttt{FieldFaithful HashDigestFiberBoundaryUp} instances for the displayed digest, digest-sameness, fiber, GAP, refusal, transport, continuation, provenance, and naming carrier.

\closureat{HashDigestFiberBoundaryUp}{obligationStr}

\begin{closurestatus}{\HashDigestFiberBoundaryUp}
  \constructivestory{A $\HashDigestFiberBoundaryUp$ packet is generated from visible digest exposure, digest-sameness comparison, source-side fiber membership, $\mathsf{GAP}$ provenance, source-equality refusal, componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local naming. The carrier first reads digest equality as a visible comparison, then keeps the source-side fiber and gap rows attached so that equality of digest rows is not confused with equality of sources. The classifier accepts the boundary only when digest, fiber, gap, and refusal data remain present as one finite NameCert packet.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{thm:hash-digest-fiber-boundary-obligation-scope} binds the displayed carrier rows together with the checked TasteGate marker attached to \autoref{thm:hash-digest-fiber-boundary-namecert-obligations}.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{It does not claim source-equality reflection, selected preimage recovery, quotient collapse, global digest universes, hidden source payloads, or unledgered source reads.}
  \upgradepath{To strengthen the verification axis, formalize the digest-sameness, fiber, gap, refusal, field-faithful, nontriviality, and scoped obligation rows as a checked boundary.}
\end{closurestatus}
