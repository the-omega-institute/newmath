\chapter{A Concrete Naming Certificate for $\BishopDiagonalContinuityBarrierUp$}
\label{ch:concrete-instances-bishop-diagonal-continuity-barrier-namecert}
\origin{human}

Bishop--Bridges and Troelstra-style constructive continuity principles treat diagonal arguments over function spaces as finite evidence disciplines rather than as licenses to choose a discontinuity point. The $\BishopDiagonalContinuityBarrierUp$ packet records the boundary where a diagonal probe against a real-valued continuity claim must expose its source windows, rational readback, and replay rows before any obstruction is accepted. It sits beside the finite window route of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}, the continuous-map surface of \autoref{ch:concrete-instances-continuousmap-namecert}, and the Baire-space source discipline of \autoref{ch:concrete-instances-bairespace-namecert}. The packet names a choice-free diagonal continuity barrier, not a completed counterexample extracted from countable choice or ambient completeness.

\begin{definition}[Bishop diagonal continuity barrier carrier]
\label{def:bishop-diagonal-continuity-barrier-carrier}
A $\BishopDiagonalContinuityBarrierUp$ carrier is a finite $\BHist$ packet
$$
B=(S,R,A,F,U,W,D,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ source-window row, recording the finite address requests used by the diagonal probe.
\item $R$ is the $\RegSeqRatUp$ readback row, exposing regular rational values attached to those source windows.
\item $A$ is the terminal $\RealUp$ seal row reached only after the regular rational readback has been displayed.
\item $F$ is the $\ContinuousMapUp$ row whose claimed continuity is being inspected through the displayed windows.
\item $U$ is the $\BaireSpaceUp$ or source-window data row supplying the finite branch segment on which the diagonal probe is formed.
\item $W$ is the diagonal probe-window row, pairing the inspected source window with the requested target tolerance.
\item $D$ is the barrier ledger, recording that the diagonal read is an obstruction to an unexposed continuity route rather than a selected discontinuity point.
\item $H$ records componentwise $\hsame$ transport for the source-window, regular readback, Real seal, continuous-map, source data, diagonal window, barrier ledger, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a continuity request through $S,R,A,F,U,W,D,H$.
\item $P$ is the $\Pkg$ provenance over $\BishopDiagonalContinuityBarrierUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\ContinuousMapUp$, $\BaireSpaceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopDiagonalContinuityBarrierUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,R,A,F,U,W,D,H,C,P,N$. It has no coordinate for countable choice, a selected discontinuity point, quotient function equality, or ambient Real completeness.
\end{definition}

\begin{theorem}[Bishop diagonal continuity barrier NameCert obligations]
\label{thm:bishop-diagonal-continuity-barrier-namecert-obligations}
For every accepted $\BishopDiagonalContinuityBarrierUp$ carrier
$$
B=(S,R,A,F,U,W,D,H,C,P,N),
$$
the local $\NameCert_{\BishopDiagonalContinuityBarrierUp}$ surface consists exactly of carrier admission for the $\StreamNameUp$ source windows, $\RegSeqRatUp$ readback, $\RealUp$ seal, $\ContinuousMapUp$ row, $\BaireSpaceUp$ or source-window row, diagonal probe windows, barrier ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every barrier read must expose $S,R,A,F,U,W,D,H,C,P,N$, and no obligation is stored outside those rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-diagonal-continuity-barrier-carrier}. The analytic rows are exactly the source windows, regular rational readback, Real seal, continuous-map row, source data, diagonal probe windows, and barrier ledger.

The continuity-facing read is $F,U,W,D$. The source data row fixes the finite branch segment, the diagonal window records the inspected request, and the barrier ledger records only the failure of an unexposed continuity route.

The Real-facing read is $S,R,A$. The StreamName window and regular rational readback are displayed before the Real seal is consumed, so the packet cannot appeal to ambient Real completeness.

Finally $H,C,P,N$ transport, replay, source, and name the same finite rows. Since no carrier coordinate contains countable choice, a selected discontinuity point, quotient function equality, or ambient Real completeness, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Bishop diagonal continuity barrier choice-free route]
\label{thm:bishop-diagonal-continuity-barrier-choice-free-route}
Every accepted $\BishopDiagonalContinuityBarrierUp$ read is a choice-free route
$$
\StreamNameUp,\RegSeqRatUp,\RealUp,\ContinuousMapUp,\BaireSpaceUp \longmapsto W,D \longmapsto \hsame,\Cont,\Pkg,\NameCert .
$$
It may obstruct a continuity claim only through displayed source windows, regular rational readback, Real sealing, continuous-map inspection, source-window data, diagonal probe windows, transport, replay, provenance, and local naming. It does not export countable choice, a selected discontinuity point, quotient function equality, or ambient Real completeness.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:bishop-diagonal-continuity-barrier-namecert-obligations}. They require every accepted read to expose the source windows, regular readback, Real seal, continuous-map row, source data, diagonal window, and barrier ledger.

Read the diagonal stage through $U,W,D$. The source-window data and probe window are finite rows, so the obstruction is tied to a displayed request rather than to a choice-selected point.

Read the Real stage through $S,R,A$. The regular rational readback and terminal seal are consumed only after the finite StreamName windows are visible.

The structural rows $H,C,P,N$ preserve the same route by transport, replay, provenance, and local naming. Because the carrier omits countable choice, selected discontinuity points, quotient function equality, and ambient Real completeness, the accepted barrier remains the stated choice-free finite packet.
\end{proof}

\falsifiablePrediction{A $\BishopDiagonalContinuityBarrierUp$ consumer that exports a diagonal continuity obstruction without the $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\ContinuousMapUp$, source-window, diagonal probe, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows refutes the carrier of \autoref{def:bishop-diagonal-continuity-barrier-carrier}.}

\independenceWitness{streamname, regseqrat, real, continuousmap, bairespace: $\BishopDiagonalContinuityBarrierUp$ is not bijective to any listed sibling because this packet alone binds source-window data, diagonal probe windows, barrier-ledger routing, transport, replay, provenance, and local naming around a choice-free continuity obstruction.}

\closureat{BishopDiagonalContinuityBarrierUp}{seedStr}

\begin{closurestatus}{\BishopDiagonalContinuityBarrierUp}
  \constructivestory{A $\BishopDiagonalContinuityBarrierUp$ packet is generated from StreamName source windows, RegSeqRat readback, a Real seal, a ContinuousMap row, BaireSpace or source-window data, diagonal probe windows, a barrier ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-diagonal-continuity-barrier-carrier}, \autoref{thm:bishop-diagonal-continuity-barrier-namecert-obligations}, and \autoref{thm:bishop-diagonal-continuity-barrier-choice-free-route} seed the choice-free diagonal continuity barrier over finite source windows, regular rational readback, Real seals, continuous-map inspection, source-window data, diagonal probe windows, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim countable choice, selected discontinuity points, quotient function equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source-window exposure, regular readback, continuous-map inspection, diagonal probe-window routing, barrier-ledger nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BishopDiagonalContinuityBarrierUp.}
\end{closurestatus}
