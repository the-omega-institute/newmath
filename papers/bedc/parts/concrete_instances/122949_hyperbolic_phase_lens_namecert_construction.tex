\chapter{A Concrete Naming Certificate for $\HyperbolicPhaseLensUp$}
\label{ch:concrete-instances-hyperbolic-phase-lens-namecert}
\origin{ai}

The $\HyperbolicPhaseLensUp$ packet realizes the observer-expansion window of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite boundary-shadow, Busemann-displacement, Real-seal, transport, replay, provenance, and local naming rows. It sits beside the boundary-shadow packet of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the Busemann boundary surface of \autoref{ch:concrete-instances-hyperbolic-busemann-boundary-namecert}, the visual-boundary readout of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, the observer-expansion packet of \autoref{ch:concrete-instances-hyperbolic-observer-expansion-namecert}, and the observer-shadow metric route of \autoref{ch:concrete-instances-hyperbolic-observer-shadow-metric-namecert}. The packet names a finite observer-window lens; it does not construct a global hyperbolic model, quotient an ideal boundary, choose geodesic rays, identify physical spacetime, or bridge-check the route.

\begin{definition}[Hyperbolic phase lens carrier]
\label{def:hyperbolic-phase-lens-carrier}
A $\HyperbolicPhaseLensUp$ carrier is a finite $\BHist$ packet
$$
L=(B,U,V,D,R,W,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $B$ is the boundary-shadow source row, exposing the displayed shadow window read from a finite observer boundary packet.
\item $U$ is the Busemann-displacement row, recording the signed displacement comparison used by the lens.
\item $V$ is the visual-boundary row, keeping the boundary endpoint readback visible before any phase interpretation.
\item $D$ is the observer-distance row, tying the phase lens to the observer-shadow metric route.
\item $R$ is the $\RealUp$ seal for the displayed scalar comparison attached to $U$ and $D$.
\item $W$ is the finite observer-window row listing the shadows, displacements, and scalar reads inspected by the packet.
\item $H$ records componentwise $\hsame$ transport for $B,U,V,D,R,W,C,P,N$.
\item $C$ records displayed $\Cont$ replay from boundary shadow through Busemann displacement, visual-boundary readback, observer-distance comparison, Real sealing, and local naming.
\item $P$ is the $\Pkg$ provenance over the hyperbolic shadow, Busemann, visual-boundary, observer-expansion, Real-seal, transport, and replay rows.
\item $N$ is the local $\NameCert_{\HyperbolicPhaseLensUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,U,V,D,R,W,H,C,P,N$. It has no coordinate for a global hyperbolic space, quotient ideal boundary, chosen geodesic ray family, physical observer law, ambient compactification, or bridge credential.
\end{definition}
\leanstmt{BEDC.Derived.HyperbolicPhaseLensUp}

\begin{theorem}[Hyperbolic phase lens NameCert obligations]
\label{thm:hyperbolic-phase-lens-namecert-obligations}
For every accepted $\HyperbolicPhaseLensUp$ carrier
$$
L=(B,U,V,D,R,W,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicPhaseLensUp}$ surface consists exactly of boundary-shadow exposure through $B$, Busemann-displacement exposure through $U$, visual-boundary readback through $V$, observer-distance comparison through $D$, Real sealing through $R$, finite observer-window listing through $W$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-phase-lens-carrier}. The packet displays the shadow, displacement, boundary, observer-distance, Real-seal, window, transport, replay, provenance, and naming rows as its whole public surface.

The observer-window read is chained rather than detached. Boundary-shadow data enters through $B$, Busemann displacement through $U$, visual-boundary readback through $V$, and observer-distance comparison through $D$ before the Real-sealed scalar row $R$ can be consumed.

Finally $W,H,C,P,N$ list, transport, replay, source, and name the same finite route. Since no coordinate supplies a global boundary quotient or selected geodesic family, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Hyperbolic phase lens observer-window route]
\label{thm:hyperbolic-phase-lens-observer-window-route}
Every observer-expansion read exported by an accepted $\HyperbolicPhaseLensUp$ packet must first pass through \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, \autoref{ch:concrete-instances-hyperbolic-busemann-boundary-namecert}, \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, and \autoref{ch:concrete-instances-hyperbolic-observer-shadow-metric-namecert}. The exported lens is the finite route $B,U,V,D,R,W,H,C,P,N$, not a global hyperbolic compactification, a quotient ideal-boundary object, a physical observer law, or a selected geodesic-ray representative.
\end{theorem}
\begin{proof}
Read the hyperbolic siblings in dependency order. The boundary-shadow row $B$ supplies the finite shadow window, while $U$ and $V$ keep the Busemann and visual-boundary rows visible before a phase-lens read is public.

The observer-distance and Real rows then bind the scalar comparison. Rows $D$ and $R$ make the observer-shadow metric and Real seal explicit, and $W$ records the finite observer windows actually inspected by the packet.

The structural rows $H,C,P,N$ transport, replay, source, and name that same route. Because the packet has no coordinates for global compactification, quotient endpoints, geodesic-ray choice, or physical spacetime semantics, the exported lens remains a finite observer-window route.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicPhaseLensUp$ read exposes boundary-shadow, Busemann-displacement, visual-boundary, observer-distance, Real-seal, finite observer-window, transport, replay, provenance, and local naming rows before any observer-expansion consumer reads the lens.}

\independenceWitness{hyperbolic-boundary-shadow, hyperbolic-busemann-boundary, hyperbolic-visual-boundary, hyperbolic-observer-expansion, hyperbolic-observer-shadow-metric: $\HyperbolicPhaseLensUp$ is not bijective to any listed sibling because it binds shadow, displacement, visual-boundary, observer-distance, and Real-seal rows into one finite lens packet.}

\closureat{HyperbolicPhaseLensUp}{seedStr}

\begin{closurestatus}{\HyperbolicPhaseLensUp}
  \constructivestory{A $\HyperbolicPhaseLensUp$ packet is generated from a boundary-shadow source row, a Busemann-displacement row, a visual-boundary row, an observer-distance row, a Real seal, a finite observer-window listing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-phase-lens-carrier}, \autoref{thm:hyperbolic-phase-lens-namecert-obligations}, and \autoref{thm:hyperbolic-phase-lens-observer-window-route} seed the finite hyperbolic observer-window lens over boundary-shadow, Busemann-displacement, visual-boundary, observer-distance, Real-seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close global hyperbolic compactification, quotient ideal-boundary equality, selected geodesic rays, physical observer laws, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, boundary-shadow exactness, Busemann-displacement exactness, observer-window listing, Real-seal handoff, transport, replay, provenance, local naming, and non-escape for BEDC.Derived.HyperbolicPhaseLensUp.}
\end{closurestatus}
