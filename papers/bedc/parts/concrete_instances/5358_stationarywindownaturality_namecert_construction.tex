\chapter{A Concrete Naming Certificate for $\StationaryWindowNaturalityUp$}
\label{ch:concrete-instances-stationarywindownaturality-namecert}
\origin{ai}

The $\StationaryWindowNaturalityUp$ packet records the finite naturality comparison required when a transported stationary rational classifier is read through the Real-completeness finite-window route. It consumes the transported stationary diagonal surface of \autoref{ch:concrete-instances-transportedstationarydiagonal-namecert}, the route boundary of \autoref{ch:concrete-instances-stationarydiagonalwindowroute-namecert}, the observation-budget surface of \autoref{ch:concrete-instances-cauchymodulusextraction-namecert}, and the fused regular-Cauchy window packet of \autoref{ch:concrete-instances-regularcauchywindowfusion-namecert}. The carrier keeps the seed classifier and the four boundary readings in one displayed packet; it does not export a quotient stream, selected limit, host real equality, or ambient completeness principle.

\falsifiablePrediction{A RealUp stationary-window consumer that bypasses RatUp or StreamNameUp finite-window transport refutes the packet.}

\independenceWitness{transportedstationarydiagonal, stationarydiagonalwindowroute, cauchymodulusextraction, regularcauchywindowfusion: none is bijective because this carrier adds a naturality comparison row tying four boundary readings to one RatUp seed classifier.}

\begin{definition}[Stationary window naturality carrier]
\label{def:stationary-window-naturality-carrier}
A $\StationaryWindowNaturalityUp$ carrier is a finite $\BHist$ packet
$$
N=(Q,S,R,D,E,W,H,C,P,K)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the transported stationary $\RatUp$ seed classifier row;
\item $S$ is the constant $\StreamNameUp$ finite-window classifier induced by $Q$;
\item $R$ is the $\RegSeqRatUp$ seal-consumption row read from the same finite window;
\item $D$ is the $\DyadicRatCoreUp$ observation and tolerance ledger attached to that window;
\item $E$ is the $\RealUp$ finite-window seal row reached from $S,R,D$;
\item $W$ is the naturality comparison row asserting that the four boundary readings $S,R,D,E$ are replays of the same seed classifier $Q$;
\item $H$ stores componentwise $\hsame$ transports for $Q,S,R,D,E,W,C,P,K$;
\item $C$ records the displayed $\Cont$ routes that replay the seed, stream window, regular readback, dyadic ledger, real seal, and naturality row;
\item $P$ is the $\Pkg$ provenance over $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $K$ is the local $\NameCert_{\StationaryWindowNaturalityUp}$ row.
\end{enumerate}
The carrier has no coordinate for an unrecorded real comparison, a non-stationary stream, a quotient regular stream, a choice-selected limit, or an ambient completeness theorem.
\end{definition}

\begin{theorem}[Stationary window naturality NameCert obligations]
\label{thm:stationary-window-naturality-namecert-obligations}
The local $\NameCert_{\StationaryWindowNaturalityUp}$ surface for \autoref{def:stationary-window-naturality-carrier} consists exactly of carrier admission for $N=(Q,S,R,D,E,W,H,C,P,K)$, seed classifier exposure through $Q$, constant finite-window exposure through $S$, regular-sequence seal exposure through $R$, dyadic observation exposure through $D$, real finite-window seal exposure through $E$, naturality comparison exposure through $W$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $K$.
\end{theorem}
\begin{proof}
Project the carrier. The rows $Q,S,R,D,E,W,H,C,P,K$ are the only coordinates admitted to the packet, so every local naming obligation must be read from one of these rows.

The four boundary readings are tied to the seed in order. The stationary diagonal window route supplies $S$ from $Q$, the regular-sequence row $R$ consumes the same window, the dyadic ledger $D$ records the finite observations attached to that window, and the real seal row $E$ is reached only after $S,R,D$ are displayed.

The naturality row $W$ is the comparison row that keeps these readings together. It is not a host equality assertion; it is the finite statement that the StreamName, RegSeqRat, DyadicRatCore, and RealUp boundary reads are the same transported stationary seed route when inspected through $H,C,P,K$.

Since no excluded object is a coordinate of $N$, the NameCert surface is exhausted by the displayed rows.
\end{proof}

\begin{theorem}[Stationary window naturality boundary comparison]
\label{thm:stationary-window-naturality-boundary-comparison}
For an accepted $\StationaryWindowNaturalityUp$ carrier $N=(Q,S,R,D,E,W,H,C,P,K)$, every stationary finite-window consumer that reaches the $\RealUp$ seal row $E$ must pass through the same seed classifier $Q$, constant $\StreamNameUp$ window $S$, $\RegSeqRatUp$ readback $R$, $\DyadicRatCoreUp$ ledger $D$, naturality row $W$, and structural rows $H,C,P,K$.
\end{theorem}
\begin{proof}
Begin with the route sibling. The stationary diagonal window route exposes the seed-to-window path, so a Real-facing read cannot reach the stream window without first reading $Q$ and $S$.

Next use the regular-Cauchy window fusion sibling. It consumes the displayed stream window as the common source for the regular readback $R$, dyadic ledger $D$, and real seal row $E$.

The comparison row $W$ then records that these boundary readings are not separate analytic choices. They are four readings of the same transported seed route, replayed through the componentwise transports, continuations, package row, and local naming row.
\end{proof}

\begin{theorem}[Stationary window naturality downstream package]
\label{thm:stationary-window-naturality-downstream-package}
The carrier, NameCert obligations, and boundary comparison rows of $\StationaryWindowNaturalityUp$ assemble the finite naturality package consumed by transported stationary diagonal, stationary diagonal window route, Cauchy modulus extraction, regular Cauchy window fusion, and $\RealCompletenessUp$ consumers. The package exports exactly
$$
Q,S,R,D,E,W,H,C,P,K
$$
over $\RatUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\end{theorem}
\begin{proof}
The carrier supplies the seed row, the four finite-window boundary readings, the comparison row, and the structural support rows. The NameCert obligation theorem records these as the complete local naming surface.

The boundary comparison theorem forces every Real-facing read through the same finite route. Thus downstream consumers receive a naturality packet, not independent analytic witnesses for the StreamName, RegSeqRat, DyadicRatCore, and RealUp boundaries.

The listed sibling chapters provide the incoming surfaces. Transported stationary diagonal supplies the seed classifier, stationary diagonal window route supplies the seed-to-window path, Cauchy modulus extraction supplies the finite observation-budget discipline, and regular Cauchy window fusion supplies the shared readback and seal route.
\end{proof}

\begin{theorem}[Stationary window naturality TasteGate readiness]
\label{thm:stationary-window-naturality-tastegate-readiness}
The Lean TasteGate site for $\StationaryWindowNaturalityUp$ must distinguish the naturality comparison row $W$ from the four boundary readings $S,R,D,E$ and must preserve all rows $Q,S,R,D,E,W,H,C,P,K$ for classifier-facing consumers.
\end{theorem}
\begin{proof}
Nontriviality is forced by the packet shape. If $W$ collapsed to any one of $S,R,D,E$, the carrier would lose the comparison row that ties the four boundary readings to the same seed classifier $Q$.

Field faithfulness is the consumer-facing preservation condition. A classifier-facing route may inspect the seed, stream window, regular readback, dyadic ledger, real seal, naturality row, transports, continuations, package provenance, and local naming row, but no unlisted real-completion object.
\end{proof}
\leanchecked{BEDC.Derived.StationaryWindowNaturalityUp.StationaryWindowNaturalityTasteGate\_single\_carrier\_alignment}

\closureat{StationaryWindowNaturalityUp}{seedStr}

\begin{closurestatus}{\StationaryWindowNaturalityUp}
  \constructivestory{A $\StationaryWindowNaturalityUp$ packet is generated from one transported stationary $\RatUp$ seed classifier, its constant $\StreamNameUp$ finite window, the matching $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ observation ledger, $\RealUp$ seal, naturality comparison row, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:stationary-window-naturality-carrier}, \autoref{thm:stationary-window-naturality-namecert-obligations}, \autoref{thm:stationary-window-naturality-boundary-comparison}, and \autoref{thm:stationary-window-naturality-downstream-package} bind the finite naturality packet for transported stationary window readings.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not prove RealUp completeness, quotient-stream equality, selected limits, host real equality, real trichotomy, Lean verification, or a public standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, boundary-reading exactness, naturality comparison preservation, downstream package coverage, and TasteGate row-faithfulness for the same finite packet.}
\end{closurestatus}
