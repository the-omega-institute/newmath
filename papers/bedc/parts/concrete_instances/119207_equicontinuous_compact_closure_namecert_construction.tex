\chapter{A Concrete Naming Certificate for $\EquicontinuousCompactClosureUp$}
\label{ch:concrete-instances-equicontinuous-compact-closure-namecert}
\origin{human}

Following Arzela and Ascoli, the $\EquicontinuousCompactClosureUp$ packet records the finite compact-family closure data needed before an equicontinuous family may be read as closed under compact-source limit operations. It sits after the Arzela--Ascoli surface of \autoref{ch:concrete-instances-arzelaascoli-namecert}, the compact-metric source rows of \autoref{ch:concrete-instances-compactmetric-namecert}, the equicontinuity packet of \autoref{ch:concrete-instances-equicontinuity-namecert}, the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the continuous-map rows of \autoref{ch:concrete-instances-continuousmap-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a compact-family closure route for later calculus and compactness consumers; it does not claim full function-space compactness, selected subsequences, quotient map equality, or ambient completeness.

\begin{definition}[Equicontinuous compact closure carrier]
\label{def:equicontinuous-compact-closure-carrier}
An $\EquicontinuousCompactClosureUp$ carrier is a finite $\BHist$ packet
$$
E=(K,A,Q,U,F,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, including the finite compact windows and metric comparison data through which source points are read;
\item $A$ is the Arzela--Ascoli dependency row, read through the finite selection and compact-family surfaces already named by \autoref{ch:concrete-instances-arzelaascoli-namecert};
\item $Q$ is the $\EquicontinuityUp$ row for the family, exposing shared tolerance and source-radius data rather than separate moduli for each downstream read;
\item $U$ is the $\UniformModulusUp$ handoff row consumed by closure consumers after the compact-source and equicontinuity rows have been displayed;
\item $F$ is the finite $\ContinuousMapUp$ ledger for the displayed family members and their pointwise graph rows;
\item $R$ is the $\RealUp$ seal row for target-side values, endpoint comparisons, and real-valued tolerance reads;
\item $H$ records componentwise $\hsame$ transport for compact source data, Arzela--Ascoli rows, equicontinuity rows, uniform-modulus data, continuous-map rows, Real seals, and local naming;
\item $C$ records displayed $\Cont$ replay for the same dependency route;
\item $P$ is the $\Pkg$ provenance over $\EquicontinuousCompactClosureUp$, $\ArzelaAscoliUp$, $\CompactMetricUp$, $\EquicontinuityUp$, $\UniformModulusUp$, $\ContinuousMapUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\EquicontinuousCompactClosureUp}$ row.
\end{enumerate}
The carrier has no coordinate for host function-space compactness, selected convergent subsequences, quotient equality of maps, choice-selected limits, detached Real values, or ambient compactness and completeness.
\end{definition}

\begin{theorem}[Equicontinuous compact closure NameCert obligations]
\label{thm:equicontinuous-compact-closure-namecert-obligations}
For every accepted $\EquicontinuousCompactClosureUp$ carrier
$$
E=(K,A,Q,U,F,R,H,C,P,N),
$$
the local $\NameCert_{\EquicontinuousCompactClosureUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item compact-source exposure, requiring every closure read to display the $\CompactMetricUp$ row $K$ before any family or Real-facing conclusion is exported;
\item Arzela--Ascoli route exposure, requiring the row $A$ to remain tied to the finite Arzela--Ascoli packet rather than to a host function-space theorem;
\item equicontinuity and uniform-modulus binding, requiring $Q$ and $U$ to be read as one shared compact-family modulus route;
\item continuous-map and Real sealing, requiring $F$ and $R$ to expose the displayed graph rows and terminal Real seals before target-side closure data are read;
\item transport, replay, provenance, and local naming through $H,C,P,N$;
\item ledger non-escape, excluding full function-space compactness, selected subsequences, quotient map equality, choice-selected limits, detached Real pairs, Lean verification, bridge checking, and ambient completeness.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:equicontinuous-compact-closure-carrier}. Its only rows are the compact source, Arzela--Ascoli dependency, equicontinuity surface, uniform-modulus handoff, continuous-map ledger, Real seal, transport, replay, provenance, and local naming.

The compact-family closure read first consumes $K,A,Q,U,F,R$. The compact source row supplies finite source windows, the Arzela--Ascoli row supplies the family-compactness route, the equicontinuity and uniform-modulus rows provide the shared tolerance control, and the continuous-map and Real rows expose only displayed graph and target-value data.

Finally replay $H,C,P,N$. These rows preserve the same finite route under transport and provenance. Since the carrier contains no host function-space compactness, selected subsequence, quotient map equality, choice-selected limit, detached Real row, Lean verification, bridge-checking row, or ambient completeness coordinate, the listed obligations exhaust the local NameCert surface.
\end{proof}

\falsifiablePrediction{Any consumer that exports an $\EquicontinuousCompactClosureUp$ closure read while omitting $K,A,Q,U,F,R,H,C,P$, or the local $\NameCert$ row refutes the carrier of \autoref{def:equicontinuous-compact-closure-carrier}.}

\independenceWitness{arzelaascoli, compactmetric, equicontinuity, uniformmodulus, continuousmap, real: none alone is bijective with $\EquicontinuousCompactClosureUp$ because the packet jointly carries compact-source windows, Arzela--Ascoli family rows, shared equicontinuity data, a uniform-modulus handoff, continuous-map graph rows, Real seals, transport, replay, provenance, and local NameCert.}

\closureat{EquicontinuousCompactClosureUp}{seedStr}

\begin{closurestatus}{\EquicontinuousCompactClosureUp}
  \constructivestory{An $\EquicontinuousCompactClosureUp$ packet is generated from CompactMetric source rows, Arzela--Ascoli dependency rows, Equicontinuity rows, UniformModulus handoff rows, ContinuousMap graph rows, Real seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:equicontinuous-compact-closure-carrier} and \autoref{thm:equicontinuous-compact-closure-namecert-obligations} seed the finite compact-family closure surface over CompactMetric, ArzelaAscoli, Equicontinuity, UniformModulus, ContinuousMap, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close full function-space compactness, selected subsequences, quotient map equality, choice-selected limits, Lean verification, bridge checking, or ambient completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-source exposure, Arzela--Ascoli route exposure, equicontinuity and uniform-modulus binding, continuous-map and Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.EquicontinuousCompactClosureUp.}
\end{closurestatus}
