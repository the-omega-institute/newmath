\chapter{A Concrete Naming Certificate for $\PhysicalFitTowerUp$}
\label{ch:concrete-instances-physical-fit-tower-namecert}
\origin{ai}

The $\PhysicalFitTowerUp$ packet realizes the reality-constrained approximation tower of \autoref{ch:visions-reality-constrained-approximation-towers} as a finite BEDC NameCert carrier. It sits beside the finite real-observation envelope of \autoref{ch:concrete-instances-realapproximationenvelope-namecert}, the exact Real-completion boundary of \autoref{ch:concrete-instances-real-completion-exact-boundary-namecert}, and the dyadic mesh packet of \autoref{ch:concrete-instances-dyadicmesh-namecert}. Those sibling chapters expose mathematical approximation, Cauchy completion, and dyadic sampling surfaces; $\PhysicalFitTowerUp$ binds them to a reality-facing signature bundle, mathematical classifier, hidden-source ledger, descent-row refusal, and local naming row in one concrete packet.

\begin{definition}[PhysicalFitTower carrier]
\label{def:physical-fit-tower-carrier}
A $\PhysicalFitTowerUp$ carrier is a finite $\BHist$ packet
$$
F=(S,C,L,D,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the physical signature bundle row, recording the finite reality-facing observations, response signatures, and admissible reads that license a physical-fit judgment.
\item $C$ is the tower classifier row, recording the mathematical layer, compressed layer, and approximation classifier by which a consumer compares $\RealApproximationEnvelopeUp$, $\RealCompletionExactBoundaryUp$, and $\DyadicMeshUp$ data.
\item $L$ is the hidden-source ledger row, recording source structure, discarded physical response, idealization residue, and open adequacy gaps not transported by the mathematical classifier.
\item $D$ is the descent row. When displayed, it names exactly which operations may descend through the compressed layer; when absent, the corresponding operation is refused at the tower boundary.
\item $N$ is the local $\NameCert_{\PhysicalFitTowerUp}$ row naming the signature bundle, classifier, ledger, descent policy, and finite carrier together.
\end{enumerate}
The carrier has no coordinate for unrestricted passage from mathematical approximation to physical adequacy, no quotient identifying hidden physical residue with completed Real data, and no consumer route that bypasses the signature bundle and ledger.
\end{definition}

\begin{definition}[PhysicalFitTower NameCert surface]
\label{def:physical-fit-tower-namecert-surface}
The local $\NameCert_{\PhysicalFitTowerUp}$ surface for \autoref{def:physical-fit-tower-carrier} consists exactly of five rows:
\begin{enumerate}
\item carrier admission for the finite packet $F=(S,C,L,D,N)$;
\item signature-bundle exposure through $S$, requiring every physical-fit read to display the reality-facing bundle before classifier use;
\item tower-classifier exposure through $C$, requiring every mathematical approximation, completion, or mesh comparison to name the tower layer at which it is asserted;
\item hidden-source ledger exactness through $L$, requiring discarded source structure, idealization residue, and open adequacy gaps to remain visible to consumers;
\item descent and local-name closure through $D$ and $N$, requiring every operation transported across a compressed layer to pass through the displayed descent row and the same local certificate.
\end{enumerate}
These five rows are the complete public obligation surface for treating a mathematical approximation tower as physically fit.
\end{definition}

\begin{theorem}[No free descent through the physical-fit tower]
\label{thm:physical-fit-tower-no-free-descent}
Let $F=(S,C,L,D,N)$ be an accepted $\PhysicalFitTowerUp$ packet. An operation on a mathematical tower layer descends through the compressed layer only when the corresponding descent permission is displayed in $D$. If $D$ contains no row for that operation, then no consumer route in $F$ transports the operation across the compressed layer.
\end{theorem}
\begin{proof}
Read the carrier of \autoref{def:physical-fit-tower-carrier}. The only row that names transport across a compressed layer is $D$.

The NameCert surface of \autoref{def:physical-fit-tower-namecert-surface} requires descent and local-name closure through $D$ and $N$. Thus a consumer route that transports an operation across the compressed layer must exhibit the same descent row before the local name is accepted.

When the operation has no displayed row in $D$, the carrier still exposes the physical signature bundle $S$, mathematical classifier $C$, hidden-source ledger $L$, and local name $N$, but none of those rows grants descent. The requested transport is therefore refused at the tower boundary.
\end{proof}

\begin{theorem}[Physical-fit signature non-escape]
\label{thm:physical-fit-tower-signature-nonescape}
Every accepted $\PhysicalFitTowerUp$ read factors through the finite route
$$
S,C,L,D,N.
$$
In particular, a consumer cannot bypass the reality-facing signature bundle $S$ and hidden-source ledger $L$ by reading only a mathematical classifier, completed Real boundary, or dyadic mesh surface.
\end{theorem}
\begin{proof}
The carrier $F=(S,C,L,D,N)$ exposes the signature bundle and ledger as content rows, not as optional annotations. The NameCert surface requires both signature-bundle exposure and ledger exactness before the local certificate can be read.

The mathematical siblings named by the classifier row provide approximation, completion, and mesh data. They do not supply a physical signature bundle or hidden-source ledger for this carrier unless those rows are displayed inside $F$.

Therefore any accepted physical-fit read must pass through $S$ and $L$ along with $C,D,N$. A route that attempts to classify physical adequacy from $C$ alone has no coordinate in the accepted packet.
\end{proof}

\falsifiablePrediction{Every accepted PhysicalFitTowerUp packet with a missing descent row admits no consumer route that transports the corresponding operation across the compressed layer.}

\independenceWitness{realapproximationenvelope, realcompletionexactboundary, dyadicmesh, observationscheduleenvelope, compactuniformcontinuity: none is bijective because PhysicalFitTowerUp alone binds physical signature bundle, mathematical classifier, hidden-source ledger, and descent-row refusal in one carrier.}

The Lean follow-up in \texttt{lean4/BEDC/Derived/PhysicalFitTowerUp/TasteGate.lean} should provide explicit $\Nontrivial\ \PhysicalFitTowerUp$ and $\FieldFaithful\ \PhysicalFitTowerUp$ instances, together with the future target \texttt{BEDC.Derived.PhysicalFitTowerUp.TasteGate.PhysicalFitTowerTasteGate\_single\_carrier\_alignment}.

\closureat{PhysicalFitTowerUp}{seedStr}

\begin{closurestatus}{\PhysicalFitTowerUp}
  \constructivestory{A $\PhysicalFitTowerUp$ packet is generated from a finite physical signature bundle, mathematical tower classifier, hidden-source ledger, descent policy row, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:physical-fit-tower-carrier}, \autoref{def:physical-fit-tower-namecert-surface}, \autoref{thm:physical-fit-tower-no-free-descent}, and \autoref{thm:physical-fit-tower-signature-nonescape} bind the finite physical-fit tower carrier, five-row NameCert surface, descent refusal, and signature non-escape boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close Lean verification, bridge checking, total physical adequacy, unrestricted mathematical descent, host physical law, or quotient identity between Real-completion data and hidden source structure.}
  \upgradepath{Obligation closure requires checked carrier admission, signature-bundle exposure, tower-classifier exposure, ledger exactness, descent refusal, signature non-escape, TasteGate nontriviality, and field faithfulness for $S,C,L,D,N$.}
\end{closurestatus}
