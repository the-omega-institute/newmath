\chapter{A Concrete Naming Certificate for $\RegularCauchySelectorBudgetUp$}
\label{ch:concrete-instances-regularcauchyselectorbudget-namecert}
\origin{human}

The $\RegularCauchySelectorBudgetUp$ packet records the finite selector-budget step used by the Real completeness diagonal construction. It binds a displayed regular Cauchy modulus, the selected finite window budget, StreamName observation rows, a RegSeqRat witness row, the RealUp seal handoff, and the BEDC transport rows as one inspectable naming certificate. It is a local finite packet between modulus selection and seal delivery; it is not a host limit, quotient stream equality, countable choice principle, or ambient completeness theorem.

\begin{definition}[RegularCauchySelectorBudget carrier]
\label{def:regular-cauchy-selector-budget-carrier}
A $\RegularCauchySelectorBudgetUp$ carrier is a finite $\BHist$ packet
$$
Q=(\mu,W,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $\mu$ is the selector-modulus row read from the regular Cauchy input at the requested precision;
\item $W$ is the finite window-budget row naming the selected indices and StreamName observation extent allowed by $\mu$;
\item $S$ is the finite family of $\StreamNameUp$ observation rows opened by the selected window budget;
\item $R$ is the $\RegSeqRatUp$ witness row assembled from those observations for the diagonal handoff;
\item $E$ is the $\RealUp$ seal row through which the completion-facing consumer reads the selected regular witness;
\item $H$ records componentwise $\hsame$ transports for the modulus, window budget, StreamName rows, RegSeqRat witness, RealUp seal, and local structural rows;
\item $C$ records the displayed $\Cont$ routes for modulus inspection, window coverage, observation readback, RegSeqRat handoff, RealUp seal delivery, and non-escape inspection;
\item $P$ is the $\Pkg$ provenance over $\RegSeqRatUp$, $\StreamNameUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RegularCauchySelectorBudgetUp}$ row.
\end{enumerate}
The local classifier compares accepted carriers only through the visible rows $\mu,W,S,R,E,H,C,P,N$.
\end{definition}

\begin{theorem}[RegularCauchySelectorBudget NameCert obligations]
\label{thm:regular-cauchy-selector-budget-namecert-obligations}
The local $\NameCert_{\RegularCauchySelectorBudgetUp}$ surface for \autoref{def:regular-cauchy-selector-budget-carrier} consists of carrier admission, selector-modulus exactness, finite window-budget coverage, StreamName observation readback, $\RegSeqRatUp$ witness handoff, $\RealUp$ seal boundary, componentwise $\hsame$ stability, displayed $\Cont$ route stability, $\Pkg$ provenance, and non-escape from host limits, quotient streams, countable choice, hidden window enlargement, and ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchySelectorBudgetUp.RegularCauchySelectorBudgetCarrier\_namecert\_obligations}
\begin{proof}
Carrier admission is the tuple in \autoref{def:regular-cauchy-selector-budget-carrier}. It displays the selector modulus, finite window budget, StreamName rows, RegSeqRat witness, RealUp seal, transports, routes, provenance, and local naming row.

Selector-modulus exactness and window-budget coverage are read from $\mu$ and $W$. The StreamName observations $S$ and the RegSeqRat witness $R$ are listed after those rows, so the witness route cannot bypass the selected finite budget.

The RealUp seal boundary is the role of $E$, while the structural obligations are the roles of $H,C,P,N$. Since the carrier has no coordinate for a host limit, quotient stream equality, countable choice, hidden window enlargement, or ambient completeness, non-escape follows by inspection of the displayed packet.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget window coverage]
\label{thm:regular-cauchy-selector-budget-window-coverage}
Every accepted $\RegularCauchySelectorBudgetUp$ carrier covers its diagonal observation request through the displayed finite window budget $W$. For
$$
Q=(\mu,W,S,R,E,H,C,P,N),
$$
each StreamName row in $S$ is opened only inside the indices and observation extent named by $W$, and the budget $W$ is itself read from the selector modulus row $\mu$.
\end{theorem}
\begin{proof}
Project the rows $\mu$ and $W$ from the carrier. The window budget is an explicit coordinate whose role is to name the finite indices and observation extent admitted by the selected modulus route.

The StreamName rows $S$ are listed after $W$ and are consumed by the same continuation data. Thus an accepted observation row is available only as part of the selected finite budget.

The remaining rows $R,E,H,C,P,N$ consume the displayed observations. No row permits an unbounded stream scan or a second selector, so window coverage is confined to $W$.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget RegSeqRat handoff]
\label{thm:regular-cauchy-selector-budget-regseqrat-handoff}
Every $\RegSeqRatUp$ witness exported by an accepted $\RegularCauchySelectorBudgetUp$ carrier factors through the finite route
$$
\mu,W,S,R,H,C,P,N.
$$
The handoff preserves the selected modulus, finite window budget, StreamName observation rows, componentwise $\hsame$ transports, displayed $\Cont$ routes, package provenance, and local naming row.
\end{theorem}
\begin{proof}
Use \autoref{thm:regular-cauchy-selector-budget-window-coverage} to identify the selected modulus and finite window budget. This fixes $\mu,W,S$ as the only observation data available to the witness route.

The witness row $R$ is listed after the observations and is routed through the same structural rows. Therefore every exported $\RegSeqRatUp$ witness consumes the displayed StreamName packet before it can be read by a downstream consumer.

Inspect $H,C,P,N$. These rows keep the handoff stable under $\hsame$ transport, continuation replay, package provenance, and local naming, so the RegSeqRat handoff is exactly the displayed finite route.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget Real seal boundary]
\label{thm:regular-cauchy-selector-budget-real-seal-boundary}
Every Real completeness consumer of an accepted $\RegularCauchySelectorBudgetUp$ carrier reaches $\RealUp$ only through the seal row $E$ after the $\RegSeqRatUp$ witness row $R$ has been assembled from the selected StreamName window budget. The carrier exports no host-selected limit, quotient stream equality, countable choice selector, hidden observation budget, or ambient completeness witness.
\end{theorem}
\begin{proof}
Project the seal row $E$ from the carrier. It appears after the RegSeqRat witness $R$, so a real-facing route reaches $\RealUp$ only after the finite witness handoff has been assembled.

Apply \autoref{thm:regular-cauchy-selector-budget-regseqrat-handoff}. The witness $R$ is already bound to the modulus, window budget, StreamName observations, transports, routes, provenance, and local naming row.

The carrier contains no coordinate for a host-selected limit, quotient stream equality, countable choice selector, hidden observation budget, or ambient completeness. Hence the RealUp seal is a boundary around the displayed finite packet and exports no additional real object.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget non-escape]
\label{thm:regular-cauchy-selector-budget-nonescape}
An accepted $\RegularCauchySelectorBudgetUp$ carrier cannot be used to recover data outside the displayed tuple $\mu,W,S,R,E,H,C,P,N$. In particular, it does not produce an unbounded family of StreamName observations, a second modulus selector, a quotient of regular sequences, a host real limit, or a completeness principle for $\RealUp$.
\end{theorem}
\begin{proof}
Read the carrier coordinates from \autoref{def:regular-cauchy-selector-budget-carrier}. Every exported route is one of the displayed finite rows, with $H,C,P,N$ providing only transport, continuation, provenance, and naming structure for those rows.

The window coverage theorem confines StreamName observations to $W$, and the RegSeqRat handoff confines the witness to $\mu,W,S,R,H,C,P,N$. The Real seal boundary then confines completion-facing reads to $E$ after that finite witness route.

Since no additional coordinate is present, the packet has no source for unbounded observations, a second selector, quotient regular sequences, host real limits, or a RealUp completeness principle.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget obligation closure]
\label{thm:regular-cauchy-selector-budget-obligation-closure}
The accepted $\RegularCauchySelectorBudgetUp$ packet
$$
Q=(\mu,W,S,R,E,H,C,P,N)
$$
closes the local selector-budget obligation surface by packaging the carrier admission of \autoref{def:regular-cauchy-selector-budget-carrier}, the finite window coverage of \autoref{thm:regular-cauchy-selector-budget-window-coverage}, the $\RegSeqRatUp$ handoff of \autoref{thm:regular-cauchy-selector-budget-regseqrat-handoff}, the $\RealUp$ seal boundary of \autoref{thm:regular-cauchy-selector-budget-real-seal-boundary}, and the non-escape clause of \autoref{thm:regular-cauchy-selector-budget-nonescape} as one finite $\BHist$ certificate.
\end{theorem}
\begin{proof}
Carrier admission supplies the displayed tuple $Q$. The selector-modulus row $\mu$ and finite window row $W$ determine which StreamName observations $S$ are available.

Apply \autoref{thm:regular-cauchy-selector-budget-window-coverage}. It confines the observation rows to the selected finite budget and rules out hidden enlargement of the StreamName read.

Apply \autoref{thm:regular-cauchy-selector-budget-regseqrat-handoff} and \autoref{thm:regular-cauchy-selector-budget-real-seal-boundary}. These rows force the RegSeqRat witness and RealUp seal to be downstream of the same finite packet.

Finally use \autoref{thm:regular-cauchy-selector-budget-nonescape}. Since no coordinate outside $\mu,W,S,R,E,H,C,P,N$ is present, the listed rows exhaust the local selector-budget obligations.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget formal target surface]
\label{thm:regular-cauchy-selector-budget-formal-target-surface}
The Lean-facing target surface for $\RegularCauchySelectorBudgetUp$ is the finite $\BHist$ carrier $Q=(\mu,W,S,R,E,H,C,P,N)$ together with componentwise $\hsame$ rows in $H$, displayed $\Cont$ routes in $C$, $\Pkg$ provenance in $P$, and the local $\NameCert_{\RegularCauchySelectorBudgetUp}$ row $N$. A formalization of this surface must expose exactly the selector-modulus row, finite window budget, StreamName observation rows, $\RegSeqRatUp$ witness, $\RealUp$ seal, transport rows, route rows, provenance row, and naming row, with no host limit or quotient stream coordinate.
\end{theorem}
\begin{proof}
Read the rows of \autoref{def:regular-cauchy-selector-budget-carrier}. The first five coordinates carry the selector modulus, finite window budget, StreamName observations, RegSeqRat witness, and RealUp seal.

The structural coordinates are also displayed. The row $H$ supplies componentwise $\hsame$ stability, $C$ supplies continuation replay, $P$ supplies package provenance, and $N$ supplies the local naming certificate.

Thus a Lean target can be stated over the same finite $\BHist$ packet. Because the carrier has no coordinate for host limits, quotient streams, or ambient completeness, those objects cannot appear in the formal target surface.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget verification handoff]
\label{thm:regular-cauchy-selector-budget-verification-handoff}
Downstream Real and Completion formalization consumes a $\RegularCauchySelectorBudgetUp$ packet only through the finite route
$$
\mu,W,S,R,E,H,C,P,N.
$$
The consumer first reads the selector modulus and finite window budget, then reads the StreamName observations, then assembles the $\RegSeqRatUp$ witness, and only then reaches the $\RealUp$ seal. The handoff supplies no countable choice selector, quotient of regular sequences, hidden observation budget, host-selected real limit, or ambient RealUp completeness theorem.
\end{theorem}
\begin{proof}
Start with the obligation closure theorem. It packages carrier admission, window coverage, RegSeqRat handoff, RealUp seal boundary, and non-escape into the single finite packet $Q$.

The formal target surface theorem names the rows that Lean must expose. These are exactly the rows consumed by the downstream Real and Completion route.

Reading the rows in order gives the handoff discipline: $\mu$ and $W$ select the finite observation budget, $S$ supplies the observations, $R$ supplies the regular-rational witness, and $E$ supplies the seal. The non-escape theorem removes countable choice, quotient regular sequences, hidden budgets, host real limits, and ambient completeness from the handoff.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget consumer exactness]
\label{thm:regular-cauchy-selector-budget-consumer-exactness}
Every $\RegSeqRatUp$ or $\RealUp$ consumer of an accepted $\RegularCauchySelectorBudgetUp$ carrier
$$
Q=(\mu,W,S,R,E,H,C,P,N)
$$
reads exactly the displayed finite route $\mu,W,S,R,E,H,C,P,N$. The $\RegSeqRatUp$ consumer surface stops at the witness row $R$ together with the selector modulus, finite window budget, StreamName rows, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row. The $\RealUp$ consumer surface reaches the seal row $E$ only after the same $\RegSeqRatUp$ witness route has been read, and it receives no additional host limit, quotient stream equality, countable choice selector, hidden window budget, or ambient completeness witness.
\end{theorem}
\begin{proof}
Start with the carrier definition. The tuple $Q$ displays exactly the selector modulus, finite window budget, StreamName observation rows, RegSeqRat witness, RealUp seal, transports, routes, provenance, and local naming row.

Apply \autoref{thm:regular-cauchy-selector-budget-window-coverage} and \autoref{thm:regular-cauchy-selector-budget-regseqrat-handoff}. These identify the $\RegSeqRatUp$ consumer route with $\mu,W,S,R,H,C,P,N$ and force the witness row to consume only the selected finite window.

Next apply \autoref{thm:regular-cauchy-selector-budget-real-seal-boundary}. The $\RealUp$ consumer reaches $E$ only after the RegSeqRat witness has been assembled through that same finite route.

Finally use \autoref{thm:regular-cauchy-selector-budget-nonescape}, \autoref{thm:regular-cauchy-selector-budget-obligation-closure}, and \autoref{thm:regular-cauchy-selector-budget-verification-handoff}. These rows exhaust the packet surface and exclude every route outside $\mu,W,S,R,E,H,C,P,N$.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget diagonal use]
\label{thm:regular-cauchy-selector-budget-diagonal-use}
Every diagonal Real-completion consumer of an accepted $\RegularCauchySelectorBudgetUp$ packet
$$
Q=(\mu,W,S,R,E,H,C,P,N)
$$
must first read the selector modulus $\mu$, finite window budget $W$, $\StreamNameUp$ observations $S$, $\RegSeqRatUp$ witness $R$, and $\RealUp$ seal $E$ through the displayed $H,C,P,N$ rows. No diagonal consumer may substitute a host limit, quotient-stream equality, countable choice selector, hidden window enlargement, or ambient completeness witness for those finite rows.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:regular-cauchy-selector-budget-window-coverage}. It binds every observation request to the selected finite window budget $W$ read from the selector modulus $\mu$.

Apply \autoref{thm:regular-cauchy-selector-budget-regseqrat-handoff}. The $\RegSeqRatUp$ witness $R$ is assembled only after the StreamName observations $S$ have been read through the same transport, route, provenance, and naming rows.

Next use \autoref{thm:regular-cauchy-selector-budget-real-seal-boundary}. The $\RealUp$ seal $E$ is available only after that finite RegSeqRat route has been displayed.

Finally apply \autoref{thm:regular-cauchy-selector-budget-nonescape} and \autoref{thm:regular-cauchy-selector-budget-consumer-exactness}. These results exhaust the diagonal consumer surface at $\mu,W,S,R,E,H,C,P,N$ and exclude host limits, quotient-stream equality, countable choice selectors, hidden window enlargement, and ambient completeness witnesses.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget tail selector compatibility]
\label{thm:regular-cauchy-selector-budget-tail-selector-compatibility}
Every tail-selector read through an accepted $\RegularCauchySelectorBudgetUp$ carrier is compatible with the same selector modulus $\mu$ and finite window budget $W$. A tail row may inspect only StreamName observations inside $S$, may assemble its regular rational witness only through $R$, and may reach any Real-facing seal only through $E$ after the route $\mu,W,S,R,H,C,P,N$ has been displayed.
\end{theorem}
\begin{proof}
Read the selector rows $\mu$ and $W$ from \autoref{def:regular-cauchy-selector-budget-carrier}. The window coverage theorem makes $W$ the selected finite observation budget determined by $\mu$.

A tail-selector read is therefore a consumer of the displayed StreamName rows $S$. Apply \autoref{thm:regular-cauchy-selector-budget-regseqrat-handoff} to force the corresponding regular rational witness through $R$ and the structural rows $H,C,P,N$.

For a Real-facing tail read, apply \autoref{thm:regular-cauchy-selector-budget-real-seal-boundary}. The seal $E$ is available only after the same finite RegSeqRat witness route has been consumed.

The non-escape and verification handoff theorems rule out a second selector modulus, a larger window, or an independent tail budget. Thus every tail-selector read remains compatible with $\mu$ and $W$.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget Real completeness handoff]
\label{thm:regular-cauchy-selector-budget-real-completeness-handoff}
A RealCompleteness handoff that consumes an accepted $\RegularCauchySelectorBudgetUp$ packet may use only the selector modulus $\mu$, the finite window budget $W$, the StreamName rows $S$, the $\RegSeqRatUp$ witness row $R$, the $\RealUp$ seal row $E$, componentwise $\hsame$ transports $H$, displayed $\Cont$ routes $C$, $\Pkg$ provenance $P$, and the local $\NameCert_{\RegularCauchySelectorBudgetUp}$ row $N$. It does not receive a quotient stream, a host-selected real limit, countable choice, hidden window enlargement, or an ambient RealUp completeness theorem from the packet.
\end{theorem}
\begin{proof}
Project the carrier $Q=(\mu,W,S,R,E,H,C,P,N)$. These are the only rows available to a RealCompleteness handoff from the selector-budget packet.

Use \autoref{thm:regular-cauchy-selector-budget-window-coverage} to bind $S$ to the finite window budget $W$ selected by $\mu$. Use \autoref{thm:regular-cauchy-selector-budget-regseqrat-handoff} to bind $R$ to those StreamName observations and to the structural rows.

Use \autoref{thm:regular-cauchy-selector-budget-real-seal-boundary} to make $E$ the only Real-facing seal and to place it after the RegSeqRat witness route. The rows $H,C,P,N$ then provide only transport, continuation replay, provenance, and local naming for that same packet.

Finally apply \autoref{thm:regular-cauchy-selector-budget-nonescape}, \autoref{thm:regular-cauchy-selector-budget-obligation-closure}, and \autoref{thm:regular-cauchy-selector-budget-verification-handoff}. The handoff surface is exhausted by the displayed packet, so RealCompleteness receives no additional completion principle or hidden real object from this chapter.
\end{proof}

\begin{definition}[RegularCauchySelectorBudget tail-precision source]
\label{def:regular-cauchy-selector-budget-tail-precision-source}
The tail-precision source of an accepted $\RegularCauchySelectorBudgetUp$ carrier is the finite row package
$$
\mathsf{TailPrec}(Q)=(\mu,W,S,R,D,E,H,C,P,N)
$$
where $\mu$ is the selector-modulus row, $W$ is the selected finite window budget, $S$ is the StreamName observation family, $R$ is the $\RegSeqRatUp$ witness row, $D$ is the $\DyadicRatCoreUp$ tolerance row requested by the tail consumer, $E$ is the $\RealUp$ seal, $H$ records componentwise $\hsame$ transports, $C$ records displayed $\Cont$ routes, $P$ records $\Pkg$ provenance, and $N$ is the local $\NameCert$ source.
\end{definition}

\begin{theorem}[RegularCauchySelectorBudget tail-precision exactness]
\label{thm:regular-cauchy-selector-budget-tail-precision-exactness}
For an accepted $\RegularCauchySelectorBudgetUp$ packet with tail-precision source
$$
\mathsf{TailPrec}(Q)=(\mu,W,S,R,D,E,H,C,P,N),
$$
the dyadic tolerance row $D$ determines the only permitted finite window budget through $\mu,W$. Every StreamName observation read is inside $S$, every $\RegSeqRatUp$ witness read factors through $R$, and every $\RealUp$ consumer reaches only $E$ after the route
$$
D \longrightarrow \mu \longrightarrow W \longrightarrow S \longrightarrow R \longrightarrow E
$$
with transport, replay, provenance, and local naming supplied by $H,C,P,N$. No accepted route exports a quotient stream, host real equality, chosen limit, infinite tail, hidden window enlargement, or ambient completeness witness.
\end{theorem}
\begin{proof}
Project the finite tail-precision tuple. The tolerance row $D$, selector-modulus row $\mu$, and finite window row $W$ are explicit coordinates of \autoref{def:regular-cauchy-selector-budget-tail-precision-source}, so a precision read has no source for a second tolerance-to-window route.

Apply \autoref{thm:regular-cauchy-selector-budget-window-coverage} and \autoref{thm:regular-cauchy-selector-budget-tail-budget-totality}. These bind the selected StreamName observations to the finite budget chosen through $\mu,W$ and force the tail consumer to pass through $D,\mu,W,S,R,E,H,C,P,N$.

Next use \autoref{thm:regular-cauchy-selector-budget-regseqrat-handoff}. The $\RegSeqRatUp$ witness row is downstream of the displayed StreamName observations, so every regular-rational witness factors through $R$ together with the same structural rows.

Finally apply \autoref{thm:regular-cauchy-selector-budget-precision-window-nonescape} and \autoref{thm:regular-cauchy-selector-budget-real-seal-nonescape-package}. The Real-facing read reaches only the seal row $E$ after the finite route has been displayed, and the excluded quotient, host equality, chosen-limit, infinite-tail, hidden-budget, and ambient-completeness data are not coordinates of the packet.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget tail budget totality]
\label{thm:regular-cauchy-selector-budget-tail-budget-totality}
Every accepted $\RegularCauchySelectorBudgetUp$ packet exposes the requested tail precision and its selected finite window through the rows of \autoref{def:regular-cauchy-selector-budget-tail-precision-source}. A tail consumer may read the $\DyadicRatCoreUp$ tolerance row $D$, the selector modulus $\mu$, and the finite budget $W$, and may assemble the corresponding StreamName and $\RegSeqRatUp$ evidence only through $S,R,H,C,P,N$ before reaching the $\RealUp$ seal $E$.
\end{theorem}
\begin{proof}
Project the accepted carrier and its tail-precision source. The rows $\mu,W,D$ display the requested precision interface: $D$ names the tolerance row, $\mu$ names the selector modulus, and $W$ names the finite window selected for that tolerance.

Use \autoref{thm:regular-cauchy-selector-budget-window-coverage}. It confines every StreamName observation in $S$ to the finite window budget $W$ selected by $\mu$.

Use \autoref{thm:regular-cauchy-selector-budget-regseqrat-handoff} and \autoref{thm:regular-cauchy-selector-budget-real-completeness-handoff}. The witness row $R$ and seal row $E$ are downstream of the same finite route, while $H,C,P,N$ provide transport, continuation, provenance, and local naming for the displayed precision source.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget precision-window non-escape]
\label{thm:regular-cauchy-selector-budget-precision-window-nonescape}
No $\RealUp$ consumer reading \autoref{def:regular-cauchy-selector-budget-tail-precision-source} can recover quotient streams, host equality of regular sequences, chosen limits, infinite tails, or ambient completeness. The packet exposes only the tolerance row $D$, selector modulus $\mu$, finite window budget $W$, StreamName observations $S$, $\RegSeqRatUp$ witness $R$, $\RealUp$ seal $E$, and the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\end{theorem}
\begin{proof}
Start with \autoref{def:regular-cauchy-selector-budget-tail-precision-source}. The listed rows are the whole tail-precision source, and none is a quotient stream, host equality proof, chosen limit, infinite tail, or ambient completeness object.

Apply \autoref{thm:regular-cauchy-selector-budget-tail-budget-totality}. It makes every precision read pass through $D,\mu,W,S,R,E,H,C,P,N$ and keeps the selected observation window finite.

Finally apply \autoref{thm:regular-cauchy-selector-budget-nonescape} and \autoref{thm:regular-cauchy-selector-budget-real-completeness-handoff}. These boundaries already exclude quotient streams, host-selected limits, hidden window enlargement, and completion principles from the selector-budget packet, so the tail-precision source exports no stronger RealUp data.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget tail-meet factorization]
\label{thm:regular-cauchy-selector-budget-tail-meet-factorization}
Let $Q$ be an accepted $\RegularCauchySelectorBudgetUp$ packet with tail-precision source
$$
\mathsf{TailPrec}(Q)=(\mu,W,S,R,D,E,H,C,P,N).
$$
Let $T$ be an accepted $\RegularCauchyTailMeetUp$ packet from \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert} whose shared-tail presentation uses the same displayed tolerance row $D$, finite window rows $W$, regular witness row $R$, componentwise transports $H$, displayed routes $C$, provenance $P$, and local naming rows. Then every $\RealUp$ or $\RegularCauchyTailFusionUp$ consumer of the selector-budget tail source factors through the TailMeet shared-threshold row of $T$ before reaching the $\RealUp$ seal $E$.

The factorization exports no quotient stream, host real equality, chosen limit, infinite tail, ambient completeness row, or hidden threshold oracle.
\end{theorem}
\begin{proof}
Start with the tail-precision source in \autoref{def:regular-cauchy-selector-budget-tail-precision-source}. Its only selector-budget precision coordinates are $D,\mu,W,S,R,E,H,C,P,N$.

Apply \autoref{thm:regular-cauchy-selector-budget-tail-budget-totality}. It makes every admitted tail read pass through the displayed tolerance row, selector modulus, finite window budget, StreamName observations, regular witness, transports, routes, provenance, and local name before any seal read is available.

Use \autoref{thm:regular-cauchy-tail-meet-consumer-exactness} for the accepted $\RegularCauchyTailMeetUp$ packet $T$. Since the tolerance, window, witness, transport, route, provenance, and naming rows are shared with $\mathsf{TailPrec}(Q)$, the TailMeet consumer route supplies the shared-threshold row as the common finite threshold packet for the $\RealUp$ and $\RegularCauchyTailFusionUp$ consumers.

Now apply \autoref{thm:regular-cauchy-selector-budget-precision-window-nonescape} together with \autoref{thm:regular-cauchy-tail-meet-shared-threshold-nonescape}. The selector-budget side cannot bypass $D,\mu,W,S,R,E,H,C,P,N$, and the TailMeet side cannot turn the shared threshold into host equality, a selected limit, an infinite tail, ambient completeness, or a hidden oracle.

Thus every consumer route first passes through the TailMeet shared-threshold row and only then reaches the selector-budget real seal $E$. The resulting route is a finite factorization through the displayed packet rows, with none of the excluded quotient, host, selection, tail, completeness, or oracle data exported.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget tail-meet threshold coverage]
\label{thm:regular-cauchy-selector-budget-tail-meet-threshold-coverage}
Let $Q$ be an accepted $\RegularCauchySelectorBudgetUp$ packet with tail-precision source
$$
\mathsf{TailPrec}(Q)=(\mu,W,S,R,D,E,H,C,P,N).
$$
Let $T$ be an accepted $\RegularCauchyTailMeetUp$ packet whose shared-threshold route transports the same tolerance row $D$, finite window budget $W$, StreamName observations $S$, $\RegSeqRatUp$ witness $R$, $\RealUp$ seal $E$, componentwise transports $H$, displayed routes $C$, provenance $P$, and local naming rows. Then every Real-facing read of $Q$ is covered by the TailMeet shared-threshold row before $E$ is read.
\end{theorem}
\begin{proof}
First project the tail-precision source. The displayed selector-budget rows are exactly $\mu,W,S,R,D,E,H,C,P,N$, so a Real-facing read has no source outside this finite packet.

Use \autoref{thm:regular-cauchy-selector-budget-tail-budget-totality} and \autoref{thm:regular-cauchy-selector-budget-precision-window-nonescape}. They force the tolerance row, selector modulus, finite window budget, observations, regular witness, seal, transports, routes, provenance, and name to remain visible before the seal can be consumed.

Now apply \autoref{thm:regular-cauchy-selector-budget-tail-meet-factorization}. The transported tolerance, window, witness, structural, provenance, and naming rows are the rows consumed by $T$.

Finally use \autoref{thm:regular-cauchy-tail-meet-consumer-exactness} and \autoref{thm:regular-cauchy-tail-meet-shared-threshold-nonescape}. The TailMeet packet supplies the shared threshold as the finite ledger reached before any Real-facing read, and it exports no hidden threshold oracle or ambient completeness data.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget tail-meet route determinacy]
\label{thm:regular-cauchy-selector-budget-tail-meet-route-determinacy}
Two selector-budget reads using the same accepted $\RegularCauchyTailMeetUp$ packet and the same transported $D,W,S,R,H,C,P,N$ rows reach the same shared dyadic threshold ledger. The determinacy is internal to the selector-budget tail source, the TailMeet shared-threshold route, and the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\end{theorem}
\begin{proof}
Start with the two selector-budget reads and project their tail-precision sources. The hypothesis identifies the tolerance row $D$, finite window budget $W$, StreamName observations $S$, regular witness $R$, transports $H$, routes $C$, provenance $P$, and naming rows $N$ componentwise.

Apply \autoref{thm:regular-cauchy-selector-budget-tail-meet-threshold-coverage}. Both reads must pass through the TailMeet shared threshold before any Real-facing seal read can occur.

Use \autoref{thm:regular-cauchy-tail-meet-shared-threshold-route-determinacy}. The transported source, window, and modulus data in the TailMeet route determine one displayed dyadic threshold ledger.

The selector-budget factorization theorem then repacks that ledger for both reads. Since the structural rows are the same transported $H,C,P,N$ rows, the two selector-budget reads reach the same shared threshold entry.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget tail-meet non-escape]
\label{thm:regular-cauchy-selector-budget-tail-meet-nonescape}
The composite $\RegularCauchySelectorBudgetUp$ and $\RegularCauchyTailMeetUp$ route exports no quotient stream, host real equality, chosen limit, infinite tail, ambient completeness principle, or hidden threshold oracle. Its public data are only the selector-budget tail source rows $\mu,W,S,R,D,E,H,C,P,N$ and the TailMeet shared-threshold rows reached through the same displayed transports, routes, provenance, and local names.
\end{theorem}
\begin{proof}
Begin on the selector-budget side. The precision-window non-escape theorem \autoref{thm:regular-cauchy-selector-budget-precision-window-nonescape} excludes quotient streams, host equality of regular sequences, chosen limits, infinite tails, and ambient completeness from $\mathsf{TailPrec}(Q)$.

Move to the shared-threshold side through \autoref{thm:regular-cauchy-selector-budget-tail-meet-factorization}. The factorization routes admitted consumers into the TailMeet shared threshold rather than into an external completion or selection object.

Finally apply \autoref{thm:regular-cauchy-tail-meet-shared-threshold-nonescape} and \autoref{thm:regular-cauchy-tail-meet-shared-threshold-route-determinacy}. The TailMeet ledger has no hidden threshold oracle, and duplicate transported reads reach the same displayed dyadic ledger. Therefore the composite route exports only the named finite rows and none of the excluded quotient, host, selection, tail, completeness, or oracle data.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget selector-budget exactness]
\label{thm:regular-cauchy-selector-budget-selector-budget-exactness}
Let $Q=(\mu,W,S,R,E,H,C,P,N)$ be an accepted $\RegularCauchySelectorBudgetUp$ carrier. Every $\StreamNameUp$ observation row in $S$ and every $\RegSeqRatUp$ witness row read through $R$ is admitted only through the selected pair $(\mu,W)$: $\mu$ supplies the selector-modulus row, $W$ supplies the finite observation budget selected by that row, and no accepted consumer may substitute a second modulus, a larger window, or an unlisted observation family.
\end{theorem}
\begin{proof}
Start with the carrier definition. The tuple displays $\mu$ before $W$, then lists the StreamName observations $S$ and the RegSeqRat witness $R$ downstream of those selector rows.

Apply \autoref{thm:regular-cauchy-selector-budget-window-coverage}. It binds every StreamName observation to the finite indices and observation extent named by $W$, with $W$ itself read from $\mu$.

Apply \autoref{thm:regular-cauchy-selector-budget-regseqrat-handoff}. The witness row $R$ consumes the same displayed StreamName packet through $H,C,P,N$, so the RegSeqRat route has no source outside the selected pair.

Finally use \autoref{thm:regular-cauchy-selector-budget-tail-meet-factorization} and \autoref{thm:regular-cauchy-selector-budget-tail-meet-nonescape}. Tail-facing reads factor through the same finite rows and export no hidden threshold oracle, second selector, or enlarged observation window.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget finite-window provenance]
\label{thm:regular-cauchy-selector-budget-finite-window-provenance}
For every accepted $\RegularCauchySelectorBudgetUp$ carrier with tail-precision source
$$
\mathsf{TailPrec}(Q)=(\mu,W,S,R,D,E,H,C,P,N),
$$
the provenance row $P$ must name exactly the dependencies consumed by the finite selector route: $\DyadicRatCoreUp$ through $D$, $\StreamNameUp$ through $S$, $\RegSeqRatUp$ through $R$, $\RealUp$ through $E$, and the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the tail-precision source. The row $D$ supplies the dyadic tolerance, while $\mu,W,S,R,E,H,C,P,N$ supply the selected modulus, finite window, observations, witness, seal, transport, continuation, provenance, and local naming rows.

Use \autoref{thm:regular-cauchy-selector-budget-tail-budget-totality}. It forces every tail read to pass through $D,\mu,W,S,R,E,H,C,P,N$ before the Real seal can be reached.

Use \autoref{thm:regular-cauchy-selector-budget-selector-budget-exactness} and \autoref{thm:regular-cauchy-selector-budget-regseqrat-handoff}. These identify the StreamName and RegSeqRat components as downstream of the selected finite window route.

Finally apply \autoref{thm:regular-cauchy-selector-budget-real-seal-boundary}, \autoref{thm:regular-cauchy-selector-budget-tail-meet-factorization}, and \autoref{thm:regular-cauchy-selector-budget-tail-meet-nonescape}. The Real seal and TailMeet rows consume the same finite provenance surface, so $P$ has no additional dependency to name.
\end{proof}

\begin{theorem}[RegularCauchySelectorBudget Real seal non-escape package]
\label{thm:regular-cauchy-selector-budget-real-seal-nonescape-package}
A Real-facing consumer of an accepted $\RegularCauchySelectorBudgetUp$ packet may read only the displayed rows $\mu,W,S,R,E,H,C,P,N$. It receives the Real seal boundary through $E$ and the structural route through $H,C,P,N$, but it receives no quotient stream, host limit, countable choice selector, hidden window enlargement, ambient completeness principle, or unlisted TailMeet threshold.
\end{theorem}
\begin{proof}
Read the carrier rows from \autoref{def:regular-cauchy-selector-budget-carrier}. The only Real-facing coordinate is the seal row $E$, and it is packaged with the selected finite rows $\mu,W,S,R,H,C,P,N$.

Apply \autoref{thm:regular-cauchy-selector-budget-real-seal-boundary}. The consumer reaches $\RealUp$ only after the RegSeqRat witness has been assembled from the selected StreamName budget.

Apply \autoref{thm:regular-cauchy-selector-budget-nonescape} and \autoref{thm:regular-cauchy-selector-budget-selector-budget-exactness}. These rows exclude quotient streams, host limits, countable choice, second selectors, and hidden window enlargement from the packet surface.

Finally use \autoref{thm:regular-cauchy-selector-budget-tail-meet-factorization} and \autoref{thm:regular-cauchy-selector-budget-tail-meet-nonescape}. TailMeet interaction remains a finite factorization over displayed rows and contributes no ambient completeness principle or unlisted threshold object.
\end{proof}

\closureat{RegularCauchySelectorBudgetUp}{seedStr}
\closureat{RegularCauchySelectorBudgetUp}{obligationStr}
\closureat{RegularCauchySelectorBudgetUp}{scopedStr}
\closureat{RegularCauchySelectorBudgetUp}{publicStr}
\closureat{RegularCauchySelectorBudgetUp}{bridgedStr}
\closureat{RegularCauchySelectorBudgetUp}{matureStr}

\begin{closurestatus}{\RegularCauchySelectorBudgetUp}
  \constructivestory{A $\RegularCauchySelectorBudgetUp$ packet is generated from a selector modulus, finite window budget, StreamName observation rows, a RegSeqRat witness, a RealUp seal, componentwise hsame transports, displayed Cont routes, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{def:regular-cauchy-selector-budget-carrier}, \autoref{thm:regular-cauchy-selector-budget-namecert-obligations}, \autoref{thm:regular-cauchy-selector-budget-window-coverage}, \autoref{thm:regular-cauchy-selector-budget-regseqrat-handoff}, \autoref{thm:regular-cauchy-selector-budget-real-seal-boundary}, \autoref{thm:regular-cauchy-selector-budget-nonescape}, \autoref{thm:regular-cauchy-selector-budget-obligation-closure}, \autoref{thm:regular-cauchy-selector-budget-formal-target-surface}, \autoref{thm:regular-cauchy-selector-budget-verification-handoff}, \autoref{thm:regular-cauchy-selector-budget-consumer-exactness}, \autoref{thm:regular-cauchy-selector-budget-tail-selector-compatibility}, \autoref{thm:regular-cauchy-selector-budget-real-completeness-handoff}, \autoref{def:regular-cauchy-selector-budget-tail-precision-source}, \autoref{thm:regular-cauchy-selector-budget-tail-precision-exactness}, \autoref{thm:regular-cauchy-selector-budget-tail-budget-totality}, \autoref{thm:regular-cauchy-selector-budget-precision-window-nonescape}, \autoref{thm:regular-cauchy-selector-budget-tail-meet-factorization}, \autoref{thm:regular-cauchy-selector-budget-tail-meet-threshold-coverage}, \autoref{thm:regular-cauchy-selector-budget-tail-meet-route-determinacy}, \autoref{thm:regular-cauchy-selector-budget-tail-meet-nonescape}, \autoref{thm:regular-cauchy-selector-budget-selector-budget-exactness}, \autoref{thm:regular-cauchy-selector-budget-finite-window-provenance}, and \autoref{thm:regular-cauchy-selector-budget-real-seal-nonescape-package} bind the finite selector-budget carrier and its local NameCert obligation surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close Lean verification, RealUp completeness, quotient stream equality, host real limits, countable choice, hidden window enlargement, or a public standard bridge.}
  \upgradepath{No higher paper closure grade remains for this finite selector-budget bridge surface; Lean verification and bridge checking remain independent formal-axis work.}
\end{closurestatus}
