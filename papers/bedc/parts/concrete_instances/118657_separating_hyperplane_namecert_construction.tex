\chapter{A Concrete Naming Certificate for $\SeparatingHyperplaneUp$}
\label{ch:concrete-instances-separating-hyperplane-namecert}
\origin{human}

Following Rockafellar and finite-dimensional convex separation theory, the $\SeparatingHyperplaneUp$ packet records the finite affine-functional surface by which two displayed convex rows are separated inside a finite vector-space window. It sits after the finite alternative row of \autoref{ch:concrete-instances-farkas-lemma-namecert}, the convex carrier of \autoref{ch:concrete-instances-convexset-namecert}, the linear-map row of \autoref{ch:concrete-instances-linearmap-namecert}, the vector-space carrier of \autoref{ch:concrete-instances-vecspace-namecert}, and the supporting-hyperplane packet of \autoref{ch:concrete-instances-finite-supporting-hyperplane-namecert}. The packet names only displayed coefficient, inequality, and transport rows; it does not import a host dual space, arbitrary topological separation, selected support points, quotient dual equality, or choice.

\begin{definition}[Separating hyperplane carrier]
\label{def:separating-hyperplane-carrier}
A $\SeparatingHyperplaneUp$ carrier is a finite $\BHist$ packet
$$
S=(V,C_0,C_1,L,a_0,a_1,F,W,H,R,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $V$ is the finite-dimensional $\VecSpaceUp$ row on which every vector and scalar-action read is performed.
\item $C_0$ and $C_1$ are the two $\ConvexSetUp$ rows whose displayed convex-combination operations and inspected point rows are visible to the packet.
\item $L$ is the $\LinearMapUp$ functional row, recorded by the finite coefficient list read over $V$.
\item $a_0$ and $a_1$ are the scalar level rows marking the two displayed affine sides of the hyperplane comparison.
\item $F$ is the $\FarkasLemmaUp$ alternative row used when the separating comparison is obtained from a finite inequality system.
\item $W$ is the finite witness ledger saying that every inspected row of $C_0$ lies on the first side of $L$ and every inspected row of $C_1$ lies on the second side, with the displayed located slack between $a_0$ and $a_1$.
\item $H$ records componentwise $\hsame$ transport for vector, convex, functional, scalar-level, Farkas, witness, replay, package, and local naming rows.
\item $R$ records displayed $\Cont$ replay from a separation request through $V,C_0,C_1,L,a_0,a_1,F,W,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\SeparatingHyperplaneUp$, $\FarkasLemmaUp$, $\ConvexSetUp$, $\LinearMapUp$, $\VecSpaceUp$, $\FiniteSupportingHyperplaneUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\SeparatingHyperplaneUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a selected ambient dual vector, an arbitrary topological separation theorem, a quotient dual-space equality, or a global family of separating functionals.
\end{definition}

\begin{theorem}[Separating hyperplane NameCert obligations]
\label{thm:separating-hyperplane-namecert-obligations}
For every accepted $\SeparatingHyperplaneUp$ carrier $S=(V,C_0,C_1,L,a_0,a_1,F,W,H,R,P,N)$, the local $\NameCert_{\SeparatingHyperplaneUp}$ surface consists exactly of finite vector-space admission, two convex-set admissions, linear-functional display, scalar-level display, finite alternative handoff, one-sided witness exactness, componentwise transport, displayed replay, package provenance, and local naming. The certificate exports no host dual-space object, arbitrary topological separation theorem, selected support point, quotient dual equality, or choice principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:separating-hyperplane-carrier}. The rows $V,C_0,C_1,L,a_0,a_1,F,W$ provide the entire geometric and scalar surface of the packet.

The separation read is finite. The functional row $L$ is read through the displayed vector-space row $V$, the convex information is read through $C_0$ and $C_1$, and the finite alternative row $F$ supplies only the multiplier or feasibility ledger already visible to the packet.

Rows $H,R,P,N$ transport, replay, source, and name the same finite data. Since the carrier lacks any coordinate for the excluded ambient or choice-based objects, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Separating hyperplane Farkas route]
\label{thm:separating-hyperplane-farkas-route}
Every Farkas-facing consumer of an accepted $\SeparatingHyperplaneUp$ packet factors through the finite route
$$
\VecSpaceUp \longmapsto \ConvexSetUp \longmapsto \LinearMapUp \longmapsto \FarkasLemmaUp \longmapsto \FiniteSupportingHyperplaneUp .
$$
The consumer receives the same finite vector window, two convex rows, a displayed linear functional, scalar levels, the finite alternative row, the one-sided witness ledger, transport, replay, provenance, and local naming. It receives no detached separation principle and no selected ambient dual object.
\end{theorem}
\begin{proof}
Begin with a Farkas-facing separation request. The vector and convex data are read through $V,C_0,C_1$, so all subsequent coefficient rows remain tied to the displayed finite-dimensional surface.

Read $L,a_0,a_1$ next. These rows provide the affine comparison seen by the consumer, while the finite alternative row $F$ explains the inequality ledger without invoking a topological separation theorem.

Finally compare the witness row $W$ with the supporting-hyperplane sibling surface. The supporting packet supplies the one-sided boundary reading, and the present packet carries the two-sided separation ledger through the same structural rows $H,R,P,N$.

Therefore the public route is the displayed finite chain above. Any read bypassing those rows would require a carrier coordinate that the packet does not contain.
\end{proof}

\closureat{SeparatingHyperplaneUp}{seedStr}

\begin{closurestatus}{\SeparatingHyperplaneUp}
  \constructivestory{A $\SeparatingHyperplaneUp$ packet is generated from finite VecSpaceUp data, two ConvexSetUp rows, a LinearMapUp functional row, scalar-level rows, a FarkasLemmaUp alternative handoff, a finite one-sided witness ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:separating-hyperplane-carrier}, \autoref{thm:separating-hyperplane-namecert-obligations}, and \autoref{thm:separating-hyperplane-farkas-route} seed the finite separating-hyperplane packet over FarkasLemmaUp, ConvexSetUp, LinearMapUp, VecSpaceUp, FiniteSupportingHyperplaneUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close arbitrary topological separation, selected ambient dual vectors, quotient dual equality, selected support points, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite alternative handoff, one-sided witness exactness, componentwise transport, replay, provenance, and local naming for BEDC.Derived.SeparatingHyperplaneUp.}
\end{closurestatus}
