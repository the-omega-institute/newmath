\chapter{A Concrete Naming Certificate for $\CompactCauchyRealizerUp$}
\label{ch:concrete-instances-compact-cauchy-realizer-namecert}
\origin{human}

Following Bishop--Bridges constructive compact metric spaces, the $\CompactCauchyRealizerUp$ packet records the finite route by which compact finite-net data and displayed Cauchy windows are realized as a completion-facing regular rational name before any terminal Real seal is read. It sits after the compact metric finite-net surface of \autoref{ch:concrete-instances-compactmetric-namecert}, the completion-filter route of \autoref{ch:concrete-instances-metric-completion-filter-namecert}, the compact Cauchy filter packet of \autoref{ch:concrete-instances-compact-cauchy-filter-namecert}, the compact Cauchy net packet of \autoref{ch:concrete-instances-compact-cauchy-net-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite $\StreamNameUp$ windows, the $\RegSeqRatUp$ readback, and the terminal $\RealUp$ seal. The packet realizes only displayed compact-net and Cauchy-window data; it does not quotient streams, select limits, invoke countable choice, or assert ambient $\RealUp$ completeness.

\begin{definition}[Compact Cauchy realizer carrier]
\label{def:compact-cauchy-realizer-carrier}
A $\CompactCauchyRealizerUp$ carrier is a finite $\BHist$ packet
$$
Z=(K,Q,M,W,D,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the accepted $\CompactMetricUp$ finite-net source row.
\item $Q$ is the compact Cauchy row, read through the $\CompactCauchyFilterUp$ or $\CompactCauchyNetUp$ sibling surface.
\item $M$ is the $\MetricCompletionFilterUp$ completion-filter row attached to the same compact source and Cauchy request.
\item $W$ records the local Cauchy window whose compatibility is inspected before completion handoff.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row for mesh, Cauchy-window, and readback comparisons.
\item $S$ is the finite $\StreamNameUp$ request row used to inspect the displayed windows.
\item $R$ is the $\RegSeqRatUp$ readback row produced from $K,Q,M,W,D,S$.
\item $E$ is the terminal $\RealUp$ seal exposed only after the regular readback row is displayed.
\item $H$ records componentwise $\hsame$ transport for compact source, Cauchy row, completion-filter row, window, tolerance, StreamName, readback, seal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay of the same compact Cauchy realization route.
\item $P$ is the $\Pkg$ provenance over $\CompactCauchyRealizerUp$, $\CompactMetricUp$, $\MetricCompletionFilterUp$, $\CompactCauchyFilterUp$, $\CompactCauchyNetUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactCauchyRealizerUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these displayed rows. It has no coordinate for quotient stream equality, selected Cauchy representatives, countable-choice schedules, open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact Cauchy realizer NameCert obligations]
\label{thm:compact-cauchy-realizer-namecert-obligations}
For every accepted $\CompactCauchyRealizerUp$ carrier
$$
Z=(K,Q,M,W,D,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\CompactCauchyRealizerUp}$ surface consists exactly of compact finite-net admission, compact Cauchy row exposure, completion-filter routing through $\MetricCompletionFilterUp$, local Cauchy-window display, dyadic tolerance exposure, StreamName finite-window exposure, RegSeqRat readback, Real sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-cauchy-realizer-carrier}. The rows $K,Q,M,W,D,S,R,E$ are the only analytic rows, and $H,C,P,N$ are the only structural rows.

Read compactness through the sibling finite-net data. The compact metric row $K$ and compact Cauchy row $Q$ keep the source finite, while the completion-filter row $M$ routes the Cauchy request through \autoref{ch:concrete-instances-metric-completion-filter-namecert}.

The local window, tolerance, StreamName, and RegSeqRat rows then produce the displayed readback. The terminal seal $E$ is downstream of that readback, and the structural rows transport, replay, source, and name the same route.

Since the carrier has no additional coordinate, quotient streams, selected limits, countable-choice schedules, open-cover compactness, and ambient Real completeness are not obligations of the local certificate.
\end{proof}

\begin{theorem}[Compact Cauchy realizer completion handoff]
\label{thm:compact-cauchy-realizer-completion-handoff}
Every completion-facing consumer of an accepted $\CompactCauchyRealizerUp$ carrier must pass through the compact finite-net row $K$, compact Cauchy row $Q$, $\MetricCompletionFilterUp$ row $M$, local Cauchy window $W$, $\DyadicRatCoreUp$ tolerance row $D$, finite $\StreamNameUp$ row $S$, and $\RegSeqRatUp$ readback row $R$ before the $\RealUp$ seal $E$ is available. The handoff realizes compact Cauchy data as a displayed regular rational route, not as a quotient stream, selected limit, countable-choice schedule, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Apply the NameCert obligations of \autoref{thm:compact-cauchy-realizer-namecert-obligations}. They expose the compact finite-net, compact Cauchy, completion-filter, local window, tolerance, StreamName, RegSeqRat, and Real-seal rows in the carrier order.

Use the completion-filter dependency of \autoref{ch:concrete-instances-metric-completion-filter-namecert}. The row $M$ forces the completion read to remain a finite filter-facing route before any Real-facing consumer may inspect the seal.

The dyadic tolerance, StreamName, and RegSeqRat rows then convert the displayed windows into a regular rational readback. The terminal Real seal is therefore reached only after $K,Q,M,W,D,S,R$ have been displayed and transported through $H,C,P,N$.
\end{proof}

\begin{theorem}[Compact Cauchy realizer L10 scope]
\label{thm:compact-cauchy-realizer-l10-scope}
The $\CompactCauchyRealizerUp$ packet supplies an L10 Real-and-Completion scope consisting of compact finite-net data, compact Cauchy rows, completion-filter routing, dyadic tolerance rows, finite StreamName windows, RegSeqRat readback, Real sealing, transport, replay, provenance, and local naming. It may feed completion, density-net, or compact-realization consumers, but it does not close quotient stream equality, selected representatives, countable choice, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{theorem}
\begin{proof}
Start with the carrier and obligation surface, \autoref{def:compact-cauchy-realizer-carrier} and \autoref{thm:compact-cauchy-realizer-namecert-obligations}. They bind the visible L10 rows to $K,Q,M,W,D,S,R,E,H,C,P,N$.

Apply the completion handoff of \autoref{thm:compact-cauchy-realizer-completion-handoff}. Any completion-facing read must traverse the compact finite-net row, compact Cauchy row, completion-filter row, dyadic tolerance row, StreamName row, and RegSeqRat row before the Real seal.

Finally compare with the sibling compact Cauchy and density-net routes of \autoref{ch:concrete-instances-compact-cauchy-filter-namecert}, \autoref{ch:concrete-instances-compact-cauchy-net-namecert}, and \autoref{ch:concrete-instances-finite-cauchy-density-net-namecert}. Those consumers may read the displayed packet as L10 completion data, but none of them adds the excluded quotient, choice, open-cover, ambient-completeness, Lean-verification, or bridge rows.
\end{proof}

\closureat{CompactCauchyRealizerUp}{seedStr}

\begin{closurestatus}{\CompactCauchyRealizerUp}
  \constructivestory{A $\CompactCauchyRealizerUp$ packet is generated from compact finite-net rows, compact Cauchy rows, MetricCompletionFilter rows, local Cauchy windows, DyadicRatCore tolerance data, StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-cauchy-realizer-carrier}, \autoref{thm:compact-cauchy-realizer-namecert-obligations}, \autoref{thm:compact-cauchy-realizer-completion-handoff}, and \autoref{thm:compact-cauchy-realizer-l10-scope} seed the compact finite-net Cauchy realization route for L10 Real-and-Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected representatives, countable choice, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net exposure, compact Cauchy routing, completion-filter handoff, dyadic tolerance exactness, StreamName coverage, RegSeqRat readback, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactCauchyRealizerUp.}
\end{closurestatus}
