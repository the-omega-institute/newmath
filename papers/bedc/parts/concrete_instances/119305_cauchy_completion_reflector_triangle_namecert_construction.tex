\chapter{A Concrete Naming Certificate for $\CauchyCompletionReflectorTriangleUp$}
\label{ch:concrete-instances-cauchy-completion-reflector-triangle-namecert}
\origin{human}

Following Bishop and Bridges and the Bourbaki-style reflector discipline for completions, the $\CauchyCompletionReflectorTriangleUp$ packet records the finite triangle by which a Cauchy-completion reflector, its unit route, and the Real-facing regular Cauchy handoff are read as one NameCert object. It consumes the completion source discipline of \autoref{ch:concrete-instances-bishopcompletion-namecert}, the monad and unit rows of \autoref{ch:concrete-instances-cauchycompletionmonad-namecert} and \autoref{ch:concrete-instances-completion-unit-triangle-namecert}, the metric reflector route of \autoref{ch:concrete-instances-metric-completion-reflector-namecert}, and the finite rational readback stack of \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The chapter names the displayed completion-reflector triangle; it does not assert quotient completion equality, selected limits, host equality of completed objects, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[CauchyCompletionReflectorTriangle carrier]
\label{def:cauchy-completion-reflector-triangle-carrier}
A $\CauchyCompletionReflectorTriangleUp$ carrier is a finite $\BHist$ packet
$$
T=(B,R,U,M,D,S,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\BishopCompletionUp$ source row naming the constructive completion source before any reflector consumer reads it;
\item $R$ is the $\MetricCompletionReflectorUp$ comparison row, recording the displayed reflector comparison against the source metric presentation;
\item $U$ is the $\CompletionUnitTriangleUp$ unit-triangle row, tying the source point, its unit image, and the reflector comparison to one finite route;
\item $M$ is the $\CauchyCompletionMonadUp$ row recording the completion-facing unit and bind surface consumed by the triangle;
\item $D$ is the $\DyadicRatCoreUp$ tolerance row used for finite dyadic comparison;
\item $S$ is the $\StreamNameUp$ finite-window row for the inspected rational name;
\item $Q$ is the $\RegSeqRatUp$ readback row obtained from the displayed windows and tolerance cells;
\item $E$ is the terminal $\RealUp$ seal row, reached only after $B,R,U,M,D,S,Q$ have been displayed;
\item $H$ records componentwise $\hsame$ transport for the source, reflector, unit-triangle, completion, tolerance, window, readback, Real-seal, provenance, and local naming rows;
\item $C$ records the displayed $\Cont$ replay route from the completion source through the reflector comparison, unit triangle, finite rational readback, and Real seal;
\item $P$ is the $\Pkg$ provenance over $\CauchyCompletionReflectorTriangleUp$, $\BishopCompletionUp$, $\CauchyCompletionMonadUp$, $\CompletionUnitTriangleUp$, $\MetricCompletionReflectorUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\CauchyCompletionReflectorTriangleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,R,U,M,D,S,Q,E,H,C,P,N$ and their displayed $\hsame$ and $\Cont$ transports. It has no coordinate for quotient completion equality, selected representatives, host equality of completed objects, countable-choice schedules, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.CauchyCompletionReflectorTriangleUp.CauchyCompletionReflectorTriangleUp\_single\_carrier\_alignment}

\begin{theorem}[CauchyCompletionReflectorTriangle NameCert obligations]
\label{thm:cauchy-completion-reflector-triangle-namecert-obligations}
For every accepted $\CauchyCompletionReflectorTriangleUp$ carrier $T=(B,R,U,M,D,S,Q,E,H,C,P,N)$, the local $\NameCert_{\CauchyCompletionReflectorTriangleUp}$ surface consists exactly of carrier admission, completion-source exposure, reflector comparison, unit-triangle exposure, completion monad scheduling, DyadicRatCore tolerance accounting, StreamName finite-window readback, RegSeqRat regular readback, Real-seal ordering, componentwise $\hsame$ transport, displayed $\Cont$ replay, package provenance, and local naming. No local obligation is stored outside those rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-completion-reflector-triangle-carrier}. The displayed tuple is exactly $B,R,U,M,D,S,Q,E,H,C,P,N$, so every accepted packet exposes the completion source, reflector comparison, unit triangle, monad schedule, finite rational readback stack, Real seal, and structural rows before any consumer can read the triangle.

Read the analytic rows in order. The source row $B$ enters the reflector comparison $R$, the unit-triangle row $U$ binds the source and reflected point, and the monad row $M$ records the completion-facing unit and bind surface. The dyadic tolerance, finite-window, and regular-readback rows $D,S,Q$ then supply the finite rational data needed before $E$ is exposed.

The structural rows close the certificate. Componentwise $\hsame$ transport and displayed $\Cont$ replay preserve the same finite triangle through $H$ and $C$, while $P$ and $N$ record provenance and local naming. Since the carrier has no further coordinate, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[CauchyCompletionReflectorTriangle handoff]
\label{thm:cauchy-completion-reflector-triangle-handoff}
Every $\RealUp$-facing or metric-reflector consumer of an accepted $\CauchyCompletionReflectorTriangleUp$ packet must read the route
$$
\BishopCompletionUp \longmapsto \MetricCompletionReflectorUp \longmapsto \CompletionUnitTriangleUp \longmapsto \CauchyCompletionMonadUp \longmapsto \DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
through the same displayed carrier $T=(B,R,U,M,D,S,Q,E,H,C,P,N)$. The handoff exports no quotient completion, selected limit, host equality of completed objects, countable-choice selector, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:cauchy-completion-reflector-triangle-namecert-obligations}. They force every accepted handoff to expose the completion source, reflector comparison, unit triangle, completion monad row, dyadic tolerance row, finite windows, regular rational readback, Real seal, transport, replay, provenance, and local naming.

The reflector-facing part is read from $B,R,U,M$. These rows record the displayed completion source, comparison map, unit-triangle route, and completion-facing schedule before any metric reflector consumer can treat the packet as a triangle.

The Real-facing part is read from $D,S,Q,E$. The dyadic tolerance and finite window rows determine the regular rational readback, and the Real seal is downstream of those finite rows rather than a selected quotient representative.

Finally use $H,C,P,N$ to transport, replay, source, and name the same finite handoff. Because none of the rows stores quotient equality, selected limits, host equality, choice, or ambient completeness, no such object can be exported by the triangle packet.
\end{proof}

\closureat{CauchyCompletionReflectorTriangleUp}{seedStr}

\begin{closurestatus}{\CauchyCompletionReflectorTriangleUp}
  \constructivestory{A $\CauchyCompletionReflectorTriangleUp$ packet is generated from a BishopCompletion source row, MetricCompletionReflector comparison, CompletionUnitTriangle route, CauchyCompletionMonad schedule, DyadicRatCore tolerance ledger, StreamName finite windows, RegSeqRat readback, Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-completion-reflector-triangle-carrier}, \autoref{thm:cauchy-completion-reflector-triangle-namecert-obligations}, and \autoref{thm:cauchy-completion-reflector-triangle-handoff} seed the finite completion-reflector triangle route through source, reflector, unit, rational readback, and Real-seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion equality, selected limits, host equality of completed objects, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, reflector comparison, unit-triangle routing, rational readback, Real-seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.CauchyCompletionReflectorTriangleUp.}
\end{closurestatus}
