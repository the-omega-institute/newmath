\chapter{A Concrete Naming Certificate for $\FiniteWindowConfluenceUp$}
\label{ch:concrete-instances-finite-window-confluence-namecert}
\origin{ai}

The $\FiniteWindowConfluenceUp$ packet realizes the MetaCIC normalization audit map of \autoref{ch:metacic-normalization-audit-map} as finite beta-window confluence data. It sits beside the redex-frontier packet of \autoref{ch:concrete-instances-metacic-redex-frontier-namecert}, the local join ledger of \autoref{ch:concrete-instances-metacic-local-join-ledger-namecert}, the residual-substitution boundary of \autoref{ch:concrete-instances-metacic-residual-substitution-boundary-namecert}, the closed normal seal of \autoref{ch:concrete-instances-closednormalconfluenceseal-namecert}, and the normal-fragment confluence packet of \autoref{ch:concrete-instances-metacicconfluencenormalfragment-namecert}. The carrier records a bounded beta-window peak, local join evidence, substitution-boundary retention, and closed-normal endpoint rows without claiming unrestricted global confluence, quotient conversion equality, arbitrary subject reduction, or kernel consistency.

\falsifiablePrediction{A $\FiniteWindowConfluenceUp$ packet whose beta window omits the residual-substitution boundary row is rejected even when its local join ledger and closed-normal endpoint rows are present.}

\independenceWitness{metacic-redex-frontier, metacic-local-join-ledger, metacic-residual-substitution-boundary, closednormalconfluenceseal, metacicconfluencenormalfragment: none is bijective with $\FiniteWindowConfluenceUp$ because this packet jointly carries the beta-window peak, redex-frontier read, local join ledger, residual-substitution boundary, closed-normal endpoint, TasteGate readiness, transport, replay, provenance, and local naming rows.}

\begin{definition}[FiniteWindowConfluence carrier]
\label{def:finite-window-confluence-carrier}
A $\FiniteWindowConfluenceUp$ carrier is a finite $\BHist$ packet
$$
F=(B,R,J,S,N,W,G,H,C,P,K)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the bounded beta-window row, naming the source term, window radius, and two outgoing beta observations.
\item $R$ is the $\MetaCICRedexFrontierUp$ row, exposing the redex-frontier discharge data consumed by the window.
\item $J$ is the $\MetaCICLocalJoinLedgerUp$ row, recording the local peak and candidate join ledger for the same bounded window.
\item $S$ is the $\MetaCICResidualSubstitutionBoundaryUp$ row, retained whenever the join read depends on residual substitution data.
\item $N$ is the $\ClosedNormalConfluenceSealUp$ endpoint row, available only for closed normal endpoint reads.
\item $W$ is the $\MetaCICConfluenceNormalFragmentUp$ window-fragment row, keeping the closed-normal finite fragment separate from unrestricted global confluence.
\item $G$ is the $\TasteGate$ readiness row for later $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances over this packet.
\item $H$ records componentwise $\hsame$ transport for beta windows, redex frontiers, local joins, residual-substitution boundaries, closed-normal endpoints, normal fragments, TasteGate readiness, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a normalization-audit query through $B,R,J,S,N,W,G,H,P,K$.
\item $P$ is the $\Pkg$ provenance over $\FiniteWindowConfluenceUp$, $\MetaCICRedexFrontierUp$, $\MetaCICLocalJoinLedgerUp$, $\MetaCICResidualSubstitutionBoundaryUp$, $\ClosedNormalConfluenceSealUp$, $\MetaCICConfluenceNormalFragmentUp$, $\BHist$, $\hsame$, $\Cont$, $\TasteGate$, and $\NameCert$.
\item $K$ is the local $\NameCert_{\FiniteWindowConfluenceUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $B,R,J,S,N,W,G,H,C,P,K$. It contains no coordinate for unrestricted MetaCIC confluence, global Church--Rosser, quotient conversion equality, arbitrary subject reduction, evaluator equality, or kernel consistency.
\end{definition}

\begin{theorem}[FiniteWindowConfluence NameCert obligations]
\label{thm:finite-window-confluence-namecert-obligations}
For every accepted $\FiniteWindowConfluenceUp$ carrier
$$
F=(B,R,J,S,N,W,G,H,C,P,K),
$$
the local $\NameCert_{\FiniteWindowConfluenceUp}$ surface consists exactly of bounded beta-window exposure through $B$, redex-frontier readback through $R$, local-join ledger exposure through $J$, residual-substitution boundary retention through $S$, closed-normal endpoint exposure through $N$, normal-fragment separation through $W$, TasteGate readiness through $G$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $K$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-window-confluence-carrier}. Rows $B,R,J,S,N,W$ are the confluence-facing finite data, while $G,H,C,P,K$ are the structural certificate data.

The beta-window read starts with $B$ and $R$. The source, radius, and two outgoing observations are bounded by $B$, and the redex-frontier row $R$ keeps the required discharge data visible before a join can be consumed.

The local join is then read through $J$ while retaining $S$. The residual-substitution boundary is a row of the same packet, so the local join cannot be detached into a global confluence theorem or a substitution oracle.

Closed-normal endpoints are read only through $N$ and $W$. These rows expose the normal fragment and the closed-normal seal, but they do not supply arbitrary closed-term Church--Rosser or quotient conversion equality.

Finally $G,H,C,P,K$ reserve the TasteGate instance surface, transport the rows, replay the audit query, source the packet, and name it. Since no carrier coordinate contains unrestricted confluence, subject reduction, evaluator equality, or kernel consistency, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[FiniteWindowConfluence normalization-audit handoff]
\label{thm:finite-window-confluence-normalization-audit-handoff}
Every normalization-audit consumer of an accepted $\FiniteWindowConfluenceUp$ carrier factors through
$$
B,R,J,S,N,W,G,H,C,P,K .
$$
The consumer may inspect the bounded beta window, redex frontier, local join ledger, residual-substitution boundary, closed-normal endpoint, normal fragment, TasteGate readiness, transport, replay, provenance, and local name. It cannot bypass this route to obtain unrestricted MetaCIC confluence, global Church--Rosser, quotient conversion equality, arbitrary subject reduction, evaluator equality, or kernel consistency.
\end{theorem}
\begin{proof}
Start with a normalization-audit query. The only beta-window entrance is $B$, and the only redex-facing discharge surface is $R$, so the query must retain the bounded source, outgoing beta observations, and redex-frontier data.

The local join route is then constrained by $J$ and $S$. Row $J$ records the candidate join ledger, while $S$ keeps the residual-substitution boundary visible as an input rather than as a solved global theorem.

The endpoint route is constrained by $N$ and $W$. A closed-normal read may use the closed-normal seal and normal-fragment packet, but those rows remain finite endpoint data, not an unrestricted confluence theorem.

The structural rows $G,H,C,P,K$ preserve the same packet under TasteGate readiness, transport, replay, provenance, and local naming. Hence every normalization-audit consumer remains inside the displayed finite route.
\end{proof}

\begin{theorem}[FiniteWindowConfluence TasteGate readiness]
\label{thm:finite-window-confluence-tastegate-readiness}
The later Lean side must provide $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ $\TasteGate$ instances for $\FiniteWindowConfluenceUp$ at the displayed finite carrier surface
$$
B,R,J,S,N,W,G,H,C,P,K .
$$
Those instances must certify that the packet is neither empty nor field-forgetting: every accepted carrier visibly retains the bounded beta window, redex frontier, local join ledger, residual-substitution boundary, closed-normal endpoint, normal-fragment row, transport, replay, provenance, and local NameCert data.
\end{theorem}
\begin{proof}
The readiness row $G$ in \autoref{def:finite-window-confluence-carrier} is tied to the whole finite-window packet, not to the local join ledger alone.

Nontriviality must exhibit an accepted packet with all displayed rows present. FieldFaithfulness must show that deleting any of $B,R,J,S,N,W,H,C,P,K$ changes the readable carrier surface. Thus the TasteGate burden remains exactly at the finite beta-window confluence boundary.
\end{proof}

\begin{theorem}[FiniteWindowConfluence sibling route]
\label{thm:finite-window-confluence-sibling-route}
Every public confluence read exported by an accepted $\FiniteWindowConfluenceUp$ packet routes through the sibling rows named in the carrier: the $\MetaCICRedexFrontierUp$ redex-frontier row, the $\MetaCICLocalJoinLedgerUp$ local-join ledger, the $\MetaCICResidualSubstitutionBoundaryUp$ residual-substitution boundary, the $\ClosedNormalConfluenceSealUp$ closed-normal seal, and the $\MetaCICConfluenceNormalFragmentUp$ normal-fragment confluence row. A consumer cannot read the packet as an isolated confluence theorem without also retaining those sibling routes.
\end{theorem}
\begin{proof}
Begin with the carrier of \autoref{def:finite-window-confluence-carrier}. Its public confluence-facing rows are $B,R,J,S,N,W$, so the bounded beta-window row is not readable without the redex-frontier row of \autoref{ch:concrete-instances-metacic-redex-frontier-namecert} and the local-join ledger of \autoref{ch:concrete-instances-metacic-local-join-ledger-namecert}.

The residual part of the read is held by $S$. Thus any join consumer that keeps $J$ must also pass through the residual-substitution boundary of \autoref{ch:concrete-instances-metacic-residual-substitution-boundary-namecert}; otherwise the packet would claim a substitution oracle that is not present in its finite rows.

The endpoint part is held by $N$ and $W$. These rows point respectively to \autoref{ch:concrete-instances-closednormalconfluenceseal-namecert} and \autoref{ch:concrete-instances-metacicconfluencenormalfragment-namecert}, so the read remains a closed-normal finite fragment rather than unrestricted MetaCIC confluence.

The structural rows $G,H,C,P,K$ then apply the existing NameCert obligations, the normalization-audit handoff, and the TasteGate readiness row to the same packet. Hence the public route is exactly the displayed finite sibling route.
\end{proof}

\begin{theorem}[FiniteWindowConfluence residual diamond scope]
\label{thm:finite-window-confluence-residual-diamond-scope}
Any local diamond read over the bounded beta window of an accepted $\FiniteWindowConfluenceUp$ carrier must expose the redex-frontier row, local-join ledger, residual-substitution boundary, closed-normal endpoint row, and normal-fragment row together. In carrier notation, a diamond-facing consumer may inspect
$$
B,R,J,S,N,W
$$
only as the finite route already transported, replayed, sourced, and named by $G,H,C,P,K$. The read remains a finite residual-diamond scope; it does not claim unrestricted Church--Rosser, global subject reduction, quotient conversion equality, evaluator equality, or kernel consistency.
\end{theorem}
\begin{proof}
Apply the NameCert obligation surface of \autoref{thm:finite-window-confluence-namecert-obligations}. It requires the bounded beta-window row, redex-frontier readback, local-join ledger, residual-substitution boundary, closed-normal endpoint, and normal-fragment separation to remain visible in the same accepted packet.

Next apply the normalization-audit handoff of \autoref{thm:finite-window-confluence-normalization-audit-handoff}. A local diamond read is a normalization-audit consumer, so it cannot bypass $R,J,S,N,W$ after entering through $B$.

Finally use the sibling route of \autoref{thm:finite-window-confluence-sibling-route}. The redex-frontier, local-join, residual-substitution, closed-normal, and normal-fragment rows are sibling dependencies rather than optional annotations. Hence the diamond scope is finite and residual-boundary-aware, with no route to the global principles excluded from the carrier.
\end{proof}

\begin{theorem}[FiniteWindowConfluence residual-boundary inversion]
\label{thm:finite-window-confluence-residual-boundary-inversion}
If a local diamond read from an accepted $\FiniteWindowConfluenceUp$ packet reaches $J$ as a candidate join, then the same read also exposes $S$ as the residual-substitution boundary row. The candidate join cannot be reinterpreted as a join independent of residual data; it remains in the route
$$
B,R,J,S,N,W,G,H,C,P,K .
$$
\end{theorem}
\begin{proof}
Project the accepted carrier $F=(B,R,J,S,N,W,G,H,C,P,K)$. The local join ledger $J$ and residual-substitution boundary $S$ are adjacent confluence-facing rows of the same finite packet.

Use the NameCert obligations of \autoref{thm:finite-window-confluence-namecert-obligations} and the normalization-audit handoff of \autoref{thm:finite-window-confluence-normalization-audit-handoff}. They require a diamond-facing read to keep the bounded beta window, redex frontier, local join, residual-substitution boundary, closed-normal endpoint, and normal fragment in one transported and replayed route.

Finally apply the sibling route of \autoref{thm:finite-window-confluence-sibling-route}. It keeps the redex-frontier, local-join, residual-substitution, closed-normal, and normal-fragment rows together, so $J$ cannot be cited as an unconditional global confluence join or as a join detached from residual-substitution data.
\end{proof}

\begin{theorem}[FiniteWindowConfluence normalization-frontier handoff]
\label{thm:finite-window-confluence-normalization-frontier-handoff}
Every local diamond read from an accepted $\FiniteWindowConfluenceUp$ carrier consumed by a $\NormalizationCandidateFrontierUp$ route carries the rows $B,R,J,S,N,W$ through the frontier packet's candidate-evidence, bounded-route, obstruction, transport, replay, provenance, and local NameCert rows. The handoff is therefore a bounded candidate-mediated route from finite-window confluence data to the normalization frontier, not a proof of global Church--Rosser, unrestricted subject reduction, evaluator equality, quotient conversion equality, or kernel consistency.
\end{theorem}
\begin{proof}
Start with residual-boundary inversion, \autoref{thm:finite-window-confluence-residual-boundary-inversion}. A local diamond read that reaches the candidate join ledger $J$ must also expose the residual-substitution boundary $S$, while $B,R,N,W$ remain in the same finite confluence route.

Now pass to the frontier through \autoref{thm:normalization-candidate-frontier-stability} and \autoref{thm:normalization-candidate-frontier-audit-handoff}. The frontier can consume positive normalization evidence only while its candidate-evidence row $E$ and bounded-route row $B$ remain visible, so the finite diamond read is carried as bounded candidate data rather than as an evidence-free confluence theorem.

Finally use obstruction exactness and local confluence scope for the frontier, \autoref{thm:normalization-candidate-frontier-obstruction-exactness} and \autoref{thm:normalization-candidate-frontier-local-confluence-scope}. These rows keep obstruction, transport, replay, provenance, and local naming in the handoff, so erased candidate evidence or erased residual data reaches the obstruction route instead of exporting global Church--Rosser, subject reduction, evaluator equality, quotient conversion equality, or kernel consistency.
\end{proof}

\begin{theorem}[FiniteWindowConfluence MetaCIC frontier forward route]
\label{thm:finite-window-confluence-metacic-frontier-forward-route}
Every $\MetaCICParallelDiamondFrontierUp$ consumer of an accepted $\FiniteWindowConfluenceUp$ carrier must keep the bounded beta-window row, redex-frontier row, local-join ledger, residual-substitution boundary, closed-normal endpoint row, normal-fragment row, componentwise transport, displayed replay, provenance, and local $\NameCert$ row visible before any frontier handoff. In carrier notation, the forward route is
$$
B,R,J,S,N,W,H,C,P,K ,
$$
read first through the finite-window obligations, residual-boundary inversion, and normalization-frontier handoff, then through the row discipline of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}. This is a substantive dependency on \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}: the frontier consumes bounded-window evidence only after retained residual-substitution, critical-pair, candidate-join, checker, obstruction, transport, replay, provenance, and local naming rows are visible. The route supplies bounded-window evidence to the parallel-diamond frontier only; it does not claim global confluence, unrestricted subject reduction, quotient term equality, evaluator equality, or kernel consistency.
\end{theorem}
\begin{proof}
Apply the NameCert obligations of \autoref{thm:finite-window-confluence-namecert-obligations}. They expose the bounded beta-window, redex-frontier, local-join, residual-substitution, closed-normal endpoint, and normal-fragment rows together with transport, replay, provenance, and local naming.

Next apply the residual-boundary inversion of \autoref{thm:finite-window-confluence-residual-boundary-inversion}. Any read that reaches the local join ledger $J$ still carries $S$ as residual-substitution boundary data, so the route cannot be recast as a residual-free local join.

Now use the normalization-frontier handoff of \autoref{thm:finite-window-confluence-normalization-frontier-handoff}. It already transports the finite diamond read through candidate evidence, bounded-route, obstruction, transport, replay, provenance, and local NameCert rows before a frontier consumer may inspect it.

Finally compare with the $\MetaCICParallelDiamondFrontierUp$ row discipline of \autoref{thm:metacic-parallel-diamond-frontier-obligations} and \autoref{thm:metacic-parallel-diamond-frontier-handoff}. That frontier requires retained residual-substitution, critical-pair, candidate-join, residual-frontier, checker, obstruction, transport, replay, provenance, and local naming rows. The finite-window packet can therefore feed only its displayed bounded route into the frontier, with no route to global confluence, unrestricted subject reduction, quotient term equality, evaluator equality, or kernel consistency.
\end{proof}

\closureat{FiniteWindowConfluenceUp}{seedStr}

\begin{closurestatus}{\FiniteWindowConfluenceUp}
  \constructivestory{A $\FiniteWindowConfluenceUp$ packet is generated from a bounded beta-window row, MetaCICRedexFrontier row, MetaCICLocalJoinLedger row, MetaCICResidualSubstitutionBoundary row, ClosedNormalConfluenceSeal endpoint row, MetaCICConfluenceNormalFragment row, Nontrivial and FieldFaithful TasteGate readiness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-window-confluence-carrier}, \autoref{thm:finite-window-confluence-namecert-obligations}, \autoref{thm:finite-window-confluence-normalization-audit-handoff}, and \autoref{thm:finite-window-confluence-tastegate-readiness} seed the finite beta-window confluence packet for MetaCIC normalization-audit consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close unrestricted MetaCIC confluence, global Church--Rosser, quotient conversion equality, arbitrary subject reduction, evaluator equality, kernel consistency, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, beta-window exposure, redex-frontier readback, local-join exactness, residual-substitution boundary retention, closed-normal endpoint exposure, normal-fragment separation, TasteGate nontriviality, FieldFaithful coverage, transport, replay, provenance, and local naming for BEDC.Derived.FiniteWindowConfluenceUp.}
\end{closurestatus}
