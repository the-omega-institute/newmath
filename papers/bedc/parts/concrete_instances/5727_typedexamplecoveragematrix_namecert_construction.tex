\chapter{A Concrete Naming Certificate for $\TypedExampleCoverageMatrixUp$}
\label{ch:concrete-instances-typedexamplecoveragematrix-namecert}
\origin{ai}

The $\TypedExampleCoverageMatrixUp$ packet realizes the TypedExamples audit-map vision of \autoref{ch:metacic-typedexamples-audit-map} as a finite coverage matrix. It sits beside the MetaCIC gallery of \autoref{ch:concrete-instances-metacictypedexamplesgallery-namecert}, the typed-example gallery of \autoref{ch:concrete-instances-typedexamplegallery-namecert}, the worked-example seal of \autoref{ch:concrete-instances-metacic-typedexamples-seal-namecert}, the regression-spend budget of \autoref{ch:concrete-instances-typedexampleauditbudget-namecert}, and the beta-step boundary of \autoref{ch:concrete-instances-betastepboundary-namecert}. Those siblings expose gallery rows, checker rows, seal rows, budget rows, and beta-boundary rows; this chapter binds them as one finite row-indexed coverage object for checker-audit consumers.

\begin{definition}[Typed example coverage matrix carrier]
\label{def:typed-example-coverage-matrix-carrier}
A $\TypedExampleCoverageMatrixUp$ carrier is a finite $\BHist$ packet
$$
M=(F,G,C,B,R,E,H,K,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the finite family index for accepted typed-example families;
\item $G$ records, for each row of $F$, the paired gallery cells inherited from the MetaCIC and typed-example gallery packets;
\item $C$ records the checker cells attached to the same rows;
\item $B$ records the beta-boundary cells attached to the same rows;
\item $R$ records the regression-spend cells and their accepted coverage budget;
\item $E$ is the exactness row asserting that every accepted family in $F$ has the displayed $G,C,B,R$ cells and no extra coverage cell;
\item $H$ records componentwise $\hsame$ transports for the family, gallery, checker, beta, regression-spend, exactness, provenance, and local naming rows;
\item $K$ records the displayed $\Cont$ routes by which checker-audit consumers read exactly the cells $F,G,C,B,R,E$;
\item $P$ is the $\Pkg$ provenance over the sibling gallery, seal, budget, beta-boundary, transport, continuation, and exactness rows;
\item $N$ is the local $\NameCert_{\TypedExampleCoverageMatrixUp}$ row.
\end{enumerate}
The carrier has no coordinate for checker completeness, subject reduction, normalization, arbitrary beta closure, or unindexed regression spending.
\end{definition}

\begin{definition}[Typed example coverage matrix row family]
\label{def:typed-example-coverage-matrix-row-family}
For an accepted $\TypedExampleCoverageMatrixUp$ carrier $M=(F,G,C,B,R,E,H,K,P,N)$, the \emph{coverage row family} is the finite subfamily of $F$ whose displayed cells name the boolean, natural-number, pair, option, list, sum, and dependent-function gallery examples. The row family is read only through the paired matrix coordinates $G,C,B,R,E$: $G$ names the example cell, $C$ names its checker cell, $B$ names the beta-boundary cell available for that example, $R$ names the regression-spend cell, and $E$ records that all four cells belong to the same family row.
\end{definition}

\begin{theorem}[Typed example coverage matrix NameCert obligations]
\label{thm:typed-example-coverage-matrix-namecert-obligations}
For the carrier of \autoref{def:typed-example-coverage-matrix-carrier}, the local $\NameCert_{\TypedExampleCoverageMatrixUp}$ surface consists exactly of five finite obligation rows:
\begin{enumerate}
\item carrier admission for $M=(F,G,C,B,R,E,H,K,P,N)$;
\item family-index exactness through $F$ against the accepted typed-example family rows;
\item paired gallery, checker, beta, and regression-spend cell exposure through $G,C,B,R$;
\item matrix exactness through $E$, requiring each accepted row to carry all four cell kinds and no unindexed cell;
\item transport, continuation, provenance, and local naming through $H,K,P,N$.
\end{enumerate}
\end{theorem}
\begin{proof}
Carrier admission is projection of the displayed packet. The row $F$ supplies the finite family index before any coverage cell is read.

The rows $G,C,B,R$ are paired by position. A consumer can inspect a gallery cell, checker cell, beta cell, or regression-spend cell only as the cell attached to the same family row.

The exactness row $E$ records that the four cell kinds are jointly present for every accepted family and that no extra coverage coordinate exists outside the matrix. The rows $H,K,P,N$ then transport, route, name, and package only the displayed rows.
\end{proof}

\begin{theorem}[Typed example coverage matrix obligation surface]
\label{thm:typed-example-coverage-matrix-obligation-surface}
For MetaCIC audit consumers, the full $\TypedExampleCoverageMatrixUp$ obligation surface is the family-index row $F$, gallery cells $G$, checker cells $C$, beta-boundary cells $B$, regression-spend cells $R$, exactness row $E$, and structural rows $H,K,P,N$. A consumer may read a coverage obligation only after the same accepted matrix row displays all of $F,G,C,B,R,E$ and the structural rows route, transport, source, and name that row; no detached gallery cell, checker cell, beta-boundary cell, regression-spend cell, or ambient MetaCIC theorem is part of the surface.
\end{theorem}
\begin{proof}
Start from the carrier of \autoref{def:typed-example-coverage-matrix-carrier} and the row family of \autoref{def:typed-example-coverage-matrix-row-family}. They display the family index $F$ and attach gallery, checker, beta-boundary, regression-spend, and exactness cells to the same finite matrix rows.

Apply the NameCert obligations of \autoref{thm:typed-example-coverage-matrix-namecert-obligations}. These obligations make carrier admission, family-index exactness, paired cell exposure, matrix exactness, and the structural rows $H,K,P,N$ the required read surface for every MetaCIC audit consumer.

The regression exactness, non-escape, spend boundary, row totality, checker-cell locality, and family exhaustion results then keep each consumer obligation on its displayed row. Thus the obligation surface is exactly $F,G,C,B,R,E,H,K,P,N$, with no hidden cell and no external checker theorem added to the matrix.
\end{proof}

\begin{theorem}[Typed example coverage matrix regression exactness]
\label{thm:typed-example-coverage-matrix-regression-exactness}
Every regression-spend cell in an accepted $\TypedExampleCoverageMatrixUp$ packet is paired with the unique displayed family row and with the corresponding gallery, checker, and beta cells. Conversely, every accepted family row has a displayed regression-spend cell in $R$.
\end{theorem}
\begin{proof}
Project the accepted packet to $F,G,C,B,R,E$. The exactness row $E$ is read against the common family index $F$, so each regression-spend entry is indexed before it is exposed to a checker-audit consumer.

For a displayed spend cell, $E$ supplies the matching gallery, checker, and beta cells in the same row. For a displayed family row, $E$ supplies the corresponding regression-spend cell in $R$.

Since the carrier has no unindexed spend coordinate, these two readings exhaust regression coverage for the packet.
\end{proof}

\begin{theorem}[Typed example coverage matrix non-escape]
\label{thm:typed-example-coverage-matrix-nonescape}
Every checker-audit consumer of an accepted $\TypedExampleCoverageMatrixUp$ carrier reads coverage only through the displayed matrix rows $F,G,C,B,R,E,H,K,P,N$. The consumer cannot replace a missing gallery, checker, beta, or regression-spend cell by a sibling packet alone, an ambient checker theorem, or an unindexed budget row.
\end{theorem}
\begin{proof}
Let a checker-audit consumer route be given. The route must pass through the displayed continuation row $K$ and therefore reaches only the family, cell, exactness, transport, provenance, and local naming rows.

The sibling packets provide provenance for the cell contents, but the matrix pairing is carried by $F$ and $E$. A sibling gallery, seal, budget, or beta-boundary packet alone does not store the combined cell row.

No coordinate in the carrier names an ambient checker theorem or an unindexed budget row. Hence the accepted consumer surface is exactly the finite coverage matrix.
\end{proof}

\begin{theorem}[Typed example coverage matrix spend boundary]
\label{thm:typed-example-coverage-matrix-spend-boundary}
If a checker-audit consumer spends a regression budget from an accepted $\TypedExampleCoverageMatrixUp$ packet, the spend is licensed only for the family row whose gallery, checker, beta, and regression-spend cells are all displayed in the same matrix row.
\end{theorem}
\begin{proof}
A regression spend is read from $R$ through the continuation route $K$. The exactness row $E$ identifies the family row in $F$ and the paired gallery, checker, and beta cells in $G,C,B$.

The transport row $H$ preserves the same row identity under componentwise $\hsame$ movement, while $P$ and $N$ record provenance and local naming only. They do not create a second spend endpoint.

Therefore a spend is confined to the displayed matrix row that carries all four cell kinds.
\end{proof}

\begin{theorem}[Typed example coverage matrix gallery rows]
\label{thm:typed-example-coverage-matrix-gallery-rows}
The coverage row family of \autoref{def:typed-example-coverage-matrix-row-family} contains displayed rows for the boolean, natural-number, pair, option, list, sum, and dependent-function gallery examples. For each such row, the $\TypedExampleCoverageMatrixUp$ packet exposes the same-family gallery cell in $G$, checker cell in $C$, beta-boundary cell in $B$, regression-spend cell in $R$, and exactness witness in $E$.
\end{theorem}
\begin{proof}
Project the accepted carrier to $F,G,C,B,R,E$. The row family is a finite subfamily of $F$, so each listed gallery example is named before any checker-audit cell is read.

Read $E$ at each of the boolean, natural-number, pair, option, list, sum, and dependent-function rows. The exactness row displays the matching $G,C,B,R$ cells at the same family index.

Because the cells are paired inside the same carrier, coverage of these examples is matrix coverage. It is not a claim that an external checker proves every term in the surrounding type system.
\end{proof}

\begin{theorem}[Typed example coverage matrix boundary rows]
\label{thm:typed-example-coverage-matrix-boundary-rows}
The accepted $\TypedExampleCoverageMatrixUp$ row family does not claim total dependent coverage, polymorphic total coverage, subject reduction, normalization, or checker completeness. Any consumer that needs those properties must add a separate carrier and NameCert surface; they are not obtained from the boolean, natural-number, pair, option, list, sum, or dependent-function gallery rows alone.
\end{theorem}
\begin{proof}
Inspect the carrier inventory of \autoref{def:typed-example-coverage-matrix-carrier}. The only coverage coordinates are the finite family index $F$, the displayed cells $G,C,B,R$, and the exactness row $E$.

The row family of \autoref{def:typed-example-coverage-matrix-row-family} selects named gallery examples from $F$. It does not add a coordinate for arbitrary dependent typing, polymorphic instantiation, subject reduction, normalization, or checker completeness.

The support rows $H,K,P,N$ transport, replay, source, and name the same matrix. They cannot manufacture a global type-system theorem outside the displayed coverage rows.
\end{proof}

\begin{theorem}[Typed example coverage matrix public audit route]
\label{thm:typed-example-coverage-matrix-public-audit-route}
Every public checker-audit read of an accepted $\TypedExampleCoverageMatrixUp$ packet passes through the finite route
$$
F,G,C,B,R,E,H,K,P,N .
$$
The route first names the family row $F$, then reads the paired gallery, checker, beta-boundary, and regression-spend cells $G,C,B,R$, then exposes matrix exactness through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $K$, $\Pkg$ provenance through $P$, and the local $\NameCert_{\TypedExampleCoverageMatrixUp}$ row through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:typed-example-coverage-matrix-carrier}. It contains no public checker-audit coordinates outside the displayed family, cell, exactness, transport, replay, provenance, and naming rows.

Use the row-family and NameCert obligation theorems, \autoref{def:typed-example-coverage-matrix-row-family} and \autoref{thm:typed-example-coverage-matrix-namecert-obligations}. They force a public audit consumer to enter through $F$ and to keep $G,C,B,R,E$ paired at the same finite row.

Apply non-escape and boundary rows, \autoref{thm:typed-example-coverage-matrix-nonescape} and \autoref{thm:typed-example-coverage-matrix-boundary-rows}. These exclude hidden checker completeness, subject reduction, normalization, arbitrary beta closure, and unindexed budget reads.

Finally read the structural rows $H,K,P,N$. They transport, replay, source, and name the same matrix route, so every public checker-audit read passes through exactly the displayed finite surface.
\end{proof}

\begin{theorem}[Typed example coverage matrix formal target boundary]
\label{thm:typed-example-coverage-matrix-formal-target-boundary}
The $\TypedExampleCoverageMatrixUp$ packet creates future formal targets only for the displayed finite rows
$$
F,G,C,B,R,E,H,K,P,N .
$$
Those targets may encode carrier admission, row-family exposure, paired gallery-checker-beta-spend exactness, public audit routing, non-escape, transport, replay, provenance, and local NameCert naming. They do not assert checker completeness, subject reduction, normalization, arbitrary beta closure, arbitrary gallery soundness, or coverage of examples outside the displayed family rows.
\end{theorem}
\begin{proof}
Start from the carrier and public audit route, \autoref{def:typed-example-coverage-matrix-carrier} and \autoref{thm:typed-example-coverage-matrix-public-audit-route}. Together they name the complete finite route that a checker-audit consumer may inspect.

Use the row-totality, checker-cell locality, and family-exhaustion theorems. They keep every gallery, checker, beta-boundary, and regression-spend cell attached to one displayed family row and rule out hidden families outside $F$.

Apply the boundary rows theorem. Since the matrix has no coordinate for checker completeness, subject reduction, normalization, arbitrary beta closure, arbitrary gallery soundness, or total example coverage, no formal target created by the matrix can include those claims.

The remaining formal targets are therefore row-local obligations about $F,G,C,B,R,E,H,K,P,N$ and their finite NameCert route.
\end{proof}

\begin{theorem}[Typed example coverage matrix public export]
\label{thm:typed-example-coverage-matrix-public-export}
Every MetaCIC audit consumer of an accepted $\TypedExampleCoverageMatrixUp$ packet receives only the family-indexed gallery, checker, beta-boundary, and regression-spend rows $F,G,C,B,R,E,H,K,P,N$. The export may cite the MetaCIC typed-examples gallery of \autoref{ch:concrete-instances-metacictypedexamplesgallery-namecert}, the typed-example audit budget of \autoref{ch:concrete-instances-typedexampleauditbudget-namecert}, and the beta-step boundary of \autoref{ch:concrete-instances-betastepboundary-namecert}, but it does not export checker completeness, subject reduction, normalization, arbitrary beta closure, or gallery soundness beyond the displayed family rows.
\end{theorem}
\begin{proof}
Project the accepted matrix carrier. The public rows are the family index $F$, the paired gallery, checker, beta-boundary, and regression-spend cells $G,C,B,R$, the exactness row $E$, and the structural rows $H,K,P,N$.

Read the cited sibling chapters through provenance. The MetaCIC typed-examples gallery supplies gallery cells, the audit-budget packet supplies regression-spend cells, and the beta-step boundary supplies beta-boundary cells; the coverage matrix is the place where those cells share one family index.

Apply the boundary-row theorem above. Since the exported rows do not contain subject reduction, normalization, checker completeness, arbitrary beta closure, or gallery soundness coordinates, a MetaCIC audit consumer receives only the displayed row-indexed matrix surface.
\end{proof}

\begin{theorem}[Typed example coverage matrix critical-path spend exhaustion]
\label{thm:typed-example-coverage-matrix-criticalpath-budget-exhaustion}
Any checker-audit spend from an accepted $\TypedExampleCoverageMatrixUp$ packet must expose the same family row in $F$, gallery cell in $G$, checker cell in $C$, beta-boundary cell in $B$, regression spend in $R$, exactness row in $E$, and structural rows $H,K,P,N$. Therefore the matrix cannot be used as subject-reduction evidence, normalization evidence, or checker-completeness evidence.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:typed-example-coverage-matrix-carrier}. The only spend coordinate is $R$, and it is indexed by the same finite family row $F$ as the gallery, checker, beta-boundary, and exactness cells.

Use regression exactness, the spend boundary, and checker-cell locality. These theorems keep every spend attached to the displayed $G,C,B,R,E$ cells at one row and prevent a consumer from moving the checker cell or budget cell to a different family.

Finally apply non-escape and family exhaustion. The structural rows $H,K,P,N$ preserve and replay the same packet, while the carrier has no coordinate for subject reduction, normalization, or checker completeness. The critical-path spend is therefore exhausted by the displayed matrix row.
\end{proof}

\begin{theorem}[Typed example coverage matrix row totality]
\label{thm:typed-example-coverage-matrix-row-totality}
Every accepted family row in $F$ has displayed gallery, checker, beta-boundary, and regression-spend cells in $G,C,B,R$, with the exactness row $E$ recording that the four cells belong to the same row.
\end{theorem}
\begin{proof}
Project the accepted carrier to $F,G,C,B,R,E$. A family row is accepted only as an element of the finite index $F$, so the row is named before any coverage cell is consumed.

Read the exactness row $E$ at that family index. The row-totality clause in $E$ displays the corresponding gallery cell in $G$, checker cell in $C$, beta-boundary cell in $B$, and regression-spend cell in $R$.

Because $E$ is part of the same carrier, the four cells are not imported independently from sibling chapters. They are the displayed cells of the accepted matrix row.
\end{proof}

\begin{theorem}[Typed example coverage matrix checker-cell locality]
\label{thm:typed-example-coverage-matrix-checker-cell-locality}
A checker cell from $C$ may be consumed only at the family row whose gallery cell in $G$, beta-boundary cell in $B$, and regression-spend cell in $R$ are paired with it through $E$.
\end{theorem}
\begin{proof}
Let a checker-audit consumer read a checker cell. The read is routed through $K$, hence it reaches $C$ only through the displayed matrix rows.

The exactness row $E$ identifies the family row in $F$ attached to that checker cell. At the same index it displays the paired gallery, beta-boundary, and regression-spend cells in $G,B,R$.

The transport row $H$ preserves this row identity under componentwise $\hsame$ movement. Thus a checker cell cannot be moved to a different family row or paired with unrelated sibling data.
\end{proof}

\begin{theorem}[Typed example coverage matrix family exhaustion]
\label{thm:typed-example-coverage-matrix-family-exhaustion}
The accepted family index $F$ exhausts the matrix surface of an accepted $\TypedExampleCoverageMatrixUp$ carrier: no ambient checker theorem, sibling gallery row, beta-boundary row, or unindexed budget row can add a hidden family outside $F$.
\end{theorem}
\begin{proof}
Inspect the carrier inventory. The only family coordinate is $F$, and the only coverage-cell coordinates are $G,C,B,R$ paired by $E$.

The provenance row $P$ records sibling ancestry, but provenance is not a second family index. Likewise, the continuation row $K$ only replays reads from the displayed matrix; it does not allocate another row.

Since the carrier has no coordinate for an ambient checker theorem or unindexed budget row, every accepted family is already one of the rows of $F$.
\end{proof}
\begin{theorem}[Typed example coverage matrix carrier admission]
\label{thm:typed-example-coverage-matrix-carrier-admission}
For an accepted packet $M=(F,G,C,B,R,E,H,K,P,N)$, carrier admission is exactly the finite matrix row $M$ together with the family index $F$ and the displayed checker-audit rows $G,C,B,R,E,H,K,P,N$. The carrier admission row does not replace the matrix by a sibling gallery packet, a checker packet, or a budget packet.
\end{theorem}
\begin{proof}
Project the accepted packet of \autoref{def:typed-example-coverage-matrix-carrier}. The family index $F$ is the first displayed row, so the carrier is admitted before any checker-audit consumer reads a cell.

Apply the NameCert obligations of \autoref{thm:typed-example-coverage-matrix-namecert-obligations}. They keep the displayed matrix together with the paired gallery, checker, beta, regression-spend, exactness, transport, continuation, provenance, and local naming rows. No sibling packet can supply the same combined row.
\end{proof}
\begin{theorem}[Typed example coverage matrix context coverage]
\label{thm:typed-example-coverage-matrix-context-coverage}
Every accepted $\TypedExampleCoverageMatrixUp$ carrier covers its typed-example context exactly through the family rows in $F$ and the paired gallery, checker, beta, and regression-spend rows in $G,C,B,R$. The context coverage row is therefore the displayed matrix row, not an ambient parameter-transport theorem.
\end{theorem}
\begin{proof}
Read the row-totality theorem and the checker-cell locality theorem. Together they force each family row in $F$ to carry all four displayed cell kinds in the same matrix row.

Then use family exhaustion. It rules out hidden family rows outside the matrix surface, so the accepted context is exhausted by the displayed $F,G,C,B,R$ rows and their exactness row $E$.
\end{proof}
\begin{theorem}[Typed example coverage matrix ledger non-escape]
\label{thm:typed-example-coverage-matrix-ledger-nonescape}
The local ledger row of an accepted $\TypedExampleCoverageMatrixUp$ packet cannot escape the matrix surface: every regression-spend read, checker read, gallery read, or beta-boundary read remains tied to the same family row and its paired exactness data. In particular, the ledger does not leave a residual unindexed budget row behind the displayed packet.
\end{theorem}
\begin{proof}
Project the carrier and read the continuation row $K$. It routes every read through the displayed matrix rows, so a ledger read reaches only $F,G,C,B,R,E,H,P,N$.

Now apply regression exactness and non-escape. They keep the spend row paired with the family row and rule out replacement by a sibling packet or an unindexed budget row. The ledger therefore stays inside the accepted matrix surface.
\end{proof}

\begin{theorem}[TypedExampleCoverageMatrix component route]
\label{thm:typed-example-coverage-matrix-component-route}
Any checker, beta-boundary, gallery, or regression-spend consumer of an accepted $\TypedExampleCoverageMatrixUp$ carrier must read the gallery row, checker row, beta-step row, regression-spend row, row-totality evidence, checker-locality evidence, family-exhaustion evidence, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert_{\TypedExampleCoverageMatrixUp}$ rows as one packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:typed-example-coverage-matrix-carrier}. The accepted packet already displays the family row $F$, the gallery row $G$, the checker row $C$, the beta-boundary row $B$, the regression-spend row $R$, the exactness row $E$, and the structural rows $H,K,P,N$.

Apply the NameCert obligations of \autoref{thm:typed-example-coverage-matrix-namecert-obligations}. They force a consumer to read the family index, paired cell exposure, matrix exactness, transport, continuation, provenance, and local naming as one finite matrix packet.

The regression exactness, non-escape, spend boundary, row-totality, checker-cell locality, and family-exhaustion theorems keep every checker, beta-boundary, gallery, or regression-spend read on the same accepted row. Therefore the consumer cannot split the packet into independent cells.
\end{proof}

\begin{theorem}[Typed example coverage matrix TasteGate obligation surface]
\label{thm:typed-example-coverage-matrix-tastegate-obligation-surface}
Before any closure promotion, the coverage-matrix obligation surface of an accepted $\TypedExampleCoverageMatrixUp$ packet is exactly the finite family index $F$, the paired gallery, checker, beta, and regression-spend cells $G,C,B,R$, the exactness row $E$, and the nonescape route through $H,K,P,N$. A TasteGate witness for this chapter must expose those rows as field-faithful carrier evidence and must not replace the displayed matrix by a sibling gallery, checker theorem, beta route, regression budget, or ambient audit object.
\end{theorem}
\begin{proof}
Project the carrier $M=(F,G,C,B,R,E,H,K,P,N)$ of \autoref{def:typed-example-coverage-matrix-carrier}. The family index $F$ is named before any coverage cell can be inspected.

Use row totality \autoref{thm:typed-example-coverage-matrix-row-totality} and checker-cell locality \autoref{thm:typed-example-coverage-matrix-checker-cell-locality}. They bind each gallery, checker, beta, and regression-spend cell to the same family row through $E$.

Finally apply family exhaustion \autoref{thm:typed-example-coverage-matrix-family-exhaustion} and non-escape \autoref{thm:typed-example-coverage-matrix-nonescape}. The remaining rows $H,K,P,N$ transport, replay, source, and name only the displayed matrix, so a TasteGate carrier witness must be built from the same obligation surface before the closure block can record a stronger grade.
\end{proof}

\begin{theorem}[Typed example coverage matrix scoped export]
\label{thm:typed-example-coverage-matrix-scoped-export}
An accepted $\TypedExampleCoverageMatrixUp$ packet exports exactly the finite row-indexed coverage surface over MetaCIC typed examples, gallery rows, checker rows, seal rows, regression-spend rows, beta-boundary rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\TypedExampleCoverageMatrixUp}$. The export is the scoped matrix surface $F,G,C,B,R,E,H,K,P,N$; it is not checker completeness, subject reduction, normalization, arbitrary beta closure, arbitrary gallery soundness, or an ambient MetaCIC audit theorem.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:typed-example-coverage-matrix-carrier}. The row family of \autoref{def:typed-example-coverage-matrix-row-family} and the context coverage theorem of \autoref{thm:typed-example-coverage-matrix-context-coverage} identify the exported index as the finite family rows in $F$ together with their paired $G,C,B,R,E$ cells.

Use ledger non-escape \autoref{thm:typed-example-coverage-matrix-ledger-nonescape}. It keeps checker, gallery, beta-boundary, seal, and regression-spend reads tied to the same matrix row, so the export cannot acquire a residual budget, sibling gallery, or ambient checker route outside $F$.

Finally apply the component route of \autoref{thm:typed-example-coverage-matrix-component-route} and the TasteGate obligation surface of \autoref{thm:typed-example-coverage-matrix-tastegate-obligation-surface}. These rows preserve the same componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ support, and therefore the scoped export is exactly the finite matrix surface stated above.
\end{proof}

\begin{theorem}[Typed example coverage matrix public certificate]
\label{thm:typed-example-coverage-matrix-public-certificate}
The public checker-audit certificate exported by $\TypedExampleCoverageMatrixUp$ is exactly the finite row-indexed surface
$$
F,G,C,B,R,E,H,K,P,N .
$$
Any consumer citing this certificate receives family-index exactness through $F,E$, paired gallery, checker, beta-boundary, and regression cells through $G,C,B,R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $K$, $\Pkg$ provenance through $P$, and the local $\NameCert_{\TypedExampleCoverageMatrixUp}$ row through $N$. The certificate exports no subject-reduction theorem, normalization theorem, checker-completeness theorem, arbitrary beta closure, arbitrary gallery soundness, hidden family row, or unindexed regression budget.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:typed-example-coverage-matrix-namecert-obligations}. They identify carrier admission, family-index exactness, paired cell exposure, matrix exactness, transport, replay, provenance, and local naming as the only certificate rows.

Use row totality and checker-cell locality, \autoref{thm:typed-example-coverage-matrix-row-totality} and \autoref{thm:typed-example-coverage-matrix-checker-cell-locality}. These bind every accepted family row to its gallery, checker, beta-boundary, and regression cells, so the public consumer receives the paired matrix row rather than detached sibling cells.

Apply non-escape and critical-path spend exhaustion, \autoref{thm:typed-example-coverage-matrix-nonescape} and \autoref{thm:typed-example-coverage-matrix-criticalpath-budget-exhaustion}. They keep gallery, checker, beta-boundary, and regression-spend reads tied to the same family row and rule out subject reduction, normalization, checker completeness, and unindexed budget use. The public certificate is therefore exactly the displayed surface $F,G,C,B,R,E,H,K,P,N$.
\end{proof}

\begin{corollary}[Typed example coverage matrix consumer exactness]
\label{cor:typed-example-coverage-matrix-consumer-exactness}
Any consumer of the public certificate in \autoref{thm:typed-example-coverage-matrix-public-certificate} receives exactly the displayed family-indexed gallery, checker, beta-boundary, regression-spend, exactness, transport, continuation, provenance, and $\NameCert_{\TypedExampleCoverageMatrixUp}$ rows. The consumer cannot derive checker completeness, subject reduction, normalization, arbitrary beta closure, arbitrary gallery soundness, hidden family rows, or an unindexed regression budget from that certificate.
\end{corollary}
\begin{proof}
Start with the public certificate. It displays only the finite surface $F,G,C,B,R,E,H,K,P,N$ and names the family-indexed gallery, checker, beta-boundary, regression-spend, exactness, transport, replay, provenance, and local naming rows.

Use family exhaustion from \autoref{thm:typed-example-coverage-matrix-family-exhaustion}. No hidden family row can be added outside $F$.

Use checker-cell locality from \autoref{thm:typed-example-coverage-matrix-checker-cell-locality}. Every checker read remains paired with the same gallery, beta-boundary, regression-spend, and exactness data.

Finally apply non-escape through \autoref{thm:typed-example-coverage-matrix-nonescape} and \autoref{thm:typed-example-coverage-matrix-ledger-nonescape}. These prevent a consumer from replacing the displayed rows by checker completeness, subject reduction, normalization, arbitrary beta closure, arbitrary gallery soundness, or an unindexed budget row.
\end{proof}

\begin{theorem}[Typed example coverage matrix formal target row]
\label{thm:typed-example-coverage-matrix-formal-target-row}
The next formal target surface for $\TypedExampleCoverageMatrixUp$ consists of a $\mathsf{Nontrivial}$ witness pair, a $\mathsf{FieldFaithful}$ mk-field projection, and TasteGate single-carrier alignment over the displayed rows
$$
F,G,C,B,R,E,H,K,P,N .
$$
This target surface is confined to the already displayed coverage-matrix carrier, row-totality evidence, checker-cell locality, family exhaustion, TasteGate obligation surface, and public certificate. It does not include checker completeness, subject reduction, normalization, arbitrary beta closure, or gallery soundness.
\end{theorem}
\begin{proof}
Use the carrier of \autoref{def:typed-example-coverage-matrix-carrier} to supply the witness pair and mk-field projections from the displayed rows $F,G,C,B,R,E,H,K,P,N$.

Apply row totality, checker-cell locality, and family exhaustion, \autoref{thm:typed-example-coverage-matrix-row-totality}, \autoref{thm:typed-example-coverage-matrix-checker-cell-locality}, and \autoref{thm:typed-example-coverage-matrix-family-exhaustion}. These keep every gallery, checker, beta-boundary, and regression-spend cell tied to the same finite family row.

Finally apply the TasteGate obligation surface and the public certificate, \autoref{thm:typed-example-coverage-matrix-tastegate-obligation-surface} and \autoref{thm:typed-example-coverage-matrix-public-certificate}. They identify the single-carrier alignment surface and exclude checker completeness, subject reduction, normalization, arbitrary beta closure, and gallery soundness from the target.
\end{proof}

\begin{theorem}[Typed example coverage matrix Lean handoff contract]
\label{thm:typed-example-coverage-matrix-lean-handoff-contract}
Any Lean namespace accepting the formal target row for $\TypedExampleCoverageMatrixUp$ must expose $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances over the displayed matrix rows
$$
F,G,C,B,R,E,H,K,P,N .
$$
The namespace may not replace the matrix by the gallery sibling, the beta-boundary sibling, the regression-budget sibling, or an ambient audit theorem; the instances must preserve the row-indexed coverage carrier itself.
\end{theorem}
\begin{proof}
Read the formal target row of \autoref{thm:typed-example-coverage-matrix-formal-target-row}. It names the witness pair, mk-field projection, and TasteGate alignment over exactly $F,G,C,B,R,E,H,K,P,N$.

Use the public certificate of \autoref{thm:typed-example-coverage-matrix-public-certificate}. That certificate is the finite matrix surface and excludes checker completeness, subject reduction, normalization, arbitrary beta closure, arbitrary gallery soundness, hidden family rows, and unindexed regression budgets.

Therefore a future namespace can discharge the target only by exposing the nontriviality and field-faithfulness witnesses for the displayed matrix rows themselves. A sibling gallery or ambient audit theorem would omit the shared row index $F$ or the paired cells $G,C,B,R,E$, so it cannot serve as the handoff contract.
\end{proof}

\falsifiablePrediction{If the coverage-matrix row is removed, a checker-audit consumer can no longer verify that every accepted typed-example family has paired gallery, checker, beta, and regression-spend cells.}

\independenceWitness{Nearest siblings: metacictypedexamplesgallery, typedexamplegallery, metacic-typedexamples-seal, typedexampleauditbudget, betastepboundary. None carries the combined matrix-cell carrier because each sibling exposes one surface family, while the coverage matrix stores the shared family index and the paired gallery, checker, beta, and regression-spend cells in one packet.}

The Lean follow-up for namespace $\mathsf{BEDC.Derived.TypedExampleCoverageMatrixUp.TasteGate}$ must include $\mathsf{Nontrivial}\ \TypedExampleCoverageMatrixUp$ and $\mathsf{FieldFaithful}\ \TypedExampleCoverageMatrixUp$ instances with an explicit witness pair and mk-field projections.

\closureat{TypedExampleCoverageMatrixUp}{scopedStr}

\begin{theorem}[Typed example coverage matrix scope closure record]
\label{thm:typed-example-coverage-matrix-scope-closure-record}
The scoped paper surface of $\TypedExampleCoverageMatrixUp$ is the finite matrix export recorded in \autoref{thm:typed-example-coverage-matrix-scoped-export}, with its obligation rows supplied by carrier admission, family-index exactness, paired gallery-checker-beta-spend exposure, matrix exactness, row-totality, checker-cell locality, family exhaustion, the TasteGate obligation surface, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\TypedExampleCoverageMatrixUp}$. This record closes only that scoped matrix surface.
\end{theorem}

\begin{theorem}[Typed example coverage matrix public bridge]
\label{thm:typed-example-coverage-matrix-public-bridge}
The public $\TypedExampleCoverageMatrixUp$ read exports exactly the finite family index, the gallery cells, checker cells, beta-boundary cells, regression-spend cells, exactness row, nonescape route, component route, and Lean-handoff boundary already displayed by the chapter. In carrier notation it is the public route through $F,G,C,B,R,E,H,K,P,N$, and it exports no checker completeness, subject reduction, normalization, arbitrary beta closure, gallery soundness, hidden family row, or unindexed regression budget.
\end{theorem}
\begin{proof}
Read the carrier of \autoref{def:typed-example-coverage-matrix-carrier}. The rows $F,G,C,B,R,E,H,K,P,N$ are the whole public matrix surface: $F$ supplies the family index, $G,C,B,R$ supply the paired gallery, checker, beta, and regression-spend cells, and $E,H,K,P,N$ supply exactness, transport, replay, provenance, and local naming.

Apply the nonescape and component routes, \autoref{thm:typed-example-coverage-matrix-nonescape} and \autoref{thm:typed-example-coverage-matrix-public-audit-route}, together with the public certificate of \autoref{thm:typed-example-coverage-matrix-public-certificate}. These results keep every exported row attached to the same finite family index and exclude hidden gallery, checker, beta, or spend data.

Finally use the Lean handoff contract of \autoref{thm:typed-example-coverage-matrix-lean-handoff-contract}. It fixes the formal boundary at the displayed matrix rows themselves, so the public bridge exports exactly the existing paper-side witness surface and does not alter the verification axis.
\end{proof}

\begin{closurestatus}{\TypedExampleCoverageMatrixUp}
  \constructivestory{A $\TypedExampleCoverageMatrixUp$ packet is generated from a finite typed-example family index, paired gallery cells, checker cells, beta cells, regression-spend cells, exactness rows, componentwise transports, continuation routes, package provenance, and a local NameCert row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:typed-example-coverage-matrix-carrier}, \autoref{def:typed-example-coverage-matrix-row-family}, \autoref{thm:typed-example-coverage-matrix-public-audit-route}, \autoref{thm:typed-example-coverage-matrix-boundary-rows}, \autoref{thm:typed-example-coverage-matrix-scoped-export}, and \autoref{thm:typed-example-coverage-matrix-scope-closure-record} bind the finite rows $F,G,C,B,R,E,H,K,P,N$ as the row-local coverage matrix surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close checker completeness, subject reduction, normalization, arbitrary beta closure, arbitrary gallery soundness, Lean verification, or bridge checking.}
  \upgradepath{Public closure requires a Lean TasteGate carrier marker for $\TypedExampleCoverageMatrixUp$ together with a public-object read of the finite row surface $F,G,C,B,R,E,H,K,P,N$.}
\end{closurestatus}
