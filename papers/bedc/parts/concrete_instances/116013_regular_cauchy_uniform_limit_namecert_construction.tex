\chapter{A Concrete Naming Certificate for $\RegularCauchyUniformLimitUp$}
\label{ch:concrete-instances-regular-cauchy-uniform-limit-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchyUniformLimitUp$ packet records the finite uniform-limit route for a sequence of regular Cauchy real-name packets before any completion-facing consumer may read a terminal real seal. It sits over the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the convergence-criterion route of \autoref{ch:concrete-instances-cauchy-convergence-criterion-namecert}. The packet names a constructive uniform-limit handoff; it does not assert ambient $\RealUp$ completeness, quotient stream equality, host function equality, selected limits, or countable choice.

\begin{definition}[Regular Cauchy uniform-limit carrier]
\label{def:regular-cauchy-uniform-limit-carrier}
A $\RegularCauchyUniformLimitUp$ carrier is a finite $\BHist$ packet
$$
U=(S,M,D,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the sequence row, listing the accepted regular Cauchy real-name packets whose values are available only through displayed finite histories.
\item $M$ is the uniform Cauchy modulus row, giving one finite precision schedule that controls the tail of the whole sequence.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, recording the requested dyadic radii, split budgets, and comparison rows consumed by $M$.
\item $W$ is the $\StreamNameUp$ window row for the sequence indices, value names, modulus requests, and terminal read positions.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the windows in $W$ and to the regular rational observations required by $D$.
\item $E$ is the terminal $\RealUp$ seal row reached only after the rows $S,M,D,W,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the sequence, modulus, tolerance, window, readback, real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a uniform-limit request through sequence lookup, modulus inspection, dyadic tolerance, finite windows, regular rational readback, and real sealing.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyUniformLimitUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\CauchyConvergenceCriterionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyUniformLimitUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $S,M,D,W,R,E,H,C,P,N$. It has no coordinate for quotient stream equality, a selected limit, countable-choice schedules, host function equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Regular Cauchy uniform-limit NameCert obligations]
\label{thm:regular-cauchy-uniform-limit-namecert-obligations}
For every accepted $\RegularCauchyUniformLimitUp$ carrier
$$
U=(S,M,D,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyUniformLimitUp}$ surface consists exactly of the following finite obligation rows: carrier admission for the displayed regular Cauchy sequence, uniform modulus, DyadicRatCore tolerance ledger, StreamName windows, RegSeqRat readback, Real seal, transport, replay, provenance, and local name; uniform-modulus exactness through $M$ and $D$; finite-window readback through $W$ and $R$; convergence-criterion compatibility through the $\CauchyConvergenceCriterionUp$ route named by $P$; terminal Real-seal discipline through $E$; and componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ support.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-uniform-limit-carrier}. The rows $S,M,D,W,R,E,H,C,P,N$ are the only available coordinates, so carrier admission and local naming are finite row projections.

The uniformity row is $M$, and its tolerance budget is the dyadic row $D$. Any precision request must read those rows before a tail comparison can be consumed, so the route is uniform over the displayed sequence rather than selected separately for each index.

The observation rows are $W$ and $R$. They force all inspected values through StreamName windows and RegSeqRat readbacks before the terminal seal $E$ is available.

Finally $H,C,P,N$ transport, replay, source, and name the same packet. Since the carrier has no quotient, selector, choice, host-function, or ambient-completeness coordinate, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy uniform-limit Real handoff]
\label{thm:regular-cauchy-uniform-limit-real-handoff}
Every $\RealUp$ or completion-facing consumer of an accepted $\RegularCauchyUniformLimitUp$ packet factors through the finite route
$$
\StreamNameUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp,\ \CauchyConvergenceCriterionUp,\ \RealUp .
$$
The handoff reads the sequence row, a displayed uniform Cauchy modulus, dyadic tolerance rows, finite StreamName windows, RegSeqRat readbacks, the convergence-criterion route, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row before the terminal real seal is consumed. It exports no ambient $\RealUp$ completeness, quotient stream equality, selected limit, countable choice, or host function equality.
\end{theorem}
\begin{proof}
Begin with the obligation theorem \autoref{thm:regular-cauchy-uniform-limit-namecert-obligations}. It places the uniform modulus and dyadic tolerance rows before any real-facing read.

The sibling route then fixes the finite observation order. StreamName supplies the windows, RegSeqRat supplies the rational readback rows, and DyadicRatCore supplies the tolerance comparisons needed by the uniform modulus.

The convergence-criterion row named in the provenance keeps the handoff on the regular Cauchy route before the terminal $\RealUp$ seal is read. Transport and replay through $H$ and $C$ preserve the same packet, while $P,N$ source and name it.

Because none of the excluded principles is present in the carrier, the consumer receives only the displayed finite route and not an ambient completeness or quotient-function endpoint.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyUniformLimitUp$ real-facing read exposes regular Cauchy sequence data, one uniform modulus row, DyadicRatCore tolerance rows, StreamName windows, RegSeqRat readbacks, a convergence-criterion route, a terminal RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert rows.}

\independenceWitness{streamname, regseqrat, dyadicratcore, real, cauchyconvergencecriterion: none is bijective to $\RegularCauchyUniformLimitUp$ because this packet alone binds a sequence of regular Cauchy real-name packets to one displayed uniform modulus and a terminal RealUp handoff without quotient streams, selected limits, countable choice, host function equality, or ambient Real completeness.}

\closureat{RegularCauchyUniformLimitUp}{seedStr}

\begin{closurestatus}{\RegularCauchyUniformLimitUp}
  \constructivestory{A $\RegularCauchyUniformLimitUp$ packet is generated from regular Cauchy real-name sequence rows, one uniform Cauchy modulus row, DyadicRatCore tolerance rows, StreamName windows, RegSeqRat readbacks, a convergence-criterion route, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-uniform-limit-carrier}, \autoref{thm:regular-cauchy-uniform-limit-namecert-obligations}, and \autoref{thm:regular-cauchy-uniform-limit-real-handoff} seed the finite regular-Cauchy uniform-limit route through uniform modulus, dyadic tolerance, StreamName windows, RegSeqRat readback, convergence criterion, transport, replay, provenance, local naming, and Real seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient Real completeness, quotient stream equality, selected limits, countable choice, host function equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, uniform-modulus exactness, dyadic tolerance coverage, finite-window readback, convergence-criterion compatibility, Real-seal discipline, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyUniformLimitUp.}
\end{closurestatus}
