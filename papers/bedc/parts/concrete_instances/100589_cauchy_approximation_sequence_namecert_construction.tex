\chapter{A Concrete Naming Certificate for $\CauchyApproximationSequenceUp$}
\label{ch:concrete-instances-cauchy-approximation-sequence-namecert}
\origin{human}

Bishop--Bridges constructive analysis treats a real name as a precision-indexed approximation practice: a requested tolerance is answered by displayed rational data and an explicit regularity route, not by a completed host value. The $\CauchyApproximationSequenceUp$ packet records that finite request-to-seal surface between the dyadic endpoint ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the regular completion boundary of \autoref{ch:concrete-instances-bishop-regular-cauchy-completion-namecert}. It gives later Real and Completion consumers one named route from precision requests to finite approximants, while refusing quotient stream equality, selected limits, countable choice, host real equality, trichotomy, and ambient $\RealUp$ completeness.

\begin{definition}[Cauchy approximation sequence carrier]\label{def:cauchy-approximation-sequence-carrier}
A $\CauchyApproximationSequenceUp$ carrier is a finite $\BHist$ packet
$$
A=(Q,D,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the requested-precision row, listing the finite tolerance questions currently being answered by the packet.
\item $D$ is the $\DyadicRatCoreUp$ approximant ledger, recording dyadic approximants, dyadic radius comparisons, and the finite error budgets attached to each requested precision.
\item $S$ is the $\StreamNameUp$ stream-window schedule opened for the requested precisions and no other hidden observations.
\item $R$ is the $\RegSeqRatUp$ regular-rational readback row obtained from the displayed stream windows and dyadic approximant ledger.
\item $E$ is the $\RealUp$ seal row reached only after $Q,D,S,R$ have been exposed and replayed.
\item $H$ records componentwise $\hsame$ transport for requested precisions, dyadic approximants, stream-window schedules, regular readbacks, the Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a precision request through $D$, then through $S$ and $R$, and finally into the terminal $\RealUp$ seal.
\item $P$ is the $\Pkg$ provenance over $\CauchyApproximationSequenceUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BishopRegularCauchyCompletionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyApproximationSequenceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $Q,D,S,R,E,H,C,P,N$. The carrier has no coordinate for quotient stream equality, selected limits, countable choice, host real equality, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Cauchy approximation sequence NameCert obligations]\label{thm:cauchy-approximation-sequence-namecert-obligations}
For every accepted $\CauchyApproximationSequenceUp$ carrier $A=(Q,D,S,R,E,H,C,P,N)$, the local $\NameCert_{\CauchyApproximationSequenceUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the requested-precision row, dyadic approximant ledger, stream-window schedule, regular-rational readback, Real seal, transport, replay, provenance, and local naming rows;
\item request exactness, requiring each precision question to be read from $Q$ before any dyadic approximant in $D$ is accepted;
\item dyadic ledger exactness, requiring $D$ to record the approximant and tolerance comparison used for the displayed request, not an ambient real value;
\item window and readback exactness, requiring $S$ and $R$ to expose only the finite stream windows and regular rational data demanded by $Q$ and justified by $D$;
\item Real-seal boundary, requiring $E$ to remain downstream of $Q,D,S,R$ and never to serve as a selected limit or host equality object;
\item componentwise transport and replay, requiring $H$ and $C$ to preserve exactly the displayed route from requested precision to Real seal;
\item provenance and local naming, requiring $P$ and $N$ to export the finite route over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\BishopRegularCauchyCompletionUp$ without adding quotient equality, choice, trichotomy, or ambient completeness.
\end{enumerate}
No approximation-sequence obligation is stored outside these rows.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyApproximationSequenceUp.CauchyApproximationSequenceTasteGate\_single\_carrier\_alignment}
\begin{proof}
Carrier admission is projection from \autoref{def:cauchy-approximation-sequence-carrier}. The accepted packet displays exactly the requested precisions $Q$, dyadic ledger $D$, stream windows $S$, regular readback $R$, Real seal $E$, and structural rows $H,C,P,N$.

Request exactness and dyadic ledger exactness follow from the route order. A consumer first reads a finite precision question from $Q$; only then may it consult $D$ for the dyadic approximant and tolerance comparison attached to that question.

The window and readback rows are likewise downstream of the request and ledger. The schedule $S$ opens only the finite windows demanded by $Q$, and $R$ records the regular-rational data read through those windows under the displayed dyadic budget.

Finally, the Real seal row $E$ is terminal for this finite route. The transport row $H$, replay row $C$, provenance row $P$, and local naming row $N$ preserve and name the same displayed packet, so no quotient stream equality, selected limit, countable-choice selector, host real equality, trichotomy principle, or ambient completeness theorem can become a local obligation.
\end{proof}

\begin{theorem}[Cauchy approximation sequence Real seal boundary]\label{thm:cauchy-approximation-sequence-real-seal-boundary}
Every $\RealUp$ consumer of an accepted $\CauchyApproximationSequenceUp$ carrier reads only the displayed request-to-seal rows
$$
Q,D,S,R,E,H,C,P,N .
$$
Consequently the consumer receives finite requested precisions, dyadic approximants, stream-window schedules, regular-rational readbacks, a Real seal, componentwise transport, displayed replay, provenance, and local naming data. It receives no quotient stream equality, selected limit, countable choice, host real equality, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Let a $\RealUp$ consumer read an accepted packet $A=(Q,D,S,R,E,H,C,P,N)$. Carrier projection gives access only to the rows named in \autoref{def:cauchy-approximation-sequence-carrier}.

The read begins with a requested precision in $Q$. The dyadic ledger $D$ supplies the corresponding approximant and tolerance comparison; the stream schedule $S$ opens the finite observation windows; and the regular readback $R$ exposes the rational data that the Real seal may consume.

The terminal row $E$ is therefore reached only after the finite route through $Q,D,S,R$ has been displayed. The structural rows $H,C,P,N$ transport, replay, source, and name the same route without adding any second equality or limit coordinate.

Since the packet has no carrier row for quotient stream equality, selected limits, countable choice, host real equality, trichotomy, or ambient $\RealUp$ completeness, a Real-facing consumer cannot read any of those objects from the local certificate.
\end{proof}

\falsifiablePrediction{Every $\CauchyApproximationSequenceUp$ Real-facing read factors through finite requested precisions, a DyadicRatCore approximant ledger, selected StreamName windows, RegSeqRat readbacks, a RealUp seal row, hsame transport, Cont replay, Pkg provenance, and local naming.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, bishopregularcauchycompletion: $\CauchyApproximationSequenceUp$ is not bijective to any listed sibling because it jointly carries requested precisions, dyadic approximant rows, finite stream-window schedules, regular-rational readback, a terminal Real seal, transport, replay, provenance, and local naming in one request-indexed packet.}

\closureat{CauchyApproximationSequenceUp}{seedStr}

\begin{closurestatus}{\CauchyApproximationSequenceUp}
  \constructivestory{A $\CauchyApproximationSequenceUp$ packet is generated from requested precision rows, a DyadicRatCore approximant ledger, selected StreamName windows, RegSeqRat readback, a RealUp seal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-approximation-sequence-carrier}, \autoref{thm:cauchy-approximation-sequence-namecert-obligations}, and \autoref{thm:cauchy-approximation-sequence-real-seal-boundary} seed the finite request-indexed route from dyadic precision questions to RealUp endpoint consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected limits, countable choice, host real equality, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, request exactness, dyadic ledger exactness, window and readback exactness, Real seal boundary, componentwise transport, replay, provenance, and local naming for BEDC.Derived.CauchyApproximationSequenceUp.}
\end{closurestatus}
