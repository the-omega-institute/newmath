\chapter{A Concrete Naming Certificate for $\FiniteObservationRefutationUp$}
\label{ch:concrete-instances-finite-observation-refutation-namecert}
\origin{ai-composite}

The $\FiniteObservationRefutationUp$ packet realizes the finite-observation open-fit boundary of \autoref{sec:finite-observation-open-fit} as a concrete BEDC NameCert carrier. It sits beside the open physical fit surface of \autoref{ch:concrete-instances-open-physical-fit-namecert}, the reality-constrained open-fit packet of \autoref{ch:concrete-instances-reality-constrained-open-fit-namecert}, the open-fit packet of \autoref{ch:concrete-instances-open-fit-packet-namecert}, the open-fit residue gate of \autoref{ch:concrete-instances-open-fit-residue-gate-namecert}, and the physical model audit surface of \autoref{ch:concrete-instances-physical-model-audit-namecert}. Those siblings expose finite fit, open continuation, residue, verdict, and audit rows; this carrier isolates the displayed defeating observation row beside the finite fit row without adding a final-fit oracle.

\begin{definition}[FiniteObservationRefutation carrier]
\label{def:finite-observation-refutation-carrier}
A $\FiniteObservationRefutationUp$ carrier is a finite $\BHist$ packet
$$
J=(T,\Pi,O,M,F,D,V,L,E,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $T$ is the finite trace row through which the observation is read.
\item $\Pi$ is the observation-bundle row naming the finite questions used for the read.
\item $O$ is the observed-signature row extracted from $T$ through $\Pi$.
\item $M$ is the model-signature row generated over the same finite trace and bundle.
\item $F$ is the finite-fit row whose acceptance is being tested.
\item $D$ is the displayed defeating-observation row, storing the finite observation that makes $O$ and $M$ fail the accepted classifier.
\item $V$ is the verdict row separating finite defeat, current undefeatedness, and final-fit refusal.
\item $L$ is the ledger row recording the finite fit scope, the defeating observation, and the refusal to promote the prefix into final confirmation.
\item $E$ is the open-continuation row recording admissible observation extensions still outside the finite packet.
\item $H$ records componentwise $\hsame$ transport for $T,\Pi,O,M,F,D,V,L,E,C,P,N$.
\item $C$ records displayed $\Cont$ replay through the trace, bundle, signature, fit, defeat, verdict, ledger, and continuation rows.
\item $P$ is the $\Pkg$ provenance over $\FiniteObservationRefutationUp$, $\OpenPhysicalFitUp$, $\RealityConstrainedOpenFitUp$, $\OpenFitPacketUp$, $\OpenFitResidueGateUp$, $\PhysicalModelAuditUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteObservationRefutationUp}$ row.
\end{enumerate}
The carrier has no coordinate for total model identity, host physical truth, all-prefix confirmation, or a final-fit oracle.
\end{definition}
\leanchecked{BEDC.Derived.FiniteObservationRefutationUp.FiniteObservationRefutationTasteGate\_single\_carrier\_alignment}

\begin{theorem}[FiniteObservationRefutation NameCert obligations]
\label{thm:finite-observation-refutation-namecert-obligations}
For the carrier of \autoref{def:finite-observation-refutation-carrier}, the local $\NameCert_{\FiniteObservationRefutationUp}$ surface consists exactly of carrier admission for
$$
J=(T,\Pi,O,M,F,D,V,L,E,H,C,P,N),
$$
trace and bundle exposure through $T,\Pi$, observed and model signature exposure through $O,M$, finite-fit exposure through $F$, defeating-observation exposure through $D$, verdict exposure through $V$, ledger and open-continuation exposure through $L,E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the displayed carrier. The tuple $J=(T,\Pi,O,M,F,D,V,L,E,H,C,P,N)$ is the complete finite packet, so carrier admission exposes every row that can be read by a consumer.

Read the observational rows first. The trace and bundle rows $T,\Pi$ determine the finite questions, while $O$ and $M$ are the observed and model-generated signatures compared by the finite-fit row $F$.

The defeating observation is stored in $D$ and classified by $V$. A finite defeat therefore enters the certificate as a visible row of the same packet, not as a hidden host objection or a global claim about all observations.

The ledger and continuation rows separate defeat from openness. The ledger $L$ records the finite scope and the refusal of final confirmation, while $E$ preserves admissible observations not yet queried. The structural rows $H,C,P,N$ transport, replay, authenticate, and name that same packet, so the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[FiniteObservationRefutation open-fit consumer]
\label{thm:finite-observation-refutation-open-fit-consumer}
Every open-fit consumer reading an accepted $\FiniteObservationRefutationUp$ packet receives the displayed finite route
$$
T,\Pi,O,M,F,D,V,L,E,H,C,P,N.
$$
A displayed defeating observation refutes only the finite-fit row $F$ through $D$ and $V$; it does not supply model-reality identity, host physical truth, total confirmation, all-prefix fit, or a final-fit oracle.
\end{theorem}
\begin{proof}
Start with the carrier in \autoref{def:finite-observation-refutation-carrier}. The only rows available to an open-fit consumer are the trace, bundle, signature, finite-fit, defeating-observation, verdict, ledger, continuation, transport, replay, provenance, and naming rows displayed in $J$.

Apply the NameCert obligation surface of \autoref{thm:finite-observation-refutation-namecert-obligations}. It forces the consumer route to expose $D$ and $V$ together with $F$, so a defeat is a finite observation against the displayed finite-fit row.

Use the sibling boundaries from \autoref{thm:open-physical-fit-refutation-boundary}, \autoref{thm:reality-constrained-open-fit-finite-refutation-surface}, \autoref{thm:open-fit-finite-refutation-surface}, \autoref{thm:open-fit-residue-gate-namecert-obligations}, and \autoref{thm:physical-model-audit-failure-surface-exhaustion}. They keep refutation, residue, open continuation, and audit failure surfaces inside finite carrier rows rather than turning them into final physical adequacy.

Since $J$ has no coordinate for final fit or total identity, the consumer receives a finite refutation packet and an open continuation boundary. The defeating observation can invalidate the current finite-fit row, but it cannot close the remaining observation surface or provide a final-fit oracle.
\end{proof}

\begin{theorem}[Defeating-observation row exposure]
\label{thm:finite-observation-refutation-defeat-row-exposure}
Every accepted $\FiniteObservationRefutationUp$ packet exposes $D$ and $V$ as the only finite defeating-observation route for the displayed finite-fit row $F$. In particular, a consumer of
$$
J=(T,\Pi,O,M,F,D,V,L,E,H,C,P,N)
$$
may refute $F$ through the pair $D,V$, but cannot extract any second defeat row, hidden host verdict, total model identity, or final-fit oracle from the packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-observation-refutation-carrier}. Its visible coordinates contain one defeating-observation row $D$ and one verdict row $V$, both placed beside the finite-fit row $F$.

Apply the NameCert obligation surface of \autoref{thm:finite-observation-refutation-namecert-obligations}. It exposes $D$ and $V$ as ordinary carrier rows and lists no additional defeat coordinate.

Apply the open-fit consumer boundary of \autoref{thm:finite-observation-refutation-open-fit-consumer}. The consumer can read the finite route through $D,V$, while the absent coordinates rule out hidden host truth, total identity, all-prefix confirmation, and final-fit output. Thus $D,V$ are the exhaustive finite defeating-observation route for $F$.
\end{proof}

\begin{theorem}[Open-continuation separation]
\label{thm:finite-observation-refutation-open-continuation-separation}
For every accepted $\FiniteObservationRefutationUp$ packet, the continuation row $E$ preserves admissible future observations while the pair $D,V$ refutes only the current finite-fit row $F$. The same packet therefore separates current finite defeat from closure of the remaining observation surface.
\end{theorem}
\begin{proof}
Read the displayed tuple $J=(T,\Pi,O,M,F,D,V,L,E,H,C,P,N)$. The row $E$ is a continuation coordinate, whereas $D$ and $V$ are the defeating-observation and verdict coordinates attached to $F$.

Use \autoref{thm:finite-observation-refutation-namecert-obligations} to keep these rows distinct under local naming. The certificate exposes $L$ as the ledger of finite scope and $E$ as the open-continuation row, so the defeating route through $D,V$ does not consume the continuation surface.

Use \autoref{thm:finite-observation-refutation-open-fit-consumer} to read the packet as an open-fit boundary. The finite defeat invalidates the displayed row $F$, while the absence of a final-fit oracle and the presence of $E$ leave the admissible continuation rows outside the current packet.
\end{proof}

\begin{theorem}[TasteGate witness surface]
\label{thm:finite-observation-refutation-tastegate-witness-surface}
The existing Lean TasteGate, $\mathsf{Nontrivial}$, and $\mathsf{FieldFaithful}$ witnesses for $\FiniteObservationRefutationUp$ are consumed as the single-carrier witness surface for the displayed tuple
$$
T,\Pi,O,M,F,D,V,L,E,H,C,P,N.
$$
They certify that the packet has a nonempty carrier, field-readable coordinates, and one BEDC witness surface over the same carrier shape used by the local NameCert obligations.
\end{theorem}
\begin{proof}
Start from the checked TasteGate marker attached to \autoref{def:finite-observation-refutation-carrier}. It names the Lean theorem that aligns the $\FiniteObservationRefutationUp$ carrier with the single tuple shape
$$
T,\Pi,O,M,F,D,V,L,E,H,C,P,N.
$$

The carrier definition gives the row order, and \autoref{thm:finite-observation-refutation-namecert-obligations} supplies the local naming duties for those rows. The Lean witness surface supplies nontriviality and field faithfulness for the same carrier rather than for a separate presentation.

Finally apply \autoref{thm:finite-observation-refutation-defeat-row-exposure} and \autoref{thm:finite-observation-refutation-open-continuation-separation}. They identify the defeat and continuation rows consumed by the TasteGate surface, so the witness certifies exactly the displayed packet rather than an abstract replacement.
\end{proof}

\falsifiablePrediction{A displayed defeating observation refutes only the finite-fit row and never supplies a final-fit oracle.}

\independenceWitness{openphysicalfit, realityconstrainedopenfit, openfitpacket, openfitresiduegate, physicalmodelaudit: none is bijective because the refutation packet alone stores the defeating observation row as a public carrier coordinate beside fit, continuation, ledger, transport, route, provenance, and local NameCert rows.}

The Lean TasteGate path \texttt{lean4/BEDC/Derived/FiniteObservationRefutationUp/TasteGate.lean} provides explicit $\mathsf{Nontrivial}\ \FiniteObservationRefutationUp$ and $\mathsf{FieldFaithful}\ \FiniteObservationRefutationUp$ instances for the displayed carrier.

\closureat{FiniteObservationRefutationUp}{obligationStr}[BEDC.Derived.FiniteObservationRefutationUp.FiniteObservationRefutationTasteGate\_single\_carrier\_alignment]

\begin{closurestatus}{\FiniteObservationRefutationUp}
  \constructivestory{A $\FiniteObservationRefutationUp$ packet is generated from a finite trace, observation bundle, observed signature, model signature, finite-fit row, displayed defeating observation, verdict, ledger, open-continuation row, componentwise hsame transports, displayed Cont routes, Pkg provenance, and local NameCert row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:finite-observation-refutation-carrier}, \autoref{thm:finite-observation-refutation-namecert-obligations}, \autoref{thm:finite-observation-refutation-open-fit-consumer}, \autoref{thm:finite-observation-refutation-defeat-row-exposure}, \autoref{thm:finite-observation-refutation-open-continuation-separation}, and \autoref{thm:finite-observation-refutation-tastegate-witness-surface} bind the finite refutation carrier, local NameCert obligations, open-fit consumer boundary, defeating-observation exposure, open-continuation separation, and TasteGate witness surface.}
  \leanchecked{BEDC.Derived.FiniteObservationRefutationUp.taste\_gate}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.FiniteObservationRefutationUp.FiniteObservationRefutationTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \origin{ai-composite}
  \notclaimed{This chapter does not close Lean verification, bridge checking, model-reality identity, host physical truth, all-prefix fit, total confirmation, or final-fit oracles.}
  \upgradepath{Scoped closure requires checked carrier admission and theorem coverage for field faithfulness across $T,\Pi,O,M,F,D,V,L,E,H,C,P,N$.}
\end{closurestatus}
