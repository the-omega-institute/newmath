$\RegularCauchyChoiceFreeCompletionUp$ consumes the metric-completion dependency surface of \autoref{ch:concrete-instances-metriccompletion-namecert} through the displayed $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CauchyCompletionReflectorUp$, and $\RealUp$ seal route already bound by its carrier and real-seal theorems. The packet receives this route as finite completion-facing data and receives no quotient completion, selected limit, countable-choice schedule, host equality of completed objects, or ambient $\RealUp$ completeness principle.

\begin{theorem}[Regular Cauchy choice-free sibling lattice route]
\label{thm:regular-cauchy-choice-free-completion-sibling-lattice-route}
Every accepted $\RegularCauchyChoiceFreeCompletionUp$ packet consumes \autoref{ch:concrete-instances-metriccompletion-namecert} and \autoref{ch:concrete-instances-bishop-completion-induction-namecert} only through the displayed sibling lattice
$$
\RegSeqRatUp,\ \StreamNameUp,\ \DyadicRatCoreUp,\ \CompletionUniquenessUp,\ \CauchyCompletionReflectorUp,\ \RealUp .
$$
The route exports finite completion-facing data for the choice-free carrier, transport, replay, provenance, and local naming rows. It exports no quotient completion, selected limit, choice schedule, host equality of completed objects, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Read the metric-completion sibling first. Its completion-facing surface is consumed only after the regular rational source row, stream-window row, and dyadic tolerance row have been displayed by the choice-free packet.

Read the Bishop completion induction sibling next. The induction route is attached to the same $\CompletionUniquenessUp$ comparison and $\CauchyCompletionReflectorUp$ route before the terminal $\RealUp$ seal is exposed.

The carrier, real-seal, transport, replay, provenance, and local naming rows preserve exactly this sibling lattice. Since the packet contains no quotient-completion, selected-limit, choice-schedule, host-equality, or ambient-completeness coordinate, no consumer can extract those endpoints from the route.
\end{proof}

\begin{theorem}[Regular Cauchy choice-free sibling route]
\label{thm:regular-cauchy-choice-free-completion-sibling-route}
Every accepted $\RegularCauchyChoiceFreeCompletionUp$ route consumes \autoref{ch:concrete-instances-metriccompletion-namecert}, \autoref{ch:concrete-instances-bishop-completion-induction-namecert}, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, and $\RealUp$ as sibling $\NameCert$ surfaces. The route exposes them only as finite completion-facing rows for regular rational source data, finite stream windows, dyadic tolerance accounting, induction replay, metric-completion dependency, and terminal real sealing. It exposes no quotient completion, selected limit, countable-choice schedule, host equality of completed objects, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Read the sibling lattice of \autoref{thm:regular-cauchy-choice-free-completion-sibling-lattice-route}. The metric-completion and Bishop-induction siblings are admitted only after the regular rational, stream-window, and dyadic-tolerance surfaces have been displayed.

The route then reaches the terminal $\RealUp$ seal through the same completion-facing rows. Since no quotient-completion, selected-limit, choice-schedule, host-equality, or ambient-completeness coordinate is present in the carrier, the listed sibling surfaces exhaust the visible route.
\end{proof}
