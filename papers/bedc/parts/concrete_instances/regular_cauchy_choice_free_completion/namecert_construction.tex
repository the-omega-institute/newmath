\chapter{A Concrete Naming Certificate for $\RegularCauchyChoiceFreeCompletionUp$}
\label{ch:concrete-instances-regular-cauchy-choice-free-completion-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchyChoiceFreeCompletionUp$ packet records the choice-free regular-Cauchy route to a completion-facing real seal as finite BEDC data. It sits after the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the separated comparison surface of \autoref{ch:concrete-instances-completion-uniqueness-namecert}, the public reflector route of \autoref{ch:concrete-instances-cauchycompletionreflector-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a non-quotient completion route; it does not choose representatives, quotient regular sequences, assert ambient $\RealUp$ completeness, or introduce countable choice.

\begin{definition}[Regular-Cauchy choice-free completion carrier]
\label{def:regular-cauchy-choice-free-completion-carrier}
A $\RegularCauchyChoiceFreeCompletionUp$ carrier is a finite $\BHist$ packet
$$
K=(R,W,D,U,F,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RegSeqRatUp$ source row, carrying the regular rational name and its displayed readback requests.
\item $W$ is the $\StreamNameUp$ window row, listing the finite windows through which the regular name is inspected.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger attached to those windows and to the regular-Cauchy modulus requests.
\item $U$ is the $\CompletionUniquenessUp$ comparison row, recording separated comparison of two displayed completion routes without quotienting them.
\item $F$ is the $\CauchyCompletionReflectorUp$ route row, exposing unit, counit, idempotent replay, universal factorization, and reflector transport as public finite data.
\item $E$ is the terminal $\RealUp$ seal row reached only after $R,W,D,U,F$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the source, window, tolerance, comparison, reflector, real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a regular-Cauchy request through $R,W,D,U,F,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyChoiceFreeCompletionUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\CompletionUniquenessUp$, $\CauchyCompletionReflectorUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyChoiceFreeCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $R,W,D,U,F,E,H,C,P,N$. It has no coordinate for quotient completion equality, selected representatives, countable choice, host equality of completed objects, open-cover compactness, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Regular-Cauchy choice-free completion NameCert obligations]
\label{thm:regular-cauchy-choice-free-completion-namecert-obligations}
For every accepted $\RegularCauchyChoiceFreeCompletionUp$ carrier
$$
K=(R,W,D,U,F,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyChoiceFreeCompletionUp}$ surface consists exactly of regular rational source exposure through $R$, finite window exposure through $W$, dyadic tolerance exposure through $D$, separated completion comparison through $U$, reflector routing through $F$, terminal real sealing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-choice-free-completion-carrier}. Rows $R,W,D$ are the regular-Cauchy observation spine, and rows $U,F,E$ are the completion-facing comparison, reflector, and real-seal data.

The observation spine is read before the reflector row is consumed. The $\RegSeqRatUp$ source is inspected only through $\StreamNameUp$ windows, and the dyadic tolerance ledger fixes the finite precision route available to a completion consumer.

The comparison and reflector rows stay displayed. Row $U$ records separated comparison of completion routes, while $F$ exposes the public unit, counit, idempotent, extension, and reflector faces without adding a quotient carrier or a selected limit.

Finally $H,C,P,N$ transport, replay, source, and name exactly the same packet. Since no excluded coordinate occurs in the carrier, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Regular-Cauchy choice-free real-seal route]
\label{thm:regular-cauchy-choice-free-completion-real-seal-route}
Every $\RealUp$ read of an accepted $\RegularCauchyChoiceFreeCompletionUp$ packet factors through the finite route
$$
\RegSeqRatUp,\ \StreamNameUp,\ \DyadicRatCoreUp,\ \CompletionUniquenessUp,\ \CauchyCompletionReflectorUp,\ \RealUp .
$$
The route exposes regular readback, finite windows, dyadic tolerance, separated comparison, and the public reflector surface before the terminal real seal is consumed. It does not close ambient $\RealUp$ completeness, quotient regular sequences, selected representatives, countable choice, host equality of completed points, or open-cover compactness.
\end{theorem}
\begin{proof}
Begin with the obligation surface of \autoref{thm:regular-cauchy-choice-free-completion-namecert-obligations}. It forces every real-facing consumer through the source, window, tolerance, comparison, reflector, and seal rows.

The sibling rows supply the route in order. The regular source is read through \autoref{ch:concrete-instances-regseqrat-namecert}; finite access is through \autoref{ch:concrete-instances-streamname-namecert}; dyadic tolerance is through \autoref{ch:concrete-instances-dyadicratcore-namecert}; separated comparison is through \autoref{ch:concrete-instances-completion-uniqueness-namecert}; and public reflection is through \autoref{ch:concrete-instances-cauchycompletionreflector-namecert}.

The terminal row $E$ is therefore only the displayed $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. Because the carrier has no quotient, selector, choice, host-equality, compactness, or ambient-completeness coordinate, none can be exported by the seal.
\end{proof}

\begin{theorem}[Regular Cauchy choice-free completion StreamName anchor]
\label{thm:regular-cauchy-choice-free-completion-streamname-anchor}
Every accepted $\RegularCauchyChoiceFreeCompletionUp$ completion read factors through the finite $\StreamNameUp$ window row and the $\RegSeqRatUp$ readback row before any terminal $\RealUp$ seal is consumed. The read is therefore anchored in \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}, with $W$ listing the inspected windows, $R$ supplying the regular rational source rows, and $E$ serving only as the downstream real-name seal.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-choice-free-completion-carrier}. The rows $R,W,E$ are displayed before any completion-facing consumer can read the real seal.

Apply the real-seal route theorem \autoref{thm:regular-cauchy-choice-free-completion-real-seal-route}. It places $\RegSeqRatUp$ and $\StreamNameUp$ upstream of the $\RealUp$ terminal row and keeps the completion comparison and reflector rows downstream of the same finite observation surface.

Thus every accepted read factors first through the sibling StreamName and RegSeqRat anchors, and the Real row is only the terminal seal of that displayed route.
\end{proof}

\begin{theorem}[Regular-Cauchy choice-free cofinal-window refinement]
\label{thm:regular-cauchy-choice-free-completion-obligation-refinement}
Let
$$
K=(R,W,D,U,F,E,H,C,P,N)
$$
be an accepted $\RegularCauchyChoiceFreeCompletionUp$ carrier. If a completion-facing precision request for $K$ is served by a displayed $\RegularCauchyWindowCofinalityUp$ row, then the request refines through the finite route
$$
\StreamNameUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \CauchyCompletionReflectorUp,\ \RealUp
$$
before the real seal is consumed. The refinement preserves the StreamName request rows, DyadicRatCore tolerance rows, RegSeqRat classifier and regularity rows, the reflector row $F$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ support.

No quotient stream equality, selected limit, countable-choice schedule, host equality of completed objects, open-cover compactness datum, or ambient $\RealUp$ completeness principle can replace the displayed cofinal-window surface in this refinement.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:regular-cauchy-choice-free-completion-namecert-obligations}. It projects $K$ onto the displayed source, window, tolerance, comparison, reflector, seal, transport, replay, provenance, and naming rows.

Use \autoref{thm:regular-cauchy-window-cofinality-namecert-obligations} for the precision request. The cofinality row exposes the finite $\StreamNameUp$ window, the $\DyadicRatCoreUp$ tolerance comparison, the $\RegSeqRatUp$ readback, and the cofinal schedule before any completion-facing consumer can read the request.

Finally apply \autoref{thm:regular-cauchy-choice-free-completion-real-seal-route}. The real-seal route places the reflector row $F$ and the terminal $\RealUp$ seal downstream of the same finite observation surface, so the cofinal-window refinement is consumed as displayed BEDC data and not as quotient streams, selected limits, countable choice, host equality, compactness, or ambient completeness.
\end{proof}

\begin{theorem}[Regular-Cauchy cofinal-window exactness]
\label{thm:regular-cauchy-choice-free-completion-cofinal-window-exactness}
Let
$$
K=(R,W,D,U,F,E,H,C,P,N)
$$
be an accepted $\RegularCauchyChoiceFreeCompletionUp$ carrier. Every completion-facing precision request for $K$ served by a $\RegularCauchyWindowCofinalityUp$ row is exact only through the displayed route
$$
\StreamNameUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \CauchyCompletionReflectorUp,\ \RealUp .
$$
The exactness surface is the finite StreamName window row, the DyadicRatCore tolerance row, the RegSeqRat source readback, the reflector row $F$, and the terminal real seal $E$. It cannot be replaced by quotient stream equality, selected limits, countable-choice schedules, host equality, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:regular-cauchy-choice-free-completion-carrier}. The only completion-facing rows available from $K$ are the displayed source, window, tolerance, comparison, reflector, seal, transport, replay, provenance, and local naming rows.

Apply \autoref{thm:regular-cauchy-window-cofinality-namecert-obligations} to the cofinal-window request. It exposes the finite window and tolerance data as BEDC rows before the request can be consumed by a completion-facing read.

Finally apply \autoref{thm:regular-cauchy-choice-free-completion-real-seal-route}. Since the real seal is reached only after the same displayed route through $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\CauchyCompletionReflectorUp$, the cofinal-window row is the unique completion-facing exactness surface available to the packet.
\end{proof}

\begin{theorem}[Regular-Cauchy choice-free window coverage]
\label{thm:regular-cauchy-choice-free-completion-window-coverage}
Let
$$
K=(R,W,D,U,F,E,H,C,P,N)
$$
be an accepted $\RegularCauchyChoiceFreeCompletionUp$ carrier. Before a completion-facing precision request for $K$ is read, the packet exposes the $\RegSeqRatUp$ source row $R$, the $\StreamNameUp$ window row $W$, the $\DyadicRatCoreUp$ tolerance row $D$, and the displayed $\RegularCauchyWindowCofinalityUp$ cofinal-window rows that serve the request. These rows are the public finite window surface for the request, and no later reflector or real-seal read can bypass them.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:regular-cauchy-choice-free-completion-carrier}. Its first three rows are exactly the regular-rational source, finite-window, and dyadic-tolerance rows available before the comparison, reflector, and real-seal rows.

Use \autoref{thm:regular-cauchy-choice-free-completion-namecert-obligations} to identify those rows as part of the local NameCert surface. For a precision request served by a cofinal-window row, \autoref{thm:regular-cauchy-choice-free-completion-obligation-refinement} and \autoref{thm:regular-cauchy-choice-free-completion-cofinal-window-exactness} place the $\RegularCauchyWindowCofinalityUp$ evidence on the same displayed route.

Finally apply the real-seal route theorem \autoref{thm:regular-cauchy-choice-free-completion-real-seal-route}. Since the terminal seal is downstream of $R,W,D$ and the cofinal-window evidence, every accepted precision read sees that finite window surface before any completion-facing seal is consumed.
\end{proof}

\begin{theorem}[Regular-Cauchy choice-free tail reflection]
\label{thm:regular-cauchy-choice-free-completion-tail-reflection}
Let
$$
K=(R,W,D,U,F,E,H,C,P,N)
$$
be an accepted $\RegularCauchyChoiceFreeCompletionUp$ carrier. Every tail precision read of $K$ reflects through the displayed route
$$
\StreamNameUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \CauchyCompletionReflectorUp,\ \RealUp,
$$
preserving the packet rows $R,W,D,U,F,E,H,C,P,N$ and any $\RegularCauchyWindowCofinalityUp$ support used to serve the request. The reflected read cannot be replaced by quotient stream equality, selected limits, countable-choice schedules, host equality, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-choice-free-completion-carrier}. The tail precision read can inspect only the source, window, tolerance, comparison, reflector, seal, transport, replay, provenance, and local naming rows.

Apply the NameCert obligations of \autoref{thm:regular-cauchy-choice-free-completion-namecert-obligations}. They bind the accepted packet to the rows $R,W,D,U,F,E,H,C,P,N$ and give no separate completion object.

Use \autoref{thm:regular-cauchy-choice-free-completion-cofinal-window-exactness} and \autoref{thm:regular-cauchy-choice-free-completion-window-coverage}. The cofinal support exposes the finite $\RegularCauchyWindowCofinalityUp$ rows beside the $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerances, and $\RegSeqRatUp$ readback before the reflector and real seal are consumed.

Finally apply the real-seal route of \autoref{thm:regular-cauchy-choice-free-completion-real-seal-route}. The reflected read reaches $\CauchyCompletionReflectorUp$ and $\RealUp$ only downstream of that finite tail-window surface, so quotient stream equality, selected limits, countable choice, host equality, open-cover compactness, and ambient completeness cannot replace it.
\end{proof}

\begin{theorem}[Regular-Cauchy choice-free reflector nonescape]
\label{thm:regular-cauchy-choice-free-completion-reflector-nonescape}
Let
$$
K=(R,W,D,U,F,E,H,C,P,N)
$$
be an accepted $\RegularCauchyChoiceFreeCompletionUp$ carrier. The $\CauchyCompletionReflectorUp$ row $F$ and the terminal $\RealUp$ seal row $E$ export no quotient representative, selected limit, countable-choice schedule, host equality of completed objects, open-cover compactness datum, or ambient $\RealUp$ completeness principle. They only expose the displayed reflector route and the downstream real-name seal already named by the packet.
\end{theorem}
\begin{proof}
Start with \autoref{thm:regular-cauchy-choice-free-completion-namecert-obligations}. The obligation surface lists $F$ as a displayed reflector row and $E$ as a terminal real-seal row, with no carrier coordinate for quotient representatives, selected limits, choice schedules, host equality, compactness, or ambient completeness.

The route theorem \autoref{thm:regular-cauchy-choice-free-completion-real-seal-route} keeps both rows downstream of the regular source, finite windows, dyadic tolerance, and separated comparison rows. Thus $F$ can only route the displayed completion data, and $E$ can only seal the resulting real-name boundary.

The cofinal-window exactness theorem \autoref{thm:regular-cauchy-choice-free-completion-cofinal-window-exactness} rules out replacing the displayed window surface by any excluded completion principle. Hence neither the reflector row nor the real-seal row exports data outside the carrier's finite route.
\end{proof}

\begin{theorem}[Regular-Cauchy choice-free transport and replay preservation]
\label{thm:regular-cauchy-choice-free-completion-transport-replay}
Let
$$
K=(R,W,D,U,F,E,H,C,P,N)
$$
be an accepted $\RegularCauchyChoiceFreeCompletionUp$ carrier. Componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and the local $\NameCert_{\RegularCauchyChoiceFreeCompletionUp}$ row $N$ preserve exactly the packet rows $R,W,D,U,F,E$ together with their displayed cofinal-window support. They add no quotient completion principle, selected limit, countable-choice schedule, host equality, open-cover compactness, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Project the carrier definition. Rows $H,C,P,N$ are not new completion data; they are the transport, replay, provenance, and local naming rows attached to the displayed source, window, tolerance, comparison, reflector, and seal rows.

Apply \autoref{thm:regular-cauchy-choice-free-completion-namecert-obligations}. It says the local NameCert surface consists exactly of $R,W,D,U,F,E,H,C,P,N$, so transport and replay preserve the same packet rows rather than extending the classifier with an extra completion coordinate.

Use \autoref{thm:regular-cauchy-choice-free-completion-real-seal-route} and \autoref{thm:regular-cauchy-choice-free-completion-window-coverage} for the downstream route. The same finite observation surface reaches the reflector and real seal after componentwise transport, displayed replay, provenance, and local naming, and the excluded completion principles never appear in the carrier.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyChoiceFreeCompletionUp$ read exposes RegSeqRat source, StreamName window, DyadicRatCore tolerance, CompletionUniqueness comparison, CauchyCompletionReflector route, RealUp seal, componentwise hsame, Cont replay, Pkg provenance, and local NameCert rows before any completion-facing real seal is consumed.}

\independenceWitness{regseqrat, streamname, dyadicratcore, completion-uniqueness, cauchycompletionreflector, real: none is bijective with $\RegularCauchyChoiceFreeCompletionUp$ because this packet alone binds the regular-Cauchy source, finite-window tolerance spine, separated comparison row, public reflector route, and terminal RealUp seal without quotient completion or countable choice.}

\closureat{RegularCauchyChoiceFreeCompletionUp}{seedStr}

\begin{closurestatus}{\RegularCauchyChoiceFreeCompletionUp}
  \constructivestory{A $\RegularCauchyChoiceFreeCompletionUp$ packet is generated from RegSeqRat source rows, StreamName windows, DyadicRatCore tolerance rows, CompletionUniqueness comparison rows, CauchyCompletionReflector route rows, RealUp seal rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-choice-free-completion-carrier}, \autoref{thm:regular-cauchy-choice-free-completion-namecert-obligations}, and \autoref{thm:regular-cauchy-choice-free-completion-real-seal-route} seed the finite choice-free regular-Cauchy completion route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completions, selected representatives, countable choice, host equality of completed objects, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, window coverage, dyadic tolerance exactness, separated comparison, reflector routing, RealUp seal nonescape, TasteGate nontriviality, FieldFaithful coverage, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyChoiceFreeCompletionUp.}
\end{closurestatus}
