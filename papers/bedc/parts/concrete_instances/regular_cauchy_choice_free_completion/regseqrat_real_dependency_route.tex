The $\RegularCauchyChoiceFreeCompletionUp$ packet consumes \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-real-namecert}, and \autoref{ch:concrete-instances-streamname-namecert} through finite regular-Cauchy windows before any completion-facing seal is read. The $\RegSeqRatUp$ row supplies the regular rational source and readback requests, the $\StreamNameUp$ row bounds those requests by displayed windows, and the $\RealUp$ row appears only as the terminal seal reached after the reflector and comparison rows have replayed the same finite data. This route gives downstream completion consumers no quotient representative, selected limit, countable-choice schedule, host equality of completed objects, or ambient real-completeness principle.
