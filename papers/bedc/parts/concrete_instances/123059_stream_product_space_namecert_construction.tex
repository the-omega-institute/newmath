\chapter{A Concrete Naming Certificate for $\StreamProductSpaceUp$}
\label{ch:concrete-instances-stream-product-space-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis and Kelley's product-space topology, the $\StreamProductSpaceUp$ packet records the countable product interface obtained when certified stream names are read coordinatewise through finite product windows. It sits over the stream-name surface of \autoref{ch:concrete-instances-streamname-namecert}, the binary product constructor of \autoref{ch:concrete-instances-prod-namecert}, the metric observation route of \autoref{ch:concrete-instances-metric-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite product-window carrier for countably indexed certified names; it does not import Tychonoff compactness, countable choice, quotient equality of streams, selected limits, or ambient completeness of $\RealUp$.

\begin{definition}[Stream product-space carrier]
\label{def:stream-product-space-carrier}
A $\StreamProductSpaceUp$ carrier is a finite $\BHist$ packet
$$
S=(I,W,\Pi,M,R,E,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the finite index-window row listing the coordinates inspected by the current product observation.
\item $W$ is the family of $\StreamNameUp$ rows exposed on the coordinates of $I$.
\item $\Pi$ is the iterated $\ProdUp$ spine that packages the coordinate rows from $W$ into one finite product-window row.
\item $M$ is the $\MetricUp$ observation row used to compare product windows at the displayed coordinates.
\item $R$ is the $\RegSeqRatUp$ readback row carrying regular rational tolerances for the inspected product window.
\item $E$ is the terminal $\RealUp$ seal row reached only after $R$ has been displayed.
\item $F$ is the finite-projection ledger recording which coordinate projection is being read from $\Pi$.
\item $H$ records componentwise $\hsame$ transport for $I,W,\Pi,M,R,E,F$ and the local certificate rows.
\item $C$ records displayed $\Cont$ replay from coordinate stream names through product packaging, metric observation, regular rational readback, Real sealing, and finite projection.
\item $P$ is the $\Pkg$ provenance over $\StreamProductSpaceUp$, $\StreamNameUp$, $\ProdUp$, $\MetricUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\StreamProductSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,W,\Pi,M,R,E,F,H,C,P,N$. It has no coordinate for an infinite choice function, quotient equality of stream names, a selected product limit, Tychonoff compactness, or ambient real completeness beyond the displayed $\RealUp$ seal.
\end{definition}

\begin{theorem}[Stream product-space NameCert obligations]
\label{thm:stream-product-space-namecert-obligations}
For every accepted $\StreamProductSpaceUp$ carrier
$$
S=(I,W,\Pi,M,R,E,F,H,C,P,N),
$$
the local $\NameCert_{\StreamProductSpaceUp}$ surface consists exactly of finite index-window exposure through $I$, coordinate stream-name exposure through $W$, finite product packaging through $\Pi$, metric observation through $M$, regular rational readback through $R$, Real sealing through $E$, finite projection ledgering through $F$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The product-window data are $I,W,\Pi,F$, the observation data are $M,R,E$, and the structural rows are $H,C,P,N$.

The index row $I$ bounds the visible coordinate set. The stream rows $W$ are read only on those coordinates, and the iterated product row $\Pi$ packages exactly that finite visible family.

The observation route then passes through $M,R,E$. The metric row compares displayed product windows, the regular rational row records the tolerance readback, and the Real row seals only that readback.

Finally read $F,H,C,P,N$. The projection ledger exposes one coordinate of the same finite product row, while transport, replay, provenance, and local naming preserve the displayed rows. Since no excluded infinite selector or quotient object is a carrier coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Stream product-space pointwise transport]
\label{thm:stream-product-space-pointwise-transport}
Every accepted $\StreamProductSpaceUp$ packet transports coordinate observations pointwise: if a consumer reads coordinate $i$ through the finite projection ledger $F$, then the read factors through the displayed index window $I$, the coordinate $\StreamNameUp$ row in $W$, the product spine $\Pi$, and the structural rows $H,C,P,N$ before any metric, regular-rational, or Real-facing observation is consumed.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:stream-product-space-namecert-obligations}. It forces every coordinate read to expose $I,W,\Pi,F$ as part of the same accepted carrier.

The projection row $F$ has no independent coordinate data. It can only point into the product spine $\Pi$, and $\Pi$ was built from the $\StreamNameUp$ rows displayed by $W$ over the finite index window $I$.

The rows $H,C,P,N$ preserve that route under transport, replay, provenance, and local naming. Thus a downstream consumer receives a pointwise stream-name transport surface, not an externally selected product element or quotient stream equality.
\end{proof}

\begin{theorem}[Stream product-space finite projection scope]
\label{thm:stream-product-space-finite-projection-scope}
Every Real-facing or metric-facing read from an accepted $\StreamProductSpaceUp$ packet remains scoped to a finite projection window. The route must pass through
$$
\StreamNameUp,\ \ProdUp,\ \MetricUp,\ \RegSeqRatUp,\ \RealUp
$$
as displayed rows of the carrier, and it exports no Tychonoff theorem, countable-choice selector, quotient equality of stream names, selected limit, or ambient completeness principle for $\RealUp$.
\end{theorem}
\begin{proof}
Apply the NameCert obligation theorem \autoref{thm:stream-product-space-namecert-obligations}. Any Real-facing or metric-facing consumer must read $M,R,E$ downstream of the finite product-window data $I,W,\Pi,F$.

The metric row $M$ observes only the displayed product window. The regular rational readback $R$ and the Real seal $E$ are reached after that observation, so they cannot expand the scope to an infinite product object.

The excluded principles are not fields of the carrier and are not produced by the structural rows $H,C,P,N$. The public scope is therefore the finite projection route through certified stream names and product packaging.
\end{proof}

\falsifiablePrediction{An accepted $\StreamProductSpaceUp$ read is refuted if it reaches a metric or Real-facing product observation without displaying the finite index window, coordinate StreamName rows, product spine, metric observation row, RegSeqRat readback, Real seal, finite projection ledger, componentwise transport, displayed replay, provenance, and local NameCert rows.}

\independenceWitness{streamname, prod, metric, regseqrat, real: $\StreamProductSpaceUp$ is not bijective to any listed sibling because it jointly binds finite coordinate windows, certified stream-name rows, product packaging, metric observation, rational readback, Real sealing, finite projection ledgering, transport, replay, provenance, and local naming in one countable-product interface.}

\closureat{StreamProductSpaceUp}{seedStr}

\begin{closurestatus}{\StreamProductSpaceUp}
  \constructivestory{A $\StreamProductSpaceUp$ packet is generated from a finite index-window row, coordinate StreamName rows, an iterated Prod spine, a Metric observation row, a RegSeqRat readback row, a Real seal row, a finite projection ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:stream-product-space-carrier}, \autoref{thm:stream-product-space-namecert-obligations}, \autoref{thm:stream-product-space-pointwise-transport}, and \autoref{thm:stream-product-space-finite-projection-scope} seed the finite countable-product interface for certified stream names through StreamNameUp, ProdUp, MetricUp, RegSeqRatUp, RealUp, and local NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Tychonoff compactness, countable choice, quotient stream equality, selected limits, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, coordinate StreamName exposure, product-spine packaging, metric observation, regular rational readback, Real sealing, finite projection determinacy, transport, replay, provenance, and local naming for BEDC.Derived.StreamProductSpaceUp.}
\end{closurestatus}
