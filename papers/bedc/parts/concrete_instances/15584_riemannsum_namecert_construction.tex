\chapter{A Concrete Naming Certificate for $\RiemannSumUp$}
\label{ch:concrete-instances-riemannsum-namecert}
\origin{human}

Following Riemann and Darboux, the $\RiemannSumUp$ packet records the finite tagged-sum surface used before a consumer asks for an integral carrier. It sits between the dyadic mesh ledger of \autoref{ch:concrete-instances-dyadicmesh-namecert}, the finite partition surface of \autoref{ch:concrete-instances-partition-namecert}, the stepwise approximation rows of \autoref{ch:concrete-instances-dyadicstepfunction-namecert}, the Darboux comparison carrier of \autoref{ch:concrete-instances-darbouxintegral-namecert}, and the broader tagged-mesh handoff of \autoref{ch:concrete-instances-riemannintegral-namecert}. The packet names only the finite mesh, tag, value, width, and arithmetic rows of a displayed Riemann sum; it does not assert host integrability, a limiting process, quotient equality of functions, or choice-selected tags outside the packet.

\begin{definition}[Riemann sum carrier]
\label{def:riemann-sum-carrier}
A $\RiemannSumUp$ carrier is a finite $\BHist$ packet
$$
Q=(M,T,F,W,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the mesh row, read through $\DyadicMeshUp$ and the finite $\PartitionUp$ boundary, naming the ordered cells, endpoints, and adjacency ledger.
\item $T$ is the tag-containment row, recording one displayed tag for each carried cell together with the dyadic or rational containment witness that places the tag inside that cell.
\item $F$ is the integrand-read window, exposing exactly the finitely many carried values needed at the displayed tags.
\item $W$ is the cell-width ledger, recording the width row attached to each mesh cell through the same dyadic or rational endpoint data.
\item $S$ is the finite sum row, computed from the displayed tag values and cell widths by the carried arithmetic route.
\item $H$ records componentwise $\hsame$ transport for mesh, tag containment, integrand reads, width rows, sum row, continuation row, provenance row, and local naming row.
\item $C$ records displayed $\Cont$ replay from mesh cells to tag reads, from tag reads and width rows to summands, and from summands to the finite sum row.
\item $P$ is the $\Pkg$ provenance over $\RiemannSumUp$, $\DyadicMeshUp$, $\PartitionUp$, $\DyadicStepFunctionUp$, $\DarbouxIntegralUp$, $\RiemannIntegralUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RiemannSumUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,T,F,W,S,H,C,P,N$. It has no coordinate for a host limit over refinements, a measure-theoretic integral, a quotient function space, a tag-choice principle, or a completed $\RealUp$ value.
\end{definition}
\leanchecked{BEDC.Derived.RiemannSumUp.RiemannSumTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Riemann sum NameCert obligations]
\label{thm:riemann-sum-namecert-obligations}
For every accepted $\RiemannSumUp$ carrier $Q=(M,T,F,W,S,H,C,P,N)$, the local $\NameCert_{\RiemannSumUp}$ surface consists of carrier admission, mesh-tag containment, finite integrand readback, cell-width exposure, finite sum exactness, componentwise transport, continuation replay, provenance, and ledger non-escape. These obligations are discharged entirely by the displayed route
$$
M\longmapsto T\longmapsto F,W\longmapsto S
$$
together with $H,C,P,N$.
\end{theorem}
\leanchecked{BEDC.Derived.RiemannSumUp.RiemannSumCarrier\_namecert\_obligations}
\begin{proof}
Carrier admission is the projection of the finite tuple in \autoref{def:riemann-sum-carrier}. The mesh row $M$ fixes the displayed cell list before any tag or value row is read, and the tag-containment row $T$ attaches one tag witness to each carried cell.

The value and width rows are then read componentwise. The window $F$ exposes only the tag values named by $T$, while $W$ exposes only the widths of the cells named by $M$. No external function equality or hidden refinement family is consulted.

Finite sum exactness is the arithmetic replay from these displayed rows to the row $S$. Since every summand is formed from one carried tag value and one carried width row, the sum is a finite $\BHist$ computation rather than a limiting assertion.

Transport and continuation use $H$ and $C$ to replay the same finite route after componentwise $\hsame$ movement. The provenance row $P$ and local name $N$ source the packet, and the absence of any host-limit, measure, quotient, or tag-choice coordinate gives ledger non-escape.
\end{proof}

\begin{theorem}[Riemann sum tagged-mesh exactness]
\label{thm:riemann-sum-tagged-mesh-exactness}
Every accepted $\RiemannSumUp$ read factors through the displayed mesh row, the tag-containment row, the integrand-read window, the cell-width ledger, and the finite sum row. If two accepted packets agree on those rows under componentwise $\hsame$ transport, then every downstream consumer reads the same finite tagged-sum boundary through $C,P,N$.
\end{theorem}
\begin{proof}
Project the accepted carrier through \autoref{def:riemann-sum-carrier}. The only route to a summand begins at a cell of $M$, passes through its tag witness in $T$, reads the corresponding value in $F$, and multiplies by the corresponding width in $W$.

Agreement under componentwise $\hsame$ transport identifies those rows cell by cell. Replaying $C$ therefore rebuilds the same summand ledger and the same finite row $S$ on both packets.

The downstream consumer has access only to $S$ together with the replay, provenance, and local name rows. Hence agreement on the displayed tagged-mesh surface determines the public finite-sum boundary, with no extra channel for hidden tags or host function equality.
\end{proof}

\begin{theorem}[Riemann sum Darboux handoff]
\label{thm:riemann-sum-darboux-handoff}
An accepted $\RiemannSumUp$ packet can be handed to a $\DarbouxIntegralUp$ or $\RiemannIntegralUp$ consumer only as the finite tagged-mesh row consisting of $M,T,F,W,S,H,C,P,N$. The handoff supplies the sum side of a Darboux or Riemann comparison, but it does not supply an integral value, a convergence claim over all refinements, a quotient of integrands, or a choice-selected tag family.
\end{theorem}
\begin{proof}
Start from the obligation surface of \autoref{thm:riemann-sum-namecert-obligations}. It exhausts the packet as mesh, tag-containment, value, width, finite-sum, transport, replay, provenance, and local-name data.

A $\DarbouxIntegralUp$ consumer may compare this finite row with upper and lower step-function rows, and a $\RiemannIntegralUp$ consumer may place it inside a larger tagged-mesh integration packet. In both cases the consumer receives the same finite sum row $S$ together with its visible construction route.

The excluded analytic claims are not carrier coordinates. The packet has no row for a completed integral, no family of all refinements, no host quotient of functions, and no choice-selected tag assignment outside $T$. Thus the handoff is exactly the finite tagged-sum side of the integration boundary.
\end{proof}
\leanchecked{BEDC.Derived.RiemannSumUp.RiemannSumCarrier\_darboux\_handoff}

\begin{theorem}[Riemann sum mesh-tag obligation]
\label{thm:riemann-sum-mesh-tag-obligation}
Every obligation-level $\RiemannSumUp$ consumer of an accepted carrier $Q=(M,T,F,W,S,H,C,P,N)$ must expose the mesh row $M$ and tag-containment row $T$ before reading an integrand value, a cell-width ledger, or the finite sum row. The mesh row supplies the displayed cells, and $T$ supplies exactly one carried tag-containment witness for each cell named by $M$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:riemann-sum-carrier}. Its only mesh coordinate is $M$, and its only tag-containment coordinate is $T$.

Apply the NameCert obligations of \autoref{thm:riemann-sum-namecert-obligations}. They place mesh-tag containment before finite integrand readback, width exposure, and sum exactness.

Use tagged-mesh exactness and the Darboux handoff, \autoref{thm:riemann-sum-tagged-mesh-exactness} and \autoref{thm:riemann-sum-darboux-handoff}. Both force downstream comparison reads through the same displayed $M,T$ boundary, so no consumer can substitute hidden cells or choice-selected tags.
\end{proof}
\leanchecked{BEDC.Derived.RiemannSumUp.RiemannSumCarrier\_mesh\_tag\_obligation}

\begin{theorem}[Riemann sum width and sum exactness obligation]
\label{thm:riemann-sum-width-exactness-obligation}
For every accepted $\RiemannSumUp$ carrier $Q=(M,T,F,W,S,H,C,P,N)$, the cell-width ledger $W$ and finite sum row $S$ must be replayed from the same mesh row $M$ and tag-containment row $T$. The obligation surface admits a summand only when its tag value is read through $F$ at a tag contained by $T$ and its width is read through $W$ at the corresponding cell of $M$.
\end{theorem}
\leanchecked{BEDC.Derived.RiemannSumUp.RiemannSumCarrier\_width\_sum\_exactness\_obligation}
\begin{proof}
Start with the carrier and NameCert obligation surface. The rows $M,T,F,W,S$ are the only finite arithmetic rows available to the packet, and \autoref{thm:riemann-sum-namecert-obligations} orders them as mesh and tag data before value, width, and sum data.

Apply tagged-mesh exactness. Theorem~\ref{thm:riemann-sum-tagged-mesh-exactness} says every summand route begins with a cell of $M$, passes through its tag witness in $T$, and then reads the matching value and width rows before rebuilding $S$.

The Darboux handoff preserves this same finite arithmetic boundary. A comparison consumer may inspect the sum side of the bracket, but it receives $S$ only as the replay of $M,T,F,W$, not as a host limiting object.
\end{proof}

\begin{theorem}[Riemann sum ledger non-escape obligation]
\label{thm:riemann-sum-ledger-nonescape-obligation}
An accepted $\RiemannSumUp$ packet has no obligation-level route to host limits, quotient function equality, measure integration, or choice-selected tag families. Every downstream read remains inside the displayed finite ledger $M,T,F,W,S,H,C,P,N$ before any Darboux or Riemann integral consumer adds its own rows.
\end{theorem}
\leanchecked{BEDC.Derived.RiemannSumUp.RiemannSumCarrier\_ledger\_nonescape\_obligation}
\begin{proof}
Project the carrier of \autoref{def:riemann-sum-carrier}. The packet contains mesh, tag, finite value, width, sum, transport, replay, provenance, and local naming rows, and no coordinate for host analytic structure.

Apply the NameCert obligations and tagged-mesh exactness. These theorems force all accepted reads through the finite row route $M,T,F,W,S,H,C,P,N$.

Finally use the Darboux handoff. Theorem~\ref{thm:riemann-sum-darboux-handoff} identifies the exported object as the finite tagged-sum side of a comparison, so host limits, quotient function equality, measure integration, and external tag choice remain outside the ledger.
\end{proof}

\begin{theorem}[Riemann sum obligation-closure package]
\label{thm:riemann-sum-obligation-closure-package}
The $\RiemannSumUp$ packet supports obligationClosure exactly by carrier admission, mesh-tag containment, finite integrand readback, cell-width exposure, finite-sum exactness, tagged-mesh exactness, Darboux/Riemann handoff, componentwise transport, $\Cont$ replay, provenance, and ledger non-escape. For an accepted carrier $Q=(M,T,F,W,S,H,C,P,N)$, every obligation-level consumer reads only the finite route through those nine rows and imports no integrability theorem, limiting process, quotient function equality, or tag-choice principle.
\end{theorem}
\begin{proof}
Begin with the carrier of \autoref{def:riemann-sum-carrier}. It displays exactly $M,T,F,W,S,H,C,P,N$, so carrier admission and ledger non-escape are finite row projections rather than analytic closure assumptions.

Apply \autoref{thm:riemann-sum-namecert-obligations}. The mesh row, tag-containment row, integrand-read window, cell-width ledger, finite sum row, transport, replay, provenance, and local naming rows give the obligation surface named in the statement.

Next use \autoref{thm:riemann-sum-tagged-mesh-exactness}. It identifies downstream reads when the mesh, tags, finite values, widths, and sum row agree under componentwise $\hsame$, giving the exact tagged-mesh boundary required by obligationClosure.

Finally apply \autoref{thm:riemann-sum-darboux-handoff}. DarbouxIntegral and RiemannIntegral consumers receive only the finite tagged-sum side of the comparison. Since the packet has no row for integrability, all refinements, quotient function equality, or a choice-selected tag family, the obligation-closure package is exactly the listed finite surface.
\end{proof}

\begin{theorem}[Riemann sum tagless refinement ledger]
\label{thm:riemann-sum-tagless-refinement-ledger}
Let $Q=(M,T,F,W,S,H,C,P,N)$ be an accepted $\RiemannSumUp$ carrier. Any refinement-facing $\DarbouxIntegralUp$ or $\RiemannIntegralUp$ consumer of $Q$ must replay the same nine rows $M,T,F,W,S,H,C,P,N$. It may compare refinements only through displayed mesh-cell subdivision and transported tag-containment rows. The packet exports no external tag selector, quotient integrand equality, host limiting process, or measure integral.
\end{theorem}
\leanchecked{BEDC.Derived.RiemannSumUp.RiemannSumCarrier\_tagless\_refinement\_ledger}
\begin{proof}
Apply the mesh-tag obligation of \autoref{thm:riemann-sum-mesh-tag-obligation}. A refinement-facing consumer must first expose the displayed mesh row $M$ and the tag-containment row $T$, so every subdivision comparison is anchored in the carried cells and carried tag witnesses.

Apply the width and sum exactness obligation of \autoref{thm:riemann-sum-width-exactness-obligation}. The rows $F,W,S$ are replayed from the same displayed mesh and tag route, hence a refined comparison may inspect only the subdivided cell-width ledger and the finite summand ledger produced from it.

Transport is componentwise through $H$ and continuation replay is displayed through $C$. Thus transported tag-containment rows remain the only tag data available after refinement; no external selector can be projected from the packet.

Finally use ledger non-escape and the Darboux handoff, \autoref{thm:riemann-sum-ledger-nonescape-obligation} and \autoref{thm:riemann-sum-darboux-handoff}. They identify the exported object as the finite tagged-sum side of a Darboux or Riemann comparison, excluding quotient integrand equality, host limiting processes, and measure integrals.
\end{proof}

\closureat{RiemannSumUp}{seedStr}
\closureat{RiemannSumUp}{obligationStr}

\begin{closurestatus}{\RiemannSumUp}
  \constructivestory{A $\RiemannSumUp$ packet is generated from a finite mesh row, tag-containment row, integrand-read window, cell-width ledger, finite sum row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{The finite scope is closed for $\BHist$ mesh rows, $\PartitionUp$, $\DyadicMeshUp$, $\DyadicStepFunctionUp$, $\DarbouxIntegralUp$, $\RiemannIntegralUp$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\RiemannSumUp}$ row.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host integrability, convergence over all refinements, measure-theoretic integration, quotient function equality, choice-selected tags, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, mesh-tag containment, finite readback, width exposure, finite sum exactness, tagged-mesh exactness, Darboux handoff, transport, replay, provenance, and ledger non-escape for BEDC.Derived.RiemannSumUp.}
\end{closurestatus}
