\chapter{A Concrete Naming Certificate for $\RHCriticalStripWindowCertificateUp$}
\label{ch:concrete-instances-rh-critical-strip-window-certificate-namecert}
\origin{ai}

The $\RHCriticalStripWindowCertificateUp$ packet realizes a finite critical-strip window route from \autoref{ch:visions-rh-three-boundary-separation} as NameCert data. It sits beside the three-boundary separation of \autoref{ch:concrete-instances-rh-boundary-kind-separation-namecert}, the zeta-zero witness surface of \autoref{ch:concrete-instances-zeta-zeros-namecert}, the complex carrier of \autoref{ch:concrete-instances-complex-namecert}, the real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the finite prime-window gluing route of \autoref{ch:concrete-instances-finite-prime-window-gluing-namecert}. The packet exposes a finite critical-strip window and certified-zero row while refusing to turn that window into an exact zero decider or a global RH rigidity proof.

\begin{definition}[RH critical-strip window certificate carrier]
\label{def:rh-critical-strip-window-certificate-carrier}
An $\RHCriticalStripWindowCertificateUp$ carrier is a finite $\BHist$ packet
$$
W=(B,M,Z,E,R,G,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the finite critical-strip window row, storing the inspected complex histories and their critical-strip bounds.
\item $M$ is the mirror fixed-surface row, recording the functional-equation mirror surface available on the same finite window.
\item $Z$ is the certified-zero exposure row, naming supplied zeta-zero packets inside the displayed window.
\item $E$ is the exact-decider refusal row, recording that the carrier does not provide $\prod_s\mathsf{Decidable}(\mathsf{ZetaZero}(s))$.
\item $R$ is the rigidity refusal row, recording that the carrier does not provide the global RH route from every critical-strip zero to an $\mathsf{OnCritLine}$ witness.
\item $G$ is the finite prime-window gluing row available only as a dependency of the displayed certified-zero exposure, not as a substitute for $B,M,Z,E,R$.
\item $H$ records componentwise $\hsame$ transport for the strip window, mirror row, certified-zero row, refusal rows, gluing row, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from an RH-facing finite-window request through $B,M,Z,E,R,G,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RHCriticalStripWindowCertificateUp$, $\RHBoundaryKindSeparationUp$, $\ZetaZerosUp$, $\ComplexUp$, $\RealUp$, $\FinitePrimeWindowGluingUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RHCriticalStripWindowCertificateUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,M,Z,E,R,G,H,C,P,N$. It has no coordinate for an exact zero decider, an RH proof, a zero enumeration theorem, a quotient zero set, a hidden critical-line classifier, or global rigidity outside the displayed finite window.
\end{definition}

\begin{theorem}[RH critical-strip window certificate NameCert obligations]
\label{thm:rh-critical-strip-window-certificate-namecert-obligations}
For every accepted $\RHCriticalStripWindowCertificateUp$ carrier
$$
W=(B,M,Z,E,R,G,H,C,P,N),
$$
the local $\NameCert_{\RHCriticalStripWindowCertificateUp}$ surface consists exactly of carrier admission for the finite critical-strip window, mirror fixed-surface row, certified-zero exposure row, exact-decider refusal row, rigidity refusal row, finite prime-window gluing row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:rh-critical-strip-window-certificate-carrier}. The rows $B,M,Z,E,R,G$ are displayed before transport, replay, provenance, and local naming are read.

The certified-zero row $Z$ is window-local: it exposes supplied zero packets only inside $B$. The mirror row $M$ records the fixed-surface geometry available on that same finite surface.

The refusal rows $E$ and $R$ are part of the carrier inventory. Since no other row has the type of a total zero decider or global RH rigidity route, transport through $H$ and replay through $C$ cannot manufacture either object.
\end{proof}

\begin{theorem}[RH critical-strip window certificate boundary separation]
\label{thm:rh-critical-strip-window-certificate-boundary-separation}
An accepted $\RHCriticalStripWindowCertificateUp$ packet separates finite critical-strip inspection from exact zero decision and global RH rigidity. A consumer may read certified zero exposure inside $B$, but it may not infer a total zero decider or a proof that every critical-strip zero lies on the mirror fixed surface.
\end{theorem}
\begin{proof}
Use the NameCert obligations of \autoref{thm:rh-critical-strip-window-certificate-namecert-obligations}. They keep the finite strip window, mirror row, certified-zero exposure, exact-decider refusal, and rigidity refusal as separate rows.

The exact-decider refusal row $E$ blocks a read of $\prod_s\mathsf{Decidable}(\mathsf{ZetaZero}(s))$. The rigidity refusal row $R$ blocks a read of the global route from every certified critical-strip zero to an $\mathsf{OnCritLine}$ witness.

The finite prime-window gluing row $G$ is only a sibling dependency for the displayed window. It does not change the carrier inventory, so the accepted packet remains a finite certificate rather than an exact decider or global RH proof.
\end{proof}

\falsifiablePrediction{Any accepted $\RHCriticalStripWindowCertificateUp$ consumer that reads an exact zeta-zero decider or a global RH rigidity proof from the finite window rows $B,M,Z,G,H,C,P,N$ without displaying the refusal rows $E,R$ refutes the carrier surface.}

\independenceWitness{rh-boundary-kind-separation, zeta-zeros, complex, finite-prime-window-gluing, rh-free-zero-initiality: no listed sibling is bijective to $\RHCriticalStripWindowCertificateUp$, because this packet binds a finite critical-strip window, mirror fixed-surface row, certified-zero exposure, exact-decider refusal, and rigidity refusal in one local NameCert surface.}

\closureat{RHCriticalStripWindowCertificateUp}{seedStr}

\begin{closurestatus}{\RHCriticalStripWindowCertificateUp}
  \constructivestory{An $\RHCriticalStripWindowCertificateUp$ packet is generated from a finite critical-strip window row, mirror fixed-surface row, certified-zero exposure row, exact-decider refusal row, rigidity refusal row, finite prime-window gluing row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:rh-critical-strip-window-certificate-carrier}, \autoref{thm:rh-critical-strip-window-certificate-namecert-obligations}, and \autoref{thm:rh-critical-strip-window-certificate-boundary-separation} seed the finite RH critical-strip window certificate while separating mirror geometry from exact zero decision and global RH rigidity.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not prove RH, refute RH, decide all zeta zeros, enumerate all zeros, select a quotient zero set, supply a hidden critical-line classifier, close Lean verification, or provide bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, window exactness, mirror fixed-surface exposure, certified-zero routing, exact-decider refusal, rigidity refusal, gluing handoff, transport, replay, provenance, and local naming for BEDC.Derived.RHCriticalStripWindowCertificateUp.}
\end{closurestatus}
