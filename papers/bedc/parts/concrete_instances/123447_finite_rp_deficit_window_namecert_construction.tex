\chapter{A Concrete Naming Certificate for $\FiniteRpDeficitWindowUp$}
\label{ch:concrete-instances-finite-rp-deficit-window-namecert}
\origin{ai}

The $\FiniteRpDeficitWindowUp$ packet realizes the minimal RP deficit search boundary of \autoref{ch:visions-innovation-input-minimal-rp-deficit-search-boundary} as finite Q3 source, neutral-window, link-deficit, least-eigenvalue, obstruction, transport, replay, provenance, and NameCert rows. It consumes the finite-window RP carrier of \autoref{ch:concrete-instances-finite-window-rp-boundary-namecert}, the pentary rigidity surface of \autoref{ch:concrete-instances-pentary-hermitian-rigidity-namecert}, and the golden-palindrome boundary of \autoref{ch:concrete-instances-golden-palindrome-boundary-namecert}. The packet names a finite search object for negative RP evidence; it does not assert complete reflection positivity, a limiting Hilbert space, a global phase classifier, or a selected optimum.

\begin{definition}[Finite RP deficit window carrier]
\label{def:finite-rp-deficit-window-carrier}
A $\FiniteRpDeficitWindowUp$ carrier is a finite $\BHist$ packet
$$
D=(Q,W,L,E,O,T,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the exact Q3 source row, recording the finite phase word, alphabet size, and equality $\sum_a v_a=\sqrt N$ used by the search.
\item $W$ is the neutral finite-window row, naming the anchored future block set, exponent bound, enumeration, and reflected matrix support.
\item $L$ is the link-deficit row, displaying the checked deficits $\Delta_d(v)$ for the finite link range.
\item $E$ is the least-eigenvalue row for the neutral RP matrix attached to $W$.
\item $O$ is the obstruction row: it records either a negative link deficit, a negative neutral-window least eigenvalue, or the visible absence of those obstructions in the displayed window.
\item $T$ is the transport row from $\FiniteWindowRpBoundaryUp$, keeping the finite-window RP boundary and charged/neutral split visible.
\item $R$ is the replay row for pentary and golden boundary consumers, allowing $\PentaryHermitianRigidityUp$ and $\GoldenPalindromeBoundaryUp$ to read the same finite deficit window without replacing it by their own carriers.
\item $H$ records componentwise $\hsame$ transport for the Q3 source, neutral window, link deficits, least-eigenvalue row, obstruction row, boundary transport, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a minimal RP deficit request through $Q,W,L,E,O,T,R,H$.
\item $P$ is the $\Pkg$ provenance over $\FiniteRpDeficitWindowUp$, $\FiniteWindowRpBoundaryUp$, $\PentaryHermitianRigidityUp$, $\GoldenPalindromeBoundaryUp$, finite $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteRpDeficitWindowUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $Q,W,L,E,O,T,R,H,C,P,N$. It has no coordinate for after-the-fact projection to Q3, infinite-window RP positivity, a selected optimizer, a limiting Hilbert space, host spectral semantics, or bridge checking.
\end{definition}

\begin{theorem}[Finite RP deficit window NameCert obligations]
\label{thm:finite-rp-deficit-window-namecert-obligations}
For every accepted $\FiniteRpDeficitWindowUp$ carrier
$$
D=(Q,W,L,E,O,T,R,H,C,P,N),
$$
the local $\NameCert_{\FiniteRpDeficitWindowUp}$ surface consists exactly of exact Q3 source admission through $Q$, neutral finite-window exposure through $W$, link-deficit exposure through $L$, least-eigenvalue exposure through $E$, obstruction classification through $O$, finite-window RP boundary transport through $T$, pentary and golden replay through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-rp-deficit-window-carrier}. The finite search data are exactly the Q3 source row $Q$, neutral-window row $W$, link-deficit row $L$, least-eigenvalue row $E$, and obstruction row $O$.

The finite-window boundary transport $T$ keeps the charged/neutral split visible before any deficit is exported. Thus a negative link deficit or negative neutral least eigenvalue is recorded as a finite obstruction row, not as a detached numerical annotation.

The replay row $R$ allows the pentary rigidity and golden-palindrome siblings to consume the same deficit window. Because $R$ reads $Q,W,L,E,O,T$ rather than rebuilding them, the sibling consumers cannot replace the finite search object by a separate carrier.

Finally $H,C,P,N$ preserve the same rows under transport, replay, provenance, and local naming. Since no carrier coordinate supplies Q3 projection after optimization, infinite-window positivity, selected optimality, limiting Hilbert space, host spectral semantics, or bridge checking, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Finite RP deficit obstruction transport]
\label{thm:finite-rp-deficit-window-obstruction-transport}
Let $D=(Q,W,L,E,O,T,R,H,C,P,N)$ be an accepted $\FiniteRpDeficitWindowUp$ carrier. Any public read that exports a negative link deficit or a negative neutral-window least eigenvalue must preserve the exact Q3 source $Q$, the neutral window $W$, the corresponding deficit row $L$ or $E$, the obstruction row $O$, and the finite-window boundary transport $T$. If any of those rows is absent, the packet exports no RP obstruction to $\PentaryHermitianRigidityUp$ or $\GoldenPalindromeBoundaryUp$ consumers.
\end{theorem}
\begin{proof}
Start from the obstruction row $O$. By carrier definition, $O$ is downstream of the exact Q3 source, the neutral finite window, and either the displayed link-deficit row or the displayed least-eigenvalue row.

Transport through $H$ and replay through $C$ preserve only componentwise displayed rows. A consumer may therefore move the obstruction along $\hsame$ comparisons only while retaining $Q,W,L,E,O$ and the finite-window boundary transport $T$.

Now read the sibling replay row $R$. It hands the finite deficit window to $\PentaryHermitianRigidityUp$ and $\GoldenPalindromeBoundaryUp$ consumers only after those rows remain visible. If the source, window, deficit, obstruction, or boundary-transport row is dropped, $R$ has no displayed deficit to replay.

Thus the obstruction cannot escape as a bare numerical claim or as a hidden RP failure. It remains a finite NameCert row tied to the exact Q3 source and neutral window.
\end{proof}

\falsifiablePrediction{An accepted $\FiniteRpDeficitWindowUp$ consumer that exports an RP obstruction while omitting the exact Q3 source row, neutral-window row, link-deficit or least-eigenvalue row, obstruction row, or finite-window-rp-boundary transport refutes the packet.}

\independenceWitness{finite-window-rp-boundary, pentary-hermitian-rigidity, golden-palindrome-boundary, BHist, NameCert: the finite-window-rp-boundary supplies charged/neutral boundary transport, pentary-hermitian-rigidity supplies a pentary obstruction consumer, golden-palindrome-boundary supplies an anchored palindrome consumer, and the BHist/NameCert siblings supply finite history and naming discipline. None is bijective with $\FiniteRpDeficitWindowUp$ because this packet jointly carries exact Q3 source, neutral window, link deficits, least eigenvalues, obstruction, transport, replay, provenance, and local naming rows.}

\closureat{FiniteRpDeficitWindowUp}{seedStr}

\begin{closurestatus}{\FiniteRpDeficitWindowUp}
  \constructivestory{A $\FiniteRpDeficitWindowUp$ packet is generated from an exact Q3 source row, neutral finite-window row, link-deficit rows, least-eigenvalue rows, obstruction row, finite-window RP boundary transport, pentary and golden replay, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-rp-deficit-window-carrier}, \autoref{thm:finite-rp-deficit-window-namecert-obligations}, and \autoref{thm:finite-rp-deficit-window-obstruction-transport} seed the finite minimal RP deficit search packet over Q3 source, neutral windows, link deficits, least eigenvalues, obstruction rows, and sibling replay.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, complete reflection positivity, infinite-window RP positivity, limiting Hilbert-space construction, selected optimality, host spectral semantics, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, exact Q3 source retention, neutral-window exposure, link-deficit exactness, least-eigenvalue exposure, obstruction transport, finite-window boundary replay, pentary and golden sibling replay, provenance, and local naming for BEDC.Derived.FiniteRpDeficitWindowUp.}
\end{closurestatus}
