\chapter{A Concrete Naming Certificate for $\SequentialCoreflectionUp$}
\label{ch:concrete-instances-sequential-coreflection-namecert}
\origin{human}

Following Franklin and Kelley on sequential spaces and coreflective topology, the $\SequentialCoreflectionUp$ packet records the sequentially generated readback of a displayed space as finite BEDC NameCert data. It sits between the first-countable handoff of \autoref{ch:concrete-instances-first-countable-sequential-closure-namecert}, the sequential closure route of \autoref{ch:concrete-instances-sequentialclosure-namecert}, the cofinite-tail filter of \autoref{ch:concrete-instances-frechetfilter-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational sequence route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}. The certificate exposes the countable-name tests through which later closure and Real-facing consumers may inspect sequential reachability, without importing a host topological coreflection or a completed quotient space.

\begin{definition}[Sequential coreflection carrier]
\label{def:sequential-coreflection-carrier}
A $\SequentialCoreflectionUp$ carrier is a finite $\BHist$ packet
$$
S=(X,Q,L,F,W,T,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the source space row whose original open or closure observations are read only through displayed BEDC packet coordinates.
\item $Q$ is the sequential-coreflection classifier row, recording that a point-name is tested by finite sequence windows and their eventual tail behaviour rather than by arbitrary host neighbourhoods.
\item $L$ is the $\SequentialClosureUp$ row naming the sequence-closure test consumed by the carrier.
\item $F$ is the $\FrechetFilterUp$ row recording cofinite-tail admission for the inspected sequence windows.
\item $W$ is the $\StreamNameUp$ window row displaying the finite prefixes and tail requests used by the test.
\item $T$ is the first-countable handoff row, read through $\FirstCountableSequentialClosureUp$ when a countable neighbourhood base is displayed.
\item $R$ is the $\RegSeqRatUp$ handoff row used when the inspected sequence data is passed to rational or metric readbacks.
\item $E$ is the $\RealUp$ seal row reached only after $W,F,L,T,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the source row, classifier row, closure row, tail filter, stream windows, first-countable handoff, rational handoff, real seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay along the route
$$
X,Q,L,F,W,T,R,E \longmapsto H,C,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\SequentialCoreflectionUp$, $\FirstCountableSequentialClosureUp$, $\SequentialClosureUp$, $\FrechetFilterUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SequentialCoreflectionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a host topological coreflection, quotient point equality, selected subsequences, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Sequential coreflection NameCert obligations]
\label{thm:sequential-coreflection-namecert-obligations}
For every accepted $\SequentialCoreflectionUp$ carrier
$$
S=(X,Q,L,F,W,T,R,E,H,C,P,N),
$$
the local $\NameCert_{\SequentialCoreflectionUp}$ surface consists exactly of carrier admission, source-space exposure through $X$, sequential-coreflection classifier exposure through $Q$, sequential-closure exposure through $L$, cofinite-tail exposure through $F$, finite stream-window exposure through $W$, first-countable handoff exposure through $T$, regular rational handoff through $R$, real-name sealing through $E$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:sequential-coreflection-carrier}. Its mathematical rows are exactly the source row $X$, classifier row $Q$, sequential-closure row $L$, Frechet-tail row $F$, stream-window row $W$, first-countable handoff row $T$, regular rational handoff $R$, and Real seal $E$.

The sequential classifier is therefore read through displayed countable-name tests. Row $W$ exposes only finite stream windows, row $F$ supplies only cofinite-tail discipline, and row $L$ records the sequence-closure test visible to the packet.

The first-countable and Real-facing routes remain downstream. Row $T$ is consumed only when a countable base handoff has already been displayed, while $R$ and $E$ pass inspected sequence data to regular rational and Real consumers without selecting a subsequence or identifying quotient points.

Finally attach $H,C,P,N$. These structural rows transport, replay, source, and name the same finite dependency route, so the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Sequential coreflection first-countable handoff]
\label{thm:sequential-coreflection-first-countable-handoff}
Every accepted $\FirstCountableSequentialClosureUp$ read consumed by a $\SequentialCoreflectionUp$ packet factors through the displayed route
$$
\StreamNameUp,\ \FrechetFilterUp,\ \SequentialClosureUp,\ \RegSeqRatUp,\ \RealUp
$$
as exposed by $W,F,L,R,E$ and transported by $H,C,P,N$. The handoff lets a first-countable consumer replay the same finite sequence windows as a sequential-coreflection read, but it exports no host topology coreflection, quotient point equality, selected subsequence, countable-choice schedule, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the first-countable row $T$ in \autoref{def:sequential-coreflection-carrier}. A consumer may use it only after the source row $X$ and sequential classifier row $Q$ have exposed the sequence test being replayed.

The countable-name data is then read through $W,F,L$. Finite prefixes and tail requests come from the $\StreamNameUp$ window row $W$, eventual-tail discipline comes from $\FrechetFilterUp$ row $F$, and the closure test itself is the displayed $\SequentialClosureUp$ row $L$.

The Real-facing handoff is downstream from the same windows. Row $R$ repacks the inspected sequence observations as regular rational data when such a readback is requested, and row $E$ seals the resulting Real-facing boundary.

Thus the first-countable handoff is a replay of the displayed sequence route, not a separate topology theorem. The structural rows $H,C,P,N$ preserve that route and add no selected subsequence, quotient point equality, countable-choice schedule, or ambient completeness coordinate.
\end{proof}

\falsifiablePrediction{Every accepted $\SequentialCoreflectionUp$ read factors through source-space, sequential-classifier, SequentialClosure, FrechetFilter, StreamName, FirstCountableSequentialClosure, RegSeqRat, Real, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows before exporting a sequentially generated closure read.}

\independenceWitness{first-countable-sequential-closure, sequentialclosure, frechetfilter, streamname, regseqrat, real: $\SequentialCoreflectionUp$ is not bijective to any listed sibling because this packet alone binds the source row, sequential-coreflection classifier, sequence-closure route, cofinite-tail filter, finite stream windows, first-countable handoff, regular rational readback, Real seal, transport, replay, provenance, and local naming into one finite certificate.}

\closureat{SequentialCoreflectionUp}{seedStr}

\begin{closurestatus}{\SequentialCoreflectionUp}
  \constructivestory{A $\SequentialCoreflectionUp$ packet is generated from a source-space row, a sequential-coreflection classifier row, SequentialClosure rows, FrechetFilter tail rows, StreamName windows, a FirstCountableSequentialClosure handoff, RegSeqRat readback rows, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:sequential-coreflection-carrier}, \autoref{thm:sequential-coreflection-namecert-obligations}, and \autoref{thm:sequential-coreflection-first-countable-handoff} seed the finite sequential-coreflection certificate over countable-name sequence tests, closure rows, tail filters, first-countable handoff rows, rational readback, Real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host topology coreflection, quotient point equality, selected subsequences, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, sequential-classifier exposure, SequentialClosure exactness, FrechetFilter tail discipline, StreamName window replay, first-countable handoff, RegSeqRat and Real non-escape, transport, provenance, and local naming for BEDC.Derived.SequentialCoreflectionUp.}
\end{closurestatus}
