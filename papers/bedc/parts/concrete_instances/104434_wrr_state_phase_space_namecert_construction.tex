\chapter{A Concrete Naming Certificate for $\WrrStatePhaseSpaceUp$}
\label{ch:concrete-instances-wrr-state-phase-space-namecert}
\origin{ai}

The $\WrrStatePhaseSpaceUp$ packet realizes the WRR hotspot state-vector branch of \autoref{ch:visions-philosophy-auditable-generativity} and the source analysis in \autoref{subsec:philosophy-dna-window-six-boundary-tiles} as finite NameCert data. It sits beside the codon-window carrier of \autoref{ch:concrete-instances-codon-six-bit-window-namecert}, the transition matrix of \autoref{ch:concrete-instances-codon-transition-matrix-namecert}, the core state graph of \autoref{ch:concrete-instances-core-state-graph-namecert}, and the finite observation fold of \autoref{ch:concrete-instances-finite-observation-fold-namecert}. Those siblings expose codon windows, transition rows, graph compression, and finite observation folding; this packet binds the WRR hotspot state vector, invariant anchors, active support, reassignment preorder, table provenance, transport, replay, and local naming as one concrete finite phase-space object.

\begin{definition}[WRR state phase-space carrier]
\label{def:wrr-state-phase-space-carrier}
A $\WrrStatePhaseSpaceUp$ carrier is a finite $\BHist$ packet
$$
W=(S,A,V,R,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the state-vector ledger recording the eight-coordinate $C_{\mathrm{WRR}}$ assignment vector of \autoref{def:philosophy-wrr-hotspot-state-vector}.
\item $A$ is the invariant-anchor ledger for the $\mathrm{UGG}$ and $\mathrm{AAG}$ coordinates of \autoref{thm:philosophy-wrr-invariant-anchors}.
\item $V$ is the active-support row naming the six moving WRR coordinates of \autoref{cor:philosophy-wrr-active-hotspot-support}.
\item $R$ is the reassignment-preorder row recording the finite inclusions of \autoref{cor:philosophy-core-reassignment-preorder}.
\item $T$ is the table-provenance row linking each state vector to its curated genetic-code table identifier and comparison-family ledger.
\item $H$ records componentwise $\hsame$ transport for the state vector, anchors, active support, preorder, table provenance, replay, package, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a hotspot-state consumer through $S,A,V,R,T,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\WrrStatePhaseSpaceUp$, $\CodonSixBitWindowUp$, $\CodonTransitionMatrixUp$, $\CoreStateGraphUp$, $\FiniteObservationFoldUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\WrrStatePhaseSpaceUp}$ row.
\end{enumerate}
The carrier predicate asserts exactly that these rows form one finite WRR hotspot state phase space. It does not assert a biochemical time parameter, an evolutionary tree, a complete biological state space, or a dynamics law outside the displayed comparison-family ledger.
\end{definition}

\begin{theorem}[WRR state phase-space NameCert obligations]
\label{thm:wrr-state-phase-space-namecert-obligations}
For an accepted $\WrrStatePhaseSpaceUp$ carrier $W=(S,A,V,R,T,H,C,P,N)$, the local $\NameCert_{\WrrStatePhaseSpaceUp}$ surface consists exactly of the following obligations:
\begin{enumerate}
\item carrier admission for the state-vector ledger, invariant-anchor ledger, active-support row, reassignment-preorder row, table-provenance row, componentwise transport, replay, package provenance, and local naming row;
\item state-vector exactness, requiring every hotspot-state read to use the coordinate order fixed by $S$ and the WRR cube source rather than an unnamed codon projection;
\item anchor and support stability, requiring $A$ and $V$ to keep invariant anchors separate from active reassignment coordinates under $H$ and $C$;
\item preorder provenance, requiring each inclusion in $R$ to be sourced by finite table identifiers in $T$ and replayed only through the same comparison-family ledger;
\item ledger non-escape, requiring $P$ and $N$ to export no temporal dynamics, no causal reassignment rule, no evolutionary tree, no unobserved-state impossibility claim, and no context-free translation model.
\end{enumerate}
Lean follow-up must provide $\mathsf{Nontrivial}\ \WrrStatePhaseSpaceUp$ and $\mathsf{FieldFaithful}\ \WrrStatePhaseSpaceUp$ instances in $\mathsf{BEDC.Derived.WrrStatePhaseSpaceUp.TasteGate}$ for this displayed finite carrier.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:wrr-state-phase-space-carrier}. The tuple displays exactly the state-vector, anchor, support, preorder, table-provenance, transport, replay, provenance, and naming rows.

State-vector exactness reads the WRR coordinate order from the source definition and stores it in $S$. The carrier has no second codon projection row, so a hotspot-state consumer cannot silently change the coordinate order.

Anchor and support stability use the split between $A$ and $V$. The invariant anchors remain named separately from the active support, while $H$ and $C$ transport and replay the same split rather than merging it into a single untyped state list.

Preorder provenance is the role of $R$ and $T$. Each displayed inclusion is tied to finite table identifiers before it is packaged, so the preorder is an observed comparison-family row and not a temporal or causal law.

The remaining claim is ledger non-escape. Since temporal dynamics, causal reassignment mechanisms, evolutionary-tree structure, exhaustive biological state space, and ribosome-context models are not carrier coordinates, no NameCert consumer can read them from $W$.
\end{proof}

\falsifiablePrediction{Every accepted $\WrrStatePhaseSpaceUp$ consumer reads a WRR state-vector ledger, invariant-anchor ledger, active-support row, reassignment-preorder row, table-provenance row, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local naming before citing hotspot phase-space compression.}

\independenceWitness{codon-six-bit-window, codon-transition-matrix, core-state-graph, finite-observation-fold; $\WrrStatePhaseSpaceUp$ is not bijective to any listed sibling because it jointly carries state vectors, invariant anchors, active support, reassignment preorder, table provenance, transport, replay, package provenance, and local NameCert rows as one hotspot phase-space packet.}

\closureat{WrrStatePhaseSpaceUp}{seedStr}
\begin{closurestatus}{\WrrStatePhaseSpaceUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:wrr-state-phase-space-carrier} and \autoref{thm:wrr-state-phase-space-namecert-obligations} seed the finite WRR hotspot phase-space surface over state-vector, invariant-anchor, active-support, reassignment-preorder, table-provenance, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, temporal biological dynamics, causal reassignment mechanisms, evolutionary-tree reconstruction, exhaustive biological state-space closure, or context-free translation dynamics.}
  \upgradepath{Obligation closure requires checked carrier admission, state-vector exactness, anchor and support stability, preorder provenance, ledger non-escape, and TasteGate nontriviality plus field faithfulness for BEDC.Derived.WrrStatePhaseSpaceUp.}
  \constructivestory{A $\WrrStatePhaseSpaceUp$ packet is generated from WRR state-vector, invariant-anchor, active-support, reassignment-preorder, table-provenance, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
\end{closurestatus}
