\chapter{A Concrete Naming Certificate for $\WeakOmniscienceBoundaryUp$}
\label{ch:concrete-instances-weak-omniscience-boundary-namecert}
\origin{human}

Following Bishop, Bridges, and Troelstra--van Dalen on constructive analysis and weak omniscience principles, the $\WeakOmniscienceBoundaryUp$ packet names the finite boundary where L10 real-completion reads may cite displayed evidence without becoming LPO, LLPO, or choice principles. It sits over the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the Kleene-tree obstruction of \autoref{ch:concrete-instances-kleene-tree-namecert}, the Specker sequence surface of \autoref{ch:concrete-instances-speckersequence-namecert}, and the finite-prefix choice-sequence row of \autoref{ch:concrete-instances-choice-sequence-namecert}. The packet refuses decision extraction from real-completion reads while preserving finite witness routes through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

\begin{definition}[Weak omniscience boundary carrier]
\label{def:weak-omniscience-boundary-carrier}
A $\WeakOmniscienceBoundaryUp$ carrier is a finite $\BHist$ packet
$$
W=(R,E,K,S,Q,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RegSeqRatUp$ readback row naming the finite regular-rational windows available to an L10 Real or Completion consumer.
\item $E$ is the terminal $\RealUp$ seal row reached only after the displayed regular-rational windows have been read.
\item $K$ is the $\KleeneTreeUp$ obstruction row, carrying finite prefix-tree evidence without selecting an infinite branch.
\item $S$ is the $\SpeckerSequenceUp$ row, carrying monotone bounded finite-window evidence without exposing a located supremum.
\item $Q$ is the $\ChoiceSequenceUp$ prefix row, carrying displayed choice-sequence answers only on the inspected finite window.
\item $F$ is the refusal row recording that LPO tests, LLPO splits, selected limits, quotient stream equality, trichotomy, countable choice, open-cover compactness, and ambient $\RealUp$ completeness are not coordinates of the packet.
\item $H$ records componentwise $\hsame$ transport for $R,E,K,S,Q,F$.
\item $C$ records displayed $\Cont$ replay from a Real or Completion request through the regular-rational readback, terminal seal, obstruction rows, prefix rows, and refusal row.
\item $P$ is the $\Pkg$ provenance over $\WeakOmniscienceBoundaryUp$, $\RegSeqRatUp$, $\RealUp$, $\KleeneTreeUp$, $\SpeckerSequenceUp$, $\ChoiceSequenceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\WeakOmniscienceBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $R,E,K,S,Q,F,H,C,P,N$. It has no hidden coordinate from which a real-completion read can decide LPO, split an LLPO instance, choose a limit, identify quotient streams, invoke countable choice, extract open-cover compactness, or complete ambient $\RealUp$.
\end{definition}

\begin{theorem}[Weak omniscience boundary NameCert obligations]
\label{thm:weak-omniscience-boundary-namecert-obligations}
For every accepted $\WeakOmniscienceBoundaryUp$ carrier
$$
W=(R,E,K,S,Q,F,H,C,P,N),
$$
the local $\NameCert_{\WeakOmniscienceBoundaryUp}$ surface consists exactly of regular-rational readback admission through $R$, terminal real-seal handoff through $E$, finite tree-obstruction readback through $K$, Specker finite-window readback through $S$, choice-sequence prefix readback through $Q$, refusal nonescape through $F$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:weak-omniscience-boundary-carrier}. The Real and Completion route first exposes the regular-rational row $R$ and then the terminal seal row $E$.

The obstruction rows are finite. The Kleene row $K$ supplies only displayed prefix-tree data, the Specker row $S$ supplies only displayed monotone bounded windows, and the choice-sequence row $Q$ supplies only displayed finite-prefix answers.

The refusal row $F$ binds the missing principles to absent coordinates rather than to extra assumptions. Transport by $H$, replay by $C$, provenance by $P$, and local naming by $N$ preserve the same displayed rows and add no LPO test, LLPO split, selected limit, quotient stream equality, trichotomy branch, countable-choice selector, open-cover compactness principle, or ambient completeness field.
\end{proof}

\begin{theorem}[Weak omniscience boundary real-completion nonescape]
\label{thm:weak-omniscience-boundary-real-completion-nonescape}
Every L10 Real or Completion consumer of an accepted $\WeakOmniscienceBoundaryUp$ packet factors through the finite route
$$
\RegSeqRatUp\longmapsto \RealUp\longmapsto
\KleeneTreeUp,\quad \SpeckerSequenceUp,\quad \ChoiceSequenceUp
$$
together with the refusal row and the structural rows $H,C,P,N$. The route may preserve finite witness evidence from the sibling packets, but it cannot extract LPO, LLPO, selected limits, quotient stream equality, trichotomy, countable choice, open-cover compactness, or ambient $\RealUp$ completeness from a real-completion read.
\end{theorem}
\begin{proof}
Apply \autoref{thm:weak-omniscience-boundary-namecert-obligations}. It fixes the consumer route to the displayed regular-rational row, terminal real seal, three finite sibling rows, refusal row, and structural rows.

The three sibling rows do not store decision principles. The Kleene row preserves finite prefix-tree obstruction data, the Specker row preserves finite monotone bounded windows without a located supremum, and the choice-sequence row preserves only displayed prefix answers.

It remains to inspect the Real and Completion handoff. Since the terminal $\RealUp$ seal is reached only through $R$ and is followed by the explicit refusal row $F$, any attempted extraction of LPO, LLPO, selected limits, quotient equality, trichotomy, choice, compactness, or ambient completeness asks for a coordinate outside the accepted carrier.
\end{proof}

\falsifiablePrediction{Every accepted $\WeakOmniscienceBoundaryUp$ Real or Completion read factors through $\RegSeqRatUp$, $\RealUp$, $\KleeneTreeUp$, $\SpeckerSequenceUp$, $\ChoiceSequenceUp$, the refusal row, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and the local $\NameCert$ row before the consumer may cite the boundary.}

\independenceWitness{regseqrat, real, kleene-tree, speckersequence, choice-sequence: $\WeakOmniscienceBoundaryUp$ is not bijective to any listed sibling because it jointly carries regular-rational readback, a terminal Real seal, finite Kleene obstruction data, Specker finite-window data, choice-sequence prefix data, an explicit weak-omniscience refusal row, transport, replay, provenance, and local naming.}

\closureat{WeakOmniscienceBoundaryUp}{seedStr}

\begin{closurestatus}{\WeakOmniscienceBoundaryUp}
  \constructivestory{A $\WeakOmniscienceBoundaryUp$ packet is generated from RegSeqRat readback, a RealUp seal, KleeneTree finite obstruction rows, SpeckerSequence finite-window rows, ChoiceSequence prefix rows, an explicit refusal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:weak-omniscience-boundary-carrier}, \autoref{thm:weak-omniscience-boundary-namecert-obligations}, and \autoref{thm:weak-omniscience-boundary-real-completion-nonescape} seed the finite weak-omniscience boundary for L10 Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close LPO, LLPO, selected limits, quotient stream equality, trichotomy, countable choice, open-cover compactness, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, RegSeqRat readback exactness, Real-seal handoff, Kleene-tree obstruction stability, Specker finite-window stability, ChoiceSequence prefix stability, and refusal nonescape for BEDC.Derived.WeakOmniscienceBoundaryUp.}
\end{closurestatus}
