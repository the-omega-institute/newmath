\chapter{A Concrete Naming Certificate for $\KuratowskiFourteenSetUp$}
\label{ch:concrete-instances-kuratowski-fourteen-set-namecert}
\origin{human}

Following Kuratowski's closure-complement theorem, the $\KuratowskiFourteenSetUp$ packet records the classical fourteen-set orbit of one source subset as finite BEDC rows. It sits over the topology carrier of \autoref{ch:concrete-instances-topology-namecert}, the membership discipline of \autoref{ch:concrete-instances-setlike-namecert}, the finite endpoint classifier of \autoref{ch:concrete-instances-bool-namecert}, the list-spine route of \autoref{ch:concrete-instances-list-namecert}, the closure operator packet of \autoref{ch:concrete-instances-kuratowski-closure-namecert}, and the closure-complement route of \autoref{ch:concrete-instances-kuratowski-closure-complement-namecert}. The packet names a finite orbit classifier only; it does not import a host powerset, Boolean algebra of all subsets, quotient equality of subsets, selected representatives, or an ambient topological lattice.

\begin{definition}[Kuratowski fourteen-set carrier]
\label{def:kuratowski-fourteen-set-carrier}
A $\KuratowskiFourteenSetUp$ carrier is a finite $\BHist$ packet
$$
K=(T,S,L,C,W,O,D,H,R,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the $\TopologyUp$ source row carrying the finite closure-context read.
\item $S$ is the $\SetLikeUp$ source-set row whose orbit is inspected.
\item $L$ is the $\KuratowskiClosureUp$ closure row, including the displayed extensivity, monotonicity, and idempotence reads used by the orbit steps.
\item $C$ is the complement row, exposed through $\BoolUp$ membership flips against the displayed source universe.
\item $W$ is the $\ListUp$ word ledger of closure/complement words admitted for the orbit computation.
\item $O$ is the generated orbit list, with one row for each displayed word in $W$ and its resulting set row.
\item $D$ is the duplicate-collapse classifier, recording when two generated orbit rows are identified by the finite membership observations already displayed.
\item $H$ records componentwise $\hsame$ transport for the topology row, source-set row, closure row, complement row, word ledger, orbit rows, duplicate classifier, replay, provenance, and local name.
\item $R$ records displayed $\Cont$ replay from source set to closure/complement word evaluation, orbit insertion, and duplicate collapse.
\item $P$ is the $\Pkg$ provenance over $\KuratowskiFourteenSetUp$, $\KuratowskiClosureComplementUp$, $\TopologyUp$, $\SetLikeUp$, $\BoolUp$, $\ListUp$, $\KuratowskiClosureUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\KuratowskiFourteenSetUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $T,S,L,C,W,O,D,H,R,P,N$. It contains no coordinate for host powerset equality, Boolean algebra of arbitrary subsets, quotient extensionality, selected representatives, infinite word closure, or a completed lattice of opens and closed sets.
\end{definition}

\begin{theorem}[Kuratowski fourteen-set NameCert obligations]
\label{thm:kuratowski-fourteen-set-namecert-obligations}
For every accepted $\KuratowskiFourteenSetUp$ carrier
$$
K=(T,S,L,C,W,O,D,H,R,P,N),
$$
the local $\NameCert_{\KuratowskiFourteenSetUp}$ surface consists exactly of carrier admission for the topology source row, source-set row, closure row, complement row, finite word ledger, generated orbit list, duplicate-collapse classifier, componentwise transport, displayed replay, package provenance, and local naming. Every fourteen-set read must therefore expose the finite closure/complement word list and the duplicate-collapse row before a consumer may read the orbit as a Kuratowski boundary.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:kuratowski-fourteen-set-carrier}. The rows $T,S,L,C$ supply the topological context, inspected source set, closure operator, and complement operation.

Induct over the finite word list $W$. The empty word returns the source-set row $S$. For a cons step, the new leading letter is either closure or complement: closure uses the displayed row $L$, complement uses the displayed row $C$, and the induction hypothesis supplies the already generated predecessor row in $O$.

After each word step, insert the resulting row into the orbit list $O$ and inspect $D$. The duplicate-collapse classifier records exactly the finite membership observations that identify the new row with an earlier orbit row or keep it as a visible new entry.

The structural rows $H,R,P,N$ preserve and name this finite computation. Componentwise transport cannot add a host powerset equality, displayed replay follows only the word-ledger route, provenance records the dependency chain, and the local name binds the packet. Thus the NameCert surface is precisely the listed finite orbit and collapse data.
\end{proof}

\falsifiablePrediction{Every $\KuratowskiFourteenSetUp$ consumer must display the source set, closure row, complement row, finite closure/complement word ledger, generated orbit list, duplicate-collapse classifier, hsame transport, Cont replay, Pkg provenance, and local NameCert row before citing the fourteen-set boundary.}

\independenceWitness{kuratowski-closure-complement, kuratowski-closure, topology, setlike, list: $\KuratowskiFourteenSetUp$ is not bijective to any listed sibling because it jointly carries one source set, closure and complement generators, a finite word ledger, generated orbit rows, duplicate-collapse classification, transport, replay, provenance, and local naming in one packet.}

\closureat{KuratowskiFourteenSetUp}{seedStr}

\begin{closurestatus}{\KuratowskiFourteenSetUp}
  \constructivestory{A $\KuratowskiFourteenSetUp$ packet is generated from a topology row, source-set row, Kuratowski closure row, complement row, finite List word ledger, generated orbit list, duplicate-collapse classifier, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:kuratowski-fourteen-set-carrier} and \autoref{thm:kuratowski-fourteen-set-namecert-obligations} seed the finite closure/complement orbit packet and its duplicate-collapse boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host powersets, Boolean algebras of arbitrary subsets, quotient extensionality, selected representatives, infinite word closure, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source-set exposure, closure-row exposure, complement-row exposure, finite word replay, orbit-list coverage, duplicate-collapse exactness, transport, replay, provenance, and local naming for BEDC.Derived.KuratowskiFourteenSetUp.}
\end{closurestatus}
