\chapter{A Concrete Naming Certificate for $\WronskianUp$}
\label{ch:concrete-instances-wronskian-namecert}
\origin{human}

Following Sturm--Liouville theory and Bishop--Bridges constructive analysis, the $\WronskianUp$ packet records the finite determinant-and-derivative surface used to inspect linear dependence before an ODE or real-analysis consumer may read a terminal real seal. It sits after the derivative rows of \autoref{ch:concrete-instances-derivative-namecert}, the continuous-map family surface of \autoref{ch:concrete-instances-continuousmap-namecert}, the finite stream-window route of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a constructive Wronskian certificate as displayed BEDC rows; it does not import host function equality, classical limits, selected differentiability witnesses, quotient real names, or ambient $\RealUp$ completeness.

\falsifiablePrediction{A consumer that reads a Wronskian determinant or linear-dependence verdict without the derivative, continuous-map, StreamName, RegSeqRat, DyadicRatCore, and Real-seal rows has left the $\WronskianUp$ packet.}

\independenceWitness{derivative, continuousmap, streamname, regseqrat, dyadicratcore, real: $\WronskianUp$ is not bijective to any one sibling because it jointly carries derivative jets, determinant rows, rational readback, terminal real sealing, transport, replay, provenance, and local naming.}

\begin{definition}[Wronskian carrier]
\label{def:wronskian-carrier}
A $\WronskianUp$ carrier is a finite $\BHist$ packet
$$
W=(F,D,J,\Omega,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the accepted $\ContinuousMapUp$ finite family row for the inspected functions;
\item $D$ is the $\DerivativeUp$ derivative-admission row for the displayed derivative depths;
\item $J$ is the finite jet table whose entries are read only through the same derivative and continuous-map rows;
\item $\Omega$ is the alternating determinant ledger over the displayed jet table;
\item $S$ is the $\StreamNameUp$ finite-request section selecting the inspected windows;
\item $R$ is the $\RegSeqRatUp$ readback row for the determinant values on those windows;
\item $E$ is the terminal $\RealUp$ seal for the displayed Wronskian value rows;
\item $H$ records componentwise $\hsame$ transport of the family, derivative, jet, determinant, window, readback, and seal rows;
\item $C$ records displayed $\Cont$ replay from function-family requests through derivative jets, determinant assembly, StreamName windows, RegSeqRat readback, and the Real seal;
\item $P$ is the $\Pkg$ provenance over $\WronskianUp$, $\ContinuousMapUp$, $\DerivativeUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\WronskianUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these rows. It has no coordinate for host function equality, a classical limit, a selected differentiability witness, quotient real equality, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[Wronskian NameCert obligations]
\label{thm:wronskian-namecert-obligations}
For every accepted $\WronskianUp$ carrier
$$
W=(F,D,J,\Omega,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\WronskianUp}$ surface consists exactly of continuous-family admission through $F$, derivative-row admission through $D$, finite jet exposure through $J$, determinant-ledger exposure through $\Omega$, finite-window exposure through $S$, regular rational readback through $R$, terminal real sealing through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\leanchecked{BEDC.Derived.WronskianUp.WronskianNameCertObligations}
\begin{proof}
Project the carrier of \autoref{def:wronskian-carrier}. The family row, derivative row, jet table, determinant ledger, window row, readback row, and Real seal row are visible before the structural rows are read.

Read the analytic route in order. The row $F$ supplies the continuous-map family surface, $D$ supplies derivative admission, $J$ records only the displayed finite derivative jets, and $\Omega$ assembles those jets into the Wronskian determinant ledger.

The value-facing route then passes through $S,R,E$. The $\StreamNameUp$ row fixes the inspected finite requests, $\RegSeqRatUp$ reads the determinant values as regular rational data, and $\RealUp$ seals only that displayed readback.

The rows $H,C,P,N$ transport, replay, source, and name the same packet. Since the carrier has no coordinate for the excluded host principles, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Wronskian determinant-ledger exactness]
\label{thm:wronskian-determinant-ledger-exactness}
Every determinant or linear-dependence read of an accepted $\WronskianUp$ packet must expose the derivative row $D$, continuous-map family row $F$, finite jet table $J$, determinant ledger $\Omega$, $\StreamNameUp$ window row $S$, $\RegSeqRatUp$ readback row $R$, $\DyadicRatCoreUp$ tolerance route, and terminal $\RealUp$ seal $E$. The read is exact only as the displayed dependency route through \autoref{ch:concrete-instances-derivative-namecert}, \autoref{ch:concrete-instances-continuousmap-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert}; it exports no host function equality, selected differentiability witness, or quotient Real equality.
\end{theorem}
\leanchecked{BEDC.Derived.WronskianUp.WronskianCarrier\_determinant\_ledger\_exactness}
\begin{proof}
Project the carrier of \autoref{def:wronskian-carrier}. The determinant-facing rows are $F,D,J,\Omega$, so determinant assembly can only inspect the displayed continuous-map family, derivative admission, jet table, and alternating ledger.

Route value reads through $S,R,E$. The finite window row fixes the requests, the regular rational row reads determinant values, and the Real seal exposes only those displayed values with the dyadic tolerance route named by the package row.

The structural rows $H,C,P,N$ preserve this inventory under transport, replay, provenance, and local naming. Since none of these rows contains host function equality, a selected differentiability witness, or quotient Real equality, determinant-ledger exactness is confined to the displayed sibling route.
\end{proof}

\begin{theorem}[Wronskian regular Cauchy determinant handoff]
\label{thm:wronskian-regular-cauchy-determinant-handoff}
Every accepted $\WronskianUp$ packet whose rows read derivative data, $\ContinuousMapUp$ moduli, $\RealUp$ seals, $\RegSeqRatUp$ regular rational readbacks, $\StreamNameUp$ finite windows, and $\DyadicRatCoreUp$ tolerance rows exports a determinant handoff only through those displayed rows. In carrier notation, the determinant handoff is available only after the route
$$
F,D,J,\Omega,S,R,E,H,C,P,N
$$
has exposed the continuous-map family row, derivative row, finite jet table, determinant ledger, stream-window row, regular readback, terminal Real seal, componentwise transport, displayed replay, provenance, and local naming. $\SturmSeparationUp$ and $\SturmRootCountUp$ consumers may cite this handoff, but they receive no hidden quotient Real equality, host determinant equality, selected differentiability witness, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.WronskianUp.WronskianCarrier\_regular\_cauchy\_determinant\_handoff}
\begin{proof}
Project the carrier of \autoref{def:wronskian-carrier}. The derivative and continuous-map data are displayed as $D$ and $F$, while the determinant ledger is assembled only from the finite jet table $J$ and the alternating row $\Omega$.

Route the value-facing read through $S,R,E$. The $\StreamNameUp$ row fixes the finite windows, the $\RegSeqRatUp$ row reads the determinant values as regular rational data, and the $\RealUp$ seal exposes only those displayed readbacks with the dyadic tolerance route named by $P$.

Apply componentwise transport and replay through $H$ and $C$. Since the determinant ledger has already been tied to $F,D,J,\Omega,S,R,E$, stability under $\hsame$ and $\Cont$ replay can only move the same displayed rows.

Finally pass through provenance and local naming. The Sturm consumers cited in the statement are downstream readers of this handoff, not additional coordinates of the $\WronskianUp$ carrier, so they cannot import hidden quotient Real equality, host determinant equality, selected differentiability witnesses, or ambient completeness.
\end{proof}

\begin{theorem}[Wronskian RegSeqRat derivative-window dependency]
\label{thm:wronskian-regseqrat-window-dependency}
Every accepted $\WronskianUp$ derivative-read packet over $\RealUp$ consumes derivative rows only through the finite route
$$
F,D,J,\Omega,S,R,E,H,C,P,N .
$$
The derivative rows are exposed as $\StreamNameUp$ windows from \autoref{ch:concrete-instances-streamname-namecert}, $\RegSeqRatUp$ regular readback from \autoref{ch:concrete-instances-regseqrat-namecert}, $\DyadicRatCoreUp$ tolerance budgets from \autoref{ch:concrete-instances-dyadicratcore-namecert}, and $\ContinuousMapUp$ graph rows from \autoref{ch:concrete-instances-continuousmap-namecert} before the $\SturmRootCountUp$ and $\SturmSeparationUp$ consumers of \autoref{ch:concrete-instances-sturm-root-count-namecert} and \autoref{ch:concrete-instances-sturm-separation-namecert} may read a determinant or separation verdict. The packet exports no host derivative equality, selected differentiability witness, quotient real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.WronskianUp.WronskianCarrier\_regseqrat\_window\_dependency}
\begin{proof}
Project the carrier of \autoref{def:wronskian-carrier}. The continuous-map row $F$, derivative row $D$, finite jet table $J$, and determinant ledger $\Omega$ expose the derivative-facing data before any value row is read.

Route each derivative value through the window and readback rows. The $\StreamNameUp$ row $S$ fixes the finite inspected windows, the $\RegSeqRatUp$ row $R$ supplies regular rational readback, and the dyadic tolerance route named by $P$ is the only tolerance budget visible to the determinant ledger.

Apply the regular Cauchy determinant handoff of \autoref{thm:wronskian-regular-cauchy-determinant-handoff}. It keeps the determinant route attached to the displayed continuous-map, derivative, stream-window, regular-readback, dyadic-tolerance, and Real-seal rows.

Finally use the Sturm separation handoff of \autoref{thm:wronskian-sturm-separation-handoff}. The Sturm consumers receive the same finite route as downstream input, so they cannot replace it by host derivative equality, selected differentiability data, quotient real equality, or ambient completeness.
\end{proof}

\begin{theorem}[Wronskian determinant-route stability]
\label{thm:wronskian-determinant-route-stability}
Let
$$
W=(F,D,J,\Omega,S,R,E,H,C,P,N)
$$
and
$$
W'=(F',D',J',\Omega',S',R',E',H',C',P',N')
$$
be accepted $\WronskianUp$ packets. Suppose the displayed componentwise $\hsame$ transport rows identify the continuous-family rows $F,F'$, derivative rows $D,D'$, finite jet tables $J,J'$, determinant ledgers $\Omega,\Omega'$, $\StreamNameUp$ window rows $S,S'$, $\RegSeqRatUp$ readback rows $R,R'$, $\DyadicRatCoreUp$ tolerance routes named by $P,P'$, and terminal $\RealUp$ seals $E,E'$. Then every determinant or linear-dependence read of $W$ and $W'$ follows the same finite route through those rows. In particular, transport cannot replace the determinant route by host function equality, selected differentiability witnesses, quotient real equality, or ambient completeness.
\end{theorem}
\begin{proof}
Project both carriers using \autoref{def:wronskian-carrier}. The hypotheses identify exactly the rows that the carrier exposes to determinant-facing and value-facing consumers: the continuous-family row, derivative row, jet table, determinant ledger, finite window row, regular readback row, dyadic tolerance route, and terminal Real seal.

Apply \autoref{thm:wronskian-determinant-ledger-exactness}. Any determinant read must already pass through $F,D,J,\Omega,S,R,E$ and the named dyadic tolerance route. Since the corresponding primed rows are related componentwise by the displayed $\hsame$ transport, the read has no second route on which to switch representatives.

Use \autoref{thm:wronskian-linear-dependence-boundary} for the linear-dependence consumer boundary. Linear-dependence reads are downstream of the same determinant ledger and value route, so the componentwise transport carries them through the same finite rows.

Finally apply \autoref{thm:wronskian-real-seal-nonescape}. The terminal Real seal is reached only after the displayed route has been read. Host function equality, selected differentiability witnesses, quotient real equality, and ambient completeness are absent from both carriers, so transport cannot introduce them.
\end{proof}
\leanchecked{BEDC.Derived.WronskianUp.WronskianCarrier\_determinant\_route\_stability}

\begin{theorem}[Wronskian linear-dependence boundary]
\label{thm:wronskian-linear-dependence-boundary}
Every linear-dependence or Sturm--Liouville-facing consumer of an accepted $\WronskianUp$ packet must pass through
$$
F,D,J,\Omega,S,R,E,H,C,P,N .
$$
The consumer receives derivative jets, determinant rows, finite request windows, regular rational readback, dyadic tolerance arithmetic, and the terminal $\RealUp$ seal. It receives no host function equality, selected basis relation, classical zero theorem, quotient real equality, countable-choice row, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start with \autoref{thm:wronskian-namecert-obligations}. It identifies the displayed carrier rows as the whole local NameCert surface and gives no second source for derivative, determinant, readback, or seal data.

The derivative and determinant rows supply the only linear-dependence-facing evidence. A consumer may inspect $J$ and $\Omega$, but those rows are already downstream of the continuous-map and derivative admissions $F,D$.

The value route is fixed by $S,R,E$. The finite window is read by $\StreamNameUp$, the determinant value is read by $\RegSeqRatUp$ with $\DyadicRatCoreUp$ tolerance arithmetic, and the terminal endpoint is the displayed $\RealUp$ seal.

Therefore a linear-dependence consumer cannot replace the packet by host function equality, a selected basis relation, a classical zero theorem, quotient real equality, countable choice, or ambient completeness. Those objects are absent from the carrier and from the local classifier.
\end{proof}
\leanchecked{BEDC.Derived.WronskianUp.WronskianCarrier\_linear\_dependence\_boundary}

\begin{theorem}[Wronskian linear-dependence witness route]
\label{thm:wronskian-linear-dependence-witness}
A linear-dependence witness read for an accepted $\WronskianUp$ packet is admitted only when the finite jet table $J$, determinant ledger $\Omega$, $\StreamNameUp$ window row $S$, $\RegSeqRatUp$ readback row $R$, $\DyadicRatCoreUp$ tolerance route named by $P$, and terminal $\RealUp$ seal $E$ are all displayed. The witness is therefore a finite Wronskian readback route through the packet rows, not a host zero theorem, selected basis relation, quotient real equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:wronskian-carrier}. A witness-facing read can inspect only the displayed family, derivative, jet, determinant, window, readback, seal, transport, replay, provenance, and local naming rows.

Invoke determinant-ledger exactness and determinant-route stability, \autoref{thm:wronskian-determinant-ledger-exactness} and \autoref{thm:wronskian-determinant-route-stability}. Exactness forces the read through $J,\Omega,S,R,E$ and the named dyadic tolerance route, while stability keeps that route fixed under componentwise transport.

The linear-dependence boundary then applies through \autoref{thm:wronskian-linear-dependence-boundary}. Since host zero theorems and selected basis relations are not carrier coordinates and are not supplied by the sibling rows, the admitted witness is only the displayed finite readback route.
\end{proof}
\leanchecked{BEDC.Derived.WronskianUp.WronskianCarrier\_linear\_dependence\_witness}

\begin{theorem}[Wronskian Real-seal nonescape]
\label{thm:wronskian-real-seal-nonescape}
Every Real-facing consumer of an accepted $\WronskianUp$ packet reaches the terminal $\RealUp$ seal only through the displayed route
$$
F,D,J,\Omega,S,R,E,H,C,P,N
$$
and through the sibling rows $\ContinuousMapUp$, $\DerivativeUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The packet exports no quotient stream, host real equality, selected limit, selected differentiability witness, countable-choice row, ambient completeness theorem, or bridge row.
\end{theorem}
\begin{proof}
Project the Real-facing rows from \autoref{def:wronskian-carrier}. The terminal seal row $E$ is downstream of the family, derivative, jet, determinant, finite-window, and regular-readback rows.

Apply \autoref{thm:wronskian-namecert-obligations}. It states that these rows, together with $H,C,P,N$, are the complete local NameCert surface for the accepted packet.

Use \autoref{thm:wronskian-linear-dependence-boundary} for the consumer boundary. Any downstream read that wants a Wronskian value or a linear-dependence verdict must pass through the same finite route before it can inspect the Real seal.

The excluded objects are not carrier coordinates and are not supplied by the sibling rows. Hence the terminal $\RealUp$ seal cannot be reached through quotient streams, host equality, selected limits, selected differentiability witnesses, countable choice, ambient completeness, or bridge data.
\end{proof}
\leanchecked{BEDC.Derived.WronskianUp.WronskianCarrier\_real\_seal\_nonescape}

\begin{theorem}[Wronskian carrier obligation]
\label{thm:wronskian-carrier-obligation}
Every accepted $\WronskianUp$ carrier exposes the obligation rows
$$
F,D,J,\Omega,S,R,E,H,C,P,N
$$
before any determinant, linear-dependence, ODE, or Real-facing consumer reads the packet. The rows must include the continuous-map family, derivative admissions, finite jet table, determinant ledger, StreamName windows, RegSeqRat readback, DyadicRatCore tolerance route named by provenance, terminal Real seal, componentwise transport, continuation replay, package provenance, and local NameCert naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:wronskian-carrier}. Its analytic rows are $F,D,J,\Omega,S,R,E$, and its structural rows are $H,C,P,N$.

Use the NameCert obligation theorem. It identifies continuous-family admission, derivative-row admission, finite jet exposure, determinant-ledger exposure, finite-window exposure, regular rational readback, Real sealing, transport, replay, provenance, and local naming as the complete local surface.

The determinant-ledger and Real-seal nonescape theorems then force every downstream consumer through the same row sequence. Thus the carrier obligation is row projection and route preservation, not determinant algebra over an ambient host field.
\end{proof}
\leanchecked{BEDC.Derived.WronskianUp.WronskianCarrier\_obligation}

\begin{theorem}[Wronskian classifier stability]
\label{thm:wronskian-classifier-stability}
The $\WronskianUp$ classifier is stable under componentwise $\hsame$ transport of the continuous-map family rows, derivative rows, finite jet tables, determinant ledgers, StreamName windows, RegSeqRat readbacks, DyadicRatCore tolerance routes, Real seals, continuation rows, provenance rows, and local NameCert rows. Such transport preserves the finite Wronskian route and cannot substitute host function equality, selected differentiability witnesses, quotient real equality, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Start with determinant-route stability, \autoref{thm:wronskian-determinant-route-stability}. It already transports determinant and linear-dependence reads across componentwise identified rows without creating a second representative route.

Apply the carrier obligation theorem. Since each accepted read begins with the same displayed row inventory, classifier comparison may only inspect those rows and their componentwise transports.

Finally use Real-seal nonescape. The terminal seal is downstream of the displayed route through $\ContinuousMapUp$, $\DerivativeUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$, so the classifier cannot switch to any excluded host or quotient principle.
\end{proof}
\leanchecked{BEDC.Derived.WronskianUp.WronskianCarrier\_classifier\_stability}

\begin{theorem}[Wronskian ledger exactness]
\label{thm:wronskian-ledger-exactness}
The Wronskian determinant ledger $\Omega$ is exact only as a displayed route through the derivative row $D$, continuous-map family row $F$, finite jet table $J$, StreamName window row $S$, RegSeqRat readback row $R$, DyadicRatCore tolerance route named by $P$, and Real seal $E$. Removing any one of these rows changes the ledger surface and prevents a consumer from reading the same $\WronskianUp$ packet.
\end{theorem}
\begin{proof}
Use determinant-ledger exactness. It requires determinant and linear-dependence reads to expose the derivative, continuous-map, finite-jet, determinant, StreamName, RegSeqRat, DyadicRatCore, and Real-seal route.

Apply carrier obligation and classifier stability. They keep the ledger tied to $F,D,J,\Omega,S,R,E,H,C,P,N$ under transport and route replay, so no host determinant equality or selected differentiability witness can replace a missing row.

If a listed row is removed, the continuation row $C$ and provenance row $P$ no longer replay the same dependency chain. The packet therefore fails to expose the exact Wronskian ledger claimed by the local NameCert surface.
\end{proof}
\leanchecked{BEDC.Derived.WronskianUp.WronskianCarrier\_ledger\_exactness}

\begin{theorem}[Wronskian obligation closure scope]
\label{thm:wronskian-obligation-closure-scope}
The carrier, NameCert obligations, determinant ledger, determinant-route stability, linear-dependence boundary, witness route, and Real-seal nonescape theorems form the visible $\WronskianUp$ obligation surface over the $\ContinuousMapUp$, $\DerivativeUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows. Every accepted read of
$$
W=(F,D,J,\Omega,S,R,E,H,C,P,N)
$$
therefore remains inside the finite route through continuous-map admission, derivative admission, jet display, determinant assembly, dyadic tolerance, finite request windows, regular rational readback, terminal Real sealing, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming.
\end{theorem}
\begin{proof}
Start with the carrier and NameCert obligations, \autoref{def:wronskian-carrier} and \autoref{thm:wronskian-namecert-obligations}. They expose exactly the rows $F,D,J,\Omega,S,R,E,H,C,P,N$ and no additional analytic coordinate.

Use determinant-ledger exactness and determinant-route stability, \autoref{thm:wronskian-determinant-ledger-exactness} and \autoref{thm:wronskian-determinant-route-stability}. These results keep determinant reads on the route through $\ContinuousMapUp$, $\DerivativeUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows.

Finally apply the linear-dependence boundary, witness route, and Real-seal nonescape theorems, \autoref{thm:wronskian-linear-dependence-boundary}, \autoref{thm:wronskian-linear-dependence-witness}, and \autoref{thm:wronskian-real-seal-nonescape}. They show that every consumer-facing read remains within the displayed obligation surface and exports none of the excluded host or quotient principles.
\end{proof}
\leanchecked{BEDC.Derived.WronskianUp.WronskianCarrier\_obligation\_closure\_scope}

\begin{theorem}[Wronskian differential-equation consumer handoff]
\label{thm:wronskian-differential-equation-consumer-handoff}
Every ODE, Sturm--Liouville, or linear-dependence consumer of an accepted $\WronskianUp$ packet must enter through the determinant-ledger route of \autoref{thm:wronskian-obligation-closure-scope} before reading any $\RealUp$-seal conclusion. In particular, the consumer must expose $F,D,J,\Omega,S,R,E,H,C,P,N$ and the sibling rows $\ContinuousMapUp$, $\DerivativeUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before it may use a Wronskian value or a linear-dependence verdict.
\end{theorem}
\begin{proof}
Apply the obligation closure scope of \autoref{thm:wronskian-obligation-closure-scope}. It identifies the determinant-ledger route as the visible surface for every accepted read.

Use the linear-dependence boundary and witness route, \autoref{thm:wronskian-linear-dependence-boundary} and \autoref{thm:wronskian-linear-dependence-witness}. These force ODE-facing and linear-dependence-facing consumers to read the derivative jets, determinant ledger, dyadic tolerance route, StreamName window, and RegSeqRat readback before any verdict is available.

Finally use Real-seal nonescape from \autoref{thm:wronskian-real-seal-nonescape}. The terminal seal row is downstream of the same determinant-ledger route, so no consumer can reach a Real-seal conclusion through host function equality, selected bases, classical zero theorems, quotient real equality, countable choice, or ambient completeness.
\end{proof}
\leanchecked{BEDC.Derived.WronskianUp.WronskianCarrier\_differential\_equation\_consumer\_handoff}

\begin{theorem}[Wronskian Sturm separation handoff]
\label{thm:wronskian-sturm-separation-handoff}
Any $\SturmSeparationUp$ or $\SturmRootCountUp$ consumer that reads a $\WronskianUp$ zero or nonzero separation must first expose the Wronskian carrier rows
$$
F,D,J,\Omega,S,R,E,H,C,P,N
$$
together with the Sturm comparison and root-count sibling route. The handoff supplies only the displayed finite determinant-ledger route through $\ContinuousMapUp$, $\DerivativeUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$; it supplies no host zero theorem, selected basis relation, quotient real equality, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Project the Wronskian carrier of \autoref{def:wronskian-carrier}. The rows $F,D,J,\Omega,S,R,E,H,C,P,N$ display the function family, derivative admission, jet table, determinant ledger, finite request windows, regular rational readback, Real seal, transport, replay, provenance, and local naming before any consumer read is available.

Apply determinant-ledger exactness and determinant-route stability, \autoref{thm:wronskian-determinant-ledger-exactness} and \autoref{thm:wronskian-determinant-route-stability}. These keep zero and nonzero determinant reads attached to the same displayed route through continuous-map admission, derivative admission, finite windows, dyadic tolerance, regular rational readback, and the terminal Real seal.

Use the linear-dependence boundary and Real-seal nonescape results, \autoref{thm:wronskian-linear-dependence-boundary} and \autoref{thm:wronskian-real-seal-nonescape}. They prevent the consumer from replacing the displayed determinant ledger by host function equality, selected bases, quotient real equality, countable choice, or ambient completeness.

Finally route the zero-separation read through the existing Sturm sibling chapters as consumers. The $\SturmSeparationUp$ and $\SturmRootCountUp$ rows therefore receive a Wronskian handoff only after the determinant and Real-facing rows have been shown; they do not add new coordinates to the Wronskian packet.
\end{proof}
\leanchecked{BEDC.Derived.WronskianUp.WronskianCarrier\_sturm\_separation\_handoff}

\begin{theorem}[Wronskian differential scope ledger]
\label{thm:wronskian-differential-scope-ledger}
Every determinant, zero-separation, or linear-dependence consumer of an accepted $\WronskianUp$ packet must expose the $\ContinuousMapUp$ and $\DerivativeUp$ rows, the $\RegSeqRatUp$ and $\DyadicRatCoreUp$ readback rows, the terminal $\RealUp$ seal, and the $\SturmSeparationUp$ and $\SturmRootCountUp$ handoff rows before any local NameCert export is available. In carrier notation, the visible differential scope is the route
$$
F,D,J,\Omega,S,R,E,H,C,P,N
$$
together with the Sturm handoff of \autoref{thm:wronskian-sturm-separation-handoff}. The ledger exports no host derivative equality, selected differentiability witness, selected basis relation, quotient real equality, countable choice, ambient $\RealUp$ completeness, or Sturm conclusion outside the displayed route.
\end{theorem}
\begin{proof}
Project the Wronskian carrier of \autoref{def:wronskian-carrier}. The rows $F,D,J,\Omega,S,R,E,H,C,P,N$ are the only displayed continuous-map, derivative, jet, determinant, stream-window, regular-readback, Real-seal, transport, replay, provenance, and local naming coordinates.

Use determinant-ledger exactness and derivative-route stability through \autoref{thm:wronskian-determinant-ledger-exactness} and \autoref{thm:wronskian-determinant-route-stability}. These keep every determinant read attached to $\ContinuousMapUp$, $\DerivativeUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ before a consumer can inspect the value.

Apply the linear-dependence boundary and Real-seal nonescape, \autoref{thm:wronskian-linear-dependence-boundary} and \autoref{thm:wronskian-real-seal-nonescape}. They keep zero and nonzero reads on the same finite route and prevent replacement by host derivative equality, selected differentiability witnesses, selected bases, quotient real equality, countable choice, or ambient completeness.

Finally use the Sturm separation handoff of \autoref{thm:wronskian-sturm-separation-handoff}. The $\SturmSeparationUp$ and $\SturmRootCountUp$ consumers receive only the displayed Wronskian route, so local NameCert export remains scoped to the differential ledger named in the statement.
\end{proof}
\leanchecked{BEDC.Derived.WronskianUp.WronskianCarrier\_differential\_scope\_ledger}

\begin{theorem}[Wronskian L10 scope binding]
\label{thm:wronskian-l10-scope-binding}
Every accepted $\WronskianUp$ packet consumed by determinant, derivative, or Sturm-separation routes must expose the finite route through $\ContinuousMapUp$ and $\DerivativeUp$ rows, $\DyadicRatCoreUp$ tolerance rows, $\RegSeqRatUp$ readback, $\StreamNameUp$ windows, the terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\WronskianUp}$ row before any zero or nonzero determinant conclusion is public. The route exports no host differentiable function, selected representative, quotient stream, trichotomy branch, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.WronskianUp.WronskianCarrier\_l10\_scope\_binding}
\begin{proof}
Project the Wronskian carrier of \autoref{def:wronskian-carrier}. The carrier displays $F,D,J,\Omega,S,R,E,H,C,P,N$, so every determinant-facing consumer starts from continuous-map admission, derivative admission, the finite jet table, and the determinant ledger.

Apply determinant-ledger exactness and determinant-route stability, \autoref{thm:wronskian-determinant-ledger-exactness} and \autoref{thm:wronskian-determinant-route-stability}. These force determinant, zero, and nonzero reads through the visible $\ContinuousMapUp$, $\DerivativeUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ route before any public conclusion is available.

Use Real-seal nonescape and the Sturm separation handoff, \autoref{thm:wronskian-real-seal-nonescape} and \autoref{thm:wronskian-sturm-separation-handoff}. The terminal seal and the Sturm-facing consumer both remain downstream of the same displayed route.

Finally apply the differential scope ledger of \autoref{thm:wronskian-differential-scope-ledger}. Since host differentiable functions, selected representatives, quotient streams, trichotomy branches, and ambient completeness are absent from the carrier and classifier, the L10 public boundary is exactly the finite row route named in the statement.
\end{proof}

\begin{theorem}[Wronskian obligation-closure package]
\label{thm:wronskian-obligation-closure-package}
The carrier of \autoref{def:wronskian-carrier}, the NameCert obligations of \autoref{thm:wronskian-namecert-obligations}, determinant-ledger exactness, determinant-route stability, the linear-dependence boundary, the witness route, and Real-seal nonescape assemble the $\WronskianUp$ obligationClosure surface over $\ContinuousMapUp$, $\DerivativeUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The assembled package exports no host function equality, selected differentiability witness, quotient Real equality, countable choice, ambient completeness, or bridge row.
\end{theorem}
\begin{proof}
Project the carrier rows $F,D,J,\Omega,S,R,E,H,C,P,N$ from \autoref{def:wronskian-carrier}. The NameCert obligation theorem reads these as continuous-map admission, derivative admission, finite jet exposure, determinant exposure, StreamName windowing, RegSeqRat readback, Real sealing, transport, replay, provenance, and local naming.

Use determinant-ledger exactness and determinant-route stability. They bind determinant reads to the displayed finite route through $\ContinuousMapUp$, $\DerivativeUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ and keep the route invariant under componentwise $\hsame$ transport.

Apply the linear-dependence boundary, witness route, and Real-seal nonescape theorems. These force every ODE-facing or Real-facing consumer through the same rows and show that the excluded host principles are absent from the carrier, classifier, transport, replay, provenance, and local NameCert surfaces.
\end{proof}
\leanchecked{BEDC.Derived.WronskianUp.WronskianCarrier\_obligation\_closure\_package}
\closureat{WronskianUp}{scopedStr}

\begin{theorem}[Wronskian public determinant export]
\label{thm:wronskian-public-determinant-export}
Every accepted $\WronskianUp$ packet exports exactly the finite determinant ledger route through $\ContinuousMapUp$, $\DerivativeUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The exported public surface is the displayed route
$$
\begin{gathered}
F,D,J,\Omega,S,R,E,H,C,P,N,
\end{gathered}
$$
with determinant, zero-separation, and linear-dependence reads confined to those rows. It exports no host function equality, selected differentiability witness, quotient real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the Wronskian carrier of \autoref{def:wronskian-carrier}. The rows $F,D,J,\Omega,S,R,E,H,C,P,N$ are the only continuous-map, derivative, jet, determinant, finite-window, readback, Real-seal, transport, replay, provenance, and naming coordinates available to a public consumer.

Apply determinant-ledger exactness and determinant-route stability, \autoref{thm:wronskian-determinant-ledger-exactness} and \autoref{thm:wronskian-determinant-route-stability}. These force every determinant read through the same displayed route and keep that route fixed under componentwise $\hsame$ transport.

Use the linear-dependence boundary and Real-seal nonescape theorems, \autoref{thm:wronskian-linear-dependence-boundary} and \autoref{thm:wronskian-real-seal-nonescape}. They place zero-separation and linear-dependence reads downstream of the determinant ledger and terminal Real seal without adding host equality or a selected differentiability witness.

Finally apply the L10 scope binding of \autoref{thm:wronskian-l10-scope-binding}. The public export is therefore exactly the finite route named in the statement, and every excluded quotient, host, or ambient-completeness principle remains outside the packet.
\end{proof}
\leanchecked{BEDC.Derived.WronskianUp.WronskianCarrier\_public\_determinant\_export}

\begin{closurestatus}{\WronskianUp}
  \constructivestory{A $\WronskianUp$ packet is generated from a finite continuous-map family row, derivative-admission rows, a displayed jet table, an alternating determinant ledger, finite StreamName requests, regular rational readback, a terminal Real seal, componentwise transport, continuation replay, provenance, and a local NameCert row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:wronskian-carrier}, \autoref{thm:wronskian-namecert-obligations}, \autoref{thm:wronskian-determinant-ledger-exactness}, \autoref{thm:wronskian-determinant-route-stability}, \autoref{thm:wronskian-linear-dependence-boundary}, \autoref{thm:wronskian-linear-dependence-witness}, \autoref{thm:wronskian-real-seal-nonescape}, \autoref{thm:wronskian-carrier-obligation}, \autoref{thm:wronskian-classifier-stability}, \autoref{thm:wronskian-ledger-exactness}, \autoref{thm:wronskian-l10-scope-binding}, and \autoref{thm:wronskian-obligation-closure-package} bind the finite Wronskian carrier, determinant-ledger route, transport stability, linear-dependence boundary, witness route, Real-seal nonescape surface, carrier obligations, classifier stability, ledger exactness, L10 scope boundary, and obligation package.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host function equality, classical zero theorems, selected bases, selected differentiability witnesses, quotient real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Public export requires a checked Wronskian carrier theorem family, public consumer surface, and bridge discipline for BEDC.Derived.WronskianUp.}
\end{closurestatus}
