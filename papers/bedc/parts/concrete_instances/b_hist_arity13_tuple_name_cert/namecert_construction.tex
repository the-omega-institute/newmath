\chapter{A Concrete Naming Certificate for $\BHistArity13TupleNameCertUp$}
\label{ch:concrete-instances-b-hist-arity13-tuple-namecert}
\origin{ai-composite}

The $\BHistArity13TupleNameCertUp$ packet is the arity-thirteen capstone for the carrier-isomorphism bucket displayed by the neighboring tuple chapters \autoref{ch:concrete-instances-b-hist-octa-tuple-name-cert-namecert}, \autoref{ch:concrete-instances-b-hist-nona-tuple-name-cert-namecert}, \autoref{ch:concrete-instances-b-hist-deca-tuple-name-cert-namecert}, \autoref{ch:concrete-instances-b-hist-hendeca-tuple-namecert}, and \autoref{ch:concrete-instances-b-hist-dodeca-tuple-name-cert-namecert}. It is composite over representative bucket siblings including $\AuditMapRouteCompilerUp$ from \autoref{ch:concrete-instances-audit-map-route-compiler-namecert}, $\AnalogyCertificateGateUp$ from \autoref{ch:concrete-instances-analogycertificategate-namecert}, $\AxisCarryDiamondRouteUp$ from \autoref{ch:concrete-instances-axis-carry-diamond-route-namecert}, $\CompressionLedgerFunctorUp$ from \autoref{ch:concrete-instances-compression-ledger-functor-namecert}, $\FiniteObservationRefutationUp$ from \autoref{ch:concrete-instances-finite-observation-refutation-namecert}, and $\FocusedMultiHistLedgerUp$ from \autoref{ch:concrete-instances-focused-multi-hist-ledger-namecert}. The chapter gives the thirteen-row carrier a single BEDC name so downstream consumers can cite the tag, comparison, transport, replay, provenance, and local-name surface without copying the member-local tuple schema.

\begin{definition}[BHistArity13TupleNameCert carrier]
\label{def:b-hist-arity13-tuple-name-cert-carrier}
A $\BHistArity13TupleNameCertUp$ carrier is a finite $\BHist$ packet
$$
T=(R_0,R_1,R_2,R_3,R_4,R_5,R_6,R_7,R_8,R_9,R_{10},R_{11},R_{12},H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $R_0,\ldots,R_{12}$ are the thirteen tag-encoded carrier rows. Each row is a $\BHist$ entry whose tag records the local carrier role used by a cited bucket sibling and whose payload is read only as a displayed BEDC history component.
\item $H$ records componentwise $\hsame$ transport for $R_0,\ldots,R_{12},C,P,L$ and no transport row outside the displayed packet.
\item $C$ records the displayed $\Cont$ route that replays row admission, componentwise comparison, transport, provenance readback, and local naming for the same thirteen-row packet.
\item $P$ is the $\Pkg$ provenance over the arity-thirteen tag encoder, the cited tuple capstones, the representative bucket siblings named above, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $L$ is the local $\NameCert_{\BHistArity13TupleNameCertUp}$ row.
\end{enumerate}
The carrier has no coordinate for an untagged row, a fourteenth carrier component, a host-side equality test, a bucket-wide rewrite of sibling chapters, or a detached provenance table.
\end{definition}

\begin{definition}[BHistArity13TupleNameCert classifier]
\label{def:b-hist-arity13-tuple-name-cert-classifier}
Two $\BHistArity13TupleNameCertUp$ packets
$$
T=(R_0,R_1,R_2,R_3,R_4,R_5,R_6,R_7,R_8,R_9,R_{10},R_{11},R_{12},H,C,P,L)
$$
and
$$
T'=(R'_0,R'_1,R'_2,R'_3,R'_4,R'_5,R'_6,R'_7,R'_8,R'_9,R'_{10},R'_{11},R'_{12},H',C',P',L')
$$
are classifier-equivalent exactly when each displayed carrier row compares componentwise,
$$
\begin{aligned}
&R_0 \sim R'_0,\quad R_1 \sim R'_1,\quad R_2 \sim R'_2,\quad R_3 \sim R'_3,\quad R_4 \sim R'_4,\quad R_5 \sim R'_5,\\
&R_6 \sim R'_6,\quad R_7 \sim R'_7,\quad R_8 \sim R'_8,\quad R_9 \sim R'_9,\quad R_{10} \sim R'_{10},\quad R_{11} \sim R'_{11},\\
&R_{12} \sim R'_{12},
\end{aligned}
$$
the transport rows display the same $\hsame$ routes for those components and for $C,P,L$, and the replay rows display the same $\Cont$ route from row admission through local naming. No comparison is available through an omitted component, an untagged payload, or a member-chapter rewrite.
\end{definition}

\begin{theorem}[BHistArity13TupleNameCert carrier admission]
\label{thm:b-hist-arity13-tuple-name-cert-carrier-admission}
Every accepted $\BHistArity13TupleNameCertUp$ packet admits exactly the carrier rows
$$
R_0,R_1,R_2,R_3,R_4,R_5,R_6,R_7,R_8,R_9,R_{10},R_{11},R_{12},H,C,P,L.
$$
The thirteen rows are tag-encoded $\BHist$ components, and $H,C,P,L$ are the only structural rows by which a consumer may transport, replay, source, or locally name the packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:b-hist-arity13-tuple-name-cert-carrier}. The definition lists the thirteen tag-encoded $\BHist$ rows and then lists the transport, continuation, provenance, and local naming rows.

There is no remaining coordinate in the packet. Thus any accepted carrier read lands in exactly the displayed rows, and any proposed fourteenth component or detached structural table lies outside the accepted surface.
\end{proof}

\begin{theorem}[BHistArity13TupleNameCert classifier exactness]
\label{thm:b-hist-arity13-tuple-name-cert-classifier-exactness}
For accepted $\BHistArity13TupleNameCertUp$ packets $T$ and $T'$, the classifier of \autoref{def:b-hist-arity13-tuple-name-cert-classifier} is exact for the arity-thirteen carrier surface: it compares the thirteen tagged $\BHist$ rows componentwise, compares the displayed $\hsame$ and $\Cont$ routes through $H,H',C,C'$, and preserves the provenance and local naming rows through $P,P',L,L'$. It does not compare by bucket membership alone, by a host equality test, or by a hidden isomorphism between member chapters.
\end{theorem}
\begin{proof}
Start with the classifier definition. It requires one comparison for each of $R_0,\ldots,R_{12}$, then requires the displayed transport and replay routes to match the same component list.

The provenance and local naming rows are part of the same carrier, so their comparison is not an external bucket judgment. Since the classifier has no clause for bucket membership alone, host equality, or hidden chapter isomorphism, the displayed componentwise route is the exact comparison surface.
\end{proof}

\begin{theorem}[BHistArity13TupleNameCert tag exhaustion]
\label{thm:b-hist-arity13-tuple-name-cert-tag-exhaustion}
For an accepted $\BHistArity13TupleNameCertUp$ packet, the tags carried by $R_0,\ldots,R_{12}$ exhaust the carrier rows available to the local $\NameCert$ interface. Any consumer may transport or replay these rows only through the displayed $H$ and $C$ rows, and cannot manufacture an unlisted tag, collapse two tags into one bucket witness, import a carrier row from a sibling capstone, or read a fourteenth carrier component.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:b-hist-arity13-tuple-name-cert-carrier}. The carrier has exactly the thirteen tag-encoded $\BHist$ rows together with $H,C,P,L$, and \autoref{thm:b-hist-arity13-tuple-name-cert-carrier-admission} identifies this displayed list as the whole accepted row inventory.

The transport and replay access paths are therefore already fixed by $H$ and $C$. No consumer can introduce another tag or merge two tags into a single bucket witness, because such a move would require an additional carrier row or a non-displayed transport clause.

Finally apply \autoref{thm:b-hist-arity13-tuple-name-cert-classifier-exactness}. Every public comparison remains componentwise over the same thirteen tags and the same displayed structural rows, so sibling capstones cannot contribute hidden carrier rows and no fourteenth component is readable through the local $\NameCert$ surface.
\end{proof}

\begin{theorem}[BHistArity13TupleNameCert scoped support package]
\label{thm:b-hist-arity13-tuple-name-cert-scoped-support-package}
The carrier admission and classifier exactness clauses bind the $\BHistArity13TupleNameCertUp$ packet to the displayed BEDC support
$$
\BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert,
$$
to the tuple capstones from arity eight through arity twelve, and to the representative bucket members $\AuditMapRouteCompilerUp$, $\AnalogyCertificateGateUp$, $\AxisCarryDiamondRouteUp$, $\CompressionLedgerFunctorUp$, $\FiniteObservationRefutationUp$, and $\FocusedMultiHistLedgerUp$. The resulting scope is the arity-thirteen tag-encoded $\BHist$ packet with displayed $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row; it is not a bucket-wide sibling rewrite, a host-side equality classifier, or a hidden fourteenth row.
\end{theorem}
\begin{proof}
Apply \autoref{thm:b-hist-arity13-tuple-name-cert-carrier-admission} to identify the admitted rows and exclude any fourteenth coordinate. Apply \autoref{thm:b-hist-arity13-tuple-name-cert-classifier-exactness} to keep comparison on the thirteen tagged $\BHist$ rows and the displayed structural rows.

Apply \autoref{thm:b-hist-arity13-tuple-name-cert-tag-exhaustion} to rule out unlisted tags, row fusion, sibling-carrier imports, and hidden components at the local $\NameCert$ interface.

The support package is therefore the named BEDC packet itself. Its provenance can source the cited tuple capstones and representative bucket members, but no carrier or classifier row rewrites those sibling chapters or replaces their local certificates.
\end{proof}

\falsifiablePrediction{Any accepted consumer route that reads an untagged row, a fourteenth carrier row, bucket membership alone, a provenance table detached from $P$, or a sibling-capstone rewrite fails carrier admission or classifier exactness for $\BHistArity13TupleNameCertUp$.}

\independenceWitness{b-hist-octa-tuple-name-cert, b-hist-nona-tuple-name-cert, b-hist-deca-tuple-name-cert, b-hist-hendeca-tuple-name-cert, and b-hist-dodeca-tuple-name-cert: none is bijective to $\BHistArity13TupleNameCertUp$ because this packet carries thirteen tagged rows together with transport, replay, provenance, and local $\NameCert$ in one packet, while the neighboring capstones carry different arities and each sibling supplies only a member-family surface.}

\closureat{BHistArity13TupleNameCertUp}{seedStr}

\begin{closurestatus}{\BHistArity13TupleNameCertUp}
  \constructivestory{A $\BHistArity13TupleNameCertUp$ packet is generated from thirteen tag-encoded $\BHist$ carrier rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, package provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:b-hist-arity13-tuple-name-cert-carrier}, \autoref{def:b-hist-arity13-tuple-name-cert-classifier}, \autoref{thm:b-hist-arity13-tuple-name-cert-carrier-admission}, \autoref{thm:b-hist-arity13-tuple-name-cert-classifier-exactness}, \autoref{thm:b-hist-arity13-tuple-name-cert-tag-exhaustion}, and \autoref{thm:b-hist-arity13-tuple-name-cert-scoped-support-package} identify the arity-thirteen carrier-isomorphism surface and its BEDC support package.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai-composite}
  \notclaimed{This chapter does not provide a Lean declaration, standard bridge, member-chapter replacement, or public acceptance interface.}
  \upgradepath{Formalization requires a Lean carrier for BEDC.Derived.BHistArity13TupleNameCertUp and checked carrier-admission plus classifier-exactness theorems.}
\end{closurestatus}
