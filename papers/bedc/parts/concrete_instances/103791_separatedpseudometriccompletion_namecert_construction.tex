\chapter{A Concrete Naming Certificate for $\SeparatedPseudometricCompletionUp$}
\label{ch:concrete-instances-separatedpseudometriccompletion-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis and the Bourbaki--Kelley separated-reflection route for uniform and metric completions, the $\SeparatedPseudometricCompletionUp$ packet records the traditional passage from pre-separated pseudometric evidence to a separated metric completion as finite NameCert data. It starts from the pre-separated distance surface of \autoref{ch:concrete-instances-pseudometric-namecert}, routes zero-distance evidence through the separated classifier of \autoref{ch:concrete-instances-separated-quotient-metric-namecert}, and hands the resulting separated metric surface to the completion boundary of \autoref{ch:concrete-instances-metriccompletion-namecert}. Its numerical readback uses the finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational route of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the L10 completion bridge without quotient primitives, host equality, selected representatives, selected limits, countable choice, trichotomy, open-cover compactness, or ambient completeness of $\RealUp$.

\begin{definition}[SeparatedPseudometricCompletion carrier]
\label{def:separated-pseudometric-completion-carrier}
A $\SeparatedPseudometricCompletionUp$ carrier is a finite $\BHist$ packet
$$
A=(P,Z,Q,M,S,R,D,E,H,C,K,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $P$ is the source $\PseudometricUp$ packet. It supplies point admission, displayed distance witnesses, dyadic tolerance rows, stream windows, regular rational readback, a real-distance seal, and zero-distance boundary data before separation.
\item $Z$ is the displayed zero-distance row. It records the finite evidence consumed by the separated classifier and does not identify point rows by host equality.
\item $Q$ is the $\SeparatedQuotientMetricUp$ classifier output. It is the only row that turns the zero-distance evidence in $Z$ into separated metric point-classifier evidence.
\item $M$ is the $\MetricCompletionUp$ completion handoff. It consumes the separated metric surface exposed by $Q$ and supplies the completion-facing route without adding quotient completion equality.
\item $S$ is the $\StreamNameUp$ finite-window row opened before any regular rational or real-distance readback is inspected.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the windows in $S$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance and comparison ledger used by the source pseudometric and by the completion-facing readback.
\item $E$ is the terminal $\RealUp$ seal reached only after $S,R,D$ and the separated classifier route have been displayed.
\item $H$ records componentwise $\hsame$ transport for the pseudometric, zero-distance, separated classifier, completion handoff, finite-window, regular rational, dyadic, real-seal, continuation, package, and naming rows.
\item $C$ records displayed $\Cont$ replay from a completion request through $P,Z,Q,M,S,R,D,E,H,K,N$ in that order.
\item $K$ is the $\Pkg$ provenance over $\PseudometricUp$, $\SeparatedQuotientMetricUp$, $\MetricCompletionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SeparatedPseudometricCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these displayed rows. It has no coordinate for quotient primitives, host equality, selected representatives, selected limits, countable choice, trichotomy, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}
\leanchecked{BEDC.Derived.SeparatedPseudometricCompletionUp.SeparatedPseudometricCompletionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[SeparatedPseudometricCompletion NameCert obligations]
\label{thm:separated-pseudometric-completion-namecert-obligations}
For every accepted $\SeparatedPseudometricCompletionUp$ carrier $A=(P,Z,Q,M,S,R,D,E,H,C,K,N)$, the local $\NameCert_{\SeparatedPseudometricCompletionUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the source $\PseudometricUp$ packet, zero-distance row, $\SeparatedQuotientMetricUp$ classifier output, $\MetricCompletionUp$ handoff, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\RealUp$ seal, componentwise transport, continuation replay, provenance, and local naming rows;
\item zero-distance separation, requiring every point-identity read from $Z$ to pass through the separated classifier output $Q$ before a metric or completion consumer may use it;
\item completion handoff, requiring $M$ to consume the separated metric surface from $Q$ rather than the pre-separated pseudometric row $P$ directly;
\item numerical readback ordering, requiring $S,R,D,E$ to be displayed in order before any completion-facing real-distance seal is exported;
\item transport and replay stability, requiring $H$ and $C$ to preserve the same route under componentwise $\hsame$ transport and displayed $\Cont$ replay;
\item provenance exactness, requiring $K$ and $N$ to name exactly the displayed BEDC dependencies and the local certificate row;
\item non-escape, excluding quotient primitives, host equality, selected representatives, selected limits, countable choice, trichotomy, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, and bridge checking.
\end{enumerate}
No separated pseudometric completion NameCert obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:separated-pseudometric-completion-carrier}. The tuple lists the source pseudometric packet, zero-distance evidence, separated classifier output, completion handoff, finite windows, regular rational readback, dyadic ledger, real seal, transport, replay, provenance, and local naming rows.

Zero-distance separation follows from the order $P,Z,Q$. The source pseudometric row exposes zero-distance evidence, the row $Z$ records the displayed evidence, and the row $Q$ is the first coordinate that can export separated point-classifier evidence.

The completion handoff is then forced through $M$. Since $M$ is downstream of $Q$, a completion-facing consumer cannot read point identity directly from the pre-separated pseudometric row.

Numerical readback is the displayed route $S,R,D,E$. Finite windows are opened first, regular rational data and dyadic comparisons are read next, and the terminal real seal is reached only after those rows have been displayed.

Transport and replay stability are supplied by $H$ and $C$. They move the listed rows componentwise and replay the same finite route for consumers, so the certificate remains inside the displayed dependency surface.

The provenance and non-escape clauses are inventory checks on $K,N$ and on the fields of $A$. None of the excluded principles occurs among the carrier coordinates, hence none can be part of the local NameCert surface.
\end{proof}
\leanchecked{BEDC.Derived.SeparatedPseudometricCompletionUp.SeparatedPseudometricCompletionCarrier\_namecert\_obligations}

\begin{theorem}[SeparatedPseudometricCompletion completion route]
\label{thm:separated-pseudometric-completion-completion-route}
Let $A=(P,Z,Q,M,S,R,D,E,H,C,K,N)$ be an accepted $\SeparatedPseudometricCompletionUp$ packet. Every completion-facing consumer factors through the finite route
$$
\PseudometricUp\to Z\to\SeparatedQuotientMetricUp\to\MetricCompletionUp\to\StreamNameUp\to\RegSeqRatUp\to\DyadicRatCoreUp\to\RealUp .
$$
Thus the consumer receives a separated metric completion handoff only after the zero-distance evidence has passed through the separated classifier and the numerical readback rows have been displayed. The packet exports no direct route from pre-separated pseudometric evidence to completion equality, host equality, selected representatives, selected limits, countable choice, trichotomy, open-cover compactness, or ambient completeness of $\RealUp$.
\end{theorem}
\begin{proof}
Start from an accepted packet $A$. The source row $P$ supplies pre-separated distance evidence, and the row $Z$ records the zero-distance boundary as displayed data.

The replay row $C$ sends that evidence through $Q$ before the completion handoff $M$ can be read. This makes the separated classifier output the only route from zero distance to the metric point surface consumed by completion.

After $M$, a completion-facing numerical request reads the windows $S$, the regular rational row $R$, the dyadic ledger $D$, and finally the real seal $E$. The transport row $H$ and the provenance rows $K,N$ name the same displayed chain.

The excluded objects are absent from the carrier. There is no quotient primitive, host equality field, selected representative, selected limit, countable-choice schedule, trichotomy row, open-cover compactness row, or ambient $\RealUp$ completeness row, so no consumer can obtain one from the packet.
\end{proof}

\begin{theorem}[SeparatedPseudometricCompletion obligation-closure package]
\label{thm:separated-pseudometric-completion-obligation-closure-package}
For every accepted $\SeparatedPseudometricCompletionUp$ carrier
$$
A=(P,Z,Q,M,S,R,D,E,H,C,K,N),
$$
the carrier admission, zero-distance separation, completion handoff, numerical readback ordering, transport and replay stability, provenance exactness, and non-escape rows assemble the full $\obligationClosure$ surface over $\PseudometricUp$, $\SeparatedQuotientMetricUp$, $\MetricCompletionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The package records the L10 separated-completion route without quotient primitives, host equality, selected representatives, selected limits, countable choice, trichotomy, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{theorem}
\begin{proof}
Start from the carrier and NameCert obligation theorem. The tuple $A$ exposes the source pseudometric packet, zero-distance row, separated classifier output, completion handoff, finite windows, regular rational readback, dyadic ledger, Real seal, transport, replay, provenance, and local naming rows; \autoref{thm:separated-pseudometric-completion-namecert-obligations} identifies the exact obligations attached to that finite list.

Next use the completion route theorem. By \autoref{thm:separated-pseudometric-completion-completion-route}, every completion-facing consumer must pass from $P$ through $Z,Q,M$ and then through $S,R,D,E$ before reading the structural rows.

Transport, replay, and provenance do not add a second route. The rows $H,C,K,N$ preserve and name the same displayed packet, so zero-distance separation, completion handoff, numerical readback, and non-escape remain carrier-local.

Since the carrier has no coordinate for any excluded quotient, choice, equality, compactness, completeness, verification, or bridge endpoint, the displayed rows exhaust the obligation-closure package.
\end{proof}

\begin{theorem}[SeparatedPseudometricCompletion scoped consumer route]
\label{thm:separated-pseudometric-completion-scope}
Every accepted $\SeparatedPseudometricCompletionUp$ packet
$$
A=(P,Z,Q,M,S,R,D,E,H,C,K,N)
$$
that is consumed by an L10 completion reader must expose $P,Z,Q,M,S,R,D,E,H,C,K,N$ in that order. The row $P$ is anchored in $\PseudometricUp$; $Z,Q$ are anchored in separated zero-distance classification; $M$ is anchored in $\MetricCompletionUp$; $S,R,D,E$ are anchored in $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$; and $H,C,K,N$ are anchored in $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The route exports no quotient primitive, host equality, selected representative, selected limit, countable choice, trichotomy, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:separated-pseudometric-completion-namecert-obligations}. They identify the accepted carrier rows as the source pseudometric packet, zero-distance row, separated classifier output, completion handoff, finite windows, regular rational readback, dyadic ledger, Real seal, transport, replay, provenance, and local name.

Next apply the completion-route theorem. By \autoref{thm:separated-pseudometric-completion-completion-route}, an L10 completion reader cannot pass directly from the source pseudometric row to completion equality. It must read $P$, then the zero-distance evidence $Z$, then the separated classifier output $Q$, and only then the completion handoff $M$.

The numerical readback is the ordered block $S,R,D,E$. The finite-window row is exposed before regular rational readback, the dyadic ledger controls the tolerance comparison, and the $\RealUp$ seal is reached only after those rows have been displayed.

Finally use the obligation-closure package of \autoref{thm:separated-pseudometric-completion-obligation-closure-package}. The rows $H,C,K,N$ transport, replay, source, and name the same finite route, so they do not create an additional completion path.

The refusal clauses are carrier-local. None of the excluded quotient, equality, selection, choice, order, compactness, completeness, verification, or bridge objects occurs among $P,Z,Q,M,S,R,D,E,H,C,K,N$, hence none can be exported by an accepted L10 completion consumer.
\end{proof}

\closureat{SeparatedPseudometricCompletionUp}{obligationStr}
\closureat{SeparatedPseudometricCompletionUp}{scopedStr}[thm:separated-pseudometric-completion-scope]

\begin{closurestatus}{\SeparatedPseudometricCompletionUp}
  \constructivestory{A $\SeparatedPseudometricCompletionUp$ packet is generated from a source $\PseudometricUp$ packet, displayed zero-distance evidence, a $\SeparatedQuotientMetricUp$ classifier output, a $\MetricCompletionUp$ handoff, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, a $\DyadicRatCoreUp$ ledger, a $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{thm:separated-pseudometric-completion-scope} binds the scoped L10 consumer route over $P,Z,Q,M,S,R,D,E,H,C,K,N$ from pre-separated pseudometric evidence through separated classification into metric completion.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient primitives, host equality, selected representatives, selected limits, countable choice, trichotomy, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Public closure requires an exported separated-completion NameCert theorem for downstream L10 completion consumers.}
\end{closurestatus}
