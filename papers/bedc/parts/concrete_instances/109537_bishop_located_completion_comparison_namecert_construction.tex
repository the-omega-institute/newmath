\chapter{A Concrete Naming Certificate for $\BishopLocatedCompletionComparisonUp$}
\label{ch:concrete-instances-bishop-located-completion-comparison-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopLocatedCompletionComparisonUp$ packet records the finite comparison route between located metric completion, Bishop metric completion, constructive metric completion, and the general metric-completion comparison surface. It sits after \autoref{ch:concrete-instances-located-metric-completion-namecert}, \autoref{ch:concrete-instances-bishop-metric-completion-namecert}, \autoref{ch:concrete-instances-constructive-metric-completion-namecert}, \autoref{ch:concrete-instances-metric-completion-comparison-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The chapter names one located completion route through displayed finite rows; it does not assert quotient completion equality, selected representatives, countable choice, host equality of completion classes, or ambient completeness of $\RealUp$.

\begin{definition}[Bishop located-completion comparison carrier]
\label{def:bishop-located-completion-comparison-carrier}
A $\BishopLocatedCompletionComparisonUp$ carrier is a finite $\BHist$ packet
$$
K=(L,B,Q,M,R,S,D,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the $\LocatedMetricCompletionUp$ route, carrying the located source and the displayed completion-facing windows.
\item $B$ is the $\BishopMetricCompletionUp$ route, carrying the Bishop regular Cauchy presentation of the same located source.
\item $Q$ is the $\ConstructiveMetricCompletionUp$ route, carrying the constructive metric completion packet over located metrics, regular Cauchy names, dyadic tolerances, and separated comparison.
\item $M$ is the $\MetricCompletionComparisonUp$ row comparing the visible completion presentations through paired dense embeddings, a common dense source, and finite round-trip reads.
\item $R$ is the $\RegSeqRatUp$ readback row shared by the Bishop and constructive routes.
\item $S$ is the $\StreamNameUp$ finite-window row inspected before any regular rational readback or comparison map is named.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used to compare the located, Bishop, constructive, and metric-comparison windows at finite precision.
\item $A$ is the terminal $\RealUp$ seal row reached only after $L,B,Q,M,R,S,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the four completion routes, readback, window, tolerance, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a located completion request through the Bishop and constructive presentations, the metric-comparison row, the shared readback rows, and then the Real seal.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedCompletionComparisonUp$, the eight sibling chapters named above, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopLocatedCompletionComparisonUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient completions, selected representatives, countable choice, host equality of completion classes, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[BishopLocatedCompletionComparison NameCert obligations]
\label{thm:bishop-located-completion-comparison-namecert-obligations}
For every accepted $\BishopLocatedCompletionComparisonUp$ carrier $K=(L,B,Q,M,R,S,D,A,H,C,P,N)$, the local $\NameCert_{\BishopLocatedCompletionComparisonUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the located completion route, Bishop metric completion route, constructive metric completion route, metric-completion comparison row, regular rational readback, stream windows, dyadic tolerance ledger, Real seal, transport, replay, provenance, and local naming row;
\item located route comparison, requiring $L,B,Q,M$ to share the same displayed located source before any completion-facing comparison is exported;
\item $\RegSeqRatUp$ and $\StreamNameUp$ readback compatibility, requiring $S$ to be read before $R$ and requiring both rows to be replayed through $C$ before the comparison row $M$ reaches the seal;
\item Real seal handoff, requiring $A$ to remain downstream of $L,B,Q,M,R,S,D$;
\item ledger refusal for quotient completions, selected representatives, countable choice, host equality of completion classes, and ambient completeness of $\RealUp$.
\end{enumerate}
No obligation is stored outside these finite rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-located-completion-comparison-carrier}. The projection exposes exactly the located, Bishop, constructive, and metric-comparison routes, together with the shared regular readback, stream-window, dyadic-tolerance, Real-seal, transport, replay, provenance, and naming rows.

Use the located route as the common source. The rows $L,B,Q,M$ can be compared only after the located metric completion route has been displayed, so the Bishop and constructive presentations are read as finite routes over the same located completion request.

Next read the shared observation rows. The stream row $S$ supplies the inspected finite windows, $R$ reads them as regular rational data, and $D$ records the tolerance ledger used by the comparison row. Replay by $C$ preserves this order before the seal row $A$ can be consumed.

Finally attach $H,P,N$. Componentwise transport, provenance, and local naming preserve the same finite route and add no coordinate for quotient completions, selected representatives, countable choice, host equality, or ambient Real completeness. Hence the listed rows exhaust the NameCert obligations.
\end{proof}

\falsifiablePrediction{A $\BishopLocatedCompletionComparisonUp$ consumer that reaches a comparison map or $\RealUp$ seal while omitting the located completion row, Bishop completion row, constructive completion row, metric-comparison row, RegSeqRat readback, StreamName windows, DyadicRatCore tolerance ledger, componentwise hsame transport, Cont replay, Pkg provenance, or local NameCert row refutes the packet.}

\independenceWitness{located-metric-completion, bishop-metric-completion, constructive-metric-completion, metric-completion-comparison, regseqrat, streamname, dyadicratcore, real: no listed sibling supplies the whole comparison packet because each contributes only a completion presentation, comparison surface, readback row, window row, tolerance ledger, or seal, while this chapter binds them into one located completion route.}

\closureat{BishopLocatedCompletionComparisonUp}{seedStr}

\begin{closurestatus}{\BishopLocatedCompletionComparisonUp}
  \constructivestory{A $\BishopLocatedCompletionComparisonUp$ packet is generated from LocatedMetricCompletion, BishopMetricCompletion, ConstructiveMetricCompletion, MetricCompletionComparison, RegSeqRat, StreamName, DyadicRatCore, Real, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-located-completion-comparison-carrier} and \autoref{thm:bishop-located-completion-comparison-namecert-obligations} seed the finite located completion comparison route over Bishop, constructive, metric-comparison, readback, tolerance, and Real-seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient completion equality, selected representatives, countable choice, host equality of completion classes, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located route comparison, RegSeqRat and StreamName readback compatibility, Real seal handoff, transport replay, provenance non-escape, and ledger refusals for BEDC.Derived.BishopLocatedCompletionComparisonUp.}
\end{closurestatus}
