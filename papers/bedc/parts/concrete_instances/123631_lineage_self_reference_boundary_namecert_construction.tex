\chapter{A Concrete Naming Certificate for $\LineageSelfReferenceBoundaryUp$}
\label{ch:concrete-instances-lineage-self-reference-boundary-namecert}
\origin{ai}

The $\LineageSelfReferenceBoundaryUp$ packet realizes the lineage and self-reference boundary of \autoref{ch:visions-innovation-input-lineage-self-reference-boundary} as finite NameCert data. It sits beside the innovation-input geometry boundary of \autoref{ch:visions-innovation-input-geometry-boundary}, the theorem-DNA complexity-scoring packet of \autoref{ch:concrete-instances-theorem-dna-complexity-scoring-namecert}, the discovery-pipeline calculus of \autoref{ch:concrete-instances-discovery-pipeline-calculus-namecert}, and the GroundCompiler audit packet of \autoref{ch:concrete-instances-groundcompiler-audit-packet-namecert}. The packet names the refinement-row lineage, binary self-substitution compatibility, circular-correlation orientation, boundary refusal, transport, provenance, and local naming rows without identifying the boundary with a quantum cellular automaton, a reversible Markov input, a theorem-DNA oracle, or an unledgered discovery claim.

\begin{definition}[Lineage self-reference boundary carrier]
\label{def:lineage-self-reference-boundary-carrier}
A $\LineageSelfReferenceBoundaryUp$ carrier is a finite $\BHist$ packet
$$
L=(R,B,E,Q,O,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the refinement-walk lineage row, recording the quantum-walk, Haar scaling, tensor-network skeleton, and one-particle QCA-shadow neighbours only as lineage pointers.
\item $B$ is the binary self-substitution row with the displayed compatibility equation $a+b=\sqrt2$.
\item $E$ is the equation ledger that records the phase-polygon compatibility row and the residual-rate handoff.
\item $Q$ is the circular-correlation row that records the oriented value of the one-step correlation.
\item $O$ is the time-arrow boundary row, separating binary oriented compatibility from reversible ternary or square-alphabet witnesses.
\item $T$ is the TasteGate obligation row for the finite boundary packet.
\item $H$ records componentwise $\hsame$ transport for lineage, substitution, equation, correlation, boundary, TasteGate, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from refinement lineage through binary compatibility and circular correlation to the boundary refusal.
\item $P$ records $\Pkg$ provenance over \autoref{ch:visions-innovation-input-lineage-self-reference-boundary}, \autoref{ch:visions-innovation-input-geometry-boundary}, \autoref{ch:concrete-instances-theorem-dna-complexity-scoring-namecert}, \autoref{ch:concrete-instances-discovery-pipeline-calculus-namecert}, \autoref{ch:concrete-instances-groundcompiler-audit-packet-namecert}, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\LineageSelfReferenceBoundaryUp}$ row.
\end{enumerate}
The Lean-side \texttt{TasteGate.lean} target for $\LineageSelfReferenceBoundaryUp$ will provide explicit $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances for this ten-row carrier surface.
\end{definition}

\begin{theorem}[Lineage self-reference boundary NameCert obligations]
\label{thm:lineage-self-reference-boundary-namecert-obligations}
For every accepted $\LineageSelfReferenceBoundaryUp$ carrier $L=(R,B,E,Q,O,T,H,C,P,N)$, the local $\NameCert_{\LineageSelfReferenceBoundaryUp}$ surface consists exactly of refinement-row lineage exposure, binary self-substitution exposure, compatibility-equation exposure, circular-correlation orientation, time-arrow boundary refusal, TasteGate obligation exposure, componentwise transport, displayed replay, provenance, and local naming. No consumer may read the packet as a QCA construction, a reversible binary self-reference, a theorem-DNA score, a discovery-pipeline admission, or a GroundCompiler audit verdict without the matching displayed row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:lineage-self-reference-boundary-carrier}. The rows $R,B,E,Q,O,T,H,C,P,N$ are the whole visible packet.

The lineage row $R$ records neighbouring descriptions without replacing the refinement walk. The binary row $B$ and equation row $E$ then expose the compatibility equation before any self-reference read can inspect the circular-correlation row $Q$.

The boundary row $O$ is read after that oriented correlation, so the refusal is a displayed consequence of the packet surface rather than a hidden time-arrow assertion. The TasteGate row $T$ keeps the carrier-facing obligation separate from theorem-DNA scoring, discovery-pipeline admission, and GroundCompiler audit verdicts.

Finally $H,C,P,N$ transport, replay, source, and name the same finite rows. Since the carrier has no coordinate for the excluded neighbour objects, the listed rows exhaust the local certificate surface.
\end{proof}

\falsifiablePrediction{Every accepted $\LineageSelfReferenceBoundaryUp$ consumer that reads the self-reference boundary must expose refinement-row lineage, binary compatibility, phase-equation, circular-correlation orientation, boundary refusal, TasteGate obligation, transport, replay, provenance, and local naming rows.}

\independenceWitness{\texttt{innovation\_input\_geometry\_boundary}, \texttt{theorem\_dna\_complexity\_scoring}, \texttt{discovery\_pipeline\_calculus}, \texttt{groundcompiler\_audit\_packet}: none of these neighbours carries the full ten-row packet $L=(R,B,E,Q,O,T,H,C,P,N)$, because the geometry boundary supplies the input-flow geometry, theorem-DNA scoring supplies a scoring gate, discovery-pipeline calculus supplies admission and classifier-shift rows, and the GroundCompiler audit packet supplies compiler-facing audit rows, while this chapter binds lineage, self-substitution, correlation, and boundary refusal as one finite NameCert surface.}

\closureat{LineageSelfReferenceBoundaryUp}{seedStr}

\begin{closurestatus}{\LineageSelfReferenceBoundaryUp}
  \constructivestory{A $\LineageSelfReferenceBoundaryUp$ packet is generated from refinement-lineage rows, binary self-substitution rows, compatibility-equation rows, circular-correlation rows, time-arrow boundary rows, TasteGate obligation rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:lineage-self-reference-boundary-carrier} and \autoref{thm:lineage-self-reference-boundary-namecert-obligations} seed the finite lineage self-reference boundary over innovation-input, theorem-DNA, discovery-pipeline, GroundCompiler audit, TasteGate, transport, replay, provenance, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a QCA construction, reversible binary self-reference, theorem-DNA scoring, discovery-pipeline admission, GroundCompiler audit verdict, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, lineage exposure, binary compatibility exposure, correlation orientation, boundary refusal, TasteGate instance rows, transport, replay, provenance, and local naming for BEDC.Derived.LineageSelfReferenceBoundaryUp.}
\end{closurestatus}
