\chapter{A Concrete Naming Certificate for $\RegularCauchyCompletionPrincipleUp$}
\label{ch:concrete-instances-regular-cauchy-completion-principle-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy completions, the $\RegularCauchyCompletionPrincipleUp$ packet records the textbook completion theorem surface between the rational regular-sequence carrier and the public real seal. It reads the dyadic accounting of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-name route of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational sequence carrier of \autoref{ch:concrete-instances-regseqrat-namecert}, the Cauchy comparison discipline of \autoref{ch:concrete-instances-cauchyspace-namecert}, and the terminal seal of \autoref{ch:concrete-instances-real-namecert}. The packet names completion as a finite handoff principle, not as primitive completeness of $\RealUp$, selected limits, quotient representatives, countable choice, or host equality of real names.

\begin{definition}[RegularCauchyCompletionPrinciple carrier]
\label{def:regular-cauchy-completion-principle-carrier}
A $\RegularCauchyCompletionPrincipleUp$ carrier is a finite $\BHist$ packet
$$
K=(D,S,Q,C,R,E,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger that controls every approximation window used by the packet.
\item $S$ is the $\StreamNameUp$ finite stream-name row exposing the observable prefixes on which completion requests are judged.
\item $Q$ is the $\RegSeqRatUp$ regular rational sequence row whose modulus is read through $D$ and $S$.
\item $C$ is the $\CauchySpaceUp$ comparison row, carrying the Cauchy relation and located comparison discipline used before any real seal is inspected.
\item $R$ is the $\RealUp$ seal row reached only after the dyadic, stream, regular-sequence, and Cauchy-space rows have been displayed.
\item $E$ is the embedding handoff row that presents a regular rational sequence as a real-facing name without identifying it with a host real object.
\item $H$ records componentwise $\hsame$ transport for the dyadic ledger, stream prefixes, regular sequence row, Cauchy comparison, real seal, embedding handoff, replay, provenance, and local naming rows.
\item $T$ records displayed $\Cont$ replay from regular Cauchy data through the dyadic tolerance ledger, stream-name windows, Cauchy comparison row, embedding handoff, and real seal.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyCompletionPrincipleUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\CauchySpaceUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyCompletionPrincipleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for primitive $\RealUp$ completeness, selected limits, quotient classes of Cauchy sequences, countable-choice schedules, or host equality of real names.
\end{definition}

\begin{theorem}[RegularCauchyCompletionPrinciple NameCert obligations]
\label{thm:regular-cauchy-completion-principle-namecert-obligations}
For every accepted $\RegularCauchyCompletionPrincipleUp$ carrier $K=(D,S,Q,C,R,E,H,T,P,N)$, the local $\NameCert_{\RegularCauchyCompletionPrincipleUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the dyadic ledger, stream-name row, regular rational sequence row, Cauchy comparison row, real seal, embedding handoff, transport, replay, provenance, and local naming;
\item regular-sequence readiness, requiring $D$ and $S$ to be displayed before the $\RegSeqRatUp$ row $Q$ can be consumed;
\item Cauchy comparison exposure, requiring the $\CauchySpaceUp$ row $C$ to mediate all completion-facing comparisons before the terminal $\RealUp$ seal $R$ is inspected;
\item embedding handoff, requiring the row $E$ to present the regular rational sequence to real-facing consumers without adding selected limits or quotient representatives;
\item classifier transport, requiring every accepted comparison to use only the componentwise transport $H$, replay $T$, provenance $P$, and local name $N$;
\item ledger refusal, excluding primitive $\RealUp$ completeness, host equality of real names, countable choice, selected limits, and quotient completion representatives.
\end{enumerate}
No completion-principle obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:regular-cauchy-completion-principle-carrier}. The accepted tuple displays exactly the dyadic ledger, stream-name row, regular rational sequence, Cauchy comparison, real seal, embedding handoff, and structural rows.

Regular-sequence readiness is read in order. The dyadic row $D$ supplies the tolerance scale, the stream row $S$ supplies the finite observable prefixes, and the regular rational sequence row $Q$ is admitted only through those two rows.

The completion-facing comparison is then forced through $C$. Since $R$ is downstream of $D,S,Q,C$, an accepted packet cannot read the real seal before the Cauchy comparison discipline has been displayed.

Finally inspect the embedding handoff and the structural rows. The row $E$ is the only bridge from regular rational sequence data to real-facing consumption, while $H,T,P,N$ transport, replay, source, and name that same route. The refused objects have no coordinate in the carrier, so they cannot become local NameCert obligations.
\end{proof}

\begin{theorem}[RegularCauchyCompletionPrinciple embedding handoff]
\label{thm:regular-cauchy-completion-principle-embedding-handoff}
Every $\RealUp$ consumer of an accepted $\RegularCauchyCompletionPrincipleUp$ packet reads the regular Cauchy completion route through
$$
D,S \longmapsto Q \longmapsto C \longmapsto E,R \longmapsto H,T,P,N .
$$
The route starts with dyadic tolerances and stream-name windows, passes through the regular rational sequence and Cauchy comparison rows, and reaches the embedding handoff and terminal real seal only afterward. No primitive completeness theorem for $\RealUp$, selected limit, quotient representative, countable-choice schedule, or host equality of real names bypasses this route.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:regular-cauchy-completion-principle-carrier}. The rows $D$ and $S$ are the only tolerance and window rows, and $Q$ is the only regular rational sequence row attached to them.

Read the comparison row next. The completion request reaches $C$ before it can reach the embedding row $E$ or the real seal $R$, so every real-facing consumer inherits the displayed Cauchy comparison discipline.

The structural rows preserve the same dependency order. Componentwise transport $H$, replay $T$, provenance $P$, and local naming $N$ package the route without adding any unlisted completion object. Hence every accepted real-facing read factors through the displayed handoff.
\end{proof}

\closureat{RegularCauchyCompletionPrincipleUp}{seedStr}

\begin{closurestatus}{\RegularCauchyCompletionPrincipleUp}
  \constructivestory{A $\RegularCauchyCompletionPrincipleUp$ packet is generated from DyadicRatCore tolerance accounting, StreamName windows, a RegSeqRat regular rational sequence row, CauchySpace comparison, a Real seal, an embedding handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-completion-principle-carrier}, \autoref{thm:regular-cauchy-completion-principle-namecert-obligations}, and \autoref{thm:regular-cauchy-completion-principle-embedding-handoff} seed the textbook completion-principle surface between RegSeqRat and Real.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close primitive Real completeness, selected limits, quotient completions, countable choice, host equality of real names, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, regular-sequence readiness, Cauchy comparison exposure, embedding handoff, classifier transport, and ledger refusal for BEDC.Derived.RegularCauchyCompletionPrincipleUp.}
\end{closurestatus}
