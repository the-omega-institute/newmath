\chapter{A Concrete Naming Certificate for $\DarbouxDerivativePropertyUp$}
\label{ch:concrete-instances-darboux-derivative-property-namecert}
\origin{human}

Following Darboux and Bishop--Bridges constructive analysis, the $\DarbouxDerivativePropertyUp$ packet records the finite certificate surface for the classical theorem that a displayed derivative has the Darboux property. It sits over the difference-quotient discipline of \autoref{ch:concrete-instances-derivative-namecert}, the target-value handoff of \autoref{ch:concrete-instances-darboux-property-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the derivative-Darboux route as finite NameCert data rather than as host differentiability semantics, quotient equality of functions, a selected intermediate point, trichotomy, open-cover compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Darboux derivative property carrier]
\label{def:darboux-derivative-property-carrier}
A $\DarbouxDerivativePropertyUp$ carrier is a finite $\BHist$ packet
$$
D=(I,F,Q,L,U,Y,S,R,T,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the interval source row, exposing the lower and upper endpoint reads and the finite orientation data used by derivative requests.
\item $F$ is the function-source row whose local difference-quotient reads are inspected only through the derivative packet.
\item $Q$ is the $\DerivativeUp$ quotient row, carrying the displayed nonzero-step quotient and metric-distance ledger for derivative observations.
\item $L$ and $U$ are the two derivative-value rows read at the endpoint-side observations of $I$.
\item $Y$ is the target derivative value displayed between the endpoint-side derivative rows.
\item $S$ records the finite endpoint-side comparison data that place $Y$ between $L$ and $U$.
\item $R$ is the $\DarbouxPropertyUp$ handoff row reached after the derivative quotient route has exposed the target-value bracket.
\item $T$ is the $\RegSeqRatUp$ readback row for the derivative quotient windows and requested dyadic tolerances.
\item $E$ is the $\RealUp$ seal exposed only after $Q,R,T$ have been displayed.
\item $H$ stores componentwise $\hsame$ transport for the interval source, function source, derivative quotient, endpoint derivative values, target value, comparison data, Darboux handoff, readback, real seal, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from a derivative-Darboux request through $I,F,Q,L,U,Y,S,R,T,E$.
\item $P$ is the $\Pkg$ provenance over $\DarbouxDerivativePropertyUp$, $\DerivativeUp$, $\DarbouxPropertyUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DarbouxDerivativePropertyUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,F,Q,L,U,Y,S,R,T,E,H,C,P,N$. It has no coordinate for host differentiability semantics, quotient equality of functions, selected intermediate points, real trichotomy, open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Darboux derivative property NameCert obligations]
\label{thm:darboux-derivative-property-namecert-obligations}
For every accepted $\DarbouxDerivativePropertyUp$ carrier $D=(I,F,Q,L,U,Y,S,R,T,E,H,C,P,N)$, the local $\NameCert_{\DarbouxDerivativePropertyUp}$ surface consists exactly of carrier admission through the displayed packet of \autoref{def:darboux-derivative-property-carrier}; interval-source exposure through $I$; derivative quotient exposure through $F,Q$; endpoint-side derivative value and target-value exposure through $L,U,Y,S$; DarbouxProperty handoff through $R$; regular rational and dyadic readback through $T$ and the $\DyadicRatCoreUp$ tolerance surface named by $P$; Real-seal exposure through $E$; componentwise transport through $H$; displayed continuation replay through $C$; provenance through $P$; local naming through $N$; and non-escape from host differentiability semantics, quotient equality of functions, selected intermediate points, trichotomy, open-cover compactness, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:darboux-derivative-property-carrier}. The interval row $I$, function row $F$, and derivative quotient row $Q$ expose the only derivative-source data, while $L,U,Y,S$ expose the finite endpoint-side bracket for the requested target derivative value.

The Darboux handoff is the row $R$. It can be read only after the displayed quotient route and the target-value comparison data have been exposed, so the packet separates the derivative route from the continuous-image Darboux route of \autoref{ch:concrete-instances-darboux-theorem-namecert}.

The readback rows are then $T$ and $E$. The row $T$ supplies the $\RegSeqRatUp$ readback against the $\DyadicRatCoreUp$ tolerance data named by provenance, and $E$ is the terminal $\RealUp$ seal for derivative-Darboux consumers.

Finally $H,C,P,N$ transport, replay, source, and name exactly those rows. Since the carrier has no coordinate for host differentiability semantics, quotient equality of functions, selected intermediate points, trichotomy, open-cover compactness, or ambient Real completeness, none of those objects can enter the local NameCert surface.
\end{proof}

\begin{theorem}[Darboux derivative property Darboux handoff]
\label{thm:darboux-derivative-property-darboux-handoff}
Every accepted $\DarbouxPropertyUp$ consumer reading a $\DarbouxDerivativePropertyUp$ carrier factors through the finite derivative route
$$
\DerivativeUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp,\ \RealUp,\ \DarbouxPropertyUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
In particular, the consumer first reads the interval source, function source, derivative quotient row, endpoint-side derivative values, target value, and comparison data; then the regular rational and dyadic tolerance readback; and only then the $\DarbouxPropertyUp$ handoff and terminal $\RealUp$ seal.
\end{theorem}
\begin{proof}
Start from an accepted carrier $D=(I,F,Q,L,U,Y,S,R,T,E,H,C,P,N)$. The continuation row $C$ exposes derivative-Darboux reads only through the interval source, function source, quotient row, endpoint-side values, target value, and finite comparison data before any Darboux-facing handoff is available.

The derivative route is forced by $Q,T,E$. The carrier contains no alternate route from the function source to the target-value handoff, so a consumer cannot replace the displayed quotient windows by host differentiability semantics or quotient equality of functions.

After the derivative quotient and readback rows are visible, the consumer reaches $R$ as the $\DarbouxPropertyUp$ handoff. Transport through $H$ and replay through $C$ preserve the same route, while $P,N$ only package and name it.

Thus every accepted Darboux-property read factors through the derivative quotient, regular readback, dyadic tolerance, and real-seal rows, with selected intermediate points, trichotomy, open-cover compactness, and ambient Real completeness remaining outside the packet.
\end{proof}

\closureat{DarbouxDerivativePropertyUp}{seedStr}

\begin{closurestatus}{\DarbouxDerivativePropertyUp}
  \constructivestory{A $\DarbouxDerivativePropertyUp$ packet is generated from an interval source, a function source, a Derivative quotient row, endpoint-side derivative values, target-value comparison data, a DarbouxProperty handoff, RegSeqRat readback, DyadicRatCore tolerance data, a terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:darboux-derivative-property-carrier}, \autoref{thm:darboux-derivative-property-namecert-obligations}, and \autoref{thm:darboux-derivative-property-darboux-handoff} seed the derivative-Darboux property as a finite quotient-readback route from displayed derivative observations to DarbouxProperty handoff and Real-seal exposure.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host differentiability semantics, quotient equality of functions, selected intermediate points, trichotomy, open-cover compactness, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, derivative quotient exposure, endpoint-side comparison, DarbouxProperty handoff, RegSeqRat and DyadicRatCore readback, Real-seal exposure, transport and replay stability, provenance exhaustion, and non-escape for BEDC.Derived.DarbouxDerivativePropertyUp.}
\end{closurestatus}
