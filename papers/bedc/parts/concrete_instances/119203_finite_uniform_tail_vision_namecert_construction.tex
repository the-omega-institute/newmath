\chapter{A Concrete Naming Certificate for $\FiniteUniformTailVisionUp$}
\label{ch:concrete-instances-finite-uniform-tail-vision-namecert}
\origin{ai}

The $\FiniteUniformTailVisionUp$ packet realizes the observer-expansion tail route of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It binds finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, $\UniformModulusUp$ tail control, and terminal $\RealUp$ seals over the compact finite-net surface of \autoref{ch:concrete-instances-compactmetric-namecert}. The packet is a finite uniform-tail readback carrier; it is not a hidden hyperbolic boundary object, not an open-cover compactness theorem, not a selected limit, and not an ambient real-completeness principle.

\begin{definition}[Finite uniform-tail vision carrier]
\label{def:finite-uniform-tail-vision-carrier}
A $\FiniteUniformTailVisionUp$ carrier is a finite $\BHist$ packet
$$
V=(K,W,R,U,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net source row that bounds the observer-expansion tail surface.
\item $W$ is the $\StreamNameUp$ window row storing the displayed tail observations.
\item $R$ is the $\RegSeqRatUp$ readback row reached from those windows.
\item $U$ is the $\UniformModulusUp$ row that gives one finite tail bound for the displayed compact source.
\item $E$ is the terminal $\RealUp$ seal row exposed only after $K,W,R,U$ have been replayed.
\item $H$ records componentwise $\hsame$ transport for compact source, window, readback, modulus, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an observer-tail request through $K,W,R,U,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\FiniteUniformTailVisionUp$, $\CompactMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\UniformModulusUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteUniformTailVisionUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these displayed rows. It has no coordinate for open-cover compactness, a selected tail point, a quotient stream, a host convergence predicate, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Finite uniform-tail vision readback]
\label{thm:finite-uniform-tail-vision-readback}
For every accepted $\FiniteUniformTailVisionUp$ carrier
$$
V=(K,W,R,U,E,H,C,P,N),
$$
any observer-expansion tail consumer that reaches the $\RealUp$ seal $E$ must first expose the compact finite-net row $K$, the finite $\StreamNameUp$ window row $W$, the $\RegSeqRatUp$ readback row $R$, and the $\UniformModulusUp$ tail-control row $U$. The readback is therefore a finite uniform-tail packet over displayed NameCert rows.

No hidden boundary center, selected limit, quotient stream, open-cover compactness row, choice schedule, or ambient completeness theorem may replace this route.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-uniform-tail-vision-carrier}. The only analytic rows are $K,W,R,U,E$, followed by transport, replay, provenance, and local naming rows.

Read the finite source first. The compact metric row $K$ supplies the finite-net surface, while $W$ records the visible tail observations and $R$ repacks them as regular rational readback data.

Apply the uniform-modulus row $U$. It provides one displayed tail bound for the same $K,W,R$ surface, so the terminal seal $E$ cannot be detached from the finite uniform-tail route.

Finally inspect the refusal ledger. Since the carrier contains no hidden boundary center, selected limit, quotient stream, open-cover compactness row, choice schedule, or ambient completeness coordinate, every observer-expansion consumer must use the displayed route.
\end{proof}

\falsifiablePrediction{An accepted $\FiniteUniformTailVisionUp$ consumer that reaches a $\RealUp$ seal without displaying CompactMetric finite-net data, StreamName windows, RegSeqRat readback, and UniformModulus tail control refutes the packet surface.}

\independenceWitness{streamname, regseqrat, real, uniformmodulus, compactmetric: no listed sibling is bijective to $\FiniteUniformTailVisionUp$ because only this packet binds the compact finite-net source, finite observer-tail windows, regular readback, uniform modulus, Real seal, transport, replay, provenance, and local naming as one NameCert route.}

\closureat{FiniteUniformTailVisionUp}{seedStr}

\begin{closurestatus}{\FiniteUniformTailVisionUp}
  \constructivestory{A $\FiniteUniformTailVisionUp$ packet is generated from a CompactMetric finite-net source, StreamName tail windows, RegSeqRat readback, UniformModulus tail control, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-uniform-tail-vision-carrier} and \autoref{thm:finite-uniform-tail-vision-readback} seed the finite uniform-tail readback route for the observer-expansion vision.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close hyperbolic boundary semantics, open-cover compactness, selected limits, quotient streams, host convergence, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked compact finite-net exposure, StreamName window stability, RegSeqRat readback, UniformModulus tail control, Real seal, transport, replay, provenance, and local naming for BEDC.Derived.FiniteUniformTailVisionUp.}
\end{closurestatus}
