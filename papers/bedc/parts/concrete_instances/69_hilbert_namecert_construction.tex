\chapter{A Concrete Naming Certificate for $\HilbertUp$}
\label{ch:concrete-instances-hilbert-namecert}

The Hilbert-space interface $\HilbertUp$ packages a Banach space whose norm is induced by an inner product. It depends on $\NameCert_{\InnerProductUp}$ (\autoref{ch:concrete-instances-innerproduct-namecert}) and $\NameCert_{\BanachUp}$ (\autoref{ch:concrete-instances-banach-namecert}), and is the substrate for the spectral and orthogonal-decomposition certificates of functional analysis.

\input{parts/concrete_instances/hilbert/carrier_and_certificate.tex}

\input{parts/concrete_instances/hilbert/projection_and_geometry.tex}

\input{parts/concrete_instances/hilbert/orthogonal_projection_row.tex}

\input{parts/concrete_instances/hilbert/orthogonal_residual_decomposition.tex}

\closureat{HilbertUp}{matureStr}[BEDC.Derived.HilbertUp.HilbertSingletonProjection\_semantic\_name\_certificate]
\begin{closurestatus}{\HilbertUp}
  \constructivestory{A Hilbert object in this concrete closure is the same singleton carrier $A_{\emp}$ seen simultaneously as Banach, normed, and inner-product data, with all three surfaces sharing the classifier $\rho_{\emp}$ and the same empty-history endpoint. The inner product is the constant RealUp zero row, the induced norm is the singleton $\NormUp$ norm, the Banach metric and completion rows are the singleton completion rows, and the projection map sends every carried endpoint back to $\emp$, so any non-singleton endpoint or separate closed-subspace carrier is excluded. The source is the compatible Banach-plus-inner-product package, the pattern records norm-induced metric compatibility, complete-limit stability, endpoint readback, projection existence, projection idempotence, uniqueness, orthogonal residual decomposition, and universal zero orthogonality, and the ledger keeps the Banach surface, NormUp zero row, constant inner-product row, selected projection endpoint, and residual rows aligned. The five name-certificate obligations close because $\emp$ inhabits $A_{\emp}$, $\rho_{\emp}$ supplies reflexivity, symmetry, and transitivity, and the compatibility rows transport Banach metric, norm, inner-product, limit, and projection witnesses along the same classifier. A history $h$ lies in the signature gap of a $\HilbertUp$ token $p$ when the source-domain accepts the singleton compatible carrier and projection row, the signature relation pairs it with a projected answer endpoint and evidence ledger, and token introduction names that answer as $p$; unfolded, $h$ is carried by $A_{\emp}$ and the answer is the $\rho_{\emp}$-class of $\emp$ with zero norm and zero residual inner product. The paper bridge reads the package as the one-point Hilbert space and reads back its unique point, zero norm, zero inner product, and projection endpoint through the same singleton rows. The chapter registers this singleton projection package under the name $\HilbertUp$; arbitrary Hilbert spaces, arbitrary closed-subspace projections, spectral theory, and checked bridge verification are not introduced.}
  \theoryclosure{\matureClosure}
  \scopeclosed{The Hilbert surface is closed for the singleton projection NameCert package: carrier $A_{\emp}$, classifier $\rho_{\emp}$, projection endpoint $\pi_{\emp}$, Banach singleton completion row, NormUp zero row, constant inner-product zero row, residual decomposition rows, and the one-point standard bridge of \autoref{thm:hilbert-singleton-projection-standard-bridge}.}
  \formalstatus{\auditCleanV}
  \leantarget{BEDC.Derived.HilbertUp.HilbertSingletonProjection\_semantic\_name\_certificate}
  \bridgestatus{paperBridge}
  \notclaimed{It does not close arbitrary Hilbert spaces, arbitrary closed-subspace projections, spectral theory, or a bridgeChecked verification witness.}
  \upgradepath{An independent verification-axis step may add bridgeChecked certification for the displayed one-point standard bridge, while the paper-side singleton projection package already carries the mature closure grade.}
\end{closurestatus}
