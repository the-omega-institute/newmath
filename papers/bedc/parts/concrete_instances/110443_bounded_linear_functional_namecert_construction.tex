\chapter{A Concrete Naming Certificate for $\BoundedLinearFunctionalUp$}
\label{ch:concrete-instances-bounded-linear-functional-namecert}
\origin{human}

Following Banach and Bourbaki functional analysis, the $\BoundedLinearFunctionalUp$ packet records the scalar-valued bounded-linear-operator row that duality and weak-star consumers inspect before any host dual space is available. It sits over the linear-map route of \autoref{ch:concrete-instances-linearmap-namecert}, the bounded-linear-operator surface of \autoref{ch:concrete-instances-boundedlinearoperator-namecert}, the scalar-bound neighbour \autoref{ch:concrete-instances-operator-norm-namecert}, the Banach-dual row of \autoref{ch:concrete-instances-banach-dual-namecert}, the finite weak-star window of \autoref{ch:concrete-instances-banach-alaoglu-namecert}, the scalar endpoint seal of \autoref{ch:concrete-instances-real-namecert}, and the normed source surface of \autoref{ch:concrete-instances-normedspace-namecert}. The chapter names only displayed scalar-functional data: source rows, linearity rows, scalar target readback, norm-bound evidence, transport, replay, provenance, and local naming.

\begin{definition}[Bounded linear functional carrier]
\label{def:bounded-linear-functional-carrier}
For a displayed $\NormedSpaceUp$ source row $E$ over the scalar endpoint surface of $\RealUp$, a $\BoundedLinearFunctionalUp(E)$ carrier is a finite $\BHist$ packet
$$
\Lambda=(E,L,T,K,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the source normed-space row, including its carrier endpoints, scalar action, additive row, norm row, and classifier.
\item $L$ is the $\LinearMapUp$ row from $E$ to the scalar $\RealUp$ target.
\item $T$ is the scalar-valued $\BoundedLinearOperatorUp(E,\RealUp)$ row carrying the same endpoint action as $L$.
\item $K$ is the $\RealUp$ nonnegative bound endpoint for the functional.
\item $O$ is the $\OperatorNormUp$ handoff row exposing where the bound $K$ is used.
\item $H$ records componentwise $\hsame$ transport for the source, linear-map row, bounded-operator row, scalar bound, operator-norm handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a scalar-functional request through $E,L,T,K,O,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\BoundedLinearFunctionalUp$, $\LinearMapUp$, $\BoundedLinearOperatorUp$, $\OperatorNormUp$, $\BanachDualUp$, $\BanachAlaogluUp$, $\RealUp$, $\NormedSpaceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BoundedLinearFunctionalUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through the displayed source classifier, scalar endpoint transport, bounded-operator transport, bound transport, norm-bound ledger transport, replay, provenance, and local naming rows. It has no coordinate for a host dual space, quotient equality of functions, selected representatives, Hahn--Banach separation, unrestricted weak-star compactness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Bounded linear functional NameCert obligations]
\label{thm:bounded-linear-functional-namecert-obligations}
For every accepted $\BoundedLinearFunctionalUp(E)$ carrier $\Lambda=(E,L,T,K,O,H,C,P,N)$, the local $\NameCert_{\BoundedLinearFunctionalUp}$ surface consists exactly of source normed-space admission, linear-map admission, scalar-valued bounded-operator admission, RealUp bound exactness, operator-norm ledger exactness, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, local naming, and non-escape from host dual objects. A consumer may inspect $E,L,T,K,O,H,C,P,N$, but it receives no ambient function space, quotient operator equality, selected representative, Hahn--Banach witness, unrestricted weak-star compactness theorem, or bridge data.
\end{theorem}
\begin{proof}
Project the carrier rows. The analytic coordinates are exactly the source normed-space row $E$, the linear-map row $L$, the scalar-valued bounded-operator row $T$, the RealUp bound endpoint $K$, and the operator-norm handoff $O$.

The linearity obligation is read through $\LinearMapUp$, while boundedness is read through $\BoundedLinearOperatorUp(E,\RealUp)$. The scalar target is already the displayed $\RealUp$ endpoint surface, so the functional row never becomes a host-level function.

The bound exactness is the shared route through $K$ and $O$. The operator-norm handoff records where the scalar bound is used, and componentwise $\hsame$ transport moves the source, action, bound, ledger, replay, provenance, and local naming rows together.

No remaining coordinate can manufacture a dual-space quotient, selected representative, Hahn--Banach witness, unrestricted compactness theorem, or bridge check. Hence the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Bounded linear functional operator-norm handoff]
\label{thm:bounded-linear-functional-operator-norm-handoff}
Every accepted $\BoundedLinearFunctionalUp(E)$ packet exports the finite route
$$
\LinearMapUp,\BoundedLinearOperatorUp,\RealUp,\OperatorNormUp
\longmapsto
\BoundedLinearFunctionalUp
$$
to functional-analysis consumers. The route first exposes the carried scalar-valued linear action, then the bounded-operator row, then the RealUp bound endpoint and operator-norm ledger; it does not expose a host supremum, quotient equality of functionals, selected norm-attaining vector, or hidden Banach completeness.
\end{theorem}
\begin{proof}
Begin with an accepted packet $\Lambda=(E,L,T,K,O,H,C,P,N)$. The read of $L$ supplies only the displayed linear-map action from the source row to the scalar target.

The read of $T$ then packages the same action as a scalar-valued $\BoundedLinearOperatorUp$ row. Its target row is $\RealUp$, and its bound endpoint is the displayed scalar $K$.

Finally the operator-norm handoff $O$ records the norm-bound ledger that downstream consumers may inspect. Because no host norm function, quotient functional equality, selected vector, or hidden completion row is present, the exported handoff is exactly the displayed scalar-bound route.
\end{proof}

\begin{theorem}[Bounded linear functional dual-row nonescape]
\label{thm:bounded-linear-functional-dual-row-nonescape}
Any read of a $\BanachDualUp$ or $\BanachAlaogluUp$ consumer that uses a $\BoundedLinearFunctionalUp$ row factors through the same source normed-space row, scalar-valued bounded-operator row, RealUp bound endpoint, operator-norm handoff, componentwise transport, replay, provenance, and local naming rows of \autoref{def:bounded-linear-functional-carrier}. The duality consumer receives a finite scalar-functional row, not a host dual space, a quotient function class, an unrestricted weak-star compactness theorem, or a selected cluster point.
\end{theorem}
\begin{proof}
Read the duality consumer first. A $\BanachDualUp$ request needs a scalar-functional row over a displayed source, while a $\BanachAlaogluUp$ request needs a finite family of such scalar-functional observations.

In either case the only scalar-functional packet available here is $\Lambda$. Its source row, bounded-operator row, bound endpoint, operator-norm handoff, and structural rows are projected in the order displayed by \autoref{def:bounded-linear-functional-carrier}.

The downstream dual chapters may reuse those rows, but they cannot add coordinates that are absent from the packet. Thus quotient dual objects, host function classes, unrestricted weak-star compactness, selected cluster points, and bridge checks remain outside the read.
\end{proof}

\falsifiablePrediction{Every accepted $\BoundedLinearFunctionalUp$ scalar-functional read factors through a displayed $\NormedSpaceUp$ source, a $\LinearMapUp$ action row, a scalar-valued $\BoundedLinearOperatorUp$ row, a $\RealUp$ bound endpoint, an $\OperatorNormUp$ ledger, and the local $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}

\independenceWitness{linearmap, boundedlinearoperator, operatornorm, banachdual, banachalaoglu, real, and normedspace: no listed sibling is bijective to $\BoundedLinearFunctionalUp$ because this packet stores the scalar target restriction, the dual-facing provenance row, the operator-norm handoff, transport, replay, and local naming together as one finite scalar-functional surface.}

\closureat{BoundedLinearFunctionalUp}{seedStr}

\begin{closurestatus}{\BoundedLinearFunctionalUp}
  \constructivestory{A $\BoundedLinearFunctionalUp$ packet is generated from a displayed $\NormedSpaceUp$ source, a $\LinearMapUp$ scalar action, a scalar-valued $\BoundedLinearOperatorUp$ row, a $\RealUp$ bound endpoint, an $\OperatorNormUp$ ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bounded-linear-functional-carrier}, \autoref{thm:bounded-linear-functional-namecert-obligations}, \autoref{thm:bounded-linear-functional-operator-norm-handoff}, and \autoref{thm:bounded-linear-functional-dual-row-nonescape} seed the scalar-functional row consumed by BanachDualUp and BanachAlaogluUp.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host dual spaces, quotient equality of functionals, selected representatives, Hahn--Banach separation, unrestricted weak-star compactness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked source admission, scalar linear-map admission, scalar-valued bounded-operator admission, RealUp bound exactness, operator-norm handoff exactness, transport, replay, provenance, local naming, and nonescape for BEDC.Derived.BoundedLinearFunctionalUp.}
\end{closurestatus}
