\begin{theorem}[Sorgenfrey plane lower-limit topology dependency]
\label{thm:sorgenfrey-plane-lower-limit-topology-dependency}
Every accepted $\SorgenfreyPlaneUp$ carrier consumes the lower-limit topology rows before a subspace-topology or separation-facing read can inspect the plane. In the notation of \autoref{def:sorgenfrey-plane-carrier}, the component rows $L_X,L_Y,O_X,O_Y$ are read through the lower-limit product route of \autoref{thm:sorgenfrey-plane-lower-limit-product-dependency-route}, and every restriction-facing consumer then passes through the subspace-topology sibling of \autoref{ch:concrete-instances-subspace-topology-namecert}. Thus the plane packet exposes a lower-limit topology dependency child route rather than an ambient host topology, selected rectangle basis, quotient equality on $\RealUp\times\RealUp$, metrizability theorem, or standalone normality theorem.
\end{theorem}
