The $\SorgenfreyPlaneUp$ carrier consumes the lower-limit line route of \autoref{ch:concrete-instances-sorgenfrey-line-namecert} and the product-topology classifier of \autoref{ch:concrete-instances-product-topology-namecert} before any separation, subspace, compactness, or open-cover consumer inspects the plane. In the notation of \autoref{def:sorgenfrey-plane-carrier}, the two $\SorgenfreyLineUp$ rows $L_X,L_Y$ and the $\ProductTopologyUp$ row $Q$ form the horizontal sibling lattice boundary; the later rectangle, observable-open, transport, replay, provenance, and local naming rows cannot replace either dependency with a host plane topology, selected rectangle basis, quotient equality on $\RealUp\times\RealUp$, or ambient separation theorem.
