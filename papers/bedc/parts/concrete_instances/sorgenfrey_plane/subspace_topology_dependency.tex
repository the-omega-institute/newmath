The $\SorgenfreyPlaneUp$ packet reads the product lower-limit topology through the displayed subspace-topology carrier interface of \autoref{ch:concrete-instances-subspace-topology-namecert}. The dependency is active only after the two lower-limit component rows, the $\ProductTopologyUp$ product row, rectangle ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row have been displayed, so the plane route exports a finite subspace-topology read rather than a host powerset topology, selected rectangle basis, quotient equality on $\RealUp \times \RealUp$, metrizability, or normality.
