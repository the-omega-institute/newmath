The $\SorgenfreyPlaneUp$ hub displays its topology dependency lattice through \autoref{ch:concrete-instances-subspace-topology-namecert}, \autoref{ch:concrete-instances-product-topology-namecert}, and \autoref{ch:concrete-instances-sorgenfrey-line-namecert}. These sibling routes supply the subspace, product, and lower-limit topology rows before any hub consumer reads the plane certificate.
