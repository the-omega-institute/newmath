\chapter{A Concrete Naming Certificate for $\RadialRegisterObstructionFrontierUp$}
\label{ch:concrete-instances-radial-register-obstruction-frontier-namecert}
\origin{ai}

The $\RadialRegisterObstructionFrontierUp$ packet realizes the finite obstruction frontier isolated in \autoref{ch:visions-rh-radial-register-obstruction-frontier}. It records the circle--triangle--register rows that must be displayed before a nontrivial radial factor can be refused as hidden unit phase, compact prime-register gauge, finite residue erasure, or a harmless cofinal-tail artifact. Its frontier row consumes the export and refusal boundary of \autoref{ch:concrete-instances-machine-export-boundary-namecert} when a radial-register obstruction is presented to machine-facing consumers, so export cannot bypass the displayed register and refusal ledgers. The packet is an obstruction NameCert for the radial-register boundary; it is not a proof of the Riemann hypothesis, a zeta-source construction, a zero classifier, or a machine-export theorem.

\begin{definition}[Radial register obstruction frontier carrier]
\label{def:radial-register-obstruction-frontier-carrier}
A $\RadialRegisterObstructionFrontierUp$ carrier is a finite $\BHist$ packet
$$
F=(Z,T,B,R,L,K,C,O,H,Q,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Z$ is the zeta observation source row, naming the zero-relevant packet whose radial leg is inspected.
\item $T$ is the circle--triangle--register row exposing phase, triangular decomposition, and finite residual registration.
\item $B$ is the continuous--discrete bridge row relating continuous phase and prime-register readback for the displayed source.
\item $R$ is the radial-scale row, recording whether a nontrivial positive-real scale is ledgered.
\item $L$ is the no-hidden-radial-register refusal ledger, separating compact gauge triviality from ledger-explicit scale data.
\item $K$ is the compact-gauge obstruction row consumed from the source vision.
\item $C$ is the constructor-support row, naming the displayed source construction, bridge, and tower-support obligations that remain visible.
\item $O$ is the obstruction-frontier row, listing the non-closed zeta embedding, projection faithfulness, and cofinal-tower obligations.
\item $H$ records componentwise $\hsame$ transport for the source, register, bridge, radial scale, refusal ledger, compact obstruction, constructor support, frontier, provenance, and local naming rows.
\item $Q$ is the $\Pkg$ provenance over $\RadialRegisterObstructionFrontierUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RadialRegisterObstructionFrontierUp}$ row.
\end{enumerate}
The classifier compares packets only through $Z,T,B,R,L,K,C,O,H,Q,N$. It has no coordinate for an unledgered nontrivial radial scale, a zero-dependent oracle repair, host analytic continuation, completed zeta-source construction, or machine-export proof boundary.
\end{definition}

\begin{theorem}[Radial register obstruction frontier obligations]
\label{thm:radial-register-obstruction-frontier-obligations}
For every accepted $\RadialRegisterObstructionFrontierUp$ carrier
$$
F=(Z,T,B,R,L,K,C,O,H,Q,N),
$$
the local $\NameCert_{\RadialRegisterObstructionFrontierUp}$ surface consists exactly of carrier admission for the zeta observation source $Z$, circle--triangle--register row $T$, continuous--discrete bridge row $B$, radial-scale row $R$, no-hidden-radial-register refusal ledger $L$, compact-gauge obstruction row $K$, constructor-support row $C$, obstruction-frontier row $O$, componentwise transport $H$, provenance $Q$, and local naming $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:radial-register-obstruction-frontier-carrier}. The obstruction data visible to the classifier are precisely the source, register, bridge, radial-scale, refusal-ledger, compact-obstruction, support, and frontier rows.

The refusal row $L$ is read together with $R$ and $K$. A nontrivial scale may be accepted only as ledgered data, while a compact phase or compact prime-register gauge is forced through the compact-gauge obstruction row. Thus the packet admits no hidden radial coordinate.

The support and frontier rows remain separate. The row $C$ records the displayed source-construction and bridge obligations, while $O$ lists the zeta embedding, projection-faithfulness, and cofinal-tower obligations that this packet does not close. Transport, provenance, and local naming preserve the same finite obstruction surface.
\end{proof}

\falsifiablePrediction{Endpoint-agreeing circle--triangle--register packets with an unledgered nontrivial radial scale refute the $\RadialRegisterObstructionFrontierUp$ classifier.}

\independenceWitness{zeta-zeros, rh-route-halting-boundary, proofwitnesschain, machine-export-boundary: none is bijective to $\RadialRegisterObstructionFrontierUp$ because zeta zeros name zero packets, the RH route-halting boundary names a stopping frontier, ProofWitnessChain names proof-row sequencing, and MachineExportBoundary names export limits, while this packet jointly carries register, bridge, radial-scale, refusal-ledger, compact-obstruction, support, frontier, transport, provenance, and local NameCert rows.}

The Lean follow-up namespace is \texttt{BEDC.Derived.RadialRegisterObstructionFrontierUp}; it must provide $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances in \texttt{lean4/BEDC/Derived/RadialRegisterObstructionFrontierUp/TasteGate.lean}.

\closureat{RadialRegisterObstructionFrontierUp}{seedStr}

\begin{closurestatus}{\RadialRegisterObstructionFrontierUp}
  \constructivestory{A $\RadialRegisterObstructionFrontierUp$ packet is generated from a zeta observation source, circle--triangle--register rows, continuous--discrete bridge data, radial-scale ledger rows, compact-gauge obstruction, constructor-support rows, obstruction-frontier rows, componentwise hsame transport, Pkg provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:radial-register-obstruction-frontier-carrier} and \autoref{thm:radial-register-obstruction-frontier-obligations} seed the finite obstruction classifier for non-hidden radial-register rows without closing the zeta-source or RH route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close RH, zeta-source construction, zeta embedding, projection faithfulness, cofinal triangular tower coverage, machine export, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, bridge visibility, radial-scale refusal exactness, compact-gauge obstruction routing, frontier non-closure preservation, TasteGate nontriviality, and field faithfulness for BEDC.Derived.RadialRegisterObstructionFrontierUp.}
\end{closurestatus}
