\chapter{A Concrete Naming Certificate for $\CompactIntervalCantorBisectionUp$}
\label{ch:concrete-instances-compact-interval-cantor-bisection-namecert}
\origin{human}

Following Cantor's nested-interval bisection and the Bishop--Bridges finite-means reading of compact intervals, the $\CompactIntervalCantorBisectionUp$ packet records a closed interval search as finite dyadic interval data before any completion consumer reads a limit. It is anchored in the dyadic interval net of \autoref{ch:concrete-instances-dyadic-interval-net-namecert}, the located interval bisection route of \autoref{ch:concrete-instances-bishop-located-interval-refinement-chain-namecert}, the nested interval convergence surface of \autoref{ch:concrete-instances-monotone-cauchy-convergence-namecert}, the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. The chapter treats Cantor bisection as a finite NameCert obligation surface over displayed intervals, not as open-cover compactness, a selected limit, countable choice, or ambient real completeness.

\begin{definition}[Compact interval Cantor bisection carrier]
\label{def:compact-interval-cantor-bisection-carrier}
A $\CompactIntervalCantorBisectionUp$ carrier is a finite $\BHist$ packet
$$
B=(D,L,N,R,Q,H,C,P,M)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicIntervalNetUp$ row listing the finite dyadic cells used at the displayed bisection depth.
\item $L$ is the $\LocatedIntervalBisectionUp$ row recording the left/right interval decision ledger for each inspected cell.
\item $N$ is the $\NestedIntervalConvergenceUp$ row that retains the nested enclosure relation through the finite bisection spine.
\item $R$ is the $\RegSeqRatUp$ readback row for endpoint windows and shrinking-radius budgets.
\item $Q$ is the terminal $\RealUp$ seal row reached only after the dyadic cells, located bisection ledger, nested enclosure row, and regular-rational readback have been displayed.
\item $H$ records componentwise $\hsame$ transport for the dyadic interval net, located bisection, nested enclosure, regular-rational readback, real seal, continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a compact interval request through finite dyadic subdivision, located bisection, nested enclosure, regular-rational readback, and the real seal.
\item $P$ is the $\Pkg$ provenance over $\CompactIntervalCantorBisectionUp$, $\DyadicIntervalNetUp$, $\LocatedIntervalBisectionUp$, $\NestedIntervalConvergenceUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $M$ is the local $\NameCert_{\CompactIntervalCantorBisectionUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $D,L,N,R,Q,H,C,P,M$. It has no coordinate for open-cover compactness, selected finite subcovers, countable-choice schedules, quotient equality of real names, host interval endpoints, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact interval Cantor bisection NameCert obligations]
\label{thm:compact-interval-cantor-bisection-namecert-obligations}
For every accepted $\CompactIntervalCantorBisectionUp$ carrier
$$
B=(D,L,N,R,Q,H,C,P,M),
$$
the local $\NameCert_{\CompactIntervalCantorBisectionUp}$ surface consists exactly of carrier admission, finite dyadic interval-net exposure, located interval bisection exposure, nested interval convergence retention, regular-rational endpoint readback, terminal real sealing, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Carrier admission projects the tuple $D,L,N,R,Q,H,C,P,M$. The finite dyadic interval-net row $D$ is the only source of cells inspected by the bisection route.

The located bisection row $L$ records the finite left/right interval decisions, and the nested enclosure row $N$ keeps the resulting chain visible as a displayed interval spine. These rows precede every completion-facing read.

The readback row $R$ converts endpoint windows and shrinking-radius budgets into regular rational data, and the seal row $Q$ is downstream of that readback. Transport, replay, provenance, and local naming preserve the same route through $H,C,P,M$.

Since the carrier has no coordinate for open covers, selected subcovers, countable-choice schedules, quotient equality of real names, host endpoints, or ambient completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Compact interval Cantor bisection five-row route]
\label{thm:compact-interval-cantor-bisection-five-row-route}
Every completion-facing consumer of an accepted $\CompactIntervalCantorBisectionUp$ packet reads the five visible mathematical rows
$$
\DyadicIntervalNetUp
\longmapsto
\LocatedIntervalBisectionUp
\longmapsto
\NestedIntervalConvergenceUp
\longmapsto
\RegSeqRatUp
\longmapsto
\RealUp
$$
before inspecting transport, replay, provenance, or local naming data. The route exports no open-cover compactness theorem, selected limit, countable-choice schedule, quotient real-name equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Begin with the dyadic interval net row $D$. It supplies the finite cell list and dyadic endpoint arithmetic consumed by the located bisection ledger.

The located bisection row $L$ records only displayed interval decisions. The nested convergence row $N$ then retains the enclosure relation for the finite bisection spine without selecting a host-real limit.

The regular-rational row $R$ reads back the endpoint windows and shrinking-radius budgets, and the real seal $Q$ can be opened only after that readback. The structural rows $H,C,P,M$ replay this same chain and add no excluded compactness or choice coordinate.
\end{proof}

\closureat{CompactIntervalCantorBisectionUp}{seedStr}

\begin{closurestatus}{\CompactIntervalCantorBisectionUp}
  \constructivestory{A $\CompactIntervalCantorBisectionUp$ packet is generated from a finite dyadic interval net, a located bisection ledger, a nested interval convergence row, regular-rational endpoint readback, a terminal real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-interval-cantor-bisection-carrier}, \autoref{thm:compact-interval-cantor-bisection-namecert-obligations}, and \autoref{thm:compact-interval-cantor-bisection-five-row-route} seed the finite Cantor-bisection route over DyadicIntervalNet, LocatedIntervalBisection, NestedIntervalConvergence, RegSeqRat, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, a selected limit, countable choice, quotient real-name equality, host interval endpoints, or ambient real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, five-row route exactness, classifier nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactIntervalCantorBisectionUp.}
\end{closurestatus}
