\chapter{A Concrete Naming Certificate for $\ComplexityClassUp$}
\label{ch:concrete-instances-complexityclass-namecert}

The complexity-class interface $\ComplexityClassUp$ packages a resource-bounded acceptance predicate parametrised by an explicit unary modulus on input length. It depends on $\NameCert_{\NatUp}$ (\autoref{ch:concrete-instances-nat-namecert}), $\NameCert_{\TuringMachineUp}$ (\autoref{ch:concrete-instances-turingmachine-namecert}), and $\NameCert_{\ComputableUp}$ (\autoref{ch:concrete-instances-computable-namecert}).

\section{Carrier definition}
\label{sec:complexityclass-carrier}

A $\ComplexityClassUp$ token carries: a deterministic $\TuringMachineUp$ acceptor, a unary modulus $\beta$ that bounds the configuration-trace length as a function of the unary input length, and a resource type tag (time, space, or alternation depth). The carrier accepts an input history $h$ when the bounded simulation of the acceptor on $h$ within $\beta(|h|)$ rows reaches a halted accepting configuration. The classifier is $\hsame$ on input histories of the same unary length; the ledger keeps the modulus value, the trace length, and the halting verdict visible.

\begin{closurestatus}{\ComplexityClassUp}
  \constructivestory{A $\ComplexityClassUp$ token names a deterministic $\TuringMachineUp$ acceptor together with a displayed unary modulus $\beta$ and a resource-type tag (time, space, or alternation depth). The carrier accepts an input history $h$ when the bounded simulation of the acceptor on $h$, in the sense of $\NameCert_{\ComputableUp}$, halts in an accepting configuration within $\beta(|h|)$ trace rows; the resource-type tag selects whether $\beta$ bounds trace length, head-cell footprint, or alternation depth. The classifier is $\hsame$ on input histories of the same unary length; the source covers unary input lengths, modulus values, configuration-trace lengths, head-cell footprints, and alternation rows; the pattern records halted-acceptance, modulus monotonicity, footprint admissibility, and alternation-depth admissibility rows; the ledger keeps the modulus $\beta(|h|)$, the realised trace length or footprint, the alternation depth, and the halting verdict visible. The five name-certificate obligations are supplied by the bounded-simulation certificate of $\NameCert_{\ComputableUp}$ together with monotonicity of $\beta$: the inhabited identity certificate is the trivial accepter at $\beta\equiv 0$; classifier stability is reflexive, symmetric, and transitive $\hsame$ on inputs of fixed length; transport repacks bounded simulations through input transport; ledger exactness identifies the accepted row with the bounded-simulation verdict; the extension boundary fixes monotonicity of $\beta$ in the unary input length. A history $h$ lies in the signature gap of a $\ComplexityClassUp$ token $p$ when the source-domain accepts the bounded-resource simulation, the signature relation pairs it with the halting verdict, and token introduction names that verdict as $p$. The chapter registers this resource-bounded surface under the name $\ComplexityClassUp$; it is a deterministic-resource-class object with no nondeterministic, randomised, average-case, oracle, or interactive-proof bridge.}
  \theoryclosure{\seedClosure}
  \scopeclosed{The seed scope binds the deterministic $\TuringMachineUp$ acceptor, the unary modulus $\beta$, the resource-type tag (time, space, or alternation depth), and the bounded-simulation halting verdict. Nondeterministic, probabilistic, and oracle resources are excluded.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{The chapter does not close $\mathsf{P}$-versus-$\mathsf{NP}$, randomised classes, oracle separations, average-case complexity, or interactive-proof systems.}
  \upgradepath{Obligation closure requires a checked $\ComplexityClassUp$ resource-monotonicity obligation theorem covering modulus growth and bounded-simulation halting.}
\end{closurestatus}
