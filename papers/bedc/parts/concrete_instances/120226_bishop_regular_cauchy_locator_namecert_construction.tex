\chapter{A Concrete Naming Certificate for $\BishopRegularCauchyLocatorUp$}
\label{ch:concrete-instances-bishop-regular-cauchy-locator-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopRegularCauchyLocatorUp$ packet records the located regular-Cauchy real interface as finite BEDC NameCert data. It sits over the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite-window naming surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, and the located-interval packet of \autoref{ch:concrete-instances-locatedinterval-namecert}. The packet names a constructive locator for a regular Cauchy real name; it is not a selected real representative, quotient stream, trichotomy principle, ambient completeness theorem, or host interval object.

\begin{definition}[Bishop regular Cauchy locator carrier]
\label{def:bishop-regular-cauchy-locator-carrier}
A $\BishopRegularCauchyLocatorUp$ carrier is a finite $\BHist$ packet
$$
L=(S,R,D,I,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ finite-window schedule inspected by the locator request;
\item $R$ is the $\RegSeqRatUp$ regular rational readback attached to that schedule;
\item $D$ is the $\DyadicRatCoreUp$ tolerance row recording the dyadic radius, midpoint, and comparison budget used by the request;
\item $I$ is the $\LocatedIntervalUp$ row naming the displayed interval against which the regular Cauchy name is located;
\item $E$ is the $\RealUp$ seal row reached only after $S,R,D,I$ have been displayed;
\item $H$ records componentwise $\hsame$ transport for the schedule, readback, dyadic tolerance, located interval, Real seal, replay, provenance, and local name;
\item $C$ records displayed $\Cont$ replay of the route
$$
S \longmapsto R \longmapsto D \longmapsto I \longmapsto E ;
$$
\item $P$ is the $\Pkg$ provenance over $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\LocatedIntervalUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\BishopRegularCauchyLocatorUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $S,R,D,I,E,H,C,P,N$. It has no coordinate for a selected host real, quotient equality of streams, undecidable trichotomy, hidden interval endpoint, countable-choice schedule, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop regular Cauchy locator NameCert obligations]
\label{thm:bishop-regular-cauchy-locator-namecert-obligations}
For every accepted $\BishopRegularCauchyLocatorUp$ carrier
$$
L=(S,R,D,I,E,H,C,P,N),
$$
the local $\NameCert_{\BishopRegularCauchyLocatorUp}$ surface consists exactly of the following five obligations:
\begin{enumerate}
\item carrier admission for the StreamName window, RegSeqRat readback, dyadic tolerance row, located interval, RealUp seal, transport, replay, provenance, and local name;
\item locator-window compatibility, requiring $S$ and $R$ to expose the same finite regular-Cauchy request before the located interval row is read;
\item dyadic-interval exactness, requiring $D$ and $I$ to display the midpoint, radius, and interval-side data used by the locator;
\item Real-seal nonescape, requiring $E$ to consume only the displayed StreamName, RegSeqRat, DyadicRatCore, and LocatedInterval route;
\item structural exhaustion, requiring $H,C,P,N$ to transport, replay, source, and name only the displayed packet.
\end{enumerate}
No locator obligation is stored in a host representative, quotient stream, trichotomy oracle, selected interval endpoint, choice schedule, or ambient completeness row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-regular-cauchy-locator-carrier}. Its analytic rows are exactly the finite window $S$, regular rational readback $R$, dyadic tolerance row $D$, located interval $I$, and Real seal $E$.

Read the locator route in order. The StreamName and RegSeqRat rows expose the inspected regular-Cauchy request; the dyadic row supplies the tolerance data; and the LocatedInterval row records the interval side against which the request is located.

Finally inspect the structural rows. The $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row preserve the same finite route, while the carrier excludes host representatives, quotient streams, trichotomy, selected endpoints, choice schedules, and ambient completeness.
\end{proof}

\begin{theorem}[Bishop regular Cauchy locator Real handoff]
\label{thm:bishop-regular-cauchy-locator-real-handoff}
Every $\RealUp$-facing locator read exported by an accepted $\BishopRegularCauchyLocatorUp$ packet factors through
$$
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \LocatedIntervalUp \longmapsto \RealUp .
$$
The handoff exports a finite located regular-Cauchy route, not a completed host real, quotient-stream equality, hidden interval endpoint, countable-choice selector, or ambient completeness claim.
\end{theorem}
\begin{proof}
Start with $L=(S,R,D,I,E,H,C,P,N)$. Projection exposes $S,R,D,I$ before the terminal Real-facing row $E$ is available.

The route through $S$ and $R$ fixes the inspected regular-Cauchy window. The dyadic row $D$ fixes the tolerance budget, and the located interval $I$ fixes the interval evidence for that same request.

The Real seal $E$ is therefore downstream of the displayed finite locator route. Transport, replay, provenance, and local naming do not add another route, so the packet supplies only the constructive located handoff named in the statement.
\end{proof}

\closureat{BishopRegularCauchyLocatorUp}{seedStr}

\begin{closurestatus}{\BishopRegularCauchyLocatorUp}
  \constructivestory{A $\BishopRegularCauchyLocatorUp$ packet is generated from a StreamName window, RegSeqRat readback, DyadicRatCore tolerance row, LocatedInterval row, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-regular-cauchy-locator-carrier}, \autoref{thm:bishop-regular-cauchy-locator-namecert-obligations}, and \autoref{thm:bishop-regular-cauchy-locator-real-handoff} seed the finite located regular-Cauchy locator route for Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim trichotomy, quotient stream equality, selected host representatives, selected interval endpoints, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, locator-window compatibility, dyadic-interval exactness, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BishopRegularCauchyLocatorUp.}
\end{closurestatus}
