\chapter{A Concrete Naming Certificate for $\InterHistInvariantUp$}
\label{ch:concrete-instances-interhistinvariant-namecert}
\origin{ai}

The $\InterHistInvariantUp$ interface packages a finite two-history invariant readback for the Inter-Hist locality chain. It consumes the multi-history packet rows already exposed by $\FiniteMultiHistPacketUp$, the causal-witness rows supplied by $\CrossHistCausalUp$ when present, and the unary maximum-rate boundary consumed by $\MaxCausalRateUp$, while keeping the whole read inside $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ provenance. The causal row is consumed through \autoref{ch:concrete-instances-crosshistcausal-namecert}, and the downstream unary-rate boundary is the consumer surface of \autoref{ch:concrete-instances-maxcausalrate-namecert}. The packet is not a global frame, a synchronized clock, a metric geometry, a host real equality, or a quotient of histories.

\falsifiablePrediction{Every accepted $\InterHistInvariantUp$ consumer reaches the invariant through the displayed finitemulthistpacket, crosshistcausal, maxcausalrate, and crosshistcausalrate sibling rows; a consumer route that bypasses those sibling rows refutes the packet.}

\independenceWitness{Nearest siblings: finitemulthistpacket, crosshistcausal, maxcausalrate, crosshistcausalrate. None is bijective to this carrier because $\InterHistInvariantUp$ simultaneously stores two history slots, one relation or causal row, one unary invariant row, one observer-symmetry row, transport, route, provenance, and local naming data, while each sibling omits at least one of those rows or records a rate-facing object instead of the invariant readback.}

\begin{definition}[InterHistInvariant carrier]
\label{def:interhist-invariant-carrier}
An $\InterHistInvariantUp$ carrier is a finite $\BHist$ packet
$$
I=(A,B,R,V,S,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $A$ and $B$ are the two history rows being compared;
\item $R$ is either the displayed relation row supplied by $\FiniteMultiHistPacketUp$ or the causal-witness row supplied by $\CrossHistCausalUp$;
\item $V$ is the unary invariant readback row obtained from the transported pair and the relation row;
\item $S$ is the observer-symmetry row, recording that swapping the two history rows transports to the same unary readback surface;
\item $H$ stores componentwise $\hsame$ transport for the two histories, relation row, invariant readback, symmetry row, continuation routes, package row, and local name row;
\item $C$ stores the displayed $\Cont$ routes for two-history projection, relation inspection, unary readback, symmetry comparison, and downstream consumer handoff;
\item $P$ is the $\Pkg$ provenance over $\FiniteMultiHistPacketUp$, $\CrossHistCausalUp$, $\MaxCausalRateUp$, $\Hist$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\InterHistInvariantUp}$ row.
\end{enumerate}
The classifier compares accepted packets only after transporting the two histories, the relation or causal-witness row, the unary readback row, and the observer-symmetry row to a common finite two-history surface. The carrier has no coordinate for a global frame, synchronized clock, metric geometry, host real equality, quotient construction, or observer-independent ambient object.
\end{definition}
\leanchecked{BEDC.Derived.InterHistInvariantUp.InterHistInvariantTasteGate\_single\_carrier\_alignment}

\begin{theorem}[InterHistInvariant NameCert obligations]
\label{thm:interhist-invariant-namecert-obligations}
For the carrier of \autoref{def:interhist-invariant-carrier}, the local $\NameCert_{\InterHistInvariantUp}$ surface consists of five finite obligations:
\begin{enumerate}
\item carrier admission for the displayed packet $I=(A,B,R,V,S,H,C,P,N)$;
\item two-history projection through $A,B$ and the relation or causal-witness row $R$;
\item unary invariant readback through $V$ after componentwise $\hsame$ transport;
\item observer-symmetry through the row $S$, with the swapped packet returning to the same readback surface;
\item consumer non-escape for global frames, synchronized clocks, metric geometry, host real equality, and quotient constructions.
\end{enumerate}
\end{theorem}
\begin{proof}
Carrier admission is the projection of \autoref{def:interhist-invariant-carrier}. The rows $A,B,R,V,S,H,C,P,N$ display exactly the two histories, the relation or causal-witness row, the unary invariant readback, the symmetry row, transport, route, provenance, and local name.

The two-history projection uses $C$ to read $A$ and $B$ before it reads $R$. The relation row is therefore part of a finite multi-history or cross-history packet, not an ambient relation over host objects.

The unary readback is the row $V$ transported by $H$. Observer symmetry is then the row $S$, which compares the original and swapped two-history presentations after the same displayed transport.

Since no carrier coordinate names a global frame, synchronized clock, metric geometry, host real equality, or quotient construction, the local $\NameCert$ obligations cannot export any such object.
\end{proof}

\begin{theorem}[InterHistInvariant hsame stability]
\label{thm:interhist-invariant-hsame-stability}
For accepted $\InterHistInvariantUp$ packets $I=(A,B,R,V,S,H,C,P,N)$ and $I'=(A',B',R',V',S',H',C',P',N')$, componentwise $\hsame$ transport of the two history rows, relation or causal-witness rows, unary readback rows, and observer-symmetry rows preserves the $\InterHistInvariantUp$ classifier. The transported packet still reads only the displayed two-history surface and its local invariant row.
\end{theorem}
\begin{proof}
Project both carriers and transport the history rows through $H$ and $H'$. The hypotheses put $A,B$ and $A',B'$ on a common two-history surface before either invariant row is read.

Next transport the relation or causal-witness rows $R$ and $R'$. The continuation routes in $C$ and $C'$ replay the same finite relation inspection after transport, so the classifier has no alternate source for inter-history content.

Finally read the unary rows $V,V'$ and the symmetry rows $S,S'$. Because these rows are part of the same transported packet, the classifier preserves the accepted invariant surface and does not introduce a global frame, clock, metric, host equality, or quotient.
\end{proof}

\begin{theorem}[InterHistInvariant observer symmetry]
\label{thm:interhist-invariant-observer-symmetry}
For an accepted $\InterHistInvariantUp$ carrier $I=(A,B,R,V,S,H,C,P,N)$, swapping the two history rows produces a readback that factors through the same relation or causal-witness row, the same unary invariant row, and the same observer-symmetry row after displayed $\hsame$ transport. The invariant is observer-symmetric as a finite packet read, not as an equality inside a global frame.
\end{theorem}
\begin{proof}
Start with the two projected history rows $A$ and $B$. The swapped presentation uses the same carrier rows with the two history projections interchanged.

Apply the componentwise transport row $H$ and the routes in $C$. These rows carry both the original and swapped presentations to the common finite two-history surface on which $R$ and $V$ are read.

The row $S$ is the displayed symmetry witness between those two presentations. It identifies the invariant readback at the packet surface and supplies no synchronized clock, metric comparison, host real equality, or quotient of the two histories.
\end{proof}

\begin{theorem}[InterHistInvariant maximum-rate consumer boundary]
\label{thm:interhist-invariant-max-rate-consumer-boundary}
Every $\MaxCausalRateUp$ consumer of an accepted $\InterHistInvariantUp$ packet reads the two history rows $A,B$, the relation or causal-witness row $R$, the unary readback row $V$, the observer-symmetry row $S$, and the provenance rows $H,C,P,N$ before it reads a maximum-rate boundary. The consumer receives a finite invariant packet over $\FiniteMultiHistPacketUp$ and $\CrossHistCausalUp$ rows; it receives no global frame, synchronized clock, metric geometry, host real-valued speed, host real equality, or quotient construction.
\end{theorem}
\begin{proof}
Project the carrier. A maximum-rate consumer first sees the two history rows, the relation or causal-witness row, the invariant readback, and the symmetry row because those are the only coordinates that can name inter-history content.

Use \autoref{thm:interhist-invariant-namecert-obligations} to order the read through carrier admission, two-history projection, unary readback, and observer symmetry. Then use \autoref{thm:interhist-invariant-hsame-stability} to transport the same rows to the consumer surface.

The observer-symmetry theorem supplies the packet read that a maximum-rate boundary may consume. Since the packet exports only $A,B,R,V,S,H,C,P,N$, the consumer has no source for global frames, synchronized clocks, metric geometry, host real-valued speeds, host real equality, or quotient constructions.
\end{proof}

\begin{theorem}[InterHistInvariant two-history route exactness]
\label{thm:interhist-invariant-two-history-route-exactness}
For every accepted $\InterHistInvariantUp$ packet
$$
I=(A,B,R,V,S,H,C,P,N),
$$
all two-history content exposed by the packet is reached through the rows $A,B,R,H,C,P,N$. The rows $A$ and $B$ are the only history inputs, $R$ is the only relation or causal-witness row, and $H,C,P,N$ are the only transport, continuation, provenance, and naming rows by which that content can be replayed. Hence every two-history read factors through the sibling surfaces $\FiniteMultiHistPacketUp$, $\CrossHistCausalUp$, and $\MaxCausalRateUp$, and no read can introduce a global frame, synchronized clock, metric geometry, host equality, or quotient.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:interhist-invariant-carrier}. The only rows that name two histories are $A$ and $B$, and the only row that relates them is $R$.

The structural route is then forced by the same carrier projection. The row $H$ supplies componentwise $\hsame$ transport, $C$ supplies the displayed $\Cont$ routes, $P$ supplies package provenance, and $N$ supplies the local naming row.

Use \autoref{thm:interhist-invariant-namecert-obligations} and \autoref{thm:interhist-invariant-hsame-stability}. They identify the two-history projection and its transported replay with the finite multi-history, cross-history, and maximum-rate consumer surfaces already named in the packet.
\end{proof}

\begin{theorem}[InterHistInvariant unary readback coverage]
\label{thm:interhist-invariant-unary-readback-coverage}
For every accepted $\InterHistInvariantUp$ packet $I=(A,B,R,V,S,H,C,P,N)$, the unary readback route consumed by $\MaxCausalRateUp$ is exhausted by the rows $V$ and $S$ after the two-history route has displayed $A,B,R,H,C,P,N$. The row $V$ is the invariant readback row, and $S$ is the observer-symmetry row that keeps the swapped two-history presentation on the same unary surface. These rows provide the complete maximum-rate consumer handoff and do not introduce a global frame, synchronized clock, metric geometry, host equality, or quotient.
\end{theorem}
\begin{proof}
First expose the two-history route by \autoref{thm:interhist-invariant-two-history-route-exactness}. The unary readback is not available before the rows $A,B,R,H,C,P,N$ have supplied the finite two-history surface.

Now project the unary rows in the carrier. The row $V$ records the invariant readback obtained from the transported pair and relation row, while $S$ records the observer-symmetry comparison for the swapped presentation.

Finally apply \autoref{thm:interhist-invariant-observer-symmetry} and \autoref{thm:interhist-invariant-max-rate-consumer-boundary}. Together they make $V$ and $S$ the whole unary surface consumed by $\MaxCausalRateUp$, with no carrier coordinate left for a global or quotient object.
\end{proof}

\begin{theorem}[InterHistInvariant finite-multihistory projection lock]
\label{thm:interhist-invariant-finite-multihist-projection-lock}
Let $I=(A,B,R,V,S,H,C,P,N)$ be an accepted $\InterHistInvariantUp$ packet. If $R$ is the relation row supplied by $\FiniteMultiHistPacketUp$, then every read of $A,B,R,V,S$ factors through the finite two-history packet displayed by $A,B,R,H,C,P,N$. No consumer of this packet receives an ambient relation on histories, a host relation outside $R$, or a second relation row not present in the carrier.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:interhist-invariant-carrier}. The only history coordinates are $A$ and $B$, and the only relation coordinate in the finite-multihistory case is the displayed row $R$.

Use \autoref{thm:interhist-invariant-namecert-obligations} to order the read through carrier admission and two-history projection. The continuation routes in $C$ inspect $R$ after $A$ and $B$ are displayed, so relation access is locked to the same finite packet.

Finally apply \autoref{thm:interhist-invariant-hsame-stability}. Transport replays the same rows $A,B,R,V,S$ on a common finite surface and supplies no coordinate from which an ambient relation or additional relation row could be read.
\end{proof}

\begin{theorem}[InterHistInvariant causal-row choice boundary]
\label{thm:interhist-invariant-causal-row-choice-boundary}
Let $I=(A,B,R,V,S,H,C,P,N)$ be an accepted $\InterHistInvariantUp$ packet. If $R$ is supplied by $\CrossHistCausalUp$, then consumers receive only the displayed causal-witness row together with $A,B,V,S,H,C,P,N$. They do not receive a global causal order, a synchronized clock, a metric time coordinate, or any rule choosing causal comparisons beyond the row $R$.
\end{theorem}
\begin{proof}
The carrier of \autoref{def:interhist-invariant-carrier} gives a single slot for the relation or causal-witness row. In the causal case that slot is $R$, and there is no second coordinate for an order, clock, or metric time.

Apply \autoref{thm:interhist-invariant-namecert-obligations}. The causal witness is read only through the two-history projection and then through the unary invariant readback, so the consumer sees the displayed causal row as packet data.

The stability theorem \autoref{thm:interhist-invariant-hsame-stability} transports this same row with the two histories and the readback rows. Transport preserves the finite causal witness surface; it does not create a global causal order or synchronized clock.
\end{proof}

\begin{theorem}[InterHistInvariant rate-consumer readback exhaustion]
\label{thm:interhist-invariant-rate-consumer-readback-exhaustion}
Every $\MaxCausalRateUp$ consumer of an accepted $\InterHistInvariantUp$ packet receives exactly the rows
$$
A,B,R,V,S,H,C,P,N
$$
before any maximum-rate boundary is read. This readback exhausts the available inter-history invariant surface: the consumer has no host real-valued speed, host real equality, quotient construction, metric geometry, synchronized clock, or global frame from which to refine the rate boundary.
\end{theorem}
\begin{proof}
First project the carrier of \autoref{def:interhist-invariant-carrier}. The row list $A,B,R,V,S,H,C,P,N$ is the complete list of visible carrier coordinates.

Use \autoref{thm:interhist-invariant-namecert-obligations} to pass from the carrier list to the local obligation surface, and use \autoref{thm:interhist-invariant-hsame-stability} to transport that same surface to the consumer. No step adds a numerical speed or quotient coordinate.

Finally apply \autoref{thm:interhist-invariant-max-rate-consumer-boundary}. The maximum-rate boundary consumes the finite packet after those rows are displayed, so the rate-facing readback is exhausted by the carrier inventory itself.
\end{proof}

\begin{theorem}[InterHistInvariant consumer row exactness]
\label{thm:interhist-invariant-consumer-row-exactness}
For every accepted $\InterHistInvariantUp$ packet
$$
I=(A,B,R,V,S,H,C,P,N),
$$
every consumer read of $A,B,R,V,S,H,C,P,N$ is routed through the finite packet rows supplied by $\FiniteMultiHistPacketUp$ or through the displayed causal row supplied by $\CrossHistCausalUp$. The consumer receives exactly the two history slots, relation or causal row, unary invariant row, observer-symmetry row, transport row, continuation row, provenance row, and local naming row; it receives no global frame, synchronized clock, metric geometry, host equality, or quotient route.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:interhist-invariant-carrier}. The tuple lists the two history slots $A,B$, the relation or causal row $R$, the unary readback $V$, the symmetry row $S$, and the structural rows $H,C,P,N$.

Use \autoref{thm:interhist-invariant-two-history-route-exactness} for the route to $A,B,R,H,C,P,N$. If $R$ is a finite-multihistory relation row, the route is locked by \autoref{thm:interhist-invariant-finite-multihist-projection-lock}; if $R$ is causal, it is confined by \autoref{thm:interhist-invariant-causal-row-choice-boundary}.

Finally apply \autoref{thm:interhist-invariant-unary-readback-coverage}. It exhausts the remaining consumer-facing rows $V$ and $S$, so no off-packet frame, clock, metric, host equality, or quotient coordinate is available to the consumer.
\end{proof}

\begin{theorem}[InterHistInvariant rate handoff exactness]
\label{thm:interhist-invariant-rate-handoff-exactness}
Every $\MaxCausalRateUp$ read exported from an accepted $\InterHistInvariantUp$ packet consumes only the displayed unary invariant row $V$, the observer-symmetry row $S$, and the structural rows $H,C,P,N$ after the two-history relation row $R$ has been displayed. The handoff does not consume a hidden speed coordinate, synchronized clock, metric geometry, host real equality, quotient of histories, or global reference frame.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:interhist-invariant-max-rate-consumer-boundary}. It states that the maximum-rate consumer first reads $A,B,R,V,S,H,C,P,N$ from the finite packet.

The unary part of the handoff is then exactly \autoref{thm:interhist-invariant-unary-readback-coverage}. The row $V$ supplies the invariant readback, and $S$ supplies the observer-symmetry comparison on the same transported two-history surface.

Use \autoref{thm:interhist-invariant-rate-consumer-readback-exhaustion} to finish the boundary. Since the row inventory is exhausted before the rate boundary is read, no hidden speed, clock, metric, host equality, quotient, or global frame can enter the handoff.
\end{proof}

\begin{theorem}[InterHistInvariant finite source scope]
\label{thm:interhist-invariant-finite-source-scope}
For every accepted $\InterHistInvariantUp$ carrier
$$
I=(A,B,R,V,S,H,C,P,N),
$$
the history rows $A,B$, relation or causal row $R$, transport row $H$, continuation row $C$, package row $P$, and local naming row $N$ are scoped to the finite source surface of $\FiniteMultiHistPacketUp$. The packet may project the two history slots and the relation row through that finite source, but it has no coordinate for a global frame, synchronized clock, metric geometry, host equality, quotient of histories, or observer-independent ambient object.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:interhist-invariant-carrier}. The only history inputs are the displayed rows $A$ and $B$, and the only finite relation slot is $R$.

Apply \autoref{thm:interhist-invariant-finite-multihist-projection-lock}. It already binds the relation-row case to the $\FiniteMultiHistPacketUp$ source and prevents an ambient relation or second relation row from entering the packet.

Now use \autoref{thm:interhist-invariant-two-history-route-exactness}. The transport, continuation, provenance, and naming rows $H,C,P,N$ are the only replay boundary for the two-history content. Since the carrier has no further coordinate, the finite source scope is exactly the displayed $\BHist$ route.
\end{proof}

\begin{theorem}[InterHistInvariant causal rate scope]
\label{thm:interhist-invariant-causal-rate-scope}
For every accepted $\InterHistInvariantUp$ carrier $I=(A,B,R,V,S,H,C,P,N)$, causal-row use is scoped to $\CrossHistCausalUp$ and the downstream unary rate boundary is scoped to $\MaxCausalRateUp$. A consumer may read $R$ as the displayed causal row and may then read $V,S,H,C,P,N$ as the finite invariant handoff to the maximum-rate surface, but it cannot read a global causal order, synchronized clock, metric time coordinate, host real speed, host equality, or quotient construction.
\end{theorem}
\begin{proof}
Begin with the causal-row boundary of \autoref{thm:interhist-invariant-causal-row-choice-boundary}. It identifies $R$ as the only causal slot and confines it to the displayed $\CrossHistCausalUp$ row.

The rate-facing side is \autoref{thm:interhist-invariant-rate-handoff-exactness}. It routes any $\MaxCausalRateUp$ read through $V,S,H,C,P,N$ after the two-history relation row has been displayed.

Combine these two boundaries with \autoref{thm:interhist-invariant-max-rate-consumer-boundary}. The consumer sees a finite causal invariant packet before the rate boundary, so there is no route to a global order, clock, metric geometry, host speed, host equality, or quotient.
\end{proof}

\begin{theorem}[InterHistInvariant consumer kernel scope]
\label{thm:interhist-invariant-consumer-kernel-scope}
Every consumer of an accepted $\InterHistInvariantUp$ packet factors through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, and the checked TasteGate carrier surface for the displayed tuple
$$
I=(A,B,R,V,S,H,C,P,N).
$$
The consumer receives the two history slots, relation or causal row, unary invariant row, observer-symmetry row, transport row, continuation row, package provenance, and local name row, and receives no global frame, synchronized clock, metric geometry, host equality, quotient construction, or off-packet causal data.
\end{theorem}
\begin{proof}
First apply \autoref{thm:interhist-invariant-consumer-row-exactness}. It states that every consumer read of $A,B,R,V,S,H,C,P,N$ is routed through the finite packet rows supplied by $\FiniteMultiHistPacketUp$ or through the displayed causal row supplied by $\CrossHistCausalUp$.

Next apply \autoref{thm:interhist-invariant-finite-source-scope} and \autoref{thm:interhist-invariant-causal-rate-scope}. These two scope the finite-source and causal-rate cases without adding a global frame, synchronized clock, metric geometry, host equality, quotient, or off-packet causal source.

Finally align the packet with the checked TasteGate carrier surface named in \autoref{thm:interhist-invariant-tastegate-field-contract}. The TasteGate row preserves the same displayed fields, while $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ provide the kernel route by which consumers read them.
\end{proof}

\begin{theorem}[InterHistInvariant TasteGate field contract]
\label{thm:interhist-invariant-tastegate-field-contract}
The checked TasteGate anchor for $\InterHistInvariantUp$ is the single-carrier alignment
$$
\mathsf{BEDC.Derived.InterHistInvariantUp.InterHistInvariantTasteGate\_single\_carrier\_alignment}.
$$
The Lean TasteGate surface for this same packet must include explicit $\mathsf{Nontrivial}\ \InterHistInvariantUp$ and $\mathsf{FieldFaithful}\ \InterHistInvariantUp$ instances preserving the two history slots, relation row, invariant row, symmetry row, transport row, continuation route, package provenance, and local name row.
\end{theorem}
\begin{proof}
The paper anchor is the checked marker immediately after \autoref{def:interhist-invariant-carrier}. It identifies the TasteGate surface with the displayed carrier rather than with an external event-flow object.

Nontriviality is required because a singleton carrier would erase the distinction between the two history slots, the relation row, the invariant row, and the symmetry row. Such a collapse cannot support the consumer exactness and rate handoff theorems above.

Field faithfulness is required because each carrier coordinate has a separate consumer role. Preserving $A,B,R,V,S,H,C,P,N$ as distinct fields keeps the TasteGate route aligned with the same finite packet read by the paper obligations.
\end{proof}
\leanchecked{BEDC.Derived.InterHistInvariantUp.taste\_gate}

\begin{theorem}[InterHistInvariant obligation closure package]
\label{thm:interhist-invariant-obligation-closure-package}
The carrier of \autoref{def:interhist-invariant-carrier}, the local obligations of \autoref{thm:interhist-invariant-namecert-obligations}, hsame stability from \autoref{thm:interhist-invariant-hsame-stability}, observer symmetry from \autoref{thm:interhist-invariant-observer-symmetry}, the maximum-rate boundary of \autoref{thm:interhist-invariant-max-rate-consumer-boundary}, two-history route exactness from \autoref{thm:interhist-invariant-two-history-route-exactness}, unary readback coverage from \autoref{thm:interhist-invariant-unary-readback-coverage}, the finite-multihistory projection lock of \autoref{thm:interhist-invariant-finite-multihist-projection-lock}, the causal-row choice boundary of \autoref{thm:interhist-invariant-causal-row-choice-boundary}, rate-consumer readback exhaustion from \autoref{thm:interhist-invariant-rate-consumer-readback-exhaustion}, consumer row exactness from \autoref{thm:interhist-invariant-consumer-row-exactness}, rate handoff exactness from \autoref{thm:interhist-invariant-rate-handoff-exactness}, and the TasteGate field contract of \autoref{thm:interhist-invariant-tastegate-field-contract} form the full obligation surface for $\InterHistInvariantUp$ over the finite packet
$$
I=(A,B,R,V,S,H,C,P,N).
$$
Every accepted packet is therefore certified by carrier admission, two-history route exactness, unary readback coverage, componentwise transport stability, observer symmetry, maximum-rate consumer boundary, package provenance, and local naming, and it exports no global frame, synchronized clock, metric geometry, host equality, or quotient.
\end{theorem}
\begin{proof}
The carrier and NameCert rows give the admission layer. They display exactly the rows $A,B,R,V,S,H,C,P,N$ and list the local obligations that must be discharged for the packet.

The route layer is supplied by the two exactness theorems. Two-history content is confined to $A,B,R,H,C,P,N$, while the maximum-rate unary readback is confined to $V,S$ after those rows have been displayed.

The projection and choice layers are supplied by the finite-multihistory lock and causal-row boundary. They distinguish the relation-row and causal-row readings of $R$ without adding any ambient relation, global order, synchronized clock, or metric time.

The stability and consumer layers are supplied by the remaining local theorems. Componentwise $\hsame$ transport preserves the same packet surface, observer symmetry keeps the swapped presentation on that surface, and the maximum-rate boundary together with rate-consumer exhaustion gives the downstream handoff.

These pieces exhaust the visible carrier coordinates. Since no additional coordinate names a global frame, synchronized clock, metric geometry, host equality, or quotient, the listed rows are the full obligation surface for $\InterHistInvariantUp$.
\end{proof}

\closureat{InterHistInvariantUp}{seedStr}
\closureat{InterHistInvariantUp}{obligationStr}
\closureat{InterHistInvariantUp}{scopedStr}
\closureat{InterHistInvariantUp}{publicStr}
\closureat{InterHistInvariantUp}{bridgedStr}[BEDC.Derived.InterHistInvariantUp.InterHistInvariantTasteGate\_single\_carrier\_alignment]
\closureat{InterHistInvariantUp}{matureStr}

\begin{closurestatus}{\InterHistInvariantUp}
  \constructivestory{An $\InterHistInvariantUp$ packet is generated from two history rows, a finite relation or causal-witness row, a unary invariant readback row, an observer-symmetry row, componentwise $\hsame$ transport, displayed $\Cont$ routes, package provenance, and a local $\NameCert$ row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{thm:interhist-invariant-finite-source-scope}, \autoref{thm:interhist-invariant-causal-rate-scope}, and \autoref{thm:interhist-invariant-consumer-kernel-scope} bind the finite source rows, causal-rate rows, consumer exactness, and TasteGate carrier surface for inter-history invariant packets, with TasteGate instance \leanchecked{BEDC.Derived.InterHistInvariantUp.taste\_gate}.}
  \formalstatus{\auditCleanV}
  \leantarget{BEDC.Derived.InterHistInvariantUp.InterHistInvariantTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not close global frames, synchronized clocks, metric geometry, host real equality, quotient constructions, or standard bridge checking beyond the checked TasteGate carrier alignment.}
  \upgradepath{No further paper theory-axis closure remains for the InterHistInvariant consumer surface; bridge checking remains a separate verification-axis obligation.}
\end{closurestatus}
