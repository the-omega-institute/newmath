\chapter{A Concrete Naming Certificate for $\DiagonalLimitStabilityUp$}
\label{ch:concrete-instances-diagonal-limit-stability-namecert}
\origin{ai}

The $\DiagonalLimitStabilityUp$ packet is the finite comparison layer between the diagonal representative used in the Real completeness route of \autoref{ch:visions-real-completeness} and the sealed $\RealUp$ limit route. It consumes the selector surface of \autoref{ch:concrete-instances-diagonal-tail-selector-namecert}, the seal boundary of \autoref{ch:concrete-instances-cauchylimitseal-namecert}, the mesh rows of \autoref{ch:concrete-instances-regular-cauchy-mesh-namecert}, the diagonal budget rows of \autoref{ch:concrete-instances-cauchydiagonalbudget-namecert}, and the observation-bound surface of \autoref{ch:concrete-instances-realapproximationenvelope-namecert}. The carrier records a displayed stability comparison; it does not identify quotient streams, choose a host limit, or import ambient completeness.

\falsifiablePrediction{If a DiagonalLimitStabilityUp packet is accepted, every RealUp seal read factors through either the displayed DiagonalTailSelectorUp row or the displayed CauchyLimitSealUp row; a consumer route bypassing both refutes the packet.}

\independenceWitness{regularcauchymesh, cauchydiagonalbudget, diagonaltailselector, cauchylimitseal, realapproximationenvelope: none is bijective because this carrier has a stability-comparison row between the diagonal representative and the sealed RealUp route; mesh chapters lack the seal row, selector chapters lack the stability comparison, seal chapters lack the diagonal representative source, and envelope chapters package observation bounds without the representative-seal classifier.}

\begin{definition}[Diagonal-limit stability carrier]
\label{def:diagonal-limit-stability-carrier}
A $\DiagonalLimitStabilityUp$ carrier is a finite $\BHist$ packet
$$
T=(R,D,L,S,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RegSeqRatUp$ source row carrying the regular rational name whose finite observations feed the diagonal construction;
\item $D$ is the diagonal representative row, read through the $\DiagonalTailSelectorUp$ selector surface;
\item $L$ is the limit selector row that records the finite diagonal-limit route selected for the representative;
\item $S$ is the $\RealUp$ seal row reached through the $\CauchyLimitSealUp$ boundary;
\item $W$ is the finite $\StreamNameUp$ window row used to compare the representative readback with the sealed route;
\item $H$ stores componentwise $\hsame$ transports for $R,D,L,S,W,C,P,N$;
\item $C$ stores the $\Cont$ continuations that replay source, representative, selector, seal, window, transport, provenance, and local naming reads;
\item $P$ is the $\Pkg$ provenance over the same finite route;
\item $N$ is the local $\NameCert_{\DiagonalLimitStabilityUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient regular streams, host real equality, a choice-selected limit representative, trichotomy, or an ambient completeness theorem.
\end{definition}
\leandef{BEDC.Derived.DiagonalLimitStabilityUp.DiagonalLimitStabilityUp}

\begin{theorem}[Diagonal-limit stability NameCert obligations]
\label{thm:diagonal-limit-stability-namecert-obligations}
For the carrier of \autoref{def:diagonal-limit-stability-carrier}, the local $\NameCert_{\DiagonalLimitStabilityUp}$ surface consists exactly of carrier admission for $T$, source exposure through $R$, diagonal representative exposure through $D$, limit-selector exposure through $L$, $\RealUp$ seal exposure through $S$, finite-window stability comparison through $W$, componentwise transport through $H$, continuation replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier displayed in \autoref{def:diagonal-limit-stability-carrier}. The source, representative, selector, seal, and finite-window rows are explicit coordinates, and the transport, route, provenance, and local certificate rows are the only structural rows available to a consumer.

The diagonal representative row is read through the sibling selector surface, while the seal row is read through the sibling Cauchy-limit seal surface. The stability comparison is therefore not a new analytic equality; it is the finite assertion that the selected representative row and the sealed route are read under the same displayed window and structural provenance.

The excluded objects have no coordinate in $T$. Hence the local $\NameCert$ obligations are exhausted by the nine displayed rows.
\end{proof}
\leanchecked{BEDC.Derived.DiagonalLimitStabilityUp.DiagonalLimitStabilityTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Diagonal-limit stability RealCompleteness handoff]
\label{thm:diagonal-limit-stability-real-completeness-handoff}
Every $\RealCompletenessUp$ consumer that reads the sealed diagonal limit from an accepted $\DiagonalLimitStabilityUp$ carrier $T=(R,D,L,S,W,H,C,P,N)$ must first pass through the displayed diagonal representative row $D$ or the displayed $\CauchyLimitSealUp$ row $S$, and in either case the read is replayed through the same finite window $W$, transports $H$, continuations $C$, provenance $P$, and local naming row $N$.
\end{theorem}
\begin{proof}
Read the Real completeness route from \autoref{ch:visions-real-completeness}. A consumer of the sealed diagonal limit needs a representative side and a seal side before the $\RealUp$ read can be inspected.

The representative side of $T$ is the row $D$, which is anchored in the diagonal-tail selector sibling. The seal side of $T$ is the row $S$, which is anchored in the Cauchy-limit seal sibling. The limit-selector row $L$ names the finite route between them, and $W$ is the common observation window on which the stability comparison is made.

The structural rows $H,C,P,N$ replay the same finite comparison for downstream consumers. Since no additional representative, seal, quotient, host equality, selected external limit, or ambient completeness row is present, every sealed diagonal-limit read factors through the displayed handoff.
\end{proof}

\begin{theorem}[Diagonal-limit stability budget-seal factorization]
\label{thm:diagonal-limit-stability-budget-seal-factorization}
Every $\DiagonalLimitStabilityUp$ consumer that reads the $\RealCompletenessUp$ handoff factors through the diagonal budget row $D$, $\StreamNameUp$ window row $W$, $\RegSeqRatUp$ readback row $R$, $\RealUp$ seal row $S$, $\BHist$ history rows, componentwise $\hsame$ row $H$, displayed $\Cont$ route $C$, $\Pkg$ provenance $P$, and local $\NameCert_{\DiagonalLimitStabilityUp}$ row $N$. The consumer receives no host limit, ambient completeness coordinate, external representative equality, or detached Real seal outside this packet.
\end{theorem}
\begin{proof}
Start from the Real-completeness handoff theorem, \autoref{thm:diagonal-limit-stability-real-completeness-handoff}. It forces every sealed diagonal-limit read to enter through $D$ or $S$ and then replay through the same finite window and structural rows.

Use the root-route budget package from the sibling diagonal-limit compatibility chapter, \autoref{thm:diagonal-limit-compatibility-root-route-budget-package}. It supplies the finite budget route that feeds the diagonal representative side before the Real seal is read.

The local NameCert obligations in \autoref{thm:diagonal-limit-stability-namecert-obligations} keep the readback row $R$, window row $W$, transports $H$, continuation $C$, provenance $P$, and naming row $N$ attached to the same carrier. Hence the Real-completion handoff factors from finite budget to seal without exporting a host limit or ambient completeness coordinate.
\end{proof}

\begin{theorem}[Representative-seal window comparison]
\label{thm:diagonal-limit-stability-representative-seal-comparison}
For an accepted $\DiagonalLimitStabilityUp$ carrier $T=(R,D,L,S,W,H,C,P,N)$, the displayed diagonal representative row $D$ and the displayed $\RealUp$ seal row $S$ are compared only through the common finite window row $W$. Neither $D$ nor $S$ can be consumed as an independent Real-completeness input outside the same packet.
\end{theorem}
\begin{proof}
The carrier exposes $D$ as the representative-side row and $S$ as the seal-side row, but the comparison coordinate supplied by the carrier is $W$. Thus any consumer that reads either side as part of the stability comparison must also read the window coordinate that makes the two sides commensurable.

There is no coordinate in $T$ for an external representative equality, a host-level seal equality, or a separate comparison witness. The only displayed comparison boundary is the packet's own window row, so independent consumption of $D$ or $S$ would require a row absent from the carrier.
\end{proof}

\begin{theorem}[Real-completeness window coverage]
\label{thm:diagonal-limit-stability-window-coverage}
Every $\RealCompletenessUp$ consumer of the accepted packet $T=(R,D,L,S,W,H,C,P,N)$ factors through $W,H,C,P,N$ after choosing either the representative side $D$ or the seal side $S$.
\end{theorem}
\begin{proof}
By \autoref{thm:diagonal-limit-stability-real-completeness-handoff}, a Real-completeness consumer first reaches the packet through $D$ or through $S$. Once that side is chosen, the same theorem records the replay rows $W,H,C,P,N$ as the boundary through which the read continues.

The rows $W,H,C,P,N$ are not optional decorations: $W$ fixes the common observation window, $H$ carries the componentwise transports, $C$ carries the displayed continuation routes, $P$ carries provenance, and $N$ carries the local naming row. Since the carrier has no additional Real-completeness boundary, each accepted packet read factors through those rows.
\end{proof}

\begin{theorem}[Transport replay for local NameCert obligations]
\label{thm:diagonal-limit-stability-transport-replay}
For an accepted $\DiagonalLimitStabilityUp$ carrier $T=(R,D,L,S,W,H,C,P,N)$, the componentwise $\hsame$ rows in $H$ and the displayed $\Cont$ routes in $C$ replay the same nine-coordinate packet and preserve the local $\NameCert_{\DiagonalLimitStabilityUp}$ obligations.
\end{theorem}
\begin{proof}
The row $H$ is displayed as componentwise transport for $R,D,L,S,W,C,P,N$, while $C$ is displayed as the continuation replay for the same source, representative, selector, seal, window, transport, provenance, and local naming reads. Both rows therefore address the coordinates of the single carrier $T$ rather than a second packet.

The local $\NameCert$ obligations are exactly the rows listed in \autoref{thm:diagonal-limit-stability-namecert-obligations}. Because $H$ and $C$ replay those rows componentwise and by displayed continuations, downstream use preserves the same local obligation surface.
\end{proof}

\begin{theorem}[Diagonal-limit stability obligation package]
\label{thm:diagonal-limit-stability-obligation-package}
For an accepted $\DiagonalLimitStabilityUp$ carrier $T=(R,D,L,S,W,H,C,P,N)$, the carrier, local $\NameCert$ obligations, $\RealCompletenessUp$ handoff, falsifiable prediction, independence witness, TasteGate marker, and route exclusions jointly form the obligation package for $\DiagonalLimitStabilityUp$ over $\RegSeqRatUp$, $\DiagonalTailSelectorUp$, $\CauchyLimitSealUp$, $\StreamNameUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The package binds selector route exactness through $D$, seal route exactness through $S$, finite-window coverage through $W$, componentwise transport replay through $H$, continuation replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
First use \autoref{thm:diagonal-limit-stability-namecert-obligations}. It projects the accepted carrier and binds the source, representative, selector, seal, finite-window, transport, continuation, provenance, and local naming rows as the complete local $\NameCert$ surface.

Next use \autoref{thm:diagonal-limit-stability-real-completeness-handoff}. It forces every Real-completeness seal read through either the displayed diagonal representative row $D$ or the displayed $\CauchyLimitSealUp$ row $S$, and in both cases replays the same window $W$ and structural rows $H,C,P,N$.

The selector and seal routes are therefore exact at the displayed boundary. Selector exactness is the fact that $D$ is the only diagonal representative row available to the carrier; seal exactness is the fact that $S$ is the only $\RealUp$ seal row available to the carrier. Window coverage is the common row $W$, which is read before downstream consumers receive the sealed route.

Finally inspect the chapter-level side conditions. The falsifiable prediction excludes consumers that bypass both displayed route rows, the independence witness separates the packet from its sibling carriers, the TasteGate marker records single-carrier alignment, and the excluded coordinates rule out quotient streams, host real equality, choice-selected limit representatives, trichotomy, and ambient completeness. These clauses are precisely the obligation package needed before an obligation-closure reading of the same finite packet.
\end{proof}

\begin{theorem}[Diagonal-limit stability selector-seal scope]
\label{thm:diagonal-limit-stability-selector-seal-scope}
For an accepted $\DiagonalLimitStabilityUp$ carrier $T=(R,D,L,S,W,H,C,P,N)$, the row $D$ is read only through $\DiagonalTailSelectorUp$, and the row $S$ is read only through $\CauchyLimitSealUp$, before any $\RealCompletenessUp$ consumer reads the stability comparison. The comparison therefore has no route that bypasses the selector-seal pair.
\end{theorem}
\begin{proof}
Start from \autoref{def:diagonal-limit-stability-carrier} and \autoref{thm:diagonal-limit-stability-real-completeness-handoff}. They expose $D$ as the diagonal representative route and $S$ as the seal route before any Real-completeness read.

Use \autoref{thm:diagonal-limit-stability-representative-seal-comparison} and \autoref{thm:diagonal-limit-stability-window-coverage}. The common window $W$ is the only comparison boundary for the two sides, and every Real-completeness consumer reaches it after choosing $D$ or $S$.

The transport replay and obligation package rows then keep the selector and seal reads inside the same finite packet.
\end{proof}

\begin{theorem}[Diagonal-limit stability primitive route exhaustion]
\label{thm:diagonal-limit-stability-primitive-route-exhaustion}
For an accepted $\DiagonalLimitStabilityUp$ carrier $T=(R,D,L,S,W,H,C,P,N)$, the rows $W,H,C,P,N$ exhaust the primitive $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ data of the selector-seal route. They do not introduce quotient-stream equality, host real equality, a selected limit representative, trichotomy, or ambient completeness.
\end{theorem}
\begin{proof}
Apply \autoref{thm:diagonal-limit-stability-namecert-obligations}. It lists the finite source, representative, selector, seal, window, transport, continuation, provenance, and local naming rows as the local obligation surface.

Then use \autoref{thm:diagonal-limit-stability-transport-replay}. The $H$ and $C$ rows replay the same carrier by componentwise $\hsame$ transport and displayed continuations, while $P$ and $N$ remain provenance and local naming rows.

Together with the carrier, handoff, window coverage, and obligation package, these rows leave no primitive coordinate outside $W,H,C,P,N$ for the selector-seal route.
\end{proof}

\begin{theorem}[Diagonal-limit stability NameCert scope closure]
\label{thm:diagonal-limit-stability-namecert-scope-closure}
The selector-seal scope row and primitive route exhaustion row, combined with the obligation package of \autoref{thm:diagonal-limit-stability-obligation-package}, close the local $\NameCert_{\DiagonalLimitStabilityUp}$ scope for the displayed packet $T=(R,D,L,S,W,H,C,P,N)$. The closed scope is exactly the route through $\RegSeqRatUp$, $\DiagonalTailSelectorUp$, $\CauchyLimitSealUp$, $\StreamNameUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ named in the carrier.
\end{theorem}
\begin{proof}
Use \autoref{thm:diagonal-limit-stability-selector-seal-scope}. It fixes the consumer-visible order of the selector row $D$, seal row $S$, and Real-completeness comparison.

Use \autoref{thm:diagonal-limit-stability-primitive-route-exhaustion}. It fixes the primitive route rows $W,H,C,P,N$ as the complete $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ surface.

Now apply \autoref{thm:diagonal-limit-stability-obligation-package}. Since the carrier, handoff, representative-seal comparison, window coverage, and transport replay have already been packaged for the same finite tuple, the local NameCert scope closes exactly over the displayed selector-seal packet and over nothing else.
\end{proof}

\begin{theorem}[Diagonal-limit stability selector-seal primitive binding]
\label{thm:diagonal-limit-stability-selector-seal-primitive-binding}
For an accepted $\DiagonalLimitStabilityUp$ carrier $T=(R,D,L,S,W,H,C,P,N)$, the row $D$ is bound to the $\DiagonalTailSelectorUp$ primitive surface, and the row $S$ is bound to the $\CauchyLimitSealUp$ primitive surface together with the displayed $\RealUp$ seal row. Both reads are replayed only through the displayed $\BHist$ packet, componentwise $\hsame$ transports, $\Cont$ continuations, $\Pkg$ provenance, and local $\NameCert_{\DiagonalLimitStabilityUp}$ row.
\end{theorem}
\begin{proof}
Use the carrier definition to isolate the two route rows. The coordinate $D$ is the only diagonal representative row available to the packet, and the coordinate $S$ is the only seal row available to the packet.

Apply \autoref{thm:diagonal-limit-stability-selector-seal-scope}. It fixes the consumer-visible order in which the selector row and seal row can be read before a Real-completeness consumer inspects the stability comparison.

Now apply \autoref{thm:diagonal-limit-stability-primitive-route-exhaustion}. The replay rows are exactly the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, so neither the selector read nor the seal read has a second primitive route.
\end{proof}

\begin{theorem}[Diagonal-limit stability window-transport primitive binding]
\label{thm:diagonal-limit-stability-window-transport-primitive-binding}
For an accepted $\DiagonalLimitStabilityUp$ carrier $T=(R,D,L,S,W,H,C,P,N)$, the common finite window $W$ and the replay rows $H,C,P,N$ are the whole primitive transport surface for the selector-seal comparison. This surface contains no quotient stream, host real equality, selected representative, trichotomy row, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Start with \autoref{thm:diagonal-limit-stability-window-coverage}. It forces every Real-completeness consumer to pass through the common window $W$ and the structural rows $H,C,P,N$ after the representative side or the seal side has been chosen.

Use \autoref{thm:diagonal-limit-stability-transport-replay}. The $H$ and $C$ rows replay the same accepted carrier, while $P$ and $N$ keep provenance and local naming attached to that carrier.

The exclusions follow from the carrier definition and from \autoref{thm:diagonal-limit-stability-primitive-route-exhaustion}. Since none of the excluded objects is a coordinate of $T$, it cannot enter the primitive transport surface.
\end{proof}

\begin{theorem}[Diagonal-limit stability scoped NameCert boundary]
\label{thm:diagonal-limit-stability-scoped-namecert-boundary}
The selector-seal primitive binding of \autoref{thm:diagonal-limit-stability-selector-seal-primitive-binding}, the window-transport primitive binding of \autoref{thm:diagonal-limit-stability-window-transport-primitive-binding}, and the local scope closure of \autoref{thm:diagonal-limit-stability-namecert-scope-closure} justify scoped closure for the displayed $\NameCert_{\DiagonalLimitStabilityUp}$ boundary of $T=(R,D,L,S,W,H,C,P,N)$.
\end{theorem}
\begin{proof}
The selector-seal primitive binding fixes the only route by which $D$ and $S$ can be read. It binds the representative side to $\DiagonalTailSelectorUp$ and the seal side to $\CauchyLimitSealUp$ and $\RealUp$.

The window-transport primitive binding fixes the only route by which those reads are transported. It keeps $W,H,C,P,N$ as the finite primitive surface and excludes the analytic and quotient-level coordinates absent from the carrier.

Finally apply \autoref{thm:diagonal-limit-stability-namecert-scope-closure}. Its closed local scope is therefore not merely an obligation list; it is the displayed scoped boundary of the local $\NameCert_{\DiagonalLimitStabilityUp}$ packet.
\end{proof}

\begin{theorem}[Diagonal-limit stability tail-refinement invariance]
\label{thm:diagonal-limit-stability-tail-refinement-invariance}
Let
$$
T=(R,D,L,S,W,H,C,P,N)
$$
and
$$
T'=(R,D',L',S,W',H',C',P',N')
$$
be accepted $\DiagonalLimitStabilityUp$ packets with the same $\RegSeqRatUp$ source row $R$ and the same $\RealUp$ seal row $S$. Suppose $W'$ is a displayed $\StreamNameUp$ tail refinement of $W$, $D'$ is the transported $\DiagonalTailSelectorUp$ row for the refined window, and the row $S$ is read in both packets through the same $\CauchyLimitSealUp$ boundary and the same $\RealUp$ seal boundary. Then every $\RealCompletenessUp$ consumer that reads the sealed diagonal-limit route through $T'$ receives the same sealed $\RealUp$ route as the corresponding read through $T$, now replayed through $W',H',C',P',N'$. The refinement introduces no quotient stream, host real equality, choice-selected representative, trichotomy, or ambient completeness row.
\end{theorem}
\begin{proof}
First compare the two packets at the seal side. By \autoref{thm:diagonal-limit-stability-real-completeness-handoff}, a Real-completeness consumer must enter the accepted packet through the displayed diagonal representative row or the displayed $\CauchyLimitSealUp$ row, and in either case must continue through the packet window and structural rows.

Next identify the representative and seal comparison boundary. The representative-seal comparison of \autoref{thm:diagonal-limit-stability-representative-seal-comparison} says that the representative side and the seal side are compared only through the common finite window. Since $W'$ is a displayed tail refinement of $W$ and $D'$ is the transported selector row for that refined window, the refined packet changes the inspection window but not the sealed $\RealUp$ boundary.

Now use the coverage and replay rows. By \autoref{thm:diagonal-limit-stability-window-coverage}, every consumer read factors through the window and structural rows after choosing the representative side or the seal side. By \autoref{thm:diagonal-limit-stability-transport-replay}, the transports and continuations replay the same carrier coordinates, so the refined rows $H',C',P',N'$ serve the same local route over the refined window $W'$.

Finally apply \autoref{thm:diagonal-limit-stability-namecert-scope-closure}. The closed local scope contains exactly the displayed route through $\RegSeqRatUp$, $\DiagonalTailSelectorUp$, $\CauchyLimitSealUp$, $\StreamNameUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. Therefore the tail-refined packet preserves the sealed RealUp route for every RealCompletenessUp consumer and supplies no additional primitive or analytic coordinate.
\end{proof}

\begin{theorem}[Cofinal tail-refinement confluence]
\label{thm:diagonal-limit-stability-cofinal-refinement-confluence}
Let
$$
T_0=(R,D,L,S,W,H,C,P,N),
$$
$$
T_1=(R,D_1,L_1,S,W_1,H_1,C_1,P_1,N_1),
$$
and
$$
T_2=(R,D_2,L_2,S,W_2,H_2,C_2,P_2,N_2)
$$
be accepted $\DiagonalLimitStabilityUp$ packets with the same $\RegSeqRatUp$ source row $R$ and the same $\RealUp$ seal row $S$. Suppose $W_1$ and $W_2$ are displayed $\StreamNameUp$ tail refinements of $W$, and suppose there is a common cofinal displayed $\StreamNameUp$ tail refinement $W_{12}$ of both $W_1$ and $W_2$ such that the $\DiagonalTailSelectorUp$ rows $D_1$ and $D_2$ transport to the same displayed selector read over $W_{12}$ while the $\CauchyLimitSealUp$ row remains the seal row $S$. Then every $\RealCompletenessUp$ consumer reading through $T_1$ and every $\RealCompletenessUp$ consumer reading through $T_2$ factors through the same sealed $\RealUp$ route replayed over the common window $W_{12}$. The confluence uses only the displayed $\BHist$ packet, componentwise $\hsame$ transports, $\Cont$ continuations, $\Pkg$ provenance, and local $\NameCert_{\DiagonalLimitStabilityUp}$ rows, and it supplies no quotient stream equality, host real equality, selected representative, trichotomy row, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
First compare each accepted refinement with the base packet. Apply \autoref{thm:diagonal-limit-stability-tail-refinement-invariance} to the refinement from $T_0$ to $T_1$, and apply the same theorem to the refinement from $T_0$ to $T_2$. Both readings preserve the sealed $\RealUp$ route because the source row $R$ and the seal row $S$ are shared.

Next force both consumer reads into the common comparison boundary. By \autoref{thm:diagonal-limit-stability-representative-seal-comparison}, the representative side and the seal side of each accepted packet are compared only through the displayed finite window. By \autoref{thm:diagonal-limit-stability-window-coverage}, every $\RealCompletenessUp$ consumer reaches the window and structural rows after choosing the representative side or the seal side. Since $W_{12}$ is cofinal over both $W_1$ and $W_2$, the two refined reads pass through the same displayed window boundary after transporting $D_1$ and $D_2$.

Now replay the transported packets over that boundary. Use \autoref{thm:diagonal-limit-stability-transport-replay} to identify the componentwise $\hsame$ rows and displayed $\Cont$ routes as replays of the same carrier coordinates. The rows $H_1,C_1,P_1,N_1$ and $H_2,C_2,P_2,N_2$ therefore contribute only the displayed transport, continuation, provenance, and local naming data over $W_{12}$, while the seal side remains $S$.

Finally apply \autoref{thm:diagonal-limit-stability-namecert-scope-closure}. The closed local scope is exactly the route through $\RegSeqRatUp$, $\DiagonalTailSelectorUp$, $\CauchyLimitSealUp$, $\StreamNameUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. Thus both accepted refinements factor through the same sealed $\RealUp$ route over $W_{12}$, with no additional analytic, quotient, representative-choice, trichotomy, or completeness coordinate.
\end{proof}

\begin{theorem}[Diagonal-limit stability public export]
\label{thm:diagonal-limit-stability-public-export}
An accepted $\DiagonalLimitStabilityUp$ packet
$$
T=(R,D,L,S,W,H,C,P,N)
$$
together with the selector-seal primitive binding, the window-transport primitive binding, the scoped $\NameCert_{\DiagonalLimitStabilityUp}$ boundary, tail-refinement invariance, and cofinal refinement confluence exports a public $\RealUp$-completeness-facing route from $\RegSeqRatUp$ through $\DiagonalTailSelectorUp$ and $\CauchyLimitSealUp$ to the sealed $\RealUp$ row. The export is exactly the displayed route through $R,D,L,S,W,H,C,P,N$ and supplies no quotient-stream equality, host real equality, choice-selected representative, trichotomy, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
First use \autoref{thm:diagonal-limit-stability-selector-seal-primitive-binding} and \autoref{thm:diagonal-limit-stability-window-transport-primitive-binding}. They bind the representative side to the $\DiagonalTailSelectorUp$ primitive surface, the seal side to the $\CauchyLimitSealUp$ and $\RealUp$ primitive surfaces, and the comparison route to the finite window and replay rows.

Next apply \autoref{thm:diagonal-limit-stability-scoped-namecert-boundary}. The scoped boundary turns those primitive bindings into the displayed local $\NameCert_{\DiagonalLimitStabilityUp}$ surface for the same accepted packet $T$.

Finally use \autoref{thm:diagonal-limit-stability-tail-refinement-invariance} and \autoref{thm:diagonal-limit-stability-cofinal-refinement-confluence}. Tail refinement and cofinal comparison preserve the sealed $\RealUp$ route through the same public-facing selector-seal corridor, while the carrier exclusions keep quotient-stream equality, host real equality, selected representatives, trichotomy, and ambient completeness outside the export.
\end{proof}

\closureat{DiagonalLimitStabilityUp}{seedStr}
\closureat{DiagonalLimitStabilityUp}{obligationStr}[BEDC.Derived.DiagonalLimitStabilityUp.DiagonalLimitStabilityTasteGate\_single\_carrier\_alignment]
\closureat{DiagonalLimitStabilityUp}{scopedStr}[BEDC.Derived.DiagonalLimitStabilityUp.DiagonalLimitStabilityTasteGate\_single\_carrier\_alignment]
\closureat{DiagonalLimitStabilityUp}{publicStr}[BEDC.Derived.DiagonalLimitStabilityUp.DiagonalLimitStabilityTasteGate\_single\_carrier\_alignment]

\begin{closurestatus}{\DiagonalLimitStabilityUp}
  \constructivestory{A $\DiagonalLimitStabilityUp$ packet is generated from a $\RegSeqRatUp$ source row, diagonal representative row, limit selector row, $\RealUp$ seal row, finite $\StreamNameUp$ window, componentwise $\hsame$ transports, displayed $\Cont$ continuations, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\publicClosure}
  \scopeclosed{\autoref{thm:diagonal-limit-stability-scoped-namecert-boundary} binds the selector row, seal row, finite-window transport, replay surface, provenance, and local NameCert row to the displayed BEDC primitives, and \autoref{thm:diagonal-limit-stability-public-export} exports the resulting route as the public Real-completeness-facing selector-seal corridor.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.DiagonalLimitStabilityUp.DiagonalLimitStabilityTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \origin{ai}
  \leanchecked{BEDC.Derived.DiagonalLimitStabilityUp.taste\_gate}
  \notclaimed{This chapter does not prove quotient-stream equality, host real equality, a choice-selected limit representative, trichotomy, ambient completeness, or a public standard bridge.}
  \upgradepath{The next paper step is a standard bridge or a mature cross-chapter consumer package; the public selector-seal route remains limited to the displayed packet.}
\end{closurestatus}
