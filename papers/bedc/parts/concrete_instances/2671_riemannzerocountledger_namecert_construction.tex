\chapter{A Concrete Naming Certificate for $\RiemannZeroCountLedgerUp$}
\label{ch:concrete-instances-riemann-zero-count-ledger-namecert}
\origin{human}

The $\RiemannZeroCountLedgerUp$ interface records the finite height-window packet needed by the zero-count witness direction of \autoref{thm:rh-witness-zero-count}. It packages a requested imaginary-height bound, the visible critical-strip zero packets below that height, a unary count row, a Riemann-von Mangoldt comparison ledger, an explicit error row, transport and continuation routes, package provenance, and local naming data. The packet is a counting witness surface only: it does not prove $\mathsf{RH}$, does not assert zero-set completeness, and does not supply analytic continuation.

The ledger consumes its zero-side inputs through existing concrete-instance surfaces. The critical-line route is supplied by \autoref{ch:concrete-instances-criticallinewitness-namecert}, while the zero packet source is supplied by \autoref{ch:concrete-instances-zeta-zeros-namecert}; the count ledger therefore records a dependency route already visible in its carrier rows rather than a standalone RH-side object.

\begin{definition}[RiemannZeroCountLedger carrier]
\label{def:riemann-zero-count-ledger-carrier}
A $\RiemannZeroCountLedgerUp$ carrier is a finite $\BHist$ packet
$$
L=(T,Z,U,M,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the $\NatUp$ height request row naming the imaginary-height window $|\Im(s)| \le T$;
\item $Z$ is the finite packet list of $\CriticalStripZetaZeroWitnessUp$ rows whose zero and critical-strip coordinates are displayed inside the requested height window;
\item $U$ is the $\UnaryHistory$ count row assigned to the visible list $Z$;
\item $M$ is the Riemann-von Mangoldt comparison ledger, recording the main-term row, logarithmic row, normalization row, and comparison route used for the same height request;
\item $E$ is the explicit error row bounding the gap between the unary count $U$ and the comparison ledger $M$;
\item $H$ stores componentwise $\hsame$ transports for the height request, zero packet list, unary count row, comparison ledger, error row, package provenance, and local naming row;
\item $C$ stores the displayed $\Cont$ routes for reading the height request, filtering the finite zero packet list, counting the list, comparing against $M$, exposing the error row, and handing the result to an RH-facing consumer;
\item $P$ is the $\Pkg$ provenance over $\NatUp$, $\UnaryHistory$, $\CriticalStripZetaZeroWitnessUp$, $\ZetaZerosUp$, $\CritStripUp$, $\RealUp$, $\RatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RiemannZeroCountLedgerUp}$ row.
\end{enumerate}
The carrier exposes only a finite height-window count ledger. It contains no completed zero-set quotient, no proof that the listed packets exhaust all analytic zeros below the height, no analytic-continuation constructor, and no proof of $\mathsf{RH}$.
\end{definition}
\leanchecked{BEDC.Derived.RiemannZeroCountLedgerUp.RiemannZeroCountLedgerTasteGate\_single\_carrier\_alignment}

\begin{theorem}[RiemannZeroCountLedger NameCert obligations]
\label{thm:riemann-zero-count-ledger-namecert-obligations}
The local $\NameCert_{\RiemannZeroCountLedgerUp}$ surface for the carrier of \autoref{def:riemann-zero-count-ledger-carrier} consists of six finite obligations:
\begin{enumerate}
\item carrier admission: the accepted object is the displayed $\BHist$ packet $L=(T,Z,U,M,E,H,C,P,N)$;
\item height-window binding: the height request row $T$ is the only row used to filter the critical-strip zero packets in $Z$;
\item zero-list visibility: every counted entry is a displayed $\CriticalStripZetaZeroWitnessUp$ packet with its $\ZetaZerosUp$ and $\CritStripUp$ coordinates still visible;
\item unary-count exactness: the count row $U$ is the $\UnaryHistory$ count of the displayed finite list $Z$ and not a cardinality of an ambient zero set;
\item comparison-error ledger: the Riemann-von Mangoldt comparison row $M$ and explicit error row $E$ are both read before the count is exported;
\item non-escape: every downstream zero-count read factors through $T,Z,U,M,E,H,C,P,N$ and has no route to zero-set completeness, analytic continuation, host cardinality, or a proof of $\mathsf{RH}$.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.RiemannZeroCountLedgerUp.RiemannZeroCountLedgerCarrier\_namecert\_obligations}
\begin{proof}
Carrier admission is projection from \autoref{def:riemann-zero-count-ledger-carrier}. The height request, zero packet list, unary count, comparison ledger, error row, transports, continuations, provenance, and local naming row are explicit coordinates.

Height-window binding and zero-list visibility are componentwise reads. The route first reads $T$, then filters the visible $\CriticalStripZetaZeroWitnessUp$ rows in $Z$, while each listed zero keeps its $\ZetaZerosUp$ and $\CritStripUp$ coordinates visible through the stored transport and continuation rows.

The count and comparison obligations are the rows $U,M,E$. The count is the unary count of the displayed list, and the comparison is only the finite Riemann-von Mangoldt ledger with its explicit error row.

Since the carrier has no coordinate for an ambient zero set, analytic continuation, host cardinality, zero-set completeness, or an RH proof, every downstream read is exhausted by the displayed rows.
\end{proof}

\begin{theorem}[RiemannZeroCountLedger window exhaustion]
\label{thm:riemann-zero-count-ledger-window-exhaustion}
Every RH-facing zero-count read exported by an accepted $\RiemannZeroCountLedgerUp$ carrier can inspect only
$$
T,Z,U,M,E,H,C,P,N.
$$
It may read the requested height, the displayed finite list of critical-strip zero packets, the unary count, the Riemann-von Mangoldt comparison ledger, the explicit error row, componentwise $\hsame$ transports, displayed $\Cont$ routes, package provenance, and local $\NameCert$ row. It cannot infer that the finite list contains all analytic zeros below the height, cannot construct analytic continuation, cannot read a host zero-set cardinality, and cannot close $\mathsf{RH}$.
\end{theorem}
\leanchecked{BEDC.Derived.RiemannZeroCountLedgerUp.RiemannZeroCountLedgerCarrier\_window\_exhaustion}
\begin{proof}
Start with an RH-facing zero-count request. By the carrier definition, the only height input is $T$, and the only zero data available at that height is the finite packet list $Z$.

Apply the zero-list and unary-count obligations of \autoref{thm:riemann-zero-count-ledger-namecert-obligations}. They force the count read to pass through displayed $\CriticalStripZetaZeroWitnessUp$ packets and then through the unary count row $U$.

Next read the comparison surface. The rows $M$ and $E$ record the Riemann-von Mangoldt comparison and the explicit constructive error before the result is exported.

The remaining rows $H,C,P,N$ provide transport, replay, provenance, and local naming only. Therefore the height-window surface is exhausted by the displayed packet and has no route to zero-set completeness, analytic continuation, host cardinality, or a proof of the RH predicate.
\end{proof}

\begin{theorem}[RiemannZeroCountLedger height-filter stability]
\label{thm:riemann-zero-count-ledger-height-filter-stability}
For every accepted $\RiemannZeroCountLedgerUp$ carrier $L=(T,Z,U,M,E,H,C,P,N)$, a transported zero-count read preserves the displayed height request row $T$ before the finite zero packet list $Z$ is filtered. The componentwise $\hsame$ row $H$ and displayed $\Cont$ replay row $C$ may transport and replay the height request, but they cannot replace $T$ by a new height window, a host real bound, or an untracked zero-set predicate.
\end{theorem}
\leanchecked{BEDC.Derived.RiemannZeroCountLedgerUp.RiemannZeroCountLedgerCarrier\_height\_filter\_stability}
\begin{proof}
Project the carrier of \autoref{def:riemann-zero-count-ledger-carrier}. The height request is the coordinate $T$, and the zero packet list $Z$ is filtered only after that coordinate is read.

Apply the height-window binding obligation of \autoref{thm:riemann-zero-count-ledger-namecert-obligations}. It makes $T$ the unique displayed row used by the finite filter, so a downstream read cannot introduce a second height source.

Now read the transport and replay rows. The componentwise $\hsame$ entries in $H$ preserve the height row as the same displayed coordinate, while the $\Cont$ route in $C$ replays the read of $T$ before the filter on $Z$.

Therefore the transported packet still filters $Z$ under the original displayed height row. Since no carrier coordinate stores a host real bound or ambient zero-set predicate, those endpoints cannot be inferred by transport.
\end{proof}

\begin{theorem}[RiemannZeroCountLedger comparison-error nonescape]
\label{thm:riemann-zero-count-ledger-comparison-error-nonescape}
Every RH-facing export of an accepted $\RiemannZeroCountLedgerUp$ carrier must read the Riemann-von Mangoldt comparison ledger $M$ and explicit error row $E$ before the unary count row $U$ is handed to a consumer. The export is only a finite comparison-error ledger for the displayed height window; it cannot imply $\mathsf{RH}$, analytic continuation, zero-set completeness, host cardinality, or completeness of the analytic zero list.
\end{theorem}
\leanchecked{BEDC.Derived.RiemannZeroCountLedgerUp.RiemannZeroCountLedgerCarrier\_comparison\_error\_nonescape}
\begin{proof}
Start with the exported zero-count read of \autoref{thm:riemann-zero-count-ledger-window-exhaustion}. That theorem exhausts the read by the displayed rows $T,Z,U,M,E,H,C,P,N$.

Use the comparison-error ledger obligation in \autoref{thm:riemann-zero-count-ledger-namecert-obligations}. It requires both $M$ and $E$ to be opened before the count is exported, so the consumer receives a comparison row together with its explicit error boundary.

The rows $M$ and $E$ compare the unary count against the finite Riemann-von Mangoldt ledger for the same displayed height request. They do not add a coordinate for analytic continuation, all zeros below the height, host cardinality, or a proof of the RH predicate.

Thus the export is non-escaping: it records the comparison-error surface for the finite packet and refuses each analytic endpoint named in the statement.
\end{proof}

\begin{theorem}[RiemannZeroCountLedger zero-list count transport]
\label{thm:riemann-zero-count-ledger-zero-list-count-transport}
Let $L=(T,Z,U,M,E,H,C,P,N)$ and $L'=(T',Z',U',M',E',H',C',P',N')$ be accepted $\RiemannZeroCountLedgerUp$ carriers connected by the componentwise $\hsame$ transport and displayed $\Cont$ replay rows of their local packets. If the transported read identifies the height request, zero-list row, and unary-count row with the displayed $T,Z,U$ rows of $L$, then the transported packet preserves the same finite zero list $Z$ and the same unary count $U$ before any comparison-error export is consumed.
\end{theorem}
\begin{proof}
Project both accepted carriers. In each packet, the zero-list row and unary-count row are displayed coordinates, not ambient zero-set or host-cardinality data.

Apply zero-list visibility and unary-count exactness from \autoref{thm:riemann-zero-count-ledger-namecert-obligations}. These obligations force the count to be the $\UnaryHistory$ count of the displayed finite list and force every counted entry to remain a visible $\CriticalStripZetaZeroWitnessUp$ packet.

Now use the hypothesis on the componentwise transport and replay rows. The $\hsame$ entries identify the transported height, zero-list, and count rows with $T,Z,U$, and the $\Cont$ entries replay those reads before the comparison rows are consumed.

Consequently the transported export preserves the displayed finite list and its unary count. Any attempt to read a different finite list, host cardinality, zero-set completeness theorem, or analytic continuation would bypass the named coordinates and would no longer be a transported read of the accepted packet.
\end{proof}
\leanchecked{BEDC.Derived.RiemannZeroCountLedgerUp.RiemannZeroCountLedgerCarrier\_zero\_list\_count\_transport}

\closureat{RiemannZeroCountLedgerUp}{seedStr}

\begin{closurestatus}{\RiemannZeroCountLedgerUp}
  \constructivestory{A $\RiemannZeroCountLedgerUp$ packet is generated from a $\NatUp$ height request, a finite list of $\CriticalStripZetaZeroWitnessUp$ packets, a $\UnaryHistory$ count row, a Riemann-von Mangoldt comparison ledger, an explicit error row, displayed $\hsame$ transports, $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{The seed scope records the finite height-window zero-count carrier, local naming obligations, unary-count exactness, comparison-error ledger, and RH-facing readback boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not prove $\mathsf{RH}$, construct analytic continuation, enumerate all analytic zeros, close zero-set completeness, assert host cardinality, or provide bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, height-window binding, zero-list visibility, unary-count exactness, comparison-error ledger exactness, and non-escape for the displayed zero-count packet.}
\end{closurestatus}
