\chapter{A Concrete Naming Certificate for $\FirmNonexpansiveProjectionUp$}
\label{ch:concrete-instances-firm-nonexpansive-projection-namecert}
\origin{human}

Following Bruck, Reich, and standard Hilbert projection theory, the $\FirmNonexpansiveProjectionUp$ packet records the finite BEDC surface by which a closed convex Hilbert projection can be consumed as a firm nonexpansive operator. It sits after the projection endpoint of \autoref{ch:concrete-instances-metricprojection-namecert}, the finite projection comparison route of \autoref{ch:concrete-instances-finite-metric-projection-namecert}, the proximal handoff of \autoref{ch:concrete-instances-proximal-map-namecert}, the nonexpansive regular Cauchy route of \autoref{ch:concrete-instances-regular-cauchy-nonexpansive-map-namecert}, and the Chebyshev-center boundary of \autoref{ch:concrete-instances-chebyshev-center-namecert}. The packet names the firm inequality ledger over $\HilbertUp$, $\ConvexSetUp$, $\CompleteMetricUp$, and $\RealUp$ rows; it does not import a host projection map, a choice-selected nearest point, quotient Hilbert equality, or an ambient Hilbert projection theorem.

\begin{definition}[Firm nonexpansive projection carrier]\label{def:firm-nonexpansive-projection-carrier}
A $\FirmNonexpansiveProjectionUp$ carrier is a finite $\BHist$ packet
$$
Q=(H,K,M,E,S,D,L,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $H$ is the $\HilbertUp$ source row, exposing the inner-product, norm, and induced metric ledgers needed for projection-facing comparisons.
\item $K$ is the $\ConvexSetUp$ source row, recording the closed convex subset whose projection endpoint is being read.
\item $M$ is the $\MetricProjectionUp$ endpoint row, carrying the nearest-point endpoint only through the finite route of \autoref{def:metric-projection-carrier}.
\item $E$ is the $\CompleteMetricUp$ service row used to replay the minimizing-window endpoint admission without adding an ambient completeness principle.
\item $S$ is the $\RealUp$ and RegSeqRat distance-readback row, exposing squared-distance and inner-product comparisons only through displayed rational windows.
\item $D$ is the inequality ledger for the finite firm-nonexpansive comparison, recording that every inspected pair of inputs and projected endpoints is read through the displayed relation
$$
\langle p_x-p_y,x-y\rangle \geq \lVert p_x-p_y\rVert^2 .
$$
\item $L$ records componentwise $\hsame$ transport for the Hilbert, convex, projection endpoint, completeness service, real readback, inequality ledger, replay, provenance, and local naming rows.
\item $T$ records displayed $\Cont$ replay from projection endpoint reads through the real comparison ledger and the firm inequality ledger.
\item $P$ is the $\Pkg$ provenance over $\FirmNonexpansiveProjectionUp$, $\MetricProjectionUp$, $\HilbertUp$, $\ConvexSetUp$, $\CompleteMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FirmNonexpansiveProjectionUp}$ row.
\end{enumerate}
The local classifier compares accepted carriers only by these rows. It has no coordinate for a host projector, selected nearest point, quotient equality of Hilbert points, trichotomy on real comparisons, countable choice, or a global fixed-point theorem.
\end{definition}

\begin{theorem}[FirmNonexpansiveProjection NameCert obligations]\label{thm:firm-nonexpansive-projection-namecert-obligations}
For an accepted $\FirmNonexpansiveProjectionUp$ carrier $Q=(H,K,M,E,S,D,L,T,C,P,N)$, the local $\NameCert_{\FirmNonexpansiveProjectionUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the Hilbert source, closed convex source, MetricProjection endpoint, CompleteMetric service, real distance readback, inequality ledger, transport, replay, provenance, and local naming rows;
\item endpoint dependency, requiring every projected point used by the firm inequality to be read first through the $\MetricProjectionUp$ endpoint row $M$;
\item real readback exactness, requiring every squared-distance and inner-product comparison to pass through the shared $\RealUp$ and RegSeqRat row $S$;
\item firm inequality visibility, requiring the ledger $D$ to display the finite comparison before any consumer treats the projection as nonexpansive;
\item nonescape from host projection maps, choice-selected nearest points, quotient Hilbert equality, trichotomy, countable choice, and ambient fixed-point principles.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the accepted carrier. The rows $H,K,M,E,S,D$ are the complete mathematical surface, while $L,T,C,P,N$ transport, replay, package, and name the same displayed packet.

The endpoint dependency is inherited from \autoref{thm:metric-projection-namecert-obligations}. The projection endpoint is read through the Hilbert source, closed convex source, distance ledger, minimizing window, and endpoint row before the firm comparison ledger can inspect it.

The real readback and firm inequality rows are then finite. The row $S$ supplies the displayed real and regular-rational comparisons, and $D$ records the inspected inequality
$$
\langle p_x-p_y,x-y\rangle \geq \lVert p_x-p_y\rVert^2 .
$$

Transport, replay, provenance, and local naming preserve this route. Since no carrier row stores a host projector, a selected nearest point, quotient Hilbert equality, trichotomy, countable choice, or a fixed-point theorem, those principles cannot be exported by the local certificate.
\end{proof}

\begin{theorem}[FirmNonexpansiveProjection MetricProjection handoff]\label{thm:firm-nonexpansive-projection-metric-projection-handoff}
Every consumer that uses an accepted $\FirmNonexpansiveProjectionUp$ packet as a nonexpansive projection must first read the $\MetricProjectionUp$ endpoint row $M$ and the firm inequality ledger $D$. The consumer may then use the projection only through the displayed route
$$
H,K,M,E,S,D \longmapsto L,T,C,P,N .
$$
It receives no host Hilbert projection map, no selected nearest-point operator, no quotient equality of projected points, no trichotomy decision for real comparisons, and no global fixed-point conclusion.
\end{theorem}
\begin{proof}
Start with the projected endpoint. By \autoref{def:firm-nonexpansive-projection-carrier}, endpoint data enter the packet only through the $\MetricProjectionUp$ row $M$, and \autoref{thm:metric-projection-closest-point-nonescape} keeps that row inside the finite projection route.

Next read the inequality ledger. The row $D$ may compare two projected endpoints only after $H,K,M,E,S$ are displayed, so a nonexpansive use cannot bypass the Hilbert source, closed convex source, complete metric replay, or real readback.

The structural rows $L,T,C,P,N$ only transport, replay, source, and name the same finite data. Thus the handoff exports a firm nonexpansive projection certificate, not a host projector, selected nearest-point operator, quotient equality principle, trichotomy decision, or fixed-point theorem.
\end{proof}

\falsifiablePrediction{Every accepted $\FirmNonexpansiveProjectionUp$ consumer read factors through a HilbertUp source, ConvexSetUp closed convex source, MetricProjectionUp endpoint row, CompleteMetricUp replay service, RealUp and RegSeqRat distance readback, finite firm inequality ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert.}

\independenceWitness{metricprojection, finite-metric-projection, proximal-map, regular-cauchy-nonexpansive-map, chebyshev-center: $\FirmNonexpansiveProjectionUp$ is not bijective to any listed sibling because it jointly carries a MetricProjection endpoint, a CompleteMetric replay service, a RealUp distance-readback row, and an explicit firm inequality ledger before any nonexpansive consumer read.}

\closureat{FirmNonexpansiveProjectionUp}{seedStr}

\begin{closurestatus}{\FirmNonexpansiveProjectionUp}
  \constructivestory{A $\FirmNonexpansiveProjectionUp$ packet is generated from a Hilbert source, closed convex source, MetricProjection endpoint row, CompleteMetric replay service, RealUp and RegSeqRat distance readback, finite firm inequality ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:firm-nonexpansive-projection-carrier}, \autoref{thm:firm-nonexpansive-projection-namecert-obligations}, and \autoref{thm:firm-nonexpansive-projection-metric-projection-handoff} seed the finite firm-projection certificate surface over Hilbert, closed convex, metric projection, complete metric, real readback, and inequality-ledger rows.}
  \formalstatus{\unformalizedV}
  \leantarget{BEDC.Derived.FirmNonexpansiveProjectionUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, a host Hilbert projection map, selected nearest points, quotient equality of Hilbert points, trichotomy, countable choice, a Krasnoselskii--Mann convergence theorem, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, endpoint dependency, real readback exactness, firm inequality visibility, transport, replay, provenance, and local naming for BEDC.Derived.FirmNonexpansiveProjectionUp.}
\end{closurestatus}
