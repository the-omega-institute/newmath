\chapter{A Concrete Naming Certificate for $\BishopLocatedOscillationUp$}
\label{ch:concrete-instances-bishop-located-oscillation-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopLocatedOscillationUp$ packet records the finite NameCert route by which a continuous map on compact metric data exposes located oscillation bounds through dyadic windows and regular rational readback. It sits beside the compact finite-net carrier of \autoref{ch:concrete-instances-compactmetric-namecert}, the map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, the dyadic and stream naming rows of \autoref{ch:concrete-instances-dyadicratcore-namecert} and \autoref{ch:concrete-instances-streamname-namecert}, the regular rational and Real readback rows of \autoref{ch:concrete-instances-regseqrat-namecert} and \autoref{ch:concrete-instances-real-namecert}, and the explicit oscillation schedule of \autoref{ch:concrete-instances-oscillation-modulus-namecert}. The packet is an L10 finite-window certificate: it does not select extrema, invoke quotient equality, use Real completeness, or import choice.

\begin{definition}[Bishop located oscillation carrier]
\label{def:bishop-located-oscillation-carrier}
A $\BishopLocatedOscillationUp$ carrier is a finite $\BHist$ packet
$$
L_{\mathrm{osc}}=(K,F,D,S,Q,R,O,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net row, exposing only the bundled compact carrier, its finite coverage histories, and the named metric comparisons used by the oscillation request.
\item $F$ is the $\ContinuousMapUp$ row, recording the map whose local oscillation is read at the displayed compact windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, carrying the target oscillation scale and the source-window cuts.
\item $S$ is the $\StreamNameUp$ row naming the finite stream of windows inspected by the request.
\item $Q$ is the $\RegSeqRatUp$ regular rational readback row attached to those windows.
\item $R$ is the $\RealUp$ seal reached only after the stream and regular rational rows have been displayed.
\item $O$ is the $\OscillationModulusUp$ row translating the local oscillation read into an explicit tolerance-window schedule.
\item $W$ is the located window row, the finite ledger that records which compact windows have been inspected and which dyadic bound each read returns.
\item $H$ records componentwise $\hsame$ transport for the compact, map, dyadic, stream, regular-rational, Real, oscillation-modulus, located-window, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a tolerance request through $K,F,D,S,Q,R,O,W$ and back to the exported located oscillation row.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedOscillationUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\OscillationModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopLocatedOscillationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these twelve rows and the displayed routes among them. It has no coordinate for selected extrema, quotient equality of streams, open-cover compactness, Real completeness, countable choice, or bridge-axis evidence.
\end{definition}

\begin{theorem}[Bishop located oscillation NameCert obligations]
\label{thm:bishop-located-oscillation-namecert-obligations}
For every accepted $\BishopLocatedOscillationUp$ carrier $L_{\mathrm{osc}}=(K,F,D,S,Q,R,O,W,H,C,P,N)$, the local $\NameCert_{\BishopLocatedOscillationUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the compact metric row, continuous map row, dyadic tolerance row, stream-name row, regular rational row, Real seal, oscillation-modulus row, located-window row, transport row, replay row, provenance row, and local naming row;
\item located-window coverage, requiring each oscillation read to name one displayed compact window from $W$ before any Real-facing value is exposed;
\item dyadic precision visibility, requiring the target tolerance and source-window cut to be read from $D$ and then replayed through $S,Q,R$;
\item oscillation-modulus handoff, requiring $O$ to consume the same $K,F,D,S,Q,R,W$ route before exporting the tolerance-window schedule;
\item ledger non-escape, requiring $H,C,P,N$ to export no selected extrema, quotient equality of streams, open-cover compactness, Real completeness, countable choice, or bridge-axis evidence.
\end{enumerate}
No local obligation is stored outside those rows.
\end{theorem}
\begin{proof}
Project the accepted carrier. Its analytic rows are exactly $K,F,D,S,Q,R,O,W$, and its structural rows are exactly $H,C,P,N$.

Carrier admission is therefore componentwise. The compact row supplies only the finite coverage histories, the map row supplies only the named continuous-map read, and the dyadic, stream, regular rational, and Real rows supply the displayed readback route.

The located-window row is the only place where a compact window is named for the oscillation request. The oscillation-modulus row consumes that same route, so the exported schedule cannot bypass $K,F,D,S,Q,R,W$.

Transport, replay, provenance, and local naming preserve the same rows. Since no row stores selected extrema, quotient equality, open-cover compactness, Real completeness, countable choice, or bridge data, none of those objects can become a local obligation.
\end{proof}

\begin{theorem}[Bishop located oscillation finite-window handoff]
\label{thm:bishop-located-oscillation-finite-window-handoff}
Every Real-facing or compact-uniform consumer of an accepted $\BishopLocatedOscillationUp$ packet reads oscillation data through the finite route
$$
\CompactMetricUp,\ \ContinuousMapUp,\ \DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp,\ \OscillationModulusUp,\ \BishopLocatedOscillationUp .
$$
Consequently the consumer receives a located finite-window oscillation certificate and not a selected maximum, quotient equality of streams, open-cover compactness theorem, Real completeness principle, countable-choice schedule, or bridge certificate.
\end{theorem}
\begin{proof}
Begin with the accepted packet. By \autoref{def:bishop-located-oscillation-carrier}, every consumer-visible read of oscillation data must pass through the compact row $K$, the map row $F$, the dyadic tolerance row $D$, the stream row $S$, the regular rational row $Q$, the Real seal $R$, the oscillation-modulus row $O$, and the located-window ledger $W$.

The replay row $C$ orders the route from the tolerance request to the exported schedule, while $H$ transports the same components and $P,N$ source and name them. Thus a Real-facing read and a compact-uniform read see the same finite-window handoff.

Because the carrier has no coordinate for selected extrema, quotient stream equality, open-cover compactness, Real completeness, choice, or bridge data, the handoff cannot import those principles.
\end{proof}

\falsifiablePrediction{Every $\BishopLocatedOscillationUp$ consumer read factors through one CompactMetric row, one ContinuousMap row, one DyadicRatCore tolerance row, one StreamName window row, one RegSeqRat readback row, one RealUp seal, one OscillationModulus row, one located-window ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert.}

\independenceWitness{compactmetric, continuousmap, dyadicratcore, streamname, regseqrat, real, oscillation-modulus: $\BishopLocatedOscillationUp$ is not bijective to any listed sibling because it jointly carries compact finite-window admission, continuous-map oscillation reads, dyadic precision, stream naming, regular rational readback, Real sealing, oscillation-modulus export, located-window bookkeeping, transport, replay, provenance, and local naming.}

\closureat{BishopLocatedOscillationUp}{seedStr}

\begin{closurestatus}{\BishopLocatedOscillationUp}
  \constructivestory{A $\BishopLocatedOscillationUp$ packet is generated from CompactMetric finite-window data, a ContinuousMap row, a DyadicRatCore tolerance row, a StreamName window row, a RegSeqRat readback row, a RealUp seal, an OscillationModulus row, a located-window ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-located-oscillation-carrier}, \autoref{thm:bishop-located-oscillation-namecert-obligations}, and \autoref{thm:bishop-located-oscillation-finite-window-handoff} seed the finite-window located oscillation certificate used by Real-facing and compact-uniform consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected extrema, quotient equality of streams, open-cover compactness, Real completeness, countable choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located-window coverage, dyadic precision visibility, oscillation-modulus handoff, transport stability, replay stability, provenance exhaustion, and local naming for BEDC.Derived.BishopLocatedOscillationUp.}
\end{closurestatus}
