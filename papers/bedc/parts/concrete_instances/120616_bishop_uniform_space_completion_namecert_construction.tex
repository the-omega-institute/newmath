\chapter{A Concrete Naming Certificate for $\BishopUniformSpaceCompletionUp$}
\label{ch:concrete-instances-bishop-uniform-space-completion-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopUniformSpaceCompletionUp$ packet records a constructive uniform-space completion as finite BEDC NameCert data. It sits after the finite observation discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, and the entourage completion surface of \autoref{ch:concrete-instances-entourage-completion-namecert}. The packet gives the L10 completion route a located Cauchy-filter basis, regular observation windows, dense embedding rows, separated classifier rows, and structural provenance without quotienting filters, selecting representatives, invoking countable choice, or importing host equality.

\begin{definition}[Bishop uniform-space completion carrier]
\label{def:bishop-uniform-space-completion}
A $\BishopUniformSpaceCompletionUp$ carrier is a finite $\BHist$ packet
$$
U=(X,L,F,W,R,D,E,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the source $\UniformSpaceUp$ or $\MetricUp$ row, exposing the finite entourage or metric requests that the completion consumer may inspect.
\item $L$ is the located uniformity row, recording finite positive-radius or entourage-location evidence before any Cauchy-filter read is made.
\item $F$ is the $\CauchyFilterBasisUp$ or $\CauchyFilterUp$ row, carrying the located Cauchy-filter base through displayed finite requests.
\item $W$ is the regular finite observation-window row over $\StreamNameUp$, and $R$ is the matching $\RegSeqRatUp$ readback row.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used to compare finite observations and filter requests.
\item $E$ is the dense embedding row from the source carrier into the completion packet; it records graph membership before equality or separation is read.
\item $S$ is the separated classifier row, comparing accepted completion names only through the displayed located Cauchy-filter base, observation windows, readback rows, tolerance ledger, and dense embedding row.
\item $H$ records componentwise $\hsame$ transport for $X,L,F,W,R,D,E,S$ and for the structural rows.
\item $C$ records displayed $\Cont$ replay from a completion request through the located filter base, observation windows, readback, tolerance, dense embedding, and separated classifier.
\item $P$ is the $\Pkg$ provenance over $\BishopUniformSpaceCompletionUp$, $\UniformSpaceUp$, $\MetricUp$, $\CauchyFilterBasisUp$, $\CauchyFilterUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\EntourageCompletionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopUniformSpaceCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for quotient filter equality, host uniform-space equality, selected limits, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop uniform-space completion NameCert obligations]
\label{thm:bishop-uniform-space-completion-namecert-obligations}
For every accepted $\BishopUniformSpaceCompletionUp$ carrier $U=(X,L,F,W,R,D,E,S,H,C,P,N)$, the local $\NameCert_{\BishopUniformSpaceCompletionUp}$ surface consists of five rows:
\begin{enumerate}
\item source and located-uniformity admission, requiring $X$ and $L$ to display every entourage, metric, or positive-radius request before the completion route is read;
\item located Cauchy-filter basis exposure, requiring $F$ to carry only finite filter-base requests over the displayed source and located rows;
\item regular finite observation, requiring $W,R,D$ to expose StreamName windows, RegSeqRat readback, and DyadicRatCore tolerances for the same filter requests;
\item dense embedding and separated classifier exactness, requiring $E$ and $S$ to compare completion names through the displayed source graph, located Cauchy-filter base, observation windows, and tolerance ledger;
\item structural provenance, requiring $H,C,P,N$ to transport, replay, source, and locally name the same packet without adding quotient filters, selected representatives, countable choice, host equality, or ambient completeness.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-uniform-space-completion}. The displayed mathematical rows are $X,L,F,W,R,D,E,S$, and the structural rows are $H,C,P,N$.

The source and located rows $X,L$ precede every Cauchy-filter request. The filter-base row $F$ is therefore read as a finite located basis rather than as a completed quotient object.

The observation rows $W,R,D$ bind every inspected request to finite StreamName windows, regular rational readback, and dyadic tolerances. Dense embedding and separation are then read through $E,S$, so equality-sensitive completion reads remain attached to displayed evidence.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since the carrier has no coordinate for quotient filters, selected limits, countable choice, host equality, or ambient completeness, the five listed rows exhaust the local certificate surface.
\end{proof}

\begin{theorem}[Bishop uniform-space completion dense image]
\label{thm:bishop-uniform-space-completion-dense-image}
Every accepted $\BishopUniformSpaceCompletionUp$ carrier $U=(X,L,F,W,R,D,E,S,H,C,P,N)$ exports its source object to completion-facing consumers only through the dense embedding row $E$ and the separated classifier row $S$. A consumer that reads a completion name must first expose the located source rows $X,L$, the Cauchy-filter basis $F$, the regular observation route $W,R,D$, and then the displayed embedding and separation rows. Thus the source image is dense in the completion packet as a finite NameCert route, not as a quotient completion, a choice-selected representative, host equality, or an ambient completeness theorem.
\end{theorem}
\begin{proof}
Start from the NameCert obligations of \autoref{thm:bishop-uniform-space-completion-namecert-obligations}. They force every completion-facing read to expose the source, located-uniformity, Cauchy-filter basis, observation, readback, and tolerance rows before any endpoint is consumed.

The dense embedding row $E$ records the source graph inside the completion packet. Since $E$ is downstream of $X,L,F,W,R,D$, an image read cannot be detached from the finite located Cauchy-filter evidence that produced it.

The separated classifier row $S$ is the only comparison row for completion names. It reads through $E$ and the same observation surface, so density is witnessed by the displayed route rather than by quotienting filters or selecting representatives.

Transport, replay, provenance, and local naming are carried by $H,C,P,N$. These rows preserve the same packet and add no host equality or ambient completeness data, giving the claimed dense-image handoff.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopUniformSpaceCompletionUp$ consumer that reaches a completion name while omitting the located source rows, Cauchy-filter basis row, StreamName windows, RegSeqRat readback, DyadicRatCore tolerance ledger, dense embedding row, separated classifier row, hsame transport, Cont replay, Pkg provenance, or local NameCert row refutes the carrier.}

\independenceWitness{uniform-space, metric, cauchy-filter-basis, cauchy-filter, streamname, regseqrat, dyadicratcore, entourage-completion: no listed sibling is bijective to $\BishopUniformSpaceCompletionUp$ because this packet jointly binds located uniform-space requests, Cauchy-filter bases, regular finite observation windows, dense embedding rows, separated classifier rows, transport, replay, provenance, and local naming as one completion-facing NameCert surface.}

\closureat{BishopUniformSpaceCompletionUp}{seedStr}

\begin{closurestatus}{\BishopUniformSpaceCompletionUp}
  \constructivestory{A $\BishopUniformSpaceCompletionUp$ packet is generated from a source uniform-space or metric row, located uniformity evidence, a located Cauchy-filter basis, StreamName observation windows, RegSeqRat readbacks, DyadicRatCore tolerances, a dense embedding row, a separated classifier row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-uniform-space-completion}, \autoref{thm:bishop-uniform-space-completion-namecert-obligations}, and \autoref{thm:bishop-uniform-space-completion-dense-image} seed the located uniform-space completion route over Cauchy-filter bases, regular finite observation windows, dense embedding rows, separated classifier rows, hsame transport, Cont replay, Pkg provenance, and local NameCert data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completions, host uniform-space equality, choice-selected representatives, countable choice, trichotomy, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located Cauchy-filter basis exposure, regular observation exactness, dense embedding exactness, separated classifier exactness, transport, replay, provenance, and local naming for BEDC.Derived.BishopUniformSpaceCompletionUp.}
\end{closurestatus}
