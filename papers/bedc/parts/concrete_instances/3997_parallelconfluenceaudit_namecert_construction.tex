\chapter{A Concrete Naming Certificate for $\ParallelConfluenceAuditUp$}
\label{ch:concrete-instances-parallelconfluenceaudit-namecert}
\origin{ai}

The $\ParallelConfluenceAuditUp$ packet realizes the confluence-side audit map of \autoref{ch:metacic-confluence-audit-map} as a finite NameCert surface. It sits next to the beta-substitution packet of \autoref{ch:concrete-instances-betasubstitutionpreservation-namecert} and the beta-discharge packet of \autoref{ch:concrete-instances-betasubstitutiondischarge-namecert}; the closed-term side is compatible with the seal of \autoref{ch:concrete-instances-closedtermcontextseal-namecert}. The packet records the parallel-step route, substitution-compatibility boundary, conditional parallel-diamond handoff, closed-diamond-to-star-confluence handoff, closed-normal and atom fragments, non-claim boundary, componentwise transports, displayed routes, provenance, and local naming row without turning the conditional Lean bridges into an unconditional closed Church--Rosser theorem.

\begin{definition}[ParallelConfluenceAudit carrier]
\label{def:parallel-confluence-audit-carrier}
A $\ParallelConfluenceAuditUp$ carrier is a finite $\BHist$ packet
$$
A=(P,S,D,C,Nm,At,No,H,R,L,G)
$$
with the following displayed rows:
\begin{enumerate}
\item $P$ is the parallel-step route row for the MetaCIC $\mathrm{BetaParallel}$ geometry;
\item $S$ is the substitution-compatibility boundary row naming $\mathrm{BetaParallelSubstitutionCompatibility}$ as an input, not as a proved coordinate;
\item $D$ is the conditional parallel-diamond handoff row corresponding to the route from $S$ to the parallel diamond;
\item $C$ is the closed-diamond-to-star-confluence handoff row for the closed $\mathrm{BetaStarStep}$ confluence route;
\item $Nm$ is the closed-normal fragment row recording the unconditional normal-source cases;
\item $At$ is the atom and non-lambda shape row recording the unconditional atom-side and impossible-shape fragments;
\item $No$ records the non-claim boundary for arbitrary closed Church--Rosser, $\mathrm{BetaParallelSubstitutionCompatibility}$, and $\mathrm{BetaParallelDiamondClosed}$;
\item $H$ records componentwise $\hsame$ transports for $P,S,D,C,Nm,At,No,L,G$;
\item $R$ displays the $\Cont$ routes through which consumers may read the parallel-step, conditional bridge, closed-star, normal-fragment, atom-fragment, and non-claim rows;
\item $L$ is the $\Pkg$ provenance over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, $\BetaSubstitutionPreservationUp$, $\BetaSubstitutionDischargeUp$, $\ClosedTermContextSealUp$, and the MetaCIC confluence audit map;
\item $G$ is the local $\NameCert_{\ParallelConfluenceAuditUp}$ row.
\end{enumerate}
The carrier is exactly this finite packet. It contains no proof of $\mathrm{BetaParallelSubstitutionCompatibility}$, no proof of $\mathrm{BetaParallelDiamondClosed}$, and no arbitrary closed Church--Rosser theorem for $\mathrm{BetaStarStep}$.
\end{definition}
\leanchecked{BEDC.Derived.ParallelConfluenceAuditUp.ParallelConfluenceAuditTasteGate\_single\_carrier\_alignment}

\begin{theorem}[ParallelConfluenceAudit NameCert obligations]
\label{thm:parallel-confluence-audit-namecert-obligations}
For the carrier of \autoref{def:parallel-confluence-audit-carrier}, the local $\NameCert_{\ParallelConfluenceAuditUp}$ surface consists of carrier admission for
$$
A=(P,S,D,C,Nm,At,No,H,R,L,G),
$$
parallel-step route visibility, substitution-boundary visibility, conditional parallel-diamond handoff visibility, closed-diamond-to-star-confluence handoff visibility, closed-normal fragment visibility, atom and non-lambda fragment visibility, non-claim exactness, componentwise $\hsame$ transport, displayed $\Cont$ route replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\leanchecked{BEDC.Derived.ParallelConfluenceAuditUp.ParallelConfluenceAudit\_namecert\_obligations}
\begin{proof}
Project the carrier. The inventory is exactly $P,S,D,C,Nm,At,No,H,R,L,G$, so every local naming obligation is read from a displayed finite row.

The positive confluence-side rows are $P,D,C,Nm,At$. The route $P$ records the parallel-step geometry, $D$ records only the substitution-conditional parallel-diamond handoff, $C$ records only the closed-diamond-to-star-confluence handoff, and $Nm,At$ record the unconditional closed-normal and atom fragments.

The boundary rows are $S$ and $No$. The row $S$ names the substitution-compatibility input as a premise boundary, while $No$ records the absence of an arbitrary closed Church--Rosser theorem and the absence of proofs of the two named premises.

Finally $H,R,L,G$ provide transport, continuation replay, provenance, and the local name for the same packet. Since no other coordinate exists, the obligations are exhausted by the displayed rows.
\end{proof}

\begin{theorem}[ParallelConfluenceAudit conditional bridge]
\label{thm:parallel-confluence-audit-conditional-bridge}
Let $A=(P,S,D,C,Nm,At,No,H,R,L,G)$ be an accepted $\ParallelConfluenceAuditUp$ carrier. Any read of the conditional confluence bridge reaches the parallel-diamond row $D$ only through the substitution-compatibility boundary $S$, and reaches the closed-star confluence handoff $C$ only through the parallel-diamond handoff row $D$. The closed-normal row $Nm$ and atom row $At$ remain unconditional fragments, not replacements for $S,D,C$.
\end{theorem}
\leanchecked{BEDC.Derived.ParallelConfluenceAuditUp.ParallelConfluenceAudit\_conditional\_bridge\_rows}
\begin{proof}
Begin with the conditional route data. The displayed continuation row $R$ exposes the parallel-step route $P$, then the substitution boundary $S$, and only then the parallel-diamond handoff $D$.

The closed-star handoff is downstream of $D$. A consumer that reads $C$ therefore reads the closed-diamond-to-star bridge as a handoff from the parallel diamond, not as an independent proof of the diamond premise.

The rows $Nm$ and $At$ are separate fragment rows. They may be cited for closed-normal sources, atom sources, and non-lambda shape cases, but their carrier positions do not provide either $S$ or $D$.

The package row $L$ and local name $G$ keep these dependencies in one NameCert packet. Thus every bridge read factors through $S,D,C$ in that order.
\end{proof}
\leanchecked{BEDC.Derived.ParallelConfluenceAuditUp.ParallelConfluenceAudit\_conditional\_bridge\_route\_closed}

\begin{theorem}[ParallelConfluenceAudit non-escape]
\label{thm:parallel-confluence-audit-nonescape}
Every accepted $\ParallelConfluenceAuditUp$ packet exposes confluence data only through the rows
$$
P,S,D,C,Nm,At,No,H,R,L,G.
$$
It exports no arbitrary closed Church--Rosser theorem for $\mathrm{BetaStarStep}$, no proof of $\mathrm{BetaParallelSubstitutionCompatibility}$, and no proof of $\mathrm{BetaParallelDiamondClosed}$.
\end{theorem}
\begin{proof}
Inspect the carrier coordinates. The only positive rows are the parallel-step route, the two conditional handoffs, and the closed-normal and atom fragments.

Use \autoref{thm:parallel-confluence-audit-conditional-bridge}. Reads of the bridge rows must pass through $S,D,C$, so the conditional premises remain visible at every consumer-facing route.

Use \autoref{thm:parallel-confluence-audit-namecert-obligations} for local naming and provenance. The non-claim row $No$ is part of the same finite packet as the displayed routes, and the missing endpoint theorems have no carrier coordinate.

Therefore a downstream consumer may cite the audit packet only as the finite confluence-side map described above. It cannot turn the map into arbitrary closed Church--Rosser, a proof of substitution compatibility, or a proof of the closed parallel diamond.
\end{proof}
\leanchecked{BEDC.Derived.ParallelConfluenceAuditUp.ParallelConfluenceAudit\_nonescape}

\begin{theorem}[ParallelConfluenceAudit residual premise retention]
\label{thm:parallel-confluence-audit-residual-premise-retention}
Let $A=(P,S,D,C,Nm,At,No,H,R,L,G)$ be an accepted $\ParallelConfluenceAuditUp$ carrier. Any residual-substitution premise read by a confluence consumer is retained as the displayed boundary row $S$ throughout the route
$$
P \longmapsto S \longmapsto D \longmapsto C \longmapsto H \longmapsto R .
$$
The packet may transport the premise through the finite typed-window route, but it does not discharge $\mathrm{BetaParallelSubstitutionCompatibility}$ or replace it by evaluator equality, quotient term equality, subject reduction, kernel consistency, or full Church--Rosser.
\end{theorem}
\begin{proof}
Project the carrier rows. The row $S$ is explicitly the substitution-compatibility boundary, and the rows $D$ and $C$ occur downstream of it.

Use the conditional bridge theorem. It forces every read of the parallel-diamond handoff to pass through $S$ before $D$, and every closed-star handoff to pass through $D$ before $C$.

The transport and replay rows $H,R$ preserve the same premise-bearing route. Since the non-claim row $No$ excludes the discharged premise and the full confluence endpoints, the residual-substitution premise remains visible rather than becoming a hidden proof.
\end{proof}
\leanchecked{BEDC.Derived.ParallelConfluenceAuditUp.ParallelConfluenceAudit\_residual\_premise\_retention}

\begin{theorem}[ParallelConfluenceAudit typed-window local join]
\label{thm:parallel-confluence-audit-typed-window-local-join}
Every typed-window local join exposed by an accepted $\ParallelConfluenceAuditUp$ packet is mediated by the finite rows $P,S,D,C,Nm,At,No,H,R,L,G$. The join may use the closed-normal fragment $Nm$ and the atom or non-lambda fragment $At$ as local cases, while the conditional handoffs $S,D,C$ remain retained premises. It exports no full Church--Rosser theorem, subject reduction theorem, evaluator equality, quotient term equality, or kernel consistency statement.
\end{theorem}
\begin{proof}
Start with a typed-window local join request. The carrier has no independent join coordinate, so the request first reads the parallel-step route $P$ and the substitution boundary $S$.

The conditional rows then determine the join surface. The row $D$ is only the parallel-diamond handoff under the retained premise, and $C$ is only the closed-diamond-to-star handoff.

The fragment rows $Nm$ and $At$ provide local closed-normal and atom-shape cases. They are useful inside the typed window, but their positions do not remove $S,D,C$ or create global evaluator equality.

Finally read $No,H,R,L,G$. The non-claim, transport, replay, provenance, and naming rows keep the local join candidate inside the displayed finite packet and block the listed global MetaCIC conclusions.
\end{proof}
\leanchecked{BEDC.Derived.ParallelConfluenceAuditUp.ParallelConfluenceAudit\_typed\_window\_local\_join}

\begin{theorem}[ParallelConfluenceAudit candidate-mediated boundary]
\label{thm:parallel-confluence-audit-candidate-mediated-boundary}
Every confluence candidate exported by an accepted $\ParallelConfluenceAuditUp$ packet is mediated by the displayed rows
$$
P,S,D,C,Nm,At,No,H,R,L,G .
$$
It is a candidate-mediated typed-window boundary: residual-substitution premises are retained, local fragment joins are named, and the endpoint remains short of full Church--Rosser, subject reduction, evaluator equality, quotient term equality, and kernel consistency.
\end{theorem}
\begin{proof}
Read the candidate from the carrier. The rows $P,S,D,C$ provide the parallel-step route and the two conditional handoffs, with $S$ retained as a premise boundary.

Read the local fragments next. The rows $Nm$ and $At$ cover closed-normal and atom or non-lambda cases inside the displayed typed window, not arbitrary closed terms.

Apply non-escape. The row $No$ and \autoref{thm:parallel-confluence-audit-nonescape} prevent the candidate from being read as full Church--Rosser or as proofs of the named conditional premises.

The structural rows $H,R,L,G$ only transport, replay, source, and name this candidate-mediated boundary. They add no evaluator equality, quotient term equality, subject reduction theorem, or kernel-consistency theorem.
\end{proof}

\closureat{ParallelConfluenceAuditUp}{seedStr}

\begin{closurestatus}{\ParallelConfluenceAuditUp}
  \constructivestory{A $\ParallelConfluenceAuditUp$ packet is generated from a parallel-step route, substitution-compatibility boundary, conditional parallel-diamond handoff, closed-diamond-to-star-confluence handoff, closed-normal fragment row, atom and non-lambda shape row, non-claim row, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:parallel-confluence-audit-carrier}, \autoref{thm:parallel-confluence-audit-namecert-obligations}, \autoref{thm:parallel-confluence-audit-conditional-bridge}, and \autoref{thm:parallel-confluence-audit-nonescape} bind the finite confluence audit packet and its conditional bridge boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not prove arbitrary closed Church--Rosser for $\mathrm{BetaStarStep}$, $\mathrm{BetaParallelSubstitutionCompatibility}$, $\mathrm{BetaParallelDiamondClosed}$, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, conditional-route exactness through $S,D,C$, fragment coverage for $Nm$ and $At$, and non-escape for the named confluence endpoints.}
\end{closurestatus}
