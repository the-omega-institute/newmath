\begin{definition}[List cons-boundary coherence]
\label{def:list-cons-boundary-coherence}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier and let
$\sim_A$ be a source classifier. Define
$\mathsf{ListConsBoundaryCoherent}_{A,\sim_A}$ as the predicate
$$
\forall m\,a\,a'\,t\,t'\,p\,p'\,xs\,xs'.\;
A(a)\to A(a')\to
\mathsf{ListSpineRep}_A(t,xs)\to
\mathsf{ListSpineRep}_A(t',xs')\to
\ContR(a,t,p)\to \ContR(a',t',p')\to
\hsame(m,\Eone(p))\to \hsame(m,\Eone(p'))\to
((a\sim_A a')\land \mathsf{ListClassifierSpec}(\sim_A,xs,xs')).
$$
\end{definition}

\begin{theorem}[List spine coherence from cons-boundary coherence]
\label{thm:list-spine-coherence-from-cons-boundary-coherence}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier and let
$\sim_A$ be a source classifier. If
$\mathsf{ListConsBoundaryCoherent}_{A,\sim_A}$ holds, then
$\mathsf{ListSpineRepCoherent}_{A,\sim_A}$ holds.
\end{theorem}
\begin{proof}
Compare two finite-spine presentations of the same history by cases on the
displayed spines. The nil-nil case is the nil constructor of
$\mathsf{ListClassifierSpec}$. The mixed nil-cons cases contradict
Theorem~\ref{thm:list-spine-rep-nil-cons-no-confusion}. In the cons-cons case,
both presentations expose the same list endpoint together with displayed head
histories, tail spines, tail representations, continuation witnesses, and
endpoint witnesses. Definition~\ref{def:list-cons-boundary-coherence} supplies
the conjunction
$(a\sim_A a')\land \mathsf{ListClassifierSpec}(\sim_A,xs,xs')$; its left
conjunct is the head classifier witness, and its right conjunct is the tail
finite-spine classifier witness. These are the two constructor arguments for
$\mathsf{ListClassifierSpec}(\sim_A,-,-)$ on the whole cons spines.
\end{proof}
\leanchecked{BEDC.Derived.ListUp.ListSpineRep\_coherent\_from\_cons\_boundary}

\begin{theorem}[List spine coherence iff cons-boundary coherence]
\label{thm:list-spine-coherence-iff-cons-boundary-coherence}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier and let
$\sim_A$ be a source classifier. Then
$\mathsf{ListSpineRepCoherent}_{A,\sim_A}$ holds if and only if
$\mathsf{ListConsBoundaryCoherent}_{A,\sim_A}$ holds.
\end{theorem}
\begin{proof}
For the forward implication, assume
$\mathsf{ListSpineRepCoherent}_{A,\sim_A}$. Given two displayed cons
presentations of the same endpoint, view them as two
$\mathsf{ListSpineRep}_A$ presentations with cons spines. Spine coherence
returns a $\mathsf{ListClassifierSpec}(\sim_A,-,-)$ witness relating those two
cons spines. The cons-cons branch of
Theorem~\ref{thm:list-classifier-case-exactness} inverts that witness and
exposes exactly the required head classifier $a\sim_A a'$ together with the
tail classifier $\mathsf{ListClassifierSpec}(\sim_A,xs,xs')$. These are the two
fields of Definition~\ref{def:list-cons-boundary-coherence}. The reverse
implication is precisely
Theorem~\ref{thm:list-spine-coherence-from-cons-boundary-coherence}.
\end{proof}

\begin{theorem}[List public cons readback determinacy]
\label{thm:list-public-cons-readback-deterministic}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier and let
$\sim_A$ be a source classifier. For any
$m,a,a',t,t',p,p':\mathsf{BHist}$, if
$\mathsf{ListConsBoundaryCoherent}_{A,\sim_A}$,
$A(a)$, $A(a')$, $\mathsf{ListHistoryCarrier}_A(t)$,
$\mathsf{ListHistoryCarrier}_A(t')$, $\ContR(a,t,p)$,
$\ContR(a',t',p')$, $\hsame(m,\Eone(p))$, and
$\hsame(m,\Eone(p'))$ hold, then
$$
a\sim_A a'\land
\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(t,t').
$$
The roadmap ten-field fit is: traditional target finite-list constructor
readback; BEDC source $\mathsf{BHist}$; source carrier $A$; public carrier
$\mathsf{ListHistoryCarrier}_A$; classifier
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}$; stability supplied by
$\mathsf{ListConsBoundaryCoherent}_{A,\sim_A}$; ledger two cons endpoints;
exactness through finite spine representations; standard bridge finite spines;
status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:list-spine-representation-coverage} to the two displayed
tail carrier witnesses. This gives finite spines $xs$ and $xs'$ with
$\mathsf{ListSpineRep}_A(t,xs)$ and
$\mathsf{ListSpineRep}_A(t',xs')$.

Use Definition~\ref{def:list-cons-boundary-coherence} on the two displayed cons
presentations of the endpoint $m$. The hypotheses supply the source membership
for $a$ and $a'$, the two finite tail presentations, the two continuation
witnesses, and the two endpoint equalities. The resulting conjunction gives
$a\sim_A a'$ and $\mathsf{ListClassifierSpec}(\sim_A,xs,xs')$.

Repack the two tail spine representations together with
$\mathsf{ListClassifierSpec}(\sim_A,xs,xs')$ through
Definition~\ref{def:list-spine-representation}. This yields
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(t,t')$, which pairs with the
head classifier to give the displayed conclusion.
\end{proof}
\leanchecked{BEDC.Derived.ListUp.ListSpineBridgeClassifier\_public\_cons\_readback\_deterministic}

\begin{theorem}[Deterministic list spine length readback]
\label{thm:list-spine-cons-boundary-length-deterministic}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier and let
$\sim_A$ be a source classifier. For every history $m$ and finite spines $xs$
and $ys$, if $\mathsf{ListConsBoundaryCoherent}_{A,\sim_A}$,
$\mathsf{ListSpineRep}_A(m,xs)$, and $\mathsf{ListSpineRep}_A(m,ys)$ hold, then
$$
\ell(xs)=\ell(ys).
$$
The roadmap ten-field fit is: traditional target finite-list readback; BEDC source $\mathsf{BHist}$; carrier $\mathsf{ListHistoryCarrier}_A$; classifier $\mathsf{ListClassifierSpec}$; ledger finite spine; status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Theorem~\ref{thm:list-spine-coherence-from-cons-boundary-coherence} turns the
cons-boundary coherence witness into
$\mathsf{ListSpineRepCoherent}_{A,\sim_A}$. Apply this coherence datum to the
two displayed $\mathsf{ListSpineRep}_A$ presentations of the endpoint $m$ to
obtain $\mathsf{ListClassifierSpec}(\sim_A,xs,ys)$. Then
Theorem~\ref{thm:list-classifier-length-eq} gives equality of the two finite
spine lengths.
\end{proof}
\leanchecked{BEDC.Derived.ListUp.ListSpineRep\_cons\_boundary\_length\_deterministic}

\begin{definition}[List public length readback]
\label{def:list-public-length-readback}
For a BEDC history source carrier $A:\mathsf{BHist}\to\mathsf{Prop}$, define
$\mathsf{ListPublicLength}_A(h,n)$ by
$$
\mathsf{ListPublicLength}_A(h,n)
\quad\Longleftrightarrow\quad
\exists xs:\mathsf{HistSpine}.\;
\mathsf{ListSpineRep}_A(h,xs)\land \ell(xs)=n.
$$
Here $\ell$ is the finite-spine presentation length. It is not a $\NatUp$
operation and is not an additional field of the public certificate.
\end{definition}

\begin{theorem}[List public length readback is well-defined under cons-boundary coherence]
\label{thm:list-public-length-readback-well-defined}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier, let
$\sim_A$ be a source classifier, and let
$\mathsf{cert}:\NameCert(A,\sim_A)$. Assume
$\mathsf{ListConsBoundaryCoherent}_{A,\sim_A}$. Then the public length readback
has the following two properties.
\begin{enumerate}
  \item If $\mathsf{ListPublicLength}_A(h,n)$ and
  $\mathsf{ListPublicLength}_A(h,m)$, then $n=m$.
  \item If $\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(h,k)$,
  $\mathsf{ListPublicLength}_A(h,n)$, and
  $\mathsf{ListPublicLength}_A(k,m)$, then $n=m$.
\end{enumerate}
The roadmap ten-field fit is: traditional target finite-list length readback;
BEDC source $\mathsf{BHist}$ with $\NameCert$; carrier
$\mathsf{ListHistoryCarrier}_A$; classifier
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}$; ledger finite-spine length
witnesses; exactness through $\mathsf{ListSpineRep}_A$ and
$\mathsf{ListClassifierSpec}$; status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
For uniqueness at one endpoint, unpack the two
$\mathsf{ListPublicLength}_A$ witnesses. They give finite spines $xs$ and $ys$,
representations $\mathsf{ListSpineRep}_A(h,xs)$ and
$\mathsf{ListSpineRep}_A(h,ys)$, and displayed equalities
$\ell(xs)=n$ and $\ell(ys)=m$. Apply
Theorem~\ref{thm:list-spine-cons-boundary-length-deterministic} to the two
representations of $h$ under the cons-boundary coherence hypothesis, obtaining
$\ell(xs)=\ell(ys)$. Chain the inverse of $\ell(xs)=n$, this equality, and
$\ell(ys)=m$ to get $n=m$.

For preservation along the public classifier, unpack the two public length
witnesses as represented spines $xs$ for $h$ and $ys$ for $k$, with
$\ell(xs)=n$ and $\ell(ys)=m$. First apply
Theorem~\ref{thm:list-spine-coherence-from-cons-boundary-coherence} to obtain
$\mathsf{ListSpineRepCoherent}_{A,\sim_A}$. Then apply
Theorem~\ref{thm:list-spine-classifier-represented-length}, using
$\mathsf{cert}$, the coherence witness, the displayed endpoint
representations, and the classifier witness
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(h,k)$, to obtain
$\ell(xs)=\ell(ys)$. Chain this length equality with $\ell(xs)=n$ and
$\ell(ys)=m$ to get $n=m$.
\end{proof}

\begin{theorem}[List public length readback totality and classified endpoints]
\label{thm:list-public-length-readback-total}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier, let
$\sim_A$ be a source classifier, and let
$\mathsf{cert}:\NameCert(A,\sim_A)$. Assume
$\mathsf{ListConsBoundaryCoherent}_{A,\sim_A}$. Then the public length readback
is total on public list histories and supplies common witnesses for classified
endpoints:
\begin{enumerate}
  \item If $\mathsf{ListHistoryCarrier}_A(h)$, then
  $\exists n.\;\mathsf{ListPublicLength}_A(h,n)$.
  \item If $\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(h,k)$, then
  $\exists n.\;
  \mathsf{ListPublicLength}_A(h,n)\land
  \mathsf{ListPublicLength}_A(k,n)$.
\end{enumerate}
The roadmap ten-field fit is: traditional target public finite-list length
readback; BEDC source $\mathsf{BHist}$ with $\NameCert$; carrier
$\mathsf{ListHistoryCarrier}_A$; classifier
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}$; ledger finite-spine
presentations; exactness through $\mathsf{ListSpineRep}_A$ and the public
length definition; classified endpoint readback through the spine bridge; status
$\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
\begin{enumerate}
  \item For totality, apply the forward direction of
  Theorem~\ref{thm:list-spine-representation-coverage} to the displayed
  $\mathsf{ListHistoryCarrier}_A(h)$ witness. This gives a finite spine $xs$
  with $\mathsf{ListSpineRep}_A(h,xs)$. Choose $n=\ell(xs)$ and repack this
  representation with the reflexive length equality in
  Definition~\ref{def:list-public-length-readback}.

  \item For classified endpoints, unfold
  $\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}$ through
  Definition~\ref{def:list-spine-representation}. The classifier witness gives
  finite spines $xs$ and $ys$, endpoint presentations
  $\mathsf{ListSpineRep}_A(h,xs)$ and $\mathsf{ListSpineRep}_A(k,ys)$, and a
  finite-spine classifier witness
  $\mathsf{ListClassifierSpec}(\sim_A,xs,ys)$.

  Apply Theorem~\ref{thm:list-classifier-length-eq} to obtain
  $\ell(xs)=\ell(ys)$. Choose $n=\ell(xs)$. The representation of $h$ gives
  $\mathsf{ListPublicLength}_A(h,n)$ by reflexivity. The representation of $k$
  gives $\mathsf{ListPublicLength}_A(k,n)$ after rewriting the equality
  $\ell(xs)=\ell(ys)$ in the opposite direction.

  Repack the two public length witnesses under the chosen value $n$.
\end{enumerate}
\end{proof}

\begin{theorem}[List public-object certificate with public length readback]
\label{thm:list-public-object-certificate-with-length-readback}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier, let
$\sim_A$ be a source classifier, and let
$\mathsf{cert}:\NameCert(A,\sim_A)$. If
$\mathsf{ListConsBoundaryCoherent}_{A,\sim_A}$ holds, then the public
$\ListUp$ object has the semantic certificate and public length readback package:
\begin{enumerate}
  \item
  $\mathsf{SemanticNameCert}(\mathsf{ListHistoryCarrier}_A,
  \mathsf{ListHistoryPattern}_A,
  \mathsf{ListHistoryLedger}_A,
  \mathsf{ListSpineHistoryClassifier}_{A,\sim_A})$.
  \item
  $\forall h.\;\mathsf{ListHistoryCarrier}_A(h)\to
  \exists n.\;\mathsf{ListPublicLength}_A(h,n)$.
  \item
  $\forall h\,n\,m.\;\mathsf{ListPublicLength}_A(h,n)\to
  \mathsf{ListPublicLength}_A(h,m)\to n=m$.
  \item
  $\forall h\,k.\;\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(h,k)\to
  \exists n.\;\mathsf{ListPublicLength}_A(h,n)\land
  \mathsf{ListPublicLength}_A(k,n)$.
  \item
  $\forall h\,k\,n\,m.\;
  \mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(h,k)\to
  \mathsf{ListPublicLength}_A(h,n)\to
  \mathsf{ListPublicLength}_A(k,m)\to n=m$.
\end{enumerate}
\end{theorem}
\begin{proof}
The roadmap ten-field fit is: traditional target finite-list public certificate
with length readback; BEDC source $\mathsf{BHist}$ plus source $\NameCert$;
carrier $\mathsf{ListHistoryCarrier}_A$; classifier
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}$; stability from
cons-boundary coherence; ledger $\mathsf{ListHistoryLedger}_A$ plus
$\mathsf{ListPublicLength}_A$; core theorem this package; exactness
finite-spine length readback; standard bridge finite spines; status
$\mathsf{ProofTarget}$.

\begin{enumerate}
  \item The certificate clause is exactly
  Theorem~\ref{thm:list-public-object-certificate-from-cons-boundary-coherence}
  applied to $\mathsf{cert}$ and the cons-boundary coherence witness.

  \item The total public length readback clause is the first component of
  Theorem~\ref{thm:list-public-length-readback-total}.

  \item The uniqueness clause is the first component of
  Theorem~\ref{thm:list-public-length-readback-well-defined}.

  \item The classified endpoint common-witness clause is the second component of
  Theorem~\ref{thm:list-public-length-readback-total}.

  \item The equality of endpoint readbacks under the public classifier is the
  second component of
  Theorem~\ref{thm:list-public-length-readback-well-defined}.
\end{enumerate}
\end{proof}

\begin{theorem}[List public zero length and nonempty readback]
\label{thm:list-public-zero-length-and-nonempty-readback}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier, let
$\mathsf{Rel}:\mathsf{BHist}\to\mathsf{BHist}\to\mathsf{Prop}$ be a source
classifier, and assume
$\mathsf{ListConsBoundaryCoherent}_{A,\mathsf{Rel}}$. For every history $h$, the
public length ledger is exact at zero and exposes a cons readback at nonzero
public endpoints:
\begin{enumerate}
  \item $\mathsf{ListPublicLength}_A(h,0)$ holds if and only if
  $\hsame(h,\emp)$ holds.
  \item If $\mathsf{ListHistoryCarrier}_A(h)$ and $\neg\hsame(h,\emp)$ hold, then
  $$
  \exists a\,t\,p:\mathsf{BHist}.\;
  \left(
  \begin{array}{l}
  A(a)\land \mathsf{ListHistoryCarrier}_A(t)\land
  \ContR(a,t,p)\land\\
  \hsame(h,\Eone(p))\land\\
  \forall n.\;\mathsf{ListPublicLength}_A(h,n)\to n\neq 0
  \end{array}
  \right).
  $$
\end{enumerate}
The ten-field fit is: traditional target finite-list zero/nonempty readback;
BEDC source $\mathsf{BHist}$ with $\ContR$; carrier
$\mathsf{ListHistoryCarrier}_A$; classifier
$\mathsf{ListSpineHistoryClassifier}_{A,\mathsf{Rel}}$; stability from
cons-boundary coherence; ledger $\mathsf{ListPublicLength}_A$; core theorem
zero/nonempty exactness; exactness nil iff zero and nonempty gives cons;
standard bridge finite spines; status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
For the zero endpoint, unfold
Definition~\ref{def:list-public-length-readback}. A public length witness with
value $0$ gives a finite spine $xs$ with $\mathsf{ListSpineRep}_A(h,xs)$ and
$\ell(xs)=0$. Case-analyze $xs$. The cons case has successor length, so it cannot
have length $0$; the remaining nil case is exactly the nil representation, hence
it supplies $\hsame(h,\emp)$.

Conversely, from $\hsame(h,\emp)$ choose the nil finite spine. The nil clause of
$\mathsf{ListSpineRep}_A$ gives $\mathsf{ListSpineRep}_A(h,\mathsf{nil})$, and
$\ell(\mathsf{nil})=0$ repacks it as $\mathsf{ListPublicLength}_A(h,0)$.

For the nonempty readback, first turn the cons-boundary hypothesis into
$\mathsf{ListSpineRepCoherent}_{A,\mathsf{Rel}}$ by
Theorem~\ref{thm:list-spine-coherence-from-cons-boundary-coherence}. Apply
Theorem~\ref{thm:list-public-carrier-constructor-readback-under-spine-coherence}
to the displayed $\mathsf{ListHistoryCarrier}_A(h)$ witness.

The nil alternative contains $\hsame(h,\emp)$ and is eliminated by the assumed
$\neg\hsame(h,\emp)$. The cons alternative supplies histories $a,t,p$ with
$A(a)$, $\mathsf{ListHistoryCarrier}_A(t)$, $\ContR(a,t,p)$, and
$\hsame(h,\Eone(p))$.

It remains to show that no public length witness at this endpoint has value
$0$. Given $\mathsf{ListPublicLength}_A(h,n)$ and an equality $n=0$, rewrite the
witness to obtain $\mathsf{ListPublicLength}_A(h,0)$. The zero equivalence just
proved gives $\hsame(h,\emp)$, contradicting the nonempty hypothesis. Therefore
$n\neq 0$, and the cons witnesses together with this length exclusion are the
required existential data.
\end{proof}

\begin{theorem}[List classified zero length endpoint readback]
\label{thm:list-classified-zero-length-endpoint-readback}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier, let
$\sim_A$ be a source classifier, and let
$\mathsf{cert}:\NameCert(A,\sim_A)$. Assume
$\mathsf{ListConsBoundaryCoherent}_{A,\sim_A}$. For histories $h,k$, if
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(h,k)$ holds, then:
\begin{enumerate}
  \item The zero public-length facts agree along the classifier:
  $$
  \mathsf{ListPublicLength}_A(h,0)
  \Longleftrightarrow
  \mathsf{ListPublicLength}_A(k,0).
  $$

  \item If either endpoint has public length zero, then both endpoints read
  back as the empty history:
  $$
  \mathsf{ListPublicLength}_A(h,0)\vee
  \mathsf{ListPublicLength}_A(k,0)
  \quad\Longrightarrow\quad
  \hsame(h,\emp)\land\hsame(k,\emp).
  $$
\end{enumerate}
The ten-field fit is: traditional target finite-list classified zero readback;
BEDC source $\mathsf{BHist}$ with $\NameCert$; carrier
$\mathsf{ListHistoryCarrier}_A$; classifier
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}$; stability from
cons-boundary coherence; ledger $\mathsf{ListPublicLength}_A$; core theorem
zero public-length transport and empty endpoint exposure; exactness by common
public witnesses and zero readback; standard bridge finite spines; status
$\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:list-public-length-readback-total} to the displayed
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(h,k)$ witness. It supplies a
finite value $m$ together with $\mathsf{ListPublicLength}_A(h,m)$ and
$\mathsf{ListPublicLength}_A(k,m)$.

For the forward zero transport, assume
$\mathsf{ListPublicLength}_A(h,0)$. The $h$-side witness at value $m$ and the
given zero witness have the same endpoint, so the uniqueness clause of
Theorem~\ref{thm:list-public-length-readback-well-defined} identifies $m=0$.
Rewriting the $k$-side common witness gives
$\mathsf{ListPublicLength}_A(k,0)$.

For the reverse zero transport, assume
$\mathsf{ListPublicLength}_A(k,0)$. The $k$-side witness at value $m$ and the
given zero witness have the same endpoint, so the uniqueness clause of
Theorem~\ref{thm:list-public-length-readback-well-defined} identifies $m=0$.
Rewriting the $h$-side common witness gives
$\mathsf{ListPublicLength}_A(h,0)$.

It remains to read back the endpoints. If the displayed disjunction gives
$\mathsf{ListPublicLength}_A(h,0)$, then the first part of
Theorem~\ref{thm:list-public-zero-length-and-nonempty-readback} gives
$\hsame(h,\emp)$, and the forward zero transport gives
$\mathsf{ListPublicLength}_A(k,0)$. Applying the same zero readback theorem to
the $k$-side gives $\hsame(k,\emp)$.

If the displayed disjunction gives $\mathsf{ListPublicLength}_A(k,0)$, the same
argument with the reverse zero transport first gives
$\mathsf{ListPublicLength}_A(h,0)$, and then the zero readback theorem gives
$\hsame(h,\emp)$ and $\hsame(k,\emp)$.
\end{proof}

\begin{theorem}[List public successor length cons readback]
\label{thm:list-public-successor-length-cons-readback}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier. For
every history $h$ and every finite length value $n$, if
$\mathsf{ListPublicLength}_A(h,n+1)$ holds, then
$$
\exists a\,t\,p:\mathsf{BHist}.\;
\left(
\begin{array}{l}
A(a)\land \mathsf{ListHistoryCarrier}_A(t)\land
\mathsf{ListPublicLength}_A(t,n)\land\\
\ContR(a,t,p)\land \hsame(h,\Eone(p))
\end{array}
\right).
$$
The ten-field fit is: traditional target finite-list successor readback; BEDC
source $\mathsf{BHist}$ with $\ContR$; carrier
$\mathsf{ListHistoryCarrier}_A$; classifier-independent spine exposure; ledger
$\mathsf{ListPublicLength}_A$; core theorem successor public length inversion;
exactness by finite-spine constructor analysis; standard bridge finite spines;
status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Unfold Definition~\ref{def:list-public-length-readback}. The public length
witness gives a finite spine $xs$ with $\mathsf{ListSpineRep}_A(h,xs)$ and
$\ell(xs)=n+1$.

Case-analyze $xs$. In the nil case, the displayed length equality would force
$0=n+1$, which is impossible for a finite length value. Thus the represented
spine is a cons spine.

The cons representation exposes histories $a,t,p$ with $A(a)$, a tail spine
representation for $t$, the continuation boundary $\ContR(a,t,p)$, and the
endpoint presentation $\hsame(h,\Eone(p))$. The tail representation supplies
$\mathsf{ListHistoryCarrier}_A(t)$ by the carrier coverage direction for
represented spines.

The cons length equality identifies the tail-spine length with $n$. Repack the
tail representation and this predecessor length equality through
Definition~\ref{def:list-public-length-readback}, obtaining
$\mathsf{ListPublicLength}_A(t,n)$. Together with $A(a)$, $\ContR(a,t,p)$, and
$\hsame(h,\Eone(p))$, these data are the required existential witnesses.
\end{proof}

\begin{theorem}[List public successor length constructor exactness]
\label{thm:list-public-successor-length-constructor-exactness}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier, let
$\sim_A$ be a source classifier, and let
$\mathsf{cert}:\NameCert(A,\sim_A)$. Assume
$\mathsf{ListConsBoundaryCoherent}_{A,\sim_A}$. For every history $h$ and every
finite length value $n$,
$$
\mathsf{ListPublicLength}_A(h,n+1)
\quad\Longleftrightarrow\quad
\exists a\,t\,p:\mathsf{BHist}.\;
\left(
\begin{array}{l}
A(a)\land \mathsf{ListHistoryCarrier}_A(t)\land
\mathsf{ListPublicLength}_A(t,n)\land\\
\ContR(a,t,p)\land \hsame(h,\Eone(p))
\end{array}
\right).
$$
The ten-field fit is: traditional target finite-list public successor
constructor law; BEDC source $\mathsf{BHist}$ with $\ContR$; carrier
$\mathsf{ListHistoryCarrier}_A$; classifier protected by source $\NameCert$ and
cons-boundary coherence; ledger $\mathsf{ListPublicLength}_A$; core theorem
constructor readback in both directions; exactness by finite-spine cons
formation; standard bridge finite spines; status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
For the forward direction, apply
Theorem~\ref{thm:list-public-successor-length-cons-readback}. This exposes the
head history, tail history, continuation boundary, predecessor public length,
and endpoint presentation required by the right-hand side.

For the reverse direction, assume histories $a,t,p$ with $A(a)$,
$\mathsf{ListHistoryCarrier}_A(t)$, $\mathsf{ListPublicLength}_A(t,n)$,
$\ContR(a,t,p)$, and $\hsame(h,\Eone(p))$.

Unfold Definition~\ref{def:list-public-length-readback} for the tail public
length. This gives a finite spine $xs$ with
$\mathsf{ListSpineRep}_A(t,xs)$ and $\ell(xs)=n$.

Use the cons constructor for $\mathsf{ListSpineRep}_A$ with the displayed
source membership $A(a)$, the tail representation, the continuation witness,
and the endpoint presentation. It gives
$\mathsf{ListSpineRep}_A(h,\mathsf{cons}(a,xs))$.

The represented cons spine has length $\ell(xs)+1$, so the displayed tail
length identifies it with $n+1$. Repack this cons representation and length
identity through Definition~\ref{def:list-public-length-readback}, obtaining
$\mathsf{ListPublicLength}_A(h,n+1)$.
\end{proof}

\begin{theorem}[List classified successor length cons readback]
\label{thm:list-classified-successor-length-cons-readback}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier, let
$\sim_A$ be a source classifier, and let
$\mathsf{cert}:\NameCert(A,\sim_A)$. Assume
$\mathsf{ListConsBoundaryCoherent}_{A,\sim_A}$. If
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(h,k)$ and
$\mathsf{ListPublicLength}_A(h,n+1)$ hold, then
$$
\exists a\,t\,p:\mathsf{BHist}.\;
\left(
\begin{array}{l}
A(a)\land \mathsf{ListHistoryCarrier}_A(t)\land
\mathsf{ListPublicLength}_A(t,n)\land\\
\ContR(a,t,p)\land \hsame(k,\Eone(p))
\end{array}
\right).
$$
The ten-field fit is: traditional target finite-list classified successor
readback; BEDC source $\mathsf{BHist}$ with $\ContR$; carrier
$\mathsf{ListHistoryCarrier}_A$; classifier
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}$; stability from
cons-boundary coherence and source $\NameCert$; ledger
$\mathsf{ListPublicLength}_A$; core theorem classified successor public-length
inversion; exactness by common public-length witnesses and successor spine
analysis; standard bridge finite spines; status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Use Theorem~\ref{thm:list-public-length-readback-total} on the displayed
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(h,k)$ witness. It supplies a
finite value $m$ together with
$\mathsf{ListPublicLength}_A(h,m)$ and
$\mathsf{ListPublicLength}_A(k,m)$.

The given witness $\mathsf{ListPublicLength}_A(h,n+1)$ and the $h$-side witness
at value $m$ have the same endpoint. Apply
Theorem~\ref{thm:list-public-length-readback-well-defined} to identify
$m=n+1$, and rewrite the $k$-side witness as
$\mathsf{ListPublicLength}_A(k,n+1)$.

Now apply Theorem~\ref{thm:list-public-successor-length-cons-readback} to the
endpoint $k$ with length $n+1$. The resulting histories $a,t,p$ satisfy
$A(a)$, $\mathsf{ListHistoryCarrier}_A(t)$,
$\mathsf{ListPublicLength}_A(t,n)$, $\ContR(a,t,p)$, and
$\hsame(k,\Eone(p))$, which are exactly the displayed existential data.
\end{proof}

\begin{theorem}[List classified successor component readback]
\label{thm:list-classified-successor-component-readback}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier, let
$\sim_A$ be a source classifier, and let
$\mathsf{cert}:\NameCert(A,\sim_A)$. Assume
$\mathsf{ListConsBoundaryCoherent}_{A,\sim_A}$. For histories $h,k$ and every
finite length value $n$, if
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(h,k)$ and
$\mathsf{ListPublicLength}_A(h,n+1)$ hold, then
$$
\exists a\,b\,t\,u\,p\,q:\mathsf{BHist}.\;
\left(
\begin{array}{l}
A(a)\land A(b)\land
\mathsf{ListHistoryCarrier}_A(t)\land
\mathsf{ListHistoryCarrier}_A(u)\land\\
\mathsf{ListPublicLength}_A(t,n)\land
\mathsf{ListPublicLength}_A(u,n)\land\\
\ContR(a,t,p)\land \ContR(b,u,q)\land\\
\hsame(h,\Eone(p))\land \hsame(k,\Eone(q))\land\\
a\sim_A b\land
\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(t,u)
\end{array}
\right).
$$
The ten-field fit is: traditional target finite-list classified successor
component readback; BEDC source $\mathsf{BHist}$ with $\ContR$; carrier
$\mathsf{ListHistoryCarrier}_A$; classifier
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}$; stability from
cons-boundary coherence and source $\NameCert$; ledger
$\mathsf{ListPublicLength}_A$ on both tails; core theorem componentwise
successor inversion; exactness by public cons endpoint inversion; standard
bridge finite spines; status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
First obtain the representation coherence needed by the public endpoint
inversion. Apply
Theorem~\ref{thm:list-spine-coherence-from-cons-boundary-coherence} to the
assumed $\mathsf{ListConsBoundaryCoherent}_{A,\sim_A}$ witness.

Next read back the public successor endpoint $h$. Apply
Theorem~\ref{thm:list-public-successor-length-cons-readback} to
$\mathsf{ListPublicLength}_A(h,n+1)$, obtaining histories $a,t,p$ with
$A(a)$, $\mathsf{ListHistoryCarrier}_A(t)$,
$\mathsf{ListPublicLength}_A(t,n)$, $\ContR(a,t,p)$, and
$\hsame(h,\Eone(p))$.

Read back the classified successor endpoint $k$ with
Theorem~\ref{thm:list-classified-successor-length-cons-readback}, using the
same classifier witness and public length witness. Rename its histories as
$b,u,q$. This gives $A(b)$, $\mathsf{ListHistoryCarrier}_A(u)$,
$\mathsf{ListPublicLength}_A(u,n)$, $\ContR(b,u,q)$, and
$\hsame(k,\Eone(q))$.

Finally apply Theorem~\ref{thm:list-public-classifier-cons-endpoint-inversion}
to the two displayed cons endpoint presentations and the original
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(h,k)$ witness. The conclusion
is $a\sim_A b$ together with
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(t,u)$. Repack these with the
two readback packages to obtain the displayed existential witnesses.
\end{proof}

\begin{theorem}[List classified successor component readback determinacy]
\label{thm:list-classified-successor-component-readback-determinacy}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier and let
$\sim_A$ be a source classifier. Assume
$\mathsf{ListConsBoundaryCoherent}_{A,\sim_A}$. For histories
$h,k,a,b,t,u,p,q,a',b',t',u',p',q'$ and every finite length value $n$, suppose
the two six-tuples $(a,b,t,u,p,q)$ and $(a',b',t',u',p',q')$ both satisfy the
classified successor component package for the same $h,k,n$:
$$
\begin{array}{l}
A(a)\land A(b)\land
\mathsf{ListHistoryCarrier}_A(t)\land
\mathsf{ListHistoryCarrier}_A(u)\land\\
\mathsf{ListPublicLength}_A(t,n)\land
\mathsf{ListPublicLength}_A(u,n)\land\\
\ContR(a,t,p)\land \ContR(b,u,q)\land\\
\hsame(h,\Eone(p))\land \hsame(k,\Eone(q))\land\\
a\sim_A b\land
\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(t,u)
\end{array}
$$
and
$$
\begin{array}{l}
A(a')\land A(b')\land
\mathsf{ListHistoryCarrier}_A(t')\land
\mathsf{ListHistoryCarrier}_A(u')\land\\
\mathsf{ListPublicLength}_A(t',n)\land
\mathsf{ListPublicLength}_A(u',n)\land\\
\ContR(a',t',p')\land \ContR(b',u',q')\land\\
\hsame(h,\Eone(p'))\land \hsame(k,\Eone(q'))\land\\
a'\sim_A b'\land
\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(t',u')
\end{array}
$$
hold. Then
$$
a\sim_A a'\land b\sim_A b'\land
\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(t,t')\land
\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(u,u').
$$
The ten-field fit is: traditional target finite-list classified successor
component readback; BEDC source $\mathsf{BHist}$ with $\ContR$; carrier
$\mathsf{ListHistoryCarrier}_A$; classifier
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}$; stability from
cons-boundary coherence; ledger same public successor endpoints $h,k$ with
common predecessor length $n$; core theorem witness-choice determinacy;
exactness by public cons endpoint readback; standard bridge finite spines;
status $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:list-public-cons-readback-deterministic} to the two
$h$-side cons presentations. The first package supplies $A(a)$,
$\mathsf{ListHistoryCarrier}_A(t)$, $\ContR(a,t,p)$, and
$\hsame(h,\Eone(p))$; the second supplies the corresponding data for
$a',t',p'$. The conclusion gives $a\sim_A a'$ and
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(t,t')$.

Apply the same theorem to the two $k$-side cons presentations. The first
package supplies $A(b)$, $\mathsf{ListHistoryCarrier}_A(u)$,
$\ContR(b,u,q)$, and $\hsame(k,\Eone(q))$; the second supplies the
corresponding data for $b',u',q'$. The conclusion gives $b\sim_A b'$ and
$\mathsf{ListSpineHistoryClassifier}_{A,\sim_A}(u,u')$. Repack the four
conclusions in the displayed order.
\end{proof}

\begin{definition}[List source hsame compatibility]
\label{def:list-source-hsame-compatible}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier and let
$\sim_A$ be a source classifier. Define
$\mathsf{ListSourceHsameCompatible}(A,\sim_A)$ as source-carrier completeness for
$\hsame$:
$$
\forall a\,b.\;
A(a)\to A(b)\to \hsame(a,b)\to a\sim_A b.
$$
\end{definition}
\leandef{BEDC.Derived.ListUp.ListSourceHsameCompatible}

\begin{theorem}[List source hsame compatibility for hsame]
\label{thm:list-source-hsame-compatible-hsame}
For every BEDC history source carrier $A:\mathsf{BHist}\to\mathsf{Prop}$,
$$
\mathsf{ListSourceHsameCompatible}(A,\hsame).
$$
\end{theorem}
\begin{proof}
Unfold $\mathsf{ListSourceHsameCompatible}$. For histories $a$ and $b$, the
two source-carrier witnesses $A(a)$ and $A(b)$ are part of the interface, and
the final displayed input is already the required $\hsame(a,b)$ classifier
witness. Return that witness.
\end{proof}

\begin{theorem}[List source-classifier weakening]
\label{thm:list-source-classifier-weakening}
Let $A,B:\mathsf{BHist}\to\mathsf{Prop}$ be BEDC history source carriers, and
let $\sim_A$ and $\sim_B$ be source classifiers. The ten-field fit is:
traditional target finite-list source refinement; BEDC source $\mathsf{BHist}$
with $\ContR$; source relation carried by $\sim_A$ and $\sim_B$; carrier
generated list/spine bridge; classifier recursive and spine bridge; stability
under source/classifier refinement; ledger unchanged finite spine; core theorem
the four displayed implications below; exactness preservation under
source/classifier refinement; status $\mathsf{ProofTarget}$. If source
membership and source classification weaken from $A$ to $B$,
$$
(\forall a:\mathsf{BHist}.\; A(a)\to B(a))
\land
(\forall a\,b:\mathsf{BHist}.\; a\sim_A b\to a\sim_B b),
$$
then for all histories $h,k$ and finite spines $xs$,
$$
\begin{array}{rcl}
\mathsf{ListHistoryCarrier}_A(h) &\to&
\mathsf{ListHistoryCarrier}_B(h),\\
\mathsf{ListHistoryClassifier}^{\mathrm{rec}}_{A,\sim_A}(h,k) &\to&
\mathsf{ListHistoryClassifier}^{\mathrm{rec}}_{B,\sim_B}(h,k),\\
\mathsf{ListSpineRep}_A(h,xs) &\to& \mathsf{ListSpineRep}_B(h,xs),\\
\mathsf{ListSpineBridgeClassifier}_{A,\sim_A}(h,k) &\to&
\mathsf{ListSpineBridgeClassifier}_{B,\sim_B}(h,k).
\end{array}
$$
\end{theorem}
\begin{proof}
Induct separately on the displayed carrier, recursive classifier, spine
representation, and bridge witness. The nil clauses are rebuilt directly. In
each cons clause, apply the source-carrier inclusion to the head datum, apply the
classifier inclusion to the head classifier datum when present, and reuse the
same $\ContR$ and endpoint $\hsame$ witnesses. For a bridge witness, keep the
same finite spines, transport the two spine representations by the representation
induction, transport the finite-spine classifier entry by entry using the
classifier inclusion, and rebuild the bridge witness with those transported
components.
\end{proof}

\input{parts/concrete_instances/list/11_unary_length_append_additivity}
