\chapter{A Concrete Naming Certificate for $\MetaCICConfluenceBudgetMatrixUp$}
\label{ch:concrete-instances-metacic-confluence-budget-matrix-namecert}
\origin{ai}

The $\MetaCICConfluenceBudgetMatrixUp$ packet realizes the budget-matrix frontier in \autoref{sec:metacic-open-problems-confluence} as finite NameCert data. It sits beside the residual-substitution boundary of \autoref{ch:concrete-instances-metacic-residual-substitution-boundary-namecert}, the confluence diamond ledger of \autoref{ch:concrete-instances-metacic-confluence-diamond-ledger-namecert}, the residual local-diamond witness of \autoref{ch:concrete-instances-metacic-residual-local-diamond-witness-namecert}, the residual-join budget of \autoref{ch:concrete-instances-metacic-residual-join-budget-namecert}, and the parallel-diamond selector of \autoref{ch:concrete-instances-metacic-parallel-diamond-selector-namecert}. The packet records bounded residual-substitution, closed parallel-diamond, and arbitrary closed-star confluence frontiers in one matrix; it does not supply unconditional substitution compatibility, arbitrary closed-term Church--Rosser, global subject reduction, or kernel consistency.

\begin{definition}[MetaCIC confluence budget-matrix carrier]
\label{def:metacic-confluence-budget-matrix-carrier}
A $\MetaCICConfluenceBudgetMatrixUp$ carrier is a finite $\BHist$ packet
$$
M=(T,R,D,S,J,B,O,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the typed closed-window row naming the inspected MetaCIC source, context boundary, and closedness budget.
\item $R$ is the residual-substitution frontier row recording the substitution compatibility data that remain visible.
\item $D$ is the closed parallel-diamond frontier row recording the bounded diamond premise or retained diamond obstruction.
\item $S$ is the closed star-confluence frontier row recording the finite multi-step budget attached to the same source window.
\item $J$ is the local-join scope row listing the candidate joins that are admitted inside the displayed matrix.
\item $B$ is the budget matrix row aligning residual-substitution, parallel-diamond, and closed-star budgets by finite row and column names.
\item $O$ is the obstruction row retaining the boundary names not discharged by $R,D,S,J,B$.
\item $L$ is the Lean TasteGate obligation row, naming the later need for $\mathsf{Nontrivial}\ \MetaCICConfluenceBudgetMatrixUp$ and $\mathsf{FieldFaithful}\ \MetaCICConfluenceBudgetMatrixUp$ instances before a checked TasteGate theorem can read the matrix as a single carrier.
\item $H$ records componentwise $\hsame$ transport for $T,R,D,S,J,B,O,L,C,P,N$.
\item $C$ records displayed $\Cont$ replay from a confluence-frontier request through $T,R,D,S,J,B,O,L$ and then to the structural rows.
\item $P$ is the $\Pkg$ provenance over $\MetaCICConfluenceBudgetMatrixUp$, $\MetaCICResidualSubstitutionBoundaryUp$, $\MetaCICConfluenceDiamondLedgerUp$, $\MetaCICParallelDiamondSelectorUp$, $\MetacicResidualDiamondReadbackUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetaCICConfluenceBudgetMatrixUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $T,R,D,S,J,B,O,L,H,C,P,N$. It has no coordinate for unconditional substitution compatibility, arbitrary closed-term Church--Rosser, a hidden confluence oracle, unbounded checker search, or kernel consistency.
\end{definition}

\begin{theorem}[MetaCIC confluence budget-matrix NameCert obligations]
\label{thm:metacic-confluence-budget-matrix-namecert-obligations}
For every accepted $\MetaCICConfluenceBudgetMatrixUp$ carrier $M=(T,R,D,S,J,B,O,L,H,C,P,N)$, the local $\NameCert_{\MetaCICConfluenceBudgetMatrixUp}$ surface consists exactly of typed closed-window exposure through $T$, residual-substitution frontier exposure through $R$, closed parallel-diamond frontier exposure through $D$, closed star-confluence frontier exposure through $S$, local-join scope through $J$, finite budget-matrix alignment through $B$, obstruction retention through $O$, Lean TasteGate obligation naming through $L$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-confluence-budget-matrix-carrier}. The source row $T$ fixes the typed closed window before any confluence-facing frontier row can be read.

The rows $R,D,S$ expose the three open confluence frontiers as visible data: residual-substitution compatibility, closed parallel diamond, and arbitrary closed-star confluence. The local-join row $J$ and matrix row $B$ align only finite candidate joins and finite row-column budgets for that same closed window.

The obstruction row $O$ remains visible, so the matrix cannot erase a missing substitution-compatibility input or promote a bounded local join into arbitrary closed-term Church--Rosser. The TasteGate row $L$ records that later Lean work must provide $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances for this carrier before a checked gate can certify single-carrier alignment.

Finally, $H,C,P,N$ transport, replay, source, and name the same finite route. Since no carrier coordinate stores an unconditional confluence theorem, hidden oracle, unbounded checker, or kernel-consistency endpoint, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[MetaCIC confluence budget-matrix local join scope]
\label{thm:metacic-confluence-budget-matrix-local-join-scope}
Every accepted $\MetaCICConfluenceBudgetMatrixUp$ carrier that exports a local join from $J$ must keep the typed closed-window row $T$, residual-substitution frontier $R$, closed parallel-diamond frontier $D$, closed star-confluence frontier $S$, budget matrix $B$, and obstruction row $O$ visible on the same route. Thus a local join admitted by the matrix is scoped to the displayed finite budget and cannot be read as unconditional substitution compatibility, arbitrary closed-term Church--Rosser, or global subject reduction.
\end{theorem}
\begin{proof}
Start with the carrier rows of \autoref{def:metacic-confluence-budget-matrix-carrier}. The local-join row $J$ is displayed only together with the frontier rows $R,D,S$ and the budget matrix $B$ for the same typed closed window $T$.

Apply the NameCert obligations of \autoref{thm:metacic-confluence-budget-matrix-namecert-obligations}. They force any confluence-facing read to expose the residual-substitution frontier, closed parallel-diamond frontier, closed star-confluence frontier, local-join scope, finite budget alignment, and obstruction retention before the structural rows can be consumed.

The obstruction row $O$ prevents the matrix from hiding the conditional boundary around substitution compatibility. Transport through $H$, replay through $C$, provenance through $P$, and naming through $N$ preserve only the displayed matrix route, so the exported local join remains scoped to the finite budget rather than becoming an unconditional confluence endpoint.
\end{proof}

\falsifiablePrediction{Every accepted $\MetaCICConfluenceBudgetMatrixUp$ consumer that exports a confluence-facing local join must display typed closed-window, residual-substitution frontier, closed parallel-diamond frontier, closed star-confluence frontier, finite budget-matrix, obstruction, TasteGate-obligation, transport, replay, provenance, and local NameCert rows.}

\independenceWitness{metacic-residual-substitution-boundary, metacic-confluence-diamond-ledger, metacic-residual-local-diamond-witness, metacic-residual-join-budget, metacic-parallel-diamond-selector: no listed sibling is bijective to $\MetaCICConfluenceBudgetMatrixUp$ because this packet jointly carries the residual-substitution, closed parallel-diamond, closed star-confluence, local-join, finite matrix, obstruction-retention, TasteGate-obligation, transport, replay, provenance, and local naming rows.}

\closureat{MetaCICConfluenceBudgetMatrixUp}{seedStr}

\begin{closurestatus}{\MetaCICConfluenceBudgetMatrixUp}
  \constructivestory{A $\MetaCICConfluenceBudgetMatrixUp$ packet is generated from a typed closed-window row, residual-substitution frontier, closed parallel-diamond frontier, closed star-confluence frontier, local-join scope, finite budget matrix, obstruction row, Lean TasteGate obligation row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-confluence-budget-matrix-carrier}, \autoref{thm:metacic-confluence-budget-matrix-namecert-obligations}, and \autoref{thm:metacic-confluence-budget-matrix-local-join-scope} seed the finite budget-matrix frontier for MetaCIC confluence work.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim unconditional substitution compatibility, arbitrary closed-term Church--Rosser, global subject reduction, unbounded checker search, Lean verification, bridge checking, or kernel consistency.}
  \upgradepath{Obligation closure requires checked carrier admission, typed-window exposure, residual-substitution frontier exposure, closed parallel-diamond frontier exposure, closed star-confluence frontier exposure, local-join scope, budget-matrix alignment, obstruction retention, transport, replay, provenance, local naming, and TasteGate Nontrivial and FieldFaithful instances for BEDC.Derived.MetaCICConfluenceBudgetMatrixUp.}
\end{closurestatus}
