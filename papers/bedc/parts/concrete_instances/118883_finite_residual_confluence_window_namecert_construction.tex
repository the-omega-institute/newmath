\chapter{A Concrete Naming Certificate for $\FiniteResidualConfluenceWindowUp$}
\label{ch:concrete-instances-finite-residual-confluence-window-namecert}
\origin{ai}

The $\FiniteResidualConfluenceWindowUp$ packet realizes the MetaCIC confluence audit map as bounded residual-join evidence. It sits beside the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}, the local-join ledger of \autoref{ch:concrete-instances-metacic-local-join-ledger-namecert}, the residual critical-pair budget of \autoref{ch:concrete-instances-metacic-residual-critical-pair-budget-namecert}, the residual-join determinacy packet of \autoref{ch:concrete-instances-metacic-residual-join-determinacy-namecert}, and the bounded normal-equality checker of \autoref{ch:concrete-instances-bounded-normal-equality-checker-namecert}. The chapter records a finite inspection window for local residual joins; it does not claim global confluence, unrestricted residual-substitution compatibility, subject reduction, quotient syntax equality, evaluator soundness, or kernel consistency.

\begin{definition}[Finite residual confluence window carrier]
\label{def:finite-residual-confluence-window-carrier}
A $\FiniteResidualConfluenceWindowUp$ carrier is a finite $\BHist$ packet
$$
W=(T,R,K,J,B,O,G,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the typed-window row, naming the finite MetaCIC source and target fragments inspected before residual data are read.
\item $R$ is the residual-frontier row, listing the bounded redex and residual branches visible inside the window.
\item $K$ is the finite budget row, recording the depth, branch, and critical-pair bounds consumed by the window.
\item $J$ is the candidate local-join row, carrying the displayed common-reduct candidates for the bounded residual branches.
\item $B$ is the bounded checker row, routed through the bounded normal-equality checker before a candidate join is accepted.
\item $O$ is the obstruction-retention row, recording that residual-substitution compatibility and arbitrary parallel diamond remain named premises outside the packet.
\item $G$ is the TasteGate preparation row for future $\mathrm{Nontrivial}$ and $\mathrm{FieldFaithful}$ instances over the displayed carrier fields.
\item $H$ records componentwise $\hsame$ transport for $T,R,K,J,B,O,G,P,N$.
\item $C$ records displayed $\Cont$ replay through the typed window, residual frontier, finite budget, candidate join, bounded checker, obstruction, TasteGate preparation, provenance, and local naming rows.
\item $P$ is the $\Pkg$ provenance over $\FiniteResidualConfluenceWindowUp$, $\MetaCICParallelDiamondFrontierUp$, $\MetaCICLocalJoinLedgerUp$, $\MetaCICResidualCriticalPairBudgetUp$, $\MetaCICResidualJoinDeterminacyUp$, $\BoundedNormalEqualityCheckerUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteResidualConfluenceWindowUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $T,R,K,J,B,O,G,H,C,P,N$. It has no coordinate for a global Church--Rosser theorem, unrestricted substitution compatibility, subject reduction, quotient conversion equality, or a host evaluator.
\end{definition}

\begin{theorem}[Finite residual confluence window obligations]
\label{thm:finite-residual-confluence-window-namecert-obligations}
For every accepted $\FiniteResidualConfluenceWindowUp$ carrier $W=(T,R,K,J,B,O,G,H,C,P,N)$, the local $\NameCert_{\FiniteResidualConfluenceWindowUp}$ surface consists exactly of typed-window exposure through $T$, residual-frontier exposure through $R$, finite budget exposure through $K$, candidate local-join exposure through $J$, bounded checker exposure through $B$, obstruction retention through $O$, TasteGate preparation through $G$, transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-residual-confluence-window-carrier}. The confluence-facing rows are $T,R,K,J,B,O$, and the structural rows are $G,H,C,P,N$.

Read the bounded evidence in order. The typed window $T$ and residual frontier $R$ identify the inspected peak, the budget row $K$ bounds the branch search, and the candidate local-join row $J$ names the common-reduct candidates that survive that finite window.

The bounded checker row $B$ verifies only the displayed candidate comparison. The obstruction row $O$ keeps residual-substitution compatibility and arbitrary parallel diamond outside the packet, while $G,H,C,P,N$ preserve the same finite carrier fields for future TasteGate, transport, replay, provenance, and local naming obligations.
\end{proof}

\begin{theorem}[Finite residual confluence window non-escape]
\label{thm:finite-residual-confluence-window-nonescape}
Every confluence-audit consumer of an accepted $\FiniteResidualConfluenceWindowUp$ packet receives only bounded local join evidence through $T,R,K,J,B,O,G,H,C,P,N$. The packet may certify that a displayed residual peak has a bounded candidate join, but it cannot erase $O$, manufacture residual-substitution compatibility, prove a full parallel diamond, export arbitrary closed-term confluence, or supply subject reduction.
\end{theorem}
\begin{proof}
Start with the typed window and residual frontier. They name only the finite source, target, redex, and residual branches inspected by the packet.

Use the budget and candidate rows next. The rows $K$ and $J$ restrict candidate common reducts to the displayed window, and the bounded checker row $B$ compares only those candidates.

Finally read the obstruction and structural rows. The obstruction row $O$ records the missing global inputs, while $G,H,C,P,N$ prepare field-faithful TasteGate checking, transport, replay, source, and name the same finite surface. Thus the consumer receives bounded residual-join evidence and nothing stronger.
\end{proof}

\falsifiablePrediction{A confluence-audit read that accepts $\FiniteResidualConfluenceWindowUp$ while omitting any of $T,R,K,J,B,O,G,H,C,P,N$ refutes the carrier surface.}

\independenceWitness{metacic-parallel-diamond-frontier, metacic-local-join-ledger, metacic-residual-critical-pair-budget, metacic-residual-join-determinacy, bounded-normal-equality-checker: $\FiniteResidualConfluenceWindowUp$ is not bijective to any listed sibling because it jointly binds a typed window, residual frontier, finite budget, candidate local join, bounded checker, obstruction retention, TasteGate preparation, transport, replay, provenance, and local NameCert row.}

The Lean follow-up for $\FiniteResidualConfluenceWindowUp$ must provide $\mathrm{Nontrivial}$ and $\mathrm{FieldFaithful}$ instances over the displayed carrier fields before the packet can be consumed as checked confluence evidence.

\closureat{FiniteResidualConfluenceWindowUp}{seedStr}

\begin{closurestatus}{\FiniteResidualConfluenceWindowUp}
  \constructivestory{A $\FiniteResidualConfluenceWindowUp$ packet is generated from typed-window rows, residual-frontier rows, finite budget rows, candidate local joins, bounded checker rows, obstruction-retention rows, TasteGate preparation rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-residual-confluence-window-carrier}, \autoref{thm:finite-residual-confluence-window-namecert-obligations}, and \autoref{thm:finite-residual-confluence-window-nonescape} seed the bounded residual-join evidence surface for the MetaCIC confluence audit map.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close global confluence, unrestricted residual-substitution compatibility, parallel diamond, subject reduction, quotient syntax equality, Lean verification, or bridge checking for $\FiniteResidualConfluenceWindowUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, typed-window exposure, residual-frontier exposure, finite budget exactness, candidate local-join exactness, bounded checker exactness, obstruction retention, Nontrivial and FieldFaithful TasteGate instances, transport, replay, provenance, and local naming for BEDC.Derived.FiniteResidualConfluenceWindowUp.}
\end{closurestatus}
