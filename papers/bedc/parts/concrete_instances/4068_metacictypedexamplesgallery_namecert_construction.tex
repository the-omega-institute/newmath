\chapter{A Concrete Naming Certificate for $\MetaCICTypedExamplesGalleryUp$}
\label{ch:concrete-instances-metacictypedexamplesgallery-namecert}
\origin{human}

The $\MetaCICTypedExamplesGalleryUp$ packet realizes the typed-example audit map of \autoref{ch:metacic-typedexamples-audit-map} as a finite NameCert gallery. It sits beside the confluence audit surface of \autoref{ch:concrete-instances-metacic-confluence-audit-namecert} and the closed-term substitution boundary of \autoref{ch:concrete-instances-closedtermsubstitutionboundary-namecert}: those chapters constrain global metatheorem reads, while this chapter exposes the worked syntax and typing examples as finite rows.

\begin{definition}[MetaCIC typed-examples gallery carrier]
\label{def:metacic-typedexamples-gallery-carrier}
A $\MetaCICTypedExamplesGalleryUp$ carrier is a finite $\BHist$ packet
$$
G=(B,N,L,S,E,I,A,R,K,H,C,P,M)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ records the Church Bool example rows, including the displayed true, false, and eliminator typing reads;
\item $N$ records the Church Nat example rows, including zero, successor, and numeral typing reads;
\item $L$ records the Church List example rows, including nil, cons, and eliminator typing reads;
\item $S$ records the Church Sum example rows, including left injection, right injection, and case-analysis typing reads;
\item $E$ records equality example rows, including reflexivity and transport-shaped reads that stay inside the displayed typing ledger;
\item $I$ records identity example rows, including polymorphic identity and dependent identity examples;
\item $A$ records application example rows, including function application and argument instantiation reads;
\item $R$ records beta-redex example rows and their displayed reduction endpoints;
\item $K$ records checker-acceptance rows for the example families named by $B,N,L,S,E,I,A,R$;
\item $H$ records componentwise $\hsame$ transport for every gallery row, route row, provenance row, and local naming row;
\item $C$ records the displayed $\Cont$ routes for syntax readback, typing readback, beta-redex replay, checker-acceptance replay, and consumer exposure;
\item $P$ records $\Pkg$ provenance over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, the MetaCIC typed-example rows, and the checker rows named by \autoref{ch:metacic-typedexamples-audit-map};
\item $M$ is the local $\NameCert_{\MetaCICTypedExamplesGalleryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only by these displayed rows. It has no coordinate for arbitrary closed-term normalization, confluence, subject reduction, conversion decidability, or a complete algebraic theory of the Church encodings.
\end{definition}

\begin{theorem}[MetaCIC typed-examples gallery obligation matrix]
\label{thm:metacictypedexamplesgallery-obligation-matrix}
Each accepted $\MetaCICTypedExamplesGalleryUp$ typed example row must expose its context row, term row, typing derivation row, reduction-window row, local-join row, normalization-frontier row, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and $\NameCert$ row before it can be consumed by $\MetaCICCriticalPathUp$. The matrix is read from the finite gallery rows
$$
B,N,L,S,E,I,A,R,K,H,C,P,M,
$$
with local-join and normalization-frontier access routed through the beta-redex and checker rows $R,K$ rather than through a global confluence, subject-reduction, normalization, or conversion-completeness theorem.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-typedexamples-gallery-carrier}. The family rows $B,N,L,S,E,I,A,R$ are the only context, term, typing, and reduction-window sources available to an accepted gallery example, while $K$ is the checker boundary attached to those displayed sources.

The local-join and normalization-frontier reads are therefore sibling routes of the finite beta-redex and checker surface. They are the same finite rows later isolated by \autoref{thm:metacic-typedexamples-gallery-typed-family-coverage} and \autoref{thm:metacic-typedexamples-gallery-checker-route-factorization}, with the redex-frontier handoff boundary recorded in \autoref{thm:metacic-redex-frontier-normalization-window-handoff}.

Finally add the structural rows. Transport is projected through $H$, replay through $C$, provenance through $P$, and local naming through $M$. Since the carrier has no coordinate for global confluence, subject reduction, normalization, or conversion completeness, $\MetaCICCriticalPathUp$ can consume only the displayed obligation matrix.
\end{proof}

\begin{theorem}[MetaCIC typed-examples gallery NameCert obligations]
\label{thm:metacic-typedexamples-gallery-namecert-obligations}
For every accepted $\MetaCICTypedExamplesGalleryUp$ carrier
$$
G=(B,N,L,S,E,I,A,R,K,H,C,P,M),
$$
the local $\NameCert_{\MetaCICTypedExamplesGalleryUp}$ surface consists exactly of carrier exposure for the finite gallery, representative theorem inventory for the Church Bool, Nat, List, Sum, equality, identity, application, and beta-redex rows, regression exactness for the displayed example endpoints, checker handoff through $K$, non-escape from global metatheorems, and ledger provenance through $P,M$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-typedexamples-gallery-carrier}. The rows $B,N,L,S,E,I,A,R$ are the complete representative theorem inventory exposed by this gallery packet.

The checker handoff is the row $K$, and every read of $K$ is replayed through the displayed route row $C$. Componentwise transport through $H$ preserves the same example rows and does not add a new theorem coordinate.

Finally read provenance through $P$ and local naming through $M$. Since the carrier has no coordinate for subject reduction, confluence, normalization, or conversion decidability, the gallery obligations are exhausted by the finite example rows and their checker-acceptance routes.
\end{proof}

\begin{theorem}[MetaCIC typed-examples gallery obligation surface]
\label{thm:metacic-typedexamples-gallery-obligation-surface}
For every accepted $\MetaCICTypedExamplesGalleryUp$ carrier
$$
G=(B,N,L,S,E,I,A,R,K,H,C,P,M),
$$
the obligation surface consists exactly of the Church Bool row $B$, Church Nat row $N$, Church List row $L$, Church Sum row $S$, equality row $E$, identity row $I$, application row $A$, beta-redex row $R$, checker-acceptance row $K$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and the local $\NameCert_{\MetaCICTypedExamplesGalleryUp}$ row $M$. It excludes subject reduction, confluence, normalization, conversion completeness, arbitrary example coverage, and checker completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:metacic-typedexamples-gallery-carrier}. The rows $B,N,L,S,E,I,A,R$ are the only example-family rows, and $K$ is the only checker-acceptance row attached to them.

The structural support is also displayed. Componentwise transport is read through $H$, replay through $C$, provenance through $P$, and local naming through $M$.

There is no carrier coordinate for subject reduction, confluence, normalization, conversion completeness, arbitrary example coverage, or checker completeness. Therefore the obligation surface is bounded by exactly the displayed rows.
\end{proof}

\begin{theorem}[MetaCIC typed-examples gallery regression surface]
\label{thm:metacic-typedexamples-gallery-regression-surface}
An accepted $\MetaCICTypedExamplesGalleryUp$ packet is a finite regression surface for represented syntax and typing. Consumers may inspect the Church Bool, Nat, List, Sum, equality, identity, application, beta-redex, checker-acceptance, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows, but they cannot read the packet as a subject-reduction theorem, a confluence theorem, a normalization theorem, or a decidability theorem.
\end{theorem}
\begin{proof}
Start with the visible coordinates of $G$. They are example rows and routes for syntax, typing, beta-redex replay, checker acceptance, transport, provenance, and local naming.

Each consumer read factors through the finite route row $C$ and the checker row $K$. These rows can replay the represented gallery examples, but they have no endpoint for a global preservation or conversion principle.

Therefore the accepted packet is exactly a finite regression surface for the displayed examples. The excluded metatheorems remain outside the carrier because no row of $G$ can be projected as such a theorem.
\end{proof}

\begin{theorem}[MetaCIC typed-examples gallery typed-family coverage]
\label{thm:metacic-typedexamples-gallery-typed-family-coverage}
For every accepted $\MetaCICTypedExamplesGalleryUp$ carrier
$$
G=(B,N,L,S,E,I,A,R,K,H,C,P,M),
$$
every typed-example family read exposed by the gallery factors through one of the displayed family rows $B,N,L,S,E,I,A,R$ and then through the checker row $K$, componentwise transport row $H$, replay row $C$, provenance row $P$, and local naming row $M$. The gallery exposes no further subject-reduction family, confluence family, normalization family, conversion-completeness family, arbitrary-example-coverage family, or checker-completeness family.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-typedexamples-gallery-carrier}. Its only typed-example family rows are the Church Bool, Church Nat, Church List, Church Sum, equality, identity, application, and beta-redex rows named by $B,N,L,S,E,I,A,R$.

Now split over that finite displayed family inventory. A Bool read is routed through $B$, a Nat read through $N$, a List read through $L$, a Sum read through $S$, an equality read through $E$, an identity read through $I$, an application read through $A$, and a beta-redex read through $R$.

Checker acceptance is then forced through $K$. Transport of the same family row is read through $H$, replay is read through $C$, and the exported consumer-facing certificate is packaged by $P$ and named by $M$.

Finally apply the regression surface of \autoref{thm:metacic-typedexamples-gallery-regression-surface}. Since the carrier has no coordinate for subject reduction, confluence, normalization, conversion completeness, arbitrary example coverage, or checker completeness, no additional typed-family read is exposed by the gallery.
\end{proof}

\begin{theorem}[MetaCIC typed-examples gallery checker-route factorization]
\label{thm:metacic-typedexamples-gallery-checker-route-factorization}
For every accepted $\MetaCICTypedExamplesGalleryUp$ carrier
$$
G=(B,N,L,S,E,I,A,R,K,H,C,P,M),
$$
every gallery consumer read factors through exactly one displayed example-family row among $B,N,L,S,E,I,A,R$ and then through $K,H,C,P,M$. The checker row $K$ is therefore only the finite acceptance boundary for the chosen displayed family. It cannot be read as subject reduction, confluence, normalization, conversion completeness, or checker completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-typedexamples-gallery-carrier}. Its displayed example-family rows are exactly $B,N,L,S,E,I,A,R$, so a gallery read first chooses one of those finite rows.

Case split over the displayed family row. Bool reads route through $B$, Nat reads through $N$, List reads through $L$, Sum reads through $S$, equality reads through $E$, identity reads through $I$, application reads through $A$, and beta-redex reads through $R$.

After that family choice, acceptance must pass through $K$, and the same accepted route is transported by $H$, replayed by $C$, sourced by $P$, and named by $M$.

Finally cite the regression surface theorem \autoref{thm:metacic-typedexamples-gallery-regression-surface}. Since the packet has no coordinate for the excluded global metatheorems, the checker row remains a finite family acceptance boundary and cannot be consumed as subject reduction, confluence, normalization, conversion completeness, or checker completeness.
\end{proof}
\leanchecked{BEDC.MetaCIC.gallery\_checker\_route\_factorization}

\begin{theorem}[MetaCIC typed-examples gallery kernel scope]
\label{thm:metacic-typed-examples-gallery-kernel-scope}
For every accepted $\MetaCICTypedExamplesGalleryUp$ carrier
$$
G=(B,N,L,S,E,I,A,R,K,H,C,P,M),
$$
the kernel-facing scope of the gallery is exactly the finite $\BHist$ route named by \autoref{def:metacic-typedexamples-gallery-carrier}, constrained by the obligation matrix of \autoref{thm:metacictypedexamplesgallery-obligation-matrix}, the typed-family coverage of \autoref{thm:metacic-typedexamples-gallery-typed-family-coverage}, and the checker-route factorization of \autoref{thm:metacic-typedexamples-gallery-checker-route-factorization}. A kernel consumer may inspect the displayed family row, checker row, transport row, replay row, provenance row, and local $\NameCert_{\MetaCICTypedExamplesGalleryUp}$ row. It receives no subject reduction, confluence, normalization, conversion completeness, arbitrary example coverage, or checker completeness theorem.
\end{theorem}
\leanchecked{BEDC.MetaCIC.gallery\_kernel\_scope}
\begin{proof}
Project the carrier of \autoref{def:metacic-typedexamples-gallery-carrier}. The only kernel-facing rows are the displayed family rows $B,N,L,S,E,I,A,R$, the checker row $K$, transport row $H$, replay row $C$, provenance row $P$, and local naming row $M$.

Apply the obligation matrix of \autoref{thm:metacictypedexamplesgallery-obligation-matrix}. It forces each consumed example to display its context, term, typing, reduction-window, local-join, normalization-frontier, transport, replay, provenance, and local naming rows inside the same finite packet.

Now apply the typed-family coverage and checker-route factorization theorems. They place every family read inside one displayed row and then through $K,H,C,P,M$. Since the carrier has no coordinate for the excluded global metatheorems, the kernel-facing scope is exactly this finite route.
\end{proof}

\begin{theorem}[MetaCIC typed-examples gallery coverage scope]
\label{thm:metacic-typed-examples-gallery-coverage-scope}
For every accepted $\MetaCICTypedExamplesGalleryUp$ carrier
$$
G=(B,N,L,S,E,I,A,R,K,H,C,P,M),
$$
the coverage claim exported by the gallery is scoped to the displayed Church Bool, Church Nat, Church List, Church Sum, equality, identity, application, and beta-redex rows. The claim is read through the carrier of \autoref{def:metacic-typedexamples-gallery-carrier}, the obligation matrix of \autoref{thm:metacictypedexamplesgallery-obligation-matrix}, the typed-family coverage of \autoref{thm:metacic-typedexamples-gallery-typed-family-coverage}, and the checker-route factorization of \autoref{thm:metacic-typedexamples-gallery-checker-route-factorization}. It does not cover arbitrary examples, checker completeness, subject reduction, confluence, normalization, or conversion completeness.
\end{theorem}
\begin{proof}
Start with the finite family inventory in the carrier. It lists exactly the rows $B,N,L,S,E,I,A,R$ and gives no row for an arbitrary example family.

The obligation matrix requires each displayed family read to keep its local typing, reduction-window, checker, transport, replay, provenance, and naming data visible. Thus coverage is a row-by-row property of the finite inventory, not a theorem about every possible MetaCIC example.

Finally use typed-family coverage and checker-route factorization. The first theorem says every gallery family read factors through the displayed family rows, and the second says each consumer read then passes through $K,H,C,P,M$. The omitted global claims remain outside the scope because no carrier row names them.
\end{proof}

\begin{theorem}[MetaCIC typed-examples counterexample boundary]
\label{thm:metacic-typedexamples-gallery-subject-reduction-counterexample-boundary}
An accepted $\MetaCICTypedExamplesGalleryUp$ row can be consumed as a finite regression row or as a finite counterexample boundary only when its context row, term row, typing row, reduction-window row, checker row, transport row, replay row, provenance row, and local $\NameCert_{\MetaCICTypedExamplesGalleryUp}$ row are all displayed inside
$$
G=(B,N,L,S,E,I,A,R,K,H,C,P,M).
$$
The row cannot be read as global subject reduction, and it also cannot be read as a global failure of subject reduction. It is only bounded evidence about the displayed Church Bool, Church Nat, Church List, Church Sum, equality, identity, application, or beta-redex family chosen from $B,N,L,S,E,I,A,R$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-typedexamples-gallery-carrier}. The finite family row supplies the context, term, typing, and reduction-window data, while $K$ supplies the checker boundary and $H,C,P,M$ supply transport, replay, provenance, and local naming.

Apply the coverage scope of \autoref{thm:metacic-typed-examples-gallery-coverage-scope}. It restricts every exported claim to the displayed family rows and refuses arbitrary example coverage.

Now apply checker-route factorization from \autoref{thm:metacic-typedexamples-gallery-checker-route-factorization}. Any consumer read must pass through one displayed family and then through $K,H,C,P,M$, so a regression or counterexample use remains a finite row-boundary claim.

Since the carrier has no coordinate for global subject reduction and no coordinate for a global countertheorem, neither conclusion can be projected from the gallery packet.
\end{proof}

\begin{theorem}[MetaCIC typed-examples gallery scoped route]
\label{thm:metacic-typed-examples-gallery-scoped-route}
Every consumer route out of an accepted $\MetaCICTypedExamplesGalleryUp$ packet is a scoped route through
$$
B,N,L,S,E,I,A,R,K,H,C,P,M .
$$
The route is determined by the carrier of \autoref{def:metacic-typedexamples-gallery-carrier}, the obligation matrix of \autoref{thm:metacictypedexamplesgallery-obligation-matrix}, the typed-family coverage theorem of \autoref{thm:metacic-typedexamples-gallery-typed-family-coverage}, and the checker-route factorization theorem of \autoref{thm:metacic-typedexamples-gallery-checker-route-factorization}. The route exports only the bounded gallery surface and keeps subject reduction, confluence, normalization, conversion completeness, arbitrary example coverage, and checker completeness outside the claim.
\end{theorem}
\begin{proof}
Choose a consumer read from an accepted gallery packet. By the carrier definition, the read must start inside one of the displayed rows $B,N,L,S,E,I,A,R,K,H,C,P,M$.

If the read asks for example-family content, typed-family coverage forces a unique displayed family entrance among $B,N,L,S,E,I,A,R$. Checker-route factorization then sends the same read through $K,H,C,P,M$.

If the read asks for structural certificate content, the obligation matrix restricts it to the same transport, replay, provenance, and local naming rows. Therefore every consumer route remains inside the displayed scoped route, and the global metatheorem claims are not exported by this packet.
\end{proof}

\begin{theorem}[MetaCIC typed-examples gallery scope rows]
\label{thm:metacic-typedexamples-gallery-scope-rows}
For every accepted $\MetaCICTypedExamplesGalleryUp$ carrier
$$
G=(B,N,L,S,E,I,A,R,K,H,C,P,M),
$$
the accepted gallery scope is exactly the row set
$$
B,N,L,S,E,I,A,R,K,H,C,P,M .
$$
Rows $B,N,L,S,E,I,A,R$ are the displayed typed-family rows, $K$ is the checker boundary for those families, and $H,C,P,M$ are the transport, replay, provenance, and local naming rows. The scope contains no subject-reduction row, confluence row, normalization row, conversion-completeness row, arbitrary-example row, or checker-completeness row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-typedexamples-gallery-carrier}. Its displayed coordinates are precisely $B,N,L,S,E,I,A,R,K,H,C,P,M$, so every accepted row must be one of those coordinates.

Apply the obligation matrix of \autoref{thm:metacictypedexamplesgallery-obligation-matrix}. It requires each accepted gallery read to display its context, term, typing, reduction-window, local-join, normalization-frontier, transport, replay, provenance, and local naming data inside the finite carrier.

Next use typed-family coverage and checker-route factorization. The theorem \autoref{thm:metacic-typedexamples-gallery-typed-family-coverage} confines every family read to $B,N,L,S,E,I,A,R$, and \autoref{thm:metacic-typedexamples-gallery-checker-route-factorization} sends each such read through $K,H,C,P,M$.

Finally cite the scoped route of \autoref{thm:metacic-typed-examples-gallery-scoped-route}. It excludes the global metatheorem rows, so the accepted scope is exactly the displayed thirteen-row surface.
\end{proof}

\begin{theorem}[MetaCIC typed-examples gallery scope export]
\label{thm:metacic-typed-examples-gallery-scope-export}
Every exported gallery read from an accepted $\MetaCICTypedExamplesGalleryUp$ packet is exactly one displayed family row among
$$
B,N,L,S,E,I,A,R
$$
followed by the finite checker and certificate route
$$
K,H,C,P,M.
$$
No consumer may read the exported gallery as global subject reduction, confluence, normalization, conversion completeness, arbitrary example coverage, or checker completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-typedexamples-gallery-carrier}. The only family rows available for export are $B,N,L,S,E,I,A,R$, and the only post-family route rows are $K,H,C,P,M$.

Apply the obligation matrix and typed-family coverage theorems, \autoref{thm:metacictypedexamplesgallery-obligation-matrix} and \autoref{thm:metacic-typedexamples-gallery-typed-family-coverage}. They force each exported family read to keep its displayed context, term, typing, reduction-window, checker, transport, replay, provenance, and local naming data inside the finite carrier.

Use the checker-route factorization and scoped route theorems, \autoref{thm:metacic-typedexamples-gallery-checker-route-factorization} and \autoref{thm:metacic-typed-examples-gallery-scoped-route}. These identify the consumer route as one family row followed by $K,H,C,P,M$, with no bypass through a global checker theorem.

Finally apply the scope rows theorem \autoref{thm:metacic-typedexamples-gallery-scope-rows}. Since the accepted scope is exactly the thirteen displayed rows, the excluded global metatheorems and arbitrary coverage claims are not exportable from the gallery packet.
\end{proof}

\begin{theorem}[MetaCIC typed-examples gallery scoped-closure route]
\label{thm:metacic-typed-examples-gallery-scoped-closure-route}
For every accepted $\MetaCICTypedExamplesGalleryUp$ carrier
$$
G=(B,N,L,S,E,I,A,R,K,H,C,P,M),
$$
the scoped-closure route is exhausted by $B,N,L,S,E,I,A,R,K,H,C,P,M$ together with the obligation matrix, typed-family coverage, checker-route factorization, scoped-route theorem, and scope-rows theorem. The route refuses subject reduction, confluence, normalization, conversion completeness, arbitrary example coverage, and checker completeness as claims of the gallery packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-typedexamples-gallery-carrier}. Its only visible coordinates are the displayed family rows $B,N,L,S,E,I,A,R$, the checker boundary $K$, and the structural rows $H,C,P,M$.

Apply the obligation matrix, coverage theorem, and checker-route factorization: \autoref{thm:metacictypedexamplesgallery-obligation-matrix}, \autoref{thm:metacic-typedexamples-gallery-typed-family-coverage}, and \autoref{thm:metacic-typedexamples-gallery-checker-route-factorization}. They keep every accepted example read inside the displayed family rows and then route it through $K,H,C,P,M$.

Now use the scoped-route theorem and the scope-rows theorem, \autoref{thm:metacic-typed-examples-gallery-scoped-route} and \autoref{thm:metacic-typedexamples-gallery-scope-rows}. These identify the complete accepted route with $B,N,L,S,E,I,A,R,K,H,C,P,M$.

Since no carrier row names subject reduction, confluence, normalization, conversion completeness, arbitrary example coverage, or checker completeness, those global properties cannot be projected from the scoped gallery route.
\end{proof}

\begin{closurestatus}{\MetaCICTypedExamplesGalleryUp}
  \constructivestory{A $\MetaCICTypedExamplesGalleryUp$ packet is generated from finite $\BHist$ rows for selected typed examples, checker rows, beta or conversion readbacks, route rows, provenance, and local naming. Each gallery entry is replayable by $\Cont$ through the finite checker surface, so consumers can inspect represented examples without receiving a global calculus theorem. Componentwise $\hsame$ preserves the example, checker, and route rows, while $\Pkg$ records the MetaCIC typed-example dependencies. The gallery is therefore a bounded regression surface for displayed examples and not a proof of preservation, normalization, or checker completeness.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{This chapter binds the MetaCIC typed-examples gallery carrier, obligation matrix, checker-route factorization, finite regression surface, typed-family coverage, and exclusion of global metatheorems.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{It does not claim global subject reduction, confluence, normalization, conversion completeness, arbitrary example coverage, or checker completeness.}
  \upgradepath{Obligation closure requires Lean encodings of the gallery carrier, checker-route replay, componentwise transport, and regression-surface non-escape theorem.}
\end{closurestatus}
