\chapter{A Concrete Naming Certificate for $\BishopRegularCauchyCofinalStabilityUp$}
\label{ch:concrete-instances-bishop-regular-cauchy-cofinal-stability-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopRegularCauchyCofinalStabilityUp$ packet records the finite route by which a regular Cauchy rational name remains stable after a cofinal reindexing of its observation schedule. It sits over the regular rational sequence carrier of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream observations of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}, and the interleaving comparison route of \autoref{ch:concrete-instances-regularcauchyinterleaving-namecert}. The packet exposes cofinal schedule stability as finite NameCert data; it does not import quotient stream equality, selected subsequences, countable choice, host real equality, or ambient completeness of $\RealUp$.

\begin{definition}[Bishop regular Cauchy cofinal stability carrier]
\label{def:bishop-regular-cauchy-cofinal-stability-carrier}
A $\BishopRegularCauchyCofinalStabilityUp$ carrier is a finite $\BHist$ packet
$$
S=(R,K,D,W,I,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the source $\RegSeqRatUp$ regular rational name row.
\item $K$ is the cofinal schedule row, recording the finite monotone reindexing windows through which the same tail of $R$ is inspected.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row that fixes the dyadic budget before and after the cofinal schedule is read.
\item $W$ is the $\StreamNameUp$ finite-observation row opened along the schedule $K$.
\item $I$ is the $\RegularCauchyInterleavingUp$ comparison row, displaying the interleaving route between the original schedule and the cofinal schedule.
\item $E$ is the $\RealUp$ seal row reached only after $R,K,D,W,I$ have been exposed.
\item $H$ records componentwise $\hsame$ transport for the source name, cofinal schedule, dyadic budget, finite window, interleaving route, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a Real-facing tolerance request through $R,K,D,W,I$ and then into the terminal seal row $E$.
\item $P$ is the $\Pkg$ provenance over $\BishopRegularCauchyCofinalStabilityUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\RegularCauchyInterleavingUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopRegularCauchyCofinalStabilityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $R,K,D,W,I,E,H,C,P,N$. It has no coordinate for quotient equality of streams, a selected cofinal subsequence, countable-choice schedules, host real equality, or completeness of $\RealUp$.
\end{definition}
\leanstmt{BEDC.Derived.BishopRegularCauchyCofinalStabilityUp}

\begin{theorem}[Bishop regular Cauchy cofinal NameCert obligations]
\label{thm:bishop-regular-cauchy-cofinal-stability-namecert-obligations}
For every accepted $\BishopRegularCauchyCofinalStabilityUp$ carrier $S=(R,K,D,W,I,E,H,C,P,N)$, the local $\NameCert_{\BishopRegularCauchyCofinalStabilityUp}$ surface consists exactly of carrier admission for the source $\RegSeqRatUp$ row $R$; cofinal schedule exposure through $K$; dyadic tolerance exposure through $D$; finite stream-window exposure through $W$; interleaving comparison through $I$; Real-seal handoff through $E$; componentwise $\hsame$ transport through $H$; displayed $\Cont$ replay through $C$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:bishop-regular-cauchy-cofinal-stability-carrier}. Its displayed packet lists exactly the rows $R,K,D,W,I,E,H,C,P,N$, so any accepted cofinal read begins with the regular rational name, the cofinal schedule, the dyadic budget, and the finite stream window.

The interleaving row gives the comparison surface. It records how the original regular Cauchy schedule and the cofinal schedule are read through the same dyadic tolerance ledger before either route reaches the terminal $\RealUp$ seal.

The support rows preserve that inventory. Componentwise $\hsame$ transport, displayed $\Cont$ replay, package provenance, and local naming can only replay the displayed rows; since no quotient stream equality, selected subsequence, countable-choice schedule, host real equality, or ambient completeness coordinate occurs in the carrier, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Bishop regular Cauchy cofinal Real seal]
\label{thm:bishop-regular-cauchy-cofinal-stability-real-seal}
Every $\RealUp$-facing read of an accepted $\BishopRegularCauchyCofinalStabilityUp$ carrier $S=(R,K,D,W,I,E,H,C,P,N)$ factors through the finite route
$$
\RegSeqRatUp \longmapsto \BishopRegularCauchyCofinalStabilityUp \longmapsto \RegularCauchyInterleavingUp \longmapsto \RealUp .
$$
The read exposes the same source regular rational name, cofinal schedule, dyadic tolerance budget, finite StreamName observations, and interleaving route before the terminal Real seal is available. It exports no quotient stream equality, selected cofinal subsequence, countable choice, host real equality, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Read the rows $R,K,D,W,I,E$ from the carrier. The source regular rational name $R$ supplies the tail data, $K$ scopes the cofinal reindexing schedule, $D$ fixes the dyadic tolerance, and $W$ exposes only finite observations of that schedule.

Next use the interleaving row $I$. It places the original and cofinal observations in one finite comparison route before the seal row $E$ is exposed, so a Real-facing consumer cannot read a completed real independently of the displayed schedule.

Finally apply the structural rows $H,C,P,N$. Transport and replay preserve the same route, while provenance and local naming bind the packet as a $\BishopRegularCauchyCofinalStabilityUp$ certificate. Because the carrier lacks any quotient, choice-selected, host-equality, or completeness coordinate, the terminal $\RealUp$ seal cannot be reached by an escaping read.
\end{proof}

\closureat{BishopRegularCauchyCofinalStabilityUp}{seedStr}

\begin{closurestatus}{\BishopRegularCauchyCofinalStabilityUp}
  \constructivestory{A $\BishopRegularCauchyCofinalStabilityUp$ packet is generated from a regular rational sequence row, a cofinal schedule row, a dyadic tolerance ledger, finite StreamName observations, a RegularCauchyInterleaving comparison row, a terminal Real seal, componentwise hsame transport, displayed Cont replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-regular-cauchy-cofinal-stability-carrier}, \autoref{thm:bishop-regular-cauchy-cofinal-stability-namecert-obligations}, and \autoref{thm:bishop-regular-cauchy-cofinal-stability-real-seal} seed the quotient-free cofinal reindexing route for regular Cauchy rational names.}
  \formalstatus{\formalTargetV}
  \leantarget{BEDC.Derived.BishopRegularCauchyCofinalStabilityUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected cofinal subsequences, countable-choice schedules, host real equality, ambient Real completeness, theorem-level Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, cofinal schedule exposure, dyadic budget preservation, interleaving route stability, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BishopRegularCauchyCofinalStabilityUp.}
\end{closurestatus}
