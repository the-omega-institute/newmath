\chapter{A Concrete Naming Certificate for $\NestedIntervalRealUp$}
\label{ch:concrete-instances-nested-interval-real-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, the $\NestedIntervalRealUp$ packet records the classical nested located interval route to a real name as finite BEDC data. It sits after the located interval carrier of \autoref{ch:concrete-instances-located-real-interval-namecert}, the bisection route of \autoref{ch:concrete-instances-located-interval-bisection-namecert}, the dyadic refinement surface of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the nested dyadic Cauchy route of \autoref{ch:concrete-instances-nested-dyadic-cauchy-namecert}, the finite $\StreamNameUp$ windows of \autoref{ch:concrete-instances-streamname-namecert}, the $\RegSeqRatUp$ readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names only the displayed interval-nest route; it is not open-cover compactness, countable choice, quotient real equality, selected-limit data, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[NestedIntervalReal carrier]
\label{def:nested-interval-real-carrier}
A $\NestedIntervalRealUp$ carrier is a finite $\BHist$ packet
$$
J=(I,B,D,E,W,R,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the finite family of nested $\LocatedRealIntervalUp$ rows whose endpoints, radii, locators, and metric brackets are visible to the packet.
\item $B$ is the $\LocatedIntervalBisectionUp$ refinement ledger, recording how each displayed interval row narrows to the next row.
\item $D$ is the $\DyadicRatCoreUp$ diameter-decay ledger, assigning to each displayed stage a dyadic bound whose carrier proves that later intervals have diameter at most the requested tolerance.
\item $E$ records the endpoint $\RealUp$ seals for all displayed interval endpoints and states that endpoint reads are available only through the interval rows in $I$.
\item $W$ is the finite $\StreamNameUp$ window family opened for the midpoint, left-endpoint, right-endpoint, and diameter observations used by the nest.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from $W,D,E$, so every rational approximation is sourced by a displayed window and a dyadic diameter bound.
\item $S$ is the terminal $\RealUp$ seal reached by the nested interval route after $I,B,D,E,W,R$ have been replayed.
\item $H$ records componentwise $\hsame$ transport for the interval rows, bisection ledger, diameter ledger, endpoint seals, stream windows, regular readback, terminal seal, replay, provenance, and local naming data.
\item $C$ records displayed $\Cont$ replay for consumers that inspect the interval nest, the dyadic diameter decay, the regular readback, or the terminal seal.
\item $P$ is the $\Pkg$ provenance over $\NestedIntervalRealUp$, $\LocatedRealIntervalUp$, $\LocatedIntervalBisectionUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\NestedIntervalRealUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,B,D,E,W,R,S,H,C,P,N$. It has no coordinate for arbitrary open covers, a countable selector, a quotient construction of reals, selected limiting points, host real equality, or a proof that $\RealUp$ is complete.
\end{definition}

\begin{theorem}[NestedIntervalReal NameCert obligations]
\label{thm:nested-interval-real-namecert-obligations}
For every accepted $\NestedIntervalRealUp$ carrier
$$
J=(I,B,D,E,W,R,S,H,C,P,N),
$$
the local $\NameCert_{\NestedIntervalRealUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the located interval rows, bisection ledger, dyadic diameter-decay ledger, endpoint seals, stream windows, regular readback, terminal seal, transports, replay, provenance, and local naming;
\item nesting exactness, requiring every later interval read to factor through the previous interval row and the displayed $\LocatedIntervalBisectionUp$ refinement in $B$;
\item diameter decay, requiring every requested tolerance to be witnessed by a dyadic bound in $D$ and by endpoint seals in $E$ before the regular readback row is exposed;
\item readback ordering, requiring $\StreamNameUp$ windows in $W$ and $\RegSeqRatUp$ readback in $R$ to be replayed before the terminal $\RealUp$ seal $S$ is available;
\item classifier stability, requiring transported packets to compare only through the stored $\hsame$ and $\Cont$ rows;
\item ledger non-escape, requiring $P,N$ to source and name no open-cover compactness, countable choice, selected limit, host real equality, quotient real construction, or $\RealUp$ completeness theorem.
\end{enumerate}
No nested-interval real obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:nested-interval-real-carrier}. The accepted packet displays exactly one interval-nest family, one bisection ledger, one dyadic diameter ledger, one endpoint-seal row, one finite window family, one regular readback row, one terminal real seal, and the structural rows that transport, replay, package, and name them.

Nesting exactness is read from $I$ and $B$. The only way to pass from one interval stage to the next is the displayed bisection row, so a consumer cannot substitute an external interval selection rule for the stored refinement ledger.

Diameter decay is read from $D$ together with the endpoint seals $E$. For a requested tolerance, the packet first displays the dyadic bound and then opens only the endpoint reads attached to the corresponding interval rows.

The readback route is downstream of those rows. Stream windows in $W$ expose finite observations, the $\RegSeqRatUp$ row $R$ packages them as regular rational readback, and the $\RealUp$ seal $S$ is reached only after that readback is replayed.

Finally, $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier contains no open-cover, choice, selected-limit, host-equality, quotient-construction, or real-completeness coordinate, none of those objects can enter the local NameCert surface.
\end{proof}

\begin{theorem}[NestedIntervalReal Cauchy selection route]
\label{thm:nested-interval-real-cauchy-selection}
Every accepted $\NestedIntervalRealUp$ carrier $J=(I,B,D,E,W,R,S,H,C,P,N)$ exports a constructive Cauchy selection route:
$$
I,B,D,E \longmapsto W \longmapsto R \longmapsto S .
$$
The selected rational approximation at a displayed precision is not chosen from an ambient intersection. It is the row read from the stored interval stage whose dyadic diameter bound in $D$ is below that precision, whose endpoint seals are named in $E$, and whose finite observations pass through $W$ before the $\RegSeqRatUp$ readback $R$ and terminal $\RealUp$ seal $S$ are available.
\end{theorem}
\begin{proof}
Start with an accepted packet and a displayed precision request. The diameter ledger $D$ contains the finite dyadic row that bounds all later intervals below that request, and $B$ records how the needed later interval is reached from the earlier rows in $I$.

The candidate approximation is then read from the named interval stage. Its endpoint information is not a host real point; it is the endpoint $\RealUp$ seal row $E$ attached to the same located interval row.

The finite observations are opened through $W$. Once those windows have been replayed, the $\RegSeqRatUp$ row $R$ supplies the regular rational readback demanded by the Cauchy consumer.

The terminal seal $S$ is therefore a seal over the displayed Cauchy route. No ambient intersection point, choice-selected subsequence, quotient representative, or independent completeness theorem is used to obtain the approximation.
\end{proof}

\begin{theorem}[NestedIntervalReal ledger nonescape]
\label{thm:nested-interval-real-ledger-nonescape}
For an accepted $\NestedIntervalRealUp$ carrier $J=(I,B,D,E,W,R,S,H,C,P,N)$, every $\RealUp$-facing, $\RegSeqRatUp$-facing, or interval-facing read exported by $J$ is generated by the rows named in \autoref{def:nested-interval-real-carrier}. In particular, the packet exports no open-cover compactness principle, countable-choice selector, selected limit, host real equality, quotient construction of reals, or proof of $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier. An interval-facing read must pass through $I,B,D,E$, because these are the only rows that contain located interval data, bisection refinement, dyadic diameter decay, and endpoint seals.

A $\RegSeqRatUp$-facing read must pass through $W$ and $R$. These are the only finite window and regular readback rows available to the packet.

A $\RealUp$-facing read must then pass through $S$, and any comparison of two accepted packets must use $H$ and $C$ before $P$ and $N$ can record provenance and local naming.

Thus every exported read is generated by the displayed ledger. The excluded compactness, choice, selected-limit, host-equality, quotient-construction, and completeness objects are absent from the carrier and cannot be produced by projection, transport, replay, provenance, or local naming.
\end{proof}

\closureat{NestedIntervalRealUp}{seedStr}

\begin{closurestatus}{\NestedIntervalRealUp}
  \constructivestory{A $\NestedIntervalRealUp$ packet is generated from nested located interval rows, a located-bisection refinement ledger, dyadic diameter-decay rows, endpoint $\RealUp$ seals, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, a terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:nested-interval-real-carrier}, \autoref{thm:nested-interval-real-namecert-obligations}, \autoref{thm:nested-interval-real-cauchy-selection}, and \autoref{thm:nested-interval-real-ledger-nonescape} seed the finite nested located interval route from diameter-decaying intervals to regular rational readback and the terminal Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, countable choice, selected limits, host real equality, quotient construction, $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, nesting exactness, diameter decay, endpoint-seal ordering, Cauchy selection routing, classifier stability, and ledger non-escape for BEDC.Derived.NestedIntervalRealUp.}
\end{closurestatus}
