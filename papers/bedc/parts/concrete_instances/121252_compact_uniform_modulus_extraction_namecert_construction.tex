\chapter{A Concrete Naming Certificate for $\CompactUniformModulusExtractionUp$}
\label{ch:concrete-instances-compact-uniform-modulus-extraction-namecert}
\origin{human}

Following Bishop and Bridges on compact metric uniform continuity, the $\CompactUniformModulusExtractionUp$ packet records the finite extraction route from compact nets and pointwise continuity data to a tolerance-only uniform-modulus readback. It sits over the compact metric source of \autoref{ch:concrete-instances-compact-namecert}, the continuous-map local-radius surface of \autoref{ch:concrete-instances-continuous-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet is the concrete NameCert handoff for \autoref{ch:visions-compact-uniform-continuity}; it does not choose a global modulus from outside the displayed finite data.

\begin{definition}[Compact uniform modulus extraction carrier]
\label{def:compact-uniform-modulus-extraction-carrier}
A $\CompactUniformModulusExtractionUp$ carrier is a finite $\BHist$ packet
$$
U=(K,L,D,F,Q,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact-net row, carrying the finite $\ProbeBundle$ centers, coverage witnesses, and compact-metric source data used by the extraction.
\item $L$ is the $\ContinuousFunctionUp$ local-radius row, recording the pointwise radius data and graph certificate consumed at each displayed center.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, carrying the requested output tolerance, radius slack, and finite lower-bound comparisons.
\item $F$ is the finite fold row over the compact net, retaining the minimum positive radius ledger obtained from the displayed centers.
\item $Q$ is the $\RegSeqRatUp$ readback row for the folded precision and the local regular-rational comparisons.
\item $R$ is the terminal $\RealUp$ seal row, available only after $K,L,D,F,Q$ have all been displayed.
\item $H$ records componentwise $\hsame$ transport for the compact-net, local-radius, dyadic, fold, readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a tolerance request through the compact-net fold and into the uniform-modulus readback.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformModulusExtractionUp$, $\CompactUp$, $\ContinuousFunctionUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\CompactUniformModulusExtractionUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $K,L,D,F,Q,R,H,C,P,N$. It has no coordinate for open-cover choice, selected external moduli, quotient equality of real names, trichotomy, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact uniform modulus extraction obligations]
\label{thm:compact-uniform-modulus-extraction-obligations}
For every accepted $\CompactUniformModulusExtractionUp$ carrier $U=(K,L,D,F,Q,R,H,C,P,N)$, the local $\NameCert_{\CompactUniformModulusExtractionUp}$ surface consists exactly of compact-net admission through $K$, local-radius exposure through $L$, dyadic tolerance and slack readback through $D$, finite lower-bound folding through $F$, regular rational readback through $Q$, Real sealing through $R$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$. A uniform-modulus consumer therefore receives only the finite route
$$
K,L,D \longmapsto F \longmapsto Q \longmapsto R,H,C,P,N .
$$
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-modulus-extraction-carrier}. The compact-net row $K$ supplies the finite center family and coverage witnesses, while $L$ supplies the local continuous-map radius data attached to those centers.

Read the dyadic tolerance row $D$. It is the only tolerance budget available to the extraction, and the finite fold row $F$ records the lower-bound ledger obtained by comparing the displayed positive radii.

The regular rational readback $Q$ and terminal Real seal $R$ are downstream of that fold. Transport, replay, provenance, and local naming through $H,C,P,N$ preserve the same displayed packet.

Since no row stores open-cover choice, an external modulus selector, quotient equality, trichotomy, countable choice, or ambient Real completeness, the listed coordinates exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Compact uniform modulus Real-seal route]
\label{thm:compact-uniform-modulus-extraction-real-seal-route}
Every Real-facing read from an accepted $\CompactUniformModulusExtractionUp$ packet must expose the compact-net row $K$, continuous-map local-radius row $L$, $\DyadicRatCoreUp$ tolerance row $D$, finite lower-bound fold $F$, and $\RegSeqRatUp$ readback $Q$ before the terminal $\RealUp$ seal $R$ is consumed. The route exports a displayed uniform-modulus handoff and no open-cover choice principle, selected external modulus, quotient real equality, countable-choice schedule, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Apply the obligation theorem \autoref{thm:compact-uniform-modulus-extraction-obligations}. It places $K,L,D$ before the fold row $F$, and places $F$ before the regular rational readback $Q$.

The Real seal $R$ is therefore downstream of compact-net coverage, local continuity radii, dyadic tolerance comparison, and folded precision readback. A Real-facing consumer cannot inspect the terminal seal before those rows have been displayed in the same packet.

Finally replay $H,C,P,N$. These rows transport, replay, source, and name the finite route but do not add open-cover choice, external modulus selection, quotient equality, countable choice, or ambient completeness. The Real-facing handoff is exactly the displayed compact-net extraction route.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactUniformModulusExtractionUp$ consumer must display compact-net rows, ContinuousFunction local-radius rows, DyadicRatCore tolerance rows, finite lower-bound fold rows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row before a uniform-modulus readback is public.}

\independenceWitness{compact, continuous, dyadicratcore, regseqrat, real: $\CompactUniformModulusExtractionUp$ is not bijective to any listed sibling because it binds compact-net coverage, local continuous radii, dyadic tolerance comparison, finite lower-bound folding, regular rational readback, terminal Real sealing, transport, replay, provenance, and local naming as one extraction carrier.}

\closureat{CompactUniformModulusExtractionUp}{seedStr}

\begin{closurestatus}{\CompactUniformModulusExtractionUp}
  \constructivestory{A $\CompactUniformModulusExtractionUp$ packet is generated from compact-net coverage, ContinuousFunction local-radius rows, DyadicRatCore tolerance comparison, finite lower-bound folding, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-modulus-extraction-carrier}, \autoref{thm:compact-uniform-modulus-extraction-obligations}, and \autoref{thm:compact-uniform-modulus-extraction-real-seal-route} seed the finite extraction route from compact nets and local continuity radii to a uniform-modulus readback.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover choice, an external modulus selector, quotient equality of Real names, trichotomy, countable choice, ambient RealUp completeness, Lean theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-net coverage, local-radius exposure, dyadic tolerance exactness, finite fold stability, RegSeqRat readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformModulusExtractionUp.}
\end{closurestatus}
