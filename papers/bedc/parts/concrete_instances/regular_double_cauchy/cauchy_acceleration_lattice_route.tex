The $\RegularDoubleCauchyUp$ route reads $\CauchyAccelerationUp$ and $\CauchySequenceSpaceUp$ before a diagonal or rectangular Real-facing handoff is exported. The acceleration row of \autoref{ch:concrete-instances-cauchy-acceleration-namecert} supplies the finite speedup ledger, while the sequence-space surface of \autoref{ch:concrete-instances-cauchysequencespace-namecert} keeps the downstream Cauchy-name consumer tied to the same $\RegSeqRatUp$ readback of \autoref{ch:concrete-instances-regseqrat-namecert} and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}.
