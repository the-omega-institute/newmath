\chapter{A Concrete Naming Certificate for $\RegularDoubleCauchyUp$}
\label{ch:concrete-instances-regular-double-cauchy-namecert}
\origin{human}

Following Pringsheim's regular convergence of double sequences, Moore--Osgood interchange discipline, and Bishop--Bridges constructive Cauchy completion, the $\RegularDoubleCauchyUp$ packet records a two-parameter regular Cauchy certificate that does not collapse immediately to a diagonal route. It refines the diagonal handoff of \autoref{ch:concrete-instances-cauchy-double-sequence-namecert} by keeping row and column stability visible before the downstream completion consumer reads the terminal seal. The packet sits over the finite $\StreamNameUp$ windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}, and the completion-facing route of \autoref{ch:concrete-instances-real-cauchy-completion-namecert}. It provides Moore--Osgood and completion consumers with a finite rectangular regularity surface without asserting host double-sequence convergence, quotient arrays, selected limits, countable choice, or ambient $\RealUp$ completeness.

\begin{theorem}[RegularDoubleCauchy carrier]
\label{thm:regular-double-cauchy-carrier}
A $\RegularDoubleCauchyUp$ carrier is a finite $\BHist$ packet
$$
A=(S,R,D,T,W,H,C,P,N)
$$
with the following visible rows: $S$ is the $\StreamNameUp$ family schedule for finite rectangular windows; $R$ is the $\RegSeqRatUp$ row-column readback attached to every admitted row and column face; $D$ is the $\DyadicRatCoreUp$ rectangular tolerance ledger; $T$ is the diagonal cofinal schedule read only after the rectangular ledger has been displayed; $W$ is the $\RealCauchyCompletionUp/\RealUp$ seal handoff; $H$ records componentwise $\hsame$ transport; $C$ records displayed $\Cont$ replay; $P$ is the $\Pkg$ provenance over $\RegularDoubleCauchyUp$, $\CauchyDoubleSequenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealCauchyCompletionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$; and $N$ is the local $\NameCert_{\RegularDoubleCauchyUp}$ row.

The carrier is rectangular before it is diagonal. It contains no coordinate for a host double limit, quotient equality of arrays, a selected limiting value, countable-choice scheduling, or ambient real completeness.
\end{theorem}
\begin{proof}
Read the packet as a displayed $\BHist$ row. The entries $S,R,D$ expose the finite rectangular data before the diagonal schedule $T$ is available, so the consumer cannot begin with a collapsed single sequence.

The completion-facing row $W$ is downstream from the same visible schedule, readback, and tolerance ledger. Transport and replay are exhausted by $H$ and $C$, while provenance and naming are exhausted by $P$ and $N$.

Since the listed tuple has no further coordinate, the excluded host convergence, quotient-array, selected-limit, choice, and ambient-completeness data cannot be read from the carrier.
\end{proof}

\begin{theorem}[RegularDoubleCauchy row-column stability]
\label{thm:regular-double-cauchy-row-column-stability}
For every accepted $\RegularDoubleCauchyUp$ carrier $A=(S,R,D,T,W,H,C,P,N)$, any two finite reads that agree on the displayed $\StreamNameUp$ rectangular window schedule and on the $\DyadicRatCoreUp$ tolerance ledger have the same row and column $\RegSeqRatUp$ readback under the componentwise $\hsame$ transport recorded in $H$. The stability is local to the finite rows $S,R,D,H,C,P,N$ and does not identify quotient arrays or assert a host double-sequence limit.
\end{theorem}
\begin{proof}
Project the schedule and ledger rows. The rectangular window family is supplied by $S$, and the tolerance comparison used for every row and column face is supplied by $D$.

Apply the readback row $R$ to the same displayed windows and ledger entries. Componentwise $\hsame$ transport in $H$ preserves the row face and the column face before any completion handoff is inspected.

The replay row $C$ repeats this route for a consumer request, and $P,N$ keep the request inside the local packet. No quotient-array or host-limit coordinate is present, so the stability conclusion is exactly the visible row-column readback.
\end{proof}

\begin{theorem}[RegularDoubleCauchy diagonal readback]
\label{thm:regular-double-cauchy-diagonal-readback}
Every diagonal read of an accepted $\RegularDoubleCauchyUp$ carrier factors through the rectangular route
$$
S,D \longmapsto R \longmapsto T \longmapsto W .
$$
Thus a $\CauchyDoubleSequenceUp$, $\MooreOsgoodUp$, $\RealCauchyCompletionUp$, or $\RealUp$ consumer sees the diagonal cofinal schedule only after the StreamName windows, RegSeqRat row-column readback, and DyadicRatCore rectangular tolerance ledger have been displayed.
\end{theorem}
\begin{proof}
Start with the rectangular rows. A diagonal request must first open the finite windows in $S$ and account for the requested precision in $D$.

The row-column readback $R$ is then the only route from those rectangular windows to a regular rational name. The diagonal schedule $T$ is read after this regularity data, not before it.

Finally the completion-facing handoff $W$ consumes the scheduled readback. Therefore every cited downstream consumer receives the diagonal route as a readback of the rectangular certificate, not as an independent host convergence assertion.
\end{proof}

\begin{theorem}[RegularDoubleCauchy ledger exactness]
\label{thm:regular-double-cauchy-ledger-exactness}
In an accepted $\RegularDoubleCauchyUp$ carrier, the dyadic tolerance ledger $D$ is the unique finite row from which a completion-facing consumer may justify rectangular Cauchy bounds before reading the diagonal schedule $T$ or the seal handoff $W$. Every precision request must pass through $S,D,R$ with the same $H,C,P,N$ provenance, and no selected limit, countable-choice schedule, quotient array, or ambient completeness row can replace that ledger.
\end{theorem}
\begin{proof}
Project the accepted carrier. The only tolerance row is $D$, and it is attached to the same finite window schedule $S$ that supplies the rectangular observations.

Readback through $R$ uses that ledger before any diagonal schedule or completion-facing row appears. The continuation row $C$ records the same order for each accepted request, while $H$ transports the ledger componentwise.

The remaining rows $P,N$ only source and name the route. Since there is no second tolerance, selected-limit, quotient, choice, or completeness coordinate, ledger exactness is forced by the tuple inventory.
\end{proof}

\begin{theorem}[RegularDoubleCauchy NameCert surface]
\label{thm:regular-double-cauchy-namecert-surface}
The local $\NameCert_{\RegularDoubleCauchyUp}$ surface consists exactly of carrier admission, rectangular window scheduling, row-column $\RegSeqRatUp$ stability, $\DyadicRatCoreUp$ ledger exactness, diagonal cofinal readback, $\RealCauchyCompletionUp/\RealUp$ seal handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local naming row. It is the non-diagonal regularity certificate consumed by CauchyDoubleSequence and Moore--Osgood routes before their diagonal or iterated-limit handoffs, and it exports no host double-sequence convergence theorem, quotient-array classifier, selected limit, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Combine the carrier theorem with row-column stability. These statements expose the rectangular windows, regular readback faces, tolerance ledger, transport, replay, provenance, and local name.

The diagonal readback theorem supplies the only passage from the rectangular surface to a completion-facing diagonal route. Ledger exactness supplies the only precision evidence available on that passage.

Thus the NameCert surface is the finite union of the listed rows and obligations. Downstream CauchyDoubleSequence and Moore--Osgood consumers may use this surface as a regularity certificate, but no excluded host or choice principle has a row from which it could be exported.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularDoubleCauchyUp$ read factors through rectangular StreamName windows, RegSeqRat row-column readback, a DyadicRatCore tolerance ledger, a diagonal cofinal schedule, RealCauchyCompletion and Real seal handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{cauchydoublesequence, mooreosgood, streamname, regseqrat, dyadicratcore, realcauchycompletion, real: $\RegularDoubleCauchyUp$ is not bijective to any listed sibling because it keeps rectangular row-column regularity visible before the diagonal or iterated-limit consumer is allowed to read a completion-facing seal.}

\closureat{RegularDoubleCauchyUp}{seedStr}

\begin{closurestatus}{\RegularDoubleCauchyUp}
  \constructivestory{A $\RegularDoubleCauchyUp$ packet is generated from finite rectangular StreamName windows, RegSeqRat row-column readback, a DyadicRatCore tolerance ledger, a diagonal cofinal schedule, a RealCauchyCompletion and Real seal handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{thm:regular-double-cauchy-carrier}, \autoref{thm:regular-double-cauchy-row-column-stability}, \autoref{thm:regular-double-cauchy-diagonal-readback}, \autoref{thm:regular-double-cauchy-ledger-exactness}, and \autoref{thm:regular-double-cauchy-namecert-surface} seed the rectangular two-parameter regularity surface for downstream diagonal and completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host double-sequence convergence, quotient arrays, selected limits, countable choice, ambient $\RealUp$ completeness, bridge checking, or Lean verification.}
  \upgradepath{Obligation closure requires checked carrier admission, row-column stability, diagonal readback, ledger exactness, and local NameCert surface proofs for BEDC.Derived.RegularDoubleCauchyUp.}
\end{closurestatus}
