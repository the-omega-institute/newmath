The $\RegularDoubleCauchyUp$ route reads paired Cauchy windows through the finite stream surface of \autoref{ch:concrete-instances-streamname-namecert} and the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert} before any Real-facing handoff. This sibling dependency keeps the double-window packet tied to displayed $\StreamNameUp$ and $\RegSeqRatUp$ rows, with the downstream $\RealUp$ seal supplied only through \autoref{ch:concrete-instances-real-namecert}.
