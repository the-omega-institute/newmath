\chapter{A Concrete Naming Certificate for $\DirichletUniformTailSelectorUp$}
\label{ch:concrete-instances-dirichlet-uniform-tail-selector-namecert}
\origin{human}

Following Dirichlet and the Hardy/Bishop--Bridges treatment of series tests, the $\DirichletUniformTailSelectorUp$ packet records the finite selector route by which a displayed Dirichlet tail estimate is handed to a uniform Cauchy series consumer. It sits after the real-series source of \autoref{ch:concrete-instances-realseries-namecert}, the bounded-partial-sum Dirichlet-test face of \autoref{ch:concrete-instances-dirichlet-test-namecert}, the shared-tail handoff of \autoref{ch:concrete-instances-uniform-cauchy-series-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite tail-budget selector for Real-and-Completion consumers; it does not assert host infinite sums, quotient series equality, selected limits, countable choice, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Dirichlet uniform tail selector carrier]
\label{def:dirichlet-uniform-tail-selector-carrier}
A $\DirichletUniformTailSelectorUp$ carrier is a finite $\BHist$ packet
$$
U=(S,D,T,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\RealSeriesUp$ source row, exposing only the displayed term stream and finite partial-sum windows consumed by the selector.
\item $D$ is the $\DirichletTestUp$ face row, exposing bounded partial sums, coefficient windows, null-tail data, and the finite Dirichlet tail-estimate schedule.
\item $T$ is the selector row. For each requested precision and displayed source window it names the finite tail threshold admitted by the Dirichlet-test face.
\item $W$ is the $\UniformCauchySeriesUp$ handoff row, recording that the selector threshold is read as the shared uniform tail threshold for the displayed partial-sum comparisons.
\item $R$ is the $\RegSeqRatUp$ readback row for the selected tail window and the admitted partial-sum comparisons.
\item $E$ is the terminal $\RealUp$ seal row reached only after $S,D,T,W,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the source, Dirichlet face, selector, uniform-tail handoff, regular readback, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the finite route
$$
S,D \longmapsto T \longmapsto W \longmapsto R \longmapsto E,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\DirichletUniformTailSelectorUp$, $\RealSeriesUp$, $\DirichletTestUp$, $\UniformCauchySeriesUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DirichletUniformTailSelectorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,D,T,W,R,E,H,C,P,N$. It has no coordinate for a host infinite sum, quotient equality of partial-sum streams, a selected limiting real, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Dirichlet uniform tail selector NameCert obligations]
\label{thm:dirichlet-uniform-tail-selector-namecert-obligations}
For every accepted $\DirichletUniformTailSelectorUp$ carrier $U=(S,D,T,W,R,E,H,C,P,N)$, the local $\NameCert_{\DirichletUniformTailSelectorUp}$ surface consists exactly of real-series source admission through $S$, Dirichlet-test face exposure through $D$, finite tail-threshold selection through $T$, uniform Cauchy handoff through $W$, regular rational readback through $R$, terminal real sealing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.

No selector obligation is stored outside those rows.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:dirichlet-uniform-tail-selector-carrier}. The source side is exhausted by $S$ and $D$: $S$ exposes the real-series term and partial-sum windows, while $D$ exposes the bounded partial sums, coefficient windows, null-tail ledger, and finite Dirichlet estimate schedule.

Read the selector row next. The row $T$ names the finite threshold used for the requested precision, and $W$ records the same threshold as the shared uniform Cauchy tail row for the displayed partial-sum comparisons.

The endpoint route is downstream. The row $R$ reads the selected tail window through $\RegSeqRatUp$, and $E$ seals only that readback through $\RealUp$.

Finally the rows $H,C,P,N$ transport, replay, source, and name the same finite packet. Since no other coordinate is present, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Dirichlet uniform tail handoff]
\label{thm:dirichlet-uniform-tail-selector-handoff}
Every $\UniformCauchySeriesUp$-facing read of an accepted $\DirichletUniformTailSelectorUp$ carrier factors through the finite route
$$
S,D \longmapsto T \longmapsto W \longmapsto R \longmapsto E,H,C,P,N .
$$
Thus the uniform tail budget exported to Real-and-Completion consumers is a displayed Dirichlet selector threshold read through regular rational data and a terminal real seal, not a host infinite sum, quotient series equality, selected limit, countable-choice schedule, trichotomy, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with a uniform-tail request. The carrier first exposes $S$, so the request sees only the displayed $\RealSeriesUp$ term stream and partial-sum windows.

The request then reads the Dirichlet face $D$. This row supplies the bounded partial sums, coefficient windows, null-tail data, and estimate schedule needed to justify a finite tail threshold.

The selector threshold is the row $T$. The handoff row $W$ is downstream of $T$, so a $\UniformCauchySeriesUp$ consumer receives the threshold only after the Dirichlet-test data have been displayed.

Finally $R$ and $E$ route the selected tail window through $\RegSeqRatUp$ readback and the terminal $\RealUp$ seal. The structural rows $H,C,P,N$ preserve this same path, so no excluded host-side summability, quotient, limit, choice, order, or completeness coordinate can be exported.
\end{proof}

\falsifiablePrediction{A Real-and-Completion consumer that reads $\DirichletUniformTailSelectorUp$ without first exposing the $\RealSeriesUp$ source row $S$, the $\DirichletTestUp$ face row $D$, the selector row $T$, the $\UniformCauchySeriesUp$ handoff row $W$, the $\RegSeqRatUp$ readback row $R$, or the $\RealUp$ seal row $E$ refutes the carrier of \autoref{def:dirichlet-uniform-tail-selector-carrier}.}

\independenceWitness{realseries, dirichlet-test, uniform-cauchy-series, regseqrat, real: no listed sibling supplies the whole selector packet $U=(S,D,T,W,R,E,H,C,P,N)$, because each sibling contributes only a source, a test face, a uniform-tail handoff, a regular readback, or a terminal seal while this chapter binds them as one finite selector certificate.}

\closureat{DirichletUniformTailSelectorUp}{seedStr}

\begin{closurestatus}{\DirichletUniformTailSelectorUp}
  \constructivestory{A $\DirichletUniformTailSelectorUp$ packet is generated from RealSeries source rows, DirichletTest face rows, a finite selector threshold, a UniformCauchySeries handoff, RegSeqRat readback, a RealUp seal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dirichlet-uniform-tail-selector-carrier}, \autoref{thm:dirichlet-uniform-tail-selector-namecert-obligations}, and \autoref{thm:dirichlet-uniform-tail-selector-handoff} seed the finite Dirichlet uniform tail-selector route over $\RealSeriesUp$, $\DirichletTestUp$, $\UniformCauchySeriesUp$, $\RegSeqRatUp$, and $\RealUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host infinite sums, quotient series equality, selected limits, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked source admission, Dirichlet face exposure, finite selector thresholding, uniform Cauchy handoff, regular readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.DirichletUniformTailSelectorUp.}
\end{closurestatus}
