\chapter{A Concrete Naming Certificate for $\FinSetUp$}
\label{ch:concrete-instances-finset-namecert}

The finite-set interface $\FinSetUp$ packages a $\NameCert_{\SetUp}$ together with a finite enumeration witness via $\NameCert_{\ListUp}$ (\autoref{ch:concrete-instances-list-namecert}). It depends on $\NameCert_{\SetUp}$ (\autoref{ch:concrete-instances-set-namecert}) and is the substrate for combinatorial certificates such as $\NameCert_{\PermutationUp}$ and $\NameCert_{\GraphUp}$.

\section{Carrier definition}
\label{sec:finset-carrier}

\section{The certificate}
\label{sec:finset-certificate}

\begin{definition}[Finite-spine enumeration membership]
\label{def:finset-enumeration-membership}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be the carrier of a fixed $\SetUp$ certificate with classifier $\sim_A$. For a finite BEDC-history spine $xs:\mathsf{HistSpine}$, let $\mathsf{Pos}(xs)$ be its finite position carrier and let $\mathsf{entry}_{xs}:\mathsf{Pos}(xs)\to\mathsf{BHist}$ be its entry readback. Define enumeration membership by
$$
\mathsf{EnumMem}_{A,\sim_A}(z,xs)
\Longleftrightarrow
A(z)\land
\exists i:\mathsf{Pos}(xs).\;A(\mathsf{entry}_{xs}(i))\land z\sim_A\mathsf{entry}_{xs}(i).
$$
The enumeration-induced $\FinSetUp$ classifier between two finite-spine presentations is
$$
\mathsf{FinSetEnumClassifier}_{A,\sim_A}(xs,ys)
\Longleftrightarrow
\forall z:\mathsf{BHist}.\;
\mathsf{EnumMem}_{A,\sim_A}(z,xs)
\Longleftrightarrow
\mathsf{EnumMem}_{A,\sim_A}(z,ys).
$$
This is the extensional classifier read from the enumeration witness used by the finite-set presentation in \autoref{ch:concrete-instances-finset-namecert}.
\end{definition}

\begin{definition}[Classifier-preserving enumeration permutation]
\label{def:finset-classifier-preserving-enumeration-permutation}
For finite BEDC-history spines $xs$ and $ys$, define $\mathsf{EnumPerm}_{A,\sim_A}(xs,ys)$ to mean that there is a certified finite-position bijection
$$
\pi:\mathsf{Pos}(xs)\cong\mathsf{Pos}(ys)
$$
such that every matched pair of entries is carried by the fixed $\SetUp$ source and classified by its classifier:
$$
\forall i:\mathsf{Pos}(xs).\;
A(\mathsf{entry}_{xs}(i))\land
A(\mathsf{entry}_{ys}(\pi(i)))\land
\mathsf{entry}_{xs}(i)\sim_A\mathsf{entry}_{ys}(\pi(i)).
$$
The inverse position map $\pi^{-1}$ is part of the certified bijection. This is the enumeration-level presentation datum used by the $\PermutationUp$ interface in \autoref{ch:concrete-instances-permutation-namecert}.
\end{definition}

\begin{theorem}[FinSet classifier invariant under enumeration permutation]
\label{thm:finset-classifier-enumeration-permutation-invariant}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be the carrier of a fixed $\SetUp$ certificate, let $\sim_A$ be its classifier, and let $\mathsf{cert}:\NameCert(A,\sim_A)$. For finite BEDC-history spines $xs$ and $ys$, if
$$
\mathsf{EnumPerm}_{A,\sim_A}(xs,ys)
$$
holds, then
$$
\mathsf{FinSetEnumClassifier}_{A,\sim_A}(xs,ys)
$$
holds. Equivalently, replacing a $\FinSetUp$ enumeration witness by a certified permutation that preserves the fixed source classifier pointwise leaves the induced finite-set classifier unchanged.
\end{theorem}
\begin{proof}
Unfold \autoref{def:finset-classifier-preserving-enumeration-permutation} and choose the certified bijection $\pi$ with its pointwise classifier-preservation field. Unfold \autoref{def:finset-enumeration-membership}. It is enough to fix a query history $z$ and prove the two membership implications.

For the forward implication, suppose $\mathsf{EnumMem}_{A,\sim_A}(z,xs)$. Then $A(z)$ holds and there is a position $i:\mathsf{Pos}(xs)$ such that $A(\mathsf{entry}_{xs}(i))$ and
$$
z\sim_A\mathsf{entry}_{xs}(i)
$$
hold. The permutation field at $i$ gives $A(\mathsf{entry}_{ys}(\pi(i)))$ and
$$
\mathsf{entry}_{xs}(i)\sim_A\mathsf{entry}_{ys}(\pi(i)).
$$
The transitivity field of the naming certificate from \autoref{def:lean-carrier-naming-certificate}, applied to these two classifier witnesses, gives
$$
z\sim_A\mathsf{entry}_{ys}(\pi(i)).
$$
Using the position $\pi(i)$ and the carried-entry witness just obtained, this repacks as $\mathsf{EnumMem}_{A,\sim_A}(z,ys)$.

For the reverse implication, suppose $\mathsf{EnumMem}_{A,\sim_A}(z,ys)$. Then $A(z)$ holds and there is a position $j:\mathsf{Pos}(ys)$ such that $A(\mathsf{entry}_{ys}(j))$ and
$$
z\sim_A\mathsf{entry}_{ys}(j)
$$
hold. Put $i=\pi^{-1}(j)$. The pointwise preservation field at $i$ gives
$$
\mathsf{entry}_{xs}(i)\sim_A\mathsf{entry}_{ys}(j),
$$
because $\pi(i)=j$, and it also gives $A(\mathsf{entry}_{xs}(i))$. The symmetry field of $\mathsf{cert}$ reverses the matched-entry comparison to
$$
\mathsf{entry}_{ys}(j)\sim_A\mathsf{entry}_{xs}(i).
$$
Transitivity of the same classifier composes this with $z\sim_A\mathsf{entry}_{ys}(j)$ and yields
$$
z\sim_A\mathsf{entry}_{xs}(i).
$$
The position $i$ therefore witnesses $\mathsf{EnumMem}_{A,\sim_A}(z,xs)$.

The two implications hold for every query history $z$, so the two enumeration-induced predicates are pointwise equivalent. By \autoref{def:finset-enumeration-membership}, this is exactly $\mathsf{FinSetEnumClassifier}_{A,\sim_A}(xs,ys)$.
\end{proof}
\leanchecked{BEDC.Derived.FinsetUp.FinsetEnumerationClassifier\_permutation\_invariant}

\begin{definition}[Finite-spine occurrence]
\label{def:finite-spine-occurrence}
For a finite spine $xs:\mathsf{HistSpine}$ and a history $x:\mathsf{BHist}$, the predicate $\mathsf{SpineMember}(x,xs)$ is generated by two clauses: $\mathsf{SpineMember}(x,\mathsf{cons}(x,ys))$ for every tail $ys$, and if $\mathsf{SpineMember}(x,ys)$ then $\mathsf{SpineMember}(x,\mathsf{cons}(y,ys))$ for every head $y$. There is no clause for $\mathsf{nil}$.
\end{definition}

\begin{definition}[FinSet enumeration carrier]
\label{def:finset-enumeration-carrier}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be the carrier of the underlying $\NameCert_{\SetUp}$, let $\sim_A$ be its classifier, and let $xs:\mathsf{HistSpine}$ be the finite spine supplied by the $\ListUp$ enumeration witness of \autoref{def:list-spine-representation}. Define the finite-set carrier represented by $xs$ by
$$
\mathsf{FinSetEnumCarrier}_{A,\sim_A}(xs)(a)
\Longleftrightarrow
A(a)\land
\exists x:\mathsf{BHist}.\;\mathsf{SpineMember}(x,xs)\land a\sim_A x.
$$
\end{definition}
\leandef{BEDC.Derived.FinsetUp.FinsetEnumerationCarrier}

\begin{lemma}[Enumeration spine entries are source-carried]
\label{lem:finset-enumeration-spine-entries-source-carried}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier. If $\mathsf{ListSpineRep}_A(e,xs)$ and $\mathsf{SpineMember}(x,xs)$ hold, then $A(x)$ holds.
\end{lemma}
\begin{proof}
Induct on the displayed $\mathsf{ListSpineRep}_A(e,xs)$ witness from \autoref{def:list-spine-representation}. The nil representation has $xs=\mathsf{nil}$, and \autoref{def:finite-spine-occurrence} gives no occurrence clause for $\mathsf{nil}$. In the cons representation, the occurrence of $x$ in $\mathsf{cons}(a,xs')$ is either the head occurrence or a tail occurrence. The head case returns the stored source witness $A(a)$. The tail case applies the induction hypothesis to the tail representation $\mathsf{ListSpineRep}_A(t,xs')$ and the displayed tail occurrence.
\end{proof}
\leanchecked{BEDC.Derived.FinsetUp.FinsetEnumerationBundle\_member\_source\_carried}

\begin{theorem}[FinSet enumeration membership exactness]
\label{thm:finset-enumeration-membership-exactness}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be the carrier of the underlying $\NameCert_{\SetUp}$, let $\sim_A$ be its classifier, let $\mathsf{cert}:\NameCert(A,\sim_A)$, and let $e$ and $xs$ satisfy $\mathsf{ListSpineRep}_A(e,xs)$. Then for every history $a$,
$$
\mathsf{FinSetEnumCarrier}_{A,\sim_A}(xs)(a)
\Longleftrightarrow
\exists x:\mathsf{BHist}.\;\mathsf{SpineMember}(x,xs)\land a\sim_A x.
$$
\end{theorem}
\begin{proof}
For the forward direction, unpack \autoref{def:finset-enumeration-carrier} and keep the displayed occurrence and classifier witness.

For the reverse direction, take a witness $x$ with $\mathsf{SpineMember}(x,xs)$ and $a\sim_A x$. The enumeration witness and \autoref{lem:finset-enumeration-spine-entries-source-carried} give $A(x)$. The symmetry field of $\mathsf{cert}$, as part of \autoref{def:lean-carrier-naming-certificate}, turns $a\sim_A x$ into $x\sim_A a$. Carrier transport from the same certificate carries $A(x)$ along this comparison to obtain $A(a)$. Pair this carrier witness with the original occurrence and classifier witness, and repack by \autoref{def:finset-enumeration-carrier}.
\end{proof}
\leanchecked{BEDC.Derived.FinsetUp.FinsetEnumerationCarrier\_membership\_exactness}

\begin{lemma}[Finite-spine occurrence has a position readback]
\label{lem:finset-spine-member-position-readback}
For every finite BEDC-history spine $xs:\mathsf{HistSpine}$ and history $y:\mathsf{BHist}$, if $\mathsf{SpineMember}(y,xs)$ holds, then
$$
\exists j:\mathsf{Pos}(xs).\;\mathsf{entry}_{xs}(j)=y.
$$
\end{lemma}
\begin{proof}
Induct on the generated occurrence witness from \autoref{def:finite-spine-occurrence}. In the head clause $\mathsf{SpineMember}(y,\mathsf{cons}(y,ys))$, choose the head position of $\mathsf{cons}(y,ys)$; its readback is $y$. In the tail clause, the induction hypothesis gives a position $j:\mathsf{Pos}(ys)$ with $\mathsf{entry}_{ys}(j)=y$. Use the tail-position inclusion $\mathsf{Pos}(ys)\to\mathsf{Pos}(\mathsf{cons}(x,ys))$; its readback is the same tail entry, so it witnesses the displayed equality for $\mathsf{cons}(x,ys)$.
\end{proof}

\begin{theorem}[FinSet duplicate insertion invariance]
\label{thm:finset-duplicate-insertion-invariant}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be the carrier of a fixed $\SetUp$ certificate, let $\sim_A$ be its classifier, and let $\mathsf{cert}:\NameCert(A,\sim_A)$. For a finite BEDC-history spine $xs:\mathsf{HistSpine}$ and histories $x,y:\mathsf{BHist}$, if
$$
A(x)\land A(y)\land \mathsf{SpineMember}(y,xs)\land x\sim_A y,
$$
then
$$
\mathsf{FinSetEnumClassifier}_{A,\sim_A}(\mathsf{cons}(x,xs),xs)
$$
holds. Thus a source-carried head entry whose source classifier class is already represented in the finite spine is invisible to the induced $\FinSetUp$ classifier.
\end{theorem}
\begin{proof}
Unpack the displayed assumptions as $A(x)$, $A(y)$, an occurrence $\mathsf{SpineMember}(y,xs)$, and a classifier witness $x\sim_A y$. By \autoref{lem:finset-spine-member-position-readback}, choose a position $j:\mathsf{Pos}(xs)$ with $\mathsf{entry}_{xs}(j)=y$.

Unfold \autoref{def:finset-enumeration-membership}. It is enough to fix a query history $z$ and prove the two membership implications between $\mathsf{cons}(x,xs)$ and $xs$.

For the forward implication, suppose $\mathsf{EnumMem}_{A,\sim_A}(z,\mathsf{cons}(x,xs))$. Then $A(z)$ holds and some position of the cons spine carries an entry classified with $z$. Case-analyze that position. If it is the head position, the readback is $x$, so the witness supplies $z\sim_A x$. The transitivity field of $\mathsf{cert}$ from \autoref{def:lean-carrier-naming-certificate} composes $z\sim_A x$ with $x\sim_A y$ and gives $z\sim_A y$. Rewriting $y$ as $\mathsf{entry}_{xs}(j)$, the position $j$ together with $A(y)$ repacks as $\mathsf{EnumMem}_{A,\sim_A}(z,xs)$. If the cons-position is inherited from the tail, its readback is already a position of $xs$ with the same carried-entry and classifier witnesses, so it repacks directly as $\mathsf{EnumMem}_{A,\sim_A}(z,xs)$.

For the reverse implication, suppose $\mathsf{EnumMem}_{A,\sim_A}(z,xs)$. Keep its source witness $A(z)$ and its position $i:\mathsf{Pos}(xs)$ with $A(\mathsf{entry}_{xs}(i))$ and $z\sim_A\mathsf{entry}_{xs}(i)$. View $i$ as the corresponding tail position of $\mathsf{cons}(x,xs)$. The cons readback at that tail position is $\mathsf{entry}_{xs}(i)$, so the same carried-entry and classifier witnesses repack as $\mathsf{EnumMem}_{A,\sim_A}(z,\mathsf{cons}(x,xs))$.

The two implications hold for every query history $z$. By the classifier clause in \autoref{def:finset-enumeration-membership}, this is exactly $\mathsf{FinSetEnumClassifier}_{A,\sim_A}(\mathsf{cons}(x,xs),xs)$.
\end{proof}
\leanchecked{BEDC.Derived.FinsetUp.FinsetEnumerationClassifier\_duplicate\_insert}

\begin{lemma}[Certified finite-position bijections compose]
\label{lem:finset-enum-position-bijection-composition}
For finite BEDC-history spines $xs$, $ys$, and $zs$, certified finite-position bijections
$$
\pi:\mathsf{Pos}(xs)\cong\mathsf{Pos}(ys)
\qquad\text{and}\qquad
\rho:\mathsf{Pos}(ys)\cong\mathsf{Pos}(zs)
$$
compose to a certified finite-position bijection
$$
\rho\circ\pi:\mathsf{Pos}(xs)\cong\mathsf{Pos}(zs),
$$
whose inverse position map is $\pi^{-1}\circ\rho^{-1}$.
\end{lemma}
\begin{proof}
The certified-bijection data in \autoref{def:finset-classifier-preserving-enumeration-permutation} consist of a forward position map, an inverse position map, and the two inverse identities. For the composite, take the forward position map $\rho\circ\pi$ and the inverse position map $\pi^{-1}\circ\rho^{-1}$. The left and right inverse identities follow by applying first the inverse identities of $\rho$ and then the inverse identities of $\pi$, in the corresponding order. Thus the composite map carries the same certified-bijection data required in \autoref{def:finset-classifier-preserving-enumeration-permutation}.
\end{proof}

\begin{theorem}[EnumPerm transitivity by bijection composition]
\label{thm:enumperm-transitivity-by-bijection-composition}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be the carrier of a fixed $\SetUp$ certificate, let $\sim_A$ be its classifier, and let $\mathsf{cert}:\NameCert(A,\sim_A)$. For finite BEDC-history spines $xs$, $ys$, and $zs$, if
$$
\mathsf{EnumPerm}_{A,\sim_A}(xs,ys)
\qquad\text{and}\qquad
\mathsf{EnumPerm}_{A,\sim_A}(ys,zs)
$$
hold, then
$$
\mathsf{EnumPerm}_{A,\sim_A}(xs,zs)
$$
holds.
\end{theorem}
\begin{proof}
Unfold \autoref{def:finset-classifier-preserving-enumeration-permutation}. Choose from the first permutation a certified bijection $\pi:\mathsf{Pos}(xs)\cong\mathsf{Pos}(ys)$ and a pointwise preservation field
$$
P_\pi(i):
A(\mathsf{entry}_{xs}(i))\land
A(\mathsf{entry}_{ys}(\pi(i)))\land
\mathsf{entry}_{xs}(i)\sim_A\mathsf{entry}_{ys}(\pi(i)).
$$
Choose from the second permutation a certified bijection $\rho:\mathsf{Pos}(ys)\cong\mathsf{Pos}(zs)$ and a pointwise preservation field
$$
P_\rho(j):
A(\mathsf{entry}_{ys}(j))\land
A(\mathsf{entry}_{zs}(\rho(j)))\land
\mathsf{entry}_{ys}(j)\sim_A\mathsf{entry}_{zs}(\rho(j)).
$$
By \autoref{lem:finset-enum-position-bijection-composition}, $\rho\circ\pi$ is a certified bijection from $\mathsf{Pos}(xs)$ to $\mathsf{Pos}(zs)$.

It remains to verify the pointwise classifier-preservation field for the composite. Fix $i:\mathsf{Pos}(xs)$. The field $P_\pi(i)$ gives
$$
A(\mathsf{entry}_{xs}(i))
\qquad\text{and}\qquad
\mathsf{entry}_{xs}(i)\sim_A\mathsf{entry}_{ys}(\pi(i)).
$$
The field $P_\rho(\pi(i))$ gives
$$
A(\mathsf{entry}_{zs}(\rho(\pi(i))))
\qquad\text{and}\qquad
\mathsf{entry}_{ys}(\pi(i))\sim_A\mathsf{entry}_{zs}(\rho(\pi(i))).
$$
The transitivity field of $\mathsf{cert}$, equivalently \autoref{thm:namecert-equivalence-transitivity}, composes the two classifier witnesses and yields
$$
\mathsf{entry}_{xs}(i)\sim_A\mathsf{entry}_{zs}(\rho(\pi(i))).
$$
Together with the displayed carrier witnesses, this is exactly the pointwise preservation clause for the composite certified bijection. Repacking the composite bijection and this field gives $\mathsf{EnumPerm}_{A,\sim_A}(xs,zs)$.
\end{proof}
\leanchecked{BEDC.Derived.FinsetUp.FinsetEnumerationPermutation\_trans}

\begin{lemma}[Pointwise enumeration membership equivalences compose]
\label{lem:finset-enummem-pointwise-equivalence-composition}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier, let $\sim_A$ be a source classifier, and let $xs$, $ys$, and $zs$ be finite BEDC-history spines. If, for every query history $q$,
$$
\mathsf{EnumMem}_{A,\sim_A}(q,xs)
\Longleftrightarrow
\mathsf{EnumMem}_{A,\sim_A}(q,ys)
$$
and
$$
\mathsf{EnumMem}_{A,\sim_A}(q,ys)
\Longleftrightarrow
\mathsf{EnumMem}_{A,\sim_A}(q,zs)
$$
hold, then, for every query history $q$,
$$
\mathsf{EnumMem}_{A,\sim_A}(q,xs)
\Longleftrightarrow
\mathsf{EnumMem}_{A,\sim_A}(q,zs).
$$
\end{lemma}
\begin{proof}
Fix a query history $q$. For the forward implication, assume $\mathsf{EnumMem}_{A,\sim_A}(q,xs)$. The forward half of the first displayed equivalence gives $\mathsf{EnumMem}_{A,\sim_A}(q,ys)$, and the forward half of the second displayed equivalence gives $\mathsf{EnumMem}_{A,\sim_A}(q,zs)$.

For the reverse implication, assume $\mathsf{EnumMem}_{A,\sim_A}(q,zs)$. The reverse half of the second displayed equivalence gives $\mathsf{EnumMem}_{A,\sim_A}(q,ys)$, and the reverse half of the first displayed equivalence gives $\mathsf{EnumMem}_{A,\sim_A}(q,xs)$. Thus the two enumeration-membership predicates are equivalent at $q$. Since $q$ was arbitrary, the pointwise equivalence holds for all query histories.
\end{proof}

\begin{theorem}[FinSet enumeration classifier transitivity]
\label{thm:finset-enumeration-classifier-transitivity}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be the carrier of a fixed $\SetUp$ certificate, let $\sim_A$ be its classifier, and let $\mathsf{cert}:\NameCert(A,\sim_A)$. For finite BEDC-history spines $xs$, $ys$, and $zs$, if
$$
\mathsf{FinSetEnumClassifier}_{A,\sim_A}(xs,ys)
\qquad\text{and}\qquad
\mathsf{FinSetEnumClassifier}_{A,\sim_A}(ys,zs)
$$
hold, then
$$
\mathsf{FinSetEnumClassifier}_{A,\sim_A}(xs,zs)
$$
holds.
\end{theorem}
\begin{proof}
The certificate fixes the ambient source and classifier. Unfold the classifier clause in \autoref{def:finset-enumeration-membership}. The first hypothesis supplies, for every query history $q$, the equivalence between $\mathsf{EnumMem}_{A,\sim_A}(q,xs)$ and $\mathsf{EnumMem}_{A,\sim_A}(q,ys)$. The second supplies, for the same $q$, the equivalence between $\mathsf{EnumMem}_{A,\sim_A}(q,ys)$ and $\mathsf{EnumMem}_{A,\sim_A}(q,zs)$.

By \autoref{lem:finset-enummem-pointwise-equivalence-composition}, these two pointwise equivalences compose to
$$
\forall q:\mathsf{BHist}.\;
\mathsf{EnumMem}_{A,\sim_A}(q,xs)
\Longleftrightarrow
\mathsf{EnumMem}_{A,\sim_A}(q,zs).
$$
Applying the same classifier clause in \autoref{def:finset-enumeration-membership} repacks this pointwise statement as $\mathsf{FinSetEnumClassifier}_{A,\sim_A}(xs,zs)$.
\end{proof}
\leanchecked{BEDC.Derived.FinsetUp.FinsetEnumerationClassifier\_trans}

\begin{definition}[Finite-spine predicate enumeration]
\label{def:finset-spine-enumerates-predicate}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be the carrier of a fixed $\SetUp$ certificate with classifier $\sim_A$. For a finite BEDC-history spine $xs:\mathsf{HistSpine}$ and a predicate $U:\mathsf{BHist}\to\mathsf{Prop}$, define
$$
\mathsf{FinSetSpineEnumerates}_{A,\sim_A}(xs,U)
\Longleftrightarrow
\forall z:\mathsf{BHist}.\;
\mathsf{EnumMem}_{A,\sim_A}(z,xs)\Longleftrightarrow U(z).
$$
\end{definition}

\begin{lemma}[Enumeration membership splits over spine append]
\label{lem:finset-enummem-append-split}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be the carrier of a fixed $\SetUp$ certificate with classifier $\sim_A$. For finite BEDC-history spines $xs,ys:\mathsf{HistSpine}$ and every query history $z:\mathsf{BHist}$,
$$
\mathsf{EnumMem}_{A,\sim_A}(z,xs{++}ys)
\Longleftrightarrow
\mathsf{EnumMem}_{A,\sim_A}(z,xs)\vee
\mathsf{EnumMem}_{A,\sim_A}(z,ys).
$$
\end{lemma}
\begin{proof}
Proceed by induction on the left spine $xs$, using the finite-spine constructors from \autoref{def:list-spine-representation} and the position readback clause of \autoref{def:finset-enumeration-membership}.

If $xs=\mathsf{nil}$, then $xs{++}ys=ys$ and $\mathsf{Pos}(\mathsf{nil})$ has no positions. Thus $\mathsf{EnumMem}_{A,\sim_A}(z,\mathsf{nil})$ has no position witness, while $\mathsf{EnumMem}_{A,\sim_A}(z,\mathsf{nil}{++}ys)$ is exactly $\mathsf{EnumMem}_{A,\sim_A}(z,ys)$. The required equivalence follows.

Now write $xs=\mathsf{cons}(x,xs_0)$ and assume the statement for $xs_0$ and $ys$. Then $xs{++}ys=\mathsf{cons}(x,xs_0{++}ys)$. A position of this cons spine is either the head position, whose readback is $x$, or a tail position coming from $xs_0{++}ys$.

For the forward implication, suppose $\mathsf{EnumMem}_{A,\sim_A}(z,\mathsf{cons}(x,xs_0{++}ys))$. Keep the displayed witness $A(z)$ and case-analyze its position. In the head case, the same head position of $\mathsf{cons}(x,xs_0)$ carries $A(x)$ and the same classifier witness $z\sim_A x$, so $\mathsf{EnumMem}_{A,\sim_A}(z,\mathsf{cons}(x,xs_0))$ holds. In the tail case, the witness is a position of $xs_0{++}ys$ with unchanged readback. The induction hypothesis gives either $\mathsf{EnumMem}_{A,\sim_A}(z,xs_0)$ or $\mathsf{EnumMem}_{A,\sim_A}(z,ys)$. The first alternative embeds through the tail position of $\mathsf{cons}(x,xs_0)$, giving the left disjunct; the second alternative is already the right disjunct.

For the reverse implication, suppose $\mathsf{EnumMem}_{A,\sim_A}(z,\mathsf{cons}(x,xs_0))\vee\mathsf{EnumMem}_{A,\sim_A}(z,ys)$. If the left disjunct holds, case-analyze its cons position. A head witness repacks at the head of $\mathsf{cons}(x,xs_0{++}ys)$. A tail witness gives $\mathsf{EnumMem}_{A,\sim_A}(z,xs_0)$; by the reverse half of the induction hypothesis it yields $\mathsf{EnumMem}_{A,\sim_A}(z,xs_0{++}ys)$, which then embeds through the tail position of $\mathsf{cons}(x,xs_0{++}ys)$. If the right disjunct holds, the reverse half of the induction hypothesis gives $\mathsf{EnumMem}_{A,\sim_A}(z,xs_0{++}ys)$, and the same tail-position embedding gives $\mathsf{EnumMem}_{A,\sim_A}(z,\mathsf{cons}(x,xs_0{++}ys))$.

The two implications prove the displayed equivalence for the cons spine, completing the induction.
\end{proof}
\leanchecked{BEDC.Derived.FinsetUp.FinsetEnumerationCarrier\_bundleAppend\_split}
\leanchecked{BEDC.Derived.FinsetUp.FinsetEnumerationCarrier\_append\_split}

\begin{theorem}[Finite-spine concatenation enumerates finite-set union]
\label{thm:finset-spine-concatenation-enumerates-union}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be the carrier of a fixed $\SetUp$ certificate with classifier $\sim_A$. Let $xs,ys:\mathsf{HistSpine}$ be finite BEDC-history spines, and let $U,V:\mathsf{BHist}\to\mathsf{Prop}$ be predicates. If
$$
\mathsf{FinSetSpineEnumerates}_{A,\sim_A}(xs,U)
\qquad\text{and}\qquad
\mathsf{FinSetSpineEnumerates}_{A,\sim_A}(ys,V)
$$
hold, then the concatenated finite spine enumerates the finite-set union predicate:
$$
\mathsf{FinSetSpineEnumerates}_{A,\sim_A}(xs{++}ys,\lambda z.\;U(z)\vee V(z)).
$$
\end{theorem}
\begin{proof}
Unfold \autoref{def:finset-spine-enumerates-predicate}. Fix a query history $z:\mathsf{BHist}$. By \autoref{lem:finset-enummem-append-split},
$$
\mathsf{EnumMem}_{A,\sim_A}(z,xs{++}ys)
\Longleftrightarrow
\mathsf{EnumMem}_{A,\sim_A}(z,xs)\vee
\mathsf{EnumMem}_{A,\sim_A}(z,ys).
$$
The first enumeration hypothesis converts $\mathsf{EnumMem}_{A,\sim_A}(z,xs)$ exactly into $U(z)$, and the second converts $\mathsf{EnumMem}_{A,\sim_A}(z,ys)$ exactly into $V(z)$.

For the forward implication, assume $\mathsf{EnumMem}_{A,\sim_A}(z,xs{++}ys)$. The append split gives membership in $xs$ or membership in $ys$. Applying the corresponding enumeration hypothesis yields $U(z)$ or $V(z)$.

For the reverse implication, assume $U(z)\vee V(z)$. In the $U(z)$ case, the reverse half of the first enumeration hypothesis gives $\mathsf{EnumMem}_{A,\sim_A}(z,xs)$, and the reverse half of the append split gives membership in $xs{++}ys$. In the $V(z)$ case, the reverse half of the second enumeration hypothesis gives $\mathsf{EnumMem}_{A,\sim_A}(z,ys)$, and the reverse half of the append split again gives membership in $xs{++}ys$.

Thus for every $z$ the appended enumeration predicate is equivalent to $U(z)\vee V(z)$, which is exactly the displayed enumeration claim.
\end{proof}
\leanchecked{BEDC.Derived.FinsetUp.FinsetEnumerationCarrier\_bundleAppend\_union}

\begin{lemma}[Certified finite-position identity bijection]
\label{lem:finset-enum-position-identity-bijection}
For every finite BEDC-history spine $xs:\mathsf{HistSpine}$, the identity map on $\mathsf{Pos}(xs)$ is a certified finite-position bijection
$$
\mathsf{id}_{xs}:\mathsf{Pos}(xs)\cong\mathsf{Pos}(xs),
$$
with inverse map again the identity map.
\end{lemma}
\begin{proof}
Take the forward position map to be $i\mapsto i$ and take the inverse position map to be the same function. The two inverse identities are the pointwise equalities $i=i$ and $j=j$ for positions of $xs$. These data are exactly the certified finite-position bijection data used in \autoref{def:finset-classifier-preserving-enumeration-permutation}.
\end{proof}

\begin{theorem}[EnumPerm identity closure]
\label{thm:enumperm-identity-closure}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be the carrier of a fixed $\SetUp$ certificate, let $\sim_A$ be its classifier, and let $\mathsf{cert}:\NameCert(A,\sim_A)$. For every finite BEDC-history spine $xs:\mathsf{HistSpine}$, if every entry of $xs$ is carried by $A$,
$$
\forall i:\mathsf{Pos}(xs).\;A(\mathsf{entry}_{xs}(i)),
$$
then
$$
\mathsf{EnumPerm}_{A,\sim_A}(xs,xs)
$$
holds.
\end{theorem}
\begin{proof}
Use the certified identity bijection from \autoref{lem:finset-enum-position-identity-bijection}. It remains only to verify the pointwise preservation field required by \autoref{def:finset-classifier-preserving-enumeration-permutation}.

Fix a position $i:\mathsf{Pos}(xs)$. The carried-entry hypothesis gives
$$
A(\mathsf{entry}_{xs}(i)).
$$
For the identity bijection, the matched source position and target position are both $i$, so the source and target entries in the matched pair are the same history $\mathsf{entry}_{xs}(i)$. The reflexivity field of $\mathsf{cert}$, supplied by the naming certificate in \autoref{def:lean-carrier-naming-certificate}, applies to the displayed carrier witness and gives
$$
\mathsf{entry}_{xs}(i)\sim_A\mathsf{entry}_{xs}(i).
$$
Thus the matched pair at $i$ satisfies
$$
A(\mathsf{entry}_{xs}(i))\land
A(\mathsf{entry}_{xs}(i))\land
\mathsf{entry}_{xs}(i)\sim_A\mathsf{entry}_{xs}(i).
$$
Since $i$ was arbitrary, the certified identity bijection carries the required pointwise classifier-preservation field. Repacking these data by \autoref{def:finset-classifier-preserving-enumeration-permutation} proves $\mathsf{EnumPerm}_{A,\sim_A}(xs,xs)$.
\end{proof}

\begin{lemma}[Pointwise enumeration membership equivalences invert]
\label{lem:finset-enummem-pointwise-equivalence-symmetry}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be a BEDC history source carrier, let $\sim_A$ be a source classifier, and let $xs$ and $ys$ be finite BEDC-history spines. If, for every query history $q$,
$$
\mathsf{EnumMem}_{A,\sim_A}(q,xs)
\Longleftrightarrow
\mathsf{EnumMem}_{A,\sim_A}(q,ys)
$$
holds, then, for every query history $q$,
$$
\mathsf{EnumMem}_{A,\sim_A}(q,ys)
\Longleftrightarrow
\mathsf{EnumMem}_{A,\sim_A}(q,xs)
$$
holds.
\end{lemma}
\begin{proof}
Fix a query history $q$. The displayed hypothesis at $q$ supplies both membership implications between the two finite-spine enumeration predicates. For the forward implication in the inverted direction, assume $\mathsf{EnumMem}_{A,\sim_A}(q,ys)$. The reverse half of the hypothesis at $q$ gives $\mathsf{EnumMem}_{A,\sim_A}(q,xs)$. For the reverse implication in the inverted direction, assume $\mathsf{EnumMem}_{A,\sim_A}(q,xs)$. The forward half of the hypothesis at $q$ gives $\mathsf{EnumMem}_{A,\sim_A}(q,ys)$. Since $q$ was arbitrary, the inverted pointwise equivalence holds for all query histories.
\end{proof}

\begin{theorem}[FinSet enumeration classifier symmetry]
\label{thm:finset-enumeration-classifier-symmetry}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be the carrier of a fixed $\SetUp$ certificate, let $\sim_A$ be its classifier, and let $xs$ and $ys$ be finite BEDC-history spines. If
$$
\mathsf{FinSetEnumClassifier}_{A,\sim_A}(xs,ys)
$$
holds, then
$$
\mathsf{FinSetEnumClassifier}_{A,\sim_A}(ys,xs)
$$
holds.
\end{theorem}
\leanchecked{BEDC.Derived.FinsetUp.FinsetEnumerationClassifier\_symm}
\begin{proof}
Unfold the classifier clause in \autoref{def:finset-enumeration-membership}. The hypothesis supplies, for every query history $q$, the pointwise equivalence
$$
\mathsf{EnumMem}_{A,\sim_A}(q,xs)
\Longleftrightarrow
\mathsf{EnumMem}_{A,\sim_A}(q,ys).
$$
By \autoref{lem:finset-enummem-pointwise-equivalence-symmetry}, this equivalence inverts pointwise to
$$
\forall q:\mathsf{BHist}.\;
\mathsf{EnumMem}_{A,\sim_A}(q,ys)
\Longleftrightarrow
\mathsf{EnumMem}_{A,\sim_A}(q,xs).
$$
Applying the same classifier clause in \autoref{def:finset-enumeration-membership} repacks this pointwise statement as $\mathsf{FinSetEnumClassifier}_{A,\sim_A}(ys,xs)$.
\end{proof}

\begin{lemma}[Certified finite-position bijections invert]
\label{lem:finset-enum-position-bijection-inversion}
For finite BEDC-history spines $xs$ and $ys$, any certified finite-position bijection
$$
\pi:\mathsf{Pos}(xs)\cong\mathsf{Pos}(ys)
$$
has an inverse certified finite-position bijection
$$
\pi^{-1}:\mathsf{Pos}(ys)\cong\mathsf{Pos}(xs),
$$
whose inverse position map is $\pi$.
\end{lemma}
\begin{proof}
By the certified-bijection clause recalled in \autoref{def:finset-classifier-preserving-enumeration-permutation}, the datum for $\pi$ consists of the forward position map $\pi$, the inverse position map $\pi^{-1}$, and the two inverse identities
$$
\pi^{-1}(\pi(i))=i
\qquad\text{and}\qquad
\pi(\pi^{-1}(j))=j.
$$
For the reversed bijection, take forward map $\pi^{-1}$ and inverse map $\pi$. Its two identities are exactly the same two identities with the source and target position carriers exchanged.
\end{proof}

\begin{theorem}[EnumPerm inverse symmetry]
\label{thm:enumperm-inverse-symmetry}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be the carrier of a fixed $\SetUp$ certificate, let $\sim_A$ be its classifier, and let $\mathsf{cert}:\NameCert(A,\sim_A)$. For finite BEDC-history spines $xs$ and $ys$, if
$$
\mathsf{EnumPerm}_{A,\sim_A}(xs,ys)
$$
holds, then
$$
\mathsf{EnumPerm}_{A,\sim_A}(ys,xs)
$$
holds.
\end{theorem}
\begin{proof}
Unfold \autoref{def:finset-classifier-preserving-enumeration-permutation}. Choose the certified bijection
$$
\pi:\mathsf{Pos}(xs)\cong\mathsf{Pos}(ys)
$$
and its pointwise preservation field $P_\pi$. By \autoref{lem:finset-enum-position-bijection-inversion}, $\pi^{-1}$ is a certified bijection from $\mathsf{Pos}(ys)$ to $\mathsf{Pos}(xs)$.

It remains to verify the pointwise preservation field for this inverse bijection. Fix $j:\mathsf{Pos}(ys)$ and put $i=\pi^{-1}(j)$. The inverse identity gives $\pi(i)=j$. The field $P_\pi(i)$ gives
$$
A(\mathsf{entry}_{xs}(i))
\qquad\text{and}\qquad
A(\mathsf{entry}_{ys}(\pi(i)))
$$
and also
$$
\mathsf{entry}_{xs}(i)\sim_A\mathsf{entry}_{ys}(\pi(i)).
$$
Using $\pi(i)=j$, these become the carrier witnesses $A(\mathsf{entry}_{xs}(i))$ and $A(\mathsf{entry}_{ys}(j))$, together with the classifier comparison
$$
\mathsf{entry}_{xs}(i)\sim_A\mathsf{entry}_{ys}(j).
$$
The symmetry field of $\mathsf{cert}$, supplied by \autoref{def:lean-carrier-naming-certificate}, reverses this comparison to
$$
\mathsf{entry}_{ys}(j)\sim_A\mathsf{entry}_{xs}(i).
$$
Thus $j$ satisfies the pointwise carrier-and-classifier clause required for the inverse map $\pi^{-1}$. Since $j$ was arbitrary, the inverse certified bijection and this pointwise field repack as $\mathsf{EnumPerm}_{A,\sim_A}(ys,xs)$.
\end{proof}
\leanchecked{BEDC.Derived.FinsetUp.FinsetEnumerationPermutation\_symm}

\begin{theorem}[EnumPerm identity reflexivity]
\label{thm:enumperm-identity-reflexivity}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be the carrier of a fixed $\SetUp$ certificate, let $\sim_A$ be its classifier, and let $\mathsf{cert}:\NameCert(A,\sim_A)$. For every finite BEDC-history spine $xs:\mathsf{HistSpine}$, if every position entry is carried by $A$,
$$
\forall i:\mathsf{Pos}(xs).\;A(\mathsf{entry}_{xs}(i)),
$$
then
$$
\mathsf{EnumPerm}_{A,\sim_A}(xs,xs)
$$
holds, witnessed by the identity bijection on $\mathsf{Pos}(xs)$.
\end{theorem}
\begin{proof}
Unfold \autoref{def:finset-classifier-preserving-enumeration-permutation}. Supply the certified finite-position bijection whose forward map and inverse map are both the identity function on $\mathsf{Pos}(xs)$. The two inverse identities are the pointwise equalities $i=i$ and $j=j$, so this is a certified bijection from $\mathsf{Pos}(xs)$ to itself.

It remains to verify the pointwise carrier-and-classifier field. Fix a position $i:\mathsf{Pos}(xs)$. The hypothesis gives
$$
A(\mathsf{entry}_{xs}(i)).
$$
Under the identity position map, the matched source and target entries are both $\mathsf{entry}_{xs}(i)$. The reflexivity field of $\mathsf{cert}$, supplied by \autoref{def:lean-carrier-naming-certificate}, gives
$$
\mathsf{entry}_{xs}(i)\sim_A\mathsf{entry}_{xs}(i).
$$
Therefore the matched pair at $i$ satisfies
$$
A(\mathsf{entry}_{xs}(i))\land
A(\mathsf{entry}_{xs}(i))\land
\mathsf{entry}_{xs}(i)\sim_A\mathsf{entry}_{xs}(i).
$$
Since $i$ was arbitrary, the identity certified bijection has the required pointwise preservation field. Repacking these data proves $\mathsf{EnumPerm}_{A,\sim_A}(xs,xs)$.
\end{proof}
\leanchecked{BEDC.Derived.FinsetUp.FinsetEnumerationPermutation\_refl}

\begin{definition}[FinSet NameCert obligation packet]
\label{def:finset-namecert-obligation-packet}
The finite-set certificate obligation packet for $\FinSetUp$ consists of the following ten fields:
\begin{enumerate}
  \item a traditional finite subset predicate $U:\mathsf{BHist}\to\mathsf{Prop}$;
  \item a BEDC source certificate $\NameCert_{\SetUp}$ with carrier $A$ and classifier $\sim_A$;
  \item a $\NameCert_{\ListUp}$ finite spine $xs:\mathsf{HistSpine}$ satisfying $\mathsf{ListSpineRep}_A(e,xs)$;
  \item the carrier $\mathsf{FinSetEnumCarrier}_{A,\sim_A}(xs)$ from \autoref{def:finset-enumeration-carrier};
  \item the classifier $\mathsf{FinSetEnumClassifier}_{A,\sim_A}$ from \autoref{def:finset-enumeration-membership};
  \item the stability rows for certified enumeration permutation, duplicate insertion, classifier transitivity, classifier symmetry, and append-union concatenation;
  \item the finite-position ledger $\mathsf{Pos}(xs)$ with readback $\mathsf{entry}_{xs}$ and certified bijections between position carriers;
  \item the exactness predicate comparing $\mathsf{FinSetEnumCarrier}_{A,\sim_A}(xs)(a)$ with finite-spine occurrence modulo $\sim_A$;
  \item the standard finite-set bridge that reads the enumerated predicate $U$ through $\mathsf{FinSetSpineEnumerates}_{A,\sim_A}(xs,U)$;
  \item the status assertion that the listed fields are obligations for $\NameCert_{\FinSetUp}$ rather than a closed public finite-set API.
\end{enumerate}
\end{definition}

\begin{theorem}[FinSet carrier obligation]
\label{thm:finset-carrier-obligation}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be the source carrier of a fixed $\NameCert_{\SetUp}$, let $\sim_A$ be its classifier, let $\mathsf{cert}:\NameCert(A,\sim_A)$, and let $e$ and $xs$ satisfy $\mathsf{ListSpineRep}_A(e,xs)$. Then the carrier field of \autoref{def:finset-namecert-obligation-packet} is exact: for every history $a$, membership in $\mathsf{FinSetEnumCarrier}_{A,\sim_A}(xs)$ is precisely finite-spine occurrence in $xs$ modulo $\sim_A$.
\end{theorem}
\begin{proof}
The carrier field of the packet is definitionally the carrier in \autoref{def:finset-enumeration-carrier}. The required exactness statement is exactly the bidirectional membership statement of \autoref{thm:finset-enumeration-membership-exactness}, with the displayed $\mathsf{ListSpineRep}_A(e,xs)$ witness supplying the finite-spine source condition.

Thus the packet records no additional carrier structure beyond the source certificate, the finite spine, and the exactness row already proved for $\mathsf{FinSetEnumCarrier}_{A,\sim_A}$.
\end{proof}

\begin{theorem}[FinSet classifier obligation rows]
\label{thm:finset-classifier-obligation}
Let $A:\mathsf{BHist}\to\mathsf{Prop}$ be the source carrier of a fixed $\NameCert_{\SetUp}$, let $\sim_A$ be its classifier, and let $\mathsf{cert}:\NameCert(A,\sim_A)$. The classifier field of \autoref{def:finset-namecert-obligation-packet} carries the finite-set obligation rows for certified enumeration permutation, duplicate insertion, append-union enumeration, and equivalence closure.
\end{theorem}
\begin{proof}
The permutation row is \autoref{thm:finset-classifier-enumeration-permutation-invariant}: a certified finite-position bijection preserving $A$ and $\sim_A$ pointwise leaves $\mathsf{FinSetEnumClassifier}_{A,\sim_A}$ unchanged.

The duplicate row is \autoref{thm:finset-duplicate-insertion-invariant}: a source-carried head entry already represented in the spine is invisible to the classifier. The append-union row is \autoref{thm:finset-spine-concatenation-enumerates-union}: concatenating finite spines enumerates the union predicate.

The equivalence rows are \autoref{thm:finset-enumeration-classifier-transitivity} and \autoref{thm:finset-enumeration-classifier-symmetry}. Together with the reflexive identity row proved above, these rows give the classifier obligation surface needed by the packet.
\end{proof}

\begin{theorem}[FinSet stability exactness ledger and bridge obligations]
\label{thm:finset-stability-exactness-ledger-obligation}
For any packet as in \autoref{def:finset-namecert-obligation-packet}, the displayed local rows jointly determine the finite-set stability, exactness, ledger, and bridge obligations over the BEDC source $\NameCert_{\SetUp}$ and the finite-spine source $\NameCert_{\ListUp}$.
\end{theorem}
\begin{proof}
First, the exactness obligation is the carrier equivalence supplied by \autoref{thm:finset-carrier-obligation}, whose proof rests on \autoref{thm:finset-enumeration-membership-exactness}. This binds ordinary finite-spine occurrence to the BEDC carrier field through the fixed source classifier.

Next, the stability obligations are the classifier rows collected in \autoref{thm:finset-classifier-obligation}: certified permutation, duplicate insertion, transitivity, symmetry, and append-union concatenation. These cite \autoref{thm:finset-classifier-enumeration-permutation-invariant}, \autoref{thm:finset-duplicate-insertion-invariant}, \autoref{thm:finset-enumeration-classifier-transitivity}, \autoref{thm:finset-enumeration-classifier-symmetry}, and \autoref{thm:finset-spine-concatenation-enumerates-union}.

Finally, the ledger obligation is the finite position carrier $\mathsf{Pos}(xs)$ with readback $\mathsf{entry}_{xs}$, used by the same permutation and occurrence rows. The bridge obligation is the predicate enumeration clause $\mathsf{FinSetSpineEnumerates}_{A,\sim_A}(xs,U)$ together with the append-union row, which identifies the standard finite-set surface without asserting the larger finite-set API.
\end{proof}

\begin{closurestatus}{\FinSetUp}
  \theoryclosure{\scopedClosure}
  \scopeclosed{The finite-spine enumeration certificate over $\NameCert_{\SetUp}$ and $\NameCert_{\ListUp}$ is bound to $\mathsf{FinSetEnumCarrier}_{A,\sim_A}$, $\mathsf{FinSetEnumClassifier}_{A,\sim_A}$, $\mathsf{EnumPerm}$, the $\mathsf{Pos}$ and entry ledger, duplicate, permutation, symmetry, transitivity, append-union rows, and $\mathsf{FinSetSpineEnumerates}_{A,\sim_A}$ as the standard finite-set bridge surface.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.FinsetUp.FinsetEnumerationClassifier\_permutation\_invariant}
  \bridgestatus{none}
  \notclaimed{Subset calculus, cardinality, decidable membership, power set, graph finiteness, and a full standard finite-set bridge remain outside this scoped certificate.}
  \upgradepath{$\publicClosure$ requires a public $\FinSetUp$ export theorem rather than another scoped packet.}
\end{closurestatus}
