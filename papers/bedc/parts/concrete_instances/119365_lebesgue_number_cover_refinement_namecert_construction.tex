\chapter{A Concrete Naming Certificate for $\LebesgueNumberCoverRefinementUp$}
\label{ch:concrete-instances-lebesgue-number-cover-refinement-namecert}
\origin{human}

Following Bishop--Bridges constructive compactness and Kelley's cover-refinement vocabulary, the $\LebesgueNumberCoverRefinementUp$ packet records the finite route that turns a displayed compact cover, a positive Lebesgue-number radius, and a finite refinement request into a retained dyadic cover refinement. It sits over the radius lemma of \autoref{ch:concrete-instances-lebesgue-number-lemma-namecert}, the finite comparison packet of \autoref{ch:concrete-instances-finite-cover-refinement-namecert}, the star-refinement modulus route of \autoref{ch:concrete-instances-finite-cover-star-refinement-modulus-namecert}, and the located finite-subcover route of \autoref{ch:concrete-instances-located-heine-borel-subcover-namecert}. The packet names a finite cover-refinement certificate; it is not arbitrary open-cover compactness, not a selected subcover functional, and not ambient real completeness.

\begin{definition}[Lebesgue-number cover-refinement carrier]
\label{def:lebesgue-number-cover-refinement-carrier}
A $\LebesgueNumberCoverRefinementUp$ carrier is a finite $\BHist$ packet
$$
R=(K,L,F,D,S,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact source row, exposing the compact metric or located compact interval data only through the finite carrier already accepted by the surrounding chapter.
\item $L$ is the $\LebesgueNumberLemmaUp$ positive-radius row attached to the displayed cover request.
\item $F$ is the $\FiniteCoverRefinementUp$ comparison row that records how the retained cover members refine the source cover.
\item $D$ is the dyadic cover-refinement row, storing the mesh, ball, or cell comparisons used to compare the radius row with the retained finite cover.
\item $S$ is the retained subcover row, listing only the finitely many cover witnesses reached through $K,L,F,D$.
\item $E$ is the terminal $\RealUp$ seal for radius, endpoint, and coverage observations used by the refinement read.
\item $H$ stores componentwise $\hsame$ transport for the compact source, radius row, finite refinement row, dyadic refinement row, retained subcover row, Real seal, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from a compact cover request through $K,L,F,D,S,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\LebesgueNumberCoverRefinementUp$, $\LebesgueNumberLemmaUp$, $\FiniteCoverRefinementUp$, $\FiniteCoverStarRefinementModulusUp$, $\LocatedHeineBorelSubcoverUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\LebesgueNumberCoverRefinementUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,L,F,D,S,E,H,C,P,N$. It has no coordinate for arbitrary open-cover compactness, choice-selected subcovers, quotient real equality, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Lebesgue-number cover-refinement NameCert obligations]
\label{thm:lebesgue-number-cover-refinement-namecert-obligations}
For every accepted $\LebesgueNumberCoverRefinementUp$ carrier
$$
R=(K,L,F,D,S,E,H,C,P,N),
$$
the local $\NameCert_{\LebesgueNumberCoverRefinementUp}$ surface consists exactly of carrier admission for the displayed compact source, Lebesgue-number radius, finite cover refinement, dyadic cover refinement, retained subcover, Real seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming rows; radius compatibility through $L,D$; finite refinement exactness through $F,S$; Real-seal non-escape through $E$; and structural preservation through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:lebesgue-number-cover-refinement-carrier}. The analytic rows are $K,L,F,D,S,E$, and the structural rows are $H,C,P,N$.

The positive-radius data are read from $L$ and compared with the dyadic refinement row $D$. The finite comparison row $F$ then determines which retained witnesses may enter $S$.

The terminal seal $E$ is downstream of those finite rows. Transport, replay, provenance, and local naming preserve the same displayed packet, so no obligation can be discharged by an arbitrary open-cover compactness principle, a selected subcover functional, quotient real equality, countable choice, or ambient completeness.
\end{proof}

\begin{theorem}[Lebesgue-number radius-to-refinement route]
\label{thm:lebesgue-number-cover-refinement-radius-route}
Every accepted $\LebesgueNumberCoverRefinementUp$ cover read factors through the finite route
$$
\LebesgueNumberLemmaUp \longmapsto \FiniteCoverRefinementUp \longmapsto D,S \longmapsto \RealUp .
$$
The exported object is the retained dyadic cover-refinement surface over $K,L,F,D,S,E,H,C,P,N$, not an arbitrary open-cover compactness theorem or a choice-selected subcover.
\end{theorem}
\begin{proof}
Start with an accepted carrier $R=(K,L,F,D,S,E,H,C,P,N)$. The compact source is $K$, the positive-radius row is $L$, and the finite comparison row is $F$.

The dyadic row $D$ compares the radius with the displayed finite cells. The retained subcover row $S$ is readable only after that comparison, so a consumer cannot bypass the radius ledger or the finite refinement packet.

Finally the Real seal $E$ and the structural rows $H,C,P,N$ replay the same finite route. Since the carrier has no coordinate for arbitrary open covers, selected subcovers, quotient real equality, countable choice, or ambient completeness, the exported refinement is exactly the displayed BEDC packet.
\end{proof}

\falsifiablePrediction{An accepted $\LebesgueNumberCoverRefinementUp$ consumer that exports a retained cover refinement without displaying $K,L,F,D,S,E,H,C,P,N$ refutes the carrier surface.}

\independenceWitness{Nearest siblings: lebesgue-number-lemma, finite-cover-refinement, finite-cover-star-refinement-modulus, and located-heine-borel-subcover. The packet binds the positive-radius row to a retained dyadic finite-refinement surface, while each sibling supplies only its own carrier route.}

\closureat{LebesgueNumberCoverRefinementUp}{seedStr}

\begin{closurestatus}{\LebesgueNumberCoverRefinementUp}
  \constructivestory{A $\LebesgueNumberCoverRefinementUp$ packet is generated from a compact source row, a LebesgueNumberLemma positive-radius row, a FiniteCoverRefinement comparison row, dyadic cover-refinement rows, retained finite subcover witnesses, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:lebesgue-number-cover-refinement-carrier}, \autoref{thm:lebesgue-number-cover-refinement-namecert-obligations}, and \autoref{thm:lebesgue-number-cover-refinement-radius-route} seed the finite radius-to-refinement route over compact source rows, positive-radius ledgers, finite comparison rows, dyadic cover refinements, retained subcovers, and Real seals.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close arbitrary open-cover compactness, selected subcovers, quotient real equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, radius compatibility, finite refinement exactness, dyadic refinement admission, retained-subcover exactness, Real-seal non-escape, transport, replay, provenance, and local naming for BEDC.Derived.LebesgueNumberCoverRefinementUp.}
\end{closurestatus}
