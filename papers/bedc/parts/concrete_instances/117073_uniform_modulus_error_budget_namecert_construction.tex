\chapter{A Concrete Naming Certificate for $\UniformModulusErrorBudgetUp$}
\label{ch:concrete-instances-uniform-modulus-error-budget-namecert}
\origin{human}

Following Bishop and Bridges compact-uniform continuity, the $\UniformModulusErrorBudgetUp$ packet records the finite error-budget ledger used to pass from compact finite nets and pointwise moduli to a tolerance-only uniform-modulus handoff. It reads the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map graph of \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the rational lower-bound surface of \autoref{ch:concrete-instances-rat-namecert}, the metric triangle rows of \autoref{ch:concrete-instances-metric-namecert}, and the Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only finite epsilon-third, compact-net, rational-fold, metric-triangle, transport, replay, provenance, and local naming rows.

\begin{definition}[Uniform modulus error-budget carrier]
\label{def:uniform-modulus-error-budget-carrier}
A $\UniformModulusErrorBudgetUp$ carrier is a finite $\BHist$ packet
$$
E=(X,F,U,Q,M,R,B,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\CompactMetricUp$ finite-net source row.
\item $F$ is the $\ContinuousMapUp$ pointwise-modulus row over the same metric source.
\item $U$ is the $\UniformModulusUp$ consumer row that receives the tolerance-only precision history.
\item $Q$ is the $\RatUp$ positive-radius and lower-bound fold row over the displayed compact-net probes.
\item $M$ is the $\MetricUp$ source-and-target triangle row used to split and recombine the error estimate.
\item $R$ is the $\RealUp$ seal row for the displayed source and target distance estimates.
\item $B$ is the epsilon-third budget row, recording the three visible tolerances, their rational comparisons, and the folded precision consumed by $U$.
\item $T$ is the compact-net triangle ledger tying $X,F,Q,M,R,B$ to one finite estimate route.
\item $H$ records componentwise $\hsame$ transport for $X,F,U,Q,M,R,B,T$.
\item $C$ records displayed $\Cont$ replay for compact-net reads, pointwise-modulus reads, rational-fold reads, metric-triangle reads, Real-seal reads, provenance, and local naming.
\item $P$ is the $\Pkg$ provenance over $\UniformModulusErrorBudgetUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\RatUp$, $\MetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\UniformModulusErrorBudgetUp}$ row.
\end{enumerate}
Accepted packets are compared only through $X,F,U,Q,M,R,B,T,H,C,P,N$. They have no coordinate for a selected Lebesgue number, an external modulus selector, quotient equality of metric points, sequential compactness, countable choice, or ambient Real completeness.
\end{definition}

\begin{theorem}[Uniform modulus error-budget NameCert obligations]
\label{thm:uniform-modulus-error-budget-namecert-obligations}
For the carrier of \autoref{def:uniform-modulus-error-budget-carrier}, the local $\NameCert_{\UniformModulusErrorBudgetUp}$ surface consists exactly of compact-metric finite-net exposure, continuous-map pointwise-modulus exposure, uniform-modulus consumer exposure, rational lower-bound fold exposure, metric triangle exposure, Real-seal exposure, epsilon-third budget exposure, compact-net triangle-ledger exposure, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier. The compact finite-net row $X$, continuous-map row $F$, rational fold $Q$, metric row $M$, Real seal $R$, and budget row $B$ are the only rows from which a tolerance estimate can be read.

The budget row splits the requested tolerance into three displayed rational comparisons. The compact-net ledger $T$ then routes the source point to a displayed center, applies the pointwise-modulus row at that center, and uses the metric triangle row to recombine the three estimates before the uniform-modulus consumer row $U$ is exposed.

Transport, replay, provenance, and local naming keep this same finite inventory. Since the carrier contains no selected Lebesgue number, external modulus selector, quotient point equality, sequence extractor, choice principle, or ambient Real-completeness row, none of those objects can be exported by the NameCert surface.
\end{proof}

\begin{theorem}[Uniform modulus error-budget triangle route]
\label{thm:uniform-modulus-error-budget-triangle-route}
Let $E=(X,F,U,Q,M,R,B,T,H,C,P,N)$ be an accepted $\UniformModulusErrorBudgetUp$ carrier. Every $\UniformModulusUp$ consumer must read the epsilon-third budget $B$, the compact-net row $X$, the continuous-map row $F$, the $\RatUp$ fold row $Q$, the $\MetricUp$ triangle row $M$, and the $\RealUp$ seal row $R$ before reaching $U$. The handoff realizes the compact-uniform-continuity estimate as a finite triangle budget and exports no host compactness theorem or external modulus function.
\end{theorem}
\begin{proof}
Begin with the accepted carrier and expose the budget row $B$. Its three rational comparisons are displayed histories, so the tolerance split is part of the packet rather than a hidden arithmetic choice.

Next read the compact-net route. The row $X$ supplies the finite centers, $F$ supplies pointwise-modulus data at those centers, and $Q$ folds the visible positive radii to a common precision history.

Finally apply the metric triangle ledger. The row $M$ combines the source-center, source-pair, and target-distance estimates, while $R$ seals the displayed Real distance rows before $U$ consumes the resulting tolerance-only modulus. The structural rows $H,C,P,N$ transport and replay this route without adding host compactness, quotient equality, or an external modulus function.
\end{proof}

\falsifiablePrediction{An accepted $\UniformModulusErrorBudgetUp$ consumer is refuted if it reaches the UniformModulusUp handoff without displaying the epsilon-third budget, CompactMetric row, ContinuousMap row, RatUp fold, Metric triangle row, RealUp seal, transport, replay, provenance, and local $\NameCert$ rows.}

\independenceWitness{compactmetric, continuousmap, uniformmodulus, rat, metric, real: none is bijective to $\UniformModulusErrorBudgetUp$ because this packet jointly carries the epsilon-third budget, compact finite-net route, pointwise-modulus rows, rational lower-bound fold, metric triangle estimates, Real seal, transport, replay, provenance, and local naming.}

\closureat{UniformModulusErrorBudgetUp}{seedStr}

\begin{closurestatus}{\UniformModulusErrorBudgetUp}
  \constructivestory{A $\UniformModulusErrorBudgetUp$ packet is generated from CompactMetric finite-net rows, ContinuousMap pointwise-modulus rows, a UniformModulus consumer row, Rat positive-radius folds, Metric triangle rows, Real distance seals, an epsilon-third budget row, a compact-net triangle ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:uniform-modulus-error-budget-carrier}, \autoref{thm:uniform-modulus-error-budget-namecert-obligations}, and \autoref{thm:uniform-modulus-error-budget-triangle-route} seed the finite epsilon-third and metric-triangle budget consumed by compact-uniform-continuity routes.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, selected Lebesgue numbers, external modulus selection, quotient metric equality, sequential compactness, countable choice, or ambient Real completeness for $\UniformModulusErrorBudgetUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, budget-row exactness, compact-net exposure, continuous-map exposure, rational-fold exactness, metric-triangle compatibility, Real-seal nonescape, uniform-modulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.UniformModulusErrorBudgetUp.}
\end{closurestatus}
