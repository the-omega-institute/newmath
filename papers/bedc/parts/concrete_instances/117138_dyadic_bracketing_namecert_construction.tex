\chapter{A Concrete Naming Certificate for $\DyadicBracketingUp$}
\label{ch:concrete-instances-dyadic-bracketing-namecert}
\origin{human}

Following Bishop and Bridges constructive real analysis, the $\DyadicBracketingUp$ packet records the finite bracket data that feeds L10 real names before any completion-facing consumer reads a $\RealUp$ seal. It sits after the dyadic arithmetic surface of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the located interval surface of \autoref{ch:concrete-instances-located-real-interval-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names finite lower and upper dyadic brackets with tolerance and nesting rows; it is not a quotient real construction, a selected completed limit, trichotomy on arbitrary reals, countable choice, or ambient completeness for $\RealUp$.

\begin{definition}[Dyadic bracketing carrier]
\label{def:dyadic-bracketing-carrier}
A $\DyadicBracketingUp$ carrier is a finite $\BHist$ packet
$$
B=(L,U,G,S,R,E,I,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the lower $\DyadicRatCoreUp$ endpoint row.
\item $U$ is the upper $\DyadicRatCoreUp$ endpoint row.
\item $G$ is the finite gap and tolerance ledger, recording $L \leq U$ and the dyadic scale at which the bracket is accepted.
\item $S$ is the $\StreamNameUp$ window row whose observations request and replay the bracket.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the displayed endpoints, gap ledger, and stream window.
\item $E$ is the terminal $\RealUp$ seal row, available only after the regular rational readback row $R$ is visible.
\item $I$ is the $\LocatedRealIntervalUp$ source row that packages the same bracket as a located endpoint interval.
\item $H$ records componentwise $\hsame$ transport for endpoint, gap, stream, readback, seal, interval, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a tolerance request through $L,U,G,S,I$ to $R$ and then to $E$.
\item $P$ is the $\Pkg$ provenance over $\DyadicBracketingUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\LocatedRealIntervalUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DyadicBracketingUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for quotient stream equality, a chosen real representative, arbitrary real trichotomy, countable choice, or an ambient completeness principle.
\end{definition}
\leanchecked{BEDC.Derived.DyadicBracketingUp.DyadicBracketingTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Dyadic bracketing NameCert obligations]
\label{thm:dyadic-bracketing-namecert-obligations}
For every accepted $\DyadicBracketingUp$ carrier
$$
B=(L,U,G,S,R,E,I,H,C,P,N),
$$
the local $\NameCert_{\DyadicBracketingUp}$ surface consists exactly of lower endpoint exposure, upper endpoint exposure, dyadic gap and tolerance compatibility, finite stream-window readback, located-interval packaging, regular rational handoff, terminal $\RealUp$ seal nonescape, componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local naming. Every Real or Completion consumer must read the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\LocatedRealIntervalUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows before treating the bracket as a real-name object.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-bracketing-carrier}. The endpoint rows $L,U$ and the gap ledger $G$ are the only dyadic data visible to the local classifier.

Read the observation and interval rows next. The stream window $S$ requests the bracket at a finite tolerance, and the located interval row $I$ repackages the same displayed endpoints without introducing a host interval object.

The consumer-facing handoff is downstream. The row $R$ is the regular rational readback from $L,U,G,S,I$, and the $\RealUp$ seal $E$ is exposed only after $R$.

Finally the structural rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since the excluded quotient, selected-limit, trichotomy, choice, and completeness data are not carrier coordinates, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Dyadic bracketing finite-bracket induction]
\label{thm:dyadic-bracketing-finite-bracket-induction}
Let $B=(L,U,G,S,R,E,I,H,C,P,N)$ be an accepted $\DyadicBracketingUp$ carrier. Any finite refinement history of the displayed bracket preserves the same route
$$
L,U,G,S,I \longmapsto R \longmapsto E
$$
by induction on the bracket ledger. The base bracket reads the displayed lower and upper dyadic endpoints; each successor refinement tightens the gap ledger through $\DyadicRatCoreUp$ rows, replays the finite $\StreamNameUp$ window, preserves the $\LocatedRealIntervalUp$ packaging, and leaves the $\RegSeqRatUp$ readback before the terminal $\RealUp$ seal.
\end{theorem}
\begin{proof}
Project the accepted carrier. The rows $L,U,G,S,I$ are visible before the regular rational readback and the Real seal, so the initial bracket is a finite dyadic object rather than a completed real.

Induct on the finite bracket ledger recorded by $G$. In the base case the ledger contains the displayed endpoint order and tolerance scale, and the stream window $S$ reads that bracket directly.

For a successor refinement, the new endpoint rows are obtained through the displayed $\DyadicRatCoreUp$ comparisons in $G$. The stream window is replayed through $S$, and the located interval row $I$ records the same refined endpoints as a finite interval package.

Apply \autoref{thm:dyadic-bracketing-namecert-obligations} to the consumer-facing rows. The $\RegSeqRatUp$ readback $R$ remains downstream of the bracket ledger, and $E$ remains downstream of $R$, so the induction cannot export quotient real equality, selected limits, trichotomy, countable choice, or ambient completeness.
\end{proof}

\begin{theorem}[Dyadic bracketing L10 real-name handoff]
\label{thm:dyadic-bracketing-l10-real-name-handoff}
Every L10 Real or Completion consumer of an accepted $\DyadicBracketingUp$ carrier must pass through the finite dyadic bracket rows before reading the regular rational handoff and terminal real-name seal. In carrier notation, the dependency order is $L,U,G,S,I$ before $R$, and $R$ before $E$, with $H,C,P,N$ preserving the same route under transport, replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Start from the finite-bracket induction of \autoref{thm:dyadic-bracketing-finite-bracket-induction}. It keeps every accepted refinement inside the displayed dyadic endpoint, gap, stream-window, and located-interval rows.

Use the obligation theorem \autoref{thm:dyadic-bracketing-namecert-obligations}. It identifies the regular rational readback as a handoff from those rows rather than as a hidden Cauchy quotient or selected limit.

The terminal row is therefore ordered after $R$. Componentwise transport and continuation replay preserve this order, and the provenance and local naming rows do not add any alternate Real-facing coordinate.
\end{proof}

\falsifiablePrediction{Every accepted $\DyadicBracketingUp$ Real-facing read factors through lower and upper $\DyadicRatCoreUp$ endpoint rows, a finite gap ledger, a $\StreamNameUp$ window, a $\LocatedRealIntervalUp$ package, a $\RegSeqRatUp$ readback, a $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{dyadicratcore, streamname, regseqrat, locatedrealinterval, real: $\DyadicBracketingUp$ is not bijective to any listed sibling because it binds two dyadic endpoints, one finite gap ledger, a stream window, located interval packaging, regular readback, Real seal, transport, replay, provenance, and local naming as one bracket certificate.}

\closureat{DyadicBracketingUp}{seedStr}

\begin{closurestatus}{\DyadicBracketingUp}
  \constructivestory{A $\DyadicBracketingUp$ packet is generated from lower and upper DyadicRatCore endpoint rows, a finite gap and tolerance ledger, StreamName windows, LocatedRealInterval packaging, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-bracketing-carrier}, \autoref{thm:dyadic-bracketing-namecert-obligations}, \autoref{thm:dyadic-bracketing-finite-bracket-induction}, and \autoref{thm:dyadic-bracketing-l10-real-name-handoff} seed the finite dyadic bracketing route feeding L10 Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, selected completed limits, arbitrary real trichotomy, countable choice, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked endpoint exposure, dyadic gap compatibility, stream-window readback, located-interval packaging, RegSeqRat handoff, RealUp seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.DyadicBracketingUp.}
\end{closurestatus}
