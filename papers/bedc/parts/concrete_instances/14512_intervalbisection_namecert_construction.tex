\chapter{A Concrete Naming Certificate for $\IntervalBisectionUp$}
\label{ch:concrete-instances-intervalbisection-namecert}
\origin{human}

Following Bishop--Bridges constructive interval bisection, the $\IntervalBisectionUp$ packet records the explicit split of a displayed located interval into left and right located subintervals through a finite midpoint route. It consumes the endpoint-cell surface of \autoref{ch:concrete-instances-locatedinterval-namecert}, the dyadic-cell surface of \autoref{ch:concrete-instances-dyadicinterval-namecert}, the rational-cell surface of \autoref{ch:concrete-instances-rationalinterval-namecert}, the finite-window interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readbacks of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, and the finite-net consumer boundary of \autoref{ch:concrete-instances-closed-interval-compact-net-namecert}. The packet is an explicit bisection certificate for one located interval; it is not open-cover compactness, trichotomy, quotient real equality, or a choice principle for selecting a branch.

\begin{definition}[IntervalBisection carrier]
\label{def:interval-bisection-carrier}
An $\IntervalBisectionUp$ carrier is a finite $\BHist$ packet
$$
K=(I,M,L,R,D,S,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the parent $\LocatedIntervalUp$ row, including its lower and upper endpoint observations and locatedness ledger;
\item $M$ is the midpoint route, computed through the displayed $\RationalIntervalUp$ endpoint cells and recorded as a midpoint row before any branch is inspected;
\item $L$ is the left subinterval row with inherited lower endpoint, midpoint upper endpoint, and its local locatedness witness;
\item $R$ is the right subinterval row with midpoint lower endpoint, inherited upper endpoint, and its local locatedness witness;
\item $D$ is the $\DyadicIntervalUp$ refinement ledger aligning the parent cell, midpoint cell, and the two child cells at the requested mesh scale;
\item $S$ is the $\StreamNameUp$ finite-window row for the parent endpoints, midpoint read, and child endpoint observations;
\item $Q$ is the $\RegSeqRatUp$ readback row for the same finite windows, including the rational midpoint calculation and the endpoint reuse checks;
\item $E$ is the pair of $\RealUp$ seal rows reached by the left and right child intervals through $S,Q,D$ and through no detached quotient-real equality coordinate;
\item $H$ records componentwise $\hsame$ transport for the parent interval, midpoint route, left and right subinterval rows, dyadic ledger, windows, readbacks, real seals, and branch handoff;
\item $C$ records displayed $\Cont$ routes by which consumers replay the parent row, midpoint route, both child rows, the dyadic ledger, finite windows, readbacks, real seals, and the nested-cell handoff;
\item $P$ is the $\Pkg$ provenance over $\IntervalBisectionUp$, $\LocatedIntervalUp$, $\DyadicIntervalUp$, $\RationalIntervalUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\ClosedIntervalCompactNetUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\IntervalBisectionUp}$ row.
\end{enumerate}
The carrier exports only the displayed parent-to-midpoint-to-children route. It has no coordinate for an arbitrary open cover, selected subcover, trichotomy on real names, quotient real equality, hidden endpoint choice, or ambient compactness theorem.
\end{definition}
\leanchecked{BEDC.Derived.IntervalBisectionUp.IntervalBisectionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[IntervalBisection NameCert obligations]
\label{thm:interval-bisection-namecert-obligations}
For an accepted $\IntervalBisectionUp$ carrier $K=(I,M,L,R,D,S,Q,E,H,C,P,N)$, the local $\NameCert_{\IntervalBisectionUp}$ surface consists exactly of carrier admission for those twelve rows, midpoint formation from the displayed parent endpoint cells, left and right located-subinterval admission, dyadic refinement compatibility, finite-window readback compatibility, child real-seal exposure, componentwise classifier transport, consumer replay, package provenance, and local naming.
\end{theorem}
\leanchecked{BEDC.Derived.IntervalBisectionUp.IntervalBisectionNamecertObligations}
\begin{proof}
Carrier admission is the tuple of \autoref{def:interval-bisection-carrier}. The parent interval, midpoint route, two child intervals, dyadic ledger, finite windows, regular readbacks, real seals, transports, continuations, package provenance, and local naming row are all explicit $\BHist$ coordinates.

The midpoint obligation is read through $I,M,Q$. The parent row supplies the displayed endpoint cells, the midpoint route records the rational midpoint before branch inspection, and the regular readback row checks that the midpoint is read from the same finite endpoint windows.

The child-interval obligations are read through $L,R,D,S,Q,E$. The left row reuses the lower endpoint of $I$ and the midpoint upper endpoint; the right row reuses the midpoint lower endpoint and the upper endpoint of $I$. The dyadic ledger, finite windows, readbacks, and real seals expose that both child rows remain inside the same displayed completion route.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier has no coordinate for branch choice, trichotomy, quotient real equality, selected subcovers, or ambient compactness, none of those objects can become local NameCert obligations.
\end{proof}
\leanchecked{BEDC.Derived.IntervalBisectionUp.IntervalBisectionCarrier\_namecert\_obligations}

\begin{theorem}[IntervalBisection endpoint transport]
\label{thm:interval-bisection-endpoint-transport}
Every endpoint-facing consumer of an accepted $\IntervalBisectionUp$ packet receives the displayed transport route
$$
I,M,L,R,D,S,Q,E,H,C,P,N.
$$
The route preserves the parent lower endpoint into the left child, the parent upper endpoint into the right child, and the midpoint endpoint into both children by componentwise $\hsame$ transport over the displayed $\BHist$ rows.
\end{theorem}
\leanchecked{BEDC.Derived.IntervalBisectionUp.IntervalBisectionEndpointTransport}
\begin{proof}
Begin with the parent row $I$ and midpoint route $M$. These two rows determine the three endpoint positions that can be exported: the parent lower endpoint, the midpoint endpoint, and the parent upper endpoint.

Project the child rows $L$ and $R$. The left child has the inherited lower endpoint and the midpoint upper endpoint; the right child has the midpoint lower endpoint and the inherited upper endpoint. No further endpoint selector appears in the carrier.

Now read $D,S,Q,E$. The dyadic ledger aligns the parent and child cells, the finite windows expose only the required endpoint reads, the regular readbacks check the same midpoint calculation, and the real seals close the two child endpoint routes.

Finally the structural rows $H,C,P,N$ transport, replay, source, and name this endpoint route. Componentwise $\hsame$ is therefore the only endpoint-transport mechanism available to a consumer.
\end{proof}

\begin{theorem}[IntervalBisection nested-cell handoff]
\label{thm:interval-bisection-nested-cell-handoff}
Every nested-interval or finite-net consumer of an accepted $\IntervalBisectionUp$ packet factors its child-cell request through the displayed route
$$
I,M,L,R,D,H,C,P,N
$$
before it may read $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, or $\ClosedIntervalCompactNetUp$ data downstream. Thus the packet hands off a pair of finite child cells, not a selected infinite branch or an open-cover compactness argument.
\end{theorem}
\leanchecked{BEDC.Derived.IntervalBisectionUp.IntervalBisectionCarrier\_nested\_cell\_handoff}
\begin{proof}
First read the parent interval and midpoint route. The row $I$ fixes the displayed located interval, and $M$ fixes the midpoint row used by both children.

Next use the child rows and dyadic ledger. The rows $L$ and $R$ are the only child-cell exports, while $D$ records their refinement relation to the parent cell and midpoint cell.

The handoff to downstream consumers is then replayed through $H,C,P,N$. A nested-interval consumer may iterate the same finite packet shape, and a $\ClosedIntervalCompactNetUp$ consumer may read the two displayed child cells as finite mesh data, but neither consumer receives an infinite branch selector or an open-cover subcover selector from this packet.

The stream, regular-rational, and real-seal rows $S,Q,E$ remain available only as finite readbacks for the already displayed cells. They cannot introduce quotient real equality or a hidden choice of a child branch because those coordinates are absent from the carrier.
\end{proof}

\begin{theorem}[IntervalBisection obligation carrier exposure]
\label{thm:interval-bisection-obligation-carrier}
Every accepted $\IntervalBisectionUp$ packet exposes its root-unblock carrier rows before any located interval, dyadic interval, rational interval, StreamName, RegSeqRat, Real, or closed-interval compact-net consumer reads the bisection. The exposed rows are exactly the parent $\LocatedIntervalUp$ row $I$, midpoint route $M$, left and right child rows $L,R$, $\DyadicIntervalUp$ refinement ledger $D$, $\StreamNameUp$ finite-window row $S$, $\RegSeqRatUp$ readback row $Q$, $\RealUp$ child seals $E$, componentwise transport $H$, replay $C$, provenance $P$, and local naming $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:interval-bisection-carrier}. Its displayed tuple is $K=(I,M,L,R,D,S,Q,E,H,C,P,N)$, so the only carrier data available to a consumer are precisely those rows.

The interval-facing rows are $I,M,L,R,D$. They expose the located parent cell, the rational midpoint route, both child cells, and the dyadic refinement ledger before any downstream readback is opened.

The readback-facing rows are $S,Q,E$. They expose finite StreamName windows, regular rational readback, and the two Real child seals only after the bisection cells have been displayed.

The structural rows $H,C,P,N$ preserve and name the same packet. Since no additional carrier coordinate exists, every listed consumer reads the bisection through the displayed root-unblock surface.
\end{proof}
\leanchecked{BEDC.Derived.IntervalBisectionUp.IntervalBisectionObligationCarrier}

\begin{theorem}[IntervalBisection obligation transport]
\label{thm:interval-bisection-obligation-transport}
Let $K=(I,M,L,R,D,S,Q,E,H,C,P,N)$ and $K'=(I',M',L',R',D',S',Q',E',H',C',P',N')$ be accepted $\IntervalBisectionUp$ packets. If the located interval, midpoint, child interval, dyadic ledger, finite-window, readback, and Real-seal rows transport componentwise under the displayed $\hsame$ rows and replay through the displayed $\Cont$ routes, then every located interval, dyadic interval, rational interval, StreamName, RegSeqRat, Real, and $\ClosedIntervalCompactNetUp$ consumer receives the same bisection obligation surface from $K$ and $K'$.
\end{theorem}
\begin{proof}
The componentwise hypotheses identify the interval rows $I,M,L,R,D$ with $I',M',L',R',D'$ through the transport rows $H,H'$. Thus the parent cell, midpoint route, two child cells, and dyadic refinement ledger determine the same finite bisection shape.

The readback rows transport in the same way. The finite windows $S,S'$, regular readbacks $Q,Q'$, and Real seals $E,E'$ are replayed through $C,C'$, so no consumer can observe a second bisection route outside the transported rows.

Finally $P,N$ and $P',N'$ source and name the transported packet. The consumer list is exhausted by those interval, readback, and structural rows, so the transported packets expose the same obligation surface.
\end{proof}

\begin{theorem}[IntervalBisection obligation ledger exhaustion]
\label{thm:interval-bisection-obligation-ledger}
For every accepted $\IntervalBisectionUp$ packet, the root-unblock ledger for located interval, dyadic interval, rational interval, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\ClosedIntervalCompactNetUp$ consumers is exhausted by the rows
$$
I,M,L,R,D,S,Q,E,H,C,P,N.
$$
The ledger contains no row for selected infinite branch choice, open-cover compactness, quotient real equality, host endpoint equality, trichotomy, or an ambient compactness theorem.
\end{theorem}
\begin{proof}
Use the carrier exposure theorem to list the only rows in the packet. The located, rational, and dyadic interval consumers read $I,M,L,R,D$; the StreamName, RegSeqRat, and Real consumers read $S,Q,E$ after those interval rows have been displayed.

Use the transport theorem for stability. Any replayed or transported bisection obligation is still read through $H,C,P,N$ together with the same interval and readback rows, so the ledger has no second channel.

The excluded objects are absent from the carrier and from the NameCert obligations of \autoref{thm:interval-bisection-namecert-obligations}. Therefore the listed tuple exhausts the root-unblock ledger for all named consumers.
\end{proof}

\begin{theorem}[IntervalBisection midpoint formation]
\label{thm:interval-bisection-midpoint-formation}
For every accepted $\IntervalBisectionUp$ packet $K=(I,M,L,R,D,S,Q,E,H,C,P,N)$, the midpoint row $M$ is formed only from the displayed $\LocatedIntervalUp$ parent endpoint rows and the $\RationalIntervalUp$ endpoint cells read through $I$ and $Q$. No midpoint obligation may inspect a child branch, a Real equality row, or a compactness consumer before $M$ has been displayed.
\end{theorem}
\begin{proof}
Project $I$ and $Q$ from the carrier. The parent interval row supplies the lower and upper endpoint observations, while the regular rational readback records the rational endpoint cells visible to the bisection packet.

The midpoint row $M$ is placed between the parent row and the child rows in \autoref{def:interval-bisection-carrier}. Hence it is available before $L,R,D,S,Q,E$ are used as child-facing data, and its inputs are exactly the displayed endpoint cells.

Since the carrier has no separate midpoint selector, Real equality coordinate, or compactness oracle, the midpoint obligation is exhausted by the located parent endpoint rows and the rational endpoint-cell calculation.
\end{proof}

\begin{theorem}[IntervalBisection child-cell admission]
\label{thm:interval-bisection-child-cell-admission}
For every accepted $\IntervalBisectionUp$ packet $K=(I,M,L,R,D,S,Q,E,H,C,P,N)$, the left and right child cells $L,R$ are admitted through the displayed $\DyadicIntervalUp$ refinement ledger $D$, the finite $\StreamNameUp$ windows $S$, the $\RegSeqRatUp$ readback row $Q$, and the terminal $\RealUp$ child seals $E$. The packet admits exactly the inherited-left and inherited-right child cells; it does not admit a selected infinite branch or a quotient-real endpoint.
\end{theorem}
\begin{proof}
Read the child rows $L,R$ together with the dyadic ledger $D$. These rows display the inherited parent endpoints, the midpoint endpoint, and the refinement relation between parent and child cells.

Next read $S,Q,E$. The finite windows expose only the endpoint observations needed by the displayed cells, the regular rational row checks the midpoint calculation and endpoint reuse, and the Real seals close the two child routes after those finite rows are visible.

The admitted children are therefore the two displayed cells named by $L,R$. No carrier coordinate selects one child as an infinite branch, and no quotient-real equality row is present for replacing the displayed endpoint route.
\end{proof}

\begin{theorem}[IntervalBisection finite-net handoff]
\label{thm:interval-bisection-finite-net-handoff}
Every $\ClosedIntervalCompactNetUp$ consumer of an accepted $\IntervalBisectionUp$ packet reads the bisection through the finite rows
$$
I,M,L,R,D,S,Q,E,H,C,P,N.
$$
The handoff supplies parent and child mesh cells with finite readback and Real seals, but it supplies no selected branch, no selected subcover, and no open-cover compactness theorem.
\end{theorem}
\begin{proof}
Start with the ledger exhaustion theorem. It lists the only rows available to located-interval, dyadic-interval, StreamName, RegSeqRat, Real, and closed-interval compact-net consumers.

A finite-net consumer first sees the parent interval, midpoint, child cells, and dyadic refinement rows $I,M,L,R,D$. These rows supply a finite mesh refinement rather than an infinite nested branch.

The readback rows $S,Q,E$ then expose the finite observation windows, rational endpoint checks, and Real child seals. Transport, replay, provenance, and local naming through $H,C,P,N$ preserve the same finite handoff without adding an open-cover or selected-subcover coordinate.
\end{proof}

\begin{theorem}[IntervalBisection obligation closure package]
\label{thm:interval-bisection-obligation-closure-package}
The carrier exposure, obligation transport, obligation ledger, midpoint formation, child-cell admission, and finite-net handoff theorems form the obligation-closure package for $\IntervalBisectionUp$. Every root-unblock consumer named in this chapter receives exactly the displayed bisection route, and every excluded object remains outside the carrier.
\end{theorem}
\begin{proof}
The carrier exposure theorem fixes the visible tuple $K=(I,M,L,R,D,S,Q,E,H,C,P,N)$, while the obligation transport theorem shows that componentwise $\hsame$ and displayed $\Cont$ replay preserve that tuple for all named consumers.

The obligation ledger theorem exhausts the root-unblock rows. The midpoint formation theorem binds $M$ to the displayed located and rational endpoint rows, and the child-cell admission theorem binds $L,R$ to the dyadic, StreamName, RegSeqRat, and Real rows.

Finally the finite-net handoff theorem routes $\ClosedIntervalCompactNetUp$ consumers through the same finite mesh data. Together these clauses close the obligation surface without adding branch selection, selected subcovers, open-cover compactness, quotient real equality, trichotomy, or ambient compactness.
\end{proof}

\closureat{IntervalBisectionUp}{scopedStr}

\begin{closurestatus}{\IntervalBisectionUp}
  \constructivestory{An $\IntervalBisectionUp$ packet is generated from a displayed LocatedIntervalUp parent row, a RationalIntervalUp midpoint route, left and right located subinterval rows, a DyadicIntervalUp refinement ledger, StreamNameUp finite windows, RegSeqRatUp readbacks, RealUp child seals, componentwise hsame transport, displayed Cont replay, package provenance, and a local NameCert row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:interval-bisection-carrier}, \autoref{thm:interval-bisection-namecert-obligations}, \autoref{thm:interval-bisection-endpoint-transport}, \autoref{thm:interval-bisection-nested-cell-handoff}, \autoref{thm:interval-bisection-obligation-carrier}, \autoref{thm:interval-bisection-obligation-transport}, \autoref{thm:interval-bisection-obligation-ledger}, \autoref{thm:interval-bisection-midpoint-formation}, \autoref{thm:interval-bisection-child-cell-admission}, \autoref{thm:interval-bisection-finite-net-handoff}, and \autoref{thm:interval-bisection-obligation-closure-package} bind exactly the carrier, endpoint transport, nested-cell handoff, obligation carrier, hsame transport, Cont replay, Pkg provenance, and local NameCert rows for displayed located intervals and finite-net consumers.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.IntervalBisectionUp.IntervalBisectionCarrier\_namecert\_obligations}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, trichotomy, quotient real equality, selected branch choice, selected subcover choice, or bridge checking.}
  \upgradepath{Scoped closure requires a public consumer theorem that every accepted bisection handoff is stable under repeated finite-net refinement for BEDC.Derived.IntervalBisectionUp.}
\end{closurestatus}
