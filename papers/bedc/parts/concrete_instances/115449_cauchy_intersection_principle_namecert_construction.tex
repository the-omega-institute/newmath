\chapter{A Concrete Naming Certificate for $\CauchyIntersectionPrincipleUp$}
\label{ch:concrete-instances-cauchy-intersection-principle-namecert}
\origin{human}

Following Bishop and Bridges on nested intervals, regular Cauchy reals, and constructive real approximation, the $\CauchyIntersectionPrincipleUp$ packet records the finite intersection route by which a displayed family of nested dyadic windows reaches a Real-facing seal. It sits after the dyadic tolerance surface of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the Cauchy source discipline of \autoref{ch:concrete-instances-cauchy-namecert}, and the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite common-window intersection principle for Real and Completion consumers; it does not select a completed limit, quotient Cauchy names, invoke countable choice, decide trichotomy, or assert ambient $\RealUp$ completeness.

\begin{definition}[Cauchy intersection principle carrier]
\label{def:cauchy-intersection-principle-carrier}
A $\CauchyIntersectionPrincipleUp$ carrier is a finite $\BHist$ packet
$$
K=(D,M,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the nested dyadic window row, recording finite left and right endpoint data over $\DyadicRatCoreUp$ together with the displayed inclusion checks between successive windows.
\item $M$ is the $\CauchyModulusUp$ row, recording the finite precision requests under which the displayed windows are certified Cauchy.
\item $S$ is the $\StreamNameUp$ finite-observation row exposing exactly the inspected window stages and no unseen tail.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the same finite observations and dyadic tolerance ledger.
\item $E$ is the terminal $\RealUp$ seal row, available only after $D,M,S,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the dyadic-window, modulus, stream-observation, regular-readback, Real-seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an intersection request through $D,M,S,R,E,H$ and back to the structural rows.
\item $P$ is the $\Pkg$ provenance over $\CauchyIntersectionPrincipleUp$, $\DyadicRatCoreUp$, $\CauchyModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyIntersectionPrincipleUp}$ row.
\end{enumerate}
The carrier exports only this finite nested-window packet. It has no coordinate for selected limits, quotient equality of Cauchy names, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.CauchyIntersectionPrincipleUp.CauchyIntersectionPrincipleTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Cauchy intersection principle NameCert obligations]
\label{thm:cauchy-intersection-principle-namecert-obligations}
For every accepted $\CauchyIntersectionPrincipleUp$ carrier $K=(D,M,S,R,E,H,C,P,N)$, the local $\NameCert_{\CauchyIntersectionPrincipleUp}$ surface consists exactly of carrier admission through the displayed nested dyadic windows, Cauchy modulus requests, StreamName finite observations, RegSeqRat readback, Real-seal nonescape, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-intersection-principle-carrier}. The rows $D,M,S,R,E,H,C,P,N$ are the complete public packet, so carrier admission can only admit data already displayed in those rows.

The analytic route first reads $D$ and $M$. Thus every intersection-facing consumer sees the nested dyadic windows and the finite Cauchy modulus requests before any stream window, regular readback, or Real seal is available.

Next read $S$ and $R$. The stream row names the inspected finite stages, while the regular rational row supplies readback from the same finite observations and dyadic tolerance ledger.

Finally use $E,H,C,P,N$. The Real seal is terminal in this packet, and the transport, replay, provenance, and local naming rows preserve the same displayed route. Since no carrier row stores selected limits, quotient equality, countable choice, trichotomy, host real equality, or ambient completeness, none of those principles can be exported by the local certificate.
\end{proof}

\begin{theorem}[Cauchy intersection window extraction]
\label{thm:cauchy-intersection-principle-window-extraction}
Every completion-facing read of an accepted $\CauchyIntersectionPrincipleUp$ carrier factors through
$$
\DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
using the finite rows $D,M,S,R,E,H,C,P,N$. The consumer receives a displayed nested-window intersection route and a terminal Real seal; it receives no selected limit, quotient equality of Cauchy names, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Apply the obligation theorem. It forces every completion-facing consumer to expose the dyadic window row and the Cauchy modulus row before the inspected stream stages can be read.

The sibling route then supplies the finite readback. DyadicRatCore supplies the endpoint and tolerance rows, StreamName supplies the inspected window stages, RegSeqRat supplies the regular rational readback, and the Real chapter supplies only the terminal seal.

The structural rows do not enlarge the route. Componentwise $\hsame$ transport and displayed $\Cont$ replay preserve the same packet, while $P$ and $N$ source and name it. Therefore the extracted window remains a finite BEDC packet rather than a selected completed limit or an ambient completeness theorem.
\end{proof}

\falsifiablePrediction{Every accepted CauchyIntersectionPrincipleUp consumer exposes nested DyadicRatCore windows, a finite CauchyModulus schedule, StreamName observations, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows before it can cite the intersection route.}

\independenceWitness{dyadicratcore, streamname, regseqrat, cauchy, real: removing any one of these sibling routes leaves the packet without the corresponding dyadic window, finite observation, regular readback, Cauchy modulus discipline, or terminal Real seal row.}

\closureat{CauchyIntersectionPrincipleUp}{seedStr}

\begin{closurestatus}{\CauchyIntersectionPrincipleUp}
  \constructivestory{A $\CauchyIntersectionPrincipleUp$ packet is generated from nested DyadicRatCore windows, a finite CauchyModulus schedule, StreamName observations, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert evidence.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-intersection-principle-carrier}, \autoref{thm:cauchy-intersection-principle-namecert-obligations}, and \autoref{thm:cauchy-intersection-principle-window-extraction} seed the finite nested-window intersection route from dyadic tolerance data to a Real-facing seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected limits, quotient equality of Cauchy names, countable choice, trichotomy, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, nested-window exactness, Cauchy modulus discipline, finite-observation readback, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CauchyIntersectionPrincipleUp.}
\end{closurestatus}
