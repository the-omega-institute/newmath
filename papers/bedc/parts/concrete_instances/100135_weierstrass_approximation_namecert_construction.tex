\chapter{A Concrete Naming Certificate for $\WeierstrassApproximationUp$}
\label{ch:concrete-instances-weierstrass-approximation-namecert}
\origin{human}

Following Weierstrass' approximation theorem in the Bishop--Bridges constructive reading, the $\WeierstrassApproximationUp$ packet records a finite polynomial-approximation surface for a continuous real-valued map on a compact real interval. It consumes the real-name endpoint surface of \autoref{ch:concrete-instances-real-namecert}, the interval-window rows of \autoref{ch:concrete-instances-real-interval-enclosure-namecert}, the compact-uniform-continuity route of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, the polynomial code surface of \autoref{ch:concrete-instances-polynomial-namecert}, the endpoint handoff discipline of \autoref{ch:concrete-instances-uniformlimit-namecert}, and the finite tagged-sum mesh rows of \autoref{ch:concrete-instances-riemannsum-namecert}. The packet names the inspected approximation ledger only; it is not an open-cover compactness theorem, a Stone--Weierstrass algebra theorem, a quotient function-space construction, a host supremum norm, a selector for infinite data, or a completeness principle for ambient $\RealUp$.

\begin{definition}[Weierstrass approximation carrier]
\label{def:weierstrass-approximation-carrier}
A $\WeierstrassApproximationUp$ carrier is a finite $\BHist$ packet
$$
W=(I,F,\varepsilon,P,S,M,E,T,C,R,Q)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the compact real interval row, read through $\RealUp$ endpoints and the $\RealIntervalEnclosureUp$ interval-window service.
\item $F$ is the continuous-map row over the same interval source, exposing only finite real-name windows on inspected inputs.
\item $\varepsilon$ is the requested positive rational error row.
\item $P$ is the displayed $\PolynomialUp$ row whose coefficient ledger is finite and whose evaluation route is read through the same $\RealUp$ arithmetic surface as $F$.
\item $S$ is the finite interval-enclosure sample family, recording the inspected subintervals, sample points, and real-name windows used to compare $F$ with $P$.
\item $M$ is the compact-uniform-modulus row obtained from the $\CompactUniformContinuityUp$ route for $F$ on $I$.
\item $E$ is the uniform-error ledger saying that every displayed sample comparison is bounded by $\varepsilon$ after the modulus row has selected the required mesh scale.
\item $T$ stores componentwise $\hsame$ transport for the interval row, continuous-map row, error row, polynomial row, sample family, compact-uniform-modulus row, and uniform-error ledger.
\item $C$ stores the displayed $\Cont$ routes from interval enclosures to samples, from samples to function and polynomial evaluations, and from those evaluations to the uniform-error ledger.
\item $R$ is the $\Pkg$ provenance over $\WeierstrassApproximationUp$, $\RealUp$, $\RealIntervalEnclosureUp$, $\CompactUniformContinuityUp$, $\PolynomialUp$, $\UniformLimitUp$, $\RiemannSumUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $Q$ is the local $\NameCert_{\WeierstrassApproximationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows, their displayed transports, their continuation routes, provenance, and local naming row. It has no coordinate for open-cover compactness, Stone--Weierstrass generality, quotient equality of functions, a host supremum norm, selected infinite sample data, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Weierstrass approximation NameCert obligations]
\label{thm:weierstrass-approximation-namecert-obligations}
For every accepted $\WeierstrassApproximationUp$ carrier
$$
W=(I,F,\varepsilon,P,S,M,E,T,C,R,Q),
$$
the local $\NameCert_{\WeierstrassApproximationUp}$ surface consists of the following finite obligations:
\begin{enumerate}
\item carrier admission for the interval, continuous-map, error, polynomial, sample, modulus, uniform-error, transport, continuation, provenance, and local naming rows;
\item compact interval source compatibility, requiring every sample and endpoint read to pass through the same $\RealIntervalEnclosureUp$ route attached to $I$;
\item polynomial-row admissibility, requiring $P$ to be a displayed $\PolynomialUp$ code with finite coefficient and evaluation ledgers rather than a host function;
\item uniform-error exactness, requiring $E$ to bind the sample comparisons to the requested rational error row $\varepsilon$ after the compact-uniform-modulus row $M$ has fixed the mesh scale;
\item transport stability, requiring $T$ and $C$ to replay the same interval, sample, evaluation, and error rows under componentwise $\hsame$ movement;
\item ledger refusal, excluding open-cover compactness, Stone--Weierstrass algebra closure, quotient function spaces, host supremum norms, selected infinite data, ambient $\RealUp$ completeness, and any standard bridge beyond the displayed rows.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.WeierstrassApproximationUp.WeierstrassApproximationTasteGate\_single\_carrier\_alignment}
\begin{proof}
Carrier admission is the projection of \autoref{def:weierstrass-approximation-carrier}. The accepted packet exposes exactly the compact interval, continuous map, rational error, polynomial code, sample family, modulus, uniform-error ledger, transport, continuation, provenance, and local name rows.

Source compatibility starts with the interval row $I$. Every sample in $S$ is read from the same finite interval-enclosure service, so the function row $F$ and the polynomial row $P$ are evaluated on the same inspected real-name windows.

Polynomial admissibility is the row $P$. Its coefficient and evaluation ledgers are finite $\PolynomialUp$ data, and the carrier gives no second coordinate for a host function or a quotient of functions.

Uniform-error exactness is the route through $M$ and $E$. The compact-uniform-modulus row fixes the displayed mesh scale for the requested rational error, and $E$ records the finite comparisons between the inspected $F$ values and the inspected polynomial values.

Transport stability is componentwise. The rows in $T$ move the interval, map, error, polynomial, sample, modulus, and error ledgers, while $C$ replays the same comparison route after transport.

The refusal clauses follow from the inventory. No coordinate names an open cover, a Stone--Weierstrass algebra, a quotient function space, a host norm, an infinite selected sample family, ambient completeness, or a standard bridge.
\end{proof}

\begin{theorem}[Weierstrass approximation polynomial handoff]
\label{thm:weierstrass-approximation-polynomial-handoff}
Every downstream approximation consumer of an accepted $\WeierstrassApproximationUp$ carrier factors through the displayed route
$$
I,F,\varepsilon \longmapsto S,M \longmapsto P,E \longmapsto T,C,R,Q .
$$
Consequently the consumer reads only the $\RealIntervalEnclosureUp$, $\CompactUniformContinuityUp$, $\PolynomialUp$, $\UniformLimitUp$, $\RiemannSumUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows displayed by \autoref{def:weierstrass-approximation-carrier}.
\end{theorem}
\begin{proof}
Begin with the accepted carrier $W=(I,F,\varepsilon,P,S,M,E,T,C,R,Q)$. The interval and map rows determine the inspected source windows, and the requested error row fixes the comparison threshold.

The sample family $S$ is read through $\RealIntervalEnclosureUp$ and the compact-uniform-modulus row $M$ is read through $\CompactUniformContinuityUp$. These rows determine the finite mesh on which the consumer may compare the continuous-map windows with the polynomial windows.

The polynomial row $P$ supplies only the finite $\PolynomialUp$ coefficient and evaluation route. The error row $E$ then records the comparison between those polynomial evaluations and the corresponding function evaluations, with $\UniformLimitUp$ and $\RiemannSumUp$ appearing only as downstream finite-window and mesh consumers named in the provenance row.

Finally $T,C,R,Q$ transport, replay, source, and name the same route. Since the carrier has no other coordinate, every downstream approximation consumer factors through exactly these displayed BEDC rows and cannot read an open-cover proof, Stone--Weierstrass algebra closure, quotient function space, host supremum norm, selected infinite data, or ambient real completeness.
\end{proof}

\begin{theorem}[Weierstrass approximation UniformLimit consumer]
\label{thm:weierstrass-approximation-uniformlimit-consumer}
Every $\UniformLimitUp$-facing read of an accepted $\WeierstrassApproximationUp$ carrier first reads the compact interval row, continuous-map row, requested rational error, polynomial row, finite samples, compact-uniform modulus, and uniform-error ledger. The consumer boundary is therefore the route
$$
I,F,\varepsilon,P,S,M,E \longmapsto T,C,R,Q,
$$
with $\UniformLimitUp$ appearing only as the finite-window consumer named by the provenance row and by \autoref{ch:concrete-instances-uniformlimit-namecert}.
\end{theorem}
\begin{proof}
Start from the carrier of \autoref{def:weierstrass-approximation-carrier}. Its rows $I,F,\varepsilon,P,S,M,E$ are the only interval, map, error, polynomial, sample, modulus, and uniform-error data exposed before structural replay.

Apply the polynomial handoff of \autoref{thm:weierstrass-approximation-polynomial-handoff}. It forces every approximation consumer to pass through the same source-window, sample, modulus, polynomial, and error route before $T,C,R,Q$ can transport, replay, source, and name the packet.

The $\UniformLimitUp$ consumer boundary is then exactly the sibling chapter \autoref{ch:concrete-instances-uniformlimit-namecert}. Since the accepted carrier has no coordinate for a host supremum norm, quotient function space, selected infinite sample family, or ambient completeness principle, a uniform-limit-facing read receives only the displayed finite approximation ledger.
\end{proof}

\closureat{WeierstrassApproximationUp}{seedStr}

\begin{closurestatus}{\WeierstrassApproximationUp}
  \constructivestory{A $\WeierstrassApproximationUp$ packet is generated from a compact real interval row, continuous-map row, requested rational error row, displayed polynomial row, finite interval-enclosure samples, compact-uniform-modulus row, uniform-error ledger, componentwise transport, continuation replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:weierstrass-approximation-carrier}, \autoref{thm:weierstrass-approximation-namecert-obligations}, and \autoref{thm:weierstrass-approximation-polynomial-handoff} seed the finite polynomial-approximation surface over compact real intervals and routed continuous-map windows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, Stone--Weierstrass generality, quotient function spaces, host supremum norms, selected infinite data, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact interval source compatibility, polynomial-row admissibility, uniform-error exactness, transport stability, ledger refusal, and polynomial handoff proofs for BEDC.Derived.WeierstrassApproximationUp.}
\end{closurestatus}
