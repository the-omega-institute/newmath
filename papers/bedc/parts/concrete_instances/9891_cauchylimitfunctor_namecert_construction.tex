\chapter{A Concrete Naming Certificate for $\CauchyLimitFunctorUp$}
\label{ch:concrete-instances-cauchy-limit-functor-namecert}
\origin{human}

The $\CauchyLimitFunctorUp$ packet names the functorial transport surface for Cauchy limit seals in the Real and Completion route of \autoref{ch:visions-real-completeness}. It sits after the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real seal of \autoref{ch:concrete-instances-real-namecert}. It also consumes the existing limit-seal boundary of \autoref{ch:concrete-instances-cauchylimitseal-namecert} and the completion-seal classifier of \autoref{ch:concrete-instances-real-completion-seal-classifier-namecert}. The packet is a routed transport object: it carries a finite source seal, a finite target seal, and the displayed map between their regular Cauchy data without adding a host-level completion theorem.

\falsifiablePrediction{If a completion consumer accepts a transported Cauchy limit seal while changing the source stream window, target stream window, regular-rational readback, dyadic tolerance ledger, or RealUp endpoint row outside the displayed transport packet, it refutes the $\CauchyLimitFunctorUp$ classifier.}

\independenceWitness{cauchylimitseal, realcompletionsealclassifier, streamname, regseqrat, dyadicratcore, real: no listed sibling is bijective to $\CauchyLimitFunctorUp$ because the functor packet carries source and target limit-seal rows together with the finite transport map, classifier row, dyadic tolerance ledger, regular-rational readback, RealUp endpoint, componentwise transports, continuation replay, provenance, and local NameCert row.}

\begin{definition}[Cauchy limit functor carrier]
\label{def:cauchy-limit-functor-carrier}
A $\CauchyLimitFunctorUp$ carrier is a finite $\BHist$ packet
$$
F=(S,T,M,W_0,W_1,R,D,K,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source $\CauchyLimitSealUp$ row whose limit-seal route is being transported;
\item $T$ is the target $\CauchyLimitSealUp$ row reached after transport;
\item $M$ is the finite transport-map row from the source window and readback data to the target window and readback data;
\item $W_0$ is the source $\StreamNameUp$ window row read by $S$;
\item $W_1$ is the target $\StreamNameUp$ window row read by $T$;
\item $R$ is the $\RegSeqRatUp$ readback row preserved by the displayed transport;
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger controlling the transported precision request;
\item $K$ is the $\RealCompletionSealClassifierUp$ row that recognizes the transported seal boundary;
\item $E$ is the terminal $\RealUp$ endpoint row consumed after the target seal is displayed;
\item $H$ records componentwise $\hsame$ transports for the two limit seals, map row, stream windows, regular-rational readback, dyadic ledger, classifier, endpoint, provenance, and local naming rows;
\item $C$ records the displayed $\Cont$ replay route from $S$ through $M,W_0,W_1,R,D,K$ to $T$ and then to $E$;
\item $P$ is the $\Pkg$ provenance over \autoref{ch:concrete-instances-cauchylimitseal-namecert}, \autoref{ch:concrete-instances-real-completion-seal-classifier-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-real-namecert}, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\CauchyLimitFunctorUp}$ row.
\end{enumerate}
The carrier exports no quotient stream equality, selected host limit, ambient completeness principle, countable-choice selector, detached Real value, or second transport route beyond the displayed packet.
\end{definition}

\begin{definition}[Cauchy limit functor classifier]
\label{def:cauchy-limit-functor-classifier}
The $\CauchyLimitFunctorUp$ classifier compares two carriers only by the displayed rows
$$
S,T,M,W_0,W_1,R,D,K,E,H,C,P,N.
$$
Two packets are classifier-equal exactly when the source and target limit-seal rows agree, the transport-map row agrees, the source and target $\StreamNameUp$ windows agree, the $\RegSeqRatUp$ readback and $\DyadicRatCoreUp$ tolerance ledger agree, and the classifier, endpoint, transport, continuation, provenance, and local naming rows agree componentwise. The classifier has no coordinate for quotient equality of Cauchy names, host real equality, an untracked selector, a detached limit object, or an ambient completeness theorem.
\end{definition}

\begin{theorem}[Cauchy limit functor NameCert obligations]
\label{thm:cauchy-limit-functor-namecert-obligations}
For the carrier of \autoref{def:cauchy-limit-functor-carrier}, the local $\NameCert_{\CauchyLimitFunctorUp}$ surface consists exactly of the following five obligation rows:
\begin{enumerate}
\item carrier exposure for $F=(S,T,M,W_0,W_1,R,D,K,E,H,C,P,N)$;
\item source-to-target seal exposure through the ordered route $S \to M \to T$;
\item finite observation exposure through $W_0,W_1,R,D$;
\item completion-consumer exposure through $K$ and $E$;
\item structural exposure through componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local naming $N$.
\end{enumerate}
No $\CauchyLimitFunctorUp$ consumer can read the transported limit seal without preserving all five rows.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyLimitFunctorUp.CauchyLimitFunctorCarrier\_namecert\_obligations}
\begin{proof}
Project the carrier of \autoref{def:cauchy-limit-functor-carrier}. Its displayed packet is exactly $F=(S,T,M,W_0,W_1,R,D,K,E,H,C,P,N)$, so the carrier row supplies the finite object being named.

Read the seal route next. The source seal $S$ is transported only through the finite map row $M$, and the target seal $T$ is reached only after that row is displayed. Thus the seal exposure is the ordered route $S \to M \to T$.

Now inspect the observation rows. The source and target windows $W_0,W_1$, the regular-rational readback $R$, and the dyadic tolerance ledger $D$ are the finite data over which the transport map is read. The completion-facing rows $K,E$ then recognize the transported seal boundary and deliver the terminal $\RealUp$ endpoint.

Finally the rows $H,C,P,N$ transport, replay, record provenance for, and name the same packet. Since the carrier and classifier contain no further coordinates, a consumer that bypasses any one of the five obligation rows leaves the $\CauchyLimitFunctorUp$ surface.
\end{proof}

\begin{theorem}[Cauchy limit functor source-target seal exposure]
\label{thm:cauchy-limit-functor-source-target-seal-exposure}
For every accepted $\CauchyLimitFunctorUp$ packet with carrier
$$
F=(S,T,M,W_0,W_1,R,D,K,E,H,C,P,N),
$$
the source seal is exposed to consumers only through the finite route $S \to M \to T$. In particular, a consumer can cite $S$ and $T$ as the displayed source and target $\CauchyLimitSealUp$ rows, and can cite $M$ as the finite transport-map row between them, but receives no detached source-target equality, host-level limit comparison, quotient stream equality, or second seal route outside the classifier of \autoref{def:cauchy-limit-functor-classifier}.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyLimitFunctorUp.CauchyLimitFunctorCarrier\_source\_target\_seal\_exposure}
\begin{proof}
Use the carrier of \autoref{def:cauchy-limit-functor-carrier}. Its first three displayed rows are exactly the source seal $S$, the target seal $T$, and the transport-map row $M$.

Apply the classifier of \autoref{def:cauchy-limit-functor-classifier}. It compares the seal data through the listed coordinates $S,T,M,W_0,W_1,R,D,K,E,H,C,P,N$ and has no coordinate for a detached source-target equality or an alternate route.

Finally apply the NameCert obligation surface of \autoref{thm:cauchy-limit-functor-namecert-obligations}. The second obligation row is precisely source-to-target seal exposure through $S \to M \to T$, so any accepted packet exposes the seal only by that displayed finite route.
\end{proof}

\begin{theorem}[Cauchy limit functor finite observation exposure]
\label{thm:cauchy-limit-functor-finite-observation-exposure}
For every accepted $\CauchyLimitFunctorUp$ packet with carrier
$$
F=(S,T,M,W_0,W_1,R,D,K,E,H,C,P,N),
$$
the observation side available to a consumer is exactly
$$
W_0,W_1,R,D,K,E.
$$
The consumer may inspect the two stream windows, the regular-rational readback, the dyadic tolerance ledger, the completion-seal classifier row, and the terminal $\RealUp$ endpoint row. It receives no quotient stream equality, selected limit, countable-choice selector, host real equality, detached completion object, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-limit-functor-carrier}. The observation rows are the source and target windows $W_0,W_1$, the readback row $R$, the tolerance ledger $D$, the completion classifier $K$, and the endpoint row $E$.

Use the classifier of \autoref{def:cauchy-limit-functor-classifier}. It allows comparison only along the displayed carrier coordinates, so there is no classifier coordinate for quotient stream equality, a selected limit, countable choice, host real equality, or an ambient completion theorem.

The NameCert obligations of \autoref{thm:cauchy-limit-functor-namecert-obligations} split the observation surface into finite observation exposure through $W_0,W_1,R,D$ and completion-consumer exposure through $K,E$. These two rows give exactly the displayed observation side and nothing outside it.
\end{proof}

\begin{theorem}[Cauchy limit functor structural transport]
\label{thm:cauchy-limit-functor-structural-transport}
For every accepted $\CauchyLimitFunctorUp$ packet with carrier
$$
F=(S,T,M,W_0,W_1,R,D,K,E,H,C,P,N),
$$
the only structural transport, continuation replay, provenance, and local naming rows available to consumers are $H,C,P,N$. Thus all componentwise $\hsame$ transport is carried by $H$, all displayed replay is carried by $C$, all dependency provenance is carried by $P$, and the local certificate name is carried by $N$.
\end{theorem}
\begin{proof}
Start from the carrier of \autoref{def:cauchy-limit-functor-carrier}. Its final four rows are $H,C,P,N$, respectively recording componentwise $\hsame$ transports, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\CauchyLimitFunctorUp}$ row.

Read those rows through the classifier of \autoref{def:cauchy-limit-functor-classifier}. Since the classifier compares only the displayed coordinates, it cannot supply any additional transport object, replay path, provenance row, or local name beyond $H,C,P,N$.

Apply the fifth obligation row of \autoref{thm:cauchy-limit-functor-namecert-obligations}. Structural exposure is exactly componentwise transport $H$, displayed continuation replay $C$, provenance $P$, and local naming $N$, so every consumer is confined to those structural rows.
\end{proof}

\begin{theorem}[Cauchy limit functor Real-completion consumer boundary]
\label{thm:cauchy-limit-functor-real-completion-consumer}
Every Real and Completion consumer that cites an accepted $\CauchyLimitFunctorUp$ packet factors through the finite route
$$
S \longmapsto M \longmapsto W_0,W_1,R,D \longmapsto K \longmapsto T \longmapsto E \longmapsto H,C,P,N.
$$
The consumer may use the source and target $\CauchyLimitSealUp$ rows, the transport map, the two $\StreamNameUp$ windows, the $\RegSeqRatUp$ readback, the $\DyadicRatCoreUp$ tolerance ledger, the $\RealCompletionSealClassifierUp$ row, the terminal $\RealUp$ endpoint, and the structural $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. It receives no quotient stream equality, host real equality, selected limit, detached completion object, ambient completeness principle, countable-choice selector, or second transport route.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:cauchy-limit-functor-namecert-obligations}. They force every accepted read through the carrier row, the source-to-target seal route, the finite observation rows, the completion-consumer rows, and the structural rows.

Use the classifier of \autoref{def:cauchy-limit-functor-classifier}. It compares only $S,T,M,W_0,W_1,R,D,K,E,H,C,P,N$, so a Real and Completion consumer cannot distinguish two reads by adding quotient stream equality, host real equality, a selected limit, or an ambient completion proof.

Finally follow the route displayed in the carrier. The source seal $S$ is transported by $M$ over the windows $W_0,W_1$, readback $R$, and tolerance ledger $D$; the classifier row $K$ recognizes the transported boundary before the target seal $T$ and endpoint row $E$ are consumed. The structural rows $H,C,P,N$ package that same finite route, so no detached completion object, choice-selected witness, or second route remains available.
\end{proof}
\leanchecked{BEDC.Derived.CauchyLimitFunctorUp.CauchyLimitFunctorCarrier\_real\_completion\_consumer\_boundary}

\begin{theorem}[Cauchy limit functor primitive scope package]
\label{thm:cauchy-limit-functor-primitive-scope-package}
Every accepted $\CauchyLimitFunctorUp$ packet with carrier
$$
F=(S,T,M,W_0,W_1,R,D,K,E,H,C,P,N)
$$
binds its source-target seal route, finite observation rows, completion-consumer rows, and structural rows only through the primitive BEDC objects $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, together with the sibling source certificates $\CauchyLimitSealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealCompletionSealClassifierUp$, and $\RealUp$. Thus the scoped packet consists exactly of the displayed carrier row, classifier coordinates, source-to-target route $S \to M \to T$, observation rows $W_0,W_1,R,D$, completion rows $K,E$, structural rows $H,C,P,N$, and their cited sibling certificates.

No quotient stream equality, host real equality, selected limit, detached completion object, ambient completeness principle, countable-choice selector, or second route enters the scoped packet.
\end{theorem}
\begin{proof}
Start with the carrier and classifier of \autoref{def:cauchy-limit-functor-carrier} and \autoref{def:cauchy-limit-functor-classifier}. The carrier is the finite $\BHist$ packet $F=(S,T,M,W_0,W_1,R,D,K,E,H,C,P,N)$, and the classifier reads exactly those coordinates, so every row available to the packet is already one of the displayed source, target, map, observation, completion, structural, provenance, or naming rows.

Apply the NameCert obligation surface of \autoref{thm:cauchy-limit-functor-namecert-obligations}. Its five rows identify carrier exposure, source-to-target seal exposure, finite observation exposure, completion-consumer exposure, and structural exposure. These are the only rows a local $\NameCert_{\CauchyLimitFunctorUp}$ consumer may preserve.

Next use \autoref{thm:cauchy-limit-functor-source-target-seal-exposure}, \autoref{thm:cauchy-limit-functor-finite-observation-exposure}, and \autoref{thm:cauchy-limit-functor-structural-transport}. They place the seal route at $S \to M \to T$, the finite observation surface at $W_0,W_1,R,D,K,E$, and the structural surface at $H,C,P,N$, where $H$ carries componentwise $\hsame$, $C$ carries displayed $\Cont$, $P$ carries $\Pkg$ provenance, and $N$ carries the local $\NameCert$ row.

Finally apply the Real-completion consumer boundary of \autoref{thm:cauchy-limit-functor-real-completion-consumer}. It keeps the completion read inside the same route through the sibling source certificates $\CauchyLimitSealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealCompletionSealClassifierUp$, and $\RealUp$. Since each cited result excludes quotient stream equality, host real equality, selected limits, detached completion objects, ambient completeness, countable choice, and second routes, the primitive-scoped packet contains no further object beyond $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, and those sibling certificates.
\end{proof}

\begin{theorem}[Cauchy limit functor public seal export]
\label{thm:cauchy-limit-functor-public-seal-export}
Every accepted $\CauchyLimitFunctorUp$ packet with carrier
$$
F=(S,T,M,W_0,W_1,R,D,K,E,H,C,P,N)
$$
exports a reusable public transport surface from the source $\CauchyLimitSealUp$ row $S$ through the displayed transport map $M$, the $\StreamNameUp$ windows $W_0,W_1$, the $\RegSeqRatUp$ readback $R$, the $\DyadicRatCoreUp$ tolerance ledger $D$, the $\RealCompletionSealClassifierUp$ row $K$, and the terminal $\RealUp$ endpoint $E$. The exported surface is reusable only as the public route
$$
S \longmapsto M \longmapsto W_0,W_1,R,D \longmapsto K \longmapsto T \longmapsto E \longmapsto H,C,P,N,
$$
with $H$ carrying componentwise $\hsame$ transport, $C$ carrying displayed $\Cont$ replay, $P$ carrying $\Pkg$ provenance, and $N$ carrying the local $\NameCert_{\CauchyLimitFunctorUp}$ row.

No quotient stream equality, host real equality, selected limit, detached completion object, ambient completeness principle, countable-choice selector, or second transport route is exported by this public surface.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyLimitFunctorUp.CauchyLimitFunctorCarrier\_public\_seal\_export}
\begin{proof}
Start with the carrier and classifier of \autoref{def:cauchy-limit-functor-carrier} and \autoref{def:cauchy-limit-functor-classifier}. They make the public packet a finite read of the displayed rows $S,T,M,W_0,W_1,R,D,K,E,H,C,P,N$, so a reusable export can only cite those rows.

Apply the NameCert obligations of \autoref{thm:cauchy-limit-functor-namecert-obligations}. The obligation rows expose the carrier, the source-target seal route, finite observations, Real-completion consumer boundary, and structural rows. Use \autoref{thm:cauchy-limit-functor-source-target-seal-exposure} and \autoref{thm:cauchy-limit-functor-finite-observation-exposure} to identify the public route through $S,M,W_0,W_1,R,D,K,T,E$.

Next apply \autoref{thm:cauchy-limit-functor-structural-transport}. It supplies exactly the reusable transport, replay, provenance, and naming rows $H,C,P,N$, so the exported surface can be handed to later consumers without adding any route outside the packet.

Finally use \autoref{thm:cauchy-limit-functor-real-completion-consumer} and \autoref{thm:cauchy-limit-functor-primitive-scope-package}. The first confines Real and Completion consumers to the same finite transport route, while the second keeps the route primitive-scoped. The exclusions in those two results remove quotient stream equality, host real equality, selected limits, detached completion objects, ambient completeness, countable choice, and second routes from the public export.
\end{proof}

\begin{theorem}[Cauchy limit functor terminal-window fusion]
\label{thm:cauchy-limit-functor-terminal-window-fusion}
Every accepted $\CauchyLimitFunctorUp$ packet with carrier
$$
F=(S,T,M,W_0,W_1,R,D,K,E,H,C,P,N)
$$
fuses the source terminal $\StreamNameUp$ window $W_0$, target terminal $\StreamNameUp$ window $W_1$, $\RegSeqRatUp$ readback $R$, $\DyadicRatCoreUp$ tolerance ledger $D$, $\RealCompletionSealClassifierUp$ classifier row $K$, and terminal $\RealUp$ row $E$ into the single terminal-window route
$$
S \longmapsto M \longmapsto W_0,W_1,R,D \longmapsto K \longmapsto T \longmapsto E \longmapsto H,C,P,N.
$$
The route has no quotient stream equality, selected host limit, countable-choice selector, detached completion object, or second Real-facing route.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-limit-functor-carrier}. The only finite source and target windows are $W_0$ and $W_1$, and the only Real-facing observation rows attached to them are $R,D,K,E$.

Use finite observation exposure, \autoref{thm:cauchy-limit-functor-finite-observation-exposure}. It identifies $W_0,W_1,R,D,K,E$ as the complete observation side available to a consumer and excludes quotient stream equality, selected limits, countable-choice selectors, host real equality, detached completion objects, and ambient completeness.

Finally apply the Real-completion consumer boundary and the public seal export, \autoref{thm:cauchy-limit-functor-real-completion-consumer} and \autoref{thm:cauchy-limit-functor-public-seal-export}. Together they force the terminal Real read through the same route from $S$ through $M$, the two windows, readback, tolerance, classifier, target seal, endpoint, and structural rows. Hence the terminal-window route is fused inside the accepted packet and no second Real-facing route remains.
\end{proof}

\begin{theorem}[Cauchy limit functor Real-seal naturality]
\label{thm:cauchy-limit-functor-real-seal-naturality}
Let
$$
F=(S,T,M,W_0,W_1,R,D,K,E,H,C,P,N)
$$
be an accepted $\CauchyLimitFunctorUp$ packet. Componentwise $\hsame$ transport along the structural row $H$ preserves the terminal-window route of \autoref{thm:cauchy-limit-functor-terminal-window-fusion} before any Real-facing seal is made public. Thus the transported source seal, target seal, stream windows, readback, tolerance ledger, classifier row, terminal Real row, continuation replay, provenance, and local name still form the same single route, with no public seal read adding a quotient stream equality, selected host limit, countable-choice selector, detached completion object, or second Real-facing route.
\end{theorem}
\begin{proof}
Read the classifier of \autoref{def:cauchy-limit-functor-classifier}. It compares packets only by the rows $S,T,M,W_0,W_1,R,D,K,E,H,C,P,N$, so componentwise transport cannot create a new coordinate for a second seal route or a detached Real object.

Use structural transport, \autoref{thm:cauchy-limit-functor-structural-transport}. The row $H$ is the only componentwise $\hsame$ transport surface, while $C,P,N$ keep the replay, provenance, and local name attached to the same packet.

Apply terminal-window fusion, \autoref{thm:cauchy-limit-functor-terminal-window-fusion}. Since the fused route is already determined before public seal export, transporting each displayed component along $H$ preserves that same route. The exclusions in the fusion theorem therefore persist before any Real-facing seal becomes public.
\end{proof}

\begin{theorem}[Cauchy limit functor bridge-scope coherence]
\label{thm:cauchy-limit-functor-bridge-scope-coherence}
Every bridge-scope consumer of an accepted $\CauchyLimitFunctorUp$ packet with carrier
$$
F=(S,T,M,W_0,W_1,R,D,K,E,H,C,P,N)
$$
must preserve the finite route
$$
S,T,M,W_0,W_1,R,D,K,E,H,C,P,N
$$
through $\CauchyLimitSealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealCompletionSealClassifierUp$, and $\RealUp$. The bridge-facing read may expose the public transport surface of \autoref{thm:cauchy-limit-functor-public-seal-export}, but it cannot add quotient stream equality, host real equality, selected limits, ambient completeness, countable choice, or a second transport route.
\end{theorem}
\begin{proof}
Start with the carrier and classifier. The packet rows are exactly $S,T,M,W_0,W_1,R,D,K,E,H,C,P,N$, and the classifier reads only those coordinates.

Apply the public seal export, \autoref{thm:cauchy-limit-functor-public-seal-export}. It exposes the reusable route from $S$ through $M$, the stream windows $W_0,W_1$, the readback $R$, the tolerance ledger $D$, the classifier row $K$, the target seal $T$, the terminal endpoint $E$, and the structural rows $H,C,P,N$.

Next use the primitive scope package and Real-completion consumer boundary, \autoref{thm:cauchy-limit-functor-primitive-scope-package} and \autoref{thm:cauchy-limit-functor-real-completion-consumer}. These place the route through $\CauchyLimitSealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealCompletionSealClassifierUp$, and $\RealUp$ without admitting a detached completion object.

Carrier-coordinate exhaustion gives bridge-scope coherence. Since the displayed packet has no coordinate for quotient stream equality, host real equality, selected limits, ambient completeness, countable choice, or a second transport route, a bridge-scope consumer can preserve only the finite route already exported by the public seal surface.
\end{proof}

\begin{theorem}[CauchyLimitFunctor standard reflection bridge surface]
\label{thm:cauchy-limit-functor-standard-reflection-bridge-surface}
Every standard-reflection read of an accepted $\CauchyLimitFunctorUp$ packet with carrier
$$
F=(S,T,M,W_0,W_1,R,D,K,E,H,C,P,N)
$$
factors through the displayed source seal $S$, target seal $T$, transport row $M$, $\StreamNameUp$ windows $W_0,W_1$, $\RegSeqRatUp$ readback $R$, $\DyadicRatCoreUp$ tolerance ledger $D$, $\RealCompletionSealClassifierUp$ classifier row $K$, $\RealUp$ endpoint $E$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert_{\CauchyLimitFunctorUp}$ row $N$.

Thus the bridge surface is the single route
$$
S \longmapsto M \longmapsto W_0,W_1,R,D \longmapsto K \longmapsto T \longmapsto E \longmapsto H,C,P,N,
$$
read through the same primitive-scoped packet. It exports no quotient stream equality, host real equality, selected limit, detached completion object, ambient completeness principle, countable-choice selector, or alternate completion transport.
\end{theorem}
\begin{proof}
Start from the carrier and classifier of \autoref{def:cauchy-limit-functor-carrier} and \autoref{def:cauchy-limit-functor-classifier}. They make $S,T,M,W_0,W_1,R,D,K,E,H,C,P,N$ the only rows visible to a standard-reflection read.

Apply the NameCert obligation surface of \autoref{thm:cauchy-limit-functor-namecert-obligations}. Its obligation rows expose the source-target seal route, finite observations, Real-completion consumer boundary, structural transport, provenance, and local naming surface for the same accepted packet.

Next use \autoref{thm:cauchy-limit-functor-source-target-seal-exposure}, \autoref{thm:cauchy-limit-functor-finite-observation-exposure}, and \autoref{thm:cauchy-limit-functor-structural-transport}. These identify the route through $S,M,T$, the windows $W_0,W_1$, readback $R$, tolerance ledger $D$, classifier row $K$, endpoint $E$, and the structural rows $H,C,P,N$.

Finally apply \autoref{thm:cauchy-limit-functor-real-completion-consumer}, \autoref{thm:cauchy-limit-functor-primitive-scope-package}, and \autoref{thm:cauchy-limit-functor-public-seal-export}. They keep Real-completion consumption inside the primitive-scoped public seal route. The bridge-scope coherence theorem then makes any standard-reflection read preserve that route without adding quotient equality, host real equality, selected limits, detached completion objects, ambient completeness, countable choice, or a second transport.
\end{proof}

\begin{theorem}[Cauchy limit functor seal route totality]
\label{thm:cauchy-limit-functor-seal-route-totality}
Every accepted $\CauchyLimitFunctorUp$ packet with carrier
$$
F=(S,T,M,W_0,W_1,R,D,K,E,H,C,P,N)
$$
exposes its public route only through the displayed source seal $S$, target seal $T$, map row $M$, $\StreamNameUp$ finite-window rows $W_0,W_1$, $\RegSeqRatUp$ readback row $R$, $\DyadicRatCoreUp$ tolerance row $D$, $\RealUp$ endpoint row $E$, the provenance row $\Pkg$, the replay row $\Cont$, the componentwise transport row $\hsame$, and the local row $\NameCert_{\CauchyLimitFunctorUp}$. No accepted packet admits a public route through quotient stream equality, host real equality, selected limits, detached completion objects, ambient completeness, countable choice, or any second transport row.
\end{theorem}
\begin{proof}
Start with the source-target seal exposure of \autoref{thm:cauchy-limit-functor-source-target-seal-exposure}. It makes $S$, $M$, and $T$ the displayed route from the source $\CauchyLimitSealUp$ side to the target seal side.

Next apply finite observation exposure, \autoref{thm:cauchy-limit-functor-finite-observation-exposure}. This inserts the $\StreamNameUp$ windows $W_0,W_1$, the $\RegSeqRatUp$ readback $R$, the $\DyadicRatCoreUp$ tolerance row $D$, the classifier row $K$, and the terminal $\RealUp$ endpoint $E$ into the same route.

Finally use the primitive scope package, \autoref{thm:cauchy-limit-functor-primitive-scope-package}, together with the carrier and classifier definitions. The remaining visible rows are exactly the structural transport, displayed replay, provenance, and local naming rows $H,C,P,N$, so every public route is exhausted by the listed $\Pkg$, $\Cont$, $\hsame$, and $\NameCert$ data.
\end{proof}

\begin{theorem}[Cauchy limit functor finite-window naturality]
\label{thm:cauchy-limit-functor-finite-window-naturality}
Let two accepted $\CauchyLimitFunctorUp$ packets share the same source seal row, target seal row, and map row inside the displayed carrier. If their $\StreamNameUp$ finite windows are $\hsame$-compatible and are transported by that same map row, then the two packets induce the same $\RegSeqRatUp$ readback route and the same $\RealUp$ terminal seal route through the displayed rows $S,T,M,W_0,W_1,R,D,K,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Begin with structural transport, \autoref{thm:cauchy-limit-functor-structural-transport}. The shared map row $M$ and the $\hsame$ row $H$ make the two finite-window reads componentwise compatible inside the carrier.

Apply finite observation exposure. Since the windows are transported by the same map row, the readback row $R$ and tolerance row $D$ are read from the same finite observation surface, and the classifier row $K$ sees no additional coordinate.

The source-target seal exposure then carries this common finite observation route to the same target seal row and terminal $\RealUp$ endpoint. Carrier-coordinate exhaustion rules out a second route, so the induced $\RegSeqRatUp$ and $\RealUp$ seal routes agree.
\end{proof}

\begin{theorem}[Cauchy limit functor completion consumer exactness]
\label{thm:cauchy-limit-functor-completion-consumer-exactness}
Every $\RealUp$ or completion-facing consumer of an accepted $\CauchyLimitFunctorUp$ output factors through the source-target seal route and the bridge-scope coherence route before reading any terminal seal. Such a consumer may read $S,T,M,W_0,W_1,R,D,K,E,H,C,P,N$, but it cannot bypass the public seal route by introducing quotient stream equality, host real equality, a selected limit, a detached completion object, ambient completeness, countable choice, or an alternate completion transport.
\end{theorem}
\begin{proof}
Use the Real-completion consumer boundary, \autoref{thm:cauchy-limit-functor-real-completion-consumer}. It confines every $\RealUp$ or completion-facing read to the finite route through the source seal, target seal, map row, finite windows, readback, tolerance ledger, classifier row, and terminal endpoint.

Next apply the public seal export, \autoref{thm:cauchy-limit-functor-public-seal-export}. It packages the same route with the structural transport, replay, provenance, and local NameCert rows, making those rows the only reusable surface for downstream consumers.

Finally apply bridge-scope coherence, \autoref{thm:cauchy-limit-functor-bridge-scope-coherence}. Any terminal seal read must preserve the bridge-scoped route already exported by the public surface, so completion-facing exactness follows without adding a second completion route.
\end{proof}

\begin{theorem}[Cauchy limit functor endpoint compatibility]
\label{thm:cauchy-limit-functor-source-target-endpoint-compatibility}
Let two accepted $\CauchyLimitFunctorUp$ packets agree on the displayed source seal $S$, target seal $T$, transport map $M$, finite windows $W_0,W_1$, readback row $R$, dyadic tolerance row $D$, classifier row $K$, and structural rows $H,C,P,N$. Then their terminal $\RealUp$ endpoint row $E$ agrees under the displayed classifier.
\end{theorem}
\begin{proof}
Project the classifier of \autoref{def:cauchy-limit-functor-classifier}. Agreement on $S,T,M,W_0,W_1,R,D,K,H,C,P,N$ leaves the terminal endpoint row as the only completion-facing coordinate still to be read by the packet classifier.

Apply finite observation exposure, \autoref{thm:cauchy-limit-functor-finite-observation-exposure}. The displayed windows, readback, dyadic ledger, and classifier row determine the finite observation route that reaches the terminal $\RealUp$ endpoint.

Finally use the Real-completion consumer boundary, \autoref{thm:cauchy-limit-functor-real-completion-consumer}. A completion-facing read cannot introduce quotient stream equality, a selected limit, host real equality, or a detached completion object, so the terminal endpoint row must be the one reached by the displayed classifier route.
\end{proof}

\begin{theorem}[Cauchy limit functor window-map determinacy]
\label{thm:cauchy-limit-functor-window-map-determinacy}
Fix an accepted $\CauchyLimitFunctorUp$ packet's source seal $S$, transport map $M$, finite windows $W_0,W_1$, readback row $R$, dyadic tolerance row $D$, and classifier row $K$. Within the packet classifier, these rows determine the target seal $T$ and terminal endpoint $E$.
\end{theorem}
\begin{proof}
Begin with source-target seal exposure, \autoref{thm:cauchy-limit-functor-source-target-seal-exposure}. The source seal and transport map are the displayed route to the target seal row; no second target-seal coordinate is present in the carrier.

Apply finite observation exposure. The fixed windows, readback, dyadic tolerance ledger, and classifier row give the same finite observation route toward the terminal endpoint.

Now use completion consumer exactness, \autoref{thm:cauchy-limit-functor-completion-consumer-exactness}. A Real-facing or completion-facing consumer must pass through the source-target seal route and bridge-scope coherence before reading a terminal seal. Since all rows on that route are fixed, both $T$ and $E$ are determined within the accepted classifier.
\end{proof}

\begin{theorem}[Cauchy limit functor transport-map no-confusion]
\label{thm:cauchy-limit-functor-transport-map-no-confusion}
Two accepted $\CauchyLimitFunctorUp$ packets cannot be classifier-equal while disagreeing on the displayed transport-map row $M$. Equivalently, classifier equality of accepted packets projects to agreement of the transport map.
\end{theorem}
\begin{proof}
Unpack the carrier of \autoref{def:cauchy-limit-functor-carrier}. The transport-map row $M$ is one of the displayed coordinates of the finite packet, not an inferred property of the endpoint.

Use the classifier of \autoref{def:cauchy-limit-functor-classifier}. Classifier equality compares packet coordinates componentwise through the displayed transport rows, so equality cannot skip $M$ while preserving the surrounding source, target, window, readback, tolerance, endpoint, provenance, replay, and naming rows.

Finally apply structural transport, \autoref{thm:cauchy-limit-functor-structural-transport}. The $\hsame$ and $\Cont$ rows replay the same displayed transport map through the consumer route. If two packets disagreed on $M$, that disagreement would be visible before any terminal $\RealUp$ endpoint is read, contradicting classifier equality.
\end{proof}

\begin{theorem}[Cauchy limit functor identity consumer]
\label{thm:cauchy-limit-functor-identity-consumer}
Suppose an accepted $\CauchyLimitFunctorUp$ packet has the same displayed $\CauchyLimitSealUp$ row as source and target, and suppose its transport row is the identity replay over the same $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealCompletionSealClassifierUp$, and $\RealUp$ rows. Then every identity consumer of the packet still reads only the finite route isolated by \autoref{thm:cauchy-limit-functor-standard-reflection-bridge-surface}.

In particular, the identity consumer may reuse the displayed rows
$$
S,T,M,W_0,W_1,R,D,K,E,H,C,P,N,
$$
but it exports no quotient stream equality, host real equality, selected limit, detached completion object, ambient completeness principle, countable-choice selector, or alternate completion transport.
\end{theorem}
\begin{proof}
Start with the carrier and classifier of \autoref{def:cauchy-limit-functor-carrier} and \autoref{def:cauchy-limit-functor-classifier}. The identity assumption identifies the source and target seal rows inside the already displayed carrier; it does not create a new carrier coordinate or a second classifier read.

Apply \autoref{thm:cauchy-limit-functor-source-target-seal-exposure}, \autoref{thm:cauchy-limit-functor-finite-observation-exposure}, and \autoref{thm:cauchy-limit-functor-structural-transport}. The first keeps the route at the same $\CauchyLimitSealUp$ row, the second keeps the observation through $W_0,W_1,R,D,K,E$, and the third records the identity replay through $H,C,P,N$.

Next use \autoref{thm:cauchy-limit-functor-real-completion-consumer}, \autoref{thm:cauchy-limit-functor-public-seal-export}, and \autoref{thm:cauchy-limit-functor-bridge-scope-coherence}. They keep any Real-completion read, reusable public export, and bridge-scope read inside the same primitive-scoped route.

Finally apply \autoref{thm:cauchy-limit-functor-standard-reflection-bridge-surface}. Since the identity replay has no coordinate beyond the cited carrier rows and sibling certificates, the consumer reads exactly the finite route already isolated there and nothing outside it.
\end{proof}

\begin{theorem}[Cauchy limit functor composite consumer]
\label{thm:cauchy-limit-functor-composite-consumer}
Let two accepted $\CauchyLimitFunctorUp$ packets be composable in the sense that the middle target seal row of the first packet and the middle source seal row of the second packet agree componentwise, and their shared observation rows agree through the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealCompletionSealClassifierUp$, and $\RealUp$ certificates. Then the composite consumer route is the concatenation of the two finite routes displayed by \autoref{thm:cauchy-limit-functor-standard-reflection-bridge-surface}.

The composite consumer exports only this concatenated finite route through the two packets' rows $S,T,M,W_0,W_1,R,D,K,E,H,C,P,N$. It exports no quotient stream equality, selected limit, ambient completeness principle, countable-choice selector, alternate completion transport, or host-level equality between the two completion presentations.
\end{theorem}
\begin{proof}
Use the carrier and classifier definitions for both packets. Componentwise agreement of the middle seal and observation rows lets the second route begin exactly where the first route ends, while each packet still exposes only its own displayed carrier rows.

Apply \autoref{thm:cauchy-limit-functor-source-target-seal-exposure} and \autoref{thm:cauchy-limit-functor-finite-observation-exposure} to each packet. These two results identify the composable seal route and the shared finite observation surface without adding a quotient or selected limiting object at the middle row.

Next apply \autoref{thm:cauchy-limit-functor-structural-transport}, \autoref{thm:cauchy-limit-functor-real-completion-consumer}, and \autoref{thm:cauchy-limit-functor-public-seal-export}. The structural row supplies the componentwise replay, the Real-completion boundary keeps both reads inside the displayed certificates, and the public export gives the reusable surface for concatenation.

Finally use \autoref{thm:cauchy-limit-functor-bridge-scope-coherence} and \autoref{thm:cauchy-limit-functor-standard-reflection-bridge-surface}. Bridge-scope coherence prevents a second route at the shared middle row, and the standard-reflection bridge surface identifies the composite as the two finite routes placed end to end.
\end{proof}

\begin{theorem}[Cauchy limit functor Real-completion handoff exhaustion]
\label{thm:cauchy-limit-functor-real-completion-handoff-exhaustion}
Every $\RealUp$-and-completion consumer using either the identity route of \autoref{thm:cauchy-limit-functor-identity-consumer} or the composite route of \autoref{thm:cauchy-limit-functor-composite-consumer} factors through the same displayed rows
$$
S,T,M,W_0,W_1,R,D,K,E,H,C,P,N
$$
and through the sibling certificates $\CauchyLimitSealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealCompletionSealClassifierUp$, and $\RealUp$ already cited in this chapter.

Consequently the handoff is exhausted by the primitive-scoped bridge surface: it has no extra output for quotient stream equality, host real equality, selected limits, detached completion objects, ambient completeness, countable choice, or alternate completion transport.
\end{theorem}
\begin{proof}
Begin with the carrier and classifier definitions. They make $S,T,M,W_0,W_1,R,D,K,E,H,C,P,N$ the complete coordinate list for any accepted packet, so an identity or composite handoff has no independent row to read.

Apply \autoref{thm:cauchy-limit-functor-source-target-seal-exposure}, \autoref{thm:cauchy-limit-functor-finite-observation-exposure}, and \autoref{thm:cauchy-limit-functor-structural-transport}. These locate the seal, observation, and replay parts of the handoff inside the cited sibling certificates and structural rows.

Next use \autoref{thm:cauchy-limit-functor-real-completion-consumer}, \autoref{thm:cauchy-limit-functor-public-seal-export}, and \autoref{thm:cauchy-limit-functor-bridge-scope-coherence}. They confine every Real-and-completion consumer to the public bridge-scope route and exclude detached completion data from the handoff.

Finally apply \autoref{thm:cauchy-limit-functor-standard-reflection-bridge-surface}, together with the identity and composite consumer theorems above. The only available handoff is the identity route or the concatenated finite route, and both are exhausted by the same primitive-scoped rows and sibling certificates.
\end{proof}

\closureat{CauchyLimitFunctorUp}{seedStr}
\closureat{CauchyLimitFunctorUp}{obligationStr}
\closureat{CauchyLimitFunctorUp}{scopedStr}
\closureat{CauchyLimitFunctorUp}{publicStr}
\closureat{CauchyLimitFunctorUp}{bridgedStr}
\closureat{CauchyLimitFunctorUp}{matureStr}

\begin{closurestatus}{\CauchyLimitFunctorUp}
  \constructivestory{A $\CauchyLimitFunctorUp$ packet is generated from source and target CauchyLimitSeal rows, a finite transport-map row, source and target StreamName windows, a RegSeqRat readback, a DyadicRatCore tolerance ledger, a RealCompletionSealClassifier row, a terminal RealUp endpoint, componentwise transports, displayed replay, provenance, and a local NameCert row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{def:cauchy-limit-functor-carrier}, \autoref{def:cauchy-limit-functor-classifier}, \autoref{thm:cauchy-limit-functor-namecert-obligations}, \autoref{thm:cauchy-limit-functor-source-target-seal-exposure}, \autoref{thm:cauchy-limit-functor-finite-observation-exposure}, \autoref{thm:cauchy-limit-functor-structural-transport}, \autoref{thm:cauchy-limit-functor-real-completion-consumer}, \autoref{thm:cauchy-limit-functor-primitive-scope-package}, \autoref{thm:cauchy-limit-functor-public-seal-export}, \autoref{thm:cauchy-limit-functor-bridge-scope-coherence}, \autoref{thm:cauchy-limit-functor-standard-reflection-bridge-surface}, \autoref{thm:cauchy-limit-functor-seal-route-totality}, \autoref{thm:cauchy-limit-functor-finite-window-naturality}, \autoref{thm:cauchy-limit-functor-completion-consumer-exactness}, \autoref{thm:cauchy-limit-functor-identity-consumer}, \autoref{thm:cauchy-limit-functor-composite-consumer}, and \autoref{thm:cauchy-limit-functor-real-completion-handoff-exhaustion} bind the finite transport carrier, classifier, five NameCert obligation rows, separate seal-route exposure, finite observation exposure, structural transport, Real-completion consumer boundary, primitive scope package, reusable public transport surface, bridge-scope route coherence, standard-reflection bridge surface, seal-route totality, finite-window naturality, completion consumer exactness, identity consumer, composite consumer, and Real-completion handoff exhaustion.}
  \formalstatus{\unformalizedV}
  \bridgestatus{paperBridge}
  \origin{human}
  \notclaimed{This chapter does not close Lean verification, bridge checking, quotient stream equality, host real equality, selected limits, ambient completeness, countable choice, or a universal completion functor.}
  \upgradepath{No further paper-side closure grade remains; Lean verification and bridge checking remain separate axes for the same primitive-scoped $\CauchyLimitFunctorUp$ consumer surface.}
\end{closurestatus}
