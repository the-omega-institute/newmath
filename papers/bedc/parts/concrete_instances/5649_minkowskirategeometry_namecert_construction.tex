\chapter{A Concrete Naming Certificate for $\MinkowskiRateGeometryUp$}
\label{ch:concrete-instances-minkowski-rate-geometry-namecert}
\origin{ai}

The $\MinkowskiRateGeometryUp$ interface realizes the Lorentz-geometry step of the Inter-Hist Locality chain of \autoref{ch:visions-inter-hist-locality}. It consumes the finite multi-history configuration surface of \autoref{ch:concrete-instances-multihistconfig-namecert}, the causal witness rows of \autoref{ch:concrete-instances-crosshistcausal-namecert}, the maximum-rate boundary of \autoref{ch:concrete-instances-maxcausalrate-namecert}, and the observer-symmetric frame-rate packet of \autoref{ch:concrete-instances-lorentz-frame-rate-namecert}. Its new content is a symmetric distance-shape ledger: the packet records which rate-induced comparisons may be read as a Minkowski-like boundary, while still exporting no metric tensor, global frame, synchronized clock, host real-valued coordinate, or ambient Lorentz manifold.

\begin{definition}[Minkowski rate geometry carrier]
\label{def:minkowski-rate-geometry-carrier}
A $\MinkowskiRateGeometryUp$ carrier is a finite $\BHist$ packet
$$
M=(G,X,R,L,D,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $G$ is an accepted $\MultiHistConfigUp$ row displaying the finite observer-history configuration and its no-global-sync boundary;
\item $X$ is the finite family of $\CrossHistCausalUp$ rows over selected pairs of histories in $G$;
\item $R$ is an accepted $\MaxCausalRateUp$ row whose unary bound is read over $G$ and the causal family $X$;
\item $L$ is an accepted $\LorentzFrameRateUp$ row tying the same $G,X,R$ surface to an observer-symmetric frame-rate ledger;
\item $D$ is the symmetric distance-shape ledger, recording the rate-induced light-boundary comparisons licensed by $L$ and requiring the same row after swapping the two displayed observer histories;
\item $H$ records componentwise $\hsame$ transport for $G$, $X$, $R$, $L$, $D$, the continuation routes, provenance, and local naming row;
\item $C$ records visible $\Cont$ routes by which a consumer reads $G$, then $X$, then $R$, then $L$, then $D$, and transports the packet through $H$ into the local certificate;
\item $P$ is the $\Pkg$ provenance over $\MultiHistConfigUp$, $\CrossHistCausalUp$, $\MaxCausalRateUp$, $\LorentzFrameRateUp$, the distance-shape ledger, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\MinkowskiRateGeometryUp}$ row.
\end{enumerate}
The carrier exports only this finite ledger surface. The word ``geometry'' names the symmetric boundary shape induced by the maximum-rate invariant, not an imported continuum, coordinate chart, or metric tensor.
\end{definition}

\begin{theorem}[Minkowski rate geometry NameCert obligations]
\label{thm:minkowski-rate-geometry-namecert-obligations}
For the carrier of \autoref{def:minkowski-rate-geometry-carrier}, the local $\NameCert_{\MinkowskiRateGeometryUp}$ surface consists exactly of six finite obligations:
\begin{enumerate}
\item carrier admission for the displayed packet $M=(G,X,R,L,D,H,C,P,N)$;
\item upstream exactness, requiring $G$, $X$, $R$, and $L$ to be the displayed multi-Hist, causal, maximum-rate, and Lorentz-frame-rate rows read in that order;
\item distance-shape exactness, requiring every entry of $D$ to be induced by the observer-symmetric maximum-rate row of $L$;
\item observer symmetry, requiring the $D$ row to be stable under swapping the two displayed observer histories and transporting the swap through $H$;
\item route stability, requiring all reads of $D$ to pass through the visible $\Cont$ routes in $C$ and the provenance row $P$;
\item non-escape, excluding global frames, synchronized clocks, metric tensors, host real-valued coordinates, ambient Lorentz manifolds, and distance rows not generated by $G,X,R,L,D,H,C,P,N$.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.MinkowskiRateGeometryUp.MinkowskiRateGeometryCarrier\_namecert\_obligations}
\begin{proof}
Carrier admission is projection from \autoref{def:minkowski-rate-geometry-carrier}. The packet displays the configuration, causal family, maximum-rate row, frame-rate row, distance-shape ledger, transport rows, continuation routes, provenance, and local name before any consumer reads the certificate.

Upstream exactness reads the rows in dependency order. The maximum-rate row $R$ is tied to $G$ and $X$, and the Lorentz-frame-rate row $L$ is tied to the same $G,X,R$ surface.

Distance-shape exactness then reads $D$ only after $L$. Thus the boundary shape is a rate-induced ledger row, not an independent metric object.

Observer symmetry and route stability are the componentwise $H$ and $C$ clauses. They move the displayed rows together and keep every read inside the same finite packet.

The non-escape clause is the carrier inventory. Since no excluded structure is a coordinate of $M$, no local naming projection can export one.
\end{proof}

\begin{theorem}[Minkowski rate geometry symmetry]
\label{thm:minkowski-rate-geometry-symmetry}
Let
$$
M=(G,X,R,L,D,H,C,P,N)
$$
be an accepted $\MinkowskiRateGeometryUp$ carrier. Every distance-shape comparison in $D$ is observer-symmetric: swapping the two displayed observer histories in a listed pair and transporting the packet through $H$ returns the same distance-shape row in $D$ along the visible routes in $C$.
\end{theorem}
\leanchecked{BEDC.Derived.MinkowskiRateGeometryUp.MinkowskiRateGeometrySymmetry}
\begin{proof}
Read the distance-shape ledger $D$ through the route required by \autoref{def:minkowski-rate-geometry-carrier}. Each row of $D$ is attached to the observer-symmetric frame-rate packet $L$.

The frame-rate row inherits its maximum-rate row from the same $G$ and $X$ surface, so swapping the displayed observers does not select a different rate source. It only reorders the same selected pair inside the finite configuration.

Finally transport through $H$ and replay the routes in $C$. The comparison returns to the same distance-shape ledger row, because $D$ records symmetric shape data and has no coordinate for a privileged observer slot.
\end{proof}

\begin{theorem}[Minkowski rate geometry non-escape]
\label{thm:minkowski-rate-geometry-nonescape}
The public boundary of an accepted $\MinkowskiRateGeometryUp$ packet exports no global frame, synchronized clock, metric tensor, host real-valued coordinate system, ambient Lorentz manifold, or distance comparison detached from the maximum-rate invariant. A consumer can read a Minkowski-like boundary only through the finite route
$$
G,X,R,L,D,H,C,P,N.
$$
\end{theorem}
\leanchecked{BEDC.Derived.MinkowskiRateGeometryUp.MinkowskiRateGeometryNonescape}
\begin{proof}
Project the accepted carrier. Its coordinates are exactly the upstream configuration, causal, maximum-rate, and Lorentz-frame-rate rows, the distance-shape ledger, transport rows, continuation routes, provenance, and local name.

The NameCert obligations require every distance-shape row to be induced by $L$, and $L$ is itself tied to the displayed maximum-rate row $R$ over $G$ and $X$. Hence no detached distance comparison is available.

The symmetry theorem keeps the boundary observer-symmetric under componentwise transport. It does not add a metric tensor, coordinate chart, or global frame.

Therefore the public packet is a finite rate-to-geometry boundary. Any richer geometric structure would require a coordinate absent from $G,X,R,L,D,H,C,P,N$.
\end{proof}

\begin{theorem}[Minkowski rate geometry TasteGate obligation]
\label{thm:minkowski-rate-geometry-tastegate-obligation}
The later Lean \texttt{TasteGate.lean} development for $\MinkowskiRateGeometryUp$ must include explicit $\mathsf{Nontrivial}\ \MinkowskiRateGeometryUp$ and $\mathsf{FieldFaithful}\ \MinkowskiRateGeometryUp$ instances. These instances must preserve the multi-Hist row, causal family, maximum-rate row, Lorentz-frame-rate row, symmetric distance-shape ledger, $\hsame$ transport, $\Cont$ routes, package provenance, and local naming row.
\end{theorem}
\begin{proof}
The carrier is not determined by any single upstream row. It adds $D$, the symmetric distance-shape ledger, after the observer-symmetric maximum-rate surface has already been exposed by $L$.

Nontriviality rules out collapsing the packet to a bare maximum-rate row or a bare Lorentz-frame-rate row. Field faithfulness records preservation of every coordinate $G,X,R,L,D,H,C,P,N$, so a downstream TasteGate consumer can distinguish the rate-induced geometry boundary from each sibling surface it consumes.
\end{proof}
\leanchecked{BEDC.Derived.MinkowskiRateGeometryUp.MinkowskiRateGeometryTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Minkowski rate geometry carrier envelope]
\label{thm:minkowski-rate-geometry-carrier-envelope}
Every accepted $\MinkowskiRateGeometryUp$ packet is exhausted by the displayed carrier coordinates
$$
G,X,R,L,D,H,C,P,N.
$$
The NameCert obligations of \autoref{thm:minkowski-rate-geometry-namecert-obligations}, the symmetry theorem \autoref{thm:minkowski-rate-geometry-symmetry}, the non-escape theorem \autoref{thm:minkowski-rate-geometry-nonescape}, and the TasteGate marker \texttt{BEDC.Derived.MinkowskiRateGeometryUp.MinkowskiRateGeometryTasteGate\_single\_carrier\_alignment} leave no additional geometry coordinate outside the finite carrier envelope.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:minkowski-rate-geometry-carrier}. Its coordinates are exactly the multi-Hist configuration $G$, causal family $X$, maximum-rate row $R$, Lorentz-frame-rate row $L$, symmetric distance-shape ledger $D$, transport row $H$, continuation row $C$, provenance row $P$, and local name $N$.

Apply the NameCert obligations and non-escape theorem. They require every consumer read to expose the same displayed rows and exclude global frames, synchronized clocks, metric tensors, host real-valued coordinates, ambient Lorentz manifolds, and detached distance comparisons.

Finally use the symmetry theorem and the TasteGate marker. Symmetry keeps the distance-shape ledger inside the same observer-swapping route, while field faithfulness records every coordinate of the single carrier, so no hidden geometry field remains beyond $G,X,R,L,D,H,C,P,N$.
\end{proof}
\leanchecked{BEDC.Derived.MinkowskiRateGeometryUp.MinkowskiRateGeometryCarrier\_envelope}

\begin{theorem}[Minkowski rate geometry classifier stability]
\label{thm:minkowski-rate-geometry-classifier-stability}
Let two accepted $\MinkowskiRateGeometryUp$ packets be related by the componentwise $\hsame$ transport row displayed in their carriers. Then the classifier preserves the symmetric distance-shape row only through the route
$$
G,X,R,L,D,H,C,P,N.
$$
In particular, classifier stability may use the NameCert obligations, observer symmetry, non-escape, and the TasteGate marker, but it may not compare packets through an external metric tensor, ambient Lorentz manifold, global frame, synchronized clock, or host real-valued coordinate.
\end{theorem}
\begin{proof}
Read the componentwise transport row $H$ from the carrier. It transports the upstream configuration, causal family, maximum-rate row, Lorentz-frame-rate row, distance-shape ledger, continuation routes, provenance, and local name together.

Apply \autoref{thm:minkowski-rate-geometry-symmetry}. The transported comparison remains the same symmetric distance-shape row because swapping observers is handled inside $D$ and replayed through $C$.

Apply \autoref{thm:minkowski-rate-geometry-nonescape} and the TasteGate marker. Non-escape forbids external comparison coordinates, and field faithfulness keeps the classifier aligned with the displayed single-carrier route.
\end{proof}
\leanchecked{BEDC.Derived.MinkowskiRateGeometryUp.MinkowskiRateGeometryCarrier\_classifier\_stability}

\begin{theorem}[Minkowski rate geometry ledger exactness]
\label{thm:minkowski-rate-geometry-ledger-exactness}
For every accepted $\MinkowskiRateGeometryUp$ packet, the distance-shape ledger $D$ is induced by the displayed $\MaxCausalRateUp$ row $R$ and $\LorentzFrameRateUp$ row $L$ through the NameCert route
$$
G,X,R,L,D,H,C,P,N.
$$
The ledger cannot export a metric tensor, coordinate chart, global frame, synchronized clock, host real-valued coordinate, or ambient Lorentz manifold. Its exact public content is the symmetric rate-induced boundary named by the carrier, the NameCert obligations, the symmetry and non-escape theorems, and the TasteGate marker.
\end{theorem}
\leanchecked{BEDC.Derived.MinkowskiRateGeometryUp.MinkowskiRateGeometryCarrier\_ledger\_exactness}
\begin{proof}
Project the rows $R,L,D$ from the accepted carrier. The maximum-rate row $R$ is read over the same $G,X$ surface, and the Lorentz-frame-rate row $L$ ties that maximum-rate surface to the observer-symmetric frame-rate ledger.

The NameCert obligations require every entry of $D$ to be induced by $L$ rather than supplied as an independent metric object. The symmetry theorem keeps this induced ledger invariant under the displayed observer swap.

Use non-escape and the TasteGate marker to close the ledger boundary. Since the carrier is field-faithful for $G,X,R,L,D,H,C,P,N$ and contains no metric-tensor or manifold coordinate, the public ledger is exactly the finite rate-induced distance-shape row.
\end{proof}

\begin{theorem}[Minkowski rate geometry scope binding]
\label{thm:minkowski-rate-geometry-scope-binding}
The finite rate-to-geometry packet is scoped only over $\MultiHistConfigUp$, $\CrossHistCausalUp$, $\MaxCausalRateUp$, $\LorentzFrameRateUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, and the displayed TasteGate marker
$$
\texttt{BEDC.Derived.MinkowskiRateGeometryUp.MinkowskiRateGeometryTasteGate\_single\_carrier\_alignment}.
$$
Within that scope, promotion reads only the finite carrier $G,X,R,L,D,H,C,P,N$ and never a metric tensor, coordinate chart, synchronized clock, global frame, host real-valued speed, curved spacetime, or ambient Lorentz manifold.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:minkowski-rate-geometry-carrier}. Its public rows are the multi-Hist configuration $G$, cross-Hist causal family $X$, maximum-rate row $R$, Lorentz-frame-rate row $L$, distance-shape ledger $D$, transport $H$, continuation $C$, provenance $P$, and local name $N$.

Apply the NameCert obligations of \autoref{thm:minkowski-rate-geometry-namecert-obligations}. They force upstream exactness through $\MultiHistConfigUp$, $\CrossHistCausalUp$, $\MaxCausalRateUp$, and $\LorentzFrameRateUp$ before any distance-shape row is visible.

Use non-escape and ledger exactness from \autoref{thm:minkowski-rate-geometry-nonescape} and \autoref{thm:minkowski-rate-geometry-ledger-exactness}. These theorems keep every geometry read inside the rate-induced ledger and rule out external metric, coordinate, clock, frame, speed, spacetime, and manifold coordinates.

Finally read the TasteGate marker
$$
\texttt{BEDC.Derived.MinkowskiRateGeometryUp.MinkowskiRateGeometryTasteGate\_single\_carrier\_alignment}.
$$
It aligns field faithfulness with the same single carrier; it does not enlarge the scope. Therefore promotion is bound exactly to the listed finite packet and its displayed kernel rows.
\end{proof}
\leanchecked{BEDC.Derived.MinkowskiRateGeometryUp.MinkowskiRateGeometryScopeBinding}

\begin{theorem}[Minkowski rate geometry public certificate]
\label{thm:minkowski-rate-geometry-public-certificate}
The public certificate exported by $\MinkowskiRateGeometryUp$ is exactly the finite route
$$
G,X,R,L,D,H,C,P,N.
$$
It consumes the displayed $\MultiHistConfigUp$, $\CrossHistCausalUp$, $\MaxCausalRateUp$, and $\LorentzFrameRateUp$ rows, then exposes only the rate-induced distance-shape ledger, componentwise transport, continuation route, provenance, and local name. It refuses metric tensors, host real coordinates, synchronized clocks, global frames, and ambient Lorentz manifolds as public certificate fields.
\end{theorem}
\begin{proof}
Start with the carrier envelope of \autoref{def:minkowski-rate-geometry-carrier} and \autoref{thm:minkowski-rate-geometry-carrier-envelope}. These identify the complete public inventory as $G,X,R,L,D,H,C,P,N$.

Apply the NameCert obligations, observer symmetry, non-escape, ledger exactness, and scope binding from \autoref{thm:minkowski-rate-geometry-namecert-obligations}, \autoref{thm:minkowski-rate-geometry-symmetry}, \autoref{thm:minkowski-rate-geometry-nonescape}, \autoref{thm:minkowski-rate-geometry-ledger-exactness}, and \autoref{thm:minkowski-rate-geometry-scope-binding}. They force every distance-shape read to pass through the upstream multi-Hist, causal, maximum-rate, and Lorentz-frame-rate rows before it reaches $D$.

Finally use the TasteGate marker
$$
\texttt{BEDC.Derived.MinkowskiRateGeometryUp.MinkowskiRateGeometryTasteGate\_single\_carrier\_alignment}.
$$
It records field faithfulness for the single carrier and does not admit any extra coordinate. Hence the certificate is public exactly at the listed finite route and refuses continuum Lorentz data outside that route.
\end{proof}

\falsifiablePrediction{Deleting the observer-symmetry row permits two observers over the same MaxCausalRateUp packet to read incompatible geometry rows, while deleting the MaxCausalRateUp row leaves no rate-induced distance boundary.}

\independenceWitness{Nearest siblings: multihistconfig, crosshistcausal, maxcausalrate, lorentzframerate, and observerlocalitytransport; no bijection exists because this carrier adds a symmetric distance-shape ledger that each sibling explicitly lacks or refuses.}

\closureat{MinkowskiRateGeometryUp}{scopedStr}

\begin{closurestatus}{\MinkowskiRateGeometryUp}
  \constructivestory{A $\MinkowskiRateGeometryUp$ packet is generated from a multi-Hist configuration, finite cross-Hist causal rows, a maximum-rate row, an observer-symmetric Lorentz-frame-rate row, a symmetric distance-shape ledger, componentwise $\hsame$ transport, displayed $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:minkowski-rate-geometry-carrier}, \autoref{thm:minkowski-rate-geometry-namecert-obligations}, \autoref{thm:minkowski-rate-geometry-symmetry}, \autoref{thm:minkowski-rate-geometry-nonescape}, \autoref{thm:minkowski-rate-geometry-tastegate-obligation}, \autoref{thm:minkowski-rate-geometry-carrier-envelope}, \autoref{thm:minkowski-rate-geometry-classifier-stability}, \autoref{thm:minkowski-rate-geometry-ledger-exactness}, and \autoref{thm:minkowski-rate-geometry-scope-binding} specify the finite rate-to-geometry packet, its NameCert obligations, observer-symmetry boundary, non-escape surface, TasteGate instance surface, carrier envelope, classifier stability, ledger exactness, and finite scope binding.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a metric tensor, coordinate chart, synchronized clock, global frame, ambient Lorentz manifold, host real-valued speed, curved spacetime, Lean verification, or bridge checking for $\MinkowskiRateGeometryUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, upstream exactness, distance-shape exactness, observer symmetry, route stability, non-escape, and TasteGate nontriviality with field faithfulness.}
\end{closurestatus}
