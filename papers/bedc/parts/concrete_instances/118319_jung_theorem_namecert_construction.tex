\chapter{A Concrete Naming Certificate for $\JungTheoremUp$}
\label{ch:concrete-instances-jung-theorem-namecert}
\origin{human}

Following Jung's theorem and compact metric radius bounds, the $\JungTheoremUp$ packet records the finite BEDC surface by which a displayed compact metric source exports a radius-bound handoff through Chebyshev-center and finite-projection rows. It sits after the compact-metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the Chebyshev-center certificate of \autoref{ch:concrete-instances-chebyshev-center-namecert}, the finite metric projection boundary of \autoref{ch:concrete-instances-finite-metric-projection-namecert}, and the terminal real-radius seal of \autoref{ch:concrete-instances-real-namecert}. The packet names one constructive radius-bound route; it is not a host inner-product theorem, selected center principle, quotient compactum, ambient completeness claim, or choice-based extremal argument.

\begin{definition}[Jung theorem carrier]
\label{def:jung-theorem-carrier}
A $\JungTheoremUp$ carrier is a finite $\BHist$ packet
$$
J=(K,M,C,F,R,B,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, including its finite-net and compactness-facing observation schedule.
\item $M$ is the underlying $\MetricUp$ distance row read by every radius comparison.
\item $C$ is the $\ChebyshevCenterUp$ center-candidate row, exposing the finite center ledger before any radius claim is read.
\item $F$ is the $\FiniteMetricProjectionUp$ projection row, recording the finite subconfiguration on which the radius comparison is witnessed.
\item $R$ is the $\RealUp$ radius-seal row carrying the displayed bound as real-name data.
\item $B$ is the Jung-bound handoff row, recording that $K,M,C,F$ are consumed before $R$ is exported.
\item $H$ records componentwise $\hsame$ transport for the compact source, metric distance row, center row, projection row, radius seal, bound handoff, replay row, provenance row, and local naming row.
\item $T$ records displayed $\Cont$ replay for the same row order.
\item $P$ is the $\Pkg$ provenance over $\JungTheoremUp$, $\CompactMetricUp$, $\MetricUp$, $\ChebyshevCenterUp$, $\FiniteMetricProjectionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\JungTheoremUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,M,C,F,R,B,H,T,P,N$. It has no coordinate for host Euclidean structure, selected centers, quotient compacta, ambient completeness, or choice.
\end{definition}

\begin{theorem}[Jung theorem NameCert obligations]
\label{thm:jung-theorem-namecert-obligations}
For every accepted $\JungTheoremUp$ carrier
$$
J=(K,M,C,F,R,B,H,T,P,N),
$$
the local $\NameCert_{\JungTheoremUp}$ surface consists exactly of carrier admission for the displayed compact metric source, metric distance row, Chebyshev-center row, finite-projection row, radius seal, bound handoff, transport, replay, provenance, and local naming row; compact-metric dependency exposure through $K$; distance comparison exposure through $M$; center-candidate exposure through $C$; finite witness exposure through $F$; real-radius sealing through $R$; and nonescape from host geometry, selected centers, quotient compacta, ambient completeness, and choice.

Every downstream consumer of this packet must read the compact metric source before reading the metric distance row, read the Chebyshev-center and finite-projection rows before reading the Jung-bound handoff, and read the $\RealUp$ radius row only after that finite route is displayed.
\end{theorem}
\begin{proof}
Project the accepted carrier through \autoref{def:jung-theorem-carrier}. The compact source row $K$ and metric row $M$ are the only places where compactness-facing and distance-facing data occur, so every radius request begins with those rows.

The center and witness data are then forced through $C$ and $F$. The row $C$ exposes the Chebyshev-center candidate ledger, while $F$ records the finite metric projection that witnesses the displayed radius comparison. No other carrier coordinate can supply a center or finite subconfiguration.

Finally $B,R,H,T,P,N$ hand off, seal, transport, replay, source, and name the same finite route. Since the carrier has no row for host Euclidean structure, selected centers, quotient compacta, ambient completeness, or choice, the listed clauses exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Jung theorem radius-bound handoff]
\label{thm:jung-theorem-radius-bound-handoff}
Every accepted $\JungTheoremUp$ carrier hands a compact-metric consumer only the displayed finite route
$$
K,M \longmapsto C,F \longmapsto B \longmapsto R \longmapsto H,T,P,N .
$$
The handoff records a radius-bound interface over $\CompactMetricUp$, $\MetricUp$, $\ChebyshevCenterUp$, $\FiniteMetricProjectionUp$, and $\RealUp$ rows. It does not export a host geometric proof, a selected minimizing center, a quotient compact object, an ambient completeness theorem, or a choice principle.
\end{theorem}
\begin{proof}
Start with the compact-metric and metric rows $K,M$. They provide the displayed source and distance data that a radius-bound consumer is allowed to inspect.

Next read the center and projection rows $C,F$. These rows make the finite Chebyshev-center candidate and finite metric witness visible before the bound handoff is consumed.

The handoff and real-radius rows $B,R$ then expose the stated bound as $\RealUp$ data. The structural rows $H,T,P,N$ preserve the same finite route under transport, replay, provenance, and local naming, so no host geometry, selected center, quotient compactum, ambient completeness, or choice-based extremal principle is introduced.
\end{proof}

\closureat{JungTheoremUp}{seedStr}

\begin{closurestatus}{\JungTheoremUp}
  \constructivestory{A $\JungTheoremUp$ packet is generated from a compact metric source row, metric distance row, Chebyshev-center row, finite-projection row, real-radius seal, bound handoff, componentwise transport, displayed replay, package provenance, and local naming row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:jung-theorem-carrier}, \autoref{thm:jung-theorem-namecert-obligations}, and \autoref{thm:jung-theorem-radius-bound-handoff} seed the finite compact-metric radius-bound route through CompactMetricUp, MetricUp, ChebyshevCenterUp, FiniteMetricProjectionUp, RealUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host Euclidean geometry, selected centers, quotient compacta, ambient completeness, Lean theorem verification, or bridge checking.}
  \upgradepath{The next paper-side grade requires a full obligation package over carrier admission, compact-metric dependency exposure, distance comparison exposure, center and projection rows, radius sealing, transport, replay, provenance, and nonescape.}
\end{closurestatus}
