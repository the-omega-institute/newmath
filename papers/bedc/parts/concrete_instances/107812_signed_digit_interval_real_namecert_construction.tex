\chapter{A Concrete Naming Certificate for $\SignedDigitIntervalRealUp$}
\label{ch:concrete-instances-signed-digit-interval-real-namecert}
\origin{human}

Following Avizienis and Bishop--Bridges, the $\SignedDigitIntervalRealUp$ packet records a closed real interval whose endpoints are supplied by signed-digit finite-window streams, normalized dyadic endpoint rows, StreamName schedules, RegSeqRat readbacks, and a terminal RealUp seal. It consumes the signed-digit stream surface of \autoref{ch:concrete-instances-signeddigitstream-namecert}, the signed-digit real endpoint route of \autoref{ch:concrete-instances-signeddigitreal-namecert}, the dyadic normalization ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite schedule interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the closed bounded interval carrier of \autoref{ch:concrete-instances-closedboundedinterval-namecert}. The packet gives Real and Completion consumers a finite-window interval source without exporting a quotient stream, a selected infinite expansion, or ambient real completeness.

\begin{definition}[Signed digit interval real carrier]
\label{def:signed-digit-interval-real-carrier}
A $\SignedDigitIntervalRealUp$ carrier is a finite $\BHist$ packet
$$
I=(L,U,D,O,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the lower signed-digit endpoint row, carrying the finite signed-digit windows admitted by the signed-digit stream and signed-digit real routes.
\item $U$ is the upper signed-digit endpoint row, carrying the corresponding finite signed-digit windows for the upper endpoint.
\item $D$ is the $\DyadicRatCoreUp$ normalization ledger, recording the dyadic endpoint rows obtained from the inspected signed-digit windows.
\item $O$ is the endpoint-order row, recording that the normalized lower endpoint is contained below the normalized upper endpoint through the displayed dyadic comparisons.
\item $S$ is the finite $\StreamNameUp$ schedule, listing exactly the endpoint windows and precision requests inspected by the packet.
\item $R$ is the $\RegSeqRatUp$ readback row for the normalized lower and upper endpoint observations.
\item $E$ is the $\RealUp$ seal row exposed only after the signed-digit endpoint rows, dyadic ledger, order row, schedule, and regular readback have been displayed.
\item $H$ records componentwise $\hsame$ transport for the endpoint rows, dyadic ledger, order row, schedule, readback, seal, replay, provenance, and local naming row.
\item $C$ records displayed $\Cont$ replay from signed-digit windows through dyadic normalization, endpoint ordering, regular readback, and the RealUp seal.
\item $P$ is the $\Pkg$ provenance over $\SignedDigitIntervalRealUp$, $\SignedDigitStreamUp$, $\SignedDigitRealUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\ClosedBoundedIntervalUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\SignedDigitIntervalRealUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,U,D,O,S,R,E,H,C,P,N$. It has no coordinate for quotient stream equality, a selected infinite signed-digit expansion, real trichotomy, a selected point of the interval, open-cover compactness, countable choice, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Signed digit interval real NameCert obligations]
\label{thm:signed-digit-interval-real-namecert-obligations}
For every accepted $\SignedDigitIntervalRealUp$ carrier
$$
I=(L,U,D,O,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\SignedDigitIntervalRealUp}$ obligations are exhausted by carrier admission for the eleven displayed rows; signed-digit endpoint normalization through $L,U,D$; dyadic containment through the normalized endpoint ledger $D$; endpoint-order preservation through $O$; regular readback through $R$ along the finite schedule $S$; terminal RealUp sealing through $E$; componentwise transport through $H$; continuation replay through $C$; package provenance through $P$; and local naming through $N$.

No obligation may call on quotient stream equality, a selected infinite expansion, trichotomy, a selected point of the interval, open-cover compactness, countable choice, or ambient RealUp completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:signed-digit-interval-real-carrier}. The endpoint rows $L$ and $U$ are the only signed-digit sources available to the packet, and the schedule row $S$ records the finite windows through which those sources are inspected.

The dyadic and order obligations are then read through $D$ and $O$. The ledger $D$ normalizes the inspected endpoint windows into dyadic rows, while $O$ records the displayed lower-before-upper comparison needed by the closed interval carrier.

The regular readback and seal obligations pass through $R$ and $E$. The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route, so the listed rows exhaust the local certificate and leave the excluded principles outside the packet.
\end{proof}

\begin{theorem}[Signed digit interval real seal nonescape]
\label{thm:signed-digit-interval-real-seal-nonescape}
An accepted $\SignedDigitIntervalRealUp$ carrier exports its interval-facing RealUp seal only through
$$
L,U,D,O,S,R,E,H,C,P,N.
$$
Consequently every consumer of the packet receives a finite signed-digit endpoint route, dyadic containment ledger, endpoint-order row, regular readback, and closed-interval seal; it receives no quotient stream equality, selected infinite expansion, trichotomy, selected point of the interval, open-cover compactness, countable choice, or ambient RealUp completeness.
\end{theorem}
\begin{proof}
Start with the endpoint rows. The lower and upper endpoints are visible only as the finite signed-digit windows displayed by $L$ and $U$ along the schedule $S$.

Normalize those endpoint windows through $D$ and compare them through $O$. This is the only place where the carrier records containment of the lower endpoint below the upper endpoint, and it is expressed as a finite dyadic ledger rather than as trichotomy or quotient real equality.

The RealUp-facing read then passes through $R,E,H,C,P,N$. Since the carrier has no further public coordinates, any interval consumer is confined to the displayed finite route and none of the excluded selection, compactness, choice, or completeness principles can escape through the seal.
\end{proof}

\closureat{SignedDigitIntervalRealUp}{seedStr}

\begin{closurestatus}{\SignedDigitIntervalRealUp}
  \constructivestory{A $\SignedDigitIntervalRealUp$ packet is generated from lower and upper signed-digit endpoint rows, a DyadicRatCore normalization ledger, an endpoint-order row, StreamName finite schedules, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:signed-digit-interval-real-carrier}, \autoref{thm:signed-digit-interval-real-namecert-obligations}, and \autoref{thm:signed-digit-interval-real-seal-nonescape} seed the finite signed-digit endpoint route for closed RealUp intervals.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected infinite signed-digit expansions, trichotomy, selected interval points, open-cover compactness, countable choice, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, endpoint normalization, dyadic containment, endpoint-order preservation, regular readback, RealUp seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.SignedDigitIntervalRealUp.}
\end{closurestatus}
