\begin{theorem}[CompleteMetricLimitUniqueness regular-Cauchy real route]
\label{thm:complete-metric-limit-uniqueness-regular-route}
Every uniqueness read consumed by a $\CompleteMetricLimitUniquenessUp$ packet factors through the displayed $\CompleteMetricUp$ separatedness rows of \autoref{ch:concrete-instances-completemetric-namecert}, the $\RegularCauchyUniquenessUp$ tolerance-exactness route of \autoref{ch:concrete-instances-regular-cauchy-uniqueness-namecert}, the $\RegSeqRatUp$ readback rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the $\StreamNameUp$ finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the $\DyadicRatCoreUp$ tolerance rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the $\RealUp$ sealing route of \autoref{ch:concrete-instances-real-namecert}, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\CompleteMetricLimitUniquenessUp}$ row. Thus any regular-Cauchy consumer reaches uniqueness only through
$$
\CompleteMetricUp,\RegularCauchyUniquenessUp,\RegSeqRatUp,\StreamNameUp,\DyadicRatCoreUp,\RealUp,\hsame,\Cont,\Pkg,\NameCert_{\CompleteMetricLimitUniquenessUp}
$$
before it may read the result.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:complete-metric-limit-uniqueness-carrier}. Its two limit rows, metric-distance witness, zero-distance row, separated classifier, transport, replay, provenance, and local name are the only rows visible to a uniqueness consumer.

Apply the common-tail and zero-distance routes, \autoref{thm:complete-metric-limit-uniqueness-common-tail-contraction} and \autoref{thm:complete-metric-limit-uniqueness-zero-distance-reflection}. They force the comparison through the same regular-Cauchy readback and the same Real zero-distance seal before separatedness is inspected.

Now read the finite-window siblings. The $\RegSeqRatUp$ source, $\StreamNameUp$ windows, and $\DyadicRatCoreUp$ tolerance rows provide the regular-Cauchy exactness surface, while the $\RegularCauchyUniquenessUp$ sibling supplies the tolerance uniqueness boundary used by regular consumers.

Finally use separatedness and the sibling lattice route, \autoref{thm:complete-metric-limit-uniqueness-separatedness} and \autoref{thm:complete-metric-limit-uniqueness-sibling-route}. The structural rows preserve that same finite route through $\hsame$, $\Cont$, $\Pkg$, and local naming, so the consumer cannot read quotient stream equality, a selected limit, countable choice, ambient $\RealUp$ completeness, or host equality of metric points.
\end{proof}
