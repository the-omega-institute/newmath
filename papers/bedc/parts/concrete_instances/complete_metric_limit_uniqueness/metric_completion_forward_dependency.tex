CompleteMetricLimitUniquenessUp reads MetricCompletionUp only through the displayed CompleteMetricUp limit rows and the CauchyFilterCompletionUp source route before the zero-distance and separated-classifier handoff. The dependency is therefore a finite forward route through \autoref{ch:concrete-instances-completemetric-namecert} and \autoref{ch:concrete-instances-cauchyfiltercompletion-namecert}; it does not collapse into host equality or an ambient completeness claim.
