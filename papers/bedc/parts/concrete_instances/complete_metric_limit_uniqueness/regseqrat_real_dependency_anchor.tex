\begin{theorem}[Complete metric limit uniqueness RegSeqRat--Real dependency anchor]
\label{thm:complete-metric-limit-uniqueness-regseqrat-real-dependency-anchor}
Every separated $\CompleteMetricLimitUniquenessUp$ read must expose the $\RegSeqRatUp$ readback of \autoref{ch:concrete-instances-regseqrat-namecert} and the $\RealUp$ zero-distance seal of \autoref{ch:concrete-instances-real-namecert} before separatedness publishes uniqueness. The public dependency route factors through the existing Cauchy-filter route, the Cauchy-limit-seal route, and the completion-regular-Cauchy child route; it exports no selected limit, quotient representative equality, host equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:complete-metric-limit-uniqueness-carrier}. Its public uniqueness read is available only after the source row, two limit rows, metric witness, zero-distance row, separatedness row, transport, replay, provenance, and local naming row have been displayed.

Use the Cauchy-filter and Cauchy-limit-seal child routes. They place the regular Cauchy data and the $\RealUp$ zero-distance seal before the separated classifier can publish uniqueness.

Now apply the completion-regular-Cauchy child route. It identifies the $\RegSeqRatUp$ readback as the finite regular-rational route consumed by the completion-facing read. Since the structural rows only transport, replay, source, and name that same finite packet, separatedness receives exactly the RegSeqRat--Real dependency anchor stated above.
\end{proof}
