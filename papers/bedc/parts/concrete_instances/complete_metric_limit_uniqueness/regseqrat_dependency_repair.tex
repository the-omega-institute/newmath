\begin{theorem}[CompleteMetricLimitUniqueness RegSeqRat dependency route]
\label{thm:complete-metric-limit-uniqueness-regseqrat-dependency}
Every $\CompleteMetricLimitUniquenessUp$ read that reaches a separated completion limit comparison must expose the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert} before it consumes the separated-completion boundary of \autoref{ch:concrete-instances-separatedcompletion-namecert}, the separated limit uniqueness packet of \autoref{ch:concrete-instances-separated-limit-uniqueness-namecert}, or the local separated-completion limit route of \autoref{thm:complete-metric-limit-uniqueness-separated-completion-limit-route}. The public route remains the finite packet
$$
\RegSeqRatUp,\StreamNameUp,\DyadicRatCoreUp,\RealUp,\SeparatedCompletionUp,\SeparatedLimitUniquenessUp,\CompleteMetricLimitUniquenessUp
$$
with the $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows attached to the same readback surface.
\end{theorem}
\begin{proof}
Start with the regular-Cauchy real route \autoref{thm:complete-metric-limit-uniqueness-regular-route}. It already places the $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows before any zero-distance comparison can be consumed.

Next use the separated-completion limit route \autoref{thm:complete-metric-limit-uniqueness-separated-completion-limit-route}. That route admits the separated-completion boundary only after the completion source and zero-distance read have been fixed, so the separated comparison cannot be read from a detached completion object.

Finally attach the separated limit uniqueness packet through \autoref{ch:concrete-instances-separated-limit-uniqueness-namecert}. Its carrier reads the two complete-metric limit rows, the common comparison route, the metric distance witness, the Real zero-distance row, and the separated classifier in one displayed packet. Thus the RegSeqRat readback and separated completion limit dependency remain one finite NameCert route, with no quotient completion equality, selected limit, host metric equality, countable choice, or ambient $\RealUp$ completeness coordinate.
\end{proof}
