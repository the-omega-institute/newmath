The $\CompleteMetricLimitUniquenessUp$ packet consumes $\RegSeqRatUp$ regular readback from \autoref{ch:concrete-instances-regseqrat-namecert}, $\SeparatedCompletionUp$ zero-distance separation from \autoref{ch:concrete-instances-separatedcompletion-namecert}, $\MetricCompletionUp$ completion witnesses from \autoref{ch:concrete-instances-metriccompletion-namecert}, and $\RealUp$ seal rows from \autoref{ch:concrete-instances-real-namecert} before exporting the uniqueness route.
