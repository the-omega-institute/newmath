\begin{theorem}[Complete metric limit uniqueness regular-Cauchy forward link]
\label{thm:complete-metric-limit-uniqueness-regular-cauchy-forward-link}
Every consumer that reads a separated $\CompleteMetricLimitUniquenessUp$ result after a regular-Cauchy completion seal first exposes the regular rational readback rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}, and the metric-completion boundary of \autoref{ch:concrete-instances-metriccompletion-namecert}. Only after those rows are visible may it consume the complete-metric separatedness route of \autoref{ch:concrete-instances-completemetric-namecert} and the local uniqueness row of $\CompleteMetricLimitUniquenessUp$.

Thus the forward dependency lattice is
$$
\RegSeqRatUp,\RealUp,\MetricCompletionUp,\CompleteMetricUp
\longrightarrow
\CompleteMetricLimitUniquenessUp,
$$
with the arrow mediated by displayed $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows. The link exports no selected limit, quotient Cauchy stream equality, countable-choice schedule, host completion object, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from a regular-Cauchy completion presentation. The regular rational readback rows must be public before the Real seal is available, so the consumer first reads \autoref{ch:concrete-instances-regseqrat-namecert} and \autoref{ch:concrete-instances-real-namecert}.

Next pass through the metric-completion boundary. The completion row supplies the displayed completion-facing carrier, while the complete-metric sibling supplies the separatedness surface used by a uniqueness read.

Finally consume the local $\CompleteMetricLimitUniquenessUp$ row through the existing separated-route and sibling-lattice theorems in this chapter. Since every source row has already been displayed through $\hsame$, $\Cont$, $\Pkg$, and local naming, the forward link cannot add a selected limit, quotient stream equality, countable choice, a host completion object, or ambient $\RealUp$ completeness.
\end{proof}
