The $\CompleteMetricLimitUniquenessUp$ packet consumes the separated metric reflection of \autoref{ch:concrete-instances-separatedmetricreflection-namecert} and the completion uniqueness route of \autoref{ch:concrete-instances-metric-completion-uniqueness-namecert} before asserting uniqueness for a displayed limit route. This child dependency keeps the two candidate limit rows over the same completion witness until separatedness turns the zero-distance row into a uniqueness handoff; it exports no quotient representative, selected limit, ambient completeness principle, or host equality of metric points.
