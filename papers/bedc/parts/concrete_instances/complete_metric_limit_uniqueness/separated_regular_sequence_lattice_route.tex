\begin{theorem}[Complete metric limit uniqueness separated regular-sequence lattice route]
\label{thm:complete-metric-limit-uniqueness-separated-regular-sequence-lattice-route}
Every $\CompleteMetricLimitUniquenessUp$ uniqueness read that consumes the separated identity route also consumes the regular-sequence Real route. The route cites the separated metric certificate of \autoref{ch:concrete-instances-separatedmetric-namecert}, the regular-sequence schedule of \autoref{ch:concrete-instances-regular-sequence-space-namecert}, and the Real seal of \autoref{ch:concrete-instances-real-namecert} before the two candidate limit rows can be identified.
\end{theorem}
\begin{proof}
Start with the displayed carrier of \autoref{def:complete-metric-limit-uniqueness-carrier}. The candidate limit rows are read over the same completion source before any separatedness conclusion is available.

Use the regular-sequence route through \autoref{thm:complete-metric-limit-uniqueness-regular-sequence-space-route}. It exposes the regular-sequence schedule and the Real zero-distance seal as visible rows.

Only then apply the separated metric dependency route. The separated classifier turns the displayed zero-distance seal into uniqueness, while the route exports no quotient representative, selected endpoint, ambient completeness principle, or host equality of points.
\end{proof}
