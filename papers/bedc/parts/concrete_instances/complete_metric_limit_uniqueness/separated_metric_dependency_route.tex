\begin{theorem}[Complete metric separated dependency route]
\label{thm:complete-metric-limit-uniqueness-separated-metric-dependency-route}
Every $\CompleteMetricLimitUniquenessUp$ uniqueness read consumes the complete-metric service of \autoref{ch:concrete-instances-completemetric-namecert} and the separated-metric classifier of \autoref{ch:concrete-instances-separatedmetric-namecert} before it compares two displayed limit seals. In carrier notation the read must first expose the shared source $A$, the two complete-metric limit rows $L_{0},L_{1}$, the metric comparison row $M$, the zero-distance row $Z$, and only then the separated classifier row $S$.

Consequently the public comparison is a finite separated complete-metric route over $A,L_{0},L_{1},M,Z,S,H,C,P,N$, not host point equality, quotient representative equality, selected-limit data, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the complete-metric dependency. The two rows $L_{0}$ and $L_{1}$ are readable only as complete-metric limit rows over the shared source $A$, so the consumer cannot compare detached endpoints before the complete-metric service has been displayed.

Next expose the comparison rows. The metric witness $M$ and the zero-distance row $Z$ provide the finite distance-zero evidence needed for a separated read, while still withholding host equality and quotient representative data.

Finally consume the separated-metric classifier of \autoref{ch:concrete-instances-separatedmetric-namecert}. The row $S$ turns the displayed zero-distance comparison into the uniqueness handoff, and the structural rows $H,C,P,N$ transport, replay, source, and name only that same route.
\end{proof}
