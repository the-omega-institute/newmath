The $\CompleteMetricLimitUniquenessUp$ packet consumes separatedness through the separated metric certificate of \autoref{ch:concrete-instances-separatedmetric-namecert} and reads completion-source data through \autoref{ch:concrete-instances-metriccompletion-namecert}. This sibling route keeps the two candidate limit rows over the same completion source before the separated classifier can turn the displayed zero-distance row into a uniqueness handoff; it exports no host equality of metric points, quotient representative, selected limit, or ambient completeness principle.
