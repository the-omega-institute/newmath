\begin{theorem}[Complete-metric dependency route for limit uniqueness]
\label{thm:complete-metric-limit-uniqueness-complete-metric-dependency-route}
Every $\CompleteMetricLimitUniquenessUp$ uniqueness read first consumes the separated completion row supplied by \autoref{ch:concrete-instances-completemetric-namecert}. The uniqueness surface then exposes only the displayed complete-metric limit comparison, so downstream consumers receive separated limit uniqueness rather than a host equality of completion representatives.
\end{theorem}
\begin{proof}
Read the complete-metric carrier before the uniqueness row. Its separated completion data supplies the metric source, completion witness, and separation boundary needed by a limit comparison.

The $\CompleteMetricLimitUniquenessUp$ packet then packages the two candidate limit rows and the Cauchy witness through its local dependency ledger. Since the route is mediated by the complete-metric separated row, the exposed conclusion is the finite uniqueness handoff and not a quotient representative equality.
\end{proof}
