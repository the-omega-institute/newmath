Every $\CompleteMetricLimitUniquenessUp$ dossier read that is consumed by completion-lattice readers must first expose the downstream route through the complete-metric service of \autoref{ch:concrete-instances-completemetric-namecert}, the Cauchy-filter completion service of \autoref{ch:concrete-instances-cauchyfiltercompletion-namecert}, and the separated metric reflection service of \autoref{ch:concrete-instances-separatedmetricreflection-namecert}. Only after those three rows have been displayed may a public uniqueness read leave the packet; the route therefore records a finite completion-to-separatedness dependency rather than an ambient equality principle.
