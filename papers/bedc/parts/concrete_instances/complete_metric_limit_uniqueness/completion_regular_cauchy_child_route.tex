The $\CompleteMetricLimitUniquenessUp$ child route consumes the displayed $\CompleteMetricUp$ rows of \autoref{ch:concrete-instances-completemetric-namecert}, the $\MetricCompletionUp$ completion boundary, the $\RegSeqRatUp$ regular-Cauchy readback, the $\StreamNameUp$ finite observation windows, and the terminal $\RealUp$ seal before a uniqueness read is exported. The route records this dependency lattice as finite sibling data: complete-metric limits are compared only after the completion row, regular-rational tolerance rows, stream windows, and Real zero-distance seal have been displayed, and the export remains a NameCert uniqueness read rather than a selected limit, quotient representative equality, countable-choice schedule, or host completeness principle.
