The $\CompleteMetricLimitUniquenessUp$ packet consumes the separated-complete metric comparison route of \autoref{ch:concrete-instances-completemetric-namecert} together with the regular-sequence space of \autoref{ch:concrete-instances-regular-sequence-space-namecert}, the regular-rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, and the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}. This dependency keeps the uniqueness comparison horizontal in the dossier lattice: before the $\RealUp$ zero-distance seal and separated-reflection uniqueness can be consumed, the chapter reads CompleteMetric separation, regular-sequence readback, regular rational tails, stream windows, transport, replay, provenance, and local naming as displayed siblings rather than as a kernel-only float.
