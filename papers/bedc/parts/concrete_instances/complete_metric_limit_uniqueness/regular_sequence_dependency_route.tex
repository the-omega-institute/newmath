The $\CompleteMetricLimitUniquenessUp$ packet consumes the separated-complete metric comparison route of \autoref{ch:concrete-instances-completemetric-namecert} together with the regular-sequence readback surface of \autoref{ch:concrete-instances-regseqrat-namecert} before a uniqueness read reaches the $\RealUp$ seal. This dependency keeps the uniqueness comparison horizontal in the dossier lattice: the chapter reads CompleteMetric separation, regular rational tails, transport, replay, provenance, and local naming as displayed siblings rather than as a kernel-only float.
