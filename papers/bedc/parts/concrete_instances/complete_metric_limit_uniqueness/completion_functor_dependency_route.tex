The $\CompleteMetricLimitUniquenessUp$ packet consumes the separated-completion functor of \autoref{ch:concrete-instances-separated-completion-functor-namecert} and the Cauchy-completion functoriality route of \autoref{ch:concrete-instances-cauchy-completion-functoriality-namecert} only after the two displayed complete-metric limit rows, the zero-distance row, and the separated classifier have been exposed in the same carrier. The dependency route therefore keeps the two candidate limit reads over one visible completion source before any functorial or separated-completion consumer may export uniqueness.

This child route adds no equality principle beyond the local packet. It records that downstream uniqueness readers must still pass through the paired limit rows, the common zero-distance certificate, the separated classifier, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\CompleteMetricLimitUniquenessUp}$ row before the functorial completion surface is consumed.
