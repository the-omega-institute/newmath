\noindent The $\CompleteMetricLimitUniquenessUp$ comparison route consumes the finite $\RegularCauchyFilterBasisUp$ basis corridor of \autoref{ch:concrete-instances-regular-cauchy-filter-basis-namecert} before it compares completion-limit rows. Thus a limit-uniqueness consumer first reads the regular Cauchy filter-basis windows, rational readback, finite meet, transport, replay, provenance, and local NameCert surface, and only then compares the separated completion-limit coordinates carried by this chapter.
