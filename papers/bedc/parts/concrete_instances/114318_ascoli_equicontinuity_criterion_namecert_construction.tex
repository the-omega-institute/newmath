\chapter{A Concrete Naming Certificate for $\AscoliEquicontinuityCriterionUp$}
\label{ch:concrete-instances-ascoli-equicontinuity-criterion-namecert}
\origin{human}

Following Arzela and Ascoli, and the Bishop--Bridges finite-net reading of compact metric continuity, the $\AscoliEquicontinuityCriterionUp$ packet records the criterion by which compact finite-net data and equicontinuity data are displayed before a uniform-modulus route is accepted. It sits between the compact source of \autoref{ch:concrete-instances-compactmetric-namecert}, the family-radius surface of \autoref{ch:concrete-instances-equicontinuity-namecert}, the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the finite epsilon-net ledger of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}, and the metric carrier discipline of \autoref{ch:concrete-instances-metric-namecert}. The chapter names the finite handoff criterion only; it does not assert open-cover compactness, a host function-space compactness theorem, quotient equality of maps, selected subsequences, countable choice, or ambient completeness.

\begin{definition}[Ascoli equicontinuity criterion carrier]
\label{def:ascoli-equicontinuity-criterion-carrier}
An $\AscoliEquicontinuityCriterionUp$ carrier is a finite $\BHist$ packet
$$
A=(K,E,U,F,M,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact metric source row, read through $\CompactMetricUp$ and the underlying $\MetricSpaceUp$ distance classifier.
\item $E$ is the $\EquicontinuityUp$ family-radius row, displaying one source-radius ledger for the inspected family at each requested target tolerance.
\item $U$ is the $\UniformModulusUp$ handoff row, exposed only after the compact source and equicontinuity rows have been displayed.
\item $F$ is the $\FiniteEpsilonNetUp$ row, carrying the finite center bundle, membership witnesses, and coverage-distance ledger for the selected tolerance scale.
\item $M$ records the metric comparison rows over centers and arbitrary inspected points, including the triangle and tolerance-budget data used to pass from finite centers to the whole compact source.
\item $H$ records componentwise $\hsame$ transport for compact source, equicontinuity, uniform-modulus, finite-net, metric, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a criterion request through the finite route $K,E,F,M,U$.
\item $P$ is the $\Pkg$ provenance over $\AscoliEquicontinuityCriterionUp$, $\CompactMetricUp$, $\EquicontinuityUp$, $\UniformModulusUp$, $\FiniteEpsilonNetUp$, $\MetricSpaceUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\AscoliEquicontinuityCriterionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these nine rows. No coordinate stores an arbitrary open cover, a host compactness proof for a function space, quotient equality of functions, a selected subsequence, countable choice, or ambient completeness.
\end{definition}

\begin{theorem}[Ascoli equicontinuity criterion NameCert obligations]
\label{thm:ascoli-equicontinuity-criterion-namecert-obligations}
For every accepted $\AscoliEquicontinuityCriterionUp$ carrier $A=(K,E,U,F,M,H,C,P,N)$, the local $\NameCert_{\AscoliEquicontinuityCriterionUp}$ surface consists exactly of carrier admission for the compact metric row, equicontinuity family row, uniform-modulus handoff row, finite epsilon-net row, metric comparison row, componentwise transport, displayed replay, provenance, and local naming; finite-net coverage for the compact source; equicontinuity visibility over the same tolerance request; metric triangle transport from net centers to inspected points; and non-escape for open-cover compactness, host function-space compactness, quotient equality, selected subsequences, countable choice, and ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:ascoli-equicontinuity-criterion-carrier}. The finite $\BHist$ packet exposes exactly $K,E,U,F,M,H,C,P,N$, so carrier admission and local naming have no hidden coordinate beyond the displayed rows.

The compact source is read through $K$ and $F$. The $\CompactMetricUp$ row supplies the compact metric discipline, while the $\FiniteEpsilonNetUp$ row displays the center bundle and coverage witnesses for the tolerance scale that the criterion will use.

The family-radius part is read through $E$. The $\EquicontinuityUp$ row is tied to the same tolerance request as the finite-net row, so the accepted packet cannot choose a separate radius ledger after the centers have been selected.

The metric comparison row $M$ transports estimates from centers to arbitrary inspected points. It supplies the triangle and tolerance-budget rows needed before $U$ may be read as a $\UniformModulusUp$ handoff.

Finally $H,C,P,N$ transport, replay, source, and name only this route. Since the carrier has no coordinate for open-cover compactness, host function-space compactness, quotient equality, selected subsequences, countable choice, or ambient completeness, those objects are outside the local NameCert surface.
\end{proof}

\begin{theorem}[Ascoli equicontinuity criterion finite-net handoff]
\label{thm:ascoli-equicontinuity-criterion-finite-net-handoff}
Every accepted $\AscoliEquicontinuityCriterionUp$ consumer that exports a uniform-modulus read must first display the finite route
$$
\CompactMetricUp,\ \EquicontinuityUp,\ \FiniteEpsilonNetUp,\ \MetricSpaceUp,\ \UniformModulusUp
$$
inside the carrier $A=(K,E,U,F,M,H,C,P,N)$. The handoff exports exactly the compact source row, family-radius row, finite center bundle, metric comparison ledger, structural transport, replay, provenance, and local naming rows, and it exports no open-cover compactness, host function-space compactness, quotient equality of maps, selected subsequence, countable-choice schedule, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start with \autoref{thm:ascoli-equicontinuity-criterion-namecert-obligations}. It lists the accepted rows available to a uniform-modulus consumer and requires the compact metric source, equicontinuity row, finite epsilon-net row, metric comparison row, and structural rows to be present.

Read the finite-net handoff through $F$. The center bundle and coverage witnesses are finite, and the source metric comparison rows in $M$ are the only route from a displayed center estimate to an arbitrary inspected source point.

Read the equicontinuity ledger through $E$ before the uniform-modulus row $U$. The same tolerance request is visible to $E$, $F$, and $M$, so the consumer receives one finite radius-and-center budget rather than an unrecorded analytic compactness argument.

The replay row $C$ then orders the handoff as $K,E,F,M,U$, while $H,P,N$ preserve transport, provenance, and local naming for the same route. Because the carrier omits all host compactness, quotient, selection, choice, and ambient-completeness coordinates, the uniform-modulus read is exhausted by the displayed finite packet.
\end{proof}

\falsifiablePrediction{Every accepted $\AscoliEquicontinuityCriterionUp$ uniform-modulus consumer first displays CompactMetric, Equicontinuity, FiniteEpsilonNet, MetricSpace, BHist, hsame, Cont, Pkg, and local NameCert rows before it may read the UniformModulus handoff.}

\independenceWitness{compactmetric, equicontinuity, uniformmodulus, finite-epsilon-net, metric: no listed sibling is bijective to $\AscoliEquicontinuityCriterionUp$ because only this packet jointly binds compact-source coverage, family-radius visibility, finite-net centers, metric triangle transport, uniform-modulus handoff, componentwise transport, replay, provenance, and local naming in one criterion carrier.}

\closureat{AscoliEquicontinuityCriterionUp}{seedStr}

\begin{closurestatus}{\AscoliEquicontinuityCriterionUp}
  \constructivestory{An $\AscoliEquicontinuityCriterionUp$ packet is generated from a CompactMetricUp source row, an EquicontinuityUp family-radius row, a UniformModulusUp handoff row, a FiniteEpsilonNetUp center bundle, MetricSpaceUp comparison rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:ascoli-equicontinuity-criterion-carrier}, \autoref{thm:ascoli-equicontinuity-criterion-namecert-obligations}, and \autoref{thm:ascoli-equicontinuity-criterion-finite-net-handoff} seed the finite Ascoli equicontinuity criterion route from compact finite nets and family equicontinuity into a uniform-modulus handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, host function-space compactness, quotient equality of maps, selected subsequences, countable choice, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-net coverage, equicontinuity visibility, metric triangle transport, uniform-modulus handoff exactness, non-escape, provenance, replay, and local naming for BEDC.Derived.AscoliEquicontinuityCriterionUp.}
\end{closurestatus}
