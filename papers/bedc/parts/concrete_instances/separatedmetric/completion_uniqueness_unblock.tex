\begin{theorem}[SeparatedMetric zero-distance Real reflection]
\label{thm:separatedmetric-zero-distance-real-reflection}
Let $S=(X,D,Z,L_0,L_1,E,H,C,P,N)$ be an accepted $\SeparatedMetricUp$ carrier. Every completion-facing read that reflects a zero-distance claim into a Real-facing comparison must first display the $\MetricUp$ distance rows of $X$, the $\RealUp$ zero-distance seal $D$, the separated classifier row $Z$, the $\CompleteMetricUp$ limit rows $L_0,L_1$, the $\CauchySequenceEquivalenceUp$ handoff $E$, and the structural rows $H,C,P,N$.

The reflection is a finite NameCert route through $X,D,Z,L_0,L_1,E,H,C,P,N$. It is not quotient completion equality, selected limit equality, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier and read the Real-facing rows first. The metric source $X$ supplies the distance witness, and $D$ reads that witness as a $\RealUp$ zero-distance seal before the separated classifier $Z$ may be exported.

For completion-facing requests, use the completion rows $L_0,L_1$ and the handoff $E$. These rows bind the two proposed completion readings to the displayed $\CompleteMetricUp$ and $\CauchySequenceEquivalenceUp$ data before the zero-distance route is consumed.

Transport and replay are then componentwise through $H$ and $C$, with $P,N$ sourcing and naming the same route. Since the carrier has no quotient equality, selected limit, trichotomy, or ambient Real-completeness coordinate, the reflection is exhausted by the displayed rows.
\end{proof}

\begin{theorem}[SeparatedMetric Cauchy-equivalence limit coherence]
\label{thm:separatedmetric-cauchy-equivalence-limit-coherence}
Let $S=(X,D,Z,L_0,L_1,E,H,C,P,N)$ be an accepted $\SeparatedMetricUp$ carrier. If a downstream completion consumer reads the two $\CompleteMetricUp$ limit rows $L_0,L_1$ as coherent, then the coherence read must pass through the $\CauchySequenceEquivalenceUp$ row $E$, the $\MetricUp$ row $X$, the $\RealUp$ zero-distance row $D$, the separated classifier row $Z$, and the structural rows $H,C,P,N$.

Thus completion uniqueness is exported only as Cauchy-equivalence limit coherence followed by separated zero-distance classification. It is not ambient completion equality, a selected representative theorem, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the two limit rows. The carrier exposes $L_0$ and $L_1$ only as $\CompleteMetricUp$ rows paired with the Cauchy-equivalence handoff $E$.

Read $E$ before any classifier conclusion. It supplies the finite Cauchy-sequence comparison route that lets the two limit rows be placed over the same metric source $X$.

The zero-distance claim is then read through $D$ and consumed by $Z$. The transport, replay, provenance, and local naming rows $H,C,P,N$ preserve exactly this route for downstream consumers.

No quotient completion object, selected limit, trichotomy row, or ambient completeness theorem is present in the carrier. Therefore the only exported coherence is the displayed Cauchy-equivalence and separated-classifier route.
\end{proof}

\begin{theorem}[SeparatedMetric NameCert ledger exhaustion]
\label{thm:separatedmetric-namecert-ledger-exhaustion}
For an accepted $\SeparatedMetricUp$ carrier $S=(X,D,Z,L_0,L_1,E,H,C,P,N)$, the completion-uniqueness ledger is exhausted by the rows $X,D,Z,L_0,L_1,E,H,C,P,N$. Every $\MetricUp$, $\RealUp$, $\CompleteMetricUp$, and $\CauchySequenceEquivalenceUp$ consumer that asks for separated completion uniqueness must project those rows before it can read the local $\NameCert_{\SeparatedMetricUp}$ conclusion.

No additional ledger row is available for quotient completion equality, selected limits, trichotomy, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier inventory. It lists the metric source, Real zero-distance seal, separated classifier, two complete-metric limit rows, Cauchy-equivalence handoff, transport, replay, provenance, and local name.

Apply the zero-distance reflection route of \autoref{thm:separatedmetric-zero-distance-real-reflection}. That route accounts for Real-facing zero-distance reads without adding a host equality coordinate.

Apply the limit-coherence route of \autoref{thm:separatedmetric-cauchy-equivalence-limit-coherence}. That route accounts for completion-facing uniqueness reads by sending $L_0,L_1$ through $E,X,D,Z,H,C,P,N$.

These two projections cover the completion-uniqueness ledger. Since no further row exists in the carrier, every excluded equality, selection, trichotomy, or ambient completeness principle is refused by ledger exhaustion.
\end{proof}
