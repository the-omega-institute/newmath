\chapter{A Concrete Naming Certificate for $\PhilosophyMapTargetUp$}
\label{ch:concrete-instances-philosophy-map-target-namecert}
\origin{ai}

The $\PhilosophyMapTargetUp$ packet realizes the formal target draft of \autoref{subsec:philosophy-package-formal-target-draft} as a finite NameCert carrier. It sits beside the concept registry surface of \autoref{ch:concrete-instances-concept-registry-surface-namecert}, the core registry index of \autoref{ch:concrete-instances-core-registry-index-namecert}, the audit system of \autoref{ch:concrete-instances-auditsystem-namecert}, the axiom-purity gate of \autoref{ch:concrete-instances-axiom-purity-gate-namecert}, and the closure-status reference of \autoref{ch:concrete-instances-closure-status-reference-namecert}. Those siblings register concepts, index registry rows, audit accepted packets, rule out host-level axioms, and display closure ledgers; this carrier binds them into one philosophy-map target only when every exported coordinate remains a named BEDC row.

\begin{definition}[Philosophy map target carrier]
\label{def:philosophy-map-target-carrier}
A $\PhilosophyMapTargetUp$ carrier is a finite $\BHist$ packet
$$
M=(R,G,K,S,A,X,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the registry-map row, read through $\ConceptRegistrySurfaceUp$ and $\CoreRegistryIndexUp$, naming concept entries, theorem entries, tradition entries, scientific entries, and registry-route entries;
\item $G$ is the gap-map row, naming typed gaps, formal target rows, missing bridge rows, and not-claimed surfaces;
\item $K$ is the cannot-claim row, recording blocked readings for subject substance, global truth, host equality, quotient import, choice import, and hidden far-end objects;
\item $S$ is the closure-status row, read through $\ClosureStatusReferenceUp$, recording paper closure, verification status, bridge status, and not-claimed scope for every exported row;
\item $A$ is the audit row, read through $\AuditSystemUp$ and $\AxiomPurityGateUp$, recording the finite audit route that rejects forbidden host-equality, quotient, choice, and global-truth reads;
\item $X$ is the synthesis row, recording the philosophy-map target named in \autoref{def:philosophy-map-target} as registry maps, gap maps, cannot-claim conflict checks, closure-status rows, no-smuggling refusal, and audit evidence;
\item $H$ records componentwise $\hsame$ transport for $R,G,K,S,A,X$;
\item $C$ records displayed $\Cont$ replay through the registry maps, gap maps, cannot-claim checks, closure rows, audit rows, and synthesis row;
\item $P$ is the $\Pkg$ provenance over $\ConceptRegistrySurfaceUp$, $\CoreRegistryIndexUp$, $\AuditSystemUp$, $\AxiomPurityGateUp$, $\ClosureStatusReferenceUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\PhilosophyMapTargetUp}$ row.
\end{enumerate}
The carrier has no coordinate for a prose-only philosophical map, an unchecked truth predicate, a hidden host equality, a quotient witness, a choice operator, or a closure-status read outside the displayed packet.
\end{definition}
\leanchecked{BEDC.Derived.PhilosophyMapTargetUp.TasteGate.PhilosophyMapTargetTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Philosophy map target NameCert obligations]
\label{thm:philosophy-map-target-namecert-obligations}
For the carrier of \autoref{def:philosophy-map-target-carrier}, the local $\NameCert_{\PhilosophyMapTargetUp}$ surface consists exactly of five finite obligation rows:
\begin{enumerate}
\item carrier admission for $M=(R,G,K,S,A,X,H,C,P,N)$;
\item registry-map coverage through $R$, requiring every concept, theorem, tradition, scientific, and route entry to factor through $\ConceptRegistrySurfaceUp$ and $\CoreRegistryIndexUp$;
\item gap-map and cannot-claim coverage through $G$ and $K$, requiring every missing proof, formal target, blocked reading, and cannot-claim conflict to be displayed before export;
\item closure-status and audit coverage through $S$ and $A$, requiring every paper closure, verification grade, bridge status, not-claimed surface, and axiom-purity refusal to be read through $\ClosureStatusReferenceUp$, $\AuditSystemUp$, and $\AxiomPurityGateUp$;
\item structural preservation through $X,H,C,P,N$, requiring the synthesis row, componentwise transport, displayed replay routes, package provenance, and local naming to preserve the same finite packet.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.PhilosophyMapTargetUp.PhilosophyMapTargetTasteGate\_single\_carrier\_alignment}
\begin{proof}
Project the carrier of \autoref{def:philosophy-map-target-carrier}. The rows $R,G,K,S,A,X$ expose the registry, gap, cannot-claim, closure-status, audit, and synthesis data before any philosophy-map target can be accepted.

The registry row factors through the concept registry surface and core registry index. The gap and cannot-claim rows keep missing proof sites, formal targets, blocked readings, and conflict checks as displayed coordinates rather than prose authority.

The closure-status and audit rows bind every exported reading to a visible closure ledger and to an axiom-purity route. The remaining rows $X,H,C,P,N$ transport, replay, source, and name that same packet, so the five listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Philosophy map target no-smuggling boundary]
\label{thm:philosophy-map-target-nosmuggling-boundary}
Every accepted $\PhilosophyMapTargetUp$ packet exposes philosophy-map content only through the rows $R,G,K,S,A,X,H,C,P,N$. If the no-smuggling row $K$ is absent, a consumer may request a forbidden host-equality, quotient, choice, or global-truth read; with $K$ present, no public consumer can read those coordinates from the packet.
\end{theorem}
\begin{proof}
Use \autoref{thm:philosophy-map-target-namecert-obligations}. The cannot-claim row $K$ is the only row that records the forbidden readings, while $A$ records the audit route that rejects them and $S$ records the closure-status boundary where they remain not claimed.

If $K$ is removed, the carrier no longer contains the displayed refusal row for host equality, quotient import, choice import, or global-truth import. A request for one of those readings is then no longer blocked by the accepted packet, because the remaining registry, gap, closure, audit, and synthesis rows do not themselves name every refusal coordinate.

With $K$ present, the refusal row is transported by $H$, replayed only through $C$, sourced by $P$, and named by $N$. A public consumer receives the finite packet and cannot project any forbidden coordinate not already displayed there.
\end{proof}

\begin{theorem}[Philosophy map target public law boundary]
\label{thm:philosophy-map-target-public-law-boundary}
Every public $\PhilosophyMapTargetUp$ export factors through the registry-map row $R$, gap-map row $G$, cannot-claim row $K$, closure-status row $S$, audit row $A$, synthesis row $X$, and structural rows $H,C,P,N$. The export therefore carries only the displayed registry, gap, refusal, closure, audit, synthesis, transport, replay, provenance, and naming coordinates, and no public consumer can read a prose-only philosophy map, unchecked truth predicate, hidden host equality, quotient witness, choice operator, or closure-status row outside the packet.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:philosophy-map-target-carrier}. It exposes exactly the rows $R,G,K,S,A,X,H,C,P,N$ and has no coordinate for a prose-only philosophical map, unchecked truth predicate, hidden host equality, quotient witness, choice operator, or external closure-status read.

Apply the NameCert obligations of \autoref{thm:philosophy-map-target-namecert-obligations}. Registry-map coverage is read through $R$, gap-map and cannot-claim coverage are read through $G$ and $K$, closure-status and audit coverage are read through $S$ and $A$, and the synthesis and structural obligations preserve the same finite packet through $X,H,C,P,N$.

Finally use the no-smuggling boundary of \autoref{thm:philosophy-map-target-nosmuggling-boundary}. Since the forbidden readings are blocked by $K$ and the audit and closure rows remain displayed, a public export can factor only through the named rows and cannot acquire an additional public-law coordinate.
\end{proof}

\falsifiablePrediction{Every accepted $\PhilosophyMapTargetUp$ packet whose no-smuggling row is removed admits a forbidden host-equality, quotient, choice, or global-truth read; with the row present no public consumer can read those coordinates.}

\independenceWitness{conceptregistrysurface, coreregistryindex, auditsystem, axiompuritygate, closurestatusreference: none is bijective because $\PhilosophyMapTargetUp$ simultaneously carries registry maps, gap maps, cannot-claim conflict checks, closure-status rows, and no-smuggling refusal in one packet.}

The Lean follow-up must provide $\Nontrivial\ \PhilosophyMapTargetUp$ and $\FieldFaithful\ \PhilosophyMapTargetUp$ instances in \texttt{lean4/BEDC/Derived/PhilosophyMapTargetUp/TasteGate.lean}. Those instances must preserve $R,G,K,S,A,X,H,C,P,N$ as separate displayed fields before the target $\mathsf{BEDC.Derived.PhilosophyMapTargetUp.TasteGate.PhilosophyMapTargetTasteGate\_single\_carrier\_alignment}$ can be cited by a checked paper marker.

\closureat{PhilosophyMapTargetUp}{seedStr}
\closureat{PhilosophyMapTargetUp}{obligationStr}
\closureat{PhilosophyMapTargetUp}{publicStr}[BEDC.Derived.PhilosophyMapTargetUp.PhilosophyMapTargetTasteGate\_single\_carrier\_alignment]

\begin{closurestatus}{\PhilosophyMapTargetUp}
  \constructivestory{A $\PhilosophyMapTargetUp$ packet is generated from registry-map, gap-map, cannot-claim, closure-status, audit, synthesis, componentwise hsame transport, displayed Cont replay, package provenance, and local NameCert rows.}
  \theoryclosure{\publicClosure}
  \scopeclosed{\autoref{def:philosophy-map-target-carrier}, \autoref{thm:philosophy-map-target-namecert-obligations}, \autoref{thm:philosophy-map-target-nosmuggling-boundary}, and \autoref{thm:philosophy-map-target-public-law-boundary} bind the displayed carrier, NameCert obligations, no-smuggling boundary, sibling scope, and public export over $\ConceptRegistrySurfaceUp$, $\CoreRegistryIndexUp$, $\AuditSystemUp$, $\AxiomPurityGateUp$, and $\ClosureStatusReferenceUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, philosophical completeness, global truth, host equality, quotient import, choice import, or all downstream registry entries.}
  \upgradepath{Lean closure requires Nontrivial PhilosophyMapTargetUp and FieldFaithful PhilosophyMapTargetUp instances in \texttt{lean4/BEDC/Derived/PhilosophyMapTargetUp/TasteGate.lean}.}
\end{closurestatus}
