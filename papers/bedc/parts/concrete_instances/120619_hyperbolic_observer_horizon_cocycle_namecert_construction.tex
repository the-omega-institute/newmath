\chapter{A Concrete Naming Certificate for $\HyperbolicObserverHorizonCocycleUp$}
\label{ch:concrete-instances-hyperbolic-observer-horizon-cocycle-namecert}
\origin{ai}

The $\HyperbolicObserverHorizonCocycleUp$ packet realizes the observer-horizon branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite boundary-shadow, Busemann-cocycle, phase-distance, transport, provenance, and refusal rows. It sits after the boundary-shadow cross-ratio route of \autoref{ch:concrete-instances-boundary-shadow-cross-ratio-namecert}, the hyperbolic observer phase route of \autoref{ch:concrete-instances-hyperbolic-observer-phase-namecert}, the phase-transport budget of \autoref{ch:concrete-instances-hyperbolic-phase-transport-budget-namecert}, the Gromov-boundary readback of \autoref{ch:concrete-instances-gromov-boundary-namecert}, and the visual-flow shadow route of \autoref{ch:concrete-instances-hyperbolic-visual-flow-shadow-namecert}. The packet names a finite cocycle ledger for horizon-facing observer reads; it is not a global hyperbolic model, quotient boundary theorem, selected geodesic ray, physical observer, or analytic ergodic statement.

\falsifiablePrediction{Every accepted $\HyperbolicObserverHorizonCocycleUp$ read is refuted if it reaches a phase-distance or horizon-cocycle conclusion without displaying the boundary-shadow, Busemann-cocycle, observer-horizon, transport, provenance, and refusal rows.}

\independenceWitness{boundary-shadow-cross-ratio, hyperbolic-observer-phase, hyperbolic-phase-transport-budget, gromov-boundary, hyperbolic-visual-flow-shadow: none is bijective to $\HyperbolicObserverHorizonCocycleUp$ because this packet jointly carries an observer horizon row, Busemann-cocycle ledger, phase-distance readback, transport replay, provenance, refusal, and local NameCert naming.}

Later Lean work must supply Nontrivial and FieldFaithful TasteGate instances before any positive discovery claim can be read from this packet. This chapter records the finite carrier and obligation surface only.

\begin{definition}[Hyperbolic observer horizon-cocycle carrier]
\label{def:hyperbolic-observer-horizon-cocycle-carrier}
A $\HyperbolicObserverHorizonCocycleUp$ carrier is a finite $\BHist$ packet
$$
O=(B,U,H,D,C,A,T,R,P,F,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the boundary-shadow row, recording the visible shadow window seen by the observer.
\item $U$ is the Busemann-cocycle row, recording the finite signed comparison between two displayed horizon reads.
\item $H$ is the observer-horizon row, tying the boundary-shadow and Busemann rows to a displayed observer expansion route.
\item $D$ is the phase-distance row, recording the finite readback consumed by phase-geometry consumers.
\item $C$ is the cocycle-consistency ledger, requiring additivity and basepoint transport to be read only through displayed finite rows.
\item $A$ is the apophatic refusal row, preventing the horizon far end from becoming an interior center, selected geodesic endpoint, or physical observer.
\item $T$ records componentwise $\hsame$ transport for boundary-shadow, Busemann-cocycle, horizon, phase-distance, cocycle ledger, refusal, replay, provenance, and local naming rows.
\item $R$ records displayed $\Cont$ replay from an observer request through $B,U,H,D,C,A,T$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicObserverHorizonCocycleUp$, $\BoundaryShadowCrossRatioUp$, $\HyperbolicObserverPhaseUp$, $\HyperbolicPhaseTransportBudgetUp$, $\HyperbolicVisualFlowShadowUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $F$ is the FieldFaithful readiness row, recording that later field-facing comparison must preserve the same finite phase-distance and Busemann-cocycle data.
\item $N$ is the local $\NameCert_{\HyperbolicObserverHorizonCocycleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,U,H,D,C,A,T,R,P,F,N$. It has no coordinate for a quotient visual boundary, selected geodesic ray, physical observer state, ambient hyperbolic manifold, analytic ergodic invariant, or hidden basepoint equality.
\end{definition}

\begin{theorem}[Hyperbolic observer horizon-cocycle NameCert obligations]
\label{thm:hyperbolic-observer-horizon-cocycle-namecert-obligations}
For every accepted $\HyperbolicObserverHorizonCocycleUp$ carrier
$$
O=(B,U,H,D,C,A,T,R,P,F,N),
$$
the local $\NameCert_{\HyperbolicObserverHorizonCocycleUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the boundary-shadow, Busemann-cocycle, observer-horizon, phase-distance, cocycle-ledger, refusal, transport, replay, provenance, FieldFaithful-readiness, and local naming rows;
\item boundary-shadow exposure, requiring every horizon read to display $B$ before it consumes the cocycle row $U$;
\item Busemann-cocycle exactness, requiring $U,C,T,R$ to preserve the same finite horizon comparison under basepoint transport and replay;
\item phase-distance handoff, requiring $D$ and $F$ to remain attached before any phase-geometry or field-facing consumer reads the packet;
\item apophatic refusal, requiring $A,P,N$ to keep selected rays, physical observers, quotient boundary equality, ambient manifolds, and analytic ergodic invariants outside the certificate surface.
\end{enumerate}
No observer horizon-cocycle obligation is stored outside $B,U,H,D,C,A,T,R,P,F,N$.
\end{theorem}
\begin{proof}
Project the accepted carrier from \autoref{def:hyperbolic-observer-horizon-cocycle-carrier}. The packet displays the boundary-shadow, Busemann-cocycle, observer-horizon, phase-distance, cocycle-ledger, refusal, transport, replay, provenance, FieldFaithful-readiness, and local naming rows.

Boundary-shadow exposure comes first. A horizon read reaches the Busemann row only after the visible shadow window $B$ has been displayed, so the horizon cannot be replaced by a hidden ideal-boundary representative.

Busemann-cocycle exactness is the joint role of $U,C,T,R$. The cocycle ledger $C$ records the finite comparison, while transport and replay preserve the same basepoint-facing route.

The phase-distance handoff uses $D$ and $F$. These rows keep the phase readback and later field-facing comparison attached to the same finite observer route.

Finally, the refusal row $A$ and provenance rows $P,N$ exclude selected rays, physical observer states, quotient boundary equality, ambient manifolds, and analytic ergodic invariants. Since none of those objects is a coordinate of $O$, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Hyperbolic observer horizon-cocycle phase handoff]
\label{thm:hyperbolic-observer-horizon-cocycle-phase-handoff}
Every phase-geometry consumer of an accepted $\HyperbolicObserverHorizonCocycleUp$ carrier
$$
O=(B,U,H,D,C,A,T,R,P,F,N)
$$
must read the displayed route
$$
B,U,H,D,C,A,T,R,P,F,N
$$
before treating the packet as horizon-cocycle data. The handoff exports finite boundary-shadow, Busemann-cocycle, phase-distance, transport, provenance, refusal, and readiness rows, not quotient boundary equality, selected rays, physical observer states, ambient hyperbolic manifolds, or analytic ergodic invariants.
\end{theorem}
\begin{proof}
Use the obligation theorem \autoref{thm:hyperbolic-observer-horizon-cocycle-namecert-obligations}. It places boundary-shadow exposure before Busemann-cocycle readback, then keeps the observer-horizon and phase-distance rows attached to the same finite route.

Read transport and replay next. Rows $T$ and $R$ preserve the displayed cocycle ledger $C$ rather than introducing an unshown basepoint equality or quotient boundary representative.

Finally apply the refusal and readiness rows. The apophatic row $A$ blocks selected-ray and physical-observer interpretations, while $F$ records only later FieldFaithful readiness. Thus a phase consumer receives the finite horizon-cocycle packet and no stronger analytic or quotient endpoint.
\end{proof}

\closureat{HyperbolicObserverHorizonCocycleUp}{seedStr}

\begin{closurestatus}{\HyperbolicObserverHorizonCocycleUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-observer-horizon-cocycle-carrier}, \autoref{thm:hyperbolic-observer-horizon-cocycle-namecert-obligations}, and \autoref{thm:hyperbolic-observer-horizon-cocycle-phase-handoff} seed the finite horizon-cocycle route from boundary-shadow and Busemann-cocycle rows through phase-distance, transport, provenance, refusal, readiness, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient visual-boundary equality, selected geodesic rays, physical observer states, ambient hyperbolic manifolds, analytic ergodic invariants, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, boundary-shadow exposure, Busemann-cocycle exactness, phase-distance handoff, apophatic refusal, Nontrivial TasteGate support, FieldFaithful TasteGate support, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicObserverHorizonCocycleUp.}
  \constructivestory{A $\HyperbolicObserverHorizonCocycleUp$ packet is generated from boundary-shadow row, Busemann-cocycle row, observer-horizon row, phase-distance row, cocycle-consistency ledger, apophatic refusal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, FieldFaithful readiness, and a local NameCert row.}
\end{closurestatus}
