\chapter{A Concrete Naming Certificate for $\MetaCICDecidableConversionFrontierUp$}
\label{ch:concrete-instances-metacic-decidable-conversion-frontier-namecert}
\origin{ai}

The $\MetaCICDecidableConversionFrontierUp$ packet realizes the bounded conversion boundary in \autoref{ch:metacic-decidable-audit-map} as finite NameCert data. It sits beside the decidability boundary of \autoref{ch:concrete-instances-metacicdecidableboundary-namecert}, the normalization frontier of \autoref{ch:concrete-instances-metacicnormalizationfrontier-namecert}, the bounded normal-conversion bridge of \autoref{ch:concrete-instances-bounded-normal-conversion-bridge-namecert}, the normal-form consistency seal of \autoref{ch:concrete-instances-normal-form-consistency-seal-namecert}, and the critical-path packet of \autoref{ch:concrete-instances-metaciccriticalpath-namecert}. The packet names the exact point where bounded normal equality may be consumed by a conversion-facing checker while the unrestricted closed-term conversion theorem remains outside the carrier.

\falsifiablePrediction{Every accepted $\MetaCICDecidableConversionFrontierUp$ read exposes metacicdecidableboundary, metacicnormalizationfrontier, bounded-normal-conversion-bridge, normal-form-consistency-seal, metaciccriticalpath, bounded-checker, finished-normal, obstruction, transport, replay, provenance, and local $\NameCert$ rows before any conversion-frontier handoff is consumed.}

\independenceWitness{metacicdecidableboundary, metacicnormalizationfrontier, bounded-normal-conversion-bridge, normal-form-consistency-seal, metaciccriticalpath: the decidability boundary records checker limits without the normal endpoint row; the normalization frontier records candidate and endpoint evidence without the conversion checker; the bridge records bounded normal comparison without the full audit map; the consistency seal records closed normal-form safety without conversion completeness; and the critical path records dependency pressure without this finite conversion-frontier carrier. None is bijective to $\MetaCICDecidableConversionFrontierUp$.}

\begin{definition}[MetaCIC decidable conversion frontier carrier]
\label{def:metacic-decidable-conversion-frontier-carrier}
A $\MetaCICDecidableConversionFrontierUp$ carrier is a finite $\BHist$ packet
$$
F=(D,N,B,S,K,Q,O,H,C,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\MetaCICDecidableBoundaryUp$ row, recording the typing-checker and same-term decision surface available to the audit map.
\item $N$ is the $\MetaCICNormalizationFrontierUp$ row, recording candidate evidence, finished-normal evidence, endpoint use, and retained obstruction data.
\item $B$ is the $\BoundedNormalConversionBridgeUp$ row, comparing bounded normalizer outputs under the displayed finished-normal boundary.
\item $S$ is the $\NormalFormConsistencySealUp$ row, recording the closed normal-form consistency seal consumed by conversion-facing checks.
\item $K$ is the $\MetaCICCriticalPathUp$ row, retaining the dependency-pressure ledger for conversion, normalization, confluence, and subject-reduction consumers.
\item $Q$ is the bounded equality-checker row, recording the compared normal forms, finite fuel budget, and Boolean equality output.
\item $O$ is the obstruction row, naming the excluded endpoints: unrestricted closed-term beta-conversion, arbitrary-fuel completeness, evaluator equality, quotient syntax equality, and kernel consistency.
\item $H$ records componentwise $\hsame$ transport for $D,N,B,S,K,Q,O$ and the local naming row.
\item $C$ records displayed $\Cont$ replay from a conversion query through $D,N,B,S,K,Q,O,H,P,L$.
\item $P$ is the $\Pkg$ provenance over $\MetaCICDecidableConversionFrontierUp$, $\MetaCICDecidableBoundaryUp$, $\MetaCICNormalizationFrontierUp$, $\BoundedNormalConversionBridgeUp$, $\NormalFormConsistencySealUp$, $\MetaCICCriticalPathUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\MetaCICDecidableConversionFrontierUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $D,N,B,S,K,Q,O,H,C,P,L$. It has no coordinate for unrestricted closed-term beta-conversion, arbitrary-fuel completeness, evaluator equality, quotient syntax equality, subject reduction, confluence, or kernel consistency.
\end{definition}

\begin{theorem}[MetaCIC decidable conversion frontier NameCert obligations]
\label{thm:metacic-decidable-conversion-frontier-namecert-obligations}
For every accepted $\MetaCICDecidableConversionFrontierUp$ carrier
$$
F=(D,N,B,S,K,Q,O,H,C,P,L),
$$
the local $\NameCert_{\MetaCICDecidableConversionFrontierUp}$ surface consists exactly of decidability-boundary exposure through $D$, normalization-frontier exposure through $N$, bounded normal-conversion bridge exposure through $B$, normal-form seal exposure through $S$, critical-path exposure through $K$, bounded equality-checker exposure through $Q$, obstruction retention through $O$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $L$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-decidable-conversion-frontier-carrier}. Rows $D,N,B,S,K$ are the sibling audit and bridge surfaces, while $Q$ is the local bounded checker row and $O$ keeps the excluded endpoints visible.

The conversion-facing read must pass through the bounded bridge and the normal-form seal together. The bridge supplies only bounded normal comparison, and the seal supplies the closed normal-form safety row; neither row contains arbitrary-fuel completeness or unrestricted beta-conversion.

Finally $H,C,P,L$ transport, replay, source, and name the same packet. Since no carrier coordinate contains evaluator equality, quotient syntax equality, subject reduction, confluence, or kernel consistency, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[MetaCIC decidable conversion frontier bounded route]
\label{thm:metacic-decidable-conversion-frontier-bounded-route}
Every conversion-facing consumer of an accepted $\MetaCICDecidableConversionFrontierUp$ carrier factors through
$$
\MetaCICDecidableBoundaryUp,\ \MetaCICNormalizationFrontierUp,\ \BoundedNormalConversionBridgeUp,\ \NormalFormConsistencySealUp,\ \MetaCICCriticalPathUp
$$
and then through the local bounded equality-checker row $Q$ with obstruction retention $O$. The route exports a bounded conversion frontier only; it does not prove unrestricted closed-term beta-conversion or arbitrary-fuel completeness.
\end{theorem}
\begin{proof}
Start with \autoref{thm:metacic-decidable-conversion-frontier-namecert-obligations}. It forces the consumer to read the decidability boundary, normalization frontier, bounded conversion bridge, consistency seal, and critical-path rows before using the local checker row.

The local checker row $Q$ is interpreted under the obstruction row $O$. Thus a positive bounded comparison can be replayed as finite NameCert data, but the retained obstruction prevents it from being cited as an unrestricted conversion theorem.

Transport and replay through $H$ and $C$, with provenance and naming through $P$ and $L$, preserve that boundary. No sibling route adds arbitrary-fuel completeness, evaluator equality, quotient syntax equality, subject reduction, confluence, or kernel consistency to the carrier.
\end{proof}

\closureat{MetaCICDecidableConversionFrontierUp}{seedStr}

\begin{closurestatus}{\MetaCICDecidableConversionFrontierUp}
  \constructivestory{A $\MetaCICDecidableConversionFrontierUp$ packet is generated from MetaCICDecidableBoundary, MetaCICNormalizationFrontier, BoundedNormalConversionBridge, NormalFormConsistencySeal, MetaCICCriticalPath, bounded equality-checker, obstruction, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-decidable-conversion-frontier-carrier}, \autoref{thm:metacic-decidable-conversion-frontier-namecert-obligations}, and \autoref{thm:metacic-decidable-conversion-frontier-bounded-route} seed the finite bounded-conversion frontier for MetaCIC decidability consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim unrestricted closed-term beta-conversion, arbitrary-fuel completeness, evaluator equality, quotient syntax equality, subject reduction, confluence, kernel consistency, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, bounded-checker exposure, normalization-frontier coverage, bounded bridge exactness, normal-form seal compatibility, critical-path retention, obstruction nonescape, transport, replay, provenance, and local naming for BEDC.Derived.MetaCICDecidableConversionFrontierUp.}
\end{closurestatus}
