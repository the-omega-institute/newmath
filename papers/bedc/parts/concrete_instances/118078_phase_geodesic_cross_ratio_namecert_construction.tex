\chapter{A Concrete Naming Certificate for $\PhaseGeodesicCrossRatioUp$}
\label{ch:concrete-instances-phase-geodesic-cross-ratio-namecert}
\origin{ai}

The $\PhaseGeodesicCrossRatioUp$ packet realizes the geodesic endpoint and cross-ratio branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It sits after the endpoint route of \autoref{ch:concrete-instances-hyperbolic-geodesic-boundary-namecert}, the four-boundary comparison packet of \autoref{ch:concrete-instances-hyperbolic-cross-ratio-namecert}, the noncrossing geodesic-lamination surface of \autoref{ch:concrete-instances-hyperbolic-geodesic-lamination-namecert}, the disk phase-transport route of \autoref{ch:concrete-instances-poincare-disk-phase-transport-namecert}, and the metric readback of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}. The packet names a finite phase-facing endpoint comparison, not a completed hyperbolic plane, quotient ideal boundary, selected geodesic representative, host Mobius invariant, or physical observer dynamics.

\begin{definition}[Phase geodesic cross-ratio carrier]
\label{def:phase-geodesic-cross-ratio-carrier}
A $\PhaseGeodesicCrossRatioUp$ carrier is a finite $\BHist$ packet
$$
G=(D,M,E_0,E_1,E_2,E_3,L,X,\Phi,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\PoincareDiskPhaseTransportUp$ disk and phase-transport row.
\item $M$ is the $\HyperbolicMetricUp$ row used to read endpoint and geodesic comparisons.
\item $E_0,E_1,E_2,E_3$ are four boundary endpoint rows, each attached to the displayed disk route rather than to a host ideal boundary.
\item $L$ is the $\HyperbolicGeodesicLaminationUp$ compatibility row, recording that the endpoint pairs form the finite geodesic leaves consumed by lamination-facing readers.
\item $X$ is the $\HyperbolicCrossRatioUp$ row comparing the four endpoints through the finite cross-ratio packet.
\item $\Phi$ is the phase-readout row, attaching the cross-ratio comparison to the visible phase-transport ledger without treating phase equality as source equality.
\item $R$ is the refusal row excluding completed hyperbolic planes, quotient ideal-boundary equality, selected geodesic representatives, host Mobius invariants, and physical observer dynamics.
\item $H$ records componentwise $\hsame$ transport for disk, metric, endpoint, lamination, cross-ratio, phase, refusal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from disk phase transport through metric, endpoints, lamination, cross-ratio, phase readout, and refusal.
\item $P$ is the $\Pkg$ provenance over $\PhaseGeodesicCrossRatioUp$, $\PoincareDiskPhaseTransportUp$, $\HyperbolicMetricUp$, $\HyperbolicGeodesicBoundaryUp$, $\HyperbolicGeodesicLaminationUp$, $\HyperbolicCrossRatioUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PhaseGeodesicCrossRatioUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,M,E_0,E_1,E_2,E_3,L,X,\Phi,R,H,C,P,N$. It has no coordinate for fewer than four endpoint rows, a detached phase scalar, a host global geometry, quotient boundary equality, or unrecorded geodesic selection.
\end{definition}

\begin{theorem}[Phase geodesic cross-ratio NameCert obligations]
\label{thm:phase-geodesic-cross-ratio-namecert-obligations}
For every accepted $\PhaseGeodesicCrossRatioUp$ carrier
$$
G=(D,M,E_0,E_1,E_2,E_3,L,X,\Phi,R,H,C,P,N),
$$
the local $\NameCert_{\PhaseGeodesicCrossRatioUp}$ surface is exhausted by disk phase transport, hyperbolic metric readback, four displayed endpoint rows, geodesic-lamination compatibility, hyperbolic cross-ratio comparison, phase readout, explicit refusal, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:phase-geodesic-cross-ratio-carrier}. The disk and metric rows $D,M$ fix the displayed geometry before any endpoint or phase comparison is visible.

The four endpoint rows $E_0,E_1,E_2,E_3$ are read next. Lamination compatibility $L$ ensures that the endpoint pairs are geodesic-facing finite leaves, and the cross-ratio row $X$ compares the same endpoints through \autoref{ch:concrete-instances-hyperbolic-cross-ratio-namecert}.

The phase row $\Phi$ is downstream from those geometric rows. It records only the visible phase readout attached to the same disk transport, while $R$ refuses host global geometry, quotient boundary equality, selected geodesics, detached Mobius invariance, and physical observer dynamics.

Finally $H,C,P,N$ transport, replay, source, and name the same packet. Since there is no further carrier coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Phase geodesic cross-ratio route]
\label{thm:phase-geodesic-cross-ratio-route}
Every phase-facing consumer of an accepted $\PhaseGeodesicCrossRatioUp$ packet factors through
$$
\PoincareDiskPhaseTransportUp \longmapsto \HyperbolicGeodesicBoundaryUp \longmapsto \HyperbolicGeodesicLaminationUp \longmapsto \HyperbolicCrossRatioUp .
$$
The consumer receives disk phase transport, hyperbolic metric readback, four endpoint rows, lamination compatibility, cross-ratio comparison, phase readout, refusal, transport, replay, provenance, and local naming; it receives no completed hyperbolic plane, quotient ideal boundary, selected geodesic representative, host Mobius invariant, or physical observer-dynamics row.
\end{theorem}
\begin{proof}
Use \autoref{thm:phase-geodesic-cross-ratio-namecert-obligations}. It places the disk transport and metric rows before the four endpoint rows can be inspected.

The endpoint rows then feed the lamination and cross-ratio rows $L$ and $X$. The lamination row keeps the geodesic leaves finite and displayed, while the cross-ratio row keeps the four-boundary comparison inside the existing hyperbolic cross-ratio packet.

Only after those rows are visible may $\Phi$ expose a phase readout. The refusal row $R$ and structural rows $H,C,P,N$ keep that readout attached to the displayed route, so the phase-facing consumer cannot escape to host global geometry, quotient boundary equality, selected geodesics, detached Mobius invariance, or physical observer dynamics.
\end{proof}

\falsifiablePrediction{An accepted $\PhaseGeodesicCrossRatioUp$ phase read that omits disk transport, hyperbolic metric readback, four endpoint rows, lamination compatibility, cross-ratio comparison, refusal, transport, replay, provenance, or local NameCert rows refutes the carrier surface.}

\independenceWitness{hyperbolic-geodesic-boundary, hyperbolic-geodesic-lamination, hyperbolic-cross-ratio, poincare-disk-phase-transport, hyperbolic-metric: the geodesic-boundary packet names endpoints without phase cross-ratio readout, the lamination packet names noncrossing leaves without the four-point phase row, the cross-ratio packet compares endpoints without owning the phase transport, the disk phase-transport packet names reversible phase surfaces without the geodesic endpoint comparison, and the metric packet supplies distance readback without the endpoint-phase carrier. None is bijective to $\PhaseGeodesicCrossRatioUp$.}

\closureat{PhaseGeodesicCrossRatioUp}{seedStr}

\begin{closurestatus}{\PhaseGeodesicCrossRatioUp}
  \constructivestory{A $\PhaseGeodesicCrossRatioUp$ packet is generated from PoincareDiskPhaseTransportUp rows, HyperbolicMetricUp readback, four HyperbolicGeodesicBoundaryUp endpoint rows, HyperbolicGeodesicLaminationUp compatibility, HyperbolicCrossRatioUp comparison, a phase readout row, explicit refusal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:phase-geodesic-cross-ratio-carrier}, \autoref{thm:phase-geodesic-cross-ratio-namecert-obligations}, and \autoref{thm:phase-geodesic-cross-ratio-route} seed the finite phase-facing route from geodesic endpoints to cross-ratio readout.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, completed hyperbolic planes, quotient ideal-boundary equality, selected geodesic representatives, host Mobius invariants, or physical observer dynamics.}
  \upgradepath{Obligation closure requires checked carrier admission, endpoint display, lamination compatibility, cross-ratio comparison, phase-readout nonescape, refusal, transport, replay, provenance, and local naming for BEDC.Derived.PhaseGeodesicCrossRatioUp.}
\end{closurestatus}
