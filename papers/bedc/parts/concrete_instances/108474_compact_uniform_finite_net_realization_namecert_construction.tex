\chapter{A Concrete Naming Certificate for $\CompactUniformFiniteNetRealizationUp$}
\label{ch:concrete-instances-compact-uniform-finite-net-realization-namecert}
\origin{human}

Following Lebesgue and Bishop--Bridges, the $\CompactUniformFiniteNetRealizationUp$ packet realizes the finite-net route of \autoref{ch:visions-compact-uniform-continuity} as concrete NameCert data. It reads the compact carrier of \autoref{ch:concrete-instances-compact-namecert}, the compact metric finite-net surface of \autoref{ch:concrete-instances-compactmetric-namecert}, the metric source rows of \autoref{ch:concrete-instances-metric-namecert}, the continuous-map and continuous-function rows of \autoref{ch:concrete-instances-continuousmap-namecert} and \autoref{ch:concrete-instances-continuous-namecert}, the rational lower-bound surface of \autoref{ch:concrete-instances-rat-namecert}, the finite-net fold of \autoref{ch:concrete-instances-finitenetminimumfold-namecert}, and the source-lock packet of \autoref{ch:concrete-instances-metricspace-compact-continuity-source-lock-namecert}. The packet records only finite $\mathsf{ProbeBundle}$ data, local pointwise-modulus rows, rational folds, and the resulting uniform-modulus handoff; it does not claim open-cover compactness, selected subsequences, countable choice, or a semantic Heine--Cantor theorem outside the displayed BEDC rows.

\begin{definition}[Compact uniform finite-net realization carrier]
\label{def:compact-uniform-vision-finite-net-realization}
A $\CompactUniformFiniteNetRealizationUp$ carrier is a finite $\BHist$ packet
$$
K=(C,M,F,B,L,R,U,T,H,D,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $C$ is the $\CompactUp$ admission row, tying the source object to the compact naming certificate.
\item $M$ is the $\MetricUp$ source row, carrying the source-distance and target-distance ledgers.
\item $F$ is the $\ContinuousUp$ or $\ContinuousMapUp$ graph row, with pointwise modulus data readable at displayed source centers.
\item $B$ is the finite $\mathsf{ProbeBundle}$ row of compact-net centers and source-distance coverage witnesses.
\item $L$ is the local-radius ledger, assigning a positive $\RatUp$ radius to each bundled center from the pointwise-modulus row.
\item $R$ is the $\RatUp$ lower-bound fold row over the finite bundle, using only primitive recursion over displayed probes.
\item $U$ is the $\UniformModulusUp$ handoff row produced from the folded radius and the continuous graph rows.
\item $T$ is the metric triangle row, joining the source-center estimate, the source-pair estimate, and the target-distance estimate.
\item $H$ records componentwise $\hsame$ transport for $C,M,F,B,L,R,U,T$ and for the structural rows below.
\item $D$ records displayed $\Cont$ replay from compact finite-net inspection through pointwise-modulus reads, rational lower-bound folding, metric triangle estimates, and uniform-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformFiniteNetRealizationUp$, $\CompactUp$, $\CompactMetricUp$, $\MetricUp$, $\ContinuousUp$, $\ContinuousMapUp$, $\RatUp$, $\UniformModulusUp$, $\MetricTriangleUniformEstimateUp$, $\ProbeBundle$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformFiniteNetRealizationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $C,M,F,B,L,R,U,T,H,D,P,N$. It has no coordinate for open-cover compactness, a selected subcover outside the finite bundle, a selected subsequence, quotient equality of real values, or an external modulus function.
\end{definition}

\begin{theorem}[Compact finite-net modulus handoff]
\label{thm:compact-uniform-vision-modulus-handoff}
Every $\UniformModulusUp$ consumer of an accepted $\CompactUniformFiniteNetRealizationUp$ packet receives the compact-uniform-continuity route through the finite chain
$$
\CompactUp,\ \MetricUp,\ \ContinuousUp,\ \ProbeBundle,\ \RatUp
\longmapsto
\UniformModulusUp .
$$
The consumer first displays the compact metric source, then the finite ProbeBundle centers and coverage rows, then the pointwise-modulus radii supplied by the continuous row, then the rational lower-bound fold, and only then the metric-triangle estimate and uniform-modulus handoff.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-vision-finite-net-realization}. The rows $C,M,F,B,L,R,U,T,H,D,P,N$ are the complete displayed inventory.

Read the compact and metric source rows first. The compact row admits the source object, while the metric row supplies the distance ledgers used by both the finite-net cover and the target estimate.

Next read the finite bundle. The $\mathsf{ProbeBundle}$ row $B$ supplies exactly the displayed centers and source-distance coverage rows, and the continuous row $F$ supplies pointwise-modulus radii only at those centers. The local-radius ledger $L$ therefore remains tied to the finite compact-net data.

Now fold the radii. The $\RatUp$ row $R$ is obtained by primitive recursion over $B$, so the common lower bound is a classifier-stable history rather than a choice-selected minimum.

Finally the metric triangle row $T$ and uniform-modulus row $U$ export the handoff, with $H,D,P,N$ transporting, replaying, sourcing, and naming the same finite surface. Thus the realization supplies the vision route as NameCert data without importing open-cover compactness, selected subsequences, countable choice, or an external modulus selector.
\end{proof}

\begin{theorem}[Compact uniform finite-net realization nonescape]
\label{thm:compact-uniform-finite-net-realization-nonescape}
Let $K=(C,M,F,B,L,R,U,T,H,D,P,N)$ be an accepted $\CompactUniformFiniteNetRealizationUp$ carrier. Any consumer that reaches the uniform-modulus handoff must factor through the same finite ProbeBundle row $B$, the same $\RatUp$ lower-bound fold $R$, the same continuous-map pointwise-modulus row $F$, and the same metric triangle row $T$. No alternate path inside the carrier exports open-cover compactness, a selected subcover outside $B$, a selected subsequence, or a host-level modulus function.
\end{theorem}
\begin{proof}
Start from the output row $U$. It is stored after $C,M,F,B,L,R,T$ in the carrier, so the uniform-modulus handoff cannot be read without the compact source, metric source, continuous row, finite bundle, local radii, rational fold, and triangle estimate.

The finite bundle is the only cover coordinate. Since $B$ is the sole row containing centers and coverage witnesses, any source point used by the handoff must be routed through one of the displayed probe names.

The lower-bound and triangle rows are likewise unique inside the packet. The fold row $R$ compares only the radii displayed by $L$, and $T$ is the only row converting source estimates and pointwise target estimates into the final uniform estimate.

Transport, replay, provenance, and local naming preserve this route rather than adding new analytic data. Hence every consumer of $U$ remains inside the finite NameCert surface described by the compact-uniform-continuity vision.
\end{proof}

\falsifiablePrediction{An accepted $\CompactUniformFiniteNetRealizationUp$ read is refuted if a UniformModulusUp handoff is reached without displaying the CompactUp row, MetricUp source row, ContinuousUp pointwise-modulus row, finite ProbeBundle centers, RatUp lower-bound fold, metric triangle row, transport, replay, provenance, and local naming rows.}

\independenceWitness{compact, compactmetric, metric, continuous, rat, probebundle, finitenetminimumfold, and uniformmodulus: none is bijective to the finite-net realization packet because only $\CompactUniformFiniteNetRealizationUp$ records the compact-uniform-continuity vision route from finite ProbeBundle coverage through rational folding to the UniformModulusUp handoff.}

\closureat{CompactUniformFiniteNetRealizationUp}{seedStr}

\begin{closurestatus}{\CompactUniformFiniteNetRealizationUp}
  \constructivestory{A $\CompactUniformFiniteNetRealizationUp$ packet is generated from CompactUp admission rows, MetricUp source rows, ContinuousUp or ContinuousMapUp pointwise-modulus rows, finite ProbeBundle center and coverage rows, RatUp lower-bound folds, UniformModulusUp handoff rows, MetricTriangleUniformEstimateUp rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-vision-finite-net-realization}, \autoref{thm:compact-uniform-vision-modulus-handoff}, and \autoref{thm:compact-uniform-finite-net-realization-nonescape} seed the finite ProbeBundle realization route for compact-uniform-continuity consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subcovers outside the finite bundle, selected subsequences, countable choice, semantic Heine--Cantor theorems, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact source exposure, metric source exposure, continuous pointwise-modulus exposure, finite ProbeBundle coverage, RatUp lower-bound folding, metric triangle exactness, uniform-modulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformFiniteNetRealizationUp.}
\end{closurestatus}
