\chapter{A Concrete Naming Certificate for $\BishopCantorDiagonalArgumentUp$}
\label{ch:concrete-instances-bishop-cantor-diagonal-argument-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopCantorDiagonalArgumentUp$ packet records the constructive diagonal boundary at which a displayed countable stream of real-name candidates is met by a finite window, dyadic separation budget, and terminal Real-facing handoff. It sits after the dyadic arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the diagonal refusal surface for an inspected countable-name list; it does not assert quotient real equality, host real inequality, a selected representative for every stream, countable choice, or ambient completeness of $\RealUp$.

\begin{definition}[Bishop Cantor diagonal argument carrier]
\label{def:bishop-cantor-diagonal-argument-carrier}
A $\BishopCantorDiagonalArgumentUp$ carrier is a finite $\BHist$ packet
$$
D=(L,W,Q,A,S,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the inspected countable-name ledger, exposing only the finitely requested $\StreamNameUp$ entries and their local index labels.
\item $W$ is the diagonal finite-window row, selecting the inspected entry, observed digit window, and comparison position used by the diagonal read.
\item $Q$ is the $\DyadicRatCoreUp$ separation budget attached to exactly the selected window.
\item $A$ is the local apartness row stating that the diagonal readback differs from the selected ledger entry at the displayed dyadic window.
\item $S$ is the $\StreamNameUp/\RegSeqRatUp$ readback row that turns the diagonal finite windows into a regular rational name.
\item $R$ is the terminal $\RealUp$ handoff row reached only after $L,W,Q,A,S$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the ledger, window, dyadic budget, apartness row, readback, Real handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from ledger inspection through diagonal-window selection, dyadic separation, regular readback, and the terminal Real handoff.
\item $P$ is the $\Pkg$ provenance over $\BishopCantorDiagonalArgumentUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopCantorDiagonalArgumentUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,W,Q,A,S,R,H,C,P,N$. It has no coordinate for quotient reals, host real equality, selected representatives, a global enumeration of all reals, countable choice, excluded middle, or ambient completeness.
\end{definition}

\begin{theorem}[Bishop Cantor diagonal argument NameCert obligations]
\label{thm:bishop-cantor-diagonal-argument-namecert-obligations}
For every accepted $\BishopCantorDiagonalArgumentUp$ carrier
$$
D=(L,W,Q,A,S,R,H,C,P,N),
$$
the local $\NameCert_{\BishopCantorDiagonalArgumentUp}$ surface consists exactly of five finite obligation rows:
\begin{enumerate}
\item ledger admission for the displayed countable-name entries in $L$;
\item diagonal-window admission for the selected finite observation row $W$;
\item dyadic separation admission for the budget $Q$ and apartness row $A$;
\item regular readback and Real-facing handoff through $S$ and $R$;
\item transport, replay, provenance, and local naming through $H,C,P,N$.
\end{enumerate}
These rows exhaust the local certificate and add no quotient real, host equality, global enumeration, selected representative, countable-choice, excluded-middle, or ambient-completeness coordinate.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-cantor-diagonal-argument-carrier}. Its countable-name data are confined to the inspected ledger row $L$, while the diagonal read is confined to $W,Q,A,S,R$ and the structural rows $H,C,P,N$.

The first two obligations read the finite inspection surface. Row $L$ exposes only the requested stream-name entries, and row $W$ records the selected entry and observation window used by the diagonal step.

The third obligation is the constructive separation budget. Row $Q$ supplies the displayed dyadic tolerance, and row $A$ attaches the diagonal apartness claim to that same finite window.

The fourth obligation is downstream. Row $S$ records the StreamName/RegSeqRat readback generated from the diagonal windows, and row $R$ serves the Real-facing handoff only after those rows are public.

Finally $H,C,P,N$ preserve, replay, source, and name the same packet. Since no other carrier coordinate exists, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Bishop Cantor diagonal argument Real handoff]
\label{thm:bishop-cantor-diagonal-argument-real-handoff}
Every $\RealUp$-facing consumer of an accepted $\BishopCantorDiagonalArgumentUp$ packet receives the diagonal read only through the route
$$
L \longmapsto W \longmapsto Q \longmapsto A \longmapsto S \longmapsto R \longmapsto H,C,P,N .
$$
Thus the consumer sees an inspected countable-name ledger, a finite diagonal window, a dyadic separation budget, a displayed apartness row, StreamName/RegSeqRat readback, a terminal Real handoff, transport, replay, provenance, and local naming. It cannot replace that route by quotient real equality, host real equality, selected representatives, countable choice, excluded middle, or ambient Real completeness.
\end{theorem}
\begin{proof}
Use the NameCert obligations of \autoref{thm:bishop-cantor-diagonal-argument-namecert-obligations}. They place ledger inspection, diagonal-window selection, dyadic separation, apartness, and regular readback before any terminal Real-facing row is available.

Read the handoff in route order. A consumer first sees the finite part of the countable-name ledger, then the diagonal observation window, then the dyadic budget and apartness row attached to that window.

Only after those rows are displayed can the consumer read $S$ as StreamName/RegSeqRat data and $R$ as the terminal $\RealUp$ handoff. The structural rows transport, replay, source, and name this same finite route.

The refusal clauses follow from the carrier inventory. Quotient real equality, host equality, selected representatives, countable choice, excluded middle, and ambient completeness are not fields of $D$, so they cannot be exported by the accepted handoff.
\end{proof}

\falsifiablePrediction{An accepted $\BishopCantorDiagonalArgumentUp$ consumer is refuted whenever it exposes a terminal $\RealUp$ diagonal handoff without first displaying the inspected ledger, diagonal finite window, dyadic separation budget, apartness row, StreamName/RegSeqRat readback, transport, replay, provenance, and local NameCert rows.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, bishop-l10-completion-terminal: no listed sibling carries the inspected countable-name ledger together with diagonal-window selection, dyadic apartness, regular readback, terminal Real handoff, transport, replay, provenance, and local NameCert rows for the constructive diagonal boundary.}

\closureat{BishopCantorDiagonalArgumentUp}{seedStr}

\begin{closurestatus}{\BishopCantorDiagonalArgumentUp}
  \constructivestory{A $\BishopCantorDiagonalArgumentUp$ packet is generated from an inspected countable-name ledger, diagonal finite-window selection, DyadicRatCore separation, apartness, StreamName/RegSeqRat readback, terminal RealUp handoff, hsame transport, Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-cantor-diagonal-argument-carrier}, \autoref{thm:bishop-cantor-diagonal-argument-namecert-obligations}, \autoref{thm:bishop-cantor-diagonal-argument-real-handoff}, the falsifiable prediction, and the independence witness seed the finite diagonal route from inspected countable names to Real-facing consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, host real equality, a global enumeration of all reals, selected representatives, countable choice, excluded middle, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked ledger admission, diagonal-window admission, dyadic separation, apartness readback, Real handoff, transport stability, replay stability, provenance non-escape, and local naming for BEDC.Derived.BishopCantorDiagonalArgumentUp.}
\end{closurestatus}
