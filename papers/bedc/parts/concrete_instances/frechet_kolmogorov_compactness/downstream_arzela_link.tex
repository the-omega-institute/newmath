\begin{theorem}[Frechet--Kolmogorov downstream Arzela--Ascoli link]
\label{thm:frechet-kolmogorov-downstream-arzela-link}
Every Arzela--Ascoli finite-net consumer of an accepted $\FrechetKolmogorovCompactnessUp$ packet must first read the $\CompactMetricUp$ source row, the $\TotallyBoundedUp$ finite-net row, the $\UniformEquicontinuityModulusUp$ row, the $\CompleteMetricUp$ completion boundary, the $\StreamNameUp$ observation windows, the $\RegSeqRatUp$ readback rows, and the $\RealUp$ seal rows carried by the displayed dependency route. The handoff exports no selected subsequence, selected subcover, open-cover compactness theorem, quotient completion equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the existing Frechet--Kolmogorov carrier through the compactness dependency route. The compact metric source and total-bounded finite-net rows are visible before any function-family compactness read is offered downstream.

Read the Arzela--Ascoli-facing rows next. The dependency route of \autoref{thm:frechet-kolmogorov-arzela-ascoli-dependency-route} and the compact handoff of \autoref{thm:frechet-kolmogorov-arzela-ascoli-compact-handoff} require the uniform-equicontinuity modulus before the finite-net consumer receives the family surface.

Finally inspect the completion and real-readback side. The compact metric total-bounded dependency of \autoref{thm:frechet-kolmogorov-compact-metric-total-bounded-dependency} keeps the $\CompleteMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows attached to the same finite route. Since no route theorem supplies a selected subsequence, selected subcover, open-cover compactness theorem, quotient completion equality, or ambient completeness coordinate, the downstream Arzela--Ascoli link exports only the displayed finite lattice.
\end{proof}
