The $\FrechetKolmogorovCompactnessUp$ packet exposes a real-sequence compactness read only after its compact-metric finite-net rows, bounded-function-family rows, and Arzela--Ascoli finite-window rows have all been displayed. Thus every downstream compactness consumer first passes through the compact source of \autoref{ch:concrete-instances-compactmetric-namecert}, the bounded family surface of \autoref{ch:concrete-instances-bounded-function-family-namecert}, and the Arzela--Ascoli route of \autoref{ch:concrete-instances-arzelaascoli-namecert}; the Frechet--Kolmogorov carrier then contributes only the finite $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows that assemble those dependencies into the real-sequence compactness read.
