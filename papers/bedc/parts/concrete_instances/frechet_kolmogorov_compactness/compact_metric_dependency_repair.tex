The $\FrechetKolmogorovCompactnessUp$ packet consumes the compact-metric total-bounded route from \autoref{ch:concrete-instances-compactmetric-namecert} and the finite extraction route of \autoref{ch:concrete-instances-arzelaascoli-namecert} before it can export a compactness criterion. Thus the compact metric source, finite extraction surface, transport rows, replay rows, and local $\NameCert$ data are visible as horizontal dependencies rather than as an isolated compactness kernel.
