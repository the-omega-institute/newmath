The compactness export of $\FrechetKolmogorovCompactnessUp$ is read through a compact-metric finite-net source together with the Arzela--Ascoli equicontinuity and tightness rows. The compact metric source fixes the finite-net budget; the Arzela--Ascoli sibling supplies the function-family tightness surface; the local replay rows then expose compactness only after both pieces have been displayed.

\begin{theorem}[Frechet--Kolmogorov compact-metric tightness route]
\label{thm:frechet-kolmogorov-compactness-tightness-route}
Every accepted $\FrechetKolmogorovCompactnessUp$ consumer that exports compactness for function-family windows must first read the compact-metric finite-net data of \autoref{ch:concrete-instances-compactmetric-namecert} and the equicontinuity/tightness rows of \autoref{ch:concrete-instances-arzelaascoli-namecert}. The export is therefore a finite dependency route through compact metric source rows, finite-net coverage rows, Arzela--Ascoli tightness rows, transport, replay, provenance, and local $\NameCert$ data, not a selected subsequence, open-cover compactness theorem, quotient function-family equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the Frechet--Kolmogorov carrier through its compactness-facing rows. The compact metric source supplies the finite-net data that any downstream compactness read must inspect before it can speak about a family window.

Next read the Arzela--Ascoli sibling route. Its equicontinuity and tightness rows are consumed after the compact-metric finite nets and before the local compactness export, so the function-family conclusion remains tied to displayed finite data.

Finally project the structural rows. Componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local naming preserve exactly the compact-metric and tightness route, leaving no coordinate for a hidden subsequence, open-cover theorem, quotient equality of families, or ambient completeness principle.
\end{proof}
