\begin{theorem}[Frechet--Kolmogorov hub dependency index]
\label{thm:frechet-kolmogorov-hub-dependency-index}
The hub route for $\FrechetKolmogorovCompactnessUp$ reads the $\CompactMetricUp$ source rows, the $\TotallyBoundedUp$ finite-net rows, the Arzela--Ascoli finite-modulus rows, the $\RegSeqRatUp$ rational readback rows, the $\RealUp$ terminal boundary rows, and the $\MetricCompletionUp$ sibling rows before any Frechet--Kolmogorov compactness consumer can inspect the local compactness packet. Thus the top-level input boundary records the same dependency lattice that the child certificates expose: compact metric source, total-bounded budget, finite-modulus equicontinuity, regular rational comparison, Real boundary, completion handoff, transport, replay, provenance, and local $\NameCert$ data.
\end{theorem}
\begin{proof}
Read the hub as an ordered certificate boundary. Its child files first expose the name certificate spine and then the compact-metric, total-bounded, Arzela--Ascoli, locally convex, and metric-completion routes.

The rational and Real rows are read through the same sibling lattice. The $\RegSeqRatUp$ and $\RealUp$ surfaces supply readback and terminal boundary data before the compactness consumer opens the Frechet--Kolmogorov packet.

Consequently a consumer cannot bypass the sibling rows at the hub boundary. The hub records the dependency lattice directly while leaving all theorem content in child files.
\end{proof}
