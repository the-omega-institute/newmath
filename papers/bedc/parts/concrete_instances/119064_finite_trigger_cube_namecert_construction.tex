\chapter{A Concrete Naming Certificate for $\FiniteTriggerCubeUp$}
\label{ch:concrete-instances-finite-trigger-cube-namecert}
\origin{ai}

The $\FiniteTriggerCubeUp$ packet realizes the conditional trigger ensemble of \autoref{def:philosophy-conditional-trigger-ensemble} as finite BEDC data: cube coordinates, trigger subsets, blocker rows, energy rows, rank rows, and local naming are displayed before any probability-like read is consumed. The packet consumes the finite conditional-trigger ensemble of \autoref{ch:concrete-instances-conditional-trigger-ensemble-namecert}, the energy-biased trigger reliability rows of \autoref{ch:concrete-instances-energy-biased-trigger-reliability-namecert}, the trigger-blocker duality rows of \autoref{ch:concrete-instances-trigger-blocker-duality-namecert}, and the trigger-energy partition rows of \autoref{ch:concrete-instances-trigger-energy-partition-namecert}. The carrier is finite combinatorics over the trigger cube, but its role is BEDC-native: it turns the ensemble into a falsifiable NameCert surface rather than an ambient probability measure or an unbounded stochastic model.

\begin{definition}[Finite trigger-cube carrier]
\label{def:finite-trigger-cube-carrier}
A $\FiniteTriggerCubeUp$ carrier is a finite $\BHist$ packet
$$
F=(Q,M,B,T,R,E,W,Z,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the finite cube row, naming the visible cube coordinates and face incidences.
\item $M$ is the boundary-mask row, including the distinguished blocked mask $M^{\star}$ when the conditional trigger ensemble is read.
\item $B$ is the finite subset row under inspection.
\item $T$ is the trigger row, recording whether $B$ meets the displayed full-trigger condition.
\item $R$ is the blocker row, recording the complementary blocked condition used to form $Z_{\mathrm{block}}$.
\item $E$ is the energy row $\mathcal{E}(B)$.
\item $W$ is the finite weight row $t^{|B|}y^{\mathcal{E}(B)}$.
\item $Z$ is the partition-rank row, listing the finite sums used for $Z_{\mathrm{all}}$, $Z_{\mathrm{block}}$, and $Z_{\mathrm{trig}}$.
\item $H$ records componentwise $\hsame$ transport for cube, mask, subset, trigger, blocker, energy, weight, rank, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the finite route
$$
Q,M,B,T,R,E,W,Z \longmapsto H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\FiniteTriggerCubeUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows, with a pointer back to the conditional trigger ensemble theorem of \autoref{thm:philosophy-conditional-trigger-ensemble-theorem}.
\item $N$ is the local $\NameCert_{\FiniteTriggerCubeUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $Q,M,B,T,R,E,W,Z,H,C,P,N$. It has no coordinate for an infinite probability space, hidden sampler, host independence axiom, unbounded cube, selected random variable, or unstated measure.
\end{definition}

\begin{theorem}[Finite trigger-cube NameCert obligations]
\label{thm:finite-trigger-cube-namecert-obligations}
For every accepted $\FiniteTriggerCubeUp$ carrier
$$
F=(Q,M,B,T,R,E,W,Z,H,C,P,N),
$$
the local $\NameCert_{\FiniteTriggerCubeUp}$ surface consists exactly of cube admission, boundary-mask exposure, subset exposure, trigger exposure, blocker exposure, energy exposure, finite weight exposure, partition-rank exposure, componentwise transport, displayed replay, provenance, and local naming. A conditional-trigger consumer may read the distributional expressions of \autoref{def:philosophy-conditional-trigger-ensemble} only after those finite rows have been displayed.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-trigger-cube-carrier}. The cube, mask, subset, trigger, blocker, energy, weight, and partition-rank rows are displayed before transport, replay, provenance, and local naming.

The trigger and blocker rows are separate fields, so a full-trigger read cannot silently replace the blocker complement by an unstated event. The energy and weight rows are finite table entries, and the partition-rank row records the finite sums that support the conditional expressions.

Transport and replay preserve the same displayed fields through $H$ and $C$. Since no carrier coordinate names an infinite probability space, hidden sampler, host independence axiom, unbounded cube, selected random variable, or unstated measure, the listed finite rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Finite trigger-cube ensemble route]
\label{thm:finite-trigger-cube-ensemble-route}
Every conditional trigger-ensemble read carried by $\FiniteTriggerCubeUp$ factors through the finite cube route
$$
Q,M,B,T,R,E,W,Z \longmapsto \NameCert_{\FiniteTriggerCubeUp}.
$$
The route displays the trigger row, blocker row, energy row, finite weight row, and partition-rank row before any expectation, reliability, or low-energy conditional table is read. It exports finite rows only, not a host probability space, hidden sampler, independence axiom, or unbounded stochastic model.
\end{theorem}
\begin{proof}
Apply \autoref{thm:finite-trigger-cube-namecert-obligations}. It gives the finite inventory through which the conditional trigger expressions must be read.

The rows $T$ and $R$ separate full-trigger and blocked cases, while $E,W,Z$ display the energy, monomial weight, and finite partition sums. Thus the expectation and low-energy tables consume finite rank data rather than a measure-theoretic background object.

Finally, $H,C,P,N$ preserve and name the same route. The refused probabilistic and unbounded objects are absent from the carrier, so the ensemble route remains a finite trigger-cube NameCert surface.
\end{proof}

\falsifiablePrediction{Every accepted $\FiniteTriggerCubeUp$ conditional read displays cube coordinates, boundary mask, subset, trigger, blocker, energy, finite weight, partition-rank, transport, replay, provenance, and local $\NameCert$ rows before any expectation or reliability expression is consumed.}

\independenceWitness{conditional-trigger-ensemble, finite-cube, blocker-ledger, energy-rank-table: none is bijective to $\FiniteTriggerCubeUp$ because this packet jointly carries cube coordinates, boundary mask, trigger rows, blocker rows, energy rows, finite weights, partition ranks, transport, replay, provenance, and local naming in one finite carrier.}

\begin{theorem}[Finite trigger-cube TasteGate obligations]
\label{thm:finite-trigger-cube-tastegate-obligations}
A checked TasteGate for $\FiniteTriggerCubeUp$ must exhibit a nonempty accepted trigger-cube packet and must preserve the displayed fields $Q,M,B,T,R,E,W,Z,H,C,P,N$ separately. In particular, changing the trigger row while holding the blocker row fixed, or changing the energy row while holding the subset row fixed, must be visible to the classifier before a consumer can treat the conditional ensemble as a named packet.
\end{theorem}
\begin{proof}
The carrier definition lists trigger, blocker, energy, and rank as separate rows. A nonempty accepted packet witnesses that the carrier is not only an empty formal shell.

Field preservation is required because the conditional ensemble reads trigger and blocker data differently, and it reads energy before forming the finite weight row. Collapsing those fields would make the displayed partition-rank table unable to distinguish full-trigger, blocked, and energy-biased reads.

Thus the TasteGate obligation is to keep the finite fields visible before classifier acceptance. This is a paper-side obligation statement and does not assert a checked Lean target.
\end{proof}

\closureat{FiniteTriggerCubeUp}{seedStr}

\begin{closurestatus}{\FiniteTriggerCubeUp}
  \constructivestory{A $\FiniteTriggerCubeUp$ packet is generated from finite cube coordinates, a boundary mask, a subset row, trigger and blocker rows, an energy row, a finite weight row, partition-rank rows, componentwise transport, displayed replay, provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-trigger-cube-carrier}, \autoref{thm:finite-trigger-cube-namecert-obligations}, \autoref{thm:finite-trigger-cube-ensemble-route}, and \autoref{thm:finite-trigger-cube-tastegate-obligations} seed the finite trigger-cube realization of the conditional trigger ensemble as displayed cube, trigger, blocker, energy, rank, transport, replay, provenance, and naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim an infinite probability space, a host sampler, a measure-theoretic independence axiom, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked cube admission, trigger exposure, blocker exposure, energy exposure, weight exposure, partition-rank exposure, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.FiniteTriggerCubeUp.}
\end{closurestatus}
