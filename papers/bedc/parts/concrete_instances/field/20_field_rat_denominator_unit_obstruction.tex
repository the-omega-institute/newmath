\begin{theorem}[Rat denominator continuation is semigroup-only on the positive carrier]
\label{thm:field-rat-denominator-continuation-semigroup-only}
For the carrier $\mathsf{RatHistoryCarrier}$ and continuation relation $\Cont$,
Theorems~\ref{thm:field-rat-denominator-continuation-carrier-closure} and
\ref{thm:field-rat-denominator-continuation-semigroup-laws} provide carrier
closure and classifier associativity on positive Rat denominators. The empty
history is not an internal carrier endpoint: $\mathsf{RatHistoryCarrier}(\emp)$
implies $\mathsf{False}$. Consequently the endpoint laws in
Theorem~\ref{thm:field-rat-denominator-continuation-external-boundary-laws}
are external empty-history endpoint laws. They do not provide an internal
$\MonoidUp$ unit or an internal $\FieldUp$ unit on the Rat denominator carrier.
\end{theorem}
\begin{proof}
The semigroup clauses are exactly the content of the displayed closure and
semigroup-law theorems. Carrier closure keeps every realized continuation result
inside $\mathsf{RatHistoryCarrier}$ when both inputs are positive Rat
denominators, and the semigroup law identifies the two continuation bracketings
by the Rat history classifier.

For the unit boundary, suppose $\mathsf{RatHistoryCarrier}(\emp)$. Reflexivity
of $\hsame$ at $\emp$ gives $\emp\hsame\emp$. Applying
Theorem~\ref{thm:rat-history-carrier-not-empty} to the assumed carrier witness
and this reflexive endpoint equality yields $\mathsf{False}$. Thus the
empty-history clauses in
Theorem~\ref{thm:field-rat-denominator-continuation-external-boundary-laws}
can be used only as external endpoint laws, not as an internal carrier unit.
\end{proof}

\begin{theorem}[Rat denominator continuation has no internal carrier unit]
\label{thm:field-rat-denominator-continuation-no-internal-unit}
For every history $u:\mathsf{BHist}$, the conjunction of
$\mathsf{RatHistoryCarrier}(u)$ with the internal right-unit classifier law
$$
\forall d,r:\mathsf{BHist}.\;
\mathsf{RatHistoryCarrier}(d)\to
\Cont(d,u,r)\to
\mathsf{RatHistoryClassifier}(r,d)
$$
implies $\mathsf{False}$. Consequently no carried $u$ can be a two-sided
internal continuation unit on $\mathsf{RatHistoryCarrier}$ under
$\mathsf{RatHistoryClassifier}$.
\end{theorem}
\begin{proof}
Assume a carried candidate $u$ and the displayed right-unit classifier law.
Let $d_1:=\Eone(\emp)$. The positive-denominator construction gives
$\mathsf{RatHistoryCarrier}(d_1)$.

Use the canonical continuation witness
$\Cont(d_1,u,\mathsf{append}(d_1,u))$. Applying the assumed right-unit law at
$d_1$ and $r:=\mathsf{append}(d_1,u)$ gives
$\mathsf{RatHistoryClassifier}(\mathsf{append}(d_1,u),d_1)$, whose comparison
component is $\mathsf{append}(d_1,u)\hsame d_1$.

Transport the continuation result along this comparison to obtain
$\Cont(d_1,u,d_1)$. The kernel theorem
\texttt{BEDC.FKernel.Cont.cont\_right\_unit\_unique} then forces
$u\hsame\emp$.

Finally apply Theorem~\ref{thm:rat-history-carrier-not-empty} to the assumed
carrier witness $\mathsf{RatHistoryCarrier}(u)$ and the equality
$u\hsame\emp$. This contradiction proves that no carried endpoint can realize
the required right-unit law, and a two-sided internal unit would contain that
right-unit law as one of its clauses.
\end{proof}
\leanchecked{BEDC.Derived.FieldUp.field\_rat\_denominator\_continuation\_no\_internal\_unit}

\begin{theorem}[Rat denominator continuation has no one-sided internal carrier unit]
\label{thm:field-rat-denominator-continuation-no-one-sided-internal-unit}
For every history $u:\mathsf{BHist}$, suppose
$\mathsf{RatHistoryCarrier}(u)$. Then each of the following two laws separately
implies $\mathsf{False}$:
\begin{enumerate}
\item the internal right-unit classifier law
$$
\forall d,r:\mathsf{BHist}.\;
\mathsf{RatHistoryCarrier}(d)\to
\Cont(d,u,r)\to
\mathsf{RatHistoryClassifier}(r,d),
$$
\item the internal left-unit classifier law
$$
\forall d,r:\mathsf{BHist}.\;
\mathsf{RatHistoryCarrier}(d)\to
\Cont(u,d,r)\to
\mathsf{RatHistoryClassifier}(r,d).
$$
\end{enumerate}
Thus no carried $u$ can realize either one-sided internal continuation unit law
on $\mathsf{RatHistoryCarrier}$ under $\mathsf{RatHistoryClassifier}$.
\end{theorem}
\begin{proof}
For the right-unit law, apply
Theorem~\ref{thm:field-rat-denominator-continuation-no-internal-unit} to the
same carried candidate $u$. The assumed carrier witness and the displayed
right-unit law are exactly the hypotheses of that theorem, so the conclusion is
$\mathsf{False}$.

For the left-unit law, let $d_1:=\Eone(\emp)$. The positive-denominator
construction gives $\mathsf{RatHistoryCarrier}(d_1)$.

Use the canonical continuation witness
$\Cont(u,d_1,\mathsf{append}(u,d_1))$. Applying the assumed left-unit law at
$d_1$ and $r:=\mathsf{append}(u,d_1)$ gives
$\mathsf{RatHistoryClassifier}(\mathsf{append}(u,d_1),d_1)$, whose comparison
component is $\mathsf{append}(u,d_1)\hsame d_1$.

Transport the continuation result along this comparison to obtain
$\Cont(u,d_1,d_1)$. The kernel theorem
\texttt{BEDC.FKernel.Cont.cont\_left\_unit\_unique} then forces
$u\hsame\emp$.

Finally apply Theorem~\ref{thm:rat-history-carrier-not-empty} to the assumed
carrier witness $\mathsf{RatHistoryCarrier}(u)$ and the equality
$u\hsame\emp$. This contradiction proves that no carried endpoint can realize
the required left-unit law.
\end{proof}

\begin{definition}[Concrete Rat denominator continuation semigroup instance]
\label{def:field-rat-denominator-continuation-semigroup-instance}
The concrete denominator-side semigroup instance for
$\FieldUp(\mathsf{RatUp})$ has carrier
$\mathsf{RatHistoryCarrier}$, classifier
$\mathsf{RatHistoryClassifier}$, operation represented by the concrete
continuation relation $\Cont(d,e,r)$, and ledger policy
$\mathsf{RatHistoryLedgerPolicy}$. Its data intentionally contains no identity
field: the operation is packaged only as the carried continuation fragment whose
results are compared by $\mathsf{RatHistoryClassifier}$.
\end{definition}

\textbf{Traditional target}: denominator-side multiplicative semigroup fragment
for $\FieldUp(\mathsf{RatUp})$; \textbf{BEDC source}:
$\mathsf{RatHistoryCarrier}$ plus $\Cont$; \textbf{carrier choice}:
$\mathsf{RatHistoryCarrier}$; \textbf{classifier choice}:
$\mathsf{RatHistoryClassifier}$; \textbf{stability obligations}: closure,
associativity, commutativity, classifier congruence, ledger closure, and no
internal carrier unit; \textbf{ledger}: matching continuation witnesses;
\textbf{core theorems}:
\autoref{thm:field-rat-denominator-continuation-carrier-closure},
\autoref{thm:field-rat-denominator-continuation-semigroup-laws},
\autoref{thm:field-rat-denominator-continuation-commutative-classifier},
\autoref{thm:field-rat-denominator-continuation-classifier-closure},
\autoref{thm:field-rat-classifier-continuation-right-closed},
\autoref{thm:field-rat-denominator-continuation-ledger-closure},
\autoref{thm:field-rat-denominator-continuation-associative-classifier-congruence},
and \autoref{thm:field-rat-denominator-continuation-no-internal-unit};
\textbf{exactness predicate}: carrier, classifier, and ledger preservation under
continuation; \textbf{standard bridge}: deferred; \textbf{status}:
$\mathsf{ProofTarget}$.

\begin{theorem}[Rat denominator continuation semigroup package]
\label{thm:field-rat-denominator-continuation-semigroup-package}
The concrete instance of
Definition~\ref{def:field-rat-denominator-continuation-semigroup-instance}
satisfies
the following package clauses:
\begin{enumerate}
  \item for all histories $d,e,f,de,ef,left,right:\mathsf{BHist}$, if
  $\mathsf{RatHistoryCarrier}(d)$, $\mathsf{RatHistoryCarrier}(e)$,
  $\mathsf{RatHistoryCarrier}(f)$, $\Cont(d,e,de)$, $\Cont(e,f,ef)$,
  $\Cont(de,f,left)$, and $\Cont(d,ef,right)$, then
  $\mathsf{RatHistoryCarrier}(de)$, $\mathsf{RatHistoryCarrier}(ef)$,
  $\mathsf{RatHistoryCarrier}(left)$, $\mathsf{RatHistoryCarrier}(right)$, and
  $\mathsf{RatHistoryClassifier}(left,right)$;
  \item for all histories $d,d',u,u',r,s:\mathsf{BHist}$, if
  $\mathsf{RatHistoryClassifier}(d,d')$, $\mathsf{UnaryHistory}(u)$,
  $\mathsf{UnaryHistory}(u')$, $u\hsame u'$, $\Cont(d,u,r)$, and
  $\Cont(d',u',s)$, then $\mathsf{RatHistoryClassifier}(r,s)$;
  \item for all histories $d,d',e,r,r':\mathsf{BHist}$, if
  $\mathsf{RatHistoryClassifier}(d,d')$, $\mathsf{RatHistoryCarrier}(e)$,
  $\Cont(d,e,r)$, and $\Cont(d',e,r')$, then
  $\mathsf{RatHistoryClassifier}(r,r')$;
  \item for all histories $\rho,v,u,u',r,s:\mathsf{BHist}$, if
  $\mathsf{RatHistoryLedgerPolicy}(\rho,v)$, $\mathsf{UnaryHistory}(u)$,
  $\mathsf{UnaryHistory}(u')$, $u\hsame u'$, $\Cont(\rho,u,r)$, and
  $\Cont(v,u',s)$, then $\mathsf{RatHistoryLedgerPolicy}(r,s)$;
  \item for every history $u:\mathsf{BHist}$,
  $\mathsf{RatHistoryCarrier}(u)$ together with the right-unit classifier law
  $$
  \forall d,r:\mathsf{BHist}.\;
  \mathsf{RatHistoryCarrier}(d)\to
  \Cont(d,u,r)\to
  \mathsf{RatHistoryClassifier}(r,d)
  $$
  implies $\mathsf{False}$. Hence no carried endpoint can serve as a right or
  two-sided internal unit for this continuation instance.
\end{enumerate}
\end{theorem}
\begin{proof}
For the first clause, apply
\autoref{thm:field-rat-denominator-continuation-semigroup-laws}. It gives the
two intermediate carriers, the two outer carriers, and $left\hsame right$.
Combining the two outer carriers with this comparison yields
$\mathsf{RatHistoryClassifier}(left,right)$.

The same clause records the carrier closure supplied by
\autoref{thm:field-rat-denominator-continuation-carrier-closure}. The
classifier statement for the two bracketings is the semigroup associativity
component, while
\autoref{thm:field-rat-denominator-continuation-commutative-classifier} records
the corresponding commutative classifier coherence for swapped carried
continuations.

For classifier preservation under matching continuation tails, use
\autoref{thm:field-rat-denominator-continuation-classifier-closure}. For the
shared carried right endpoint form, use
\autoref{thm:field-rat-classifier-continuation-right-closed}. The compatible
associative version with classified triples is exactly
\autoref{thm:field-rat-denominator-continuation-associative-classifier-congruence}.

For the ledger clause, apply
\autoref{thm:field-rat-denominator-continuation-ledger-closure} to the displayed
ledger-related endpoints, matching unary tails, matching tail comparison, and
continuation witnesses. The conclusion is the stated preservation of
$\mathsf{RatHistoryLedgerPolicy}$.

For the final obstruction clause, apply
\autoref{thm:field-rat-denominator-continuation-no-internal-unit}. Its statement
is precisely the contradiction obtained from a carried endpoint satisfying the
right-unit classifier law, and any two-sided internal unit contains that
right-unit clause.
\end{proof}

\begin{theorem}[Rat denominator continuation is not a FieldUp multiplication]
\label{thm:field-rat-denominator-continuation-not-field-multiplication}
For every proposed carried unit $u:\mathsf{BHist}$, suppose
$\mathsf{RatHistoryCarrier}(u)$ and suppose the proposed carried
multiplication on the Rat denominator carrier is the continuation relation
$\Cont$. If the corresponding $\FieldUp(\mathsf{RatUp})$ right-unit law under
$\mathsf{RatHistoryClassifier}$ is
$$
\forall d,r:\mathsf{BHist}.\;
\mathsf{RatHistoryCarrier}(d)\to
\Cont(d,u,r)\to
\mathsf{RatHistoryClassifier}(r,d),
$$
then the assumptions imply $\mathsf{False}$. Consequently a
$\FieldUp(\mathsf{RatUp})$ certificate cannot take Rat-denominator
continuation itself as its carried multiplication together with a carried
unit.
\end{theorem}
\begin{proof}
Unpack the attempted $\FieldUp$ multiplication data for the carried operation
$\Cont$. The right-unit field supplies exactly the displayed classifier law:
for every Rat denominator $d$ and every continuation result $r$ from
$\Cont(d,u,r)$, the result must classify as the original denominator $d$ under
$\mathsf{RatHistoryClassifier}$.

Apply
Theorem~\ref{thm:field-rat-denominator-continuation-no-one-sided-internal-unit}
to the same history $u$, using the carried-unit witness
$\mathsf{RatHistoryCarrier}(u)$ and the right-unit clause with this unpacked
field. Its conclusion is $\mathsf{False}$.

Theorem~\ref{thm:field-rat-denominator-continuation-semigroup-only} records the
positive part left intact by this contradiction: continuation on Rat
denominators still has carrier closure and semigroup-level associativity, but
those clauses do not supply an internal $\MonoidUp$ unit or the unit required by
$\FieldUp$ multiplication.
\end{proof}

\begin{theorem}[Rat denominator continuation has no internal left carrier unit]
\label{thm:field-rat-denominator-continuation-no-internal-left-unit}
For every history $u:\mathsf{BHist}$, the conjunction of
$\mathsf{RatHistoryCarrier}(u)$ with the internal left-unit classifier law
$$
\forall d,r:\mathsf{BHist}.\;
\mathsf{RatHistoryCarrier}(d)\to
\Cont(u,d,r)\to
\mathsf{RatHistoryClassifier}(r,d)
$$
implies $\mathsf{False}$. Thus no carried $u$ can realize the left endpoint
half of an internal continuation unit on $\mathsf{RatHistoryCarrier}$ under
$\mathsf{RatHistoryClassifier}$.
\end{theorem}
\begin{proof}
Assume a carried candidate $u$ and the displayed left-unit classifier law. Let
$d_1:=\Eone(\emp)$. The positive-denominator construction gives
$\mathsf{RatHistoryCarrier}(d_1)$.

Use the canonical continuation witness
$\Cont(u,d_1,\mathsf{append}(u,d_1))$. Applying the assumed left-unit law at
$d_1$ and $r:=\mathsf{append}(u,d_1)$ gives
$\mathsf{RatHistoryClassifier}(\mathsf{append}(u,d_1),d_1)$, whose comparison
component is $\mathsf{append}(u,d_1)\hsame d_1$.

Transport the continuation result along this comparison to obtain
$\Cont(u,d_1,d_1)$. The kernel theorem
\texttt{BEDC.FKernel.Cont.cont\_left\_unit\_unique} then forces
$u\hsame\emp$.

Finally apply Theorem~\ref{thm:rat-history-carrier-not-empty} to the assumed
carrier witness $\mathsf{RatHistoryCarrier}(u)$ and the equality
$u\hsame\emp$. This contradiction proves that no carried endpoint can realize
the required left-unit law.
\end{proof}
\leanchecked{BEDC.Derived.FieldUp.field\_rat\_denominator\_continuation\_no\_internal\_left\_unit}

\begin{theorem}[Rat denominator continuation commutative-semigroup obstruction package]
\label{thm:field-rat-denominator-continuation-commutative-semigroup-obstruction-package}
For the concrete BEDC carrier $\mathsf{RatHistoryCarrier}$, continuation
relation $\Cont$, and classifier $\mathsf{RatHistoryClassifier}$, Rat
denominator continuation satisfies the following five-field package.
\begin{enumerate}
\item Carrier closure: if $\mathsf{RatHistoryCarrier}(d)$,
$\mathsf{RatHistoryCarrier}(e)$, and $\Cont(d,e,r)$, then
$\mathsf{RatHistoryCarrier}(r)$.
\item Classifier associativity: if $d,e,f,de,ef,\ell,r:\mathsf{BHist}$ are
related by $\Cont(d,e,de)$, $\Cont(e,f,ef)$, $\Cont(de,f,\ell)$, and
$\Cont(d,ef,r)$, with $d,e,f$ carried by $\mathsf{RatHistoryCarrier}$, then the
two bracketed results are carried and satisfy $\ell\hsame r$.
\item Classifier commutativity: if $\mathsf{RatHistoryCarrier}(d)$,
$\mathsf{RatHistoryCarrier}(e)$, $\Cont(d,e,r)$, and $\Cont(e,d,s)$, then
$\mathsf{RatHistoryClassifier}(r,s)$.
\item Internal left-unit impossibility: for every $u:\mathsf{BHist}$, the
conjunction of $\mathsf{RatHistoryCarrier}(u)$ with
$$
\forall d,r:\mathsf{BHist}.\;
\mathsf{RatHistoryCarrier}(d)\to
\Cont(u,d,r)\to
\mathsf{RatHistoryClassifier}(r,d)
$$
implies $\mathsf{False}$.
\item Internal right-unit impossibility: for every $u:\mathsf{BHist}$, the
conjunction of $\mathsf{RatHistoryCarrier}(u)$ with
$$
\forall d,r:\mathsf{BHist}.\;
\mathsf{RatHistoryCarrier}(d)\to
\Cont(d,u,r)\to
\mathsf{RatHistoryClassifier}(r,d)
$$
implies $\mathsf{False}$.
\end{enumerate}
Consequently Rat-denominator continuation is a concrete commutative
semigroup-level operation under $\mathsf{RatHistoryClassifier}$ with no internal
one-sided carried unit, and therefore cannot serve as the carried multiplication
of $\FieldUp(\mathsf{RatUp})$.
\end{theorem}
\begin{proof}
The carrier-closure field is exactly
\autoref{thm:field-rat-denominator-continuation-carrier-closure}; it uses only
the concrete carrier $\mathsf{RatHistoryCarrier}$ and the concrete continuation
relation $\Cont$.

The associativity field is
\autoref{thm:field-rat-denominator-continuation-semigroup-laws}. It supplies the
two intermediate carriers, the two outer result carriers, and the classifier
comparison between the left- and right-bracketed continuations.

The commutativity field is
\autoref{thm:field-rat-denominator-continuation-commutative-classifier}. It
compares the two swapped denominator continuations by
$\mathsf{RatHistoryClassifier}$.

The internal left-unit obstruction is the left law of
\autoref{thm:field-rat-denominator-continuation-no-one-sided-internal-unit}.
The internal right-unit obstruction is the right law of
\autoref{thm:field-rat-denominator-continuation-no-one-sided-internal-unit}.

Combining those two one-sided obstructions with the closure, associativity, and
commutativity fields gives precisely a commutative semigroup-level continuation
operation, not a carried field multiplication with an internal one-sided unit.
\end{proof}

\begin{theorem}[Rat denominator continuation full obstruction package]
\label{thm:field-rat-denominator-continuation-full-obstruction-package}
For the concrete BEDC carrier $\mathsf{RatHistoryCarrier}$, classifier
$\mathsf{RatHistoryClassifier}$, operation relation $\Cont$, and ledger
$\mathsf{RatHistoryLedgerPolicy}$, Rat denominator continuation satisfies the
following six-clause certificate-shaped obstruction package.
\begin{enumerate}
\item Carrier closure: if $\mathsf{RatHistoryCarrier}(d)$,
$\mathsf{RatHistoryCarrier}(e)$, and $\Cont(d,e,r)$, then
$\mathsf{RatHistoryCarrier}(r)$.
\item Classifier associativity: if $d,e,f,de,ef,\ell,r:\mathsf{BHist}$ are
related by $\Cont(d,e,de)$, $\Cont(e,f,ef)$, $\Cont(de,f,\ell)$, and
$\Cont(d,ef,r)$, with $d,e,f$ carried by $\mathsf{RatHistoryCarrier}$, then the
two bracketed results are carried and satisfy
$\mathsf{RatHistoryClassifier}(\ell,r)$.
\item Classifier commutativity: if $\mathsf{RatHistoryCarrier}(d)$,
$\mathsf{RatHistoryCarrier}(e)$, $\Cont(d,e,r)$, and $\Cont(e,d,s)$, then
$\mathsf{RatHistoryClassifier}(r,s)$.
\item Binary classifier congruence: if
$\mathsf{RatHistoryClassifier}(d,d')$,
$\mathsf{RatHistoryClassifier}(e,e')$, $\Cont(d,e,r)$, and
$\Cont(d',e',s)$, then $\mathsf{RatHistoryClassifier}(r,s)$.
\item Ledger closure for classified unary continuation tails: if
$\mathsf{RatHistoryLedgerPolicy}(\rho,v)$, $\mathsf{UnaryHistory}(u)$,
$\mathsf{UnaryHistory}(u')$, $u\hsame u'$, $\Cont(\rho,u,r)$, and
$\Cont(v,u',s)$, then $\mathsf{RatHistoryLedgerPolicy}(r,s)$.
\item Internal carried-unit obstruction: for every history $u:\mathsf{BHist}$,
if $\mathsf{RatHistoryCarrier}(u)$, then the left-unit classifier law

$$
\forall d,r:\mathsf{BHist}.\;
\mathsf{RatHistoryCarrier}(d)\to
\Cont(u,d,r)\to
\mathsf{RatHistoryClassifier}(r,d)
$$

and the right-unit classifier law

$$
\forall d,r:\mathsf{BHist}.\;
\mathsf{RatHistoryCarrier}(d)\to
\Cont(d,u,r)\to
\mathsf{RatHistoryClassifier}(r,d)
$$

each imply $\mathsf{False}$.
\end{enumerate}
Consequently Rat-denominator continuation is a classifier-and-ledger
commutative semigroup fragment, but it cannot be promoted to the multiplicative
carrier of $\MonoidUp$ or $\FieldUp$ because no carried one-sided unit exists.
\end{theorem}
\begin{proof}
The carrier-closure clause is exactly
\autoref{thm:field-rat-denominator-continuation-carrier-closure}. The
associativity clause is
\autoref{thm:field-rat-denominator-continuation-semigroup-laws}; its conclusion
supplies the two intermediate carriers, the two outer result carriers, and
$\ell\hsame r$, which repacks as
$\mathsf{RatHistoryClassifier}(\ell,r)$.

The commutativity clause is
\autoref{thm:field-rat-denominator-continuation-commutative-classifier}. For
binary classifier congruence, unpack the classifier
$\mathsf{RatHistoryClassifier}(e,e')$ to obtain unary denominator witnesses for
the two right inputs and the comparison $e\hsame e'$, then apply
\autoref{thm:field-rat-denominator-continuation-binary-classifier-congruence}
to the classified left inputs and the two continuation witnesses.

The ledger clause is
\autoref{thm:field-rat-denominator-continuation-ledger-closure}. It applies to
the displayed ledger endpoints, matched unary continuation tails, tail
comparison, and continuation witnesses, yielding
$\mathsf{RatHistoryLedgerPolicy}(r,s)$.

The carried-unit obstruction is
\autoref{thm:field-rat-denominator-continuation-no-one-sided-internal-unit}. Its
two conclusions rule out the left-unit and right-unit classifier laws for any
carried candidate $u$, completing the certificate-shaped package.
\end{proof}

\begin{theorem}[Rat denominator continuation obstruction is context-invariant]
\label{thm:field-rat-denominator-context-invariant-obstruction-package}
For all histories
$p,p',q,q',d,e,pd,pe,left,right:\mathsf{BHist}$, suppose
$\mathsf{UnaryHistory}(p)$, $\mathsf{UnaryHistory}(p')$,
$\mathsf{UnaryHistory}(q)$, $\mathsf{UnaryHistory}(q')$, $p\hsame p'$,
$q\hsame q'$, $\mathsf{RatHistoryCarrier}(d)$,
$\mathsf{RatHistoryCarrier}(e)$, and the four displayed continuation
witnesses

$$
\Cont(p,d,pd),\quad
\Cont(p',e,pe),\quad
\Cont(pd,q,left),\quad
\Cont(pe,q',right).
$$

Then the matched unary context wrapping preserves the full obstruction data:
\begin{enumerate}
\item
$\mathsf{RatHistoryClassifier}(d,e)\Longleftrightarrow
\mathsf{RatHistoryClassifier}(left,right)$.
\item
$\mathsf{RatHistoryLedgerPolicy}(d,e)\to
\mathsf{RatHistoryLedgerPolicy}(left,right)$.
\item
For every history $u:\mathsf{BHist}$, if
$\mathsf{RatHistoryCarrier}(u)$, then the internal left-unit classifier law

$$
\forall x,r:\mathsf{BHist}.\;
\mathsf{RatHistoryCarrier}(x)\to
\Cont(u,x,r)\to
\mathsf{RatHistoryClassifier}(r,x)
$$

and the internal right-unit classifier law

$$
\forall x,r:\mathsf{BHist}.\;
\mathsf{RatHistoryCarrier}(x)\to
\Cont(x,u,r)\to
\mathsf{RatHistoryClassifier}(r,x)
$$

each imply $\mathsf{False}$.
\end{enumerate}
\end{theorem}
\begin{proof}
The classifier claim is exactly
\autoref{thm:field-rat-denominator-hsame-matched-context-classifier-exactness}
with the displayed unary prefix contexts, unary suffix contexts, context
comparisons, carrier witnesses, and continuation witnesses.

For the ledger claim, apply
\autoref{thm:field-rat-denominator-continuation-common-context-ledger-closure}
to the same matched unary context data and the displayed continuation
witnesses. Its conclusion transports the Rat ledger policy from $(d,e)$ to
$(left,right)$.

It remains to record that the matched context does not create an internal
carried unit. The left-unit obstruction is
\autoref{thm:field-rat-denominator-continuation-no-internal-left-unit}, applied
to the carried candidate $u$ and the displayed left-unit classifier law. The
right-unit obstruction is
\autoref{thm:field-rat-denominator-continuation-no-internal-unit}, applied to
the same carried candidate $u$ and the displayed right-unit classifier law.
\end{proof}

\begin{theorem}[Rat denominator continuation associative obstruction package]
\label{thm:field-rat-denominator-associative-obstruction-package}
For the concrete BEDC carrier $\mathsf{RatHistoryCarrier}$, classifier
$\mathsf{RatHistoryClassifier}$, operation relation $\Cont$, and ledger
$\mathsf{RatHistoryLedgerPolicy}$, Rat denominator continuation satisfies the
following certificate-shaped obstruction package.
\begin{enumerate}
\item Carrier closure: if $\mathsf{RatHistoryCarrier}(d)$,
$\mathsf{RatHistoryCarrier}(e)$, and $\Cont(d,e,r)$, then
$\mathsf{RatHistoryCarrier}(r)$.
\item Binary classifier congruence: if
$\mathsf{RatHistoryClassifier}(d,d')$,
$\mathsf{RatHistoryClassifier}(e,e')$, $\Cont(d,e,r)$, and
$\Cont(d',e',r')$, then $\mathsf{RatHistoryClassifier}(r,r')$.
\item Classified-triple associativity congruence: for all histories
$d,d',e,e',f,f',de,de',ef,ef',left,left',right,right':\mathsf{BHist}$, if
$\mathsf{RatHistoryClassifier}(d,d')$,
$\mathsf{RatHistoryClassifier}(e,e')$,
$\mathsf{RatHistoryClassifier}(f,f')$, $\Cont(d,e,de)$,
$\Cont(d',e',de')$, $\Cont(e,f,ef)$, $\Cont(e',f',ef')$,
$\Cont(de,f,left)$, $\Cont(de',f',left')$, $\Cont(d,ef,right)$, and
$\Cont(d',ef',right')$, then

$$
\mathsf{RatHistoryClassifier}(left,left')\land
\mathsf{RatHistoryClassifier}(right,right')\land
left\hsame right\land left'\hsame right'.
$$

\item Hsame-matched context classifier-and-ledger exactness: for all histories
$p,p',q,q',d,e,pd,pe,left,right:\mathsf{BHist}$, if
$\mathsf{UnaryHistory}(p)$, $\mathsf{UnaryHistory}(p')$,
$\mathsf{UnaryHistory}(q)$, $\mathsf{UnaryHistory}(q')$, $p\hsame p'$,
$q\hsame q'$, $\mathsf{RatHistoryCarrier}(d)$,
$\mathsf{RatHistoryCarrier}(e)$, $\Cont(p,d,pd)$, $\Cont(p',e,pe)$,
$\Cont(pd,q,left)$, and $\Cont(pe,q',right)$, then both equivalences hold:

$$
\mathsf{RatHistoryClassifier}(left,right)\Longleftrightarrow
\mathsf{RatHistoryClassifier}(d,e).
$$

$$
\mathsf{RatHistoryLedgerPolicy}(left,right)\Longleftrightarrow
\mathsf{RatHistoryLedgerPolicy}(d,e).
$$

\item Ledger closure for classified unary continuation tails: if
$\mathsf{RatHistoryLedgerPolicy}(\rho,v)$, $\mathsf{UnaryHistory}(u)$,
$\mathsf{UnaryHistory}(u')$, $u\hsame u'$, $\Cont(\rho,u,r)$, and
$\Cont(v,u',s)$, then $\mathsf{RatHistoryLedgerPolicy}(r,s)$.
\item Endpoint-forced emptiness: if a history $u:\mathsf{BHist}$ satisfies
either one-sided classifier unit law

$$
\forall d,r:\mathsf{BHist}.\;
\mathsf{RatHistoryCarrier}(d)\to
\Cont(d,u,r)\to
\mathsf{RatHistoryClassifier}(r,d)
$$

or

$$
\forall d,r:\mathsf{BHist}.\;
\mathsf{RatHistoryCarrier}(d)\to
\Cont(u,d,r)\to
\mathsf{RatHistoryClassifier}(r,d),
$$

then $u\hsame\emp$.
\item Endpoint uniqueness: for all histories $u,v:\mathsf{BHist}$, if both
endpoints satisfy the right-unit classifier law

$$
\forall d,r:\mathsf{BHist}.\;
\mathsf{RatHistoryCarrier}(d)\to
\Cont(d,w,r)\to
\mathsf{RatHistoryClassifier}(r,d),
$$

for $w=u$ and for $w=v$, then $u\hsame v$; if both endpoints satisfy the
left-unit classifier law

$$
\forall d,r:\mathsf{BHist}.\;
\mathsf{RatHistoryCarrier}(d)\to
\Cont(w,d,r)\to
\mathsf{RatHistoryClassifier}(r,d),
$$

for $w=u$ and for $w=v$, then $u\hsame v$; and if $u$ satisfies the right-unit
classifier law while $v$ satisfies the left-unit classifier law, then
$u\hsame v$.
\item Carried one-sided unit obstruction: for every history $u:\mathsf{BHist}$,
if $\mathsf{RatHistoryCarrier}(u)$, then each one-sided classifier unit law for
$u$ implies $\mathsf{False}$.
\end{enumerate}
Consequently the Rat-denominator continuation spine contains the closure,
operation congruence, associative classified composition, context exactness, and
ledger stability required for a certificate, while its unit endpoint is forced
outside the carried denominator carrier.
\end{theorem}
\begin{proof}
The carrier-closure field is exactly
\autoref{thm:field-rat-denominator-continuation-carrier-closure}. The binary
classifier congruence field is
\autoref{thm:field-rat-denominator-continuation-binary-classifier-congruence};
it compares two continuation outputs from two classified input pairs.

The classified-triple associativity congruence field is
\autoref{thm:field-rat-denominator-continuation-associative-classifier-congruence}.
It handles two classified triples of continuation data at once, producing
classifier comparisons for the corresponding left and right bracketings together
with the two internal associativity comparisons.

For hsame-matched context exactness, apply
\autoref{thm:field-rat-denominator-hsame-matched-context-exactness-package} to
the displayed unary prefixes, unary suffixes, context comparisons, Rat carriers,
and continuation witnesses. Its two equivalences are precisely the classifier
and ledger exactness clauses in the package.

The ledger-closure field is
\autoref{thm:field-rat-denominator-continuation-ledger-closure}. It transports
the Rat ledger along classified unary continuation tails and returns
$\mathsf{RatHistoryLedgerPolicy}(r,s)$.

For endpoint-forced emptiness, use
\autoref{thm:field-rat-denominator-continuation-unit-endpoint-empty}. Its right
and left clauses force any one-sided classifier unit endpoint to satisfy
$u\hsame\emp$.

Endpoint uniqueness is
\autoref{thm:field-rat-denominator-continuation-unit-endpoint-unique}; its three
clauses cover the displayed right/right, left/left, and mixed one-sided
classifier-unit comparisons.

Finally, the carried one-sided unit obstruction is
\autoref{thm:field-rat-denominator-continuation-no-one-sided-internal-unit},
which turns either one-sided classifier unit law into $\mathsf{False}$ once the
endpoint is carried by $\mathsf{RatHistoryCarrier}$.
\end{proof}
\input{parts/concrete_instances/field/20_field_rat_denominator_unit_envelope_laws}

\input{parts/concrete_instances/field/20_field_rat_denominator_unit_targets}

\input{parts/concrete_instances/field/20_field_rat_denominator_unit_carrier}

\input{parts/concrete_instances/field/20_field_rat_denominator_unit_obstruction_targets}
