\begin{theorem}[RatUp FieldUp phase-exit record certificate boundary]
\label{thm:ratup-fieldup-phase-exit-record-certificate-boundary}
For $\mathsf{RatUpFieldSource}_F$, let $\mathcal E_F$ be the concrete
$\FieldUp(\RatUp)$ exit object of
Definition~\ref{def:ratup-fieldup-concrete-exit-object}. Fix the concrete
denominator tuple $(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)$, histories
$h,k,\ell:\mathsf{BHist}$ with carrier witnesses $C_D^u(h)$, $C_D^u(k)$, and
$C_D^u(\ell)$, selectors $s,t,u_0,v,w,z:\mathsf{BMark}$, contexts
$L,R,r:\mathsf{BHist}$, and a support witness
$\mathsf{ThreeSupp}_D^u(h,k,\ell)$. Also fix Rat-classified endpoint pairs
$x,x',y,y':\mathsf{BHist}$ satisfying
$\mathsf{RatHistoryClassifier}(x,x')$ and
$\mathsf{RatHistoryClassifier}(y,y')$, a carried denominator
$q:\mathsf{BHist}$ with $\mathsf{RatHistoryCarrier}(q)$, apart-zero context data
$L_0,R_0,\tau:\mathsf{BHist}$ with $p:\mathsf{FieldApartZero}(L_0)$, histories
$a,b,u,d:\mathsf{BHist}$, the contextual action $\Gamma$, the affine source
$\mathsf{AffAut}_F$, and the same BEDC history operations $add$, $mul$, $neg$,
and $zero$ satisfying the CommRing hypotheses used by
\autoref{thm:commring-zero-linear-factor-bilateral-exact-package}. If
$$
H:\bigl((a+b)\hsame0 \lor (a+(-b))\hsame0\bigr)
$$
and $U_D(u)$, then the same exit object $\mathcal E_F$ supplies one
$\FieldUp(\RatUp)$ phase algebra exit package containing:
\begin{enumerate}
  \item $\mathsf{RatUpFieldPhaseExit}(\mathcal E_F)$ for the displayed data and
  guard hypothesis;
  \item the source, certificate-obligation, lower-reduct, denominator-law, and
  raw-continuation separation rows of
  \autoref{thm:ratup-fieldup-concrete-certificate-boundary};
  \item the square row, external-unit classifier separation, and canonical
  action-and-inverse rows exposed by
  \autoref{thm:ratup-fieldup-phase-exit-record-guard-projection}.
\end{enumerate}
Every row is read over the displayed $\mathsf{BHist}$ source,
$\mathsf{RatHistoryCarrier}$, $\mathsf{RatHistoryClassifier}$, denominator
tuple, contextual action, affine source, guard datum, histories, contexts,
selectors, and carrier witnesses.
\end{theorem}
\begin{proof}
First apply
\autoref{thm:ratup-fieldup-canonical-phase-exit-record}. It gives
$\mathsf{RatUpFieldPhaseExit}(\mathcal E_F)$ from exactly the displayed source,
exit object, denominator tuple, endpoint data, apart-zero context, affine data,
CommRing operations, and guard hypothesis $H$.

Next invoke \autoref{thm:ratup-fieldup-concrete-certificate-boundary}. Its four
rows record the same $\mathsf{RatUpFieldSource}_F$ source, the concrete
\autoref{def:field-certificate-obligations} certificate, the lower algebra
reduct interface, the denominator append and relational packages, and the
raw-continuation separation.

Finally feed the phase-exit record just obtained, together with $U_D(u)$, into
\autoref{thm:ratup-fieldup-phase-exit-record-guard-projection}. The result is
the square row, the external-unit classifier separation, and the canonical
action-and-inverse support over the same carrier, classifier, denominator tuple,
contextual action, affine source, histories, contexts, and selectors.

Conjunction introduction packs these three sources into the public
$\FieldUp(\RatUp)$ phase algebra exit package. No source row, certificate row,
lower-reduct row, denominator row, guard row, action row, or inverse row is
reindexed.
\end{proof}

Per-object template: traditional target rational field phase algebra exit
package; BEDC source $\mathsf{BHist}$, $\RatUp$, $\CommRingUp$, and
$\FieldUp$; carriers $\mathsf{RatHistoryCarrier}$, $C_D^u$, $U_D$, and
$\mathsf{AffAut}_F$; classifiers $\mathsf{RatHistoryClassifier}$, $R_D^u$,
$\hsame$, and affine classifier; stability obligations canonical phase-exit
record, certificate boundary rows, zero-factor square row, external-unit
classifier separation, and action/inverse support; ledger denominator tuple,
external-unit endpoint, guard witness, contextual action, affine source,
histories, contexts, selectors, and carrier witnesses; core theorem this block;
exactness the displayed phase algebra exit package; downstream consumer
$\FieldUp(\RatUp)$ exit criterion; status $\mathsf{ProofTarget}$.

\begin{theorem}[RatUp FieldUp phase-exit record final-inventory separation]
\label{thm:ratup-fieldup-phase-exit-record-final-inventory-separation}
Fix the concrete source $\mathsf{RatUpFieldSource}_F$, the operation table of
Definition~\ref{def:ratup-fieldup-concrete-operation-table}, and the concrete
exit object $\mathcal E_F$ of
Definition~\ref{def:ratup-fieldup-concrete-exit-object}. Let
$(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)$ be the concrete denominator tuple, let
$h,k,\ell:\mathsf{BHist}$ satisfy $C_D^u(h)$, $C_D^u(k)$, $C_D^u(\ell)$, and
$\mathsf{ThreeSupp}_D^u(h,k,\ell)$, and let
$s,t,\upsilon,v,w,z:\mathsf{BMark}$ and $L,R,r:\mathsf{BHist}$ be the chosen
selectors and contexts. For Rat-classified endpoint pairs
$x,x',y,y':\mathsf{BHist}$ satisfying
$$
\mathsf{RatHistoryClassifier}(x,x')
\land
\mathsf{RatHistoryClassifier}(y,y'),
$$
for every carried denominator $q:\mathsf{BHist}$ satisfying
$\mathsf{RatHistoryCarrier}(q)$, fix apart-zero context data
$L_0,R_0,\tau:\mathsf{BHist}$ with $p:\mathsf{FieldApartZero}(L_0)$, phase guard
histories $\alpha,\beta:\mathsf{BHist}$, the contextual action $\Gamma$, the
affine source $\mathsf{AffAut}_F$, and the same BEDC history operations $add$,
$mul$, $neg$, and $zero$ satisfying the CommRing hypotheses used by
\autoref{thm:commring-zero-linear-factor-bilateral-exact-package}. If
$$
H:\bigl((\alpha+\beta)\hsame0 \lor (\alpha+(-\beta))\hsame0\bigr),
$$
and histories $u,d:\mathsf{BHist}$ satisfy $U_D(u)$ and
$\mathsf{RatHistoryCarrier}(d)$, then the same exit object $\mathcal E_F$
supplies one $\mathsf{BHist}$-anchored $\FieldUp(\RatUp)$ phase-exit boundary
containing, at the same time:
\begin{enumerate}
  \item the phase-exit record certificate boundary of
  \autoref{thm:ratup-fieldup-phase-exit-record-certificate-boundary} for the
  displayed source, operation table, exit object, denominator tuple, histories,
  selectors, contexts, endpoint pairs, carried denominator, apart-zero data,
  contextual action, affine source, operations, guard hypothesis, and
  external-unit witness;
  \item the final-inventory carried-external classifier separation row
  $$
  \bigl(
  \mathsf{RatHistoryClassifier}(u,d)\lor
  \mathsf{RatHistoryClassifier}(d,u)
  \bigr)\to\mathsf{False}
  $$
  of
  \autoref{thm:ratup-fieldup-final-inventory-carried-external-unit-separation}
  for the same concrete source, operation table, exit object, denominator tuple,
  histories, support witness, selectors, contexts, endpoint pairs, carried
  denominator, external-unit witness, and carried denominator endpoint.
\end{enumerate}
No carrier, classifier, denominator tuple, contextual action, affine source,
selector, context, Rat endpoint, carried denominator, external-unit witness, or
carried denominator endpoint is replaced between the two row families.
\end{theorem}
\begin{proof}
First instantiate
\autoref{thm:ratup-fieldup-phase-exit-record-certificate-boundary} with the
displayed source, exit object, denominator tuple, histories $h,k,\ell$, support
witness, selectors $s,t,\upsilon,v,w,z$, contexts $L,R,r$, Rat-classified
endpoint pairs $x,x',y,y'$, carried denominator $q$, apart-zero data
$L_0,R_0,\tau,p$, phase guard histories $\alpha,\beta,u,d$, contextual action
$\Gamma$, affine source $\mathsf{AffAut}_F$, CommRing operations, guard
hypothesis $H$, and witness $U_D(u)$. This yields the phase-exit record
certificate boundary over $\mathcal E_F$.

Next instantiate
\autoref{thm:ratup-fieldup-final-inventory-carried-external-unit-separation}
with the same concrete source, operation table, exit object, denominator tuple,
histories $h,k,\ell$, support witness, selectors, contexts, Rat-classified
endpoint pairs, carried denominator $q$, external unit $u$, and carried
denominator endpoint $d$. The hypotheses $U_D(u)$ and
$\mathsf{RatHistoryCarrier}(d)$ supply exactly the carried-external separation
row needed by the phase-exit boundary.

Finally pair the two row families. Both cited results were read at the identical
source, carrier, classifier, denominator tuple, contextual action, affine source,
histories, selectors, contexts, endpoint pairs, carried denominator,
external-unit witness, and carried denominator endpoint, so the packaged
boundary is a single concrete $\mathsf{BHist}$-anchored certificate for the
Phase Algebra Hierarchy exit criterion.
\end{proof}

Per-object template: traditional target rational field phase-exit
final-inventory separation; BEDC source $\mathsf{BHist}$, $\RatUp$,
$\CommRingUp$, and $\FieldUp$; carriers $\mathsf{RatHistoryCarrier}$, $C_D^u$,
$U_D$, and $\mathsf{AffAut}_F$; classifiers $\mathsf{RatHistoryClassifier}$,
$R_D^u$, $\hsame$, and affine classifier; stability obligations phase-exit
record certificate boundary and final-inventory carried-external classifier
separation; ledger denominator tuple, operation table, external-unit endpoint,
carried denominator endpoint, guard witness, contextual action, affine source,
histories, contexts, selectors, and carrier witnesses; core theorem this block;
exactness the displayed paired phase-exit and final-inventory boundary;
downstream consumer Phase Algebra Hierarchy $\FieldUp(\RatUp)$ exit criterion;
status $\mathsf{ProofTarget}$.

\begin{theorem}[RatUp FieldUp certificate record absorbs phase-exit boundary]
\label{thm:ratup-fieldup-certificate-record-absorbs-phase-exit-boundary}
Fix the concrete source $\mathsf{RatUpFieldSource}_F$, the operation table of
Definition~\ref{def:ratup-fieldup-concrete-operation-table}, and the concrete
exit object $\mathcal E_F$ of
Definition~\ref{def:ratup-fieldup-concrete-exit-object}. Let
$(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)$ be the concrete denominator tuple, let
$h,k,\ell:\mathsf{BHist}$ satisfy $C_D^u(h)$, $C_D^u(k)$, $C_D^u(\ell)$, and
$\mathsf{ThreeSupp}_D^u(h,k,\ell)$, and let
$s,t,\upsilon,v,w,z:\mathsf{BMark}$ and $L,R,r:\mathsf{BHist}$ be the selected
selectors and contexts. For Rat-classified endpoint pairs
$x,x',y,y':\mathsf{BHist}$ satisfying
$$
\mathsf{RatHistoryClassifier}(x,x')
\land
\mathsf{RatHistoryClassifier}(y,y'),
$$
for a carried denominator $q:\mathsf{BHist}$ satisfying
$\mathsf{RatHistoryCarrier}(q)$, fix apart-zero context data
$L_0,R_0,\tau:\mathsf{BHist}$ with $p:\mathsf{FieldApartZero}(L_0)$, phase
guard histories $\alpha,\beta:\mathsf{BHist}$, the contextual action
$\Gamma$, the affine source $\mathsf{AffAut}_F$, and the same BEDC history
operations $add$, $mul$, $neg$, and $zero$ satisfying the CommRing hypotheses
used by \autoref{thm:commring-zero-linear-factor-bilateral-exact-package}. If
$$
H:\bigl((\alpha+\beta)\hsame0 \lor (\alpha+(-\beta))\hsame0\bigr),
$$
and histories $u,d:\mathsf{BHist}$ satisfy $U_D(u)$ and
$\mathsf{RatHistoryCarrier}(d)$, then the certificate-obligation record of
Definition~\ref{def:field-certificate-obligations} contains one concrete
$\FieldUp(\RatUp)$ phase-exit boundary consisting of:
\begin{enumerate}
  \item the phase-exit record certificate boundary of
  \autoref{thm:ratup-fieldup-phase-exit-record-certificate-boundary} for the
  displayed source, operation table, exit object, denominator tuple, histories,
  selectors, contexts, endpoint pairs, carried denominator, apart-zero data,
  guard hypothesis, contextual action, affine source, operations, and
  external-unit witness;
  \item the final row inventory absorbed by
  \autoref{thm:ratup-fieldup-record-absorbs-final-row-inventory} for the same
  denominator tuple, histories, carrier witnesses, selectors, contexts, and
  support witness;
  \item the carried-external classifier separation supplied by
  \autoref{thm:ratup-fieldup-phase-exit-record-final-inventory-separation} for
  the same source, operation table, exit object, denominator tuple, histories,
  selectors, contexts, endpoint pairs, carried denominator, apart-zero data,
  guard hypothesis, contextual action, affine source, operations,
  external-unit witness, and carried denominator endpoint.
\end{enumerate}
These three row families are read as one certificate-record obligation over the
displayed $\mathsf{BHist}$ source, with no source, tuple, selector, context,
endpoint, guard, operation, external-unit witness, or carried endpoint
reindexed.
\end{theorem}
\begin{proof}
Instantiate
\autoref{thm:ratup-fieldup-record-absorbs-final-row-inventory} with the
displayed denominator tuple, histories $h,k,\ell$, carrier witnesses, selectors
$s,t,\upsilon,v,w,z$, contexts $L,R,r$, and the support witness
$\mathsf{ThreeSupp}_D^u(h,k,\ell)$. This places the complete final row
inventory in the certificate-obligation record.

Next instantiate
\autoref{thm:ratup-fieldup-phase-exit-record-final-inventory-separation} with
the same concrete source, operation table, exit object, denominator tuple,
histories, selectors, contexts, Rat endpoint pairs, carried denominator,
apart-zero data, phase guard histories, contextual action, affine source,
operations, guard hypothesis, external-unit witness, and carried denominator
endpoint. Project its phase-exit boundary row and its carried-external
classifier separation row.

The projected boundary row contains
\autoref{thm:ratup-fieldup-phase-exit-record-certificate-boundary} at the same
indices, while the absorbed inventory row is already stored in the certificate
record. Conjunction introduction repacks the boundary, the absorbed inventory,
and the carried-external separation as one concrete certificate-record
obligation. No source, tuple, selector, context, endpoint, guard, witness, or
operation is reindexed.
\end{proof}

Per-object template: traditional target rational field phase-exit boundary as
certificate-record obligation; BEDC source $\mathsf{BHist}$, $\mathsf{BMark}$,
$\RatUp$, $\CommRingUp$, and $\FieldUp$; carriers
$\mathsf{RatHistoryCarrier}$, $C_D^u$, $U_D$, and $\mathsf{AffAut}_F$;
classifiers $\mathsf{RatHistoryClassifier}$, $R_D^u$, $\hsame$, and affine
classifier; stability obligations phase-exit record certificate boundary,
record-absorbed final row inventory, and carried-external classifier
separation; ledger denominator tuple, operation table, external-unit endpoint,
carried denominator endpoint, guard witness, contextual action, affine source,
histories, contexts, selectors, and carrier witnesses; core theorem this
block; exactness the displayed certificate-record phase-exit boundary;
downstream consumer Phase Algebra Hierarchy $\FieldUp(\RatUp)$ exit criterion;
status $\mathsf{ProofTarget}$.
\begin{closurestatus}{\FieldUp}
  \theoryclosure{\matureClosure}
  \scopeclosed{The mature paper scope is the concrete $\FieldUp(\RatUp)$ exit record: certificate-record boundary, final row inventory, selector-independent exit rows, concrete operation table, apart-zero inverse rows, denominator normalization, endpoint exactness, carried-external separation, and the conservative paper bridge over $\RatUp$, $\CommRingUp$, $\FieldUp$, and $\mathsf{BHist}$.}
  \formalstatus{\bridgeCheckedV}
  \leantarget{BEDC.Derived.FieldUp.FieldUp\_StdBridge}
  \bridgestatus{bridgeChecked}
  \notclaimed{Unchecked bridge targets, arbitrary fields, field extensions, algebraic closure, valuation theory, ordered-field completeness, and machine-checked standard bridge closure remain outside this boundary.}
  \upgradepath{No further upgrade -- this chapter is at the top of the formal axis.}
\end{closurestatus}
