\section{The certificate}

\begin{definition}[Field source specification]
\label{def:field-source-specification}
The source is a certified commutative ring carrier plus constructive apartness from zero, packaged through the derived predicate $\mathsf{NonZero}_F$.
\end{definition}

\begin{definition}[Field pattern specification]
\label{def:field-pattern-specification}
The pattern is commutative-ring expression formation extended by inverse formation only from a pair $(a,p)$ with $p:\mathsf{NonZero}_F(a)$.
\end{definition}

\begin{definition}[Field classifier specification]
\label{def:field-classifier-specification}
The classifier is ring sameness $\sim_F$. Nonzero evidence is stable under classifier replacement: from $a\sim_F b$ and $p:\mathsf{NonZero}_F(a)$ the certificate supplies transported evidence $q:\mathsf{NonZero}_F(b)$.
\end{definition}

\begin{definition}[Field stability certificate]
\label{def:field-stability-certificate}
The stability certificate consists of:
\begin{enumerate}
  \item all commutative-ring stability fields
  \item contradiction of $\mathsf{NonZero}_F(0)$
  \item stability of $\mathsf{NonZero}_F$ under the carrier classifier
  \item inverse closure for explicit witnesses $p:\mathsf{NonZero}_F(a)$
  \item left and right multiplicative inverse laws indexed by each witness $p:\mathsf{NonZero}_F(a)$
\end{enumerate}
\end{definition}

\begin{theorem}[Field inverse congruence from apartness transport]
\label{thm:field-inverse-congruence-from-apartness}
For a BEDC history-level $\FieldUp$ candidate supplying multiplication on $\mathsf{BHist}$, a unit $1$, a unary nonzero certificate $\mathsf{NonZero}_F$, an inverse operation $\mathsf{inv}(a,p)$ indexed by $p:\mathsf{NonZero}_F(a)$, multiplicative associativity, left and right unit laws, multiplication congruence under $\hsame$, and left and right inverse laws indexed by nonzero witnesses, if $a\hsame b$, $p:\mathsf{NonZero}_F(a)$, and $q:\mathsf{NonZero}_F(b)$ is any available witness, then $\mathsf{inv}(a,p)\hsame\mathsf{inv}(b,q)$. When $q$ is obtained from $p$ along $a\hsame b$, the certificate transport clause supplies that witness.
\end{theorem}
\begin{proof}
Transport the left inverse law for $\mathsf{inv}(a,p)$ across $a\hsame b$ to obtain a left inverse witness over $b$. Combine this transported witness with the right inverse law for $\mathsf{inv}(b,q)$ at $b$. The group left/right inverse uniqueness argument, applied inside the multiplicative group of nonzero field elements, identifies the transported inverse with the chosen inverse at $b$.
\end{proof}
\leanchecked{BEDC.Derived.FieldUp.field\_inverse\_congruence\_from\_apartness}

\begin{theorem}[Field inverse cancel from apartness-indexed inverses]
\label{thm:field-inverse-cancel-from-apartness}
For a BEDC history-level $\FieldUp$ candidate supplying multiplication on
$\mathsf{BHist}$, a unit $1$, a unary nonzero certificate
$\mathsf{NonZero}_F$, an inverse operation $\mathsf{inv}(a,p)$ indexed by
$p:\mathsf{NonZero}_F(a)$, multiplicative associativity, left and right unit
laws, multiplication congruence under $\hsame$, and left and right inverse
laws indexed by nonzero witnesses, if $p:\mathsf{NonZero}_F(a)$,
$q:\mathsf{NonZero}_F(b)$, and
$\mathsf{inv}(a,p)\hsame\mathsf{inv}(b,q)$, then $a\hsame b$.
\end{theorem}
\begin{proof}
Transport the left inverse law for $a$ across the equality of
proof-indexed inverses to make $a$ a right inverse of the common inverse
$\mathsf{inv}(b,q)$. The right inverse law for $b$ makes $b$ the
matching left inverse. Group left/right inverse uniqueness identifies $b$
with $a$, and symmetry gives $a\hsame b$.
\end{proof}
\leanchecked{BEDC.Derived.FieldUp.field\_inverse\_cancel\_from\_apartness}

\begin{theorem}[Field inverse classifier exactness from apartness]
\label{thm:field-inverse-classifier-exact-from-apartness}
For a BEDC history-level $\FieldUp$ candidate supplying multiplication on
$\mathsf{BHist}$, a unit $1$, a unary nonzero certificate
$\mathsf{NonZero}_F$, an inverse operation $\mathsf{inv}(a,p)$ indexed by
$p:\mathsf{NonZero}_F(a)$, multiplicative associativity, left and right unit
laws, multiplication congruence under $\hsame$, and left and right inverse
laws indexed by nonzero witnesses, any elements $a,b:\mathsf{BHist}$ with
explicit witnesses $p_a:\mathsf{NonZero}_F(a)$ and
$p_b:\mathsf{NonZero}_F(b)$ satisfy
$$
a\hsame b \Longleftrightarrow
\mathsf{inv}(a,p_a)\hsame\mathsf{inv}(b,p_b).
$$
\end{theorem}
\begin{proof}
For the forward direction, apply
\autoref{thm:field-inverse-congruence-from-apartness} to $a\hsame b$, using
the displayed witnesses $p_a$ and $p_b$ as the two proof indices. This gives
the classifier equality of the two indexed inverses.

For the reverse direction, apply
\autoref{thm:field-inverse-cancel-from-apartness} to the supplied equality
between indexed inverses. Its cancellation conclusion is exactly $a\hsame b$,
so the two implications combine into the stated classifier exactness.
\end{proof}

\begin{theorem}[Field inverse of an indexed inverse]
\label{thm:field-inverse-involutive-from-apartness}
For a BEDC history-level $\FieldUp$ candidate with multiplication on
$\mathsf{BHist}$, unit $1$, proof-indexed inverse $\mathsf{inv}(a,p)$,
associativity, left and right identity, multiplication congruence, and left
inverse laws indexed by nonzero witnesses, if
$p_a:\mathsf{NonZero}_F(a)$ and
$p_{\mathsf{inv}}:\mathsf{NonZero}_F(\mathsf{inv}(a,p_a))$, then
$$
\mathsf{inv}(\mathsf{inv}(a,p_a),p_{\mathsf{inv}})\hsame a.
$$
\end{theorem}
\begin{proof}
Set the middle element in the inverse-uniqueness argument to
$\mathsf{inv}(a,p_a)$. The left inverse law for $p_{\mathsf{inv}}$ gives
$$
\mathsf{inv}(\mathsf{inv}(a,p_a),p_{\mathsf{inv}})\mathsf{inv}(a,p_a)\hsame 1.
$$

The left inverse law for $p_a$ gives
$$
\mathsf{inv}(a,p_a)a\hsame 1.
$$

Associativity, multiplication congruence, and the identity laws give the
standard left/right inverse uniqueness chain
$x\hsame x\,1\hsame x(yz)\hsame (xy)z\hsame 1\,z\hsame z$, with
$x=\mathsf{inv}(\mathsf{inv}(a,p_a),p_{\mathsf{inv}})$,
$y=\mathsf{inv}(a,p_a)$, and $z=a$. Hence the two outer witnesses are
identified, which is the stated equality.
\end{proof}

\begin{theorem}[Field inverse product reversal from apartness-indexed inverses]
\label{thm:field-inverse-product-reverse-from-apartness}
For a BEDC history-level $\FieldUp$ candidate with proof-indexed inverses,
explicit witnesses $p_{ab}:\mathsf{NonZero}_F(ab)$,
$p_a:\mathsf{NonZero}_F(a)$, and $p_b:\mathsf{NonZero}_F(b)$ determine
the product inverse by
$$
\mathsf{inv}(ab,p_{ab})\hsame \mathsf{inv}(b,p_b)\mathsf{inv}(a,p_a).
$$
\end{theorem}
\begin{proof}
The reversed product $\mathsf{inv}(b,p_b)\mathsf{inv}(a,p_a)$ is a right
inverse of $ab$: reassociate to expose $b\mathsf{inv}(b,p_b)$, collapse it
to the unit, and finish with the right inverse law for $a$. The indexed left
inverse of $ab$ and group uniqueness of left and right inverse witnesses
identify the chosen inverse of $ab$ with this reversed product.
\end{proof}
\leanchecked{BEDC.Derived.FieldUp.field\_inverse\_product\_reverse\_from\_apartness}

\begin{theorem}[Field inverse three-factor product reversal from apartness-indexed inverses]
\label{thm:field-inverse-three-factor-product-reversal-from-apartness}
For a BEDC history-level $\FieldUp$ candidate with multiplication associativity,
multiplication congruence, left and right identity, proof-indexed inverse laws,
product nonzero exclusion, and conversion maps
$$
\alpha_{ab}:\mathsf{ApartZero}_F(\mathsf{mul}(a,b))\to
\mathsf{NonZero}_F(\mathsf{mul}(a,b)),
\quad
\alpha_a:\mathsf{ApartZero}_F(a)\to\mathsf{NonZero}_F(a),
$$
$$
\alpha_b:\mathsf{ApartZero}_F(b)\to\mathsf{NonZero}_F(b),
\quad
\alpha_c:\mathsf{ApartZero}_F(c)\to\mathsf{NonZero}_F(c),
$$
where $\mathsf{ApartZero}_F(h):=(h\hsame0\to\bot)$, any witness
$$
p_{abc}:\mathsf{NonZero}_F(\mathsf{mul}(\mathsf{mul}(a,b),c))
$$
induces witnesses $p_{ab}$, $p_a$, $p_b$, and $p_c$ such that
$$
\mathsf{inv}(\mathsf{mul}(\mathsf{mul}(a,b),c),p_{abc})\hsame
\mathsf{mul}(\mathsf{inv}(c,p_c),
\mathsf{mul}(\mathsf{inv}(b,p_b),\mathsf{inv}(a,p_a))).
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:field-product-nonzero-excludes-zero-factors} to
$p_{abc}$ for the binary product $(\mathsf{mul}(a,b),c)$. This gives
$q_{ab}:\mathsf{ApartZero}_F(\mathsf{mul}(a,b))$ and
$q_c:\mathsf{ApartZero}_F(c)$; set $p_{ab}:=\alpha_{ab}(q_{ab})$ and
$p_c:=\alpha_c(q_c)$.

Apply the same theorem to $p_{ab}$ for the binary product $(a,b)$. The two
resulting apartness witnesses $q_a:\mathsf{ApartZero}_F(a)$ and
$q_b:\mathsf{ApartZero}_F(b)$ determine $p_a:=\alpha_a(q_a)$ and
$p_b:=\alpha_b(q_b)$.

Now apply \autoref{thm:field-inverse-product-reverse-from-apartness} to
$(\mathsf{mul}(a,b),c)$ with witnesses $p_{abc}$, $p_{ab}$, and $p_c$. Apply it
again to $(a,b)$ with witnesses $p_{ab}$, $p_a$, and $p_b$. Multiplication
congruence transports the second equality into the right factor of the first
one, and associativity plus transitivity of $\hsame$ gives the displayed
three-factor reversed product.
\end{proof}

Per-object certificate: Traditional target: inverse of a finite triple product.
BEDC source: $\FieldUp$ plus zero-product exclusion. Carrier choice:
$\mathsf{BHist}$. Classifier choice: $\hsame$. Stability obligations: product
nonzero exclusion, inverse product reversal, associativity, and multiplication
congruence. Ledger: $p_{abc}$ plus induced factor witnesses. Core theorem list:
Theorem~\ref{thm:field-inverse-three-factor-product-reversal-from-apartness}.
Exactness predicate: reversed inverse equality. Standard bridge: deferred.
Status: $\mathsf{ProofTarget}$.

\begin{theorem}[Field multiplication by an indexed right inverse cancels]
\label{thm:field-mul-inverse-right-cancel-from-apartness}
For a BEDC history-level $\FieldUp$ candidate with proof-indexed inverses,
any witness $p_b:\mathsf{NonZero}_F(b)$ determines
$$
(a b)\mathsf{inv}(b,p_b)\hsame a.
$$
\end{theorem}
\begin{proof}
Reassociate $(a b)\mathsf{inv}(b,p_b)$ to
$a(b\mathsf{inv}(b,p_b))$. The apartness-indexed right inverse law collapses
the tail to the unit, and the right identity law leaves $a$.
\end{proof}
\leanchecked{BEDC.Derived.FieldUp.field\_mul\_inverse\_right\_cancel\_from\_apartness}

\begin{theorem}[Field right-multiplication equation solution from apartness]
\label{thm:field-right-mul-equation-solution-from-apartness}
For a BEDC history-level $\FieldUp$ candidate with proof-indexed inverses,
any witness $p_b:\mathsf{NonZero}_F(b)$ makes $xb\hsame a$ imply
$x\hsame a\mathsf{inv}(b,p_b)$.
\end{theorem}
\begin{proof}
First cancel the indexed right inverse in $(x b)\mathsf{inv}(b,p_b)$ to
identify it with $x$. Then transport the supplied equality $x b\hsame a$
through right multiplication by $\mathsf{inv}(b,p_b)$.
\end{proof}
\leanchecked{BEDC.Derived.FieldUp.field\_right\_mul\_equation\_solution\_from\_apartness}

\begin{theorem}[Field right-multiplication equation exactness from apartness]
\label{thm:field-right-mul-equation-exact-from-apartness}
For a BEDC history-level $\FieldUp$ candidate with proof-indexed inverses,
any witness $p_b:\mathsf{NonZero}_F(b)$ makes the right-multiplication
equation and its displayed solution equivalent:
$$
x b\hsame a
\Longleftrightarrow
x\hsame a\mathsf{inv}(b,p_b).
$$
\end{theorem}
\begin{proof}
For the forward direction, apply
Theorem~\ref{thm:field-right-mul-equation-solution-from-apartness}. This is
the one-way solver from the equation $x b\hsame a$ to the proof-indexed
solution $x\hsame a\mathsf{inv}(b,p_b)$.

For the reverse direction, transport the assumed solution through right
multiplication by $b$. The transported right side is
$(a\mathsf{inv}(b,p_b))b$.

Reassociate the factors as $a(\mathsf{inv}(b,p_b)b)$. The indexed left
inverse law collapses $\mathsf{inv}(b,p_b)b$ to the unit, and the right
identity law leaves $a$.
\end{proof}
\leanchecked{BEDC.Derived.FieldUp.field\_right\_mul\_equation\_solution\_from\_apartness\_iff}
\leanchecked{BEDC.Derived.FieldUp.field\_right\_mul\_equation\_exact\_from\_apartness}

\begin{theorem}[Field multiplication by an indexed left inverse cancels]
\label{thm:field-mul-inverse-left-cancel-from-apartness}
For a BEDC history-level $\FieldUp$ candidate with proof-indexed inverses,
any witness $p_a:\mathsf{NonZero}_F(a)$ determines
$$
\mathsf{inv}(a,p_a)(a b)\hsame b.
$$
\end{theorem}
\begin{proof}
Reassociate $\mathsf{inv}(a,p_a)(a b)$ to
$(\mathsf{inv}(a,p_a)a)b$. The apartness-indexed left inverse law collapses
the head to the unit, and the left identity law leaves $b$.
\end{proof}
\leanchecked{BEDC.Derived.FieldUp.field\_mul\_inverse\_left\_cancel\_from\_apartness}

\begin{theorem}[Field left-multiplication equation solution from apartness]
\label{thm:field-left-mul-equation-solution-from-apartness}
For a BEDC history-level $\FieldUp$ candidate with proof-indexed inverses,
any witness $p_a:\mathsf{NonZero}_F(a)$ makes $a x\hsame c$ imply
$x\hsame \mathsf{inv}(a,p_a)c$.
\end{theorem}
\begin{proof}
First use Theorem~\ref{thm:field-mul-inverse-left-cancel-from-apartness}
to identify $x$ with $\mathsf{inv}(a,p_a)(a x)$.

Then transport the supplied equality $a x\hsame c$ through left multiplication
by $\mathsf{inv}(a,p_a)$, yielding
$\mathsf{inv}(a,p_a)(a x)\hsame \mathsf{inv}(a,p_a)c$.
\end{proof}

\begin{theorem}[Field left-multiplication equation exactness from apartness]
\label{thm:field-left-mul-equation-exact-from-apartness}
For a BEDC history-level $\FieldUp$ candidate with proof-indexed inverses,
any witness $p_a:\mathsf{NonZero}_F(a)$ makes the left-multiplication
equation and its displayed solution equivalent:
$$
a x\hsame c
\Longleftrightarrow
x\hsame \mathsf{inv}(a,p_a)c.
$$
\end{theorem}
\begin{proof}
For the forward direction, apply
Theorem~\ref{thm:field-left-mul-equation-solution-from-apartness}. This is
the one-way solver from the equation $a x\hsame c$ to the proof-indexed
solution $x\hsame \mathsf{inv}(a,p_a)c$.

For the reverse direction, transport the assumed solution through left
multiplication by $a$. The transported right side is
$a(\mathsf{inv}(a,p_a)c)$.

Reassociate the factors as $(a\mathsf{inv}(a,p_a))c$. The indexed right
inverse law collapses $a\mathsf{inv}(a,p_a)$ to the unit, and the left
identity law leaves $c$.
\end{proof}
\leanchecked{BEDC.Derived.FieldUp.field\_left\_mul\_equation\_exact\_from\_apartness}

\begin{theorem}[Field middle multiplication equation solution from apartness]
\label{thm:field-middle-mul-equation-solution-from-apartness}
For a BEDC history-level $\FieldUp$ candidate with proof-indexed inverses,
explicit witnesses $p_a:\mathsf{NonZero}_F(a)$ and
$p_b:\mathsf{NonZero}_F(b)$ make
$$
(a x)b\hsame c
$$
imply
$$
x\hsame \mathsf{inv}(a,p_a)(c\mathsf{inv}(b,p_b)).
$$
\end{theorem}
\begin{proof}
First solve the right factor equation using the indexed right inverse of $b$,
obtaining $a x\hsame c\mathsf{inv}(b,p_b)$. Then cancel the left factor
with the indexed left inverse of $a$, transporting the right-factor solution
through left multiplication by $\mathsf{inv}(a,p_a)$.
\end{proof}
\leanchecked{BEDC.Derived.FieldUp.field\_middle\_mul\_equation\_solution\_from\_apartness}

\begin{theorem}[Field middle multiplication equation exactness from apartness]
\label{thm:field-middle-mul-equation-exact-from-apartness}
For a BEDC history-level $\FieldUp$ candidate with proof-indexed inverses,
explicit witnesses $p_a:\mathsf{NonZero}_F(a)$ and
$p_b:\mathsf{NonZero}_F(b)$ make the middle equation and its displayed
solution equivalent:
$$
(a x)b\hsame c
\Longleftrightarrow
x\hsame \mathsf{inv}(a,p_a)(c\mathsf{inv}(b,p_b)).
$$
\end{theorem}
\begin{proof}
For the forward direction, apply
Theorem~\ref{thm:field-middle-mul-equation-solution-from-apartness}. It is
exactly the one-way solver from the middle equation to the proof-indexed
displayed solution.

For the reverse direction, transport the assumed solution through the
congruence of left multiplication by $a$ and right multiplication by $b$.
The resulting term is
$\bigl(a(\mathsf{inv}(a,p_a)(c\mathsf{inv}(b,p_b)))\bigr)b$.

Reassociate the factors so that $a\mathsf{inv}(a,p_a)$ is exposed at the
left and $\mathsf{inv}(b,p_b)b$ is exposed at the right. The indexed right
inverse law collapses $a\mathsf{inv}(a,p_a)$ to the unit, and the indexed
left inverse law collapses $\mathsf{inv}(b,p_b)b$ to the unit. The left and
right identity laws then leave $c$.
\end{proof}
\leanchecked{BEDC.Derived.FieldUp.field\_middle\_mul\_equation\_exact\_from\_apartness}
\leanvariant{BEDC.Derived.FieldUp.field\_middle\_mul\_equation\_solution\_from\_apartness\_iff}
