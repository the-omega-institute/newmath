\begin{theorem}[RatUp FieldUp operation-table final inventory four-endpoint coverage]
\label{thm:ratup-fieldup-operation-table-final-inventory-four-endpoint-coverage}
Fix the concrete source $\mathsf{RatUpFieldSource}_F$, the operation table of
Definition~\ref{def:ratup-fieldup-concrete-operation-table}, and the concrete
exit object $\mathcal E_F$ of
Definition~\ref{def:ratup-fieldup-concrete-exit-object}. Let
$(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)$ be the concrete denominator tuple, let
$h,k,\ell:\mathsf{BHist}$ satisfy $C_D^u(h)$, $C_D^u(k)$, $C_D^u(\ell)$, and
$\mathsf{ThreeSupp}_D^u(h,k,\ell)$, and let
$s,t,u,v,w,z:\mathsf{BMark}$ and $L,R,r:\mathsf{BHist}$ be the chosen selectors
and contexts. For Rat-classified endpoint pairs
$a,a',b,b':\mathsf{BHist}$ satisfying
$$
\mathsf{RatHistoryClassifier}(a,a')
\land
\mathsf{RatHistoryClassifier}(b,b'),
$$
and for every carried denominator $q:\mathsf{BHist}$ satisfying
$\mathsf{RatHistoryCarrier}(q)$, one concrete $\mathsf{BHist}$-anchored
$\FieldUp(\RatUp)$ package contains, at the same time:
\begin{enumerate}
  \item the assembled certificate operation-table coverage of
  \autoref{thm:ratup-fieldup-assembled-certificate-operation-table-coverage};
  \item the concrete operation-table laws of
  \autoref{thm:ratup-fieldup-concrete-operation-table-laws};
  \item the final row inventory of
  \autoref{thm:ratup-fieldup-final-row-inventory-coverage};
  \item the carried-denominator unit obstruction of
  \autoref{thm:rat-denominator-carried-continuation-unit-obstruction};
  \item the four Rat-classified product exclusions
  $\mathsf{mul}(a,b)\hsame\emp\to\bot$,
  $\mathsf{mul}(a,b')\hsame\emp\to\bot$,
  $\mathsf{mul}(a',b)\hsame\emp\to\bot$, and
  $\mathsf{mul}(a',b')\hsame\emp\to\bot$.
\end{enumerate}
Thus the Phase Algebra Hierarchy consumer reads the operation table, final
inventory rows, carried-denominator unit obstruction, and all four
Rat-classified product exclusions from one concrete $\mathsf{BHist}$ package.
\end{theorem}
\begin{proof}
First build the table, final-inventory, unit-obstruction, and diagonal product
rows. Apply
\autoref{thm:ratup-fieldup-operation-table-final-inventory-nonzero-coverage} to
the concrete source, operation table, exit object, denominator tuple, histories,
selectors, contexts, Rat-classified endpoint pairs, and carried denominator
$q$. This gives the assembled operation-table coverage, concrete
operation-table laws, final row inventory, carried-denominator unit obstruction,
and the diagonal Rat-classified product rows inside the same
$\mathsf{BHist}$-anchored package.

Next build the four endpoint product rows. Apply
\autoref{thm:ratup-fieldup-four-endpoint-product-nonempty} to the same
field-operation data, zero-empty identification, apart-zero transport,
zero-apart contradiction, and the two displayed
$\mathsf{RatHistoryClassifier}$ hypotheses. This supplies the two diagonal rows
and the two mixed rows for the same endpoint quadruple.

Finally keep the same source, table, exit object, denominator tuple, selectors,
contexts, endpoint pairs, and carried denominator. Conjunction introduction
packages the table rows, final inventory, unit obstruction, and four endpoint
product exclusions as the concrete final-inventory $\FieldUp(\RatUp)$ package
claimed.
\end{proof}

Per-object template: traditional target final $\FieldUp(\RatUp)$ exit package
with four Rat-classified product rows; BEDC source $\mathsf{BHist}$,
$\mathsf{BMark}$, and $\mathsf{RatUpFieldSource}_F$; carrier
$\mathsf{RatHistoryCarrier}$ and $C_D^u$; classifier
$\mathsf{RatHistoryClassifier}$ and $R_D^u$; stability obligations
operation-table coverage, final row inventory, carried-denominator unit
obstruction, and four endpoint product nonemptiness; ledger selector sextuple
$s,t,u,v,w,z$, contexts $L,R,r$, endpoint quadruple $a,a',b,b'$, and carried
denominator $q$; exactness one concrete final-inventory package with four
products apart from $\emp$; downstream consumer Phase Algebra Hierarchy
$\FieldUp(\RatUp)$; status $\mathsf{ProofTarget}$.

\begin{theorem}[RatUp FieldUp operation-table final inventory two-sided unit coverage]
\label{thm:ratup-fieldup-operation-table-final-inventory-two-sided-unit-coverage}
Fix the concrete source $\mathsf{RatUpFieldSource}_F$, the operation table of
Definition~\ref{def:ratup-fieldup-concrete-operation-table}, and the concrete
exit object $\mathcal E_F$ of
Definition~\ref{def:ratup-fieldup-concrete-exit-object}. Let
$(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)$ be the concrete denominator tuple, let
$h,k,\ell:\mathsf{BHist}$ satisfy $C_D^u(h)$, $C_D^u(k)$, $C_D^u(\ell)$, and
$\mathsf{ThreeSupp}_D^u(h,k,\ell)$, and let
$s,t,u,v,w,z:\mathsf{BMark}$ and $L,R,r:\mathsf{BHist}$ be the chosen selectors
and contexts. For Rat-classified endpoint pairs
$a,a',b,b':\mathsf{BHist}$ satisfying
$$
\mathsf{RatHistoryClassifier}(a,a')
\land
\mathsf{RatHistoryClassifier}(b,b'),
$$
and for every carried denominator $q:\mathsf{BHist}$ satisfying
$\mathsf{RatHistoryCarrier}(q)$, one concrete $\mathsf{BHist}$-anchored
$\FieldUp(\RatUp)$ package contains, at the same time:
\begin{enumerate}
  \item the assembled certificate operation-table coverage of
  \autoref{thm:ratup-fieldup-assembled-certificate-operation-table-coverage};
  \item the concrete operation-table laws of
  \autoref{thm:ratup-fieldup-concrete-operation-table-laws};
  \item the final row inventory of
  \autoref{thm:ratup-fieldup-final-row-inventory-coverage};
  \item the four Rat-classified product exclusions
  $\mathsf{mul}(a,b)\hsame\emp\to\bot$,
  $\mathsf{mul}(a,b')\hsame\emp\to\bot$,
  $\mathsf{mul}(a',b)\hsame\emp\to\bot$, and
  $\mathsf{mul}(a',b')\hsame\emp\to\bot$ from
  \autoref{thm:ratup-fieldup-operation-table-final-inventory-four-endpoint-coverage};
  \item the append right-unit denial at $q$,
  $$
  (\forall x:\mathsf{BHist}.\;
  C_D(x)\to R_D(\mu_D(x,q),x))\to \mathsf{False};
  $$
  \item the append left-unit denial at $q$,
  $$
  (\forall x:\mathsf{BHist}.\;
  C_D(x)\to R_D(\mu_D(q,x),x))\to \mathsf{False};
  $$
  \item the relational denial for $\Cont(x,q,r)$,
  $$
  (\forall x,r:\mathsf{BHist}.\;
  C_D(x)\to \Cont(x,q,r)\to R_D(r,x))\to \mathsf{False};
  $$
  \item the relational denial for $\Cont(q,x,r)$,
  $$
  (\forall x,r:\mathsf{BHist}.\;
  C_D(x)\to \Cont(q,x,r)\to R_D(r,x))\to \mathsf{False}.
  $$
\end{enumerate}
Here $C_D,R_D,\mu_D$ are the strict-denominator presentation of
Definition~\ref{def:field-rat-denominator-concrete-continuation-instance},
identified there with $\mathsf{RatHistoryCarrier}$,
$\mathsf{RatHistoryClassifier}$, and append. Thus the Phase Algebra Hierarchy
consumer can read the operation table, final inventory rows, Rat-classified
product exclusions, and both append and relational one-sided unit denials from
one concrete $\mathsf{BHist}$ package without changing source, selectors,
contexts, or denominator tuple.
\end{theorem}
\begin{proof}
First build the operation-table, final-inventory, unit-obstruction, and four
endpoint product rows. Apply
\autoref{thm:ratup-fieldup-operation-table-final-inventory-four-endpoint-coverage} to
the concrete source, operation table, exit object, denominator tuple, histories,
selectors, contexts, Rat-classified endpoint pairs, and carried denominator
$q$. This gives the assembled operation-table coverage, the concrete
operation-table laws, the final inventory, carried-denominator unit
obstruction, and the four Rat-classified product exclusions inside the same
$\mathsf{BHist}$-anchored package.

Next build the strict denominator rows. Apply
\autoref{thm:rat-denominator-append-continuation-final-exit-obstruction-guard}
at the carried denominator $q$, using the identification of $C_D(q)$ with
$\mathsf{RatHistoryCarrier}(q)$ in
Definition~\ref{def:field-rat-denominator-concrete-continuation-instance}. Its
append presentation supplies the right-unit and left-unit denials, and its
relational presentation supplies the two $\Cont$ endpoint denials.

Finally, keep the witnesses named by the same source, operation table, exit
object, denominator tuple, selectors, contexts, endpoint pairs, and carried
denominator. Conjunction introduction packages these row families as the single
concrete final $\FieldUp(\RatUp)$ exit package claimed.
\end{proof}

Per-object template: traditional target $\FieldUp$ final concrete exit package;
BEDC source $\mathsf{BHist}$, $\Cont$, $\mathsf{RatHistoryCarrier}$, and
$\mathsf{RatHistoryClassifier}$; carrier $C_D^u$; classifier $R_D^u$; stability
obligations operation-table coverage plus two-sided unit obstruction; ledger
selectors $s,t,u,v,w,z$ and contexts $L,R,r$; exactness as the concrete final
package; downstream consumer Phase Algebra Hierarchy $\FieldUp(\RatUp)$; status
$\mathsf{ProofTarget}$.

\begin{theorem}[RatUp FieldUp operation-table final inventory four-endpoint unit coverage]
\label{thm:ratup-fieldup-operation-table-final-inventory-four-endpoint-unit-coverage}
Fix the concrete source $\mathsf{RatUpFieldSource}_F$, the operation table of
Definition~\ref{def:ratup-fieldup-concrete-operation-table}, and the concrete
exit object $\mathcal E_F$ of
Definition~\ref{def:ratup-fieldup-concrete-exit-object}. Let
$(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)$ be the concrete denominator tuple, let
$h,k,\ell:\mathsf{BHist}$ satisfy $C_D^u(h)$, $C_D^u(k)$, $C_D^u(\ell)$, and
$\mathsf{ThreeSupp}_D^u(h,k,\ell)$, and let
$s,t,u,v,w,z:\mathsf{BMark}$ and $L,R,r:\mathsf{BHist}$ be the chosen selectors
and contexts. For Rat-classified endpoint pairs
$a,a',b,b':\mathsf{BHist}$ satisfying
$$
\mathsf{RatHistoryClassifier}(a,a')
\land
\mathsf{RatHistoryClassifier}(b,b'),
$$
and for every carried denominator $q:\mathsf{BHist}$ satisfying
$\mathsf{RatHistoryCarrier}(q)$, one concrete $\mathsf{BHist}$-anchored
$\FieldUp(\RatUp)$ package contains, at the same time:
\begin{enumerate}
  \item the assembled certificate operation-table coverage of
  \autoref{thm:ratup-fieldup-assembled-certificate-operation-table-coverage};
  \item the concrete operation-table laws of
  \autoref{thm:ratup-fieldup-concrete-operation-table-laws};
  \item the final row inventory of
  \autoref{thm:ratup-fieldup-final-row-inventory-coverage};
  \item the four Rat-classified product exclusions
  $(\mathsf{mul}(a,b)\hsame\emp\to\bot)$,
  $(\mathsf{mul}(a,b')\hsame\emp\to\bot)$,
  $(\mathsf{mul}(a',b)\hsame\emp\to\bot)$, and
  $(\mathsf{mul}(a',b')\hsame\emp\to\bot)$;
  \item the append right-unit denial at $q$,
  $$
  (\forall x:\mathsf{BHist}.\;
  C_D(x)\to R_D(\mu_D(x,q),x))\to \mathsf{False};
  $$
  \item the append left-unit denial at $q$,
  $$
  (\forall x:\mathsf{BHist}.\;
  C_D(x)\to R_D(\mu_D(q,x),x))\to \mathsf{False};
  $$
  \item the relational denial for $\Cont(x,q,r)$,
  $$
  (\forall x,r:\mathsf{BHist}.\;
  C_D(x)\to \Cont(x,q,r)\to R_D(r,x))\to \mathsf{False};
  $$
  \item the relational denial for $\Cont(q,x,r)$,
  $$
  (\forall x,r:\mathsf{BHist}.\;
  C_D(x)\to \Cont(q,x,r)\to R_D(r,x))\to \mathsf{False}.
  $$
\end{enumerate}
Here $C_D,R_D,\mu_D$ are the strict-denominator presentation of
Definition~\ref{def:field-rat-denominator-concrete-continuation-instance},
identified there with $\mathsf{RatHistoryCarrier}$,
$\mathsf{RatHistoryClassifier}$, and append. Thus the Phase Algebra Hierarchy
consumer can read the operation table, final inventory rows, all four
Rat-classified product exclusions, and both append and relational one-sided
unit denials from one concrete $\mathsf{BHist}$ package without changing
source, selectors, contexts, or denominator tuple.
\end{theorem}
\begin{proof}
First apply
\autoref{thm:ratup-fieldup-operation-table-final-inventory-two-sided-unit-coverage}
to the concrete source, operation table, exit object, denominator tuple,
histories, support witness, selectors, contexts, Rat-classified endpoint pairs,
and carried denominator $q$. This supplies the operation-table rows, final
inventory row, diagonal product exclusions, and the four append and relational
one-sided unit denials inside the same $\mathsf{BHist}$-anchored package.

Next apply \autoref{thm:ratup-fieldup-four-endpoint-product-nonempty} to the
same two Rat classifier witnesses. Its four endpoint rows give the two diagonal
product exclusions already present in the two-sided package and add the mixed
rows for $\mathsf{mul}(a,b')$ and $\mathsf{mul}(a',b)$.

Finally keep the same source, operation table, exit object, denominator tuple,
selectors, contexts, endpoint pairs, and carried denominator. Conjunction
introduction packages the operation-table rows, final inventory, all four
product exclusions, and the four one-sided unit denials into the concrete final
$\FieldUp(\RatUp)$ exit package claimed.
\end{proof}

Per-object template: traditional target $\FieldUp$ final concrete four-endpoint
exit package; BEDC source $\mathsf{BHist}$, $\Cont$,
$\mathsf{RatHistoryCarrier}$, and $\mathsf{RatHistoryClassifier}$; carrier
$C_D^u$; classifier $R_D^u$; stability obligations operation-table coverage,
four-endpoint product nonemptiness, and two-sided unit obstruction; ledger
selectors $s,t,u,v,w,z$, contexts $L,R,r$, Rat endpoint pairs, and carried
denominator $q$; exactness as the concrete final four-endpoint package;
downstream consumer Phase Algebra Hierarchy $\FieldUp(\RatUp)$; status
$\mathsf{ProofTarget}$.

\begin{theorem}[RatUp FieldUp final inventory carried-external unit separation]
\label{thm:ratup-fieldup-final-inventory-carried-external-unit-separation}
Fix the concrete source $\mathsf{RatUpFieldSource}_F$, the operation table of
Definition~\ref{def:ratup-fieldup-concrete-operation-table}, and the concrete
exit object $\mathcal E_F$ of
Definition~\ref{def:ratup-fieldup-concrete-exit-object}. Let
$(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)$ be the concrete denominator tuple, let
$h,k,\ell:\mathsf{BHist}$ satisfy $C_D^u(h)$, $C_D^u(k)$, $C_D^u(\ell)$, and
$\mathsf{ThreeSupp}_D^u(h,k,\ell)$, and let
$s,t,\upsilon,v,w,z:\mathsf{BMark}$ and $L,R,r:\mathsf{BHist}$ be the chosen
selectors and contexts. For Rat-classified endpoint pairs
$a,a',b,b':\mathsf{BHist}$ satisfying
$$
\mathsf{RatHistoryClassifier}(a,a')
\land
\mathsf{RatHistoryClassifier}(b,b'),
$$
for every carried denominator $q:\mathsf{BHist}$ satisfying
$\mathsf{RatHistoryCarrier}(q)$, and for histories
$u,d:\mathsf{BHist}$ satisfying $U_D(u)$ and
$\mathsf{RatHistoryCarrier}(d)$, one concrete $\mathsf{BHist}$-anchored
$\FieldUp(\RatUp)$ final-inventory boundary contains, at the same time:
\begin{enumerate}
  \item the operation-table final inventory rows and two-sided unit coverage
  rows of
  \autoref{thm:ratup-fieldup-operation-table-final-inventory-two-sided-unit-coverage}
  for the displayed source, table, exit object, tuple, histories, selectors,
  contexts, endpoint pairs, and carried denominator $q$;
  \item the carried-external classifier separation row
  $$
  \bigl(
  \mathsf{RatHistoryClassifier}(u,d)\lor
  \mathsf{RatHistoryClassifier}(d,u)
  \bigr)\to\mathsf{False}
  $$
  supplied by
  \autoref{thm:field-rat-denominator-external-unit-classifier-disjointness}
  from $U_D(u)$.
\end{enumerate}
Thus the same concrete final exit object $\mathcal E_F$ records both the
complete operation-table final inventory consumed by the Phase Algebra
Hierarchy and the classifier separation between an external denominator unit
and a carried Rat denominator.
\end{theorem}
\begin{proof}
First apply
\autoref{thm:ratup-fieldup-operation-table-final-inventory-two-sided-unit-coverage}
to the concrete source, operation table, exit object, denominator tuple,
histories $h,k,\ell$, support witness, selectors $s,t,\upsilon,v,w,z$,
contexts $L,R,r$, Rat-classified endpoint pairs, and the carried denominator
$q$. This supplies the operation-table coverage rows, concrete operation-table
laws, final row inventory, four Rat-classified product exclusions, and both
append and relational one-sided unit denials inside $\mathcal E_F$.

Next apply
\autoref{thm:field-rat-denominator-external-unit-classifier-disjointness} to
the histories $u,d$ and the displayed witness $U_D(u)$. The resulting
implication rules out both possible Rat-classifier orientations
$\mathsf{RatHistoryClassifier}(u,d)$ and
$\mathsf{RatHistoryClassifier}(d,u)$. The carrier witness
$\mathsf{RatHistoryCarrier}(d)$ identifies the second endpoint as one of the
carried denominator objects governed by the final-inventory boundary.

Finally keep the same source, table, exit object, denominator tuple, selectors,
contexts, endpoint pairs, carried denominator, external-unit witness, and
carried-denominator witness. Conjunction introduction packages the
two-sided final inventory rows and the carried-external classifier separation
row as one concrete $\mathsf{BHist}$-anchored $\FieldUp(\RatUp)$ boundary.
\end{proof}

Per-object template: traditional target final $\FieldUp(\RatUp)$
carried-external unit boundary; BEDC source $\mathsf{BHist}$, $\mathsf{BMark}$,
$\Cont$, and $\mathsf{RatUpFieldSource}_F$; carrier
$\mathsf{RatHistoryCarrier}$ and $C_D^u$; classifier
$\mathsf{RatHistoryClassifier}$ and $R_D^u$; stability obligations
operation-table final inventory, two-sided unit obstruction, and
carried-external classifier separation; ledger selectors
$s,t,\upsilon,v,w,z$, contexts $L,R,r$, carried denominator $q$, external unit
$u$, and carried denominator endpoint $d$; core theorem list
\autoref{thm:ratup-fieldup-operation-table-final-inventory-two-sided-unit-coverage}
and
\autoref{thm:field-rat-denominator-external-unit-classifier-disjointness};
exactness predicate final inventory plus external-unit classifier
disjointness; standard bridge Phase Algebra Hierarchy $\FieldUp(\RatUp)$;
status $\mathsf{ProofTarget}$.

\begin{theorem}[RatUp FieldUp final inventory phase-exit boundary]
\label{thm:ratup-fieldup-final-inventory-phase-exit-boundary}
Fix the concrete source $\mathsf{RatUpFieldSource}_F$, the operation table of
Definition~\ref{def:ratup-fieldup-concrete-operation-table}, and the concrete
exit object $\mathcal E_F$ of
Definition~\ref{def:ratup-fieldup-concrete-exit-object}. Let
$(C_D^u,R_D^u,\mu_D^u,e_D^u,N_D^u)$ be the concrete denominator tuple, let
$h,k,\ell:\mathsf{BHist}$ satisfy $C_D^u(h)$, $C_D^u(k)$, $C_D^u(\ell)$, and
$\mathsf{ThreeSupp}_D^u(h,k,\ell)$, and let
$s,t,\upsilon,v,w,z:\mathsf{BMark}$ and $L,R,r:\mathsf{BHist}$ be the chosen
selectors and contexts. For Rat-classified endpoint pairs
$x,x',y,y':\mathsf{BHist}$ satisfying
$$
\mathsf{RatHistoryClassifier}(x,x')
\land
\mathsf{RatHistoryClassifier}(y,y'),
$$
for every carried denominator $q:\mathsf{BHist}$ satisfying
$\mathsf{RatHistoryCarrier}(q)$, fix apart-zero context data
$L_0,R_0,\tau:\mathsf{BHist}$ with $p:\mathsf{FieldApartZero}(L_0)$, guard
histories $\alpha,\beta:\mathsf{BHist}$, the contextual action $\Gamma$, the
affine source $\mathsf{AffAut}_F$, and the same BEDC history operations $add$,
$mul$, $neg$, and $zero$ satisfying the CommRing hypotheses used by
\autoref{thm:commring-zero-linear-factor-bilateral-exact-package}. If
$$
H:\bigl((\alpha+\beta)\hsame0 \lor (\alpha+(-\beta))\hsame0\bigr),
$$
and histories $u,d:\mathsf{BHist}$ satisfy $U_D(u)$ and
$\mathsf{RatHistoryCarrier}(d)$, then the same exit object $\mathcal E_F$
supplies one concrete $\mathsf{BHist}$-anchored $\FieldUp(\RatUp)$ boundary
containing, at the same time:
\begin{enumerate}
  \item the phase-exit record certificate boundary of
  \autoref{thm:ratup-fieldup-phase-exit-record-certificate-boundary} for the
  displayed source, exit object, denominator tuple, histories, selectors,
  contexts, endpoint pairs, carried denominator, apart-zero data, contextual
  action, affine source, operations, guard hypothesis, and external-unit
  witness;
  \item the operation-table final inventory rows and two-sided unit coverage of
  \autoref{thm:ratup-fieldup-operation-table-final-inventory-two-sided-unit-coverage}
  for the displayed source, table, exit object, denominator tuple, histories,
  selectors, contexts, endpoint pairs, and carried denominator;
  \item the carried-external classifier separation row
  $$
  \bigl(
  \mathsf{RatHistoryClassifier}(u,d)\lor
  \mathsf{RatHistoryClassifier}(d,u)
  \bigr)\to\mathsf{False}
  $$
  of
  \autoref{thm:ratup-fieldup-final-inventory-carried-external-unit-separation}
  for the same external unit, carried endpoint, unit witness, and carried
  denominator endpoint.
\end{enumerate}
Thus the traditional final $\FieldUp(\RatUp)$ exit boundary reads the phase
record certificate, the final-inventory unit coverage, and the carried-external
classifier separation from one unchanged concrete exit package.
\end{theorem}
\begin{proof}
First invoke
\autoref{thm:ratup-fieldup-phase-exit-record-certificate-boundary} with the
displayed guard hypothesis $H$ and the external-unit witness $U_D(u)$. Its
certificate rows are read over the same source, exit object, denominator tuple,
selectors, contexts, Rat endpoints, carried denominator, contextual action,
affine source, and CommRing operations.

Next invoke
\autoref{thm:ratup-fieldup-final-inventory-carried-external-unit-separation}
with the same concrete source, operation table, exit object, denominator tuple,
histories, selectors, contexts, endpoint pairs, carried denominator $q$,
external unit $u$, carried endpoint $d$, witness $U_D(u)$, and carrier witness
$\mathsf{RatHistoryCarrier}(d)$. That theorem already imports the
operation-table final inventory and two-sided unit coverage of
\autoref{thm:ratup-fieldup-operation-table-final-inventory-two-sided-unit-coverage}.

Finally conjoin the two packages. The source, table, exit object, denominator
tuple, selectors, contexts, endpoint pairs, carried denominator, external-unit
witness, carried endpoint, and guard datum are unchanged, so the conclusion is
one concrete $\mathsf{BHist}$-anchored phase-exit boundary rather than two
separately indexed row families.
\end{proof}

Per-object template: traditional target final $\FieldUp(\RatUp)$ exit
boundary; BEDC source $\mathsf{BHist}$, $\RatUp$, $\CommRingUp$, and
$\FieldUp$; carriers $\mathsf{RatHistoryCarrier}$ and denominator carrier
$C_D^u$; classifiers $\mathsf{RatHistoryClassifier}$, $R_D^u$, and $\hsame$;
stability obligations phase-exit record plus final-inventory unit separation;
ledger source, operation table, exit object, denominator tuple, histories,
selectors, contexts, Rat-classified endpoint pairs, carried denominator
$q$, external unit $u$, carried endpoint $d$, guard hypothesis $H$, and witness
$U_D(u)$; exactness one concrete boundary; downstream consumer Phase Algebra
Hierarchy exit criterion; status $\mathsf{ProofTarget}$.
