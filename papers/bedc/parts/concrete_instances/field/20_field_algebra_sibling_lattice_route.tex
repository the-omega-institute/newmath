The terminal operation-table, nonzero-inverse, and denominator-selector rows
for $\FieldUp$ consume the $\RatUp$ denominator surface of
\autoref{ch:concrete-instances-rat-namecert}, the $\RingUp$ operation rows of
\autoref{ch:concrete-instances-ring-namecert}, and the $\CommRingUp$
commutative multiplication surface of
\autoref{ch:concrete-instances-commring-namecert}. This is a finite NameCert
sibling route through the algebra lattice, not a host field primitive.
