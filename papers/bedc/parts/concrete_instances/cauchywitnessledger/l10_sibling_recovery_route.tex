The $\CauchyWitnessLedgerUp$ row is the witness-ledger surface recovered by \autoref{ch:concrete-instances-cauchy-witness-pullback-namecert} and consumed by \autoref{ch:concrete-instances-cauchy-witness-gluing-namecert} inside the Real-completion route. In that lattice position the ledger remains the finite $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ surface over the displayed budget, observation, synchronizer, and classifier rows; it supplies no quotient stream equality, choice principle, or ambient completeness theorem.
