\chapter{A Concrete Naming Certificate for $\FiniteCoverOscillationLadderUp$}
\label{ch:concrete-instances-finite-cover-oscillation-ladder-namecert}
\origin{human}

Following Bishop--Bridges and the Heine--Cantor finite-cover proof, the $\FiniteCoverOscillationLadderUp$ packet records the compact uniform-continuity route as a finite ladder of cover cells, oscillation radii, dyadic tolerances, transport, replay, and provenance. It sits after the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the finite lower-bound fold of \autoref{ch:concrete-instances-finite-lower-bound-fold-namecert}, the metric triangle estimate of \autoref{ch:concrete-instances-metric-triangle-uniform-estimate-namecert}, and the downstream $\UniformModulusUp$ consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet exposes the finite oscillation ladder before any uniform-modulus consumer inspects the route; it is not open-cover compactness, selected subcover choice, quotient equality of functions, or ambient metric completeness.

\begin{definition}[Finite cover oscillation ladder carrier]
\label{def:finite-cover-oscillation-ladder-carrier}
A $\FiniteCoverOscillationLadderUp$ carrier is a finite $\BHist$ packet
$$
L=(K,F,M,O,D,B,U,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, including the finite cover cells used by the compact argument.
\item $F$ is the $\ContinuousMapUp$ graph and pointwise-modulus row.
\item $M$ is the finite cover-cell ledger, indexed by a displayed $\mathsf{ProbeBundle}$.
\item $O$ is the oscillation-radius row assigning a local output bound to each displayed cover cell.
\item $D$ is the dyadic tolerance row, recording the tolerance split and comparison budget.
\item $B$ is the $\FiniteLowerBoundFoldUp$ row that folds the local radii into one precision ledger.
\item $U$ is the $\UniformModulusUp$ handoff row receiving the folded precision.
\item $T$ is the $\MetricTriangleUniformEstimateUp$ row that turns source-cell and target-cell estimates into the final uniform estimate.
\item $H$ records componentwise $\hsame$ transport for compact source, map, cover cells, oscillation radii, dyadic tolerances, fold, handoff, triangle estimate, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the ladder request through $K,F,M,O,D,B,U,T,H$.
\item $P$ is the $\Pkg$ provenance over the compact-uniform-continuity vision, the five sibling anchors, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteCoverOscillationLadderUp}$ row.
\end{enumerate}
The carrier contains no coordinate for open-cover compactness, selected subcovers, external modulus selectors, quotient equality of maps, countable choice, or ambient metric completeness.
\end{definition}

\begin{theorem}[Finite cover oscillation ladder NameCert obligations]
\label{thm:finite-cover-oscillation-ladder-namecert-obligations}
For every accepted $\FiniteCoverOscillationLadderUp$ carrier
$$
L=(K,F,M,O,D,B,U,T,H,C,P,N),
$$
the local $\NameCert_{\FiniteCoverOscillationLadderUp}$ surface consists exactly of compact-metric source exposure, continuous-map row visibility, finite cover-cell admission, oscillation-radius display, dyadic tolerance routing, finite lower-bound folding, uniform-modulus handoff, metric-triangle estimate exposure, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier. The compact source, continuous map, cover-cell ledger, oscillation-radius row, dyadic tolerance row, lower-bound fold, uniform-modulus handoff, and triangle estimate are the only analytic rows available.

Read $K,F,M,O,D$ first. These rows display the compact cells, the pointwise map data, the local oscillation bounds, and the dyadic tolerance budget before any folded precision is exported.

Read $B,U,T$ next. The lower-bound fold produces the shared precision, the uniform-modulus handoff receives it, and the triangle estimate records the final source-and-target metric calculation.

Transport, replay, provenance, and local naming are confined to $H,C,P,N$. Since no excluded compactness, selector, quotient, choice, or completeness coordinate occurs in the carrier, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Finite cover oscillation ladder Heine--Cantor route]
\label{thm:finite-cover-oscillation-ladder-heine-cantor-route}
Every Heine--Cantor consumer that reads a $\FiniteCoverOscillationLadderUp$ packet must expose the compact-metric finite cells $K,M$, the continuous-map pointwise data $F$, the oscillation radii $O$, the dyadic tolerance budget $D$, the lower-bound fold $B$, the uniform-modulus handoff $U$, the metric-triangle estimate $T$, and the structural rows $H,C,P,N$ before it may inspect a tolerance-only uniform-modulus row.
\end{theorem}
\begin{proof}
Apply the NameCert obligations of \autoref{thm:finite-cover-oscillation-ladder-namecert-obligations}. They make the compact cells, map rows, oscillation radii, dyadic budget, lower-bound fold, handoff, and triangle estimate visible in that order.

The finite lower-bound fold is the only precision-producing row. It consumes the local oscillation radii and exposes a single folded precision to the $\UniformModulusUp$ handoff.

The metric triangle estimate is read after the handoff. Thus a Heine--Cantor consumer sees a finite BEDC route from cover cells and local oscillations to a uniform-modulus row, not a selected subcover or host modulus theorem.
\end{proof}

\begin{theorem}[Finite cover oscillation ladder public seed]
\label{thm:finite-cover-oscillation-ladder-public-seed}
The carrier and obligations of \autoref{def:finite-cover-oscillation-ladder-carrier} and \autoref{thm:finite-cover-oscillation-ladder-namecert-obligations} seed a public compact-uniform-continuity packet: downstream uniform-modulus consumers may inspect cover cells, oscillation radii, dyadic tolerance rows, finite lower-bound folding, metric-triangle transport, replay, provenance, and local naming, but they receive no open-cover compactness theorem, selected subcover, external modulus selector, quotient function equality, countable choice, or ambient metric completeness.
\end{theorem}
\begin{proof}
Combine the Heine--Cantor route of \autoref{thm:finite-cover-oscillation-ladder-heine-cantor-route} with the carrier exclusions in \autoref{def:finite-cover-oscillation-ladder-carrier}. The route exposes the finite cells and oscillation ladder before any uniform-modulus handoff is readable.

The structural rows keep the ladder stable under componentwise transport and displayed replay. Since the carrier omits every excluded host endpoint, the public seed is precisely the displayed finite packet consumed by $\UniformModulusUp$.
\end{proof}

\closureat{FiniteCoverOscillationLadderUp}{seedStr}

\begin{closurestatus}{\FiniteCoverOscillationLadderUp}
  \constructivestory{A $\FiniteCoverOscillationLadderUp$ packet is generated from CompactMetric finite-cover cells, ContinuousMap pointwise-modulus rows, oscillation-radius rows, dyadic tolerance rows, a FiniteLowerBoundFold row, a UniformModulus handoff, a MetricTriangleUniformEstimate row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-cover-oscillation-ladder-carrier}, \autoref{thm:finite-cover-oscillation-ladder-namecert-obligations}, \autoref{thm:finite-cover-oscillation-ladder-heine-cantor-route}, and \autoref{thm:finite-cover-oscillation-ladder-public-seed} seed the finite cover-cell and oscillation-radius route from compact continuity data to a uniform-modulus handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, open-cover compactness, selected subcovers, external modulus selectors, quotient function equality, countable choice, ambient metric completeness, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-cell exposure, continuous-map visibility, oscillation-radius display, dyadic tolerance routing, finite lower-bound folding, uniform-modulus handoff, metric-triangle estimate exposure, provenance, and local naming for BEDC.Derived.FiniteCoverOscillationLadderUp.}
\end{closurestatus}
