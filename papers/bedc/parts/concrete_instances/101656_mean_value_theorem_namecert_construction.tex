\chapter{A Concrete Naming Certificate for $\MeanValueTheoremUp$}
\label{ch:concrete-instances-mean-value-theorem-namecert}
\origin{human}

Following Lagrange, Cauchy, and the Bishop--Bridges reading of constructive analysis, the $\MeanValueTheoremUp$ packet records the theorem-facing finite BEDC boundary between endpoint secant data and a displayed derivative quotient row. It consumes the terminal real-name surface of \autoref{ch:concrete-instances-real-namecert}, the graph and modulus rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the derivative quotient interface of \autoref{ch:concrete-instances-derivative-namecert}, the Rolle handoff of \autoref{ch:concrete-instances-rolle-theorem-namecert}, the bisection-facing route of \autoref{ch:concrete-instances-intermediatevalue-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The chapter names a finite route from displayed endpoint values and secant slope to a theorem-facing derivative row; it is not a host differentiability theorem, an equality of quotient reals, or a selected interior point in an ambient completed line.

\begin{definition}[Mean value theorem carrier]
\label{def:mean-value-theorem-carrier}
A $\MeanValueTheoremUp$ carrier is a finite $\BHist$ packet
$$
M=(A,B,S,F,D,R,Q,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A,B$ are the endpoint $\RealUp$ name rows for the displayed interval boundary and endpoint value reads.
\item $S$ is the secant-slope seal row, binding the endpoint difference and interval-width readback to a displayed $\RealUp$ slope seal rather than host division in a completed field.
\item $F$ is the $\ContinuousMapUp$ row naming the real-valued map on the displayed interval source.
\item $D$ is the $\DerivativeUp$ quotient row, exposing only the inspected metric quotient route used by the theorem-facing conclusion.
\item $R$ is the $\RolleTheoremUp$ handoff row, carrying the finite Rolle-facing packet applied to the usual auxiliary difference against the secant slope.
\item $Q$ is the $\RegSeqRatUp$ readback row for endpoint values, interval width, secant windows, and derivative-quotient inspections.
\item $T$ is the $\DyadicRatCoreUp$ tolerance ledger accounting for the dyadic windows used by $S,Q,D$, and the Rolle handoff row.
\item $H$ records componentwise $\hsame$ transport for endpoint names, secant-slope seals, continuous-map rows, derivative quotients, Rolle handoff rows, rational readbacks, dyadic ledgers, replay rows, provenance rows, and local naming rows.
\item $C$ records displayed $\Cont$ replay from endpoint reads to the secant seal, from map windows to the auxiliary Rolle packet, from rational readback to dyadic tolerance checks, and from the Rolle handoff to the theorem-facing derivative row.
\item $P$ is the $\Pkg$ provenance over $\MeanValueTheoremUp$, $\RealUp$, $\ContinuousMapUp$, $\DerivativeUp$, $\RolleTheoremUp$, $\IntermediateValueUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MeanValueTheoremUp}$ row.
\end{enumerate}
The classifier compares accepted theorem packets only through these rows. It has no coordinate for host differentiability, quotient real equality, a selected interior point, trichotomy, ambient $\RealUp$ completeness, open-cover compactness, or a standard bridge.
\end{definition}
\leanchecked{BEDC.Derived.MeanValueTheoremUp.MeanValueTheoremTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Mean value theorem NameCert obligations]
\label{thm:mean-value-theorem-namecert-obligations}
For every accepted $\MeanValueTheoremUp$ carrier
$$
M=(A,B,S,F,D,R,Q,T,H,C,P,N),
$$
the local $\NameCert_{\MeanValueTheoremUp}$ surface consists exactly of the finite obligation rows for endpoint real-name admission, secant-slope sealing, continuous-map admission, derivative quotient inspection, Rolle handoff, regular rational readback, dyadic tolerance budgeting, componentwise transport, displayed replay, provenance, and local naming. No obligation row may bypass the displayed $\RealUp$, $\ContinuousMapUp$, $\DerivativeUp$, $\RolleTheoremUp$, $\IntermediateValueUp$, $\RegSeqRatUp$, or $\DyadicRatCoreUp$ sources.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:mean-value-theorem-carrier}. The endpoint rows $A,B$ supply the displayed real names, and the row $S$ is the only secant-slope seal available to the packet.

The map and derivative surfaces are fixed next. The row $F$ supplies the continuous-map data, while $D$ supplies the displayed derivative quotient route.

The theorem-facing handoff then passes through $R$. That row is the only Rolle packet in the carrier, and the readback and tolerance rows $Q,T$ are the only finite arithmetic ledgers attached to it.

Finally, $H,C,P,N$ preserve, replay, source, and name the same rows. Since the carrier has no further coordinate, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Mean value theorem Rolle handoff]
\label{thm:mean-value-theorem-rolle-handoff}
Every theorem-facing derivative row exported by an accepted $\MeanValueTheoremUp$ carrier factors through the displayed Rolle handoff row $R$. The route reads the auxiliary difference against the secant-slope seal through $A,B,S,F,Q,T$, then exports the derivative quotient row $D$ only after the Rolle packet has supplied the stationary-zero boundary.
\end{theorem}
\begin{proof}
Start with an accepted carrier $M=(A,B,S,F,D,R,Q,T,H,C,P,N)$. The endpoint rows and secant-slope seal determine the auxiliary difference data before any theorem-facing derivative conclusion is available.

The continuous-map row $F$, regular readback $Q$, and dyadic ledger $T$ supply the finite observations consumed by the auxiliary packet. These rows are the only route from endpoint data to the displayed Rolle input.

Read the Rolle handoff row $R$. It supplies the stationary-zero boundary for the auxiliary difference, and the derivative quotient row $D$ is exported only through that boundary.

Transport and replay through $H$ and $C$ preserve this order, while $P$ and $N$ add only provenance and local naming. Thus the derivative row is routed through $R$ rather than through a host mean-value theorem or a selected ambient point.
\end{proof}

\begin{theorem}[Mean value theorem Real seal boundary]
\label{thm:mean-value-theorem-real-seal-boundary}
Every $\RealUp$ seal consumed or exported by an accepted $\MeanValueTheoremUp$ carrier is routed through one of the displayed endpoint, secant-slope, regular-readback, dyadic-ledger, Rolle-handoff, or derivative-quotient rows of \autoref{def:mean-value-theorem-carrier}. Consequently the carrier cannot export host differentiability, quotient real equality, selected interior points, trichotomy, ambient $\RealUp$ completeness, or bridge data.
\end{theorem}
\begin{proof}
Inspect the real-facing coordinates of the carrier. The endpoint rows $A,B$ expose the initial real names, $S$ exposes the secant-slope seal, and $Q,T$ expose the finite readback and tolerance budget.

The Rolle handoff row $R$ is the only theorem route from those seals to a stationary-zero boundary. The derivative quotient row $D$ is therefore read as a displayed Real seal attached to that route.

The structural rows do not add new real data. They transport, replay, source, and name the same carrier inventory.

There is no coordinate for host differentiability, quotient real equality, a selected interior point, trichotomy, ambient Real completeness, or a bridge object. Therefore every Real seal remains inside the displayed packet boundary.
\end{proof}

\closureat{MeanValueTheoremUp}{seedStr}

\begin{closurestatus}{\MeanValueTheoremUp}
  \constructivestory{A $\MeanValueTheoremUp$ packet is generated from endpoint real-name rows, a secant-slope seal, a continuous-map row, a derivative quotient row, a Rolle handoff row, regular rational readback, a dyadic tolerance ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:mean-value-theorem-carrier}, \autoref{thm:mean-value-theorem-namecert-obligations}, \autoref{thm:mean-value-theorem-rolle-handoff}, and \autoref{thm:mean-value-theorem-real-seal-boundary} seed the finite theorem-facing mean-value boundary over displayed endpoint, secant-slope, continuous-map, derivative, Rolle, regular-rational, and dyadic rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host differentiability, quotient real equality, selected interior points, trichotomy, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, NameCert obligation coverage, Rolle handoff routing, Real seal boundary, and TasteGate field faithfulness for BEDC.Derived.MeanValueTheoremUp.}
\end{closurestatus}
