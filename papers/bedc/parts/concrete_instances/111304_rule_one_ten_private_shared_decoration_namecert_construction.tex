\chapter{A Concrete Naming Certificate for $\RuleOneTenPrivateSharedDecorationUp$}
\label{ch:concrete-instances-rule-one-ten-private-shared-decoration-namecert}
\origin{ai}

The $\RuleOneTenPrivateSharedDecorationUp$ packet realizes the private/shared decoration model of \autoref{ch:visions-cellular-automaton-substrate-reading} as finite Rule 110 classifier-decoration NameCert data. It consumes the cellular automaton carrier of \autoref{ch:concrete-instances-cellularautomaton-namecert}, the row-classifier surface of \autoref{ch:concrete-instances-cellularrowclassifier-namecert}, the finite pattern catalog of \autoref{ch:concrete-instances-cellularpatterncatalog-namecert}, the Rule 110 classifier of \autoref{ch:concrete-instances-rule110-pattern-classifier-namecert}, and the finite partition carrier of \autoref{ch:concrete-instances-partition-namecert}. The packet names quotient support families, private blocks, shared-coordinate incidence rows, Rule 110 pattern-classifier rows, decoration-count provenance, transport, replay, provenance, and local naming; it does not import a host cellular semantics, an infinite orbit classifier, quotient support equality, a biological model, or a hidden enumeration oracle.

\begin{definition}[Rule 110 private/shared decoration carrier]
\label{def:rule-one-ten-private-shared-decoration-carrier}
A $\RuleOneTenPrivateSharedDecorationUp$ carrier is a finite $\BHist$ packet
$$
D=(Q,B,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the quotient support family row, naming the finite family of support blocks inspected by the decoration.
\item $B$ is the private-block row, recording for each accepted support a displayed nonempty private coordinate block.
\item $S$ is the shared-coordinate incidence row. For each shared coordinate it records the displayed set of incident private blocks and requires cardinality at least two.
\item $R$ is the $\RuleOneTenPatternClassifierUp$ row, attaching the decorated support family to finite Rule 110 row-window and pattern-tag reads.
\item $E$ is the decoration-count provenance row. It records the partition and row-set enumeration data used by the private/shared decoration theorem without exposing a host generating-function oracle.
\item $H$ records componentwise $\hsame$ transport for quotient support, private-block, shared-incidence, classifier, enumeration, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a decoration request through $Q,B,S,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RuleOneTenPrivateSharedDecorationUp$, $\CellularAutomatonUp$, $\CellularRowClassifierUp$, $\CellularPatternCatalogUp$, $\RuleOneTenPatternClassifierUp$, $\PartitionUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RuleOneTenPrivateSharedDecorationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for absent private blocks, singleton shared incidence, host quotient equality, unbounded Rule 110 evolution, biological interpretation, or an external enumeration procedure.
\end{definition}

\begin{theorem}[Rule 110 private/shared decoration NameCert obligations]
\label{thm:rule-one-ten-private-shared-decoration-namecert-obligations}
For every accepted $\RuleOneTenPrivateSharedDecorationUp$ carrier
$$
D=(Q,B,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\RuleOneTenPrivateSharedDecorationUp}$ surface consists exactly of quotient support family admission through $Q$, private-block nonemptiness through $B$, shared-coordinate incidence through $S$, Rule 110 pattern-classifier routing through $R$, decoration-count provenance through $E$, componentwise $\hsame$ stability through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:rule-one-ten-private-shared-decoration-carrier}. The decoration data are exactly the quotient support row $Q$, the private-block row $B$, the shared-incidence row $S$, the Rule 110 classifier row $R$, and the enumeration provenance row $E$.

Private-block nonemptiness is read before a support can be accepted. Shared-coordinate incidence is then read as a displayed row set of size at least two, so a shared coordinate cannot be collapsed into a private coordinate or erased from the decoration.

The Rule 110 classifier row $R$ attaches the decorated support family to finite row-window and pattern-tag reads from the cellular siblings. The count provenance row $E$ records the finite partition and row-set enumeration route and does not supply an external counting oracle.

Finally $H,C,P,N$ transport, replay, source, and name the same carrier. Since no excluded host object is present as a coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Private/shared decoration classifier handoff]
\label{thm:rule-one-ten-private-shared-decoration-classifier-handoff}
Every accepted $\RuleOneTenPrivateSharedDecorationUp$ consumer that reads a decorated Rule 110 support family factors through
$$
\CellularAutomatonUp,\ \CellularRowClassifierUp,\ \CellularPatternCatalogUp,\ \RuleOneTenPatternClassifierUp,\ \PartitionUp
\longmapsto
Q,B,S,R,E,H,C,P,N .
$$
The exported object is a finite classifier-decoration packet with private-block nonemptiness, shared incidence, and decoration-count provenance. It is not an infinite cellular-orbit classifier, a host quotient-support equality, a biological stochastic model, or an unrestricted enumeration theorem.
\end{theorem}
\begin{proof}
Open the accepted carrier using \autoref{def:rule-one-ten-private-shared-decoration-carrier}. A support-family read can inspect only $Q$, and each support must expose its nonempty private block through $B$ before shared coordinates are consumed.

The shared-coordinate row $S$ supplies the incidence sets whose size is at least two. The classifier row $R$ then routes the accepted decoration into the existing cellular automaton, row-classifier, pattern-catalog, and Rule 110 pattern-classifier surfaces.

The provenance row $E$ records the finite decoration-count route, while $H,C,P,N$ preserve the same data under transport, replay, package provenance, and local naming. Thus every accepted consumer receives exactly the displayed finite packet and none of the excluded host semantics.
\end{proof}

\falsifiablePrediction{Any accepted $\RuleOneTenPrivateSharedDecorationUp$ consumer that omits private-block nonemptiness or shared-coordinate incidence rows while reading a decorated Rule 110 support family refutes the carrier surface of \autoref{def:rule-one-ten-private-shared-decoration-carrier}.}

\independenceWitness{cellularautomaton, cellularrowclassifier, cellularpatterncatalog, rule110patternclassifier: no listed sibling is bijective to $\RuleOneTenPrivateSharedDecorationUp$ because this packet jointly binds quotient support families, nonempty private blocks, shared-coordinate incidence, Rule 110 classifier routing, decoration-count provenance, transport, replay, provenance, and local naming.}

The Lean TasteGate file for this packet must provide $\mathsf{Nontrivial}\ \RuleOneTenPrivateSharedDecorationUp$ and $\mathsf{FieldFaithful}\ \RuleOneTenPrivateSharedDecorationUp$ instances for the displayed rows $Q,B,S,R,E,H,C,P,N$ before the decoration carrier can serve as a verified classifier-decoration consumer.

\closureat{RuleOneTenPrivateSharedDecorationUp}{seedStr}

\begin{closurestatus}{\RuleOneTenPrivateSharedDecorationUp}
  \constructivestory{A $\RuleOneTenPrivateSharedDecorationUp$ packet is generated from quotient support family rows, nonempty private-block rows, shared-coordinate incidence rows, Rule 110 pattern-classifier rows, decoration-count provenance, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:rule-one-ten-private-shared-decoration-carrier}, \autoref{thm:rule-one-ten-private-shared-decoration-namecert-obligations}, and \autoref{thm:rule-one-ten-private-shared-decoration-classifier-handoff} seed the finite private/shared classifier-decoration surface for Rule 110 support-family consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close infinite Rule 110 orbit classification, host quotient-support equality, biological interpretation, unrestricted enumeration, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked quotient support admission, private-block nonemptiness, shared-coordinate incidence, Rule 110 classifier routing, decoration-count provenance, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.RuleOneTenPrivateSharedDecorationUp.}
\end{closurestatus}
