\chapter{A Concrete Naming Certificate for $\ClosedFiberUniformConvexityUp$}
\label{ch:concrete-instances-closed-fiber-uniform-convexity-namecert}
\origin{human}

Following Clarkson's uniformly convex space theory and the Bishop--Bridges practice of keeping metric and real-name evidence explicit, the $\ClosedFiberUniformConvexityUp$ packet records a closed fiber inside a uniformly convex carrier as finite NameCert data. It sits over the modulus route of \autoref{ch:concrete-instances-uniformly-convex-space-namecert}, the complete-metric boundary of \autoref{ch:concrete-instances-completemetric-namecert}, and the terminal real-name discipline of \autoref{ch:concrete-instances-real-namecert}. The packet gives projection, fixed-point, and completion consumers a named closed-fiber convexity surface without importing quotient subspaces, selected nearest points, weak compactness, or choice.

\begin{definition}[Closed fiber uniform convexity carrier]
\label{def:closed-fiber-uniform-convexity-carrier}
A $\ClosedFiberUniformConvexityUp$ carrier is a finite $\BHist$ packet
$$
F=(U,K,\Phi,\Gamma,\Delta,\Pi,E,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ is the ambient $\UniformlyConvexSpaceUp$ row, exposing the norm, metric, midpoint, and positive modulus routes only through its local certificate.
\item $K$ is the $\CompleteMetricUp$ boundary row used by Cauchy and fixed-point consumers of the fiber.
\item $\Phi$ is the fiber membership classifier, read as a displayed predicate on the ambient carrier rather than as a quotient subspace.
\item $\Gamma$ is the closedness row: every finite Cauchy read in the fiber that lands through $K$ must replay $\Phi$ at the displayed limit row.
\item $\Delta$ is the uniform-convexity restriction row, reusing the ambient Clarkson modulus from $U$ for pairs whose membership reads pass through $\Phi$.
\item $\Pi$ is the projection-route row, recording how a consumer may ask for a fiber-facing projection bound through the displayed metric, real, midpoint, and closedness rows.
\item $E$ is the $\RealUp$ seal for norm gaps, radius comparisons, modulus margins, and projection-distance readback.
\item $H$ records componentwise $\hsame$ transport for $U,K,\Phi,\Gamma,\Delta,\Pi,E$ and the local structural rows.
\item $T$ records displayed $\Cont$ replay from a closed-fiber request through the ambient uniform-convexity, complete-metric, membership, closedness, projection, and real-seal rows.
\item $P$ is the $\Pkg$ provenance over $\ClosedFiberUniformConvexityUp$, $\UniformlyConvexSpaceUp$, $\CompleteMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ClosedFiberUniformConvexityUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these eleven rows. It has no coordinate for quotient fibers, selected nearest points, weak compactness, Hahn--Banach separation, or an ambient choice principle.
\end{definition}

\begin{theorem}[Closed fiber uniform convexity NameCert obligations]
\label{thm:closed-fiber-uniform-convexity-namecert-obligations}
For every accepted $\ClosedFiberUniformConvexityUp$ packet $F=(U,K,\Phi,\Gamma,\Delta,\Pi,E,H,T,P,N)$, the local $\NameCert_{\ClosedFiberUniformConvexityUp}$ surface consists exactly of ambient uniformly convex admission, complete-metric boundary visibility, fiber-membership readback, closedness replay for finite Cauchy reads, restricted modulus admission, projection-route exposure, real-seal comparison, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No obligation is stored outside $U,K,\Phi,\Gamma,\Delta,\Pi,E,H,T,P,N$.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:closed-fiber-uniform-convexity-carrier}. The rows $U,K,\Phi,\Gamma,\Delta,\Pi,E,H,T,P,N$ are the whole accepted packet.

Read the ambient and closedness rows first. The row $U$ supplies only the certified uniform-convexity route, while $K,\Phi,\Gamma$ bind completion-facing Cauchy reads to the displayed fiber predicate.

Read the convexity and projection rows next. The row $\Delta$ restricts the ambient modulus to pairs whose membership is displayed by $\Phi$, and $\Pi$ exposes only the metric, midpoint, closedness, and real-comparison route already present in $U,K,\Gamma,E$.

Finally $H,T,P,N$ transport, replay, source, and name the same finite packet. Since no coordinate stores quotient fibers, selected nearest points, weak compactness, Hahn--Banach separation, or choice, those objects cannot be obligations of the local certificate.
\end{proof}

\begin{theorem}[Closed fiber uniform convexity projection route]
\label{thm:closed-fiber-uniform-convexity-projection-route}
Every accepted $\ClosedFiberUniformConvexityUp$ consumer that asks for a fiber-facing projection or distance estimate factors through
$$
\UniformlyConvexSpaceUp,\CompleteMetricUp,\RealUp \longmapsto \Phi,\Gamma,\Delta,\Pi,H,T,P,N .
$$
The route may read the restricted Clarkson modulus only after the ambient uniformly convex row, the fiber-membership row, the complete-metric closedness row, and the real-valued comparison seal have been displayed. It exports a finite closed-fiber NameCert surface, not a selected nearest point, quotient fiber equality, weak compactness, Hahn--Banach separation, or choice.
\end{theorem}
\begin{proof}
Begin with a projection or distance request. The ambient row $U$ supplies the only norm, metric, midpoint, and modulus data, and the real seal $E$ supplies the only visible comparison values.

Pass through the fiber rows before reading the modulus. The predicate row $\Phi$ displays membership, and the closedness row $\Gamma$ ensures that completion-facing Cauchy reads landing through $K$ replay the same predicate at the limit row.

Only then may $\Delta$ and $\Pi$ answer the request. The restricted modulus is tied to pairs already admitted by $\Phi$, and the projection route is tied to the displayed metric, midpoint, closedness, and real-comparison data.

Finally $H,T,P,N$ keep the route invariant under transport, continuation replay, provenance, and local naming. The excluded selected nearest point, quotient-fiber equality, weak compactness, Hahn--Banach separation, and choice objects are absent from the carrier, so the finite route is the whole exported surface.
\end{proof}

\falsifiablePrediction{An accepted $\ClosedFiberUniformConvexityUp$ read is refuted if a consumer obtains a restricted uniform-convexity margin or fiber-facing projection bound without first displaying the ambient $\UniformlyConvexSpaceUp$, $\CompleteMetricUp$, fiber-membership, closedness, $\RealUp$, transport, replay, provenance, and local $\NameCert$ rows.}

\independenceWitness{uniformly-convex-space, completemetric, real: no listed sibling is bijective to $\ClosedFiberUniformConvexityUp$ because the packet jointly carries an ambient uniformly convex route, a complete-metric boundary, fiber membership, closedness replay, restricted modulus access, projection-route exposure, real comparisons, transport, replay, provenance, and local naming.}

\closureat{ClosedFiberUniformConvexityUp}{seedStr}

\begin{closurestatus}{\ClosedFiberUniformConvexityUp}
  \constructivestory{A $\ClosedFiberUniformConvexityUp$ packet is generated from an ambient UniformlyConvexSpace row, a CompleteMetric boundary row, fiber membership, closedness replay, restricted modulus access, projection-route exposure, Real comparison rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:closed-fiber-uniform-convexity-carrier}, \autoref{thm:closed-fiber-uniform-convexity-namecert-obligations}, and \autoref{thm:closed-fiber-uniform-convexity-projection-route} seed the finite closed-fiber route over uniformly convex, complete-metric, and real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient fiber equality, selected nearest points, weak compactness, Hahn--Banach separation, choice, Lean verification, or a standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, closedness replay, restricted modulus exactness, projection-route nonescape, and local naming for BEDC.Derived.ClosedFiberUniformConvexityUp.}
\end{closurestatus}
