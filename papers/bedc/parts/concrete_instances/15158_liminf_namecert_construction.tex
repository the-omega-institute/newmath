\chapter{A Concrete Naming Certificate for $\LiminfUp$}
\label{ch:concrete-instances-liminf-namecert}
\origin{human}

Following Hardy and Bishop--Bridges, $\LiminfUp$ records the finite tail-lower-cut companion to the tail-upper-cut surface of \autoref{ch:concrete-instances-limsup-namecert}. The packet consumes the bounded sequence rows of \autoref{ch:concrete-instances-boundedrealsequence-namecert}, the lower-cut observation discipline of \autoref{ch:concrete-instances-bounded-real-lower-cut-namecert}, the upper-cut comparison discipline of \autoref{ch:concrete-instances-bounded-real-upper-cut-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. It gives Real-and-Completion consumers a finite lower-tail boundary for bounded real sequences without importing Real completeness, trichotomy, a host infimum, quotient stream equality, selected subsequences, or open-cover compactness.

The lower-tail rows are consumed together with the bounded upper-envelope data of \autoref{ch:concrete-instances-bounded-real-limsup-namecert} in Real sequence oscillation packets. In that route, tail-cut lower envelope rows from $\LiminfUp$ pair with bounded upper envelope rows from $\BoundedRealLimsupUp$ before any gap-width or zero-gap convergence certificate is exported.

\begin{definition}[Liminf carrier]
\label{def:liminf-carrier}
A $\LiminfUp$ carrier is a finite $\BHist$ packet
$$
I=(S,L,D,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the bounded $\BoundedRealSequenceUp$ row whose finite windows, bound ledger, and displayed $\RealUp$ entries are being inspected.
\item $L$ is the tail lower-cut schedule, recording for each displayed tail index the bounded lower-cut observations read through $\BoundedRealLowerCutUp$.
\item $D$ is the dyadic tolerance ledger, recording the finite rational tolerances used to compare tail lower cuts with the upper observation surface.
\item $T$ is the terminal $\RealUp$ seal row named as the liminf candidate exposed by the packet.
\item $H$ records componentwise $\hsame$ transports for $S,L,D,T$ and the structural rows.
\item $C$ records the displayed $\Cont$ routes by which consumers replay the bounded sequence row, tail lower-cut schedule, dyadic tolerance ledger, terminal real seal, transports, provenance, and local naming row.
\item $P$ is the $\Pkg$ provenance over $\LiminfUp$, $\BoundedRealSequenceUp$, $\BoundedRealLowerCutUp$, $\BoundedRealUpperCutUp$, $\RealUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LiminfUp}$ row.
\end{enumerate}
The carrier stores a finite lower-tail cut surface for a bounded real sequence. It has no coordinate for Real completeness, trichotomy, a host infimum, quotient stream equality, selected subsequences, or open-cover compactness.
\end{definition}
\leandef{BEDC.Derived.LiminfUp.LiminfCarrier}

\begin{lemma}[Liminf carrier route rows]
\label{lem:liminf-carrier-route-rows}
Every accepted $\LiminfUp$ carrier of \autoref{def:liminf-carrier} projects the three route rows
$$
\Cont(S,L,D),\qquad \Cont(D,T,C),\qquad \PkgSig(B,P,\mathsf{pkg}),
$$
namely bounded-sequence to lower-cut to dyadic replay, dyadic to terminal-real to continuation replay, and bundle provenance through the displayed package row.
\end{lemma}
\leanchecked{BEDC.Derived.LiminfUp.LiminfCarrier\_route\_rows}
\begin{proof}
Project the displayed carrier rows of \autoref{def:liminf-carrier}. The route coordinate $C$ contains the bounded sequence, lower-cut, dyadic, terminal, and replay continuations, while the provenance row $P$ supplies the package signature.

Thus the accepted carrier exposes exactly the two $\Cont$ rows and the $\PkgSig$ provenance row named above, without adding a lower-envelope theorem or a host infimum.
\end{proof}

\begin{definition}[Liminf classifier]
\label{def:liminf-classifier}
Two $\LiminfUp$ carriers
$$
I=(S,L,D,T,H,C,P,N)
\qquad\text{and}\qquad
I'=(S',L',D',T',H',C',P',N')
$$
are classified as the same displayed liminf packet exactly when the bounded sequence rows match componentwise, the tail lower-cut schedules expose the same finite tail observations, the dyadic tolerance ledgers carry the same rational comparison budgets, the terminal $\RealUp$ seals are transported by the displayed $\hsame$ data, the continuation replays preserve the same consumer routes, and the provenance and local NameCert rows name the same $\LiminfUp$ surface. The classifier is therefore rowwise over $S,L,D,T,H,C,P,N$ and does not identify carriers by host sequence equality, quotient streams, selected cofinal subsequences, or a completed infimum construction.
\end{definition}
\leandef{BEDC.Derived.LiminfUp.LiminfClassifier}

\begin{theorem}[Liminf NameCert obligations]
\label{thm:liminf-namecert-obligations}
For an accepted $\LiminfUp$ carrier $I=(S,L,D,T,H,C,P,N)$, the local $\NameCert_{\LiminfUp}$ surface consists of six finite obligation rows:
\begin{enumerate}
\item carrier formation for the bounded real sequence row, tail lower-cut schedule, dyadic tolerance ledger, terminal $\RealUp$ seal, transports, continuations, provenance, and local naming data;
\item classifier stability for the componentwise comparison of \autoref{def:liminf-classifier};
\item lower-tail exactness, stating that every lower observation in $L$ is read from a finite tail of $S$ through the bounded lower-cut surface;
\item upper-cut compatibility, stating that every tolerance row in $D$ compares the same tail data against the bounded upper-cut observation surface;
\item terminal seal routing, stating that the candidate row $T$ is consumed only as a $\RealUp$ seal after the displayed tail-cut checks have been replayed;
\item ledger non-escape, excluding Real completeness, trichotomy, host infimum selection, quotient stream equality, selected subsequences, and open-cover compactness.
\end{enumerate}
\end{theorem}
\begin{proof}
Carrier formation is projection from \autoref{def:liminf-carrier}. The packet displays exactly the bounded sequence row, tail lower-cut schedule, dyadic tolerance ledger, terminal real seal, componentwise transports, continuation routes, provenance, and local name.

Classifier stability is the componentwise comparison of \autoref{def:liminf-classifier}. Transport along $H$ may replace the visible rows only by rows with the same scheduled tail observations, the same dyadic budgets, and the same terminal seal route, while $C$ preserves the consumer replay order.

Lower-tail exactness is supplied by $L$ together with the bounded sequence row $S$. Each accepted lower observation names the finite tail index, the inspected sequence windows, and the bounded lower-cut certificate through which the read is made.

Upper-cut compatibility is supplied by $D$ and the sibling bounded upper-cut discipline. The tolerance ledger records only finite dyadic comparisons between the same tail data and the upper observation surface, so it does not become a host supremum or a completeness witness.

Terminal seal routing is the role of $T$. A consumer may read the candidate liminf only after the sequence tail, lower-cut, upper-cut, and tolerance rows have been replayed; the terminal coordinate remains a $\RealUp$ seal rather than a completed infimum object.

The non-escape row follows from the carrier inventory. No displayed field carries Real completeness, trichotomy, a host infimum, quotient stream equality, selected subsequences, or open-cover compactness.
\end{proof}
\leanchecked{BEDC.Derived.LiminfUp.LiminfNameCertObligations}

\begin{theorem}[Liminf tail-cut route]
\label{thm:liminf-tail-cut-route}
Every Real-and-Completion consumer of an accepted $\LiminfUp$ packet factors through the finite route
$$
S\longmapsto L\longmapsto D\longmapsto T\longmapsto H\longmapsto C\longmapsto P\longmapsto N
$$
before using the displayed liminf surface.
\end{theorem}
\begin{proof}
Begin with the bounded sequence row $S$. It supplies the only displayed source of sequence entries, finite windows, and boundedness evidence available to the packet.

The tail lower-cut schedule $L$ then names which tails and lower observations are inspected. The dyadic ledger $D$ records the tolerance comparisons that align those tail-lower observations with the bounded upper-cut surface.

Only after those rows are replayed can a consumer inspect the terminal real seal $T$. The structural rows $H,C,P,N$ then transport, replay, source, and name the same finite route, so the consumer cannot bypass the tail-cut surface by reading a host infimum or a selected subsequence.
\end{proof}
\leanchecked{BEDC.Derived.LiminfUp.LiminfTailCutRoute}

\begin{theorem}[Liminf tail lower-envelope compatibility]
\label{thm:liminf-tail-lower-envelope-compatibility}
Every accepted $\LiminfUp$ packet
$$
I=(S,L,D,T,H,C,P,N)
$$
whose bounded sequence row $S$ is paired with the bounded upper-envelope surface of $\BoundedRealLimsupUp$ factors its lower-tail schedule through $\BoundedRealLowerCutUp$ and its dyadic tolerance ledger through $\BoundedRealUpperCutUp$ before any gap-width or zero-gap convergence consumer reads the terminal $\RealUp$ seal $T$.
\end{theorem}
\begin{proof}
Project the accepted carrier into its displayed rows $S,L,D,T,H,C,P,N$. The row $S$ is the bounded sequence source described in \autoref{def:liminf-carrier}, and the paired upper-envelope data is read through the sibling $\BoundedRealLimsupUp$ chapter rather than through an ambient supremum.

Apply the tail-cut route of \autoref{thm:liminf-tail-cut-route}. That route forces every consumer to replay $S$ before $L$, and $L$ is exactly the finite lower-tail schedule whose observations are certified by the bounded lower-cut chapter. Thus the lower-envelope half of the paired liminf--limsup surface reaches the consumer only through $\BoundedRealLowerCutUp$.

The dyadic tolerance row $D$ is then replayed before the terminal seal $T$. By the upper-cut compatibility row in \autoref{thm:liminf-namecert-obligations}, those tolerances compare the same tail data against the bounded upper-cut surface, so the upper comparison half factors through $\BoundedRealUpperCutUp$.

The structural rows $H,C,P,N$ only transport, replay, source, and name this ordered route. Hence a later gap-width or zero-gap convergence certificate can read $T$ only after the bounded sequence row, lower-cut schedule, upper-cut tolerance ledger, and limsup sibling surface have all been exposed as finite certificate data.
\end{proof}
\leanchecked{BEDC.Derived.LiminfUp.LiminfTailLowerEnvelopeCompatibility}

\begin{theorem}[Liminf RegSeqRat lower-cut public export]
\label{thm:liminf-regseqrat-lower-cut-public-export}
Every $\RegSeqRatUp$ or Real-and-Completion consumer of an accepted $\LiminfUp$ packet receives the lower-envelope claim only after the displayed route exposes the bounded sequence row $S$, the $\BoundedRealLowerCutUp$ tail schedule $L$, the $\BoundedRealUpperCutUp$ tolerance comparison through the $\DyadicRatCoreUp$ ledger $D$, and the terminal $\RealUp$ seal $T$. The public export is the finite packet
$$
S,L,D,T,H,C,P,N
$$
read through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows; it does not export a host infimum, selected tail subsequence, quotient stream equality, trichotomy decision, open-cover compactness, or ambient Real completeness.
\end{theorem}
\leanchecked{BEDC.Derived.LiminfUp.LiminfRegSeqRatLowerCutPublicExport}
\begin{proof}
Project the accepted carrier of \autoref{def:liminf-carrier}. Its public read surface is exactly the row family $S,L,D,T,H,C,P,N$, where $S$ is the bounded sequence source, $L$ is the finite lower-tail schedule, $D$ is the dyadic tolerance ledger, and $T$ is the terminal Real seal.

Apply the route theorem \autoref{thm:liminf-tail-cut-route}. It orders every consumer read through $S,L,D,T$ before the structural rows $H,C,P,N$ can transport, replay, source, and name the packet.

Use the lower-envelope compatibility theorem, \autoref{thm:liminf-tail-lower-envelope-compatibility}. It identifies the lower side with $\BoundedRealLowerCutUp$ and the comparison side with $\BoundedRealUpperCutUp$ through the same $\DyadicRatCoreUp$ ledger before any Real-and-Completion consumer may inspect $T$.

The export is therefore public only as a finite RegSeqRat-facing handoff. Since no carrier row stores a host infimum, selected subsequence, quotient stream equality, trichotomy decision, open-cover compactness, or ambient completeness principle, the lower-envelope claim cannot be strengthened beyond those displayed rows.
\end{proof}

\begin{theorem}[Liminf tail-envelope monotone refinement]
\label{thm:liminf-tail-envelope-monotone-refinement}
Let an accepted $\LiminfUp$ packet $I=(S,L,D,T,H,C,P,N)$ be paired with bounded upper-envelope data from $\BoundedRealLimsupUp$. If the displayed tail schedule $L$ is refined by a later monotone tail schedule while preserving the same bounded sequence row $S$, lower-cut route through $\BoundedRealLowerCutUp$, upper-cut tolerance ledger $D$, and terminal $\RealUp$ seal $T$, then every gap-width or zero-gap convergence consumer reads the refined packet through the same bounded-sequence, lower-envelope, upper-tolerance, and terminal-seal route.
\end{theorem}
\begin{proof}
Project the carrier and classifier. The bounded sequence row $S$ is unchanged, and the classifier of \autoref{def:liminf-classifier} permits only rowwise comparison of the displayed tail schedule, dyadic ledger, terminal seal, replay, provenance, and local name.

Refine the tail schedule monotonically. The new lower observations remain lower-cut reads through $\BoundedRealLowerCutUp$, and the tolerance row $D$ still compares the same bounded-sequence data against the upper-cut surface before the terminal seal is visible.

Apply the lower-envelope compatibility theorem above. It already forces every liminf--limsup consumer to expose $S,L,D,T$ in that order, with the bounded upper-envelope surface present before any gap-width or zero-gap convergence certificate can inspect $T$.

The structural rows $H,C,P,N$ transport, replay, source, and name the refined route. Since the refinement changes only the displayed tail schedule and preserves the bounded sequence row, lower-cut route, upper-cut tolerance ledger, and terminal Real seal, no zero-gap consumer can bypass the finite envelope or read a host infimum, selected subsequence, quotient stream equality, or ambient completeness principle.
\end{proof}
\leanchecked{BEDC.Derived.LiminfUp.LiminfTailEnvelopeMonotoneRefinement}

\begin{theorem}[Liminf--limsup zero-gap convergence]
\label{thm:liminf-limsup-zero-gap-convergence}
Let an accepted $\LiminfUp$ packet
$$
I=(S,L,D,T,H,C,P,N)
$$
be paired with an accepted $\BoundedRealLimsupUp$ packet whose bounded sequence row is the same displayed $S$, whose tail schedule is the same displayed $L$, and whose gap-width ledger is displayed as zero along the same dyadic tolerance row $D$. Then every convergence consumer that treats the lower and upper envelopes as coincident must first read the terminal $\RealUp$ seal through the shared route
$$
S,L,D,T,H,C,P,N
$$
before exporting the convergence result.
\end{theorem}
\leanchecked{BEDC.Derived.LiminfUp.LiminfLimsupZeroGapConvergence}
\begin{proof}
Start with the lower envelope. Theorem~\ref{thm:liminf-tail-lower-envelope-compatibility} forces the $\LiminfUp$ half of the pair to expose the bounded sequence row $S$, the lower-tail schedule $L$, and the dyadic tolerance row $D$ before the terminal seal $T$ can be consumed.

Next use the monotone refinement theorem, \autoref{thm:liminf-tail-envelope-monotone-refinement}. It keeps later tail reads on the same bounded-sequence, lower-envelope, upper-tolerance, and terminal-seal route whenever the displayed schedule is refined without changing $S,D,T$.

Now attach the sibling $\BoundedRealLimsupUp$ packet. Its upper-envelope surface is paired with the same $S$, the same tail schedule $L$, and the same zero gap-width ledger over $D$, so a convergence consumer has no separate upper route from which to read a different terminal value.

Therefore the zero-gap read can only identify the two envelope surfaces after the common route $S,L,D,T,H,C,P,N$ has been displayed. The packet exports no Real completeness principle, trichotomy, selected subsequence, quotient stream equality, host infimum, or host supremum as a substitute for that finite route.
\end{proof}

\begin{theorem}[Liminf tail lower-upper gap width]
\label{thm:liminf-tail-lower-upper-gap-width}
Let an accepted $\LiminfUp$ packet
$$
I=(S,L,D,T,H,C,P,N)
$$
be paired with a $\BoundedRealLimsupUp$ upper-envelope packet over the same bounded sequence row $S$. Any zero-gap or convergence-facing read from the pair is exposed only after the shared bounded sequence row $S$, lower-cut schedule $L$, upper-cut comparison surface, dyadic tolerance ledger $D$, terminal $\RealUp$ seal $T$, and structural rows $H,C,P,N$ have been displayed. The resulting gap-width handoff is a finite dyadic envelope route, not a Real completeness principle, host infimum or supremum, selected subsequence, or quotient stream equality.
\end{theorem}
\leanchecked{BEDC.Derived.LiminfUp.LiminfTailLowerUpperGapWidth}
\begin{proof}
Start with lower-tail compatibility, \autoref{thm:liminf-tail-lower-envelope-compatibility}. It forces the $\LiminfUp$ half of the pair to expose $S,L,D,T$ before any lower-envelope consumer can inspect the terminal seal.

Next apply monotone refinement, \autoref{thm:liminf-tail-envelope-monotone-refinement}. It keeps later tail reads on the same bounded-sequence, lower-cut, upper-comparison, dyadic-tolerance, and terminal-seal route whenever the schedule is refined without changing $S,D,T$.

Finally use zero-gap convergence, \autoref{thm:liminf-limsup-zero-gap-convergence}. Once the $\BoundedRealLimsupUp$ packet is paired with the same $S$ and dyadic tolerance row $D$, a convergence-facing read can only pass through the shared finite gap-width route.

The structural rows $H,C,P,N$ transport, replay, source, and name that route. Since the proof uses only the displayed lower envelope, upper comparison surface, dyadic ledger, and Real seal, it introduces no Real completeness, host infimum, host supremum, selected subsequence, or quotient stream equality.
\end{proof}

\begin{theorem}[Liminf Real seal non-escape]
\label{thm:liminf-real-seal-nonescape}
An accepted $\LiminfUp$ packet exposes its candidate value only as the terminal $\RealUp$ row $T$ reached through the displayed tail-cut route. In particular, it does not export Real completeness, trichotomy, a host infimum, quotient stream equality, selected subsequences, or open-cover compactness.
\end{theorem}
\leanchecked{BEDC.Derived.LiminfUp.LiminfRealSealNonescape}
\begin{proof}
The only value-facing coordinate in the carrier is $T$. All sequence-facing information is confined to $S,L,D$, and all structural access is confined to $H,C,P,N$.

A consumer that asks for the candidate value must therefore follow the continuation replay from the bounded sequence row through the tail lower-cut and tolerance ledgers before reaching $T$. The replay exposes a sealed real name, not a construction by infimum, compactness, or subsequence selection.

Since no carrier field stores the excluded principles, componentwise transport and continuation replay cannot introduce them. The $\RealUp$ seal remains terminal for the packet, and the liminf surface remains a finite tail-cut certificate.
\end{proof}

\begin{theorem}[Liminf tail-infimum obligation route]
\label{thm:liminf-obligation-tail-infimum-route}
Every accepted $\LiminfUp$ obligation read must expose the rows
$$
S,L,D,T,H,C,P,N
$$
before any tail-infimum consumer can read the packet. The source row $S$, tail lower-cut schedule $L$, dyadic tolerance ledger $D$, terminal $\RealUp$ seal $T$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert$ row $N$ are therefore the whole tail-infimum surface visible to the consumer.
\end{theorem}
\leanchecked{BEDC.Derived.LiminfUp.LiminfObligationTailInfimumRoute}
\begin{proof}
Project the accepted carrier of \autoref{def:liminf-carrier}. Its tuple already orders the bounded sequence source, lower-tail schedule, dyadic ledger, terminal seal, transport, replay, provenance, and local naming rows.

Apply the route theorem \autoref{thm:liminf-tail-cut-route}. It forces every Real-and-Completion consumer to replay $S,L,D,T,H,C,P,N$ before using the displayed liminf surface.

A tail-infimum consumer is such a value-facing consumer, so it receives no additional row. The read is exhausted by the displayed carrier route and cannot bypass it through a host infimum or selected subsequence.
\end{proof}

\begin{theorem}[Liminf bounded-cut compatibility obligation]
\label{thm:liminf-obligation-bounded-cut-compatibility}
Every accepted $\LiminfUp$ obligation read that compares lower-cut and upper-cut data must use the same bounded sequence row $S$ and the same dyadic tolerance ledger $D$. The lower-cut observations in $L$ and the upper-cut comparison surface are compatible only as finite reads from that shared bounded sequence and shared tolerance ledger before the terminal $\RealUp$ seal $T$ is consumed.
\end{theorem}
\leanchecked{BEDC.Derived.LiminfUp.LiminfBoundedCutCompatibilityObligation}
\begin{proof}
Read the carrier rows $S,L,D,T,H,C,P,N$. The row $S$ is the only bounded sequence source, and $D$ is the only displayed tolerance ledger available to comparisons.

Use \autoref{thm:liminf-namecert-obligations}. Its lower-tail exactness row routes lower observations through the finite tail data of $S$, while its upper-cut compatibility row uses $D$ to compare that same tail data against the bounded upper-cut surface.

The structural rows $H,C,P,N$ preserve this common source and tolerance ledger under transport, replay, provenance, and local naming. Thus a consumer cannot pair the lower cut with a different bounded sequence, a different dyadic budget, a host supremum, or ambient completeness data.
\end{proof}

\begin{theorem}[Liminf Real-seal exhaustion obligation]
\label{thm:liminf-obligation-real-seal-exhaustion}
Every value-facing read of an accepted $\LiminfUp$ packet ends at the displayed $\RealUp$ seal $T$. It cannot expose a host infimum, selected subsequence, trichotomy, quotient stream equality, open-cover compactness, or ambient $\RealUp$ completeness beyond the finite route $S,L,D,T,H,C,P,N$.
\end{theorem}
\begin{proof}
Start from \autoref{thm:liminf-real-seal-nonescape}. The theorem identifies $T$ as the only value-facing coordinate and places it after the bounded sequence, lower-cut, and tolerance rows.

The obligation route of \autoref{thm:liminf-obligation-tail-infimum-route} gives the complete list of rows a value-facing consumer may read. The bounded-cut compatibility obligation keeps lower-cut and upper-cut reads on the same $S,D$ route before $T$ is reached.

No carrier row remains after $T$ except the structural transport, replay, provenance, and local naming rows already listed. Hence the displayed Real seal exhausts the value-facing surface and the excluded host principles are not exported by the packet.
\end{proof}
\leanchecked{BEDC.Derived.LiminfUp.LiminfRealSealExhaustionObligation}

\begin{theorem}[Liminf lower-cut readback scope]
\label{thm:liminf-lower-cut-readback-scope}
Every accepted $\LiminfUp$ lower-tail read must expose the bounded sequence source $S$, the finite lower-cut schedule $L$, the dyadic tolerance ledger $D$, and the terminal $\RealUp$ seal $T$ before any lower-cut readback is consumed. The source row is read through $\BoundedRealSequenceUp$, the lower observations through $\BoundedRealLowerCutUp$, the comparison rows through $\BoundedRealUpperCutUp$ and $\DyadicRatCoreUp$, and the terminal value only as the $\RealUp$ seal named in \autoref{def:liminf-carrier}, \autoref{thm:liminf-namecert-obligations}, \autoref{thm:liminf-tail-cut-route}, and \autoref{thm:liminf-real-seal-nonescape}.
\end{theorem}
\leanchecked{BEDC.Derived.LiminfUp.LiminfLowerCutReadbackScope}
\begin{proof}
Project the carrier $I=(S,L,D,T,H,C,P,N)$ of \autoref{def:liminf-carrier}. The source row $S$ is the displayed bounded real sequence, and the schedule $L$ is the finite lower-cut ledger attached to tails of that same source.

Use the NameCert obligations of \autoref{thm:liminf-namecert-obligations}. Their lower-tail exactness row routes each accepted lower observation through $\BoundedRealLowerCutUp$, while the upper-cut compatibility row routes comparisons through $\BoundedRealUpperCutUp$ using the dyadic tolerance ledger $D$.

Apply the tail-cut route of \autoref{thm:liminf-tail-cut-route}. It orders the read as $S,L,D,T,H,C,P,N$, so the terminal value cannot be inspected before the bounded source, lower schedule, and dyadic comparison rows have been displayed.

Finally use the Real-seal non-escape theorem, \autoref{thm:liminf-real-seal-nonescape}. The endpoint is only the row $T$, not a host infimum, trichotomy decision, selected subsequence, quotient stream equality, or ambient completeness principle.
\end{proof}

\begin{theorem}[Liminf scoped tail envelope]
\label{thm:liminf-scoped-tail-envelope}
Every accepted $\LiminfUp$ consumer must route lower-tail data through the shared $\BoundedRealSequenceUp$ row, the $\BoundedRealLowerCutUp$ schedule, the $\DyadicRatCoreUp$ tolerance ledger, the $\BoundedRealUpperCutUp$ comparison surface, and the terminal $\RealUp$ seal before any tail-envelope read is public. The scoped route is the finite envelope
$$
S,L,D,T,H,C,P,N
$$
read through $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows; it exports no host infimum, trichotomy decision, selected subsequence, quotient stream equality, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.LiminfUp.LiminfScopedTailEnvelope}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:liminf-namecert-obligations}. They identify the accepted packet as the finite row family $S,L,D,T,H,C,P,N$ and place lower-tail exactness, upper-cut compatibility, terminal sealing, transport, replay, provenance, and local naming in that family.

Use the tail-cut route of \autoref{thm:liminf-tail-cut-route}. It orders every value-facing read through the bounded sequence row, lower-tail schedule, dyadic tolerance ledger, and terminal Real seal before the structural rows can replay the packet to a consumer.

Apply the bounded-cut compatibility obligation, \autoref{thm:liminf-obligation-bounded-cut-compatibility}. It forces lower-cut and upper-cut comparisons to use the same source row $S$ and the same tolerance ledger $D$, so the lower envelope cannot be paired with an unrelated upper surface.

Finally read the lower-cut scope theorem, \autoref{thm:liminf-lower-cut-readback-scope}. It identifies the public lower-tail readback as a route through $\BoundedRealSequenceUp$, $\BoundedRealLowerCutUp$, $\BoundedRealUpperCutUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and the terminal $\RealUp$ seal, with $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ preserving the same displayed rows.
\end{proof}

\begin{theorem}[Liminf lower-cut readback]
\label{thm:liminf-lower-cut-readback}
Every lower-tail observation exposed by an accepted $\LiminfUp$ carrier
$$
I=(S,L,D,T,H,C,P,N)
$$
is read through the $\BoundedRealLowerCutUp$ row $L$, compared through the shared $\DyadicRatCoreUp$ ledger $D$, and sealed only at the terminal $\RealUp$ row $T$. The readback may cite the bounded source $S$ and the upper comparison surface, but it does not expose a host infimum, a selected subsequence, trichotomy, quotient stream equality, or ambient Real completeness.
\end{theorem}
\leanchecked{BEDC.Derived.LiminfUp.LiminfLowerCutReadback}
\begin{proof}
Project the carrier of \autoref{def:liminf-carrier}. The source $S$ is the bounded sequence row, $L$ is the finite lower-tail schedule, $D$ is the dyadic tolerance ledger, and $T$ is the only terminal Real seal.

Use \autoref{thm:liminf-lower-cut-readback-scope}. It already forces public lower-tail readback to pass through $\BoundedRealSequenceUp$, $\BoundedRealLowerCutUp$, $\BoundedRealUpperCutUp$, $\DyadicRatCoreUp$, and the terminal $\RealUp$ seal in that same displayed packet.

The present readback only isolates the lower-cut side of that route. Every lower observation is therefore a read from $L$ compared against $D$, and the value-facing endpoint remains $T$ rather than a host infimum, selected subsequence, trichotomy decision, quotient stream equality, or ambient completeness principle.
\end{proof}

\begin{theorem}[Liminf regular-sequence route]
\label{thm:liminf-regular-sequence-route}
Every $\RegSeqRatUp$-facing consumer of an accepted $\LiminfUp$ packet must pass through
$$
\BoundedRealSequenceUp,\quad
\BoundedRealLowerCutUp,\quad
\BoundedRealUpperCutUp,\quad
\DyadicRatCoreUp,\quad
\RegSeqRatUp,\quad
\RealUp
$$
before reading the liminf surface $S,L,D,T,H,C,P,N$. The route exposes finite regular-sequence and cut data only; it does not claim Real completeness, an actual infimum object, a quotient-real equality, a selected tail subsequence, or an open-cover compactness principle.
\end{theorem}
\leanchecked{BEDC.Derived.LiminfUp.LiminfRegularSequenceRoute}
\begin{proof}
Begin with \autoref{thm:liminf-scoped-tail-envelope}. The scoped envelope names the bounded sequence, lower-cut schedule, dyadic tolerance ledger, upper comparison surface, and terminal Real seal as the public order of the liminf packet.

Next read the regular-sequence-facing part of the route. The bounded source row $S$ is interpreted through $\BoundedRealSequenceUp$, its lower and upper observations pass through $\BoundedRealLowerCutUp$ and $\BoundedRealUpperCutUp$, and all finite comparisons use the common $\DyadicRatCoreUp$ ledger $D$.

The consumer may then pass through $\RegSeqRatUp$ and only finally through the $\RealUp$ seal $T$. The support rows $H,C,P,N$ transport, replay, source, and name the same finite packet, so the route supplies no host infimum, selected subsequence, quotient equality, Real-completeness principle, or compactness claim.
\end{proof}

\begin{closurestatus}{\LiminfUp}
  \constructivestory{A $\LiminfUp$ packet is generated from a bounded RealUp sequence row, a finite tail lower-cut schedule, a dyadic tolerance ledger, a terminal RealUp seal, componentwise hsame transports, Cont replay routes, package provenance, and a local NameCert row.}
  \theoryclosure{\publicClosure}
  \scopeclosed{\autoref{def:liminf-carrier}, \autoref{def:liminf-classifier}, \autoref{thm:liminf-namecert-obligations}, \autoref{thm:liminf-tail-cut-route}, \autoref{thm:liminf-real-seal-nonescape}, \autoref{thm:liminf-lower-cut-readback-scope}, \autoref{thm:liminf-scoped-tail-envelope}, and \autoref{thm:liminf-regseqrat-lower-cut-public-export} expose the finite tail-cut liminf surface as a public RegSeqRat-facing export for bounded real sequences.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Real completeness, trichotomy, a host infimum, quotient stream equality, selected subsequences, open-cover compactness, Lean verification, or a standard bridge.}
  \upgradepath{Public closure requires checked carrier formation, classifier stability, lower-tail exactness, upper-cut compatibility, terminal seal routing, tail-cut route ordering, lower-cut readback, and Real seal non-escape for the displayed packet.}
\end{closurestatus}
