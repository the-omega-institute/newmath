\chapter{A Concrete Naming Certificate for $\FinitePrefixBarUp$}
\label{ch:concrete-instances-finite-prefix-bar-namecert}
\origin{human}

Following Brouwer, Troelstra--van Dalen, and Bishop--Bridges on spreads, bars, and finite choice-sequence observation, the $\FinitePrefixBarUp$ packet records the constructive L10 interface by which a finite bar over admitted spread prefixes is named before any countable-name or real-facing consumer can read it. It sits after the spread-prefix carrier of \autoref{ch:concrete-instances-brouwer-spread-namecert}, the spread-bar route of \autoref{ch:concrete-instances-brouwer-spread-bar-namecert}, and the finite fan packet of \autoref{ch:concrete-instances-fantheorem-namecert}. The packet is a finite certificate surface, not a completed branch, countable choice principle, quotient stream equality, fan compactness theorem, or ambient $\RealUp$ completeness row.

\begin{definition}[Finite prefix bar carrier]
\label{def:finite-prefix-bar-carrier}
A $\FinitePrefixBarUp$ carrier is a finite $\BHist$ packet
$$
F=(S,B,A,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the accepted $\BrouwerSpreadUp$ row, including the decidable prefix tree, StreamName schedule, and admitted finite-prefix window.
\item $B$ is the $\BrouwerSpreadBarUp$ row restricted to the prefixes visible in $S$, with decidable bar decisions and monotone prefix-extension checks.
\item $A$ is the $\FanTheoremUp$ comparison row, used only as a finite bounded-bar witness when the displayed spread is binary or finitely branching.
\item $W$ is the $\StreamNameUp$ read window through which countable-name consumers request the displayed prefixes.
\item $R$ is the $\RegSeqRatUp$ readback row attached to every analytic observation generated from the finite prefix window.
\item $E$ is the terminal $\RealUp$ seal row exposed to real-facing consumers after the prefix, bar, stream-name, and regular-rational rows have already been displayed.
\item $H$ records componentwise $\hsame$ transport for spread prefixes, bar decisions, fan comparison, stream windows, regular-rational readback, Real seals, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a finite prefix-bar request through
$$
S,B,A \longmapsto W \longmapsto R \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\FinitePrefixBarUp$, $\BrouwerSpreadUp$, $\BrouwerSpreadBarUp$, $\FanTheoremUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FinitePrefixBarUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,B,A,W,R,E,H,C,P,N$. It has no coordinate for a completed branch, a selector over all bars, quotient stream equality, fan compactness as an ambient theorem, selected limits, or Real completeness.
\end{definition}

\begin{theorem}[Finite prefix bar NameCert obligations]
\label{thm:finite-prefix-bar-namecert-obligations}
For every accepted $\FinitePrefixBarUp$ carrier
$$
F=(S,B,A,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\FinitePrefixBarUp}$ surface consists exactly of carrier admission for the Brouwer spread prefix row, restricted spread-bar row, finite fan comparison row, StreamName window, RegSeqRat readback, Real seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming row; classifier agreement on the same rows; and nonescape requiring every public read to factor through $S,B,A,W,R,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-prefix-bar-carrier}. The tuple exposes the spread prefix row $S$, the restricted bar row $B$, the finite fan comparison row $A$, the stream-name window $W$, the regular-rational readback $R$, and the terminal Real seal $E$ before the structural rows are considered.

Classifier agreement is rowwise agreement over that same tuple. The spread component is read through \autoref{ch:concrete-instances-brouwer-spread-namecert}, the finite bar component through \autoref{ch:concrete-instances-brouwer-spread-bar-namecert}, and the finite fan comparison through \autoref{ch:concrete-instances-fantheorem-namecert}.

The structural rows close the certificate surface. The transport row $H$ compares only displayed coordinates, the continuation row $C$ replays only the finite route, and $P,N$ source and name the same packet. Since no extra coordinate exists, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Finite prefix bar nonescape]
\label{thm:finite-prefix-bar-nonescape}
Every countable-name, completion-facing, or real-facing read of an accepted $\FinitePrefixBarUp$ packet must first pass through the finite route
$$
S,B,A \longmapsto W \longmapsto R \longmapsto E,H,C,P,N .
$$
The read receives only admitted spread prefixes, restricted bar decisions, finite fan comparison data, StreamName windows, RegSeqRat readbacks, a terminal Real seal, transport, replay, provenance, and local naming. It does not obtain a completed branch, countable choice, quotient stream equality, fan compactness as a global theorem, selected limits, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with a public read of the packet. The only route to the analytic rows passes through the displayed prefix and bar rows $S,B$ and, when available, the finite fan comparison row $A$.

Replay the continuation row $C$. It opens the StreamName window $W$, then reads the regular-rational row $R$, and only after those rows have been displayed does it expose the terminal Real seal $E$.

Finally attach transport, provenance, and naming through $H,P,N$. The packet has no field for a branch selector, quotient stream equality, an external compactness theorem, selected limits, or Real completeness. Therefore every downstream read remains inside the stated finite route.
\end{proof}

\falsifiablePrediction{Every accepted $\FinitePrefixBarUp$ public StreamName, RegSeqRat, Real, or Completion-facing read factors through a Brouwer spread prefix row, restricted Brouwer spread-bar row, finite FanTheorem comparison row, StreamName window, RegSeqRat readback, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row.}

\independenceWitness{brouwer-spread, brouwer-spread-bar, fan-theorem, streamname, regseqrat, real: $\FinitePrefixBarUp$ is not bijective to any listed sibling because it jointly carries finite spread-prefix admission, restricted bar decisions, bounded fan comparison, stream-name windows, regular-rational readback, terminal Real sealing, transport, replay, provenance, and local naming while refusing completed branches, countable choice, quotient streams, ambient compactness, selected limits, and Real completeness.}

\closureat{FinitePrefixBarUp}{seedStr}

\begin{closurestatus}{\FinitePrefixBarUp}
  \constructivestory{A $\FinitePrefixBarUp$ packet is generated from Brouwer spread prefixes, restricted Brouwer spread-bar decisions, finite FanTheorem comparison data, StreamName windows, RegSeqRat readbacks, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-prefix-bar-carrier}, \autoref{thm:finite-prefix-bar-namecert-obligations}, and \autoref{thm:finite-prefix-bar-nonescape} seed the constructive L10 finite-bar interface for countable-name and real-facing consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed branches, countable choice, quotient stream equality, fan compactness, selected limits, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier agreement, finite-bar route exactness, StreamName and RegSeqRat handoff, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.FinitePrefixBarUp.}
\end{closurestatus}
