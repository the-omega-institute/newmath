\chapter{A Concrete Naming Certificate for $\MetricRegularCauchyCompletionUp$}
\label{ch:concrete-instances-metric-regular-cauchy-completion-namecert}
\origin{human}

Following Bishop--Bridges constructive completion theory, the $\MetricRegularCauchyCompletionUp$ packet records the finite route by which a metric-space point stream is read as regular-Cauchy completion data and then exposed to Real-facing consumers. It sits after the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the completeness boundary of \autoref{ch:concrete-instances-completemetric-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a completion-facing regular-Cauchy handoff; it does not quotient Cauchy filters, choose limit representatives, assert ambient completeness, or import host real equality.

\begin{definition}[Metric regular Cauchy completion carrier]
\label{def:metric-regular-cauchy-completion-carrier}
A $\MetricRegularCauchyCompletionUp$ carrier is a finite $\BHist$ packet
$$
K=(X,M,S,R,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the underlying point row of the named metric-space source.
\item $M$ is the $\MetricSpaceUp$ metric-structure row, exposing only the displayed distance and comparison data available to the packet.
\item $S$ is the $\StreamNameUp$ finite-window row recording the inspected source windows of the Cauchy name.
\item $R$ is the $\RegSeqRatUp$ readback row by which the inspected windows are consumed as regular rational approximation data.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used for metric-window comparison and regular-Cauchy budget replay.
\item $E$ is the terminal $\RealUp$ seal row reached only after $M,S,R,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for point rows, metric rows, stream windows, regular readback, dyadic tolerances, Real sealing, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a completion precision request through
$$
X,M,S,D \longmapsto R \longmapsto E \longmapsto H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\MetricRegularCauchyCompletionUp$, $\MetricSpaceUp$, $\CompleteMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetricRegularCauchyCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,M,S,R,D,E,H,C,P,N$. It has no coordinate for quotient completion equality, a selected completion point, a selected limit representative, countable choice, host real equality, or an ambient completeness proof beyond the displayed $\CompleteMetricUp$ boundary reference.
\end{definition}

\begin{theorem}[Metric regular Cauchy completion NameCert obligations]
\label{thm:metric-regular-cauchy-completion-namecert-obligations}
For every accepted $\MetricRegularCauchyCompletionUp$ carrier
$$
K=(X,M,S,R,D,E,H,C,P,N),
$$
the local $\NameCert_{\MetricRegularCauchyCompletionUp}$ surface consists exactly of carrier admission for \autoref{def:metric-regular-cauchy-completion-carrier}; metric-window exactness through $X,M,S,D$; regular-Cauchy readback through $R$; dyadic tolerance replay through $D$; Real-seal nonescape through $E$; componentwise $\hsame$ transport through $H$; displayed $\Cont$ replay through $C$; $\Pkg$ provenance through $P$; and local naming through $N$. A completion-facing read may cite $\CompleteMetricUp$ as the sibling boundary named in provenance, but the packet itself exposes no hidden completeness theorem or quotient completion object.
\end{theorem}
\begin{proof}
Project the accepted carrier. Its visible coordinates are exactly $X,M,S,R,D,E,H,C,P,N$, so carrier admission and local naming are finite row projections.

The metric-window side is $X,M,S,D$. The point and metric rows determine the displayed distance comparisons, the stream row supplies the inspected windows, and the dyadic ledger fixes the tolerance budget used by those comparisons.

The regular-Cauchy and Real-facing reads are downstream of those rows. The row $R$ consumes the visible windows as regular rational data, and the seal row $E$ can be reached only along the displayed route through $M,S,R,D$.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier has no additional coordinate for quotient equality, selected limits, choice, host real equality, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Metric regular Cauchy completion Real-seal boundary]
\label{thm:metric-regular-cauchy-completion-real-seal-boundary}
Every completion-facing read of an accepted $\MetricRegularCauchyCompletionUp$ carrier reaches $\RealUp$ only through the displayed route
$$
\MetricSpaceUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \DyadicRatCoreUp,\quad X,M,S,R,D,E,H,C,P,N .
$$
Thus the Real seal is a visible boundary of the metric-window, stream-window, regular-readback, and dyadic-tolerance rows; it is not a quotient completion, selected representative, countable-choice schedule, host real primitive, or ambient completeness assertion.
\end{theorem}
\begin{proof}
Begin with a read that asks for the Real-facing seal. The carrier gives no direct Real coordinate before the point row, metric row, inspected stream windows, regular readback, and dyadic tolerance ledger have been exposed.

Read the displayed route. The rows $X,M,S,D$ provide the metric-window comparison surface, and $R$ records the regular-Cauchy readback attached to those same windows and tolerances.

Only then may $E$ be consumed as the $\RealUp$ seal. The structural rows $H,C,P,N$ preserve, replay, source, and name the same route, while $P$ records $\CompleteMetricUp$ only as a sibling boundary for completion consumers.

Because no further carrier coordinate exists, the read cannot escape into quotient completion equality, a selected representative, a countable-choice schedule, host real primitives, or an ambient completeness theorem.
\end{proof}

\begin{theorem}[Metric regular Cauchy completion complete-metric handoff]
\label{thm:metric-regular-cauchy-completion-complete-metric-handoff}
Every accepted $\MetricRegularCauchyCompletionUp$ packet may cite $\CompleteMetricUp$ only through the displayed metric row, $\StreamNameUp$ finite windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance rows, the $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row. The complete-metric sibling is therefore a boundary row for completion consumers, not an ambient completeness theorem available inside the packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metric-regular-cauchy-completion-carrier}. The visible rows are $X,M,S,R,D,E,H,C,P,N$, and the only place $\CompleteMetricUp$ appears is the provenance boundary attached to the completion-facing route.

Use the NameCert obligations of \autoref{thm:metric-regular-cauchy-completion-namecert-obligations}. They force the metric-window, regular-readback, dyadic-tolerance, Real-seal, transport, replay, provenance, and local naming rows to be displayed before any completion-facing read is accepted.

Finally apply the Real-seal boundary of \autoref{thm:metric-regular-cauchy-completion-real-seal-boundary}. Since the seal is reached only through the displayed finite route, the complete-metric citation remains a sibling handoff row and cannot supply quotient completion equality, a selected limit, countable choice, or ambient completeness.
\end{proof}

\begin{theorem}[Metric regular Cauchy completion window universality]
\label{thm:metric-regular-cauchy-completion-window-universality}
Let two completion-facing reads of accepted $\MetricRegularCauchyCompletionUp$ packets have the same displayed metric-window rows, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerances, $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows. Then they determine the same completion-facing surface for every consumer that reads through $\MetricRegularCauchyCompletionUp$.
\end{theorem}
\begin{proof}
Read both packets through \autoref{def:metric-regular-cauchy-completion-carrier}. The classifier of an accepted packet has no coordinate beyond the displayed metric, stream, readback, dyadic, Real-seal, transport, replay, provenance, and local naming rows.

Apply \autoref{thm:metric-regular-cauchy-completion-namecert-obligations}. Equal displayed rows give the same carrier admission, metric-window exactness, regular-Cauchy readback, dyadic tolerance replay, Real-seal nonescape, transport, replay, provenance, and local naming obligations.

The Real-seal boundary theorem then forces every completion-facing consumer to pass through those same rows before reaching $\RealUp$. Hence no quotient stream equality, selected limit, countable-choice schedule, or ambient completeness coordinate remains that could distinguish the two completion-facing surfaces.
\end{proof}

\falsifiablePrediction{Every accepted $\MetricRegularCauchyCompletionUp$ completion-facing read exposes a $\MetricSpaceUp$ metric row, $\StreamNameUp$ finite windows, a $\RegSeqRatUp$ readback row, a $\DyadicRatCoreUp$ tolerance ledger, and the displayed packet rows before it consumes the terminal $\RealUp$ seal.}

\independenceWitness{metricspace, completemetric, streamname, regseqrat, dyadicratcore, real: no listed sibling supplies the whole packet $K=(X,M,S,R,D,E,H,C,P,N)$, because each sibling contributes only a metric row, a completeness boundary, finite windows, regular rational readback, dyadic tolerances, or a Real seal while this chapter binds the completion route with transport, replay, provenance, and local naming.}

\closureat{MetricRegularCauchyCompletionUp}{seedStr}

\begin{closurestatus}{\MetricRegularCauchyCompletionUp}
  \constructivestory{A $\MetricRegularCauchyCompletionUp$ packet is generated from point rows, MetricSpace metric rows, StreamName finite windows, RegSeqRat readback, DyadicRatCore tolerances, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metric-regular-cauchy-completion-carrier}, \autoref{thm:metric-regular-cauchy-completion-namecert-obligations}, and \autoref{thm:metric-regular-cauchy-completion-real-seal-boundary} seed the metric regular-Cauchy completion packet over metric windows, finite streams, regular rational readback, dyadic tolerances, and Real sealing.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion equality, selected completion points, countable choice, host real equality, Lean verification, audit cleanliness, bridge checking, or ambient completeness of $\RealUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, metric-window exactness, regular-Cauchy readback, dyadic tolerance replay, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.MetricRegularCauchyCompletionUp.}
\end{closurestatus}
