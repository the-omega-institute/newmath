\chapter{A Concrete Naming Certificate for $\PoincareDiskAreaElementUp$}
\label{ch:concrete-instances-poincare-disk-area-element-namecert}
\origin{ai}

The $\PoincareDiskAreaElementUp$ packet realizes the local area-read branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite disk, metric, radius, Real-seal, and observer-expansion certificate data. It consumes the disk carrier of \autoref{ch:concrete-instances-unitdisk-namecert}, the Poincare disk metric surface of \autoref{ch:concrete-instances-poincare-disk-metric-namecert}, the hyperbolic metric row of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the observer-expansion packet of \autoref{ch:concrete-instances-hyperbolic-observer-expansion-namecert}, the dyadic shadow cone of \autoref{ch:concrete-instances-dyadic-shadow-cone-namecert}, and the dyadic and Real substrate of \autoref{ch:concrete-instances-dyadicratcore-namecert} and \autoref{ch:concrete-instances-real-namecert}. The packet names a local area-element readback, not a global hyperbolic surface, quotient disk equality, selected coordinate atlas, physical horizon law, or ambient measure-completeness theorem.

\begin{definition}[Poincare disk area-element carrier]
\label{def:poincare-disk-area-element-carrier}
A $\PoincareDiskAreaElementUp$ carrier is a finite $\BHist$ packet
$$
A=(U,M,G,D,B,O,R,F,T,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ is the $\UnitDiskUp$ disk-coordinate row for the displayed local point or finite disk window.
\item $M$ is the $\PoincareDiskMetricUp$ row that reads the Poincare disk metric over the same coordinate data.
\item $G$ is the $\HyperbolicMetricUp$ row comparing the disk metric read with the hyperbolic metric surface consumed downstream.
\item $D$ is the $\DyadicRatCoreUp$ dyadic-radius row, recording the finite radius scale at which local area is inspected.
\item $B$ is the area-budget row, binding the displayed radius and metric rows to a finite area-element budget.
\item $O$ is the $\HyperbolicObserverExpansionUp$ provenance row that records which observer-expansion branch receives the local area readback.
\item $R$ is the terminal $\RealUp$ seal row for the displayed area budget, available only after $U,M,G,D,B,O$ are visible.
\item $F$ is the refusal row excluding completed disk quotients, selected coordinate atlases, global area measures, physical horizon semantics, and ambient completeness.
\item $T$ records componentwise $\hsame$ transport for disk coordinates, metric rows, dyadic radii, area budgets, observer-expansion provenance, Real seals, refusal data, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a local disk-area request through $U,M,G,D,B,O,R,F,T,P,N$.
\item $P$ is the $\Pkg$ provenance over $\PoincareDiskAreaElementUp$, $\UnitDiskUp$, $\PoincareDiskMetricUp$, $\HyperbolicMetricUp$, $\DyadicRatCoreUp$, $\DyadicShadowConeUp$, $\HyperbolicObserverExpansionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\PoincareDiskAreaElementUp}$ row.
\end{enumerate}
The carrier has no coordinate for a completed Poincare disk quotient, selected global chart, ambient measure object, physical observer law, or hyperbolic completeness theorem.
\end{definition}

\begin{theorem}[Poincare disk area-element NameCert obligations]
\label{thm:poincare-disk-area-element-namecert-obligations}
For every accepted $\PoincareDiskAreaElementUp$ carrier $A=(U,M,G,D,B,O,R,F,T,C,P,N)$, the local $\NameCert_{\PoincareDiskAreaElementUp}$ surface consists exactly of the following five obligation rows:
\begin{enumerate}
\item disk-and-metric admission through $U,M,G$;
\item dyadic-radius and area-budget exactness through $D,B$;
\item observer-expansion provenance and Real-seal routing through $O,R$;
\item refusal and transport preservation through $F,T$;
\item displayed replay, provenance, and local naming through $C,P,N$.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:poincare-disk-area-element-carrier}. The disk and metric rows are $U,M,G$, the finite radius and area-budget rows are $D,B$, and the observer-facing and Real-facing rows are $O,R$.

The budget row $B$ is available only after the disk coordinate, metric comparison, and dyadic-radius rows have been displayed. Thus an area-element read is local and finite before any Real seal can be consumed.

The rows $F,T,C,P,N$ then refuse nonlocal endpoints, transport the same displayed data, replay the route, record provenance, and name the local packet. No additional carrier coordinate can supply a global measure, quotient disk equality, selected chart, physical horizon law, or ambient completeness theorem.
\end{proof}

\falsifiablePrediction{Every accepted $\PoincareDiskAreaElementUp$ read factors through UnitDisk, PoincareDiskMetric, HyperbolicMetric, DyadicRatCore, Real, observer-expansion provenance, transport, replay, package, and local NameCert rows before an area budget is visible.}

\independenceWitness{unitdisk, poincare-disk-metric, hyperbolic-metric, hyperbolic-observer-expansion, dyadic-shadow-cone: no listed sibling is bijective to $\PoincareDiskAreaElementUp$ because this packet jointly carries disk coordinates, metric comparison, dyadic radius, area budget, observer-expansion provenance, Real sealing, refusal, transport, replay, provenance, and local naming as one finite route.}

The Lean TasteGate route must provide \texttt{Nontrivial PoincareDiskAreaElementUp} and \texttt{FieldFaithful PoincareDiskAreaElementUp} instances in \texttt{lean4/BEDC/Derived/PoincareDiskAreaElementUp/TasteGate.lean}. Those instances must keep $U,M,G,D,B,O,R,F,T,C,P,N$ as distinct fields before any checked marker is used.

\closureat{PoincareDiskAreaElementUp}{seedStr}

\begin{closurestatus}{\PoincareDiskAreaElementUp}
  \constructivestory{A $\PoincareDiskAreaElementUp$ packet is generated from UnitDisk coordinate rows, PoincareDiskMetric rows, HyperbolicMetric rows, DyadicRatCore radius rows, area-budget rows, HyperbolicObserverExpansion provenance, a RealUp seal, refusal data, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:poincare-disk-area-element-carrier} and \autoref{thm:poincare-disk-area-element-namecert-obligations} seed the finite local disk-area route over disk coordinates, metric rows, dyadic radii, area budgets, observer-expansion provenance, Real seals, refusal, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim completed disk quotients, selected global charts, ambient measure objects, physical observer laws, hyperbolic completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked disk-and-metric admission, dyadic-radius exactness, area-budget exactness, observer-expansion provenance, Real-seal routing, refusal, transport, replay, provenance, local naming, TasteGate nontriviality, and field faithfulness for BEDC.Derived.PoincareDiskAreaElementUp.}
\end{closurestatus}
