\chapter{A Concrete Naming Certificate for $\WeakStarCompactnessUp$}
\label{ch:concrete-instances-weak-star-compactness-namecert}
\origin{human}

Following Bourbaki and Kelley on locally convex spaces and weak-star compactness, the $\WeakStarCompactnessUp$ packet records the finite dual-ball compactness interface that sits between the weak-star observation rows of \autoref{ch:concrete-instances-weak-star-topology-namecert} and the finite dual-ball handoff of \autoref{ch:concrete-instances-banach-alaoglu-namecert}. It is read over the locally convex source of \autoref{ch:concrete-instances-locally-convex-space-namecert} and the compactness carrier of \autoref{ch:concrete-instances-compact-namecert}. The chapter names the compactness-facing dual-ball route as finite NameCert data; it does not identify a host dual space, choose cluster points, invoke ultrafilters, or export unrestricted product compactness.

\begin{definition}[Weak-star compactness carrier]
\label{def:weak-star-compactness-carrier}
A $\WeakStarCompactnessUp$ carrier is a finite $\BHist$ packet
$$
K=(E,T,B,A,C,R,H,Q,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the locally convex source row, read through the finite seminorm and convex balanced neighbourhood surface of $\LocallyConvexSpaceUp$.
\item $T$ is the weak-star topology row, exposing only the finite functional observation windows admitted by $\WeakStarTopologyUp$.
\item $B$ is the finite dual-ball row, listing the displayed bounded functionals, source probes, scalar bounds, and tolerance rows inspected by the packet.
\item $A$ is the Banach--Alaoglu handoff row, used only as the finite dual-ball window described by $\BanachAlaogluUp$.
\item $C$ is the compactness row, read through $\CompactUp$ as finite-net or located compactness evidence for the displayed window, not as an unrestricted host compactness predicate.
\item $R$ is the scalar readback row for real-valued functional values, bounds, and comparisons.
\item $H$ records componentwise $\hsame$ transport for the locally convex source, weak-star topology window, dual-ball row, Banach--Alaoglu handoff, compactness row, scalar readback, replay, provenance, and naming rows.
\item $Q$ records displayed $\Cont$ replay from a compactness-facing dual-ball request through $E,T,B,A,C,R,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\WeakStarCompactnessUp$, $\LocallyConvexSpaceUp$, $\WeakStarTopologyUp$, $\BanachAlaogluUp$, $\CompactUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\WeakStarCompactnessUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $E,T,B,A,C,R,H,Q,P,N$. It has no coordinate for a chosen dual element, a selected cluster point, ultrafilter extension, quotient equality of functionals, unrestricted Tychonoff compactness, or ambient completeness.
\end{definition}
\leanchecked{BEDC.Derived.WeakStarCompactnessUp.WeakStarCompactnessTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Weak-star compactness NameCert obligations]
\label{thm:weak-star-compactness-namecert-obligations}
For every accepted $\WeakStarCompactnessUp$ carrier $K=(E,T,B,A,C,R,H,Q,P,N)$, the local $\NameCert_{\WeakStarCompactnessUp}$ surface consists exactly of locally convex source admission through $E$, weak-star observation-window admission through $T$, finite dual-ball admission through $B$, Banach--Alaoglu finite-window handoff through $A$, compactness evidence through $C$, scalar readback through $R$, componentwise transport through $H$, displayed replay through $Q$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier in \autoref{def:weak-star-compactness-carrier}. The rows $E,T,B,A,C,R,H,Q,P,N$ are the complete displayed inventory of the packet.

The source and topology rows are read first. The locally convex source $E$ fixes the finite seminorm and neighbourhood surface, while $T$ restricts weak-star access to finite functional observation windows.

The compactness-facing data then pass through $B,A,C,R$. The dual-ball row $B$ lists the bounded functional probes, $A$ cites only the Banach--Alaoglu finite-window handoff, $C$ supplies the displayed compactness evidence for that window, and $R$ records the scalar readbacks.

Transport, replay, provenance, and naming are supplied by $H,Q,P,N$. Since no other carrier coordinate is present, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Weak-star compactness finite dual-ball route]
\label{thm:weak-star-compactness-finite-dual-ball-route}
Every compactness export from an accepted $\WeakStarCompactnessUp$ packet factors through the finite route
$$
E,T,B,A,C,R,H,Q,P,N .
$$
Thus a functional-analysis consumer may read weak-star compactness only after exposing the locally convex source, the weak-star observation window, the finite dual-ball row, the Banach--Alaoglu handoff, compactness evidence, scalar readback, transport, replay, provenance, and local NameCert rows. The export carries no chosen dual element, no selected cluster point, no ultrafilter extension, no quotient equality of functionals, no unrestricted Tychonoff compactness, and no ambient completeness coordinate.
\end{theorem}
\begin{proof}
Begin with a compactness-facing export. By \autoref{thm:weak-star-compactness-namecert-obligations}, the export must expose the locally convex source $E$ and the weak-star observation window $T$ before any dual-ball compactness data can be inspected.

The finite dual-ball row $B$ and Banach--Alaoglu handoff row $A$ then determine the displayed functional probes and scalar bounds. Compactness evidence is read only through $C$, and scalar values and comparisons are read only through $R$.

The structural rows $H,Q,P,N$ preserve that same route under transport, replay, provenance, and local naming. Because the carrier contains no selection, ultrafilter, quotient, unrestricted product, or ambient completeness row, none of those data can be exported by projection from the accepted packet.
\end{proof}

\falsifiablePrediction{Every public $\WeakStarCompactnessUp$ compactness read factors through a displayed $\LocallyConvexSpaceUp$ source, a finite $\WeakStarTopologyUp$ observation window, a finite dual-ball row, a $\BanachAlaogluUp$ handoff, a $\CompactUp$ compactness row, scalar readback, $\BHist$ transport, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ naming. A public read that exports selected dual elements, cluster-point choice, ultrafilters, quotient functional equality, unrestricted product compactness, or ambient completeness from this packet refutes the carrier boundary.}

\independenceWitness{weakstartopology, banachalaoglu, locallyconvexspace, compact: none is bijective with $\WeakStarCompactnessUp$ because this carrier binds the locally convex source, weak-star finite observation window, finite dual-ball handoff, compactness evidence, scalar readback, transport, replay, provenance, and local NameCert rows while refusing selection and unrestricted compactness coordinates.}

\closureat{WeakStarCompactnessUp}{seedStr}

\begin{closurestatus}{\WeakStarCompactnessUp}
  \constructivestory{A $\WeakStarCompactnessUp$ packet is generated from a locally convex source row, a weak-star finite observation window, a finite dual-ball row, a Banach--Alaoglu handoff, compactness evidence, scalar readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:weak-star-compactness-carrier}, \autoref{thm:weak-star-compactness-namecert-obligations}, and \autoref{thm:weak-star-compactness-finite-dual-ball-route} seed the finite dual-ball compactness interface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected dual elements, cluster-point choice, ultrafilter extension, quotient functional equality, unrestricted Tychonoff compactness, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, weak-star observation-window admission, finite dual-ball exactness, compactness-row handoff, scalar readback, transport, replay, provenance, and local naming for BEDC.Derived.WeakStarCompactnessUp.}
\end{closurestatus}
