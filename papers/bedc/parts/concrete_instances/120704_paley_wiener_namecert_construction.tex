\chapter{A Concrete Naming Certificate for $\PaleyWienerUp$}
\label{ch:concrete-instances-paley-wiener-namecert}
\origin{human}

Following Paley and Wiener on Fourier transforms of compactly supported functions, the $\PaleyWienerUp$ packet records the finite support-and-growth surface that a constructive Fourier-analysis consumer must inspect before it reads a band-limited or exponential-type conclusion. It sits beside the Fourier transform route of \autoref{ch:concrete-instances-fourier-transform-namecert}, the Schwartz-function test-window surface of \autoref{ch:concrete-instances-schwartz-function-namecert}, the tempered-distribution action row of \autoref{ch:concrete-instances-tempered-distribution-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only finite support windows, displayed growth ledgers, Fourier handoff rows, test-function probes, distribution-facing action rows, scalar readback, transport, replay, provenance, and local naming; it does not assert Fourier inversion, a completed entire-function space, quotient equality of functions or distributions, selected representatives, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Paley--Wiener carrier]
\label{def:paley-wiener-carrier}
A $\PaleyWienerUp$ carrier is a finite $\BHist$ packet
$$
P=(K,F,G,S,T,R,E,H,C,Q,N)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $K$ is the compact support window row, naming the finite spatial support bounds and inspected test domains.
\item $F$ is the $\FourierTransformUp$ handoff row, recording the transform-facing read of the supported source.
\item $G$ is the growth ledger row, listing the finite exponential-type and polynomial-control bounds visible to the packet.
\item $S$ is the $\SchwartzFunctionUp$ probe row, exposing the rapidly decreasing test windows used to compare source and transform reads.
\item $T$ is the $\TemperedDistributionUp$ action row, recording the distribution-facing interpretation of the same finite transform data.
\item $R$ is the $\RegSeqRatUp$ scalar readback row for the displayed bounds.
\item $E$ is the terminal $\RealUp$ seal row for scalar comparisons.
\item $H$ records componentwise $\hsame$ transport for support, Fourier handoff, growth, probes, distribution action, scalar readback, seal, provenance, and local name.
\item $C$ records displayed $\Cont$ replay from support inspection through Fourier handoff, growth readback, distribution action, scalar sealing, and local naming.
\item $Q$ is the $\Pkg$ provenance over $\PaleyWienerUp$, $\FourierTransformUp$, $\SchwartzFunctionUp$, $\TemperedDistributionUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PaleyWienerUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,F,G,S,T,R,E,H,C,Q,N$. It has no coordinate for host Fourier inversion, completed spaces of entire functions, quotient equality of functions or distributions, selected representatives, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Paley--Wiener NameCert obligations]
\label{thm:paley-wiener-namecert-obligations}
For every accepted $\PaleyWienerUp$ carrier $P=(K,F,G,S,T,R,E,H,C,Q,N)$, the local $\NameCert_{\PaleyWienerUp}$ surface consists exactly of carrier admission for compact support windows, Fourier-transform handoff, finite growth ledgers, Schwartz-function probes, tempered-distribution action, regular rational readback, Real scalar sealing, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Any Fourier-analysis, distribution, or scalar-bound consumer of the packet must read through those rows before it cites a Paley--Wiener boundary.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:paley-wiener-carrier}. The support row $K$, transform row $F$, growth row $G$, Schwartz probe row $S$, distribution action row $T$, readback row $R$, and seal row $E$ are the only analytic coordinates.

The Fourier-facing read is finite. It exposes the support window before the transform handoff, and it exposes the growth ledger before any scalar comparison can be sealed in $\RealUp$.

The test and distribution rows keep the route inside the displayed sibling certificates. The $\SchwartzFunctionUp$ probes and $\TemperedDistributionUp$ action rows are read through $S,T$, not through a host function space or a quotient distribution.

Finally $H,C,Q,N$ transport, replay, source, and name the same finite packet. Since the carrier has no coordinate for Fourier inversion, completed entire-function spaces, quotient equality, selected representatives, countable choice, or ambient completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Paley--Wiener Fourier support handoff]
\label{thm:paley-wiener-fourier-support-handoff}
Every Fourier-facing read of an accepted $\PaleyWienerUp$ carrier factors through the finite route
$$
\FourierTransformUp \longmapsto \SchwartzFunctionUp \longmapsto \TemperedDistributionUp
\longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
after the compact support row $K$ and growth ledger $G$ have been displayed. The handoff exports support-window and growth-bound data together with transport, replay, provenance, and local naming, but it exports no Fourier inversion theorem, completed entire-function space, quotient equality of functions or distributions, selected representative, countable-choice schedule, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Start from the accepted carrier $P=(K,F,G,S,T,R,E,H,C,Q,N)$. The support row $K$ and Fourier handoff row $F$ are upstream of every transform-facing read.

The growth ledger $G$ is then read before scalar sealing. It records only finite exponential-type and polynomial-control bounds, and the scalar comparison passes through $R$ and $E$.

Next expose the sibling analysis rows. The Schwartz probe row $S$ supplies the test-window comparison, and the tempered-distribution action row $T$ carries the same transform data toward distribution-facing consumers.

Finally replay the route through $H,C,Q,N$. Because no host inversion, completed function space, quotient equality, selected representative, countable choice, or ambient completeness coordinate appears in the carrier, the Fourier support handoff remains the displayed finite NameCert route.
\end{proof}

\falsifiablePrediction{Every accepted $\PaleyWienerUp$ Fourier-facing read exposes compact support windows, Fourier handoff, growth ledger, SchwartzFunction probes, TemperedDistribution action, RegSeqRat readback, Real seal, hsame transport, Cont replay, Pkg provenance, and local NameCert rows before any Paley--Wiener boundary is cited.}

\independenceWitness{fourier-transform, schwartz-function, tempered-distribution, regseqrat, real: $\PaleyWienerUp$ is not bijective to any listed sibling because it jointly carries compact support windows, Fourier handoff, growth ledgers, Schwartz probes, tempered-distribution action, scalar readback, sealing, transport, replay, provenance, and local naming while refusing inversion and completed entire-function data.}

\closureat{PaleyWienerUp}{seedStr}

\begin{closurestatus}{\PaleyWienerUp}
  \constructivestory{A $\PaleyWienerUp$ packet is generated from compact support windows, FourierTransformUp handoff rows, finite growth ledgers, SchwartzFunctionUp probes, TemperedDistributionUp action rows, RegSeqRatUp scalar readback, RealUp sealing, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:paley-wiener-carrier}, \autoref{thm:paley-wiener-namecert-obligations}, and \autoref{thm:paley-wiener-fourier-support-handoff} seed the finite Paley--Wiener support and growth route over Fourier transform, Schwartz-function, tempered-distribution, scalar-readback, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not claim Fourier inversion, completed entire-function spaces, quotient equality of functions or distributions, selected representatives, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, support-window exactness, Fourier handoff, growth-ledger stability, Schwartz-probe and tempered-distribution action rows, scalar readback, transport, replay, provenance, and local naming for BEDC.Derived.PaleyWienerUp.}
\end{closurestatus}
