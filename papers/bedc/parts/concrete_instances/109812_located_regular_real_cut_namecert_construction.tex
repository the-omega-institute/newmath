\chapter{A Concrete Naming Certificate for $\LocatedRegularRealCutUp$}
\label{ch:concrete-instances-located-regular-real-cut-namecert}
\origin{human}

Following Bishop and Bridges' constructive real analysis, the $\LocatedRegularRealCutUp$ packet records the finite boundary where regular-Cauchy real names expose located lower and upper rational cut rows before the terminal real seal is read. It sits beside the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the Dedekind cut carrier of \autoref{ch:concrete-instances-dedekindcut-namecert}, and the located regular sequence route of \autoref{ch:concrete-instances-located-regseqrat-namecert}. The packet is a finite NameCert bridge from regular-Cauchy observations to located cut boundaries; it does not claim Dedekind completeness, quotient equality of real names, selected limits, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Located regular real cut carrier]
\label{def:located-regular-real-cut-carrier}
A $\LocatedRegularRealCutUp$ carrier is a finite $\BHist$ packet
$$
K=(L,U,M,D,R,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the lower rational cut row, recording inhabited rounded lower observations read through the finite cut-facing side of $\DedekindCutUp$.
\item $U$ is the upper rational cut row, recording rounded upper observations attached to the same inspected real-name request.
\item $M$ is the locatedness modulus row, binding every rational comparison request to a finite window on which either the lower row or the upper row is displayed with the required separation.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row supplying dyadic radii, bracket widths, midpoint requests, and comparison tolerances for the cut boundary.
\item $R$ is the $\RegSeqRatUp$ readback row, exposing the regular rational observations from which the cut rows and locatedness modulus are read.
\item $A$ is the terminal $\RealUp$ seal row reached only after $L,U,M,D,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for lower cut rows, upper cut rows, locatedness modulus rows, dyadic tolerance rows, regular readback rows, Real seal rows, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a cut-boundary request through $L,U,M,D,R,A,H$.
\item $P$ is the $\Pkg$ provenance over $\LocatedRegularRealCutUp$, $\DedekindCutUp$, $\LocatedRegSeqRatUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedRegularRealCutUp}$ row.
\end{enumerate}
The carrier accepts only the displayed lower row, upper row, locatedness modulus, dyadic tolerance, regular readback, Real seal, transport, replay, provenance, and naming rows. It has no coordinate for Dedekind completeness, quotient real equality, selected representatives, trichotomy, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Located regular real cut obligations]
\label{thm:located-regular-real-cut-obligations}
For every accepted $\LocatedRegularRealCutUp$ carrier $K=(L,U,M,D,R,A,H,C,P,N)$, the local $\NameCert_{\LocatedRegularRealCutUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the displayed lower cut row, upper cut row, locatedness modulus, dyadic tolerance ledger, regular readback, Real seal, transport, replay, provenance, and local naming row;
\item located lower and upper stability, requiring $L$ and $U$ to remain rounded, separated, and transported componentwise under the same $\hsame$ row;
\item regular Cauchy handoff, requiring every cut-boundary read to pass through $R$ and the located regular sequence discipline of \autoref{ch:concrete-instances-located-regseqrat-namecert};
\item Real seal non-escape, requiring $A$ to be downstream of $L,U,M,D,R$ and not an independent completeness row;
\item ledger exactness, requiring $P$ and $N$ to name only $L,U,M,D,R,A,H,C$ and to exclude quotient equality, selected limits, trichotomy, countable choice, and ambient $\RealUp$ completeness.
\end{enumerate}
No NameCert obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-regular-real-cut-carrier}. The displayed analytic rows are the lower cut row $L$, upper cut row $U$, locatedness modulus $M$, dyadic tolerance row $D$, regular readback row $R$, and Real seal row $A$.

The lower and upper stability clauses are componentwise. Transport through $H$ preserves the two cut rows together with the same modulus and dyadic tolerance row, so the classifier cannot move a lower observation without moving the matching upper boundary data.

The regular Cauchy handoff is the route through $R$. The located regular sequence sibling supplies the finite windows and readback discipline before the cut boundary can be consumed, while $D$ supplies the tolerance ledger on which the locatedness modulus is read.

Finally $A,C,P,N$ seal, replay, source, and name only the displayed route. Since none of the carrier coordinates is a quotient, selected limit, trichotomy branch, choice schedule, or ambient completeness theorem, the listed obligations exhaust the local certificate.
\end{proof}

\begin{theorem}[Located regular real cut Cauchy boundary route]
\label{thm:located-regular-real-cut-cauchy-boundary-route}
Every accepted $\LocatedRegularRealCutUp$ consumer factors through
$$
\DyadicRatCoreUp,\ \RegSeqRatUp,\ \LocatedRegSeqRatUp,\ \DedekindCutUp,\ \RealUp
$$
as displayed finite rows before it can read the local $\NameCert_{\LocatedRegularRealCutUp}$ row. The route exports a located lower--upper cut boundary for a regular-Cauchy real name; it does not export Dedekind completeness, quotient real equality, selected limits, trichotomy, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:located-regular-real-cut-obligations}. A consumer first sees the dyadic tolerance row $D$, regular readback row $R$, and locatedness modulus $M$ before the lower and upper cut rows can be treated as a located boundary.

The Dedekind cut sibling supplies the cut-facing row shape, while the located regular sequence sibling supplies the Cauchy-facing source and tail-window discipline. These rows meet only through the displayed finite packet $K$.

The Real seal row $A$ is terminal for this route. Componentwise transport, continuation replay, package provenance, and local naming preserve the same displayed rows, so no consumer can bypass them by invoking quotient equality, a selected representative, trichotomy, choice, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every accepted $\LocatedRegularRealCutUp$ read factors through displayed lower rational cut, upper rational cut, locatedness modulus, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\LocatedRegSeqRatUp$, $\DedekindCutUp$, $\RealUp$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.}

\independenceWitness{dyadicratcore, regseqrat, real, dedekindcut, located-regseqrat: no listed sibling is bijective to $\LocatedRegularRealCutUp$ because this packet alone binds lower and upper cut rows, a locatedness modulus, dyadic tolerance, regular readback, terminal real seal, transport, replay, provenance, and local naming into one finite carrier.}

\closureat{LocatedRegularRealCutUp}{seedStr}

\begin{closurestatus}{\LocatedRegularRealCutUp}
  \constructivestory{A $\LocatedRegularRealCutUp$ packet is generated from lower rational cut rows, upper rational cut rows, a locatedness modulus, DyadicRatCore tolerance rows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-regular-real-cut-carrier}, \autoref{thm:located-regular-real-cut-obligations}, and \autoref{thm:located-regular-real-cut-cauchy-boundary-route} seed the finite bridge from regular-Cauchy real names to located lower--upper cut boundaries.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Dedekind completeness, quotient real equality, selected limits, trichotomy, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located lower and upper stability, regular Cauchy handoff, Real seal non-escape, and ledger exactness for BEDC.Derived.LocatedRegularRealCutUp.}
\end{closurestatus}
