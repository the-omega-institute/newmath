\chapter{A Concrete Naming Certificate for $\LocatedCauchyIntervalCompletionUp$}
\label{ch:concrete-instances-located-cauchy-interval-completion-namecert}
\origin{human}

Following Bishop and Bridges on located intervals and regular Cauchy completion, the $\LocatedCauchyIntervalCompletionUp$ packet records the finite BEDC route by which a located interval source produces a regular Cauchy interval-name surface and then reaches the Real-facing seal. It sits after the located interval carrier of \autoref{ch:concrete-instances-located-real-interval-namecert}, the finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal $\RealUp$ boundary of \autoref{ch:concrete-instances-real-namecert}, and the completion criterion of \autoref{ch:concrete-instances-bishop-completion-criterion-namecert}. The packet is a located interval completion certificate, not quotient real equality, open-cover compactness, countable choice, a selected limit, or ambient $\RealUp$ completeness.

\begin{definition}[Located Cauchy interval completion carrier]
\label{def:located-cauchy-interval-completion-carrier}
A $\LocatedCauchyIntervalCompletionUp$ carrier is a finite $\BHist$ packet
$$
T=(I,W,D,Q,S,A,H,C,P,N)
$$
with the following rows:
\begin{enumerate}
\item $I$ is the $\LocatedRealIntervalUp$ source row, exposing endpoints, radius data, and locatedness probes as finite interval evidence.
\item $W$ is the $\StreamNameUp$ finite-window row through which interval observations are requested.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for interval widths, endpoint comparison slack, and refinement budgets.
\item $Q$ is the $\RegSeqRatUp$ readback row obtained from the displayed windows and dyadic tolerances.
\item $S$ is the $\BishopCompletionCriterionUp$ row recording that the regular interval windows meet the displayed Cauchy completion criterion.
\item $A$ is the terminal $\RealUp$ seal row reached only after $I,W,D,Q,S$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the interval source, windows, dyadic ledger, regular readback, completion criterion, Real seal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a Real or Completion request through the same ordered rows.
\item $P$ is the $\Pkg$ provenance over $\LocatedCauchyIntervalCompletionUp$, $\LocatedRealIntervalUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\BishopCompletionCriterionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedCauchyIntervalCompletionUp}$ row.
\end{enumerate}
The carrier predicate asserts exactly that these ten rows form one located Cauchy interval completion packet. It has no coordinate for quotient reals, selected representatives, host interval equality, open-cover compactness, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Located Cauchy interval completion NameCert obligations]
\label{thm:located-cauchy-interval-completion-namecert-obligations}
For every accepted $\LocatedCauchyIntervalCompletionUp$ carrier $T=(I,W,D,Q,S,A,H,C,P,N)$, the local $\NameCert_{\LocatedCauchyIntervalCompletionUp}$ surface consists exactly of carrier admission for the located interval source, finite windows, dyadic tolerance ledger, regular readback, Bishop completion criterion, terminal Real seal, componentwise transport, displayed replay, provenance, and local naming rows. It also includes route exactness from $I$ through $W,D,Q,S,A$ and ledger exhaustion excluding quotient reals, selected limits, open-cover compactness, countable choice, host equality, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:located-cauchy-interval-completion-carrier}. The projected rows are exactly the located interval source, finite window, dyadic ledger, regular readback, completion criterion, Real seal, and structural rows.

Route exactness follows from the tuple order. A public completion read begins with $I$, opens only the finite windows in $W$, measures them by the dyadic ledger $D$, reads them through $Q$, and then checks the displayed Bishop completion criterion $S$ before $A$ is available.

The transport, replay, provenance, and local naming obligations are the rows $H,C,P,N$. Since the carrier contains no further coordinate, the excluded quotient, choice, compactness, host equality, selected-limit, and ambient-completeness principles cannot be exported.
\end{proof}

\begin{theorem}[Located Cauchy interval completion Real seal nonescape]
\label{thm:located-cauchy-interval-completion-real-seal-nonescape}
Every $\RealUp$ seal exported by an accepted $\LocatedCauchyIntervalCompletionUp$ carrier is downstream of the displayed route
$$
\LocatedRealIntervalUp(I)\longmapsto \StreamNameUp(W)\longmapsto \DyadicRatCoreUp(D)\longmapsto \RegSeqRatUp(Q)\longmapsto \BishopCompletionCriterionUp(S).
$$
The seal therefore names only the finite located Cauchy interval completion read and exports no quotient stream equality, selected host limit, countable-choice schedule, open-cover compactness witness, host interval equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the source interval row $I$. It supplies endpoint, radius, and locatedness data through the located real interval certificate rather than through a host closed interval.

Read the finite observation route. The stream row $W$ schedules the inspected windows, the dyadic ledger $D$ records the tolerance and refinement budget, and $Q$ supplies the regular rational readback for the requested precision.

Apply the completion criterion row $S$. The Real seal $A$ is public only after that displayed criterion has been read from the same finite route.

The structural rows $H,C,P,N$ transport and replay this route without adding analytic data. Thus the listed quotient, choice, compactness, host, selected-limit, and ambient-completeness principles remain outside the packet.
\end{proof}

\begin{theorem}[Located Cauchy interval completion L10 handoff]
\label{thm:located-cauchy-interval-completion-l10-handoff}
Any Phase Real and Completion consumer of $\LocatedCauchyIntervalCompletionUp$ must consume the L10 handoff through the four displayed source faces $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, with the located interval row and Bishop completion criterion kept visible as source and acceptance rows. Agreement on a terminal Real seal alone cannot replace the displayed interval source, tolerance ledger, regular readback, or completion criterion.
\end{theorem}
\begin{proof}
Project the L10 faces from \autoref{def:located-cauchy-interval-completion-carrier}. The rows $W,D,Q,A$ are exactly the StreamName, DyadicRatCore, RegSeqRat, and Real faces available to a Real and Completion consumer.

Keep the source and acceptance rows visible. The located interval row $I$ supplies the finite interval data, while $S$ records the Bishop completion criterion that permits the terminal seal to be read as a completion handoff.

Use \autoref{thm:located-cauchy-interval-completion-namecert-obligations} and \autoref{thm:located-cauchy-interval-completion-real-seal-nonescape}. Together they force every accepted handoff through $I,W,D,Q,S,A,H,C,P,N$ and rule out terminal-seal agreement as a substitute for the displayed route.
\end{proof}

\falsifiablePrediction{Every accepted $\LocatedCauchyIntervalCompletionUp$ read must expose a located interval source, StreamName finite windows, DyadicRatCore tolerance ledger, RegSeqRat readback, BishopCompletionCriterion acceptance, Real seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{located-real-interval, streamname, dyadicratcore, regseqrat, bishop-completion-criterion, real: $\LocatedCauchyIntervalCompletionUp$ is not bijective to any listed sibling because it binds located interval source data to the regular Cauchy completion handoff and terminal Real seal in one finite NameCert packet.}

\closureat{LocatedCauchyIntervalCompletionUp}{seedStr}

\begin{closurestatus}{\LocatedCauchyIntervalCompletionUp}
  \constructivestory{A $\LocatedCauchyIntervalCompletionUp$ packet is generated from a located interval source row, finite StreamName windows, DyadicRatCore tolerance data, RegSeqRat readback, a BishopCompletionCriterion row, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-cauchy-interval-completion-carrier}, \autoref{thm:located-cauchy-interval-completion-namecert-obligations}, \autoref{thm:located-cauchy-interval-completion-real-seal-nonescape}, and \autoref{thm:located-cauchy-interval-completion-l10-handoff} seed the located Cauchy interval completion route through located interval, StreamName, DyadicRatCore, RegSeqRat, BishopCompletionCriterion, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient real equality, open-cover compactness, countable choice, selected host limits, host interval equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, route exactness, Real-seal nonescape, L10 handoff, transport, replay, provenance, and local naming for BEDC.Derived.LocatedCauchyIntervalCompletionUp.}
\end{closurestatus}
