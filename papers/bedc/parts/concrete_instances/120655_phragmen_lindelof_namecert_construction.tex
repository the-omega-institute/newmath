\chapter{A Concrete Naming Certificate for $\PhragmenLindelofUp$}
\label{ch:concrete-instances-phragmen-lindelof-namecert}
\origin{human}

Following Phragmen and Lindelof, the $\PhragmenLindelofUp$ packet records the unbounded-strip companion to the bounded vertical-strip ledger of \autoref{ch:concrete-instances-hadamard-three-lines-namecert}. It sits over the holomorphic carrier of \autoref{ch:concrete-instances-holomorphic-namecert}, the complex carrier of \autoref{ch:concrete-instances-complex-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the bounded-strip estimate of \autoref{ch:concrete-instances-hadamard-three-lines-namecert}, and the boundary maximum surface of \autoref{ch:concrete-instances-maximum-modulus-principle-namecert}. The packet names finite boundary, growth, and exhaustion rows for an unbounded strip; it does not import host maximum principles, selected suprema, open-cover compactness, or ambient real completeness.

\begin{definition}[Phragmen--Lindelof carrier]
\label{def:phragmen-lindelof-carrier}
A $\PhragmenLindelofUp$ carrier is a finite $\BHist$ packet
$$
L=(S,F,B,G,E,T,M,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the unbounded vertical-strip row, displaying the two boundary lines and the exhaustion windows that approximate the strip by bounded sub-strips.
\item $F$ is the $\HolomorphicUp$ map row on the displayed strip, with values in the $\ComplexUp$ carrier.
\item $B$ is the boundary-line readback row, exposing only finite probes on the two boundary lines.
\item $G$ is the growth-control row, recording the finite majorant data allowed along the unbounded direction.
\item $E$ is the bounded-exhaustion row, routing each inspected window through the $\HadamardThreeLinesUp$ strip ledger before any unbounded-strip bound is read.
\item $T$ is the terminal $\RealUp$ bound row produced by the exhaustion schedule.
\item $M$ is the maximum-modulus boundary handoff row, usable only as a displayed local boundary row from \autoref{ch:concrete-instances-maximum-modulus-principle-namecert}.
\item $H$ records componentwise $\hsame$ transport for the strip, map, boundary, growth, exhaustion, bound, handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an unbounded-strip request through $S,F,B,G,E,T,M,H$.
\item $P$ is the $\Pkg$ provenance over $\PhragmenLindelofUp$, $\HadamardThreeLinesUp$, $\HolomorphicUp$, $\ComplexUp$, $\RealUp$, $\MaximumModulusPrincipleUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PhragmenLindelofUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these displayed rows. It has no coordinate for a host supremum, quotient function equality, a selected boundary point, countable choice, open-cover compactness, or an ambient maximum principle.
\end{definition}
\leanstmt{BEDC.Derived.PhragmenLindelofUp}

\begin{theorem}[Phragmen--Lindelof NameCert obligations]
\label{thm:phragmen-lindelof-namecert-obligations}
For every accepted $\PhragmenLindelofUp$ carrier $L=(S,F,B,G,E,T,M,H,C,P,N)$, the local $\NameCert_{\PhragmenLindelofUp}$ surface is exhausted by unbounded-strip admission, holomorphic-map exposure, boundary-line readback, growth-control display, bounded-exhaustion routing, terminal $\RealUp$ bound production, maximum-modulus boundary handoff, componentwise transport, displayed replay, provenance, and local naming. A consumer of the packet may not infer a host maximum principle, a selected boundary supremum, open-cover compactness, quotient function equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:phragmen-lindelof-carrier}. The rows $S,F,B,G$ display the unbounded strip, holomorphic source, boundary reads, and growth-control data before an estimate can be consumed.

The exhaustion is finite at each read. The row $E$ sends every inspected window through the bounded-strip route of \autoref{ch:concrete-instances-hadamard-three-lines-namecert}, and $T$ records the resulting $\RealUp$ bound for the displayed schedule.

Finally $M,H,C,P,N$ provide only the local boundary handoff, transport, replay, provenance, and naming rows. Since none of these rows adds selected suprema, host maximum principles, open-cover compactness, quotient function equality, countable choice, or ambient Real completeness, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Phragmen--Lindelof strip boundary handoff]
\label{thm:phragmen-lindelof-strip-boundary-handoff}
Every unbounded-strip consumer of an accepted $\PhragmenLindelofUp$ carrier factors through
$$
\HolomorphicUp \longmapsto \ComplexUp \longmapsto \HadamardThreeLinesUp \longmapsto \RealUp
$$
together with the displayed rows $S,F,B,G,E,T,M,H,C,P,N$. The handoff exports a finite exhaustion ledger for strip growth control; it does not export a host maximum principle, a selected extremal boundary point, an open-cover compactness argument, or quotient equality of holomorphic sources.
\end{theorem}
\begin{proof}
Read the strip source first. The packet exposes the unbounded-strip row $S$ and the $\HolomorphicUp$ map row $F$ over the $\ComplexUp$ carrier before any boundary estimate is available.

Next open the boundary and growth rows. The boundary-line row $B$ supplies finite probes, while $G$ supplies the displayed growth majorant data that permits the bounded-window exhaustion to be read.

The handoff is then forced through the bounded companion. The row $E$ routes each inspected window through the $\HadamardThreeLinesUp$ ledger, and $T$ seals the resulting bound as a $\RealUp$ row for the same displayed exhaustion schedule.

The remaining rows preserve locality. The boundary handoff $M$, transport $H$, replay $C$, provenance $P$, and local naming $N$ do not introduce a selected supremum, host maximum principle, open-cover compactness, quotient function equality, or ambient completeness assertion.
\end{proof}

\falsifiablePrediction{An accepted $\PhragmenLindelofUp$ read is refuted if the bounded-exhaustion row $E$ is preserved while the displayed unbounded-strip row $S$, holomorphic map row $F$, boundary-line row $B$, growth-control row $G$, and terminal $\RealUp$ bound row $T$ are removed.}

\independenceWitness{hadamard-three-lines, holomorphic, complex, real, maximum-modulus-principle: none is bijective with $\PhragmenLindelofUp$ because the packet jointly carries an unbounded-strip row, holomorphic source row, finite boundary probes, growth-control data, bounded-exhaustion routing, terminal real bound, local maximum-modulus boundary handoff, transport, replay, provenance, and local naming.}

\closureat{PhragmenLindelofUp}{seedStr}

\begin{closurestatus}{\PhragmenLindelofUp}
  \constructivestory{A $\PhragmenLindelofUp$ packet is generated from an unbounded vertical strip, a holomorphic map row, boundary-line probes, growth-control data, bounded-window exhaustion through HadamardThreeLines, a terminal Real bound, local maximum-modulus boundary handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:phragmen-lindelof-carrier}, \autoref{thm:phragmen-lindelof-namecert-obligations}, and \autoref{thm:phragmen-lindelof-strip-boundary-handoff} seed the unbounded-strip growth-control packet over HadamardThreeLines, Holomorphic, Complex, Real, and MaximumModulusPrinciple rows.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host maximum principles, selected boundary suprema, open-cover compactness, quotient function equality, countable choice, ambient $\RealUp$ completeness, checked theorem obligations, or bridge checking.}
  \upgradepath{Obligation closure requires checked unbounded-strip admission, holomorphic-map exposure, boundary-line readback, growth-control display, bounded-exhaustion routing, terminal Real bound production, maximum-modulus boundary handoff, and structural transport for BEDC.Derived.PhragmenLindelofUp.}
\end{closurestatus}
