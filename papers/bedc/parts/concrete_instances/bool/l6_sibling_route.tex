The $\BoolUp$ endpoint surface supplies the two visible rows consumed by the L6 finite-data constructor lattice. The nullable branch of \autoref{ch:concrete-instances-option-namecert}, the tagged alternatives of \autoref{ch:concrete-instances-sum-namecert}, the paired constructor route of \autoref{ch:concrete-instances-prod-namecert}, and the finite spine classifier of \autoref{ch:concrete-instances-list-namecert} all read those endpoints as displayed discriminator rows. The route exports only the two endpoint marks and their history classifier, leaving payload carriers, product coordinates, coproduct injections, and list recursion to the corresponding sibling packets.
