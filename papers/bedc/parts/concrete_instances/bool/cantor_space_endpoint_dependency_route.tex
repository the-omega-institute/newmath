The $\CantorSpaceUp$ finite-prefix surface consumes $\BoolUp$ as the finite
endpoint ledger for each $\StreamNameUp$ prefix window. The endpoint rows read
through \autoref{def:bool-public-endpoint-constructors},
\autoref{thm:bool-public-constructor-case-readback}, and
\autoref{thm:bool-cantor-space-endpoint-route} supply only the empty and
one-step Boolean histories attached to a displayed prefix coordinate.

Thus the dependency into \autoref{ch:concrete-instances-cantorspace-namecert}
is a finite endpoint dependency: $\CantorSpaceUp$ owns the prefix cylinder,
stream schedule, list-indexed window, and local NameCert route, while
$\BoolUp$ supplies the two visible endpoint rows consumed at each inspected
coordinate. The route does not export host Boolean predicates, quotient stream
equality, branch selection, fan-theorem compactness, open-cover compactness, or
ambient product completeness.
