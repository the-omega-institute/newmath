\begin{proposition}[Bool finite-data dependency lattice]
\label{prop:bool-finite-data-dependency-lattice}
The $\BoolUp$ endpoint discriminator rows are consumed as finite-data tags by the sibling constructors of \autoref{ch:concrete-instances-option-namecert}, \autoref{ch:concrete-instances-sum-namecert}, \autoref{ch:concrete-instances-prod-namecert}, and \autoref{ch:concrete-instances-list-namecert}, while equality and readback routes pass through the finite classifier boundary of \autoref{ch:concrete-instances-eqtype-namecert}. Thus the Bool chapter exposes a concrete sibling lattice route: absent/present, left/right, component-presence, list-step, and equality-facing consumers all read the same two endpoint rows before their own payload rows are inspected.
\end{proposition}
