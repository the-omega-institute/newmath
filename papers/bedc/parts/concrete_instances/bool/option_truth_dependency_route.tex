The $\BoolUp$ truth rows feed the constructor-disjointness and finite-data routes of $\OptionUp$ as the displayed absent/present discriminator cited in \autoref{ch:concrete-instances-option-namecert}. The dependency uses only the two endpoint histories, their local classifier, componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\BoolUp}$ row, while payload carriers, nullable classifiers, and option equality remain owned by the $\OptionUp$ packet.
