\chapter{A Concrete Naming Certificate for $\PringsheimDoubleSequenceUp$}
\label{ch:concrete-instances-pringsheim-double-sequence-namecert}
\origin{human}

Following Pringsheim and Hardy on double sequences and double series, the $\PringsheimDoubleSequenceUp$ packet records rectangular-window convergence as finite NameCert data rather than as an ambient selected double limit. It sits after the finite stream rows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the real seal of \autoref{ch:concrete-instances-real-namecert}, the Cauchy sequence-space route of \autoref{ch:concrete-instances-cauchysequencespace-namecert}, and the diagonal cofinal-tail route of \autoref{ch:concrete-instances-diagonalcofinaltail-namecert}. The carrier is the traditional Pringsheim rectangular-window object reconstructed over the Phase Real and completion route; it does not assert quotient streams, selected double limits, host real equality, countable choice, a general iterated-limit interchange theorem, or ambient $\RealUp$ completeness.

\begin{definition}[Pringsheim double-sequence carrier]
\label{def:pringsheim-double-sequence-carrier}
A $\PringsheimDoubleSequenceUp$ carrier is a finite $\BHist$ packet
$$
D=(I,J,W,T,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ and $J$ are unary index schedules for the two Pringsheim directions.
\item $W$ is the rectangular finite $\StreamNameUp$ window row, displaying exactly the pairs inspected below a requested rectangle.
\item $T$ is the $\DyadicRatCoreUp$ tolerance row, including the displayed side bounds, dyadic slack, and rectangle-tail budget.
\item $Q$ is the $\RegSeqRatUp$ readback row for the rectangular observations and tolerance requests.
\item $E$ is the $\RealUp$ seal row, visible only after the schedules, window, tolerance, and regular readback rows have been displayed.
\item $H$ is componentwise $\hsame$ transport for the schedules, windows, tolerance, readback, seal, replay, provenance, and local naming rows.
\item $C$ is displayed $\Cont$ replay from index requests to rectangular windows, from windows to dyadic tolerances, and from tolerances to regular readbacks and the real seal.
\item $P$ is the $\Pkg$ provenance over $\PringsheimDoubleSequenceUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\CauchySequenceSpaceUp$, $\DiagonalCofinalTailUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PringsheimDoubleSequenceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,J,W,T,Q,E,H,C,P,N$. It contains no coordinate for quotient streams, selected double limits, host real equality, countable choice, general iterated-limit interchange, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.PringsheimDoubleSequenceUp.PringsheimDoubleSequenceTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Pringsheim double-sequence NameCert obligations]
\label{thm:pringsheim-double-sequence-namecert-obligations}
For every accepted $\PringsheimDoubleSequenceUp$ carrier $D=(I,J,W,T,Q,E,H,C,P,N)$, the local $\NameCert_{\PringsheimDoubleSequenceUp}$ surface consists exactly of carrier admission for the two unary schedules, rectangular window, dyadic tolerance, regular readback, real seal, transport, replay, provenance, and local naming rows; rectangular-window exactness through $I,J,W$; tolerance exactness through $T$; regular readback through $Q$; real-seal nonescape through $E$; and structural preservation through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:pringsheim-double-sequence-carrier}. The tuple lists all rows available to a certificate consumer, so carrier admission is componentwise over the displayed schedules, rectangular windows, tolerances, readbacks, seal, and structural rows.

The dependency order is exhausted from left to right. A request first exposes $I$ and $J$, then the finite rectangle $W$, then the dyadic budget $T$, and only then the regular rational readback $Q$ and real seal $E$.

The structural rows preserve that route. Componentwise transport, continuation replay, provenance, and local naming cannot introduce quotient streams, selected double limits, host real equality, choice, iterated-limit interchange, or ambient completeness because no such row is present in $D$.
\end{proof}

\begin{theorem}[Pringsheim double-sequence rectangular-window exactness]
\label{thm:pringsheim-double-sequence-rectangular-window-exactness}
Every accepted rectangular-window read from $D=(I,J,W,T,Q,E,H,C,P,N)$ factors through the two unary schedules $I,J$ and the finite $\StreamNameUp$ rectangle $W$ before it can consume a $\DyadicRatCoreUp$ tolerance row, a $\RegSeqRatUp$ readback row, or a $\RealUp$ seal. The read exports the displayed rectangle route and not a quotient stream, selected double limit, host real equality, countable-choice selector, or general iterated-limit interchange theorem.
\end{theorem}
\begin{proof}
Start with a rectangular-window read. The only index data available are the unary schedules $I$ and $J$, so the read cannot inspect a pair before both one-dimensional schedules have been displayed.

The window row $W$ then fixes the finite rectangle. The tolerance row $T$ may be read only for that displayed rectangle, and $Q$ repacks the same observations as regular rational data.

Finally $E,H,C,P,N$ seal, transport, replay, source, and name the same route. Since the carrier has no quotient, selector, host-equality, choice, interchange, or completeness coordinate, the rectangular-window route is exact at the finite packet boundary.
\end{proof}

\begin{theorem}[Pringsheim double-sequence Real-seal handoff]
\label{thm:pringsheim-double-sequence-real-seal-handoff}
For every accepted $\PringsheimDoubleSequenceUp$ packet $D=(I,J,W,T,Q,E,H,C,P,N)$, any completion-facing or $\RealUp$-facing consumer may cite only the finite route
$$
I,J,W \longmapsto T \longmapsto Q \longmapsto E,H,C,P,N .
$$
The handoff records a rectangular-window Pringsheim seal for the Phase Real and completion route; it does not supply ambient Real completeness, selected double limits, quotient stream equality, host real equality, countable choice, or a general iterated-limit interchange theorem.
\end{theorem}
\begin{proof}
Apply \autoref{thm:pringsheim-double-sequence-namecert-obligations}. It identifies the seal as downstream of the schedules, rectangular window, tolerance row, and regular readback.

Apply \autoref{thm:pringsheim-double-sequence-rectangular-window-exactness}. The completion-facing route remains tied to the same finite rectangle and dyadic budget before the regular handoff and seal are read.

Thus every Real-facing consumer receives the displayed finite route through $E,H,C,P,N$. The refused analytic and quotient objects have no carrier coordinate and cannot be exported by the handoff.
\end{proof}

\falsifiablePrediction{Every accepted $\PringsheimDoubleSequenceUp$ completion-facing read exposes two unary index schedules and a rectangular finite StreamName window before any DyadicRatCore tolerance row, RegSeqRat readback, or RealUp seal is consumed.}

\independenceWitness{streamname, dyadicratcore, regseqrat, real, cauchysequencespace, diagonalcofinaltail: $\PringsheimDoubleSequenceUp$ is not bijective to any listed sibling because it jointly carries two unary schedules, rectangular windows, dyadic tolerance rows, regular readbacks, a Real seal, transport, replay, provenance, and local naming.}

\closureat{PringsheimDoubleSequenceUp}{seedStr}

\begin{closurestatus}{\PringsheimDoubleSequenceUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:pringsheim-double-sequence-carrier}, \autoref{thm:pringsheim-double-sequence-namecert-obligations}, \autoref{thm:pringsheim-double-sequence-rectangular-window-exactness}, and \autoref{thm:pringsheim-double-sequence-real-seal-handoff} seed the finite rectangular-window double-sequence route over schedules, StreamName windows, DyadicRatCore tolerances, RegSeqRat readbacks, and RealUp sealing.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient streams, selected double limits, host real equality, countable choice, general iterated-limit interchange, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, rectangular-window exactness, dyadic tolerance handoff, RegSeqRat readback, Real seal nonescape, and structural transport for BEDC.Derived.PringsheimDoubleSequenceUp.}
  \constructivestory{A $\PringsheimDoubleSequenceUp$ packet is generated from two unary index schedules, rectangular StreamName windows, DyadicRatCore tolerances, RegSeqRat readbacks, a RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
