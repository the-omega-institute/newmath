\chapter{A Concrete Naming Certificate for $\BishopLocatedRealUp$}
\label{ch:concrete-instances-bishop-located-real-namecert}
\origin{human}

From Bishop--Bridges constructive real analysis, the $\BishopLocatedRealUp$ packet records the finite located-real interface consumed by L10 Real and Completion routes. It sits after the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the located interval surface of \autoref{ch:concrete-instances-locatedinterval-namecert}, the apartness evidence surface of \autoref{ch:concrete-instances-apartnessreal-namecert}, the located Cauchy route of \autoref{ch:concrete-instances-bishop-located-cauchy-real-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet supplies a displayed located-cut boundary for later completion and order consumers; it is not host real equality, quotient-class equality, trichotomy, countable choice, a selected limit, or ambient $\RealUp$ completeness.

\begin{definition}[BishopLocatedReal carrier]
\label{def:bishop-located-real-carrier}
A $\BishopLocatedRealUp$ carrier is a finite $\BHist$ packet
$$
L=(D,S,R,I,A,B,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, carrying the rational cells, dyadic split radii, slack rows, and comparison budget used by every located read.
\item $S$ is the $\StreamNameUp$ finite-window row inspected at the dyadic tolerances recorded in $D$.
\item $R$ is the $\RegSeqRatUp$ regular rational readback obtained from the same stream windows and tolerance ledger.
\item $I$ is the $\LocatedIntervalUp$ row naming the finite interval enclosure in which the located real may be inspected.
\item $A$ is the $\ApartnessRealUp$ positive-separation row available only when a displayed window exhibits a strict gap from one side of the located cut.
\item $B$ is the $\BishopLocatedCauchyRealUp$ row binding the located interval and regular Cauchy readback to one shared dyadic tolerance route.
\item $E$ is the terminal $\RealUp$ seal reached only after $D,S,R,I,A,B$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the dyadic ledger, stream windows, regular readback, located interval, apartness row, located Cauchy row, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a located-cut request through $D,S,R,I,A,B,E,H$.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedRealUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\LocatedIntervalUp$, $\ApartnessRealUp$, $\BishopLocatedCauchyRealUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopLocatedRealUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these eleven rows. It has no coordinate for host real equality, quotient classes, trichotomy, countable choice, selected representatives, or an ambient completeness assertion.
\end{definition}

\begin{theorem}[BishopLocatedReal NameCert obligations]
\label{thm:bishop-located-real-namecert-obligations}
For every accepted $\BishopLocatedRealUp$ carrier $L=(D,S,R,I,A,B,E,H,C,P,N)$, the local $\NameCert_{\BishopLocatedRealUp}$ surface consists exactly of carrier admission, dyadic tolerance admission, finite StreamName window admission, RegSeqRat readback, located interval admission, apartness-row admission, Bishop located Cauchy admission, Real seal admission, componentwise transport, displayed replay, provenance, and local naming for the displayed rows $D,S,R,I,A,B,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the accepted packet. The analytic rows are precisely the dyadic tolerance ledger $D$, the finite stream windows $S$, the regular rational readback $R$, the located interval row $I$, the apartness row $A$, the located Cauchy row $B$, and the terminal Real seal $E$.

The local certificate reads these rows in the displayed order. A located request may inspect endpoint or cut information only after the shared tolerance and window rows are visible, and a Real-facing request reaches $E$ only after the regular readback, located interval, apartness, and located Cauchy rows have been displayed.

Transport and replay remain componentwise. The rows $H,C,P,N$ transport, replay, source, and name the same finite packet, so no outside equality, choice principle, selected representative, trichotomy split, or ambient completeness datum can enter the local surface.
\end{proof}

\begin{theorem}[BishopLocatedReal RegSeqRat route]
\label{thm:bishop-located-real-regseqrat-route}
Every completion-facing read of an accepted $\BishopLocatedRealUp$ carrier factors through the route
$$
\DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp
\longmapsto
\BishopLocatedCauchyRealUp
\longmapsto
\RealUp .
$$
The route exposes a dyadic tolerance ledger, a finite stream window, a regular rational readback, a located Cauchy handoff, componentwise transport, replay, provenance, and local naming before any terminal real seal is consumed.
\end{theorem}
\begin{proof}
Begin with \autoref{def:bishop-located-real-carrier}. The only regular rational source row is $R$, and $R$ is downstream of the dyadic ledger $D$ and the finite stream window $S$.

The located Cauchy row $B$ is admitted only as a row of the same packet. It binds the regular readback to the displayed located interval data before the terminal Real seal $E$ can be read.

Apply \autoref{thm:bishop-located-real-namecert-obligations}. The certificate forces every completion-facing read through $D,S,R,B,E,H,C,P,N$, and the carrier contains no row for quotient streams, selected limits, countable choice, host real equality, or ambient $\RealUp$ completeness.
\end{proof}

\begin{theorem}[BishopLocatedReal located-cut stability]
\label{thm:bishop-located-real-located-cut-stability}
Let $L=(D,S,R,I,A,B,E,H,C,P,N)$ and $L'=(D',S',R',I',A',B',E',H',C',P',N')$ be accepted $\BishopLocatedRealUp$ carriers. If $H$ records componentwise $\hsame$ transport from each displayed row of $L$ to the corresponding row of $L'$, then the located-cut information of $L$ transports to $L'$ only through
$$
D,S,R,I,A,B,E,H,C,P,N .
$$
In particular, located interval containment, positive apartness evidence, and located Cauchy compatibility remain attached to the same finite StreamName, RegSeqRat, and DyadicRatCore route.
\end{theorem}
\begin{proof}
Project both accepted carriers. The located-cut rows are $I,A,B$: the interval row $I$ records the finite enclosure, the apartness row $A$ records positive gap evidence, and the located Cauchy row $B$ binds those observations to the regular readback.

Componentwise transport through $H$ carries $D$ to $D'$, $S$ to $S'$, $R$ to $R'$, and then $I,A,B$ to $I',A',B'$. The replay row $C$ cannot change the order of these reads; it only replays the displayed route.

The NameCert obligations of \autoref{thm:bishop-located-real-namecert-obligations} then repack the transported rows as an accepted located-cut surface for $L'$. Since the packet has no host equality, trichotomy, quotient, choice, or completeness coordinate, stability is exactly the displayed row-local transport.
\end{proof}

\begin{theorem}[BishopLocatedReal Real-seal boundary]
\label{thm:bishop-located-real-real-seal-boundary}
Every $\RealUp$-facing read exported by an accepted $\BishopLocatedRealUp$ carrier is exhausted by the displayed rows $D,S,R,I,A,B,E,H,C,P,N$. Thus the public real-name boundary supplies only dyadic tolerance data, finite stream windows, RegSeqRat readback, located interval evidence, ApartnessReal separation rows, BishopLocatedCauchyReal compatibility, the terminal Real seal, transport, replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Read the carrier of \autoref{def:bishop-located-real-carrier}. The terminal Real row is $E$, and $E$ appears only after the dyadic, stream, regular rational, located interval, apartness, and located Cauchy rows.

The RegSeqRat route of \autoref{thm:bishop-located-real-regseqrat-route} keeps every completion-facing read downstream of $D,S,R,B$. The stability result of \autoref{thm:bishop-located-real-located-cut-stability} keeps located interval and apartness reads on the same displayed route.

Finally, \autoref{thm:bishop-located-real-namecert-obligations} supplies $H,C,P,N$ and excludes all non-displayed data. Therefore no consumer can read host real equality, quotient classes, trichotomy, countable choice, a selected representative, or ambient $\RealUp$ completeness from the Real-seal boundary.
\end{proof}

\closureat{BishopLocatedRealUp}{seedStr}

\begin{closurestatus}{\BishopLocatedRealUp}
  \constructivestory{A $\BishopLocatedRealUp$ packet is generated from a DyadicRatCore tolerance ledger, StreamName finite windows, RegSeqRat readback, a LocatedInterval row, an ApartnessReal row, a BishopLocatedCauchyReal row, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-located-real-carrier}, \autoref{thm:bishop-located-real-namecert-obligations}, \autoref{thm:bishop-located-real-regseqrat-route}, \autoref{thm:bishop-located-real-located-cut-stability}, and \autoref{thm:bishop-located-real-real-seal-boundary} seed the finite located-real interface over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\LocatedIntervalUp$, $\ApartnessRealUp$, $\BishopLocatedCauchyRealUp$, and $\RealUp$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim host real equality, quotient classes, trichotomy, countable choice, selected representatives, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, RegSeqRat route exactness, located-cut transport, Real seal non-escape, transport, replay, provenance, and local naming for BEDC.Derived.BishopLocatedRealUp.}
\end{closurestatus}
