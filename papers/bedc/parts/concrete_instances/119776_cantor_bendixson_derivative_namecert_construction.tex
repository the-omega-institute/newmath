\chapter{A Concrete Naming Certificate for $\CantorBendixsonDerivativeUp$}
\label{ch:concrete-instances-cantor-bendixson-derivative-namecert}
\origin{human}

Following Cantor and Bendixson, the $\CantorBendixsonDerivativeUp$ packet records one finite derivative step for a displayed closed real-set row before any rank, located, or decomposition-facing consumer reads it. It sits between the broader $\CantorBendixsonUp$ packet of \autoref{ch:concrete-instances-cantor-bendixson-namecert}, the located derivative route of \autoref{ch:concrete-instances-located-cantor-bendixson-namecert}, and the finite rank budget of \autoref{ch:concrete-instances-cantor-bendixson-rank-namecert}. The carrier also cites the closed-set boundary of \autoref{ch:concrete-instances-closedset-namecert}, the stream-name windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic comparison core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the real-name seal of \autoref{ch:concrete-instances-real-namecert}. It names only a finite derivative-step operation on displayed data; it does not claim a transfinite rank, a full scattered-perfect decomposition, a selected enumeration of isolated points, quotient closed-set equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Cantor--Bendixson derivative carrier]
\label{def:cantor-bendixson-derivative-carrier}
A $\CantorBendixsonDerivativeUp$ carrier is a finite $\BHist$ packet
$$
D=(F,A,I,F',W,R,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the input $\ClosedSetUp$ row, recording the closed real-set observations whose derivative is being inspected.
\item $A$ is the finite accumulation-test row, listing the displayed neighborhood and non-isolation tests used to decide whether an inspected point remains in the derivative row.
\item $I$ is the isolated-rejection row, recording only displayed separating intervals, dyadic radii, and membership failures that remove a point from the next derivative row.
\item $F'$ is the output $\ClosedSetUp$ row, read as the derivative-step closed set produced from $F,A,I$.
\item $W$ is the $\StreamNameUp$ window family through which the input row, accumulation tests, isolated rejections, and output row are exposed to finite consumers.
\item $R$ is the $\RegSeqRatUp$ readback row for the same windows and stage-local derivative observations.
\item $Q$ is the $\DyadicRatCoreUp$ ledger for the radii, interval separations, containment checks, and neighborhood comparisons used by $F,A,I,F',W,R$.
\item $E$ is the terminal $\RealUp$ seal boundary for the displayed real-name reads.
\item $H$ records componentwise $\hsame$ transport for $F,A,I,F',W,R,Q,E$ and for the replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of every derivative-step request through
$$
F,A,I \longmapsto F' \longmapsto W,R,Q,E .
$$
\item $P$ is the $\Pkg$ provenance over $\CantorBendixsonDerivativeUp$, $\CantorBendixsonUp$, $\LocatedCantorBendixsonUp$, $\CantorBendixsonRankUp$, $\ClosedSetUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CantorBendixsonDerivativeUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these rows. It has no coordinate for transfinite derivative chains, selected isolated-point enumerations, quotient closed-set equality, countable choice, host closed-set equality, or ambient completeness outside the displayed real-name route.
\end{definition}

\begin{theorem}[Cantor--Bendixson derivative NameCert obligations]
\label{thm:cantor-bendixson-derivative-namecert-obligations}
For every accepted $\CantorBendixsonDerivativeUp$ carrier
$$
D=(F,A,I,F',W,R,Q,E,H,C,P,N),
$$
the local $\NameCert_{\CantorBendixsonDerivativeUp}$ surface is exhausted by the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed input closed set, accumulation tests, isolated-rejection row, output closed set, StreamName windows, RegSeqRat readback, DyadicRatCore comparisons, Real seal, transport, replay, provenance, and local naming rows;
\item derivative-step classifier stability, requiring componentwise $\hsame$ transport to preserve the same input row, tested neighborhoods, rejected isolated rows, and output row;
\item isolated-rejection exactness, requiring each removed point to carry a displayed separating interval, dyadic radius, and membership check inside $I$ and $Q$;
\item output closed-set handoff, requiring $F'$ to be downstream of $F,A,I$ before any $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, or $\RealUp$ consumer reads it;
\item structural nonescape, requiring $W,R,Q,E,H,C,P,N$ to export no transfinite rank, full decomposition, hidden selector, quotient equality, countable-choice schedule, or ambient closed-set theorem.
\end{enumerate}
No derivative-step obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cantor-bendixson-derivative-carrier}. The packet displays exactly the input closed-set row, accumulation tests, isolated-rejection row, output closed-set row, observation windows, rational readback, dyadic comparison ledger, Real seal, and structural rows.

The derivative work is a single finite step. Row $A$ records the inspected neighborhoods, while $I$ records the displayed isolating data that prevents a rejected point from remaining in $F'$.

The output row is therefore read only after the input and rejection rows have been displayed. The windows $W$, readback $R$, dyadic ledger $Q$, and Real seal $E$ expose that same step to finite consumers without adding a further derivative stage.

Finally $H,C,P,N$ transport, replay, source, and name those rows. Since the carrier has no coordinate for transfinite chains, selected enumerations, quotient closed-set equality, countable choice, or ambient completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Cantor--Bendixson derivative closed-set handoff]
\label{thm:cantor-bendixson-derivative-closed-set-handoff}
Every accepted $\CantorBendixsonDerivativeUp$ consumer that asks for the derivative-step closed set must first display the route
$$
F,A,I \longmapsto F' \longmapsto W,R,Q,E \longmapsto H,C,P,N .
$$
Thus the exported closed-set row $F'$ is a finite BEDC handoff over the $\ClosedSetUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ surfaces. The consumer receives no host transfinite derivative chain, no selected list of isolated points, no quotient closed-set equality, no countable-choice schedule, and no ambient compactness or completeness theorem.
\end{theorem}
\begin{proof}
Begin with the input and test rows. The closed-set row $F$ and accumulation-test row $A$ are the only data from which a derivative-step read may decide whether an inspected point remains.

Next read the isolated-rejection row. Each rejection in $I$ is accompanied by the displayed interval, radius, and membership data recorded through the dyadic comparison ledger $Q$.

The output closed-set row $F'$ is downstream of those rows. The StreamName windows, RegSeqRat readback, DyadicRatCore comparisons, and Real seal expose $F'$ only through the same finite route.

The transport, replay, provenance, and local naming rows preserve that route and add no mathematical coordinate for the excluded host-level data. Hence the closed-set handoff factors through the displayed derivative-step packet.
\end{proof}

\falsifiablePrediction{Every accepted $\CantorBendixsonDerivativeUp$ consumer that reads the output closed-set row while omitting the input closed set, accumulation tests, isolated-rejection data, stream windows, rational readback, dyadic comparisons, Real seal, transport, replay, provenance, or local name has left the packet.}

\independenceWitness{cantorbendixson, locatedcantorbendixson, cantorbendixsonrank, closedset, streamname, regseqrat, dyadicratcore, real: $\CantorBendixsonDerivativeUp$ is not bijective with any listed sibling because it isolates one derivative-step operator with input and output closed-set rows, accumulation tests, isolated rejections, finite windows, arithmetic readback, transport, replay, provenance, and local naming as one finite packet.}

\closureat{CantorBendixsonDerivativeUp}{seedStr}

\begin{closurestatus}{\CantorBendixsonDerivativeUp}
  \constructivestory{A $\CantorBendixsonDerivativeUp$ packet is generated from an input ClosedSetUp row, finite accumulation tests, isolated-rejection data, an output ClosedSetUp row, StreamName windows, RegSeqRat readback, DyadicRatCore comparisons, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cantor-bendixson-derivative-carrier}, \autoref{thm:cantor-bendixson-derivative-namecert-obligations}, and \autoref{thm:cantor-bendixson-derivative-closed-set-handoff} seed the finite one-step derivative operator for closed real-set packets.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close the full Cantor--Bendixson decomposition, host ordinal rank, selected isolated-point enumeration, quotient closed-set equality, countable choice, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, derivative-step classifier stability, isolated-rejection exactness, output closed-set handoff, structural nonescape, and route replay for BEDC.Derived.CantorBendixsonDerivativeUp.}
\end{closurestatus}
