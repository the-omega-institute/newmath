\chapter{A Concrete Naming Certificate for $\DyadicIntervalNestingUp$}
\label{ch:concrete-instances-dyadic-interval-nesting-namecert}
\origin{human}

Following Bishop and Bridges' constructive real analysis, the $\DyadicIntervalNestingUp$ packet names the finite interval-nesting route that turns dyadic endpoint data into a regular rational real-name boundary. It sits between the endpoint ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational handoff of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal seal of \autoref{ch:concrete-instances-real-namecert}, and the finite nested-chain sibling of \autoref{ch:concrete-instances-nesteddyadicinterval-namecert}. The packet records a constructive nesting route for L10 Real and Completion work; it does not assert an infinite intersection point, quotient stream equality, host real equality, countable choice, or ambient completeness of $\RealUp$.

\begin{definition}[DyadicIntervalNesting carrier]\label{def:dyadic-interval-nesting-carrier}
A $\DyadicIntervalNestingUp$ carrier is a finite $\BHist$ packet
$$
J=(D,S,R,E,I,H,C,P,N)
$$
with five displayed NameCert source rows and four structural rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ endpoint and width ledger for the displayed dyadic intervals, including lower endpoint, upper endpoint, and dyadic comparison reads.
\item $S$ is the $\StreamNameUp$ finite-observation schedule naming the interval-prefix windows that may be opened by a consumer.
\item $R$ is the $\RegSeqRatUp$ handoff row obtained from scheduled endpoint or midpoint reads after the dyadic width ledger has answered the current tolerance request.
\item $E$ is the $\RealUp$ seal row reached only after $D,S,R$ and the nesting row have been displayed.
\item $I$ is the existing $\NestedDyadicIntervalUp$ sibling row, carrying the finite decreasing-chain witness and adjacent refinement ledger.
\item $H$ stores componentwise $\hsame$ transport for the dyadic endpoint ledger, stream schedule, regular rational handoff, real seal, nested-chain row, continuation row, provenance row, and local naming row.
\item $C$ stores the $\Cont$ replay from a finite interval-prefix request through $I,D,S,R,E$ and then through the structural rows.
\item $P$ is the $\Pkg$ provenance over $\DyadicIntervalNestingUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\NestedDyadicIntervalUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DyadicIntervalNestingUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,S,R,E,I,H,C,P,N$. The carrier has no coordinate for completed intersections, quotient real names, host real equality, selected limits, countable choice, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.DyadicIntervalNestingUp.DyadicIntervalNestingTasteGate\_single\_carrier\_alignment}

\begin{theorem}[DyadicIntervalNesting NameCert obligations]\label{thm:dyadic-interval-nesting-namecert-obligations}
For every accepted $\DyadicIntervalNestingUp$ carrier $J=(D,S,R,E,I,H,C,P,N)$, the local $\NameCert_{\DyadicIntervalNestingUp}$ surface consists exactly of the following five rows:
\begin{enumerate}
\item dyadic endpoint admission through $D$, requiring every interval endpoint and width comparison to be read from $\DyadicRatCoreUp$;
\item finite observation scheduling through $S$, requiring every public interval-prefix read to be one of the displayed $\StreamNameUp$ windows;
\item regular rational handoff through $R$, requiring the $\RegSeqRatUp$ row to be formed only after the endpoint ledger and finite schedule have been projected;
\item real seal nonescape through $E$, requiring every $\RealUp$ read to pass through $D,S,R$ and the nested-chain row $I$;
\item sibling nesting compatibility through $I$, requiring the displayed interval chain to be the finite $\NestedDyadicIntervalUp$ refinement surface before any regular rational or real-name consumer reads it.
\end{enumerate}
The structural rows $H,C,P,N$ transport, replay, source, and name these five rows and add no further mathematical coordinate.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-interval-nesting-carrier}. The five mathematical source rows are precisely $D,S,R,E,I$, and each is named by a concrete sibling chapter in the L10 Real and Completion path.

The dyadic endpoint row supplies the lower endpoint, upper endpoint, and width comparison data. The stream row restricts all public reads to finite interval-prefix windows, so no hidden tail can be inspected.

The regular rational handoff is downstream of those two rows. It repacks scheduled endpoint or midpoint observations only after the dyadic tolerance ledger has selected the displayed comparison used by the current request.

The nested-chain row $I$ supplies the adjacent refinement ledger already exposed by $\NestedDyadicIntervalUp$. The real seal $E$ is reached after this finite route, and the structural rows only preserve the same route under $\hsame$ transport and $\Cont$ replay. Since no other coordinate is present, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[DyadicIntervalNesting regular name route]\label{thm:dyadic-interval-nesting-regular-name-route}
Every $\RegSeqRatUp$ or $\RealUp$ consumer of an accepted $\DyadicIntervalNestingUp$ carrier factors through the concrete route
$$
\NestedDyadicIntervalUp \longmapsto \DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
Thus the consumer reads a finite nested dyadic chain, its dyadic endpoint and width ledger, the scheduled finite windows, the regular rational handoff, and finally the real seal. It does not receive an infinite intersection point, quotient stream equality, host real equality, selected limit, countable-choice selector, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Start with the sibling nesting row $I$. It supplies a finite decreasing dyadic chain and adjacent refinement rows, so every downstream read begins with displayed interval data rather than with a completed point.

Read $D$ next. The endpoint and width comparisons used by the nesting route are dyadic rational rows, and all tolerance comparisons are made inside that displayed ledger.

The schedule row $S$ then fixes the finite interval windows available to the consumer. The handoff row $R$ repacks exactly those scheduled reads as a regular rational name, and the seal row $E$ exposes the corresponding $\RealUp$ boundary only after the handoff has occurred.

The rows $H,C,P,N$ transport, replay, source, and name this route. They do not add quotient equality, host equality, choice, a selected limit, or completeness, so every regular-name and real-name consumer remains inside the displayed chain.
\end{proof}

\closureat{DyadicIntervalNestingUp}{seedStr}

\begin{closurestatus}{\DyadicIntervalNestingUp}
  \constructivestory{A $\DyadicIntervalNestingUp$ packet is generated from a finite NestedDyadicInterval row, DyadicRatCore endpoint and width ledgers, StreamName windows, a RegSeqRat handoff, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-interval-nesting-carrier}, \autoref{thm:dyadic-interval-nesting-namecert-obligations}, and \autoref{thm:dyadic-interval-nesting-regular-name-route} seed the finite dyadic interval nesting route from endpoint ledgers and scheduled windows to regular rational readback and the RealUp seal.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.DyadicIntervalNestingUp.DyadicIntervalNestingTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \notclaimed{This chapter does not close infinite interval intersections, quotient stream equality, host real equality, selected limits, countable choice, ambient $\RealUp$ completeness, full Lean verification for every obligation row, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, endpoint ledger exactness, finite-window scheduling, regular rational handoff, sibling nesting compatibility, and Real seal nonescape for BEDC.Derived.DyadicIntervalNestingUp.}
\end{closurestatus}
