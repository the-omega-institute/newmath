\chapter{A Concrete Naming Certificate for $\MartingaleConvergenceWitnessUp$}
\label{ch:concrete-instances-martingale-convergence-witness-namecert}
\origin{human}

Following Doob, the $\MartingaleConvergenceWitnessUp$ packet extracts the finite witness surface by which upcrossing control is consumed by $\MartingaleConvergenceUp$ without importing the whole convergence-preparation packet. It sits after the martingale convergence route of \autoref{ch:concrete-instances-martingale-convergence-namecert}, the upcrossing ledger of \autoref{ch:concrete-instances-upcrossing-namecert}, the Doob bound of \autoref{ch:concrete-instances-doob-upcrossing-namecert}, the bounded stopping route of \autoref{ch:concrete-instances-optionalstopping-namecert}, the finite schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal Real boundary of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite extraction boundary for later martingale convergence consumers; it is not an almost-sure quotient, a selected exceptional set, a completed filtration, a measure-completion object, or an ambient convergence theorem.

\begin{definition}[Martingale convergence witness carrier]
\label{def:martingale-convergence-witness-carrier}
A $\MartingaleConvergenceWitnessUp$ carrier is a finite $\BHist$ packet
$$
W=(M,T,U,D,S,R,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MartingaleUp$ finite adapted-source row over the inspected probability and endpoint data.
\item $T$ is the bounded stopping-window row consumed through $\StoppingTimeUp$ and $\OptionalStoppingUp$.
\item $U$ is the $\UpcrossingUp$ completed-crossing count for the displayed lower and upper boundary rows.
\item $D$ is the $\DoobUpcrossingUp$ inequality row bounding the finite count $U$.
\item $S$ is the $\StreamNameUp$ finite schedule row naming the requested observation windows.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the same finite windows and rational budgets.
\item $L$ is the $\RealUp$ lower and upper boundary row read by the upcrossing and convergence consumers.
\item $H$ records componentwise $\hsame$ transport for the martingale, stopping, upcrossing, Doob-bound, schedule, readback, Real-boundary, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through the same finite route.
\item $P$ is the $\Pkg$ provenance over $\MartingaleConvergenceWitnessUp$, $\MartingaleConvergenceUp$, $\MartingaleUp$, $\StoppingTimeUp$, $\OptionalStoppingUp$, $\UpcrossingUp$, $\DoobUpcrossingUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MartingaleConvergenceWitnessUp}$ row.
\end{enumerate}
The classifier compares accepted witness packets only through these displayed rows and their componentwise transports.
\end{definition}

\begin{theorem}[Martingale convergence witness NameCert obligations]
\label{thm:martingale-convergence-witness-namecert-obligations}
For every accepted $\MartingaleConvergenceWitnessUp$ carrier
$$
W=(M,T,U,D,S,R,L,H,C,P,N),
$$
the local $\NameCert_{\MartingaleConvergenceWitnessUp}$ surface consists exactly of martingale-source exposure through $M$, bounded stopping exposure through $T$, upcrossing-count exposure through $U$, Doob-bound exposure through $D$, finite schedule exposure through $S$, regular rational readback through $R$, Real boundary exposure through $L$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:martingale-convergence-witness-carrier}. Its stochastic source row is $M$, its bounded stopping row is $T$, and its upcrossing witness rows are $U,D$.

Read the schedule and boundary side next. The rows $S,R,L$ are the finite observation schedule, regular rational readback, and Real lower-upper boundary rows already required by \autoref{ch:concrete-instances-martingale-convergence-namecert}.

The structural rows $H,C,P,N$ transport, replay, source, and name exactly this finite packet. Since no coordinate stores an almost-sure quotient, a selected exceptional set, a completed filtration, a measure-completion object, or an ambient convergence theorem, the listed rows exhaust the local certificate.
\end{proof}

\begin{theorem}[Martingale convergence witness upcrossing factorization]
\label{thm:martingale-convergence-witness-upcrossing-factorization}
Every upcrossing read exported by an accepted $\MartingaleConvergenceWitnessUp$ packet factors through
$$
\MartingaleUp,\ \StoppingTimeUp,\ \OptionalStoppingUp,\ \UpcrossingUp,\ \DoobUpcrossingUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp .
$$
In particular, the witness may be consumed by $\MartingaleConvergenceUp$ only after the martingale row, bounded stopping row, completed-crossing count, Doob bound, finite schedule, regular rational readback, and Real boundary rows have all been displayed.
\end{theorem}
\begin{proof}
Begin with the accepted carrier $W=(M,T,U,D,S,R,L,H,C,P,N)$. The rows $M$ and $T$ expose the finite martingale source and bounded stopping window before any crossing count is read.

Apply the sibling upcrossing packets. The row $U$ is the finite completed-count ledger supplied through \autoref{ch:concrete-instances-upcrossing-namecert}, and $D$ is the finite Doob-bound row supplied through \autoref{ch:concrete-instances-doob-upcrossing-namecert}.

Now read the consumer-facing rows. The finite schedule $S$, the regular rational readback $R$, and the Real boundary row $L$ are precisely the rows through which \autoref{ch:concrete-instances-martingale-convergence-namecert} admits a convergence-preparation read.

Finally transport and replay are componentwise through $H$ and $C$, with $P,N$ fixing provenance and local naming. Thus the export route factors through the displayed sibling chapters and cannot bypass them by an ambient limiting event or an almost-sure quotient.
\end{proof}

\begin{theorem}[Martingale convergence witness finite extraction]
\label{thm:martingale-convergence-witness-finite-extraction}
The finite extraction of an accepted $\MartingaleConvergenceWitnessUp$ packet is exactly the subpacket
$$
(M,T,U,D,S,R,L,H,C,P,N)
$$
needed by a $\MartingaleConvergenceUp$ consumer to read a bounded upcrossing witness. The extraction exports no selected limit, no exceptional null set, no completed filtration, no measure completion, no unbounded stopping principle, and no ambient convergence theorem.
\end{theorem}
\begin{proof}
Project the carrier and use \autoref{thm:martingale-convergence-witness-namecert-obligations}. The local NameCert surface admits only the martingale, bounded stopping, upcrossing count, Doob bound, finite schedule, regular rational readback, Real boundary, transport, replay, provenance, and local naming rows.

Use \autoref{thm:martingale-convergence-witness-upcrossing-factorization}. Any convergence-facing read through $\MartingaleConvergenceUp$ receives the witness only along the displayed route through martingale, optional-stopping, upcrossing, Doob-upcrossing, StreamName, RegSeqRat, and Real rows.

The refused objects have no coordinate in the extracted packet and no structural row in $H,C,P,N$. Row exhaustion therefore prevents the extraction from exporting a selected limit, exceptional null set, completed filtration, measure completion, unbounded stopping principle, or ambient convergence theorem.
\end{proof}

\closureat{MartingaleConvergenceWitnessUp}{seedStr}

\begin{closurestatus}{\MartingaleConvergenceWitnessUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:martingale-convergence-witness-carrier}, \autoref{thm:martingale-convergence-witness-namecert-obligations}, \autoref{thm:martingale-convergence-witness-upcrossing-factorization}, and \autoref{thm:martingale-convergence-witness-finite-extraction} seed the finite upcrossing witness boundary consumed by $\MartingaleConvergenceUp$.}
  \formalstatus{\formalTargetV}
  \leanstmt{BEDC.Derived.MartingaleConvergenceWitnessUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close almost-sure quotient equality, selected exceptional sets, completed filtrations, measure completion, selected limits, unbounded stopping principles, Lean theorem verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, bounded stopping exposure, upcrossing-count exactness, Doob-bound consumption, finite schedule readback, Real-boundary routing, transport, replay, provenance, and local naming for BEDC.Derived.MartingaleConvergenceWitnessUp.}
  \constructivestory{A $\MartingaleConvergenceWitnessUp$ packet is generated from martingale, bounded stopping, finite upcrossing, Doob-bound, StreamName schedule, RegSeqRat readback, Real-boundary, transport, replay, provenance, and local NameCert rows.}
\end{closurestatus}
