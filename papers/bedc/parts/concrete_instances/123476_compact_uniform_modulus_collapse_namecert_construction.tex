\chapter{A Concrete Naming Certificate for $\CompactUniformModulusCollapseUp$}
\label{ch:concrete-instances-compact-uniform-modulus-collapse-namecert}
\origin{ai}

The $\CompactUniformModulusCollapseUp$ packet realizes the compact-uniform-continuity route of \autoref{ch:visions-compact-uniform-continuity} as finite NameCert data: a compact source supplies finite net rows, a continuous-map source supplies local radius rows, metric-triangle comparisons fold those rows into one modulus, and the Real seal exposes the resulting tolerance readback. The packet consumes the compact carrier of \autoref{ch:concrete-instances-compact-namecert}, the metric carrier of \autoref{ch:concrete-instances-metric-namecert}, the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}. It names the finite collapse of local compact-continuity evidence into a displayed uniform-modulus row; it is not an open-cover compactness theorem, a host function-space theorem, or a selected subcover principle.

\begin{definition}[Compact uniform modulus collapse carrier]
\label{def:compact-uniform-modulus-collapse-carrier}
A $\CompactUniformModulusCollapseUp$ carrier is a finite $\BHist$ packet
$$
U=(K,M,F,R,G,Q,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactUp$ source row carrying the finite net or finite cover data inspected by the packet.
\item $M$ is the $\MetricUp$ distance row used to compare net centers, source points, and target values.
\item $F$ is the $\ContinuousMapUp$ local continuity row, giving the displayed local radius obligations at the inspected centers.
\item $R$ is the local-radius ledger, pairing every finite net center with the radius row supplied by $F$.
\item $G$ is the folded-modulus row obtained by taking the finite lower-bound fold over the displayed local radii.
\item $Q$ is the metric-triangle row certifying that the folded radius carries arbitrary inspected source points back to the local center rows.
\item $S$ is the $\RegSeqRatUp$ and $\RealUp$ readback route for the resulting tolerance and value windows.
\item $H$ records componentwise $\hsame$ transport for the compact source, metric row, continuous-map row, local-radius ledger, folded modulus, triangle row, readback route, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a compact-uniform request through $K,M,F,R,G,Q,S,H$.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformModulusCollapseUp$, $\CompactUp$, $\MetricUp$, $\ContinuousMapUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformModulusCollapseUp}$ row.
\end{enumerate}
The carrier exports no open-cover compactness theorem, arbitrary subcover selection, host uniform-continuity proof, quotient real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact uniform modulus collapse NameCert obligations]
\label{thm:compact-uniform-modulus-collapse-namecert-obligations}
For every accepted $\CompactUniformModulusCollapseUp$ carrier $U=(K,M,F,R,G,Q,S,H,C,P,N)$, the local $\NameCert_{\CompactUniformModulusCollapseUp}$ surface consists exactly of compact finite-net exposure, metric distance exposure, continuous-map local-radius exposure, finite local-radius ledger formation, folded-modulus extraction, metric-triangle transport, regular rational and Real readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-modulus-collapse-carrier}. The compact, metric, continuous-map, local-radius, folded-modulus, triangle, and readback rows are all displayed coordinates of one finite packet.

The compact source and continuous-map source are read first. The finite net row $K$ supplies only the inspected compact data, while $F$ supplies local radius rows at the named centers.

The collapse step is finite. The row $R$ pairs centers with local radii, $G$ folds those radii into one displayed modulus row, and $Q$ records the metric-triangle route that carries nearby source points back through the chosen center row.

Finally $S,H,C,P,N$ read the tolerance through regular rational and Real rows, transport the packet componentwise, replay the request, record provenance, and name the local certificate. Since no other coordinate exists, the listed rows exhaust the NameCert surface.
\end{proof}

\begin{theorem}[Compact uniform modulus collapse vision readback]
\label{thm:compact-uniform-modulus-collapse-vision-readback}
The $\CompactUniformModulusCollapseUp$ packet realizes the compact-uniform-continuity vision by the finite route
$$
K,M,F,R \longmapsto G,Q \longmapsto S,H,C,P,N .
$$
The route exposes a compact finite-net source, local continuous-map radii, a folded modulus, metric-triangle transport, regular rational readback, Real sealing, replay, provenance, and local naming, and it exports none of the host compactness or function-space principles excluded by \autoref{ch:visions-compact-uniform-continuity}.
\end{theorem}
\begin{proof}
Start with the compact-uniform request. The carrier forces the request to read the compact source row $K$, the metric distance row $M$, and the continuous-map local row $F$ before a modulus row is visible.

The local-radius ledger $R$ and folded-modulus row $G$ then perform the finite collapse. The metric-triangle row $Q$ is the only route from an inspected source point back to a center radius, so the packet does not appeal to an open-cover compactness theorem or selected subcover outside the displayed data.

The readback row $S$ and structural rows $H,C,P,N$ seal and name the same finite route. This is exactly the finite-net, local-radius, folded-modulus, metric-triangle, Real-seal, transport, replay, provenance, and NameCert route required by the vision.
\end{proof}

\falsifiablePrediction{A $\CompactUniformModulusCollapseUp$ classifier is refuted by any compact-uniform request whose finite net and local-radius rows agree but whose folded modulus row fails to dominate one displayed local radius used by the metric-triangle row.}

\independenceWitness{compact\_modulus\_extraction, finite\_subcover\_modulus\_budget, finitenetminimumfold, compact\_interval\_uniform\_continuity: none is bijective to $\CompactUniformModulusCollapseUp$ because this packet jointly carries compact finite-net data, metric comparisons, continuous-map local radii, a folded modulus, a triangle route, RegSeqRat and Real readback, transport, replay, provenance, and local NameCert rows. Lean follow-up instances should include TasteGate Nontrivial and FieldFaithful coverage for this finite collapse surface.}

\closureat{CompactUniformModulusCollapseUp}{seedStr}

\begin{closurestatus}{\CompactUniformModulusCollapseUp}
  \constructivestory{A $\CompactUniformModulusCollapseUp$ packet is generated from compact finite-net data, metric rows, continuous-map local radii, a local-radius ledger, a folded modulus, a metric-triangle row, RegSeqRat and Real readback, componentwise hsame transport, Cont replay, Pkg provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-modulus-collapse-carrier}, \autoref{thm:compact-uniform-modulus-collapse-namecert-obligations}, and \autoref{thm:compact-uniform-modulus-collapse-vision-readback} seed the finite collapse route from compact local radii to a displayed uniform modulus and Real readback.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, arbitrary subcover selection, host uniform-continuity theorems, quotient real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-net exposure, local-radius exactness, folded-modulus domination, metric-triangle routing, Real readback, transport, replay, provenance, local naming, TasteGate Nontrivial, and FieldFaithful follow-up instances for BEDC.Derived.CompactUniformModulusCollapseUp.}
\end{closurestatus}
