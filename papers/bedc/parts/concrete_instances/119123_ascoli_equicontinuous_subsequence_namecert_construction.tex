\chapter{A Concrete Naming Certificate for $\AscoliEquicontinuousSubsequenceUp$}
\label{ch:concrete-instances-ascoli-equicontinuous-subsequence-namecert}
\origin{human}

Following Arzela and Ascoli, the $\AscoliEquicontinuousSubsequenceUp$ packet records the constructive finite-net route by which an equicontinuous family exposes a subsequence certificate without selecting an infinite host subsequence. It sits after the finite-net extraction boundary of \autoref{ch:concrete-instances-arzela-ascoli-finite-net-namecert}, the equicontinuous-family carrier of \autoref{ch:concrete-instances-equicontinuous-family-namecert}, the compact source surface of \autoref{ch:concrete-instances-compact-namecert}, the finite-window stream interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names only the finite diagonal extraction certificate consumed by downstream compactness arguments; it is not open-cover compactness, a host function-space compactness theorem, quotient equality of maps, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Ascoli equicontinuous subsequence carrier]
\label{def:ascoli-equicontinuous-subsequence-carrier}
An $\AscoliEquicontinuousSubsequenceUp$ carrier is a finite $\BHist$ packet
$$
S=(A,E,K,W,R,L,D,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the $\ArzelaAscoliFiniteNetUp$ finite-net extraction row, exposing the finite centers, tolerance budgets, and value windows available to the diagonal read.
\item $E$ is the $\EquicontinuousFamilyUp$ row, recording one shared modulus route for the displayed family and compact source.
\item $K$ is the $\CompactUp$ source row, read only through finite cover, located, or compact-metric certificates already visible to the packet.
\item $W$ is the $\StreamNameUp$ finite-window row naming the inspected family indices, source centers, and target value observations.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the rational value windows in $W$.
\item $L$ is the finite subsequence-ledger row: it records a displayed increasing list of selected indices for the inspected windows, not a host infinite subsequence.
\item $D$ is the diagonal compatibility row, requiring each later window in $L$ to refine the finite-net and equicontinuity data exposed by $A,E,K,W,R$.
\item $H$ records componentwise $\hsame$ transport for the finite-net, equicontinuity, compact, stream, rational-readback, subsequence-ledger, diagonal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a finite compactness request through $A,E,K,W,R,L,D$ to the downstream subsequence certificate.
\item $P$ is the $\Pkg$ provenance over $\AscoliEquicontinuousSubsequenceUp$, $\ArzelaAscoliFiniteNetUp$, $\EquicontinuousFamilyUp$, $\CompactUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\AscoliEquicontinuousSubsequenceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,E,K,W,R,L,D,H,C,P,N$. It has no coordinate for a selected infinite subsequence, a host function-space limit, quotient equality of maps, open-cover compactness, countable choice, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.AscoliEquicontinuousSubsequenceUp.AscoliEquicontinuousSubsequenceUp\_single\_carrier\_alignment}

\begin{theorem}[Ascoli equicontinuous subsequence obligations]
\label{thm:ascoli-equicontinuous-subsequence-obligations}
For every accepted $\AscoliEquicontinuousSubsequenceUp$ carrier
$$
S=(A,E,K,W,R,L,D,H,C,P,N),
$$
the local $\NameCert_{\AscoliEquicontinuousSubsequenceUp}$ surface consists exactly of carrier admission; finite-net extraction exposure through $A$; equicontinuity exposure through $E$; compact-source exposure through $K$; finite-window exposure through $W$; regular rational readback through $R$; finite subsequence-ledger exposure through $L$; diagonal compatibility through $D$; componentwise transport through $H$; displayed replay through $C$; package provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:ascoli-equicontinuous-subsequence-carrier}. The rows $A,E,K,W,R,L,D$ are the only mathematical rows, and $H,C,P,N$ are the only structural rows.

The finite-net and equicontinuity inputs are read first. The row $A$ supplies the finite extraction windows of \autoref{ch:concrete-instances-arzela-ascoli-finite-net-namecert}, while $E$ supplies the shared family modulus and $K$ supplies the compact source boundary.

The finite observation route then passes through $W$ and $R$. These rows expose only displayed stream windows and regular rational readbacks before any Real-facing consumer reaches the terminal seal surface of \autoref{ch:concrete-instances-real-namecert}.

The subsequence information is the finite ledger $L$ together with the diagonal compatibility row $D$. Because $L$ records only displayed increasing finite lists, it cannot provide an infinite host subsequence or a choice-selected limit.

Finally $H,C,P,N$ transport, replay, source, and name the same rows. Since the excluded subsequence-choice, function-space limit, quotient equality, open-cover compactness, countable-choice, and ambient-completeness objects are absent from the carrier, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Ascoli equicontinuous finite-net extraction]
\label{thm:ascoli-equicontinuous-subsequence-finite-net-extraction}
Every compactness-facing consumer of an accepted $\AscoliEquicontinuousSubsequenceUp$ carrier must read the displayed route
$$
\ArzelaAscoliFiniteNetUp,\ \EquicontinuousFamilyUp,\ \CompactUp,\ \StreamNameUp,\ \RegSeqRatUp
$$
through the rows $A,E,K,W,R$ before it may consume the finite subsequence-ledger row $L$ and diagonal compatibility row $D$. The exported certificate is a finite-net subsequence surface for the inspected windows; it supplies no selected infinite subsequence, host function-space compactness theorem, quotient equality of maps, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the finite-net extraction row $A$. It forces the consumer to use the finite centers and tolerance budgets already named by \autoref{ch:concrete-instances-arzela-ascoli-finite-net-namecert}.

Next read the equicontinuity and compact-source rows. The row $E$ gives a single family modulus over the displayed compact source $K$, so the diagonal read cannot replace the shared modulus by memberwise radius choices.

The observation windows are then exposed through $W$ and $R$. The row $W$ names only finite stream coordinates, and $R$ translates those observations through regular rational readback before any $\RealUp$ seal is available.

Only after those rows are fixed can $L$ and $D$ be consumed. They certify finite increasing-index windows and their compatibility with the same finite-net and equicontinuity data.

The structural rows $H,C,P,N$ preserve that route under transport, replay, provenance, and local naming. Because the carrier contains no infinite subsequence selector, host function-space compactness object, quotient map equality, countable-choice schedule, or ambient completeness theorem, none can be exported by the finite-net certificate.
\end{proof}

\falsifiablePrediction{Every accepted $\AscoliEquicontinuousSubsequenceUp$ compactness-facing read factors through ArzelaAscoliFiniteNet, EquicontinuousFamily, Compact, StreamName, RegSeqRat, finite subsequence-ledger, diagonal-compatibility, hsame transport, Cont replay, Pkg provenance, and local NameCert rows.}

\independenceWitness{arzela-ascoli-finite-net, equicontinuous-family, compact, streamname, regseqrat, real: $\AscoliEquicontinuousSubsequenceUp$ is not bijective to any listed sibling because it jointly carries finite-net extraction, equicontinuity, compact-source, finite stream-window, regular rational readback, finite subsequence-ledger, diagonal compatibility, transport, replay, provenance, and local naming rows.}

\closureat{AscoliEquicontinuousSubsequenceUp}{seedStr}

\begin{closurestatus}{\AscoliEquicontinuousSubsequenceUp}
  \constructivestory{An $\AscoliEquicontinuousSubsequenceUp$ packet is generated from an ArzelaAscoliFiniteNet row, an EquicontinuousFamily row, a Compact source row, StreamName finite windows, RegSeqRat readback, a finite subsequence ledger, diagonal compatibility, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:ascoli-equicontinuous-subsequence-carrier}, \autoref{thm:ascoli-equicontinuous-subsequence-obligations}, and \autoref{thm:ascoli-equicontinuous-subsequence-finite-net-extraction} seed the finite-net subsequence certificate consumed by compactness-facing Ascoli routes.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected infinite subsequences, host function-space compactness, quotient equality of maps, open-cover compactness, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-net extraction exposure, equicontinuity exposure, compact-source exposure, finite-window exposure, regular rational readback, finite subsequence-ledger exposure, diagonal compatibility, transport, replay, provenance, and local naming for BEDC.Derived.AscoliEquicontinuousSubsequenceUp.}
\end{closurestatus}
