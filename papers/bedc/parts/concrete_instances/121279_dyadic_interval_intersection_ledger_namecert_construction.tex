\chapter{A Concrete Naming Certificate for $\DyadicIntervalIntersectionLedgerUp$}
\label{ch:concrete-instances-dyadic-interval-intersection-ledger-namecert}
\origin{human}

Following Bishop and Bridges on finite interval reasoning over constructive real numbers, the $\DyadicIntervalIntersectionLedgerUp$ packet records the displayed overlap ledger for two finite dyadic interval rows before any completion-facing consumer treats the overlap as a Real interval. It sits over the dyadic endpoint arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite endpoint-cell surface of \autoref{ch:concrete-instances-dyadicinterval-namecert}, the finite naming windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite boundary ledger for nested-interval, finite-cover, and compactness consumers; it does not import trichotomy, quotient interval equality, selected points, or ambient $\RealUp$ completeness.

\begin{definition}[Dyadic interval intersection ledger carrier]
\label{def:dyadic-interval-intersection-ledger-carrier}
A $\DyadicIntervalIntersectionLedgerUp$ carrier is a finite $\BHist$ packet
$$
L=(D,I_0,I_1,B,O,W,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ endpoint-arithmetic row, carrying lower and upper endpoint comparisons for the two inspected dyadic intervals.
\item $I_0$ and $I_1$ are the two $\DyadicIntervalUp$ source rows whose overlap boundary is being recorded.
\item $B$ is the finite boundary row, displaying the two candidate lower endpoints, the two candidate upper endpoints, and the endpoint comparisons needed to retain the intersection cell.
\item $O$ is the overlap ledger row, recording the retained lower endpoint, retained upper endpoint, and the finite witness that the lower endpoint does not pass the upper endpoint.
\item $W$ is the $\StreamNameUp$ window row for the retained overlap endpoints.
\item $Q$ is the $\RegSeqRatUp$ readback row for the retained dyadic endpoint windows and tolerance requests.
\item $E$ is the terminal $\RealUp$ seal row, available only after $D,I_0,I_1,B,O,W,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the dyadic arithmetic, interval, boundary, overlap, window, readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the two source interval rows through the overlap ledger and into the Real-facing seal.
\item $P$ is the $\Pkg$ provenance over $\DyadicIntervalIntersectionLedgerUp$, $\DyadicRatCoreUp$, $\DyadicIntervalUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\DyadicIntervalIntersectionLedgerUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $D,I_0,I_1,B,O,W,Q,E,H,C,P,N$. It has no coordinate for host interval equality, quotient real equality, trichotomy of endpoints, selected overlap points, open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Dyadic interval intersection ledger NameCert obligations]
\label{thm:dyadic-interval-intersection-ledger-namecert-obligations}
For every accepted $\DyadicIntervalIntersectionLedgerUp$ carrier $L=(D,I_0,I_1,B,O,W,Q,E,H,C,P,N)$, the local $\NameCert_{\DyadicIntervalIntersectionLedgerUp}$ surface consists exactly of dyadic endpoint arithmetic through $D$, the two source interval rows $I_0,I_1$, finite boundary comparison through $B$, retained-overlap exposure through $O$, stream-window exposure through $W$, regular rational readback through $Q$, terminal Real sealing through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$. Thus every accepted interval-intersection consumer must read the finite route
$$
D,I_0,I_1 \longmapsto B \longmapsto O \longmapsto W,Q \longmapsto E,H,C,P,N .
$$
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-interval-intersection-ledger-carrier}. The rows $I_0$ and $I_1$ name the two inspected dyadic intervals, while $D$ supplies the endpoint arithmetic used to compare their displayed bounds.

The boundary row $B$ records only finite comparisons among those endpoints. The overlap row $O$ retains the displayed lower and upper endpoint selected by those comparisons together with the finite nonempty-cell witness.

The stream window row $W$ and regular rational readback row $Q$ are downstream of the retained overlap ledger, and the Real seal $E$ is downstream of both. Transport, replay, provenance, and local naming through $H,C,P,N$ preserve that same displayed route.

Since the carrier has no coordinate for host interval equality, quotient real equality, endpoint trichotomy, selected overlap points, open-cover compactness, or ambient Real completeness, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Dyadic interval intersection ledger Real-seal nonescape]
\label{thm:dyadic-interval-intersection-ledger-real-seal}
Every Real-facing read from an accepted $\DyadicIntervalIntersectionLedgerUp$ packet factors through the dyadic endpoint row $D$, the two $\DyadicIntervalUp$ source rows $I_0,I_1$, the finite boundary row $B$, the retained overlap ledger $O$, the $\StreamNameUp$ window row $W$, and the $\RegSeqRatUp$ readback row $Q$ before the terminal $\RealUp$ seal $E$ is consumed. The handoff exports a finite intersection boundary and no trichotomy principle, quotient interval equality, selected point, open-cover compactness theorem, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Apply the NameCert obligations of \autoref{thm:dyadic-interval-intersection-ledger-namecert-obligations}. They place the two source interval rows and the dyadic endpoint arithmetic before the boundary comparison row.

The retained overlap ledger $O$ is downstream of those finite comparisons, and the stream and regular-rational readback rows $W,Q$ are downstream of the retained endpoints. A Real-facing consumer therefore reaches $E$ only after the finite overlap route has been displayed.

Finally replay $H,C,P,N$. These rows transport, replay, source, and name the same packet without adding host interval equality, quotient Real equality, endpoint trichotomy, a selected point, open-cover compactness, or ambient completeness. The Real-facing handoff remains a finite BEDC ledger.
\end{proof}

\falsifiablePrediction{Every accepted DyadicIntervalIntersectionLedgerUp consumer must display DyadicRatCore endpoint arithmetic, two DyadicInterval source rows, a finite boundary row, retained overlap ledger, StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row before a Real-facing overlap read is public.}

\independenceWitness{dyadicratcore, dyadicinterval, streamname, regseqrat, real: $\DyadicIntervalIntersectionLedgerUp$ is not bijective to any listed sibling because it binds two source intervals, finite endpoint comparisons, the retained overlap ledger, readback windows, terminal Real sealing, transport, replay, provenance, and local naming as one intersection-boundary carrier.}

\closureat{DyadicIntervalIntersectionLedgerUp}{seedStr}

\begin{closurestatus}{\DyadicIntervalIntersectionLedgerUp}
  \constructivestory{A $\DyadicIntervalIntersectionLedgerUp$ packet is generated from DyadicRatCore endpoint arithmetic, two DyadicInterval source rows, finite boundary comparison, retained overlap ledger, StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-interval-intersection-ledger-carrier}, \autoref{thm:dyadic-interval-intersection-ledger-namecert-obligations}, and \autoref{thm:dyadic-interval-intersection-ledger-real-seal} seed the finite interval-intersection boundary for DyadicRatCore, DyadicInterval, StreamName, RegSeqRat, and Real consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim endpoint trichotomy, host interval equality, quotient Real equality, selected overlap points, open-cover compactness, ambient RealUp completeness, Lean theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic endpoint comparisons, source-interval exposure, retained-overlap stability, StreamName windowing, RegSeqRat readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.DyadicIntervalIntersectionLedgerUp.}
\end{closurestatus}
