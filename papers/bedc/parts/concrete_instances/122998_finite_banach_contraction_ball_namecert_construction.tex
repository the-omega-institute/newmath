\chapter{A Concrete Naming Certificate for $\FiniteBanachContractionBallUp$}
\label{ch:concrete-instances-finite-banach-contraction-ball-namecert}
\origin{human}

Following Banach and Bishop--Bridges on constructive contraction arguments, the $\FiniteBanachContractionBallUp$ packet records the finite ball-budget data needed before a Picard/Cauchy consumer may spend a contraction estimate inside a located ball. It sits beside the complete metric source of \autoref{ch:concrete-instances-completemetric-namecert}, the metric comparison surface of \autoref{ch:concrete-instances-metric-namecert}, the finite Picard iteration row of \autoref{ch:concrete-instances-picard-iteration-namecert}, the StreamName windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite contraction-ball Cauchy budget, not a fixed-point theorem.

\begin{definition}[Finite Banach contraction-ball carrier]
\label{def:finite-banach-contraction-ball-carrier}
A $\FiniteBanachContractionBallUp$ carrier is a finite $\BHist$ packet
$$
B=(X,d,B_0,k,I,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the $\CompleteMetricUp$ source row naming the carried complete metric service.
\item $d$ is the $\MetricUp$ distance row used for the displayed ball and adjacent iterate comparisons.
\item $B_0$ is the located ball row, recording the center, radius, and finite membership checks consumed by the argument.
\item $k$ is the contraction constant row, displayed as rational data with $0\le k<1$.
\item $I$ is the $\PicardIterationUp$ row listing the visible iterates and the application schedule inside $B_0$.
\item $W$ is the $\StreamNameUp$ window row for the displayed Picard names.
\item $R$ is the $\RegSeqRatUp$ readback row generated from the ball, contraction, and Picard-window budgets.
\item $E$ is the $\RealUp$ seal row available only after the regular rational readback is exposed.
\item $H$ records componentwise $\hsame$ transport for the complete metric, metric, ball, contraction, Picard, stream, regular rational, Real, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the metric and ball rows through contraction estimates, Picard windows, regular rational readback, and the Real seal.
\item $P$ is the $\Pkg$ provenance over $\FiniteBanachContractionBallUp$, $\CompleteMetricUp$, $\MetricUp$, $\PicardIterationUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FiniteBanachContractionBallUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $X,d,B_0,k,I,W,R,E,H,C,P,N$. It has no coordinate for a selected fixed point, countable choice, quotient equality of Picard names, or ambient completeness outside the displayed $\CompleteMetricUp$ row.
\end{definition}

\begin{theorem}[Finite Banach contraction-ball NameCert obligations]
\label{thm:finite-banach-contraction-ball-namecert-obligations}
For every accepted $\FiniteBanachContractionBallUp$ carrier
$$
B=(X,d,B_0,k,I,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\FiniteBanachContractionBallUp}$ surface consists exactly of complete-metric source exposure through $X$, metric comparison through $d$, located-ball exposure through $B_0$, contraction-modulus exposure through $k$, Picard-window exposure through $I$, StreamName exposure through $W$, RegSeqRat readback through $R$, Real seal delivery through $E$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. Its analytic rows are the complete metric source, metric comparison row, located ball, contraction constant, Picard window, stream window, regular rational readback, and Real seal.

The ball and contraction data are read before any Cauchy-facing estimate is available. The row $B_0$ confines the argument to displayed membership checks, while $k$ supplies only the displayed rational shrink bound.

The Picard route is then confined to $I,W,R,E$. The finite iteration row lists the visible applications, the stream row names those windows, the regular rational row reads the finite estimates, and the Real row seals only that displayed readback.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since no excluded fixed-point or quotient object is a coordinate of the carrier, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Finite Banach contraction-ball Picard Cauchy route]
\label{thm:finite-banach-contraction-ball-picard-cauchy-route}
Every Picard/Cauchy consumer of an accepted $\FiniteBanachContractionBallUp$ packet must read the finite route
$$
\CompleteMetricUp,\ \MetricUp,\ \PicardIterationUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp
$$
together with the located ball row $B_0$, the contraction modulus row $k$, componentwise transport, displayed replay, provenance, and local naming before any fixed-point-facing conclusion is inspected. The route supplies a complete-metric source row, a located ball, a rational contraction modulus, a finite iteration window, StreamName windows, RegSeqRat handoff, and a Real seal; it supplies no selected fixed point, no countable-choice schedule, no quotient equality of names, and no ambient completeness beyond $X$.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:finite-banach-contraction-ball-namecert-obligations}. It places the complete metric, metric comparison, located ball, and contraction rows before the iteration and readback rows.

Read the Picard window inside the displayed ball. The row $I$ may spend the contraction modulus only after $B_0$ and $k$ have been exposed, so the packet cannot be read as a global contraction principle or as an unbounded orbit.

The Cauchy handoff remains finite. The rows $W$ and $R$ expose only StreamName windows and regular rational estimates, while $E$ seals the visible Real-facing boundary without selecting a host limit.

Transport, replay, provenance, and local naming preserve this same route. Thus any fixed-point-facing consumer must import its endpoint or uniqueness claim from a separate certificate.
\end{proof}

\falsifiablePrediction{Every accepted $\FiniteBanachContractionBallUp$ Picard/Cauchy read exposes a complete metric source, metric comparison row, located ball row, contraction modulus, finite Picard iteration window, StreamName windows, RegSeqRat readback, Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row before a fixed-point-facing conclusion is consumed.}

\independenceWitness{complete-metric, metric, streamname, regseqrat, real, picard-iteration: $\FiniteBanachContractionBallUp$ is not bijective to any listed sibling because it jointly binds a located ball, rational contraction modulus, finite Picard window, Cauchy readback, Real seal, transport, replay, provenance, and local naming in one packet.}

\closureat{FiniteBanachContractionBallUp}{seedStr}

\begin{closurestatus}{\FiniteBanachContractionBallUp}
  \constructivestory{A $\FiniteBanachContractionBallUp$ packet is generated from a CompleteMetric source row, Metric comparison row, located ball row, contraction-modulus row, PicardIteration row, StreamName windows, RegSeqRat readback, RealUp seal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-banach-contraction-ball-carrier}, \autoref{thm:finite-banach-contraction-ball-namecert-obligations}, and \autoref{thm:finite-banach-contraction-ball-picard-cauchy-route} seed the finite located contraction-ball budget for Picard/Cauchy consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected fixed points, countable choice, quotient equality of Picard names, ambient completeness beyond the carried $\CompleteMetricUp$ row, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located-ball exposure, contraction-modulus exactness, Picard-window exposure, StreamName and RegSeqRat readback, Real seal delivery, transport, replay, provenance, and local naming for BEDC.Derived.FiniteBanachContractionBallUp.}
\end{closurestatus}
