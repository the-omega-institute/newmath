\chapter{A Concrete Naming Certificate for $\CompactGridModulusExtractionUp$}
\label{ch:concrete-instances-compact-grid-modulus-extraction-namecert}
\origin{human}

Following Bishop-style compact metric finite-net arguments, the $\CompactGridModulusExtractionUp$ packet realizes the compact-grid route of \autoref{ch:visions-compact-uniform-continuity} as finite NameCert data. It sits beside compact metric coverage in \autoref{ch:concrete-instances-compactmetric-namecert}, continuous-map pointwise control in \autoref{ch:concrete-instances-continuousmap-namecert}, the uniform-modulus consumer of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, rational lower-bound readback in \autoref{ch:concrete-instances-rat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet records a finite compact-net grid and modulus extraction boundary without importing open-cover compactness, selected subcovers, countable choice, or ambient real completeness.

\begin{definition}[Compact grid modulus extraction carrier]
\label{def:compact-grid-modulus-extraction-carrier}
A $\CompactGridModulusExtractionUp$ carrier is a finite $\BHist$ packet
$$
G=(K,F,D,Q,M,U,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net row, including the displayed grid centers and coverage witnesses.
\item $F$ is the $\ContinuousMapUp$ pointwise-modulus row over the same compact metric source.
\item $D$ is the $\DyadicRatCoreUp$ grid-tolerance row used to compare source distances and target tolerances.
\item $Q$ is the $\RatUp$ lower-bound fold row over the finite grid radii.
\item $M$ is the extracted modulus-candidate row produced from $K,F,D,Q$.
\item $U$ is the downstream $\UniformModulusUp$ handoff row.
\item $E$ is the $\RealUp$ seal row for Real-valued compact-continuity consumers.
\item $H$ records componentwise $\hsame$ transport for compact grid data, continuous-map pointwise rows, dyadic tolerances, rational folds, extracted modulus candidates, UniformModulus handoff, Real seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay through the compact grid, pointwise modulus, dyadic tolerance, rational fold, extracted modulus row, UniformModulus handoff, and Real seal.
\item $P$ is the $\Pkg$ provenance over $\CompactGridModulusExtractionUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\DyadicRatCoreUp$, $\RatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactGridModulusExtractionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,F,D,Q,M,U,E,H,C,P,N$. It has no coordinate for open-cover compactness, selected subcovers, countable choice, quotient real equality, selected limits, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact grid modulus extraction NameCert obligations]
\label{thm:compact-grid-modulus-extraction-namecert-obligations}
For every accepted $\CompactGridModulusExtractionUp$ carrier $G=(K,F,D,Q,M,U,E,H,C,P,N)$, the local $\NameCert_{\CompactGridModulusExtractionUp}$ surface consists exactly of carrier admission for the compact finite grid, continuous-map pointwise rows, dyadic tolerance row, rational lower-bound fold, extracted modulus candidate, UniformModulus handoff, Real seal, transport, replay, provenance, and local naming rows; finite-net coverage accounting; lower-bound stability under $\hsame$; handoff exactness to $\UniformModulusUp$; and refusal of open-cover or choice-selected compactness outside the displayed grid.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-grid-modulus-extraction-carrier}. The construction rows are $K,F,D,Q,M,U,E$, and the structural rows are $H,C,P,N$.

The compact grid row $K$ supplies only displayed centers and coverage witnesses. The continuous-map row $F$ supplies pointwise modulus data over those centers, while $D$ and $Q$ record the dyadic and rational lower-bound calculations needed to extract $M$.

The replay row $C$ sends the extracted candidate to $\UniformModulusUp$ through $U$ and then to Real-valued consumers through $E$. Transport, provenance, and local naming preserve that same finite packet, so no open-cover compactness, selected subcover, choice selector, quotient real equality, selected limit, or ambient completeness row is present.
\end{proof}

\begin{theorem}[Compact grid modulus extraction finite-net handoff]
\label{thm:compact-grid-modulus-extraction-finite-net-handoff}
Every accepted $\CompactGridModulusExtractionUp$ carrier hands compact coverage to the modulus extractor only through the displayed finite route
$$
K,F,D,Q \longmapsto M .
$$
The handoff uses compact metric grid centers, continuous-map pointwise rows, dyadic tolerances, and a rational lower-bound fold; it does not use open-cover compactness, a selected subcover, sequential compactness, or countable choice.
\end{theorem}
\begin{proof}
Start from the compact grid row $K$. It exposes a finite net with displayed coverage witnesses rather than an arbitrary open cover.

Attach the continuous-map row $F$ at each displayed grid center. The pointwise radius data is then compared through the dyadic tolerance row $D$ and folded through the rational lower-bound row $Q$.

The extracted row $M$ is therefore downstream of the finite grid and lower-bound fold. Since no carrier coordinate names an open cover, selected subcover, sequential compactness witness, or choice selector, the finite-net handoff is the only compactness source available.
\end{proof}

\begin{theorem}[Compact grid modulus extraction uniform-modulus boundary]
\label{thm:compact-grid-modulus-extraction-uniform-modulus-boundary}
Every $\UniformModulusUp$ consumer of an accepted $\CompactGridModulusExtractionUp$ carrier reads the boundary
$$
K,F,D,Q,M \longmapsto U \longmapsto E,H,C,P,N .
$$
The exported modulus is a finite compact-grid extraction from compact metric coverage, continuous pointwise rows, dyadic tolerances, and rational lower bounds. It is not an ambient Heine--Cantor oracle, quotient real equality, selected limit, countable-choice selector, or $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Use the finite-net handoff theorem to place $M$ after $K,F,D,Q$. This ensures that the candidate modulus is derived from the displayed grid route.

The UniformModulus row $U$ receives only that candidate and the structural replay data. The Real seal $E$ is downstream of the same handoff and can be inspected only after the finite grid extraction has been displayed.

Finally read $H,C,P,N$. These rows transport, replay, source, and name the same packet, so the boundary contains no ambient uniform-continuity theorem, quotient real equality, selected limit, choice selector, or real-completeness row.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactGridModulusExtractionUp$ read factors through CompactMetric finite grid data, ContinuousMap pointwise-modulus rows, DyadicRatCore tolerance, RatUp lower-bound fold, an extracted modulus candidate, UniformModulus handoff, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row.}

\independenceWitness{compactmetric, continuousmap, uniformmodulus, dyadicratcore, rat, real: $\CompactGridModulusExtractionUp$ is not bijective to any listed sibling because it jointly carries compact finite-net coverage, pointwise continuous rows, dyadic grid tolerance, rational lower-bound fold, extracted modulus candidate, UniformModulus handoff, Real seal, transport, replay, provenance, and local naming.}

\closureat{CompactGridModulusExtractionUp}{seedStr}

\begin{closurestatus}{\CompactGridModulusExtractionUp}
  \constructivestory{A $\CompactGridModulusExtractionUp$ packet is generated from CompactMetric finite grid data, ContinuousMap pointwise-modulus rows, DyadicRatCore tolerance, RatUp lower-bound fold, an extracted modulus candidate, UniformModulus handoff, RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-grid-modulus-extraction-carrier}, \autoref{thm:compact-grid-modulus-extraction-namecert-obligations}, \autoref{thm:compact-grid-modulus-extraction-finite-net-handoff}, and \autoref{thm:compact-grid-modulus-extraction-uniform-modulus-boundary} seed the finite compact-grid route from compact metric coverage to UniformModulus handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subcovers, sequential compactness, quotient real equality, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-grid coverage accounting, pointwise-modulus routing, dyadic tolerance and rational lower-bound stability, UniformModulus handoff exactness, Real-seal nonescape, and structural replay for BEDC.Derived.CompactGridModulusExtractionUp.}
\end{closurestatus}
