\chapter{A Concrete Naming Certificate for $\RealSequentialApartnessUp$}
\label{ch:concrete-instances-real-sequential-apartness-namecert}
\origin{human}

Following Bishop and Bridges on constructive real numbers, apartness, and sequence inspection, the $\RealSequentialApartnessUp$ packet records a finite boundary where two real-name sequences are distinguished by displayed regular-rational windows and a positive dyadic gap. It sits after the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-window source of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic positive-gap ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the apartness route of \autoref{ch:concrete-instances-apartnessreal-namecert}, and the located apartness route of \autoref{ch:concrete-instances-located-real-apartness-namecert}. The packet names sequence-level finite-window apartness only; it is not trichotomy, quotient equality of real names, a selected representative, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[RealSequentialApartness carrier]\label{def:real-sequential-apartness-carrier}
A $\RealSequentialApartnessUp$ carrier is a finite $\BHist$ packet
$$
S=(L,R,W,Q,D,A,J,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ and $R$ are the left and right sequence-source rows, exposing only the inspected real-name sequence entries requested by the packet.
\item $W$ is the $\StreamNameUp$ finite-window row that lists the sequence indices, name prefixes, and observation windows currently available.
\item $Q$ is the $\RegSeqRatUp$ readback row for the regular rational approximants visible in $W$.
\item $D$ is the $\DyadicRatCoreUp$ positive-gap row, recording a displayed dyadic radius separating the two inspected regular-rational windows.
\item $A$ is the $\ApartnessRealUp$ route, read only after $W,Q,D$ have been displayed.
\item $J$ is the $\LocatedRealApartnessUp$ route, binding the same positive-gap evidence to a located finite-window apartness request.
\item $E$ is the terminal $\RealUp$ seal for the inspected entries, reached only through $W,Q,D,A,J$.
\item $H$ records componentwise $\hsame$ transport for the sequence-source, stream-window, regular-rational, positive-gap, apartness, located-apartness, Real-seal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a sequence apartness request through $L,R,W,Q,D,A,J,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RealSequentialApartnessUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\ApartnessRealUp$, $\LocatedRealApartnessUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealSequentialApartnessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,R,W,Q,D,A,J,E,H,C,P,N$. It has no coordinate for real trichotomy, quotient equality of real names, selected representatives, countable choice, pointwise host equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[RealSequentialApartness NameCert obligations]\label{thm:real-sequential-apartness-namecert-obligations}
For every accepted $\RealSequentialApartnessUp$ carrier $S=(L,R,W,Q,D,A,J,E,H,C,P,N)$, the local $\NameCert_{\RealSequentialApartnessUp}$ surface consists exactly of carrier admission for the two sequence-source rows, the finite $\StreamNameUp$ window, the $\RegSeqRatUp$ readback row, the positive $\DyadicRatCoreUp$ gap row, the $\ApartnessRealUp$ route, the $\LocatedRealApartnessUp$ route, the terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-sequential-apartness-carrier}. The tuple lists the sequence sources, finite stream window, regular rational readback, positive dyadic gap, apartness route, located apartness route, Real seal, and structural rows as the whole accepted surface.

The apartness evidence is read in row order. The window row $W$ exposes the inspected sequence entries, the readback row $Q$ exposes the regular rational approximants, and the positive-gap row $D$ supplies the dyadic separation before any apartness route is available.

Finally $A,J,E,H,C,P,N$ preserve and name that same finite route. Since no trichotomy, quotient real equality, selected representative, countable-choice schedule, host point equality, or ambient completeness coordinate appears in the carrier, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[RealSequentialApartness RegSeqRat-located bridge]\label{thm:real-sequential-apartness-regseqrat-located-bridge}
Every located sequence-apartness read exported by an accepted $\RealSequentialApartnessUp$ carrier factors through
$$
\StreamNameUp
\longmapsto
\RegSeqRatUp
\longmapsto
\DyadicRatCoreUp
\longmapsto
\ApartnessRealUp
\longmapsto
\LocatedRealApartnessUp
\longmapsto
\RealUp .
$$
Thus a consumer receives only finite windows, regular-rational readback, a displayed positive dyadic gap, the apartness and located-apartness routes, the Real seal, transport, replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Start with the carrier surface. The rows $L,R,W$ expose the two inspected sequence sources only through finite windows.

Next read $Q$ and $D$. The regular-rational row supplies the approximants visible in the window, and the dyadic row supplies the positive gap used by the apartness request.

The apartness rows $A$ and $J$ are therefore downstream of the same displayed data, and the Real seal $E$ is read only after them. The structural rows $H,C,P,N$ transport, replay, source, and name the route without adding any hidden real equality or choice coordinate.
\end{proof}

\begin{theorem}[RealSequentialApartness nonescape]\label{thm:real-sequential-apartness-nonescape}
No accepted $\RealSequentialApartnessUp$ carrier can export real trichotomy, quotient equality of sequence names, a selected representative, countable choice, pointwise host equality, or ambient $\RealUp$ completeness. Every exported apartness claim remains inside the displayed finite-window route $L,R,W,Q,D,A,J,E,H,C,P,N$.
\end{theorem}
\begin{proof}
The only public rows of the carrier are those listed in \autoref{def:real-sequential-apartness-carrier}. They record finite windows, regular readback, a positive dyadic gap, apartness routes, a Real seal, and structural transport.

Apply the obligation surface of \autoref{thm:real-sequential-apartness-namecert-obligations}. Each accepted read is forced through those rows before local naming is available.

The excluded principles have no row in the carrier and no replay coordinate in $C$. Hence they cannot be exported by transport, provenance, or local NameCert, and every sequence-apartness claim remains inside the displayed route.
\end{proof}

\leanstmt{BEDC.Derived.RealSequentialApartnessUp}

\falsifiablePrediction{Every accepted $\RealSequentialApartnessUp$ read factors through StreamNameUp windows, RegSeqRatUp readback, a positive DyadicRatCoreUp gap, ApartnessRealUp and LocatedRealApartnessUp routes, a RealUp seal, hsame transport, Cont replay, Pkg provenance, and local naming.}

\independenceWitness{apartness-real, located-real-apartness, real: $\RealSequentialApartnessUp$ is not bijective to any listed sibling because it jointly carries two sequence-source rows, finite windows, regular-rational readback, a positive dyadic gap, located apartness routing, a Real seal, transport, replay, provenance, and local NameCert rows in one packet.}

\closureat{RealSequentialApartnessUp}{seedStr}

\begin{closurestatus}{\RealSequentialApartnessUp}
  \constructivestory{A $\RealSequentialApartnessUp$ packet is generated from two sequence-source rows, StreamNameUp finite windows, RegSeqRatUp readback, a positive DyadicRatCoreUp gap, ApartnessRealUp and LocatedRealApartnessUp routes, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-sequential-apartness-carrier}, \autoref{thm:real-sequential-apartness-namecert-obligations}, \autoref{thm:real-sequential-apartness-regseqrat-located-bridge}, and \autoref{thm:real-sequential-apartness-nonescape} seed the finite sequence-level apartness boundary for L10 Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close trichotomy, quotient equality of real or sequence names, selected representatives, countable choice, ambient RealUp completeness, theorem-checked verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-window exposure, regular-rational readback, positive-gap exactness, apartness routing, located-apartness routing, RealUp sealing, transport, replay, provenance, and local naming for BEDC.Derived.RealSequentialApartnessUp.}
\end{closurestatus}
