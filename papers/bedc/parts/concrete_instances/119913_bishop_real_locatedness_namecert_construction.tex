\chapter{A Concrete Naming Certificate for $\BishopRealLocatednessUp$}
\label{ch:concrete-instances-bishop-real-locatedness-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\BishopRealLocatednessUp$ packet records located real evidence as finite regular-Cauchy witness data rather than as real trichotomy, quotient-real equality, or ambient completeness. It sits after the dyadic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the metric comparison surface of \autoref{ch:concrete-instances-metric-namecert}. The packet names the finite evidence by which a regular real name can answer located interval requests.

\begin{definition}[Bishop real locatedness carrier]
\label{def:bishop-real-locatedness-carrier}
A $\BishopRealLocatednessUp$ carrier is a finite $\BHist$ packet
$$
L=(S,R,D,E,M,Q,H,C,P,N)
$$
over $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\MetricUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ with the following displayed rows:
\begin{enumerate}
\item $S$ is the finite $\StreamNameUp$ window ledger naming exactly the real-name observations inspected by the packet.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from those windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance and interval-comparison ledger used to compare the readback with displayed rational bounds.
\item $E$ is the terminal $\RealUp$ seal row available only after the stream, regular-readback, and dyadic rows have been exposed.
\item $M$ is the $\MetricUp$ comparison row that records the located-distance request against rational interval endpoints.
\item $Q$ is the locatedness query ledger. Each row names a pair of rational endpoints $a<b$ and records which side of the displayed interval is witnessed by the finite readback and dyadic comparison rows.
\item $H$ records componentwise $\hsame$ transport for the stream, readback, dyadic, real-seal, metric, query, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a locatedness request through $Q,S,R,D,E,M,H$.
\item $P$ is the $\Pkg$ provenance over $\BishopRealLocatednessUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\MetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopRealLocatednessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for host real trichotomy, quotient-real equality, a selected Dedekind cut, a completed Cauchy quotient, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop real locatedness NameCert obligations]
\label{thm:bishop-real-locatedness-namecert-obligations}
For every accepted $\BishopRealLocatednessUp$ carrier $L=(S,R,D,E,M,Q,H,C,P,N)$, the local $\NameCert_{\BishopRealLocatednessUp}$ surface consists exactly of stream-window admission, regular-rational readback, dyadic tolerance comparison, real-seal nonescape, metric interval comparison, locatedness query admission, componentwise transport, displayed replay, provenance, and local naming. Every locatedness answer must cite the visible $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of the same packet.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-real-locatedness-carrier}. The stream row $S$, regular readback $R$, dyadic ledger $D$, real seal $E$, metric row $M$, and query ledger $Q$ are all visible coordinates of one finite packet.

Read a locatedness request through $Q$. The requested rational interval is compared only after the packet exposes the finite stream windows, the regular rational readback, and the dyadic tolerance ledger, so the answer is a finite witness row rather than a host real-order decision.

The structural rows $H,C,P,N$ transport, replay, source, and name the same displayed coordinates. Since the carrier has no coordinate for trichotomy, quotient-real equality, a selected cut, a completed Cauchy quotient, countable choice, or ambient completeness, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Bishop real locatedness RegSeqRat witness route]
\label{thm:bishop-real-locatedness-regseqrat-witness-route}
Every locatedness witness exported by an accepted $\BishopRealLocatednessUp$ packet factors through the finite route
$$
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \RealUp \longmapsto \MetricUp .
$$
Thus a consumer may read a located interval answer only after it has displayed the finite stream windows, the regular rational readback, the dyadic tolerance comparison, the real-name seal, and the metric comparison row. The route does not export real trichotomy, quotient-real equality, selected cuts, selected limits, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Begin with a consumer of a locatedness answer. Carrier admission exposes the query row $Q$ together with $S,R,D,E,M$, so the answer is attached to the same finite witness tuple.

Replay the request through $C$. The consumer first reads the finite $\StreamNameUp$ windows, then the $\RegSeqRatUp$ regular readback, then the $\DyadicRatCoreUp$ tolerance comparison. Only after those rows are visible may the terminal $\RealUp$ seal and the $\MetricUp$ comparison row be inspected.

Transport by $H$ preserves this order componentwise. The package and local NameCert rows cite only the displayed route, so no excluded real-order, quotient, choice, selected-limit, or completeness datum can be projected from the packet.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopRealLocatednessUp$ read exposes StreamName finite windows, RegSeqRat readback, DyadicRatCore tolerance comparison, RealUp seal, MetricUp interval comparison, locatedness query admission, componentwise hsame, Cont replay, Pkg provenance, and local NameCert rows before a located interval answer is served.}

\independenceWitness{streamname, regseqrat, dyadicratcore, real, metric: $\BishopRealLocatednessUp$ is not bijective with any listed sibling because it jointly carries finite windows, regular readback, dyadic interval comparison, real sealing, metric comparison, locatedness queries, transport, replay, provenance, and local naming as one witness packet.}

\closureat{BishopRealLocatednessUp}{seedStr}

\begin{closurestatus}{\BishopRealLocatednessUp}
  \constructivestory{A $\BishopRealLocatednessUp$ packet is generated from StreamName finite windows, RegSeqRat readback, DyadicRatCore tolerance comparison, a RealUp seal, MetricUp interval comparison, locatedness query rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-real-locatedness-carrier}, \autoref{thm:bishop-real-locatedness-namecert-obligations}, and \autoref{thm:bishop-real-locatedness-regseqrat-witness-route} seed the located-real witness route from regular Cauchy names to finite interval answers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close real trichotomy, quotient-real equality, selected cuts, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located-query exactness, RegSeqRat readback, DyadicRatCore comparison, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BishopRealLocatednessUp.}
\end{closurestatus}
