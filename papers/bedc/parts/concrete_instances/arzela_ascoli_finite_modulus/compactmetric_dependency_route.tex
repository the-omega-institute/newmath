\begin{theorem}[Arzela--Ascoli compact metric forward reference]
\label{thm:arzela-ascoli-finite-modulus-compactmetric-forward-reference}
Every accepted $\ArzelaAscoliFiniteModulusUp$ extraction first reads the $\CompactMetricUp$ finite-net source row $K$ before any selector output, finite window, regular rational readback, or terminal $\RealUp$ seal is public. The route then passes through the continuous-map row $F$, finite modulus row $M$, window row $W$, readback row $R$, seal row $E$, and structural rows $H,C,P,N$; it exports no open-cover compactness theorem, selected subsequence choice, host function-space compactness, quotient real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:arzela-ascoli-finite-modulus-carrier}. The compact source row $K$ is the only coordinate that names compact metric data, and it is read as finite-net and dyadic-radius information.

Apply the finite-net extraction theorem \autoref{thm:arzela-ascoli-equicontinuous-finite-net-extraction}. It places $K$ before the shared modulus row $M$, the inspected windows $W$, and the regular rational readback $R$.

Finally use the nonescape theorem \autoref{thm:arzela-ascoli-subsequence-nonescape}. The terminal seal $E$ and the structural rows $H,C,P,N$ preserve the same finite route, so no selector-facing consumer can bypass the $\CompactMetricUp$ source row or replace it by an excluded host compactness principle.
\end{proof}
