\begin{theorem}[Arzela--Ascoli compact metric forward route]
\label{thm:arzela-ascoli-finite-modulus-compactmetric-forward-route}
Every accepted $\ArzelaAscoliFiniteModulusUp$ packet consumes the $\CompactMetricUp$ finite-net rows of \autoref{ch:concrete-instances-compactmetric-namecert} and the $\EquicontinuityUp$ pointwise modulus rows of \autoref{ch:concrete-instances-equicontinuity-namecert} before exporting its finite subfamily modulus route. In the carrier of \autoref{ch:concrete-instances-arzela-ascoli-finite-modulus-namecert}, the compact metric source row is read before the equicontinuity family row, the shared finite modulus row, the finite window ledger, the regular rational readback, and the terminal real seal.
\end{theorem}
\begin{proof}
Project the accepted $\ArzelaAscoliFiniteModulusUp$ carrier through the local NameCert chapter \autoref{ch:concrete-instances-arzela-ascoli-finite-modulus-namecert}. The compact metric coordinate supplies only finite-net centers, radius rows, and source coverage windows.

Read the equicontinuity coordinate next. Its pointwise modulus rows are attached to the displayed family and to the compact metric source windows, so the shared finite modulus cannot be exported before those two dependency surfaces are visible.

The finite subfamily modulus route then reads the window ledger, regular rational readback, real seal, transport, replay, provenance, and local naming rows. Since neither dependency chapter exports a selected subsequence or host function-space compactness row, the Arzela--Ascoli packet remains a finite forward route from $\CompactMetricUp$ and $\EquicontinuityUp$ into the local selector surface.
\end{proof}
