The $\ArzelaAscoliFiniteModulusUp$ selector-facing route also consumes the $\CompactUniformContinuityLedgerUp$ packet of \autoref{ch:concrete-instances-compact-uniform-continuity-ledger-namecert}. This child route records the finite compact-net, pointwise modulus, rational lower-bound fold, and $\UniformModulusUp$ handoff rows as a dependency before any finite extraction data is public.

\begin{theorem}[Arzela--Ascoli compact-uniform-continuity ledger route]
\label{thm:arzela-ascoli-finite-modulus-compact-uniform-continuity-ledger-route}
Every accepted $\ArzelaAscoliFiniteModulusUp$ selector read that exports finite-modulus data must first consume the $\CompactUniformContinuityLedgerUp$ rows through finite compact-net data, pointwise modulus rows, rational lower-bound folds, and the terminal $\UniformModulusUp$ handoff. In the notation of \autoref{def:arzela-ascoli-finite-modulus-carrier}, the read remains inside
$$
K,F,M,W,R,E,H,C,P,N .
$$
It therefore exports no open-cover compactness theorem, selected subcover, selected subsequence, host function-space compactness, quotient real equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:arzela-ascoli-finite-modulus-carrier}. The compact source row $K$, continuous-map family row $F$, finite modulus row $M$, finite windows $W$, regular readback $R$, terminal seal $E$, and structural rows $H,C,P,N$ are the only rows available to a selector-facing consumer.

Apply \autoref{thm:arzela-ascoli-finite-modulus-forward-lattice}. It places compact finite-net data, inspected pointwise rows, finite tolerance ledgers, dyadic arithmetic, regular rational readback, and the $\RealUp$ seal before any selector row is exported.

Read the $\CompactUniformContinuityLedgerUp$ route through \autoref{thm:compact-uniform-continuity-ledger-finite-fold-route}. Its lower-bound fold and $\UniformModulusUp$ handoff supply the compact-uniform-continuity dependency consumed by the finite modulus row $M$.

Finally apply \autoref{thm:arzela-ascoli-subsequence-nonescape}. Since the carrier contains no field for open-cover compactness, selected subcovers, selected subsequences, quotient real equality, countable choice, or ambient completeness, the selector-facing export cannot bypass the compact-uniform-continuity ledger.
\end{proof}
