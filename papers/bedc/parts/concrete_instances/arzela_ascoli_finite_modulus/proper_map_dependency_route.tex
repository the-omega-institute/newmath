\begin{theorem}[Arzela--Ascoli finite modulus proper-map consumer]
\label{thm:arzela-ascoli-finite-modulus-proper-map-consumer}
Every $\ProperMapUp$ compact-preimage consumer that reads a finite-modulus
selector from an accepted $\ArzelaAscoliFiniteModulusUp$ packet must first
display the dependency route
$$
\CompactMetricUp,\quad \CompactUniformContinuityUp,\quad \EquicontinuityUp,\quad
\StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp,\quad \DyadicRatCoreUp,\quad
\ProperMapUp .
$$
The consumer may then use the finite-modulus selector as compact-preimage
evidence for the proper-map boundary; it does not obtain host compactness,
selected subsequences, quotient real equality, ambient Real completeness, or a
ProperMap compact-preimage row detached from the displayed selector route.
\end{theorem}
\begin{proof}
Project the $\ArzelaAscoliFiniteModulusUp$ carrier through the sibling route export. \autoref{thm:arzela-ascoli-finite-modulus-sibling-route} places compact metric data, equicontinuity bounds, compact-uniform-continuity transfer, finite stream windows, regular rational readback, the terminal Real seal, and dyadic tolerance arithmetic before any selector-facing export.

Next use the existing sibling lattice repair. The lattice paragraph preceding \autoref{thm:arzela-ascoli-finite-modulus-sibling-route} ties the compact source rows of \autoref{ch:concrete-instances-compactmetric-namecert} and the compact-uniform rows of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert} to the equicontinuity children, so the selector cannot be read before the displayed compact and modulus rows.

Finally attach the proper-map compact-preimage boundary. \autoref{thm:proper-map-compact-preimage-boundary} requires every compact-preimage read to remain behind the source, target, continuous-map, compact-preimage, transport, replay, provenance, and local naming rows of $\ProperMapUp$. Hence a proper-map consumer may use the finite-modulus selector only after all rows in the displayed route have been shown.
\end{proof}
