The $\ArzelaAscoliFiniteModulusUp$ dependency lattice consumes the Cauchy sequential criterion of \autoref{ch:concrete-instances-cauchy-sequential-criterion-namecert} only after its compact finite-modulus, equicontinuity, finite-window, regular readback, and terminal real-seal rows have been displayed. Thus a sequence-facing compactness consumer reads the same compact metric row $K$, continuous-family row $F$, finite modulus row $M$, window row $W$, readback row $R$, seal row $E$, transport row $H$, replay row $C$, provenance row $P$, and local naming row $N$ before the Cauchy sequential criterion can be used as a completion-facing dependency.

Every $\ArzelaAscoliFiniteModulusUp$ read that reaches the Cauchy sequential criterion first exposes the compact finite-modulus and equicontinuity rows named by \autoref{thm:arzela-ascoli-finite-modulus-forward-lattice} and \autoref{thm:arzela-ascoli-finite-modulus-compact-uniform-repair}. The dependency is a displayed row handoff to \autoref{ch:concrete-instances-cauchy-sequential-criterion-namecert}, not a host compactness theorem, selected subsequence choice, quotient real equality, or ambient $\RealUp$ completeness claim.
