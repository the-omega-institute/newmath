\begin{theorem}[Arzela--Ascoli finite-modulus sibling dependency route]
\label{thm:arzela-ascoli-finite-modulus-sibling-dependency-route}
Every accepted $\ArzelaAscoliFiniteModulusUp$ read must pass through the $\CompactMetricUp$, $\EquicontinuityFamilyUp$, $\UniformModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ sibling rows before it exports the finite-modulus handoff. The route exposes compact metric source data, equicontinuity-family discipline, uniform tolerance-to-precision data, finite stream windows, regular-rational readback, and the terminal Real seal as separate rows, and it does not replace them by host compactness, selected subsequences, quotient Real equality, or ambient completeness.
\end{theorem}
\begin{proof}
Read the finite carrier of \autoref{def:arzela-ascoli-finite-modulus-carrier}. Its selector-facing handoff is available only after the compact source, finite-window, regular readback, Real seal, transport, replay, provenance, and local naming rows have been displayed.

Now use the sibling lattice and forward routes, including \autoref{thm:arzela-ascoli-finite-modulus-forward-lattice}, \autoref{thm:arzela-ascoli-equicontinuity-family-dependency-route}, and \autoref{thm:arzela-ascoli-finite-modulus-uniformmodulus-forward-route}. These rows insert the $\CompactMetricUp$, $\EquicontinuityFamilyUp$, and $\UniformModulusUp$ dependencies before the exported finite modulus.

The remaining rows are the L10 source rows $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ already carried by the same finite packet. Since the handoff is obtained by exhausting those displayed rows, no host compactness principle, selected subsequence, quotient equality, or ambient completeness row can substitute for the sibling dependency route.
\end{proof}
