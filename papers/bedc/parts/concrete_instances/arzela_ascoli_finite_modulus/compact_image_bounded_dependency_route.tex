\begin{theorem}[Arzela--Ascoli compact-image bounded dependency route]
\label{thm:arzela-ascoli-finite-modulus-compact-image-bounded-dependency-route}
Every accepted $\ArzelaAscoliFiniteModulusUp$ finite-selector read that exports selector-facing modulus data must first consume the compact source nets of \autoref{ch:concrete-instances-compactmetric-namecert}, the displayed map rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the finite tolerance ledger of \autoref{ch:concrete-instances-uniformmodulus-namecert}, and the bounded image radius route of \autoref{ch:concrete-instances-compact-continuous-image-total-bounded-namecert}. In carrier notation, the read stays inside
$$
K,F,M,W,R,E,H,C,P,N .
$$
Thus the selector-facing finite modulus is exported only after compact source nets, uniform continuity data, bounded image radius data, regular rational readback, and the terminal $\RealUp$ seal have all been displayed.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:arzela-ascoli-finite-modulus-carrier}. The compact source row $K$, continuous-map row $F$, finite modulus row $M$, window row $W$, regular readback row $R$, terminal seal row $E$, and structural rows $H,C,P,N$ are the only rows available to a selector-facing consumer.

Apply the forward lattice theorem \autoref{thm:arzela-ascoli-finite-modulus-forward-lattice}. It places compact metric finite-net data, continuous-map inspection, finite common modulus data, finite windows, dyadic tolerance arithmetic, regular rational readback, and the $\RealUp$ seal before any downstream selector row is public.

Read the compact-image boundedness sibling next. The total-bounded image certificate of \autoref{ch:concrete-instances-compact-continuous-image-total-bounded-namecert} supplies the bounded radius route consumed after the compact source nets, continuous-map rows, and uniform modulus ledger have been displayed.

Finally use subsequence nonescape from \autoref{thm:arzela-ascoli-subsequence-nonescape}. Since the carrier has no coordinate for open-cover compactness, selected subsequence choice, host function-space compactness, quotient real equality, or ambient completeness, the selector-facing finite modulus cannot bypass the compact-image boundedness data while remaining inside the accepted packet.
\end{proof}
