The $\ArzelaAscoliFiniteModulusUp$ hub consumes the compact-uniform-continuity route of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, the shared-radius discipline of \autoref{ch:concrete-instances-equicontinuity-namecert}, and the tolerance-only ledger of \autoref{ch:concrete-instances-uniformmodulus-namecert} before its finite-modulus selector is public. Thus the hub records the actual sibling lattice used by the carrier: compact finite-net rows, equicontinuity rows, uniform-modulus rows, finite windows, regular rational readback, and the terminal $\RealUp$ seal stay visible as separate dependencies rather than collapsing into a host compactness or function-space compactness theorem.
