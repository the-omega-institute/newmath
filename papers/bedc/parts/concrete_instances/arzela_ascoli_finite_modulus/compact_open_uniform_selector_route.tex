The hub route for $\ArzelaAscoliFiniteModulusUp$ records the selector-facing dependency already present in the carrier: a finite-modulus read must pass through the compact-open metric rows of \autoref{ch:concrete-instances-compact-open-metric-namecert} and the compact-uniform handoff rows of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert} before the rows $K,F,M,W,R,E,H,C,P,N$ can be exported to a downstream selector. Thus the hub links the compact-open comparison surface and compact-uniform continuity surface through the existing finite-modulus selector, not through a host function-space compactness theorem, selected subsequence choice, quotient real equality, or ambient $\RealUp$ completeness principle.
