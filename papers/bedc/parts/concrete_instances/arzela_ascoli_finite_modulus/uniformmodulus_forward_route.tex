\begin{theorem}[Arzela--Ascoli UniformModulus forward route]
\label{thm:arzela-ascoli-finite-modulus-uniformmodulus-forward-route}
Every accepted $\ArzelaAscoliFiniteModulusUp$ selector read must first consume the $\UniformModulusUp$ finite tolerance-to-precision ledger, together with the $\CompactMetricUp$, $\ContinuousMapUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows, before any selector-facing finite-modulus data is public.

The read remains inside the displayed carrier route
$$
K,F,M,W,R,E,H,C,P,N .
$$
It does not export host function-space compactness, selected subsequence choice, quotient real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:arzela-ascoli-finite-modulus-carrier}. The compact metric row $K$, continuous-map family row $F$, finite modulus row $M$, finite windows $W$, regular readback $R$, Real seal $E$, and structural rows $H,C,P,N$ are the only visible selector coordinates.

Apply the existing forward lattice, \autoref{thm:arzela-ascoli-finite-modulus-forward-lattice}. It already forces downstream selector reads to pass through the compact metric, continuous-map, finite-window, $\RegSeqRatUp$, $\RealUp$, and $\DyadicRatCoreUp$ rows before the selector-facing handoff.

Now read the uniform-modulus chapter, \autoref{ch:concrete-instances-uniformmodulus-namecert}. Its finite $\mathsf{ProbeBundle}$ fold supplies the tolerance-to-precision ledger that keeps the image-side windows tied to one displayed compact-continuous modulus route.

Repack the route through $H,C,P,N$. Since no carrier coordinate supplies host function-space compactness, selected subsequence choice, quotient real equality, or ambient completeness, the UniformModulus handoff is a dependency edge inside the finite packet rather than an added analytic principle.
\end{proof}
