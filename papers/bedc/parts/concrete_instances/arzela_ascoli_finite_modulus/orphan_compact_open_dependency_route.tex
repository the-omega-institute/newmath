\begin{theorem}[Compact-open equicontinuity dependency route]
\label{thm:arzela-ascoli-finite-modulus-compact-open-equicontinuity-dependency-route}
Every accepted $\ArzelaAscoliFiniteModulusUp$ finite-modulus read that compares a family of maps consumes the compact-open metric surface of \autoref{ch:concrete-instances-compact-open-metric-namecert} and the equicontinuity family surface of \autoref{ch:concrete-instances-equicontinuity-namecert} before exporting selector-facing rows. The exported selector data therefore remains downstream of compact-open source-window comparison, equicontinuity radius discipline, finite modulus rows, $\RegSeqRatUp$ readback, the terminal $\RealUp$ seal, and the structural $H,C,P,N$ rows, rather than a host function-space compactness theorem or a selected subsequence principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:arzela-ascoli-finite-modulus-carrier}. A family-comparison read already exposes compact source data, continuous-map rows, finite modulus rows, window ledgers, regular readback, the terminal Real seal, and structural replay rows.

Use the compact-open dependency route of \autoref{thm:arzela-ascoli-finite-modulus-compact-open-dependency}. It places the compact-open metric comparison surface before any selector-facing finite-modulus export can be read.

Use the equicontinuity anchors \autoref{thm:arzela-ascoli-finite-modulus-equicontinuity-forward-anchor} and \autoref{thm:arzela-ascoli-equicontinuity-family-dependency-route}. They force the same family comparison to consume the equicontinuity family-window and shared-radius rows before finite subfamily modulus data is public.

Thus a selector read that compares maps must pass through both sibling surfaces. Since neither sibling supplies host function-space compactness, selected subsequences, quotient real equality, countable choice, or ambient $\RealUp$ completeness, the Arzela--Ascoli export remains a finite displayed dependency route.
\end{proof}
