\begin{theorem}[Arzela--Ascoli finite modulus compact-metric sibling readback]
\label{thm:arzela-ascoli-finite-modulus-compactmetric-sibling-readback}
Any finite-modulus selector read through an accepted $\ArzelaAscoliFiniteModulusUp$ packet must first consume the $\CompactMetricUp$ finite-net rows of \autoref{ch:concrete-instances-compactmetric-namecert} and the $\ContinuousMapUp$ family rows before the selector row can be exported. The route passes through the compact-uniform-continuity finite-modulus handoff of \autoref{ch:concrete-instances-compact-uniform-continuity-finite-modulus-namecert}; it therefore exposes finite covers, continuity witnesses, and the shared modulus ledger before a downstream consumer may read the $\ArzelaAscoliFiniteModulusUp$ selector.
\end{theorem}

\begin{proof}
Read the compact-metric sibling first. The finite-net rows named by \autoref{ch:concrete-instances-compactmetric-namecert} provide the bounded finite cover surface on which a modulus selector can be evaluated.

Next read the $\ContinuousMapUp$ family rows. These rows keep the maps in the family visible as continuous data rather than as an external collection of functions.

Finally pass through \autoref{ch:concrete-instances-compact-uniform-continuity-finite-modulus-namecert}. Its finite-modulus handoff is the point where compactness and continuity are combined, so the $\ArzelaAscoliFiniteModulusUp$ selector row can be exported only after those sibling rows have been consumed.
\end{proof}

\begin{theorem}[Arzela--Ascoli compact-equicontinuity finite-modulus dependency]
\label{thm:arzela-ascoli-finite-modulus-compact-equicontinuity-dependency}
Every accepted $\ArzelaAscoliFiniteModulusUp$ selector route consumes three sibling surfaces before the Arzela--Ascoli readback is public: the compact finite-net source of \autoref{ch:concrete-instances-compactmetric-namecert}, the shared family-modulus discipline of \autoref{ch:concrete-instances-equicontinuity-namecert}, and the compact-uniform finite-modulus handoff of \autoref{ch:concrete-instances-compact-uniform-continuity-finite-modulus-namecert}. In carrier rows, the compact metric dependency supplies $K$, equicontinuity supplies the family-level constraint read by $F$ and $M$, and compact-uniform continuity supplies the finite modulus handoff consumed before $W,R,E,H,C,P,N$ are exported.
\end{theorem}

\begin{proof}
Start with the compact metric sibling. Its finite-net source gives the only compact-domain row available to the selector route, so the readback cannot begin from an unrecorded host compactness theorem.

Next consume the equicontinuity sibling. The shared family-modulus rows constrain the continuous-map family before the finite-modulus row $M$ can be read as a common selector budget.

Finally use the compact-uniform finite-modulus handoff. It combines compact finite nets with the displayed continuity rows before the stream windows, regular rational readback, and terminal real seal are exposed. Therefore the Arzela--Ascoli readback is a consumer of those three sibling surfaces, not a kernel-only packet detached from the dependency lattice.
\end{proof}
