\chapter{A Concrete Naming Certificate for $\ArzelaAscoliFiniteModulusUp$}
\label{ch:concrete-instances-arzela-ascoli-finite-modulus-namecert}
\origin{human}

Following Bishop and Bridges, the $\ArzelaAscoliFiniteModulusUp$ packet records the finite-modulus subsequence route for an equicontinuous family on a compact metric source. It consumes the uniform-continuity witness pattern of \autoref{ch:concrete-instances-heine-cantor-namecert}, the compact source surface of \autoref{ch:concrete-instances-compactmetric-namecert}, the displayed map rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the window schedules of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the dyadic tolerance arithmetic of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The chapter names a finite extraction certificate; it does not assert general open-cover compactness, selected subsequence choice, host function-space compactness, quotient real equality, or ambient $\RealUp$ completeness.

Concretely, $\ArzelaAscoliFiniteModulusUp$ consumes the equicontinuity-family rows of \autoref{ch:concrete-instances-equicontinuity-namecert} and the compact metric finite-net rows of \autoref{ch:concrete-instances-compactmetric-namecert} before exporting a finite modulus packet.

The same finite-modulus route is consumed by the downstream selector lattice of \autoref{ch:concrete-instances-equicontinuity-namecert}, \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, and \autoref{ch:concrete-instances-compactnetmodulusselector-namecert}. Before selector rows are exported, the route also consumes the finite ProbeBundle realization route of \autoref{ch:concrete-instances-compact-uniform-finite-net-realization-namecert}, which supplies compact metric coverage, continuous pointwise moduli, RatUp lower-bound folds, and UniformModulusUp handoff rows for $\ArzelaAscoliFiniteModulusUp$. It also reads the finite compactness certificate of \autoref{ch:concrete-instances-finite-modulus-compactness-namecert}, whose compact-metric, finite-net-equicontinuity, compact-uniform-continuity-modulus, and finite-epsilon-net rows are available before the selector row is exported. The downstream equicontinuity and compact-uniform-continuity chapters consume the shared finite-modulus row $M$ only through this displayed lattice, so their dependency on $\ArzelaAscoliFiniteModulusUp$ is a row-level handoff rather than a hub-level citation. Those siblings may read the packet only after the compact metric, continuous-map, finite-window, $\RegSeqRatUp$, $\RealUp$, and $\DyadicRatCoreUp$ rows are displayed. In row form, the selector lattice receives exactly $K,F,M,W,R,E,H,C,P,N$: $K$ supplies the compact finite-net source, $F$ the inspected continuous-map family, $M$ the shared finite modulus, $W$ the windows, $R$ the rational readback, $E$ the terminal real seal, and $H,C,P,N$ the transport, replay, provenance, and local naming rows.

The finite-modulus extraction route also consumes the compact-uniform-continuity surface of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert} and the uniform modulus ledger of \autoref{ch:concrete-instances-uniformmodulus-namecert} before exporting any Arzela--Ascoli selection row. Compact finite nets provide the domain-side finite cover, while $\UniformModulusUp$ supplies the image-side modulus ledger that keeps the extracted windows tied to one displayed tolerance schedule.

\begin{definition}[Arzela--Ascoli finite-modulus carrier]
\label{def:arzela-ascoli-finite-modulus-carrier}
An $\ArzelaAscoliFiniteModulusUp$ carrier is a finite $\BHist$ packet
$$
A=(K,F,M,W,R,E,H,C,P,N)
$$
with the following rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source certificate, read only through finite net and dyadic-radius data;
\item $F$ is the $\ContinuousMapUp$ family surface, inspected by finite $\StreamNameUp$ windows rather than by a host function-space object;
\item $M$ is the finite common modulus ledger, routed through $\HeineCantorUp$ and $\DyadicRatCoreUp$ rows;
\item $W$ is the finite net-window extraction surface for the chosen inspected depths;
\item $R$ is the $\RegSeqRatUp$ readback row for the extracted finite values;
\item $E$ is the terminal $\RealUp$ seal for the displayed pointwise limit windows;
\item $H$ records componentwise $\hsame$ transport of $K,F,M,W,R,E$;
\item $C$ records displayed $\Cont$ replay of the same finite extraction route;
\item $P$ is the $\Pkg$ provenance over $\ArzelaAscoliFiniteModulusUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\HeineCantorUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\ArzelaAscoliFiniteModulusUp}$ row.
\end{enumerate}
The carrier has no coordinate for open-cover compactness, a choice-selected subsequence, function-space compactness, quotient equality of real names, or ambient real completeness.
\end{definition}
\leandef{BEDC.Derived.ArzelaAscoliFiniteModulusUp.ArzelaAscoliFiniteModulusCarrier}

\begin{theorem}[Arzela--Ascoli finite-modulus NameCert obligations]
\label{thm:arzela-ascoli-finite-modulus-namecert-obligations}
For the carrier of \autoref{def:arzela-ascoli-finite-modulus-carrier}, the local $\NameCert_{\ArzelaAscoliFiniteModulusUp}$ surface consists exactly of carrier admission, compact-metric source admission through $K$, continuous-map family admission through $F$, finite common modulus admission through $M$, finite net-window extraction through $W$, regular rational readback through $R$, pointwise $\RealUp$ sealing through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\leanchecked{BEDC.Derived.ArzelaAscoliFiniteModulusUp.ArzelaAscoliFiniteModulusNamecertObligations}
\begin{proof}
Project the carrier. The compact source row $K$, continuous-map row $F$, and finite common modulus row $M$ are visible before any limit seal is read.

Read the extraction rows next. The finite windows $W$ are selected from displayed net data and inspected depths, while $R$ reads those windows as regular rational schedules through $\RegSeqRatUp$ and $\DyadicRatCoreUp$.

Finally the seal row $E$ and the structural rows $H,C,P,N$ transport, replay, source, and name exactly that finite route. Since no other coordinate appears in the carrier, no open-cover compactness, selected subsequence choice, host function-space compactness, quotient real equality, or ambient completeness obligation is introduced.
\end{proof}

\begin{theorem}[Arzela--Ascoli equicontinuous finite-net extraction]
\label{thm:arzela-ascoli-equicontinuous-finite-net-extraction}
Let $A=(K,F,M,W,R,E,H,C,P,N)$ be an accepted $\ArzelaAscoliFiniteModulusUp$ carrier. Every equicontinuous extraction row factors through the compact metric finite-net data in $K$, the common finite modulus in $M$, and the inspected $\StreamNameUp$ windows in $W$ before the regular rational readback $R$ is consumed. Thus the extraction is a dyadic finite-net construction over $\DyadicRatCoreUp$ and not a host compactness or function-space compactness principle.
\end{theorem}
\begin{proof}
Use the compact source row $K$ to obtain only the displayed finite-net and radius data. The row does not expose an arbitrary open-cover compactness theorem.

Apply the finite common modulus row $M$. It routes the inspected family values through the same Heine--Cantor-style modulus ledger at every selected depth.

The row $W$ then records the finite net windows, and $R$ reads them as $\RegSeqRatUp$ schedules. Transport and replay by $H$ and $C$ preserve the same dyadic route, so no selected subsequence or host function-space compactness object is available.
\end{proof}

\begin{theorem}[Arzela--Ascoli subsequence nonescape]
\label{thm:arzela-ascoli-subsequence-nonescape}
Every subsequence or limit-facing consumer of an accepted $\ArzelaAscoliFiniteModulusUp$ carrier must pass through
$$
K,F,M,W,R,E,H,C,P,N .
$$
The consumer receives finite compact-source nets, continuous-map inspection rows, a shared modulus ledger, finite extraction windows, regular rational readback, and a $\RealUp$ seal. It receives no selected subsequence choice, no host function-space compactness theorem, no quotient equality of real names, and no ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:arzela-ascoli-equicontinuous-finite-net-extraction}. It places the compact-source nets, common modulus, finite windows, and regular rational readback before the limit seal.

Then read the seal row $E$. It is downstream of $K,F,M,W,R$ and therefore names only the displayed pointwise limit windows.

The rows $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier inventory in \autoref{def:arzela-ascoli-finite-modulus-carrier} has no field for the excluded host principles, those principles cannot escape through the subsequence consumer.
\end{proof}

\begin{theorem}[Arzela--Ascoli finite-modulus forward lattice]
\label{thm:arzela-ascoli-finite-modulus-forward-lattice}
Every downstream read of an accepted $\ArzelaAscoliFiniteModulusUp$ packet by \autoref{ch:concrete-instances-equicontinuity-namecert}, \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, or \autoref{ch:concrete-instances-compactnetmodulusselector-namecert} factors through the displayed rows
$$
K,F,M,W,R,E,H,C,P,N .
$$
The consuming selector receives compact metric finite-net data, continuous-map inspection data, a finite common modulus ledger, finite windows, regular rational readback, the terminal $\RealUp$ seal, and dyadic tolerance arithmetic before it can export any finite selector row. It receives no selected subsequence choice, host function-space compactness, quotient real equality, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:arzela-ascoli-finite-modulus-carrier}. The compact metric row $K$, continuous-map family row $F$, finite common modulus row $M$, and window row $W$ are visible before any selector-facing consumer can read a limit row.

Apply \autoref{thm:arzela-ascoli-equicontinuous-finite-net-extraction}. It places the finite-net and modulus route ahead of the $\RegSeqRatUp$ readback row $R$ and keeps the dyadic tolerance arithmetic in the displayed dependency surface.

Before the selector row is exported, read \autoref{ch:concrete-instances-finite-modulus-compactness-namecert}. Its compact-metric, finite-net-equicontinuity, compact-uniform-continuity-modulus, and finite-epsilon-net rows provide the finite compactness certificate that feeds the same selector route.

Finally apply \autoref{thm:arzela-ascoli-subsequence-nonescape}. The real seal $E$ and structural rows $H,C,P,N$ preserve the same finite route for the equicontinuity, compact-uniform-continuity, and compact-net-modulus selector siblings. Since no excluded host principle is a carrier coordinate, no downstream selector can consume one through this packet.
\end{proof}

\begin{theorem}[Arzela--Ascoli Heine--Cantor compactness route]
\label{thm:arzela-ascoli-finite-modulus-heine-cantor-compactness-route}
Every selector-facing finite-modulus read of an accepted $\ArzelaAscoliFiniteModulusUp$ carrier
$$
A=(K,F,M,W,R,E,H,C,P,N)
$$
first consumes the compact uniform modulus route supplied by $\HeineCantorUp$, the finite-net source data of $\CompactMetricUp$, the pointwise rows of $\ContinuousMapUp$, the tolerance ledger of $\UniformModulusUp$, the finite windows of $\StreamNameUp$, the regular readback of $\RegSeqRatUp$, the tolerance arithmetic of $\DyadicRatCoreUp$, and the terminal $\RealUp$ seal. Only after these rows are visible may the selector export a finite-modulus handoff to \autoref{ch:concrete-instances-equicontinuity-namecert}, \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, or \autoref{ch:concrete-instances-compactnetmodulusselector-namecert}.
\end{theorem}
\begin{proof}
Project the carrier. The displayed rows $K,F,M,W,R,E,H,C,P,N$ expose the compact source, inspected continuous-map family, finite modulus ledger, finite windows, rational readback, terminal real seal, transport, replay, provenance, and local naming.

Apply \autoref{thm:arzela-ascoli-finite-modulus-forward-lattice}. It places the selector-facing read behind the compact metric, continuous-map, finite-modulus, window, readback, real-seal, and dyadic tolerance rows.

Now read \autoref{thm:arzela-ascoli-finite-modulus-compact-uniform-repair}. The compact-uniform finite-selector route forces the same consumer to pass through the $\HeineCantorUp$, $\UniformModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\DyadicRatCoreUp$ sibling rows before it reaches the compact-uniform-continuity surface.

Finally apply \autoref{thm:arzela-ascoli-subsequence-nonescape}. Since the packet has no selected subsequence choice, host function-space compactness, quotient real equality, or ambient completeness coordinate, the Heine--Cantor compactness route remains a finite row-level dependency for every selector-facing export.
\end{proof}

\begin{theorem}[Arzela--Ascoli compact-open dependency]
\label{thm:arzela-ascoli-finite-modulus-compact-open-dependency}
Every finite-modulus extraction read that compares a family of maps in an accepted $\ArzelaAscoliFiniteModulusUp$ packet must first pass through the $\CompactOpenMetricUp$ finite source-net, graph-sampling, target-distance, and sup-distance rows of \autoref{ch:concrete-instances-compact-open-metric-namecert} before exporting the $\ArzelaAscoliFiniteModulusUp$ selector row. In the notation of \autoref{def:arzela-ascoli-finite-modulus-carrier}, the dependency is read through
$$
K,F,M,W,R,E,H,C,P,N .
$$
\end{theorem}
\begin{proof}
Project the accepted carrier. The compact source row $K$ supplies the finite source-net surface, while the continuous-map family row $F$ supplies the maps whose graph samples are compared.

Read the modulus and window rows $M,W$. They restrict the comparison to displayed finite source windows and keep the sampled values tied to one tolerance schedule.

Now apply the compact-open metric route of \autoref{ch:concrete-instances-compact-open-metric-namecert}. Its target-distance and finite sup-distance rows are consumed before the regular rational readback $R$ and terminal seal $E$ are available to the selector.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Thus the Arzela--Ascoli finite-modulus selector receives a compact-open finite sup-distance dependency, not an unrecorded host function-space comparison.
\end{proof}

\begin{theorem}[Arzela--Ascoli compact-uniform finite-selector route]
\label{thm:arzela-ascoli-finite-modulus-compact-uniform-repair}
Any $\CompactUniformContinuityUp$ finite-selector consumer of an accepted $\ArzelaAscoliFiniteModulusUp$ packet must first read
$$
K,F,M,W,R,E,H,C,P,N
$$
together with the compact metric, continuous map, Heine--Cantor, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\DyadicRatCoreUp$ sibling rows. The route then lands in \autoref{ch:concrete-instances-compactuniformcontinuity-namecert} only through the finite selector surface also consumed by \autoref{ch:concrete-instances-compactnetmodulusselector-namecert}.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:arzela-ascoli-finite-modulus-carrier}. The rows $K,F,M,W,R,E,H,C,P,N$ expose the compact metric source, continuous-map family, finite common modulus, finite windows, regular rational readback, Real seal, transport, replay, provenance, and local naming.

Apply \autoref{thm:arzela-ascoli-finite-modulus-forward-lattice}. It already forces every downstream finite-selector read to pass through the compact metric, continuous-map, Heine--Cantor, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, and $\DyadicRatCoreUp$ rows before any selector-facing handoff is available.

Therefore the compact-uniform-continuity consumer reaches \autoref{ch:concrete-instances-compactuniformcontinuity-namecert} by the same finite route that feeds \autoref{ch:concrete-instances-compactnetmodulusselector-namecert}, with no selected subsequence choice, host function-space compactness, quotient real equality, or ambient $\RealUp$ completeness principle inserted.
\end{proof}

\falsifiablePrediction{Every consumer of an $\ArzelaAscoliFiniteModulusUp$ packet must read the compact metric finite nets, continuous-map family windows, finite common modulus, regular rational readback, and $\RealUp$ seal before consuming the extracted limit row.}

\independenceWitness{compactmetric, continuousmap, heine-cantor, streamname, regseqrat, real, dyadicratcore: $\ArzelaAscoliFiniteModulusUp$ is not bijective to any listed sibling because it jointly carries compact-source nets, a continuous-map family surface, a finite common modulus ledger, finite extraction windows, regular rational readback, Real sealing, componentwise transport, continuation replay, provenance, and local naming.}

\closureat{ArzelaAscoliFiniteModulusUp}{seedStr}

\begin{closurestatus}{\ArzelaAscoliFiniteModulusUp}
  \constructivestory{An $\ArzelaAscoliFiniteModulusUp$ packet is generated from compact metric finite nets, continuous-map family windows, a finite common modulus ledger, finite extraction windows, regular rational readback, a RealUp seal, componentwise transport, continuation replay, provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:arzela-ascoli-finite-modulus-carrier}, \autoref{thm:arzela-ascoli-finite-modulus-namecert-obligations}, \autoref{thm:arzela-ascoli-equicontinuous-finite-net-extraction}, and \autoref{thm:arzela-ascoli-subsequence-nonescape} bind the finite-modulus extraction surface for one displayed equicontinuous family.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close general open-cover compactness, selected subsequence choice, host function-space compactness, quotient real equality, ambient $\RealUp$ completeness, or Lean verification.}
  \upgradepath{Scoped closure requires the finite-net extraction rows, Real seal row, and NameCert dependency surface to be formalized for BEDC.Derived.ArzelaAscoliFiniteModulusUp.}
\end{closurestatus}
