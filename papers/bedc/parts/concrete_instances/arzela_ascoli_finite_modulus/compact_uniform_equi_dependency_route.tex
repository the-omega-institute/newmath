The $\ArzelaAscoliFiniteModulusUp$ route consumes the compact-uniform-continuity finite modulus surface of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert} together with the equicontinuity-family rows of \autoref{ch:concrete-instances-equicontinuity-namecert} before it extracts the finite-modulus window. The same dependency route may also read the finite-net minimum fold of \autoref{ch:concrete-instances-finitenetminimumfold-namecert} when the compact source has already exposed its finite inspection schedule, but it receives only the displayed compact source, equicontinuity, modulus-window, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.
