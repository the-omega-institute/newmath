\begin{theorem}[Arzela--Ascoli compact uniform modulus dependency route]
\label{thm:arzela-ascoli-finite-modulus-compact-uniform-modulus-dependency-route}
Every accepted $\ArzelaAscoliFiniteModulusUp$ selector-facing read consumes the $\CompactUniformModulusUp$ finite-net fold of \autoref{ch:concrete-instances-compact-uniform-modulus-namecert} as a substantive dependency before the finite-modulus row is public. The same read passes through the compact-uniform-continuity ledger surface of \autoref{ch:concrete-instances-compact-uniform-continuity-ledger-namecert}, so compact source coverage, pointwise continuity data, rational lower-bound folding, $\UniformModulusUp$ output, finite windows, $\RegSeqRatUp$ readback, and the terminal $\RealUp$ seal remain one displayed route rather than a local Arzela--Ascoli row alone.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:arzela-ascoli-finite-modulus-carrier}. The selector-facing route exposes compact source data, continuous-family data, the finite modulus row, finite windows, regular rational readback, the Real seal, and the structural transport, replay, provenance, and naming rows.

Now read \autoref{ch:concrete-instances-compact-uniform-modulus-namecert}. Its $\CompactUniformModulusUp$ finite-net fold supplies the compact-domain coverage and tolerance-only modulus handoff consumed before the Arzela--Ascoli finite modulus can be exported.

Finally read the compact-uniform-continuity ledger route through \autoref{ch:concrete-instances-compact-uniform-continuity-ledger-namecert}. That ledger keeps the compact finite-net, pointwise modulus, rational lower-bound fold, and $\UniformModulusUp$ handoff visible to the same selector-facing consumer. Since no carrier row supplies host function-space compactness, selected subsequence choice, quotient real equality, or ambient completeness, the dependency is a displayed finite-modulus route rather than a background compactness assumption.
\end{proof}
