The $\ArzelaAscoliFiniteModulusUp$ dependency lattice consumes the finite-net rows of $\CompactMetricUp$ through \autoref{ch:concrete-instances-compactmetric-namecert}, the pointwise-to-uniform modulus rows of $\CompactUniformContinuityUp$ through \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, and the tolerance schedules of $\CauchyModulusUp$ through \autoref{ch:concrete-instances-cauchymodulus-namecert}.

The route is a sibling ledger for compactness and uniform-modulus consumers; it does not add a theorem body to the hub, choose a subcover, select a modulus from an ambient proof, or assert compact-open completeness.
