\begin{theorem}[Regular Cauchy minimum-modulus forward sibling route]
\label{thm:regular-cauchy-min-modulus-forward-sibling-route}
Every downstream read of $\RegularCauchyMinModulusUp$ by the selector of \autoref{ch:concrete-instances-cauchy-subsequence-selection-modulus-namecert} must first expose the $\CauchyModulusMeetUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows already fixed by this chapter. In carrier notation, the selector may consume the minimum row only after the route
$$
S_0,S_1,M,m,W,Q,D,E,H,C,P,N
$$
has displayed $M$ as the meet surface, $D$ as the dyadic tolerance ledger, $W$ as the finite stream-window service, $Q$ as the regular-rational readback row, and $E$ as the Real-facing seal.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:regular-cauchy-min-modulus-carrier}. It displays $S_0,S_1,M,m,W,Q,D,E,H,C,P,N$ as one finite packet, so the downstream selector cannot read $m$ apart from the meet, window, readback, dyadic, seal, transport, replay, provenance, and naming rows.

Apply the NameCert obligations of \autoref{thm:regular-cauchy-min-modulus-namecert-obligations}. They force meet compatibility through $M$, finite minimum exactness through $m$ and $D$, and readback stability through $W$ and $Q$ before any consumer treats the minimum schedule as accepted data.

Apply the L10 route of \autoref{thm:regular-cauchy-min-modulus-l10-route} and the positive-gap theorem of \autoref{thm:regular-cauchy-min-modulus-positive-gap}. These results place the displayed minimum before the $\RealUp$ seal while keeping the $\LocatednessModulusUp$ and $\RegularCauchyWindowInductionUp$ context visible.

Hence a downstream Cauchy-subsequence selector receives only the already named finite route. It receives no quotient stream equality, selected limit, hidden completion object, countable-choice selector, or ambient $\RealUp$ completeness principle.
\end{proof}
