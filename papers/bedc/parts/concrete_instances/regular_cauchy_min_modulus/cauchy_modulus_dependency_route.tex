Every $\RegularCauchyMinModulusUp$ consumer reads the modulus comparison rows of \autoref{ch:concrete-instances-cauchymodulus-namecert} before it may normalize a source tail request into the minimum-modulus row. The same route then consumes the regular-sequence handoff of \autoref{ch:concrete-instances-regseqrat-namecert}, so RealUp completion consumers inspect regular readback data only after the CauchyModulus request schedule has been displayed. The dependency supplies source request normalization and regular readback for the minimum row; it supplies no quotient stream equality, selected limit, hidden completion object, countable choice, or ambient $\RealUp$ completeness.
