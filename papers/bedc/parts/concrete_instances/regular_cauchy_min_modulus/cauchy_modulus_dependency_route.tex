Every $\RegularCauchyMinModulusUp$ consumer reads the modulus comparison rows of \autoref{ch:concrete-instances-cauchymodulus-namecert} and the regular-sequence handoff of \autoref{ch:concrete-instances-regseqrat-namecert} before its minimum-modulus row can be exposed to RealUp completion consumers; the route supplies no quotient stream equality, selected limit, hidden completion object, countable choice, or ambient $\RealUp$ completeness.
