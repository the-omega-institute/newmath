The Real-facing route for $\RegularCauchyMinModulusUp$ is a finite dependency route through the $\RegSeqRatUp$ readbacks of \autoref{ch:concrete-instances-regseqrat-namecert}, the $\DyadicRatCoreUp$ tolerance rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the $\StreamNameUp$ finite windows of \autoref{ch:concrete-instances-streamname-namecert}, and the terminal $\RealUp$ sealing surface of \autoref{ch:concrete-instances-real-namecert}. A consumer may read the minimum-modulus row only after those displayed rows are present in the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ route; it receives no quotient stream equality, selected limit, countable-choice schedule, or ambient $\RealUp$ completeness coordinate.
