\chapter{A Concrete Naming Certificate for $\FrechetUrysohnCompletionUp$}
\label{ch:concrete-instances-frechet-urysohn-completion-namecert}
\origin{human}

Following Frechet and Urysohn, the $\FrechetUrysohnCompletionUp$ packet records the completion-facing sequential-closure interface as finite BEDC NameCert data. It sits after the Frechet--Urysohn carrier of \autoref{ch:concrete-instances-frechet-urysohn-namecert}, the sequential-space interface of \autoref{ch:concrete-instances-sequential-space-namecert}, the complete-metric service of \autoref{ch:concrete-instances-completemetric-namecert}, the regular-sequence readback of \autoref{ch:concrete-instances-regular-sequence-space-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names the displayed sequential completion route; it does not assert quotient sequence equality, selected representatives, countable choice, ambient Real completeness, or a host completion theorem.

\begin{definition}[Frechet--Urysohn completion carrier]
\label{def:frechet-urysohn-completion-carrier}
A $\FrechetUrysohnCompletionUp$ carrier is a finite $\BHist$ packet
$$
F=(U,S,M,R,E,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $U$ is the accepted $\FrechetUrysohnUp$ source row, carrying the topological sequential-closure route.
\item $S$ is the $\SequentialSpaceUp$ source row, exposing the sequence-indexed convergence interface read by the completion consumer.
\item $M$ is the $\CompleteMetricUp$ service row, recording only displayed Cauchy-window admission, limit-service requests, and convergence-distance reads.
\item $R$ is the $\RegularSequenceSpaceUp$ readback row attached to the same displayed sequence windows.
\item $E$ is the terminal $\RealUp$ seal row, available only after $U,S,M,R$ have been displayed.
\item $A$ is the completion-admission row tying the Frechet--Urysohn sequential closure read to the complete-metric service and regular-sequence readback.
\item $H$ records componentwise $\hsame$ transport for source, sequential, complete-metric, regular-sequence, seal, admission, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from each completion-facing request through $U,S,M,R,E,A,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\FrechetUrysohnCompletionUp$, $\FrechetUrysohnUp$, $\SequentialSpaceUp$, $\CompleteMetricUp$, $\RegularSequenceSpaceUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\FrechetUrysohnCompletionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $U,S,M,R,E,A,H,C,P,N$. It has no coordinate for quotient sequence equality, selected subsequences, chosen completion representatives, countable choice, ambient Real completeness, or a host completion object.
\end{definition}

\begin{theorem}[Frechet--Urysohn completion NameCert obligations]
\label{thm:frechet-urysohn-completion-namecert-obligations}
For every accepted $\FrechetUrysohnCompletionUp$ carrier
$$
F=(U,S,M,R,E,A,H,C,P,N),
$$
the local $\NameCert_{\FrechetUrysohnCompletionUp}$ surface consists exactly of Frechet--Urysohn source admission through $U$, sequential-source admission through $S$, complete-metric service exposure through $M$, regular-sequence readback through $R$, terminal Real-seal ordering through $E$, completion admission through $A$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:frechet-urysohn-completion-carrier}. The source, sequential, complete-metric, regular-sequence, Real-seal, and admission rows are the only rows visible before transport and provenance are read.

The source row $U$ supplies the Frechet--Urysohn sequential-closure route, while $S$ supplies the displayed sequence-indexed convergence interface. The completion-facing consumer cannot reach the complete-metric service row $M$ or the regular-sequence readback row $R$ without those two displayed sources.

The seal row $E$ is downstream of $U,S,M,R$. The rows $A,H,C,P,N$ then bind the completion request to the same route, transport it componentwise, replay it, source it, and name it locally.

Since the carrier has no quotient sequence, selected representative, choice, ambient completeness, or host completion coordinate, none of those objects can be an obligation of the local certificate.
\end{proof}

\begin{theorem}[Frechet--Urysohn completion sequential density]
\label{thm:frechet-urysohn-completion-sequential-density}
Every completion-facing sequential-density read exported by an accepted $\FrechetUrysohnCompletionUp$ packet factors through the displayed route
$$
\FrechetUrysohnUp,\ \SequentialSpaceUp
\longmapsto
\CompleteMetricUp,\ \RegularSequenceSpaceUp
\longmapsto
\RealUp
\longmapsto
A,H,C,P,N .
$$
Thus the packet distinguishes Frechet--Urysohn sequential closure from an ambient completeness theorem: it exposes the finite sequence windows, complete-metric service rows, regular-sequence readback, and terminal Real seal before a density consumer may read the completion-facing admission row.
\end{theorem}
\begin{proof}
Begin with the NameCert obligations of \autoref{thm:frechet-urysohn-completion-namecert-obligations}. They force any completion-facing read to display the Frechet--Urysohn source row $U$ and the sequential-space row $S$ before the complete-metric or regular-sequence rows are available.

Read $M$ and $R$ next. The complete-metric service supplies only displayed Cauchy-window and limit-service data, while the regular-sequence row records the finite sequence readback attached to the same displayed windows.

The terminal Real seal $E$ is then consumed as a seal, not as a proof of ambient Real completeness. The admission and structural rows $A,H,C,P,N$ preserve the route for the density consumer.

Therefore sequential density in this packet is a finite displayed handoff from Frechet--Urysohn closure to completion-facing readback. It is not quotient stream equality, a selected dense subsequence, a chosen completion representative, countable choice, or a general completeness theorem.
\end{proof}

\falsifiablePrediction{Every accepted $\FrechetUrysohnCompletionUp$ completion-facing read factors through a FrechetUrysohn source row, a SequentialSpace row, a CompleteMetric service row, a RegularSequenceSpace readback row, a terminal RealUp seal, completion admission, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{frechet-urysohn, sequential-space, completemetric, regular-sequence-space, real: $\FrechetUrysohnCompletionUp$ is not bijective to any listed sibling because it binds sequential closure, completion service, regular-sequence readback, Real sealing, transport, replay, provenance, and naming as one completion-facing packet.}

\closureat{FrechetUrysohnCompletionUp}{seedStr}

\begin{closurestatus}{\FrechetUrysohnCompletionUp}
  \constructivestory{A $\FrechetUrysohnCompletionUp$ packet is generated from a FrechetUrysohn source row, a SequentialSpace source row, a CompleteMetric service row, a RegularSequenceSpace readback row, a terminal RealUp seal, completion admission, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:frechet-urysohn-completion-carrier}, \autoref{thm:frechet-urysohn-completion-namecert-obligations}, and \autoref{thm:frechet-urysohn-completion-sequential-density} seed the completion-facing sequential-density route over Frechet--Urysohn, SequentialSpace, CompleteMetric, RegularSequenceSpace, Real-seal, admission, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient sequence equality, selected subsequences, chosen completion representatives, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Frechet--Urysohn source exactness, SequentialSpace source routing, CompleteMetric service ordering, RegularSequenceSpace readback, Real-seal ordering, completion admission, transport, replay, provenance, and local naming for BEDC.Derived.FrechetUrysohnCompletionUp.}
\end{closurestatus}
