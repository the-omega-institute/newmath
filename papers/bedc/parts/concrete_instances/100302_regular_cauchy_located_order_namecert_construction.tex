\chapter{A Concrete Naming Certificate for $\RegularCauchyLocatedOrderUp$}
\label{ch:concrete-instances-regular-cauchy-located-order-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RegularCauchyLocatedOrderUp$ packet names a finite located-comparison surface for two regular Cauchy rational names before any completed-real order principle is exposed. It sits over the regular rational rows of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the Real seal boundary of \autoref{ch:concrete-instances-real-namecert}, the regular Cauchy separation route of \autoref{ch:concrete-instances-regularcauchyseparation-namecert}, and the apartness-real boundary of \autoref{ch:concrete-instances-apartnessreal-namecert}. The certificate records a located order read as finite NameCert data; it does not import constructive-real trichotomy, host real equality, quotient stream equality, selected limits, countable choice, or ambient completeness.

\begin{definition}[Regular Cauchy located order carrier]
\label{def:regular-cauchy-located-order-carrier}
A $\RegularCauchyLocatedOrderUp$ carrier is a finite $\BHist$ packet
$$
O=(L,R,W,D,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ is the left $\RegSeqRatUp$ regular rational name row.
\item $R$ is the right $\RegSeqRatUp$ regular rational name row.
\item $W$ is the common $\StreamNameUp$ finite-window ledger shared by the left and right observations.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row recording the dyadic radius, slack, and rational comparison budget used by the located read.
\item $Q$ is the located comparison row, displaying the finite evidence that the inspected windows place the two names to the left, to the right, or still inside the unresolved tolerance band named by $D$.
\item $E$ is the $\RealUp$ seal row reached only after $L,R,W,D,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the two regular rational names, the common window, dyadic tolerance row, located comparison row, Real seal, continuation, provenance, and local naming row.
\item $C$ records displayed $\Cont$ replay from each located-order request through $L,R,W,D,Q,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyLocatedOrderUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\RegularCauchySeparationUp$, $\ApartnessRealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyLocatedOrderUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows and their displayed transports. It has no coordinate for a host order on completed reals, quotient stream equality, trichotomy, a selected limit, countable choice, Real completeness, or a standard bridge.
\end{definition}
\leanchecked{BEDC.Derived.RegularCauchyLocatedOrderUp.RegularCauchyLocatedOrderUp\_single\_carrier\_alignment}

\begin{theorem}[Regular Cauchy located order NameCert obligations]
\label{thm:regular-cauchy-located-order-namecert-obligations}
For every accepted carrier $O=(L,R,W,D,Q,E,H,C,P,N)$ of \autoref{def:regular-cauchy-located-order-carrier}, the local $\NameCert_{\RegularCauchyLocatedOrderUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the left and right $\RegSeqRatUp$ rows $L,R$, the shared $\StreamNameUp$ window row $W$, the $\DyadicRatCoreUp$ tolerance row $D$, the located comparison row $Q$, the $\RealUp$ seal row $E$, and the structural rows $H,C,P,N$;
\item classifier stability, requiring every accepted located-order read to inspect the same displayed route $L,R,W,D,Q$ before the Real seal row is exposed;
\item common-window transport, requiring $\hsame$ transport through $H$ and replay through $C$ to preserve the same left and right observation windows named by $W$;
\item comparison-row exactness, requiring the left, right, and unresolved-band outcomes to be read only from the finite dyadic comparison data stored in $D$ and $Q$;
\item Real-seal handoff, requiring $E$ to be consumed only after the source names, common window, dyadic tolerance, and located comparison rows have been retained;
\item ledger refusal, excluding trichotomy, host real order, quotient stream equality, selected limits, countable choice, ambient completeness, Lean verification, and bridge checking from the local NameCert surface.
\end{enumerate}
No obligation row is stored outside the displayed packet.
\end{theorem}
\begin{proof}
Project the carrier. The rows $L,R,W,D,Q,E$ are the analytic coordinates, while $H,C,P,N$ transport, replay, source, and name the same finite packet.

Read the classifier in route order. A located-order request first reads the two regular rational names $L$ and $R$, then aligns them over the common finite window $W$, then consults the dyadic tolerance ledger $D$, and only then reads the comparison row $Q$.

The common-window condition follows from the structural rows. Componentwise $\hsame$ in $H$ preserves the left and right windows together, and $\Cont$ replay in $C$ repeats the same order of reads rather than allowing a detached comparison row.

The Real-facing row is downstream of the comparison route. Since $E$ appears only after $L,R,W,D,Q$, any consumer that bypasses those rows has not obtained the displayed packet's Real seal boundary.

The excluded principles have no carrier coordinate. Thus the listed carrier admission, classifier stability, common-window transport, comparison exactness, Real-seal handoff, and ledger-refusal rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy located order Real seal boundary]
\label{thm:regular-cauchy-located-order-real-seal-boundary}
For every accepted $\RegularCauchyLocatedOrderUp$ packet $O=(L,R,W,D,Q,E,H,C,P,N)$, every Real-facing read factors through the finite route
$$
(L,R)\longmapsto W\longmapsto D\longmapsto Q
$$
before reaching the $\RealUp$ seal boundary $E$. Consequently the packet exports no trichotomy, host real equality, quotient stream, selected limit, countable choice, or $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start from the obligation surface of \autoref{thm:regular-cauchy-located-order-namecert-obligations}. Carrier admission exposes the seal row only as part of the displayed finite packet, and classifier stability places every located-order read on the route through $L,R,W,D,Q$.

The comparison row is the last analytic gate before the seal boundary. It reads only the dyadic tolerance and rational comparison data already stored in $D$ over the common window $W$, so it cannot select a completed real, quotient a stream, or invoke a host order.

Finally consume $E$ as a boundary row. The transport, continuation, provenance, and local naming rows preserve the same route, so any Real-facing read must pass through the finite located-comparison packet before it reaches the seal.
\end{proof}

\begin{closurestatus}{\RegularCauchyLocatedOrderUp}
  \constructivestory{A $\RegularCauchyLocatedOrderUp$ packet is generated from left and right RegSeqRat rows, a shared StreamName window, a DyadicRatCore tolerance ledger, a located comparison row, a RealUp seal boundary, componentwise hsame transport, continuation replay, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-located-order-carrier}, \autoref{thm:regular-cauchy-located-order-namecert-obligations}, and \autoref{thm:regular-cauchy-located-order-real-seal-boundary} seed the finite located-order boundary for regular Cauchy rational names over $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\RegularCauchySeparationUp$, and $\ApartnessRealUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim full $\RealAlgOrderUp$, trichotomy, host real equality, quotient stream equality, selected limits, countable choice, Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier stability, common-window transport, comparison-row exactness, Real-seal handoff, and ledger refusal for the displayed located-order packet.}
\end{closurestatus}
