The $\CalculusUp$ derivative-facing route reads finite computations through \autoref{ch:concrete-instances-finite-difference-namecert} before using the derivative carrier of \autoref{ch:concrete-instances-derivative-namecert}. Its Real-facing rows remain tied to the finite $\StreamNameUp$ windows of \autoref{ch:concrete-instances-streamname-namecert}, the $\DyadicRatCoreUp$ tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the $\RegSeqRatUp$ readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}.

Consequently the finite-difference derivative route is a downstream consumer of the L10 real-completion spine before any derivative endpoint is read: the dyadic tolerance rows of \autoref{ch:concrete-instances-dyadicratcore-namecert} feed the regular rational sequence rows of \autoref{ch:concrete-instances-regseqrat-namecert}, and those rows feed the real-completion seal of \autoref{ch:concrete-instances-real-namecert}. The derivative carrier may inspect finite differences only after that spine has supplied the tolerance-visible $\RealUp$ endpoint.
