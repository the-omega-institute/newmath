\begin{theorem}[Calculus real and continuous-map dependency route]
\label{thm:calculus-real-continuous-dependency-route}
Every $\CalculusUp$ derivative, integral, or limit-facing consumer that reads a scalar endpoint or function-value row must expose the $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert} and the $\ContinuousMapUp$ modulus-and-evaluation surface of \autoref{ch:concrete-instances-continuousmap-namecert} before the calculus packet can replay the read through its local $\NameCert$ row. Thus the calculus hub records a substantive L10-to-analysis edge through Real names and continuous-map data rather than a detached kernel-only route.
\end{theorem}
