The $\CalculusUp$ limit and endpoint consumers that reuse a Real-completion diagonal estimate read the shared-threshold packet of \autoref{ch:concrete-instances-regular-cauchy-diagonal-meet-namecert} before exporting a Real-facing calculus row. That sibling packet exposes the route through the shared modulus, regular-Cauchy tail meet, witness envelope, diagonal-window coverage, and comparison row; calculus therefore receives a displayed finite diagonal-meet boundary rather than a detached host limit, quotient stream equality, or selected completion witness.
