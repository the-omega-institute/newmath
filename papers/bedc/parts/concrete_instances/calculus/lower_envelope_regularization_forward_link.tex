\begin{lemma}[Calculus lower-envelope Real-facing route]
\label{lem:calculus-lower-envelope-real-facing-route}
Every $\CalculusUp$ derivative, integral, or variation consumer that reads a lower-envelope regularization as a Real-facing value must route first through \autoref{ch:concrete-instances-lowersemicontinuous-namecert}, then through \autoref{ch:concrete-instances-epigraph-namecert}, then through the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and finally through the terminal seal of \autoref{ch:concrete-instances-real-namecert}. Thus the calculus packet sees the lower-envelope value only after the finite lower-semicontinuity, epigraph, regular-rational, and Real rows are displayed, with no route through host suprema, quotient functions, selected minimizers, or ambient $\RealUp$ completeness.
\end{lemma}
\leanchecked{BEDC.Derived.CalculusUp.CalculusLowerEnvelopeRealFacingRoute}
\begin{proof}
Read the lower-envelope request as a calculus-facing value. The lower-semicontinuity row supplies the admissible lower-envelope boundary, and the epigraph row supplies the displayed value/lower-bound comparison used by that boundary.

Next pass to regular rational data. The $\RegSeqRatUp$ route records the finite readback through visible windows, so the calculus consumer has a concrete value surface before it can inspect any Real seal.

Finally read the $\RealUp$ seal. Since the route has already passed through the displayed lower-semicontinuity, epigraph, and regular-rational rows, no derivative or integral consumer can replace it by a host supremum, quotient-function equality, selected minimizer, or ambient completeness principle.
\end{proof}

\begin{theorem}[Calculus lower-envelope regularization handoff]
\label{thm:calculus-lower-envelope-regularization-handoff}
Any $\CalculusUp$ derivative, integral, or variation consumer that imports lower-envelope regularization must preserve the finite route
$$
\LowerSemicontinuousUp,\EpigraphUp,\RegSeqRatUp,\DyadicRatCoreUp,\RealUp
$$
before any calculus operation reads the regularized value. The consumer first displays the lower-semicontinuity obligation and epigraph comparison rows, then reads the regular rational approximation together with the $\DyadicRatCoreUp$ tolerance ledger, and only then consumes the terminal $\RealUp$ seal. The handoff exports no host supremum, quotient function, selected minimizer, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Begin with the route lemma above. It already forces a calculus-facing lower-envelope value through $\LowerSemicontinuousUp$, $\EpigraphUp$, $\RegSeqRatUp$, and $\RealUp$ before the value is visible to a derivative, integral, or variation consumer.

Next use the lower-envelope regularization packet of \autoref{ch:concrete-instances-lower-envelope-regularization-namecert}. Its NameCert obligations keep the finite lower-envelope ledger between the epigraph row and the regular-rational readback, while its L10 handoff keeps the regularized value inside the displayed envelope route.

Finally insert the shared dyadic tolerance discipline. The regularization carrier records the finite comparison ledger read by the regular-rational row, and downstream calculus consumers must retain that $\DyadicRatCoreUp$ tolerance surface before they read the $\RealUp$ seal.

The resulting handoff is the listed finite route. Since the cited rows contain no coordinate for host suprema, quotient functions, selected minimizers, or ambient completeness, no calculus operation can recover those objects from the handoff.
\end{proof}
