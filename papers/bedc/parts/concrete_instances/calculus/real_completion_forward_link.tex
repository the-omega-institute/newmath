The $\CalculusUp$ real-completion route is read as a child of the existing limit handoff. The endpoint surface of \autoref{thm:calculus-limit-dependency-route-real-handoff} and \autoref{thm:calculus-limit-real-terminal-export} already fixes the finite readback order through $\LimitUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before any calculus consumer can export a Real-facing value.

\begin{theorem}[Calculus Real-completion forward link]
\label{thm:calculus-real-completion-forward-link}
Every $\CalculusUp$ Real-facing endpoint export that consumes a $\LimitUp$ row must pass through the $\RealUp$ terminal seal, the $\RegSeqRatUp$ regular rational readback row, the $\StreamNameUp$ schedule row, and the $\DyadicRatCoreUp$ tolerance row displayed by \autoref{thm:calculus-limit-dependency-route-real-handoff} and \autoref{thm:calculus-limit-real-terminal-export}. The forward link is the finite L10 route from calculus limits into the Real-completion spine of \autoref{ch:concrete-instances-real-namecert}; it is not a host limit operator, quotient stream equality, selected representative, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Read the endpoint export through \autoref{thm:calculus-limit-dependency-route-real-handoff}. That theorem already places every derivative, integral, or endpoint consumer behind the $\LimitUp$ surface together with the $\RealUp$ terminal seal, $\RegSeqRatUp$ readback, and $\StreamNameUp$ schedule rows.

Now apply \autoref{thm:calculus-limit-real-terminal-export}. It inserts the $\DyadicRatCoreUp$ tolerance row before the regular readback is sealed as a Real-facing value, so the downstream calculus export cannot skip the displayed real-completion readback order.

Since the route has no coordinate for a host limit operator, quotient stream equality, selected representative, or ambient completeness theorem, the child link records only the finite L10 dependency lattice already visible in the sibling handoff theorems.
\end{proof}
