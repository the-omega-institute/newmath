\begin{theorem}[Calculus real-completion CauchySpace dependency link]
\label{thm:calculus-real-completion-dependency-link}
Every accepted $\CalculusUp$ derivative, integral, or limit consumer that reaches Real-facing completion data must pass through the $\CauchySpaceUp$ filter-uniform and regular-sequence boundary before the terminal $\RealUp$ seal is read. Concretely, the existing route through \autoref{thm:calculus-real-completion-sibling-route} factors through the Cauchy-space consumer surface of \autoref{ch:concrete-instances-cauchyspace-namecert}: the $\CauchyFilterUp$ and $\UniformSpaceUp$ rows expose the completion request, the $\RegSeqRatUp$ row supplies the regular rational readback, the $\StreamNameUp$ row supplies any displayed sequential name, and only then may the $\RealUp$ seal be consumed.
\end{theorem}
\begin{proof}
Start with a Real-facing $\CalculusUp$ consumer and read it through \autoref{thm:calculus-real-completion-sibling-route}. That route already forces the endpoint through the $\RealUp$, $\RegSeqRatUp$, and $\StreamNameUp$ completion stack rather than through ambient completeness.

Now insert the Cauchy-space boundary supplied by \autoref{ch:concrete-instances-cauchyspace-namecert}. Its Real-consumer rows require the displayed filter, uniform, regular-name, precompletion, transport, replay, provenance, and local naming data before a Real-facing consumer can inspect completion data.

Thus the calculus endpoint cannot jump directly from a local derivative, integral, or limit row to a Real seal. It first traverses the Cauchy-space filter-uniform and regular-sequence interface, and the route exports no host quotient completion, selected limit, countable-choice selector, or ambient $\RealUp$ completeness theorem.
\end{proof}
