The $\CalculusUp$ product-distance route is read through the Cauchy product metric packet before derivative-product or endpoint consumers expose any paired Real data. This child route records that the calculus hub uses the finite product-metric surface of \autoref{ch:concrete-instances-cauchy-product-metric-namecert} rather than a host product metric.

\begin{theorem}[Calculus product metric forward link]
\label{thm:calculus-product-metric-forward-link}
Every accepted $\CalculusUp$ derivative-product, endpoint, or paired-distance consumer that uses product-distance data must first display the $\CauchyProductMetricUp$ packet through $\CauchyProductUp$, $\MetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ before reading either component as a $\RealUp$ value. The route supplies the finite product-distance ledger, paired stream windows, regular rational readbacks, dyadic tolerance coordinates, metric distance row, and componentwise terminal Real seals. It supplies no host product metric, quotient stream equality, selected product limit, detached Real carrier, detached Real pair, or ambient Real completeness principle.
\end{theorem}
\begin{proof}
Begin with the real handoff of \autoref{thm:calculus-limit-dependency-route-real-handoff}. It already forces endpoint reads through $\LimitUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before a calculus consumer may expose a Real-facing value.

Now insert the product-distance sibling. The $\CauchyProductMetricUp$ carrier of \autoref{ch:concrete-instances-cauchy-product-metric-namecert} reads two $\CauchyProductUp$ rows, the $\MetricUp$ distance row, paired stream windows, regular rational readbacks, the $\DyadicRatCoreUp$ product-distance ledger, and the terminal $\RealUp$ seal.

The terminal Real seal is componentwise. A paired-distance read may expose the left component and the right component only after the product-metric sibling has displayed the two carried streams, their dyadic comparison ledger, and the Real-facing seals for those same coordinates. It cannot replace that route by a detached host product metric or a selected product limit whose components are later read as independent Real values.

Therefore a derivative-product, endpoint, or paired-distance consumer can use product-distance data only after that finite route is visible. Since no row in the calculus packet or the product-metric sibling carries a host product metric, quotient stream equality, selected product limit, detached Real carrier, detached Real pair, or ambient completeness theorem, the forward link remains a BEDC dependency route.
\end{proof}

\begin{theorem}[Calculus product metric finite modulus route]
\label{thm:calculus-product-metric-forward-modulus-route}
Any $\CalculusUp$ paired-distance read that reaches component $\RealUp$ seals must expose the finite product-metric modulus rows before those component seals are read. Concretely, the read factors through the $\CauchyProductMetricUp$ route of \autoref{ch:concrete-instances-cauchy-product-metric-namecert}, the two $\CauchyProductUp$ source rows, paired $\StreamNameUp$ windows, paired $\RegSeqRatUp$ readbacks, the $\DyadicRatCoreUp$ product-distance ledger, the $\MetricUp$ distance row, and only then the terminal component $\RealUp$ seals. It does not factor through a host product metric, quotient stream equality, ambient completeness, or a detached product Real.
\end{theorem}
\begin{proof}
Start with the forward link of \autoref{thm:calculus-product-metric-forward-link}. It already makes the product-distance sibling visible before any calculus derivative-product, endpoint, or paired-distance consumer can inspect component Real data.

Next read the product-metric sibling through \autoref{thm:cauchy-product-metric-real-seal-boundary}. That boundary orders the route by the two product sources, paired stream windows, regular rational readbacks, dyadic product-distance ledger, metric row, and terminal Real seal. Thus the finite modulus rows are not optional decoration: they are the rows through which the paired distance is displayed before the component seals are available.

The excluded coordinates are the same on both sides of the handoff. Neither the calculus route nor the $\CauchyProductMetricUp$ route contains a host product metric, quotient stream equality, selected product limit, detached product Real, or ambient completeness witness. Therefore every accepted paired-distance read keeps the finite modulus route visible before it reads the component $\RealUp$ seals.
\end{proof}
