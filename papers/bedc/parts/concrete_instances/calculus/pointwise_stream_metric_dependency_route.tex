\begin{theorem}[Calculus pointwise stream metric dependency route]
\label{thm:calculus-pointwise-stream-metric-dependency-route}
Every accepted $\CalculusUp$ derivative, integral, or local-error consumer that compares real-valued stream data must first read the pointwise metric-stream packet of \autoref{ch:concrete-instances-pointwise-stream-metric-namecert}. The comparison route consumes the displayed $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance ledger, $\RegSeqRatUp$ readback, terminal $\RealUp$ seal, and metric row before derivative or integral estimates may treat the local error as a real-valued bound.
\end{theorem}
\leanchecked{BEDC.Derived.CalculusUp.CalculusPointwiseStreamMetricDependencyRoute}
\begin{proof}
Project the calculus consumer through its derivative, integral, or local limit row. A local error comparison needs a pointwise distance before it can be read as a Real-facing estimate.

Use \autoref{ch:concrete-instances-pointwise-stream-metric-namecert}. Its carrier displays the stream source, finite pointwise windows, dyadic tolerance ledger, regular-rational readback, terminal Real seal, and metric row before any downstream completion or calculus consumer receives the distance packet.

Thus the calculus route reads pointwise stream metric control before derivative and integral consumers compare real-valued local errors. The dependency exports no host function equality, quotient stream equality, selected representative, countable-choice schedule, or ambient $\RealUp$ completeness principle.
\end{proof}
