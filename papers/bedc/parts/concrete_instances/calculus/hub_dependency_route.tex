The $\CalculusUp$ hub consumes its Real-facing dependency route through the existing BEDC name certificates for \autoref{ch:concrete-instances-real-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, and \autoref{ch:concrete-instances-dyadicratcore-namecert}. Its paired-distance consumers enter the product-metric child route of \autoref{thm:calculus-product-metric-forward-link} before exposing any derivative-product, endpoint, or component $\RealUp$ seal, so the hub records a horizontal dependency on those finite route packets rather than a bare list of local children.
