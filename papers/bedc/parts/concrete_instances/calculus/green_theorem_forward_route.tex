The $\CalculusUp$ plane-boundary branch reads vector-calculus integration through the Green theorem certificate before treating the result as a calculus-facing Riemann-integral dependency. This keeps the planar region, boundary curve, differential-form row, scalar curl or divergence row, and tagged integration handoff visible as a finite route.

Thus any $\CalculusUp$ consumer of plane boundary integration passes through \autoref{ch:concrete-instances-green-theorem-namecert} and \autoref{ch:concrete-instances-stokes-theorem-namecert} before reading the $\RiemannIntegralUp$ packet of \autoref{ch:concrete-instances-riemannintegral-namecert}.
