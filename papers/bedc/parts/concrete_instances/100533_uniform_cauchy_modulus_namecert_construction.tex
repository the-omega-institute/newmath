\chapter{A Concrete Naming Certificate for $\UniformCauchyModulusUp$}
\label{ch:concrete-instances-uniform-cauchy-modulus-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\UniformCauchyModulusUp$ packet records the finite shared Cauchy-window budget needed by the Real and completion route before any quotient stream, selected limit, or ambient completeness theorem is available. It sits between the selected-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the endpoint seal of \autoref{ch:concrete-instances-real-namecert}. It also reads the shared-bound interface of \autoref{ch:concrete-instances-cauchymodulusmeet-namecert} and the filter-side uniformity route of \autoref{ch:concrete-instances-uniform-cauchy-filter-namecert}.

\begin{definition}[Uniform Cauchy modulus carrier]\label{def:uniform-cauchy-modulus-carrier}
A $\UniformCauchyModulusUp$ carrier is a finite $\BHist$ packet
$$
U=(S,R,D,M,W,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the selected $\StreamNameUp$ window row whose finite stream observations are visible to the packet;
\item $R$ is the $\RegSeqRatUp$ readback row attached to the selected windows in $S$;
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger recording every requested rational radius and comparison budget;
\item $M$ is the shared Cauchy-window modulus row: one displayed budget assigns a finite tail window to each requested dyadic tolerance;
\item $W$ is the finite witness row saying that all accepted pairs of selected windows beyond the budget in $M$ are read inside the tolerance recorded in $D$;
\item $E$ is the $\RealUp$ handoff row exposed only after $S,R,D,M,W$ have been displayed;
\item $H$ stores componentwise $\hsame$ transport for the stream windows, rational readbacks, dyadic ledgers, shared modulus, witness row, handoff, provenance, and local naming rows;
\item $C$ stores displayed $\Cont$ replay from selected windows to regular readback, from regular readback to dyadic tolerance accounting, from the tolerance ledger to the shared modulus, and from the witness row to the $\RealUp$ handoff;
\item $P$ is the $\Pkg$ provenance over $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchyModulusMeetUp$, $\UniformCauchyFilterUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\UniformCauchyModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,R,D,M,W,E,H,C,P,N$. It has no coordinate for quotient stream equality, selected limits, countable choice, open-cover compactness, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.UniformCauchyModulusUp.UniformCauchyModulusUpTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Uniform Cauchy modulus NameCert obligations]\label{thm:uniform-cauchy-modulus-namecert-obligations}
For every accepted $\UniformCauchyModulusUp$ carrier $U=(S,R,D,M,W,E,H,C,P,N)$, the local $\NameCert_{\UniformCauchyModulusUp}$ surface consists exactly of five obligation rows:
\begin{enumerate}
\item carrier admission for the displayed selected-window, regular-readback, dyadic-ledger, shared-modulus, witness, handoff, transport, replay, provenance, and naming rows;
\item classifier transport, requiring accepted comparisons to preserve $S,R,D,M,W,E,H,C,P,N$ componentwise and to preserve no hidden completion datum;
\item shared-window budget exactness, requiring the single row $M$ to govern every displayed Cauchy comparison requested by $D$;
\item finite witness replay, requiring each accepted comparison beyond the budget in $M$ to pass through $S$, $R$, $D$, and $W$ before any endpoint-facing handoff is read;
\item $\RealUp$ handoff nonescape, requiring $E$ to consume only the displayed finite route and never quotient stream equality, selected limits, countable choice, open-cover compactness, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.UniformCauchyModulusUp.UniformCauchyModulusTasteGate\_e0\_row\_round\_trip}
\begin{proof}
Carrier admission is projection from \autoref{def:uniform-cauchy-modulus-carrier}. The displayed packet has exactly the rows $S,R,D,M,W,E,H,C,P,N$, so the local certificate has no detached limit selector, quotient stream coordinate, or ambient completeness witness.

Classifier transport is componentwise. The row $H$ stores the permitted $\hsame$ comparisons, while $C$ replays only the displayed route from selected windows through regular readback, dyadic budgets, the shared modulus row, the finite witness row, and the endpoint handoff.

The shared-window budget is unique inside the packet. A tolerance request may ask for many finite window comparisons, but those comparisons all read the same displayed modulus row $M$ before the witness row $W$ can be exported.

Finally, the $\RealUp$ handoff row $E$ is downstream of the same finite route. Since none of the excluded principles is a coordinate of the carrier, none can be exported by the local $\NameCert_{\UniformCauchyModulusUp}$ surface.
\end{proof}

\begin{theorem}[Uniform Cauchy modulus Real handoff]\label{thm:uniform-cauchy-modulus-real-handoff}
Every $\RealUp$ or completion-facing consumer of an accepted $\UniformCauchyModulusUp$ carrier may read only the displayed selected-window row $S$, the $\RegSeqRatUp$ readback row $R$, the $\DyadicRatCoreUp$ tolerance ledger $D$, the shared modulus row $M$, the finite witness row $W$, and the structural rows $H,C,P,N$ before reading the handoff row $E$. Such a consumer cannot read quotient stream equality, selected limits, countable choice, open-cover compactness, trichotomy, host real equality, or ambient $\RealUp$ completeness from the packet.
\end{theorem}
\begin{proof}
Project the accepted carrier. The endpoint-facing row is $E$, but its admissible inputs are already fixed by the preceding rows: selected windows in $S$, rational readback in $R$, tolerance accounting in $D$, the shared budget in $M$, and finite comparison witnesses in $W$.

Transport, replay, provenance, and local naming do not add an endpoint object. They only preserve the same route through $H,C,P,N$.

Thus every $\RealUp$ or completion-facing read must pass through the finite carrier surface before reaching $E$. Since the carrier has no coordinate for the excluded principles, no such principle can be extracted from this handoff.
\end{proof}

\begin{theorem}[UniformCauchyModulus shared-window budget]\label{thm:uniform-cauchy-modulus-shared-window-budget}
For every accepted $\UniformCauchyModulusUp$ carrier $U=(S,R,D,M,W,E,H,C,P,N)$, a finite precision request must first read the selected $\StreamNameUp$ windows $S$, the $\RegSeqRatUp$ readbacks $R$, the $\DyadicRatCoreUp$ tolerance ledger $D$, the shared modulus row $M$, and the finite witness row $W$. If two endpoint-facing consumers share $M$ and $W$, then their $\RealUp$ handoff row $E$ is reached through the same displayed budget route
$$
S \longrightarrow R \longrightarrow D \longrightarrow M \longrightarrow W \longrightarrow E
$$
as replayed by $H$ and $C$. No quotient stream, selected limit, countable choice, ambient completeness row, host real equality, open-cover compactness, or trichotomy coordinate can enter this budget.
\end{theorem}
\begin{proof}
Project the accepted carrier. The only rows available before the endpoint handoff are the selected-window row $S$, regular readback row $R$, dyadic tolerance row $D$, shared modulus row $M$, finite witness row $W$, transport row $H$, replay row $C$, provenance row $P$, and local naming row $N$.

Induct over the displayed witness row $W$. The empty witness route exposes no endpoint-facing read, so no consumer can reach $E$ without the selected windows, readbacks, tolerance ledger, and shared modulus already displayed. In the step case, the next witness cell is read only after its selected windows have been transported through $H$ and replayed through $C$ along the route from $S$ to $R$, from $R$ to $D$, and from $D$ to $M$.

Now take two endpoint-facing consumers that share $M$ and $W$. The induction gives the same finite witness route for both consumers, and the carrier has only one handoff row $E$ downstream of that route. Hence both consumers reach $E$ through the displayed shared-window budget.

The excluded principles are not coordinates of $U$ and are not produced by $H$ or $C$. Therefore the finite budget cannot import quotient stream equality, a selected limit, countable choice, ambient completeness, host real equality, open-cover compactness, or trichotomy.
\end{proof}

\begin{theorem}[Uniform Cauchy modulus single-route exhaustion]\label{thm:uniform-cauchy-modulus-single-route-exhaustion}
For every accepted $\UniformCauchyModulusUp$ carrier $U=(S,R,D,M,W,E,H,C,P,N)$, any two $\RealUp$ or completion-facing consumers that use the same precision request and the displayed witness row $W$ factor through the same ordered route
$$
S \longrightarrow R \longrightarrow D \longrightarrow M \longrightarrow W \longrightarrow E .
$$
The route is exhausted by the displayed rows of $U$: no second modulus row, detached tail selector, quotient stream equality, selected limit, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness can be inserted between the precision request and the endpoint handoff.
\end{theorem}
\begin{proof}
Project the accepted carrier. By \autoref{def:uniform-cauchy-modulus-carrier}, the endpoint-facing order is already displayed as selected windows $S$, regular rational readbacks $R$, dyadic tolerance accounting $D$, one shared Cauchy-window modulus row $M$, finite witness replay $W$, and handoff row $E$.

The single-row obligation is fixed by \autoref{thm:uniform-cauchy-modulus-namecert-obligations}: accepted comparisons must preserve the same row $M$ for every displayed Cauchy comparison requested by $D$. Thus two consumers with the same precision request cannot split into distinct modulus rows or detached tail selectors before the witness row is read.

Apply \autoref{thm:uniform-cauchy-modulus-shared-window-budget} to the common witness row $W$. Its witness induction gives the same finite route through $S,R,D,M,W$ for both consumers, with transport and replay supplied only by $H$ and $C$.

Finally, \autoref{thm:uniform-cauchy-modulus-real-handoff} gives the nonescape boundary at $E$. Since the excluded principles are not coordinates of the carrier and are not produced by $H$ or $C$, neither consumer can insert quotient stream equality, a selected limit, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness into the route.
\end{proof}

\falsifiablePrediction{Every $\UniformCauchyModulusUp$ endpoint-facing read factors through selected StreamName windows, RegSeqRat readbacks, a DyadicRatCore tolerance ledger, one shared Cauchy-window modulus row, finite witness replay, a RealUp handoff row, hsame transport, Cont replay, Pkg provenance, and local naming.}

\independenceWitness{streamname, regseqrat, dyadicratcore, cauchymodulusmeet, uniformcauchyfilter, real: $\UniformCauchyModulusUp$ is not bijective to any listed sibling because it jointly carries selected windows, regular rational readbacks, dyadic tolerance accounting, one shared finite Cauchy-window budget, witness replay, endpoint handoff, transport, continuation, provenance, and local naming.}

\closureat{UniformCauchyModulusUp}{seedStr}

\begin{closurestatus}{\UniformCauchyModulusUp}
  \constructivestory{A $\UniformCauchyModulusUp$ packet is generated from selected StreamName windows, RegSeqRat readbacks, a DyadicRatCore tolerance ledger, one shared finite Cauchy-window modulus row, finite witness replay, a RealUp handoff row, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:uniform-cauchy-modulus-carrier}, \autoref{thm:uniform-cauchy-modulus-namecert-obligations}, \autoref{thm:uniform-cauchy-modulus-real-handoff}, \autoref{thm:uniform-cauchy-modulus-shared-window-budget}, and \autoref{thm:uniform-cauchy-modulus-single-route-exhaustion} seed the finite shared Cauchy-window budget route for Real and completion consumers.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.UniformCauchyModulusUp.UniformCauchyModulusTasteGate\_e0\_row\_round\_trip}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected limits, countable choice, open-cover compactness, trichotomy, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier transport, shared-window budget exactness, finite witness replay, Real handoff nonescape, and TasteGate field faithfulness for BEDC.Derived.UniformCauchyModulusUp.}
\end{closurestatus}
