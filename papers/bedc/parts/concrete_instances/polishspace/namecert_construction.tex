\chapter{A Concrete Naming Certificate for $\PolishSpaceUp$}
\label{ch:concrete-instances-polishspace-namecert}
\origin{human}

Following Alexandrov and Urysohn, a Polish space is treated here as a complete
separable metric object whose admissible data are displayed by finite naming
routes rather than by ambient set-theoretic selection. The
$\PolishSpaceUp$ packet synthesizes the metric carrier of
\autoref{ch:concrete-instances-metric-namecert}, the completion handoff of
\autoref{ch:concrete-instances-completemetric-namecert}, the countable dense
surface of \autoref{ch:concrete-instances-separablemetric-namecert}, the
finite stream-window interface of
\autoref{ch:concrete-instances-streamname-namecert}, and the regular rational
readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}. Its
role is to expose the L10 complete-separable metric synthesis node without
claiming choice-selected dense points, quotient equality, open-cover
compactness, or a host-level completion principle.

\begin{definition}[Polish-space carrier]
\label{def:polish-space-carrier}
A $\PolishSpaceUp$ carrier is a finite $\BHist$ packet
$$
P=(M,K,D,S,R,W,H,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricSpaceUp$ row containing the admitted point-history
carrier, metric readbacks, distance classifier, and local metric
$\NameCert$.
\item $K$ is the $\CompleteMetricUp$ handoff row; it records the displayed
Cauchy-window and limit-seal route over the same metric carrier.
\item $D$ is the $\SeparableMetricUp$ row; it stores the countable dense
schedule as public finite name data, not as a choice function over arbitrary
balls.
\item $S$ is the $\StreamNameUp$ schedule that enumerates the inspected dense
names and the Cauchy-window requests made by downstream consumers.
\item $R$ is the $\RegSeqRatUp$ readback row used to observe finite rational
windows on the scheduled dense names and Cauchy approximants.
\item $W$ is the finite observation ledger joining requested tolerances,
stream positions, regular rational windows, metric-radius checks, and
completion handoff rows.
\item $H$ records componentwise $\hsame$ transport for $M,K,D,S,R,W$ and for
the local certificate rows.
\item $C$ records the displayed $\Cont$ routes from a metric-radius request to
the dense schedule, from a Cauchy-window request to the completion handoff,
and from either request to the regular rational readback.
\item $G$ is the $\Pkg$ provenance over $\MetricSpaceUp$,
$\CompleteMetricUp$, $\SeparableMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$,
$\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PolishSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only by transporting these displayed
rows to a common metric, complete-metric, separable-metric, stream-name, and
regular-readback surface. It has no coordinate for selected dense points,
quotient equality of completions, arbitrary open covers, or ambient host
sets.
\end{definition}
\leanchecked{BEDC.Derived.PolishSpaceUp.PolishSpaceTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Polish-space NameCert obligations]
\label{thm:polish-space-namecert-obligations}
For every accepted $\PolishSpaceUp$ carrier
$P=(M,K,D,S,R,W,H,C,G,N)$, the local $\NameCert_{\PolishSpaceUp}$ surface
consists exactly of six obligation rows:
\begin{enumerate}
\item carrier admission for the displayed metric, complete-metric,
separable-metric, stream-name, regular-readback, observation-ledger,
transport, continuation, provenance, and local-name rows;
\item classifier transport to a common $\MetricSpaceUp$ surface before either
the complete-metric or separable-metric row is consumed;
\item stability of the completion handoff $K$ and the countable dense schedule
$D$ under the componentwise $\hsame$ transports recorded in $H$;
\item exactness of the observation ledger $W$ as the only source of metric
radius checks, Cauchy windows, dense-name inspections, and regular rational
readbacks;
\item $\Cont$ replay from radius or Cauchy-window requests through $S,R,W$ to
the corresponding metric, completion, or dense-schedule row;
\item $\Pkg$ provenance and local $\NameCert$ naming for exactly the displayed
packet.
\end{enumerate}
No NameCert obligation is stored outside these six rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:polish-space-carrier}. The projection
exposes the metric row, complete-metric handoff, separable-metric dense
schedule, stream schedule, regular rational readback, observation ledger,
transport rows, continuation rows, provenance row, and local name.

The classifier route begins at the metric surface. The rows in $H$ transport
$M,K,D,S,R,W$ together, so the completion handoff and dense schedule are never
compared over different metric carriers.

The observation exactness row is exhausted by $W$. A metric-radius request, a
Cauchy-window request, and a dense-name inspection all read their finite
stream positions and regular rational observations through this ledger.

Finally replay $C$ and attach $G,N$. These rows route, source, and name the
already displayed packet; they do not introduce selected dense points,
quotient completion equality, arbitrary open covers, or ambient host sets.
Hence the six listed rows exhaust the local Polish-space naming obligations.
\end{proof}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceNamecertObligations}

\begin{theorem}[Polish-space root density obligations]
\label{thm:polishspace-root-density-obligations}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. The
root density obligation is the $\SeparableMetricUp$ dense schedule row $D$
read over the same $\MetricSpaceUp$ carrier $M$, through $\StreamNameUp$
windows $S$, $\RegSeqRatUp$ readback $R$, the observation ledger $W$, the
supporting $\BHist$ packet, componentwise $\hsame$ transport $H$, displayed
$\Cont$ replay $C$, $\Pkg$ provenance $G$, and local $\NameCert$ row $N$.
It exports no selected dense point, external enumeration, or host topology.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:polish-space-carrier}. The displayed
rows include $M,D,S,R,W,H,C,G,N$, with $D$ stored as a separable-metric row
beside the metric source $M$.

Apply \autoref{thm:polish-space-namecert-obligations}. Carrier admission and
classifier transport keep $D$ over the transported $\MetricSpaceUp$ surface,
while ledger exactness forces every dense-name inspection through $S,R,W$.

The rows $H,C,G,N$ then transport, replay, source, and name that same finite
route. Since no further coordinate is present, the root density obligation is
exactly the displayed $\BHist$ route and not a selected dense subset or host
enumeration theorem.
\end{proof}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishspaceRootDensityObligations}

\begin{theorem}[Polish-space root completion obligations]
\label{thm:polishspace-root-completion-obligations}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. The
root completion obligation is the $\CompleteMetricUp$ handoff row $K$ over
the same $\MetricSpaceUp$ carrier $M$ used by the dense schedule $D$, with
Cauchy-window requests read through $\StreamNameUp$ windows $S$,
$\RegSeqRatUp$ readback $R$, observation ledger $W$, $\BHist$ support,
componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$
provenance $G$, and local $\NameCert$ row $N$.
\end{theorem}
\begin{proof}
Project $P$ through \autoref{def:polish-space-carrier}. The completion row is
$K$, and its metric source is the same row $M$ that supports the separable
schedule $D$.

Use \autoref{thm:polish-space-namecert-obligations}. The classifier transport
and stability clauses keep $K$ and $D$ over the same transported metric
carrier before either row is consumed.

The observation ledger $W$ supplies the Cauchy-window route through $S$ and
$R$, while $H,C,G,N$ provide transport, replay, provenance, and local naming.
No quotient completion, selected limit, separate host metric, or ambient
completeness principle is present in the root obligation.
\end{proof}
\leanchecked{BEDC.Derived.PolishSpaceUp.PolishSpaceCarrier\_completion\_density\_handoff}

\begin{theorem}[Polish-space root stream readback obligations]
\label{thm:polishspace-root-stream-readback-obligations}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Every
root readback of dense names, metric-radius checks, and Cauchy windows
factors through the displayed $\StreamNameUp$ row $S$ and $\RegSeqRatUp$ row
$R$ before it can be consumed by the $\MetricSpaceUp$, $\CompleteMetricUp$,
or $\SeparableMetricUp$ rows $M,K,D$. The factorization is supported exactly
by $W,H,C,G,N$ as observation ledger, $\hsame$ transport, $\Cont$ replay,
$\Pkg$ provenance, and local $\NameCert$ rows.
\end{theorem}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceRootStreamReadbackObligations}
\begin{proof}
Start from an accepted packet and project its stream-facing rows from
\autoref{def:polish-space-carrier}. The stream schedule $S$, regular rational
readback $R$, and observation ledger $W$ are displayed before any analytic
consumer reads $M,K$, or $D$.

Apply \autoref{thm:polish-space-namecert-obligations}. Ledger exactness makes
$W$ the only route for metric-radius checks, Cauchy windows, dense-name
inspections, and regular rational readbacks.

The transport, replay, provenance, and naming rows $H,C,G,N$ do not add a
second readback channel. Therefore every root stream readback remains inside
the finite $\BHist$ packet and cannot escape to an external oracle, selected
limit, host enumeration, or ambient $\RealUp$ completeness.
\end{proof}

\begin{theorem}[Polish-space StreamName basis]
\label{thm:polish-space-streamname-basis}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Every basis-level dense-name read of $P$ factors through the displayed $\StreamNameUp$ row $S$ and the $\RegSeqRatUp$ readback row $R$ before it can be consumed as $\MetricSpaceUp$, $\CompleteMetricUp$, or $\SeparableMetricUp$ evidence. The factorization is supported exactly by the $\BHist$ packet, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $G$, and the local $\NameCert$ row $N$.
\end{theorem}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceStreamNameBasis}
\begin{proof}
Project the carrier of \autoref{def:polish-space-carrier}. The stream schedule $S$, regular rational readback $R$, and observation ledger $W$ are displayed before any dense-name consumer can read the separable row $D$.

Use \autoref{thm:polish-space-namecert-obligations}. Its observation-exactness clause makes $W$ the only route from a basis request to finite stream positions and regular rational windows, while $H$ keeps the metric, completion, separable, stream, and readback rows on the same accepted packet.

Replay the request through $C$ and source it through $G,N$. Since the carrier has no selected dense sequence, quotient point equality, host complete separable space, or choice-selected oracle coordinate, the basis read is exactly the displayed StreamName and RegSeqRat route.
\end{proof}

\begin{theorem}[Polish-space complete-separable handoff]
\label{thm:polish-space-complete-separable-handoff}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. A complete-separable consumer may read $P$ only as the finite handoff from the $\MetricSpaceUp$ row $M$, the $\CompleteMetricUp$ handoff $K$, the $\SeparableMetricUp$ schedule $D$, the $\StreamNameUp$ row $S$, and the $\RegSeqRatUp$ readback row $R$, with $W,H,C,G,N$ preserving observation, transport, replay, provenance, and local naming.
\end{theorem}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceCompleteSeparableHandoff}
\begin{proof}
First expose the carrier rows. The metric row $M$ is the common source, while $K$ and $D$ are only the displayed completion and separability rows over that source.

The StreamName and RegSeqRat rows provide the finite inspection route for the dense schedule and Cauchy-window requests. The ledger $W$ records those inspections, and $H$ transports all five mathematical rows together.

Finally replay the handoff through $C$ and attach $G,N$. The packet contains no host complete separable space, quotient completion equality, choice-selected dense sequence, or ambient $\RealUp$ completeness row, so the complete-separable handoff is bounded by $M,K,D,S,R,W,H,C,G,N$.
\end{proof}

\begin{theorem}[Polish-space completion nonescape]
\label{thm:polish-space-completion-nonescape}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Every completion-facing consumer read factors through the displayed $\CompleteMetricUp$ row $K$, $\StreamNameUp$ row $S$, $\RegSeqRatUp$ readback row $R$, observation ledger $W$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $G$, local $\NameCert$ row $N$, and any terminal $\RealUp$ seal already exposed by those rows. No read escapes to host complete separable spaces, quotient point equality, choice-selected dense sequences, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceCompletionNonescape}
\begin{proof}
Start from a completion-facing request and project the accepted packet. The only completion row is $K$, and its finite windows are recorded through $S,R,W$ before a terminal real-name seal can be consumed.

Apply \autoref{thm:polish-space-namecert-obligations}. Carrier admission and classifier transport keep the completion handoff over the metric source $M$, while observation exactness prevents a completion request from bypassing the stream and regular-rational readback rows.

The structural rows $H,C,G,N$ transport, replay, source, and name that same finite route. Since no coordinate names a host completion, quotient point equality, selected dense sequence, or ambient real-completeness theorem, every completion-facing consumer remains inside the displayed packet.
\end{proof}

\begin{theorem}[Polish-space dense-schedule root obligation]
\label{thm:polishspace-dense-schedule-root-obligation}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. The
root dense-schedule obligation is the finite route that reads the
$\SeparableMetricUp$ row $D$ only after the $\MetricSpaceUp$ row $M$, the
$\StreamNameUp$ schedule $S$, the $\RegSeqRatUp$ readback row $R$, and the
observation ledger $W$ have been fixed by $\BHist$ support, componentwise
$\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the
local $\NameCert$ row. It exports no choice-selected dense point and no
ambient enumeration theorem.
\end{theorem}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceDenseScheduleRootObligationCert}
\begin{proof}
Apply \autoref{thm:polish-space-namecert-obligations}. Carrier admission
exposes $M,D,S,R,W,H,C,G,N$ as displayed rows of the same packet.

The classifier-transport clause uses $H$ to keep the dense schedule over the
metric surface $M$. The exactness clause makes $W$ the only route from a
dense-name request to the stream schedule $S$ and the regular rational
readback row $R$.

Replay the request through $C$ and source it through $G,N$. Since the carrier
has no coordinate for a selected dense subset or an external enumeration, the
root dense-schedule obligation is exactly the finite route just displayed.
\end{proof}

\begin{theorem}[Polish-space complete-metric root obligation]
\label{thm:polishspace-complete-metric-root-obligation}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. The
root complete-metric obligation reads the $\CompleteMetricUp$ handoff $K$
only over the already fixed $\MetricSpaceUp$ surface $M$ and through the same
$\StreamNameUp$, $\RegSeqRatUp$, and observation-ledger rows $S,R,W$ used by
dense-schedule consumers. The supporting rows are precisely the displayed
$\BHist$ packet, $\hsame$ transport $H$, $\Cont$ replay $C$, $\Pkg$
provenance $G$, and local $\NameCert$ row $N$.
\end{theorem}
\begin{proof}
Use \autoref{thm:polish-space-namecert-obligations}. Its carrier-admission
and classifier-transport rows require $K$ to remain over the metric carrier
$M$ before any completion-facing request is consumed.

The stability row transports $K$ together with $D,S,R,W$. Thus a Cauchy-window
request and a dense-name request share the same stream schedule, regular
rational readback, and finite observation ledger.

The replay and provenance rows $C,G,N$ add only routing, sourcing, and local
naming. No quotient completion, selected limit, ambient completeness
principle, or separate host metric is present in the carrier, so the
complete-metric root obligation is bounded by these rows.
\end{proof}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceCompleteMetricRootObligation}

\begin{theorem}[Polish-space StreamName-to-Real root obligation]
\label{thm:polishspace-streamname-real-root-obligation}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Every
StreamName-to-Real readback at the root factors through the displayed
$\StreamNameUp$ row $S$, $\RegSeqRatUp$ row $R$, and observation ledger $W$
before it can be consumed as metric, complete-metric, or separable-metric
evidence. The factorization is supported only by the $\BHist$ rows,
componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$
provenance $G$, and local $\NameCert$ row $N$.
\end{theorem}
\leanchecked{BEDC.Derived.PolishSpaceUp.PolishSpaceStreamNameRealRootObligation}
\begin{proof}
Start with the admitted packet and its observation-ledger exactness from
\autoref{thm:polish-space-namecert-obligations}. Any readback request must
enter through the stream schedule $S$ and the regular rational row $R$.

The ledger $W$ records which finite rational windows are inspected before
the result is used by $M,K$, or $D$. Componentwise transport in $H$ keeps
these rows on the same accepted carrier, while $C$ replays the displayed
route.

The provenance and naming rows $G,N$ identify the packet but do not add an
ambient real-completeness claim, a selected limit, or an external readback
oracle. Therefore the StreamName-to-Real root obligation is precisely the
finite factorization through $S,R,W,H,C,G,N$.
\end{proof}

\begin{theorem}[Polish-space StreamName dense handoff]
\label{thm:polish-space-streamname-countable-dense-handoff}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Every
consumer that reads the countable dense surface must factor through the
displayed $\StreamNameUp$ schedule $S$, the finite observation ledger $W$, and
the $\RegSeqRatUp$ readback row $R$ before using the $\SeparableMetricUp$ row
$D$ or the $\CompleteMetricUp$ handoff $K$. Consequently the packet exports a
countable dense handoff by finite names and regular rational windows, not by a
choice-selected dense subset or a quotient-completion representative.
\end{theorem}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceStreamNameDenseHandoff}
\begin{proof}
Start with a consumer of the dense surface. Carrier admission exposes $D$
only as a separable-metric row over the metric carrier $M$ and the stream
schedule $S$.

Read the requested dense-name index through $S$ and the finite ledger $W$.
The regular rational observation used by the consumer is then obtained through
$R$, so the handoff has a visible finite window before the dense row is
consumed.

If the same consumer also requests a completion read, the route passes through
$K$ after the same metric surface and observation ledger have been fixed.
The transports in $H$ keep the dense schedule, completion handoff, and metric
classifier on one admitted surface.

The remaining rows $C,G,N$ only replay, source, and name this finite route.
Since the carrier has no row for a choice-selected dense subset or a quotient
completion representative, the dense handoff can export only the displayed
stream-name and regular-readback data.
\end{proof}

\begin{theorem}[Polish-space observation-ledger exhaustion]
\label{thm:polish-space-observation-ledger-exhaustion}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Every
$\MetricSpaceUp$, $\CompleteMetricUp$, $\SeparableMetricUp$, $\StreamNameUp$,
and $\RegSeqRatUp$ consumer read from $P$ factors through the finite
observation ledger $W$ together with the componentwise $\hsame$ rows $H$, the
displayed $\Cont$ replay rows $C$, the $\Pkg$ provenance row $G$, and the local
$\NameCert$ row $N$. In particular, dense-schedule reads and completion reads
cannot bypass $W$: the rows $D$ and $K$ are consumable only after the finite
stream windows in $S$ and regular rational readbacks in $R$ have been displayed
inside the observation ledger.
\end{theorem}
\begin{proof}
Project the accepted carrier as $P=(M,K,D,S,R,W,H,C,G,N)$. The projection
exposes the metric row, complete-metric handoff, separable-metric dense
schedule, stream-name schedule, regular rational readback, observation ledger,
transport, replay, provenance, and local naming rows.

Apply \autoref{thm:polish-space-namecert-obligations}. Its exactness row
identifies $W$ as the only source of metric-radius checks, Cauchy-window
requests, dense-name inspections, and regular rational readbacks.

A dense-schedule consumer reaches $D$ only through an index displayed by $S$
and an observation recorded in $W$. A completion consumer reaches $K$ only
after its Cauchy-window request has been recorded in the same ledger. In both
cases the inspected value is read back through $R$ before the metric,
completion, or separability row is consumed.

The remaining rows $H,C,G,N$ transport, replay, source, and name this finite
route. Since the carrier has no coordinate outside the displayed ledger route,
no consumer of $M,K,D,S,$ or $R$ can bypass $W$.
\end{proof}

\begin{theorem}[Polish-space root obligation package]
\label{thm:polish-space-root-obligation-package}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Before any complete-separable metric synthesis is consumed, $P$ exposes exactly the $\MetricSpaceUp$ row $M$, the $\CompleteMetricUp$ row $K$, the $\SeparableMetricUp$ row $D$, the $\StreamNameUp$ row $S$, and the $\RegSeqRatUp$ row $R$, together with the finite observation ledger $W$, componentwise $\hsame$ transports $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $G$, and local $\NameCert$ row $N$.
\end{theorem}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceRootObligationPackage}
\begin{proof}
Project the carrier from \autoref{def:polish-space-carrier}. Its displayed rows are precisely the metric, complete, separable, stream, regular-readback, observation-ledger, transport, replay, provenance, and local naming rows.

Use \autoref{thm:polish-space-namecert-obligations} to read those rows as the local NameCert obligation package. It binds metric admission, completion handoff, dense schedule, stream windows, regular rational readback, and ledger exactness in one finite surface.

Finally apply \autoref{thm:polish-space-observation-ledger-exhaustion}. The root obligation package has no route around $W,H,C,G,N$, so every complete-separable consumer first sees exactly the five displayed analytic rows and their finite support.
\end{proof}

\begin{theorem}[Polish-space completion observation triad]
\label{thm:polish-space-completion-observation-triad}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. The complete-metric handoff $K$ and the separable dense schedule $D$ are both consumable only after the finite $\StreamNameUp$ observation ledger $S$, the $\RegSeqRatUp$ readback row $R$, and the support ledger $W$ have been displayed over the same metric row $M$. Thus every complete-separable consumer reads the triad
$$
S,R,W
$$
before either $K$ or $D$, and the packet exports no selected dense points, quotient completion equality, countable-choice schedule, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from \autoref{thm:polish-space-root-obligation-package}. It displays $M,K,D,S,R,W,H,C,G,N$ before the complete-separable synthesis can be consumed.

Apply \autoref{thm:polish-space-observation-ledger-exhaustion}. Dense-schedule reads and completion reads are both routed through $W$, and their finite windows and rational observations are supplied by $S$ and $R$.

The componentwise transport $H$ keeps $K$ and $D$ over the same metric row $M$, while $C,G,N$ replay, source, and name the route. Since no carrier coordinate stores selected dense points, quotient completion equality, a countable-choice schedule, or ambient $\RealUp$ completeness, none of those objects can be exported.
\end{proof}

\begin{theorem}[Polish-space complete-metric source lock]
\label{thm:polish-space-complete-metric-source-lock}
For an accepted $\PolishSpaceUp$ packet $P=(M,K,D,S,R,W,H,C,G,N)$, every read of the complete-metric handoff $K$ is locked to the source metric row $M$ after the finite observation rows $S,R,W$ have been displayed. The handoff may use the dense schedule $D$ only through the same observation route, and it cannot replace that route by quotient completion equality, selected dense representatives, countable choice, or completeness of an ambient $\RealUp$ object.
\end{theorem}
\begin{proof}
Project $P$ through \autoref{def:polish-space-carrier}. The row $K$ is a $\CompleteMetricUp$ handoff over the metric carrier $M$, not an independent completion object.

Use \autoref{thm:polish-space-namecert-obligations}. The classifier transport row fixes the common metric surface before $K$ or $D$ is read, and the exactness row identifies $W$ as the only source of Cauchy-window requests and regular rational readbacks.

Now apply \autoref{thm:polish-space-completion-observation-triad}. The rows $S$ and $R$ display the finite observations on which the handoff depends. Therefore a complete-metric consumer receives $K$ only as a source-locked finite route over $M$, with no additional quotient, selector, choice, or ambient-completeness coordinate.
\end{proof}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceCompleteMetricSourceLock}

\begin{theorem}[Polish-space separable dense schedule handoff]
\label{thm:polish-space-separable-dense-schedule-handoff}
For an accepted $\PolishSpaceUp$ packet $P=(M,K,D,S,R,W,H,C,G,N)$, every read of the separable dense schedule $D$ is handed off only after the finite $\StreamNameUp$ schedule $S$, the $\RegSeqRatUp$ readback row $R$, and the observation ledger $W$ have been displayed. The dense schedule is therefore a finite NameCert route over $M$ and $K$, not a choice-selected dense subset, countable-choice enumeration, quotient-completion representative, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceSeparableDenseScheduleHandoff}
\begin{proof}
Begin with a consumer of $D$. By \autoref{thm:polish-space-streamname-countable-dense-handoff}, the consumer must read the displayed stream schedule $S$, ledger $W$, and regular readback $R$ before using the separable-metric row.

The root obligation package fixes the surrounding rows $M,K,H,C,G,N$. Transport through $H$ keeps the dense schedule on the same metric carrier as the complete-metric handoff, and replay through $C$ records the finite request route.

Finally invoke \autoref{thm:polish-space-completion-observation-triad}. Since the carrier has no row for selected dense points, countable choice, quotient completion representatives, or ambient $\RealUp$ completeness, the only exported dense-schedule handoff is the displayed finite NameCert route.
\end{proof}

\begin{theorem}[Polish-space complete-separable source lock]
\label{thm:polish-space-complete-separable-source-lock}
For an accepted $\PolishSpaceUp$ packet $P=(M,K,D,S,R,W,H,C,G,N)$, every complete-separable consumer reads the $\CompleteMetricUp$ handoff $K$ and the $\SeparableMetricUp$ dense schedule $D$ over the same $\MetricSpaceUp$ row $M$. The componentwise transport row $H$ may move $K$ and $D$ only together with $M,S,R,W,C,G,N$, so the complete side cannot be sourced from one metric carrier while the separable side is sourced from another.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:polish-space-carrier}. The rows $K$ and $D$ are both stored beside the single metric row $M$, while $S,R,W$ provide the finite observation route before either analytic row is consumed.

Apply the NameCert obligations and complete-metric source lock. The classifier-transport clause of \autoref{thm:polish-space-namecert-obligations} fixes the common metric surface, and \autoref{thm:polish-space-complete-metric-source-lock} keeps the complete-metric handoff tied to $M$ after the observation rows have been displayed.

Finally use the separable dense schedule handoff. Theorem~\ref{thm:polish-space-separable-dense-schedule-handoff} routes $D$ through the same $S,R,W$ support, while $H,C,G,N$ only transport, replay, source, and name that packet. Thus complete separability is read from one displayed metric source.
\end{proof}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceCompleteSeparableSourceLock}

\begin{theorem}[Polish-space dense completion ledger refusal]
\label{thm:polish-space-dense-completion-ledger-refusal}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Every dense-completion read exported by $P$ factors through the stream windows $S$, regular rational readback $R$, and observation ledger $W$ before the rows $K$ and $D$ are consumed. The packet refuses selected dense points, quotient completion equality, countable choice, and ambient $\RealUp$ completeness as sources for the complete-separable handoff.
\end{theorem}
\begin{proof}
Start from the separable dense schedule handoff. Theorem~\ref{thm:polish-space-separable-dense-schedule-handoff} requires every read of $D$ to pass through $S,R,W$ before the dense schedule is exported.

The completion side follows the same ledger. Theorems~\ref{thm:polish-space-completion-observation-triad} and~\ref{thm:polish-space-complete-metric-source-lock} require completion-facing reads of $K$ to use the displayed stream windows, regular readbacks, and observation ledger over $M$.

Now apply the L10 consumer boundary. Theorem~\ref{thm:polish-space-l10-consumer-boundary} excludes selected dense points, quotient completion equality, countable choice, and ambient $\RealUp$ completeness from the public boundary. Since $H,C,G,N$ only preserve the displayed ledger route, no refused object can replace $S,R,W$.
\end{proof}

\begin{theorem}[Polish-space StreamName readback stability]
\label{thm:polish-space-stream-readback-stability}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Transporting the packet along the componentwise $\hsame$ row $H$ and replaying the same request through $C$ preserves the $\StreamNameUp$ windows $S$ and the $\RegSeqRatUp$ readback row $R$ componentwise before any complete-separable synthesis is consumed. The transported synthesis therefore reads the same finite stream and regular-rational observations through $W,G,N$.
\end{theorem}
\begin{proof}
Begin with metric readback stability. Theorem~\ref{thm:polish-space-metric-readback-stability} states that componentwise transport through $H$ preserves the metric, completion, dense-schedule, stream, regular-readback, ledger, replay, provenance, and naming rows.

Apply observation-ledger exhaustion. Theorem~\ref{thm:polish-space-observation-ledger-exhaustion} forces every dense-schedule and completion read through the finite ledger $W$, with windows supplied by $S$ and rational observations supplied by $R$.

Finally attach complete-separable source lock. Theorem~\ref{thm:polish-space-complete-separable-source-lock} keeps the complete and separable rows over the same transported $M$. Hence the complete-separable synthesis cannot alter the StreamName or RegSeqRat readbacks before it is consumed.
\end{proof}

\begin{theorem}[Polish-space complete separable synthesis]
\label{thm:polish-space-complete-separable-synthesis}
Every accepted $\PolishSpaceUp$ packet synthesizes a $\CompleteSeparableMetricUp$ read over the same metric carrier named by $M$. The synthesis consumes the $\CompleteMetricUp$ handoff $K$, the $\SeparableMetricUp$ dense schedule $D$, the finite $\StreamNameUp$ windows $S$, the $\RegSeqRatUp$ readback $R$, and the ledger route $W,H,C,G,N$; it exports no selected dense points, quotient completion equality, countable-choice schedule, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the root obligation package of \autoref{thm:polish-space-root-obligation-package}. The metric row $M$ is fixed before both completion and separability are read, so the synthesis is over one displayed metric carrier.

The completion side is supplied by $K$, and the separability side is supplied by $D$. The dense schedule can be inspected only through $S$, while regular rational observations pass through $R$ after their finite windows have been displayed.

The ledger exhaustion theorem, \autoref{thm:polish-space-observation-ledger-exhaustion}, keeps both sides inside $W,H,C,G,N$. Since the carrier contains no selected dense-point row, quotient-completion equality, countable-choice coordinate, or ambient real-completeness row, the resulting complete separable metric read is exactly the finite synthesis surface named above.
\end{proof}

\begin{theorem}[Polish-space complete separable non-escape]
\label{thm:polish-space-complete-separable-nonescape}
Every $\CompleteSeparableMetricUp$ consumer of an accepted $\PolishSpaceUp$ packet factors through the finite route
$$
M,K,D,S,R,W,H,C,G,N.
$$
The route is forced by \autoref{thm:polish-space-namecert-obligations}, exhausted by \autoref{thm:polish-space-observation-ledger-exhaustion}, and repacked by \autoref{thm:polish-space-complete-separable-synthesis}. Hence the synthesis exports no selected dense points, quotient completion equality, countable-choice schedule, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with a $\CompleteSeparableMetricUp$ consumer of an accepted packet. By \autoref{thm:polish-space-namecert-obligations}, the packet presents only the metric carrier $M$, completion handoff $K$, separable schedule $D$, stream windows $S$, regular readback $R$, and support rows $W,H,C,G,N$.

Use \autoref{thm:polish-space-observation-ledger-exhaustion}. It forces dense-schedule reads and completion reads through the observation ledger $W$, componentwise transport $H$, displayed replay $C$, provenance $G$, and local name $N$ before the consumer can use the complete or separable side.

Finally apply \autoref{thm:polish-space-complete-separable-synthesis}. The synthesis repacks the same rows as a complete-separable metric read over $M$. Since none of those rows stores selected dense points, quotient completion equality, a countable-choice schedule, open-cover compactness, or ambient $\RealUp$ completeness, no such datum can be exported by the consumer.
\end{proof}

\begin{theorem}[Polish-space L10 consumer boundary]
\label{thm:polish-space-l10-consumer-boundary}
An L10 consumer of an accepted $\PolishSpaceUp$ packet may import only the finite boundary consisting of $\MetricSpaceUp$, $\CompleteMetricUp$, $\SeparableMetricUp$, $\CompleteSeparableMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, the observation ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows. The boundary excludes selected dense points, quotient completion equality, countable choice, open-cover compactness, and ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the packet through \autoref{thm:polish-space-root-obligation-package}. This gives the finite L10 rows $M,K,D,S,R$ and the support rows $W,H,C,G,N$ before any downstream consumer can inspect the packet.

Use \autoref{thm:polish-space-complete-separable-synthesis}. It repacks the completion and separability sides as a $\CompleteSeparableMetricUp$ read over the same metric carrier while preserving the stream windows, regular readback, and ledger route.

The exclusion list follows from the carrier and ledger exactness. None of the displayed rows supplies a selected dense point, quotient-completion equality, countable-choice schedule, open-cover compactness principle, or ambient $\RealUp$ completeness datum, so an L10 consumer cannot cite any of those objects through $\PolishSpaceUp$.
\end{proof}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceCarrier\_l10\_consumer\_boundary}

\begin{theorem}[Polish-space completion dense schedule obligation]
\label{thm:polish-space-completion-dense-schedule-obligation}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Every completion-facing dense-schedule obligation exported from $P$ factors through the $\MetricSpaceUp$ row $M$, the $\CompleteMetricUp$ handoff $K$, the $\SeparableMetricUp$ schedule $D$, the $\StreamNameUp$ windows $S$, the $\RegSeqRatUp$ readback row $R$, the observation ledger $W$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $G$, and the local $\NameCert$ row $N$.

The obligation is exactly the finite route certified by \autoref{thm:polish-space-namecert-obligations}, constrained by \autoref{thm:polish-space-observation-ledger-exhaustion}, synthesized by \autoref{thm:polish-space-complete-separable-synthesis}, and bounded at L10 by \autoref{thm:polish-space-l10-consumer-boundary}. It exposes no choice-selected dense point, quotient completion, countable-choice schedule, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier through \autoref{def:polish-space-carrier}. The displayed analytic rows are precisely $M,K,D,S,R$, and the support rows are $W,H,C,G,N$.

Apply \autoref{thm:polish-space-namecert-obligations} to read those rows as local NameCert obligations. The exactness clause makes $W$ the only route for metric-radius checks, Cauchy windows, dense-name inspections, and regular rational readbacks.

Use \autoref{thm:polish-space-observation-ledger-exhaustion} and \autoref{thm:polish-space-complete-separable-synthesis}. They force the completion side and dense side through the same finite stream and readback route before the complete-separable metric synthesis can be consumed.

Finally apply \autoref{thm:polish-space-l10-consumer-boundary}. Since the boundary contains no coordinate for selected dense points, quotient completions, countable choice, open-cover compactness, or ambient real completeness, the exported obligation is exactly the displayed finite route.
\end{proof}

\begin{theorem}[Polish-space metric readback stability]
\label{thm:polish-space-metric-readback-stability}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Suppose a downstream consumer transports the metric row, completion handoff, dense schedule, stream windows, regular rational readback, and ledger rows along the componentwise $\hsame$ data recorded in $H$ and replays the same request through the displayed $\Cont$ routes in $C$. Then the transported packet preserves the same $\MetricSpaceUp$ readback boundary for $M$, the same $\CompleteMetricUp$ and $\SeparableMetricUp$ handoff surface for $K,D$, and the same $\StreamNameUp$ and $\RegSeqRatUp$ finite observations through $S,R,W,G,N$.

Thus metric readback stability is a finite NameCert property of $P$ and not a quotient-completion equality, selected dense representative, countable-choice enumeration, or host-level equality of metric spaces.
\end{theorem}
\begin{proof}
Begin with the carrier projection and the componentwise transport row $H$. The row transports $M,K,D,S,R,W$ together with the local certificate data, so a metric readback cannot move independently from the completion or dense-schedule rows.

Apply \autoref{thm:polish-space-namecert-obligations}. Its classifier-transport and stability clauses require $K$ and $D$ to remain over the transported metric surface before either row is consumed.

Next use \autoref{thm:polish-space-observation-ledger-exhaustion}. Every finite observation read by the transported consumer still passes through $S,R,W$, and the replay row $C$ records the same request route.

The L10 boundary theorem, \autoref{thm:polish-space-l10-consumer-boundary}, supplies the exclusion list. Because no accepted packet coordinate stores quotient completion equality, selected dense representatives, countable-choice enumerations, or host-level metric equality, the stability claim is only the displayed finite transport-and-replay property.
\end{proof}

\begin{theorem}[Polish-space L10 root unblock package]
\label{thm:polish-space-l10-root-unblock-package}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. The L10 root obligation package exported by $P$ consists of the completion-dense schedule route of \autoref{thm:polish-space-completion-dense-schedule-obligation}, the metric readback stability route of \autoref{thm:polish-space-metric-readback-stability}, the finite observation exhaustion of \autoref{thm:polish-space-observation-ledger-exhaustion}, the complete-separable synthesis of \autoref{thm:polish-space-complete-separable-synthesis}, and the consumer boundary of \autoref{thm:polish-space-l10-consumer-boundary}.

Consequently every downstream complete-separable metric chapter may import only $\MetricSpaceUp$, $\CompleteMetricUp$, $\SeparableMetricUp$, $\CompleteSeparableMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows through the displayed tuple $P=(M,K,D,S,R,W,H,C,G,N)$. The package does not supply choice-selected dense points, quotient completions, countable-choice schedules, host equality, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
First assemble the finite route. The completion-dense schedule obligation displays $M,K,D,S,R,W,H,C,G,N$ before any dense or complete read leaves the chapter boundary.

Next apply metric readback stability. It shows that transported consumers still read the same metric, completion, dense-schedule, stream, regular-readback, ledger, replay, provenance, and naming rows.

The observation-ledger and complete-separable synthesis theorems then repack this route as a $\CompleteSeparableMetricUp$ surface over the metric row $M$. This is the only synthesis route available to an L10 consumer.

Finally invoke the consumer boundary theorem. It permits the listed BEDC rows and refuses selected dense points, quotient completions, countable choice, host equality, open-cover compactness, and ambient real completeness, so the root unblock package is exactly the displayed finite package.
\end{proof}

\begin{theorem}[Polish-space complete-separable consumer package]
\label{thm:polish-space-l10-complete-separable-consumer-package}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Every L10 consumer that reads $P$ as a complete-separable metric input must receive a finite $\BHist$ route whose visible rows are the $\CompleteMetricUp$ row $K$, the $\SeparableMetricUp$ row $D$, the $\StreamNameUp$ windows $S$, the $\RegSeqRatUp$ readback row $R$, the observation ledger $W$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $G$, and the local $\NameCert$ row $N$ over the same metric source $M$.

The package refuses choice-selected dense points, quotient equality of completed points, arbitrary open covers, host completion, and any complete-separable witness not routed through the displayed tuple $P=(M,K,D,S,R,W,H,C,G,N)$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The row $K$ is the completion-facing input, $D$ is the separability-facing input, and $S,R,W$ are the finite stream, regular-readback, and observation rows that make those inputs consumable.

Apply \autoref{thm:polish-space-l10-root-unblock-package}. It lists exactly the routes that an L10 root obligation may export from $P$ and keeps the support rows $H,C,G,N$ attached to the same packet.

Use \autoref{thm:polish-space-l10-consumer-boundary}. Since the carrier has no row for a chosen dense point, quotient equality, arbitrary open cover, host completion, or ambient completion object, a complete-separable consumer can only receive the displayed finite package.
\end{proof}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceL10CompleteSeparableConsumerPackage}

\begin{theorem}[Polish-space completion schedule stability]
\label{thm:polish-space-completion-schedule-stability}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Any componentwise $\hsame$ transport of the completion side preserves the same schedule route from the $\CompleteMetricUp$ row $K$ through the $\StreamNameUp$ windows $S$, the $\RegSeqRatUp$ readback row $R$, the observation ledger $W$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $G$, and the local $\NameCert$ row $N$ over $M$.

Thus the transported schedule remains a finite $\BHist$ route and cannot be replaced by a choice-selected dense sequence, quotient equality, arbitrary open-cover refinement, host completion, or external completion of the metric carrier.
\end{theorem}
\begin{proof}
Start from the transport row $H$. It transports the metric, completion, separability, stream, readback, ledger, replay, provenance, and naming rows componentwise rather than moving $K$ alone.

Apply \autoref{thm:polish-space-metric-readback-stability}. The theorem keeps metric readback attached to $S,R,W$ after transport, so completion-side schedule reads still pass through the same finite observation route.

Finally use \autoref{thm:polish-space-observation-ledger-exhaustion}. The ledger exhaustion prevents the transported consumer from substituting selected dense points, quotient equality, arbitrary open covers, host completion, or an external completion object for the displayed schedule.
\end{proof}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceCompletionScheduleStability}

\begin{theorem}[Polish-space observation ledger consumer exactness]
\label{thm:polish-space-observation-ledger-consumer-exactness}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. For every complete-separable metric consumer of $P$, the observation ledger $W$ is exact: all requests for the $\CompleteMetricUp$ row $K$, the $\SeparableMetricUp$ row $D$, the $\StreamNameUp$ windows $S$, and the $\RegSeqRatUp$ readback row $R$ are replayed through $H,C,G,N$ before the local $\NameCert$ exports the result.

Consequently no consumer can read beyond the displayed $\BHist$ ledger into choice-selected dense points, quotient equality, arbitrary open covers, host completion, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the observation ledger theorem, \autoref{thm:polish-space-observation-ledger-exhaustion}. It states that the finite observation route covers metric-radius checks, Cauchy-window checks, dense-name inspections, and regular rational readbacks before any consumer leaves the packet.

Attach the complete-separable synthesis route. By \autoref{thm:polish-space-complete-separable-synthesis}, the complete and separable rows are consumed over the same metric source and through the same finite stream and readback rows.

The support rows close exactness. Transport $H$, replay $C$, provenance $G$, and local naming $N$ keep the consumer on the displayed $\BHist$ route, so no selected dense point, quotient equality, open-cover argument, host completion, or ambient Real-completeness row is available.
\end{proof}

\begin{theorem}[Polish-space complete-separable metric row exposure]
\label{thm:polish-space-complete-separable-metric-row}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Before any complete-separable metric consumer reads $P$, the packet exposes the $\CompleteMetricUp$ row $K$, the $\SeparableMetricUp$ row $D$, the synthesized $\CompleteSeparableMetricUp$ surface, the $\StreamNameUp$ finite windows $S$, the $\RegSeqRatUp$ readback row $R$, the componentwise $\hsame$ transport $H$, the displayed $\Cont$ replay $C$, the $\Pkg$ provenance $G$, and the local $\NameCert$ row $N$ over the same $\MetricSpaceUp$ row $M$.

Thus the complete-separable consumer receives the finite route
$$
M,K,D,S,R,W \longmapsto H,C,G,N
$$
and no selected dense point, quotient completion, countable-choice schedule, open-cover compactness principle, host equality of metric spaces, or ambient $\RealUp$ completeness datum.
\end{theorem}
\leanchecked{BEDC.Derived.PolishSpaceUp.PolishSpaceCompleteSeparableMetricRow}
\begin{proof}
Project the carrier through \autoref{def:polish-space-carrier}. The metric source is $M$, the completion handoff is $K$, and the dense schedule is $D$; the stream, regular readback, and observation rows are $S,R,W$.

Apply \autoref{thm:polish-space-complete-separable-synthesis}. It repacks $K$ and $D$ as the complete-separable metric surface only after the stream windows and regular rational readback have been made visible over the same metric carrier.

Use \autoref{thm:polish-space-l10-root-unblock-package}. The package keeps the support rows $H,C,G,N$ attached to the same displayed route, so a consumer cannot read complete separability before the listed rows have been exposed.
\end{proof}

\begin{theorem}[Polish-space stream-density readback row]
\label{thm:polish-space-stream-density-readback-row}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Every dense-schedule consumer that reads the $\SeparableMetricUp$ row $D$ must first pass through the $\StreamNameUp$ windows $S$ and the $\RegSeqRatUp$ readback row $R$, then replay the observation ledger $W$ through $H,C,G,N$ before treating the schedule as a complete-separable metric input.

In particular, a consumer cannot read separability from $D$ as a bare countable enumeration, choice-selected dense point, quotient-completion representative, open-cover compactness argument, or ambient real-completeness witness.
\end{theorem}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceStreamDensityReadbackRow}
\begin{proof}
Start with the dense side of \autoref{thm:polish-space-completion-dense-schedule-obligation}. It places $D$ downstream of the metric row $M$ and ties it to the finite stream windows $S$ and regular rational readback $R$.

Read the observation ledger next. By \autoref{thm:polish-space-observation-ledger-exhaustion}, metric-radius checks, Cauchy windows, dense-name inspections, and regular readbacks all pass through $W$ before the packet exports a separability-facing read.

Finally use \autoref{thm:polish-space-metric-readback-stability}. Transport, replay, provenance, and local naming preserve the same $S,R,W$ route, so a dense-schedule consumer receives finite stream-density readback rather than any hidden enumeration or completion principle.
\end{proof}

\begin{definition}[Polish-space root carrier]
\label{def:polishspace-carrier}
The root-facing $\PolishSpaceUp$ carrier is the tuple
$P=(M,K,D,S,R,W,H,C,G,N)$ of \autoref{def:polish-space-carrier}, read as a
single $\BHist$ packet whose consumer-visible rows are $\MetricSpaceUp$,
$\CompleteMetricUp$, $\SeparableMetricUp$, $\CompleteSeparableMetricUp$,
$\StreamNameUp$, $\RegSeqRatUp$, componentwise $\hsame$, displayed $\Cont$,
$\Pkg$ provenance, and local $\NameCert$ data. The root view has no additional
row for selected dense points, quotient completions, host equality,
open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Polish-space separable complete metric obligations]
\label{thm:polishspace-separable-complete-metric-obligations}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted root-facing $\PolishSpaceUp$
carrier. Every $\CompleteSeparableMetricUp$ obligation exported from $P$
factors through the $\MetricSpaceUp$ row $M$, the $\CompleteMetricUp$ handoff
$K$, the $\SeparableMetricUp$ dense schedule $D$, the $\StreamNameUp$ windows
$S$, the $\RegSeqRatUp$ readback row $R$, the observation ledger $W$,
componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$
provenance $G$, and the local $\NameCert$ row $N$.
\end{theorem}
\begin{proof}
Start with \autoref{def:polishspace-carrier}. The root carrier is only the
displayed tuple $P=(M,K,D,S,R,W,H,C,G,N)$.

Apply \autoref{thm:polish-space-complete-separable-metric-row}. It exposes the
complete, separable, stream, regular-readback, replay, provenance, and naming
rows over the same metric source before any complete-separable consumer reads
the packet.

Finally use \autoref{thm:polish-space-stream-density-readback-row}. It forces
dense-schedule reads through $S,R,W,H,C,G,N$, so the complete-separable
obligation has exactly the listed finite route.
\end{proof}
\leanchecked{BEDC.Derived.PolishSpaceUp.PolishSpaceCarrier\_separable\_complete\_metric\_obligations}

\begin{theorem}[Polish-space NameCert ledger transport]
\label{thm:polishspace-namecert-ledger-transport}
For an accepted root-facing $\PolishSpaceUp$ carrier
$P=(M,K,D,S,R,W,H,C,G,N)$, componentwise $\hsame$ transport may move the
$\MetricSpaceUp$, $\CompleteMetricUp$, $\SeparableMetricUp$,
$\CompleteSeparableMetricUp$, $\StreamNameUp$, and $\RegSeqRatUp$ rows only
together with the observation ledger $W$, displayed $\Cont$ replay $C$, $\Pkg$
provenance $G$, and local $\NameCert$ row $N$. No transported consumer can
read a complete-separable metric surface after dropping the ledger.
\end{theorem}
\begin{proof}
Read the componentwise transport row $H$ from
\autoref{def:polishspace-carrier}. It is part of the same finite packet as the
metric, completion, separability, stream, regular-readback, and ledger rows.

Apply \autoref{thm:polish-space-metric-readback-stability}. Transport preserves
the metric readback boundary together with the StreamName and RegSeqRat finite
observations through $S,R,W,G,N$.

The L10 root package of \autoref{thm:polish-space-l10-root-unblock-package}
then keeps every complete-separable consumer inside that transported ledger.
Thus ledger-free transport is not an exported row of the packet.
\end{proof}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishspaceNamecertLedgerTransport}

\begin{theorem}[Polish-space standard readiness]
\label{thm:polishspace-standard-readiness}
An accepted root-facing $\PolishSpaceUp$ carrier is ready for standard
complete-separable metric consumers only as the displayed $\BHist$ route
through $\MetricSpaceUp$, $\CompleteMetricUp$, $\SeparableMetricUp$,
$\CompleteSeparableMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\hsame$,
$\Cont$, $\Pkg$, and $\NameCert$ rows. A downstream $\RealUp$ consumer may
receive only the regular-readback side exposed through this route; it does not
receive ambient Real completeness or host equality of metric spaces.
\end{theorem}
\begin{proof}
Use \autoref{thm:polishspace-separable-complete-metric-obligations}. It
displays the complete-separable metric route over $M,K,D,S,R,W,H,C,G,N$.

Apply \autoref{thm:polishspace-namecert-ledger-transport}. Standard consumers
must preserve the observation ledger and replay rows when they move the metric,
completion, separability, stream, and readback data.

The $\RealUp$ clause is only a downstream boundary. The root carrier exposes
regular rational readback through $R$ but has no coordinate for ambient Real
completeness or host metric equality, so those claims cannot be imported.
\end{proof}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishspaceStandardReadiness}

\begin{theorem}[Polish-space L10 root unblock package]
\label{thm:polishspace-l10-root-unblock-package}
The root-facing $\PolishSpaceUp$ packet unlocks L10 metric-completion
consumers exactly by the finite carrier of \autoref{def:polishspace-carrier},
the complete-separable metric obligations of
\autoref{thm:polishspace-separable-complete-metric-obligations}, the ledger
transport of \autoref{thm:polishspace-namecert-ledger-transport}, and the
standard readiness boundary of \autoref{thm:polishspace-standard-readiness}.
It exports no selected dense point, quotient completion, countable-choice
schedule, host equality, open-cover compactness, or ambient $\RealUp$
completeness datum.
\end{theorem}
\begin{proof}
Assemble the carrier route first. The definition exposes $M,K,D,S,R,W,H,C,G,N$
as one finite $\BHist$ packet.

The separable complete metric obligations show how $\CompleteSeparableMetricUp$
consumers read that packet, and ledger transport keeps the rows tied together
under $\hsame$ movement.

Finally apply standard readiness. The only exported L10 boundary is the
displayed route through metric, completion, separability, stream, regular
readback, transport, replay, provenance, and local naming rows, with the listed
objects excluded from the carrier.
\end{proof}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishspaceL10RootUnblockPackage}

\begin{theorem}[Polish-space effective-representation handoff]
\label{thm:polish-space-effective-representation-handoff}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Any $\EffectivePolishSpaceUp$ or $\ComputablePolishSpaceUp$ consumer of $P$ must first expose the Polish complete-separable package formed by the $\MetricSpaceUp$ row $M$, the $\CompleteMetricUp$ row $K$, the countable dense schedule $D$, the $\StreamNameUp$ windows $S$, the $\RegSeqRatUp$ readback row $R$, the observation ledger $W$, the componentwise $\hsame$ transports $H$, the displayed $\Cont$ replay $C$, the $\Pkg$ provenance $G$, the local $\NameCert$ row $N$, and the terminal $\RealUp$ seal route before represented-space or computable-analysis vocabulary is read.

Consequently the represented-space boundary of \autoref{ch:concrete-instances-representedspace-namecert}, the admissible-representation boundary of \autoref{ch:concrete-instances-admissible-representation-namecert}, the effective Polish packet of \autoref{ch:concrete-instances-effective-polish-space-namecert}, and the computable Polish packet of \autoref{ch:concrete-instances-computable-polish-space-namecert} can consume $\PolishSpaceUp$ only through the displayed finite route of this chapter. They receive no selected dense points, quotient completion equality, unscheduled name oracle, host representation equality, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the Polish carrier projection. The complete-separable source is displayed by the metric row $M$, complete-metric handoff $K$, separable dense schedule $D$, finite stream windows $S$, regular readback $R$, and observation ledger $W$.

Apply \autoref{thm:polish-space-streamname-countable-dense-handoff}. Any dense-schedule read first opens the $\StreamNameUp$ schedule and the $\RegSeqRatUp$ readback before it can consume the separable row. Apply \autoref{thm:polish-space-stream-density-readback-row} to keep that dense schedule attached to $W,H,C,G,N$.

Next apply \autoref{thm:polish-space-observation-ledger-exhaustion}. Completion reads and dense reads both pass through the same finite ledger, so an effective or computable Polish consumer cannot replace the displayed route by an external representation source.

Finally read the sibling consumer surfaces. The $\EffectivePolishSpaceUp$ and $\ComputablePolishSpaceUp$ chapters import $\PolishSpaceUp$ only after their dense-realizer, represented-space, StreamName, RegSeqRat, and Real-seal rows have been displayed; the represented-space and admissible-representation chapters likewise require scheduled names and regular readback before target-facing vocabulary is available. Since $P$ has no coordinate for selected dense points, quotient completions, host representation equality, countable choice, open-cover compactness, or ambient $\RealUp$ completeness, the handoff is exactly the finite route named in the statement.
\end{proof}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceEffectiveRepresentationHandoff}

\input{parts/concrete_instances/polishspace/root_obligation_rows.tex}

\falsifiablePrediction{An accepted $\PolishSpaceUp$ consumer that reaches a
$\SeparableMetricUp$, $\CompleteMetricUp$, or $\MetricSpaceUp$ row without
reading the displayed $\StreamNameUp$ schedule, finite observation ledger,
$\RegSeqRatUp$ readback, componentwise $\hsame$ transports, $\Cont$ routes,
$\Pkg$ provenance, and local $\NameCert$ row refutes the carrier surface.}

\independenceWitness{metric, completemetric, separablemetric, streamname,
regseqrat: no listed sibling supplies the whole Polish packet
$P=(M,K,D,S,R,W,H,C,G,N)$, because each sibling contributes only the distance
carrier, completion handoff, dense schedule, stream schedule, or rational
readback while this chapter binds the complete-separable metric synthesis
route with transport, replay, provenance, and local naming.}

\closureat{PolishSpaceUp}{seedStr}

\begin{closurestatus}{\PolishSpaceUp}
  \constructivestory{A $\PolishSpaceUp$ packet is generated from a $\MetricSpaceUp$ carrier, a $\CompleteMetricUp$ handoff, a $\SeparableMetricUp$ countable dense schedule, $\StreamNameUp$ finite windows, $\RegSeqRatUp$ regular rational readbacks, an observation ledger, componentwise $\hsame$ transports, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:polish-space-carrier}, \autoref{thm:polish-space-namecert-obligations}, \autoref{thm:polish-space-streamname-countable-dense-handoff}, and \autoref{thm:polish-space-observation-ledger-exhaustion} seed the L10 complete-separable metric synthesis route over $\MetricSpaceUp$, $\CompleteMetricUp$, $\SeparableMetricUp$, $\StreamNameUp$, and $\RegSeqRatUp$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim choice-selected dense points, quotient completion equality, arbitrary open-cover compactness, host set equality, Lean verification, or bridge checking.}
  \upgradepath{A formal route can encode the displayed carrier and prove the finite NameCert obligation surface and stream-name dense handoff.}
\end{closurestatus}
