\begin{definition}[Polish-space completion-density unblock]
\label{def:polish-space-completion-density-unblock}
For an accepted $\PolishSpaceUp$ carrier
$P=(M,K,D,S,R,W,H,C,G,N)$, the completion-density unblock surface is the
finite route that exposes the $\MetricSpaceUp$ row $M$, the
$\CompleteMetricUp$ handoff $K$, and the $\SeparableMetricUp$ dense schedule
$D$ only through the shared $\StreamNameUp$ windows $S$, $\RegSeqRatUp$
readback $R$, observation ledger $W$, componentwise $\hsame$ transport $H$,
displayed $\Cont$ replay $C$, $\Pkg$ provenance $G$, and local $\NameCert$
row $N$. A downstream consumer may read complete and dense data from this
surface, but it receives no split metric carrier, selected dense point,
quotient completion equality, countable-choice schedule, or ambient
$\RealUp$ completeness datum.
\end{definition}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceCompletionDensityUnblock}

\begin{theorem}[Polish-space completion-density obligation]
\label{thm:polish-space-completion-density-obligation}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Every
completion-density root request first fixes the common $\MetricSpaceUp$ row
$M$, then reads the $\CompleteMetricUp$ handoff $K$ and the
$\SeparableMetricUp$ schedule $D$ through the same $\StreamNameUp$,
$\RegSeqRatUp$, and observation-ledger rows $S,R,W$. The supporting rows
$H,C,G,N$ transport, replay, source, and name only that displayed packet.
\end{theorem}
\begin{proof}
Start from \autoref{def:polish-space-completion-density-unblock}. The metric
coordinate $M$ is the single source for both the complete row $K$ and the
dense row $D$.

Read a completion-density request through $S,R,W$. The stream row supplies
the finite dense-name and Cauchy-window positions, the regular rational row
supplies the observable windows, and the ledger records that both kinds of
request use the same route.

It remains to transport and name the packet. Componentwise $\hsame$ transport
in $H$ keeps $M,K,D,S,R,W$ together, while $C,G,N$ replay, source, and name
the same finite data. No row remains from which a consumer could extract a
split metric carrier, selected dense point, quotient completion equality, or
ambient completeness principle.
\end{proof}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceCompletionDensityObligation}

\begin{theorem}[Polish-space countable dense readback]
\label{thm:polish-space-countable-dense-readback}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. Every
countable dense readback of $P$ is the finite route
$$
M \longmapsto D \longmapsto (S,R,W) \longmapsto (H,C,G,N),
$$
with any completion-facing use of $K$ read over the same $M,S,R,W$ surface.
Thus the dense readback is a displayed StreamName and RegSeqRat schedule, not
a selected dense subset, host enumeration, quotient point equality, or
countable-choice row.
\end{theorem}
\begin{proof}
Project the completion-density unblock surface. The dense schedule is the
$D$ row, and its only observation route is through the stream windows $S$,
regular rational readback $R$, and ledger $W$.

Use \autoref{thm:polish-space-completion-density-obligation}. The obligation
keeps $D$ over $M$ and forces completion-facing consumers of $K$ to share the
same $S,R,W$ readback surface.

Finally attach $H,C,G,N$. These rows do not add a second dense source; they
only transport, replay, source, and name the displayed route. Hence the
countable dense readback remains finite and schedule-based.
\end{proof}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceCountableDenseReadback}

\begin{theorem}[Polish-space complete-metric separable synthesis]
\label{thm:polish-space-complete-metric-separable-synthesis}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ carrier. The
complete-metric and separable-metric data synthesize into one
complete-separable root packet exactly when both $K$ and $D$ are consumed over
the common $\MetricSpaceUp$ row $M$ and through the shared
$\StreamNameUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and
$\NameCert$ rows $S,R,W,H,C,G,N$. The synthesis exports no independent
completion coordinate, selected dense sequence, host equality, or ambient
$\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Begin with the accepted carrier and apply
\autoref{thm:polish-space-completion-density-obligation}. The complete row
$K$ and the separable row $D$ are both read over $M$ and through the same
$S,R,W$ observation surface.

Next use \autoref{thm:polish-space-countable-dense-readback}. The dense side
has already been reduced to finite StreamName and RegSeqRat readback, and the
completion-facing use of $K$ cannot bypass that surface.

The remaining rows $H,C,G,N$ assemble the synthesis. They provide
componentwise transport, displayed replay, provenance, and local naming for
the single packet, while no carrier coordinate names an independent
completion, selected dense sequence, host equality, or ambient
real-completeness principle.
\end{proof}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceCompleteMetricSeparableSynthesis}

\begin{theorem}[Polish-space L10 sibling route]
\label{thm:polish-space-l10-sibling-route}
Every completion-density read of an accepted $\PolishSpaceUp$ carrier is routed through the sibling chapters \autoref{ch:concrete-instances-metric-namecert}, \autoref{ch:concrete-instances-completemetric-namecert}, \autoref{ch:concrete-instances-separablemetric-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The route uses the completion-density unblock surface of \autoref{def:polish-space-completion-density-unblock}, the completion-density obligation of \autoref{thm:polish-space-completion-density-obligation}, the countable dense readback of \autoref{thm:polish-space-countable-dense-readback}, and the complete-metric separable synthesis of \autoref{thm:polish-space-complete-metric-separable-synthesis}. It adds no selected dense point, quotient completion equality, countable choice, or ambient Real completeness.
\end{theorem}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpaceL10SiblingRoute}
\begin{proof}
Start with the unblock definition. It exposes $M,K,D,S,R,W,H,C,G,N$ as the only rows available to a completion-density consumer, with $M$ supplying the common metric source.

Apply the completion-density obligation and the countable dense readback. These place the complete-metric handoff and the separable schedule over the same StreamName and RegSeqRat windows, so the route must pass through the MetricSpace, CompleteMetric, SeparableMetric, StreamName, RegSeqRat, and Real sibling chapters named in the statement.

Finally apply the complete-metric separable synthesis. The synthesis repacks those sibling reads into the single $\PolishSpaceUp$ surface through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. Since no carrier coordinate names a selected dense point, quotient completion equality, countable-choice schedule, or ambient Real-completeness theorem, the sibling route adds none of those objects.
\end{proof}

\begin{definition}[Polish-space complete-separable obligation carrier]
\label{def:polishspace-obligation-complete-separable-carrier}
A complete-separable obligation carrier for $\PolishSpaceUp$ is a finite $\BHist$ packet
$$
P=(M,C,D,S,R,E,H,K,N)
$$
whose rows display a $\MetricSpaceUp$ source $M$, a $\CompleteMetricUp$ handoff $C$, a $\SeparableMetricUp$ dense-schedule row $D$, $\StreamNameUp$ windows $S$, $\RegSeqRatUp$ readback $R$, a $\RealUp$ seal $E$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $K$, and local $\NameCert_{\PolishSpaceUp}$ naming $N$. The carrier has no coordinate for host completion, quotient equality of completion points, a selected dense set, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Polish-space complete-separable NameCert obligations]
\label{thm:polishspace-complete-separable-namecert-obligations}
For every accepted complete-separable obligation carrier
$$
P=(M,C,D,S,R,E,H,K,N),
$$
the local $\NameCert_{\PolishSpaceUp}$ surface requires carrier admission, complete-metric handoff through $C$, separable dense-schedule exposure through $D$, finite $\StreamNameUp$ windows $S$, $\RegSeqRatUp$ readback $R$, $\RealUp$ seal nonescape through $E$, componentwise transport $H$, displayed replay $K$, and local naming $N$. These rows bind the complete and separable routes over the same $\MetricSpaceUp$ source $M$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:polishspace-obligation-complete-separable-carrier}. The metric row $M$ is the single source for both the complete-metric handoff $C$ and the separable dense-schedule row $D$.

Read observations through $S,R,E$. The StreamName row supplies finite Cauchy and dense-name windows, the regular rational row supplies the displayed readback, and the Real seal is downstream of those rows.

Finally attach $H,K,N$. Transport, replay, and local naming preserve the same finite packet, so the complete and separable obligations remain bound to $M,C,D,S,R,E$ rather than to any host completion or selected dense set.
\end{proof}

\begin{theorem}[Polish-space real completion readback nonescape]
\label{thm:polishspace-real-completion-readback-nonescape}
No $\PolishSpaceUp$ consumer of the complete-separable obligation carrier
$$
P=(M,C,D,S,R,E,H,K,N)
$$
may bypass $M,C,D,S,R,E,H,K,N$ to obtain host completion, quotient equality of completion points, a selected dense set, countable choice, open-cover compactness, or ambient $\RealUp$ completeness. Any real-facing completion read must first expose the metric row, complete-metric handoff, separable dense schedule, StreamName windows, RegSeqRat readback, and Real seal.
\end{theorem}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishspaceRealCompletionReadbackNonescape}
\begin{proof}
Apply \autoref{thm:polishspace-complete-separable-namecert-obligations}. It places every complete or separable read over the common metric source and through the displayed observation and real-readback rows.

The structural rows $H,K,N$ cannot add a second completion route. They only transport, replay, and name the carrier, so any consumer that reaches a real-facing completion read remains inside $M,C,D,S,R,E$.

Since the carrier definition contains no host completion, quotient equality, selected dense set, countable-choice schedule, open-cover compactness row, or ambient real-completeness datum, none of those objects can be exported by a $\PolishSpaceUp$ consumer of this packet.
\end{proof}
