\begin{theorem}[Polish-space public complete-separable real readback]
\label{thm:polishspace-public-complete-separable-real-readback}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ packet. Every public read of $P$ offered to an L10-adjacent downstream consumer must expose the common $\CompleteMetricUp$ handoff $K$, $\SeparableMetricUp$ dense schedule $D$, $\StreamNameUp$ window row $S$, $\RegSeqRatUp$ readback row $R$, $\RealUp$-facing seal route, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $G$, and local $\NameCert_{\PolishSpaceUp}$ row $N$ before the consumer may use the packet as a complete-separable real readback.

The route is public only as the displayed finite surface over $M,K,D,S,R,W,H,C,G,N$. It does not assert classical descriptive set theory, a choice-selected dense sequence, host enumeration of points, quotient completion equality, selected limits, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier through \autoref{def:polish-space-carrier}. The rows $M,K,D,S,R,W,H,C,G,N$ are the accepted packet, so a public read cannot detach the complete handoff, dense schedule, stream windows, rational readback, transport, replay, provenance, or local name from the carrier.

Use the complete-separable obligation surface of \autoref{thm:polish-space-complete-separable-obligation-surface}. It places $K$ and $D$ over the same metric row $M$ and keeps $S,R,W,H,C,G,N$ visible as the common route.

Next apply \autoref{thm:polish-space-streamname-density-scope}, \autoref{thm:polish-space-regseqrat-real-readback-scope}, and \autoref{thm:polishspace-complete-separable-window-exhaustion}. These theorems force dense access through $\StreamNameUp$, force real-facing readback through $\RegSeqRatUp$, and exhaust the complete-separable window before any downstream consumer can read it.

Finally read the sibling anchors \autoref{ch:concrete-instances-completemetric-namecert}, \autoref{ch:concrete-instances-separablemetric-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert} only through the displayed rows. Since the carrier has no coordinate for the excluded choice, quotient, selected-limit, or ambient-completeness principles, the public route is exactly the stated finite readback surface.
\end{proof}
\leanchecked{BEDC.Derived.PolishSpaceUp.PolishSpacePublicCompleteSeparableRealReadback}

\begin{theorem}[Polish-space public finite-window boundary]
\label{thm:polishspace-public-finite-window-boundary}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ packet. Every public read of $P$ offered to an L10 consumer is exhausted by the displayed $\CompleteMetricUp$ row $K$, $\SeparableMetricUp$ row $D$, $\StreamNameUp$ row $S$, $\RegSeqRatUp$ row $R$, terminal $\RealUp$ readback through $W$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $G$, and the local $\NameCert_{\PolishSpaceUp}$ row $N$ over the metric source $M$.

The boundary cannot introduce a choice-selected dense sequence, quotient completion equality, open-cover compactness, selected limits, a host enumeration of points, or ambient $\RealUp$ completeness outside the displayed finite packet.
\end{theorem}
\begin{proof}
Start with \autoref{thm:polishspace-public-complete-separable-real-readback}. It already identifies the public readback surface as $M,K,D,S,R,W,H,C,G,N$ and refuses the choice, quotient, selected-limit, and ambient-completeness coordinates.

Use the sibling anchors named there. The complete-metric row is read only through \autoref{ch:concrete-instances-completemetric-namecert}, the separable row only through \autoref{ch:concrete-instances-separablemetric-namecert}, the stream and regular-rational rows only through \autoref{ch:concrete-instances-streamname-namecert} and \autoref{ch:concrete-instances-regseqrat-namecert}, and the real-facing endpoint only through \autoref{ch:concrete-instances-real-namecert}.

Finally apply \autoref{thm:polish-space-complete-separable-obligation-surface} and \autoref{thm:polishspace-complete-separable-window-exhaustion}. They keep the complete and separable sides over the same metric source and exhaust the finite window before any consumer reads it. Since no carrier coordinate stores the refused objects, the public boundary is exactly the displayed route.
\end{proof}
\leanchecked{BEDC.Derived.PolishSpaceUp.PolishSpacePublicFiniteWindowBoundary}

\begin{theorem}[Polish-space public readback export]
\label{thm:polishspace-public-readback-export}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ packet. A public export of $P$ to L10 consumers is exactly the finite route through the $\MetricSpaceUp$ source $M$, $\CompleteMetricUp$ row $K$, $\SeparableMetricUp$ row $D$, $\StreamNameUp$ row $S$, $\RegSeqRatUp$ row $R$, terminal $\RealUp$ seal $W$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $G$, and local $\NameCert_{\PolishSpaceUp}$ row $N$.

The export refuses choice-selected dense sequences, quotient completion equality, selected limits, host enumeration of points, open-cover compactness, and ambient $\RealUp$ completeness outside the displayed packet.
\end{theorem}
\begin{proof}
Start with \autoref{thm:polishspace-public-complete-separable-real-readback}. It identifies the complete-separable readback surface as the displayed rows $M,K,D,S,R,W,H,C,G,N$ and keeps the real-facing endpoint inside the packet.

Apply \autoref{thm:polishspace-public-finite-window-boundary}. It says that every L10-facing public read is exhausted by the same finite window through complete metric, separable metric, stream, regular-rational, real-seal, transport, replay, provenance, and local naming rows.

Now compose these two boundaries with the sibling anchors already named in the file: \autoref{ch:concrete-instances-metric-namecert}, \autoref{ch:concrete-instances-completemetric-namecert}, \autoref{ch:concrete-instances-separablemetric-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. Since the carrier has no coordinate for the refused choice, quotient, selected-limit, host-enumeration, compactness, or ambient-completeness principles, the public export is exactly the displayed L10 readback route.
\end{proof}
\leanchecked{BEDC.Derived.PolishspaceUp.PolishSpacePublicReadbackExport}

\begin{theorem}[Polish-space public readback uniqueness]
\label{thm:polishspace-public-readback-uniqueness}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ packet. Suppose two public exports of $P$ expose the same $\CompleteMetricUp$ row $K$, $\SeparableMetricUp$ row $D$, $\StreamNameUp$ row $S$, $\RegSeqRatUp$ row $R$, and terminal $\RealUp$ route $W$, and suppose their displayed transport rows identify the corresponding components by componentwise $\hsame$.

Then the two exports agree as finite L10 readback routes over $M,K,D,S,R,W,H,C,G,N$. This uniqueness is only the displayed packet uniqueness: it does not select a dense sequence, identify quotient completions, choose limits, or claim ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the public readback export of \autoref{thm:polishspace-public-readback-export}. It says that any public export of $P$ is exactly the finite route through $M,K,D,S,R,W,H,C,G,N$ and has no hidden choice, quotient, selected-limit, host-enumeration, compactness, or ambient-completeness coordinate.

Use the finite-window boundary of \autoref{thm:polishspace-public-finite-window-boundary}. Since both exports expose the same complete, separable, stream, regular-rational, and real rows, the L10-facing window of each export is exhausted by the same displayed components.

Finally apply the readback theorem of \autoref{thm:polishspace-public-complete-separable-real-readback}. Componentwise $\hsame$ on the exposed rows transports the same complete-separable real readback route through $H$, while $C,G,N$ only replay, source, and name that route. Thus the two public exports agree as finite readback routes, without adding any of the refused external principles.
\end{proof}
\leanchecked{BEDC.Derived.PolishSpaceUp.PolishSpacePublicReadbackUniqueness}

\begin{theorem}[Polish-space complete-separable public interface]
\label{thm:polishspace-complete-separable-real-readback-public-interface}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ packet. A public L10-facing read of $P$ is the conjunction of the metric source row $M$, the $\CompleteMetricUp$ row $K$, the $\SeparableMetricUp$ dense schedule $D$, the $\StreamNameUp$ finite-window service $S$, the $\RegSeqRatUp$ readback $R$, the terminal $\RealUp$ seal $W$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $G$, and the local $\NameCert_{\PolishSpaceUp}$ row $N$.

This interface refuses choice-selected dense sequences, quotient completion equality, host enumeration of points, selected limits, open-cover compactness, and ambient $\RealUp$ completeness outside the displayed packet.
\end{theorem}
\leanchecked{BEDC.Derived.PolishSpaceUp.PolishSpaceCompleteSeparableRealReadbackPublicInterface}
\begin{proof}
Start with \autoref{thm:polishspace-public-complete-separable-real-readback}. It identifies the accepted packet rows $M,K,D,S,R,W,H,C,G,N$ as the complete-separable real readback surface.

Apply the finite-window boundary of \autoref{thm:polishspace-public-finite-window-boundary}. It keeps every L10-facing read inside the complete metric, separable metric, stream, regular-rational, real-seal, transport, replay, provenance, and local naming rows.

Use the public readback export theorem \autoref{thm:polishspace-public-readback-export}. The export theorem supplies the metric source row together with the same complete, separable, window, readback, seal, and structural rows, so the interface is the displayed conjunction rather than a new abstract equivalence schema.

Finally apply public readback uniqueness, \autoref{thm:polishspace-public-readback-uniqueness}. Since any two public exports with the same displayed rows agree only as finite readback routes, the refused choice, quotient, host-enumeration, selected-limit, compactness, and ambient-completeness principles cannot enter the interface.
\end{proof}

\begin{theorem}[Polish-space public complete-separable real readback frontier]
\label{thm:polishspace-public-complete-separable-real-readback-frontier}
Let $P=(M,K,D,S,R,W,H,C,G,N)$ be an accepted $\PolishSpaceUp$ packet whose public complete-separable readback has been accepted through \autoref{thm:polishspace-complete-separable-real-readback-public-interface}. A Real-facing point can be exposed to a downstream consumer only through the displayed $\CompleteMetricUp$ row $K$, $\SeparableMetricUp$ dense-window row $D$, $\StreamNameUp$ window row $S$, $\RegSeqRatUp$ readback row $R$, $\DyadicRatCoreUp$ tolerance discipline inherited by the regular readback route, and the terminal $\RealUp$ seal $W$, together with $H,C,G,N$.

The frontier exports no open-cover compactness principle, selected limit, quotient real equality, choice-selected dense sequence, host enumeration of points, or ambient $\RealUp$ completeness outside that finite route.
\end{theorem}
\begin{proof}
First read the four public-interface blocks already present in this child file. The complete-separable real readback theorem, the finite-window boundary, the public readback export, and the complete-separable public interface keep every public read inside the same packet rows $M,K,D,S,R,W,H,C,G,N$.

Next inspect the sibling sources. The complete metric row is grounded in \autoref{ch:concrete-instances-completemetric-namecert}, the complete-separable surface is read through \autoref{ch:concrete-instances-complete-separable-metric-namecert}, the window route through \autoref{ch:concrete-instances-streamname-namecert}, the regular readback through \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal seal through \autoref{ch:concrete-instances-real-namecert}. The dyadic tolerance discipline enters only through the regular-rational route and its \autoref{ch:concrete-instances-dyadicratcore-namecert} tolerance ledger.

Finally apply public readback uniqueness. Since any accepted public export agrees only as the displayed finite route, a downstream consumer has no coordinate from which to read open-cover compactness, selected limits, quotient real equality, a choice-selected dense sequence, host enumeration, or ambient $\RealUp$ completeness. The Real-facing point is therefore exposed exactly at the stated frontier.
\end{proof}
\leanchecked{BEDC.Derived.PolishSpaceUp.PolishSpacePublicCompleteSeparableRealReadbackFrontier}
