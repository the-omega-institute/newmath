\begin{theorem}[Polish-space effective complete-separable obligation]
\label{thm:polishspace-effective-complete-separable-obligation}
Every accepted $\PolishSpaceUp$ complete-separable consumer reads the $\MetricSpaceUp$ row $M$, the $\CompleteMetricUp$ row $K$, the $\SeparableMetricUp$ dense-schedule row $D$, the $\StreamNameUp$ window row $S$, the $\RegSeqRatUp$ readback row $R$, the Real-facing observation ledger $W$, and the structural rows $H,C,G,N$ through the same finite packet before any completion use is public. The obligation exports no selected dense sequence, quotient completion equality, unscheduled name oracle, host representation equality, or ambient $\RealUp$ completeness datum.
\end{theorem}
\begin{proof}
Apply \autoref{thm:polish-space-root-complete-separable-synthesis}. It identifies the complete-separable surface as the displayed carrier route through metric, completion, separability, stream windows, regular readback, observation, transport, replay, provenance, and local naming rows.

Apply \autoref{thm:polish-space-complete-separable-unblock-package}. Completion-facing reads cannot leave that finite route for a quotient completion, selected limit representative, or host completeness principle.

Finally apply \autoref{thm:polish-space-l10-consumer-boundary}. The L10 consumer boundary keeps $\MetricSpaceUp$, $\CompleteMetricUp$, $\SeparableMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, and the Real-facing rows in one packet, so an effective complete-separable consumer receives the shared obligation surface and no additional completion coordinate.
\end{proof}
