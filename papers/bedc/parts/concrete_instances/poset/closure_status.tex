\closureat{PosetUp}{matureStr}[BEDC.Derived.PreorderUp.preorder\_prefix\_stability\_certificate\_fields]

\begin{closurestatus}{\PosetUp}
  \constructivestory{A partial order here is the unary-prefix preorder with one additional visible row: if two carried unary histories extend to each other, the two continuation tails must both collapse and the endpoints are classified by $\hsame$. The carrier is still the unary history predicate over $\mathsf{BHist}$, excluding b0 branches and accepting only the empty history and repeated b1-stacks, while the classifier remains the carrier-aware history-sameness relation inherited from $\PreorderUp$. The source covers the certified prefix-preorder carrier plus the antisymmetry field, the pattern records bidirectional prefix comparison followed by classifier collapse, and the ledger stores the two directed continuation witnesses, their carried endpoints, and the resulting $\hsame$ readback. Since the inherited unary carrier is inhabited by $\emp$, its classifier is an equivalence, and carrier transport along $\hsame$ is already present, adding the antisymmetry row preserves all five name-certificate obligations while closing the partial-order law. A history $h$ lies in the signature gap of a $\PosetUp$ token $p$ when the source-domain accepts $h$, the signature relation pairs it with an answer endpoint and evidence ledger, and token introduction names that answer as $p$; concretely, $h$ is a unary endpoint in the prefix carrier and every opposing-comparison use in the ledger collapses to the classifier evidence encoded by $p$. Finite-chain rows and downstream handoff rows reuse the same continuation ledger for $\TotalOrderUp$ and $\LatticeUp$ consumers, but they do not change the carrier or add a standard bridge. The chapter registers this antisymmetric unary-prefix package under the name $\PosetUp$; partial order means an extra collapse theorem on the existing prefix relation, not host equality, quotient representatives, or an external poset object.}
  \theoryclosure{\matureClosure}
  \scopeclosed{The obligation scope binds the UnaryHistory carrier, BHist predicate, hsame relation, Preorder prefix comparison, antisymmetry row, Cont transport, Pkg provenance, NameCert classifier, and ledger rows displayed by the PosetUp packet.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.PreorderUp.preorder\_prefix\_stability\_certificate\_fields}
  \bridgestatus{paperBridge}
  \notclaimed{The chapter does not claim host order relations, quotient equality, external preorder carriers, or lattice structure.}
  \upgradepath{No further paper-axis grade remains; only future standard bridge verification can raise the bridge status.}
\end{closurestatus}
