\chapter{A Concrete Naming Certificate for $\GronwallInequalityUp$}
\label{ch:concrete-instances-gronwall-inequality-namecert}
\origin{human}

Following Gronwall and the Bishop--Bridges constructive ODE route, the $\GronwallInequalityUp$ packet records the finite majorant certificate by which a nonnegative error envelope is bounded before a Picard local-flow comparison is read. It consumes the local existence route of \autoref{ch:concrete-instances-picard-lindelof-namecert}, the local-flow endpoint surface of \autoref{ch:concrete-instances-picard-lindelof-local-flow-namecert}, the Lipschitz ledger of \autoref{ch:concrete-instances-lipschitzmap-namecert}, the complete-metric convergence boundary of \autoref{ch:concrete-instances-completemetric-namecert}, the sealed real-name row of \autoref{ch:concrete-instances-real-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The packet names only a displayed comparison route over finite interval, Lipschitz, envelope, dyadic exponential majorant, Picard-flow comparison, transport, replay, provenance, and local naming rows.

\begin{definition}[Gronwall inequality carrier]
\label{def:gronwall-inequality-carrier}
A $\GronwallInequalityUp$ carrier is a finite $\BHist$ packet
$$
P=(I,L,E,D,M,F,R,H,C,G,N)
$$
with the following visible rows:
\begin{enumerate}
\item $I$ is the interval row, recording the displayed time window, its endpoint order, and the local $\RealUp$ names through which the comparison is inspected.
\item $L$ is the $\LipschitzMapUp$ row naming the Lipschitz constant used by the ODE or local-flow comparison on the same interval.
\item $E$ is the nonnegative envelope row, storing the displayed error or separation function, its initial bound, and the monotonicity reads used by downstream comparison.
\item $D$ is the $\DyadicRatCoreUp$ exponential-majorant row, containing the dyadic coefficients, finite truncation budget, and upper rational enclosure for the Gronwall factor.
\item $M$ is the majorant comparison row, asserting that every inspected envelope read in $E$ is bounded by the dyadic exponential row $D$ after reading the Lipschitz row $L$ and interval row $I$.
\item $F$ is the Picard-flow comparison row, supplied by $\PicardLindelofUp$ or $\PicardLindelofLocalFlowUp$ when the carrier is consumed by local ODE stability.
\item $R$ is the $\RegSeqRatUp$ readback row for rational observations of the interval, Lipschitz constant, envelope samples, and dyadic majorant.
\item $H$ records componentwise $\hsame$ transport for the interval, Lipschitz ledger, envelope, dyadic majorant, comparison, Picard-flow handoff, rational readback, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through
$$
I,L,E,D,M,F,R \longmapsto \NameCert_{\GronwallInequalityUp}.
$$
\item $G$ is the $\Pkg$ provenance row over $\GronwallInequalityUp$, $\PicardLindelofUp$, $\PicardLindelofLocalFlowUp$, $\LipschitzMapUp$, $\CompleteMetricUp$, $\RealUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ dependency rows.
\item $N$ is the local $\NameCert_{\GronwallInequalityUp}$ row.
\end{enumerate}
The carrier has no coordinate for an ambient real-completeness principle, a host ODE solver, a choice-selected solution curve, a host equality of functions, or a global continuation theorem.
\end{definition}

\begin{theorem}[Gronwall inequality NameCert obligations]
\label{thm:gronwall-inequality-namecert-obligations}
For an accepted $\GronwallInequalityUp$ carrier
$$
P=(I,L,E,D,M,F,R,H,C,G,N),
$$
the local $\NameCert_{\GronwallInequalityUp}$ surface consists exactly of carrier admission for the displayed rows, dyadic majorant admission for $D$, envelope monotonicity for $E$, comparison stability for $M$, Picard handoff for $F$, rational readback for $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $G$, and local naming through $N$. No obligation is discharged by a host ODE solver, ambient real completeness, host function equality, or a choice-selected solution curve.
\end{theorem}
\leanchecked{BEDC.Derived.GronwallInequalityUp.GronwallInequalityTasteGate\_single\_carrier\_alignment}
\begin{proof}
Carrier admission is projection from \autoref{def:gronwall-inequality-carrier}. The accepted packet displays the interval, Lipschitz ledger, nonnegative envelope, dyadic majorant, comparison, Picard-flow handoff, rational readback, transport, replay, provenance, and local name.

The source rows $I,L,E,R$ determine the comparison problem. The interval row fixes the finite time window, the Lipschitz row supplies the displayed constant, the envelope row supplies the nonnegative quantity being bounded, and the regular-rational row supplies the observations through which the comparison is inspected.

The dyadic majorant obligation is local to $D$. It records the finite exponential enclosure used by the comparison, so the certificate never needs an unbounded real exponential object or an ambient completeness argument.

The comparison row $M$ then binds the envelope to the majorant after reading $I,L,E,D,R$. Its stability is componentwise: transporting the interval, Lipschitz, envelope, dyadic, and readback rows transports the displayed inequality route.

Finally $F$ is the only ODE-facing exit. It may hand the bound to $\PicardLindelofUp$ or $\PicardLindelofLocalFlowUp$, but only through the displayed rows. The rows $H,C,G,N$ transport, replay, source, and name this same finite route, so the certificate cannot export a host ODE solver, a choice-selected solution curve, host function equality, or global continuation data.
\end{proof}

\begin{theorem}[Gronwall inequality Picard handoff]
\label{thm:gronwall-inequality-picard-handoff}
Every accepted $\GronwallInequalityUp$ carrier factors any Picard--Lindelof local-flow stability read through the displayed route
$$
\LipschitzMapUp \longmapsto \GronwallInequalityUp \longmapsto \DyadicRatCoreUp \longmapsto \RegSeqRatUp \longmapsto \RealUp.
$$
The read first opens the Lipschitz constant, then the nonnegative envelope, then the dyadic exponential majorant, then the regular-rational observation rows, and only then seals the comparison as a $\RealUp$ bound usable by $\PicardLindelofUp$ or $\PicardLindelofLocalFlowUp$.
\end{theorem}
\begin{proof}
Begin with the accepted carrier $P$. The only Lipschitz data available to a stability consumer is the row $L$, and the only envelope data is the row $E$ on the interval $I$.

Read the majorant next. The row $D$ supplies the finite dyadic exponential enclosure, while $R$ supplies the regular rational observations needed to compare envelope samples with the majorant.

The comparison row $M$ combines these displayed rows into the Gronwall bound. Since $M$ reads $I,L,E,D,R$ before producing the comparison, any resulting real bound is sealed through the same $\RealUp$ row rather than through ambient completeness.

The Picard-facing row $F$ can now consume the bound. It receives only the displayed Lipschitz, envelope, dyadic majorant, regular-rational, and real-seal rows together with transport, replay, provenance, and local naming. Thus every Picard--Lindelof local-flow stability read factors through the stated route and cannot bypass it by invoking a host solver, host function equality, or a choice-selected solution curve.
\end{proof}

\falsifiablePrediction{Every $\GronwallInequalityUp$ stability read factors through one interval row, one LipschitzMap constant row, one nonnegative envelope row, one DyadicRatCore exponential-majorant row, one comparison row, one Picard-flow handoff, one RegSeqRat readback, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}

\independenceWitness{picardlindelof, picardlindeloflocalflow, lipschitzmap, completemetric, real, regseqrat, dyadicratcore: $\GronwallInequalityUp$ is not bijective to any listed sibling because it jointly carries the interval, Lipschitz constant, nonnegative envelope, dyadic exponential majorant, Picard-flow comparison, rational readback, and the transport/replay/provenance rows binding them into a finite stability certificate.}

\closureat{GronwallInequalityUp}{seedStr}

\begin{closurestatus}{\GronwallInequalityUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:gronwall-inequality-carrier}, \autoref{thm:gronwall-inequality-namecert-obligations}, and \autoref{thm:gronwall-inequality-picard-handoff} seed the constructive ODE stability route over an interval row, Lipschitz ledger, nonnegative envelope, dyadic exponential majorant, Picard-flow comparison, rational readback, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim ambient Real completeness, a host ODE solver, host function equality, choice-selected solution curves, global continuation, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic majorant admission, envelope monotonicity, comparison stability, Picard handoff, rational readback, transport, replay, provenance, and local naming for BEDC.Derived.GronwallInequalityUp.}
  \constructivestory{A $\GronwallInequalityUp$ packet is generated from one interval row, one LipschitzMap constant row, one nonnegative envelope row, one DyadicRatCore exponential-majorant row, one comparison row, one Picard-flow handoff, one RegSeqRat readback row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
