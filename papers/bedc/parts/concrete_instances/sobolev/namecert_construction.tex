\chapter{A Concrete Naming Certificate for $\SobolevUp$}
\label{ch:concrete-instances-sobolev-namecert}
\origin{human}

The $\SobolevUp$ interface records the finite naming surface for a Sobolev-style weak-derivative packet. Classical references such as Evans, \emph{Partial Differential Equations}, and Adams--Fournier, \emph{Sobolev Spaces}, describe the surrounding analytic theory. BEDC records only the finite certificate surface needed by later PDE, variational, compact-embedding, and functional-analysis consumers: a displayed domain row, metric or complete-metric base row, normed or Hilbert codomain row, integrability magnitude row, weak-gradient test row, package provenance, and local naming evidence.

The packet consumes the $\RealUp$ domain surface of \autoref{ch:concrete-instances-real-namecert}, the metric or complete-metric base through \autoref{ch:concrete-instances-metric-namecert} or \autoref{ch:concrete-instances-completemetric-namecert}, and the weak-gradient magnitude route through the $\IntegralUp$ and $\DerivativeUp$ sibling certificates of \autoref{ch:concrete-instances-integral-namecert} and \autoref{ch:concrete-instances-derivative-namecert}. These dependencies are read as finite carrier rows and ledger routes, not as a host Sobolev space or completed function quotient.

The packet is not distribution theory, not Lebesgue completion, not quotient equality of functions, and not a completed function space. It is a finite $\mathsf{BHist}$ carrier whose accepted observations are compared by displayed rows and $\hsame$ routes.

\begin{definition}[Sobolev BHist carrier]
\label{def:sobolev-bhist-carrier}
A $\SobolevUp$ carrier is a finite $\mathsf{BHist}$ packet
$$
S=(D,M,V,L,G,H,C,P,N)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $D$ is the $\RealUp$ domain row naming the scalar observation surface on which the weak tests are read;
\item $M$ is a $\MetricSpaceUp$ or $\CompleteMetricUp$ base row naming the ambient finite metric information used by the domain consumer;
\item $V$ is a $\NormUp$ or $\HilbertUp$ codomain row naming the magnitude target for function and weak-gradient observations;
\item $L$ is the $\IntegralUp$ magnitude row recording the finite integrability ledger for the displayed function and weak-gradient reads;
\item $G$ is the $\DerivativeUp$ weak-gradient test row, displayed as the finite list of test-observation routes and their recorded weak-derivative responses;
\item $H$ stores the componentwise $\hsame$ routes for the domain, base, codomain, integrability, weak-gradient, provenance, and local naming rows;
\item $C$ stores the displayed $\Cont$ routes by which a consumer reads the domain row, metric base row, codomain norm row, integrability ledger, and weak-gradient test ledger;
\item $P$ is the $\Pkg$ provenance over $\RealUp$, $\MetricSpaceUp$ or $\CompleteMetricUp$, $\NormUp$ or $\HilbertUp$, $\IntegralUp$, $\DerivativeUp$, $\mathsf{BHist}$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\SobolevUp}$ evidence.
\end{enumerate}
The carrier has no coordinate for a host function space, a distributional dual, a quotient by almost-everywhere equality, a completed Lebesgue measure space, or a choice-selected representative.
\end{definition}
\leanchecked{BEDC.Derived.SobolevUp.SobolevTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Sobolev weak-gradient source totality]
\label{thm:sobolev-weak-gradient-source-totality}
Every weak-gradient read from an accepted $\SobolevUp$ carrier $S=(D,M,V,L,G,H,C,P,N)$ factors through the displayed $\RealUp$ domain row $D$, the $\DerivativeUp$ test row $G$, componentwise $\hsame$ transport in $H$, and $\Cont$ replay in $C$. The read cannot start from a host weak-gradient predicate, distributional dual, quotient function, or completed measure carrier; its source is the finite $\BHist$ route recorded by the packet.
\end{theorem}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_weak\_gradient\_source\_totality}
\begin{proof}
Project the carrier of \autoref{def:sobolev-bhist-carrier}. The only domain source is $D$, and the only weak-gradient source is the displayed test row $G$.

Next use $H$ and $C$. Transport moves the domain and test rows componentwise, while replay exposes the same finite route to consumers without adding a background analytic object.

Finally repack the read through $P$ and $N$. Since no carrier coordinate stores a host weak-gradient predicate, distributional dual, quotient function, or completed measure carrier, every weak-gradient source read is exhausted by $D,G,H,C,P,N$.
\end{proof}

\begin{theorem}[Sobolev integral magnitude ledger stability]
\label{thm:sobolev-integral-magnitude-ledger-stability}
For every accepted $\SobolevUp$ carrier $S=(D,M,V,L,G,H,C,P,N)$, the $\IntegralUp$ magnitude row $L$ transports componentwise through $H$ and remains attached to the same displayed domain, codomain, and weak-gradient rows under $\Cont$ replay. The transported magnitude ledger exposes no Lebesgue-completion row, quotient-function row, selected representative, or host measure-space equality.
\end{theorem}
\begin{proof}
Start with the magnitude coordinate $L$. It is a finite $\IntegralUp$ row attached to the displayed domain $D$, codomain $V$, and weak-gradient tests $G$.

Transport is componentwise through $H$. Thus the magnitude row moves only with its visible domain, codomain, weak-gradient, provenance, and local naming coordinates.

Replay through $C$ preserves that attachment for downstream consumers. The omitted completion, quotient, representative, and host-equality objects are not coordinates of the packet, so the magnitude ledger cannot expose them after transport.
\end{proof}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_integral\_magnitude\_ledger\_stability}

\begin{theorem}[Sobolev obligation package]
\label{thm:sobolev-obligation-package}
The rows $D,M,V,L,G,H,C,P,N$ of an accepted $\SobolevUp$ carrier form the finite $\NameCert_{\SobolevUp}$ obligation package consumed by PDE, variational, compact-embedding, and functional-analysis successors. The package consists of carrier admission, domain and metric-base exposure, codomain magnitude exposure, integral magnitude accounting, weak-gradient test routing, componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local naming evidence. It exports no distribution theory, quotient function equality, Lebesgue completion, trace theorem, compact embedding theorem, or completed Sobolev space.
\end{theorem}
\begin{proof}
Read the carrier inventory. The displayed rows $D,M,V,L,G,H,C,P,N$ are the complete finite packet and provide the only source for successor chapters.

The analytic-facing rows are $D,M,V,L,G$. They expose the Real domain, metric or complete-metric base, normed or Hilbert codomain, integral magnitude ledger, and derivative test ledger before any successor can consume Sobolev data.

The structural rows $H,C,P,N$ then transport, replay, source, and name the same packet. Coordinate exhaustion excludes distribution theory, quotient function equality, Lebesgue completion, trace, compact embedding, and completed Sobolev-space objects from the obligation package.
\end{proof}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_obligation\_package}

\begin{definition}[Sobolev classifier]
\label{def:sobolev-classifier}
Two accepted $\SobolevUp$ carriers are classified as the same weak-derivative certificate exactly when their visible domain, base, codomain, magnitude, weak-gradient, provenance, and naming rows compare componentwise through the stored $\hsame$ routes. The classifier compares the $\RealUp$ domain row, the $\MetricSpaceUp$ or $\CompleteMetricUp$ base row, the $\NormUp$ or $\HilbertUp$ codomain row, the $\IntegralUp$ magnitude row, the $\DerivativeUp$ weak-gradient test row, the $\Pkg$ row, and the local $\NameCert$ row.

The classifier does not compare host functions, ambient distributions, completed $L^p$ classes, quotient representatives, or equality in a background Sobolev space.
\end{definition}

\begin{theorem}[Sobolev NameCert obligation surface]
\label{thm:sobolev-namecert-obligation-surface}
The local $\NameCert_{\SobolevUp}$ obligation surface for the carrier of \autoref{def:sobolev-bhist-carrier} consists of five finite rows:
\begin{enumerate}
\item carrier habitation by the zero weak-gradient packet, with a displayed $\RealUp$ domain row, metric base row, codomain norm row, zero $\IntegralUp$ magnitude row, zero $\DerivativeUp$ weak-gradient test row, package provenance, and local naming evidence;
\item classifier reflexivity, symmetry, and transitivity by the componentwise classifier of \autoref{def:sobolev-classifier};
\item carrier transport under the classifier, preserving the same visible domain, metric base, codomain, integrability, weak-gradient, provenance, and naming rows up to $\hsame$;
\item ledger exactness for weak-derivative observations, requiring every accepted derivative read to factor through the displayed $G$ row and the magnitude ledger $L$;
\item completion-boundary non-escape, preventing any consumer from reading distribution theory, Lebesgue completion, quotient equality, or a host Sobolev space from the finite carrier.
\end{enumerate}
\end{theorem}
\begin{proof}
For habitation, use the displayed zero weak-gradient test row. The domain, metric base, codomain, zero magnitude, provenance, and local naming coordinates are all finite carrier rows, so no ambient function space is needed.

For classifier stability, compare accepted carriers by the rows named in \autoref{def:sobolev-classifier}. Reflexivity, symmetry, and transitivity are inherited componentwise from the displayed $\hsame$ routes and are repacked into the same finite carrier shape.

For transport and exactness, inspect any downstream read. It can project only $D$, $M$, $V$, $L$, $G$, $H$, $C$, $P$, or $N$, and the weak-derivative read is the displayed route through $G$ with its magnitude entry in $L$. Coordinate exhaustion gives the non-escape boundary.
\end{proof}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_namecert\_obligation\_surface}

\begin{theorem}[Sobolev weak-derivative ledger]
\label{thm:sobolev-weak-derivative-ledger}
Every accepted weak-derivative observation exported by a $\SobolevUp$ carrier factors through the finite route
$$
D \longmapsto M \longmapsto V \longmapsto L \longmapsto G.
$$
The route first fixes the displayed $\RealUp$ domain row and metric base, then reads the $\NormUp$ or $\HilbertUp$ codomain row, then records the $\IntegralUp$ magnitude ledger before exposing the $\DerivativeUp$ weak-gradient test row. It exports no distributional dual object, no completed $L^p$ class, no quotient representative, and no host equality of functions.
\end{theorem}
\begin{proof}
Start with a consumer request for a weak-derivative observation. Carrier projection forces the request to identify the domain row $D$ and the metric or complete-metric base row $M$ before any derivative data can be read.

The codomain and magnitude rows are then read through $V$ and $L$. These rows specify the finite normed target and the accepted integrability ledger for the observation, not an ambient measure completion.

Only after that route reaches $G$ does the weak-gradient test row become visible. Since $H$, $C$, $P$, and $N$ record transport, continuation, provenance, and local naming without adding analytic coordinates, the exported observation is exactly the finite ledger described above.
\end{proof}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_weak\_derivative\_ledger}

\begin{theorem}[Sobolev completion boundary]
\label{thm:sobolev-completion-boundary}
A $\RealUp$, $\IntegralUp$, $\DerivativeUp$, $\HilbertUp$, functional-analysis, PDE, variational, or compact-embedding consumer citing an accepted $\SobolevUp$ packet receives only the displayed carrier rows of \autoref{def:sobolev-bhist-carrier}, the classifier of \autoref{def:sobolev-classifier}, the five obligation rows of \autoref{thm:sobolev-namecert-obligation-surface}, and the weak-derivative ledger of \autoref{thm:sobolev-weak-derivative-ledger}. The citation cannot assert full distribution theory, Lebesgue completion, quotient equality of functions, density of smooth functions, compact embedding, trace theory, or a completed Sobolev space.
\end{theorem}
\begin{proof}
Factor the consumer citation through the local $\NameCert_{\SobolevUp}$ surface. The available data are the finite rows $D,M,V,L,G,H,C,P,N$ and the classifier routes between accepted packets.

Apply the weak-derivative ledger theorem to any derivative-facing read. It fixes the route through the displayed domain, base, codomain, magnitude, and weak-gradient rows before the observation is exported.

The excluded analytic objects are not coordinates of the packet and are not produced by the classifier or continuation rows. Therefore downstream consumers may use the finite weak-derivative certificate surface, but they cannot read any stronger completion, quotient, density, compactness, or trace result from this chapter.
\end{proof}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_completion\_boundary}

\begin{theorem}[Sobolev weak-derivative root-unblock package]
\label{thm:sobolev-weak-derivative-root-unblock-package}
Every accepted $\SobolevUp$ packet $S=(D,M,V,L,G,H,C,P,N)$ exports a finite root package combining carrier admission, classifier stability, weak-derivative ledger exactness, metric or complete-metric boundary data, $\IntegralUp$ and $\DerivativeUp$ handoff, and the terminal $\RealUp$ seal nonescape. Any downstream analysis consumer must read this package through the displayed weak-gradient magnitude route
$$
D \longmapsto M \longmapsto V \longmapsto L \longmapsto G
$$
before reaching completion-facing rows.

The package supplies no distributional dual object, Lebesgue completion, quotient equality of functions, density theorem, compact embedding theorem, trace theorem, or completed Sobolev space.
\end{theorem}
\begin{proof}
Project the carrier and classifier surfaces. The carrier rows $D,M,V,L,G,H,C,P,N$ are the complete visible inventory, and the classifier compares them componentwise through the stored $\hsame$ routes.

Apply the NameCert obligation surface. Carrier habitation, classifier stability, carrier transport, weak-derivative ledger exactness, and completion-boundary non-escape are already recorded as finite rows over the same packet.

Use the weak-derivative ledger theorem to identify the route used by every derivative-facing read. The read passes from the $\RealUp$ domain row through the metric or complete-metric base, the normed or Hilbert codomain row, the $\IntegralUp$ magnitude ledger, and only then the $\DerivativeUp$ weak-gradient test row.

Finally apply the completion boundary theorem. It confines downstream PDE, variational, functional-analysis, compact-embedding, and completion-facing consumers to the displayed carrier, classifier, obligation, and ledger rows. Coordinate exhaustion then gives the Real seal nonescape and rules out all stronger analytic objects listed in the statement.
\end{proof}

\begin{theorem}[Sobolev weak-derivative carrier transport]
\label{thm:sobolev-weak-derivative-carrier-transport}
Let $S=(D,M,V,L,G,H,C,P,N)$ and $S'=(D',M',V',L',G',H',C',P',N')$ be accepted $\SobolevUp$ packets whose domain, base, codomain, magnitude, weak-gradient, provenance, and local naming rows are connected by the componentwise $\hsame$ entries recorded in $H$ and $H'$. Any weak-derivative consumer transported from $S$ to $S'$ must carry the same $\BHist$ route
$$
D \longmapsto M \longmapsto V \longmapsto L \longmapsto G
$$
to
$$
D' \longmapsto M' \longmapsto V' \longmapsto L' \longmapsto G'
$$
through the displayed $\Cont$ replay rows. The transport preserves the carrier, classifier, weak-derivative ledger, completion-boundary rows, $\Pkg$ provenance, and local $\NameCert$ evidence, and it supplies no distributional dual, quotient function class, completed measure carrier, compact embedding object, or trace row.
\end{theorem}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_weak\_derivative\_carrier\_transport}
\begin{proof}
Begin with the classifier of \autoref{def:sobolev-classifier}. It compares accepted packets only through the visible rows $D,M,V,L,G,P,N$ and the componentwise $\hsame$ transport stored in $H$.

Next replay the consumer route through $C$ and $C'$. Since every weak-derivative read is forced by \autoref{thm:sobolev-weak-derivative-ledger} to pass through the displayed domain, base, codomain, magnitude, and weak-gradient rows, transport replaces each row by its classified mate and keeps the same finite route.

Finally use the completion boundary theorem. The excluded analytic objects are absent from both carrier inventories, so transport across the classifier cannot create them.
\end{proof}

\begin{theorem}[Sobolev classifier locality]
\label{thm:sobolev-classifier-locality}
The $\SobolevUp$ classifier is local to the rows displayed by the carrier of \autoref{def:sobolev-bhist-carrier}. A downstream consumer may decide sameness of accepted weak-derivative packets only by reading the $\BHist$ rows $D,M,V,L,G$, the $\hsame$ transport row $H$, the $\Cont$ replay row $C$, the $\Pkg$ provenance row $P$, and the local $\NameCert$ row $N$. It cannot appeal to host function equality, equality in a quotient of measurable functions, distributional equality outside the displayed weak-gradient tests, ambient Hilbert completion, compact embedding, trace data, or density of smooth approximants.
\end{theorem}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_classifier\_locality}
\begin{proof}
Read the classifier definition. Its comparison surface is componentwise and mentions only the displayed domain, base, codomain, magnitude, weak-gradient, provenance, and naming rows.

Apply carrier transport. Any consumer trying to move a classified packet must replay the same $\Cont$ route through the same finite weak-derivative ledger, so the classifier remains local to the carrier inventory.

The nonlocal analytic objects listed in the statement are not coordinates of $S$. Coordinate exhaustion therefore blocks them from acting as classifier inputs or classifier witnesses.
\end{proof}

\begin{theorem}[Sobolev root obligation package]
\label{thm:sobolev-root-obligation-package}
Every accepted $\SobolevUp$ packet $S=(D,M,V,L,G,H,C,P,N)$ exports a root obligation package consisting of carrier admission, classifier stability, weak-gradient ledger exactness, completion-boundary nonescape, and $\RealUp$ seal nonescape. The package is read only through the displayed rows
$$
D,M,V,L,G,H,C,P,N .
$$
The carrier row admits the packet, the classifier row keeps sameness componentwise, the weak-gradient ledger forces derivative-facing reads through $D,M,V,L,G$, the completion boundary blocks ambient Sobolev completion, and the $\RealUp$ domain seal in $D$ cannot be read outside the same finite route.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:sobolev-bhist-carrier}. It lists exactly the displayed rows $D,M,V,L,G,H,C,P,N$, so carrier admission has no additional analytic coordinate.

Apply the classifier of \autoref{def:sobolev-classifier} and the NameCert obligation surface of \autoref{thm:sobolev-namecert-obligation-surface}. These give classifier stability, carrier transport, weak-derivative ledger exactness, and completion-boundary nonescape over the same rows.

Use the weak-derivative ledger and completion boundary, \autoref{thm:sobolev-weak-derivative-ledger} and \autoref{thm:sobolev-completion-boundary}. The derivative-facing route must pass through $D,M,V,L,G$, while completion-facing and Real-facing consumers remain inside the carrier and structural rows.

Finally repack the route through $H,C,P,N$. Since these rows only transport, replay, source, and name the same packet, the Real seal and completion boundary cannot escape the finite root obligation package.
\end{proof}

\begin{theorem}[Sobolev weak-gradient consumer boundary]
\label{thm:sobolev-weak-gradient-consumer-boundary}
Every PDE, variational, and functional-analysis consumer of an accepted $\SobolevUp$ weak-gradient read must pass through
$$
D,M,V,L,G,H,C,P,N
$$
before reading Sobolev data. In particular, such a consumer first reads the $\RealUp$ domain row $D$, the metric or complete-metric row $M$, the norm or Hilbert row $V$, the $\IntegralUp$ magnitude ledger $L$, and the $\DerivativeUp$ weak-gradient row $G$, then uses $H,C,P,N$ only for transport, replay, provenance, and local naming. It cannot read a host weak-gradient predicate, quotient function equality, distributional dual, Lebesgue completion, density theorem, compact embedding theorem, or trace theorem from the packet.
\end{theorem}
\begin{proof}
Start with a downstream consumer request. The carrier definition forces the request to enter through the displayed inventory $D,M,V,L,G,H,C,P,N$.

Apply the weak-derivative ledger. It orders every derivative-facing observation from the Real domain row through the metric base, codomain, integral magnitude ledger, and derivative test row before the observation is exported.

Now use classifier locality and carrier transport. They keep sameness and transport local to the same finite rows, so PDE, variational, and functional-analysis consumers cannot substitute an ambient analytic object for the displayed route.

The completion boundary removes the excluded objects. Distributional duals, quotient equality, Lebesgue completion, density, compactness, trace, and host weak-gradient predicates are not coordinates of the packet, and $H,C,P,N$ add only structural replay and naming.
\end{proof}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_weak\_gradient\_consumer\_boundary}

\begin{theorem}[Sobolev integral and derivative source package]
\label{thm:sobolev-integral-derivative-source-package}
In every accepted $\SobolevUp$ packet, the $\IntegralUp$ magnitude row $L$ and the $\DerivativeUp$ weak-gradient row $G$ are source rows for the weak-gradient ledger. Neither row can be replaced by distribution theory, Lebesgue completion, or an ambient quotient-function carrier: $L$ supplies the displayed magnitude ledger, $G$ supplies the displayed weak-gradient tests, and the route from $D$ through $M,V,L,G$ is the only analytic source package exported to downstream Real, metric, derivative, integral, Hilbert, PDE, variational, or functional-analysis consumers.
\end{theorem}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_integral\_derivative\_source\_package}
\begin{proof}
Project $L$ and $G$ from \autoref{def:sobolev-bhist-carrier}. The row $L$ is explicitly the integral magnitude ledger, and $G$ is explicitly the derivative-facing weak-gradient test row.

Apply \autoref{thm:sobolev-weak-derivative-ledger}. It places $L$ immediately before $G$ in the finite route from the Real domain row and metric base through the codomain row, so the weak-gradient read has a displayed source package.

Use \autoref{thm:sobolev-completion-boundary} and \autoref{thm:sobolev-classifier-locality}. They state that downstream consumers and classifier reads remain inside the same finite carrier rows and cannot appeal to distribution theory, Lebesgue completion, or quotient equality.

Finally transport through $H$ and replay through $C$. Provenance $P$ and local naming $N$ source the same $L,G$ package, so no replacement analytic carrier can discharge the integral or derivative source obligations.
\end{proof}

\begin{theorem}[Sobolev root-unblock certificate]
\label{thm:sobolev-root-unblock-certificate}
Every accepted $\SobolevUp$ packet exports a root-unblock certificate consisting exactly of the carrier row, local classifier, weak-derivative ledger, carrier transport row, classifier-locality row, completion-boundary row, $\Pkg$ provenance, and local $\NameCert$ evidence. Any Hilbert, metric, integral, derivative, PDE, variational, or compactness-facing consumer must first pass through these finite $\BHist$, $\hsame$, and $\Cont$ rows before reading the weak-gradient data. The certificate supplies no completed Sobolev space, no distributional dual, no quotient function equality, no selected representative, no density theorem, no compact embedding theorem, and no trace theorem.
\end{theorem}
\begin{proof}
Project the accepted packet. The carrier exposes exactly $D,M,V,L,G,H,C,P,N$, and the NameCert obligation surface records habitation, classifier stability, transport, ledger exactness, and completion-boundary nonescape.

Use the weak-derivative ledger and carrier transport rows. They force every derivative-facing read to traverse the displayed domain, base, codomain, magnitude, and weak-gradient route, then preserve that route under componentwise $\hsame$ comparison.

Use classifier locality for downstream consumers. Hilbert, metric, integral, derivative, PDE, variational, and compactness-facing reads can cite only the local classifier and the replayed route, not a host analytic object outside the packet.

The completion boundary then removes the remaining escape routes. Since the excluded spaces, quotients, representatives, density results, compact embeddings, and traces are not carrier coordinates, the root-unblock certificate is exhausted by the displayed finite rows.
\end{proof}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_root\_unblock\_certificate}

\begin{theorem}[Sobolev integral and derivative handoff]
\label{thm:sobolev-integral-derivative-handoff}
Every $\IntegralUp$, $\DerivativeUp$, $\HilbertUp$, $\MetricSpaceUp$, $\CompleteMetricUp$, PDE, or variational consumer of an accepted $\SobolevUp$ packet must traverse the displayed route
$$
D \longmapsto M \longmapsto V \longmapsto L \longmapsto G \longmapsto H,C,P,N .
$$
Thus weak-derivative data are consumed only through the finite integral and derivative ledgers. The handoff supplies no distribution theory, Lebesgue completion, quotient equality of functions, compact embedding theorem, trace row, selected representative, or completed Sobolev space.
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:sobolev-bhist-carrier}. The only analytic-facing coordinates are the domain row $D$, the metric or complete-metric base row $M$, the codomain row $V$, the integral magnitude ledger $L$, and the weak-gradient test row $G$.

Apply the weak-derivative ledger theorem. It places every derivative-facing observation on the route from $D$ through $M,V,L$ and then to $G$, so the integral and derivative consumers read the same finite ledger rather than an ambient function space.

Use the root-unblock certificate to carry the route to Hilbert, metric, complete-metric, PDE, and variational consumers. The transport, replay, provenance, and local naming rows $H,C,P,N$ preserve the displayed route without adding analytic coordinates.

Finally apply the completion boundary. The excluded distribution, completion, quotient, compactness, trace, representative, and completed-space objects are absent from the carrier inventory, so the handoff is exhausted by the finite route displayed above.
\end{proof}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_integral\_derivative\_handoff}

\begin{theorem}[Sobolev BHist carrier scope]
\label{thm:sobolev-bhist-carrier-scope}
Every accepted $\SobolevUp$ read is scoped by the finite $\BHist$ carrier rows
$$
D,M,V,L,G,H,C,P,N
$$
of \autoref{def:sobolev-bhist-carrier}. A consumer may reach $\RealUp$, $\MetricSpaceUp$ or $\CompleteMetricUp$, $\NormUp$ or $\HilbertUp$, $\IntegralUp$, and $\DerivativeUp$ only through those displayed rows, their componentwise $\hsame$ transports, their $\Cont$ replay route, the $\Pkg$ provenance row, and the local $\NameCert$ evidence.
\end{theorem}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_bhist\_carrier\_scope}
\begin{proof}
Project the accepted carrier. The carrier definition lists exactly the domain row $D$, metric or complete-metric base $M$, codomain row $V$, magnitude ledger $L$, weak-gradient row $G$, and structural rows $H,C,P,N$.

Apply the classifier locality and carrier transport rows. They compare and move accepted packets only through the same displayed coordinates, with $\hsame$ and $\Cont$ preserving the route rather than adding a host analytic object.

Finally inspect a downstream analysis read. Since every readable sibling is named in $P$ and replayed through $C$, the $\RealUp$, metric, complete-metric, norm, Hilbert, integral, and derivative surfaces can appear only as the finite rows already present in the $\BHist$ packet.
\end{proof}

\begin{theorem}[Sobolev derivative norm ledger]
\label{thm:sobolev-derivative-norm-ledger}
Every derivative-facing $\SobolevUp$ consumer first reads the $\DerivativeUp$ weak-gradient row $G$ and the $\IntegralUp$ magnitude row $L$ through the displayed codomain row $V$. If $V$ is Hilbert-facing, the same route reads $\HilbertUp$ only through the finite $\NormUp$ magnitude interface carried by $V$ before the $\NameCert_{\SobolevUp}$ row is exposed. Thus derivative magnitude, norm control, $\hsame$ transport, $\Cont$ replay, and $\Pkg$ provenance are one finite ledger, not a quotient Sobolev norm or a completed Hilbert-space argument.
\end{theorem}
\begin{proof}
Begin with the integral and derivative handoff. It forces the consumer route through $D,M,V,L,G$ before the structural rows are read.

The codomain row $V$ is therefore fixed before $L$ and $G$ are interpreted. The $\IntegralUp$ magnitude ledger reads weak-gradient size through $L$, and the $\DerivativeUp$ row reads the corresponding weak-gradient tests through $G$.

If the codomain is Hilbert-facing, the Hilbert row remains the displayed codomain surface inside $V$. The route supplies norm data only by the finite norm interface already named there, and the later rows $H,C,P,N$ merely transport, replay, source, and name the same derivative-norm ledger.
\end{proof}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_derivative\_norm\_ledger}

\begin{theorem}[Sobolev finite-window root package]
\label{thm:sobolev-finite-window-root-package}
Every accepted $\SobolevUp$ packet exports its downstream-ready finite root package only through the rows
$$
D,M,V,L,G,H,C,P,N .
$$
Here $\RealUp$ is read through $D$, $\MetricSpaceUp$ or $\CompleteMetricUp$ through $M$, $\NormUp$ or $\HilbertUp$ through $V$, $\IntegralUp$ through $L$, and $\DerivativeUp$ through $G$, with $H,C,P,N$ carrying transport, replay, provenance, and local naming. Thus PDE, variational, functional-analysis, Hilbert, metric, integral, and derivative consumers receive a finite $\BHist$ obligation package, not distributional duals, Lebesgue completion, quotient equality, density, compact embedding, trace rows, or selected representatives.
\end{theorem}
\begin{proof}
Project the accepted carrier through \autoref{def:sobolev-bhist-carrier}. Its visible coordinates are exactly $D,M,V,L,G,H,C,P,N$, and the local NameCert obligation surface records carrier admission, classifier stability, transport, ledger exactness, and completion-boundary non-escape over those rows.

Use the weak-derivative ledger and classifier locality rows. Every derivative-facing observation must traverse the displayed route from $D$ through $M,V,L$ to $G$, and every sameness decision remains local to the same carrier inventory and its componentwise $\hsame$ evidence.

Apply the derivative norm ledger. It places norm and Hilbert-facing magnitude reads inside $V$ before the integral and derivative rows are exposed, so the finite root package is stable under downstream norm, integral, derivative, and completion-facing consumption.

The excluded analytic objects are absent from the carrier, classifier, weak-derivative ledger, and derivative norm ledger. Coordinate exhaustion therefore blocks distributional duals, Lebesgue completion, quotient equality, density, compact embedding, trace rows, and selected representatives from entering the exported root package.
\end{proof}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_finite\_window\_root\_package}

\begin{theorem}[Sobolev finite energy carrier]
\label{thm:sobolev-finite-energy-carrier}
Let $S=(D,M,V,L,G,H,C,P,N)$ be an accepted $\SobolevUp$ packet. Its finite energy carrier is exactly the displayed $\BHist$ route through the $\RealUp$ domain row $D$, the $\MetricSpaceUp$ or $\CompleteMetricUp$ base row $M$, the $\NormUp$ or $\HilbertUp$ codomain row $V$, the $\IntegralUp$ magnitude ledger $L$, the $\DerivativeUp$ weak-gradient row $G$, the componentwise $\hsame$ transport row $H$, the displayed $\Cont$ replay row $C$, the $\Pkg$ provenance row $P$, and the local $\NameCert_{\SobolevUp}$ row $N$. No Real, metric, integral, derivative, or Hilbert-facing consumer can read an energy datum outside these coordinates.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:sobolev-bhist-carrier}. The tuple $D,M,V,L,G,H,C,P,N$ is the complete coordinate list, and each analytic face is named by one of those rows.

Apply the NameCert obligation surface and \autoref{thm:sobolev-finite-window-root-package}. Carrier admission, classifier stability, weak-derivative ledger exactness, and completion-boundary non-escape all refer back to the same finite inventory.

Coordinate exhaustion then handles the consumer side. A Real, metric, integral, derivative, or Hilbert read must enter through the named row assigned to that face; the remaining rows only transport, replay, source, and name the same route.
\end{proof}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_finite\_energy\_carrier}

\begin{theorem}[Sobolev weak-derivative scope]
\label{thm:sobolev-weak-derivative-scope}
Let $S=(D,M,V,L,G,H,C,P,N)$ be an accepted $\SobolevUp$ packet. Every weak-derivative observation of $S$ is scoped by the finite path
$$
D,M,V,L,G,H,C,P,N .
$$
The derivative row $G$ is readable only after the domain, base, codomain, and integral magnitude rows $D,M,V,L$ have been displayed, and the resulting observation is preserved only by the componentwise $\hsame$ row $H$, the displayed $\Cont$ replay row $C$, the $\Pkg$ provenance row $P$, and the local $\NameCert$ row $N$.
\end{theorem}
\begin{proof}
Start from the weak-derivative ledger in the carrier definition. The row $G$ is not a free-standing predicate; it is the derivative-facing coordinate attached to the already displayed rows $D,M,V,L$.

Use the classifier and transport rows. Sameness of accepted Sobolev packets is componentwise through $H$, and replay of a weak-derivative read is through $C$, so the observation cannot leave the same carrier route.

Finally apply the finite-window root package. It packages Real, metric, norm or Hilbert, integral, derivative, transport, replay, provenance, and naming rows as one finite $\BHist$ surface, which proves the stated scope by coordinate exhaustion.
\end{proof}

\begin{theorem}[Sobolev complete-metric handoff]
\label{thm:sobolev-complete-metric-handoff}
Let $S=(D,M,V,L,G,H,C,P,N)$ be an accepted $\SobolevUp$ packet. Any $\CompleteMetricUp$, $\MetricSpaceUp$, $\HilbertUp$, PDE, variational, integral, or compactness-facing consumer receives the Sobolev data only through the finite handoff
$$
D,M,V,L,G,H,C,P,N .
$$
The handoff may read complete-metric boundary information through $M$ and Hilbert or norm information through $V$, but it does not export a completed Sobolev carrier, selected metric limit, quotient function class, distributional dual, density theorem, compact embedding, or trace theorem.
\end{theorem}
\begin{proof}
Begin with the finite energy carrier. It fixes $D,M,V,L,G,H,C,P,N$ as the only readable rows and assigns metric or complete-metric data to $M$ and norm or Hilbert data to $V$.

Next apply the weak-derivative scope. The integral and derivative rows $L$ and $G$ remain downstream of the same domain, base, and codomain rows, and their observations are preserved only by $H,C,P,N$.

The downstream handoff is therefore just a repackaging of the finite root package. Since no coordinate names a completed Sobolev carrier, selected limit, quotient class, distributional dual, density theorem, compact embedding, or trace theorem, none of those objects can be supplied to the listed consumers.
\end{proof}

\begin{definition}[Sobolev finite-window carrier]
\label{def:sobolev-finite-window-carrier}
A finite-window $\SobolevUp$ carrier is a displayed $\BHist$ packet
$$
W=(D,M,V,L,G,T,H,C,P,N)
$$
over the following rows:
\begin{enumerate}
\item $D$ is the $\RealUp$ domain row for the finite scalar observation window.
\item $M$ is the $\MetricSpaceUp$ or $\CompleteMetricUp$ base row attached to the same window.
\item $V$ is the $\NormUp$ or $\HilbertUp$ codomain row through which function and weak-gradient magnitudes are read.
\item $L$ is the $\IntegralUp$ magnitude row for the displayed function and weak-gradient observations.
\item $G$ is the $\DerivativeUp$ weak-derivative ledger row, containing only the finitely inspected test routes and their responses.
\item $T$ is the transport ledger for weak-derivative rows, recording how each displayed test route is moved under the local classifier.
\item $H$ records componentwise $\hsame$ comparison for $D,M,V,L,G,T,P,N$.
\item $C$ records displayed $\Cont$ replay from a Sobolev read through the finite window into $D,M,V,L,G,T,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\SobolevUp$, $\RealUp$, $\MetricSpaceUp$, $\CompleteMetricUp$, $\NormUp$, $\HilbertUp$, $\IntegralUp$, $\DerivativeUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SobolevUp}$ row for this finite window.
\end{enumerate}
The finite-window classifier compares accepted packets only through these rows. It does not compare ambient Hilbert completions, quotient function classes, measure-theoretic representatives, host equality of functions, or selected extensions beyond the displayed weak-derivative tests.
\end{definition}

\begin{theorem}[Sobolev completion-boundary nonescape]
\label{thm:sobolev-completion-boundary-nonescape}
Every completion-facing read of a finite-window $\SobolevUp$ carrier $W=(D,M,V,L,G,T,H,C,P,N)$ is confined to
$$
D,M,V,L,G,T,H,C,P,N .
$$
In particular, a consumer may read $\RealUp$, $\MetricSpaceUp$, $\CompleteMetricUp$, $\NormUp$, $\HilbertUp$, $\IntegralUp$, $\DerivativeUp$, weak-derivative transport, replay, provenance, and local naming only through the displayed finite-window rows. It cannot export ambient Hilbert completion, quotient functions, measure-theoretic choice, host equality outside the displayed tests, density of smooth functions, compact embedding, trace theory, or a selected representative.
\end{theorem}
\leanchecked{BEDC.Derived.SobolevUp.SobolevFiniteWindowCarrier\_completion\_boundary\_nonescape}
\begin{proof}
Project the finite-window carrier of \autoref{def:sobolev-finite-window-carrier}. Its analytic rows are exactly $D,M,V,L,G,T$, and its structural rows are exactly $H,C,P,N$.

Read a completion-facing request through the replay row $C$. The route first reaches the $\RealUp$ domain row and metric or complete-metric base row, then the norm or Hilbert codomain row, and only then the integral magnitude and derivative test rows.

The transport ledger $T$ and componentwise $\hsame$ row $H$ move only those displayed weak-derivative rows. They do not add a quotient class, a measure-completion selector, or equality of host functions.

Finally $P$ and $N$ record provenance and local naming for the same finite window. Since the excluded analytic objects are absent from the carrier inventory and from the replay route, no completion-facing consumer can export them from this packet.
\end{proof}

\begin{theorem}[Sobolev Hilbert completion descent]
\label{thm:sobolev-hilbert-completion-descent}
Any $\HilbertUp$, $\MetricSpaceUp$, $\CompleteMetricUp$, PDE, variational, or completion-facing consumer of a $\SobolevUp$ packet descends to the carrier rows of \autoref{thm:sobolev-bhist-carrier-scope} before it may cite weak-derivative data. The descent route passes through the displayed $\RealUp$ domain row, the metric or complete-metric base row, the codomain norm or Hilbert row, the $\IntegralUp$ magnitude ledger, the $\DerivativeUp$ weak-gradient row, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ evidence. It supplies no completed Sobolev space, distributional dual, quotient function equality, density theorem, compact embedding, trace row, or selected representative.
\end{theorem}
\begin{proof}
Start with a Hilbert-facing or completion-facing consumer. The BHist carrier scope theorem forces the read to land on the displayed finite inventory $D,M,V,L,G,H,C,P,N$.

Use the derivative norm ledger. It identifies the analytic handoff as a route from the codomain row $V$ through the integral magnitude row $L$ and derivative row $G$, with Hilbert information restricted to the finite norm-facing surface in $V$.

The completion boundary then applies to the same route. The structural rows record transport, replay, provenance, and local naming only, so no completed Sobolev space, distributional dual, quotient function equality, density, compactness, trace, or selected representative can be read by descent.
\end{proof}

\begin{theorem}[Sobolev weak-gradient ledger exhaustion]
\label{thm:sobolev-weak-gradient-ledger-exhaustion}
Let $S=(D,M,V,L,G,H,C,P,N)$ be an accepted $\SobolevUp$ packet. Every weak-gradient read exported by $S$ is exhausted by the displayed rows $D,M,V,L,G$ together with the structural rows $H,C,P,N$. In particular, the read reaches $\RealUp$ only through $D$, $\MetricSpaceUp$ or $\CompleteMetricUp$ only through $M$, $\NormUp$ or $\HilbertUp$ only through $V$, $\IntegralUp$ only through $L$, $\DerivativeUp$ only through $G$, and $\BHist,\hsame,\Cont,\Pkg,\NameCert$ only through the carrier and structural rows already displayed by $S$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The weak-gradient route has no analytic coordinate beyond $D,M,V,L,G$, and its structural support is exactly $H,C,P,N$.

Apply the Sobolev weak-derivative ledger theorem. It forces each derivative-facing observation to pass through the domain, metric or complete-metric base, codomain, magnitude ledger, and weak-gradient test row before the observation is exported.

The BHist carrier scope theorem then accounts for the structural rows. The $\hsame$ transports, $\Cont$ replay, package provenance, and local NameCert evidence preserve the displayed weak-gradient route without adding an ambient analytic object.
\end{proof}

\begin{theorem}[Sobolev integral-derivative transport stability]
\label{thm:sobolev-integral-derivative-transport-stability}
Let $S=(D,M,V,L,G,H,C,P,N)$ and $S'=(D',M',V',L',G',H',C',P',N')$ be accepted $\SobolevUp$ packets compared by the displayed componentwise $\hsame$ transport rows. Any consumer that transports an integral or derivative read from $S$ to $S'$ must transport the same route
$$
D \longmapsto M \longmapsto V \longmapsto L \longmapsto G
$$
to
$$
D' \longmapsto M' \longmapsto V' \longmapsto L' \longmapsto G'
$$
through the displayed $\Cont$ replay rows, with provenance and local naming preserved by $P,N$ and $P',N'$. The transport cannot introduce a host distribution, quotient function equality, completed measure carrier, compact embedding row, or trace row.
\end{theorem}
\begin{proof}
Begin with classifier locality. The classifier compares accepted packets only through the displayed carrier rows and the $\hsame$ transport surface.

Use carrier transport to move the route. The source read through $D,M,V,L,G$ is replaced componentwise by the target read through $D',M',V',L',G'$, and the replay rows $C,C'$ preserve the same consumer order.

Finally apply weak-gradient ledger exhaustion. Since the source and target packets expose only the listed Real, metric, complete-metric, norm, Hilbert, integral, derivative, BHist, transport, replay, provenance, and NameCert rows, no excluded analytic object can appear during transport.
\end{proof}

\begin{theorem}[Sobolev completion-facing root scope]
\label{thm:sobolev-completion-facing-root-scope}
Every completion-facing, PDE-facing, variational, compact-embedding, Hilbert, metric, or derivative consumer of an accepted $\SobolevUp$ packet must first read the finite root scope
$$
D,M,V,L,G,H,C,P,N .
$$
Thus completion-facing access remains confined to $\RealUp$, $\MetricSpaceUp$ or $\CompleteMetricUp$, $\NormUp$ or $\HilbertUp$, $\IntegralUp$, $\DerivativeUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ as displayed carrier rows. It does not provide a completed Sobolev space, distributional dual, quotient equality of functions, density theorem, compact embedding theorem, trace theorem, or selected representative.
\end{theorem}
\begin{proof}
Start with Sobolev Hilbert completion descent. It sends every Hilbert-facing or completion-facing request to the carrier rows before weak-derivative data are cited.

Apply weak-gradient ledger exhaustion to identify the analytic portion of the root scope. The rows $D,M,V,L,G$ are the only path to Real, metric, complete-metric, norm, Hilbert, integral, and derivative reads.

Apply integral-derivative transport stability for transported consumers. The rows $H,C,P,N$ preserve, replay, source, and name the same finite root scope, so downstream PDE, variational, compactness-facing, and completion-facing consumers cannot read outside it.
\end{proof}

\begin{theorem}[Sobolev trace-window obligation]
\label{thm:sobolev-trace-window-obligation}
Every accepted $\SobolevUp$ packet exposes any boundary-facing trace-window read only through finite $\BHist$ rows and the displayed $\hsame$ transport surface. A consumer may read a trace window after it has traversed the carrier rows $D,M,V,L,G,H,C,P,N$, the weak-derivative ledger, the derivative norm ledger, and the local $\NameCert_{\SobolevUp}$ evidence. The obligation supplies a finite boundary-window route, not a global trace theorem, selected representative, quotient function equality, or ambient measure-completion choice.
\end{theorem}
\begin{proof}
Unpack the carrier of \autoref{def:sobolev-bhist-carrier}. The only readable coordinates are the displayed domain, base, codomain, integral magnitude, weak-gradient, transport, replay, provenance, and local naming rows.

Use the weak-derivative ledger and derivative norm ledger. They force boundary-facing analytic data to pass through the same $D,M,V,L,G$ route before the structural rows $H,C,P,N$ can replay the observation.

Finally apply the completion-facing root scope. Since the root scope excludes completed Sobolev spaces, quotient equality, selected representatives, and ambient measure choice, a trace-window consumer receives only the finite $\BHist$ rows and their $\hsame$ transports.
\end{proof}

\begin{theorem}[Sobolev local Poincare ledger]
\label{thm:sobolev-local-poincare-ledger}
Every local derivative or integral window read from an accepted $\SobolevUp$ packet is routed through displayed finite Poincare-style comparison ledgers. The route compares the $\DerivativeUp$ row $G$ against the $\IntegralUp$ magnitude row $L$ over the fixed domain, metric, and codomain rows $D,M,V$, then transports the comparison through $H$ and replays it through $C$. It does not invoke an ambient measure selection, global representative, or unrecorded normed-space completion.
\end{theorem}
\begin{proof}
Begin with the derivative norm ledger. It places derivative magnitude and norm control inside the route from $V$ through $L$ to $G$, so every local comparison has a displayed analytic source.

Read the same route through the finite-window root package. The package exposes $D,M,V,L,G$ before the structural rows, so the comparison is a finite ledger over the displayed domain, base, codomain, integral, and derivative data.

Transport now uses only $H$ and $C$. These rows preserve and replay the displayed local comparison; they contain no coordinate for choosing an ambient measure carrier, selecting a representative, or completing a normed space.
\end{proof}

\begin{theorem}[Sobolev completion consumer export]
\label{thm:sobolev-completion-consumer-export}
Every downstream $\HilbertUp$, $\CompleteMetricUp$, PDE, variational, or compactness-facing consumer of an accepted $\SobolevUp$ packet reads only the Sobolev carrier, the weak-derivative ledger, the derivative norm ledger, the integral and norm rows, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows. The export surface contains no additional distributional dual, quotient-function equality, density theorem, compact embedding theorem, trace theorem, or completed Sobolev carrier.
\end{theorem}
\begin{proof}
Start with Sobolev Hilbert completion descent. It sends each completion-facing consumer to the carrier rows before any weak-derivative datum can be cited.

Apply weak-gradient ledger exhaustion and integral-derivative transport stability. These theorems identify the analytic payload as the finite route through $D,M,V,L,G$ and preserve it across $H,C,P,N$.

Use the trace-window obligation and local Poincare ledger for boundary-facing and local comparison reads. Both obligations remain inside the same carrier, transport, replay, provenance, and local NameCert rows, so the completion consumer export has no additional analytic coordinate.
\end{proof}

\begin{theorem}[Sobolev Real completion root window]
\label{thm:sobolev-real-completion-root-window}
Every accepted $\SobolevUp$ packet carries a Real-completion root window in its finite $\BHist$ surface. The $\RealUp$ row $D$ supplies the scalar observation and terminal seal rows, the $\IntegralUp$ row $L$ and $\DerivativeUp$ row $G$ supply the weak-gradient magnitude ledger, and the structural rows $H,C,P,N$ transport, replay, source, and name the same route. Thus a Real-facing completion consumer receives scalar and limit-facing rows only through the displayed carrier, not through ambient $\RealUp$ completeness, quotient equality of functions, selected representatives, or choice.
\end{theorem}
\begin{proof}
Start with the Sobolev completion consumer export. It sends every completion-facing request to the carrier rows before any analytic datum is read.

Apply the weak-derivative ledger. The scalar route begins at the $\RealUp$ domain row $D$ and reaches the weak-gradient magnitude rows only through $L$ and $G$.

Finally use the completion boundary. Since $H,C,P,N$ merely preserve, replay, source, and name the same finite route, the Real-completion root window exposes no ambient Real completeness, quotient equality, selected representative, or choice coordinate.
\end{proof}

\begin{theorem}[Sobolev metric completion root window]
\label{thm:sobolev-metric-completion-root-window}
Every metric-facing or complete-metric-facing consumer of an accepted $\SobolevUp$ packet must read distance and Cauchy-boundary information through the displayed base row $M$ together with the carrier route $D,M,V,L,G,H,C,P,N$. The $\MetricUp$ or $\CompleteMetricUp$ surface supplies only the finite distance and Cauchy rows needed by the Sobolev packet, and the route exports no completed Sobolev carrier, function quotient, selected Cauchy limit, or unrecorded metric completion.
\end{theorem}
\begin{proof}
Begin with the finite root scope. It records that completion-facing access is confined to the displayed rows $D,M,V,L,G,H,C,P,N$.

Read the metric base row $M$. This row is the only coordinate for $\MetricUp$ or $\CompleteMetricUp$ distance and Cauchy information, so a consumer cannot obtain a separate completed function-space object.

Use the completion consumer export to repack the route. The rows $H,C,P,N$ transport, replay, source, and name the same metric window, while the excluded quotient, selected-limit, and unrecorded-completion objects remain absent from the carrier.
\end{proof}

\begin{theorem}[Sobolev Hilbert-norm root window]
\label{thm:sobolev-hilbert-norm-root-window}
Every Hilbert-facing or norm-facing consumer of an accepted $\SobolevUp$ packet reads the weak-gradient ledger through the codomain row $V$, the $\IntegralUp$ magnitude row $L$, and the $\DerivativeUp$ test row $G$. If $V$ is displayed as a $\HilbertUp$ row, the Hilbert structure is still read only through the same finite norm and weak-gradient ledgers; if $V$ is displayed as a $\NormUp$ row, no hidden inner-product or completed Hilbert carrier is supplied. In both cases the route is confined to $D,M,V,L,G,H,C,P,N$.
\end{theorem}
\begin{proof}
Start with Sobolev Hilbert completion descent. It requires Hilbert-facing consumers to read the carrier rows before citing weak-derivative data.

Apply weak-gradient ledger exhaustion. The codomain row $V$, integral magnitude row $L$, and derivative row $G$ are the only ledger rows that expose norm, Hilbert, integral, and derivative information.

Finally use integral-derivative transport stability. Transport across $H$ and replay through $C$ preserve the same weak-gradient ledger rows, while $P$ and $N$ only source and name them. No hidden inner-product object, completed Hilbert carrier, or quotient function class is introduced.
\end{proof}

\begin{theorem}[Sobolev carrier energy window]
\label{thm:sobolev-carrier-energy-window}
Every accepted $\SobolevUp$ packet $S=(D,M,V,L,G,H,C,P,N)$ exposes one finite energy window. The window consists of the $\RealUp$ domain row $D$, the $\MetricSpaceUp$ or $\CompleteMetricUp$ base row $M$, the $\NormUp$ or $\HilbertUp$ codomain row $V$, the $\IntegralUp$ magnitude row $L$, the $\DerivativeUp$ weak-gradient row $G$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert$ evidence $N$. No consumer can split this window into a separate measure-completion object, quotient function class, distributional dual, selected representative, or completed Sobolev carrier.
\end{theorem}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_energy\_window}
\begin{proof}
Project the carrier of \autoref{def:sobolev-bhist-carrier}. Its visible coordinates are exactly $D,M,V,L,G,H,C,P,N$, with the domain, base, codomain, magnitude, weak-gradient, transport, replay, provenance, and naming roles fixed by the carrier definition.

Apply the NameCert obligation surface. It requires carrier habitation, classifier stability, carrier transport, weak-derivative exactness, and completion-boundary non-escape over the same finite rows.

Finally use the completion consumer export and Hilbert-norm root window. They force downstream completion, PDE, variational, Hilbert-facing, and norm-facing consumers to read the displayed energy rows together, so no omitted analytic object can be recovered from the packet.
\end{proof}

\begin{theorem}[Sobolev weak-derivative route]
\label{thm:sobolev-weak-derivative-route}
Every weak-derivative-facing consumer of an accepted $\SobolevUp$ packet must read the $\IntegralUp$ magnitude row $L$ before it reads the $\DerivativeUp$ weak-gradient response row $G$. The route is the finite carrier path
$$
D \longmapsto M \longmapsto V \longmapsto L \longmapsto G
$$
transported by $H$ and replayed by $C$; it does not read distribution theory, quotient equality of functions, ambient almost-everywhere classes, or a host weak-derivative predicate.
\end{theorem}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_weak\_derivative\_route}
\begin{proof}
Begin with the weak-derivative ledger theorem. It orders every derivative-facing read through the domain, base, codomain, magnitude, and weak-gradient rows before the observation is exported.

The magnitude row $L$ is therefore not optional. It is the only displayed $\IntegralUp$ ledger available for the weak-gradient response, and the derivative row $G$ is read only after that magnitude ledger is fixed.

Transport and replay add no analytic coordinates. The rows $H$ and $C$ preserve the same route, while $P$ and $N$ source and name it, so distribution theory, quotient equality, almost-everywhere classes, and host predicates remain outside the packet.
\end{proof}

\begin{theorem}[Sobolev completion-norm handoff]
\label{thm:sobolev-completion-norm-handoff}
Every completion-facing consumer of an accepted $\SobolevUp$ packet receives only the displayed Hilbert or norm row $V$ together with the finite weak-gradient ledger $D,M,V,L,G,H,C,P,N$. If the base row $M$ is read as $\CompleteMetricUp$ data, that read supplies only the displayed metric or Cauchy-boundary information needed by the packet; it does not create a completed Sobolev space, a quotient normed function space, a selected Cauchy limit, or a completed Lebesgue carrier.
\end{theorem}
\begin{proof}
Start with the metric completion root window. It confines metric-facing and complete-metric-facing reads to the displayed base row $M$ together with the carrier route $D,M,V,L,G,H,C,P,N$.

Apply the Hilbert-norm root window. Hilbert-facing and norm-facing consumers read $V$ only through the same weak-gradient ledger, with $L$ and $G$ providing the integral magnitude and derivative response rows.

Use the completion consumer export to repack the handoff. The exported surface is the finite norm and weak-gradient ledger plus structural transport, replay, provenance, and naming rows; no completed Sobolev space, quotient function carrier, selected limit, or completed Lebesgue object is a coordinate of the packet.
\end{proof}

\begin{theorem}[Sobolev root-unblock package]
\label{thm:sobolev-root-unblock-package}
Every accepted $\SobolevUp$ packet $S=(D,M,V,L,G,H,C,P,N)$ exports a root-unblock package consisting of the $\RealUp$ domain surface $D$, the $\MetricSpaceUp$ or $\CompleteMetricUp$ base row $M$, the $\NormUp$ or $\HilbertUp$ codomain row $V$, the $\IntegralUp$ magnitude ledger $L$, the $\DerivativeUp$ weak-gradient ledger $G$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert$ evidence $N$. Downstream analysis chapters may cite this package only through the displayed carrier, weak-derivative, completion-boundary, Hilbert-norm, and root-window routes already exposed in the chapter.

The package supplies no host Sobolev space, distributional dual, quotient function class, completed Lebesgue carrier, selected representative, compact embedding theorem, trace theorem, or density theorem.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:sobolev-bhist-carrier}. The visible inventory is exactly $D,M,V,L,G,H,C,P,N$, and each row has the analytic or structural role named in the statement.

Apply the local NameCert obligation surface, the weak-derivative ledger, and the root obligation package. These results give carrier admission, classifier stability, weak-gradient ledger exactness, completion-boundary nonescape, and $\RealUp$ seal nonescape over the same finite rows.

Use the metric completion root window, Hilbert-norm root window, and completion-norm handoff to expose the downstream route. Metric, complete-metric, Hilbert, norm, integral, derivative, PDE, variational, and compactness-facing consumers must traverse the displayed carrier route before reading Sobolev data.

The excluded analytic objects are absent from the carrier and from the cited routes. Transport, replay, provenance, and local naming preserve the package but do not add new analytic coordinates.
\end{proof}

\begin{theorem}[Sobolev root weak-gradient carrier]
\label{thm:sobolev-root-weak-gradient-carrier}
Every accepted $\SobolevUp$ packet $S=(D,M,V,L,G,H,C,P,N)$ exposes weak-gradient data only through the finite $\BHist$ route
$$
D,M,V,L,G,H,C,P,N .
$$
Here $D$ is the $\RealUp$ domain row, $M$ is the $\MetricSpaceUp$ or $\CompleteMetricUp$ base row, $V$ is the $\NormUp$ or $\HilbertUp$ codomain row, $L$ is the $\IntegralUp$ magnitude ledger, $G$ is the $\DerivativeUp$ weak-gradient ledger, $H$ is componentwise $\hsame$ transport, $C$ is displayed $\Cont$ replay, $P$ is $\Pkg$ provenance, and $N$ is local $\NameCert$ evidence. No root weak-gradient read may pass through distributional duals, Lebesgue completion, quotient function equality, compact embedding, or trace theory.
\end{theorem}
\begin{proof}
Project the carrier rows. The accepted packet exposes exactly $D,M,V,L,G,H,C,P,N$, so every weak-gradient read begins with the displayed $\RealUp$, metric or complete-metric, norm or Hilbert, integral, and derivative coordinates.

Route the weak-gradient request through $L$ and $G$. The integral ledger supplies the magnitude surface before the derivative row is interpreted, and the derivative row records only the displayed weak-gradient response.

The structural rows keep the read inside the same packet. Componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ evidence preserve the route without adding distributional duals, Lebesgue completion, quotient equality, compact embedding, or trace data.
\end{proof}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_root\_weak\_gradient\_carrier}

\begin{theorem}[Sobolev root norm-completion ledger]
\label{thm:sobolev-root-norm-completion-ledger}
For every accepted $\SobolevUp$ packet $S=(D,M,V,L,G,H,C,P,N)$, any completion-facing or norm-facing root consumer must read the displayed ledger
$$
D,M,V,L,G,H,C,P,N
$$
as one $\BHist$ packet over $\RealUp$, $\MetricSpaceUp$ or $\CompleteMetricUp$, $\NormUp$ or $\HilbertUp$, $\IntegralUp$, $\DerivativeUp$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and $\NameCert$. The ledger does not create a completed Sobolev space, a Lebesgue-completed measure carrier, quotient equality of functions, a selected representative, a distributional dual, compact embedding, or trace theory.
\end{theorem}
\begin{proof}
Begin with the root-unblock package. It already presents $D,M,V,L,G,H,C,P,N$ as the downstream analysis surface, with $M$ carrying the metric or complete-metric boundary and $V$ carrying the norm or Hilbert boundary.

Route norm and completion reads through the weak-gradient ledgers. The consumer must pass through the integral row $L$ and derivative row $G$ before interpreting Sobolev data as energy or weak-gradient information.

Finally replay and source the same ledger through $H,C,P,N$. These rows preserve the finite packet and refuse completed Sobolev spaces, Lebesgue completion, quotient equality, selected representatives, distributional duals, compact embeddings, and traces as absent coordinates.
\end{proof}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_root\_norm\_completion\_ledger}

\begin{theorem}[Sobolev root trace nonescape]
\label{thm:sobolev-root-trace-nonescape}
Let $S=(D,M,V,L,G,H,C,P,N)$ be an accepted $\SobolevUp$ packet. Every boundary-facing, PDE-facing, or variational read that cites $\RealUp$, $\MetricSpaceUp$ or $\CompleteMetricUp$, $\NormUp$ or $\HilbertUp$, $\IntegralUp$, and $\DerivativeUp$ data from $S$ is confined to the displayed $\BHist$ rows
$$
D,M,V,L,G,H,C,P,N
$$
with componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert$ row $N$. No trace operator, boundary value theorem, distributional dual, Lebesgue completion, quotient function equality, compact embedding, or selected representative escapes this finite route.
\end{theorem}
\begin{proof}
Project the same carrier rows used by the root-unblock package. Boundary-facing consumers receive only the domain row $D$, base row $M$, codomain row $V$, integral row $L$, derivative row $G$, and structural rows $H,C,P,N$.

Route any claimed trace read through $L$ and $G$. The packet permits an integral and weak-gradient ledger read, but it contains no separate boundary-value or trace coordinate.

Use transport, replay, provenance, and naming to close the route. Since $H,C,P,N$ preserve only the displayed packet, distributional duals, Lebesgue completion, quotient equality, compact embedding, selected representatives, and trace theory cannot be exported by a $\SobolevUp$ citation.
\end{proof}

\begin{theorem}[Sobolev test-function source]
\label{thm:sobolev-test-function-source}
Every accepted $\SobolevUp$ test-function read must expose the finite source route
$$
D,M,V,L,G,H,C,P,N
$$
before any weak-gradient test is public. The row $D$ supplies the $\RealUp$ domain surface, $M$ supplies the metric or complete-metric base, $V$ supplies the norm or Hilbert codomain, $L$ supplies the $\IntegralUp$ magnitude ledger, $G$ supplies the $\DerivativeUp$ weak-gradient test row, and $H,C,P,N$ transport, replay, source, and name the same packet. No test-function consumer may cite a host dense subspace, a quotient function representative, a distributional dual, or a selected approximating sequence outside these displayed rows.
\end{theorem}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_test\_function\_source}
\begin{proof}
Project the carrier of \autoref{def:sobolev-bhist-carrier}. Its visible coordinates are exactly $D,M,V,L,G,H,C,P,N$, so a test-function read has no separate coordinate beyond the same finite Sobolev packet.

Apply the weak-derivative route and the integral-derivative source package. These place $L$ before $G$ and make the weak-gradient test public only after the integral magnitude row and derivative response row have both been displayed.

Finally use the root-unblock package. It repacks the carrier, weak-derivative, completion-boundary, Hilbert-norm, and root-window routes into one downstream surface, while the excluded host dense subspace, quotient representative, distributional dual, and selected approximation data remain absent from the carrier.
\end{proof}

\begin{theorem}[Sobolev weak-gradient transport]
\label{thm:sobolev-weak-gradient-transport}
If two accepted $\SobolevUp$ packets have $\hsame$-related carrier rows, then transport preserves the displayed weak-gradient ledger through the $\IntegralUp$ and $\DerivativeUp$ rows. Concretely, the transported read of $D,M,V,L,G,H,C,P,N$ carries the same integral magnitude row $L$, the same weak-gradient response row $G$, and the same structural replay through $H,C,P,N$; it does not create host equality of functions, an ambient almost-everywhere quotient, a completed Lebesgue carrier, or an external weak-gradient predicate.
\end{theorem}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_weak\_gradient\_transport}
\begin{proof}
Start with the carrier energy window. The rows $D,M,V,L,G,H,C,P,N$ form one energy packet, and $L$ and $G$ are the only integral and derivative ledgers available for weak-gradient data.

Use componentwise $\hsame$ transport through $H$. Since $H$ relates the displayed coordinates rather than host functions, the transported packet preserves the $\IntegralUp$ magnitude row and the $\DerivativeUp$ weak-gradient response row as carrier rows.

Replay through $C$ and keep the provenance and naming rows $P,N$ attached to the same route. No step adds host equality of functions, almost-everywhere quotient data, completed Lebesgue measure, or an external weak-gradient predicate.
\end{proof}

\begin{theorem}[Sobolev PDE consumer admission]
\label{thm:sobolev-pde-consumer-admission}
Every PDE-facing or variational consumer may cite $\SobolevUp$ only through the finite rows
$$
D,M,V,L,G,H,C,P,N .
$$
The citation must display the test-function source route of \autoref{thm:sobolev-test-function-source} and the weak-gradient transport route of \autoref{thm:sobolev-weak-gradient-transport}. It supplies no host Sobolev space, quotient function class, Lebesgue completion, compact embedding theorem, trace theory, density theorem, selected representative, or undisplayed weak-gradient oracle.
\end{theorem}
\begin{proof}
Begin with the root-unblock package. It is already the downstream analysis surface for Sobolev data and contains exactly the carrier rows $D,M,V,L,G,H,C,P,N$.

Add the test-function source theorem. A PDE or variational read cannot request weak-gradient tests before the domain, metric, codomain, integral, derivative, transport, replay, provenance, and naming rows have all been displayed.

Add the weak-gradient transport theorem. It ensures that any transported consumer still reads the same $L$ and $G$ ledgers through $H,C,P,N$, rather than through host equality, quotient functions, completed measure theory, or an external weak-gradient predicate.

The remaining exclusions follow by coordinate exhaustion. Compact embeddings, trace theory, density theorems, selected representatives, and host Sobolev spaces are not rows of the accepted packet, so they cannot be exported by a $\SobolevUp$ citation.
\end{proof}

\begin{theorem}[Sobolev weak-gradient energy carrier]
\label{thm:sobolev-weak-gradient-energy-carrier}
For every accepted $\SobolevUp$ packet $S=(D,M,V,L,G,H,C,P,N)$, the weak-gradient energy carrier is the finite route
$$
D \longmapsto M \longmapsto V \longmapsto L \longmapsto G \longmapsto H,C,P,N .
$$
The $\RealUp$ domain row $D$, metric or complete-metric row $M$, norm or Hilbert row $V$, $\IntegralUp$ magnitude row $L$, and $\DerivativeUp$ weak-gradient row $G$ must be read together before any downstream PDE, variational, Hilbert, norm, integral, derivative, or completion consumer receives energy data.

No energy read can be discharged by a host weak-gradient predicate, quotient function class, distributional dual, completed measure carrier, selected representative, compact embedding, trace row, or density theorem.
\end{theorem}
\begin{proof}
Start with the carrier energy window. It identifies $D,M,V,L,G,H,C,P,N$ as one finite energy window and rules out splitting it into a separate measure-completion object or quotient function class.

Apply the weak-derivative route and integral-derivative source package. They place the $\IntegralUp$ magnitude row $L$ immediately before the $\DerivativeUp$ weak-gradient row $G$ and make both rows the displayed source package for derivative-facing consumers.

Use the derivative norm ledger and Hilbert-norm root window. These keep norm and Hilbert-facing reads inside the codomain row $V$ before the integral and derivative rows are interpreted.

Finally replay the route through $H,C,P,N$. Those rows transport, replay, source, and name the same weak-gradient energy carrier, while the excluded host objects remain outside the displayed packet.
\end{proof}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_weak\_gradient\_energy\_carrier}

\begin{theorem}[Sobolev completion-metric handoff]
\label{thm:sobolev-completion-metric-handoff}
Every completion-facing, metric-facing, Hilbert-facing, PDE-facing, or variational consumer of an accepted $\SobolevUp$ packet receives Sobolev data through the displayed handoff
$$
D,M,V,L,G,H,C,P,N .
$$
The base row $M$ is the only $\MetricSpaceUp$ or $\CompleteMetricUp$ boundary read, the codomain row $V$ is the only $\NormUp$ or $\HilbertUp$ magnitude read, and $L,G$ are the only integral and derivative ledgers exported to the consumer. The handoff does not create a completed Sobolev carrier, selected Cauchy limit, quotient function class, distributional dual, completed Lebesgue carrier, compact embedding object, trace row, or density theorem.
\end{theorem}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_completion\_metric\_handoff}
\begin{proof}
Begin with the complete-metric handoff and completion-norm handoff. They force completion-facing consumers to read the base row $M$ and codomain row $V$ only as coordinates of the same finite Sobolev packet.

Apply the weak-gradient energy carrier. Integral and derivative data are then read through $L$ and $G$ downstream of $D,M,V$, with $H,C,P,N$ preserving the route.

Use the completion boundary and completion-boundary nonescape results. These results block completed Sobolev spaces, quotient function carriers, selected limits, completed measure carriers, distributional duals, compact embeddings, traces, and density statements from entering the handoff.

Thus the completion-metric consumer receives exactly the displayed $\BHist$ route and its structural transport, replay, provenance, and naming rows.
\end{proof}

\begin{theorem}[Sobolev source-triple handoff]
\label{thm:sobolev-source-triple-handoff}
Every accepted $\SobolevUp$ packet $S=(D,M,V,L,G,H,C,P,N)$ routes its weak-gradient consumer through the displayed source triple
$$
D \longmapsto M \longmapsto V
$$
before the $\IntegralUp$ magnitude row $L$ and the $\DerivativeUp$ weak-gradient row $G$ are available. Here $D$ is the $\RealUp$ domain row, $M$ is the $\CompleteMetricUp$ or $\MetricSpaceUp$ base row, and $V$ is the $\HilbertUp$ or $\NormUp$ codomain row. The handoff supplies no distribution theory, Lebesgue completion, density of smooth functions, compact embedding, trace theorem, or completed Sobolev space.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:sobolev-bhist-carrier}. Its analytic source rows are exactly $D,M,V,L,G$, with $H,C,P,N$ serving only transport, replay, provenance, and local naming.

Apply the weak-derivative ledger, completion-boundary nonescape, and Hilbert-norm root window. These results order the read from the Real domain row through the metric or complete-metric base and the norm or Hilbert codomain before the integral magnitude and derivative weak-gradient rows are interpreted.

Finally use the root-unblock package and the completion-metric handoff. They package the same $D,M,V,L,G,H,C,P,N$ route for downstream completion-facing and weak-gradient consumers, while the excluded distributional, measure-completion, density, compactness, trace, and completed-space objects remain outside the carrier.
\end{proof}
\leanchecked{BEDC.Derived.SobolevUp.SobolevCarrier\_source\_triple\_handoff}
