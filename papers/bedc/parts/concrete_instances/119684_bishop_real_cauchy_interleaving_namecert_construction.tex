\chapter{A Concrete Naming Certificate for $\BishopRealCauchyInterleavingUp$}
\label{ch:concrete-instances-bishop-real-cauchy-interleaving-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopRealCauchyInterleavingUp$ packet records the finite Real-and-Completion route by which two regular Cauchy names may be interleaved and read as one completion-facing Bishop real name. It sits beside the regular rational carrier of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite window surface of \autoref{ch:concrete-instances-streamname-namecert}, the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}, the parity substream route of \autoref{ch:concrete-instances-regular-cauchy-even-odd-substreams-namecert}, and the interleaving criterion of \autoref{ch:concrete-instances-cauchy-interleaving-criterion-namecert}. The packet is a finite-means certificate for manipulating regular Cauchy real names; it is not a quotient representative, selected limit, countable-choice schedule, host equality of reals, or ambient $\RealUp$ completeness principle.

\begin{definition}[Bishop real Cauchy interleaving carrier]
\label{def:bishop-real-cauchy-interleaving-carrier}
A $\BishopRealCauchyInterleavingUp$ carrier is a finite $\BHist$ packet
$$
I=(S_0,S_1,W_0,W_1,D_0,D_1,J,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S_0$ and $S_1$ are the two source $\RegSeqRatUp$ rows, each carrying a regular Cauchy name before any Real-facing seal is read.
\item $W_0$ and $W_1$ are the $\StreamNameUp$ finite-window rows that expose the inspected even-side and odd-side observation windows.
\item $D_0$ and $D_1$ are the $\DyadicRatCoreUp$ tolerance ledgers attached to the two source rows.
\item $J$ is the $\CauchyInterleavingCriterionUp$ row, recording the finite selector and common-window comparison that licenses the interleaving.
\item $R$ is the $\RegSeqRatUp$ readback row for the interleaved regular Cauchy name produced from the two source rows and the common-window criterion.
\item $E$ is the terminal $\RealUp$ seal reached only after $S_0,S_1,W_0,W_1,D_0,D_1,J,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the two source rows, the two window rows, the two tolerance ledgers, the interleaving criterion, the interleaved readback, the Real seal, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from any Real or Completion request through exactly the same finite rows.
\item $P$ is the $\Pkg$ provenance over $\BishopRealCauchyInterleavingUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegularCauchyEvenOddSubstreamsUp$, $\CauchyInterleavingCriterionUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopRealCauchyInterleavingUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these displayed coordinates. It contains no coordinate for quotient-name equality, selected representatives, countable choice, host real equality, or ambient Real completeness.
\end{definition}

\begin{theorem}[Bishop real Cauchy interleaving NameCert obligations]
\label{thm:bishop-real-cauchy-interleaving-obligations}
For every accepted $\BishopRealCauchyInterleavingUp$ carrier
$$
I=(S_0,S_1,W_0,W_1,D_0,D_1,J,R,E,H,C,P,N),
$$
the local $\NameCert_{\BishopRealCauchyInterleavingUp}$ surface consists exactly of two regular Cauchy source rows, two finite window rows, two dyadic tolerance ledgers, one interleaving criterion row, the interleaved regular readback row, the terminal Real seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every Real or Completion consumer must pass through these rows before reading the interleaved Bishop real name.
\end{theorem}
\leanchecked{BEDC.Derived.BishopRealCauchyInterleavingUp.BishopRealCauchyInterleavingCarrier\_namecert\_obligations}
\begin{proof}
Project the carrier of \autoref{def:bishop-real-cauchy-interleaving-carrier}. The rows $S_0,S_1$ expose the two regular Cauchy names, while $W_0,W_1$ restrict the read to finite stream windows and $D_0,D_1$ attach the visible dyadic tolerance budgets.

The interleaving step is then forced through $J$. That row is the finite $\CauchyInterleavingCriterionUp$ comparison, so the interleaved readback $R$ can be consumed only after the common-window selector and the two source tolerance ledgers have been displayed.

Finally $E,H,C,P,N$ seal, transport, replay, source, and name the same route. Since the carrier has no row for quotient-name equality, selected representatives, countable choice, host real equality, or ambient completeness, these displayed coordinates exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Bishop real Cauchy interleaving window route]
\label{thm:bishop-real-cauchy-interleaving-window-route}
Let
$$
I=(S_0,S_1,W_0,W_1,D_0,D_1,J,R,E,H,C,P,N)
$$
be an accepted $\BishopRealCauchyInterleavingUp$ carrier. Any finite Real or Completion request that reads the interleaved name factors through the sibling route
$$
\RegularCauchyEvenOddSubstreamsUp,\ \CauchyInterleavingCriterionUp,\ \RegSeqRatUp,\ \StreamNameUp,\ \RealUp
$$
on the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. The route exposes the even and odd source windows before the interleaved readback and exports no quotient stream, selected representative, countable-choice schedule, host real equality, or ambient Real-completeness theorem.
\end{theorem}
\begin{proof}
Start with the parity-facing sibling. The $\RegularCauchyEvenOddSubstreamsUp$ route supplies the standard even and odd window discipline, so any request for the interleaved name first reads finite windows $W_0,W_1$ over the two displayed source rows.

Next apply the interleaving criterion row $J$. It binds the two window routes to one common finite comparison before the interleaved $\RegSeqRatUp$ readback $R$ and terminal $\RealUp$ seal $E$ are consumed.

The structural rows $H,C,P,N$ preserve this route inside the same $\BHist$ packet. Therefore every accepted request remains on the displayed Bishop real Cauchy interleaving surface and cannot import any of the excluded global objects.
\end{proof}

\falsifiablePrediction{An accepted $\BishopRealCauchyInterleavingUp$ consumer is refuted if it reaches the $\RealUp$ seal without first displaying both $\RegSeqRatUp$ source rows, both $\StreamNameUp$ window rows, both $\DyadicRatCoreUp$ tolerance ledgers, the $\CauchyInterleavingCriterionUp$ row, the interleaved readback row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ evidence.}

\independenceWitness{regseqrat, streamname, real, regular-cauchy-even-odd-substreams, cauchy-interleaving-criterion: no listed sibling is bijective with $\BishopRealCauchyInterleavingUp$ because this packet jointly carries two source regular Cauchy names, two finite window rows, two dyadic tolerance ledgers, one common interleaving criterion, one interleaved readback, a Real seal, transport, replay, provenance, and local naming.}

\closureat{BishopRealCauchyInterleavingUp}{seedStr}

\begin{closurestatus}{\BishopRealCauchyInterleavingUp}
  \constructivestory{A $\BishopRealCauchyInterleavingUp$ packet is generated from two RegSeqRat source rows, two StreamName finite-window rows, two DyadicRatCore tolerance ledgers, a CauchyInterleavingCriterion row, an interleaved RegSeqRat readback, a terminal RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-real-cauchy-interleaving-carrier}, \autoref{thm:bishop-real-cauchy-interleaving-obligations}, and \autoref{thm:bishop-real-cauchy-interleaving-window-route} seed the finite Bishop real Cauchy interleaving route over regular Cauchy sources, finite windows, dyadic tolerance ledgers, interleaving criteria, Real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient-name equality, selected representatives, countable-choice schedules, host real equality, ambient Real completeness, Lean theorem verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source-row visibility, finite-window exactness, dyadic tolerance compatibility, interleaving criterion stability, interleaved readback nonescape, Real-seal boundary control, transport, replay, provenance, and local naming for BEDC.Derived.BishopRealCauchyInterleavingUp.}
\end{closurestatus}
