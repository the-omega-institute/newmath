\chapter{A Concrete Naming Certificate for $\BishopHeineBorelIntervalUp$}
\label{ch:concrete-instances-bishop-heine-borel-interval-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\BishopHeineBorelIntervalUp$ packet records the finite-net interval compactness read that is available for a located real interval when the endpoints, tolerance rows, and net witnesses are all displayed. It sits on the finite route through $\DyadicRatCoreUp$ of \autoref{ch:concrete-instances-dyadicratcore-namecert}, $\RegSeqRatUp$ of \autoref{ch:concrete-instances-regseqrat-namecert}, $\RealUp$ of \autoref{ch:concrete-instances-real-namecert}, $\LocatedCompactUp$ of \autoref{ch:concrete-instances-locatedcompact-namecert}, and $\TotallyBoundedUp$ of \autoref{ch:concrete-instances-totallybounded-namecert}: dyadic tolerances name the requested mesh, regular rational rows read endpoint and distance windows, the Real seal closes the endpoint names, located compactness supplies the inside-or-apart boundary, and total boundedness supplies the finite net. The packet is not the general open-cover Heine--Borel theorem; it exports no countable choice, quotient real equality, selected finite subcover, or ambient $\RealUp$ completeness.

\begin{definition}[Located compactness finite-net extraction carrier]
\label{def:located-compactness-net-extraction-carrier}
A $\BishopHeineBorelIntervalUp$ carrier is a finite $\BHist$ packet
$$
I=(E,D,R,L,T,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the located interval endpoint row, naming the two $\RealUp$ endpoint seals and the interval orientation check;
\item $D$ is the positive $\DyadicRatCoreUp$ tolerance and mesh ledger used to request a finite net;
\item $R$ is the $\RegSeqRatUp$ readback row for endpoint windows, mesh comparisons, and distance estimates;
\item $L$ is the $\LocatedCompactUp$ locatedness row, exposing the finite inside-or-apart tests for the interval boundary;
\item $T$ is the $\TotallyBoundedUp$ finite-net row, exposing a closed $\mathsf{ProbeBundle}$ of interval centers, $\mathsf{InBundle}$ membership evidence, and coverage radii;
\item $F$ is the extraction row pairing each displayed tolerance request with the corresponding finite list of centers and the coverage comparison read through $D,R,L,T$;
\item $H$ records componentwise $\hsame$ transports for endpoints, dyadic tolerances, regular rational readbacks, locatedness, finite nets, extraction, replay, provenance, and naming;
\item $C$ records displayed $\Cont$ replay from a mesh request through $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\LocatedCompactUp$, and $\TotallyBoundedUp$ before the extraction row is consumed;
\item $P$ is the $\Pkg$ provenance over $\BishopHeineBorelIntervalUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\LocatedCompactUp$, $\TotallyBoundedUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\BishopHeineBorelIntervalUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these ten displayed rows. It has no coordinate for an arbitrary open cover, a choice-selected subcover, quotient real equality, a host compact interval, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Located compactness finite-net extraction obligations]
\label{thm:located-compactness-net-extraction-namecert-obligations}
For every accepted $\BishopHeineBorelIntervalUp$ carrier $I=(E,D,R,L,T,F,H,C,P,N)$, the local $\NameCert_{\BishopHeineBorelIntervalUp}$ surface consists exactly of finite endpoint admission, dyadic mesh admission, regular rational readback, located-boundary testing, total-bounded finite-net exposure, extraction exactness, componentwise transport, displayed replay, provenance, and local naming. Thus the interval compactness read available to Real and Completion consumers is the finite-net extraction surface over $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\LocatedCompactUp$, and $\TotallyBoundedUp$, not an open-cover compactness principle, countable-choice schedule, quotient real equality, selected finite subcover, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Project the accepted packet $I=(E,D,R,L,T,F,H,C,P,N)$. The endpoint row $E$, dyadic ledger $D$, and regular rational readback $R$ name the interval and the requested mesh before any compactness consumer reads data from the packet.

Use the locatedness row $L$ and finite-net row $T$. The located boundary is inspected by finite inside-or-apart comparisons, and the total-bounded surface supplies only closed $\mathsf{ProbeBundle}$ centers, $\mathsf{InBundle}$ membership rows, and coverage radii.

The extraction row $F$ pairs those displayed requests with finite center data. Componentwise transport in $H$ and displayed replay in $C$ preserve the same route through $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\LocatedCompactUp$, and $\TotallyBoundedUp$.

Finally, $P$ and $N$ record provenance and local naming for exactly that route. Since the carrier contains no open-cover, choice, quotient-equality, selected-subcover, or ambient-completeness coordinate, the listed obligations exhaust the NameCert surface.
\end{proof}

\begin{closurestatus}{\BishopHeineBorelIntervalUp}
  \constructivestory{A $\BishopHeineBorelIntervalUp$ packet is generated from displayed Real endpoint seals, a DyadicRatCoreUp mesh ledger, RegSeqRatUp readbacks, a LocatedCompactUp located-boundary row, a TotallyBoundedUp finite-net row, an extraction row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:located-compactness-net-extraction-carrier} and \autoref{thm:located-compactness-net-extraction-namecert-obligations} close the paper-side finite-net extraction surface for located real intervals over the displayed Real, RegSeqRat, DyadicRatCore, LocatedCompact, and TotallyBounded route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim general open-cover Heine--Borel compactness, countable choice, quotient real equality, selected finite subcovers, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{A formal route can encode the ten-row carrier and prove the finite-net extraction obligation theorem.}
\end{closurestatus}
