\chapter{A Concrete Naming Certificate for $\RealCompletionUniversalPropertyUp$}
\label{ch:concrete-instances-real-completion-universal-property-namecert}
\origin{human}

Following Bishop and Bridges on constructive completions, the $\RealCompletionUniversalPropertyUp$ packet records the L10 universal-property neighbour of the Real completion interface as finite NameCert data. It consumes the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the completion interface of \autoref{ch:concrete-instances-cauchy-real-completion-interface-namecert}, and the Bishop regular Cauchy completion route of \autoref{ch:concrete-instances-bishop-regular-cauchy-completion-namecert}. Its universal-property row is consumed by the Bishop completion comparison square of \autoref{ch:concrete-instances-bishop-real-completion-adjoint-namecert} and by the Cauchy completion functor route of \autoref{ch:concrete-instances-cauchycompletionfunctor-namecert}, so the extension-uniqueness packet is a visible L10 lattice node rather than an isolated completion label. The packet names extension and uniqueness rows for completion-facing consumers without asserting quotient streams, selected limits, host equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Real completion universal-property carrier]
\label{def:real-completion-universal-property-carrier}
A $\RealCompletionUniversalPropertyUp$ carrier is a finite $\BHist$ packet
$$
G=(D,S,R,B,E,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ dense-source row, exposing only named dyadic tolerances and dyadic source observations.
\item $S$ is the $\StreamNameUp$ finite-window row through which completion-facing source reads are scheduled.
\item $R$ is the $\RegSeqRatUp$ regular-readback row attached to the same stream windows and dyadic tolerances.
\item $B$ is the $\BishopRegularCauchyCompletionUp$ handoff row, recording the regular Cauchy completion route before any terminal Real seal is consumed.
\item $E$ is the $\CauchyRealCompletionInterfaceUp$ extension row, displaying the completion-interface read that receives $D,S,R,B$.
\item $U$ is the universal-property row: it records existence and uniqueness of the displayed extension only as a finite route over $D,S,R,B,E$.
\item $H$ records componentwise $\hsame$ transport for the dyadic, stream, regular-readback, completion, interface, universal-property, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a dense-source request through $D,S,R,B,E,U,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RealCompletionUniversalPropertyUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\BishopRegularCauchyCompletionUp$, $\CauchyRealCompletionInterfaceUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealCompletionUniversalPropertyUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $D,S,R,B,E,U,H,C,P,N$. It contains no coordinate for quotient streams, selected limits, host equality, countable choice, ambient $\RealUp$ completeness, or bridge credentials.
\end{definition}

\begin{theorem}[Real completion universal-property NameCert obligations]
\label{thm:real-completion-universal-property-namecert-obligations}
For every accepted $\RealCompletionUniversalPropertyUp$ carrier $G=(D,S,R,B,E,U,H,C,P,N)$, the local $\NameCert_{\RealCompletionUniversalPropertyUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the displayed dyadic source, stream-window, regular-readback, Bishop regular Cauchy completion, completion-interface, universal-property, transport, replay, provenance, and local naming rows;
\item dense-source exposure, requiring every extension read to project $D$ before inspecting the completion-interface row;
\item stream-window exactness, requiring $S$ to schedule the finite observations consumed by the regular-readback and completion handoff rows;
\item regular-readback handoff, requiring $R$ and then $B$ to mediate between the finite windows and the completion-facing interface;
\item extension existence, requiring $E$ and $U$ to display a completion-interface extension over the same $D,S,R,B$ coordinates;
\item extension uniqueness, requiring any two accepted extension reads over the same displayed dyadic, stream, regular-readback, and terminal Real-seal rows to share the same completion-facing route;
\item non-escape, requiring $H,C,P,N$ to transport, replay, source, and name no quotient stream, selected limit, host equality, countable-choice schedule, ambient Real-completeness theorem, Lean verification, or bridge credential.
\end{enumerate}
No local obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:real-completion-universal-property-carrier}. The accepted tuple displays exactly the dyadic source, stream-window, regular-readback, completion, interface, universal-property, transport, replay, provenance, and local naming rows.

The dense-source and stream-window clauses follow from the order of the displayed route. An extension read must first expose $D$ and then schedule finite observations through $S$ before the regular readback or completion interface can be consumed.

The regular-readback handoff is carried by $R$ and $B$. These rows bind the stream windows and dyadic tolerances to the Bishop regular Cauchy completion route, so the interface cannot choose a quotient representative or host limit.

The extension rows $E$ and $U$ then record existence and uniqueness only over the fixed $D,S,R,B$ coordinates. Transport, replay, provenance, and local naming through $H,C,P,N$ preserve that route and provide no coordinate for the excluded principles.
\end{proof}

\begin{theorem}[Real completion universal-property carrier rows]
\label{thm:real-completion-universal-property-carrier-rows}
Every accepted $\RealCompletionUniversalPropertyUp$ carrier displays exactly the rows
$$
D,S,R,B,E,U,H,C,P,N
$$
of \autoref{def:real-completion-universal-property-carrier} before the NameCert obligations of \autoref{thm:real-completion-universal-property-namecert-obligations} can be read. The dense source, stream windows, regular readback, Bishop completion handoff, completion-interface extension, and universal-property rows are therefore public only as one finite route.
\end{theorem}
\begin{proof}
Project the carrier definition. It names $D,S,R,B,E,U,H,C,P,N$ as the full packet, with no coordinate for a quotient stream, selected completed point, host equality, or ambient completeness theorem.

Apply the NameCert obligation theorem. Its seven obligation rows are indexed by the same carrier coordinates, so each public universal-property read must expose the dense source, finite windows, readback, completion handoff, extension row, and uniqueness route in that order.

The structural rows preserve the displayed packet. They replay and name the route already present in the carrier rather than adding a second completion principle.
\end{proof}

\begin{theorem}[Real completion universal-property extension route]
\label{thm:real-completion-universal-property-extension-route}
Let $G=(D,S,R,B,E,U,H,C,P,N)$ be an accepted $\RealCompletionUniversalPropertyUp$ carrier. Any completion-facing extension read from $G$ must consume $\DyadicRatCoreUp$ through $D$, $\StreamNameUp$ through $S$, $\RegSeqRatUp$ through $R$, and the Bishop completion row through $B$ before the completion-interface row $E$ and universal-property row $U$ are public.
\end{theorem}
\begin{proof}
Start with the carrier rows of \autoref{thm:real-completion-universal-property-carrier-rows}. The dense dyadic row $D$, finite stream row $S$, regular readback row $R$, and Bishop completion row $B$ are the entrance rows of the packet.

Use the obligation theorem \autoref{thm:real-completion-universal-property-namecert-obligations}. Dense-source exposure, stream-window exactness, and regular-readback handoff force an extension read to consume those sibling rows before the completion-interface row is inspected.

The extension rows $E$ and $U$ then record the public extension and uniqueness route over the same $D,S,R,B$ coordinates. The structural rows $H,C,P,N$ keep the route local to the carrier.
\end{proof}

\begin{theorem}[Real completion universal-property extension uniqueness scope]
\label{thm:real-completion-universal-property-extension-uniqueness-scope}
Let $G=(D,S,R,B,E,U,H,C,P,N)$ and $G'=(D',S',R',B',E',U',H',C',P',N')$ be accepted $\RealCompletionUniversalPropertyUp$ carriers. Suppose the displayed rows $D,S,R,B,E$ and $D',S',R',B',E'$ are related componentwise by the $\hsame$ transports recorded in $H$ and $H'$, and suppose the two packets are replayed by the displayed $\Cont$ rows $C$ and $C'$. Then any two public $U$ rows naming completion-facing extensions share the same finite extension route through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\BishopRegularCauchyCompletionUp$, and $\CauchyRealCompletionInterfaceUp$.

The uniqueness export is scoped to that route. It exports no quotient stream, selected limit, host equality, countable-choice schedule, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Project the two accepted carriers. Their public extension rows can be read only after the displayed dyadic, stream, regular-readback, Bishop completion, and completion-interface coordinates have been exposed.

Apply the NameCert obligation theorem \autoref{thm:real-completion-universal-property-namecert-obligations}. The extension existence and uniqueness rows are indexed by $D,S,R,B,E$ in the first carrier and by $D',S',R',B',E'$ in the second carrier, so the comparison has no independent quotient or limit coordinate to inspect.

Use the extension route theorem \autoref{thm:real-completion-universal-property-extension-route}. It forces every completion-facing extension read to consume $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\BishopRegularCauchyCompletionUp$, and $\CauchyRealCompletionInterfaceUp$ in that order before the universal-property row is public.

Finally use the structural rows. The transports $H,H'$ relate the corresponding dyadic, stream, readback, completion, and interface rows, while the replay rows $C,C'$ preserve their order. The provenance and local naming rows $P,N,P',N'$ repack the same route as a public certificate, and no row remains for quotient streams, selected limits, host equality, countable choice, or ambient $\RealUp$ completeness.
\end{proof}

\begin{theorem}[Real completion universal-property nonescape]
\label{thm:real-completion-universal-property-nonescape}
Every public $\RealCompletionUniversalPropertyUp$ universal-property read factors through the carrier rows
$$
D,S,R,B,E,U,H,C,P,N
$$
and through the extension route of \autoref{thm:real-completion-universal-property-extension-route}. It cannot export quotient streams, selected limits, host equality, countable choice, ambient $\RealUp$ completeness, or a detached universal-property principle outside the displayed dense-source and completion-interface rows.
\end{theorem}
\begin{proof}
Combine the carrier-row theorem with the extension route theorem. Together they force the public read through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, the Bishop completion row, the completion-interface row, and the universal-property row.

Apply the non-escape obligation from \autoref{thm:real-completion-universal-property-namecert-obligations}. The only remaining rows are transport, replay, provenance, and local naming, and each preserves the displayed route.

Since the carrier stores no quotient stream, selected limit, host equality, choice schedule, ambient completeness theorem, or detached adjunction datum, none can be exported by a public universal-property consumer.
\end{proof}

\begin{theorem}[Real completion universal-property obligation upgrade]
\label{thm:real-completion-universal-property-obligation-upgrade}
For every accepted $\RealCompletionUniversalPropertyUp$ carrier $G=(D,S,R,B,E,U,H,C,P,N)$, the seven obligation rows of \autoref{thm:real-completion-universal-property-namecert-obligations} form the obligation surface for $\RealCompletionUniversalPropertyUp$ over $D,S,R,B,E,U,H,C,P,N$. They are not merely seed prose: carrier admission, dense-source exposure, stream-window exactness, regular-readback handoff, extension existence, extension uniqueness, and non-escape together determine the public universal-property route for completion-facing consumers without quotient streams, selected limits, countable choice, host equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the existing obligation list of \autoref{thm:real-completion-universal-property-namecert-obligations}. Its seven rows are already indexed by the displayed carrier coordinates $D,S,R,B,E,U,H,C,P,N$, so the public surface is read from the finite packet rather than from an external completion theorem.

Use factorization through \autoref{thm:real-completion-universal-property-factorization-obligation}. The dense-source, stream-window, regular-readback, completion-handoff, and extension rows force any universal factorization witness to pass through the displayed dyadic, stream, readback, completion, interface, and universal-property coordinates.

Next apply Real-seal uniqueness and extension-uniqueness factorization, \autoref{thm:real-completion-universal-property-real-seal-uniqueness} and \autoref{thm:real-completion-universal-property-extension-uniqueness-factorization}. These rows bind uniqueness to the same $D,S,R,B,E,U,H,C,P,N$ route.

The non-escape row is then carried by $H,C,P,N$. Since the carrier contains no quotient-stream, selected-limit, countable-choice, host-equality, or ambient-completeness coordinate, the seven rows constitute the obligation surface and do not export any stronger completion principle.
\end{proof}

\begin{theorem}[Real completion universal-property factorization obligation]
\label{thm:real-completion-universal-property-factorization-obligation}
Any accepted $\RealCompletionUniversalPropertyUp$ carrier must factor a completion-facing map through the displayed $\StreamNameUp$ finite-window row, $\RegSeqRatUp$ regular readback row, terminal $\RealUp$ seal row, Cauchy-modulus row, and provenance rows before it exports a universal factorization witness. The carrier has no coordinate for quotient representatives, selected limits, host real equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-completion-universal-property-carrier}. The completion-facing route enters through the dyadic source row $D$, finite stream window $S$, regular readback row $R$, Bishop regular Cauchy completion handoff $B$, completion-interface row $E$, and universal-property row $U$ before transport, replay, provenance, and local naming are read.

Apply the local NameCert obligations of \autoref{thm:real-completion-universal-property-namecert-obligations}. Dense-source exposure, stream-window exactness, regular-readback handoff, extension existence, extension uniqueness, and non-escape force a universal factorization witness to pass through the displayed finite-window, readback, Cauchy-modulus, Real-seal, and provenance rows.

The sibling routes \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert} supply those rows as finite BEDC surfaces. Since the carrier definition stores no quotient representative, selected limit, host equality, countable-choice schedule, or ambient completeness coordinate, this is a concrete L10 carrier obligation rather than a parameter-transport theorem.
\end{proof}

\begin{theorem}[Real completion universal-property Real-seal uniqueness]
\label{thm:real-completion-universal-property-real-seal-uniqueness}
Let $G=(D,S,R,B,E,U,H,C,P,N)$ be an accepted $\RealCompletionUniversalPropertyUp$ carrier. If two accepted extension reads display the same dyadic row $D$, stream row $S$, regular-readback row $R$, Bishop regular Cauchy completion row $B$, and terminal $\RealUp$ seal row named by the completion interface, then they have the same completion-facing route through $E,U,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier and the two accepted reads. The hypotheses identify the dyadic source, finite windows, regular readback, Bishop regular Cauchy handoff, and terminal Real seal rows.

Apply the extension uniqueness row of \autoref{thm:real-completion-universal-property-namecert-obligations}. Since uniqueness is recorded over exactly the displayed $D,S,R,B,E$ coordinates, the two reads cannot diverge into separate completion-interface routes.

The remaining structural rows $H,C,P,N$ transport, replay, source, and name the same accepted route. Because the carrier has no quotient-stream, selected-limit, host-equality, countable-choice, ambient-completeness, or bridge coordinate, the common displayed seal determines the completion-facing route.
\end{proof}

\begin{theorem}[Real completion universal-property extension-uniqueness factorization]
\label{thm:real-completion-universal-property-extension-uniqueness-factorization}
Let $G=(D,S,R,B,E,U,H,C,P,N)$ be an accepted $\RealCompletionUniversalPropertyUp$ carrier. Any two extension reads over the same dyadic row $D$, stream row $S$, regular-readback row $R$, Bishop regular Cauchy completion row $B$, and completion-interface extension row $E$ factor through the same $U,H,C,P,N$ route before a completion-facing consumer can read uniqueness. The factorization exports no quotient stream, selected limit, host equality, countable-choice schedule, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Project the accepted carrier and the two extension reads. The common coordinates $D,S,R,B,E$ are the only entrance rows for extension data in \autoref{def:real-completion-universal-property-carrier}.

Apply the NameCert obligation row of \autoref{thm:real-completion-universal-property-namecert-obligations}. Extension uniqueness is stored over the displayed extension route, so the two reads must pass through the same universal-property row $U$ and the same structural rows $H,C,P,N$ before a consumer can use the uniqueness claim.

The non-escape inventory of the carrier then excludes alternate routes. Since quotient streams, selected limits, host equality, countable choice, and ambient $\RealUp$ completeness are absent from $G$, the uniqueness read cannot be reinterpreted as any of those principles.
\end{proof}

\begin{theorem}[Real completion universal-property Bishop Kleisli comparison]
\label{thm:real-completion-universal-property-bishop-kleisli-comparison}
Let $G=(D,S,R,B,E,U,H,C,P,N)$ be an accepted $\RealCompletionUniversalPropertyUp$ carrier. Any Bishop Kleisli triangle consumer may read the $\RealCompletionUniversalPropertyUp$ universal-property row only through the displayed route
$$
D,S,R,B,E,U,H,C,P,N
$$
and then through the Bishop triangle route
$$
M,K,F,U,R,E,J,H,C,P,N .
$$
The comparison exports no quotient stream, selected completed point, host equality, countable-choice schedule, ambient $\RealUp$ completeness, or detached adjunction principle.
\end{theorem}
\begin{proof}
Project the accepted carrier $G$. By \autoref{thm:real-completion-universal-property-namecert-obligations}, its universal-property row is visible only after the dyadic source, stream windows, regular readback, Bishop regular Cauchy completion handoff, and completion-interface extension rows have been displayed.

Use \autoref{thm:real-completion-universal-property-extension-uniqueness-factorization} for the uniqueness handoff. Any completion-facing read of uniqueness must pass through $U,H,C,P,N$ over the same $D,S,R,B,E$ coordinates before it reaches a downstream consumer.

Finally apply the Bishop triangle obligation surface of \autoref{thm:bishop-completion-kleisli-triangle-obligations}. That consumer route reads the monad, Kleisli, functor, universal-property, regular readback, Real seal, triangle ledger, transport, replay, provenance, and local naming rows as $M,K,F,U,R,E,J,H,C,P,N$. Since both packets exclude quotient streams, selected completed points, host equality, countable choice, ambient $\RealUp$ completeness, and detached adjunction data, the comparison exports none of those objects.
\end{proof}

\begin{theorem}[Real completion universal-property diagonal-family extension]
\label{thm:real-completion-universal-property-diagonal-family-extension}
Let $G=(D,S,R,B,E,U,H,C,P,N)$ be an accepted $\RealCompletionUniversalPropertyUp$ carrier. Any family-extension export from $G$ must be displayed through a finite-window $\StreamNameUp$ family, the $\RegSeqRatUp$ readback row, a $\NullSequenceUp$ zero-tail comparison, the terminal $\RealUp$ seal, and the same universal-property route
$$
D,S,R,B,E,U,H,C,P,N .
$$
The export can therefore extend only the displayed diagonal family served by those rows. It exports no quotient streams, selected limits, host equality, countable choice, ambient $\RealUp$ completeness, or hidden equality principle.
\end{theorem}
\begin{proof}
Project the accepted carrier. The only universal-property entrance rows are the dyadic source $D$, finite stream windows $S$, regular readback $R$, Bishop regular Cauchy completion handoff $B$, completion-interface row $E$, universal-property row $U$, and the structural rows $H,C,P,N$.

Apply \autoref{thm:real-completion-universal-property-extension-uniqueness-factorization}. Any extension read over the displayed dyadic, stream, regular-readback, Bishop completion, and interface rows factors through the same $U,H,C,P,N$ route before uniqueness can be consumed.

Next use \autoref{thm:real-completion-universal-property-bishop-kleisli-comparison}. A Bishop Kleisli triangle consumer reads the universal-property row only after the $D,S,R,B,E,U,H,C,P,N$ route is visible, so a family extension cannot bypass the finite-window and regular-readback stages.

The diagonal family adds a $\NullSequenceUp$ zero-tail comparison as a displayed consumer row between the regular readback and the Real seal. Since the carrier has no coordinate for quotient streams, selected limits, host equality, choice, or ambient completeness, the resulting extension is exactly the displayed finite-window family route and no stronger completion principle.
\end{proof}

\closureat{RealCompletionUniversalPropertyUp}{seedStr}

\begin{closurestatus}{\RealCompletionUniversalPropertyUp}
  \constructivestory{A $\RealCompletionUniversalPropertyUp$ packet is generated from DyadicRatCore dense-source rows, StreamName finite windows, RegSeqRat regular readback, BishopRegularCauchyCompletion handoff, CauchyRealCompletionInterface extension rows, a universal-property row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-completion-universal-property-carrier}, \autoref{thm:real-completion-universal-property-namecert-obligations}, and \autoref{thm:real-completion-universal-property-real-seal-uniqueness} seed the finite universal-property packet over dyadic, stream, regular-readback, completion, interface, extension, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient streams, selected limits, host equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dense-source exposure, stream-window exactness, regular-readback handoff, extension existence, extension uniqueness, and non-escape for BEDC.Derived.RealCompletionUniversalPropertyUp.}
\end{closurestatus}
