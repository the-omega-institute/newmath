The $\RealCompletionUniversalPropertyUp$ hub exposes its completion lattice edge
before any universal-property row is public. A public read consumes
$\CompletionUp$ and $\RealUp$ through the Bishop regular completion handoff,
the regular rational readback, the finite stream window, and the dyadic source
ledger:
$$
\CompletionUp,\RealUp
\longleftarrow
\BishopRegularCauchyCompletionUp
\longleftarrow
\RegSeqRatUp
\longleftarrow
\StreamNameUp
\longleftarrow
\DyadicRatCoreUp .
$$
The route is anchored by
\autoref{ch:concrete-instances-completion-namecert},
\autoref{ch:concrete-instances-real-namecert},
\autoref{ch:concrete-instances-regseqrat-namecert}, and
\autoref{ch:concrete-instances-streamname-namecert}. It makes the L10
completion dependency visible at the hub boundary while leaving the existing
sibling-dependency theorem as the detailed local certificate.
