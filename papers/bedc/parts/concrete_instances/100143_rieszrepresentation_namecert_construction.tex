\chapter{A Concrete Naming Certificate for $\RieszRepresentationUp$}
\label{ch:concrete-instances-rieszrepresentation-namecert}
\origin{human}

Following Riesz, the Riesz representation packet treats a bounded scalar-valued functional as data that can be read against a representing vector or representing measure only after the relevant Banach, Hilbert, norm, and measure surfaces have already supplied their carriers and classifiers. It depends on $\NameCert_{\BanachUp}$ (\autoref{ch:concrete-instances-banach-namecert}), $\NameCert_{\HilbertUp}$ (\autoref{ch:concrete-instances-hilbert-namecert}), $\NameCert_{\FunctionalAnalysisUp}$ (\autoref{ch:concrete-instances-functionalanalysis-namecert}), $\NameCert_{\NormUp}$ (\autoref{ch:concrete-instances-norm-namecert}), and $\NameCert_{\MeasureUp}$ (\autoref{ch:concrete-instances-measure-namecert}). The packet is a cross-chapter representation surface: it names the carrier and obligations needed by later spectral and measure-representation chapters without asserting every classical representation theorem at once.

\section{Carrier definition}
\label{sec:rieszrepresentation-carrier}

\begin{definition}[RieszRepresentation carrier]
\label{def:rieszrepresentation-carrier}
A $\RieszRepresentationUp$ carrier is a representation row
$$
\mathsf{RieszRep}(S,T,\lambda,r,\Lambda)
$$
whose source $S$ is either a Hilbert row or a locally measured Banach-function row supplied by the dependency certificates named above. The target $T$ is the scalar $\RealUp$ face used by the bounded-functional row of $\FunctionalAnalysisUp$, $\lambda$ is the bounded scalar-valued functional row, $r$ is the proposed representing datum, and $\Lambda$ is the visible ledger binding the functional row to the representing datum.

In the Hilbert branch, $r$ is a carried Hilbert endpoint and the ledger records the row
$$
\lambda(x) \sim_{\RealUp} \langle x,r\rangle
$$
for every carried Hilbert endpoint $x$, using the inner-product and norm rows of $\HilbertUp$. In the measure branch, $r$ is a finite signed or positive measure row over the displayed measurable carrier, and the ledger records scalar integration rows against bounded test endpoints. The classifier compares two representation rows only by inherited transport of the functional endpoint, representing datum, scalar value rows, and ledger entries; it does not identify host-level functions, host-level vectors, or host-level measures.
\end{definition}

\section{Certificate obligations}
\label{sec:rieszrepresentation-obligations}

\begin{theorem}[RieszRepresentation NameCert obligation surface]
\label{thm:rieszrepresentation-namecert-obligation-surface}
The $\RieszRepresentationUp$ packet exposes exactly the following NameCert obligation surface:
\begin{enumerate}
  \item a source row from $\BanachUp$, $\HilbertUp$, $\FunctionalAnalysisUp$, $\NormUp$, and $\MeasureUp$ identifying the carried functional domain;
  \item a bounded scalar-valued functional row inherited from $\FunctionalAnalysisUp$;
  \item a representing datum row, either a Hilbert endpoint or a measure endpoint, with its own carrier and classifier;
  \item a scalar evaluation ledger comparing the functional value with the inner-product or integration value on every carried test endpoint;
  \item a boundary row saying which representation branch is present and which branch is outside the current certificate.
\end{enumerate}
The classifier preserves the packet precisely when these five rows are transported by their inherited classifiers.
\end{theorem}
\begin{proof}
Read the bounded functional through the dual-space face of $\FunctionalAnalysisUp$. That face already fixes the source carrier, scalar target, pointwise classifier, norm-bound row, and visible ledger, so the representation packet begins with a stable functional endpoint rather than a host-level functional equality.

For the Hilbert branch, use the carrier-sharing rows of $\HilbertUp$. The representing datum is a carried Hilbert endpoint, and the displayed scalar comparison is an inner-product value row transported by the same Hilbert classifier that transports the functional source endpoint.

For the measure branch, use the event and value rows of $\MeasureUp$. The representing datum is a measure endpoint, and the displayed scalar comparison is an integration ledger over the carried test endpoints. Transport preserves the test endpoint, measure endpoint, scalar output, and evidence row componentwise.

These two branches share only the bounded-functional and scalar-output faces. The boundary row records which branch is active, so the certificate does not collapse vector representation and measure representation into one host-level equality.
\end{proof}
\leanchecked{BEDC.Derived.RieszRepresentationUp.RieszRepresentationNameCertObligations}

\begin{theorem}[RieszRepresentation Hilbert functional bridge]
\label{thm:rieszrepresentation-hilbert-functional-bridge}
For a Hilbert source already accepted by $\NameCert_{\HilbertUp}$, a $\RieszRepresentationUp$ Hilbert branch row packages a bounded scalar-valued functional $\lambda$ together with a representing endpoint $r$ and the scalar ledger
$$
\lambda(x) \sim_{\RealUp} \langle x,r\rangle
$$
on every carried endpoint $x$. If the functional row, Hilbert endpoint row, or representing endpoint row is transported by the inherited classifiers, then the displayed scalar ledger transports to the new row and exposes the same $\FunctionalAnalysisUp$ dual-space face.
\end{theorem}
\begin{proof}
Start from the $\HilbertUp$ dependency. It supplies a Banach surface, an inner-product surface, the norm-induced compatibility ledger, and classifier transport for carried endpoints.

Next read $\lambda$ through the $\FunctionalAnalysisUp$ dual-space face. Its pointwise classifier compares scalar outputs on carried Hilbert inputs, so it is already stated in the form needed by the displayed ledger.

Finally transport the representing endpoint through the Hilbert classifier. Inner-product stability moves $\langle x,r\rangle$ along the transported endpoint rows, and the scalar $\RealUp$ classifier moves the value comparison. The repacked row is therefore the same dual-space face plus a transported representing endpoint ledger.
\end{proof}
\leanchecked{BEDC.Derived.RieszRepresentationUp.RieszRepresentationHilbertFunctionalBridge}

\begin{theorem}[Riesz representation dual functional route]
\label{thm:riesz-representation-dual-functional-route}
Every $\RieszRepresentationUp$ bounded-functional branch must read its source through the displayed $\FunctionalAnalysisUp$ dual-space row, the $\NormUp$ bound row, and the $\BanachUp$ carrier row before a representing datum is consumed. The route exposes the functional endpoint, scalar $\RealUp$ target, bound ledger, source carrier, and branch boundary as finite rows; it does not assert arbitrary dual identification, nonconstructive Hahn--Banach extension, uniqueness of representing data, or host-level equality of linear maps.
\end{theorem}
\begin{proof}
Start with the source row of \autoref{thm:rieszrepresentation-namecert-obligation-surface}. It already requires the Banach, FunctionalAnalysis, and Norm certificates to be visible before a bounded scalar-valued functional row is read.

Next use the Hilbert bridge of \autoref{thm:rieszrepresentation-hilbert-functional-bridge} only for the branch in which the source is Hilbert. The same bounded-functional face remains the entry point, but the representing datum is then a carried Hilbert endpoint rather than an arbitrary dual object.

For the non-Hilbert branch, keep the boundary row active. Since no row supplies arbitrary dual identification, Hahn--Banach extension, representing uniqueness, or host-level linear-map equality, the route is exactly the finite dual-functional surface named in the theorem.
\end{proof}
\leanchecked{BEDC.Derived.RieszRepresentationUp.RieszRepresentationDualFunctionalRoute}

\begin{theorem}[Riesz representation measure handoff]
\label{thm:riesz-representation-measure-handoff}
The measure branch of a $\RieszRepresentationUp$ packet hands a bounded functional row to $\MeasureUp$ only after the source carrier, scalar target, boundedness ledger, measurable endpoint, and test-endpoint integration ledger have been displayed. The handoff records a finite measure-facing representation surface; it does not claim uniqueness of representing measures, full measure representation, Radon regularity, countable choice, or ambient completeness of the scalar target.
\end{theorem}
\begin{proof}
Read the bounded functional through \autoref{thm:riesz-representation-dual-functional-route}. This fixes the source carrier, scalar target, and norm-bound ledger before the measure endpoint is inspected.

Now project the measure branch listed in \autoref{def:rieszrepresentation-carrier}. The representing datum is a measure endpoint, and the scalar comparison is an integration ledger over carried bounded test endpoints.

The branch boundary records that this is a handoff to $\MeasureUp$, not a full representation theorem. Because the packet has no coordinate for uniqueness, Radon regularity, countable choice, or ambient scalar completeness, the handoff cannot export those endpoints.
\end{proof}
\leanchecked{BEDC.Derived.RieszRepresentationUp.RieszRepresentationMeasureHandoff}

\begin{theorem}[Riesz representation Banach-Hilbert scope]
\label{thm:riesz-representation-banach-hilbert-scope}
The exact scope of the $\RieszRepresentationUp$ packet is the finite family of rows supplied by $\BanachUp$, $\HilbertUp$, $\FunctionalAnalysisUp$, $\NormUp$, and $\MeasureUp$: a source carrier, bounded scalar-valued functional, norm-bound ledger, optional Hilbert representing endpoint, optional measure endpoint, scalar comparison ledger, branch boundary, and inherited classifier transport. No accepted packet extends this scope to arbitrary nonconstructive duality, spectral representation, selected representing datum, quotient equality of functions, or uniqueness of representing measures.
\end{theorem}
\begin{proof}
Combine the obligation surface of \autoref{thm:rieszrepresentation-namecert-obligation-surface} with the Hilbert bridge of \autoref{thm:rieszrepresentation-hilbert-functional-bridge}. These two rows display the source carrier, bounded functional, Hilbert representing endpoint when present, scalar comparison ledger, and branch boundary.

Add the dual-functional route and measure handoff above. They force the Banach, FunctionalAnalysis, Norm, and Measure rows to remain visible before either a Hilbert or measure representation read is consumed.

The classifier of \autoref{def:rieszrepresentation-carrier} compares packets only through the inherited rows and ledger entries. Therefore the scope is exactly the five named dependency certificates and their branch rows, with the excluded representation theorems left outside the packet.
\end{proof}
\leanchecked{BEDC.Derived.RieszRepresentationUp.RieszRepresentationBanachHilbertScope}

\begin{theorem}[Riesz representation branch-boundary nonescape]
\label{thm:riesz-representation-branch-boundary-nonescape}
Every accepted $\RieszRepresentationUp$ packet keeps the Hilbert representing-endpoint row and the measure representing-endpoint row separated by the branch boundary of \autoref{def:rieszrepresentation-carrier}. A consumer may project one branch only after reading the shared $\FunctionalAnalysisUp$ bounded-functional row and the active branch row. It cannot infer representing uniqueness, Radon regularity, Hahn--Banach extension, spectral representation, or host-level equality from the inactive branch.
\end{theorem}
\begin{proof}
Use the NameCert obligation surface of \autoref{thm:rieszrepresentation-namecert-obligation-surface}. Its boundary row is part of every accepted packet, so a consumer cannot read either representing endpoint before the active branch has been displayed.

For the Hilbert side, apply the Hilbert functional bridge of \autoref{thm:rieszrepresentation-hilbert-functional-bridge}. It identifies the representing datum only as a carried Hilbert endpoint tied to the shared bounded-functional row and scalar comparison ledger.

For the measure side, apply the dual-functional route of \autoref{thm:riesz-representation-dual-functional-route} and the measure handoff of \autoref{thm:riesz-representation-measure-handoff}. These rows keep the bounded-functional face visible while exposing only the measure endpoint and its test-endpoint integration ledger.

Finally project the boundary coordinate from the carrier definition. Since the classifier compares packets through the active branch rows and the shared bounded-functional row, no inactive branch coordinate can export uniqueness, Radon regularity, Hahn--Banach extension, spectral representation, or host-level equality.
\end{proof}
\leanchecked{BEDC.Derived.RieszRepresentationUp.RieszRepresentationBranchBoundaryNonescape}

\begin{theorem}[RieszRepresentation public export]
\label{thm:rieszrepresentation-public-export}
The public export surface of a $\RieszRepresentationUp$ packet is the finite certificate obtained by composing the obligation surface of \autoref{thm:rieszrepresentation-namecert-obligation-surface}, the Hilbert functional bridge of \autoref{thm:rieszrepresentation-hilbert-functional-bridge}, the dual-functional route of \autoref{thm:riesz-representation-dual-functional-route}, the measure handoff of \autoref{thm:riesz-representation-measure-handoff}, the Banach-Hilbert scope of \autoref{thm:riesz-representation-banach-hilbert-scope}, and the branch-boundary nonescape theorem of \autoref{thm:riesz-representation-branch-boundary-nonescape}. A public consumer may read only the source carrier, bounded scalar-valued functional row, active representing datum row, scalar comparison ledger, inherited classifier transports, and branch boundary displayed by these certificates; it receives no Hahn--Banach extension, representing uniqueness, Radon regularity, spectral representation, selected representative, or host-level equality of functional rows.
\end{theorem}
\leanchecked{BEDC.Derived.RieszRepresentationUp.RieszRepresentationPublicExport}
\begin{proof}
Start with the obligation surface. It supplies the source row, bounded scalar-valued functional row, representing datum row, scalar evaluation ledger, and branch boundary as the local NameCert inventory.

For the Hilbert branch, use the Hilbert functional bridge to expose only a carried Hilbert endpoint and its inner-product scalar ledger. For the shared bounded-functional entry, use the dual-functional route so that every public read still passes through the displayed $\FunctionalAnalysisUp$, $\NormUp$, and $\BanachUp$ rows.

For the measure branch, use the measure handoff to expose only the representing measure endpoint and the integration ledger over carried test endpoints. Then apply the Banach-Hilbert scope and branch-boundary nonescape theorems: together they keep the active branch public while preventing the inactive branch or any unlisted representation theorem from escaping the packet.
\end{proof}

\begin{theorem}[Riesz representation obligation closure package]
\label{thm:riesz-representation-obligation-closure}
The carrier, obligation surface, Hilbert bridge, dual-functional route, measure handoff, Banach-Hilbert scope, and branch-boundary nonescape of this chapter assemble the full obligationClosure surface for $\RieszRepresentationUp$ over $\BanachUp$, $\HilbertUp$, $\FunctionalAnalysisUp$, $\NormUp$, and $\MeasureUp$. The package closes exactly the displayed finite representation obligations and does not claim a full representation theorem, nonconstructive duality, Hahn--Banach extension, selected representative, or uniqueness of representing data.
\end{theorem}
\leanchecked{BEDC.Derived.RieszRepresentationUp.RieszRepresentationObligationClosure}
\begin{proof}
Start with the carrier of \autoref{def:rieszrepresentation-carrier} and the obligation surface of \autoref{thm:rieszrepresentation-namecert-obligation-surface}. Together they identify the source row, bounded scalar-valued functional row, representing datum row, scalar evaluation ledger, and branch boundary as the only public rows of the packet.

Apply the Hilbert bridge \autoref{thm:rieszrepresentation-hilbert-functional-bridge} and the dual-functional route \autoref{thm:riesz-representation-dual-functional-route}. These results force every Hilbert-facing or bounded-functional read through the displayed $\HilbertUp$, $\FunctionalAnalysisUp$, $\NormUp$, and $\BanachUp$ rows before a representing datum is consumed.

Use the measure handoff \autoref{thm:riesz-representation-measure-handoff} for the measure branch, then combine the Banach-Hilbert scope theorem \autoref{thm:riesz-representation-banach-hilbert-scope} with branch-boundary nonescape \autoref{thm:riesz-representation-branch-boundary-nonescape}. The resulting package is exactly the finite obligation surface over the five named dependencies, while full representation, nonconstructive duality, Hahn--Banach extension, selected representatives, and uniqueness remain outside the carrier.
\end{proof}

\begin{theorem}[Riesz representation scope binding]
\label{thm:riesz-representation-scope-binding}
Every accepted $\RieszRepresentationUp$ packet scopes the bounded-functional route through $\BanachUp$, $\HilbertUp$, $\FunctionalAnalysisUp$, $\NormUp$, and $\MeasureUp$ before either branch is projected. The branch-boundary rows keep Hilbert representing endpoints and measure endpoints separate, so a Hilbert projection cannot export a measure endpoint and a measure projection cannot export a Hilbert representing vector. The scoped packet exposes only the shared bounded-functional face, the active representing row, scalar comparison ledgers, inherited classifier transport, and the branch boundary.
\end{theorem}
\leanchecked{BEDC.Derived.RieszRepresentationUp.RieszRepresentationScopeBinding}
\begin{proof}
Start with the obligation surface of \autoref{thm:rieszrepresentation-namecert-obligation-surface}. It requires the source row, bounded scalar-valued functional row, representing datum row, scalar ledger, and branch boundary to be displayed as the local NameCert surface.

For the Hilbert branch, apply the Hilbert bridge \autoref{thm:rieszrepresentation-hilbert-functional-bridge}. For the shared bounded-functional entry, use the dual-functional route \autoref{thm:riesz-representation-dual-functional-route}. These bind the $\HilbertUp$, $\FunctionalAnalysisUp$, $\NormUp$, and $\BanachUp$ rows before a representing endpoint is consumed.

For the measure branch, apply the measure handoff \autoref{thm:riesz-representation-measure-handoff}. Then combine Banach-Hilbert scope \autoref{thm:riesz-representation-banach-hilbert-scope} with branch-boundary nonescape \autoref{thm:riesz-representation-branch-boundary-nonescape}. The boundary coordinate keeps the Hilbert and measure endpoints disjoint while preserving the shared bounded-functional face, so the accepted packet has exactly the scoped route stated above.
\end{proof}

\begin{theorem}[Riesz representation functional-carrier obligation]
\label{thm:riesz-representation-functional-carrier-obligation}
Every accepted $\RieszRepresentationUp$ packet must display the functional-analysis, Banach, Hilbert, Norm, and Real rows that make the bounded scalar-valued functional readable before any representing datum is projected. The obligation is the finite row
$$
(F,A,H,N,R,B)
$$
where $F$ is the $\FunctionalAnalysisUp$ bounded-functional face, $A$ is the $\BanachUp$ source carrier, $H$ is the optional $\HilbertUp$ branch source, $N$ is the $\NormUp$ bound ledger, $R$ is the scalar $\RealUp$ seal, and $B$ is the branch boundary. The row exports carrier admission and classifier transport only; it does not assert a host representation theorem, a selected representing vector, or equality of host linear maps.
\end{theorem}
\begin{proof}
Read the carrier of \autoref{def:rieszrepresentation-carrier}. Its source and bounded-functional coordinates already require the Banach, Hilbert, FunctionalAnalysis, Norm, and Real faces before a representing datum can be consumed.

Apply the dual-functional route of \autoref{thm:riesz-representation-dual-functional-route}. It keeps the bounded scalar-valued functional row visible as a finite NameCert coordinate rather than as a host-level function equality.

Finally use the branch-boundary nonescape theorem. Since $B$ is part of the displayed row, the functional-carrier obligation cannot select a representing datum or identify host linear maps outside the active branch.
\end{proof}
\leanchecked{BEDC.Derived.RieszRepresentationUp.RieszRepresentationFunctionalCarrierObligation}

\begin{theorem}[Riesz representation measure-row obligation]
\label{thm:riesz-representation-measure-row-obligation}
In the measure branch, every accepted $\RieszRepresentationUp$ packet must display the Measure and Real rows
$$
(M,T,I,R,L,B)
$$
before a measure-facing representation read is exported. Here $M$ is the $\MeasureUp$ representing endpoint, $T$ is the finite test-endpoint schedule, $I$ is the integration ledger over those tests, $R$ is the scalar $\RealUp$ seal, $L$ is the inherited bounded-functional ledger, and $B$ records that the measure branch is active. This obligation exports only finite NameCert rows for the measure handoff; it does not choose a representing measure, prove uniqueness, assert Radon regularity, or close a host measure-representation theorem.
\end{theorem}
\begin{proof}
Start from the measure branch in \autoref{def:rieszrepresentation-carrier}. The representing datum is a carried measure endpoint, and the scalar comparison is an integration ledger over displayed test endpoints.

Use the measure handoff theorem \autoref{thm:riesz-representation-measure-handoff}. It forces the source carrier, scalar target, boundedness ledger, measure endpoint, and test-endpoint integration ledger to be visible before the handoff is consumed.

The branch boundary then blocks the extra conclusions. Because no row carries a selected measure, uniqueness proof, Radon regularity theorem, or host-level representation theorem, the measure obligation remains a finite packet row.
\end{proof}
\leanchecked{BEDC.Derived.RieszRepresentationUp.RieszRepresentationMeasureRowObligation}

\begin{theorem}[Riesz representation Real-seal obligation]
\label{thm:riesz-representation-real-seal-obligation}
Every scalar comparison exported by $\RieszRepresentationUp$ must pass through the terminal $\RealUp$ seal row shared by the Hilbert and measure branches. In the Hilbert branch the sealed comparison is the inner-product value ledger; in the measure branch it is the integration value ledger. The seal exports only scalar value rows and their inherited classifier transport, not Real completeness, selected representatives, equality of host functions, or a full representation theorem.
\end{theorem}
\leanchecked{BEDC.Derived.RieszRepresentationUp.RieszRepresentationRealSealObligation}
\begin{proof}
For the Hilbert branch, apply \autoref{thm:rieszrepresentation-hilbert-functional-bridge}. The comparison row is a scalar ledger between the functional value and an inner-product value, both read through the same Real classifier.

For the measure branch, apply \autoref{thm:riesz-representation-measure-handoff}. The comparison row is an integration value ledger over displayed tests, again sealed by the scalar Real row.

Combine these branches with \autoref{thm:riesz-representation-banach-hilbert-scope}. The common Real seal is the only scalar export coordinate, so ambient Real completeness, selected representatives, host function equality, and full representation theorems remain outside the packet.
\end{proof}

\begin{theorem}[Riesz representation scoped-closure handoff]
\label{thm:riesz-representation-scoped-closure-handoff}
The scoped closure surface for $\RieszRepresentationUp$ is exactly the composition of the carrier of \autoref{def:rieszrepresentation-carrier}, the obligation surface of \autoref{thm:rieszrepresentation-namecert-obligation-surface}, the Hilbert functional bridge of \autoref{thm:rieszrepresentation-hilbert-functional-bridge}, the dual-functional route of \autoref{thm:riesz-representation-dual-functional-route}, the measure handoff of \autoref{thm:riesz-representation-measure-handoff}, the Banach-Hilbert scope of \autoref{thm:riesz-representation-banach-hilbert-scope}, the branch-boundary nonescape theorem of \autoref{thm:riesz-representation-branch-boundary-nonescape}, the functional-carrier obligation of \autoref{thm:riesz-representation-functional-carrier-obligation}, the measure-row obligation of \autoref{thm:riesz-representation-measure-row-obligation}, and the Real-seal obligation of \autoref{thm:riesz-representation-real-seal-obligation}. Every consumer must display the $\BanachUp$, $\HilbertUp$, $\FunctionalAnalysisUp$, $\NormUp$, $\MeasureUp$, and $\RealUp$ rows before projecting either the Hilbert branch or the measure branch.
\end{theorem}
\begin{proof}
Begin with the carrier and NameCert obligation surface. They provide the source row, bounded functional row, representing datum row, scalar comparison ledger, inherited transport, and branch boundary as the only rows available to a consumer.

For the Hilbert branch, apply the Hilbert functional bridge together with the Banach-Hilbert scope. These force the $\BanachUp$, $\HilbertUp$, $\FunctionalAnalysisUp$, and $\NormUp$ rows to remain visible before a representing endpoint is projected.

For the measure branch, apply the dual-functional route, the measure handoff, and the measure-row obligation. These bind the $\FunctionalAnalysisUp$, $\NormUp$, $\BanachUp$, $\MeasureUp$, and $\RealUp$ rows before a measure-facing representation read is exported.

Finally use branch-boundary nonescape, the functional-carrier obligation, and the Real-seal obligation. The branch boundary keeps the Hilbert and measure projections separate, while the Real seal is the only scalar export coordinate. Therefore scoped closure is exactly the displayed composition and not a full representation theorem, selected representative, uniqueness theorem, or host-level equality.
\end{proof}

\closureat{RieszRepresentationUp}{seedStr}
\begin{closurestatus}{\RieszRepresentationUp}
  \constructivestory{A $\RieszRepresentationUp$ packet is generated from an inherited bounded scalar-valued functional row, a source row from the Banach, Hilbert, FunctionalAnalysis, Norm, and Measure certificates, a representing Hilbert endpoint or measure endpoint, scalar comparison ledgers for inner-product or integration values, componentwise classifier transport, and a branch-boundary row.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:rieszrepresentation-carrier}, \autoref{thm:rieszrepresentation-namecert-obligation-surface}, \autoref{thm:rieszrepresentation-hilbert-functional-bridge}, \autoref{thm:riesz-representation-measure-handoff}, \autoref{thm:riesz-representation-banach-hilbert-scope}, and \autoref{thm:riesz-representation-scoped-closure-handoff} bind the carrier, NameCert obligation surface, Hilbert functional bridge, measure handoff, Banach-Hilbert scope, and scoped-closure handoff over the existing Banach, Hilbert, FunctionalAnalysis, Norm, Measure, and Real rows.}
  \formalstatus{\auditCleanV}
  \leantarget{BEDC.Derived.RieszRepresentationUp.RieszRepresentationNameCertObligations}
  \bridgestatus{none}
  \notclaimed{It does not claim full measure-representation, arbitrary nonconstructive duality, spectral representation, uniqueness of representing measures, Lean verification, or a standard bridge.}
  \upgradepath{Public closure requires a public export package that packages the scoped rows for downstream consumers without adding full representation, selected representatives, uniqueness, or bridge checking.}
\end{closurestatus}
