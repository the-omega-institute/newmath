\begin{theorem}[StreamName finite-window reindex determinacy]
\label{thm:streamname-window-reindex-determinacy}
Let $W_0$ and $W_1$ be accepted $\StreamNameUp$ finite-window packets. Suppose their reindex maps are $\hsame$-compatible, their certified endpoint rows are identical, and their displayed $\BHist$, $\Cont$, $\Pkg$, and $\NameCert$ support rows transport componentwise across the same reindex route. Then $W_0$ and $W_1$ determine the same public finite observation section.

The determinacy statement is internal to the displayed StreamName packet. It does not use quotient stream equality, host sequence equality, selected limits, countable choice, or a completion row.
\end{theorem}
\begin{proof}
First read both packets through the transported finite-window endpoint package of \autoref{thm:rat-stream-name-finite-window-transported-selected-full-endpoint-package}. The shared endpoint rows and the $\hsame$-compatible reindex route place both packets on the same finite endpoint surface.

Next apply successor projection determinacy, \autoref{thm:streamname-successor-window-projection-determinacy}. It prevents the reindex route from creating two public successor projections once endpoint rows and support rows have been identified.

Finally use terminal window fusion, \autoref{thm:streamname-real-completion-terminal-window-fusion}. The fused terminal packet has no extra coordinate beyond the finite window, endpoint data, and displayed support rows, so both accepted packets export the same public finite observation section.
\end{proof}

\begin{theorem}[StreamName public reindexing identity law]
\label{thm:streamname-public-reindexing-identity-law}
For every public $\StreamNameUp$ finite-window packet over a
$\mathsf{ProbeBundle}$, the certified identity reindexer preserves the
displayed $\mathsf{RatStreamNameFiniteWindowClassifier}$, $\UnaryHistory$
observation rows, $\mathsf{InBundle}$ membership rows, pointwise rational
classifier rows, $\hsame$ comparisons, $\Cont$ routes, $\Pkg$ provenance, and
local $\NameCert$ rows. The resulting public finite observation section is the
same section read through the identity route.

This identity law is a statement about the public finite-window packet only.
It does not introduce quotient stream equality, host sequence equality,
selected limits, countable choice, a completion row, or a standard bridge row.
\end{theorem}
\begin{proof}
Read the packet through the public finite-window interface and the public
reindexing obligation of
\autoref{def:streamname-public-reindexing-obligation}. On an identity
reindexer, every certified $\UnaryHistory$ observation remains the displayed
observation, and every $\mathsf{InBundle}$ row is read from the same
$\mathsf{ProbeBundle}$.

The pointwise rational classifier, $\hsame$ comparison, $\Cont$ route,
$\Pkg$ provenance, and local $\NameCert$ row therefore reappear as the same
displayed rows. No extra endpoint, quotient representative, or host-index
coordinate is available in the public obligation surface.

Apply \autoref{thm:streamname-window-reindex-determinacy} to the original
packet and to the identity-reindexed packet. The hypotheses are exactly the
shared endpoint rows, the identity $\hsame$ route, and the componentwise
agreement of the displayed support rows, so the exported public finite
observation section is unchanged.
\end{proof}

\begin{theorem}[StreamName public reindexing associative law]
\label{thm:streamname-public-reindexing-associative-law}
Let $r$ and $s$ be composable certified unary reindexers displayed over public
$\StreamNameUp$ finite-window packets. The two-step packet obtained by
reindexing along $r$ and then along $s$ exposes the same public finite-window
section as the single packet obtained by the composite reindexer. The common
section has the same $\mathsf{RatStreamNameFiniteWindowClassifier}$,
$\UnaryHistory$ ledger, $\mathsf{InBundle}$ transport, pointwise rational
classifier rows, $\hsame$ comparisons, composed $\Cont$ route, displayed
$\Pkg$ provenance, and local $\NameCert$ rows.

The law is confined to the finite public surface. It does not claim quotient
stream equality, host sequence equality, selected limits, countable choice,
completion data, or a standard bridge row.
\end{theorem}
\begin{proof}
First apply the public reindexing obligation of
\autoref{def:streamname-public-reindexing-obligation} to the $r$ leg and then
to the $s$ leg. The two applications list only the certified
$\UnaryHistory$ observations, $\mathsf{InBundle}$ membership rows, pointwise
rational classifier rows, $\hsame$ comparisons, $\Cont$ routes, $\Pkg$
provenance, and local naming rows displayed by the finite packets.

Next use the finite-window reindexing composition theorem,
\autoref{thm:streamname-finite-window-reindexing-composition}. It identifies
the two restricted finite $\mathsf{BHist}$ packets with the packet obtained by
restricting once along the composite reindexer, with the classifier, endpoint,
$\hsame$, and finite ledger rows merely repacked over the target
$\mathsf{ProbeBundle}$.

Finally apply \autoref{thm:streamname-window-reindex-determinacy} to the
two-step packet and the composite packet. Their reindex maps are
$\hsame$-compatible, their endpoint rows agree, and their displayed
$\BHist$, $\Cont$, $\Pkg$, and $\NameCert$ rows transport componentwise, so the
exported public finite observation section is the same.
\end{proof}

\begin{theorem}[StreamName reindex terminal coverage]
\label{thm:streamname-reindex-terminal-coverage}
A certified public $\StreamNameUp$ finite-window reindex route preserves exactly
the terminal four-face L10 exit route. Namely, if an accepted reindex route
transports the public finite window together with the displayed $\BHist$,
$\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows, then the transported
exit packet exposes the same $\StreamNameUp$ window, the same
$\DyadicRatCoreUp$ ledger, the same $\RegSeqRatUp$ readback, and the same
$\RealUp$ seal as its terminal Phase Real and Completion route.

Conversely, any terminal four-face route transported by the identity certified
reindexer or by composable certified reindexers reads the same public finite
observation section. The coverage is confined to the finite route and excludes
quotient stream equality, host sequence equality, selected limits, countable
choice, ambient $\RealUp$ completeness, metric completeness, and standard
bridge rows.
\end{theorem}
\begin{proof}
Start with the public finite-window section. The determinacy theorem,
\autoref{thm:streamname-window-reindex-determinacy}, says that matching
endpoint rows, $\hsame$-compatible reindex maps, and componentwise transported
$\BHist$, $\Cont$, $\Pkg$, and $\NameCert$ rows determine one public finite
observation section.

For the identity route, apply
\autoref{thm:streamname-public-reindexing-identity-law}. It keeps the displayed
finite window, unary-history ledger, bundle membership, rational classifier,
$\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows unchanged, so the
terminal packet is read from the same public section.

For composable certified routes, apply
\autoref{thm:streamname-public-reindexing-associative-law}. The two-step and
composite reindexings expose the same finite section, hence they present the
same support rows to the exit boundary.

Finally use \autoref{thm:streamname-open-phase-four-face-terminality} and
\autoref{thm:streamname-open-phase-four-face-exit-readback}. Terminality fixes
the four faces once the StreamName window, DyadicRatCore ledger, RegSeqRat
readback, RealUp seal, and support rows agree, while readback says every
terminal exit read factors through exactly those rows. Thus transported
identity and composable routes cover precisely the finite four-face exit route,
with no additional quotient, host, limit, choice, completeness, or bridge
coordinate.
\end{proof}

\begin{theorem}[StreamName public reindexing section cancellation]
\label{thm:streamname-public-reindexing-section-cancellation}
Let $R_0$ and $R_1$ be certified public $\StreamNameUp$ finite-window reindex
routes displayed over the same $\mathsf{ProbeBundle}$. Suppose they expose the
same public finite observation section, the same endpoint rows, and the same
displayed $\BHist$, and suppose their componentwise $\hsame$, $\Cont$, $\Pkg$,
and local $\NameCert$ support rows agree over that section. Then every terminal
Phase Real and Completion consumer reads the same four-face route through
$\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ from $R_0$
and $R_1$.

Thus the consumer cannot distinguish the two routes by reindexer shape, host
sequence equality, quotient stream equality, selected limits, countable choice,
ambient completeness, metric completeness, or a standard bridge row. The
visible data are exactly the displayed finite section, endpoint rows, and
support rows.
\end{theorem}
\begin{proof}
First apply \autoref{thm:streamname-window-reindex-determinacy} to the two
certified routes. The shared endpoint rows, the common displayed
$\mathsf{ProbeBundle}$, the displayed $\BHist$, and the componentwise
$\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ support rows identify the
public finite observation section read by the two packets.

The identity and composite presentations do not add observable data. Apply
\autoref{thm:streamname-public-reindexing-identity-law} to any identity leg
and \autoref{thm:streamname-public-reindexing-associative-law} to any
composite presentation. Both laws reduce the public presentation back to the
same finite section with the same displayed support rows.

Finally apply \autoref{thm:streamname-reindex-terminal-coverage} and
\autoref{thm:streamname-real-completion-terminal-window-fusion}. Terminal
coverage says every accepted reindex route reaches the same four-face exit
surface once the finite section and support rows agree, while terminal window
fusion carries that agreement through the $\StreamNameUp$ window,
$\DyadicRatCoreUp$ ledger, $\RegSeqRatUp$ readback, and $\RealUp$ seal. Hence
the terminal consumer reads one route, independent of the certified reindexer
shape and of the excluded quotient, host, limit, choice, completeness, or
bridge coordinates.
\end{proof}

\begin{theorem}[StreamName public reindexing cancellation boundary]
\label{thm:streamname-public-reindexing-cancellation-boundary}
Let $R_0$ and $R_1$ be certified public $\StreamNameUp$ finite-window reindex
routes displayed over a finite $\mathsf{ProbeBundle}$, and suppose both feed
the same terminal Phase Real and Completion four-face route. Then any public
distinction between $R_0$ and $R_1$ must appear in one of the displayed finite
observation section rows:
$$
\mathsf{RatStreamNameFiniteWindowClassifier},\quad
\mathsf{ProbeBundle},\quad
\mathsf{InBundle},\quad
\mathsf{UnaryHistory},
$$
the pointwise $\RatUp$ classifier rows, the endpoint $\hsame$ rows, the
displayed $\Cont$ routes, the $\Pkg$ provenance rows, or the local
$\NameCert$ rows.

If all those rows agree, then downstream $\RegSeqRatUp$ and $\RealUp$
consumers receive the same terminal route through
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp.
$$
The boundary contains no quotient stream equality, host sequence equality,
selected limits, countable choice, ambient $\RealUp$ completeness, metric
completeness, or standard bridge row.
\end{theorem}
\begin{proof}
First use \autoref{thm:streamname-window-reindex-determinacy}. The finite
window classifier, bundle membership, unary-history display, endpoint
$\hsame$ data, displayed $\Cont$ routes, $\Pkg$ provenance, and local
$\NameCert$ rows are exactly the public rows from which a certified reindex
packet is read.

Next apply the identity and associativity laws,
\autoref{thm:streamname-public-reindexing-identity-law} and
\autoref{thm:streamname-public-reindexing-associative-law}. Identity legs and
composite presentations cannot create an additional visible distinction
beyond the same finite observation rows.

Now use \autoref{thm:streamname-reindex-terminal-coverage}. Every certified
public reindex route that reaches the terminal Phase Real and Completion
surface is read through the four displayed faces
$\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$, with no
extra quotient, host, selector, choice, completeness, metric, or bridge
coordinate.

Finally apply
\autoref{thm:streamname-public-reindexing-section-cancellation}. Once the
listed finite rows agree, section cancellation identifies the terminal read of
$R_0$ and $R_1$. The terminal four-face readback used by
\autoref{thm:streamname-reindex-finite-cover-terminal-descent} then passes
the same route to downstream $\RegSeqRatUp$ and $\RealUp$ consumers, so any
remaining public distinction would have had to occur in the displayed finite
rows already listed.
\end{proof}

\begin{theorem}[StreamName reindex finite-cover terminal descent]
\label{thm:streamname-reindex-finite-cover-terminal-descent}
Let $\{\mathcal W_i\}_{i\in I}$ be a finite family of certified public
$\StreamNameUp$ finite-window packets, each equipped with a certified public
reindex route to the terminal Phase Real and Completion surface. Suppose that
on every overlap the reindexed packets expose the same public finite
observation section, the same endpoint rows, the same displayed $\BHist$, and
the same componentwise $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$
support rows. Suppose also that the associated local terminal packets satisfy
the common-tail $\RealUp$ boundary agreement hypotheses of
\autoref{thm:streamname-terminal-fusion-finite-cover-cocycle-exactness}.

Then the reindexed family descends to one finite-request section on the
terminal four-face route
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp.
$$
The descended section has only the displayed finite StreamName windows, the
$\DyadicRatCoreUp$ endpoint-radius-distance ledger, the $\RegSeqRatUp$
common-tail classifier and regularity rows, the $\RealUp$ seal-source row, and
the support rows
$$
\BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert.
$$
It does not assert quotient stream equality, host sequence equality, selected
limits, countable choice, ambient $\RealUp$ completeness,
metric-completeness data, or standard bridge rows.
\end{theorem}
\begin{proof}
First compare the overlap reads after reindexing. Apply
\autoref{thm:streamname-public-reindexing-section-cancellation} to each pair
of certified overlap routes. The shared finite observation section, endpoint
rows, displayed $\BHist$, and componentwise $\hsame/\Cont/\Pkg/\NameCert$
support rows identify the terminal read on the four visible faces.

Next keep the identified reads inside the public terminal surface. By
\autoref{thm:streamname-reindex-terminal-coverage}, every accepted reindex
route exposes exactly the $\StreamNameUp$ window, $\DyadicRatCoreUp$ ledger,
$\RegSeqRatUp$ readback, and $\RealUp$ seal, with no additional quotient,
host, selector, choice, completeness, metric, or bridge coordinate.

Now use the assumed common-tail $\RealUp$ boundary agreement. Apply
\autoref{thm:streamname-terminal-fusion-finite-cover-cocycle-exactness} to
read that boundary agreement as the finite-cover cocycle condition for the
local terminal fused packets on the same four-face surface.

Finally apply
\autoref{thm:streamname-terminal-fusion-cocycle-section-descent}. The cocycle
condition descends the local terminal packets to one finite-request section,
and the descent theorem lists exactly the displayed StreamName, DyadicRatCore,
RegSeqRat, Real, and support rows. Thus the reindex data contribute only the
public finite sections already cancelled above.
\end{proof}

\begin{theorem}[StreamName reindex four-face exit naturality]
\label{thm:streamname-reindex-four-face-exit-naturality}
Let $R$ be a support-row-preserving certified public $\StreamNameUp$
finite-window reindex route, displayed over a finite
$\mathsf{ProbeBundle}$, from a local StreamName window to the terminal Phase
Real and Completion exit surface. Suppose the route preserves the public
finite observation section, the endpoint rows, the displayed $\BHist$, and the
componentwise support rows
$$
\hsame,\quad \Cont,\quad \Pkg,\quad \NameCert.
$$
Then transport along $R$ is natural at the terminal exit surface: the source
read and the transported read have the same visible four-face Phase Real and
Completion surface, namely
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp,
$$
and the only support rows visible to every terminal consumer are
$$
\BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert.
$$

No additional coordinate is transported or identified. In particular, the
naturality statement does not assert quotient stream equality, host sequence
equality, selected limits, countable choice, ambient $\RealUp$ completeness,
metric completeness, or any standard bridge row.
\end{theorem}
\begin{proof}
First read $R$ through the public finite observation section. By
\autoref{thm:streamname-window-reindex-determinacy}, preservation of the
finite section, endpoint rows, displayed $\BHist$, and componentwise
$\hsame/\Cont/\Pkg/\NameCert$ support rows fixes the public data seen before
and after the certified reindex route.

Next place that fixed public section on the terminal route. Apply
\autoref{thm:streamname-reindex-terminal-coverage}. The route reaches exactly
the four visible faces $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$,
and $\RealUp$, with the listed support rows and with no quotient, host,
selector, choice, completeness, metric, or bridge coordinate added to the
surface.

The certified route shape itself is immaterial to the terminal consumer. Use
\autoref{thm:streamname-public-reindexing-section-cancellation} to compare
any two presentations of the same preserved section; section cancellation
keeps the terminal read invariant under reindexer shape.

Finally apply
\autoref{thm:streamname-reindex-finite-cover-terminal-descent} to the
singleton finite family determined by $R$. Finite-family descent says the
terminal section remains a section on the same four-face surface with exactly
the same support rows. Hence support-row-preserving certified reindex
transport is natural for the terminal Phase Real and Completion exit surface,
and it carries none of the excluded quotient, host, limit, choice,
completeness, metric, or bridge data.
\end{proof}

\begin{theorem}[StreamName terminal reindex four-face pullback]
\label{thm:streamname-terminal-reindex-four-face-pullback}
Let $R_0$ and $R_1$ be certified public $\StreamNameUp$ finite-window
reindex routes into the terminal Phase Real and Completion exit boundary.
Assume both routes preserve the same public finite observation section, the
same endpoint rows, the same displayed $\BHist$, and the same componentwise
support rows
$$
\hsame,\quad \Cont,\quad \Pkg,\quad \NameCert.
$$
Assume also that the local $\NameCert$ support used by each route is the same
finite support read over the preserved section. Then $R_0$ and $R_1$ determine
the same terminal pullback over the four visible faces
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp.
$$
Conversely, every terminal pullback produced by such a support-row-preserving
reindex route is read from exactly the public finite observation section, the
endpoint rows, displayed $\BHist$, componentwise $\hsame/\Cont/\Pkg$ rows, and
local $\NameCert$ support listed above.

The assertion is only about this four-face terminal pullback. It does not
assert quotient stream equality, host sequence equality, selected limits,
countable choice, ambient $\RealUp$ completeness, metric-completeness data, or
standard bridge rows.
\end{theorem}
\begin{proof}
First apply
\autoref{thm:streamname-reindex-four-face-exit-naturality} separately to
$R_0$ and $R_1$. Each route has the same transported terminal exit surface,
and that surface has exactly the displayed $\StreamNameUp$,
$\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ faces with the listed support
rows.

Next compare the two route presentations at the public section. By
\autoref{thm:streamname-public-reindexing-section-cancellation}, the common
finite observation section, endpoint rows, displayed $\BHist$, and
componentwise $\hsame/\Cont/\Pkg/\NameCert$ support cancel the reindexer
shape. Thus the two routes expose the same terminal read before any
four-face packaging is imposed.

Now keep that read inside the terminal surface. Use
\autoref{thm:streamname-reindex-terminal-coverage} to see that no fifth face
or hidden support coordinate is present: the route contributes precisely the
StreamName, DyadicRatCore, RegSeqRat, Real, and local support rows named in
the statement.

Finally apply
\autoref{thm:streamname-reindex-finite-cover-terminal-descent} to the
singleton finite family determined by either route. Singleton descent
identifies the finite-family terminal section with the same four-face
terminal pullback, so $R_0$ and $R_1$ give one pullback over the shared rows.

For the converse direction, start with any terminal pullback produced by a
support-row-preserving reindex route of the stated kind. The same terminal
coverage theorem lists all rows from which it can be read, and the singleton
descent theorem repacks those rows without adding quotient, host, selector,
choice, completeness, metric, or bridge data. Hence the pullback is read from
exactly the public section, endpoint rows, displayed $\BHist$,
componentwise $\hsame/\Cont/\Pkg$ rows, and local $\NameCert$ support.
\end{proof}

\begin{theorem}[StreamName reindex tail-window terminal equivalence]
\label{thm:streamname-reindex-tail-window-terminal-equivalence}
Let $\mathcal{F}$ be a finite family of accepted current Phase Real and
Completion $\StreamNameUp$ request sections, all displayed over the same
public endpoints and the same accepted $\RegSeqRatUp$ spine. Let
$\mathcal{W}$ be a finite chain of $\StreamNameUp$ tail-window coverage
packets over those endpoints and that spine, and let $R$ be a
support-row-preserving certified public $\StreamNameUp$ reindex route for
the family. Assume that $R$ preserves the public finite observation section,
the endpoint rows, the displayed $\BHist$, and the componentwise support rows
$$
\hsame,\quad \Cont,\quad \Pkg,\quad \NameCert.
$$
Then reindexing along $R$ preserves the equivalence between the
finite-section descent-gluing public reflection surface of $\mathcal{F}$ and
the tail-window terminal read of $\mathcal{W}$. Both sides are identified by
the same four-face terminal route
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp,
$$
with the same carrier rows, the same local $\NameCert$ support, and the same
classifier verdict.

This equivalence is confined to the displayed public terminal surface. It
does not assert quotient stream equality, host sequence equality, selected
limits, countable choice, ambient $\RealUp$ completeness,
metric-completeness data, or standard bridge rows.
\end{theorem}
\begin{proof}
First place the certified reindex route on the public finite observation
section. By
\autoref{thm:streamname-terminal-reindex-four-face-pullback}, the reindexed
route is read through exactly the same finite section, endpoints, displayed
$\BHist$, componentwise $\hsame/\Cont/\Pkg$ rows, and local $\NameCert$
support as the original route.

Next descend the reindexed finite family. Apply
\autoref{thm:streamname-reindex-finite-cover-terminal-descent} to the family
carried by $R$. The descent-gluing surface remains a finite terminal public
reflection surface over the displayed $\StreamNameUp$, $\DyadicRatCoreUp$,
$\RegSeqRatUp$, and $\RealUp$ faces, with no extra quotient, host, selector,
choice, completeness, metric, or bridge coordinate.

Now compare that descended surface with the tail-window terminal read. Use
\autoref{thm:streamname-finite-section-tail-window-terminal-equivalence} for
the finite request sections and tail-window coverage packets over the shared
public endpoints and accepted $\RegSeqRatUp$ spine. This identifies the
descent-gluing public reflection surface with the terminal read of the
tail-window chain, preserving the displayed carrier rows, local $\NameCert$
support, and classifier verdict.

Finally keep the comparison inside the four visible faces. By
\autoref{thm:streamname-reindex-terminal-coverage}, the certified public
reindex route has no access to a fifth coordinate: the only terminal faces
are $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$, and
the excluded quotient, host, limit, choice, completeness, metric, and bridge
data remain outside the assertion. Hence certified reindexing preserves the
tail-window terminal equivalence for the finite descent-gluing surface.
\end{proof}

\begin{theorem}[StreamName public reindex zigzag normal form]
\label{thm:streamname-reindex-zigzag-normal-form}
Let $Z$ be a finite zigzag of support-row-preserving certified public
$\StreamNameUp$ finite-window reindex routes whose visible endpoints are the
same public finite observation sections and whose spine row is the same
accepted $\RegSeqRatUp$ readback. Suppose every leg of $Z$ preserves the
displayed public finite section, endpoint rows, displayed $\BHist$, and the
componentwise support rows
$$
\hsame,\quad \Cont,\quad \Pkg,\quad \NameCert.
$$
Then $Z$ determines one terminal four-face packet over
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp
$$
with exactly those displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and
$\NameCert$ support rows.

Conversely, any two displayed public finite-window sections over the same
public endpoints and accepted $\RegSeqRatUp$ spine that determine the same
terminal four-face pullback are connected by a finite zigzag of
support-row-preserving certified public reindex routes. The assertion is
confined to the displayed public terminal surface. It excludes quotient stream
equality, host sequence equality, selected limits, countable choice, ambient
$\RealUp$ completeness, metric-completeness data, standard bridge rows, and
any aggregate status row.
\end{theorem}
\begin{proof}
Start with the forward direction. Insert certified identity legs where a
zigzag endpoint is repeated and compose adjacent forward-facing legs whenever
their endpoints match. The identity insertion is harmless by
\autoref{thm:streamname-public-reindexing-identity-law}, and the collapse of a
two-leg presentation to its composite is harmless by
\autoref{thm:streamname-public-reindexing-associative-law}.

Whenever two neighboring presentations expose the same public finite section,
use \autoref{thm:streamname-public-reindexing-section-cancellation}. This
removes the reindexer shape from the terminal consumer and leaves only the
displayed finite section, endpoint rows, $\BHist$, $\hsame$, $\Cont$, $\Pkg$,
and local $\NameCert$ support.

The remaining finite family of displayed sections is then descended by
\autoref{thm:streamname-reindex-finite-cover-terminal-descent}. The descent
surface is a finite terminal public reflection surface with no added quotient,
host, selector, choice, completeness, metric, bridge, or aggregate coordinate.

Finally apply
\autoref{thm:streamname-terminal-reindex-four-face-pullback} and
\autoref{thm:streamname-reindex-tail-window-terminal-equivalence}. The first
theorem reads every normalized reindex presentation through the same four
visible faces, and the second identifies the finite-section descent surface
with the tail-window terminal read over the shared accepted $\RegSeqRatUp$
spine. Hence the whole zigzag determines one terminal four-face packet with
the displayed support rows named in the statement.

For the converse direction, begin with two displayed public finite-window
sections that have the same terminal four-face pullback. Read both sections
through \autoref{thm:streamname-terminal-reindex-four-face-pullback}; the
common pullback lists exactly the shared $\StreamNameUp$, $\DyadicRatCoreUp$,
$\RegSeqRatUp$, and $\RealUp$ faces and the displayed support rows.

Use \autoref{thm:streamname-reindex-tail-window-terminal-equivalence} to place
both sections on the same tail-window terminal read, then apply
\autoref{thm:streamname-reindex-finite-cover-terminal-descent} to repack that
read as a finite public family. Identity legs connect each section to its
displayed occurrence in the family, associative composition joins consecutive
legs, and section cancellation identifies parallel displayed sections. These
legs form the required support-row-preserving finite zigzag.
\end{proof}

\begin{theorem}[StreamName reindex support-row necessity]
\label{thm:streamname-reindex-support-row-necessity}
A certified public $\StreamNameUp$ finite-window reindex route feeds the
terminal four-face Phase Real and Completion route if and only if the same
public finite observation section displays the $\StreamNameUp$ window, the
$\DyadicRatCoreUp$ ledger, the $\RegSeqRatUp$ readback, the $\RealUp$ seal,
and the shared support rows
$$
\BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert.
$$
Here $\hsame$ is componentwise, $\Cont$ is displayed on the same preserved
finite section, $\Pkg$ records the route provenance visible to the terminal
consumer, and $\NameCert$ is the local certificate support for that section.

No quotient stream equality, host sequence equality, selected limit, countable
choice datum, ambient $\RealUp$ completeness assumption, metric-completeness
assumption, or standard bridge row can replace any of these displayed support
rows in the terminal route.
\end{theorem}
\begin{proof}
For the forward direction, start with a certified public $\StreamNameUp$
finite-window reindex route feeding the terminal four-face surface. Apply
\autoref{thm:streamname-reindex-terminal-coverage}. The coverage theorem
restricts every such terminal read to the four visible faces
$\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$, together
with the displayed $\BHist$, componentwise $\hsame$, displayed $\Cont$,
$\Pkg$, and local $\NameCert$ rows.

Now compare the certified route with its terminal pullback presentation. Use
\autoref{thm:streamname-terminal-reindex-four-face-pullback}. The pullback
inventory says that the terminal route is read from exactly the public finite
observation section, endpoint rows, displayed $\BHist$, componentwise
$\hsame/\Cont/\Pkg$ rows, and local $\NameCert$ support. Thus feeding the
terminal four-face route forces the displayed window, ledger, readback, seal,
and support rows named in the statement.

For the reverse direction, assume the public finite observation section
already displays the four faces and the listed support rows. Apply
\autoref{thm:streamname-reindex-four-face-exit-naturality} to transport the
section along the certified public reindex route without changing the visible
terminal surface.

Then apply
\autoref{thm:streamname-reindex-zigzag-normal-form}. The normal form theorem
packs any finite support-row-preserving presentation over the same public
endpoints and accepted $\RegSeqRatUp$ spine into one terminal four-face
packet. Hence the displayed support rows are sufficient for the certified
route to feed the terminal Phase Real and Completion route.

Finally rule out substitutions. By
\autoref{thm:streamname-public-reindexing-cancellation-boundary}, cancellation
is confined to the preserved public section and its displayed support rows,
while the terminal pullback inventory just cited lists no quotient stream,
host equality, selected-limit, choice, ambient-completeness,
metric-completeness, or bridge coordinate. None of those external rows can
replace a required support row in the four-face route.
\end{proof}
