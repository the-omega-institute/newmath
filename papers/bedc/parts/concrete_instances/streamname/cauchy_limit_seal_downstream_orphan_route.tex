\begin{theorem}[StreamName CauchyLimitSeal downstream orphan route]
\label{thm:streamname-cauchy-limit-seal-downstream-orphan-route}
Every $\StreamNameUp$ finite-window packet used by a completion-facing seal consumer is consumed through the sibling $\CauchyLimitSealUp$ route of \autoref{ch:concrete-instances-cauchylimitseal-namecert} after the $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, and $\RealUp$ seal rows are displayed. The route carries only the selected StreamName window, regular-rational readback, dyadic ledger, Real seal, displayed $\BHist$ observations, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ rows.

It exports no quotient streams, selected host limits, host real equality, hidden tail witnesses, ambient completeness, or metric-completion rows.
\end{theorem}
\begin{proof}
Start with \autoref{thm:streamname-cauchy-limit-seal-forward-lattice}. It places every StreamName-to-CauchyLimitSeal consumer read on the selected finite window, the RegSeqRat readback, the $\DyadicRatCoreUp$ ledger, the $\RealUp$ seal row, and the displayed transport, route, provenance, and naming rows.

Use reverse source exhaustion, \autoref{thm:streamname-cauchy-limit-seal-reverse-source-exhaustion}. A route entering the StreamName common-refinement packet from the CauchyLimitSeal finite-window seal surface contributes no additional row beyond the selected window, readback, dyadic ledger, Real seal, and local BEDC routing coordinates.

Finally apply the sibling CauchyLimitSeal route, \autoref{thm:cauchy-limit-seal-streamname-real-terminal-handoff} and \autoref{thm:cauchy-limit-seal-lattice-meet-terminal-factorization}. These rows force the completion-facing consumer through the displayed StreamName schedule before the seal is consumed, and they leave no carrier coordinate for quotient streams, selected host limits, host real equality, hidden tail witnesses, ambient completeness, or metric-completion data.
\end{proof}
