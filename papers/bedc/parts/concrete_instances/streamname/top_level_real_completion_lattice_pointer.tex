The top-level $\StreamNameUp$ certificate is a Real-and-completion dependency node: its finite-window rows are consumed by \autoref{ch:concrete-instances-regseqrat-namecert}, its dyadic observation rows are read through \autoref{ch:concrete-instances-dyadicratcore-namecert}, and its seal-facing route is consumed by \autoref{ch:concrete-instances-real-namecert}. This route exports no quotient stream equality, selected host limit, countable-choice selector, or ambient completeness principle.
