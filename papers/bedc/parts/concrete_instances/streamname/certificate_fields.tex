\section{The certificate}
\label{sec:streamname-certificate}

\begin{theorem}[Rational stream-name certificate fields]
\label{thm:rat-stream-name-certificate-fields}
For certified observation maps $s,t,u:\Hist\to\Hist$, the carrier and
classifier above supply the rational stream-name certificate fields:
\begin{enumerate}
  \item $\mathsf{RatStreamNameCarrier}(s)$ implies
  $\mathsf{RatStreamNameClassifier}(s,s)$.
  \item $\mathsf{RatStreamNameClassifier}(s,t)$ implies
  $\mathsf{RatStreamNameClassifier}(t,s)$.
  \item $\mathsf{RatStreamNameClassifier}(s,t)$ and
  $\mathsf{RatStreamNameClassifier}(t,u)$ imply
  $\mathsf{RatStreamNameClassifier}(s,u)$.
  \item $\mathsf{RatStreamNameClassifier}(s,t)$ implies, for every $n$ with
  $\mathsf{UnaryHistory}(n)$, both $\mathsf{RatHistoryCarrier}(s(n))$ and
  $\mathsf{RatHistoryCarrier}(t(n))$.
  \item If $\mathsf{RatStreamNameClassifier}(s,t)$ and streams $s',t'$ satisfy
  $\forall n,\ \mathsf{UnaryHistory}(n)\to\hsame(s(n),s'(n))$ and
  $\forall n,\ \mathsf{UnaryHistory}(n)\to\hsame(t(n),t'(n))$, then
  $\mathsf{RatStreamNameClassifier}(s',t')$.
\end{enumerate}
\end{theorem}
\begin{proof}
For reflexivity, the two endpoint carrier rows of
$\mathsf{RatStreamNameClassifier}(s,s)$ are both the supplied carrier of $s$.
For its pointwise classifier row, fix a certified observation history $n$ with
witness $\nu:\mathsf{UnaryHistory}(n)$. The carrier gives
$\mathsf{RatHistoryCarrier}(s(n))$ from $\nu$, and RatUp classifier
reflexivity turns that pointwise rational witness into
$\mathsf{RatHistoryClassifier}(s(n),s(n))$.

For symmetry, unpack the supplied stream classifier into its carrier rows for
$s$ and $t$ and its pointwise classifier row. The reversed classifier uses the
$t$ carrier as its first carrier row and the $s$ carrier as its second carrier
row. For the pointwise row, fix $(n,\nu)$ and apply RatUp rational-history
classifier symmetry to the projected point classifier at $(n,\nu)$.

For transitivity, unpack $\mathsf{RatStreamNameClassifier}(s,t)$ and
$\mathsf{RatStreamNameClassifier}(t,u)$. The resulting classifier from $s$ to
$u$ uses the first carrier row of the first bundle and the second carrier row
of the second bundle. For the pointwise row, fix $(n,\nu)$ and compose the two
projected point classifiers at $(n,\nu)$ by RatUp rational-history classifier
transitivity.

For endpoint carriers, fix a certified observation history $n$ with witness
$\nu:\mathsf{UnaryHistory}(n)$. Project the two bundled carrier rows of
$\mathsf{RatStreamNameClassifier}(s,t)$ and read them at $(n,\nu)$; these are
exactly the rational-history carrier witnesses required at the two endpoint
observations.

For transport, first transport the two bundled carrier rows along the supplied
history-sameness witnesses, producing $\mathsf{RatStreamNameCarrier}(s')$ and
$\mathsf{RatStreamNameCarrier}(t')$. For the pointwise row, fix $(n,\nu)$ and
apply
$\mathsf{BEDC.Derived.RatUp.RatHistoryClassifier\_hsame\_transport}$ to the
projected point classifier at $(n,\nu)$ and to the two supplied endpoint
history-sameness witnesses at $(n,\nu)$. The transported carrier rows and the
transported pointwise classifier row repack as
$\mathsf{RatStreamNameClassifier}(s',t')$.
\end{proof}

\leanchecked{BEDC.Derived.StreamNameUp.RatStreamName\_certificate\_fields}

\begin{theorem}[Rational stream-name observation one-tail pair readback]
\label{thm:rat-stream-name-classifier-observation-e1-pair-readback}
For certified observation maps $s,t:\Hist\to\Hist$, if
$\mathsf{RatStreamNameClassifier}(s,t)$ and $n$ is a unary observation, then
displayed one-extension endpoints
$$
\hsame(s(n),\Eone(a))\quad\text{and}\quad \hsame(t(n),\Eone(b))
$$
determine unary tails with the internal comparison
$$
\mathsf{UnaryHistory}(a)\land\mathsf{UnaryHistory}(b)\land\hsame(a,b).
$$
\end{theorem}
\begin{proof}
Read the stream-name classifier at the certified observation $n$. Transport the
resulting RatUp point classifier to the displayed one-extension endpoints, then
apply rational one-tail classifier exactness.
\end{proof}
\leanchecked{BEDC.Derived.StreamNameUp.RatStreamNameClassifier\_observation\_e1\_pair\_readback}

\begin{theorem}[Rational stream-name observation shape exclusions]
\label{thm:rat-stream-name-classifier-observation-shape-exclusions}
For certified observation maps $s,t:\Hist\to\Hist$ and an observation history
$n$, suppose
$$
\mathsf{RatStreamNameClassifier}(s,t)\quad\text{and}\quad
\mathsf{UnaryHistory}(n).
$$
Then the selected observations satisfy the positive-denominator and unary
endpoint package
$$
\mathsf{PositiveUnaryDenominator}(s(n))\land
\mathsf{PositiveUnaryDenominator}(t(n))\land
\mathsf{UnaryHistory}(s(n))\land
\mathsf{UnaryHistory}(t(n)).
$$
They also satisfy the two empty-history exclusions
$$
(\hsame(s(n),\emp)\to\mathsf{False})\land
(\hsame(t(n),\emp)\to\mathsf{False}),
$$
and for all histories $z_s,z_t:\Hist$ the two zero-headed exclusions
$$
(\hsame(s(n),\Ezero(z_s))\to\mathsf{False})\land
(\hsame(t(n),\Ezero(z_t))\to\mathsf{False}).
$$
\end{theorem}
\leanchecked{BEDC.Derived.StreamNameUp.RatStreamNameClassifier\_observation\_shape\_exclusions}
\begin{proof}
Read the stream-name classifier at the certified observation $(n,\nu)$, where
$\nu:\mathsf{UnaryHistory}(n)$. Apply
\autoref{thm:rat-history-classifier-positive-denominators} to this point
classifier. This gives positive unary denominator witnesses for $s(n)$ and
$t(n)$.

Apply \autoref{thm:positive-unary-denominator-unary-and-nonempty} separately
to the two positive denominator witnesses. The unary components give
$\mathsf{UnaryHistory}(s(n))$ and $\mathsf{UnaryHistory}(t(n))$, and the
nonempty components give the two exclusions against $\emp$.

For a displayed zero-headed shape $\hsame(s(n),\Ezero(z_s))$, transport the
positive denominator witness for $s(n)$ across that sameness by
\autoref{thm:rat-positive-unary-denominator-hsame-transport}. The transported
witness contradicts \autoref{thm:rat-positive-denominator-e0-absurd}. The
argument for $\hsame(t(n),\Ezero(z_t))$ is identical, using the positive
denominator witness for $t(n)$.
\end{proof}

\begin{theorem}[Rational stream-name observation full endpoint package]
\label{thm:rat-stream-name-observation-full-endpoint-package}
For certified observation maps $s,t:\Hist\to\Hist$, an observation history $n$,
and histories $a,b:\Hist$, suppose
$$
\mathsf{RatStreamNameClassifier}(s,t)\quad\text{and}\quad
\mathsf{UnaryHistory}(n),
$$
and suppose the selected observations are displayed as one-extension endpoints
$$
\hsame(s(n),\Eone(a))\quad\text{and}\quad
\hsame(t(n),\Eone(b)).
$$
Then the following selected-observation endpoint package holds:
\begin{enumerate}
  \item $\mathsf{RatHistoryClassifier}(s(n),t(n))$.
  \item $\mathsf{PositiveUnaryDenominator}(s(n))\land
  \mathsf{PositiveUnaryDenominator}(t(n))$.
  \item $\mathsf{UnaryHistory}(s(n))\land\mathsf{UnaryHistory}(t(n))$.
  \item $(\hsame(s(n),\emp)\to\mathsf{False})\land
  (\hsame(t(n),\emp)\to\mathsf{False})$.
  \item For all histories $z_s,z_t:\Hist$,
  $(\hsame(s(n),\Ezero(z_s))\to\mathsf{False})\land
  (\hsame(t(n),\Ezero(z_t))\to\mathsf{False})$.
  \item $\mathsf{UnaryHistory}(a)\land\mathsf{UnaryHistory}(b)\land
  \hsame(a,b)$.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.StreamNameUp.RatStreamNameClassifier\_observation\_full\_endpoint\_package}
\begin{proof}
Unpack \autoref{def:rat-stream-name-classifier} at the certified observation
$(n,\nu)$, where $\nu:\mathsf{UnaryHistory}(n)$. The pointwise row gives
$\mathsf{RatHistoryClassifier}(s(n),t(n))$.

Apply \autoref{thm:rat-stream-name-classifier-observation-shape-exclusions} to
the same stream classifier and unary observation. This supplies the two positive
unary denominator rows, the unary-history rows for the selected observations,
the empty-history exclusions, and the zero-headed exclusions for arbitrary
$z_s,z_t$.

Apply \autoref{thm:rat-stream-name-classifier-observation-e1-pair-readback} to
the stream classifier, the unary observation, and the two displayed
one-extension endpoint rows. This gives
$\mathsf{UnaryHistory}(a)\land\mathsf{UnaryHistory}(b)\land\hsame(a,b)$.

The point classifier, the shape rows, and the one-tail readback rows are exactly
the listed endpoint package.
\end{proof}

Fit. Traditional target: selected rational-stream observation endpoint package.
BEDC source: $\RatUp+\StreamNameUp$ over $\mathsf{BHist}$. Carrier:
$\mathsf{RatStreamNameCarrier}$. Classifier:
$\mathsf{RatStreamNameClassifier}$. Stability: pointwise $\hsame$ endpoint
display at the selected unary observation. Ledger: the selected observation, the
two one-extension displays, and arbitrary zero-headed probes. Core theorem:
\autoref{thm:rat-stream-name-observation-full-endpoint-package}. Exactness:
rational point classifier, endpoint shape exclusions, and one-tail equality
readback. Standard bridge: $\RealUp$ contextual endpoint certificates consume
this selected-observation package. Status: $\mathsf{ProofTarget}$.

\begin{theorem}[Rational stream-name transported observation shape exclusions]
\label{thm:rat-stream-name-classifier-transported-observation-shape-exclusions}
For certified observation maps $s,t,s',t':\Hist\to\Hist$ and an observation
history $n$, suppose
$$
\mathsf{RatStreamNameClassifier}(s,t)\quad\text{and}\quad
\mathsf{UnaryHistory}(n),
$$
and suppose the selected endpoints are related pointwise by transport rows
$$
\forall k,\ \mathsf{UnaryHistory}(k)\to\hsame(s(k),s'(k)),
\quad
\forall k,\ \mathsf{UnaryHistory}(k)\to\hsame(t(k),t'(k)).
$$
Then the transported selected observations satisfy the positive-denominator and
unary endpoint package
$$
\mathsf{PositiveUnaryDenominator}(s'(n))\land
\mathsf{PositiveUnaryDenominator}(t'(n))\land
\mathsf{UnaryHistory}(s'(n))\land
\mathsf{UnaryHistory}(t'(n)).
$$
They also satisfy the two empty-history exclusions
$$
(\hsame(s'(n),\emp)\to\mathsf{False})\land
(\hsame(t'(n),\emp)\to\mathsf{False}),
$$
and for all histories $z_s,z_t:\Hist$ the two zero-headed exclusions
$$
(\hsame(s'(n),\Ezero(z_s))\to\mathsf{False})\land
(\hsame(t'(n),\Ezero(z_t))\to\mathsf{False}).
$$
\end{theorem}
\leanchecked{BEDC.Derived.StreamNameUp.RatStreamNameClassifier\_transported\_observation\_shape\_exclusions}
\begin{proof}
Read the stream-name classifier at the certified observation $(n,\nu)$, where
$\nu:\mathsf{UnaryHistory}(n)$. This gives the point classifier
$\mathsf{RatHistoryClassifier}(s(n),t(n))$.

Apply \autoref{thm:rat-history-classifier-hsame-transport} to this classifier
and to the two pointwise transport rows evaluated at $n$. The result is the
transported selected classifier
$\mathsf{RatHistoryClassifier}(s'(n),t'(n))$.

Apply \autoref{thm:rat-history-classifier-positive-denominators} to the
transported selected classifier. Then apply
\autoref{thm:positive-unary-denominator-unary-and-nonempty} separately to the
two positive-denominator witnesses. These rows give the displayed positive
denominators, unary endpoint witnesses, and exclusions against $\emp$.

It remains to exclude zero-headed displays. If $\hsame(s'(n),\Ezero(z_s))$
holds, transport $\mathsf{PositiveUnaryDenominator}(s'(n))$ along that sameness
by \autoref{thm:rat-positive-unary-denominator-hsame-transport}, then contradict
\autoref{thm:rat-positive-denominator-e0-absurd}. The same argument applied to
$t'(n)$ and $z_t$ gives the second zero-headed exclusion.
\end{proof}

Fit. Traditional target: transported selected rational-stream observation.
BEDC source: $\RatUp+\StreamNameUp$ over $\mathsf{BHist}$. Carrier:
$\mathsf{RatStreamNameCarrier}$. Classifier:
$\mathsf{RatStreamNameClassifier}$. Stability: pointwise $\hsame$ transport.
Ledger: the selected observation and two transport rows. Core theorem:
\autoref{thm:rat-stream-name-classifier-transported-observation-shape-exclusions}.
Exactness: the displayed shape package. Standard bridge: pointwise rational
stream equality under observation transport. Status: $\mathsf{ProofTarget}$.

\begin{theorem}[Rational stream-name transported observation full endpoint package]
\label{thm:rat-stream-name-transported-observation-full-endpoint-package}
For certified observation maps $s,t,s',t':\Hist\to\Hist$, an observation
history $n$, and histories $a,b,z_s,z_t:\Hist$, suppose
$$
\mathsf{RatStreamNameClassifier}(s,t)\quad\text{and}\quad
\mathsf{UnaryHistory}(n),
$$
suppose the selected endpoints are related pointwise by transport rows
$$
\forall k,\ \mathsf{UnaryHistory}(k)\to\hsame(s(k),s'(k)),
\quad
\forall k,\ \mathsf{UnaryHistory}(k)\to\hsame(t(k),t'(k)),
$$
and suppose the transported selected observations are displayed as
one-extension endpoints
$$
\hsame(s'(n),\Eone(a))\quad\text{and}\quad \hsame(t'(n),\Eone(b)).
$$
Then the transported selected-observation endpoint package holds:
\begin{enumerate}
  \item $\mathsf{RatHistoryClassifier}(s'(n),t'(n))$.
  \item $\mathsf{PositiveUnaryDenominator}(s'(n))\land
  \mathsf{PositiveUnaryDenominator}(t'(n))$.
  \item $\mathsf{UnaryHistory}(s'(n))\land\mathsf{UnaryHistory}(t'(n))$.
  \item $(\hsame(s'(n),\emp)\to\mathsf{False})\land
  (\hsame(t'(n),\emp)\to\mathsf{False})$.
  \item $(\hsame(s'(n),\Ezero(z_s))\to\mathsf{False})\land
  (\hsame(t'(n),\Ezero(z_t))\to\mathsf{False})$.
  \item $\mathsf{UnaryHistory}(a)\land\mathsf{UnaryHistory}(b)\land
  \hsame(a,b)$.
\end{enumerate}
\end{theorem}
\begin{proof}
Unpack \autoref{def:rat-stream-name-classifier} at the certified observation
$(n,\nu)$, where $\nu:\mathsf{UnaryHistory}(n)$. The pointwise row gives the
source point classifier $\mathsf{RatHistoryClassifier}(s(n),t(n))$.

Apply \autoref{thm:rat-history-classifier-hsame-transport} to this source
classifier and to the two pointwise transport rows evaluated at $n$. This gives
the transported point classifier
$\mathsf{RatHistoryClassifier}(s'(n),t'(n))$.

Apply
\autoref{thm:rat-stream-name-classifier-transported-observation-shape-exclusions}
to the same stream classifier, unary observation, and transport rows. It
supplies the transported positive-denominator rows, unary endpoint rows, empty
exclusions, and universal zero-headed exclusions. Instantiate the last two rows
at the displayed histories $z_s$ and $z_t$.

For the one-tail readback, transport
$\mathsf{RatHistoryClassifier}(s'(n),t'(n))$ along the two displayed
one-extension endpoint comparisons. The transported classifier is
$\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$.

Apply \autoref{thm:rat-history-classifier-e1-tail-unary-iff} in the forward
direction to read back $\mathsf{UnaryHistory}(a)$,
$\mathsf{UnaryHistory}(b)$, and $\hsame(a,b)$. The transported point
classifier, transported shape rows, and one-tail readback rows form exactly the
listed endpoint package.
\end{proof}

\begin{theorem}[Rational stream-name reindexing and map stability]
\label{thm:rat-stream-name-reindexing-and-map-stability}
For certified observation maps $s,t,r:\Hist\to\Hist$, suppose $r$ preserves
certified unary observations:
$$
\forall n,\ \mathsf{UnaryHistory}(n)\to\mathsf{UnaryHistory}(r(n)).
$$
Then $\mathsf{RatStreamNameCarrier}(s)$ implies
$\mathsf{RatStreamNameCarrier}(s\circ r)$, and
$\mathsf{RatStreamNameClassifier}(s,t)$ implies
$\mathsf{RatStreamNameClassifier}(s\circ r,t\circ r)$.

For a history map $F:\Hist\to\Hist$, suppose $F$ preserves RatUp carriers and
classifiers:
$$
\forall h,\ \mathsf{RatHistoryCarrier}(h)\to
\mathsf{RatHistoryCarrier}(F(h))
$$
and
$$
\forall h,k,\ \mathsf{RatHistoryClassifier}(h,k)\to
\mathsf{RatHistoryClassifier}(F(h),F(k)).
$$
Then $\mathsf{RatStreamNameCarrier}(s)$ implies
$\mathsf{RatStreamNameCarrier}(F\circ s)$, and
$\mathsf{RatStreamNameClassifier}(s,t)$ implies
$\mathsf{RatStreamNameClassifier}(F\circ s,F\circ t)$.
\end{theorem}
\begin{proof}
For reindexing, unfold $\mathsf{RatStreamNameCarrier}$. Fix an observation
history $n$ with witness $\nu:\mathsf{UnaryHistory}(n)$. The reindexer
hypothesis turns $\nu$ into a certified unary observation witness for $r(n)$,
and the carrier of $s$ gives the required rational-history carrier at
$s(r(n))$.

For the reindexed classifier, unfold
$\mathsf{RatStreamNameClassifier}$ and fix the same certified observation
data $(n,\nu)$. The reindexer hypothesis again supplies
$\mathsf{UnaryHistory}(r(n))$, so the classifier of $s$ and $t$ can be read at
$r(n)$, giving the point classifier between $s(r(n))$ and $t(r(n))$.

For mapped streams, unfold $\mathsf{RatStreamNameCarrier}$ and fix
$n$ with witness $\nu:\mathsf{UnaryHistory}(n)$. The carrier of $s$ gives
$\mathsf{RatHistoryCarrier}(s(n))$, and the carrier-preservation hypothesis
for $F$ transports it to $\mathsf{RatHistoryCarrier}(F(s(n)))$.

For mapped classifiers, unfold $\mathsf{RatStreamNameClassifier}$ and fix
$n$ with witness $\nu:\mathsf{UnaryHistory}(n)$. The classifier of $s$ and
$t$ gives $\mathsf{RatHistoryClassifier}(s(n),t(n))$, and the
classifier-preservation hypothesis for $F$ transports it to
$\mathsf{RatHistoryClassifier}(F(s(n)),F(t(n)))$.
\end{proof}
\leanchecked{BEDC.Derived.StreamNameUp.RatStreamName\_reindexing\_and\_map\_stability}

\begin{theorem}[Rational stream-name reindexing composition law]
\label{thm:rat-stream-name-reindexing-composition-law}
For certified observation maps $s,t,r,q:\Hist\to\Hist$, suppose $r$ and $q$
preserve certified unary observations:
$$
\forall n,\ \mathsf{UnaryHistory}(n)\to\mathsf{UnaryHistory}(r(n))
$$
and
$$
\forall n,\ \mathsf{UnaryHistory}(n)\to\mathsf{UnaryHistory}(q(n)).
$$
Then the composite observation map $n\mapsto q(r(n))$ preserves certified
unary observations. Consequently, $\mathsf{RatStreamNameCarrier}(s)$ implies
$\mathsf{RatStreamNameCarrier}(s\circ q\circ r)$, and
$\mathsf{RatStreamNameClassifier}(s,t)$ implies
$\mathsf{RatStreamNameClassifier}(s\circ q\circ r,t\circ q\circ r)$.
\end{theorem}
\begin{proof}
Fix a certified observation history $n$ with witness
$\nu:\mathsf{UnaryHistory}(n)$. The preservation witness for $r$ sends $\nu$
to a certified unary observation witness for $r(n)$, and the preservation
witness for $q$ sends that result to a certified unary observation witness for
$q(r(n))$. This proves unary preservation for the composite observation map.

For the carrier consequence, unfold $\mathsf{RatStreamNameCarrier}$ and fix
the same certified observation data $(n,\nu)$. The composite unary witness just
constructed lets the carrier of $s$ be read at $q(r(n))$, giving the required
rational-history carrier at $(s\circ q\circ r)(n)$.

For the classifier consequence, unfold $\mathsf{RatStreamNameClassifier}$ and
fix $(n,\nu)$. The composite unary witness again lets the classifier of $s$
and $t$ be read at $q(r(n))$, yielding the point classifier between
$(s\circ q\circ r)(n)$ and $(t\circ q\circ r)(n)$.
\end{proof}
\leanchecked{BEDC.Derived.StreamNameUp.RatStreamName\_reindexing\_composition\_law}

\begin{definition}[Rational stream-name finite probe window]
\label{def:rat-stream-name-finite-window-classifier}
For certified observation maps $s,t:\Hist\to\Hist$ and a probe bundle
$\mathcal{P}:\mathsf{ProbeBundle}$, define
$\mathsf{RatStreamNameFiniteWindowClassifier}(s,t,\mathcal{P})$ to mean that
every bundled certified unary observation is classified pointwise:
$$
\forall n:\Hist,
\mathsf{InBundle}(n,\mathcal{P})\to
\mathsf{UnaryHistory}(n)\to
\mathsf{RatHistoryClassifier}(s(n),t(n)).
$$
\end{definition}

\begin{theorem}[Rational stream-name classifier covers finite probe windows]
\label{thm:rat-stream-name-finite-window-from-classifier}
For certified observation maps $s,t:\Hist\to\Hist$ and every probe bundle
$\mathcal{P}:\mathsf{ProbeBundle}$,
$$
\mathsf{RatStreamNameClassifier}(s,t)
\Longrightarrow
\mathsf{RatStreamNameFiniteWindowClassifier}(s,t,\mathcal{P}).
$$
\end{theorem}
\begin{proof}
Unfold Definition~\ref{def:rat-stream-name-classifier} and fix a history $n$
with evidence $\mathsf{InBundle}(n,\mathcal{P})$ and
$\mathsf{UnaryHistory}(n)$. The stream-name classifier is pointwise on every
certified unary observation, so its row at $n$ gives
$\mathsf{RatHistoryClassifier}(s(n),t(n))$.

Since the bundled history and unary witness were arbitrary, these pointwise
RatUp classifier rows repack exactly as
$\mathsf{RatStreamNameFiniteWindowClassifier}(s,t,\mathcal{P})$.
\end{proof}

\begin{theorem}[Rational stream-name finite windows exhaust the classifier]
\label{thm:rat-stream-name-finite-window-exhausts-classifier}
For certified observation maps $s,t:\Hist\to\Hist$,
$$
\mathsf{RatStreamNameClassifier}(s,t)
\Longleftrightarrow
\forall n:\Hist,\ \mathsf{UnaryHistory}(n)\to
\mathsf{RatStreamNameFiniteWindowClassifier}
(s,t,\Bcons(n,\Bnil)).
$$
\end{theorem}
\leanchecked{BEDC.Derived.StreamNameUp.RatStreamNameFiniteWindowClassifier\_exhausts\_classifier}
\begin{proof}
For the forward implication, fix a certified unary observation $n$. Apply
\autoref{thm:rat-stream-name-finite-window-from-classifier} to the full
stream-name classifier and to the singleton bundle $\Bcons(n,\Bnil)$. This
gives the required finite-window classifier at that observation.

For the reverse implication, fix a certified unary observation $n$. The
singleton-window hypothesis at $n$, together with the head membership of
$n$ in $\Bcons(n,\Bnil)$ from
\autoref{def:bundle-generation}, gives
$\mathsf{RatHistoryClassifier}(s(n),t(n))$.

The carrier fields of this RatUp classifier provide
$\mathsf{RatHistoryCarrier}(s(n))$ and
$\mathsf{RatHistoryCarrier}(t(n))$. Since $n$ was arbitrary, these fields
assemble the two stream-name carriers, and the same pointwise RatUp classifier
rows assemble $\mathsf{RatStreamNameClassifier}(s,t)$.
\end{proof}

The ten-field fit is: traditional target finite-window exhaustion; BEDC source
$\RatUp+\StreamNameUp+\mathsf{ProbeBundle}$; carrier the existing stream-name
carrier rows for $s$ and $t$; classifier the existing pointwise stream-name
classifier row; stability singleton-window coverage over certified unary
observations; ledger the singleton observation bundle $\Bcons(n,\Bnil)$;
core theorem \autoref{thm:rat-stream-name-finite-window-from-classifier};
exactness singleton membership in the probe bundle; downstream
$\RealUp/\RegSeqRat$ finite-prefix consumption; status
$\mathsf{ProofTarget}$.

\begin{theorem}[Rational stream-name finite-window coverage after certified reindexing and point maps]
\label{thm:rat-stream-name-finite-window-reindexed-map-coverage}
For certified observation maps $s,t,r:\Hist\to\Hist$, a history point map
$F:\Hist\to\Hist$, and a probe bundle
$\mathcal{P}:\mathsf{ProbeBundle}$, suppose $r$ preserves certified unary
observations:
$$
\forall n,\ \mathsf{UnaryHistory}(n)\to\mathsf{UnaryHistory}(r(n)).
$$
Suppose also that $F$ preserves RatUp carriers and classifiers:
$$
\forall h,\ \mathsf{RatHistoryCarrier}(h)\to
\mathsf{RatHistoryCarrier}(F(h))
$$
and
$$
\forall h,k,\ \mathsf{RatHistoryClassifier}(h,k)\to
\mathsf{RatHistoryClassifier}(F(h),F(k)).
$$
If $\mathsf{RatStreamNameClassifier}(s,t)$, then
$$
\mathsf{RatStreamNameFiniteWindowClassifier}
(F\circ s\circ r,F\circ t\circ r,\mathcal{P}).
$$
\end{theorem}
\begin{proof}
First apply
\autoref{thm:rat-stream-name-reindexing-and-map-stability} to the observation
reindexer $r$. The reindexing row sends the given stream-name classifier for
$s$ and $t$ to a classifier for $s\circ r$ and $t\circ r$.

Apply the mapped-classifier row of the same theorem to the point map $F$ and
to the reindexed classifier just obtained. The carrier-preservation hypothesis
is part of that stability package, while the classifier-preservation hypothesis
transports every RatUp point classifier through $F$. This yields
$\mathsf{RatStreamNameClassifier}(F\circ s\circ r,F\circ t\circ r)$.

Finally apply
\autoref{thm:rat-stream-name-finite-window-from-classifier} to this transported
stream-name classifier and the selected probe bundle $\mathcal{P}$. The result
is precisely the displayed finite-window classifier for the reindexed and
point-mapped observation streams.
\end{proof}

The ten-field fit is: traditional target finite certified observation windows;
BEDC source $\RatUp+\StreamNameUp+\mathsf{ProbeBundle}$; carrier observation
maps; classifier finite probe-window restriction of the stream classifier;
stability pointwise classifier projection; ledger the bundle and selected
observation; core theorem
\autoref{thm:rat-stream-name-finite-window-from-classifier} and
\autoref{thm:rat-stream-name-finite-window-reindexed-map-coverage}; exactness
pointwise RatUp classification on bundled unary observations; standard bridge
finite prefixes of rational streams; status $\mathsf{ProofTarget}$.

\begin{theorem}[Rational stream-name finite-window structural laws]
\label{thm:rat-stream-name-finite-window-structural-laws}
For certified observation maps $s,t:\Hist\to\Hist$ and probe bundles
$\mathcal{P},\mathcal{Q}:\mathsf{ProbeBundle}$, the finite-window classifier
has the following structural laws.
\begin{enumerate}
\item If every bundled observation of $\mathcal{Q}$ is also bundled in
$\mathcal{P}$, then
$\mathsf{RatStreamNameFiniteWindowClassifier}(s,t,\mathcal{P})$ implies
$\mathsf{RatStreamNameFiniteWindowClassifier}(s,t,\mathcal{Q})$.
\item
$\mathsf{RatStreamNameFiniteWindowClassifier}
(s,t,\mathsf{bundleAppend}(\mathcal{P},\mathcal{Q}))$ implies both
$\mathsf{RatStreamNameFiniteWindowClassifier}(s,t,\mathcal{P})$ and
$\mathsf{RatStreamNameFiniteWindowClassifier}(s,t,\mathcal{Q})$.
\item If the finite-window classifier holds on $\mathcal{P}$ and on
$\mathcal{Q}$, then it holds on
$\mathsf{bundleAppend}(\mathcal{P},\mathcal{Q})$.
\end{enumerate}
\end{theorem}
\begin{proof}
Unfold Definition~\ref{def:rat-stream-name-finite-window-classifier}. For the
subbundle law, fix an arbitrary history $n$ with
$\mathsf{InBundle}(n,\mathcal{Q})$ and a unary witness
$\mathsf{UnaryHistory}(n)$. The assumed subbundle map transports the bundle
membership from $\mathcal{Q}$ to $\mathcal{P}$, and the classifier on
$\mathcal{P}$ gives $\mathsf{RatHistoryClassifier}(s(n),t(n))$.

For the two projections from an appended bundle, fix $n$ first with
$\mathsf{InBundle}(n,\mathcal{P})$ and then with
$\mathsf{InBundle}(n,\mathcal{Q})$. In each case the append membership lemma
for $\mathsf{bundleAppend}$ injects the selected membership into
$\mathsf{InBundle}(n,\mathsf{bundleAppend}(\mathcal{P},\mathcal{Q}))$. The
classifier on the appended bundle then supplies the corresponding point
RatUp classifier row.

For recomposition, fix $n$ with membership in
$\mathsf{bundleAppend}(\mathcal{P},\mathcal{Q})$ and a unary witness. The
kernel equivalence for appended bundles rewrites this membership as a
left-or-right membership alternative. In the left case apply the classifier on
$\mathcal{P}$; in the right case apply the classifier on $\mathcal{Q}$. Both
cases yield the same required conclusion
$\mathsf{RatHistoryClassifier}(s(n),t(n))$.
\end{proof}
\leanchecked{BEDC.Derived.StreamNameUp.RatStreamNameFiniteWindowClassifier\_structural\_laws}

\begin{theorem}[Rational stream-name finite-window selected zero-head exclusion]
\label{thm:rat-stream-name-finite-window-selected-zero-head-exclusion}
For certified observation maps $s,t:\Hist\to\Hist$, a probe bundle
$\mathcal{P}:\mathsf{ProbeBundle}$, an observation history $n$, and histories
$z_s,z_t:\Hist$, suppose
$$
\mathsf{RatStreamNameFiniteWindowClassifier}(s,t,\mathcal{P}),
\quad
\mathsf{InBundle}(n,\mathcal{P}),
\quad
\mathsf{UnaryHistory}(n).
$$
Then the selected finite-window endpoints cannot be zero-headed:
$$
(\hsame(s(n),\Ezero(z_s))\to\mathsf{False})\land
(\hsame(t(n),\Ezero(z_t))\to\mathsf{False}).
$$
\end{theorem}
\begin{proof}
Unfold Definition~\ref{def:rat-stream-name-finite-window-classifier} at the
selected observation $n$, its bundle membership, and its unary witness. This
produces the selected RatUp point classifier
$\mathsf{RatHistoryClassifier}(s(n),t(n))$.

Apply \autoref{thm:rat-history-classifier-positive-denominators} to this
selected classifier. The result supplies positive-denominator witnesses for
both selected endpoint histories $s(n)$ and $t(n)$.

For the left endpoint, assume a displayed zero-headed comparison
$\hsame(s(n),\Ezero(z_s))$. Transport the positive-denominator witness for
$s(n)$ along that comparison by
\autoref{thm:rat-positive-unary-denominator-hsame-transport}. The transported
witness contradicts \autoref{thm:rat-positive-denominator-e0-absurd}.

For the right endpoint, the same argument applied to
$\hsame(t(n),\Ezero(z_t))$ transports the positive-denominator witness for
$t(n)$ to a zero-headed denominator witness, again contradicting
\autoref{thm:rat-positive-denominator-e0-absurd}. These two contradictions
give the displayed conjunction.
\end{proof}
\leanchecked{BEDC.Derived.StreamNameUp.RatStreamNameFiniteWindowClassifier\_selected\_zero\_head\_exclusion}

The compact ten-field fit is: traditional target finite rational observation
endpoint sanity; BEDC source $\RatUp+\StreamNameUp+\mathsf{ProbeBundle}$;
carrier observation maps; classifier finite-window restriction; ledger
selected probe observation; exactness selected RatUp point classification;
downstream $\RealUp/\RegSeqRat$ selected endpoint normalization; standard
bridge ordinary finite rational stream endpoints; status
$\mathsf{ProofTarget}$.

\begin{theorem}[Rational stream-name finite-window selected full endpoint package]
\label{thm:rat-stream-name-finite-window-selected-full-endpoint-package}
For certified observation maps $s,t:\Hist\to\Hist$, a probe bundle
$\mathcal{P}:\mathsf{ProbeBundle}$, an observation history $n$, and histories
$a,b,z_s,z_t:\Hist$, suppose
$$
\mathsf{RatStreamNameFiniteWindowClassifier}(s,t,\mathcal{P}),
\quad
\mathsf{InBundle}(n,\mathcal{P}),
\quad
\mathsf{UnaryHistory}(n),
$$
and suppose the selected observations are displayed as one-extension endpoints
$$
\hsame(s(n),\Eone(a))\quad\text{and}\quad \hsame(t(n),\Eone(b)).
$$
Then the selected finite-window endpoints carry the full endpoint package:
\begin{enumerate}
  \item $\mathsf{RatHistoryClassifier}(s(n),t(n))$.
  \item $\mathsf{PositiveUnaryDenominator}(s(n))\land
  \mathsf{PositiveUnaryDenominator}(t(n))$.
  \item $\mathsf{UnaryHistory}(s(n))\land\mathsf{UnaryHistory}(t(n))$.
  \item $(\hsame(s(n),\emp)\to\mathsf{False})\land
  (\hsame(t(n),\emp)\to\mathsf{False})$.
  \item $(\hsame(s(n),\Ezero(z_s))\to\mathsf{False})\land
  (\hsame(t(n),\Ezero(z_t))\to\mathsf{False})$.
  \item $\mathsf{UnaryHistory}(a)\land\mathsf{UnaryHistory}(b)\land
  \hsame(a,b)$.
\end{enumerate}
\end{theorem}
\begin{proof}
Unfold Definition~\ref{def:rat-stream-name-finite-window-classifier} at the
selected observation $n$, its bundle membership, and its unary witness. This
gives the selected point classifier
$\mathsf{RatHistoryClassifier}(s(n),t(n))$.

Apply \autoref{thm:rat-history-classifier-positive-denominators} to this
selected classifier. Then apply
\autoref{thm:positive-unary-denominator-unary-and-nonempty} separately to the
two positive-denominator witnesses. These rows give the two positive
denominators, the two unary endpoint histories, and the two exclusions against
$\emp$.

It remains to exclude zero-headed displays. If
$\hsame(s(n),\Ezero(z_s))$ holds, transport the positive-denominator witness for
$s(n)$ along that sameness by
\autoref{thm:rat-positive-unary-denominator-hsame-transport}, then contradict
\autoref{thm:rat-positive-denominator-e0-absurd}. The same argument applied to
$t(n)$ and $z_t$ gives the second zero-headed exclusion.

For the one-tail readback, transport the selected point classifier across the
two displayed one-extension endpoint comparisons, using
\autoref{thm:rat-history-classifier-hsame-transport}. The transported
classifier is $\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$.

Apply \autoref{thm:rat-history-classifier-e1-tail-unary-iff} in the forward
direction to read back $\mathsf{UnaryHistory}(a)$,
$\mathsf{UnaryHistory}(b)$, and $\hsame(a,b)$. The selected classifier, shape
rows, zero-headed exclusions, and one-tail readback rows are exactly the listed
endpoint package.
\end{proof}

\begin{theorem}[Rational stream-name finite-window stability fields]
\label{thm:rat-stream-name-finite-window-stability-fields}
For certified observation maps $s,t,u,s',t':\Hist\to\Hist$ and a probe bundle
$\mathcal{P}:\mathsf{ProbeBundle}$, the finite-window classifier has the
following stability fields.
\begin{enumerate}
  \item If every bundled unary observation of $s$ is carried,
  $$
  \forall n,\ \mathsf{InBundle}(n,\mathcal{P})\to
  \mathsf{UnaryHistory}(n)\to\mathsf{RatHistoryCarrier}(s(n)),
  $$
  then $\mathsf{RatStreamNameFiniteWindowClassifier}(s,s,\mathcal{P})$.
  \item $\mathsf{RatStreamNameFiniteWindowClassifier}(s,t,\mathcal{P})$
  implies
  $\mathsf{RatStreamNameFiniteWindowClassifier}(t,s,\mathcal{P})$.
  \item $\mathsf{RatStreamNameFiniteWindowClassifier}(s,t,\mathcal{P})$ and
  $\mathsf{RatStreamNameFiniteWindowClassifier}(t,u,\mathcal{P})$ imply
  $\mathsf{RatStreamNameFiniteWindowClassifier}(s,u,\mathcal{P})$.
  \item If $\mathsf{RatStreamNameFiniteWindowClassifier}(s,t,\mathcal{P})$
  and the endpoints satisfy
  $$
  \forall n,\ \mathsf{InBundle}(n,\mathcal{P})\to
  \mathsf{UnaryHistory}(n)\to\hsame(s(n),s'(n))
  $$
  and
  $$
  \forall n,\ \mathsf{InBundle}(n,\mathcal{P})\to
  \mathsf{UnaryHistory}(n)\to\hsame(t(n),t'(n)),
  $$
  then $\mathsf{RatStreamNameFiniteWindowClassifier}(s',t',\mathcal{P})$.
\end{enumerate}
\end{theorem}
\begin{proof}
Unfold Definition~\ref{def:rat-stream-name-finite-window-classifier}. For
reflexivity, fix a bundled certified unary observation $(n,\pi,\nu)$. The
given carrier row supplies $\mathsf{RatHistoryCarrier}(s(n))$, and RatUp
classifier reflexivity turns that carrier witness into
$\mathsf{RatHistoryClassifier}(s(n),s(n))$.

For symmetry, fix the same bundled unary observation data. The finite-window
classifier from $s$ to $t$ supplies the point classifier
$\mathsf{RatHistoryClassifier}(s(n),t(n))$, and RatUp classifier symmetry
reverses it.

For transitivity, fix a bundled unary observation and read the two
finite-window classifiers at it. RatUp classifier transitivity composes
$\mathsf{RatHistoryClassifier}(s(n),t(n))$ with
$\mathsf{RatHistoryClassifier}(t(n),u(n))$, giving the point classifier from
$s(n)$ to $u(n)$.

For endpoint transport, fix $(n,\pi,\nu)$ again. The source finite-window
classifier gives $\mathsf{RatHistoryClassifier}(s(n),t(n))$, while the two
endpoint transport rows give $\hsame(s(n),s'(n))$ and $\hsame(t(n),t'(n))$ at
the same bundled unary observation. RatUp point classifier transport along
these two $\hsame$ rows gives
$\mathsf{RatHistoryClassifier}(s'(n),t'(n))$.
\end{proof}
\leanchecked{BEDC.Derived.StreamNameUp.RatStreamNameFiniteWindowClassifier\_stability\_fields}

The ten-field fit is: traditional target finite rational observation-window
stability; BEDC source $\RatUp+\StreamNameUp+\mathsf{ProbeBundle}$; carrier
observation maps; classifier finite-window restriction; stability the four
displayed rows; ledger the finite probe bundle and endpoint transports; core
theorem \autoref{thm:rat-stream-name-finite-window-stability-fields};
exactness pointwise RatUp classification; standard bridge finite prefixes of
rational streams; status $\mathsf{ProofTarget}$.
