The $\DyadicIntervalNetUp$ L10 route consumes the $\DyadicRatCoreUp$ radius and endpoint arithmetic rows of \autoref{ch:concrete-instances-dyadicratcore-namecert} together with the located endpoint-and-cell surface of \autoref{ch:concrete-instances-locatedinterval-namecert} before interval-cover consumers read the listed dyadic cells. This dependency keeps the net packet on finite radius rows and located interval endpoints before the downstream cover route of \autoref{ch:concrete-instances-dyadic-interval-cover-namecert} is exposed. The bounded interval net consumer of \autoref{ch:concrete-instances-closedboundedintervalnet-namecert} reads the same DyadicRatCore endpoint arithmetic and dyadic interval refinement route before compact interval or Real-completion consumers may inspect a finite cover.
