The $\DyadicIntervalNetUp$ hub routes finite interval-cell readers through the L10 lattice before compact interval or completion consumers inspect a cell. The dependency first exposes $\DyadicRatCoreUp$ endpoint arithmetic from \autoref{ch:concrete-instances-dyadicratcore-namecert}, then $\StreamNameUp$ finite windows from \autoref{ch:concrete-instances-streamname-namecert}, then $\RegSeqRatUp$ readback from \autoref{ch:concrete-instances-regseqrat-namecert}, and finally the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. Compact interval and completion-facing consumers therefore read finite interval cells only after those sibling rows are visible, not as ambient compactness, selected limits, quotient interval equality, countable choice, or $\RealUp$ completeness.
