\chapter{Dyadic Interval Net NameCert Construction}
\label{ch:concrete-instances-dyadic-interval-net-namecert}
\origin{human}

Following Bishop and Bridges, the $\DyadicIntervalNetUp$ packet records finite dyadic interval nets as the displayed cover primitive used by Real and Completion arguments before any consumer may invoke compactness language. It sits after the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite StreamName windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. As a consumer route, the finite interval-cell row consumes dyadic endpoint arithmetic from \autoref{ch:concrete-instances-dyadicratcore-namecert}, exposes endpoint observations through \autoref{ch:concrete-instances-streamname-namecert}, reads tolerances through \autoref{ch:concrete-instances-regseqrat-namecert}, and seals only through \autoref{ch:concrete-instances-real-namecert}. The packet gives compact-interval, completion, and uniform-modulus consumers a finite cover surface; it does not assert open-cover compactness, selected subcovers, quotient stream equality, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.

The same finite dyadic cell data is consumed by \autoref{ch:concrete-instances-closedboundedintervalnet-namecert} and by the compact-interval uniform-modulus route as displayed finite cells, not as open-cover compactness. The finite Cauchy density-net route of \autoref{ch:concrete-instances-finite-cauchy-density-net-namecert} consumes these dyadic cell rows as finite density witnesses for L10 completion reads.

\begin{definition}[Dyadic interval net carrier]\label{def:dyadic-interval-net-carrier}
A $\DyadicIntervalNetUp$ carrier is a finite $\BHist$ packet
$$
I=(D,J,E,S,R,Q,H,C,P,N)
$$
with the following rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ endpoint row. It lists the dyadic endpoints, midpoint and radius arithmetic, and successor comparisons used by the net cells.
\item $J$ is the finite interval-cell row. It records a finite list of closed dyadic cells, each with displayed left and right endpoints and a cell radius.
\item $E$ is the endpoint-incidence row. It records that adjacent cells overlap or touch at displayed dyadic endpoints and that the first and last cells bracket the named interval endpoints.
\item $S$ is the $\StreamNameUp$ window row exposed when an interval endpoint or cell endpoint is read.
\item $R$ is the $\RegSeqRatUp$ readback row for the dyadic endpoints and the tolerance schedule induced by the cell radii.
\item $Q$ is the terminal $\RealUp$ seal row reached only after the displayed StreamName and RegSeqRat rows have been inspected.
\item $H$ records componentwise $\hsame$ transport for $D,J,E,S,R,Q$ and the structural rows.
\item $C$ records displayed $\Cont$ replay of the finite route
$$
D,J,E \longmapsto S,R \longmapsto Q,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\DyadicIntervalNetUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\DyadicIntervalNetUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,J,E,S,R,Q,H,C,P,N$. It has no coordinate for an arbitrary open cover, a selected finite subcover, quotient stream equality, a chosen limit point, countable choice, trichotomy, or completed real compactness.
\end{definition}

\begin{theorem}[Dyadic interval net cover refinement]\label{thm:dyadic-interval-net-cover-refinement}
For every accepted $\DyadicIntervalNetUp$ carrier $I=(D,J,E,S,R,Q,H,C,P,N)$, every Real and Completion consumer that reads a finite dyadic cover through $I$ factors through the finite route
$$
D,J,E \longmapsto S,R \longmapsto Q .
$$
The interval-cell row $J$ and endpoint-incidence row $E$ refine any displayed dyadic cover request into listed dyadic cells whose endpoints and radii are checked in $D$, whose inspected name coordinates are exposed in $S$, whose tolerance schedule is read in $R$, and whose Real boundary is sealed in $Q$. No open-cover compactness, selected subcover, quotient stream equality, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness is available along this route.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:dyadic-interval-net-carrier}. The rows $D,J,E$ are the only cover-facing data: $D$ supplies dyadic endpoint arithmetic, $J$ supplies the finite list of cells, and $E$ supplies endpoint incidence and bracketing.

The refinement is therefore a finite list operation. A displayed cover request is answered by reading the listed cells in $J$, checking their dyadic radii and endpoint comparisons in $D$, and using $E$ to verify that adjacent cells cover the named interval without opening a host compactness argument.

The Real-facing portion is delayed. The inspected endpoint names pass through $S$, the induced tolerance schedule passes through $R$, and the terminal real-name boundary is exposed only by $Q$.

Finally replay $H,C,P,N$. Transport, continuation replay, provenance, and local naming preserve exactly the same finite route, so the consumer cannot replace the dyadic net by a selected subcover, quotient stream equality, choice, trichotomy, host equality, or ambient completeness principle.
\end{proof}

\begin{theorem}[Dyadic interval net uniform subnet]\label{thm:dyadic-interval-net-uniform-subnet}
Every compact-uniform-continuity finite-cover read that consumes an accepted $\DyadicIntervalNetUp$ packet must expose dyadic cell rows, interval-radius rows, $\StreamNameUp$ or $\RegSeqRatUp$ window rows, and a $\UniformModulusUp$ handoff before the compact finite-net conclusion is read. In carrier notation, the read begins with the endpoint arithmetic and cell rows $D,J,E$, then passes through the window and readback rows $S,R$, and only then may a compact finite-net or uniform-modulus consumer inspect the terminal Real-facing seal $Q$ and the structural rows $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-interval-net-carrier}. The dyadic endpoint row $D$, finite interval-cell row $J$, and endpoint-incidence row $E$ are the only rows that can display cell membership, adjacency, and interval radii.

Apply the cover refinement theorem \autoref{thm:dyadic-interval-net-cover-refinement}. It forces every finite cover read to consume those dyadic rows before StreamName, RegSeqRat, or Real-facing data are visible.

The compact-uniform-continuity consumer then reads the finite-window route already named by this chapter. Endpoint observations pass through $S$, tolerance and radius readback pass through $R$, and the compact-interval uniform-modulus route can receive a $\UniformModulusUp$ handoff only after those rows have been exposed.

Finally replay $H,C,P,N$ together with the sibling route paragraphs to \autoref{ch:concrete-instances-closedboundedintervalnet-namecert} and \autoref{ch:concrete-instances-compact-interval-uniform-modulus-namecert}. The resulting finite-subnet branch is a displayed dyadic-cell route, not open-cover compactness, selected subcovers, quotient stream equality, countable choice, trichotomy, host real equality, or ambient completeness.
\end{proof}

\begin{theorem}[Dyadic interval net mesh-refinement induction]
\label{thm:dyadic-interval-net-mesh-refinement-induction}
For every accepted $\DyadicIntervalNetUp$ carrier
$$
I=(D,J,E,S,R,Q,H,C,P,N)
$$
and every displayed finite dyadic refinement depth $n$, repeated dyadic bisection of the interval-cell row preserves endpoint incidence, finite-cell coverage, $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance rows, $\RegSeqRatUp$ readback, and the terminal $\RealUp$ seal route. Each refined packet remains routed through
$$
D,J,E \longmapsto S,R \longmapsto Q,H,C,P,N,
$$
and every public read still occurs before the Real seal is opened.
\end{theorem}
\begin{proof}
Induct on the displayed finite refinement depth $n$. At depth zero, the accepted carrier itself supplies the endpoint incidence, coverage, StreamName windows, dyadic tolerances, RegSeqRat readback, and Real seal route.

For the step, apply \autoref{thm:dyadic-interval-net-cover-refinement} to the current refined packet. Bisection replaces each listed dyadic cell by finitely many listed subcells whose endpoints, radii, and adjacency checks are still read through the dyadic endpoint row $D$ and the incidence row $E$.

The consumer-facing rows remain downstream. Endpoint observations pass through $S$, tolerance schedules and regular rational reads pass through $R$, and the terminal real-name boundary remains $Q$.

Finally transport, replay, provenance, and local naming preserve the refined route componentwise. Since every induction step displays its finite cells before $Q$, no open-cover compactness, selected subcover, quotient stream equality, countable choice, trichotomy, host real equality, or ambient completeness principle enters the induction.
\end{proof}

\begin{theorem}[Dyadic interval net subdivision-depth coverage]
\label{thm:dyadic-interval-net-subdivision-depth-coverage}
For every accepted $\DyadicIntervalNetUp$ carrier
$$
I=(D,J,E,S,R,Q,H,C,P,N)
$$
and every displayed finite subdivision depth $n$, each refined cell coverage read factors through the $\DyadicRatCoreUp$ endpoint arithmetic row $D$, the finite cell list $J$, endpoint incidence $E$, $\StreamNameUp$ windows $S$, $\RegSeqRatUp$ tolerance readback $R$, and the terminal $\RealUp$ seal $Q$ before any compact-interval or uniform-modulus consumer may inspect it. The structural rows $H,C,P,N$ preserve the same route at every depth, so subdivision coverage remains finite dyadic interval-net data.
\end{theorem}
\begin{proof}
Induct on the displayed depth $n$. At depth zero, the claim is the accepted carrier route: the original listed cells are read through $D,J,E$, endpoint observations pass through $S$, tolerance readback passes through $R$, and the Real-facing boundary is $Q$.

For the successor depth, apply the cover refinement theorem \autoref{thm:dyadic-interval-net-cover-refinement}. Each bisection replaces a parent cell by finitely displayed child cells whose endpoints, radii, and adjacency checks still live in the dyadic endpoint and incidence rows.

Now use mesh-refinement induction from \autoref{thm:dyadic-interval-net-mesh-refinement-induction}. It transports the finite coverage route across the displayed bisection step and keeps the endpoint windows, tolerance rows, and Real seal downstream of the refined cells.

Finally replay $H,C,P,N$. Componentwise transport, displayed continuation replay, provenance, and local naming preserve the same route for compact-interval and uniform-modulus consumers, without adding open-cover compactness, selected subcovers, quotient stream equality, countable choice, trichotomy, host real equality, or ambient completeness.
\end{proof}

The induction step consumes the finite subdivision packet of \autoref{ch:concrete-instances-dyadic-interval-subdivision-namecert}: each bisection reads its dyadic scale, grid, and cell-coverage rows before the refined interval-net cells are handed back to $D,J,E$.

The same finite-cell surface is consumed by the compact interval uniform-modulus route of \autoref{ch:concrete-instances-compact-interval-uniform-modulus-namecert}. A downstream consumer reads the $\DyadicRatCoreUp$ endpoint row, the $\StreamNameUp$ window row, the $\RegSeqRatUp$ tolerance row, and the terminal $\RealUp$ seal in the displayed order, so the interval net remains a finite cover handoff rather than an open-cover compactness principle.

\begin{definition}[Dyadic compact-modulus carrier]
\label{def:dyadic-compact-modulus-carrier}
A dyadic compact-modulus carrier over an accepted $\DyadicIntervalNetUp$ packet $I=(D,J,E,S,R,Q,H,C,P,N)$ is a finite $\BHist$ packet
$$
M=(I,K,B,U,L,S',R',Q',T,V,W,N')
$$
with the following rows:
\begin{enumerate}
\item $K$ is the compact-interval source row, read only through the finite dyadic cells already listed in $J$ and their endpoint-incidence data $E$.
\item $B$ is the mesh-bound row, recording a displayed dyadic scale and the rational lower-bound fold used to compare cell radii.
\item $U$ is the pointwise modulus row, listing the local modulus data attached to the displayed cells rather than a selected global modulus.
\item $L$ is the finite lower-bound fold over the listed dyadic cells, producing the public compact-modulus budget from $B$ and $U$.
\item $S'$ and $R'$ are the $\StreamNameUp$ and $\RegSeqRatUp$ readback rows for endpoint observations, mesh bounds, and tolerance budgets.
\item $Q'$ is the $\RealUp$ seal row for the compact-interval endpoint and value data, reached only after $S'$ and $R'$ are displayed.
\item $T$ records componentwise $\hsame$ transport for $I,K,B,U,L,S',R',Q'$ and the structural rows.
\item $V$ records displayed $\Cont$ replay from dyadic cells through mesh bounds, pointwise modulus rows, lower-bound fold, readback, and seal.
\item $W$ is the $\Pkg$ provenance over $\DyadicIntervalNetUp$, compact-interval source rows, uniform-modulus consumers, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N'$ is the local dyadic compact-modulus $\NameCert$ row.
\end{enumerate}
The classifier compares accepted compact-modulus packets only through $I,K,B,U,L,S',R',Q',T,V,W,N'$. It has no coordinate for open-cover compactness, a selected finite subcover, a selected global modulus, quotient interval equality, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Dyadic compact-modulus NameCert obligations]
\label{thm:dyadic-compact-modulus-namecert-obligations}
For every accepted dyadic compact-modulus carrier $M=(I,K,B,U,L,S',R',Q',T,V,W,N')$, the local NameCert surface consists exactly of carrier admission for those twelve rows; finite-cell exposure through the accepted $\DyadicIntervalNetUp$ packet $I$; compact-interval source exposure through $K$; mesh-bound exposure through $B$; pointwise modulus exposure through $U$; rational lower-bound folding through $L$; $\StreamNameUp$ and $\RegSeqRatUp$ readback through $S'$ and $R'$; terminal $\RealUp$ sealing through $Q'$; componentwise $\hsame$ transport through $T$; displayed $\Cont$ replay through $V$; $\Pkg$ provenance through $W$; and local naming through $N'$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-compact-modulus-carrier}. Its only cover-facing input is the accepted interval-net packet $I$, and its only compact-interval input is the source row $K$ read through the finite cells of $I$.

The modulus budget is finite. The mesh-bound row $B$ records the dyadic scale, $U$ lists pointwise modulus rows attached to displayed cells, and $L$ folds the rational lower bounds over the finite cell list.

The readback and seal rows remain downstream. Endpoint observations and tolerance budgets pass through $S'$ and $R'$, and the terminal real boundary is $Q'$.

Finally replay $T,V,W,N'$. Transport, continuation replay, provenance, and local naming preserve exactly those rows, so no open cover, selected finite subcover, selected global modulus, quotient interval equality, countable choice, or ambient completeness row can enter the NameCert surface.
\end{proof}

\begin{theorem}[Dyadic compact-modulus Heine--Cantor route]
\label{thm:dyadic-compact-modulus-heine-cantor-route}
Every compact-interval uniform-modulus or Heine--Cantor finite-net consumer of an accepted dyadic compact-modulus carrier $M=(I,K,B,U,L,S',R',Q',T,V,W,N')$ must read the finite route
$$
I,K \longmapsto B,U,L \longmapsto S',R',Q',T,V,W,N'
$$
before it may use the public compact-modulus budget. The route sends dyadic cells, mesh bounds, endpoint incidence, compact-interval source data, pointwise modulus rows, rational lower-bound fold, $\StreamNameUp$ and $\RegSeqRatUp$ readback, and $\RealUp$ sealing to compact-interval uniform-modulus consumers. It exports no open-cover compactness, selected subcover, quotient interval equality, countable choice, selected global modulus, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start from the dyadic interval net uniform subnet theorem, \autoref{thm:dyadic-interval-net-uniform-subnet}. It forces compact-uniform-continuity reads to expose dyadic cell rows, interval-radius rows, StreamName or RegSeqRat windows, and a uniform-modulus handoff before a compact finite-net conclusion is read.

Apply the compact-modulus obligations of \autoref{thm:dyadic-compact-modulus-namecert-obligations}. They refine that handoff through the compact-interval source row $K$, mesh-bound row $B$, pointwise modulus row $U$, and finite lower-bound fold $L$.

Now use the Real source link and Real seal nonescape theorems, \autoref{thm:dyadic-interval-net-real-source-link} and \autoref{thm:dyadic-interval-net-real-seal-nonescape}. The endpoint observations and tolerance budgets must pass through the displayed StreamName and RegSeqRat readback rows before the Real seal is opened.

Finally replay $T,V,W,N'$. The compact-interval uniform-modulus and Heine--Cantor consumers receive the finite dyadic-cell route just described, with no coordinate for open-cover compactness, selected subcovers, quotient interval equality, countable-choice schedules, selected global moduli, or ambient Real completeness.
\end{proof}

\begin{theorem}[Dyadic interval net obligation surface]\label{thm:dyadic-interval-net-obligation-surface}
For every accepted $\DyadicIntervalNetUp$ carrier $I=(D,J,E,S,R,Q,H,C,P,N)$, carrier admission and cell coverage are exhausted by the displayed rows $D,J,E,H,C,P,N$. The endpoint arithmetic, finite interval-cell list, endpoint incidence, componentwise transport, replay, provenance, and local naming rows supply the complete finite obligation surface for a dyadic net request, and no open-cover compactness or selected finite subcover is exported.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dyadic-interval-net-carrier}. The rows $D,J,E$ display the endpoint arithmetic, listed cells, and incidence witnesses, while $H,C,P,N$ transport, replay, source, and name those rows.

Cell coverage is therefore a finite row check. Each requested interval read is matched with cells already listed in $J$, the endpoint comparisons already listed in $D$, and the bracketing and adjacency data already listed in $E$.

The structural rows do not add a second cover object. They preserve the displayed packet componentwise, so no open cover, selected subcover, choice principle, or ambient compactness row can enter the obligation surface.
\end{proof}

\begin{theorem}[Dyadic interval net RegSeqRat route]\label{thm:dyadic-interval-net-regseqrat-route}
Every accepted $\DyadicIntervalNetUp$ packet consumed by a $\RegSeqRatUp$ or Real-facing route first exposes the dyadic interval endpoints, finite net cells, $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance rows, $\RegSeqRatUp$ readback, and terminal $\RealUp$ seal. Concretely, the route reads the endpoint arithmetic and cell rows $D,J,E$, then the window row $S$ of \autoref{ch:concrete-instances-streamname-namecert}, then the tolerance and readback rows supplied by \autoref{ch:concrete-instances-dyadicratcore-namecert} and \autoref{ch:concrete-instances-regseqrat-namecert}, and only then the Real seal $Q$. No quotient stream, selected limit, selected subcover, countable-choice schedule, or ambient completeness coordinate is available to the consumer.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:dyadic-interval-net-carrier}. The rows $D,J,E$ are the finite interval-net rows: dyadic endpoints, listed cells, and endpoint incidence.

Use the NameCert obligation surface of \autoref{thm:dyadic-interval-net-obligation-surface}. It permits a downstream consumer to inspect only the displayed carrier rows, so any RegSeqRat-facing request must first pass through the listed cells and the dyadic endpoint arithmetic already present in $D,J,E$.

The readback rows are then forced in order. The StreamName row $S$ supplies finite observation windows, the DyadicRatCore tolerance information supplies the dyadic radius and comparison rows, and the RegSeqRat row $R$ supplies the regular rational readback before $Q$ can be opened.

Finally replay $H,C,P,N$. Transport, continuation replay, provenance, and local naming preserve the same route, leaving no coordinate from which a quotient stream, selected limit, selected subcover, choice schedule, or ambient completeness principle could be read.
\end{proof}

\begin{theorem}[Dyadic interval net StreamName and RegSeqRat handoff]\label{thm:dyadic-interval-net-streamname-regseqrat-handoff}
Every downstream consumer of an accepted $\DyadicIntervalNetUp$ carrier that inspects interval endpoints or cell tolerances must factor the read through
$$
D,J,E \longmapsto S,R \longmapsto H,C,P,N .
$$
The $\StreamNameUp$ row $S$ exposes the finite endpoint windows and the $\RegSeqRatUp$ row $R$ exposes the rational tolerance readback; neither row may be skipped or replaced by quotient stream equality, selected limits, or countable choice.
\end{theorem}
\begin{proof}
Start from a downstream endpoint or tolerance read. By the carrier definition, the only cover-facing rows available before the read are $D,J,E$, so the requested endpoint or cell radius is first located inside the displayed dyadic net.

The read then passes through $S$ and $R$. The StreamName row supplies finite observation windows for the endpoint names, and the RegSeqRat row supplies the regular rational schedule attached to those observations.

Finally replay $H,C,P,N$. Componentwise transport, continuation replay, provenance, and local naming keep the same finite handoff visible to the consumer, excluding quotient stream equality, selected limits, and choice.
\end{proof}

\begin{theorem}[Dyadic interval net Real seal nonescape]\label{thm:dyadic-interval-net-real-seal-nonescape}
For every accepted $\DyadicIntervalNetUp$ carrier $I=(D,J,E,S,R,Q,H,C,P,N)$, the terminal $\RealUp$ seal row $Q$ is reachable only after the finite dyadic net route
$$
D,J,E \longmapsto S,R
$$
has been displayed. Any Real or Completion consumer that reaches $Q$ remains inside the same routed packet $D,J,E,S,R,Q,H,C,P,N$ and cannot escape to host real equality, real trichotomy, ambient $\RealUp$ completeness, selected subcovers, quotient equality, selected limits, or countable choice.
\end{theorem}
\begin{proof}
Project the accepted packet. The carrier places $Q$ downstream of the endpoint arithmetic, finite cells, endpoint incidence, StreamName windows, and RegSeqRat readback rows.

Thus a Real or Completion consumer must first traverse $D,J,E,S,R$. These rows display the cover cells, endpoint reads, and tolerance schedule before any Real-facing boundary is available.

Once $Q$ is reached, the structural rows $H,C,P,N$ only preserve the same displayed route. Since no excluded equality, selection, trichotomy, choice, or completeness row is present in the carrier, the Real seal cannot be used as an escape from the finite dyadic interval-net packet.
\end{proof}

\begin{theorem}[Dyadic interval net Real source link]\label{thm:dyadic-interval-net-real-source-link}
Every Real-facing finite-net consumer of an accepted $\DyadicIntervalNetUp$ carrier $I=(D,J,E,S,R,Q,H,C,P,N)$ must read the dyadic endpoint arithmetic row $D$, the finite cell row $J$, the endpoint-incidence row $E$, the $\StreamNameUp$ window row $S$, the $\RegSeqRatUp$ readback row $R$, and the terminal $\RealUp$ seal row $Q$ in that displayed order before it can hand the packet to closed-bounded interval, compact-interval, or uniform-modulus consumers. The source link exports only the routed packet $D,J,E,S,R,Q,H,C,P,N$; it exports no selected subcover, quotient stream equality, host real equality, countable-choice schedule, trichotomy, or ambient completeness principle.
\end{theorem}
\begin{proof}
Begin with the cover refinement theorem \autoref{thm:dyadic-interval-net-cover-refinement}. It places every finite-net read at the dyadic endpoint, cell, and incidence rows $D,J,E$ before any Real-facing boundary is visible.

Next use the RegSeqRat route and the StreamName and RegSeqRat handoff, \autoref{thm:dyadic-interval-net-regseqrat-route} and \autoref{thm:dyadic-interval-net-streamname-regseqrat-handoff}. These force endpoint observations through $S$ and rational readback through $R$, so the consumer cannot skip directly from dyadic cells to a terminal real object.

Finally apply the Real seal nonescape theorem \autoref{thm:dyadic-interval-net-real-seal-nonescape}. Once $Q$ is reached, the structural rows $H,C,P,N$ preserve the same displayed packet, giving closed-bounded interval, compact-interval, and uniform-modulus consumers a finite source link and no ambient compactness or choice endpoint.
\end{proof}

\begin{theorem}[Dyadic interval net barycentric refinement consumer]\label{thm:dyadic-interval-net-barycentric-refinement-consumer}
Let $I=(D,J,E,S,R,Q,H,C,P,N)$ be an accepted $\DyadicIntervalNetUp$ carrier. If a displayed $\DyadicBarycentricRefinementUp$ packet refines the parent cells in $J$ by finite barycentric child cells, then every downstream consumer of the refined net still reads endpoint arithmetic through $\DyadicRatCoreUp$, finite endpoint windows through $\StreamNameUp$, tolerance readback through $\RegSeqRatUp$, and the terminal seal through $\RealUp$ before any compact-interval or completion consumer inspects it. The refined route remains a finite L10 route through
$$
D,J,E \longmapsto S,R \longmapsto Q,H,C,P,N
$$
together with the sibling refinement rows of \autoref{ch:concrete-instances-dyadic-barycentric-refinement-namecert}; it exports no open-cover compactness, selected subcover, quotient stream equality, countable-choice schedule, trichotomy, host real equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start from the cover refinement theorem \autoref{thm:dyadic-interval-net-cover-refinement}. The parent cells and endpoint-incidence rows already force the consumer to read the finite cell ledger $J,E$ and the endpoint arithmetic row $D$ before any Real-facing boundary is visible.

Use mesh-refinement induction from \autoref{thm:dyadic-interval-net-mesh-refinement-induction}. A barycentric child-cell packet is a displayed finite refinement of the same kind: each child endpoint and mesh witness is listed before the StreamName and RegSeqRat rows are opened.

Now apply the Real source link \autoref{thm:dyadic-interval-net-real-source-link} and the sibling $\DyadicBarycentricRefinementUp$ chapter. The sibling packet supplies the parent pointers, barycenter rows, and child-cell ledger, while this chapter supplies the endpoint, window, readback, and Real-seal order consumed by compact-interval and completion arguments.

Finally replay $H,C,P,N$. Componentwise transport, continuation replay, provenance, and local naming keep the refined packet on the same finite L10 route, so barycentric refinement cannot introduce selected subcovers, quotient stream equality, choice, trichotomy, host equality, or ambient completeness.
\end{proof}

\begin{theorem}[Dyadic interval net finite-chain coverage]
\label{thm:dyadic-interval-net-finite-chain-coverage}
Every accepted finite chain of interval cells consumed by closed-bounded interval, compact-interval, or uniform-modulus routes is exhausted by the displayed rows
$$
D,J,E,S,R,Q,H,C,P,N .
$$
Coverage is checked by finite induction over the listed cells in $J$: each cell reads its dyadic endpoints and radius from $D$, its adjacency and endpoint incidence from $E$, its observed endpoint windows from $S$, its rational readback and tolerance schedule from $R$, and its terminal Real-facing boundary from $Q$. No open-cover compactness, selected subcover, quotient stream equality, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness is available along the chain.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:dyadic-interval-net-carrier}. The finite chain is the listed interval-cell row $J$, while $D$ and $E$ provide the only endpoint arithmetic and incidence data attached to those cells.

Use the cover refinement theorem \autoref{thm:dyadic-interval-net-cover-refinement}. It forces every consumer-facing cover read to pass through $D,J,E$ before any StreamName, RegSeqRat, or Real-facing row is visible.

Induct over the finite list of cells using \autoref{thm:dyadic-interval-net-mesh-refinement-induction}. The base case reads the initial listed cell data. The step consumes one displayed cell, preserves adjacency through $E$, and leaves the remaining listed cells under the same $D,J,E,S,R,Q,H,C,P,N$ route.

Finally replay the downstream rows. The windows $S$, readback $R$, seal $Q$, and structural rows $H,C,P,N$ preserve the finite chain without adding open-cover compactness, selected subcovers, quotient stream equality, choice, trichotomy, host real equality, or ambient completeness.
\end{proof}

\begin{theorem}[Dyadic interval net finite-cell dependency chain]\label{thm:dyadic-interval-net-finite-cell-dependency-chain}
Every finite-cell read of an accepted $\DyadicIntervalNetUp$ packet consumed by compact, closed-bounded, or uniform-modulus chapters must expose $\DyadicRatCoreUp$ endpoint arithmetic, $\RegSeqRatUp$ readback, $\DyadicIntervalRefinementUp$ retained-cell rows, and the terminal $\RealUp$ seal before the interval-net row is public. The dependency chain is a finite route through the displayed packet and sibling refinement rows, not an open-cover compactness theorem, selected subcover, quotient stream equality, countable-choice schedule, host real equality, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with finite-chain coverage, \autoref{thm:dyadic-interval-net-finite-chain-coverage}. It exhausts every downstream finite-cell read by the displayed rows $D,J,E,S,R,Q,H,C,P,N$.

Apply the compact-interval forward route and the uniform-modulus refinement route, \autoref{thm:dyadic-interval-net-compact-interval-forward-route} and \autoref{thm:dyadic-interval-net-uniform-modulus-refinement-route}. These routes keep compact and modulus consumers downstream of endpoint arithmetic, retained finite cells, StreamName windows, RegSeqRat readback, and the Real seal.

Finally read the sibling dependency route of \autoref{thm:dyadic-interval-net-orphan-route}. It supplies the $\DyadicIntervalRefinementUp$ retained-cell dependency together with DyadicRatCore, RegSeqRat, and Real rows, so the public interval-net read remains the stated finite-cell dependency chain.
\end{proof}

\begin{theorem}[Dyadic interval net sibling cover route]\label{thm:dyadic-interval-net-sibling-route}
Every accepted $\DyadicIntervalNetUp$ finite-cell route is consumed by the finite membership ledger of \autoref{ch:concrete-instances-dyadic-interval-cover-namecert}. The consumer route sends the $\DyadicRatCoreUp$ endpoint row $D$, the finite cell and endpoint-incidence rows $J,E$, the $\StreamNameUp$ window row $S$, the $\RegSeqRatUp$ readback row $R$, and the $\RealUp$ seal row $Q$ into the cover packet's endpoint, mesh, membership, window, readback, and seal rows. This is a substantive downstream cover-consumer dependency: it records how the interval-cover chapter uses the net carrier, and it is not a self-reference.
\end{theorem}
\begin{proof}
Project the net carrier. The rows $D,J,E$ are the finite cell data, and the rows $S,R,Q$ are the StreamName, RegSeqRat, and Real-facing handoff data already ordered by \autoref{thm:dyadic-interval-net-real-seal-nonescape}.

Read the cover consumer. The DyadicIntervalCover packet uses dyadic endpoints, mesh and membership rows, finite windows, regular readbacks, and a Real seal, so the net rows provide exactly the finite-cell source consumed by that ledger.

The dependency is external to this chapter. It points from the net carrier to the sibling cover chapter and does not add a second net theorem, selected subcover, open-cover compactness row, or ambient Real completeness principle.
\end{proof}

\begin{theorem}[Dyadic interval net regular tail cover]
\label{thm:dyadic-interval-net-regular-tail-cover}
Every $\DyadicIntervalNetUp$ cover consumed by a regular-tail or Real-facing route must expose $\DyadicRatCoreUp$ endpoint arithmetic, finite cell incidence, $\StreamNameUp$ windows, $\RegSeqRatUp$ tolerance and readback, and the terminal $\RealUp$ seal before any cover conclusion is public. The cover is therefore a finite route through $D,J,E,S,R,Q,H,C,P,N$, not a selected subcover, open-cover compactness principle, quotient stream equality, countable-choice schedule, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Start with the carrier of \autoref{def:dyadic-interval-net-carrier}. The rows $D,J,E$ expose dyadic endpoints, listed cells, and endpoint incidence before any cover read can be exported.

Apply the RegSeqRat route and StreamName handoff, \autoref{thm:dyadic-interval-net-regseqrat-route} and \autoref{thm:dyadic-interval-net-streamname-regseqrat-handoff}. These force regular-tail consumers to read the finite windows $S$ and the tolerance/readback row $R$ after the endpoint and incidence rows.

Finally use the Real seal nonescape theorem \autoref{thm:dyadic-interval-net-real-seal-nonescape}. The seal row $Q$ is downstream of the displayed finite route and cannot be read as selected subcovers, open-cover compactness, quotient stream equality, countable choice, or ambient Real completeness.
\end{proof}

\closureat{DyadicIntervalNetUp}{seedStr}

\begin{closurestatus}{\DyadicIntervalNetUp}
  \constructivestory{A $\DyadicIntervalNetUp$ packet is generated from dyadic endpoint arithmetic, finite interval cells, endpoint incidence, StreamName windows, RegSeqRat readback, Real seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-interval-net-carrier} and \autoref{thm:dyadic-interval-net-cover-refinement} seed the finite dyadic interval-net carrier over dyadic endpoint arithmetic, listed interval cells, endpoint incidence, StreamName windows, RegSeqRat readback, Real seals, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, selected subcovers, quotient stream equality, countable choice, trichotomy, host real equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic cell coverage, endpoint-incidence refinement, StreamName and RegSeqRat readback routing, Real seal ordering, provenance, and local naming for BEDC.Derived.DyadicIntervalNetUp.}
\end{closurestatus}
