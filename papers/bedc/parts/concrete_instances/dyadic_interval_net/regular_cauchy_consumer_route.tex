\begin{theorem}[DyadicIntervalNet regular-Cauchy consumer route]
\label{thm:dyadic-interval-net-regular-cauchy-consumer-route}
Every $\RegSeqRatUp$ or $\RealUp$ completion-facing consumer that reads a $\DyadicIntervalNetUp$ surface must first expose the $\DyadicRatCoreUp$ endpoint arithmetic, $\StreamNameUp$ finite windows, $\RegSeqRatUp$ readback, terminal $\RealUp$ sealing, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows attached to the interval-net packet. The dyadic interval cells therefore enter regular-Cauchy and Real-name consumers only through the finite route named by \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}.
\end{theorem}
\begin{proof}
Project the accepted $\DyadicIntervalNetUp$ carrier. Its public surface is finite dyadic cell incidence, so endpoint arithmetic is read first through the $\DyadicRatCoreUp$ row rather than through host real endpoints.

The completion-facing read then opens the $\StreamNameUp$ finite windows and the $\RegSeqRatUp$ regular-rational readback. These rows are the only route by which the interval cells become regular-Cauchy data for a downstream consumer.

The terminal $\RealUp$ seal is downstream of that readback. Componentwise $\hsame$ transport and displayed $\Cont$ replay preserve the same finite rows, while $\Pkg$ provenance and local $\NameCert$ name the packet.

Thus a regular-Cauchy or Real-facing consumer receives the interval-net surface only after the listed rows have been displayed. The route exports no selected limit, quotient stream equality, countable-choice schedule, host real endpoint, ambient completeness theorem, or detached interval-net object.
\end{proof}
