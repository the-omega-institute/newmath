The $\DyadicIntervalNetUp$ prime forward route exposes a horizontal L10 dependency before arithmetic or Real-facing consumers inspect interval cells: dyadic endpoint and radius arithmetic comes from \autoref{ch:concrete-instances-dyadicratcore-namecert}, finite observation windows come from \autoref{ch:concrete-instances-streamname-namecert}, regular rational readback comes from \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal seal comes from \autoref{ch:concrete-instances-real-namecert}. Thus a prime-window or compact-interval consumer reads a finite dyadic interval net only after those displayed sibling rows are visible, not as an ambient compactness principle or host Real-completion object.
