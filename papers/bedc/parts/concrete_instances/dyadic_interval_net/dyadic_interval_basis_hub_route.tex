Every $\DyadicIntervalNetUp$ packet reads the dyadic interval basis of \autoref{ch:concrete-instances-dyadic-interval-basis-namecert}, the finite epsilon-net surface of \autoref{ch:concrete-instances-finite-epsilon-net-namecert}, and the dyadic rational core of \autoref{ch:concrete-instances-dyadicratcore-namecert} before compactness or Real-completion consumers inspect the mesh. This route keeps the interval cells, finite covering rows, and endpoint arithmetic visible before any downstream compactness, Cauchy, or $\RealUp$ seal argument treats the net as a completed object.

\begin{theorem}[Dyadic interval net basis route]
\label{thm:dyadic-interval-net-basis-route}
Every accepted $\DyadicIntervalNetUp$ read first consumes the dyadic interval basis, finite epsilon-net, and $\DyadicRatCoreUp$ endpoint rows before any compactness or Real-completion consumer reads the mesh.
\end{theorem}
\begin{proof}
Project the mesh route through \autoref{ch:concrete-instances-dyadic-interval-basis-namecert}. The interval basis supplies the displayed cells, and \autoref{ch:concrete-instances-finite-epsilon-net-namecert} supplies the finite net rows over those cells.

The endpoint and width comparisons are then read through \autoref{ch:concrete-instances-dyadicratcore-namecert}. Only after these three sibling rows are visible can a compactness or Real-completion consumer inspect the mesh as a net.
\end{proof}
