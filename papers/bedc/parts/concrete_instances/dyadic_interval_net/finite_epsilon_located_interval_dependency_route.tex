The finite interval-net surface of $\DyadicIntervalNetUp$ is read before the finite coverage consumer of \autoref{ch:concrete-instances-finite-epsilon-net-namecert} and the endpoint-radius consumer of \autoref{ch:concrete-instances-located-real-interval-namecert}. The interval cells $D,J,E$ expose dyadic endpoints and adjacency first; only then may a downstream finite epsilon-net package read centers, tolerances, and coverage rows, or a located real interval package read endpoint, radius, locator, and metric-bracket rows.

\begin{theorem}[Dyadic interval net finite-epsilon located-interval dependency]
\label{thm:dyadic-interval-net-finite-epsilon-located-interval-dependency}
Every finite-epsilon-net or located-real-interval consumer of an accepted $\DyadicIntervalNetUp$ carrier
$$
I=(D,J,E,S,R,Q,H,C,P,N)
$$
must read the finite interval-net route
$$
D,J,E \longmapsto S,R \longmapsto Q,H,C,P,N
$$
before exposing interval coverage, center coverage, or endpoint-radius locatedness. Thus \autoref{ch:concrete-instances-finite-epsilon-net-namecert} receives a finite cell surface rather than a host finite subset, and \autoref{ch:concrete-instances-located-real-interval-namecert} receives displayed endpoint-radius data rather than host interval equality.
\end{theorem}
\begin{proof}
Project the accepted carrier. The rows $D,J,E$ are the only dyadic interval-net cells, endpoint incidences, and bracketing data available to any consumer.

The finite epsilon-net consumer can therefore read centers and coverage only after the interval cells have been displayed. Its coverage row is downstream of the same finite dyadic route, so it cannot replace the interval net by a host finite subset or selected open-cover proof.

The located real interval consumer is similarly downstream. It may read endpoints, radii, locators, and metric brackets only after the dyadic endpoint and incidence rows have supplied the displayed interval cells.

Finally $S,R,Q,H,C,P,N$ preserve the route through StreamName windows, rational readback, the terminal Real seal, transport, replay, provenance, and local naming. No host interval equality, arbitrary open cover, selected subcover, countable choice, or ambient completeness row is exported.
\end{proof}
