The $\DyadicIntervalNetUp$ carrier reads its finite dyadic interval-net dependency route through \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The substantive sibling rows keep endpoint arithmetic, regular rational readback, and the terminal real seal visible as finite interval-net data rather than as an ambient compactness or host-completion principle.
