The $\DyadicIntervalNetUp$ carrier supplies the finite dyadic cell row and endpoint-incidence surface consumed by \autoref{ch:concrete-instances-closedboundedintervalnet-namecert} and \autoref{ch:concrete-instances-compact-interval-uniform-modulus-namecert} before any compact or uniform-modulus consumer reads the $\RealUp$ seal. Its dependency route still passes through \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadic-interval-refinement-namecert}, and \autoref{ch:concrete-instances-real-namecert}, so endpoint arithmetic, retained-cell refinement, regular rational readback, and terminal sealing remain visible as finite interval-net data rather than as an ambient compactness or host-completion principle.
