The $\DyadicIntervalNetUp$ carrier reads its dyadic endpoint and radius arithmetic from \autoref{ch:concrete-instances-dyadicratcore-namecert} and its located endpoint cells from \autoref{ch:concrete-instances-locatedinterval-namecert}. The substantive sibling rows then pass through $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ as finite interval-net readback data rather than as an ambient compactness or host-completion principle.
