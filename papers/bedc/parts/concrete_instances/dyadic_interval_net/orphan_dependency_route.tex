\begin{theorem}[DyadicIntervalNet sibling dependency route]
\label{thm:dyadic-interval-net-orphan-route}
Every finite-cell read of a $\DyadicIntervalNetUp$ carrier consumed by \autoref{ch:concrete-instances-closedboundedintervalnet-namecert}, \autoref{ch:concrete-instances-compact-interval-uniform-modulus-namecert}, or \autoref{ch:concrete-instances-regular-sequence-space-namecert} must first expose DyadicRatCore endpoint arithmetic, RegSeqRat readback, DyadicIntervalRefinement retained-cell rows, and the terminal $\RealUp$ seal. Thus no compact, uniform-modulus, or common-window completion consumer can use the interval-net carrier as an ambient compactness principle, a host completion theorem, or an untracked real endpoint object.
\end{theorem}
\begin{proof}
Read the dependency route through \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadic-interval-refinement-namecert}, and \autoref{ch:concrete-instances-real-namecert}. These sibling chapters supply endpoint arithmetic, regular rational readback, retained-cell refinement, and terminal sealing before the finite-cell row is visible to any compact or uniform-modulus consumer. Therefore the DyadicIntervalNet packet remains a finite interval-net route rather than an ambient compactness or host-completion principle.

The closed-bounded interval net consumer of \autoref{ch:concrete-instances-closedboundedintervalnet-namecert} reads the same DyadicRatCore endpoint arithmetic, RegSeqRat tolerance readback, DyadicIntervalRefinement retained-cell rows, and terminal $\RealUp$ seal before it treats the finite cells as a bounded interval net.

The regular sequence space consumer of \autoref{ch:concrete-instances-regular-sequence-space-namecert} uses the same finite-cell refinements as a common-window completion route. Its StreamName and RegSeqRat windows must still pass through DyadicRatCore tolerances, retained dyadic cells, and the terminal $\RealUp$ seal before Real-facing reads are public.
\end{proof}

\begin{theorem}[DyadicIntervalNet compactness consumer route]
\label{thm:dyadic-interval-net-compactness-consumer-route}
Any compactness consumer using a $\DyadicIntervalNetUp$ carrier factors through the existing compact-interval finite-net route of \autoref{ch:concrete-instances-heine-borel-finite-net-namecert} and \autoref{ch:concrete-instances-heine-borel-net-compactness-namecert} only after DyadicRatCore endpoints, RegSeqRat tolerance readback, DyadicIntervalRefinement retained cells, and the terminal $\RealUp$ seal are displayed. The route exports finite interval cells and endpoint incidence to compactness-facing consumers; it does not export open-cover compactness, selected subcovers, countable choice, quotient stream equality, host Real equality, or ambient Real completeness.
\end{theorem}
\begin{proof}
Start with the sibling dependency route of \autoref{thm:dyadic-interval-net-orphan-route}. It places DyadicRatCore endpoint arithmetic, RegSeqRat readback, DyadicIntervalRefinement retained-cell rows, and the terminal Real seal before any compact, uniform-modulus, or completion consumer can use the finite-cell row.

For a named compactness consumer, route the read through \autoref{ch:concrete-instances-heine-borel-finite-net-namecert} and \autoref{ch:concrete-instances-heine-borel-net-compactness-namecert}. Those chapters record the compactness-facing boundary as a finite-net route, so the interval-net packet is consumed as displayed finite cells rather than as a detached compactness theorem.

For compact-interval finite-net uses, read the finite-net siblings through the compact-interval handoff already cited by the DyadicIntervalNet routes. The same endpoint arithmetic, tolerance readback, retained dyadic cells, and terminal seal remain visible before any finite-net conclusion is public.

Thus both consumer paths preserve the L10 finite-cover route. Since neither path adds a coordinate for open-cover compactness, selected subcovers, countable choice, quotient stream equality, host Real equality, or ambient Real completeness, the DyadicIntervalNet packet remains a finite dependency edge for compactness consumers.
\end{proof}
