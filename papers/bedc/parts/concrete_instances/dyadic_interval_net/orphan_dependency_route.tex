The $\DyadicIntervalNetUp$ carrier reads its finite dyadic interval-net dependency route through \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The one-step dyadic interval refinement supplied by \autoref{ch:concrete-instances-dyadic-interval-refinement-namecert} remains the sibling route that exposes retained-cell refinement before a cover or completion consumer reads the net. The substantive sibling rows keep endpoint arithmetic, retained-cell refinement, regular rational readback, and the terminal real seal visible as finite interval-net data rather than as an ambient compactness or host-completion principle.
