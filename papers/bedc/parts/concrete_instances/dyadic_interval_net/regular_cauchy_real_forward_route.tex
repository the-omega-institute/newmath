\begin{theorem}[DyadicIntervalNet regular-Cauchy Real forward route]
\label{thm:dyadic-interval-net-regular-cauchy-real-forward-route}
Every $\DyadicIntervalNetUp$ consumer that exports interval windows toward regular-Cauchy or Real-completion routes must first pass through \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, and \autoref{ch:concrete-instances-real-namecert} before any terminal $\RealUp$ read is available.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:dyadic-interval-net-carrier}. The finite dyadic interval cells expose endpoint and radius data, but they do not by themselves license a regular-Cauchy or Real-facing read.

For completion-facing consumers, route the endpoint windows through the $\StreamNameUp$ schedule and the regular rational readback through $\RegSeqRatUp$. Only after those two sibling rows have been displayed may the terminal $\RealUp$ seal be read.

Thus every such consumer factors through the StreamName, RegSeqRat, and Real chapters named above. The route supplies no ambient compactness theorem, selected limit, quotient stream equality, host real endpoint, or unrestricted completion principle.
\end{proof}
