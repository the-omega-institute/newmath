The $\DyadicIntervalNetUp$ route reads dyadic mesh cells through \autoref{ch:concrete-instances-dyadicratcore-namecert} and exports finite interval mesh data to the $\RealUp$-facing route of \autoref{ch:concrete-instances-real-namecert}. This is a finite net dependency, not compactness, open-cover completeness, or an ambient completeness principle for intervals.
