The $\DyadicIntervalNetUp$ finite-net route supplies the finite dyadic endpoints, listed cells, endpoint incidence, and mesh witnesses that a finite-net consumer can read before any compact-interval boundary is exposed. The same route then hands those rows to \autoref{ch:concrete-instances-heine-borel-finite-net-namecert} for finite-net coverage and to \autoref{ch:concrete-instances-compact-interval-uniform-modulus-namecert} for compact-interval modulus control. The dependency is horizontal and finite: it records how the dyadic net packet feeds those sibling consumers, and it does not claim open-cover compactness, selected subcovers, quotient interval equality, countable choice, or ambient $\RealUp$ completeness.

The displayed endpoint, radius, and cell-incidence rows are also the finite-net surface consumed by \autoref{ch:concrete-instances-closedboundedintervalnet-namecert}; both the closed-bounded interval and compact-interval uniform-modulus routes receive listed dyadic cells, not open-cover compactness.
