\chapter{A Concrete Naming Certificate for $\SemicontinuousExtremeValueUp$}
\label{ch:concrete-instances-semicontinuous-extreme-value-namecert}
\origin{human}

Following Bishop and Bridges constructive compact analysis, the $\SemicontinuousExtremeValueUp$ packet records the finite-net route by which lower-semicontinuous minimum-facing rows and upper-semicontinuous maximum-facing rows are inspected over a compact metric source. It sits over the compact-metric finite-net surface of \autoref{ch:concrete-instances-compactmetric-namecert}, the lower boundary of \autoref{ch:concrete-instances-lowersemicontinuous-namecert}, the upper boundary of \autoref{ch:concrete-instances-uppersemicontinuous-namecert}, the dyadic tolerance rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback rows of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only a displayed comparison certificate; it does not import open-cover compactness, select an extremal point, form a host supremum or infimum, identify quotient real names, or use ambient $\RealUp$ completeness.

\begin{definition}[Semicontinuous extreme-value carrier]
\label{def:semicontinuous-extreme-value-carrier}
A $\SemicontinuousExtremeValueUp$ carrier is a finite $\BHist$ packet
$$
E=(K,L,U,D,S,R,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row, exposing the finite-net centers, radii, and coverage witnesses inspected by the compact-analysis route.
\item $L$ is the $\LowerSemicontinuousUp$ row used by minimum-facing reads through displayed lower-bound and epigraph windows.
\item $U$ is the $\UpperSemicontinuousUp$ row used by maximum-facing reads through displayed upper-bound and sublevel windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger comparing finite-net values, radii, and one-sided error margins.
\item $S$ is the $\StreamNameUp$ schedule row selecting the finite source and value windows attached to the net centers.
\item $R$ is the $\RegSeqRatUp$ readback row for the rational value windows exposed by $S$.
\item $Q$ is the terminal $\RealUp$ seal row for the displayed lower and upper comparison values.
\item $H$ records componentwise $\hsame$ transport for the compact source, semicontinuity rows, dyadic ledger, stream windows, regular rational readbacks, Real seals, replay row, provenance row, and local naming row.
\item $C$ records displayed $\Cont$ replay from an extreme-value request through $K,L,U,D,S,R,Q,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\SemicontinuousExtremeValueUp$, $\CompactMetricUp$, $\LowerSemicontinuousUp$, $\UpperSemicontinuousUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\SemicontinuousExtremeValueUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through the displayed compact finite-net row, one-sided semicontinuity rows, dyadic tolerance ledger, finite stream windows, regular rational readbacks, Real seals, transport, replay, provenance, and local naming. It has no coordinate for selected minimizers or maximizers, open-cover compactness, host suprema or infima, quotient real equality, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Semicontinuous extreme-value NameCert obligations]
\label{thm:semicontinuous-extreme-value-namecert-obligations}
For every accepted $\SemicontinuousExtremeValueUp$ carrier $E=(K,L,U,D,S,R,Q,H,C,P,N)$, the local $\NameCert_{\SemicontinuousExtremeValueUp}$ surface consists exactly of carrier admission, compact finite-net exposure through $K$, lower-semicontinuous minimum-facing exposure through $L$, upper-semicontinuous maximum-facing exposure through $U$, dyadic tolerance comparison through $D$, finite window selection through $S$, regular rational readback through $R$, terminal Real sealing through $Q$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:semicontinuous-extreme-value-carrier}. The compact source, one-sided semicontinuity rows, dyadic ledger, stream schedule, regular readback, Real seal, and structural rows are all named coordinates of the same finite packet.

The lower-facing and upper-facing reads are separated by $L$ and $U$. The lower row supplies finite lower-bound windows for minimum-facing comparisons, while the upper row supplies finite upper-bound windows for maximum-facing comparisons.

The dyadic, stream, regular rational, and Real rows bind those one-sided comparisons to the compact finite-net source. Since the carrier stores no selected extremal point, host supremum or infimum, open-cover compactness theorem, quotient real equality, countable-choice schedule, or ambient completeness row, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Semicontinuous extreme-value finite-net route]
\label{thm:semicontinuous-extreme-value-finite-net-route}
Every extreme-value-facing read exported by an accepted $\SemicontinuousExtremeValueUp$ carrier factors through the finite route
$$
\CompactMetricUp,\ \LowerSemicontinuousUp,\ \UpperSemicontinuousUp,\ \DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp .
$$
The route compares displayed lower and upper value windows over compact finite nets, then seals the comparison data as Real-facing rows. It does not select an extremal point, invoke open-cover compactness, compute a host supremum or infimum, identify quotient real names, use countable choice, or import ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the compact row $K$. It supplies only the displayed finite-net centers, radii, and coverage witnesses that an extreme-value read may inspect.

Attach the one-sided semicontinuity rows. The lower row $L$ gives the minimum-facing lower-window route, and the upper row $U$ gives the maximum-facing upper-window route; neither row selects a point or exports a host order-completeness principle.

Now read the finite comparison data through $D,S,R,Q$. The dyadic ledger sets the tolerance, the stream schedule selects the inspected windows, the regular rational row reads those windows, and the Real row seals the displayed values.

Finally replay and name the same packet through $H,C,P,N$. Because every consumer path has already passed through the listed finite rows, there is no route to open-cover compactness, selected extrema, host suprema or infima, quotient real equality, countable choice, or ambient Real completeness.
\end{proof}

\falsifiablePrediction{An accepted $\SemicontinuousExtremeValueUp$ packet cannot export a minimum-facing or maximum-facing comparison without displaying the compact finite-net row, the appropriate one-sided semicontinuity row, the dyadic tolerance ledger, the StreamName window, the RegSeqRat readback, and the terminal Real seal.}

\independenceWitness{compactmetric, lowersemicontinuous, uppersemicontinuous, dyadicratcore, streamname, regseqrat, real: no listed sibling supplies the full packet $E=(K,L,U,D,S,R,Q,H,C,P,N)$, because each contributes only compact finite nets, one-sided semicontinuity, dyadic tolerance, finite windows, rational readback, or terminal Real sealing while this chapter binds them into one local NameCert carrier.}

\closureat{SemicontinuousExtremeValueUp}{seedStr}

\begin{closurestatus}{\SemicontinuousExtremeValueUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:semicontinuous-extreme-value-carrier}, \autoref{thm:semicontinuous-extreme-value-namecert-obligations}, and \autoref{thm:semicontinuous-extreme-value-finite-net-route} seed the finite compact-analysis route for lower-semicontinuous minimum-facing and upper-semicontinuous maximum-facing comparisons over compact finite nets.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close selected extrema, open-cover compactness, host suprema or infima, quotient real equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking for $\SemicontinuousExtremeValueUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net exposure, lower and upper semicontinuity exposure, dyadic tolerance comparison, StreamName-RegSeqRat-Real readback, transport, replay, provenance, and local naming for BEDC.Derived.SemicontinuousExtremeValueUp.}
  \constructivestory{A $\SemicontinuousExtremeValueUp$ packet is generated from a CompactMetric finite-net row, LowerSemicontinuous and UpperSemicontinuous one-sided rows, a DyadicRatCore tolerance ledger, StreamName windows, RegSeqRat readbacks, a Real seal, componentwise hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
