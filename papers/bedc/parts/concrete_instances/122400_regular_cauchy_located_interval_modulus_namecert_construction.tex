\chapter{A Concrete Naming Certificate for $\RegularCauchyLocatedIntervalModulusUp$}
\label{ch:concrete-instances-regular-cauchy-located-interval-modulus-namecert}
\origin{human}

Following Bishop--Bridges regular real practice, the $\RegularCauchyLocatedIntervalModulusUp$ packet records the finite route by which located interval endpoint data is read together with a regular Cauchy modulus before any Real-facing seal is consumed. It sits beside the locatedness source of \autoref{ch:concrete-instances-cauchy-locatedness-namecert}, the modulus source of \autoref{ch:concrete-instances-realcauchymodulus-namecert}, the stability source of \autoref{ch:concrete-instances-regular-cauchy-modulus-stability-namecert}, the interval source of \autoref{ch:concrete-instances-located-real-interval-namecert}, the stream window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic radius core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only displayed located interval and modulus rows; it does not assert quotient equality of real names, selected representatives, trichotomy, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[RegularCauchyLocatedIntervalModulus carrier]
\label{def:regular-cauchy-located-interval-modulus-carrier}
A $\RegularCauchyLocatedIntervalModulusUp$ carrier is a finite $\BHist$ packet
$$
L=(I,A,B,M,W,Q,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the $\LocatedRealIntervalUp$ interval row, recording the located lower-endpoint and upper-endpoint incidence used by the interval consumer.
\item $A$ and $B$ are the endpoint rows, each read through $\CauchyLocatednessUp$ and tied to the same located interval request.
\item $M$ is the $\RealCauchyModulusUp$ row, recording the finite regular Cauchy modulus budget consumed by the endpoint reads.
\item $W$ is the $\StreamNameUp$ window row through which the endpoint names and modulus request are inspected.
\item $Q$ is the $\RegSeqRatUp$ readback row obtained from the displayed endpoint windows and modulus budget.
\item $D$ is the $\DyadicRatCoreUp$ radius row, carrying the dyadic tolerance and interval-radius comparisons used by the locatedness test.
\item $E$ is the terminal $\RealUp$ seal row, available only after $I,A,B,M,W,Q,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for interval rows, endpoint rows, modulus rows, windows, readback, dyadic radii, Real sealing, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a located-interval modulus request through $I,A,B,M,W,Q,D,E,H$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyLocatedIntervalModulusUp$, $\CauchyLocatednessUp$, $\RealCauchyModulusUp$, $\RegularCauchyModulusStabilityUp$, $\LocatedRealIntervalUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RegularCauchyLocatedIntervalModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,A,B,M,W,Q,D,E,H,C,P,N$. It has no coordinate for host endpoint equality, quotient equality of regular names, selected representatives, trichotomy, countable choice, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[RegularCauchyLocatedIntervalModulus obligations]
\label{thm:regular-cauchy-located-interval-modulus-obligations}
For every accepted $\RegularCauchyLocatedIntervalModulusUp$ carrier
$$
L=(I,A,B,M,W,Q,D,E,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyLocatedIntervalModulusUp}$ surface consists exactly of carrier admission for the located interval row, the two $\CauchyLocatednessUp$ endpoint rows, the $\RealCauchyModulusUp$ regular modulus row, the $\RegularCauchyModulusStabilityUp$ stability dependency, the $\StreamNameUp$ window row, the $\RegSeqRatUp$ readback row, the $\DyadicRatCoreUp$ radius row, the terminal $\RealUp$ seal row, componentwise transport, displayed replay, package provenance, and local naming. Any interval-modulus consumer must read those rows before citing the packet as a located regular Cauchy modulus witness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-located-interval-modulus-carrier}. The interval and endpoint rows are $I,A,B$, and they supply the located interval surface through $\LocatedRealIntervalUp$ and $\CauchyLocatednessUp$.

Read the modulus and radius rows next. Row $M$ supplies the regular Cauchy budget, the stability dependency keeps that budget attached to the same regular read, and row $D$ records the dyadic radius comparisons used by the locatedness test.

The window and readback rows $W,Q$ turn the endpoint and modulus data into a finite regular rational read. The terminal seal $E$ is downstream of that read and is not an ambient completeness theorem.

Finally replay $H,C,P,N$. Componentwise transport, displayed continuation, provenance, and local naming preserve the same finite packet. Since no row stores host endpoint equality, quotient equality, selected representatives, trichotomy, choice, or ambient Real completeness, the listed rows exhaust the local obligation surface.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyLocatedIntervalModulusUp$ read exposes located interval endpoint rows, a regular Cauchy modulus row, StreamName windows, RegSeqRat readback, DyadicRatCore radii, a terminal RealUp seal, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert data before any interval-modulus consumer can cite it.}

\independenceWitness{cauchy-locatedness, realcauchymodulus, regular-cauchy-modulus-stability, located-real-interval, streamname, regseqrat, dyadicratcore, real: $\RegularCauchyLocatedIntervalModulusUp$ is not bijective to any listed sibling because it binds located interval endpoints, regular Cauchy modulus budget, stability dependency, finite windows, regular readback, dyadic radius comparisons, Real sealing, transport, replay, provenance, and local naming in one packet.}

\closureat{RegularCauchyLocatedIntervalModulusUp}{seedStr}

\begin{closurestatus}{\RegularCauchyLocatedIntervalModulusUp}
  \constructivestory{A $\RegularCauchyLocatedIntervalModulusUp$ packet is generated from located interval endpoint rows, a regular Cauchy modulus row, a stability dependency, StreamName windows, RegSeqRat readback, DyadicRatCore radius comparisons, a RealUp seal, hsame transport, Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-located-interval-modulus-carrier}, \autoref{thm:regular-cauchy-located-interval-modulus-obligations}, the falsifiable prediction, and the independence witness seed the finite located-interval regular-modulus route over endpoint, modulus, window, readback, radius, seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Real completeness, quotient equality of regular names, selected representatives, trichotomy, countable choice, host endpoint equality, Lean verification, axiom-clean audit, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, endpoint locatedness, regular Cauchy modulus stability, StreamName window admission, RegSeqRat readback, DyadicRatCore radius exactness, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyLocatedIntervalModulusUp.}
\end{closurestatus}
