\chapter{A Concrete Naming Certificate for $\ContinuationMonadUp$}
\label{ch:concrete-instances-continuationmonad-namecert}
\origin{human}

The $\ContinuationMonadUp$ packet packages the continuation-as-small-step reading as a finite Kleisli-style certificate over existing BEDC rows. It names source histories, target histories, continuation tails, the unit tail, displayed $\Cont$ routes, componentwise $\hsame$ transports, $\Pkg$ provenance, and a local $\NameCert$ ledger. It does not add quotient morphisms, host functions, or a general category primitive.

The packet consumes the authorized generator-boundary discipline of \autoref{ch:concrete-instances-generatorclosure-namecert} and the continuation hom-carrier surface of \autoref{ch:concrete-instances-category-namecert}. Generator closure supplies the refusal of unlisted continuation rules, while $\CategoryUp$ supplies the $\mathsf{CategoryHomCarrier}$ and $\mathsf{ContinuationMorphism}$ reading used in the ledger row.

\begin{definition}[ContinuationMonad NameCert source]
\label{def:continuation-monad-namecert-source}
A $\ContinuationMonadUp$ source is a finite $\BHist$ packet
$$
Z=(A,B,C,f,g,u,H,K,L,N)
$$
with the following displayed coordinates:
\begin{enumerate}
\item $A$, $B$, and $C$ are the source, middle, and target history rows for a two-step continuation read.
\item $f$ is the displayed continuation tail from $A$ to $B$, and $g$ is the displayed continuation tail from $B$ to $C$.
\item $u$ is the displayed unit tail, read as the empty continuation route at a history row.
\item $H$ stores the componentwise $\hsame$ transports for the three history rows, the two non-unit tails, the unit tail, and the displayed composite tail.
\item $K$ stores the displayed $\Cont$ routes for the unit read, the two small-step reads, and their composed Kleisli read.
\item $L$ is the finite ledger recording continuation associativity and unit behaviour as displayed tail-routing facts, together with the $\mathsf{CategoryHomCarrier}$ and $\mathsf{ContinuationMorphism}$ readings used by $\CategoryUp$.
\item $N$ is the local $\NameCert_{\ContinuationMonadUp}$ ledger for carrier admission, classifier stability, ledger exactness, package provenance, and the Kleisli composition boundary.
\end{enumerate}
The packet is a finite naming certificate. It has no quotient morphism coordinate, no host-function coordinate, and no general category object outside $\BHist$.
\end{definition}

\begin{definition}[ContinuationMonad carrier]
\label{def:continuation-monad-carrier}
The concrete $\ContinuationMonadUp$ carrier is the finite $\BHist$ triple surface
$$
\mathcal{K}(A,B,C)=(f,g,u;H,K,L,N)
$$
displayed inside \autoref{def:continuation-monad-namecert-source}. The carrier rows are the source, middle, and target histories $A,B,C$; the two continuation tails $f,g$; the unit tail $u$; the componentwise $\hsame$ row $H$; the $\Cont$ route row $K$; the unit and associativity ledger $L$; and the local $\NameCert_{\ContinuationMonadUp}$ row $N$.

Admission to the carrier means that these rows form the only readable continuation object. In particular, the unit tail is a displayed tail row in the same packet, and the two-step readback is a $\Cont$ ledger route rather than a host Kleisli category.
\end{definition}

\begin{theorem}[ContinuationMonad unit laws]
\label{thm:continuation-monad-unit-laws}
For every accepted carrier $\mathcal{K}(A,B,C)$ of \autoref{def:continuation-monad-carrier}, the displayed unit tail $u$ is left and right neutral for the finite continuation routes recorded in $K$. The left-unit route reads $u$ before $f$ and returns the same $A$-to-$B$ tail row, while the right-unit route reads $f$ before $u$ and returns the same $A$-to-$B$ tail row, both under the componentwise $\hsame$ transports stored in $H$.
\end{theorem}
\begin{proof}
Read the carrier through the route row $K$. The unit route and the two small-step routes are displayed coordinates of the packet, so the left-unit and right-unit reads are finite ledger reads rather than host equalities.

The transport row $H$ supplies the componentwise $\hsame$ comparisons needed to identify the returned tail row with $f$. Since the unit tail $u$ has no coordinate outside the packet, neutrality is exactly the ledger row for the displayed unit route.
\end{proof}

\begin{theorem}[ContinuationMonad associativity]
\label{thm:continuation-monad-associativity}
For every accepted carrier $\mathcal{K}(A,B,C)$ of \autoref{def:continuation-monad-carrier}, the two possible two-step readbacks of $f$ followed by $g$ reassociate to the same displayed composite continuation route. The equality is the associativity row in $L$ transported along $H$ and read through the $\Cont$ row $K$.
\end{theorem}
\begin{proof}
The carrier contains only two non-unit continuation tails and one displayed composite readback. Thus both parenthesized route reads must land in the same $\Cont$ coordinate of $K$.

The ledger row $L$ records that this shared coordinate is the associativity witness for the two-step continuation surface. Transport along $H$ preserves the source, middle, target, and tail rows, so the reassociation remains the same finite ledger fact after componentwise $\hsame$ transport.
\end{proof}

\begin{theorem}[ContinuationMonad NameCert obligations]
\label{thm:continuation-monad-namecert-obligations}
The local $\NameCert_{\ContinuationMonadUp}$ surface for \autoref{def:continuation-monad-carrier} consists of five finite rows: carrier admission for $\mathcal{K}(A,B,C)$; classifier stability under componentwise $\hsame$ transport of histories and tails; unit and associativity ledger exactness for $L$; $\Pkg$ provenance over $\BHist$, $\Cont$, $\hsame$, $\mathsf{CategoryHomCarrier}$, and $\mathsf{ContinuationMorphism}$; and local naming rows for the Kleisli composition handoff.
\end{theorem}
\begin{proof}
Carrier admission is the displayed tuple of \autoref{def:continuation-monad-carrier}. It fixes the readable histories, tails, routes, ledger, and local naming row.

Classifier stability follows from $H$, and the route laws follow from \autoref{thm:continuation-monad-unit-laws} and \autoref{thm:continuation-monad-associativity}. These rows account for the unit and associativity behaviour inside the finite packet.

The remaining provenance rows package the same data for CategoryUp and for the local naming ledger. No quotient Kleisli category, host function, or general category primitive is added by the certificate.
\end{proof}

\begin{theorem}[ContinuationMonad carrier obligation]
\label{thm:continuation-monad-carrier-obligation}
For every accepted $\ContinuationMonadUp$ source packet of \autoref{def:continuation-monad-namecert-source}, carrier admission is exhausted by the rows $A,B,C,f,g,u,H,K,L,N$. The admitted object is the displayed two-step continuation surface: source and target histories, continuation tails, the unit tail, the $\Cont$ routes, componentwise $\hsame$ transports, package provenance, and the local $\NameCert_{\ContinuationMonadUp}$ row.
\end{theorem}
\leanchecked{BEDC.Derived.ContinuationMonadUp.ContinuationMonadCarrier\_route\_closure}
\begin{proof}
Project the packet coordinates. The history rows $A,B,C$ supply the BEDC source, middle, and target data, while $f,g,u$ supply the displayed tails used by the small-step interpretation.

The continuation coordinate $K$ records the only admissible routes: unit, first step, second step, and composed step. The transport coordinate $H$ compares exactly those rows componentwise under $\hsame$.

The ledger and certificate rows $L,N$ bind the package provenance and local naming obligations. Since the definition lists no other coordinates, carrier admission cannot read a quotient morphism, host function, or external category primitive.
\end{proof}

\begin{theorem}[ContinuationMonad classifier stability]
\label{thm:continuation-monad-classifier-stability}
The $\ContinuationMonadUp$ classifier is stable under componentwise $\hsame$ transport of $A,B,C,f,g,u$ and of the displayed composite tail. Transported packets preserve the same unit route, the same two small-step $\Cont$ routes, and the same Kleisli readback recorded by $K$ and $L$.
\end{theorem}
\begin{proof}
Use the componentwise transport row $H$. It transports the three history rows and the displayed tails without changing the shape of the packet.

The continuation routes are preserved because $K$ stores displayed $\Cont$ data rather than host functions. The CategoryUp continuation-morphism reading identifies each transported tail with the same small-step route over the transported endpoints.

Finally the ledger row $L$ records associativity and unit behaviour as continuation-tail routing facts. Repacking those transported rows gives the same local classifier and no access to quotient arrows or host equality.
\end{proof}

\begin{theorem}[ContinuationMonad ledger exactness]
\label{thm:continuation-monad-ledger-exactness}
The finite ledger of an accepted $\ContinuationMonadUp$ packet is exact for the route from the unit tail and the two displayed continuation tails through the composed Kleisli readback. Every ledger entry is one of the unit, associativity, $\Cont$, $\hsame$, $\mathsf{CategoryHomCarrier}$, $\mathsf{ContinuationMorphism}$, $\Pkg$, or local $\NameCert$ rows named in \autoref{def:continuation-monad-namecert-source}.
\end{theorem}
\begin{proof}
Read the source packet. The only dynamic rows are the two displayed tails, the unit tail, and the composed continuation route stored in $K$.

Associativity and unit behaviour are ledger facts about those displayed tails. They are not imported from host functions or quotient arrows; they are the same continuation-tail facts used by the CategoryUp continuation morphism surface.

The package row records provenance for the finite BEDC objects named in the statement. Thus every ledger entry is accounted for by the packet, and no external category or monad equality can enter the certificate.
\end{proof}

\begin{theorem}[ContinuationMonad NameCert package]
\label{thm:continuation-monad-namecert-package}
The local $\NameCert_{\ContinuationMonadUp}$ package consists exactly of five obligation rows: carrier admission, classifier stability under componentwise $\hsame$, ledger exactness for unit and associativity routes, package provenance over $\BHist$, $\Cont$, $\hsame$, $\mathsf{CategoryHomCarrier}$, and $\mathsf{ContinuationMorphism}$, and the Kleisli composition boundary of \autoref{thm:continuation-monad-kleisli-composition-boundary}.
\end{theorem}
\begin{proof}
Carrier admission is supplied by \autoref{thm:continuation-monad-carrier-obligation}. It identifies the finite source packet and excludes all unlisted carrier data.

Classifier stability is supplied by \autoref{thm:continuation-monad-classifier-stability}. Ledger exactness is supplied by \autoref{thm:continuation-monad-ledger-exactness}.

The remaining rows are bookkeeping rows. The $\Pkg$ coordinate records provenance over the named BEDC kernel and CategoryUp surfaces, and the local certificate coordinate records that the Kleisli boundary is the public consumer of the same displayed continuation data.
\end{proof}

\begin{theorem}[ContinuationMonad root generator obligation]
\label{thm:continuation-monad-root-generator-obligation}
Every accepted $\ContinuationMonadUp$ packet receives its unit and bind rows only through the displayed $\CategoryUp$ hom-carrier and the authorized generator boundary. The unit row is the displayed tail $u$, the bind route is the finite composition route through $f,g,K,L$, and both are read after the generator-boundary refusal of unlisted continuation rules.
\end{theorem}
\begin{proof}
Start with carrier admission from \autoref{thm:continuation-monad-carrier-obligation}. The accepted packet exposes the histories $A,B,C$, tails $f,g,u$, transport row $H$, route row $K$, ledger row $L$, and local naming row $N$.

Read the categorical surface through \autoref{ch:concrete-instances-category-namecert}. The $\mathsf{CategoryHomCarrier}$ and $\mathsf{ContinuationMorphism}$ entries used by $L$ are readings of the displayed continuation tails, not independent host morphisms.

Now apply the generator boundary of \autoref{ch:concrete-instances-generatorclosure-namecert}. The unit and bind rows can be consumed only as authorized rows of the displayed packet, while unlisted continuation rules are refused at the generator boundary.
\end{proof}

\begin{theorem}[ContinuationMonad bind-route classifier stability]
\label{thm:continuation-monad-bind-route-classifier-stability}
Componentwise $\hsame$ transport of the unit row, bind route, continuation rows, and package rows of an accepted $\ContinuationMonadUp$ packet preserves the local Kleisli classifier. The transported classifier still reads the same $\CategoryUp$ hom-carrier, the same authorized generator boundary, the same $\Cont$ routes, and the same $\NameCert_{\ContinuationMonadUp}$ ledger.
\end{theorem}
\begin{proof}
Use classifier stability from \autoref{thm:continuation-monad-classifier-stability}. Transport along $H$ preserves the three histories, the unit tail, the two continuation tails, and the displayed composite tail.

Use ledger exactness from \autoref{thm:continuation-monad-ledger-exactness}. The bind route remains a row of $K$ and $L$, so its category-facing reading is still the $\mathsf{CategoryHomCarrier}$ and $\mathsf{ContinuationMorphism}$ surface described in \autoref{ch:concrete-instances-category-namecert}.

The generator boundary remains local because the transported route has no coordinate for an unlisted continuation rule. Repacking the transported rows with $\Pkg$ provenance and the local NameCert row gives the same Kleisli classifier.
\end{proof}

\begin{theorem}[ContinuationMonad NameCert ledger exactness]
\label{thm:continuation-monad-namecert-ledger-exactness}
Every consumer-visible continuation-monad route is exhausted by the carrier, classifier, unit, bind, associativity, $\Cont$, $\Pkg$, and $\NameCert$ rows of the accepted $\ContinuationMonadUp$ packet. No consumer-visible route can bypass the displayed $\CategoryUp$ hom-carrier or the authorized generator boundary.
\end{theorem}
\begin{proof}
Project the packet using \autoref{thm:continuation-monad-namecert-package}. The local NameCert package consists of carrier admission, classifier stability, ledger exactness, package provenance, and the Kleisli composition boundary.

The unit and associativity rows are supplied by \autoref{thm:continuation-monad-unit-laws} and \autoref{thm:continuation-monad-associativity}. The bind route is the displayed two-step readback through $f,g,K,L$ and is already bounded by the Kleisli composition surface.

Finally combine \autoref{ch:concrete-instances-category-namecert} with \autoref{ch:concrete-instances-generatorclosure-namecert}. The category-facing hom-carrier and the authorized generator boundary account for every public route into the packet, so no host function, quotient Kleisli arrow, or unlisted generator rule remains visible.
\end{proof}

\begin{theorem}[ContinuationMonad root-unblock source triad]
\label{thm:continuation-monad-root-unblock-source-triad}
Every accepted $\ContinuationMonadUp$ packet must expose the source history $A$, middle history $B$, target history $C$, continuation tails $f,g$, and unit tail $u$ together before any Kleisli read is available. The first readable root surface is therefore the finite triad-and-tail packet $A,B,C,f,g,u,H,K,L,N$, not an external function space or quotient Kleisli category.
\end{theorem}
\begin{proof}
Project carrier admission from \autoref{thm:continuation-monad-carrier-obligation}. The accepted packet displays the three histories, the two continuation tails, the unit tail, componentwise transports, route rows, ledger rows, and the local name as one finite carrier.

Use ledger exactness from \autoref{thm:continuation-monad-namecert-ledger-exactness}. A Kleisli read is consumer-visible only through the carrier, classifier, unit, bind, associativity, $\Cont$, $\Pkg$, and $\NameCert$ rows of that same packet.

Thus the read cannot begin from a host function, an external quotient, or a partial tail. The root surface is the displayed source, middle, and target triad together with $f,g,u,H,K,L,N$.
\end{proof}

\begin{theorem}[ContinuationMonad root-unblock bind totality]
\label{thm:continuation-monad-root-unblock-bind-totality}
Every bind-route consumer of an accepted $\ContinuationMonadUp$ packet factors through the displayed $\Cont$ row $K$, the componentwise $\hsame$ row $H$, and the ledger row $L$. The bind route is total only as a read of $A,B,C,f,g,u,H,K,L,N$ and is not host function composition, host monad equality, or a quotient Kleisli arrow.
\end{theorem}
\begin{proof}
Start with a bind-route consumer and project the packet rows. The only displayed route row is $K$, the only componentwise transport row is $H$, and the consumer-visible accounting row is $L$.

Use \autoref{thm:continuation-monad-bind-route-classifier-stability}. Transport along $H$ preserves the local Kleisli classifier and keeps the route on the same displayed histories and continuation tails.

Use \autoref{thm:continuation-monad-namecert-ledger-exactness} to close the readback. Since every consumer-visible continuation-monad route is exhausted by the displayed rows, the bind route is total precisely through $K,H,L$ inside the finite packet and has no host-composition coordinate.
\end{proof}

\begin{theorem}[ContinuationMonad root-unblock downstream package]
\label{thm:continuation-monad-root-unblock-downstream-package}
The source triad of \autoref{thm:continuation-monad-root-unblock-source-triad} and the bind totality of \autoref{thm:continuation-monad-root-unblock-bind-totality} form the downstream root-unblock package consumed by the CategoryUp surface of \autoref{ch:concrete-instances-category-namecert} and the generator-boundary surface of \autoref{ch:concrete-instances-generatorclosure-namecert}. Downstream consumers receive exactly $A,B,C,f,g,u,H,K,L,N$ together with the displayed category and generator readings; they receive no host function composition, quotient Kleisli category, or unlisted continuation rule.
\end{theorem}
\begin{proof}
First expose the source packet. \autoref{thm:continuation-monad-root-unblock-source-triad} supplies the three histories, two continuation tails, unit tail, transports, routes, ledger, and local name before any Kleisli read.

Next expose the bind route. \autoref{thm:continuation-monad-root-unblock-bind-totality} forces every bind consumer through $K,H,L$ inside the same finite packet.

Finally read the downstream surfaces. The CategoryUp chapter reads the displayed hom-carrier and continuation-morphism rows, while the generator-boundary chapter refuses unlisted continuation rules. Together with ledger exactness, these readings package the root-unblock surface without exporting host function composition, quotient Kleisli categories, or extra continuation generators.
\end{proof}

\begin{theorem}[ContinuationMonad Kleisli composition boundary]
\label{thm:continuation-monad-kleisli-composition-boundary}
Every Kleisli-style consumer of an accepted $\ContinuationMonadUp$ packet factors through the displayed route
$$
A \xrightarrow{\,f\,} B \xrightarrow{\,g\,} C
$$
together with the unit tail $u$, the componentwise $\hsame$ transports, the $\Cont$ routes in $K$, the ledger $L$, package provenance, and the local naming row $N$. The boundary exports no quotient Kleisli category, no host function composition, no standard monad laws as host equalities, and no general category primitive.
\end{theorem}
\begin{proof}
Start from the consumer route. By carrier admission, it can inspect only the displayed histories, tails, transports, continuations, ledger, package provenance, and local certificate.

Use classifier stability to move along componentwise $\hsame$ without changing the route. The displayed two-step path remains the route through $f$ and $g$, with $u$ serving only as the unit tail inside the same continuation ledger.

Use ledger exactness to identify the composed readback with the continuation route recorded in $K$ and $L$. Therefore a Kleisli-style consumer is bounded by the named BEDC rows and cannot read quotient Kleisli categories, host functions, host monad equalities, or a general category primitive.
\end{proof}

\begin{theorem}[ContinuationMonad Kleisli composite scope]
\label{thm:continuation-monad-kleisli-composite-scope}
For an accepted $\ContinuationMonadUp$ packet, any composite consumer that reads
$$
A \xrightarrow{\,f\,} B \xrightarrow{\,g\,} C
$$
must factor through the displayed tails $f,g,u$, the $\Cont$ route row $K$, the componentwise $\hsame$ row $H$, the ledger row $L$, the $\mathsf{CategoryHomCarrier}$ and $\mathsf{ContinuationMorphism}$ readings, $\Pkg$ provenance, and the local $\NameCert_{\ContinuationMonadUp}$ row $N$. The consumer cannot replace this finite composite by host function composition, quotient Kleisli arrows, or a general category primitive.
\end{theorem}
\begin{proof}
Begin with carrier admission. The accepted packet exposes only the histories $A,B,C$, the tails $f,g,u$, the transport row $H$, the continuation route row $K$, the ledger row $L$, package provenance, and the local name.

Use classifier stability to move through the componentwise $\hsame$ row without changing the displayed two-step route. The $\mathsf{CategoryHomCarrier}$ and $\mathsf{ContinuationMorphism}$ readings are therefore readings of those same continuation rows, not additional morphism objects.

Apply ledger exactness and the Kleisli composition boundary. They identify the composite readback with the recorded $K,L$ route and confine every consumer of the composite to the displayed BEDC packet.

Since no coordinate names host function composition, quotient Kleisli arrows, or a general category primitive, the composite scope is exhausted by the listed rows.
\end{proof}

\begin{theorem}[ContinuationMonad scoped route dependencies]
\label{thm:continuation-monad-scoped-route-dependencies}
For every accepted $\ContinuationMonadUp$ packet, the composite consumer route factors through exactly the displayed $\BHist$, $\Cont$, $\hsame$, $\mathsf{CategoryHomCarrier}$, $\mathsf{ContinuationMorphism}$, $\Pkg$, and $\NameCert$ rows. The dependency surface is the finite route through the histories $A,B,C$, continuation tails $f,g,u$, componentwise transports, continuation ledger, package provenance, and local naming row.
\end{theorem}
\begin{proof}
Start with carrier obligation. It projects the accepted packet to the histories, tails, transport row, continuation route row, ledger, provenance, and local certificate.

Use classifier stability and ledger exactness to keep every read of the composite consumer on those rows. The $\mathsf{CategoryHomCarrier}$ and $\mathsf{ContinuationMorphism}$ readings are then readings of the same displayed continuation route, not independent categorical data.

Apply the Kleisli boundary and Kleisli composite scope. Together they identify the composite as the finite route through the listed BEDC rows and exhaust the scoped dependencies of the accepted packet.
\end{proof}

\begin{theorem}[ContinuationMonad scope non-escape]
\label{thm:continuation-monad-scope-nonescape}
For every accepted $\ContinuationMonadUp$ packet, the scoped composite route exports no quotient Kleisli arrows, no host function composition, and no general category primitive. It exports only the displayed continuation tails, $\Cont$ route rows, componentwise $\hsame$ transports, ledger row, package provenance, and local $\NameCert$ row.
\end{theorem}
\begin{proof}
Project the same packet using carrier obligation. The visible rows are finite continuation data, transport data, ledger data, package provenance, and local naming data.

Classifier stability and ledger exactness keep transport and readback inside that finite route. The Kleisli composition boundary excludes quotient Kleisli arrows, host function composition, and general category primitives from the public boundary.

The Kleisli composite scope supplies the final scope claim: every composite consumer has already factored through the displayed rows, so there is no exported object beyond the scoped route.
\end{proof}

\begin{theorem}[ContinuationMonad route readback determinacy]
\label{thm:continuation-monad-route-readback-determinacy}
For an accepted $\ContinuationMonadUp$ packet $Z=(A,B,C,f,g,u,H,K,L,N)$, the displayed $\Cont$ route row determines the unit readback, first-step readback, second-step readback, and composite readback under componentwise $\hsame$ transport. Any consumer reading those four results must use the same rows $u,f,g,H,K,L,N$ and receives no second host route, host function composition, quotient Kleisli arrow, or host monad equality.
\end{theorem}
\begin{proof}
Start with carrier obligation and classifier stability. Carrier admission supplies the displayed tails $f,g,u$ and the route row $K$, while classifier stability keeps componentwise $\hsame$ transport on the same histories and continuation tails.

Use ledger exactness to read the unit, first step, second step, and composite from $K$ and $L$. The ledger records the readbacks as finite continuation data attached to $Z$, not as host functions or quotient arrows.

Finally apply the Kleisli composition boundary and scope non-escape. The only route available to a public consumer is the displayed $\Cont$ route row, so no second host route or host monad equality can be projected.
\end{proof}

\begin{theorem}[ContinuationMonad route readback totality]
\label{thm:continuation-monad-route-readback-totality}
For an accepted $\ContinuationMonadUp$ packet $Z=(A,B,C,f,g,u,H,K,L,N)$, every public readback of the route is one of the displayed unit, first-step, second-step, or composite rows recorded by $K$, $L$, and $N$. The readback surface is exhausted by the histories $A,B,C$, tails $u,f,g$, componentwise transport $H$, continuation route row $K$, ledger row $L$, and local naming row $N$.
\end{theorem}
\begin{proof}
Start with the carrier obligation. It projects the accepted packet to exactly $A,B,C,f,g,u,H,K,L,N$, so a public route read has no source coordinate outside the displayed continuation packet.

Use classifier stability and ledger exactness to keep the unit, first-step, second-step, and composite reads on $H$, $K$, and $L$. These rows record the route as finite continuation data rather than as host functions.

Finally apply \autoref{thm:continuation-monad-kleisli-composition-boundary}, \autoref{thm:continuation-monad-scoped-route-dependencies}, and \autoref{thm:continuation-monad-route-readback-determinacy}. The boundary supplies the finite Kleisli-facing route, the dependency theorem restricts all consumers to the listed BEDC rows, and determinacy says the four displayed readbacks exhaust the public route.
\end{proof}

\begin{theorem}[ContinuationMonad unit-composite readback totality]
\label{thm:continuation-monad-unit-composite-readback-totality}
For an accepted $\ContinuationMonadUp$ packet $Z=(A,B,C,f,g,u,H,K,L,N)$, unit-law consumers and composite-law consumers are jointly exhausted by $u,f,g,H,K,L,N$. They may read the displayed unit row, the two displayed step rows, the displayed composite row, componentwise $\hsame$ transport, the continuation ledger, and the local $\NameCert_{\ContinuationMonadUp}$ row, but they cannot request host monad equalities.
\end{theorem}
\begin{proof}
Project the packet by carrier obligation. The only unit input is the tail $u$, and the only composite inputs are the tails $f$ and $g$ together with the route row $K$.

Apply classifier stability and ledger exactness. Stability keeps all unit and composite reads under the same transport row $H$, while exactness records their readbacks through $K$ and $L$.

Now use \autoref{thm:continuation-monad-kleisli-composition-boundary}, \autoref{thm:continuation-monad-scoped-route-dependencies}, and \autoref{thm:continuation-monad-route-readback-determinacy}. The boundary excludes host monad equality, the scoped dependencies leave only $u,f,g,H,K,L,N$, and determinacy identifies any public unit-law or composite-law read with one of those displayed rows.
\end{proof}

\begin{theorem}[ContinuationMonad category-boundary readback totality]
\label{thm:continuation-monad-category-boundary-readback-totality}
For an accepted $\ContinuationMonadUp$ packet $Z=(A,B,C,f,g,u,H,K,L,N)$, the $\mathsf{CategoryHomCarrier}$ and $\mathsf{ContinuationMorphism}$ handoff is total exactly on the same finite route rows $A,B,C,f,g,u,H,K,L,N$. A category-boundary consumer receives only the displayed $\BHist$, $\Cont$, $\hsame$, $\Pkg$, and $\NameCert$ surface of that packet, with no independent category object, host function, quotient Kleisli arrow, or host monad equality.
\end{theorem}
\begin{proof}
First use the carrier obligation to expose the route rows $A,B,C,f,g,u,H,K,L,N$. The category-facing names are attached to those histories, tails, transport, route, ledger, provenance, and naming rows.

Classifier stability and ledger exactness identify the $\mathsf{CategoryHomCarrier}$ and $\mathsf{ContinuationMorphism}$ readings with the same displayed continuation data. No category-boundary read can leave the transport row $H$ or the ledger rows $K$ and $L$.

Finally apply \autoref{thm:continuation-monad-kleisli-composition-boundary}, \autoref{thm:continuation-monad-scoped-route-dependencies}, and \autoref{thm:continuation-monad-route-readback-determinacy}. The category boundary is therefore total precisely on the finite route readbacks already determined by $Z$, and every excluded host or quotient object remains outside the public handoff.
\end{proof}

\begin{theorem}[ContinuationMonad unit route non-escape]
\label{thm:continuation-monad-unit-route-nonescape}
Every public unit-law read of an accepted $\ContinuationMonadUp$ packet factors through the unit tail $u$, componentwise transport row $H$, continuation route row $K$, ledger row $L$, and local $\NameCert_{\ContinuationMonadUp}$ row $N$. The read may expose the finite $\BHist$, $\Cont$, $\hsame$, $\Pkg$, and $\NameCert$ rows of the packet, but it cannot export host monad equality, a host identity function, or a quotient Kleisli unit.
\end{theorem}
\begin{proof}
Use carrier obligation to project the unit tail $u$ and the visible package rows. The unit law is therefore attached to the same finite packet as the non-unit continuation tails.

Classifier stability keeps transport through $H$, and ledger exactness records the unit read through $K$ and $L$. The public read is a ledger read of the displayed route, not a host equality proof.

The local NameCert package and Kleisli composition boundary give the non-escape conclusion. Since the accepted packet contains no host monad equality coordinate, no host identity-function coordinate, and no quotient Kleisli unit coordinate, a public unit-law consumer cannot export any of them.
\end{proof}

\begin{theorem}[ContinuationMonad category handoff exactness]
\label{thm:continuation-monad-category-handoff-exactness}
For every accepted $\ContinuationMonadUp$ packet, the $\mathsf{CategoryHomCarrier}$ and $\mathsf{ContinuationMorphism}$ readings consume exactly the displayed continuation tails $f,g,u$ together with the $\Cont$ route row $K$, componentwise $\hsame$ row $H$, ledger row $L$, package provenance, and local naming row. They do not consume quotient Kleisli arrows, host functions, host monad equalities, or an independent category object.
\end{theorem}
\begin{proof}
Read the handoff through carrier obligation. The only category-facing rows exposed by the packet are the histories, tails, transport, continuation route, ledger, provenance, and local name.

Use classifier stability and ledger exactness to align the $\mathsf{CategoryHomCarrier}$ and $\mathsf{ContinuationMorphism}$ readings with the same $f,g,u,H,K,L,N$ route. The category-facing names therefore read the continuation packet rather than a separate host category.

Apply the Kleisli composition boundary and scope non-escape. Together they rule out quotient Kleisli arrows, host functions, host monad equalities, and independent category objects as sources for the handoff.
\end{proof}

\begin{theorem}[ContinuationMonad obligation closure]
\label{thm:continuation-monad-obligation-closure}
For every accepted $\ContinuationMonadUp$ source packet, the carrier source, unit laws, associativity, classifier stability, ledger exactness, local $\NameCert$ package, and Kleisli composition boundary jointly supply the $\obligationClosure$ surface for $\ContinuationMonadUp$ over $\BHist$, $\Cont$, $\hsame$, $\mathsf{CategoryHomCarrier}$, $\mathsf{ContinuationMorphism}$, $\Pkg$, and $\NameCert$. The closure surface exports only the displayed continuation tails, route rows, transport rows, ledger rows, package provenance, and naming row.
\end{theorem}
\begin{proof}
First use the carrier source and carrier obligation. They identify the finite packet rows $A,B,C,f,g,u,H,K,L,N$ and exclude quotient morphisms, host functions, and any general category primitive outside the displayed packet.

Next apply the unit laws and associativity rows. These bind the unit tail and the two-step readback as ledger facts about the displayed $\Cont$ routes rather than as host monad equalities.

Use classifier stability and ledger exactness to transport the same route data along componentwise $\hsame$. The $\mathsf{CategoryHomCarrier}$ and $\mathsf{ContinuationMorphism}$ readings are therefore readings of the same finite continuation rows.

Finally apply the local NameCert package and the Kleisli composition boundary. They repack carrier admission, stability, exactness, provenance, and consumer boundary as the public obligation surface for $\ContinuationMonadUp$.
\end{proof}

\begin{definition}[ContinuationMonad kernel scope row]
\label{def:continuation-monad-kernel-scope-row}
The $\ContinuationMonadUp$ kernel scope row is the finite list
$$
\BHist,\quad \Cont,\quad \hsame,\quad \mathsf{CategoryHomCarrier},\quad \mathsf{ContinuationMorphism},\quad \Pkg,\quad \NameCert.
$$
For a packet $Z=(A,B,C,f,g,u,H,K,L,N)$, every public continuation read must name its source, middle, and target histories, displayed tails, unit tail, transport row, route row, ledger row, package provenance, and local naming row through this list.
\end{definition}

\begin{theorem}[ContinuationMonad route row grounding]
\label{thm:continuation-monad-route-row-grounding}
Every route consumer of an accepted $\ContinuationMonadUp$ packet factors through the finite route data
$$
A,B,C,f,g,u,H,K,L,N.
$$
The histories $A,B,C$, tails $f,g,u$, componentwise transport row $H$, continuation route row $K$, ledger row $L$, and local naming row $N$ are therefore the complete grounding surface for the kernel scope of \autoref{def:continuation-monad-kernel-scope-row}.
\end{theorem}
\begin{proof}
Project the accepted source packet. The carrier source lists exactly $A,B,C,f,g,u,H,K,L,N$, and the kernel scope row lists the only BEDC objects through which those coordinates can be read.

Use carrier obligation and classifier stability to bind all transported reads to $H$ and all continuation reads to $K$. The CategoryUp hom-carrier and continuation-morphism readings are readings of the same displayed tails, not additional route coordinates.

Apply ledger exactness and the Kleisli composition boundary. The ledger $L$ and local name $N$ package the same finite route for downstream consumers, so no consumer can factor through a quotient Kleisli arrow, host function, or unlisted categorical primitive.
\end{proof}

\begin{theorem}[ContinuationMonad downstream unblock surface]
\label{thm:continuation-monad-downstream-unblock-surface}
The scoped downstream unblock surface for $\ContinuationMonadUp$ consists of carrier admission for $A,B,C,f,g,u,H,K,L,N$, classifier stability under $H$, ledger exactness through $K$ and $L$, package provenance through $\Pkg$, and the Kleisli boundary recorded by $N$. Any downstream Real, completion, category, or unary-continuation consumer must use those rows and cannot replace them by quotient morphisms, host functions, or external monad laws.
\end{theorem}
\begin{proof}
Carrier admission supplies the finite continuation packet. Classifier stability then keeps transported histories and tails inside the componentwise $\hsame$ row $H$.

Ledger exactness supplies the unit, associativity, and composed-read facts through $K$ and $L$. These facts are finite continuation rows, not host monad equalities.

Finally the package provenance and Kleisli boundary expose exactly the rows named in the kernel scope. Downstream consumers may depend on that surface, but they receive no route outside $A,B,C,f,g,u,H,K,L,N$.
\end{proof}

\begin{theorem}[ContinuationMonad route bridge boundary]
\label{thm:continuation-monad-route-bridge-boundary}
For every accepted $\ContinuationMonadUp$ packet
$$
Z=(A,B,C,f,g,u,H,K,L,N),
$$
the bridge from the displayed finite route
$$
A \xrightarrow{\,f\,} B \xrightarrow{\,g\,} C
$$
to the standard monad-reading boundary is mediated only by the rows $\BHist$, $\Cont$, $\hsame$, $\mathsf{CategoryHomCarrier}$, $\mathsf{ContinuationMorphism}$, $\Pkg$, and $\NameCert$. The boundary exports no quotient Kleisli arrow, no host function composition, and no host monad equality.
\end{theorem}
\begin{proof}
Begin with the public package of \autoref{thm:continuation-monad-namecert-package}. It supplies carrier admission, classifier stability, ledger exactness, package provenance, and the Kleisli boundary for the same packet $Z$.

Apply \autoref{thm:continuation-monad-kleisli-composition-boundary} to the displayed two-step route. The standard monad-reading boundary can see only the finite continuation route named by $A,B,C,f,g,u,H,K,L,N$.

Finally use \autoref{thm:continuation-monad-scoped-route-dependencies}. The CategoryUp hom-carrier and continuation-morphism readings are readings of the same $\BHist$, $\Cont$, and $\hsame$ rows, so no quotient arrow, host composition, or host monad equality is exported.
\end{proof}

\begin{theorem}[ContinuationMonad finite standard handoff]
\label{thm:continuation-monad-finite-standard-handoff}
The public $\ContinuationMonadUp$ handoff to standard monad notation is finite: the source, middle, and target histories are $A,B,C$; the displayed non-unit tails are $f,g$; the unit tail is $u$; the transport, route, ledger, and naming rows are $H,K,L,N$. Every standard-looking unit or composite read is a reading of these rows through $\BHist$, $\Cont$, $\hsame$, $\mathsf{CategoryHomCarrier}$, $\mathsf{ContinuationMorphism}$, $\Pkg$, and $\NameCert$, not a host-side monad structure.
\end{theorem}
\begin{proof}
Use \autoref{thm:continuation-monad-kleisli-composite-scope}. It identifies every composite consumer with the displayed tails, route row, transport row, ledger row, CategoryUp readings, package provenance, and local certificate.

Next apply \autoref{thm:continuation-monad-scoped-route-dependencies}. The standard notation has no separate carrier once the dependency surface has been restricted to the named BEDC rows.

The public package of \autoref{thm:continuation-monad-namecert-package} supplies the carrier, stability, exactness, provenance, and boundary obligations for this handoff. Hence the handoff is finite and contains no host monad equality, host function composition, or quotient Kleisli arrow.
\end{proof}

\begin{theorem}[ContinuationMonad public route bridge totality]
\label{thm:continuation-monad-public-route-bridge-totality}
The public $\ContinuationMonadUp$ route bridge is total on the displayed packet $Z=(A,B,C,f,g,u,H,K,L,N)$. Every downstream Real, completion, category, or unary-continuation consumer that asks for the standard monad-reading boundary receives exactly the finite route surface over $\BHist$, $\Cont$, $\hsame$, $\mathsf{CategoryHomCarrier}$, $\mathsf{ContinuationMorphism}$, $\Pkg$, and $\NameCert$; no consumer receives an extra quotient Kleisli arrow, host function composition, or host monad equality.
\end{theorem}
\begin{proof}
First apply \autoref{thm:continuation-monad-downstream-unblock-surface}. It states that each downstream consumer must use carrier admission, classifier stability, ledger exactness, package provenance, and the Kleisli boundary of the same packet.

Then use \autoref{thm:continuation-monad-route-bridge-boundary}. The route bridge has already identified the only rows through which the standard boundary is readable.

Finally apply \autoref{thm:continuation-monad-finite-standard-handoff}. Since the handoff is finite and contains no host-side monad structure, totality holds for all displayed downstream consumers exactly on the route rows $A,B,C,f,g,u,H,K,L,N$.
\end{proof}

\begin{theorem}[ContinuationMonad bridge package]
\label{thm:continuation-monad-bridge-package}
The public $\ContinuationMonadUp$ packet forms a bridge package from its displayed finite route
$$
A \xrightarrow{\,f\,} B \xrightarrow{\,g\,} C
$$
and unit tail $u$ to the standard monad-reading boundary. The package consists precisely of the $\BHist$ rows $A,B,C$, the $\Cont$ route row $K$, the componentwise $\hsame$ row $H$, the $\mathsf{CategoryHomCarrier}$ and $\mathsf{ContinuationMorphism}$ readings, the $\Pkg$ provenance row, and the $\NameCert_{\ContinuationMonadUp}$ row $N$. It exports no quotient Kleisli arrow, no host function composition, and no host monad equality.
\end{theorem}
\begin{proof}
Start with \autoref{thm:continuation-monad-namecert-package}. It supplies the public NameCert package containing carrier admission, classifier stability, ledger exactness, package provenance, and the Kleisli composition boundary.

Use \autoref{thm:continuation-monad-kleisli-composition-boundary} and \autoref{thm:continuation-monad-kleisli-composite-scope} to bind the standard-looking composite read to the displayed route through $f$ and $g$, with $u$ as the unit tail inside the same packet.

Apply \autoref{thm:continuation-monad-scoped-route-dependencies} to restrict every dependency to $\BHist$, $\Cont$, $\hsame$, $\mathsf{CategoryHomCarrier}$, $\mathsf{ContinuationMorphism}$, $\Pkg$, and $\NameCert$.

Finally use \autoref{thm:continuation-monad-downstream-unblock-surface} and \autoref{thm:continuation-monad-public-route-bridge-totality}. They show that downstream consumers receive the complete bridge package and no quotient Kleisli arrow, host function composition, or host monad equality.
\end{proof}

\begin{theorem}[ContinuationMonad bind-route coverage]
\label{thm:continuation-monad-bind-route-coverage}
Every accepted bind read of a $\ContinuationMonadUp$ packet factors through the displayed source $\BHist$ row, the continuation route row, the Kleisli composite row, the componentwise $\hsame$ transport, the $\Pkg$ provenance row, and the local $\NameCert$ row. Thus a bind consumer can inspect only the rows $A,B,C,f,g,u,H,K,L,N$ named by the bridge package, where $A,B,C$ are the $\BHist$ sources, $K$ is the $\Cont$ route, $L$ is the composite ledger, $H$ is the $\hsame$ transport, and $N$ is the local $\NameCert$ certificate with $\Pkg$ provenance.
\end{theorem}
\begin{proof}
Begin with the unit laws and associativity rows. They identify the accepted bind read as a replay of the displayed unit tail and the two-step Kleisli composite rather than as a host monad operation.

Next use the scoped route dependencies. The only histories visible to the read are the $\BHist$ rows $A,B,C$, and the only continuation data are the displayed $\Cont$ route $K$ and ledger $L$.

Transport is then forced through the componentwise $\hsame$ row $H$. The route bridge boundary and bridge package attach the same read to $\Pkg$ provenance and the local $\NameCert$ row $N$, so no hidden evaluator, quotient Kleisli arrow, or host function can supply an additional bind route.
\end{proof}

\begin{theorem}[ContinuationMonad Kleisli ledger associativity]
\label{thm:continuation-monad-kleisli-ledger-associativity}
Two nested Kleisli composites in an accepted $\ContinuationMonadUp$ packet have the same consumer-visible ledger exactly when both composites are replayed through the existing associativity row and the same scoped-route dependencies over $\BHist$, $\Cont$, $\hsame$, $\Pkg$, and $\NameCert$. The comparison is a ledger comparison inside $L$, not an equality supplied by a hidden evaluator or by host-side monad laws.
\end{theorem}
\begin{proof}
For the forward direction, read the common consumer-visible ledger through the bridge package. The package exposes the source $\BHist$ rows, the $\Cont$ route row, the $\hsame$ transport, the $\Pkg$ provenance row, and the local $\NameCert$ row, so the common ledger must be the displayed row $L$.

Apply the associativity row to replay the two nested composites against that same row $L$. The scoped route dependencies keep both replays inside the route rows $A,B,C,f,g,u,H,K,L,N$.

For the reverse direction, assume both nested composites replay through associativity and the scoped-route dependencies. Since the route bridge boundary exports only the displayed ledger and no host evaluator, the two consumer-visible ledgers coincide exactly as reads of $L$.
\end{proof}

\begin{theorem}[ContinuationMonad downstream consumer exhaustion]
\label{thm:continuation-monad-downstream-consumer-exhaustion}
Any downstream CategoryUp or generator-closure consumer of the $\ContinuationMonadUp$ bridge package can inspect only the unit, bind, route, ledger, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows already displayed in this chapter. In particular, every such consumer is exhausted by the rows $u,f,g,K,L,H,N$ over the source $\BHist$ rows $A,B,C$ with $\Pkg$ provenance and receives no quotient Kleisli category, ambient evaluator, host function composition, or extra monad law.
\end{theorem}
\begin{proof}
First expose the consumer through the bridge package. The package has already bound the standard-looking route to the displayed $\BHist$, $\Cont$, $\hsame$, $\Pkg$, and $\NameCert$ rows.

Next apply the unit laws, associativity, scoped route dependencies, and route bridge boundary. These rows supply the unit read, bind read, route replay, and ledger comparison by finite continuation data.

The downstream CategoryUp and generator-closure consumers therefore have no remaining coordinate to inspect. Any extra evaluator, quotient Kleisli category, host function composition, or independent monad law would have to be a row outside $A,B,C,f,g,u,H,K,L,N$ and the $\Pkg$ provenance surface, which the bridge package does not export.
\end{proof}

\begin{theorem}[ContinuationMonad generator-category consumer triad]
\label{thm:continuation-monad-generator-category-consumer-triad}
For an accepted $\ContinuationMonadUp$ packet
$Z=(A,B,C,f,g,u,H,K,L,N)$, every downstream consumer that jointly reads the
generator-closure refusal surface and the $\CategoryUp$ hom route factors
through exactly the displayed rows
$$
A,B,C,f,g,u,H,K,L,N,\quad \BHist,\quad \Cont,\quad \hsame,\quad \Pkg,\quad
\NameCert,\quad \mathsf{CategoryHomCarrier},\quad
\mathsf{ContinuationMorphism}.
$$
The triad consists of the source histories $A,B,C$, the continuation tails
$f,g,u$ with route and ledger rows $K,L$, and the local transport, package, and
name rows $H,N$. It exports no quotient Kleisli arrow, host function, or host
monad equality.
\end{theorem}
\begin{proof}
Project the carrier $Z$. The histories $A,B,C$ and tails $f,g,u$ are the only
source and route rows available to a downstream reader.

Route the CategoryUp read through the displayed
$\mathsf{CategoryHomCarrier}$ and $\mathsf{ContinuationMorphism}$ entries in
$L$. These entries read the same finite continuation tails and do not introduce
an independent category object.

Route the generator consumer through the refusal surface named by the packet.
The refusal surface admits the listed continuation rules and rejects any
unlisted continuation rule before the consumer reaches $K,L,H,N$.

Finally combine the package row with the local $\NameCert$ row. Since no
coordinate outside $A,B,C,f,g,u,H,K,L,N$ is exported, quotient Kleisli arrows,
host functions, and host monad equalities have no consumer-facing route.
\end{proof}

\begin{theorem}[ContinuationMonad generator refusal row]
\label{thm:continuation-monad-generator-refusal-row}
For an accepted $\ContinuationMonadUp$ packet
$Z=(A,B,C,f,g,u,H,K,L,N)$, every generator-closure consumer of the unit or bind
surface reads the authorized continuation rows $f,g,u,K,L$ through the same
refusal ledger before it can inspect $\Pkg$ provenance or the local
$\NameCert$ row. Any requested continuation rule not displayed by
$A,B,C,f,g,u,H,K,L,N$ is refused at that ledger and cannot be supplied by a
quotient Kleisli arrow, host function, or host monad equality.
\end{theorem}
\begin{proof}
Start from the displayed unit and bind rows. The unit tail is $u$, the non-unit
tails are $f,g$, and the accepted route and ledger are $K,L$.

Apply the generator-closure refusal surface. It separates authorized displayed
continuation rows from unlisted continuation rules before the route reaches the
package and local naming coordinates.

Use componentwise $\hsame$ transport only through $H$. The transport preserves
the same refusal ledger and does not add a route for a hidden evaluator or a
quotient Kleisli arrow.

The $\Pkg$ and $\NameCert$ rows therefore certify exactly the displayed
generator-facing surface, with no host function or host monad equality available
to replace the finite route.
\end{proof}

\begin{theorem}[ContinuationMonad Category hom route totality]
\label{thm:continuation-monad-category-hom-route-totality}
For an accepted $\ContinuationMonadUp$ packet
$Z=(A,B,C,f,g,u,H,K,L,N)$, every $\CategoryUp$ consumer of
$\mathsf{CategoryHomCarrier}$ or $\mathsf{ContinuationMorphism}$ is total on the
displayed route $A,B,C,f,g,u,H,K,L,N$. The consumer reads the hom surface only
through $\BHist$, $\Cont$, $\hsame$, $\Pkg$, $\NameCert$, the
$\mathsf{CategoryHomCarrier}$ row, and the $\mathsf{ContinuationMorphism}$ row,
and it receives no independent category object, quotient Kleisli arrow, host
function, or host monad equality.
\end{theorem}
\begin{proof}
Project the CategoryUp-facing carrier rows. The hom carrier and continuation
morphism readings are stored in the same finite ledger $L$ that records the
accepted route through $f,g,u$ and $K$.

Use classifier stability to transport this read through $H$. The transported
hom route still has endpoints $A,B,C$ and still reads only the displayed
continuation rows.

Package provenance and the local $\NameCert$ row then close the route for every
CategoryUp consumer. Because the packet has no coordinate for an independent
category object or host evaluator, the displayed hom route is total on exactly
the finite packet.
\end{proof}

\begin{theorem}[ContinuationMonad category-generator source triad]
\label{thm:continuation-monad-category-generator-source-triad}
Every accepted $\ContinuationMonadUp$ packet exposes the $\CategoryUp$ hom-carrier row, the $\mathsf{ContinuationMorphism}$ row, and the generator-closure refusal row before either the public unit read or the public bind read is available. The exposed triad is read from $A,B,C,f,g,u,H,K,L,N$ over $\BHist$, $\Cont$, $\hsame$, $\Pkg$, and $\NameCert$, and not from an independent category object, quotient Kleisli arrow, host function space, or unlisted generator family.
\end{theorem}
\begin{proof}
Begin with carrier admission and the root generator obligation. The carrier obligation identifies $A,B,C,f,g,u,H,K,L,N$ as the available rows, while the root generator obligation places the refusal surface before any generator-facing read can use unit or bind.

Next apply bind-route classifier stability and the Category hom route totality theorem. These results route the $\CategoryUp$ hom-carrier and $\mathsf{ContinuationMorphism}$ entries through the same finite ledger $L$ and the same transport row $H$.

The unit and bind reads therefore come after the triad is already visible. Since the packet has no coordinate for an independent category object, quotient Kleisli arrow, host function space, or unlisted generator family, no downstream reader can bypass the displayed triad.
\end{proof}

\begin{theorem}[ContinuationMonad bind readback exhaustion]
\label{thm:continuation-monad-bind-readback-exhaustion}
Every bind readback exposed by an accepted $\ContinuationMonadUp$ packet is exhausted by the rows $A,B,C,f,g,u,H,K,L,N$. The readback may inspect the source histories $A,B,C$, the displayed continuation tails $f,g,u$, the transport row $H$, the route and ledger rows $K,L$, the $\Pkg$ provenance, and the local $\NameCert$ row $N$; it cannot read host functions, quotient Kleisli arrows, unlisted generators, or an external monad evaluator.
\end{theorem}
\begin{proof}
First use bind-route classifier stability. It identifies every accepted bind read with the displayed continuation route through $f,g,u,K,L$ and transports that read only through $H$.

Then use NameCert ledger exactness. The ledger records exactly the finite rows $A,B,C,f,g,u,H,K,L,N$ with $\Pkg$ provenance, so any consumer-visible bind readback must be a readback of that finite packet.

Finally apply the Kleisli composite scope and bind-route coverage theorems. They prevent a host function, quotient Kleisli arrow, unlisted generator, or external evaluator from supplying another bind readback outside the listed rows.
\end{proof}

\begin{theorem}[ContinuationMonad root-unblock package]
\label{thm:continuation-monad-root-unblock-package}
The root obligation package of an accepted $\ContinuationMonadUp$ packet consists exactly of carrier admission, classifier stability, $\CategoryUp$ hom routing, generator refusal, ledger exactness, and the local $\NameCert$ rows over $A,B,C,f,g,u,H,K,L,N$. This package is the surface available to downstream continuation chapters before they read any standard monad boundary, and it exports no quotient Kleisli category, host function composition, ambient effect algebra, or independent generator list.
\end{theorem}
\begin{proof}
Assemble the package from the existing obligation rows. Carrier admission supplies the packet rows, classifier stability fixes their $\hsame$ transport, category hom routing places the $\CategoryUp$ read in the finite ledger, and generator refusal excludes unlisted continuation rules.

Add NameCert ledger exactness and Kleisli composite scope. These rows make the visible ledger exact for $A,B,C,f,g,u,H,K,L,N$ and keep every composite read inside the same finite route.

The resulting package is precisely the root obligation surface available to downstream continuation chapters. Any quotient Kleisli category, host function composition, ambient effect algebra, or independent generator list would require a row outside the displayed carrier, and no such row is exported.
\end{proof}

\begin{theorem}[ContinuationMonad root unit-bind exhaustion]
\label{thm:continuation-monad-root-unit-bind-exhaustion}
For an accepted $\ContinuationMonadUp$ packet $Z=(A,B,C,f,g,u,H,K,L,N)$, every root unit read and root bind read is exhausted by the displayed rows $u,f,g,K,L,H,N$ together with the source histories $A,B,C$. The unit and bind consumers may inspect only the unit tail, the two continuation tails, componentwise transport, the route and ledger rows, package provenance, and the local $\NameCert$ row; they receive no quotient Kleisli arrow, host function composition, or host monad equality.
\end{theorem}
\begin{proof}
Start with \autoref{thm:continuation-monad-unit-laws} and \autoref{thm:continuation-monad-associativity}. They place the unit and bind laws inside the finite route named by $u,f,g,K,L$.

Use \autoref{thm:continuation-monad-ledger-exactness} and \autoref{thm:continuation-monad-bind-route-coverage}. The ledger reads those rows through the same accepted packet and accounts for every public unit or bind request.

Finally apply \autoref{thm:continuation-monad-scoped-route-dependencies} and \autoref{thm:continuation-monad-unit-route-nonescape}. The only remaining transports are the displayed $H$ rows and the local certificate $N$, so the host and quotient readings have no exported route.
\end{proof}

\begin{theorem}[ContinuationMonad root generator-category lock]
\label{thm:continuation-monad-root-generator-category-lock}
For an accepted $\ContinuationMonadUp$ packet $Z=(A,B,C,f,g,u,H,K,L,N)$, generator-closure reads and $\CategoryUp$ hom reads are locked to the same finite route $A,B,C,f,g,u,H,K,L,N$. A downstream consumer cannot separate the generator refusal ledger from the $\mathsf{CategoryHomCarrier}$ and $\mathsf{ContinuationMorphism}$ rows, and it cannot replace them by a quotient Kleisli category, an ambient category object, or host-function composition.
\end{theorem}
\begin{proof}
First apply \autoref{thm:continuation-monad-generator-refusal-row}. The generator surface reads the continuation tails and route rows through the same refusal ledger before it reaches package provenance or local naming.

Next apply \autoref{thm:continuation-monad-category-hom-route-totality}. The $\CategoryUp$ hom carrier and continuation-morphism entries are total exactly on the displayed route and do not supply an independent category object.

The two reads are then combined by \autoref{thm:continuation-monad-category-generator-source-triad}. Since both sides use the same $H,K,L,N$ rows over $A,B,C,f,g,u$, no downstream consumer can split the generator ledger from the Category hom route.
\end{proof}

\begin{theorem}[ContinuationMonad root downstream unblock package]
\label{thm:continuation-monad-root-downstream-unblock-package}
The root downstream surface of an accepted $\ContinuationMonadUp$ packet is the package formed by \autoref{thm:continuation-monad-root-unit-bind-exhaustion} and \autoref{thm:continuation-monad-root-generator-category-lock}. Downstream continuation chapters receive the finite rows $A,B,C,f,g,u,H,K,L,N$ as the complete unit, bind, generator-refusal, and Category hom route surface, and they receive no quotient Kleisli arrows, host-function composition, ambient category completion, or unlisted continuation generators.
\end{theorem}
\begin{proof}
Use \autoref{thm:continuation-monad-root-unit-bind-exhaustion} to exhaust the unit and bind consumers by the finite rows of $Z$.

Use \autoref{thm:continuation-monad-root-generator-category-lock} to attach the generator-refusal and Category hom reads to the same rows. This supplies the generator and category boundary without adding a second route.

Finally apply \autoref{thm:continuation-monad-downstream-consumer-exhaustion} and \autoref{thm:continuation-monad-root-unblock-package}. These theorems identify the downstream consumer surface with the finite root obligation package, so every excluded host, quotient, category-completion, or unlisted-generator object remains outside the packet.
\end{proof}

\closureat{ContinuationMonadUp}{seedStr}
\closureat{ContinuationMonadUp}{obligationStr}
\closureat{ContinuationMonadUp}{scopedStr}
\closureat{ContinuationMonadUp}{publicStr}
\closureat{ContinuationMonadUp}{bridgedStr}
\closureat{ContinuationMonadUp}{matureStr}

\begin{closurestatus}{\ContinuationMonadUp}
  \constructivestory{A $\ContinuationMonadUp$ packet is generated from source, middle, and target histories, two displayed continuation tails, a unit tail, componentwise $\hsame$ transports, displayed $\Cont$ routes, CategoryUp continuation-morphism readings, package provenance, and local $\NameCert$ evidence.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{thm:continuation-monad-public-route-bridge-totality} and \autoref{thm:continuation-monad-bridge-package} expose the public bridge package from the finite continuation route rows $A,B,C,f,g,u,H,K,L,N$ to the standard monad-reading boundary over $\BHist$, $\Cont$, $\hsame$, $\mathsf{CategoryHomCarrier}$, $\mathsf{ContinuationMorphism}$, $\Pkg$, and $\NameCert$.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close full category theory, quotient Kleisli categories, host functions, standard monad laws as host equalities, arbitrary effects, or bridge checking.}
  \upgradepath{The theory closure is complete for the finite $\ContinuationMonadUp$ bridge package; Lean verification and bridge checking remain separate verification-axis obligations.}
\end{closurestatus}
