\chapter{A Concrete Naming Certificate for $\MetaCICResidualJoinGaugeUp$}
\label{ch:concrete-instances-metacic-residual-join-gauge-namecert}
\origin{ai}

The $\MetaCICResidualJoinGaugeUp$ packet realizes a bounded residual-join gauge for the MetaCIC confluence frontier of \autoref{sec:metacic-open-problems-confluence}. It sits beside the residual-substitution boundary of \autoref{ch:concrete-instances-metacic-residual-substitution-boundary-namecert}, the confluence critical-pair boundary of \autoref{ch:concrete-instances-metacic-confluence-critical-pair-boundary-namecert}, the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}, the closed type-checker seal of \autoref{ch:concrete-instances-closed-type-checker-seal-namecert}, and the closed-normal confluence fragment of \autoref{ch:concrete-instances-metacicconfluencenormalfragment-namecert}. The packet records a finite residual-pair and bounded local-join surface without claiming residual-substitution compatibility, full Church--Rosser, subject reduction, evaluator equality, quotient syntax equality, or kernel consistency.

\begin{definition}[MetaCIC residual-join gauge carrier]
\label{def:metacic-residual-join-gauge-carrier}
A $\MetaCICResidualJoinGaugeUp$ carrier is a finite $\BHist$ packet
$$
J=(T,R,K,L,B,S,F,O,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $T$ is the typed-window row exposing the finite MetaCIC source, target, and context window inspected by the gauge.
\item $R$ is the residual-pair row, recording the two residual contraction paths whose local join is being tested.
\item $K$ is the critical-pair boundary row, keeping the inspected peak inside the confluence critical-pair surface.
\item $L$ is the bounded local-join row, listing the candidate common reducts and the finite search budget used to inspect them.
\item $B$ is the bounded checker row for the local join.
\item $S$ is the residual-substitution boundary row, retained as a premise-facing obstruction rather than discharged by the packet.
\item $F$ is the closed type-checker seal and closed-normal fragment row used to keep the inspected terms inside the sealed fragment.
\item $O$ is the obstruction-retention row for residual substitution, full parallel diamond, full Church--Rosser, subject reduction, evaluator equality, quotient syntax equality, and kernel consistency.
\item $H$ records componentwise $\hsame$ transport for $T,R,K,L,B,S,F,O$ and for replay, provenance, and naming.
\item $C$ records displayed $\Cont$ replay through the same residual-pair and bounded local-join route.
\item $P$ is the $\Pkg$ provenance over $\MetaCICResidualJoinGaugeUp$, $\MetaCICResidualSubstitutionBoundaryUp$, $\MetaCICConfluenceCriticalPairBoundaryUp$, $\MetaCICParallelDiamondFrontierUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetaCICResidualJoinGaugeUp}$ row.
\end{enumerate}
The classifier compares accepted gauges only through $T,R,K,L,B,S,F,O,H,C,P,N$. It has no coordinate for a premise-free residual-substitution theorem, full closed confluence, evaluator equality, quotient syntax equality, subject reduction, or kernel consistency.
\end{definition}

\begin{theorem}[MetaCIC residual-join gauge NameCert obligations]
\label{thm:metacic-residual-join-gauge-namecert-obligations}
For every accepted $\MetaCICResidualJoinGaugeUp$ carrier
$$
J=(T,R,K,L,B,S,F,O,H,C,P,N),
$$
the local $\NameCert_{\MetaCICResidualJoinGaugeUp}$ surface consists exactly of typed-window admission through $T$, residual-pair exposure through $R$, critical-pair boundary retention through $K$, bounded local-join exposure through $L$, bounded checker routing through $B$, residual-substitution boundary retention through $S$, closed-checker and closed-normal fragment sealing through $F$, obstruction retention through $O$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-residual-join-gauge-carrier}. The confluence-facing rows are $T,R,K,L,B,S,F,O$, and the structural certificate rows are $H,C,P,N$.

Read the residual rows first. The typed-window row $T$ scopes the inspected source and target data, while $R$ and $K$ expose the residual pair and its critical-pair boundary before any local-join read is available.

Read the bounded join rows next. The local-join row $L$ and checker row $B$ provide only a finite inspection surface, and the row $F$ keeps the route inside the closed type-checker seal and closed-normal fragment.

Finally $S,O,H,C,P,N$ retain the residual-substitution premise, refused endpoints, transport, replay, provenance, and local naming. Since no carrier coordinate discharges residual substitution or proves full confluence, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[MetaCIC residual-join gauge diamond handoff]
\label{thm:metacic-residual-join-gauge-diamond-handoff}
Every confluence consumer of an accepted $\MetaCICResidualJoinGaugeUp$ packet must read the residual pair $R$, bounded local-join row $L$, checker row $B$, residual-substitution boundary $S$, closed type-checker and closed-normal fragment row $F$, obstruction row $O$, and structural rows $H,C,P,N$ before using the packet in a conditional diamond route. The handoff supplies a finite bounded local-join gauge; it does not claim residual-substitution compatibility, full parallel diamond, full Church--Rosser, subject reduction, evaluator equality, quotient syntax equality, or kernel consistency.
\end{theorem}
\begin{proof}
Start from the obligation theorem \autoref{thm:metacic-residual-join-gauge-namecert-obligations}. It forces every confluence-facing read through the typed-window, residual-pair, critical-pair, bounded local-join, checker, residual-substitution, sealed-fragment, and obstruction rows.

The residual pair and critical-pair rows scope the peak. A consumer cannot read a local join before $R$ and $K$ identify the finite residual pair under inspection.

The bounded local-join and checker rows then expose only a finite gauge. The closed type-checker seal and closed-normal fragment row $F$ keep the inspected route inside the sealed fragment, while the retained boundary row $S$ prevents the handoff from being retyped as residual-substitution compatibility.

Finally $O,H,C,P,N$ preserve the refused endpoints and the structural certificate. Hence the packet may feed a later conditional diamond argument, but it does not itself close the full confluence or typing endpoints.
\end{proof}

\falsifiablePrediction{A $\MetaCICResidualJoinGaugeUp$ consumer that exports a diamond handoff without residual-pair, bounded local-join, bounded checker, residual-substitution boundary, closed type-checker seal, closed-normal fragment, obstruction, transport, replay, provenance, and local naming rows refutes the carrier of \autoref{def:metacic-residual-join-gauge-carrier}.}

\independenceWitness{metacic-residual-substitution-boundary, metacic-confluence-critical-pair-boundary, metacic-parallel-diamond-frontier, closed-type-checker-seal, metacicconfluencenormalfragment: the residual-substitution boundary retains the missing compatibility premise without the bounded local-join gauge; the critical-pair boundary records peaks without the residual-pair gauge; the parallel-diamond frontier records the broader conditional handoff without this bounded local-join packet; the closed type-checker seal records typing admission without residual joins; and the closed-normal fragment records normal-source joins without the residual-pair gauge. None is bijective to $\MetaCICResidualJoinGaugeUp$.}

Lean TasteGate.lean must later provide $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances for $\MetaCICResidualJoinGaugeUp$ so the typed-window, residual-pair, critical-pair, bounded local-join, checker, residual-substitution boundary, sealed-fragment, obstruction, transport, replay, provenance, and local naming fields remain distinguishable.

\closureat{MetaCICResidualJoinGaugeUp}{seedStr}

\begin{closurestatus}{\MetaCICResidualJoinGaugeUp}
  \constructivestory{A $\MetaCICResidualJoinGaugeUp$ packet is generated from typed-window, residual-pair, critical-pair boundary, bounded local-join, bounded checker, residual-substitution boundary, closed checker and closed-normal fragment, obstruction, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-residual-join-gauge-carrier}, \autoref{thm:metacic-residual-join-gauge-namecert-obligations}, and \autoref{thm:metacic-residual-join-gauge-diamond-handoff} seed the finite residual-pair and bounded local-join gauge for conditional MetaCIC diamond consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close residual-substitution compatibility, full parallel diamond, full Church--Rosser, subject reduction, evaluator equality, quotient syntax equality, kernel consistency, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked typed-window admission, residual-pair exposure, critical-pair boundary retention, bounded local-join exposure, bounded checker routing, residual-substitution boundary retention, sealed-fragment routing, obstruction retention, transport, replay, provenance, Nontrivial, FieldFaithful, and local naming for BEDC.Derived.MetaCICResidualJoinGaugeUp.}
\end{closurestatus}
