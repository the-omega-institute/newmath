\chapter{A Concrete Naming Certificate for $\CompactModulusOscillationRouterUp$}
\label{ch:concrete-instances-compact-modulus-oscillation-router-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis and the Heine--Cantor finite-net route, the $\CompactModulusOscillationRouterUp$ packet records the finite oscillation-to-uniform-modulus handoff for compact sources. It sits after the compact finite-net surfaces of \autoref{ch:concrete-instances-compact-namecert} and \autoref{ch:concrete-instances-compactmetric-namecert}, the pointwise continuity surfaces of \autoref{ch:concrete-instances-continuous-namecert} and \autoref{ch:concrete-instances-continuousmap-namecert}, the oscillation rows of \autoref{ch:concrete-instances-real-oscillation-namecert} and \autoref{ch:concrete-instances-oscillation-modulus-namecert}, the tolerance readbacks of \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}, and the downstream uniform-modulus packet of \autoref{ch:concrete-instances-uniformmodulus-namecert}. The packet names the router by which compact finite-net data and pointwise oscillation ledgers become a tolerance-only $\UniformModulusUp$ row; it does not assert open-cover compactness, semantic continuity, quotient function equality, selected centers outside the finite net, an external modulus oracle, or ambient Real completeness.

\begin{definition}[Compact modulus oscillation router carrier]
\label{def:compact-modulus-oscillation-router-carrier}
A $\CompactModulusOscillationRouterUp$ carrier is a finite $\BHist$ packet
$$
R=(K,M,F,O,Q,D,S,A,U,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactUp$ admission row naming the compact source as BEDC data.
\item $M$ is the $\CompactMetricUp$ finite-net row, carrying bundled centers, coverage witnesses, and the finite net inspected by the router.
\item $F$ is the $\ContinuousUp$ or $\ContinuousMapUp$ pointwise continuity row at each displayed center.
\item $O$ is the $\RealOscillationUp$ pointwise oscillation ledger read from the same center and tolerance data.
\item $Q$ is the $\OscillationModulusUp$ row converting the displayed oscillation ledger into a tolerance-to-window modulus route.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used by the pointwise oscillation and modulus rows.
\item $S$ is the $\RegSeqRatUp$ regular readback row for the rational windows opened by $D,Q$.
\item $A$ is the terminal $\RealUp$ seal for source and target distance or oscillation comparisons.
\item $U$ is the $\UniformModulusUp$ handoff row, exposed only after $K,M,F,O,Q,D,S,A$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for compact, compact-metric, continuity, oscillation, modulus, dyadic, regular, Real, uniform-modulus, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a target tolerance request through the compact finite net, pointwise continuity rows, oscillation ledger, oscillation modulus, dyadic and regular readback, Real seal, and uniform-modulus handoff.
\item $P$ is the $\Pkg$ provenance over $\CompactModulusOscillationRouterUp$, $\CompactUp$, $\CompactMetricUp$, $\ContinuousUp$, $\ContinuousMapUp$, $\RealOscillationUp$, $\OscillationModulusUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactModulusOscillationRouterUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through these rows. It has no coordinate for open-cover compactness, semantic continuity outside the displayed pointwise rows, quotient equality of functions or points, selected centers outside $M$, an external modulus oracle, countable choice, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.CompactModulusOscillationRouterUp.TasteGate.CompactModulusOscillationRouterTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Compact modulus oscillation router NameCert obligations]
\label{thm:compact-modulus-oscillation-router-namecert-obligations}
For every accepted $\CompactModulusOscillationRouterUp$ carrier
$$
R=(K,M,F,O,Q,D,S,A,U,H,C,P,N),
$$
the local $\NameCert_{\CompactModulusOscillationRouterUp}$ surface consists exactly of compact admission, compact-metric finite-net admission, pointwise continuity admission, pointwise oscillation exposure, oscillation-modulus exactness, dyadic tolerance visibility, regular readback stability, Real-seal nonescape, UniformModulus handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local naming through the displayed rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-modulus-oscillation-router-carrier}. The rows $K,M,F,O,Q,D,S,A,U,H,C,P,N$ are the whole accepted packet.

Compact admission and finite-net admission are read from $K$ and $M$. The pointwise continuity row $F$ is then inspected only at the displayed centers and windows supplied by those compact rows.

The oscillation route is carried by $O,Q,D,S,A$. The ledger $O$ records the pointwise oscillation data, $Q$ turns it into a displayed modulus route, $D$ supplies the dyadic tolerance, $S$ supplies regular rational readback, and $A$ seals the Real-facing comparison.

The uniform-modulus handoff $U$ is downstream of those rows. Transport, replay, provenance, and local naming through $H,C,P,N$ preserve the same finite route, so the listed obligations exhaust the local NameCert surface and none of the refused host objects is exported.
\end{proof}

\begin{theorem}[Compact modulus oscillation router handoff]
\label{thm:compact-modulus-oscillation-router-vision-handoff}
Every $\UniformModulusUp$ consumer of an accepted $\CompactModulusOscillationRouterUp$ packet receives a tolerance-only ledger only after reading the finite route
$$
\CompactUp,\ \CompactMetricUp,\ \ContinuousUp,\ \ContinuousMapUp,\ \RealOscillationUp,\ \OscillationModulusUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \RealUp .
$$
The handoff is the displayed compact finite-net and pointwise oscillation route, not an open-cover compactness theorem, semantic continuity principle, quotient equality, selected external center, external modulus oracle, countable-choice schedule, or ambient Real-completeness claim.
\end{theorem}
\begin{proof}
Start with the compact and metric rows $K,M$. They provide the finite centers and coverage witnesses available to the router.

Read the pointwise continuity and oscillation rows $F,O$ at those centers. The oscillation-modulus row $Q$ converts the displayed oscillation ledger into a tolerance-to-window route over the dyadic row $D$ and regular readback row $S$.

The Real seal $A$ records the numerical comparison reached by the same route. Only then is the $\UniformModulusUp$ row $U$ visible to a downstream consumer.

The structural rows $H,C,P,N$ transport, replay, source, and name that finite route. Since the packet contains no coordinate for the excluded host objects, every uniform-modulus consumer remains inside the displayed compact finite-net and pointwise oscillation handoff.
\end{proof}

\falsifiablePrediction{Every accepted $\CompactModulusOscillationRouterUp$ read factors through CompactUp admission, CompactMetric finite-net rows, Continuous or ContinuousMap pointwise rows, RealOscillation and OscillationModulus ledgers, DyadicRatCore tolerance rows, RegSeqRat readback, RealUp seals, UniformModulusUp handoff, hsame transport, Cont replay, Pkg provenance, and local NameCert rows.}

\independenceWitness{compact, compactmetric, continuous, continuousmap, real-oscillation, oscillation-modulus, dyadicratcore, regseqrat, real, uniformmodulus: none is bijective with $\CompactModulusOscillationRouterUp$ because this packet alone binds the compact finite-net route to pointwise oscillation and a downstream tolerance-only uniform-modulus handoff.}

\closureat{CompactModulusOscillationRouterUp}{seedStr}

\begin{closurestatus}{\CompactModulusOscillationRouterUp}
  \constructivestory{A $\CompactModulusOscillationRouterUp$ packet is generated from Compact and CompactMetric rows, Continuous and ContinuousMap pointwise rows, RealOscillation and OscillationModulus ledgers, DyadicRatCore tolerance rows, RegSeqRat readback, RealUp seals, UniformModulusUp handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-modulus-oscillation-router-carrier}, \autoref{thm:compact-modulus-oscillation-router-namecert-obligations}, and \autoref{thm:compact-modulus-oscillation-router-vision-handoff} seed the compact finite-net and pointwise oscillation route before UniformModulus consumers inspect the tolerance-only ledger.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, semantic continuity, quotient function or point equality, selected centers outside the finite net, external modulus selection, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked compact admission, finite-net admission, pointwise continuity admission, oscillation exposure, oscillation-modulus exactness, tolerance visibility, regular readback stability, Real-seal nonescape, UniformModulus handoff, transport, replay, provenance, and local naming for BEDC.Derived.CompactModulusOscillationRouterUp.}
\end{closurestatus}
