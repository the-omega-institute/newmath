The $\GapClosureBoundaryUp$ dependency route to \autoref{ch:concrete-instances-finitewitnessedrefutation-namecert} is read before any contrary witness is interpreted. The displayed boundary row fixes whether the consumer receives the supported source branch or the refusal branch, while the bounded search surface of \autoref{ch:concrete-instances-boundedsearchrefutation-namecert} supplies only the finite search ledger used after that branch has been selected.

\begin{lemma}[GapClosureBoundary finite witnessed-refutation route]\label{lem:gap-closure-boundary-finite-witnessed-refutation-route}
Any $\FiniteWitnessedRefutationUp$ consumer of a $\GapClosureBoundaryUp$ packet first consumes the displayed source/refusal row of the boundary packet. It may then read the bounded-search witness ledger, but it receives no hidden closure row, off-packet contradiction oracle, or unbounded search choice from the boundary.
\end{lemma}
