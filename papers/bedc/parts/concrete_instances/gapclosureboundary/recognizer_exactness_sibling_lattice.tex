The $\GapClosureBoundaryUp$ sibling lattice first consumes the recognizer route of \autoref{ch:concrete-instances-groundcompilerrecognizerflow-namecert}, the audit-packet route of \autoref{ch:concrete-instances-groundcompilerrecognizerauditpacket-namecert}, and the finite-trace evaluator of \autoref{ch:concrete-instances-finitetraceevaluator-namecert}. Only after those three rows are visible does the boundary expose its source/refusal branch: recognized traces may pass to the displayed source row, while exactness failures remain on the refusal row with the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ surface.
