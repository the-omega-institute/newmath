\chapter{A Concrete Naming Certificate for $\RegularCauchyProductMetricUp$}
\label{ch:concrete-instances-regular-cauchy-product-metric-namecert}
\origin{human}

Following Bishop--Bridges product-metric and regular-Cauchy sequence mathematics, the $\RegularCauchyProductMetricUp$ packet realizes the paired finite-window product-distance route used by the compact-uniform-continuity construction of \autoref{ch:visions-compact-uniform-continuity}. It sits after the product-distance packet of \autoref{ch:concrete-instances-cauchy-product-metric-namecert}, the regular product route of \autoref{ch:concrete-instances-regularcauchyproduct-namecert}, the finite stream interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet gives finite-net and uniform-modulus consumers a paired regular-Cauchy product metric surface without host product metrics, quotient stream equality, selected product limits, countable choice, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy product-metric carrier]
\label{def:regular-cauchy-product-metric-carrier}
A $\RegularCauchyProductMetricUp$ carrier is a finite $\BHist$ packet
$$
M=(A,B,K,W_A,W_B,R_A,R_B,D,\Delta,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ and $B$ are the two accepted regular Cauchy source rows.
\item $K$ is the accepted $\RegularCauchyProductUp$ row joining the two source names by displayed product windows.
\item $W_A$ and $W_B$ are the synchronized $\StreamNameUp$ finite-window rows inspected by finite-net consumers.
\item $R_A$ and $R_B$ are the $\RegSeqRatUp$ readback rows attached to the two displayed window streams.
\item $D$ is the $\DyadicRatCoreUp$ product-distance ledger recording coordinate tolerances, product radius, and comparison budget.
\item $\Delta$ is the $\CauchyProductMetricUp$ metric row read from $K,W_A,W_B,R_A,R_B,D$.
\item $E$ is the terminal $\RealUp$ seal for the product-distance read.
\item $H$ records componentwise $\hsame$ transport for the source rows, product row, windows, readbacks, dyadic ledger, metric row, Real seal, continuation, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a finite-net product-distance request through $A,B,K,W_A,W_B,R_A,R_B,D,\Delta,E$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyProductMetricUp$, $\RegularCauchyProductUp$, $\CauchyProductMetricUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyProductMetricUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,B,K,W_A,W_B,R_A,R_B,D,\Delta,E,H,C,P,N$. It has no coordinate for host product metrics, quotient stream equality, selected limits, compactness, trichotomy, detached real carriers, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Regular Cauchy product-metric NameCert obligations]
\label{thm:regular-cauchy-product-metric-namecert-obligations}
For every accepted $\RegularCauchyProductMetricUp$ carrier $M=(A,B,K,W_A,W_B,R_A,R_B,D,\Delta,E,H,C,P,N)$, the local $\NameCert_{\RegularCauchyProductMetricUp}$ surface consists exactly of carrier admission for the two regular Cauchy sources, the $\RegularCauchyProductUp$ product row, synchronized $\StreamNameUp$ windows, paired $\RegSeqRatUp$ readbacks, the $\DyadicRatCoreUp$ product-distance ledger, the $\CauchyProductMetricUp$ metric row, the $\RealUp$ seal, componentwise transport, displayed continuation replay, package provenance, local naming, and refusal of host product metrics, quotient stream equality, selected limits, compactness, trichotomy, detached real carriers, ambient $\RealUp$ completeness, Lean verification, and bridge checking.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-product-metric-carrier}. The accepted packet exposes exactly the two source rows, the regular product row, synchronized windows, regular readbacks, dyadic ledger, metric row, Real seal, and structural rows.

The finite-net route reads the windows before it reads the product-distance ledger. Hence $D$ is tied to $W_A,W_B,R_A,R_B$ and cannot be replaced by a host product metric.

The metric row $\Delta$ is then forced through the sibling $\CauchyProductMetricUp$ route. It consumes the regular product and dyadic ledger as displayed rows, so compact-uniform consumers receive a product-distance certificate rather than quotient stream equality or selected limits.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since no coordinate stores compactness, host product metrics, quotient streams, selected representatives, detached real carriers, ambient completeness, or bridge checking, the listed obligations exhaust the local certificate surface.
\end{proof}

\closureat{RegularCauchyProductMetricUp}{seedStr}

\begin{closurestatus}{\RegularCauchyProductMetricUp}
  \constructivestory{A $\RegularCauchyProductMetricUp$ packet is generated from two regular Cauchy source rows, a $\RegularCauchyProductUp$ row, synchronized $\StreamNameUp$ windows, paired $\RegSeqRatUp$ readbacks, a $\DyadicRatCoreUp$ product-distance ledger, a $\CauchyProductMetricUp$ metric row, a $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-product-metric-carrier} and \autoref{thm:regular-cauchy-product-metric-namecert-obligations} seed the finite regular-Cauchy product-distance route consumed by compact-uniform-continuity finite-net and uniform-modulus consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim compactness, host product metrics, quotient stream equality, selected limits, trichotomy, detached real carriers, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, product-window exactness, dyadic product-distance ledger stability, CauchyProductMetric handoff, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyProductMetricUp.}
\end{closurestatus}
