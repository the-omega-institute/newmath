\chapter{A Concrete Naming Certificate for $\DiniCompactRealSealUp$}
\label{ch:concrete-instances-dini-compact-real-seal-namecert}
\origin{human}

Following Dini and Bishop--Bridges constructive analysis, the $\DiniCompactRealSealUp$ packet records the compact Dini theorem route as finite Real-seal data. It sits after the constructive Dini theorem packet of \autoref{ch:concrete-instances-constructive-dini-theorem-namecert} and the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}: those siblings supply the monotone compact-uniform route and compact source rows, while this chapter packages the terminal Real-facing seal required by L10 completion consumers.

\begin{definition}[Dini compact Real-seal carrier]
\label{def:dini-compact-real-seal-carrier}
A $\DiniCompactRealSealUp$ carrier is a finite $\BHist$ packet
$$
S=(D,K,F,M,U,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\ConstructiveDiniTheoremUp$ theorem packet row, carrying the constructive Dini route already named by the sibling chapter;
\item $K$ is the $\CompactMetricUp$ source row whose finite compact windows are the only source of compactness data used by the seal;
\item $F$ is the $\ContinuousMapUp$ family row for the displayed monotone continuous maps inspected on $K$;
\item $M$ is the monotone ledger and finite Dini modulus row, recording the comparison windows and tolerance budget used by the route;
\item $U$ is the $\UniformLimitUp$ handoff row exported after the finite Dini modulus has been displayed;
\item $W$ is the $\StreamNameUp$ finite-window row through which Real-facing values are observed;
\item $R$ is the $\RegSeqRatUp$ readback row attached to the same stream windows;
\item $E$ is the terminal $\RealUp$ seal row;
\item $H$ records componentwise $\hsame$ transport for the Dini theorem, compact source, continuous family, monotone ledger, uniform handoff, StreamName windows, RegSeqRat readback, Real seal, replay, provenance, and local naming rows;
\item $C$ records displayed $\Cont$ replay from the compact source through the monotone ledger, finite Dini modulus, uniform handoff, finite stream windows, regular-rational readback, and terminal Real seal;
\item $P$ is the $\Pkg$ provenance over $\ConstructiveDiniTheoremUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformLimitUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\DiniCompactRealSealUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for open-cover compactness, a host uniform-convergence theorem, quotient equality of functions, countable choice, selected limiting values, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Dini compact Real-seal NameCert obligations]
\label{thm:dini-compact-real-seal-namecert-obligations}
For every accepted $\DiniCompactRealSealUp$ carrier
$$
S=(D,K,F,M,U,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\DiniCompactRealSealUp}$ surface consists exactly of carrier admission for $S$, the constructive Dini theorem row $D$, compact source exposure through $K$, continuous-family exposure through $F$, monotone ledger and finite Dini modulus exposure through $M$, uniform-limit handoff through $U$, StreamName finite windows through $W$, RegSeqRat readback through $R$, dyadic tolerance provenance through $P$, terminal Real seal through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:dini-compact-real-seal-carrier}. The rows $D,K,F,M,U,W,R,E,H,C,P,N$ are the whole accepted packet, so every Real-facing read starts from displayed finite data rather than from an ambient theorem about uniform convergence.

Read the compact source and monotone family first. The compact row $K$ supplies the finite source windows, the continuous-map row $F$ supplies the inspected family values, and the monotone ledger $M$ records the comparison windows and Dini tolerance budget.

The uniform handoff is downstream from those rows. The row $U$ may be read only after the finite Dini modulus in $M$ has been displayed, and its Real-facing output passes through $W$ and $R$ before reaching $E$.

The structural rows finish the certificate. Transport, replay, provenance, and local naming through $H,C,P,N$ preserve the same compact source, monotone ledger, finite Dini modulus, uniform handoff, StreamName windows, RegSeqRat readback, dyadic tolerance, and terminal $\RealUp$ seal. Since no excluded object is a carrier coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Dini compact Real-seal uniform route]
\label{thm:dini-compact-real-seal-uniform-route}
Every Real-facing read exported by an accepted $\DiniCompactRealSealUp$ packet factors through the compact source, monotone ledger, finite Dini modulus, uniform handoff, StreamName finite windows, RegSeqRat readback, dyadic tolerance provenance, and terminal $\RealUp$ seal. Equivalently, a consumer reads the constructive Dini theorem packet of $\ConstructiveDiniTheoremUp$ through $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformLimitUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows before any terminal Real value is public.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:dini-compact-real-seal-namecert-obligations}. They force every consumer to expose $D,K,F,M,U,W,R,E,H,C,P,N$ before using the local name.

The route then factors through the constructive Dini sibling. The theorem packet row $D$ and compact source row $K$ read the monotone compact route of $\ConstructiveDiniTheoremUp$ and the compact windows of $\CompactMetricUp$ before the Dini modulus row $M$ is visible.

Next the uniform handoff row $U$ consumes the finite Dini modulus. Its output is not a host uniform-convergence theorem: it is a displayed $\UniformLimitUp$ row that must still pass through the finite $\StreamNameUp$ windows $W$ and the $\RegSeqRatUp$ readback row $R$.

Finally the dyadic and Real-facing rows seal the read. Provenance $P$ records the $\DyadicRatCoreUp$ tolerance surface attached to the same budget, and the terminal row $E$ is the only $\RealUp$ seal exported by the packet. Thus no open-cover compactness, quotient function equality, selected limit, countable choice, hidden host limit, or ambient Real completeness can be read from the route.
\end{proof}

\falsifiablePrediction{An accepted $\DiniCompactRealSealUp$ consumer that reads a terminal Real value without first displaying the compact source row, monotone ledger, finite Dini modulus, uniform handoff, StreamName window, RegSeqRat readback, dyadic tolerance provenance, and Real seal refutes the carrier surface.}

\independenceWitness{constructive-dini-theorem, compactmetric, continuousmap, uniformlimit, streamname, regseqrat, dyadicratcore, real: $\DiniCompactRealSealUp$ is not bijective to any listed sibling because it binds the compact Dini route to a terminal L10 Real-seal packet in one local NameCert carrier.}

\closureat{DiniCompactRealSealUp}{seedStr}

\begin{closurestatus}{\DiniCompactRealSealUp}
  \constructivestory{A $\DiniCompactRealSealUp$ packet is generated from ConstructiveDiniTheorem, CompactMetric, ContinuousMap, monotone ledger, finite Dini modulus, UniformLimit, StreamName, RegSeqRat, DyadicRatCore, Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dini-compact-real-seal-carrier}, \autoref{thm:dini-compact-real-seal-namecert-obligations}, and \autoref{thm:dini-compact-real-seal-uniform-route} seed the finite Real-seal route for compact Dini packets over compact metric, continuous-map, uniform-limit, StreamName, RegSeqRat, dyadic, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not prove Lean verification, bridge checking, open-cover compactness, quotient equality of functions, countable choice, selected limiting values, or ambient Real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, Dini route exactness, finite modulus exposure, uniform handoff, StreamName and RegSeqRat readback, dyadic tolerance preservation, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.DiniCompactRealSealUp.}
\end{closurestatus}
