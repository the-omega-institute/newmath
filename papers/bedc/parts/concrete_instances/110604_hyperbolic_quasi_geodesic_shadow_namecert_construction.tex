\chapter{A Concrete Naming Certificate for $\HyperbolicQuasiGeodesicShadowUp$}
\label{ch:concrete-instances-hyperbolic-quasi-geodesic-shadow-namecert}
\origin{ai}

The $\HyperbolicQuasiGeodesicShadowUp$ packet realizes the observer-expansion shadow route of \autoref{sec:hyperbolic-observer-expansion} as finite quasi-geodesic, boundary, visual-metric, Busemann, and lamination-compatible rows. It sits after disk readout through \autoref{ch:concrete-instances-unitdisk-namecert}, the metric surface of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the visual-metric packet of \autoref{ch:concrete-instances-hyperbolic-visual-metric-namecert}, the Busemann cocycle route of \autoref{ch:concrete-instances-hyperbolic-busemann-cocycle-namecert}, the geodesic-lamination packet of \autoref{ch:concrete-instances-hyperbolic-geodesic-lamination-namecert}, and the observer-expansion packet of \autoref{ch:concrete-instances-hyperbolic-observer-expansion-namecert}. The packet names a finite shadow tied to a displayed quasi-geodesic route; it does not complete the boundary, choose a global geodesic representative, identify quotient ideal points, or import host hyperbolic geometry.

\begin{definition}[Hyperbolic quasi-geodesic shadow carrier]
\label{def:hyperbolic-quasi-geodesic-shadow-carrier}
A $\HyperbolicQuasiGeodesicShadowUp$ carrier is a finite $\BHist$ packet
$$
Q=(D,G,E,V,B,L,H,C,P,A,N)
$$
with the following rows:
\begin{enumerate}
\item $D$ is the disk readout row, exposing only displayed $\UnitDiskUp$ coordinates and the associated $\HyperbolicMetricUp$ metric-read data.
\item $G$ is the quasi-geodesic route row, recording the finite path samples, distortion budget, endpoint-facing direction, and local route ledger inspected by the shadow request.
\item $E$ is the boundary endpoint row named by the displayed route, not by a completed ideal-boundary quotient.
\item $V$ is the visual-metric shadow row, recording the finite shadow window read through $\HyperbolicVisualMetricUp$ and the boundary endpoint $E$.
\item $B$ is the Busemann cocycle tolerance row, recording the displayed additive tolerance used to compare the route and the shadow window.
\item $L$ is the geodesic-lamination compatibility row, recording whether the displayed route is compatible with the finite noncrossing leaf data consumed by $\HyperbolicGeodesicLaminationUp$.
\item $H$ records componentwise $\hsame$ transport for disk readout, quasi-geodesic route, boundary endpoint, visual-metric shadow, Busemann tolerance, lamination compatibility, replay, provenance, refusal, and local naming rows.
\item $C$ records displayed $\Cont$ replay from an observer-expansion shadow request through $D,G,E,V,B,L,H,P,A,N$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicQuasiGeodesicShadowUp$, $\UnitDiskUp$, $\HyperbolicMetricUp$, $\HyperbolicVisualMetricUp$, $\HyperbolicBusemannCocycleUp$, $\HyperbolicGeodesicLaminationUp$, $\HyperbolicObserverExpansionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $A$ is the refusal row excluding completed boundary quotients, selected global geodesics, ambient hyperbolic compactification, physical observer trajectories, and host curvature semantics.
\item $N$ is the local $\NameCert_{\HyperbolicQuasiGeodesicShadowUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $D,G,E,V,B,L,H,C,P,A,N$.
\end{definition}

\begin{theorem}[Hyperbolic quasi-geodesic shadow NameCert obligations]
\label{thm:hyperbolic-quasi-geodesic-shadow-namecert-obligations}
For every accepted $\HyperbolicQuasiGeodesicShadowUp$ carrier
$$
Q=(D,G,E,V,B,L,H,C,P,A,N),
$$
the local $\NameCert_{\HyperbolicQuasiGeodesicShadowUp}$ surface consists exactly of disk-readout visibility, quasi-geodesic route visibility, boundary endpoint exposure, visual-metric shadow-window exposure, Busemann cocycle tolerance, geodesic-lamination compatibility, componentwise transport, replay, provenance, refusal, and local naming rows.

The packet exports no completed ideal boundary, quotient boundary equality, selected global geodesic, ambient hyperbolic compactification, physical observer trajectory, host curvature theorem, or global field semantics.
\end{theorem}
\begin{proof}
Project the carrier rows in order. The disk readout $D$ supplies the only coordinate and metric source, while $G$ supplies the displayed route samples and distortion budget inspected by the request.

The endpoint and shadow rows are downstream of that route. The endpoint row $E$ names only the boundary read visible from $G$, and the visual row $V$ records the finite shadow window tied to that endpoint.

The comparison rows $B$ and $L$ keep the shadow route finite. The Busemann tolerance is a displayed cocycle budget, and lamination compatibility is a finite noncrossing-leaf check, not a global geodesic-foliation theorem.

Finally $H,C,P,A,N$ transport, replay, source, refuse, and name the same packet. Since the excluded host objects are not carrier rows, they cannot appear as NameCert obligations.
\end{proof}

\begin{theorem}[Hyperbolic quasi-geodesic shadow stability]
\label{thm:hyperbolic-quasi-geodesic-shadow-stability}
Let $Q_0$ and $Q_1$ be accepted $\HyperbolicQuasiGeodesicShadowUp$ carriers whose disk readout, quasi-geodesic route, boundary endpoint, visual-metric shadow window, Busemann tolerance, lamination compatibility, refusal, provenance, and local naming rows are componentwise $\hsame$-transported. Then every observer-expansion consumer reads the same finite shadow surface through $Q_0$ and $Q_1$.

The stability claim is confined to the finite rows $D,G,E,V,B,L,H,C,P,A,N$. It does not identify completed boundaries, quotient ideal points, selected geodesics, ambient compactifications, physical observer trajectories, host curvature data, or global fields.
\end{theorem}
\begin{proof}
Start with an observer-expansion shadow request for $Q_0$. The request must read disk coordinates and metric data through $D$ and then the finite route samples and distortion budget through $G$.

Use the componentwise $\hsame$ transport. It carries the endpoint $E$, visual shadow window $V$, Busemann tolerance $B$, and lamination compatibility row $L$ to the corresponding rows of $Q_1$.

The refusal row is transported with the same packet. Therefore the transported consumer sees the same excluded host objects and cannot insert completed boundary quotients, selected global geodesics, ambient compactifications, physical trajectories, curvature theorems, or global fields.

The replay row $C$ and the provenance and naming rows $P,N$ then present the transported shadow as the same finite observer-expansion surface.
\end{proof}

\begin{theorem}[Hyperbolic quasi-geodesic shadow vision handoff]
\label{thm:hyperbolic-quasi-geodesic-shadow-vision-handoff}
Every observer-expansion consumer of an accepted $\HyperbolicQuasiGeodesicShadowUp$ carrier must read the route
$$
\UnitDiskUp,\ \HyperbolicMetricUp,\ \HyperbolicVisualMetricUp,\ \HyperbolicBusemannCocycleUp,\ \HyperbolicGeodesicLaminationUp,\ \HyperbolicObserverExpansionUp
\longmapsto
\HyperbolicQuasiGeodesicShadowUp
$$
through disk readout, quasi-geodesic route, boundary endpoint, visual-metric shadow, Busemann tolerance, lamination compatibility, transport, replay, provenance, refusal, and local NameCert rows. The handoff realizes the vision-level shadow route as finite NameCert data without turning observer expansion into physical cosmology or a host hyperbolic-boundary theorem.
\end{theorem}
\begin{proof}
The vision handoff begins with disk and metric data. Rows $D$ and $G$ ensure that the consumer reads only displayed disk observations, metric readouts, finite route samples, and a distortion budget.

The endpoint and shadow window are then forced. Rows $E$ and $V$ bind the boundary endpoint and visual-metric shadow to the same route, while $B$ records the Busemann tolerance needed to compare route and shadow.

Lamination compatibility is read through $L$. This makes the packet consumable by geodesic-lamination and observer-expansion rows without selecting a global geodesic representative or completing the boundary.

The rows $H,C,P,A,N$ transport, replay, source, refuse, and name the handoff. Thus the vision-level quasi-geodesic shadow is realized as finite NameCert data and not as ambient hyperbolic geometry.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicQuasiGeodesicShadowUp$ read must expose a boundary endpoint, visual-metric shadow window, Busemann cocycle tolerance, quasi-geodesic route, transport row, provenance row, refusal row, and local NameCert before any observer-expansion consumer may read the shadow.}

\independenceWitness{hyperbolic-visual-metric, hyperbolic-busemann-cocycle, hyperbolic-geodesic-lamination, hyperbolic-observer-expansion: $\HyperbolicQuasiGeodesicShadowUp$ is not bijective to these siblings because it binds the quasi-geodesic route and shadow window together before the observer-expansion packet may consume the boundary read.}

The Lean TasteGate for this packet must later provide $\mathsf{Nontrivial}\ \HyperbolicQuasiGeodesicShadowUp$ and $\mathsf{FieldFaithful}\ \HyperbolicQuasiGeodesicShadowUp$ in \texttt{lean4/BEDC/Derived/HyperbolicQuasiGeodesicShadowUp/TasteGate.lean}. Those instances must keep $D,G,E,V,B,L,H,C,P,A,N$ as separate fields before any checked carrier-alignment theorem can serve as a public TasteGate realization.

\closureat{HyperbolicQuasiGeodesicShadowUp}{seedStr}

\begin{closurestatus}{\HyperbolicQuasiGeodesicShadowUp}
  \constructivestory{A $\HyperbolicQuasiGeodesicShadowUp$ packet is generated from disk readout rows, a quasi-geodesic route, a boundary endpoint, a visual-metric shadow window, Busemann cocycle tolerance, geodesic-lamination compatibility, componentwise hsame transport, displayed Cont replay, Pkg provenance, refusal, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-quasi-geodesic-shadow-carrier}, \autoref{thm:hyperbolic-quasi-geodesic-shadow-namecert-obligations}, \autoref{thm:hyperbolic-quasi-geodesic-shadow-stability}, and \autoref{thm:hyperbolic-quasi-geodesic-shadow-vision-handoff} seed the finite quasi-geodesic shadow route for observer-expansion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed boundary quotients, selected global geodesics, ambient hyperbolic compactification, physical observer trajectories, host curvature semantics, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked disk readout, quasi-geodesic route admission, endpoint exposure, visual-metric shadow windows, Busemann tolerance, lamination compatibility, TasteGate nontriviality, field faithfulness, refusal exactness, transport, replay, provenance, and local naming for future BEDC.Derived.HyperbolicQuasiGeodesicShadowUp.}
\end{closurestatus}
