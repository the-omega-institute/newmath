\chapter{A Concrete Naming Certificate for $\CompactRealIntervalUp$}
\label{ch:concrete-instances-compact-real-interval-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis and the Heine-Borel finite-net reading of closed intervals, the $\CompactRealIntervalUp$ packet records a compact closed interval as finite BEDC NameCert data. It sits over the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the dyadic scale and tolerance rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the stream-window interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the finite-net compactness siblings \autoref{ch:concrete-instances-compactmetric-namecert} and \autoref{ch:concrete-instances-heine-borel-finite-net-namecert}. The packet is an interval carrier for later compact-real consumers; it does not import host compactness, selected subcovers, quotient real equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Compact real interval carrier]
\label{def:compact-real-interval-carrier}
A $\CompactRealIntervalUp$ carrier is a finite $\BHist$ packet
$$
I=(A,B,O,D,W,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ and $B$ are the two located $\RealUp$ endpoint rows, each exposed through finite real-name windows before any interval request is consumed.
\item $O$ is the order and apartness ledger, recording the displayed endpoint order, located overlap tests, boundary apartness rows, and refusal of trichotomy outside the inspected interval windows.
\item $D$ is the $\DyadicRatCoreUp$ finite tolerance ledger, recording common-scale comparisons, mesh radii, endpoint slack, and dyadic containment checks.
\item $W$ is the finite dyadic net schedule, listing the mesh windows used to inspect the interval between the endpoint rows.
\item $S$ is the $\StreamNameUp$ window row, carrying the finite stream requests that read endpoint and mesh data without opening a quotient stream.
\item $R$ is the $\RegSeqRatUp$ readback row, repacking endpoint, mesh, order, and tolerance checks as regular rational finite windows.
\item $E$ is the terminal $\RealUp$ seal row for the endpoint and inspected mesh values.
\item $H$ records componentwise $\hsame$ transport for endpoints, order/apartness ledgers, dyadic tolerances, finite nets, stream windows, regular readbacks, Real seals, replay rows, provenance rows, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a compact-interval request through $A,B,O,D,W,S,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CompactRealIntervalUp$, $\RealUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\CompactMetricUp$, $\HeineBorelFiniteNetUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactRealIntervalUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $A,B,O,D,W,S,R,E,H,C,P,N$. It has no coordinate for host compactness, arbitrary open covers, selected subcovers, quotient real equality, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[Compact real interval finite-net obligations]
\label{thm:compact-real-interval-net-obligations}
For every accepted $\CompactRealIntervalUp$ carrier
$$
I=(A,B,O,D,W,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\CompactRealIntervalUp}$ surface consists of the following finite obligations:
\begin{enumerate}
\item endpoint admission for the two located $\RealUp$ rows $A$ and $B$;
\item order and apartness admission for the displayed ledger $O$;
\item dyadic scale and tolerance admission for the $\DyadicRatCoreUp$ row $D$;
\item finite-net schedule admission for $W$, including mesh containment and endpoint slack;
\item StreamName finite-window admission for $S$ and regular rational readback for $R$;
\item terminal Real seal admission for $E$ after the finite endpoint, dyadic, stream, and regular rows are visible;
\item componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming through $H,C,P,N$.
\end{enumerate}
No compact-interval obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-real-interval-carrier}. The endpoint rows $A,B$ and the order/apartness row $O$ expose the interval boundary and the located comparison surface available to the packet.

Read the finite inspection rows next. The dyadic ledger $D$ supplies common-scale tolerance checks, while $W$ lists the finite mesh windows to which interval inspection is confined.

The stream and regular rows are downstream. The row $S$ carries the finite StreamName requests, and $R$ repacks the same endpoint, mesh, and tolerance reads as regular rational windows before the terminal Real seal $E$ is consumed.

Finally, $H,C,P,N$ transport, replay, source, and locally name the same finite packet. Since the carrier has no coordinate for host compactness, arbitrary open covers, selected subcovers, quotient equality, countable choice, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Compact real interval NameCert scope]
\label{thm:compact-real-interval-namecert-scope}
Every accepted $\CompactRealIntervalUp$ consumer factors through the displayed route
$$
\RealUp,\quad
\DyadicRatCoreUp,\quad
\StreamNameUp,\quad
\RegSeqRatUp,\quad
\CompactMetricUp,\quad
\HeineBorelFiniteNetUp .
$$
Thus the compact closed-interval surface exports only endpoint Real seals, dyadic tolerance and finite-net data, finite StreamName windows, regular rational readbacks, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row. It does not export host compactness, arbitrary open-cover compactness, selected subcovers, quotient real equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the endpoint and tolerance rows. The carrier exposes the interval through $A,B,O,D$, so every later compact-interval read is attached to the same located Real endpoints and dyadic tolerance ledger.

Use the finite-net and window rows. The row $W$ records the displayed dyadic net schedule, $S$ restricts stream requests to finite windows, and $R$ records the regular rational readback of exactly those windows.

The compactness siblings are read only through the same finite rows. The $\CompactMetricUp$ and $\HeineBorelFiniteNetUp$ provenance in $P$ witnesses compatibility with finite-net compactness, but it does not add an arbitrary cover selector or host compactness coordinate.

The structural rows $H,C,P,N$ preserve the route by transport, replay, sourcing, and local naming. Since no refused object appears in the carrier, every accepted consumer remains inside the stated scope.
\end{proof}

\falsifiablePrediction{Every $\CompactRealIntervalUp$ read factors through two located $\RealUp$ endpoint rows, a $\DyadicRatCoreUp$ tolerance ledger, a finite dyadic net schedule, $\StreamNameUp$ finite windows, $\RegSeqRatUp$ readback, finite-net compactness sibling provenance, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert_{\CompactRealIntervalUp}$ rows.}

\independenceWitness{real, dyadicratcore, streamname, regseqrat, compactmetric, heineborelfinitemesh: $\CompactRealIntervalUp$ is not bijective to any listed sibling because it jointly carries endpoint Real seals, order/apartness data, dyadic common-scale tolerances, a finite interval net schedule, finite stream windows, regular readback, finite-net compactness provenance, transport, replay, provenance, and local naming.}

\closureat{CompactRealIntervalUp}{seedStr}

\begin{closurestatus}{\CompactRealIntervalUp}
  \constructivestory{A $\CompactRealIntervalUp$ packet is generated from two located RealUp endpoint rows, an order and apartness ledger, a DyadicRatCoreUp finite tolerance ledger, a finite dyadic net schedule, StreamNameUp finite windows, RegSeqRatUp readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-real-interval-carrier}, \autoref{thm:compact-real-interval-net-obligations}, and \autoref{thm:compact-real-interval-namecert-scope} seed the compact closed-interval carrier over endpoint Real seals, dyadic tolerances, finite nets, StreamName windows, RegSeqRat readback, and finite-net compactness sibling provenance.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host compactness, arbitrary open-cover compactness, selected subcovers, quotient real equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking for $\CompactRealIntervalUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, finite-net schedule exactness, dyadic tolerance stability, StreamName window pullback, RegSeqRat readback, Real endpoint sealing, replay stability, and nonescape for BEDC.Derived.CompactRealIntervalUp.}
\end{closurestatus}
