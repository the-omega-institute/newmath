\begin{theorem}[Compact real interval dossier lattice route]
\label{thm:compact-real-interval-dossier-lattice-route}
Every $\CompactRealIntervalUp$ dossier consumer that inspects an accepted interval packet must first display the $\ClosedBoundedIntervalUp$ endpoint rows, the $\DyadicRatCoreUp$ tolerance rows, the $\StreamNameUp$ finite windows, the $\RegSeqRatUp$ readback rows, the terminal $\RealUp$ seals, the $\CompactMetricUp$ finite-net compatibility row, and the $\HeineBorelFiniteNetUp$ provenance row. Only after those rows have been replayed through componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\CompactRealIntervalUp}$ row may the consumer inspect the compact interval packet itself.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-real-interval-carrier}. The endpoint rows $A$ and $B$ give the closed-bounded interval source, while the dyadic tolerance row $D$ records the finite scale and containment ledger attached to those endpoints.

The stream and readback rows are then forced into the same route. The $\StreamNameUp$ row $S$ supplies only finite observation windows, the $\RegSeqRatUp$ row $R$ reconstructs the corresponding rational readback, and the $\RealUp$ seal row $E$ is reached only after those finite reads are visible.

The compactness-facing rows are provenance and compatibility rows, not host compactness principles. The $\CompactMetricUp$ and $\HeineBorelFiniteNetUp$ entries in $P$ record finite-net compatibility and finite-net provenance before any downstream consumer can use the interval packet as compact data.

Finally $H,C,P,N$ transport, replay, source, and name that same lattice route. Since no carrier coordinate stores arbitrary open covers, selected subcovers, quotient real equality, countable choice, or ambient real completeness, a compact interval consumer can inspect only the displayed dossier lattice.
\end{proof}
