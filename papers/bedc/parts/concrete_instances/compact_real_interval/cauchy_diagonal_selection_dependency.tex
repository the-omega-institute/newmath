\begin{theorem}[Compact real interval Cauchy diagonal dependency]
\label{thm:compact-real-interval-cauchy-diagonal-dependency}
Every $\CompactRealIntervalUp$ finite-net or nested-window consumer that passes to $\CauchyDiagonalSelectionUp$ must first expose the interval endpoint rows, the located interval row, the compact finite-net row, the $\StreamNameUp$ window row, the $\RegSeqRatUp$ handoff row, the terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\CompactRealIntervalUp}$ row. The route may not import selected subsequences, open-cover compactness, quotient real equality, or ambient completeness.
\end{theorem}
\begin{proof}
Project the compact real interval carrier of \autoref{def:compact-real-interval-carrier}. The endpoint rows $A,B$, the order and located interval row $O$, the finite dyadic net schedule $W$, and the compact finite-net entries in $P$ are visible before any diagonal read is available.

The stream and rational readback rows are then forced into the Cauchy-facing order. The $\StreamNameUp$ row $S$ supplies finite observation windows, the $\RegSeqRatUp$ row $R$ supplies the rational handoff, and the $\RealUp$ row $E$ supplies the terminal seal.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since no carrier coordinate stores a selected subsequence, arbitrary open cover, quotient real equality, or ambient completeness principle, every compact-interval handoff to $\CauchyDiagonalSelectionUp$ is confined to the displayed rows.
\end{proof}
