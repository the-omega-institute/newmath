The $\CompactRealIntervalUp$ packet consumes the closed-bounded-interval source of \autoref{ch:concrete-instances-closedboundedinterval-namecert}, the finite interval-net rows internal to its compact interval carrier, $\StreamNameUp$ windows from \autoref{ch:concrete-instances-streamname-namecert}, $\RegSeqRatUp$ readback from \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. This dependency route keeps every compact-real-interval read tied to displayed interval endpoints, finite net data, stream observations, regular rational reconstruction, and local real naming before downstream compactness consumers inspect the packet.
