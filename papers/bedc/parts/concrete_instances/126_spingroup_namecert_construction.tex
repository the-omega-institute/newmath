\chapter{A Concrete Naming Certificate for $\SpinGroupUp$}
\label{ch:concrete-instances-spingroup-namecert}

The Spin-group interface $\SpinGroupUp$ packages the connected double cover of the orthogonal group as a subgroup of the Clifford-algebra units. It depends on $\NameCert_{\CliffordUp}$ (\autoref{ch:concrete-instances-clifford-namecert}) and $\NameCert_{\GroupUp}$ (\autoref{ch:concrete-instances-group-namecert}).

\section{Carrier definition}
\label{sec:spingroup-carrier}

\begin{definition}[SpinGroupUp BHist carrier packet]
\label{def:spingroup-bhist-carrier-packet}
Fix the $\NameCert_{\CliffordUp}$ source of
\autoref{ch:concrete-instances-clifford-namecert} and the
$\NameCert_{\GroupUp}$ source of
\autoref{ch:concrete-instances-group-namecert}. A
$\SpinGroupUp$ carrier packet is the finite $\mathsf{BHist}$ packet whose
visible rows consist of:
\begin{enumerate}
\item the Clifford unit row and its transported $\GroupUp$ identity row;
\item vector rows accepted by the Clifford source and their norm-one
classifier witnesses;
\item finite Clifford product rows closed under the carried group operation;
\item a cover row naming the induced orthogonal action on vector histories;
\item a subgroup row saying the accepted product histories lie inside the
Clifford unit group surface;
\item an action row recording conjugation of vector histories by accepted
spin histories;
\item a classifier row generated by the Clifford classifier, the Group
classifier, $\hsame$ endpoint transport, and finite $\Cont$ product
transport;
\item $\Pkg$ provenance rows for every Clifford source row, Group source row,
cover row, subgroup row, action row, classifier row, and finite ledger row.
\end{enumerate}
The packet is a BEDC carrier presentation. It does not identify spin histories
by a host quotient or by a topological connected-component predicate.
\end{definition}

\begin{definition}[SpinGroup root BHist carrier]
\label{def:spingroup-root-bhist-carrier}
The concrete $\SpinGroupUp$ root carrier is the finite $\mathsf{BHist}$ packet whose visible rows are all of the following:
\begin{enumerate}
  \item a carried $\CliffordUp$ endpoint row supplied by $\NameCert_{\CliffordUp}$;
  \item a singleton-history $\GroupUp$ law row supplied by $\NameCert_{\GroupUp}$ for the same visible endpoint shape;
  \item a unit-like Clifford endpoint row, namely a Clifford carrier row classified with the Clifford unit endpoint;
  \item $\Cont$ and $\hsame$ witnesses recording that the displayed multiplication and inverse rows are transported through the Clifford endpoint classifier.
\end{enumerate}
The packet contains no host orthogonal group, no quotient algebra, and no ambient matrix representation; its carrier evidence is exactly the listed $\mathsf{BHist}$, $\Cont$, $\hsame$, $\NameCert$, and $\mathsf{UnaryHistory}$ data.
\end{definition}

\begin{theorem}[SpinGroup source scope]
\label{thm:spingroup-root-carrier-source-scope}
Every row of \autoref{def:spingroup-root-bhist-carrier} is sourced through $\NameCert_{\CliffordUp}$ or $\NameCert_{\GroupUp}$. The Clifford endpoint, unit-like endpoint, product transport, and inverse transport rows are read from the Clifford certificate; the singleton-history associativity, unit, and inverse law rows are read from the group certificate over $\mathsf{UnaryHistory}$. No additional source row is admitted by the $\SpinGroupUp$ root carrier.
\end{theorem}
\begin{proof}
Unpack the root carrier packet. Each constructor in the packet names either a Clifford carrier row, a Clifford classifier row, a group singleton-history law row, or a transport witness connecting these rows.

The Clifford rows are discharged by the carrier, unit, product, and classifier clauses of $\NameCert_{\CliffordUp}$. The group law rows are discharged by the singleton-history law surface of $\NameCert_{\GroupUp}$, with the visible endpoint supplied by the same $\mathsf{UnaryHistory}$ spine.

It remains only to account for the transport witnesses. These are ordinary $\Cont$ and $\hsame$ rows whose endpoints are already present in the two certificates, so repacking them adds no third source.
\end{proof}

\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_source\_scope}

\begin{theorem}[Clifford unit lift for SpinGroup]
\label{thm:spingroup-root-clifford-unit-lift}
If a $\CliffordUp$ carrier row is classified with the Clifford unit endpoint, then the same row lifts to the $\SpinGroupUp$ root carrier by pairing it with the singleton $\GroupUp$ identity law row and the reflexive $\Cont$ and $\hsame$ transport witnesses. The lift preserves the visible Clifford endpoint classifier and records the row as unit-like inside the SpinGroup packet.
\end{theorem}
\begin{proof}
Start with the displayed Clifford carrier row and its classifier witness to the unit endpoint. The carrier clause of $\NameCert_{\CliffordUp}$ supplies the endpoint row, while classifier reflexivity supplies the stable unit comparison needed by the packet.

Next choose the singleton-history identity row from $\NameCert_{\GroupUp}$. Since the endpoint spine is the same unary history, the group row and the Clifford unit row share the visible boundary required by the root carrier.

Finally repack the Clifford row, the singleton identity law, and the reflexive $\Cont$ and $\hsame$ witnesses into \autoref{def:spingroup-root-bhist-carrier}. No multiplication or inverse row is consumed in this special case.
\end{proof}

\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_clifford\_unit\_lift}
\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_unit\_lift}

\begin{theorem}[SpinGroup group-law transport]
\label{thm:spingroup-root-group-law-transport}
Let $a$, $b$, $a'$, and $b'$ be $\SpinGroupUp$ root-carrier rows whose Clifford endpoints are classified by the $\CliffordUp$ endpoint classifier and whose group-law rows are singleton-history $\GroupUp$ rows. If $\hsame(a,a')$ and $\hsame(b,b')$ hold through the Clifford classifier, then multiplication and inverse rows transported by $\Cont$ remain inside the $\SpinGroupUp$ root carrier and carry the corresponding $\GroupUp$ multiplication and inverse laws.
\end{theorem}
\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_group\_law\_transport}
\begin{proof}
Unpack the two root-carrier rows. The Clifford parts expose carried endpoints and classifier witnesses, while the group parts expose the singleton-history multiplication and inverse law rows.

For multiplication, compose the two Clifford endpoint transports with the Clifford product transport supplied by $\NameCert_{\CliffordUp}$. The group singleton-history multiplication row is then read through $\NameCert_{\GroupUp}$ and placed over the transported endpoint.

For inverse, use the same endpoint classifier transport with the Clifford inverse endpoint row. The singleton-history inverse law supplies the group side, and the $\Cont$ witness repacks the result as a root-carrier inverse row.

The final packet is again of the form in \autoref{def:spingroup-root-bhist-carrier}: the visible Clifford endpoint has moved only by $\hsame$, and every group-law row remains a $\GroupUp$ row over the same unary history.
\end{proof}

\begin{theorem}[SpinGroup NameCert obligation surface]
\label{thm:spingroup-root-namecert-obligation-surface}
The rows in \autoref{def:spingroup-root-bhist-carrier}, together with \autoref{thm:spingroup-root-carrier-source-scope}, \autoref{thm:spingroup-root-clifford-unit-lift}, and \autoref{thm:spingroup-root-group-law-transport}, form the $\SpinGroupUp$ root $\NameCert$ obligation surface. The surface consists precisely of Clifford source scope, unit lift, multiplication transport, inverse transport, singleton-history group laws, and finite ledger exactness for these rows.
\end{theorem}
\begin{proof}
Read the obligation surface row by row. Source scope is supplied by \autoref{thm:spingroup-root-carrier-source-scope}; the unit face is supplied by \autoref{thm:spingroup-root-clifford-unit-lift}; multiplication and inverse transport are supplied by \autoref{thm:spingroup-root-group-law-transport}.

The classifier side is finite because every row is a named component of the carrier packet. Inverting a proposed $\SpinGroupUp$ root obligation therefore yields either a Clifford carrier/classifier row, a singleton-history $\GroupUp$ law row, or a displayed $\Cont$ or $\hsame$ witness between those endpoints.

Repacking these cases gives the stated root $\NameCert$ surface. No Spin or Pin global presentation, orthogonal action, matrix model, or host quotient algebra is used by this obligation surface.
\end{proof}
\begin{theorem}[SpinGroup root ledger exhaustion]
\label{thm:spingroup-root-ledger-exhaustion}
Every finite ledger row exposed by \autoref{def:spingroup-root-bhist-carrier} is one of the displayed $\mathsf{BHist}$ rows, $\Cont$ witnesses, $\hsame$ witnesses, $\NameCert_{\CliffordUp}$ source rows, $\NameCert_{\GroupUp}$ source rows, or singleton $\mathsf{UnaryHistory}$ group-law rows. Conversely, each displayed row of these forms is accepted by the root carrier ledger.
\end{theorem}
\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_ledger\_semantic\_exhaustion}
\begin{proof}
Project an arbitrary ledger row from the root carrier packet. Constructor inversion leaves only the four packet coordinates listed in \autoref{def:spingroup-root-bhist-carrier}: the Clifford endpoint row, the singleton-history group-law row, the unit-like Clifford endpoint row, and the displayed $\Cont/\hsame$ transport witnesses.

Apply \autoref{thm:spingroup-root-carrier-source-scope} to the source rows. It confines the Clifford side to $\NameCert_{\CliffordUp}$ and the group-law side to $\NameCert_{\GroupUp}$ over the same $\mathsf{UnaryHistory}$ spine.

For the converse direction, repack each displayed row through the same constructor projections. The unit-like case uses \autoref{thm:spingroup-root-clifford-unit-lift}, while product and inverse ledger rows use \autoref{thm:spingroup-root-group-law-transport}. Thus no ledger row lies outside the concrete BEDC packet, and no displayed packet row is omitted.
\end{proof}

\begin{theorem}[SpinGroup root transport closure]
\label{thm:spingroup-root-transport-closure}
Let $x$ and $y$ be rows of the concrete $\SpinGroupUp$ root carrier of \autoref{def:spingroup-root-bhist-carrier}. If $x\hsame y$ through the Clifford endpoint classifier and the connecting row is carried by $\Cont$ over the same $\mathsf{UnaryHistory}$ group-law spine, then every unit, multiplication, and inverse row admitted from $x$ transports to an admitted root-carrier row over $y$.
\end{theorem}
\begin{proof}
Unpack $x$ and $y$ as root carrier packets. Their Clifford endpoint rows are sourced by $\NameCert_{\CliffordUp}$, and their singleton-history group laws are sourced by $\NameCert_{\GroupUp}$ by \autoref{thm:spingroup-root-carrier-source-scope}.

The unit row transports first. If the row is the unit-like Clifford endpoint, \autoref{thm:spingroup-root-clifford-unit-lift} supplies the accepted target packet after the $\hsame$ comparison has moved the visible endpoint.

For multiplication and inverse, apply \autoref{thm:spingroup-root-group-law-transport}. The theorem composes the Clifford endpoint transport with the product or inverse $\Cont$ row and then reads the matching singleton-history group law from $\NameCert_{\GroupUp}$. The transported row is therefore again a row of \autoref{def:spingroup-root-bhist-carrier}.
\end{proof}
\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_transport\_closure}

\begin{theorem}[SpinGroup root NameCert threshold package]
\label{thm:spingroup-root-namecert-threshold-package}
The concrete root packet of \autoref{def:spingroup-root-bhist-carrier} reaches the $\NameCert_{\SpinGroupUp}$ threshold determined by $\NameCert_{\CliffordUp}$ and $\NameCert_{\GroupUp}$: its carrier rows are $\mathsf{BHist}$ rows, its classifier movement is by $\hsame$, its transport rows are by $\Cont$, its group-law evidence is singleton $\mathsf{UnaryHistory}$ evidence, and its ledger is exhausted by \autoref{thm:spingroup-root-ledger-exhaustion}.
\end{theorem}
\begin{proof}
Start with the obligation surface of \autoref{thm:spingroup-root-namecert-obligation-surface}. It identifies the required source, unit, multiplication, inverse, singleton-history group-law, and ledger faces of the root certificate.

The carrier and source faces are supplied by constructor projection from \autoref{def:spingroup-root-bhist-carrier} together with \autoref{thm:spingroup-root-carrier-source-scope}. The unit face is supplied by \autoref{thm:spingroup-root-clifford-unit-lift}.

The transport faces are supplied by \autoref{thm:spingroup-root-group-law-transport} and \autoref{thm:spingroup-root-transport-closure}. Finally, \autoref{thm:spingroup-root-ledger-exhaustion} shows that the ledger has exactly the displayed BEDC rows, so the package has no hidden orthogonal-group, matrix, or quotient-algebra obligation.
\end{proof}
\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_root\_namecert\_threshold\_package}

\input{parts/concrete_instances/spingroup/root_public_consumer_boundary}

\begin{definition}[SpinGroup double-cover interface row]
\label{def:spingroup-double-cover-interface-row}
A $\SpinGroupUp$ double-cover interface row is the finite $\mathsf{BHist}$ packet over \autoref{def:spingroup-root-bhist-carrier} whose visible coordinates are the Clifford endpoint row, the singleton $\GroupUp$ law row, the unit-like Clifford endpoint, the $\Cont$ and $\hsame$ transport witnesses between those endpoints, the $\mathsf{UnaryHistory}$ group-law spine, and the $\Pkg$ provenance rows used by the root carrier. The interface exports only these rows to the public double-cover boundary. It does not export a host Clifford quotient, a matrix realization, or an orthogonal-group primitive as an independent source.
\end{definition}

\begin{theorem}[SpinGroup double-cover source exhaustion]
\label{thm:spingroup-double-cover-source-exhaustion}
Every public double-cover row admitted by \autoref{def:spingroup-double-cover-interface-row} projects through the $\NameCert_{\CliffordUp}$, $\NameCert_{\GroupUp}$, $\Cont$, $\hsame$, $\mathsf{UnaryHistory}$, and $\Pkg$ rows already named by \autoref{thm:spingroup-root-carrier-source-scope}, \autoref{thm:spingroup-root-group-law-transport}, and \autoref{thm:spingroup-root-ledger-exhaustion}. Conversely, any row obtained from those named sources and repacked into the finite interface packet is accepted as a public double-cover row.
\end{theorem}
\begin{proof}
Project the public row from \autoref{def:spingroup-double-cover-interface-row}. Constructor inversion leaves the same displayed coordinates as the root carrier: the Clifford endpoint, the singleton group-law row, the unit-like endpoint, the transport witnesses, the unary history spine, and the provenance row.

The source classification is then inherited without a new case split. Apply \autoref{thm:spingroup-root-carrier-source-scope} to the Clifford and group-facing coordinates, and apply \autoref{thm:spingroup-root-group-law-transport} to the product and inverse transport coordinates.

It remains to account for ledger visibility. Apply \autoref{thm:spingroup-root-ledger-exhaustion}; it says that the displayed $\mathsf{BHist}$ rows, $\Cont$ rows, $\hsame$ rows, $\mathsf{UnaryHistory}$ rows, and $\Pkg$ provenance rows exhaust the root ledger. Repacking those rows into the double-cover interface gives the converse inclusion and leaves no host quotient, matrix, or orthogonal primitive outside the named BEDC sources.
\end{proof}
\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_double\_cover\_source\_exhaustion}

\begin{theorem}[SpinGroup NameCert root obligation package]
\label{thm:spingroup-namecert-root-obligation-package}
The interface of \autoref{def:spingroup-double-cover-interface-row} forms a root $\NameCert_{\SpinGroupUp}$ obligation package with the ten visible fields: carrier $\mathsf{BHist}$ row, Clifford source classifier, singleton $\GroupUp$ source classifier, $\hsame$ stability, $\Cont$ stability, unit endpoint, multiplication endpoint, inverse endpoint, exact finite ledger, and absence of a standard bridge claim. Each field is read from \autoref{def:spingroup-root-bhist-carrier}, \autoref{thm:spingroup-root-namecert-threshold-package}, and \autoref{thm:spingroup-double-cover-source-exhaustion}.
\end{theorem}
\begin{proof}
Start from the threshold package of \autoref{thm:spingroup-root-namecert-threshold-package}. It supplies the carrier, classifier movement, transport rows, singleton-history group-law evidence, and finite ledger exactness required of the root certificate.

Now refine the package into the ten visible fields. The carrier field is the finite $\mathsf{BHist}$ row of \autoref{def:spingroup-root-bhist-carrier}. The two source classifiers are supplied by \autoref{thm:spingroup-root-carrier-source-scope}. The $\hsame$ and $\Cont$ stability fields, together with unit, multiplication, and inverse endpoints, are supplied by \autoref{thm:spingroup-root-group-law-transport} and \autoref{thm:spingroup-root-transport-closure}.

The ledger and boundary fields are final projections. The ledger is exact by \autoref{thm:spingroup-root-ledger-exhaustion}, while \autoref{thm:spingroup-double-cover-source-exhaustion} shows that the public double-cover interface has no additional source row. Thus the obligation package contains the root certificate surface but does not assert a standard bridge.
\end{proof}
\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_root\_namecert\_obligation\_package}

\begin{theorem}[SpinGroup Clifford even-unit carrier obligation]
\label{thm:spingroup-clifford-even-unit-carrier-obligation}
The $\SpinGroupUp$ carrier is the $\mathsf{BHist}$-coded row of Clifford unit
data whose displayed product is even and whose projection lies in the
orthogonal action boundary. Its visible carrier entries are inherited from
$\NameCert_{\CliffordUp}$ and $\NameCert_{\GroupUp}$: Clifford unit rows, even
product witnesses, group operation and inverse rows, $\hsame$ transport,
$\Cont$ and $\Pkg$ provenance, and ledger entries naming the projection to the
orthogonal action interface. The carrier does not export a host Clifford
algebra element apart from these certified rows.
\end{theorem}
\begin{proof}
Read the Clifford unit rows from
\autoref{ch:concrete-instances-clifford-namecert}. They provide the visible
$\mathsf{BHist}$ product, unit, inverse, even-product, and projection rows used
by the spin carrier.

Read the group-facing operation boundary from
\autoref{ch:concrete-instances-group-namecert}. It supplies the multiplication,
unit, inverse, carrier transport, and ledger slots needed to regard the
Clifford unit rows as group rows.

Repack the shared $\hsame$, $\Cont$, $\Pkg$, and ledger provenance as the
carrier for $\SpinGroupUp$. The repacking keeps the Clifford and group rows
visible and records the orthogonal projection as a boundary row, so it adds no
analytic or host-level carrier beyond the displayed BEDC histories.
\end{proof}

\section{The certificate}
\label{sec:spingroup-certificate}

\begin{theorem}[SpinGroup carrier obligation]\label{thm:spingroup-carrier-obligation}
A $\SpinGroupUp$ certificate carries its object row as a $\mathsf{BHist}$ predicate over Clifford-even lift rows, rotation/projection endpoint rows, and a source row supplied by $\NameCert_{\CliffordUp}$. The carrier is not a host spin group: every admitted spin row is a finite BEDC packet whose visible fields are a Clifford carrier row, an even-parity $\mathsf{UnaryHistory}$ ledger, a product-unit row, and a projection row into the already certified orthogonal surface.
\end{theorem}
\begin{proof}
Read the carrier slot of the roadmap ten-field template. The traditional target is a spin lift, but the BEDC source is the $\NameCert_{\CliffordUp}$ row together with the group operation rows named by $\NameCert_{\GroupUp}$.

The $\mathsf{BHist}$ carrier predicate therefore accepts exactly the finite Clifford lift packet, the even-parity $\mathsf{UnaryHistory}$ ledger, and the projection endpoint row. The projection is read by $\Cont$ from the lift packet to the rotation endpoint, while $\hsame$ supplies only classifier transport between carried representatives.

No host spin group is introduced. The certificate surface exports the rows named by the $\NameCert$ dependencies and the displayed BEDC ledgers, so any downstream carrier use must project through those rows.
\end{proof}

\begin{theorem}[SpinGroup classifier obligation]\label{thm:spingroup-classifier-obligation}
Two $\SpinGroupUp$ carrier rows are classified exactly when their Clifford lift rows, double-cover projection rows, and even-Clifford ledger rows are compared by $\hsame$ in the same $\mathsf{BHist}$ scope. The classifier is componentwise over the lift, projection, and parity ledger; it does not identify two rows by appealing to an external Lie-group equality.
\end{theorem}
\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_classifier\_obligation}
\begin{proof}
Use the classifier field of the roadmap ten-field template after \autoref{thm:spingroup-carrier-obligation} has fixed the carried rows. The source classifier comes from $\NameCert_{\CliffordUp}$, and the visible projection endpoint is a $\mathsf{BHist}$ row, so the only available comparison is $\hsame$.

For two carrier packets, project their Clifford lift rows, their projection rows, and their even-parity $\mathsf{UnaryHistory}$ ledgers. Componentwise $\hsame$ on these three rows is stable under the $\NameCert$ transport fields, and the $\Cont$ projection row keeps the lift endpoint attached to the same public rotation surface.

Conversely, any classifier row for $\SpinGroupUp$ must be readable through these same projections, because the carrier has no additional equality field. Hence the displayed componentwise comparison is the whole classifier obligation.
\end{proof}

\begin{theorem}[SpinGroup lift parity ledger]\label{thm:spingroup-lift-parity-ledger}
The hidden ledger of a $\SpinGroupUp$ certificate records two rows: the even-Clifford parity row as $\mathsf{UnaryHistory}$ data over $\mathsf{BHist}$ words, and the two-to-one projection row as $\Cont$ data from the Clifford lift to the orthogonal endpoint. These rows are ledger data of the certificate, not new public carrier primitives.
\end{theorem}
\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_lift\_parity\_ledger}
\begin{proof}
Project the ledger field of the roadmap ten-field template. The Clifford source supplies word histories and product rows, while the spin certificate adds the parity and projection ledgers needed to read a lift as a double-cover point.

The parity row is unary because it records the even length of the Clifford word presentation. It is scoped as $\mathsf{UnaryHistory}$ over $\mathsf{BHist}$ rather than as an ambient integer parity predicate, and $\hsame$ transport is inherited from the Clifford word classifier.

The projection row is a $\Cont$ witness from the lift packet to the rotation endpoint. It is hidden ledger data because it witnesses how the public orthogonal row is read from the lift, while the public boundary remains the projection endpoint itself.
\end{proof}

\begin{theorem}[SpinGroup concrete lift closure]\label{thm:spingroup-concrete-lift-closure}
If two admitted $\SpinGroupUp$ lift rows are composable through the Clifford product ledger, then $\Cont$-composition of the two lift packets preserves the carrier and transports the classifier. The resulting packet has a Clifford-even lift row, a composed projection row, and a transported even-parity ledger in the same $\mathsf{BHist}$ certificate scope.
\end{theorem}
\begin{proof}
Unpack the operation and stability fields of the roadmap ten-field template. Each input lift is already a $\mathsf{BHist}$ packet with a Clifford carrier row, an even $\mathsf{UnaryHistory}$ ledger, and a projection row.

Compose the lift packets using the Clifford product continuation supplied by $\NameCert_{\CliffordUp}$. The two even-parity ledgers compose to another even ledger inside the same unary history scope, and the two projection rows compose by $\Cont$ to the corresponding orthogonal endpoint.

Classifier transport is componentwise. The lift comparison is transported by the Clifford $\NameCert$ classifier, the projection comparison by the composed $\Cont$ rows, and the parity comparison by $\hsame$ on the unary ledger. These are exactly the carrier fields of \autoref{thm:spingroup-carrier-obligation}.
\end{proof}
\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_concrete\_lift\_closure}

\begin{theorem}[SpinGroup spin double cover boundary]\label{thm:spingroup-spin-double-cover-boundary}
The public boundary of $\SpinGroupUp$ is the double-cover projection row from a Clifford-even lift to the already certified orthogonal or special-orthogonal surface. The boundary is a $\mathsf{BHist}$ projection ledger classified by $\hsame$ and transported by $\Cont$; it is not an external Lie group supplied outside the BEDC certificate.
\end{theorem}
\begin{proof}
Read the exactness and boundary fields of the roadmap ten-field template. The carrier and ledger obligations have already fixed the Clifford lift, the even $\mathsf{UnaryHistory}$ row, and the projection endpoint as the complete visible surface.

The public row is therefore the projection endpoint, together with its $\Cont$ witness from the Clifford-even lift. The orthogonal or special-orthogonal target is already certified by its own $\NameCert$ row, so $\SpinGroupUp$ exports only the double-cover boundary into that surface.

Exactness runs in the reverse direction. Any public spin-boundary use must recover the lift row, parity ledger, and projection continuation from the same $\mathsf{BHist}$ packet; rows outside those fields are not part of this certificate.
\end{proof}
\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_spin\_double\_cover\_boundary}

\begin{theorem}[SpinGroupUp root obligation surface]
\label{thm:spingroup-root-obligation-surface}
The $\SpinGroupUp$ NameCert obligation surface consists exactly of the carrier
packet of \autoref{def:spingroup-bhist-carrier-packet}, the generated
classifier on its accepted histories, stability under $\hsame$ and finite
$\Cont$ product transport, and the finite ledger rows naming unit, vector,
product, cover, subgroup, action, classifier, and $\Pkg$ provenance data.
In the roadmap ten-field template, the traditional target is the spin group
inside the Clifford units; the BEDC sources are the cited $\CliffordUp$ and
$\GroupUp$ certificates; the carrier, classifier, stability, ledger,
exactness, and status rows are precisely the rows displayed in the packet;
the standard bridge row is empty.
\end{theorem}
\begin{proof}
Read the carrier field from
\autoref{def:spingroup-bhist-carrier-packet}. The unit, vector, and product
rows are Clifford rows, while the identity and product laws are read through
the $\GroupUp$ source of \autoref{ch:concrete-instances-group-namecert}.

The classifier field is generated only from the two source classifiers,
$\hsame$ endpoint replacement, and finite $\Cont$ product transport. Thus
carrier stability follows componentwise: Clifford transport keeps the
word-level rows inside \autoref{ch:concrete-instances-clifford-namecert}, and
Group transport keeps the unit and product rows inside the group source.

The remaining roadmap fields are the finite ledger and exactness fields. The
cover, subgroup, action, and provenance rows are named in the packet, so any
public $\SpinGroupUp$ row is obtained by repacking those rows into the
ten-field certificate surface. No additional row is available for topology,
quotient equality, analytic connectedness, or a standard bridge.
\end{proof}
\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_root\_namecert\_obligation\_surface}

\begin{theorem}[SpinGroupUp Clifford unit carrier obligation]
\label{thm:spingroup-clifford-unit-carrier-obligation}
A $\SpinGroupUp$ carrier is a $\mathsf{BHist}$ packet whose visible rows are a
$\CliffordUp$ unit row, an orthogonal-action endpoint row, and a $\GroupUp$
carrier row. The packet exports no host Lie group, matrix equality, or ambient
Clifford algebra equality beyond the displayed BEDC rows.
\end{theorem}
\begin{proof}
Use the dependency surface stated at the start of this chapter. The
$\CliffordUp$ certificate supplies the unit row inside the Clifford-algebra
interface, while the $\GroupUp$ certificate supplies the public group-carrier
row read by downstream group consumers.

The $\SpinGroupUp$ carrier is the ten-field certificate packet that places
those two dependency rows beside the orthogonal-action endpoint row. Since the
carrier is a $\mathsf{BHist}$ packet, all equality and transport visible at the
interface is through $\hsame$ and the named classifier rows, not through a host
matrix or Lie-group equality.
\end{proof}
\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_clifford\_unit\_carrier\_obligation}

\input{parts/concrete_instances/spingroup/ledger_exclusion_surface}

\begin{theorem}[SpinGroupUp Clifford-unit source obligation]
\label{thm:spingroup-clifford-unit-source-obligation}
A $\SpinGroupUp$ source row is a displayed $\CliffordUp$ unit endpoint together
with even-product provenance, a $\GroupUp$ multiplication row, and
$\mathsf{BHist}$, $\Cont$, and $\mathsf{Pkg}$ ledgers tying the unit read and
the product read to the same carried source. The row records multiplication
and inverse access only through the inherited Clifford-unit and group
certificates, and it does not assert an uncarried host-level spin subgroup.
\end{theorem}
\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_clifford\_unit\_source\_obligation}
\begin{proof}
Read the Clifford dependency as a unit endpoint in the $\CliffordUp$ carrier.
The even-product provenance is a finite $\mathsf{BHist}$ ledger whose factors
are carried Clifford generators and whose product endpoint is the displayed
unit row.

Read the group dependency as the multiplication and inverse package on those
unit endpoints. The $\GroupUp$ row supplies the operation laws only for the
displayed carried endpoints, while $\Cont$ records the product computation and
$\mathsf{Pkg}$ records the shared provenance.

Combining the two dependency reads gives the $\SpinGroupUp$ source row. Every
field is inherited from $\CliffordUp$, $\GroupUp$, $\mathsf{BHist}$, $\Cont$,
and $\mathsf{Pkg}$, so no ambient subgroup predicate or external Clifford
algebra quotient is added.
\end{proof}

\begin{theorem}[SpinGroupUp double-cover fiber classifier]
\label{thm:spingroup-double-cover-fiber-classifier}
The double-cover classifier for $\SpinGroupUp$ compares two carried spin
endpoints by their Clifford-unit carrier rows together with the shared
$\mathsf{OrthogonalGroup}^{\uparrow}$ action endpoint. Two spin rows are in the same fiber
exactly when the Clifford-unit comparisons and the displayed orthogonal-action
readback agree in the package ledger; the classifier does not quotient by
host-level signs or by an unrecorded kernel object.
\end{theorem}
\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_double\_cover\_fiber\_classifier}
\begin{proof}
Unpack two $\SpinGroupUp$ source rows using
\autoref{thm:spingroup-clifford-unit-source-obligation}. Each row exposes a
carried Clifford unit, even-product provenance, multiplication and inverse
rows, and package provenance.

Compare the Clifford-unit coordinates with the inherited $\CliffordUp$
classifier. Then compare the displayed orthogonal-action endpoint using the
shared action readback recorded in the same $\mathsf{Pkg}$ ledger.

The fiber classifier is the conjunction of these displayed comparisons. Since
the sign ambiguity is represented only by carried Clifford-unit rows and their
action endpoint, the construction never forms a host sign quotient or an
unrecorded double-cover kernel.
\end{proof}

\begin{theorem}[SpinGroupUp group consumer boundary]
\label{thm:spingroup-group-consumer-boundary}
Downstream $\LieGroupUp$ or $\PinGroupUp$ consumers of $\SpinGroupUp$ may read
only the Clifford unit row, multiplication and inverse rows, the double-cover
classifier of \autoref{thm:spingroup-double-cover-fiber-classifier}, and
$\NameCert$ provenance. They may not inspect a host Lie group, a host sign
quotient, or an uncarried Clifford algebra carrier outside the displayed BEDC
source package.
\end{theorem}
\begin{proof}
The source obligation exposes the carried unit and the group operation rows.
These are sufficient for group-style consumers to read multiplication,
inverse, identity provenance, and the finite even-product ledger.

The double-cover classifier exposes exactly the additional fiber data used by
geometry consumers: the Clifford-unit comparison and the shared orthogonal
action endpoint. The consumer therefore reads a package of named
$\mathsf{BHist}$ rows, not an ambient quotient.

The $\NameCert$ provenance binds these rows to the $\SpinGroupUp$ interface.
Any $\LieGroupUp$ or $\PinGroupUp$ consumer that needs more structure must add
its own displayed source rows rather than reading an unrecorded host object
through this boundary.
\end{proof}

\begin{theorem}[SpinGroup even-product classifier obligation]
\label{thm:spingroup-even-product-classifier-obligation}
The $\SpinGroupUp$ classifier is inherited from the Clifford unit classifier
together with group classifier transport. For carried spin rows it is stable
under multiplication, inverse, and replacement of even-product witnesses, and
it preserves the projection ledger into the orthogonal action boundary. The
classifier surface uses only $\mathsf{BHist}$ rows, $\hsame$ transport,
$\Cont/\Pkg$ provenance, $\NameCert$ fields, and the Clifford and group ledgers.
\end{theorem}
\begin{proof}
Apply \autoref{ch:concrete-instances-clifford-namecert} to read the Clifford
unit classifier and the even-product witness transport rows. These rows compare
the displayed Clifford products through certified $\mathsf{BHist}$ data.

Apply \autoref{ch:concrete-instances-group-namecert} to transport the same
classifier through multiplication and inverse. The group boundary supplies the
operation and inverse stability rows without replacing the BEDC comparison by
host equality.

The projection ledger is carried along the same $\Cont$ and $\Pkg$ provenance
used by the carrier theorem. Thus multiplication, inverse, and even-witness
replacement preserve the classifier and leave the orthogonal action boundary
unchanged.
\end{proof}

\begin{theorem}[SpinGroup NameCert obligation surface]
\label{thm:spingroup-namecert-obligation-surface}
The carrier row of
\autoref{thm:spingroup-clifford-even-unit-carrier-obligation} and the
classifier row of
\autoref{thm:spingroup-even-product-classifier-obligation} assemble the
$\NameCert_{\SpinGroupUp}$ obligation surface. This surface contains the
Clifford even-unit carrier, group transport rows, classifier stability, inverse
and multiplication stability, even-product witness stability, projection
ledger, $\hsame$ transport, $\Cont/\Pkg$ provenance, and the exact visible
ledger boundary. It explicitly does not assert connected Lie topology,
smooth-manifold structure, analytic covering data, or a topological double
cover theorem.
\end{theorem}
\begin{proof}
Start with the carrier theorem. It fixes the accepted histories as Clifford
unit rows with even-product witnesses and an orthogonal-action projection
ledger, all read through \autoref{ch:concrete-instances-clifford-namecert} and
\autoref{ch:concrete-instances-group-namecert}.

Then apply the classifier theorem. It supplies classifier transport under
group multiplication and inverse and preserves the even-product and projection
ledger rows.

Repack these rows into the $\NameCert$ fields: source, carrier, classifier,
stability, exactness, and ledger. Since every row is a $\mathsf{BHist}$,
$\hsame$, $\Cont$, or $\Pkg$ row inherited from the Clifford and group
boundaries, the resulting $\SpinGroupUp$ surface is a BEDC certificate surface
only and carries no Lie-topological or analytic covering assertion.
\end{proof}

\begin{theorem}[SpinGroup spin-boundary inclusion transport]
\label{thm:spingroup-spin-boundary-inclusion-transport}
Let a $\SpinGroupUp$ root carrier row be admitted by
\autoref{thm:spingroup-root-carrier-source-scope}. Transport it by $\hsame$
along the Clifford endpoint and by $\Cont$ along the shared unary group-law
spine. The transported row is still an admitted Spin boundary row: its Clifford
unit component, even-word component, and singleton group-law component remain
inside the $\mathsf{BHist}$ surface named by the SpinGroup certificate.
\end{theorem}
\begin{proof}
Start with the root source scope of
\autoref{thm:spingroup-root-carrier-source-scope}. It exposes the accepted row
only as a Clifford source row together with the displayed group-law spine.

Apply \autoref{thm:spingroup-root-transport-closure}. The $\hsame$ Clifford
endpoint transport preserves the unit and even-word witnesses, and the
$\Cont$ group-law transport preserves the singleton group interface.

Finally apply \autoref{thm:spingroup-group-interface-boundary}. It identifies
the transported unary group-law spine with the same boundary read by the
SpinGroup carrier. Repacking these transported rows gives an admitted Spin
boundary row and adds no host orthogonal group or quotient row.
\end{proof}

\begin{theorem}[SpinGroup Clifford-unit ledger coverage]
\label{thm:spingroup-clifford-unit-ledger-coverage}
Every ledger row visible at the $\SpinGroupUp$ spin boundary is one of the
following finite $\mathsf{BHist}$ rows: a Clifford unit row, a singleton Group
law row, a $\Cont$ transport row, an $\hsame$ transport row, or a $\Pkg$
provenance row. No spin-boundary ledger row is supplied by an ambient
orthogonal group, matrix model, quotient algebra, or analytic covering datum.
\end{theorem}
\begin{proof}
Apply \autoref{thm:spingroup-root-ledger-exhaustion}. It exhausts the root
ledger by the visible Clifford endpoint, even-product witness, group-law
transport, $\Cont$ readback, $\hsame$ comparison, and $\Pkg$ provenance rows.

Then apply \autoref{thm:spingroup-clifford-unit-carrier-obligation}. This
identifies the Clifford side of the ledger with unit rows already accepted by
the consumed $\CliffordUp$ certificate.

The remaining ledger entries are the singleton group-law row and the transport
or provenance rows listed by the root exhaustion theorem. Hence every visible
row has one of the five forms in the statement, and there is no further
external ledger source to inspect.
\end{proof}

\begin{theorem}[SpinGroup PinGroup consumer source exactness]
\label{thm:spingroup-pin-consumer-source-exactness}
The $\PinGroupUp$ spin-inclusion consumer reads exactly the $\SpinGroupUp$
boundary rows: the Clifford unit rows, singleton group-law rows, $\Cont$
transport rows, $\hsame$ transport rows, and $\Pkg$ provenance rows exposed by
the SpinGroup certificate. It reads no host orthogonal group, matrix model,
quotient algebra, analytic covering datum, or separate source outside that
boundary.
\end{theorem}
\begin{proof}
First use \autoref{thm:spingroup-spin-boundary-inclusion-transport}. It shows
that the rows transported into the consumer remain Spin boundary rows rather
than host-level Clifford or group equalities.

Next use \autoref{thm:spingroup-clifford-unit-ledger-coverage}. The ledger
visible to the consumer is exhausted by Clifford unit, singleton group-law,
$\Cont$, $\hsame$, and $\Pkg$ rows.

Finally apply \autoref{thm:spingroup-namecert-obligation-surface} and
\autoref{thm:pingroup-spingroup-inclusion-surface}. The SpinGroup obligation
surface is the exported boundary, and the PinGroup inclusion surface consumes
that boundary as its source. Therefore the consumer source is exact and admits
none of the excluded host, model, quotient, analytic, or covering data.
\end{proof}

\begin{theorem}[SpinGroup root threshold obligation triple]
\label{thm:spingroup-root-threshold-obligation-triple}
An accepted $\SpinGroupUp$ root packet exposes exactly three threshold rows:
the $\mathsf{BHist}$ Clifford unit carrier row, the even-word parity row, and
the group-law transport row. These rows are visible only through $\Cont$,
$\hsame$, and $\NameCert$ provenance from
\autoref{def:spingroup-bhist-carrier-packet} and
\autoref{def:spingroup-root-bhist-carrier}. No additional root row is available
outside the threshold package of
\autoref{thm:spingroup-root-namecert-threshold-package} and the obligation
surface of \autoref{thm:spingroup-namecert-obligation-surface}.
\end{theorem}
\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_threshold\_obligation\_triple}
\begin{proof}
Project the accepted packet through
\autoref{def:spingroup-root-bhist-carrier}. Its constructors expose only the
Clifford endpoint row, the singleton group-law row, the unit-like Clifford row,
and the displayed $\Cont/\hsame$ transport witnesses.

Next compare this projection with the wider packet of
\autoref{def:spingroup-bhist-carrier-packet}. The wider packet contributes the
finite Clifford product rows and the parity ledger, while
\autoref{thm:spingroup-root-namecert-threshold-package} confines them to the
same $\NameCert_{\CliffordUp}$ and $\NameCert_{\GroupUp}$ provenance.

Finally use \autoref{thm:spingroup-namecert-obligation-surface}. It repacks
the carrier, classifier, stability, exactness, and ledger faces and leaves only
the Clifford unit carrier, even-word parity, and group-law transport as the
root threshold rows. Thus the packet has no host quotient, ambient orthogonal
group equality, or matrix-model row.
\end{proof}

\begin{theorem}[SpinGroup root even-word classifier inversion]
\label{thm:spingroup-root-even-word-classifier-inversion}
Any accepted classifier comparison between $\SpinGroupUp$ root packets projects
to the even Clifford word row, the inherited group carrier row, and the
double-cover fiber row. The inversion is internal to the displayed
$\mathsf{BHist}$ packet: it reads the even-word component from
\autoref{def:spingroup-bhist-carrier-packet}, the group carrier component from
\autoref{def:spingroup-root-bhist-carrier}, and the fiber component from
\autoref{thm:spingroup-double-cover-fiber-classifier}. It does not use a host
quotient or an ambient orthogonal group equality.
\end{theorem}
\leanchecked{BEDC.Derived.SpinGroupUp.SpinGroupRootCarrier\_even\_word\_classifier\_inversion}
\begin{proof}
Start with the accepted classifier comparison and project it through
\autoref{thm:spingroup-namecert-obligation-surface}. The classifier face is a
BEDC row assembled from $\hsame$, $\Cont$, $\Pkg$, and the displayed Clifford
and group certificate rows.

The even component is obtained by projecting the Clifford product part of
\autoref{def:spingroup-bhist-carrier-packet}. Because
\autoref{thm:spingroup-root-namecert-threshold-package} fixes the root source
surface, this projection is the even Clifford word row rather than an ambient
parity predicate.

The inherited group carrier row is the singleton-history group-law component
of \autoref{def:spingroup-root-bhist-carrier}. The remaining projection is the
double-cover fiber classifier supplied by
\autoref{thm:spingroup-double-cover-fiber-classifier}. These three projected
rows exhaust the accepted comparison, so no host quotient or external
orthogonal equality is read.
\end{proof}

\input{parts/concrete_instances/spingroup/double_cover_consumer_boundary}
\input{parts/concrete_instances/spingroup/root_boundary_consumer_exactness_and_status}
