\chapter{A Concrete Naming Certificate for $\RealCauchyCompletionUp$}
\label{ch:concrete-instances-real-cauchy-completion-namecert}
\origin{human}

The $\RealCauchyCompletionUp$ interface names the Bishop--Bridges constructive Cauchy completion surface for real names. It sits above $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and the diagonal packet $\RealDiagonalCompletionUp$, and it exports only the $\RealUp$ seal rows that can be read through those certificates. The chapter records a concrete completion object for the real-and-completion phase without forming quotient classes of regular streams, selecting limits by choice, or appealing to host real equality.

The regular-family input side is consumed through the already licensed $\RegularCauchyNameUp$ boundary of \autoref{ch:concrete-instances-regularcauchyname-namecert}. That boundary packages the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ seal route that $\RealCauchyCompletionUp$ uses as its visible regular-family input surface.

\begin{definition}[RealCauchyCompletion carrier]
\label{def:real-cauchy-completion-carrier}
A $\RealCauchyCompletionUp$ carrier is a finite $\mathsf{BHist}$ packet
$$
C=(X,\mu,D,E,P,N)
$$
with the following visible coordinates:
\begin{enumerate}
\item $X$ is a $\RegSeqRatUp$-indexed regular Cauchy family, with every inspected member presented through finite $\StreamNameUp$ windows and $\DyadicRatCoreUp$ rational observations;
\item $\mu$ is the displayed unary modulus row that assigns a late family member to each requested precision;
\item $D$ is the associated $\RealDiagonalCompletionUp$ packet, including its diagonal selector, scheduled windows, and regular-sequence readback ledger;
\item $E$ is the consumer-facing $\RealUp$ seal row attached to the diagonal representative;
\item $P$ is the $\Pkg$ provenance row naming the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealDiagonalCompletionUp$, and $\RealUp$ source certificates;
\item $N$ is the local $\NameCert_{\RealCauchyCompletionUp}$ row.
\end{enumerate}
The local classifier compares two carriers only by displayed $\hsame$ transport of the modulus rows, diagonal packets, readback ledgers, $\RealUp$ seal rows, package provenance, and local certificate rows.
\end{definition}
\leandef{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier}

\begin{theorem}[RealCauchyCompletion NameCert obligations]
\label{thm:real-cauchy-completion-namecert-obligations}
The $\NameCert_{\RealCauchyCompletionUp}$ surface consists of five obligations: carrier habitation by the packet of \autoref{def:real-cauchy-completion-carrier}; classifier stability under displayed $\hsame$ transport and $\Cont$ routes; diagonal handoff through $\RealDiagonalCompletionUp$; non-escape of quotient, choice, host real equality, and ambient completeness data; and the standard Bishop reading as a constructive Cauchy completion over visible regular rational names.
\end{theorem}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_namecert\_obligation\_surface}
\begin{proof}
Carrier habitation is the assembly of the displayed family, modulus, diagonal packet, real seal, package provenance, and local certificate row. The classifier is componentwise because each coordinate is a visible finite row, and the only permitted transports are the stored $\hsame$ comparisons followed by the recorded $\Cont$ routes.

The diagonal handoff is delegated to $\RealDiagonalCompletionUp$: every consumer read of the candidate limit first passes through the modulus, diagonal selector, scheduled $\StreamNameUp$ window, and $\RegSeqRatUp$ readback ledger. The carrier therefore exposes only the rows named by $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealDiagonalCompletionUp$, $\RealUp$, $\Pkg$, and $\NameCert$.

The Bishop reading is exactly this finite witness surface. It supplies a constructive regular-Cauchy completion interface while withholding quotient stream equality, choice-selected limits, host real equality, and any ambient completeness theorem not carried by the named certificate rows.
\end{proof}

\begin{theorem}[RealCauchyCompletion diagonal handoff]
\label{thm:real-cauchy-completion-diagonal-handoff}
For every accepted $\RealCauchyCompletionUp$ carrier $C$ and every unary precision request $n$, the $\RealUp$ consumer read of the completed name factors through the modulus row $\mu(n)$, the associated $\RealDiagonalCompletionUp$ packet $D$, one finite $\StreamNameUp$ window, one $\RegSeqRatUp$ readback row, and the displayed $\DyadicRatCoreUp$ observation ledger.
\end{theorem}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_diagonal\_handoff}
\begin{proof}
Project the carrier by \autoref{def:real-cauchy-completion-carrier}. The modulus coordinate selects the late member of the regular Cauchy family that is relevant for the request $n$.

Use the diagonal packet coordinate $D$ to read the chosen member through its diagonal selector. The selected finite stream window and its regular-sequence readback are exactly the rows exposed by the $\RealDiagonalCompletionUp$ ledger.

Attach the $\RealUp$ seal row $E$ to that readback and retain the package and local certificate provenance. Since the consumer reaches $E$ only along this displayed route, the handoff factors through the stated finite rows.
\end{proof}

\begin{theorem}[RealCauchyCompletion non-escape boundary]
\label{thm:real-cauchy-completion-non-escape-boundary}
An accepted $\RealCauchyCompletionUp$ carrier exports no quotient of regular streams, no host real carrier, no host real equality, no choice-selected limit, and no uniqueness principle beyond the displayed $\RealUp$ classifier rows. Every exported read is mediated by $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealDiagonalCompletionUp$, $\RealUp$, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ data.
\end{theorem}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_non\_escape\_boundary}
\begin{proof}
Inspect the carrier coordinates. Each field is either a finite history row, a displayed transport route, a package provenance row, a local certificate row, or one of the named dependency certificates.

The diagonal coordinate already records its own non-escape boundary: it exposes selected windows and readbacks, not quotient representatives or host limits. Passing through $\RealUp$ adds only the seal row and its classifier-visible comparisons.

Thus a downstream consumer can cite precisely the listed rows. There is no coordinate from which quotient stream equality, host real equality, a selected limit, or an ambient completeness object can be recovered.
\end{proof}

\begin{theorem}[RealCauchyCompletion standard Bishop reading]
\label{thm:real-cauchy-completion-standard-bishop-reading}
Read in the Bishop--Bridges constructive-analysis sense, $\RealCauchyCompletionUp$ is the completion interface for regular Cauchy families over rational names: the input is a $\RegSeqRatUp$-indexed regular Cauchy family with an explicit modulus, and the output is the $\RealUp$ name obtained by diagonal extraction and finite ledger transport through $\RealDiagonalCompletionUp$.
\end{theorem}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_standard\_bishop\_reading}
\begin{proof}
The input side is fully displayed: the regular Cauchy family is a $\RegSeqRatUp$ row family, and the modulus is a unary row stored in the carrier. No convergence fact is inferred from an ambient metric space or from an external completeness principle.

The output side is the diagonal representative already carried by $\RealDiagonalCompletionUp$. Its windows are finite $\StreamNameUp$ reads, its rational observations are $\DyadicRatCoreUp$ rows, and its real-facing surface is the $\RealUp$ seal.

This is the Bishop-style reading of completion as an explicit transformation from regular Cauchy data to a real name with a visible modulus and ledger. The certificate records the construction interface and its boundary, leaving machine checking of the full real-completion theorem to the corresponding Lean namespace.
\end{proof}

\begin{theorem}[RealCauchyCompletion carrier transport stability]
\label{thm:real-cauchy-completion-carrier-transport-stability}
Let $C=(X,\mu,D,E,P,N)$ be an accepted $\RealCauchyCompletionUp$ carrier. Any displayed $\hsame$ transport that preserves the $X$, $\mu$, $D$, $E$, $P$, and $N$ coordinates through the $\RegSeqRatUp$, $\RealDiagonalCompletionUp$, $\RealUp$, $\Pkg$, $\Cont$, and $\NameCert$ rows preserves both the $\RealCauchyCompletionUp$ carrier predicate and the local classifier. In particular, transported packets are compared only by the same component rows that classified the original packet.
\end{theorem}
\begin{proof}
Project the carrier by \autoref{def:real-cauchy-completion-carrier}. The regular family coordinate $X$ and the modulus coordinate $\mu$ are transported through the displayed $\RegSeqRatUp$ rows, so the input side remains a visible regular Cauchy family with the same precision schedule.

The diagonal coordinate $D$ is transported through $\RealDiagonalCompletionUp$, and the seal coordinate $E$ is transported through $\RealUp$. The recorded $\Cont$ routes carry the finite readback ledgers along those transports, so the consumer-facing route continues to expose the same scheduled stream windows and rational observations.

Finally, $P$ and $N$ transport through the package and local certificate rows. Since the classifier of \autoref{def:real-cauchy-completion-carrier} compares exactly these displayed coordinates, componentwise transport preserves accepted carriers and preserves classifier equivalence.
\end{proof}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_transport\_stability}

\begin{theorem}[RealCauchyCompletion modulus diagonal exactness]
\label{thm:real-cauchy-completion-modulus-diagonal-exactness}
For an accepted $\RealCauchyCompletionUp$ carrier $C=(X,\mu,D,E,P,N)$ and a unary precision request $n$, the row $\mu(n)$ determines the displayed diagonal packet used for that request together with its finite $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ readback rows. The route contains no quotient of regular streams, no host real equality, and no choice-selected limit.
\end{theorem}
\begin{proof}
The modulus row is a coordinate of the carrier, not an external completeness principle. Evaluating it at $n$ selects the late regular-sequence member whose readback must be inspected for that precision.

The diagonal coordinate $D$ then gives the corresponding displayed packet: a scheduled stream window, the regular-sequence ledger for that window, and the dyadic rational observation rows used by the readback. These rows are finite history data already named by the carrier provenance.

Because the selected route is exhausted by $\mu$, $D$, and the finite readback ledgers, there is no remaining operation that could form quotient stream equality, appeal to host real equality, or select a limit by choice.
\end{proof}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_modulus\_diagonal\_exactness}

\begin{theorem}[RealCauchyCompletion real seal readback scope]
\label{thm:real-cauchy-completion-real-seal-readback-scope}
The $\RealUp$ seal exported by an accepted $\RealCauchyCompletionUp$ carrier is consumed only through the diagonal readback route named by $\mu$ and $D$. Every consumer read of the seal is mediated by the displayed finite $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\Cont$, $\Pkg$, and $\NameCert$ packet rows.
\end{theorem}
\begin{proof}
The seal coordinate $E$ is attached to the diagonal representative carried by $D$. A consumer therefore reaches $E$ only after the modulus row has selected the relevant regular-sequence member and the diagonal ledger has exposed the finite window and rational observation rows.

The $\Cont$ routes record how those rows are read without leaving the displayed packet. The package provenance $P$ and local certificate row $N$ bind the dependency certificates that make the route public to downstream consumers.

Thus the exported seal has a restricted readback scope: it is a real-facing boundary for the named diagonal packet, not an independent host real, quotient class, or ambient completion object.
\end{proof}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_real\_seal\_readback\_scope}

\begin{theorem}[RealCauchyCompletion stream-regseq-tail seal factorization]
\label{thm:real-cauchy-completion-stream-regseq-tail-seal-factorization}
Let $C=(X,\mu,D,E,P,N)$ be an accepted $\RealCauchyCompletionUp$ carrier. Every $\RealUp$-and-completion consumer read of the exported seal factors through a finite $\StreamNameUp$ window, a $\RegSeqRatUp$ readback row, a $\DyadicRatCoreUp$ observation ledger, the diagonal packet $D$, and then the $\RealUp$ seal row $E$.

The exported route is exactly the displayed completion chain carried by $C$. It exports no quotient of regular streams, no host real equality, no selected limit, and no ambient completeness principle.
\end{theorem}
\begin{proof}
Project the carrier by \autoref{def:real-cauchy-completion-carrier}. The coordinates $X$ and $\mu$ supply the visible regular-family input and the finite precision request that selects the relevant stream window.

Apply the diagonal handoff of \autoref{thm:real-cauchy-completion-diagonal-handoff}. The selected request reaches the diagonal packet $D$ only through the $\StreamNameUp$ window, the $\RegSeqRatUp$ readback row, and the $\DyadicRatCoreUp$ observation ledger already recorded in the carrier route.

Finally use \autoref{thm:real-cauchy-completion-non-escape-boundary} and \autoref{thm:real-cauchy-completion-real-seal-readback-scope}. These rows identify $E$ as the only exported $\RealUp$ seal reached by the route, while excluding quotient stream equality, host real equality, choice of a selected limit, and ambient completeness from the consumer surface.
\end{proof}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_stream\_regseq\_tail\_seal\_factorization}

\begin{theorem}[RealCauchyCompletion tail selector fusion]
\label{thm:real-cauchy-completion-tail-selector-fusion}
Let $C=(X,\mu,D,E,P,N)$ be an accepted $\RealCauchyCompletionUp$ carrier whose completion read is served by a $\RegularCauchyNameUp$ source and by a $\CauchyLimitSealUp$ tail-budget meet over the same finite $\StreamNameUp$ window. Every $\RealUp$ seal read of $C$ factors through one finite route consisting of the $\StreamNameUp$ tail-compatible window request, the $\RegularCauchyNameUp$ dyadic-radius ledger, the $\CauchyLimitSealUp$ tail-budget meet, the $\RealUp$ name-seal source factorization, and the visible $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The fused route is exactly the displayed finite consumer packet. It has no quotient stream equality, no host real equality, no selected limit, no metric-completeness datum, and no ambient completeness coordinate.
\end{theorem}
\begin{proof}
Start with \autoref{thm:real-cauchy-completion-stream-regseq-tail-seal-factorization}. It places every completion-facing seal read on the route through the selected finite stream window, regular-sequence readback row, dyadic observation ledger, diagonal packet, and exported $\RealUp$ seal.

Next use the StreamName tail-compatible request and coverage rows, \autoref{thm:streamname-tail-compatible-window-request} and \autoref{thm:streamname-tail-compatible-window-coverage}. They identify the finite window as the certified observation index, tail-selector route, finite-window budget surface, pointwise $\RegSeqRatUp$ handoff, and shared support rows.

Attach the CauchyLimitSeal component by \autoref{thm:cauchy-limit-seal-tail-budget-meet} and \autoref{thm:cauchy-limit-seal-tail-budget-meet-exactness}. The selector row, observation-budget row, dyadic ledger, regular handoff, real seal, transports, continuations, provenance, and local name are therefore read inside the same tail-budget meet.

Finally apply \autoref{thm:real-name-seal-source-factorization}. Repacking the shared StreamName, RegularCauchyName, CauchyLimitSeal, and Real name-seal rows gives one finite consumer route over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, with the listed quotient, host-equality, selected-limit, metric-completeness, and ambient-completeness coordinates absent.
\end{proof}

\begin{theorem}[RealCauchyCompletion RegularCauchyName terminal handoff]
\label{thm:real-cauchy-completion-regularcauchyname-terminal-handoff}
Let $C=(X,\mu,D,E,P,N)$ be an accepted $\RealCauchyCompletionUp$ carrier whose regular-family input is read through an accepted $\RegularCauchyNameUp$ source packet $G=(S,Q,\rho,L,E_G,P_G,N_G)$. Every completion-facing $\RealUp$ seal read of $C$ factors through the RegularCauchyName finite window ledger $L$, the dyadic-radius row $\rho$, the $\StreamNameUp$ schedule $S$, the $\RegSeqRatUp$ readback row, the $\RealDiagonalCompletionUp$ packet $D$, and the $\RealCauchyCompletionUp$ seal row $E$.

The handoff exports no quotient stream, no selected host limit, no host real equality, no ambient completeness row, and no metric-completeness row. It is a finite certificate route through the displayed $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ data of the two accepted packets.
\end{theorem}
\begin{proof}
Start from the accepted $\RegularCauchyNameUp$ packet $G$. By \autoref{thm:regularcauchyname-window-ledger-exhaustion}, every precision read of $G$ is exhausted by the displayed finite route through the stream schedule, dyadic observation row, dyadic-radius row, finite ledger, seal row, package provenance, and local certificate.

Use \autoref{thm:regularcauchyname-real-seal-factorization} to identify the real-facing endpoint of that route. The $\RealUp$ seal exposed by $G$ is reached only after the same dyadic-radius row, $\StreamNameUp$ handoff, and $\RegSeqRatUp$ readback have been displayed as certificate rows.

Now pass to the accepted completion carrier $C$. By \autoref{thm:real-cauchy-completion-stream-regseq-tail-seal-factorization}, the completion-facing seal read factors through the selected finite stream window, the regular-sequence readback row, the dyadic observation ledger, the diagonal packet $D$, and then the exported $\RealUp$ seal row $E$.

Combining the two routes gives the terminal handoff from the RegularCauchyName finite ledger into the RealDiagonalCompletion packet and then into the RealCauchyCompletion seal row. Finally, \autoref{thm:real-cauchy-completion-non-escape-boundary} excludes quotient streams, selected host limits, host real equality, ambient completeness, and metric-completeness data from the exported surface.
\end{proof}

\begin{theorem}[RealCauchyCompletion ProbeBundle window coverage]
\label{thm:real-cauchy-completion-probebundle-window-coverage}
Let $C=(X,\mu,D,E,P,N)$ be an accepted $\RealCauchyCompletionUp$ carrier, and let $Q$ be a finite $\mathsf{ProbeBundle}$ of unary precision requests. For every request $n$ bundled in $Q$, the consumer read factors through the displayed row $\mu(n)$, the associated $\RealDiagonalCompletionUp$ packet, one finite $\StreamNameUp$ window, one $\RegSeqRatUp$ readback row, and the corresponding $\DyadicRatCoreUp$ observation ledger.

Moreover, the whole bundled family repacks as a single finite consumer surface over $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The assembled surface carries the local $\mathsf{SemanticNameCert}$ whose carrier is the bundled row satisfying $\hsame$ with the surface, $\UnaryHistory$, and $\PkgSig$, whose pattern forgets only the $\PkgSig$ row, whose ledger records $\PkgSig$ together with $\hsame$, and whose classifier is $\hsame$. This surface contains no quotient stream, no selected host limit, no host real equality, and no ambient completeness object.
\end{theorem}
\begin{proof}
Project the carrier by \autoref{def:real-cauchy-completion-carrier}. For each bundled precision request $n$, apply \autoref{thm:real-cauchy-completion-diagonal-handoff} to obtain the route through $\mu(n)$, the diagonal packet $D$, the finite stream window, the regular-sequence readback row, and the dyadic observation ledger.

The selected row is the same row identified by \autoref{thm:real-cauchy-completion-modulus-diagonal-exactness}. Thus the bundled requests do not create separate hidden completion data; they reuse the displayed diagonal rows attached to the unary modulus.

Finally, apply \autoref{thm:real-cauchy-completion-real-seal-readback-scope} to the family of reads. Since $Q$ is finite, the pointwise routes assemble into one finite $\mathsf{BHist}$ consumer surface with the recorded $\hsame$ transports, $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ row, and the same non-escape boundary is preserved for the bundle.

The checked Lean target supplies exactly this certificate row: its source predicate requires $\hsame$ with the bundle surface, $\UnaryHistory$, and $\PkgSig$; its pattern drops only $\PkgSig$; its ledger keeps $\PkgSig$ and $\hsame$; and its classifier is the same $\hsame$ relation.
\end{proof}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_probe\_bundle\_window\_coverage}

\begin{theorem}[RealCauchyCompletion selector-refusal boundary]
\label{thm:real-cauchy-completion-selector-refusal-boundary}
Let $C=(X,\mu,D,E,P,N)$ be an accepted $\RealCauchyCompletionUp$ carrier, and let $Q$ be a finite $\mathsf{ProbeBundle}$ of precision requests. The bundled read determined by $Q$ may use only the displayed modulus rows, the diagonal packet, the finite $\StreamNameUp$ windows, the $\RegSeqRatUp$ readback rows, the $\DyadicRatCoreUp$ observation rows, the $\RealUp$ seal, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ data carried by $C$.

The same bundled read cannot be reinterpreted as a selected host limit, a quotient stream equality, an ambient completeness object, or a host real equality. It remains a finite certificate-row read of the completion interface.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-cauchy-completion-probebundle-window-coverage} to the finite request bundle $Q$. This routes every bundled precision request through the displayed modulus row, the finite stream window, the regular-sequence readback row, the dyadic observation ledger, and the assembled $\mathsf{BHist}$ consumer surface.

The selected diagonal row is the one fixed by \autoref{thm:real-cauchy-completion-modulus-diagonal-exactness}. Hence the bundle reuses the carrier's visible diagonal packet and does not acquire any separate completion coordinate beyond the rows already named by $C$.

For the consumer endpoint, use \autoref{thm:real-cauchy-completion-real-seal-readback-scope}. The only real-facing object reached by the bundle is the $\RealUp$ seal along the displayed $\Cont$, $\Pkg$, and $\NameCert$ route.

Finally apply \autoref{thm:real-cauchy-completion-non-escape-boundary}. It excludes selected host limits, quotient stream equality, host real equality, and ambient completeness from the exported surface, so the finite bundle remains a certificate-row read rather than a limit selector.
\end{proof}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionUp\_StdBridge}

\begin{theorem}[RealCauchyCompletion obligation closure package]
\label{thm:real-cauchy-completion-obligation-closure-package}
The carrier of \autoref{def:real-cauchy-completion-carrier}, the
$\NameCert_{\RealCauchyCompletionUp}$ obligation surface, the diagonal handoff,
the non-escape boundary, and the Bishop reading jointly supply the full
paper-side obligation surface for $\RealCauchyCompletionUp$ over
$\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$,
$\RealDiagonalCompletionUp$, and $\RealUp$.

The package preserves the exclusions on quotient streams, host real equality,
selected limits, and ambient completeness: every exported completion read is
mediated by the displayed regular family row, unary modulus row, diagonal
packet, finite stream window, regular-rational readback, dyadic observation
ledger, $\RealUp$ seal row, $\hsame$ transports, $\Cont$ routes, $\Pkg$
provenance, and local $\NameCert$ row.
\end{theorem}
\begin{proof}
The carrier row is \autoref{def:real-cauchy-completion-carrier}. It lists the
regular Cauchy family, modulus, diagonal packet, real seal, package provenance,
and local certificate as finite packet coordinates.

The obligation surface is \autoref{thm:real-cauchy-completion-namecert-obligations}.
It binds carrier habitation, classifier stability, diagonal handoff,
non-escape, and Bishop-style reading as the five rows consumed by the local
certificate.

The diagonal and real-facing readback rows are
\autoref{thm:real-cauchy-completion-diagonal-handoff},
\autoref{thm:real-cauchy-completion-modulus-diagonal-exactness}, and
\autoref{thm:real-cauchy-completion-real-seal-readback-scope}, with finite
bundle coverage supplied by
\autoref{thm:real-cauchy-completion-probebundle-window-coverage}. They force every
consumer read through the modulus-selected diagonal packet, finite
$\StreamNameUp$ window, $\RegSeqRatUp$ readback, dyadic observation ledger, and
$\RealUp$ seal.

The boundary and interpretation rows are
\autoref{thm:real-cauchy-completion-non-escape-boundary} and
\autoref{thm:real-cauchy-completion-standard-bishop-reading}. They record that
the packet is a constructive regular-Cauchy completion interface while
withholding quotient stream equality, host real equality, choice-selected
limits, and ambient completeness.

Assembling these five displayed rows gives the stated paper-side obligation
closure package. No extra coordinate is introduced, so the exclusions are
preserved componentwise.
\end{proof}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_obligation\_closure\_package}

\begin{theorem}[RealCauchyCompletion regular-family source adequacy]
\label{thm:real-cauchy-completion-regular-family-source-adequacy}
Let $C=(X,\mu,D,E,P,N)$ be an accepted $\RealCauchyCompletionUp$ carrier. The visible regular-family source surface is adequate for the exported $\RealUp$ completion read precisely when every unary precision request factors through the same displayed rows already named by the carrier: the $\RegSeqRatUp$ family row $X$, the modulus row $\mu$, the $\RealDiagonalCompletionUp$ packet $D$, a finite $\StreamNameUp$ window, the $\DyadicRatCoreUp$ observation ledger, the local $\RealUp$ seal row $E$, and the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Equivalently, the regular-family input route and the public $\RealUp$ seal readback determine each other over exactly those rows. Neither direction includes a quotient stream, selected host limit, host real equality, ambient completeness, arithmetic package, order package, or metric-completeness row.
\end{theorem}
\begin{proof}
For the forward direction, project $C$ through \autoref{def:real-cauchy-completion-carrier}. The projection exposes $X$, $\mu$, $D$, $E$, $P$, and $N$ as the carrier coordinates available to a completion-facing consumer.

Apply \autoref{thm:real-cauchy-completion-diagonal-handoff}, \autoref{thm:real-cauchy-completion-modulus-diagonal-exactness}, and \autoref{thm:real-cauchy-completion-real-seal-readback-scope}. These rows force each unary precision request through the modulus-selected diagonal packet, one finite $\StreamNameUp$ window, the $\RegSeqRatUp$ readback, the $\DyadicRatCoreUp$ ledger, and the local $\RealUp$ seal row. For a finite request bundle, \autoref{thm:real-cauchy-completion-probebundle-window-coverage} repacks the same pointwise route as one $\mathsf{BHist}$ consumer surface with the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

For the reverse direction, start from a public completion read whose every unary request factors through precisely the listed rows. Repack the family row, modulus, diagonal packet, finite windows, dyadic ledger, seal row, provenance, and local naming row as the carrier coordinates of \autoref{def:real-cauchy-completion-carrier}.

Use \autoref{thm:real-cauchy-completion-namecert-obligations} to discharge the local carrier, classifier-stability, handoff, non-escape, and Bishop-reading obligations, then use \autoref{thm:real-cauchy-completion-obligation-closure-package} to bind those obligations as the public $\RealCauchyCompletionUp$ package. The non-escape boundary of \autoref{thm:real-cauchy-completion-non-escape-boundary} and the Bishop reading of \autoref{thm:real-cauchy-completion-standard-bishop-reading} exclude quotient streams, selected host limits, host real equality, ambient completeness, and the unrelated arithmetic, order, and metric-completeness rows from both readings.
\end{proof}

\begin{theorem}[RealCauchyCompletion regular-name route agreement]
\label{thm:real-cauchy-completion-regular-name-route-agreement}
Let $C=(X,\mu,D,E,P,N)$ be an accepted $\RealCauchyCompletionUp$ carrier. The $\RegularCauchyNameUp$ regular-family input route and the $\CauchyCompletionMonadUp$ finite completion-transformer route determine the same public $\RealUp$ seal surface exactly when both routes factor through the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealDiagonalCompletionUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows attached to $C$.

The agreement is a finite seal-surface agreement, not an equality of quotient streams, a selected limit, a host real equality, a trichotomy result, or an ambient completeness principle.
\end{theorem}
\begin{proof}
First read the regular-family side through \autoref{thm:real-cauchy-completion-regular-family-source-adequacy}. It forces every unary precision request through the regular family row, modulus row, diagonal packet, finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, and the local $\RealUp$ seal row.

Next read the public seal side through \autoref{thm:real-cauchy-completion-public-real-name-export}. This repacks the same rows as a public $\RealUp$-facing consumer surface over $\BHist$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$ provenance, and the local $\NameCert$ row.

Finally compare with the completion-transformer route of \autoref{ch:concrete-instances-cauchycompletionmonad-namecert}. The unit boundary, diagonal-bind stability, schedule composition boundary, and public real-seal factorization there put every $\CauchyCompletionMonadUp$ read through the same finite window, dyadic-observation, regular-readback, diagonal, seal, transport, route, provenance, and naming rows. Since both routes reach the public seal only through this displayed surface, they agree exactly on that surface and on no host-level completion object.
\end{proof}

\begin{theorem}[RealCauchyCompletion public real-name export]
\label{thm:real-cauchy-completion-public-real-name-export}
The carrier of \autoref{def:real-cauchy-completion-carrier}, the diagonal handoff, the non-escape boundary, carrier transport stability, modulus-diagonal exactness, real-seal readback scope, $\mathsf{ProbeBundle}$ window coverage, and obligation-closure package assemble into a public $\RealCauchyCompletionUp$ consumer export over $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

The exported surface is the $\RealUp$-facing name route through the displayed regular family row, unary modulus row, $\RealDiagonalCompletionUp$ packet, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, $\DyadicRatCoreUp$ observation rows, and the local certificate. It is not a quotient equality theorem, a host completeness theorem, or a proof of host equality of real numbers.
\end{theorem}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_public\_real\_name\_export}
\begin{proof}
Begin with the finite carrier of \autoref{def:real-cauchy-completion-carrier}. It supplies the regular family row, modulus, diagonal packet, real seal, package provenance, and local naming row that form the consumer-visible packet.

The diagonal route is supplied by \autoref{thm:real-cauchy-completion-diagonal-handoff} and sharpened by \autoref{thm:real-cauchy-completion-modulus-diagonal-exactness}. These results force each precision read through the displayed modulus row, the associated diagonal packet, one finite stream window, one regular rational readback, and the dyadic observation ledger.

Transport and finite-window reuse are supplied by \autoref{thm:real-cauchy-completion-carrier-transport-stability} and \autoref{thm:real-cauchy-completion-probebundle-window-coverage}. They show that the same rows assemble over $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ for finite consumer bundles.

Finally apply \autoref{thm:real-cauchy-completion-real-seal-readback-scope}, \autoref{thm:real-cauchy-completion-non-escape-boundary}, and \autoref{thm:real-cauchy-completion-obligation-closure-package}. These identify the public object as a $\RealUp$-facing name surface and preserve the exclusions on quotient stream equality, selected host limits, host real equality, and ambient completeness.
\end{proof}

\begin{theorem}[RealCauchyCompletion regular-family input interface]
\label{thm:real-cauchy-completion-regular-family-input-interface}
Every accepted $\RealCauchyCompletionUp$ carrier binds its $\RegularCauchyFamilyUp$ input rows to the completion carrier of \autoref{def:real-cauchy-completion-carrier}. The binding sends the regular family row $X$ and unary modulus row $\mu$ to the diagonal packet $D$, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, $\DyadicRatCoreUp$ observation ledger, $\RealUp$ seal row, $\Pkg$ provenance, and local $\NameCert$ row, all inside the displayed $\mathsf{BHist}$, $\hsame$, and $\Cont$ surface.
\end{theorem}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_regular\_family\_input\_interface}
\begin{proof}
Start with the completion carrier. Its coordinates explicitly contain the regular-family row, the unary modulus, the diagonal packet, the real seal, package provenance, and local naming certificate.

Apply the diagonal handoff of \autoref{thm:real-cauchy-completion-diagonal-handoff}. For each precision request, the modulus selects the family row that is read through one finite stream window, one regular-rational readback, and the dyadic observation ledger.

The exact selected row is the one identified by \autoref{thm:real-cauchy-completion-modulus-diagonal-exactness}. The $\RealUp$ seal then remains in the scope described by \autoref{thm:real-cauchy-completion-real-seal-readback-scope}, so the input rows are bound to the completion carrier without creating quotient stream equality, host real equality, or a selected host limit.
\end{proof}

\begin{theorem}[RealCauchyCompletion downstream consumer handoff]
\label{thm:real-cauchy-completion-downstream-consumer-handoff}
For any accepted $\RealCauchyCompletionUp$ carrier and any finite consumer request bundle, the downstream handoff is the route through the unary modulus, the $\RealDiagonalCompletionUp$ diagonal packet, the scheduled $\StreamNameUp$ windows, the $\RegSeqRatUp$ readback rows, the $\DyadicRatCoreUp$ ledger, the $\RealUp$ seal, $\Pkg$, and $\NameCert$. The route is stable under the displayed $\hsame$ and $\Cont$ rows and exports no data outside that surface.
\end{theorem}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_downstream\_consumer\_handoff}
\begin{proof}
Apply \autoref{thm:real-cauchy-completion-probebundle-window-coverage} to the finite bundle. Each request is reduced to the modulus-selected diagonal packet, its finite stream window, its regular-rational readback, and the dyadic observation rows.

Use \autoref{thm:real-cauchy-completion-carrier-transport-stability} to transport the same rows through the displayed $\hsame$ comparisons and $\Cont$ routes. Package provenance and the local naming row stay attached to the consumer-facing surface throughout the transport.

Finally, \autoref{thm:real-cauchy-completion-real-seal-readback-scope} identifies the exported endpoint as the $\RealUp$ seal reached through that route. The non-escape boundary of \autoref{thm:real-cauchy-completion-non-escape-boundary} excludes quotient streams, selected host limits, host real equality, and ambient completeness from the downstream handoff.
\end{proof}

\begin{theorem}[RealCauchyCompletion CauchyLimitSeal route]
\label{thm:real-cauchy-completion-cauchy-limit-seal-route}
An accepted $\RealCauchyCompletionUp$ carrier and an accepted $\CauchyLimitSealUp$ packet determine the same public finite seal route exactly when both reads factor through the displayed $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\RealDiagonalCompletionUp$ packet, $\RealUp$ seal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. This is the sibling seal route recorded by \autoref{ch:concrete-instances-cauchylimitseal-namecert}; it is not a quotient-stream equality, a selected host-limit principle, or an ambient completeness theorem.
\end{theorem}
\begin{proof}
Project the $\RealCauchyCompletionUp$ carrier by \autoref{def:real-cauchy-completion-carrier}. Its visible coordinates supply the regular-family row, unary modulus, diagonal packet, real seal, package provenance, and local certificate row.

Apply the diagonal handoff of \autoref{thm:real-cauchy-completion-diagonal-handoff} and the public real-name export of \autoref{thm:real-cauchy-completion-public-real-name-export}. These rows force every completion read through the displayed finite stream window, regular-rational readback, dyadic ledger, diagonal packet, real seal, $\BHist$ surface, componentwise $\hsame$ transport, displayed $\Cont$ route, $\Pkg$ provenance, and local $\NameCert$ row.

Compare this route with the $\CauchyLimitSealUp$ consumer handoff of \autoref{ch:concrete-instances-cauchylimitseal-namecert}. The sibling packet reaches its public seal through the same displayed windows, readbacks, dyadic observations, diagonal completion packet, seal row, transports, route records, provenance, and naming rows.

Since both public reads reach the seal only along this common finite surface, the two routes agree exactly on that surface. The non-escape boundary from \autoref{thm:real-cauchy-completion-non-escape-boundary} keeps quotient streams, selected host limits, host real equality, and ambient completeness outside the conclusion.
\end{proof}

\begin{theorem}[RealCauchyCompletion finite request fusion]
\label{thm:real-cauchy-completion-finite-request-fusion}
Let $C$ be an accepted $\RealCauchyCompletionUp$ carrier, and let $Q$ and $Q'$ be finite $\mathsf{ProbeBundle}$ request surfaces for $C$. If the unary precision rows selected by $Q$ and $Q'$ are $\hsame$-compatible, then their downstream handoff routes fuse into one finite $\mathsf{BHist}$ consumer packet over the same unary modulus, $\RealDiagonalCompletionUp$ diagonal packet, $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, $\DyadicRatCoreUp$ observations, $\RealUp$ seal, $\Pkg$ provenance, and $\NameCert$ row. The fused packet exports no quotient stream equality, selected host limit, host real equality, or ambient completeness theorem.
\end{theorem}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_finite\_request\_fusion}
\begin{proof}
Apply the $\mathsf{ProbeBundle}$ window coverage theorem separately to $Q$ and $Q'$. Each finite request surface is reduced to modulus-selected diagonal reads, scheduled stream windows, regular-rational readbacks, and dyadic observation rows from the accepted carrier.

Use the assumed $\hsame$-compatibility of the unary precision rows to identify the modulus channel shared by the two surfaces. Modulus-diagonal exactness then places both request families on the same displayed diagonal packet and its associated finite windows.

Next apply carrier transport stability and the downstream consumer handoff. The two finite handoff packets are combined by finite packet union, while the stored $\hsame$ and $\Cont$ rows transport the readbacks, dyadic observations, real seal, provenance, and local naming row along the same carrier surface.

Finally inherit the non-escape boundary componentwise from the two handoffs. Since neither input packet contains a quotient stream, selected host limit, host real equality, or ambient completeness field, the fused finite consumer packet cannot export any such row.
\end{proof}

\begin{theorem}[RealCauchyCompletion scoped consumer route]
\label{thm:real-cauchy-completion-scoped-consumer-route}
The regular-family input interface of \autoref{thm:real-cauchy-completion-regular-family-input-interface}, the downstream handoff of \autoref{thm:real-cauchy-completion-downstream-consumer-handoff}, and the obligation package of \autoref{thm:real-cauchy-completion-obligation-closure-package} assemble the public consumer theorem for $\RealCauchyCompletionUp$. Its scope is exactly the completion-facing route through $\RegularCauchyFamilyUp$, modulus selection, diagonal packet, finite windows, regular-rational readback, dyadic ledger, $\RealUp$ seal, $\mathsf{ProbeBundle}$ coverage, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\end{theorem}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_scoped\_consumer\_route}
\begin{proof}
The input side is supplied by \autoref{thm:real-cauchy-completion-regular-family-input-interface}. It binds the $\RegularCauchyFamilyUp$ rows to the carrier coordinates and to the modulus-selected diagonal readback route.

The consumer side is supplied by \autoref{thm:real-cauchy-completion-downstream-consumer-handoff}. It packages finite request bundles through the same diagonal packet, stream windows, regular-rational readbacks, dyadic ledger, real seal, package provenance, and local certificate rows.

The package side is \autoref{thm:real-cauchy-completion-obligation-closure-package}. Together with carrier stability, modulus-diagonal exactness, real-seal readback scope, and the non-escape boundary, it gives a scoped public theorem about the concrete BEDC completion interface and nothing stronger than that interface.
\end{proof}

\begin{theorem}[RealCauchyCompletion standard bridge boundary]
\label{thm:real-cauchy-completion-standard-bridge-boundary}
The standard BEDC completion-interface reading of any accepted $\RealCauchyCompletionUp$ carrier factors exactly through the displayed $\RegularCauchyFamilyUp$ row, the unary modulus row, the $\RealDiagonalCompletionUp$ packet, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readbacks, $\DyadicRatCoreUp$ observations, the $\RealUp$ seal, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. This boundary exports no quotient stream, host equality of real numbers, selected limit, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Use \autoref{thm:real-cauchy-completion-regular-family-input-interface} to place the accepted carrier on the regular-family row, unary modulus row, diagonal packet, finite stream windows, regular-rational readbacks, dyadic observation ledger, real seal, package provenance, and local naming row.

The downstream side is then \autoref{thm:real-cauchy-completion-downstream-consumer-handoff}. It shows that every finite consumer request reads the same visible route through $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

Finally combine \autoref{thm:real-cauchy-completion-real-seal-readback-scope} with \autoref{thm:real-cauchy-completion-non-escape-boundary}. These rows identify the exported endpoint as the $\RealUp$ seal and exclude quotient streams, host real equality, selected limits, and ambient completeness from the standard completion-interface reading.
\end{proof}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_standard\_bridge\_boundary}

\begin{theorem}[RealCauchyCompletion bridge handoff]
\label{thm:real-cauchy-completion-bridge-handoff}
The public real-name export of \autoref{thm:real-cauchy-completion-public-real-name-export}, the regular-family input interface of \autoref{thm:real-cauchy-completion-regular-family-input-interface}, the downstream consumer handoff of \autoref{thm:real-cauchy-completion-downstream-consumer-handoff}, and the standard bridge boundary of \autoref{thm:real-cauchy-completion-standard-bridge-boundary} jointly supply the paper bridge needed for the public-to-bridged $\RealCauchyCompletionUp$ closure step.
\end{theorem}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_bridge\_handoff}
\begin{proof}
The public export supplies the $\RealUp$-facing name surface. The input interface pins that surface to the regular-family row, unary modulus, diagonal packet, stream windows, regular-rational readbacks, dyadic observations, package provenance, and local certificate.

The downstream handoff shows that all finite consumer bundles pass through the same rows, with $\hsame$ transport and $\Cont$ routes preserving the visible package. The standard boundary then records that this is exactly the standard BEDC completion-interface reading.

Composing these four displayed rows gives a paper bridge from the public certificate to the standard completion schema. The conclusion remains paper-side only: it does not assert machine bridge checking, quotient equality, host equality, selected limits, or ambient completeness.
\end{proof}

\begin{theorem}[Real Cauchy completion mature limit-seal consumption]
\label{thm:real-cauchy-completion-mature-limit-seal-consumption}
The regular-family row of \autoref{thm:real-cauchy-completion-regular-family-input-interface}, the diagonal packet of \autoref{thm:real-cauchy-completion-diagonal-handoff}, finite $\StreamNameUp$ windows, the $\DyadicRatCoreUp$ ledger, $\RegSeqRatUp$ readback, the $\RealUp$ seal of \autoref{thm:real-cauchy-completion-real-seal-readback-scope}, $\Pkg$ provenance, and the $\NameCert_{\RealCauchyCompletionUp}$ row jointly supply the finite consumer boundary used by downstream completion examples. The boundary is a limit-seal consumption surface and does not claim quotient equality of regular streams, host equality of real numbers, selected limits, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_mature\_limit\_seal\_consumption}
\begin{proof}
Start with an accepted carrier. The regular-family interface fixes the source rows and unary modulus, while the diagonal handoff identifies the scheduled packet through which each precision request is read.

The finite windows and dyadic ledger then give the concrete observation route. Each request exposes one $\StreamNameUp$ window, its $\DyadicRatCoreUp$ observations, and the corresponding $\RegSeqRatUp$ readback before any real-facing row is consumed.

The real-seal readback scope places the $\RealUp$ seal after that route. Package provenance and the local $\NameCert$ row attach the dependency certificates that downstream completion examples are allowed to cite.

Thus the mature consumer boundary is finite and seal-facing: it packages the regular-family row, diagonal packet, windows, dyadic ledger, regular readback, seal, provenance, and naming row. Since no additional coordinate is introduced, quotient equality, host real equality, selected limits, and ambient completeness remain outside the claim.
\end{proof}

\begin{theorem}[RealCauchyCompletion family tail synchronization]
\label{thm:real-cauchy-completion-family-tail-synchronization}
Let $C$ and $C'$ be accepted $\RealCauchyCompletionUp$ carriers whose regular-family rows are classified through the $\RegSeqRatUp$ common-refinement classifier, and let the requested precisions be carried by one finite $\mathsf{ProbeBundle}$. Then the two $\RealUp$ seal reads synchronize through a single displayed tail window before either seal is read.

The window consists of the shared unary modulus bound selected from the two carriers, one $\StreamNameUp$ window for the finite request bundle, one $\RegSeqRatUp$ readback row supplied by the common refinement, and one $\DyadicRatCoreUp$ observation ledger. The synchronized route stays inside the rows of \autoref{def:real-cauchy-completion-carrier}, $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, and it exports no quotient equality of regular streams, host equality of real numbers, selected limit, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Project the two accepted carriers through \autoref{def:real-cauchy-completion-carrier}. This exposes the two regular-family rows, their unary modulus rows, diagonal packets, finite stream-window ledgers, regular-rational readbacks, dyadic observation ledgers, real-seal rows, package provenance, and local naming rows.

Use the regular-family input interface of \autoref{thm:real-cauchy-completion-regular-family-input-interface} and the downstream consumer handoff of \autoref{thm:real-cauchy-completion-downstream-consumer-handoff}. These two rows identify the only route by which a finite precision request may travel from the regular-family input to a seal-facing consumer read.

The $\RegSeqRatUp$ common-refinement classifier and the $\StreamNameUp$ pointwise handoff supply the shared finite window. The unary modulus bound is read once for the bundled requests, and the same window feeds both regular-rational readback rows.

Finally read the corresponding $\DyadicRatCoreUp$ observation ledger before either $\RealUp$ seal is consumed. The real-seal readback scope and non-escape boundary then show that both seal reads are synchronized by that displayed finite tail route, with no exported quotient stream, selected host limit, host real equality, or ambient completeness principle.
\end{proof}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_family\_tail\_synchronization}

\begin{theorem}[RealCauchyCompletion budgeted common refinement]
\label{thm:real-cauchy-completion-budgeted-common-refinement}
Let $C$ and $C'$ be accepted $\RealCauchyCompletionUp$ carriers whose regular-family rows classify through one $\RegSeqRatUp$ common-refinement row, and let $B$ be an accepted $\RealObservationBudgetUp$ packet carrying the same finite $\mathsf{ProbeBundle}$ request surface. Then the synchronized $\RealUp$ seal route factors through one budgeted common-refinement packet.

This packet consists exactly of the $\RealObservationBudgetUp$ precision rows, finite $\StreamNameUp$ windows, $\DyadicRatCoreUp$ readbacks and displayed radii, $\RegSeqRatUp$ handoff rows, $\RealUp$ seal boundary, componentwise $\hsame$ transports, displayed $\Cont$ route replay, $\Pkg$ provenance, and local $\NameCert$ rows. It exports no quotient equality of regular streams, host real equality, selected limit, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-cauchy-completion-family-tail-synchronization} to the two accepted carriers and the shared finite request surface. This gives the common tail window through which both seal reads pass before either $\RealUp$ seal is consumed.

Now read the accepted $\RealObservationBudgetUp$ packet. Window coverage in \autoref{thm:real-observation-budget-window-coverage} identifies the same finite $\StreamNameUp$ windows, while \autoref{thm:real-observation-budget-regseq-handoff} places every $\RegSeqRatUp$ handoff after the precision row, windows, dyadic readbacks, and displayed radii.

The real seal boundary of \autoref{thm:real-observation-budget-real-seal-boundary} puts the seal rows after those budget coordinates, and \autoref{thm:real-observation-budget-nonescape} excludes quotient equality, host real equality, selected limits, ambient completeness, and hidden unbounded schedules from the packet.

Finally use \autoref{thm:real-cauchy-completion-downstream-consumer-handoff} to replay the synchronized route for downstream consumers, and use \autoref{thm:real-cauchy-completion-non-escape-boundary} to keep the resulting route inside the displayed $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ surface. Thus the completion synchronization and the observation budget are the same finite common-refinement packet at the consumer boundary.
\end{proof}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_budgeted\_common\_refinement}

\begin{theorem}[RealCauchyCompletion null-tail budget correspondence]
\label{thm:real-cauchy-completion-null-tail-budget-correspondence}
Let $C$ and $C'$ be accepted $\RealCauchyCompletionUp$ carriers whose regular-family rows are classified through one $\RegSeqRatUp$ common-refinement row, and let one finite $\mathsf{ProbeBundle}$ carry the requested precision observations. On the displayed BEDC rows, the synchronized $\RealUp$ seal route of \autoref{thm:real-cauchy-completion-family-tail-synchronization} is equivalent to giving one $\NullSequenceUp$ zero-tail packet whose tolerance-window package is carried by one $\DyadicTailBoundUp$ finite budget ledger.

The correspondence lives only over $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\NullSequenceUp$, $\DyadicTailBoundUp$, and $\RealUp$. It exports no quotient streams, selected limits, host real equality, trichotomy, or ambient completeness.
\end{theorem}
\begin{proof}
Begin with the synchronized tail route. By \autoref{thm:real-cauchy-completion-family-tail-synchronization}, both accepted completion carriers read the same finite tail window before either $\RealUp$ seal is consumed; this window is built from the common $\RegSeqRatUp$ refinement, the finite $\StreamNameUp$ request window, and the $\DyadicRatCoreUp$ observation ledger.

Turn that shared window into zero-tail evidence by applying \autoref{thm:null-sequence-zero-tail-criterion}. The common-window requirement is supplied by \autoref{thm:null-sequence-common-window-refinement}, so the two carrier reads are compared by one $\NullSequenceUp$ packet rather than by a host equality of limits.

Now account for the precision budget. Apply \autoref{thm:dyadic-tail-bound-budget-composition} to package the tolerance windows into one finite $\DyadicTailBoundUp$ ledger, and apply \autoref{thm:dyadic-tail-bound-real-seal-factorization} to route the $\RealUp$ seal through that ledger and the regular-rational readback.

Conversely, a $\NullSequenceUp$ zero-tail packet equipped with the stated $\DyadicTailBoundUp$ budget ledger determines the same displayed tail window for the finite request bundle. Re-reading it through the common $\RegSeqRatUp$ refinement gives exactly the synchronized seal route, and all named transports remain inside the listed BEDC rows.
\end{proof}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_null\_tail\_budget\_correspondence}

\begin{theorem}[RealCauchyCompletion synchronized null-tail consumer]
\label{thm:real-cauchy-completion-synchronized-null-tail-consumer}
Let $C$ and $C'$ be accepted $\RealCauchyCompletionUp$ carriers whose regular-family rows classify through one $\RegSeqRatUp$ common-refinement row, and let one finite $\mathsf{ProbeBundle}$ carry the precision requests. Then the synchronized seal route and the null-tail dyadic-budget packet assemble into one consumer-visible $\RealUp$ equality surface.

This surface is exactly the finite BEDC route over $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\NullSequenceUp$, $\DyadicTailBoundUp$, and $\RealUp$: two accepted carrier reads are equal for the consumer only by passing through the common tail window, the shared zero-tail certificate, the finite dyadic budget ledger, and the resulting $\RealUp$ seal. It exports no quotient stream equality, selected limit, host real equality, trichotomy, or ambient completeness theorem.
\end{theorem}
\begin{proof}
First build the common consumer route. Apply \autoref{thm:real-cauchy-completion-family-tail-synchronization} to the two accepted carriers and the shared $\mathsf{ProbeBundle}$. This gives one finite tail window before either $\RealUp$ seal is consumed, with the window read through the common $\RegSeqRatUp$ refinement, the $\StreamNameUp$ request window, and the $\DyadicRatCoreUp$ ledger.

Next convert the synchronized window into the null-tail budget packet. Apply \autoref{thm:real-cauchy-completion-null-tail-budget-correspondence}. Its forward direction packages the same synchronized route as one $\NullSequenceUp$ zero-tail packet equipped with one $\DyadicTailBoundUp$ finite budget ledger; its reverse direction rereads such a packet as the same synchronized seal route.

The certificate ingredients are the displayed null-sequence and dyadic-tail rows. Use \autoref{thm:null-sequence-zero-tail-criterion} and \autoref{thm:null-sequence-common-window-refinement} for the shared zero-tail window, then use \autoref{thm:dyadic-tail-bound-budget-composition} and \autoref{thm:dyadic-tail-bound-real-seal-factorization} for the finite tolerance ledger and its $\RealUp$ seal factorization.

Combining these rows gives a reusable consumer equality surface rather than a stronger equality principle. Every transport still runs through $\mathsf{BHist}$, $\hsame$, $\Cont$, $\Pkg$, $\NameCert$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\NullSequenceUp$, $\DyadicTailBoundUp$, and $\RealUp$, so quotient streams, selected limits, host real equality, trichotomy, and ambient completeness remain outside the claim.
\end{proof}
\leanchecked{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_synchronized\_null\_tail\_consumer}

\begin{theorem}[RealCauchyCompletion tail-budget coherence consumer]
\label{thm:real-cauchy-completion-tail-budget-coherence-consumer}
Every accepted $\RealCauchyCompletionUp$ carrier $C=(X,\mu,D,E,P,N)$ whose diagonal packet consumes a $\RegularCauchyTailBudgetUp$ row reads its completion-facing $\RealUp$ seal only after the regular-family row, unary modulus row, diagonal packet, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, and the tail-budget row of \autoref{ch:concrete-instances-regular-cauchy-tail-budget-namecert} have been displayed. The consumer route excludes quotient stream equality, selected limits, host real equality, and ambient completeness.
\end{theorem}
\begin{proof}
Project the accepted carrier through \autoref{def:real-cauchy-completion-carrier}. This exposes the regular-family row $X$, unary modulus $\mu$, diagonal packet $D$, completion-facing seal $E$, package provenance $P$, and local naming row $N$.

Apply the diagonal handoff of \autoref{thm:real-cauchy-completion-diagonal-handoff}. The precision request must pass through $\mu$, the diagonal packet $D$, one finite $\StreamNameUp$ window, one $\RegSeqRatUp$ readback row, and the displayed $\DyadicRatCoreUp$ observation ledger before the seal row can be read.

Now read the consumed $\RegularCauchyTailBudgetUp$ row from \autoref{ch:concrete-instances-regular-cauchy-tail-budget-namecert}. Its observation gate supplies the tail-threshold, finite observation service, window service, dyadic ledger, regular readback, and seal handoff that the diagonal packet is using as its budget-row source.

Finally use the real-seal readback scope of \autoref{thm:real-cauchy-completion-real-seal-readback-scope} and the downstream consumer handoff of \autoref{thm:real-cauchy-completion-downstream-consumer-handoff}. They keep the completion-facing seal inside the displayed regular-family, modulus, diagonal, finite-window, readback, dyadic-ledger, tail-budget, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ route, so no quotient stream equality, selected limit, host real equality, or ambient completeness is exported.
\end{proof}

\begin{theorem}[RealCauchyCompletion tail-budget seal route]
\label{thm:real-cauchy-completion-tail-budget-seal-route}
Every accepted $\RealCauchyCompletionUp$ carrier whose diagonal packet consumes a $\RegularCauchyTailBudgetUp$ row and whose completion-facing seal is compared through a $\CauchyLimitSealUp$ packet over the same finite $\StreamNameUp$ window reads its $\RealUp$ seal only through the finite route consisting of that $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, the tail-budget threshold, the $\CauchyLimitSealUp$ tail-budget meet of \autoref{ch:concrete-instances-cauchylimitseal-namecert}, the $\RealUp$ seal, and the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The route exports no quotient streams, selected limits, host real equality, trichotomy, countable choice, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the accepted carrier through \autoref{def:real-cauchy-completion-carrier}. This displays the completion carrier as its regular-family row, unary modulus, diagonal packet, completion-facing seal, package provenance, and local naming row.

Apply the diagonal handoff of \autoref{thm:real-cauchy-completion-diagonal-handoff}. The same finite $\StreamNameUp$ window must carry the precision request through the $\RegSeqRatUp$ readback and $\DyadicRatCoreUp$ ledger before the completion-facing seal can be consumed.

Now apply \autoref{thm:real-cauchy-completion-tail-budget-coherence-consumer}. It forces the diagonal packet to expose the $\RegularCauchyTailBudgetUp$ threshold and its finite observation service inside the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ route.

Attach the sibling seal packet from \autoref{ch:concrete-instances-cauchylimitseal-namecert}. The $\CauchyLimitSealUp$ route compares the completion-facing seal over the same finite window and meets the tail-budget row before the $\RealUp$ seal is read.

Finally use \autoref{thm:real-cauchy-completion-real-seal-readback-scope}. Since both the completion carrier and the seal packet have already been routed through the finite window, readback row, dyadic ledger, tail-budget threshold, $\CauchyLimitSealUp$ meet, structural transports, package provenance, and local naming row, the exported surface contains no quotient streams, selected limits, host real equality, trichotomy, countable choice, or ambient completeness.
\end{proof}

\begin{theorem}[RealCauchyCompletion tail-fusion budget consumer]
\label{thm:real-cauchy-completion-tail-fusion-budget-consumer}
Every accepted $\RealCauchyCompletionUp$ carrier whose diagonal packet consumes both a $\RegularCauchyTailFusionUp$ row and a $\RegularCauchyTailBudgetUp$ row reaches its $\RealUp$ seal only through the finite route consisting of the regular-family row, unary modulus row, diagonal packet, finite $\StreamNameUp$ windows, $\DyadicRatCoreUp$ ledger, $\RegSeqRatUp$ readback, the $\RegularCauchyTailFusionUp$ shared-threshold row of \autoref{ch:concrete-instances-regularcauchytailfusion-namecert}, the $\RegularCauchyTailBudgetUp$ budget row of \autoref{ch:concrete-instances-regular-cauchy-tail-budget-namecert}, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and the local $\NameCert$ row.

The resulting consumer route exports no quotient stream equality, selected limit, host real equality, trichotomy, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start with the displayed completion carrier. The family-tail synchronization row identifies the shared finite tail window through which the regular-family input, unary modulus, diagonal packet, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, and $\DyadicRatCoreUp$ ledger must pass before any $\RealUp$ seal is read.

Next add the budgeted common-refinement row. It places the same synchronized window under the finite budget surface used by the consumer request bundle, so the precision rows, displayed radii, regular readback, dyadic observations, $\hsame$ transports, $\Cont$ routes, $\Pkg$ provenance, and local naming rows are read as one packet.

Now read the synchronized null-tail consumer row together with the tail-budget coherence consumer row. The null-tail row supplies the consumer-visible equality surface over the shared tail window, while the tail-budget row forces the diagonal packet to expose the $\RegularCauchyTailBudgetUp$ budget ledger before the completion-facing seal is consumed.

Finally insert the $\RegularCauchyTailFusionUp$ shared-threshold row into that route. The tail-fusion packet contributes only the finite cofinal threshold and seal-facing handoff already named by its public consumer surface; it does not introduce quotient stream equality, a selected host limit, host real equality, trichotomy, or ambient completeness. Hence the $\RealCauchyCompletionUp$ seal is reached exactly through the finite fusion-and-budget route stated above.
\end{proof}

\begin{theorem}[RealCauchyCompletion tail-cofinality schedule factorization]
\label{thm:real-cauchy-completion-tail-cofinality-schedule-factorization}
Every accepted $\RealCauchyCompletionUp$ carrier whose diagonal packet consumes a $\TailCofinalityScheduleUp$ row, a $\NullSequenceUp$ zero-tail packet, and the tail-budget and tail-fusion rows of \autoref{thm:real-cauchy-completion-tail-budget-coherence-consumer} and \autoref{thm:real-cauchy-completion-tail-fusion-budget-consumer} reaches its $\RealUp$ seal only through the displayed finite schedule factorization.

That factorization consists exactly of the regular-family row, unary modulus, diagonal packet, the $\TailCofinalityScheduleUp$ finite-window route, the $\NullSequenceUp$ zero-tail package, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows. It exports no quotient streams, selected limits, host real equality, trichotomy, or ambient completeness.
\end{theorem}
\begin{proof}
Start with the accepted completion carrier and apply \autoref{thm:real-cauchy-completion-tail-fusion-budget-consumer}. This forces every seal-facing consumer to expose the regular-family row, unary modulus, diagonal packet, finite $\StreamNameUp$ windows, $\DyadicRatCoreUp$ ledger, $\RegSeqRatUp$ readback, fusion row, budget row, structural transports, provenance, and local naming row before the $\RealUp$ seal is read.

Next read \autoref{thm:real-cauchy-completion-synchronized-null-tail-consumer}. It identifies the same finite tail window with the $\NullSequenceUp$ zero-tail package and the dyadic budget ledger, so the consumer-visible equality surface is still a displayed BEDC packet rather than a quotient stream or selected host limit.

Now insert the $\TailCofinalityScheduleUp$ packet. The schedule real-seal readback theorem \autoref{thm:tail-cofinality-schedule-real-seal-readback} orders the schedule rows as the unary request, finite window, dyadic ledger, regular-rational readback, and real seal. The scoped-binding theorem \autoref{thm:tail-cofinality-schedule-scoped-binding} keeps that route inside $\BHist$, $\hsame$, $\Cont$, $\Pkg$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, and the local $\NameCert$ row.

Combining these three routes gives a single factorization from the completion diagonal packet to the $\RealUp$ seal. Since each cited route excludes quotient streams, selected limits, host real equality, trichotomy, ambient completeness, hidden tail oracles, and unbounded schedules, the assembled carrier reaches the seal only through the finite schedule and null-tail rows named in the statement.
\end{proof}

\begin{theorem}[RealCauchyCompletion tail schedule budget exhaustion]
\label{thm:real-cauchy-completion-tail-schedule-budget-exhaustion}
Every accepted $\RealCauchyCompletionUp$ carrier whose diagonal packet consumes $\TailCofinalityScheduleUp$, $\RegularCauchyTailBudgetUp$, $\RegularCauchyTailFusionUp$, $\NullSequenceUp$, and an accepted $\RealObservationBudgetUp$ finite request bundle has a single consumer-visible route through exactly the regular-family row, unary modulus, diagonal packet, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, tail-budget row, tail-fusion shared-threshold row, null-tail zero package, observation-budget precision rows, $\RealUp$ seal, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$.

The route exports no quotient stream equality, selected limit, host real equality, trichotomy, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Begin with the accepted completion carrier and apply \autoref{thm:real-cauchy-completion-tail-cofinality-schedule-factorization}. The diagonal packet is already forced through the regular-family row, unary modulus, $\TailCofinalityScheduleUp$ finite-window route, $\NullSequenceUp$ zero-tail packet, $\DyadicRatCoreUp$ ledger, $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\RealUp$ seal, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$.

Next apply \autoref{thm:real-cauchy-completion-tail-fusion-budget-consumer}. It refines the same seal-facing route by making the $\RegularCauchyTailFusionUp$ shared-threshold row and the $\RegularCauchyTailBudgetUp$ budget row explicit before the completion seal is read.

Apply the budgeted common-refinement theorem \autoref{thm:real-cauchy-completion-budgeted-common-refinement}. It supplies the finite consumer window for the accepted $\RealObservationBudgetUp$ request bundle. Read it together with the tail-budget coherence theorem \autoref{thm:real-cauchy-completion-tail-budget-coherence-consumer}; this keeps each precision row inside the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\hsame$, $\Cont$, $\Pkg$, and local naming route.

Now use the synchronized null-tail consumer theorem \autoref{thm:real-cauchy-completion-synchronized-null-tail-consumer}. It identifies the same finite window with the $\NullSequenceUp$ zero-tail package, so the route has one shared carrier packet rather than separate schedule, budget, fusion, null-tail, and observation-budget exits.

Finally apply the $\RealObservationBudgetUp$ non-escape theorem \autoref{thm:real-observation-budget-nonescape}. Intersecting the exclusions from the schedule factorization, fusion-budget consumer, budgeted common refinement, synchronized null-tail consumer, and observation-budget row removes quotient stream equality, selected limits, host real equality, trichotomy, countable choice, and ambient completeness from the exported surface. What remains is exactly the finite consumer-visible route named in the statement.
\end{proof}

\begin{theorem}[RealCauchyCompletion bounded-monotone witness consumer]
\label{thm:real-cauchy-completion-bounded-monotone-witness-consumer}
Every accepted $\RealCauchyCompletionUp$ carrier whose regular-family input is supplied by an accepted $\BoundedMonotoneCauchyWitnessUp$ packet reads its completion-facing $\RealUp$ seal through exactly the bounded source row, scheduled $\StreamNameUp$ windows, Cauchy witness modulus, dyadic tolerance ledger, located trap, $\RegSeqRatUp$ readback, diagonal completion packet, observation-budget request bundle, local $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The route is completion-facing only in this displayed sense. It exports no selected limit, quotient stream equality, host real equality, trichotomy, open-cover compactness, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start with the accepted $\BoundedMonotoneCauchyWitnessUp$ packet. The completion boundary theorem \autoref{thm:bounded-monotone-cauchy-witness-completion-boundary} keeps every completion-facing read inside the bounded source, regular-rational handoff, stream schedule, Cauchy witness, dyadic ledger, located trap, $\RealUp$ seal, transport, route, provenance, and local naming rows.

Next use the tail-window service theorem \autoref{thm:bounded-monotone-cauchy-witness-tail-window-service}. It forces each tolerance request to pass through the scheduled $\StreamNameUp$ windows, the Cauchy witness modulus, and the dyadic tolerance ledger before the located trap or any seal-facing row can be read.

Now apply the Real seal handoff theorem \autoref{thm:bounded-monotone-cauchy-witness-real-seal-handoff}. This places the $\RealUp$ seal after the bounded source row, $\RegSeqRatUp$ readback, scheduled windows, Cauchy witness, dyadic ledger, and located trap, so the completion carrier receives a displayed regular-family input rather than an external limiting object.

Finally compose this input route with the $\RealCauchyCompletionUp$ consumer rows. The diagonal handoff, downstream consumer handoff, and tail schedule budget exhaustion theorems \autoref{thm:real-cauchy-completion-diagonal-handoff}, \autoref{thm:real-cauchy-completion-downstream-consumer-handoff}, and \autoref{thm:real-cauchy-completion-tail-schedule-budget-exhaustion} add the diagonal completion packet, observation-budget request bundle, $\BHist$ carrier surface, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ rows. Since none of the cited packets contains a coordinate for selected limits, quotient stream equality, host real equality, trichotomy, open-cover compactness, or ambient completeness, the assembled completion-facing witness route exposes exactly the rows named in the statement.
\end{proof}

\begin{theorem}[RealCauchyCompletion uniform criterion seal factorization]
\label{thm:real-cauchy-completion-uniform-criterion-seal-factorization}
Every accepted $\RealCauchyCompletionUp$ carrier whose diagonal packet consumes an accepted $\UniformCauchyCriterionUp$ row and a $\CauchyLimitSealUp$ row reaches its $\RealUp$ seal only through the same finite seal route. That route consists of the regular-family row, unary modulus, uniform shared-tail threshold, Cauchy-limit seal selector, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.

The factorization exports no quotient streams, selected limits, host real equality, trichotomy, countable choice, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start with the accepted completion carrier and apply \autoref{thm:real-cauchy-completion-diagonal-handoff}. The diagonal packet already routes the regular-family input, unary modulus, finite windows, regular-rational readback, dyadic ledger, transport rows, continuation routes, package provenance, and local naming rows before the completion-facing seal is read.

Use \autoref{thm:real-cauchy-completion-public-real-name-export} and \autoref{thm:real-cauchy-completion-tail-schedule-budget-exhaustion}. They keep the public $\RealUp$ seal on the displayed completion route and make the finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows explicit.

Attach the uniform criterion side through \autoref{thm:uniform-cauchy-criterion-real-seal-handoff} and \autoref{thm:uniform-cauchy-criterion-window-refinement}. The accepted criterion contributes the shared family threshold and its refined finite-window route, not a quotient family or selected limiting object.

Now attach the Cauchy-limit seal side through \autoref{thm:cauchy-limit-seal-uniform-criterion-pullback} and \autoref{thm:cauchy-limit-seal-realup-consumer-boundary}. These rows pull the uniform criterion back to the same scheduled windows and require the seal selector, dyadic observation ledger, regular readback, transports, routes, provenance, and local naming rows before any $\RealUp$ consumer read.

The combined route is therefore one finite BEDC packet: regular family, unary modulus, uniform shared-tail threshold, Cauchy-limit selector, windows, readback, dyadic ledger, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The non-escape clauses in the completion, uniform-criterion, and Cauchy-limit-seal rows exclude quotient streams, selected limits, host real equality, trichotomy, countable choice, and ambient completeness from the exported seal.
\end{proof}

\begin{theorem}[RealCauchyCompletion witness-ledger classifier lock]
\label{thm:real-cauchy-completion-witness-ledger-classifier-lock}
Every accepted $\RealCauchyCompletionUp$ carrier whose completion-facing consumer also reads an accepted $\CauchyWitnessLedgerUp$ packet $L=(Q,B,S,K,H,C,P,N)$ reaches the public $\RealUp$ seal through the completion-seal classifier only by the shared finite route
$$
Q \longmapsto B \longmapsto S \longmapsto K .
$$
The shared route is read together with the $\RealCauchyCompletionUp$ regular-family row, unary modulus, diagonal packet, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The consumer-side lock is relative to the witness-ledger surface of \autoref{ch:concrete-instances-cauchy-witness-ledger-namecert} and the completion-seal classifier surface of \autoref{ch:concrete-instances-real-completion-seal-classifier-namecert}. It exports no quotient stream equality, host real equality, selected external limit, countable-choice row, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Start with the accepted $\RealCauchyCompletionUp$ carrier. The public real-name export theorem \autoref{thm:real-cauchy-completion-public-real-name-export} keeps the public $\RealUp$ read on the displayed completion route, and \autoref{thm:real-cauchy-completion-uniform-criterion-seal-factorization} factors that route through the regular-family row, unary modulus, diagonal packet, finite windows, regular-rational readback, dyadic ledger, transports, continuations, provenance, and local naming rows.

Now read the accepted $\CauchyWitnessLedgerUp$ packet. The Real-completion handoff \autoref{thm:cauchy-witness-ledger-real-completion-handoff} places every Real-facing ledger consumer on the displayed completion route. The synchronizer-classifier lock \autoref{thm:cauchy-witness-ledger-synchronizer-classifier-lock} orders that route as $Q \longmapsto B \longmapsto S \longmapsto K$, and the ledger-exhaustion theorem \autoref{thm:cauchy-witness-ledger-ledger-exhaustion} removes any public coordinate outside $Q,B,S,K,H,C,P,N$ and the structural rows.

Finally identify the two consumer paths at the shared classifier row $K$. The RealCauchyCompletion path supplies the completion-facing public $\RealUp$ seal, while the witness ledger supplies the same finite budget, observation, synchronizer, and classifier route. Since both cited chapters expose only the displayed BEDC rows, the combined consumer cannot export quotient stream equality, host real equality, a selected external limit, a countable-choice row, or an ambient completeness theorem.
\end{proof}

\begin{theorem}[RealCauchyCompletion witness-ledger uniform seal route]
\label{thm:real-cauchy-completion-witness-ledger-uniform-seal-route}
Every accepted $\RealCauchyCompletionUp$ carrier whose completion-facing route is also read through an accepted $\CauchyWitnessLedgerUp$ packet and an accepted $\UniformCauchyCriterionUp$ packet factors through one finite seal route. The route consists exactly of the regular-family row, unary modulus, diagonal packet, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\CauchyWitnessLedgerUp$ budget, synchronizer, and classifier rows, $\UniformCauchyCriterionUp$ shared-threshold rows, the public $\RealUp$ seal, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The combined route exports no quotient stream, selected host limit, host real equality, countable-choice row, second family-limit route, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with the completion-facing seal. The uniform criterion seal factorization theorem \autoref{thm:real-cauchy-completion-uniform-criterion-seal-factorization} places the accepted $\RealCauchyCompletionUp$ carrier on the finite regular-family, unary-modulus, diagonal-packet, window, readback, dyadic-ledger, transport, continuation, package, naming, and public $\RealUp$ seal route.

Next read the witness-ledger side. The witness-ledger classifier lock \autoref{thm:real-cauchy-completion-witness-ledger-classifier-lock} identifies the completion-seal classifier with the shared finite route through the ledger budget, observation, synchronizer, and classifier rows, and \autoref{thm:cauchy-witness-ledger-root-consumer-exhaustion} exhausts downstream completion consumers at those displayed ledger rows.

Finally attach the uniform criterion consumer surface. The downstream Real consumer export \autoref{thm:uniform-cauchy-criterion-downstream-real-consumer-export} and the Real-completion budget lock \autoref{thm:uniform-cauchy-criterion-real-completion-budget-lock} keep every $\RealCauchyCompletionUp$ read behind the same shared-threshold and finite-window rows. Intersecting the three non-escape clauses leaves no coordinate for quotient streams, selected host limits, host real equality, countable choice, a second family-limit route, or ambient $\RealUp$ completeness, so the sole exposed route is the finite BEDC packet named in the statement.
\end{proof}

\begin{theorem}[RealCauchyCompletion shared-tail modulus consumer route]
\label{thm:real-cauchy-completion-shared-tail-modulus-consumer-route}
Every accepted $\RealCauchyCompletionUp$ carrier whose diagonal packet consumes an accepted $\CauchySharedTailModulusUp$ row reaches the public $\RealUp$ seal through a single finite route. The route consists of the regular-family row, unary modulus, shared-tail threshold, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The route is relative to the shared-tail modulus surface of \autoref{ch:concrete-instances-cauchy-shared-tail-modulus-namecert}. It exports no quotient streams, selected limits, host real equality, countable-choice row, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the accepted completion carrier. The public real-name export theorem, \autoref{thm:real-cauchy-completion-public-real-name-export}, keeps the $\RealUp$ seal on the displayed completion path, while the diagonal handoff and downstream consumer handoff theorems bind that path to the regular-family row, unary modulus, finite windows, regular-rational readback, dyadic ledger, transports, continuations, provenance, and local naming rows.

Now attach the accepted $\CauchySharedTailModulusUp$ packet. Its consumer contribution is the shared-tail threshold for the same tail comparison, not a separate limit selector or quotient-stream equality.

Combining the two projections gives one displayed route from the completion diagonal packet to the public seal. The exclusions in the completion and shared-tail surfaces remove quotient streams, selected limits, host real equality, countable choice, and ambient completeness from the exported carrier.
\end{proof}

\begin{theorem}[RealCauchyCompletion dyadic tail-radius ledger route]
\label{thm:real-cauchy-completion-dyadic-tail-radius-ledger-route}
Every accepted $\RealCauchyCompletionUp$ carrier whose diagonal packet consumes an accepted $\DyadicTailRadiusLedgerUp$ row reaches the public $\RealUp$ seal only after the dyadic tail-radius ledger has been displayed. The finite route is the regular-family row, unary modulus, shared tail request, dyadic tail-radius ledger, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The route is relative to the dyadic ledger surface of \autoref{ch:concrete-instances-dyadic-tail-radius-ledger-namecert}. It exports no quotient-stream representative, selected limit, host real equality, countable-choice row, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Start from the displayed completion route supplied by \autoref{thm:real-cauchy-completion-tail-schedule-budget-exhaustion}. This already places the public seal after the regular-family row, unary modulus, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, diagonal packet, transports, continuations, provenance, and local naming rows.

Insert the $\DyadicTailRadiusLedgerUp$ packet at the tail-radius step. Its ledger row records the dyadic tolerance and radius accounting used by the same finite windows, so the completion consumer reads a displayed dyadic budget rather than an external completeness witness.

The resulting path is still the completion path. Since neither the completion route nor the dyadic ledger surface contains a coordinate for quotient representatives, selected limits, host real equality, countable choice, or ambient completeness, the public seal can only be reached through the rows named in the statement.
\end{proof}

\begin{theorem}[RealCauchyCompletion CauchyCriterion sibling route]
\label{thm:real-cauchy-completion-cauchycriterion-sibling-route}
Every accepted $\RealCauchyCompletionUp$ carrier whose diagonal packet consumes accepted $\CauchySharedTailModulusUp$, $\DyadicTailRadiusLedgerUp$, and $\CauchyCriterionUp$ rows reaches the public $\RealUp$ seal only through the combined finite rows of the regular-family row, unary modulus, shared-tail threshold, dyadic tail-radius ledger, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

The combined route is relative to \autoref{ch:concrete-instances-cauchy-shared-tail-modulus-namecert}, \autoref{ch:concrete-instances-dyadic-tail-radius-ledger-namecert}, and \autoref{ch:concrete-instances-cauchycriterion-namecert}. It exports no quotient streams, selected limits, host real equality, countable-choice row, or ambient completeness principle.
\end{theorem}
\begin{proof}
First apply the shared-tail modulus route above. It identifies the completion seal path with the regular-family row, unary modulus, shared-tail threshold, scheduled windows, readback, dyadic ledger, structural transport, provenance, and local naming rows.

Next apply the dyadic tail-radius ledger route above. It shows that the same scheduled windows must expose their dyadic radius accounting before the public $\RealUp$ seal is consumed.

Finally attach the sibling $\CauchyCriterionUp$ packet. Its role is to certify the Cauchy-condition route for the displayed shared threshold and dyadic ledger, not to import a selected limit or ambient completeness theorem.

The three sibling routes therefore assemble one completion-seal consumer packet. Because each component route is confined to $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows and excludes quotient streams, selected limits, host real equality, countable choice, and ambient completeness, the carrier reaches the public seal only through the finite rows listed in the statement.
\end{proof}

\begin{theorem}[RealCauchyCompletion diagonal seal uniformity]
\label{thm:real-cauchy-completion-diagonal-seal-uniformity}
Every accepted $\RealCauchyCompletionUp$ carrier whose diagonal packet consumes the same displayed $\CauchyCriterionUp$ row and the same singleton or fixed finite-family $\UniformCauchyCriterionUp$ rows factors through one finite completion route. The route consists of the regular-family row, shared unary modulus, dyadic tolerance, tail ledger, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\RealUp$ seal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The diagonal seal therefore exports no quotient streams, selected limits, host real equality, countable choice, ambient completeness principle, or second family-limit route. A downstream $\CompletionUp$ or $\RealCauchyCompletionUp$ consumer reads only the displayed finite seal path.
\end{theorem}
\begin{proof}
First apply \autoref{thm:real-cauchy-completion-cauchycriterion-sibling-route}. It places the completion seal behind the regular-family row, unary modulus, finite stream windows, regular-rational readback, dyadic ledger, $\RealUp$ seal, and structural $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Next use \autoref{thm:cauchycriterion-uniformcriterion-finite-family-reduction}. The displayed $\CauchyCriterionUp$ packet and the singleton or fixed finite-family $\UniformCauchyCriterionUp$ packet share the same modulus, dyadic tolerance, tail ledger, stream-name source, regular-rational tail, and real-seal rows, so the family route restricts to the same finite CauchyCriterion path.

Finally apply \autoref{thm:uniform-cauchy-criterion-downstream-real-consumer-export}. Its downstream consumer clause keeps the $\RealUp$ seal on the uniform finite-family route. Intersecting the non-escape clauses from the three cited routes removes quotient streams, selected limits, host real equality, countable choice, ambient completeness, and any second family-limit route from the diagonal seal.
\end{proof}

\begin{theorem}[RealCauchyCompletion StreamName RegSeqRat Real seal correspondence]
\label{thm:real-cauchy-completion-streamname-regseqrat-real-seal-correspondence}
Every accepted $\RealCauchyCompletionUp$ packet whose regular family, unary modulus, diagonal packet, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, and $\RealUp$ seal rows are displayed determines exactly the same public finite-window seal boundary as the composed handoff
$$
\StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$
Conversely, any consumer that uses this composed handoff must factor through the displayed $\RealCauchyCompletionUp$ regular-family, diagonal, stream-window, readback, dyadic-ledger, and real-seal rows before it reads a limit-seal boundary.

The correspondence is relative to the finite-name, regular-sequence, dyadic, and real-name surfaces of \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-real-namecert}. It exports no quotient stream, selected limit, host real equality, countable-choice row, or ambient completeness principle.
\end{theorem}
\begin{proof}
First use \autoref{thm:real-cauchy-completion-stream-regseq-tail-seal-factorization}. It places the finite $\StreamNameUp$ windows and $\RegSeqRatUp$ readback on the same tail-seal route as the accepted completion packet, so the stream-to-regular handoff is not a separate public boundary.

Next apply \autoref{thm:real-cauchy-completion-public-real-name-export}. This exports the public $\RealUp$ seal only through the displayed completion packet and the finite rows carried by the sibling $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ chapters.

Use \autoref{thm:real-cauchy-completion-regular-family-input-interface} to identify the input side of the route with the regular-family row and unary modulus. Then apply \autoref{thm:real-cauchy-completion-diagonal-seal-uniformity}, which keeps the diagonal packet and the real-seal consumer on one finite completion path.

For the converse, take any consumer of the composed $\StreamNameUp \to \RegSeqRatUp \to \RealUp$ handoff. The cited factorization and export theorems force its stream windows, regular-rational readback, dyadic ledger, and real seal to pass through the displayed completion rows before a limit-seal boundary is available. The sibling chapters supply exactly those finite public surfaces, so no consumer can bypass the $\RealCauchyCompletionUp$ rows while still reading the advertised boundary.
\end{proof}

\begin{theorem}[RealCauchyCompletion tail-budget uniform seal boundary]
\label{thm:real-cauchy-completion-tail-budget-uniform-seal-boundary}
Every accepted $\RealCauchyCompletionUp$ packet whose finite $\StreamNameUp$ windows, $\RegSeqRatUp$ tail selector, dyadic budget row, and $\RealUp$ endpoint seal satisfy the StreamName-RegSeqRat tail-budget uniformity hypotheses has a uniform completion-facing seal boundary. Every later finite window accepted by the same pointwise $\StreamNameUp$ classifier factors through the same displayed completion route.

Consequently the completion seal exports no new limit selector, quotient stream, host equality, countable-choice row, ambient completeness datum, or second completion-facing boundary beyond the displayed regular-family row, unary modulus, diagonal packet, finite stream windows, regular-rational readback, dyadic budget ledger, $\RealUp$ endpoint seal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\end{theorem}
\begin{proof}
Start with \autoref{thm:real-cauchy-completion-streamname-regseqrat-real-seal-correspondence}. It identifies the public completion seal boundary with the composed $\StreamNameUp \to \RegSeqRatUp \to \RealUp$ handoff, relative to the displayed regular-family, diagonal, stream-window, readback, dyadic-ledger, and real-seal rows.

Next apply \autoref{thm:streamname-regseqrat-tail-budget-uniformity}. For every later finite window accepted by the same pointwise StreamName classifier, the RegSeqRat tail selector and the RealUp budget consumer read the same tail-budget certificate. Thus enlarging the finite window changes only the visible window ledger.

Transport this uniform handoff back through \autoref{thm:real-cauchy-completion-stream-regseq-tail-seal-factorization} and \autoref{thm:real-cauchy-completion-public-real-name-export}. The first theorem keeps the StreamName windows and RegSeqRat readback on the completion tail-seal route, and the second theorem exposes the public RealUp seal only through the displayed completion rows.

The non-escape clauses of the cited routes now intersect at the same boundary: the later windows share one completion-facing route and cannot introduce a fresh limit selector, quotient stream, host equality, countable-choice row, ambient completeness datum, or second seal boundary.
\end{proof}

\begin{theorem}[RealCauchyCompletion witness-ledger observation-budget route]
\label{thm:real-cauchy-completion-witness-ledger-observation-budget-route}
Every accepted $\RealCauchyCompletionUp$ carrier whose completion-facing consumer is read through both an accepted $\CauchyWitnessLedgerUp$ packet and an accepted $\RealObservationBudgetUp$ packet reaches the public $\RealUp$ seal only through one shared finite route. The route consists exactly of the regular-family row, unary modulus, diagonal packet, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, witness-ledger budget, synchronizer, and classifier rows, observation-budget precision, window, and disclosure rows, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.

The route exports no quotient equality of regular streams, host real equality, selected limit, hidden unbounded observation schedule, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
First apply the witness-ledger classifier lock \autoref{thm:real-cauchy-completion-witness-ledger-classifier-lock}. It places the completion-facing public seal behind the regular-family row, unary modulus, diagonal packet, finite windows, regular-rational readback, dyadic ledger, and witness-ledger budget, synchronizer, and classifier rows. The uniform seal route \autoref{thm:real-cauchy-completion-witness-ledger-uniform-seal-route} keeps this consumer path on the same finite seal boundary.

Next apply the budgeted common-refinement theorem \autoref{thm:real-cauchy-completion-budgeted-common-refinement}. The accepted $\RealObservationBudgetUp$ packet supplies the precision rows, finite $\StreamNameUp$ windows, dyadic readbacks, regular handoffs, seal boundary, $\hsame$ transports, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows for the same request surface.

Finally intersect the observation-budget window coverage, real-seal boundary, and non-escape rows from \autoref{thm:real-observation-budget-window-coverage}, \autoref{thm:real-observation-budget-real-seal-boundary}, and \autoref{thm:real-observation-budget-nonescape}. The two readings therefore share one finite completion-facing route, and all omitted objects stay outside the public $\RealUp$ seal.
\end{proof}

\begin{theorem}[RealCauchyCompletion finite-window completion coverage]
\label{thm:real-cauchy-completion-finite-window-completion-coverage}
For every accepted $\RealCauchyCompletionUp$ packet and every finite $\mathsf{ProbeBundle}$ of unary precision requests, the completion-facing $\RealUp$ seal read is exhausted by the displayed finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback rows, $\DyadicRatCoreUp$ observation ledger, diagonal packet, $\RealUp$ seal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The exhausted route is the same finite-name path from $\StreamNameUp$ through $\RegSeqRatUp$ and $\DyadicRatCoreUp$ into $\RealUp$. It exports no quotient stream, selected limit, host real equality, countable-choice row, ambient completeness coordinate, or completion principle beyond those displayed certificate rows.
\end{theorem}
\begin{proof}
First apply \autoref{thm:real-cauchy-completion-probebundle-window-coverage}. It turns the finite $\mathsf{ProbeBundle}$ into one consumer surface whose pointwise requests pass through the displayed modulus rows, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback rows, $\DyadicRatCoreUp$ ledgers, and the structural $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ data.

Next use the StreamName handoff and the sibling finite-window coverage rows for regular sequences, dyadic observations, and real-name seals. These identify each window read as the same finite route from $\StreamNameUp$ to $\RegSeqRatUp$, then through $\DyadicRatCoreUp$ observation data into the public $\RealUp$ seal.

Finally apply \autoref{thm:real-cauchy-completion-streamname-regseqrat-real-seal-correspondence} together with the non-escape boundary of \autoref{thm:real-cauchy-completion-non-escape-boundary}. The composed route is therefore the only completion-facing seal read available to the accepted packet, and quotient streams, selected limits, host real equality, countable choice, ambient completeness, and any hidden completion coordinate remain outside the exported surface.
\end{proof}

\begin{theorem}[RealCauchyCompletion monadic four-face exit correspondence]
\label{thm:real-cauchy-completion-monadic-four-face-exit-correspondence}
For every accepted $\RealCauchyCompletionUp$ packet whose visible rows are the regular-family row, unary modulus, diagonal packet, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\RealUp$ seal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, the public L10 four-face exit coincides with the finite completion-transformer route read through $\CauchyCompletionMonadUp$.

Both readings expose exactly the same four roadmap objects $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$. Neither reading adds quotient stream equality, selected limits, host real equality, countable choice, ambient completeness, or bridge-axis evidence.
\end{theorem}
\begin{proof}
Start with \autoref{thm:real-cauchy-completion-streamname-regseqrat-real-seal-correspondence}. It identifies the public completion seal with the finite handoff through $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$, while keeping the $\DyadicRatCoreUp$ ledger on the same displayed completion route.

Apply \autoref{thm:real-cauchy-completion-finite-window-completion-coverage}. Every finite precision request is exhausted by the displayed windows, readbacks, dyadic ledger, diagonal packet, real seal, and structural $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, so the L10 exit has no additional coordinate.

Now read the same boundary through \autoref{ch:concrete-instances-cauchycompletionmonad-namecert}. Its unit, bind, public seal, finite-window exhaustion, and standard completion-boundary rows use the same four roadmap objects as the completion transformer. Intersecting the non-escape clauses on both sides gives one shared four-face exit and excludes quotient streams, selected limits, host real equality, countable choice, ambient completeness, and bridge-axis evidence.
\end{proof}

\closureat{RealCauchyCompletionUp}{seedStr}
\closureat{RealCauchyCompletionUp}{obligationStr}
\closureat{RealCauchyCompletionUp}{scopedStr}
\closureat{RealCauchyCompletionUp}{publicStr}
\closureat{RealCauchyCompletionUp}{bridgedStr}
\closureat{RealCauchyCompletionUp}{matureStr}

\begin{closurestatus}{\RealCauchyCompletionUp}
  \constructivestory{A $\RealCauchyCompletionUp$ packet is generated from a $\RegSeqRatUp$-indexed regular Cauchy family, an explicit unary modulus, a $\RealDiagonalCompletionUp$ diagonal packet, finite $\StreamNameUp$ windows, $\DyadicRatCoreUp$ observations, a $\RealUp$ seal row, package provenance, and a local $\NameCert$ row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{The mature paper scope is the public real-name export, standard bridge boundary, and limit-seal consumer surface through the displayed regular-family row, unary modulus, diagonal packet, finite windows, regular-rational readback, dyadic ledger, $\RealUp$ seal, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.}
  \formalstatus{\axiomCleanV}
  \leantarget{BEDC.Derived.RealCauchyCompletionUp.RealCauchyCompletionCarrier\_namecert\_obligation\_surface}
  \bridgestatus{paperBridge}
  \origin{human}
  \notclaimed{This chapter does not close $\RealUp$ completeness, quotient equality of regular streams, uniqueness of limits, host real construction, or bridge checking.}
  \upgradepath{Lean verification and bridge checking remain the next obligations for the same finite regular-family, diagonal, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\Pkg$, and $\NameCert$ rows.}
\end{closurestatus}
