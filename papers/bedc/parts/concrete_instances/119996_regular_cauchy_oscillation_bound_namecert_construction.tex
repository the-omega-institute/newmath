\chapter{A Concrete Naming Certificate for $\RegularCauchyOscillationBoundUp$}
\label{ch:concrete-instances-regular-cauchy-oscillation-bound-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy real names and explicit moduli of convergence, the $\RegularCauchyOscillationBoundUp$ packet records the finite tail-oscillation bound by which a regular rational sequence can expose a Real-facing Cauchy control row before a terminal seal is read. It sits after the tail-diameter boundary of \autoref{ch:concrete-instances-cauchy-tail-diameter-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name chapter of \autoref{ch:concrete-instances-real-namecert}. The packet gives Real and Completion routes one local L10 source row for tail oscillation bounds; it does not choose a limit, quotient Cauchy streams, compare host real values, invoke countable choice, or claim ambient $\RealUp$ completeness.

\begin{definition}[Regular-Cauchy oscillation-bound carrier]
\label{def:regular-cauchy-oscillation-bound-carrier}
A $\RegularCauchyOscillationBoundUp$ carrier is a finite $\BHist$ packet
$$
O=(S,R,D,T,B,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ window service row that selects the inspected tail windows;
\item $R$ is the $\RegSeqRatUp$ regular-rational readback row whose values are compared on those windows;
\item $D$ is the $\DyadicRatCoreUp$ tolerance row storing the requested oscillation radius and comparison scale;
\item $T$ is the sibling $\CauchyTailDiameterUp$ boundary row for the same $S,R,D$ surface;
\item $B$ is the oscillation-bound ledger, recording a finite index after which every inspected pair from $S$ has $D$-bounded $R$-oscillation;
\item $E$ is the terminal $\RealUp$ seal reached only after $S,R,D,T,B$ have been displayed;
\item $H$ stores componentwise $\hsame$ transports for the stream, regular readback, dyadic tolerance, tail-diameter, bound, seal, provenance, and local naming rows;
\item $C$ stores displayed $\Cont$ replay routes for reading the tail windows, comparing the regular rational values, exposing the bound ledger, and handing the result to a Real-facing consumer;
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyOscillationBoundUp$, $\CauchyTailDiameterUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RegularCauchyOscillationBoundUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a selected limit, quotient stream equality, host real equality, unlisted tolerance, countable choice, or ambient completion theorem.
\end{definition}

\begin{theorem}[Regular-Cauchy oscillation-bound NameCert obligations]
\label{thm:regular-cauchy-oscillation-bound-namecert-obligations}
For the carrier of \autoref{def:regular-cauchy-oscillation-bound-carrier}, the local $\NameCert_{\RegularCauchyOscillationBoundUp}$ surface consists of six finite obligations:
\begin{enumerate}
\item source binding: the bound ledger $B$ reads the displayed $\StreamNameUp$ window $S$, $\RegSeqRatUp$ source row $R$, and $\DyadicRatCoreUp$ tolerance row $D$;
\item tail-diameter anchoring: the sibling $\CauchyTailDiameterUp$ row $T$ is read before any oscillation-bound consumer may inspect the terminal seal;
\item bound exactness: $B$ records a finite tail index and a displayed pairwise comparison route witnessing $D$-bounded oscillation for the inspected tail windows;
\item stability: componentwise $\hsame$ transport through $H$ preserves the same source, tolerance, tail-diameter, bound, seal, replay, provenance, and naming rows;
\item ledger non-escape: every downstream consumer of the oscillation bound factors through $S,R,D,T,B,E,H,C,P,N$;
\item real-seal routing: a $\RealUp$ or completion-facing consumer can read the terminal seal only after the stream window, regular readback, dyadic tolerance, tail-diameter boundary, oscillation-bound ledger, transport, replay, provenance, and local naming rows have been exposed.
\end{enumerate}
\end{theorem}
\begin{proof}
Carrier admission is projection from the displayed tuple $O=(S,R,D,T,B,E,H,C,P,N)$. The source binding clause reads the stream window, regular-rational values, and dyadic tolerance before any oscillation statement is available.

The tail-diameter and bound clauses are local. The row $T$ supplies the sibling diameter boundary for the same displayed data, while $B$ records the finite index and pairwise comparison route that make the requested oscillation bound visible on the inspected tail.

Stability and replay are supplied by $H$ and $C$. Componentwise transport keeps every row over the same source data, and the continuation routes replay the window read, rational comparison, bound exposure, and Real-facing handoff in that order.

The non-escape and seal clauses follow from absence of extra coordinates. A $\RealUp$ or completion-facing consumer sees only the listed packet rows, so the certificate cannot export a selected limit, quotient stream equality, host comparison, countable choice, or ambient completeness theorem.
\end{proof}

\closureat{RegularCauchyOscillationBoundUp}{seedStr}

\begin{closurestatus}{\RegularCauchyOscillationBoundUp}
  \constructivestory{A $\RegularCauchyOscillationBoundUp$ packet is generated from a finite StreamName window, RegSeqRat readback, DyadicRatCore tolerance row, CauchyTailDiameter boundary, oscillation-bound ledger, Real seal, componentwise hsame transport, Cont replay, Pkg provenance, and local NameCert data.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-oscillation-bound-carrier} and \autoref{thm:regular-cauchy-oscillation-bound-namecert-obligations} seed the finite L10 Real and Completion surface for regular-Cauchy tail oscillation bounds.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, selected limits, quotient stream equality, host real comparison, countable choice, ambient RealUp completeness, or standard bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source binding, tail-diameter anchoring, bound exactness, hsame stability, ledger non-escape, Real seal routing, replay, provenance, and local naming for BEDC.Derived.RegularCauchyOscillationBoundUp.}
\end{closurestatus}
