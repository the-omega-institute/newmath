\begin{theorem}[Metric distance witness visible-context empty distance exactness]
\label{thm:metric-distance-witness-visible-context-empty-distance-iff}
For histories $p,q,x,y$, a visible-context distance witness whose central
distance ledger is empty is exactly unary evidence for the visible prefix and
suffix together with empty classification of both central endpoints:
$$
\mathsf{MetricDistanceWitness}(p\cdot x,y\cdot q,(p\cdot\emp)\cdot q)
\Longleftrightarrow
\mathsf{UnaryHistory}(p)\ \wedge\ \mathsf{UnaryHistory}(q)\ \wedge\
\hsame(x,\emp)\ \wedge\ \hsame(y,\emp).
$$
\end{theorem}
\begin{proof}
Apply visible-context exactness to peel off the prefix and suffix. The central
witness is then $\mathsf{MetricDistanceWitness}(x,y,\emp)$, which empty-distance
exactness identifies with $\hsame(x,\emp)$ and $\hsame(y,\emp)$. Conversely,
the two empty endpoint classifiers build the central empty-distance witness,
and visible-context exactness reattaches the same prefix and suffix.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_visible\_context\_empty\_distance\_iff}

\begin{theorem}[Metric visible-context empty-distance continuation splice]
\label{thm:metric-distance-witness-visible-context-empty-distance-continuation-splice}
For histories $p,q,x,y,l,r,m,o$, suppose
$$
\mathsf{MetricDistanceWitness}(p\cdot x,y\cdot q,(p\cdot\emp)\cdot q)
$$
and continuations $\ContR(l,x,m)$ and $\ContR(m,r,o)$ hold. Then the empty
central endpoint exposed by the witness splices the continuation directly:
$$
\ContR(l,r,o).
$$
\end{theorem}
\begin{proof}
Apply visible-context empty-distance exactness to the witness. This gives
$\hsame(x,\emp)$. Substitute the empty central endpoint into
$\ContR(l,x,m)$, so the first continuation identifies $m$ with $l$. The second
continuation $\ContR(m,r,o)$ is therefore the desired direct continuation
$\ContR(l,r,o)$.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_visible\_context\_empty\_distance\_continuation\_splice}

\begin{theorem}[Metric visible-context empty-distance continuation splice determinacy]
\label{thm:metric-distance-witness-visible-context-empty-distance-continuation-splice-hsame}
For histories $p,q,x,y,l,r,m,o,o'$, suppose
$$
\mathsf{MetricDistanceWitness}(p\cdot x,y\cdot q,(p\cdot\emp)\cdot q)
$$
and continuations $\ContR(l,x,m)$, $\ContR(m,r,o)$, and $\ContR(l,r,o')$
hold. Then the spliced result is determined by the displayed direct
continuation:
$$
\hsame(o,o').
$$
\end{theorem}
\begin{proof}
The visible-context empty-distance continuation splice turns the first two
continuations into $\ContR(l,r,o)$. BEDC continuation determinacy compares this
spliced continuation with the displayed continuation $\ContR(l,r,o')$, giving
$\hsame(o,o')$.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_visible\_context\_empty\_distance\_continuation\_splice\_hsame}

\begin{theorem}[Metric visible-context empty-distance continuation result determinacy]
\label{thm:metric-distance-witness-visible-context-empty-distance-continuation-results-hsame}
For histories $p,q,x,y,l,m,n$, suppose
$$
\mathsf{MetricDistanceWitness}(p\cdot x,y\cdot q,(p\cdot\emp)\cdot q)
$$
and continuations $\ContR(l,x,m)$ and $\ContR(l,y,n)$ hold. Then the two
continuation results are classified together:
$$
\hsame(m,n).
$$
\end{theorem}
\begin{proof}
Apply visible-context empty-distance exactness to the metric witness. This
identifies both central endpoints $x$ and $y$ with $\emp$. After substituting
these endpoint classifiers, the two displayed continuations have the same
left source and the same empty right endpoint. BEDC continuation determinacy
therefore compares their results and gives $\hsame(m,n)$.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_visible\_context\_empty\_distance\_continuation\_results\_hsame}

\begin{theorem}[Metric visible-context empty-distance append unit laws]
\label{thm:metric-distance-witness-visible-context-empty-distance-append-unit-laws}
For histories $p,q,x,y,l,r$, suppose
$$
\mathsf{MetricDistanceWitness}(p\cdot x,y\cdot q,(p\cdot\emp)\cdot q).
$$
Then the central endpoints act as append units:
$$
x\cdot r=r\quad\wedge\quad l\cdot y=l.
$$
\end{theorem}
\begin{proof}
Apply visible-context empty-distance exactness to the witness. This exposes
$\hsame(x,\emp)$ and $\hsame(y,\emp)$. Substituting these empty endpoints turns
the two displayed append equations into the BEDC left and right append unit
laws.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_visible\_context\_empty\_distance\_append\_unit\_laws}

\begin{theorem}[Metric visible-context empty-distance witness splice]
\label{thm:metric-distance-witness-visible-context-empty-distance-witness-splice}
For histories $p,q,x,y,l,r,m,o$, suppose
$$
\mathsf{MetricDistanceWitness}(p\cdot x,y\cdot q,(p\cdot\emp)\cdot q)
$$
and unary endpoint evidence for $l$ and $r$ hold. If
$\ContR(l,x,m)$ and $\ContR(m,r,o)$ hold, then the spliced continuation is a
metric distance witness:
$$
\mathsf{MetricDistanceWitness}(l,r,o).
$$
\end{theorem}
\begin{proof}
The visible-context empty-distance continuation splice supplies
$\ContR(l,r,o)$. Together with the unary endpoint hypotheses, unary
continuation closure gives the unary carrier for $o$. These three fields are
exactly the concrete metric distance witness from $l$ to $r$ with output $o$.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_visible\_context\_empty\_distance\_witness\_splice}

\begin{theorem}[Metric visible-context empty-distance continuation witness depth package]
\label{thm:metric-distance-witness-visible-context-empty-distance-continuation-witness-depth}
For histories $p,q,x,y,l,r,m,o:\mathsf{BHist}$, suppose
$$
\mathsf{MetricDistanceWitness}(p\cdot x,y\cdot q,(p\cdot\emp)\cdot q)
$$
holds together with $\mathsf{UnaryHistory}(l)$, $\mathsf{UnaryHistory}(r)$,
$\ContR(l,x,m)$, and $\ContR(m,r,o)$. Then the direct continuation carries a
metric witness whose depth is induced by the two visible endpoints:
$$
\mathsf{MetricDistanceWitness}(l,r,o)\wedge
\mathsf{MetricDistanceDepth}(o)=
\mathsf{MetricDistanceDepth}(l)+\mathsf{MetricDistanceDepth}(r).
$$
\end{theorem}
\begin{proof}
Apply the visible-context empty-distance continuation splice to the distance
witness and the two continuation ledgers. This gives the direct continuation
$\ContR(l,r,o)$.

The unary carriers for $l$ and $r$ then close the continuation under the metric
distance witness constructor, so the direct continuation packages as
$\mathsf{MetricDistanceWitness}(l,r,o)$.

Finally, metric witness depth additivity applied to this witness gives
$\mathsf{MetricDistanceDepth}(o)=
\mathsf{MetricDistanceDepth}(l)+\mathsf{MetricDistanceDepth}(r)$. Pairing the
witness with that displayed equation is the required conjunction.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_visible\_context\_empty\_distance\_continuation\_witness\_depth}

The ten-field fit is: traditional target visible-context zero-distance splice;
BEDC source $\mathsf{MetricDistanceWitness}$ plus $\ContR$; carrier
$\mathsf{MetricDistanceWitness}$; classifier $\hsame$; stability obligations
the two unary carriers and the two continuation ledgers; ledger the hidden
empty central distance; core theorem this package; exactness the displayed
witness-plus-depth conjunction; standard bridge deferred; and status
$\mathsf{ProofTarget}$.
