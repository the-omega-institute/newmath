\begin{theorem}[Metric distance witness prefix-independent symmetric classifier]
\label{thm:metric-distance-witness-visible-context-prefix-independent-symmetric-classifier}
For histories $p,q,p',q',x,y,d_{xy},d_{yx}$, symmetric metric distance
witnesses with independent visible contexts still determine the same central
distance classifier:
$$
\begin{array}{l}
\mathsf{MetricDistanceWitness}(p\cdot x,y\cdot q,(p\cdot d_{xy})\cdot q)
\Longrightarrow
\\
\mathsf{MetricDistanceWitness}(p'\cdot y,x\cdot q',(p'\cdot d_{yx})\cdot q')
\Longrightarrow
\hsame(d_{xy},d_{yx}).
\end{array}
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:metric-distance-witness-visible-context-iff} to both
visible-context witnesses. This recovers central witnesses
$\mathsf{MetricDistanceWitness}(x,y,d_{xy})$ and
$\mathsf{MetricDistanceWitness}(y,x,d_{yx})$. The central symmetric classifier
then gives $\hsame(d_{xy},d_{yx})$.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_visible\_context\_prefix\_independent\_symmetric\_classifier}

\begin{theorem}[Metric distance witness prefix-independent symmetric zero-depth collapse]
\label{thm:metric-distance-witness-prefix-independent-symmetric-zero-depth-collapse}
For histories $p,q,p',q',x,y,d_{xy},d_{yx}$, symmetric visible-context
distance witnesses with independent contexts and a zero-depth forward distance
collapse the two central distances and both central endpoints:
$$
\begin{array}{l}
\mathsf{MetricDistanceWitness}(p\cdot x,y\cdot q,(p\cdot d_{xy})\cdot q)
\Longrightarrow
\\
\mathsf{MetricDistanceWitness}(p'\cdot y,x\cdot q',(p'\cdot d_{yx})\cdot q')
\Longrightarrow
\mathsf{MetricDistanceDepth}(d_{xy})=0
\Longrightarrow
\\
\hsame(d_{xy},d_{yx})\land
\mathsf{MetricDistanceDepth}(d_{yx})=0\land
\hsame(x,\emp)\land\hsame(y,\emp).
\end{array}
$$
\end{theorem}
\begin{proof}
Apply
\autoref{thm:metric-distance-witness-visible-context-prefix-independent-symmetric-classifier}
to the two visible-context witnesses. This gives
$\hsame(d_{xy},d_{yx})$.

Apply
\autoref{thm:metric-distance-witness-visible-context-depth-zero-collapse} to
the first visible-context witness and the zero-depth hypothesis for $d_{xy}$.
This gives the empty endpoint classifiers $\hsame(x,\emp)$ and
$\hsame(y,\emp)$.

Finally peel the second visible-context witness with
\autoref{thm:metric-distance-witness-visible-context-iff}. The recovered
central witness $\mathsf{MetricDistanceWitness}(y,x,d_{yx})$, together with the
two empty endpoint classifiers in reversed order, is the input required by
\autoref{thm:metric-distance-witness-empty-endpoints-depth-zero}; hence
$\mathsf{MetricDistanceDepth}(d_{yx})=0$. Combining these three pieces gives
the displayed conjunction.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_visible\_context\_prefix\_independent\_symmetric\_zero\_depth\_collapse}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_prefix\_independent\_symmetric\_zero\_depth\_collapse}

\begin{theorem}[Metric prefix-independent symmetric zero-depth reverse empty-boundary]
\label{thm:metric-prefix-independent-symmetric-zero-depth-reverse-empty-boundary}
For histories $p,q,p',q',x,y,d_{xy},d_{yx}$, symmetric visible-context
distance witnesses with independent contexts and a zero-depth forward distance
also package the reverse context as its own empty-boundary distance witness:
$$
\begin{array}{l}
\mathsf{MetricDistanceWitness}(p\cdot x,y\cdot q,(p\cdot d_{xy})\cdot q)
\Longrightarrow
\\
\mathsf{MetricDistanceWitness}(p'\cdot y,x\cdot q',(p'\cdot d_{yx})\cdot q')
\Longrightarrow
\mathsf{MetricDistanceDepth}(d_{xy})=0
\Longrightarrow
\\
\mathsf{MetricDistanceWitness}(p'\cdot\emp,\emp\cdot q',
(p'\cdot\emp)\cdot q').
\end{array}
$$
\end{theorem}
\begin{proof}
Peel the reverse visible-context witness with
\autoref{thm:metric-distance-witness-visible-context-iff}. This recovers the
unary context data for $p'$ and $q'$ together with the central reverse witness
$\mathsf{MetricDistanceWitness}(y,x,d_{yx})$.

Apply
\autoref{thm:metric-distance-witness-prefix-independent-symmetric-zero-depth-collapse}
to the two displayed visible-context witnesses and the zero-depth forward
distance. The resulting endpoint classifiers include $\hsame(y,\emp)$ and
$\hsame(x,\emp)$.

Finally use
\autoref{thm:metric-distance-witness-visible-context-empty-distance-iff} in the
reverse context. The recovered carriers for $p'$ and $q'$, together with the
empty endpoint classifiers in reverse order, rebuild exactly
$\mathsf{MetricDistanceWitness}(p'\cdot\emp,\emp\cdot q',(p'\cdot\emp)\cdot q')$.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_prefix\_independent\_symmetric\_zero\_depth\_reverse\_empty\_boundary\_package}

\begin{theorem}[Metric prefix-independent symmetric zero-depth two-sided empty-boundary package]
\label{thm:metric-prefix-independent-symmetric-zero-depth-two-sided-empty-boundary-package}
\label{thm:metric-prefix-independent-symmetric-zero-depth-two-sided-empty-boundary}
For histories $p,q,p',q',x,y,d_{xy},d_{yx}$, symmetric visible-context
distance witnesses with independent contexts and a zero-depth forward distance
package both contextual empty-boundary witnesses together with the reverse
zero-depth row and the central endpoint classifiers:
$$
\begin{array}{l}
\mathsf{MetricDistanceWitness}(p\cdot x,y\cdot q,(p\cdot d_{xy})\cdot q)
\Longrightarrow
\\
\mathsf{MetricDistanceWitness}(p'\cdot y,x\cdot q',(p'\cdot d_{yx})\cdot q')
\Longrightarrow
\mathsf{MetricDistanceDepth}(d_{xy})=0
\Longrightarrow
\\
\mathsf{MetricDistanceWitness}(p\cdot\emp,\emp\cdot q,
(p\cdot\emp)\cdot q)\land
\\
\mathsf{MetricDistanceWitness}(p'\cdot\emp,\emp\cdot q',
(p'\cdot\emp)\cdot q')\land
\mathsf{MetricDistanceDepth}(d_{yx})=0\land
\hsame(x,\emp)\land\hsame(y,\emp).
\end{array}
$$
\end{theorem}
\begin{proof}
Apply
\autoref{thm:metric-distance-witness-prefix-independent-symmetric-zero-depth-collapse}
to the two visible-context witnesses and the zero-depth forward distance. This
gives the same-distance classifier, the reverse zero-depth row, and the two
central endpoint classifiers $\hsame(x,\emp)$ and $\hsame(y,\emp)$.

Peel the forward visible-context witness with
\autoref{thm:metric-distance-witness-visible-context-iff} to recover the unary
carrier rows for $p$ and $q$. These carriers, together with the endpoint
classifiers from the collapse theorem, are exactly the right-hand data in
\autoref{thm:metric-distance-witness-visible-context-empty-distance-iff};
hence they build
$\mathsf{MetricDistanceWitness}(p\cdot\emp,\emp\cdot q,(p\cdot\emp)\cdot q)$.

Apply
\autoref{thm:metric-prefix-independent-symmetric-zero-depth-reverse-empty-boundary}
to the same hypotheses to obtain the reverse contextual empty-boundary witness.
Conjoining the forward package, the reverse package, the reverse zero-depth
row, and the two endpoint classifiers gives the displayed conclusion.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_prefix\_independent\_symmetric\_zero\_depth\_two\_sided\_empty\_boundary\_package}

The ten-field fit is: traditional target metric zero-depth symmetric boundary
packaging; BEDC source $\mathsf{BHist}$ with
$\mathsf{MetricDistanceWitness}$ and $\mathsf{MetricDistanceDepth}$; carrier
$\mathsf{MetricDistanceWitness}$; classifier $\hsame$; stability zero-depth
endpoint collapse; ledger the two symmetric visible contexts; core theorem
Theorem~\ref{thm:metric-prefix-independent-symmetric-zero-depth-two-sided-empty-boundary-package};
exactness two-sided empty-boundary packaging; standard bridge none claimed;
status $\mathsf{ProofTarget}$.

\begin{theorem}[Metric prefix-independent symmetric zero-depth continuation splice]
\label{thm:metric-prefix-independent-symmetric-zero-depth-continuation-splice}
For histories $p,q,p',q',x,y,d_{xy},d_{yx},l,r,mid,out$, symmetric
visible-context distance witnesses with independent contexts and zero-depth
forward distance make any continuation through the reverse central distance
splice across the empty boundary:
$$
\begin{array}{l}
\mathsf{MetricDistanceWitness}(p\cdot x,y\cdot q,(p\cdot d_{xy})\cdot q)
\to
\mathsf{MetricDistanceWitness}(p'\cdot y,x\cdot q',(p'\cdot d_{yx})\cdot q')
\to
\mathsf{MetricDistanceDepth}(d_{xy})=0
\to
\\
\ContR(l,d_{yx},mid)\to
\ContR(mid,r,out)\to
\ContR(l,r,out).
\end{array}
$$
\end{theorem}
\begin{proof}
The symmetric zero-depth collapse theorem gives
$\mathsf{MetricDistanceDepth}(d_{yx})=0$. The depth-zero classifier theorem
converts this to $\hsame(d_{yx},\emp)$. Case analysis reduces the left
continuation to the right-unit case, identifying $mid$ with $l$, and the
remaining continuation is exactly the displayed splice result.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_prefix\_independent\_symmetric\_zero\_depth\_continuation\_splice}

\begin{theorem}[Metric prefix-independent symmetric zero-depth witness splice]
\label{thm:metric-prefix-independent-symmetric-zero-depth-witness-splice}
For histories $p,q,p',q',x,y,d_{xy},d_{yx},l,r,mid,out$, symmetric
visible-context distance witnesses with independent contexts and zero-depth
forward distance turn any unary continuation through the reverse central
distance into a metric distance witness:
$$
\begin{array}{l}
\mathsf{MetricDistanceWitness}(p\cdot x,y\cdot q,(p\cdot d_{xy})\cdot q)
\to
\mathsf{MetricDistanceWitness}(p'\cdot y,x\cdot q',(p'\cdot d_{yx})\cdot q')
\to
\mathsf{MetricDistanceDepth}(d_{xy})=0
\to
\\
\mathsf{UnaryHistory}(l)\to \mathsf{UnaryHistory}(r)\to
\ContR(l,d_{yx},mid)\to \ContR(mid,r,out)\to
\mathsf{MetricDistanceWitness}(l,r,out).
\end{array}
$$
\end{theorem}
\begin{proof}
The continuation-splice theorem gives \(\ContR(l,r,out)\) from the two
visible-context witnesses, the zero-depth row, and the two displayed
continuation ledgers through \(d_{yx}\). The supplied unary histories for
\(l\) and \(r\), together with unary continuation closure, give the unary
carrier row for \(out\). These three carrier rows and the spliced continuation
are exactly the metric distance witness package.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_prefix\_independent\_symmetric\_zero\_depth\_witness\_splice}

\begin{theorem}[Metric symmetric zero-depth distance continuation result]
\label{thm:metric-distance-witness-prefix-independent-symmetric-zero-depth-distance-continuation-result}
For histories $p,q,p',q',x,y,d_{xy},d_{yx},l,mid,out$, symmetric
visible-context distance witnesses with independent contexts and zero-depth
forward distance make any two-step continuation through the two central
distances return to the original left endpoint:
$$
\begin{array}{l}
\mathsf{MetricDistanceWitness}(p\cdot x,y\cdot q,(p\cdot d_{xy})\cdot q)
\to
\mathsf{MetricDistanceWitness}(p'\cdot y,x\cdot q',(p'\cdot d_{yx})\cdot q')
\to
\mathsf{MetricDistanceDepth}(d_{xy})=0
\to
\\
\ContR(l,d_{xy},mid)\to
\ContR(mid,d_{yx},out)\to
\hsame(out,l).
\end{array}
$$
\end{theorem}
\begin{proof}
The forward zero-depth hypothesis converts $d_{xy}$ to the empty history by
the depth-zero classifier theorem. The symmetric zero-depth collapse theorem
also gives zero depth for $d_{yx}$, hence $d_{yx}\hsame\emp$. Case analysis on
these two empty-distance classifiers reduces both continuations to right-unit
continuations, first identifying $mid$ with $l$ and then $out$ with $mid$.
Transitivity gives the displayed $\hsame(out,l)$.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_prefix\_independent\_symmetric\_zero\_depth\_distance\_continuation\_result}

\begin{theorem}[Metric symmetric zero-depth reverse distance continuation result]
\label{thm:metric-distance-witness-prefix-independent-symmetric-zero-depth-reverse-distance-continuation-result}
For histories $p,q,p',q',x,y,d_{xy},d_{yx},l,mid,out$, symmetric
visible-context distance witnesses with independent contexts and zero-depth
forward distance also make the reverse-ordered two-step continuation return to
the original left endpoint:
$$
\begin{array}{l}
\mathsf{MetricDistanceWitness}(p\cdot x,y\cdot q,(p\cdot d_{xy})\cdot q)
\to
\mathsf{MetricDistanceWitness}(p'\cdot y,x\cdot q',(p'\cdot d_{yx})\cdot q')
\to
\mathsf{MetricDistanceDepth}(d_{xy})=0
\to
\\
\ContR(l,d_{yx},mid)\to
\ContR(mid,d_{xy},out)\to
\hsame(out,l).
\end{array}
$$
\end{theorem}
\begin{proof}
Apply
\autoref{thm:metric-distance-witness-prefix-independent-symmetric-zero-depth-collapse}
to the two visible-context witnesses and the zero-depth hypothesis for
$d_{xy}$. This gives $\mathsf{MetricDistanceDepth}(d_{yx})=0$.

Now apply
\autoref{thm:metric-distance-witness-prefix-independent-symmetric-zero-depth-distance-continuation-result}
with the two visible contexts swapped: $p',q',y,x,d_{yx}$ play the forward
roles and $p,q,x,y,d_{xy}$ play the reverse roles. The recovered zero-depth
row for $d_{yx}$ is the forward depth hypothesis in that swapped instance, and
the displayed ledgers are exactly $\ContR(l,d_{yx},mid)$ followed by
$\ContR(mid,d_{xy},out)$.

Thus the swapped distance-continuation result returns $\hsame(out,l)$. The
fit records traditional target zero-distance round trip, BEDC source
$\mathsf{BHist}$ with $\ContR$ and $\mathsf{MetricDistanceWitness}$, carrier
the visible-context witness pair, classifier $\hsame$, stability from
zero-depth collapse and continuation unit laws, ledger the two central distance
histories, core theorem this reverse-order round trip, exactness
boundary-unit cancellation, standard bridge zero metric distance as identity
path, and status $\mathsf{ProofTarget}$.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_prefix\_independent\_symmetric\_zero\_depth\_reverse\_distance\_continuation\_result}
