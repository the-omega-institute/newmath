The $\MetricUp$ seed surface is the carrier, distance witness, boundary exactness, real-distance interface, triangle continuation, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row described by \autoref{thm:metric-seed-closure-surface}. Its binding closure registration is recorded in the obligation-status child file, where the same checked Lean target is read independently from the paper-side closure grade.
