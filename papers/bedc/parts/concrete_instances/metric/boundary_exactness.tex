\begin{theorem}[Metric distance witness empty-source visible-distance target exactness]
\label{thm:metric-distance-witness-empty-source-e1-distance-target-exactness}
For histories $target,d$, a concrete distance witness from the empty source
whose distance ledger is visibly one-headed identifies the target with that
same one-headed ledger and exposes unary evidence for the ledger tail:
$$
\mathsf{MetricDistanceWitness}(\emp,target,\Eone(d))
\Longrightarrow
target=\Eone(d)\land\mathsf{UnaryHistory}(d).
$$
\end{theorem}
\begin{proof}
Apply empty-left exactness to obtain unary evidence for $target$ and the
classifier $\hsame(\Eone(d),target)$. One-constructor inversion on this
classifier presents $target$ as $\Eone(d)$ up to the exposed tail equality.
Substitution into the unary target witness and unary one-step inversion give
$\mathsf{UnaryHistory}(d)$.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_empty\_source\_e1\_distance\_target\_exactness}

\begin{theorem}[Metric distance witness empty-target visible-distance source exactness]
\label{thm:metric-distance-witness-empty-target-e1-distance-source-exactness}
For histories $source,d$, a concrete distance witness into the empty target
whose distance ledger is visibly one-headed identifies the source with that
same one-headed ledger and exposes unary evidence for the ledger tail:
$$
\mathsf{MetricDistanceWitness}(source,\emp,\Eone(d))
\Longrightarrow
source=\Eone(d)\land\mathsf{UnaryHistory}(d).
$$
\end{theorem}
\begin{proof}
Apply empty-right exactness to obtain unary evidence for $source$ and the
classifier $\hsame(\Eone(d),source)$. One-constructor inversion on this
classifier presents $source$ as $\Eone(d)$ up to the exposed tail equality.
Substitution into the unary source witness and unary one-step inversion give
$\mathsf{UnaryHistory}(d)$.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_empty\_target\_e1\_distance\_source\_exactness}

\begin{theorem}[Metric nonempty distance has a nonempty endpoint]
\label{thm:metric-distance-witness-nonempty-distance-endpoint-nonempty}
For histories $x,y,d:\mathsf{BHist}$, if
$\mathsf{MetricDistanceWitness}(x,y,d)$ holds and $d$ is not history-same as
$\emp$, then at least one endpoint is not history-same as $\emp$:
$$
\mathsf{MetricDistanceWitness}(x,y,d)\to
(d\hsame\emp\to\mathsf{False})\to
\bigl((x\hsame\emp\to\mathsf{False})
\vee (y\hsame\emp\to\mathsf{False})\bigr).
$$
\end{theorem}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_nonempty\_distance\_endpoint\_nonempty}
\begin{proof}
Case-split the two endpoint histories. A visible endpoint constructor is
immediately disjoint from $\emp$, giving the required side of the disjunction.
In the remaining empty/empty case, the continuation field of the metric witness
identifies the distance result with $\emp$, contradicting the assumed
nonempty distance.
\end{proof}

\begin{theorem}[Metric nonempty distance endpoint positive depth]
\label{thm:metric-distance-witness-nonempty-distance-endpoint-positive-depth}
For histories $x,y,d:\mathsf{BHist}$, if
$\mathsf{MetricDistanceWitness}(x,y,d)$ holds and $d$ is not history-same as
$\emp$, then at least one endpoint has positive structural metric depth:
$$
\mathsf{MetricDistanceWitness}(x,y,d)\to
(d\hsame\emp\to\mathsf{False})\to
\bigl(0<\mathsf{MetricDistanceDepth}(x)
\vee 0<\mathsf{MetricDistanceDepth}(y)\bigr).
$$
\end{theorem}
\begin{proof}
Apply the nonempty-distance endpoint theorem to obtain a nonempty endpoint.
The positive-depth classifier for metric histories converts that endpoint
nonemptiness into positive structural depth on the corresponding side of the
disjunction.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_nonempty\_distance\_endpoint\_positive\_depth}

\begin{theorem}[Metric positive distance e-one shape]
\label{thm:metric-distance-witness-positive-distance-e1-shape}
For histories $x,y,d:\mathsf{BHist}$, if
$\mathsf{MetricDistanceWitness}(x,y,d)$ holds and the structural depth of
$d$ is not zero, then $d$ is visibly one-headed with a unary tail:
$$
\mathsf{MetricDistanceWitness}(x,y,d)\to
(\mathsf{MetricDistanceDepth}(d)=0\to\mathsf{False})\to
\exists tail,\ d=\Eone(tail)\wedge\mathsf{UnaryHistory}(tail).
$$
\end{theorem}
\begin{proof}
Unpack the metric distance witness to obtain the unary carrier for the
distance history $d$. Case analysis on $d$ rules out $\emp$ by the positive
depth hypothesis and rules out an $\Ezero$ head by unary-history inversion.
The remaining $\Eone$ case exposes its tail, and one-step unary inversion gives
the unary tail witness.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_positive\_distance\_e1\_shape}

\begin{theorem}[Metric nonempty distance endpoint e-one cases]
\label{thm:metric-distance-witness-nonempty-distance-endpoint-e1-cases}
For histories $x,y,d:\mathsf{BHist}$, if
$\mathsf{MetricDistanceWitness}(x,y,d)$ holds and the distance endpoint is not
history-same as $\emp$, then at least one endpoint is visibly one-headed with a
unary tail:
$$
\begin{array}{l}
\mathsf{MetricDistanceWitness}(x,y,d)\to
(d\hsame\emp\to\mathsf{False})\to\\
\qquad
\bigl(\exists x_0,\ x=\Eone(x_0)\land\mathsf{UnaryHistory}(x_0)\bigr)
\vee
\bigl(\exists y_0,\ y=\Eone(y_0)\land\mathsf{UnaryHistory}(y_0)\bigr).
\end{array}
$$
\end{theorem}
\begin{proof}
First apply the endpoint non-emptiness theorem to the metric witness and the
nonempty distance assumption. On whichever side is nonempty, use the endpoint's
unary-history carrier from the metric witness and apply unary nonempty
one-tail readback.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_nonempty\_distance\_endpoint\_e1\_cases}

\begin{theorem}[Metric empty-source nonempty distance target e-one cases]
\label{thm:metric-distance-witness-empty-source-nonempty-distance-target-e1-cases}
For histories $target,dist:\mathsf{BHist}$, if a concrete metric distance
witness starts at the empty source and its distance endpoint is not
history-same as $\emp$, then the target endpoint is visibly one-headed with a
unary tail:
$$
\mathsf{MetricDistanceWitness}(\emp,target,dist)\to
(dist\hsame\emp\to\mathsf{False})\to
\exists targetTail,\ target=\Eone(targetTail)\land
\mathsf{UnaryHistory}(targetTail).
$$
\end{theorem}
\begin{proof}
Apply the nonempty-distance endpoint e-one cases theorem. The source-side
visible branch would identify $\emp$ with a one-headed history, which is
impossible by constructor disjointness. The remaining branch is exactly the
target e-one readback.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_empty\_source\_nonempty\_distance\_target\_e1\_cases}

\begin{theorem}[Metric empty-target nonempty distance source e-one cases]
\label{thm:metric-distance-witness-empty-target-nonempty-distance-source-e1-cases}
For histories $source,dist:\mathsf{BHist}$, if a concrete metric distance
witness ends at the empty target and its distance endpoint is not history-same
as $\emp$, then the source endpoint is visibly one-headed with a unary tail:
$$
\mathsf{MetricDistanceWitness}(source,\emp,dist)\to
(dist\hsame\emp\to\mathsf{False})\to
\exists sourceTail,\ source=\Eone(sourceTail)\land
\mathsf{UnaryHistory}(sourceTail).
$$
\end{theorem}
\begin{proof}
Apply the nonempty-distance endpoint e-one cases theorem. The target-side
visible branch would identify $\emp$ with a one-headed history, which is
impossible by constructor disjointness. The remaining branch is exactly the
source e-one readback.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_empty\_target\_nonempty\_distance\_source\_e1\_cases}

\begin{theorem}[Metric empty-boundary nonempty distance positive endpoint depth]
\label{thm:metric-distance-witness-empty-boundary-nonempty-distance-positive-endpoint-depth}
For histories $source,target,dist:\mathsf{BHist}$, a concrete metric distance
witness with exactly one empty boundary and a nonempty distance endpoint forces
positive structural depth on the other endpoint:
$$
\begin{array}{l}
\bigl(\mathsf{MetricDistanceWitness}(\emp,target,dist)\to
(dist\hsame\emp\to\mathsf{False})\to
0<\mathsf{MetricDistanceDepth}(target)\bigr)
\land\\
\bigl(\mathsf{MetricDistanceWitness}(source,\emp,dist)\to
(dist\hsame\emp\to\mathsf{False})\to
0<\mathsf{MetricDistanceDepth}(source)\bigr).
\end{array}
$$
\end{theorem}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_empty\_boundary\_nonempty\_distance\_positive\_endpoint\_depth}
\begin{proof}
For the empty-source implication, apply
Theorem~\ref{thm:metric-distance-witness-empty-source-nonempty-distance-target-e1-cases}
to the displayed witness and the nonempty distance hypothesis. The result
expresses $target$ as $\Eone(targetTail)$ with a unary tail, so the structural
metric depth of $target$ is a successor and is therefore positive.

For the empty-target implication, apply
Theorem~\ref{thm:metric-distance-witness-empty-target-nonempty-distance-source-e1-cases}
in the same way. The source endpoint is read back as $\Eone(sourceTail)$ with a
unary tail, and the one-headed shape gives positive structural metric depth for
$source$.
\end{proof}

The ten-field fit is: traditional target paired empty-boundary nonzero-distance
endpoint depth; BEDC source $\mathsf{MetricDistanceWitness}$ over
$\mathsf{BHist}$; carrier the surviving boundary endpoint; classifier
$\hsame$; stability unary one-headed readback and depth positivity; ledger the
nonempty distance endpoint; core theorem
Theorem~\ref{thm:metric-distance-witness-empty-boundary-nonempty-distance-positive-endpoint-depth};
exactness positive depth on the nonempty boundary endpoint; standard bridge
finite unary metric boundary readback; status $\mathsf{ProofTarget}$.

\begin{theorem}[Metric visible-boundary positive-depth endpoint readback]
\label{thm:metric-distance-witness-visible-boundary-positive-depth-endpoint-readback}
For histories $p,q,source,target,dist:\mathsf{BHist}$, a visible-context
metric distance witness with exactly one empty central boundary and positive
central distance depth reads back the nonempty central endpoint as one-headed
and positive-depth:
$$
\begin{array}{l}
\bigl(
\mathsf{MetricDistanceWitness}(p\cdot\emp,target\cdot q,(p\cdot dist)\cdot q)
\to
(\mathsf{MetricDistanceDepth}(dist)=0\to\mathsf{False})
\to\\
\qquad
\exists targetTail,\ target=\Eone(targetTail)\land
\mathsf{UnaryHistory}(targetTail)\land
0<\mathsf{MetricDistanceDepth}(target)
\bigr)
\land\\
\bigl(
\mathsf{MetricDistanceWitness}(p\cdot source,\emp\cdot q,(p\cdot dist)\cdot q)
\to
(\mathsf{MetricDistanceDepth}(dist)=0\to\mathsf{False})
\to\\
\qquad
\exists sourceTail,\ source=\Eone(sourceTail)\land
\mathsf{UnaryHistory}(sourceTail)\land
0<\mathsf{MetricDistanceDepth}(source)
\bigr).
\end{array}
$$
\end{theorem}
\begin{proof}
For the empty central source implication, apply
\autoref{thm:metric-distance-witness-visible-context-iff} to peel the visible
prefix $p$ and suffix $q$ from the displayed witness. This recovers the central
witness $\mathsf{MetricDistanceWitness}(\emp,target,dist)$.

The positive-depth hypothesis rules out
$\mathsf{MetricDistanceDepth}(dist)=0$. By
\autoref{thm:metric-distance-depth-zero-iff-empty}, this is the same
nonempty-distance input required by the empty-source e-one cases theorem.
Apply
\autoref{thm:metric-distance-witness-empty-source-nonempty-distance-target-e1-cases}
to obtain $target=\Eone(targetTail)$ with unary $targetTail$. The one-headed
displayed endpoint has successor structural metric depth, so
$0<\mathsf{MetricDistanceDepth}(target)$.

For the empty central target implication, the same visible-context peeling
recovers $\mathsf{MetricDistanceWitness}(source,\emp,dist)$. The same
zero-depth equivalence converts positive depth of $dist$ into the
nonempty-distance input, and
\autoref{thm:metric-distance-witness-empty-target-nonempty-distance-source-e1-cases}
gives $source=\Eone(sourceTail)$ with unary $sourceTail$. The displayed
one-headed source again has positive structural metric depth.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_visible\_boundary\_positive\_depth\_endpoint\_readback}

\begin{theorem}[Metric distance witness zero-headed component impossibility]
\label{thm:metric-distance-witness-e0-component-absurd}
For histories $x,y,d:\mathsf{BHist}$, no concrete metric distance witness has
a zero-headed source, target, or distance component:
$$
\begin{array}{l}
\bigl(\mathsf{MetricDistanceWitness}(\Ezero(x),y,d)\to\mathsf{False}\bigr)
\land
\bigl(\mathsf{MetricDistanceWitness}(x,\Ezero(y),d)\to\mathsf{False}\bigr)
\land
\bigl(\mathsf{MetricDistanceWitness}(x,y,\Ezero(d))\to\mathsf{False}\bigr).
\end{array}
$$
\end{theorem}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_e0\_component\_absurd}
\begin{proof}
Unpack the concrete witness. Its three component fields are unary-history
evidence for the source, target, and distance histories. BEDC unary histories
are generated only from $\emp$ by $\Eone$, so each displayed $\Ezero$ component
contradicts unary no-zero-extension.
\end{proof}

\begin{theorem}[Metric empty distance visible endpoint impossibility]
\label{thm:metric-distance-witness-empty-distance-visible-endpoint-absurd}
For histories $x,y,d,tx,ty$, if a metric distance witness has a result
history-same as $\emp$, then neither endpoint can be history-same as a visible
one-headed history:
$$
\begin{array}{l}
\mathsf{MetricDistanceWitness}(x,y,d)\to
\hsame(d,\emp)\to\\
\qquad
\bigl(\hsame(x,\Eone(tx))\to\mathsf{False}\bigr)\land
\bigl(\hsame(y,\Eone(ty))\to\mathsf{False}\bigr).
\end{array}
$$
\end{theorem}
\begin{proof}
Transport the continuation result of the metric witness along the displayed
distance classifier, obtaining an empty-distance witness over the same
endpoints. Empty-distance exactness identifies both endpoints with $\emp$.
Composing either endpoint comparison with a visible one-headed classifier
contradicts the empty/e-one no-confusion theorem.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_empty\_distance\_visible\_endpoint\_absurd}

\begin{theorem}[Metric empty-distance composite endpoint collapse]
\label{thm:metric-distance-witness-empty-distance-composite-endpoint-collapse}
For histories $x,y,z,d_{xy},d_{yz}$, if
$\mathsf{MetricDistanceWitness}(x,y,d_{xy})$ and
$\mathsf{MetricDistanceWitness}(y,z,d_{yz})$ both have distance histories
classified as $\emp$, then all three endpoints classify together:
$$
d_{xy}\hsame\emp\to d_{yz}\hsame\emp\to
\HSame(x,y)\wedge\HSame(y,z)\wedge\HSame(x,z).
$$
\end{theorem}
\begin{proof}
Transport each distance witness to the empty-distance case. Empty-distance
exactness identifies the corresponding endpoint pair with $\emp$, and
transitivity through $\emp$ gives the three endpoint comparisons.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_empty\_distance\_composite\_endpoint\_collapse}

\begin{theorem}[Metric e1-pair empty-distance impossibility]
\label{thm:metric-distance-witness-e1-pair-empty-distance-absurd}
For histories $x,y:\mathsf{BHist}$,
$$
\mathsf{MetricDistanceWitness}(\Eone(x),\Eone(y),\emp)\to\mathsf{False}.
$$
\end{theorem}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_e1\_pair\_empty\_distance\_absurd}
\begin{proof}
Apply empty-distance exactness to the displayed metric witness. It forces the
left endpoint $\Eone(x)$ to be history-same as $\emp$, contradicting the
disjointness of visible one-headed histories from the empty history.
\end{proof}

\begin{theorem}[Metric e1 endpoints force e1 distance]
\label{thm:metric-distance-witness-e1-endpoints-distance-e1-shape}
For histories $x,y,d:\mathsf{BHist}$, a metric distance witness between two
one-headed endpoints forces the distance history itself to be one-headed with a
unary tail:
$$
\mathsf{MetricDistanceWitness}(\Eone(x),\Eone(y),d)
\Longrightarrow
\exists d_0:\mathsf{BHist},\;
d=\Eone(d_0)\land\mathsf{UnaryHistory}(d_0).
$$
\end{theorem}
\begin{proof}
Case-analyze the distance history. The empty case contradicts
\autoref{thm:metric-distance-witness-e1-pair-empty-distance-absurd}; the
zero-headed case contradicts the unary carrier field of the witness. The
one-headed case exposes the displayed tail, and unary inversion supplies its
carrier proof.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_e1\_endpoints\_distance\_e1\_shape}

\begin{theorem}[Metric right-boundary visible context distance source]
\label{thm:metric-distance-witness-right-boundary-visible-context-distance-source}
For histories $p,q,x,d:\mathsf{BHist}$, a visible-context distance witness with
empty central right endpoint identifies the central distance with the visible
source and gives the same structural depth:
$$
\mathsf{MetricDistanceWitness}(p\cdot x,\emp\cdot q,(p\cdot d)\cdot q)
\Longrightarrow
\hsame(d,x)\land
\mathsf{MetricDistanceDepth}(d)=\mathsf{MetricDistanceDepth}(x).
$$
\end{theorem}
\begin{proof}
Right-boundary visible-context exactness extracts the central classifier
$\hsame(d,x)$. Case analysis on that classifier identifies the two histories,
and the metric-depth equality follows by reflexivity.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_right\_boundary\_visible\_context\_distance\_source}

\begin{theorem}[Metric right-boundary visible context positive-depth source shape]
\label{thm:metric-distance-witness-right-boundary-visible-context-positive-depth-source-e1-shape}
For histories $p,q,x,d:\mathsf{BHist}$, a visible-context distance witness with
empty central right endpoint and positive central distance depth forces the
central source to be visibly one-headed with a unary tail:
$$
\mathsf{MetricDistanceWitness}(p\cdot x,\emp\cdot q,(p\cdot d)\cdot q)
\Longrightarrow
\bigl(\mathsf{MetricDistanceDepth}(d)=0\to\mathsf{False}\bigr)
\Longrightarrow
\exists x_0:\mathsf{BHist},\;
x=\Eone(x_0)\land\mathsf{UnaryHistory}(x_0).
$$
\end{theorem}
\begin{proof}
Apply right-boundary visible-context exactness. It exposes unary evidence for
the central source and the classifier $\hsame(d,x)$. If $x$ classified as
$\emp$, transitivity would classify $d$ as $\emp$, and the zero-depth classifier
would contradict the positive-depth hypothesis. Thus $x$ is a nonempty unary
history, so unary nonempty e-one tail readback supplies the required $x_0$.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_right\_boundary\_visible\_context\_positive\_depth\_source\_e1\_shape}

\begin{theorem}[Metric left-boundary visible-context distance target]
\label{thm:metric-distance-witness-left-boundary-visible-context-distance-target}
For histories $p,q,y,d$, a visible-context distance witness whose central left
endpoint is empty identifies the central distance with the target and aligns
their structural depths:
$$
\mathsf{MetricDistanceWitness}(p\cdot\emp,y\cdot q,(p\cdot d)\cdot q)
\Longrightarrow
\hsame(d,y)\land
\mathsf{MetricDistanceDepth}(d)=\mathsf{MetricDistanceDepth}(y).
$$
\end{theorem}
\begin{proof}
Apply left-boundary visible-context exactness to peel the visible prefix and
suffix. The central empty-left witness gives $\hsame(d,y)$. Case analysis on
that classifier reduces the displayed depth equation to reflexivity.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_left\_boundary\_visible\_context\_distance\_target}
