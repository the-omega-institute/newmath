\begin{theorem}[Metric distance witness semantic name certificate]
\label{thm:metric-distance-witness-semantic-namecert}
For unary endpoint histories $x,y$, the concrete distance-witness predicate
$d\mapsto\mathsf{MetricDistanceWitness}(x,y,d)$ carries a semantic naming
certificate whose source, pattern, and ledger predicates are the same concrete
witness predicate and whose classifier is $\hsame$.
\end{theorem}
\begin{proof}
The carrier witness is the concrete distance history $x\cdot y$. Unary append
closure supplies its unary carrier witness, and the continuation field is the
reflexive ledger $\ContR(x,y,x\cdot y)$. The classifier fields are the
reflexive, symmetric, and transitive laws for $\hsame$. Carrier transport is
history-sameness transport of the concrete distance witness.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricDistanceWitness\_semanticNameCert}

\begin{definition}[MetricSpace ledger policy]
\label{def:metric-ledger-policy}
Record carrier witnesses and the real ledger of each distance value.
\end{definition}

\begin{definition}[\(\MetricSpaceUp\) certificate obligations]
\label{def:metric-certificate-obligations}
The carrier, source, pattern, classifier, stability, and ledger data listed above are the obligations required for the interface name \(\MetricSpaceUp\).
\end{definition}
\leandef{BEDC.Derived.MetricUp.MetricCertificateObligations}

\begin{definition}[Metric ball budget predicate]
\label{def:metric-ball-budget-predicate}
For a certified $\MetricSpaceUp$ setup on a carrier $X$ with distance
$d:X\times X\to\RealUp$, a ball-budget structure supplies an ordered-real
comparison $\le_{\mathsf R}$ on $\RealUp$, an addition operation $+$ on
$\RealUp$, a transitivity field for $\le_{\mathsf R}$, and a two-sided
addition-monotonicity field saying that $a\le_{\mathsf R} a'$ and
$b\le_{\mathsf R} b'$ entail $a+b\le_{\mathsf R} a'+b'$. Relative to this
chosen budget structure, the closed ball-budget predicate is
$$
\mathsf{MetricBallBudget}_d(x,y;r) :\Longleftrightarrow d(x,y)\le_{\mathsf R} r.
$$
\end{definition}
\leandef{BEDC.Derived.MetricUp.MetricBallBudget}

\begin{theorem}[Metric ball budget composition]
\label{thm:metric-ball-budget-composition}
Let $X$ be a carrier equipped with the $\MetricSpaceUp$ certificate obligations
of \autoref{def:metric-certificate-obligations}, and let
$d:X\times X\to\RealUp$ be its certified distance. Assume also the ball-budget
ordered-real fields of \autoref{def:metric-ball-budget-predicate}: the
comparison $\le_{\mathsf R}$, addition $+$, budget-transitivity, and two-sided
budget-addition-monotonicity. For $x,y,z:X$ and budgets $r,s:\RealUp$,
$$
\mathsf{MetricBallBudget}_d(x,y;r)\Longrightarrow
\mathsf{MetricBallBudget}_d(y,z;s)\Longrightarrow
\mathsf{MetricBallBudget}_d(x,z;r+s).
$$
Equivalently,
$$
d(x,y)\le_{\mathsf R} r\Longrightarrow d(y,z)\le_{\mathsf R} s\Longrightarrow d(x,z)\le_{\mathsf R} r+s.
$$
\end{theorem}
\begin{proof}
Unfold \autoref{def:metric-ball-budget-predicate}. The two premises are the
budget comparisons $d(x,y)\le_{\mathsf R} r$ and
$d(y,z)\le_{\mathsf R} s$. The triangle-inequality field of
\autoref{def:metric-stability-certificate} gives
$$
d(x,z)\le_{\mathsf R} d(x,y)+d(y,z).
$$

Apply the supplied two-sided budget-addition-monotonicity field to the two
budget premises. This gives
$$
d(x,y)+d(y,z)\le_{\mathsf R} r+s.
$$

Apply the supplied budget-transitivity field to the triangle inequality and
the displayed budget bound. This yields $d(x,z)\le_{\mathsf R} r+s$.
Refolding \autoref{def:metric-ball-budget-predicate} gives the closed
ball-budget conclusion.
\end{proof}
\leanchecked{BEDC.Derived.MetricUp.MetricBallBudget\_composition}

\begin{theorem}[Metric distance witness standard bridge]
\label{thm:metric-distance-witness-standard-bridge}
For the public $\MetricUp$ distance-witness surface over unary
$\mathsf{BHist}$ endpoints $x,y$, there is a conservative standard
interpretation whose BEDC carrier is
$$
\{\,d\mid \mathsf{MetricDistanceWitness}(x,y,d)\,\},
$$
whose classifier is $\hsame$, whose forward row reads such a witness as the
finite append-distance presentation between the two unary endpoints, and whose
reverse row reconstructs the BEDC witness from that finite append presentation.
The bridge is only for this generated distance-witness surface; it does not
assert arbitrary host real metrics.
\end{theorem}
\begin{proof}
The carrier and classifier come from
\autoref{thm:metric-distance-witness-semantic-namecert}: the public BEDC
objects are exactly the $d$ satisfying
$\mathsf{MetricDistanceWitness}(x,y,d)$, and the classifier is $\hsame$.

For the forward row, apply
\autoref{thm:metric-distance-witness-visible-context-iff} to read the visible
unary context as the finite append-distance presentation between the endpoints.
\autoref{thm:metric-distance-witness-visible-context-determinacy} ensures that
this presentation is determined by the visible context, while
\autoref{thm:metric-distance-witness-symmetric-classifier} transports the same
presentation across the symmetric endpoint order.

For the reverse row, read the finite append-distance presentation back through
\autoref{thm:metric-distance-witness-visible-context-iff}. The resulting BEDC
witness is classified by the same $\hsame$ row, and
\autoref{thm:metric-ball-budget-composition} supplies the budget-compatible
composition law needed by the public distance-witness surface.

Thus the two rows form a conservative paper-side standard bridge for the
generated $\mathsf{MetricDistanceWitness}$ surface only. No completeness,
compactness, topology, or arbitrary host-real metric structure is introduced.
\end{proof}

\paragraph{Primitive boundary.}
\(\MetricSpaceUp\) is not a BEDC primitive. It is admitted only when those obligations are supplied.
\input{parts/concrete_instances/32_metric_empty_boundary_visible_context_distance_empty}

\begin{theorem}[Metric distance witness standard boundary bridge]
\label{thm:metric-distance-witness-standard-boundary-bridge}
For the concrete $\mathsf{MetricDistanceWitness}$ interface on unary
$\mathsf{BHist}$ endpoint witnesses, the paper-side bridge is the conservative
boundary interpretation assembled from the semantic NameCert, visible-context
exactness, the left and empty boundary readback rows, and the zero-depth
identity-path row. It identifies exactly the BEDC boundary-distance witness rows
with the corresponding standard boundary-metric reading, and back again, for the
concrete $\mathsf{BHist}$ witness surface.
\end{theorem}
\begin{proof}
The semantic carrier and classifier are the concrete
$\mathsf{MetricDistanceWitness}$ predicate and $\hsame$ classifier supplied by
\autoref{thm:metric-distance-witness-semantic-namecert}. Thus the bridge has no
separate host carrier: its public objects are exactly the displayed
$\mathsf{BHist}$ distance witnesses.

The boundary readback is supplied in the two directions by
\autoref{thm:metric-distance-witness-left-boundary-visible-context-iff} and
\autoref{thm:metric-distance-witness-empty-boundary-visible-context-iff}. These
rows say that the left and empty BEDC boundary surfaces read as the same
standard boundary-distance data, and that the standard reading returns to the
same BEDC rows.

Finally,
\autoref{thm:metric-distance-witness-prefix-independent-symmetric-zero-depth-reverse-distance-continuation-result}
provides the zero-depth symmetric identity-path row used by the standard
boundary metric reading. Together these rows give a conservative two-way
boundary interpretation for the concrete witness surface only. No arbitrary
host metric space, completeness, compactness, or topology-induced-by-metric
bridge is being claimed.
\end{proof}

\begin{closurestatus}{\MetricUp}
  \theoryclosure{\matureClosure}
  \scopeclosed{The closed scope is the $\mathsf{MetricDistanceWitness}$ semantic certificate, ball-budget composition, the standard distance-witness bridge, the standard boundary bridge, and the named cross-chapter consumers in $\TotallyBoundedUp$, $\CompleteMetricUp$, $\CompactMetricUp$, $\ContinuousMapUp$, and the compact uniform-continuity capstone.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.MetricUp.MetricDistanceWitness\_semanticNameCert}
  \bridgestatus{paperBridge}
  \notclaimed{This chapter does not close arbitrary host metric spaces, general completeness, compactness, topology induced by a metric, or Lean-checked bridge status.}
  \upgradepath{The remaining paper-to-Lean step is a Lean-checked bridge target together with any non-singleton standard-metric interfaces that are introduced later, without making verification status a function of paper closure.}
\end{closurestatus}

\begin{remark}[Zero-axiom encoding note]
\label{rem:metric-zero-axiom-note}
Replaces decidable equality with apartness and explicit real-modulus inequalities.
\end{remark}
