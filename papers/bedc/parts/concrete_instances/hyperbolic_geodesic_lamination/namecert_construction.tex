\chapter{A Concrete Naming Certificate for $\HyperbolicGeodesicLaminationUp$}
\label{ch:concrete-instances-hyperbolic-geodesic-lamination-namecert}
\origin{ai}

The $\HyperbolicGeodesicLaminationUp$ packet realizes the boundary-paired noncrossing leaf surface of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert data. It sits after the endpoint route of \autoref{ch:concrete-instances-hyperbolic-geodesic-boundary-namecert}, the Busemann boundary row of \autoref{ch:concrete-instances-hyperbolic-busemann-boundary-namecert}, the visual-boundary read of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, the cross-ratio comparison of \autoref{ch:concrete-instances-hyperbolic-cross-ratio-namecert}, and the observer-shadow metric of \autoref{ch:concrete-instances-hyperbolic-observer-shadow-metric-namecert}. The packet names a finite lamination handoff, not a completed geodesic space, quotient boundary, physical foliation, global hyperbolic model, or unlogged field-faithfulness assertion.

\begin{definition}[Hyperbolic geodesic lamination carrier]
\label{def:hyperbolic-geodesic-lamination-carrier}
A $\HyperbolicGeodesicLaminationUp$ carrier is a finite $\BHist$ packet
$$
G=(E,L,N,M,B,V,H,C,P,F,A)
$$
with the following displayed rows:
\begin{enumerate}
\item $E$ is the endpoint-pair row, recording the two boundary-facing endpoints of each displayed leaf.
\item $L$ is the geodesic-leaf row, attaching each endpoint pair to a finite geodesic-boundary route.
\item $N$ is the noncrossing row, recording the finite compatibility ledger among the displayed leaves.
\item $M$ is the hyperbolic-metric read row used when leaf separation or ordering is inspected.
\item $B$ is the Busemann row, attaching the leaves to displayed boundary distance-difference data.
\item $V$ is the visual-shadow row, recording the observer-facing visual shadow through which the lamination is read.
\item $H$ records componentwise $\hsame$ transport for endpoint-pair, leaf, noncrossing, metric, Busemann, visual-shadow, replay, provenance, refusal, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a boundary-lamination request through $E,L,N,M,B,V$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicGeodesicLaminationUp$, $\HyperbolicGeodesicBoundaryUp$, $\HyperbolicBusemannBoundaryUp$, $\HyperbolicVisualBoundaryUp$, $\HyperbolicCrossRatioUp$, $\HyperbolicObserverShadowMetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $F$ is the refusal row excluding host global laminations, quotient boundary identifications, physical foliations, hidden crossing tests, and unlogged TasteGate witnesses.
\item $A$ is the local $\NameCert_{\HyperbolicGeodesicLaminationUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $E,L,N,M,B,V,H,C,P,F,A$. It has no coordinate for completed geodesic spaces, ambient boundary quotients, host noncrossing predicates, physical laminations, or implicit $\Nontrivial$ and $\FieldFaithful$ witnesses.
\end{definition}

\begin{theorem}[Hyperbolic geodesic lamination NameCert obligations]
\label{thm:hyperbolic-geodesic-lamination-namecert-obligations}
For every accepted $\HyperbolicGeodesicLaminationUp$ carrier
$$
G=(E,L,N,M,B,V,H,C,P,F,A),
$$
the local $\NameCert_{\HyperbolicGeodesicLaminationUp}$ surface consists exactly of endpoint-pair admission, geodesic-leaf admission, noncrossing ledger compatibility, hyperbolic-metric readback, Busemann attachment, visual-shadow exposure, componentwise transport, displayed replay, provenance, explicit refusal, and local naming through $E,L,N,M,B,V,H,C,P,F,A$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-geodesic-lamination-carrier}. The endpoint, leaf, noncrossing, metric, Busemann, visual-shadow, transport, replay, provenance, refusal, and naming rows are the whole lamination packet.

The endpoint-pair and geodesic-leaf rows $E,L$ are read first. They attach every displayed leaf to finite boundary-facing data rather than to an ambient set of complete geodesics.

The noncrossing and metric rows $N,M$ then record the compatibility tests available to consumers. These tests are displayed rows, so the packet does not import a host noncrossing predicate.

The Busemann and visual-shadow rows $B,V$ bind the leaf family to boundary and observer-facing routes. Finally $H,C,P,F,A$ transport, replay, source, refuse exits, and name the same finite carrier, leaving completed global laminations, quotient boundaries, physical foliations, and implicit TasteGate witnesses outside the certificate.
\end{proof}

\begin{theorem}[Hyperbolic geodesic lamination boundary route]
\label{thm:hyperbolic-geodesic-lamination-boundary-route}
Every boundary-lamination consumer of an accepted $\HyperbolicGeodesicLaminationUp$ carrier must pass through endpoint-pair, geodesic-leaf, noncrossing, hyperbolic-metric, Busemann, visual-shadow, transport, provenance, refusal, and local $\NameCert$ rows before it can export a lamination handoff.
\end{theorem}
\begin{proof}
Let
$$
G=(E,L,N,M,B,V,H,C,P,F,A)
$$
be an accepted carrier. A boundary-facing read starts with $E,L$, so its endpoints and leaves are displayed before the consumer sees a lamination token.

The noncrossing row $N$ and metric row $M$ supply the finite compatibility and metric-read tests. The Busemann and visual-shadow rows $B,V$ then attach those tests to the boundary and observer-facing surfaces cited by provenance.

Transport and replay through $H,C$ preserve the displayed route, while $P,F,A$ source, refuse exits, and name it. Hence any boundary-lamination handoff factors through the listed rows and cannot be claimed by a consumer that omits endpoint pairs, noncrossing evidence, hyperbolic metric reads, visual-shadow data, transport, provenance, refusal, or local naming.
\end{proof}

\falsifiablePrediction{Any boundary lamination consumer omitting endpoint-pair, noncrossing, hyperbolic-metric, visual-shadow, transport, provenance, or local $\NameCert$ rows cannot claim the $\HyperbolicGeodesicLaminationUp$ handoff.}

\independenceWitness{hyperbolic-geodesic-boundary, hyperbolic-busemann-boundary, hyperbolic-visual-boundary, hyperbolic-cross-ratio, hyperbolic-observer-shadow-metric: the geodesic-boundary packet names endpoint routes without the noncrossing ledger, the Busemann packet names distance-difference data without leaf compatibility, the visual-boundary packet names boundary readback without lamination rows, the cross-ratio packet compares boundary quadruples without owning geodesic leaves, and the observer-shadow metric packet reads visible shadows without endpoint-pair noncrossing. None is bijective to $\HyperbolicGeodesicLaminationUp$.}

The future \texttt{lean4/BEDC/Derived/HyperbolicGeodesicLaminationUp/TasteGate.lean} file must include explicit $\Nontrivial\ \HyperbolicGeodesicLaminationUp$ and $\FieldFaithful\ \HyperbolicGeodesicLaminationUp$ instances. Those instances preserve $E,L,N,M,B,V,H,C,P,F,A$ as distinct fields before a checked carrier-alignment theorem can serve as a TasteGate witness.

\closureat{HyperbolicGeodesicLaminationUp}{seedStr}

\begin{closurestatus}{\HyperbolicGeodesicLaminationUp}
  \constructivestory{A $\HyperbolicGeodesicLaminationUp$ packet is generated from endpoint-pair rows, geodesic-leaf rows, a noncrossing ledger, hyperbolic-metric reads, Busemann attachment, visual-shadow exposure, componentwise hsame transport, displayed Cont replay, Pkg provenance, explicit refusal, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-geodesic-lamination-carrier}, \autoref{thm:hyperbolic-geodesic-lamination-namecert-obligations}, and \autoref{thm:hyperbolic-geodesic-lamination-boundary-route} seed the finite boundary-paired noncrossing lamination route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed geodesic spaces, quotient boundary equality, physical foliations, host global laminations, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked endpoint-pair admission, geodesic-leaf admission, noncrossing compatibility, metric readback, Busemann attachment, visual-shadow exposure, TasteGate nontriviality, field faithfulness, refusal, and local naming for BEDC.Derived.HyperbolicGeodesicLaminationUp.}
\end{closurestatus}
