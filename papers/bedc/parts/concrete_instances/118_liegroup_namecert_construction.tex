\chapter{A Concrete Naming Certificate for $\LieGroupUp$}
\label{ch:concrete-instances-liegroup-namecert}

The Lie-group interface $\LieGroupUp$ packages a group carrier whose underlying set is a manifold and whose multiplication and inverse are constructively smooth. It depends on $\NameCert_{\GroupUp}$ (\autoref{ch:concrete-instances-group-namecert}) and $\NameCert_{\ManifoldUp}$ (\autoref{ch:concrete-instances-manifold-namecert}), and is the substrate for representation, exponential, and root-system certificates.

\input{parts/concrete_instances/liegroup/carrier_and_certificate.tex}
\input{parts/concrete_instances/liegroup/singleton_standard_bridge.tex}
\input{parts/concrete_instances/118_liegroup_source_ledger_and_downstream.tex}

\closureat{LieGroupUp}{bridgedStr}[BEDC.Derived.LieGroupUp.LieGroupSingleton\_semantic\_name\_certificate]

\begin{closurestatus}{\LieGroupUp}
  \constructivestory{The concrete Lie group here is the singleton history carrier $L_{\emp}$: a history is accepted exactly when it is $\hsame$ to $\emp$, and the same predicate serves simultaneously as the $\GroupUp$ carrier, the $\ManifoldUp$ point carrier, and the $\LieGroupUp$ carrier. Two accepted histories classify together by the restricted row $R_{\emp}$, so multiplication by $\mathsf{append}$, inverse returning $\emp$, the unit $\emp$, and the singleton chart all stay inside one shared classifier rather than being transported across duplicate sources. The source covers the single $\mathsf{BHist}$ carrier and its group and chart rows, the pattern records singleton group laws, constant chart coverage, smooth multiplication and inverse readback, and conjugation source rows, while the ledger keeps $\emp$, $\hsame$, $\Cont$, $\mathsf{append}$, the singleton chart value, and the constant transition visible. Nonemptiness is witnessed by $\emp$, reflexivity, symmetry, and transitivity are those of $\hsame$ restricted to $L_{\emp}$, and carrier closure follows because every public operation endpoint is again classified with $\emp$. A history $h$ lies in the signature gap of a $\LieGroupUp$ token $p$ when the source accepts $h$ as a singleton group/manifold row, the signature relation pairs it with an answer endpoint and evidence ledger, and token introduction names that answer as $p$; unfolded, $h$ and the answer are both the empty singleton endpoint with the displayed operation, chart, and conjugation rows. The paper bridge reads this package as the standard one-point Lie group and reads it back through the same singleton carrier, group laws, and chart rows. The chapter registers this singleton smooth-group construction under the name $\LieGroupUp$; no non-singleton Lie group, exponential theory, representation theory, or root-system theory is part of the primitive package.}
  \theoryclosure{\matureClosure}
  \scopeclosed{The public singleton boundary is the shared $\mathsf{BHist}$ carrier $L_{\emp}$, restricted $\hsame$ classifier, singleton $\GroupUp$ laws, singleton $\ManifoldUp$ chart rows, classifier transport, smooth operation readback, exact ledger, downstream export, and conjugation source rows.}
  \formalstatus{\auditCleanV}
  \leantarget{BEDC.Derived.LieGroupUp.LieGroupSingleton\_semantic\_name\_certificate}
  \bridgestatus{paperBridge}
  \notclaimed{This chapter does not close non-singleton Lie groups, exponential theory, representation theory, root-system theory, or bridgeChecked verification.}
  \upgradepath{The remaining advancement is bridgeChecked verification for the displayed paper bridge, not additional theory-axis maturity for the singleton Lie-group package.}
\end{closurestatus}
