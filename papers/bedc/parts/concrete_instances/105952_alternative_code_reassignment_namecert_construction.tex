\chapter{A Concrete Naming Certificate for $\AlternativeCodeReassignmentUp$}
\label{ch:concrete-instances-alternative-code-reassignment-namecert}
\origin{ai}

The $\AlternativeCodeReassignmentUp$ packet realizes the comparative genetic-code reassignment spectrum of \autoref{def:philosophy-alternative-code-reassignment-spectrum} as finite NameCert data. It packages the standard-code classifier, curated alternative table row, reassignment set, module ledger, sparse support-count ledger, transport, replay, provenance, and local naming rows; it does not treat the observed comparative spectrum as a kernel fact about biological mechanism or as an oracle for all translation systems.

The standard-code classifier row and codon carrier are read through \autoref{ch:concrete-instances-dna-window-six-boundary-tile-namecert}, where the finite boundary-tile classifier rows, transport, replay, provenance, and NameCert rows are already displayed. The sparse support-count ledger and compression boundary are consumed through \autoref{ch:concrete-instances-reassignment-support-compression-namecert}, so the reassignment packet depends only on finite support-count ledgers, transport, replay, provenance, and local NameCert rows. Its concrete sibling lattice also passes through the four-phase alphabet bridge of \autoref{ch:concrete-instances-dna-four-phase-alphabet-bridge-namecert}, the six-bit codon window of \autoref{ch:concrete-instances-codon-six-bit-window-namecert}, and the support-compression surface of \autoref{ch:concrete-instances-reassignment-support-compression-namecert}.

\begin{definition}[Alternative-code reassignment carrier]
\label{def:alternative-code-reassignment-carrier}
An $\AlternativeCodeReassignmentUp$ carrier is a finite $\BHist$ packet
$$
R=(S,T,D,M,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the standard-code classifier row, naming the displayed $Q_{6}$ codon carrier and the standard assignment classifier used as the comparison base.
\item $T$ is the alternative-table row, recording one curated non-standard table on the same codon carrier together with its displayed assignment labels.
\item $D$ is the reassignment-set row, listing exactly those codons whose row in $T$ differs from the standard classifier row $S$.
\item $M$ is the module ledger over the Stop/Trp, Ile/Met, Phe/Leu, and AGA/AGG local modules of \autoref{def:philosophy-four-reassignment-modules}.
\item $Q$ is the support-count ledger, recording sparse codon support and multiplicity information as displayed in \autoref{thm:philosophy-sparse-reassignment-codon-support}.
\item $H$ records componentwise $\hsame$ transport for the standard classifier, alternative table, reassignment set, module ledger, support-count ledger, provenance, replay, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the standard classifier and alternative-table comparison to the reassignment set, then to the module ledger and support-count ledger.
\item $P$ is the $\Pkg$ provenance over $\AlternativeCodeReassignmentUp$, the DNA six-bit boundary tile source rows, $\ReassignmentSupportCompressionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\AlternativeCodeReassignmentUp}$ row.
\end{enumerate}
The carrier contains no biochemical mechanism coordinate, no exhaustive table-family oracle, no unbounded codon-table search, no quotient identifying biological labels with BEDC constructors, and no claim that reassignment frequency is a cause.
\end{definition}

\begin{theorem}[Alternative-code reassignment NameCert obligations]
\label{thm:alternative-code-reassignment-namecert-obligations}
For an accepted $\AlternativeCodeReassignmentUp$ carrier $R=(S,T,D,M,Q,H,C,P,N)$, the local $\NameCert_{\AlternativeCodeReassignmentUp}$ surface consists exactly of carrier admission for the displayed comparison packet, standard-code classifier exposure through $S$, alternative-table exposure through $T$, reassignment-set exactness through $D$, module-ledger grouping through $M$, sparse support-count exposure through $Q$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:alternative-code-reassignment-carrier}. The comparison starts from the standard classifier row $S$ and the alternative-table row $T$, both over the same displayed codon carrier.

The reassignment row $D$ is then forced by finite row comparison. The module ledger $M$ groups those displayed changes by the four local modules, and the support-count row $Q$ records the sparse support and multiplicity data available to downstream consumers.

The structural rows $H,C,P,N$ transport, replay, source, and name exactly this finite packet. No other coordinate exists, so the listed rows exhaust the local NameCert obligation surface.
\end{proof}

\begin{theorem}[Alternative-code reassignment spectrum nonescape]
\label{thm:alternative-code-reassignment-spectrum-nonescape}
Every consumer of an accepted $\AlternativeCodeReassignmentUp$ packet factors through the finite route
$$
S,T
\longmapsto
D
\longmapsto
M,Q
\longmapsto
H,C,P,N .
$$
It receives a displayed standard-code comparison, alternative-table row, reassignment set, module ledger, sparse support-count ledger, transport, replay, provenance, and local NameCert surface. It does not receive biological causality, exhaustive coverage of possible genetic codes, a mechanism for translation dynamics, an unbounded codon-table oracle, or a quotient from external labels into BEDC kernel facts.
\end{theorem}
\begin{proof}
Use the obligation surface of \autoref{thm:alternative-code-reassignment-namecert-obligations}. The only comparison inputs are $S$ and $T$, and the only reassignment output is the finite row $D$ obtained from them.

The module and support-count reads pass through $M,Q$. These rows preserve the comparative spectrum as displayed finite data and do not add a mechanistic or exhaustive biological premise.

Finally $H,C,P,N$ preserve the same packet under transport, replay, provenance, and local naming. Since the excluded claims have no carrier coordinate, they cannot be exported by an accepted $\AlternativeCodeReassignmentUp$ read.
\end{proof}

\falsifiablePrediction{An accepted $\AlternativeCodeReassignmentUp$ read is refuted if the standard-code comparison, curated alternative table, reassignment set, module ledger, sparse support-count ledger, and local NameCert rows can no longer be routed through dna-four-phase-alphabet-bridge, codon-six-bit-window, reassignment-support-compression, and dna-window-six-boundary-tile sibling surfaces.}

\independenceWitness{dna-four-phase-alphabet-bridge, codon-six-bit-window, reassignment-support-compression, dna-window-six-boundary-tile: none is bijective with $\AlternativeCodeReassignmentUp$ because the siblings respectively expose alphabet phase rows, codon-window coordinates, support-compression counts, or boundary-tile classifier rows, while the reassignment packet jointly binds standard and alternative table comparison, reassignment exactness, module grouping, sparse support accounting, transport, replay, provenance, and local naming.}

\closureat{AlternativeCodeReassignmentUp}{seedStr}

\begin{closurestatus}{\AlternativeCodeReassignmentUp}
  \constructivestory{An $\AlternativeCodeReassignmentUp$ packet is generated from a standard-code classifier, alternative table row, reassignment set, module ledger, sparse support-count ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:alternative-code-reassignment-carrier}, \autoref{thm:alternative-code-reassignment-namecert-obligations}, and \autoref{thm:alternative-code-reassignment-spectrum-nonescape} seed the finite comparative-code reassignment spectrum surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim biological causality, exhaustive genetic-code coverage, translation mechanism, unbounded table search, Lean verification, or a standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, standard-code comparison, alternative-table exposure, reassignment-set exactness, module-ledger grouping, sparse support-count exposure, transport preservation, and local naming for BEDC.Derived.AlternativeCodeReassignmentUp.}
\end{closurestatus}
