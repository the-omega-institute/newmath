\chapter{A Concrete Naming Certificate for $\RegularCauchyTailFiberUp$}
\label{ch:concrete-instances-regular-cauchy-tail-fiber-namecert}
\origin{ai}

The $\RegularCauchyTailFiberUp$ packet records the common finite tail-fiber classifier used when two regular Cauchy source tails must feed one $\RealUp$ seal boundary. It sits between the shared-tail threshold packet of \autoref{ch:concrete-instances-regular-cauchy-tail-meet-namecert}, the tail-agreement seal of \autoref{ch:concrete-instances-realtailagreementseal-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, and the regular rational carrier of \autoref{ch:concrete-instances-regseqrat-namecert}. The packet is a Real-and-Completion diagonal route component; it is not a quotient of streams, a host real equality, or an ambient completeness principle.

\falsifiablePrediction{If the shared tail fiber row is removed, the packet cannot factor both $\RealTailAgreementSealUp$ and $\RegularCauchyTailMeetUp$ consumer reads through one finite $\RealUp$ seal route.}

\independenceWitness{regular-cauchy-tail-meet, realtailagreementseal, regularcauchytailfusion, modulus-indexed-tail-cover: these siblings expose threshold, agreement, fusion, or cover rows, while $\RegularCauchyTailFiberUp$ exposes the common fiber classifier tying the two source tails to one $\RealUp$ seal boundary.}

\begin{definition}[Regular Cauchy tail fiber carrier]
\label{def:regular-cauchy-tail-fiber-carrier}
A $\RegularCauchyTailFiberUp$ carrier is a finite $\BHist$ packet
$$
F=(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R_0$ and $R_1$ are the two $\RegSeqRatUp$ source rows whose late regular-rational tails are being compared;
\item $W_0$ and $W_1$ are the finite $\StreamNameUp$ windows through which the two source rows are observed;
\item $D_0$ and $D_1$ are $\DyadicRatCoreUp$ tail-radius ledgers for the two observed tails;
\item $T$ is the shared tail fiber row classifying the two source tails as one finite tail boundary;
\item $A$ is the $\RealUp$ tail-agreement seal row reached through the shared fiber;
\item $H$ records componentwise $\hsame$ transport for the source rows, windows, dyadic ledgers, shared fiber, seal, replay route, provenance, and local name;
\item $C$ records displayed $\Cont$ routes for reading both source tails, both windows, both dyadic ledgers, the shared fiber row, and the seal row;
\item $P$ is the $\Pkg$ provenance over $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealTailAgreementSealUp$, $\RegularCauchyTailMeetUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RegularCauchyTailFiberUp}$ row.
\end{enumerate}
The carrier accepts only packets whose two regular Cauchy source tails meet in the displayed shared fiber row before any $\RealUp$ seal is read. It contains no quotient stream, no host real equality, no selected limit, and no completion theorem beyond the finite seal boundary.
\end{definition}

\begin{theorem}[Regular Cauchy tail fiber NameCert obligations]
\label{thm:regular-cauchy-tail-fiber-namecert-obligations}
For every accepted $\RegularCauchyTailFiberUp$ carrier
$$
F=(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyTailFiberUp}$ surface consists exactly of source-row exposure for $R_0,R_1$, finite-window exposure for $W_0,W_1$, dyadic tail-radius exposure for $D_0,D_1$, shared tail-fiber exposure for $T$, $\RealUp$ tail-agreement seal exposure for $A$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-tail-fiber-carrier}. The first six rows expose exactly the two source tails, their finite observation windows, and their dyadic tail-radius ledgers.

Next read the common fiber row $T$. This is the only row that identifies the two late-tail observations as one finite tail boundary before the seal row is available.

Finally read $A,H,C,P,N$. The seal row is reached only after the shared fiber has been displayed, and the transport, continuation, provenance, and naming rows replay that same finite packet. Thus the local NameCert obligations are exhausted by the listed carrier coordinates.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber window projection]
\label{thm:regular-cauchy-tail-fiber-window-projection}
Every $\StreamNameUp$ or $\RegSeqRatUp$ consumer read of an accepted $\RegularCauchyTailFiberUp$ carrier factors through the finite projection
$$
(R_0,R_1,W_0,W_1,D_0,D_1,T).
$$
The read cannot reach the $\RealUp$ tail-agreement seal row $A$ without first passing through the shared tail fiber row $T$ and the displayed continuation route $C$.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyTailFiberUp.RegularCauchyTailFiberWindowProjection\_changes\_packet}
\begin{proof}
Start with a source-facing consumer. Projection exposes $R_0,R_1$ as the only regular-rational source rows and $W_0,W_1$ as the only finite stream-name windows attached to them.

The dyadic-radius ledgers $D_0,D_1$ are then read at those same windows. A consumer has no separate radius or tolerance row outside the displayed packet.

The seal-facing step is forced through $T$ and $C$. Since $A$ is attached to the shared fiber route, a consumer cannot bypass the common fiber and read a detached $\RealUp$ seal.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber shared seal factorization]
\label{thm:regular-cauchy-tail-fiber-shared-seal-factorization}
For an accepted $\RegularCauchyTailFiberUp$ carrier $F=(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N)$, the $\RealTailAgreementSealUp$ consumer route and the $\RegularCauchyTailMeetUp$ consumer route share the same finite factorization through $T$ before reaching the $\RealUp$ seal row $A$.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyTailFiberUp.RegularCauchyTailFiber\_shared\_fiber\_route\_changes\_packet}
\begin{proof}
Read the tail-meet side first. The meet route consumes $R_0,R_1,W_0,W_1,D_0,D_1$ and then exposes the shared threshold boundary as the fiber row $T$.

Read the tail-agreement side next. Its seal route uses the same $T$ as the finite agreement boundary before the $\RealUp$ seal row $A$ is available.

The administrative rows $H,C,P,N$ transport, replay, package, and name this one route. Since no second fiber coordinate is present, both consumer routes factor through the same shared row.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber non-escape]
\label{thm:regular-cauchy-tail-fiber-nonescape}
Every downstream read of an accepted $\RegularCauchyTailFiberUp$ packet remains inside
$$
R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N.
$$
It exports no quotient stream equality, no host real equality, no selected limit, no countable-choice selector, and no ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Project the accepted carrier. The only source, window, radius, common-fiber, and seal rows are the coordinates listed in \autoref{def:regular-cauchy-tail-fiber-carrier}.

Use the window projection and shared seal factorization. Any consumer first reads the two finite windows and dyadic-radius ledgers, then reaches the common fiber $T$, and only then reaches the seal row $A$.

Inspect the excluded data. No coordinate stores quotient stream equality, host real equality, a selected limit, choice, or ambient completeness. The packet therefore cannot export such data to a downstream Real-and-Completion route.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber follow-up TasteGate contract]
\label{thm:regular-cauchy-tail-fiber-tastegate-contract}
The Lean TasteGate follow-up for $\RegularCauchyTailFiberUp$ must provide $\mathsf{Nontrivial}\ \RegularCauchyTailFiberUp$ and $\mathsf{FieldFaithful}\ \RegularCauchyTailFiberUp$ instances whose witnesses include an explicit $\mathsf{witness\_pair}$ and constructor-field injection proofs for the two source rows, two windows, two dyadic-radius ledgers, shared fiber, seal row, transport, continuation, provenance, and local naming fields.
\end{theorem}
\begin{proof}
The carrier has twelve displayed fields, and each field is observable by a NameCert obligation or by the non-escape boundary. A nontrivial instance must therefore exhibit two distinguishable accepted packets through a concrete witness pair rather than by host equality.

Field faithfulness must then inspect constructor fields. If two accepted packets agree under the public classifier, the proof has to inject through the source rows, windows, dyadic ledgers, fiber row, seal row, and administrative rows separately.

These are precisely the fields used by the window projection, shared seal factorization, and non-escape theorems. The TasteGate follow-up is therefore a fieldwise carrier proof for this packet, not a quotient-stream or host-real argument.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyTailFiberUp.RegularCauchyTailFiberTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Regular Cauchy tail fiber classifier stability]
\label{thm:regular-cauchy-tail-fiber-classifier-stability}
For an accepted $\RegularCauchyTailFiberUp$ carrier
$$
F=(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N),
$$
componentwise $\hsame$ transport through the row $H$ preserves the common fiber classifier on $R_0,R_1,W_0,W_1,D_0,D_1,T,A$. In particular, a transported read still classifies the two regular Cauchy source tails through the same finite row $T$ before reaching the $\RealUp$ seal row $A$, and the transport introduces neither quotient-stream equality nor host-real equality.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-tail-fiber-carrier}. The row $H$ is displayed as componentwise $\hsame$ transport for the two source rows, the two windows, the two dyadic ledgers, the shared fiber row, and the seal row.

Apply the window projection of \autoref{thm:regular-cauchy-tail-fiber-window-projection}. Any source-facing read reaches $R_0,R_1,W_0,W_1,D_0,D_1$ before it can see a seal-facing row, so the transported classifier has the same finite source and window support.

Apply the shared seal factorization of \autoref{thm:regular-cauchy-tail-fiber-shared-seal-factorization}. The tail-meet route and the tail-agreement route both pass through the same row $T$ before $A$ is available.

Finally use non-escape from \autoref{thm:regular-cauchy-tail-fiber-nonescape}. Since the accepted packet contains no quotient-stream equality and no host-real equality, componentwise transport along $H$ cannot export either kind of equality.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber paired-window exactness]
\label{thm:regular-cauchy-tail-fiber-paired-window-exactness}
For an accepted $\RegularCauchyTailFiberUp$ carrier
$$
F=(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N),
$$
every $\RegularCauchyTailMeetUp$ read and every $\RealTailAgreementSealUp$ read factors through the same finite tuple
$$
(R_0,R_1,W_0,W_1,D_0,D_1,T).
$$
Thus the BEDC correspondence between the regular-tail meet route and the real-tail agreement route is witnessed by one paired-window tuple, not by an external equality of streams or host reals.
\end{theorem}
\begin{proof}
Start with a $\RegularCauchyTailMeetUp$ consumer. By \autoref{thm:regular-cauchy-tail-fiber-window-projection}, its source and window observations are exactly the two regular-rational rows, the two finite stream windows, and the two dyadic ledgers, followed by the shared fiber row $T$.

Now read a $\RealTailAgreementSealUp$ consumer. By \autoref{thm:regular-cauchy-tail-fiber-shared-seal-factorization}, the seal route uses the same shared fiber row $T$ before it reaches the seal row $A$.

The two reads therefore have identical finite support up to $T$. The remaining rows $H,C,P,N$ transport, replay, package, and name that support, so the correspondence is the displayed BEDC route through the paired-window tuple.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber seal-consumer exhaustion]
\label{thm:regular-cauchy-tail-fiber-seal-consumer-exhaustion}
For an accepted $\RegularCauchyTailFiberUp$ carrier
$$
F=(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N),
$$
all downstream reads by $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegularCauchyTailMeetUp$, $\RealTailAgreementSealUp$, and $\RealUp$ consumers remain inside
$$
R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N.
$$
No listed consumer can read a detached source tail, detached stream window, detached dyadic radius, second fiber row, external seal, quotient-stream witness, host-real witness, selected limit, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Use the carrier projection and the NameCert obligations of \autoref{thm:regular-cauchy-tail-fiber-namecert-obligations}. The only exposed rows are the two source rows, two windows, two dyadic ledgers, shared fiber, seal, transport, continuation, provenance, and local name.

For $\RegSeqRatUp$, $\StreamNameUp$, and $\DyadicRatCoreUp$ consumers, apply \autoref{thm:regular-cauchy-tail-fiber-window-projection}. Their reads are exhausted by $R_0,R_1,W_0,W_1,D_0,D_1,T$ before any seal boundary is available.

For $\RegularCauchyTailMeetUp$, $\RealTailAgreementSealUp$, and $\RealUp$ consumers, apply \autoref{thm:regular-cauchy-tail-fiber-shared-seal-factorization} and \autoref{thm:regular-cauchy-tail-fiber-nonescape}. The shared seal route reaches $A$ only through $T$, and the packet exports no further equality, selector, or completeness data.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber classifier exactness]
\label{thm:regular-cauchy-tail-fiber-classifier-exactness}
For accepted $\RegularCauchyTailFiberUp$ carriers
$$
F=(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N)
$$
and
$$
F'=(R'_0,R'_1,W'_0,W'_1,D'_0,D'_1,T',A',H',C',P',N'),
$$
the local classifier can identify the two packets only through common $\StreamNameUp$ windows, transported $\DyadicRatCoreUp$ tail-radius rows, the shared fiber row, and the $\RealTailAgreementSealUp$ seal row. In particular, classifier agreement is exhausted by displayed agreement of $W_0,W_1,D_0,D_1,T,A$ with $W'_0,W'_1,D'_0,D'_1,T',A'$ under the available $\hsame$ transports, and it does not use quotient equality of streams or host real equality.
\end{theorem}
\begin{proof}
Project both carriers through \autoref{def:regular-cauchy-tail-fiber-carrier}. The only rows available to the local classifier are the two source rows, two finite windows, two dyadic ledgers, shared fiber, seal, transport, continuation, provenance, and local name.

Apply the window projection of \autoref{thm:regular-cauchy-tail-fiber-window-projection}. Any source-facing classification must first pass through the common finite windows and their dyadic tail-radius ledgers before a seal-facing row can be observed.

Use the classifier stability and paired-window exactness rows. The $\hsame$ transports in $H$ and $H'$ carry the displayed window, radius, fiber, and seal coordinates component by component, while the shared fiber row $T$ is the unique route from the paired windows to the seal row $A$.

Finally apply non-escape. Since neither accepted packet contains quotient stream equality or host real equality, no such equality can participate in the classifier.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyTailFiberUp.RegularCauchyTailFiberPacket\_classifier\_exactness}

\begin{theorem}[Regular Cauchy tail fiber RegSeqRat Real handoff lock]
\label{thm:regular-cauchy-tail-fiber-regseq-real-handoff-lock}
Every $\RealUp$-facing consumer of an accepted $\RegularCauchyTailFiberUp$ packet
$$
F=(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N)
$$
must read the packet through the ordered handoff
$$
R_0,R_1 \longrightarrow W_0,W_1 \longrightarrow D_0,D_1 \longrightarrow T \longrightarrow A.
$$
Here $R_0,R_1$ are the two $\RegSeqRatUp$ rows, $W_0,W_1$ are the two $\StreamNameUp$ windows, $D_0,D_1$ are the two $\DyadicRatCoreUp$ ledgers, $T$ is the shared tail-fiber row, and $A$ is the $\RealTailAgreementSealUp$ row consumed at the $\RealUp$ boundary. The handoff cannot bypass these rows by using host real equality, quotient stream equality, selected limits, choice-like selectors, or ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-tail-fiber-carrier}. The accepted packet exposes exactly the two source rows, two windows, two dyadic ledgers, shared fiber, seal row, transports, continuation, package provenance, and local $\NameCert$ row.

Apply the NameCert obligations and window projection from \autoref{thm:regular-cauchy-tail-fiber-namecert-obligations} and \autoref{thm:regular-cauchy-tail-fiber-window-projection}. They force any source-facing read to start at the two $\RegSeqRatUp$ rows and pass through the displayed $\StreamNameUp$ windows and $\DyadicRatCoreUp$ ledgers.

Use shared seal factorization and classifier exactness from \autoref{thm:regular-cauchy-tail-fiber-shared-seal-factorization} and \autoref{thm:regular-cauchy-tail-fiber-classifier-exactness}. The common tail-fiber row $T$ is the route from the window-ledger surface to the seal row $A$, and the classifier has no quotient-stream or host-real coordinate.

Close with non-escape from \autoref{thm:regular-cauchy-tail-fiber-nonescape}. Since the packet exports no selected limit, choice-like selector, ambient completeness theorem, host real equality, or quotient stream equality, every $\RealUp$-facing handoff remains locked to the displayed ordered row sequence.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber tail-agreement seal exactness]
\label{thm:regular-cauchy-tail-fiber-tail-agreement-seal-exactness}
Let
$$
F=(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N)
$$
be an accepted $\RegularCauchyTailFiberUp$ packet. The tail-agreement seal consumed by $\RealTailAgreementSealUp$ is exactly the displayed row $A$ reached through the shared fiber row $T$ from the paired windows and dyadic ledgers. No detached seal row, external tail-agreement certificate, quotient stream equality, host real equality, selected limit, or ambient completeness witness can replace $A$.
\end{theorem}
\begin{proof}
Start from the carrier and the local $\NameCert$ obligations. They list a single seal row $A$ together with the paired source, window, dyadic, fiber, transport, continuation, package, and name rows.

Use the window projection to identify the two source rows through $W_0,W_1$ and $D_0,D_1$. The seal-facing route therefore begins with the same displayed finite support as the $\RegSeqRatUp$, $\StreamNameUp$, and $\DyadicRatCoreUp$ consumers.

Apply shared seal factorization. It sends the paired window-ledger support through the common tail-fiber row $T$ before the $\RealTailAgreementSealUp$ row is reachable, so the consumed seal is the displayed row $A$ reached through $T$.

Finally combine classifier exactness with non-escape. The classifier has no detached seal coordinate, and the packet exports no quotient-stream equality, host real equality, selected limit, or ambient completeness witness; hence there is no second seal available to the consumer.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber completion consumer nonescape]
\label{thm:regular-cauchy-tail-fiber-completion-consumer-nonescape}
Every completion-facing consumer of an accepted $\RegularCauchyTailFiberUp$ packet receives only the packet rows
$$
R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N.
$$
In particular, the consumer receives no selected limit, choice-like selector, ambient completeness theorem, quotient stream equality, host equality of reals, detached tail, detached window, detached dyadic ledger, second fiber, or detached tail-agreement seal.
\end{theorem}
\begin{proof}
Project the carrier and read the NameCert obligations. The completion-facing surface is the displayed finite tuple: two $\RegSeqRatUp$ rows, two $\StreamNameUp$ windows, two $\DyadicRatCoreUp$ ledgers, one shared fiber row, one tail-agreement seal row, and the structural rows.

Apply window projection and shared seal factorization. They route all source, window, dyadic, tail meet, and seal reads through $R_0,R_1,W_0,W_1,D_0,D_1,T,A$ before any completion-facing consumer can inspect the packet.

Use classifier exactness to keep the local classifier on the displayed window, ledger, fiber, and seal rows. The classifier supplies no quotient stream coordinate and no host real coordinate.

Finally invoke non-escape. Since the carrier contains no selected limit, choice-like selector, ambient completeness theorem, host equality, detached tail, detached window, detached ledger, second fiber, or detached seal, the completion-facing consumer cannot receive any such datum.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber source-swap stability]
\label{thm:regular-cauchy-tail-fiber-source-swap-stability}
For an accepted $\RegularCauchyTailFiberUp$ carrier
$$
F=(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N),
$$
the packet obtained by swapping the two source rows together with their $\StreamNameUp$ windows and $\DyadicRatCoreUp$ ledgers,
$$
F^{\mathrm{op}}=(R_1,R_0,W_1,W_0,D_1,D_0,T,A,H,C,P,N),
$$
has the same shared fiber route $T$ and reaches the same $\RealTailAgreementSealUp$ row $A$ through the same $\Cont$, $\Pkg$, and $\NameCert$ route. The source swap changes only the displayed order of the two source-window-radius triples; it does not create quotient-stream equality, host real equality, selected limits, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyTailFiberUp.RegularCauchyTailFiberPacket\_source\_swap\_stability}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-tail-fiber-carrier}. The source-facing coordinates occur as the two triples $(R_0,W_0,D_0)$ and $(R_1,W_1,D_1)$, while the fiber, seal, continuation, provenance, and local-name rows are the shared coordinates $T,A,C,P,N$.

Apply window projection from \autoref{thm:regular-cauchy-tail-fiber-window-projection}. Reading the original order or the swapped order exposes the same two source-window-radius triples before either read reaches a seal-facing row.

Use shared seal factorization and paired-window exactness from \autoref{thm:regular-cauchy-tail-fiber-shared-seal-factorization} and \autoref{thm:regular-cauchy-tail-fiber-paired-window-exactness}. Both source orders pass through the common fiber row $T$ before reaching the seal row $A$, and the continuation and package rows are reused without adding a second route.

Finally apply classifier exactness and non-escape from \autoref{thm:regular-cauchy-tail-fiber-classifier-exactness} and \autoref{thm:regular-cauchy-tail-fiber-nonescape}. The classifier sees only the displayed window, dyadic ledger, fiber, and seal coordinates, so the swap cannot introduce quotient-stream equality, host real equality, selected limits, or ambient completeness.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber componentwise transport]
\label{thm:regular-cauchy-tail-fiber-componentwise-transport}
For an accepted $\RegularCauchyTailFiberUp$ carrier
$$
F=(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N),
$$
componentwise $\hsame$ transport of
$$
R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N
$$
preserves carrier admission and preserves the local classifier on the transported packet. The transported carrier is read through the same displayed $H$ and $C$ rows, so every source, window, radius, fiber, seal, provenance, and local-name coordinate remains part of the same finite packet.
\end{theorem}
\begin{proof}
Start with the carrier projection. The row $H$ is part of the accepted packet and lists componentwise $\hsame$ transport for every coordinate that the packet exposes.

Replay the displayed continuation row $C$. The transported source rows, windows, dyadic ledgers, shared fiber, and seal are read in the same order as the original carrier, so carrier admission is preserved by rebuilding the finite tuple with transported coordinates.

Apply classifier stability. Because the classifier reads only the window, radius, fiber, and seal coordinates transported by $H$, the transported packet has the same local classifier support and cannot introduce an external classifier coordinate.
\end{proof}
\leanchecked{BEDC.Derived.RegularCauchyTailFiberUp.RegularCauchyTailFiberPacket\_componentwise\_transport}

\begin{theorem}[Regular Cauchy tail fiber ledger exhaustion]
\label{thm:regular-cauchy-tail-fiber-ledger-exhaustion}
Every downstream Real-and-Completion consumer of an accepted $\RegularCauchyTailFiberUp$ carrier
$$
F=(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N)
$$
is exhausted by the finite rows
$$
R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N.
$$
Such a consumer cannot read quotient stream equality, a selected limit, choice, ambient completeness, or host real equality from the packet.
\end{theorem}
\begin{proof}
Use the shared seal factorization of \autoref{thm:regular-cauchy-tail-fiber-shared-seal-factorization}. Every $\RegularCauchyTailMeetUp$ or $\RealTailAgreementSealUp$ route passes through the same shared fiber row $T$ before it can reach the seal row $A$.

Apply the seal-consumer exhaustion theorem. The downstream Real-and-Completion consumers listed there are already confined to the two source rows, two windows, two dyadic ledgers, shared fiber, seal, transport, continuation, provenance, and local name.

Finish with non-escape. The carrier contains no quotient stream equality, selected limit, choice row, ambient completeness theorem, or host real equality, so none of these can be exported to a downstream consumer.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber seal totality]
\label{thm:regular-cauchy-tail-fiber-seal-totality}
Every $\RealTailAgreementSealUp$, $\RegularCauchyTailMeetUp$,
$\CauchyLimitSealUp$, or $\RealUp$ seal consumer of an accepted
$\RegularCauchyTailFiberUp$ carrier
$$
F=(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N)
$$
is total exactly on
$$
R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N.
$$
The consumer reads no detached source row, detached window, second dyadic
ledger, second fiber, external seal, selected limit, quotient stream equality,
host real equality, or ambient completeness witness.
\end{theorem}
\leanchecked{BEDC.Derived.RegularCauchyTailFiberUp.RegularCauchyTailFiberPacket\_seal\_totality}
\begin{proof}
Start with paired-window exactness. It identifies the common support for
$\RegularCauchyTailMeetUp$ and $\RealTailAgreementSealUp$ reads as the two
source rows, two windows, two dyadic ledgers, and the shared fiber row $T$.

Apply shared seal factorization and seal-consumer exhaustion. The seal-facing
consumers reach $A$ only through the same finite packet, and the Real-facing
consumers named there do not leave $R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N$.

Use classifier exactness to keep the visible classifier on the displayed
window, ledger, fiber, and seal rows. This prevents a seal consumer from
creating an additional source, window, dyadic ledger, or fiber coordinate.

Finally apply ledger exhaustion. The downstream Real-and-Completion route is
already exhausted by the displayed tuple, so no selected limit, quotient stream
equality, host real equality, or ambient completeness witness is exported.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber source-seal ledger totality]
\label{thm:regular-cauchy-tail-fiber-source-seal-ledger-totality}
Every accepted $\RegularCauchyTailFiberUp$ carrier
$$
F=(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N)
$$
exposes exactly the two $\RegSeqRatUp$ source rows $R_0,R_1$, the two $\StreamNameUp$ windows $W_0,W_1$, the two $\DyadicRatCoreUp$ tail-radius ledgers $D_0,D_1$, the shared fiber row $T$, the $\RealTailAgreementSealUp$ and $\RealUp$ seal row $A$, and the structural rows $H,C,P,N$. No other source, window, dyadic ledger, fiber, seal, transport, continuation, package, or local NameCert row is available to a consumer of the carrier.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-tail-fiber-carrier}. Its displayed coordinates list the two regular-rational source rows, the two stream windows, the two dyadic tail-radius ledgers, the shared fiber row, the seal row, and the four structural rows.

Use the NameCert obligations of \autoref{thm:regular-cauchy-tail-fiber-namecert-obligations}. They identify the same coordinates as the whole local certificate surface, so a consumer cannot obtain a detached source, window, ledger, fiber, or seal row from the local name.

Finally apply ledger exhaustion from \autoref{thm:regular-cauchy-tail-fiber-ledger-exhaustion}. Every downstream Real-and-Completion read is confined to the same finite list, which gives totality for the source-to-seal ledger.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber window-fiber classifier coverage]
\label{thm:regular-cauchy-tail-fiber-window-fiber-classifier-coverage}
Every local classifier read of an accepted $\RegularCauchyTailFiberUp$ carrier is covered by the paired windows $W_0,W_1$, the dyadic ledgers $D_0,D_1$, the shared fiber row $T$, and the seal row $A$. The classifier has no quotient stream coordinate and no host-real coordinate.
\end{theorem}
\begin{proof}
Begin with the window projection of \autoref{thm:regular-cauchy-tail-fiber-window-projection}. Any source-facing classifier read reaches the two source rows through the paired windows and their dyadic ledgers before it can see a seal-facing row.

Next use classifier stability and paired-window exactness from \autoref{thm:regular-cauchy-tail-fiber-classifier-stability} and \autoref{thm:regular-cauchy-tail-fiber-paired-window-exactness}. These results keep the transported classifier on the same finite window, ledger, and fiber support.

Finally apply classifier exactness from \autoref{thm:regular-cauchy-tail-fiber-classifier-exactness}. The only equality data available to the classifier is displayed agreement of the window, ledger, fiber, and seal coordinates under componentwise transport, so quotient streams and host reals are outside its coverage.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber completion handoff scope]
\label{thm:regular-cauchy-tail-fiber-completion-handoff-scope}
Every downstream Real-and-Completion handoff from an accepted $\RegularCauchyTailFiberUp$ carrier factors through
$$
(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N)
$$
before reaching the $\RealUp$ seal boundary. The handoff cannot claim selected limits, ambient completeness, quotient streams, or host equality.
\end{theorem}
\begin{proof}
Use the shared seal factorization of \autoref{thm:regular-cauchy-tail-fiber-shared-seal-factorization}. Both the regular-tail meet route and the tail-agreement route pass through the same shared fiber row $T$ before the seal row $A$ is reachable.

Apply seal-consumer exhaustion from \autoref{thm:regular-cauchy-tail-fiber-seal-consumer-exhaustion}. The Real-and-Completion consumers named there read only the two source rows, two windows, two dyadic ledgers, shared fiber, seal, and structural rows.

Close with ledger exhaustion from \autoref{thm:regular-cauchy-tail-fiber-ledger-exhaustion}. Since the packet exports no selected limit, ambient completeness theorem, quotient stream equality, or host real equality, the handoff scope stops at the finite tuple and the seal boundary.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber shared-window seal determinacy]
\label{thm:regular-cauchy-tail-fiber-shared-window-seal-determinacy}
Let
$$
F=(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N)
$$
and
$$
F'=(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N)
$$
be accepted $\RegularCauchyTailFiberUp$ carriers with the same displayed source rows, windows, dyadic ledgers, shared fiber, seal row, transports, continuations, package provenance, and local $\NameCert$ row. Every $\RealTailAgreementSealUp$ or $\RealUp$ seal read available from either carrier factors through the same finite shared-window route
$$
(W_0,W_1,D_0,D_1)\longrightarrow T\longrightarrow A
$$
before it reaches the seal row $A$. No quotient stream, host equality, selected limit, or ambient completeness coordinate can distinguish the two seal routes.
\end{theorem}
\begin{proof}
Project the displayed rows of the two carriers. Since the tuples show the same $R_0,R_1,W_0,W_1,D_0,D_1,T,H,C,P,N$ rows, any source-facing read begins with the same finite support.

Use the window projection of \autoref{thm:regular-cauchy-tail-fiber-window-projection}. It pins each source row to the corresponding displayed window and dyadic ledger, so the two carriers expose the same $W_0,W_1,D_0,D_1$ window surface before any seal-facing route is reached.

Apply shared seal factorization from \autoref{thm:regular-cauchy-tail-fiber-shared-seal-factorization}. The seal route must pass through the common fiber row $T$ before it can reach $A$, so the $\RealTailAgreementSealUp$ and $\RealUp$ readings have the same finite path through the shared fiber.

Finally combine completion handoff scope with non-escape from \autoref{thm:regular-cauchy-tail-fiber-completion-handoff-scope} and \autoref{thm:regular-cauchy-tail-fiber-nonescape}. These results rule out detached seal paths, quotient-stream equality, host equality, selected limits, and ambient completeness coordinates; hence no external coordinate can separate the two displayed seal routes.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber source non-escape]
\label{thm:regular-cauchy-tail-fiber-source-nonescape}
Every source-facing read of an accepted $\RegularCauchyTailFiberUp$ packet that is later consumed by $\RegularCauchyTailMeetUp$, $\RealTailAgreementSealUp$, or $\RealUp$ remains inside
$$
R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N.
$$
The packet exports no detached source tail, detached $\StreamNameUp$ window, second fiber row, quotient stream witness, host-real witness, selected limit, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-tail-fiber-carrier}. Its source-facing data are exactly the two source rows, paired windows, dyadic ledgers, shared fiber row, seal row, and structural rows displayed in the theorem.

Apply paired-window exactness from \autoref{thm:regular-cauchy-tail-fiber-paired-window-exactness}. Any source read that reaches a downstream meet, tail-agreement seal, or Real consumer must keep the two windows and two dyadic ledgers paired with the same shared fiber row.

Use seal-consumer exhaustion from \autoref{thm:regular-cauchy-tail-fiber-seal-consumer-exhaustion} and source-seal ledger totality from \autoref{thm:regular-cauchy-tail-fiber-source-seal-ledger-totality}. These rows account for every seal-facing read through $R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N$ before any downstream consumer can inspect the packet.

Finally apply shared-window seal determinacy from \autoref{thm:regular-cauchy-tail-fiber-shared-window-seal-determinacy}. Since the finite route through the paired windows, ledgers, fiber, and seal row is already determined, no detached tail, detached window, second fiber, quotient witness, host-real witness, selected limit, or ambient completeness theorem can be exported.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber meet route]
\label{thm:regular-cauchy-tail-fiber-meet-route}
Every accepted shared-window tail-fiber read of a $\RegularCauchyTailFiberUp$ packet factors through the displayed $\StreamNameUp$ finite-window rows, the $\RegSeqRatUp$ classifier rows, the $\DyadicRatCoreUp$ tolerance rows, and the $\CauchyLimitSealUp$ completion handoff before it may be consumed as a completion-facing fiber. The route exports no detached tail, detached finite window, second fiber, quotient stream witness, host-real equality, selected limit, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Project the accepted carrier rows
$$
R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N.
$$
The two source rows are read only through the displayed windows, dyadic ledgers, shared fiber, seal row, histories, continuations, package provenance, and local naming row.

Apply the window projection theorem \autoref{thm:regular-cauchy-tail-fiber-window-projection}. It places each $\RegSeqRatUp$ source read behind the corresponding $\StreamNameUp$ finite-window row and $\DyadicRatCoreUp$ tolerance row.

Next use shared-window seal determinacy from \autoref{thm:regular-cauchy-tail-fiber-shared-window-seal-determinacy}. The two accepted routes therefore meet at the same shared fiber row $T$ before any seal-facing read can reach $A$.

Transport the classifier componentwise by \autoref{thm:regular-cauchy-tail-fiber-componentwise-transport}, then exhaust the ledger by \autoref{thm:regular-cauchy-tail-fiber-ledger-exhaustion}. These steps keep the classifier, finite windows, dyadic tolerance rows, and shared fiber on the displayed packet.

Finally apply completion handoff scope from \autoref{thm:regular-cauchy-tail-fiber-completion-handoff-scope}. The resulting completion-facing fiber is already routed through the finite-window, classifier, tolerance, shared-seal, and $\CauchyLimitSealUp$ handoff surface, so no external coordinate remains available to the consumer.
\end{proof}

\begin{theorem}[Regular Cauchy tail fiber Real-seal determinacy]
\label{thm:regular-cauchy-tail-fiber-real-seal-determinacy}
Let
$$
F=(R_0,R_1,W_0,W_1,D_0,D_1,T,A,H,C,P,N)
$$
be an accepted $\RegularCauchyTailFiberUp$ packet. Any $\RealUp$ seal-facing consumer that agrees with the shared meet route through the displayed fiber row $T$ reaches the unique displayed seal row $A$. Such a consumer cannot replace $A$ by a second seal, quotient-stream equality, host real equality, selected limit, or ambient completeness witness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-tail-fiber-carrier}. The only seal-facing rows displayed by the accepted packet are the paired source rows, paired windows, dyadic ledgers, shared fiber row $T$, seal row $A$, transports, continuation, provenance, and local name.

Use the window projection theorem \autoref{thm:regular-cauchy-tail-fiber-window-projection} and shared seal factorization from \autoref{thm:regular-cauchy-tail-fiber-shared-seal-factorization}. Together they force every seal-facing read to pass through the displayed windows and then through $T$ before reaching $A$.

Apply paired-window exactness from \autoref{thm:regular-cauchy-tail-fiber-paired-window-exactness} and classifier exactness from \autoref{thm:regular-cauchy-tail-fiber-classifier-exactness}. These rows identify the two window ledgers and the source classifiers with the same finite meet route, so an agreeing $\RealUp$ consumer has no second path to a different seal coordinate.

Finally invoke the meet route \autoref{thm:regular-cauchy-tail-fiber-meet-route} and non-escape from \autoref{thm:regular-cauchy-tail-fiber-nonescape}. The meet route supplies the shared route through $T$, while non-escape excludes detached seal data, quotient-stream equality, host real equality, selected limits, and ambient completeness witnesses. Hence the displayed row $A$ is the unique seal row available to the consumer.
\end{proof}

\closureat{RegularCauchyTailFiberUp}{seedStr}
\closureat{RegularCauchyTailFiberUp}{obligationStr}[BEDC.Derived.RegularCauchyTailFiberUp.RegularCauchyTailFiberTasteGate\_single\_carrier\_alignment]
\closureat{RegularCauchyTailFiberUp}{scopedStr}[BEDC.Derived.RegularCauchyTailFiberUp.RegularCauchyTailFiberTasteGate\_single\_carrier\_alignment]
\closureat{RegularCauchyTailFiberUp}{publicStr}[BEDC.Derived.RegularCauchyTailFiberUp.RegularCauchyTailFiberTasteGate\_single\_carrier\_alignment]
\closureat{RegularCauchyTailFiberUp}{bridgedStr}[BEDC.Derived.RegularCauchyTailFiberUp.taste\_gate]
\closureat{RegularCauchyTailFiberUp}{matureStr}[BEDC.Derived.RegularCauchyTailFiberUp.taste\_gate]

\begin{closurestatus}{\RegularCauchyTailFiberUp}
  \constructivestory{A $\RegularCauchyTailFiberUp$ packet is generated from two $\RegSeqRatUp$ source rows, two finite $\StreamNameUp$ windows, two $\DyadicRatCoreUp$ tail-radius ledgers, one shared tail fiber row, one $\RealUp$ tail-agreement seal row, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{def:regular-cauchy-tail-fiber-carrier}, \autoref{thm:regular-cauchy-tail-fiber-namecert-obligations}, \autoref{thm:regular-cauchy-tail-fiber-window-projection}, \autoref{thm:regular-cauchy-tail-fiber-shared-seal-factorization}, \autoref{thm:regular-cauchy-tail-fiber-nonescape}, \autoref{thm:regular-cauchy-tail-fiber-tastegate-contract}, \autoref{thm:regular-cauchy-tail-fiber-classifier-stability}, \autoref{thm:regular-cauchy-tail-fiber-paired-window-exactness}, \autoref{thm:regular-cauchy-tail-fiber-seal-consumer-exhaustion}, \autoref{thm:regular-cauchy-tail-fiber-classifier-exactness}, \autoref{thm:regular-cauchy-tail-fiber-regseq-real-handoff-lock}, \autoref{thm:regular-cauchy-tail-fiber-tail-agreement-seal-exactness}, \autoref{thm:regular-cauchy-tail-fiber-completion-consumer-nonescape}, \autoref{thm:regular-cauchy-tail-fiber-source-swap-stability}, \autoref{thm:regular-cauchy-tail-fiber-componentwise-transport}, \autoref{thm:regular-cauchy-tail-fiber-ledger-exhaustion}, \autoref{thm:regular-cauchy-tail-fiber-source-seal-ledger-totality}, \autoref{thm:regular-cauchy-tail-fiber-window-fiber-classifier-coverage}, \autoref{thm:regular-cauchy-tail-fiber-completion-handoff-scope}, \autoref{thm:regular-cauchy-tail-fiber-shared-window-seal-determinacy}, and \autoref{thm:regular-cauchy-tail-fiber-real-seal-determinacy} specify the finite common-tail fiber carrier, local NameCert rows, window projection, shared seal factorization, non-escape boundary, TasteGate follow-up contract, classifier stability, paired-window exactness, seal-consumer exhaustion, classifier exactness, RegSeqRat-to-Real handoff lock, tail-agreement seal exactness, completion consumer nonescape, source-swap stability, componentwise transport, ledger exhaustion, source-seal ledger totality, classifier coverage, completion handoff scope, shared-window seal determinacy, and Real-seal determinacy.}
  \leanchecked{BEDC.Derived.RegularCauchyTailFiberUp.taste\_gate}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{This chapter does not prove quotient equality of regular streams, host real equality, selected-limit existence, ambient $\RealUp$ completeness, Lean verification, or bridge checking for $\RegularCauchyTailFiberUp$.}
  \upgradepath{No further paper-side theory closure remains for the common-tail fiber and $\RealUp$ seal handoff; bridge checking remains on the verification axis.}
\end{closurestatus}
