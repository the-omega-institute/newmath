\chapter{A Concrete Naming Certificate for $\RealPositiveRadiusReadbackUp$}
\label{ch:concrete-instances-real-positive-radius-readback-namecert}
\origin{human}

Following Bishop and Bridges constructive analysis, the $\RealPositiveRadiusReadbackUp$ packet records the finite lower-bound readback surface through which a positive real name exposes an inspectable radius before reciprocal, reciprocal-modulus, or completion-facing consumers use the positivity witness. It sits after the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the located-positive-real witness of \autoref{ch:concrete-instances-located-positive-real-namecert}, the positive dyadic row of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream window of \autoref{ch:concrete-instances-streamname-namecert}, and the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}. The packet is a displayed radius-readback boundary; it is not trichotomy, quotient real equality, a total inverse, a selected limit, or ambient $\RealUp$ completeness.

\begin{definition}[Real positive radius readback surface]
\label{def:real-positive-radius-readback-surface}
A $\RealPositiveRadiusReadbackUp$ carrier is a finite $\BHist$ packet
$$
X=(R,L,D,W,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the sealed $\RealUp$ row for the positive real name whose radius is being read.
\item $L$ is the $\LocatedPositiveRealUp$ witness that exposes the positivity route for the same real-name seal.
\item $D$ is the positive $\DyadicRatCoreUp$ radius row read from the located witness and displayed as the lower-bound budget.
\item $W$ is the finite $\StreamNameUp$ window through which the source name and radius comparison are inspected.
\item $Q$ is the $\RegSeqRatUp$ readback row that records the rational observations supporting the finite radius read.
\item $H$ records componentwise $\hsame$ transport for the real seal, located-positive witness, dyadic radius, finite window, regular readback, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
L,D,W,Q \longmapsto R .
$$
\item $P$ is the $\Pkg$ provenance over $\RealPositiveRadiusReadbackUp$, $\RealUp$, $\LocatedPositiveRealUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealPositiveRadiusReadbackUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $R,L,D,W,Q,H,C,P,N$. It has no coordinate for trichotomy, quotient equality of real names, a total inverse operation, selected limits, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Real positive radius readback obligations]
\label{thm:real-positive-radius-readback-obligations}
For every accepted $\RealPositiveRadiusReadbackUp$ carrier
$$
X=(R,L,D,W,Q,H,C,P,N),
$$
the local $\NameCert_{\RealPositiveRadiusReadbackUp}$ surface consists exactly of real-seal admission through $R$, located-positive witness exposure through $L$, positive dyadic lower-bound readback through $D$, finite stream-window inspection through $W$, regular rational readback through $Q$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-positive-radius-readback-surface}. The analytic rows are $R,L,D,W,Q$, and the structural rows are $H,C,P,N$.

The located witness $L$ is the only route from the positive real name to a displayed radius. The dyadic row $D$, the finite window $W$, and the regular readback $Q$ are downstream of that witness and therefore form the complete lower-bound readback surface.

The rows $H,C,P,N$ transport, replay, source, and name the same finite packet. Since the carrier has no coordinate for the excluded order, quotient, inverse, selected-limit, or completeness data, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Real positive radius readback route]
\label{thm:real-positive-radius-readback-route}
Every reciprocal-facing or completion-facing read of an accepted $\RealPositiveRadiusReadbackUp$ packet factors through the finite route
$$
\LocatedPositiveRealUp,\ \DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp \longmapsto \RealUp .
$$
In particular, a consumer reaches the $\RealUp$ seal only after the located-positive witness, positive dyadic radius, finite stream window, and regular rational readback have been displayed in the same packet.
\end{theorem}
\begin{proof}
Inspect the route coordinates in \autoref{def:real-positive-radius-readback-surface}. The $\RealUp$ row $R$ is read through the located-positive witness $L$ together with the radius, window, and readback rows $D,W,Q$.

Apply the obligation surface of \autoref{thm:real-positive-radius-readback-obligations}. It binds $L,D,W,Q$ to the same transport, replay, provenance, and local naming rows, so a downstream consumer cannot detach the positive-radius read from the source real-name seal.

Thus every accepted read reaches $R$ through the displayed route. The packet contains no row for trichotomy, quotient real equality, total inversion, selected limits, or ambient completeness, so none of those endpoints is exported by the route.
\end{proof}

\falsifiablePrediction{An accepted $\RealPositiveRadiusReadbackUp$ packet cannot export the same positive-radius readback if it omits the LocatedPositiveReal witness, positive DyadicRatCore radius, StreamName finite window, RegSeqRat readback, transport, replay, provenance, or local naming row.}

\independenceWitness{real, located-positive-real, dyadicratcore, streamname, and regseqrat: $\RealPositiveRadiusReadbackUp$ is not bijective to any listed sibling because only this packet binds the positive real seal, located lower-bound witness, positive radius row, finite window, regular readback, transport, replay, provenance, and local NameCert surface as one readback boundary.}

\closureat{RealPositiveRadiusReadbackUp}{seedStr}

\begin{closurestatus}{\RealPositiveRadiusReadbackUp}
  \constructivestory{A $\RealPositiveRadiusReadbackUp$ packet is generated from a RealUp seal, a LocatedPositiveRealUp witness, a positive DyadicRatCoreUp radius row, a StreamNameUp finite window, a RegSeqRatUp readback, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:real-positive-radius-readback-surface}, \autoref{thm:real-positive-radius-readback-obligations}, and \autoref{thm:real-positive-radius-readback-route} seed the finite lower-bound readback route for positive real names.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close trichotomy, quotient real equality, total inverse operations, selected limits, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located-positive witness exposure, positive dyadic radius exactness, finite stream-window inspection, regular readback, transport, replay, provenance, and local naming for BEDC.Derived.RealPositiveRadiusReadbackUp.}
\end{closurestatus}
