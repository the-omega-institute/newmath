\chapter{A Concrete Naming Certificate for $\BishopLocatedOscillationSpaceUp$}
\label{ch:concrete-instances-bishop-located-oscillation-space-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopLocatedOscillationSpaceUp$ packet records a located metric subspace together with a finite oscillation gauge before any Real or Completion consumer reads the resulting function space. It sits over the metric source of \autoref{ch:concrete-instances-metric-namecert}, the located-subset surface of \autoref{ch:concrete-instances-locatedset-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}, and the explicit modulus-continuity row of \autoref{ch:concrete-instances-moduluscontinuity-namecert}. The packet exposes oscillation control as finite located metric data, not as an external selector, quotient function equality, countable choice, host continuity, or ambient completeness.

\begin{definition}[Bishop located oscillation space carrier]
\label{def:bishop-located-oscillation-space-carrier}
A $\BishopLocatedOscillationSpaceUp$ carrier is a finite $\BHist$ packet
$$
O=(M,L,G,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricUp$ source row naming the finite metric requests and comparison budget visible to the packet.
\item $L$ is the $\LocatedSetUp$ row for the inspected subspace, including the positive and negative locatedness tests allowed over the source row $M$.
\item $G$ is the located oscillation gauge row. It records the displayed input cells, local radius requests, dyadic slack, and oscillation bounds served on the same support as $M$ and $L$.
\item $R$ is the $\RegSeqRatUp$ readback row for the sampled values and oscillation bounds carried by $G$.
\item $E$ is the terminal $\RealUp$ seal reached only after the metric source, located-set row, oscillation gauge, and regular rational readback have been displayed.
\item $H$ records componentwise $\hsame$ transport for the metric source, located subset, oscillation gauge, regular readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from each oscillation request through $M,L,G,R,E,H$.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedOscillationSpaceUp$, $\MetricUp$, $\LocatedSetUp$, $\RegSeqRatUp$, $\RealUp$, $\ModulusContinuityUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopLocatedOscillationSpaceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $M,L,G,R,E,H,C,P,N$. It has no coordinate for selected representatives, quotient function equality, a detached continuity principle, countable choice, global compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop located oscillation space NameCert obligations]
\label{thm:bishop-located-oscillation-space-namecert-obligations}
For every accepted $\BishopLocatedOscillationSpaceUp$ carrier
$$
O=(M,L,G,R,E,H,C,P,N),
$$
the local $\NameCert_{\BishopLocatedOscillationSpaceUp}$ surface consists exactly of carrier admission for the displayed rows, metric-source exposure through $M$, located-subset admission through $L$, oscillation-gauge exactness through $G$, regular rational readback through $R$, Real-seal nonescape through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:bishop-located-oscillation-space-carrier}. The packet displays exactly the metric source, located subset, oscillation gauge, regular readback, Real seal, transport, replay, provenance, and local name.

The source and located rows are read first. Row $M$ fixes the metric comparison surface, and row $L$ restricts every oscillation request to the located subset carried on that surface.

The oscillation obligation is the gauge row $G$. Its local radii and oscillation bounds are valid only on the same located support, and the readback row $R$ exposes those bounds as regular rational data before the Real seal $E$ can be consumed.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Since the carrier has no row for selected representatives, quotient equality, detached continuity, countable choice, global compactness, or ambient Real completeness, none of those principles is exported by the local NameCert surface.
\end{proof}

\begin{theorem}[Bishop located oscillation space modulus route]
\label{thm:bishop-located-oscillation-space-modulus-route}
Every $\ModulusContinuityUp$ consumer of an accepted $\BishopLocatedOscillationSpaceUp$ carrier reads the finite route
$$
M,L,G \longmapsto R,E \longmapsto H,C,P,N .
$$
The consumer receives a source metric row, a located subset row, a displayed oscillation gauge, regular rational readback, and a Real seal before any modulus-continuity handoff is visible. The route exports no external selector, host continuity theorem, quotient function equality, selected representative, countable-choice schedule, global compactness, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Project the carrier through \autoref{def:bishop-located-oscillation-space-carrier}. The only analytic rows available to a modulus consumer are $M,L,G,R,E$.

Use \autoref{thm:bishop-located-oscillation-space-namecert-obligations}. It forces the metric source and located subset to be displayed before the oscillation gauge is read, and it forces the regular rational row and Real seal to be displayed before the handoff reaches a $\ModulusContinuityUp$ consumer.

The structural rows $H,C,P,N$ preserve that same finite route. Therefore the modulus-facing read is a located oscillation handoff over displayed metric data, not a host continuity principle or a completion-side selector.
\end{proof}

\closureat{BishopLocatedOscillationSpaceUp}{seedStr}

\begin{closurestatus}{\BishopLocatedOscillationSpaceUp}
  \constructivestory{A $\BishopLocatedOscillationSpaceUp$ packet is generated from a MetricUp source row, a LocatedSetUp row, a located oscillation gauge, RegSeqRatUp readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-located-oscillation-space-carrier}, \autoref{thm:bishop-located-oscillation-space-namecert-obligations}, and \autoref{thm:bishop-located-oscillation-space-modulus-route} seed the located metric oscillation route for Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim selected representatives, quotient function equality, detached continuity, countable choice, global compactness, ambient $\RealUp$ completeness, Lean theorem checking, or standard bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, metric-source exposure, located-subset admission, oscillation-gauge exactness, regular rational readback, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.BishopLocatedOscillationSpaceUp.}
\end{closurestatus}
