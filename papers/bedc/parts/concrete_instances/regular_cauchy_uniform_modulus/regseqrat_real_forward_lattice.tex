\begin{theorem}[Regular Cauchy uniform modulus RegSeqRat--Real forward lattice]
\label{thm:regular-cauchy-uniform-modulus-regseqrat-real-forward-lattice}
Every $\RegularCauchyUniformModulusUp$ consumer that exports a uniform tail budget must first read the sibling rows $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ through the same finite packet. The uniform bound is available only after the stream windows, regular rational readback, dyadic tolerance ledger, and terminal Real seal route have been displayed with componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming. The forward lattice exports no quotient stream, selected limit, countable-choice schedule, host real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the $\RegularCauchyUniformModulusUp$ carrier. The stream row supplies the finite windows, the dyadic row supplies the tolerance ledger, and the modulus row supplies the uniform tail budget.

The regular rational and Real-facing reads are downstream of those rows. A consumer must keep the $\RegSeqRatUp$ readback and terminal $\RealUp$ seal on the same displayed route before exporting the tail budget to a completion-facing use.

Finally read the structural rows. The $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ data preserve and name the same packet, and no row supplies quotient streams, selected limits, countable choice, host real equality, or ambient completeness.
\end{proof}
