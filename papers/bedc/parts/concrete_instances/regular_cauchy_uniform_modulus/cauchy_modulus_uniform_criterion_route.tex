\begin{theorem}[RegularCauchyUniformModulus Cauchy criterion route]
\label{thm:regular-cauchy-uniform-modulus-cauchy-criterion-route}
Every accepted $\RegularCauchyUniformModulusUp$ read depends on the $\CauchyModulusUp$ schedule of \autoref{ch:concrete-instances-cauchymodulus-namecert} and the $\UniformCauchyCriterionUp$ shared-threshold row of \autoref{ch:concrete-instances-uniformcauchycriterion-namecert} before any $\RegSeqRatUp$ or $\RealUp$ consumer inspects the displayed modulus. The schedule supplies the finite tolerance-to-index request, the uniform criterion row synchronizes that request across the relevant tail windows, and only then may the regular rational readback and terminal Real seal be consumed. The route exports no selected limit, quotient stream equality, host real equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the $\CauchyModulusUp$ dependency first. It provides the finite schedule that turns a dyadic tolerance request into the index boundary used by the modulus row.

Then read the $\UniformCauchyCriterionUp$ dependency. Its shared-threshold row records that the same index boundary controls all tail-window comparisons needed by the regular readback.

Only after those two dependency rows are visible can the existing $\RegSeqRatUp$ and $\RealUp$ consumers inspect the displayed $\RegularCauchyUniformModulusUp$ packet. Since the route contains no selected limit, quotient stream, host equality, choice schedule, or ambient completeness row, the dependency remains a finite sibling route.
\end{proof}
