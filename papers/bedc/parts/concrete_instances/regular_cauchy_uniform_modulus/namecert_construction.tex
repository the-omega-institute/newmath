\chapter{A Concrete Naming Certificate for $\RegularCauchyUniformModulusUp$}
\label{ch:concrete-instances-regular-cauchy-uniform-modulus-namecert}
\origin{human}

Following Bishop and Bridges, a regular Cauchy real name is useful to Real and Completion consumers only when its uniform tail budget is displayed before any terminal real seal is consumed. The $\RegularCauchyUniformModulusUp$ packet records that budget as finite BEDC data over the stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic observation ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. It names the explicit uniform-modulus surface needed by regular Cauchy tails; it is not a quotient of sequences, a selected limit, countable choice, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy uniform modulus carrier]
\label{def:regular-cauchy-uniform-modulus-carrier}
A $\RegularCauchyUniformModulusUp$ carrier is a finite $\BHist$ packet
$$
U=(S,D,M,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ row, carrying the finite windows opened on the source name before any regular readback is inspected.
\item $D$ is the $\DyadicRatCoreUp$ observation row, containing the dyadic tolerance requests and arithmetic comparisons used by the tail budget.
\item $M$ is the displayed regular modulus row; for each requested dyadic tolerance it records the finite index bound after which the regular Cauchy readbacks are stable inside that tolerance.
\item $T$ is the finite tail ledger joining stream windows, dyadic tolerances, regular readbacks, and the modulus row.
\item $H$ records componentwise $\hsame$ transport for $S,D,M,T$ and the structural certificate rows.
\item $C$ records displayed $\Cont$ replay from a tail-stability request through the same stream, dyadic, modulus, and tail-ledger rows.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyUniformModulusUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyUniformModulusUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $S,D,M,T,H,C,P,N$. It has no coordinate for quotient stream equality, selected limit witnesses, host real equality, countable choice, or ambient completeness.
\end{definition}

\begin{theorem}[Regular Cauchy uniform modulus NameCert obligations]
\label{thm:regular-cauchy-uniform-modulus-namecert-obligations}
For every accepted $\RegularCauchyUniformModulusUp$ carrier
$U=(S,D,M,T,H,C,P,N)$, the local
$\NameCert_{\RegularCauchyUniformModulusUp}$ surface consists exactly of carrier admission, stream-window exposure, dyadic tolerance accounting, displayed regular modulus admission, finite tail-ledger exactness, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every $\RegSeqRatUp$ or $\RealUp$ consumer that uses a regular tail budget must read these rows before it can consume the terminal real-name seal.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-uniform-modulus-carrier}. The projection displays the stream row $S$, dyadic observation row $D$, regular modulus row $M$, finite tail ledger $T$, transport row $H$, continuation row $C$, provenance row $P$, and local name $N$.

The numerical obligations are exhausted by $S,D,M,T$. Stream windows are opened in $S$, tolerance comparisons are read in $D$, the index bound is displayed in $M$, and the ledger $T$ records the finite route joining those rows to regular rational readbacks.

The structural obligations are exhausted by $H,C,P,N$. They transport, replay, source, and name the same displayed route, while the carrier lists no quotient stream, selected limit, host equality, choice schedule, or ambient completeness coordinate. Hence the listed rows are exactly the local $\NameCert_{\RegularCauchyUniformModulusUp}$ obligations.
\end{proof}

\begin{theorem}[Regular Cauchy uniform tail stability]
\label{thm:regular-cauchy-uniform-modulus-tail-stability}
Let $U=(S,D,M,T,H,C,P,N)$ be an accepted
$\RegularCauchyUniformModulusUp$ carrier. Every tail-stability request for the associated regular Cauchy readback factors through the finite route
$$
\StreamNameUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \RegularCauchyUniformModulusUp,\ \RealUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
After the modulus row $M$ has supplied the displayed index bound, the tail ledger $T$ is the only admissible source of stability data for the requested dyadic tolerance. No consumer may replace that route by a host convergence theorem, a selected Cauchy limit, quotient equality of streams, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Use \autoref{thm:regular-cauchy-uniform-modulus-namecert-obligations}. A tail-stability request first opens the stream row $S$ and the dyadic observation row $D$ for the requested tolerance.

The modulus row $M$ then supplies the finite index bound. The ledger $T$ binds that bound to the same stream windows and dyadic comparisons, so the regular rational readback can only be consumed through the displayed route.

Finally $H,C,P,N$ transport, replay, source, and name the packet. Since no carrier coordinate stores a selected limit, quotient stream class, host real equality, choice schedule, or ambient completeness principle, the displayed tail ledger is the complete stability surface.
\end{proof}

\begin{theorem}[Regular Cauchy uniform modulus tail-budget exactness]
\label{thm:regular-cauchy-uniform-modulus-tail-budget-exactness}
Let $U=(S,D,M,T,H,C,P,N)$ be an accepted
$\RegularCauchyUniformModulusUp$ carrier. Before any tail-stability or completion-facing consumer reads the modulus row, the packet exposes the same finite dyadic budget in $D$, the same $\StreamNameUp$ window in $S$, the same $\RegSeqRatUp$ readback boundary carried by $T$, and the same $\RealUp$ seal route recorded by $H,C,P,N$. Thus the tail budget is exact as a displayed BEDC row: no later consumer may widen it into a quotient stream, selected limit, host real equality, countable-choice schedule, or ambient $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-uniform-modulus-carrier}. The rows $S,D,M,T$ expose the stream window, dyadic tolerance ledger, displayed modulus, and finite tail ledger before the structural rows are consulted.

Use \autoref{thm:regular-cauchy-uniform-modulus-namecert-obligations}. Its obligation list requires every $\RegSeqRatUp$ or $\RealUp$ consumer to read the stream-window exposure, dyadic tolerance accounting, modulus-row admission, and tail-ledger exactness before the terminal real-name seal is consumed.

Apply \autoref{thm:regular-cauchy-uniform-modulus-tail-stability}. The tail-stability route factors through the same $S,D,M,T$ rows, so a completion-facing read cannot replace the displayed budget by a different modulus surface.

Finally inspect the carrier inventory. There is no quotient stream row, selected limit row, host real equality row, countable-choice row, or ambient completeness row, and $H,C,P,N$ only transport, replay, source, and name the already displayed packet. Hence the exposed tail budget is exact for every downstream read.
\end{proof}

\begin{theorem}[Regular Cauchy uniform modulus dyadic locator handoff]
\label{thm:regular-cauchy-uniform-modulus-dyadic-locator-handoff}
Let $U=(S,D,M,T,H,C,P,N)$ be an accepted
$\RegularCauchyUniformModulusUp$ carrier. Every Real- or Completion-facing consumer receives the dyadic tolerance row $D$ before it may consume the regular modulus row $M$, the finite tail ledger $T$, the $\RegSeqRatUp$ readback route, and the terminal $\RealUp$ seal. Thus the handoff from dyadic locator data to regular tail data is a displayed BEDC boundary; it exports no quotient stream, selected limit, countable-choice schedule, host equality, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with \autoref{thm:regular-cauchy-uniform-modulus-namecert-obligations}. Its obligation surface places dyadic tolerance accounting in $D$ before displayed regular modulus admission in $M$ and finite tail-ledger exactness in $T$.

Apply \autoref{thm:regular-cauchy-uniform-modulus-tail-stability}. A tail-stability request opens the $\StreamNameUp$ row $S$ and the $\DyadicRatCoreUp$ row $D$ before the modulus bound and regular rational readback are available.

Now use \autoref{thm:regular-cauchy-uniform-modulus-tail-budget-exactness}. The exposed budget remains the same displayed dyadic row throughout the completion-facing route, so the terminal $\RealUp$ seal can only be read after $D,M,T$ have been consumed in that order.

Finally inspect the carrier inventory. The rows $H,C,P,N$ transport, replay, source, and name the same handoff, while the carrier has no coordinate for quotient streams, selected limits, countable choice, host equality, or ambient completeness.
\end{proof}

\begin{theorem}[Regular Cauchy uniform modulus lattice crosslink]
\label{thm:regular-cauchy-uniform-modulus-lattice-crosslink}
Every accepted $\RegularCauchyUniformModulusUp$ carrier presents the same
$$
S,D,M,T,H,C,P,N
$$
route needed by downstream $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, and $\CauchySequenceSpaceUp$ completion-facing reads. The crosslink keeps stream windows, dyadic budgets, regular readback, uniform tail ledgers, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming visible as one L10 lattice route; it exports no quotient stream equality, selected limit, host real equality, countable-choice schedule, or ambient completeness row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-uniform-modulus-carrier}. The rows $S,D,M,T$ expose the stream window, dyadic tolerance ledger, displayed modulus, and finite tail ledger, while $H,C,P,N$ transport, replay, source, and name that same packet.

Apply \autoref{thm:regular-cauchy-uniform-modulus-namecert-obligations}. Its obligation surface requires every $\RegSeqRatUp$ or $\RealUp$ consumer to pass through the displayed stream-window, dyadic tolerance, modulus, and tail-ledger rows before the terminal seal is consumed.

Apply \autoref{thm:regular-cauchy-uniform-modulus-tail-budget-exactness}. The tail budget exposed to a completion-facing read is the same displayed BEDC row throughout the route, so it cannot be replaced by a quotient stream, selected Cauchy limit, host real equality, choice schedule, or ambient completeness theorem.

The sibling completion-facing window route is the one named in \autoref{ch:concrete-instances-cauchysequencespace-namecert}. Therefore the accepted carrier supplies exactly the $S,D,M,T,H,C,P,N$ lattice crosslink used by $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, and $\CauchySequenceSpaceUp$ consumers.
\end{proof}

\falsifiablePrediction{Every accepted $\RegularCauchyUniformModulusUp$ consumer displays a $\StreamNameUp$ row, a $\DyadicRatCoreUp$ observation row, a regular modulus row, a finite tail ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row before any $\RegSeqRatUp$ readback or terminal $\RealUp$ seal is consumed.}

\independenceWitness{streamname, dyadicratcore, regseqrat, real: $\RegularCauchyUniformModulusUp$ is not bijective to any listed sibling because it packages the displayed uniform tail budget and finite tail ledger as one NameCert surface between stream observations, dyadic tolerances, regular rational readback, and the terminal real-name seal.}

\closureat{RegularCauchyUniformModulusUp}{seedStr}

\begin{closurestatus}{\RegularCauchyUniformModulusUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-uniform-modulus-carrier}, \autoref{thm:regular-cauchy-uniform-modulus-namecert-obligations}, and \autoref{thm:regular-cauchy-uniform-modulus-tail-stability} seed the finite uniform-modulus surface over $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close Lean verification, quotient stream equality, selected limits, host real equality, countable choice, bridge checking, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, stream-window exposure, dyadic tolerance accounting, modulus-row admission, finite tail-ledger exactness, transport replay, and non-escape over the same displayed packet.}
  \constructivestory{A $\RegularCauchyUniformModulusUp$ packet is generated from finite stream windows, dyadic tolerance observations, a displayed regular modulus row, a tail ledger, componentwise transport, displayed continuation replay, package provenance, and local naming evidence.}
\end{closurestatus}
