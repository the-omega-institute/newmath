The $\RegularCauchyUniformModulusUp$ tail-budget route consumes the finite $\StreamNameUp$ windows of \autoref{ch:concrete-instances-streamname-namecert}, the $\RegSeqRatUp$ regular readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the $\DyadicTailBallUp$ late-tail containment boundary of \autoref{ch:concrete-instances-dyadic-tail-ball-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert} in that order. A consumer may inspect the displayed modulus only after the stream windows and regular readback have exposed the dyadic late-tail ledger, and it may pass to $\RealUp$ only through the same seal-facing route. The route exports no quotient stream equality, selected limit, countable-choice schedule, host real equality, or ambient $\RealUp$ completeness.
