\chapter{A Concrete Naming Certificate for $\LocatedCauchyRepresentationUp$}
\label{ch:concrete-instances-located-cauchy-representation-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis and the computable-analysis convention that real-facing objects are read through effective names, the $\LocatedCauchyRepresentationUp$ packet records the finite interface where a Cauchy representation also carries located Cauchy evidence before the terminal real seal is consumed. It sits after the Cauchy-representation boundary of \autoref{ch:concrete-instances-cauchy-representation-namecert}, the sequence-level located Cauchy packet of \autoref{ch:concrete-instances-located-cauchy-sequence-namecert}, the located real boundary of \autoref{ch:concrete-instances-locatedreal-namecert}, and the represented-space source surface of \autoref{ch:concrete-instances-representedspace-namecert}. The packet gives L10 Real and Completion consumers a single BEDC-native route through displayed finite rows; it does not import quotient streams, selected limits, countable choice, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[LocatedCauchyRepresentation carrier]
\label{def:located-cauchy-representation-carrier}
A $\LocatedCauchyRepresentationUp$ carrier is a finite $\BHist$ packet
$$
L=(R,Q,J,S,M,D,X,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\CauchyRepresentationUp$ decoder row, recording the finite representation route by which stream observations are decoded.
\item $Q$ is the $\LocatedCauchySequenceUp$ evidence row, recording the located tail witness attached to the decoded observations.
\item $J$ is the $\LocatedRealUp$ comparison-window row, exposing the finite interval and locatedness comparisons that the downstream real-facing consumer may inspect.
\item $S$ is the $\StreamNameUp$ finite-window row used by the decoder and by the located Cauchy evidence.
\item $M$ is the $\RegSeqRatUp$ readback row attached to the displayed stream windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row used for the regular readback, located tail, and located real comparison windows.
\item $X$ is the $\RepresentedSpaceUp$ exposure row, presenting the same finite name to represented-space consumers without adding a second representation relation.
\item $E$ is the terminal $\RealUp$ seal row, read only after $R,Q,J,S,M,D,X$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the decoder, located evidence, comparison windows, stream windows, readback, tolerances, represented-space exposure, real seal, continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from representation decoding through located Cauchy evidence, located real comparison, represented-space exposure, and the terminal seal handoff.
\item $P$ is the $\Pkg$ provenance over $\LocatedCauchyRepresentationUp$, $\CauchyRepresentationUp$, $\LocatedCauchySequenceUp$, $\LocatedRealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RepresentedSpaceUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedCauchyRepresentationUp}$ row.
\end{enumerate}
The carrier predicate asserts exactly that these rows form one finite located Cauchy representation packet. It has no coordinate for quotient streams, selected host limits, countable choice, trichotomy, host real equality, or ambient completeness.
\end{definition}

\begin{theorem}[LocatedCauchyRepresentation NameCert obligations]
\label{thm:located-cauchy-representation-namecert-obligations}
For every accepted $\LocatedCauchyRepresentationUp$ carrier $L=(R,Q,J,S,M,D,X,E,H,C,P,N)$, the local $\NameCert_{\LocatedCauchyRepresentationUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the decoder, located Cauchy evidence, located real comparison windows, stream windows, regular rational readback, dyadic tolerance ledger, represented-space exposure, terminal real seal, transport, replay, provenance, and local naming rows;
\item decoder exactness, requiring every representation read to pass through the $\CauchyRepresentationUp$ row $R$ before any located evidence is inspected;
\item located sequence exactness, requiring the row $Q$ to expose only the located Cauchy tail witnesses attached to the displayed decoder and stream-window route;
\item comparison-window exactness, requiring the $\LocatedRealUp$ row $J$ to read its finite location windows through the same regular rational and dyadic rows $M,D$;
\item represented-space compatibility, requiring $X$ to expose the same finite name and target route rather than a separate representation relation;
\item real-seal ordering, requiring $E$ to remain downstream of $R,Q,J,S,M,D,X$ and never to serve as a selected host limit;
\item componentwise transport, continuation replay, package provenance, and local naming for exactly the displayed rows $H,C,P,N$;
\item refusal of quotient streams, selected limits, countable choice, trichotomy, host real equality, and ambient completeness as local certificate data.
\end{enumerate}
No located Cauchy representation obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the accepted carrier. The visible analytic route is the representation decoder $R$, located Cauchy evidence $Q$, located real comparison windows $J$, stream windows $S$, regular rational readback $M$, dyadic tolerance ledger $D$, represented-space exposure $X$, and terminal seal $E$.

Decoder exactness comes first. A representation-facing consumer reaches located evidence only after reading the $\CauchyRepresentationUp$ row, so the packet cannot be reread as an unbounded stream or a selected limit.

The located rows are tied to the same finite route. The row $Q$ supplies located tail evidence over the displayed stream windows, while $J$ supplies located real comparison windows over the same regular rational readback and dyadic tolerance ledger.

The represented-space and seal rows add no independent analytic coordinate. The row $X$ exposes the same name to represented-space consumers, and $E$ is downstream of the decoder, located evidence, comparison windows, readback, and tolerance rows.

Finally, $H,C,P,N$ transport, replay, source, and name the displayed packet without adding data. Since the carrier has no coordinate for quotient streams, selected limits, countable choice, trichotomy, host real equality, or ambient completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[LocatedCauchyRepresentation Real-seal handoff]
\label{thm:located-cauchy-representation-real-seal-handoff}
Let $L=(R,Q,J,S,M,D,X,E,H,C,P,N)$ be an accepted $\LocatedCauchyRepresentationUp$ carrier. Every $\RealUp$-facing or $\RepresentedSpaceUp$-facing read of $L$ factors through the finite handoff route
$$
R,Q,J,S,M,D,X \longmapsto E \longmapsto H,C,P,N .
$$
Thus the terminal real seal and the represented-space exposure are reached only through the Cauchy-representation decoder, located Cauchy evidence, located real comparison windows, finite stream windows, regular rational readback, and dyadic tolerance ledger. The route contains no quotient stream, selected host limit, countable-choice selector, trichotomy branch, host real equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Begin with a real-facing or represented-space-facing read. Carrier admission exposes the target rows $X$ and $E$ only as part of the same finite packet as the decoder, located evidence, comparison windows, stream windows, readback, and tolerances.

Replay the continuation row $C$. The read first consumes $R$, then the located evidence $Q$ and comparison windows $J$, and then the common stream-window, regular-readback, and dyadic-tolerance route $S,M,D$.

Only after that replay can $X$ and $E$ be inspected. The represented-space row offers the same finite name to its consumer, while the real-seal row is the terminal seal of that route rather than a host-selected limit.

The support rows $H,C,P,N$ preserve and name the same route. Since the packet contains none of the excluded coordinates, no consumer can bypass the displayed handoff to read quotient streams, selected limits, countable choice, trichotomy, host real equality, or ambient completeness.
\end{proof}

\begin{closurestatus}{\LocatedCauchyRepresentationUp}
  \constructivestory{A $\LocatedCauchyRepresentationUp$ packet is generated from a CauchyRepresentation decoder, LocatedCauchySequence evidence, LocatedReal comparison windows, StreamName finite windows, RegSeqRat readback, DyadicRatCore tolerances, RepresentedSpace exposure, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-cauchy-representation-carrier}, \autoref{thm:located-cauchy-representation-namecert-obligations}, and \autoref{thm:located-cauchy-representation-real-seal-handoff} seed the finite located Cauchy representation route from CauchyRepresentation and LocatedCauchySequence data to LocatedReal comparison windows, RepresentedSpace exposure, and a terminal Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient streams, selected host limits, countable choice, trichotomy, host real equality, ambient Real completeness, Lean verification, or a public standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, decoder exactness, located sequence exactness, comparison-window exactness, represented-space compatibility, Real seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.LocatedCauchyRepresentationUp.}
\end{closurestatus}
