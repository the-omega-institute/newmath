\chapter{A Concrete Naming Certificate for $\VariationDiminishingUp$}
\label{ch:concrete-instances-variation-diminishing-namecert}
\origin{human}

Following Schoenberg and Karlin, the $\VariationDiminishingUp$ packet records a finite analysis row for transformations that do not increase displayed sign variation. It consumes the total-variation function surface of \autoref{ch:concrete-instances-total-variation-function-namecert}, the bounded-variation real-function carrier of \autoref{ch:concrete-instances-bounded-variation-real-function-namecert}, the regulated-function source of \autoref{ch:concrete-instances-regulatedrealfunction-namecert}, and the tagged integration boundary of \autoref{ch:concrete-instances-riemannintegral-namecert}. The packet names the finite comparison by which a kernel or transformation row is read as variation-diminishing without importing a host operator algebra, total-positivity theorem, selected partitions, quotient functions, or ambient completeness of $\RealUp$.

\begin{definition}[Variation-diminishing carrier]
\label{def:variation-diminishing-carrier}
A $\VariationDiminishingUp$ carrier is a finite $\BHist$ packet
$$
V=(B,T,R,I,K,M,H,C,P,N)
$$
with the following rows:
\begin{enumerate}
\item $B$ is the $\BoundedVariationRealFunctionUp$ source row naming the accepted bounded-variation input surface.
\item $T$ is the $\TotalVariationFunctionUp$ row naming the displayed total-variation ledger used to count variation before and after the transformation.
\item $R$ is the $\RegulatedRealFunctionUp$ row naming the regulated readback through which input and output functions are compared by finite step windows.
\item $I$ is the $\RiemannIntegralUp$ row naming the finite tagged-mesh integration boundary used when the transformation is read by kernel action or integral averaging.
\item $K$ is the kernel or transformation row, including its displayed positivity-order or sign-regularity window.
\item $M$ is the variation comparison row recording that each accepted finite sign-change ledger for the output is routed to a no-larger sign-change ledger for the input.
\item $H$ records componentwise $\hsame$ transport for bounded-variation source, total-variation ledger, regulated readback, integral boundary, kernel row, comparison row, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a finite transformation request through $B,T,R,I,K,M,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\VariationDiminishingUp$, $\BoundedVariationRealFunctionUp$, $\TotalVariationFunctionUp$, $\RegulatedRealFunctionUp$, $\RiemannIntegralUp$, $\BHist$, $\hsame$, $\Cont$, and sibling $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\VariationDiminishingUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $B,T,R,I,K,M,H,C,P,N$. It has no coordinate for host function equality, quotient representatives, selected limiting partitions, a global total-positivity theorem, measure completion, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Variation-diminishing NameCert obligations]
\label{thm:variation-diminishing-namecert-obligations}
For every accepted $\VariationDiminishingUp$ carrier
$$
V=(B,T,R,I,K,M,H,C,P,N),
$$
the local $\NameCert_{\VariationDiminishingUp}$ surface consists exactly of carrier admission for the bounded-variation source row $B$, total-variation ledger $T$, regulated-function readback $R$, Riemann-integral boundary $I$, kernel or transformation row $K$, variation comparison row $M$, componentwise transport $H$, displayed replay $C$, provenance $P$, and local naming $N$.

The obligation surface requires every output variation read to pass through the displayed comparison row $M$ after $B,T,R,I,K$ have been projected. It refuses host operator equality, quotient equality of functions, selected partitions, unrestricted total positivity, measure-theoretic kernels, and ambient completeness of $\RealUp$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:variation-diminishing-carrier}. The source row $B$ supplies the bounded-variation input, while $T$ supplies the total-variation ledger on which finite sign-change comparisons can be read.

The rows $R$ and $I$ keep the analytic route finite. Regulated readback gives step-window access to input and output functions, and the Riemann-integral boundary records only the tagged-mesh action needed by a kernel or averaging read.

The row $K$ names the transformation window, and $M$ is the only row that compares variation before and after the transformation. Thus the NameCert consumer obtains a variation-diminishing assertion only through the displayed comparison row, not through a host theorem about all kernels.

The structural rows $H,C,P,N$ transport, replay, source, and name the same finite packet. Since no carrier coordinate stores quotient functions, selected limiting partitions, unrestricted total positivity, measure completion, or ambient real completeness, no local obligation can require them.
\end{proof}

\begin{theorem}[Variation-diminishing total-variation route]
\label{thm:variation-diminishing-total-variation-route}
Every accepted $\VariationDiminishingUp$ consumer read factors through the route
$$
\BoundedVariationRealFunctionUp,\ \TotalVariationFunctionUp,\ \RegulatedRealFunctionUp,\ \RiemannIntegralUp
\longmapsto
\VariationDiminishingUp .
$$
The output variation ledger is accepted only after the bounded-variation input, cumulative total-variation ledger, regulated readback, tagged integration boundary, kernel row, and comparison row have been displayed. The route exports no selected limit, quotient function equality, global total-positivity principle, measure-completion object, or ambient $\RealUp$ completeness assertion.
\end{theorem}
\begin{proof}
A variation-diminishing request first projects $B$ and $T$. These rows bind the input function to the bounded-variation and total-variation sibling surfaces, so the requested comparison starts from finite variation data rather than from a host function space.

Next project $R$ and $I$. The regulated row supplies finite step-window readback, while the integral boundary supplies the tagged-mesh route through which a kernel or averaging transformation can be inspected.

The transformation row $K$ and comparison row $M$ then give the only accepted variation comparison. They state that the displayed output sign-change ledger routes to a no-larger input ledger in the same finite packet.

Finally $H,C,P,N$ preserve the route under transport, replay, provenance, and local naming. Hence every public read of $\VariationDiminishingUp$ passes through the four cited sibling anchors and does not acquire the excluded host-level principles.
\end{proof}

\falsifiablePrediction{A $\VariationDiminishingUp$ packet whose output variation read bypasses the displayed total-variation ledger or the regulated readback row is not accepted by the local NameCert.}

\independenceWitness{total-variation-function, bounded-variation-real-function, regulatedrealfunction, riemannintegral: $\VariationDiminishingUp$ is not bijective to any listed sibling because it binds the source variation ledger, regulated readback, tagged integration boundary, transformation row, and no-increase comparison as one finite packet.}

\closureat{VariationDiminishingUp}{seedStr}

\begin{closurestatus}{\VariationDiminishingUp}
  \constructivestory{A $\VariationDiminishingUp$ packet is generated from bounded-variation source data, a total-variation ledger, regulated readback, Riemann tagged-mesh boundary, a kernel or transformation row, a variation comparison row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:variation-diminishing-carrier}, \autoref{thm:variation-diminishing-namecert-obligations}, \autoref{thm:variation-diminishing-total-variation-route}, the falsifiable prediction, and the independence witness seed the finite variation-diminishing route over the current analysis rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close host operator equality, quotient equality of functions, selected limiting partitions, unrestricted total positivity, measure completion, Lean verification, bridge checking, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, bounded-variation source visibility, total-variation ledger exactness, regulated readback, tagged integration boundary, transformation-row exposure, comparison-row stability, transport, replay, provenance, and local naming for future BEDC.Derived.VariationDiminishingUp.}
\end{closurestatus}
