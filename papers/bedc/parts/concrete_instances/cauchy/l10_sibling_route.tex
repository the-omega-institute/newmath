\begin{theorem}[Cauchy L10 sibling route]\label{thm:cauchy-orphan-real-route}
The Cauchy packet family exported by this hub is consumed through the L10 route
$$
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \RealUp \longmapsto \MetricCompletionUp .
$$
Concretely, the finite-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal real seal of \autoref{ch:concrete-instances-real-namecert}, and the completion boundary of \autoref{ch:concrete-instances-metriccompletion-namecert} are sibling inputs to the Cauchy carriers rather than hidden ambient structure.
\end{theorem}
\begin{proof}
Read any Cauchy carrier exported by the hub through its finite observation rows. The stream coordinate supplies the inspected windows, the regular-rational coordinate supplies the numerical readback, and the dyadic coordinate supplies the tolerance budget.

The Real-facing seal is reached only after those rows have been displayed. When a completion consumer is present, it receives the same displayed route through the metric-completion boundary.

Thus the hub's Cauchy packet family is attached to the Real and MetricCompletion siblings by visible L10 rows, with no quotient representative, selected limit, countable-choice schedule, host real equality, or ambient completeness coordinate added by the route.
\end{proof}
