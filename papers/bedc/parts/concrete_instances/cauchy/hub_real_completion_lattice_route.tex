\begin{theorem}[Cauchy hub Real-completion lattice route]
\label{thm:cauchy-hub-real-completion-lattice-route}
Every public $\CauchyUp$ route consumed by a Real-completion chapter factors through the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert} before completion consumers read it. The hub exports only the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows attached to that lattice; it exports no quotient stream, selected limit, countable-choice schedule, host real equality, or ambient $\RealUp$ completeness theorem.
\end{theorem}
\begin{proof}
Start from the hub input order for $\CauchyUp$. The root sequence and stream obligation files expose finite observation windows and the local NameCert route before any completion-facing sibling can consume the packet.

Follow the Real-completion lattice through the named sibling chapters. A consumer first reads the finite $\StreamNameUp$ window, then the $\RegSeqRatUp$ regular rational row, then the $\DyadicRatCoreUp$ tolerance ledger, and finally the $\RealUp$ seal that is visible to completion packets.

The carrier rows available at the hub are exactly the displayed $\BHist$ route data, componentwise $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local naming rows. Since none of those rows stores quotient streams, selected limits, choice schedules, host real equality, or ambient Real completeness, every completion-facing read factors through the finite lattice stated above.
\end{proof}
