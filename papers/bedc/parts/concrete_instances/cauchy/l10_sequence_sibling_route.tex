The $\CauchyUp$ hub exposes its sequence-facing child through the same L10 naming lattice used by Real and Completion consumers. A Cauchy sequence read is not a detached stream fact: it is a finite route through the StreamName windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerances of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}.

\begin{theorem}[Cauchy sequence RegSeqRat window route]
\label{thm:cauchy-sequence-regseqrat-window-route}
Every $\CauchyUp$ sequence read used by a $\RealUp$ or Completion consumer factors through a displayed $\StreamNameUp$ finite-window row, a $\RegSeqRatUp$ readback row, a $\DyadicRatCoreUp$ tolerance ledger, and the $\RealUp$ seal before it can be consumed as a Real-facing or completion-facing boundary. The route exports no quotient stream equality, selected limit, countable-choice schedule, host real equality, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Read the sequence child as the visible Cauchy-facing row of the hub. A consumer first inspects the finite StreamName window, then reads the same data as a regular rational row, and then checks the requested tolerance through the dyadic ledger.

Only after those displayed rows are present can the terminal Real seal be consumed. Since the route contains no quotient coordinate, selected endpoint, choice schedule, host equality row, or ambient completeness row, the consumer receives exactly the finite L10 window route.
\end{proof}
