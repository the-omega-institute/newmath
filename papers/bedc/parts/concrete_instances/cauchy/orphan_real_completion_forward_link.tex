\begin{theorem}[Cauchy Real completion lattice repair]
\label{thm:cauchy-orphan-real-completion-lattice-repair}
Every completion-facing $\CauchyUp$ read that reaches the Real and Completion lattice factors through finite $\StreamNameUp$ windows from \autoref{ch:concrete-instances-streamname-namecert}, $\RegSeqRatUp$ regular rational readback from \autoref{ch:concrete-instances-regseqrat-namecert}, $\DyadicRatCoreUp$ tolerance rows from \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}, and the $\CauchyFilterCompletionUp$ consumer rows of \autoref{ch:concrete-instances-cauchyfiltercompletion-namecert} before any completion-facing conclusion is exposed. The route carries the same finite $\BHist$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows as the root sequence packet, and it exports no quotient streams, selected limits, countable choice, host equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the existing $\CauchyUp$ root sequence surface. Its public rows expose finite sequence observations and structural $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ data before any downstream consumer can read a completion-facing claim.

Follow the named sibling route. The observation is first read as a $\StreamNameUp$ finite window, then as a $\RegSeqRatUp$ regular rational readback, then through $\DyadicRatCoreUp$ tolerance rows, and finally through the terminal $\RealUp$ seal.

Only after those rows are visible may the consumer enter the $\CauchyFilterCompletionUp$ surface. Since no row in the route stores quotient stream equality, selected limits, countable choice, host equality, or ambient Real completeness, the completion-facing conclusion is exactly the finite lattice read described above.
\end{proof}
