The $\CauchyUp$ root sequence surface is consumed by the Real and Completion lattice only through the displayed sibling route: finite StreamName windows from \autoref{ch:concrete-instances-streamname-namecert}, regular rational readback from \autoref{ch:concrete-instances-regseqrat-namecert}, dyadic tolerances from \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}, and the existing Cauchy completion consumer route of \autoref{ch:concrete-instances-cauchyfiltercompletion-namecert}. The forward link records that completion-facing consumers receive the same finite $\BHist$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ rows as the root sequence packet, not quotient streams, selected limits, countable choice, host equality, or ambient $\RealUp$ completeness.
