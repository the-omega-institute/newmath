\begin{theorem}[Cauchy sequence RegSeqRat stream anchor]
\label{thm:cauchy-sequence-regseqrat-stream-anchor}
Every Cauchy sequence root read that feeds an L10 $\RealUp$ or completion consumer factors through finite $\StreamNameUp$ windows, $\RegSeqRatUp$ regular readback, $\DyadicRatCoreUp$ tolerances, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\CauchyUp}$ row before the $\RealUp$ seal is exposed.
\end{theorem}
\begin{proof}
Project the sequence root carrier through the hub child route. A consumer first receives finite window requests, not an infinite sequence object or a selected representative.

The regular rational readback is then interpreted through the displayed dyadic tolerance ledger. This places every tail request inside the $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ rows before any Real-facing or completion-facing seal can be consumed.

The structural rows preserve the same route. Componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row source and name the finite readback path, so no quotient stream equality, selected limit, countable choice, host equality, or ambient $\RealUp$ completeness row is exported.
\end{proof}
