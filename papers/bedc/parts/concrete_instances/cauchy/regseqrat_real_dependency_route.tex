The public $\CauchyUp$ route consumed by completion and real-readback arguments is routed through the same finite lattice: $\StreamNameUp$ windows from \autoref{ch:concrete-instances-streamname-namecert}, $\RegSeqRatUp$ rational readback from \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}, the net-facing completion surface of \autoref{ch:concrete-instances-cauchynetcompletion-namecert}, and the filter-facing completion surface of \autoref{ch:concrete-instances-cauchyfiltercompletion-namecert}. The hub exports this dependency route only as finite $\BHist$, componentwise $\hsame$, displayed $\Cont$, $\Pkg$, and local $\NameCert$ data; it exports no quotient stream equality, selected limit, countable-choice schedule, host real equality, or ambient completeness principle.
