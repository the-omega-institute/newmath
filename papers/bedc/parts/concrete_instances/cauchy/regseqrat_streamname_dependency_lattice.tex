The $\CauchyUp$ carrier reads regular rational tails through \autoref{ch:concrete-instances-regseqrat-namecert} and finite observation schedules through \autoref{ch:concrete-instances-streamname-namecert}. The tolerance rows supplied by \autoref{ch:concrete-instances-dyadicratcore-namecert} and the terminal sealing of \autoref{ch:concrete-instances-real-namecert} remain downstream of that lattice, so the hub exposes the RegSeqRat and StreamName dependencies before completion-facing consumers inspect the Real boundary.
