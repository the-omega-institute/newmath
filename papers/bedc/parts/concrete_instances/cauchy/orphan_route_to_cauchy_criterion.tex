\begin{theorem}[Cauchy route to CauchyCriterion]
\label{thm:cauchy-orphan-route-to-cauchy-criterion}
The $\CauchyUp$ hub's finite route is consumed by \autoref{ch:concrete-instances-cauchycriterion-namecert} through the displayed $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. The route exports no detached completion carrier, quotient sequence equality, selected modulus outside the displayed rows, countable-choice schedule, or host completeness principle.
\end{theorem}
\begin{proof}
Read the hub through its existing finite siblings. The $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, and $\DyadicRatCoreUp$ tolerance rows provide the only Cauchy data visible to downstream criteria.

The structural rows $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ transport, replay, source, and name that same finite route. Therefore the Cauchy criterion consumer receives exactly the displayed BEDC rows and no external completion or choice object.
\end{proof}
