The $\CauchyUp$ dossier also routes finite Cauchy source data into the L10 completion interface: its displayed request windows and modulus rows feed the $\StreamNameUp$ surface of \autoref{ch:concrete-instances-streamname-namecert}, its rational readback is consumed by \autoref{ch:concrete-instances-regseqrat-namecert}, and its terminal seal-facing row reaches \autoref{ch:concrete-instances-real-namecert} through the finite section packet of \autoref{ch:concrete-instances-finite-section-completion-interface-namecert}. Thus $\CauchyUp$ supplies the finite Cauchy source row consumed by the L10 completion route without asserting quotient stream equality, selected limits, countable choice, host real equality, or ambient $\RealUp$ completeness.
