\section{StreamName Readback Route}
\label{sec:cauchy-orphan-streamname-route}

The $\CauchyUp$ completion hub has a substantive L10 route through the finite-window sibling lattice rather than only through the kernel sequence carrier. A completion consumer that reads a Cauchy packet must first expose $\StreamNameUp$ windows as in \autoref{ch:concrete-instances-streamname-namecert}, replay the $\RegSeqRatUp$ rational readback, check the $\DyadicRatCoreUp$ tolerance rows, and then seal the resulting endpoint through $\RealUp$.

\begin{theorem}[Cauchy stream-name sibling route]
\label{thm:cauchy-orphan-streamname-route}
Every $\CauchyUp$ completion consumer that claims a Real-facing endpoint factors its read through $\StreamNameUp$ finite windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerances, and the terminal $\RealUp$ seal rows. The route exposes the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ coordinates used by the Cauchy hub, and it has no direct access to selected limits, quotient completion classes, ambient Real completeness, or countable choice.
\end{theorem}
\begin{proof}
Read the Cauchy hub through its existing sequence and completion children. The only windowed observation route begins with the $\StreamNameUp$ finite-window sibling and then replays the $\RegSeqRatUp$ rational readback against the $\DyadicRatCoreUp$ tolerance rows.

The endpoint seal is therefore downstream of the finite readback route. Once $\RealUp$ receives the sealed row, the structural coordinates $\BHist,\hsame,\Cont,\Pkg,\NameCert$ preserve that same finite route and provide no coordinate for selected limits, quotient classes, ambient completeness, or countable choice.
\end{proof}
