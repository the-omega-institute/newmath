\begin{theorem}[Cauchy hub Bishop completion forward route]
\label{thm:cauchy-orphan-bishop-completion-route}
The regular-source material routed by the $\CauchyUp$ hub is consumed by \autoref{ch:concrete-instances-bishop-regular-cauchy-completion-namecert} through the displayed $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. The forward route exports no detached Cauchy representative, quotient stream equality, selected limit, countable-choice schedule, or ambient real completeness principle.
\end{theorem}
\begin{proof}
Read the Cauchy hub through its regular-source children. Those children expose dyadic tolerance arithmetic, finite stream windows, regular rational readback, and the terminal real seal as $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows.

The Bishop regular Cauchy completion chapter consumes exactly that L10-facing route. Its completion read is mediated by the same finite $\BHist$ carrier discipline, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming.

Since the hub route contains no detached representative, quotient stream equality, selected limit, countable-choice schedule, or ambient real completeness coordinate, the forward edge to the Bishop completion packet is precisely the displayed finite dependency route.
\end{proof}
