\begin{theorem}[Cauchy L10 child dependency route]
\label{thm:cauchy-l10-child-dependency-route}
Every finite-window read exported by the $\CauchyUp$ hub is consumed by sibling L10 chapters only after it passes through the child routes displayed in this chapter. The regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}, and the completion-facing route of \autoref{ch:concrete-instances-cauchyfiltercompletion-namecert} read the $\CauchyUp$ packet through finite stream windows, dyadic tolerance rows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows. No child route exports quotient stream equality, selected limits, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the hub through its child files. The core carrier in \autoref{ch:concrete-instances-cauchy-namecert} displays the stream-window, dyadic tolerance, regular readback, transport, replay, provenance, and naming rows before any downstream consumer can inspect a Cauchy object.

The sibling dependency spine then sends those rows to $\RegSeqRatUp$ and $\RealUp$. The cited regular-rational and Real chapters consume only the displayed finite route; they do not receive a quotient stream, selected limit, choice schedule, trichotomy oracle, host equality coordinate, or ambient completeness proof.

The completion-facing consumer follows the same rows through the Cauchy-filter completion surface. Because the hub contains only child inputs and each child route exposes finite $\BHist$ and $\NameCert$ data, the L10 dependency path is connected through sibling chapters rather than through an unstated top-level object.
\end{proof}

The L10 route consumes $\CauchyUp$ through the finite-window surface of
\autoref{ch:concrete-instances-streamname-namecert}, the regular readback of
\autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance rows
of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal
seal rows of \autoref{ch:concrete-instances-real-namecert}. These four child
dependencies keep the Cauchy packet attached to finite stream windows,
regular rational evidence, dyadic budgets, and the Real seal before any L10
consumer may read it.
