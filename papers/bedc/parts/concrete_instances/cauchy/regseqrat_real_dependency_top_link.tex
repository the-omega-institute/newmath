\begin{theorem}[Cauchy RegSeqRat Real dependency top link]
\label{thm:cauchy-regseqrat-real-dependency-route}
Every completion-facing $\CauchyUp$ consumer must expose the regular-source row of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite $\StreamNameUp$ windows, the $\DyadicRatCoreUp$ tolerance ledger, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert} before it can read a completion endpoint. The route remains inside componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert_{\CauchyUp}$ row.
\end{theorem}
\begin{proof}
Read the top-level $\CauchyUp$ hub through its displayed children. The hub exports only ordered sibling routes, so a completion-facing consumer first encounters finite observation windows and dyadic tolerance data rather than a hidden completion endpoint.

The regular-source handoff is then forced through the $\RegSeqRatUp$ row named by \autoref{ch:concrete-instances-regseqrat-namecert}. Only after that handoff is visible can the consumer read the terminal $\RealUp$ seal named by \autoref{ch:concrete-instances-real-namecert}.

The structural rows preserve the same path. Componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row keep the completion read inside the finite packet, with no quotient stream, selected limit, countable-choice schedule, host equality, or ambient $\RealUp$ completeness coordinate.
\end{proof}
