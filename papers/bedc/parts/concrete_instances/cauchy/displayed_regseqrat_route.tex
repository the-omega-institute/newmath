The $\CauchyUp$ hub exposes a displayed Real and Completion route only after finite stream windows and regular rational readback rows have been made visible. This child route records the dependency on \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert} without adding substantive prose to the hub.

\begin{theorem}[Cauchy displayed RegSeqRat route]
\label{thm:cauchy-displayed-regseqrat-route}
Any $\CauchyUp$ route used by the L10 $\RealUp$ or $\CompletionUp$ surface must pass through finite $\StreamNameUp$ windows and $\RegSeqRatUp$ readback rows before a $\RealUp$ seal is consumed. The route carries only displayed $\BHist$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows, and it exports no quotient stream equality, selected limit, countable-choice schedule, host real equality, or ambient completeness principle.
\end{theorem}
\begin{proof}
Begin with a Real- or Completion-facing Cauchy read. The finite window surface of \autoref{ch:concrete-instances-streamname-namecert} is the first visible observation boundary, so the consumer cannot treat the Cauchy source as an uninspected host sequence.

Read those windows through \autoref{ch:concrete-instances-regseqrat-namecert}. The regular rational readback row supplies the displayed tail data required before any terminal seal can be used.

Finally consume the terminal row of \autoref{ch:concrete-instances-real-namecert}. The structural rows transport, replay, source, and name the same finite route, so the Cauchy handoff reaches the L10 Real and Completion surface only after StreamName windows and RegSeqRat readback have been displayed.
\end{proof}
