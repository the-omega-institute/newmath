\begin{theorem}[Cauchy RegSeqRat and Real sibling lattice repair]
\label{thm:cauchy-regseqrat-real-sibling-lattice-repair}
The $\CauchyUp$ route used by this hub depends on the $\RegSeqRatUp$ sibling for regular rational windows and on the $\RealUp$ sibling for the terminal seal boundary. Thus every completion-facing consumer first reads the regular rational dependency of \autoref{ch:concrete-instances-regseqrat-namecert} and then the Real seal of \autoref{ch:concrete-instances-real-namecert}; the hub exports no quotient stream, selected limit, countable-choice schedule, or ambient $\RealUp$ completeness row of its own.
\end{theorem}
\begin{proof}
Use the displayed RegSeqRat dependency files already routed through the hub. They force the Cauchy read through regular rational windows before any completion-facing row can be interpreted.

Then apply the terminal Real sibling route named by \autoref{ch:concrete-instances-real-namecert}. The seal boundary is therefore read as a sibling obligation after RegSeqRat evidence, not as hidden content of the top-level hub.
\end{proof}
