The $\CauchyUp$ hub also exposes the forward lattice used by regular-real consumers: the displayed Cauchy source is first read as $\RegSeqRatUp$ data and then handed to the $\RegularCauchyRealUp$ packet before any terminal real-facing consumer treats it as a completed object.

\begin{theorem}[Cauchy RegSeqRat to RegularCauchyReal sibling route]
\label{thm:cauchy-regseqrat-sibling-route}
Every accepted $\CauchyUp$ route consumed by a regular-real boundary must pass through the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert} before it reaches the $\RegularCauchyRealUp$ carrier of \autoref{ch:concrete-instances-regular-cauchy-real-namecert}. The route exports a displayed L10 handoff through finite windows, regular readback, transport, replay, provenance, and local naming; it exports no detached Cauchy representative, quotient stream equality, selected limit, countable-choice schedule, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Read the hub through its existing regular-source children. Those children expose the Cauchy data as finite observation windows and regular rational rows before any real-facing seal can be consumed.

The $\RegSeqRatUp$ row is then the source read by the $\RegularCauchyRealUp$ carrier. Its sibling chapter adds the StreamName, dyadic tolerance, convergence handoff, Real seal, transport, replay, provenance, and local naming rows around the same regular readback.

Since neither route contains a detached representative, quotient stream equality, selected limit, countable-choice schedule, or ambient completeness coordinate, the handoff is exactly the displayed finite L10 lattice.
\end{proof}
