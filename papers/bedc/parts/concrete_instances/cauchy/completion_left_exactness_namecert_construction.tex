\chapter{A Concrete Naming Certificate for $\CauchyCompletionLeftExactnessUp$}
\label{ch:concrete-instances-cauchy-completion-left-exactness-namecert}
\origin{human}

Following Bishop and Bridges on constructive completion and the finite-limit language of regular category theory, the $\CauchyCompletionLeftExactnessUp$ packet records the boundary where a displayed Cauchy-completion route preserves the finite-limit rows that its source already exposes. It sits beside the Cauchy source route of \autoref{ch:concrete-instances-cauchy-namecert}, the metric-completion universal-property route of \autoref{ch:concrete-instances-metric-completion-universal-property-namecert}, the functorial completion route of \autoref{ch:concrete-instances-cauchycompletionfunctor-namecert}, the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only a finite left-exactness boundary for displayed completion data; it does not assert quotient completions, selected limits, countable choice, host equality, or ambient $\RealUp$ completeness.

\begin{definition}[Cauchy completion left-exactness carrier]
\label{def:cauchy-completion-left-exactness-carrier}
A $\CauchyCompletionLeftExactnessUp$ carrier is a finite $\BHist$ packet
$$
L=(S,U,D,K,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source finite-limit row, recording the displayed terminal, product, equalizer, or kernel-pair data available before completion is read.
\item $U$ is the completion unit row, recording the source-to-completion unit reads for every object named by $S$.
\item $D$ is the dense image row, keeping each completed finite-limit comparison tied to the same source rows and unit rows.
\item $K$ is the kernel-pair preservation row, recording that equalizer and pullback comparisons are read through the displayed source kernel-pair data rather than through host quotient equality.
\item $E$ is the extension ledger, recording the universal-property and functorial completion rows used to transport finite-limit witnesses into completion-facing reads.
\item $H$ records componentwise $\hsame$ transport for $S,U,D,K,E,C,P,N$.
\item $C$ records displayed $\Cont$ replay from a finite-limit request through $S,U,D,K,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CauchyCompletionLeftExactnessUp$, $\CauchyUp$, $\MetricCompletionUp$, $\CauchyCompletionFunctorUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyCompletionLeftExactnessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for quotient completion equality, selected representatives, selected finite limits, countable choice, host equality of completed objects, or ambient $\RealUp$ completeness.
\end{definition}
\leanchecked{BEDC.Derived.CauchyCompletionLeftExactnessUp.TasteGate.CauchyCompletionLeftExactnessTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Cauchy completion left-exactness NameCert obligations]
\label{thm:cauchy-completion-left-exactness-namecert-obligations}
For every accepted $\CauchyCompletionLeftExactnessUp$ carrier
$$
L=(S,U,D,K,E,H,C,P,N),
$$
the local $\NameCert_{\CauchyCompletionLeftExactnessUp}$ surface consists exactly of carrier admission for the source finite-limit row, completion unit rows, dense image rows, kernel-pair preservation row, extension ledger, componentwise $\hsame$ transport, displayed $\Cont$ replay, provenance, and local naming. Every completion-facing finite-limit read must expose $S,U,D,K$ before $E,H,C,P,N$ can be consumed.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyUp.CauchyCompletionLeftExactnessNameCertObligations}
\begin{proof}
Project the carrier of \autoref{def:cauchy-completion-left-exactness-carrier}. The source finite-limit row, unit row, dense image row, kernel-pair row, extension ledger, transport, replay, provenance, and local name are exactly the displayed coordinates.

The finite-limit boundary is read through $S,U,D,K$. The source row supplies the terminal, product, equalizer, or kernel-pair data; the unit row embeds the displayed source names into completion-facing reads; the dense image row keeps those reads tied to source data; and the kernel-pair row confines exactness comparisons to displayed finite rows.

The remaining rows $E,H,C,P,N$ transport and name the same route. Since quotient completion equality, selected representatives, selected finite limits, countable choice, host equality, and ambient completeness have no carrier coordinate, projection of the displayed rows exhausts the NameCert obligations.
\end{proof}

\begin{theorem}[Cauchy completion left-exactness carrier admission]
\label{thm:cauchy-completion-left-exactness-carrier-admission}
For every accepted $\CauchyCompletionLeftExactnessUp$ carrier
$$
L=(S,U,D,K,E,H,C,P,N),
$$
the source finite-limit row $S$, completion unit row $U$, dense image row $D$, kernel-pair preservation row $K$, extension ledger $E$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, provenance row $P$, and local $\NameCert$ row $N$ are all admitted as finite coordinates of the same packet. No completion-facing read may treat one of these rows as ambient host data detached from the carrier.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyUp.CauchyCompletionLeftExactness\_carrier\_admission}
\begin{proof}
Project the carrier of \autoref{def:cauchy-completion-left-exactness-carrier}. Its coordinates are exactly $S,U,D,K,E,H,C,P,N$, so each named row is admitted by carrier membership.

Apply \autoref{thm:cauchy-completion-left-exactness-namecert-obligations}. The obligation surface requires every completion-facing finite-limit read to expose $S,U,D,K$ before the extension, transport, replay, provenance, and naming rows can be consumed.

Since quotient completion equality, selected representatives, selected finite limits, countable choice, host equality, and ambient $\RealUp$ completeness have no carrier coordinate, none of the admitted rows can be reread as detached host data.
\end{proof}

\begin{theorem}[Cauchy completion left-exactness unit-density route]
\label{thm:cauchy-completion-left-exactness-unit-density-route}
Every displayed finite-limit comparison read through an accepted $\CauchyCompletionLeftExactnessUp$ carrier factors first through the source finite-limit row $S$, then through the completion unit row $U$, and then through the dense image row $D$ before the extension ledger $E$ and structural rows $H,C,P,N$ are inspected. This route records the finite source-to-completion passage without selecting representatives of completed objects or invoking countable choice.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyUp.CauchyCompletionLeftExactnessUnitDensityRoute}
\begin{proof}
Begin with the carrier admission theorem \autoref{thm:cauchy-completion-left-exactness-carrier-admission}. It keeps $S,U,D$ as visible coordinates of the same finite packet.

The source row $S$ supplies the finite-limit data before completion is read. The unit row $U$ records the displayed source-to-completion unit for each object named by $S$, and the dense image row $D$ keeps the resulting completion-facing comparison tied back to the same source and unit rows.

Only after this route is exposed can $E,H,C,P,N$ be consumed. The extension ledger and the structural rows transport, replay, package, and name the source-unit-density passage; they do not add selected representatives, countable choice, or host equality of completed objects.
\end{proof}

\begin{theorem}[Cauchy completion left-exactness kernel-pair preservation]
\label{thm:cauchy-completion-left-exactness-kernel-pair-preservation}
For every accepted $\CauchyCompletionLeftExactnessUp$ carrier $L=(S,U,D,K,E,H,C,P,N)$, any equalizer, pullback, or kernel-pair comparison visible after completion is read through the displayed kernel-pair row $K$ together with the source finite-limit row $S$, completion unit row $U$, and dense image row $D$. The comparison cannot be exported as quotient completion equality, a selected finite limit, or ambient equality of completed objects.
\end{theorem}
\begin{proof}
Project the kernel-pair coordinate $K$ from the carrier. By definition, $K$ records equalizer and pullback comparisons through displayed source kernel-pair data rather than through a host quotient.

Use the unit-density route theorem \autoref{thm:cauchy-completion-left-exactness-unit-density-route}. It places every completion-facing finite-limit comparison behind $S,U,D$, so the kernel-pair row is read as part of the same source-to-completion passage.

Finally apply \autoref{thm:cauchy-completion-left-exactness-namecert-obligations}. The rows $E,H,C,P,N$ only extend, transport, replay, package, and name the displayed comparison. They add no coordinate for quotient completion equality, selected finite limits, or ambient equality of completed objects, so preservation remains confined to $K$ and its source, unit, and dense-image context.
\end{proof}
\leanchecked{BEDC.Derived.CauchyUp.CauchyCompletionLeftExactnessKernelPairPreservation}

\begin{theorem}[Cauchy completion left-exactness regular-limit pullback]
\label{thm:cauchy-completion-left-exactness-regular-limit-pullback}
For every accepted $\CauchyCompletionLeftExactnessUp$ carrier $L=(S,U,D,K,E,H,C,P,N)$, any regular Cauchy limit read used by a finite-limit comparison factors through the source finite-limit row $S$, the completion unit row $U$, the dense image row $D$, the kernel-pair row $K$, the $\RegSeqRatUp$ readback surface, and the terminal $\RealUp$ seal before the extension ledger $E$ is consumed. Thus the regular-limit comparison is a pullback of displayed source, unit, dense-image, kernel-pair, regular-readback, and real-seal rows, not a quotient completion or a selected representative of a completed object.
\end{theorem}
\begin{proof}
Project the accepted carrier. The rows $S,U,D,K,E,H,C,P,N$ are the only admitted coordinates, so a finite-limit comparison cannot begin from a hidden completed object or a host-level limit.

Apply \autoref{thm:cauchy-completion-left-exactness-unit-density-route}. It places every completion-facing finite-limit comparison behind the source finite-limit row, the completion unit row, and the dense image row before the extension ledger and structural rows are inspected.

Next apply \autoref{thm:cauchy-completion-left-exactness-kernel-pair-preservation}. Equalizer, pullback, and kernel-pair comparisons remain confined to the displayed kernel-pair row together with the same source, unit, and dense-image context.

The regular Cauchy limit read is then exposed through the sibling $\RegSeqRatUp$ readback surface named in the chapter introduction, and the resulting endpoint is sealed only through the sibling $\RealUp$ boundary. Since those sibling rows are read after $S,U,D,K$ and before $E$, the comparison has the announced displayed pullback route. No coordinate remains for quotient completion equality, selected representatives, selected finite limits, or ambient $\RealUp$ completeness.
\end{proof}
\leanchecked{BEDC.Derived.CauchyUp.CauchyCompletionLeftExactnessRegularLimitPullback}

\begin{theorem}[Cauchy completion left-exactness RegSeqRat unit pullback]
\label{thm:cauchy-completion-left-exactness-regseqrat-unit-pullback}
For every accepted $\CauchyCompletionLeftExactnessUp$ carrier $L=(S,U,D,K,E,H,C,P,N)$, any finite-limit comparison whose completion unit route reaches a $\RegSeqRatUp$ readback factors first through the source finite-limit row $S$, completion unit row $U$, dense image row $D$, and kernel-pair row $K$. It then reads the $\RegSeqRatUp$ surface and terminal $\RealUp$ seal before the extension ledger and structural rows $E,H,C,P,N$ are consumed. Componentwise $\hsame$ transport preserves that order, so the exported comparison contains no quotient completion, selected representative, selected finite limit, host equality, countable-choice schedule, or ambient $\RealUp$ completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchyUp.CauchyCompletionLeftExactnessRegSeqRatUnitPullback}
\begin{proof}
Start with the carrier admission theorem \autoref{thm:cauchy-completion-left-exactness-carrier-admission}. It keeps $S,U,D,K,E,H,C,P,N$ as finite coordinates of one accepted packet, so the comparison cannot begin from an external completed object.

Apply the unit-density route theorem \autoref{thm:cauchy-completion-left-exactness-unit-density-route}. This places every completion-facing finite-limit comparison behind $S,U,D$ before the extension ledger and structural rows are inspected.

Next apply the kernel-pair preservation theorem \autoref{thm:cauchy-completion-left-exactness-kernel-pair-preservation}. Pullback and equalizer comparisons remain confined to $K$ together with the same source, unit, and dense-image context.

Finally use the regular-limit pullback theorem \autoref{thm:cauchy-completion-left-exactness-regular-limit-pullback}. Its readback route inserts the $\RegSeqRatUp$ surface and the terminal $\RealUp$ seal after $S,U,D,K$ and before $E,H,C,P,N$. Componentwise $\hsame$ transport therefore preserves the displayed order, while the excluded quotient, selection, choice, host-equality, and ambient-completeness rows remain absent from the carrier.
\end{proof}

\closureat{CauchyCompletionLeftExactnessUp}{seedStr}

\begin{closurestatus}{\CauchyCompletionLeftExactnessUp}
  \constructivestory{A $\CauchyCompletionLeftExactnessUp$ packet is generated from source finite-limit rows, completion unit rows, dense image rows, kernel-pair preservation, an extension ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-completion-left-exactness-carrier} and \autoref{thm:cauchy-completion-left-exactness-namecert-obligations} seed the finite left-exactness boundary for displayed Bishop-style Cauchy completion.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completions, selected finite limits, selected representatives, countable choice, host equality of completed objects, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, source finite-limit exposure, unit-row exposure, dense image confinement, kernel-pair preservation, extension-ledger confinement, transport, replay, provenance, and local naming for BEDC.Derived.CauchyCompletionLeftExactnessUp.}
\end{closurestatus}
