\begin{theorem}[Cauchy filter-completion dependency route]
\label{thm:cauchy-orphan-filter-completion-dependency-route}
Every $\CauchyUp$ read used by completion-facing consumers is routed through the $\CauchyFilterCompletionUp$ packet only after the $\CauchyFilterUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows are displayed. The route begins with finite Cauchy sequence evidence, opens the Cauchy-filter basis, records the inspected stream windows, reads the regular rational data, accounts for dyadic tolerances, and then seals the terminal Real row before completion data is consumed. It exports no quotient stream equality, quotient filter, selected limit, countable-choice schedule, host real equality, trichotomy, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Read the $\CauchyUp$ hub through its sequence and regular-source children. Those children display the finite stream windows and regular rational readback before any completion-facing row is available.

The $\CauchyFilterUp$ basis is then exposed as a filter route over the same finite evidence. The $\DyadicRatCoreUp$ tolerance rows and the terminal $\RealUp$ seal record the numerical part of the route before the $\CauchyFilterCompletionUp$ packet can consume it.

The structural rows preserve the same finite handoff through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ data. Since the hub provides no quotient stream equality, quotient filter, selected limit, countable-choice schedule, host real equality, trichotomy, or ambient completeness coordinate, the completion dependency is exactly the displayed route.
\end{proof}
