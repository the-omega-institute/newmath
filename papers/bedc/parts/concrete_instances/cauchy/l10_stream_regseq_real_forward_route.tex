The $\CauchyUp$ rows are consumed downstream by \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The route opens only finite stream windows, regular Cauchy tail budgets, and terminal $\RealUp$ seal rows, so a Cauchy packet can feed the L10 real-completion argument without adding content to the hub file.
