The $\CauchyUp$ hub supplies a forward L10 route to \autoref{ch:concrete-instances-cauchyfiltercompletion-namecert}: finite sequence evidence is first read through $\StreamNameUp$ windows, then through $\RegSeqRatUp$ regular-rational readback, then through $\DyadicRatCoreUp$ tolerance rows, and finally through the terminal $\RealUp$ seal before the $\CauchyFilterCompletionUp$ packet consumes completion data. This route records dependency flow only; it exports no quotient stream equality, selected filter limit, countable-choice schedule, host real equality, trichotomy, or ambient completeness coordinate.
