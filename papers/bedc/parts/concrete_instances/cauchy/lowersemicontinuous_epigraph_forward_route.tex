The $\CauchyUp$ hub supplies the finite $\StreamNameUp$ and $\RegSeqRatUp$ readback rows consumed by \autoref{ch:concrete-instances-lowersemicontinuous-namecert} through its epigraph Real-export route. The forward edge reads only synchronized stream windows, regular rational tail budgets, terminal $\RealUp$ seal data, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows; it does not add a quotient stream equality, selected limit, countable choice row, host lower-semicontinuity predicate, or ambient $\RealUp$ completeness principle.
