Every completion-facing $\CauchyUp$ read first exposes the regular rational row of \autoref{ch:concrete-instances-regseqrat-namecert}, then passes the same finite-window and tolerance data to the $\MetricCompletionUp$ boundary of \autoref{ch:concrete-instances-metriccompletion-namecert}, and only afterward reaches the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. Thus the visible dependency route is $\CauchyUp \to \RegSeqRatUp \to \MetricCompletionUp \to \RealUp$, not a self-contained hub read, quotient stream, selected limit, or ambient completeness principle.
