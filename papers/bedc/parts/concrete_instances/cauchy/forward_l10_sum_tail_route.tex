\begin{theorem}[Cauchy forward L10 sum-tail route]
\label{thm:cauchy-base-forward-l10-route}
Every $\CauchyUp$ consumer that reaches the regular sum tail-estimate surface must first pass through the regular rational source of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}, and the finite tail-estimate packet of \autoref{ch:concrete-instances-regular-cauchy-sum-tail-estimate-namecert}. The route is an L10 dependency path through displayed StreamName, DyadicRatCore, RegSeqRat, RegularCauchySum, Real, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows; it exports no quotient stream equality, selected limit, countable-choice schedule, host real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Read the $\CauchyUp$ hub through its child routes. The core carrier exposes finite StreamName windows, DyadicRatCore tolerances, RegSeqRat readback, and the terminal Real-facing seal before any downstream completion or arithmetic consumer is available.

The regular sum tail-estimate chapter consumes exactly that surface twice, once for each regular source row. Its tail-estimate ledger then adds the RegularCauchySum row and finite error budget without creating a detached limit object.

Thus the forward L10 route from the Cauchy hub to the sum-tail packet is the displayed dependency path through RegSeqRat, Real, and the finite tail-estimate surface. Since each row is already a finite NameCert coordinate, no excluded host or choice principle can be read from the hub.
\end{proof}

\begin{theorem}[Cauchy forward L10 sum-tail route nonescape]
\label{thm:cauchy-forward-l10-sum-tail-route-nonescape}
A consumer of the $\CauchyUp$ forward L10 sum-tail route of \autoref{thm:cauchy-base-forward-l10-route} cannot read quotient stream equality, selected limits, countable-choice schedules, host real equality, or ambient $\RealUp$ completeness. Every exported row is confined to the displayed $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RegularCauchySumUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ coordinates of that finite route.
\end{theorem}
\begin{proof}
Apply \autoref{thm:cauchy-base-forward-l10-route}. Any consumer that reaches the regular sum tail-estimate surface first factors through the finite StreamName, DyadicRatCore, RegSeqRat, RegularCauchySum, Real, history, transport, replay, provenance, and local naming rows named there.

Project the excluded coordinates from that route. Quotient stream equality, selected limits, countable-choice schedules, host real equality, and ambient $\RealUp$ completeness are not rows of the displayed packet, so no downstream consumer can obtain them from the forward L10 sum-tail surface.
\end{proof}
