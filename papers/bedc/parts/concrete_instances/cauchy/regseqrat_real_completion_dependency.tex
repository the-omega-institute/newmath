Every completion-facing consumer of the $\CauchyUp$ hub reads the finite Cauchy packet through \autoref{ch:concrete-instances-regseqrat-namecert} and \autoref{ch:concrete-instances-real-namecert} before any Real seal is exposed to completion rows. The route first displays the finite $\StreamNameUp$ windows, the $\RegSeqRatUp$ readback, and the dyadic tolerance ledger, then reaches the terminal $\RealUp$ seal, and only then lets Cauchy completion consumers inspect the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows. Thus the hub records a structural dependency route, not quotient stream equality, selected limits, countable choice, host real equality, or ambient Real completeness.
