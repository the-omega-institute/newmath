The $\CauchyUp$ route consumes the L10 sibling chapters before the real locator and sequence algebra inputs are read. In particular, the regular-rational source of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, and the terminal seal of \autoref{ch:concrete-instances-real-namecert} are already part of the displayed dependency spine for every downstream Cauchy locator or algebra consumer.
