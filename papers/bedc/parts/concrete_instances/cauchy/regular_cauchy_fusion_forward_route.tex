\begin{theorem}[Cauchy regular-fusion forward route]
\label{thm:cauchy-orphan-regular-cauchy-fusion-forward-route}
Every $\CauchyUp$ completion-facing read that uses a fused regular-Cauchy tail passes through the sibling $\RegularCauchyFusionUp$ packet before the Cauchy hub exports the route. The displayed route consumes the $\StreamNameUp$ finite windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance rows, the terminal $\RealUp$ seal, $\BHist$ evidence, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows before any completion consumer may inspect the fused tail.
\end{theorem}
\begin{proof}
Read the fused tail as a sibling packet rather than as a hidden coordinate of the Cauchy hub. The $\RegularCauchyFusionUp$ row supplies the displayed fusion boundary, while $\StreamNameUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ expose the finite observation windows, regular-rational readback, and dyadic tolerance rows used by the tail.

The completion-facing consumer then reaches the $\RealUp$ seal only after the finite route has been replayed through $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. Thus the Cauchy hub exports a sibling dependency route for fused regular-Cauchy tails, not a quotient stream, selected limit, choice schedule, trichotomy oracle, or ambient completeness row.
\end{proof}
