\chapter{A Concrete Naming Certificate for $\ConstructiveMeanValueUp$}
\label{ch:concrete-instances-constructive-mean-value-namecert}
\origin{human}

Following Cauchy, Lagrange, Rolle, and the Bishop--Bridges treatment of constructive analysis, the $\ConstructiveMeanValueUp$ packet records the finite BEDC boundary of the mean-value witness that is obtained by reducing a displayed secant comparison to the already named Rolle packet. It consumes the terminal real-name surface of \autoref{ch:concrete-instances-real-namecert}, the pointwise graph and modulus rows of \autoref{ch:concrete-instances-continuousmap-namecert}, the compact interval locator of \autoref{ch:concrete-instances-compact-interval-locator-namecert}, the finite attainment route of \autoref{ch:concrete-instances-extremevalue-namecert}, the derivative metric quotient rows of \autoref{ch:concrete-instances-derivative-namecert}, and the stationary packet of \autoref{ch:concrete-instances-rolle-namecert}. The chapter names only the finite witness surface: interval data, secant-slope rows, adjusted Rolle data, derivative quotient inspection, an interior witness, a terminal $\RealUp$ equality seal, transport, replay, provenance, and local naming.

\begin{definition}[Constructive mean-value carrier]
\label{def:constructive-mean-value-carrier}
A $\ConstructiveMeanValueUp$ carrier is a finite $\BHist$ packet
$$
M=(I,S,R,D,W,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the compact interval source row, exposing located endpoint names, interval cells, and the finite boundary data inspected by the witness route.
\item $S$ is the secant-slope row, recording the displayed endpoint-value difference, interval-width seal, and quotient route as $\RealUp$ data rather than as a host real quotient.
\item $R$ is the adjusted $\RolleUp$ packet obtained by subtracting the displayed secant line from the named $\ContinuousMapUp$ row over the same compact interval source.
\item $D$ is the $\DerivativeUp$ metric quotient row for the adjusted map together with the derivative quotient route of the original displayed map.
\item $W$ is the interior witness row exported by the Rolle packet $R$.
\item $E$ is the terminal $\RealUp$ equality seal saying that the derivative quotient read at $W$ is classifier-equal to the secant-slope row $S$.
\item $H$ records componentwise $\hsame$ transport for the interval source, secant slope, adjusted Rolle packet, derivative quotient, interior witness, equality seal, replay rows, provenance rows, and local naming rows.
\item $C$ records displayed $\Cont$ replay from interval inspection to endpoint reads, from the secant row to the adjusted Rolle packet, from the Rolle stationary row to derivative inspection, and from the derivative quotient to the terminal equality seal.
\item $P$ is the $\Pkg$ provenance over $\ConstructiveMeanValueUp$, $\RolleUp$, $\CompactIntervalLocatorUp$, $\ContinuousMapUp$, $\ExtremeValueUp$, $\DerivativeUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ConstructiveMeanValueUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for host differentiability, a choice-selected point, quotient real equality, open-cover compactness, ambient $\RealUp$ completeness, or a standard bridge.
\end{definition}
\leanchecked{BEDC.Derived.ConstructiveMeanValueUp.ConstructiveMeanValueTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Constructive mean-value NameCert obligations]
\label{thm:constructive-mean-value-namecert-obligations}
For every accepted $\ConstructiveMeanValueUp$ carrier
$$
M=(I,S,R,D,W,E,H,C,P,N),
$$
the local $\NameCert_{\ConstructiveMeanValueUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item compact interval admission through $I$;
\item secant-slope construction through $S$ over displayed $\RealUp$ endpoint and interval-width seals;
\item adjusted Rolle admission through $R$ over the same interval and continuous-map source;
\item derivative quotient routing through $D$ and the interior witness row $W$;
\item terminal equality sealing through $E$;
\item structural stability through $H,C,P,N$.
\end{enumerate}
No mean-value obligation is stored outside the displayed carrier.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:constructive-mean-value-carrier}. The row $I$ supplies the compact interval source, and $S$ supplies the only secant-slope data available to the packet.

The adjusted theorem route is then fixed. The row $R$ is the sole $\RolleUp$ packet in the carrier, and it is built over the same interval and continuous-map source rather than over a detached host function.

Inspect the derivative-facing rows. The quotient row $D$, the interior witness row $W$, and the terminal equality row $E$ are the only path from the adjusted Rolle stationary read back to the original secant-slope comparison.

Finally, the structural rows $H,C,P,N$ transport, replay, source, and name the same row inventory. Since the carrier has no further coordinate, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Constructive mean-value Rolle factorization]
\label{thm:constructive-mean-value-rolle-factorization}
Every mean-value witness exported by an accepted $\ConstructiveMeanValueUp$ carrier factors through the displayed $\RolleUp$ stationary packet $R$. The exported witness is the interior row $W$ together with the terminal $\RealUp$ equality seal $E$ between the derivative quotient route $D$ and the secant-slope row $S$; it does not assert host differentiability, a selected point, quotient equality of completed reals, open-cover compactness, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with an accepted carrier $M=(I,S,R,D,W,E,H,C,P,N)$. The interval and secant data are visible before any witness is read, and the carrier exposes no other endpoint or quotient source.

Read the adjusted Rolle packet $R$. By \autoref{ch:concrete-instances-rolle-namecert}, its stationary output is already constrained to the compact interval source, endpoint equality, continuous-map row, extreme-value witness, derivative quotient route, interior witness, and zero-seal boundary displayed inside the Rolle packet.

The mean-value carrier consumes exactly that stationary output. Its row $W$ is the interior witness inherited from $R$, while $D$ and $E$ translate the adjusted zero-seal into the displayed equality between the original derivative quotient and the secant slope.

Transport and replay through $H$ and $C$ preserve this factorization, and $P,N$ add only source and naming data. Thus every exported mean-value witness passes through $R$ before it reaches $D,W,E$, with no hidden host differentiability, selected point, quotient equality, open-cover compactness, or ambient completeness.
\end{proof}

\falsifiablePrediction{Every $\ConstructiveMeanValueUp$ witness read factors through a compact interval source row, secant-slope row, adjusted $\RolleUp$ packet, $\DerivativeUp$ quotient route, interior witness row, terminal $\RealUp$ equality seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{real, continuousmap, compact-interval-locator, extremevalue, derivative, rolle: $\ConstructiveMeanValueUp$ is not bijective to any listed sibling because it jointly carries secant-slope data, an adjusted $\RolleUp$ stationary packet, derivative quotient comparison, an interior witness, a terminal equality seal, transport, replay, provenance, and local naming as one mean-value witness packet.}

\closureat{ConstructiveMeanValueUp}{seedStr}

\begin{closurestatus}{\ConstructiveMeanValueUp}
  \constructivestory{A $\ConstructiveMeanValueUp$ packet is generated from a compact interval source row, a secant-slope row, an adjusted $\RolleUp$ packet, a $\DerivativeUp$ quotient route, an interior witness row, a terminal $\RealUp$ equality seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:constructive-mean-value-carrier}, \autoref{thm:constructive-mean-value-namecert-obligations}, and \autoref{thm:constructive-mean-value-rolle-factorization} seed the finite mean-value witness surface over compact interval, secant-slope, adjusted Rolle, derivative quotient, interior witness, and Real equality-seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host differentiability, selected points, quotient equality of completed reals, open-cover compactness, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, secant-slope construction, adjusted Rolle factorization, derivative quotient equality sealing, and TasteGate field faithfulness for BEDC.Derived.ConstructiveMeanValueUp.}
\end{closurestatus}
