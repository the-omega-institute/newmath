\chapter{A Concrete Naming Certificate for $\MedianDepthSealingCornerUp$}
\label{ch:concrete-instances-median-depth-sealing-corner-namecert}
\origin{ai}

The $\MedianDepthSealingCornerUp$ packet realizes the two-layer median closure and unique $\mathrm{ACG}$ sealing corner of \autoref{thm:philosophy-median-depth-sealing} as finite NameCert data. It consumes the DNA window-six boundary tile packet of \autoref{ch:concrete-instances-dna-window-six-boundary-tile-namecert}, the trigger-blocker route of \autoref{ch:concrete-instances-trigger-blocker-duality-namecert}, the stop/Trp face deficiency packet of \autoref{ch:concrete-instances-stop-trp-face-rank-deficiency-namecert}, and the Boolean trigger lattice of \autoref{ch:concrete-instances-boolean-trigger-lattice-namecert}. The packet records a median-layer route and a unique-corner seal; it is not a biological mechanism, stochastic fitness model, quotient codon semantics, or unbounded support-closure oracle.

\begin{definition}[Median depth sealing corner carrier]
\label{def:median-depth-sealing-corner-carrier}
A $\MedianDepthSealingCornerUp$ carrier is a finite $\BHist$ packet
$$
K=(B,L,U,F,W,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the DNA window-six boundary-tile source row.
\item $L$ is the median-layer ledger, displaying the active reassignment support, the first median layer, and the second median layer.
\item $U$ is the unique sealing-corner witness row, naming $\mathrm{ACG}$ as the sole strict second-layer median point.
\item $F$ is the f-vector transition row recording the passage from the punctured main $\mathrm{WNR}$ $4$-cube to the sealed cube.
\item $W$ is the witness row displaying the median witnesses for the first layer and the $\mathrm{ACG}$ second-layer witness before $F$ is read.
\item $H$ records componentwise $\hsame$ transport for the boundary tile, median-layer, unique-corner, f-vector, witness, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through
$$
B,L,U,W \longmapsto F .
$$
\item $P$ is the $\Pkg$ provenance over $\MedianDepthSealingCornerUp$, $\DnaWindowSixBoundaryTileUp$, $\TriggerBlockerDualityUp$, $\StopTrpFaceRankDeficiencyUp$, $\BooleanTriggerLatticeUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MedianDepthSealingCornerUp}$ row.
\end{enumerate}
The carrier compares accepted packets only through $B,L,U,F,W,H,C,P,N$ and has no coordinate for hidden codon semantics, stochastic biology, unrestricted median search, or host cubical-complex equality.
\end{definition}

\begin{theorem}[Median depth sealing corner NameCert obligations]
\label{thm:median-depth-sealing-corner-namecert-obligations}
For every accepted $\MedianDepthSealingCornerUp$ carrier $K=(B,L,U,F,W,H,C,P,N)$, the local $\NameCert_{\MedianDepthSealingCornerUp}$ surface consists exactly of finite median-layer admission, unique $\mathrm{ACG}$ corner exposure, f-vector transition exposure, witness-row exposure, componentwise transport, continuation replay, provenance, and local naming rows.
\end{theorem}
\begin{proof}
Project the carrier. The boundary-tile row $B$ is the source, while $L$ records the two median layers and $U$ names the unique strict second-layer corner.

The witness row $W$ must be read before the f-vector transition $F$. Thus an accepted read displays both median layers and the $\mathrm{ACG}$ witness before it consumes the cubical transition from the punctured main $\mathrm{WNR}$ cell to the sealed cell.

The structural rows $H,C,P,N$ transport, replay, source, and name only this finite packet. Since no row stores hidden codon semantics, stochastic dynamics, an unrestricted median oracle, or host cubical equality, the listed rows exhaust the local NameCert surface.
\end{proof}

\falsifiablePrediction{Every accepted $\MedianDepthSealingCornerUp$ read displays the two median layers and the $\mathrm{ACG}$ witness row before the f-vector transition row is consumed.}

\independenceWitness{dna-window-six-boundary-tile, trigger-blocker-duality, stop-trp-face-rank-deficiency, boolean-trigger-lattice: none is bijective with $\MedianDepthSealingCornerUp$ because this packet jointly carries the two median layers, the unique $\mathrm{ACG}$ corner, the f-vector transition, witness rows, transport, replay, provenance, and local naming.}

The Lean follow-up must provide explicit $\mathsf{Nontrivial}\ \MedianDepthSealingCornerUp$ and $\mathsf{FieldFaithful}\ \MedianDepthSealingCornerUp$ TasteGate instances so that $B,L,U,F,W,H,C,P,N$ remain separate displayed fields.

\closureat{MedianDepthSealingCornerUp}{seedStr}

\begin{closurestatus}{\MedianDepthSealingCornerUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:median-depth-sealing-corner-carrier} and \autoref{thm:median-depth-sealing-corner-namecert-obligations} seed the finite median-layer and unique sealing-corner packet for the DNA window-six boundary-tile route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim biological mechanism, stochastic fitness, quotient codon semantics, unrestricted median search, host cubical equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, median-layer exposure, unique-corner exposure, f-vector transition exposure, witness exposure, Nontrivial MedianDepthSealingCornerUp, and FieldFaithful MedianDepthSealingCornerUp for BEDC.Derived.MedianDepthSealingCornerUp.}
  \constructivestory{A $\MedianDepthSealingCornerUp$ packet is generated from a DNA boundary-tile source, median-layer rows, a unique ACG sealing-corner row, f-vector transition data, witness rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
