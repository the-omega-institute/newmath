The selector dependency route is consumed by both the fast subsequence handoff of \autoref{ch:concrete-instances-fastcauchysubsequence-namecert} and the regular-Cauchy gluing route of \autoref{ch:concrete-instances-regseqrat-namecert}. Thus a downstream Cauchy consumer reads the same displayed selector schedule, finite windows, and gap ledger before treating the selected route as fast enough for completion-facing readback.
