\chapter{A Concrete Naming Certificate for $\CauchySubsequenceSelectorUp$}
\label{ch:concrete-instances-cauchy-subsequence-selector-namecert}
\origin{human}

Following Bishop and Bridges on constructive Cauchy sequences, the $\CauchySubsequenceSelectorUp$ packet records the finite selector schedule by which a regular rational Cauchy name may expose a monotone subsequence corridor before a Real or Completion consumer reads the terminal seal. It sits after the regular rational source of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite window surface of \autoref{ch:concrete-instances-streamname-namecert}, the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}, and the Cauchy hub of \autoref{ch:concrete-instances-cauchysequencespace-namecert}. The packet gives diagonal, tail, and completion-facing chapters a named L10 selector object; it is not a quotient of streams, a selected host limit, countable choice, trichotomy, or ambient $\RealUp$ completeness.

Any subsequence-selector read must display the $\CauchyDiagonalSelectionUp$ route of \autoref{ch:concrete-instances-cauchy-diagonal-selection-namecert} and the $\RegularCauchyWindowInductionUp$ finite-window schedule of \autoref{ch:concrete-instances-regular-cauchy-window-induction-namecert} before reaching $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ consumers. The selector is therefore a visible diagonal-and-window corridor, not a detached subsequence choice. The fast selector route of \autoref{ch:concrete-instances-fastcauchysubsequence-namecert} consumes this schedule when the cofinal subsequence corridor must be made fast enough for $\RealUp$ completion windows, while \autoref{ch:concrete-instances-regular-cauchy-gluing-namecert} consumes compatible selected windows when the subsequence corridor is glued back into a regular Cauchy route.

\begin{definition}[Cauchy subsequence selector carrier]
\label{def:cauchy-subsequence-selector-carrier}
A $\CauchySubsequenceSelectorUp$ carrier is a finite $\BHist$ packet
$$
S=(R,W,M,G,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RegSeqRatUp$ source row whose rational reads are being sampled.
\item $W$ is the $\StreamNameUp$ finite-window row exposing the source observations consumed by the selector.
\item $M$ is the monotone selector row, recording a finite schedule of indices and the strict order checks between successive selected positions.
\item $G$ is the Cauchy-gap ledger, recording the tail comparison requests that make the selected windows compatible with the source Cauchy evidence.
\item $E$ is the terminal $\RealUp$ seal row, available only after $R,W,M,G$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the source, window, monotone selector, Cauchy-gap, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a subsequence request through $R,W,M,G,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CauchySubsequenceSelectorUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchySubsequenceSelectorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $R,W,M,G,E,H,C,P,N$. It has no coordinate for quotient stream equality, selected limits, host real equality, countable choice, trichotomy, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Cauchy subsequence selector NameCert obligations]
\label{thm:cauchy-subsequence-selector-namecert-obligations}
For every accepted $\CauchySubsequenceSelectorUp$ carrier $S=(R,W,M,G,E,H,C,P,N)$, the local $\NameCert_{\CauchySubsequenceSelectorUp}$ surface consists exactly of carrier admission, selector monotonicity, $\StreamNameUp$ window coverage, Cauchy-gap compatibility, $\RegSeqRatUp$ handoff, $\RealUp$ seal non-escape, componentwise transport, displayed replay, package provenance, and local naming. Each obligation is read through the finite route
$$
R,W,M,G \longrightarrow E \longrightarrow H,C,P,N .
$$
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:cauchy-subsequence-selector-carrier}. The packet first exposes the regular rational source $R$, the finite observation windows $W$, the monotone selector schedule $M$, and the Cauchy-gap ledger $G$.

Selector monotonicity is local to $M$. The window coverage obligation then reads $W$ at precisely the positions displayed by $M$, so every selected index has a finite $\StreamNameUp$ observation before any handoff is consumed.

The $\RegSeqRatUp$ handoff is the source row $R$ restricted along the displayed schedule and checked against the Cauchy-gap ledger $G$. The terminal seal $E$ is downstream of those rows, and it is only a $\RealUp$ seal for the displayed subsequence corridor.

Transport through $H$, replay through $C$, provenance $P$, and local naming $N$ preserve the same route. Since quotient stream equality, selected limits, host real equality, countable choice, trichotomy, and ambient Real completeness are absent from the carrier coordinates, no obligation can discharge outside the displayed finite packet.
\end{proof}

\begin{theorem}[Cauchy subsequence selector real handoff]
\label{thm:cauchy-subsequence-selector-real-handoff}
Every $\RealUp$ or Completion-facing consumer of an accepted $\CauchySubsequenceSelectorUp$ packet must first read the $\RegSeqRatUp$ source row, the $\StreamNameUp$ finite windows, the monotone selector schedule, and the Cauchy-gap ledger. The handoff to $E$ therefore supplies a finite subsequence corridor from regular rational data to Real-facing data, not a host subsequence theorem, not a selected limit, and not a quotient construction.
\end{theorem}
\begin{proof}
Project an accepted carrier $S=(R,W,M,G,E,H,C,P,N)$. The source row $R$ is the only regular rational input, and $W$ is the only finite observation surface available to a consumer.

The selector row $M$ records the monotone schedule. The Cauchy-gap ledger $G$ records the finite tail comparisons that let the scheduled windows be read as a subsequence corridor of the same source.

Only after $R,W,M,G$ are present may the consumer inspect $E$ as a Real-facing seal. The transport, replay, provenance, and naming rows keep that order visible to downstream chapters.

The carrier contains no coordinate for quotient stream equality, a chosen host subsequence, a selected limit, countable choice, trichotomy, or ambient $\RealUp$ completeness. Thus every Real or Completion handoff factors through the displayed selector rows.
\end{proof}

\begin{theorem}[Cauchy subsequence selector sibling lattice route]
\label{thm:cauchy-subsequence-selector-sibling-lattice-route}
Every $\CauchySubsequenceSelectorUp$ consumer must pass through $\CauchyDiagonalSelectionUp$ and $\RegularCauchyWindowInductionUp$ before the $\StreamNameUp$, $\RegSeqRatUp$, and terminal $\RealUp$ seal rows are consumed. The selector route therefore reads the diagonal-selection corridor, the regular Cauchy finite-window induction ledger, the source row $R$, finite windows $W$, monotone selector schedule $M$, Cauchy-gap ledger $G$, seal row $E$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\CauchySubsequenceSelectorUp}$ row as one sibling lattice. Before any gluing-facing consumer reads $\RegularCauchyGluingUp$, the selected finite windows are certified as regular-Cauchy subsequence data through \autoref{ch:concrete-instances-regular-cauchy-subsequence-namecert}; only then can the gluing route consume the selector rows $R,W,M,G$ before the terminal $\RealUp$ seal.

The route exports no detached subsequence choice, quotient stream equality, selected limit, countable choice, trichotomy, host real equality, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project an accepted carrier $S=(R,W,M,G,E,H,C,P,N)$ from \autoref{def:cauchy-subsequence-selector-carrier}. The source, window, monotone selector, Cauchy-gap, and seal rows are the only analytic rows available to a consumer, while $H,C,P,N$ are the structural rows that transport, replay, source, and name them.

Apply \autoref{thm:cauchy-subsequence-selector-namecert-obligations}. It forces carrier admission, selector monotonicity, window coverage, Cauchy-gap compatibility, regular-rational handoff, seal non-escape, transport, replay, provenance, and local naming before any consumer can read the selector.

Apply \autoref{thm:cauchy-subsequence-selector-real-handoff}. The $\RealUp$ or Completion-facing read reaches $E$ only after $R,W,M,G$ have been displayed, so the diagonal-selection corridor and regular Cauchy window-induction ledger must be upstream of the $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows named in the introduction; the same displayed windows are first certified by \autoref{ch:concrete-instances-regular-cauchy-subsequence-namecert} before $\RegularCauchyGluingUp$ can consume them.

Since the carrier has no coordinate for detached subsequence choice, quotient stream equality, selected limits, countable choice, trichotomy, host real equality, or ambient completeness, the sibling lattice cannot export any of those principles.
\end{proof}

\begin{theorem}[Cauchy subsequence selector diagonal-window readiness]
\label{thm:cauchy-subsequence-selector-diagonal-window-readiness}
Every fast-selector or gluing-facing consumer of a $\CauchySubsequenceSelectorUp$ packet must expose the $\CauchyDiagonalSelectionUp$ route and the $\RegularCauchyWindowInductionUp$ finite schedule before it consumes the selector rows $R,W,M,G,E,H,C,P,N$. In particular, a $\FastCauchySubsequenceUp$ consumer cannot read the fast cofinal corridor, and a $\RegularCauchyGluingUp$ consumer cannot read compatible selected windows, until the diagonal route and finite-window schedule have already displayed the source row, finite windows, monotone selector row, Cauchy-gap ledger, terminal seal, transport, replay, provenance, and local naming rows.
\end{theorem}
\begin{proof}
Apply \autoref{thm:cauchy-subsequence-selector-namecert-obligations}. It makes $R,W,M,G,E,H,C,P,N$ the full NameCert surface for an accepted selector packet, so a downstream consumer has no row outside that finite display from which to read subsequence data.

Apply \autoref{thm:cauchy-subsequence-selector-real-handoff}. The Real or Completion-facing handoff reaches $E$ only after the regular rational source, finite windows, selector schedule, and Cauchy-gap ledger have been displayed.

Now apply \autoref{thm:cauchy-subsequence-selector-sibling-lattice-route}. It places the $\CauchyDiagonalSelectionUp$ corridor and the $\RegularCauchyWindowInductionUp$ schedule before the $\StreamNameUp$, $\RegSeqRatUp$, terminal $\RealUp$, and gluing-facing rows are consumed. Therefore both the fast-selector route and the gluing route must expose the diagonal-window readiness surface before reading $R,W,M,G,E,H,C,P,N$.
\end{proof}

\begin{theorem}[Cauchy subsequence selector regular-window handoff]
\label{thm:cauchy-subsequence-selector-regular-window-handoff}
Every $\CauchySubsequenceSelectorUp$ handoff that is later glued or sealed through $\RealUp$ first consumes $\RegularCauchySubsequenceUp$ as the finite-window certificate that the selected rows remain regular-Cauchy subsequence data. Thus the selected windows $W$ and monotone schedule $M$ are certified through \autoref{ch:concrete-instances-regular-cauchy-subsequence-namecert} before any $\RegularCauchyGluingUp$ or terminal $\RealUp$ consumer reads $R,W,M,G,E$.
\end{theorem}
\begin{proof}
Project the accepted carrier $S=(R,W,M,G,E,H,C,P,N)$. The selected windows $W$ and monotone schedule $M$ are the only rows that can present subsequence data to a downstream consumer.

Use the sibling lattice route of \autoref{thm:cauchy-subsequence-selector-sibling-lattice-route}. It places the diagonal-selection corridor and regular Cauchy window-induction ledger before the $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows are consumed.

The regular-window handoff is therefore a finite certification step: $W$ and $M$ are read as regular-Cauchy subsequence data before gluing or sealing can consume $R,W,M,G,E$. The route has no coordinate for a detached subsequence choice, selected host limit, or ambient completeness principle.
\end{proof}

\begin{theorem}[Cauchy subsequence selector forward density route]
\label{thm:cauchy-subsequence-selector-forward-density-route}
Every $\RegularCauchyDenseEmbeddingUp$ or density-facing completion consumer that reads selected Cauchy subsequence windows from an accepted $\CauchySubsequenceSelectorUp$ packet must first pass through $\RegularCauchySubsequenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, and the terminal $\RealUp$ seal route displayed by the selector. The forward density read therefore consumes the selector rows $R,W,M,G,E,H,C,P,N$ as finite regular-Cauchy subsequence data, not as a separate host subsequence, a choice-selected limit, or an ambient completeness principle.
\end{theorem}
\begin{proof}
Apply the Real handoff theorem, \autoref{thm:cauchy-subsequence-selector-real-handoff}. It forces every Real or Completion-facing read through the regular rational source row $R$, finite windows $W$, monotone selector schedule $M$, Cauchy-gap ledger $G$, and terminal seal row $E$.

Use the sibling lattice route of \autoref{thm:cauchy-subsequence-selector-sibling-lattice-route} together with the regular-window handoff of \autoref{thm:cauchy-subsequence-selector-regular-window-handoff}. These place $\StreamNameUp$, $\RegSeqRatUp$, and $\RegularCauchySubsequenceUp$ before any downstream gluing, sealing, or density-facing consumer reads the selected windows.

Thus a $\RegularCauchyDenseEmbeddingUp$ consumer receives only the displayed finite selector corridor. Since the carrier has no coordinate for a detached host subsequence, selected limit, quotient stream equality, or ambient completeness, the forward density route cannot bypass the existing selector handoff.
\end{proof}

\closureat{CauchySubsequenceSelectorUp}{seedStr}

\begin{closurestatus}{\CauchySubsequenceSelectorUp}
  \constructivestory{A $\CauchySubsequenceSelectorUp$ packet is generated from a RegSeqRat source row, finite StreamName windows, monotone selector schedule, Cauchy-gap ledger, terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-subsequence-selector-carrier}, \autoref{thm:cauchy-subsequence-selector-namecert-obligations}, and \autoref{thm:cauchy-subsequence-selector-real-handoff} seed the finite selector corridor from regular rational Cauchy data to a Real-facing seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient stream equality, selected limits, countable choice, trichotomy, host real equality, Lean verification, bridge checking, or ambient Real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, selector monotonicity, StreamName window coverage, Cauchy-gap compatibility, RegSeqRat handoff, Real seal non-escape, transport, replay, provenance, and local naming.}
\end{closurestatus}
