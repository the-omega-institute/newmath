The selector packet also consumes the regular-Cauchy subsequence surface of \autoref{ch:concrete-instances-regular-cauchy-subsequence-namecert}. Its monotone selector row, finite windows, and Cauchy-gap ledger are read as a route inside the same regular-Cauchy finite-window surface before the terminal $\RealUp$ seal is available. Thus a downstream completion consumer sees the selected subsequence as displayed regular-Cauchy data, not as a detached host subsequence or a separate choice principle.
