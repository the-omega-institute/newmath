\chapter{A Concrete Naming Certificate for $\ReassignmentSupportCompressionUp$}
\label{ch:concrete-instances-reassignment-support-compression-namecert}
\origin{ai}

The $\ReassignmentSupportCompressionUp$ packet realizes the codon reassignment support compression branch of the DNA six-bit boundary tile analysis as finite NameCert data. The support row is read through the six-bit codon window of \autoref{ch:concrete-instances-codon-six-bit-window-namecert} and the boundary-tile coverage surface of \autoref{ch:concrete-instances-six-boundary-tile-coverage-namecert}; the compression packet therefore consumes those displayed coordinate and coverage rows before it names motif and prime-cover data. It packages the displayed $13$-codon support, five-motif formula, seven prime implicants, and unique five-motif prime cover from \autoref{def:philosophy-reassigned-support} through finite transport and replay rows, rather than treating the support as a host-side Boolean simplification oracle.

\falsifiablePrediction{Every accepted $\ReassignmentSupportCompressionUp$ read factors through displayed codon support, motif-expansion, prime-implicant, minimal-cover, transport, replay, provenance, and local NameCert rows.}

\independenceWitness{codon-six-bit-window, stopcodonzeckendorfsuffix, codon-transition-matrix, bitvector: none is bijective to $\ReassignmentSupportCompressionUp$ because this packet simultaneously binds the reassigned $13$-codon support, five-motif expansion, seven prime implicants, unique minimum prime cover, transport, replay, provenance, and local naming rows. The six-bit window carrier supplies coordinates without the support theorem; the Zeckendorf suffix row names a stop-codon boundary; the transition matrix records table movement; and a raw bitvector lacks motif and cover obligations.}

\begin{definition}[ReassignmentSupportCompression carrier]
\label{def:reassignment-support-compression-carrier}
A $\ReassignmentSupportCompressionUp$ carrier is a finite packet
$$
K=(S,M,I,U,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $S$ is the displayed reassigned support row containing exactly the $13$ codons of \autoref{thm:philosophy-reassigned-support};
\item $M$ is the five-motif expansion row recording $\mathrm{CUN}$, $\mathrm{AGR}$, $\mathrm{UNA}$, $\mathrm{WWA}$, and $\mathrm{UAR}$ as the support formula;
\item $I$ is the prime-implicant row recording exactly the seven maximal exact subcubes of \autoref{thm:philosophy-reassignment-prime-implicants};
\item $U$ is the minimal-cover row recording the unique five-prime cover of \autoref{thm:philosophy-minimal-prime-implicant-cover};
\item $H$ stores componentwise $\hsame$ transport for the support, motif, prime-implicant, minimal-cover, provenance, and local naming rows;
\item $C$ stores the displayed $\Cont$ replay route from codon support to motif expansion, from motif expansion to prime implicants, and from prime implicants to the minimum-cover row;
\item $P$ is the $\Pkg$ provenance over the DNA six-bit boundary tile source rows, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\ReassignmentSupportCompressionUp}$ row.
\end{enumerate}
The carrier contains no host Boolean minimizer, no unbounded codon-table oracle, no genetic-code semantics beyond the displayed reassignment support, and no quotient identifying support lists with motif certificates.
\end{definition}

\begin{theorem}[ReassignmentSupportCompression NameCert obligations]
\label{thm:reassignment-support-compression-namecert-obligations}
Every accepted $\ReassignmentSupportCompressionUp$ packet satisfies the following finite obligations:
\begin{enumerate}
\item carrier admission exposes the support, motif, prime-implicant, minimal-cover, transport, replay, provenance, and NameCert rows of \autoref{def:reassignment-support-compression-carrier};
\item support exactness identifies $S$ with the displayed $13$-codon reassigned support and rejects any added or omitted codon;
\item motif expansion identifies $M$ with the five IUPAC motifs whose union is the same support;
\item prime-cover compression identifies $I$ with the seven prime implicants and $U$ with the unique five-prime cover;
\item nonescape prevents a consumer from replacing the displayed rows by a host minimizer, a raw bitvector equality, a transition-matrix equivalence, a stop-codon suffix rule, or an unbounded codon-table oracle.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier rows of \autoref{def:reassignment-support-compression-carrier}. The support row $S$ is the only entry point for codon membership, and the continuation row $C$ replays it first into the five-motif row $M$.

Next replay $M$ through the finite prime-implicant enumeration row $I$. This preserves exactly the displayed maximal exact subcubes because every admitted implicant is checked against the already fixed support row.

Finally project the minimal-cover row $U$ from $I$. The package, transport, replay, and local naming rows carry no additional semantic oracle, so a consumer cannot bypass the finite support, motif, prime-implicant, and cover rows. The Lean follow-up will provide `Nontrivial ReassignmentSupportCompressionUp` and `FieldFaithful ReassignmentSupportCompressionUp` in `lean4/BEDC/Derived/ReassignmentSupportCompressionUp/TasteGate.lean`.
\end{proof}

\begin{theorem}[ReassignmentSupportCompression sibling lattice]
\label{thm:reassignment-support-compression-sibling-lattice}
Every accepted $\ReassignmentSupportCompressionUp$ consumer must read the codon six-bit window of \autoref{ch:concrete-instances-codon-six-bit-window-namecert}, the stop-codon Zeckendorf suffix surface of \autoref{ch:concrete-instances-stopcodonzeckendorfsuffix-namecert}, the codon transition matrix of \autoref{ch:concrete-instances-codon-transition-matrix-namecert}, and the bitvector carrier of \autoref{ch:concrete-instances-bitvector-namecert} before treating the five-motif row $M$ and the prime-cover rows $I,U$ as support-compression data.

Thus the packet is read as a horizontal lattice over displayed codon coordinates, stop-boundary suffixes, transition rows, and finite bitvector surfaces. It cannot be consumed as an isolated Boolean minimization certificate, a raw support bitvector, a stop-codon suffix rule, or a transition-matrix equivalence.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:reassignment-support-compression-carrier}. The support row $S$ is already tied to displayed codon membership, and the packet's provenance row names the DNA six-bit boundary tile source rows before the motif row is replayed.

Read the sibling surfaces in order. The codon six-bit window supplies the coordinate surface, the stop-codon Zeckendorf suffix supplies the stop-boundary comparison row, the codon transition matrix supplies table-movement visibility, and the bitvector carrier supplies the finite support encoding boundary.

Only after those sibling rows are visible can the consumer interpret $M,I,U$ as support-compression rows. Since the carrier contains no host Boolean minimizer, raw bitvector equality, isolated suffix rule, or transition-matrix quotient, the five-motif and prime-cover rows remain downstream of the sibling lattice.
\end{proof}

\closureat{ReassignmentSupportCompressionUp}{seedStr}

\begin{closurestatus}{\ReassignmentSupportCompressionUp}
  \constructivestory{A $\ReassignmentSupportCompressionUp$ packet is generated from the reassigned codon support, five-motif expansion, seven-prime-implicant surface, unique minimum prime cover, componentwise transport, displayed replay, provenance, and local naming rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:reassignment-support-compression-carrier} and \autoref{thm:reassignment-support-compression-namecert-obligations} seed the finite support-compression route for the reassignment support branch.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a checked Lean certificate, genetic-code semantics outside the displayed support, host Boolean minimization, table-transition completeness, or biological causality.}
  \upgradepath{Obligation closure requires checked carrier admission, support exactness, motif expansion, prime-implicant enumeration, minimum-cover uniqueness, and nonescape from host minimizers.}
\end{closurestatus}
