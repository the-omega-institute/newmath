\chapter{A Concrete Naming Certificate for $\BertrandSeriesTestUp$}
\label{ch:concrete-instances-bertrand-series-test-namecert}
\origin{human}

Following Bertrand's logarithmic refinement of ratio-type convergence tests and Bishop--Bridges constructive real-series practice, the $\BertrandSeriesTestUp$ packet records the finite second-order quotient-defect route that turns displayed positive-series tail data into a regular Cauchy comparison handoff. It sits beside the neighboring quotient-defect surfaces of \autoref{ch:concrete-instances-raabe-duhamel-test-namecert}, \autoref{ch:concrete-instances-kummer-test-namecert}, and \autoref{ch:concrete-instances-gauss-test-namecert}, refines the ratio and comparison routes of \autoref{ch:concrete-instances-regular-cauchy-ratio-test-namecert} and \autoref{ch:concrete-instances-regular-cauchy-comparison-test-namecert}, and exports endpoints only through the dyadic, stream, regular-rational, and Real seals of \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}. The packet names finite Bertrand-test evidence only through inspected source windows, adjacent quotient windows, a logarithmic defect ledger, dyadic domination, comparison-test routing, regular rational readback, and terminal Real sealing; it is not host summability, quotient series equality, selected sums, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.

\begin{definition}[Bertrand series test carrier]\label{def:bertrand-series-test-carrier}
A $\BertrandSeriesTestUp$ carrier is a finite $\BHist$ packet
$$
B=(S,Q,L,D,T,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the source real-series row, exposing only positive-term windows, partial-sum windows, and tail-threshold requests used by the current convergence test.
\item $Q$ is the adjacent quotient-window row, recording the inspected neighboring-term quotients on the same displayed tail window.
\item $L$ is the logarithmic defect ledger, storing the finite index, logarithmic scale, rational margin, and second-order defect rows used by the Bertrand comparison.
\item $D$ is the $\DyadicRatCoreUp$ domination ledger, converting the displayed logarithmic defect and margin rows into a finite majorant or minorant comparison.
\item $T$ is the $\RegularCauchyComparisonTestUp$ handoff row that consumes $S,Q,L,D$ before any convergence conclusion is exposed.
\item $W$ is the $\StreamNameUp$ finite-window row for the source, quotient, logarithmic defect, domination, and comparison rows.
\item $R$ is the $\RegSeqRatUp$ readback row reached only after the comparison handoff and stream windows have been displayed.
\item $E$ is the terminal $\RealUp$ seal for endpoint-facing consumers.
\item $H$ stores componentwise $\hsame$ transport for the source, quotient, logarithmic defect, domination, comparison, window, readback, seal, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from each tail request through $Q,L,D,T,W,R,E$.
\item $P$ is the $\Pkg$ provenance over $\BertrandSeriesTestUp$, $\RaabeDuhamelTestUp$, $\KummerTestUp$, $\GaussTestUp$, $\RegularCauchyRatioTestUp$, $\RegularCauchyComparisonTestUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BertrandSeriesTestUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,Q,L,D,T,W,R,E,H,C,P,N$. It has no coordinate for host summability, quotient series equality, selected sums, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Bertrand series test NameCert obligations]\label{thm:bertrand-series-test-namecert-obligations}
For every accepted $\BertrandSeriesTestUp$ carrier
$$
B=(S,Q,L,D,T,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\BertrandSeriesTestUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the source real-series windows, adjacent quotient windows, logarithmic defect ledger, dyadic domination ledger, comparison-test handoff, stream windows, $\RegSeqRatUp$ readback, $\RealUp$ seal, transport, replay, provenance, and local naming row;
\item quotient-window exactness, requiring every quotient read used by the Bertrand defect to be read from the displayed neighboring positive-term windows in $S$ and $Q$;
\item logarithmic-defect stability, requiring the finite index, logarithmic scale, rational margin, and second-order defect rows in $L$ to remain attached to the accepted tail request;
\item dyadic domination routing, requiring $D$ and $W$ to convert the displayed logarithmic defect data into the same finite comparison windows consumed by $T$;
\item regular and Real handoff, requiring endpoint-facing data to pass through $T,W,R,E$ in that order before a $\RegSeqRatUp$ or $\RealUp$ consumer receives data;
\item ledger non-escape, requiring $H,C,P,N$ to transport, replay, source, and name only the displayed rows and to exclude host summability, quotient series equality, selected sums, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, and bridge checking.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bertrand-series-test-carrier}. Carrier admission is exactly the inventory $S,Q,L,D,T,W,R,E,H,C,P,N$, so the local certificate has no detached row for an infinite sum, quotient series equality, selected sum, or ambient completeness theorem.

The quotient and logarithmic-defect clauses are read before the comparison handoff. The row $S$ exposes only inspected positive-term and partial-sum windows, the row $Q$ exposes only neighboring-term quotient windows, and $L$ records the finite index, logarithmic scale, rational margin, and second-order defect attached to the current tail request.

The domination route is then forced through $D,W,T$. The dyadic ledger $D$ converts the displayed logarithmic defect data into comparison evidence, the window row $W$ fixes the finite stream windows, and the comparison-test row $T$ is the only route that may export a regular Cauchy conclusion.

Finally, $R$ and $E$ supply the regular rational and Real-facing endpoint route. The structural rows $H,C,P,N$ preserve, replay, source, and name the same route, so none of them can introduce host summability, quotient series equality, selected sums, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge data.
\end{proof}

\begin{theorem}[Bertrand series test tail route]\label{thm:bertrand-series-test-tail-route}
Every endpoint-facing read of an accepted $\BertrandSeriesTestUp$ carrier factors through
$$
\RaabeDuhamelTestUp,\ \KummerTestUp,\ \GaussTestUp,\ \RegularCauchyRatioTestUp,\ \RegularCauchyComparisonTestUp,\ \DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp .
$$
Concretely, the read consumes source real-series windows and adjacent quotient rows, reads the logarithmic second-order defect ledger, converts that ledger into dyadic domination and stream-window evidence, enters the $\RegularCauchyComparisonTestUp$ handoff, and reaches $\RegSeqRatUp$ and $\RealUp$ only through the displayed readback and seal rows.
\end{theorem}
\begin{proof}
Begin with an endpoint-facing read of an accepted carrier. By \autoref{def:bertrand-series-test-carrier}, the only source data available before the defect estimate are the source windows in $S$ and the adjacent quotient windows in $Q$.

The Bertrand-specific data are then read from $L$ and $D$. The logarithmic ledger supplies the finite index, logarithmic scale, rational margin, and second-order defect, while the dyadic ledger turns those rows into the domination evidence needed for comparison.

The neighboring quotient-defect tests are reference faces in the same lattice. The Raabe--Duhamel, Kummer, and Gauss packets identify the adjacent first-order and auxiliary defect surfaces, while the ratio route supplies the shared adjacent-quotient discipline. The accepted $\BertrandSeriesTestUp$ endpoint route still has to enter the comparison handoff $T$.

After $T$ is displayed, the route proceeds through the finite $\StreamNameUp$ windows in $W$, the $\RegSeqRatUp$ readback row $R$, and the terminal $\RealUp$ seal $E$. The transport, replay, provenance, and naming rows keep the same route, so no host summability, quotient series equality, selected sums, countable choice, trichotomy, or ambient $\RealUp$ completeness can enter.
\end{proof}

\falsifiablePrediction{Every accepted $\BertrandSeriesTestUp$ endpoint-facing read factors through source real-series windows, adjacent quotient rows, a logarithmic defect ledger, dyadic domination, a RegularCauchyComparisonTest handoff, StreamName windows, RegSeqRat readback, a RealUp seal, hsame transport, Cont replay, Pkg provenance, and local naming.}

\independenceWitness{raabeduhameltest, kummertest, gausstest, regularcauchyratiotest, regularcauchycomparisontest, dyadicratcore, streamname, regseqrat, real: $\BertrandSeriesTestUp$ is not bijective to any listed sibling because it jointly carries source series windows, adjacent quotient windows, a logarithmic second-order defect ledger, dyadic domination, comparison-test routing, regular rational readback, Real sealing, transport, replay, provenance, and local naming.}

\closureat{BertrandSeriesTestUp}{seedStr}

\begin{closurestatus}{\BertrandSeriesTestUp}
  \constructivestory{A $\BertrandSeriesTestUp$ packet is generated from source real-series windows, adjacent quotient windows, a logarithmic second-order defect ledger, dyadic domination, a RegularCauchyComparisonTest handoff, StreamName windows, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bertrand-series-test-carrier}, \autoref{thm:bertrand-series-test-namecert-obligations}, and \autoref{thm:bertrand-series-test-tail-route} seed the finite Bertrand convergence-test boundary over displayed source series, quotient, logarithmic defect, comparison, $\RegSeqRatUp$, and $\RealUp$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host summability, quotient series equality, selected sums, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires Lean theorems for carrier admission, quotient-window exactness, logarithmic-defect stability, dyadic domination routing, regular and Real handoff, ledger non-escape, and TasteGate field faithfulness for BEDC.Derived.BertrandSeriesTestUp.}
\end{closurestatus}
