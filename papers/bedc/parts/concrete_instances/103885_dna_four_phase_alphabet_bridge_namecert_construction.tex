\chapter{A Concrete Naming Certificate for $\DNAFourPhaseAlphabetBridgeUp$}
\label{ch:concrete-instances-dna-four-phase-alphabet-bridge-namecert}
\origin{human}

The $\DNAFourPhaseAlphabetBridgeUp$ packet records the traditional genetic-code four-base alphabet as a conservative finite readback over BEDC finite-data carriers. It uses the two-endpoint $\BoolUp$ surface of \autoref{ch:concrete-instances-bool-namecert} and the componentwise product discipline of \autoref{ch:concrete-instances-prod-namecert}, while its source vision is the DNA four-phase bridge of \autoref{subsec:philosophy-channel-genetic-code-bridge}. The packet names a finite external alphabet bridge; it does not prove a molecular-biology theorem, identify BEDC channel glyphs with bases, or turn biological bases into BEDC primitives.

\begin{definition}[DNAFourPhaseAlphabetBridge carrier]
\label{def:dna-four-phase-alphabet-bridge-carrier}
A $\DNAFourPhaseAlphabetBridgeUp$ carrier is a finite $\BHist$ packet
$$
D=(B,Q,A,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\BoolUp$ endpoint row. It supplies the two finite axes used by the readback.
\item $Q$ is the $\ProdUp$ quadrant row. It records the four displayed pairs of Bool endpoints without adding a fifth phase or quotienting any pair.
\item $A$ is the external alphabet row. It names the target-side symbols $\mathrm{A}$, $\mathrm{C}$, $\mathrm{G}$, and $\mathrm{T}$ only as readback labels.
\item $E$ is the bridge-exactness row. It records that each displayed quadrant has exactly one external alphabet label and that each external label is read from exactly one displayed quadrant.
\item $H$ records componentwise $\hsame$ transport for the Bool axes, product quadrant, external alphabet label, exactness, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
B,Q \longmapsto A,E \longmapsto H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\DNAFourPhaseAlphabetBridgeUp$, $\BoolUp$, $\ProdUp$, the source vision bridge, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\DNAFourPhaseAlphabetBridgeUp}$ row.
\end{enumerate}
The classifier compares accepted packets through $B,Q,A,E,H,C,P,N$. It has no coordinate for molecular identity, biological translation correctness, mutation dynamics, channel-glyph equality, or a global genetic-code theorem.
\end{definition}

\begin{definition}[DNAFourPhaseAlphabetBridge classifier]
\label{def:dna-four-phase-alphabet-bridge-classifier}
Two accepted $\DNAFourPhaseAlphabetBridgeUp$ packets classify together exactly when their Bool endpoint rows, product quadrant rows, external alphabet rows, exactness rows, transport rows, replay rows, provenance rows, and local naming rows classify componentwise by $\hsame$.
The external alphabet labels are compared only as the target-side labels recorded by the bridge row; they are not BEDC source constructors.
\end{definition}

\begin{theorem}[DNAFourPhaseAlphabetBridge NameCert obligations]
\label{thm:dna-four-phase-alphabet-bridge-namecert-obligations}
For every accepted $\DNAFourPhaseAlphabetBridgeUp$ carrier $D=(B,Q,A,E,H,C,P,N)$, the local $\NameCert_{\DNAFourPhaseAlphabetBridgeUp}$ surface consists exactly of Bool endpoint admission, product quadrant formation, four-label external alphabet readback, quadrant-label exactness, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the accepted carrier. The row $B$ supplies only the two finite Bool axes, and the row $Q$ forms their product quadrants through the existing product discipline.

The row $A$ names the four external alphabet labels, while $E$ records the finite readback exactness between quadrants and labels. These rows are target-side readbacks and do not create source constructors for biological bases.

Finally $H,C,P,N$ transport, replay, source, and locally name the same route. Since the carrier contains no molecular identity row, no translation-correctness row, and no channel-glyph identification row, the listed obligations exhaust the local certificate surface.
\end{proof}

\begin{theorem}[DNAFourPhaseAlphabetBridge readback boundary]
\label{thm:dna-four-phase-alphabet-bridge-readback-boundary}
Every accepted $\DNAFourPhaseAlphabetBridgeUp$ readback factors through
$$
B,Q \longmapsto A,E
$$
before it can be used as four-phase alphabet evidence. Thus the packet exports a conservative finite readback boundary, not an identification between BEDC channel glyphs and DNA bases.
\end{theorem}
\begin{proof}
Read $B$ to obtain the two finite axes and $Q$ to obtain the four displayed product quadrants. These rows are internal finite-data rows.

Only after those rows are displayed may the external alphabet row $A$ and exactness row $E$ be inspected. The bridge therefore keeps internal finite carriers and external biological labels in separate rows.

Any consumer that treats the labels as BEDC channel glyphs or as a molecular-biology theorem bypasses the accepted carrier boundary.
\end{proof}

\falsifiablePrediction{An accepted $\DNAFourPhaseAlphabetBridgeUp$ packet that identifies BEDC channel glyphs with $\mathrm{A}$, $\mathrm{C}$, $\mathrm{G}$, or $\mathrm{T}$ fails the classifier row, because the accepted carrier records only Bool endpoint axes, product quadrants, external alphabet labels, exactness, transport, replay, provenance, and local naming.}

\independenceWitness{Bool, Prod, channel genetic-code bridge: $\DNAFourPhaseAlphabetBridgeUp$ is not bijective to any listed sibling because it binds two finite Bool axes and product quadrant formation to an external four-label readback while preserving the separation between BEDC finite carriers and biological alphabet labels.}

The Lean TasteGate follow-up is \texttt{BEDC.Derived.DNAFourPhaseAlphabetBridgeUp}, with explicit $\Nontrivial\ \DNAFourPhaseAlphabetBridgeUp$ and $\FieldFaithful\ \DNAFourPhaseAlphabetBridgeUp$ instances preserving $B,Q,A,E,H,C,P,N$ as separate displayed fields.

\closureat{DNAFourPhaseAlphabetBridgeUp}{seedStr}

\begin{closurestatus}{\DNAFourPhaseAlphabetBridgeUp}
  \constructivestory{A $\DNAFourPhaseAlphabetBridgeUp$ packet is generated from BoolUp endpoint axes, a ProdUp quadrant row, a four-label external DNA alphabet readback row, bridge exactness, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dna-four-phase-alphabet-bridge-carrier}, \autoref{def:dna-four-phase-alphabet-bridge-classifier}, \autoref{thm:dna-four-phase-alphabet-bridge-namecert-obligations}, and \autoref{thm:dna-four-phase-alphabet-bridge-readback-boundary} seed the finite four-phase alphabet bridge and its readback boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim molecular-biology theorems, biological translation correctness, channel-glyph identification, mutation dynamics, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier transport, quadrant-label exactness, readback boundary, nontriviality, and field faithfulness for BEDC.Derived.DNAFourPhaseAlphabetBridgeUp.}
\end{closurestatus}
