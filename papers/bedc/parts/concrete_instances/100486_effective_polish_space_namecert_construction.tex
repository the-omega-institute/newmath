\chapter{A Concrete Naming Certificate for $\EffectivePolishSpaceUp$}
\label{ch:concrete-instances-effective-polish-space-namecert}
\origin{human}

Following Alexandrov-Urysohn and Weihrauch, the $\EffectivePolishSpaceUp$ packet records complete separable metric data with a visible dense enumeration and explicit Cauchy realizer route before any Polish or Real consumer reads the completed object. It sits over the Polish-space boundary of \autoref{ch:concrete-instances-polishspace-namecert}, the indexed dense-window surface of \autoref{ch:concrete-instances-countable-dense-subset-namecert}, the explicit-modulus Cauchy route of \autoref{ch:concrete-instances-effective-cauchy-sequence-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names effective complete-separable metric data on the current Real and Completion route; it is not a choice-selected dense set, quotient completion equality, open-cover compactness, host equality, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[Effective Polish space carrier]\label{def:effective-polish-space-carrier}
An $\EffectivePolishSpaceUp$ carrier is a finite $\BHist$ packet
$$
E=(P,D,Q,S,R,M,L,H,C,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $P$ is the $\PolishSpaceUp$ row, carrying the complete separable metric surface that downstream Polish consumers are allowed to inspect.
\item $D$ is the $\CountableDenseSubsetUp$ row, exposing the indexed dense family and the finite dense windows cited by precision requests.
\item $Q$ is the $\EffectiveCauchySequenceUp$ row, carrying the explicit-modulus Cauchy data generated from the displayed dense indices.
\item $S$ is the $\StreamNameUp$ row, listing the finite observation windows through which dense indices and Cauchy stages are read.
\item $R$ is the $\RegSeqRatUp$ readback row obtained only after the dense index, stream window, and effective Cauchy rows have been displayed.
\item $M$ is the dyadic and Real tolerance ledger, storing radii, slack rows, comparison budgets, and seal thresholds used by $D,Q,S,R$.
\item $L$ is the terminal $\RealUp$ seal row reached by the displayed readback route.
\item $H$ records componentwise $\hsame$ transport for the Polish row, dense row, effective Cauchy row, stream windows, regular readback, tolerance ledger, Real seal, and structural rows.
\item $C$ records displayed $\Cont$ replay from a Polish or Real request through $D,S,Q,R,M,L,H,G,N$.
\item $G$ is the $\Pkg$ provenance over $\EffectivePolishSpaceUp$, $\PolishSpaceUp$, $\CountableDenseSubsetUp$, $\EffectiveCauchySequenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\EffectivePolishSpaceUp}$ row.
\end{enumerate}
The carrier predicate asserts exactly that these rows form one finite effective complete-separable metric packet. It has no coordinate for choice-selected dense points, quotient completion equality, open-cover compactness, ambient Real completeness, host equality, trichotomy, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Effective Polish space NameCert obligations]\label{thm:effective-polish-space-namecert-obligations}
For every accepted $\EffectivePolishSpaceUp$ carrier $E=(P,D,Q,S,R,M,L,H,C,G,N)$, the local $\NameCert_{\EffectivePolishSpaceUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the displayed Polish, dense, effective Cauchy, StreamName, RegSeqRat, tolerance-ledger, Real-seal, transport, replay, provenance, and local naming rows;
\item dense enumerator visibility, requiring every finite dense-window request to read the indexed row $D$ before it reaches $S,Q,R$, or $L$;
\item effective Cauchy realizer handoff, requiring every Cauchy realization to pass through the explicit row $Q$ and its displayed stream windows before regular rational readback;
\item complete-separable metric compatibility, requiring the Polish row $P$ to be read only through the same dense family, explicit Cauchy route, tolerance ledger, and Real seal used by downstream consumers;
\item nonescape, excluding choice-selected dense points, quotient completion equality, open-cover compactness, ambient $\RealUp$ completeness, host equality, trichotomy, Lean verification, and bridge checking.
\end{enumerate}
No effective-Polish-space obligation is stored outside these rows.
\end{theorem}
\leanchecked{BEDC.Derived.EffectivePolishSpaceUp.EffectivePolishSpaceUpTasteGate\_single\_carrier\_alignment}
\begin{proof}
Carrier admission is the projection of \autoref{def:effective-polish-space-carrier}. The projected rows are exactly the Polish-space row $P$, dense enumerator $D$, effective Cauchy row $Q$, finite stream windows $S$, regular readback $R$, tolerance ledger $M$, Real seal $L$, and structural rows $H,C,G,N$.

Dense enumerator visibility is supplied by \autoref{ch:concrete-instances-countable-dense-subset-namecert}. A finite approximation request must first cite the displayed index row $D$ before the corresponding window can be served by \autoref{ch:concrete-instances-streamname-namecert}.

The effective Cauchy handoff uses \autoref{ch:concrete-instances-effective-cauchy-sequence-namecert}. Once the dense index and stream window have been displayed, the row $Q$ supplies the explicit modulus route, and \autoref{ch:concrete-instances-regseqrat-namecert} reads the regular rational handoff only downstream of that route.

Metric compatibility is read through \autoref{ch:concrete-instances-polishspace-namecert} and the terminal seal of \autoref{ch:concrete-instances-real-namecert}. Since $P$ and $L$ are carried in the same finite packet as $D,Q,S,R,M$, the complete-separable metric surface cannot introduce a second dense family, hidden completion equality, or ambient completeness coordinate.

Finally, $H,C,G,N$ transport, replay, package, and name the same displayed rows. They add no analytic coordinate beyond the carrier, so the five listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Effective Polish space realizer handoff]\label{thm:effective-polish-space-realizer-handoff}
Every Polish-space or Real-facing consumer of an accepted $\EffectivePolishSpaceUp$ carrier $E=(P,D,Q,S,R,M,L,H,C,G,N)$ factors through the displayed route
$$
D \longmapsto S \longmapsto Q \longmapsto R \longmapsto M \longmapsto L \longmapsto P,H,C,G,N .
$$
Thus the consumer receives the displayed dense index, finite StreamName windows, EffectiveCauchySequence modulus route, RegSeqRat readback, dyadic and Real tolerance ledger, Real seal, Polish row, transport, replay, provenance, and local naming before it can inspect the completed object.
\end{theorem}
\begin{proof}
Start with a Polish-space or Real-facing read of $E$. By \autoref{def:effective-polish-space-carrier}, the only dense-coordinate row is $D$, and the only effective Cauchy route is $Q$.

The finite observation part reads $D$ through \autoref{ch:concrete-instances-countable-dense-subset-namecert} and then reads $S$ through \autoref{ch:concrete-instances-streamname-namecert}. These rows expose the dense index and the finite windows without selecting a hidden dense point.

The realization part is then forced through \autoref{ch:concrete-instances-effective-cauchy-sequence-namecert}. The row $Q$ supplies the explicit modulus route; the row $R$ records the regular rational readback from \autoref{ch:concrete-instances-regseqrat-namecert}; and the ledger $M$ records the tolerance data used before the terminal seal $L$ is read.

The Polish row $P$ and the Real seal $L$ are therefore downstream of the same finite route. The structural rows $H,C,G,N$ preserve and name that route, so every consumer factors through the displayed handoff and has no bypass through choice, quotient completion equality, open-cover compactness, host equality, trichotomy, or ambient Real completeness.
\end{proof}

\begin{theorem}[Effective Polish space non-escape]\label{thm:effective-polish-space-nonescape}
Let $E=(P,D,Q,S,R,M,L,H,C,G,N)$ be an accepted $\EffectivePolishSpaceUp$ carrier. No consumer of $E$ can read choice-selected dense points, quotient completion equality, open-cover compactness, ambient $\RealUp$ completeness, host equality, or trichotomy from the packet unless that datum is already one of the displayed rows $P,D,Q,S,R,M,L,H,C,G,N$.
\end{theorem}
\begin{proof}
Inspect the carrier rows in \autoref{def:effective-polish-space-carrier}. The analytic rows are only the Polish-space row, countable dense row, effective Cauchy row, finite stream windows, regular readback, tolerance ledger, and terminal Real seal.

The structural rows $H,C,G,N$ provide sameness transport, replay, provenance, and local naming for those same rows. They do not add a selector for dense points, a quotient-equality principle for completions, an open-cover compactness theorem, an ambient completeness theorem, a host equality predicate, or trichotomy.

Therefore the excluded data have no carrier coordinate, replay edge, provenance entry, or local NameCert row from which to be read. Any consumer of $E$ is confined to the displayed effective complete-separable route.
\end{proof}

\falsifiablePrediction{Every $\EffectivePolishSpaceUp$ Polish-space or Real-facing read factors through a displayed dense index, StreamName finite windows, EffectiveCauchySequence modulus route, RegSeqRat readback, dyadic and Real tolerance ledger, RealUp seal, Polish row, transport, replay, provenance, and local naming.}

\independenceWitness{polishspace, countable-dense-subset, effective-cauchy-sequence, streamname, regseqrat, real: $\EffectivePolishSpaceUp$ is not bijective to any listed sibling because it jointly carries a Polish row, indexed dense family, effective Cauchy realizer, stream windows, regular readback, tolerance ledger, Real seal, transport, replay, provenance, and local naming.}

\closureat{EffectivePolishSpaceUp}{seedStr}
\begin{closurestatus}{\EffectivePolishSpaceUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:effective-polish-space-carrier}, \autoref{thm:effective-polish-space-namecert-obligations}, \autoref{thm:effective-polish-space-realizer-handoff}, and \autoref{thm:effective-polish-space-nonescape} seed the finite effective complete-separable metric route through $\PolishSpaceUp$, $\CountableDenseSubsetUp$, $\EffectiveCauchySequenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close choice-selected dense points, quotient completion equality, open-cover compactness, ambient $\RealUp$ completeness, host equality, trichotomy, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dense enumerator visibility, effective Cauchy realizer handoff, complete-separable metric compatibility, transport and replay stability, and nonescape for BEDC.Derived.EffectivePolishSpaceUp.}
  \constructivestory{An $\EffectivePolishSpaceUp$ packet is generated from a displayed PolishSpace row, CountableDenseSubset row, EffectiveCauchySequence row, StreamName finite windows, RegSeqRat readback, dyadic and Real tolerance ledger, RealUp seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
\end{closurestatus}
