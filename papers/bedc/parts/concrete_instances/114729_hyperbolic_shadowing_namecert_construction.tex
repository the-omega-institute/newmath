\chapter{A Concrete Naming Certificate for $\HyperbolicShadowingUp$}
\label{ch:concrete-instances-hyperbolic-shadowing-namecert}
\origin{ai}

The $\HyperbolicShadowingUp$ packet realizes the quasi-geodesic fellow-traveller route of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite shadowing data. It sits after the metric comparison rows of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the quasi-geodesic shadow packet of \autoref{ch:concrete-instances-hyperbolic-quasi-geodesic-shadow-namecert}, the Morse-boundary stability rows of \autoref{ch:concrete-instances-morse-boundary-namecert}, and the boundary-window route of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}. The packet names finite fellow-traveller windows, boundary attachments, Busemann tolerance rows, and local NameCert data only; it does not complete an ideal boundary, select a global geodesic representative, quotient paths, import a host hyperbolic space, or identify the observer-expansion reading with physical dynamics.

\begin{definition}[Hyperbolic shadowing carrier]
\label{def:hyperbolic-shadowing-carrier}
A $\HyperbolicShadowingUp$ carrier is a finite $\BHist$ packet
$$
S=(M,Q,F,B,U,D,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\HyperbolicMetricUp$ row, exposing the finite metric comparisons used to read a quasi-geodesic route.
\item $Q$ is the $\HyperbolicQuasiGeodesicShadowUp$ row, recording the route samples, distortion budget, boundary endpoint, and visual shadow window being shadowed.
\item $F$ is the fellow-traveller row, naming the finite comparison windows in which the route and the shadowing geodesic remain within the displayed tolerance.
\item $B$ is the $\MorseBoundaryUp$ row that records the stability gauge and boundary-facing direction used by the fellow-traveller comparison.
\item $U$ is the $\HyperbolicBoundaryShadowUp$ row, recording the finite boundary-shadow window attached to the same endpoint data.
\item $D$ is the Busemann-tolerance row, carrying additive comparison budgets for the route, shadow window, and boundary attachment.
\item $R$ is the $\RealUp$ seal row for metric distances, tolerance budgets, and Busemann comparisons.
\item $H$ records componentwise $\hsame$ transport for metric, quasi-geodesic, fellow-traveller, Morse-boundary, boundary-shadow, Busemann, Real, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a shadowing request through $M,Q,F,B,U,D,R$.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicShadowingUp$, $\HyperbolicMetricUp$, $\HyperbolicQuasiGeodesicShadowUp$, $\MorseBoundaryUp$, $\HyperbolicBoundaryShadowUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicShadowingUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $M,Q,F,B,U,D,R,H,C,P,N$. It has no coordinate for completed ideal-boundary quotients, selected global geodesics, quotient path equality, host hyperbolic compactification, physical observer trajectories, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Hyperbolic shadowing NameCert obligations]
\label{thm:hyperbolic-shadowing-namecert-obligations}
For every accepted $\HyperbolicShadowingUp$ carrier
$$
S=(M,Q,F,B,U,D,R,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicShadowingUp}$ surface consists exactly of HyperbolicMetric comparison rows, HyperbolicQuasiGeodesicShadow route rows, finite fellow-traveller windows, MorseBoundary stability rows, HyperbolicBoundaryShadow attachment rows, Busemann tolerance rows, Real seals, componentwise transport, displayed continuation replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-shadowing-carrier}. The rows $M,Q,F,B,U,D,R,H,C,P,N$ are the complete certificate surface.

The route and metric rows are read first. The metric row $M$ supplies only displayed distance comparisons, and $Q$ supplies the finite quasi-geodesic shadow data already named by \autoref{ch:concrete-instances-hyperbolic-quasi-geodesic-shadow-namecert}.

The fellow-traveller and boundary rows then bind the shadowing claim. The row $F$ records finite comparison windows, while $B$ and $U$ attach those windows to the Morse-boundary and boundary-shadow surfaces.

Finally $D,R,H,C,P,N$ compare, seal, transport, replay, source, and name the same route. Since no host coordinate is present, the obligations cannot export completed boundaries, selected global geodesics, quotient path equality, physical observer dynamics, or ambient real completeness.
\end{proof}

\begin{theorem}[Hyperbolic shadowing fellow-traveller handoff]
\label{thm:hyperbolic-shadowing-fellow-traveller-handoff}
Every observer-expansion consumer of an accepted $\HyperbolicShadowingUp$ carrier must read the route
$$
\HyperbolicMetricUp,\ \HyperbolicQuasiGeodesicShadowUp,\ \MorseBoundaryUp,\ \HyperbolicBoundaryShadowUp,\ \RealUp
\longmapsto
\HyperbolicShadowingUp
$$
through finite metric comparisons, quasi-geodesic shadow rows, fellow-traveller windows, Morse stability, boundary-shadow attachment, Busemann tolerance, Real seals, transport, replay, provenance, and local naming. The handoff realizes the hyperbolic observer-expansion shadow route as finite NameCert data.
\end{theorem}
\begin{proof}
Begin with an accepted carrier $S=(M,Q,F,B,U,D,R,H,C,P,N)$. A shadowing consumer can inspect the quasi-geodesic route only after the metric row $M$ and the quasi-geodesic shadow row $Q$ have been displayed.

The fellow-traveller comparison is then forced through $F$. This row names finite comparison windows and does not select a global geodesic representative or quotient path class.

The boundary-facing evidence is read through $B,U,D,R$. The Morse-boundary row supplies a finite stability gauge, the boundary-shadow row supplies the endpoint window, the Busemann row supplies the additive tolerance, and the Real row seals the displayed comparisons.

The structural rows $H,C,P,N$ transport, replay, source, and name that finite route for observer-expansion consumers. No completed ideal boundary, host hyperbolic compactification, physical observer trajectory, quotient geodesic equality, or ambient completeness row can be inserted into the handoff.
\end{proof}

\closureat{HyperbolicShadowingUp}{seedStr}

\begin{closurestatus}{\HyperbolicShadowingUp}
  \constructivestory{A $\HyperbolicShadowingUp$ packet is generated from HyperbolicMetric comparison rows, HyperbolicQuasiGeodesicShadow route rows, finite fellow-traveller windows, MorseBoundary stability rows, HyperbolicBoundaryShadow attachment rows, Busemann tolerance rows, Real seals, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-shadowing-carrier}, \autoref{thm:hyperbolic-shadowing-namecert-obligations}, and \autoref{thm:hyperbolic-shadowing-fellow-traveller-handoff} seed the finite hyperbolic shadowing route for observer-expansion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim completed boundary quotients, selected global geodesics, quotient path equality, host hyperbolic compactification, physical observer trajectories, ambient RealUp completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, metric comparison routing, quasi-geodesic shadow routing, fellow-traveller windows, MorseBoundary stability, HyperbolicBoundaryShadow attachment, Busemann tolerance, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicShadowingUp.}
\end{closurestatus}
