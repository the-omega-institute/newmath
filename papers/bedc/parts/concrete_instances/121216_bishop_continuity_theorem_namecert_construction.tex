\chapter{A Concrete Naming Certificate for $\BishopContinuityTheoremUp$}
\label{ch:concrete-instances-bishop-continuity-theorem-namecert}
\origin{human}

Following Bishop and Bridges, constructive continuity theorems are read here as finite modulus-producing routes over displayed names rather than as host statements about arbitrary functions. The $\BishopContinuityTheoremUp$ packet sits between the fan-continuity route of \autoref{ch:concrete-instances-fan-continuity-principle-namecert}, the bar-induction continuity route of \autoref{ch:concrete-instances-bar-induction-continuity-namecert}, the local modulus handoff of \autoref{ch:concrete-instances-bishop-continuity-modulus-namecert}, the finite observation surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names the theorem surface by which fan or bar continuity evidence is routed into a Bishop local modulus for Real and RegSeqRat consumers; it does not assert choice, weak limited principle of omniscience, quotient equality of real names, a selected host functional, or ambient $\RealUp$ completeness.

\begin{definition}[Bishop continuity theorem carrier]
\label{def:bishop-continuity-theorem-carrier}
A $\BishopContinuityTheoremUp$ carrier is a finite $\BHist$ packet
$$
T=(F,B,M,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\FanContinuityPrincipleUp$ route, when the theorem is read through a fan-continuity source.
\item $B$ is the $\BarInductionContinuityUp$ route, when the theorem is read through a bar-induction source.
\item $M$ is the $\BishopContinuityModulusUp$ handoff row produced by the selected displayed source route.
\item $S$ is the $\StreamNameUp$ finite-window row used to inspect the source and image names before the modulus is consumed.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from those finite windows and the modulus-controlled tolerance ledger.
\item $E$ is the terminal $\RealUp$ seal read only after $F$ or $B$, $M$, $S$, and $R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the fan route, bar route, modulus row, window row, readback row, real seal, and local certificate row.
\item $C$ records displayed $\Cont$ replay from the chosen continuity source through $M,S,R,E$.
\item $P$ is the $\Pkg$ provenance over $\BishopContinuityTheoremUp$, $\FanContinuityPrincipleUp$, $\BarInductionContinuityUp$, $\BishopContinuityModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\BishopContinuityTheoremUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $F,B,M,S,R,E,H,C,P,N$ and the displayed choice of continuity source. It has no coordinate for a choice principle, weak limited principle of omniscience, quotient equality of real names, selected host functional, detached semantic continuity theorem, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop continuity theorem NameCert obligations]
\label{thm:bishop-continuity-theorem-namecert-obligations}
For the carrier of \autoref{def:bishop-continuity-theorem-carrier}, the local $\NameCert_{\BishopContinuityTheoremUp}$ surface consists exactly of carrier admission for $T=(F,B,M,S,R,E,H,C,P,N)$, continuity-source exposure through $F$ or $B$, local modulus handoff through $M$, finite observation through $S$, regular rational readback through $R$, terminal real seal through $E$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier to its displayed rows. The theorem source is either the fan-continuity row $F$ or the bar-induction continuity row $B$, and no third continuity-source coordinate is available.

Read the modulus row next. The local handoff $M$ is the $\BishopContinuityModulusUp$ surface consumed by the theorem, so the source route can affect downstream Real or RegSeqRat consumers only by producing that displayed modulus row.

Finally route the numeric observations through $S,R,E$. The $\StreamNameUp$ row supplies finite windows, the $\RegSeqRatUp$ row reads them back as regular rational data, and the $\RealUp$ seal is reached only after those rows are displayed. The structural rows $H,C,P,N$ transport, replay, source, and name the same finite packet, so the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Bishop continuity theorem fan-modulus handoff]
\label{thm:bishop-continuity-theorem-fan-modulus-handoff}
Whenever an accepted $\BishopContinuityTheoremUp$ packet uses the $\FanContinuityPrincipleUp$ source row $F$, its downstream continuity output factors through the $\BishopContinuityModulusUp$ row $M$ before any $\StreamNameUp$, $\RegSeqRatUp$, or $\RealUp$ consumer can read the theorem. The handoff is the finite route
$$
F \longmapsto M \longmapsto S \longmapsto R \longmapsto E,
$$
with transport, replay, provenance, and local naming confined to $H,C,P,N$.
\end{theorem}
\begin{proof}
Start with the source row $F$ in \autoref{def:bishop-continuity-theorem-carrier}. It carries the fan-continuity evidence only as the displayed packet of \autoref{ch:concrete-instances-fan-continuity-principle-namecert}.

Apply the NameCert obligations of \autoref{thm:bishop-continuity-theorem-namecert-obligations}. They require the theorem output to pass through $M$ before the finite observation and readback rows $S,R,E$ are visible.

The route is then fixed by the continuation row $C$. It replays the fan source into the local modulus, then into the stream windows, regular rational readback, and terminal real seal. Since $H,C,P,N$ are the only structural rows, no choice selector, quotient equality, host functional, or completeness principle can enter the handoff.
\end{proof}

\begin{theorem}[Bishop continuity theorem nonescape boundary]
\label{thm:bishop-continuity-theorem-nonescape-boundary}
Every $\RegSeqRatUp$ or $\RealUp$ consumer of an accepted $\BishopContinuityTheoremUp$ packet receives only the displayed continuity-source row $F$ or $B$, the local modulus row $M$, the finite $\StreamNameUp$ windows $S$, the $\RegSeqRatUp$ readback $R$, the terminal $\RealUp$ seal $E$, and the structural rows $H,C,P,N$. Such a consumer cannot extract choice, weak limited principle of omniscience, quotient equality of real names, a selected host functional, detached semantic continuity, a general compactness theorem, or ambient $\RealUp$ completeness from the packet.
\end{theorem}
\begin{proof}
Read the consumer request through \autoref{thm:bishop-continuity-theorem-namecert-obligations}. The only numeric-facing route reaches $R$ and $E$ after one displayed continuity source and the local modulus row have already been exposed.

If the source is fan continuity, use \autoref{thm:bishop-continuity-theorem-fan-modulus-handoff}. If the source is bar-induction continuity, the same carrier projection replaces $F$ by $B$ and still routes through $M,S,R,E$ before the consumer can read any endpoint data.

The excluded principles are absent from the carrier inventory. Transport by $H$, replay by $C$, provenance by $P$, and local naming by $N$ preserve the displayed finite rows; they do not create choice, WLPO, quotient equality, host functional selection, semantic continuity outside the packet, compactness, or real completeness.
\end{proof}

\falsifiablePrediction{Any accepted BishopContinuityTheoremUp packet that exports a RegSeqRat or Real continuity read without displaying a FanContinuityPrinciple or BarInductionContinuity source, a BishopContinuityModulus row, StreamName windows, RegSeqRat readback, and Real seal fails the carrier classifier.}

\independenceWitness{fancontinuityprinciple, barinductioncontinuity, bishopcontinuitymodulus, streamname, regseqrat, real: none is bijective with $\BishopContinuityTheoremUp$ because this carrier jointly records the theorem source, local modulus handoff, finite observation windows, regular rational readback, terminal real seal, transport, replay, provenance, and one local NameCert row while refusing choice, WLPO, quotient equality, host function selection, and ambient Real completeness.}

\closureat{BishopContinuityTheoremUp}{seedStr}

\begin{closurestatus}{\BishopContinuityTheoremUp}
  \constructivestory{A $\BishopContinuityTheoremUp$ packet is generated from a FanContinuityPrinciple or BarInductionContinuity source row, a BishopContinuityModulus handoff row, StreamName windows, RegSeqRat readback, terminal RealUp seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-continuity-theorem-carrier}, \autoref{thm:bishop-continuity-theorem-namecert-obligations}, \autoref{thm:bishop-continuity-theorem-fan-modulus-handoff}, \autoref{thm:bishop-continuity-theorem-nonescape-boundary}, the falsifiable prediction, and the independence witness seed the constructive L10 continuity-theorem surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close choice, WLPO, quotient equality of real names, selected host functionals, detached semantic continuity, general compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, fan and bar source exposure, BishopContinuityModulus handoff, StreamName windows, RegSeqRat readback, Real seal nonescape, transport, replay, provenance, and local NameCert rows for BEDC.Derived.BishopContinuityTheoremUp.}
\end{closurestatus}
