\chapter{A Concrete Naming Certificate for $\SobolevTraceCompactWindowUp$}
\label{ch:concrete-instances-sobolev-trace-compact-window-namecert}
\origin{human}

Following Adams--Fournier and Evans on Sobolev trace theorems, the $\SobolevTraceCompactWindowUp$ packet records the finite boundary-read window through which a Sobolev-class object on a compact metric domain exposes trace data to Real-facing consumers. It sits after the compact source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous representative route of \autoref{ch:concrete-instances-continuousmap-namecert}, the finite observation schedules of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names only a compact-window trace carrier; it does not assert a full Sobolev embedding theorem, a measure-theoretic trace space, open-cover compactness, or ambient $\RealUp$ completeness.

\begin{definition}[Sobolev trace compact-window carrier]
\label{def:sobolev-trace-compact-window-carrier}
A $\SobolevTraceCompactWindowUp$ carrier is a finite $\BHist$ packet
$$
T=(K,B,S,Q,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact-window domain row, carrying the $\CompactMetricUp$ source, the finite boundary neighbourhoods inspected by the trace request, and the compact metric window in which all reads occur.
\item $B$ is the boundary trace row, recording the finite side of the trace request, the boundary labels, and the local restriction data consumed by the packet.
\item $S$ is the $\StreamNameUp$ finite-window row for the domain and boundary observations opened before trace data is read.
\item $Q$ is the $\RegSeqRatUp$ readback row for the rational approximations attached to the interior and boundary observations.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, listing the dyadic precision requests and comparisons used by the trace window.
\item $E$ is the terminal $\RealUp$ seal for the trace values and tolerance comparisons visible after $K,B,S,Q,D$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the compact window, trace row, stream windows, rational readback, dyadic tolerances, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a trace-window request through the same compact, boundary, stream, rational, dyadic, and Real rows.
\item $P$ is the $\Pkg$ provenance over $\SobolevTraceCompactWindowUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SobolevTraceCompactWindowUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,B,S,Q,D,E,H,C,P,N$. It has no coordinate for a measure-theoretic trace space, Sobolev embedding, quotient function equality, open-cover compactness, selected representative, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Sobolev trace compact-window NameCert obligations]
\label{thm:sobolev-trace-compact-window-namecert-obligations}
For every accepted $\SobolevTraceCompactWindowUp$ carrier $T=(K,B,S,Q,D,E,H,C,P,N)$, the local $\NameCert_{\SobolevTraceCompactWindowUp}$ surface consists exactly of carrier admission for the compact-window domain, boundary trace row, StreamName observations, RegSeqRat readback, DyadicRatCore tolerance ledger, Real seal, componentwise transport, displayed replay, provenance, and local naming rows; compact-window exposure through $K$; boundary-row exposure through $B$; finite-window and rational readback through $S,Q$; dyadic tolerance accounting through $D$; Real-seal nonescape through $E$; and preservation of the same route through $H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:sobolev-trace-compact-window-carrier}. Its visible rows are exactly $K,B,S,Q,D,E,H,C,P,N$, so every local obligation is read from one of those rows.

The analytic boundary side is finite. The compact metric window $K$ and boundary trace row $B$ are exposed before any rational or Real-facing consumer reads trace values.

The readback side is downstream. Stream windows $S$, regular rational readback $Q$, dyadic tolerances $D$, and the Real seal $E$ supply the only numerical data available to the trace request.

Finally $H,C,P,N$ transport, replay, source, and name the same route. Because the carrier stores no measure-theoretic trace space, Sobolev embedding theorem, quotient function equality, open-cover compactness principle, selected representative, or ambient completeness row, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Sobolev trace compact-window trace route]
\label{thm:sobolev-trace-compact-window-trace-route}
Every boundary trace read accepted by a $\SobolevTraceCompactWindowUp$ packet must first expose
$$
\CompactMetricUp,\ \ContinuousMapUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \DyadicRatCoreUp,\ \RealUp
$$
through the displayed rows $K,B,S,Q,D,E,H,C,P,N$. The trace consumer receives a compact metric domain window, a boundary trace row, finite StreamName observations, regular rational readback, dyadic tolerance comparisons, and a terminal Real seal; it receives no measure-theoretic trace space, global Sobolev embedding, quotient function equality, selected representative, open-cover compactness theorem, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with \autoref{thm:sobolev-trace-compact-window-namecert-obligations}. It requires carrier admission and compact-window exposure before any boundary trace read can inspect the packet.

The boundary side is the pair $K,B$. These rows bind the trace request to the displayed compact metric source and to the finite boundary labels, so a consumer cannot enter through an untracked host trace operator.

The numerical side is $S,Q,D,E$. These rows expose finite windows, rational readback, dyadic tolerances, and the terminal Real seal in that order. The remaining rows $H,C,P,N$ preserve the same route under transport, replay, provenance, and naming.
\end{proof}

\begin{theorem}[Sobolev trace compact-window compact handoff]
\label{thm:sobolev-trace-compact-window-compact-handoff}
Every compact-metric or Real-facing consumer of an accepted $\SobolevTraceCompactWindowUp$ packet reads the compact-window route
$$
K,B,S,Q,D,E
$$
before consuming a boundary trace value. The handoff remains a finite NameCert route over $\CompactMetricUp$, $\ContinuousMapUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows; it does not open an ambient compactness theorem, a measure-theoretic trace construction, quotient equality of representatives, countable choice, or ambient Real completeness.
\end{theorem}
\begin{proof}
Use \autoref{def:sobolev-trace-compact-window-carrier}. The compact-facing side is the displayed domain window $K$ together with the boundary trace row $B$.

Apply \autoref{thm:sobolev-trace-compact-window-trace-route}. It orders the finite StreamName observations, RegSeqRat readback, dyadic tolerance row, and Real seal after the compact and boundary rows.

The structural rows $H,C,P,N$ do not add analytic content; they only preserve the already displayed packet. Hence every compact handoff remains inside the named finite route and cannot import the excluded host objects.
\end{proof}

\begin{theorem}[Sobolev trace compact-window nonescape]
\label{thm:sobolev-trace-compact-window-nonescape}
Every accepted $\SobolevTraceCompactWindowUp$ public read factors through the displayed rows
$$
K,B,S,Q,D,E,H,C,P,N.
$$
Thus the packet exposes only a compact-window trace NameCert over $\CompactMetricUp$, $\ContinuousMapUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. It cannot export a full Sobolev embedding theorem, a measure-theoretic trace space, open-cover compactness, quotient function equality, countable choice, a selected representative, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:sobolev-trace-compact-window-carrier}. The public packet has exactly the ten displayed rows $K,B,S,Q,D,E,H,C,P,N$.

Apply \autoref{thm:sobolev-trace-compact-window-namecert-obligations} and \autoref{thm:sobolev-trace-compact-window-compact-handoff}. They force every trace-facing read to pass through the compact, boundary, finite-window, rational-readback, dyadic-tolerance, and Real-seal rows before structural transport and provenance are consumed.

The excluded analytic objects are not carrier coordinates and are not introduced by the structural rows. Therefore every public read remains inside the displayed compact-window trace route.
\end{proof}

\falsifiablePrediction{Every accepted $\SobolevTraceCompactWindowUp$ consumer displays a CompactMetric domain window, a boundary trace row, StreamName finite observations, RegSeqRat readback, DyadicRatCore tolerances, a RealUp seal, hsame transport, Cont replay, Pkg provenance, and a local NameCert row before any boundary trace value is consumed.}

\independenceWitness{compactmetric, continuousmap, streamname, regseqrat, dyadicratcore, real: no listed sibling is bijective with $\SobolevTraceCompactWindowUp$ because this packet alone binds a compact-window boundary trace row to the finite Real-facing readback route.}

\closureat{SobolevTraceCompactWindowUp}{seedStr}

\begin{closurestatus}{\SobolevTraceCompactWindowUp}
  \constructivestory{A $\SobolevTraceCompactWindowUp$ packet is generated from a compact metric domain window, a boundary trace row, finite StreamName observations, RegSeqRat readback, DyadicRatCore tolerances, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:sobolev-trace-compact-window-carrier}, \autoref{thm:sobolev-trace-compact-window-namecert-obligations}, \autoref{thm:sobolev-trace-compact-window-trace-route}, \autoref{thm:sobolev-trace-compact-window-compact-handoff}, and \autoref{thm:sobolev-trace-compact-window-nonescape} seed the finite compact-window trace route over compact, continuous-map, stream, rational-readback, dyadic-tolerance, and Real-seal data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, full Sobolev embedding, measure-theoretic trace theory, open-cover compactness, quotient function equality, countable choice, selected representatives, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-window exposure, boundary trace exactness, finite-window readback, dyadic tolerance accounting, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.SobolevTraceCompactWindowUp.}
\end{closurestatus}
