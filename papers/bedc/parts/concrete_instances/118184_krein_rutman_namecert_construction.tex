\chapter{A Concrete Naming Certificate for $\KreinRutmanUp$}
\label{ch:concrete-instances-krein-rutman-namecert}
\origin{human}

Following Krein and Rutman, the $\KreinRutmanUp$ packet records the finite NameCert surface for a compact positive operator acting on a cone inside an ordered Banach-style environment. It sits over the Banach carrier of \autoref{ch:concrete-instances-banach-namecert}, the compact-operator surface of \autoref{ch:concrete-instances-compactoperator-namecert}, the scalar $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}, and the compact-convex spectral sibling of \autoref{ch:concrete-instances-krein-milman-compact-convex-namecert}. The packet names the dependency route for a positive eigenvector handoff; it does not assert a host spectral theorem, a chosen spectral radius, or a completed cone interior principle.

\begin{definition}[Krein--Rutman carrier]
\label{def:krein-rutman-carrier}
A $\KreinRutmanUp$ carrier is a finite $\BHist$ packet
$$
K=(B,C,T,Q,\rho,V,H,R,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the ordered Banach row, carrying the normed vector carrier, order relation, cone membership, and real scalar reads used by the packet.
\item $C$ is the positive cone row, exposing finite cone-membership, nonzero, order, and normalization tests.
\item $T$ is the compact positive operator row, read through $\CompactOperatorUp$, recording the finite operator graph windows and the proof rows that $T(C)\subseteq C$.
\item $Q$ is the compact orbit-window row, listing the finitely many normalized iterate, image, and enclosure rows inspected by the certificate.
\item $\rho$ is the spectral-bound row, sealed through $\RealUp$, naming only the displayed radius comparison and not a selected host supremum.
\item $V$ is the positive-eigenvector handoff row, consisting of a displayed nonzero cone element, its normalized readback, and the finite equality route asserting that $T$ carries it to the scalar multiple named by $\rho$.
\item $H$ records componentwise $\hsame$ transport for $B,C,T,Q,\rho,V$ and for the replay, provenance, and local naming rows.
\item $R$ records displayed $\Cont$ replay from ordered Banach and cone reads through the compact positive operator, compact orbit window, spectral-bound row, and positive-eigenvector handoff.
\item $P$ is the $\Pkg$ provenance over $\KreinRutmanUp$, $\BanachUp$, $\CompactOperatorUp$, $\RealUp$, $\KreinMilmanCompactConvexUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\KreinRutmanUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $B,C,T,Q,\rho,V,H,R,P,N$. It has no coordinate for an unrestricted spectrum, a host compactness theorem, a selected cone interior point, a quotient eigenline equality, countable choice, or ambient real completeness.
\end{definition}

\begin{theorem}[Krein--Rutman NameCert obligations]
\label{thm:krein-rutman-namecert-obligations}
For every accepted $\KreinRutmanUp$ carrier
$$
K=(B,C,T,Q,\rho,V,H,R,P,N),
$$
the local $\NameCert_{\KreinRutmanUp}$ surface consists exactly of ordered Banach admission for $B$, positive cone admission for $C$, compact positive operator admission for $T$, compact orbit-window exposure for $Q$, real spectral-bound sealing for $\rho$, positive-eigenvector handoff for $V$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $R$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:krein-rutman-carrier}. The visible rows are precisely the ordered Banach row, cone row, compact positive operator row, compact orbit window, spectral-bound row, positive-eigenvector handoff row, transport, replay, provenance, and local naming.

The operator route is finite. The row $T$ records only compact positive operator graph windows and cone-preservation rows, while $Q$ records only the normalized orbit and enclosure data admitted by the certificate.

The spectral read is also displayed. The row $\rho$ is sealed through $\RealUp$, and the handoff row $V$ records a nonzero cone element together with the finite route asserting the scalar-multiple read against $T$ and $\rho$.

Finally $H,R,P,N$ transport, replay, source, and name the same rows. Since the carrier lacks coordinates for unrestricted spectra, host compactness, selected cone interiors, quotient eigenlines, countable choice, and ambient completeness, the listed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Krein--Rutman positive-eigenvector handoff]
\label{thm:krein-rutman-positive-eigenvector-handoff}
Every consumer that reads an accepted $\KreinRutmanUp$ packet as positive-eigenvector evidence must first expose the ordered Banach row $B$, positive cone row $C$, compact positive operator row $T$, compact orbit window $Q$, real spectral-bound row $\rho$, positive-eigenvector handoff row $V$, componentwise $\hsame$ rows, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row. The handoff exports only this finite BEDC route and does not export a host spectral-radius theorem, an unrestricted eigenbasis, a quotient eigenline, a chosen cone interior, or a completeness principle.
\end{theorem}
\begin{proof}
Start from the NameCert obligations of \autoref{thm:krein-rutman-namecert-obligations}. They force any accepted read through the rows $B,C,T,Q,\rho,V,H,R,P,N$.

The positive-eigenvector part is reached only after the operator and cone rows are visible. The cone row $C$ supplies nonzero and positivity tests, while $T$ supplies the compact positive operator windows that preserve the displayed cone.

Next read $Q,\rho,V$. The compact orbit window provides the finite approximation and enclosure route, the real spectral-bound row seals the scalar comparison, and $V$ records the displayed vector handoff against that same operator and scalar.

The structural rows $H,R,P,N$ preserve the same finite route for downstream consumers. No listed coordinate is a host spectrum, unrestricted eigenbasis, quotient eigenline, selected cone interior, or ambient completeness principle, so the handoff is exactly the finite positive-eigenvector certificate surface.
\end{proof}

\closureat{KreinRutmanUp}{seedStr}

\begin{closurestatus}{\KreinRutmanUp}
  \constructivestory{A $\KreinRutmanUp$ packet is generated from ordered Banach, positive cone, compact positive operator, compact orbit-window, real spectral-bound, positive-eigenvector handoff, transport, replay, provenance, and local naming rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:krein-rutman-carrier}, \autoref{thm:krein-rutman-namecert-obligations}, and \autoref{thm:krein-rutman-positive-eigenvector-handoff} seed the compact positive operator NameCert surface for the finite positive-eigenvector handoff route.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close a host Krein--Rutman theorem, spectral-radius existence, unrestricted eigenbasis, quotient eigenline, chosen cone interior, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact positive operator routing, cone preservation, spectral-bound sealing, positive-eigenvector handoff, transport, replay, provenance, and local naming for $B,C,T,Q,\rho,V,H,R,P,N$.}
\end{closurestatus}
