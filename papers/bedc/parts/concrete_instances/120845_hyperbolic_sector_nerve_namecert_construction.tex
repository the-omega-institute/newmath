\chapter{A Concrete Naming Certificate for $\HyperbolicSectorNerveUp$}
\label{ch:concrete-instances-hyperbolic-sector-nerve-namecert}
\origin{ai}

The $\HyperbolicSectorNerveUp$ packet realizes the residual-sector boundary of \autoref{ch:metacic-confluence-audit-map} as finite MetaCIC NameCert data. It sits beside the confluence substitution boundary of \autoref{ch:concrete-instances-metacic-confluence-substitution-boundary-namecert}, the local join ledger of \autoref{ch:concrete-instances-metacic-local-join-ledger-namecert}, the residual checker of \autoref{ch:concrete-instances-metacic-confluence-residual-checker-namecert}, and the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}. The carrier names a finite residual-sector nerve: branches, candidate joins, retained obstructions, transport, replay, provenance, and local naming are explicit rows, while full Church--Rosser, unrestricted substitution compatibility, subject reduction, evaluator equality, and kernel consistency remain outside the packet.

\begin{definition}[Hyperbolic sector nerve carrier]
\label{def:hyperbolic-sector-nerve-carrier}
A $\HyperbolicSectorNerveUp$ carrier is a finite $\BHist$ packet
$$
S=(W,B,J,O,T,R,G,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $W$ is the typed residual-window row naming the finite source term window and its local context.
\item $B$ is the residual-branch row recording the two finite reduction branches inspected inside $W$.
\item $J$ is the candidate-join row recording the proposed common reducts visible at the inspected branch tips.
\item $O$ is the obstruction-retention row naming the residual-substitution boundary still required before a full diamond can be consumed.
\item $T$ is the transport row for branch, join, and obstruction data under componentwise $\hsame$.
\item $R$ is the replay row for the finite residual-sector route through $W,B,J,O,T$.
\item $G$ is the TasteGate readiness row, reserving the later $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instance obligations for this finite carrier surface.
\item $H$ records componentwise $\hsame$ transport for $W,B,J,O,T,R,G,C,P,N$.
\item $C$ records displayed $\Cont$ replay through residual windows, branches, candidate joins, obstruction retention, transport, TasteGate readiness, provenance, and naming.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicSectorNerveUp$, the MetaCIC confluence audit map, residual-substitution boundary rows, local-join rows, residual-checker rows, parallel-diamond frontier rows, $\BHist$, $\hsame$, $\Cont$, $\TasteGate$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicSectorNerveUp}$ row.
\end{enumerate}
The carrier has no coordinate for a full parallel diamond, closed Church--Rosser, unrestricted substitution compatibility, subject reduction, evaluator equality, quotient equality of terms, or kernel consistency.
\end{definition}

\begin{theorem}[Hyperbolic sector nerve NameCert obligations]
\label{thm:hyperbolic-sector-nerve-namecert-obligations}
For every accepted $\HyperbolicSectorNerveUp$ carrier
$$
S=(W,B,J,O,T,R,G,H,C,P,N),
$$
the local $\NameCert_{\HyperbolicSectorNerveUp}$ surface consists exactly of typed residual-window exposure through $W$, residual-branch exposure through $B$, candidate-join exposure through $J$, obstruction retention through $O$, transport through $T,H$, replay through $R,C$, TasteGate readiness through $G$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the finite carrier. The rows $W,B,J$ display the inspected residual sector: a typed window, two local branches, and candidate joins at the branch tips.

Read $O$ before any diamond-facing consumer. This retained obstruction is the residual-substitution boundary named by the confluence audit map, so the carrier cannot turn the candidate joins into a full confluence theorem.

Finally read $T,R,G,H,C,P,N$. Transport and replay preserve the same finite sector, while the TasteGate readiness row records the later Nontrivial and FieldFaithful obligations without treating them as checked evidence. Since no stronger global row occurs in the carrier, the listed rows exhaust the local certificate.
\end{proof}

\begin{theorem}[Hyperbolic sector nerve audit-map concretization]
\label{thm:hyperbolic-sector-nerve-audit-map-concretization}
Every confluence-audit read exported by an accepted $\HyperbolicSectorNerveUp$ packet factors through finite residual-branch, candidate-join, obstruction-retention, transport, replay, provenance, and local NameCert rows. The read may feed a conditional parallel-diamond consumer, but it does not discharge residual-substitution compatibility, prove full Church--Rosser, prove subject reduction, decide evaluator equality, or establish kernel consistency.
\end{theorem}
\begin{proof}
Start with $W,B,J$. These rows display only a finite local sector of the residual graph and its candidate joins.

Next inspect $O$. The obstruction row remains visible before the handoff is consumed, so the packet records the audit-map boundary instead of erasing it.

The rows $T,R,H,C,P,N$ transport, replay, source, and name the same finite sector. Therefore the exported read is a concrete conditional-boundary packet, not an unrestricted confluence theorem or kernel metatheorem.
\end{proof}

\begin{theorem}[Hyperbolic sector nerve TasteGate readiness]
\label{thm:hyperbolic-sector-nerve-tastegate-readiness}
The later Lean TasteGate file for $\HyperbolicSectorNerveUp$ must provide $\mathsf{Nontrivial}$ and $\mathsf{FieldFaithful}$ instances at the displayed carrier surface
$$
W,B,J,O,T,R,G,H,C,P,N.
$$
Nontriviality must exhibit an accepted packet with all finite residual-sector rows present. Field faithfulness must show that deleting any of the displayed branch, join, obstruction, transport, replay, provenance, or naming rows changes the readable carrier surface.
\end{theorem}
\begin{proof}
The readiness row $G$ records the instance boundary but not the instance proof. It is downstream of $W,B,J,O$ and upstream of the structural rows, so it cannot hide a missing residual-sector coordinate.

The nontriviality obligation is therefore tied to the existence of a displayed packet with all rows present. Field faithfulness is tied to componentwise comparison of those rows under $H,C,P,N$, ensuring that the future Lean surface cannot collapse the residual branch, candidate join, or obstruction-retention data into an empty marker.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicSectorNerveUp$ confluence-audit read exposes the obstruction-retention row $O$ before any conditional parallel-diamond handoff is consumed.}

\independenceWitness{metacic-confluence-substitution-boundary, metacic-local-join-ledger, metacic-confluence-residual-checker, metacic-parallel-diamond-frontier: none is bijective with $\HyperbolicSectorNerveUp$ because this packet jointly carries a typed residual window, residual branches, candidate joins, obstruction retention, transport, replay, TasteGate readiness, provenance, and local naming as one finite nerve surface.}

The Lean TasteGate file later supplies $\mathsf{Nontrivial}\ \HyperbolicSectorNerveUp$ and $\mathsf{FieldFaithful}\ \HyperbolicSectorNerveUp$ instances for the rows $W,B,J,O,T,R,G,H,C,P,N$. Those instances are obligations of the future Lean surface, not paper-side checked evidence.

\closureat{HyperbolicSectorNerveUp}{seedStr}

\begin{closurestatus}{\HyperbolicSectorNerveUp}
  \constructivestory{A $\HyperbolicSectorNerveUp$ packet is generated from a typed residual window, residual-branch rows, candidate-join rows, obstruction-retention rows, transport rows, replay rows, TasteGate readiness, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-sector-nerve-carrier}, \autoref{thm:hyperbolic-sector-nerve-namecert-obligations}, \autoref{thm:hyperbolic-sector-nerve-audit-map-concretization}, and \autoref{thm:hyperbolic-sector-nerve-tastegate-readiness} seed the finite residual-sector, candidate-join, obstruction-retention, transport, replay, TasteGate-readiness, provenance, and local naming surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close residual-substitution compatibility, full parallel diamond, closed Church--Rosser, subject reduction, evaluator equality, quotient equality of terms, kernel consistency, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, residual-branch exposure, candidate-join exactness, obstruction retention, transport stability, replay stability, provenance exhaustion, local naming, and Nontrivial and FieldFaithful TasteGate instances for BEDC.Derived.HyperbolicSectorNerveUp.}
\end{closurestatus}
