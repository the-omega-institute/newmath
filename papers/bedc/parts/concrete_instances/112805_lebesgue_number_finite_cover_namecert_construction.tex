\chapter{A Concrete Naming Certificate for $\LebesgueNumberFiniteCoverUp$}
\label{ch:concrete-instances-lebesgue-number-finite-cover-namecert}
\origin{human}

Following Lebesgue and Bishop--Bridges constructive compactness, the $\LebesgueNumberFiniteCoverUp$ packet records the finite Lebesgue-number surface for compact metric finite covers as NameCert data. It sits over the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the metric distance rows of \autoref{ch:concrete-instances-metric-namecert}, the finite-cover refinement route of \autoref{ch:concrete-instances-finite-cover-refinement-namecert}, the compact Lebesgue-number ledger of \autoref{ch:concrete-instances-compact-lebesgue-number-namecert}, the finite-cover radius-stability row of \autoref{ch:concrete-instances-finite-cover-radius-stability-namecert}, and the compact uniform-modulus consumer of \autoref{ch:concrete-instances-compact-uniform-modulus-namecert}. The packet exposes a positive radius for one displayed finite cover route; it is not an arbitrary open-cover compactness theorem, a choice-selected subcover, or an ambient completeness claim.

\begin{definition}[LebesgueNumberFiniteCover carrier]
\label{def:lebesgue-number-finite-cover-carrier}
A $\LebesgueNumberFiniteCoverUp$ carrier is a finite $\BHist$ packet
$$
L=(X,U,R,F,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the compact metric source row, exposing the metric carrier, compact finite-net witnesses, and distance-read data consumed by the route.
\item $U$ is the finite cover row, listing the displayed cover cells, their local metric-containment clauses, and the finite index spine inspected by the packet.
\item $R$ is the positive radius row, recording the rational lower bound that is to be read as a Lebesgue number for the displayed finite cover.
\item $F$ is the finite refinement and mesh row, recording the mesh budget and every refinement comparison used to pass from compact finite-net data to the cover cells of $U$.
\item $E$ is the cover-exactness ledger, saying that every inspected metric ball of radius below $R$ is routed into a named member of $U$ through the displayed refinement rows.
\item $H$ records componentwise $\hsame$ transport for compact metric, finite cover, positive radius, refinement, exactness, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a compact finite-cover request through
$$
X,U,R,F,E \longmapsto H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\LebesgueNumberFiniteCoverUp$, $\CompactMetricUp$, $\MetricUp$, $\FiniteCoverRefinementUp$, $\CompactLebesgueNumberUp$, $\FiniteCoverRadiusStabilityUp$, $\CompactUniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LebesgueNumberFiniteCoverUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these nine rows. It has no coordinate for an arbitrary open cover, a selected subcover outside $U$, countable choice, a host-level finite minimum, quotient equality of covers, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[LebesgueNumberFiniteCover NameCert obligations]
\label{thm:lebesgue-number-finite-cover-namecert-obligations}
For every accepted $\LebesgueNumberFiniteCoverUp$ carrier
$$
L=(X,U,R,F,E,H,C,P,N),
$$
the local $\NameCert_{\LebesgueNumberFiniteCoverUp}$ surface consists exactly of carrier admission, compact metric source admission, finite-cover admission, positive radius admission, finite refinement and mesh admission, cover-exactness replay, refinement-stability transport, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. No NameCert obligation is stored outside $X,U,R,F,E,H,C,P,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:lebesgue-number-finite-cover-carrier}. The public surface contains only the compact metric source, finite cover, positive radius, refinement, exactness, transport, replay, provenance, and naming rows.

The compact source row $X$ supplies the finite metric inspections used by the packet, and the cover row $U$ supplies the only cover cells that may be named. Thus carrier admission and finite-cover exactness are checked against one displayed finite spine.

The radius row $R$ is positive before it is consumed. The refinement and mesh row $F$ records every finite comparison needed to route the compact-source inspections into $U$, while the exactness ledger $E$ records the containment of every inspected radius ball below $R$.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since the tuple has no coordinate for an arbitrary open cover, selected subcover outside the packet, countable choice, host finite minima, quotient cover equality, or ambient completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[LebesgueNumberFiniteCover finite-cover handoff]
\label{thm:lebesgue-number-finite-cover-finite-cover-handoff}
Every compact-uniform consumer reading an accepted $\LebesgueNumberFiniteCoverUp$ packet receives the radius row $R$ only through the finite route
$$
X,U,F,E \longmapsto R \longmapsto H,C,P,N .
$$
The route may be consumed by compact uniform-modulus and finite-cover stability chapters, but it exports no arbitrary open-cover compactness principle and no selected subcover outside the displayed packet.
\end{theorem}
\begin{proof}
Use the NameCert obligations of \autoref{thm:lebesgue-number-finite-cover-namecert-obligations}. A consumer must first display the compact metric source $X$ and finite cover $U$.

The refinement and exactness rows $F,E$ then determine where each inspected metric ball lands. The positive radius $R$ is readable only after those rows have fixed the finite cover route.

Transport and replay through $H,C$ preserve this order, and $P,N$ record provenance and local naming. The handoff therefore supplies a finite Lebesgue-number packet without adding open-cover compactness, a choice-selected subcover, or ambient completion data.
\end{proof}

\falsifiablePrediction{Every accepted $\LebesgueNumberFiniteCoverUp$ consumer that claims a Lebesgue-number radius for a compact metric finite cover must display the compact metric source row, finite cover row, positive radius row, finite refinement and mesh row, cover-exactness ledger, hsame transport, Cont replay, Pkg provenance, and local NameCert before the radius is inspected.}

\independenceWitness{compactmetric, metric, finite-cover-refinement, compact-lebesgue-number, compact-uniform-modulus: $\LebesgueNumberFiniteCoverUp$ is not bijective to any listed sibling because it jointly carries one displayed compact metric source, one finite cover, one positive radius row, and one exact refinement ledger as a single local NameCert surface.}

\closureat{LebesgueNumberFiniteCoverUp}{seedStr}

\begin{closurestatus}{\LebesgueNumberFiniteCoverUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:lebesgue-number-finite-cover-carrier}, \autoref{thm:lebesgue-number-finite-cover-namecert-obligations}, and \autoref{thm:lebesgue-number-finite-cover-finite-cover-handoff} seed the finite Lebesgue-number carrier, local obligation surface, and compact-uniform handoff.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close arbitrary open-cover compactness, selected subcovers outside the displayed finite cover, countable choice, host finite minima, quotient cover equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Formal verification can encode carrier admission, compact metric source admission, finite-cover exactness, radius positivity, refinement stability, cover-exactness replay, transport, provenance, and local naming for BEDC.Derived.LebesgueNumberFiniteCoverUp.}
  \constructivestory{A $\LebesgueNumberFiniteCoverUp$ packet is generated from a compact metric source row, finite cover row, positive radius row, finite refinement and mesh row, cover-exactness ledger, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
