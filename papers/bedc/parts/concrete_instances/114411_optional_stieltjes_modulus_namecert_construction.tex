\chapter{A Concrete Naming Certificate for $\OptionalStieltjesModulusUp$}
\label{ch:concrete-instances-optional-stieltjes-modulus-namecert}
\origin{human}

Following Riemann, Stieltjes, and Bishop--Bridges constructive analysis, the $\OptionalStieltjesModulusUp$ packet records the finite modulus surface needed before a regulated Riemann--Stieltjes consumer may read an integration boundary. It sits beside the regulated-function carrier of \autoref{ch:concrete-instances-regulated-function-namecert}, the variation row of \autoref{ch:concrete-instances-boundedvariation-namecert}, the integration-by-parts route of \autoref{ch:concrete-instances-riemann-stieltjes-integration-parts-namecert}, the dyadic step approximation surface of \autoref{ch:concrete-instances-dyadic-step-approximation-namecert}, the regular rational source of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite handoff object for optional partitions, oscillation budgets, variation control, and Real readback; it does not import selected limits, measure completion, quotient integration equality, host total variation, or countable choice.

\begin{definition}[Optional Stieltjes modulus carrier]
\label{def:optional-stieltjes-modulus-carrier}
An $\OptionalStieltjesModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(F,G,V,P,S,Q,R,E,H,C,K,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\RegulatedFunctionUp$ integrand row, carrying the regulated oscillation windows read by the Stieltjes consumer.
\item $G$ is the integrator row, read through the $\BoundedVariationUp$ carrier and the finite variation ledger named by $V$.
\item $V$ is the bounded-variation budget row, recording the variation mass used by the modulus request.
\item $P$ is the optional partition row, listing the tagged dyadic subdivision windows that may be refined before the integration read.
\item $S$ is the $\DyadicStepApproximationUp$ row, recording the step approximation attached to the displayed partition.
\item $Q$ is the modulus row, assigning a finite dyadic tolerance to every displayed partition-refinement request.
\item $R$ is the $\RegSeqRatUp$ readback row, storing the regular rational estimate exported by the modulus.
\item $E$ is the terminal $\RealUp$ seal row for the regulated Stieltjes estimate.
\item $H$ records componentwise $\hsame$ transport for the integrand, integrator, variation, partition, step approximation, modulus, readback, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a Riemann--Stieltjes request through $F,G,V,P,S,Q,R,E,H$.
\item $K$ is the $\Pkg$ provenance over $\OptionalStieltjesModulusUp$, $\RegulatedFunctionUp$, $\BoundedVariationUp$, $\RiemannStieltjesIntegrationPartsUp$, $\DyadicStepApproximationUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\OptionalStieltjesModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for a measure completion, selected limit of partitions, quotient integration equality, host total-variation theorem, ambient Real completeness, or countable-choice selection of tags.
\end{definition}
\leanchecked{BEDC.Derived.OptionalStieltjesModulusUp.OptionalStieltjesModulusTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Optional Stieltjes modulus NameCert obligations]
\label{thm:optional-stieltjes-modulus-namecert-obligations}
For every accepted $\OptionalStieltjesModulusUp$ carrier
$$
M=(F,G,V,P,S,Q,R,E,H,C,K,N),
$$
the local $\NameCert_{\OptionalStieltjesModulusUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the regulated integrand, bounded-variation integrator, variation budget, optional partition, dyadic step approximation, modulus, regular rational readback, Real seal, transport, replay, provenance, and local naming rows;
\item regulated-window exactness, requiring every oscillation read to inspect $F$ and the displayed partition row $P$ before the modulus row $Q$ is consumed;
\item variation-budget exactness, requiring every Stieltjes-facing request to read $G,V$ before any optional refinement or Real seal is public;
\item step-approximation compatibility, requiring the dyadic step row $S$ to be attached to the same partition and variation rows;
\item readback and seal ordering, requiring $Q$ to feed $R$ before the terminal $\RealUp$ seal $E$ is exported;
\item componentwise transport, displayed replay, package provenance, and local naming through $H,C,K,N$.
\end{enumerate}
No obligation is stored outside $F,G,V,P,S,Q,R,E,H,C,K,N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:optional-stieltjes-modulus-carrier}. The readable rows are exactly the regulated integrand, bounded-variation integrator, variation budget, optional partition, dyadic step approximation, modulus, readback, Real seal, and structural rows.

The regulated and variation stages are separate but synchronized. The integrand row $F$ supplies the oscillation windows, while $G,V$ supply the bounded-variation budget before any optional partition refinement is read.

The finite approximation stage is $P,S,Q$. The partition row is displayed first, the dyadic step row is attached to it, and the modulus row assigns the finite tolerance used by downstream estimates.

Finally $R,E,H,C,K,N$ read back, seal, transport, replay, source, and name the same route. Since no carrier coordinate supplies measure completion, selected partition limits, quotient integration equality, host total variation, ambient Real completeness, or countable choice, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Optional Stieltjes modulus Riemann--Stieltjes handoff]
\label{thm:optional-stieltjes-modulus-rs-handoff}
Every Riemann--Stieltjes regulated integration consumer of an accepted $\OptionalStieltjesModulusUp$ carrier factors through the finite route
$$
(F,G,V) \longmapsto (P,S,Q) \longmapsto R \longmapsto E .
$$
The consumer receives regulated-function windows, bounded-variation control, optional partition data, dyadic step approximation, a finite modulus, regular rational readback, and a terminal Real seal. It receives no measure-completion object, selected partition limit, quotient integration equality, host total-variation theorem, ambient $\RealUp$ completeness, or choice-selected tag family.
\end{theorem}
\begin{proof}
Start with the carrier projection. The only integration-facing source rows are $F,G,V$, so a consumer must read the regulated integrand and bounded-variation budget before a partition or modulus is available.

The optional partition and approximation stage is then forced through $P,S,Q$. This gives a finite dyadic partition surface and a finite tolerance assignment rather than a selected limiting mesh.

The readback stage is $R,E$. The regular rational row records the estimate supplied by the modulus, and the Real seal exposes only that terminal read.

The structural rows $H,C,K,N$ preserve the same route under $\hsame$, $\Cont$, $\Pkg$, and local naming. Because the excluded integration and choice principles have no carrier coordinate, every regulated Riemann--Stieltjes handoff factors through the displayed finite packet.
\end{proof}

\leanstmt{BEDC.Derived.OptionalStieltjesModulusUp}

\closureat{OptionalStieltjesModulusUp}{seedStr}

\begin{closurestatus}{\OptionalStieltjesModulusUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:optional-stieltjes-modulus-carrier}, \autoref{thm:optional-stieltjes-modulus-namecert-obligations}, and \autoref{thm:optional-stieltjes-modulus-rs-handoff} seed the finite optional Stieltjes modulus route through regulated-function windows, bounded-variation control, optional partitions, dyadic step approximations, regular rational readback, Real seals, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \leantarget{BEDC.Derived.OptionalStieltjesModulusUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close measure completion, selected partition limits, quotient integration equality, host total variation, ambient $\RealUp$ completeness, countable choice, Lean proof obligations, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, regulated-window exactness, variation-budget exactness, step-approximation compatibility, readback-seal ordering, transport stability, replay, provenance, and local naming for BEDC.Derived.OptionalStieltjesModulusUp.}
  \constructivestory{An $\OptionalStieltjesModulusUp$ packet is generated from a regulated-function row, bounded-variation integrator row, variation budget, optional partition row, dyadic step approximation row, modulus row, RegSeqRat readback, terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
