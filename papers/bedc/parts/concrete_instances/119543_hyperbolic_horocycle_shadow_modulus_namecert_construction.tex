\chapter{A Concrete Naming Certificate for $\HyperbolicHorocycleShadowModulusUp$}
\label{ch:concrete-instances-hyperbolic-horocycle-shadow-modulus-namecert}
\origin{ai}

The $\HyperbolicHorocycleShadowModulusUp$ packet realizes the boundary-tangent branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite observer-expansion data. It sits after the horocycle packet of \autoref{ch:concrete-instances-hyperbolic-horocycle-namecert}, the Busemann boundary route of \autoref{ch:concrete-instances-hyperbolic-busemann-boundary-namecert}, the visual-boundary readout of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, the boundary-shadow route of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, and the hyperbolic metric packet of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}. The packet names a finite modulus budget for horocycle-shadow reads; it is not a global hyperbolic model, quotient ideal boundary, selected geodesic ray, ambient measure theorem, or physical observer dynamics.

\begin{definition}[Hyperbolic horocycle-shadow modulus carrier]
\label{def:hyperbolic-horocycle-shadow-modulus-carrier}
A $\HyperbolicHorocycleShadowModulusUp$ carrier is a finite $\BHist$ packet
$$
M=(Q,B,V,S,W,G,E,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the $\HyperbolicHorocycleUp$ boundary-tangent horocycle row.
\item $B$ is the $\HyperbolicBusemannBoundaryUp$ row that records the Busemann boundary read and horocycle level.
\item $V$ is the $\HyperbolicVisualBoundaryUp$ row used for the visible boundary face.
\item $S$ is the $\HyperbolicBoundaryShadowUp$ row that records the finite shadow window.
\item $W$ is the shadow-budget modulus row, comparing horocycle level, visual boundary window, and boundary-shadow tolerance.
\item $G$ is the $\HyperbolicMetricUp$ metric-read row over the same displayed disk and boundary data.
\item $E$ is the terminal $\RealUp$ seal for the displayed modulus comparison.
\item $A$ is the TasteGate route row recording that the Lean path requires $\mathrm{Nontrivial}$ and $\mathrm{FieldFaithful}$ instances before the modulus carrier can be checked beyond encoded syntax.
\item $H$ stores componentwise $\hsame$ transport for the horocycle, Busemann, visual-boundary, shadow, modulus, metric, Real-seal, TasteGate, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
\HyperbolicHorocycleUp,\ \HyperbolicBusemannBoundaryUp,\ \HyperbolicVisualBoundaryUp
\longmapsto
\HyperbolicBoundaryShadowUp,\ W,\ \HyperbolicMetricUp,\ \RealUp .
$$
\item $P$ is the $\Pkg$ provenance over $\HyperbolicHorocycleShadowModulusUp$, $\HyperbolicHorocycleUp$, $\HyperbolicBusemannBoundaryUp$, $\HyperbolicVisualBoundaryUp$, $\HyperbolicBoundaryShadowUp$, $\HyperbolicMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HyperbolicHorocycleShadowModulusUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $Q,B,V,S,W,G,E,A,H,C,P,N$. It has no coordinate for completed ideal-boundary quotients, selected rays, ambient visual measures, global curvature assumptions, host shadow sigma-algebras, or physical observer trajectories.
\end{definition}
\leandef{BEDC.Derived.HyperbolicHorocycleShadowModulusUp}

\begin{theorem}[Hyperbolic horocycle-shadow modulus NameCert obligations]
\label{thm:hyperbolic-horocycle-shadow-modulus-namecert-obligations}
For every accepted $\HyperbolicHorocycleShadowModulusUp$ carrier $M=(Q,B,V,S,W,G,E,A,H,C,P,N)$, the local $\NameCert_{\HyperbolicHorocycleShadowModulusUp}$ surface consists exactly of horocycle admission, Busemann-boundary readback, visual-boundary readback, boundary-shadow window exposure, shadow-budget modulus exactness, hyperbolic-metric comparison, terminal Real sealing, TasteGate route retention, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-horocycle-shadow-modulus-carrier}. The geometric rows are $Q,B,V,S,W,G,E,A$, and the structural rows are $H,C,P,N$.

The modulus row is boundary-tangent. It can be read only after the horocycle row $Q$, Busemann boundary row $B$, visual-boundary row $V$, boundary-shadow row $S$, and metric row $G$ have been displayed.

The Real seal $E$ records the terminal comparison, while $A$ keeps the Nontrivial and FieldFaithful TasteGate obligations visible. The route therefore remains encoded carrier data until those instances are supplied.

Transport, replay, provenance, and naming preserve exactly the displayed rows. Since no excluded host boundary, measure, ray-selection, or global-geometry object is present, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Hyperbolic horocycle-shadow modulus boundary route]
\label{thm:hyperbolic-horocycle-shadow-modulus-boundary-route}
Every boundary-tangent shadow-modulus read from an accepted $\HyperbolicHorocycleShadowModulusUp$ packet must expose
$$
\HyperbolicHorocycleUp,\ \HyperbolicBusemannBoundaryUp,\ \HyperbolicVisualBoundaryUp,\ \HyperbolicBoundaryShadowUp,\ \HyperbolicMetricUp,\ \RealUp
$$
before the modulus ledger $W$ is exported through $A,H,C,P,N$. The route is a finite observer-expansion packet; it does not assert a completed ideal boundary, selected geodesic ray, ambient visual measure, global hyperbolic model, or physical observer dynamics.
\end{theorem}
\begin{proof}
Start from the horocycle row $Q$. It fixes the boundary-tangent level at which the shadow comparison is being read.

Read the Busemann and visual-boundary rows $B,V$ next. They expose the boundary face and level data without replacing the packet by a quotient ideal boundary.

Apply the boundary-shadow row $S$ and the metric row $G$. These determine the finite shadow window and metric comparison consumed by the modulus row $W$, while the Real seal $E$ records only the displayed terminal comparison.

Finally pass through the TasteGate route and structural rows $A,H,C,P,N$. Since the route explicitly waits for Nontrivial and FieldFaithful instances and stores no selected rays, global measures, or ambient model, the exported object is the finite horocycle-shadow modulus packet stated above.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicHorocycleShadowModulusUp$ read must expose hyperbolic-horocycle, hyperbolic-boundary-shadow, hyperbolic-busemann-boundary, hyperbolic-visual-boundary, hyperbolic-metric, Real-seal, TasteGate route, transport, replay, provenance, and local NameCert rows before the shadow-modulus ledger is exported.}

\independenceWitness{hyperbolic-horocycle, hyperbolic-boundary-shadow, hyperbolic-busemann-boundary, hyperbolic-visual-boundary, hyperbolic-metric: $\HyperbolicHorocycleUp$ lacks the shadow-modulus ledger, $\HyperbolicBoundaryShadowUp$ lacks the boundary-tangent horocycle row, $\HyperbolicBusemannBoundaryUp$ lacks the visual shadow budget, $\HyperbolicVisualBoundaryUp$ lacks the Busemann-level modulus row, and $\HyperbolicMetricUp$ lacks the joint boundary-tangent shadow ledger. None is bijective to $\HyperbolicHorocycleShadowModulusUp$ under NameCert-row preservation.}

The Lean follow-up for $\HyperbolicHorocycleShadowModulusUp$ requires $\mathrm{Nontrivial}$ and $\mathrm{FieldFaithful}$ TasteGate instances preserving $Q,B,V,S,W,G,E,A,H,C,P,N$ before a checked carrier-alignment theorem can witness the modulus packet.

\closureat{HyperbolicHorocycleShadowModulusUp}{seedStr}

\begin{closurestatus}{\HyperbolicHorocycleShadowModulusUp}
  \constructivestory{A $\HyperbolicHorocycleShadowModulusUp$ packet is generated from HyperbolicHorocycleUp rows, HyperbolicBusemannBoundaryUp rows, HyperbolicVisualBoundaryUp rows, HyperbolicBoundaryShadowUp rows, a shadow-budget modulus row, HyperbolicMetricUp rows, a RealUp seal, a TasteGate route, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-horocycle-shadow-modulus-carrier}, \autoref{thm:hyperbolic-horocycle-shadow-modulus-namecert-obligations}, and \autoref{thm:hyperbolic-horocycle-shadow-modulus-boundary-route} seed the finite horocycle-shadow modulus packet for boundary-tangent observer-expansion reads.}
  \formalstatus{\encodedDefV}
  \leantarget{BEDC.Derived.HyperbolicHorocycleShadowModulusUp}
  \bridgestatus{none}
  \notclaimed{This chapter does not close completed ideal boundaries, selected geodesic rays, ambient visual measures, global hyperbolic geometry, Nontrivial or FieldFaithful TasteGate instances, Lean theorem verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked horocycle admission, Busemann readback, visual-boundary readback, boundary-shadow exposure, modulus exactness, metric comparison, Real sealing, TasteGate nontriviality, field faithfulness, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicHorocycleShadowModulusUp.}
\end{closurestatus}
