\chapter{A Concrete Naming Certificate for $\BishopLocatedRealSegmentUp$}
\label{ch:concrete-instances-bishop-located-real-segment-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, $\BishopLocatedRealSegmentUp$ names the finite certificate surface for a located real interval whose endpoints are presented by regular rational data and whose order evidence is explicitly displayed. The packet sits over the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\LocatedRealUp$, and $\LocatedRealOrderUp$ siblings of \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-real-namecert}, \autoref{ch:concrete-instances-locatedreal-namecert}, and \autoref{ch:concrete-instances-located-real-order-namecert}. It gives later located-interval and completion arguments a routed endpoint packet, not a classical interval object.

\begin{definition}[Bishop located real segment carrier]
\label{def:bishop-located-real-segment-carrier}
A $\BishopLocatedRealSegmentUp$ carrier is a finite $\BHist$ packet
$$
I=(L,R,O,S,Q,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ and $R$ are endpoint rows, each naming a left or right $\RealUp$ endpoint through the same finite packet rather than through host real equality.
\item $O$ is the located-order evidence row, read through $\LocatedRealUp$ and $\LocatedRealOrderUp$, stating that the displayed left endpoint is below the displayed right endpoint at the requested finite tolerances.
\item $S$ is the finite $\StreamNameUp$ window row used to inspect endpoint names.
\item $Q$ is the $\RegSeqRatUp$ readback row attached to those windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger recording endpoint radii, comparison budgets, and dyadic inclusion tests.
\item $E$ is the pair of terminal $\RealUp$ seals reached after $S,Q,D,O$ have been replayed.
\item $H$ records componentwise $\hsame$ transport for endpoint rows, located-order evidence, stream windows, regular readbacks, dyadic tolerances, terminal seals, and structural rows.
\item $C$ records the displayed $\Cont$ routes by which consumers replay the endpoint, order, window, readback, tolerance, seal, transport, provenance, and local naming rows.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedRealSegmentUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\LocatedRealUp$, $\LocatedRealOrderUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopLocatedRealSegmentUp}$ row.
\end{enumerate}
The carrier stores a located segment boundary as finite endpoint and order data. It has no coordinate for Real completeness, trichotomy, quotient real equality, selected representatives, or open-cover compactness.
\end{definition}

\begin{definition}[Bishop located real segment classifier]
\label{def:bishop-located-real-segment-classifier}
Two $\BishopLocatedRealSegmentUp$ carriers
$$
I=(L,R,O,S,Q,D,E,H,C,P,N)
\qquad\text{and}\qquad
I'=(L',R',O',S',Q',D',E',H',C',P',N')
$$
are classified as the same displayed located segment exactly when the left and right endpoint rows match componentwise, the located-order rows expose the same endpoint order boundary, the stream windows inspect the same finite endpoint prefixes, the regular rational readbacks and dyadic tolerance ledgers carry the same comparison budgets, the terminal $\RealUp$ seals are transported by the displayed $\hsame$ data, and the continuation, provenance, and local NameCert rows name the same $\BishopLocatedRealSegmentUp$ packet. The classifier is rowwise over $L,R,O,S,Q,D,E,H,C,P,N$ and does not identify intervals by quotient real equality, selected representatives, trichotomy, or a host subset of the real line.
\end{definition}

\begin{theorem}[Bishop located real segment NameCert obligations]
\label{thm:bishop-located-real-segment-namecert-obligations}
For an accepted $\BishopLocatedRealSegmentUp$ carrier $I=(L,R,O,S,Q,D,E,H,C,P,N)$, the local $\NameCert_{\BishopLocatedRealSegmentUp}$ surface consists of seven finite obligation rows:
\begin{enumerate}
\item carrier formation for the two endpoint rows, located-order evidence, finite stream windows, regular rational readbacks, dyadic tolerance ledger, terminal $\RealUp$ seals, transports, continuations, provenance, and local naming data;
\item classifier stability for the componentwise comparison of \autoref{def:bishop-located-real-segment-classifier};
\item endpoint order stability, stating that the displayed located-order row is transported together with the endpoint rows and dyadic comparison budgets;
\item stream-window readback, stating that endpoint prefixes are inspected only through the $\StreamNameUp$ and $\RegSeqRatUp$ rows;
\item dyadic tolerance exactness, stating that interval inclusion and endpoint comparison checks use only the displayed $\DyadicRatCoreUp$ ledger;
\item terminal seal routing, stating that endpoint consumers reach the two $\RealUp$ seals only after the window, readback, tolerance, and located-order rows have been replayed;
\item ledger non-escape, excluding Real completeness, trichotomy, quotient real equality, selected representatives, and open-cover compactness.
\end{enumerate}
\end{theorem}
\begin{proof}
Carrier formation is projection from \autoref{def:bishop-located-real-segment-carrier}. The packet displays exactly the endpoint rows, located-order evidence, finite stream windows, regular readbacks, dyadic tolerance ledger, terminal seals, transport rows, continuation routes, provenance, and local name.

Classifier stability follows from \autoref{def:bishop-located-real-segment-classifier}. Transport along $H$ may replace visible rows only by rows with the same endpoint order boundary, the same stream windows, and the same dyadic comparison budgets, while $C$ preserves the replay order.

The readback and exactness rows are the displayed roles of $S,Q,D,O$. The stream row exposes endpoint prefixes, the regular row reads them as rational approximants, the dyadic ledger records the finite tolerances, and the located-order row binds those comparisons to the endpoint boundary.

Terminal seal routing is the role of $E$. A consumer may read the left or right real endpoint only after the stream, regular, dyadic, and located-order rows have been replayed.

The non-escape row follows from the carrier inventory. No displayed field carries Real completeness, trichotomy, quotient real equality, selected representatives, or open-cover compactness.
\end{proof}

\begin{theorem}[Bishop located real segment endpoint order stability]
\label{thm:bishop-located-real-segment-endpoint-order-stability}
Let $I=(L,R,O,S,Q,D,E,H,C,P,N)$ be an accepted $\BishopLocatedRealSegmentUp$ carrier. Every endpoint-facing transport of $I$ preserves the located order row $O$ together with the endpoint rows $L,R$ and dyadic tolerance ledger $D$. Hence the transported packet remains a located segment certificate and cannot turn into an unordered pair of real names, a trichotomy witness, or a quotient interval.
\end{theorem}
\begin{proof}
Project $L,R,O,D,H$ from the carrier of \autoref{def:bishop-located-real-segment-carrier}. The endpoint rows and located-order row are displayed in the same packet, and the dyadic tolerance ledger records the finite comparison budgets attached to them.

Use the classifier of \autoref{def:bishop-located-real-segment-classifier}. It compares accepted packets by the componentwise endpoint, order, stream, readback, tolerance, seal, transport, replay, provenance, and naming rows, not by a host subset or quotient equality of real names.

The transport row $H$ therefore preserves the order boundary whenever it transports the endpoints. Since $C,P,N$ replay and source the same finite packet, no consumer obtains an unordered pair, a trichotomy decision, or a quotient interval from the transported carrier.
\end{proof}

\begin{theorem}[Bishop located real segment Real seal non-escape]
\label{thm:bishop-located-real-segment-real-seal-nonescape}
An accepted $\BishopLocatedRealSegmentUp$ packet exposes its endpoints only as the terminal $\RealUp$ seals $E$ reached through the displayed route
$$
S\longmapsto Q\longmapsto D\longmapsto O\longmapsto E\longmapsto H\longmapsto C\longmapsto P\longmapsto N .
$$
In particular, it does not export Real completeness, trichotomy, quotient real equality, selected representatives, or open-cover compactness.
\end{theorem}
\begin{proof}
The only endpoint-facing value coordinate in the carrier is $E$. The rows $S,Q,D,O$ supply the stream windows, regular rational readbacks, dyadic tolerances, and located-order evidence that must be replayed before $E$ is consumed.

Continuation replay through $C$ follows the displayed route, and componentwise transport through $H$ preserves the same endpoint and order rows. Provenance through $P$ and local naming through $N$ record the packet as a $\BishopLocatedRealSegmentUp$ certificate.

Since no carrier field stores the excluded principles, the terminal seals cannot be read as a completeness theorem, trichotomy decision, quotient real equality, selected representative, or open-cover compactness principle.
\end{proof}

\begin{closurestatus}{\BishopLocatedRealSegmentUp}
  \constructivestory{A $\BishopLocatedRealSegmentUp$ packet is generated from two RealUp endpoint rows, located-order evidence, finite StreamName windows, RegSeqRat readbacks, a DyadicRatCore tolerance ledger, terminal RealUp seals, componentwise hsame transports, Cont replay routes, package provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-located-real-segment-carrier}, \autoref{def:bishop-located-real-segment-classifier}, \autoref{thm:bishop-located-real-segment-namecert-obligations}, \autoref{thm:bishop-located-real-segment-endpoint-order-stability}, and \autoref{thm:bishop-located-real-segment-real-seal-nonescape} seed the finite located real segment surface over displayed endpoint, order, stream, regular rational, dyadic, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Real completeness, trichotomy, quotient real equality, selected representatives, open-cover compactness, Lean verification, or a standard bridge.}
  \upgradepath{Obligation closure requires checked carrier formation, classifier stability, endpoint order stability, stream-window readback, dyadic tolerance exactness, terminal seal routing, and Real seal non-escape for the displayed packet.}
\end{closurestatus}
