\chapter{A Concrete Naming Certificate for $\CauchySubnetUp$}
\label{ch:concrete-instances-cauchy-subnet-namecert}
\origin{human}

Following Bishop--Bridges and Kelley filter terminology, the $\CauchySubnetUp$ packet records the finite restriction surface by which a displayed Cauchy-filter source is read along a cofinal subnet before a filter-limit handoff or completion-facing consumer may inspect the route. It sits after the $\CauchyFilterUp$ surface of \autoref{ch:concrete-instances-cauchyfilter-namecert}, the $\CauchyFilterLimitUp$ route of \autoref{ch:concrete-instances-cauchyfilterlimit-namecert}, and the $\MetricCompletionUp$ boundary of \autoref{ch:concrete-instances-metriccompletion-namecert}; it reads finite windows through $\StreamNameUp$ in \autoref{ch:concrete-instances-streamname-namecert}, regular rational rows through $\RegSeqRatUp$ in \autoref{ch:concrete-instances-regseqrat-namecert}, dyadic tolerances through $\DyadicRatCoreUp$ in \autoref{ch:concrete-instances-dyadicratcore-namecert}, and terminal seals through $\RealUp$ in \autoref{ch:concrete-instances-real-namecert}. The packet names cofinal subnet restriction as BEDC data, not as quotient filter equality, selected host subnets, countable choice, host real equality, or ambient completeness.

\begin{definition}[Cauchy subnet carrier]
\label{def:cauchy-subnet-carrier}
A $\CauchySubnetUp$ carrier is a finite $\BHist$ packet
$$
S=(F,J,W,R,D,L,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the source $\CauchyFilterUp$ row whose neighborhoods are to be restricted.
\item $J$ is the cofinal subnet-index ledger. It records the finite dominance checks showing that every accepted request to the subnet is backed by a displayed request to the source filter.
\item $W$ is the $\StreamNameUp$ finite-window row inspected after $F$ and $J$ have been replayed.
\item $R$ is the $\RegSeqRatUp$ regular rational readback row attached to the displayed windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger used by the restricted windows and regular readback.
\item $L$ is the $\CauchyFilterLimitUp$ handoff row available only after the source filter, cofinal ledger, windows, readback, and tolerance rows have been displayed.
\item $E$ is the $\RealUp$ or $\MetricCompletionUp$ seal row reached by completion-facing consumers through the same handoff route.
\item $H$ records componentwise $\hsame$ transport for the source filter, cofinal ledger, windows, readback, tolerance ledger, limit handoff, seal, continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a filter request through $F,J,W,R,D,L,E$ in that order.
\item $P$ is the $\Pkg$ provenance over $\CauchySubnetUp$, $\CauchyFilterUp$, $\CauchyFilterLimitUp$, $\MetricCompletionUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchySubnetUp}$ row.
\end{enumerate}
The carrier has no coordinate for quotient filters, selected host subnets, countable choice, host real equality, or ambient completeness.
\end{definition}
\leanchecked{BEDC.Derived.CauchySubnetUp.CauchySubnetTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Cauchy subnet NameCert obligations]
\label{thm:cauchy-subnet-namecert-obligations}
For every accepted $\CauchySubnetUp$ carrier $S=(F,J,W,R,D,L,E,H,C,P,N)$, the local $\NameCert_{\CauchySubnetUp}$ surface consists exactly of the following obligation rows:
\begin{enumerate}
\item carrier admission for the source Cauchy filter, cofinal subnet-index ledger, StreamName window, RegSeqRat readback, DyadicRatCore tolerance ledger, CauchyFilterLimit handoff, completion or Real seal, transport, continuation, provenance, and local naming rows;
\item cofinal restriction coverage, requiring every accepted subnet request to read the ledger $J$ together with the source filter row $F$ before any window row is inspected;
\item finite-window and tolerance exactness, requiring $W,R,D$ to be the only finite rows through which the restricted source reaches a regular rational comparison;
\item filter-limit handoff, requiring $F,J,W,R,D$ to repack as the handoff row $L$ before any completion-facing or Real-facing seal is available;
\item ledger refusal, excluding quotient filters, selected host subnets, countable choice, host real equality, and ambient completeness.
\end{enumerate}
No NameCert obligation is stored outside these rows.
\end{theorem}
\leanchecked{BEDC.Derived.CauchySubnetUp.CauchySubnetNameCertObligations}
\begin{proof}
Project the carrier of \autoref{def:cauchy-subnet-carrier}. The projection exposes exactly the source Cauchy filter row $F$, cofinal subnet-index ledger $J$, StreamName window $W$, RegSeqRat readback $R$, DyadicRatCore tolerance ledger $D$, filter-limit handoff $L$, seal row $E$, and structural rows $H,C,P,N$.

The continuation row $C$ gives the only replay route. A subnet request first reads the source filter row and the cofinal ledger, then reaches the finite windows, regular readback, and dyadic tolerance ledger.

The handoff row $L$ is downstream of those five displayed rows, and the completion or Real seal $E$ is downstream of $L$. Since the carrier contains no quotient-filter coordinate, selected host subnet, countable-choice selector, host real equality row, or ambient-completeness row, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Cauchy subnet filter-limit handoff]
\label{thm:cauchy-subnet-filter-limit-handoff}
Let $S=(F,J,W,R,D,L,E,H,C,P,N)$ be an accepted $\CauchySubnetUp$ carrier. Every completion-facing or Real-facing consumer of the restricted Cauchy source factors through the finite route
$$
F,J,W,R,D \longmapsto L \longmapsto E \longmapsto H,C,P,N .
$$
Thus the consumer receives a $\CauchyFilterLimitUp$ handoff only after the source $\CauchyFilterUp$ row, cofinal subnet-index ledger, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, and $\DyadicRatCoreUp$ tolerance ledger have been displayed. The route exports no quotient filters, selected host subnets, countable choice, host real equality, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchySubnetUp.CauchySubnet\_filter\_limit\_handoff}
\begin{proof}
Begin with a completion-facing or Real-facing consumer. Carrier admission provides the handoff row $L$ and seal row $E$, but only as rows downstream of $F,J,W,R,D$.

Replay the continuation row $C$. The request first consumes the source Cauchy filter row and the cofinal subnet-index ledger, then inspects the finite window row, the regular rational readback, and the dyadic tolerance ledger.

Only after that displayed route has been replayed can the packet expose $L$, and only after $L$ can it expose $E$. Componentwise transport through $H$ and provenance through $P,N$ preserve the same dependency order, so the handoff is a finite BEDC route rather than quotient filter equality, selected host subnet choice, countable choice, host real equality, or ambient completeness.
\end{proof}

\begin{theorem}[Cauchy subnet cofinal ledger coverage]
\label{thm:cauchy-subnet-cofinal-ledger-coverage}
Let $S=(F,J,W,R,D,L,E,H,C,P,N)$ be an accepted $\CauchySubnetUp$ carrier. Every accepted subnet request admitted by the cofinal ledger row $J$ has a displayed source-filter request in $F$. Every completion-facing read must pass through
$$
F,J,W,R,D,L,E,H,C,P,N
$$
before it exposes a $\CauchyFilterLimitUp$, $\MetricCompletionUp$, or $\RealUp$ seal. Thus cofinal restriction is a finite ledger coverage route, not a quotient-filter equality, selected host subnet, countable-choice selector, host real equality, or ambient completeness assertion.
\end{theorem}
\leanchecked{BEDC.Derived.CauchySubnetUp.CauchySubnet\_cofinal\_ledger\_coverage}
\leanvariant{BEDC.Derived.CauchySubnetUp.CauchySubnetCarrier\_cofinal\_ledger\_coverage}
\begin{proof}
Project the carrier of \autoref{def:cauchy-subnet-carrier}. The row $J$ is specified as the cofinal subnet-index ledger, and its accepted requests are backed by displayed source requests to the filter row $F$.

Use \autoref{thm:cauchy-subnet-namecert-obligations} for row exhaustion. After $F$ and $J$, every admissible read proceeds through the finite-window row $W$, regular rational readback $R$, dyadic tolerance ledger $D$, handoff row $L$, seal row $E$, and structural rows $H,C,P,N$.

Finally apply \autoref{thm:cauchy-subnet-filter-limit-handoff}. It identifies the only endpoint route to $\CauchyFilterLimitUp$, $\MetricCompletionUp$, or $\RealUp$, so no completion-facing consumer can bypass the cofinal ledger coverage.
\end{proof}

\begin{theorem}[CauchySubnet cofinal reindexing route]
\label{thm:cauchysubnet-cofinal-reindexing-route}
Let $S=(F,J,W,R,D,L,E,H,C,P,N)$ be an accepted $\CauchySubnetUp$ carrier. Any accepted cofinal subnet request whose ledger $J$ reindexes $\StreamNameUp$ windows preserves the route
$$
F,J,W,R,D,L,E,H,C,P,N .
$$
Consequently the $\RegSeqRatUp$ readback row $R$, the $\DyadicRatCoreUp$ tolerance ledger $D$, and any $\RealUp$ or $\MetricCompletionUp$ seal access remain downstream of the cofinal ledger. The route exports no quotient filter equality, selected subnet, countable-choice schedule, host real equality, or ambient-completeness coordinate.
\end{theorem}
\begin{proof}
Start from the local carrier and obligation surface. \autoref{thm:cauchy-subnet-namecert-obligations} requires every accepted subnet request to read the source filter row $F$ and cofinal ledger row $J$ before any finite StreamName window is inspected.

Use \autoref{thm:cauchy-subnet-cofinal-ledger-coverage}. It gives a displayed source-filter request for each accepted cofinal ledger request, so reindexing the StreamName windows cannot detach $W$ from the preceding rows $F,J$.

Finally apply \autoref{thm:cauchy-subnet-filter-limit-handoff}. The handoff theorem fixes the downstream order through $W,R,D,L,E,H,C,P,N$, so RegSeqRat readback, DyadicRatCore tolerance, MetricCompletion access, and Real seal access remain behind the cofinal ledger.
\end{proof}

\begin{theorem}[Cauchy subnet obligation-closure package]
\label{thm:cauchy-subnet-obligation-closure-package}
The carrier definition of \autoref{def:cauchy-subnet-carrier}, together with \autoref{thm:cauchy-subnet-namecert-obligations}, \autoref{thm:cauchy-subnet-filter-limit-handoff}, and \autoref{thm:cauchy-subnet-cofinal-ledger-coverage}, closes the $\CauchySubnetUp$ obligation surface for carrier admission, cofinal coverage, finite-window exactness, tolerance exactness, filter-limit handoff, and ledger refusal. In particular, every accepted $\CauchySubnetUp$ consumer read is forced through
$$
F,J,W,R,D,L,E,H,C,P,N
$$
before it may expose a $\CauchyFilterLimitUp$, $\MetricCompletionUp$, or $\RealUp$ endpoint, and the packet has no remaining local obligation coordinate for quotient filters, selected host subnets, countable choice, host real equality, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.CauchySubnetUp.CauchySubnet\_obligation\_closure\_package}
\begin{proof}
Carrier admission is exactly the row projection in \autoref{def:cauchy-subnet-carrier}. The source filter, cofinal ledger, finite windows, rational readback, dyadic tolerance ledger, limit handoff, completion or Real seal, transport, replay, provenance, and local naming rows are all visible there.

Apply \autoref{thm:cauchy-subnet-namecert-obligations} to exhaust the local NameCert surface. Its obligation list accounts for carrier admission, cofinal restriction coverage, finite-window and tolerance exactness, the downstream filter-limit handoff, and refusal of the excluded host-level data.

The two route theorems then identify the order of consumption. \autoref{thm:cauchy-subnet-filter-limit-handoff} forces every endpoint through $F,J,W,R,D,L,E,H,C,P,N$, while \autoref{thm:cauchy-subnet-cofinal-ledger-coverage} prevents a completion-facing read from bypassing the cofinal ledger. These clauses leave no further local obligation row for the $\CauchySubnetUp$ packet.
\end{proof}
\leanchecked{BEDC.Derived.CauchySubnetUp.CauchySubnetCarrier\_obligation\_closure\_package}

\begin{theorem}[Cauchy subnet scoped route]
\label{thm:cauchy-subnet-scoped-route}
Every accepted $\CauchySubnetUp$ read is scoped exactly by the displayed $\CauchyFilterUp$ source row, $\CauchyFilterLimitUp$ handoff row, $\MetricCompletionUp$ endpoint row, $\StreamNameUp$ finite-window row, $\RegSeqRatUp$ rational readback row, $\DyadicRatCoreUp$ tolerance ledger, $\RealUp$ seal row, the surrounding $\BHist$ packet, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\CauchySubnetUp}$ row. A consumer that omits any one of these rows receives only a partial subnet restriction and cannot expose a completion-facing endpoint for the accepted packet.
\end{theorem}
\begin{proof}
Start with the local obligation inventory. \autoref{thm:cauchy-subnet-namecert-obligations} lists carrier admission, cofinal restriction coverage, finite-window and tolerance exactness, filter-limit handoff, and ledger refusal as the only local rows.

Next read the endpoint route. \autoref{thm:cauchy-subnet-filter-limit-handoff} forces every completion-facing or Real-facing consumer through $F,J,W,R,D,L,E,H,C,P,N$, while \autoref{thm:cauchy-subnet-cofinal-ledger-coverage} prevents the cofinal ledger $J$ from being bypassed before the finite windows are inspected.

Finally apply \autoref{thm:cauchy-subnet-obligation-closure-package}. It packages the carrier, handoff, cofinal coverage, and refusal clauses into the same displayed packet, so the exact scope of an accepted read is precisely the listed CauchyFilter, CauchyFilterLimit, MetricCompletion, StreamName, RegSeqRat, DyadicRatCore, Real, BHist, hsame, Cont, Pkg, and NameCert surface.
\end{proof}

\begin{theorem}[CauchySubnet scoped kernel route]
\label{thm:cauchy-subnet-scoped-kernel-route}
Every accepted $\CauchySubnetUp$ obligation in this chapter depends only on the displayed $\CauchyFilterUp$ source, $\StreamNameUp$ finite-window row, $\RegSeqRatUp$ readback row, $\DyadicRatCoreUp$ tolerance ledger, $\CauchyFilterLimitUp$ handoff, $\MetricCompletionUp$ endpoint row, $\RealUp$ seal row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\CauchySubnetUp}$ rows. Within that kernel scope, cofinal ledger coverage, finite-window exactness, tolerance exactness, filter-limit handoff, and endpoint nonescape are exactly the obligations named by the carrier and obligation package; outside that scope the packet exports no quotient filters, selected host subnets, countable choice, host real equality, or ambient completeness.
\end{theorem}
\begin{proof}
Use \autoref{thm:cauchy-subnet-namecert-obligations} to list the local obligation rows. They name carrier admission, cofinal restriction coverage, finite-window and tolerance exactness, filter-limit handoff, and ledger refusal, all inside the displayed carrier surface.

Use \autoref{thm:cauchy-subnet-filter-limit-handoff} and \autoref{thm:cauchy-subnet-cofinal-ledger-coverage} to fix the consumption order. Every completion-facing read must pass through the source filter, cofinal ledger, StreamName window, RegSeqRat readback, DyadicRatCore tolerance row, CauchyFilterLimit handoff, and MetricCompletion or Real endpoint.

Finally apply \autoref{thm:cauchy-subnet-obligation-closure-package}. The same package includes componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row, so the obligation surface is scoped to the named kernel rows and does not acquire any excluded host-level coordinate.
\end{proof}
\leanchecked{BEDC.Derived.CauchySubnetUp.CauchySubnetCarrier\_scoped\_kernel\_route}

\begin{theorem}[CauchySubnet public certificate route]
\label{thm:cauchysubnet-public-certificate-route}
Every public completion-facing consumer of an accepted $\CauchySubnetUp$ packet reads the cofinal ledger through $\CauchyFilterUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchyFilterLimitUp$, $\MetricCompletionUp$, and $\RealUp$ before it can expose the local $\NameCert_{\CauchySubnetUp}$ row. The public route exports no quotient filters, selected host subnets, countable choice, host real equality, or ambient completeness.
\end{theorem}
\begin{proof}
First use \autoref{thm:cauchy-subnet-namecert-obligations}. It identifies the local rows that must be displayed before the packet can be consumed: source Cauchy filter, cofinal subnet-index ledger, finite windows, regular readback, dyadic tolerance, handoff, endpoint seal, transport, replay, provenance, and local naming.

Next apply \autoref{thm:cauchy-subnet-filter-limit-handoff} and \autoref{thm:cauchy-subnet-cofinal-ledger-coverage}. These results force every completion-facing read through the cofinal ledger and then through the finite-window, readback, tolerance, handoff, and endpoint rows.

Finally apply \autoref{thm:cauchy-subnet-scoped-kernel-route}. It pins the route to the displayed kernel rows and excludes quotient filters, selected host subnets, countable choice, host real equality, and ambient completeness before the local $\NameCert_{\CauchySubnetUp}$ row is public.
\end{proof}

\begin{theorem}[Cauchy subnet endpoint nonescape]
\label{thm:cauchy-subnet-endpoint-nonescape}
Every $\CauchyFilterLimitUp$, $\MetricCompletionUp$, or $\RealUp$ endpoint read of an accepted $\CauchySubnetUp$ packet factors through the displayed rows
$$
F,J,W,R,D,L,E,H,C,P,N.
$$
The endpoint is therefore reached only by the source Cauchy filter row $F$, cofinal subnet ledger $J$, finite-window row $W$, regular-rational readback $R$, dyadic tolerance ledger $D$, filter-limit handoff $L$, endpoint seal $E$, componentwise transport $H$, displayed replay $C$, provenance $P$, and local naming row $N$.
\end{theorem}
\begin{proof}
Start with the filter-limit handoff of \autoref{thm:cauchy-subnet-filter-limit-handoff}. It forces every completion-facing or Real-facing consumer through the displayed route from $F$ to $E$ before any $\CauchyFilterLimitUp$, $\MetricCompletionUp$, or $\RealUp$ endpoint can be exposed.

Next use the cofinal ledger coverage of \autoref{thm:cauchy-subnet-cofinal-ledger-coverage}. This prevents a consumer from bypassing $J$ and fixes the finite-window, readback, and tolerance rows $W,R,D$ as part of the same endpoint route.

Finally apply the scoped kernel route of \autoref{thm:cauchy-subnet-scoped-kernel-route} and the public certificate route of \autoref{thm:cauchysubnet-public-certificate-route}. They add the structural rows $H,C,P,N$ and exclude quotient filters, selected host subnets, countable choice, host real equality, and ambient completeness from every accepted endpoint read.
\end{proof}

\begin{closurestatus}{\CauchySubnetUp}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{def:cauchy-subnet-carrier}, \autoref{thm:cauchy-subnet-namecert-obligations}, \autoref{thm:cauchy-subnet-filter-limit-handoff}, \autoref{thm:cauchy-subnet-cofinal-ledger-coverage}, \autoref{thm:cauchy-subnet-obligation-closure-package}, and \autoref{thm:cauchy-subnet-scoped-kernel-route} close the finite cofinal subnet obligation surface between CauchyFilter sources, StreamName and RegSeqRat readback, DyadicRatCore tolerance rows, CauchyFilterLimit handoff, MetricCompletion or Real consumers, and the hsame, Cont, Pkg, and NameCert support rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient filters, selected host subnets, countable choice, host real equality, ambient completeness, or Lean verification.}
  \upgradepath{Obligation closure requires checked carrier admission, cofinal restriction coverage, finite-window exactness, tolerance exactness, filter-limit handoff, and ledger refusal proofs for the same packet.}
  \constructivestory{A $\CauchySubnetUp$ packet is generated from a source CauchyFilter row, a cofinal subnet-index ledger, StreamName windows, RegSeqRat readback, a DyadicRatCore tolerance ledger, a CauchyFilterLimit handoff, a MetricCompletion or Real seal, componentwise transport, continuation replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
