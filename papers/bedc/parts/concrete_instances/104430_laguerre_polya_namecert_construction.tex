\chapter{A Concrete Naming Certificate for $\LaguerrePolyaUp$}
\label{ch:concrete-instances-laguerre-polya-namecert}
\origin{human}

Following Laguerre and Polya's real-rooted polynomial limit theory, the $\LaguerrePolyaUp$ packet records a function only through polynomial approximants with displayed real roots, coefficient-window convergence, and a real-power-series endpoint. It consumes the finite real-polynomial evaluator of \autoref{ch:concrete-instances-real-polynomial-namecert}, the coefficient and radius-window surface of \autoref{ch:concrete-instances-realpowerseries-namecert}, the root-bracket boundary of \autoref{ch:concrete-instances-real-root-bracket-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the finite readback rows of \autoref{ch:concrete-instances-regseqrat-namecert} and \autoref{ch:concrete-instances-streamname-namecert}. The packet names the constructive Laguerre--Polya route as BEDC certificate data; it does not import a host entire-function primitive, a selected infinite product, quotient function equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Laguerre--Polya carrier]
\label{def:laguerre-polya-carrier}
A $\LaguerrePolyaUp$ carrier is a finite $\BHist$ packet
$$
L=(A,B,R,S,Q,W,E,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the approximant-family row. It records a finite inspection of $\RealPolynomialUp$ names, their coefficient windows, degree bounds, evaluation requests, and the index window used by the local certificate.
\item $B$ is the real-root evidence row. For every inspected approximant it stores the finite $\RealRootBracketUp$ data witnessing the displayed real-root route for the roots relevant to that inspection.
\item $R$ is the coefficient-readback row. It records $\RegSeqRatUp$ readbacks for the approximant coefficients, center coefficients, bracket endpoints, and requested real output names.
\item $S$ is the $\StreamNameUp$ window row. It stores the finite windows used to compare polynomial coefficients, root brackets, partial evaluations, and endpoint power-series coefficients.
\item $Q$ is the $\RealPowerSeriesUp$ endpoint row. It exposes the limiting coefficient stream, radius or subradius window, partial-sum route, and endpoint seal available to Real-facing consumers.
\item $W$ is the convergence-window ledger. It records the finite Cauchy comparison between inspected polynomial coefficient windows and the endpoint power-series windows at the requested precision.
\item $E$ is the evaluation-compatibility row. It records that each displayed polynomial evaluation from $A$ and each endpoint read from $Q$ use the same real-name, readback, and window discipline.
\item $T$ is the real-root limit route row. It records how the root-bracket data $B$ is retained through the finite coefficient-window convergence ledger before the endpoint series is exposed.
\item $H$ records componentwise $\hsame$ transport for approximant rows, root brackets, readbacks, stream windows, endpoint power-series rows, convergence windows, evaluation compatibility, the root-limit route, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from a Laguerre--Polya request through polynomial approximants, real-root brackets, regular readbacks, stream windows, endpoint power-series rows, and the root-limit route.
\item $P$ is the $\Pkg$ provenance over $\LaguerrePolyaUp$, $\RealPolynomialUp$, $\RealPowerSeriesUp$, $\RealRootBracketUp$, $\RealUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LaguerrePolyaUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $A,B,R,S,Q,W,E,T,H,C,P,N$. It has no coordinate for host entire functions, selected infinite products, quotient function spaces, non-displayed zero sets, countable-choice selectors, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Laguerre--Polya NameCert obligations]
\label{thm:laguerre-polya-namecert-obligations}
For every accepted $\LaguerrePolyaUp$ carrier
$$
L=(A,B,R,S,Q,W,E,T,H,C,P,N),
$$
the local $\NameCert_{\LaguerrePolyaUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the approximant family, real-root brackets, regular readbacks, stream windows, endpoint power-series row, convergence-window ledger, evaluation compatibility, real-root limit route, transport, replay, provenance, and local naming rows;
\item approximant discipline, requiring $A$ to expose only $\RealPolynomialUp$ names with finite coefficient windows and displayed evaluation requests;
\item real-root discipline, requiring $B$ to route every inspected approximant root through $\RealRootBracketUp$ rows before it can contribute to the limiting packet;
\item endpoint discipline, requiring $Q$ to expose the limiting object through $\RealPowerSeriesUp$ coefficient, radius-window, partial-sum, and endpoint rows;
\item convergence discipline, requiring $W$ and $E$ to compare approximant rows and endpoint rows only through $\RegSeqRatUp$, $\StreamNameUp$, and $\RealUp$ readbacks;
\item limit-route discipline, requiring $T$ to retain the bracketed real-root information through the displayed coefficient-window convergence route;
\item non-escape, requiring $H,C,P,N$ to export only the displayed route and no host entire function, selected infinite product, quotient function equality, non-displayed zero set, countable choice, or ambient $\RealUp$ completeness.
\end{enumerate}
No Laguerre--Polya obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:laguerre-polya-carrier}. The accepted packet displays exactly the approximant family, root-bracket rows, readback rows, stream windows, endpoint power-series row, convergence ledger, evaluation compatibility row, root-limit route, and structural rows.

The approximant and root disciplines are the first stage. The row $A$ gives only finite $\RealPolynomialUp$ data, while $B$ attaches the relevant roots to $\RealRootBracketUp$ evidence before any limiting route is available.

The endpoint and convergence disciplines are the second stage. The row $Q$ exposes the limiting object as a $\RealPowerSeriesUp$ endpoint, while $W$ and $E$ compare approximants and endpoint reads through $\RegSeqRatUp$, $\StreamNameUp$, and $\RealUp$ rows.

The limit-route discipline is the final analytic step. The row $T$ records that the displayed real-root evidence survives exactly along the coefficient-window convergence route already stored in $W$ and $E$.

Finally $H,C,P,N$ transport, replay, source, and name the same finite data. Since no excluded analytic object appears as a carrier coordinate, the local NameCert surface has no obligation row for such an object.
\end{proof}

\begin{theorem}[Laguerre--Polya real-root limit route]
\label{thm:laguerre-polya-real-root-limit-route}
Every Real-facing read from an accepted $\LaguerrePolyaUp$ carrier factors through
$$
A,B \longmapsto R,S \longmapsto Q,W,E \longmapsto T \longmapsto H,C,P,N .
$$
Thus the consumer first reads finite $\RealPolynomialUp$ approximants and their $\RealRootBracketUp$ evidence, then the $\RegSeqRatUp$ and $\StreamNameUp$ comparison windows, then the $\RealPowerSeriesUp$ endpoint and convergence ledger, and only then the retained real-root limit route. The handoff exports a displayed Laguerre--Polya certificate, not a host entire-function primitive or selected zero set.
\end{theorem}
\begin{proof}
Start with an accepted packet $L=(A,B,R,S,Q,W,E,T,H,C,P,N)$. The only approximant coordinates are $A$ and $B$, so a consumer cannot read a limiting function before it has inspected finite real-polynomial rows and their displayed real-root brackets.

The comparison stage is $R,S,W,E$. These rows bind coefficient reads, bracket endpoints, real-name windows, and approximant-versus-endpoint comparisons to regular rational readbacks and finite stream windows.

The endpoint stage is $Q$. It exposes the limiting object only as the $\RealPowerSeriesUp$ endpoint row named by the same comparison windows, so the endpoint is not a host entire function or quotient function-space element.

The retained root route is $T$, followed by $H,C,P,N$. These rows preserve, replay, package, and name the displayed data, and therefore the Real-facing consumer receives exactly the finite Laguerre--Polya route listed in the statement.
\end{proof}

\falsifiablePrediction{Every $\LaguerrePolyaUp$ read factors through finite $\RealPolynomialUp$ approximants, $\RealRootBracketUp$ root evidence, $\RegSeqRatUp$ coefficient readbacks, $\StreamNameUp$ comparison windows, one $\RealPowerSeriesUp$ endpoint row, a convergence-window ledger, evaluation compatibility, a retained real-root limit route, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{real-polynomial, realpowerseries, real-root-bracket, real, regseqrat, streamname: $\LaguerrePolyaUp$ is not bijective to any listed sibling because it jointly carries real-rooted polynomial approximants, displayed root brackets, coefficient-window convergence, endpoint power-series data, evaluation compatibility, root-limit routing, transport, replay, provenance, and local naming.}

\closureat{LaguerrePolyaUp}{seedStr}

\begin{closurestatus}{\LaguerrePolyaUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:laguerre-polya-carrier}, \autoref{thm:laguerre-polya-namecert-obligations}, and \autoref{thm:laguerre-polya-real-root-limit-route} seed the finite Laguerre--Polya route from real-rooted polynomial approximants through coefficient-window convergence to a real-power-series endpoint.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host entire functions, selected infinite products, quotient function spaces, non-displayed zero sets, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, approximant discipline, real-root discipline, endpoint discipline, convergence discipline, limit-route discipline, and non-escape for BEDC.Derived.LaguerrePolyaUp.}
  \constructivestory{A $\LaguerrePolyaUp$ packet is generated from finite $\RealPolynomialUp$ approximants, $\RealRootBracketUp$ root evidence, $\RegSeqRatUp$ coefficient and endpoint readbacks, $\StreamNameUp$ comparison windows, a $\RealPowerSeriesUp$ endpoint row, convergence-window and evaluation-compatibility ledgers, a retained real-root limit route, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
\end{closurestatus}
