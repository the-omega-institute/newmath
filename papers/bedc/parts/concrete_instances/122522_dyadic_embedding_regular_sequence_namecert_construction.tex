\chapter{A Concrete Naming Certificate for $\DyadicEmbeddingRegularSequenceUp$}
\label{ch:concrete-instances-dyadic-embedding-regular-sequence-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy real names, the $\DyadicEmbeddingRegularSequenceUp$ packet records the finite L10 route by which one displayed dyadic rational source is embedded as a constant regular rational sequence before a $\RealUp$ consumer may inspect the terminal seal. It sits beside the dyadic carrier of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only the finite embedding handoff; it does not assert quotient equality of real names, selected limits, countable choice, trichotomy, or ambient $\RealUp$ completeness.

\begin{definition}[DyadicEmbeddingRegularSequence carrier]
\label{def:dyadic-embedding-regular-sequence-carrier}
A $\DyadicEmbeddingRegularSequenceUp$ carrier is a finite $\BHist$ packet
$$
E=(D,W,R,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ source row, naming the dyadic rational being embedded and the dyadic arithmetic cells available to later tolerance reads;
\item $W$ is the constant $\StreamNameUp$ window row, exposing finite observations whose entries all read the same source dyadic row $D$;
\item $R$ is the $\RegSeqRatUp$ regularity row, recording the regular rational Cauchy schedule for the constant stream and its dyadic tolerance comparison;
\item $L$ is the terminal $\RealUp$ seal row, available only after $D,W,R$ have been displayed;
\item $H$ records componentwise $\hsame$ transport for dyadic source, constant window, regularity, Real seal, replay, provenance, and local naming rows;
\item $C$ records displayed $\Cont$ replay from a Real-facing request through dyadic admission, constant-window inspection, regular rational readback, Real seal consumption, provenance, and local naming;
\item $P$ is the $\Pkg$ provenance over $\DyadicEmbeddingRegularSequenceUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\DyadicEmbeddingRegularSequenceUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,W,R,L,H,C,P,N$. No coordinate stores a host real, quotient representative, selected limit, countable-choice schedule, trichotomy branch, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[DyadicEmbeddingRegularSequence obligation surface]
\label{thm:dyadic-embedding-regular-sequence-obligation-surface}
For every accepted $\DyadicEmbeddingRegularSequenceUp$ carrier
$$
E=(D,W,R,L,H,C,P,N),
$$
the local $\NameCert_{\DyadicEmbeddingRegularSequenceUp}$ surface consists exactly of dyadic source admission, constant $\StreamNameUp$ window exposure, $\RegSeqRatUp$ regularity readback for the constant stream, terminal $\RealUp$ seal gating, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. A Real-facing consumer may inspect the embedded dyadic name only through these rows.
\end{theorem}
\begin{proof}
Project the carrier $E=(D,W,R,L,H,C,P,N)$. The dyadic row $D$ is exposed before any stream or regularity read, so the source rational is a visible BEDC row rather than a hidden scalar.

The window row $W$ repeats the same source dyadic row at every displayed observation. The regularity row $R$ reads that constant window together with the dyadic tolerance budget, so the Cauchy schedule is finite row data.

Only after $D,W,R$ have been displayed can the Real seal $L$ be consumed. The support rows $H,C,P,N$ transport, replay, source, and name the same route. Since the packet has no coordinate for quotient equality, selected limits, countable choice, trichotomy, or ambient Real completeness, those data are not exported by the obligation surface.
\end{proof}

\begin{theorem}[DyadicEmbeddingRegularSequence Real seal]
\label{thm:dyadic-embedding-regular-sequence-real-seal}
Every $\RealUp$ read exported by an accepted $\DyadicEmbeddingRegularSequenceUp$ carrier first exposes the $\DyadicRatCoreUp$ source row $D$, the constant $\StreamNameUp$ window row $W$, and the $\RegSeqRatUp$ regularity row $R$. Therefore the Real seal is a finite embedding route from a dyadic rational to a regular sequence name, not a selected host real or an ambient completeness theorem.
\end{theorem}
\begin{proof}
Read the source row $D$ and the constant window row $W$. They make the dyadic rational and its repeated finite observations visible before any Real-facing consumer acts.

Use the regularity row $R$. It supplies the regular rational Cauchy readback and the dyadic tolerance comparison for the constant stream.

The terminal row $L$ is then reached through the displayed embedding route. Componentwise transport, replay, provenance, and local naming preserve that same route and add no host real, quotient representative, selected limit, choice schedule, trichotomy branch, or ambient completeness principle.
\end{proof}

\closureat{DyadicEmbeddingRegularSequenceUp}{seedStr}

\begin{closurestatus}{\DyadicEmbeddingRegularSequenceUp}
  \constructivestory{A $\DyadicEmbeddingRegularSequenceUp$ packet is generated from a DyadicRatCoreUp source row, a constant StreamNameUp window row, a RegSeqRatUp regularity row, a terminal RealUp seal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:dyadic-embedding-regular-sequence-carrier}, \autoref{thm:dyadic-embedding-regular-sequence-obligation-surface}, and \autoref{thm:dyadic-embedding-regular-sequence-real-seal} seed the finite dyadic-to-regular-sequence embedding surface.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \origin{human}
  \notclaimed{This chapter does not close quotient equality of real names, selected limits, countable choice, trichotomy, ambient $\RealUp$ completeness, Lean verification, audit cleanliness, or bridge checking.}
  \upgradepath{Obligation closure requires checked dyadic source admission, constant stream-window exposure, regular rational readback, Real seal gating, transport, replay, provenance, and local naming for BEDC.Derived.DyadicEmbeddingRegularSequenceUp.}
\end{closurestatus}
