\chapter{A Concrete Naming Certificate for $\MetricCompletionFunctorialSealUp$}
\label{ch:concrete-instances-metric-completion-functorial-seal-namecert}
\origin{human}

Following Bishop and Bridges on metric completions, the $\MetricCompletionFunctorialSealUp$ packet records the terminal finite seal by which a uniformly controlled map between displayed metric sources is read as a completion-facing functorial handoff. It sits after the metric-completion carrier of \autoref{ch:concrete-instances-metriccompletion-namecert}, the functoriality packet of \autoref{ch:concrete-instances-metric-completion-functoriality-namecert}, the dense-embedding surface of \autoref{ch:concrete-instances-metric-completion-dense-embedding-namecert}, the finite stream interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet gives Real and Completion consumers a single finite terminal-seal object; it does not assert quotient completion equality, selected representatives, countable choice, host function equality, or ambient $\RealUp$ completeness.

\begin{definition}[Metric completion functorial seal carrier]
\label{def:metric-completion-functorial-seal-carrier}
A $\MetricCompletionFunctorialSealUp$ carrier is a finite $\BHist$ packet
$$
S=(M,F,D,W,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricCompletionUp$ source-completion row, naming the displayed source and target completion boundaries.
\item $F$ is the $\MetricCompletionFunctorUp$ functoriality row, recording the map graph and the finite continuity control used by the completion handoff.
\item $D$ is the $\MetricCompletionDenseEmbeddingUp$ row, exposing the dense source embeddings that must be read before the completed map is sealed.
\item $W$ is the $\StreamNameUp$ finite-window row opened by the source and target approximation requests.
\item $R$ is the $\RegSeqRatUp$ readback row, repacking the opened windows as regular rational source and target names.
\item $E$ is the $\RealUp$ terminal seal, visible only after $M,F,D,W,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the completion, functoriality, dense-embedding, window, readback, Real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the same completion-facing functorial route.
\item $P$ is the $\Pkg$ provenance over $\MetricCompletionFunctorialSealUp$, $\MetricCompletionUp$, $\MetricCompletionFunctorUp$, $\MetricCompletionDenseEmbeddingUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MetricCompletionFunctorialSealUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $M,F,D,W,R,E,H,C,P,N$. It has no coordinate for quotient completion equality, selected representatives, countable choice, host function equality, global extension uniqueness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Metric completion functorial seal NameCert obligations]
\label{thm:metric-completion-functorial-seal-namecert-obligations}
For every accepted $\MetricCompletionFunctorialSealUp$ carrier
$$
S=(M,F,D,W,R,E,H,C,P,N),
$$
the local $\NameCert_{\MetricCompletionFunctorialSealUp}$ surface consists exactly of completion-boundary admission, functorial map-row admission, dense-embedding exposure, finite StreamName window inspection, RegSeqRat readback, terminal Real seal discipline, componentwise $\hsame$ transport, displayed $\Cont$ replay, package provenance, and local naming. Every completion-facing consumer must read the displayed route
$$
\MetricCompletionUp \longmapsto \MetricCompletionFunctorUp \longmapsto \MetricCompletionDenseEmbeddingUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
before the functorial seal is public.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metric-completion-functorial-seal-carrier}. The completion boundary, functorial map row, dense embedding row, finite stream window, regular rational readback, and Real seal are the only analytic coordinates.

The completion consumer first reads $M,F,D$. These rows display the source and target completion boundaries, the map graph with its continuity control, and the dense embeddings whose images are being sealed.

The finite observation rows then mediate the Real-facing read. The window row $W$ and readback row $R$ expose only inspected stream fragments and regular rational names before $E$ becomes visible.

Finally $H,C,P,N$ transport, replay, source, and name the same finite route. Since the carrier contains no coordinate for quotient completion equality, selected representatives, countable choice, host function equality, global extension uniqueness, or ambient Real completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Metric completion functorial seal dense readback]
\label{thm:metric-completion-functorial-seal-dense-readback}
Every dense-source read of an accepted $\MetricCompletionFunctorialSealUp$ carrier must expose the $\MetricCompletionDenseEmbeddingUp$ row before the $\StreamNameUp$ window, $\RegSeqRatUp$ readback, and terminal $\RealUp$ seal are consumed. Thus the completed map is available only through the displayed dense embedding and regular readback route, not through a selected completion representative, quotient stream equality, host function equality, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Start with the NameCert obligations of \autoref{thm:metric-completion-functorial-seal-namecert-obligations}. They place the dense-embedding row $D$ between the functorial completion row $F$ and the window/readback rows $W,R$.

The dense row controls the public source of the readback. A consumer may inspect the completed map only after the source embedding has been displayed, and the subsequent StreamName and RegSeqRat rows record the finite approximation data actually read.

The terminal seal $E$ and structural rows $H,C,P,N$ preserve that same route. No carrier coordinate supplies a selected representative, quotient stream equality, host function equality, or ambient completeness principle, so the dense readback remains the displayed finite handoff.
\end{proof}

\falsifiablePrediction{Every $\MetricCompletionFunctorialSealUp$ completion-facing read exposes MetricCompletionUp, MetricCompletionFunctorUp, MetricCompletionDenseEmbeddingUp, StreamNameUp, RegSeqRatUp, RealUp, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming before the functorial seal is public.}

\independenceWitness{metriccompletion, metric-completion-functoriality, metric-completion-dense-embedding, streamname, regseqrat, real: no listed sibling is bijective to $\MetricCompletionFunctorialSealUp$ because this packet jointly carries the completion boundary, functorial map row, dense embedding row, finite observation window, regular rational readback, and terminal Real seal as one NameCert object.}

\closureat{MetricCompletionFunctorialSealUp}{seedStr}

\begin{closurestatus}{\MetricCompletionFunctorialSealUp}
  \constructivestory{A $\MetricCompletionFunctorialSealUp$ packet is generated from MetricCompletionUp source and target boundaries, a MetricCompletionFunctorUp map row, a MetricCompletionDenseEmbeddingUp dense-source row, StreamNameUp windows, RegSeqRatUp readback, a RealUp terminal seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metric-completion-functorial-seal-carrier}, \autoref{thm:metric-completion-functorial-seal-namecert-obligations}, and \autoref{thm:metric-completion-functorial-seal-dense-readback} seed the finite terminal-seal route for metric-completion functorial reads.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient completion equality, selected representatives, countable choice, host function equality, global extension uniqueness, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, functorial map-row exactness, dense-embedding exposure, StreamName window inspection, RegSeqRat readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.MetricCompletionFunctorialSealUp.}
\end{closurestatus}
