\chapter{A Concrete Naming Certificate for $\BishopLocatedZeroUp$}
\label{ch:concrete-instances-bishop-located-zero-namecert}
\origin{human}

Following Bishop and Bridges, \emph{Constructive Analysis}, the $\BishopLocatedZeroUp$ packet records a constructive located zero witness over the L10 real-name substrate. It sits over the real-name seal of \autoref{ch:concrete-instances-real-namecert}, the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream-window route of \autoref{ch:concrete-instances-streamname-namecert}, and the located interval oscillation surface of \autoref{ch:concrete-instances-bishop-located-interval-oscillation-namecert}. The packet names the finite sign-change and bisection rows that later interval-search consumers may inspect; it is not a host intermediate-value theorem, quotient real equality, selected global root, countable choice, or ambient $\RealUp$ completeness principle.

\begin{definition}[Bishop located zero carrier]
\label{def:bishop-located-zero-carrier}
A $\BishopLocatedZeroUp$ carrier is a finite $\BHist$ packet
$$
Z=(B,M,S,Q,A,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the sign-change bracket row, recording lower and upper rational endpoint windows, the two displayed sign reads, and the non-overlap evidence that the bracket remains located.
\item $M$ is the uniform modulus row for the displayed function route, translating a requested sign tolerance into a finite source-window budget.
\item $S$ is the bisection schedule row, carrying the finite midpoint sequence and the chosen retained half at each displayed stage.
\item $Q$ is the $\RegSeqRatUp$ readback row for the midpoint and endpoint windows produced by $S$.
\item $A$ is the terminal $\RealUp$ seal read only after the bracket, modulus, schedule, and readback rows have been displayed.
\item $H$ records componentwise $\hsame$ transport for brackets, moduli, schedules, readbacks, seals, and local naming rows.
\item $C$ records displayed $\Cont$ replay through the route $B,M,S,Q,A,H$.
\item $P$ is the $\Pkg$ provenance over $\BishopLocatedZeroUp$, $\BishopLocatedIntervalOscillationUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\BishopLocatedZeroUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for host real equality, a selected global root, quotient stream equality, countable choice, open-cover compactness, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Bishop located zero NameCert obligations]
\label{thm:bishop-located-zero-namecert-obligations}
For every accepted $\BishopLocatedZeroUp$ carrier $Z=(B,M,S,Q,A,H,C,P,N)$, the local $\NameCert_{\BishopLocatedZeroUp}$ surface is exhausted by sign-change bracket admission through $B$, uniform modulus exposure through $M$, bisection schedule exposure through $S$, RegSeqRat readback through $Q$, Real sealing through $A$, componentwise transport through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-located-zero-carrier}. The analytic input is not a host function equality claim; it is the displayed bracket row $B$ together with the modulus row $M$.

The finite bisection data is then forced. The schedule row $S$ records the retained half at each displayed stage, $Q$ reads the same midpoint and endpoint windows as regular-rational data, and $A$ seals the resulting real-name boundary only after those rows have been displayed.

The structural rows $H,C,P,N$ transport, replay, source, and name that same route. Since the carrier has no selected-root, quotient, choice, compactness, host-equality, or completeness coordinate, no such object can become a local NameCert obligation.
\end{proof}

\begin{theorem}[Bishop located zero bisection soundness]
\label{thm:bishop-located-zero-bisection-soundness}
Let $Z=(B,M,S,Q,A,H,C,P,N)$ be an accepted $\BishopLocatedZeroUp$ carrier. Every zero-witness read produced by the displayed bisection schedule factors through the sign-change bracket $B$, uniform modulus $M$, retained-half schedule $S$, regular-rational readback $Q$, and terminal Real seal $A$ before it reaches transport, replay, provenance, and naming rows. The read is therefore a constructive located zero surface inside the L10 real-name route, not a host intermediate-value theorem or a selected global root.
\end{theorem}
\begin{proof}
Start with the sign-change bracket. The row $B$ is the only source for the two sign reads and the located endpoint comparison, so a bisection consumer cannot begin from an ambient real interval.

Next use the modulus and schedule rows. The row $M$ gives the finite tolerance-to-window budget, and $S$ records each retained half by displayed midpoint data. The readback row $Q$ then supplies the regular-rational midpoint and endpoint route consumed by the terminal seal $A$.

Finally attach the structural rows. Componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and the local $\NameCert$ row preserve the same finite packet. Thus bisection soundness is a row-exhaustion statement for $B,M,S,Q,A,H,C,P,N$, with no host equality, quotient stream, selected limit, countable choice, compactness, or ambient completeness imported.
\end{proof}

\falsifiablePrediction{Every accepted $\BishopLocatedZeroUp$ consumer that reads a zero witness while omitting one of the sign-change bracket, uniform modulus, bisection schedule, RegSeqRat readback, Real seal, hsame, Cont, Pkg, or NameCert rows has left the packet.}

\independenceWitness{bishoplocatedintervaloscillation, streamname, regseqrat, real: $\BishopLocatedZeroUp$ is not bijective with any listed sibling because it joins sign-change bracket data, a uniform modulus, a finite bisection schedule, regular-rational readback, Real sealing, transport, replay, provenance, and local naming as one finite packet.}

\closureat{BishopLocatedZeroUp}{seedStr}

\begin{closurestatus}{\BishopLocatedZeroUp}
  \constructivestory{A $\BishopLocatedZeroUp$ packet is generated from a sign-change bracket, a uniform modulus, a finite bisection schedule, RegSeqRat readback, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-located-zero-carrier}, \autoref{thm:bishop-located-zero-namecert-obligations}, and \autoref{thm:bishop-located-zero-bisection-soundness} seed the finite located zero witness surface for L10 Real and Completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim a host intermediate-value theorem, selected global root, quotient real equality, quotient stream equality, countable choice, open-cover compactness, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, bracket admission, modulus exposure, bisection schedule exposure, RegSeqRat readback, Real sealing, transport, replay, provenance, and local naming for BEDC.Derived.BishopLocatedZeroUp.}
\end{closurestatus}
