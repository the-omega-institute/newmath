\chapter{A Concrete Naming Certificate for $\ClaimAuditWitnessUp$}
\label{ch:concrete-instances-claim-audit-witness-namecert}
\origin{ai}

The $\ClaimAuditWitnessUp$ packet realizes the witness side of the legitimate-claim audit of \autoref{ch:visions-legitimate-claim-audit} as finite BEDC NameCert data. It sits beside the two-axis closure discipline of \autoref{ch:proof-obligations-lean-scaffold-contract}, the gap boundary of \autoref{ch:concrete-instances-gapclosureboundary-namecert}, the legitimate-claim ledger of \autoref{ch:concrete-instances-legitimate-claim-audit-ledger-namecert}, and the claim-audit boundary of \autoref{ch:concrete-instances-claim-audit-boundary-namecert}. Those siblings classify claim status and refusal boundaries; this packet records the particular witness row that lets a reader audit one claim without promoting missing support into theorem content.

\begin{definition}[Claim audit witness carrier]
\label{def:claim-audit-witness-carrier}
A $\ClaimAuditWitnessUp$ carrier is a finite $\BHist$ packet

$$
W=(Q,O,T,F,R,H,C,P,N)
$$

with the following displayed rows:
\begin{enumerate}
\item $Q$ is the claim-text row, recording the finite statement being audited.
\item $O$ is the cited-object row, naming the theorem, definition, closure block, carrier, or refusal boundary that the claim cites.
\item $T$ is the closure-axis token row, recording the displayed theory-closure token attached to the cited object.
\item $F$ is the formal-status token row, recording the displayed verification token attached to the cited object.
\item $R$ is the off-table refusal row, recording the boundary exposed when $Q$ asks for a closure or verification status not displayed by $T$ and $F$.
\item $H$ records componentwise $\hsame$ transport for claim text, cited object, closure token, formal token, refusal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from claim admission through cited-object lookup, two-axis token readback, refusal exposure, and permitted witness publication.
\item $P$ is the $\Pkg$ provenance over $\ClaimAuditWitnessUp$, $\LegitimateClaimAuditLedgerUp$, $\ClaimAuditBoundaryUp$, $\GapClosureBoundaryUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\ClaimAuditWitnessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $Q,O,T,F,R,H,C,P,N$. It has no coordinate for a global truth predicate, hidden marker resolution, off-table verification, or claim strength beyond the displayed theory and formal tokens.
\end{definition}

\begin{theorem}[Claim audit witness NameCert obligations]
\label{thm:claim-audit-witness-namecert-obligations}
For every accepted $\ClaimAuditWitnessUp$ carrier $W=(Q,O,T,F,R,H,C,P,N)$, the local $\NameCert_{\ClaimAuditWitnessUp}$ surface consists exactly of claim-text admission, cited-object readback, closure-axis token readback, formal-status token readback, off-table refusal exposure, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:claim-audit-witness-carrier}. The rows $Q,O,T,F$ display the claim, the cited object, and the two tokens that license what may be said about the object.

If the claim asks for a status not displayed by $T$ and $F$, the row $R$ is exposed before publication. Thus a missing bridge, missing Lean theorem, or missing closure grade remains a refusal row rather than becoming theorem content.

The rows $H,C,P,N$ preserve the witness under transport, replay, provenance, and local naming. Since the carrier has no hidden truth predicate, marker resolver, off-table verification row, or stronger claim coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Claim audit witness two-axis nonescape]
\label{thm:claim-audit-witness-two-axis-nonescape}
Every accepted $\ClaimAuditWitnessUp$ read factors a claim through the displayed route

$$
Q,O,T,F,R,H,C,P,N.
$$

Consequently a consumer may publish only the closure and verification strength displayed by $T$ and $F$; if stronger support is requested, the consumer must expose $R$ as a refusal row before the claim can be read.
\end{theorem}
\begin{proof}
Start from \autoref{thm:claim-audit-witness-namecert-obligations}. The cited object $O$ is not read alone: it is paired with the theory token $T$ and the formal token $F$ inside the same finite packet.

The refusal row $R$ handles off-table requests. When $Q$ asks for public closure, bridge checking, theorem status, or object truth not displayed by the two tokens, the replay route $C$ sends the request to $R$ rather than to a hidden proof source.

Finally $H,P,N$ transport, package, and name the same distinction. They cannot raise either axis, discharge a missing bridge, or resolve a marker not displayed by the packet.
\end{proof}

\falsifiablePrediction{If a claim-audit read publishes a stronger closure or verification status than the displayed rows $T$ and $F$ while omitting the refusal row $R$, it is not a $\ClaimAuditWitnessUp$ packet.}

\independenceWitness{legitimate-claim-audit-ledger, claim-audit-boundary, gapclosureboundary: none is bijective to $\ClaimAuditWitnessUp$ because the ledger records claim status, the boundary records permitted publication, and the gap boundary records refusal routes, while this packet jointly carries one claim text, one cited object, two-axis tokens, an off-table refusal row, transport, replay, provenance, and local naming.}

The future TasteGate file for $\mathsf{lean4/BEDC/Derived/ClaimAuditWitnessUp}$ must supply explicit $\mathsf{Nontrivial}\ \ClaimAuditWitnessUp$ and $\mathsf{FieldFaithful}\ \ClaimAuditWitnessUp$ instances. The field-faithfulness witness must keep $Q,O,T,F,R,H,C,P,N$ distinct, so claim text, cited object, theory token, formal token, and refusal exposure cannot collapse.

\closureat{ClaimAuditWitnessUp}{seedStr}

\begin{closurestatus}{\ClaimAuditWitnessUp}
  \constructivestory{A $\ClaimAuditWitnessUp$ packet is generated from claim text, cited object, theory-closure token, formal-status token, off-table refusal, componentwise hsame, displayed Cont, Pkg provenance, and local NameCert rows.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:claim-audit-witness-carrier}, \autoref{thm:claim-audit-witness-namecert-obligations}, and \autoref{thm:claim-audit-witness-two-axis-nonescape} seed the finite witness surface for two-axis legitimate-claim audits.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close global truth, registry-wide claim correctness, Lean marker resolution, bridge checking, or any closure or verification status beyond the displayed packet rows.}
  \upgradepath{Obligation closure requires checked carrier admission, cited-object readback, two-axis token readback, refusal exposure, transport, replay, provenance, local naming, and TasteGate field faithfulness for BEDC.Derived.ClaimAuditWitnessUp.}
\end{closurestatus}
