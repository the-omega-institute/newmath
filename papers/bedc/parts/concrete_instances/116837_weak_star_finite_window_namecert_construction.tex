\chapter{A Concrete Naming Certificate for $\WeakStarFiniteWindowUp$}
\label{ch:concrete-instances-weak-star-finite-window-namecert}
\origin{human}

Following Banach--Alaoglu and Kelley functional analysis, the $\WeakStarFiniteWindowUp$ packet records the finite observation surface that a weak-star compactness route may inspect before any full compactness theorem is invoked. It sits between the weak-star topology window of \autoref{ch:concrete-instances-weak-star-topology-namecert}, the finite dual-ball handoff of \autoref{ch:concrete-instances-banach-alaoglu-namecert}, the locally convex source rows of \autoref{ch:concrete-instances-locally-convex-space-namecert}, the compact metric consumer rows of \autoref{ch:concrete-instances-compactmetric-namecert}, and the scalar seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only finite dual-ball probes, sampled weak-star coordinates, and tolerance windows; it does not assert full weak-star compactness, choose subnets, construct quotient dual spaces, or import ambient completeness.

\begin{definition}[Weak-star finite-window carrier]
\label{def:weak-star-finite-window-carrier}
A $\WeakStarFiniteWindowUp$ carrier is a finite $\BHist$ packet
$$
W=(D,S,E,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the finite dual-ball probe row, listing the displayed bounded functional probes and source points admitted from $\BanachAlaogluUp$ and $\WeakStarTopologyUp$.
\item $S$ is the sampled weak-star coordinate row, recording only the scalar values read from the finite probes in $D$.
\item $E$ is the $\RealUp$ tolerance-window row, containing the finite scalar radii and comparison bounds used by the observation.
\item $T$ records componentwise $\hsame$ transport for the dual-ball probes, sampled coordinates, tolerance windows, replay, provenance, and local naming rows.
\item $H$ records the displayed finite-window history, including the locally convex source and compact-metric consumer context visible to the packet.
\item $C$ records displayed $\Cont$ replay from a weak-star observation request through
$$
D \longmapsto S \longmapsto E \longmapsto T,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\WeakStarFiniteWindowUp$, $\BanachAlaogluUp$, $\WeakStarTopologyUp$, $\LocallyConvexSpaceUp$, $\CompactMetricUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\WeakStarFiniteWindowUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,S,E,T,H,C,P,N$. It has no coordinate for full weak-star compactness, choice-selected subnets, quotient dual spaces, or ambient completeness.
\end{definition}

\begin{theorem}[Weak-star finite-window NameCert obligations]
\label{thm:weak-star-finite-window-namecert-obligations}
For every accepted $\WeakStarFiniteWindowUp$ carrier $W=(D,S,E,T,H,C,P,N)$, the local $\NameCert_{\WeakStarFiniteWindowUp}$ surface consists exactly of finite dual-ball probe admission through $D$, sampled weak-star coordinate admission through $S$, $\RealUp$ tolerance-window admission through $E$, componentwise transport through $T$, finite-window history exposure through $H$, displayed replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier. The finite rows $D,S,E,T,H,C,P,N$ are the entire public inventory of the packet.

The dual-ball and topology data enter only through $D$. The sampled coordinates are then read through $S$, and every scalar comparison must pass through the $\RealUp$ tolerance row $E$.

The structural rows do not add compactness or selection data. Transport, history, replay, provenance, and naming preserve the same finite route, so the NameCert surface is exhausted by the listed rows.
\end{proof}

\begin{theorem}[Weak-star finite-window Banach--Alaoglu route]
\label{thm:weak-star-finite-window-banach-alaoglu-route}
Every Banach--Alaoglu, weak-star topology, locally convex, compact metric, or Real-and-Completion consumer that reads an accepted $\WeakStarFiniteWindowUp$ carrier must factor its request through
$$
D \longmapsto S \longmapsto E \longmapsto T,H,C,P,N .
$$
The consumer receives finite dual-ball probes, sampled weak-star coordinates, $\RealUp$ tolerance windows, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. It receives no full weak-star compactness theorem, no choice-selected subnet, no quotient dual space, and no ambient completeness principle.
\end{theorem}
\begin{proof}
Start with a compactness-facing or duality-facing request. The finite probe row $D$ must be exposed before any scalar coordinate is visible, so the consumer cannot read an unrestricted dual ball or a completed function space.

The sampled weak-star coordinate row $S$ and the tolerance row $E$ then determine the only scalar comparisons available to the request. These rows are finite and pass through the displayed $\RealUp$ seal.

Finally, $T,H,C,P,N$ transport, replay, source, and name the same finite observation. Since none of the carrier rows is a subnet selector, quotient-dual construction, full weak-star compactness theorem, or ambient completeness principle, the route remains the finite Banach--Alaoglu window named in the statement.
\end{proof}

\falsifiablePrediction{Every public $\WeakStarFiniteWindowUp$ read factors through finite dual-ball probes, sampled weak-star coordinates, a $\RealUp$ tolerance window, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming. A public read that exports full weak-star compactness, a choice-selected subnet, quotient dual space data, or ambient completeness from this packet refutes the carrier boundary.}

\independenceWitness{banach-alaoglu, weak-star-topology, locally-convex-space, compactmetric, real: $\WeakStarFiniteWindowUp$ is not bijective to any listed sibling because it jointly carries finite dual-ball probes, sampled weak-star coordinates, tolerance windows, history, transport, replay, provenance, and local naming while refusing full compactness, subnet choice, quotient dual spaces, and ambient completeness.}

\closureat{WeakStarFiniteWindowUp}{seedStr}

\begin{closurestatus}{\WeakStarFiniteWindowUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:weak-star-finite-window-carrier}, \autoref{thm:weak-star-finite-window-namecert-obligations}, and \autoref{thm:weak-star-finite-window-banach-alaoglu-route} seed the finite weak-star observation surface for dual-ball and compactness-facing consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close full weak-star compactness, choice-selected subnets, quotient dual spaces, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite dual-ball probe admission, sampled coordinate exactness, Real tolerance-window routing, transport, replay, provenance, and local naming for BEDC.Derived.WeakStarFiniteWindowUp.}
  \constructivestory{A $\WeakStarFiniteWindowUp$ packet is generated from finite dual-ball probes, sampled weak-star coordinates, Real tolerance windows, componentwise hsame transport, finite-window history, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
