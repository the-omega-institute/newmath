\chapter{A Concrete Naming Certificate for $\SpeckerMonotoneBoundaryUp$}
\label{ch:concrete-instances-specker-monotone-boundary-namecert}
\origin{human}

Following Specker's bounded monotone sequence construction and Bishop--Bridges constructive analysis, the $\SpeckerMonotoneBoundaryUp$ packet names the finite boundary between bounded monotone rational stream data and any later located-supremum or completion-facing claim. It is read next to the Specker obstruction surface of \autoref{ch:concrete-instances-specker-sequence-boundary-namecert}, the stream schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet records monotone bounded window data and the refusal rows that keep that data from becoming a located supremum or ambient completeness principle.

\begin{definition}[Specker monotone boundary carrier]
\label{def:specker-monotone-boundary-carrier}
A $\SpeckerMonotoneBoundaryUp$ carrier is a finite $\BHist$ packet
$$
M=(S,W,D,R,B,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\SpeckerSequenceBoundaryUp$ source row naming the nonlocated-supremum obstruction inherited from the bounded monotone Specker stream.
\item $W$ is the $\StreamNameUp$ schedule selecting the finite rational windows inspected by the boundary packet.
\item $D$ is the $\DyadicRatCoreUp$ tolerance and bound ledger read from those windows.
\item $R$ is the $\RegSeqRatUp$ readback row for the regular rational data visible at the scheduled windows.
\item $B$ is the monotone bounded stream ledger recording displayed order, bound, and window-comparison data before any supremum-facing row is considered.
\item $L$ is the refusal row separating the displayed stream data from a located supremum, a selected limit, and any ambient $\RealUp$ completeness claim.
\item $H$ stores componentwise $\hsame$ transport for $S,W,D,R,B,L$.
\item $C$ stores displayed $\Cont$ replay from source and schedule rows through dyadic bounds, regular readback, monotone bounded evidence, and the refusal row.
\item $P$ is the $\Pkg$ provenance over $\SpeckerMonotoneBoundaryUp$, $\SpeckerSequenceBoundaryUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\SpeckerMonotoneBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,W,D,R,B,L,H,C,P,N$. It has no coordinate for a located supremum, selected limit, quotient stream equality, trichotomy, countable-choice selector, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Specker monotone boundary NameCert obligations]
\label{thm:specker-monotone-boundary-namecert-obligations}
For every accepted $\SpeckerMonotoneBoundaryUp$ carrier
$$
M=(S,W,D,R,B,L,H,C,P,N),
$$
the local $\NameCert_{\SpeckerMonotoneBoundaryUp}$ surface consists exactly of source admission through $S$, finite schedule selection through $W$, dyadic tolerance and bound accounting through $D$, regular rational readback through $R$, bounded monotone ledger admission through $B$, supremum-and-completeness refusal through $L$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:specker-monotone-boundary-carrier}. The analytic rows are precisely the Specker boundary source, stream schedule, dyadic ledger, regular rational readback, bounded monotone ledger, and refusal row.

The structural rows $H,C,P,N$ transport, replay, source, and name those same coordinates. Since the carrier contains no further analytic field, the listed rows exhaust the local NameCert obligations.

The excluded principles would require extra public coordinates. A located supremum, selected limiting real, quotient stream equality, trichotomy branch, countable-choice selector, or ambient completeness theorem is therefore not part of the accepted packet.
\end{proof}

\begin{theorem}[Specker monotone boundary nonescape]
\label{thm:specker-monotone-boundary-nonescape}
Every consumer of an accepted $\SpeckerMonotoneBoundaryUp$ carrier receives only the finite rows $S,W,D,R,B,L,H,C,P,N$. Consequently bounded monotone rational stream data may be passed to $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$-facing readers only through the displayed schedule, tolerance ledger, regular readback, bounded monotone ledger, and refusal row; it exports no located supremum, selected limit, trichotomy, quotient stream equality, countable choice, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Use \autoref{thm:specker-monotone-boundary-namecert-obligations} to identify the complete public surface of the accepted packet. Every analytic read passes through $S,W,D,R,B,L$ before the structural rows can name or replay it.

The Specker source row records the obstruction surface, while $W,D,R,B$ expose only finite stream windows, dyadic bounds, regular rational readbacks, and monotone bounded comparison data. These rows do not contain a selector for a supremum or a quotient equality on streams.

Finally read the refusal row $L$. It is the only completion-facing boundary coordinate, and it records separation from located supremum and ambient Real completeness rather than supplying them. Transport, replay, provenance, and naming preserve that finite route, so none of the excluded principles can escape from the packet.
\end{proof}

\falsifiablePrediction{Every $\SpeckerMonotoneBoundaryUp$ consumer that treats bounded monotone rational stream data as a located supremum or as ambient $\RealUp$ completeness without displaying $S,W,D,R,B,L,H,C,P,N$ refutes \autoref{def:specker-monotone-boundary-carrier}.}

\independenceWitness{Nearest siblings: specker-sequence-boundary, streamname, regseqrat, dyadicratcore, and real. $\SpeckerMonotoneBoundaryUp$ is not bijective to any listed sibling because it jointly carries the Specker obstruction source, finite stream schedule, dyadic bound ledger, regular rational readback, bounded monotone ledger, refusal row, transport, replay, provenance, and local naming.}

\closureat{SpeckerMonotoneBoundaryUp}{seedStr}

\begin{closurestatus}{\SpeckerMonotoneBoundaryUp}
  \constructivestory{A $\SpeckerMonotoneBoundaryUp$ packet is generated from a SpeckerSequenceBoundary source row, a StreamName schedule, a DyadicRatCore tolerance and bound ledger, a RegSeqRat readback row, a bounded monotone stream ledger, a located-supremum and Real-completeness refusal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:specker-monotone-boundary-carrier}, \autoref{thm:specker-monotone-boundary-namecert-obligations}, and \autoref{thm:specker-monotone-boundary-nonescape} seed the finite boundary between bounded monotone rational stream data and completion-facing claims.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim located supremum construction, selected limits, trichotomy, quotient stream equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, NameCert obligations, monotone bounded readback, nonescape, classifier transport, displayed replay, provenance, and local naming for BEDC.Derived.SpeckerMonotoneBoundaryUp.}
\end{closurestatus}
