\chapter{A Concrete Naming Certificate for $\MetaCICBoundedChurchRosserFragmentUp$}
\label{ch:concrete-instances-metacic-bounded-church-rosser-fragment-namecert}
\origin{ai}

The $\MetaCICBoundedChurchRosserFragmentUp$ packet realizes the conditional confluence boundary of \autoref{sec:metacic-confluence-audit-map-conditional} as finite NameCert data. It sits beside the critical-path source of \autoref{ch:concrete-instances-metaciccriticalpath-namecert}, the typed parallel-diamond boundary of \autoref{ch:concrete-instances-metacic-typed-parallel-diamond-boundary-namecert}, the parallel-diamond frontier of \autoref{ch:concrete-instances-metacic-parallel-diamond-frontier-namecert}, and the confluence audit packet of \autoref{ch:concrete-instances-metacic-confluence-audit-packet-namecert}. The packet records atom fragments, normal-source fragments, conditional diamond rows, obstruction retention, transport, replay, provenance, and local naming without claiming full Church--Rosser, residual-substitution discharge, subject reduction, quotient syntax equality, or kernel consistency.

\begin{definition}[MetaCIC bounded Church--Rosser fragment carrier]
\label{def:metacic-bounded-church-rosser-fragment-carrier}
A $\MetaCICBoundedChurchRosserFragmentUp$ carrier is a finite $\BHist$ packet
$$
M=(A,N,C,O,T,R,P,S,H)
$$
with the following visible rows:
\begin{enumerate}
\item $A$ is the atom fragment row, retaining the unconditional atom joins already exposed by the checked confluence substrate;
\item $N$ is the normal-source row, retaining the unconditional closed-normal fragments already exposed by the checked substrate;
\item $C$ is the conditional-diamond row, recording the substitution-sensitive bridge to the parallel-diamond frontier;
\item $O$ is the obstruction row, keeping the residual-substitution boundary visible rather than silently discharged;
\item $T$ is the typed-window row, recording the finite confluence input window read by the audit map;
\item $R$ is the residual-frontier row, tracking the residual-premise budget available to the bridge;
\item $P$ is the $\MetaCICParallelDiamondFrontierUp$ provenance row consumed by the fragment;
\item $S$ is the $\MetaCICTypedParallelDiamondBoundaryUp$ source row, exposing the typed boundary before the conditional bridge is read;
\item $H$ records componentwise $\hsame$ transport for the atom, normal-source, conditional-diamond, obstruction, typed-window, residual-frontier, provenance, source, and local naming rows.
\end{enumerate}
The carrier has no coordinate for full Church--Rosser, residual-substitution discharge, subject reduction, quotient syntax equality, evaluator equality, or kernel consistency.
\end{definition}

\begin{theorem}[MetaCIC bounded Church--Rosser fragment NameCert obligations]
\label{thm:metacic-bounded-church-rosser-fragment-namecert-obligations}
For every accepted $\MetaCICBoundedChurchRosserFragmentUp$ carrier
$$
M=(A,N,C,O,T,R,P,S,H),
$$
the local $\NameCert_{\MetaCICBoundedChurchRosserFragmentUp}$ surface consists exactly of carrier admission for the displayed rows, atom-fragment exposure through $A$, normal-source exposure through $N$, conditional-diamond exposure through $C$, obstruction retention through $O$, typed-window exposure through $T$, residual-frontier exposure through $R$, parallel-frontier provenance through $P$, typed-boundary source exposure through $S$, and componentwise transport through $H$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:metacic-bounded-church-rosser-fragment-carrier}. The packet displays the atom and normal-source fragments, the conditional diamond row, the obstruction row, the typed window, the residual frontier, the provenance row, the typed boundary source, and the transport row.

Atom and normal-source exposure are the unconditional part of the packet. Those rows record the checked fragments already available from the confluence substrate, so the packet can name them without turning them into a global confluence theorem.

The conditional-diamond row $C$ and obstruction row $O$ keep the residual-substitution boundary visible. The typed-window row $T$, residual-frontier row $R$, provenance row $P$, and source row $S$ place that conditional bridge inside the same finite audit surface rather than outside it.

The structural row $H$ only transports the displayed packet. Because the carrier has no coordinate for full Church--Rosser, residual-substitution discharge, subject reduction, quotient syntax equality, evaluator equality, or kernel consistency, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[MetaCIC bounded Church--Rosser fragment conditional route]
\label{thm:metacic-bounded-church-rosser-fragment-conditional-route}
Every accepted $\MetaCICBoundedChurchRosserFragmentUp$ packet factors its confluence-facing read through the displayed route
$$
\MetaCICCriticalPathUp \longmapsto \MetaCICTypedParallelDiamondBoundaryUp \longmapsto \MetaCICParallelDiamondFrontierUp \longmapsto \MetaCICConfluenceAuditPacketUp .
$$
The route first opens the critical path, then the typed boundary, then the parallel frontier, and only then the audit packet. The fragment therefore names a bounded Church--Rosser boundary, not a full closed Church--Rosser theorem or a residual-substitution discharge.
\end{theorem}
\begin{proof}
Start with the critical-path surface. It supplies the finite source package from which the confluence audit map reads its candidate evidence.

Next read the typed parallel-diamond boundary. That boundary keeps the typed-window and residual-substitution rows visible before any conditional bridge is consumed.

Then read the parallel-diamond frontier. The frontier keeps the local-join and obstruction rows visible, so the route remains conditional rather than unconditional.

Finally consume the audit packet. The route reaches the confluence audit map only after the checked fragments and boundary rows have been displayed, so it cannot be reinterpreted as full Church--Rosser, residual-substitution discharge, or kernel consistency.
\end{proof}

\begin{theorem}[MetaCIC bounded Church--Rosser fragment nonescape]
\label{thm:metacic-bounded-church-rosser-fragment-nonescape}
Every accepted $\MetaCICBoundedChurchRosserFragmentUp$ consumer must keep the atom fragment, normal-source fragment, conditional-diamond row, obstruction row, typed-window row, residual-frontier row, provenance row, and typed-boundary source row visible before citing the packet as confluence evidence. No consumer may replace that route by full Church--Rosser, residual-substitution discharge, subject reduction, quotient syntax equality, or kernel consistency.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:metacic-bounded-church-rosser-fragment-carrier}. The visible rows are atom, normal-source, conditional diamond, obstruction, typed-window, residual-frontier, provenance, source, and transport.

Apply the NameCert obligations of \autoref{thm:metacic-bounded-church-rosser-fragment-namecert-obligations}. They keep the unconditional fragments and the conditional bridge separate, so a consumer must display the boundary rows before the fragment may be cited.

Use the conditional route of \autoref{thm:metacic-bounded-church-rosser-fragment-conditional-route}. It forces the confluence-facing read through the critical-path, typed-boundary, frontier, and audit rows, and therefore prevents the packet from being cited as full Church--Rosser or a residual-substitution discharge.
\end{proof}

\falsifiablePrediction{An accepted $\MetaCICBoundedChurchRosserFragmentUp$ packet that is cited as confluence evidence must display atom-fragment, normal-source, conditional-diamond, obstruction, typed-window, residual-frontier, provenance, source, and transport rows.}

\independenceWitness{metaciccriticalpath, metacic-parallel-diamond-frontier, metacic-typed-parallel-diamond-boundary, metacicconfluenceauditpacket: none is bijective to $\MetaCICBoundedChurchRosserFragmentUp$ because this packet jointly carries the atom fragment, normal-source fragment, conditional-diamond row, obstruction row, typed-window row, residual-frontier row, provenance row, source row, and transport row that keep the conditional boundary finite. The Lean TasteGate.lean follow-up must provide Nontrivial and FieldFaithful instances for the same carrier surface.}

\closureat{MetaCICBoundedChurchRosserFragmentUp}{seedStr}

\begin{closurestatus}{\MetaCICBoundedChurchRosserFragmentUp}
  \constructivestory{A $\MetaCICBoundedChurchRosserFragmentUp$ packet is generated from atom fragments, normal-source fragments, a conditional-diamond row, an obstruction row, a typed-window row, a residual-frontier row, a parallel-frontier provenance row, a typed-boundary source row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:metacic-bounded-church-rosser-fragment-carrier}, \autoref{thm:metacic-bounded-church-rosser-fragment-namecert-obligations}, \autoref{thm:metacic-bounded-church-rosser-fragment-conditional-route}, and \autoref{thm:metacic-bounded-church-rosser-fragment-nonescape} bind the finite conditional confluence boundary for atom fragments, normal-source fragments, typed windows, residual frontiers, obstruction retention, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim full Church--Rosser, residual-substitution discharge, subject reduction, quotient syntax equality, evaluator equality, kernel consistency, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked atom-fragment exposure, normal-source exposure, conditional-diamond routing, obstruction retention, typed-window exposure, residual-frontier exposure, transport, replay, provenance, and local naming for BEDC.Derived.MetaCICBoundedChurchRosserFragmentUp.}
\end{closurestatus}
