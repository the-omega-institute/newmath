\chapter{A Concrete Naming Certificate for $\ArzelaAscoliFiniteEquicontinuousUp$}
\label{ch:concrete-instances-arzela-ascoli-finite-equicontinuous-namecert}
\origin{human}

Following Arzela, Ascoli, and Bishop--Bridges constructive compactness, the $\ArzelaAscoliFiniteEquicontinuousUp$ packet records the finite extraction surface where equicontinuity and compact finite nets are read before a sequence-window consumer reaches a real seal. It sits downstream of the finite interval-net route of \autoref{ch:concrete-instances-heine-borel-finite-net-namecert}, the modulus composition route of \autoref{ch:concrete-instances-uniform-modulus-composition-namecert}, the compact uniform modulus extractor of \autoref{ch:concrete-instances-compact-uniform-modulus-extractor-namecert}, the convergent-sequence boundary of \autoref{ch:concrete-instances-real-convergent-sequence-namecert}, the located Cauchy subsequence route of \autoref{ch:concrete-instances-located-cauchy-subsequence-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the real seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names only the finite equicontinuous extraction packet; it does not assert open-cover compactness, selected infinite subsequence choice, quotient stream equality, countable choice, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Arzela--Ascoli finite equicontinuous carrier]
\label{def:arzela-ascoli-finite-equicontinuous-carrier}
An $\ArzelaAscoliFiniteEquicontinuousUp$ carrier is a finite $\BHist$ packet
$$
A=(K,E,U,M,D,W,Q,L,S,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the compact finite-net source row, read through the $\HeineBorelFiniteNetUp$ and compact-metric finite-net data already visible to the packet.
\item $E$ is the finite equicontinuity-modulus row, assigning each displayed target tolerance a displayed source radius uniformly over the inspected family.
\item $U$ is the $\UniformModulusCompositionUp$ and $\CompactUniformModulusExtractorUp$ compatibility row, recording that the compact finite-net and equicontinuity ledgers use the same tolerance requests.
\item $M$ is the diagonal or finite-window subsequence row, exposed only as the finite window data consumed by $\RealConvergentSequenceUp$ and $\LocatedCauchySubsequenceUp$ routes.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for source radii, target tolerances, mesh slack, and comparison budgets.
\item $W$ is the finite $\StreamNameUp$ window row naming the inspected source points, function-family indices, and target observations.
\item $Q$ is the $\RegSeqRatUp$ readback row attached to the rational observations on $W$.
\item $L$ is the located convergence-window row used by the $\LocatedCauchySubsequenceUp$ consumer.
\item $S$ is the terminal $\RealUp$ seal row, available only after $K,E,U,M,D,W,Q,L$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for all source, modulus, window, readback, located, seal, replay, provenance, and naming rows.
\item $C$ records displayed $\Cont$ replay from a finite extraction request through $K,E,U,M,D,W,Q,L,S$.
\item $P$ is the $\Pkg$ provenance over $\ArzelaAscoliFiniteEquicontinuousUp$, $\HeineBorelFiniteNetUp$, $\UniformModulusCompositionUp$, $\CompactUniformModulusExtractorUp$, $\RealConvergentSequenceUp$, $\LocatedCauchySubsequenceUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\ArzelaAscoliFiniteEquicontinuousUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,E,U,M,D,W,Q,L,S,H,C,P,N$. It has no coordinate for open-cover compactness, selected infinite subsequence choice, quotient stream equality, countable choice, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Arzela--Ascoli finite equicontinuous obligations]
\label{thm:arzela-ascoli-finite-equicontinuous-obligations}
For every accepted $\ArzelaAscoliFiniteEquicontinuousUp$ carrier
$$
A=(K,E,U,M,D,W,Q,L,S,H,C,P,N),
$$
the local $\NameCert_{\ArzelaAscoliFiniteEquicontinuousUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the compact finite-net row, equicontinuity modulus row, compact-modulus compatibility row, finite diagonal window, dyadic tolerance ledger, stream windows, rational readback, located convergence window, real seal, transport, replay, provenance, and local naming rows;
\item compact-net exposure, requiring every source point used by the extraction to be read through the displayed $\HeineBorelFiniteNetUp$ finite-net data before any target observation is consumed;
\item equicontinuity visibility, requiring the shared source-radius row $E$ and compatibility row $U$ to be displayed before the diagonal window $M$ can be read;
\item L10 readback order, requiring the dyadic tolerance ledger, stream windows, regular rational readback, located Cauchy window, and terminal Real seal to appear in the order $D,W,Q,L,S$;
\item non-escape, requiring $H,C,P,N$ to transport, replay, source, and name only these finite rows and to export no open-cover compactness, selected infinite subsequence choice, quotient stream equality, countable choice, host real equality, or ambient $\RealUp$ completeness.
\end{enumerate}
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:arzela-ascoli-finite-equicontinuous-carrier}. The packet exposes the compact finite-net source, equicontinuity modulus, compact-modulus compatibility row, finite diagonal window, tolerance ledger, stream windows, rational readback, located convergence row, real seal, and structural rows as one finite $\BHist$ object.

Compact-net exposure is the row $K$. A consumer must first read the finite net and coverage witnesses supplied by the Heine--Borel finite-net route, so the source compactness input is a displayed finite object rather than an arbitrary open cover.

Equicontinuity visibility is supplied by $E$ and $U$. The requested target tolerance is converted into a displayed source radius, and the compact uniform modulus row records that the same tolerance budget is used by the finite-net and diagonal-window reads.

The L10 readback order is then forced by the packet coordinates. The diagonal window $M$ can be inspected only through $D,W,Q,L$, and the terminal row $S$ is downstream of those dyadic, stream, rational, and located Cauchy rows.

Finally, $H,C,P,N$ preserve that row inventory. Since the carrier has no coordinate for open-cover compactness, selected infinite subsequence choice, quotient stream equality, countable choice, host real equality, or ambient Real completeness, none of those objects can be exported by the local naming surface.
\end{proof}

\falsifiablePrediction{Every $\ArzelaAscoliFiniteEquicontinuousUp$ consumer read that reaches $\RealUp$ first displays compact finite-net rows, equicontinuity finite modulus rows, compact-modulus compatibility rows, a finite diagonal window, DyadicRatCore tolerances, StreamName windows, RegSeqRat readback, and located Cauchy window rows.}

\independenceWitness{heineborelfinet, uniformmoduluscomposition, compactuniformmodulusextractor, realconvergentsequence, locatedcauchysubsequence, dyadicratcore, streamname, regseqrat, real: none is bijective to $\ArzelaAscoliFiniteEquicontinuousUp$ because this packet jointly carries compact finite-net exposure, equicontinuity-radius visibility, compact-modulus compatibility, diagonal-window data, dyadic tolerances, stream windows, rational readback, located convergence rows, real sealing, transport, replay, provenance, and local naming.}

\closureat{ArzelaAscoliFiniteEquicontinuousUp}{seedStr}

\begin{closurestatus}{\ArzelaAscoliFiniteEquicontinuousUp}
  \constructivestory{An $\ArzelaAscoliFiniteEquicontinuousUp$ packet is generated from compact finite-net rows, equicontinuity finite modulus rows, compact-modulus compatibility, a finite diagonal or subsequence window, DyadicRatCore tolerances, StreamName windows, RegSeqRat readback, LocatedCauchySubsequence rows, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:arzela-ascoli-finite-equicontinuous-carrier} and \autoref{thm:arzela-ascoli-finite-equicontinuous-obligations} seed the finite equicontinuous extraction packet consumed by L10 compact-continuous and Real-completion routes.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim open-cover compactness, selected infinite subsequence choice, quotient stream equality, countable choice, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-net exposure, equicontinuity visibility, L10 readback order, non-escape, transport, replay, provenance, and local naming for BEDC.Derived.ArzelaAscoliFiniteEquicontinuousUp.}
\end{closurestatus}
