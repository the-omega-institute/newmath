\chapter{A Concrete Naming Certificate for $\CompactUniformCauchyTailRouterUp$}
\label{ch:concrete-instances-compact-uniform-cauchy-tail-router-namecert}
\origin{human}

Following Bishop and Bridges on compact uniform continuity and Cauchy convergence, the $\CompactUniformCauchyTailRouterUp$ packet records the finite route that turns compact finite-net modulus data into a Cauchy-tail handoff for regular real-name consumers. It realizes the compact-uniform-continuity route of \autoref{ch:visions-compact-uniform-continuity} through the compact metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the compact uniform modulus packet of \autoref{ch:concrete-instances-compact-uniform-modulus-namecert}, the finite stream interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The router is a displayed finite tail packet; it is not a host uniform-continuity theorem, selected global modulus, quotient stream equality, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Compact uniform Cauchy-tail router carrier]
\label{def:compact-uniform-cauchy-tail-router-carrier}
A $\CompactUniformCauchyTailRouterUp$ carrier is a finite $\BHist$ packet
$$
T=(K,M,S,D,R,L,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ finite-net source row.
\item $M$ is the $\CompactUniformModulusUp$ row that converts compact finite-net and pointwise-modulus data into a tolerance-only modulus ledger.
\item $S$ is the $\StreamNameUp$ finite observation row for the source and target Cauchy windows opened by the router.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger for the requested tail precision and compact-net radius budget.
\item $R$ is the $\RegSeqRatUp$ readback row obtained from the displayed stream and dyadic rows.
\item $L$ is the Cauchy-tail ledger, recording the finite threshold and the pairwise tail comparisons consumed by downstream regular real-name readers.
\item $E$ is the terminal $\RealUp$ seal row reached only after $K,M,S,D,R,L$ have been exposed.
\item $H$ records componentwise $\hsame$ transport for the compact source, modulus, stream windows, dyadic ledger, regular readback, tail ledger, real seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a compact-uniform Cauchy-tail request through $K,M,S,D,R,L,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CompactUniformCauchyTailRouterUp$, $\CompactMetricUp$, $\CompactUniformModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformCauchyTailRouterUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,M,S,D,R,L,E,H,C,P,N$. It has no coordinate for a host uniform-continuity theorem, selected global modulus, quotient stream equality, selected limits, countable-choice schedules, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Compact uniform Cauchy-tail router obligation surface]
\label{thm:compact-uniform-cauchy-tail-router-obligation-surface}
For every accepted $\CompactUniformCauchyTailRouterUp$ carrier $T=(K,M,S,D,R,L,E,H,C,P,N)$, the local $\NameCert_{\CompactUniformCauchyTailRouterUp}$ surface consists exactly of carrier admission for the compact metric source, compact uniform modulus row, stream windows, dyadic tolerance ledger, regular rational readback, Cauchy-tail ledger, terminal Real seal, componentwise transport, displayed replay, provenance, and local naming.

Consequently every $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, or $\RealUp$ consumer of the router must read
$$
\CompactMetricUp \longmapsto \CompactUniformModulusUp \longmapsto \StreamNameUp \longmapsto \DyadicRatCoreUp \longmapsto \RegSeqRatUp \longmapsto \RealUp
$$
through $K,M,S,D,R,L,E,H,C,P,N$ and cannot replace the route by a selected global modulus, quotient stream equality, selected real limit, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-cauchy-tail-router-carrier}. The compact source and uniform-modulus rows are exactly $K$ and $M$, so no host compactness theorem or external modulus is available to the consumer.

The tail route is then finite. The row $S$ names the inspected stream windows, $D$ records the dyadic tolerance budget for the same request, $R$ provides the regular rational readback, and $L$ records the threshold and pairwise tail comparisons read by downstream consumers.

The terminal seal $E$ is downstream of those rows. Componentwise transport, displayed replay, provenance, and local naming preserve the same finite packet, and the carrier has no additional coordinate for quotient stream equality, selected limits, countable choice, or ambient Real completeness. Thus the listed rows exhaust the obligation surface.
\end{proof}

\closureat{CompactUniformCauchyTailRouterUp}{seedStr}

\begin{closurestatus}{\CompactUniformCauchyTailRouterUp}
  \constructivestory{A $\CompactUniformCauchyTailRouterUp$ packet is generated from compact metric finite-net rows, CompactUniformModulus rows, StreamName windows, DyadicRatCore tolerance budgets, RegSeqRat readback, a Cauchy-tail ledger, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-cauchy-tail-router-carrier} and \autoref{thm:compact-uniform-cauchy-tail-router-obligation-surface} seed the compact-uniform Cauchy-tail route through compact metric, uniform-modulus, StreamName, dyadic tolerance, RegSeqRat, and Real seal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host uniform-continuity semantics, selected global moduli, quotient stream equality, selected real limits, countable choice, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, compact finite-net exposure, uniform-modulus handoff, stream-window exactness, dyadic tail-budget exposure, RegSeqRat readback, Cauchy-tail threshold coverage, Real seal non-escape, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformCauchyTailRouterUp.}
\end{closurestatus}
