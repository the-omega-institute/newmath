\begin{theorem}[Finite Lebesgue-number root radius choice-free tail exhaustion]
\label{thm:finite-lebesgue-number-root-radius-choice-free-tail-exhaustion}
Every $\CompactMetricUp$, $\ContinuousMapUp$, $\TotallyBoundedUp$, or $\UniformModulusUp$ radius read admitted by an accepted $\FiniteLebesgueNumberUp$ root route factors through the choice-free tail-radius ledger $\Theta_{\lambda}^{\mathrm{tail}}$ and the Real-phase source-exhaustion ledger $\Xi_{\lambda}^{\mathrm{real}}$. The visible root radius is therefore read only from the finite rows over $\DyadicCoverUp$, $\RationalBallUp$, $\DyadicMeshUp$, selected finite-tail admission, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, and not from open-cover compactness, selected subcovers, quotient stream equality, countable choice, detached $\RealUp$ completeness, or external modulus data.
\end{theorem}
\begin{proof}
Start with the tail ledger. \autoref{thm:finite-lebesgue-number-choice-free-tail-radius-admission} places every compact, continuous, totally bounded, and uniform tail-radius read inside $\Theta_{\lambda}^{\mathrm{tail}}$, and \autoref{thm:finite-lebesgue-number-tail-radius-transport-determinacy} makes that read stable under the recorded componentwise $\hsame$ transports.

Now attach the Real-phase route. \autoref{thm:finite-lebesgue-number-real-phase-source-exhaustion} forces the same radius read through $\Xi_{\lambda}^{\mathrm{real}}$ before any endpoint can inspect it, while \autoref{thm:finite-lebesgue-number-real-phase-offtable-refusal} removes host equality, quotient stream equality, selected limits, detached completeness, choice, external modulus data, and arbitrary open-cover compactness from the accepted rows.

The two ledgers share the displayed dyadic cover, rational-ball radius, dyadic mesh, stream-name, regular-rational, Real endpoint, transport, replay, provenance, and naming rows. Hence the root route exposes the radius only through the stated finite tail-and-Real packet, so none of the refused compactness, selection, quotient, choice, completeness, or modulus coordinates remains available to an admitted consumer.
\end{proof}
\leanchecked{BEDC.Derived.FiniteLebesgueNumberUp.FiniteLebesgueNumberRootRadiusChoiceFreeTailExhaustion}

\begin{theorem}[Finite Lebesgue-number root radius source determinacy]
\label{thm:finite-lebesgue-number-root-radius-source-determinacy}
Let two accepted $\FiniteLebesgueNumberUp$ root routes carry tail-radius ledgers and Real-phase source ledgers for visible radii $\lambda$ and $\mu$. If their $\DyadicCoverUp$, $\RationalBallUp$, $\DyadicMeshUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\Cont$, $\Pkg$, and $\NameCert$ rows agree componentwise under the recorded $\hsame$ transports, and if their selected finite-tail admission rows name the same local source, then every $\CompactMetricUp$, $\ContinuousMapUp$, $\TotallyBoundedUp$, and $\UniformModulusUp$ consumer receives the same downstream root-radius source from the two routes.
\end{theorem}
\begin{proof}
First compare the tail-radius rows. The componentwise hypotheses identify the dyadic cover, rational-ball, dyadic mesh, stream-name, regular-rational, Real endpoint, history, replay, provenance, and local naming rows, while the shared selected finite-tail admission fixes the only finite tail through which a consumer can read the radius.

Apply \autoref{thm:finite-lebesgue-number-tail-radius-transport-determinacy}. The two tail ledgers therefore expose the same consumer radius along the L10 route, with no second selector, quotient stream, choice coordinate, host equality, or detached Real representative.

It remains to identify the source side. \autoref{thm:finite-lebesgue-number-real-phase-source-exhaustion} confines the source route to the same Real-phase ledger, and \autoref{thm:finite-lebesgue-number-real-phase-offtable-refusal} prevents any outside source from being substituted after the rows agree. Thus the downstream compact, continuous, totally bounded, and uniform consumers receive the same root-radius source.
\end{proof}
\leanchecked{BEDC.Derived.FiniteLebesgueNumberUp.FiniteLebesgueNumberRootRadiusSourceDeterminacy}

\begin{theorem}[Finite Lebesgue-number root radius NameCert obligation package]
\label{thm:finite-lebesgue-number-root-radius-namecert-obligation-package}
An accepted $\FiniteLebesgueNumberUp$ root radius satisfies the NameCert obligation package for the Real-and-Completion route when its finite rows provide:
\begin{enumerate}
\item the choice-free tail-radius admission and transport determinacy of \autoref{thm:finite-lebesgue-number-choice-free-tail-radius-admission} and \autoref{thm:finite-lebesgue-number-tail-radius-transport-determinacy};
\item the Real-phase source exhaustion and offtable refusal of \autoref{thm:finite-lebesgue-number-real-phase-source-exhaustion} and \autoref{thm:finite-lebesgue-number-real-phase-offtable-refusal};
\item the root tail exhaustion and source determinacy of \autoref{thm:finite-lebesgue-number-root-radius-choice-free-tail-exhaustion} and \autoref{thm:finite-lebesgue-number-root-radius-source-determinacy}.
\end{enumerate}
Under these clauses, every accepted $\CompactMetricUp$, $\ContinuousMapUp$, $\TotallyBoundedUp$, and $\UniformModulusUp$ read is accounted for by the finite $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ packet over the displayed $\DyadicCoverUp$, $\RationalBallUp$, $\DyadicMeshUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteLebesgueNumberUp.FiniteLebesgueNumberRootRadiusNameCertObligationPackage}
\begin{proof}
Assemble the tail side first. The choice-free tail-radius admission theorem supplies the finite ledger through which each admitted consumer reads the tail radius, and tail-radius transport determinacy says that componentwise agreement on that ledger fixes the consumer-visible radius.

Assemble the Real-phase side next. The Real-phase source-exhaustion theorem supplies the finite source ledger for the same radius, and the offtable refusal theorem rules out host equality, quotient stream equality, selected limits, detached completeness, choice, external modulus data, and arbitrary open-cover compactness as substitute sources.

Finally apply the two root-radius theorems above. Root tail exhaustion identifies the common finite packet seen by consumers, and root source determinacy says that agreement on the displayed dyadic, stream, regular-rational, Real, history, replay, provenance, and naming rows determines the downstream source. These clauses are exactly the NameCert obligation package claimed for the accepted Real-and-Completion route.
\end{proof}
