\begin{definition}[Finite Lebesgue-number compact radius-window pullback packet]
\label{def:finite-lebesgue-number-compact-radius-window-pullback}
For an accepted $\FiniteLebesgueNumberUp$ carrier, the compact radius-window pullback packet is the finite $\BHist$ row
$$
\Omega_{\lambda}=(B,S,Q,R,K,F,U,H,C,P,N)
$$
whose entries are the $\DyadicRatCoreUp$ radius row $B$, the $\StreamNameUp$ finite-window row $S$, the $\RegSeqRatUp$ readback row $Q$, the $\RealUp$ seal face $R$, the $\CompactMetricUp$ and $\ContinuousMapUp$ consumer row $K,F$, the $\UniformModulusUp$ row $U$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert$ naming $N$. The packet is accepted only when $B,S,Q,R,K,F,U$ are read as one radius-window obligation through the same $H,C,P,N$ support, with no open-cover compactness, selected limit, quotient stream equality, selected subcover, host minimum, choice-like cover selection, or unbudgeted radius selector.
\end{definition}
\leandef{BEDC.Derived.FiniteLebesgueNumberUp.FiniteLebesgueNumberCompactRadiusWindowPullbackPacket}

\begin{theorem}[Finite Lebesgue-number compact radius-window pullback]
\label{thm:finite-lebesgue-number-compact-radius-window-pullback}
Every compact or continuous consumer admitted downstream of an accepted compact radius-window pullback packet $\Omega_{\lambda}$ receives the positive radius through exactly the displayed $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of \autoref{def:finite-lebesgue-number-compact-radius-window-pullback}. The pullback supplies a finite radius-window obligation for $\CompactMetricUp$ and $\ContinuousMapUp$ consumers; it does not supply open-cover compactness, selected limits, quotient stream equality, selected subcovers, host minima, choice-like cover selection, or unbudgeted radius selection.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteLebesgueNumberUp.FiniteLebesgueNumberCompactRadiusWindowPullback}
\begin{proof}
Begin with the open Phase radius classifier pullback. \autoref{thm:finite-lebesgue-number-open-phase-radius-classifier-pullback} fixes every accepted positive-radius read to the dyadic radius, finite-window, regular-rational readback, and Real source faces before any compact or continuous endpoint can inspect the radius.

Next use the L10 sibling route. \autoref{thm:finite-lebesgue-number-l10-sibling-route} forces compact, compact-net, continuous-map, and uniform-modulus reads through the finite-name chain and then through the dyadic mesh and cover surfaces. The row $B,S,Q,R$ of $\Omega_{\lambda}$ is therefore the same finite source route, not a second compactness witness.

Attach the compact-continuous unblock triad. \autoref{thm:finite-lebesgue-number-compact-continuous-triad} and \autoref{thm:finite-lebesgue-number-uniform-continuity-handoff} make the $\CompactMetricUp$, $\ContinuousMapUp$, and $\UniformModulusUp$ rows consume the shared finite dyadic-radius packet through the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

Finally apply root-radius coherence. \autoref{thm:finite-lebesgue-number-root-radius-coherence} prevents the consumer from distinguishing two routes whose displayed radius, stream, regular-rational, Real, transport, replay, provenance, and naming rows agree. Since none of the cited surfaces contains a coordinate for open-cover compactness, selected limits, quotient stream equality, selected subcovers, host minima, choice-like cover selection, or unbudgeted radius selection, the accepted packet is exactly the stated compact radius-window pullback.
\end{proof}

\begin{theorem}[Finite Lebesgue-number uniform-modulus consumer pullback]
\label{thm:finite-lebesgue-number-uniform-modulus-consumer-pullback}
Every $\UniformModulusUp$ row admitted by $\Omega_{\lambda}$ is pulled back through the same compact radius-window packet read by the $\CompactMetricUp$ and $\ContinuousMapUp$ rows. Thus the uniform consumer receives the radius only as the finite $\BHist$ packet over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ naming; it cannot replace that packet by an external modulus, an open-cover compactness argument, a selected limit, quotient stream equality, a selected subcover, a host minimum, choice-like cover selection, or unbudgeted radius selection.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteLebesgueNumberUp.FiniteLebesgueNumberUniformModulusConsumerPullback}
\begin{proof}
First read the compact-continuous triad. \autoref{thm:finite-lebesgue-number-compact-continuous-triad} identifies the compact, compact-net, continuous-map, and uniform-modulus rows as consumers of one finite dyadic-radius packet.

Now apply the compact radius-window pullback theorem above. It places the $\CompactMetricUp$ and $\ContinuousMapUp$ reads on $\Omega_{\lambda}$, whose source faces are the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows and whose support is exactly $H,C,P,N$.

Use the root radius-window consumer exactness and root-radius coherence package to rule out a detached uniform source. \autoref{thm:finite-lebesgue-number-root-radius-window-consumer-exactness} states that every compact, continuous, or uniform endpoint receives only the finite root rows, and \autoref{thm:finite-lebesgue-number-compact-uniform-radius-admission} reads those rows through the coherent root-radius ledger.

The uniform-modulus row therefore has no remaining coordinate for an external modulus, open-cover compactness, selected limits, quotient stream equality, selected subcovers, host minima, choice-like cover selection, or unbudgeted radius selection. Its only admissible source is the compact radius-window packet.
\end{proof}

\begin{theorem}[Finite Lebesgue-number budgeted Real-completion nonescape]
\label{thm:finite-lebesgue-number-budgeted-real-completion-nonescape}
The Real-completion route of an accepted $\FiniteLebesgueNumberUp$ carrier cannot turn a compact radius-window pullback packet into an unbudgeted radius selector. Any terminal $\RealUp$ or completion-facing radius read remains the finite packet $\Omega_{\lambda}$ transported by $\hsame$ and replayed by $\Cont$ through the displayed $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\Pkg$, and $\NameCert$ rows. In particular the Real-completion endpoint has no coordinate for open-cover compactness, selected limits, quotient stream equality, selected subcovers, host minima, choice-like cover selection, or unbudgeted radius selection.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteLebesgueNumberUp.FiniteLebesgueNumberBudgetedRealCompletionNonescape}
\begin{proof}
Start with the existing Real-completion radius nonescape statement. \autoref{thm:finite-lebesgue-number-real-completion-radius-nonescape} says that the terminal Phase Real or Completion radius read remains the coherent finite root-radius packet, transported by $\hsame$ and replayed by $\Cont$ through the displayed source faces.

Next use the open Phase radius classifier pullback. \autoref{thm:finite-lebesgue-number-open-phase-radius-classifier-pullback} identifies the radius classifier before the compact or continuous consumer sees it, so the Real-completion endpoint cannot add a new selected-limit or quotient-stream coordinate after the classifier has crossed the open Phase boundary.

Attach the compact radius-window pullback and uniform-modulus consumer pullback. The first theorem in this file places the compact and continuous consumers on $\Omega_{\lambda}$, and the uniform theorem above makes the $\UniformModulusUp$ row consume the same packet. Thus the Real-completion route and the compact-uniform route share the same budgeted radius-window obligation.

Finally apply root-radius coherence. \autoref{thm:finite-lebesgue-number-root-radius-coherence} makes the displayed radius, stream, regular-rational, Real, transport, replay, provenance, and naming rows determine the consumer-facing obligation. Since those rows contain no open-cover compactness, selected limits, quotient stream equality, selected subcovers, host minima, choice-like cover selection, or unbudgeted radius selection, the budgeted packet is nonescaping at the Real-completion endpoint.
\end{proof}
