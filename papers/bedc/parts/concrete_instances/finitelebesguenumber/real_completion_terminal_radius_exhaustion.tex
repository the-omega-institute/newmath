\begin{definition}[Finite Lebesgue-number Real-completion terminal-radius ledger]
\label{def:finite-lebesgue-number-real-completion-terminal-radius-ledger}
For an accepted $\FiniteLebesgueNumberUp$ route, the Real-completion terminal-radius ledger is the finite $\BHist$ packet
$$
\Theta_{\lambda}^{\mathrm{term}}=(D,W,\lambda,M,S,Q,R,K,U,H,C,P,N)
$$
whose entries are the $\DyadicCoverUp$ cover row $D$, the $\FiniteWindowEnvelopeUp$ row $W$, the positive $\RationalBallUp$ radius row $\lambda$, the $\DyadicMeshUp$ row $M$, the $\StreamNameUp$ source row $S$, the $\RegSeqRatUp$ readback row $Q$, the $\RealUp$ endpoint row $R$, the compact and compact-net consumer row $K$, the continuous-map and uniform-modulus consumer row $U$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert$ naming $N$.

The ledger is terminal for the L10 Real-and-Completion source route: a downstream radius consumer may inspect only the displayed $\DyadicCoverUp$, $\FiniteWindowEnvelopeUp$, $\RationalBallUp$, $\DyadicMeshUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\UniformModulusUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, with $\CompactNetModulusSelectorUp$ read through the compact row $K$. It has no coordinate for open-cover compactness, a host minimum, quotient equality, selected limits, choice, or an ambient Real-completeness primitive.
\end{definition}

\begin{theorem}[Finite Lebesgue-number Real-completion terminal-radius exhaustion]
\label{thm:finite-lebesgue-number-real-completion-terminal-radius-exhaustion}
Every $\CompactMetricUp$, $\ContinuousMapUp$, $\CompactNetModulusSelectorUp$, or $\UniformModulusUp$ consumer admitted downstream of an accepted Real-completion terminal-radius ledger of \autoref{def:finite-lebesgue-number-real-completion-terminal-radius-ledger} factors its positive-radius read through the displayed L10 packet
$$
D,W,\lambda,M,S,Q,R,K,U,H,C,P,N .
$$
Consequently the compact, compact-net, continuous-map, and uniform-modulus endpoints cannot read open-cover compactness, host minima, quotient equality, selected limits, choice, or Real completeness outside the $\DyadicCoverUp$, $\FiniteWindowEnvelopeUp$, $\RationalBallUp$, $\DyadicMeshUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ route.
\end{theorem}
\begin{proof}
Begin with the finite-window side. \autoref{thm:finite-lebesgue-number-window-coverage-exactness} routes every accepted window read through the finite window $W$, a $\StreamNameUp$ row, a $\RegSeqRatUp$ row, a $\RealUp$ row, a mesh cell of $M$, and a named cover cell of $D$. Thus the window coordinate used by a downstream radius consumer is already inside the displayed terminal ledger.

Next attach the L10 source route. \autoref{thm:finite-lebesgue-number-l10-source-lock} forces the positive-radius read through $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\DyadicMeshUp$, and $\DyadicCoverUp$, while \autoref{thm:finite-lebesgue-number-phase-real-source-lock} exposes the same route to compact and continuous consumers without adding a host scalar or open-cover compactness witness.

Now read the root consumer boundary. \autoref{thm:finite-lebesgue-number-root-radius-window-consumer-exactness} states that $\CompactMetricUp$, $\CompactNetModulusSelectorUp$, $\ContinuousMapUp$, and $\UniformModulusUp$ consumers receive exactly the root rows $D,W,\lambda,M,S,Q,R,H,C,P,N$ and no open-cover compactness, host minimum, quotient equality, selected limit, choice, or Real-completeness coordinate.

Finally use the L10 phase consumer readiness theorem. \autoref{thm:finite-lebesgue-number-l10-phase-consumer-readiness} places the compact, compact-net, continuous-map, and uniform-modulus endpoints on the same finite packet, so the consumer rows $K$ and $U$ do not introduce another radius source. Therefore every terminal positive-radius read factors through $\Theta_{\lambda}^{\mathrm{term}}$, and the excluded compactness, equality, selected-limit, choice, and completeness data remain unavailable.
\end{proof}
