\begin{theorem}[Finite Lebesgue-number open-phase radius-window exhaustion]
\label{thm:finite-lebesgue-number-open-phase-radius-window-exhaustion}
For every accepted $\FiniteLebesgueNumberUp$ route exposed to the current Real-and-Completion four-face source route, the open-phase radius-window ledger is exhausted by the displayed rows
$$
\DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
Equivalently, every positive-radius window visible from the root obligation surface is read through the dyadic-rational core, stream-name source, regular-rational readback, Real endpoint, history transport, continuation, package provenance, and local NameCert rows. It has no coordinate for open-cover compactness, host minima, quotient equality, selected limits, choice, detached Real completeness, or a second radius source.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteLebesgueNumberUp.FiniteLebesgueNumberOpenPhaseRadiusWindowLedgerExhaustion}
\begin{proof}
Begin with the root radius-window obligation surface. \autoref{thm:finite-lebesgue-number-root-radius-window-obligation-surface} states that the positive radius and finite-window obligations are already carried by the finite dyadic cover, rational-ball radius, dyadic mesh, stream-name, regular-rational, Real, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Next attach the Phase Real source lock. \autoref{thm:finite-lebesgue-number-phase-real-source-lock} forces each compact- or continuous-visible public radius read through $\DyadicCoverUp$, $\RationalBallUp$, $\DyadicMeshUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before any downstream endpoint can inspect it.

Finally use the Real-completion terminal-radius exhaustion. \autoref{thm:finite-lebesgue-number-real-completion-terminal-radius-exhaustion} says that the terminal compact, compact-net, continuous-map, and uniform-modulus radius reads still factor through the same L10 packet and introduce no outside compactness, equality, selected-limit, choice, or completeness coordinate. Thus the open-phase radius-window ledger is exactly the displayed four-face source route with the BEDC support rows.
\end{proof}

\begin{theorem}[Finite Lebesgue-number Real-source ledger coverage]
\label{thm:finite-lebesgue-number-real-source-ledger-coverage}
The finite Lebesgue-number Real-source ledger covers every source row used by the current radius-window packet:
$$
\DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp
$$
are the only source faces, and $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ are the only BEDC transport and naming rows that carry those faces to the accepted $\FiniteLebesgueNumberUp$ obligation surface.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteLebesgueNumberUp.FiniteLebesgueNumberRealSourceLedgerCoverage}
\begin{proof}
Read the root ledger first. \autoref{def:finite-lebesgue-number-root-radius-window-obligation-ledger} lists the dyadic cover, finite window, positive radius, dyadic mesh, stream-name, regular-rational, Real endpoint, history, transport, continuation, provenance, and local naming rows, with no independent compactness or completeness coordinate.

Use the Phase Real source lock to identify the source faces. \autoref{thm:finite-lebesgue-number-phase-real-source-lock} routes the consumer-facing radius through the dyadic, stream-name, regular-rational, and Real rows, and it leaves only $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ as the BEDC support carrying the public packet.

The preceding open-phase exhaustion theorem then removes any remaining escape route at the radius-window boundary. Therefore the Real-source ledger covers exactly the displayed four source faces and the named support rows.
\end{proof}

\begin{theorem}[Finite Lebesgue-number RegSeq-StreamName nonescape]
\label{thm:finite-lebesgue-number-regseq-streamname-nonescape}
In an accepted $\FiniteLebesgueNumberUp$ open-phase radius-window ledger, the $\RegSeqRatUp$ and $\StreamNameUp$ rows cannot be bypassed by a host sequence, quotient stream equality, selected limit, choice-selected representative, or ambient completeness witness. Every downstream read of the radius window that reaches $\CompactMetricUp$, $\CompactNetModulusSelectorUp$, $\ContinuousMapUp$, or $\UniformModulusUp$ must pass through the displayed $\StreamNameUp$ and $\RegSeqRatUp$ rows together with $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteLebesgueNumberUp.FiniteLebesgueNumberRegSeqStreamNameNonescape}
\begin{proof}
Start from Real-source ledger coverage. \autoref{thm:finite-lebesgue-number-real-source-ledger-coverage} identifies $\StreamNameUp$ and $\RegSeqRatUp$ as source faces of the finite radius-window packet, not as optional annotations.

Now apply the root radius-window consumer exactness theorem. \autoref{thm:finite-lebesgue-number-root-radius-window-consumer-exactness} says that compact, compact-net, continuous-map, and uniform-modulus consumers receive exactly the displayed finite rows and no host minimum, quotient equality, selected limit, choice, open-cover compactness, or Real-completeness coordinate.

Finally attach the Real-completion terminal-radius exhaustion. \autoref{thm:finite-lebesgue-number-real-completion-terminal-radius-exhaustion} keeps terminal radius reads on the same L10 packet. Hence bypassing $\StreamNameUp$ or $\RegSeqRatUp$ would require a coordinate absent from both the root consumer boundary and the terminal Real-completion ledger, so the stated nonescape follows.
\end{proof}
