\begin{definition}[Finite Lebesgue-number root-radius coherence ledger]
\label{def:finite-lebesgue-number-root-radius-coherence-ledger}
For an accepted $\FiniteLebesgueNumberUp$ carrier, the root-radius coherence ledger is the finite $\BHist$ row
$$
\Xi_{\lambda}=(D,B,\lambda,M,S,Q,R,H,C,P,N)
$$
whose entries are the displayed $\DyadicCoverUp$ cover row $D$, the $\RationalBallUp$ radius readback row $B$ for the positive radius $\lambda$, the $\DyadicMeshUp$ mesh row $M$, the $\StreamNameUp$ source row $S$, the $\RegSeqRatUp$ regular-rational approximation row $Q$, the $\RealUp$ endpoint row $R$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert$ naming $N$. The ledger is coherent exactly when $B,\lambda,M,S,Q,R$ are read as one named root-radius row through the same $H,C,P,N$ support, with no separate host Real equality, quotient-stream coordinate, selected subcover, countable-choice selector, or ambient completeness field.
\end{definition}
\leandef{BEDC.Derived.FiniteLebesgueNumberUp.FiniteLebesgueNumberRootRadiusCoherenceLedger}

\begin{theorem}[Finite Lebesgue-number root-radius coherence]
\label{thm:finite-lebesgue-number-root-radius-coherence}
Let two accepted $\FiniteLebesgueNumberUp$ routes carry root-radius coherence ledgers $\Xi_{\lambda}$ and $\Xi'_{\lambda}$. If their $\DyadicCoverUp$, $\RationalBallUp$, $\DyadicMeshUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows agree componentwise under the recorded $\hsame$ transports, and if their displayed $\Cont$ replays follow the same $\Pkg$ and $\NameCert$ support rows, then both routes expose the same root-radius obligation to every downstream consumer. The coherent obligation is the finite row $\Xi_{\lambda}$ of \autoref{def:finite-lebesgue-number-root-radius-coherence-ledger}, not a detached compactness or completion datum.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteLebesgueNumberUp.FiniteLebesgueNumberCarrier\_root\_radius\_coherence}
\begin{proof}
Begin with the root radius-window surface. \autoref{thm:finite-lebesgue-number-root-radius-window-obligation-surface} already restricts the positive radius and finite-window obligations to the finite dyadic cover, rational-ball, mesh, stream-name, regular-rational, Real, history, transport, continuation, provenance, and local naming rows.

Next apply the componentwise hypothesis. Agreement under $\hsame$ fixes the rows $D,B,\lambda,M,S,Q,R$, and the common $\Cont$ replay fixes how those rows are inspected. The remaining $P,N$ rows name the same package and local certificate, so there is no second route through which a consumer could distinguish $\Xi_{\lambda}$ from $\Xi'_{\lambda}$.

Finally use the L10 sibling route. \autoref{thm:finite-lebesgue-number-l10-sibling-route} forces compact, compact-net, continuous-map, and uniform-modulus reads through the finite-name chain before the positive radius is visible. Therefore the two routes expose the same finite root-radius obligation and no outside compactness, equality, choice, selected-subcover, quotient-stream, or completeness coordinate.
\end{proof}

\begin{theorem}[Finite Lebesgue-number compact-uniform radius admission]
\label{thm:finite-lebesgue-number-compact-uniform-radius-admission}
Every $\CompactMetricUp$, $\CompactNetModulusSelectorUp$, $\ContinuousMapUp$, and $\UniformModulusUp$ consumer admitted by an accepted $\FiniteLebesgueNumberUp$ route receives the positive radius only through the coherent root-radius ledger of \autoref{def:finite-lebesgue-number-root-radius-coherence-ledger}. In particular, admission to those consumers is the finite packet over $\DyadicCoverUp$, $\RationalBallUp$, $\DyadicMeshUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$; it is not an independent compactness theorem, host minimum, selected subcover, countable-choice modulus, quotient-stream equality, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteLebesgueNumberUp.FiniteLebesgueNumberCompactUniformRadiusAdmission}
\begin{proof}
Start with root-radius coherence. \autoref{thm:finite-lebesgue-number-root-radius-coherence} identifies the consumer-facing positive radius with the finite ledger $\Xi_{\lambda}$ whenever the component rows are transported and replayed through the same support.

Attach the compact-uniform source triangle. \autoref{thm:finite-lebesgue-number-compact-uniform-source-triangle} makes every compact, continuous-map, or uniform-modulus consumer factor through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before the route reaches the consumer endpoint. The same theorem ties those four source faces to the visible dyadic cover, rational-ball, and mesh packet.

Now use the root radius-window consumer exactness supplied by \autoref{thm:finite-lebesgue-number-root-radius-window-obligation-surface} together with the L10 route of \autoref{thm:finite-lebesgue-number-l10-sibling-route}. The only admitted consumer data are the finite rows already named in $\Xi_{\lambda}$, so no consumer obtains an additional compactness, minimum, selected-subcover, choice, quotient, or completeness field.
\end{proof}

\begin{theorem}[Finite Lebesgue-number Real-completion radius nonescape]
\label{thm:finite-lebesgue-number-real-completion-radius-nonescape}
The Real-and-completion route of an accepted $\FiniteLebesgueNumberUp$ carrier cannot replace the coherent root-radius ledger by host $\RealUp$ equality, quotient streams, selected subcovers, countable choice, or ambient completeness. Any terminal radius read for the Phase Real or Completion route remains the finite $\BHist$ packet $\Xi_{\lambda}$ of \autoref{def:finite-lebesgue-number-root-radius-coherence-ledger}, transported by $\hsame$ and replayed by $\Cont$ through the displayed $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ source faces.
\end{theorem}
\leanchecked{BEDC.Derived.FiniteLebesgueNumberUp.FiniteLebesgueNumberRealCompletionRadiusNonescape}
\begin{proof}
First read the source triangle. \autoref{thm:finite-lebesgue-number-compact-uniform-source-triangle} places the Phase Real source faces before any compact-uniform consumer can inspect the radius, and it ties those faces to the same dyadic cover, rational-ball, and mesh packet.

Next apply root-radius coherence and compact-uniform admission. \autoref{thm:finite-lebesgue-number-root-radius-coherence} makes the radius stable under the displayed $\hsame$ and $\Cont$ rows, while \autoref{thm:finite-lebesgue-number-compact-uniform-radius-admission} states that admitted compact, compact-net, continuous-map, and uniform-modulus consumers receive only that finite ledger.

It remains to check the terminal exclusions. The L10 sibling route of \autoref{thm:finite-lebesgue-number-l10-sibling-route} and the root radius-window obligation surface of \autoref{thm:finite-lebesgue-number-root-radius-window-obligation-surface} contain no fields for host Real equality, quotient streams, selected subcovers, countable choice, or ambient completeness. Since the terminal radius read is assembled only from those cited rows, each proposed escape has no ledger coordinate and the coherent root-radius packet remains the endpoint.
\end{proof}
