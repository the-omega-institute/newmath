\chapter{A Concrete Naming Certificate for $\AronszajnPanitchpakdiInjectiveMetricUp$}
\label{ch:concrete-instances-aronszajn-panitchpakdi-injective-metric-namecert}
\origin{human}

Following Aronszajn--Panitchpakdi on hyperconvex metric spaces and injective metric extension, the $\AronszajnPanitchpakdiInjectiveMetricUp$ packet records the finite NameCert surface where a displayed metric source may be read as an injective metric object. It sits between the metric source of \autoref{ch:concrete-instances-metric-namecert}, the hyperconvex ball-family route of \autoref{ch:concrete-instances-hyperconvex-metric-namecert}, the complete-metric boundary of \autoref{ch:concrete-instances-completemetric-namecert}, the terminal real-radius rows of \autoref{ch:concrete-instances-real-namecert}, and the envelope consumer of \autoref{ch:concrete-instances-injective-envelope-metric-namecert}. The packet names the Aronszajn--Panitchpakdi injective surface itself rather than another envelope synonym: finite extension requests, hyperconvex ball witnesses, completion visibility, and real-radius seals are displayed before any public consumer can treat the source as injective.

\begin{definition}[Aronszajn--Panitchpakdi injective metric carrier]
\label{def:aronszajn-panitchpakdi-injective-metric-carrier}
An $\AronszajnPanitchpakdiInjectiveMetricUp$ carrier is a finite $\BHist$ packet
$$
A=(M,B,E,K,R,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricUp$ source row, including the inspected point classifier and finite distance observations.
\item $B$ is the $\HyperconvexMetricUp$ ball-family row, exposing finite closed-ball requests, real radii, pairwise compatibility, and the finite intersection handoff.
\item $E$ is the finite injective-extension row, recording the admissible one-step extension boundary over the same metric source rather than a selected host hull point.
\item $K$ is the $\CompleteMetricUp$ seal row, keeping completion-facing reads downstream of the metric source, hyperconvex ball data, and finite extension boundary.
\item $R$ is the $\RealUp$ radius and distance row used by the metric observations, closed-ball tests, extension budgets, and completion boundary.
\item $H$ records componentwise $\hsame$ transport for $M,B,E,K,R,C,P,N$.
\item $C$ records displayed $\Cont$ replay from metric observations through ball-family compatibility, finite extension admissibility, complete-metric sealing, and real-radius readback.
\item $P$ is the $\Pkg$ provenance over $\AronszajnPanitchpakdiInjectiveMetricUp$, $\MetricUp$, $\HyperconvexMetricUp$, $\CompleteMetricUp$, $\RealUp$, $\InjectiveEnvelopeMetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\AronszajnPanitchpakdiInjectiveMetricUp}$ row.
\end{enumerate}
The carrier has no coordinate for arbitrary ball-family intersections, a chosen injective hull point, a quotient completion representative, countable choice, a host extension function, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Aronszajn--Panitchpakdi injective metric obligations]
\label{thm:aronszajn-panitchpakdi-injective-metric-obligations}
For every accepted $\AronszajnPanitchpakdiInjectiveMetricUp$ carrier $A=(M,B,E,K,R,H,C,P,N)$, the local $\NameCert_{\AronszajnPanitchpakdiInjectiveMetricUp}$ surface consists exactly of carrier admission for the displayed packet, metric-source exposure through $M$, hyperconvex finite ball-family exposure through $B$, finite injective-extension exposure through $E$, complete-metric sealing through $K$, real-radius and distance readback through $R$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:aronszajn-panitchpakdi-injective-metric-carrier}. The rows $M,B,E,K,R,H,C,P,N$ are the complete displayed inventory available to an injective-metric consumer.

Read the Aronszajn--Panitchpakdi side through the hyperconvex row. The row $B$ supplies only the finite ball-family surface of \autoref{ch:concrete-instances-hyperconvex-metric-namecert}: displayed closed balls, real radii, pairwise compatibility, and a finite intersection handoff over the same metric source.

Read the injective side through $E$. It records finite one-step extension admissibility before any envelope consumer from \autoref{ch:concrete-instances-injective-envelope-metric-namecert} may use the packet, and it does not select a host hull point or a global extension function.

Finally inspect $K,R,H,C,P,N$. The complete-metric and real-radius rows seal the same finite route, while transport, replay, provenance, and local naming preserve it without adding arbitrary-family intersections, quotient completions, countable choice, or ambient real completeness. Hence the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Aronszajn--Panitchpakdi envelope-facing route]
\label{thm:aronszajn-panitchpakdi-injective-metric-envelope-route}
Every envelope-facing or completion-neighbour consumer of an accepted $\AronszajnPanitchpakdiInjectiveMetricUp$ packet must read
$$
\MetricUp\longmapsto \HyperconvexMetricUp\longmapsto \CompleteMetricUp\longmapsto \RealUp
$$
through the displayed rows $M,B,K,R,H,C,P,N$ and must read the finite injective-extension boundary $E$ before it treats the packet as an $\InjectiveEnvelopeMetricUp$ source.
\end{theorem}
\begin{proof}
Start with the carrier and obligation theorem. The metric source $M$ is displayed before the hyperconvex row $B$, and $B$ is the only row that exposes compatible closed-ball data.

The finite extension boundary $E$ is read before an envelope-facing consumer can use the packet as injective. The completion seal $K$ and real-radius row $R$ remain downstream of the same metric and ball-family rows, so they cannot add a chosen hull point or an arbitrary intersection witness.

The structural rows $H,C,P,N$ transport, replay, source, and name that same route. Thus every envelope-facing read factors through the displayed Aronszajn--Panitchpakdi surface rather than through an ungrounded injective-envelope synonym.
\end{proof}

\falsifiablePrediction{Every accepted $\AronszajnPanitchpakdiInjectiveMetricUp$ consumer that treats a metric packet as injective without first reading the displayed MetricUp, HyperconvexMetricUp, CompleteMetricUp, RealUp, finite extension, transport, replay, provenance, and local naming rows refutes the carrier surface.}

\independenceWitness{metric, hyperconvex-metric, completemetric, real, injective-envelope-metric: no listed sibling supplies the whole packet $A=(M,B,E,K,R,H,C,P,N)$, because this chapter binds the Aronszajn--Panitchpakdi injective surface before the envelope consumer reads it.}

\closureat{AronszajnPanitchpakdiInjectiveMetricUp}{seedStr}

\begin{closurestatus}{\AronszajnPanitchpakdiInjectiveMetricUp}
  \constructivestory{An $\AronszajnPanitchpakdiInjectiveMetricUp$ packet is generated from a metric source row, a hyperconvex finite ball-family row, a finite injective-extension boundary, a complete-metric seal, real radius and distance rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:aronszajn-panitchpakdi-injective-metric-carrier}, \autoref{thm:aronszajn-panitchpakdi-injective-metric-obligations}, and \autoref{thm:aronszajn-panitchpakdi-injective-metric-envelope-route} seed the finite Aronszajn--Panitchpakdi injective metric surface over metric, hyperconvex, complete-metric, real-radius, finite-extension, and envelope-facing rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, arbitrary-family intersections, selected injective hull points, quotient completions, countable choice, host extension functions, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, hyperconvex ball-family exposure, finite extension exposure, complete-metric sealing, Real radius readback, transport, replay, provenance, and local naming for BEDC.Derived.AronszajnPanitchpakdiInjectiveMetricUp.}
\end{closurestatus}
