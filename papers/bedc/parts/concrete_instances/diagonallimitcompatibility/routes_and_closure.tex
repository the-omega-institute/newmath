\begin{theorem}[Diagonal-limit triangle seal route]
\label{thm:diagonal-limit-compatibility-triangle-seal-route}
For an accepted $\DiagonalLimitCompatibilityUp$ packet
$$
K=(R,T,S,D,W,Q,L,H,C,P,N),
$$
the seal route $S$ reaches the readback row $Q$ and the $\RealUp$ seal row $L$ only after the triangle row $T$ has been aligned with the dyadic ledger $D$ and the shared stream-window row $W$. The route is the one exposed by \autoref{def:diagonal-limit-compatibility-carrier}, \autoref{thm:diagonal-limit-compatibility-namecert-obligations}, \autoref{thm:diagonal-limit-compatibility-triangle-selector-alignment}, and \autoref{thm:diagonal-limit-compatibility-seal-route-compatibility}; it cannot bypass classifier transport, local provenance, or the non-escape boundary.
\end{theorem}
\begin{proof}
Project the carrier $K$. The displayed packet places $R,T,S,D,W,Q,L$ before the transport, continuation, provenance, and local naming rows $H,C,P,N$.

Apply triangle-selector alignment. It fixes the triangle row $T$ to the same dyadic tolerance ledger $D$ and stream-window family $W$ already named by the diagonal packet.

Now apply seal-route compatibility. The seal route can read $Q$ and $L$ only through $R,T,D,W$, so the route to the completion-facing rows has already consumed the triangle row and its two ledgers.

Classifier transport and non-escape close the boundary. Transport preserves the same route through $H$ and $C$, while non-escape excludes quotient streams, selected host limits, host real equality, choice data, and ambient completeness.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityTriangleSealTransportRoute}
\leanvariant{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_triangle\_seal\_route}

\begin{theorem}[Diagonal-limit window ledger exactness]
\label{thm:diagonal-limit-compatibility-window-ledger-exactness}
For an accepted $\DiagonalLimitCompatibilityUp$ packet
$$
K=(R,T,S,D,W,Q,L,H,C,P,N),
$$
the row $W$ is the exact shared-window ledger read by the diagonal source $R$, the triangle row $T$, the dyadic ledger $D$, the seal route $S$, the readback row $Q$, and the $\RealUp$ seal row $L$. A completion consumer reaches $Q,L$ only after it has read the same $W$ through the triangle row, dyadic ledger, componentwise $\hsame$ transport $H$, displayed $\Cont$ route $C$, package provenance $P$, and local $\NameCert$ row $N$.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_window\_ledger\_exactness}
\begin{proof}
Begin with the shared-window ledger theorem. It identifies $W$ as the finite window row used by the diagonal selector, triangle-bound estimate, dyadic tolerance read, regular readback, and downstream seal consumer.

Use the triangle-seal route theorem. Since the route to $Q,L$ must pass through $T,D,W$, no consumer can replace $W$ after the triangle row has been fixed.

Finally apply classifier transport and the NameCert obligations. The componentwise $\hsame$ and $\Cont$ rows preserve the displayed classifier, and the local $\NameCert$ row records that the same window ledger is the one carried by $K$.
\end{proof}

\begin{theorem}[Diagonal-limit completion boundary]
\label{thm:diagonal-limit-compatibility-completion-boundary}
Every completion-facing or $\RealUp$ consumer of an accepted $\DiagonalLimitCompatibilityUp$ packet
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
reaches $Q,L$ only after reading the triangle row $T$, the dyadic ledger $D$, the stream-window ledger $W$, the componentwise $\hsame$ transport $H$, the displayed $\Cont$ route $C$, package provenance $P$, and the local $\NameCert$ row $N$. The boundary is the one assembled from the carrier, local NameCert obligations, triangle selector alignment, seal route compatibility, classifier transport, non-escape, the shared-window ledger, \autoref{thm:diagonal-limit-compatibility-triangle-seal-route}, and \autoref{thm:diagonal-limit-compatibility-window-ledger-exactness}.

The consumer receives no quotient stream, selected host limit, host real equality, choice datum, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityCompletionBoundary}
\begin{proof}
First assemble the route. The carrier supplies the rows of $K$, the local NameCert obligations identify their order, and triangle selector alignment fixes $T$ to the displayed $D,W$ ledgers.

Next apply seal route compatibility together with the triangle-seal route theorem. These results force every read of $Q,L$ through the route determined by $R,T,D,W,S$.

Use window ledger exactness to keep the stream-window row fixed through the same route. The componentwise $\hsame$ transport, displayed $\Cont$ route, package provenance, and local NameCert row then carry that fixed route to the completion consumer without adding another ledger.

Finally use non-escape. Since no quotient stream, selected host limit, host real equality, choice datum, or ambient completeness coordinate is present in the packet, the displayed rows are the complete completion boundary.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility terminal formal target]
\label{thm:diagonal-limit-compatibility-terminal-formal-target}
An accepted $\DiagonalLimitCompatibilityUp$ packet reaches any $\CauchyLimitSealUp$ or $\RealUp$ terminal consumer only through the displayed diagonal route, triangle row, dyadic ledger, shared stream-window ledger, readback row, $\RealUp$ seal row, $\hsame$ transport, $\Cont$ route, $\Pkg$ provenance, and local $\NameCert$ row:
$$
K=(R,T,S,D,W,Q,L,H,C,P,N).
$$
This is the exact formalization target for the present packet. It exports no quotient stream, selected host limit, host real equality, choice datum, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_terminal\_formal\_target}
\begin{proof}
Start with the triangle seal route and window ledger exactness. \autoref{thm:diagonal-limit-compatibility-triangle-seal-route} places the seal route behind $R,T,D,W$, and \autoref{thm:diagonal-limit-compatibility-window-ledger-exactness} keeps the shared stream-window ledger fixed through the readback and seal rows.

Use the completion boundary, \autoref{thm:diagonal-limit-compatibility-completion-boundary}. It forces any terminal $\CauchyLimitSealUp$ or $\RealUp$ consumer to pass through the triangle row, dyadic ledger, stream-window ledger, transport, continuation, provenance, and local naming rows before $Q$ or $L$ is read.

Next apply tolerance ledger handoff and seal consumer factorization. \autoref{thm:diagonal-limit-compatibility-tolerance-ledger-handoff} fixes the dyadic ledger and window family, while \autoref{thm:diagonal-limit-compatibility-seal-consumer-factorization} routes the terminal seal read through the displayed packet rows.

The same rows define the formal target because every cited theorem consumes only $K=(R,T,S,D,W,Q,L,H,C,P,N)$. Non-escape supplies the final exclusion: quotient streams, selected host limits, host real equality, choice data, and ambient completeness have no coordinate in the packet and cannot be exported.
\end{proof}

\begin{theorem}[Diagonal-limit tolerance ledger handoff]
\label{thm:diagonal-limit-compatibility-tolerance-ledger-handoff}
For an accepted $\DiagonalLimitCompatibilityUp$ packet
$$
K=(R,T,S,D,W,Q,L,H,C,P,N),
$$
every tolerance-facing read that reaches the seal route first uses the same $\DyadicRatCoreUp$ ledger $D$ and $\StreamNameUp$ window family $W$ named by the diagonal packet $R$ and triangle row $T$. No downstream tolerance consumer can replace $D$ or $W$ with an alternate ledger or window family after entering the $\CauchyLimitSealUp$ route.
\end{theorem}
\begin{proof}
Project the carrier $K$. The rows $R,T,D,W$ are displayed before the seal route $S$ can be read.

Apply \autoref{thm:diagonal-limit-compatibility-triangle-selector-alignment}. It identifies the triangle row $T$ with the same selected window family $W$ and dyadic tolerance ledger $D$ already named by the diagonal packet $R$.

Finally use non-escape, \autoref{thm:diagonal-limit-compatibility-non-escape}. Since the packet contains no alternate tolerance ledger or second window family, every read that reaches $S$ keeps the displayed $D,W$ handoff.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityToleranceLedgerHandoff}

\begin{theorem}[Diagonal-limit seal consumer factorization]
\label{thm:diagonal-limit-compatibility-seal-consumer-factorization}
Every downstream $\CauchyLimitSealUp$ or $\RealUp$ read of an accepted $\DiagonalLimitCompatibilityUp$ packet factors through
$$
R,T,D,W,Q,L,H,C,P,N
$$
and cannot bypass the triangle, tolerance, or window rows. The seal consumer may read $Q$ and $L$ only after the packet has exposed the diagonal route $R$, the triangle row $T$, the dyadic ledger $D$, and the window family $W$.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:diagonal-limit-compatibility-seal-route-compatibility}. It places every seal-route read of $Q$ and $L$ behind the displayed rows $R,T,D,W$.

Use classifier transport, \autoref{thm:diagonal-limit-compatibility-classifier-transport}. The componentwise transports $H$ and continuation routes $C$ preserve the same compatibility classifier and do not add another seal-consumer path.

Apply non-escape, \autoref{thm:diagonal-limit-compatibility-non-escape}. The exported surface is exhausted by the listed rows and their provenance, so no $\CauchyLimitSealUp$ or $\RealUp$ consumer can bypass the triangle, tolerance, or window coordinates.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilitySealConsumerFactorization}

\begin{theorem}[Diagonal-limit window route lock]
\label{thm:diagonal-limit-compatibility-window-route-lock}
For an accepted $\DiagonalLimitCompatibilityUp$ packet
$$
K=(R,T,S,D,W,Q,L,H,C,P,N),
$$
the $\StreamNameUp$ window row $W$ is locked before any triangle estimate,
tolerance read, seal-route read, or $\RealUp$ read can be consumed. Every such
consumer uses the same displayed window family $W$ already named by the
carrier and cannot choose a second window route after the packet has been
accepted.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:diagonal-limit-compatibility-carrier}.
The row $W$ is part of the accepted packet, displayed before the downstream
triangle, tolerance, seal, and real-facing reads are interpreted.

Apply the shared-window ledger theorem,
\autoref{thm:diagonal-limit-compatibility-shared-window-ledger}. It ties the
diagonal source route to the same stream-window row $W$ used by the packet.

Now apply the tolerance ledger handoff theorem,
\autoref{thm:diagonal-limit-compatibility-tolerance-ledger-handoff}. Since
the tolerance route reaches the seal through the fixed $D,W$ pair, no later
triangle, seal, or $\RealUp$ consumer has a fresh window coordinate from
which to build an alternate route.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_window\_route\_lock}

\begin{theorem}[Diagonal-limit terminal selector non-escape]
\label{thm:diagonal-limit-compatibility-terminal-selector-nonescape}
For an accepted $\DiagonalLimitCompatibilityUp$ packet
$$
K=(R,T,S,D,W,Q,L,H,C,P,N),
$$
every terminal $\CauchyLimitSealUp$ or $\RealUp$ selector read of $K$ passes through the same diagonal route $R$, triangle row $T$, dyadic ledger $D$, stream-window row $W$, readback row $Q$, seal row $L$, componentwise $\hsame$ transport $H$, displayed $\Cont$ route $C$, $\Pkg$ provenance $P$, and local $\NameCert$ row $N$. The terminal read cannot introduce a second selector, quotient stream, host limit, choice datum, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start with the window route lock, \autoref{thm:diagonal-limit-compatibility-window-route-lock}. It fixes the stream-window row $W$ before any terminal seal or $\RealUp$ read is consumed.

Next use tolerance ledger handoff, \autoref{thm:diagonal-limit-compatibility-tolerance-ledger-handoff}. The dyadic ledger $D$ and the window row $W$ remain the same rows named by the diagonal route and triangle row.

Apply seal consumer factorization, \autoref{thm:diagonal-limit-compatibility-seal-consumer-factorization}. It routes every $\CauchyLimitSealUp$ or $\RealUp$ read through $R,T,D,W,Q,L,H,C,P,N$ rather than through a detached terminal selector.

Finally use the completion boundary, \autoref{thm:diagonal-limit-compatibility-completion-boundary}. Since the packet exposes no quotient stream, host limit, choice datum, or ambient completeness coordinate, the terminal selector cannot escape the displayed diagonal route.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityTerminalSelectorNonescape}

\begin{theorem}[Diagonal-limit seal readback exactness]
\label{thm:diagonal-limit-compatibility-seal-readback-exactness}
For an accepted $\DiagonalLimitCompatibilityUp$ packet
$$
K=(R,T,S,D,W,Q,L,H,C,P,N),
$$
every $\CauchyLimitSealUp$ consumer reaches the readback row $Q$ and the
seal row $L$ only through the displayed route
$$
R,T,D,W,H,C,P,N.
$$
Thus $Q$ and $L$ are read as the packet's own seal readback and real seal
coordinates, not as independently selectable completion data.
\end{theorem}
\begin{proof}
Start with seal-route compatibility,
\autoref{thm:diagonal-limit-compatibility-seal-route-compatibility}. It
places the reads of $Q$ and $L$ behind the diagonal source row $R$, triangle
row $T$, dyadic ledger $D$, and window row $W$.

Use seal consumer factorization,
\autoref{thm:diagonal-limit-compatibility-seal-consumer-factorization}. The
factorization adds the transport, continuation, provenance, and local naming
rows $H,C,P,N$ to the same route and excludes bypasses around the displayed
triangle, tolerance, and window data.

Consequently every seal consumer reads $Q$ and $L$ through the listed rows,
so the readback and seal coordinates are exact coordinates of the accepted
packet.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilitySealReadbackExactness}

\begin{theorem}[Diagonal-limit Real seal non-escape]
\label{thm:diagonal-limit-compatibility-real-seal-nonescape}
For an accepted $\DiagonalLimitCompatibilityUp$ packet, the $\RealUp$ seal row
$L$ exports no quotient stream, selected host limit, host real equality,
choice datum, or ambient completeness principle. A $\RealUp$ consumer can read
$L$ only as the displayed seal coordinate reached through the packet route and
cannot extract host-level completion data from it.
\end{theorem}
\begin{proof}
Apply the non-escape theorem,
\autoref{thm:diagonal-limit-compatibility-non-escape}. The exported
$\DiagonalLimitCompatibilityUp$ surface contains only the named rows of the
accepted packet and not any quotient stream, selected host limit, host real
equality, choice datum, or ambient completeness principle.

Now use seal readback exactness,
\autoref{thm:diagonal-limit-compatibility-seal-readback-exactness}. It forces
any $\RealUp$ read of $L$ to pass through the displayed seal route and
readback rows, so $L$ cannot be used as an escape hatch into a host-level
completion object.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_real\_seal\_nonescape}

\begin{theorem}[Diagonal-limit compatibility source-route exactness]
\label{thm:diagonal-limit-compatibility-source-route-exactness}
For an accepted $\DiagonalLimitCompatibilityUp$ packet
$$
K=(R,T,S,D,W,Q,L,H,C,P,N),
$$
there is one displayed source route from the accepted
$\RealCompletenessUp$ diagonal packet $R$ through the
$\RegularCauchyTriangleBoundUp$ row $T$, the $\DyadicRatCoreUp$ ledger $D$,
the $\StreamNameUp$ windows $W$, the $\CauchyLimitSealUp$ route $S$, the
$\RegSeqRatUp$ readback $Q$, and the $\RealUp$ seal class $L$. This route is
the route exposed by the carrier and by the local $\NameCert$ obligations,
using the sibling certificate surfaces
\autoref{ch:concrete-instances-realcompleteness-namecert},
\autoref{ch:concrete-instances-regular-cauchy-triangle-bound-namecert}, and
\autoref{ch:concrete-instances-cauchylimitseal-namecert}. It has no second
source, no alternate triangle row, and no bypass around the displayed
tolerance, window, readback, or seal coordinates.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilitySourceRouteExactness}
\begin{proof}
Project the accepted carrier. Its displayed mathematical rows are exactly
$R,T,S,D,W,Q,L$, and its transport, continuation, provenance, and local naming
rows are exactly $H,C,P,N$.

Use the local NameCert obligation theorem to read the source route in order.
The triangle-selector alignment row fixes $T$ to the same $D,W$ data consumed
by the accepted diagonal packet $R$, matching the diagonal and triangle
certificate surfaces.

Next use seal-route compatibility. The route $S$ reaches $Q$ and $L$ only
after $R,T,D,W$ have been fixed, so the Cauchy-limit seal route and the
RealUp seal are attached to the same displayed source route.

Finally apply classifier transport and non-escape. The componentwise
$\hsame$ and $\Cont$ rows preserve this route, while the exported packet
contains no coordinate from which a second source or bypass route could be
formed.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility regular readback exactness]
\label{thm:diagonal-limit-compatibility-regular-readback-exactness}
For an accepted $\DiagonalLimitCompatibilityUp$ packet
$$
K=(R,T,S,D,W,Q,L,H,C,P,N),
$$
the $\RegSeqRatUp$ readback row $Q$ is determined by the same triangle row
$T$, dyadic ledger $D$, and stream-window row $W$ used by the diagonal source
route. After $D,W,T$ are fixed, the packet cannot choose another
$\RegSeqRatUp$ readback row or another dyadic witness for the seal handoff.
This readback is the one compatible with
\autoref{ch:concrete-instances-dyadicratcore-namecert},
\autoref{ch:concrete-instances-streamname-namecert}, and
\autoref{ch:concrete-instances-regseqrat-namecert}.
\end{theorem}
\begin{proof}
Start from the carrier projection and isolate $D,W,T,Q$. The dyadic ledger,
window family, triangle estimate, and readback row are all displayed before a
completion-facing consumer can read the seal class.

Apply triangle-selector alignment and the shared-window ledger theorem. These
facts identify the selected windows and tolerance ledger consumed by $T$ with
the same $W,D$ rows already exposed by the diagonal packet.

Now apply seal-route compatibility and the tolerance-ledger handoff theorem.
The route to $Q$ passes through the fixed $D,W,T$ rows, so the readback row
has no independent dyadic or window witness to select.

The non-escape theorem supplies the boundary. Since the packet exports no
alternate dyadic ledger, second window family, or separate readback
coordinate, $Q$ is the exact regular-rational readback for the displayed
compatibility route.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityRegularReadbackExactness}

\begin{theorem}[Diagonal-limit compatibility route ledger totality]
\label{thm:diagonal-limit-compatibility-route-ledger-totality}
Every completion-facing consumer accepted by a $\DiagonalLimitCompatibilityUp$
packet factors through one displayed route ledger
$$
(R,T,D,W,Q,L,H,C,P,N).
$$
Here $T$ is the triangle row, $D$ is the $\DyadicRatCoreUp$ ledger, $W$ is
the $\StreamNameUp$ window row, $Q$ is the $\RegSeqRatUp$ readback, and $L$
is the $\RealUp$ seal. The rows $H,C,P,N$ only transport, continue, package,
and locally name that ledger. They do not add quotient streams,
host-selected limits, host real equality, choice data, or ambient
completeness.
\end{theorem}
\begin{proof}
Begin with source-route exactness,
\autoref{thm:diagonal-limit-compatibility-source-route-exactness}. It places
the consumer route on the displayed diagonal source row $R$ and its triangle,
dyadic, window, readback, seal, transport, continuation, provenance, and
NameCert rows.

Use regular readback exactness,
\autoref{thm:diagonal-limit-compatibility-regular-readback-exactness}. This
binds $Q$ to the same $T,D,W$ rows, so the readback cannot be selected from a
separate dyadic ledger or a second stream window.

Next apply seal readback exactness,
\autoref{thm:diagonal-limit-compatibility-seal-readback-exactness}. The
$\RealUp$ seal row $L$ reads the same regular-rational data and does not
introduce an independent host real witness.

The route lock is supplied by
\autoref{thm:diagonal-limit-compatibility-window-route-lock}. It keeps the
window row $W$ attached to the triangle and dyadic route rather than to a
consumer-chosen stream family.

Finally use Real seal non-escape,
\autoref{thm:diagonal-limit-compatibility-real-seal-nonescape}. Since the
packet has no coordinates for quotient streams, host-selected limits, host
real equality, choice data, or ambient completeness, the displayed ledger is
total for accepted completion-facing consumers.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityRouteLedger\_semantic\_name\_certificate}

\begin{theorem}[Diagonal-limit compatibility uniform-window Real seal pullback]
\label{thm:diagonal-limit-compatibility-uniform-window-real-seal-pullback}
Let
$$
K=(R,T,S_D,D,W,Q,L,H,C,P,N)
$$
be an accepted $\DiagonalLimitCompatibilityUp$ packet and let
$$
U=(F,M,T_U,R_U,S,E,H_U,C_U,P_U,N_U)
$$
be an accepted $\UniformLimitUp$ packet. Suppose the two packets share the displayed $\StreamNameUp$ finite-window row of \autoref{ch:concrete-instances-streamname-namecert} and the displayed $\RegSeqRatUp$ readback row of \autoref{ch:concrete-instances-regseqrat-namecert}. Then every consumer common to the $\RealUp$ and completion-facing routes of \autoref{ch:concrete-instances-real-namecert} and \autoref{ch:concrete-instances-uniformlimit-namecert} factors through the finite pullback surface made of the diagonal rows
$$
R,T,D,W,Q,L,H,C,P,N
$$
and the uniform-limit rows
$$
F,M,T_U,R_U,S,E,H_U,C_U,P_U,N_U.
$$
The pullback exports only the common finite window, regular readback, seal rows, displayed $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ rows. It exports no quotient stream, pointwise-only limit, selected host limit, host real equality, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
First read the diagonal side. Source-route exactness fixes the diagonal rows $R,T,D,W$ before the displayed seal route can be consumed, and regular readback exactness identifies $Q$ as the readback from the same $T,D,W$ rows.

Next read the uniform-limit side. The seal factorization for $\UniformLimitUp$ routes $F,M,T_U$ through the regular handoff $R_U$, the seal row $S$, the endpoint row $E$, and the displayed support rows $H_U,C_U,P_U,N_U$.

The shared-window and shared-readback hypotheses identify the two consumer surfaces at the common $\StreamNameUp$ and $\RegSeqRatUp$ rows. Pulling back along those rows leaves a finite $\BHist$ surface whose coordinates are exactly the displayed diagonal and uniform-limit rows.

Finally apply the non-escape clauses on both chapters. The diagonal packet has no coordinate for a quotient stream, selected host limit, host real equality, or ambient completeness theorem, and the uniform-limit packet has no coordinate for a pointwise-only limit or selected host limit. Therefore any common Real or Completion consumer factors through the stated pullback surface and nowhere else.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityUniformWindowRealSealPullback}

\begin{theorem}[Diagonal-limit compatibility selector-seal pullback]
\label{thm:diagonal-limit-compatibility-selector-seal-pullback}
Let $K=(R,T,S,D,W,Q,L,H,C,P,N)$ be an accepted $\DiagonalLimitCompatibilityUp$ packet. Suppose its displayed route reads a $\DiagonalTailSelectorUp$ source-schedule packet and a $\CauchyLimitSealUp$ observation-budget packet before any terminal $\RealUp$ consumer reads the seal row $L$. Then the selected finite windows, observation budget, $\RegSeqRatUp$ readback, and $\RealUp$ seal row pull back to the same $\RegSeqRatUp/\RealUp$-facing carrier surface
$$
R,T,S,D,W,Q,L,H,C,P,N.
$$
Every terminal $\RealUp$ consumer factors first through the $\DiagonalTailSelectorUp$ source-schedule readback and the $\CauchyLimitSealUp$ observation-budget pullback, and only then through the completion-facing read of $Q$ and $L$.
\end{theorem}
\begin{proof}
Begin with the uniform-window Real seal pullback of \autoref{thm:diagonal-limit-compatibility-uniform-window-real-seal-pullback}. It already identifies the finite window, regular readback, Real seal, displayed transport, continuation, provenance, and local naming rows as one finite consumer surface.

Next read the selector side. The diagonal-tail selector handoff fixes the source schedule before the seal-facing route can reach $Q$ or $L$, so the selected windows cannot be replaced by a second host selector after the packet has been accepted.

Now read the seal side. The $\CauchyLimitSealUp$ observation-budget and Real handoff routes force the budget row and seal row to travel through the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows. Thus the selector readback and seal budget meet at the displayed $\RegSeqRatUp/\RealUp$ carrier surface.

Finally inspect a terminal $\RealUp$ consumer. Since the packet exports no quotient stream, selected host limit, host real equality, choice datum, or ambient completeness coordinate, the consumer must pass through the selector source-schedule readback and the seal-budget pullback before any completion-facing read of $Q$ and $L$ is available.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_selector\_seal\_pullback}

\begin{theorem}[Diagonal-limit compatibility completion consumer nonescape]
\label{thm:diagonal-limit-compatibility-completion-consumer-nonescape}
Every completion-facing or $\RealUp$ consumer of an accepted
$\DiagonalLimitCompatibilityUp$ packet factors through
$$
R,T,S,D,W,Q,L,H,C,P,N.
$$
The consumer receives the accepted diagonal source route, triangle row,
dyadic ledger, stream windows, seal route, regular readback, real seal class,
componentwise transports, displayed continuations, provenance, and local
naming row. It receives no quotient stream, selected host limit, host real
equality, choice datum, or ambient completeness theorem. The boundary is the
one witnessed by \autoref{ch:concrete-instances-real-namecert},
\autoref{thm:diagonal-limit-compatibility-source-route-exactness}, and
\autoref{thm:diagonal-limit-compatibility-regular-readback-exactness}.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.CompletionConsumerNonescape.DiagonalLimitCompatibilityCompletionConsumerNonescape}
\begin{proof}
Project the accepted packet and read the exported rows. The only
consumer-facing coordinates are the seal route $S$, the readback $Q$, the
real seal class $L$, and their displayed transports and continuations through
$H,C,P,N$.

Use source-route exactness to fix the route from $R$ through $T,D,W,S,Q$ to
$L$. Use regular readback exactness to fix $Q$ as the readback determined by
the same $D,W,T$ rows.

It remains to inspect the consumer boundary. The existing non-escape theorem
excludes quotient streams, selected host limits, host real equality, choice
data, and ambient completeness from the exported packet.

Therefore every completion-facing or $\RealUp$ consumer factors through the
displayed rows alone, and no host-level completion object can enter through
the compatibility certificate.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility finite source lock]
\label{thm:diagonal-limit-compatibility-finite-source-lock}
For an accepted $\DiagonalLimitCompatibilityUp$ packet
$$
K=(R,T,S,D,W,Q,L,H,C,P,N),
$$
every compatibility read starts from the displayed $\RealCompletenessUp$ source row $R$, the $\RegularCauchyTriangleBoundUp$ estimate row $T$, the $\CauchyLimitSealUp$ route row $S$, the $\DyadicRatCoreUp$ ledger row $D$, the $\StreamNameUp$ window row $W$, the $\RegSeqRatUp$ readback row $Q$, the $\RealUp$ seal row $L$, and the support rows $H,C,P,N$. No compatibility consumer may introduce a second source route or an ambient completion principle outside those rows.
\end{theorem}
\begin{proof}
Project the carrier and read the finite row list $R,T,S,D,W,Q,L,H,C,P,N$. These are the only coordinates available to a compatibility consumer.

Use source-route exactness, \autoref{thm:diagonal-limit-compatibility-source-route-exactness}. It identifies the unique displayed route from $R$ through $T,D,W,S,Q$ to $L$.

Use completion-consumer non-escape, \autoref{thm:diagonal-limit-compatibility-completion-consumer-nonescape}. It forces every completion-facing or $\RealUp$ consumer to factor through the same listed rows.

The general non-escape theorem, \autoref{thm:diagonal-limit-compatibility-non-escape}, excludes quotient streams, selected host limits, host real equality, choice data, and ambient completeness. Therefore the source lock is exactly the finite packet.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityFiniteSourceLock}

\begin{theorem}[Diagonal-limit compatibility Real seal source lock]
\label{thm:diagonal-limit-compatibility-real-seal-source-lock}
Every $\RealUp$ seal-facing read of an accepted $\DiagonalLimitCompatibilityUp$ packet factors through the $\CauchyLimitSealUp$ route row $S$ and the $\RegSeqRatUp$ readback row $Q$ before it reaches the seal row $L$. The read cannot bypass $S$ or $Q$ by appealing to host real equality, a selected host limit, a quotient stream, or an ambient completion theorem.
\end{theorem}
\begin{proof}
Start with seal-readback exactness, \autoref{thm:diagonal-limit-compatibility-seal-readback-exactness}. It binds the $\RealUp$ seal row $L$ to the displayed seal route and readback rows.

Apply source-route exactness, \autoref{thm:diagonal-limit-compatibility-source-route-exactness}. The route to $L$ passes through $S$ and $Q$ after the source, triangle, dyadic, and window rows have been fixed.

Use regular-readback exactness, \autoref{thm:diagonal-limit-compatibility-regular-readback-exactness}. The readback $Q$ is determined by the same $D,W,T$ rows and is not an independent host real witness.

Finally apply completion-consumer non-escape, \autoref{thm:diagonal-limit-compatibility-completion-consumer-nonescape}. Every seal-facing read remains inside $S,Q,L$ and the packet support rows, with no host completion bypass.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityRealSealSourceLock}

\begin{theorem}[Diagonal-limit compatibility triangle-seal scope]
\label{thm:diagonal-limit-compatibility-triangle-seal-scope}
In an accepted $\DiagonalLimitCompatibilityUp$ packet, the triangle estimate row $T$ and the seal route row $S$ share the same dyadic ledger row $D$ and stream-window row $W$. A consumer may not read the triangle estimate against one tolerance/window ledger and the seal route against another.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityTriangleSealScope}
\begin{proof}
Project the accepted packet and isolate $T,S,D,W$. The carrier has one dyadic ledger row and one stream-window row for both the triangle estimate and the route to the seal.

Use regular-readback exactness, \autoref{thm:diagonal-limit-compatibility-regular-readback-exactness}. It fixes $Q$ from the same $T,D,W$ rows used by the diagonal source route.

Use source-route exactness, \autoref{thm:diagonal-limit-compatibility-source-route-exactness}. The seal route $S$ appears on that same route, after $R,T,D,W$ and before $Q,L$.

Completion-consumer non-escape, \autoref{thm:diagonal-limit-compatibility-completion-consumer-nonescape}, then prevents a consumer from splitting the route. The triangle row and the seal row therefore share the displayed $D,W$ ledger rows.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility window fusion]
\label{thm:diagonal-limit-compatibility-window-fusion}
For an accepted $\DiagonalLimitCompatibilityUp$ packet
$$
K=(R,T,S,D,W,Q,L,H,C,P,N),
$$
the shared $\StreamNameUp$ window row $W$ simultaneously serves the $\RealCompletenessUp$ diagonal packet $R$, the $\RegularCauchyTriangleBoundUp$ row $T$, the $\CauchyLimitSealUp$ route $S$, the $\DyadicRatCoreUp$ ledger $D$, and the $\RegSeqRatUp$ readback row $Q$. A consumer cannot split these rows into separate window surfaces after the packet has been accepted.
\end{theorem}
\begin{proof}
Project the carrier and read the row inventory. The packet has exactly one window coordinate $W$ and places it beside the diagonal source, triangle row, seal route, dyadic ledger, readback row, and Real seal row.

Apply the NameCert obligations, triangle-selector alignment, and seal-route compatibility. These force the reads of $R,T,S,D,Q$ to pass through the displayed window coordinate rather than through independently chosen window data.

Now use window-ledger exactness and seal-readback exactness. The former binds the diagonal, triangle, dyadic, seal, readback, and seal rows to the same ledger $W$; the latter keeps the readback and seal route attached to that route.

Finally apply Real seal non-escape and triangle-seal scope. Since the accepted packet contains no second $\StreamNameUp$ coordinate and no host completion row, every consumer sees one fused window surface.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityWindowFusion}

\begin{theorem}[Diagonal-limit compatibility Real seal exactness]
\label{thm:diagonal-limit-compatibility-real-seal-exactness}
For an accepted $\DiagonalLimitCompatibilityUp$ packet
$$
K=(R,T,S,D,W,Q,L,H,C,P,N),
$$
the $\RealUp$ seal row $L$ is reachable exactly through the displayed route
$$
R,T,S,D,W,Q,H,C,P,N.
$$
It exports no quotient stream, selected host limit, host equality, choice datum, or ambient completeness principle.
\end{theorem}
\begin{proof}
Begin with the carrier and local NameCert obligations. They expose the route from the diagonal packet $R$ through the triangle row $T$, seal route $S$, dyadic ledger $D$, shared window row $W$, readback row $Q$, and support rows $H,C,P,N$ before the Real seal row can be consumed.

Use triangle-selector alignment, seal-route compatibility, and window fusion. These theorems keep the triangle, seal, tolerance, window, and readback rows on the same displayed route and prevent a consumer from selecting a separate window or tolerance surface.

Apply seal-readback exactness and Real seal non-escape. The read of $L$ passes through the packet's own seal readback route, while quotient streams, selected host limits, host equality, choice data, and ambient completeness are absent from the carrier.

Finally use triangle-seal scope. It binds the triangle estimate and seal route to the same $D,W$ ledgers, so the listed rows are the exact Real seal boundary.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_real\_seal\_exactness}

\begin{theorem}[Diagonal-limit compatibility tail-selector scope]\label{thm:diagonal-limit-compatibility-tail-selector-scope}
For an accepted $\DiagonalLimitCompatibilityUp$ packet $K=(R,T,S,D,W,Q,L,H,C,P,N)$, every tail-selector read is mediated by the displayed $\StreamNameUp$ finite-window row $W$ and the route $R,T,D,W,S,Q,L$. It cannot choose a host tail, a second window surface, or an ambient completion witness.
\end{theorem}
\begin{proof}
Source-route exactness fixes the route from $R$ through $T,D,W,S,Q$ to $L$, while window fusion and Real seal source lock keep the selector attached to that same $W$ row. Regular readback exactness identifies $Q$ with the displayed $T,D,W$ data, and completion-consumer non-escape excludes any host tail or ambient completion coordinate.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_tail\_selector\_scope}
\begin{theorem}[Diagonal-limit compatibility Real--regular source factorization]\label{thm:diagonal-limit-compatibility-real-regseq-source-factorization}
Every $\RealUp$ readback from an accepted $\DiagonalLimitCompatibilityUp$ packet factors through the displayed $\RegSeqRatUp$ row $Q$ and the $\DyadicRatCoreUp$ tolerance ledger $D$ already carried by the packet. The readback has no source outside the cited regular, dyadic, stream-window, and seal rows.
\end{theorem}
\begin{proof}
Apply regular readback exactness to bind $Q$ to $T,D,W$, then apply source-route exactness to place this readback on the same route from $R$ to $L$ through $S$. Real seal source lock forces every seal-facing read through $S,Q,L$, and completion-consumer non-escape removes quotient streams, selected host limits, host real equality, choice data, and ambient completeness from the factorization.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityRealRegseqSourceFactorization}
\begin{theorem}[Diagonal-limit compatibility completion seal budget]\label{thm:diagonal-limit-compatibility-completion-seal-budget}
Any $\CompletionUp$-facing consumer of an accepted $\DiagonalLimitCompatibilityUp$ packet is bounded by the displayed seal budget $R,T,S,D,W,Q,L,H,C,P,N$. It may consume the packet as a finite compatibility route, but it cannot claim ambient completeness or a host-level completion object.
\end{theorem}
\begin{proof}
The source route and Real seal source lock identify the whole budget ending at $L$, and regular readback exactness accounts for the $\RegSeqRatUp$ and $\DyadicRatCoreUp$ coordinates used by that route. Completion-consumer non-escape permits reads through the displayed packet and rules out any additional completeness, limit-selection, quotient, choice, or host-real coordinate.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityCompletionSealBudget}

\begin{theorem}[Diagonal-limit compatibility regular Cauchy sequence handoff]
\label{thm:diagonal-limit-compatibility-regular-cauchy-sequence-handoff}
Let
$$
S=(F,\mu,W,D,R,G,E,H,C,P,N)
$$
be an accepted $\RegularCauchySequenceUp$ packet and let
$$
K=(R_0,T,S_0,D_0,W_0,Q,L,H_0,C_0,P_0,N_0)
$$
be an accepted $\DiagonalLimitCompatibilityUp$ packet. Suppose the selected-window rows $W,W_0$, the dyadic ledgers $D,D_0$, the $\RegSeqRatUp$ readback rows $R,Q$, and the $\RealUp$ seal rows $E,L$ are related by the displayed $\hsame$ transports carried by $H$ and $H_0$, and suppose the continuation, provenance, and local naming rows $C,P,N$ and $C_0,P_0,N_0$ expose the same consumer-facing route. Then every $\CompletionUp$-facing read of $K$ that consumes the regular-family data of $S$ factors first through \autoref{thm:regular-cauchy-sequence-diagonal-window-coverage-handoff} and \autoref{thm:regular-cauchy-sequence-diagonal-tail-selector-factorization}, then through \autoref{thm:diagonal-tail-selector-limit-handoff}, \autoref{thm:diagonal-limit-compatibility-seal-consumer-factorization}, \autoref{thm:cauchy-limit-seal-realup-consumer-boundary}, and \autoref{thm:diagonal-limit-compatibility-completion-seal-budget}. The resulting route exports the finite family row, modulus, selected windows, dyadic ledgers, $\RegSeqRatUp$ readbacks, tail-selector handoff, seal route, $\RealUp$ seal rows, displayed transports, continuations, provenance, and local $\NameCert$ rows, but exports no quotient stream, selected host limit, host real equality, choice data, hidden tail oracle, or ambient completeness principle.
\end{theorem}
\begin{proof}
First read the accepted $\RegularCauchySequenceUp$ packet. The diagonal-window coverage handoff confines the family-facing data to $F,\mu,W,D,R,G,E,H,C,P,N$, so a downstream consumer sees the selected windows, dyadic ledger, regular readback, and Real seal handoff only through the displayed finite packet.

Next apply \autoref{thm:regular-cauchy-sequence-diagonal-tail-selector-factorization}. The matching $\hsame$ transports identify the regular packet's selected-window, dyadic, readback, and seal rows with the rows consumed by the diagonal-tail selector route.

Now pass through \autoref{thm:diagonal-tail-selector-limit-handoff}. This attaches the selector route to the same finite windows and readbacks before any diagonal-limit or seal-facing consumer can read the compatibility packet.

Finally use the diagonal-limit compatibility and seal boundaries. The seal consumer factorization, the $\CauchyLimitSealUp$ $\RealUp$ consumer boundary, and the completion seal budget force the remaining read through $R_0,T,S_0,D_0,W_0,Q,L,H_0,C_0,P_0,N_0$ and exclude quotient streams, selected host limits, host real equality, choice data, hidden tail oracles, and ambient completeness.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityRegularCauchySequenceHandoff}

\begin{theorem}[Diagonal-limit compatibility source-route budget lock]
\label{thm:diagonal-limit-compatibility-source-route-budget-lock}
For an accepted $\DiagonalLimitCompatibilityUp$ packet
$$
K=(R,T,S,D,W,Q,L,H,C,P,N),
$$
every completion-facing or $\RealUp$ consumer first reads the same $\DiagonalTailSelectorUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, and $\CauchyLimitSealUp$ source route before it reaches the $\RealUp$ seal row $L$. The locked route is the finite row sequence
$$
R,T,S,D,W,Q,H,C,P,N
$$
with sibling anchors \autoref{ch:concrete-instances-diagonal-tail-selector-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-cauchylimitseal-namecert}, and \autoref{ch:concrete-instances-real-namecert}.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_source\_route\_budget\_lock}
\begin{proof}
Project the carrier and isolate the source route $R,T,S,D,W,Q$ together with the support rows $H,C,P,N$. The packet contains one diagonal source, one tail-selector route, one stream-window row, one dyadic ledger, one seal route, and one regular readback before the Real seal.

Apply source-route exactness and regular-readback exactness. These force the readback $Q$ to be determined by the same $T,D,W$ rows and keep the route from $R$ to $L$ inside the displayed source sequence.

Now use the finite source lock and the Real seal source lock. The first excludes a second source route, while the second forces every Real-facing read through $S,Q,L$ after the tail-selector, stream-window, and dyadic rows have been read.

Finally apply completion-consumer non-escape. Since the packet exports no quotient stream, selected host limit, host real equality, choice datum, hidden tail oracle, or ambient completeness row, the displayed route is the complete budget lock.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility public budget route]
\label{thm:diagonal-limit-compatibility-public-budget-route}
Every public Real-completion consumer of an accepted $\DiagonalLimitCompatibilityUp$ packet
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
factors through the displayed route
$$
R,T,D,W,S,Q,L,H,C,P,N.
$$
The $\StreamNameUp$ window row $W$ and the $\DyadicRatCoreUp$ tolerance ledger $D$ are fixed before any $\CauchyLimitSealUp$ readback row $Q$ or $\RealUp$ seal row $L$ is consumed. The route ties the $\RealCompletenessUp$ source, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\CauchyLimitSealUp$ route, and $\RealUp$ seal to the same public budget surface.
\end{theorem}
\begin{proof}
Begin with source-route budget lock, \autoref{thm:diagonal-limit-compatibility-source-route-budget-lock}. It forces every completion-facing or $\RealUp$ consumer through the finite source route before the Real seal row is read.

Use tolerance ledger handoff, \autoref{thm:diagonal-limit-compatibility-tolerance-ledger-handoff}, and window ledger exactness, \autoref{thm:diagonal-limit-compatibility-window-ledger-exactness}. These keep the dyadic ledger $D$ and the stream-window row $W$ fixed for the diagonal source, triangle row, seal route, readback row, and Real seal.

Now apply seal consumer factorization, \autoref{thm:diagonal-limit-compatibility-seal-consumer-factorization}. The public consumer reaches $Q$ and $L$ only through the displayed $R,T,D,W$ rows together with the support rows $H,C,P,N$.

Finally apply Real seal non-escape, \autoref{thm:diagonal-limit-compatibility-real-seal-nonescape}. Since quotient streams, selected host limits, host equality, choice data, and ambient completeness are absent from the packet, the displayed public budget route is exhaustive.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_public\_budget\_route}

\begin{theorem}[Diagonal-limit compatibility tail-selector consumer boundary]
\label{thm:diagonal-limit-compatibility-tail-selector-consumer-boundary}
Let $K=(R,T,S,D,W,Q,L,H,C,P,N)$ be an accepted $\DiagonalLimitCompatibilityUp$ packet. Any consumer that reads the tail selector used by a completion or $\RealUp$ route must first pass through the displayed $\DiagonalTailSelectorUp$ source, the $\StreamNameUp$ windows $W$, the $\DyadicRatCoreUp$ ledger $D$, and the $\CauchyLimitSealUp$ route $S$ before it may read $L$. It cannot choose a second tail selector, a host tail, or an ambient completion witness.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityTailSelectorConsumerBoundary}
\begin{proof}
Begin with source-route exactness. It places the tail-selector read on the route from the diagonal source $R$ through $T,D,W,S,Q$ to the Real seal row $L$.

Use regular-readback exactness to bind $Q$ to the same $T,D,W$ rows. The selected tail is therefore read through the StreamName and DyadicRatCore coordinates already present in the packet.

Apply the finite source lock and Real seal source lock. These results prevent the consumer from replacing the route by a second source path or by a direct Real seal read that bypasses $S$ and $Q$.

Completion-consumer non-escape supplies the boundary. No host tail, quotient stream, selected host limit, choice datum, or ambient completion coordinate is carried by $K$, so the tail-selector consumer remains inside the displayed route.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility Real seal route exhaustion]
\label{thm:diagonal-limit-compatibility-real-seal-route-exhaustion}
For an accepted $\DiagonalLimitCompatibilityUp$ packet
$$
K=(R,T,S,D,W,Q,L,H,C,P,N),
$$
the Real seal route is exhausted by the source rows $R,T,S,D,W,Q$ and the support rows $H,C,P,N$ before $L$ is read. Every completion-facing Real seal consumer therefore factors through the same DiagonalTailSelector, StreamName, DyadicRatCore, and CauchyLimitSeal route named by \autoref{ch:concrete-instances-diagonal-tail-selector-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-cauchylimitseal-namecert}, and \autoref{ch:concrete-instances-real-namecert}.
\end{theorem}
\begin{proof}
Read the accepted carrier. The Real seal row $L$ is displayed only after the diagonal source, triangle row, seal route, dyadic ledger, stream window, and regular readback rows.

Apply source-route exactness, regular-readback exactness, and the finite source lock. Together these results identify one source route and one readback route leading to $L$, with no alternate dyadic ledger, window family, or selector path.

Now apply the Real seal source lock. It forces every seal-facing read through $S$ and $Q$ before $L$, so a consumer cannot read the Real seal as an independent host completion object.

Finally use completion-consumer non-escape. The packet lacks quotient streams, selected host limits, host real equality, choice data, and ambient completeness; hence the displayed source route exhausts the Real seal boundary.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_real\_seal\_route\_exhaustion}

\begin{theorem}[Diagonal-limit compatibility public packet export]
\label{thm:diagonal-limit-compatibility-public-packet-export}
For an accepted $\DiagonalLimitCompatibilityUp$ packet
$$
K=(R,T,S,D,W,Q,L,H,C,P,N),
$$
the public export surface is exactly the finite carrier row $K$, the route ledger $R,T,S,Q,L$, the triangle-seal row $T,S,Q$, the window ledger $W$, the $\RealUp$ seal row $L$, and the route-exhaustion rows $H,C,P,N$ already named by the scoped closure. Every exported consumer row is read through the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ coordinates of $K$; there is no additional quotient stream, selected host limit, host real equality, choice datum, ambient completeness witness, hidden tail oracle, or independent route selector.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityPublicPacketExport}
\begin{proof}
Start with the carrier and NameCert rows. \autoref{def:diagonal-limit-compatibility-carrier} and \autoref{thm:diagonal-limit-compatibility-namecert-obligations} expose exactly the displayed source, triangle, seal-route, dyadic-ledger, window, readback, Real-seal, transport, continuation, provenance, and local naming rows.

Next assemble the ledger surface. \autoref{thm:diagonal-limit-compatibility-triangle-seal-route}, \autoref{thm:diagonal-limit-compatibility-window-ledger-exactness}, and \autoref{thm:diagonal-limit-compatibility-tolerance-ledger-handoff} identify the triangle seal, stream-window ledger, and dyadic tolerance ledger inside the same packet.

The consumer endpoint is fixed by \autoref{thm:diagonal-limit-compatibility-seal-consumer-factorization}, \autoref{thm:diagonal-limit-compatibility-real-seal-exactness}, and \autoref{thm:diagonal-limit-compatibility-real-seal-route-exhaustion}. These force the $\CauchyLimitSealUp$ readback and $\RealUp$ seal to remain on the displayed route.

Finally use \autoref{thm:diagonal-limit-compatibility-non-escape} and \autoref{thm:diagonal-limit-compatibility-completion-consumer-nonescape}. Since the excluded analytic objects have no carrier coordinate in $K$, the listed rows are the entire public packet.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility route-ledger public exactness]
\label{thm:diagonal-limit-compatibility-route-ledger-public-exactness}
For an accepted $\DiagonalLimitCompatibilityUp$ packet $K=(R,T,S,D,W,Q,L,H,C,P,N)$, the public route ledger is exactly the route
$$
R,T,S,D,W,Q,L,H,C,P,N.
$$
The rows $T,D,W,Q$ are not optional side ledgers: the source route, regular readback, window fusion, and Real seal exactness theorems force them to be the same rows used by every public completion-facing or $\RealUp$ consumer.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityRouteLedgerPublicExactness}
\begin{proof}
Read source-route exactness first. \autoref{thm:diagonal-limit-compatibility-source-route-exactness} places the route from $R$ through the selector, seal, dyadic, window, readback, and Real rows, and \autoref{thm:diagonal-limit-compatibility-regular-readback-exactness} binds $Q$ to the same $T,D,W$ data.

Use the ledger theorems to remove side choices. \autoref{thm:diagonal-limit-compatibility-window-fusion} and \autoref{thm:diagonal-limit-compatibility-window-ledger-exactness} keep the window row $W$ fixed, while \autoref{thm:diagonal-limit-compatibility-real-seal-exactness} keeps $L$ attached to that same route.

The public route then follows from \autoref{thm:diagonal-limit-compatibility-public-budget-route} and \autoref{thm:diagonal-limit-compatibility-real-seal-route-exhaustion}. They identify the displayed ledger as the route read by public consumers and exhaust the remaining support rows $H,C,P,N$.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility downstream public boundary]
\label{thm:diagonal-limit-compatibility-downstream-public-boundary}
Downstream $\RealCompletenessUp$, $\CauchyLimitSealUp$, and regular-Cauchy consumers of an accepted $\DiagonalLimitCompatibilityUp$ packet may consume only the public route
$$
R,T,S,D,W,Q,L,H,C,P,N.
$$
They may transport that route through the displayed $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, but they cannot introduce quotient streams, selected limits, host equality, ambient completeness, hidden tail oracles, or independent route selectors.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityDownstreamPublicBoundary}
\begin{proof}
Begin with the public packet export, \autoref{thm:diagonal-limit-compatibility-public-packet-export}. It fixes the finite carrier, route ledger, triangle seal, window ledger, Real seal, and route-exhaustion rows as one exported packet.

Attach the downstream consumers with \autoref{thm:diagonal-limit-compatibility-completion-boundary}, \autoref{thm:diagonal-limit-compatibility-seal-consumer-factorization}, and \autoref{thm:diagonal-limit-compatibility-tail-selector-consumer-boundary}. These theorems route completion, seal, and regular-tail reads through the same displayed packet.

Finally apply \autoref{thm:diagonal-limit-compatibility-completion-consumer-nonescape} and \autoref{thm:diagonal-limit-compatibility-real-seal-nonescape}. The excluded objects are not part of the exported carrier or support rows, so no downstream consumer can supply them from this public boundary.
\end{proof}

\begin{theorem}[DiagonalLimitCompatibility standard bridge surface]
\label{thm:diagonal-limit-compatibility-standard-bridge-surface}
For an accepted $\DiagonalLimitCompatibilityUp$ packet $K=(R,T,S,D,W,Q,L,H,C,P,N)$, the public diagonal-limit compatibility packet bridges the displayed budget, selector, limit-seal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows to the standard reflection schema. The bridge surface is the same finite public route
$$
R,T,S,D,W,Q,L,H,C,P,N,
$$
with no quotient stream, selected host limit, host real equality, choice datum, hidden tail oracle, or ambient completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityStandardBridgeSurface}
\begin{proof}
Start from the public route of \autoref{thm:diagonal-limit-compatibility-downstream-public-boundary}. It already exposes the budget, selector, seal, dyadic, window, readback, Real seal, transport, continuation, package, and local name rows as the only rows available to completion-facing consumers.

Synchronize the seal source with \autoref{thm:diagonal-limit-compatibility-seal-source-synchronization} and \autoref{thm:diagonal-limit-compatibility-real-seal-source-lock}. These theorems keep the limit-seal route attached to the same diagonal source and force every Real-facing read through the displayed seal and readback rows.

Lock the source and budget rows with \autoref{thm:diagonal-limit-compatibility-source-route-budget-lock} and \autoref{thm:diagonal-limit-compatibility-finite-source-lock}. The selector-budget package rows supplied by \autoref{thm:diagonal-limit-compatibility-budget-selector-carrier} and \autoref{thm:diagonal-limit-compatibility-budget-selector-route} then give the standard reflection input without adding a second selector or analytic coordinate.

Finally repack the same finite tuple as the bridge surface. The $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows are already displayed by $K$, and the non-escape theorems exclude every quotient, selected-limit, host-equality, choice, and ambient-completeness row not present in that tuple.
\end{proof}

\begin{theorem}[DiagonalLimitCompatibility bridge support minimality]
\label{thm:diagonal-limit-compatibility-bridge-support-minimality}
For an accepted $\DiagonalLimitCompatibilityUp$ packet $K=(R,T,S,D,W,Q,L,H,C,P,N)$, every standard-reflection or completion-facing bridge read must retain the finite support rows
$$
R,T,S,D,W,Q,L,H,C,P,N.
$$
Deleting $D$ loses the dyadic tolerance ledger, deleting $W$ loses the $\StreamNameUp$ window ledger, deleting $Q$ loses the $\RegSeqRatUp$ readback row, deleting $L$ loses the $\RealUp$ seal, and deleting one of $H,C,P,N$ loses componentwise transport, displayed route, package provenance, or local naming. No quotient stream, selected host limit, host real equality, choice datum, hidden tail oracle, or ambient completeness row can replace any missing support row.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityBridgeSupportMinimality}
\begin{proof}
First lock the route that any bridge consumer may read. \autoref{thm:diagonal-limit-compatibility-source-route-budget-lock} forces completion-facing and $\RealUp$ consumers through the source, selector, seal, dyadic, window, readback, and Real rows before $L$ is available. \autoref{thm:diagonal-limit-compatibility-public-budget-route} identifies this same route as the public budget route, and \autoref{thm:diagonal-limit-compatibility-route-ledger-public-exactness} records it as exactly $R,T,S,D,W,Q,L,H,C,P,N$.

Next attach the bridge consumer to that route. \autoref{thm:diagonal-limit-compatibility-downstream-public-boundary} makes downstream completion and regular-Cauchy reads factor through the same displayed packet, while \autoref{thm:diagonal-limit-compatibility-standard-bridge-surface} repacks that packet as the standard reflection surface. Thus a standard-reflection or completion-facing bridge read has no support beyond the listed rows.

It remains to check that no listed row is eliminable. Removing $D$ breaks the tolerance handoff used before readback; removing $W$ breaks the stream-window ledger used by the seal and readback rows; removing $Q$ removes the regular rational readback; removing $L$ removes the Real seal read by the bridge; and removing one of $H,C,P,N$ removes the transport, route, provenance, or local naming row that makes the packet public.

Finally apply \autoref{thm:diagonal-limit-compatibility-non-escape}, \autoref{thm:diagonal-limit-compatibility-completion-consumer-nonescape}, and \autoref{thm:diagonal-limit-compatibility-real-seal-nonescape}. These clauses exclude quotient streams, selected host limits, host equality, choice data, hidden tail oracles, and ambient completeness from the carrier, so none of them can supply a substitute for a deleted finite support row.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility budget-selector route meet]
\label{thm:diagonal-limit-compatibility-budget-selector-route-meet}
Let $K=(R,T,S,D,W,Q,L,H,C,P,N)$ be an accepted $\DiagonalLimitCompatibilityUp$ packet, and let $B$ be an accepted $\FiniteObservationBudgetSelectorUp$ packet using the same $\StreamNameUp$ window row $W$, $\DyadicRatCoreUp$ ledger $D$, $\RegSeqRatUp$ readback row $Q$, and $\RealUp$ seal row $L$. The completion-facing route of $K$ and the finite observation budget-selector route of $B$ meet at the same displayed budget surface
$$
\begin{aligned}
  R,T,S,D,W,Q,L,H,C,P,N .
\end{aligned}
$$
Every completion-facing, $\RealUp$-facing, or selector-facing consumer that preserves those shared rows must read the same budget surface, with no quotient stream, selected host limit, host real equality, choice datum, hidden tail oracle, independent selector, or ambient completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityBudgetSelectorRouteMeet}
\begin{proof}
First lock the diagonal-limit route. Apply \autoref{thm:diagonal-limit-compatibility-source-route-budget-lock} and \autoref{thm:diagonal-limit-compatibility-public-budget-route}. These theorems force the accepted packet $K$ to expose the completion-facing budget through $R,T,S,D,W,Q,L,H,C,P,N$.

Next identify the public ledger. The route-ledger public exactness theorem \autoref{thm:diagonal-limit-compatibility-route-ledger-public-exactness} states that the displayed budget surface is the exact public ledger for $K$, and \autoref{thm:diagonal-limit-compatibility-standard-bridge-surface} repacks the same rows for bridge-facing consumers.

Now attach the selector side through \autoref{ch:concrete-instances-finiteobservationbudgetselector-namecert}. Its accepted carrier and budget-route determinacy use the same $\StreamNameUp$ window, $\DyadicRatCoreUp$ ledger, $\RegSeqRatUp$ readback, and $\RealUp$ seal rows, so the selector route has no separate budget surface once it shares $W,D,Q,L$ with $K$.

The two routes therefore meet at the displayed tuple. The non-escape clauses for the diagonal-limit packet and for the finite observation budget selector exclude quotient streams, selected host limits, host real equality, choice data, hidden tail oracles, independent selectors, and ambient completeness from the shared surface.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility budget-selector bridge-meet exactness]
\label{thm:diagonal-limit-compatibility-budget-selector-bridge-meet-exactness}
Let $K=(R,T,S,D,W,Q,L,H,C,P,N)$ be an accepted $\DiagonalLimitCompatibilityUp$ packet, and let $B$ be an accepted $\FiniteObservationBudgetSelectorUp$ packet using the same $\StreamNameUp$ window row $W$, $\DyadicRatCoreUp$ ledger $D$, $\RegSeqRatUp$ readback row $Q$, and $\RealUp$ seal row $L$. Then every bridge-facing, completion-facing, or selector-facing consumer preserving those rows reads the same standard-reflection support surface
$$
R,T,S,D,W,Q,L,H,C,P,N.
$$
No quotient stream, host real equality, countable choice, hidden selector, independent selected limit, or ambient completeness coordinate refines this bridge meet.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityBudgetSelectorBridgeMeetExactness}
\begin{proof}
First apply the budget-selector route meet, \autoref{thm:diagonal-limit-compatibility-budget-selector-route-meet}. It places the diagonal route and selector route on the same displayed budget surface whenever $W,D,Q,L$ are shared.

Next identify the public ledger. The route-ledger public exactness theorem \autoref{thm:diagonal-limit-compatibility-route-ledger-public-exactness} says that this surface is exactly $R,T,S,D,W,Q,L,H,C,P,N$ for accepted diagonal-limit consumers.

Now pass to bridge-facing consumers. The standard bridge surface theorem \autoref{thm:diagonal-limit-compatibility-standard-bridge-surface} repacks the same rows as the standard-reflection support, and bridge support minimality \autoref{thm:diagonal-limit-compatibility-bridge-support-minimality} prevents deletion or replacement of any listed row.

Finally use the non-escape clauses from the route meet and the bridge support theorem. Quotient streams, host real equality, countable choice, hidden selectors, selected limits, and ambient completeness are absent from both packets, so they cannot refine the common bridge meet.
\end{proof}

\closureat{DiagonalLimitCompatibilityUp}{seedStr}
\closureat{DiagonalLimitCompatibilityUp}{obligationStr}
\closureat{DiagonalLimitCompatibilityUp}{scopedStr}
\closureat{DiagonalLimitCompatibilityUp}{publicStr}
\closureat{DiagonalLimitCompatibilityUp}{bridgedStr}

\begin{closurestatus}{\DiagonalLimitCompatibilityUp}
  \constructivestory{A $\DiagonalLimitCompatibilityUp$ packet is generated from an accepted $\RealCompletenessUp$ diagonal-limit packet, a $\RegularCauchyTriangleBoundUp$ three-leg estimate, a $\CauchyLimitSealUp$ seal route, a $\DyadicRatCoreUp$ tolerance ledger, $\StreamNameUp$ windows, a $\RegSeqRatUp$ readback row, a $\RealUp$ seal class, displayed $\hsame$ transports, $\Cont$ routes, package provenance, and a local naming row.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{def:diagonal-limit-compatibility-carrier}, \autoref{thm:diagonal-limit-compatibility-namecert-obligations}, \autoref{thm:diagonal-limit-compatibility-non-escape}, \autoref{thm:diagonal-limit-compatibility-triangle-seal-route}, \autoref{thm:diagonal-limit-compatibility-window-ledger-exactness}, \autoref{thm:diagonal-limit-compatibility-completion-boundary}, \autoref{thm:diagonal-limit-compatibility-tolerance-ledger-handoff}, \autoref{thm:diagonal-limit-compatibility-seal-consumer-factorization}, \autoref{thm:diagonal-limit-compatibility-window-route-lock}, \autoref{thm:diagonal-limit-compatibility-seal-readback-exactness}, \autoref{thm:diagonal-limit-compatibility-real-seal-nonescape}, \autoref{thm:diagonal-limit-compatibility-source-route-exactness}, \autoref{thm:diagonal-limit-compatibility-regular-readback-exactness}, \autoref{thm:diagonal-limit-compatibility-completion-consumer-nonescape}, \autoref{thm:diagonal-limit-compatibility-finite-source-lock}, \autoref{thm:diagonal-limit-compatibility-real-seal-source-lock}, \autoref{thm:diagonal-limit-compatibility-triangle-seal-scope}, \autoref{thm:diagonal-limit-compatibility-window-fusion}, \autoref{thm:diagonal-limit-compatibility-real-seal-exactness}, \autoref{thm:diagonal-limit-compatibility-real-seal-route-exhaustion}, \autoref{thm:diagonal-limit-compatibility-public-packet-export}, \autoref{thm:diagonal-limit-compatibility-standard-bridge-surface}, and \autoref{thm:diagonal-limit-compatibility-bridge-support-minimality} bind the finite carrier, route ledger, triangle seal, stream-window ledger, RegSeqRat readback, Real seal, completion boundary, route exhaustion, bridge support, and standard bridge surface to the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows of the public packet.}
  \formalstatus{\unformalizedV}
  \bridgestatus{paperBridge}
  \origin{human}
  \notclaimed{This chapter does not close Lean verification, bridge checking, quotient stream equality, selected host limits, host real equality, choice, or ambient completeness.}
  \upgradepath{The remaining work is formal-axis checking of the displayed bridge route.}
\end{closurestatus}
