\begin{theorem}[Diagonal-limit compatibility root budget carrier row]
\label{thm:diagonal-limit-compatibility-root-budget-carrier-row}
The root budget carrier row of an accepted $\DiagonalLimitCompatibilityUp$ packet is the finite packet surface over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ already displayed by the compatibility packet. In packet notation, the root budget route may read only the displayed diagonal source, triangle estimate, seal route, dyadic ledger, stream window, regular readback, Real seal row, componentwise transport, continuation route, provenance, and local naming rows.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityCarrier\_root\_budget\_carrier\_row}
\begin{proof}
Project the accepted compatibility packet. The carrier rows already list the budget-facing source, window, seal, transport, route, provenance, and local name.

Apply the root budget source theorem. \autoref{thm:diagonal-limit-compatibility-root-budget-source} identifies the budget source with the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ surface used by the selector-budget route.

Since the carrier exports no extra budget coordinate, the root budget carrier row is exactly the displayed finite packet surface.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility root budget classifier row]
\label{thm:diagonal-limit-compatibility-root-budget-classifier-row}
The root budget classifier of an accepted $\DiagonalLimitCompatibilityUp$ packet is stable under componentwise $\hsame$ transport of the same displayed packet rows and under replay of the displayed $\Cont$ routes. A downstream route that transports the diagonal source, dyadic ledger, stream window, regular readback, seal row, provenance, and local name by the recorded $\hsame$ rows reaches the same root budget surface after replaying the recorded $\Cont$ rows.
\end{theorem}
\begin{proof}
Begin with the displayed transport rows. Componentwise $\hsame$ movement changes only the representation of each listed coordinate and leaves the packet inventory fixed.

Replay the displayed continuation rows. The root window route theorem \autoref{thm:diagonal-limit-compatibility-root-window-route} places the $\StreamNameUp$ window before the $\RegSeqRatUp$ readback and $\RealUp$ seal handoff, so replay follows the same budget order after transport.

The classifier therefore has no second route on which to branch. It reads the transported packet through the same finite root budget surface.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityCarrier\_root\_budget\_classifier\_row}

\begin{theorem}[Diagonal-limit compatibility root budget ledger row]
\label{thm:diagonal-limit-compatibility-root-budget-ledger-row}
The root budget route of an accepted $\DiagonalLimitCompatibilityUp$ packet has exact ledger boundary and non-escape. Its source is fixed by \autoref{thm:diagonal-limit-compatibility-root-budget-source}, its window order by \autoref{thm:diagonal-limit-compatibility-root-window-route}, its seal boundary by \autoref{thm:diagonal-limit-compatibility-root-seal-boundary}, and its consumer boundary by \autoref{thm:diagonal-limit-compatibility-root-consumer-nonescape}. The route also consumes the sibling L10 surfaces $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before any completion-facing handoff is available.
\end{theorem}
\begin{proof}
First read the source and window rows. The root budget source theorem fixes the finite budget surface, and the root window route theorem orders the $\StreamNameUp$ window before the regular readback and Real seal.

Next read the seal boundary. The root seal boundary theorem forces every Real-facing read through the displayed $\RegSeqRatUp$ readback and seal rows.

Finally apply consumer non-escape. \autoref{thm:diagonal-limit-compatibility-root-consumer-nonescape} exhausts the downstream surface by the displayed packet rows, so no host limit, quotient stream, choice datum, host real equality, or ambient completeness coordinate can enter the root budget route.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityRootBudget\_ledger\_row}

\begin{theorem}[Diagonal-limit compatibility root budget triad exactness]
\label{thm:diagonal-limit-compatibility-root-budget-triad-exactness}
For every accepted $\DiagonalLimitCompatibilityUp$ packet, the carrier row, classifier row, and ledger row jointly exhaust the root-budget route seen by any $\RealUp$ or $\CompletionUp$ consumer. Every such consumer read through the root budget factors through the displayed diagonal source, dyadic ledger, $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\RealUp$ seal, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert_{\DiagonalLimitCompatibilityUp}$ row. No host limit, quotient stream, choice datum, host real equality, or ambient completeness coordinate is part of this route.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_root\_budget\_triad\_exactness}
\begin{proof}
First read the carrier and classifier rows. The carrier row \autoref{thm:diagonal-limit-compatibility-root-budget-carrier-row} fixes the finite packet surface, and the classifier row \autoref{thm:diagonal-limit-compatibility-root-budget-classifier-row} keeps componentwise $\hsame$ transport and $\Cont$ replay on that same surface.

Next impose the ordered window and seal boundaries. The root window route theorem \autoref{thm:diagonal-limit-compatibility-root-window-route} places the $\StreamNameUp$ window before the $\RegSeqRatUp$ readback and $\RealUp$ seal, while the root seal boundary theorem \autoref{thm:diagonal-limit-compatibility-root-seal-boundary} prevents a Real-facing read from bypassing those rows.

Finally apply the ledger and consumer boundaries. The ledger row \autoref{thm:diagonal-limit-compatibility-root-budget-ledger-row} records the exact route inventory, and consumer non-escape \autoref{thm:diagonal-limit-compatibility-root-consumer-nonescape} excludes host limits, quotient streams, choice data, host real equality, and ambient completeness. Thus every $\RealUp$ or $\CompletionUp$ read through the root budget factors through the displayed diagonal packet and no additional coordinate.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility root budget terminal route uniqueness]
\label{thm:diagonal-limit-compatibility-root-budget-terminal-route-uniqueness}
Let $R_0$ and $R_1$ be $\RealUp$- or $\CompletionUp$-facing consumers of an accepted $\DiagonalLimitCompatibilityUp$ packet. Suppose both consumers preserve the displayed diagonal source, dyadic ledger, $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\RealUp$ seal, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert_{\DiagonalLimitCompatibilityUp}$ row. Then $R_0$ and $R_1$ determine the same root-budget terminal route through those rows. There is no second seal, quotient stream, host real equality, selected limit, or ambient completeness coordinate through which either consumer can factor a distinct terminal read.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityRootBudget\_terminal\_route\_uniqueness}
\begin{proof}
First read the root-budget carrier. By \autoref{thm:diagonal-limit-compatibility-root-budget-carrier-row}, both consumers see only the displayed finite packet surface over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and the local $\NameCert$ row.

Next transport the classifier. The classifier row of \autoref{thm:diagonal-limit-compatibility-root-budget-classifier-row} says that componentwise $\hsame$ movement and replay of the displayed $\Cont$ routes keep the diagonal source, ledger, window, readback, seal, provenance, and name on the same root-budget surface.

Now apply the ledger row. \autoref{thm:diagonal-limit-compatibility-root-budget-ledger-row} orders the source, window, readback, and seal boundary and excludes any route coordinate beyond the displayed packet inventory.

Finally use triad exactness. \autoref{thm:diagonal-limit-compatibility-root-budget-triad-exactness} states that every $\RealUp$ or $\CompletionUp$ read through the root budget factors through exactly this carrier, classifier, and ledger triad. Since $R_0$ and $R_1$ preserve the same triad rows, there is no remaining coordinate from which a second terminal route could be formed.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility root-budget Real completion factorization]
\label{thm:diagonal-limit-compatibility-root-budget-real-completion-factorization}
Every $\RealUp$ or $\CompletionUp$ consumer of an accepted $\DiagonalLimitCompatibilityUp$ root-budget packet factors through the displayed diagonal source, dyadic ledger, $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\RealUp$ seal, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert_{\DiagonalLimitCompatibilityUp}$ row. The factorization has no quotient stream, selected limit, host real equality, choice datum, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
First apply triad exactness. By \autoref{thm:diagonal-limit-compatibility-root-budget-triad-exactness}, every $\RealUp$ or $\CompletionUp$ read through the root budget is already exhausted by the carrier, classifier, and ledger rows over the displayed diagonal source, dyadic ledger, $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\RealUp$ seal, transports, routes, provenance, and local name.

Next use terminal-route uniqueness. \autoref{thm:diagonal-limit-compatibility-root-budget-terminal-route-uniqueness} says that two consumers preserving this same displayed packet inventory determine the same terminal route, so a consumer has no second terminal read through a quotient stream, selected limit, host real equality, choice datum, or ambient completeness coordinate.

The carrier row, classifier row, and ledger row therefore exhaust the visible factorization surface. Any $\RealUp$ or $\CompletionUp$ consumer of the accepted root-budget packet factors through exactly those displayed rows.
\end{proof}
