\begin{theorem}[Diagonal-limit compatibility root budget carrier row]
\label{thm:diagonal-limit-compatibility-root-budget-carrier-row}
The root budget carrier row of an accepted $\DiagonalLimitCompatibilityUp$ packet is the finite packet surface over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ already displayed by the compatibility packet. In packet notation, the root budget route may read only the displayed diagonal source, triangle estimate, seal route, dyadic ledger, stream window, regular readback, Real seal row, componentwise transport, continuation route, provenance, and local naming rows.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityCarrier\_root\_budget\_carrier\_row}
\begin{proof}
Project the accepted compatibility packet. The carrier rows already list the budget-facing source, window, seal, transport, route, provenance, and local name.

Apply the root budget source theorem. \autoref{thm:diagonal-limit-compatibility-root-budget-source} identifies the budget source with the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ surface used by the selector-budget route.

Since the carrier exports no extra budget coordinate, the root budget carrier row is exactly the displayed finite packet surface.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility root budget classifier row]
\label{thm:diagonal-limit-compatibility-root-budget-classifier-row}
The root budget classifier of an accepted $\DiagonalLimitCompatibilityUp$ packet is stable under componentwise $\hsame$ transport of the same displayed packet rows and under replay of the displayed $\Cont$ routes. A downstream route that transports the diagonal source, dyadic ledger, stream window, regular readback, seal row, provenance, and local name by the recorded $\hsame$ rows reaches the same root budget surface after replaying the recorded $\Cont$ rows.
\end{theorem}
\begin{proof}
Begin with the displayed transport rows. Componentwise $\hsame$ movement changes only the representation of each listed coordinate and leaves the packet inventory fixed.

Replay the displayed continuation rows. The root window route theorem \autoref{thm:diagonal-limit-compatibility-root-window-route} places the $\StreamNameUp$ window before the $\RegSeqRatUp$ readback and $\RealUp$ seal handoff, so replay follows the same budget order after transport.

The classifier therefore has no second route on which to branch. It reads the transported packet through the same finite root budget surface.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityCarrier\_root\_budget\_classifier\_row}

\begin{theorem}[Diagonal-limit compatibility root budget ledger row]
\label{thm:diagonal-limit-compatibility-root-budget-ledger-row}
The root budget route of an accepted $\DiagonalLimitCompatibilityUp$ packet has exact ledger boundary and non-escape. Its source is fixed by \autoref{thm:diagonal-limit-compatibility-root-budget-source}, its window order by \autoref{thm:diagonal-limit-compatibility-root-window-route}, its seal boundary by \autoref{thm:diagonal-limit-compatibility-root-seal-boundary}, and its consumer boundary by \autoref{thm:diagonal-limit-compatibility-root-consumer-nonescape}. The route also consumes the sibling L10 surfaces $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before any completion-facing handoff is available.
\end{theorem}
\begin{proof}
First read the source and window rows. The root budget source theorem fixes the finite budget surface, and the root window route theorem orders the $\StreamNameUp$ window before the regular readback and Real seal.

Next read the seal boundary. The root seal boundary theorem forces every Real-facing read through the displayed $\RegSeqRatUp$ readback and seal rows.

Finally apply consumer non-escape. \autoref{thm:diagonal-limit-compatibility-root-consumer-nonescape} exhausts the downstream surface by the displayed packet rows, so no host limit, quotient stream, choice datum, host real equality, or ambient completeness coordinate can enter the root budget route.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityRootBudget\_ledger\_row}

\begin{theorem}[Diagonal-limit compatibility root budget triad exactness]
\label{thm:diagonal-limit-compatibility-root-budget-triad-exactness}
For every accepted $\DiagonalLimitCompatibilityUp$ packet, the carrier row, classifier row, and ledger row jointly exhaust the root-budget route seen by any $\RealUp$ or $\CompletionUp$ consumer. Every such consumer read through the root budget factors through the displayed diagonal source, dyadic ledger, $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\RealUp$ seal, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert_{\DiagonalLimitCompatibilityUp}$ row. No host limit, quotient stream, choice datum, host real equality, or ambient completeness coordinate is part of this route.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_root\_budget\_triad\_exactness}
\begin{proof}
First read the carrier and classifier rows. The carrier row \autoref{thm:diagonal-limit-compatibility-root-budget-carrier-row} fixes the finite packet surface, and the classifier row \autoref{thm:diagonal-limit-compatibility-root-budget-classifier-row} keeps componentwise $\hsame$ transport and $\Cont$ replay on that same surface.

Next impose the ordered window and seal boundaries. The root window route theorem \autoref{thm:diagonal-limit-compatibility-root-window-route} places the $\StreamNameUp$ window before the $\RegSeqRatUp$ readback and $\RealUp$ seal, while the root seal boundary theorem \autoref{thm:diagonal-limit-compatibility-root-seal-boundary} prevents a Real-facing read from bypassing those rows.

Finally apply the ledger and consumer boundaries. The ledger row \autoref{thm:diagonal-limit-compatibility-root-budget-ledger-row} records the exact route inventory, and consumer non-escape \autoref{thm:diagonal-limit-compatibility-root-consumer-nonescape} excludes host limits, quotient streams, choice data, host real equality, and ambient completeness. Thus every $\RealUp$ or $\CompletionUp$ read through the root budget factors through the displayed diagonal packet and no additional coordinate.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility root budget terminal route uniqueness]
\label{thm:diagonal-limit-compatibility-root-budget-terminal-route-uniqueness}
Let $R_0$ and $R_1$ be $\RealUp$- or $\CompletionUp$-facing consumers of an accepted $\DiagonalLimitCompatibilityUp$ packet. Suppose both consumers preserve the displayed diagonal source, dyadic ledger, $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\RealUp$ seal, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert_{\DiagonalLimitCompatibilityUp}$ row. Then $R_0$ and $R_1$ determine the same root-budget terminal route through those rows. There is no second seal, quotient stream, host real equality, selected limit, or ambient completeness coordinate through which either consumer can factor a distinct terminal read.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityRootBudget\_terminal\_route\_uniqueness}
\begin{proof}
First read the root-budget carrier. By \autoref{thm:diagonal-limit-compatibility-root-budget-carrier-row}, both consumers see only the displayed finite packet surface over $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and the local $\NameCert$ row.

Next transport the classifier. The classifier row of \autoref{thm:diagonal-limit-compatibility-root-budget-classifier-row} says that componentwise $\hsame$ movement and replay of the displayed $\Cont$ routes keep the diagonal source, ledger, window, readback, seal, provenance, and name on the same root-budget surface.

Now apply the ledger row. \autoref{thm:diagonal-limit-compatibility-root-budget-ledger-row} orders the source, window, readback, and seal boundary and excludes any route coordinate beyond the displayed packet inventory.

Finally use triad exactness. \autoref{thm:diagonal-limit-compatibility-root-budget-triad-exactness} states that every $\RealUp$ or $\CompletionUp$ read through the root budget factors through exactly this carrier, classifier, and ledger triad. Since $R_0$ and $R_1$ preserve the same triad rows, there is no remaining coordinate from which a second terminal route could be formed.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility root-budget Real completion factorization]
\label{thm:diagonal-limit-compatibility-root-budget-real-completion-factorization}
Every $\RealUp$ or $\CompletionUp$ consumer of an accepted $\DiagonalLimitCompatibilityUp$ root-budget packet factors through the displayed diagonal source, dyadic ledger, $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\RealUp$ seal, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert_{\DiagonalLimitCompatibilityUp}$ row. The factorization has no quotient stream, selected limit, host real equality, choice datum, or ambient completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityCarrier\_root\_budget\_real\_completion\_factorization}
\begin{proof}
First apply triad exactness. By \autoref{thm:diagonal-limit-compatibility-root-budget-triad-exactness}, every $\RealUp$ or $\CompletionUp$ read through the root budget is already exhausted by the carrier, classifier, and ledger rows over the displayed diagonal source, dyadic ledger, $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\RealUp$ seal, transports, routes, provenance, and local name.

Next use terminal-route uniqueness. \autoref{thm:diagonal-limit-compatibility-root-budget-terminal-route-uniqueness} says that two consumers preserving this same displayed packet inventory determine the same terminal route, so a consumer has no second terminal read through a quotient stream, selected limit, host real equality, choice datum, or ambient completeness coordinate.

The carrier row, classifier row, and ledger row therefore exhaust the visible factorization surface. Any $\RealUp$ or $\CompletionUp$ consumer of the accepted root-budget packet factors through exactly those displayed rows.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility Cauchy limit functor handoff]
\label{thm:diagonal-limit-compatibility-cauchy-limit-functor-handoff}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
be an accepted $\DiagonalLimitCompatibilityUp$ root-budget packet, and let a $\RealUp$ or $\CompletionUp$ consumer of $K$ be routed through the terminal Real-completion surface of \autoref{thm:diagonal-limit-compatibility-root-budget-real-completion-factorization}. Suppose the same route also supplies an accepted $\CauchyLimitFunctorUp$ packet
$$
F=(S_0,S_1,M,W_0,W_1,Q',D',K',E,H',C',P',N')
$$
whose source seal $S_0$ is the displayed source seal read by the root-budget route and whose rows preserve the displayed source seal, target seal, $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance ledger, $\RealCompletionSealClassifierUp$ row, terminal $\RealUp$ endpoint, componentwise $\hsame$ transports, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert_{\CauchyLimitFunctorUp}$ row. Then the consumer factors through the same finite terminal route already fixed by the root-budget factorization. No quotient stream, selected host limit, host real equality, choice row, detached completion object, or independent completion route remains after the functor packet is consumed.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_cauchy\_limit\_functor\_handoff}
\begin{proof}
First apply the root-budget Real-completion factorization. \autoref{thm:diagonal-limit-compatibility-root-budget-real-completion-factorization} places the consumer on the displayed route through the diagonal source, dyadic ledger, stream window, regular readback, Real seal, componentwise transports, continuations, provenance, and local name.

Next read the $\CauchyLimitFunctorUp$ surface. The NameCert obligations of \autoref{thm:cauchy-limit-functor-namecert-obligations} force the functor packet through its source-to-target seal route, finite observation rows, completion-consumer rows, componentwise transports, continuation replay, provenance, and local name. The Real-completion consumer boundary of \autoref{thm:cauchy-limit-functor-real-completion-consumer} prevents that packet from exporting quotient stream equality, host real equality, selected limits, detached completion objects, ambient completeness, countable-choice selectors, or a second transport route.

Finally insert the functor packet into the terminal route. Root-budget terminal-route uniqueness from \autoref{thm:diagonal-limit-compatibility-root-budget-terminal-route-uniqueness} identifies every Real- or Completion-facing read that preserves the displayed packet rows, and selector-synchronizer exactness in \autoref{thm:diagonal-limit-compatibility-root-budget-selector-synchronizer-exactness} keeps the finite observation surface on the same route. Since the functor preserves the displayed source seal and its terminal Real endpoint, it is consumed on that route rather than opening an independent completion endpoint.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility finite observation-budget factorization]
\label{thm:diagonal-limit-compatibility-finite-observation-budget-factorization}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
be an accepted $\DiagonalLimitCompatibilityUp$ root-budget packet. Suppose a $\RealUp$ or $\CompletionUp$ consumer of $K$ also consumes an accepted $\FiniteObservationBudgetSelectorUp$ packet and an accepted $\CauchySealBudgetSynchronizerUp$ packet on the displayed route. Then the consumer factors through the same finite packet rows: the diagonal source, dyadic ledger $D$, $\StreamNameUp$ window $W$, $\RegSeqRatUp$ readback $Q$, $\RealUp$ seal $L$, the finite observation-budget selector rows, the synchronizer request, seal, and budget rows, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and the local $\NameCert_{\DiagonalLimitCompatibilityUp}$ row. The factorization exports no quotient stream, selected host limit, host real equality, countable-choice row, hidden observation budget, or ambient completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_finite\_observation\_budget\_factorization}
\begin{proof}
First use \autoref{thm:diagonal-limit-compatibility-root-budget-real-completion-factorization}. It places the $\RealUp$ or $\CompletionUp$ consumer on the root-budget route through the displayed diagonal source, dyadic ledger, stream window, regular readback, Real seal, componentwise transports, continuations, provenance, and local name.

Next insert the finite observation-budget selector. The route theorem \autoref{thm:diagonal-limit-compatibility-budget-selector-route} says that the selector handoff is mediated only by the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows, and \autoref{thm:finite-observation-budget-selector-nonescape} excludes quotient streams, host real equality, countable-choice data, hidden limit selectors, and ambient completeness from the selector packet.

Now insert the seal-budget synchronizer. By \autoref{thm:diagonal-limit-compatibility-seal-budget-synchronizer-handoff}, the accepted $\CauchySealBudgetSynchronizerUp$ request, seal, budget, selector, compatibility, transport, route, provenance, and local naming rows are read before $Q$ and $L$ are available to the consumer.

Finally apply \autoref{thm:diagonal-limit-compatibility-real-seal-budget-nonescape} and root-budget terminal route uniqueness from \autoref{thm:diagonal-limit-compatibility-root-budget-terminal-route-uniqueness}. The synchronizer and selector are inserted on the same finite packet route, and terminal uniqueness leaves no second route through a quotient stream, selected host limit, host real equality, countable-choice row, hidden observation budget, or ambient completeness coordinate.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility root-budget selector synchronizer exactness]
\label{thm:diagonal-limit-compatibility-root-budget-selector-synchronizer-exactness}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
be an accepted $\DiagonalLimitCompatibilityUp$ root-budget packet. Any $\RealUp$ or $\CompletionUp$ consumer of $K$ that preserves the displayed diagonal source, dyadic ledger $D$, $\StreamNameUp$ window $W$, $\RegSeqRatUp$ readback $Q$, $\RealUp$ seal $L$, the finite observation-budget selector rows, the $\CauchySealBudgetSynchronizerUp$ request, seal, and budget rows, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and the local $\NameCert_{\DiagonalLimitCompatibilityUp}$ row determines the same terminal root-budget route through the selector and synchronizer. No quotient stream, selected host limit, host real equality, choice row, hidden observation budget, or ambient completeness coordinate remains on the terminal path.
\end{theorem}
\begin{proof}
First apply finite observation-budget factorization. \autoref{thm:diagonal-limit-compatibility-finite-observation-budget-factorization} places the consumer, selector, and synchronizer on the same displayed finite packet route through the diagonal source, ledger, stream window, regular readback, Real seal, transports, continuations, provenance, and local name.

Next use root-budget terminal route uniqueness. \autoref{thm:diagonal-limit-compatibility-root-budget-terminal-route-uniqueness} identifies all $\RealUp$- and $\CompletionUp$-facing reads that preserve this displayed route, so the selector and synchronizer cannot open a second terminal path.

Finally combine triad exactness with consumer non-escape. The triad exactness theorem \autoref{thm:diagonal-limit-compatibility-root-budget-triad-exactness} exhausts the carrier, classifier, and ledger rows, while \autoref{thm:diagonal-limit-compatibility-root-consumer-nonescape} excludes quotient streams, selected host limits, host real equality, choice rows, hidden observation budgets, and ambient completeness coordinates.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_root\_budget\_selector\_synchronizer\_exactness}

\begin{theorem}[Diagonal-limit compatibility finite observation-budget refusal exactness]
\label{thm:diagonal-limit-compatibility-finite-observation-budget-refusal-exactness}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
be an accepted $\DiagonalLimitCompatibilityUp$ root-budget packet. Any $\RealUp$ or $\CompletionUp$ consumer of $K$ that requests quotient stream identity, a selected host limit, host real equality, a countable-choice row, a hidden observation budget, an independent seal-budget route, or ambient completeness is routed to the visible refusal boundary unless that request first factors through the finite observation-budget selector rows and the $\CauchySealBudgetSynchronizerUp$ rows named in \autoref{thm:diagonal-limit-compatibility-finite-observation-budget-factorization}. Thus the only admitted completion-facing route through $K$ is the displayed finite route through $D$, $W$, $Q$, $L$, the selector packet, the synchronizer request, seal, and budget rows, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and the local $\NameCert_{\DiagonalLimitCompatibilityUp}$ row.
\end{theorem}
\begin{proof}
Start with the finite factorization theorem. \autoref{thm:diagonal-limit-compatibility-finite-observation-budget-factorization} names the admitted route for any $\RealUp$ or $\CompletionUp$ consumer that has already consumed the accepted $\FiniteObservationBudgetSelectorUp$ packet and the accepted $\CauchySealBudgetSynchronizerUp$ packet on the displayed route.

The selector route gives the first boundary. By \autoref{thm:diagonal-limit-compatibility-budget-selector-route}, the selector handoff reads only the visible $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows. The selector non-escape theorem \autoref{thm:finite-observation-budget-selector-nonescape} refuses quotient streams, host real equality, countable-choice data, hidden selectors, and ambient completeness outside that finite selector surface.

The synchronizer gives the seal-budget boundary. \autoref{thm:diagonal-limit-compatibility-seal-budget-synchronizer-handoff} places the synchronizer request, seal, and budget rows on the same displayed route before the regular readback $Q$ and Real seal $L$ are available to the consumer. Hence an independent seal-budget route has no separate entry point.

Finally apply root-budget terminal route uniqueness from \autoref{thm:diagonal-limit-compatibility-root-budget-terminal-route-uniqueness}. Once the selector and synchronizer rows are not part of the request, the remaining requested coordinates would have to form a second terminal route through quotient identity, a selected host limit, host real equality, choice, hidden observation budget, independent seal budget, or ambient completeness. Terminal uniqueness excludes that second route, so the request is routed to the visible refusal boundary.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_finite\_observation\_budget\_refusal\_exactness}

\begin{theorem}[Diagonal-limit compatibility selector-synchronizer admission criterion]
\label{thm:diagonal-limit-compatibility-selector-synchronizer-admission-criterion}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
be an accepted $\DiagonalLimitCompatibilityUp$ root-budget packet. A $\RealUp$ or $\CompletionUp$ consumer of $K$ is admitted to the terminal root-budget route exactly when its request factors through both the displayed $\FiniteObservationBudgetSelectorUp$ rows and the $\CauchySealBudgetSynchronizerUp$ request, seal, and budget rows named by \autoref{thm:diagonal-limit-compatibility-finite-observation-budget-factorization}. In that case the admitted request has the unique terminal route through $D$, $W$, $Q$, $L$, the selector packet, the synchronizer request, seal, and budget rows, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and the local $\NameCert_{\DiagonalLimitCompatibilityUp}$ row. Every completion-facing request that does not factor through both displayed gates is routed to the visible refusal boundary.
\end{theorem}
\begin{proof}
First suppose the consumer is admitted to the terminal root-budget route. Finite observation-budget factorization in \autoref{thm:diagonal-limit-compatibility-finite-observation-budget-factorization} supplies the displayed route through the diagonal source, dyadic ledger, stream window, regular readback, Real seal, selector rows, synchronizer request, seal, and budget rows, transports, continuations, provenance, and local name.

The two gates are forced on this route. The selector route theorem \autoref{thm:diagonal-limit-compatibility-budget-selector-route} and selector non-escape theorem \autoref{thm:finite-observation-budget-selector-nonescape} keep the finite observation-budget component inside the displayed selector rows. The synchronizer handoff theorem \autoref{thm:diagonal-limit-compatibility-seal-budget-synchronizer-handoff} then places the request, seal, and budget rows before the readback $Q$ and Real seal $L$ can be consumed.

Uniqueness gives the terminal route. By \autoref{thm:diagonal-limit-compatibility-root-budget-selector-synchronizer-exactness}, a request preserving the displayed selector and synchronizer rows determines the single terminal root-budget route through those rows and cannot open a second completion-facing path.

Conversely, suppose a $\RealUp$ or $\CompletionUp$ request does not factor through both displayed gates. The refusal exactness theorem \autoref{thm:diagonal-limit-compatibility-finite-observation-budget-refusal-exactness} says that quotient stream identity, selected host limits, host real equality, countable-choice rows, hidden observation budgets, independent seal-budget routes, and ambient completeness are visible only at the refusal boundary unless the request first passes through the selector rows and synchronizer rows named above.

Thus the admission test is exactly the joint factorization through the displayed $\FiniteObservationBudgetSelectorUp$ rows and $\CauchySealBudgetSynchronizerUp$ request, seal, and budget rows. Requests satisfying that test use the unique terminal route; all other completion-facing requests are refused.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility selector-synchronizer Real-seal scope]
\label{thm:diagonal-limit-compatibility-selector-synchronizer-real-seal-scope}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
be an accepted $\DiagonalLimitCompatibilityUp$ root-budget packet. For every $\RealUp$ or $\CompletionUp$ consumer admitted by the selector-synchronizer admission criterion, the Real seal scope of the consumer is exhausted by the dyadic ledger $D$, the $\StreamNameUp$ window $W$, the $\RegSeqRatUp$ readback $Q$, the $\RealUp$ seal $L$, the $\FiniteObservationBudgetSelectorUp$ rows, the $\CauchySealBudgetSynchronizerUp$ request, seal, and budget rows, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and the local $\NameCert_{\DiagonalLimitCompatibilityUp}$ row. No quotient stream, selected host limit, host real equality, countable-choice row, hidden observation budget, independent seal-budget route, or ambient completeness coordinate remains in the Real seal scope.
\end{theorem}
\begin{proof}
Begin with the finite observation-budget factorization. \autoref{thm:diagonal-limit-compatibility-finite-observation-budget-factorization} places every admitted $\RealUp$ or $\CompletionUp$ consumer on the displayed route through $D$, $W$, $Q$, $L$, the selector rows, the synchronizer request, seal, and budget rows, transports, continuations, provenance, and local name.

Next apply selector-synchronizer exactness. \autoref{thm:diagonal-limit-compatibility-root-budget-selector-synchronizer-exactness} identifies this displayed selector and synchronizer route as the terminal root-budget route, while \autoref{thm:diagonal-limit-compatibility-finite-observation-budget-refusal-exactness} routes every request outside the two displayed gates to the refusal boundary.

The admission criterion then supplies the exact gate. By \autoref{thm:diagonal-limit-compatibility-selector-synchronizer-admission-criterion}, an admitted consumer is precisely one whose request factors through both the finite observation-budget selector rows and the synchronizer request, seal, and budget rows.

Finally use Real-seal-budget non-escape and terminal route uniqueness. \autoref{thm:diagonal-limit-compatibility-real-seal-budget-nonescape} prevents the Real seal from acquiring a hidden budget coordinate, and \autoref{thm:diagonal-limit-compatibility-root-budget-terminal-route-uniqueness} leaves no second terminal route through quotient streams, selected host limits, host real equality, countable-choice rows, independent seal-budget routes, or ambient completeness. Thus the displayed rows exhaust the Real seal scope.
\end{proof}
