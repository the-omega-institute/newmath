\begin{theorem}[Diagonal-limit compatibility root budget coherence]
\label{thm:diagonal-limit-compatibility-root-budget-coherence}
For an accepted $\DiagonalLimitCompatibilityUp$ packet
$$
K=(R,T,S,D,W,Q,L,H,C,P,N),
$$
the root-budget route is the same displayed route used by the rows
$D,W,Q,L,H,C,P,N$. It reads the $\DyadicRatCoreUp$ ledger $D$, the
$\StreamNameUp$ window row $W$, the $\RegSeqRatUp$ readback row $Q$, the
$\RealUp$ seal row $L$, the componentwise $\hsame$ transports $H$, the
displayed $\Cont$ routes $C$, the $\Pkg$ provenance $P$, and the local
$\NameCert$ row $N$ through the carrier of
\autoref{def:diagonal-limit-compatibility-carrier}. It has no root-budget
source outside that $\BHist/\hsame/\Cont/\Pkg/\NameCert$ surface.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_root\_budget\_coherence}
\begin{proof}
Project the accepted carrier. The only root-facing data in the packet are the
displayed rows $R,T,S,D,W,Q,L,H,C,P,N$, with $D,W,Q,L$ carrying the budget,
window, readback, and seal coordinates and $H,C,P,N$ carrying transport,
route, provenance, and naming.

Apply the root budget source theorem,
\autoref{thm:diagonal-limit-compatibility-root-budget-source}. It identifies
the budget source with the same finite structural surface used by the
selector-budget route, rather than with a hidden host-side source.

Now use the root window route and root seal boundary theorems,
\autoref{thm:diagonal-limit-compatibility-root-window-route} and
\autoref{thm:diagonal-limit-compatibility-root-seal-boundary}. They keep the
route through $W,Q,L$ after the displayed ledger and window rows and before
any Real-facing handoff.

Finally apply root consumer non-escape,
\autoref{thm:diagonal-limit-compatibility-root-consumer-nonescape}. Since no
additional budget coordinate is exported, the root-budget route is exactly
the route already used by $D,W,Q,L,H,C,P,N$.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility root selector budget exhaustion]
\label{thm:diagonal-limit-compatibility-root-selector-budget-exhaustion}
Every selector-budget consumer of an accepted $\DiagonalLimitCompatibilityUp$
packet factors through
$$
R,T,S,D,W,Q,L.
$$
It must first read the accepted diagonal source $R$, the triangle selector row
$T$, the seal route $S$, the dyadic ledger $D$, the finite window row $W$, the
regular readback row $Q$, and the Real seal row $L$, transported by the same
$H,C,P,N$ support rows. No selector-budget consumer can introduce a second
diagonal route, alternate window route, alternate dyadic ledger, selected
host limit, quotient stream, choice datum, or ambient completeness input.
\end{theorem}
\begin{proof}
Start with the carrier projection. A selector-budget consumer sees one
diagonal source row $R$, one triangle selector row $T$, one seal route $S$,
one dyadic ledger $D$, one stream-window row $W$, one regular readback $Q$,
and one Real seal row $L$.

Use triangle selector alignment together with source-route exactness,
\autoref{thm:diagonal-limit-compatibility-triangle-selector-alignment} and
\autoref{thm:diagonal-limit-compatibility-source-route-exactness}. These
theorems attach the selector route to the same $R,T,D,W$ rows and place the
route to $Q,L$ on that displayed source path.

Next apply the root budget carrier and classifier rows,
\autoref{thm:diagonal-limit-compatibility-root-budget-carrier-row} and
\autoref{thm:diagonal-limit-compatibility-root-budget-classifier-row}. The
carrier row supplies the finite packet surface, and the classifier row keeps
componentwise $\hsame$ transport and $\Cont$ replay on that same route.

The exhaustion claim follows from root budget ledger row and completion
consumer non-escape,
\autoref{thm:diagonal-limit-compatibility-root-budget-ledger-row} and
\autoref{thm:diagonal-limit-compatibility-completion-consumer-nonescape}.
Together they leave no selector-budget coordinate outside
$R,T,S,D,W,Q,L,H,C,P,N$.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityCarrier\_root\_selector\_budget\_exhaustion}

\begin{theorem}[Diagonal-limit compatibility Real seal readback lock]
\label{thm:diagonal-limit-compatibility-root-real-seal-readback-lock}
Every $\RealUp$ or $\CompletionUp$ seal read of an accepted
$\DiagonalLimitCompatibilityUp$ packet reaches $L$ only after the
$\StreamNameUp$ window row $W$, the $\RegSeqRatUp$ readback row $Q$, the
$\DyadicRatCoreUp$ ledger $D$, and the $\CauchyLimitSealUp$ route $S$ have
been fixed on the displayed compatibility route. The read is therefore locked
to the carrier route
$$
R,T,S,D,W,Q,L,H,C,P,N
$$
and cannot be replaced by a host real equality, selected limit, quotient
stream, choice datum, or ambient completeness principle.
\end{theorem}
\begin{proof}
First read the seal source synchronization theorem,
\autoref{thm:diagonal-limit-compatibility-seal-source-synchronization}. It
fixes $R,T,S,D,W,Q,L$ together with $H,C,P,N$ before a seal-facing consumer
can inspect the readback or seal rows.

Next apply the triangle readback lock and regular readback exactness,
\autoref{thm:diagonal-limit-compatibility-triangle-readback-lock} and
\autoref{thm:diagonal-limit-compatibility-regular-readback-exactness}. These
results place $Q$ behind the same $T,D,W$ route used by the seal row and
exclude an independent regular readback source.

Now use seal readback exactness and Real seal exactness,
\autoref{thm:diagonal-limit-compatibility-seal-readback-exactness} and
\autoref{thm:diagonal-limit-compatibility-real-seal-exactness}. They identify
the route to $L$ with the displayed seal, dyadic, window, readback, transport,
continuation, provenance, and local naming rows.

Finally apply the completion consumer gate,
\autoref{thm:diagonal-limit-compatibility-completion-consumer-gate}. Every
$\RealUp$ or $\CompletionUp$ read remains downstream of the same finite
budget-source and seal-budget routes, so no host-side completion data can
replace the locked readback route.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityCarrier\_root\_real\_seal\_readback\_lock}

\begin{theorem}[Diagonal-limit compatibility finite-window handoff boundary]
\label{thm:diagonal-limit-compatibility-finite-window-handoff-boundary}
Every $\RealUp$ or $\CompletionUp$ consumer of an accepted
$\DiagonalLimitCompatibilityUp$ packet reaches the public handoff only through
the displayed finite route
$$
R,T,S,D,W,Q,L,H,C,P,N.
$$
The handoff consumes the root-budget route, the selector-budget route, and the
Real seal readback lock as one carrier boundary. It cannot be replaced by host
real equality, a quotient stream, a selected limit, choice data, or ambient
completeness.
\end{theorem}
\begin{proof}
First apply
\autoref{thm:diagonal-limit-compatibility-root-budget-coherence}. This fixes
the root-budget route on the same $D,W,Q,L,H,C,P,N$ surface that the accepted
carrier exposes to downstream consumers.

Next apply
\autoref{thm:diagonal-limit-compatibility-root-selector-budget-exhaustion}.
The selector-budget read is exhausted by $R,T,S,D,W,Q,L$ and the structural
rows, so no second diagonal route, second window route, alternate ledger, or
choice datum is available.

Finally apply
\autoref{thm:diagonal-limit-compatibility-root-real-seal-readback-lock}. The
seal read reaches $L$ only after $S,D,W,Q$ have been fixed on the displayed
compatibility route.

The non-escape clauses
\autoref{thm:diagonal-limit-compatibility-root-consumer-nonescape} and
\autoref{thm:diagonal-limit-compatibility-completion-consumer-nonescape}
remove host real equality, quotient streams, selected limits, and ambient
completeness from the public handoff. Therefore the finite route
$R,T,S,D,W,Q,L,H,C,P,N$ is the complete boundary for $\RealUp$ and
$\CompletionUp$ consumers.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility root budget public boundary]
\label{thm:diagonal-limit-compatibility-root-budget-public-boundary}
Every $\RealUp$ or $\CompletionUp$ consumer of an accepted
$\DiagonalLimitCompatibilityUp$ packet is confined to the single displayed
route
$$
R,T,S,D,W,Q,L,H,C,P,N.
$$
The route simultaneously consumes the root-budget coherence, selector-budget
exhaustion, Real-seal readback lock, and finite-window handoff boundary. It
admits no host real equality, quotient stream, choice datum, selected limit, or
ambient completeness input.
\end{theorem}
\begin{proof}
Begin with root-budget coherence,
\autoref{thm:diagonal-limit-compatibility-root-budget-coherence}. It places the
root-facing budget read on the same $D,W,Q,L,H,C,P,N$ surface exposed by the
accepted carrier.

Next apply selector-budget exhaustion,
\autoref{thm:diagonal-limit-compatibility-root-selector-budget-exhaustion}. The
selector route has already consumed $R,T,S,D,W,Q,L$ together with the same
transport, continuation, provenance, and naming rows.

Then apply the Real-seal readback lock,
\autoref{thm:diagonal-limit-compatibility-root-real-seal-readback-lock}. This
locks every $\RealUp$ or $\CompletionUp$ seal read to $L$ only after the
displayed $S,D,W,Q$ route has been fixed.

Use the finite-window handoff boundary,
\autoref{thm:diagonal-limit-compatibility-finite-window-handoff-boundary}, to
package these three reads as one public consumer boundary for the route
$R,T,S,D,W,Q,L,H,C,P,N$.

Finally, the non-escape clauses
\autoref{thm:diagonal-limit-compatibility-root-consumer-nonescape} and
\autoref{thm:diagonal-limit-compatibility-completion-consumer-nonescape} close
coordinate exhaustion. They leave no host real equality, quotient stream,
choice datum, selected limit, or ambient completeness input outside the
displayed packet route.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility root-budget handoff totality]
\label{thm:diagonal-limit-compatibility-root-budget-handoff-totality}
Every root-budget handoff from an accepted
$\DiagonalLimitCompatibilityUp$ packet to a $\RealUp$ or $\CompletionUp$
consumer is exhausted by the ordered route
$$
R,T,S,D,W,Q,L,H,C,P,N.
$$
The handoff is already the finite Real and Completion route carried by the
packet. It exposes no host real equality, quotient stream, selected limit,
choice data, ambient completeness, or second budget source.
\end{theorem}
\begin{proof}
Begin with
\autoref{thm:diagonal-limit-compatibility-root-budget-coherence}. It fixes the
root budget on the displayed $D,W,Q,L,H,C,P,N$ carrier rows before any
downstream consumer can read the packet.

Next apply
\autoref{thm:diagonal-limit-compatibility-root-selector-budget-exhaustion}.
This consumes the selector contribution on $R,T,S,D,W,Q,L$ and leaves only
the displayed transport, continuation, provenance, and naming rows available
to the handoff.

Now use
\autoref{thm:diagonal-limit-compatibility-root-real-seal-readback-lock}. The
Real-facing read reaches $L$ only after the public selector and budget rows
have already been fixed on the same route.

Apply
\autoref{thm:diagonal-limit-compatibility-finite-window-handoff-boundary} and
\autoref{thm:diagonal-limit-compatibility-root-budget-public-boundary}. These
two public-boundary theorems package the root budget, selector budget, Real
seal lock, and finite-window handoff as one route for $\RealUp$ and
$\CompletionUp$ consumers.

The non-escape clauses attached to those five rows remove every remaining
off-route coordinate. Hence the root-budget handoff is total for the displayed
finite route, with no host real equality, quotient stream, selected limit,
choice data, ambient completeness, or second budget source.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility budget coherence terminal lock]
\label{thm:diagonal-limit-compatibility-budget-coherence-terminal-lock}
For every accepted $\DiagonalLimitCompatibilityUp$ packet
$K=(R,T,S,D,W,Q,L,H,C,P,N)$, every $\RealUp$ or $\CompletionUp$ consumer that
reads the root-budget handoff passes through the single ordered route
$$
R,T,S,D,W,Q,L,H,C,P,N.
$$
The terminal route is the displayed
$\BHist/\hsame/\Cont/\Pkg/\NameCert$ packet. It admits no host real equality,
quotient stream, selected limit, choice datum, ambient completeness input,
alternate dyadic ledger, alternate $\StreamNameUp$ window, or second budget
source.
\end{theorem}
\begin{proof}
Begin with root-budget coherence,
\autoref{thm:diagonal-limit-compatibility-root-budget-coherence}. It places
the budget read on the same $D,W,Q,L,H,C,P,N$ rows carried by the accepted
packet.

Next use selector-budget exhaustion,
\autoref{thm:diagonal-limit-compatibility-root-selector-budget-exhaustion}.
The selector contribution consumes $R,T,S,D,W,Q,L$ on the same packet surface,
so no alternate dyadic ledger or alternate $\StreamNameUp$ window can sit
between the root budget and the public selector read.

Apply the Real-seal readback lock,
\autoref{thm:diagonal-limit-compatibility-root-real-seal-readback-lock}. It
forces every $\RealUp$ or $\CompletionUp$ readback to reach $L$ only through
the already fixed selector, window, dyadic, and regular readback rows.

The finite-window handoff boundary
\autoref{thm:diagonal-limit-compatibility-finite-window-handoff-boundary} and
the root-budget public boundary
\autoref{thm:diagonal-limit-compatibility-root-budget-public-boundary} package
these rows as one public consumer route. The handoff totality theorem,
\autoref{thm:diagonal-limit-compatibility-root-budget-handoff-totality},
then exhausts the route for every root-budget handoff.

All cited route coordinates are therefore identical:
$$
R,T,S,D,W,Q,L,H,C,P,N.
$$
Since the displayed $\BHist/\hsame/\Cont/\Pkg/\NameCert$ packet has already
consumed the budget, selector, window, readback, and seal rows, no host real
equality, quotient stream, selected limit, choice datum, ambient completeness,
alternate dyadic ledger, alternate $\StreamNameUp$ window, or second budget
source remains available to a $\RealUp$ or $\CompletionUp$ consumer.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility root budget consumer chain]
\label{thm:diagonal-limit-compatibility-root-budget-consumer-chain}
Every $\RealUp$ or $\CompletionUp$ consumer of an accepted
$\DiagonalLimitCompatibilityUp$ packet reaches the public read only through
the ordered chain
$$
\text{root-budget coherence}
\to
\text{selector-budget exhaustion}
\to
\text{Real-seal readback lock}
\to
\text{finite-window handoff}
\to
\text{public boundary}
$$
on the displayed route
$$
R,T,S,D,W,Q,L,H,C,P,N.
$$
Thus the $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, and
$\CompletionUp$-facing reads consume one ordered BEDC route rather than a
host limit, quotient stream, choice datum, host real equality, or ambient
completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityRootBudgetConsumerChain}
\begin{proof}
First apply
\autoref{thm:diagonal-limit-compatibility-root-budget-coherence}. It fixes the
root-budget read on the same $D,W,Q,L,H,C,P,N$ surface carried by the accepted
packet.

Next apply
\autoref{thm:diagonal-limit-compatibility-root-selector-budget-exhaustion}. It
places the selector budget behind $R,T,S,D,W,Q,L$ and rules out a second
diagonal source, alternate window route, alternate dyadic ledger, selected
host limit, quotient stream, choice datum, or ambient completeness input
between the root-budget read and the selector read.

Then apply
\autoref{thm:diagonal-limit-compatibility-root-real-seal-readback-lock}. The
seal read reaches $L$ only after $S,D,W,Q$ have been fixed on the displayed
compatibility route, so no host real equality can enter between the selector
budget and the $\RealUp$ read.

Now use
\autoref{thm:diagonal-limit-compatibility-finite-window-handoff-boundary}. It
packages the root-budget route, selector-budget route, and Real-seal lock as
one finite handoff on $R,T,S,D,W,Q,L,H,C,P,N$.

Finally apply
\autoref{thm:diagonal-limit-compatibility-root-budget-public-boundary}. The
public boundary is downstream of those four links on the same route, so every
$\RealUp$ or $\CompletionUp$ consumer reaches the public read without inserting
a host limit, quotient stream, choice datum, host real equality, or ambient
completeness coordinate between any two links of the chain.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility TailMeet refinement seal route]
\label{thm:diagonal-limit-compatibility-tailmeet-refinement-seal-route}
Every accepted $\DiagonalLimitCompatibilityUp$ packet whose root budget route
enters through $\RegularCauchyTailMeetUp$, whose refinement read is governed by
$\CauchyModulusRefinementUp$, and whose endpoint seal is consumed by
$\RealTailAgreementSealUp$ factors through the single displayed L10 route
$$
R,T,S,D,W,Q,L,H,C,P,N
$$
followed by the TailMeet budget-seal rows, the refinement rows, and the
agreement-seal rows. The composed route exports no quotient stream, host real
equality, selected limit, choice datum, side-channel completion carrier, or
ambient completeness coordinate.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_root\_tailmeet\_refinement\_seal\_route}
\begin{proof}
Begin with the local diagonal packet. Apply
\autoref{thm:diagonal-limit-compatibility-root-budget-consumer-chain}. It
places every $\RealUp$ or $\CompletionUp$ consumer of the accepted
$\DiagonalLimitCompatibilityUp$ carrier on the ordered route
$R,T,S,D,W,Q,L,H,C,P,N$ before the public read.

Next attach the TailMeet entry and refinement handoff. Use
\autoref{thm:cauchy-modulus-refinement-bridge-consumer-route-exhaustion}. A
consumer entering through the $\RegularCauchyTailMeetUp$ root budget-seal
package must consume the displayed TailMeet rows and then the refinement route
before any endpoint seal is exported.

The route is unique at the refinement surface. Apply
\autoref{thm:cauchy-modulus-refinement-tailmeet-budget-route-uniqueness}. It
identifies the exported $\RealUp$ and $\CompletionUp$ handoff as the same
public route under the displayed $\hsame$ rows, with no side-channel completion
carrier or ambient completeness coordinate.

Finally use
\autoref{thm:real-tail-agreement-seal-root-selector-completion-lattice}. The
$\RealTailAgreementSealUp$ endpoint is reached through the finite completion
lattice after the selector, window, dyadic readback, regular readback, and
agreement-seal rows have been consumed.

These four route certificates exhaust the available
$\BHist/\hsame/\Cont/\Pkg/\NameCert$ coordinates. Since the diagonal packet,
TailMeet package, refinement read, and agreement seal each expose only their
displayed rows, no quotient stream, host real equality, selected limit, choice
datum, side-channel completion carrier, or ambient completeness coordinate can
be exported by the composed route.
\end{proof}

\begin{theorem}[Root TailMeet refinement Real-seal correspondence]
\label{thm:diagonal-limit-compatibility-root-tailmeet-refinement-real-seal-correspondence}
For every accepted $\DiagonalLimitCompatibilityUp$ packet entering through
$\RegularCauchyTailMeetUp$, refined by $\CauchyModulusRefinementUp$, and read
by $\RealTailAgreementSealUp$, the endpoint $\RealUp/\CompletionUp$ consumer
route is equivalent to the staged public read obtained by first consuming the
displayed diagonal route
$$
R,T,S,D,W,Q,L,H,C,P,N,
$$
then the TailMeet budget-seal rows, then the refinement rows, and then the
agreement-seal rows. Under this correspondence the endpoint route exports no
quotient stream, host real equality, selected limit, choice datum,
side-channel completion carrier, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
For the forward direction, start from an endpoint $\RealUp/\CompletionUp$
consumer of the accepted diagonal packet. Apply
\autoref{thm:diagonal-limit-compatibility-root-budget-consumer-chain} to place
the consumer on the displayed $R,T,S,D,W,Q,L,H,C,P,N$ spine. Then use
\autoref{thm:cauchy-modulus-refinement-bridge-consumer-route-exhaustion} to
force the TailMeet budget-seal entry and the refinement handoff before the
endpoint seal can be read.

The refinement part has no alternate public route. Apply
\autoref{thm:cauchy-modulus-refinement-tailmeet-budget-route-uniqueness}; it
identifies the TailMeet-to-refinement handoff with the same exported
$\RealUp/\CompletionUp$ read. Finally apply
\autoref{thm:real-tail-agreement-seal-root-selector-completion-lattice} to
finish the endpoint through the agreement-seal lattice, so the original
consumer has exactly the staged diagonal, TailMeet, refinement, and
agreement-seal presentation.

For the reverse direction, begin with a staged public read in that order. The
diagonal root-budget chain supplies the $\DiagonalLimitCompatibilityUp$
consumer spine, the route-exhaustion theorem supplies the TailMeet and
refinement rows, and the uniqueness theorem prevents a second refinement
handoff from being inserted. The Real-tail agreement lattice then repacks the
last rows as the endpoint $\RealUp/\CompletionUp$ seal read.

Both directions use only the exposed $\BHist/\hsame/\Cont/\Pkg/\NameCert$
rows named by the four cited route theorems. Their exhaustion clauses remove
the remaining possible coordinates, so the correspondence carries no quotient
stream, host real equality, selected limit, choice datum, side-channel
completion carrier, or ambient completeness input.
\end{proof}

\begin{theorem}[Diagonal-limit compatibility root transport replay exactness]
\label{thm:diagonal-limit-compatibility-root-transport-replay-exactness}
Any root-budget consumer entering the $\RegularCauchyTailMeetUp$ refinement
route from an accepted $\DiagonalLimitCompatibilityUp$ packet is transported and
replayed through the same ordered rows
$$
R,T,S,D,W,Q,L,H,C,P,N.
$$
The replayed route cannot split into a second diagonal selector, detached
TailMeet object, host limit, quotient stream, or ambient completion coordinate.
\end{theorem}
\begin{proof}
Begin with the root budget consumer chain,
\autoref{thm:diagonal-limit-compatibility-root-budget-consumer-chain}. It
places every root-facing consumer on the displayed
$R,T,S,D,W,Q,L,H,C,P,N$ route before any TailMeet refinement handoff is read.

Then apply
\autoref{thm:diagonal-limit-compatibility-root-tailmeet-refinement-real-seal-correspondence}.
The TailMeet refinement and Real-seal endpoint consume the same root spine and
do not introduce a second diagonal selector or detached TailMeet coordinate.

The transported replay is therefore exact at the public surface. Since both
cited routes expose only the listed $\BHist/\hsame/\Cont/\Pkg/\NameCert$ rows,
there is no host limit, quotient stream, or ambient completion coordinate left
for the replay to branch through.
\end{proof}
