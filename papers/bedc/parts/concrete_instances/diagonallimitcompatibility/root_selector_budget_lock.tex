\begin{theorem}[DiagonalLimitCompatibility root selector budget lock]
\label{thm:diagonal-limit-compatibility-root-selector-budget-lock}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
be an accepted $\DiagonalLimitCompatibilityUp$ packet. Every accepted root-selector packet routes the displayed source budget through the selector row, the seal-synchronizer handoff, the $\BHist$ inventory, the componentwise $\hsame$ transports, the displayed $\Cont$ routes, the $\Pkg$ provenance row, and the local $\NameCert$ row before any diagonal-limit read reaches $D,W,Q,L$. No selector read can bypass that route by a host limit, quotient stream, host real equality, chosen completion point, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityCarrier\_root\_selector\_budget\_lock}
\begin{proof}
Project the accepted packet through \autoref{def:diagonal-limit-compatibility-carrier}. The carrier exposes the diagonal source, triangle and selector-facing budget rows, dyadic ledger, stream window, regular readback, Real seal, transport rows, continuation routes, package provenance, and local name as one finite $\BHist$ surface.

Apply \autoref{thm:diagonal-limit-compatibility-selector-budget-source-lock}. It identifies the selector-budget source with the displayed diagonal route, window row, dyadic ledger, regular readback, Real seal, and structural rows $H,C,P,N$.

Next use \autoref{thm:diagonal-limit-compatibility-seal-synchronizer-budget-handoff}. The synchronizer is read downstream of the same selector-budget prefix, so the root selector cannot obtain an independent seal source before the diagonal-limit read.

Finally apply \autoref{thm:diagonal-limit-compatibility-root-route-budget-package} and \autoref{thm:diagonal-limit-compatibility-root-route-nonescape}. They place the diagonal-limit consumer on the displayed $\BHist/\hsame/\Cont/\Pkg/\NameCert$ package and remove every route through host limits, quotient streams, host equality, chosen completion points, or ambient completeness.
\end{proof}

\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityRootSelectorBudgetLock}

\begin{theorem}[DiagonalLimitCompatibility root selector finite-window refinement]
\label{thm:diagonal-limit-compatibility-root-selector-window-refinement}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
be an accepted $\DiagonalLimitCompatibilityUp$ packet. Every root-selector read of $K$ that reaches a $\RealUp$ or $\CompletionUp$ seal refines through the same finite L10 route: the selector-budget source, the $\CauchySealBudgetSynchronizerUp$ handoff, the $\StreamNameUp$ window $W$, the $\RegSeqRatUp$ readback $Q$, the $\DyadicRatCoreUp$ ledger $D$, the $\RealUp$ seal $L$, the $\CauchyLimitSealUp$ boundary, and the structural $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. The refined route has no host limit, quotient stream, selected limit, host real equality, countable-choice row, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Start with \autoref{thm:diagonal-limit-compatibility-root-selector-budget-lock}. It fixes every root-selector read on the displayed selector-budget source and structural rows before the read may inspect $D,W,Q,L$.

Insert the seal handoff using \autoref{thm:diagonal-limit-compatibility-seal-budget-synchronizer-handoff}. The synchronizer is downstream of the same selector prefix, so the read cannot swap to a second source while moving toward a $\RealUp$ or $\CompletionUp$ seal.

Next apply \autoref{thm:diagonal-limit-compatibility-tail-schedule-readback}. The stream window $W$, regular-rational readback $Q$, dyadic ledger $D$, and Real seal $L$ remain on the finite route that the tail schedule exposes to the seal-facing consumer.

Finally combine \autoref{thm:diagonal-limit-compatibility-seal-budget-route-exhaustion} with \autoref{thm:diagonal-limit-compatibility-real-seal-budget-nonescape}. These results place the $\CauchyLimitSealUp$ boundary and every $\RealUp$ or $\CompletionUp$ consumer inside the same package rows $R,T,S,D,W,Q,L,H,C,P,N$, excluding host limits, quotient streams, selected limits, host equality, countable-choice rows, and ambient completeness.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityRootSelectorWindowRefinement}

\begin{theorem}[DiagonalLimitCompatibility seal budget route exhaustion]
\label{thm:diagonal-limit-compatibility-seal-budget-route-exhaustion}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
be an accepted $\DiagonalLimitCompatibilityUp$ packet. Every $\RealUp$ or $\CompletionUp$ seal consumer that reads the selector-budget route factors through the same public route
$$
R,T,S,D,W,Q,L,H,C,P,N .
$$
The consumer receives the source budget, selector handoff, seal synchronizer, dyadic ledger, stream window, regular readback, Real seal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, and it cannot export quotient streams, selected limits, host equality, or ambient completeness.
\end{theorem}
\begin{proof}
Start with \autoref{thm:diagonal-limit-compatibility-root-selector-budget-lock}. It fixes the selector-budget route before a diagonal-limit or seal-facing consumer can inspect the readback rows.

Use \autoref{thm:diagonal-limit-compatibility-completion-consumer-budget-nonescape}. Every completion-facing or Real-facing consumer that uses the selector-budget route is exhausted by $R,T,S,D,W,Q,L,H,C,P,N$.

Now apply \autoref{thm:diagonal-limit-compatibility-real-seal-budget-nonescape}. The inserted synchronizer and tail-schedule route remain part of the same finite compatibility surface, so no second seal route is available.

The exclusion clause follows from \autoref{thm:diagonal-limit-compatibility-root-route-consumer-exactness} and \autoref{thm:diagonal-limit-compatibility-root-route-nonescape}. The public consumer surface contains only the listed packet rows and has no coordinate for quotient streams, selected limits, host equality, or ambient completeness.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilitySealBudgetRouteExhaustion}

\begin{theorem}[DiagonalLimitCompatibility selector-lock seal route exhaustion]
\label{thm:diagonal-limit-compatibility-selector-lock-seal-route-exhaustion}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
be an accepted $\DiagonalLimitCompatibilityUp$ packet. Every accepted selector-budget read of $K$ that reaches a $\RealUp$ or $\CompletionUp$ seal passes through the root selector lock, the $\CauchySealBudgetSynchronizerUp$ handoff, the $\TailCofinalityScheduleUp$ readback when present, the $\RegSeqRatUp$ row $Q$, and the $\RealUp$ seal $L$ on the same displayed route. The route exports no quotient stream, selected host limit, host real equality, independent seal object, countable-choice row, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilitySelectorLockSealRouteExhaustion}
\begin{proof}
First combine \autoref{thm:diagonal-limit-compatibility-root-selector-budget-lock} with \autoref{thm:diagonal-limit-compatibility-seal-budget-route-exhaustion}. The first theorem fixes the upstream selector-budget prefix, and the second exhausts every $\RealUp$ or $\CompletionUp$ seal consumer by the same finite packet $R,T,S,D,W,Q,L,H,C,P,N$.

Next insert the synchronizer with \autoref{thm:diagonal-limit-compatibility-seal-budget-synchronizer-handoff}. It forces the $\CauchySealBudgetSynchronizerUp$ request, seal, budget, selector, compatibility, transport, route, provenance, and local naming rows to be read before $Q$ and $L$.

When a tail schedule is present, use \autoref{thm:diagonal-limit-compatibility-tail-schedule-readback}. The schedule readback stays on the same route through the finite window row, dyadic ledger, regular-rational readback, and Real seal.

Finally apply \autoref{thm:diagonal-limit-compatibility-real-seal-budget-nonescape}. The composed route exports only the displayed compatibility rows, synchronizer rows, tail-schedule rows, regular-rational readback, and $\RealUp$ seal, so no quotient stream, selected host limit, host equality, independent seal object, countable-choice row, or ambient completeness coordinate can escape it.
\end{proof}

\begin{theorem}[DiagonalLimitCompatibility root selector window pullback uniqueness]
\label{thm:diagonal-limit-compatibility-root-selector-window-pullback-uniqueness}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
and
$$
K'=(R',T',S',D',W',Q',L',H',C',P',N')
$$
be accepted $\DiagonalLimitCompatibilityUp$ root-selector reads. If the two reads share the same selector-budget source, the same $\CauchySealBudgetSynchronizerUp$ handoff, the same finite $\StreamNameUp$ window, the same $\RegSeqRatUp$ readback, the same $\DyadicRatCoreUp$ ledger, and the same $\RealUp$ seal row, then their completion-facing consumer surfaces are the same finite pullback through
$$
R,T,S,D,W,Q,L,H,C,P,N .
$$
No quotient stream, selected host limit, host real equality, independent seal object, countable-choice row, or ambient completeness coordinate can distinguish the two reads.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityRootSelectorWindowPullbackUniqueness}
\begin{proof}
Start with \autoref{thm:diagonal-limit-compatibility-root-selector-budget-lock}. It places both root-selector reads on the same selector-budget source and the same structural route before any consumer can inspect the diagonal ledger, finite window, regular readback, or Real seal.

Next use \autoref{thm:diagonal-limit-compatibility-seal-budget-synchronizer-handoff} and \autoref{thm:diagonal-limit-compatibility-tail-schedule-readback}. The shared synchronizer handoff and shared finite window force the reads through the same tail-schedule surface, the same $Q$ readback, and the same $D,L$ seal-facing rows.

Apply \autoref{thm:diagonal-limit-compatibility-seal-budget-route-exhaustion} and \autoref{thm:diagonal-limit-compatibility-real-seal-budget-nonescape}. These theorems exhaust every completion-facing or $\RealUp$ consumer by the displayed package rows and exclude all routes through quotient streams, selected host limits, host equality, independent seals, countable choice, or ambient completeness.

The carrier inventory then identifies the public pullback. Since the two reads agree on the source, handoff, window, readback, dyadic ledger, and Real seal rows, and since the only remaining exported rows are $H,C,P,N$ for transport, route, provenance, and local naming of that same packet, the completion-facing surfaces cannot diverge.
\end{proof}

\begin{theorem}[DiagonalLimitCompatibility completion consumer package]
\label{thm:diagonal-limit-compatibility-completion-consumer-package}
The root-selector budget lock, the seal-budget route exhaustion row, the root-route budget package, and the seal-source synchronization rows assemble the public completion-facing package for any accepted $\DiagonalLimitCompatibilityUp$ packet:
$$
R,T,S,D,W,Q,L,H,C,P,N .
$$
This package exposes the displayed source budget, selector handoff, seal synchronizer, diagonal ledger, window, readback, Real seal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, and it exposes no quotient stream, selected limit, host equality, independent completion object, or ambient completeness principle.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityCompletionConsumerPackage}
\begin{proof}
First combine \autoref{thm:diagonal-limit-compatibility-root-selector-budget-lock} with \autoref{thm:diagonal-limit-compatibility-seal-budget-route-exhaustion}. The first theorem fixes the upstream selector route, and the second theorem exhausts the downstream $\RealUp$ and $\CompletionUp$ seal consumers by the same finite package.

Next use \autoref{thm:diagonal-limit-compatibility-root-route-budget-package}. It supplies the public route through the budget source, mesh budget, triangle bound, stream window, readback, seal route, Real seal, transport, continuation, provenance, and local naming rows.

Use \autoref{thm:diagonal-limit-compatibility-seal-source-synchronization} and \autoref{thm:diagonal-limit-compatibility-seal-budget-synchronizer-handoff}. These rows attach the seal source and the synchronizer to the same route before any completion-facing read can consume the seal.

Finally apply \autoref{thm:diagonal-limit-compatibility-completion-consumer-nonescape}. Since every cited row is a displayed coordinate of the same accepted packet, the assembled package has no route through quotient streams, selected limits, host equality, independent completion data, or ambient completeness.
\end{proof}

\begin{theorem}[DiagonalLimitCompatibility root selector route determinacy]
\label{thm:diagonal-limit-compatibility-root-selector-route-determinacy}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
and
$$
K'=(R',T',S',D',W',Q',L',H',C',P',N')
$$
be accepted $\DiagonalLimitCompatibilityUp$ packets. Suppose the displayed classifier identifies their selector-budget source, $\CauchySealBudgetSynchronizerUp$ handoff, finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\RealUp$ seal row, and structural $H,C,P,N$ rows. Then every $\RealUp$ or $\CompletionUp$ consumer sees the same finite completion-facing package for the two reads. No quotient stream, selected host limit, host real equality, independent seal, countable-choice row, or ambient completeness coordinate can distinguish them.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityRootSelectorRouteDeterminacy}
\begin{proof}
Apply \autoref{thm:diagonal-limit-compatibility-root-selector-budget-lock} to both packets. The lock forces both reads onto the displayed selector prefix before either consumer can inspect the stream window, regular readback, dyadic ledger, or Real seal row.

Next combine \autoref{thm:diagonal-limit-compatibility-root-selector-window-refinement} with \autoref{thm:diagonal-limit-compatibility-seal-budget-route-exhaustion}. The shared classifier data keeps the $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and $\RealUp$ rows on one finite route through the seal-facing package.

Use \autoref{thm:diagonal-limit-compatibility-root-selector-window-pullback-uniqueness}. Since the displayed source, handoff, window, readback, ledger, seal, and structural rows agree, the completion-facing pullback surface is identified for the two reads.

Finally apply \autoref{thm:diagonal-limit-compatibility-completion-consumer-package}. Every $\RealUp$ or $\CompletionUp$ consumer receives only the finite package $R,T,S,D,W,Q,L,H,C,P,N$, so none of the excluded host or completion coordinates can witness a divergence at the public surface.
\end{proof}

\begin{theorem}[DiagonalLimitCompatibility selector-budget lock exactness]
\label{thm:diagonal-limit-compatibility-selector-budget-lock-exactness}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
and
$$
K'=(R',T',S',D',W',Q',L',H',C',P',N')
$$
be accepted $\DiagonalLimitCompatibilityUp$ packets. The root selector-budget lock classifies $K$ and $K'$ by the same finite surface exactly when the displayed selector-budget source, $\CauchySealBudgetSynchronizerUp$ handoff, $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\RealUp$ seal row, and structural $H,C,P,N$ rows agree componentwise:
$$
\begin{aligned}
&K \sim_{\mathrm{root\ selector\ budget}} K'\\
&\Longleftrightarrow
R \sim R'
\text{ and } T \sim T'
\text{ and } S \sim S'
\text{ and } D \sim D'
\text{ and } W \sim W'
\text{ and } Q \sim Q'
\text{ and } L \sim L'
\text{ and } H \sim H'
\text{ and } C \sim C'
\text{ and } P \sim P'
\text{ and } N \sim N' .
\end{aligned}
$$
Thus the displayed selector-budget, handoff, window, readback, ledger, seal, transport, continuation, provenance, and naming rows are the whole classifier surface for the accepted root read.
\end{theorem}
\begin{proof}
For the forward direction, start with \autoref{thm:diagonal-limit-compatibility-root-selector-budget-lock}. The lock prevents the root read from leaving the displayed selector-budget source and structural rows before any downstream consumer inspects the seal-facing coordinates.

Apply \autoref{thm:diagonal-limit-compatibility-root-selector-window-pullback-uniqueness}. The shared classifier surface pulls the finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, and $\RealUp$ seal row back to the same root selector route. Then use \autoref{thm:diagonal-limit-compatibility-seal-budget-route-exhaustion}; every $\RealUp$ or $\CompletionUp$ read is exhausted by the same finite rows, so no omitted host, quotient, choice, or ambient completeness coordinate can supply an additional classifier distinction.

For the reverse direction, assume the displayed selector-budget source, synchronizer handoff, window, readback, ledger, seal row, and structural $H,C,P,N$ rows agree componentwise. Repack these coordinates as the finite route $R,T,S,D,W,Q,L,H,C,P,N$ for both accepted packets.

Finally invoke \autoref{thm:diagonal-limit-compatibility-root-selector-route-determinacy}. The repacked route forces the two accepted reads to present the same completion-facing package to every terminal consumer, so the root selector-budget classifier agrees exactly on the displayed rows.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilitySelectorBudgetLockExactness}

\begin{theorem}[DiagonalLimitCompatibility selector-budget classifier pullback]
\label{thm:diagonal-limit-compatibility-selector-budget-classifier-pullback}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
and
$$
K'=(R',T',S',D',W',Q',L',H',C',P',N')
$$
be accepted $\DiagonalLimitCompatibilityUp$ root-selector reads. They induce the same completion-facing public classifier exactly when their selector budget, $\CauchySealBudgetSynchronizerUp$ handoff, $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\RealUp$ seal, and structural $H,C,P,N$ rows agree componentwise.

The classifier has no coordinate for quotient stream equality, selected host limits, host real equality, independent seals, countable-choice rows, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilitySelectorBudgetClassifierPullback}
\begin{proof}
For the forward direction, use \autoref{thm:diagonal-limit-compatibility-root-selector-budget-lock}. The accepted root read is locked to the displayed selector-budget source before the synchronizer, stream window, readback, ledger, seal, and structural rows are consumed.

Apply the pullback uniqueness theorem, \autoref{thm:diagonal-limit-compatibility-root-selector-window-pullback-uniqueness}. It identifies the finite StreamName window, RegSeqRat readback, DyadicRatCore ledger, and RealUp seal with the same selector route whenever the displayed root surface agrees.

For the reverse direction, assume componentwise agreement of the listed rows. The selector-budget lock exactness theorem repacks those coordinates as the finite route $R,T,S,D,W,Q,L,H,C,P,N$ for both reads, and \autoref{thm:diagonal-limit-compatibility-completion-consumer-package} is then the completion-facing package seen by terminal consumers.

Finally use \autoref{thm:diagonal-limit-compatibility-completion-consumer-nonescape}. Since quotient streams, selected host limits, host real equality, independent seals, choice rows, and ambient completeness are absent from the exported package, none of them can distinguish the two accepted classifier surfaces.
\end{proof}

\begin{theorem}[DiagonalLimitCompatibility selector-budget terminal pullback]
\label{thm:diagonal-limit-compatibility-selector-budget-terminal-pullback}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
and
$$
K'=(R',T',S',D',W',Q',L',H',C',P',N')
$$
be accepted $\DiagonalLimitCompatibilityUp$ packets. Suppose the displayed classifier identifies their selector-budget source, $\CauchySealBudgetSynchronizerUp$ handoff, finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\RealUp$ seal row, and structural $H,C,P,N$ rows componentwise. Then every terminal $\RealUp$ or $\CompletionUp$ consumer reads the same finite public pullback package
$$
R,T,S,D,W,Q,L,H,C,P,N .
$$
The terminal surface cannot distinguish $K$ from $K'$ by quotient stream equality, selected host limits, host real equality, independent seals, countable-choice rows, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilitySelectorBudgetTerminalPullback}
\begin{proof}
Start with \autoref{thm:diagonal-limit-compatibility-selector-budget-classifier-pullback}. The assumed componentwise agreement is exactly the classifier surface for the accepted packets, so the two reads present one completion-facing public classifier before any terminal consumer inspects the seal rows.

Next use \autoref{thm:diagonal-limit-compatibility-root-selector-window-pullback-uniqueness}. The shared finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\RealUp$ seal, and structural rows pull both packets back to the same root-selector surface.

Now apply \autoref{thm:diagonal-limit-compatibility-completion-consumer-package}. Its exported consumer data is precisely the finite package $R,T,S,D,W,Q,L,H,C,P,N$, so the public $\RealUp$ and $\CompletionUp$ reads receive the same pullback package from $K$ and $K'$.

Finally invoke \autoref{thm:diagonal-limit-compatibility-completion-consumer-nonescape}. Quotient stream equality, selected host limits, host real equality, independent seals, countable-choice rows, and ambient completeness are not exported coordinates of that package. Hence no terminal $\RealUp$ or $\CompletionUp$ consumer can separate the accepted packets at the public surface.
\end{proof}

\begin{theorem}[DiagonalLimitCompatibility terminal readback determinacy]
\label{thm:diagonal-limit-compatibility-terminal-readback-determinacy}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
and
$$
K'=(R',T',S',D',W',Q',L',H',C',P',N')
$$
be accepted $\DiagonalLimitCompatibilityUp$ root-selector reads. Suppose the displayed classifier identifies their selector-budget source, $\CauchySealBudgetSynchronizerUp$ handoff, finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\RealUp$ seal row, and structural $H,C,P,N$ rows. Then every $\RealUp$, $\CompletionUp$, or $\CauchyLimitSealUp$ terminal read receives the same finite readback package
$$
R,T,S,D,W,Q,L,H,C,P,N .
$$
No terminal consumer can distinguish the two reads by quotient streams, selected host limits, host real equality, countable-choice data, or ambient completeness.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibility\_terminal\_readback\_determinacy}
\begin{proof}
First apply \autoref{thm:diagonal-limit-compatibility-root-selector-route-determinacy}. It identifies the two accepted root-selector reads on the displayed selector budget, synchronizer handoff, finite window, regular readback, dyadic ledger, Real seal row, and structural transport, route, provenance, and naming rows.

Next use \autoref{thm:diagonal-limit-compatibility-root-selector-window-pullback-uniqueness}. The shared window, readback, ledger, and seal rows pull both reads back to the same finite completion-facing surface, so the terminal read cannot obtain a second public package by changing the window or readback representative.

Apply \autoref{thm:diagonal-limit-compatibility-seal-budget-route-exhaustion}. The $\RealUp$ and $\CompletionUp$ terminal consumers are exhausted by the same route through $R,T,S,D,W,Q,L,H,C,P,N$, and the $\CauchyLimitSealUp$ boundary is already part of that seal-facing route.

Finally invoke \autoref{thm:diagonal-limit-compatibility-completion-consumer-package}. The only exported readback package is the displayed finite package, so quotient streams, selected host limits, host real equality, countable-choice data, and ambient completeness have no coordinate by which to separate the two terminal reads.
\end{proof}

\begin{theorem}[DiagonalLimitCompatibility selector window tail seal bridge]
\label{thm:diagonal-limit-compatibility-selector-window-tail-seal-bridge}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
and
$$
K'=(R',T',S',D',W',Q',L',H',C',P',N')
$$
be accepted $\DiagonalLimitCompatibilityUp$ root-selector reads. Suppose the displayed classifier identifies their selector-budget source, $\CauchySealBudgetSynchronizerUp$ handoff, finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, tail-schedule readback whenever a terminal tail row is present, $\CauchyLimitSealUp$ boundary, $\RealUp$ seal row, and structural $H,C,P,N$ rows. Then every $\RealUp$, $\CompletionUp$, and $\CauchyLimitSealUp$ terminal consumer receives one finite terminal package
$$
R,T,S,D,W,Q,L,H,C,P,N .
$$
The selector window, synchronizer handoff, tail schedule, Cauchy limit boundary, and Real seal cannot be read as separate terminal classifier coordinates.
\end{theorem}
\begin{proof}
First apply \autoref{thm:diagonal-limit-compatibility-root-selector-window-refinement}. The selector-budget source and synchronizer handoff place the finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, and $\RealUp$ seal row on the same displayed root route.

Next use \autoref{thm:diagonal-limit-compatibility-selector-lock-seal-route-exhaustion}. This makes the seal-facing route exhaustive for the accepted packets, so the $\CauchyLimitSealUp$ boundary and any present tail-schedule readback are consumed through the same finite route instead of a separate terminal channel.

Apply \autoref{thm:diagonal-limit-compatibility-root-selector-window-pullback-uniqueness}. Since the selector window, readback, ledger, seal, boundary, and structural rows agree, both terminal reads pull back to one completion-facing surface.

Now invoke \autoref{thm:diagonal-limit-compatibility-terminal-readback-determinacy}. The terminal readback for $\RealUp$, $\CompletionUp$, and $\CauchyLimitSealUp$ consumers is already the displayed package $R,T,S,D,W,Q,L,H,C,P,N$.

Finally use \autoref{thm:diagonal-limit-compatibility-completion-consumer-package}. Its consumer inventory exports exactly that finite package, so the tail schedule, limit-seal boundary, Real seal row, and completion consumer receive the same terminal data.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilitySelectorWindowTailSealBridge}

\begin{theorem}[DiagonalLimitCompatibility terminal tail-rebase exactness]
\label{thm:diagonal-limit-compatibility-terminal-tail-rebase-exactness}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
and
$$
K'=(R',T',S',D',W',Q',L',H',C',P',N')
$$
be accepted $\DiagonalLimitCompatibilityUp$ root-selector reads. Suppose the displayed classifier identifies their selector-budget source, $\CauchySealBudgetSynchronizerUp$ handoff, finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\RealUp$ seal row, and structural $H,C,P,N$ rows. Then every terminal read obtained after a tail rebase exposes exactly the same finite package
$$
R,T,S,D,W,Q,L,H,C,P,N .
$$
The tail rebase has no coordinate for quotient streams, selected host limits, host real equality, countable-choice data, or ambient completeness.
\end{theorem}
\begin{proof}
First apply \autoref{thm:diagonal-limit-compatibility-terminal-readback-determinacy}. The shared selector-budget source, synchronizer handoff, window, readback, ledger, seal row, and structural rows already determine the finite terminal readback package for $\RealUp$, $\CompletionUp$, and $\CauchyLimitSealUp$ consumers.

Next use \autoref{thm:diagonal-limit-compatibility-selector-budget-lock-exactness}. This identifies the classifier equality with equality of the displayed finite rows, so a tail rebase cannot introduce another selector-budget source, handoff, window, readback, ledger, seal, or structural row at the terminal boundary.

Apply \autoref{thm:diagonal-limit-compatibility-root-selector-window-pullback-uniqueness}. The finite $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, and $\RealUp$ seal row pull the rebased read back to the same root selector surface.

Finally invoke \autoref{thm:diagonal-limit-compatibility-completion-consumer-package}. The exported consumer package is exactly $R,T,S,D,W,Q,L,H,C,P,N$, and the package inventory omits quotient streams, selected host limits, host equality, countable choice, and ambient completeness. Therefore the tail rebase has no public coordinate by which to change the terminal package.
\end{proof}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityTerminalTailRebaseExactness}

\begin{theorem}[DiagonalLimitCompatibility selector-budget terminal classifier]
\label{thm:diagonal-limit-compatibility-selector-budget-terminal-classifier}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
be an accepted $\DiagonalLimitCompatibilityUp$ root-selector read. The terminal classifier exported to $\RealUp$, $\CompletionUp$, and $\CauchyLimitSealUp$ consumers is the single displayed package whose source is the selector-budget row $R$, whose finite window is the $\StreamNameUp$ row $S$, whose rational readback is the $\RegSeqRatUp$ row $T$, whose dyadic ledger is the $\DyadicRatCoreUp$ row $D$, whose synchronizer handoff is the $\CauchySealBudgetSynchronizerUp$ row $Q$, and whose seal-facing endpoint is the $\RealUp$ row $L$. No terminal consumer may read these rows as independent classifier coordinates.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilitySelectorBudgetTerminalClassifier}
\begin{proof}
First apply \autoref{thm:diagonal-limit-compatibility-root-selector-window-refinement}. It places the selector-budget source, finite stream window, regular rational readback, dyadic ledger, synchronizer handoff, and Real seal on one root-selector route.

Next use \autoref{thm:diagonal-limit-compatibility-selector-budget-lock-exactness}. The classifier equality is equality of the displayed finite rows, so the terminal classifier cannot split $R,T,S,D,Q,L$ into unrelated coordinates.

Apply \autoref{thm:diagonal-limit-compatibility-terminal-readback-determinacy}. The terminal readback for $\RealUp$, $\CompletionUp$, and $\CauchyLimitSealUp$ consumers is already the package $R,T,S,D,W,Q,L,H,C,P,N$.

Finally invoke \autoref{thm:diagonal-limit-compatibility-completion-consumer-package}. It exports the same finite package to completion-facing readers, so the terminal classifier is the displayed selector-budget package rather than a host-side limit selector.
\end{proof}

\begin{theorem}[DiagonalLimitCompatibility terminal synchronizer classifier]
\label{thm:diagonal-limit-compatibility-terminal-synchronizer-classifier}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
be an accepted $\DiagonalLimitCompatibilityUp$ root-selector read. The terminal classifier exported from $K$ to $\RealUp$, $\CompletionUp$, and $\CauchyLimitSealUp$ is the single finite L10 package whose selector-budget source is $R$, whose synchronizer handoff is $Q$, whose finite $\StreamNameUp$ window is $S$, whose $\RegSeqRatUp$ readback is $T$, whose $\DyadicRatCoreUp$ ledger is $D$, whose $\CauchyLimitSealUp$ boundary is $W$, whose $\RealUp$ seal is $L$, and whose structural rows are $H,C,P,N$.

The synchronizer handoff and the Cauchy-limit boundary therefore agree componentwise with the same displayed package read by the Real and completion consumers. No quotient stream, selected host limit, countable-choice row, ambient completeness coordinate, or second terminal classifier participates in the exported surface.
\end{theorem}
\leanchecked{BEDC.Derived.DiagonallimitcompatibilityUp.DiagonalLimitCompatibilityTerminalSynchronizerClassifier}
\begin{proof}
Begin with \autoref{thm:diagonal-limit-compatibility-selector-budget-terminal-classifier}. It identifies the terminal classifier as the displayed selector-budget package rather than as independent Real, completion, or limit-seal coordinates.

Apply \autoref{thm:diagonal-limit-compatibility-selector-window-tail-seal-bridge}. The finite window, regular readback, dyadic ledger, synchronizer handoff, Cauchy-limit boundary, Real seal, and structural rows are read through the same terminal package.

Next use \autoref{thm:diagonal-limit-compatibility-terminal-readback-determinacy}. Once those rows are componentwise fixed, every $\RealUp$, $\CompletionUp$, and $\CauchyLimitSealUp$ terminal read receives $R,T,S,D,W,Q,L,H,C,P,N$ and no alternate readback surface.

Finally invoke \autoref{thm:diagonal-limit-compatibility-real-completion-route-nonescape}. The route inventory omits quotient streams, selected host limits, countable choice, and ambient completeness, so the synchronizer and Cauchy-limit rows remain bound to the same finite L10 package exported by $K$.
\end{proof}

\begin{theorem}[DiagonalLimitCompatibility Real completion route non-escape]
\label{thm:diagonal-limit-compatibility-real-completion-route-nonescape}
Every $\DiagonalLimitCompatibilityUp$ route from a selector-budget root read to a $\RealUp$ or $\CompletionUp$ consumer factors through the finite $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchySealBudgetSynchronizerUp$, and $\CauchyLimitSealUp$ rows displayed in
$$
K=(R,T,S,D,W,Q,L,H,C,P,N).
$$
The route has no coordinate for a host-selected real limit, quotient stream equality, countable-choice data, ambient completeness, or a completion consumer that bypasses the selector-budget classifier.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:diagonal-limit-compatibility-selector-budget-terminal-classifier}. The terminal classifier presented to the Real and completion consumers is the single finite selector-budget package.

Use \autoref{thm:diagonal-limit-compatibility-terminal-tail-rebase-exactness}. A tail rebase preserves the same finite package and does not create quotient streams, selected host limits, host real equality, countable-choice data, or ambient completeness.

Next apply \autoref{thm:diagonal-limit-compatibility-selector-lock-seal-route-exhaustion}. The seal-facing route is exhausted by the displayed stream, rational, dyadic, synchronizer, limit-seal, and Real rows.

Finally invoke \autoref{thm:diagonal-limit-compatibility-completion-consumer-package}. Completion consumers receive only the displayed package, so any route bypassing the selector-budget classifier is outside the accepted $\DiagonalLimitCompatibilityUp$ surface.
\end{proof}

\begin{theorem}[DiagonalLimitCompatibility tail-seal synchronizer pullback]
\label{thm:diagonal-limit-compatibility-tail-seal-synchronizer-pullback}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
and
$$
K'=(R',T',S',D',W',Q',L',H',C',P',N')
$$
be accepted $\DiagonalLimitCompatibilityUp$ root-selector reads whose terminal classifiers agree on selector budget, $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, $\CauchySealBudgetSynchronizerUp$ handoff, $\CauchyLimitSealUp$ boundary, $\RealUp$ seal, and structural rows. Then their tail-seal synchronizer reads pull back to the same finite selector-budget package, so every $\CauchySealBudgetSynchronizerUp$ and $\RealUp$ consumer reads the same terminal package
$$
R,T,S,D,W,Q,L,H,C,P,N .
$$
\end{theorem}
\begin{proof}
First use \autoref{thm:diagonal-limit-compatibility-selector-budget-terminal-classifier}. Agreement of the terminal classifiers is agreement of the displayed selector-budget, stream, rational, dyadic, synchronizer, limit-seal, Real, and structural rows.

Apply \autoref{thm:diagonal-limit-compatibility-root-selector-window-pullback-uniqueness}. The common $\StreamNameUp$ window, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ ledger, and $\RealUp$ seal pull both reads back to one root-selector surface.

Next invoke \autoref{thm:diagonal-limit-compatibility-terminal-readback-determinacy}. The terminal readback is determined by that shared finite package for Real, completion, and limit-seal consumers.

Finally use \autoref{thm:diagonal-limit-compatibility-terminal-tail-rebase-exactness}. Tail rebase preserves the terminal package, so the synchronizer handoff and seal read cannot diverge after the pullback.
\end{proof}

\begin{theorem}[DiagonalLimitCompatibility selector-budget common-refinement terminality]
\label{thm:diagonal-limit-selector-budget-common-refinement-terminality}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
be an accepted $\DiagonalLimitCompatibilityUp$ root-selector read. Suppose its selector-budget row agrees simultaneously with the $\CauchyCriterionUp$ selector-seal route and with the $\UniformCauchyCriterionUp$ finite-family Real/Completion window. Then the common refinement has a unique terminal finite package through $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchySealBudgetSynchronizerUp$, $\CauchyLimitSealUp$, and $\RealUp$. No quotient stream, selected host limit, countable-choice row, ambient completeness assertion, or independent completion object can distinguish the consumer route.
\end{theorem}
\begin{proof}
Begin with the local selector-budget terminal classifier of \autoref{thm:diagonal-limit-compatibility-selector-budget-terminal-classifier}. It identifies the terminal classifier as the single displayed selector-budget package rather than as a family of independent Real, completion, or limit-seal coordinates.

Use the tail-seal synchronizer pullback of \autoref{thm:diagonal-limit-compatibility-tail-seal-synchronizer-pullback}. Agreement of the selector budget with the synchronizer and seal rows pulls the $\CauchySealBudgetSynchronizerUp$ and $\RealUp$ consumer reads back to the same finite root-selector package.

Now read the cross-chapter inputs through their displayed routes. The $\CauchyCriterionUp$ diagonal selector terminality supplies the same selector-seal route, while the $\UniformCauchyCriterionUp$ Real-completion window exhaustion supplies the finite-family window consumed by Real and completion readers.

Finally apply the local Real completion route non-escape theorem, \autoref{thm:diagonal-limit-compatibility-real-completion-route-nonescape}. Since every accepted Real or completion consumer route factors through the displayed stream, rational, dyadic, synchronizer, limit-seal, and Real rows, there is no remaining coordinate by which a quotient stream, host-selected limit, choice row, ambient completeness assertion, or independent completion object can separate the common-refinement route.
\end{proof}

\begin{theorem}[DiagonalLimitCompatibility criterion-uniform seal pullback]
\label{thm:diagonal-limit-compatibility-criterion-uniform-seal-pullback}
Let
$$
K=(R,T,S,D,W,Q,L,H,C,P,N)
$$
be an accepted $\DiagonalLimitCompatibilityUp$ root-selector read. Suppose the selector-budget row of $K$ agrees with the $\CauchyCriterionUp$ selector-seal route and also agrees with the $\UniformCauchyCriterionUp$ finite-family Real/Completion window. Then the two consumers pull back to the same finite seal package through $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\CauchySealBudgetSynchronizerUp$, $\CauchyLimitSealUp$, and $\RealUp$.

The shared package is exactly the displayed $R,T,S,D,W,Q,L,H,C,P,N$ read of $K$. No quotient stream, selected host limit, countable-choice row, ambient completeness assertion, or independent completion object separates the CauchyCriterion and UniformCauchyCriterion consumers.
\end{theorem}
\begin{proof}
Start with the local terminal classifier, \autoref{thm:diagonal-limit-compatibility-selector-budget-terminal-classifier}. It identifies the selector-budget read of $K$ with the single displayed package rather than with separate criterion, uniform-family, Real, or completion coordinates.

Next apply the tail-seal synchronizer pullback, \autoref{thm:diagonal-limit-compatibility-tail-seal-synchronizer-pullback}. The common selector-budget row carries the $\CauchySealBudgetSynchronizerUp$, $\CauchyLimitSealUp$, and $\RealUp$ reads back to the same finite package.

Now use common-refinement terminality, \autoref{thm:diagonal-limit-selector-budget-common-refinement-terminality}. The $\CauchyCriterionUp$ selector-seal route and the $\UniformCauchyCriterionUp$ finite-family Real/Completion window therefore meet at the same terminal refinement through the stream, rational, dyadic, synchronizer, limit-seal, and Real rows.

Finally invoke Real completion route non-escape, \autoref{thm:diagonal-limit-compatibility-real-completion-route-nonescape}. Since the accepted route inventory has no coordinate for quotient streams, host-selected limits, choice rows, ambient completeness, or detached completion objects, the two criterion consumers have no public path away from the displayed seal package.
\end{proof}
