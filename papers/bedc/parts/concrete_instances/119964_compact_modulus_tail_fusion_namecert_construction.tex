\chapter{A Concrete Naming Certificate for $\CompactModulusTailFusionUp$}
\label{ch:concrete-instances-compact-modulus-tail-fusion-namecert}
\origin{human}

Following the Bishop--Bridges finite-net proof of compact uniform continuity, the $\CompactModulusTailFusionUp$ packet records the ledger that fuses a compact-source uniform modulus with the tail-window rows later consumed by Real and Completion arguments. It realizes the route of \autoref{ch:visions-compact-uniform-continuity} through the compact modulus surface of \autoref{ch:concrete-instances-compact-uniform-modulus-namecert}, the downstream $\UniformModulusUp$ handoff of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite fusion row, not a host modulus function, selected cover center, quotient stream, countable-choice schedule, or ambient compactness theorem.

\begin{definition}[Compact modulus tail fusion carrier]
\label{def:compact-modulus-tail-fusion-carrier}
A $\CompactModulusTailFusionUp$ carrier is a finite $\BHist$ packet
$$
F=(K,U,T,W,R,E,H,C,P,L,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactUniformModulusUp$ row produced from compact finite-net and pointwise-modulus data.
\item $U$ is the $\UniformModulusUp$ consumer row extracted from the compact modulus surface.
\item $T$ is the shared tail-window ledger naming the finite sequence tail on which $U$ is used.
\item $W$ is the $\StreamNameUp$ finite-window read attached to that shared tail ledger.
\item $R$ is the $\RegSeqRatUp$ readback row for the window values and uniform-modulus tolerance data.
\item $E$ is the terminal $\RealUp$ seal row available only after $K,U,T,W,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the compact modulus, uniform-modulus handoff, tail ledger, window read, regular readback, Real seal, replay, provenance, fusion ledger, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the compact modulus row through the uniform-modulus handoff, tail window, StreamName read, RegSeqRat readback, and Real seal.
\item $P$ is the $\Pkg$ provenance over $\CompactModulusTailFusionUp$, $\CompactUniformModulusUp$, $\UniformModulusUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local fusion ledger recording that the compact modulus row and the tail-window readback are consumed in one route.
\item $N$ is the local $\NameCert_{\CompactModulusTailFusionUp}$ row.
\end{enumerate}
The carrier has no coordinate for a host modulus selector, an unrecorded compact cover center, a quotient stream, countable choice, trichotomy, selected limits, or ambient completeness.
\end{definition}

\begin{theorem}[Compact modulus tail fusion obligations]
\label{thm:compact-modulus-tail-fusion-obligations}
For every accepted $\CompactModulusTailFusionUp$ carrier
$$
F=(K,U,T,W,R,E,H,C,P,L,N),
$$
the local $\NameCert_{\CompactModulusTailFusionUp}$ surface consists exactly of carrier admission, compact-modulus exposure through $K$, uniform-modulus handoff through $U$, shared tail-window ledgering through $T$, finite StreamName readback through $W$, RegSeqRat readback through $R$, Real-seal exposure through $E$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, $\Pkg$ provenance through $P$, fusion ledgering through $L$, local naming through $N$, and nonescape for host modulus selectors, hidden compact centers, quotient streams, countable choice, trichotomy, selected limits, and ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-modulus-tail-fusion-carrier}. The compact analytic source is $K$, its tolerance-only handoff is $U$, and the tail-facing consumer rows are $T,W,R,E$.

Read the compact modulus before the tail data. The row $K$ supplies the finite compact-uniform route, and $U$ is the displayed uniform-modulus consumer row, so the later tail window cannot replace it with a host selector or private modulus.

Read the tail rows next. The ledger $T$, window $W$, and regular readback $R$ bind the finite sequence tail before $E$ is exposed as a Real seal.

Finally $H,C,P,L,N$ transport, replay, source, ledger, and name the same route. Since the carrier lacks hidden compact centers, quotient streams, countable choice, trichotomy, selected limits, and ambient completeness, the listed rows exhaust the local NameCert surface.
\end{proof}

\closureat{CompactModulusTailFusionUp}{seedStr}

\begin{closurestatus}{\CompactModulusTailFusionUp}
  \constructivestory{A $\CompactModulusTailFusionUp$ packet is generated from a compact-uniform modulus row, a UniformModulus handoff, a shared tail-window ledger, finite StreamName readback, RegSeqRat readback, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, a fusion ledger, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-modulus-tail-fusion-carrier} and \autoref{thm:compact-modulus-tail-fusion-obligations} seed the finite fusion route from compact modulus rows to tail-window Real readback.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, bridge checking, host modulus selectors, hidden compact centers, quotient streams, countable choice, trichotomy, selected limits, or ambient completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, compact-modulus exposure, uniform-modulus handoff, tail-window readback, Real-seal exposure, fusion ledgering, transport, replay, provenance, and local naming for BEDC.Derived.CompactModulusTailFusionUp.}
\end{closurestatus}
