\chapter{A Concrete Naming Certificate for $\KleeneKreiselContinuousFunctionalUp$}
\label{ch:concrete-instances-kleene-kreisel-continuous-functional-namecert}
\origin{human}

Following Kleene and Kreisel on continuous functionals over choice-sequence spaces, the $\KleeneKreiselContinuousFunctionalUp$ packet records a higher-type finite-window interface for functionals read from countable names. It sits over the finite observation discipline of \autoref{ch:concrete-instances-streamname-namecert}, the binary prefix surface of \autoref{ch:concrete-instances-cantorspace-namecert}, the bounded modulus handoff of \autoref{ch:concrete-instances-fanfunctional-namecert}, the neighborhood table of \autoref{ch:concrete-instances-neighborhood-function-namecert}, and the domain-facing readback of \autoref{ch:concrete-instances-scott-continuous-namecert}. The packet is grounded in finite BEDC rows and displayed request ledgers, not in host function equality, an ambient exponential object, or a completed space of all functionals.

\begin{definition}[KleeneKreiselContinuousFunctional carrier]
\label{def:kleene-kreisel-continuous-functional-carrier}
A $\KleeneKreiselContinuousFunctionalUp$ carrier is a finite $\BHist$ packet
$$
K=(S,C,F,Nb,Sc,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ source row, exposing the finite request windows through which countable names are inspected.
\item $C$ is the $\CantorSpaceUp$ binary prefix row used when the functional read is restricted to a binary choice-sequence window.
\item $F$ is the $\FanFunctionalUp$ modulus row, displaying the bounded prefix length needed before an output row may be read on compact binary windows.
\item $Nb$ is the $\NeighborhoodFunctionUp$ table row, recording finite input-prefix requests and the output rows released by those requests.
\item $Sc$ is the $\ScottContinuousUp$ readback row, recording the directed-observation and Scott-open replay surface through which the same finite table is read as a continuity packet.
\item $H$ records componentwise $\hsame$ transport for the stream, Cantor, fan, neighborhood, Scott-continuity, replay, provenance, and naming rows.
\item $T$ records displayed $\Cont$ replay from every finite name request through $S,C,F,Nb,Sc,H$ before an output row is exposed.
\item $P$ is the $\Pkg$ provenance over $\KleeneKreiselContinuousFunctionalUp$, $\StreamNameUp$, $\CantorSpaceUp$, $\FanFunctionalUp$, $\NeighborhoodFunctionUp$, $\ScottContinuousUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\KleeneKreiselContinuousFunctionalUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through the displayed rows $S,C,F,Nb,Sc,H,T,P,N$. It has no coordinate for host equality of higher-type functions, an arbitrary map from all streams to all values, countable choice, a selected infinite path, or a semantic function-space quotient.
\end{definition}

\begin{theorem}[KleeneKreiselContinuousFunctional NameCert obligations]
\label{thm:kleene-kreisel-continuous-functional-namecert-obligations}
For every accepted $\KleeneKreiselContinuousFunctionalUp$ carrier
$$
K=(S,C,F,Nb,Sc,H,T,P,N),
$$
the local $\NameCert_{\KleeneKreiselContinuousFunctionalUp}$ surface consists exactly of carrier admission, finite-window classifier exposure, neighborhood-function transport through $Nb$, fan-functional modulus handoff through $F$, Scott-continuity readback through $Sc$, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, local naming through $N$, and non-escape from those displayed rows. Every accepted read must expose the $\StreamNameUp$ and $\CantorSpaceUp$ request rows before it may consume the neighborhood table, fan modulus, or Scott-continuity readback.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:kleene-kreisel-continuous-functional-carrier}. The source name windows are $S$, the binary prefix face is $C$, the compact modulus face is $F$, the finite neighborhood table is $Nb$, and the domain-continuity readback is $Sc$.

The functional read is finite at each public use. The neighborhood row $Nb$ lists the input-prefix requests that release an output row, while the fan row $F$ records the bounded prefix length needed on the Cantor-space face. These rows are classifier data in the BEDC packet, not host function equality or an order on completed streams.

The Scott row $Sc$ reads the same table through directed-observation and Scott-open replay. Componentwise transport through $H$ and displayed continuation replay through $T$ preserve the rows already displayed; they do not create a second functional source.

Finally $P$ and $N$ package and name the same finite route. Since the carrier contains no host exponential, no stream quotient, no selected infinite path, and no countable-choice operator, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[KleeneKreiselContinuousFunctional neighborhood handoff]
\label{thm:kleene-kreisel-continuous-functional-neighborhood-handoff}
Every accepted $\KleeneKreiselContinuousFunctionalUp$ read factors through the displayed $\NeighborhoodFunctionUp$ row and the $\StreamNameUp$ finite request rows before any output read is available. More precisely, for a carrier
$$
K=(S,C,F,Nb,Sc,H,T,P,N),
$$
any public output row must first display the StreamName request window in $S$, the corresponding neighborhood-table entry in $Nb$, and the continuation replay row in $T$. On binary choice-sequence reads the same handoff may additionally use the $\CantorSpaceUp$ row $C$ and the $\FanFunctionalUp$ modulus row $F$; on domain-facing reads it may use the $\ScottContinuousUp$ row $Sc$. No output row is admitted from a host functional, a hidden infinite stream, or a detached equality proof.
\end{theorem}
\begin{proof}
Start from an accepted public output read. By \autoref{def:kleene-kreisel-continuous-functional-carrier}, the only rows that can expose an output are the finite request row $S$, the neighborhood table $Nb$, and the displayed continuation row $T$.

Use \autoref{thm:kleene-kreisel-continuous-functional-namecert-obligations}. It forces every accepted read through carrier admission, finite-window classifier exposure, neighborhood transport, continuation replay, provenance, and local naming before the output row is treated as public data.

For the binary face, the Cantor row $C$ supplies the finite prefix window and the fan row $F$ supplies the bounded modulus handoff. For the domain face, the Scott row $Sc$ replays the same table as a continuity read. In both cases the consumer still reaches the output through $S,Nb,T$ rather than through an ambient higher-type map.

Rows $H,P,N$ transport, source, and name this handoff. Because host function equality, hidden infinite streams, selected paths, countable choice, and semantic function-space quotients are absent from $K$, no other output source remains.
\end{proof}

\falsifiablePrediction{An accepted $\KleeneKreiselContinuousFunctionalUp$ read is refuted if a consumer obtains an output row without displaying the StreamName request window, NeighborhoodFunction table entry, continuation replay, componentwise hsame transport, Pkg provenance over StreamNameUp, CantorSpaceUp, FanFunctionalUp, NeighborhoodFunctionUp, ScottContinuousUp, and local naming row.}

\independenceWitness{streamname, cantorspace, fanfunctional, neighborhood-function, scott-continuous: $\KleeneKreiselContinuousFunctionalUp$ is not bijective to any listed sibling because it jointly stores source-name requests, binary prefix restriction, compact modulus handoff, a neighborhood table, Scott-continuity readback, transport, replay, provenance, and local naming in one higher-type finite-window packet.}

\closureat{KleeneKreiselContinuousFunctionalUp}{seedStr}
\begin{closurestatus}{\KleeneKreiselContinuousFunctionalUp}
  \constructivestory{A $\KleeneKreiselContinuousFunctionalUp$ packet is generated from StreamName request windows, CantorSpace prefix rows, FanFunctional modulus rows, NeighborhoodFunction table rows, ScottContinuous readback, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:kleene-kreisel-continuous-functional-carrier}, \autoref{thm:kleene-kreisel-continuous-functional-namecert-obligations}, and \autoref{thm:kleene-kreisel-continuous-functional-neighborhood-handoff} seed the finite-window interface for continuous higher-type functional reads over named streams.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host function spaces, quotient equality of functionals, selected infinite paths, countable choice, total stream maps, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, neighborhood transport, fan modulus handoff, Scott-continuity readback, transport, replay, provenance, local naming, and a concrete Lean target for BEDC.Derived.KleeneKreiselContinuousFunctionalUp.}
\end{closurestatus}
