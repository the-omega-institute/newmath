\chapter{A Concrete Naming Certificate for $\CompactUniformModulusRestrictionUp$}
\label{ch:concrete-instances-compact-uniform-modulus-restriction-namecert}
\origin{human}

Following Bishop--Bridges compact metric analysis, the $\CompactUniformModulusRestrictionUp$ packet records the finite surface on which compact-uniform modulus data are restricted to a displayed subdomain and a displayed codomain read without importing open-cover compactness or a host function-equality principle. It sits beside the compact-uniform-continuity route of \autoref{ch:concrete-instances-compactuniformcontinuity-namecert}, the uniform-modulus ledger of \autoref{ch:concrete-instances-uniformmodulus-namecert}, the compact-metric source of \autoref{ch:concrete-instances-compactmetric-namecert}, the continuous-map carrier of \autoref{ch:concrete-instances-continuousmap-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names only the restriction carrier and its NameCert obligations; it does not assert an external modulus selector, a host equality of functions, an open-cover compactness theorem, quotient real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Compact uniform modulus restriction carrier]
\label{def:compact-uniform-modulus-restriction-carrier}
A $\CompactUniformModulusRestrictionUp$ carrier is a finite $\BHist$ packet
$$
R=(K,F,U,D,W,M,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $K$ is the $\CompactMetricUp$ source row whose finite compact-metric observations name the inspected domain window.
\item $F$ is the $\ContinuousMapUp$ row for the displayed map on that source window, including only graph reads and pointwise continuity data already admitted by the local classifier.
\item $U$ is the incoming $\UniformModulusUp$ row, recording the source-radius response for each displayed target tolerance request.
\item $D$ is the restriction-domain row, naming the finite subdomain or finite compact-window inclusion on which the restricted modulus may be read.
\item $W$ is the restricted source-window row, recording the compact observations from $K$ that remain visible after $D$ is applied.
\item $M$ is the outgoing restricted $\UniformModulusUp$ row, obtained by reading $U$ only through $D,W,F$.
\item $H$ records componentwise $\hsame$ transport for the compact source, continuous-map data, incoming modulus, restriction-domain row, restricted window, outgoing modulus, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
K,F,U,D,W \longmapsto M .
$$
\item $P$ is the $\Pkg$ provenance over $\CompactUniformModulusRestrictionUp$, $\CompactUniformContinuityUp$, $\UniformModulusUp$, $\CompactMetricUp$, $\ContinuousMapUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CompactUniformModulusRestrictionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $K,F,U,D,W,M,H,C,P,N$. It has no coordinate for open-cover compactness, host function equality, a selected external modulus, quotient equality of real names, ambient real completeness, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Compact uniform modulus restriction NameCert obligations]
\label{thm:compact-uniform-modulus-restriction-namecert-obligations}
For every accepted $\CompactUniformModulusRestrictionUp$ carrier
$$
R=(K,F,U,D,W,M,H,C,P,N),
$$
the local $\NameCert_{\CompactUniformModulusRestrictionUp}$ surface consists exactly of compact-metric source exposure, continuous-map visibility, incoming uniform-modulus exposure, restriction-domain admission, restricted-window exposure, outgoing restricted-modulus production, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. A consumer therefore reads a restricted compact-uniform modulus only through the finite route
$$
\CompactMetricUp,\ \ContinuousMapUp,\ \UniformModulusUp,\ \CompactUniformContinuityUp,\ \RealUp,\ \BHist,\ \hsame,\ \Cont,\ \Pkg,\ \NameCert .
$$
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:compact-uniform-modulus-restriction-carrier}. The compact source row $K$, continuous-map row $F$, and incoming modulus row $U$ are the only upstream analytic rows.

Read the restriction data through $D$ and $W$. The restricted window $W$ is a displayed subpacket of the compact observations already present in $K$, and the outgoing modulus $M$ is read from $U$ only after $D,W,F$ remain visible.

Replay the structural rows $H,C,P,N$. They transport, replay, source, and name the same finite route from compact-uniform continuity to the restricted modulus surface.

Since the carrier has no coordinate for open-cover compactness, host function equality, an external modulus selector, quotient real equality, or ambient $\RealUp$ completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\falsifiablePrediction{An accepted $\CompactUniformModulusRestrictionUp$ read is refuted if a restricted-modulus consumer exports $M$ without first displaying the compact source row $K$, continuous-map row $F$, incoming modulus row $U$, restriction-domain row $D$, restricted-window row $W$, and the structural rows $H,C,P,N$.}

\independenceWitness{compact-uniform-continuity, uniformmodulus, compactmetric, continuousmap, real: none is bijective to $\CompactUniformModulusRestrictionUp$ because this packet jointly carries compact source data, continuous-map evidence, an incoming modulus, a displayed restriction domain, a restricted compact window, an outgoing restricted modulus, transport, replay, provenance, and local naming.}

\closureat{CompactUniformModulusRestrictionUp}{seedStr}

\begin{closurestatus}{\CompactUniformModulusRestrictionUp}
  \constructivestory{A $\CompactUniformModulusRestrictionUp$ packet is generated from compact-metric source data, ContinuousMap rows, an incoming UniformModulus row, a displayed restriction domain, a restricted source window, an outgoing restricted UniformModulus row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:compact-uniform-modulus-restriction-carrier} and \autoref{thm:compact-uniform-modulus-restriction-namecert-obligations} seed the finite restriction route from compact-uniform modulus data to a displayed restricted-modulus consumer.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, host function equality, an external modulus selector, quotient real equality, ambient Real completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked compact source exposure, continuous-map visibility, incoming modulus exposure, restriction-domain admission, restricted-window exposure, outgoing modulus production, transport, replay, provenance, and local naming for BEDC.Derived.CompactUniformModulusRestrictionUp.}
\end{closurestatus}
