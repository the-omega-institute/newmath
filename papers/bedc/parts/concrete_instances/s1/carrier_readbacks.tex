\section{Carrier definition}

\begin{definition}[SOne carrier]
\label{def:s1-carrier}
Use pairs $(x,y)$ of Bishop real witnesses with a certified unit equation witness $x^2 + y^2 \approx 1$ under $\RealClassifier$.
\end{definition}
\leandef{BEDC.Derived.S1Up.SOneUnitHistory}
\leandef{BEDC.Derived.S1Up.SOneProductHistoryCarrier}
\leandef{BEDC.Derived.S1Up.SOneHistoryCarrier}

\begin{theorem}[SOne history carrier rational components]
\label{thm:s1-history-carrier-rational-components}
For every history $h$, the concrete $\SOneUp$ product-history carrier exposes
product components $x,y$ and rational denominator tails $d_x,d_y$ such that
both components are displayed as real constants and the BEDC continuation
ledger composes the two components into $h$:
$$
\mathsf{SOneProductHistoryCarrier}(h)
\Longrightarrow
\exists x,y,d_x,d_y.\;
\hsame(x,\Eone(d_x))\land
\mathsf{RatHistoryCarrier}(d_x)\land
\hsame(y,\Eone(d_y))\land
\mathsf{RatHistoryCarrier}(d_y)\land
\ContR(x,y,h).
$$
\end{theorem}
\begin{proof}
Unpack the $\SOneUp$ carrier as the $\ProdUp$ carrier over two real-constant
history carriers. The product carrier supplies the two component histories and
their continuation ledger into $h$. Each real-constant carrier then exposes a
rational denominator tail together with the displayed $\Eone$ presentation of
that component.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_rational\_components}

\begin{theorem}[SOne history carrier real-pair carrier]
\label{thm:s1-history-carrier-real-pair}
For histories $x,y,e,p$, the concrete $\SOneUp$ history carrier determines the
underlying product-real pair carrier:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,p)
\Longrightarrow
\mathsf{ProdHistoryCarrier}(\mathsf{RealConstantHistoryCarrier},
\mathsf{RealConstantHistoryCarrier},p).
$$
\end{theorem}
\begin{proof}
Unpack the circle carrier. Its first two fields are real-constant carrier
witnesses for $x$ and $y$, and its continuation field records
$\mathsf{Cont}(x,y,p)$. These three data are precisely the product-history
carrier introduction rule for the two real-constant component predicates.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_real\_pair}

\begin{theorem}[SOne point ledger determinacy]
\label{thm:s1-history-carrier-point-deterministic}
For histories $x,y,e,e',p,p'$, two concrete $\SOneUp$ history-carrier witnesses
with the same component histories determine the point ledger up to the BEDC
history classifier:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,p)
\Longrightarrow
\mathsf{SOneHistoryCarrier}(x,y,e',p')
\Longrightarrow
\hsame(p,p').
$$
\end{theorem}
\begin{proof}
Unpack the two circle-carrier witnesses only as far as their point-continuation
ledgers. Both ledgers have the same source component $x$ and the same right
component $y$. Continuation determinacy therefore identifies their continuation
results, giving $\hsame(p,p')$.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_point\_deterministic}

\begin{theorem}[SOne history carrier e-zero point exclusion]
\label{thm:sone-history-carrier-point-e0-absurd}
For histories $x,y,e,t$, a concrete $\SOneUp$ history-carrier witness cannot
have an $\Ezero$-headed point ledger:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,\Ezero(t))
\Longrightarrow
\bot.
$$
\end{theorem}
\begin{proof}
Unpack the circle carrier to the second real-constant component and the
continuation ledger. The second component displays $y$ as $\Eone(d)$ for a
rational history $d$. Transporting the continuation ledger along that display
makes the point ledger an $\Eone$-headed append result, contradicting the
displayed $\Ezero$ head.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_point\_e0\_absurd}

\begin{theorem}[SOne history carrier empty point exclusion]
\label{thm:sone-history-carrier-point-empty-absurd}
For histories $x,y,e$, a concrete $\SOneUp$ history-carrier witness cannot
have the empty point ledger:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,\emp)
\Longrightarrow
\bot.
$$
\end{theorem}
\begin{proof}
Unpack the circle carrier to the second real-constant component and the
continuation ledger. The second component displays $y$ as $\Eone(d)$ for a
rational history $d$. Transporting the continuation along that display makes
the point ledger an $\Eone$-headed append result, contradicting the empty
endpoint.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_point\_empty\_absurd}

\begin{theorem}[SOne history carrier empty point exclusion]
\label{thm:sone-history-carrier-empty-point-absurd}
For histories $x,y,e$, a concrete $\SOneUp$ history-carrier witness cannot have
an empty point ledger:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,\emp)
\Longrightarrow
\bot.
$$
\end{theorem}
\begin{proof}
Unpack the circle carrier to the first real-constant component and the
continuation ledger. The empty-result continuation inversion forces the first
component to be empty, while the real-constant carrier displays it as an
$\Eone$-headed rational history. Constructor no-confusion gives the
contradiction.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_empty\_point\_absurd}

\begin{theorem}[SOne history carrier e-one point shape]
\label{thm:sone-history-carrier-point-e1-shape}
For histories $x,y,e,p$, every concrete $\SOneUp$ history-carrier witness
exposes the point ledger as an $\Eone$-headed history:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,p)
\Longrightarrow
\exists t:\mathsf{BHist}.\; p=\Eone(t).
$$
\end{theorem}
\begin{proof}
Unpack the carrier to the second real-constant component and the continuation
ledger. The second component displays $y$ as $\Eone(d)$ for a rational history
$d$. Substituting that display into the continuation equation makes the point
ledger $p$ the append result with an $\Eone$-headed right component, hence
$p=\Eone(\mathsf{append}(x,d))$.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_point\_e1\_shape}

\begin{theorem}[SOne history carrier rational unit components]
\label{thm:s1-history-carrier-rational-unit-components}
For histories $x,y,e,p$, the concrete $\SOneUp$ history carrier exposes
rational denominator tails for both real components while preserving the unit
equation classifier and the continuation ledger:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,p)
\Longrightarrow
\exists d_x,d_y.\;
\hsame(x,\Eone(d_x))\land
\mathsf{RatHistoryCarrier}(d_x)\land
\hsame(y,\Eone(d_y))\land
\mathsf{RatHistoryCarrier}(d_y)\land
\mathsf{RealConstantHistoryClassifier}(e,\mathsf{SOneUnitHistory})\land
\ContR(x,y,p).
$$
\end{theorem}
\begin{proof}
Unpack the $\SOneUp$ history carrier. Its first two fields are real-constant
carrier witnesses, so each component supplies a rational denominator tail and
the corresponding $\Eone$ presentation. The remaining fields are exactly the
unit-equation classifier and the BEDC continuation ledger.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_rational\_unit\_components}

\begin{theorem}[SOne history carrier point-tail readback]
\label{thm:sone-history-carrier-point-tail-readback}
For histories $x,y,e,t$, a concrete $\SOneUp$ history-carrier witness whose
point ledger is visibly one-tagged exposes the rational component tails and
reads the visible point tail as the continuation of the first coordinate by
the second coordinate tail:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,\Eone(t))
\Longrightarrow
\exists d_x,d_y:\mathsf{BHist}.\;
\hsame(x,\Eone(d_x))\land \mathsf{RatHistoryCarrier}(d_x)\land
\hsame(y,\Eone(d_y))\land \mathsf{RatHistoryCarrier}(d_y)\land
\hsame(t,x\cdot d_y).
$$
\end{theorem}
\begin{proof}
Apply
\autoref{thm:s1-history-carrier-rational-unit-components}. This exposes
rational tails $d_x,d_y$, presentations $\hsame(x,\Eone(d_x))$ and
$\hsame(y,\Eone(d_y))$, and the continuation ledger
$\ContR(x,y,\Eone(t))$.

Using the presentation $\hsame(y,\Eone(d_y))$, the continuation result has the
form $\Eone(t)=\Eone(x\cdot d_y)$. Constructor inversion for $\Eone$ gives
$\hsame(t,x\cdot d_y)$. Package this with the two exposed rational carrier
witnesses.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_point\_tail\_readback}

\begin{theorem}[SOne y endpoint reads the point tail]
\label{thm:sone-history-carrier-y-e1-point-tail-hsame}
For histories $x,y,e,t,d_y$, a concrete $\SOneUp$ history-carrier witness with
point ledger $\Eone(t)$ and second coordinate presented as $\Eone(d_y)$ reads
back the point tail as the continuation of $x$ by $d_y$:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,\Eone(t))\to
\hsame(y,\Eone(d_y))\to
\hsame(t,x\cdot d_y).
$$
\end{theorem}
\begin{proof}
Unpack only the point-continuation field of the circle carrier. Transport the
displayed second endpoint along $\hsame(y,\Eone(d_y))$. The continuation result
then has the visible form $\Eone(t)=\Eone(x\cdot d_y)$, and one-constructor
inversion gives the displayed tail classifier.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_y\_e1\_point\_tail\_hsame}

\begin{theorem}[SOne endpoints read the e-one point tail]
\label{thm:sone-x-y-e1-point-tail-hsame}
For histories $x,y,e,t,d_x,d_y$, a concrete $\SOneUp$ history-carrier witness
with point ledger $\Eone(t)$ and coordinate presentations
$\hsame(x,\Eone(d_x))$ and $\hsame(y,\Eone(d_y))$ reads the point tail as
$$
\hsame(t,\Eone(d_x)\cdot d_y).
$$
\end{theorem}
\begin{proof}
The second coordinate presentation gives the point-tail readback
$t\sim x\cdot d_y$. Transporting the first coordinate presentation
$x\sim \Eone(d_x)$ through the same continuation tail identifies this with
$\Eone(d_x)\cdot d_y$.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_x\_y\_e1\_point\_tail\_hsame}

\begin{theorem}[SOne e-one components read point tail]
\label{thm:sone-history-carrier-e1-components-tail}
For histories $d_x,d_y,e,t$, a concrete $\SOneUp$ history-carrier witness whose
coordinates and point ledger are all one-headed exposes rational coordinate
tails and reads the point tail from the BEDC continuation:
$$
\mathsf{SOneHistoryCarrier}(\Eone(d_x),\Eone(d_y),e,\Eone(t))
\Longrightarrow
\mathsf{RatHistoryCarrier}(d_x)\land
\mathsf{RatHistoryCarrier}(d_y)\land
\hsame(t,\Eone(d_x)\cdot d_y).
$$
\end{theorem}
\begin{proof}
The first two carrier fields are real-constant carriers on one-headed
histories, hence the real-constant e-one exactness theorem gives rational
carriers for $d_x$ and $d_y$. The displayed second coordinate presentation is
reflexive, so the point-tail readback theorem gives
$\hsame(t,\Eone(d_x)\cdot d_y)$.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_e1\_components\_tail}

\begin{theorem}[SOne e-one components unit tail readback]
\label{thm:s1-history-carrier-e1-components-unit-tail-readback}
For histories $d_x,d_y,e,t$, a concrete $\SOneUp$ history-carrier witness whose
coordinates and point ledger are all one-headed exposes the two rational
coordinate tails, identifies the equation endpoint with the circle unit
history, and reads the point tail as the displayed continuation:
$$
\mathsf{SOneHistoryCarrier}(\Eone(d_x),\Eone(d_y),e,\Eone(t))
\Longrightarrow
\mathsf{RatHistoryCarrier}(d_x)\land
\mathsf{RatHistoryCarrier}(d_y)\land
\hsame(e,\mathsf{SOneUnitHistory})\land
\ContR(\Eone(d_x),d_y,t).
$$
\end{theorem}
\begin{proof}
The e-one component tail theorem supplies the two rational coordinate carriers
and the equality $t\hsame \Eone(d_x)\cdot d_y$. The equation-unit readback
identifies $e$ with $\mathsf{SOneUnitHistory}$. Reading the displayed tail
equality as a BEDC continuation gives $\ContR(\Eone(d_x),d_y,t)$, and these
four fields form the claimed package.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_e1\_components\_unit\_tail\_readback}

\begin{theorem}[SOne e-one components unit equation classifier]
\label{thm:s1-e1-components-unit-equation-classifier}
For histories $d_x,d_y,e,t$, a concrete $\SOneUp$ history-carrier witness whose
coordinates, equation endpoint, and point ledger are one-headed exposes the two
rational coordinate tails, the rational classifier from the equation tail to the
unit tail, and the displayed point continuation:
$$
\mathsf{SOneHistoryCarrier}(\Eone(d_x),\Eone(d_y),\Eone(e),\Eone(t))
\Longrightarrow
\mathsf{RatHistoryCarrier}(d_x)\land
\mathsf{RatHistoryCarrier}(d_y)\land
\mathsf{RatHistoryClassifier}(e,\Eone(\emp))\land
\ContR(\Eone(d_x),d_y,t).
$$
\end{theorem}
\begin{proof}
The unit-tail readback supplies the two rational coordinate carriers and the
displayed point continuation. The stored real-constant classifier on the
one-headed equation endpoint has target $\mathsf{SOneUnitHistory}=\Eone(\Eone(\emp))$,
so the real-constant e-one exactness theorem reads it as the rational classifier
between the equation tail and $\Eone(\emp)$.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_e1\_components\_unit\_equation\_classifier}

\begin{theorem}[SOne e-one components carrier exactness]
\label{thm:sone-e1-components-carrier-exactness}
For histories $d_x,d_y,e,t$, the one-headed presentation of a concrete
$\SOneUp$ history carrier is exact: it is present precisely when the two
coordinate tails are rational carriers, the equation tail classifies the
one-headed empty tail, and the point tail is the displayed continuation:
$$
\mathsf{SOneHistoryCarrier}(\Eone(d_x),\Eone(d_y),\Eone(e),\Eone(t))
\Longleftrightarrow
\mathsf{RatHistoryCarrier}(d_x)\land
\mathsf{RatHistoryCarrier}(d_y)\land
\mathsf{RatHistoryClassifier}(e,\Eone(\emp))\land
\ContR(\Eone(d_x),d_y,t).
$$
\end{theorem}
\begin{proof}
The forward direction is exactly the e-one unit-equation classifier readback:
apply \autoref{thm:s1-e1-components-unit-equation-classifier} to the given
$\SOneUp$ carrier witness.

For the reverse direction, start from the four displayed fields. The two
rational carrier fields repack as the real-constant carrier witnesses for
$\Eone(d_x)$ and $\Eone(d_y)$. The rational classifier
$\mathsf{RatHistoryClassifier}(e,\Eone(\emp))$ lifts along the real-constant
e-one classifier exactness theorem to the required classifier from
$\Eone(e)$ to $\mathsf{SOneUnitHistory}$.

It remains to supply the point-continuation field. The hypothesis
$\ContR(\Eone(d_x),d_y,t)$ is exactly the statement that the one-headed point
ledger $\Eone(t)$ carries the append continuation determined by
$\Eone(d_x)$ and $d_y$. With these four fields repacked, the constructor for
$\mathsf{SOneHistoryCarrier}$ gives the desired carrier witness.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_e1\_components\_carrier\_exactness}

\begin{theorem}[SOne history carrier y e-one point shape]
\label{thm:sone-history-carrier-y-e1-point-shape}
For histories $x,y,e,p,d_y$, if a concrete $\SOneUp$ history-carrier witness
presents the second coordinate as $\Eone(d_y)$, then the point ledger is
visibly one-headed and its exposed tail is the continuation of $x$ by $d_y$:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,p)
\Longrightarrow
\hsame(y,\Eone(d_y))
\Longrightarrow
\exists t:\mathsf{BHist}.\;
\hsame(p,\Eone(t))\land \ContR(x,d_y,t).
$$
\end{theorem}
\begin{proof}
Unpack the circle carrier only to its point-continuation field. Replacing the
second coordinate by its $\Eone(d_y)$ presentation makes the point ledger
definitionally $\Eone(x\cdot d_y)$. Choose the exposed payload
$t=x\cdot d_y$; the remaining continuation statement is the canonical append
ledger.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_y\_e1\_point\_shape}

\begin{theorem}[SOne unit equation determinacy]
\label{thm:s1-unit-equation-deterministic}
For histories $x,y,e,p$, every concrete $\SOneUp$ history-carrier witness
determines that its stored equation endpoint is the unit history:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,p)
\Longrightarrow
\hsame(e,\mathsf{SOneUnitHistory}).
$$
\end{theorem}
\begin{proof}
Unpack the $\mathsf{SOneHistoryCarrier}$ witness and keep the
$\mathsf{RealConstantHistoryClassifier}(e,\mathsf{SOneUnitHistory})$ field.
This classifier exposes rational tails for both endpoints. Its rational payload
classifier identifies the tails, and constructor congruence lifts that equality
through $\Eone$. Composing this equality with the stored endpoint presentations
gives $\hsame(e,\mathsf{SOneUnitHistory})$.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_unit\_equation\_deterministic}

\begin{theorem}[SOne history carrier equation-pair determinacy]
\label{thm:sone-history-carrier-equation-pair-deterministic}
For histories $x,y,e,p,x',y',e',p'$, any two concrete $\SOneUp$ history-carrier
witnesses determine history-same equation ledgers:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,p)
\Longrightarrow
\mathsf{SOneHistoryCarrier}(x',y',e',p')
\Longrightarrow
\hsame(e,e').
$$
\end{theorem}
\begin{proof}
Apply the unit-equation determinacy theorem to each carrier witness. The first
witness identifies $e$ with $\mathsf{SOneUnitHistory}$, and the second
identifies $e'$ with the same endpoint. Transitivity through that endpoint gives
$\hsame(e,e')$.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_equation\_pair\_deterministic}

\begin{theorem}[SOne equation unit readback]
\label{thm:sone-history-carrier-equation-unit}
For histories $x,y,e,p$, the concrete $\SOneUp$ history carrier determines that
its equation witness is the BEDC unit history:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,p)
\Longrightarrow
\hsame(e,\mathsf{SOneUnitHistory}).
$$
\end{theorem}
\begin{proof}
Unpack the circle carrier to its equation-classifier field
$\mathsf{RealConstantHistoryClassifier}(e,\mathsf{SOneUnitHistory})$. That field
displays $e$ and $\mathsf{SOneUnitHistory}$ as $\Eone$-sealed rational histories,
with a rational classifier between the displayed tails. The rational classifier
contains an $\hsame$ witness between those tails; one-constructor congruence
lifts it to the sealed endpoints, and transitivity with the two endpoint
displays identifies $e$ with $\mathsf{SOneUnitHistory}$.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_equation\_unit}

\begin{theorem}[SOne history carrier public readback]
\label{thm:sone-history-carrier-public-readback}
For histories $x,y,e,p$, the concrete $\SOneUp$ history carrier exposes the
product carrier, identifies the equation endpoint with the BEDC unit history,
and reads back rational tails for both real components:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,p)
\Longrightarrow
\mathsf{SOneProductHistoryCarrier}(p)\land
\hsame(e,\mathsf{SOneUnitHistory})\land
\exists d_x,d_y.\;
\hsame(x,\Eone(d_x))\land
\mathsf{RatHistoryCarrier}(d_x)\land
\hsame(y,\Eone(d_y))\land
\mathsf{RatHistoryCarrier}(d_y)\land
\ContR(x,y,p).
$$
\end{theorem}
\begin{proof}
The product carrier is obtained by repacking the two real-constant component
carriers with the continuation ledger. Equation-unit determinacy identifies
the stored equation endpoint with $\mathsf{SOneUnitHistory}$. Finally, the
rational unit component readback exposes the two rational tails and the same
continuation ledger, while its equation classifier field is no longer needed.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_public\_readback}

\begin{theorem}[SOne component classifier ledger determinacy]
\label{thm:s1-component-classifier-ledger-determinacy}
For histories $x,y,e,p,x',y',e',p'$, componentwise BEDC history
classification of two concrete $\SOneUp$ carrier witnesses determines both
hidden ledgers:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,p)
\Longrightarrow
\mathsf{SOneHistoryCarrier}(x',y',e',p')
\Longrightarrow
\hsame(x,x')
\Longrightarrow
\hsame(y,y')
\Longrightarrow
\hsame(e,e') \wedge \hsame(p,p').
$$
\end{theorem}
\begin{proof}
Unpack the two circle-carrier witnesses into their four fields. The first gives
the real-constant component carriers for $x$ and $y$, the equation classifier
$\mathsf{RealConstantHistoryClassifier}(e,\mathsf{SOneUnitHistory})$, and the
continuation ledger $\mathsf{Cont}(x,y,p)$. The second gives the corresponding
fields for $x'$, $y'$, $e'$, and $p'$.

For the equation ledger, unpack the two
$\mathsf{RealConstantHistoryClassifier}(-,\mathsf{SOneUnitHistory})$ witnesses.
Each contains a $\mathsf{RatHistoryClassifier}$ tail against the fixed
$\mathsf{SOneUnitHistory}$ endpoint. Constructor inversion for the
one-constructor rational history spine exposes the same rational tail on both
sides. The $\hsame$ symmetry and transitivity rules then connect $e$ to $e'$,
and rebuilding the history classifier witness yields $\hsame(e,e')$.

For the point ledger, apply continuation $\hsame$-respect to the two
$\mathsf{ContR}$ ledgers using the supplied component witnesses
$\hsame(x,x')$ and $\hsame(y,y')$. Continuation determinacy identifies the
resulting point histories, giving $\hsame(p,p')$.

Pair the two conclusions. The certificate fit is the traditional target
unit-circle classifier determinacy; the BEDC source $\mathsf{BHist}$ with
$\mathsf{RealConstantHistoryClassifier}$ and $\mathsf{ContR}$; the carrier
$\mathsf{SOneHistoryCarrier}$; the classifier $\hsame$ on exposed components;
the ledger equation proof and point continuation; the core theorem this
determinacy statement; the exactness claim hidden-ledger uniqueness; the
standard bridge deferred outside this theorem; and the status
$\mathsf{ProofTarget}$.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_component\_classifier\_ledger\_determinacy}

\begin{theorem}[SOne history carrier coordinate-pair determinacy]
\label{thm:sone-history-carrier-coordinate-pair-deterministic}
For histories $x,x',y,y',e,e',p$, two concrete $\SOneUp$ carrier witnesses with
the same point ledger and history-same second coordinate determine the first
coordinate and the equation witness:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,p)
\Longrightarrow
\mathsf{SOneHistoryCarrier}(x',y',e',p)
\Longrightarrow
\hsame(y,y')
\Longrightarrow
\hsame(x,x')\land\hsame(e,e').
$$
\end{theorem}
\begin{proof}
Unpack the two circle-carrier witnesses. The shared point ledger gives
$\mathsf{Cont}(x,y,p)$ and $\mathsf{Cont}(x',y',p)$; after transporting along
$\hsame(y,y')$, continuation right-cancellation identifies $x$ with $x'$.
The equation components are both classified against the fixed
$\mathsf{SOneUnitHistory}$ endpoint, so the unit-equation determinacy readback
identifies each equation witness with that endpoint, and transitivity gives
$\hsame(e,e')$.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_coordinate\_pair\_deterministic}

\begin{theorem}[SOne history carrier coordinate-pair right determinacy]
\label{thm:sone-history-carrier-coordinate-pair-right-deterministic}
For histories $x,x',y,y',e,e',p$, two concrete $\SOneUp$ carrier witnesses with
the same point ledger and history-same first coordinate determine the second
coordinate and the equation witness:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,p)
\Longrightarrow
\mathsf{SOneHistoryCarrier}(x',y',e',p)
\Longrightarrow
\hsame(x,x')
\Longrightarrow
\hsame(y,y')\land\hsame(e,e').
$$
\end{theorem}
\begin{proof}
Unpack the two circle-carrier witnesses. The shared point ledger gives
$\mathsf{Cont}(x,y,p)$ and $\mathsf{Cont}(x',y',p)$; after transporting along
$\hsame(x,x')$, continuation left-cancellation identifies $y$ with $y'$. The
equation components are both classified against the fixed
$\mathsf{SOneUnitHistory}$ endpoint, so unit-equation determinacy identifies
each equation witness with that endpoint, and transitivity gives
$\hsame(e,e')$.
\end{proof}
\leanchecked{BEDC.Derived.S1Up.SOneHistoryCarrier\_coordinate\_pair\_right\_deterministic}

\begin{theorem}[SOne shared-point component equivalence]
\label{thm:sone-shared-point-component-iff}
For histories $x,x',y,y',e,e',p$, two concrete $\SOneUp$ carrier witnesses over
the same displayed point ledger have the following shared-point component
equivalence:
$$
\mathsf{SOneHistoryCarrier}(x,y,e,p)
\Longrightarrow
\mathsf{SOneHistoryCarrier}(x',y',e',p)
\Longrightarrow
(\hsame(x,x') \Longrightarrow \hsame(y,y')\land\hsame(e,e'))
\land
(\hsame(y,y') \Longrightarrow \hsame(x,x')\land\hsame(e,e')).
$$
\end{theorem}
\begin{proof}
Assume the two displayed carrier witnesses
$\mathsf{SOneHistoryCarrier}(x,y,e,p)$ and
$\mathsf{SOneHistoryCarrier}(x',y',e',p)$.

For the first branch, suppose $\hsame(x,x')$. Apply
\autoref{thm:sone-history-carrier-coordinate-pair-right-deterministic} to the
two carrier witnesses and the first-coordinate equality. The hypotheses use the
same displayed point ledger $p$, so right determinacy recovers both
$\hsame(y,y')$ and $\hsame(e,e')$; the equation component is recovered against
the fixed $\mathsf{SOneUnitHistory}$ ledger exposed by the carrier.

For the second branch, suppose $\hsame(y,y')$. Apply
\autoref{thm:sone-history-carrier-coordinate-pair-deterministic} to the same
two carrier witnesses and the second-coordinate equality. The shared displayed
point ledger is again $p$, so coordinate-pair determinacy recovers
$\hsame(x,x')$ and the same equation-ledger comparison $\hsame(e,e')$ against
$\mathsf{SOneUnitHistory}$.

Pair the two implications.
\end{proof}
