\chapter{A Concrete Naming Certificate for $\RegularCauchyTailFilterBasisUp$}
\label{ch:concrete-instances-regular-cauchy-tail-filter-basis-namecert}
\origin{human}

Following Bishop and Bridges on constructive real completion, the $\RegularCauchyTailFilterBasisUp$ packet records the finite tail-filter basis generated by a regular Cauchy rational schedule before a completion consumer reads a $\RealUp$ seal. It sits after the regular rational sequence of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the Cauchy filter-basis surface of \autoref{ch:concrete-instances-cauchy-filter-basis-namecert}, and the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The packet names only displayed tail-filter basis rows; it does not assert quotient completion equality, selected limits, countable choice, host completeness, or ambient $\RealUp$ equality.

\begin{definition}[Regular Cauchy tail-filter basis carrier]
\label{def:regular-cauchy-tail-filter-basis-carrier}
A $\RegularCauchyTailFilterBasisUp$ carrier is a finite $\BHist$ packet
$$
T=(R,W,B,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $R$ is the $\RegSeqRatUp$ source row. It records the regular rational schedule and the explicit tail bounds that generate the basis.
\item $W$ is the $\StreamNameUp$ window row. It lists the finite observation windows, requested indices, and inspection order used to read the schedule.
\item $B$ is the $\CauchyFilterBasisUp$ basis row. It stores the finite family of tail neighborhoods generated from the regular Cauchy schedule.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row. It records dyadic radii, denominator budgets, slack comparisons, and the tail-to-radius conversion used by the basis.
\item $E$ is the $\RealUp$ endpoint seal row. It is visible only after $R,W,B,D$ have displayed the regular schedule, finite windows, basis neighborhoods, and tolerance comparisons.
\item $H$ records componentwise $\hsame$ transport for the source schedule, windows, basis, dyadic ledger, endpoint seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from regular Cauchy tail requests to finite filter-basis neighborhoods and then to the endpoint seal.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyTailFilterBasisUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\CauchyFilterBasisUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyTailFilterBasisUp}$ row.
\end{enumerate}
Accepted carriers are compared only through $R,W,B,D,E,H,C,P,N$. There is no carrier coordinate for quotient completion equality, a selected limiting real, countable choice, host completeness, or ambient $\RealUp$ equality.
\end{definition}

\begin{theorem}[Regular Cauchy tail-filter basis NameCert obligations]
\label{thm:regular-cauchy-tail-filter-basis-namecert-obligations}
For every accepted $\RegularCauchyTailFilterBasisUp$ carrier $T=(R,W,B,D,E,H,C,P,N)$, the local $\NameCert_{\RegularCauchyTailFilterBasisUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the regular rational source, stream windows, Cauchy filter-basis row, dyadic tolerance ledger, endpoint seal, transport, replay, provenance, and local naming rows;
\item tail-window exactness, requiring every basis neighborhood to name the finite $\StreamNameUp$ window and $\RegSeqRatUp$ tail request from which it was generated;
\item dyadic-radius exactness, requiring $D$ to display the radius and slack comparison consumed by each tail neighborhood in $B$;
\item endpoint-seal nonescape, requiring $E$ to remain downstream of $R,W,B,D$ and to export no quotient completion equality, selected limiting real, countable-choice schedule, host completeness, or ambient $\RealUp$ equality;
\item componentwise transport, displayed replay, provenance, and local naming for the same route through $H,C,P,N$.
\end{enumerate}
No regular Cauchy tail-filter basis obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the accepted packet $T=(R,W,B,D,E,H,C,P,N)$. The carrier definition exposes exactly the regular rational source, finite windows, basis neighborhoods, dyadic ledger, endpoint seal, and structural rows.

The basis route starts with $R$ and $W$. A tail neighborhood cannot be read until the regular Cauchy tail request and its finite observation window have both been displayed.

The dyadic comparison is then read through $D$. This row supplies the radius, denominator budget, and slack comparison used by the basis row $B$, so a filter-basis consumer receives a finite tail-to-radius certificate rather than a host completeness argument.

Finally $E,H,C,P,N$ seal, transport, replay, source, and name the same route. Since the carrier has no coordinate for quotient equality, selected limits, countable choice, host completeness, or ambient real equality, the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Regular Cauchy tail-filter endpoint handoff]
\label{thm:regular-cauchy-tail-filter-basis-endpoint-handoff}
Every completion-facing consumer of an accepted $\RegularCauchyTailFilterBasisUp$ carrier factors through
$$
\RegSeqRatUp,\StreamNameUp \longmapsto \CauchyFilterBasisUp,\DyadicRatCoreUp \longmapsto \RealUp \longmapsto \BHist,\hsame,\Cont,\Pkg,\NameCert .
$$
Thus the endpoint seal is consumed only after the regular Cauchy schedule, finite windows, tail-filter basis neighborhoods, dyadic comparisons, transport, replay, provenance, and local naming have been displayed.
\end{theorem}
\begin{proof}
Begin with a completion-facing consumer of $T=(R,W,B,D,E,H,C,P,N)$. The only source rows available to the consumer are the regular Cauchy schedule $R$ and the finite windows $W$.

The filter-basis handoff then passes through $B$ and $D$. The row $B$ records the tail neighborhoods generated by the schedule, while $D$ records the dyadic radii and slack comparisons that make those neighborhoods usable by completion data.

The endpoint row $E$ is downstream of this displayed basis route. The structural rows $H,C,P,N$ preserve the same finite packet, so the consumer cannot replace the route by quotient completion equality, a selected limit, countable choice, host completeness, or ambient $\RealUp$ equality.
\end{proof}

\falsifiablePrediction{Every $\RegularCauchyTailFilterBasisUp$ completion-facing read factors through a RegSeqRat source, StreamName windows, CauchyFilterBasis tail neighborhoods, a DyadicRatCore tolerance ledger, a RealUp seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{regseqrat, streamname, cauchyfilterbasis, dyadicratcore, real: $\RegularCauchyTailFilterBasisUp$ is not bijective to any listed sibling because it jointly carries regular Cauchy schedules, finite stream windows, generated tail-filter basis neighborhoods, dyadic tolerance comparisons, endpoint sealing, transport, replay, provenance, and local naming.}

\closureat{RegularCauchyTailFilterBasisUp}{seedStr}

\begin{closurestatus}{\RegularCauchyTailFilterBasisUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-tail-filter-basis-carrier}, \autoref{thm:regular-cauchy-tail-filter-basis-namecert-obligations}, and \autoref{thm:regular-cauchy-tail-filter-basis-endpoint-handoff} seed the finite route from regular Cauchy schedules to tail-filter basis completion data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient completion equality, selected limits, countable choice, host completeness, ambient $\RealUp$ equality, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, tail-window exactness, dyadic-radius exactness, endpoint-seal nonescape, componentwise transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyTailFilterBasisUp.}
  \constructivestory{A $\RegularCauchyTailFilterBasisUp$ packet is generated from a RegSeqRat regular Cauchy schedule, StreamName windows, CauchyFilterBasis tail neighborhoods, a DyadicRatCore tolerance ledger, a RealUp endpoint seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
