\chapter{A Concrete Naming Certificate for $\RealModulusPurityBoundaryUp$}
\label{ch:concrete-instances-real-modulus-purity-boundary-namecert}
\origin{ai}

The $\RealModulusPurityBoundaryUp$ packet realizes the axiom-purity boundary of the Real-completeness vision in \autoref{ch:visions-real-completeness}. It collects the no-choice boundary of \autoref{ch:concrete-instances-cauchy-choice-boundary-namecert}, the selected-window refusal of \autoref{ch:concrete-instances-choicefreediagonalselector-namecert}, the shared-modulus normal form of \autoref{ch:concrete-instances-regular-cauchy-modulus-normalizer-namecert}, the finite-window Real seal audit of \autoref{ch:concrete-instances-finite-window-real-seal-audit-namecert}, and the fused Real-modulus route of \autoref{ch:concrete-instances-realmodulusfusion-namecert}. Its role is to bind dyadic observations, finite StreamName windows, RegSeqRat readbacks, RealUp seals, explicit modulus budgets, transports, continuations, provenance, local naming, and an axiom-refusal row into one finite carrier. It is not a quotient stream, selected host limit, host real equality, countable-choice schedule, ambient completeness principle, or proof by $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, or $\mathsf{propext}$.

\falsifiablePrediction{Any accepted RealModulusPurityBoundaryUp packet with a selected-host-limit, quotient-stream, or countable-choice coordinate fails the local NameCert carrier predicate.}

\independenceWitness{cauchychoiceboundary, choicefreediagonalselector, regular-cauchy-modulus-normalizer, finite-window-real-seal-audit, realmodulusfusion: none is bijective to this packet because each omits at least one of the explicit axiom-purity refusal row, four-face Real route, modulus budget, or local NameCert non-escape obligation.}

\begin{definition}[Real modulus-purity boundary carrier]
\label{def:real-modulus-purity-boundary-carrier}
A $\RealModulusPurityBoundaryUp$ carrier is a finite $\BHist$ packet
$$
R=(D,S,R_0,L,B,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ observation row that records the finite dyadic tolerance read by the Real-completion consumer;
\item $S$ is the $\StreamNameUp$ finite-window row naming the visible request window attached to $D$;
\item $R_0$ is the $\RegSeqRatUp$ readback row reached from the same dyadic and stream-window data;
\item $L$ is the $\RealUp$ seal row reached only after $D,S,R_0$ have been displayed;
\item $B$ is the explicit modulus-budget and axiom-refusal row, carrying the selected finite modulus threshold and recording refusal of quotient streams, selected host limits, countable choice, $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, and $\mathsf{propext}$;
\item $H$ records componentwise $\hsame$ transports for $D,S,R_0,L,B$ and the local packet shape;
\item $C$ records the displayed $\Cont$ replay from dyadic observation and window admission through RegSeqRat readback, modulus budget, refusal, Real seal, and local naming;
\item $P$ is the $\Pkg$ provenance over \autoref{ch:concrete-instances-cauchy-choice-boundary-namecert}, \autoref{ch:concrete-instances-choicefreediagonalselector-namecert}, \autoref{ch:concrete-instances-regular-cauchy-modulus-normalizer-namecert}, \autoref{ch:concrete-instances-finite-window-real-seal-audit-namecert}, \autoref{ch:concrete-instances-realmodulusfusion-namecert}, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\RealModulusPurityBoundaryUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these nine rows. It has no coordinate for selected host limits, quotient streams, detached Real seals, countable-choice schedules, ambient completeness, or proof-irrelevant host equality.
\end{definition}
\leandef{BEDC.Derived.RealModulusPurityBoundaryUp.RealModulusPurityBoundaryUp}

\begin{theorem}[Real modulus-purity boundary NameCert obligations]
\label{thm:real-modulus-purity-boundary-namecert-obligations}
For every accepted $\RealModulusPurityBoundaryUp$ carrier
$$
R=(D,S,R_0,L,B,H,C,P,N),
$$
the local $\NameCert_{\RealModulusPurityBoundaryUp}$ surface consists exactly of dyadic observation exposure through $D$, finite-window exposure through $S$, RegSeqRat readback through $R_0$, RealUp seal exposure through $L$, explicit modulus-budget and axiom-refusal exposure through $B$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, package provenance through $P$, and local naming through $N$.
\end{theorem}
\leanchecked{BEDC.Derived.RealModulusPurityBoundaryUp.RealModulusPurityBoundary\_namecert\_obligations}
\begin{proof}
Project the carrier of \autoref{def:real-modulus-purity-boundary-carrier}. Its displayed inventory is the dyadic observation, StreamName window, RegSeqRat readback, RealUp seal, modulus-budget refusal row, and the structural support rows.

Read the Real route in dependency order. The dyadic row $D$ fixes the tolerance observation, the window row $S$ fixes the finite request surface, the readback row $R_0$ fixes the regular-rational stream read from those data, and the seal row $L$ is reached only after the four-face route has been displayed.

The budget row $B$ supplies the explicit modulus threshold and records the refusal of quotient, choice, selected-limit, and proof-extensional coordinates. The rows $H,C,P,N$ only transport, replay, package, and name the same finite packet, so the listed rows exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[Real modulus-purity boundary non-escape]
\label{thm:real-modulus-purity-boundary-nonescape}
Every accepted $\RealModulusPurityBoundaryUp$ consumer factors through the finite route
$$
D,S,R_0,L,B,H,C,P,N.
$$
The consumer may inspect dyadic observations, StreamName windows, RegSeqRat readbacks, RealUp seals, explicit modulus budgets, axiom-refusal rows, componentwise transports, continuations, provenance, and the local NameCert row. It receives no selected host limit, quotient-stream equality, host real equality, countable-choice coordinate, detached Real-completion object, ambient-completeness row, $\mathsf{Classical.choice}$ row, $\mathsf{Quot.sound}$ row, or $\mathsf{propext}$ row.
\end{theorem}
\leanchecked{BEDC.Derived.RealModulusPurityBoundaryUp.RealModulusPurityBoundary\_nonescape}
\begin{proof}
Start from \autoref{thm:real-modulus-purity-boundary-namecert-obligations}. The local certificate exposes the complete route inventory for the accepted packet and gives no second source for dyadic, window, readback, seal, budget, refusal, transport, continuation, provenance, or naming data.

Attach the five sibling boundaries. The Cauchy choice-boundary and choice-free selector chapters exclude hidden selected limits and countable-choice schedules; the regular-Cauchy modulus normalizer supplies the displayed modulus route; the finite-window Real-seal audit and Real-modulus fusion chapters keep the StreamName, RegSeqRat, dyadic, and RealUp faces on the same finite route.

Inspect the refusal row. Since $B$ records the excluded coordinates and the carrier has no other coordinate through which to read them, any consumer that asks for selected host limits, quotient streams, ambient completeness, or proof-extensional host equality has left the accepted boundary packet.
\end{proof}

\begin{theorem}[Real modulus-purity boundary constructive prediction]
\label{thm:real-modulus-purity-boundary-constructive-prediction}
Let a Real-completion consumer accept a $\RealModulusPurityBoundaryUp$ packet and preserve the displayed route
$$
D,S,R_0,L,B,H,C,P,N.
$$
Then every later RealUp or RegSeqRat read is predicted by the explicit modulus budget in $B$ together with the dyadic observation $D$, finite window $S$, RegSeqRat readback $R_0$, and RealUp seal $L$. A consumer that replaces this prediction by a selected-host-limit, quotient-stream, countable-choice, $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, or $\mathsf{propext}$ coordinate fails the local carrier predicate.
\end{theorem}
\leanchecked{BEDC.Derived.RealModulusPurityBoundaryUp.RealModulusPurityBoundary\_constructive\_prediction}
\begin{proof}
First read the displayed route. The rows $D,S,R_0,L$ give the four Real-facing observations in order, while $B$ supplies the finite modulus budget that selects the visible part of the route.

Use componentwise transport and replay next. The rows $H$ and $C$ preserve the same dyadic, window, readback, seal, and budget data under accepted consumer motion, so downstream RealUp and RegSeqRat requests return to the displayed finite packet.

Finally apply non-escape. The forbidden selected-limit, quotient, choice, and proof-extensional rows are absent from the carrier and explicitly refused by $B$. Hence constructive prediction is exactly the finite modulus-budget route, and any attempted replacement fails the carrier predicate.
\end{proof}

\begin{theorem}[Real modulus-purity boundary host-choice refusal]
\label{thm:real-modulus-purity-boundary-host-choice-refusal}
Let a Real-and-Completion consumer accept a $\RealModulusPurityBoundaryUp$ packet
$$
R=(D,S,R_0,L,B,H,C,P,N).
$$
If the consumer asks for a selected host limit, quotient-stream equality, countable-choice schedule, host real equality, or ambient-completeness coordinate, then the request is refused by the displayed row $B$. The only admissible route through the packet remains the finite dyadic observation $D$, StreamName window $S$, RegSeqRat readback $R_0$, RealUp seal $L$, modulus budget $B$, componentwise transport $H$, continuation replay $C$, package provenance $P$, local NameCert row $N$, and the refusal information carried by $B$.
\end{theorem}
\leanchecked{BEDC.Derived.RealModulusPurityBoundaryUp.RealModulusPurityBoundary\_host\_choice\_refusal}
\begin{proof}
Project the carrier in \autoref{def:real-modulus-purity-boundary-carrier}. Its row $B$ is not merely a modulus budget: it also displays refusal of quotient streams, selected host limits, countable choice, $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, and $\mathsf{propext}$.

Apply \autoref{thm:real-modulus-purity-boundary-namecert-obligations}. The local NameCert surface exposes exactly the dyadic observation, StreamName window, RegSeqRat readback, RealUp seal, modulus-budget refusal row, componentwise transport, continuation replay, package provenance, and local naming rows.

Apply \autoref{thm:real-modulus-purity-boundary-nonescape}. Since the accepted packet has no quotient, host-limit, host-equality, countable-choice, or ambient-completeness coordinate, a consumer that asks for one of those coordinates cannot obtain it from any hidden field.

Finally use \autoref{thm:real-modulus-purity-boundary-constructive-prediction}. Downstream RealUp and RegSeqRat reads are predicted by the finite modulus route already displayed by $D,S,R_0,L,B,H,C,P,N$, so the host-choice request is isolated as a refusal obligation of $B$ rather than as an admissible Real-completion operation.
\end{proof}

\begin{theorem}[Real modulus-purity boundary scoped obligation package]
\label{thm:real-modulus-purity-boundary-scope-package}
The carrier of \autoref{def:real-modulus-purity-boundary-carrier}, together with \autoref{thm:real-modulus-purity-boundary-namecert-obligations}, \autoref{thm:real-modulus-purity-boundary-nonescape}, \autoref{thm:real-modulus-purity-boundary-constructive-prediction}, and \autoref{thm:real-modulus-purity-boundary-host-choice-refusal}, assembles the full obligation surface over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, explicit modulus budget, axiom-refusal row, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$. Every admitted consumer of the packet must expose the same row tuple
$$
D,S,R_0,L,B,H,C,P,N
$$
before any Real-facing read is exported.
\end{theorem}
\begin{proof}
Project the carrier as $R=(D,S,R_0,L,B,H,C,P,N)$. The dyadic row $D$, finite StreamName window $S$, RegSeqRat readback $R_0$, Real seal $L$, and modulus-refusal row $B$ are the analytic coordinates of the packet.

Use the NameCert obligations to expose those coordinates in dependency order. The dyadic and window rows precede the readback, the readback precedes the Real seal, and the explicit budget row records both the finite modulus path and the refused axiom imports.

Apply non-escape and constructive prediction. These theorems force every downstream RealUp or RegSeqRat read to return to the displayed finite route and prevent a selected limit, quotient stream, countable choice, $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, or $\mathsf{propext}$ coordinate from being repacked as an admitted consumer field.

Finally apply host-choice refusal. The support rows $H,C,P,N$ transport, replay, source, and name only the same displayed route, so no hidden selected limit, quotient stream, countable choice, proof-extensional coordinate, or host-completeness coordinate remains outside the scoped obligation package.
\end{proof}
\leanchecked{BEDC.Derived.RealModulusPurityBoundaryUp.RealModulusPurityBoundaryScopePackage}
\leanvariant{BEDC.Derived.RealModulusPurityBoundaryUp.RealModulusPurityBoundary\_scope\_package}

The Lean file \texttt{lean4/BEDC/Derived/RealModulusPurityBoundaryUp/TasteGate.lean} provides explicit \texttt{Nontrivial RealModulusPurityBoundaryUp} and \texttt{FieldFaithful RealModulusPurityBoundaryUp} instances for the carrier of \autoref{def:real-modulus-purity-boundary-carrier}.

\begin{theorem}[Real modulus purity boundary tail-budget exhaustion]
\label{thm:real-modulus-purity-boundary-tail-budget-exhaustion}
Let
$$
R=(D,S,R_0,L,B,H,C,P,N)
$$
be an accepted $\RealModulusPurityBoundaryUp$ packet whose displayed modulus windows pass through the L10 route
$$
\DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
Then $R$ has no additional tail-budget coordinate outside the displayed dyadic observation $D$, StreamName window $S$, RegSeqRat readback $R_0$, RealUp seal $L$, modulus-budget refusal row $B$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert$ row $N$. Every completion-facing read of $R$ factors through these rows plus $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$; it cannot read ambient Real completeness, quotient streams, host-selected limits, hidden moduli, or proof-extensional tail data.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-modulus-purity-boundary-carrier}. The only analytic rows are $D,S,R_0,L,B$, and the only structural rows are $H,C,P,N$.

Apply the NameCert obligations of \autoref{thm:real-modulus-purity-boundary-namecert-obligations}. They expose the dyadic observation, finite StreamName window, RegSeqRat readback, RealUp seal, and explicit modulus-budget refusal row before any Real-facing consumer can read the packet.

Use the non-escape theorem \autoref{thm:real-modulus-purity-boundary-nonescape} and the host-choice refusal theorem \autoref{thm:real-modulus-purity-boundary-host-choice-refusal}. The window-scope and refusal rows exclude selected host limits, quotient streams, countable-choice schedules, host Real equality, and ambient-completeness coordinates.

Finally read the scoped obligation package of \autoref{thm:real-modulus-purity-boundary-scope-package}. It forces every admitted consumer to expose the same tuple $D,S,R_0,L,B,H,C,P,N$, so no hidden tail-budget coordinate remains outside the L10 four-object route and its structural certificate rows.
\end{proof}
\leanchecked{BEDC.Derived.RealModulusPurityBoundaryUp.RealModulusPurityBoundaryTailBudgetExhaustion}

\begin{theorem}[Real modulus-purity boundary dyadic witness stability]
\label{thm:real-modulus-purity-boundary-dyadic-witness-stability}
Let
$$
R=(D,S,R_0,L,B,H,C,P,N)
$$
be an accepted $\RealModulusPurityBoundaryUp$ packet. If a finite dyadic observation row $D$ is replaced inside the same $\StreamNameUp$ window $S$ by an $\hsame$-transported $\DyadicRatCoreUp$ witness recorded by $H$, then the resulting finite packet preserves the $\RealModulusPurityBoundaryUp$ carrier, the local classifier, the tail-budget ledger $B$, and the refusal of $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, and $\mathsf{propext}$. No new host-limit coordinate, quotient-stream equality, hidden modulus, or ambient Real-completeness row is introduced by the replacement.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-modulus-purity-boundary-carrier}. The dyadic observation row, StreamName window, RegSeqRat readback, RealUp seal, tail-budget ledger, transport, replay, provenance, and local naming rows are the only available coordinates.

Use the scoped obligation package and tail-budget exhaustion. The accepted packet must expose the same tuple $D,S,R_0,L,B,H,C,P,N$, and no second tail-budget or host-limit coordinate is available outside that tuple.

Transport the dyadic witness by the displayed row $H$. Because the replacement remains inside the same StreamName window $S$, the RegSeqRat readback $R_0$, RealUp seal $L$, and ledger $B$ are replayed by the same $\Cont$ row $C$ rather than by a new completion principle.

Finally inspect the refusal information in $B$. It continues to reject $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, $\mathsf{propext}$, selected host limits, quotient streams, and ambient completeness, so the hsame-transported dyadic witness preserves the carrier and classifier surface.
\end{proof}
\leanchecked{BEDC.Derived.RealModulusPurityBoundaryUp.RealModulusPurityBoundaryDyadicWitnessStability}
\leanchecked{BEDC.Derived.RealModulusPurityBoundaryUp.taste\_gate}

\begin{theorem}[Real modulus-purity boundary scope binding]
\label{thm:real-modulus-purity-boundary-scope-binding}
Every accepted $\RealModulusPurityBoundaryUp$ packet binds exactly the dyadic row $D$, StreamName window $S$, RegSeqRat readback $R_0$, Real seal $L$, finite modulus-budget row $B$, componentwise $\hsame$ transport $H$, displayed $\Cont$ replay $C$, $\Pkg$ provenance $P$, and local $\NameCert$ row $N$ as its scoped primitive dependency surface. No additional modulus, completion, quotient-stream, or host-Real row is part of the scoped carrier.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-modulus-purity-boundary-carrier}. Its displayed tuple is $D,S,R_0,L,B,H,C,P,N$, so each accepted packet first exposes precisely those rows.

Use the NameCert obligations and dyadic witness stability. They keep the dyadic observation, StreamName window, RegSeqRat readback, Real seal, finite modulus budget, transport, replay, provenance, and local naming rows componentwise stable under admitted replacements.

The TasteGate marker identifies the same carrier surface on the Lean side, while the paper scope remains a theory-axis statement. Thus the scoped primitive dependency surface is exactly the displayed tuple and no hidden row is available.
\end{proof}

\begin{theorem}[Real modulus-purity boundary source nonescape]
\label{thm:real-modulus-purity-boundary-source-nonescape}
No consumer of an accepted $\RealModulusPurityBoundaryUp$ packet may replace the scoped rows $D,S,R_0,L,B,H,C,P,N$ by hidden modulus extraction, quotient streams, selected host limits, $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, $\mathsf{propext}$, or ambient $\RealUp$ completeness. Every Real-facing read remains routed through the displayed dyadic observation, StreamName window, RegSeqRat readback, Real seal, finite modulus-budget, transport, replay, provenance, and local NameCert rows.
\end{theorem}
\begin{proof}
Start from the scoped binding theorem. It gives the complete list of rows available to any accepted consumer, with $D,S,R_0,L,B$ as the analytic surface and $H,C,P,N$ as the structural surface.

Apply non-escape, host-choice refusal, tail-budget exhaustion, and dyadic witness stability. These results exclude hidden modulus coordinates, quotient streams, selected host limits, proof-extensional rows, and ambient completeness while preserving the displayed dyadic and StreamName route.

Finally replay through $C$ and record through $P,N$. Since transport and replay only preserve the displayed rows, a consumer cannot introduce $\mathsf{Classical.choice}$, $\mathsf{Quot.sound}$, $\mathsf{propext}$, or a host Real-completeness principle as a replacement source.
\end{proof}

\begin{theorem}[Real modulus-purity boundary RegSeqRat readback induction]
\label{thm:real-modulus-purity-boundary-regseqrat-readback-induction}
Let
$$
R=(D,S,R_0,L,B,H,C,P,N)
$$
be an accepted $\RealModulusPurityBoundaryUp$ packet. Any finite induction over the displayed $\StreamNameUp$ windows of $S$ preserves the $\RegSeqRatUp$ readback row $R_0$, the terminal $\RealUp$ seal $L$, and the axiom-refusal budget $B$ through componentwise $\hsame$ transport and displayed $\Cont$ replay. In particular, an inductive consumer that extends, refines, or exhausts the visible windows may not replace $R_0,L,B$ by a quotient stream, selected host limit, hidden modulus, ambient Real-completeness row, $\mathsf{Classical.choice}$ row, $\mathsf{Quot.sound}$ row, or $\mathsf{propext}$ row.
\end{theorem}
\begin{proof}
The base window is the carrier projection of \autoref{def:real-modulus-purity-boundary-carrier}. The displayed tuple already contains the dyadic observation $D$, StreamName window $S$, RegSeqRat readback $R_0$, Real seal $L$, refusal budget $B$, transport $H$, replay $C$, provenance $P$, and local naming row $N$.

For the induction step, apply the NameCert obligations of \autoref{thm:real-modulus-purity-boundary-namecert-obligations}. Each admitted window extension must still expose the dyadic row, finite StreamName row, RegSeqRat readback, RealUp seal, and budget-refusal row before any Real-facing read is exported.

Tail-budget exhaustion, \autoref{thm:real-modulus-purity-boundary-tail-budget-exhaustion}, prevents the step from creating a second modulus or hidden tail coordinate. Dyadic witness stability, \autoref{thm:real-modulus-purity-boundary-dyadic-witness-stability}, keeps replacements inside the same StreamName window routed through $H$ and replayed by $C$.

The Lean-checked TasteGate alignment marker for the carrier identifies the same packet shape used by these paper obligations. Therefore the finite induction can only transport and replay $R_0,L,B$ along the displayed rows, and the refused host-choice and proof-extensional coordinates remain outside the accepted packet.
\end{proof}

\begin{closurestatus}{\RealModulusPurityBoundaryUp}
  \constructivestory{The Real modulus-purity boundary is generated by taking the modulus rows already recorded in $BHist$ and making them the only admissible path to a Real-facing consumer. The carrier first reads the dyadic, window, readback, seal, and finite modulus-budget rows, then records the purity boundary that forbids hidden completion or host-real rows, and finally carries a constructive prediction through $\Cont$ extensions of that same route. The $hsame$ relation ensures that a different representative of the history cannot alter the modulus path or weaken the boundary. Thus the object is a visible purity test for Real use, grounded in ledger rows rather than ambient real analysis.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{This chapter binds the Real modulus-purity boundary carrier, NameCert obligations, non-escape theorem, constructive-prediction theorem, dyadic witness stability, scoped primitive dependency surface, source nonescape theorem, RegSeqRat readback induction, and the TasteGate marker.}
  \formalstatus{\encodedDefV}
  \bridgestatus{none}
  \origin{ai}
  \notclaimed{It does not claim ambient Real completeness, hidden modulus extraction, or equality of Real representatives beyond the recorded modulus boundary.}
  \upgradepath{Scoped closure requires checked Lean proofs for the source nonescape, readback induction, and transport replay obligations without bridge checking.}
\end{closurestatus}
