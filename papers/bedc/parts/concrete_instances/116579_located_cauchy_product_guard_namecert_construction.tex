\chapter{A Concrete Naming Certificate for $\LocatedCauchyProductGuardUp$}
\label{ch:concrete-instances-located-cauchy-product-guard-namecert}
\origin{human}

Following Bishop and Bridges on located constructive real names and product stability of regular Cauchy data, the $\LocatedCauchyProductGuardUp$ packet records the finite guard that lets a product read proceed only after both factor names expose located dyadic tolerance windows. It sits over the dyadic arithmetic core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, the product packet of \autoref{ch:concrete-instances-cauchyproduct-namecert}, and the regular product route of \autoref{ch:concrete-instances-regularcauchyproduct-namecert}. The packet gives later Real algebra and completion consumers a non-quotient product-stability guard over displayed rows only.

\begin{definition}[Located Cauchy product guard carrier]
\label{def:located-cauchy-product-guard-carrier}
A $\LocatedCauchyProductGuardUp$ carrier is a finite $\BHist$ packet
$$
G=(L,R,W_L,W_R,D_L,D_R,Q,M,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $L$ and $R$ are the two accepted regular Cauchy source rows whose product is being guarded.
\item $W_L$ and $W_R$ are $\StreamNameUp$ finite windows exposing the factor observations read for the current product request.
\item $D_L$ and $D_R$ are located $\DyadicRatCoreUp$ tolerance-window rows. Each row names a dyadic radius, a finite inspection bound, and the located apartness or enclosure evidence needed before multiplication is read.
\item $Q$ is the $\CauchyProductUp$ product-source row assembled from the two windows and tolerance rows.
\item $M$ is the $\RegularCauchyProductUp$ handoff row, available only after $Q,D_L,D_R,W_L,W_R$ are displayed.
\item $E$ is the $\RealUp$ seal row reached through the regular rational readback, not by a selected product limit.
\item $H$ records componentwise $\hsame$ transport for the source rows, windows, dyadic tolerance rows, product row, regular handoff, seal, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from each product-stability request through
$$
L,R,W_L,W_R,D_L,D_R,Q,M \longmapsto E,H,P,N .
$$
\item $P$ is the $\Pkg$ provenance over $\LocatedCauchyProductGuardUp$, $\CauchyProductUp$, $\RegularCauchyProductUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedCauchyProductGuardUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $L,R,W_L,W_R,D_L,D_R,Q,M,E,H,C,P,N$. It has no coordinate for Real completeness, quotient equality of names, a selected product limit, trichotomy, countable choice, or host multiplication.
\end{definition}

\begin{theorem}[Located Cauchy product guard NameCert obligations]
\label{thm:located-cauchy-product-guard-namecert-obligations}
For every accepted $\LocatedCauchyProductGuardUp$ carrier
$$
G=(L,R,W_L,W_R,D_L,D_R,Q,M,E,H,C,P,N),
$$
the local $\NameCert_{\LocatedCauchyProductGuardUp}$ surface consists exactly of carrier admission for the two regular Cauchy source rows, finite $\StreamNameUp$ window admission for both factors, located $\DyadicRatCoreUp$ tolerance-window exactness, $\CauchyProductUp$ source assembly, $\RegularCauchyProductUp$ handoff, terminal $\RealUp$ sealing through regular readback, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-cauchy-product-guard-carrier}. The only rows available to a certificate checker are the two source rows, the two finite windows, the two located dyadic tolerance rows, the product row, the regular handoff, the terminal seal, and the structural rows.

The located tolerance rows are read before the product row. Thus $Q$ cannot be consumed until $D_L,D_R,W_L,W_R$ have fixed the finite dyadic windows for the current request.

The regular product handoff $M$ is downstream of $Q$. The rows $H,C,P,N$ transport, replay, source, and name that same finite route, so the listed rows exhaust the local certificate surface without adding completion, quotient, selected-limit, choice, trichotomy, or host multiplication data.
\end{proof}

\begin{theorem}[Located Cauchy product guard regular product handoff]
\label{thm:located-cauchy-product-guard-regular-product-handoff}
Every $\RegularCauchyProductUp$ or $\RealUp$ consumer of an accepted $\LocatedCauchyProductGuardUp$ packet must first expose the finite route
$$
\StreamNameUp,\quad
\DyadicRatCoreUp,\quad
\CauchyProductUp,\quad
\RegSeqRatUp,\quad
\RegularCauchyProductUp,\quad
\RealUp .
$$
For each displayed product request, the guard reads both factor windows, matches them with located dyadic tolerance windows, assembles the $\CauchyProductUp$ row, and only then releases the regular product handoff and terminal seal.
\end{theorem}
\begin{proof}
Start with a product-facing read. Carrier admission exposes the factor rows and finite windows, but neither $M$ nor $E$ is available before the located tolerance rows have been displayed.

Read the dyadic guard. The rows $D_L$ and $D_R$ bind the requested product tolerance to concrete dyadic windows on the two factor names. This is a finite $\BHist$ read, so no selected limit or ambient completeness principle enters the route.

Now consume $Q$ and $M$. The product row is assembled from the displayed factor windows, and the regular handoff is downstream of that assembly. Componentwise $\hsame$ transport and displayed $\Cont$ replay preserve the same row order for every accepted packet.

The terminal $\RealUp$ seal is therefore a downstream read of the regular product handoff. Since the packet never stores quotient equality of names, host multiplication, countable choice, trichotomy, or Real completeness, none of those objects can be exported by the handoff.
\end{proof}

\falsifiablePrediction{Every accepted $\LocatedCauchyProductGuardUp$ product-stability read factors through two regular Cauchy source rows, paired StreamName windows, located DyadicRatCore tolerance windows, a CauchyProduct row, RegSeqRat readback, a RegularCauchyProduct handoff, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert row.}

\independenceWitness{cauchyproduct, regularcauchyproduct, dyadicratcore, streamname, regseqrat, real: $\LocatedCauchyProductGuardUp$ is not bijective to any listed sibling because it jointly carries two located factor windows, dyadic tolerance guards, product-source assembly, regular product handoff, Real sealing, transport, replay, provenance, and local naming.}

\closureat{LocatedCauchyProductGuardUp}{seedStr}

\begin{closurestatus}{\LocatedCauchyProductGuardUp}
  \constructivestory{A $\LocatedCauchyProductGuardUp$ packet is generated from two regular Cauchy source rows, paired StreamName windows, located DyadicRatCore tolerance windows, a CauchyProduct row, RegSeqRat readback, a RegularCauchyProduct handoff, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-cauchy-product-guard-carrier}, \autoref{thm:located-cauchy-product-guard-namecert-obligations}, and \autoref{thm:located-cauchy-product-guard-regular-product-handoff} seed the finite located-tolerance guard for product stability of regular Cauchy names.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Real completeness, quotient equality of names, selected product limits, countable choice, trichotomy, host multiplication, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, located dyadic tolerance-window exactness, finite-window product assembly, regular product handoff, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.LocatedCauchyProductGuardUp.}
\end{closurestatus}
