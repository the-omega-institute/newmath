\chapter{A Concrete Naming Certificate for $\ConstructiveUltrametricCompletionUp$}
\label{ch:concrete-instances-constructive-ultrametric-completion-namecert}
\origin{human}

Following Schikhof's constructive treatment of non-Archimedean normed spaces and the completion conventions used by Bosch, Guentzer, and Remmert, the $\ConstructiveUltrametricCompletionUp$ packet records an ultrametric completion boundary as finite NameCert data. It sits beside the metric source of \autoref{ch:concrete-instances-metric-namecert}, the strong-triangle source of \autoref{ch:concrete-instances-ultrametricspace-namecert}, the abstract completion interface of \autoref{ch:concrete-instances-completion-namecert}, the separated-completion boundary of \autoref{ch:concrete-instances-separatedcompletion-namecert}, and the dyadic-radius ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}. The completion boundary consumes the strong-triangle comparison from \autoref{ch:concrete-instances-ultrametricspace-namecert} and the nested-radius witness from \autoref{ch:concrete-instances-nestedclosedball-namecert} before any $\CompletionUp$ interface row is exposed. The ultrametric source row is read through \autoref{ch:concrete-instances-ultrametricspace-namecert} before the dyadic radius ledger and separated-completion handoff are consumed. The packet records the displayed strong-triangle radius route and its separated handoff; it is not a quotient completion, a selected-representative construction, a host equality principle, countable choice, or ambient Real completeness.

\begin{definition}[Constructive ultrametric completion carrier]
\label{def:constructive-ultrametric-completion-carrier}
A $\ConstructiveUltrametricCompletionUp$ carrier is a finite $\BHist$ packet
$$
U=(X,D,T,F,S,Q,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $X$ is the ultrametric source row, recording the underlying $\MetricSpaceUp$ carrier and its distance classifier.
\item $D$ is the $\DyadicRatCoreUp$ strong-triangle radius ledger, recording dyadic radii and the maximum-radius comparison used in every inspected triple.
\item $T$ is the strong-triangle row, recording that the inspected distance requests factor through the ultrametric inequality at the radii displayed by $D$.
\item $F$ is the Cauchy filter or Cauchy sequence route row, recording the finite tail requests and radius thresholds used by the completion consumer.
\item $S$ is the $\SeparatedCompletionUp$ handoff row, recording the separated boundary reached after the same Cauchy route is displayed.
\item $Q$ is the $\CompletionUp$ interface row, recording the completion-facing request without adding quotient classes or selected representatives.
\item $H$ stores componentwise $\hsame$ transport for the ultrametric source, radius ledger, strong-triangle row, Cauchy route, separated handoff, completion interface, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from distance reads to strong-triangle radius checks, from Cauchy requests to separated handoff, and from the handoff to the completion interface.
\item $P$ is the $\Pkg$ provenance over $\ConstructiveUltrametricCompletionUp$, $\MetricSpaceUp$, $\CompletionUp$, $\SeparatedCompletionUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\ConstructiveUltrametricCompletionUp}$ row.
\end{enumerate}
The accepted carrier stores only the rows just displayed. It carries no quotient-completion object, no selected Cauchy representative, no host equality of limits, no countable-choice selector, and no ambient $\RealUp$ completeness coordinate.
\end{definition}

\begin{theorem}[Constructive ultrametric completion NameCert obligations]
\label{thm:constructive-ultrametric-completion-namecert-obligations}
For an accepted packet $U=(X,D,T,F,S,Q,H,C,P,N)$, the local $\NameCert_{\ConstructiveUltrametricCompletionUp}$ obligations are exhausted by carrier admission for the ultrametric source $X$, dyadic radius ledger $D$, strong-triangle row $T$, Cauchy filter or sequence route $F$, separated-completion handoff $S$, completion-interface row $Q$, componentwise transport $H$, displayed replay $C$, package provenance $P$, and local naming row $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:constructive-ultrametric-completion-carrier}. The source metric data is read through $X$, while the ultrametric strengthening is visible only through the strong-triangle row $T$ and the dyadic radius ledger $D$.

Read the completion-facing route next. The Cauchy filter or sequence row $F$ reads the strong-triangle rows of \autoref{ch:concrete-instances-ultrametricspace-namecert} and the nested-radius witness of \autoref{ch:concrete-instances-nestedclosedball-namecert} before the separated handoff row $S$ and completion-interface row $Q$ are available. The row $F$ displays the finite tail and radius requests, and the separated-completion row $S$ records the handoff before the abstract completion interface $Q$ can be consumed.

Finally inspect $H,C,P,N$. These rows transport, replay, package, and name the same displayed data through $\MetricSpaceUp$, $\CompletionUp$, $\SeparatedCompletionUp$, and $\DyadicRatCoreUp$. Since the packet has no quotient-completion, selected-representative, host-equality, countable-choice, or Real-completeness coordinate, no NameCert obligation can read one.
\end{proof}

\begin{theorem}[Constructive ultrametric completion separated-radius route]
\label{thm:constructive-ultrametric-completion-separated-radius-route}
Let $U=(X,D,T,F,S,Q,H,C,P,N)$ be an accepted $\ConstructiveUltrametricCompletionUp$ packet. Every completion-facing read routes the ultrametric strong-triangle row $T$ and dyadic radius ledger $D$ through the same Cauchy filter or sequence row $F$ before the $\SeparatedCompletionUp$ handoff $S$ can be consumed. The abstract completion interface $Q$ is available only after that separated-radius route has displayed the ultrametric source $X$, the dyadic ledger $D$, the strong-triangle row $T$, the Cauchy route $F$, and the separated handoff $S$.
\end{theorem}
\begin{proof}
Project the carrier in \autoref{def:constructive-ultrametric-completion-carrier}. The NameCert obligations of \autoref{thm:constructive-ultrametric-completion-namecert-obligations} already force completion-facing reads to pass through the displayed ultrametric source, dyadic radius ledger, strong-triangle row, Cauchy route, and separated-completion handoff.

Induct over the displayed finite radius-request list carried by $D$ and read by $F$. The empty list has no completion-facing radius request, so the separated handoff cannot be consumed except through the displayed structural rows.

For the cons step, the head request reads the same source $X$, dyadic ledger $D$, and strong-triangle row $T$ before it enters the Cauchy filter or sequence row $F$. The induction hypothesis preserves that route for the tail requests, so the separated handoff $S$ remains attached to the same finite radius ledger before $Q$ appears.

Thus each finite request preserves the ultrametric source, dyadic radius ledger, strong-triangle row, and separated handoff. No ambient completion, quotient limit, selected representative, or Real-completeness coordinate is available before the completion interface row is read.
\end{proof}

\falsifiablePrediction{Every accepted $\ConstructiveUltrametricCompletionUp$ completion-facing read passes through an ultrametric source row, a strong-triangle dyadic radius ledger, a Cauchy filter or sequence route, a separated-completion handoff, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming before any completion interface row is visible.}

\independenceWitness{metricspace, completion, separatedcompletion, dyadicratcore: none is bijective to ConstructiveUltrametricCompletionUp because the packet jointly carries the ultrametric source, strong-triangle radius ledger, Cauchy route, separated handoff, completion interface, transport, replay, provenance, and local NameCert rows.}

\closureat{ConstructiveUltrametricCompletionUp}{seedStr}

\begin{closurestatus}{\ConstructiveUltrametricCompletionUp}
  \constructivestory{A $\ConstructiveUltrametricCompletionUp$ packet is generated from an ultrametric source row, a dyadic strong-triangle radius ledger, a Cauchy filter or sequence route, a separated-completion handoff, an abstract completion-interface row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:constructive-ultrametric-completion-carrier} and \autoref{thm:constructive-ultrametric-completion-namecert-obligations} seed the finite ultrametric completion packet over MetricSpaceUp, CompletionUp, SeparatedCompletionUp, DyadicRatCoreUp, BHist, hsame, Cont, Pkg, and NameCert rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim quotient completion, selected representatives, host equality of limits, countable choice, ambient Real completeness, Lean verification, or a standard bridge.}
  \upgradepath{Obligation closure requires checked carrier admission, strong-triangle radius ledger exactness, Cauchy-route stability, separated-completion handoff, and completion-interface nonescape for BEDC.Derived.ConstructiveUltrametricCompletionUp.}
\end{closurestatus}
