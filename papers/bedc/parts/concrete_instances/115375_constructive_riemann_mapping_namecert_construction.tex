\chapter{A Concrete Naming Certificate for $\ConstructiveRiemannMappingUp$}
\label{ch:concrete-instances-constructive-riemann-mapping-namecert}
\origin{human}

Following Riemann, Caratheodory, and Bishop-style constructive complex analysis, the $\ConstructiveRiemannMappingUp$ packet records the finite NameCert surface needed to read a displayed simply connected planar domain through a normalized disk map route. It sits over the holomorphic source of \autoref{ch:concrete-instances-holomorphic-namecert}, the complex carrier of \autoref{ch:concrete-instances-complex-namecert}, the complex-topology disk and compact-window rows of \autoref{ch:concrete-instances-complex-topology-namecert}, the normal-family surface of \autoref{ch:concrete-instances-montel-theorem-namecert}, the pointed disk control of \autoref{ch:concrete-instances-schwarz-lemma-namecert}, the finite observation schedule of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names only the displayed approximation, normalization, and nonescape rows; it does not assert host open-cover compactness, select a conformal map, identify quotient function equality, invoke countable choice, or import ambient $\ComplexUp$ completeness.

\begin{definition}[Constructive Riemann mapping carrier]
\label{def:constructive-riemann-mapping-carrier}
A $\ConstructiveRiemannMappingUp$ carrier is a finite $\BHist$ packet
$$
R=(D,C,T,H,M,S,W,Q,E,N,Z,P,L)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the domain row, naming a connected simply connected planar source through $\ComplexTopologyUp$ open-disk, compact-window, and boundary-separation data.
\item $C$ is the $\ComplexUp$ coordinate row for the displayed source point, disk center, boundary witnesses, and target disk coordinates.
\item $T$ is the $\HolomorphicUp$ trial-map family row over the source and target disk surfaces.
\item $H$ is the normalization row, recording the selected base point, disk origin target, positive derivative-orientation ledger, and boundary separation budget as displayed finite data.
\item $M$ is the $\MontelTheoremUp$ normal-family row consumed only after the compact-window and local-boundedness data in $D,T,H$ have been exposed.
\item $S$ is the $\SchwarzLemmaUp$ pointed-disk control row, used to compare any two normalized candidates through the same displayed disk route.
\item $W$ is the $\StreamNameUp$ finite observation schedule for trial maps, compact windows, and boundary tests.
\item $Q$ is the $\RegSeqRatUp$ readback row serving the real and complex coordinate windows named by $W$.
\item $E$ is the terminal $\RealUp$ seal row for radii, separation budgets, and modulus comparisons.
\item $N$ is the nonescape row excluding host open-cover compactness, selected conformal maps, quotient function equality, countable choice, and ambient $\ComplexUp$ completeness from the packet.
\item $Z$ records componentwise $\hsame$ transport for all displayed rows.
\item $P$ records displayed $\Cont$ replay and $\Pkg$ provenance over $\ConstructiveRiemannMappingUp$, $\HolomorphicUp$, $\ComplexUp$, $\ComplexTopologyUp$, $\MontelTheoremUp$, $\SchwarzLemmaUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $L$ is the local $\NameCert_{\ConstructiveRiemannMappingUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,C,T,H,M,S,W,Q,E,N,Z,P,L$. It has no coordinate for a host-chosen conformal equivalence, quotient equality of holomorphic functions, arbitrary open-cover compactness, countable-choice selection, or completed ambient complex function space.
\end{definition}

\begin{theorem}[Constructive Riemann mapping NameCert obligations]
\label{thm:constructive-riemann-mapping-namecert-obligations}
For every accepted $\ConstructiveRiemannMappingUp$ carrier
$$
R=(D,C,T,H,M,S,W,Q,E,N,Z,P,L),
$$
the local $\NameCert_{\ConstructiveRiemannMappingUp}$ surface consists exactly of domain admission through $D$, complex-coordinate admission through $C$, holomorphic trial-family exposure through $T$, normalization through $H$, normal-family compact-window routing through $M$, pointed-disk comparison through $S$, finite observation through $W$, regular rational readback through $Q$, Real seal admission through $E$, nonescape through $N$, componentwise transport through $Z$, displayed replay and provenance through $P$, and local naming through $L$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:constructive-riemann-mapping-carrier}. The domain, complex-coordinate, holomorphic-family, normalization, normal-family, Schwarz-control, observation, readback, seal, nonescape, transport, provenance, and local-name rows are the complete visible packet.

The analytic route starts with $D,C,T,H$. These rows expose the source domain, the complex coordinates, the trial holomorphic family, and the normalization ledger before any normal-family or disk comparison read is available.

Next read $M,S,W,Q,E$. The normal-family row is tied to the displayed compact windows and local boundedness data, while the Schwarz row compares normalized candidates only through the same pointed disk route. The stream, regular rational, and Real rows keep the observation and modulus data finite.

Finally $N,Z,P,L$ preserve the boundary. Since the packet has no coordinate for host open-cover compactness, selected maps, quotient function equality, countable choice, or ambient complex completeness, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Constructive Riemann mapping normal-family route]
\label{thm:constructive-riemann-mapping-normal-family-route}
Every normal-family read exported by an accepted $\ConstructiveRiemannMappingUp$ packet factors through the displayed route
$$
D,C,T,H \longmapsto M \longmapsto S \longmapsto W,Q,E \longmapsto N,Z,P,L .
$$
Thus a consumer may inspect compact-window normality, normalized holomorphic candidates, pointed disk comparison, finite observation windows, regular rational readbacks, Real seals, transport, replay, provenance, and local naming. It cannot read the packet as an ambient existence theorem for a selected conformal map.
\end{theorem}
\begin{proof}
Begin with the source rows. The domain row $D$ and complex coordinate row $C$ supply the displayed planar source, target disk coordinates, compact windows, and boundary-separation data.

The trial family and normalization rows $T,H$ then provide the only admissible input to $M$. The $\MontelTheoremUp$ row can be consumed only through these displayed compact windows and local boundedness ledgers, so the route is normal-family data rather than open-cover compactness.

Use $S$ after $M$. The $\SchwarzLemmaUp$ row compares normalized candidates through the pointed disk route and prevents the handoff from becoming an unconstrained function-space equality.

The observation rows $W,Q,E$ keep every approximation and modulus comparison inside finite StreamName, RegSeqRat, and Real seals. The structural rows $N,Z,P,L$ then transport, replay, source, and name the same route without adding a selected conformal map.
\end{proof}

\begin{theorem}[Constructive Riemann mapping Real and Complex seal nonescape]
\label{thm:constructive-riemann-mapping-real-complex-seal-nonescape}
Any $\RealUp$ or $\ComplexUp$ read exported by an accepted $\ConstructiveRiemannMappingUp$ packet remains inside the displayed rows $C,W,Q,E,Z,P,L$ and is scoped by the domain, trial-family, normalization, normal-family, Schwarz-control, and nonescape rows $D,T,H,M,S,N$. No consumer may reinterpret those seals as host open-cover compactness, a selected conformal map, quotient equality of holomorphic functions, countable choice, or ambient $\ComplexUp$ completeness.
\end{theorem}
\begin{proof}
Project the carrier and isolate the seal rows. The complex coordinate row $C$, stream schedule $W$, regular rational readback $Q$, and Real seal $E$ are displayed coordinates of the same packet, with transport, replay, provenance, and local naming stored in $Z,P,L$.

The analytic rows $D,T,H,M,S$ scope those seals. A Real or Complex read reaches the source domain, trial family, normalization, normal-family compact-window route, and pointed disk comparison before it can use any sealed coordinate.

The nonescape row $N$ records the excluded endpoints as absent carrier coordinates. Therefore a sealed real or complex observation remains finite BEDC data and cannot be retyped as open-cover compactness, a chosen conformal equivalence, quotient function equality, countable choice, or ambient complex completeness.
\end{proof}

\falsifiablePrediction{If a purported $\ConstructiveRiemannMappingUp$ packet exports a normalized disk map without displaying the MontelTheoremUp normal-family row and the SchwarzLemmaUp pointed comparison row, it is not a $\ConstructiveRiemannMappingUp$ carrier.}

\independenceWitness{holomorphic, complex, complex-topology, montel-theorem, schwarz-lemma, streamname, regseqrat, real: no listed sibling supplies the joint carrier containing displayed domain rows, holomorphic trial-family rows, normalization, normal-family routing, pointed disk comparison, finite observation, regular rational readback, Real and Complex seals, nonescape, transport, replay, provenance, and local NameCert data while refusing host open-cover compactness, selected conformal maps, quotient function equality, countable choice, and ambient Complex completeness.}

\closureat{ConstructiveRiemannMappingUp}{seedStr}

\begin{closurestatus}{\ConstructiveRiemannMappingUp}
  \constructivestory{A $\ConstructiveRiemannMappingUp$ packet is generated from a ComplexTopology domain row, Complex coordinate rows, Holomorphic trial-map family rows, a normalization ledger, MontelTheorem normal-family routing, SchwarzLemma pointed-disk comparison, StreamName finite observation windows, RegSeqRat readback, Real seals, nonescape, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:constructive-riemann-mapping-carrier}, \autoref{thm:constructive-riemann-mapping-namecert-obligations}, \autoref{thm:constructive-riemann-mapping-normal-family-route}, and \autoref{thm:constructive-riemann-mapping-real-complex-seal-nonescape} seed the finite normal-family and seal-nonescape route for constructive Riemann mapping data.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host open-cover compactness, selected conformal maps, quotient function equality, countable choice, ambient Complex completeness, Lean verification, or bridge checking for $\ConstructiveRiemannMappingUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, domain and complex-coordinate exposure, holomorphic trial-family routing, normalization, normal-family exactness, pointed disk comparison, finite observation, regular rational readback, Real and Complex seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.ConstructiveRiemannMappingUp.}
\end{closurestatus}
