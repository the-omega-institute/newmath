\chapter{A Concrete Naming Certificate for $\HyperbolicObserverPhaseShellUp$}
\label{ch:concrete-instances-hyperbolic-observer-phase-shell-namecert}
\origin{ai}

The $\HyperbolicObserverPhaseShellUp$ packet realizes the shell-level reading of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite boundary-shadow, visual-boundary, phase-distance, transport, provenance, and NameCert rows. It unifies the boundary-shadow route of \autoref{ch:concrete-instances-hyperbolic-boundary-shadow-namecert}, the visual boundary route of \autoref{ch:concrete-instances-hyperbolic-visual-boundary-namecert}, the phase-distance observer-expansion route of \autoref{ch:concrete-instances-phase-distance-observer-expansion-namecert}, the observer phase groupoid of \autoref{ch:concrete-instances-observer-phase-groupoid-namecert}, and the Poincare disk metric boundary of \autoref{ch:concrete-instances-poincare-disk-metric-namecert}. The shell is a boundary-facing packet, not a physical cosmology and not a center-inside-disk assertion.

\begin{definition}[Hyperbolic observer phase shell carrier]
\label{def:hyperbolic-observer-phase-shell-carrier}
A $\HyperbolicObserverPhaseShellUp$ carrier is a finite $\BHist$ packet
$$
S=(B,V,D,G,M,H,T,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the boundary-shadow row, recording the finite far-end shadow data through which observer expansion is read at the boundary rather than at an interior center.
\item $V$ is the visual-boundary row, recording the finite visual-boundary readback attached to the displayed boundary shadow.
\item $D$ is the phase-distance row, recording the finite distance comparison used by observer expansion before any global hyperbolic geometry is invoked.
\item $G$ is the observer phase groupoid row, recording source, target, inverse, and composition data for the reversible phase-surface transport visible in the shell.
\item $M$ is the Poincare-disk metric row, recording only the finite disk metric comparisons needed by $B,V,D,G$.
\item $H$ records componentwise $\hsame$ transport for boundary-shadow, visual-boundary, phase-distance, groupoid, disk-metric, replay, provenance, and local naming rows.
\item $T$ records displayed $\Cont$ replay from boundary shadows to visual-boundary reads, from visual-boundary reads to phase-distance comparisons, from phase-distance comparisons to observer phase transport, and from those rows to local naming.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicObserverPhaseShellUp$, hyperbolic observer expansion, hyperbolic visual boundary, phase-distance observer expansion, observer phase groupoid, Poincare disk metric, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\HyperbolicObserverPhaseShellUp}$ row.
\end{enumerate}
The classifier compares accepted shells only through $B,V,D,G,M,H,T,P,N$. It has no coordinate for an interior center of the far end, physical cosmology, host global hyperbolic space, untracked geodesic completion, or ambient real completeness.
\end{definition}

\begin{theorem}[Hyperbolic observer phase shell NameCert obligations]
\label{thm:hyperbolic-observer-phase-shell-namecert-obligations}
For every accepted $\HyperbolicObserverPhaseShellUp$ carrier $S=(B,V,D,G,M,H,T,P,N)$, the local $\NameCert_{\HyperbolicObserverPhaseShellUp}$ surface is exhausted by boundary-shadow admission, visual-boundary readback, phase-distance comparison, observer phase groupoid transport, Poincare-disk metric comparison, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. Every shell read factors through
$$
B,V,D,G,M \longmapsto H,T,P,N .
$$
\end{theorem}
\begin{proof}
Project the accepted shell carrier. The boundary-shadow, visual-boundary, phase-distance, observer phase groupoid, and disk-metric rows are the only geometric rows visible to the local certificate.

The structural rows preserve that visible route. Componentwise $\hsame$ transport can move only displayed coordinates, and displayed $\Cont$ replay can traverse only the listed route from boundary shadow through local naming.

The provenance and local naming rows record the same finite packet. Since no other row is present, a shell consumer receives no interior center, physical-cosmology datum, host global hyperbolic space, untracked geodesic completion, or ambient real-completeness principle.
\end{proof}

\begin{theorem}[Hyperbolic observer phase shell boundary nonescape]
\label{thm:hyperbolic-observer-phase-shell-boundary-nonescape}
Any observer-expansion read of an accepted $\HyperbolicObserverPhaseShellUp$ packet is boundary-facing: it must read the boundary-shadow row, visual-boundary row, phase-distance row, observer phase groupoid row, Poincare-disk metric row, transport, replay, provenance, and local naming before it may use the shell as phase geometry evidence. The read cannot move the far end into an interior disk center, interpret the shell as physical cosmology, or appeal to host global geometry, untracked completion, or ambient real completeness.
\end{theorem}
\begin{proof}
Use \autoref{thm:hyperbolic-observer-phase-shell-namecert-obligations} to expose the shell route. The only admissible reads are the rows $B,V,D,G,M,H,T,P,N$.

The boundary-shadow row $B$ and visual-boundary row $V$ force the read to remain boundary-facing. The phase-distance and groupoid rows $D,G$ may compare and transport observer phases, but they do so through displayed finite rows rather than through a hidden global hyperbolic object.

The disk-metric row $M$ supplies only the local Poincare metric comparisons named by the packet. Transport, replay, provenance, and local naming preserve that packet, so no excluded center, cosmology, completion, or ambient real-completeness object can enter the read.
\end{proof}

\falsifiablePrediction{Every shell read of an accepted $\HyperbolicObserverPhaseShellUp$ packet factors through boundary-shadow, visual-boundary, phase-distance, transport, provenance, and local $\NameCert$ rows before any observer-expansion conclusion is consumed.}

\independenceWitness{hyperbolic-observer-expansion, hyperbolic-visual-boundary, phase-distance-observer-expansion, observer-phase-groupoid, poincare-disk-metric: $\HyperbolicObserverPhaseShellUp$ is not bijective to any listed sibling because it uniquely names their boundary-shadow, visual-boundary, phase-distance, transport, provenance, and local NameCert rows as one shell-level carrier.}

The later TasteGate file for this shell must require Lean `Nontrivial` and `FieldFaithful` instances before any positive discovery or classifier-shift gate is recorded. Without those instances, the shell remains a finite NameCert packet and not a field-sensitive discovery claim.

\closureat{HyperbolicObserverPhaseShellUp}{seedStr}

\begin{closurestatus}{\HyperbolicObserverPhaseShellUp}
  \constructivestory{A $\HyperbolicObserverPhaseShellUp$ packet is generated from boundary-shadow rows, visual-boundary rows, phase-distance rows, observer phase groupoid rows, Poincare-disk metric rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-observer-phase-shell-carrier}, \autoref{thm:hyperbolic-observer-phase-shell-namecert-obligations}, and \autoref{thm:hyperbolic-observer-phase-shell-boundary-nonescape} seed the shell-level boundary-facing observer-expansion packet.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim an interior far-end center, physical cosmology, host global hyperbolic geometry, untracked geodesic completion, Lean verification, bridge checking, or ambient real completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, boundary-shadow admission, visual-boundary readback, phase-distance comparison, observer phase groupoid transport, disk-metric comparison, transport stability, replay exactness, and local NameCert replay for BEDC.Derived.HyperbolicObserverPhaseShellUp.}
\end{closurestatus}
