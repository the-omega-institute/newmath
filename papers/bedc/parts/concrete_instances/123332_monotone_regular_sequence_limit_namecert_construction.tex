\chapter{A Concrete Naming Certificate for $\MonotoneRegularSequenceLimitUp$}
\label{ch:concrete-instances-monotone-regular-sequence-limit-namecert}
\origin{human}

Following Bishop and Bridges, the $\MonotoneRegularSequenceLimitUp$ packet records the finite certificate surface for a bounded monotone regular rational sequence whose displayed windows determine a Real-facing limit seal. It sits over the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the finite stream windows of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the terminal Real-name seal of \autoref{ch:concrete-instances-real-namecert}, the bounded regular Cauchy surface of \autoref{ch:concrete-instances-bounded-regular-cauchy-namecert}, and the explicit Cauchy-rate realization route of \autoref{ch:concrete-instances-cauchy-rate-realization-namecert}. The packet names the constructive monotone-limit handoff as finite NameCert data; it is not ambient $\RealUp$ completeness, quotient stream equality, a least-upper-bound theorem, a selected host limit, trichotomy, countable choice, Lean verification, or bridge checking.

\begin{definition}[MonotoneRegularSequenceLimit carrier]
\label{def:monotone-regular-sequence-limit-carrier}
A $\MonotoneRegularSequenceLimitUp$ carrier is a finite $\BHist$ packet
$$
M=(S,B,\mu,W,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the monotone $\RegSeqRatUp$ source row, carrying the accepted rational readback together with the inspected monotonicity comparisons between adjacent finite windows.
\item $B$ is the located-bounds row, displaying lower and upper rational bounds for the same source windows and the comparison certificates that keep all inspected values inside those bounds.
\item $\mu$ is the explicit Cauchy modulus row, assigning a finite precision request to a finite monotone-tail window through the $\CauchyRateRealizationUp$ route.
\item $W$ is the $\StreamNameUp$ window row whose observations are the finite source windows and the selected tail windows named by $\mu$.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row, recording dyadic radii, endpoint comparisons, and residue budgets for the displayed monotone tails.
\item $R$ is the $\RegSeqRatUp$ readback row for the regular Cauchy tail produced from $S,B,\mu,W,D$.
\item $E$ is the $\RealUp$ seal row reached only after the bounded regular Cauchy route through $R$ has been displayed.
\item $H$ records componentwise $\hsame$ transport for the source row, bounds, modulus, windows, dyadic tolerances, readback, Real seal, continuation, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a finite precision request through the same source, bound, modulus, window, tolerance, readback, and seal rows.
\item $P$ is the $\Pkg$ provenance over $\MonotoneRegularSequenceLimitUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BoundedRegularCauchyUp$, $\CauchyRateRealizationUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\MonotoneRegularSequenceLimitUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only through $S,B,\mu,W,D,R,E,H,C,P,N$. It has no coordinate for ambient $\RealUp$ completeness, a supremum object outside the displayed bounds, quotient stream equality, host-order trichotomy, countable choice, or a detached Cauchy representative.
\end{definition}

\begin{theorem}[MonotoneRegularSequenceLimit NameCert obligations]
\label{thm:monotone-regular-sequence-limit-namecert-obligations}
For every accepted $\MonotoneRegularSequenceLimitUp$ carrier
$$
M=(S,B,\mu,W,D,R,E,H,C,P,N),
$$
the local $\NameCert_{\MonotoneRegularSequenceLimitUp}$ surface consists exactly of carrier admission for the eleven displayed rows; monotone $\RegSeqRatUp$ source admission through $S$; located-bound admission through $B$; explicit Cauchy modulus admission through $\mu$; finite $\StreamNameUp$ window admission through $W$; $\DyadicRatCoreUp$ tolerance admission through $D$; bounded regular Cauchy readback through $R$; terminal $\RealUp$ sealing through $E$; componentwise $\hsame$ transport through $H$; displayed $\Cont$ replay through $C$; $\Pkg$ provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:monotone-regular-sequence-limit-carrier}. Its analytic rows are precisely the monotone source $S$, the located bounds $B$, the modulus row $\mu$, the finite windows $W$, the dyadic tolerance ledger $D$, the regular readback $R$, and the Real seal $E$.

The NameCert obligations follow the displayed row order. A limit consumer first reads the monotone regular rational source and its located bounds, then the explicit Cauchy modulus, then the finite windows and dyadic tolerances, and only then the bounded regular Cauchy readback and terminal Real seal.

The structural rows $H,C,P,N$ transport, replay, source, and name the same route. Since the carrier has no hidden coordinate outside the displayed rows, these projections exhaust the local $\NameCert_{\MonotoneRegularSequenceLimitUp}$ surface.
\end{proof}

\begin{theorem}[MonotoneRegularSequenceLimit window extraction]
\label{thm:monotone-regular-sequence-limit-window-extraction}
Every finite precision request made against an accepted $\MonotoneRegularSequenceLimitUp$ carrier factors through the explicit window route
$$
S,B,\mu \longmapsto W,D \longmapsto R .
$$
Thus the requested regular tail is extracted from the displayed monotone $\RegSeqRatUp$ source, located bounds, Cauchy modulus, $\StreamNameUp$ windows, and $\DyadicRatCoreUp$ tolerance rows, without adding a second sequence, hidden modulus, quotient stream, selected host limit, or ambient completeness principle.
\end{theorem}
\begin{proof}
Start with a finite precision request. The only row that can answer such a request is the modulus row $\mu$, and $\mu$ is attached in the carrier to the monotone source $S$ and located bounds $B$.

The selected tail is not free-standing. It is displayed through the finite windows $W$ and checked against the dyadic tolerance row $D$, so the regular readback $R$ is obtained from the same inspected windows and the same bound data.

The carrier contains no further coordinate for a second sequence, hidden modulus, quotient stream, selected host limit, or ambient completeness theorem. Therefore every precision request factors through exactly the stated route.
\end{proof}

\begin{theorem}[MonotoneRegularSequenceLimit Real seal nonescape]
\label{thm:monotone-regular-sequence-limit-real-seal-nonescape}
An accepted $\MonotoneRegularSequenceLimitUp$ carrier may expose its $\RealUp$ seal row $E$ only after the displayed monotone source $S$, located bounds $B$, Cauchy modulus $\mu$, finite windows $W$, dyadic tolerance row $D$, and bounded regular Cauchy readback $R$ have been replayed through $H,C,P,N$. Consequently the packet cannot export ambient $\RealUp$ completeness, quotient stream equality, a least-upper-bound object outside the carrier, trichotomy, countable choice, or a detached Cauchy representative.
\end{theorem}
\begin{proof}
Take an accepted carrier and inspect the Real-facing row. The row $E$ is terminal in the carrier inventory, and the definition places it after the monotone source, bounds, modulus, windows, tolerance ledger, and regular readback.

Transport and replay preserve that same inventory. The $\hsame$ row $H$ moves the displayed coordinates componentwise, while the $\Cont$ row $C$ replays the finite precision route through the same $S,B,\mu,W,D,R,E$ data. Provenance and local naming record the same package rather than adding a separate analytic object.

Thus a Real-facing consumer can receive only the seal generated by the displayed bounded regular Cauchy route. Since the excluded objects are not carrier coordinates, none can escape through $E$.
\end{proof}

\falsifiablePrediction{An accepted $\MonotoneRegularSequenceLimitUp$ consumer that reaches a $\RealUp$ seal without displaying the monotone $\RegSeqRatUp$ source row $S$, located-bounds row $B$, explicit Cauchy modulus row $\mu$, $\StreamNameUp$ windows $W$, $\DyadicRatCoreUp$ tolerance row $D$, and bounded regular Cauchy readback row $R$ refutes the carrier surface.}

\independenceWitness{regseqrat, streamname, dyadicratcore, real, bounded-regular-cauchy, cauchy-rate-realization: no listed sibling supplies the whole eleven-row packet $M=(S,B,\mu,W,D,R,E,H,C,P,N)$, because each sibling contributes only regular readback, finite windows, dyadic tolerance, Real sealing, bounded Cauchy admission, or modulus realization while this chapter binds the monotone-limit handoff through transport, replay, provenance, and local naming.}

\closureat{MonotoneRegularSequenceLimitUp}{seedStr}

\begin{closurestatus}{\MonotoneRegularSequenceLimitUp}
  \constructivestory{A $\MonotoneRegularSequenceLimitUp$ packet is generated from a monotone $\RegSeqRatUp$ source row, located rational bounds, an explicit Cauchy modulus, finite $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance rows, bounded regular Cauchy readback, a terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:monotone-regular-sequence-limit-carrier}, \autoref{thm:monotone-regular-sequence-limit-namecert-obligations}, \autoref{thm:monotone-regular-sequence-limit-window-extraction}, and \autoref{thm:monotone-regular-sequence-limit-real-seal-nonescape} seed the finite monotone regular sequence limit route over located bounds, explicit Cauchy modulus data, window extraction, dyadic tolerance, bounded regular Cauchy readback, and the Real seal boundary.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close ambient $\RealUp$ completeness, least-upper-bound construction, quotient stream equality, host-order trichotomy, countable choice, Lean verification, or bridge checking for $\MonotoneRegularSequenceLimitUp$.}
  \upgradepath{Obligation closure requires checked carrier admission, monotone source admission, located-bound admission, Cauchy modulus realization, finite-window extraction, dyadic tolerance admission, bounded regular Cauchy readback, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.MonotoneRegularSequenceLimitUp.}
\end{closurestatus}
