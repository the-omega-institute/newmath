\chapter{A Concrete Naming Certificate for $\RegularCauchyTailWindowSelectorUp$}
\label{ch:concrete-instances-regular-cauchy-tail-window-selector-namecert}
\origin{human}

Following Bishop and Bridges on regular Cauchy real names, the $\RegularCauchyTailWindowSelectorUp$ packet records the finite cofinal-window selector used when a diagonal-limit or real-completion consumer asks for a tail window of a regular Cauchy family. It realizes the regular-Cauchy route of \autoref{sec:real-completeness-regular-cauchy} through the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the diagonal-limit representative packet of \autoref{ch:concrete-instances-diagonallimitrepresentative-namecert}. The selector is a displayed finite tail-window row, not a hidden choice function, quotient equality of real names, or ambient completeness principle.

\begin{definition}[Regular Cauchy tail-window selector carrier]
\label{def:regular-cauchy-tail-window-selector-carrier}
A $\RegularCauchyTailWindowSelectorUp$ carrier is a finite $\BHist$ packet
$$
T=(D,S,R,A,Q,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance row naming the requested positive tail budget.
\item $S$ is the $\StreamNameUp$ finite-window row for the family of regular Cauchy names.
\item $R$ is the $\RegSeqRatUp$ readback row attached to the inspected window.
\item $A$ is the tail-admission row, recording that the inspected window lies beyond the displayed Cauchy modulus request.
\item $Q$ is the cofinal selector row, naming the finite tail window consumed by the diagonal or completion-facing read.
\item $E$ is the $\RealUp$ seal row reached only after $D,S,R,A,Q$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the tolerance, window, readback, admission, selector, and seal rows.
\item $C$ records displayed $\Cont$ replay of the same selector route.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyTailWindowSelectorUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\DiagonalLimitRepresentativeUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyTailWindowSelectorUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $D,S,R,A,Q,E,H,C,P,N$. It has no coordinate for countable choice, a selected infinite subsequence, quotient real equality, hidden limits, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Regular Cauchy tail-window selector NameCert obligations]
\label{thm:regular-cauchy-tail-window-selector-namecert-obligations}
For every accepted $\RegularCauchyTailWindowSelectorUp$ carrier $T=(D,S,R,A,Q,E,H,C,P,N)$, the local $\NameCert_{\RegularCauchyTailWindowSelectorUp}$ surface consists exactly of dyadic tolerance admission, finite stream-window exposure, regular rational readback, tail-admission evidence, cofinal selector exposure, terminal Real sealing, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-tail-window-selector-carrier}. The rows $D,S,R,A,Q,E,H,C,P,N$ are the full selector packet, so there is no separate coordinate for an infinite choice schedule or hidden limit.

The finite selector is read in order. The dyadic tolerance row $D$ fixes the requested budget, $S$ supplies the inspected stream window, $R$ reads the regular rational data, and $A$ records that the window is admitted as a tail row.

The cofinal selector row $Q$ then names the window handed to diagonal and real-completion consumers. The rows $E,H,C,P,N$ seal, transport, replay, source, and name the same displayed route.
\end{proof}

\begin{theorem}[Regular Cauchy tail-window selector route]
\label{thm:regular-cauchy-tail-window-selector-route}
Every accepted $\RegularCauchyTailWindowSelectorUp$ packet supplies a finite tail-window selector for the diagonal-limit and real-completion route only through
$$
\DyadicRatCoreUp,\quad \StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp,\quad \DiagonalLimitRepresentativeUp .
$$
In carrier notation, the consumer-visible route is
$$
D,S,R,A,Q,E,H,C,P,N .
$$
The selector exports a cofinal finite window and its displayed tolerance/readback evidence; it exports no countable-choice schedule, hidden subsequence selector, quotient $\RealUp$ equality, selected limit, or ambient completeness theorem.
\end{theorem}
\begin{proof}
Start from the regular-Cauchy vision route of \autoref{sec:real-completeness-regular-cauchy}. A diagonal-limit consumer asks for one displayed tail window at a requested dyadic tolerance, not for a completed infinite schedule.

Read the sibling rows in order. The $\DyadicRatCoreUp$ row supplies the budget, the $\StreamNameUp$ row supplies the inspected finite window, and the $\RegSeqRatUp$ row supplies the regular rational readback consumed before the $\RealUp$ seal.

Finally connect the selector to \autoref{ch:concrete-instances-diagonallimitrepresentative-namecert}. The cofinal row $Q$ is the finite window handed to the diagonal representative, while $H,C,P,N$ transport, replay, source, and name the same packet. The refused choice, quotient, selected-limit, and completeness objects have no carrier coordinate.
\end{proof}

\falsifiablePrediction{An accepted $\RegularCauchyTailWindowSelectorUp$ read is refuted if a diagonal-limit or Real-completion consumer receives a tail window before the dyadic tolerance, StreamName window, RegSeqRat readback, tail-admission, cofinal selector, Real seal, transport, replay, provenance, and local NameCert rows are displayed.}

\independenceWitness{dyadicratcore, streamname, regseqrat, real, diagonallimitrepresentative: $\RegularCauchyTailWindowSelectorUp$ is not bijective to any listed sibling because it jointly carries a tolerance request, stream window, regular rational readback, tail admission, cofinal selector, Real seal, transport, replay, provenance, and local naming.}

\closureat{RegularCauchyTailWindowSelectorUp}{seedStr}

\begin{closurestatus}{\RegularCauchyTailWindowSelectorUp}
  \constructivestory{A $\RegularCauchyTailWindowSelectorUp$ packet is generated from DyadicRatCore tolerance rows, StreamName finite windows, RegSeqRat readback, tail-admission rows, cofinal selector rows, RealUp sealing, hsame transport, Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-tail-window-selector-carrier}, \autoref{thm:regular-cauchy-tail-window-selector-namecert-obligations}, and \autoref{thm:regular-cauchy-tail-window-selector-route} seed the finite cofinal-window selector route for diagonal-limit and real-completion consumers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close countable choice, quotient Real equality, hidden subsequence selection, selected limits, ambient completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic tolerance coverage, stream-window readback, tail-admission evidence, cofinal selector exactness, Real seal routing, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyTailWindowSelectorUp.}
\end{closurestatus}
