\chapter{A Concrete Naming Certificate for $\PoincareDiskBusemannCocycleUp$}
\label{ch:concrete-instances-poincare-disk-busemann-cocycle-namecert}
\origin{ai}

The $\PoincareDiskBusemannCocycleUp$ packet realizes the Poincare-disk branch of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite boundary-anchored cocycle data. It sits after the disk metric packet of \autoref{ch:concrete-instances-poincare-disk-metric-namecert}, the boundary-transport packet of \autoref{ch:concrete-instances-poincare-disk-boundary-transport-namecert}, the additive Busemann-cocycle route of \autoref{ch:concrete-instances-hyperbolic-busemann-cocycle-namecert}, the visual-metric packet of \autoref{ch:concrete-instances-hyperbolic-visual-metric-namecert}, and the horocycle carrier of \autoref{ch:concrete-instances-hyperbolic-horocycle-namecert}. The packet names a finite disk-model cocycle read; it is not a quotient ideal boundary, a selected geodesic ray, a completed hyperbolic plane, a physical observer law, or an ambient curvature theorem.

\begin{definition}[Poincare disk Busemann cocycle carrier]
\label{def:poincare-disk-busemann-cocycle-carrier}
A $\PoincareDiskBusemannCocycleUp$ carrier is a finite $\BHist$ packet
$$
C=(D,B,M,U,V,Z,O,E,H,T,G,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\PoincareDiskMetricUp$ disk-metric row over the displayed disk observations.
\item $B$ is the boundary-anchor row, read through $\PoincareDiskBoundaryTransportUp$, that records the boundary endpoint held visible by the transport.
\item $M$ is the $\HyperbolicBusemannCocycleUp$ additive cocycle row for the same boundary anchor and disk observations.
\item $U$ is the boundary-transport update row. It records the finite handoff from one disk observation window to another while retaining the boundary anchor $B$.
\item $V$ is the $\HyperbolicVisualMetricUp$ comparison row used when a visual-distance consumer reads the cocycle.
\item $Z$ is the $\HyperbolicHorocycleUp$ horocycle row that supplies the boundary-tangent level surface consumed by Busemann-style reads.
\item $O$ is the observer-expansion refusal row. It prevents the packet from being read as a physical observer trajectory, quotient boundary equality, selected ideal point, or completed disk model.
\item $E$ is the exactness ledger. It ties $D,B,M,U,V,Z,O$ to one cocycle read and records the displayed additivity and boundary-transport compatibility rows.
\item $H$ stores componentwise $\hsame$ transport for disk metric, boundary anchor, cocycle, boundary transport, visual metric, horocycle, refusal, exactness, replay, provenance, and local naming rows.
\item $T$ stores displayed $\Cont$ replay from a boundary-cocycle request through $D,B,M,U,V,Z,O,E,H$.
\item $G$ is the $\Pkg$ provenance over $\PoincareDiskBusemannCocycleUp$, $\PoincareDiskMetricUp$, $\PoincareDiskBoundaryTransportUp$, $\HyperbolicBusemannCocycleUp$, $\HyperbolicVisualMetricUp$, $\HyperbolicHorocycleUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PoincareDiskBusemannCocycleUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for completed ideal-boundary quotients, selected geodesic representatives, ambient hyperbolic planes, or physical observer dynamics.
\end{definition}

\begin{theorem}[Poincare disk Busemann cocycle boundary transport]
\label{thm:poincare-disk-busemann-cocycle-boundary-transport}
Every accepted $\PoincareDiskBusemannCocycleUp$ read that transports a Busemann comparison across disk windows must retain the route
$$
\PoincareDiskMetricUp,\ \PoincareDiskBoundaryTransportUp,\ \HyperbolicBusemannCocycleUp,\ \HyperbolicVisualMetricUp,\ \HyperbolicHorocycleUp
\longmapsto
D,B,M,U,V,Z,O,E,H,T,G,N .
$$
The boundary transport may carry the cocycle to a visual-metric or horocycle consumer only while the boundary anchor $B$, additive cocycle row $M$, transport row $U$, exactness ledger $E$, and refusal row $O$ remain displayed. It does not assert quotient boundary equality, selected geodesic rays, completed disk geometry, physical observer dynamics, or global curvature.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:poincare-disk-busemann-cocycle-carrier}. The disk observation row is $D$, the boundary anchor is $B$, and the additive Busemann comparison is $M$.

Read the boundary-transport and exactness rows next. The row $U$ moves the displayed disk window only with the same boundary anchor, while $E$ records that the additive cocycle and transport compatibility are inspected inside one finite packet.

The visual-metric and horocycle consumers are downstream. Rows $V$ and $Z$ may read only the comparison and horocycle data named by the same packet, and $O$ prevents the read from escaping into quotient boundary equality, selected geodesic representatives, completed disk geometry, physical observer dynamics, or global curvature. Rows $H,T,G,N$ then preserve, replay, source, and name the same finite route.
\end{proof}

\falsifiablePrediction{Every accepted $\PoincareDiskBusemannCocycleUp$ consumer must expose disk metric, boundary transport, Busemann cocycle, visual-metric, horocycle, exactness, refusal, transport, replay, provenance, and local NameCert rows before obtaining a boundary-cocycle handoff.}

\independenceWitness{poincare-disk-metric, poincare-disk-boundary-transport, hyperbolic-busemann-cocycle, hyperbolic-visual-metric, hyperbolic-horocycle: none is bijective to $\PoincareDiskBusemannCocycleUp$ because this packet jointly carries disk metric data, a retained boundary anchor, additive cocycle, boundary-window transport, visual-metric comparison, horocycle level data, exactness, refusal, structural replay, provenance, and local naming.}

The Lean TasteGate route must provide explicit $\Nontrivial\ \PoincareDiskBusemannCocycleUp$ and $\FieldFaithful\ \PoincareDiskBusemannCocycleUp$ instances. Nontriviality must distinguish an accepted packet with all rows $D,B,M,U,V,Z,O,E,H,T,G,N$ from a candidate missing the boundary anchor or exactness ledger. Field faithfulness must preserve those rows as distinct fields before a checked carrier-alignment theorem can serve as the TasteGate witness.

\closureat{PoincareDiskBusemannCocycleUp}{seedStr}

\begin{closurestatus}{\PoincareDiskBusemannCocycleUp}
  \constructivestory{A $\PoincareDiskBusemannCocycleUp$ packet is generated from PoincareDiskMetric rows, PoincareDiskBoundaryTransport rows, HyperbolicBusemannCocycle rows, HyperbolicVisualMetric rows, HyperbolicHorocycle rows, an exactness ledger, a refusal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:poincare-disk-busemann-cocycle-carrier} and \autoref{thm:poincare-disk-busemann-cocycle-boundary-transport} seed the finite Poincare-disk Busemann cocycle route through disk metric, boundary transport, additive cocycle, visual metric, horocycle, exactness, refusal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient ideal-boundary equality, selected geodesic rays, completed hyperbolic-plane geometry, physical observer dynamics, global curvature, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked disk-metric admission, boundary-anchor transport, Busemann cocycle exactness, visual-metric and horocycle routing, TasteGate nontriviality, field faithfulness, structural replay, provenance, and local naming for BEDC.Derived.PoincareDiskBusemannCocycleUp.}
\end{closurestatus}
