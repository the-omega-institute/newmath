\chapter{A Concrete Naming Certificate for $\FiniteCauchyWitnessMatrixUp$}
\label{ch:concrete-instances-finite-cauchy-witness-matrix-namecert}
\origin{human}

Following Bishop and Bridges, the $\FiniteCauchyWitnessMatrixUp$ packet records the finite witness grid by which a regular Cauchy construction answers a displayed family of precision requests without appealing to quotient sequence equality, countable choice, selected limits, or ambient completeness. It sits on the Real and Completion route after the finite observation interface of \autoref{ch:concrete-instances-streamname-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}, and the finite threshold-selection surface of \autoref{ch:concrete-instances-bishop-completion-modulus-selection-namecert}. The packet names a displayed matrix of witness rows; it does not name a completed real, a quotient class, or an unbounded selector.

\begin{definition}[Finite Cauchy witness matrix carrier]
\label{def:finite-cauchy-witness-matrix-carrier}
A $\FiniteCauchyWitnessMatrixUp$ carrier is a finite $\BHist$ packet
$$
W=(A,T,S,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ is the finite precision-request axis, listing the requests whose Cauchy witnesses are being recorded.
\item $T$ is the finite threshold axis, giving the admitted threshold row for each request in $A$.
\item $S$ is the $\StreamNameUp$ window matrix, recording the finite stream windows inspected at those thresholds.
\item $D$ is the $\DyadicRatCoreUp$ tolerance matrix, recording dyadic radii, denominator budgets, slack comparisons, and rowwise inequalities.
\item $R$ is the $\RegSeqRatUp$ readback matrix, obtained from the same windows and dyadic rows.
\item $E$ is the $\RealUp$ seal row, available only after $A,T,S,D,R$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for requests, thresholds, windows, dyadic tolerances, readbacks, seal, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from each precision request through its threshold, window, tolerance, readback, and seal rows.
\item $P$ is the $\Pkg$ provenance over $\FiniteCauchyWitnessMatrixUp$, $\BishopCompletionModulusSelectionUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\FiniteCauchyWitnessMatrixUp}$ row.
\end{enumerate}
The classifier compares accepted matrices only through these rows. It has no coordinate for quotient stream equality, a selected completed limit, countable choice over precision requests, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Finite Cauchy witness matrix NameCert obligations]
\label{thm:finite-cauchy-witness-matrix-namecert-obligations}
For every accepted $\FiniteCauchyWitnessMatrixUp$ carrier $W=(A,T,S,D,R,E,H,C,P,N)$, the local $\NameCert_{\FiniteCauchyWitnessMatrixUp}$ surface consists exactly of carrier admission for the finite precision requests, finite thresholds, $\StreamNameUp$ window matrix, $\DyadicRatCoreUp$ tolerance matrix, $\RegSeqRatUp$ readback matrix, terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming. A consumer that reads a Cauchy witness row must display the same $A,T,S,D,R,E,H,C,P,N$ packet before exporting its conclusion.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:finite-cauchy-witness-matrix-carrier}. The analytic rows are the request axis, threshold axis, stream-window matrix, dyadic tolerance matrix, readback matrix, and Real seal.

The threshold row is finite and displayed. The Bishop completion modulus selection route of \autoref{ch:concrete-instances-bishop-completion-modulus-selection-namecert} supplies the sibling discipline for reading a threshold before a window or tolerance row is consumed.

The window, tolerance, and readback rows are then forced to remain aligned. The sibling routes \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, and \autoref{ch:concrete-instances-regseqrat-namecert} provide the finite observation, dyadic, and regular-rational surfaces used by $S,D,R$.

Finally $E,H,C,P,N$ seal, transport, replay, source, and name the same finite matrix. Since the carrier contains no quotient, selected limit, choice schedule, host equality, or ambient completeness coordinate, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Finite Cauchy witness matrix Real handoff]
\label{thm:finite-cauchy-witness-matrix-real-handoff}
Every Real or Completion consumer of an accepted $\FiniteCauchyWitnessMatrixUp$ packet reaches the $\RealUp$ seal only through the displayed route
$$
A,T \longmapsto S,D \longmapsto R \longmapsto E \longmapsto H,C,P,N .
$$
Thus the handoff exposes finite precision requests, finite thresholds, $\StreamNameUp$ windows, $\DyadicRatCoreUp$ tolerance rows, $\RegSeqRatUp$ readbacks, the terminal $\RealUp$ seal, transport, replay, provenance, and local naming before any Real-facing conclusion is exported.
\end{theorem}
\begin{proof}
Start from the NameCert obligations. They require every witness read to display $A,T,S,D,R,E,H,C,P,N$ as one finite packet.

Read the threshold axis before the analytic rows. The selection sibling \autoref{ch:concrete-instances-bishop-completion-modulus-selection-namecert} keeps $T$ as a displayed finite threshold row rather than a hidden choice function.

Next read $S,D,R$ through the StreamName, DyadicRatCore, and RegSeqRat siblings. The same finite windows and tolerance entries feed the readback matrix, so the consumer cannot replace the matrix by quotient stream equality or by a selected limit.

The terminal handoff is the seal row $E$ of \autoref{ch:concrete-instances-real-namecert}, transported and replayed by $H$ and $C$ and sourced by $P,N$. Hence a Real or Completion consumer receives only the displayed finite witness matrix and not countable choice, host real equality, or ambient $\RealUp$ completeness.
\end{proof}

\falsifiablePrediction{An accepted $\FiniteCauchyWitnessMatrixUp$ consumer that reaches a $\RealUp$ seal without displaying the finite request axis, threshold axis, StreamName windows, DyadicRatCore tolerances, and RegSeqRat readbacks refutes the carrier surface.}

\independenceWitness{streamname, dyadicratcore, regseqrat, real, bishop-completion-modulus-selection: no listed sibling is bijective to $\FiniteCauchyWitnessMatrixUp$ because this packet jointly carries finite precision requests, finite thresholds, windows, tolerances, readbacks, a Real seal, transport, replay, provenance, and local NameCert rows as one witness matrix.}

\closureat{FiniteCauchyWitnessMatrixUp}{seedStr}

\begin{closurestatus}{\FiniteCauchyWitnessMatrixUp}
  \constructivestory{A $\FiniteCauchyWitnessMatrixUp$ packet is generated from finite precision requests, finite threshold rows, StreamName windows, DyadicRatCore tolerance rows, RegSeqRat readbacks, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:finite-cauchy-witness-matrix-carrier}, \autoref{thm:finite-cauchy-witness-matrix-namecert-obligations}, \autoref{thm:finite-cauchy-witness-matrix-real-handoff}, the falsifiable prediction, and the independence witness seed the finite witness-grid route for Real and Completion consumers.}
  \formalstatus{\formalTargetV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, quotient stream equality, selected completed limits, countable choice, host real equality, or ambient $\RealUp$ completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, threshold-row exposure, StreamName window readback, DyadicRatCore tolerance routing, RegSeqRat handoff, Real seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.FiniteCauchyWitnessMatrixUp.}
\end{closurestatus}
