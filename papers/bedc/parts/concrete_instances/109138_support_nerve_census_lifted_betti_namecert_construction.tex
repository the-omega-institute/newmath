\chapter{A Concrete Naming Certificate for $\SupportNerveCensusLiftedBettiUp$}
\label{ch:concrete-instances-support-nerve-census-lifted-betti-namecert}
\origin{ai}

The $\SupportNerveCensusLiftedBettiUp$ packet realizes the support-nerve census and codon-lifted Betti table of \autoref{subsec:philosophy-dna-window-six-boundary-tiles} as finite NameCert data. It consumes the partition-core incidence surface of \autoref{ch:concrete-instances-partition-core-incidence-namecert}, the simplicial-complex carrier of \autoref{ch:concrete-instances-simplicialcomplex-namecert}, the homology carrier of \autoref{ch:concrete-instances-homology-namecert}, the finite simplicial-chain surface of \autoref{ch:concrete-instances-finite-simplicial-chain-namecert}, the Hochster--nerve reduction of \autoref{ch:concrete-instances-hochster-nerve-betti-reduction-namecert}, and the DNA boundary-tile source of \autoref{ch:concrete-instances-dna-window-six-boundary-tile-namecert}. The packet names a finite support-to-nerve realization and lifted Betti census; it is not a host minimal-resolution oracle, stochastic biological model, quotient codon semantics, unrestricted homology computation, or coefficient-field completeness theorem.

\begin{definition}[SupportNerveCensusLiftedBetti carrier]
\label{def:support-nerve-census-lifted-betti-carrier}
A $\SupportNerveCensusLiftedBettiUp$ carrier is a finite $\BHist$ packet
$$
S=(I,U,V,F,H_0,B,L,G,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $I$ is the $\PartitionCoreIncidenceUp$ row naming the finite support-family incidence table used to enumerate the $15$ nonempty supports.
\item $U$ is the support-to-nerve realization row, assigning each accepted support family to the four-vertex nerve obtained from its displayed supports.
\item $V$ is the $\SimplicialComplexUp$ row naming the resulting nerve as a finite complex.
\item $F$ is the $\FiniteSimplicialChainUp$ row exposing the chain boundary surface used for reduced homology reads.
\item $H_0$ is the $\HomologyUp$ row for reduced homology in degrees $-1,0,1,2$.
\item $B$ is the lifted Betti ledger carrying the finite census counts and the codon lift substitution $z\mapsto 2x+x^2$.
\item $L$ is the $\HochsterNerveBettiReductionUp$ row that keeps the support-level nerve census upstream of any Betti-strand export.
\item $G$ records componentwise $\hsame$ transport for the incidence, realization, complex, chain, homology, lifted Betti, reduction, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from support-family reads to nerve realization, finite homology, and lifted Betti ledgers.
\item $P$ is the $\Pkg$ provenance over $\SupportNerveCensusLiftedBettiUp$, $\PartitionCoreIncidenceUp$, $\SimplicialComplexUp$, $\FiniteSimplicialChainUp$, $\HomologyUp$, $\HochsterNerveBettiReductionUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\SupportNerveCensusLiftedBettiUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $I,U,V,F,H_0,B,L,G,C,P,N$. It has no coordinate for hidden codon semantics, unrestricted minimal resolutions, ambient Betti-table equality, stochastic evolutionary interpretation, quotient support equality, or an external homology oracle.
\end{definition}

\begin{theorem}[SupportNerveCensusLiftedBetti NameCert obligations]
\label{thm:support-nerve-census-lifted-betti-obligations}
For every accepted $\SupportNerveCensusLiftedBettiUp$ carrier $S=(I,U,V,F,H_0,B,L,G,C,P,N)$, the local $\NameCert_{\SupportNerveCensusLiftedBettiUp}$ surface consists exactly of incidence enumeration, support-to-nerve realization, finite simplicial-complex admission, finite chain-boundary exposure, reduced homology readback in degrees $-1,0,1,2$, lifted Betti ledger exposure, Hochster--nerve reduction provenance, componentwise transport, displayed replay, package provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:support-nerve-census-lifted-betti-carrier}. The incidence, realization, complex, chain, homology, lifted Betti, reduction, transport, replay, provenance, and naming rows are exactly $I,U,V,F,H_0,B,L,G,C,P,N$.

The support-to-nerve route starts from $I$ and $U$. It can expose a nerve only through the finite $\SimplicialComplexUp$ row $V$, so the census cannot be replaced by a host powerset calculation or a quotient support object.

The homology and Betti rows are downstream of the finite chain boundary $F$. The row $H_0$ restricts homology reads to the displayed reduced degrees, while $B$ records the census and lift substitution only as finite ledger data.

Finally $L,G,C,P,N$ preserve, replay, source, and name the same route. Since no remaining coordinate stores hidden codon semantics, unrestricted resolutions, host Betti-table equality, stochastic interpretation, quotient support equality, or external homology computation, the listed obligations exhaust the local NameCert surface.
\end{proof}

\begin{theorem}[SupportNerveCensusLiftedBetti realization]
\label{thm:support-nerve-census-lifted-betti-realization}
Every accepted $\SupportNerveCensusLiftedBettiUp$ read factors through the finite route
$$
\PartitionCoreIncidenceUp
\longrightarrow
\SimplicialComplexUp
\longrightarrow
\FiniteSimplicialChainUp
\longrightarrow
\HomologyUp
\longrightarrow
\HochsterNerveBettiReductionUp .
$$
The exported census is the displayed support-family count, reduced-homology signature table, and lifted Betti ledger, not a biological causal model, unrestricted coefficient-field theorem, ambient minimal resolution, or host-level homology calculation.
\end{theorem}
\begin{proof}
Start from the NameCert obligations in \autoref{thm:support-nerve-census-lifted-betti-obligations}. They force support-family enumeration through $I$ and support-to-nerve realization through $U$ before any complex row is available.

Next the finite complex, chain, and homology rows $V,F,H_0$ expose the reduced homology signatures used by the census. These rows are displayed finite data, so the read cannot invoke an external simplicial-complex oracle or an unrestricted homology computation.

The lifted Betti ledger $B$ and Hochster--nerve row $L$ then package the codon lift as a finite strand read. Transport, replay, provenance, and local naming preserve that same route, which proves the stated realization boundary.
\end{proof}

\falsifiablePrediction{Every accepted $\SupportNerveCensusLiftedBettiUp$ read factors through displayed partition-core incidence, support-to-nerve realization, simplicial-complex, finite-chain, homology, lifted Betti, Hochster--nerve reduction, transport, replay, provenance, and local naming rows.}

\independenceWitness{partition-core-incidence, support-to-nerve-realization, simplicialcomplex, finite-simplicial-chain, homology, hochster-nerve-betti-reduction: $\SupportNerveCensusLiftedBettiUp$ is not bijective to any listed sibling because it jointly carries the support-family census, nerve realization, finite homology signature table, codon-lifted Betti ledger, and coefficient TasteGate boundary in one packet.}

The Lean follow-up must provide explicit $\mathsf{Nontrivial}\ \SupportNerveCensusLiftedBettiUp$ and $\mathsf{FieldFaithful}\ \SupportNerveCensusLiftedBettiUp$ TasteGate instances. Those instances must distinguish $I,U,V,F,H_0,B,L,G,C,P,N$ so the support census, nerve realization, and lifted Betti ledger cannot collapse into one sibling row.

\closureat{SupportNerveCensusLiftedBettiUp}{seedStr}

\begin{closurestatus}{\SupportNerveCensusLiftedBettiUp}
  \constructivestory{A $\SupportNerveCensusLiftedBettiUp$ packet is generated from partition-core incidence rows, support-to-nerve realization rows, finite simplicial-complex rows, finite chain rows, reduced homology rows, lifted Betti ledgers, Hochster--nerve reduction provenance, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:support-nerve-census-lifted-betti-carrier}, \autoref{thm:support-nerve-census-lifted-betti-obligations}, and \autoref{thm:support-nerve-census-lifted-betti-realization} seed the finite support-nerve census and lifted Betti packet over incidence, realization, simplicial-complex, chain, homology, Betti-ledger, reduction, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, biological mechanism, unrestricted minimal resolutions, quotient codon semantics, coefficient-field completeness, or ambient homology computation.}
  \upgradepath{Obligation closure requires checked incidence enumeration, support-to-nerve realization, finite-complex admission, finite-chain exposure, homology readback, lifted Betti ledger stability, TasteGate nontriviality, field faithfulness, replay stability, and ledger nonescape for BEDC.Derived.SupportNerveCensusLiftedBettiUp.}
\end{closurestatus}
