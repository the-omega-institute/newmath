\begin{theorem}[RealSeparability finite-density lattice]
\label{thm:real-separability-finite-density-lattice}
The $\RealSeparabilityUp$ density packet presents its sibling dependency lattice as the finite route through \autoref{ch:concrete-instances-rat-namecert}, \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, \autoref{ch:concrete-instances-real-namecert}, and \autoref{ch:concrete-instances-countable-dense-subset-namecert}. A density consumer reaches the local separability row only after these sibling certificates expose the rational source, dyadic tolerance, finite window, regular readback, terminal Real seal, and countable dense support.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-separability-carrier}. Its displayed rows are the rational enumeration, dyadic tolerance, StreamName window, RegSeqRat readback, Real seal, dense-window exactness ledger, transport, replay, provenance, and local naming row.

Read those rows through the sibling chapters named in the statement. The rational source is supplied by $\RatUp$, tolerance by $\DyadicRatCoreUp$, finite observation by $\StreamNameUp$, regular readback by $\RegSeqRatUp$, the terminal public exit by $\RealUp$, and dense support by $\CountableDenseSubsetUp$.

Since the carrier has no coordinate for host real equality, choice-selected dense points, quotient streams, trichotomy, or ambient completeness, this displayed sibling lattice is the whole finite-density dependency route.
\end{proof}
