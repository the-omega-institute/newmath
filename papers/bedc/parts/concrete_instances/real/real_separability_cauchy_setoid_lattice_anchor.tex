The $\RealSeparabilityUp$ countable-dense route depends on the classifier surface of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} before dense readback can be consumed. This child route pins the separability lattice to the displayed Cauchy-equivalence setoid rows, so the dense enumeration reads a finite classifier boundary rather than quotient path equality or ambient $\RealUp$ completeness.
