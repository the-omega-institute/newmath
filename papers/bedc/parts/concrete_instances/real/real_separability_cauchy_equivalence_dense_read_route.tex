The RealSeparability dense-read route exposes the Cauchy-equivalence classifier as a downstream consumer edge, not as a free equality principle. A dense-read consumer receives the classifier data of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} only after the finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance rows, and $\RealUp$ seal rows of the separability route have been displayed.

\begin{theorem}[Real separability Cauchy-equivalence dense-read route]
\label{thm:real-separability-cauchy-equivalence-dense-read-route}
Every $\RealSeparabilityUp$ dense-read consumer that uses $\CauchyEquivalenceSetoidUp$ classifier data must first display the $\StreamNameUp$ windows, the $\RegSeqRatUp$ readback row, the $\DyadicRatCoreUp$ tolerance ledger, and the terminal $\RealUp$ seal carried by the RealSeparability route. Thus the classifier is consumed as finite Cauchy-equivalence data after the L10 source rows have been shown, not as quotient stream equality, selected dense enumeration, host real equality, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the displayed RealSeparability route. The density read begins with finite source windows and rational readback; those rows are not yet a Cauchy-equivalence classifier.

Next expose the dyadic tolerance and Real-seal rows. They place the dense-read request on the same L10 route used by the existing RealSeparability children, so the classifier cannot be consumed before the source data are displayed.

Only then read the Cauchy-equivalence classifier of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert}. Since the route has no coordinate for quotient stream equality, selected dense enumeration, host real equality, countable choice, or ambient completeness, the consumer receives only the finite classifier data.
\end{proof}
