\begin{theorem}[Real separability public status route]
\label{thm:real-separability-public-status-route}
Every public $\RealSeparabilityUp$ density read is exported only after the $\CauchyEquivalenceSetoidUp$ classifier, $\DyadicRatCoreUp$ tolerance rows, $\StreamNameUp$ finite windows, $\RegSeqRatUp$ readback, and terminal $\RealUp$ seal are displayed. The route presents a finite density surface for Real and Completion consumers; it exports no quotient real equality, selected dense enumeration, countable choice, hidden host dense subset, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with the density rows already routed through \autoref{ch:concrete-instances-real-separability-namecert}. A public consumer may read a candidate only as a finite density witness, not as a selected host enumeration.

Next pass the witness through the $\StreamNameUp$ and $\RegSeqRatUp$ rows. These rows expose the finite observation windows and regular-rational readback before any Real-facing density claim is public.

The $\DyadicRatCoreUp$ tolerance ledger and the $\CauchyEquivalenceSetoidUp$ classifier then fix the comparison boundary for the read. Equivalent regular presentations replay the same displayed density route rather than becoming quotient real equality.

Only after those rows are visible does the consumer reach the terminal $\RealUp$ seal. Since no row contains a selected dense enumeration, countable-choice schedule, hidden host dense subset, quotient real equality, or ambient completeness principle, the public status route is exhausted by the displayed finite data.
\end{proof}
