The $\RealSeparabilityUp$ packet supplies the dense finite rational rows consumed by finite-cover and uniformly located finite-cover routes. The real carrier of \autoref{ch:concrete-instances-real-namecert}, the regular rational sequence route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the stream-window naming surface of \autoref{ch:concrete-instances-streamname-namecert} provide the finite rational windows that \autoref{ch:concrete-instances-finite-real-cover-namecert} reads before coverage and radius ledgers are exposed.

\begin{proposition}[Real separability finite-cover forward link]
\label{prop:real-separability-finite-cover-forward-link}
Every finite-cover consumer of $\RealSeparabilityUp$ receives dense rational data only as displayed finite windows over $\RealUp$, $\RegSeqRatUp$, and $\StreamNameUp$. The handoff may feed $\FiniteRealCoverUp$ and later uniformly located finite-cover packets through those rows, together with $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ evidence; it does not provide a choice-selected dense subsequence, an arbitrary open-cover refinement, quotient real equality, trichotomy, or ambient completeness.
\end{proposition}
