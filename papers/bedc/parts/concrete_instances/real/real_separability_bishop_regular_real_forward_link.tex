The $\RealSeparabilityUp$ finite dyadic dense-family reads consume the Bishop regular-real handoff of \autoref{ch:concrete-instances-bishop-regular-real-namecert}, after the $\RegSeqRatUp$ route of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. Thus the separability packet reaches its dense-family read through displayed L10 regular-sequence and dyadic data before it treats a Bishop regular real as a source for finite real-density evidence.
