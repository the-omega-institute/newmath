\begin{theorem}[Real separability Cauchy-setoid forward route]
\label{thm:real-separability-cauchy-setoid-forward-route}
Every $\RealSeparabilityUp$ dense-read consumer that identifies regular Cauchy names must expose the $\CauchyEquivalenceSetoidUp$ classifier row before the $\RealUp$ separability seal is exported. The route passes through the regular rational readback and dyadic tolerance rows already visible in \autoref{thm:real-separability-cauchy-setoid-route} and the local Real-separability carrier, then reaches \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} as a classifier boundary rather than as quotient stream equality.
\end{theorem}
\begin{proof}
Project the Real-separability dense-read rows from \autoref{def:real-separability-carrier}. The consumer first reads the displayed density schedule, StreamName windows, RegSeqRat readback, dyadic tolerance row, terminal Real seal, transport, replay, provenance, and local naming.

Apply the Cauchy-setoid route of \autoref{thm:real-separability-cauchy-setoid-route}. It places the $\CauchyEquivalenceSetoidUp$ classifier between regular Cauchy readback and the public dense-route handoff, so the consumer cannot export the $\RealUp$ separability seal before the classifier row is visible.

Finally use the sibling chapter \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert}. Its carrier supplies finite StreamName, RegSeqRat, DyadicRatCore, setoid-test, and Real-seal rows, not a quotient stream equality, selected representative, countable-choice selector, host real equality, or ambient completeness witness.
\end{proof}
