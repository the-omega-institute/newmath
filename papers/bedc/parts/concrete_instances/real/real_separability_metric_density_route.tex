The $\RealSeparabilityUp$ metric-density route reads its countable dense surface through the finite regular-rational and metric witnesses already exposed by sibling chapters. A density consumer first reads the $\RegSeqRatUp$ schedule of \autoref{ch:concrete-instances-regseqrat-namecert}, the $\DyadicRatCoreUp$ tolerance ledger, and the $\StreamNameUp$ finite windows; only then may it inspect the $\MetricUp$ distance witnesses of \autoref{ch:concrete-instances-metric-namecert}. Thus the separability packet exports a finite density readback over regular rational windows and metric distance rows, not a selected dense point, quotient real equality, countable-choice selector, host dense subset, or ambient completeness principle.

\begin{theorem}[Real separability metric density route]
\label{thm:real-separability-metric-density-route}
Every accepted $\RealSeparabilityUp$ density read that is consumed by a metric route factors through $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerances, $\StreamNameUp$ windows, and $\MetricUp$ distance witnesses before the local separability row is exported.
\end{theorem}
\begin{proof}
Project the accepted $\RealSeparabilityUp$ carrier through its rational enumeration, dyadic tolerance, finite stream-window, regular readback, terminal real-seal, transport, replay, provenance, and local naming rows. The metric consumer can read no dense datum before those rows have been displayed.

The regular rational schedule supplies the dense candidate as a finite readback rather than as a host dense subset. The dyadic tolerance and stream-window rows record the precision and observed entries used by that candidate.

The metric row is then read only as a displayed distance witness. Transport and replay preserve the same route, so the exported density surface remains finite and carries no selected nearest point, quotient real equality, countable-choice selector, or ambient completeness theorem.
\end{proof}
