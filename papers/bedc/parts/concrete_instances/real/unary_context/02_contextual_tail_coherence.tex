\begin{theorem}[Real stream classifier iterated unary context selected full endpoint package]
\label{thm:real-stream-classifier-iterated-unary-context-selected-full-endpoint-package}
For rational-observation streams
$x,y,p_X,p_Y,t_X,t_Y,m_X,m_Y,u_X,u_Y,q_X,q_Y,s_X,s_Y,v_X,v_Y,o_X,o_Y:
\mathbb{N}\to\Hist$, a selected index $n$, histories $a,b:\Hist$, and
zero-extension tests $z_X,z_Y:\Hist$, assume
$\mathsf{RealStreamClassifier}(x,y)$. Assume also that for every observation
index $k$ the first context layer satisfies
$$
\mathsf{UnaryHistory}(p_X(k)),\quad \mathsf{UnaryHistory}(t_X(k)),\quad
\hsame(p_X(k),p_Y(k)),\quad \hsame(t_X(k),t_Y(k)),
$$
$$
\mathsf{Cont}(p_X(k),x(k),m_X(k)),\quad
\mathsf{Cont}(m_X(k),t_X(k),u_X(k)),
$$
$$
\mathsf{Cont}(p_Y(k),y(k),m_Y(k)),\quad
\mathsf{Cont}(m_Y(k),t_Y(k),u_Y(k)),
$$
and that for every observation index $k$ the second context layer satisfies
$$
\mathsf{UnaryHistory}(q_X(k)),\quad \mathsf{UnaryHistory}(s_X(k)),\quad
\hsame(q_X(k),q_Y(k)),\quad \hsame(s_X(k),s_Y(k)),
$$
$$
\mathsf{Cont}(q_X(k),u_X(k),v_X(k)),\quad
\mathsf{Cont}(v_X(k),s_X(k),o_X(k)),
$$
$$
\mathsf{Cont}(q_Y(k),u_Y(k),v_Y(k)),\quad
\mathsf{Cont}(v_Y(k),s_Y(k),o_Y(k)).
$$
If the selected second-layer outputs satisfy
$$
\hsame(o_X(n),\Eone(a))\quad\text{and}\quad \hsame(o_Y(n),\Eone(b)),
$$
then the selected contextual endpoints carry all of the following data:
\begin{enumerate}
\item $\mathsf{PositiveUnaryDenominator}(o_X(n))\land
\mathsf{PositiveUnaryDenominator}(o_Y(n))$;
\item $\mathsf{UnaryHistory}(o_X(n))\land \mathsf{UnaryHistory}(o_Y(n))$;
\item $(\hsame(o_X(n),\emp)\to\mathsf{False})\land
(\hsame(o_Y(n),\emp)\to\mathsf{False})$;
\item $(\hsame(o_X(n),\Ezero(z_X))\to\mathsf{False})\land
(\hsame(o_Y(n),\Ezero(z_Y))\to\mathsf{False})$;
\item $\mathsf{UnaryHistory}(a)\land\mathsf{UnaryHistory}(b)\land\hsame(a,b)$.
\end{enumerate}
\end{theorem}
\begin{proof}
First apply
\autoref{thm:real-stream-classifier-unary-denominator-context-closed} to the
given RealUp stream classifier and the first-layer unary context and
continuation rows. This produces $\mathsf{RealStreamClassifier}(u_X,u_Y)$.

Next apply
\autoref{thm:real-stream-classifier-unary-denominator-context-selected-full-endpoint-package}
to this intermediate stream classifier, using the second-layer unary context
and continuation rows, the selected one-extension seals, and the two
zero-extension tests. The resulting five rows are exactly the displayed
endpoint package for $o_X(n)$ and $o_Y(n)$.
\end{proof}

Fit: traditional target RealUp iterated contextual endpoint readback; BEDC
source RealUp streams plus RatUp classifier plus $\mathsf{Cont}$; carrier
rational-observation streams; classifier $\mathsf{RealStreamClassifier}$;
stability consecutive unary context closure; ledger two context layers and
selected e-one seals; core theorem
\autoref{thm:real-stream-classifier-iterated-unary-context-selected-full-endpoint-package};
exactness the displayed endpoint package; standard bridge RegSeqRat iterated
observation readback; status $\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier unary denominator context selected classifier full endpoint package]
\label{thm:real-stream-classifier-unary-denominator-context-selected-classifier-full-endpoint-package}
For rational-observation streams
$x,y,p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$, a selected
index $n$, histories $a,b:\Hist$, and zero-extension tests $z_X,z_Y:\Hist$,
assume $\mathsf{RealStreamClassifier}(x,y)$ and the pointwise unary-context
and continuation hypotheses of
\autoref{thm:real-stream-classifier-unary-denominator-context-closed}. If the
selected contextual outputs also satisfy
$$
\hsame(o_X(n),\Eone(a))\quad\text{and}\quad \hsame(o_Y(n),\Eone(b)),
$$
then the selected contextual endpoints carry all of the following data:
\begin{enumerate}
\item $\mathsf{RatHistoryClassifier}(o_X(n),o_Y(n))$;
\item $\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$;
\item $\mathsf{PositiveUnaryDenominator}(o_X(n))\land
\mathsf{PositiveUnaryDenominator}(o_Y(n))$;
\item $\mathsf{UnaryHistory}(o_X(n))\land \mathsf{UnaryHistory}(o_Y(n))$;
\item $(\hsame(o_X(n),\emp)\to\mathsf{False})\land
(\hsame(o_Y(n),\emp)\to\mathsf{False})$;
\item $(\hsame(o_X(n),\Ezero(z_X))\to\mathsf{False})\land
(\hsame(o_Y(n),\Ezero(z_Y))\to\mathsf{False})$;
\item $\mathsf{UnaryHistory}(a)\land\mathsf{UnaryHistory}(b)\land\hsame(a,b)$.
\end{enumerate}
\end{theorem}
\begin{proof}
First apply
\autoref{thm:real-stream-classifier-unary-denominator-context-closed} to the
given RealUp stream classifier and the pointwise context-closure hypotheses.
This supplies $\mathsf{RealStreamClassifier}(o_X,o_Y)$.

By the definition of $\mathsf{RealStreamClassifier}$, evaluating the closed
stream classifier at the selected index $n$ gives
$\mathsf{RatHistoryClassifier}(o_X(n),o_Y(n))$.

Transport this endpoint classifier across the two one-extension seals using
\autoref{thm:rat-history-classifier-hsame-transport}. This gives
$\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$.

Next apply
\autoref{thm:real-stream-classifier-unary-denominator-context-selected-full-endpoint-package}
to the same hypotheses, the two displayed one-extension seals, and the
zero-extension tests. This gives the positivity, unary-shape, nonempty,
zero-headed exclusion, and e-one tail readback rows.

Package the selected rational-history classifier and its sealed transported
classifier as the first two rows, then append the five rows supplied by the
full endpoint package.
\end{proof}

Fit: traditional target RealUp contextual endpoint classifier readback; BEDC
source RealUp streams, RatUp classifier, $\mathsf{Cont}$, and unary contexts;
carrier rational-observation streams; classifier $\mathsf{RealStreamClassifier}$
with selected and sealed $\mathsf{RatHistoryClassifier}$; stability context
closure and endpoint selection; ledger selected index, contexts, continuation
outputs, e-one and zero-headed tests; core theorem
\autoref{thm:real-stream-classifier-unary-denominator-context-selected-classifier-full-endpoint-package};
exactness selected and sealed RatUp classifiers, denominator/shape rows, and
tail exclusions; standard bridge RegSeqRat selected endpoint classifier
readback; status $\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier unary denominator context selected classifier appended-tail package]
\label{thm:real-stream-classifier-unary-denominator-context-selected-classifier-appended-tail-package}
For rational-observation streams
$x,y,p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$, a selected
index $n$, histories $a,b,d_X,d_Y,z_X,z_Y:\Hist$, assume
$\mathsf{RealStreamClassifier}(x,y)$ and the pointwise unary-context and
continuation hypotheses of
\autoref{thm:real-stream-classifier-unary-denominator-context-closed}. If the
selected contextual outputs also satisfy the one-extension seals
$$
\hsame(o_X(n),\Eone(a))\quad\text{and}\quad \hsame(o_Y(n),\Eone(b)),
$$
then the selected contextual endpoints carry all of the following data:
\begin{enumerate}
\item $\mathsf{RatHistoryClassifier}(o_X(n),o_Y(n))$;
\item $\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$;
\item $\mathsf{PositiveUnaryDenominator}(o_X(n))\land
\mathsf{PositiveUnaryDenominator}(o_Y(n))$;
\item $\mathsf{UnaryHistory}(o_X(n))\land \mathsf{UnaryHistory}(o_Y(n))$;
\item $(\hsame(o_X(n),\emp)\to\mathsf{False})\land
(\hsame(o_Y(n),\emp)\to\mathsf{False})$;
\item $(\hsame(o_X(n),\Ezero(z_X))\to\mathsf{False})\land
(\hsame(o_Y(n),\Ezero(z_Y))\to\mathsf{False})$;
\item $\mathsf{UnaryHistory}(a)\land\mathsf{UnaryHistory}(b)\land\hsame(a,b)$;
\item $(\hsame(o_X(n),d_X\cdot\Ezero(z_X))\to\mathsf{False})\land
(\hsame(o_Y(n),d_Y\cdot\Ezero(z_Y))\to\mathsf{False})$.
\end{enumerate}
\end{theorem}
\begin{proof}
First apply
\autoref{thm:real-stream-classifier-unary-denominator-context-selected-classifier-full-endpoint-package}
to the same RealUp stream classifier, pointwise context-closure hypotheses, and
one-extension seals. This supplies the selected rational-history classifier,
the sealed transported rational-history classifier, the positivity rows, the
unary-shape rows, the nonempty and zero-headed exclusions, and the e-one tail
readback row.

Next apply
\autoref{thm:real-stream-classifier-unary-denominator-context-appended-e0-tail-absurd}
to the same selected contextual instance. This gives the two exclusions against
displays of $o_X(n)$ and $o_Y(n)$ as denominators with appended e-zero tails.

Repack the seven rows from the selected classifier full endpoint package with
the sealed classifier preceding the appended-tail exclusions. The resulting
tuple is exactly the displayed package for the selected contextual endpoints.
\end{proof}

Fit: traditional target RealUp contextual endpoint classifier readback; BEDC
source RealUp streams, RatUp classifier, $\mathsf{Cont}$, unary contexts, and
appended e-zero-tail obstruction; carrier rational-observation streams;
classifier $\mathsf{RealStreamClassifier}$ and selected
$\mathsf{RatHistoryClassifier}$ together with its sealed endpoint transport;
stability context closure and endpoint selection; ledger selected index,
contexts, continuation outputs, e-one seals, zero-headed tests, and
appended-tail displays; core theorem
\autoref{thm:real-stream-classifier-unary-denominator-context-selected-classifier-appended-tail-package};
exactness selected and sealed RatUp classifiers, denominator/shape rows, and
tail exclusions; standard bridge RegSeqRat selected endpoint classifier
readback; status $\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier contextual appended e-one tail cancellation]
\label{thm:real-stream-classifier-unary-denominator-context-selected-append-e1-tail-hsame-cancel}
For rational-observation streams
$x,y,p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$, a selected
index $n$, and histories $d,e,tail_D,tail_E:\Hist$, assume
$$
\mathsf{RealStreamClassifier}(x,y),
$$
and for every observation index $i$ the contexts and continuation outputs
satisfy
$$
\mathsf{UnaryHistory}(p_X(i)),\quad \mathsf{UnaryHistory}(t_X(i)),\quad
\hsame(p_X(i),p_Y(i)),\quad \hsame(t_X(i),t_Y(i)),
$$
$$
\mathsf{Cont}(p_X(i),x(i),m_X(i)),\quad
\mathsf{Cont}(m_X(i),t_X(i),o_X(i)),
$$
$$
\mathsf{Cont}(p_Y(i),y(i),m_Y(i)),\quad
\mathsf{Cont}(m_Y(i),t_Y(i),o_Y(i)).
$$
If the selected contextual outputs display as appended e-one tails,
$$
\hsame(o_X(n),\Eone(d\cdot\Eone(tail_D))),\quad
\hsame(o_Y(n),\Eone(e\cdot\Eone(tail_E))),
$$
and the displayed tails agree by $\hsame(tail_D,tail_E)$, then
$$
\hsame(d,e).
$$
\end{theorem}
\begin{proof}
First apply
\autoref{thm:real-stream-classifier-unary-denominator-context-closed} to the
source stream classifier and the pointwise unary-context and continuation
hypotheses. This supplies the contextual stream classifier
$\mathsf{RealStreamClassifier}(o_X,o_Y)$.

Apply
\autoref{thm:real-stream-classifier-selected-append-e1-tail-hsame-cancel} to
the contextual streams $o_X,o_Y$ at the selected index $n$, using the two
displayed appended e-one tail rows and the comparison
$\hsame(tail_D,tail_E)$. The conclusion is exactly the denominator-head
comparison $\hsame(d,e)$.
\end{proof}

Fit: traditional target contextual RegSeqRat endpoint cancellation; BEDC
source RealUp streams, RatUp classifier readback, $\mathsf{Cont}$, and BEDC
append cancellation; carrier rational-observation streams; classifier
$\mathsf{RealStreamClassifier}$; stability unary denominator context closure;
ledger selected index plus context witnesses and e-one tail displays; core
theorem
\autoref{thm:real-stream-classifier-unary-denominator-context-selected-append-e1-tail-hsame-cancel};
exactness denominator-head comparison after tail cancellation; standard bridge
contextual rational observation tail cancellation; status
$\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier unary denominator context selected e-one tail determinacy]
\label{thm:real-stream-classifier-unary-denominator-context-selected-e1-tail-determinacy}
For rational-observation streams
$x,y,p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$, a selected
index $n$, and histories $a,a',b,b':\Hist$, assume
$\mathsf{RealStreamClassifier}(x,y)$ and the pointwise unary-context plus
continuation hypotheses of
\autoref{thm:real-stream-classifier-unary-denominator-context-closed}. If the
selected contextual outputs satisfy the four e-one displays
$$
\hsame(o_X(n),\Eone(a)),\quad
\hsame(o_X(n),\Eone(a')),\quad
\hsame(o_Y(n),\Eone(b)),\quad
\hsame(o_Y(n),\Eone(b')),
$$
then all of the following data hold:
\begin{enumerate}
\item $\mathsf{UnaryHistory}(a)$;
\item $\mathsf{UnaryHistory}(a')$;
\item $\mathsf{UnaryHistory}(b)$;
\item $\mathsf{UnaryHistory}(b')$;
\item $\hsame(a,a')$;
\item $\hsame(b,b')$;
\item $\hsame(a,b)$;
\item $\hsame(a',b')$.
\end{enumerate}
\end{theorem}
\begin{proof}
Apply
\autoref{thm:real-stream-classifier-unary-denominator-context-e1-pair-readback}
to the displays $\hsame(o_X(n),\Eone(a))$ and
$\hsame(o_Y(n),\Eone(b))$. This supplies
$\mathsf{UnaryHistory}(a)$, $\mathsf{UnaryHistory}(b)$, and $\hsame(a,b)$.

Apply the same readback theorem again to the displays
$\hsame(o_X(n),\Eone(a'))$ and $\hsame(o_Y(n),\Eone(b'))$. This supplies
$\mathsf{UnaryHistory}(a')$, $\mathsf{UnaryHistory}(b')$, and
$\hsame(a',b')$.

For the $X$-tail comparison, compose the first display of $o_X(n)$ with the
symmetry of the second display. The resulting
$\hsame(\Eone(a),\Eone(a'))$ is inverted by the history constructor
characterization, equivalently
$\mathsf{BEDC.FKernel.Hist.hsame\_e1\_inversion\_iff}$, to get
$\hsame(a,a')$.

The same composition and e-one inversion applied to the two displays of
$o_Y(n)$ gives $\hsame(b,b')$. Package these two tail comparisons with the
two readback rows.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_unary\_denominator\_context\_selected\_e1\_tail\_determinacy}

Fit: traditional target RealUp contextual endpoint tail determinacy; BEDC
source RealUp streams, RatUp classifier, $\mathsf{Cont}$, unary contexts, and
selected e-one displays; carrier rational-observation streams; classifier
$\mathsf{RealStreamClassifier}$ and selected endpoint displays; stability
context closure and selected continuation transport; ledger selected index,
contexts, continuation outputs, and paired e-one seals; core theorem
\autoref{thm:real-stream-classifier-unary-denominator-context-selected-e1-tail-determinacy};
exactness canonical selected denominator tails; standard bridge RegSeqRat and
dyadic selected-tail consumers; status $\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier unary denominator context selected e-one tail pairwise coherence]
\label{thm:real-stream-classifier-unary-denominator-context-selected-e1-tail-pairwise-coherence}
For rational-observation streams
$x,y,p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$, a selected
index $n$, and histories $a,a',b,b':\Hist$, assume
$\mathsf{RealStreamClassifier}(x,y)$ and the pointwise unary-context plus
continuation hypotheses of
\autoref{thm:real-stream-classifier-unary-denominator-context-closed}. If the
selected contextual outputs satisfy the four e-one displays
$$
\hsame(o_X(n),\Eone(a)),\quad
\hsame(o_X(n),\Eone(a')),\quad
\hsame(o_Y(n),\Eone(b)),\quad
\hsame(o_Y(n),\Eone(b')),
$$
then all of the following data hold:
\begin{enumerate}
\item $\mathsf{UnaryHistory}(a)$;
\item $\mathsf{UnaryHistory}(a')$;
\item $\mathsf{UnaryHistory}(b)$;
\item $\mathsf{UnaryHistory}(b')$;
\item $\hsame(a,a')$;
\item $\hsame(a,b)$;
\item $\hsame(a,b')$;
\item $\hsame(a',b)$;
\item $\hsame(a',b')$;
\item $\hsame(b,b')$.
\end{enumerate}
\end{theorem}
\begin{proof}
Apply
\autoref{thm:real-stream-classifier-unary-denominator-context-selected-e1-tail-determinacy}
to the same RealUp stream classifier, selected index, pointwise unary-context
and continuation hypotheses, and four e-one displays. This supplies the four
unary-history rows together with $\hsame(a,a')$, $\hsame(b,b')$,
$\hsame(a,b)$, and $\hsame(a',b')$.

For the first cross-tail comparison, compose $\hsame(a,a')$ with
$\hsame(a',b')$ by
$\mathsf{BEDC.FKernel.Hist.hsame\_trans}$. The resulting row is
$\hsame(a,b')$.

For the second cross-tail comparison, apply
$\mathsf{BEDC.FKernel.Hist.hsame\_symm}$ to $\hsame(a,a')$ and compose the
result with $\hsame(a,b)$ by
$\mathsf{BEDC.FKernel.Hist.hsame\_trans}$. This gives $\hsame(a',b)$.

Package the four unary-history witnesses, the four tail rows supplied by
\autoref{thm:real-stream-classifier-unary-denominator-context-selected-e1-tail-determinacy},
and the two cross-tail rows just obtained.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_unary\_denominator\_context\_selected\_e1\_tail\_pairwise\_coherence}

Fit: traditional target canonical selected denominator-tail class for
contextual RealUp observations; BEDC source RealUp streams, RatUp readback,
$\mathsf{Cont}$ contexts, and FKernel hsame transitivity; carrier
rational-observation streams; classifier $\mathsf{RealStreamClassifier}$;
stability selected context closure plus e-one no-confusion; ledger selected
index, context witnesses, continuation outputs, and four e-one seals; core
theorem
\autoref{thm:real-stream-classifier-unary-denominator-context-selected-e1-tail-pairwise-coherence};
exactness pairwise selected-tail coherence; standard bridge RegSeqRat selected
dyadic-tail canonicalization; status $\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier transported contextual e-one tail pairwise coherence]
\label{thm:real-stream-classifier-transported-contextual-e1-tail-pairwise-coherence}
For rational-observation streams
$x,x',y,y',p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$, a
selected index $n$, and histories $a,a',b,b':\Hist$, assume
$$
\forall i,\ \hsame(x(i),x'(i)),\quad
\forall i,\ \hsame(y(i),y'(i)),
$$
and
$$
\mathsf{RealStreamClassifier}(x,y).
$$
Assume also that for every observation index $i$ the transported unary contexts
and continuation outputs satisfy
$$
\mathsf{UnaryHistory}(p_X(i)),\quad \mathsf{UnaryHistory}(t_X(i)),\quad
\hsame(p_X(i),p_Y(i)),\quad \hsame(t_X(i),t_Y(i)),
$$
$$
\mathsf{Cont}(p_X(i),x'(i),m_X(i)),\quad
\mathsf{Cont}(m_X(i),t_X(i),o_X(i)),
$$
$$
\mathsf{Cont}(p_Y(i),y'(i),m_Y(i)),\quad
\mathsf{Cont}(m_Y(i),t_Y(i),o_Y(i)).
$$
If the selected contextual outputs satisfy the four e-one displays
$$
\hsame(o_X(n),\Eone(a)),\quad
\hsame(o_X(n),\Eone(a')),\quad
\hsame(o_Y(n),\Eone(b)),\quad
\hsame(o_Y(n),\Eone(b')),
$$
then all of the following data hold:
\begin{enumerate}
\item $\mathsf{UnaryHistory}(a)$;
\item $\mathsf{UnaryHistory}(a')$;
\item $\mathsf{UnaryHistory}(b)$;
\item $\mathsf{UnaryHistory}(b')$;
\item $\hsame(a,a')$;
\item $\hsame(a,b)$;
\item $\hsame(a,b')$;
\item $\hsame(a',b)$;
\item $\hsame(a',b')$;
\item $\hsame(b,b')$.
\end{enumerate}
\end{theorem}
\begin{proof}
Apply
\autoref{thm:real-stream-classifier-transported-unary-denominator-context-closed}
to the pointwise representative rows, the source RealUp stream classifier, and
the displayed transported unary-context hypotheses. This gives
$\mathsf{RealStreamClassifier}(o_X,o_Y)$.

Apply \autoref{thm:real-stream-classifier-selected-e1-pair-readback} to this
endpoint classifier with the displays $\hsame(o_X(n),\Eone(a))$ and
$\hsame(o_Y(n),\Eone(b))$. This supplies $\mathsf{UnaryHistory}(a)$,
$\mathsf{UnaryHistory}(b)$, and $\hsame(a,b)$.

Apply the same selected pair readback theorem to the displays
$\hsame(o_X(n),\Eone(a'))$ and $\hsame(o_Y(n),\Eone(b'))$. This supplies
$\mathsf{UnaryHistory}(a')$, $\mathsf{UnaryHistory}(b')$, and
$\hsame(a',b')$.

For the left representative comparison, compose the two $o_X(n)$ displays with
the symmetry of the second display and invert the resulting
$\hsame(\Eone(a),\Eone(a'))$ by e-one no-confusion. This gives
$\hsame(a,a')$.

The same composition and e-one inversion applied to the two $o_Y(n)$ displays
gives $\hsame(b,b')$.

Finally compose $\hsame(a,b)$ with $\hsame(b,b')$ by
$\mathsf{BEDC.FKernel.Hist.hsame\_trans}$ to obtain $\hsame(a,b')$. Compose
the symmetry of $\hsame(a,a')$ with $\hsame(a,b)$ by the same transitivity
theorem to obtain $\hsame(a',b)$, and package these rows with the two readback
triples and the two same-side representative comparisons.
\end{proof}
