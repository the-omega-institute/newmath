\chapter{A Concrete Naming Certificate for $\RealSeparabilityUp$}
\label{ch:concrete-instances-real-separability-namecert}
\origin{human}

Following Bishop and Bridges' constructive treatment of the real line by rational approximants, and the Bourbaki description of separability by a countable dense subset, the $\RealSeparabilityUp$ packet records the finite BEDC surface at which rational density is available to $\RealUp$ consumers. It sits on the rational naming row of \autoref{ch:concrete-instances-rat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite observation windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the countable density boundary of \autoref{ch:concrete-instances-countable-dense-subset-namecert}. Thus $\RealSeparabilityUp$ consumes the finite $\StreamNameUp$ and $\RegSeqRatUp$ source surfaces of \autoref{ch:concrete-instances-streamname-namecert} and \autoref{ch:concrete-instances-regseqrat-namecert} before the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. The chapter names rational dense enumeration as finite Real data; it does not introduce host real equality, choice over balls, quotient streams, or ambient completeness.

\begin{definition}[Real separability carrier]
\label{def:real-separability-carrier}
A $\RealSeparabilityUp$ carrier is a finite $\BHist$ packet
$$
S=(Q,D,W,R,E,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $Q$ is the $\RatUp$ enumeration row. It exposes rational candidates by finite index windows and records the order in which a density request may inspect them.
\item $D$ is the $\DyadicRatCoreUp$ tolerance row attached to the same request window, supplying the dyadic precision against which the rational candidates are tested.
\item $W$ is the $\StreamNameUp$ observation-window row opened by $D$, so the consumer reads only the finite name segment required by the requested tolerance.
\item $R$ is the $\RegSeqRatUp$ readback row, transporting the inspected rational candidate and window data into the regular rational route used by the real-name interface.
\item $E$ is the terminal $\RealUp$ seal row. It records that the dense rational window is consumed as Real data only after $Q,D,W,R$ have already been displayed.
\item $T$ is the dense-window exactness ledger. It records that every density read first cites an index in $Q$, then a dyadic tolerance in $D$, then a finite window in $W$, then the regular readback in $R$, and only then the terminal seal $E$.
\item $H$ stores componentwise $\hsame$ transport for the rational enumeration, tolerance, window, readback, seal, exactness ledger, replay, provenance, and local naming rows.
\item $C$ stores displayed $\Cont$ replay from a Real-facing density request through $Q,D,W,R,T$ and into the terminal seal row $E$.
\item $P$ is the $\Pkg$ provenance over $\RealSeparabilityUp$, $\RatUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\CountableDenseSubsetUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RealSeparabilityUp}$ row.
\end{enumerate}
The carrier has no coordinate for host real equality, a choice-selected dense point, quotient stream equality, ambient completeness, trichotomy, Lean verification, or bridge checking.
\end{definition}
\leandef{BEDC.Derived.RealUp.RealSeparabilityCarrier}

The sibling lattice route is explicit: $\RealSeparabilityUp$ consumes \autoref{ch:concrete-instances-dyadic-density-real-namecert} for the dyadic tolerance witness and \autoref{ch:concrete-instances-rational-density-real-namecert} for the rational interval witness before $\CountableDenseSubsetUp$ names the dense source. This places the separability packet downstream of the dyadic and rational density witnesses rather than treating density as a detached Real-side fact.

The same dyadic tolerance witness and rational interval witness feed the finite precision requests of \autoref{ch:concrete-instances-realcauchymodulus-namecert}: a modulus consumer may read the dyadic tolerance row $D$ as its requested precision and the rational interval witness carried by $Q,T$ as the finite interval evidence before opening the StreamName and RegSeqRat rows.

\begin{theorem}[Real separability NameCert obligations]
\label{thm:real-separability-namecert-obligations}
For every accepted $\RealSeparabilityUp$ carrier
$$
S=(Q,D,W,R,E,T,H,C,P,N),
$$
the local $\NameCert_{\RealSeparabilityUp}$ surface consists exactly of the following finite obligation rows:
\begin{enumerate}
\item carrier admission for the rational enumeration, dyadic tolerance, StreamName window, RegSeqRat readback, Real seal, dense-window exactness ledger, transport, replay, provenance, and local naming rows;
\item rational enumeration exposure, requiring every density read to cite the displayed $\RatUp$ index row $Q$ before any tolerance, window, readback, or Real seal is consumed;
\item dense-window exactness, requiring $T$ to bind the same request through $Q,D,W$ so that the inspected rational candidate is tested only inside the opened finite window;
\item RegSeqRat handoff, requiring the finite rational data exposed by $Q,D,W$ to pass through $R$ before it can be read by the Real-facing boundary;
\item RealUp seal non-escape, requiring $E,P,N$ to export no host real equality, choice-selected dense point, quotient stream equality, ambient completeness, trichotomy, Lean verification, or bridge checking.
\end{enumerate}
No separability obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:real-separability-carrier}. The tuple lists exactly the rational enumeration, tolerance, finite observation window, regular rational readback, Real seal, exactness ledger, and structural rows.

Rational enumeration exposure is forced by the placement of $Q$. A density request cannot inspect a candidate until the displayed $\RatUp$ index row has been read as finite packet data.

Dense-window exactness is the row $T$ governed by $Q,D,W$. It fixes the same rational index, dyadic tolerance, and finite StreamName window before any Real-facing read is admitted.

The RegSeqRat handoff is then the row $R$. It transports the inspected rational candidate and window data into the regular rational route, so the Real boundary receives a regular readback rather than a host real point.

Finally $E,H,C,P,N$ seal, transport, replay, source, and name only the displayed route. Since the carrier contains no coordinate for host equality, selected dense points, quotient streams, ambient completeness, trichotomy, Lean verification, or bridge checking, the listed obligations exhaust the local NameCert surface.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealSeparabilityNameCertObligations}

\begin{theorem}[Real separability rational dense handoff]
\label{thm:real-separability-rational-dense-handoff}
Every $\RealUp$ density consumer admitted by an accepted $\RealSeparabilityUp$ packet factors through the finite route
$$
\RatUp \longrightarrow \DyadicRatCoreUp \longrightarrow \StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \RealUp
$$
before any separability read is exported. In particular, the consumer reads the same displayed rational enumeration row $Q$, dyadic tolerance row $D$, finite window row $W$, regular rational readback row $R$, and terminal seal row $E$ before it can consume the local $\NameCert_{\RealSeparabilityUp}$ surface.
\end{theorem}
\begin{proof}
Begin with a density consumer admitted by the carrier of \autoref{def:real-separability-carrier}. Projection exposes the finite rows $Q,D,W,R,E,T,H,C,P,N$.

The first analytic row is $Q$, the rational enumeration. The consumer therefore starts with a displayed $\RatUp$ index window, not with a choice-selected dense point or an ambient real value.

The tolerance and observation rows follow. The dyadic row $D$ supplies the requested precision, and the StreamName row $W$ opens exactly the finite window read at that precision.

The handoff to $R$ places the inspected rational candidate into the regular rational route. Only after that readback does the terminal seal $E$ present the datum as $\RealUp$.

The exactness ledger $T$, componentwise transport $H$, replay $C$, provenance $P$, and local naming row $N$ preserve this order. Hence every exported density read factors through $\RatUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before separability is consumed.
\end{proof}

\begin{theorem}[Real separability Cauchy-equivalence forward consumption]
\label{thm:real-separability-cauchy-equivalence-setoid-forward-consumption}
Every $\RealSeparabilityUp$ density read that compares two regular rational windows must consume the finite classifier route of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} before the rational dense handoff is exported. Thus an accepted separability carrier
$$
S=(Q,D,W,R,E,T,H,C,P,N)
$$
receives a finite $\StreamNameUp$ and $\RegSeqRatUp$ comparison surface rather than quotient stream equality, selected representatives, host real equality, countable-choice selectors, or ambient completeness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-separability-carrier}. A density read has only the rational enumeration row $Q$, dyadic tolerance row $D$, finite window row $W$, regular readback row $R$, terminal seal row $E$, exactness ledger $T$, transport $H$, replay $C$, provenance $P$, and local name $N$.

Apply \autoref{thm:real-separability-rational-dense-handoff}. The rational dense handoff is exported only after the read has passed through $\RatUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$.

When two regular rational windows are compared, read the classifier route of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} before that export. The classifier supplies the finite Cauchy and RegSeqRat comparison surface, so the separability packet cannot replace it with quotient stream equality, selected representatives, host real equality, countable-choice selectors, or ambient completeness.
\end{proof}

\begin{theorem}[Real separability L10 density certificate]
\label{thm:real-separability-l10-density-certificate}
Every dense dyadic schedule exported by an accepted $\RealSeparabilityUp$ carrier
$$
S=(Q,D,W,R,E,T,H,C,P,N)
$$
factors through $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\StreamNameUp$, $\CauchyEquivalenceSetoidUp$, and the terminal $\RealUp$ seal before it can serve a countable-density consumer. The route uses the Cauchy-equivalence classifier of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} to compare regular rational windows, and it exports no quotient stream equality, selected dense point, host real equality, countable-choice selector, or ambient completeness witness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-separability-carrier}. A dense dyadic schedule can read only the displayed rational enumeration row $Q$, dyadic tolerance row $D$, finite window row $W$, regular readback row $R$, terminal seal row $E$, exactness ledger $T$, transport $H$, replay $C$, provenance $P$, and local name $N$.

Apply \autoref{thm:real-separability-rational-dense-handoff}. The exported density read must pass through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before any separability consumer can use it.

Next apply \autoref{thm:real-separability-cauchy-equivalence-setoid-forward-consumption}. When the schedule compares two regular rational windows, the comparison is routed through $\CauchyEquivalenceSetoidUp$ and not through quotient stream equality or a selected representative.

Finally route the displayed schedule to the countable-density boundary by \autoref{thm:real-separability-finite-density-exhaustion}. The same rows $Q,D,W,R,E,T,H,C,P,N$ carry the dyadic schedule to the separability export, so no host equality, selected dense point, countable-choice selector, or ambient completeness witness is introduced.
\end{proof}

\begin{theorem}[Real separability dyadic-rational density handoff]
\label{thm:real-separability-dyadic-rational-density-handoff}
Every accepted $\RealSeparabilityUp$ density consumer whose tolerance row is served by an accepted $\DyadicDensityRealUp$ witness and whose interval witness is served by $\RationalDensityRealUp$ factors through the finite route
$$
\DyadicRatCoreUp \longrightarrow \DyadicDensityRealUp \longrightarrow \RationalDensityRealUp \longrightarrow \RatUp \longrightarrow \StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \RealUp .
$$
The route uses only the displayed carrier rows $Q,D,W,R,E,T,H,C,P,N$ together with the dyadic-density surface of \autoref{ch:concrete-instances-dyadic-density-real-namecert} and the rational-density surface of \autoref{ch:concrete-instances-rational-density-real-namecert}. It exports no host real equality, choice-selected dense point, quotient stream, trichotomy, or ambient completeness witness.
\end{theorem}
\begin{proof}
Project the accepted $\RealSeparabilityUp$ carrier as
$$
S=(Q,D,W,R,E,T,H,C,P,N).
$$
The row $D$ is the only tolerance row exposed to the density consumer, while $Q,W,R,E$ are the rational enumeration, finite window, regular readback, and terminal Real seal rows consumed by the existing rational dense handoff.

Read the tolerance request through \autoref{ch:concrete-instances-dyadic-density-real-namecert}. An accepted $\DyadicDensityRealUp$ witness names the dyadic source inside $\DyadicRatCoreUp$ before the Real-facing window and readback rows are inspected, so the request supplies a displayed dyadic witness rather than a selected host real point.

Next read the interval witness through \autoref{ch:concrete-instances-rational-density-real-namecert}. The accepted $\RationalDensityRealUp$ packet turns the displayed interval and dyadic tolerance into a visible $\RatUp$ witness row, with the rational witness routed through its own finite window and ledger rows before any Real seal is available.

Compose those two witness routes with \autoref{thm:real-separability-rational-dense-handoff}. The consumer first sees the $\DyadicRatCoreUp$ source, then the dyadic-density witness, then the rational-density witness, and only then the rational enumeration, StreamName window, RegSeqRat readback, and Real seal recorded by $Q,W,R,E$.

The remaining rows $T,H,C,P,N$ preserve the same order as exactness ledger, componentwise transport, displayed replay, provenance, and local naming. Since none of the three packets contains a coordinate for host real equality, a choice-selected dense point, quotient stream equality, trichotomy, or ambient completeness, the composed handoff cannot export any such data.
\end{proof}

\begin{theorem}[Real separability dyadic witness family]
\label{thm:real-separability-dyadic-witness-family}
An accepted $\RealSeparabilityUp$ density read determines a finite family of rational witnesses indexed only by the displayed dyadic tolerance row $D$ and finite $\StreamNameUp$ window $W$ of its carrier
$$
S=(Q,D,W,R,E,T,H,C,P,N).
$$
Every witness in that family is routed through $\RatUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\CountableDenseSubsetUp$, and the terminal $\RealUp$ seal before separability is exported. The family contains no choice-selected dense point, host real equality, quotient stream, trichotomy row, or ambient completeness witness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-separability-carrier}. The only rows that can index a finite density family are the dyadic tolerance row $D$ and the finite StreamName window row $W$, with the rational enumeration row $Q$ supplying the visible rational candidates.

Apply \autoref{thm:real-separability-rational-dense-handoff}. Each candidate read starts in $\RatUp$, passes through the dyadic tolerance and finite window rows, and reaches $\RegSeqRatUp$ before the terminal $\RealUp$ seal is consumed.

Next apply \autoref{thm:real-separability-dyadic-rational-density-handoff}. The dyadic-density and rational-density witnesses attach the family to the same displayed route, so the candidate family is indexed by $D,W$ rather than by a choice function over Real balls.

Finally apply \autoref{thm:real-separability-finite-density-exhaustion}. The $\CountableDenseSubsetUp$ boundary receives exactly the finite rational family already routed through $Q,D,W,R,E,T,H,C,P,N$. Since the carrier and handoff theorems expose no host equality, quotient stream, trichotomy, selected dense point, or ambient completeness coordinate, the witness family exports none of them.
\end{proof}

\begin{theorem}[Real separability dyadic countable-dense scope]
\label{thm:real-separability-dyadic-countable-dense-scope}
Every $\RealSeparabilityUp$ dense read exporting a countable dense witness must display the dyadic tolerance request, rational interval locator, $\RegSeqRatUp$ readback, $\CauchyEquivalenceSetoidUp$ classifier, and terminal $\RealUp$ seal before the countable-dense consumer reads the witness. For an accepted carrier
$$
S=(Q,D,W,R,E,T,H,C,P,N),
$$
the countable-dense route is scoped by the same rational enumeration row $Q$, dyadic tolerance row $D$, finite $\StreamNameUp$ window $W$, regular rational readback $R$, terminal seal $E$, exactness ledger $T$, transport $H$, replay $C$, provenance $P$, and local naming row $N$. It exports no host real equality, quotient stream, countable-choice selector, selected dense point, or ambient completeness witness.
\end{theorem}
\begin{proof}
Project the carrier rows from \autoref{def:real-separability-carrier}. A countable-dense export has no data beyond the rational enumeration $Q$, dyadic tolerance $D$, finite window $W$, readback $R$, Real seal $E$, exactness ledger $T$, transport $H$, replay $C$, provenance $P$, and local naming row $N$.

Use \autoref{thm:real-separability-dyadic-witness-family}. The exported witness family is indexed by the displayed dyadic tolerance request $D$ and finite StreamName window $W$, with the rational enumeration row $Q$ supplying the visible rational candidates. The rational interval locator is therefore consumed as part of the finite rational-density handoff rather than as a choice over Real balls.

Next use \autoref{thm:real-separability-cauchy-setoid-density-stability}. Every comparison of regular-rational names passes through the $\CauchyEquivalenceSetoidUp$ classifier before the dense rational row is exported, so the readback $R$ is classified by displayed Cauchy-setoid data and not by quotient stream equality.

Finally apply \autoref{thm:real-separability-finite-density-exhaustion}. The $\CountableDenseSubsetUp$ consumer receives the witness only after the route through $Q,D,W,R,E,T,H,C,P,N$ has reached the terminal $\RealUp$ seal. Since the carrier and cited routes have no coordinate for host real equality, quotient streams, countable-choice selectors, selected dense points, or ambient completeness, the countable-dense export cannot expose any of them.
\end{proof}

\begin{theorem}[Real separability RegSeqRat dense-window induction]
\label{thm:real-separability-regseqrat-dense-window-induction}
Every accepted $\RealSeparabilityUp$ dense-window read for a carrier
$$
S=(Q,D,W,R,E,T,H,C,P,N)
$$
can be consumed by induction over the displayed finite $\StreamNameUp$ window $W$ before the $\RegSeqRatUp$ readback $R$ and terminal $\RealUp$ seal $E$ are opened. The induction exposes only the rational enumeration row $Q$, dyadic tolerance row $D$, finite window $W$, dense-window exactness ledger $T$, transport $H$, replay $C$, provenance $P$, and local naming row $N$ until the readback and seal rows are explicitly displayed. It exports no selected dense point, quotient stream, trichotomy branch, ambient completeness witness, host real equality, or countable-choice selector.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-separability-carrier}. Before $R$ and $E$ are opened, the dense-window read has exactly the rational enumeration, dyadic tolerance, finite StreamName window, exactness ledger, transport, replay, provenance, and local naming rows available.

Apply the rational dense handoff \autoref{thm:real-separability-rational-dense-handoff}. It orders every density consumer through $Q,D,W$ before the RegSeqRat readback and Real seal can be used.

Use the Cauchy-equivalence forward consumption theorem \autoref{thm:real-separability-cauchy-equivalence-setoid-forward-consumption}. Any comparison of regular rational windows is deferred to the displayed classifier route, so the induction over $W$ cannot become quotient stream equality or host real equality.

Finally apply the dyadic witness family theorem \autoref{thm:real-separability-dyadic-witness-family}. The rational witnesses form a finite family indexed by $D$ and $W$, hence the dense-window read is consumed by finite-window induction before $R$ and $E$ are opened. The refused principles are absent from the carrier inventory.
\end{proof}

\begin{theorem}[Real separability finite-density selector]
\label{thm:real-separability-finite-density-selector}
Every finite density selector exported by an accepted $\RealSeparabilityUp$ carrier
$$
S=(Q,D,W,R,E,T,H,C,P,N)
$$
factors through the displayed route
$$
\RatUp \longmapsto \DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \RealUp .
$$
The selector first reads the rational enumeration row $Q$, then the dyadic tolerance row $D$, then the finite StreamName window $W$, then the RegSeqRat readback row $R$, and only then the terminal Real seal $E$. It cannot expose a chosen dense point, quotient stream, host real equality, ambient completeness witness, selected nearest point, or detached dense subset.
\end{theorem}
\begin{proof}
Project the accepted carrier. The rows available to a finite density selector are exactly the rational enumeration $Q$, dyadic tolerance $D$, finite window $W$, readback $R$, Real seal $E$, exactness ledger $T$, transport $H$, replay $C$, provenance $P$, and local naming row $N$.

Apply \autoref{thm:real-separability-rational-dense-handoff}. It forces the selector to begin at the $\RatUp$ enumeration row and to pass through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before any separability boundary is exported.

Next apply \autoref{thm:real-separability-dyadic-rational-density-handoff}. The dyadic tolerance and rational-density witness surfaces attach the selector to the same finite request, so the selected datum is displayed as a routed rational witness rather than as a point chosen from an ambient Real ball.

Finally apply \autoref{thm:real-separability-dyadic-witness-family}. The admissible witnesses form a finite family indexed by $D$ and $W$, and each family member is consumed through $Q,R,E,T,H,C,P,N$. Since no listed row contains a quotient stream, host equality, ambient completeness witness, selected nearest point, or detached dense subset, the selector cannot export any such object.
\end{proof}

\begin{theorem}[Real separability stream-window stability]
\label{thm:real-separability-stream-window-stability}
Let
$$
S=(Q,D,W,R,E,T,H,C,P,N)
$$
and
$$
S'=(Q',D',W',R',E',T',H',C',P',N')
$$
be accepted $\RealSeparabilityUp$ packets whose rational enumeration, dyadic tolerance, and finite stream-window rows are componentwise transported by displayed $\hsame$ data
$$
Q \hsame Q',\qquad D \hsame D',\qquad W \hsame W',
$$
and whose replay rows display the same $\Cont$ route from a Real-facing density request through those transported rows. Then the finite rational witness family determined by $S$ and the family determined by $S'$ are the same displayed density route. In particular, the RegSeqRat readback and terminal $\RealUp$ seal handoff agree as packet data, and no choice-selected dense point, host real equality, quotient stream equality, trichotomy row, or ambient completeness witness is introduced by replacing $S$ with $S'$.
\end{theorem}
\begin{proof}
Project the two carriers. The hypotheses identify the only rows that index the finite rational witness family: the rational enumeration rows $Q,Q'$, the dyadic tolerance rows $D,D'$, and the StreamName windows $W,W'$.

Apply \autoref{thm:real-separability-rational-dense-handoff} to both packets. Since the displayed $\Cont$ replay carries the same request through transported $Q,D,W$ rows, the inspected rational candidates are read along the same finite route before any RegSeqRat or Real seal row is visible.

Apply \autoref{thm:real-separability-dyadic-rational-density-handoff}. The dyadic-density and rational-density surfaces attach to the transported tolerance and window rows, so the two packets expose the same dyadic-rational density witness route.

Finally apply \autoref{thm:real-separability-dyadic-witness-family} and \autoref{thm:real-separability-finite-density-exhaustion}. The witness family is exhausted by the displayed route through $Q,D,W,R,E,T,H,C,P,N$ and the transported route through $Q',D',W',R',E',T',H',C',P',N'$, with no additional coordinate available for a selected dense point, host equality, quotient stream, trichotomy, or ambient completeness.
\end{proof}

\begin{theorem}[Real separability finite-density exhaustion]
\label{thm:real-separability-finite-density-exhaustion}
Every admitted $\RealSeparabilityUp$ density read is exhausted by the displayed finite route
$$
\RatUp \longrightarrow \DyadicRatCoreUp \longrightarrow \StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \RealUp \longrightarrow \CountableDenseSubsetUp .
$$
Equivalently, the consumer reads only the carrier rows $Q,D,W,R,E,T,H,C,P,N$ together with the finite rational-density and dyadic-density handoff surfaces before the local separability certificate is exported.

The route exports no choice-selected dense point, quotient stream, trichotomy row, ambient completeness witness, host real equality, selected nearest point, or detached dense subset.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-separability-carrier}. The rows $Q,D,W,R,E$ give the rational enumeration, dyadic tolerance, StreamName window, RegSeqRat readback, and terminal Real seal, while $T,H,C,P,N$ provide exactness, transport, replay, provenance, and the local naming row.

Apply \autoref{thm:real-separability-rational-dense-handoff}. It forces every density consumer through $\RatUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ before any separability read is available.

Next apply \autoref{thm:real-separability-dyadic-rational-density-handoff}. The dyadic and rational density witnesses are inserted into the same finite route, so the density source is displayed by the $\CountableDenseSubsetUp$ boundary and not by an external dense point.

The structural rows $T,H,C,P,N$ preserve that order and add no analytic coordinate. Since the carrier and the two handoff routes contain no choice-selected dense point, quotient stream, trichotomy, ambient completeness, host real equality, selected nearest point, or detached dense subset, the displayed finite route exhausts the admitted density read.
\end{proof}

\begin{theorem}[Real separability Cauchy-setoid density stability]
\label{thm:real-separability-cauchy-setoid-density-stability}
Every $\RealSeparabilityUp$ density read that compares two regular-rational names must pass through the $\CauchyEquivalenceSetoidUp$ classifier of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} before the dense rational row is exported. For an accepted separability carrier
$$
S=(Q,D,W,R,E,T,H,C,P,N),
$$
the exported density route is therefore the composite of $Q,D,W,R,E,T,H,C,P,N$ with the displayed Cauchy-equivalence classifier rows, and it exposes no quotient stream equality, selected representative, countable choice, host real equality, or ambient completeness.
\end{theorem}
\begin{proof}
Project the accepted $\RealSeparabilityUp$ carrier. The rows $Q,D,W,R,E,T,H,C,P,N$ list the rational enumeration, dyadic tolerance, finite window, regular rational readback, Real seal, dense-window exactness, transport, replay, provenance, and local naming available to the density consumer.

When the consumer compares two regular-rational names, the comparison is not a free equality on streams. It must open the $\CauchyEquivalenceSetoidUp$ classifier of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert}, where the two StreamName windows, RegSeqRat readbacks, dyadic tolerance ledger, setoid-test row, Real seal boundary, transport, replay, provenance, and local naming rows are displayed.

Compose that classifier route with \autoref{thm:real-separability-dyadic-rational-density-handoff}. The dense rational row is exported only after the regular readback $R$, dyadic row $D$, window row $W$, and terminal seal $E$ have been compared through the Cauchy-setoid surface.

The structural rows $T,H,C,P,N$ preserve this composite route. Since neither the separability carrier nor the Cauchy-setoid classifier carries quotient stream equality, selected representatives, countable choice, host real equality, or ambient completeness, the density read cannot export any of those objects.
\end{proof}

\begin{theorem}[Real separability seed-density ledger]
\label{thm:real-separability-seed-density-ledger}
Every accepted $\RealSeparabilityUp$ density read exposes the finite L10 seed route
$$
\RatUp\longmapsto \DyadicRatCoreUp\longmapsto \StreamNameUp\longmapsto \RegSeqRatUp\longmapsto \CauchyEquivalenceSetoidUp\longmapsto \CountableDenseSubsetUp\longmapsto \RealUp
$$
in that order before the read is exported as $\RealSeparabilityUp$ data. For an accepted carrier
$$
S=(Q,D,W,R,E,T,H,C,P,N),
$$
the ledger rows $Q,D,W,R,E,T,H,C,P,N$ supply respectively the rational enumeration, dyadic tolerance, finite stream window, regular-rational readback, terminal real seal, dense-window exactness, componentwise transport, replay, provenance, and local naming. The route exports no host $\RealUp$ equality, selected dense point, quotient stream, countable-choice selector, trichotomy branch, or ambient $\RealUp$ completeness witness.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-separability-carrier}. The rational enumeration row $Q$, dyadic tolerance row $D$, finite $\StreamNameUp$ window $W$, $\RegSeqRatUp$ readback $R$, terminal $\RealUp$ seal $E$, exactness ledger $T$, transport $H$, replay $C$, provenance $P$, and local name $N$ are the only rows available to a density consumer.

Apply the rational dense handoff of \autoref{thm:real-separability-rational-dense-handoff}. It places every accepted density read first on the displayed $\RatUp$ enumeration and then through the dyadic tolerance, finite window, regular readback, and terminal seal rows.

Next apply the dyadic-rational density handoff of \autoref{thm:real-separability-dyadic-rational-density-handoff}. The $\DyadicRatCoreUp$ tolerance source and rational-density witness are inserted before the separability export, so the route is a finite density route rather than a selected point from an ambient Real ball.

Use \autoref{thm:real-separability-finite-density-exhaustion} to pass the same finite family to the $\CountableDenseSubsetUp$ boundary. Then use \autoref{thm:real-separability-cauchy-setoid-density-stability} to force every comparison of regular-rational names through the $\CauchyEquivalenceSetoidUp$ classifier before that boundary is read.

The structural rows $T,H,C,P,N$ preserve the displayed order and add no analytic principle. Since the carrier and the cited routes contain no coordinate for host Real equality, selected dense points, quotient streams, countable choice, trichotomy, or ambient Real completeness, the exported $\RealSeparabilityUp$ data contains none of them.
\end{proof}

The dense Real route also reads Cauchy-equivalent regular names through the classifier of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} before export. Classifier stability keeps equivalent regular names on the same displayed density-consumer route, so the separability packet transports dense witnesses through finite RegSeqRat and Real rows rather than through quotient real equality or ambient completeness.
