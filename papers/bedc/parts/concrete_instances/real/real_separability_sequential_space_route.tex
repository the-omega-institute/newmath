The countable dense rows of $\RealSeparabilityUp$ give sequential consumers a displayed source of rational and regular-Cauchy probes before any topology or limit read is accepted. A $\SequentialSpaceUp$ packet may consume those rows through \autoref{ch:concrete-instances-sequential-space-namecert} only after the finite density route has passed through the $\StreamNameUp$ and $\LimitSpaceUp$ surfaces of \autoref{ch:concrete-instances-streamname-namecert} and \autoref{ch:concrete-instances-limit-space-namecert}. When the same read is completion-facing, the $\CauchySpaceUp$ comparison boundary of \autoref{ch:concrete-instances-cauchyspace-namecert} remains part of the route, so the dense source is not reread as a selected limit, quotient real equality, countable-choice selector, or ambient completeness principle.
