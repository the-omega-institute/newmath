\begin{lemma}[RealSeparability Polish-space forward link]
\label{lem:real-separability-polish-space-forward-link}
Every $\PolishSpaceUp$ complete-separable consumer that reads a $\RealSeparabilityUp$ dense-window route consumes the RealSeparability packet of \autoref{ch:concrete-instances-real-separability-namecert} only after the $\StreamNameUp$ windows of \autoref{ch:concrete-instances-streamname-namecert}, the $\RegSeqRatUp$ regular readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the $\DyadicRatCoreUp$ tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the $\RealUp$ seal route of \autoref{ch:concrete-instances-real-namecert} have been displayed. The resulting read is a forward edge into \autoref{ch:concrete-instances-polishspace-namecert}, not a selected dense point, quotient completion equality, countable-choice schedule, or ambient completeness claim.
\end{lemma}
\begin{proof}
Project the $\RealSeparabilityUp$ density route through its real-facing sibling rows. The forward read first exposes finite stream windows, regular rational observations, dyadic tolerance checks, and the terminal real seal before the dense-window data are exported.

The $\PolishSpaceUp$ carrier then consumes that route through its own $S,R,W$ observation ledger and complete-separable boundary. Since neither carrier has a coordinate for selected dense points, quotient completion equality, countable choice, or ambient completeness, the forward link preserves only the displayed finite rows.
\end{proof}
