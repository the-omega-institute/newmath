The forward density route from $\RealSeparabilityUp$ to substantive sibling consumers is the L10 corridor through \autoref{ch:concrete-instances-countable-dense-subset-namecert}, the dyadic-density surface of \autoref{ch:concrete-instances-dyadic-density-real-namecert}, the rational-density surface of \autoref{ch:concrete-instances-rational-density-real-namecert}, the finite window discipline of \autoref{ch:concrete-instances-streamname-namecert}, the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. A density read therefore opens a countable dense-subset row, checks dyadic and rational density against displayed stream windows, routes the tolerance through $\RegSeqRatUp$ and $\DyadicRatCoreUp$, and reaches the Real seal only after the same finite rows have been exposed.

This route makes the hub dependency visible before any Real-facing consumer treats separability as an available dense surface. It exports no selected nearest point, host dense subset, quotient real equality, countable-choice schedule, or ambient $\RealUp$ completeness coordinate.
