\begin{theorem}[Real stream classifier unary denominator context closure]
\label{thm:real-stream-classifier-unary-denominator-context-closed}
For rational-observation streams
$x,y,p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$, if
$$
\mathsf{RealStreamClassifier}(x,y),
$$
and for every observation index $n$ the contexts and continuation outputs
satisfy
$$
\mathsf{UnaryHistory}(p_X(n)),\quad \mathsf{UnaryHistory}(t_X(n)),\quad
\hsame(p_X(n),p_Y(n)),\quad \hsame(t_X(n),t_Y(n)),
$$
$$
\mathsf{Cont}(p_X(n),x(n),m_X(n)),\quad
\mathsf{Cont}(m_X(n),t_X(n),o_X(n)),
$$
$$
\mathsf{Cont}(p_Y(n),y(n),m_Y(n)),\quad
\mathsf{Cont}(m_Y(n),t_Y(n),o_Y(n)),
$$
then
$$
\mathsf{RealStreamClassifier}(o_X,o_Y).
$$
\end{theorem}
\begin{proof}
Fix an observation index $n$. The stream classifier supplies
$\mathsf{RatHistoryClassifier}(x(n),y(n))$ at that index.

Apply \autoref{thm:rat-history-classifier-unary-denominator-context-closed} to
this point classifier, the two unary-context hypotheses on $p_X(n)$ and
$t_X(n)$, and the two displayed context comparisons
$\hsame(p_X(n),p_Y(n))$ and $\hsame(t_X(n),t_Y(n))$. This gives the rational
history classifier on the associativity-normal context endpoints.

The four continuation witnesses identify those normal endpoints with $o_X(n)$
and $o_Y(n)$, using associativity of append as in
\autoref{thm:real-constant-history-classifier-cont-unary-denominator-context-closed}.
Transport the point classifier along these endpoint identifications.

Since $n$ was arbitrary, repack the transported pointwise witnesses as
$\mathsf{RealStreamClassifier}(o_X,o_Y)$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_unary\_denominator\_context\_closed}

\begin{theorem}[Real stream classifier selected continuation rational classifier]
\label{thm:real-stream-classifier-selected-continuation-rat-classifier}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$, a selected index
$n$, a shared unary suffix $q$, and continuation outputs $xq,yq$, if
$$
\mathsf{RealStreamClassifier}(x,y),\quad
\mathsf{UnaryHistory}(q),\quad
\mathsf{Cont}(x(n),q,xq),\quad
\mathsf{Cont}(y(n),q,yq),
$$
then
$$
\mathsf{RatHistoryClassifier}(xq,yq).
$$
\end{theorem}
\begin{proof}
At the selected index, the stream classifier gives
$\mathsf{RatHistoryClassifier}(x(n),y(n))$. Rational unary-denominator closure
extends this classifier along the shared unary suffix $q$, using reflexive
suffix sameness. The two displayed continuation witnesses identify the closed
append endpoints with $xq$ and $yq$, and rational classifier transport along
these endpoint displays gives the stated classifier.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_selected\_continuation\_rat\_classifier}

\begin{theorem}[Real stream classifier selected continuation e-one pair readback]
\label{thm:real-stream-classifier-selected-continuation-e1-pair-readback}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$, a selected index
$n$, a shared unary suffix $q$, continuation outputs $xq,yq$, and histories
$a,b:\Hist$, if
$$
\mathsf{RealStreamClassifier}(x,y),\quad
\mathsf{UnaryHistory}(q),\quad
\mathsf{Cont}(x(n),q,xq),\quad
\mathsf{Cont}(y(n),q,yq),
$$
and
$$
\hsame(xq,\Eone(a)),\quad \hsame(yq,\Eone(b)),
$$
then
$$
\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land \hsame(a,b).
$$
\end{theorem}
\begin{proof}
At the selected index, the stream classifier gives
$\mathsf{RatHistoryClassifier}(x(n),y(n))$. Close this rational classifier
under the shared unary suffix $q$ by rational unary-denominator continuation
closure, using reflexive sameness of the suffix on the two sides. The two
continuation displays identify the closed endpoints with $xq$ and $yq$.
Transport the closed rational classifier across those displays and then across
the two e-one endpoint seals. Rational one-tail classifier exactness reads back
the unary tails and their $\hsame$ relation.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_selected\_continuation\_e1\_pair\_readback}

Fit: traditional target streamwise rational-denominator context stability; BEDC
source RealUp streams plus RatUp classifier plus $\mathsf{Cont}$; carrier
rational-observation streams; classifier $\mathsf{RealStreamClassifier}$;
stability pointwise unary context closure; ledger pointwise prefix and tail
context witnesses with continuation outputs; core theorem
\autoref{thm:real-stream-classifier-unary-denominator-context-closed};
exactness pointwise $\mathsf{RealStreamClassifier}$ preservation; standard
bridge denominator/context stability of rational real names; status
$\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier unary context closure]
\label{thm:real-stream-classifier-unary-context-closed}
For rational-observation streams $x,y$ and pointwise unary contexts
$pref_X,pref_Y,tail_X,tail_Y$, if $\mathsf{RealStreamClassifier}(x,y)$ and
each prefix and tail is matched by $\hsame$, then the stream obtained by
placing every observation inside its matched prefix and tail context is again
classified by $\mathsf{RealStreamClassifier}$.
\end{theorem}
\begin{proof}
At each index, apply rational history unary-denominator context closure to the
point classifier supplied by the real stream classifier and to the matched
unary prefix and tail histories. These pointwise classifiers assemble into the
contextual stream classifier.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_unary\_context\_closed}

\begin{theorem}[Real stream classifier unary denominator context selected positive denominators]
\label{thm:real-stream-classifier-unary-denominator-context-selected-positive-denominators}
For rational-observation streams
$x,y,p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$ and a selected
index $n$, assume
$$
\mathsf{RealStreamClassifier}(x,y),
$$
and for every observation index the unary contexts, context comparisons, and
continuation displays satisfy the hypotheses of
\autoref{thm:real-stream-classifier-unary-denominator-context-closed}. Then
$$
\mathsf{PositiveUnaryDenominator}(o_X(n))\land
\mathsf{PositiveUnaryDenominator}(o_Y(n)).
$$
\end{theorem}
\begin{proof}
At the selected index $n$, the stream classifier gives
$\mathsf{RatHistoryClassifier}(x(n),y(n))$. Apply the rational unary
denominator context closure to the two unary contexts and their $\hsame$
comparisons. The continuation displays identify the normalized contextual
endpoints with $o_X(n)$ and $o_Y(n)$ by associativity of continuation append.

Transport the rational classifier to these displayed outputs and apply
\autoref{thm:rat-history-classifier-positive-denominators}. This yields the two
positive-denominator witnesses in the displayed conclusion.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_unary\_denominator\_context\_selected\_positive\_denominators}

\begin{theorem}[Real stream classifier unary denominator context selected shape package]
\label{thm:real-stream-classifier-unary-denominator-context-selected-shape-package}
For rational-observation streams
$x,y,p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$ and a selected
index $n$, assume $\mathsf{RealStreamClassifier}(x,y)$ and the pointwise
unary-context and continuation hypotheses of
\autoref{thm:real-stream-classifier-unary-denominator-context-closed}. Then
the selected contextual outputs $o_X(n)$ and $o_Y(n)$ are unary histories,
are disjoint from $\emp$, and are disjoint from every zero-extension endpoint:
$$
\begin{aligned}
&\mathsf{UnaryHistory}(o_X(n))\land \mathsf{UnaryHistory}(o_Y(n))\\
&{}\land(\hsame(o_X(n),\emp)\to\mathsf{False})
\land(\hsame(o_Y(n),\emp)\to\mathsf{False})\\
&{}\land(\hsame(o_X(n),\Ezero(z_X))\to\mathsf{False})
\land(\hsame(o_Y(n),\Ezero(z_Y))\to\mathsf{False}).
\end{aligned}
$$
\end{theorem}
\begin{proof}
Apply unary-denominator context closure to obtain
$\mathsf{RealStreamClassifier}(o_X,o_Y)$. At the selected index, the classifier
gives a rational-history classifier between $o_X(n)$ and $o_Y(n)$, hence
positive unary denominator witnesses for both outputs. Each positive
denominator gives unary membership and nonempty exclusion, and transport of
positive denominators across a displayed zero-extension contradicts the
zero-extension exclusion for positive denominators.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_unary\_denominator\_context\_selected\_shape\_package}

\begin{theorem}[Real stream classifier unary denominator context appended e-zero tail exclusion]
\label{thm:real-stream-classifier-unary-denominator-context-appended-e0-tail-absurd}
For rational-observation streams
$x,y,p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$, a selected
index $n$, and histories $d_X,d_Y,z_X,z_Y:\Hist$, assume
$\mathsf{RealStreamClassifier}(x,y)$ and the pointwise unary-context and
continuation hypotheses of
\autoref{thm:real-stream-classifier-unary-denominator-context-closed}. Then
the selected contextual endpoints cannot be displayed as denominators with an
appended e-zero tail:
$$
(\hsame(o_X(n),d_X\cdot\Ezero(z_X))\to\mathsf{False})\land
(\hsame(o_Y(n),d_Y\cdot\Ezero(z_Y))\to\mathsf{False}).
$$
\end{theorem}
\begin{proof}
Apply
\autoref{thm:real-stream-classifier-unary-denominator-context-selected-positive-denominators}
to the same selected index and pointwise context hypotheses. This gives
positive unary denominator witnesses for both $o_X(n)$ and $o_Y(n)$.

For the left endpoint, suppose $\hsame(o_X(n),d_X\cdot\Ezero(z_X))$.
Transport the positive denominator witness across this displayed $\hsame$ to
obtain a positive denominator witness for $d_X\cdot\Ezero(z_X)$. This
contradicts
\autoref{thm:positive-unary-denominator-append-e0-tail-absurd}.

The right endpoint is identical: a display
$\hsame(o_Y(n),d_Y\cdot\Ezero(z_Y))$ transports the positive denominator
witness for $o_Y(n)$ to the appended e-zero-tail denominator
$d_Y\cdot\Ezero(z_Y)$, again contradicting
\autoref{thm:positive-unary-denominator-append-e0-tail-absurd}.
\end{proof}

\begin{theorem}[Real stream classifier unary denominator context selected classifier shape package]
\label{thm:real-stream-classifier-unary-denominator-context-selected-classifier-shape-package}
For rational-observation streams
$x,y,p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$, a selected
index $n$, and zero-extension tests $z_X,z_Y:\Hist$, assume
$\mathsf{RealStreamClassifier}(x,y)$ and the pointwise unary-context and
continuation hypotheses of
\autoref{thm:real-stream-classifier-unary-denominator-context-closed}. Then
the selected contextual endpoints carry the classifier-and-shape package
$$
\begin{aligned}
&\mathsf{RatHistoryClassifier}(o_X(n),o_Y(n))\\
&{}\land\mathsf{PositiveUnaryDenominator}(o_X(n))
\land\mathsf{PositiveUnaryDenominator}(o_Y(n))\\
&{}\land\mathsf{UnaryHistory}(o_X(n))\land\mathsf{UnaryHistory}(o_Y(n))\\
&{}\land(\hsame(o_X(n),\emp)\to\mathsf{False})
\land(\hsame(o_Y(n),\emp)\to\mathsf{False})\\
&{}\land(\hsame(o_X(n),\Ezero(z_X))\to\mathsf{False})
\land(\hsame(o_Y(n),\Ezero(z_Y))\to\mathsf{False}).
\end{aligned}
$$
\end{theorem}
\begin{proof}
First apply
\autoref{thm:real-stream-classifier-unary-denominator-context-closed} to the
given RealUp stream classifier and the pointwise context-closure hypotheses.
This gives $\mathsf{RealStreamClassifier}(o_X,o_Y)$, and evaluating it at the
selected index $n$ gives
$\mathsf{RatHistoryClassifier}(o_X(n),o_Y(n))$.

Next apply
\autoref{thm:real-stream-classifier-unary-denominator-context-selected-positive-denominators}
to the same selected instance. This supplies the two positive unary denominator
rows for $o_X(n)$ and $o_Y(n)$.

Finally apply
\autoref{thm:real-stream-classifier-unary-denominator-context-selected-shape-package}
with the same hypotheses and the two zero-extension tests. This supplies the
unary-history rows, the nonempty exclusions, and the two zero-headed exclusions,
which together with the selected rational classifier and positivity rows are
exactly the displayed package.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_unary\_denominator\_context\_selected\_classifier\_shape\_package}

\begin{theorem}[Real stream classifier unary denominator context one-tail pair readback]
\label{thm:real-stream-classifier-unary-denominator-context-e1-pair-readback}
For rational-observation streams
$x,y,p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$ and a selected
index $n$, assume $\mathsf{RealStreamClassifier}(x,y)$ and the pointwise
unary-context and continuation hypotheses of
\autoref{thm:real-stream-classifier-unary-denominator-context-closed}. If the
displayed contextual outputs satisfy
$$
\hsame(o_X(n),\Eone(a))\quad\text{and}\quad \hsame(o_Y(n),\Eone(b)),
$$
then
$$
\mathsf{UnaryHistory}(a)\land\mathsf{UnaryHistory}(b)\land\hsame(a,b).
$$
\end{theorem}
\begin{proof}
First apply
\autoref{thm:real-stream-classifier-unary-denominator-context-closed} to obtain
$\mathsf{RealStreamClassifier}(o_X,o_Y)$. At the selected index, transport the
point classifier to the displayed one-extension endpoints and apply the
selected RealUp one-tail pair readback theorem.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_unary\_denominator\_context\_e1\_pair\_readback}

\begin{theorem}[Real stream classifier unary denominator context selected full endpoint package]
\label{thm:real-stream-classifier-unary-denominator-context-selected-full-endpoint-package}
For rational-observation streams
$x,y,p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$, a selected
index $n$, histories $a,b:\Hist$, and zero-extension tests $z_X,z_Y:\Hist$,
assume $\mathsf{RealStreamClassifier}(x,y)$ and the pointwise unary-context
and continuation hypotheses of
\autoref{thm:real-stream-classifier-unary-denominator-context-closed}. If the
selected contextual outputs also satisfy
$$
\hsame(o_X(n),\Eone(a))\quad\text{and}\quad \hsame(o_Y(n),\Eone(b)),
$$
then the selected contextual endpoints carry all of the following data:
\begin{enumerate}
\item $\mathsf{PositiveUnaryDenominator}(o_X(n))\land
\mathsf{PositiveUnaryDenominator}(o_Y(n))$;
\item $\mathsf{UnaryHistory}(o_X(n))\land \mathsf{UnaryHistory}(o_Y(n))$;
\item $(\hsame(o_X(n),\emp)\to\mathsf{False})\land
(\hsame(o_Y(n),\emp)\to\mathsf{False})$;
\item $(\hsame(o_X(n),\Ezero(z_X))\to\mathsf{False})\land
(\hsame(o_Y(n),\Ezero(z_Y))\to\mathsf{False})$;
\item $\mathsf{UnaryHistory}(a)\land\mathsf{UnaryHistory}(b)\land\hsame(a,b)$.
\end{enumerate}
\end{theorem}
\begin{proof}
First apply
\autoref{thm:real-stream-classifier-unary-denominator-context-selected-positive-denominators}
under the selected instance of the context-closure hypotheses. This supplies
the two positive unary denominator witnesses for $o_X(n)$ and $o_Y(n)$.

Next apply
\autoref{thm:real-stream-classifier-unary-denominator-context-selected-shape-package}
under the same context-closure hypotheses and the displayed zero-extension
tests. This supplies the unary-history witnesses, the nonempty exclusions, and
the zero-extension exclusions for the two selected outputs.

Finally apply
\autoref{thm:real-stream-classifier-unary-denominator-context-e1-pair-readback}
to the same context-closure hypotheses together with the two one-extension
seals. The readback row gives the unary tails and their $\hsame$ relation, and
the five displayed conclusions are exactly the collected endpoint data.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_unary\_denominator\_context\_selected\_full\_endpoint\_package}

\begin{theorem}[Real stream classifier unary denominator context selected classifier full endpoint package]
\label{thm:real-stream-classifier-unary-denominator-context-selected-classifier-full-endpoint-package}
For rational-observation streams
$x,y,p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$, a selected
index $n$, histories $a,b:\Hist$, and zero-extension tests $z_X,z_Y:\Hist$,
assume $\mathsf{RealStreamClassifier}(x,y)$ and the pointwise unary-context
and continuation hypotheses of
\autoref{thm:real-stream-classifier-unary-denominator-context-closed}. If the
selected contextual outputs also satisfy
$$
\hsame(o_X(n),\Eone(a))\quad\text{and}\quad \hsame(o_Y(n),\Eone(b)),
$$
then the selected contextual endpoints carry all of the following data:
\begin{enumerate}
\item $\mathsf{RatHistoryClassifier}(o_X(n),o_Y(n))$;
\item $\mathsf{PositiveUnaryDenominator}(o_X(n))\land
\mathsf{PositiveUnaryDenominator}(o_Y(n))$;
\item $\mathsf{UnaryHistory}(o_X(n))\land \mathsf{UnaryHistory}(o_Y(n))$;
\item $(\hsame(o_X(n),\emp)\to\mathsf{False})\land
(\hsame(o_Y(n),\emp)\to\mathsf{False})$;
\item $(\hsame(o_X(n),\Ezero(z_X))\to\mathsf{False})\land
(\hsame(o_Y(n),\Ezero(z_Y))\to\mathsf{False})$;
\item $\mathsf{UnaryHistory}(a)\land\mathsf{UnaryHistory}(b)\land\hsame(a,b)$.
\end{enumerate}
\end{theorem}
\begin{proof}
First apply
\autoref{thm:real-stream-classifier-unary-denominator-context-closed} to the
given RealUp stream classifier and the pointwise context-closure hypotheses.
This supplies $\mathsf{RealStreamClassifier}(o_X,o_Y)$.

By the definition of $\mathsf{RealStreamClassifier}$, evaluating the closed
stream classifier at the selected index $n$ gives
$\mathsf{RatHistoryClassifier}(o_X(n),o_Y(n))$.

Next apply
\autoref{thm:real-stream-classifier-unary-denominator-context-selected-full-endpoint-package}
to the same hypotheses, the two displayed one-extension seals, and the
zero-extension tests. This gives the positivity, unary-shape, nonempty,
zero-headed exclusion, and e-one tail readback rows.

Package the selected rational-history classifier as the first row and append
the five rows supplied by the full endpoint package.
\end{proof}

Fit: traditional target RealUp contextual endpoint classifier readback; BEDC
source RealUp streams, RatUp classifier, $\mathsf{Cont}$, and unary contexts;
carrier rational-observation streams; classifier $\mathsf{RealStreamClassifier}$
and selected $\mathsf{RatHistoryClassifier}$; stability context closure and
endpoint selection; ledger selected index, contexts, continuation outputs,
e-one and zero-headed tests; core theorem
\autoref{thm:real-stream-classifier-unary-denominator-context-selected-classifier-full-endpoint-package};
exactness classifier-plus-shape package; standard bridge RegSeqRat selected
endpoint classifier readback; status $\mathsf{ProofTarget}$.
