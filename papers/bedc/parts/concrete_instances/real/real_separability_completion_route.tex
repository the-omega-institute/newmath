\begin{theorem}[RealSeparability completion-facing display route]
\label{thm:real-separability-completion-facing-display-route}
Every completion-facing $\RealSeparabilityUp$ consumer reaches $\RealUp$ only after the rational enumeration of \autoref{ch:concrete-instances-rat-namecert}, the $\DyadicRatCoreUp$ tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the $\StreamNameUp$ finite-window surface of \autoref{ch:concrete-instances-streamname-namecert}, the $\RegSeqRatUp$ readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, the $\CountableDenseSubsetUp$ source of \autoref{ch:concrete-instances-countable-dense-subset-namecert}, and the terminal seal of \autoref{ch:concrete-instances-real-namecert} have all been displayed. The completion-facing route exports finite density data only through that lattice, not through quotient real equality, selected nearest points, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Begin with the rational source row in the $\RealSeparabilityUp$ carrier. It provides the enumerated rational data through the $\RatUp$ sibling surface rather than through an external dense-set selector.

The dyadic tolerance and finite-window stages then force the request through $\DyadicRatCoreUp$ and $\StreamNameUp$. The $\RegSeqRatUp$ row reads back the same displayed windows at the requested precision.

Finally the dense-source row is named by $\CountableDenseSubsetUp$, and the terminal public boundary is $\RealUp$. Since no carrier row stores quotient real equality, a selected nearest point, countable choice, or ambient completeness, completion-facing Real routes can consume only the displayed lattice.
\end{proof}
