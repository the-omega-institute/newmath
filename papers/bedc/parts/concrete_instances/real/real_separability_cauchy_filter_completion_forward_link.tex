Every completion-facing read of a $\RealSeparabilityUp$ density packet that needs filter-completion data must pass through the finite rows already displayed by the separability hub before it reaches \autoref{ch:concrete-instances-cauchyfiltercompletion-namecert}. The route first exposes the dyadic tolerance row, StreamName window, RegSeqRat readback, and terminal Real seal; only then may the downstream consumer inspect the Cauchy-filter completion packet.

\begin{theorem}[Real separability to CauchyFilterCompletion forward link]
\label{thm:real-separability-cauchy-filter-completion-forward-link}
Every $\CauchyFilterCompletionUp$ consumer of an accepted $\RealSeparabilityUp$ packet factors through the displayed $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows of \autoref{def:real-separability-carrier} before the consumer may read the completion boundary named by \autoref{ch:concrete-instances-cauchyfiltercompletion-namecert}. The handoff exports only finite density windows, dyadic tolerances, regular rational readback, terminal Real seals, transport, replay, provenance, and local naming; it exports no selected dense point, quotient filter, selected completion limit, host real equality, countable choice, or ambient completeness row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:real-separability-carrier}. Its rows $Q,D,W,R,E,T,H,C,P,N$ expose the rational enumeration, dyadic tolerance, finite StreamName window, regular rational readback, terminal Real seal, dense-window exactness ledger, transport, replay, provenance, and local naming.

Use the existing dyadic, StreamName, RegSeqRat, and Real handoff route. A completion-facing consumer cannot reach completion data until the density request has passed through $D,W,R,E$ and the exactness ledger $T$ has tied those rows to the same finite rational read.

Now pass to \autoref{ch:concrete-instances-cauchyfiltercompletion-namecert}. The completion packet reads Cauchy-filter source data, finite windows, dyadic tolerances, regular readback, and a terminal Real seal, so the separability packet can feed it only through the rows already displayed in the separability carrier.

Since neither carrier contains a coordinate for a selected dense point, quotient filter, selected limit, host equality, countable choice, or ambient completeness theorem, the forward link supplies exactly the finite horizontal route to the Cauchy-filter completion consumer.
\end{proof}
