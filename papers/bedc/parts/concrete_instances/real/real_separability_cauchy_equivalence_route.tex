The Cauchy-equivalence route for $\RealSeparabilityUp$ is the finite density-row consumer in which $\RegSeqRatUp$ windows and the $\CauchyEquivalenceSetoidUp$ classifier are both visible before the $\RealUp$ density row is exported. A separability consumer reads \autoref{ch:concrete-instances-regseqrat-namecert} for the regular rational windows, then \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} for the comparison surface that keeps equivalent regular names on the same finite density route, and only then \autoref{ch:concrete-instances-real-namecert} for the terminal Real seal. Thus the dependency edge records finite readback and classifier data, not quotient real equality, a selected dense enumeration, countable choice, or ambient $\RealUp$ completeness.

\begin{lemma}[Real separability Cauchy-equivalence dependency route]
\label{lem:real-separability-cauchy-equivalence-dependency-route}
Every $\RealSeparabilityUp$ density read that is consumed through a Cauchy-equivalence classifier must display the $\RegSeqRatUp$ readback windows and the $\CauchyEquivalenceSetoidUp$ classifier before the $\RealUp$ density row is exported. The route is a child dependency of \autoref{ch:concrete-instances-real-separability-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert}; it exports no quotient stream equality, selected dense point, host real equality, or ambient completeness.
\end{lemma}
\begin{proof}
Project the Real separability density read through the displayed finite route. The rational candidate and tolerance request are not yet Real data; they must first be read through the regular rational windows supplied by $\RegSeqRatUp$.

Next read the Cauchy-equivalence classifier. It compares the regular names only through the displayed finite classifier rows of $\CauchyEquivalenceSetoidUp$, so equivalent names replay the same density route before the Real seal is available.

Finally export the terminal $\RealUp$ density row. Since the preceding rows contain no quotient stream equality, selected dense point, host real equality, or ambient completeness coordinate, the exported separability read contains none of them.
\end{proof}
