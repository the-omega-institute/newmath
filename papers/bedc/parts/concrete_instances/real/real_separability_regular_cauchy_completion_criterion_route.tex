\begin{theorem}[Real separability regular Cauchy completion criterion route]
\label{thm:regular-cauchy-completion-criterion-real-separability-route}
Every $\RealSeparabilityUp$ density read that reaches the L10 completion tail consumes the $\RegularCauchyCompletionCriterionUp$ route of \autoref{ch:concrete-instances-regular-cauchy-completion-criterion-namecert} through the finite $\RegSeqRatUp$ and $\RealUp$ rows. The dyadic-density dependency of \autoref{thm:real-separability-dyadic-density-dependency} supplies the finite dyadic witness, while the RegSeqRat dependency of \autoref{thm:real-separability-regseqrat-dependency} keeps that witness on the displayed regular-rational readback route before the terminal Real seal exports separability.
\end{theorem}
\begin{proof}
Read the accepted $\RealSeparabilityUp$ packet through its existing density dependencies. The dyadic-density row names the finite tolerance witness, and the RegSeqRat row keeps the witness inside a displayed regular rational window rather than a host dense subset.

Now pass to the completion tail. The regular Cauchy completion criterion consumes precisely those finite windows, dyadic tolerance rows, regular readback rows, and local $\RealUp$ seal rows. Therefore the separability consumer can cite the completion criterion only through the same finite dyadic witness and RegSeqRat route already named by the RealSeparability packet.

The route adds no selected nearest point, quotient real equality, ambient completeness, or detached dense subset. It records a sibling edge from RealSeparability to the regular Cauchy completion criterion along the finite dyadic and regular-rational surface.
\end{proof}
