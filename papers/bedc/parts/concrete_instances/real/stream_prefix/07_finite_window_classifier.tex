\begin{definition}[Real unary offset bound]
\label{def:real-unary-offset-bound}
For histories $k,w:\Hist$, the unary offset bound is the BEDC continuation
witness that $k$ is a certified unary observation lying below the certified
unary bound $w$:
$$
\mathsf{UnaryOffsetLe}(k,w)
\Longleftrightarrow
\mathsf{UnaryHistory}(k)\land
\exists \tau:\Hist,\ \mathsf{UnaryHistory}(\tau)\land \ContR(k,\tau,w).
$$
\end{definition}
\leandef{BEDC.Derived.RealUp.RealUnaryOffsetLe}
\leandef{BEDC.Derived.RealUp.UnaryOffsetLe}

\begin{theorem}[Unary offset continuation transitivity]
\label{thm:real-unary-offset-cont-trans}
For histories $k,w,u,\tau,\sigma:\Hist$,
$$
\mathsf{UnaryHistory}(k)\land\mathsf{UnaryHistory}(\tau)\land
\mathsf{UnaryHistory}(\sigma)\land
\ContR(k,\tau,w)\land\ContR(w,\sigma,u)
\Longrightarrow
\mathsf{UnaryOffsetLe}(k,u).
$$
\end{theorem}
\begin{proof}
Use $\mathsf{append}(\tau,\sigma)$ as the composed offset tail. Unary append
closure supplies its unary-history row, and continuation associativity turns
the two displayed continuation rows into a continuation from $k$ through that
tail to $u$. Together with the given $\mathsf{UnaryHistory}(k)$ row, these
data repack as $\mathsf{UnaryOffsetLe}(k,u)$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.UnaryOffsetLe\_cont\_trans}

\begin{theorem}[Unary offset append-tail closure]
\label{thm:unary-offset-le-append-tail-closed}
For histories $k,w,\tau:\Hist$,
$$
\mathsf{UnaryOffsetLe}(k,w)\land \mathsf{UnaryHistory}(\tau)
\Longrightarrow
\mathsf{UnaryOffsetLe}(k,\mathsf{append}(w,\tau)).
$$
\end{theorem}
\begin{proof}
Unfold the offset witness for $\mathsf{UnaryOffsetLe}(k,w)$. It supplies
$\mathsf{UnaryHistory}(k)$ and a unary tail $\sigma$ with
$\ContR(k,\sigma,w)$. Appending the displayed unary history $\tau$ to this
tail gives $\mathsf{append}(\sigma,\tau)$, whose unary row follows from unary
append closure. Continuation associativity turns
$\ContR(k,\sigma,w)$ into a continuation from $k$ through
$\mathsf{append}(\sigma,\tau)$ to $\mathsf{append}(w,\tau)$, so the same
left endpoint repacks as the displayed offset witness.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.UnaryOffsetLe\_append\_tail\_closed}

\begin{definition}[Real unary stream finite window classifier]
\label{def:real-unary-stream-window-classifier}
For observation maps $s,t:\Hist\to\Hist$ and histories $a,w:\Hist$, read $a$
as the anchor and $w$ as the bound. The finite window classifier is the
pointwise RatUp classifier over every certified unary offset below $w$:
$$
\mathsf{RealUnaryStreamWindowClassifier}(s,t,a,w)
\Longleftrightarrow
\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(w)\land
\forall k,\ \mathsf{UnaryOffsetLe}(k,w)\to
\mathsf{RatHistoryClassifier}(
s(\mathsf{append}(a,k)),t(\mathsf{append}(a,k))).
$$
\end{definition}
\leandef{BEDC.Derived.RealUp.RealUnaryStreamWindowClassifier}

\begin{theorem}[Real unary stream finite-window coverage]
\label{thm:real-unary-stream-window-coverage}
For observation maps $s,t:\Hist\to\Hist$ and histories $a,w:\Hist$,
$$
\mathsf{RealUnaryStreamClassifier}(s,t)\land
\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(w)
\Longrightarrow
\mathsf{RealUnaryStreamWindowClassifier}(s,t,a,w).
$$
\end{theorem}
\begin{proof}
Unfold Definition~\ref{def:real-unary-stream-window-classifier}. The anchor and
bound fields are exactly the displayed assumptions $\mathsf{UnaryHistory}(a)$
and $\mathsf{UnaryHistory}(w)$.

Fix an offset $k$ and assume $\mathsf{UnaryOffsetLe}(k,w)$. Unfold
Definition~\ref{def:real-unary-offset-bound}; the offset witness projects
$\mathsf{UnaryHistory}(k)$ together with a unary tail whose continuation reaches
the bound.

Apply unary append closure to $\mathsf{UnaryHistory}(a)$ and
$\mathsf{UnaryHistory}(k)$ to obtain
$\mathsf{UnaryHistory}(\mathsf{append}(a,k))$. Evaluating
$\mathsf{RealUnaryStreamClassifier}(s,t)$ at this certified observation gives
$$
\mathsf{RatHistoryClassifier}(
s(\mathsf{append}(a,k)),t(\mathsf{append}(a,k))).
$$

Since the offset was arbitrary under $\mathsf{UnaryOffsetLe}(k,w)$, these
pointwise endpoint classifiers repack into
$\mathsf{RealUnaryStreamWindowClassifier}(s,t,a,w)$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealUnaryStreamWindowClassifier\_coverage}

\begin{theorem}[Real stream window selected full shape readback package]
\label{thm:real-stream-window-selected-full-shape-readback-package}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$, an anchor index
$n$, a radius $m$, a bounded offset $k$ with $k\le m$, and histories
$a,b,z_X,z_Y:\Hist$, assume
$\mathsf{RealStreamWindowClassifier}(x,y,n,m)$,
$\hsame(x(n+k),\Eone(a))$, and $\hsame(y(n+k),\Eone(b))$. Then the selected
endpoints at $n+k$ carry the following package:
\begin{enumerate}
\item $\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$;
\item $\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land
\hsame(a,b)$;
\item $\mathsf{PositiveUnaryDenominator}(x(n+k))\land
\mathsf{PositiveUnaryDenominator}(y(n+k))$;
\item $\mathsf{UnaryHistory}(x(n+k))\land
\mathsf{UnaryHistory}(y(n+k))\land
(\hsame(x(n+k),\emp)\to\mathsf{False})\land
(\hsame(y(n+k),\emp)\to\mathsf{False})$;
\item $(\hsame(x(n+k),\Ezero(z_X))\to\mathsf{False})\land
(\hsame(y(n+k),\Ezero(z_Y))\to\mathsf{False})$.
\end{enumerate}
\end{theorem}
\begin{proof}
Unfold the finite window classifier at the selected offset. The bound $k\le m$
turns the window hypothesis into
$\mathsf{RatHistoryClassifier}(x(n+k),y(n+k))$.

Transport this rational classifier across the two displayed one-seal endpoint
comparisons by \autoref{thm:rat-history-classifier-hsame-transport}. The
transported classifier is
$\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$, and
\autoref{thm:rat-history-classifier-e1-tail-unary-iff} reads back the two
unary tails together with $\hsame(a,b)$.

Apply \autoref{thm:rat-history-classifier-positive-denominators} to the
original endpoint classifier. This gives positive unary denominator witnesses
for $x(n+k)$ and $y(n+k)$, and
\autoref{thm:positive-unary-denominator-unary-and-nonempty} converts those
witnesses into unary and nonempty endpoint rows.

For the left zero-extension test, transport
$\mathsf{PositiveUnaryDenominator}(x(n+k))$ along the proposed comparison
$\hsame(x(n+k),\Ezero(z_X))$ by
\autoref{thm:rat-positive-unary-denominator-hsame-transport}. The resulting
positive denominator on $\Ezero(z_X)$ contradicts
\autoref{thm:rat-positive-denominator-e0-absurd}.

For the right zero-extension test, repeat the same transport from $y(n+k)$ to
$\Ezero(z_Y)$. The positive-denominator zero-head absurdity theorem gives the
second contradiction, completing the listed package.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamWindowClassifier\_selected\_full\_shape\_readback\_package}

\begin{lemma}[Real unary stream finite-window endpoint projection]
\label{lem:real-unary-stream-window-endpoint-projection}
For observation maps $s,t:\Hist\to\Hist$ and histories $a,w,k:\Hist$,
$$
\mathsf{RealUnaryStreamWindowClassifier}(s,t,a,w)\land
\mathsf{UnaryOffsetLe}(k,w)
\Longrightarrow
\mathsf{RatHistoryClassifier}(
s(\mathsf{append}(a,k)),t(\mathsf{append}(a,k))).
$$
\end{lemma}
\begin{proof}
Unfold Definition~\ref{def:real-unary-stream-window-classifier}. The window
witness contains the universal endpoint row for every offset satisfying
$\mathsf{UnaryOffsetLe}(\cdot,w)$.

Apply that row to the displayed offset witness
$\mathsf{UnaryOffsetLe}(k,w)$. The result is precisely the RatUp endpoint
classifier at $\mathsf{append}(a,k)$.
\end{proof}

\begin{theorem}[Real unary stream window finite-probe coverage]
\label{thm:real-unary-stream-window-finite-probe-coverage}
For observation maps $s,t:\Hist\to\Hist$, a unary anchor $a:\Hist$, a bound
$w:\Hist$, and a probe bundle $\mathcal{P}:\mathsf{ProbeBundle}$, suppose
$$
\mathsf{RealUnaryStreamWindowClassifier}(s,t,a,w)
$$
and suppose that every bundled certified unary observation lies below the
window bound:
$$
\forall n:\Hist,
\mathsf{InBundle}(n,\mathcal{P})\to
\mathsf{UnaryHistory}(n)\to
\mathsf{UnaryOffsetLe}(n,w).
$$
Then the shifted observation maps export a StreamName finite probe window:
$$
\mathsf{RatStreamNameFiniteWindowClassifier}
(\lambda n.\,s(\mathsf{append}(a,n)),
\lambda n.\,t(\mathsf{append}(a,n)),
\mathcal{P}).
$$
\end{theorem}
\begin{proof}
Unfold Definition~\ref{def:rat-stream-name-finite-window-classifier}. Fix a
bundled certified unary observation $n$ with
$\mathsf{InBundle}(n,\mathcal{P})$ and $\mathsf{UnaryHistory}(n)$.

The admissibility hypothesis consumes exactly these two rows and produces the
offset witness $\mathsf{UnaryOffsetLe}(n,w)$. Apply
\autoref{lem:real-unary-stream-window-endpoint-projection} to the Real unary
window classifier and this offset witness.

The projected endpoint classifier is
$$
\mathsf{RatHistoryClassifier}
(s(\mathsf{append}(a,n)),t(\mathsf{append}(a,n))).
$$

This is the required StreamName finite-window row for the shifted observation
maps at the chosen bundled unary observation. Since the bundled observation was
arbitrary, the displayed rows repack into
$\mathsf{RatStreamNameFiniteWindowClassifier}
(\lambda n.\,s(\mathsf{append}(a,n)),
\lambda n.\,t(\mathsf{append}(a,n)),\mathcal{P})$.
\end{proof}

The ten-field fit is: traditional target finite observation-window bridge;
BEDC source $\mathsf{RealUp}$ plus $\mathsf{StreamNameUp}$ plus
$\mathsf{RatUp}$ plus $\mathsf{ProbeBundle}$; carrier the shifted observation
maps $n\mapsto s(\mathsf{append}(a,n))$ and
$n\mapsto t(\mathsf{append}(a,n))$; classifier
$\mathsf{RatStreamNameFiniteWindowClassifier}$; stability inherited from the
Real window endpoint row; ledger anchor, bound, bundle membership, and offset
witness; core theorem
\autoref{thm:real-unary-stream-window-finite-probe-coverage}; exactness the
bundled offsets are exactly consumed pointwise; standard bridge
$\mathsf{RegSeqRat}$ finite-prefix consumption; status $\mathsf{ProofTarget}$.

The ten-field fit is: traditional target finite observation windows for RealUp
streams; BEDC source $\mathsf{RealUnaryStreamClassifier}$ plus
$\mathsf{RatHistoryClassifier}$ plus $\mathsf{UnaryHistory}$ plus $\ContR$;
carrier $\mathsf{BHist}$ observation maps; classifier
$\mathsf{RealUnaryStreamWindowClassifier}$; stability by unary append closure
and pointwise classifier evaluation; ledger anchor, bound, offset, and tail
witness; core theorem \autoref{thm:real-unary-stream-window-coverage};
exactness endpoint projection
\autoref{lem:real-unary-stream-window-endpoint-projection}; standard bridge
finite rational observation windows; status $\mathsf{ProofTarget}$.

\begin{theorem}[Real unary stream finite-window empty bound exactness]
\label{thm:real-unary-stream-window-empty-bound-iff}
For observation maps $s,t:\Hist\to\Hist$ and an anchor history $a:\Hist$,
the empty finite-window bound is exactly the anchor row:
$$
\mathsf{RealUnaryStreamWindowClassifier}(s,t,a,\emp)
\Longleftrightarrow
\mathsf{UnaryHistory}(a)\land \mathsf{RatHistoryClassifier}(s(a),t(a)).
$$
\end{theorem}
\begin{proof}
For the forward direction, the window classifier already stores the unary anchor
row. The empty history is below the empty bound using the empty unary tail and
the continuation identity $\ContR(\emp,\emp,\emp)$. Projecting the endpoint row
at this offset gives
$\mathsf{RatHistoryClassifier}(s(\mathsf{append}(a,\emp)),
t(\mathsf{append}(a,\emp)))$, which is the displayed anchor classifier.

For the reverse direction, assume the anchor unary row and the anchor
classifier. The bound field is certified by unary emptiness. For any offset
$k$ below $\emp$, the continuation witness into the empty result forces
$k=\emp$. The endpoint demanded by the window classifier is therefore the
stored anchor classifier.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealUnaryStreamWindowClassifier\_empty\_bound\_iff}

\begin{lemma}[Unary offset bound transitivity]
\label{lem:real-unary-offset-bound-transitivity}
For histories $k,w,u:\Hist$,
$$
\mathsf{UnaryOffsetLe}(k,w)\land \mathsf{UnaryOffsetLe}(w,u)
\Longrightarrow
\mathsf{UnaryOffsetLe}(k,u).
$$
\end{lemma}
\begin{proof}
Unfold Definition~\ref{def:real-unary-offset-bound} in the first offset row.
It gives $\mathsf{UnaryHistory}(k)$ and a unary tail $\tau:\Hist$ with
$\ContR(k,\tau,w)$.

Unfold the second offset row. It gives a unary tail $\sigma:\Hist$ with
$\ContR(w,\sigma,u)$, and it also certifies the intermediate bound $w$ as a
unary history.

Apply unary append closure to the two tails to obtain
$\mathsf{UnaryHistory}(\mathsf{append}(\tau,\sigma))$. Continuation
associativity composes $\ContR(k,\tau,w)$ with $\ContR(w,\sigma,u)$, and
determinacy identifies the composed endpoint with $u$.

Thus $k$ has the unary tail $\mathsf{append}(\tau,\sigma)$ whose continuation
reaches $u$, so Definition~\ref{def:real-unary-offset-bound} repacks this data
as $\mathsf{UnaryOffsetLe}(k,u)$.
\end{proof}

\begin{lemma}[Unary offset bound reflexivity]
\label{lem:real-unary-offset-bound-reflexivity}
For every history $w:\Hist$,
$$
\mathsf{UnaryHistory}(w)\Longrightarrow\mathsf{UnaryOffsetLe}(w,w).
$$
\end{lemma}
\begin{proof}
Unfold Definition~\ref{def:real-unary-offset-bound}. The displayed hypothesis
supplies the required unary witness for the left endpoint $w$.

Choose the tail $\emp$. Unary emptiness gives $\mathsf{UnaryHistory}(\emp)$,
and the right-unit continuation law witnesses $\ContR(w,\emp,w)$. These three
pieces repack as $\mathsf{UnaryOffsetLe}(w,w)$.
\end{proof}

\begin{theorem}[Real unary stream finite-window endpoint at bound]
\label{thm:real-unary-stream-window-endpoint-at-bound}
For observation maps $s,t:\Hist\to\Hist$ and histories $a,w:\Hist$,
$$
\mathsf{RealUnaryStreamWindowClassifier}(s,t,a,w)
\Longrightarrow
\mathsf{RatHistoryClassifier}(
s(\mathsf{append}(a,w)),t(\mathsf{append}(a,w))).
$$
\end{theorem}
\leanchecked{BEDC.Derived.RealUp.RealUnaryStreamWindowClassifier\_endpoint\_at\_bound}
\begin{proof}
Unfold Definition~\ref{def:real-unary-stream-window-classifier} only far
enough to read the stored bound field $\mathsf{UnaryHistory}(w)$.

Apply \autoref{lem:real-unary-offset-bound-reflexivity} to this bound field.
This gives the explicit witness $\mathsf{UnaryOffsetLe}(w,w)$ required for
endpoint projection.

Now apply \autoref{lem:real-unary-stream-window-endpoint-projection} with
$k=w$. The projected endpoint is exactly the boundary observation
$\mathsf{append}(a,w)$.
\end{proof}

The ten-field fit is: traditional target finite observation boundary readback;
BEDC source $\mathsf{RealUnaryStreamWindowClassifier}$ plus
$\mathsf{UnaryOffsetLe}$; carrier $\mathsf{BHist}$ observation maps;
classifier boundary endpoint RatUp classifier; stability inherited from unary
append closure and endpoint projection; ledger the boundary offset $w$ and the
empty continuation tail; core theorem
\autoref{thm:real-unary-stream-window-endpoint-at-bound}; exactness the bound
is itself a certified finite-window offset; standard bridge RegSeqRat finite
boundary observation; status $\mathsf{ProofTarget}$.

\begin{theorem}[Real unary stream finite-window restriction]
\label{thm:real-unary-stream-window-restriction}
For observation maps $s,t:\Hist\to\Hist$ and histories $a,w,u:\Hist$,
$$
\mathsf{RealUnaryStreamWindowClassifier}(s,t,a,u)\land
\mathsf{UnaryOffsetLe}(w,u)
\Longrightarrow
\mathsf{RealUnaryStreamWindowClassifier}(s,t,a,w).
$$
\end{theorem}
\begin{proof}
Unfold Definition~\ref{def:real-unary-stream-window-classifier} in the source
window. Its anchor field gives $\mathsf{UnaryHistory}(a)$, and the offset
witness $\mathsf{UnaryOffsetLe}(w,u)$ projects $\mathsf{UnaryHistory}(w)$ by
Definition~\ref{def:real-unary-offset-bound}.

Fix any offset $k$ with $\mathsf{UnaryOffsetLe}(k,w)$. Apply
\autoref{lem:real-unary-offset-bound-transitivity} to
$\mathsf{UnaryOffsetLe}(k,w)$ and $\mathsf{UnaryOffsetLe}(w,u)$, obtaining
$\mathsf{UnaryOffsetLe}(k,u)$.

The universal endpoint row of the source window at bound $u$ then yields
$$
\mathsf{RatHistoryClassifier}(
s(\mathsf{append}(a,k)),t(\mathsf{append}(a,k))).
$$

Since the anchor, restricted bound, and every endpoint row have been supplied,
Definition~\ref{def:real-unary-stream-window-classifier} repacks them as
$\mathsf{RealUnaryStreamWindowClassifier}(s,t,a,w)$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealUnaryStreamWindowClassifier\_restriction\_with\_offset\_witness}

The ten-field fit is: traditional target shrinkable finite observation windows
for RealUp streams; BEDC source $\mathsf{RealUnaryStreamWindowClassifier}$ plus
$\mathsf{UnaryOffsetLe}$ plus $\ContR$; carrier unary anchor and certified
unary bounds; classifier the restricted finite-window predicate; stability by
offset-bound transitivity and endpoint reuse at the larger bound; ledger
anchor, source bound, target bound, offset, and composed tail witnesses; core
theorem \autoref{thm:real-unary-stream-window-restriction}; exactness every
endpoint below the restricted bound is an endpoint below the source bound;
standard bridge RegSeqRat finite observation bounds; status
$\mathsf{ProofTarget}$.

\begin{theorem}[Real unary stream window selected tail-class exactness from window]
\label{thm:real-unary-stream-window-selected-tail-class-exactness-from-window}
For observation maps $s,t:\Hist\to\Hist$ and histories $a,w,k:\Hist$, assume
the finite window classifier and a selected certified offset:
$$
\mathsf{RealUnaryStreamWindowClassifier}(s,t,a,w)
\land
\mathsf{UnaryOffsetLe}(k,w).
$$
Then the selected tail class is exact:
$$
\begin{array}{l}
\exists u,v:\Hist,\;
\hsame(s(\mathsf{append}(a,k)),\Eone(u))\land
\hsame(t(\mathsf{append}(a,k)),\Eone(v))\land\\
\mathsf{UnaryHistory}(u)\land
\mathsf{UnaryHistory}(v)\land
\hsame(u,v)\land\\
\forall u',v':\Hist,\,
\bigl(\hsame(s(\mathsf{append}(a,k)),\Eone(u'))\land
\hsame(t(\mathsf{append}(a,k)),\Eone(v'))\bigr)
\Longleftrightarrow\\
\qquad
\bigl(\mathsf{UnaryHistory}(u')\land
\mathsf{UnaryHistory}(v')\land
\hsame(u,u')\land
\hsame(v,v')\bigr).
\end{array}
$$
\end{theorem}
\begin{proof}
Project the selected RatUp classifier directly from
$\mathsf{RealUnaryStreamWindowClassifier}(s,t,a,w)$ at the offset $k$. Positive
denominator readback supplies displayed one-headed representatives for both
selected endpoints. Transport the selected rational classifier across these
two endpoint displays, then apply the e-one tail classifier exactness theorem
to obtain unary tails and their $\hsame$ classifier.

For any displayed pair $u',v'$, transport the same selected classifier across
the displayed endpoint rows and read back the e-one tails. Comparing the chosen
endpoint displays with the arbitrary ones by $\hsame$ transitivity and
e-one-tail inversion gives $\hsame(u,u')$ and $\hsame(v,v')$. Conversely,
tail comparisons $\hsame(u,u')$ and $\hsame(v,v')$ lift by e-one congruence and
compose with the chosen endpoint displays, giving the displayed pair.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealUnaryStreamWindowClassifier\_selected\_tail\_class\_exactness\_from\_window}

\begin{definition}[Real stream finite window classifier]
\label{def:real-stream-finite-window-classifier}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$, an anchor index $n$,
and a zero-based window radius $m$, the finite window classifier is the bounded
pointwise rational-history classifier
$$
\mathsf{RealStreamWindowClassifier}(x,y,n,m)
\Longleftrightarrow
\forall k,\ k\le m\Longrightarrow
\mathsf{RatHistoryClassifier}(x(n+k),y(n+k)).
$$
\end{definition}
\leandef{BEDC.Derived.RealUp.RealStreamWindowClassifier}

\begin{theorem}[Real stream window classifier hsame transport]
\label{thm:real-stream-window-classifier-hsame-transport}
For rational-observation streams $x,y,x',y':\mathbb{N}\to\Hist$, an anchor
index $n$, and a radius $m$, assume
$\mathsf{RealStreamWindowClassifier}(x,y,n,m)$. If every bounded offset
$k\le m$ carries both endpoint rows
$x(n+k)\hsame x'(n+k)$ and $y(n+k)\hsame y'(n+k)$, then
$\mathsf{RealStreamWindowClassifier}(x',y',n,m)$.
\end{theorem}
\begin{proof}
Unfold the finite window classifier and fix an arbitrary offset $k\le m$. The
source window hypothesis supplies the endpoint classifier
$\mathsf{RatHistoryClassifier}(x(n+k),y(n+k))$.

The offset transport hypothesis gives the two rows
$x(n+k)\hsame x'(n+k)$ and $y(n+k)\hsame y'(n+k)$ at the same bounded
observation. Apply
\autoref{thm:rat-history-classifier-hsame-transport} to the source rational
classifier and these endpoint rows.

The result is
$\mathsf{RatHistoryClassifier}(x'(n+k),y'(n+k))$. Since the offset was
arbitrary under the bound $k\le m$, these transported endpoint classifiers
repack into $\mathsf{RealStreamWindowClassifier}(x',y',n,m)$.
\end{proof}

The ten-field fit is: traditional target finite observation-window stability
for RealUp names; BEDC source RealUp windows plus RatUp endpoint classifier
transport; carrier bounded rational-observation windows; classifier
$\mathsf{RealStreamWindowClassifier}$; stability endpoint hsame transport;
ledger anchor, radius, offset, and two endpoint transports; core theorem
\autoref{thm:real-stream-window-classifier-hsame-transport}; exactness
preservation of the bounded classifier; standard bridge finite prefix
stability for regular rational observation names; status
$\mathsf{ProofTarget}$.

\begin{theorem}[Real stream prefix-window exactness at zero]
\label{thm:real-stream-prefix-window-exactness-at-zero}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$ and every finite
offset $m$,
$$
\mathsf{RealStreamPrefixClassifier}(x,y,m)
\Longleftrightarrow
\mathsf{RealStreamWindowClassifier}(x,y,0,m).
$$
\end{theorem}
\begin{proof}
Induct on $m$. At $m=0$, the recursive prefix classifier is exactly the
rational-history classifier at observation $0$, and the window condition has
only the offset $k=0$.

For the successor step, use
\autoref{lem:real-stream-prefix-successor-inversion} to split a prefix through
$m+1$ into the prefix through $m$ and the rational-history classifier at the new
endpoint. The induction hypothesis converts the shorter prefix into the window
condition through offset $m$.

Given any $k\le m+1$, split constructively between $k\le m$ and $k=m+1$. The
first case is handled by the shorter window; the endpoint case is exactly the
successor rational-history classifier.

Conversely, assume the window condition through $m+1$. Its restriction to
offsets $k\le m$ gives the zero-based window through $m$, hence by induction the
prefix through $m$. Taking $k=m+1$ gives the endpoint classifier, and the reverse
direction of successor inversion pairs these two witnesses into the prefix
through $m+1$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamPrefixClassifier\_finite\_window\_zero\_exactness}

\begin{theorem}[Real stream classifier finite-window coverage]
\label{thm:real-stream-classifier-finite-window-coverage}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$ and indices $n,m$,
every pointwise stream classifier covers the finite observation window anchored
at $n$ with radius $m$:
$$
\mathsf{RealStreamClassifier}(x,y)
\Longrightarrow
\mathsf{RealStreamWindowClassifier}(x,y,n,m).
$$
\end{theorem}
\begin{proof}
Assume $\mathsf{RealStreamClassifier}(x,y)$ and fix an offset $k\le m$. The
stream classifier is pointwise, so evaluating it at the observation index $n+k$
gives $\mathsf{RatHistoryClassifier}(x(n+k),y(n+k))$.

Since the chosen offset was arbitrary under the window bound, these endpoint
classifiers assemble exactly into
$\mathsf{RealStreamWindowClassifier}(x,y,n,m)$.
\end{proof}

\begin{theorem}[Real stream classifier zero-window exactness]
\label{thm:real-stream-classifier-zero-window-exactness}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$,
$$
\mathsf{RealStreamClassifier}(x,y)
\Longleftrightarrow
\forall n,\ \mathsf{RealStreamWindowClassifier}(x,y,n,0).
$$
\end{theorem}
\begin{proof}
For the forward direction, assume $\mathsf{RealStreamClassifier}(x,y)$. For
each anchor $n$, apply
\autoref{thm:real-stream-classifier-finite-window-coverage} with radius $0$.
This gives $\mathsf{RealStreamWindowClassifier}(x,y,n,0)$ for every anchor.

For the reverse direction, assume every zero-radius window is classified and
fix an observation index $n$. Unfold the window at anchor $n$ and radius $0$.
Taking offset $k=0$ uses $0\le 0$ and the identity $n+0=n$, so the window row
returns $\mathsf{RatHistoryClassifier}(x(n),y(n))$.

Since $n$ was arbitrary, these rational-history rows repack as the pointwise
stream classifier $\mathsf{RealStreamClassifier}(x,y)$.
\end{proof}

The ten-field fit is: traditional target pointwise equality of countable real
names; BEDC source RealUp stream classifier plus RatUp endpoint classifier;
carrier rational-observation streams; classifier all zero-radius windows;
stability inherited from finite-window stability; ledger anchor index and zero
offset; core theorem this exactness; exactness all pointwise observations iff
all zero windows; standard bridge finite observation equality of regular
rational names; status $\mathsf{ProofTarget}$.

\begin{theorem}[Real stream finite-window stability package]
\label{thm:real-stream-window-classifier-stability-package}
For rational-observation streams $x,x',y,y',z:\mathbb{N}\to\Hist$ and indices
$n,m$, the finite window classifier supplies the following stability rows:
\begin{enumerate}
  \item if
  $\forall k,\ k\le m\Longrightarrow\mathsf{RatHistoryCarrier}(x(n+k))$,
  then $\mathsf{RealStreamWindowClassifier}(x,x,n,m)$;
  \item if $\mathsf{RealStreamWindowClassifier}(x,y,n,m)$, then
  $\mathsf{RealStreamWindowClassifier}(y,x,n,m)$;
  \item if $\mathsf{RealStreamWindowClassifier}(x,y,n,m)$ and
  $\mathsf{RealStreamWindowClassifier}(y,z,n,m)$, then
  $\mathsf{RealStreamWindowClassifier}(x,z,n,m)$;
  \item if $\mathsf{RealStreamWindowClassifier}(x,y,n,m)$ and, for every
  $k\le m$, both $\hsame(x(n+k),x'(n+k))$ and
  $\hsame(y(n+k),y'(n+k))$ hold, then
  $\mathsf{RealStreamWindowClassifier}(x',y',n,m)$.
\end{enumerate}
\end{theorem}
\leanchecked{BEDC.Derived.RealUp.RealStreamWindowClassifier\_stability\_package}
\begin{proof}
Unfold the finite window classifier in each row and fix an offset $k\le m$.

\begin{enumerate}
  \item The reflexivity row reads the carrier hypothesis at this offset. Pair
  the carrier with itself, and use $\hsame$ reflexivity to build
  $\mathsf{RatHistoryClassifier}(x(n+k),x(n+k))$.

  \item The symmetry row evaluates the source window at the offset and applies
  \autoref{thm:rat-history-classifier-symmetry} to reverse the rational-history
  classifier.

  \item The transitivity row evaluates the two source windows at the same
  offset and applies \autoref{thm:rat-history-classifier-trans} to compose the
  two rational-history classifiers through the shared middle stream $y$.

  \item The transport row evaluates the source window at the offset and reads
  the two pointwise $\hsame$ hypotheses at the same offset. Apply
  \autoref{thm:rat-history-classifier-hsame-transport} to transport the
  rational-history classifier to the endpoints $x'(n+k)$ and $y'(n+k)$.
\end{enumerate}

Since the offset was arbitrary under the bound, each row repacks into the
corresponding $\mathsf{RealStreamWindowClassifier}$ statement.
\end{proof}

The ten-field fit is: traditional target finite observation-window stability;
BEDC source RealUp rational-observation streams plus RatUp classifier; carrier
the bounded window predicate; classifier
$\mathsf{RealStreamWindowClassifier}$; stability the four displayed rows;
ledger anchor $n$, radius $m$, and offset witnesses; core theorem this block;
exactness bounded pointwise rational classification; standard bridge finite
rational observation-window agreement; status $\mathsf{ProofTarget}$.
