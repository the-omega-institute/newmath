\closureat{RealUp}{matureStr}[BEDC.Derived.RealUp.RealConstant\_rational\_embedding\_certificate]
\begin{closurestatus}{\RealUp}
  \constructivestory{The real-number boundary is built by joining four already displayed surfaces: DyadicRatCore public observations, $\StreamNameUp$ countable names and finite windows, the $\RegSeqRat$ regular stream spine, and the scoped $\RealUp$ seal/readback rows. The carrier accepts only this composite package of dyadic observations, rational stream names, regularity spine, constant-history rows, endpoint transports, and finite-window ledgers; a completion object, quotient carrier, host equality, arithmetic package, order package, or metric-completeness row is not part of the accepted boundary. Two accepted real packets are classified by the public stream-name classifier, the transported regular-stream classifier, and the real constant/history classifier over the same unary observation and ledger rows, rather than by identifying external Cauchy classes. The source specification is the four-package phase boundary, the pattern specification records dyadic observation transport, public StreamName export, finite-window exactness, regular-spine seal consumption, public real export, endpoint determinacy, and consumer-boundary exhaustion, and the ledger keeps each dyadic, stream, regularity, seal, endpoint, and provenance row visible. The five name-certificate obligations are read through these component certificates: a displayed constant rational embedding inhabits the boundary, the component classifiers provide reflexivity, symmetry, and transitivity, and $\hsame$, endpoint, stream-name, and finite-window transports keep the composite package inside the same boundary. A packet $h$ lies in the signature gap of a $\RealUp$ token $p$ when the source-domain accepts one of the four public package faces, the signature relation pairs it with an answer real-boundary packet and evidence ledger, and the token-introduction relation names that answer as $p$; unfolded, the dyadic observations, rational stream-name rows, regularity spine, and seal/readback entries match the rows encoded by $p$. The checked bridge marker attaches only to the constant rational embedding and boundary package recorded here; the chapter registers this phase-real construction under $\RealUp$ without closing completeness, full arithmetic, full order, metric completeness, quotient construction, Classical data, or any external bridge stronger than the checked BEDC schema bridge.}
  \theoryclosure{\matureClosure}
  \scopeclosed{\autoref{thm:realup-phase-real-completion-boundary} is the mature paper boundary over DyadicRatCore, $\StreamNameUp$, $\RegSeqRat$, and $\RealUp$, binding the dyadic observations, rational stream names, regular stream spine, public $\RealUp$ certificate export, endpoint and finite-window transports, and finite-window ledger.}
  \formalstatus{\bridgeCheckedV}
  \leantarget{BEDC.Derived.RealUp.RealUp\_StdBridge}
  \bridgestatus{bridgeChecked}
  \notclaimed{This block does not close $\RealUp$ completeness, full arithmetic or order, metric completeness, quotient construction, Classical data, or any external bridge stronger than the checked BEDC schema bridge.}
  \upgradepath{No further upgrade -- chapter at top of formal axis.}
\end{closurestatus}
