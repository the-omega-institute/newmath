\begin{theorem}[RegSeqRat dyadic ledger and RealUp seal obligation]
\label{thm:regseqrat-dyadic-ledger-and-real-seal-obligation}
The dyadic-ledger and $\RealUp$ seal obligation for $\RegSeqRat$ consists of
finite unary observation indices, the endpoint $\RatUp$ rows read at those
indices, the dyadic-tolerance witnesses used by regularity, and the seal export
of the recorded $\mathsf{RealUnaryStreamClassifier}$ row. Whenever sealed
$\RealUp$ candidates expose streams satisfying the $\RegSeqRat$ spine and
record their stream comparison row, the seal exports the RealUp observation
classifier required by \autoref{def:real-classifier-specification}.
\end{theorem}
\leanchecked{BEDC.Derived.RealUp.RegSeqRatDyadicLedger\_real\_seal\_obligation}
\begin{proof}
Unfold Definition~\ref{def:regseqrat-stream-certificate-spine}. Its ledger is
finite at the level of requested unary observations and dyadic-tolerance
checks, and each ledger entry is backed by endpoint $\RatUp$ carrier and
classifier rows.

The constant ledger is supplied by
\autoref{thm:regseqrat-constant-stream-spine}. The general endpoint and
transport rows are supplied by
\autoref{thm:regseqrat-stream-classifier-stability-spine}, while
\autoref{thm:regseqrat-stream-spine-scoped-package} gathers the ledger,
classifier, constant, and transport faces as one scoped certificate package.

Finally apply \autoref{thm:regseqrat-realup-seal-consumes-stream-spine}. The
seal reads the already recorded
$\mathsf{RealUnaryStreamClassifier}$ row and exports exactly the observation
classifier required by the RealUp boundary, without adding quotient or
completion data.
\end{proof}

\begin{theorem}[DyadicRatCore obligation boundary package]
\label{thm:dyadic-rat-core-obligation-boundary-package}
The DyadicRatCore obligation boundary consists exactly of the carrier,
classifier, arithmetic-stability, monotone-radius, distance and absolute-value,
operation-stability, finite-ledger extraction, and $\RegSeqRat$ seal faces
listed in this section. Its carrier face is
\autoref{thm:dyadic-rat-core-carrier-obligation}; its classifier face is
\autoref{thm:dyadic-rat-core-classifier-obligation}; its dyadic arithmetic
face is \autoref{thm:dyadic-rat-core-arithmetic-stability-obligation}; its
monotone-radius face is
\autoref{thm:dyadic-rat-core-monotone-radius-obligation}; its distance and
absolute-value face is
\autoref{thm:dyadic-rat-core-distance-absolute-obligation}; its operation
stability face is
\autoref{thm:dyadic-rat-core-operation-stability-obligation}; its finite
ledger extraction face is
\autoref{thm:dyadic-rat-core-observation-row-finite-ledger-extraction}; and
its downstream seal face is
\autoref{thm:regseqrat-dyadic-ledger-and-real-seal-obligation}.

No face in this package reads a quotient, a completion object, Classical data,
or an external metric primitive. Every named face is read from $\mathsf{BHist}$,
$\hsame$, RatUp endpoint histories, StreamNameUp request names, the
DyadicRatCore observation row, and the finite dyadic ledger consumed by
$\RegSeqRat$ and $\RealUp$.
\end{theorem}
\begin{proof}
Read the carrier and classifier faces first. Apply
\autoref{thm:dyadic-rat-core-carrier-obligation} and
\autoref{thm:dyadic-rat-core-classifier-obligation}. These identify the
endpoint rows, their $\hsame$ transport, and the RatUp positive-denominator
source used by the observation row.

Next read the stability inventory. Apply
\autoref{thm:dyadic-rat-core-arithmetic-stability-obligation},
\autoref{thm:dyadic-rat-core-monotone-radius-obligation},
\autoref{thm:dyadic-rat-core-distance-absolute-obligation}, and
\autoref{thm:dyadic-rat-core-operation-stability-obligation}. Together they
cover the dyadic arithmetic, radius refinement, distance, absolute-value, and
operation rows without adding a separate metric carrier.

Finally read the ledger and seal faces. Apply
\autoref{thm:dyadic-rat-core-observation-row-finite-ledger-extraction} for the
finite family of endpoint rows, and apply
\autoref{thm:regseqrat-dyadic-ledger-and-real-seal-obligation} for the
$\RegSeqRat$ ledger consumed by the $\RealUp$ seal.

The cited theorem list exhausts the displayed boundary: each clause is one of
the named faces above, and the proofs of those faces read only $\mathsf{BHist}$,
$\hsame$, endpoint histories, request names, observation rows, and finite
ledger entries. Hence the package has no quotient, completion object,
Classical datum, or external metric primitive as an input.
\end{proof}

\begin{theorem}[DyadicRatCore scoped primitive dependency package]
\label{thm:dyadic-rat-core-scoped-primitive-dependency-package}
Each DyadicRatCore obligation face in
\autoref{thm:dyadic-rat-core-obligation-boundary-package} is scoped over
exactly $\mathsf{BHist}$, $\hsame$, the $\RatUp$ endpoint carrier and
classifier rows, $\mathsf{PositiveUnaryDenominator}$, $\StreamNameUp$ unary
request names, the DyadicRatCore observation row, and the finite dyadic ledger
consumed by $\RegSeqRat$ and $\RealUp$. The carrier, classifier,
arithmetic-stability, monotone-radius, distance and absolute-value,
operation-stability, finite-ledger extraction, and $\RegSeqRat$ seal faces use
no quotient, completion object, Classical datum, or external metric primitive.
\end{theorem}
\begin{proof}
Start with the carrier and classifier faces. Apply
\autoref{thm:dyadic-rat-core-carrier-obligation} and
\autoref{thm:dyadic-rat-core-classifier-obligation}. These faces read
$\mathsf{BHist}$ endpoint histories, $\hsame$ transport, the $\RatUp$ carrier
and classifier rows, $\mathsf{PositiveUnaryDenominator}$, and the
DyadicRatCore observation row of
\autoref{def:dyadic-rat-core-observation-row}.

Next read the dyadic stability faces. Apply
\autoref{thm:dyadic-rat-core-arithmetic-stability-obligation},
\autoref{thm:dyadic-rat-core-monotone-radius-obligation},
\autoref{thm:dyadic-rat-core-distance-absolute-obligation}, and
\autoref{thm:dyadic-rat-core-operation-stability-obligation}. Their operation
inputs and outputs remain endpoint observation rows, while the radius,
distance, and absolute-value clauses use only the positive-denominator and
dyadic-radius ledger tokens already present in the scoped source package of
\autoref{thm:dyadic-rat-core-scoped-source-package}.

The finite-ledger face is supplied by
\autoref{thm:dyadic-rat-core-observation-row-finite-ledger-extraction}. It
uses $\StreamNameUp$ unary request names only to select finitely many endpoint
observations, then exposes the same DyadicRatCore observation rows and dyadic
ledger entries at those selected names.

Finally apply \autoref{thm:regseqrat-dyadic-ledger-and-real-seal-obligation}.
The $\RegSeqRat$ seal consumes the recorded stream spine and the finite dyadic
ledger exported by the previous faces; the $\RealUp$ observation classifier is
therefore read from the existing $\mathsf{RealUnaryStreamClassifier}$ row.

These four readings exhaust the face list of
\autoref{thm:dyadic-rat-core-obligation-boundary-package}. Every dependency is
either a primitive BEDC history, an $\hsame$ transport, a $\RatUp$ endpoint row,
a unary request name, an observation-row field, or a finite ledger entry already
licensed by the cited face theorem. No cited face introduces a quotient,
completion object, Classical datum, or external metric primitive.
\end{proof}

\begin{theorem}[DyadicRatCore scoped closure boundary]
\label{thm:dyadic-rat-core-scoped-closure-boundary}
Every DyadicRatCore obligation face is scoped over RatUp endpoint histories,
$\mathsf{PositiveUnaryDenominator}$, $\RegSeqRat$ unary observation requests,
$\mathsf{UnaryHistory}$, $\hsame$ transport, $\Cont$/FKernel unary context
laws, $\mathsf{DyadicRatCoreObservationRow}$, and the finite dyadic ledger
consumed by the $\RegSeqRat$ seal. No quotient, completion object, Classical
data, external metric primitive, $\RealUp$ completeness, or standard bridge is
part of this scope.
\end{theorem}
\begin{proof}
Begin with the scoped source package
\autoref{thm:dyadic-rat-core-scoped-source-package}. It supplies the RatUp
endpoint histories, the $\mathsf{PositiveUnaryDenominator}$ rows, the
observation row, and the dyadic-radius token used by the later ledger.

Next enumerate the obligation faces. Apply
\autoref{thm:dyadic-rat-core-carrier-obligation},
\autoref{thm:dyadic-rat-core-classifier-obligation},
\autoref{thm:dyadic-rat-core-operation-stability-obligation},
\autoref{thm:dyadic-rat-core-monotone-radius-obligation}, and
\autoref{thm:dyadic-rat-core-distance-absolute-obligation}. These faces read
only the endpoint rows, $\mathsf{UnaryHistory}$ request names, $\hsame$
transport, and the $\Cont$/FKernel unary context laws.

Finally read the finite ledger and downstream seal. Apply
\autoref{thm:dyadic-rat-core-observation-row-finite-ledger-extraction} to
extract the finite dyadic ledger from the observation row, and apply
\autoref{thm:regseqrat-dyadic-ledger-and-real-seal-obligation} to show that
the $\RegSeqRat$ seal consumes exactly that finite ledger.

The preceding list exhausts the named faces of
\autoref{thm:dyadic-rat-core-obligation-boundary-package}. Since each face has
already been bound to the displayed primitive dependency chain, the scoped
boundary contains no quotient, completion object, Classical datum, external
metric primitive, $\RealUp$ completeness claim, or standard bridge.
\end{proof}

\begin{theorem}[DyadicRatCore public observation package]
\label{thm:dyadicratcore-public-observation-package}
The public DyadicRatCore package exports exactly the observation-row carrier,
classifier, arithmetic-stability, monotone-radius, distance and absolute-value,
operation-stability, finite-ledger extraction, and $\RegSeqRat$ seal faces of
\autoref{thm:dyadic-rat-core-obligation-boundary-package}. Its primitive scope is
fixed by \autoref{thm:dyadic-rat-core-scoped-primitive-dependency-package} and
\autoref{thm:dyadic-rat-core-scoped-closure-boundary}. Thus the public package
has no quotient, completion object, Classical datum, external metric primitive,
$\RealUp$ completeness claim, or standard bridge.
\end{theorem}
\begin{proof}
First read the face inventory from
\autoref{thm:dyadic-rat-core-obligation-boundary-package}. It lists the carrier,
classifier, arithmetic-stability, monotone-radius, distance and absolute-value,
operation-stability, finite-ledger extraction, and $\RegSeqRat$ seal faces as the
whole DyadicRatCore boundary.

Next read the primitive dependency boundary from
\autoref{thm:dyadic-rat-core-scoped-primitive-dependency-package}. That theorem
binds every listed face to $\mathsf{BHist}$, $\hsame$, the $\RatUp$ endpoint
rows, $\mathsf{PositiveUnaryDenominator}$, $\StreamNameUp$ request names, the
DyadicRatCore observation row, and the finite dyadic ledger consumed by
$\RegSeqRat$ and $\RealUp$.

Finally apply \autoref{thm:dyadic-rat-core-scoped-closure-boundary}. Since the
public observation package exports only the already listed faces over the
already fixed primitive scope, it carries no quotient, completion object,
Classical datum, external metric primitive, $\RealUp$ completeness claim, or
standard bridge.
\end{proof}

\begin{theorem}[DyadicRatCore public observation hsame transport]
\label{thm:dyadicratcore-public-observation-hsame-transport}
Let $d,d':\Hist$. If $\hsame(d,d')$ holds and $d$ carries the public
DyadicRatCore observation package of
\autoref{thm:dyadicratcore-public-observation-package}, then the same public
package supplies the transported DyadicRatCore observation row at $d'$. The
transported row has the $\hsame$-transported RatUp carrier face, the
$\hsame$-transported positive-denominator face, the same certified dyadic
ledger entry, and the same no-quotient, no-Classical, no-completion boundary
fixed by
\autoref{thm:dyadic-rat-core-scoped-primitive-dependency-package} and
\autoref{thm:dyadic-rat-core-scoped-closure-boundary}.
\end{theorem}
\begin{proof}
First read the public export surface from
\autoref{thm:dyadicratcore-public-observation-package}. It exposes only the
DyadicRatCore observation-row carrier, classifier, stability, arithmetic,
ledger, and seal faces over the scoped primitive package.

Next apply \autoref{thm:dyadic-rat-core-observation-row-hsame-transport} to
the observation row at $d$ and the comparison $\hsame(d,d')$. This transports
the RatUp carrier component and the positive-denominator component to $d'$,
while keeping the certified dyadic ledger entry attached to the same
$\RegSeqRat$ spine.

Finally read the primitive and closure boundaries from
\autoref{thm:dyadic-rat-core-scoped-primitive-dependency-package} and
\autoref{thm:dyadic-rat-core-scoped-closure-boundary}. Because the transport
uses only the exported public faces and the already named ledger entry, the
transported row inherits the same finite dyadic ledger and introduces no
quotient, completion object, Classical datum, external metric primitive,
$\RealUp$ completeness claim, or standard bridge.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.DyadicRatCoreObservationRow\_public\_hsame\_transport}

\begin{theorem}[Dyadic common-exponent radius monotone]
\label{thm:dyadic-common-exponent-radius-monotone}
Let $d_0,d_1:\Hist$ be two public DyadicRatCore observation rows whose
endpoints are RatUp rows over positive unary denominators. Then there is a
shared finite $\mathsf{BHist}$ denominator and radius ledger for the pair. Its
endpoint reads are obtained by RatUp transport, $\hsame$ comparison, and
$\Cont$ computation rows, and its common radius is no larger than either
displayed input radius in the monotone-radius face of
\autoref{thm:dyadic-rat-core-obligation-boundary-package}. The construction
uses only the scoped DyadicRatCore source package of
\autoref{thm:dyadic-rat-core-scoped-primitive-dependency-package} and the
finite-ledger extraction face already exported there.
\end{theorem}
\begin{proof}
Read the two observation rows through
\autoref{thm:dyadic-rat-core-obligation-boundary-package}. Each row supplies a
RatUp endpoint, a positive unary denominator, the monotone-radius face, and a
finite dyadic ledger entry.

Use the scoped primitive package of
\autoref{thm:dyadic-rat-core-scoped-primitive-dependency-package} to compare
the two denominator histories in $\mathsf{BHist}$. The $\hsame$ and $\Cont$
rows give the finite denominator readback needed to place both endpoints over a
single displayed exponent ledger.

Finally apply the monotone-radius obligation and the finite-ledger extraction
face. They select the common displayed radius inside the existing DyadicRatCore
ledger and certify that this radius is bounded by the two input radii, with no
quotient, completion object, Classical datum, or external metric primitive.
\end{proof}

\begin{theorem}[Dyadic window denominator common refinement]
\label{thm:dyadic-window-denominator-common-refinement}
For any finite RealUp observation window whose entries are public DyadicRatCore
observation rows, every displayed dyadic row can be replaced by the
shared-denominator refinement of
\autoref{thm:dyadic-common-exponent-radius-monotone}. The resulting finite
window has the same public observation package: the RatUp endpoint rows,
positive-denominator witnesses, $\hsame$ transports, $\Cont$ computations,
monotone-radius rows, finite dyadic ledger entries, and scoped boundary from
\autoref{thm:dyadic-rat-core-scoped-closure-boundary} are all retained as
displayed $\mathsf{BHist}$ data.
\end{theorem}
\begin{proof}
Proceed through the finite RealUp window ledger entry by entry. For a singleton
entry, the displayed denominator is already a common denominator for that
entry, and the public observation package is unchanged.

For the next displayed row, apply
\autoref{thm:dyadic-common-exponent-radius-monotone} to the current common
ledger and the incoming row. This produces a refined common denominator and a
radius bounded by the two displayed radii, while transporting the RatUp
endpoint rows and positive-denominator witnesses through the same $\hsame$ and
$\Cont$ rows.

The finite-ledger extraction face then repacks the refined rows into the same
public DyadicRatCore observation package. Since the package fields are the
carrier, classifier, stability, monotone-radius, and finite-ledger rows already
listed in \autoref{thm:dyadic-rat-core-obligation-boundary-package}, replacing
each row by its refinement changes no public observation boundary.
\end{proof}

\begin{theorem}[RegSeqRat dyadic common-radius seal]
\label{thm:regseqrat-dyadic-common-radius-seal}
The $\RegSeqRat$ seal consumes exactly the refined finite dyadic ledger of
\autoref{thm:dyadic-window-denominator-common-refinement}. From that ledger it
exports the same RealUp finite-window input as
\autoref{thm:dyadicratcore-public-observation-realup-window-feed}: carried
RatUp endpoints, positive unary denominators, common-radius rows, $\hsame$ and
$\Cont$ provenance, and the scoped DyadicRatCore boundary fixed by
\autoref{thm:dyadic-rat-core-scoped-primitive-dependency-package}.
\end{theorem}
\begin{proof}
First build the common finite ledger by
\autoref{thm:dyadic-window-denominator-common-refinement}. The ledger already
contains the displayed endpoints, denominator histories, radius comparisons,
and transport rows over $\mathsf{BHist}$.

Next apply the $\RegSeqRat$ dyadic ledger seal face named in
\autoref{thm:dyadic-rat-core-obligation-boundary-package}. Its input is the
finite dyadic ledger, not an ambient completion object, so the refined common
denominator and common-radius rows are exactly the rows it reads.

Finally use \autoref{thm:dyadicratcore-public-observation-realup-window-feed}
to expose the sealed ledger as the RealUp finite-window input. The primitive
and closure boundaries ensure that the export adds no quotient, no Classical
datum, no external metric primitive, and no RealUp completeness claim.
\end{proof}

\begin{theorem}[DyadicRatCore transported public observations feed RealUp windows]
\label{thm:dyadicratcore-public-observation-hsame-transport-bridge}
Let $d,d':\Hist$. Suppose $\hsame(d,d')$ holds and the public DyadicRatCore
observation package of
\autoref{thm:dyadicratcore-public-observation-package} supplies the observation
row at $d$. Then the $\hsame$-transported public observation row at $d'$ is
available as the same RealUp finite-window exhaustion input. The transported
row carries the same finite dyadic ledger, the same RatUp endpoint and
positive-denominator faces transported along $\hsame$, and the same
no-quotient, no-completion, no-Classical, no-external-metric, no-$\RealUp$
completeness, and no-standard-bridge boundary fixed by
\autoref{thm:dyadic-rat-core-scoped-primitive-dependency-package} and
\autoref{thm:dyadic-rat-core-scoped-closure-boundary}.
\end{theorem}
\begin{proof}
First apply
\autoref{thm:dyadicratcore-public-observation-hsame-transport} to the row at
$d$ and the comparison $\hsame(d,d')$. This gives the public observation row at
$d'$ with the RatUp endpoint face and positive-denominator face transported
along $\hsame$, while keeping the certified finite dyadic ledger fixed.

Next apply \autoref{thm:dyadicratcore-public-observation-realup-window-feed}.
That theorem reads the public DyadicRatCore observation package as exactly the
carrier, classifier, stability, and finite-ledger source rows consumed by
\autoref{thm:realup-finite-window-exhaustion-obligation}. Applying it after
transport therefore makes the row at $d'$ the same RealUp finite-window input.

Finally read the primitive and closure boundaries from
\autoref{thm:dyadic-rat-core-scoped-primitive-dependency-package} and
\autoref{thm:dyadic-rat-core-scoped-closure-boundary}. Since the composite uses
only the public transport row and the RealUp finite-window consumer already
listed in those boundaries, it adds no quotient, completion object, Classical
datum, external metric primitive, $\RealUp$ completeness claim, or standard
bridge.
\end{proof}

\begin{theorem}[DyadicRatCore public observation feeds RealUp finite-window exhaustion]
\label{thm:dyadicratcore-public-observation-realup-window-feed}
The public DyadicRatCore package of
\autoref{thm:dyadicratcore-public-observation-package}, with scope fixed by
\autoref{thm:dyadic-rat-core-scoped-primitive-dependency-package} and
\autoref{thm:dyadic-rat-core-scoped-closure-boundary}, supplies exactly the
carrier, classifier, stability, and finite-ledger source rows consumed by the
RealUp finite-window exhaustion obligation of
\autoref{thm:realup-finite-window-exhaustion-obligation}. It adds no quotient,
completion object, Classical datum, external metric primitive, $\RealUp$
completeness claim, or standard bridge.
\end{theorem}
\begin{proof}
First read the public face list from
\autoref{thm:dyadicratcore-public-observation-package}. The exported surface is
the observation-row carrier, classifier, arithmetic-stability,
monotone-radius, distance and absolute-value, operation-stability,
finite-ledger extraction, and $\RegSeqRat$ seal inventory.

Next read the primitive scope from
\autoref{thm:dyadic-rat-core-scoped-primitive-dependency-package} and
\autoref{thm:dyadic-rat-core-scoped-closure-boundary}. These results bind the
exported surface to $\mathsf{BHist}$, $\hsame$, the $\RatUp$ endpoint rows,
$\mathsf{PositiveUnaryDenominator}$, $\StreamNameUp$ request names, the
DyadicRatCore observation row, and the finite dyadic ledger, while excluding
quotients, completion objects, Classical data, external metric primitives,
$\RealUp$ completeness claims, and standard bridges.

Finally apply \autoref{thm:realup-finite-window-exhaustion-obligation}. Its
finite-window field consumes the endpoint $\RatUp$ classifiers, positive unary
denominators, nonempty unary endpoint rows, common e-one tail readback, and
the $\mathsf{DyadicRatCore}$ source rows exposed through the $\RegSeqRat$
finite-window dyadic source package. Therefore the public DyadicRatCore
observation package supplies exactly the carrier, classifier, stability, and
finite-ledger rows used at the RealUp finite-window boundary, with no extra
object or bridge added.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.DyadicRatCoreObservationRow\_realup\_window\_feed}

\begin{theorem}[DyadicRatCore public observation subwindow stability]
\label{thm:dyadicratcore-public-observation-subwindow-stability}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$ and indices
$n,j,r,m$, suppose
$$
\mathsf{RealStreamWindowClassifier}(x,y,n,m)
$$
and $j+r\le m$. If the public DyadicRatCore package supplies the carrier,
classifier, stability, and finite-ledger rows consumed by the RealUp
finite-window exhaustion obligation in
\autoref{thm:dyadicratcore-public-observation-realup-window-feed}, then the
certified subwindow
$$
\mathsf{RealStreamWindowClassifier}(x,y,n+j,r)
$$
produced by
\autoref{thm:real-stream-window-subwindow-extraction} inherits the same
DyadicRatCore observation-row carrier, classifier, stability, and finite dyadic
ledger source rows through the $\StreamNameUp$ finite-window public interface of
\autoref{thm:streamname-public-finite-window-interface}. The inherited rows
still feed \autoref{thm:realup-finite-window-exhaustion-obligation} and add no
quotient, completion object, Classical datum, external metric primitive,
$\RealUp$ completeness claim, or standard bridge.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-stream-window-subwindow-extraction} to the certified
source window with shift $j$ and radius $r$. The bound $j+r\le m$ gives the
shifted finite-window classifier
$$
\mathsf{RealStreamWindowClassifier}(x,y,n+j,r).
$$

Next read the shifted window through the public finite-window interface of
\autoref{thm:streamname-public-finite-window-interface}. The interface changes
only the selected finite anchor and radius; the $\StreamNameUp$ request names,
endpoint $\RatUp$ classifiers, and bounded finite-ledger rows remain the same
rows already exposed by the public DyadicRatCore package.

Finally apply
\autoref{thm:dyadicratcore-public-observation-realup-window-feed} to identify
the inherited carrier, classifier, stability, and finite-ledger source rows as
the rows consumed by
\autoref{thm:realup-finite-window-exhaustion-obligation}. Since the construction
uses only subwindow extraction, the public $\StreamNameUp$ finite-window
surface, and the DyadicRatCore public observation feed, it introduces no
quotient, completion object, Classical datum, external metric primitive,
$\RealUp$ completeness claim, or standard bridge.
\end{proof}

\begin{theorem}[RegSeqRat tail-shift regularity stability]
\label{thm:regseqrat-tail-shift-regularity-stability}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$, a certified window
$$
\mathsf{RealStreamWindowClassifier}(x,y,n,m),
$$
and indices $j,r$ with $j+r\le m$, define the tail-shifted streams by
$$
x_j(k)=x(n+j+k),\qquad y_j(k)=y(n+j+k).
$$
Then the finite $\RegSeqRat$ regularity surface for the shifted window
$(x_j,y_j,0,r)$ inherits the same endpoint
$\mathsf{DyadicRatCoreObservationRow}$ carrier, classifier, stability, and
finite dyadic ledger rows as the source window. These inherited rows are
exactly the rows fed to
\autoref{thm:realup-finite-window-exhaustion-obligation}, and the
tail-shifted surface adds no quotient, completion object, Classical datum,
external metric primitive, $\RealUp$ completeness claim, or standard bridge.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-stream-window-subwindow-extraction} to the certified
window with shift $j$ and radius $r$. The bound $j+r\le m$ gives the shifted
finite certificate at anchor $n+j$ and radius $r$, which is the zero-based
certificate for the tail-shifted streams $x_j$ and $y_j$ at window $(0,r)$.

Next apply
\autoref{thm:dyadicratcore-public-observation-subwindow-stability}. The
subwindow keeps the public DyadicRatCore observation-row carrier, classifier,
stability, and finite dyadic ledger rows fixed while changing only the finite
anchor read by the stream certificate.

Finally read the shifted certificate through
\autoref{thm:streamname-public-finite-window-interface} and close with
\autoref{thm:realup-finite-window-exhaustion-obligation}. The RealUp
finite-window consumer therefore receives the same DyadicRatCore endpoint rows
from the tail-shifted $\RegSeqRat$ surface that it receives from the original
finite window, with no additional primitive or bridge.
\end{proof}

\begin{theorem}[RegSeqRat tail-shift dyadic-tolerance stability]
\label{thm:regseqrat-tail-shift-dyadic-tolerance-stability}
For regular rational-observation streams $x,y:\mathbb{N}\to\Hist$, a certified
window
$$
\mathsf{RealStreamWindowClassifier}(x,y,n,m),
$$
and indices $j,r$ with $\mathsf{UnaryHistory}(j)$ and $j+r\le m$, define
tail-shifted streams by
$$
x_j(k)=x(n+j+k),\qquad y_j(k)=y(n+j+k).
$$
If the source window classifies $x$ and $y$ through the displayed $\RegSeqRat$
dyadic-tolerance ledger at every requested unary observation in the window,
then the shifted window $(x_j,y_j,0,r)$ classifies the shifted streams through
the transported dyadic-tolerance window. The transported classifier uses only
the same $\mathsf{UnaryHistory}$ request rows, $\hsame$ endpoint comparisons,
finite dyadic ledger rows, and $\Cont$ provenance already exposed by the source
$\RegSeqRat$ certificate.
\end{theorem}
\begin{proof}
Project the source certificate into its regularity rows and its dyadic
classifier rows. The regularity projection is the input used by
\autoref{thm:regseqrat-tail-shift-regularity-stability}; the classifier
projection is the finite list of dyadic-tolerance requests attached to the same
unary observation window.

Reindex the finite unary window by the common tail shift $j$. The hypothesis
$j+r\le m$ identifies every shifted request $k\le r$ with the source request
$n+j+k$, and $\mathsf{UnaryHistory}(j)$ keeps the transported request inside the
same unary-history ledger. No new endpoint row is generated; the shifted row is
the source row read at the transported index.

Apply \autoref{thm:regseqrat-tail-shift-regularity-stability} to keep both
shifted streams inside the finite $\RegSeqRat$ carrier. Then repack the
dyadic-tolerance rows through the same $\Cont$ ledger and the same $\hsame$
endpoint comparisons. The resulting packet is exactly the classifier for
$(x_j,y_j,0,r)$, with the tolerance window transported by the tail shift and no
extra quotient, completion object, external metric primitive, or standard
bridge.
\end{proof}

\begin{theorem}[RegSeqRat seal finite-window source coverage]
\label{thm:regseqrat-seal-finite-window-source-coverage}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$, a certified source
window
$$
\mathsf{RealStreamWindowClassifier}(x,y,n,m),
$$
and indices $j,r$ with $j+r\le m$, the selected bounded subwindow
$$
\mathsf{RealStreamWindowClassifier}(x,y,n+j,r)
$$
feeds the $\RegSeqRat$ seal with the same finite-window source package as the
source window. More precisely, the seal receives the same
DyadicRatCore observation-row carrier, classifier, stability rows, finite
dyadic ledger, $\StreamNameUp$ finite-window interface rows, and $\RealUp$
finite-window exhaustion rows already exposed at the source window. This
coverage uses no quotient, completion object, Classical datum, external metric
primitive, $\RealUp$ completeness claim, or standard bridge.
\end{theorem}
\begin{proof}
First apply
\autoref{thm:regseqrat-finite-window-dyadic-source-exhaustion} to the source
window. This identifies the bounded $\RegSeqRat$ observation data with the
scoped DyadicRatCore carrier, classifier, stability, and finite dyadic ledger
faces at the selected finite window.

Next apply
\autoref{thm:dyadicratcore-public-observation-subwindow-stability} with the
bound $j+r\le m$. The subwindow certificate keeps the same DyadicRatCore
observation-row carrier, classifier, stability rows, and finite dyadic ledger
while changing only the finite anchor. Read that shifted anchor through
\autoref{thm:streamname-public-finite-window-interface} to obtain the matching
$\StreamNameUp$ finite-window interface rows.

Finally apply
\autoref{thm:dyadicratcore-public-observation-realup-window-feed}. The
subwindow package is therefore exactly the finite-window source input consumed
by \autoref{thm:realup-finite-window-exhaustion-obligation}. Since every row is
obtained from the source exhaustion theorem, the public subwindow stability
theorem, the $\StreamNameUp$ interface, and the RealUp feed theorem, the seal
receives no quotient, completion object, Classical datum, external metric
primitive, $\RealUp$ completeness claim, or standard bridge.
\end{proof}

\begin{theorem}[RegSeqRat nested subwindow source coverage]
\label{thm:regseqrat-nested-subwindow-source-coverage}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$, a certified source
window
$$
\mathsf{RealStreamWindowClassifier}(x,y,n,m),
$$
and indices $j,r,j',r'$ with $j+r\le m$ and $j'+r'\le r$, the nested selected
window
$$
\mathsf{RealStreamWindowClassifier}(x,y,n+j+j',r')
$$
feeds the $\RegSeqRat$ seal with the same finite-window source package as the
original source window. In particular, it exposes the same DyadicRatCore
observation-row carrier, classifier, stability rows, finite dyadic ledger,
$\StreamNameUp$ finite-window interface rows, and $\RealUp$ finite-window
exhaustion rows. This nested coverage introduces no quotient, completion
object, Classical datum, external metric primitive, $\RealUp$ completeness
claim, or standard bridge.
\end{theorem}
\begin{proof}
Apply \autoref{thm:dyadicratcore-public-observation-subwindow-stability} to the
source certificate with the bound $j+r\le m$. This gives the selected package at
anchor $n+j$ and radius $r$ with the same DyadicRatCore observation-row carrier,
classifier, stability rows, and finite dyadic ledger rows as the source window.

Apply the same theorem again to the shifted package, now using the bound
$j'+r'\le r$. The second extraction selects the window at anchor
$n+j+j'$ and radius $r'$, and it preserves exactly the same observation-row
carrier, classifier, stability, and ledger rows because the first selected
package already carries those public DyadicRatCore rows.

Finally apply \autoref{thm:regseqrat-seal-finite-window-source-coverage} and
\autoref{thm:streamname-public-finite-window-interface}. These repack the nested
certificate as the same $\RegSeqRat$ finite-window source package consumed by
the seal and by
\autoref{thm:realup-finite-window-exhaustion-obligation}, with only the finite
anchor changed to $n+j+j'$ and with no quotient, completion object, Classical
datum, external metric primitive, $\RealUp$ completeness claim, or standard
bridge.
\end{proof}

\begin{theorem}[RegSeqRat finite-window seal source exhaustion]
\label{thm:regseqrat-finite-window-seal-source-exhaustion}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$ and any certified
finite $\RegSeqRat$-to-$\RealUp$ seal window
$$
\mathsf{RealStreamWindowClassifier}(x,y,n,m),
$$
the accepted source package of the seal is exactly the pair formed by the
public $\StreamNameUp$ finite-window interface and the
$\RegSeqRat$/$\DyadicRatCore$ dyadic source-exhaustion rows. The two sides use
the same $\mathsf{BHist}$ carrier, $\mathsf{UnaryHistory}$ request rows,
$\mathsf{ProbeBundle}$ entries, $\mathsf{InBundle}$ membership rows, $\hsame$
comparisons, $\Cont$ provenance, and finite dyadic ledger rows. No quotient,
completion object, Classical datum, metric-completeness witness,
$\RealUp$-completeness witness, or bridge datum is part of the source package.
\end{theorem}
\begin{proof}
For the projection direction, apply
\autoref{thm:regseqrat-finite-window-seal-exactness}. It identifies every seal
read with the finite-window source rows carried by the certified window, so the
seal source has no row outside the displayed finite packet.

Next apply \autoref{thm:streamname-public-finite-window-interface}. The
stream-name component of the projected source is exactly the public finite
window interface, including the same unary request rows, probe entries,
membership rows, $\mathsf{BHist}$ carrier, and $\hsame$ comparisons.

Apply \autoref{thm:regseqrat-seal-finite-window-source-coverage} and
\autoref{thm:realup-finite-window-exhaustion-obligation}. These identify the
remaining projected rows with the $\RegSeqRat$/$\DyadicRatCore$ dyadic
source-exhaustion rows, the shared $\Cont$ provenance, and the finite dyadic
ledger consumed by $\RealUp$.

Conversely, start from the public $\StreamNameUp$ finite-window interface
together with the listed $\RegSeqRat$/$\DyadicRatCore$ exhaustion rows. The
coverage theorem repacks these rows as the finite-window source package for
the seal, and the exactness theorem makes this repacked source the accepted
seal source for the same certified window.

Because both readings use only the displayed interface rows, exhaustion rows,
continuation provenance, and finite ledger entries, there is no remaining
field from which a quotient, completion object, Classical datum,
metric-completeness witness, $\RealUp$-completeness witness, or bridge datum
could be projected.
\end{proof}

\begin{theorem}[RegSeqRat finite-window seal composition]
\label{thm:regseqrat-finite-window-seal-composition}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$, a certified
DyadicRatCore public observation row over a finite window
$\mathsf{RealStreamWindowClassifier}(x,y,n,m)$, the $\StreamNameUp$
finite-window interface rows, the $\RegSeqRat$ seal source-coverage row, and
the $\RealUp$ finite-window feed compose to a single finite-window seal package
over the same $\mathsf{BHist}$ carrier, $\hsame$ comparisons, $\Cont$ ledger,
and finite dyadic observation rows. Every bounded subwindow and every nested
bounded subwindow selected inside the source window is read through that same
package. The composed seal asserts no quotient carrier, external metric
primitive, $\RealUp$ completeness theorem, or standard-bridge status.
\end{theorem}
\begin{proof}
Begin with
\autoref{thm:dyadicratcore-public-observation-realup-window-feed}. It exposes
the certified DyadicRatCore observation row as the finite dyadic ledger and
RealUp consumer row attached to the displayed source window.

Apply
\autoref{thm:dyadicratcore-public-observation-subwindow-stability}. The finite
window may be restricted without changing the underlying $\mathsf{BHist}$
carrier, $\hsame$ comparisons, $\Cont$ ledger, classifier rows, or finite dyadic
ledger rows.

Next use
\autoref{thm:regseqrat-seal-finite-window-source-coverage}. This repacks the
subwindow observation row together with the $\StreamNameUp$ finite-window
interface as the source package consumed by the $\RegSeqRat$ seal and by the
RealUp finite-window feed.

Finally apply
\autoref{thm:regseqrat-nested-subwindow-source-coverage}. Nested selected
windows are obtained by repeating the same public subwindow extraction, so the
seal package remains tied to the original finite dyadic ledger rows and the
same $\mathsf{BHist}$, $\hsame$, and $\Cont$ data. Since every component is read
from the cited finite-window rows, the construction introduces no quotient
carrier, external metric primitive, $\RealUp$ completeness theorem, or
standard-bridge status.
\end{proof}

\begin{theorem}[DyadicRatCore public observation standard bridge boundary]
\label{thm:dyadicratcore-public-observation-standard-bridge-boundary}
The public DyadicRatCore observation package of
\autoref{thm:dyadicratcore-public-observation-package}, together with
$\hsame$ transport, subwindow stability, $\StreamNameUp$ finite-window rows, and
the $\RealUp$ finite-window feed, supplies a conservative standard bridge from
concrete $\mathsf{BHist}$ and $\RatUp$ dyadic observation rows to the finite
dyadic observation interface consumed by $\RegSeqRat$ and $\RealUp$. The bridge
uses only the displayed observation-row carrier, classifier, stability rows,
finite dyadic ledger, stream-name finite-window interface, and RealUp
finite-window consumer. It excludes quotient carriers, completion objects,
external metric primitives, $\RealUp$ completeness, full DyadicRatCore
arithmetic, and any machine-checked bridge status.
\end{theorem}
\begin{proof}
First read the public surface from
\autoref{thm:dyadicratcore-public-observation-package}. The exported rows are
the concrete observation-row carrier, classifier, stability faces, finite
dyadic ledger, and $\RegSeqRat$ seal interface over $\mathsf{BHist}$,
$\hsame$, and the $\RatUp$ endpoint histories.

Next apply
\autoref{thm:dyadicratcore-public-observation-hsame-transport}. The
transport clause shows that replacing a dyadic observation row by an
$\hsame$-equivalent row preserves the same finite ledger and the same public
observation interface. Apply
\autoref{thm:dyadicratcore-public-observation-subwindow-stability} to restrict
the interface to any certified finite subwindow without changing the exposed
row vocabulary.

Then use \autoref{thm:regseqrat-nested-subwindow-source-coverage}. Its nested
window coverage identifies the $\RegSeqRat$ seal input with the same finite
dyadic source package after repeated subwindow selection, and it reads the
needed $\StreamNameUp$ rows through the public finite-window interface.

Finally apply
\autoref{thm:dyadicratcore-public-observation-realup-window-feed}. The
finite-window package delivered by the preceding steps is exactly the
observation interface consumed by the RealUp finite-window exhaustion row.
Since every step factors through the public DyadicRatCore package and its
finite-window consumers, the bridge is conservative: it introduces no quotient
carrier, completion object, external metric primitive, $\RealUp$ completeness,
full DyadicRatCore arithmetic, or machine-checked bridge status.
\end{proof}

\begin{closurestatus}{\DyadicRatCore}
  \theoryclosure{\bridgedClosure}
  \scopeclosed{\autoref{thm:dyadicratcore-public-observation-standard-bridge-boundary} witnesses the conservative paper bridge from public DyadicRatCore observation rows over $\mathsf{BHist}$, $\hsame$, RatUp endpoint histories, StreamNameUp finite-window rows, and the finite dyadic ledger to the interface consumed by $\RegSeqRat$ and $\RealUp$.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.RealUp.DyadicRatCoreObservationRow\_realup\_window\_feed}
  \bridgestatus{paperBridge}
  \notclaimed{This boundary does not close Lean verification, full DyadicRatCore arithmetic, metric completeness, RealUp completeness, quotient carriers, completion objects, or machine-checked bridge status.}
  \upgradepath{A stronger grade requires Lean verification of the bridge boundary together with full arithmetic, metric-completeness, and RealUp-completeness packages where those claims are made.}
\end{closurestatus}
