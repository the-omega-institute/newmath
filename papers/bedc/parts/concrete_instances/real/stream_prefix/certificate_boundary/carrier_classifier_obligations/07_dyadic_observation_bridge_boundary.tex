\autoref{thm:dyadicratcore-public-observation-standard-bridge-boundary}
is a $\RealUp$ local observation-boundary lemma. It records how the public
DyadicRatCore observation package, $\hsame$ transport, subwindow stability,
$\StreamNameUp$ finite-window rows, and the $\RealUp$ finite-window feed align
the finite dyadic ledger with the $\RegSeqRat$ and $\RealUp$ consumer
interfaces inside the carrier-obligation subtree.

The lemma does not assign a separate closure axis to $\DyadicRatCoreUp$ from
the $\RealUp$ certificate-boundary file. The authoritative $\DyadicRatCoreUp$
status is the canonical closure block in the DyadicRatCore chapter, where the
chapter records its object token, closure grade, verification grade, and
standard-bridge status as a single objectwise commitment.
