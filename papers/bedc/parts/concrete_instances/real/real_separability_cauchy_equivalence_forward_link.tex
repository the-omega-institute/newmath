The $\RealSeparabilityUp$ dense reads that compare equivalent regular Cauchy names pass through \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} after the $\RegSeqRatUp$ window route has displayed regular rational readback and before the terminal $\RealUp$ density export is consumed.
