The $\RealSeparabilityUp$ source lattice is not a kernel-only island: its rational enumeration is read from \autoref{ch:concrete-instances-rat-namecert}, its dyadic tolerance route is read from \autoref{ch:concrete-instances-dyadicratcore-namecert}, and its density handoff reaches $\CountableDenseSubsetUp$ only after the StreamName, RegSeqRat, and Real route has displayed finite windows, regular rational readback, and the terminal real-name seal.
