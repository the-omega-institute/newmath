\chapter{Certificate Obligations for $\RealUp$}
\label{ch:concrete-instances-real-namecert}

The real number interface $\RealUp$ is specified here as a certificate-obligation schema over sealed $\RegSeqRat$ stream names. These names are built from $\DyadicRatCore$ observations through $\StreamNameUp$, then sealed with regular-sequence data and a closeness classifier. The classical Dedekind / Cauchy completion is outside this schema because quotient and completion infrastructure is not exported by the mathlib-free BEDC kernel; the interface name is available only through the dyadic rational core, certified stream names, regular-sequence certificates, and their classifier.

\section{Carrier definition}

\begin{definition}[Real carrier]
\label{def:real-carrier}
Use a sealed $\RegSeqRat$ name. Its observations form a $\StreamNameUp$ stream over $\DyadicRatCore$, and its regularity certificate is exposed only through the $\RealUp$ seal.
\end{definition}

\begin{definition}[Constant real history carrier]
\label{def:real-constant-history-carrier}
A history is in the constant real history carrier when it is internally the same as an $\Eone$-sealed rational history:
$$
\mathsf{RealConstantHistoryCarrier}(h)
\quad\Longleftrightarrow\quad
\exists d,\ \hsame(h,\Eone(d))\ \wedge\ \mathsf{RatHistoryCarrier}(d).
$$
\end{definition}
\leandef{BEDC.Derived.RealUp.RealConstantHistoryCarrier}

\begin{theorem}[Constant real carrier at an e-one seal]
\label{thm:real-constant-history-carrier-e1-iff-rat}
For every history $d$,
$$
\mathsf{RealConstantHistoryCarrier}(\Eone(d))
\quad\Longleftrightarrow\quad
\mathsf{RatHistoryCarrier}(d).
$$
\end{theorem}
\begin{proof}
Unpack the constant carrier witness. One-constructor inversion turns
$\hsame(\Eone(d),\Eone(d'))$ into $\hsame(d,d')$, and rational history carrier
transport moves the witness back along that equality. The converse uses $d$
itself as the exposed rational history and reflexivity of $\hsame$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantHistoryCarrier\_e1\_iff\_rat}

\begin{theorem}[Constant real carrier e-zero exclusion]
\label{thm:real-constant-history-carrier-e0-absurd}
For every history tail $t$,
$$
\mathsf{RealConstantHistoryCarrier}(\Ezero(t))
\Longrightarrow
\bot.
$$
\end{theorem}
\begin{proof}
Unpack the constant real carrier witness. It displays $\Ezero(t)$ as
$\Eone(d)$ for some rational history $d$. BEDC history no-confusion between
zero-extension and one-extension constructors eliminates that display.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantHistoryCarrier\_e0\_absurd}

\begin{theorem}[Constant real carrier empty exclusion]
\label{thm:real-constant-history-carrier-empty-absurd}
The empty history is not a constant real history carrier:
$$
\mathsf{RealConstantHistoryCarrier}(\emp)
\Longrightarrow
\bot.
$$
\end{theorem}
\begin{proof}
Unpack the constant real carrier witness. It displays $\emp$ as an
$\Eone$-sealed rational history, and BEDC history no-confusion between the
empty constructor and the one-extension constructor eliminates that display.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantHistoryCarrier\_empty\_absurd}

\section{The certificate-obligation schema}

\begin{definition}[Real source specification]
\label{def:real-source-specification}
The source has three supplied layers: $\DyadicRatCore$ observations, $\StreamNameUp$ stream naming, and the $\RegSeqRat$ certificate over those observations.
\end{definition}

\begin{definition}[Real pattern specification]
\label{def:real-pattern-specification}
The pattern is sealed regular-sequence generation: every finite observation is a certified dyadic rational, stream observations are stable pointwise, and the $\RegSeqRat$ regularity witness supplies the dyadic Cauchy bound.
\end{definition}

\begin{definition}[Real classifier specification]
\label{def:real-classifier-specification}
The classifier is $\RegSeqRat$ closeness: two sealed real names classify together when their dyadic regular-sequence witnesses supply comparison certificates at every requested dyadic tolerance.
\end{definition}

\begin{definition}[Constant real history classifier]
\label{def:real-constant-history-classifier}
Two constant real histories classify together when each exposes an $\Eone$-sealed rational history and the exposed rational histories classify:
$$
\mathsf{RealConstantHistoryClassifier}(h,k)
\quad\Longleftrightarrow\quad
\exists d,e,\ \hsame(h,\Eone(d))\ \wedge\ \hsame(k,\Eone(e))
\ \wedge\ \mathsf{RatHistoryClassifier}(d,e).
$$
\end{definition}
\leandef{BEDC.Derived.RealUp.RealConstantHistoryClassifier}

\begin{theorem}[Constant real classifier at e-one seals]
\label{thm:real-constant-history-classifier-e1-iff-rat}
For all histories $d$ and $e$,
$$
\mathsf{RealConstantHistoryClassifier}(\Eone(d),\Eone(e))
\quad\Longleftrightarrow\quad
\mathsf{RatHistoryClassifier}(d,e).
$$
\end{theorem}
\begin{proof}
Eliminate the two seal witnesses. One-constructor inversion gives the two tail
$\hsame$ witnesses, and rational classifier transport carries the exposed
classifier back to the displayed tails. The reverse direction chooses the same
two rational histories and uses reflexivity at both seals.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantHistoryClassifier\_e1\_iff\_rat}

\begin{theorem}[Constant real classifier unary-tail closure]
\label{thm:real-constant-history-classifier-append-unary-tail-closed}
For all histories $d,e$ and unary history $t$,
$$
\mathsf{RealConstantHistoryClassifier}(\Eone(d),\Eone(e))
\Longrightarrow
\mathsf{RealConstantHistoryClassifier}(\Eone(d\cdot t),\Eone(e\cdot t)).
$$
\end{theorem}
\begin{proof}
Read the two sealed constant-real endpoints back through
\autoref{thm:real-constant-history-classifier-e1-iff-rat} as a rational
history classifier between $d$ and $e$. The rational classifier is closed under
appending the same unary denominator tail to both exposed histories, and the
same e-one bridge repacks the appended rational classifier as a constant-real
classifier.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantHistoryClassifier\_append\_unary\_tail\_closed}

\begin{theorem}[Constant real classifier double-seal readback]
\label{thm:real-constant-history-classifier-e1-pair-readback}
If $\mathsf{RealConstantHistoryClassifier}(h,k)$ holds and the two endpoints
are displayed as $\Eone(\Eone(d))$ and $\Eone(\Eone(e))$, then the exposed
tails $d$ and $e$ are unary histories and satisfy $d\hsame e$.
\end{theorem}
\begin{proof}
Transport the classifier along the two displayed endpoint equalities to obtain
$\mathsf{RealConstantHistoryClassifier}(\Eone(\Eone(d)),\Eone(\Eone(e)))$.
The constant real e-one equivalence reads this as a rational classifier
between $\Eone(d)$ and $\Eone(e)$. Rational e-one tail readback then exposes
the two unary tail carriers and the tail equality $d\hsame e$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantHistoryClassifier\_e1\_pair\_readback}

\begin{theorem}[Constant real classifier endpoint carriers]
\label{thm:real-constant-history-classifier-endpoint-carriers}
If two constant real histories classify together, then both endpoints are in the
constant real history carrier:
$$
\mathsf{RealConstantHistoryClassifier}(h,k)
\Longrightarrow
\mathsf{RealConstantHistoryCarrier}(h)\land
\mathsf{RealConstantHistoryCarrier}(k).
$$
\end{theorem}
\begin{proof}
Unpack the classifier witness into rational histories $d,e$, endpoint displays
$\hsame(h,\Eone(d))$ and $\hsame(k,\Eone(e))$, and a rational history
classifier between $d$ and $e$. The rational classifier stores rational carrier
witnesses at both exposed endpoints, so pairing those carrier witnesses with the
two endpoint displays gives the two constant real history carriers.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantHistoryClassifier\_endpoint\_carriers}

\begin{theorem}[Constant real classifier endpoint transport]
\label{thm:real-constant-history-classifier-endpoint-transport}
If constant real histories $h,k$ classify together, and $h\hsame h'$ and
$k\hsame k'$, then $h'$ and $k'$ classify together.
\end{theorem}
\begin{proof}
Unpack the two sealed rational witnesses. Symmetry and transitivity of
$\hsame$ move the displayed endpoint equalities from $h,k$ to $h',k'$, while
the rational classifier witness remains attached to the same sealed tails.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantHistoryClassifier\_endpoint\_transport}

\begin{theorem}[Constant real classifier e-one tail readback]
\label{thm:real-constant-history-classifier-e1-tail-readback}
If a constant real classifier is displayed at two visible rational seals, then
the displayed tails carry the rational history classifier:
$$
\mathsf{RealConstantHistoryClassifier}(h,k)\Longrightarrow
\hsame(h,\Eone(d))\Longrightarrow
\hsame(k,\Eone(e))\Longrightarrow
\mathsf{RatHistoryClassifier}(d,e).
$$
\end{theorem}
\begin{proof}
Transport the classifier along the two displayed endpoint equalities to obtain
$\mathsf{RealConstantHistoryClassifier}(\Eone(d),\Eone(e))$. The e-one seal
readback theorem for constant real classifiers then exposes the rational
classifier on the two tails.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantHistoryClassifier\_e1\_tail\_readback}

\begin{theorem}[Constant real classifier unary denominator context closure]
\label{thm:real-constant-history-classifier-cont-unary-denominator-context-closed}
Let $d,e,p_D,p_E,t_D,t_E,m_D,m_E,o_D,o_E:\mathsf{BHist}$. If
$$
\mathsf{RealConstantHistoryClassifier}(\Eone(d),\Eone(e)),
$$
$p_D$ and $t_D$ are unary histories, $p_D\hsame p_E$, $t_D\hsame t_E$, and the
displayed continuation witnesses are
$$
\mathsf{Cont}(p_D,d,m_D),\quad
\mathsf{Cont}(m_D,t_D,o_D),\quad
\mathsf{Cont}(p_E,e,m_E),\quad
\mathsf{Cont}(m_E,t_E,o_E),
$$
then
$$
\mathsf{RealConstantHistoryClassifier}(\Eone(o_D),\Eone(o_E)).
$$
\end{theorem}
\begin{proof}
Use \autoref{thm:real-constant-history-classifier-e1-iff-rat} to expose the
rational classifier on $d$ and $e$. The RatUp unary-denominator context closure
classifies the associativity-normal endpoints
$\mathsf{append}(p_D,\mathsf{append}(d,t_D))$ and
$\mathsf{append}(p_E,\mathsf{append}(e,t_E))$. The four displayed continuation
witnesses identify $o_D$ and $o_E$ with those normal endpoints through
associativity of append. Transport the rational classifier along those
identifications, then reseal the two endpoints with
\autoref{thm:real-constant-history-classifier-e1-iff-rat}.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantHistoryClassifier\_cont\_unary\_denominator\_context\_closed}

\begin{theorem}[Constant real classifier invalid endpoint exclusions]
\label{thm:real-constant-history-classifier-invalid-endpoint-absurd}
For every history $h$ and history tail $t$, the constant real-history classifier
excludes every endpoint shape that is not an $\Eone$-sealed rational history:
\begin{enumerate}
  \item $\mathsf{RealConstantHistoryClassifier}(\Ezero(t),h)\Longrightarrow\bot$;
  \item $\mathsf{RealConstantHistoryClassifier}(h,\Ezero(t))\Longrightarrow\bot$;
  \item $\mathsf{RealConstantHistoryClassifier}(\emp,h)\Longrightarrow\bot$;
  \item $\mathsf{RealConstantHistoryClassifier}(h,\emp)\Longrightarrow\bot$.
\end{enumerate}
In the ten-field fit, the traditional target is sealed constant-real endpoint
no-confusion; the BEDC source is $\mathsf{BHist}$ rational seals; the carrier is
$\mathsf{RealConstantHistoryCarrier}$; the classifier is
$\mathsf{RealConstantHistoryClassifier}$; the stability obligations are endpoint
carrier projection and carrier exclusions; the ledger is invalid endpoint shape;
the core theorem is this block; the exactness predicate is impossible
non-$\Eone$ endpoint; the standard bridge is constant rational real names; and
the status is $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
\begin{enumerate}
  \item Apply
  \autoref{thm:real-constant-history-classifier-endpoint-carriers} to the
  classifier witness. Its left projection gives
  $\mathsf{RealConstantHistoryCarrier}(\Ezero(t))$. Apply
  \autoref{thm:real-constant-history-carrier-e0-absurd}.

  \item Apply
  \autoref{thm:real-constant-history-classifier-endpoint-carriers} to the
  classifier witness. Its right projection gives
  $\mathsf{RealConstantHistoryCarrier}(\Ezero(t))$. Apply
  \autoref{thm:real-constant-history-carrier-e0-absurd}.

  \item Apply
  \autoref{thm:real-constant-history-classifier-endpoint-carriers} to the
  classifier witness. Its left projection gives
  $\mathsf{RealConstantHistoryCarrier}(\emp)$. Apply
  \autoref{thm:real-constant-history-carrier-empty-absurd}.

  \item Apply
  \autoref{thm:real-constant-history-classifier-endpoint-carriers} to the
  classifier witness. Its right projection gives
  $\mathsf{RealConstantHistoryCarrier}(\emp)$. Apply
  \autoref{thm:real-constant-history-carrier-empty-absurd}.
\end{enumerate}
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantHistoryClassifier\_invalid\_endpoint\_absurd}

\begin{theorem}[Constant real classifier equivalence fields]
\label{thm:real-constant-history-classifier-equivalence-fields}
The constant real history classifier supplies the equivalence fields carried by
the constant-real certificate package:
\begin{enumerate}
  \item if $\mathsf{RealConstantHistoryCarrier}(h)$, then
  $\mathsf{RealConstantHistoryClassifier}(h,h)$;
  \item if $\mathsf{RealConstantHistoryClassifier}(h,k)$, then
  $\mathsf{RealConstantHistoryClassifier}(k,h)$;
  \item if $\mathsf{RealConstantHistoryClassifier}(h,k)$ and
  $\mathsf{RealConstantHistoryClassifier}(k,\ell)$, then
  $\mathsf{RealConstantHistoryClassifier}(h,\ell)$.
\end{enumerate}
In the ten-field fit, the traditional target is constant rational embedding
into $\RealUp$; the BEDC source is $BHist$ rational histories; the carrier is
$\mathsf{RealConstantHistoryCarrier}$; the classifier is
$\mathsf{RealConstantHistoryClassifier}$; the stability obligation is the
equivalence fields above; the ledger is the sealed rational tail; the core
theorem is this displayed package; exactness is inherited from the rational
classifier; the standard bridge is constant rational real names; and the status
is $\mathsf{ProofTarget}$.
\end{theorem}
\begin{proof}
For reflexivity, unpack $\mathsf{RealConstantHistoryCarrier}(h)$ as a sealed
rational tail $d$ with $\hsame(h,\Eone(d))$ and
$\mathsf{RatHistoryCarrier}(d)$. The rational carrier supplies the reflexive
rational classifier on $d$, using $\hsame$ reflexivity on the exposed tail.
Packing the same sealed tail twice and using the displayed endpoint witness on
both sides gives $\mathsf{RealConstantHistoryClassifier}(h,h)$.

For symmetry, unpack
$\mathsf{RealConstantHistoryClassifier}(h,k)$ into sealed rational tails $d,e$,
endpoint displays for $h$ and $k$, and
$\mathsf{RatHistoryClassifier}(d,e)$. Keep the same endpoint displays in the
opposite order and apply $\mathsf{RatHistoryClassifier\_symm}$ to the rational
classifier.

For transitivity, unpack the two classifiers into tails $d,e$ and $e',f$. The
two middle displays for $k$ compare $\Eone(e)$ and $\Eone(e')$ by $\hsame$
symmetry and transitivity; one-constructor inversion turns that comparison into
$\hsame(e,e')$. Transport the right rational classifier from $e'$ to $e$, then
compose it with the left rational classifier by
$\mathsf{RatHistoryClassifier\_trans}$. Repacking the outer endpoint displays
for $h$ and $\ell$ gives the required classifier.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantHistoryClassifier\_equivalence\_fields}

\begin{theorem}[Constant real semantic name certificate]
\label{thm:real-constant-history-semantic-namecert}
The constant real history carrier and classifier form a
$\mathsf{SemanticNameCert}$ whose source, pattern, and ledger predicates are all
$\mathsf{RealConstantHistoryCarrier}$, and whose classifier is
$\mathsf{RealConstantHistoryClassifier}$.
\end{theorem}
\begin{proof}
Use the e-one rational denominator witnessed by $\emp$ to inhabit the source
carrier. The classifier equivalence fields of
\autoref{thm:real-constant-history-classifier-equivalence-fields} supply
reflexivity, symmetry, and transitivity. Endpoint-carrier readback from
\autoref{thm:real-constant-history-classifier-endpoint-carriers} supplies
carrier transport along the classifier. Since the pattern and ledger predicates
are the same concrete carrier, their soundness rows are direct.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantHistory\_semanticNameCert}

\begin{definition}[Real stream classifiers]
\label{def:real-stream-classifiers}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$, the stream classifier is pointwise rational-history classification. The finite prefix classifier records the same data recursively through a selected observation index.
\end{definition}
\leandef{BEDC.Derived.RealUp.RealStreamClassifier}
\leandef{BEDC.Derived.RealUp.RealStreamPrefixClassifier}

\begin{definition}[Real unary-observation stream classifier]
\label{def:real-unary-observation-stream-classifier}
For rational-observation maps $s,t:\Hist\to\Hist$, the unary-observation
stream classifier is pointwise rational-history classification over certified
unary BEDC observation histories:
$$
\mathsf{RealUnaryStreamClassifier}(s,t)
\quad\Longleftrightarrow\quad
\forall n,\ \mathsf{UnaryHistory}(n)\to
\mathsf{RatHistoryClassifier}(s(n),t(n)).
$$
\end{definition}
\leandef{BEDC.Derived.RealUp.RealUnaryStreamClassifier}

\begin{theorem}[Constant rational streams bridge StreamNameUp and RealUp]
\label{thm:streamname-real-constant-prefix-bridge}
For histories $d,e:\Hist$, the RatUp point classifier, the StreamNameUp
constant-stream classifier, the RealUp unary-observation stream classifier, and
the constant real-history classifier at e-one seals are equivalent:
$$
\mathsf{RatHistoryClassifier}(d,e)
\quad\Longleftrightarrow\quad
\mathsf{RatStreamNameClassifier}(\mathsf{RatConstStream}_d,
\mathsf{RatConstStream}_e).
$$
$$
\mathsf{RatStreamNameClassifier}(\mathsf{RatConstStream}_d,
\mathsf{RatConstStream}_e)
\quad\Longleftrightarrow\quad
\mathsf{RealUnaryStreamClassifier}(\mathsf{RatConstStream}_d,
\mathsf{RatConstStream}_e).
$$
$$
\mathsf{RealUnaryStreamClassifier}(\mathsf{RatConstStream}_d,
\mathsf{RatConstStream}_e)
\quad\Longleftrightarrow\quad
\mathsf{RealConstantHistoryClassifier}(\Eone(d),\Eone(e)).
$$
\end{theorem}
\begin{proof}
For the first equivalence, the forward direction is exactly the constant
classifier part of \autoref{thm:rat-stream-name-constant-witness}. Conversely,
unfold Definition~\ref{def:rat-stream-name-classifier} and read the pointwise
classifier at the empty unary observation history $\emp$ using its unary
witness; the two constant stream values are $d$ and $e$.

For the second equivalence, unfold
Definition~\ref{def:rat-stream-name-classifier} and
Definition~\ref{def:real-unary-observation-stream-classifier}. Both predicates
are the same pointwise requirement over certified unary observation histories,
so each direction is identity transport of the displayed witness.

For the last equivalence, unfold
Definition~\ref{def:real-unary-observation-stream-classifier} and
$\mathsf{RatConstStream}$. Reading at the empty unary observation gives
$\mathsf{RatHistoryClassifier}(d,e)$, while a rational point classifier supplies
the same pointwise witness at every unary observation.

Apply \autoref{thm:real-constant-history-classifier-e1-iff-rat} to identify that
rational point classifier with
$\mathsf{RealConstantHistoryClassifier}(\Eone(d),\Eone(e))$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealUnaryStreamClassifier\_constant\_prefix\_bridge\_iff}
\leanchecked{BEDC.Derived.RealUp.StreamNameReal\_constant\_prefix\_bridge}
\leanchecked{BEDC.Derived.RealUp.RealUnaryStreamClassifier\_constant\_streamname\_bridge}
\leanchecked{BEDC.Derived.RealUp.RealConstantStream\_streamName\_bridge}

\begin{theorem}[Reindexed constant rational streams bridge StreamNameUp and RealUp]
\label{thm:real-constant-reindexed-stream-bridge}
For histories $d,e:\Hist$ and any observation map $r:\Hist\to\Hist$, the
RatUp point classifier, the StreamNameUp classifier of reindexed constant
streams, the RealUp unary-observation stream classifier of the same reindexed
streams, and the constant real-history classifier at e-one seals are
equivalent:
$$
\mathsf{RatHistoryClassifier}(d,e)
\quad\Longleftrightarrow\quad
\mathsf{RatStreamNameClassifier}(\mathsf{RatConstStream}_d\circ r,
\mathsf{RatConstStream}_e\circ r).
$$
$$
\mathsf{RatStreamNameClassifier}(\mathsf{RatConstStream}_d\circ r,
\mathsf{RatConstStream}_e\circ r)
\quad\Longleftrightarrow\quad
\mathsf{RealUnaryStreamClassifier}(\mathsf{RatConstStream}_d\circ r,
\mathsf{RatConstStream}_e\circ r).
$$
$$
\mathsf{RealUnaryStreamClassifier}(\mathsf{RatConstStream}_d\circ r,
\mathsf{RatConstStream}_e\circ r)
\quad\Longleftrightarrow\quad
\mathsf{RealConstantHistoryClassifier}(\Eone(d),\Eone(e)).
$$
\end{theorem}
\begin{proof}
For the first equivalence, unfold $\mathsf{RatConstStream}$ and
Definition~\ref{def:rat-stream-name-classifier}. The forward direction reuses
the supplied $\mathsf{RatHistoryClassifier}(d,e)$ at every certified unary
observation; the observation map is ignored because both reindexed constant
streams still evaluate to their hidden seeds $d$ and $e$. Conversely, read the
stream-name classifier at the empty unary observation history $\emp$.

For the second equivalence, unfold
Definition~\ref{def:rat-stream-name-classifier} and
Definition~\ref{def:real-unary-observation-stream-classifier}. After expanding
the reindexed constant streams, both predicates ask for the same pointwise
RatUp classifier over certified unary observations, so each direction transports
the displayed pointwise witness unchanged.

For the last equivalence, the forward direction again reads the unary stream
classifier at $\emp$ to recover $\mathsf{RatHistoryClassifier}(d,e)$, then
applies \autoref{thm:real-constant-history-classifier-e1-iff-rat}. In the
reverse direction, the same theorem unseals the e-one classifier as the RatUp
point classifier, and the constant observation maps reuse it at every certified
unary observation.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealReindexedConstantStream\_streamName\_bridge}
\leanchecked{BEDC.Derived.RealUp.RealConstantStream\_reindexed\_streamName\_bridge}

The reindexed constant-stream certificate fit has ten fields: traditional target
constant rational real names under observation reindexing; BEDC source RatUp
plus StreamNameUp plus RealUp; carrier the reindexed constant rational
observation maps and e-one real seals; classifier the three displayed bridge
rows; stability from ignoring the observation map in constant streams; ledger
hidden rational seed plus observation map; core theorem
\autoref{thm:real-constant-reindexed-stream-bridge}; exactness the three
equivalences; standard bridge target constant rational real names under
reindexed observation; downstream use by the RealUp dyadic embedding theorem.

\begin{theorem}[Constant rational stream carriers bridge StreamNameUp and RealUp]
\label{thm:streamname-real-constant-carrier-bridge}
For every history $d:\Hist$, the RatUp point carrier, the StreamNameUp
constant-stream carrier, and the RealUp constant real-history carrier at the
e-one seal are equivalent:
$$
\mathsf{RatHistoryCarrier}(d)
\quad\Longleftrightarrow\quad
\mathsf{RatStreamNameCarrier}(\mathsf{RatConstStream}_d).
$$
$$
\mathsf{RatStreamNameCarrier}(\mathsf{RatConstStream}_d)
\quad\Longleftrightarrow\quad
\mathsf{RealConstantHistoryCarrier}(\Eone(d)).
$$
\end{theorem}
\begin{proof}
The first equivalence is the carrier row of
\autoref{thm:rat-stream-name-constant-point-exactness}, read symmetrically:
the StreamNameUp constant carrier is exactly the RatUp carrier of its hidden
constant seed.

For the second equivalence, compose that carrier row with
\autoref{thm:real-constant-history-carrier-e1-iff-rat}. A StreamNameUp
constant carrier first recovers $\mathsf{RatHistoryCarrier}(d)$, then the
e-one theorem seals it as
$\mathsf{RealConstantHistoryCarrier}(\Eone(d))$; the reverse direction unseals
the RealUp constant carrier to $\mathsf{RatHistoryCarrier}(d)$ and repacks it
as the StreamNameUp constant carrier.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantStreamCarrier\_streamName\_bridge}

The carrier certificate fit has ten fields: traditional target constant
rational real carrier embedding; BEDC source RatUp histories plus StreamNameUp
observations; carrier the displayed constant stream and e-one real seal;
classifier inherited from \autoref{thm:streamname-real-constant-prefix-bridge};
stability from RatUp carrier transport and RealConstant carrier transport;
ledger the hidden rational seed and observation witness; core theorem
\autoref{thm:streamname-real-constant-carrier-bridge}; exactness the two
displayed equivalence rows; standard bridge constant rational real name; status
$\mathsf{ProofTarget}$.

\begin{theorem}[Constant rational embedding certificate]
\label{thm:real-constant-rational-embedding-certificate}
The concrete map
$$
d\longmapsto (\mathsf{RatConstStream}_d,\Eone(d))
$$
from a RatUp history seed $d:\Hist$ supplies the constant rational embedding
into RealUp. Its carrier exactness is the chain
$$
\mathsf{RatHistoryCarrier}(d)
\quad\Longleftrightarrow\quad
\mathsf{RatStreamNameCarrier}(\mathsf{RatConstStream}_d)
\quad\Longleftrightarrow\quad
\mathsf{RealConstantHistoryCarrier}(\Eone(d)).
$$
Its classifier exactness, for histories $d,e:\Hist$, is the chain
$$
\mathsf{RatHistoryClassifier}(d,e)
\quad\Longleftrightarrow\quad
\mathsf{RatStreamNameClassifier}(\mathsf{RatConstStream}_d,\mathsf{RatConstStream}_e)
\quad\Longleftrightarrow\quad
\mathsf{RealUnaryStreamClassifier}(\mathsf{RatConstStream}_d,\mathsf{RatConstStream}_e)
\quad\Longleftrightarrow\quad
\mathsf{RealConstantHistoryClassifier}(\Eone(d),\Eone(e)).
$$
The certificate therefore packages the hidden rational seed, the constant
StreamNameUp observation map, and the e-one RealUp seal as one concrete
BHist-anchored rational-to-real name.
\end{theorem}
\begin{proof}
For carrier exactness, use
\autoref{thm:streamname-real-constant-carrier-bridge}. Its two displayed rows
identify the RatUp carrier with the StreamNameUp constant-stream carrier, and
then identify that carrier with the RealUp constant history carrier at the
e-one seal.

For classifier exactness, use
\autoref{thm:streamname-real-constant-prefix-bridge}. Its three displayed rows
identify the RatUp point classifier, the StreamNameUp constant-stream
classifier, the RealUp unary stream classifier, and the RealUp constant
history classifier at the paired e-one seals.

The endpoint part of the last row is the Real constant classifier equivalence
and transport mechanism: \autoref{thm:real-constant-history-classifier-e1-iff-rat}
turns an e-one sealed RealUp classifier into the underlying RatUp classifier
and back, while
\autoref{thm:real-constant-history-classifier-endpoint-transport} preserves the
same classifier under endpoint-equivalent real histories. These facts repack
the two chains as one certificate for the displayed map.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstant\_rational\_embedding\_certificate}

The constant rational embedding certificate has ten fields: traditional target
constant rational embedding; BEDC source RatUp plus StreamNameUp plus RealUp;
carrier the constant stream and e-one seal; classifier the displayed equivalence
chain; stability inherited from RatUp classifier fields and Real constant
endpoint transport; ledger hidden rational seed plus unary observation; core
theorem \autoref{thm:real-constant-rational-embedding-certificate}; exactness
the carrier and classifier chains; standard bridge ordinary rational constants;
status $\mathsf{ProofTarget}$.

\begin{theorem}[Reindexed constant rational stream carriers bridge StreamNameUp and RealUp]
\label{thm:real-constant-stream-carrier-reindexed-streamname-bridge}
For every history $d:\Hist$ and any observation map $r:\Hist\to\Hist$, the
RatUp point carrier, the StreamNameUp carrier of the reindexed constant stream,
and the RealUp constant real-history carrier at the e-one seal are equivalent:
$$
\mathsf{RatHistoryCarrier}(d)
\quad\Longleftrightarrow\quad
\mathsf{RatStreamNameCarrier}(\mathsf{RatConstStream}_d\circ r).
$$
$$
\mathsf{RatStreamNameCarrier}(\mathsf{RatConstStream}_d\circ r)
\quad\Longleftrightarrow\quad
\mathsf{RealConstantHistoryCarrier}(\Eone(d)).
$$
\end{theorem}
\begin{proof}
Unfold Definition~\ref{def:rat-stream-name-carrier} and
$\mathsf{RatConstStream}$. A carrier of $d$ supplies the carrier at every
certified unary observation because the reindexed constant stream still returns
the seed $d$. Conversely, reading the stream carrier at $\emp$ recovers the
RatUp carrier of $d$. Composing this row with
\autoref{thm:real-constant-history-carrier-e1-iff-rat} gives the e-one RealUp
carrier row.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantStreamCarrier\_reindexed\_streamName\_bridge}

\begin{theorem}[Reindexed constant rational embedding certificate]
\label{thm:real-reindexed-constant-rational-embedding-certificate}
For histories $d,e:\Hist$ and any observation map $r:\Hist\to\Hist$, the
concrete map
$$
d\longmapsto (\mathsf{RatConstStream}_d\circ r,\Eone(d))
$$
supplies the constant rational embedding into RealUp under certified
observation reindexing. Its carrier exactness is the chain
$$
\mathsf{RatHistoryCarrier}(d)
\quad\Longleftrightarrow\quad
\mathsf{RatStreamNameCarrier}(\mathsf{RatConstStream}_d\circ r)
\quad\Longleftrightarrow\quad
\mathsf{RealConstantHistoryCarrier}(\Eone(d)).
$$
Its classifier exactness is the chain displayed in
\autoref{thm:real-constant-reindexed-stream-bridge}.
\end{theorem}
\begin{proof}
For the reindexed carrier row, unfold $\mathsf{RatConstStream}$ and
Definition~\ref{def:rat-stream-name-carrier}. The forward direction reuses the
carrier of $d$ at every observation. Conversely, read the stream-name carrier at
$\emp$ to recover the hidden seed carrier.

The e-one carrier seal is exactly
\autoref{thm:real-constant-history-carrier-e1-iff-rat}. It transports
$\mathsf{RatHistoryCarrier}(d)$ to
$\mathsf{RealConstantHistoryCarrier}(\Eone(d))$ and back, so it composes with
the reindexed StreamNameUp carrier row to give the displayed carrier chain.

For classifier exactness, apply
\autoref{thm:real-constant-reindexed-stream-bridge}. Its three rows identify the
RatUp classifier, the StreamNameUp classifier of the reindexed constant streams,
the RealUp unary-observation stream classifier of those streams, and the RealUp
constant-history classifier at the paired e-one seals.
\end{proof}

The certificate fit has ten fields: traditional target constant rational real
names; BEDC source RatUp histories plus StreamNameUp unary observations; carrier
constant rational observation maps; classifier the displayed bridge; stability
inherited from RatUp classifier fields; ledger the constant rational history and
unary observation witness; core theorem
\autoref{thm:streamname-real-constant-prefix-bridge}; exactness the displayed
equivalence; standard bridge constant rational real sequence; status
$\mathsf{ProofTarget}$.
