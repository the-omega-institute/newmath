\begin{theorem}[Real separability Cauchy approximation-tree forward link]\label{thm:real-separability-cauchy-approximation-tree-forward-link}
Every $\CauchyApproximationTreeUp$ consumer that reads a countable dense $\RealSeparabilityUp$ surface does so through the finite dense-readback route named by this chapter. The consumed branch window first reads the $\DyadicRatCoreUp$ tolerance row of \autoref{ch:concrete-instances-dyadicratcore-namecert}, then the finite $\StreamNameUp$ observation surface of \autoref{ch:concrete-instances-streamname-namecert}, then the $\RegSeqRatUp$ readback row of \autoref{ch:concrete-instances-regseqrat-namecert}, and finally the $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert} before entering the finite tree packet of \autoref{ch:concrete-instances-cauchy-approximation-tree-namecert}. Thus the dense route exports finite approximation-tree windows, not a detached dense subset, a selected real representative, quotient stream equality, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Use the dense readback rows already displayed by $\RealSeparabilityUp$. They expose countable dense data only through dyadic tolerances, finite stream observations, regular rational readbacks, and Real seals.

Now read the $\CauchyApproximationTreeUp$ carrier. Its branch windows consume exactly those four rows before a Real-facing endpoint is sealed, so the dense surface is passed forward as finite tree-window data.

The carrier has no coordinate for detached dense subsets, selected representatives, quotient stream equality, countable choice, or ambient completeness. The forward link therefore preserves the concrete dense readback surface and adds no independent completion principle.
\end{proof}
