The $\RealSeparabilityUp$ separability packet consumes the equality-facing boundary of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} before it reads the countable dense real route through the regular-rational readback of \autoref{ch:concrete-instances-regseqrat-namecert} and the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}. Thus a dense-read consumer first identifies regular-Cauchy presentations through $\CauchyEquivalenceSetoidUp$, then reads the dyadic density windows exposed by $\RegSeqRatUp$ and $\DyadicRatCoreUp$. The route exports no host countable choice principle and no selected enumeration outside the displayed $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.
