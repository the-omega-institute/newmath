The $\RealSeparabilityUp$ connected-local route supplies dense finite rational and dyadic reads to connected and locally connected consumers only after the source has passed through $\RealUp$ and $\MetricUp$. A connected-space consumer may compare these reads with \autoref{ch:concrete-instances-connected-space-namecert}, while a local neighbourhood-component consumer may use the same finite density surface in \autoref{ch:concrete-instances-locally-connected-namecert}. Thus the hub records a substantive horizontal dependency from real separability to connected-space and local-connectedness reads, without selecting a nearest rational, quotient real representative, host dense subset, path, arc, or ambient completeness principle.
