\begin{theorem}[RealSeparability to-seed status route]
\label{thm:real-separability-to-seed-rotation-route}
The $\RealSeparabilityUp$ to-seed status is recorded by the displayed child route
$$
\DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp
$$
together with the existing Cauchy-equivalence, density, completion-facing, transport, replay, provenance, and local naming rows under \autoref{ch:concrete-instances-real-separability-namecert}. The route records only seed-level paper scope for dense-window reads: it does not upgrade the verification axis, does not assert mature $\RealUp$ completeness, and does not introduce a chosen dense enumeration or countable-choice schedule.
\end{theorem}
\begin{proof}
Read the hub registration \autoref{thm:real-separability-seed-status}. It identifies $\RealSeparabilityUp$ as a child-route family rather than as theorem body stored on the hub.

Next use \autoref{thm:real-separability-seed-status-route} and \autoref{thm:real-separability-seed-child-route}. They keep the dyadic tolerance ledger, finite StreamName windows, RegSeqRat readback, and terminal Real seal visible before any density consumer receives the separability packet.

The status conclusion is therefore a theory-axis statement about the seed route. The formal verification row remains the one recorded by the surrounding closurestatus block, and the route contains no mature Real-completeness coordinate, selected dense enumeration, or countable-choice data.
\end{proof}
