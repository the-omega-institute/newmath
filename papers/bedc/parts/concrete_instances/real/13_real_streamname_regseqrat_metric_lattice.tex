The current $\RealUp$ L10 carrier is read through the $\StreamNameUp$ finite observation interface of \autoref{ch:concrete-instances-streamname-namecert} and the $\RegSeqRatUp$ regularity certificate of \autoref{ch:concrete-instances-regseqrat-namecert}. The later $\MetricSpaceUp$ consumer in \autoref{ch:concrete-instances-metric-namecert} uses the resulting $\RealUp$-valued distance interface only after those two sibling certificates are present, so the Real route is a sibling lattice through StreamName and rational regular sequences before it becomes a metric-distance surface.
