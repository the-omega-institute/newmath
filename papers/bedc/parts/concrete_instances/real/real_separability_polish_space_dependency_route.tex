The $\RealSeparabilityUp$ route supplies the countable dense schedule read by
the complete-separable consumers of
\autoref{ch:concrete-instances-polishspace-namecert} and
\autoref{ch:concrete-instances-separablemetric-namecert}. A Polish-space or
separable-metric consumer reaches that schedule only after the Real-facing
density packet has exposed its $\StreamNameUp$ windows, $\RegSeqRatUp$
readback, dyadic tolerance rows, terminal $\RealUp$ seal, componentwise
$\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local
$\NameCert$ row. The dependency is therefore a finite L10 dense-schedule
handoff, not a host dense subset, selected nearest point, quotient real
equality, countable-choice selector, or ambient completeness claim.
