\begin{definition}[Real L10 dependency lattice]
\label{def:real-l10-dependency-lattice}
The $\RealUp$ L10 dependency lattice is the ordered finite certificate route
whose source rows are read in this order:
\begin{enumerate}
\item the $\DyadicRatCoreUp$ observation rows supplied by
  \autoref{ch:concrete-instances-dyadicratcore-namecert};
\item the $\StreamNameUp$ finite stream-window rows supplied by
  \autoref{ch:concrete-instances-streamname-namecert};
\item the $\RegSeqRatUp$ regular-rational classifier rows supplied by
  \autoref{ch:concrete-instances-regseqrat-namecert};
\item the local $\RealUp$ seal, endpoint readback, and provenance rows.
\end{enumerate}
The route is finite: it is carried by the displayed $\mathsf{BHist}$ packet,
transported only by $\hsame$, read only through $\Cont$, and named only by the
local $\NameCert$ row before the $\RealUp$ seal is consumed.
\end{definition}

\begin{theorem}[Real L10 source route non-escape]
\label{thm:real-l10-source-route-nonescape}
Every accepted $\RealUp$ source-route consumer for the L10 dependency lattice
must read the $\DyadicRatCoreUp$ dyadic observation rows, then the
$\StreamNameUp$ finite stream-window rows, then the $\RegSeqRatUp$
regular-rational classifier rows, and only then the local $\RealUp$ seal rows.
The consumer may transport these rows by the displayed $\hsame$ coordinates,
read them through the displayed $\Cont$ route, and package them with the
displayed $\Pkg$ and local $\NameCert$ rows.

No accepted source-route consumer has a coordinate for a quotient stream, host
completion, countable choice, host real equality, selected host limit,
ambient completeness principle, arithmetic package, order package, or
metric-completeness row.
\end{theorem}
\begin{proof}
Start with the carrier and source specification in
\autoref{def:real-carrier} and \autoref{def:real-source-specification}. The
carrier is a sealed $\RegSeqRat$ name over $\DyadicRatCore$ observations, and
the source specification lists exactly the dyadic observation layer, the
stream-name layer, and the regular-sequence certificate layer.

Now apply the concrete seal handoff in
\autoref{thm:realup-regseqrat-streamname-seal-handoff}. It reads those layers
through the sibling chapters
\autoref{ch:concrete-instances-dyadicratcore-namecert},
\autoref{ch:concrete-instances-streamname-namecert}, and
\autoref{ch:concrete-instances-regseqrat-namecert}, then repacks the result as
the local $\RealUp$ seal handoff. This gives the ordered L10 route of
\autoref{def:real-l10-dependency-lattice}.

Finally use the exhaustion and boundary theorems for the Real RegSeqRat
StreamName dependency triple,
\autoref{thm:real-regseq-streamname-seal-route-exhaustion} and
\autoref{thm:real-regseq-streamname-cauchy-completion-interface}. They say
that every accepted consumer is exhausted by the finite rows of the displayed
packet, with only $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ structural data.
Since none of the cited source, handoff, or dependency-triple blocks exports a
quotient stream, host completion, countable choice, host real equality,
selected host limit, ambient completeness, arithmetic, order, or
metric-completeness coordinate, the consumer has no route to escape the L10
lattice.
\end{proof}

\begin{theorem}[Real L10 interface totality]
\label{thm:real-l10-dependency-lattice-interface-totality}
Every current Phase Real and Completion interface read of an accepted $\RealUp$
L10 dependency-lattice packet factors through exactly the four displayed
faces of \autoref{def:real-l10-dependency-lattice}: $\DyadicRatCoreUp$
observations, $\StreamNameUp$ finite-window rows, $\RegSeqRatUp$
regular-rational rows, and the local $\RealUp$ seal and readback rows. The
transport allowed between these faces is only the displayed $\hsame$ data,
the displayed $\Cont$ routes, the displayed $\Pkg$ provenance, and the local
$\NameCert$ data; no further quotient, completion, host-real, selector,
choice, arithmetic, order, or metric-completeness interface row is exported.
\end{theorem}
\begin{proof}
Start with the ordered source route of
\autoref{thm:real-l10-source-route-nonescape}. It confines accepted L10
consumers to the dyadic observation layer, the StreamName finite-window
layer, the RegSeqRat regular-rational layer, and the local Real seal layer,
with transport only through the displayed structural rows.

Next use the open-phase consumer theorem
\autoref{thm:realup-open-phase-four-face-consumer-totality}. It identifies
the current Phase Real and Completion reads with the same four faces, so an
interface consumer cannot insert a fifth source row between the dependency
lattice and the public open-phase surface.

The two sibling handoff theorems give the middle faces. By
\autoref{thm:streamname-finite-window-real-handoff-coverage}, finite
StreamName windows reach the Real handoff only through their displayed
window, RegSeqRat, Real-boundary, transport, continuation, provenance, and
naming rows. By \autoref{thm:regseqrat-real-seal-handoff}, the
regular-rational face supplies exactly the seal-facing rows consumed by
$\RealUp$.

Finally apply the current-surface theorem for the dependency triple,
\autoref{thm:real-open-phase-dependency-triple-current-surface}. It
repackages the same DyadicRatCore, StreamName, RegSeqRat, and local Real rows
as the current interface surface. Since every cited route exports only
$\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ data besides those four faces, the
interface read is exhausted by the displayed L10 lattice.
\end{proof}

\begin{theorem}[Real L10 terminal face order]
\label{thm:real-l10-terminal-face-order}
Every accepted $\RealUp$ open-phase terminal read consumes the four faces of
\autoref{def:real-l10-dependency-lattice} in the order
$\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp$. The
terminal $\RealUp$ seal cannot be read before the dyadic observation rows, the
finite-window rows, and the regular-rational classifier rows have been
displayed through the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and
$\NameCert$ surface.

Consequently a terminal read has no coordinate through which to consume
quotient stream equality, selected limits, host real equality, ambient
completeness, or any seal-facing row outside the displayed four-face route.
\end{theorem}
\begin{proof}
Start from the ordered route in \autoref{def:real-l10-dependency-lattice}.
The lattice lists the dyadic observation face, the StreamName finite-window
face, the RegSeqRat classifier face, and only then the local Real seal face.

Apply the non-escape theorem
\autoref{thm:real-l10-source-route-nonescape}. It prevents any accepted
source-route consumer from bypassing the dyadic, stream, or regular-rational
faces before the local seal is reached.

Next use interface totality,
\autoref{thm:real-l10-dependency-lattice-interface-totality}. Since every
open Phase Real and Completion interface read factors through exactly those
four faces, a terminal read cannot insert a further interface row between the
RegSeqRat classifier and the Real seal.

The middle dependency is exact by
\autoref{thm:streamname-dyadic-regseq-real-pullback-exactness}: the stream
window read is pulled back to the same dyadic and regular-rational source
surface. The final handoff is exact by
\autoref{thm:regseqrat-real-seal-handoff}: the Real seal consumes the
regular-rational source rows already displayed by the finite window.

Coordinate exhaustion then excludes all other terminal readings. The cited
routes expose only $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ data
over the four named faces, so quotient stream equality, selected limits, host
real equality, ambient completeness, and outside seal data have no terminal
coordinate to consume.
\end{proof}

\begin{theorem}[Real L10 terminal comparison exhaustion]
\label{thm:real-l10-terminal-comparison-exhaustion}
Let $X$ and $Y$ be accepted terminal $\RealUp$ L10 reads. Every
completion-facing comparison between $X$ and $Y$ factors through the same
four faces
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp
$$
displayed in \autoref{def:real-l10-dependency-lattice}. The comparison may
use only the common $\BHist$ packet, the displayed $\hsame$ transports, the
displayed $\Cont$ routes, the displayed $\Pkg$ provenance, and the local
$\NameCert$ coordinates of those faces.

No completion-facing comparison of $X$ and $Y$ has a coordinate for quotient
stream equality, selected limits, host real equality, ambient completeness,
arithmetic or order packages, or metric-completeness rows.
\end{theorem}
\begin{proof}
Apply the terminal face order theorem,
\autoref{thm:real-l10-terminal-face-order}, to both accepted reads. It places
the dyadic observation face, the finite StreamName window face, the RegSeqRat
classifier face, and the Real seal face in the same terminal order for $X$
and $Y$.

Next apply the four-face route lock,
\autoref{thm:real-l10-four-face-route-lock}. A completion-facing comparison
cannot leave the displayed route once the two terminal reads have been
accepted on that route; its transport and readback data remain confined to
$\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ coordinates.

The shared finite surface is supplied by
\autoref{thm:streamname-regseqrat-real-dyadic-window-pullback}. It identifies
the StreamName window, RegSeqRat readback, DyadicRatCore ledger, and Real
seal boundary as one pullback surface for the two terminal reads.

Finally use the observation triangle
\autoref{thm:dyadicratcore-streamname-regseqrat-observation-triangle}. The
dyadic, stream, and regular-rational observations meet on the same finite
triangle before the Real seal is consumed. Since the cited routes export no
quotient stream equality, selected limit, host real equality, ambient
completeness, arithmetic or order package, or metric-completeness coordinate,
the comparison is exhausted by the four displayed faces.
\end{proof}

\begin{theorem}[Real L10 metric boundary non-escape]
\label{thm:real-l10-metric-boundary-nonescape}
Every accepted current Phase Real or Completion consumer of a $\RealUp$ L10
packet factors through the four-face route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp
$$
before any $\MetricUp$ consumer or metric-completeness consumer can read the
packet. The visible route consists only of the DyadicRatCore observation face,
the StreamName finite-window face, the RegSeqRat regular-rational classifier
face, the local $\RealUp$ seal face, and the displayed $\BHist$, $\hsame$,
$\Cont$, $\Pkg$, and $\NameCert$ coordinates.

The route exports no \texttt{MetricDistanceWitness}, separatedness row,
completeness row, Cauchy-limit selector, metric triangle row, or ambient
metric-completeness coordinate. Any $\MetricUp$-facing read is therefore an
external consumer boundary for an already sealed $\RealUp$ packet, not an
input row used to construct the Real seal.
\end{theorem}
\begin{proof}
First apply the terminal comparison exhaustion theorem,
\autoref{thm:real-l10-terminal-comparison-exhaustion}. It keeps every
completion-facing comparison between accepted terminal reads on the same
four-face surface, using only the common $\BHist$, $\hsame$, $\Cont$, $\Pkg$,
and $\NameCert$ coordinates.

Next apply the four-face canonical route,
\autoref{thm:real-l10-four-face-canonical-route}. It says that every current
Phase Real or Completion consumer of the $\RealUp$ packet must pass through
the ordered DyadicRatCore, StreamName, RegSeqRat, and local Real faces before
the consumer surface is available.

The finite overlap of those faces is the pullback surface of
\autoref{thm:streamname-regseqrat-real-dyadic-window-pullback}. Thus the
StreamName window, RegSeqRat readback, DyadicRatCore ledger, and Real seal
boundary are read as one displayed route.

Finally view the $\MetricUp$ public interface only as a downstream consumer
boundary. Because the cited Real route exports no metric distance witness,
separatedness row, completeness row, Cauchy-limit selector, metric triangle
row, or ambient metric-completeness coordinate, no metric consumer can supply
an input used by the $\RealUp$ L10 seal.
\end{proof}

\begin{theorem}[Real L10 four-face canonical route]
\label{thm:real-l10-four-face-canonical-route}
Every accepted current Phase Real or Completion consumer of a $\RealUp$ L10
packet has a canonical finite route through exactly the four faces
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp
$$
of \autoref{def:real-l10-dependency-lattice}. The route is displayed only by
the common $\BHist$ packet, the shown $\hsame$ transports, the shown $\Cont$
routes, the shown $\Pkg$ provenance, and the local $\NameCert$ rows.

If two route presentations have the same four faces and the same displayed
$\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, then they induce
the same consumer surface. No quotient stream, selected host limit, host real
equality, countable-choice selector, ambient completeness principle,
arithmetic or order package, or metric-completeness row can serve as an
alternate route.
\end{theorem}
\begin{proof}
The four faces are the faces of the dependency lattice in
\autoref{def:real-l10-dependency-lattice}. Apply the non-escape theorem,
\autoref{thm:real-l10-source-route-nonescape}, to exclude any route that
leaves the dyadic, StreamName, RegSeqRat, and local Real seal sequence.

Next apply interface totality,
\autoref{thm:real-l10-dependency-lattice-interface-totality}. It says that
every current Phase Real and Completion interface read factors through
exactly those four faces, using only the displayed $\hsame$, $\Cont$, $\Pkg$,
and $\NameCert$ rows. The terminal order theorem,
\autoref{thm:real-l10-terminal-face-order}, fixes their order as
$\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp$.

For comparisons of two accepted presentations, apply
\autoref{thm:real-l10-terminal-comparison-exhaustion}. It confines every
completion-facing comparison to the common $\BHist$, $\hsame$, $\Cont$,
$\Pkg$, and $\NameCert$ data over the same ordered four faces.

The shared finite consumer surface is then the pullback surface of
\autoref{thm:streamname-regseqrat-real-dyadic-window-pullback} together with
the observation triangle of
\autoref{thm:dyadicratcore-streamname-regseqrat-observation-triangle}. These
identify the dyadic observations, finite stream windows, regular-rational
readback, and Real seal boundary before the consumer surface is read. With no
exported coordinate for quotient streams, selected host limits, host real
equality, countable choice, ambient completeness, arithmetic or order
packages, or metric-completeness rows, no alternate route remains.
\end{proof}

\begin{theorem}[Real StreamName RegSeqRat seal lattice]
\label{thm:real-streamname-regseqrat-seal-lattice}
Every completion-facing $\RealUp$ consumer that also carries a $\CauchyLimitSealUp$ row factors through the ordered four faces
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp
$$
of \autoref{def:real-l10-dependency-lattice}. The $\CauchyLimitSealUp$ row is only the compatibility row for the already displayed seal route; it does not add a second completion face, quotient stream equality, selected limit, host real equality, ambient completeness, or metric-completeness row.
\end{theorem}
\begin{proof}
Start with \autoref{def:real-l10-dependency-lattice}. Its source route is the ordered finite lattice of dyadic observation rows, finite StreamName windows, regular-rational classifier rows, and the local Real seal.

Apply \autoref{thm:real-l10-four-face-canonical-route}. Every current Phase Real or Completion consumer already has a canonical route through exactly those four faces, with only the common $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows as structural data.

For completion-facing comparisons, use \autoref{thm:real-l10-terminal-comparison-exhaustion}. It keeps both terminal reads on the same four-face route. The shared finite surface is the pullback of \autoref{thm:streamname-regseqrat-real-dyadic-window-pullback}, so the StreamName window, RegSeqRat readback, DyadicRatCore ledger, and Real seal boundary remain one displayed route.

The extra $\CauchyLimitSealUp$ row is therefore read as the compatibility surface supplied by \autoref{ch:concrete-instances-cauchylimitseal-namecert}. Since the canonical route and terminal comparison already exhaust the consumer surface, the seal row cannot add quotient stream equality, selected limits, host real equality, ambient completeness, or metric-completeness data.
\end{proof}

\begin{theorem}[RealUp stationary partition normal-form boundary]
\label{thm:realup-stationary-partition-normal-form-boundary}
Every stationary-rational partition normal-form read used by the Real-completeness diagonal route is accepted by $\RealUp$ only through the four displayed faces of \autoref{def:real-l10-dependency-lattice}:
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp.
$$
The accepted read consists of DyadicRatCore observation rows, StreamName finite-window rows, RegSeqRat regular-rational row classifiers, and the local $\RealUp$ seal row, together with the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ coordinates.

In particular, the normal form of \autoref{thm:real-completeness-stationary-partition-normal-form} may be consumed on the concrete $\RealUp$ surface only through this four-face route. It supplies no coordinate for quotient stream equality, selected host limits, countable choice, ambient completeness, arithmetic or order packages, or metric-completeness rows.
\end{theorem}
\begin{proof}
Start with the stationary normal-form theorem in \autoref{thm:real-completeness-stationary-partition-normal-form}. Its finite partition spine reduces every stationary segment ledger to a one-window classifier and then feeds the diagonal representatives through the stationary rational RealUp embedding route.

Apply the Real L10 four-face canonical route of \autoref{thm:real-l10-four-face-canonical-route}. Any current Phase Real or Completion consumer of that diagonal read must therefore pass through exactly the displayed DyadicRatCore, StreamName, RegSeqRat, and local RealUp faces, using only the common $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

The seal compatibility is the seal lattice of \autoref{thm:real-streamname-regseqrat-seal-lattice}. It reads the completion-facing seal as compatibility for the already displayed StreamName and RegSeqRat handoff, not as a second completion face.

For comparisons between two stationary partition presentations, use \autoref{thm:real-l10-terminal-comparison-exhaustion}. It keeps both terminal reads on the same four-face route and leaves no comparison coordinate outside the DyadicRatCore observations, finite StreamName windows, RegSeqRat rows, and local RealUp seal.

Thus the Real-completeness stationary partition normal form lands on the concrete L10 lattice as a finite four-face certificate. The cited routes export no quotient stream equality, selected host limit, countable-choice selector, ambient completeness principle, arithmetic or order package, or metric-completeness row, so none of those data can be read from the accepted $\RealUp$ surface.
\end{proof}

\begin{theorem}[Real L10 stationary diagonal route exhaustion]
\label{thm:real-l10-stationary-diagonal-route-exhaustion}
For any current Phase Real or Completion consumer, access to a stationary partition diagonal read is admissible exactly when the read factors through the displayed four-face route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp
$$
of \autoref{def:real-l10-dependency-lattice}. The admissible factorization consists of the DyadicRatCore observation face, the StreamName finite-window face, the RegSeqRat regular-rational face, and the local $\RealUp$ seal face, together with the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ coordinates.

Moreover, every comparison between two admissible stationary diagonal reads remains on the same four-face route. The correspondence supplies no quotient stream equality, selected host limit, countable-choice row, ambient completeness theorem, trichotomy row, arithmetic package, order package, or metric-completeness row.
\end{theorem}
\begin{proof}
For the forward direction, start with an admissible stationary partition diagonal read. Apply \autoref{thm:real-completeness-stationary-partition-normal-form} to put the stationary partition data in its finite normal form, with the diagonal representatives presented through the stationary rational $\RealUp$ embedding route.

Now apply \autoref{thm:real-l10-four-face-canonical-route}. The current Phase Real or Completion consumer must read that normal form through exactly the displayed DyadicRatCore, StreamName, RegSeqRat, and local $\RealUp$ faces, using only the common $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. The seal compatibility is then the lattice handoff of \autoref{thm:real-streamname-regseqrat-seal-lattice}, so the completion-facing seal is only compatibility for the same displayed StreamName and RegSeqRat handoff.

For the reverse direction, suppose a stationary diagonal packet is already factored through those four faces with the displayed coordinates. The same normal-form theorem supplies the stationary partition diagonal read, and the four-face canonical route repacks the displayed dyadic observations, finite StreamName windows, regular-rational classifiers, and local seal as an admissible current consumer read.

It remains to check comparisons. Apply \autoref{thm:real-l10-terminal-comparison-exhaustion}. Any comparison between two such stationary diagonal reads stays on the same ordered four-face route and has no comparison coordinate outside the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ data.

The route therefore exhausts stationary diagonal admissibility. Since the cited normal form, canonical route, seal lattice, and terminal comparison theorem do not export quotient stream equality, selected host limits, countable choice, ambient completeness, trichotomy, arithmetic or order packages, or metric-completeness rows, none of those data are available through this correspondence.
\end{proof}

\begin{theorem}[Real L10 terminal four-face correspondence]
\label{thm:real-l10-terminal-four-face-correspondence}
A terminal $\RealUp$ L10 read is accepted exactly when its visible rows factor through the four faces
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp
$$
of \autoref{def:real-l10-dependency-lattice}. The factorization uses only displayed $\BHist$ observations, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ coordinates on those four faces.

Equivalently, any four-face packet with those coordinates repacks as the current terminal $\RealUp$ seal read. The correspondence exports no quotient stream equality, selected host limit, countable-choice selector, ambient completeness principle, arithmetic package, order package, or metric-completeness row.
\end{theorem}
\begin{proof}
For the forward direction, apply \autoref{thm:real-l10-four-face-canonical-route}. It routes every current terminal Real and Completion read through the ordered DyadicRatCore, StreamName, RegSeqRat, and Real faces of the dependency lattice.

Next use \autoref{thm:real-l10-terminal-comparison-exhaustion}. It says that terminal comparison has no surface outside the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ coordinates on the same ordered four faces. Thus an accepted terminal read has exactly the visible packet stated above.

For the reverse direction, start with a packet already factored through the four faces and the displayed coordinates. Apply \autoref{thm:real-streamname-regseqrat-seal-lattice} to read the StreamName and RegSeqRat handoff as the terminal $\RealUp$ seal surface, with the DyadicRatCore observation ledger kept in the same lattice route.

The reverse repacking adds no hidden coordinate. Since the cited route, comparison, and seal theorems do not export quotient stream equality, selected host limits, countable choice, ambient completeness, arithmetic or order packages, or metric-completeness rows, the repacked terminal read is accepted precisely as the current Real seal read.
\end{proof}

\begin{theorem}[Real L10 four-face no-completeness exactness]
\label{thm:real-l10-four-face-no-completeness-exactness}
For every accepted current Phase Real and Completion terminal $\RealUp$ read, the exact accepted surface is the four-face packet
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp
$$
of \autoref{def:real-l10-dependency-lattice}, together with displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows on those faces. The read contains no quotient stream equality, selected host limit, countable-choice selector, ambient completeness principle, metric-completeness row, or standard bridge datum.
\end{theorem}
\begin{proof}
Start with the dependency lattice of \autoref{def:real-l10-dependency-lattice}. It names the available terminal route as the ordered DyadicRatCore, StreamName, RegSeqRat, and local Real faces.

Apply \autoref{thm:real-l10-four-face-canonical-route}. Every accepted current Phase Real and Completion terminal read must factor through those four faces and through the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Next use \autoref{thm:real-streamname-regseqrat-seal-lattice}. The StreamName and RegSeqRat handoff is read only as seal compatibility for the same displayed route, with the DyadicRatCore observation face and the local $\RealUp$ face kept in the packet.

Finally apply \autoref{thm:real-l10-terminal-four-face-correspondence}. The terminal correspondence repacks exactly that four-face surface as the accepted $\RealUp$ read and exports no quotient stream equality, selected host limit, countable choice, ambient completeness, metric-completeness row, or standard bridge datum.
\end{proof}

\begin{theorem}[Real L10 four-face exactness]
\label{thm:real-l10-four-face-exactness}
For every current Phase Real or Completion terminal $\RealUp$ read, acceptance is equivalent to factorization of its visible rows through the finite four-face packet
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp
$$
of \autoref{def:real-l10-dependency-lattice}. The packet is exactly the displayed $\BHist$ observations, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ coordinates on those four faces.

Conversely, every finite packet with precisely those four faces and precisely those displayed coordinates determines the same accepted terminal $\RealUp$ seal read. The exact boundary exports no quotient stream equality, selected host limit, host real equality, countable-choice selector, ambient completeness principle, arithmetic package, order package, metric-completeness row, or standard bridge row.
\end{theorem}
\begin{proof}
For the forward direction, start with an accepted current Phase Real or Completion terminal read. Apply \autoref{thm:real-l10-four-face-canonical-route} to route it through the ordered DyadicRatCore, StreamName, RegSeqRat, and local Real faces.

Next apply \autoref{thm:real-l10-terminal-comparison-exhaustion}. It confines terminal comparison to the common $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows over the same ordered faces. Thus the accepted read supplies no visible coordinate beyond the finite packet in the statement.

For the reverse direction, suppose the visible rows already form such a four-face packet. Apply \autoref{thm:real-l10-terminal-four-face-correspondence} to repack the displayed rows as the terminal $\RealUp$ read, and use \autoref{thm:real-streamname-regseqrat-seal-lattice} to read the StreamName and RegSeqRat handoff as the same local Real seal surface.

It remains to check exactness of the boundary. The non-escape theorem, \autoref{thm:real-l10-source-route-nonescape}, excludes any route that leaves the displayed source lattice, while interface totality, \autoref{thm:real-l10-dependency-lattice-interface-totality}, says that every current Phase Real and Completion interface row is already accounted for by those displayed faces and coordinates. Therefore quotient stream equality, selected host limits, host real equality, countable choice, ambient completeness, arithmetic or order packages, metric-completeness rows, and standard bridge rows are not part of the accepted terminal boundary.
\end{proof}

\begin{theorem}[Real L10 terminal pullback uniqueness]
\label{thm:real-l10-terminal-pullback-uniqueness}
Let two current Phase Real and Completion terminal $\RealUp$ reads expose the same DyadicRatCore, StreamName, RegSeqRat, and local $\RealUp$ faces, with the same displayed $\BHist$ observations, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\Pkg$ provenance, and local $\NameCert$ coordinates. Then the accepted consumer boundary of the two reads is the same finite four-face pullback surface
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp.
$$
The uniqueness claim is only about this finite boundary packet. It does not identify quotient streams, host real values, selected host limits, countable-choice selectors, ambient completeness principles, arithmetic or order packages, metric-completeness rows, or standard bridge rows.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-l10-four-face-exactness} to each terminal read. Both accepted reads are identified with the finite packet over DyadicRatCore, StreamName, RegSeqRat, and the local Real seal, using only the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ coordinates.

The two identified packets have the same four faces and the same displayed coordinates by hypothesis. Apply \autoref{thm:real-l10-terminal-four-face-correspondence} to repack either packet as the terminal $\RealUp$ read; the repacking adds no hidden coordinate.

Use \autoref{thm:real-streamname-regseqrat-seal-lattice} for seal compatibility. The StreamName and RegSeqRat handoff is the same local $\RealUp$ seal surface on both sides, so the consumer boundary is the common four-face pullback packet.

Finally apply non-escape and interface totality, \autoref{thm:real-l10-source-route-nonescape} and \autoref{thm:real-l10-dependency-lattice-interface-totality}. They keep the boundary inside the displayed source lattice and exhaust the current Phase Real and Completion interface rows, excluding quotient stream equality, host real equality, selected limits, countable choice, ambient completeness, arithmetic or order packages, metric-completeness rows, and standard bridge rows.
\end{proof}

\begin{theorem}[Real L10 terminal source lock]
\label{thm:real-l10-terminal-source-lock}
Every $\RealUp$ seal or completion-facing consumer in the L10 route is admitted exactly through the displayed source lock
$$
\StreamNameUp,\ \DyadicRatCoreUp,\ \RegSeqRatUp,\ \RealUp.
$$
The lock consists of finite stream windows, dyadic endpoint and radius rows, the regular-sequence classifier handoff, componentwise $\hsame$ transport, displayed $\Cont$ route replay, $\Pkg$ provenance, and the local $\RealUp$ seal. No quotient stream, host real equality, selected limit, ambient completeness principle, or hidden completion carrier is exported by this terminal route.
\end{theorem}
\begin{proof}
Start with the L10 dependency lattice of \autoref{def:real-l10-dependency-lattice}. It displays the four source faces and fixes the structural rows to $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ before the local $\RealUp$ seal can be consumed.

Use the RegSeqRat source lattice pointer through \autoref{thm:realup-regseqrat-streamname-seal-handoff}. The regular-sequence classifier handoff is read only after the StreamName finite windows and the DyadicRatCore observation rows have been displayed.

Next apply the RealUp seal handoff and the four-face route, \autoref{thm:real-streamname-regseqrat-seal-lattice} and \autoref{thm:real-l10-four-face-canonical-route}. They place every current seal or completion-facing consumer on the same ordered route through StreamName, DyadicRatCore, RegSeqRat, and the local Real seal.

Finally use \autoref{thm:real-l10-terminal-comparison-exhaustion} and \autoref{thm:real-l10-terminal-four-face-correspondence}. These exhaust terminal comparisons by the displayed finite windows, dyadic endpoint and radius data, regular readback, transport, continuation replay, package provenance, and local naming. Since none of the cited rows carries quotient streams, host real equality, selected limits, ambient completeness, or a hidden completion carrier, the terminal source lock is exact.
\end{proof}

\begin{theorem}[Real L10 StreamName exit route correspondence]
\label{thm:real-l10-streamname-exit-route-correspondence}
A current Phase Real and Completion terminal $\RealUp$ read is accepted by the Real L10 dependency lattice exactly when its visible finite packet is the same four-object exit route named by StreamName,
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp.
$$
Equivalently, the accepted packet is the displayed DyadicRatCore observation face, StreamName finite-window face, RegSeqRat regular-rational face, and local $\RealUp$ seal face, with only the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$ rows.

This correspondence is only a paper-side exit surface. It exports no quotient stream equality, host sequence equality, selected host limit, countable-choice selector, ambient $\RealUp$ completeness principle, metric-completeness row, arithmetic package, order package, or standard bridge row.
\end{theorem}
\begin{proof}
For the forward direction, start with a current Phase Real and Completion terminal $\RealUp$ read accepted by the Real L10 dependency lattice. Apply \autoref{thm:real-l10-four-face-exactness} to identify its visible rows with the finite four-face packet over DyadicRatCore, StreamName, RegSeqRat, and the local Real seal.

Next use the terminal source lock of \autoref{thm:real-l10-terminal-source-lock}. It fixes the same terminal read to the displayed source route and prevents any additional completion carrier or host equality coordinate from entering the packet.

Identify this packet with the StreamName exit surface by \autoref{thm:streamname-real-completion-four-object-exit-route}. The four objects are the same $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ faces, and the support rows remain exactly $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and local $\NameCert$.

Finally apply \autoref{thm:streamname-real-completion-exit-axis-separation}. It keeps the route as paper-side exit data only, so quotient stream equality, host sequence equality, selected host limits, countable choice, ambient Real completeness, metric completeness, arithmetic or order packages, and standard bridge rows are outside the accepted surface.

For the reverse direction, suppose the visible finite packet is already the StreamName four-object exit route with the displayed support rows. Apply \autoref{thm:streamname-real-completion-four-object-exit-route} to read it as the current Phase Real and Completion exit packet.

Repack that packet through \autoref{thm:real-l10-terminal-four-face-correspondence}. The DyadicRatCore observations, StreamName finite windows, RegSeqRat readback, and local Real seal become the terminal $\RealUp$ L10 read, with no hidden coordinate added.

Use \autoref{thm:real-streamname-regseqrat-seal-lattice} for the final seal compatibility. It reads the StreamName and RegSeqRat handoff as the same local $\RealUp$ seal surface, so the Real L10 lattice accepts precisely the route named by StreamName and nothing beyond the displayed rows.
\end{proof}
