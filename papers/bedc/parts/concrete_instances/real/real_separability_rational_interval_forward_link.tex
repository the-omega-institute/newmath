Every $\RealSeparabilityUp$ density consumer that reuses interval data first reads the rational interval carrier of \autoref{ch:concrete-instances-rationalinterval-namecert}, then the dyadic-density comparison route of \autoref{ch:concrete-instances-dyadic-density-real-embedding-comparison-namecert}, and finally the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}. Density consumers needing explicit lower and upper rational brackets also pass through the locator route of \autoref{ch:concrete-instances-rational-interval-locator-namecert} before the $\RealUp$-facing readback is exported. Thus the separability route exports a finite rational-interval, locator, and dyadic-density ledger rather than a detached dense subset, selected nearest point, quotient real equality, countable-choice schedule, or ambient completeness coordinate.
