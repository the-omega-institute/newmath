\begin{theorem}[RealUp finite-window terminal seal nonescape]
\label{thm:realup-finite-window-terminal-seal-nonescape}
Every terminal $\RealUp$ seal read obtained from the RegSeqRat--StreamName handoff remains downstream of finite $\DyadicRatCoreUp$ radius rows, $\StreamNameUp$ observation windows, $\RegSeqRatUp$ regularity and readback rows, componentwise transport, displayed replay, provenance, and local $\NameCert$ data. Such a seal read cannot export quotient streams, host real equality, selected limits, countable choice, or ambient completeness.
\end{theorem}
\begin{proof}
Start with the handoff theorem \autoref{thm:realup-regseqrat-streamname-seal-handoff}. It reads the terminal seal only after the stream-name classifier window, transported unary classifier row, certified endpoint carriers, finite-window ledger, and seal provenance have been displayed.

Project the finite windows named by the $\RealUp$ index paragraph. The index routes the terminal seal through $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, and the regular completion frontier before any public seal is exposed, so the seal is a read of the same finite L10 route rather than an independent host-real endpoint.

Finally apply the existing terminal seal sibling route of \autoref{thm:real-terminal-seal-sibling-route}. That route keeps the DyadicRatCore tolerance ledger, StreamName window, RegSeqRat readback, local Real seal, $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows in one displayed packet. Since none of these rows contains quotient streams, host real equality, selected limits, countable choice, or ambient completeness, the terminal seal read cannot export them.
\end{proof}
