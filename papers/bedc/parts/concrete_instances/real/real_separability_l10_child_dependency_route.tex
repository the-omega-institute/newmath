The child dependency route for $\RealSeparabilityUp$ is the finite L10 path through \autoref{ch:concrete-instances-regseqrat-namecert} and \autoref{ch:concrete-instances-real-namecert}: a density request first reads the existing separability schedule rows of \autoref{thm:real-separability-regseqrat-dependency}, then consumes the dense dyadic and rational rows of \autoref{thm:real-separability-dyadic-rational-density-handoff}, and only after those rows reaches the terminal $\RealUp$ seal. Thus the hub surface records a substantive horizontal dependency from separability to regular-rational readback and Real sealing, with no route through a host dense subset, selected nearest point, quotient stream, or ambient completeness claim.
