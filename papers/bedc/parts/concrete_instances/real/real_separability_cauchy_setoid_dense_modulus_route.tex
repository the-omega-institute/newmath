The RealSeparability dense-modulus route records the classifier boundary that a regular Cauchy dense-read consumer must cross before a displayed dense modulus is exported. It reads the Cauchy-equivalence classifier of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} together with the existing RealSeparability Cauchy-setoid and dense-read routes, including \autoref{thm:real-separability-cauchy-setoid-route} and \autoref{thm:real-separability-cauchy-equivalence-dense-read-route}.

\begin{theorem}[RealSeparability Cauchy-setoid dense-modulus route]
\label{thm:real-separability-cauchy-setoid-dense-modulus-route}
Every $\RealSeparabilityUp$ dense-read consumer that uses a regular Cauchy name must first consume the $\CauchyEquivalenceSetoidUp$ classifier, then the $\RegSeqRatUp$ readback, $\DyadicRatCoreUp$ tolerance row, $\StreamNameUp$ window, and $\RealUp$ seal before exporting the dense modulus. The exported modulus is therefore a finite classifier-mediated RealSeparability route, not quotient real equality, host stream equality, selected dense enumeration, countable choice, or ambient completion.
\end{theorem}
\begin{proof}
Start with the regular Cauchy name boundary. The classifier route of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} compares regular presentations through finite displayed rows, so the dense-read consumer cannot begin from a quotient real object or a selected representative.

Next compose with the existing RealSeparability Cauchy-setoid route of \autoref{thm:real-separability-cauchy-setoid-route}. It places the $\CauchyEquivalenceSetoidUp$ classifier before any countable dense handoff and keeps the comparison inside the displayed regular-name boundary.

Finally use the dense-read route of \autoref{thm:real-separability-cauchy-equivalence-dense-read-route}. The consumer then reads $\RegSeqRatUp$ regular readback, $\DyadicRatCoreUp$ tolerance, $\StreamNameUp$ windows, and the terminal $\RealUp$ seal before the dense modulus is exported, leaving quotient equality, host equality, selected dense enumerations, countable choice, and ambient completion outside the packet.
\end{proof}
