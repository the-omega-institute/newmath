The $\RealUp$ carrier is consumed only after the finite-window $\StreamNameUp$ surface of \autoref{ch:concrete-instances-streamname-namecert} and the regular-rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert} are both available. In this dependency row, a real seal is a downstream consumer of the visible stream window and regular rational approximation ledgers; it does not create a quotient-real equality row, selected host limit, or ambient completeness principle before those two finite surfaces have been displayed.

\begin{theorem}[Real StreamName RegSeqRat pointwise seal composition]
\label{thm:real-streamname-regseqrat-pointwise-seal-composition}
Every accepted $\RealUp$ seal-facing read whose source includes a
$\StreamNameUp$ finite-window row and whose readback includes a
$\RegSeqRatUp$ regular-rational row factors through the displayed
$\StreamNameUp$ finite-window ledger, then the $\RegSeqRatUp$ pointwise
handoff, and then the local $\mathsf{RealUnaryStreamClassifier}$ row. The
route is transported only by the displayed $\BHist$, $\hsame$, $\Cont$,
$\Pkg$, and $\NameCert$ rows already present in the Real carrier and seal
material of this chapter.

The composed route exports no quotient-real equality, selected host limit,
ambient completeness principle, or direct $\RealUp$ seal edge bypassing the
StreamName and RegSeqRat surfaces.
\end{theorem}
\begin{proof}
Start with an accepted seal-facing read at the $\RealUp$ boundary. The Real
carrier and seal material already input in this chapter exposes the read only
through the local finite-window seal package, endpoint readback, and
provenance rows.

First project the StreamName source. By
\autoref{ch:concrete-instances-streamname-namecert} and
\autoref{thm:streamname-regseqrat-pointwise-handoff}, the finite source
contributes exactly the visible StreamName window ledger, bundle membership,
unary-history observations, and pointwise $\hsame$ comparisons. These are the
only source rows available before the regular-rational readback is consumed.

Next project the RegSeqRat readback. The RegSeqRat surface of
\autoref{ch:concrete-instances-regseqrat-namecert}, together with
\autoref{thm:realup-regseqrat-streamname-seal-handoff}, reads the same
finite window as the regular-rational handoff and keeps the readback on the
displayed $\BHist$ route.

Finally transport the result into the Real unary stream classifier. The local
Real seal package reads the already assembled StreamName and RegSeqRat rows
as the classifier-facing input, so the seal-facing route has no remaining
coordinate from which to obtain quotient-real equality, a selected host
limit, an ambient completeness principle, or a direct seal edge around those
two surfaces.
\end{proof}

\begin{theorem}[RealUp StreamName RegSeqRat dyadic window exhaustion]
\label{thm:realup-streamname-regseqrat-dyadic-window-exhaustion}
Every accepted $\RealUp$ seal-facing finite-window read whose source is a $\StreamNameUp$ window and whose spine is a $\RegSeqRatUp$ row is exhausted by the route
$$
\text{StreamName finite-window ledger}
\to
\text{RegSeqRat pointwise handoff}
\to
\text{DyadicRatCore observation row}
\to
\mathsf{RealUnaryStreamClassifier}.
$$
The route reads only the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows carried by the StreamName, RegSeqRat, DyadicRatCore, and Real finite-window surfaces. No quotient-real equality, selected host limit, ambient completeness row, or off-ledger dyadic sample can determine the $\RealUp$ seal.
\end{theorem}
\begin{proof}
Begin with the pointwise seal composition of \autoref{thm:real-streamname-regseqrat-pointwise-seal-composition}. It already forces the seal-facing read to pass through the displayed StreamName finite-window ledger and the RegSeqRat pointwise handoff before it reaches the local real classifier.

The handoff is exhaustive by \autoref{thm:streamname-regseqrat-pointwise-handoff-exhaustion}. Thus the source window contributes only its visible finite ledger, unary-history observations, bundle membership, and pointwise $\hsame$ comparisons; it cannot add a second stream source or a hidden real equality row.

The dyadic part is then read through the public DyadicRatCore observation and source-boundary surface. That surface supplies the observation row used by the real finite-window classifier, while the RealUp finite-window coverage rows identify the classifier-facing input with $\mathsf{RealUnaryStreamClassifier}$.

Composing these four displayed routes leaves no unused coordinate in the seal-facing packet. A host limit, ambient completeness row, quotient-real equality, or off-ledger dyadic sample is absent from the route and therefore cannot determine the accepted $\RealUp$ seal.
\end{proof}

\begin{theorem}[Real StreamName RegSeqRat terminal seal exhaustion]
\label{thm:real-streamname-regseqrat-terminal-seal-exhaustion}
Every terminal $\RealUp$ seal-facing consumer whose source is a
$\StreamNameUp$ finite-window row and whose regularity witness is a
$\RegSeqRatUp$ row factors through exactly the route
$$
\text{StreamName finite-window ledger}
\to
\text{RegSeqRat pointwise handoff}
\to
\text{DyadicRatCore observation row}
\to
\mathsf{RealUnaryStreamClassifier}
$$
before the local $\NameCert_{\RealUp}$ row can be read. The terminal packet
has no additional carrier coordinate for quotient-real equality, a selected
host limit, ambient completeness, an off-ledger dyadic sample, or a direct
$\RealUp$ seal edge bypassing either the StreamName source surface or the
RegSeqRat regularity surface.
\end{theorem}
\begin{proof}
Apply the pointwise seal composition of
\autoref{thm:real-streamname-regseqrat-pointwise-seal-composition}. It sends
any accepted seal-facing read with a StreamName source and RegSeqRat readback
through the StreamName finite-window ledger, the RegSeqRat pointwise handoff,
and the local real classifier-facing row.

Next apply the dyadic-window exhaustion of
\autoref{thm:realup-streamname-regseqrat-dyadic-window-exhaustion}. This
identifies the only remaining classifier input as the DyadicRatCore
observation row feeding $\mathsf{RealUnaryStreamClassifier}$, with no
off-ledger dyadic sample and no ambient real-completion coordinate.

It remains only to read the terminal seal packet itself. Its carrier
coordinates are the Real finite-window seal rows and the local
$\NameCert_{\RealUp}$ rows displayed in this chapter, so the preceding two
exhaustion routes consume every available source, regularity, dyadic, and
classifier coordinate before the local certificate is opened. Hence a
quotient-real equality, selected host limit, ambient completeness row, or
direct Real seal edge around StreamName or RegSeqRat has no coordinate from
which to determine the terminal consumer.
\end{proof}
