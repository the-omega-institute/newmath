The $\RealUp$ carrier is consumed only after the finite-window $\StreamNameUp$ surface of \autoref{ch:concrete-instances-streamname-namecert} and the regular-rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert} are both available. In this dependency row, a real seal is a downstream consumer of the visible stream window and regular rational approximation ledgers; it does not create a quotient-real equality row, selected host limit, or ambient completeness principle before those two finite surfaces have been displayed.

\begin{theorem}[Real StreamName RegSeqRat pointwise seal composition]
\label{thm:real-streamname-regseqrat-pointwise-seal-composition}
Every accepted $\RealUp$ seal-facing read whose source includes a
$\StreamNameUp$ finite-window row and whose readback includes a
$\RegSeqRatUp$ regular-rational row factors through the displayed
$\StreamNameUp$ finite-window ledger, then the $\RegSeqRatUp$ pointwise
handoff, and then the local $\mathsf{RealUnaryStreamClassifier}$ row. The
route is transported only by the displayed $\BHist$, $\hsame$, $\Cont$,
$\Pkg$, and $\NameCert$ rows already present in the Real carrier and seal
material of this chapter.

The composed route exports no quotient-real equality, selected host limit,
ambient completeness principle, or direct $\RealUp$ seal edge bypassing the
StreamName and RegSeqRat surfaces.
\end{theorem}
\begin{proof}
Start with an accepted seal-facing read at the $\RealUp$ boundary. The Real
carrier and seal material already input in this chapter exposes the read only
through the local finite-window seal package, endpoint readback, and
provenance rows.

First project the StreamName source. By
\autoref{ch:concrete-instances-streamname-namecert} and
\autoref{thm:streamname-regseqrat-pointwise-handoff}, the finite source
contributes exactly the visible StreamName window ledger, bundle membership,
unary-history observations, and pointwise $\hsame$ comparisons. These are the
only source rows available before the regular-rational readback is consumed.

Next project the RegSeqRat readback. The RegSeqRat surface of
\autoref{ch:concrete-instances-regseqrat-namecert}, together with
\autoref{thm:realup-regseqrat-streamname-seal-handoff}, reads the same
finite window as the regular-rational handoff and keeps the readback on the
displayed $\BHist$ route.

Finally transport the result into the Real unary stream classifier. The local
Real seal package reads the already assembled StreamName and RegSeqRat rows
as the classifier-facing input, so the seal-facing route has no remaining
coordinate from which to obtain quotient-real equality, a selected host
limit, an ambient completeness principle, or a direct seal edge around those
two surfaces.
\end{proof}

\begin{theorem}[RealUp StreamName RegSeqRat dyadic window exhaustion]
\label{thm:realup-streamname-regseqrat-dyadic-window-exhaustion}
Every accepted $\RealUp$ seal-facing finite-window read whose source is a $\StreamNameUp$ window and whose spine is a $\RegSeqRatUp$ row is exhausted by the route
$$
\text{StreamName finite-window ledger}
\to
\text{RegSeqRat pointwise handoff}
\to
\text{DyadicRatCore observation row}
\to
\mathsf{RealUnaryStreamClassifier}.
$$
The route reads only the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows carried by the StreamName, RegSeqRat, DyadicRatCore, and Real finite-window surfaces. No quotient-real equality, selected host limit, ambient completeness row, or off-ledger dyadic sample can determine the $\RealUp$ seal.
\end{theorem}
\begin{proof}
Begin with the pointwise seal composition of \autoref{thm:real-streamname-regseqrat-pointwise-seal-composition}. It already forces the seal-facing read to pass through the displayed StreamName finite-window ledger and the RegSeqRat pointwise handoff before it reaches the local real classifier.

The handoff is exhaustive by \autoref{thm:streamname-regseqrat-pointwise-handoff-exhaustion}. Thus the source window contributes only its visible finite ledger, unary-history observations, bundle membership, and pointwise $\hsame$ comparisons; it cannot add a second stream source or a hidden real equality row.

The dyadic part is then read through the public DyadicRatCore observation and source-boundary surface. That surface supplies the observation row used by the real finite-window classifier, while the RealUp finite-window coverage rows identify the classifier-facing input with $\mathsf{RealUnaryStreamClassifier}$.

Composing these four displayed routes leaves no unused coordinate in the seal-facing packet. A host limit, ambient completeness row, quotient-real equality, or off-ledger dyadic sample is absent from the route and therefore cannot determine the accepted $\RealUp$ seal.
\end{proof}

\begin{theorem}[Real StreamName RegSeqRat terminal seal exhaustion]
\label{thm:real-streamname-regseqrat-terminal-seal-exhaustion}
Every terminal $\RealUp$ seal-facing consumer whose source is a
$\StreamNameUp$ finite-window row and whose regularity witness is a
$\RegSeqRatUp$ row factors through exactly the route
$$
\text{StreamName finite-window ledger}
\to
\text{RegSeqRat pointwise handoff}
\to
\text{DyadicRatCore observation row}
\to
\mathsf{RealUnaryStreamClassifier}
$$
before the local $\NameCert_{\RealUp}$ row can be read. The terminal packet
has no additional carrier coordinate for quotient-real equality, a selected
host limit, ambient completeness, an off-ledger dyadic sample, or a direct
$\RealUp$ seal edge bypassing either the StreamName source surface or the
RegSeqRat regularity surface.
\end{theorem}
\begin{proof}
Apply the pointwise seal composition of
\autoref{thm:real-streamname-regseqrat-pointwise-seal-composition}. It sends
any accepted seal-facing read with a StreamName source and RegSeqRat readback
through the StreamName finite-window ledger, the RegSeqRat pointwise handoff,
and the local real classifier-facing row.

Next apply the dyadic-window exhaustion of
\autoref{thm:realup-streamname-regseqrat-dyadic-window-exhaustion}. This
identifies the only remaining classifier input as the DyadicRatCore
observation row feeding $\mathsf{RealUnaryStreamClassifier}$, with no
off-ledger dyadic sample and no ambient real-completion coordinate.

It remains only to read the terminal seal packet itself. Its carrier
coordinates are the Real finite-window seal rows and the local
$\NameCert_{\RealUp}$ rows displayed in this chapter, so the preceding two
exhaustion routes consume every available source, regularity, dyadic, and
classifier coordinate before the local certificate is opened. Hence a
quotient-real equality, selected host limit, ambient completeness row, or
direct Real seal edge around StreamName or RegSeqRat has no coordinate from
which to determine the terminal consumer.
\end{proof}

\begin{theorem}[Real StreamName RegSeqRat common-refinement consumer lock]
\label{thm:real-streamname-regseqrat-common-refinement-consumer-lock}
Every terminal $\RealUp$ seal-facing consumer whose source is a common-refinement $\StreamNameUp$ window and whose regularity witness is $\RegSeqRatUp$ factors through the common-refinement terminal packet, then the $\RegSeqRatUp$ readback, then the $\DyadicRatCoreUp$ observation row, and finally the local $\mathsf{RealUnaryStreamClassifier}$ row. The route exports no quotient-real equality, host sequence equality, selected host limit, ambient $\RealUp$ completeness, independent $\RealUp$ carrier row, or metric-completeness datum.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:real-streamname-regseqrat-pointwise-seal-composition}. It sends any accepted Real seal-facing read with a StreamName source and RegSeqRat readback through the finite StreamName window, the regular-rational handoff, and the local Real classifier-facing row.

Next apply \autoref{thm:realup-streamname-regseqrat-dyadic-window-exhaustion}. The dyadic observation row is the only classifier input left after the StreamName and RegSeqRat surfaces have been read.

Use \autoref{thm:real-streamname-regseqrat-terminal-seal-exhaustion} to lock this route at the terminal seal boundary. The local $\NameCert_{\RealUp}$ row can be read only after the source, regularity, dyadic, and classifier coordinates have been consumed.

Now apply \autoref{thm:streamname-common-refinement-terminal-real-seal}. The common-refinement terminal packet supplies the StreamName window accepted by the terminal Real seal read. Then \autoref{thm:streamname-real-regseq-terminal-common-window-lock} keeps the RegSeqRat readback on that same common window, and \autoref{thm:streamname-real-regseqrat-seal-common-refinement} identifies the resulting seal route with the common-refinement handoff.

The assembled route therefore reaches the Real classifier only through the common-refinement packet, the RegSeqRat readback, and the DyadicRatCore observation row. None of the cited packets contains a coordinate for quotient-real equality, host sequence equality, selected host limits, ambient Real completeness, independent Real carrier rows, or metric-completeness data, so no terminal consumer can read one from this handoff.
\end{proof}

\begin{theorem}[StreamName RegSeqRat RealUp finite-window factorization]
\label{thm:streamname-regseqrat-realup-finite-window-factorization}
Every accepted $\RealUp$ L10 finite-window seal read whose source is a
$\StreamNameUp$ window and whose regularity witness is a $\RegSeqRatUp$ row
factors through exactly the four displayed faces
$$
\text{StreamName finite-window ledger}
\to
\text{RegSeqRat pointwise handoff}
\to
\text{DyadicRatCore observation and tolerance row}
\to
\mathsf{RealUnaryStreamClassifier}\text{ with its local }\NameCert_{\RealUp}\text{ row}.
$$
The factorization is finite and uses only the displayed $\BHist$, $\hsame$,
$\Cont$, $\Pkg$, and $\NameCert$ coordinates on those faces. It exports no
quotient stream, host sequence equality, selected limit, ambient completeness,
metric-completion datum, off-ledger dyadic sample, or direct $\RealUp$ seal
edge outside the four-face route.
\end{theorem}
\begin{proof}
Start with the pointwise seal composition of
\autoref{thm:real-streamname-regseqrat-pointwise-seal-composition}. It puts
the accepted seal-facing read on the StreamName finite-window ledger, then on
the RegSeqRat pointwise handoff, and only then on the local Real
classifier-facing row.

Next apply the dyadic-window exhaustion theorem,
\autoref{thm:realup-streamname-regseqrat-dyadic-window-exhaustion}. This
identifies the remaining classifier input with the DyadicRatCore observation
and tolerance row and rules out any off-ledger dyadic sample before the Real
classifier is read.

Use the terminal seal exhaustion theorem,
\autoref{thm:real-streamname-regseqrat-terminal-seal-exhaustion}, to attach
the local $\NameCert_{\RealUp}$ row after the source, regularity, dyadic, and
classifier coordinates have been consumed. The terminal packet therefore has
no unused coordinate from which to read quotient stream data, host sequence
equality, selected limits, ambient completeness, or metric-completion data.

Finally compare this packet with the ordered L10 dependency lattice of
\autoref{def:real-l10-dependency-lattice}. The open-phase sibling route,
\autoref{thm:real-l10-open-phase-sibling-route}, reads the same
StreamName, RegSeqRat, DyadicRatCore, and local Real faces and no other
source surface. Repacking the three seal-exhaustion results along that route
gives the claimed finite-window factorization.
\end{proof}

\begin{theorem}[Real finite-window classifier endpoint determinacy]
\label{thm:real-finite-window-classifier-endpoint-determinacy}
For every accepted $\RealUp$ finite-window seal read whose source is a
$\StreamNameUp$ window and whose regularity witness is a $\RegSeqRatUp$ row,
the displayed classifier endpoint is unique up to $\hsame$ once the four faces
of \autoref{thm:streamname-regseqrat-realup-finite-window-factorization} are
fixed: the StreamName finite-window ledger, the RegSeqRat pointwise handoff,
the DyadicRatCore observation and tolerance row, and the local
$\mathsf{RealUnaryStreamClassifier}$ row with $\NameCert_{\RealUp}$.

Equivalently, any two endpoint reads using those same four displayed faces are
related by the same finite $\BHist$, $\Cont$, $\Pkg$, and $\NameCert$ rows, and
their endpoint witnesses are $\hsame$-equal at the classifier boundary. No
quotient-real equality, host sequence equality, selected host limit, ambient
completeness row, or metric-completion datum can separate the two endpoints.
\end{theorem}
\begin{proof}
Apply
\autoref{thm:streamname-regseqrat-realup-finite-window-factorization} to the
two endpoint reads. Both reads are sent to the same four displayed faces, so
their finite StreamName ledger rows, RegSeqRat handoff rows, and local
$\NameCert_{\RealUp}$ rows are compared inside the displayed $\BHist$,
$\Cont$, and $\Pkg$ coordinates rather than through an ambient Real carrier.

Next use \autoref{thm:realup-streamname-regseqrat-dyadic-window-exhaustion}.
It fixes the dyadic classifier input by the shared DyadicRatCore observation
and tolerance row, leaving no off-ledger dyadic sample that could distinguish
one classifier endpoint from the other.

Finally use
\autoref{thm:real-streamname-regseqrat-terminal-seal-exhaustion} to lock the
terminal seal boundary. Repacking the shared finite rows through the local
$\mathsf{RealUnaryStreamClassifier}$ gives the same displayed endpoint up to
$\hsame$, and the terminal packet contains no coordinate for quotient-real
equality, host sequence equality, selected limits, ambient completeness, or
metric-completion data.
\end{proof}
