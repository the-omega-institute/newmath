The $\RealSeparabilityUp$ route reads dense rational windows through \autoref{ch:concrete-instances-regseqrat-namecert} and dyadic tolerances through \autoref{ch:concrete-instances-dyadicratcore-namecert} before the terminal $\RealUp$ seal. This places the L10 dependency on displayed regular-rational schedules and dyadic modulus rows, rather than on an external countable dense subset or a choice-selected enumeration.
