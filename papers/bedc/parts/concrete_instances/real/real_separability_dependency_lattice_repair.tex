The $\RealSeparabilityUp$ dependency lattice consumes the $\RegSeqRatUp$ readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic-density embedding route of \autoref{ch:concrete-instances-dyadic-density-real-namecert}, and the located $\RegSeqRatUp$ route of \autoref{ch:concrete-instances-located-regseqrat-namecert} before exposing its separability ledger. Thus the separability packet reads rational candidates through regular sequence windows, dyadic density through the real-density sibling, and located regular readback through the located route already carried by the hub before any $\RealUp$ density observation is named.
