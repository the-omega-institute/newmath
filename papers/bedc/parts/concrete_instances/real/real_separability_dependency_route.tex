\begin{theorem}[RealSeparability dependency route]
\label{thm:real-separability-dependency-route}
Every $\RealSeparabilityUp$ density consumer reaches the local $\NameCert$ row only after passing through the rational source of \autoref{ch:concrete-instances-rat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. Thus the separability packet exposes density as a finite rational-window route over displayed BEDC rows, not as a host dense subset, quotient real equality, selected nearest point, or ambient completeness principle.
\end{theorem}
\begin{proof}
Project the $\RealSeparabilityUp$ carrier through its local construction file. The density consumer first reads the rational enumeration row, so the route begins with the $\RatUp$ source surface rather than an external dense set.

The dyadic tolerance and finite-window stages are then forced by the displayed tolerance ledger. The $\DyadicRatCoreUp$ row fixes the requested rational scale, and the $\StreamNameUp$ row supplies exactly the inspected finite windows.

The readback and seal stages are $\RegSeqRatUp$ and $\RealUp$. Regular rational readback consumes the same finite windows, and the terminal real-name seal is reached only after that readback is displayed.

The local $\NameCert$ row packages this route with its $\hsame$, $\Cont$, and $\Pkg$ provenance. Since the route has no coordinate for a host dense subset, quotient real equality, selected nearest point, or completeness theorem, every density consumer factors through the listed sibling chapters before the local certificate row.
\end{proof}
