The $\RealSeparabilityUp$ density packet exposes its regular-Cauchy-facing route only after the $\RegSeqRatUp$ readback surface of \autoref{ch:concrete-instances-regseqrat-namecert} and the $\DyadicRatCoreUp$ tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert} have been displayed. Those rows feed the existing real-density and finite-window surfaces before any downstream regular Cauchy limit uniqueness consumer may inspect a terminal $\RealUp$ seal; the handoff exports no selected dense point, quotient real equality, countable-choice row, or ambient completeness coordinate.
