The $\RealSeparabilityUp$ dense-read route is not a free-standing lattice float. Every countable-density packet reads $\RegSeqRatUp$ regular Cauchy names from \autoref{ch:concrete-instances-regseqrat-namecert}, $\StreamNameUp$ pointwise observation windows from \autoref{ch:concrete-instances-streamname-namecert}, and $\DyadicRatCoreUp$ dyadic approximation rows from \autoref{ch:concrete-instances-dyadicratcore-namecert} before exporting its dense enumeration evidence.

Proposition-style dependency spine: if a $\RealSeparabilityUp$ consumer claims countable-density evidence while omitting the regular-rational source, the stream-window observation row, or the dyadic approximation ledger, then the read is outside the displayed finite BEDC route. The admissible route is regular Cauchy naming, finite stream observation, dyadic approximation, transport, replay, provenance, and only then the countable-density handoff.
