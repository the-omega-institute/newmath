\begin{theorem}[Real stream classifier selected e-one pair readback]
\label{thm:real-stream-classifier-selected-e1-pair-readback}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$, every selected
observation index $n$, and histories $a,b:\Hist$, if
$$
\mathsf{RealStreamClassifier}(x,y),\quad
\hsame(x(n),\Eone(a)),\quad
\hsame(y(n),\Eone(b)),
$$
then
$$
\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land \hsame(a,b).
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-stream-classifier-prefix} to the stream classifier at
the selected index $n$. This produces the finite-prefix classifier witness
needed at that index.

Apply \autoref{thm:real-stream-prefix-e1-pair-readback} to that prefix witness
and to the two displayed e-one endpoint equalities. The readback theorem returns
the two unary tail witnesses and the internal sameness relation
$\hsame(a,b)$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_selected\_e1\_pair\_readback}

\begin{theorem}[Real stream classifier transported selected e-one rational classifier readback]
\label{thm:real-stream-classifier-transported-selected-e1-rat-classifier-readback}
For rational-observation streams $x,x',y,y':\mathbb{N}\to\Hist$, every selected
observation index $n$, and histories $a,b:\Hist$, if
$$
\forall i,\ \hsame(x(i),x'(i)),\quad
\forall i,\ \hsame(y(i),y'(i)),\quad
\mathsf{RealStreamClassifier}(x,y),\quad
\hsame(x'(n),\Eone(a)),\quad
\hsame(y'(n),\Eone(b)),
$$
then
$$
\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b)).
$$
\end{theorem}
\begin{proof}
Use the forward direction of
\autoref{thm:real-stream-finite-prefix-exactness} on
$\mathsf{RealStreamClassifier}(x,y)$ and select the finite prefix at $n$.
Transport this prefix witness along the two pointwise $\hsame$ hypotheses by
\autoref{thm:real-stream-prefix-classifier-hsame-transport}, obtaining a
classified prefix for $x'$ and $y'$ at the same selected index.

Project the selected endpoint classifier with
\autoref{thm:real-stream-prefix-classifier-endpoint}. It gives
$\mathsf{RatHistoryClassifier}(x'(n),y'(n))$.

Finally apply \autoref{thm:rat-history-classifier-hsame-transport} to move this
endpoint classifier along $\hsame(x'(n),\Eone(a))$ and
$\hsame(y'(n),\Eone(b))$, producing
$\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_transported\_selected\_e1\_rat\_classifier\_readback}

Fit: traditional target selected rational endpoint readback for RealUp streams;
BEDC source RatUp plus RealUp; carrier rational-observation streams; classifier
RealStreamClassifier and RatHistoryClassifier; stability prefix exactness,
hsame transport, and endpoint projection; ledger selected observation index and
displayed e-one seals; core theorem
\autoref{thm:real-stream-classifier-transported-selected-e1-rat-classifier-readback};
exactness the displayed implication; standard bridge rational equality read from
equal real observations; status $\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier transported selected e-one pair readback]
\label{thm:real-stream-classifier-transported-selected-e1-pair-readback}
For rational-observation streams $x,x',y,y':\mathbb{N}\to\Hist$, every selected
observation index $n$, and histories $a,b:\Hist$, if
$$
\forall i,\ \hsame(x(i),x'(i)),\quad
\forall i,\ \hsame(y(i),y'(i)),\quad
\mathsf{RealStreamClassifier}(x,y),\quad
\hsame(x'(n),\Eone(a)),\quad
\hsame(y'(n),\Eone(b)),
$$
then
$$
\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land \hsame(a,b).
$$
\end{theorem}
\begin{proof}
Apply
\autoref{thm:real-stream-classifier-transported-selected-e1-rat-classifier-readback}
to obtain the endpoint classifier
$\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$.

Apply \autoref{thm:rat-history-classifier-e1-tail-unary-iff} in the forward
direction. The one-tail exactness theorem returns
$\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land \hsame(a,b)$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_transported\_selected\_e1\_pair\_readback}

\begin{theorem}[Real stream classifier transported selected e-one full readback package]
\label{thm:real-stream-classifier-transported-selected-e1-full-readback-package}
For rational-observation streams $x,x',y,y':\mathbb{N}\to\Hist$, every selected
observation index $n$, and histories $a,b:\Hist$, if
$$
\forall i,\ \hsame(x(i),x'(i)),\quad
\forall i,\ \hsame(y(i),y'(i)),\quad
\mathsf{RealStreamClassifier}(x,y),\quad
\hsame(x'(n),\Eone(a)),\quad
\hsame(y'(n),\Eone(b)),
$$
then
$$
\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))\land
\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land \hsame(a,b).
$$
\end{theorem}
\begin{proof}
Apply
\autoref{thm:real-stream-classifier-transported-selected-e1-rat-classifier-readback}
under the displayed transport, classifier, and e-one seal hypotheses. This gives
the first conjunct
$\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$.

Apply
\autoref{thm:real-stream-classifier-transported-selected-e1-pair-readback}
under the same hypotheses. This gives
$\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land \hsame(a,b)$.

Package the endpoint classifier together with the unary-tail readback triple.
The resulting conjunction is exactly the selected transported e-one readback
package displayed in the statement.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_transported\_selected\_e1\_full\_readback\_package}

Fit: traditional target selected sealed rational endpoint package for RealUp
streams; BEDC source RatUp plus RealUp finite-prefix exactness; carrier
rational-observation streams; classifier RealStreamClassifier and
RatHistoryClassifier; stability prefix hsame transport and RatUp one-tail
exactness; ledger selected index and two e-one seals; core theorem this block;
exactness the displayed conjunction; standard bridge rational equality read from
equal transported real observations; status $\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier transported selected positive denominator package]
\label{thm:real-stream-classifier-transported-selected-positive-denominator-package}
For rational-observation streams $x,x',y,y':\mathbb{N}\to\Hist$ and every
selected observation index $n$, if
$$
\forall i,\ \hsame(x(i),x'(i)),\quad
\forall i,\ \hsame(y(i),y'(i)),\quad
\mathsf{RealStreamClassifier}(x,y),
$$
then
$$
\mathsf{RatHistoryClassifier}(x'(n),y'(n))\land
\mathsf{PositiveUnaryDenominator}(x'(n))\land
\mathsf{PositiveUnaryDenominator}(y'(n)).
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-stream-classifier-transport-with-endpoint-readback}
to the two pointwise $\hsame$ hypotheses and the source stream classifier. The
endpoint field of that package gives
$\mathsf{RatHistoryClassifier}(x'(n),y'(n))$ at the selected index $n$.

Apply \autoref{thm:rat-history-classifier-positive-denominators} to this
selected endpoint classifier. It exposes
$\mathsf{PositiveUnaryDenominator}(x'(n))$ and
$\mathsf{PositiveUnaryDenominator}(y'(n))$.

Package the endpoint classifier with the two positive-denominator witnesses.
The result is exactly the displayed transported selected denominator readback
conjunction.
\end{proof}

\begin{theorem}[Real stream classifier transported selected positive unary nonempty package]
\label{thm:real-stream-classifier-transport-selected-positive-unary-nonempty-package}
For rational-observation streams $x,x',y,y':\mathbb{N}\to\Hist$ and a selected
observation index $n$, assume
$$
\forall i,\ \hsame(x(i),x'(i)),\quad
\forall i,\ \hsame(y(i),y'(i)),\quad
\mathsf{RealStreamClassifier}(x,y).
$$
Then the transported selected endpoints carry the complete rational endpoint
package
$$
\begin{aligned}
&\mathsf{RatHistoryClassifier}(x'(n),y'(n))\land
\mathsf{PositiveUnaryDenominator}(x'(n))\land
\mathsf{PositiveUnaryDenominator}(y'(n))\\
&\land\ \mathsf{UnaryHistory}(x'(n))\land
\mathsf{UnaryHistory}(y'(n))\land
(\hsame(x'(n),\emp)\to\bot)\land
(\hsame(y'(n),\emp)\to\bot).
\end{aligned}
$$
\end{theorem}
\begin{proof}
Transport the selected rational-history classifier from
$\mathsf{RatHistoryClassifier}(x(n),y(n))$ to
$\mathsf{RatHistoryClassifier}(x'(n),y'(n))$ along the two pointwise
$\hsame$ hypotheses.

Rational classifier positivity exposes positive unary denominator witnesses at
both transported endpoints. Each positive unary denominator witness gives a
unary-history row and separates that endpoint from $\emp$. Packaging these rows
with the transported classifier gives the displayed conjunction.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_transport\_selected\_positive\_unary\_nonempty\_package}

Fit: traditional target selected positive denominator readback for transported
RealUp rational observations; BEDC source RealUp plus RatUp; carrier
rational-observation streams; classifier RealStreamClassifier and
RatHistoryClassifier; stability prefix hsame transport plus RatUp denominator
exposure; ledger selected observation index and transported endpoint displays;
core theorem this block; exactness the displayed conjunction; standard bridge
positivity of rational denominators inside equal transported real observations;
status $\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier selected positive unary nonempty package]
\label{thm:real-stream-classifier-selected-positive-unary-nonempty-package}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$ and a selected
index $n$, if
$$
\mathsf{RealStreamClassifier}(x,y),
$$
then the selected endpoints carry positive unary denominator witnesses, unary
history witnesses, and nonempty boundary exclusions:
$$
\begin{array}{l}
\mathsf{PositiveUnaryDenominator}(x(n))\land
\mathsf{PositiveUnaryDenominator}(y(n))\land
\mathsf{UnaryHistory}(x(n))\land
\mathsf{UnaryHistory}(y(n))\land\\
(\hsame(x(n),\emp)\to\mathsf{False})\land
(\hsame(y(n),\emp)\to\mathsf{False}).
\end{array}
$$
\end{theorem}
\begin{proof}
Select the point classifier
$\mathsf{RatHistoryClassifier}(x(n),y(n))$ from the stream classifier. RatUp
positive-denominator projection gives the two positive denominator witnesses.
Apply positive unary denominator readback to each endpoint, yielding the unary
history witness and the empty-boundary contradiction for that endpoint.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_selected\_positive\_unary\_nonempty\_package}

\begin{theorem}[Real stream classifier selected endpoint e-one shapes]
\label{thm:real-stream-classifier-selected-endpoint-e1-shapes}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$ and a selected
index $n$, if $\mathsf{RealStreamClassifier}(x,y)$ holds, then both selected
endpoints are visibly one-headed with unary tails:
$$
\bigl(\exists x_t,\ x(n)=\Eone(x_t)\land\mathsf{UnaryHistory}(x_t)\bigr)
\land
\bigl(\exists y_t,\ y(n)=\Eone(y_t)\land\mathsf{UnaryHistory}(y_t)\bigr).
$$
\end{theorem}
\begin{proof}
Apply selected positive unary nonempty readback at the chosen index. The
package supplies unary-history evidence and empty-boundary exclusion for both
selected endpoints.

For the $x$ endpoint, unary nonempty one-tail readback chooses a tail $x_t$
with $\mathsf{UnaryHistory}(x_t)$ and gives the constructor equality
$x(n)=\Eone(x_t)$ directly. Pair this equality with the unary-tail witness.

The $y$ endpoint is treated in the same way, producing a tail $y_t$, its unary
history witness, and the constructor equality $y(n)=\Eone(y_t)$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_selected\_endpoint\_e1\_shapes}

\begin{theorem}[Real stream classifier selected endpoint common e-one tail readback]
\label{thm:real-stream-classifier-selected-endpoint-common-e1-tail-readback}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$ and a selected
index $n$, if $\mathsf{RealStreamClassifier}(x,y)$ holds, then
$$
\exists a,b,\ \hsame(x(n),\Eone(a))\land\hsame(y(n),\Eone(b))\land
\mathsf{UnaryHistory}(a)\land\mathsf{UnaryHistory}(b)\land\hsame(a,b).
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-stream-classifier-selected-endpoint-e1-shapes} at the
selected index. This gives a tail $a$ for the $x$ endpoint and a tail $b$ for the
$y$ endpoint, together with the two displayed e-one rows
$\hsame(x(n),\Eone(a))$ and $\hsame(y(n),\Eone(b))$ and the two unary-tail
witnesses.

Apply \autoref{thm:real-stream-classifier-selected-e1-pair-readback} to the same
stream classifier and to those two displayed rows. The readback theorem returns
the shared-tail row $\hsame(a,b)$ while preserving the unary-history evidence for
both tails.

Package the two tails as the existential witnesses and combine the two displayed
endpoint rows, the two unary-tail witnesses, and the common-tail row.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_selected\_endpoint\_common\_e1\_tail\_readback}

Fit: traditional target selected rational observation readback for RealUp streams;
BEDC source RatUp plus RealUp; carrier rational-observation streams; classifier
RealStreamClassifier with selected RatHistoryClassifier readback; stability
selected endpoint e-one coverage and e-one pair readback; ledger selected
observation index and the two endpoint histories; core theorem
\autoref{thm:real-stream-classifier-selected-endpoint-common-e1-tail-readback};
exactness the existential package of one-headed endpoints, unary tails, and
tail sameness; standard bridge rational equality read from the common unary tail;
status $\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier transported selected full shape readback package]
\label{thm:real-stream-classifier-transported-selected-full-shape-readback-package}
For rational-observation streams $x,x',y,y':\mathbb{N}\to\Hist$, every selected
observation index $n$, and histories $a,b,z_X,z_Y:\Hist$, if
$$
\forall i,\ \hsame(x(i),x'(i)),\quad
\forall i,\ \hsame(y(i),y'(i)),\quad
\mathsf{RealStreamClassifier}(x,y),\quad
\hsame(x'(n),\Eone(a)),\quad
\hsame(y'(n),\Eone(b)),
$$
then
$$
\begin{array}{l}
\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))\land
\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land \hsame(a,b)\land\\
\mathsf{PositiveUnaryDenominator}(x'(n))\land
\mathsf{PositiveUnaryDenominator}(y'(n))\land
\mathsf{UnaryHistory}(x'(n))\land
\mathsf{UnaryHistory}(y'(n))\land\\
(\hsame(x'(n),\emp)\to\mathsf{False})\land
(\hsame(y'(n),\emp)\to\mathsf{False})\land\\
(\hsame(x'(n),\Ezero(z_X))\to\mathsf{False})\land
(\hsame(y'(n),\Ezero(z_Y))\to\mathsf{False}).
\end{array}
$$
\end{theorem}
\begin{proof}
Apply
\autoref{thm:real-stream-classifier-transported-selected-e1-full-readback-package}
under the two pointwise transport rows, the source RealUp stream classifier, and
the two selected e-one seals. This gives the endpoint classifier
$\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$ together with the unary tail
readback
$\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land \hsame(a,b)$.

Apply
\autoref{thm:real-stream-classifier-transport-selected-positive-unary-nonempty-package}
under the same two pointwise transport rows and the same source RealUp stream
classifier. This gives the selected transported endpoint classifier, the
positive-denominator witnesses for $x'(n)$ and $y'(n)$, the unary-history rows,
and the two nonempty exclusions.

It remains to exclude displayed zero-extensions. Suppose
$\hsame(x'(n),\Ezero(z_X))$ holds. Transport
$\mathsf{PositiveUnaryDenominator}(x'(n))$ along this comparison by
\autoref{thm:rat-positive-unary-denominator-hsame-transport}, and contradict
\autoref{thm:rat-positive-denominator-e0-absurd}.

The $y'$ endpoint is identical: transport
$\mathsf{PositiveUnaryDenominator}(y'(n))$ along
$\hsame(y'(n),\Ezero(z_Y))$ and apply the same zero-extension exclusion.

Package the e-one classifier row, the unary-tail readback, the positive endpoint
rows, the unary and nonempty endpoint rows, and the two zero-extension
contradictions. The result is exactly the displayed selected full shape readback
package.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_transported\_selected\_full\_shape\_readback\_package}

Fit: traditional target selected transported endpoint-shape readback package for
RealUp; BEDC source RatUp plus StreamNameUp-shaped RealUp streams; carrier
rational-observation streams; classifiers RealStreamClassifier and
RatHistoryClassifier; stability transported prefix readback,
positive-denominator exposure, positive denominator hsame transport, and
zero-extension exclusion; ledger selected observation index, two transport rows,
two e-one seals, and two zero-extension tests; core theorem
\autoref{thm:real-stream-classifier-transported-selected-full-shape-readback-package};
exactness the displayed classifier, tail, positive-denominator, unary, nonempty,
and zero-extension package; downstream use by the RealUp dyadic embedding
certificate; status $\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier transported contextual full endpoint package]
\label{thm:real-stream-classifier-transported-contextual-full-endpoint-package}
For rational-observation streams
$x,x',y,y',p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$, a selected
index $n$, and histories $a,b,z_X,z_Y:\Hist$, assume
$$
\forall i,\ \hsame(x(i),x'(i)),\quad
\forall i,\ \hsame(y(i),y'(i)),\quad
\mathsf{RealStreamClassifier}(x,y).
$$
Assume also the selected transported context rows
$$
\mathsf{UnaryHistory}(p_X(n)),\quad
\mathsf{UnaryHistory}(t_X(n)),\quad
\hsame(p_X(n),p_Y(n)),\quad
\hsame(t_X(n),t_Y(n)),
$$
the four selected continuation rows
$$
\mathsf{Cont}(p_X(n),x'(n),m_X(n)),\quad
\mathsf{Cont}(m_X(n),t_X(n),o_X(n)),
\quad
\mathsf{Cont}(p_Y(n),y'(n),m_Y(n)),\quad
\mathsf{Cont}(m_Y(n),t_Y(n),o_Y(n)),
$$
and the two one-headed endpoint seals
$$
\hsame(o_X(n),\Eone(a))\quad\text{and}\quad \hsame(o_Y(n),\Eone(b)).
$$
Then the transported selected contextual endpoints carry all of the following
data:
\begin{enumerate}
\item $\mathsf{RatHistoryClassifier}(o_X(n),o_Y(n))$.
\item $\mathsf{PositiveUnaryDenominator}(o_X(n))$ and
$\mathsf{PositiveUnaryDenominator}(o_Y(n))$.
\item $\mathsf{UnaryHistory}(o_X(n))$ and $\mathsf{UnaryHistory}(o_Y(n))$.
\item $\hsame(o_X(n),\emp)\to\mathsf{False}$ and
$\hsame(o_Y(n),\emp)\to\mathsf{False}$.
\item $\hsame(o_X(n),\Ezero(z_X))\to\mathsf{False}$ and
$\hsame(o_Y(n),\Ezero(z_Y))\to\mathsf{False}$.
\item $\mathsf{UnaryHistory}(a)\land\mathsf{UnaryHistory}(b)\land\hsame(a,b)$.
\end{enumerate}
\end{theorem}
\begin{proof}
First take the forward implication from
\autoref{thm:real-stream-classifier-hsame-transport} and apply it to the two
pointwise $\hsame$ rows and the source classifier
$\mathsf{RealStreamClassifier}(x,y)$. This gives the transported classifier
$\mathsf{RealStreamClassifier}(x',y')$.

With that classifier in hand, apply
\autoref{thm:real-stream-classifier-selected-contextual-full-endpoint-package}
at the selected index $n$ to the displayed unary context rows, the two
context-identification rows, the four continuation rows, and the two one-headed
endpoint seals. The theorem returns exactly the six displayed endpoint rows for
$o_X(n)$ and $o_Y(n)$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_transported\_contextual\_full\_endpoint\_package}

Fit: traditional target transported contextual endpoint readback for RealUp;
BEDC source RealUp stream hsame transport plus RatUp contextual endpoint
readback; carrier rational-observation streams; classifiers RealStreamClassifier
and selected RatHistoryClassifier; stability pointwise hsame transport, Cont
context closure, and endpoint readback; ledger selected index, pointwise
transport rows, contextual continuation outputs, one-headed and zero-headed
tests; core theorem
\autoref{thm:real-stream-classifier-transported-contextual-full-endpoint-package};
exactness the six-row endpoint package; standard bridge transported RegSeqRat
selected contextual observation readback; status $\mathsf{ProofTarget}$.

\begin{theorem}[Real stream classifier transported contextual displayed e-one classifier package]
\label{thm:real-stream-classifier-transported-contextual-displayed-e1-classifier-package}
For rational-observation streams
$x,x',y,y',p_X,p_Y,t_X,t_Y,m_X,m_Y,o_X,o_Y:\mathbb{N}\to\Hist$, a selected
index $n$, and histories $a,b,z_X,z_Y:\Hist$, assume
$$
\forall i,\ \hsame(x(i),x'(i)),\quad
\forall i,\ \hsame(y(i),y'(i)),\quad
\mathsf{RealStreamClassifier}(x,y).
$$
Assume also the selected transported context rows
$$
\mathsf{UnaryHistory}(p_X(n)),\quad
\mathsf{UnaryHistory}(t_X(n)),\quad
\hsame(p_X(n),p_Y(n)),\quad
\hsame(t_X(n),t_Y(n)),
$$
the four selected continuation rows
$$
\mathsf{Cont}(p_X(n),x'(n),m_X(n)),\quad
\mathsf{Cont}(m_X(n),t_X(n),o_X(n)),
\quad
\mathsf{Cont}(p_Y(n),y'(n),m_Y(n)),\quad
\mathsf{Cont}(m_Y(n),t_Y(n),o_Y(n)),
$$
and the two one-headed endpoint seals
$$
\hsame(o_X(n),\Eone(a))\quad\text{and}\quad \hsame(o_Y(n),\Eone(b)).
$$
Then the transported selected contextual endpoints and their displayed e-one
readbacks carry all of the following data:
\begin{enumerate}
\item $\mathsf{RatHistoryClassifier}(o_X(n),o_Y(n))$.
\item $\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$.
\item $\mathsf{UnaryHistory}(a)$ and $\mathsf{UnaryHistory}(b)$.
\item $\hsame(a,b)$.
\item $\mathsf{PositiveUnaryDenominator}(o_X(n))$ and
$\mathsf{PositiveUnaryDenominator}(o_Y(n))$.
\item $\mathsf{UnaryHistory}(o_X(n))$ and $\mathsf{UnaryHistory}(o_Y(n))$.
\item $\hsame(o_X(n),\emp)\to\mathsf{False}$ and
$\hsame(o_Y(n),\emp)\to\mathsf{False}$.
\item $\hsame(o_X(n),\Ezero(z_X))\to\mathsf{False}$ and
$\hsame(o_Y(n),\Ezero(z_Y))\to\mathsf{False}$.
\end{enumerate}
\end{theorem}
\begin{proof}
Apply
\autoref{thm:real-stream-classifier-transported-contextual-full-endpoint-package}
under the displayed pointwise transport rows, selected context rows,
continuation rows, and one-headed endpoint seals. This returns the classifier
for $o_X(n)$ and $o_Y(n)$, the positive-denominator rows, the endpoint unary
rows, the two nonempty exclusions, the two zero-headed exclusions, and the tail
readback
$\mathsf{UnaryHistory}(a)\land\mathsf{UnaryHistory}(b)\land\hsame(a,b)$.

Now transport the first classifier row across the two displayed e-one seals by
\autoref{thm:rat-history-classifier-hsame-transport}. The source classifier is
$\mathsf{RatHistoryClassifier}(o_X(n),o_Y(n))$, and the endpoint comparisons are
$\hsame(o_X(n),\Eone(a))$ and $\hsame(o_Y(n),\Eone(b))$; the transported row is
$\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$.

Finally project the three tail readback components from the returned
conjunction and repack them with the transported displayed e-one classifier and
the remaining endpoint rows already supplied by
\autoref{thm:real-stream-classifier-transported-contextual-full-endpoint-package}.
\end{proof}

\begin{theorem}[Real stream prefix transported selected full shape readback package]
\label{thm:real-stream-prefix-transported-selected-full-shape-readback-package}
For rational-observation streams $x,x',y,y':\mathbb{N}\to\Hist$, indices
$m,n$, and histories $a,b,z_X,z_Y:\Hist$, if
$$
\mathsf{RealStreamPrefixClassifier}(x,y,m+n),
$$
$$
\forall i,\ \hsame(x(i),x'(i)),\quad
\forall i,\ \hsame(y(i),y'(i)),\quad
\hsame(x'(n),\Eone(a)),\quad
\hsame(y'(n),\Eone(b)),
$$
then the transported selected finite-prefix endpoints carry the full readback
package
$$
\begin{array}{l}
\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))\land
\mathsf{UnaryHistory}(a)\land \mathsf{UnaryHistory}(b)\land \hsame(a,b)\land\\
\mathsf{PositiveUnaryDenominator}(x'(n))\land
\mathsf{PositiveUnaryDenominator}(y'(n))\land
\mathsf{UnaryHistory}(x'(n))\land
\mathsf{UnaryHistory}(y'(n))\land\\
(\hsame(x'(n),\emp)\to\mathsf{False})\land
(\hsame(y'(n),\emp)\to\mathsf{False})\land\\
(\hsame(x'(n),\Ezero(z_X))\to\mathsf{False})\land
(\hsame(y'(n),\Ezero(z_Y))\to\mathsf{False}).
\end{array}
$$
\end{theorem}
\begin{proof}
First truncate the displayed prefix classifier from length $m+n$ to the selected
prefix through $n$ by
\autoref{thm:real-stream-prefix-classifier-truncation}. Transport this shorter
prefix along the two pointwise $\hsame$ rows by
\autoref{thm:real-stream-prefix-classifier-hsame-transport}, obtaining a
classified prefix for $x'$ and $y'$ at index $n$.

Project the selected endpoint by
\autoref{thm:real-stream-prefix-classifier-endpoint}. This gives
$\mathsf{RatHistoryClassifier}(x'(n),y'(n))$. Transport that endpoint classifier
across the displayed e-one seals with
\autoref{thm:rat-history-classifier-hsame-transport}, giving
$\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$.

Apply \autoref{thm:rat-history-classifier-positive-denominators} to the
transported endpoint classifier before the e-one seals. The two projected rows
are $\mathsf{PositiveUnaryDenominator}(x'(n))$ and
$\mathsf{PositiveUnaryDenominator}(y'(n))$.

Apply \autoref{thm:positive-unary-denominator-unary-and-nonempty} separately to
the two positive-denominator rows. This gives
$\mathsf{UnaryHistory}(x'(n))$, $\mathsf{UnaryHistory}(y'(n))$, and the two
empty-endpoint exclusions.

For the e-one tails, use
\autoref{thm:rat-history-classifier-e1-tail-unary-iff} in the forward direction
on $\mathsf{RatHistoryClassifier}(\Eone(a),\Eone(b))$. It supplies
$\mathsf{UnaryHistory}(a)$, $\mathsf{UnaryHistory}(b)$, and $\hsame(a,b)$.

It remains to exclude displayed zero-headed endpoints. If
$\hsame(x'(n),\Ezero(z_X))$ holds, transport
$\mathsf{PositiveUnaryDenominator}(x'(n))$ along this comparison with
\autoref{thm:rat-positive-unary-denominator-hsame-transport}, then contradict
\autoref{thm:rat-positive-denominator-e0-absurd}. The same argument for
$y'(n)$ and $z_Y$ gives the right zero-headed exclusion.

Collect the transported e-one classifier, the e-one tail rows, the positive
denominator witnesses, the unary and nonempty endpoint rows, and the two
zero-headed exclusions. These are exactly the displayed finite-prefix readback
package.
\end{proof}

Fit: traditional target finite-prefix transported selected endpoint-shape
readback package for RealUp; BEDC source RatUp plus RealUp stream prefixes;
carrier rational-observation streams; classifiers RealStreamPrefixClassifier
and RatHistoryClassifier; stability prefix truncation, prefix hsame transport,
endpoint projection, positive-denominator hsame transport, and zero-extension
exclusion; ledger selected observation index, prefix surplus index, two
transport rows, two e-one seals, and two zero-extension tests; core theorem
\autoref{thm:real-stream-prefix-transported-selected-full-shape-readback-package};
exactness the displayed classifier, tail, positive-denominator, unary, nonempty,
and zero-extension package; standard bridge finite-prefix selected endpoint
readback for transported RealUp rational observations; status
$\mathsf{ProofTarget}$.
\leanchecked{BEDC.Derived.RealUp.RealStreamPrefixClassifier\_transported\_selected\_full\_shape\_readback\_package}

\begin{theorem}[Real stream selected appended e-one tail cancellation]
\label{thm:real-stream-classifier-selected-append-e1-tail-hsame-cancel}
For rational-observation streams $x,y:\mathbb{N}\to\Hist$, index $n$, and
histories $d,e,tail_D,tail_E$, if the selected stream endpoints display as
appended e-one tails, then tail equality cancels the displayed tails and
recovers the denominator head comparison:
$$
\begin{array}{l}
\mathsf{RealStreamClassifier}(x,y)\Longrightarrow
\hsame(x(n),\Eone(d\cdot\Eone(tail_D)))\Longrightarrow\\
\qquad
\hsame(y(n),\Eone(e\cdot\Eone(tail_E)))\Longrightarrow
\hsame(tail_D,tail_E)\Longrightarrow
\hsame(d,e).
\end{array}
$$
\end{theorem}
\begin{proof}
Evaluate the stream classifier at the selected index to obtain the RatUp
history classifier for $x(n)$ and $y(n)$. Transport this classifier across the
two displayed e-one endpoint comparisons. RatUp e-one tail readback gives the
comparison between $d\cdot\Eone(tail_D)$ and $e\cdot\Eone(tail_E)$. The tail
comparison identifies the e-one suffixes, and BEDC append right cancellation
leaves exactly $\hsame(d,e)$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealStreamClassifier\_selected\_append\_e1\_tail\_hsame\_cancel}
