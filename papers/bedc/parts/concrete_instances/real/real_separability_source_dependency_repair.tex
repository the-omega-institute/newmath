The $\RealSeparabilityUp$ source ledger consumes the $\CauchyFilterCompletionUp$ route of \autoref{ch:concrete-instances-cauchyfiltercompletion-namecert} and the $\ClosedBoundedIntervalUp$ interval route of \autoref{ch:concrete-instances-closedboundedinterval-namecert} through the same finite dense rational and dyadic witness rows. Concretely, the density request reads the rational candidate row, the $\DyadicRatCoreUp$ tolerance row, the finite stream window, and the regular readback before the Cauchy-filter completion consumer sees a completion-facing dense witness, while the closed bounded interval consumer reads the same witness as an endpoint-contained interval-window row. Thus both source routes consume the displayed dense-window ledger, not a detached host dense subset, selected nearest point, quotient real equality, or ambient completeness principle.
