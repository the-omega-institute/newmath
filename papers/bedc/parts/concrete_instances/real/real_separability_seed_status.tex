The $\RealSeparabilityUp$ hub is seeded by its displayed child routes: density, Cauchy-equivalence, dyadic-lattice, completion, RegSeqRat, MetricCompletion, and countable-dense siblings jointly expose the finite separability surface without adding theorem content to the hub. Its seed route binds $\CauchyEquivalenceSetoidUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, density, completion, and countable-dense routes as the visible finite rows through which dense Real names are read, and the hub-level seed registration records exactly this child-route family. The adjacent obligation marker on the hub points to the same child-route family: the route becomes obligation-level only through the carrier, route exactness, density coverage, Cauchy compatibility, dyadic compatibility, completion handoff, transport, replay, provenance, and local naming rows named in this child status block.

\begin{theorem}[Real separability seed status]\label{thm:real-separability-seed-status}
The $\RealSeparabilityUp$ seed status binds the existing Cauchy-equivalence, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ density routes as the displayed child-route family through which dense Real names are read. It does not claim countable choice, quotient Real equality, full Real completeness, or any verified density theorem beyond the rows named in the seed ledger.
\end{theorem}

\begin{proof}
The seed child lists the visible route family and fixes the route at $\seedClosure$ with $\unformalizedV$ formal status. Thus the record is a ledger statement about displayed child routes, not a proof object for the formal axis.

The closure and verification axes remain separate: the former says which paper rows are named, while the latter is still bounded by the absence of a checked carrier admission, route exactness theorem, density coverage theorem, and completion handoff for $\RealSeparabilityUp$.
\end{proof}

\begin{theorem}[Real separability scoped density route]
\label{thm:real-separability-scoped-density-route}
Every accepted $\RealSeparabilityUp$ density read factors through the L10 source chain
$$
\CauchyEquivalenceSetoidUp,\ \DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp
$$
before exposing a countable-dense seal. The route reads the Cauchy-equivalence classifier, dyadic tolerance ledger, finite stream windows, regular-rational readback, and terminal Real seal as displayed rows, and it exports no quotient stream equality, selected dense enumeration, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the seed route named by \autoref{thm:real-separability-seed-status}. Its child-route family keeps density, Cauchy-equivalence, dyadic approximation, finite StreamName windows, RegSeqRat readback, and the Real seal visible as separate rows.

Use the Cauchy-equivalence routes, including \autoref{thm:real-separability-cauchy-equivalence-lattice-route} and \autoref{thm:real-separability-cauchy-equivalence-dense-read-route}. They place the $\CauchyEquivalenceSetoidUp$ classifier on the density read without converting it into quotient stream equality.

Next compose with the dyadic-density and L10 source routes, including \autoref{thm:real-separability-dyadic-rational-density-handoff} and \autoref{thm:real-separability-l10-density-certificate}. These keep the dyadic tolerance, stream-window, RegSeqRat, and Real rows visible before a countable-dense consumer receives the seal.

The refused endpoints follow from the same route discipline. Because the seed packet exposes only the listed child rows, it supplies neither a selected dense enumeration nor countable choice nor ambient $\RealUp$ completeness.
\end{proof}

\begin{closurestatus}{\RealSeparabilityUp}
  \constructivestory{A $\RealSeparabilityUp$ packet is generated from the existing child routes for density, Cauchy-equivalence, dyadic approximation, StreamName windows, RegSeqRat readback, Real seal, MetricCompletion handoff, completion-facing handoff, and countable dense Real-facing schedules.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{The child-route family enumerates the visible obligations for density, Cauchy-equivalence, dyadic-lattice, completion, RegSeqRat, MetricCompletion, countable-dense schedules, transport, replay, provenance, and local naming under \autoref{ch:concrete-instances-real-separability-namecert}.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This seed status does not claim a checked Lean carrier, bridge checking, mature public closure, or a standalone separability theorem beyond the child-route packet surface.}
  \upgradepath{Obligation closure requires checked carrier admission, route exactness, density coverage, Cauchy and dyadic compatibility, completion handoff, transport, replay, provenance, and local naming for BEDC.Derived.RealSeparabilityUp.}
\end{closurestatus}
