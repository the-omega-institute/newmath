\section{Real algebra and order boundary}
\label{sec:realalgorder-concrete-interface}

\begin{definition}[Concrete RealAlgOrder interface]
\label{def:realalgorder-concrete-interface}
The concrete seed for $\RealAlgOrder$ is the constant rational RealUp
subcarrier supplied by
\autoref{thm:real-constant-rational-embedding-certificate}. Its carrier is
$\mathsf{RealConstantHistoryCarrier}$, its classifier is
$\mathsf{RealConstantHistoryClassifier}$, and each algebra or order row is a
$\mathsf{BHist}$ relation obtained by applying the corresponding RatUp witness
before the e-one RealUp embedding. The rows are:
\begin{enumerate}
  \item addition:
  $\mathsf{RatAddRow}(d,e,o)$ is transported to
  $\mathsf{RealAlgAddRow}(\Eone(d),\Eone(e),\Eone(o))$;
  \item negation:
  $\mathsf{RatNegRow}(d,o)$ is transported to
  $\mathsf{RealAlgNegRow}(\Eone(d),\Eone(o))$;
  \item multiplication:
  $\mathsf{RatMulRow}(d,e,o)$ is transported to
  $\mathsf{RealAlgMulRow}(\Eone(d),\Eone(e),\Eone(o))$;
  \item absolute value:
  $\mathsf{RatAbsRow}(d,o)$ is transported to
  $\mathsf{RealAlgAbsRow}(\Eone(d),\Eone(o))$;
  \item nonnegative comparison:
  $\mathsf{RatNonnegativeRow}(d)$ is transported to
  $\mathsf{RealAlgNonnegativeRow}(\Eone(d))$;
  \item strict apartness:
  $\mathsf{RatApartRow}(d,e)$ is transported to
  $\mathsf{RealAlgApartRow}(\Eone(d),\Eone(e))$.
\end{enumerate}
Thus the interface is a constant-rational boundary object: it names only the
operation and order rows whose rational endpoints are already certified by
RatUp and whose RealUp endpoints are the constant e-one seals.
\end{definition}

\begin{theorem}[RealAlgOrder constant carrier and classifier obligations]
\label{thm:realalgorder-constant-carrier-classifier-obligations}
Let $d,e:\Hist$. The carrier and classifier face of the constant-rational
$\RealAlgOrder$ boundary consists of the two transported rows
$$
\mathsf{RatHistoryCarrier}(d)
\Longrightarrow
\mathsf{RealConstantHistoryCarrier}(\Eone(d))
$$
and
$$
\mathsf{RatHistoryClassifier}(d,e)
\Longrightarrow
\mathsf{RealConstantHistoryClassifier}(\Eone(d),\Eone(e)).
$$
These rows are stable under the e-one classifier readback of
\autoref{thm:real-constant-history-classifier-e1-iff-rat}: every displayed
Real constant classifier comparison between e-one endpoints reads back to the
RatUp classifier comparison used by the source rows.
\end{theorem}
\begin{proof}
Begin with the embedding certificate. Apply
\autoref{thm:real-constant-rational-embedding-certificate} to the rational
endpoint $d$; its carrier component is exactly the passage from
$\mathsf{RatHistoryCarrier}(d)$ to
$\mathsf{RealConstantHistoryCarrier}(\Eone(d))$.

Next read the classifier component of the same certificate at the pair
$(d,e)$. The RatUp classifier row is transported first to the constant
stream-name comparison and then to the e-one RealUp constant comparison, giving
$\mathsf{RealConstantHistoryClassifier}(\Eone(d),\Eone(e))$.

Finally apply \autoref{thm:real-constant-history-classifier-e1-iff-rat} in the
reverse readback direction. The displayed Real constant classifier comparison
is therefore the same rational classifier datum that RatUp classifier transport
uses for stability of the source carrier and classifier rows.
\end{proof}
\leanchecked{BEDC.Derived.RealAlgOrderUp.RealAlgOrderConstantCarrier\_classifier\_obligations}

\begin{theorem}[RealAlgOrder constant algebra row obligations]
\label{thm:realalgorder-constant-algebra-row-obligations}
Let $d,e,o:\Hist$. The algebra face of the constant-rational $\RealAlgOrder$
boundary consists of the four transported operation rows
$$
\begin{array}{rcl}
\mathsf{RatAddRow}(d,e,o) &\Longrightarrow&
\mathsf{RealAlgAddRow}(\Eone(d),\Eone(e),\Eone(o)),\\
\mathsf{RatNegRow}(d,o) &\Longrightarrow&
\mathsf{RealAlgNegRow}(\Eone(d),\Eone(o)),\\
\mathsf{RatMulRow}(d,e,o) &\Longrightarrow&
\mathsf{RealAlgMulRow}(\Eone(d),\Eone(e),\Eone(o)),\\
\mathsf{RatAbsRow}(d,o) &\Longrightarrow&
\mathsf{RealAlgAbsRow}(\Eone(d),\Eone(o)).
\end{array}
$$
Classifier congruence for these rows is read back through
$\mathsf{RealConstantHistoryClassifier}$ to the RatUp classifier transport on
the rational endpoints.
\end{theorem}
\begin{proof}
Use \autoref{thm:real-constant-rational-embedding-certificate} to fix the
constant-rational bridge for the displayed endpoints. It supplies the e-one
RealUp seals for $d$, $e$, and $o$, with carrier and classifier data inherited
from their RatUp histories.

For each algebra row, apply the corresponding RatUp witness before embedding.
The addition, negation, multiplication, and absolute-value witnesses therefore
remain rational ledger rows, while their visible endpoints become the
displayed e-one RealUp constants.

It remains to account for classifier congruence. Apply
\autoref{thm:real-constant-history-classifier-e1-iff-rat} to any displayed
Real constant classifier comparison among the inputs or output. The result is
the RatUp classifier comparison required by RatUp classifier transport, and the
embedding certificate repacks the transported witness as the corresponding
$\RealAlgOrder$ algebra row.
\end{proof}
\leanchecked{BEDC.Derived.RealAlgOrderUp.RealAlgOrderConstant\_algebra\_row\_obligations}

\begin{theorem}[RealAlgOrder constant order and apartness obligations]
\label{thm:realalgorder-constant-order-apartness-obligations}
Let $d,e:\Hist$. The order face of the constant-rational $\RealAlgOrder$
boundary consists of the two transported rows
$$
\mathsf{RatNonnegativeRow}(d)
\Longrightarrow
\mathsf{RealAlgNonnegativeRow}(\Eone(d))
$$
and
$$
\mathsf{RatApartRow}(d,e)
\Longrightarrow
\mathsf{RealAlgApartRow}(\Eone(d),\Eone(e)).
$$
This boundary records nonnegative and strict-apartness obligations only. It
does not assert trichotomy, least-upper-bound completeness, Cauchy
completeness, or any total order law beyond the supplied RatUp rows.
\end{theorem}
\begin{proof}
First expose the constant endpoints through
\autoref{thm:real-constant-rational-embedding-certificate}. The same
rational-to-Real passage used for the carrier and algebra rows supplies the
e-one endpoints on which the nonnegative and apartness rows are displayed.

Next transport the two RatUp order rows. A supplied
$\mathsf{RatNonnegativeRow}(d)$ gives the displayed
$\mathsf{RealAlgNonnegativeRow}(\Eone(d))$, and a supplied
$\mathsf{RatApartRow}(d,e)$ gives
$\mathsf{RealAlgApartRow}(\Eone(d),\Eone(e))$.

Finally read classifier movement back through
\autoref{thm:real-constant-history-classifier-e1-iff-rat}. The readback
produces the RatUp classifier comparisons consumed by RatUp classifier
transport, so stability of nonnegative and apartness rows is inherited only on
the constant-rational boundary. No trichotomy or completeness datum is present
in the transported rows.
\end{proof}
\leanchecked{BEDC.Derived.RealAlgOrderUp.RealAlgOrderConstant\_order\_apartness\_obligations}

\begin{theorem}[Constant rational RealAlgOrder boundary]
\label{thm:realalgorder-constant-rational-order-boundary}
Let $d,e,o:\Hist$. The RatUp carrier, classifier, nonnegative, and apartness
rows transported through the constant rational embedding give well-formed
$\RealAlgOrder$ rows on embedded RealUp constants:
$$
\mathsf{RatHistoryCarrier}(d)
\Longrightarrow
\mathsf{RealConstantHistoryCarrier}(\Eone(d)),
$$
$$
\mathsf{RatHistoryClassifier}(d,e)
\Longrightarrow
\mathsf{RealConstantHistoryClassifier}(\Eone(d),\Eone(e)),
$$
and every supplied RatUp row
$\mathsf{RatAddRow}$, $\mathsf{RatNegRow}$, $\mathsf{RatMulRow}$,
$\mathsf{RatAbsRow}$, $\mathsf{RatNonnegativeRow}$, or
$\mathsf{RatApartRow}$ with output history $o$ transports to the corresponding
$\RealAlgOrder$ row on the embedded constants. These transported rows are
stable under $\mathsf{RealConstantHistoryClassifier}$ on all displayed RealUp
constant endpoints.
\end{theorem}
\begin{proof}
Use \autoref{thm:real-constant-rational-embedding-certificate} to expose the
carrier and classifier chains from RatUp histories to StreamNameUp constant
streams and then to e-one RealUp constants. The carrier row gives
$\mathsf{RealConstantHistoryCarrier}(\Eone(d))$ from
$\mathsf{RatHistoryCarrier}(d)$, and the classifier row gives
$\mathsf{RealConstantHistoryClassifier}(\Eone(d),\Eone(e))$ from
$\mathsf{RatHistoryClassifier}(d,e)$.

For each algebra or order row, apply the corresponding RatUp witness before
embedding. Addition, negation, multiplication, absolute value, nonnegative
comparison, and strict apartness therefore keep their rational ledger as the
hidden source and receive e-one RealUp endpoints as their public boundary.

It remains to check stability under the RealUp constant classifier. Read a
classifier comparison back to the RatUp classifier at the e-one endpoint by
\autoref{thm:real-constant-history-classifier-e1-iff-rat}. RatUp classifier
transport moves the chosen algebra or order witness along the exposed rational
comparison, and the constant rational embedding certificate repacks the result
as the corresponding RealUp constant row.
\end{proof}
\leanchecked{BEDC.Derived.RealAlgOrderUp.RealAlgOrderConstant\_rational\_order\_boundary}

The intended Lean target shape for this boundary is
\texttt{BEDC.Derived.RealAlgOrderUp.constant\_rational\_order\_boundary}.

\begin{theorem}[RealAlgOrder constant rational scoped certificate]
\label{thm:realalgorder-constant-rational-scoped-certificate}
The constant-rational $\RealAlgOrder$ package is scoped exactly by
$\mathsf{RatHistoryCarrier}$, $\mathsf{RatHistoryClassifier}$, the RatUp
operation and order rows, $\mathsf{RealConstantHistoryCarrier}$,
$\mathsf{RealConstantHistoryClassifier}$, $\mathsf{BHist}$, $\hsame$,
\autoref{thm:real-constant-rational-embedding-certificate}, and
\autoref{thm:real-constant-history-classifier-e1-iff-rat}. More precisely,
it forms a ten-field scoped certificate with the following fields:
\begin{enumerate}
  \item traditional target: the constant rational real algebra and order
  surface of $\RealAlgOrder$;
  \item BEDC source: $\RatUp+\StreamNameUp+\RealUp$ restricted to RealUp
  constant rational endpoints over $\mathsf{BHist}$ and $\hsame$;
  \item carrier: $\mathsf{RealConstantHistoryCarrier}$;
  \item classifier: $\mathsf{RealConstantHistoryClassifier}$;
  \item stability: every displayed Real constant classifier comparison reads
  back to the RatUp classifier row by
  \autoref{thm:real-constant-history-classifier-e1-iff-rat};
  \item ledger: the hidden rational operation or order row remains the RatUp
  row, while the visible endpoint is sealed by the e-one RealUp constant
  embedding of \autoref{thm:real-constant-rational-embedding-certificate};
  \item carrier and classifier theorem:
  \autoref{thm:realalgorder-constant-carrier-classifier-obligations};
  \item algebra theorem:
  \autoref{thm:realalgorder-constant-algebra-row-obligations};
  \item order and apartness theorems:
  \autoref{thm:realalgorder-constant-order-apartness-obligations} and
  \autoref{thm:realalgorder-constant-rational-order-boundary};
  \item exactness: no completeness row, trichotomy row, metric row, topology
  row, or standard-bridge row is consumed.
\end{enumerate}
\end{theorem}
\begin{proof}
First project the four RealAlgOrder boundary theorems. The carrier and
classifier component comes from
\autoref{thm:realalgorder-constant-carrier-classifier-obligations}; the
algebra component comes from
\autoref{thm:realalgorder-constant-algebra-row-obligations}; the order and
apartness component comes from
\autoref{thm:realalgorder-constant-order-apartness-obligations}; and the
combined boundary comes from
\autoref{thm:realalgorder-constant-rational-order-boundary}.

Next expose the permitted source for each projected component. The only public
Real endpoints are $\Eone(d)$-sealed rational histories supplied by
\autoref{thm:real-constant-rational-embedding-certificate}. The only hidden
operation and order rows are the RatUp rows named in the concrete interface,
and the only ambient history relation used to move them is $\hsame$ on
$\mathsf{BHist}$.

Finally read every Real constant classifier move back to RatUp through
\autoref{thm:real-constant-history-classifier-e1-iff-rat}. This gives exactly
the RatUp classifier comparison required by the projected carrier,
classifier, algebra, order, and apartness rows, and no proof step asks for
completeness, trichotomy, metric structure, topology, or a standard bridge.
\end{proof}
\leanchecked{BEDC.Derived.RealAlgOrderUp.RealAlgOrderConstant\_rational\_scoped\_certificate}

The intended Lean target shape for this certificate is
\texttt{BEDC.Derived.RealAlgOrderUp.constant\_rational\_scoped\_certificate}.

\begin{theorem}[RealAlgOrder constant apartness metric handoff]
\label{thm:realalgorder-constant-apartness-metric-handoff}
For constant-rational RealUp endpoints carried by the $\RealAlgOrder$
constant carrier, the checked carrier and classifier rows, algebra rows, and
order and apartness rows package a finite $\mathsf{BHist}$ handoff for
downstream apartness and metric consumers. The handoff consists exactly of the
two displayed RealUp endpoint rows $\Eone(d)$ and $\Eone(e)$, their RatUp
witness rows, the supplied positive dyadic or apartness row, the $\hsame$ and
$\Cont$ transport routes, the visible $\mathsf{Pkg}$ provenance, and the
$\NameCert$ ledger that records the constant rational source.

Any $\ApartnessRealUp$ or $\MetricSpaceUp$ consumer may read this packet only
as finite positive-separation data in the sense of
\autoref{def:apartnessreal-finite-window-separation-row},
\autoref{thm:apartnessreal-metric-handoff}, and
\autoref{thm:apartnessreal-metric-consumer-separation-boundary}. The packet
does not export trichotomy, completeness, host real equality, quotient data,
or a classifier outside the displayed constant-rational boundary.
\end{theorem}
\begin{proof}
First project the RealAlgOrder constant boundary rows. The carrier and
classifier rows come from
\autoref{thm:realalgorder-constant-carrier-classifier-obligations}; the
operation rows come from
\autoref{thm:realalgorder-constant-algebra-row-obligations}; and the order and
apartness rows come from
\autoref{thm:realalgorder-constant-order-apartness-obligations}.

Next apply
\autoref{thm:realalgorder-constant-rational-scoped-certificate}. It packages
those rows over $\mathsf{BHist}$ with the exposed $\hsame$ transport, the
RatUp source witnesses, the e-one RealUp endpoint seals, and the visible
ledger provenance. Thus a consumer sees the two endpoint rows and the finite
positive dyadic or apartness row as one bounded packet, rather than as a
global real-order principle.

Finally match that packet with the downstream apartness surface. The finite
row of \autoref{def:apartnessreal-finite-window-separation-row}, the handoff
of \autoref{thm:apartnessreal-metric-handoff}, and the consumer boundary of
\autoref{thm:apartnessreal-metric-consumer-separation-boundary} read exactly
the endpoint, radius or apartness, transport, $\mathsf{Pkg}$, and $\NameCert$
coordinates named above. Since no cited surface contains trichotomy,
completion, host equality, or quotient coordinates, the handoff cannot export
any of them.
\end{proof}

\begin{theorem}[RealAlgOrder apartness positive-gap order consumer]
\label{thm:realalgorder-apartness-positive-gap-order-consumer}
For constant-rational RealUp endpoints carried by the $\RealAlgOrder$
boundary, a supplied RatUp apartness or nonnegative positive-gap row together
with the e-one embedding determines exactly the finite $\ApartnessRealUp$
separation packet consumed by later $\MetricSpaceUp$ and order consumers. The
packet consists of the two endpoint rows, the positive dyadic radius or
apartness row, the finite window and readback rows, the visible $\hsame$ and
$\Cont$ transports, the $\mathsf{Pkg}$ provenance, and the $\NameCert$
dependency rows.

The consumer surface exports no trichotomy, completeness, quotient equality,
host real inequality, topology row, or non-constant RealUp arithmetic. Every
downstream read is restricted to the finite separation packet of
\autoref{def:apartnessreal-finite-window-separation-row} and the consumer
boundary of \autoref{thm:apartnessreal-metric-consumer-separation-boundary}.
\end{theorem}
\begin{proof}
Project the constant-rational order and apartness rows from
\autoref{thm:realalgorder-constant-order-apartness-obligations}. The supplied
RatUp positive-gap row is then carried upward only through the e-one endpoint
embedding, so the visible RealUp endpoints remain the constant-rational rows
certified by
\autoref{thm:realalgorder-constant-rational-scoped-certificate}.

Next repackage those projected rows through
\autoref{thm:realalgorder-constant-apartness-metric-handoff}. Its handoff
surface already isolates the two endpoint rows, the positive dyadic radius or
apartness row, the $\hsame$ and $\Cont$ routes, the $\mathsf{Pkg}$ provenance,
and the $\NameCert$ ledger that records the constant rational source.

It remains to identify the packet consumed downstream. The finite row of
\autoref{def:apartnessreal-finite-window-separation-row} supplies the endpoint,
window, readback, and separation-ledger shape. Applying
\autoref{thm:apartnessreal-metric-consumer-separation-boundary} then restricts
every metric or order consumer to exactly those coordinates, so no excluded
order, quotient, host-inequality, completeness, or topology coordinate is
available to read.
\end{proof}

\begin{theorem}[RealAlgOrder constant positive-gap consumer determinacy]
\label{thm:realalgorder-constant-positive-gap-consumer-determinacy}
Let two constant-rational $\RealAlgOrder$ endpoint packets carry the same
RatUp source endpoints, the same positive dyadic or apartness row, the same
e-one $\RealUp$ embedding rows, the same finite-window and readback rows, the
same $\hsame$ and $\Cont$ transport routes, the same $\mathsf{Pkg}$
provenance, and the same $\NameCert$ ledger. Then every $\ApartnessRealUp$,
$\MetricSpaceUp$, or order consumer reading those packets reads one and the
same finite positive-gap separation packet.

The determined packet contains exactly the two constant-rational endpoint
rows, the shared positive-gap row, the finite-window and readback rows, the
visible transports, provenance, and ledger. It exports no trichotomy,
completeness, quotient equality, host real inequality, topology row, or
non-constant $\RealUp$ arithmetic.
\end{theorem}
\begin{proof}
Project the RatUp order and apartness rows for the two endpoint packets through
\autoref{thm:realalgorder-constant-order-apartness-obligations}. The
hypotheses identify the RatUp endpoints and the positive dyadic or apartness
row before any downstream consumer is allowed to read them.

Package the projected rows by
\autoref{thm:realalgorder-constant-rational-scoped-certificate}. This places
both endpoint displays over the same e-one $\RealUp$ embedding rows, the same
$\hsame$ and $\Cont$ routes, the same $\mathsf{Pkg}$ provenance, and the same
$\NameCert$ ledger.

Route the packaged data through
\autoref{thm:realalgorder-constant-apartness-metric-handoff}. Its handoff
surface has precisely the finite positive-separation coordinates listed in the
statement, so the two displays present one packet to apartness and metric
consumers.

Finally apply
\autoref{thm:realalgorder-apartness-positive-gap-order-consumer}. That theorem
identifies the downstream $\ApartnessRealUp$, $\MetricSpaceUp$, and order
consumer surface with the same finite separation packet. Since each cited
surface omits trichotomy, completeness, quotient equality, host real
inequality, topology, and non-constant real arithmetic, none of those rows is
exported by the determinacy claim.
\end{proof}

\begin{theorem}[RealAlgOrder constant public interface export]
\label{thm:realalgorder-constant-public-interface-export}
The carrier and classifier obligations of
\autoref{thm:realalgorder-constant-carrier-classifier-obligations}, the
algebra row obligations of
\autoref{thm:realalgorder-constant-algebra-row-obligations}, the order and
apartness obligations of
\autoref{thm:realalgorder-constant-order-apartness-obligations}, the rational
boundary of \autoref{thm:realalgorder-constant-rational-order-boundary}, the
scoped certificate of
\autoref{thm:realalgorder-constant-rational-scoped-certificate}, and the metric
handoff of \autoref{thm:realalgorder-constant-apartness-metric-handoff}
assemble the reusable constant-rational public $\RealAlgOrder$ interface.

Every consumer read of this interface factors through RatUp,
StreamNameUp, RealUp constant endpoints, $\mathsf{BHist}$, $\hsame$,
$\Cont$, $\mathsf{Pkg}$, and $\NameCert$. The interface exports no
trichotomy, completeness, host real equality, quotient data, non-constant
RealUp arithmetic, topology row, or standard bridge.
\end{theorem}
\begin{proof}
Project the checked carrier and classifier rows, the algebra rows, and the
order and apartness rows from the three obligation theorems named above. The
rational order boundary identifies these rows as constant-rational rows
transported from RatUp through the e-one RealUp endpoint embedding.

Package the projected rows with
\autoref{thm:realalgorder-constant-rational-scoped-certificate}. This makes
the public surface a scoped packet over $\mathsf{BHist}$, visible $\hsame$ and
$\Cont$ transport, $\mathsf{Pkg}$ provenance, and the $\NameCert$ ledger.

Finally route apartness and metric consumers through
\autoref{thm:realalgorder-constant-apartness-metric-handoff}. Since the
handoff reads only the displayed constant endpoints and finite separation
rows, none of the excluded full-real, quotient, topology, completeness, or
bridge data is available to export.
\end{proof}

\begin{theorem}[RealAlgOrder constant public order-metric interface]
\label{thm:realalgorder-constant-public-order-metric-interface}
For constant-rational $\RealUp$ endpoints carried by the $\RealAlgOrder$
package, every public order read, apartness read, and $\MetricSpaceUp$-facing
read factors through the RatUp denominator row, the $\Eone$ $\RealUp$
endpoint, the transported algebra, order, and apartness rows, the positive-gap
separation packet, $\hsame$, $\Cont$, and $\mathsf{Pkg}$ provenance.

The public boundary exports no nonconstant real arithmetic, trichotomy,
completeness, topology, quotient carrier, host-real inequality, or standard
bridge row. Thus order consumers and metric consumers receive the same finite
$\mathsf{BHist}$ constant-rational interface.
\end{theorem}
\begin{proof}
Begin with \autoref{thm:realalgorder-constant-public-interface-export}. It
packages the carrier, classifier, algebra, order, apartness, rational boundary,
and metric handoff rows into one public constant-rational $\RealAlgOrder$
surface over RatUp denominators, $\Eone$ endpoints, $\hsame$, $\Cont$,
$\mathsf{Pkg}$ provenance, and the $\NameCert$ ledger.

For the metric-facing read, apply
\autoref{thm:realalgorder-constant-apartness-metric-handoff}. The handoff
identifies the displayed $\MetricSpaceUp$ surface with the same constant
endpoint rows and the same finite positive-separation data.

For the order-facing read, apply
\autoref{thm:realalgorder-apartness-positive-gap-order-consumer}. This turns
the positive-gap separation packet into the order-consumer row without adding a
new full-real comparison principle.

The determinacy and scope controls come from
\autoref{thm:realalgorder-constant-positive-gap-consumer-determinacy} and
\autoref{thm:realalgorder-constant-rational-scoped-certificate}. They keep the
apartness, metric, and order surfaces tied to the transported rational witness,
the finite $\mathsf{BHist}$ ledger, and the displayed provenance. Since the
cited rows omit nonconstant arithmetic, trichotomy, completeness, topology,
quotient carriers, host-real inequalities, and standard bridges, the combined
public boundary omits them as well.
\end{proof}

\begin{theorem}[RealAlgOrder positive apartness nonzero metric boundary]
\label{thm:realalgorder-positive-apartness-nonzero-metric-boundary}
For constant-rational $\RealUp$ endpoints carried by the $\RealAlgOrder$
package, suppose the RatUp positive-gap or apartness row used by
\autoref{thm:realalgorder-apartness-positive-gap-order-consumer} is present.
Then every $\MetricSpaceUp$-facing handoff packet supplied by
\autoref{thm:realalgorder-constant-apartness-metric-handoff} carries a
nonempty positive-distance boundary. It cannot be read as an empty-distance
witness for the same displayed endpoints.

The exported packet is restricted to finite $\mathsf{BHist}$ endpoint rows,
the radius or apartness row, $\hsame$ transport, $\Cont$ routes,
$\mathsf{Pkg}$ provenance, and $\NameCert$ dependency rows. It exports no
trichotomy, full real completeness, quotient carrier, host-real equality, or
non-constant $\RealUp$ arithmetic.
\end{theorem}
\begin{proof}
Project the order and apartness rows from
\autoref{thm:realalgorder-constant-order-apartness-obligations}. The supplied
RatUp positive-gap row is the same row consumed by
\autoref{thm:realalgorder-apartness-positive-gap-order-consumer}, so the
constant endpoints remain tied to their rational witnesses and visible
$\Eone$ embeddings.

Pass those projected rows through
\autoref{thm:realalgorder-constant-apartness-metric-handoff}. The handoff
packet is therefore the finite positive-separation packet described by
\autoref{def:apartnessreal-finite-window-separation-row} and
\autoref{thm:apartnessreal-metric-consumer-separation-boundary}, with the
positive radius or apartness ledger still visible.

Now test the contrary empty-distance read on the metric-facing side. The
MetricUp endpoint boundary
\autoref{thm:metric-distance-witness-nonempty-distance-endpoint-nonempty}
requires a nonempty endpoint boundary from a nonempty distance, while
\autoref{thm:metric-distance-witness-empty-distance-visible-endpoint-absurd}
rules out reading the same visible positive endpoint evidence as an
empty-distance witness. Thus the positive apartness packet can only enter the
metric consumer as a nonempty positive-distance boundary.

Finally apply
\autoref{thm:realalgorder-constant-public-order-metric-interface}. It limits
the exported rows to finite endpoint histories, radius or apartness evidence,
visible $\hsame$ and $\Cont$ transport, $\mathsf{Pkg}$ provenance, and
$\NameCert$ rows. No excluded full-real or quotient coordinate is available to
turn the positive boundary into an empty-distance packet.
\end{proof}

\begin{theorem}[RealAlgOrder constant positive boundary metric consumer]
\label{thm:realalgorder-constant-positive-boundary-metric-consumer}
A constant-rational $\RealAlgOrderUp$ endpoint packet carrying a positive
apartness row or a positive-gap row exports exactly the $\MetricUp$
nonempty-distance consumer boundary. The consumer input consists of finite
$\mathsf{BHist}$ endpoint histories, the positive radius or apartness ledger,
visible $\hsame$ and $\Cont$ routes, $\mathsf{Pkg}$ provenance, and the local
$\NameCert$ rows for the constant endpoint packet.

Thus the exported metric consumer surface contains no empty-distance witness,
quotient real equality, trichotomy, selected limit, full real arithmetic row, or
ambient completeness principle.
\end{theorem}
\begin{proof}
Project the metric-facing handoff through
\autoref{thm:realalgorder-constant-apartness-metric-handoff}. It identifies
the $\MetricUp$ consumer side with the same constant endpoints and the same
positive separation data carried by the $\RealAlgOrderUp$ packet.

The positive packet is the order-facing packet isolated by
\autoref{thm:realalgorder-apartness-positive-gap-order-consumer}. Its
determinacy is controlled by
\autoref{thm:realalgorder-constant-positive-gap-consumer-determinacy}, so the
positive gap, apartness row, rational witness, and displayed endpoint histories
cannot drift apart.

Now apply the endpoint boundary rows for $\MetricUp$. The nonempty-distance
boundary of
\autoref{thm:metric-distance-witness-nonempty-distance-endpoint-nonempty}
accepts the displayed positive endpoint evidence, while
\autoref{thm:metric-distance-witness-empty-distance-visible-endpoint-absurd}
excludes reading that same evidence as an empty-distance endpoint witness.

Finally use
\autoref{thm:realalgorder-constant-public-order-metric-interface}. It exhausts
the public rows by finite endpoint histories, the positive ledger, visible
$\hsame$ and $\Cont$ routes, $\mathsf{Pkg}$ provenance, and $\NameCert$ rows.
Since no quotient, trichotomy, selected-limit, completeness, or full-arithmetic
coordinate is exported, the consumer boundary is exactly the nonempty-distance
input just described.
\end{proof}

\begin{theorem}[RealAlgOrder constant rational standard bridge boundary]
\label{thm:realalgorder-constant-rational-standard-bridge-boundary}
The constant-rational public $\RealAlgOrder$ interface of
\autoref{thm:realalgorder-constant-public-interface-export}, the
order-metric public interface of
\autoref{thm:realalgorder-constant-public-order-metric-interface}, and the
positive-boundary metric consumer of
\autoref{thm:realalgorder-constant-positive-boundary-metric-consumer}
assemble a conservative standard bridge from RatUp rational witnesses through
RealUp constant endpoints to the $\MetricUp$ nonempty-distance consumer
surface.

The bridge rows are exactly the displayed $\mathsf{BHist}$, $\hsame$,
$\Cont$, $\mathsf{Pkg}$, $\NameCert$, RatUp denominator, RealUp constant
endpoint, $\RealAlgOrder$ order and apartness, and $\MetricUp$
distance-boundary rows. The packet contains no trichotomy, completed-real
equality, full nonconstant arithmetic, topology, quotient carrier, selected
limit, completeness, or arbitrary host metric-space coordinate.
\end{theorem}
\begin{proof}
Project the public constant-rational interface by
\autoref{thm:realalgorder-constant-public-interface-export}. This supplies the
RatUp denominator witnesses, their $\Eone$ RealUp constant endpoints, the
finite $\mathsf{BHist}$ endpoint histories, visible $\hsame$ and $\Cont$
transport, $\mathsf{Pkg}$ provenance, and the $\NameCert$ rows.

Route the order and metric reads through
\autoref{thm:realalgorder-constant-public-order-metric-interface}. The order,
apartness, and metric-facing consumers therefore receive the same displayed
constant-rational rows rather than separate carrier, quotient, topology, or
completion data.

Identify the metric side with
\autoref{thm:realalgorder-constant-positive-boundary-metric-consumer}. It
pins the $\MetricUp$ consumer surface to the nonempty-distance boundary
carried by the positive-gap or apartness ledger over the same endpoints.

Conservativity follows because every row just cited is one of the finite
displayed rows named in the statement. None of trichotomy, completed-real
equality, full nonconstant arithmetic, topology, quotient carriers, selected
limits, completeness, or arbitrary host metric spaces is a coordinate of the
packet, so the bridge exports only the displayed RatUp--RealUp--MetricUp
boundary.
\end{proof}

\begin{theorem}[RealAlgOrder standard bridge row conservativity]
\label{thm:realalgorder-standard-bridge-row-conservativity}
For constant-rational $\RealUp$ endpoints carried by the
$\RealAlgOrder$ package, every standard bridge read factors through the
displayed $\RatUp$ denominator witness, $\RealUp$ embedding row,
algebra row, order and apartness row, metric handoff row, finite
$\mathsf{BHist}$ endpoint histories, visible $\hsame$ comparisons,
$\Cont$ routes, $\mathsf{Pkg}$ provenance, and local $\NameCert$ rows.

Consequently the bridge exports no full non-constant real arithmetic,
trichotomy, completeness principle, topology, quotient carrier, or
host-real inequality.
\end{theorem}
\begin{proof}
Begin with the scoped constant-rational certificate of
\autoref{thm:realalgorder-constant-rational-scoped-certificate}. It fixes the
only admissible endpoint histories as $\Eone$-embedded rational witnesses and
keeps the classifier transport, $\mathsf{BHist}$ rows, $\hsame$ rows,
$\Cont$ routes, $\mathsf{Pkg}$ provenance, and $\NameCert$ ledger visible.

Project the public order and metric rows through
\autoref{thm:realalgorder-constant-public-order-metric-interface}. The algebra,
order, apartness, and metric-facing coordinates are therefore read from the
same displayed rational witness and its constant $\RealUp$ embedding, not from
an ambient real carrier.

For positive metric consumers, apply
\autoref{thm:realalgorder-positive-boundary-metric-consumer}. The handoff row is
the visible nonempty-distance boundary obtained from the positive gap or
apartness ledger over those same endpoints.

Finally use
\autoref{thm:realalgorder-constant-rational-standard-bridge-boundary}. Its
bridge ledger is exhausted by exactly the rows listed above. Since no row names
full non-constant arithmetic, trichotomy, completeness, topology, quotient
carriers, or host-real inequalities, every standard bridge read is conservative
over the transported $\RatUp$ rows.
\end{proof}

\begin{closurestatus}{\RealAlgOrderUp}
  \constructivestory{A constant-rational real is obtained by taking a $\RatUp$ denominator history $d$ and placing one b1 boundary over it, so the admitted real histories are exactly those of the form $\Eone(d)$ coming from the rational embedding. Two such real histories name the same constant rational when the rational classifier already identifies their underlying denominators; the real layer does not add a second comparison beyond that transported rational one. The source is the constant-rational predicate, the pattern records the b1-embedded shape together with the addition, multiplication, order, and apartness rows, and the ledger keeps the visible real endpoint tied to the rational witness from which it came. Because the rational classifier is an equivalence and the embedding is injective on representatives, the embedded rational supplies the carrier witness, the transported classifier is reflexive, symmetric, and transitive, and any recognized representative remains in the constant-rational subcarrier. A real history $h$ lies in the signature gap of a $\RealAlgOrder$ token $p$ when the source-domain accepts $h$, the signature relation pairs it with a rational witness and evidence ledger, and the token-introduction relation names that witness as $p$; after unfolding, $h$ is $\Eone(d)$ for a rational $d$ classified as the rational value encoded by $p$. Each algebra or order row is read by going down to the rational witness, applying the matching $\RatUp$ row, and embedding the result back upward, as in $\Eone(d)+\Eone(e)$ being computed by $d+e$ before re-embedding. The chapter registers this transport-through-rational construction under the name $\RealAlgOrder$; these rows are inherited through the rational embedding, with no full non-constant real arithmetic, completeness, or standard bridge introduced here.}
  \theoryclosure{\scopedClosure}
  \scopeclosed{\autoref{thm:realalgorder-constant-rational-scoped-certificate} binds RatUp, StreamNameUp, RealUp constant rational endpoints, $\mathsf{BHist}$, $\hsame$, and classifier transport for carrier, classifier, algebra, order, and apartness rows.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.RealAlgOrderUp.RealAlgOrderConstant\_rational\_scoped\_certificate}
  \bridgestatus{none}
  \notclaimed{This boundary does not claim complete real arithmetic, trichotomy, completeness, metric or topological consequences, or a standard bridge.}
  \upgradepath{A stronger verification grade requires audit-clean and bridge-checked RealAlgOrder targets while preserving the independent paper and Lean axes.}
\end{closurestatus}
