\begin{theorem}[Real separability dyadic lattice forward route]
\label{thm:real-separability-dyadic-lattice-forward-route}
Every accepted $\RealSeparabilityUp$ dense-read route that exports a countable dense ledger factors first through the dyadic rational density rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, then through the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, and finally through the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The exported ledger is therefore a displayed dyadic, regular-sequence, and Real-sealed route, not a host countability claim, quotient real equality, or ambient $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Read the existing RealSeparability child routes. The density route and dyadic lattice link expose rational candidates only after the dyadic tolerance rows have fixed the finite approximation scale.

The RegSeqRat forward route then supplies the regular rational readback. It keeps the StreamName window and dyadic tolerance visible before a dense-completion or countable-density consumer can inspect the exported row.

Finally the Real seal route closes the read at $\RealUp$. Since the hub child routes carry no host countability oracle, quotient equality token, or ambient completeness coordinate, the countable dense ledger is exported only through the displayed dyadic, RegSeqRat, and Real-sealed dependency chain.
\end{proof}
