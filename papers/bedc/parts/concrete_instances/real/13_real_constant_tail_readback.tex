\begin{theorem}[Real constant classifier appended e-one tail unary readback]
\label{thm:real-constant-history-classifier-append-e1-tail-unary-readback}
For histories $d,e,tail_D,tail_E$, a constant RealUp classifier whose two
rational denominators are displayed as appended e-one tails reads back both
unary factors and the rational endpoint classifier:
$$
\mathsf{RealConstantHistoryClassifier}(\Eone(d\cdot\Eone(tail_D)),
  \Eone(e\cdot\Eone(tail_E)))
\Longrightarrow
\mathsf{UnaryHistory}(d)\land\mathsf{UnaryHistory}(tail_D)\land
\mathsf{UnaryHistory}(e)\land\mathsf{UnaryHistory}(tail_E)\land
\hsame(d\cdot\Eone(tail_D),e\cdot\Eone(tail_E)).
$$
\end{theorem}
\begin{proof}
Use the e-one RealUp/RatUp classifier equivalence to expose the underlying
RatUp history classifier. Its positive-denominator fields apply to both
displayed denominators. The appended e-one denominator decomposition reads
each positive denominator as unary evidence for its head and tail, and the
remaining classifier field is the displayed endpoint $\hsame$ relation.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantHistoryClassifier\_append\_e1\_tail\_unary\_readback}

\begin{theorem}[Real constant classifier common-head e-one tail readback]
\label{thm:real-constant-history-classifier-append-common-head-e1-tail-readback}
For histories $left,d,e,tail_D,tail_E$, a common visible head in two displayed
constant RealUp e-one tail denominators can be read back to the five unary
factors and the central denominator comparison:
$$
\begin{array}{l}
\mathsf{RealConstantHistoryClassifier}
  (\Eone(left\cdot(d\cdot\Eone(tail_D))),
   \Eone(left\cdot(e\cdot\Eone(tail_E))))\\
\qquad\Longrightarrow
\hsame(tail_D,tail_E)\Longrightarrow\\
\qquad
\mathsf{UnaryHistory}(left)\land
\mathsf{UnaryHistory}(d)\land
\mathsf{UnaryHistory}(e)\land
\mathsf{UnaryHistory}(tail_D)\land
\mathsf{UnaryHistory}(tail_E)\land
\hsame(d,e).
\end{array}
$$
\end{theorem}
\begin{proof}
Use associativity of BEDC append to expose the endpoints as appended e-one
tails with heads $left\cdot d$ and $left\cdot e$. The appended e-one tail
readback theorem gives unary evidence for both composite heads, the two tails,
and the displayed endpoint comparison. Split the composite unary heads into
their factors. The assumed tail comparison identifies the two e-one tail
suffixes, so BEDC append right cancellation removes the shared suffix; append
left cancellation then removes the common head and yields $\hsame(d,e)$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantHistoryClassifier\_append\_common\_head\_e1\_tail\_readback}
\begin{theorem}[Real constant classifier appended e-one matched-tail head readback]
\label{thm:real-constant-classifier-appended-e1-matched-tail-head-readback}
For histories $d,e,tail_D,tail_E$, if
$$
\mathsf{RealConstantHistoryClassifier}(\Eone(d\cdot\Eone(tail_D)),
  \Eone(e\cdot\Eone(tail_E)))\land
\hsame(tail_D,tail_E),
$$
then
$$
\mathsf{UnaryHistory}(d)\land\mathsf{UnaryHistory}(tail_D)\land
\mathsf{UnaryHistory}(e)\land\mathsf{UnaryHistory}(tail_E)\land
\hsame(d,e).
$$
\end{theorem}
\begin{proof}
Apply
\autoref{thm:real-constant-history-classifier-append-e1-tail-unary-readback}
to the displayed classifier. This gives the four unary rows for $d$,
$tail_D$, $e$, and $tail_E$, together with endpoint equality
$\hsame(d\cdot\Eone(tail_D),e\cdot\Eone(tail_E))$.

The assumed tail agreement $\hsame(tail_D,tail_E)$ transports through the
e-one constructor and right append congruence. Hence the right endpoint may be
read with the same displayed tail as the left endpoint:
$\hsame(e\cdot\Eone(tail_E),e\cdot\Eone(tail_D))$.

Endpoint transport therefore presents a constant-real classifier of
$\Eone(d\cdot\Eone(tail_D))$ against $\Eone(e\cdot\Eone(tail_D))$. Apply
\autoref{thm:real-constant-history-classifier-append-common-tail-cancel} with
common tail $\Eone(tail_D)$ to obtain $\hsame(d,e)$, and combine this with the
four unary rows already read back.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantHistoryClassifier\_append\_e1\_matched\_tail\_head\_readback}

The ten-field fit is: traditional target finite denominator-tail agreement
readback for RealUp constants; BEDC source RealUp constant classifiers plus
RatUp positive-denominator readback; carrier BEDC histories; classifier
$\mathsf{RealConstantHistoryClassifier}$; stability by endpoint transport,
e-one congruence, append congruence, and common-tail cancellation; ledger
displayed denominator heads and displayed e-one tails; core theorem
\autoref{thm:real-constant-classifier-appended-e1-matched-tail-head-readback};
exactness the displayed five-row conjunction; standard bridge finite
RegSeqRat denominator-tail readback; status $\mathsf{ProofTarget}$.

\begin{theorem}[Real constant classifier appended e-one tail denominator package]
\label{thm:real-constant-history-classifier-append-e1-tail-denominator-package}
For histories $d,e,tail_D,tail_E,z_D,z_E:\mathsf{BHist}$, write
$D=d\cdot\Eone(tail_D)$ and $E=e\cdot\Eone(tail_E)$. If
$$
\mathsf{RealConstantHistoryClassifier}(\Eone(D),\Eone(E)),
$$
then the appended rational denominators satisfy the following package:
\begin{enumerate}
  \item $\mathsf{RatHistoryClassifier}(D,E)$;
  \item $\mathsf{PositiveUnaryDenominator}(D)$ and
  $\mathsf{PositiveUnaryDenominator}(E)$;
  \item $\mathsf{UnaryHistory}(d)$, $\mathsf{UnaryHistory}(tail_D)$,
  $\mathsf{UnaryHistory}(e)$, and $\mathsf{UnaryHistory}(tail_E)$;
  \item $D\hsame\emp\Longrightarrow\bot$ and
  $E\hsame\emp\Longrightarrow\bot$;
  \item $D\hsame\Ezero(z_D)\Longrightarrow\bot$ and
  $E\hsame\Ezero(z_E)\Longrightarrow\bot$;
  \item $D\hsame E$.
\end{enumerate}
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-constant-history-classifier-e1-iff-rat} to the
displayed constant RealUp classifier. This exposes the underlying
$\mathsf{RatHistoryClassifier}(D,E)$ and keeps its classifier field available
for the final $D\hsame E$ row.

Next apply \autoref{thm:rat-history-classifier-positive-denominators} to the
rational classifier. The result supplies positive unary denominator witnesses
for both appended tails. Applying
\autoref{thm:rat-positive-denominator-append-eone-right-exact} to each
witness decomposes the two appended e-one denominators into the four unary
factor rows.

Finally apply \autoref{thm:positive-unary-denominator-unary-and-nonempty} to
the two positive-denominator witnesses to obtain the two exclusions against
$\emp$. If either appended denominator were history-same to a zero-extension,
transport the corresponding positive-denominator witness along that sameness
and contradict \autoref{thm:rat-positive-denominator-e0-absurd}. The hsame row
is the rational classifier endpoint field, equivalently the checked unary
readback theorem above.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstantHistoryClassifier\_append\_e1\_tail\_denominator\_package}

\begin{theorem}[Real constant appended e-one tail rational-stream bridge readback]
\label{thm:real-constant-appended-tail-rational-stream-bridge-readback}
For histories $d,e,tail_D,tail_E,z_D,z_E:\Hist$ and observation maps
$r,q:\Hist\to\Hist$, set
$$
D=d\cdot\Eone(tail_D)
\qquad\text{and}\qquad
E=e\cdot\Eone(tail_E).
$$
Write the four bridge classifier rows as
$$
A=\mathsf{RatHistoryClassifier}(D,E),
\qquad
B=\mathsf{RatStreamNameClassifier}(\mathsf{RatConstStream}_D\circ r,
\mathsf{RatConstStream}_E\circ q),
$$
$$
U=\mathsf{RealUnaryStreamClassifier}(\mathsf{RatConstStream}_D\circ r,
\mathsf{RatConstStream}_E\circ q),
\qquad
C=\mathsf{RealConstantHistoryClassifier}(\Eone(D),\Eone(E)).
$$
Let
$$
R=\mathsf{UnaryHistory}(d)\land\mathsf{UnaryHistory}(tail_D)\land
\mathsf{UnaryHistory}(e)\land\mathsf{UnaryHistory}(tail_E)\land
\hsame(D,E).
$$
Let $P_{z_D,z_E}$ denote the conjunction of the following appended-tail
denominator package rows:
\begin{enumerate}
  \item $\mathsf{RatHistoryClassifier}(D,E)$;
  \item $\mathsf{PositiveUnaryDenominator}(D)$ and
  $\mathsf{PositiveUnaryDenominator}(E)$;
  \item $\mathsf{UnaryHistory}(d)$, $\mathsf{UnaryHistory}(tail_D)$,
  $\mathsf{UnaryHistory}(e)$, and $\mathsf{UnaryHistory}(tail_E)$;
  \item $D\hsame\emp\Longrightarrow\bot$ and
  $E\hsame\emp\Longrightarrow\bot$;
  \item $D\hsame\Ezero(z_D)\Longrightarrow\bot$ and
  $E\hsame\Ezero(z_E)\Longrightarrow\bot$;
  \item $D\hsame E$.
\end{enumerate}
Then the bridge rows are linked by
$$
(A\Longleftrightarrow B)\land(B\Longleftrightarrow U)\land
(U\Longleftrightarrow C),
$$
and every row reads back the same appended-tail unary readback row:
$$
(A\Longrightarrow R)\land(B\Longrightarrow R)\land
(U\Longrightarrow R)\land(C\Longrightarrow R),
$$
and the same appended-tail denominator package:
$$
(A\Longrightarrow P_{z_D,z_E})\land(B\Longrightarrow P_{z_D,z_E})\land
(U\Longrightarrow P_{z_D,z_E})\land(C\Longrightarrow P_{z_D,z_E}).
$$
\end{theorem}
\begin{proof}
Instantiate \autoref{thm:real-constant-independent-reindexed-stream-bridge}
with the histories $D$ and $E$ and the observation maps $r$ and $q$. The three
resulting equivalences are exactly the displayed links $A\Longleftrightarrow B$,
$B\Longleftrightarrow U$, and $U\Longleftrightarrow C$.

The direct readback from $C$ is
\autoref{thm:real-constant-history-classifier-append-e1-tail-denominator-package},
applied to the decompositions $D=d\cdot\Eone(tail_D)$ and
$E=e\cdot\Eone(tail_E)$ with the displayed zero witnesses $z_D$ and $z_E$.
This gives every row of $P_{z_D,z_E}$ at once.

The direct readback from $A$ starts with the rational classifier
$\mathsf{RatHistoryClassifier}(D,E)$. Apply
\autoref{thm:rat-history-classifier-positive-denominators} to obtain both
positive unary denominator rows. Apply
\autoref{thm:rat-positive-denominator-append-eone-right-exact} to the two
positive-denominator witnesses to read back the four unary factors.

Next apply
\autoref{thm:positive-unary-denominator-unary-and-nonempty} to obtain both
exclusions against $\emp$. If either appended denominator were
history-same to $\Ezero(z_D)$ or $\Ezero(z_E)$, transport the corresponding
positive-denominator witness across that equality and contradict
\autoref{thm:rat-positive-denominator-e0-absurd}. The final $D\hsame E$ row is
the endpoint sameness field of the rational classifier.

For $B$, transport along $A\Longleftrightarrow B$ back to $A$ and use the
direct rational readback. For $U$, either transport through
$B\Longleftrightarrow U$ and then $A\Longleftrightarrow B$, or transport
through $U\Longleftrightarrow C$ and use the direct constant-history readback.
The unary row $R$ is the unary-factor and endpoint-sameness part of the same
package. Thus every bridge classifier row recovers $R$ and the full package
$P_{z_D,z_E}$.
\end{proof}

\begin{theorem}[Real constant appended-tail bridge denominator package]
\label{thm:real-constant-appended-tail-bridge-denominator-package}
For histories $d,e,tail_D,tail_E,z_D,z_E:\Hist$ and observation maps
$r,q:\Hist\to\Hist$, set
$$
D=d\cdot\Eone(tail_D)
\qquad\text{and}\qquad
E=e\cdot\Eone(tail_E).
$$
Write the bridge classifier rows as
$$
A=\mathsf{RatHistoryClassifier}(D,E),
\qquad
B=\mathsf{RatStreamNameClassifier}(\mathsf{RatConstStream}_D\circ r,
\mathsf{RatConstStream}_E\circ q),
$$
$$
U=\mathsf{RealUnaryStreamClassifier}(\mathsf{RatConstStream}_D\circ r,
\mathsf{RatConstStream}_E\circ q),
\qquad
C=\mathsf{RealConstantHistoryClassifier}(\Eone(D),\Eone(E)).
$$
Let $P(D,E,z_D,z_E)$ denote the following appended-tail denominator package:
\begin{enumerate}
  \item $\mathsf{RatHistoryClassifier}(D,E)$;
  \item $\mathsf{PositiveUnaryDenominator}(D)$ and
  $\mathsf{PositiveUnaryDenominator}(E)$;
  \item $\mathsf{UnaryHistory}(d)$, $\mathsf{UnaryHistory}(tail_D)$,
  $\mathsf{UnaryHistory}(e)$, and $\mathsf{UnaryHistory}(tail_E)$;
  \item $D\hsame\emp\Longrightarrow\bot$ and
  $E\hsame\emp\Longrightarrow\bot$;
  \item $D\hsame\Ezero(z_D)\Longrightarrow\bot$ and
  $E\hsame\Ezero(z_E)\Longrightarrow\bot$;
  \item $D\hsame E$.
\end{enumerate}
Then every bridge row entails this package:
$$
(A\Longrightarrow P(D,E,z_D,z_E))\land
(B\Longrightarrow P(D,E,z_D,z_E))\land
(U\Longrightarrow P(D,E,z_D,z_E))\land
(C\Longrightarrow P(D,E,z_D,z_E)).
$$
\end{theorem}
\begin{proof}
The $C$ row gives the package directly. Apply
\autoref{thm:real-constant-history-classifier-append-e1-tail-denominator-package}
to the displayed decompositions $D=d\cdot\Eone(tail_D)$ and
$E=e\cdot\Eone(tail_E)$, with the endpoint zero witnesses $z_D$ and $z_E$.

The bridge equivalences are those of
\autoref{thm:real-constant-appended-tail-rational-stream-bridge-readback}.
From $U$, use the final bridge equivalence to obtain $C$, then apply the
preceding package readback.

From $B$, use the second bridge equivalence and then the final bridge
equivalence. This reaches the constant real-history row, so the same package
readback supplies $P(D,E,z_D,z_E)$.

From $A$, use all three bridge equivalences in order. The resulting $C$ row
again yields the complete appended-tail denominator package.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstant\_appended\_tail\_bridge\_denominator\_package}

\begin{theorem}[Real constant appended-tail bridge denominator package exactness]
\label{thm:real-constant-appended-tail-bridge-denominator-package-exactness}
For histories $d,e,tail_D,tail_E,z_D,z_E:\Hist$ and observation maps
$r,q:\Hist\to\Hist$, set
$$
D=d\cdot\Eone(tail_D)
\qquad\text{and}\qquad
E=e\cdot\Eone(tail_E).
$$
Write the bridge classifier rows as
$$
A=\mathsf{RatHistoryClassifier}(D,E),
\qquad
B=\mathsf{RatStreamNameClassifier}(\mathsf{RatConstStream}_D\circ r,
\mathsf{RatConstStream}_E\circ q),
$$
$$
U=\mathsf{RealUnaryStreamClassifier}(\mathsf{RatConstStream}_D\circ r,
\mathsf{RatConstStream}_E\circ q),
\qquad
C=\mathsf{RealConstantHistoryClassifier}(\Eone(D),\Eone(E)).
$$
Let $P(D,E,z_D,z_E)$ denote the following appended-tail denominator package:
\begin{enumerate}
  \item $\mathsf{RatHistoryClassifier}(D,E)$;
  \item $\mathsf{PositiveUnaryDenominator}(D)$ and
  $\mathsf{PositiveUnaryDenominator}(E)$;
  \item $\mathsf{UnaryHistory}(d)$, $\mathsf{UnaryHistory}(tail_D)$,
  $\mathsf{UnaryHistory}(e)$, and $\mathsf{UnaryHistory}(tail_E)$;
  \item $D\hsame\emp\Longrightarrow\bot$ and
  $E\hsame\emp\Longrightarrow\bot$;
  \item $D\hsame\Ezero(z_D)\Longrightarrow\bot$ and
  $E\hsame\Ezero(z_E)\Longrightarrow\bot$;
  \item $D\hsame E$.
\end{enumerate}
Then the denominator package is exact for the bridge rows:
$$
P(D,E,z_D,z_E)\Longleftrightarrow A\land B\land U\land C.
$$
\end{theorem}
\begin{proof}
Assume $P(D,E,z_D,z_E)$. Its first row is the rational history classifier
$A$. Apply
\autoref{thm:real-constant-appended-tail-rational-stream-bridge-readback} to
the displayed histories and observation maps. The three bridge equivalences
carry $A$ to $B$, then to $U$, and then to $C$, so the four bridge rows hold
together.

Conversely assume $A\land B\land U\land C$. Project the final row $C$ and
apply
\autoref{thm:real-constant-history-classifier-append-e1-tail-denominator-package}
to the decompositions $D=d\cdot\Eone(tail_D)$ and
$E=e\cdot\Eone(tail_E)$, with the zero witnesses $z_D$ and $z_E$. This
recovers every row of $P(D,E,z_D,z_E)$.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstant\_appended\_tail\_bridge\_denominator\_package\_exactness}

\begin{theorem}[Real constant appended-tail bridge appended e-zero exclusion without tail agreement]
\label{thm:real-constant-appended-tail-bridge-appended-ezero-exclusion}
For histories $d,e,tail_D,tail_E,u_D,u_E,z_D,z_E:\Hist$ and observation maps
$r,q:\Hist\to\Hist$, set
$$
D=d\cdot\Eone(tail_D)
\qquad\text{and}\qquad
E=e\cdot\Eone(tail_E).
$$
Write the bridge classifier rows as
$$
A=\mathsf{RatHistoryClassifier}(D,E),
\qquad
B=\mathsf{RatStreamNameClassifier}(\mathsf{RatConstStream}_D\circ r,
\mathsf{RatConstStream}_E\circ q),
$$
$$
U=\mathsf{RealUnaryStreamClassifier}(\mathsf{RatConstStream}_D\circ r,
\mathsf{RatConstStream}_E\circ q),
\qquad
C=\mathsf{RealConstantHistoryClassifier}(\Eone(D),\Eone(E)).
$$
Let $R(D,E,u_D,u_E,z_D,z_E)$ denote the paired exclusions
$$
(\hsame(D,u_D\cdot\Ezero(z_D))\Longrightarrow\bot)\land
(\hsame(E,u_E\cdot\Ezero(z_E))\Longrightarrow\bot).
$$
Then every bridge row entails $R(D,E,u_D,u_E,z_D,z_E)$:
$$
(A\Longrightarrow R(D,E,u_D,u_E,z_D,z_E))\land
(B\Longrightarrow R(D,E,u_D,u_E,z_D,z_E))\land
(U\Longrightarrow R(D,E,u_D,u_E,z_D,z_E))\land
(C\Longrightarrow R(D,E,u_D,u_E,z_D,z_E)).
$$
\end{theorem}
\begin{proof}
Apply
\autoref{thm:real-constant-appended-tail-bridge-denominator-package}. For each
of the four bridge rows $A,B,U,C$, this supplies the package
$P(D,E,z_D,z_E)$.

Project the two positive-denominator rows of $P$. If
$\hsame(D,u_D\cdot\Ezero(z_D))$, transport the
$\mathsf{PositiveUnaryDenominator}(D)$ witness across the displayed appended
e-zero-tail sameness and contradict
\autoref{thm:positive-unary-denominator-append-e0-tail-absurd}.

The same argument applied to $E$ transports the
$\mathsf{PositiveUnaryDenominator}(E)$ witness across
$\hsame(E,u_E\cdot\Ezero(z_E))$ and gives the second contradiction. These two
contradictions are exactly $R(D,E,u_D,u_E,z_D,z_E)$ for each bridge row.
\end{proof}

\begin{theorem}[Real constant appended-tail bridge full denominator-tail package]
\label{thm:real-constant-appended-tail-bridge-full-denominator-tail-package}
For histories $d,e,tail_D,tail_E,u_D,u_E,z_D,z_E:\Hist$ and observation maps
$r,q:\Hist\to\Hist$, set
$$
D=d\cdot\Eone(tail_D)
\qquad\text{and}\qquad
E=e\cdot\Eone(tail_E).
$$
Write the bridge classifier rows as
$$
A=\mathsf{RatHistoryClassifier}(D,E),
\qquad
B=\mathsf{RatStreamNameClassifier}(\mathsf{RatConstStream}_D\circ r,
\mathsf{RatConstStream}_E\circ q),
$$
$$
U=\mathsf{RealUnaryStreamClassifier}(\mathsf{RatConstStream}_D\circ r,
\mathsf{RatConstStream}_E\circ q),
\qquad
C=\mathsf{RealConstantHistoryClassifier}(\Eone(D),\Eone(E)).
$$
Let $P(D,E,z_D,z_E)$ be the appended-tail denominator package of
\autoref{thm:real-constant-appended-tail-bridge-denominator-package}. Let
$Q(D,E,u_D,u_E,z_D,z_E)$ denote $P(D,E,z_D,z_E)$ together with
\begin{enumerate}
  \item $\hsame(D,u_D\cdot\Ezero(z_D))\Longrightarrow\bot$;
  \item $\hsame(E,u_E\cdot\Ezero(z_E))\Longrightarrow\bot$;
  \item $\hsame(d,e)$.
\end{enumerate}
If $\hsame(tail_D,tail_E)$, then every bridge row entails this full
denominator-tail package:
$$
(A\Longrightarrow Q(D,E,u_D,u_E,z_D,z_E))\land
(B\Longrightarrow Q(D,E,u_D,u_E,z_D,z_E))\land
(U\Longrightarrow Q(D,E,u_D,u_E,z_D,z_E))\land
(C\Longrightarrow Q(D,E,u_D,u_E,z_D,z_E)).
$$
\end{theorem}
\begin{proof}
Assume $\hsame(tail_D,tail_E)$. Apply
\autoref{thm:real-constant-appended-tail-bridge-denominator-package}. For each
of the four bridge rows $A,B,U,C$, this supplies the package
$P(D,E,z_D,z_E)$.

Independently,
\autoref{thm:real-constant-appended-tail-bridge-appended-ezero-exclusion}
gives the two appended e-zero-tail exclusions for $D$ and $E$ under each bridge
row.

It remains to determine the matched heads. The final row of $P$ gives
$D\hsame E$. The tail hypothesis transports through the $\Eone$ constructor,
and append congruence rewrites $e\cdot\Eone(tail_E)$ as
$e\cdot\Eone(tail_D)$. Thus the endpoint comparison has a common right suffix
$\Eone(tail_D)$, so BEDC append right cancellation yields $\hsame(d,e)$.

These three consequences together with the already recovered package $P$ are
exactly $Q(D,E,u_D,u_E,z_D,z_E)$ for each bridge row.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstant\_appended\_tail\_bridge\_full\_denominator\_tail\_package}

\begin{theorem}[Real constant appended-tail bridge full denominator-tail package exactness]
\label{thm:real-constant-appended-tail-bridge-full-denominator-tail-package-exactness}
For histories $d,e,tail_D,tail_E,u_D,u_E,z_D,z_E:\Hist$ and observation maps
$r,q:\Hist\to\Hist$, use the same definitions of $D$, $E$, the bridge rows
$A,B,U,C$, and the full package $Q(D,E,u_D,u_E,z_D,z_E)$ as in
\autoref{thm:real-constant-appended-tail-bridge-full-denominator-tail-package}.
If $\hsame(tail_D,tail_E)$, then the full denominator-tail package is exact
for the bridge rows:
$$
Q(D,E,u_D,u_E,z_D,z_E)\Longleftrightarrow A\land B\land U\land C.
$$
\end{theorem}
\begin{proof}
Assume $Q(D,E,u_D,u_E,z_D,z_E)$. Project its
$P(D,E,z_D,z_E)$ component and apply
\autoref{thm:real-constant-appended-tail-bridge-denominator-package-exactness}.
This gives the four bridge rows $A\land B\land U\land C$.

Conversely assume $\hsame(tail_D,tail_E)$ and $A\land B\land U\land C$.
Apply
\autoref{thm:real-constant-appended-tail-bridge-full-denominator-tail-package}
under the tail agreement. The first projected implication has type
$A\Longrightarrow Q(D,E,u_D,u_E,z_D,z_E)$, and the projected row $A$ supplies
its premise. Hence the full package $Q(D,E,u_D,u_E,z_D,z_E)$ holds.
\end{proof}
\leanchecked{BEDC.Derived.RealUp.RealConstant\_appended\_tail\_bridge\_full\_denominator\_tail\_package\_exactness}

\begin{theorem}[Real constant appended-tail bridge single-row full denominator-tail exactness]
\label{thm:real-constant-appended-tail-bridge-single-row-full-denominator-tail-exactness}
For histories $d,e,tail_D,tail_E,u_D,u_E,z_D,z_E:\Hist$ and observation maps
$r,q:\Hist\to\Hist$, use the same definitions of $D$, $E$, the bridge rows
$A,B,U,C$, and the full package $Q(D,E,u_D,u_E,z_D,z_E)$ as in
\autoref{thm:real-constant-appended-tail-bridge-full-denominator-tail-package}.
If $\hsame(tail_D,tail_E)$, then each concrete bridge row is exact for the
full denominator-tail package:
$$
(A\Longleftrightarrow Q(D,E,u_D,u_E,z_D,z_E))\land
(B\Longleftrightarrow Q(D,E,u_D,u_E,z_D,z_E))\land
(U\Longleftrightarrow Q(D,E,u_D,u_E,z_D,z_E))\land
(C\Longleftrightarrow Q(D,E,u_D,u_E,z_D,z_E)).
$$
\end{theorem}
\begin{proof}
Assume $\hsame(tail_D,tail_E)$. The row-to-package directions are the four
projections of
\autoref{thm:real-constant-appended-tail-bridge-full-denominator-tail-package}
under this tail agreement.

For the package-to-row directions, assume
$Q(D,E,u_D,u_E,z_D,z_E)$. Apply
\autoref{thm:real-constant-appended-tail-bridge-full-denominator-tail-package-exactness}
under the same tail agreement. The resulting conjunction
$A\land B\land U\land C$ supplies the corresponding row by projection.

Combining the four row-to-package projections with these four projections from
the exactness conjunction gives the stated rowwise equivalences for
$A,B,U,C$.
\end{proof}

The bridge-readback certificate fit has ten fields: traditional target faithful
constant rational embedding into RealUp; BEDC source RatUp plus StreamNameUp
plus RealUp; carrier the appended-tail rational histories $D,E$ and the
independently reindexed constant streams; classifier rows $A,B,U,C$; stability
from constant streams ignoring $r$ and $q$; ledger the appended e-one
denominator tails $tail_D,tail_E$ together with heads $d,e$ and the appended
e-zero-tail exclusions; core theorem
\autoref{thm:real-constant-appended-tail-rational-stream-bridge-readback}
together with
\autoref{thm:real-constant-appended-tail-bridge-denominator-package},
\autoref{thm:real-constant-appended-tail-bridge-denominator-package-exactness},
and
\autoref{thm:real-constant-appended-tail-bridge-full-denominator-tail-package},
with exactness supplied by
\autoref{thm:real-constant-appended-tail-bridge-full-denominator-tail-package-exactness};
under tail agreement the equivalence
$Q(D,E,u_D,u_E,z_D,z_E)\Longleftrightarrow A\land B\land U\land C$ recovers
$R$, the denominator package $P(D,E,z_D,z_E)$, the appended e-zero-tail
exclusions, and the matched denominator heads from any bridge classifier row;
standard bridge RegSeqRat denominator-tail canonicalization; status
$\mathsf{ProofTarget}$.
