The $\RealSeparabilityUp$ hub consumes the Cauchy-equivalence setoid route of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} before dense real-name reads are public.

The dense schedule is read through $\CauchyEquivalenceSetoidUp$, $\RegSeqRatUp$, and $\RealUp$ rather than through quotient stream equality. The setoid route compares regular names by displayed finite classifier rows, the regular-rational route supplies the readback window, and the Real seal is consumed only after those rows are visible to the separability carrier.
