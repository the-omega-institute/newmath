\begin{theorem}[Real RegSeqRat StreamName lattice route]
\label{thm:real-regseqrat-streamname-lattice-route}
Every $\RealUp$ finite-window seal route used by Phase Real and Completion consumes a $\StreamNameUp$ finite window, a $\RegSeqRatUp$ readback, and a $\DyadicRatCoreUp$ tolerance ledger through the sibling chapters \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-dyadicratcore-namecert} before the local $\RealUp$ seal row is public. The route is a horizontal lattice edge through those sibling surfaces and the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows, not a quotient stream, selected limit, host sequence equality, or $\RealUp$ completeness principle.
\end{theorem}
\begin{proof}
Start with \autoref{thm:real-l10-open-phase-sibling-route}. It already places every public $\RealUp$ seal read in the open phase after the StreamName, RegSeqRat, and DyadicRatCore sibling surfaces.

Next apply \autoref{thm:real-streamname-regseqrat-source-lattice}. The lattice identifies the finite StreamName schedule, regular-rational readback, and dyadic tolerance ledger as the source rows that must be consumed before the local Real seal is exposed.

Use \autoref{thm:real-dyadic-stream-regseq-route} to align those rows into one route. The dyadic observations, StreamName finite window, RegSeqRat readback, and Real seal remain a single finite BEDC path rather than independent analytic witnesses.

Finally apply \autoref{thm:real-streamname-regseqrat-sibling-nonorphan-route}. It confirms that the Real route is read through the same sibling chapters and exports no quotient stream, selected host limit, host sequence equality, ambient completeness row, or direct seal bypass.
\end{proof}
