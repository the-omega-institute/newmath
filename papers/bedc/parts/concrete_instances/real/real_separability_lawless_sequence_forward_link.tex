\begin{theorem}[Real separability lawless-sequence forward link]
\label{thm:real-separability-lawless-sequence-forward-link}
Any $\RealSeparabilityUp$ density consumer that reuses lawless or Baire finite-prefix observations must pass through \autoref{ch:concrete-instances-lawlesssequence-namecert}, the $\StreamNameUp$ finite-window row, the dyadic tolerance row, the $\RegSeqRatUp$ readback row, and the terminal $\RealUp$ seal before it exports separability. For an accepted separability carrier
$$
S=(Q,D,W,R,E,T,H,C,P,N),
$$
the lawless or Baire observation is admitted only as a finite prefix read routed through $W$, tested against $D$, transported through $R$, and sealed by $E$.

The route refuses host infinite sequences, countable choice, quotient streams, selected dense points, and $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Project the accepted $\RealSeparabilityUp$ carrier. The density consumer has access only to the rational enumeration row $Q$, dyadic tolerance row $D$, finite StreamName window $W$, RegSeqRat readback $R$, terminal Real seal $E$, exactness ledger $T$, transport $H$, replay $C$, provenance $P$, and local naming $N$.

If the consumer cites a lawless or Baire finite-prefix observation, the citation first enters through \autoref{ch:concrete-instances-lawlesssequence-namecert}. That chapter exposes only finite StreamName windows, Bool digit rows, Nat prefix indices, transport, replay, provenance, and local naming; it does not export an infinite sequence or a countable-choice source.

Compose that finite-window scope with the separability density route. The cited prefix window is read as the separability row $W$, the requested precision is read through the dyadic row $D$, the observed rational data are transported through the RegSeqRat row $R$, and the terminal Real boundary is reached only at $E$.

The rows $T,H,C,P,N$ preserve the same route as exactness, componentwise transport, displayed replay, provenance, and local naming. Since neither packet contains host infinite sequences, countable choice, quotient streams, selected dense points, or ambient Real completeness, the composed density consumer cannot export any of them.
\end{proof}
