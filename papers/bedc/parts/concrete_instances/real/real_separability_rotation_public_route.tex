\begin{theorem}[RealSeparability rotation-selected public route]
\label{thm:real-separability-rotation-public-route}
Every $\RealSeparabilityUp$ public-route consumer selected by the rotation frontier must pass through $\CauchyEquivalenceSetoidUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, the countable-dense seal, transport, replay, provenance, and local naming before any separability read is exported. The route exposes the finite Cauchy classifier, dyadic tolerance, stream window, regular-rational readback, terminal Real seal, and countable dense support; it exports no quotient Real equality, selected dense point, countable-choice selector, or ambient completeness coordinate.
\end{theorem}
\begin{proof}
Read the hub route through \autoref{thm:real-separability-public-status-route}. Its public status row already orders the density request through the finite L10 source route before a public countable-dense read is available.

Next use the Cauchy-equivalence and countable-dense child routes. They force the consumer to display the $\CauchyEquivalenceSetoidUp$ classifier, dyadic tolerance, finite $\StreamNameUp$ windows, $\RegSeqRatUp$ readback, terminal $\RealUp$ seal, transport, replay, provenance, and local naming before the separability read is exported.

Since the route names only those displayed rows, the refused quotient equality, selected dense point, countable-choice selector, and ambient completeness coordinates have no public export path.
\end{proof}
