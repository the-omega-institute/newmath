\begin{theorem}[Real dyadic StreamName RegSeqRat route]
\label{thm:real-dyadic-stream-regseq-route}
Every accepted $\RealUp$ open-phase seal read whose source is presented to an
L10 consumer as a $\DyadicRatCoreUp$ observation ledger, a $\StreamNameUp$
finite-window row, a $\RegSeqRatUp$ regular readback row, and the local
$\RealUp$ seal factors through one ordered finite route:
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$
The route is transported only by the displayed $\BHist$, $\hsame$, $\Cont$,
$\Pkg$, and $\NameCert$ rows of those four surfaces.

No accepted read on this route exports quotient real equality, host limits,
countable choice, ambient completeness, an independent host completion
surface, or a direct local seal edge bypassing the dyadic, stream, and
regular-rational rows.
\end{theorem}
\begin{proof}
Begin with the L10 interface totality theorem
\autoref{thm:real-l10-dependency-lattice-interface-totality}. It already
places every accepted Phase Real or Completion interface read on exactly the
four visible faces of the Real L10 dependency lattice: DyadicRatCore
observations, StreamName finite-window rows, RegSeqRat regular-rational rows,
and the local Real seal.

Next apply the source lattice theorem
\autoref{thm:real-streamname-regseqrat-source-lattice}. It identifies the
StreamName, RegSeqRat, and DyadicRatCore handoff as one source lattice before
the Real seal is public, with only the displayed $\BHist$, $\hsame$, $\Cont$,
$\Pkg$, and $\NameCert$ coordinates available for transport and packaging.

Finally use terminal budget stability,
\autoref{thm:real-streamname-regseqrat-terminal-budget-stability}. Changing
the finite observation budget changes only which displayed finite rows are
queried, not the carrier of the route. Thus the open-phase seal read remains
on the single ordered finite route and has no coordinate for quotient real
equality, host limits, countable choice, ambient completeness, host
completion, or a direct seal bypass.
\end{proof}

\begin{theorem}[Real dyadic StreamName RegSeqRat budget exactness]
\label{thm:real-dyadic-stream-regseq-budget-exactness}
Let two accepted current Phase Real and Completion $\RealUp$ seal reads be
presented on the ordered route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$
Suppose their finite observation budgets expose the same displayed
$\DyadicRatCoreUp$ observations, $\StreamNameUp$ windows, $\RegSeqRatUp$
readbacks, local $\RealUp$ seal rows, and structural $\BHist$, $\hsame$,
$\Cont$, $\Pkg$, and $\NameCert$ rows on their overlap. Then replacing one
budget by the other, or refining either budget, changes only the displayed
finite request rows. The accepted read remains the same four-face route from
dyadic observations through finite stream windows and regular-rational
readback to the local Real seal.

In particular, this budget operation is not a quotient stream equality, host
real equality, selected limit, countable-choice selector, ambient
completeness principle, metric-completeness row, or direct $\RealUp$ seal
bypass.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-dyadic-stream-regseq-route}. Both accepted reads are
already placed on the ordered finite route from $\DyadicRatCoreUp$ through
$\StreamNameUp$ and $\RegSeqRatUp$ to $\RealUp$, and the only transport rows
available on the route are the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$,
and $\NameCert$ rows.

Next apply
\autoref{thm:real-l10-dependency-lattice-interface-totality}. It says that
the current Phase Real and Completion interface has exactly the four visible
faces listed in the statement. Since the two budgets agree on those faces and
on the structural rows over their overlap, every common exposed request row
projects to the same place in the L10 lattice.

Finally use
\autoref{thm:real-streamname-regseqrat-terminal-budget-stability}. Refining
or replacing a finite observation budget changes the finite row display but
does not add a new carrier, classifier, package row, or naming certificate.
Row projection therefore preserves the same four-face acceptance route.

The exclusion clause is the non-escape part of the same three inputs. None of
the displayed route, interface-totality, or terminal-budget rows contains a
coordinate for quotient stream equality, host real equality, selected limits,
countable choice, ambient completeness, metric completeness, or a direct Real
seal bypass, so a budget change cannot certify any of those routes.
\end{proof}

\begin{theorem}[Real dyadic StreamName RegSeqRat seal equivalence]
\label{thm:real-dyadic-stream-regseq-seal-equivalence}
For a current Phase Real and Completion $\RealUp$ seal read, agreement of the
displayed $\DyadicRatCoreUp$ observation rows, $\StreamNameUp$ finite-window
rows, $\RegSeqRatUp$ regular readback rows, and local $\RealUp$ seal row,
together with the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and
$\NameCert$ coordinates, is equivalent to acceptance of the ordered four-face
route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$

This equivalence is internal to the finite dyadic-stream-regseq seal route. It
does not assert quotient stream equality, host real equality, a selected
limit, countable choice, metric completeness, ambient completion coordinates,
or any direct seal edge outside the four displayed faces.
\end{theorem}
\begin{proof}
For the forward direction, start from agreement of the displayed dyadic,
finite-window, regular-readback, and local seal rows, with the displayed
$\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ coordinates fixed. Apply
\autoref{thm:real-l10-dependency-lattice-interface-totality} to restrict the
current Phase Real and Completion read to the four visible L10 faces.

Next use \autoref{thm:real-streamname-regseqrat-source-lattice}. It identifies
the DyadicRatCore, StreamName, and RegSeqRat rows as the source lattice
available before the local Real seal is accepted. The row agreement therefore
has exactly the finite source data needed by the seal route.

Apply \autoref{thm:real-dyadic-stream-regseq-route}. The matched source and
seal rows are repacked as the ordered route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp .
$$

For the reverse direction, assume acceptance of the ordered four-face route.
The route theorem exposes precisely the dyadic observations, finite stream
windows, regular-rational readback rows, local seal row, and structural
transport and package coordinates named in the statement.

Finally apply
\autoref{thm:real-dyadic-stream-regseq-budget-exactness}. It says that
replacing or refining the finite budget changes only the displayed request
rows, so agreement on those rows is exactly the classifier agreement carried
by the accepted finite dyadic-stream-regseq seal route.

The exclusion clause follows because the four cited inputs have no coordinate
for quotient stream equality, host real equality, selected limits, countable
choice, metric completeness, ambient completion, or a direct $\RealUp$ seal
bypass.
\end{proof}

\begin{theorem}[Real Cauchy window transducer seal route]
\label{thm:real-cauchy-window-transducer-seal-route}
Any $\RealUp$ seal read exported by an accepted $\CauchyWindowTransducerUp$ packet factors through $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and then $\RealUp$. The transducer rows $S,D,W,R,E,L,H,C,P,N$ provide only finite route replay, and they provide no direct seal bypass.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-dyadic-stream-regseq-route}. It places the relevant Real-facing read on the ordered route from DyadicRatCore observations through StreamName finite windows and RegSeqRat regular readback to the Real seal.

Use \autoref{thm:cauchy-window-transducer-regseq-handoff}. In an accepted Cauchy window transducer packet, the seal row $E$ is public only after the RegSeqRat readback row $R$ has been displayed.

Finally apply \autoref{thm:cauchy-window-transducer-real-seal-nonescape}. The remaining rows $S,D,W,L,H,C,P,N$ replay the finite route and bind transport, provenance, and local naming. They do not add a quotient Real equality, host limit, countable-choice row, ambient completeness row, or direct edge to the seal.
\end{proof}

\begin{theorem}[Real StreamName RegSeqRat sibling nonorphan route]
\label{thm:real-streamname-regseqrat-sibling-nonorphan-route}
The Real seal route consumes the sibling chapter surfaces \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-cauchy-window-transducer-namecert} before the local $\RealUp$ seal is public. Thus the local Real route is a horizontal L10 dependency through dyadic observations, finite stream windows, regular-rational readback, and the Cauchy window transducer, not a self-reference and not a host-completion shortcut.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-dyadic-stream-regseq-route}. It places the Real-facing route on the ordered dyadic, StreamName, RegSeqRat, and Real seal surfaces, with only displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ coordinates available for transport.

Then apply \autoref{thm:real-cauchy-window-transducer-seal-route}. The Cauchy window transducer seal read factors through the same dyadic, stream, and regular-rational route before the local Real seal is exposed.

These two routes cite sibling chapter surfaces rather than the local Real chapter as their source. The dependency is therefore horizontal in the L10 lattice and cannot be replaced by a Real self-reference, selected host limit, ambient completion object, or direct seal bypass.
\end{proof}

\begin{theorem}[Real terminal four-face open-phase exit]
\label{thm:real-terminal-four-face-open-phase-exit}
Every current Phase Real and Completion terminal $\RealUp$ seal read is
accepted exactly when its visible rows factor through the ordered four-face
route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp
$$
displayed by the Real L10 dependency lattice. The visible coordinates of the
exit read are only the displayed $\BHist$ packet, the displayed $\hsame$
transports, the displayed $\Cont$ routes, the displayed $\Pkg$ provenance
rows, and the displayed $\NameCert$ rows on those four faces.

Thus no terminal exit read is accepted by quotient stream equality, countable
choice, host limits, metric completeness, ambient completion, host real
equality, or any direct edge to the $\RealUp$ seal outside the displayed
four-face route.
\end{theorem}
\begin{proof}
Suppose first that a current Phase Real or Completion terminal $\RealUp$ seal
read is accepted. Apply \autoref{thm:real-dyadic-stream-regseq-route}. It
places the accepted seal read on the ordered dyadic, StreamName, RegSeqRat,
and Real route and restricts transport to the displayed $\BHist$, $\hsame$,
$\Cont$, $\Pkg$, and $\NameCert$ coordinates.

Next apply
\autoref{thm:real-streamname-regseqrat-sibling-nonorphan-route}. The route is
therefore sourced by the sibling DyadicRatCore, StreamName, RegSeqRat, and
Cauchy-window-transducer surfaces before the local Real seal is public. This
rules out a local self-reference or a host-completion shortcut as the source
of acceptance.

Now read the same terminal packet through the Real L10 dependency lattice,
using \autoref{def:real-l10-dependency-lattice} and
\autoref{thm:real-l10-dependency-lattice-interface-totality}. The lattice
gives exactly the four displayed faces, and interface totality says that
current Phase Real and Completion reads have no fifth visible coordinate.

Conversely, suppose that the visible rows of a terminal read factor through
that ordered four-face route with only the displayed $\BHist$, $\hsame$,
$\Cont$, $\Pkg$, and $\NameCert$ coordinates. The route theorem supplies the
accepted Real seal handoff, and the sibling nonorphan route supplies the
horizontal source through the dyadic, stream, regular-rational, and
transducer surfaces. The Real L10 interface totality theorem then repacks
that handoff as the current Phase Real and Completion terminal surface.

The exclusion clause follows from the same three inputs. None of the route,
sibling, or L10 lattice rows exports quotient stream equality, countable
choice, host limits, metric completeness, ambient completion, host real
equality, or a direct seal bypass, so none of those data can certify a
terminal exit read.
\end{proof}

\begin{theorem}[Real completeness four-face exit certificate]
\label{thm:real-completeness-four-face-exit-certificate}
A completion-facing public read of the current $\RealUp$ seal is accepted if
and only if its visible rows factor through the four-face route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp
$$
using exactly the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and
$\NameCert$ rows on that route.

Any purported completion-facing read that uses quotient stream equality,
countable choice, host limits, metric completeness, ambient completion, host
real equality, or a direct $\RealUp$ seal bypass is rejected by the current
Phase Real and Completion surface.
\end{theorem}
\begin{proof}
For the forward direction, apply
\autoref{thm:real-terminal-four-face-open-phase-exit}. It sends every accepted
terminal $\RealUp$ seal read to the ordered four-face route and restricts the
visible coordinates to the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and
$\NameCert$ rows.

For the converse direction, use the same theorem in the reverse direction:
visible rows on the ordered four-face route, with no extra coordinate, repack
as the current Phase Real and Completion terminal surface.

Now apply \autoref{thm:real-dyadic-stream-regseq-route}. It identifies the
route as the finite handoff from DyadicRatCore observations through
StreamName finite windows and RegSeqRat regular readback to the local
$\RealUp$ seal. Apply
\autoref{thm:real-streamname-regseqrat-sibling-nonorphan-route} to record
that the handoff is horizontal through the sibling surfaces, not a local Real
self-reference and not a host-completion shortcut.

Finally use
\autoref{thm:real-l10-dependency-lattice-interface-totality}. The Real L10
interface has no fifth public coordinate beyond the displayed faces. Hence a
purported read using quotient stream equality, countable choice, host limits,
metric completeness, ambient completion, host real equality, or a direct Real
seal bypass has no accepting coordinate on the current Phase Real and
Completion surface.
\end{proof}

\begin{theorem}[Real four-face terminal cover pullback]
\label{thm:real-four-face-terminal-cover-pullback}
Let a finite family of current Phase Real and Completion terminal
$\RealUp$ seal reads be displayed over one terminal cover. Suppose that on
every pairwise overlap the visible rows agree on the $\DyadicRatCoreUp$
observation rows, the $\StreamNameUp$ finite-window rows, the
$\RegSeqRatUp$ regular-rational classifier rows, and the local $\RealUp$ seal
rows, together with the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and
$\NameCert$ coordinates. Assume also that on every triple overlap the two
restriction routes from each local read to the common section coincide:
$$
(\mathcal R_i|_{ij})|_{ijk}=(\mathcal R_i|_{ik})|_{ijk},
\qquad
(\mathcal R_i|_{ij})|_{ijk}=(\mathcal R_j|_{ij})|_{ijk}.
$$
Then the whole family pulls back to one ordered terminal four-face route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp.
$$
The resulting terminal surface has no coordinate for quotient stream equality,
host real equality, countable choice, ambient completion, metric-completeness
rows, or a direct $\RealUp$ seal bypass.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-completeness-four-face-exit-certificate} to each
member of the finite terminal cover. Each accepted seal read is therefore
already presented by the ordered dyadic, stream, regular-rational, and local
Real faces, with only the displayed structural rows available.

The pairwise overlap agreement and the triple-overlap route coherence are the
cocycle data required by
\autoref{thm:real-terminal-seal-finite-cover-cocycle-exactness}. Applying that
theorem pastes the finite family to a unique public $\RealUp$ terminal-seal
read whose restrictions are the displayed local reads. Reading this pasted seal
through the preceding normal-form certificate presents it as the ordered
terminal four-face route
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp.
$$

Finally apply
\autoref{thm:real-l10-dependency-lattice-interface-totality}. The dependency
lattice supplies no fifth route beside the four displayed faces, so the cover
repackages as one terminal four-face surface and excludes quotient stream
equality, host real equality, countable choice, ambient completion,
metric-completeness rows, and direct seal bypasses.
\end{proof}

\begin{theorem}[Real four-face terminal cover overlap compatibility]
\label{thm:real-four-face-terminal-cover-overlap-compatibility}
Let $C_0$ and $C_1$ be two finite families of current Phase Real and
Completion terminal $\RealUp$ seal reads. Suppose each family satisfies the
hypotheses of
\autoref{thm:real-four-face-terminal-cover-pullback}. Suppose further that on
a shared subcover the two families agree on the displayed $\DyadicRatCoreUp$
observation rows, the $\StreamNameUp$ finite-window rows, the
$\RegSeqRatUp$ regular-rational classifier rows, the local $\RealUp$ seal
rows, and the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$
support coordinates. Then the two pulled-back terminal four-face routes have
the same public $\RealUp$ seal classifier surface on that shared subcover.

This compatibility is only a finite four-face statement. It does not identify
quotient streams, host reals, selected limits, countable-choice witnesses,
ambient completions, metric-completeness witnesses, or any direct bypass of
the local $\RealUp$ seal.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-four-face-terminal-cover-pullback} to $C_0$ and
$C_1$. This presents both terminal covers over ordered routes
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp
$$
with only the displayed structural rows available.

On the shared subcover, use
\autoref{thm:streamname-regseqrat-finite-window-cover-determinacy}. Agreement
of the $\DyadicRatCoreUp$ observation rows and the $\StreamNameUp$
finite-window rows identifies the same finite-window source for both
pulled-back routes.

Next transport the classifier surface through the shared rows. Apply
\autoref{thm:real-dyadic-stream-regseq-seal-equivalence} to pass between the
dyadic, stream, regular-rational, and local Real seal presentations, and
apply \autoref{thm:real-dyadic-stream-regseq-budget-exactness} to keep the
$\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ coordinates exact on the
same subcover.

Finally apply
\autoref{thm:real-l10-dependency-lattice-interface-totality}. Since the
current Phase Real and Completion interface has no fifth public coordinate,
there is no additional route through quotient stream equality, host real
equality, selected limits, countable choice, ambient completion,
metric-completeness witnesses, or a direct Real seal bypass. The transported
classifier agreement is therefore exactly equality of the public $\RealUp$
seal classifier surface on the shared subcover.
\end{proof}

\begin{theorem}[Real four-face terminal cover classifier finality]
\label{thm:real-four-face-terminal-cover-classifier-finality}
Let $C_0$ be a finite terminal cover of current Phase Real and Completion
$\RealUp$ seal reads satisfying the hypotheses of
\autoref{thm:real-four-face-terminal-cover-pullback}. Let $C_1$ be a second
finite terminal presentation of the same terminal read support. Suppose that
$C_1$ also satisfies those hypotheses, and that $C_0$ and $C_1$ agree on the
$\DyadicRatCoreUp$ observation rows, the $\StreamNameUp$ finite-window rows,
the $\RegSeqRatUp$ classifier and readback rows, the local $\RealUp$ seal
rows, and the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$
support coordinates. Then $C_0$ and $C_1$ determine one and the same public
$\RealUp$ seal classifier surface.

This finality is only over the terminal four-face cover
$$
\DyadicRatCoreUp \to \StreamNameUp \to \RegSeqRatUp \to \RealUp.
$$
It excludes quotient stream equality, host real equality, selected limits,
countable choice, ambient completion, metric-completeness rows, and any
direct $\RealUp$ seal bypass from the data that can affect the public
classifier surface.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-four-face-terminal-cover-pullback} separately to
$C_0$ and $C_1$. The two presentations therefore pull back to terminal
four-face routes with the same displayed dyadic, stream, regular-rational,
local Real, and structural support rows.

On every common part of the cover, use
\autoref{thm:real-four-face-terminal-cover-overlap-compatibility}. The
agreement of the DyadicRatCore observations, StreamName windows, RegSeqRat
classifier and readback rows, local Real seal rows, and displayed support
coordinates identifies the public $\RealUp$ seal classifier surface on each
overlap.

Next pass through the four named faces. Apply
\autoref{thm:real-dyadic-stream-regseq-seal-equivalence} to identify the
classifier surface obtained from the dyadic, stream, regular-rational, and
local Real presentations.

Finally apply
\autoref{thm:real-l10-dependency-lattice-interface-totality}. The current
Phase Real and Completion dependency lattice exposes no fifth public
coordinate, so quotient stream equality, host real equality, selected limits,
countable choice, ambient completion, metric-completeness rows, and direct
Real seal bypasses have no classifier input. The overlap-compatible
four-face surface is therefore the unique public $\RealUp$ seal classifier
surface determined by the two presentations.
\end{proof}
