The forward route from $\RealSeparabilityUp$ to completion-facing density data is finite: a separability consumer first reads the regular rational handoff of \autoref{ch:concrete-instances-regseqrat-namecert}, then the source-to-seal discipline of \autoref{ch:concrete-instances-regular-cauchy-dense-embedding-namecert}, and only then the dense-image packet of \autoref{ch:concrete-instances-dense-embedding-namecert}.

\begin{theorem}[Real separability RegSeqRat forward route]
\label{thm:real-separability-regseqrat-forward-route}
Every completion-facing consumer of an accepted $\RealSeparabilityUp$ packet that reuses dense-embedding data factors through the displayed $\RegSeqRatUp$ readback row, the finite $\RegularCauchyDenseEmbeddingUp$ source-to-seal route, and the $\DenseEmbeddingUp$ image-window ledger before it exports a dense source to completion, finite-net, compact-metric, or $\CountableDenseSubsetUp$ consumers. The route supplies no host dense subset, selected nearest point, quotient real equality, countable choice, or ambient $\RealUp$ completeness coordinate.
\end{theorem}
\begin{proof}
Start with the RegSeqRat dependency of \autoref{thm:real-separability-regseqrat-dependency}. It places the rational candidate, opened StreamName window, dyadic tolerance row, and regular readback before any Real-facing separability export.

Next read the regular Cauchy dense-embedding boundary of \autoref{ch:concrete-instances-regular-cauchy-dense-embedding-namecert}. Its source-to-seal route keeps the same $\RegSeqRatUp$ row visible while the $\RealUp$ seal is reached.

Finally pass that visible source row through the dense-embedding packet of \autoref{ch:concrete-instances-dense-embedding-namecert}. The image-window and approximation ledger are then the only completion-facing dense data available to downstream finite-net, compact-metric, and countable-density consumers.

Because each stage exposes only displayed rows and none carries a host dense subset, selected nearest point, quotient real equality, countable-choice schedule, or ambient completeness witness, the composed forward route exports no such coordinate.
\end{proof}
