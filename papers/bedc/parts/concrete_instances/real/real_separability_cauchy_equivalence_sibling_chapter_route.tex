Every $\RealSeparabilityUp$ finite-density read that compares two regular real names first consumes the $\CauchyEquivalenceSetoidUp$ classifier surface of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} before the density row is exported. The route displays the $\RegSeqRatUp$ readback windows, the $\DyadicRatCoreUp$ tolerance ledger, the terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows before any separability consumer reads density; it exports no quotient real equality, selected dense enumeration, countable choice, host equality, or ambient completeness.
