\begin{theorem}[Real separability Cauchy-setoid dependency]
\label{thm:real-separability-cauchy-setoid-dependency}
The $\RealSeparabilityUp$ finite dense-read surface consumes the $\CauchyEquivalenceSetoidUp$ classifier route of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} and the regular rational readback route of \autoref{ch:concrete-instances-regseqrat-namecert} before any countable dense $\RealUp$ read is exported. Thus the separability packet may expose finite dense witnesses only after the setoid comparison, regular-rational readback, tolerance ledger, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows are visible.
\end{theorem}
\begin{proof}
Read the dependency route in order. The Cauchy-equivalence setoid fixes when two regular Cauchy names may be treated as the same dense-read source, and the $\RegSeqRatUp$ row supplies the finite rational windows that the separability packet later counts.

Only after those two sibling routes are visible may the $\RealSeparabilityUp$ packet expose its countable dense Real readback. The route therefore exports no quotient real equality, selected dense enumeration, countable choice, or ambient completeness theorem.
\end{proof}
