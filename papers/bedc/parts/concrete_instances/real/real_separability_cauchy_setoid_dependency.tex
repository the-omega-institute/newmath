The $\RealSeparabilityUp$ finite dense-read surface consumes the $\CauchyEquivalenceSetoidUp$ classifier route of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert} before any density window is exported as a Real-facing read. This dependency keeps equivalent Cauchy names on the same displayed separability route: the separability packet may expose finite dense witnesses only after the setoid comparison, regular-rational readback, tolerance ledger, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ rows are visible, and it exports no quotient real equality, selected dense enumeration, countable choice, or ambient completeness theorem.
