The $\RealSeparabilityUp$ packet supplies the dense dyadic window route consumed by $\MetricCompletionUp$ in \autoref{ch:concrete-instances-metriccompletion-namecert} and by the Cauchy comparison surface of \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert}. A completion-facing read first observes the existing $\RealUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ route, then uses those finite dyadic windows as the separability source for regular readbacks and Cauchy-equivalence comparisons.

The ledger edge is theorem-free: Real separability is used only as a displayed dense-window supplier for metric completion and Cauchy equivalence consumers, not as a host countable dense subset, selected enumeration, quotient real equality, or ambient completeness principle.
