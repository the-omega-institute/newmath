The countable dense $\RealSeparabilityUp$ rows are consumed by the
$\PolishSpaceUp$ root surface of
\autoref{ch:concrete-instances-polishspace-namecert} only after the
$\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ readbacks are displayed. This
route supplies a finite consumer edge from real separability to the Polish
complete-separable package, but it does not provide selected dense points,
quotient equality, or ambient completeness.
