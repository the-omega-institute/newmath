\begin{proposition}[RealSeparability metric-completion reverse route]
\label{prop:real-separability-metric-completion-reverse-route}
Every $\MetricCompletionUp$ or $\CauchyCompletionReflectorUp$ consumer that uses $\RealSeparabilityUp$ dense data first reads the displayed $\RatUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows. After those rows are exposed, the consumer may pass through \autoref{ch:concrete-instances-metriccompletion-namecert} and \autoref{ch:concrete-instances-cauchycompletionreflector-namecert} as a finite completion-facing route. The route exports no selected dense points, quotient $\RealUp$ equality, countable choice, or ambient $\RealUp$ completeness.
\end{proposition}
\begin{proof}
Read the rational and dyadic rows from the separability dependency route before any completion-facing consumer receives dense data. The $\RatUp$ and $\DyadicRatCoreUp$ rows identify the finite rational windows, while the $\StreamNameUp$, $\RegSeqRatUp$, and $\RealUp$ rows keep the readback inside the displayed real-name corridor.

Then pass the displayed data through the metric-completion and reflector chapters named in the statement. Since the route has already exposed all dense rows and has no coordinate for selecting points, quotienting real names, invoking countable choice, or importing ambient completeness, every completion-facing use remains a finite BEDC route.
\end{proof}
