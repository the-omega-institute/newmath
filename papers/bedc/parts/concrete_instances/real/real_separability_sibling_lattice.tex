Every $\RealSeparabilityUp$ density read is consumed through the sibling lattice $\DyadicRatCoreUp \longrightarrow \StreamNameUp \longrightarrow \RegSeqRatUp \longrightarrow \RealUp$, with substantive producer and consumer rows supplied by $\CountableDenseSubsetUp$ in \autoref{ch:concrete-instances-countable-dense-subset-namecert}, $\DyadicDensityRealUp$ in \autoref{ch:concrete-instances-dyadic-density-real-namecert}, and $\RationalDensityRealUp$ in \autoref{ch:concrete-instances-rational-density-real-namecert}.
The $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert} is the only public exit, the finite windows are those of \autoref{ch:concrete-instances-streamname-namecert}, regular readback is the route of \autoref{ch:concrete-instances-regseqrat-namecert}, and the tolerance ledger is the dyadic surface of \autoref{ch:concrete-instances-dyadicratcore-namecert}; thus a separability consumer reads density data as finite rational-window evidence before it reaches the real seal, with no host equality, selected dense point, quotient stream, or ambient completeness row.
