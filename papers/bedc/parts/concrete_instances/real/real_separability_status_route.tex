The hub status route for $\RealSeparabilityUp$ is the child closurestatus block in \autoref{thm:real-separability-seed-closure-status-block}, whose scoped surface is recorded inside the routed child file before the hub exposes the Real and completion dependency lattice.
