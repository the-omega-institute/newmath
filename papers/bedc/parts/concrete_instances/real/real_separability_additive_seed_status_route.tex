\begin{theorem}[Real separability additive seed-status route]
\label{thm:real-separability-additive-seed-status-route}
Every $\RealSeparabilityUp$ seed-status consumer reads the displayed child route
$$
\CauchyEquivalenceSetoidUp,\quad
\DyadicRatCoreUp,\quad
\StreamNameUp,\quad
\RegSeqRatUp,\quad
\RealUp,\quad
\RegularCauchyDenseEmbeddingUp
$$
before it treats the separability packet as a dense Real source. The Cauchy-equivalence classifier is read from \autoref{ch:concrete-instances-cauchy-equivalence-setoid-namecert}; dyadic tolerance, stream-window, regular-rational, and terminal Real rows are read through \autoref{ch:concrete-instances-dyadicratcore-namecert}, \autoref{ch:concrete-instances-streamname-namecert}, \autoref{ch:concrete-instances-regseqrat-namecert}, and \autoref{ch:concrete-instances-real-namecert}; and completion-facing density is handed to \autoref{ch:concrete-instances-regular-cauchy-dense-embedding-namecert}. The route exports no quotient Real equality, selected dense enumeration, countable-choice schedule, host dense subset, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with the seed-status surface of \autoref{thm:real-separability-seed-status}. It identifies $\RealSeparabilityUp$ as a child-route family, so the hub itself contributes no theorem body and no independent density object.

Read the classifier and approximation rows next. The Cauchy-equivalence setoid row fixes the comparison boundary, while the dyadic, StreamName, RegSeqRat, and Real rows display the finite tolerance, observation, readback, and seal route that a dense-name consumer must traverse.

Finally pass through the regular-Cauchy dense embedding boundary. This keeps completion-facing density attached to the same finite seed route, leaving no coordinate for quotient Real equality, selected enumeration, choice, host dense subsets, or ambient completeness.
\end{proof}
