The $\RealSeparabilityUp$ route consumes the Cauchy readback boundary of \autoref{ch:concrete-instances-cauchy-namecert} and the completion universal-property boundary of \autoref{ch:concrete-instances-metric-completion-universal-property-namecert} before exporting dense countable real reads. This pins the separability packet to its Cauchy-window source and completion-facing universal route, rather than letting the hub appear as a kernel-only index.
