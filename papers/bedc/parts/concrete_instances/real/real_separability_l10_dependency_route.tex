Every $\RealSeparabilityUp$ density witness reads through finite $\StreamNameUp$ windows before it reaches any Real-facing separability row. The route first opens the displayed window of \autoref{ch:concrete-instances-streamname-namecert}, then transports the inspected rational data through the $\RegSeqRatUp$ readback of \autoref{ch:concrete-instances-regseqrat-namecert}, checks the tolerance against the $\DyadicRatCoreUp$ rows of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and only then consumes the terminal $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert}. Thus the separability packet exports a finite density route, not a detached dense subset, selected nearest point, quotient real equality, countable choice, or ambient completeness.
