$\RealSeparabilityUp$ consumes the $\RealUp$ seal of \autoref{ch:concrete-instances-real-namecert} through the $\RegSeqRatUp$ and $\StreamNameUp$ routes of \autoref{ch:concrete-instances-regseqrat-namecert} and \autoref{ch:concrete-instances-streamname-namecert}; its displayed dense source is the finite rational row family supplied by $\DyadicRatCoreUp$ and $\RatUp$, with \autoref{ch:concrete-instances-dyadicratcore-namecert} fixing the dyadic tolerance ledger used by each L10 read. The same dense dyadic and rational source supplies downstream $\PseudometricUp$ and $\MetricCompletionUp$ distance-read routes through \autoref{ch:concrete-instances-pseudometric-namecert} and \autoref{ch:concrete-instances-metriccompletion-namecert}.
