\begin{theorem}[Real terminal seal sibling route]
\label{thm:real-terminal-seal-sibling-route}
Every public $\RealUp$ terminal seal read factors through the finite
$\StreamNameUp$ window, the $\RegSeqRatUp$ readback, and the
$\DyadicRatCoreUp$ tolerance ledger before it reaches the local Real seal.
The route is the cross-chapter certificate route through
\autoref{ch:concrete-instances-streamname-namecert},
\autoref{ch:concrete-instances-regseqrat-namecert}, and
\autoref{ch:concrete-instances-dyadicratcore-namecert}; its transport data
are only the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$
rows already exposed by those sibling chapters.

Consequently an accepted terminal seal read has no route that bypasses the
finite StreamName window, RegSeqRat readback, or DyadicRatCore tolerance
ledger. In particular it exports no host real equality, quotient-stream
equality, ambient completeness principle, countable-choice selector, selected
limit, or independent host-completion row.
\end{theorem}
\begin{proof}
Start with the handoff theorem
\autoref{thm:realup-regseqrat-streamname-seal-handoff}. It reads the public
$\RealUp$ seal as a package whose visible rows are the StreamName classifier
window, the RegSeqRat readback surface, the dyadic observation ledger, and
the local Real seal provenance. The sibling chapter references in the
statement are exactly the public sources of those rows.

Next apply the dyadic StreamName RegSeqRat route theorem
\autoref{thm:real-dyadic-stream-regseq-route}. It orders the exposed rows as
one finite BEDC path from $\DyadicRatCoreUp$ observations through the
$\StreamNameUp$ finite window and $\RegSeqRatUp$ readback before the
$\RealUp$ seal is read. Since the route carries only $\BHist$, $\hsame$,
$\Cont$, $\Pkg$, and $\NameCert$ coordinates, it has no coordinate for a host
completion or quotient-stream equality.

Finally use the finite-window seal tightness theorem
\autoref{thm:real-regseqrat-streamname-tightness}. Tightness says that an
accepted terminal seal is admissible exactly when the three sibling rows
agree on one common finite request. Any proposed route omitting one of those
rows therefore fails to be an accepted public $\RealUp$ terminal seal, and
any route adding host real equality, ambient completeness, countable choice,
or a selected limit lies outside the displayed certificate surface.
\end{proof}

\begin{theorem}[Real terminal seal four-face status pullback]
\label{thm:real-terminal-seal-four-face-status-pullback}
Every public $\RealUp$ terminal seal read whose route factors through
$$
\StreamNameUp,\quad \RegSeqRatUp,\quad \DyadicRatCoreUp,\quad \RealUp
$$
reads its audit and status data exactly as the four objectwise closurestatus
records of those faces. The $\StreamNameUp$, $\RegSeqRatUp$,
$\DyadicRatCoreUp$, and local $\RealUp$ records keep their own closurestatus,
formalstatus, leantarget, and bridgestatus rows; the terminal seal route
creates no route-level $\formalstatus$ and no aggregate bridge row.

Conversely, any terminal seal read carrying those four face records together
with the displayed
$$
\BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert
$$
rows factors through the same terminal seal sibling route and through the same
four-face terminal pullback boundary. This equivalence does not assert
quotient stream equality, selected limits, countable choice,
metric-completeness data, or any $\RealUp$ completeness claim.
\end{theorem}
\begin{proof}
First locate the route. Apply
\autoref{thm:real-terminal-seal-sibling-route}. The public terminal seal read
is confined to the StreamName window, RegSeqRat readback, DyadicRatCore
tolerance ledger, and local Real seal row, with only the displayed $\BHist$,
$\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

Next read the status data objectwise. Apply
\autoref{thm:real-l10-four-face-status-lookup} and
\autoref{thm:real-l10-current-phase-exit-audit-pullback}. These identify the
audit boundary with the product of the four local closurestatus records, while
leaving each closurestatus, formalstatus, leantarget, and bridgestatus entry
attached to its own face.

For the converse, start with a terminal seal read carrying exactly those four
face records and the displayed support rows. Apply
\autoref{thm:real-l10-terminal-pullback-coherence}. The objectwise status
threshold and the shared StreamName, RegSeqRat, DyadicRatCore, Real,
$\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ coordinates repack the
read as the same terminal pullback boundary.

The exclusion clause follows from the same three cited results. None of their
routes contains a coordinate for a route-level $\formalstatus$, aggregate
bridge row, quotient stream equality, selected limit, countable-choice row,
metric-completeness datum, or $\RealUp$ completeness claim.
\end{proof}

\begin{theorem}[Real terminal seal four-face cover normal form]
\label{thm:real-terminal-seal-four-face-cover-normal-form}
Let $\{\mathcal R_i\}_{i\in I}$ be a finite family of public $\RealUp$
terminal seal reads. Suppose each local route exposes the same four faces
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp,
$$
where the $\StreamNameUp$ finite windows agree pairwise on their restrictions,
the $\DyadicRatCoreUp$ endpoint, radius, and distance ledgers agree on every
overlap, the $\RegSeqRatUp$ classifier and regularity rows agree on the same
finite request sections, the local $\RealUp$ seal-source rows agree on the
common boundary, and all routes use the shared support rows
$$
\BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert.
$$

Then the family descends to one finite-cover normal-form packet for the public
$\RealUp$ terminal seal. Conversely, any finite-cover normal-form packet with
exactly those four faces and support rows restricts to the local terminal seal
routes $\mathcal R_i$. The packet exports no quotient stream equality, host
real equality, selected limit, countable choice, ambient completeness,
metric-completeness datum, or standard bridge row.
\end{theorem}
\begin{proof}
First read every local seal through the sibling route of
\autoref{thm:real-terminal-seal-sibling-route}. Each $\mathcal R_i$ is thereby
placed on the StreamName finite window, the RegSeqRat readback face, the
DyadicRatCore tolerance ledger, and the local Real seal-source row, with only
the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

Next build the finite-cover surface. Apply
\autoref{thm:streamname-terminal-fusion-finite-cover-cocycle-exactness} to the
pairwise StreamName window agreements. The common terminal fused packet is
then transported through the RegSeqRat finite-request section supplied by
\autoref{thm:regseqrat-finite-request-terminal-budget-factorization}.

Now transport the dyadic and Real boundary faces. Use
\autoref{thm:dyadicratcore-regseqrat-real-pullback-composition} to identify
the endpoint, radius, distance, classifier, regularity, readback, and
seal-source rows as one pullback surface over the same finite requests.

For the descent direction, apply
\autoref{thm:real-finite-cover-seal-pasting-criterion}. The pasted
StreamName surface, the shared dyadic ledger, the RegSeqRat classifier and
regularity rows, and the local Real seal-source rows therefore form one
finite-cover normal-form packet for the terminal seal.

For the restriction direction, start with such a normal-form packet and
restrict it to any window of the finite cover. The same pasting criterion and
the sibling route read the restricted packet back as the local terminal seal
route $\mathcal R_i$, because no additional coordinate is present beyond the
four displayed faces and the shared support rows.

The exclusion clause is inherited from the cited routes. None of them exports
quotient stream equality, host real equality, selected limits, countable
choice, ambient completeness, metric-completeness data, or a standard bridge
row, so those data cannot be recovered from either direction of the normal
form.
\end{proof}

\begin{theorem}[Real terminal seal four-face cover descent determinacy]
\label{thm:real-terminal-seal-four-face-cover-descent-determinacy}
Let $\mathcal P$ and $\mathcal Q$ be finite-cover normal-form packets for the
public $\RealUp$ terminal seal. Suppose both packets expose exactly the four
faces
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp,
$$
agree on all overlap restrictions for those faces, and use only the displayed
support rows
$$
\BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert.
$$

Then $\mathcal P$ and $\mathcal Q$ determine the same public $\RealUp$
terminal seal read. Conversely, equality of the public terminal seal read
recovers exactly the common $\StreamNameUp$ finite window, the
$\DyadicRatCoreUp$ endpoint-radius-distance ledger, the $\RegSeqRatUp$
finite-request classifier and regularity rows, and the local $\RealUp$
seal-source row. It recovers no quotient-stream coordinate, host-real
coordinate, selected limit, countable-choice selector, ambient-completeness
principle, metric-completeness datum, or standard-bridge coordinate.
\end{theorem}
\begin{proof}
Read both packets through the sibling route of
\autoref{thm:real-terminal-seal-sibling-route}. This places $\mathcal P$ and
$\mathcal Q$ on the same four public faces and confirms that their support
surface is exactly the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and
$\NameCert$ surface.

Apply the normal-form theorem
\autoref{thm:real-terminal-seal-four-face-cover-normal-form} to both packets.
The hypotheses on overlap restrictions put the two packets on one
finite-cover surface, because the StreamName windows, dyadic tolerance rows,
RegSeqRat readback rows, and Real seal-source rows are identified on every
common section.

The common section is determined by the finite cover itself. Use
\autoref{thm:streamname-terminal-fusion-finite-cover-cocycle-exactness} for
the fused StreamName window, and use
\autoref{thm:regseqrat-finite-request-terminal-budget-factorization} for the
RegSeqRat finite-request budget over that window.

It remains to transport the dyadic and Real rows across the common section.
Apply \autoref{thm:dyadicratcore-regseqrat-real-pullback-composition} to the
endpoint, radius, distance, classifier, regularity, readback, and seal-source
rows. They are therefore the same pullback data for $\mathcal P$ and
$\mathcal Q$.

Finally apply \autoref{thm:real-finite-cover-seal-pasting-criterion}. The
pasting criterion says that a four-face packet with those overlap
restrictions has a unique public terminal seal read, so $\mathcal P$ and
$\mathcal Q$ determine the same read.

For the converse, start from equality of the public terminal seal read and
read it back through the same sibling route and normal-form theorem. The
restriction data are exactly the four recovered faces listed in the statement.
The exclusion clause follows because every cited route uses only the displayed
support rows and has no coordinate for quotient streams, host reals, selected
limits, countable choice, ambient completeness, metric completeness, or a
standard bridge.
\end{proof}

\begin{theorem}[Real terminal seal finite-cover cocycle exactness]
\label{thm:real-terminal-seal-finite-cover-cocycle-exactness}
Let $\{\mathcal R_i\}_{i\in I}$ be a finite family of local terminal-seal
reads for the public $\RealUp$ surface. Suppose every $\mathcal R_i$ exposes
exactly the four faces
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp,
$$
with support rows
$$
\BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert,
$$
and suppose that for every overlap $i,j$ the restrictions of the
$\StreamNameUp$ finite window, the $\DyadicRatCoreUp$
endpoint-radius-distance ledger, the $\RegSeqRatUp$ finite-request classifier
and regularity rows, and the local $\RealUp$ seal-source row agree. Assume
also that on every triple overlap $i,j,k$ the two restriction routes from
$\mathcal R_i$ to the common section coincide:
$$
(\mathcal R_i|_{ij})|_{ijk}=(\mathcal R_i|_{ik})|_{ijk},
\qquad
(\mathcal R_i|_{ij})|_{ijk}=(\mathcal R_j|_{ij})|_{ijk}.
$$

Then the family pastes to a unique public $\RealUp$ terminal-seal read whose
restriction to each finite cover member is $\mathcal R_i$. Conversely, any
public $\RealUp$ terminal-seal read restricts to such a finite cocycle on the
four displayed faces. This exactness exports no quotient stream equality, host
real equality, selected limit, countable choice, ambient completeness,
metric-completeness datum, or standard bridge row.
\end{theorem}
\begin{proof}
First put every local read in four-face normal form. Apply
\autoref{thm:real-terminal-seal-four-face-cover-normal-form}; each
$\mathcal R_i$ is read as a finite StreamName window, a DyadicRatCore
endpoint-radius-distance ledger, a RegSeqRat finite-request classifier with
regularity rows, and a local Real seal-source row over the shared $\BHist$,
$\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

Next paste the StreamName face. The overlap and triple-overlap assumptions are
exactly the cocycle data used by
\autoref{thm:streamname-terminal-fusion-finite-cover-cocycle-exactness}, so
the finite windows fuse to one terminal StreamName packet and restrict back to
the given windows.

Transport the remaining faces over that fused window. Use
\autoref{thm:dyadicratcore-regseqrat-real-pullback-composition} to carry the
dyadic endpoint, radius, and distance rows together with the RegSeqRat
classifier, regularity, readback, and Real seal-source rows as one pullback
surface over the common finite requests.

Now apply \autoref{thm:real-finite-cover-seal-pasting-criterion}. The pasted
StreamName packet and transported pullback rows give a public terminal-seal
read, and its restriction to every cover member is the original
$\mathcal R_i$.

Uniqueness is the determinacy content of
\autoref{thm:real-terminal-seal-four-face-cover-descent-determinacy}. Any two
public reads with those restrictions have the same four faces on every overlap,
hence determine the same terminal-seal read.

For the converse, start with a public $\RealUp$ terminal-seal read and restrict
it along the finite cover. The normal-form theorem supplies the four local
faces, the StreamName fusion theorem gives the cocycle equations on triple
overlaps, and the pasting criterion identifies the recovered public read with
the original one.

The exclusion clause is stable under each cited step: none of the normal-form,
fusion, pullback-composition, pasting, or determinacy results introduces
quotient stream equality, host real equality, selected limits, countable
choice, ambient completeness, metric-completeness data, or a standard bridge
row.
\end{proof}

\begin{theorem}[Real terminal seal RegSeqRat cover descent equivalence]
\label{thm:real-terminal-seal-regseqrat-cover-descent-equivalence}
Let $\{\mathcal Q_i\}_{i\in I}$ be a finite family of public
$\RegSeqRatUp$ reads over one finite cover. The family presents an accepted
public $\RealUp$ terminal-seal read if and only if its cover data satisfy the
$\RegSeqRatUp$ four-face gluing rows, the $\RegSeqRatUp$ four-face
associativity rows, the $\StreamNameUp$ finite-cover descent exactness row,
the $\DyadicRatCoreUp$ endpoint-radius-distance ledger agreement, and the
local $\RealUp$ seal cocycle exactness row. The accepted source has exactly
the four public faces
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp
$$
and exactly the common support rows
$$
\BHist,\quad \hsame,\quad \Cont,\quad \Pkg,\quad \NameCert.
$$
It exports no quotient streams, host equality, selected limits, countable
choice, ambient completeness, metric completeness, aggregate formalstatus row,
or standard bridge row.
\end{theorem}
\begin{proof}
For the forward direction, start with the accepted public $\RealUp$
terminal-seal read presented by the finite $\RegSeqRatUp$ family. Apply
\autoref{thm:real-terminal-seal-finite-cover-cocycle-exactness}. The public
read restricts to local terminal-seal reads whose only visible faces are
$\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ over the
displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

Next identify the descent rows carried by those restrictions. Apply
\autoref{thm:regseqrat-real-seal-finite-cover-gluing} and
\autoref{thm:regseqrat-real-seal-finite-cover-associativity} to read the
$\RegSeqRatUp$ classifier, regularity, and readback data as a coherent
four-face cover. Apply
\autoref{thm:streamname-regseqrat-real-finite-cover-descent-exactness} to
recover the $\StreamNameUp$ finite-cover descent row, and read the
$\DyadicRatCoreUp$ endpoint, radius, and distance coordinates from the same
pullback ledger. Thus the forward image is exactly the descent packet named in
the statement.

For the reverse direction, start with a finite $\RegSeqRatUp$ cover packet
satisfying the gluing, associativity, StreamName descent, DyadicRatCore ledger,
and local Real cocycle rows. Apply
\autoref{thm:regseqrat-real-seal-common-refinement-cover-completeness}. The
packet admits a common refinement whose face restrictions are comparable over
one support spine.

Now apply
\autoref{thm:real-terminal-seal-four-face-cover-descent-determinacy}. The
refined packet determines at most one public terminal-seal read on the four
faces. Apply
\autoref{thm:real-terminal-seal-finite-cover-cocycle-exactness} to paste the
local Real cocycle and obtain that public read. The pasted read restricts back
to the original finite $\RegSeqRatUp$ descent data, so the two constructions
are inverse at the accepted public-source surface.

The negative boundary is audit-only. Apply
\autoref{thm:real-terminal-seal-audit-noncollapse}; the finite-cover route
does not create a route-level formalstatus aggregate, replacement leantarget,
or bridge row. Since all preceding steps use only the four faces and support
rows displayed above, no quotient stream, host equality, selected limit,
countable-choice principle, ambient-completeness row, or metric-completeness
row is introduced.
\end{proof}

\begin{theorem}[Real terminal seal audit noncollapse]
\label{thm:real-terminal-seal-audit-noncollapse}
Let $\{\mathcal R_i\}_{i\in I}$ be a finite family of local terminal-seal
reads satisfying the hypotheses of
\autoref{thm:real-terminal-seal-finite-cover-cocycle-exactness}, and let
$\mathcal R$ be the resulting public $\RealUp$ terminal-seal read. Then the
audit rows visible from $\mathcal R$ over the four displayed faces
$$
\StreamNameUp,\quad \DyadicRatCoreUp,\quad \RegSeqRatUp,\quad \RealUp
$$
are exactly the objectwise closurestatus, formalstatus, leantarget, and
bridgestatus rows of those four faces. The finite-cover descent supplies no
aggregate route-level status row, no replacement leantarget, and no
standard-bridge row.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-terminal-seal-finite-cover-cocycle-exactness}. The
public read $\mathcal R$ is obtained only by pasting the StreamName,
DyadicRatCore, RegSeqRat, and local Real terminal-seal faces over their common
$\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

Next read the audit coordinate through
\autoref{thm:real-l10-current-phase-exit-audit-pullback}. That pullback
identifies the current Phase Real and Completion audit report with the
objectwise product of the four face reports, so the pasted terminal-seal route
has no separate audit carrier beyond the displayed face rows.

Apply \autoref{thm:real-l10-face-status-noncollapse}. The closurestatus,
formalstatus, and bridgestatus rows therefore remain attached to their
respective faces and are not replaced by a route-level status row. Apply
\autoref{thm:real-l10-face-leantarget-noncollapse}; the same locality holds
for the leantarget rows.

Finally reuse the exclusion clause of
\autoref{thm:real-terminal-seal-finite-cover-cocycle-exactness}. Since the
descent has no coordinate for quotient streams, host reals, selected limits,
countable choice, ambient completeness, metric completeness, or a standard
bridge, the audit read cannot manufacture an aggregate bridge or verification
record.
\end{proof}

\begin{definition}[Real terminal seal dependency manifest]
\label{def:real-terminal-seal-dependency-manifest}
The $\RealUp$ terminal-seal dependency manifest is the finite packet
$$
\mathsf{Dep}_{\RealUp}^{\mathrm{term}}
  =(S,D,R,E,B,H,C,P,N)
$$
attached to a public terminal-seal read, with the following rows:
\begin{enumerate}
\item $S$ is the finite $\StreamNameUp$ window of stream names and finite
request coordinates.
\item $D$ is the $\DyadicRatCoreUp$ endpoint, radius, and distance ledger over
the window $S$.
\item $R$ is the $\RegSeqRatUp$ classifier, regularity, and readback row over
the same finite requests.
\item $E$ is the local $\RealUp$ seal-source row obtained from the
StreamName--DyadicRatCore--RegSeqRat pullback.
\item $B,H,C,P,N$ are respectively the shared $\BHist$ ledger, $\hsame$
transport, $\Cont$ route, $\Pkg$ provenance, and $\NameCert$ row used by the
four faces.
\end{enumerate}

A public terminal-seal read consumes a dependency iff it is one of these nine
rows or a restriction of one of them along a finite cover member.
\end{definition}

\begin{theorem}[Real terminal seal dependency manifest exhaustion]
\label{thm:real-terminal-seal-dependency-manifest-exhaustion}
Every public $\RealUp$ terminal-seal read factors through exactly the manifest
$\mathsf{Dep}_{\RealUp}^{\mathrm{term}}$. Equivalently, after restricting the
read to any finite cover, all data consumed by the read are exhausted by the
$\StreamNameUp$ window, the $\DyadicRatCoreUp$ endpoint-radius-distance ledger,
the $\RegSeqRatUp$ classifier/readback row, the local $\RealUp$ seal row, and
the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support. These
rows paste back to the public read and no further dependency coordinate is
available.
\end{theorem}
\begin{proof}
Put the public read in four-face normal form by applying
\autoref{thm:real-terminal-seal-four-face-cover-normal-form}. This identifies
the read with the four displayed faces over the shared $\BHist$, $\hsame$,
$\Cont$, $\Pkg$, and $\NameCert$ support, which is precisely the manifest
packet of \autoref{def:real-terminal-seal-dependency-manifest}.

Now restrict the read along a finite cover. Apply
\autoref{thm:real-terminal-seal-finite-cover-cocycle-exactness}: the local
restrictions form a cocycle on those same rows, and the cocycle pastes back to
the original public terminal-seal read.

It remains to rule out an additional dependency coordinate. Apply
\autoref{thm:real-terminal-seal-audit-noncollapse}. The only audit rows visible
from the pasted read are the objectwise rows of the four faces, and there is no
route-level replacement row. Hence every public dependency is one of the
manifest rows or its finite-cover restriction, and the manifest exhausts the
read.
\end{proof}

\begin{theorem}[Real terminal seal dependency manifest non-escape]
\label{thm:real-terminal-seal-dependency-manifest-non-escape}
The manifest $\mathsf{Dep}_{\RealUp}^{\mathrm{term}}$ contains no row for
quotient stream equality, host real equality, selected limits, countable
choice, ambient completeness, metric-completeness data, or standard-bridge
data. Consequently no public $\RealUp$ terminal-seal read may consume any of
those objects through the manifest.
\end{theorem}
\begin{proof}
The manifest rows are listed in
\autoref{def:real-terminal-seal-dependency-manifest}; each row is one of
$S,D,R,E,B,H,C,P,N$ and therefore belongs to the four-face terminal-seal route
or to its shared BEDC support.

Apply \autoref{thm:real-terminal-seal-dependency-manifest-exhaustion}. Any
public terminal-seal read consumes only those rows or their finite-cover
restrictions.

Finally apply the exclusion clauses of
\autoref{thm:real-terminal-seal-four-face-cover-normal-form},
\autoref{thm:real-terminal-seal-finite-cover-cocycle-exactness}, and
\autoref{thm:real-terminal-seal-audit-noncollapse}. None of the four-face
normal form, finite-cover cocycle, or audit projection contains quotient stream
equality, host real equality, selected limits, countable choice, ambient
completeness, metric-completeness data, or standard-bridge data. Since the
manifest has no coordinate for such data, no public read can escape through it.
\end{proof}

\begin{theorem}[Real terminal seal public source completeness]
\label{thm:real-terminal-seal-public-source-completeness}
A finite packet
$$
M=(S,D,R,E,B,H,C,P,N)
$$
reconstructs a public $\RealUp$ terminal-seal read exactly when its rows are the
terminal-seal dependency manifest rows of
\autoref{def:real-terminal-seal-dependency-manifest}: $S$ is the finite
$\StreamNameUp$ name-and-request window, $D$ is the
$\DyadicRatCoreUp$ endpoint-radius-distance ledger on that window, $R$ is the
$\RegSeqRatUp$ classifier and readback row, $E$ is the local $\RealUp$ seal row,
and $B,H,C,P,N$ are the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and
$\NameCert$ support rows. The rows must satisfy the shared-face compatibility
conditions displayed by
\autoref{thm:real-terminal-seal-four-face-cover-normal-form} and the finite-cover
cocycle compatibility of
\autoref{thm:real-terminal-seal-finite-cover-cocycle-exactness}. Under those
conditions $M$ determines a public $\RealUp$ terminal-seal read, and every public
$\RealUp$ terminal-seal read yields exactly such a compatible packet.
\end{theorem}
\begin{proof}
Start from a compatible packet $M=(S,D,R,E,B,H,C,P,N)$. The row interpretation is
fixed by \autoref{def:real-terminal-seal-dependency-manifest}: the first four
rows are the $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and local
$\RealUp$ faces, and the last five rows are the common BEDC support.

Apply \autoref{thm:real-terminal-seal-four-face-cover-normal-form}. The
shared-face compatibility hypotheses identify these rows as the four terminal
faces over one $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ spine. Apply
\autoref{thm:real-terminal-seal-finite-cover-cocycle-exactness}. The finite-cover
compatibility pastes the local face restrictions into a public terminal-seal
read.

For the reverse direction, begin with a public $\RealUp$ terminal-seal read.
Apply \autoref{thm:real-terminal-seal-dependency-manifest-exhaustion}. The read
factors through the manifest rows and no additional dependency coordinate is
available. The normal-form and finite-cover theorems provide the required
shared-face and cocycle compatibility, so the read yields a packet of the
displayed form.

Finally apply
\autoref{thm:real-terminal-seal-dependency-manifest-non-escape}. The packet has
no coordinate for quotient stream equality, host real equality, selected limits,
countable choice, ambient completeness, metric-completeness data, or
standard-bridge data. Hence the reconstruction and extraction have the same
public source surface, namely the manifest $M$.
\end{proof}

\begin{theorem}[Real terminal seal dependency manifest phase-exit face product]
\label{thm:real-terminal-seal-dependency-manifest-phase-exit-face-product}
Let
$$
\mathsf{Dep}_{\RealUp}^{\mathrm{term}}=(S,D,R,E,B,H,C,P,N)
$$
be an accepted $\RealUp$ terminal-seal dependency manifest. Then this manifest
determines exactly the Phase Real and Completion four-face product over
$\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and the local $\RealUp$
face, with the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$
support rows. Conversely, every accepted Phase Real and Completion four-face
product over those four faces restricts to this manifest. The correspondence
exports no quotient streams, host real equality, selected limits, countable
choice, ambient completeness, metric-completeness rows, aggregate formalstatus
tokens, or standard bridge rows.
\end{theorem}
\begin{proof}
For the forward direction, read the manifest through
\autoref{def:real-terminal-seal-dependency-manifest}. Its rows are precisely the
finite $\StreamNameUp$ window, the $\DyadicRatCoreUp$ endpoint ledger, the
$\RegSeqRatUp$ classifier/readback row, the local $\RealUp$ seal-source row, and
the common $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

Apply \autoref{thm:real-terminal-seal-dependency-manifest-exhaustion}. Every
public terminal-seal read consumes only those rows or their finite-cover
restrictions. Apply
\autoref{thm:real-l10-current-phase-exit-audit-pullback} to identify the Phase
Real and Completion audit read with the product of the same four face reports,
and apply \autoref{thm:real-l10-four-face-finite-pullback-public-interface} to
read that product as the public four-face interface.

For the reverse direction, start with an accepted Phase Real and Completion
four-face product over $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and
the local $\RealUp$ face. The public-interface theorem displays exactly the
same four face rows and the same shared BEDC support rows, so restricting the
product to the terminal-seal consumer surface gives the packet
$\mathsf{Dep}_{\RealUp}^{\mathrm{term}}$.

It remains to check that no coordinate has escaped the manifest. Apply
\autoref{thm:real-terminal-seal-dependency-manifest-non-escape}. The manifest
has no row for quotient streams, host real equality, selected limits, countable
choice, ambient completeness, metric-completeness data, or standard-bridge
data. The audit pullback is facewise, not aggregate, so it also supplies no
aggregate formalstatus token and no standard bridge row beyond the displayed
face records.
\end{proof}

\begin{theorem}[Real terminal seal manifest exit consumer exactness]
\label{thm:real-terminal-seal-manifest-exit-consumer-exactness}
An accepted manifest
$$
\mathsf{Dep}_{\RealUp}^{\mathrm{term}}=(S,D,R,E,B,H,C,P,N)
$$
is accepted by the current Phase Real and Completion exit consumer exactly when
its consumer rows are the four object faces
$\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and local $\RealUp$,
together with the shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$
support rows. Equivalently, the manifest is precisely the consumer surface used
by the current L10 exit route. No accepted consumer presentation adds a
quotient stream, host real equality, selected limit, countable choice, ambient
completeness, metric-completeness row, aggregate formalstatus token, or standard
bridge row.
\end{theorem}
\begin{proof}
For the forward direction, apply
\autoref{thm:real-terminal-seal-public-source-completeness}. The accepted
manifest already reconstructs a public terminal-seal read from exactly the
$\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, local $\RealUp$, $\BHist$,
$\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows.

Apply
\autoref{thm:real-terminal-seal-dependency-manifest-phase-exit-face-product}.
It identifies those rows with the Phase Real and Completion four-face product.
Then apply \autoref{thm:streamname-real-completion-four-object-exit-route},
\autoref{thm:dyadicratcore-regseqrat-bridge-handoff-exactness}, and
\autoref{thm:regseqrat-real-seal-handoff}. These three handoff surfaces read
the same finite consumer as the current L10 exit route through the StreamName,
DyadicRatCore, RegSeqRat, and Real faces.

For the reverse direction, start with a current L10 exit consumer presentation.
The four-object exit route supplies exactly the same four object faces and the
same shared BEDC support rows. The DyadicRatCore-to-RegSeqRat handoff and the
RegSeqRat-to-Real seal handoff identify the downstream reads with the displayed
dyadic ledger, regular-rational classifier, and local Real seal rows, so the
presentation restricts to
$\mathsf{Dep}_{\RealUp}^{\mathrm{term}}=(S,D,R,E,B,H,C,P,N)$.

Finally, the exclusion clause follows from
\autoref{thm:real-terminal-seal-dependency-manifest-non-escape} and from the
facewise character of
\autoref{thm:real-terminal-seal-dependency-manifest-phase-exit-face-product}.
The consumer has no coordinate in which quotient streams, host real equality,
selected limits, countable choice, ambient completeness, metric-completeness
data, aggregate formalstatus tokens, or standard bridge rows could be carried.
\end{proof}

\begin{theorem}[Real terminal seal four-face classifier correspondence]
\label{thm:real-terminal-seal-four-face-classifier-correspondence}
Let
$$
\mathsf{Dep}_{\RealUp}^{\mathrm{term}}=(S,D,R,E,B,H,C,P,N)
$$
be an accepted $\RealUp$ terminal-seal dependency manifest, and let $Q$ be an
accepted Phase Real and Completion four-face product over
$\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and the local $\RealUp$
seal face. The manifest and $Q$ determine the same public classifier verdict
exactly when their $\DyadicRatCoreUp$ tolerance ledger, $\StreamNameUp$ finite
window, $\RegSeqRatUp$ classifier/readback row, local $\RealUp$ seal row, and
shared $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows agree.
Conversely, any shared public classifier verdict for the terminal manifest and
the four-face product restricts to those same four faces and those same shared
support rows. The correspondence carries no quotient stream, host real
equality, selected limit, countable-choice coordinate, ambient completeness,
metric-completeness row, aggregate formalstatus token, or standard bridge row.
\end{theorem}
\begin{proof}
Begin with an accepted terminal manifest and an accepted four-face product.
Apply
\autoref{thm:real-terminal-seal-dependency-manifest-phase-exit-face-product}.
It identifies both presentations with the same displayed product of the
$\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and local $\RealUp$ faces
over the common $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

Apply \autoref{thm:real-terminal-seal-manifest-exit-consumer-exactness}. The
manifest is accepted by the current exit consumer precisely through these
consumer rows. Then apply
\autoref{thm:streamname-real-completion-four-object-exit-route},
\autoref{thm:dyadicratcore-regseqrat-bridge-handoff-exactness}, and
\autoref{thm:regseqrat-real-seal-handoff}. These handoff theorems align the
finite StreamName window, the dyadic tolerance ledger, the regular-rational
classifier/readback row, and the local Real seal row with the public classifier
read used by the L10 exit route.

Thus equal rows give the same public classifier verdict. No independent
classifier coordinate remains after the handoffs, because the verdict is read
from exactly the four object faces and the shared BEDC support.

For the reverse direction, suppose the terminal manifest and the four-face
product share a public classifier verdict. The four-object exit route first
restricts that verdict to the $\StreamNameUp$ window and to the
$\DyadicRatCoreUp$ tolerance ledger. The DyadicRatCore-to-RegSeqRat handoff
then restricts it to the $\RegSeqRatUp$ classifier/readback row, and the
RegSeqRat-to-Real handoff restricts it to the local $\RealUp$ seal row.

Finally, consumer exactness and the manifest-to-product identification force
the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows on
both presentations. The non-escape clause in the manifest identification
excludes quotient streams, host real equality, selected limits, countable
choice, ambient completeness, metric-completeness rows, aggregate formalstatus
tokens, and standard bridge rows, so the shared classifier verdict has no
off-table restriction beyond the four displayed faces.
\end{proof}

\begin{theorem}[Real terminal seal NameCert exit consumer exactness]
\label{thm:real-terminal-seal-namecert-exit-consumer-exactness}
An accepted terminal-seal manifest
$$
\mathsf{Dep}_{\RealUp}^{\mathrm{term}}=(S,D,R,E,B,H,C,P,N)
$$
determines the same local $\NameCert$ row as the current Phase Real and
Completion exit consumer exactly when the four object faces
$\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and local $\RealUp$, and
the support rows $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$, agree
between the manifest presentation and the L10 exit presentation. Consequently,
the local $\NameCert$ consumer row is neither an additional aggregate
certificate nor an off-table completeness assertion: it is the facewise
$\NameCert$ support already carried by the accepted terminal-seal manifest and
by the current Phase Real and Completion exit surface.
\end{theorem}
\begin{proof}
First compare the manifest with the current exit consumer. Apply
\autoref{thm:real-terminal-seal-manifest-exit-consumer-exactness}. It identifies
the accepted terminal-seal manifest with exactly the consumer rows read by the
current L10 exit route: the four object faces and the shared $\BHist$, $\hsame$,
$\Cont$, $\Pkg$, and $\NameCert$ support.

Next compare that consumer presentation with the classifier read. Apply
\autoref{thm:real-terminal-seal-four-face-classifier-correspondence}. The
public classifier verdict has no independent coordinate beyond agreement of
the $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, and local $\RealUp$
faces over the same shared support rows, so equality of the displayed
$\NameCert$ rows is exactly the NameCert component of the classifier
correspondence.

It remains to identify the same row with the Phase Real and Completion exit
route. Apply \autoref{thm:streamname-real-completion-four-object-exit-route} to
read the consumer through the four-object surface, then apply
\autoref{thm:dyadicratcore-regseqrat-bridge-handoff-exactness} and
\autoref{thm:regseqrat-real-seal-handoff}. These handoffs preserve the dyadic
ledger, stream window, regular-rational classifier/readback row, and local
Real seal row, and they do not create a second $\NameCert$ coordinate.

Conversely, if the current Phase Real and Completion exit consumer has the same
four object faces and the same $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and
$\NameCert$ rows, the preceding identifications restrict it to the accepted
terminal-seal manifest. The non-escape clauses in
\autoref{thm:real-terminal-seal-manifest-exit-consumer-exactness} and
\autoref{thm:real-terminal-seal-four-face-classifier-correspondence} exclude
quotient streams, host real equality, selected limits, choice, completeness,
aggregate formalstatus rows, and standard bridge rows, so the exact local
$\NameCert$ row is precisely the one displayed in the manifest.
\end{proof}
