\begin{theorem}[RealUp RegSeqRat finite-request fusion]
\label{thm:real-regseqrat-finite-request-fusion}
Let $R$ be an accepted $\RegSeqRatUp$ packet and let $S$ be a public finite request to a $\RealUp$ seal over $R$. Then the request factors through one finite $\BHist$ packet whose visible source rows are exactly the following data:
\begin{enumerate}
\item the $\StreamNameUp$ finite-window schedule row selected by the request;
\item the $\DyadicRatCoreUp$ endpoint, radius, and finite distance rows read at that schedule;
\item the $\RegSeqRatUp$ classifier row and regularity rows that certify the accepted packet;
\item the componentwise $\hsame$ transports tying the schedule, dyadic rows, regularity rows, and local seal row to the same request packet;
\item the displayed $\Cont$ routes from the schedule through the dyadic and regularity rows to the seal read;
\item the $\mathsf{Pkg}$ provenance over $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item the local $\NameCert_{\RealUp}$ row for the finite seal request.
\end{enumerate}
Every downstream consumer of the public seal request reads through this same packet. The fusion exports no quotient stream equality, no host real equality, no selected limit, no ambient completeness principle, no choice data, and no second $\RealUp$ seal coordinate.
\end{theorem}
\begin{proof}
First apply \autoref{thm:real-regseqrat-streamname-seal-common-window-fusion}. It places the public seal request on one common finite window with the displayed $\StreamNameUp$ schedule, transported $\DyadicRatCoreUp$ observations, $\RegSeqRatUp$ regularity spine, and local $\RealUp$ seal provenance.

Next use \autoref{thm:regseqrat-streamname-dyadic-real-handoff-exactness}. The accepted $\RegSeqRatUp$ packet can reach the real seal only through the StreamName schedule rows, dyadic endpoint and radius ledgers, finite dyadic-distance rows, classifier rows, and regularity rows named by the same finite handoff.

Now apply \autoref{thm:regseqrat-tail-budget-window-seal-pullback}. The tail-budget window pulls the seal read back to the same finite rows, so later consumers cannot substitute an off-packet tail selector, host limit, or second seal coordinate.

Finally use \autoref{thm:real-name-seal-source-factorization}. Repacking the common-window handoff, the RegSeqRat exactness route, and the tail-budget pullback gives one $\BHist$ source packet with exactly the schedule, dyadic, classifier, regularity, $\hsame$, $\Cont$, $\mathsf{Pkg}$, and local $\NameCert$ rows listed above. Since each cited boundary lacks quotient stream equality, host real equality, selected limits, ambient completeness, choice data, and duplicate Real-seal coordinates, the fused request exports none of them.
\end{proof}

\begin{theorem}[RealUp RegSeqRat finite-request selector-budget lock]
\label{thm:real-regseqrat-finite-request-fusion-selector-budget-lock}
Let $Q$ be an accepted $\RegSeqRatUp$ packet and let $S$ be a public finite request to a $\RealUp$ seal over $Q$. Suppose the finite request fusion is displayed over the rows $(W,D,R,H,C,P,N)$, where $W$ is the $\StreamNameUp$ finite-window row, $D$ is the $\DyadicRatCoreUp$ endpoint and radius row family, $R$ is the $\RegSeqRatUp$ classifier and readback row, $H$ is the componentwise $\hsame$ provenance, $C$ is the displayed $\Cont$ route, $P$ is the $\mathsf{Pkg}$ provenance, and $N$ is the local $\NameCert_{\RealUp}$ row. Then any downstream selector-budget consumer or completion-facing consumer of the same $\RealUp$ seal must reuse exactly these rows. In particular it cannot introduce a second selector budget, an off-packet tail selector, quotient stream equality, host real equality, a selected limit, choice data, or an ambient completeness row.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-regseqrat-finite-request-fusion}. The public finite request has already been repacked into one $\BHist$ packet whose displayed rows are the window row $W$, the dyadic endpoint and radius rows $D$, the classifier and readback row $R$, and the provenance rows $H$, $C$, $P$, and $N$.

Apply \autoref{thm:real-regseqrat-streamname-seal-common-window-fusion}. This identifies every seal read in the packet with the same $\StreamNameUp$ finite-window row, so a later selector-budget consumer has no second window coordinate through which to read the seal.

Apply \autoref{thm:regseqrat-tail-budget-window-seal-pullback}. The tail-budget route is pulled back to the displayed finite window and its dyadic and regularity rows, ruling out an off-packet tail selector or a second selector budget.

Finally apply \autoref{thm:real-name-seal-source-factorization}. The final $\RealUp$ seal source is the same finite source packet with its $\NameCert_{\RealUp}$ row, so completion-facing consumers inherit the listed $W$, $D$, $R$, $H$, $C$, $P$, and $N$ rows and gain no quotient stream equality, host real equality, selected limit, choice data, or ambient completeness row.
\end{proof}

\begin{theorem}[RealUp RegSeqRat finite-request triangle]
\label{thm:real-regseqrat-finite-request-triangle}
Let $Q$ be an accepted $\RegSeqRatUp$ packet and let $S$ be a public finite request to a $\RealUp$ seal over $Q$. The following four displayed BEDC routes mutually determine one another:
\begin{enumerate}
\item $S$ is the $\RealUp$ finite-request fusion packet displayed by \autoref{thm:real-regseqrat-finite-request-fusion};
\item $S$ lands in one $\StreamNameUp$ finite-window row carrying the scheduled unary observation read by the request;
\item that same finite-window row carries the $\DyadicRatCoreUp$ endpoint, radius, finite-distance ledger, and the $\RegSeqRatUp$ classifier and readback row;
\item the local $\NameCert_{\RealUp}$ seal read is reached only through those StreamName, dyadic-ledger, classifier, readback, $\hsame$, $\Cont$, and $\mathsf{Pkg}$ rows.
\end{enumerate}
Thus any one of the four route descriptions reconstructs the other three inside the same finite $\BHist$ packet. The triangle exports no quotient stream equality, no host real equality, no selected limit, no choice data, and no ambient completeness principle.
\end{theorem}
\begin{proof}
Start with the fusion packet route. By \autoref{thm:real-regseqrat-finite-request-fusion}, the public request is already repacked as one finite $\BHist$ packet whose visible rows include the $\StreamNameUp$ schedule, the $\DyadicRatCoreUp$ endpoint and radius family, the $\RegSeqRatUp$ classifier and regularity rows, the componentwise $\hsame$ transports, the displayed $\Cont$ routes, the $\mathsf{Pkg}$ provenance, and the local $\NameCert_{\RealUp}$ row.

The StreamName corner is determined by \autoref{thm:real-regseqrat-streamname-seal-common-window-fusion}. It places the seal request and the scheduled unary observation on a single common finite window, so the fusion packet determines the window route and the window route recovers the seal-facing source row of the packet.

The dyadic and classifier corner is determined by \autoref{thm:regseqrat-streamname-dyadic-real-handoff-exactness}. The same finite handoff carries the dyadic endpoint, radius, finite-distance ledger, classifier row, and readback row, and the handoff admits no off-window replacement for those rows.

It remains to identify the seal corner. Apply \autoref{thm:regseqrat-tail-budget-window-seal-pullback} to pull the seal read back to the displayed window and ledger rows, then apply \autoref{thm:real-name-seal-source-factorization} to factor the local $\NameCert_{\RealUp}$ seal source through exactly that finite packet.

Each direction of the triangle is therefore a repackaging of the same source rows: fusion packet, StreamName window, dyadic-RegSeqRat ledger, and local Real seal. Since the cited routes carry only finite $\BHist$, $\hsame$, $\Cont$, $\mathsf{Pkg}$, and $\NameCert$ provenance, the correspondence introduces no quotient stream equality, no host real equality, no selected limit, no choice data, and no ambient completeness principle.
\end{proof}

\begin{theorem}[RealUp RegSeqRat completion-consumer section exactness]
\label{thm:real-regseqrat-completion-consumer-section-exactness}
Let $Q$ be an accepted $\RegSeqRatUp$ packet, let $S$ be a public finite request to a $\RealUp$ seal over $Q$, and let $(W,D,R,H,C,P,N)$ be the finite request section displayed by \autoref{thm:real-regseqrat-finite-request-fusion-selector-budget-lock}. A completion-facing or $\RealUp$-facing consumer of $S$ is accepted exactly when its visible rows factor through that section: $W$ is the $\StreamNameUp$ finite-window row, $D$ is the $\DyadicRatCoreUp$ endpoint, radius, and distance row family, $R$ is the $\RegSeqRatUp$ classifier and readback row family, $H$ is the componentwise $\hsame$ provenance, $C$ is the displayed $\Cont$ route, $P$ is the $\mathsf{Pkg}$ provenance, and $N$ is the local $\NameCert_{\RealUp}$ row. Thus the consumer boundary exports no quotient stream equality, no host real equality, no selected limit, no choice data, no ambient completeness principle, and no second $\RealUp$ seal coordinate.
\end{theorem}
\begin{proof}
For the forward direction, apply \autoref{thm:real-regseqrat-finite-request-fusion}. The accepted seal request is represented by one finite $\BHist$ packet whose visible rows are the StreamName window, dyadic endpoint, radius, and distance rows, RegSeqRat classifier and readback rows, componentwise $\hsame$ rows, displayed $\Cont$ routes, $\mathsf{Pkg}$ provenance, and local $\NameCert_{\RealUp}$ row.

Next apply \autoref{thm:real-regseqrat-finite-request-fusion-selector-budget-lock}. Any completion-facing or selector-budget consumer of the same seal must reuse the displayed $(W,D,R,H,C,P,N)$ rows, so an accepted consumer has no remaining visible coordinate outside the finite section.

For the reverse direction, assume the visible rows of the completion-facing or $\RealUp$-facing consumer factor through $(W,D,R,H,C,P,N)$. Apply \autoref{thm:regseqrat-real-request-section-nonescape} to identify this section as the entire RegSeqRat source section seen by a Real seal, with no escaping source coordinate.

Finally apply \autoref{thm:streamname-tail-compatible-window-terminality}. The StreamName finite-window row is terminal among accepted finite-window consumers with the same observation, tail-selector route, budget surface, pointwise handoff, and support rows. Repacking the terminal window with the dyadic, classifier, readback, transport, route, package, and local naming rows gives exactly the accepted finite section above, and the exclusions from the cited nonescape and terminality theorems rule out quotient stream equality, host real equality, selected limits, choice data, ambient completeness, and a second $\RealUp$ seal coordinate.
\end{proof}

\begin{theorem}[RealUp RegSeqRat finite-request four-face source exhaustion]
\label{thm:real-regseqrat-finite-request-four-face-source-exhaustion}
Let $Q$ be an accepted $\RegSeqRatUp$ packet and let $S$ be a public finite request to a $\RealUp$ seal over $Q$. Then $S$ is accepted exactly through the following four displayed source faces:
\begin{enumerate}
\item the $\StreamNameUp$ finite-window schedule face;
\item the $\DyadicRatCoreUp$ endpoint, radius, and finite-distance face;
\item the $\RegSeqRatUp$ classifier, readback, and regularity face;
\item the local $\NameCert_{\RealUp}$ seal face.
\end{enumerate}
The only transport between these faces is the componentwise $\hsame$ family, the displayed $\Cont$ routes, and the $\mathsf{Pkg}$ provenance already present in the finite request section. Consequently the accepted source of $S$ is exhausted by those four faces, and no quotient stream equality, host real equality, selected limit, choice data, ambient completeness principle, or second $\RealUp$ seal coordinate is exported.
\end{theorem}
\begin{proof}
Apply \autoref{thm:real-regseqrat-finite-request-fusion}. It supplies the finite $\BHist$ packet whose visible rows are the StreamName schedule, DyadicRatCore endpoint, radius, and distance data, RegSeqRat classifier and regularity data, componentwise $\hsame$, displayed $\Cont$, $\mathsf{Pkg}$ provenance, and the local $\NameCert_{\RealUp}$ seal row.

Next apply \autoref{thm:real-regseqrat-finite-request-fusion-selector-budget-lock}. The displayed finite request section is the unique section available to selector-budget and completion-facing consumers of the same seal request, so none of the four faces can be replaced by a second section.

Apply \autoref{thm:real-regseqrat-finite-request-triangle}. The fusion packet, StreamName window, DyadicRatCore-RegSeqRat ledger, and local Real seal face mutually determine one another inside the same finite packet.

Finally use \autoref{thm:real-regseqrat-completion-consumer-section-exactness}. Its accepted-consumer boundary says that every completion-facing or $\RealUp$-facing consumer is accepted precisely when its visible rows factor through this same section. The four displayed faces therefore give the complete accepted source for $S$, while the exclusions recorded by the cited results rule out quotient stream equality, host real equality, selected limits, choice data, ambient completeness, and a second Real seal coordinate.
\end{proof}

\begin{theorem}[StreamName-RegSeqRat-Real finite-request seal coherence]
\label{thm:streamname-regseqrat-real-finite-request-seal-coherence}
Let $W$ be a public $\StreamNameUp$ finite-window package, let $Q$ be an accepted $\RegSeqRatUp$ packet whose finite-window seal consumes the selected rows of $W$, and let $S$ be a public finite request to a $\RealUp$ seal over $Q$. Suppose the finite request displays the same $\StreamNameUp$ window, the same $\DyadicRatCoreUp$ endpoint, radius, and distance rows, the same $\RegSeqRatUp$ classifier and readback rows, the same componentwise $\hsame$ family, the same displayed $\Cont$ routes, the same $\mathsf{Pkg}$ provenance, and the same local $\NameCert_{\RealUp}$ rows. Then the StreamName-RegSeqRat seal-composition route and the $\RealUp$ four-face source-exhaustion route determine the same finite $\BHist$ packet. This coherence exports no quotient stream equality, no host real equality, no selected limit, no countable-choice schedule, no ambient completeness row, and no second $\RealUp$ seal coordinate.
\end{theorem}
\begin{proof}
First apply \autoref{thm:streamname-real-regseq-finite-window-handoff}. The public finite-window package $W$ supplies the StreamName source rows consumed by the accepted $\RegSeqRatUp$ packet, and those rows are the displayed window rows of the request.

Next apply \autoref{thm:streamname-regseqrat-real-seal-composition}. The seal route from the StreamName window through the accepted RegSeqRat packet reaches the Real-facing seal only through the selected window rows, the RegSeqRat classifier and readback rows, and their displayed provenance.

Now apply \autoref{thm:real-regseqrat-finite-request-fusion} and \autoref{thm:real-regseqrat-finite-request-triangle}. These results repack the displayed StreamName, dyadic, RegSeqRat, $\hsame$, $\Cont$, $\mathsf{Pkg}$, and local naming rows as one finite $\BHist$ packet, and they identify the StreamName window route, the dyadic-RegSeqRat ledger route, and the Real seal route as mutually determining faces of that packet.

Finally use \autoref{thm:real-regseqrat-finite-request-four-face-source-exhaustion}. Its four-face source exhaustion says that the accepted source of $S$ has no visible face beyond the StreamName window, DyadicRatCore endpoint-radius-distance data, RegSeqRat classifier-readback data, and local $\NameCert_{\RealUp}$ seal row. Hence the StreamName-RegSeqRat seal-composition route and the RealUp four-face route name the same finite $\BHist$ packet, with none of the excluded quotient, host-real, selected-limit, countable-choice, completeness, or duplicate-seal data exported.
\end{proof}

\begin{theorem}[RealUp RegSeqRat finite-request section terminality]
\label{thm:real-regseqrat-finite-request-section-terminality}
Let $Q$ be an accepted $\RegSeqRatUp$ packet and let $S$ be a public finite request to a $\RealUp$ seal over $Q$. Suppose two accepted completion-facing or $\RealUp$-facing finite-request sections over $S$ display the same $\StreamNameUp$ finite-window row, the same $\DyadicRatCoreUp$ endpoint, radius, and finite-distance rows, the same $\RegSeqRatUp$ classifier and readback rows, the same componentwise $\hsame$ family, the same displayed $\Cont$ routes, the same $\mathsf{Pkg}$ provenance, and the same local $\NameCert_{\RealUp}$ rows. Then the two sections determine the same finite $\BHist$ packet. This terminal section exports no quotient stream equality, no host real equality, no selected limit, no choice data, no ambient completeness row, and no second $\RealUp$ seal coordinate.
\end{theorem}
\begin{proof}
First apply \autoref{thm:real-regseqrat-finite-request-fusion}. Each accepted finite request is represented by one finite $\BHist$ packet whose visible rows are exactly the StreamName window, dyadic endpoint-radius-distance data, RegSeqRat classifier-readback data, componentwise $\hsame$ transports, displayed $\Cont$ routes, $\mathsf{Pkg}$ provenance, and local $\NameCert_{\RealUp}$ rows.

Next apply \autoref{thm:real-regseqrat-finite-request-fusion-selector-budget-lock}. A completion-facing or $\RealUp$-facing consumer of the same seal cannot replace the displayed window, dyadic, classifier, readback, transport, route, package, or local naming rows by a second selector-budget section.

The StreamName coordinate is terminal by \autoref{thm:streamname-tail-compatible-window-terminality}. Since the two sections display the same finite window and the same compatible tail-budget surface, both StreamName faces collapse to the same window packet.

Now use \autoref{thm:real-regseqrat-completion-consumer-section-exactness}. Acceptance of either completion-facing or $\RealUp$-facing section is exactly factorization through the displayed $(W,D,R,H,C,P,N)$ rows, so the two sections have no visible coordinate outside those rows.

Finally apply \autoref{thm:real-regseqrat-finite-request-four-face-source-exhaustion}. The StreamName, DyadicRatCore, RegSeqRat, and local Real naming faces exhaust the accepted source of $S$. Since the two sections agree on all four faces and on the only permitted $\hsame$, $\Cont$, and $\mathsf{Pkg}$ transports between them, they determine the same finite $\BHist$ packet, and none of the excluded quotient, host-real, selected-limit, choice, completeness, or duplicate-seal data is exported.
\end{proof}

\begin{theorem}[RealUp StreamName finite-request terminal bridge]
\label{thm:real-streamname-finite-request-terminal-bridge}
Let $Q$ be an accepted $\RegSeqRatUp$ packet, let $S$ be an accepted $\RealUp$ finite-request terminal section over $Q$, and let $P$ be the $\StreamNameUp$ finite-section pullback for the finite window used by $S$. Then $S$ and $P$ determine the same finite $\BHist$ packet exactly when they display the same $\StreamNameUp$ window row, the same $\DyadicRatCoreUp$ endpoint and radius ledger, the same $\RegSeqRatUp$ classifier and readback rows, the same componentwise $\hsame$ rows, the same displayed $\Cont$ route, the same $\mathsf{Pkg}$ provenance, and the same local $\NameCert$ rows. This bridge exports no quotient streams, no selected limits, no host equality, no ambient completeness row, no countable-choice schedule, and no standard bridge rows.
\end{theorem}
\begin{proof}
For the Real-facing direction, apply \autoref{thm:real-regseqrat-finite-request-section-terminality}. The accepted terminal section over $S$ is already exhausted by the displayed StreamName window, DyadicRatCore endpoint-radius ledger, RegSeqRat classifier and readback rows, componentwise $\hsame$, displayed $\Cont$, $\mathsf{Pkg}$ provenance, and local $\NameCert$ rows.

For the StreamName-facing direction, apply \autoref{thm:streamname-finite-section-pullback-exactness} together with \autoref{thm:streamname-finite-request-section-pullback}. The pullback section has no visible source beyond the finite window rows and the finite request rows transported through the same $\BHist$ packet.

If the displayed window, endpoint-radius ledger, classifier and readback rows, $\hsame$ rows, $\Cont$ route, $\mathsf{Pkg}$ provenance, and local $\NameCert$ rows agree, the two cited terminal descriptions have the same four visible faces and the same permitted transports between them. Hence the Real terminal section and the StreamName pullback name one finite $\BHist$ packet.

Conversely, if they name the same finite $\BHist$ packet, equality of the visible packet rows forces agreement of each displayed row listed in the statement: the StreamName window, dyadic ledger, RegSeqRat classifier and readback data, componentwise transport rows, continuation route, package provenance, and local naming rows.

Finally apply \autoref{thm:streamname-real-completion-exit-lean-route-boundary}. The correspondence remains on the four-object finite exit route, so it does not add quotient streams, selected limits, host equality, ambient completeness, countable-choice schedules, or standard bridge rows.
\end{proof}

\begin{theorem}[RealUp RegSeqRat finite-request pullback determinacy]
\label{thm:real-regseqrat-finite-request-determinacy}
Let $Q$ be an accepted $\RegSeqRatUp$ packet and let $S$ be a public finite request to a $\RealUp$ seal over $Q$. Suppose two accepted completion-facing or $\RealUp$-facing finite-request sections over $S$ display the same $\StreamNameUp$ finite-section pullback, the same $\DyadicRatCoreUp$ endpoint, radius, and finite-distance rows, the same $\RegSeqRatUp$ classifier and readback rows, the same local $\RealUp$ seal row, and the same $\hsame$, $\Cont$, $\mathsf{Pkg}$, and $\NameCert$ support rows. Then the two sections determine the same finite $\BHist$ Real seal packet. This determinacy exports no quotient stream equality, no host real equality, no selected limit, no choice data, no ambient completeness row, and no second $\RealUp$ seal coordinate.
\end{theorem}
\begin{proof}
First use \autoref{thm:streamname-finite-section-pullback-exactness}. The shared $\StreamNameUp$ finite-section pullback identifies the StreamName-facing part of both accepted sections with the same finite four-face route through $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$.

Next apply \autoref{thm:real-regseqrat-finite-request-four-face-source-exhaustion}. Once the StreamName pullback is fixed, the accepted source of the request has only the StreamName schedule face, the DyadicRatCore endpoint-radius-distance face, the RegSeqRat classifier-readback face, and the local Real naming face.

Now use \autoref{thm:real-regseqrat-completion-consumer-section-exactness}. Each accepted completion-facing or $\RealUp$-facing section must factor through those visible rows together with the displayed $\hsame$, $\Cont$, $\mathsf{Pkg}$, and $\NameCert$ support rows, so neither section has an additional consumer coordinate.

Finally apply \autoref{thm:real-regseqrat-finite-request-section-terminality}. The two sections agree on the StreamName pullback and on every visible dyadic, classifier, readback, local seal, transport, route, package, and naming row; terminality therefore identifies the displayed finite $\BHist$ Real seal packet. The cited boundaries keep quotient stream equality, host real equality, selected limits, choice data, ambient completeness, and a second Real seal coordinate outside the packet.
\end{proof}

\begin{theorem}[RealUp RegSeqRat finite-request route equivalence]
\label{thm:real-regseqrat-finite-request-route-equivalence}
Let $Q$ be an accepted $\RegSeqRatUp$ packet and let $S$ be a public finite request to a $\RealUp$ seal over $Q$. A completion-facing or $\RealUp$-facing finite-request read over $S$ is accepted exactly when it factors through the same four displayed L10 source faces:
\begin{enumerate}
\item the $\StreamNameUp$ finite-window row selected by the request;
\item the $\DyadicRatCoreUp$ endpoint, radius, and finite-distance ledger read at that window;
\item the $\RegSeqRatUp$ classifier, readback, and regularity rows of the accepted packet;
\item the local $\NameCert_{\RealUp}$ seal row.
\end{enumerate}
The factorization includes precisely the displayed $\BHist$, $\hsame$, $\Cont$, and $\Pkg$ support tying those four faces to one finite packet. The displayed packet is unique among accepted completion-facing and $\RealUp$-facing reads with these rows, and the equivalence exports no quotient stream equality, no host real equality, no selected limit, no choice data, no ambient completeness, no metric-completeness row, no standard bridge row, and no second $\RealUp$ seal coordinate.
\end{theorem}
\begin{proof}
For the forward direction, apply \autoref{thm:real-regseqrat-finite-request-four-face-source-exhaustion}. Any accepted read over $S$ has no visible source beyond the StreamName finite window, the DyadicRatCore endpoint-radius-distance ledger, the RegSeqRat classifier-readback-regularity surface, and the local Real naming row.

For the reverse direction, assume the read factors through these four faces together with the displayed $\BHist$, $\hsame$, $\Cont$, and $\Pkg$ support. Apply \autoref{thm:real-regseqrat-completion-consumer-section-exactness}. Its section criterion accepts exactly the completion-facing and $\RealUp$-facing consumers whose visible rows factor through the displayed finite section, so the factored read is accepted.

It remains to identify the packet. Apply \autoref{thm:real-regseqrat-finite-request-determinacy} to any two accepted reads displaying the same four faces and support rows. Then use \autoref{thm:real-streamname-finite-request-terminal-bridge} to identify the StreamName-facing pullback with the Real-facing terminal section on the same finite $\BHist$ packet.

Thus accepted reads are precisely the four-object L10 finite route through $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and the local $\NameCert_{\RealUp}$ seal row. Since the cited boundaries carry only the listed finite support, the route does not add quotient stream equality, host real equality, selected limits, choice data, ambient completeness, metric-completeness rows, standard bridge rows, or a second Real seal coordinate.
\end{proof}

\begin{theorem}[RealUp RegSeqRat terminal four-face acceptance equivalence]
\label{thm:real-regseqrat-terminal-four-face-acceptance-equivalence}
Let $Q$ be an accepted $\RegSeqRatUp$ packet and let $S$ be a public finite request to a $\RealUp$ seal over $Q$. Acceptance of a completion-facing or $\RealUp$-facing terminal read over $S$ is equivalent to factorization through exactly the four L10 faces:
\begin{enumerate}
\item the selected $\StreamNameUp$ finite-window row;
\item the $\DyadicRatCoreUp$ endpoint, radius, and finite-distance ledger;
\item the $\RegSeqRatUp$ classifier, readback, and regularity rows;
\item the local $\NameCert_{\RealUp}$ seal row.
\end{enumerate}
The factorization uses only the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support tying those faces to one finite packet. Among accepted terminal reads displaying this support, the packet is unique. The equivalence exports no quotient stream equality, no host real equality, no selected limit, no choice data, no ambient completeness, no metric-completeness row, no standard bridge row, and no second Real seal coordinate.
\end{theorem}
\begin{proof}
For the forward direction, apply \autoref{thm:real-regseqrat-finite-request-route-equivalence}. An accepted terminal read over $S$ is accepted only through the selected StreamName window, the DyadicRatCore endpoint-radius-distance ledger, the RegSeqRat classifier-readback-regularity surface, and the local Real seal row, with the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

For the reverse direction, suppose a terminal read factors through exactly these four faces with the displayed support. Apply \autoref{thm:regseqrat-real-terminal-four-face-exactness}. Its four-face exactness repacks the StreamName, DyadicRatCore, RegSeqRat, and Real seal rows as the accepted terminal packet for the public finite request.

It remains to identify uniqueness. First use \autoref{thm:real-regseqrat-finite-request-determinacy}: two accepted terminal reads with the same four faces and support rows determine the same finite Real seal packet.

Then apply \autoref{thm:real-streamname-finite-request-terminal-bridge} to identify the StreamName-facing pullback with the Real-facing terminal section on that packet. Finally use \autoref{thm:real-l10-terminal-pullback-uniqueness}; no second completion-facing or Real-facing terminal read can display the same four L10 faces while naming a different finite $\BHist$ packet.

The cited route, exactness, bridge, and pullback-uniqueness statements expose only the listed finite rows. Therefore the equivalence carries no quotient stream equality, host real equality, selected limit, choice data, ambient completeness, metric-completeness row, standard bridge row, or second Real seal coordinate.
\end{proof}

\begin{theorem}[RealUp RegSeqRat Cauchy-witness budget projection]
\label{thm:real-regseqrat-streamname-cauchy-witness-budget-projection}
Let $Q$ be an accepted $\RegSeqRatUp$ packet, let $S$ be a public finite request to a $\RealUp$ seal over $Q$, and let $L=(Q_L,B_L,S_L,K_L,H_L,C_L,P_L,N_L)$ be an accepted $\CauchyWitnessLedgerUp$ budget row whose visible $\StreamNameUp$ finite window is the same window read by $S$. Then the visible budget projection of the combined read is exactly the displayed packet consisting of the $\StreamNameUp$ finite-window row, the $\DyadicRatCoreUp$ endpoint, radius, and finite-distance rows, the $\RegSeqRatUp$ classifier and readback rows, the local $\RealUp$ seal row, and the supporting $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. Equivalently, the Cauchy-witness ledger consumes the same finite packet through its diagonal-budget, observation-budget, synchronizer, and completion-seal rows before any completion-facing classifier is visible.

The projection exports no quotient streams, no host real equality, no selected limits, no choice data, no ambient completeness, no metric-completion rows, no standard bridge rows, and no second $\RealUp$ seal coordinate.
\end{theorem}
\begin{proof}
First apply \autoref{thm:real-regseqrat-finite-request-fusion} and \autoref{thm:real-regseqrat-finite-request-fusion-selector-budget-lock}. They place the public request on one finite packet and force every selector-budget or completion-facing consumer of $S$ to reuse the displayed StreamName, dyadic, RegSeqRat, Real seal, and support rows.

Next use \autoref{thm:real-regseqrat-finite-request-four-face-source-exhaustion}. The visible source has exactly four faces: the StreamName finite window, the dyadic endpoint-radius-distance ledger, the RegSeqRat classifier-readback surface, and the local Real seal row. There is no fifth face from which a quotient stream, selected limit, ambient completeness row, metric-completion row, or bridge row could be read.

Now apply the terminal bridge \autoref{thm:real-streamname-finite-request-terminal-bridge}. Since the ledger row and the Real request display the same finite StreamName window, the StreamName-facing pullback and the Real-facing terminal section name the same finite $\BHist$ packet with the same $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

Finally read the witness-ledger side through \autoref{thm:cauchy-witness-ledger-budget-factorization}, \autoref{thm:cauchy-witness-ledger-budget-source-coherence}, and \autoref{thm:cauchy-witness-ledger-root-source-lock}. These rows order every accepted completion-facing read through the same diagonal-budget, observation-budget, synchronizer, and completion-seal route, and they identify the ledger's $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, and $\RealUp$ source surface with the one already fixed by the finite request. Thus all budget consumers reuse the same finite packet, and the excluded quotient, host, selected-limit, choice, completeness, metric-completion, bridge, and duplicate-seal data remain outside the projection.
\end{proof}

\begin{theorem}[RealUp RegSeqRat StreamName dyadic tail-budget pullback]
\label{thm:real-regseqrat-streamname-dyadic-tail-budget-pullback}
Let $Q$ be an accepted $\RegSeqRatUp$ packet, let $S$ be a public finite request to a $\RealUp$ seal over $Q$, and let $L=(E,T,W,D,R,A,H,C,P,N)$ be an accepted $\DyadicTailRadiusLedgerUp$ row whose visible $\StreamNameUp$ finite window $W$ is the same window selected by $S$. Then the $\RealUp$ finite-request packet pulls back exactly to the shared finite packet consisting of the $\StreamNameUp$ window $W$, the $\DyadicRatCoreUp$ endpoint, radius, and finite-distance rows in $D$, the $\RegSeqRatUp$ classifier, readback, and regularity rows of $Q$ and $R$, the local $\RealUp$ seal row $A$, and the supporting $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows $H,C,P,N$.

Equivalently, every completion-facing or $\RealUp$-facing read of $S$ that is mediated by the tail-radius ledger $L$ is accepted exactly through the same four L10 faces: the selected StreamName window, the DyadicRatCore endpoint-radius-distance ledger, the RegSeqRat classifier and readback surface, and the local Real seal row. The pullback exports no quotient streams, no host real equality, no selected limits, no choice data, no ambient completeness, no metric-completion rows, no standard bridge rows, and no duplicate $\RealUp$ seal coordinates.
\end{theorem}
\begin{proof}
Start with the Real finite request. By \autoref{thm:real-regseqrat-finite-request-four-face-source-exhaustion}, the accepted source of $S$ has exactly the StreamName window face, the DyadicRatCore endpoint-radius-distance face, the RegSeqRat classifier-readback-regularity face, and the local $\NameCert_{\RealUp}$ seal face.

Next apply \autoref{thm:real-regseqrat-finite-request-route-equivalence}. A completion-facing read over $S$ is accepted precisely when it factors through those four faces together with the displayed $\BHist$, $\hsame$, $\Cont$, and $\Pkg$ support. Thus the Real-facing section has no coordinate outside the finite L10 packet.

Now use \autoref{thm:real-regseqrat-streamname-cauchy-witness-budget-projection}. Its budget projection identifies the Cauchy-witness budget surface with the same StreamName, DyadicRatCore, RegSeqRat, Real seal, and support rows already fixed by the finite request.

Finally apply the DyadicTailRadiusLedger handoff, namely \autoref{thm:dyadic-tail-radius-ledger-tail-budget-coverage} and \autoref{thm:dyadic-tail-radius-ledger-regseq-real-handoff}. Since $L$ displays the same finite StreamName window $W$ selected by $S$, tail-budget coverage places the precision, tail-window, StreamName-window, and dyadic readback rows on the same finite route, and the handoff carries that route through the RegSeqRat and Real seal rows. Repacking these rows gives exactly the shared packet $W,D,Q,R,A,H,C,P,N$, with no quotient stream, host equality, selected limit, choice, completeness, metric-completion, bridge, or duplicate Real-seal coordinate available to the pullback.
\end{proof}

\begin{theorem}[RealUp RegSeqRat reindexed finite-request descent]
\label{thm:real-regseqrat-reindexed-finite-request-descent}
Let $Q_0$ and $Q_1$ be accepted $\RegSeqRatUp$ packets, and let $S_0$ and $S_1$ be accepted finite requests to $\RealUp$ seals over them. Suppose the selected $\StreamNameUp$ finite windows of $S_0$ and $S_1$ are related by a certified public reindex route. Suppose also that the route preserves the same $\DyadicRatCoreUp$ endpoint, radius, and finite-distance ledger, the same $\RegSeqRatUp$ classifier and readback rows, the same local $\RealUp$ seal row, and the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support rows. Then $S_0$ and $S_1$ determine the same finite $\RealUp$ seal packet.

Conversely, equality of the accepted finite $\RealUp$ seal packet recovers exactly that reindexed four-face data: the transported StreamName window, the DyadicRatCore endpoint-radius-distance ledger, the RegSeqRat classifier and readback rows, the local Real seal row, and the displayed support rows $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$. The descent exports no quotient stream equality, no host real equality, no selected limits, no choice data, no ambient completeness, no metric-completeness rows, no standard bridge rows, and no duplicate $\RealUp$ seal coordinates.
\end{theorem}
\begin{proof}
First apply \autoref{thm:streamname-reindex-terminal-coverage}. The certified public reindex route carries the selected StreamName finite window to the same terminal four-face exit route, with the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows transported componentwise.

Next use \autoref{thm:real-streamname-finite-request-terminal-bridge}. The StreamName-facing pullback and the Real-facing finite request therefore read one finite $\BHist$ packet whenever they display the same StreamName window, dyadic ledger, RegSeqRat readback, Real seal row, and support rows.

Now apply \autoref{thm:real-regseqrat-finite-request-route-equivalence}. Acceptance of either request is exactly factorization through the four L10 faces: the selected StreamName window, the DyadicRatCore endpoint-radius-distance ledger, the RegSeqRat classifier-readback surface, and the local Real seal row, together with the displayed finite support.

Finally use \autoref{thm:real-regseqrat-streamname-dyadic-tail-budget-pullback}. The preserved dyadic tail-budget ledger identifies the Real-facing finite packet with the same StreamName, DyadicRatCore, RegSeqRat, Real seal, and support rows on both sides of the reindex route. Thus the two accepted requests determine the same finite $\RealUp$ seal packet.

For the converse, read equality of the accepted finite Real seal packet through the route equivalence just cited. Since the packet has no hidden coordinate beyond the four faces and displayed support, equality recovers exactly the transported StreamName window, dyadic ledger, RegSeqRat classifier and readback rows, local seal row, and $\BHist/\hsame/\Cont/\Pkg/\NameCert$ support rows. The same boundary exclusions keep quotient stream equality, host real equality, selected limits, choice data, ambient completeness, metric-completeness rows, standard bridge rows, and duplicate Real seal coordinates outside the conclusion.
\end{proof}

\begin{theorem}[RealUp RegSeqRat tail-witness budget agreement]
\label{thm:real-regseqrat-tail-witness-budget-agreement}
Let $Q$ be an accepted $\RegSeqRatUp$ packet, let $S$ be a public finite request to a $\RealUp$ seal over $Q$, let $L$ be an accepted $\CauchyWitnessLedgerUp$ budget row, and let $T$ be an accepted $\DyadicTailRadiusLedgerUp$ row. Suppose that the visible $\StreamNameUp$ finite window read by $L$ is the same finite window selected by $S$, and that the visible $\StreamNameUp$ finite window read by $T$ is the same selected window. Then the Cauchy-witness budget projection and the dyadic tail-budget pullback determine one and the same finite four-face $\RealUp$ packet: the selected $\StreamNameUp$ window, the $\DyadicRatCoreUp$ endpoint, radius, and finite-distance rows, the $\RegSeqRatUp$ classifier, readback, and regularity rows, the local $\RealUp$ seal row, and the displayed $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ support.

Conversely, equality of that finite four-face packet recovers agreement of the two displayed budget routes on precisely those rows. The agreement exports no quotient stream equality, no host real equality, no selected limit, no choice data, no ambient completeness, no metric-completeness row, no standard bridge row, and no duplicate $\RealUp$ seal coordinate.
\end{theorem}
\begin{proof}
First apply \autoref{thm:real-regseqrat-streamname-cauchy-witness-budget-projection}. The accepted CauchyWitnessLedger row $L$ projects to the selected StreamName window, the dyadic endpoint-radius-distance rows, the RegSeqRat classifier and readback surface, the local Real seal row, and the displayed finite support rows.

Next apply \autoref{thm:real-regseqrat-streamname-dyadic-tail-budget-pullback}. Since $T$ displays the same selected StreamName window, its tail-radius route pulls the Real finite request back to the identical StreamName, DyadicRatCore, RegSeqRat, local seal, and $\BHist/\hsame/\Cont/\Pkg/\NameCert$ support surface.

Now use \autoref{thm:real-regseqrat-finite-request-route-equivalence}. Acceptance through either budget route is exactly acceptance through the four L10 faces, so neither route contributes a fifth coordinate or any completion-facing row outside the finite packet.

It remains to identify the two budget descriptions. Apply \autoref{thm:real-regseqrat-reindexed-finite-request-descent} with the identity public reindex route on the shared selected StreamName window. The descent theorem identifies the two accepted finite Real seal packets because their StreamName, dyadic, RegSeqRat, local seal, and support rows agree.

For the converse, read equality of the shared packet back through the same descent and route-equivalence statements. Since the packet contains only the four displayed faces and support rows, equality recovers exactly the Cauchy-witness projection route and the dyadic tail-budget pullback route on those rows, while the listed quotient, host, limit, choice, completeness, metric-completion, standard bridge, and duplicate-seal data remain outside the conclusion.
\end{proof}
