\closureat{ContinuousUp}{matureStr}[BEDC.Derived.ContinuousUp.ContinuousFunctionCarrier\_certificate\_semanticNameCert]
\begin{closurestatus}{\ContinuousUp}
  \constructivestory{A continuous map is carried by a graph-and-modulus history packet: source, target, map code, modulus, and certificate histories are unary, the graph ledger continues the source through the map to the target, and the modulus ledger continues the target through the modulus to the certificate. Two such witnesses are classified by the graph outputs agreeing under the target classifier for classified source inputs and by their unary modulus ledgers mutually refining each requested precision; on the checked carrier rows this is enforced through $\hsame$ determinacy of the displayed continuation fields. The source consists of certified metric spaces and the totally bounded source discipline, the pattern records graph-with-modulus formation, identity, composition, prefix and restriction examples, terminal-modulus extension, and modulus-chain exactness, and the ledger keeps the graph relation, metric ledgers, precision history, modulus history, and certificate history visible. The five name-certificate obligations close because the identity map uses the empty map code with a one-step modulus witness, $\hsame$ supplies reflexive, symmetric, and transitive comparison of the displayed histories, and carrier transport follows by moving each continuation ledger across history-sameness. A carrier row lies in the signature gap of a $\ContinuousUp$ token $p$ when the source-domain accepts the graph-and-modulus packet, the signature relation pairs it with an answer packet and evidence ledger, and token introduction names that answer as $p$; unfolded, the graph sends each accepted source history to the encoded target history and the modulus ledger supplies the precision conversion recorded by $p$. The checked standard bridge reads the same relation-plus-ledger data as a continuous-map boundary and reads it back without host-level function extensionality or a numeric modulus object. The chapter registers this graph-modulus construction under the name $\ContinuousUp$; continuity is an explicit BEDC certificate surface, not a host function space, completeness theorem, or external map equality principle.}
  \theoryclosure{\matureClosure}
  \scopeclosed{The mature paper scope binds the ContinuousFunctionCarrier semantic certificate, modulus-witness determinacy and exactness rows, identity, composition, prefix, and restriction examples, and the ContinuousMapUp consumer surface cited by the chapter.}
  \formalstatus{\bridgeCheckedV}
  \leantarget{BEDC.Derived.ContinuousUp.ContinuousFunctionCarrier\_certificate\_semanticNameCert}
  \bridgestatus{bridgeChecked}
  \notclaimed{The chapter does not close host functions, host function spaces, or standard-model completeness.}
  \upgradepath{no further upgrade -- chapter at top of formal axis}
\end{closurestatus}
