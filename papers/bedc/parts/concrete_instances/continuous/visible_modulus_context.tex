\begin{theorem}[Continuous function carrier visible modulus context exactness]
\label{thm:continuous-function-carrier-visible-modulus-context-iff}
For histories $p,q,source,map,target,modulus,cert$, a visible prefix on the
source, target, and certificate ledgers together with a visible suffix on the
modulus and certificate ledgers is exactly unary evidence for the two context
histories together with the central continuous-function carrier:
$$
\mathsf{ContinuousFunctionCarrier}(p\cdot source,map,p\cdot target,
modulus\cdot q,(p\cdot cert)\cdot q)
\Longleftrightarrow
\mathsf{UnaryHistory}(p)\wedge\mathsf{UnaryHistory}(q)\wedge
\mathsf{ContinuousFunctionCarrier}(source,map,target,modulus,cert).
$$
\end{theorem}
\begin{proof}
Peel the right suffix from the modulus and certificate ledgers by modulus
suffix exactness. The remaining carrier has the common left prefix on source,
target, and certificate, so carrier prefix exactness recovers the unary prefix
and the central carrier. Conversely, rebuild the prefixed carrier first, then
reattach the modulus suffix.
\end{proof}
\leanchecked{BEDC.Derived.ContinuousUp.ContinuousFunctionCarrier\_visible\_modulus\_context\_iff}

\begin{theorem}[Continuous modulus chain visible context exactness]
\label{thm:continuous-modulus-chain-visible-context-iff}
For histories $p,q,source,first,second,target$, a visible prefix on the source
and target together with a visible suffix on the second modulus and target is
exactly unary evidence for the two contexts together with the central
two-link chain:
$$
\mathsf{ContinuousModulusChain}(p\cdot source,first,second\cdot q,
(p\cdot target)\cdot q)
\Longleftrightarrow
\mathsf{UnaryHistory}(p)\wedge\mathsf{UnaryHistory}(q)\wedge
\mathsf{ContinuousModulusChain}(source,first,second,target).
$$
\end{theorem}
\begin{proof}
Peel the right suffix from the second modulus and target by chain suffix
exactness. The remaining chain has the common left prefix on source and
target, so chain prefix exactness exposes unary $p$ and the central chain.
Conversely, rebuild the prefixed chain first, then reattach the suffix.
\end{proof}
\leanchecked{BEDC.Derived.ContinuousUp.ContinuousModulusChain\_visible\_context\_iff}

\begin{theorem}[Continuous modulus chain visible context second determinacy]
\label{thm:continuous-modulus-chain-visible-context-second-deterministic}
For histories $p,q,source,first,second,second',target$, if two visible-context
two-link modulus chains share the same visible source, first modulus, and
visible target,
$$
\mathsf{ContinuousModulusChain}(p\cdot source,first,second\cdot q,
(p\cdot target)\cdot q)
\quad\hbox{and}\quad
\mathsf{ContinuousModulusChain}(p\cdot source,first,second'\cdot q,
(p\cdot target)\cdot q),
$$
then $second\hsame second'$.
\end{theorem}
\begin{proof}
Apply the visible-context exactness statement to both chains. This removes the
common prefix and suffix contexts and exposes two central chains with the same
source, first modulus, and target. The ordinary second-modulus determinacy
theorem identifies the two central second moduli.
\end{proof}
\leanchecked{BEDC.Derived.ContinuousUp.ContinuousModulusChain\_visible\_context\_second\_deterministic}

\begin{theorem}[Continuous function carrier visible-context field determinacy]
\label{thm:continuous-function-carrier-visible-modulus-context-field-determinacy}
For histories $p,q,source,source',map,map',target,target',modulus,modulus',
cert,cert'$, visible-modulus-context continuous-function carriers determine
each central public field as follows.
\begin{enumerate}
\item Same visible source, target, modulus, and certificate determine the map:
$$
\begin{array}{l}
\mathsf{ContinuousFunctionCarrier}(p\cdot source,map,p\cdot target,
modulus\cdot q,(p\cdot cert)\cdot q)
\Longrightarrow
\mathsf{ContinuousFunctionCarrier}(p\cdot source,map',p\cdot target,
modulus\cdot q,(p\cdot cert)\cdot q)
\Longrightarrow
\hsame(map,map').
\end{array}
$$
\item Same visible map, target, modulus, and certificate determine the source:
$$
\begin{array}{l}
\mathsf{ContinuousFunctionCarrier}(p\cdot source,map,p\cdot target,
modulus\cdot q,(p\cdot cert)\cdot q)
\Longrightarrow
\mathsf{ContinuousFunctionCarrier}(p\cdot source',map,p\cdot target,
modulus\cdot q,(p\cdot cert)\cdot q)
\Longrightarrow
\hsame(source,source').
\end{array}
$$
\item Same visible source, map, target, and certificate determine the modulus:
$$
\begin{array}{l}
\mathsf{ContinuousFunctionCarrier}(p\cdot source,map,p\cdot target,
modulus\cdot q,(p\cdot cert)\cdot q)
\Longrightarrow
\mathsf{ContinuousFunctionCarrier}(p\cdot source,map,p\cdot target,
modulus'\cdot q,(p\cdot cert)\cdot q)
\Longrightarrow
\hsame(modulus,modulus').
\end{array}
$$
\item Same visible source, map, and modulus determine the target and
certificate:
$$
\begin{array}{l}
\mathsf{ContinuousFunctionCarrier}(p\cdot source,map,p\cdot target,
modulus\cdot q,(p\cdot cert)\cdot q)
\Longrightarrow
\mathsf{ContinuousFunctionCarrier}(p\cdot source,map,p\cdot target',
modulus\cdot q,(p\cdot cert')\cdot q)
\Longrightarrow
\hsame(target,target')\land\hsame(cert,cert').
\end{array}
$$
\end{enumerate}
\end{theorem}
\begin{proof}
\begin{enumerate}
\item In the map clause, apply
Theorem~\ref{thm:continuous-function-carrier-visible-modulus-context-iff} to
the two displayed visible carriers. The exposed central carriers share source,
target, modulus, and certificate, so
Theorem~\ref{thm:continuous-function-carrier-map-deterministic} gives
$\hsame(map,map')$.

\item In the source clause, apply the same exactness theorem to the two
displayed visible carriers. The exposed central carriers share map, target,
modulus, and certificate, so
Theorem~\ref{thm:continuous-function-carrier-source-deterministic} gives
$\hsame(source,source')$.

\item In the modulus clause, apply the same exactness theorem to the two
displayed visible carriers. The exposed central carriers share source, map,
target, and certificate, so
Theorem~\ref{thm:continuous-function-carrier-modulus-deterministic} gives
$\hsame(modulus,modulus')$.

\item In the target-certificate clause, apply the same exactness theorem to
the two displayed visible carriers. The exposed central carriers share source,
map, and modulus, so
Theorem~\ref{thm:continuous-function-carrier-target-cert-deterministic} gives
$\hsame(target,target')\land\hsame(cert,cert')$.
\end{enumerate}
\end{proof}

\begin{theorem}[Continuous function carrier visible-context target-certificate determinacy]
\label{thm:continuous-function-carrier-visible-modulus-context-target-cert-deterministic}
For histories $p,q,source,map,target,target',modulus,cert,cert'$, if both
visible-modulus-context carrier presentations
$$
\mathsf{ContinuousFunctionCarrier}(p\cdot source,map,p\cdot target,
modulus\cdot q,(p\cdot cert)\cdot q)
$$
and
$$
\mathsf{ContinuousFunctionCarrier}(p\cdot source,map,p\cdot target',
modulus\cdot q,(p\cdot cert')\cdot q)
$$
hold, then the public target and certificate ledgers are determined by the
central presentation:
$$
\hsame(target,target')\land\hsame(cert,cert').
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:continuous-function-carrier-visible-modulus-context-iff}
to the first visible carrier. This recovers the unary context histories
$\mathsf{UnaryHistory}(p)$ and $\mathsf{UnaryHistory}(q)$ together with the
central witness
$\mathsf{ContinuousFunctionCarrier}(source,map,target,modulus,cert)$.

Apply the same exactness theorem to the second visible carrier. The common
visible prefix and suffix are peeled in the same way, producing the central
witness
$\mathsf{ContinuousFunctionCarrier}(source,map,target',modulus,cert')$.

Now apply
Theorem~\ref{thm:continuous-function-carrier-target-cert-deterministic} to the
two central carrier witnesses. It gives the displayed
$\hsame(target,target')$ and $\hsame(cert,cert')$ conclusions, which are already
the public target and certificate comparisons required here.

The ten-field certificate fit is the following. The traditional target is
public target-certificate determinacy for visible continuous-function carrier
presentations. The BEDC source is $\mathsf{BHist}$ equipped with
$\mathsf{Cont}$. The carrier is $\mathsf{ContinuousFunctionCarrier}$ and the
classifier is $\hsame$. The stability obligation is visible-context public
readback. The ledger is source, map, target, modulus, and certificate histories
together with the visible prefix and suffix contexts. The core theorem is this
result. The exactness predicate is the displayed pair of $\hsame$ conclusions.
The standard bridge is deferred, and the status is $\mathsf{ProofTarget}$.
\end{proof}

\leanchecked{BEDC.Derived.ContinuousUp.ContinuousFunctionCarrier\_visible\_modulus\_context\_target\_cert\_deterministic}

\begin{theorem}[Continuous function carrier visible-context map determinacy]
\label{thm:continuous-function-carrier-visible-modulus-context-map-deterministic}
For histories $p,q,source,map,map',target,modulus,cert$, if both
visible-modulus-context carrier presentations
$$
\mathsf{ContinuousFunctionCarrier}(p\cdot source,map,p\cdot target,
modulus\cdot q,(p\cdot cert)\cdot q)
$$
and
$$
\mathsf{ContinuousFunctionCarrier}(p\cdot source,map',p\cdot target,
modulus\cdot q,(p\cdot cert)\cdot q)
$$
hold, then the public graph map ledger is determined by the central
presentation:
$$
\hsame(map,map').
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:continuous-function-carrier-visible-modulus-context-iff}
to the first visible carrier. This removes the shared visible prefix and suffix
contexts and exposes the central witness
$\mathsf{ContinuousFunctionCarrier}(source,map,target,modulus,cert)$.

Apply the same visible-context exactness theorem to the second carrier. The same
source, target, modulus, and certificate ledgers remain, producing
$\mathsf{ContinuousFunctionCarrier}(source,map',target,modulus,cert)$.

Now apply Theorem~\ref{thm:continuous-function-carrier-map-deterministic} to the
two central carrier witnesses. It gives the displayed $\hsame(map,map')$
comparison, which is already the public visible-context map readback required
here.

The ten-field certificate fit is the following. The traditional target is
visible map-ledger uniqueness. The BEDC source is $\mathsf{BHist}$ equipped with
$\mathsf{Cont}$. The carrier is $\mathsf{ContinuousFunctionCarrier}$ and the
classifier is $\hsame$. The stability obligation is visible-context map
readback. The ledger is source, map, target, modulus, and certificate histories
together with the visible prefix and suffix contexts. The core theorem is this
result. The exactness predicate is the displayed $\hsame$ conclusion. The
standard bridge is deferred, and the status is $\mathsf{ProofTarget}$.
\end{proof}
\begin{theorem}[Continuous function carrier visible-context graph-modulus determinacy]
\label{thm:continuous-function-carrier-visible-modulus-context-graph-modulus-deterministic}
For histories $p,q,source,map,map',target,modulus,modulus',cert$, if both
visible-modulus-context carrier presentations
$$
\mathsf{ContinuousFunctionCarrier}(p\cdot source,map,p\cdot target,
modulus\cdot q,(p\cdot cert)\cdot q)
$$
and
$$
\mathsf{ContinuousFunctionCarrier}(p\cdot source,map',p\cdot target,
modulus'\cdot q,(p\cdot cert)\cdot q)
$$
hold, then the public graph and modulus ledgers are simultaneously determined:
$$
\hsame(map,map')\land\hsame(modulus,modulus').
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:continuous-function-carrier-visible-modulus-context-iff}
to both visible carrier hypotheses. The common displayed prefix and suffix are
peeled away, leaving central carriers over
$(source,map,target,modulus,cert)$ and
$(source,map',target,modulus',cert)$.

Apply
Theorem~\ref{thm:continuous-function-carrier-graph-modulus-cont-readback} to
the two central carriers. This exposes graph ledgers
$\mathsf{Cont}(source,map,target)$ and
$\mathsf{Cont}(source,map',target)$, and certificate ledgers
$\mathsf{Cont}(target,modulus,cert)$ and
$\mathsf{Cont}(target,modulus',cert)$.

Continuation left cancellation on the two graph ledgers gives
$\hsame(map,map')$. The same cancellation principle on the two
target-certificate ledgers gives $\hsame(modulus,modulus')$, and the two
comparisons are paired as the displayed conclusion.

The ten-field certificate fit is: traditional target visible
continuous-function determinacy; BEDC source $\mathsf{BHist}$ with
$\mathsf{Cont}$; carrier $\mathsf{ContinuousFunctionCarrier}$; classifier
$\hsame$; stability obligation visible-context public readback; ledger source,
map, target, modulus, certificate, prefix, and suffix; core theorem this
statement; exactness the displayed pair of $\hsame$ conclusions; standard
bridge deferred; status $\mathsf{ProofTarget}$.
\end{proof}
\leanchecked{BEDC.Derived.ContinuousUp.ContinuousFunctionCarrier\_visible\_modulus\_context\_graph\_modulus\_deterministic}

\begin{theorem}[Continuous function carrier visible-context cycle tails empty]
\label{thm:continuous-function-carrier-visible-modulus-context-cycle-tails-empty}
For histories $p,q,source,map,target,modulus,cert$, a visible-context
continuous-function carrier whose central source and certificate are
history-same has empty central graph and modulus tails:
$$
\begin{array}{l}
\mathsf{ContinuousFunctionCarrier}(p\cdot source,map,p\cdot target,
modulus\cdot q,(p\cdot cert)\cdot q)
\Longrightarrow
\hsame(source,cert)
\Longrightarrow
\hsame(map,\emp)\land\hsame(modulus,\emp).
\end{array}
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:continuous-function-carrier-visible-modulus-context-iff}
to the displayed visible carrier. This recovers unary witnesses for the visible
prefix $p$ and suffix $q$, and it exposes the central carrier
$\mathsf{ContinuousFunctionCarrier}(source,map,target,modulus,cert)$.

Apply
Theorem~\ref{thm:continuous-function-carrier-endpoint-cert-cycle-tails-empty}
to the central carrier and the displayed $\hsame(source,cert)$ cycle. The
endpoint-certificate cycle cancellation theorem gives exactly
$\hsame(map,\emp)\land\hsame(modulus,\emp)$.

The ten-field certificate fit is the following. The traditional target is
visible cycle-tail pruning. The BEDC source is $\mathsf{BHist}$ with
$\mathsf{Cont}$. The carrier is $\mathsf{ContinuousFunctionCarrier}$ and the
classifier is $\hsame$. The stability obligation is visible-context central
readback. The ledger is source, map, target, modulus, certificate, prefix, and
suffix histories. The core theorem is this statement. The exactness predicate is
the displayed pair of empty-tail conclusions. The standard bridge is deferred,
and the status is $\mathsf{ProofTarget}$.
\end{proof}

\begin{theorem}[Continuous function carrier visible-context composition closure]
\label{thm:continuous-function-carrier-visible-modulus-context-comp-closed}
For histories $p,q,source,middle,target,f,g,fg,modF,modG,modFG,certF,certG,cert$,
suppose the two contextualized carriers
$$
\mathsf{ContinuousFunctionCarrier}(p\cdot source,f,p\cdot middle,
modF\cdot q,(p\cdot certF)\cdot q)
$$
and
$$
\mathsf{ContinuousFunctionCarrier}(p\cdot middle,g,p\cdot target,
modG\cdot q,(p\cdot certG)\cdot q)
$$
hold. If the graph, modulus, and target-certificate ledgers
$$
\mathsf{Cont}(f,g,fg),\quad
\mathsf{Cont}(modF,modG,modFG),\quad
\mathsf{Cont}(target,modFG,cert)
$$
hold, then the composite carrier remains inside the same visible context:
$$
\mathsf{ContinuousFunctionCarrier}(p\cdot source,fg,p\cdot target,
modFG\cdot q,(p\cdot cert)\cdot q).
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:continuous-function-carrier-visible-modulus-context-iff}
to the first contextualized carrier. This recovers unary witnesses for the
visible prefix $p$ and suffix $q$, together with the central carrier
$\mathsf{ContinuousFunctionCarrier}(source,f,middle,modF,certF)$.

Apply the same visible-context exactness theorem to the second contextualized
carrier. The common contexts are peeled away, exposing
$\mathsf{ContinuousFunctionCarrier}(middle,g,target,modG,certG)$ over the same
central middle and target histories.

Apply Theorem~\ref{thm:continuous-function-carrier-comp-closed} to these two
central carriers with the displayed graph, modulus, and target-certificate
ledgers. The result is the central composite carrier
$\mathsf{ContinuousFunctionCarrier}(source,fg,target,modFG,cert)$.

Finally use Theorem~\ref{thm:continuous-function-carrier-visible-modulus-context-iff}
in the reverse direction, with the recovered unary witnesses for $p$ and $q$,
to reattach the visible prefix and suffix. This yields the displayed composite
carrier in the same visible context.

The ten-field certificate fit is the following. The traditional target is
composition closure for contextual continuous-function carriers. The BEDC
source is $\mathsf{BHist}$ equipped with $\mathsf{Cont}$. The carrier is
$\mathsf{ContinuousFunctionCarrier}$ and the classifier is $\hsame$. The
stability obligation is contextual composition closure with fixed visible
prefix and suffix. The ledger consists of source, middle, target, graph map,
modulus, certificate, visible prefix, and visible suffix histories. The core
theorem is this visible-context closure statement. The exactness claim is the
reverse use of visible-context exactness after central composition closure. The
standard bridge is deferred, and the status is $\mathsf{ProofTarget}$.
\end{proof}

\begin{theorem}[Continuous function carrier visible-context composite target-certificate determinacy]
\label{thm:continuous-function-carrier-visible-context-composite-target-cert-determinacy}
For histories
$p,q,source,middle,target,target',f,g,fg,modF,modG,modFG,certF,certG,cert,cert'$,
suppose the two visible-context input carriers
$$
\mathsf{ContinuousFunctionCarrier}(p\cdot source,f,p\cdot middle,
modF\cdot q,(p\cdot certF)\cdot q)
$$
and
$$
\mathsf{ContinuousFunctionCarrier}(p\cdot middle,g,p\cdot target,
modG\cdot q,(p\cdot certG)\cdot q)
$$
hold. If the graph, modulus, and target-certificate ledgers
$$
\mathsf{Cont}(f,g,fg),\quad
\mathsf{Cont}(modF,modG,modFG),\quad
\mathsf{Cont}(target,modFG,cert)
$$
hold, and an alternate visible presentation of the same composite graph and
modulus also holds,
$$
\mathsf{ContinuousFunctionCarrier}(p\cdot source,fg,p\cdot target',
modFG\cdot q,(p\cdot cert')\cdot q),
$$
then the composite target and certificate are determined by the visible
presentation:
$$
\hsame(target,target')\land\hsame(cert,cert').
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:continuous-function-carrier-visible-modulus-context-iff}
to the first visible-context input carrier. This exposes unary witnesses for
the visible prefix $p$ and suffix $q$ together with the central carrier
$\mathsf{ContinuousFunctionCarrier}(source,f,middle,modF,certF)$.

Apply the same visible-context exactness theorem to the second input carrier.
The common contexts are peeled away, giving the central carrier
$\mathsf{ContinuousFunctionCarrier}(middle,g,target,modG,certG)$ over the same
middle and target histories.

Now apply Theorem~\ref{thm:continuous-function-carrier-comp-closed} to the two
central carriers and the displayed graph, modulus, and target-certificate
ledgers. This constructs the central composite carrier
$\mathsf{ContinuousFunctionCarrier}(source,fg,target,modFG,cert)$.

Use Theorem~\ref{thm:continuous-function-carrier-visible-modulus-context-iff}
in the reverse direction with the recovered unary witnesses for $p$ and $q$.
This repacks the central composite carrier into the visible presentation
$\mathsf{ContinuousFunctionCarrier}(p\cdot source,fg,p\cdot target,
modFG\cdot q,(p\cdot cert)\cdot q)$.

Finally apply
Theorem~\ref{thm:continuous-function-carrier-visible-modulus-context-target-cert-deterministic}
to the constructed visible composite and the alternate visible carrier. Its
conclusion is exactly
$\hsame(target,target')\land\hsame(cert,cert')$.

The ten-field certificate fit is the following. The traditional target is
visible composite target-certificate uniqueness. The BEDC source is
$\mathsf{BHist}$ with $\mathsf{Cont}$. The carrier is
$\mathsf{ContinuousFunctionCarrier}$ and the classifier is $\hsame$. The
stability obligation is visible-context composition determinacy. The ledger is
source, middle, target, map, modulus, certificate, prefix, and suffix histories.
The core theorem is this block. The exactness claim is the displayed pair of
$\hsame$ conclusions. The standard bridge is deferred. The downstream consumer
is $\mathsf{ContinuousMapUp}$ composition and category coherence, and the
status is $\mathsf{ProofTarget}$.
\end{proof}

\begin{theorem}[Continuous function carrier visible-context composite ledger determinacy]
\label{thm:continuous-function-carrier-visible-modulus-context-composite-ledger-determinacy}
For histories $p,q,source,middle,target,f,g,fg,fg',modF,modG,modFG,modFG',
certF,certG,cert,cert'$, suppose the two visible-context input carriers
$$
\mathsf{ContinuousFunctionCarrier}(p\cdot source,f,p\cdot middle,
modF\cdot q,(p\cdot certF)\cdot q)
$$
and
$$
\mathsf{ContinuousFunctionCarrier}(p\cdot middle,g,p\cdot target,
modG\cdot q,(p\cdot certG)\cdot q)
$$
hold. If both composite ledger triples
$$
\begin{array}{lll}
\mathsf{Cont}(f,g,fg),&
\mathsf{Cont}(modF,modG,modFG),&
\mathsf{Cont}(target,modFG,cert),\\
\mathsf{Cont}(f,g,fg'),&
\mathsf{Cont}(modF,modG,modFG'),&
\mathsf{Cont}(target,modFG',cert')
\end{array}
$$
hold, then the public composite graph, composite modulus, and composite
certificate ledgers are independent of the chosen admissible triple:
$$
\hsame(fg,fg')\land\hsame(modFG,modFG')\land\hsame(cert,cert').
$$
\end{theorem}
\begin{proof}
Apply Theorem~\ref{thm:continuous-function-carrier-visible-modulus-context-comp-closed}
to the two input carriers and the first composite ledger triple. This builds the
visible composite carrier over $(fg,modFG,cert)$. Apply the same closure theorem
to the same input carriers and the second composite ledger triple, giving the
visible composite carrier over $(fg',modFG',cert')$.

Apply continuation determinacy, Theorem~\ref{thm:continuation-determinacy}, to
the two graph ledgers $\mathsf{Cont}(f,g,fg)$ and
$\mathsf{Cont}(f,g,fg')$. Apply the same determinacy theorem to the two modulus
ledgers $\mathsf{Cont}(modF,modG,modFG)$ and
$\mathsf{Cont}(modF,modG,modFG')$. These give $\hsame(fg,fg')$ and
$\hsame(modFG,modFG')$.

Use Theorem~\ref{thm:continuous-function-carrier-visible-modulus-context-iff}
to peel the visible prefix and suffix from the two composite carriers. Then use
Theorem~\ref{thm:continuous-function-carrier-hsame-transport} to place the two
central composite carriers over a common graph and modulus representative while
leaving the target fixed.

Now Theorem~\ref{thm:continuous-function-carrier-target-cert-deterministic}
applies to the aligned central carriers. Its certificate conclusion gives
$\hsame(cert,cert')$, which combines with the graph and modulus comparisons
above to give the displayed triple classifier.

The ten-field certificate fit is the following. The traditional target is
composite-ledger well-definedness for continuous maps. The BEDC source is
$\mathsf{BHist}$ equipped with $\mathsf{Cont}$. The carrier is
$\mathsf{ContinuousFunctionCarrier}$ and the classifier is $\hsame$. The
stability obligation is composite ledger independence. The ledger consists of
source, middle, target, map, modulus, certificate, prefix, and suffix histories.
The core theorem is this determinacy result. The exactness claim is the
displayed triple classifier. The downstream consumer is
$\mathsf{ContinuousMapUp}$ composition well-definedness. The standard bridge is
deferred, and the status is $\mathsf{ProofTarget}$.
\end{proof}
