\chapter{A Concrete Naming Certificate for $\UpperSemicontinuousUp$}
\label{ch:concrete-instances-uppersemicontinuous-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis, upper semicontinuity is read
through upper-bound and sublevel windows rather than through a host topology.
The $\UpperSemicontinuousUp$ packet is the upper-side companion of
\autoref{ch:concrete-instances-lowersemicontinuous-namecert}: it reads
finite stream windows from \autoref{ch:concrete-instances-streamname-namecert},
regular rational readbacks from \autoref{ch:concrete-instances-regseqrat-namecert},
upper-real comparison rows from \autoref{ch:concrete-instances-upperreal-namecert},
located endpoint rows from \autoref{ch:concrete-instances-locatedreal-namecert},
and the terminal seal of \autoref{ch:concrete-instances-real-namecert}. The
chapter therefore supplies the missing constructive dual interface for the
L10 real-and-completion boundary without identifying it with the lower
epigraph certificate.

\begin{definition}[UpperSemicontinuous carrier]
\label{def:uppersemicontinuous-carrier}
An $\UpperSemicontinuousUp$ carrier is a finite $\mathsf{BHist}$ packet
$$
U=(X,F,S,W,R,O,H,C,P,N)
$$
with the following visible rows:
\begin{enumerate}
\item $X$ is the source carrier row whose accepted points are read through
  displayed $\mathsf{BHist}$ histories and classifier transport;
\item $F$ is the function graph or displayed value row whose finite reads are
  inspected only after a source window has been selected;
\item $S$ is the sublevel ledger, consisting of finite rows $(x,q)$ saying
  that the inspected value is bounded above by the rational threshold $q$ at
  the displayed window;
\item $W$ is the $\StreamNameUp$ schedule row selecting the finite source,
  value, and threshold windows used by each sublevel inspection;
\item $R$ is the $\RegSeqRatUp$ readback row giving the regular rational
  value windows and threshold witnesses consumed by $S$;
\item $O$ is the $\UpperRealUp$, $\LocatedRealUp$, and $\RealUp$ comparison
  ledger used to read upper windows, located endpoint checks, terminal real
  seals, and the finite contrast with lower-side reads;
\item $H$ records componentwise $\hsame$ transport for source histories,
  graph rows, sublevel rows, schedules, regular readbacks, comparison
  ledgers, provenance rows, and local naming rows;
\item $C$ records displayed $\Cont$ replay from source admission through
  schedule selection, regular rational readback, sublevel inspection, located
  readback, and real-seal handoff;
\item $P$ is the $\Pkg$ provenance over $\UpperSemicontinuousUp$,
  $\LowerSemicontinuousUp$, $\UpperRealUp$, $\LocatedRealUp$,
  $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\mathsf{BHist}$, $\hsame$,
  $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\UpperSemicontinuousUp}$ row.
\end{enumerate}
The classifier relates two accepted packets exactly when their source rows,
finite sublevel windows, regular rational readbacks, upper-real comparisons,
located readbacks, terminal $\RealUp$ seal rows, and provenance rows transport
componentwise along $H$ and replay through the same displayed routes in $C$.
No host topology, quotient real equality, selected limit, trichotomy,
countable choice, or $\RealUp$ completeness principle is a carrier
coordinate.
\end{definition}
\leanchecked{BEDC.Derived.UpperSemicontinuousUp.UpperSemicontinuousTasteGate\_single\_carrier\_alignment}

\begin{theorem}[UpperSemicontinuous lower-dual boundary]
\label{thm:uppersemicontinuous-lower-dual}
The accepted $\UpperSemicontinuousUp$ carrier is constructively dual to the
$\LowerSemicontinuousUp$ epigraph surface of
\autoref{ch:concrete-instances-lowersemicontinuous-namecert}: lower
semicontinuity records finite lower-bound rows, while upper semicontinuity
records finite upper-bound and sublevel rows over the same kind of stream
schedule, regular rational readback, located endpoint check, transport,
replay, provenance, and local naming route. This duality compares the two
finite certificate surfaces and does not identify their carriers,
classifiers, or exported $\NameCert$ rows.
\end{theorem}
\leanchecked{BEDC.Derived.UpperSemicontinuousUp.UpperSemicontinuousLowerDualBoundary}
\leanvariant{BEDC.Derived.UpperSemicontinuousUp.UpperSemicontinuousCarrier\_lower\_dual\_boundary}
\begin{proof}
Read the lower-side packet through
\autoref{def:lower-semicontinuous-carrier}. Its distinguishing ledger is the
finite epigraph row of lower rational bounds. Read the upper-side packet
through \autoref{def:uppersemicontinuous-carrier}. Its distinguishing ledger
is the finite sublevel row of upper rational bounds.

The common route consists of displayed source histories, stream schedules,
regular rational readbacks, located endpoint checks, $\RealUp$ seal rows,
componentwise $\hsame$ transport, $\Cont$ replay, package provenance, and a
local name. These coordinates support a boundary comparison because every
inspection on either side is finite and routed through named sibling
certificates.

The two packets remain separate certificates. The lower carrier exports
epigraph-facing rows; the upper carrier exports sublevel-facing rows. Since
their local $\NameCert$ rows are different coordinates and neither carrier
contains a quotient real equality or selected limit coordinate, the duality is
a constructive boundary relation rather than an identification.
\end{proof}

\begin{theorem}[UpperSemicontinuous finite-window stability]
\label{thm:uppersemicontinuous-finite-window-stability}
Every upper-bound read from an accepted $\UpperSemicontinuousUp$ carrier
$U=(X,F,S,W,R,O,H,C,P,N)$ factors through the finite seal route
$$
W,R,O,H,C,P,N
$$
where $W$ is the $\StreamNameUp$ schedule, $R$ is the $\RegSeqRatUp$
readback, $O$ is the $\UpperRealUp$, $\LocatedRealUp$, and $\RealUp$
comparison ledger, and $H,C,P,N$ are the transport, replay, provenance, and
local naming rows. No consumer can read an upper bound by bypassing that
finite route through host topology, quotient real equality, a selected limit,
trichotomy, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with an upper-bound inspection of $U$. The source and value rows are
not read directly: the schedule $W$ first selects the finite source, value,
and threshold windows that the inspection may observe.

The readback row $R$ supplies the regular rational data attached to those
windows. The sublevel ledger $S$ records the upper-bound row $(x,q)$ for that
same inspection, and the comparison ledger $O$ checks the row against the
$\UpperRealUp$, $\LocatedRealUp$, and terminal $\RealUp$ surfaces.

Transport and replay are exhausted by $H$ and $C$. They move the source,
graph, sublevel, readback, comparison, and seal rows together, so the same
finite route remains visible after classifier transport.

Finally, $P$ and $N$ expose exactly this route to downstream consumers. Since
the carrier has no coordinate for host topology, quotient equality, selected
limits, trichotomy, countable choice, or real completeness, those principles
cannot be obtained from an upper-bound read.
\end{proof}
\leanchecked{BEDC.Derived.UpperSemicontinuousUp.UpperSemicontinuousFiniteWindowStability}

\begin{theorem}[Upper semicontinuous carrier window]\label{thm:upper-semicontinuous-carrier-window}
Every accepted $\UpperSemicontinuousUp$ carrier exposes a finite sublevel-window row over the displayed $\RealUp$, $\RegSeqRatUp$, and $\DyadicRatCoreUp$ observations. In particular, any downstream upper-bound read must factor through
$$
S,\ W,\ R,\ O,\ E,\ H,\ C,\ P,\ N
$$
before it treats the packet as an upper semicontinuous classifier.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:uppersemicontinuous-carrier}. The source and sublevel rows $S$ and $W$ display the finite window, while $R$ supplies the regular rational readback and $O$ compares it against the upper-real and located-real ledgers.

The terminal seal $E$ is therefore reached only after the same finite inspection. Transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$ preserve the displayed rows and add no hidden host topology, quotient equality, selected limit, trichotomy, countable choice, or ambient real-completeness coordinate.
\end{proof}

\begin{theorem}[Upper semicontinuous NameCert obligations]\label{thm:upper-semicontinuous-namecert-obligations}
For the carrier of \autoref{def:uppersemicontinuous-carrier}, the local $\NameCert_{\UpperSemicontinuousUp}$ obligation surface consists exactly of carrier admission, sublevel-window exactness, upper-real classifier stability, located readback compatibility, componentwise transport, displayed replay, provenance, and local NameCert rows.
\end{theorem}
\begin{proof}
Carrier admission is the displayed tuple itself. Sublevel-window exactness is read from $W$ against the source history $S$, and the readback row $R$ supplies the only regular rational observations available to the packet.

Upper-real classifier stability and located readback compatibility are both mediated by the comparison ledger $O$ before the terminal seal $E$ can be read. The remaining rows $H,C,P,N$ are respectively the transport, replay, provenance, and local naming coordinates, so the listed obligations exhaust the finite packet.
\end{proof}

\begin{theorem}[Upper semicontinuous real handoff]\label{thm:upper-semicontinuous-real-handoff}
Every $\RealUp$-facing consumer of an accepted $\UpperSemicontinuousUp$ packet must pass through the displayed sublevel-window row and the located readback rows before reading the terminal $\RealUp$ seal. No consumer route may expose the seal while hiding $S,W,R,O,H,C,P,N$.
\end{theorem}
\begin{proof}
Start with a consumer that reads the terminal row $E$. By \autoref{thm:upper-semicontinuous-carrier-window}, that read factors through the finite window $W$, the regular rational readback $R$, and the comparison ledger $O$ over the upper-real and located-real surfaces.

Transport and replay cannot remove those rows because the accepted carrier exposes only componentwise $\hsame$ movement through $H$ and displayed $\Cont$ replay through $C$. Provenance and local naming then record the same handoff through $P$ and $N$, so the Real-facing route remains finite and visible.
\end{proof}

\begin{theorem}[UpperSemicontinuous sublevel carrier transport]
\label{thm:uppersemicontinuous-sublevel-carrier-transport}
Let $U=(X,F,S,W,R,O,H,C,P,N)$ and $U'=(X',F',S',W',R',O',H',C',P',N')$ be accepted $\UpperSemicontinuousUp$ carriers. If the displayed componentwise $\hsame$ rows identify the source histories, graph rows, sublevel rows, StreamName schedules, RegSeqRat readbacks, UpperReal and LocatedReal comparison rows, Real-seal rows, replay rows, provenance rows, and local NameCert rows, then every sublevel carrier read transported from $U$ to $U'$ is still a read of the same finite obligation surface.
\end{theorem}
\begin{proof}
Project the two carriers through \autoref{def:uppersemicontinuous-carrier}. The sublevel carrier surface is not a separate object: it is the joint row package $X,F,S,W,R,O,H,C,P,N$.

Read the transport row $H$. It moves the source, graph, sublevel, StreamName, RegSeqRat, comparison, replay, provenance, and local naming coordinates componentwise. Thus a transported sublevel read preserves the source row $X$, the graph row $F$, and the sublevel ledger $S$ together with the schedule and readback rows $W,R$.

Now replay the transported row through $C$. The displayed $\Cont$ route still passes through the UpperReal, LocatedReal, and terminal $\RealUp$ comparison ledger $O$ before the provenance and naming rows $P,N$ can be consumed. Hence transport preserves the full finite obligation surface and adds no hidden topology, quotient equality, selected limit, trichotomy, countable choice, or real-completeness coordinate.
\end{proof}

\begin{theorem}[UpperSemicontinuous Real-seal non-escape]
\label{thm:uppersemicontinuous-real-seal-nonescape}
Every $\RealUp$-facing upper-bound read of an accepted $\UpperSemicontinuousUp$ packet factors through
$$
W,R,O,H,C,P,N .
$$
The read may use the displayed source and sublevel rows already admitted by the carrier, but it cannot reach the terminal Real seal through host topology, quotient real equality, selected limits, trichotomy, countable choice, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with an upper-bound read that asks for the terminal $\RealUp$ seal. The carrier first exposes the StreamName schedule $W$ and the RegSeqRat readback $R$, so the request is already confined to a finite window before any real-facing comparison is available.

The comparison ledger $O$ is the only row that connects upper-real, located-real, and terminal real data. Therefore the read reaches the seal only after the displayed comparison row has checked the same finite sublevel inspection.

Finally use the structural rows $H,C,P,N$. They transport, replay, source, and name the same route; none of them stores a host topology, quotient equality of reals, selected limit, trichotomy, countable choice, or completeness principle. Thus every Real-facing read remains inside $W,R,O,H,C,P,N$.
\end{proof}

\closureat{UpperSemicontinuousUp}{obligationStr}

\begin{closurestatus}{\UpperSemicontinuousUp}
  \constructivestory{A $\UpperSemicontinuousUp$ packet is generated from displayed source histories, finite sublevel rows, StreamName schedules, RegSeqRat readbacks, UpperReal and LocatedReal comparison ledgers, RealUp seal rows, componentwise hsame transport, displayed Cont replay, package provenance, and a local NameCert row.}
  \theoryclosure{\obligationClosure}
  \scopeclosed{\autoref{def:uppersemicontinuous-carrier}, \autoref{thm:uppersemicontinuous-lower-dual}, \autoref{thm:uppersemicontinuous-finite-window-stability}, \autoref{thm:uppersemicontinuous-sublevel-carrier-transport}, and \autoref{thm:uppersemicontinuous-real-seal-nonescape} bind the finite upper-bound obligation surface over sublevel windows, StreamName schedules, RegSeqRat readbacks, UpperReal and LocatedReal comparison rows, transport, replay, provenance, local naming, and the Real seal.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{The chapter does not close host topology, quotient real equality, selected limits, trichotomy, countable choice, or completeness of $\RealUp$.}
  \upgradepath{The next verification step is a Lean carrier and theorem surface for BEDC.Derived.UpperSemicontinuousUp covering sublevel transport, finite-window stability, Real-seal non-escape, replay, provenance, and local naming.}
\end{closurestatus}
