The $\CantorIntersectionUniformWindowUp$ packet consumes the metric Cantor-intersection row of \autoref{ch:concrete-instances-cantor-intersection-metric-namecert} before exporting its L10 uniform-window handoff. The nested-interval theorem route supplies the finite shrinking-family discipline, while the metric row supplies the diameter and overlap witness read by the uniform window, so the handoff remains a sibling-dependent Real and Completion route rather than an isolated window certificate.
