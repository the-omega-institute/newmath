Every $\CantorIntersectionUniformWindowUp$ read consumes the nested interval sibling routes before exposing its $\RegSeqRatUp$ and $\RealUp$ handoff rows. The packet first reads the $\NestedIntervalUp$ source of \autoref{ch:concrete-instances-nestedinterval-namecert}, then the $\NestedIntervalIntersectionUp$ selector discipline, and then the $\FiniteClosedIntervalNestedIntersectionUp$ route of \autoref{ch:concrete-instances-finite-closed-interval-nested-intersection-namecert}; only after those rows are displayed may the uniform-window, regular rational, Real-seal, transport, provenance, and local $\NameCert_{\CantorIntersectionUniformWindowUp}$ rows be consumed.
