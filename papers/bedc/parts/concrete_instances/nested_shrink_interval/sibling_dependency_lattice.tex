The $\NestedShrinkIntervalUp$ carrier depends on the substantive sibling lattice through \autoref{ch:concrete-instances-nestedinterval-namecert} for the nested interval spine, \autoref{ch:concrete-instances-regseqrat-namecert} for regular rational readback, and \autoref{ch:concrete-instances-real-namecert} for the terminal seal. The rational locator and Cauchy-completion routes already represented by the child files above supply the endpoint-bracketing and completion-admission rows, so the nested-shrink packet is read as a finite dependency route rather than a selected intersection point, quotient-real equality, countable choice, or ambient completeness principle.
