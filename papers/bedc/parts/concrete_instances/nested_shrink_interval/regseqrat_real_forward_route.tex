The shrinking interval diameter route is consumed through the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert} and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. Thus a downstream L10 consumer reads $\NestedShrinkIntervalUp$ only after the finite diameter ledger has been repacked as a $\RegSeqRatUp$ route and sealed as a $\RealUp$ boundary, not as an ambient completeness or selected intersection-point claim.
