\chapter{A Concrete Naming Certificate for $\NestedShrinkIntervalUp$}
\label{ch:concrete-instances-nested-shrink-interval-namecert}
\origin{human}

Following Cantor's nested-interval argument and Bishop--Bridges regular real analysis, the $\NestedShrinkIntervalUp$ packet records the finite BEDC carrier surface for a descending located interval route whose displayed diameters shrink to the requested tolerance. It sits between the nested-closed interval carrier of \autoref{ch:concrete-instances-nested-closed-interval-namecert}, the located endpoint row of \autoref{ch:concrete-instances-locatedinterval-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite stream-window interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback surface of \autoref{ch:concrete-instances-regseqrat-namecert}, and the terminal real-name boundary of \autoref{ch:concrete-instances-real-namecert}. The chapter names only the finite shrinking-interval convergence certificate; it does not claim open-cover compactness, trichotomy, quotient reals, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Nested shrink interval carrier]
\label{def:nested-shrink-interval-carrier}
A $\NestedShrinkIntervalUp$ carrier is a finite $\BHist$ packet
$$
K=(J,L,D,S,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $J$ is the $\NestedClosedIntervalUp$ source row, exposing the retained descending closed located interval family and its containment witnesses.
\item $L$ is the $\LocatedIntervalUp$ endpoint row for the inspected interval windows, including lower and upper endpoint location data.
\item $D$ is the $\DyadicRatCoreUp$ shrink ledger recording endpoint comparisons, diameter estimates, and tolerance requests.
\item $S$ is the $\StreamNameUp$ finite-window schedule used to inspect interval endpoints and diameter rows at displayed stages.
\item $R$ is the $\RegSeqRatUp$ readback row extracted from the same stream windows and dyadic ledger.
\item $E$ is the $\RealUp$ seal row reached only after the nested interval, located endpoint, dyadic shrink, stream-window, and regular readback rows are visible.
\item $H$ records componentwise $\hsame$ transport for $J,L,D,S,R,E$ and for the replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay of the route
$$
J,L \longmapsto D,S \longmapsto R \longmapsto E .
$$
\item $P$ is the $\Pkg$ provenance over $\NestedShrinkIntervalUp$, $\NestedClosedIntervalUp$, $\LocatedIntervalUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\NestedShrinkIntervalUp}$ row.
\end{enumerate}
The classifier compares accepted carriers only by the displayed rows $J,L,D,S,R,E,H,C,P,N$. It has no coordinate for open-cover compactness, trichotomy, quotient-real equality, a selected intersection point, countable choice, or ambient completeness of $\RealUp$.
\end{definition}

\begin{theorem}[Nested shrink interval NameCert obligations]
\label{thm:nested-shrink-interval-namecert-obligations}
For every accepted $\NestedShrinkIntervalUp$ carrier
$$
K=(J,L,D,S,R,E,H,C,P,N),
$$
the local $\NameCert_{\NestedShrinkIntervalUp}$ surface consists exactly of carrier admission for the $\NestedClosedIntervalUp$ source row, $\LocatedIntervalUp$ endpoint rows, $\DyadicRatCoreUp$ shrink ledger, $\StreamNameUp$ finite windows, $\RegSeqRatUp$ readback, $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming row; classifier stability under those same displayed rows; diameter-to-tolerance exactness through $D$; finite-window readback through $S$ and $R$; Real-seal handoff through $E$; and ledger nonescape for the excluded compactness, trichotomy, quotient, selection, choice, and ambient-completeness coordinates.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:nested-shrink-interval-carrier}. The projection exposes exactly the nested closed interval source, located endpoint row, dyadic shrink ledger, finite windows, regular readback, real seal, and structural rows.

Classifier stability is componentwise on those displayed rows. The shrink estimate is not an external metric fact: it is the $D$ row, read together with the endpoint row $L$ and the stream schedule $S$ before regular readback is available.

The structural rows $H,C,P,N$ transport, replay, source, and name the same route. Since no excluded coordinate occurs in the carrier, every accepted NameCert obligation is one of the rows listed above.
\end{proof}

\begin{theorem}[Nested shrink interval diameter route]
\label{thm:nested-shrink-interval-diameter-route}
Let $K=(J,L,D,S,R,E,H,C,P,N)$ be an accepted $\NestedShrinkIntervalUp$ carrier. Every completion-facing request for the shrinking interval route first reads the $\NestedClosedIntervalUp$ source $J$ and $\LocatedIntervalUp$ endpoint row $L$, then reads the $\DyadicRatCoreUp$ diameter ledger $D$ and $\StreamNameUp$ finite windows $S$, and only then opens the $\RegSeqRatUp$ readback $R$ and $\RealUp$ seal $E$. The route exports diameter-to-tolerance convergence as displayed finite ledger data, not as open-cover compactness, trichotomy, quotient-real equality, countable choice, a selected intersection point, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with a completion-facing request and project the accepted carrier. The only interval source is $J$, and the only endpoint data are the located rows in $L$.

The diameter request is then forced through $D$. Its dyadic comparisons and tolerance rows are inspected through the finite schedule $S$, so a consumer cannot replace the shrinking route by an unrecorded compactness or completeness theorem.

After the finite windows are displayed, $R$ packages the same observations as regular rational readback and $E$ supplies the terminal real seal. The remaining rows $H,C,P,N$ preserve the route and add no excluded coordinate.
\end{proof}

\begin{theorem}[Nested shrink interval Real-seal handoff]
\label{thm:nested-shrink-interval-real-seal-handoff}
Every $\RealUp$-facing consumer admitted by an accepted $\NestedShrinkIntervalUp$ packet receives the seal only through the displayed route
$$
\NestedClosedIntervalUp,\ \LocatedIntervalUp,\ \DyadicRatCoreUp,\ \StreamNameUp,\ \RegSeqRatUp,\ \RealUp .
$$
The handoff is stable under the componentwise $\hsame$ transport and displayed $\Cont$ replay rows $H,C$, and it is sourced and named only by $P,N$.
\end{theorem}
\begin{proof}
Apply \autoref{thm:nested-shrink-interval-diameter-route}. A Real-facing consumer reaches $E$ only after the nested interval source, located endpoint row, dyadic shrink ledger, stream windows, and regular readback have all been displayed.

Transport through $H$ preserves the row roles, and replay through $C$ preserves their order. Provenance and local naming through $P,N$ attach the handoff to the packet without adding analytic content.

Thus the Real seal cannot escape to a selected point, quotient real equality, countable choice, trichotomy, open-cover compactness, or ambient real completeness.
\end{proof}

\closureat{NestedShrinkIntervalUp}{seedStr}

\begin{closurestatus}{\NestedShrinkIntervalUp}
  \constructivestory{A $\NestedShrinkIntervalUp$ packet is generated from a NestedClosedInterval source row, LocatedInterval endpoint rows, a DyadicRatCore shrink ledger, StreamName finite windows, RegSeqRat readback, a RealUp seal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:nested-shrink-interval-carrier}, \autoref{thm:nested-shrink-interval-namecert-obligations}, \autoref{thm:nested-shrink-interval-diameter-route}, and \autoref{thm:nested-shrink-interval-real-seal-handoff} seed the finite L10 shrinking-interval convergence route through nested closed intervals, located endpoint rows, dyadic diameter ledgers, stream windows, regular readback, and Real sealing.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, trichotomy, quotient reals, selected intersection points, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, classifier stability, diameter-to-tolerance exactness, finite-window readback, Real-seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.NestedShrinkIntervalUp.}
\end{closurestatus}
