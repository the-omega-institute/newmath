\begin{theorem}[Nested shrink interval compact interval route]
\label{thm:nested-shrink-interval-compact-interval-route}
Every compact-interval consumer of an accepted $\NestedShrinkIntervalUp$ packet must read the compact real interval sibling of \autoref{ch:concrete-instances-compact-real-interval-namecert} and the nested interval convergence sibling of \autoref{ch:concrete-instances-nested-interval-convergence-namecert} through the same finite endpoint, diameter, $\RegSeqRatUp$, $\RealUp$, $\hsame$, $\Cont$, and $\NameCert$ rows displayed by the shrinking packet. The compact interval route supplies endpoint and finite-net context; the nested convergence route supplies the shrinking-window handoff; neither route adds an open-cover compactness theorem, selected limit point, quotient real equality, countable-choice schedule, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Project the accepted carrier of \autoref{def:nested-shrink-interval-carrier}. The visible analytic rows are the nested source, located endpoints, dyadic diameter ledger, finite stream windows, regular rational readback, and Real seal, followed by componentwise transport, replay, provenance, and local naming.

Use the compact real interval packet of \autoref{ch:concrete-instances-compact-real-interval-namecert} only for the endpoint and finite-net context. Its compact rows are read before the shrinking packet is exported as an interval-local dependency, so the shrink route never obtains an unlisted open-cover or subcover selector.

Use the nested interval convergence packet of \autoref{ch:concrete-instances-nested-interval-convergence-namecert} for the convergence-facing handoff. That sibling reads finite endpoint windows, shrinking ledgers, $\RegSeqRatUp$ readback, and terminal $\RealUp$ sealing before a completion consumer may inspect the route.

The structural rows $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ keep the two sibling reads attached to the same displayed packet. Hence a compact-interval dependency edge from $\NestedShrinkIntervalUp$ is exactly the stated finite route and not a hidden compactness, quotient, choice, or completeness principle.
\end{proof}
