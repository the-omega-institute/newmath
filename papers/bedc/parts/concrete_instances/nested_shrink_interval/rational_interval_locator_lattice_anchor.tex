The $\NestedShrinkIntervalUp$ route consumes \autoref{ch:concrete-instances-rational-interval-locator-namecert} for rational endpoint bracketing and \autoref{ch:concrete-instances-regseqrat-namecert} for regular rational readback before the terminal $\RealUp$ seal is visible. Thus the nested shrink interval dependency lattice reads locator cells first, then the shrinking-window ledger, then the regular readback route, not a selected intersection point or ambient completeness witness.
