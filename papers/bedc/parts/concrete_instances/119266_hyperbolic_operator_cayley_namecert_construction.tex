\chapter{A Concrete Naming Certificate for $\HyperbolicOperatorCayleyUp$}
\label{ch:concrete-instances-hyperbolic-operator-cayley-namecert}
\origin{ai}

The $\HyperbolicOperatorCayleyUp$ packet realizes the hyperbolic phase/operator bridge as a finite Cayley disk-transport certificate. It sits beside the hyperbolic metric route of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the Poincare disk metric packet of \autoref{ch:concrete-instances-poincare-disk-metric-namecert}, the reversible transport surface of \autoref{ch:concrete-instances-unitarygroup-namecert}, the bounded operator-boundary discipline of \autoref{ch:concrete-instances-operatorideal-namecert}, and the spectral boundary warning of \autoref{ch:concrete-instances-spectraltheorem-namecert}. The packet names finite disk transport and operator-boundary rows; it is not a global spectral theorem, not an unbounded functional calculus, not a host Hilbert-space construction, and not a completed analytic model of the observer.

\begin{definition}[HyperbolicOperatorCayley carrier]
\label{def:hyperbolic-operator-cayley-carrier}
A $\HyperbolicOperatorCayleyUp$ carrier is a finite $\BHist$ packet
$$
K=(D,M,O,Y,B,T,F,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the disk-coordinate row, read through $\PoincareDiskMetricUp$ before any operator boundary is exposed.
\item $M$ is the $\HyperbolicMetricUp$ metric row on the displayed disk observations.
\item $O$ is the operator-window row, recording the finite operator input accepted by the packet.
\item $Y$ is the Cayley-transform row, recording the displayed disk transport induced by the operator window without invoking a global spectrum.
\item $B$ is the operator-boundary row, aligned with $\OperatorIdealUp$ and recording boundedness, compactness, or ideal-facing refusal data as finite rows.
\item $T$ is the reversible phase-transport row, consumed through $\UnitaryGroupUp$-style classifier preservation when such a reversible read is present.
\item $F$ is the local refusal row excluding global spectral measures, host functional calculus, unbounded operator domains, and observer-substance readings.
\item $H$ records componentwise $\hsame$ transport for disk, metric, operator, Cayley, boundary, reversible transport, refusal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay through disk readout, metric comparison, operator-window admission, Cayley disk transport, operator-boundary exposure, reversible transport, refusal, and local naming.
\item $P$ is the $\Pkg$ provenance over $\HyperbolicOperatorCayleyUp$, $\HyperbolicMetricUp$, $\PoincareDiskMetricUp$, $\UnitaryGroupUp$, $\OperatorIdealUp$, $\SpectralTheoremUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\HyperbolicOperatorCayleyUp}$ row.
\end{enumerate}
The Lean-side follow-up for this carrier must provide \texttt{Nontrivial HyperbolicOperatorCayleyUp} and \texttt{FieldFaithful HyperbolicOperatorCayleyUp} TasteGate instances for the same displayed packet.
\end{definition}

\begin{theorem}[HyperbolicOperatorCayley NameCert obligations]
\label{thm:hyperbolic-operator-cayley-namecert-obligations}
For every accepted $\HyperbolicOperatorCayleyUp$ carrier $K=(D,M,O,Y,B,T,F,H,C,P,N)$, the local $\NameCert_{\HyperbolicOperatorCayleyUp}$ surface consists exactly of disk-coordinate exposure, hyperbolic metric readback, finite operator-window admission, Cayley disk-transport exposure, operator-boundary exposure, reversible phase-transport exposure, global spectral refusal, componentwise transport, displayed replay, provenance, and local naming.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:hyperbolic-operator-cayley-carrier}. The rows $D,M,O,Y,B,T,F$ expose the disk, metric, operator window, Cayley transport, operator boundary, reversible phase route, and refusal surface.

The Cayley row $Y$ is local to the displayed disk and operator window. It may move disk observations through a finite transport row, but it does not supply a global spectral decomposition, a spectral measure, an unbounded functional calculus, or a host Hilbert-space theorem.

Finally $H,C,P,N$ transport, replay, source, and name the same packet. Since every excluded analytic object is named only by the refusal row $F$ and has no carrier coordinate, the listed finite rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[HyperbolicOperatorCayley vision handoff]
\label{thm:hyperbolic-operator-cayley-vision-handoff}
Every hyperbolic phase/operator read exported by an accepted $\HyperbolicOperatorCayleyUp$ packet factors through
$$
\PoincareDiskMetricUp,\ \HyperbolicMetricUp,\ \UnitaryGroupUp,\ \OperatorIdealUp .
$$
The handoff is the finite Cayley disk-transport and operator-boundary row. It cannot be strengthened into a global spectral theorem, host functional calculus, spectral-measure classifier, unbounded-domain theorem, or observer-substance model.
\end{theorem}
\begin{proof}
Start from an accepted carrier $K=(D,M,O,Y,B,T,F,H,C,P,N)$. The disk rows $D,M$ identify the Poincare-disk and hyperbolic metric surface before any operator row is consumed.

The operator rows $O,Y,B,T$ then expose only a finite window. The Cayley row $Y$ records the disk transport, the boundary row $B$ records the operator-side refusal and boundedness surface, and the reversible row $T$ is consumed only through classifier-preserving transport.

Use the refusal row $F$ together with transport and replay. The structural rows $H,C,P,N$ keep the same displayed dependency route, while $F$ blocks global spectral measures, unbounded functional calculus, host Hilbert-space carriers, and observer-substance readings. Thus the vision handoff is finite NameCert data and nothing stronger.
\end{proof}

\falsifiablePrediction{Every accepted $\HyperbolicOperatorCayleyUp$ packet exposes a finite Cayley disk-transport row and an operator-boundary row, and no accepted packet exports a global spectral theorem, spectral measure, unbounded functional calculus, host Hilbert-space construction, or observer-substance model.}

\independenceWitness{hyperbolic-metric, poincare-disk-metric, unitarygroup, operatorideal, and spectraltheorem: none is bijective to $\HyperbolicOperatorCayleyUp$ because this packet jointly carries disk coordinates, hyperbolic metric readback, an operator window, Cayley disk transport, operator-boundary rows, reversible transport, refusal, hsame transport, Cont replay, provenance, and local NameCert data.}

\closureat{HyperbolicOperatorCayleyUp}{seedStr}

\begin{closurestatus}{\HyperbolicOperatorCayleyUp}
  \constructivestory{A $\HyperbolicOperatorCayleyUp$ packet is generated from a disk-coordinate row, HyperbolicMetric row, finite operator-window row, Cayley disk-transport row, operator-boundary row, reversible phase-transport row, global spectral refusal row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:hyperbolic-operator-cayley-carrier}, \autoref{thm:hyperbolic-operator-cayley-namecert-obligations}, and \autoref{thm:hyperbolic-operator-cayley-vision-handoff} seed the finite Cayley operator route over PoincareDiskMetricUp, HyperbolicMetricUp, UnitaryGroupUp, OperatorIdealUp, and SpectralTheoremUp refusal rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close global spectral theorems, spectral measures, unbounded functional calculus, host Hilbert-space models, observer-substance readings, theorem checking, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, Cayley disk-transport exactness, operator-boundary exactness, reversible transport, spectral refusal, Nontrivial and FieldFaithful TasteGate instances, transport, replay, provenance, and local naming for BEDC.Derived.HyperbolicOperatorCayleyUp.}
\end{closurestatus}
