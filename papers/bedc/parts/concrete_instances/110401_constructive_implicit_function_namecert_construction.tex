\chapter{A Concrete Naming Certificate for $\ConstructiveImplicitFunctionUp$}
\label{ch:concrete-instances-constructive-implicit-function-namecert}
\origin{human}

Following Bishop and Bridges on constructive inverse and implicit function reasoning, the $\ConstructiveImplicitFunctionUp$ packet records a local solution graph as finite BEDC data rather than as a host-level selection of a function. It sits between the real-name surface of \autoref{ch:concrete-instances-real-namecert}, the metric ball and interval windows of \autoref{ch:concrete-instances-metricball-namecert}, \autoref{ch:concrete-instances-real-metric-namecert}, and \autoref{ch:concrete-instances-located-real-interval-namecert}, the continuous-map rows of \autoref{ch:concrete-instances-continuousmap-namecert}, and the derivative boundary of \autoref{ch:concrete-instances-derivative-namecert}. The certificate exposes the local graph, nondegenerate derivative row, located window, contraction or modulus replay, Real readback, transport, provenance, and local naming rows before any consumer may read a local solve.

\begin{definition}[Constructive implicit function carrier]
\label{def:constructive-implicit-function-carrier}
A $\ConstructiveImplicitFunctionUp$ carrier is a finite $\BHist$ packet
$$
U=(F,J,A,M,I,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the $\RealUp$ graph row for the equation and its local output coordinate, read only through displayed real-name endpoints.
\item $J$ is the derivative or Jacobian row, including the apartness or nondegeneracy witness used by the local solve.
\item $A$ is the located interval or metric-ball window row, naming the input cell, output cell, and base point through $\LocatedRealIntervalUp$, $\RealMetricUp$, or $\MetricBallUp$ data.
\item $M$ is the contraction, Lipschitz, or continuity-modulus replay row that bounds the local correction inside $A$.
\item $I$ is the terminal $\RealUp$ readback and seal row for the solved graph point.
\item $H$ records componentwise $\hsame$ transport for graph rows, derivative rows, apartness witnesses, located windows, modulus rows, Real seals, replay, provenance, and local naming.
\item $C$ records displayed $\Cont$ replay from $F,J,A,M,I$ to the local solve row, including the $\ContinuousMapUp$ and $\DerivativeUp$ reads used by the graph.
\item $P$ is the $\Pkg$ provenance over $\ConstructiveImplicitFunctionUp$, $\RealUp$, $\MetricBallUp$, $\ContinuousMapUp$, $\DerivativeUp$, $\LocatedRealIntervalUp$, $\RealMetricUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\ConstructiveImplicitFunctionUp}$ row.
\end{enumerate}
The carrier has no coordinate for a host implicit function, a selected germ, quotient function equality, global inverse theorem, ambient completeness principle, or classical choice.
\end{definition}

\begin{theorem}[Constructive implicit function NameCert obligations]
\label{thm:constructive-implicit-function-namecert-obligations}
For every accepted carrier $U=(F,J,A,M,I,H,C,P,N)$, the local $\NameCert_{\ConstructiveImplicitFunctionUp}$ surface consists of the following finite obligations:
\begin{enumerate}
\item carrier admission for the displayed graph, derivative, located-window, modulus, Real seal, transport, replay, provenance, and local name rows;
\item local graph exactness, requiring every solved point read to pass through the graph row $F$ and terminal Real seal $I$;
\item apartness or nondegeneracy stability, requiring $J$ to transport its derivative witness through $H$ without replacing it by host equality;
\item contraction or modulus replay, requiring $M$ and $C$ to keep correction steps inside the located window $A$;
\item uniqueness nonescape, requiring any two local solve reads with the same displayed rows to return to the same $F,J,A,M,I,H,C,P,N$ packet.
\end{enumerate}
No obligation is stored in a detached function object or an ambient completeness theorem.
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:constructive-implicit-function-carrier}. The packet lists exactly the graph, derivative, located-window, modulus, Real seal, transport, replay, provenance, and local name rows.

The analytic obligations are local to the displayed rows. The graph exactness read uses $F$ and $I$, while derivative stability uses $J$ together with the componentwise transports in $H$.

The local solve is kept inside the named window by $A,M,C$. Since the carrier has no host implicit-function coordinate, selected germ, quotient function equality, or ambient completeness row, uniqueness is a nonescape statement about the same finite packet.
\end{proof}

\begin{theorem}[Constructive implicit function local solve route]
\label{thm:constructive-implicit-function-local-solve-route}
Every $\ConstructiveImplicitFunctionUp$ local-solve read factors through the displayed finite route
$$
\ContinuousMapUp,\DerivativeUp,\LocatedRealIntervalUp,\MetricBallUp,\RealMetricUp,\RealUp,\BHist,\hsame,\Cont,\Pkg,\NameCert.
$$
The consumer must open the graph row $F$, derivative or Jacobian row $J$, located window $A$, contraction or modulus row $M$, Real seal $I$, transport row $H$, replay row $C$, provenance row $P$, and local name row $N$ before it can export a solved graph point.
\end{theorem}
\begin{proof}
Project the accepted carrier. The local-solve read has no available coordinate except $F,J,A,M,I,H,C,P,N$.

The graph and derivative rows are the analytic source. The located interval or metric-ball row names the window, and the contraction or modulus row records why the replay stays in that window.

Finally the Real seal and structural rows finish the route. The solve is therefore a displayed finite certificate over the named sibling chapters, not a host implicit function, selected germ, quotient of local functions, or classical existence principle.
\end{proof}

\falsifiablePrediction{Every $\ConstructiveImplicitFunctionUp$ local-solve read opens a displayed Real graph row, a derivative or Jacobian apartness row, a located interval or metric-ball row, a contraction or modulus row, a Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local naming before exporting a solved graph point.}

\independenceWitness{real, metricball, continuousmap, derivative, located-real-interval, realmetric: $\ConstructiveImplicitFunctionUp$ is not bijective to any listed sibling because it jointly carries local graph exactness, derivative nondegeneracy, located-window control, modulus replay, Real sealing, transport, provenance, and local naming as one certificate.}

\closureat{ConstructiveImplicitFunctionUp}{seedStr}

\begin{closurestatus}{\ConstructiveImplicitFunctionUp}
  \constructivestory{A $\ConstructiveImplicitFunctionUp$ packet is generated from a displayed Real graph row, a derivative or Jacobian row with apartness data, a located interval or metric-ball window, a contraction or modulus replay row, a terminal Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:constructive-implicit-function-carrier}, \autoref{thm:constructive-implicit-function-namecert-obligations}, and \autoref{thm:constructive-implicit-function-local-solve-route} seed the finite constructive implicit-function certificate over graph, derivative, located-window, modulus, Real seal, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host implicit functions, selected germs, quotient function equality, global inverse or implicit theorem packages, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, graph exactness, apartness stability, modulus replay, uniqueness nonescape, and local naming for BEDC.Derived.ConstructiveImplicitFunctionUp.}
\end{closurestatus}
