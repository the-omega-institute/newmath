\chapter{A Concrete Naming Certificate for $\OdeUp$}
\label{ch:concrete-instances-ode-namecert}

The ordinary-differential-equation interface $\OdeUp$ packages a system $\dot x = F(t, x)$ together with the local existence and uniqueness data on Lipschitz vector fields. It depends on $\NameCert_{\DerivativeUp}$ (\autoref{ch:concrete-instances-derivative-namecert}) and $\NameCert_{\BanachUp}$ (\autoref{ch:concrete-instances-banach-namecert}).

\section{Carrier definition}
\label{sec:ode-carrier}

\begin{definition}[OdeUp BHist local-flow source]
\label{def:ode-bhist-local-flow-source}
An $\OdeUp$ BHist local-flow source is a time-state carrier over the
$\BanachUp$ carrier supplied by $\NameCert_{\BanachUp}$. Its points are pairs
of histories $(t,x):\mathsf{BHist}\times\mathsf{BHist}$ whose time component
is carried by the scalar surface used by $\NameCert_{\DerivativeUp}$ and whose
state component is carried by the Banach carrier. The vector-field row is a
displayed operation
$$
F:\mathsf{BHist}\to\mathsf{BHist}\to\mathsf{BHist}
$$
whose value $F(t,x)$ is again a Banach-carried state history after transport
along the time and state classifiers.

A local-flow row consists of an initial time cell $t_0$, an initial state cell
$x_0$, an endpoint time cell $t_1$, an endpoint state cell $x_1$, a
vector-field evaluation ledger $v$, a Picard-step history $p$, and a limit
witness $\ell$ such that the step and limit are linked by displayed
continuations
$$
\Cont(t_0,x_0,p),\qquad
\Cont(p,v,\ell),\qquad
\Cont(t_1,\ell,x_1).
$$
The classifier is inherited from the Banach and derivative surfaces: two rows
are equivalent when their time cells are identified by the derivative scalar
classifier, their state cells by the Banach carrier classifier, and their
displayed ledgers by $\hsame$ transport through the same $\NameCert$ rows.
The ledger records the time cell, state cell, vector-field evaluation,
Picard-step continuation route, and limit witness. It does not assert equality
in any host function space.
\end{definition}
\leandef{BEDC.Derived.OdeUp.OdeLocalFlowRow}

\section{The certificate}
\label{sec:ode-certificate}

\begin{theorem}[OdeUp root vector-field carrier stability]
\label{thm:ode-root-vector-field-carrier-stability}
For an $\OdeUp$ BHist local-flow source, if $t$ is carried by the derivative
scalar surface and $x$ is carried by the Banach state carrier, then the
displayed vector-field value $F(t,x)$ is carried by the same Banach state
carrier. This assignment is stable under classifier transport: replacing
$t$ and $x$ by $t'$ and $x'$ with the corresponding $\NameCert_{\DerivativeUp}$
and $\NameCert_{\BanachUp}$ classifier witnesses gives
$F(t,x)\hsame F(t',x')$ in the endpoint classifier recorded by $\NameCert$.
\end{theorem}
\leanchecked{BEDC.Derived.OdeUp.OdeLocalFlowSource\_vector\_field\_carrier\_stability}
\begin{proof}
Unpack the local-flow source. The time cell is read through
$\NameCert_{\DerivativeUp}$, and the state cell is read through
$\NameCert_{\BanachUp}$. The vector-field row is part of the source data, so
its evaluation ledger records a Banach-carried output whenever both input
cells are carried.

For transport, move first along the derivative classifier on the time cell and
then along the Banach carrier classifier on the state cell. The displayed
evaluation ledger is functorial for these two classifier moves because it is
stored as a $\NameCert$ row rather than as host equality. Repacking the
transported output row gives the required Banach-carried value and its
$\hsame$ endpoint classifier.
\end{proof}

\begin{theorem}[OdeUp root Picard continuation scope]
\label{thm:ode-root-picard-continuation-scope}
In an $\OdeUp$ BHist local-flow source, Picard-step packages compose inside
the scope generated by $\NameCert_{\BanachUp}$, $\NameCert_{\DerivativeUp}$,
and $\hsame$ transport. If two adjacent Picard ledgers expose
$\Cont(t_0,x_0,p)$, $\Cont(p,v,\ell)$, and $\Cont(t_1,\ell,x_1)$ together with
a second compatible step from $x_1$ to $x_2$, then associativity of $\Cont$
builds the composite continuation route from $(t_0,x_0)$ to $(t_2,x_2)$
without adding any carrier outside those two certificate surfaces.
\end{theorem}
\begin{proof}
Read the first Picard package as a chain of displayed continuations from the
initial time-state cell to its limit witness and then to its endpoint. Read
the second package in the same way, with the endpoint state of the first row
as the carried input state of the second.

Use $\Cont$ associativity to reassociate the terminal continuation of the
first package with the initial continuation of the second package. Whenever a
shared endpoint is represented by a classifier-equal history rather than by
the same syntax, transport across $\hsame$ using the Banach and derivative
rows of $\NameCert_{\BanachUp}$ and $\NameCert_{\DerivativeUp}$.

The resulting chain is again a list of BHist continuations whose endpoints are
classified by the same $\NameCert$ surfaces. Thus the composite Picard route
remains scoped to Banach states, derivative time cells, and $\hsame$
transport; no ambient function-space carrier is introduced.
\end{proof}
\leanchecked{BEDC.Derived.OdeUp.OdeLocalFlowSource\_picard\_continuation\_scope}
\leanchecked{BEDC.Derived.OdeUp.OdeLocalFlowRow\_picard\_continuation\_scope}

\begin{theorem}[OdeUp root local-uniqueness classifier]
\label{thm:ode-root-local-uniqueness-classifier}
For an $\OdeUp$ BHist local-flow source, two local-flow witnesses with the same
initial time cell, the same initial state cell, and the same Lipschitz ledger
are classified by the same endpoint classifier. Concretely, if their
vector-field evaluation rows, Picard-step rows, and limit witnesses are
transported through the shared $\NameCert_{\DerivativeUp}$,
$\NameCert_{\BanachUp}$, $\Cont$, and $\hsame$ ledgers, then their endpoint
state histories are related by the Banach classifier recorded in $\NameCert$.
\end{theorem}
\leanchecked{BEDC.Derived.OdeUp.OdeLocalFlow\_endpoint\_classifier\_deterministic}
\begin{proof}
Unpack the two local-flow witnesses into their common initial time-state cell,
their vector-field evaluation ledgers, Picard-step histories, limit witnesses,
and endpoint histories. The shared Lipschitz ledger classifies the
vector-field rows through the derivative and Banach input classifiers, so the
two Picard steps have the same continuation data up to $\hsame$.

Transport the second Picard chain along these $\hsame$ witnesses and use
determinism of the displayed $\Cont$ result at each continuation stage. This
identifies the two limit witnesses in the classifier inherited from the
Banach carrier.

Finally transport through the endpoint continuation rows
$\Cont(t_1,\ell,x_1)$ and $\Cont(t_1,\ell',x_1')$. Since the time endpoint is
the same cell and the limit witnesses have just been classified together, the
Banach endpoint classifier relates $x_1$ and $x_1'$. This is precisely the
endpoint classifier exposed by the $\OdeUp$ source through $\NameCert$.
\end{proof}

\begin{theorem}[OdeUp local-flow concatenation endpoint determinacy]
\label{thm:ode-local-flow-concatenation-endpoint-determinacy}
Let $R_{01}$ and $R_{12}$ be $\OdeUp$ BHist local-flow rows over the same
$\NameCert_{\DerivativeUp}$ and $\NameCert_{\BanachUp}$ surfaces:
$$
R_{01}=(t_0,x_0,t_1,x_1;v_{01},p_{01},\ell_{01}),\qquad
R_{12}=(t_1,x_1,t_2,x_2;v_{12},p_{12},\ell_{12}).
$$
Assume the terminal time-state cell of $R_{01}$ is the initial time-state cell
of $R_{12}$ in the derivative and Banach classifiers, so
\autoref{thm:ode-root-picard-continuation-scope} supplies a composite Picard
route. Write the resulting composite local-flow row as
$$
R_{02}^{\circ}=(t_0,x_0,t_2,x_2;v_{02}^{\circ},p_{02}^{\circ},\ell_{02}^{\circ}).
$$
If
$$
D_{02}=(t_0,x_0,t_2,y;v_{02},p_{02},\ell_{02})
$$
is any direct $\OdeUp$ local-flow row whose Lipschitz vector-field ledger is
classified with the composite ledger,
$$
v_{02}^{\circ}\hsame v_{02},
$$
then the concatenated endpoint and the direct endpoint are related by the
Banach endpoint classifier:
$$
x_2\sim_{\BanachUp}y,
$$
where $\sim_{\BanachUp}$ denotes the classifier supplied by
$\NameCert_{\BanachUp}$.
\end{theorem}
\leanchecked{BEDC.Derived.OdeUp.OdeLocalFlowSource\_concatenation\_route\_scope}
\begin{proof}
By \autoref{thm:ode-root-picard-continuation-scope}, the two adjacent Picard
ledgers compose through the shared middle time-state cell inside the same
$\NameCert_{\DerivativeUp}$ and $\NameCert_{\BanachUp}$ surfaces. Thus the
composite row $R_{02}^{\circ}$ is a local-flow witness from the initial data
$(t_0,x_0)$ to the endpoint state $x_2$ at time $t_2$.

Unpacking \autoref{def:ode-bhist-local-flow-source}, the composite row exposes
$$
\Cont(t_0,x_0,p_{02}^{\circ}),\qquad
\Cont(p_{02}^{\circ},v_{02}^{\circ},\ell_{02}^{\circ}),\qquad
\Cont(t_2,\ell_{02}^{\circ},x_2).
$$
The direct row exposes the parallel chain
$$
\Cont(t_0,x_0,p_{02}),\qquad
\Cont(p_{02},v_{02},\ell_{02}),\qquad
\Cont(t_2,\ell_{02},y).
$$
The two witnesses have the same initial time cell, the same initial state
cell, the same endpoint time cell, and the displayed Lipschitz vector-field
ledger comparison $v_{02}^{\circ}\hsame v_{02}$. Therefore
\autoref{thm:ode-root-local-uniqueness-classifier} applies to the composite
row and the direct row. It classifies their endpoint state histories by the
Banach classifier carried by $\NameCert_{\BanachUp}$. Those endpoint histories
are exactly $x_2$ and $y$, giving $x_2\sim_{\BanachUp}y$.
\end{proof}
\leanchecked{BEDC.Derived.OdeUp.OdeLocalFlowRow\_concatenation\_endpoint\_determinacy}

\closureat{OdeUp}{obligationStr}[BEDC.Derived.OdeUp.OdeLocalFlowRow\_concatenation\_endpoint\_determinacy]

\begin{closurestatus}{\OdeUp}
  \theoryclosure{\obligationClosure}
  \scopeclosed{The obligation scope records the BHist local-flow source carrier of \autoref{def:ode-bhist-local-flow-source}, the Picard continuation scope of \autoref{thm:ode-root-picard-continuation-scope}, endpoint classifier determinacy from \autoref{thm:ode-root-local-uniqueness-classifier}, concatenation route scope from \autoref{thm:ode-local-flow-concatenation-endpoint-determinacy}, and endpoint determinacy for composed rows over the derivative and Banach NameCert surfaces.}
  \formalstatus{\auditCleanV}
  \leantarget{BEDC.Derived.OdeUp.OdeLocalFlowRow\_concatenation\_endpoint\_determinacy}
  \bridgestatus{none}
  \notclaimed{This obligation boundary does not close global existence, Banach fixed-point theory, smooth flow groups, standard ODE models, or bridge verification.}
  \upgradepath{Scoped closure requires a public local-flow certificate surface that binds the listed carrier, classifier, stability, uniqueness, route, and endpoint rows into one interface theorem.}
\end{closurestatus}

\begin{theorem}[OdeUp local-flow concatenation associativity]
\label{thm:ode-local-flow-concatenation-associativity}
Let
$$
R_{01}=(t_0,x_0,t_1,x_1;v_{01},p_{01},\ell_{01}),\qquad
R_{12}=(t_1,x_1,t_2,x_2;v_{12},p_{12},\ell_{12}),\qquad
R_{23}=(t_2,x_2,t_3,x_3;v_{23},p_{23},\ell_{23})
$$
be $\OdeUp$ BHist local-flow rows over the same $\NameCert_{\DerivativeUp}$ and $\NameCert_{\BanachUp}$ surfaces. Assume the terminal time-state cell of $R_{01}$ is classified with the initial time-state cell of $R_{12}$, and the terminal time-state cell of $R_{12}$ is classified with the initial time-state cell of $R_{23}$, in the derivative and Banach classifiers. Let
$$
R_{02}^{\circ}=(t_0,x_0,t_2,y_{02};v_{02}^{\circ},p_{02}^{\circ},\ell_{02}^{\circ}),
\qquad
R_{13}^{\circ}=(t_1,x_1,t_3,y_{13};v_{13}^{\circ},p_{13}^{\circ},\ell_{13}^{\circ})
$$
be the binary composites supplied by \autoref{thm:ode-root-picard-continuation-scope}. Let
$$
L_{03}^{\circ}=(t_0,x_0,t_3,z_L;v_L,p_L,\ell_L)
$$
be the composite of $R_{02}^{\circ}$ with $R_{23}$ supplied by the same theorem, and let
$$
R_{03}^{\circ}=(t_0,x_0,t_3,z_R;v_R,p_R,\ell_R)
$$
be the composite of $R_{01}$ with $R_{13}^{\circ}$ supplied by the same theorem. Then the two grouped composites have Banach-classified endpoint state histories:
$$
z_L\sim_{\BanachUp} z_R.
$$
\end{theorem}
\begin{proof}
Apply \autoref{thm:ode-root-picard-continuation-scope} first to $R_{01}$ and $R_{12}$ and separately to $R_{12}$ and $R_{23}$. This produces the two binary rows $R_{02}^{\circ}$ and $R_{13}^{\circ}$ inside the same $\NameCert_{\DerivativeUp}$ and $\NameCert_{\BanachUp}$ surfaces. Applying the same theorem again to the adjacent pairs $(R_{02}^{\circ},R_{23})$ and $(R_{01},R_{13}^{\circ})$ produces the two grouped rows $L_{03}^{\circ}$ and $R_{03}^{\circ}$, both read as local-flow witnesses from the same initial time-state cell $(t_0,x_0)$ to the same terminal time cell $t_3$.

Unpack \autoref{def:ode-bhist-local-flow-source}. Each grouped row is built from exactly the three displayed local-flow packages of $R_{01}$, $R_{12}$, and $R_{23}$ together with the two classifier transports at the shared interfaces. The only difference is the parenthesization of the continuation chain. By \autoref{thm:continuation-associativity}, reassociating the left-grouped chain and the right-grouped chain does not change the resulting continuation witness up to $\hsame$. Therefore the direct row $R_{03}^{\circ}$ has its Lipschitz vector-field ledger classified with the outer composite ledger of $L_{03}^{\circ}$:
$$
v_L\hsame v_R.
$$

Now apply \autoref{thm:ode-local-flow-concatenation-endpoint-determinacy} to the adjacent pair $R_{02}^{\circ},R_{23}$, taking $R_{03}^{\circ}$ as the direct row from $(t_0,x_0)$ to time $t_3$. Its hypotheses are exactly the shared initial data, the certified outer concatenation route, and the displayed ledger comparison $v_L\hsame v_R$. The conclusion of that theorem is precisely that the endpoint state history of the grouped composite $(R_{01}\circ R_{12})\circ R_{23}$ is related by the Banach endpoint classifier to the endpoint state history of the grouped composite $R_{01}\circ(R_{12}\circ R_{23})$. Hence
$$
z_L\sim_{\BanachUp} z_R.
$$
\end{proof}
