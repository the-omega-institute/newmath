\chapter{A Concrete Naming Certificate for $\RegularCauchyDiagonalModulusSelectionUp$}
\label{ch:concrete-instances-regular-cauchy-diagonal-modulus-selection-namecert}
\origin{human}

Following Bishop and Bridges, a regular Cauchy real-completion route may select a diagonal modulus only after the source regular sequence, dyadic error ledger, observation windows, and terminal real handoff are all visible. The $\RegularCauchyDiagonalModulusSelectionUp$ packet records that finite L10 selection surface. It sits after the $\RegSeqRatUp$ source of \autoref{ch:concrete-instances-regseqrat-namecert}, the $\DyadicRatCoreUp$ arithmetic ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite window rows of \autoref{ch:concrete-instances-streamname-namecert}, and the minimum-modulus packet of \autoref{ch:concrete-instances-regular-cauchy-min-modulus-namecert}, before a $\RealUp$ consumer reads the terminal handoff of \autoref{ch:concrete-instances-real-namecert}. The packet is a displayed diagonal schedule and selected-modulus ledger, not quotient equality of Cauchy names, a hidden limit selector, countable choice, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[Regular Cauchy diagonal modulus selection carrier]
\label{def:regular-cauchy-diagonal-modulus-selection-carrier}
A $\RegularCauchyDiagonalModulusSelectionUp$ carrier is a finite $\BHist$ packet
$$
D=(S,E,W,\Delta,M,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\RegSeqRatUp$ source row naming the regular rational sequence whose diagonal requests are being inspected.
\item $E$ is the $\DyadicRatCoreUp$ error row recording the displayed dyadic tolerances attached to those requests.
\item $W$ is the $\StreamNameUp$ observation row listing the finite source windows, target precisions, and inspected coordinates.
\item $\Delta$ is the diagonal schedule row, naming the finite diagonal order in which source windows and error bounds are paired.
\item $M$ is the selected modulus row. It records the finite modulus chosen from the displayed diagonal schedule and the minimum-modulus surface of \autoref{ch:concrete-instances-regular-cauchy-min-modulus-namecert}.
\item $L$ is the $\RealUp$ handoff row reached only after $S,E,W,\Delta,M$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for source, error, observation, diagonal schedule, selected modulus, Real handoff, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a real-completion request through $S,E,W,\Delta,M,L,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyDiagonalModulusSelectionUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegularCauchyMinModulusUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\RegularCauchyDiagonalModulusSelectionUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,E,W,\Delta,M,L,H,C,P,N$. It has no coordinate for quotient equality of Cauchy names, a hidden diagonal selector, countable choice over infinitely many windows, host real equality, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Regular Cauchy diagonal modulus selection obligations]
\label{thm:regular-cauchy-diagonal-modulus-selection-obligations}
For every accepted $\RegularCauchyDiagonalModulusSelectionUp$ carrier
$$
D=(S,E,W,\Delta,M,L,H,C,P,N),
$$
the local $\NameCert_{\RegularCauchyDiagonalModulusSelectionUp}$ surface consists exactly of carrier admission for the $\RegSeqRatUp$ source row, $\DyadicRatCoreUp$ error row, $\StreamNameUp$ observation row, diagonal schedule row, selected modulus row, $\RealUp$ handoff row, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, local naming, and non-escape from quotient equality, hidden selectors, countable choice, host real equality, and ambient completion.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-diagonal-modulus-selection-carrier}. The source, error, and observation rows are $S,E,W$, so every diagonal request is attached to a displayed regular-rational source, dyadic tolerance ledger, and finite observation window.

The diagonal schedule is $\Delta$, and the selected modulus is $M$. The selected row cites the minimum-modulus packet of \autoref{ch:concrete-instances-regular-cauchy-min-modulus-namecert}, so it records a finite visible budget rather than an off-packet selector.

The Real-facing row is $L$. It is reachable only after the source, dyadic, observation, schedule, and selected-modulus rows have been exposed, matching the L10 route through $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, and $\RealUp$.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since no excluded object is a carrier coordinate, the displayed rows exhaust the NameCert obligations.
\end{proof}

\begin{theorem}[Regular Cauchy diagonal modulus Real handoff]
\label{thm:regular-cauchy-diagonal-modulus-real-handoff}
Every accepted $\RealUp$ consumer read of a $\RegularCauchyDiagonalModulusSelectionUp$ packet factors through
$$
\RegSeqRatUp,\qquad
\DyadicRatCoreUp,\qquad
\StreamNameUp,\qquad
\RegularCauchyMinModulusUp,\qquad
\RealUp
$$
on the displayed rows $S,E,W,\Delta,M,L,H,C,P,N$. The handoff exports no quotient-name equality, no selected representative, no host real equality, no countable-choice row, and no ambient completeness row.
\end{theorem}
\begin{proof}
Start with an accepted carrier $D=(S,E,W,\Delta,M,L,H,C,P,N)$. The route to a Real consumer begins with the visible regular-rational source $S$, dyadic error row $E$, and stream observation row $W$.

Apply the obligation theorem above. It forces the diagonal schedule $\Delta$ and selected modulus $M$ to remain attached to the same displayed source, error, and observation rows before $L$ is readable.

The terminal handoff row $L$ is therefore a Real-facing seal over the same finite packet. Transport through $H$, replay through $C$, provenance through $P$, and local naming through $N$ preserve that route and do not add any quotient, selected-representative, choice, host-equality, or ambient-completeness coordinate.
\end{proof}

\falsifiablePrediction{A real-completion read that requests a diagonal modulus without displaying a $\RegSeqRatUp$ source row, $\DyadicRatCoreUp$ error row, $\StreamNameUp$ observation row, diagonal schedule row, selected modulus row, $\RealUp$ handoff row, $\hsame$ transport, $\Cont$ replay, $\Pkg$ provenance, and local $\NameCert$ row is rejected by the $\RegularCauchyDiagonalModulusSelectionUp$ classifier.}

\independenceWitness{regseqrat, dyadicratcore, streamname, regularcauchyminmodulus, real: no listed sibling is bijective to $\RegularCauchyDiagonalModulusSelectionUp$ because this packet jointly carries a regular-rational source, dyadic error ledger, finite observation windows, diagonal schedule, selected modulus, Real handoff, componentwise transport, replay, provenance, and local naming.}

\closureat{RegularCauchyDiagonalModulusSelectionUp}{seedStr}

\begin{closurestatus}{\RegularCauchyDiagonalModulusSelectionUp}
  \constructivestory{A $\RegularCauchyDiagonalModulusSelectionUp$ packet is generated from a RegSeqRat source row, DyadicRatCore error row, StreamName observation row, diagonal schedule row, selected modulus row, RealUp handoff row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-diagonal-modulus-selection-carrier}, \autoref{thm:regular-cauchy-diagonal-modulus-selection-obligations}, and \autoref{thm:regular-cauchy-diagonal-modulus-real-handoff} seed the finite diagonal modulus selection route consumed by L10 real-completion readers.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close Lean verification, bridge checking, quotient equality of Cauchy names, hidden limit selection, countable choice, host real equality, or ambient RealUp completeness.}
  \upgradepath{Obligation closure requires checked carrier admission, source exposure, dyadic error exactness, finite observation windows, diagonal schedule exposure, selected-modulus exactness, Real handoff, transport, replay, provenance, local naming, and nonescape for BEDC.Derived.RegularCauchyDiagonalModulusSelectionUp.}
\end{closurestatus}
