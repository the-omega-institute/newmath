\chapter{A Concrete Naming Certificate for $\LocatedRealBinarySeparatorUp$}
\label{ch:concrete-instances-located-real-binary-separator-namecert}
\origin{human}

Following Bishop and Bridges on located real numbers and finite real-name comparisons, the $\LocatedRealBinarySeparatorUp$ packet records the binary separation surface read before a later $\RealNameClassifierUp$ decision is exposed. It sits over the dyadic rational core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the finite window interface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}, and the classifier packet of \autoref{ch:concrete-instances-realnameclassifier-namecert}. The packet names only the finite located separator rows by which two regular real names are inspected; it is not a proof of full real trichotomy, quotient-real equality, selected limits, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Located real binary separator carrier]
\label{def:located-real-separator-carrier}
A $\LocatedRealBinarySeparatorUp$ carrier is a finite $\BHist$ packet
$$
S=(A,B,W,T,R,D,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $A$ and $B$ are the two displayed real-name source rows whose located separation is being inspected.
\item $W$ is the shared $\StreamNameUp$ finite-window row exposing the requested observation stages for both sources.
\item $T$ is the $\DyadicRatCoreUp$ tolerance ledger attached to the shared window.
\item $R$ is the $\RegSeqRatUp$ readback row that serves the displayed rational observations on the same window.
\item $D$ is the finite binary decision row, recording only the accepted left/right separator outcome read from $W,T,R$ after the terminal $\RealUp$ seal row is available.
\item $H$ records componentwise $\hsame$ transport for source rows, windows, dyadic tolerance rows, regular readbacks, terminal real sealing, binary decision rows, provenance, replay, and local naming.
\item $C$ records displayed $\Cont$ replay from a separator request through $A,B,W,T,R,D$ and back to the structural rows.
\item $P$ is the $\Pkg$ provenance over $\LocatedRealBinarySeparatorUp$, $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, $\RealNameClassifierUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\LocatedRealBinarySeparatorUp}$ row.
\end{enumerate}
The carrier exports only this finite located binary separator surface. It has no coordinate for quotient real equality, host equality of completed reals, selected representatives, selected limits, countable choice, trichotomy, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Located real separator dyadic decision]
\label{thm:located-real-separator-dyadic-decision}
For every accepted $\LocatedRealBinarySeparatorUp$ carrier
$$
S=(A,B,W,T,R,D,H,C,P,N),
$$
the binary decision row $D$ can be read only after the shared $\StreamNameUp$ row $W$, the $\DyadicRatCoreUp$ tolerance ledger $T$, the $\RegSeqRatUp$ readback row $R$, and the terminal $\RealUp$ seal route have been displayed. Thus a later $\RealNameClassifierUp$ consumer receives a finite decision surface, not an ambient equality or order oracle on completed real numbers.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:located-real-separator-carrier}. The source rows $A,B$ are inspected through the common finite window $W$, so a separator request has no independent host-level observation route.

Read the dyadic and regular rows next. The tolerance ledger $T$ supplies the displayed dyadic comparison data, and $R$ exposes the regular rational readback on the same finite window.

Only after that route is visible may the decision row $D$ be consumed. The sibling chapters provide the surfaces in the order $\StreamNameUp$, $\DyadicRatCoreUp$, $\RegSeqRatUp$, $\RealUp$, and $\RealNameClassifierUp$, so the classifier-facing decision is a finite packet row.

The structural rows $H,C,P,N$ transport, replay, source, and name the same route. Since the carrier contains no quotient-real equality, selected representative, selected limit, trichotomy, countable-choice schedule, or ambient completeness coordinate, none of those principles is exported.
\end{proof}

\begin{theorem}[Located real separator NameCert obligations]
\label{thm:located-real-separator-namecert-obligations}
For every accepted $\LocatedRealBinarySeparatorUp$ carrier
$$
S=(A,B,W,T,R,D,H,C,P,N),
$$
the local $\NameCert_{\LocatedRealBinarySeparatorUp}$ surface consists exactly of source-row admission through $A,B$, shared finite-window exposure through $W$, dyadic tolerance exposure through $T$, regular rational readback through $R$, finite binary decision exposure through $D$, componentwise $\hsame$ transport through $H$, displayed $\Cont$ replay through $C$, provenance through $P$, and local naming through $N$.
\end{theorem}
\begin{proof}
Project the tuple in \autoref{def:located-real-separator-carrier}. Its analytic rows are precisely $A,B,W,T,R,D$, and its structural rows are precisely $H,C,P,N$.

Apply the dyadic decision theorem. It orders the decision-facing read through the shared window, dyadic ledger, regular readback, terminal seal route, and only then the finite binary decision row.

The remaining rows account for transport, replay, provenance, and local naming. No additional carrier coordinate remains from which a consumer could recover full trichotomy, quotient real equality, selected representatives, selected limits, countable choice, or ambient $\RealUp$ completeness.
\end{proof}

\closureat{LocatedRealBinarySeparatorUp}{seedStr}

\begin{closurestatus}{\LocatedRealBinarySeparatorUp}
  \constructivestory{A $\LocatedRealBinarySeparatorUp$ packet is generated from two displayed real-name source rows, one shared StreamNameUp window, a DyadicRatCoreUp tolerance ledger, a RegSeqRatUp readback row, a finite binary decision row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:located-real-separator-carrier}, \autoref{thm:located-real-separator-dyadic-decision}, and \autoref{thm:located-real-separator-namecert-obligations} seed the finite binary separator route consumed by RealNameClassifier-facing reads.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close full real trichotomy, quotient reals, selected representatives, selected limits, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, shared-window exposure, dyadic tolerance exposure, regular rational readback, terminal seal routing, decision-row exposure, transport, replay, provenance, and local naming for BEDC.Derived.LocatedRealBinarySeparatorUp.}
\end{closurestatus}
