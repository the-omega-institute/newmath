\chapter{A Concrete Naming Certificate for $\CauchyGluingLemmaUp$}
\label{ch:concrete-instances-cauchy-gluing-lemma-namecert}
\origin{human}

Following Bishop--Bridges constructive analysis on regular Cauchy names, the $\CauchyGluingLemmaUp$ packet records the finite lemma surface by which two compatible regular rational windows are glued before a completion-facing Real seal is exposed. It sits after the finite windows of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance agreement of \autoref{ch:concrete-instances-dyadicratcore-namecert}, the downstream gluing row of \autoref{ch:concrete-instances-regular-cauchy-gluing-namecert}, and the terminal Real seal of \autoref{ch:concrete-instances-real-namecert}. The packet is a finite NameCert reconstruction of the usual constructive gluing lemma; it is not quotient stream equality, a selected common representative, countable choice, or ambient $\RealUp$ completeness.

\begin{definition}[Cauchy gluing lemma carrier]
\label{def:cauchy-gluing-lemma-carrier}
A $\CauchyGluingLemmaUp$ carrier is a finite $\BHist$ packet
$$
G=(S_0,S_1,W_0,W_1,D,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S_0$ and $S_1$ are compatible finite $\StreamNameUp$ windows naming the two local regular Cauchy observations and their common overlap schedule.
\item $W_0$ and $W_1$ are the two local $\RegSeqRatUp$ readback windows whose rational tails are compared only on the displayed overlap.
\item $D$ is the $\DyadicRatCoreUp$ tolerance-agreement row tying the two windows to the same dyadic request and the same finite overlap budget.
\item $R$ is the downstream $\RegularCauchyGluingUp$ row that consumes the two local windows, the shared tolerance ledger, and the scheduled overlap before any glued readback is public.
\item $E$ is the terminal $\RealUp$ seal reached only after the stream schedules, regular readbacks, dyadic agreement, and gluing row have been replayed.
\item $H$ records componentwise $\hsame$ transport for the two schedules, two readbacks, dyadic ledger, gluing row, Real seal, replay, provenance, and local name.
\item $C$ records displayed $\Cont$ replay from a requested glued-window read through $S_0,S_1,W_0,W_1,D,R,E,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\CauchyGluingLemmaUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RegularCauchyGluingUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\CauchyGluingLemmaUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for quotient stream equality, selected representatives, countable choice, host Real equality, hidden completion, or ambient $\RealUp$ completeness.
\end{definition}

\begin{theorem}[Cauchy gluing lemma NameCert obligations]
\label{thm:cauchy-gluing-lemma-namecert-obligations}
For every accepted $\CauchyGluingLemmaUp$ carrier
$$
G=(S_0,S_1,W_0,W_1,D,R,E,H,C,P,N),
$$
the local $\NameCert_{\CauchyGluingLemmaUp}$ surface consists exactly of carrier admission, overlap compatibility, regular-readback preservation, gluing handoff, Real-seal nonescape, transport, replay, provenance, and local naming. The carrier admission exposes the two $\StreamNameUp$ schedules, two $\RegSeqRatUp$ windows, one $\DyadicRatCoreUp$ tolerance-agreement ledger, one $\RegularCauchyGluingUp$ consumer row, one terminal $\RealUp$ seal, and the structural rows $H,C,P,N$. No local obligation is stored outside these rows.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:cauchy-gluing-lemma-carrier}. The rows $S_0,S_1,W_0,W_1,D,R,E,H,C,P,N$ are the only visible coordinates, so carrier admission and local naming are immediate projections.

Overlap compatibility is the row $D$ read together with the scheduled windows $S_0,S_1$. Regular-readback preservation is the pair $W_0,W_1$ read through the same dyadic request, so the two local rational windows cannot be replaced by quotient stream equality or selected representatives.

The gluing handoff is the row $R$. By the sibling route of \autoref{ch:concrete-instances-regular-cauchy-gluing-namecert}, $R$ consumes the displayed schedules, readbacks, and tolerance ledger before exposing any glued regular-Cauchy read.

Finally $E$ is downstream of that handoff. The structural rows $H,C,P,N$ transport, replay, source, and name the same finite route, so a Real-facing consumer receives only the listed rows and none of the refused host or quotient principles.
\end{proof}

\begin{theorem}[Cauchy gluing lemma Real seal handoff]
\label{thm:cauchy-gluing-lemma-real-seal-handoff}
Every $\RealUp$-facing consumer of an accepted $\CauchyGluingLemmaUp$ carrier must pass through the displayed route
$$
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \RegularCauchyGluingUp \longmapsto \RealUp .
$$
The consumer first reads the two finite stream windows, then the two regular rational readbacks, then the shared dyadic tolerance agreement, then the regular-Cauchy gluing row, and only then the terminal Real seal. The route exports no quotient stream equality, selected common representative, countable choice, host Real equality, hidden completion, or ambient $\RealUp$ completeness.
\end{theorem}
\leanstmt{BEDC.Derived.CauchyGluingLemmaUp}
\begin{proof}
Let $G=(S_0,S_1,W_0,W_1,D,R,E,H,C,P,N)$ be accepted. Any Real-facing read must end at $E$, and $E$ is available only after the carrier's displayed replay row $C$ has passed through the earlier rows.

The earlier rows are ordered by their dependencies. The schedules $S_0,S_1$ name the finite observations, $W_0,W_1$ give the regular rational readbacks on those observations, and $D$ records the shared dyadic tolerance agreement for the overlap.

The downstream gluing row $R$ is then the only row that can turn the two local windows into one glued regular-Cauchy read. Since the terminal seal $E$ is read after $R$, every Real-facing consumer factors through the displayed route.

The carrier contains no coordinate for the refused endpoints. Componentwise transport, replay, provenance, and local naming preserve the same route rather than adding a quotient, selector, choice principle, host equality, hidden completion, or ambient completeness theorem.
\end{proof}

\falsifiablePrediction{Every accepted $\CauchyGluingLemmaUp$ Real-facing read factors through two $\StreamNameUp$ finite windows, two $\RegSeqRatUp$ readback windows, one $\DyadicRatCoreUp$ tolerance-agreement row, one $\RegularCauchyGluingUp$ handoff, one terminal $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and local naming.}

\independenceWitness{streamname, regseqrat, dyadicratcore, regularcauchygluing, real: no listed sibling is bijective to $\CauchyGluingLemmaUp$ because this packet jointly carries two compatible finite windows, two regular readbacks, one dyadic agreement ledger, a downstream gluing row, a Real seal, transport, replay, provenance, and local naming.}

\closureat{CauchyGluingLemmaUp}{seedStr}

\begin{closurestatus}{\CauchyGluingLemmaUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-gluing-lemma-carrier}, \autoref{thm:cauchy-gluing-lemma-namecert-obligations}, and \autoref{thm:cauchy-gluing-lemma-real-seal-handoff} seed the finite gluing-lemma route over two StreamName windows, two RegSeqRat readbacks, DyadicRatCore tolerance agreement, a RegularCauchyGluing handoff, a Real seal, transport, replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient stream equality, selected representatives, countable choice, host Real equality, hidden completion, ambient RealUp completeness, Lean theorem verification, axiom-clean audit, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, overlap compatibility, regular-readback preservation, gluing handoff, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.CauchyGluingLemmaUp.}
  \constructivestory{A $\CauchyGluingLemmaUp$ packet is generated from two compatible finite StreamName windows, two RegSeqRat readback windows, one DyadicRatCore tolerance-agreement row, one RegularCauchyGluing handoff row, one Real seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
