\chapter{A Concrete Naming Certificate for $\PhaseLedgerHorocycleUp$}
\label{ch:concrete-instances-phase-ledger-horocycle-namecert}
\origin{ai}

The $\PhaseLedgerHorocycleUp$ packet realizes the focused phase-ledger and boundary-transport reading of \autoref{ch:visions-hyperbolic-phase-geometry-observer-expansion} as finite NameCert rows. It consumes the horocycle carrier of \autoref{ch:concrete-instances-hyperbolic-horocycle-namecert}, the metric-read surface of \autoref{ch:concrete-instances-hyperbolic-metric-namecert}, the boundary-fixed transport route of \autoref{ch:concrete-instances-poincare-boundary-fixed-transport-namecert}, the disk-coordinate carrier of \autoref{ch:concrete-instances-unitdisk-namecert}, the flow-style route of \autoref{ch:concrete-instances-dynsystem-namecert}, and the Poincare disk representation principles of \autoref{sec:hyperbolic-poincare-disk} and \autoref{sec:hyperbolic-observer-expansion}. The packet is a BEDC-native phase-ledger boundary row; it is not a host hyperbolic surface, not an unlogged observer trajectory, not a global clock, and not a physical cosmology.

\falsifiablePrediction{Every accepted $\PhaseLedgerHorocycleUp$ read factors through displayed phase-ledger, disk-coordinate, hyperbolic-metric, horocycle, boundary-fixed transport, flow, provenance, replay, and local $\NameCert$ rows before it exports a boundary-transport reading.}

\independenceWitness{hyperbolic-horocycle, hyperbolic-metric, poincare-boundary-fixed-transport, unitdisk, dynsystem: the horocycle packet records boundary-tangent incidence without focused phase-ledger provenance; the metric packet records distance readback without ledger phase; the boundary-fixed transport packet records a preserved boundary anchor without horocycle phase accounting; the UnitDisk packet supplies coordinates without phase-ledger replay; and the DynSystem packet records flow-style evolution without hyperbolic boundary tangency. None is bijective to $\PhaseLedgerHorocycleUp$.}

\begin{definition}[Phase-ledger horocycle carrier]
\label{def:phase-ledger-horocycle-carrier}
A $\PhaseLedgerHorocycleUp$ carrier is a finite $\BHist$ packet
$$
L=(F,D,M,Q,T,Y,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $F$ is the focused phase-ledger row, carrying the source $\Hist$ chain, visible phase readout, and provenance cells of the observer-expansion reading.
\item $D$ is the $\UnitDiskUp$ disk-coordinate row through which the ledger readout is placed on a finite disk surface.
\item $M$ is the $\HyperbolicMetricUp$ metric row over the same displayed disk observations.
\item $Q$ is the $\HyperbolicHorocycleUp$ boundary-tangent row, read only after $D$ and $M$ have been displayed.
\item $T$ is the $\PoincareBoundaryFixedTransportUp$ row preserving the boundary far-end reading along admissible disk transport.
\item $Y$ is the $\DynSystemUp$ flow-style ledger row that replays local record expansion without introducing a global clock.
\item $H$ records componentwise $\hsame$ transport for the phase ledger, disk, metric, horocycle, boundary transport, flow, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from the focused ledger through disk placement, metric readback, horocycle incidence, boundary-fixed transport, and flow-style expansion.
\item $P$ is the $\Pkg$ provenance over $\PhaseLedgerHorocycleUp$, $\HyperbolicHorocycleUp$, $\HyperbolicMetricUp$, $\PoincareBoundaryFixedTransportUp$, $\UnitDiskUp$, $\DynSystemUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\PhaseLedgerHorocycleUp}$ row.
\end{enumerate}
The carrier exports no host disk model, global universe digest, synchronized radial clock, physical observer path, selected boundary representative, quotient ideal-boundary equality, Lean verification, or bridge checking.
\end{definition}

\begin{theorem}[Phase-ledger horocycle NameCert obligations]
\label{thm:phase-ledger-horocycle-namecert-obligations}
For every accepted $\PhaseLedgerHorocycleUp$ carrier $L=(F,D,M,Q,T,Y,H,C,P,N)$, the local NameCert surface consists exactly of focused phase-ledger admission, disk-coordinate placement, hyperbolic-metric readback, horocycle boundary-tangency exposure, boundary-fixed transport, flow-style replay, componentwise transport, displayed continuation, provenance, and local naming.

Every boundary-transport read from the packet factors through
$$
\UnitDiskUp \longmapsto \HyperbolicMetricUp \longmapsto \HyperbolicHorocycleUp \longmapsto \PoincareBoundaryFixedTransportUp \longmapsto \DynSystemUp
$$
together with the focused phase-ledger row and the structural $\BHist$, $\hsame$, $\Cont$, $\Pkg$, and $\NameCert$ rows. The Lean TasteGate follow-up must include `Nontrivial PhaseLedgerHorocycleUp` and `FieldFaithful PhaseLedgerHorocycleUp` instances for the same carrier surface.
\end{theorem}
\begin{proof}
Project the accepted carrier. The focused phase-ledger row $F$ supplies the source history chain and phase readout, while $D$ places that readout on a displayed disk surface.

The metric and horocycle rows are then forced in order. Row $M$ reads the hyperbolic metric over the displayed disk data, and row $Q$ exposes the horocycle boundary-tangency surface only after $D$ and $M$ have been replayed.

Boundary transport and flow-style replay are the last public geometric rows. Row $T$ preserves the boundary far-end reading, and row $Y$ records local expansion as a finite flow-style ledger rather than as a global clock.

The structural rows $H,C,P,N$ preserve and name that route. Since the carrier contains no host disk model, universe digest, synchronized radial clock, physical observer path, selected boundary representative, or quotient ideal-boundary equality, the listed rows exhaust the local NameCert obligations.
\end{proof}

\closureat{PhaseLedgerHorocycleUp}{seedStr}

\begin{closurestatus}{\PhaseLedgerHorocycleUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:phase-ledger-horocycle-carrier} and \autoref{thm:phase-ledger-horocycle-namecert-obligations} seed the focused phase-ledger route through disk coordinates, hyperbolic metric readback, horocycle incidence, boundary-fixed transport, flow-style replay, provenance, and local naming.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close host hyperbolic geometry, global clocks, physical cosmology, quotient ideal-boundary equality, selected boundary representatives, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, phase-ledger exactness, disk placement, metric readback, horocycle incidence, boundary-fixed transport, flow replay, TasteGate nontriviality, FieldFaithful coverage, and local naming.}
  \constructivestory{A $\PhaseLedgerHorocycleUp$ packet is generated from a focused phase-ledger row, UnitDisk coordinates, HyperbolicMetric readback, HyperbolicHorocycle incidence, PoincareBoundaryFixedTransport, DynSystem flow-style replay, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
\end{closurestatus}
