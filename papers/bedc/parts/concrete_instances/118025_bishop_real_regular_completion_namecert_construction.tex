\chapter{A Concrete Naming Certificate for $\BishopRealRegularCompletionUp$}
\label{ch:concrete-instances-bishop-real-regular-completion-namecert}
\origin{human}

Following Bishop and Bridges, the $\BishopRealRegularCompletionUp$ packet records the regular completion route for rational names as finite BEDC data. It reads dyadic tolerance arithmetic from \autoref{ch:concrete-instances-dyadicratcore-namecert}, finite observation windows from \autoref{ch:concrete-instances-streamname-namecert}, regular rational readback from \autoref{ch:concrete-instances-regseqrat-namecert}, effective tail schedules from \autoref{ch:concrete-instances-cauchyrate-namecert}, and the terminal real-name seal from \autoref{ch:concrete-instances-real-namecert}. The packet names a finite means of completion; it does not identify quotient streams, select limits, use countable choice, assert host real equality, or claim ambient $\RealUp$ completeness.

\begin{definition}[Bishop real regular completion carrier]
\label{def:bishop-real-regular-completion-carrier}
A $\BishopRealRegularCompletionUp$ carrier is a finite $\BHist$ packet
$$
B=(D,S,R,M,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording the dyadic cells used to compare rational observations.
\item $S$ is the finite $\StreamNameUp$ window surface for the inspected rational name.
\item $R$ is the $\RegSeqRatUp$ regular rational readback row obtained from those windows.
\item $M$ is the $\CauchyRateUp$ effective tail schedule that bounds all admitted completion requests.
\item $E$ is the terminal $\RealUp$ seal row, exposed only after $D,S,R,M$ have been displayed.
\item $H$ records componentwise $\hsame$ transport for the tolerance, window, readback, rate, seal, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from tolerance cells and stream windows to regular readback, effective tail schedule, and terminal seal.
\item $P$ is the $\Pkg$ provenance over $\BishopRealRegularCompletionUp$, $\DyadicRatCoreUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\CauchyRateUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\BishopRealRegularCompletionUp}$ row naming exactly this finite completion packet.
\end{enumerate}
The classifier compares accepted packets only through these rows. It has no coordinate for quotient stream equality, selected representatives, countable choice, host real equality, or an ambient completeness principle for $\RealUp$.
\end{definition}
\leanchecked{BEDC.Derived.BishopRealRegularCompletionUp.BishopRealRegularCompletionTasteGate\_single\_carrier\_alignment}

\begin{theorem}[Bishop real regular completion NameCert obligations]
\label{thm:bishop-real-regular-completion-namecert-obligations}
For every accepted $\BishopRealRegularCompletionUp$ carrier
$$
B=(D,S,R,M,E,H,C,P,N),
$$
the local $\NameCert_{\BishopRealRegularCompletionUp}$ surface consists exactly of carrier admission, dyadic tolerance visibility, finite stream-window admission, regular rational readback, effective tail scheduling, terminal real-name sealing, componentwise transport, displayed replay, provenance, and local naming. Every completion-facing read must pass through
$$
D,S,R,M,E,H,C,P,N
$$
and through no quotient stream equality, selected limit, countable-choice selector, host real equality, or ambient $\RealUp$ completeness row.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:bishop-real-regular-completion-carrier}. The rows $D,S,R,M,E$ expose the dyadic tolerance ledger, finite stream windows, regular rational readback, effective tail schedule, and terminal real seal before any completion-facing consumer is available.

Read the analytic route in order. The $\StreamNameUp$ windows are interpreted through the $\DyadicRatCoreUp$ tolerance cells, the $\RegSeqRatUp$ row provides the regular rational readback, and the $\CauchyRateUp$ row bounds admitted tail requests before $E$ is exposed.

The structural rows $H,C,P,N$ preserve, replay, source, and locally name the same finite route. Since the carrier has no coordinate for quotient stream equality, selected limits, countable choice, host real equality, or ambient completeness, the listed rows exhaust the NameCert obligation surface.
\end{proof}

\begin{theorem}[Bishop real regular completion finite-means route]
\label{thm:bishop-real-regular-completion-finite-means-route}
Every $\RealUp$-facing consumer of an accepted $\BishopRealRegularCompletionUp$ packet receives a finite route
$$
\DyadicRatCoreUp \longmapsto \StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \CauchyRateUp \longmapsto \RealUp
$$
implemented by the displayed rows $D,S,R,M,E,H,C,P,N$. The route is a finite regular-completion certificate, not a quotient of streams, a selected limit, a countable-choice construction, a host real equality theorem, or a proof that all $\RealUp$ names are complete.
\end{theorem}
\begin{proof}
Start with the displayed rows $D$ and $S$. They determine which dyadic tolerances and finite observation windows are visible to the consumer.

Next read $R$ and $M$. The regular rational row $R$ is downstream of the displayed windows, and the effective rate row $M$ bounds the admitted tail requests that may ask for a completion-facing seal.

Finally read $E$ through $H,C,P,N$. Transport and replay preserve the same row order, while provenance and local naming record that the packet contains no additional quotient, selector, choice, host-equality, or completeness coordinate.
\end{proof}

\closureat{BishopRealRegularCompletionUp}{seedStr}

\begin{closurestatus}{\BishopRealRegularCompletionUp}
  \constructivestory{A $\BishopRealRegularCompletionUp$ packet is generated from dyadic tolerance rows, finite StreamName windows, RegSeqRat readback, CauchyRate tail schedules, a RealUp seal, componentwise hsame transport, displayed Cont replay, Pkg provenance, and local NameCert data.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:bishop-real-regular-completion-carrier}, \autoref{thm:bishop-real-regular-completion-namecert-obligations}, and \autoref{thm:bishop-real-regular-completion-finite-means-route} seed the finite regular-completion route over dyadic tolerance rows, stream windows, regular rational readback, effective tail schedules, terminal Real sealing, transport, replay, provenance, and local naming.}
  \formalstatus{\theoremCheckedV}
  \leantarget{BEDC.Derived.BishopRealRegularCompletionUp.BishopRealRegularCompletionTasteGate\_single\_carrier\_alignment}
  \bridgestatus{none}
  \notclaimed{The chapter does not claim quotient stream equality, selected limits, countable choice, host real equality, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, dyadic tolerance visibility, finite window admission, regular rational readback, effective tail scheduling, Real seal handoff, transport, replay, provenance, and local naming for BEDC.Derived.BishopRealRegularCompletionUp.}
\end{closurestatus}
