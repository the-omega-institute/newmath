\chapter{A Concrete Naming Certificate for $\CauchyDiagonalCompactnessUp$}
\label{ch:concrete-instances-cauchy-diagonal-compactness-namecert}
\origin{human}

Following Bolzano, Weierstrass, and Bishop--Bridges constructive analysis, the $\CauchyDiagonalCompactnessUp$ packet records the finite diagonal subsequence boundary used by the L10 Real and Completion route before any consumer may read a compactness-shaped seal. It sits downstream of the bounded-sequence source of \autoref{ch:concrete-instances-boundedrealsequence-namecert}, the finite subsequence compactness discipline of \autoref{ch:concrete-instances-bolzanoweierstrass-namecert}, the regular extraction boundary of \autoref{ch:concrete-instances-regular-cauchy-subsequence-namecert}, the finite window surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance ledger of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet names a finite diagonal window extraction route; it does not assert open-cover compactness, choice-selected infinite subsequences, quotient stream equality, host real equality, or ambient $\RealUp$ completeness.

\begin{definition}[CauchyDiagonalCompactness carrier]
\label{def:cauchy-diagonal-compactness-carrier}
A $\CauchyDiagonalCompactnessUp$ carrier is a finite $\BHist$ packet
$$
K=(B,F,W,Q,D,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $B$ is the $\BoundedRealSequenceUp$ source row, carrying the bounded sequence windows available to the diagonal construction;
\item $F$ is the finite net and refinement schedule, recording only the inspected cells and refinement levels used by the diagonal route;
\item $W$ is the diagonal subsequence selector-window row, displaying for each visible precision request the finite window of selected indices consumed by the packet;
\item $Q$ is the $\RegSeqRatUp$ readback row assembled from $B,F,W$ and read only through the finite windows of \autoref{ch:concrete-instances-streamname-namecert};
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, storing the dyadic radius, slack, and comparison entries for the displayed net refinements and selected windows;
\item $E$ is the terminal $\RealUp$ seal row, visible only after $B,F,W,Q,D$ have been displayed;
\item $H$ records componentwise $\hsame$ transport for the bounded source, finite refinement schedule, selector windows, regular readback, dyadic ledger, Real seal, replay row, provenance row, and local naming row;
\item $C$ records displayed $\Cont$ replay from each finite request through $B,F,W,Q,D,E,H,P,N$;
\item $P$ is the $\Pkg$ provenance over $\CauchyDiagonalCompactnessUp$, $\BolzanoWeierstrassUp$, $\BoundedRealSequenceUp$, $\RegularCauchySubsequenceUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$;
\item $N$ is the local $\NameCert_{\CauchyDiagonalCompactnessUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $B,F,W,Q,D,E,H,C,P,N$. It contains no open-cover compactness field, no selected infinite subsequence, no quotient stream equality, no host real equality, no countable-choice row, and no ambient $\RealUp$ completeness datum.
\end{definition}
\leanchecked{BEDC.Derived.CauchyDiagonalCompactnessUp.TasteGate.CauchyDiagonalCompactnessTasteGate\_single\_carrier\_alignment}

\begin{theorem}[CauchyDiagonalCompactness NameCert obligations]
\label{thm:cauchy-diagonal-compactness-namecert-obligations}
For an accepted $\CauchyDiagonalCompactnessUp$ carrier $K=(B,F,W,Q,D,E,H,C,P,N)$, the local $\NameCert_{\CauchyDiagonalCompactnessUp}$ surface consists exactly of the following finite obligations:
\begin{enumerate}
\item carrier admission for the bounded source, finite net and refinement schedule, selector-window row, regular rational readback, dyadic tolerance ledger, terminal Real seal, transport, replay, provenance, and local naming rows;
\item bounded-source exactness, requiring every diagonal window to read the same $\BoundedRealSequenceUp$ row $B$ that feeds the $\BolzanoWeierstrassUp$ and $\RegularCauchySubsequenceUp$ routes;
\item finite-refinement exactness, requiring $F$ to list the inspected net cells and refinement levels before any selector window is exposed;
\item readback compatibility, requiring $Q$ to be the only $\RegSeqRatUp$ readback obtained from the selected windows and requiring $D$ to be the only dyadic tolerance ledger consumed by that readback;
\item Real seal ordering, requiring $E$ to be displayed only after $B,F,W,Q,D$;
\item route stability, requiring $H,C,P,N$ to preserve the displayed packet without adding open-cover compactness, choice-selected subsequences, quotient stream equality, host real equality, countable choice, or ambient $\RealUp$ completeness.
\end{enumerate}
\end{theorem}
\begin{proof}
Carrier admission is projection from \autoref{def:cauchy-diagonal-compactness-carrier}. The tuple displays the bounded source, finite refinement schedule, selector windows, regular readback, dyadic ledger, Real seal, transport, replay, provenance, and local name.

Bounded-source exactness follows from the single coordinate $B$. Since the carrier has no second sequence source, every diagonal window and downstream readback must inspect the same bounded row used by the Bolzano--Weierstrass and regular-subsequence siblings.

Finite-refinement exactness is the projection to $F$. The schedule stores inspected cells and refinement levels before $W,Q,D$, or $E$ is available, so the selector window cannot be supplied by an external compactness oracle.

Readback compatibility and Real seal ordering follow from $W,Q,D,E$. The readback row $Q$ is downstream of the selected windows, the dyadic ledger $D$ is the only tolerance row consumed by that readback, and the terminal seal $E$ appears only after those rows are displayed.

Finally inspect $H,C,P,N$. These rows transport, replay, source, and name the same finite route, and the carrier has no coordinate for open-cover compactness, a choice-selected infinite subsequence, quotient stream equality, host real equality, countable choice, or ambient completeness.
\end{proof}

\begin{theorem}[CauchyDiagonalCompactness window extraction]
\label{thm:cauchy-diagonal-compactness-window-extraction}
Let $K=(B,F,W,Q,D,E,H,C,P,N)$ be an accepted $\CauchyDiagonalCompactnessUp$ carrier. Every finite precision request that reaches the diagonal route factors through the displayed extraction spine
$$
B,F,W \longmapsto Q,D \longmapsto E .
$$
Thus the packet exposes a finite selector-window extraction over bounded real sequence data, not a choice-selected infinite subsequence, quotient stream, open-cover compactness proof, or ambient completion principle.
\end{theorem}
\begin{proof}
Begin with a finite precision request. Carrier admission provides $B$ as the only bounded sequence source and $F$ as the only refinement schedule.

The selector-window row $W$ is then read from $B$ and $F$. It stores visible windows of selected indices, rather than an infinite selected subsequence or a host choice function.

Next the packet reads $Q$ and $D$. The regular rational readback is assembled from the selected windows, and the dyadic ledger records the tolerance data consumed by that readback.

The Real seal $E$ is downstream of this spine. The structural rows $H,C,P,N$ preserve the same finite extraction route, so no open-cover compactness field, quotient stream, selected infinite subsequence, or ambient completion datum can enter the consumer surface.
\end{proof}

\begin{theorem}[CauchyDiagonalCompactness Real seal non-escape]
\label{thm:cauchy-diagonal-compactness-real-seal-nonescape}
For an accepted $\CauchyDiagonalCompactnessUp$ carrier $K=(B,F,W,Q,D,E,H,C,P,N)$, every $\RealUp$ seal read reaches $E$ only through the bounded source $B$, finite net and refinement schedule $F$, selector-window row $W$, $\RegSeqRatUp$ readback $Q$, and $\DyadicRatCoreUp$ tolerance ledger $D$. The seal exports no open-cover compactness, choice-selected subsequence, quotient stream equality, host real equality, countable-choice row, or ambient $\RealUp$ completeness.
\end{theorem}
\begin{proof}
Start with a $\RealUp$ seal read. The carrier definition supplies $E$ as the only terminal Real seal coordinate.

The NameCert obligations place $E$ after $B,F,W,Q,D$. A consumer must therefore traverse the bounded source, finite refinement schedule, selector window, regular readback, and dyadic tolerance ledger before any Real seal can be read.

The transport and replay rows $H$ and $C$ preserve that same order, while $P$ and $N$ source and name the packet. Since the carrier contains no open-cover compactness field, selected infinite subsequence, quotient stream equality, host real equality, countable-choice row, or ambient completeness datum, the Real seal is exhausted by the displayed finite route.
\end{proof}

\closureat{CauchyDiagonalCompactnessUp}{seedStr}

\begin{closurestatus}{\CauchyDiagonalCompactnessUp}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:cauchy-diagonal-compactness-carrier}, \autoref{thm:cauchy-diagonal-compactness-namecert-obligations}, \autoref{thm:cauchy-diagonal-compactness-window-extraction}, and \autoref{thm:cauchy-diagonal-compactness-real-seal-nonescape} seed the finite diagonal compactness packet over BoundedRealSequence sources, BolzanoWeierstrass refinement discipline, RegularCauchySubsequence windows, StreamName inspection, RegSeqRat readback, DyadicRatCore tolerances, and Real seals.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close open-cover compactness, choice-selected infinite subsequences, quotient stream equality, host real equality, countable choice, ambient $\RealUp$ completeness, Lean verification, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, bounded-source exactness, finite-refinement exactness, selector-window extraction, readback compatibility, Real seal nonescape, and route stability for BEDC.Derived.CauchyDiagonalCompactnessUp.}
  \constructivestory{A $\CauchyDiagonalCompactnessUp$ packet is generated from a $\BoundedRealSequenceUp$ source, a finite net and refinement schedule, displayed diagonal selector windows, a $\RegSeqRatUp$ readback row, a $\DyadicRatCoreUp$ tolerance ledger, a $\RealUp$ seal, componentwise $\hsame$ transport, displayed $\Cont$ replay, $\Pkg$ provenance, and a local $\NameCert$ row.}
\end{closurestatus}
