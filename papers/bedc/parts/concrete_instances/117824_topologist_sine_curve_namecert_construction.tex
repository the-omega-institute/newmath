\chapter{A Concrete Naming Certificate for $\TopologistSineCurveUp$}
\label{ch:concrete-instances-topologist-sine-curve-namecert}
\origin{human}

Following Kelley and classical point-set topology, the $\TopologistSineCurveUp$ packet records the finite NameCert surface by which the oscillatory graph of $x \mapsto \sin(1/x)$ and its limiting vertical segment are read as Real- and topology-facing boundary data. It sits after the Real carrier of \autoref{ch:concrete-instances-real-namecert}, the finite stream window of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the topology handoff of \autoref{ch:concrete-instances-topology-namecert}, and the continuous-map route of \autoref{ch:concrete-instances-continuousmap-namecert}. The packet names only the finite boundary example; it does not assert connectedness, compactness, quotient equality, host subset equality, Real completeness, or a bridge theorem.

\begin{definition}[Topologist sine curve carrier]
\label{def:topologist-sine-curve-carrier}
A $\TopologistSineCurveUp$ carrier is a finite $\BHist$ packet
$$
S=(G,V,W,R,E,T,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $G$ is the oscillatory graph-window row, recording finitely many rational observation windows for the graph-side read of $x \mapsto \sin(1/x)$ away from the limiting axis.
\item $V$ is the limiting vertical-segment row, recording the displayed endpoint and interval observations used by the boundary read.
\item $W$ is the $\StreamNameUp$ finite observation window that names the graph and segment samples before any Real readback is consumed.
\item $R$ is the $\RegSeqRatUp$ readback row, translating the finite stream observations into regular rational approximants.
\item $E$ is the terminal $\RealUp$ seal for the graph and limiting-segment coordinates read through $W$ and $R$.
\item $T$ is the topology and continuous-map handoff row, available only after the graph window, segment row, stream window, regular-rational readback, and Real seal have been displayed.
\item $H$ records componentwise $\hsame$ transport for graph-window, segment, stream, regular-rational, Real, topology, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from graph-window and segment requests through $G,V,W,R,E,T,H,P,N$.
\item $P$ is the $\Pkg$ provenance over $\TopologistSineCurveUp$, $\RealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\TopologyUp$, $\ContinuousMapUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows.
\item $N$ is the local $\NameCert_{\TopologistSineCurveUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $G,V,W,R,E,T,H,C,P,N$. It has no coordinate for a host subset of $\mathbb{R}^2$, quotient equality, Real completeness, compactness, connectedness, path-connectedness, or ambient topological closure.
\end{definition}

\begin{theorem}[Topologist sine curve NameCert obligations]
\label{thm:topologist-sine-curve-namecert-obligations}
For every accepted $\TopologistSineCurveUp$ carrier $S=(G,V,W,R,E,T,H,C,P,N)$, the local $\NameCert_{\TopologistSineCurveUp}$ surface consists exactly of oscillatory graph-window admission through $G$, limiting vertical-segment visibility through $V$, finite stream observation through $W$, regular rational readback through $R$, terminal Real sealing through $E$, topology and continuous-map handoff through $T$, componentwise transport through $H$, displayed replay through $C$, package provenance through $P$, and local naming through $N$.

Every boundary consumer of the packet must read
$$
G,V,W,R,E,T,H,C,P,N
$$
before treating the packet as topologist-sine-curve evidence. The route exports no connectedness theorem, compactness theorem, quotient equality, host subset equality, Real completeness principle, or bridgeChecked evidence.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:topologist-sine-curve-carrier}. The graph-window row $G$ and vertical-segment row $V$ are separate displayed coordinates, so the limiting segment is not inferred from an ambient subset closure.

Read the finite observation rows next. The stream window $W$ supplies only finite samples, and the $\RegSeqRatUp$ row $R$ converts those samples into regular rational readback before the terminal Real seal $E$ is available.

The topology and continuous-map handoff row $T$ is downstream from $G,V,W,R,E$. Thus a consumer may cite the topology-facing boundary route only after the Real-facing finite data has already been displayed.

Finally $H,C,P,N$ transport, replay, source, and name the same finite packet. Since no row stores host subset equality, quotient equality, Real completeness, connectedness, compactness, or bridge evidence, the listed rows exhaust the local NameCert obligations.
\end{proof}

\begin{theorem}[Topologist sine curve Real-topology handoff]
\label{thm:topologist-sine-curve-real-topology-handoff}
Every topology-facing read of an accepted $\TopologistSineCurveUp$ packet factors through the displayed route
$$
\StreamNameUp,\quad \RegSeqRatUp,\quad \RealUp,\quad \TopologyUp,\quad \ContinuousMapUp .
$$
The handoff reaches the topology and continuous-map row $T$ only after the oscillatory graph window $G$, limiting vertical-segment row $V$, finite stream window $W$, regular rational readback row $R$, and terminal Real seal $E$ have been displayed. It exports no connectedness claim, compactness claim, quotient equality, host subset equality, or Real completeness principle.
\end{theorem}
\begin{proof}
Start with the graph and segment rows. The graph-window coordinate $G$ records only finite oscillatory observations, while $V$ records the displayed limiting-segment observations; neither row is a host subset of the plane.

Next read the Real-facing route. \autoref{ch:concrete-instances-streamname-namecert} supplies the finite observation window, \autoref{ch:concrete-instances-regseqrat-namecert} supplies the regular rational readback, and \autoref{ch:concrete-instances-real-namecert} supplies the terminal Real seal.

Only after those rows are visible may the packet reach \autoref{ch:concrete-instances-topology-namecert} and \autoref{ch:concrete-instances-continuousmap-namecert}. The topology-facing row is therefore a handoff from finite Real data, not a replacement for it.

Because the carrier has no row for connectedness, compactness, quotient equality, host subset equality, or Real completeness, the handoff cannot export any of those endpoints.
\end{proof}

\falsifiablePrediction{An accepted $\TopologistSineCurveUp$ consumer that reads the topology handoff without first displaying $G,V,W,R,E$ refutes the carrier surface.}

\independenceWitness{real, streamname, regseqrat, topology, continuousmap: none carries the oscillatory graph-window row, limiting vertical-segment row, finite stream window, regular rational readback, terminal Real seal, topology handoff, transport, replay, provenance, and local NameCert rows in one packet.}

\closureat{TopologistSineCurveUp}{seedStr}

\begin{closurestatus}{\TopologistSineCurveUp}
  \constructivestory{A $\TopologistSineCurveUp$ packet is generated from an oscillatory graph-window row, limiting vertical-segment row, StreamName finite observation window, RegSeqRat readback, terminal RealUp seal, topology and continuous-map handoff, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:topologist-sine-curve-carrier}, \autoref{thm:topologist-sine-curve-namecert-obligations}, and \autoref{thm:topologist-sine-curve-real-topology-handoff} seed the finite Real/topology boundary certificate over graph-window, segment, stream, regular-rational, Real, topology, continuous-map, transport, replay, provenance, and local naming rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close connectedness, compactness, quotient equality, host subset equality, Real completeness, Lean verification, or bridge checking for $\TopologistSineCurveUp$.}
  \upgradepath{Obligation closure requires checked graph-window admission, limiting vertical-segment visibility, finite stream observation, regular rational readback, terminal Real sealing, topology and continuous-map handoff, transport, replay, provenance, and local naming for BEDC.Derived.TopologistSineCurveUp.}
\end{closurestatus}
