\chapter{A Concrete Naming Certificate for $\MengerCurveUp$}
\label{ch:concrete-instances-menger-curve-namecert}
\origin{human}

Following Menger and continuum theory, the $\MengerCurveUp$ packet records the universal one-dimensional compact metric continuum by finite cube-refinement obligations rather than by an ambient subset of Euclidean space. It sits between the compactum surface of \autoref{ch:concrete-instances-menger-compactum-namecert}, the continuum boundary of \autoref{ch:concrete-instances-peano-continuum-namecert}, the planar comparison surface of \autoref{ch:concrete-instances-sierpinski-carpet-namecert}, the compact metric carrier of \autoref{ch:concrete-instances-compactmetric-namecert}, and the metric source of \autoref{ch:concrete-instances-metric-namecert}. The packet gives L10 compact and metric consumers a finite refinement ledger for the curve; it does not import open-cover compactness, host hyperspace equality, selected points, quotient continua, or ambient completeness.

\begin{definition}[Menger curve carrier]
\label{def:menger-curve-carrier}
A $\MengerCurveUp$ carrier is a finite $\BHist$ packet
$$
U=(M,K,L,S,Q,R,E,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $M$ is the $\MetricUp$ source row, carrying the displayed distance comparisons read by every cube-refinement request;
\item $K$ is the $\CompactMetricUp$ finite-net row, exposing compact metric coverage before any continuum or cube row is consumed;
\item $L$ is the $\PeanoContinuumUp$ row, recording connected locally connected compact-continuum data as a finite NameCert dependency;
\item $S$ is the $\SierpinskiCarpetUp$ comparison row, recording the planar hole-boundary sibling used only as a contrast surface;
\item $Q$ is the $\MengerCompactumUp$ row, carrying the compactum and covering-dimension surface already named for Menger compacta;
\item $R$ is the finite cube-refinement ledger, whose entries name stage, retained subcubes, face-incidence tests, and adjacency witnesses;
\item $E$ is the $\MengerCoverCompactnessUp$ row, recording finite cover requests and finite subcover witnesses for the inspected refinement stage;
\item $H$ records componentwise $\hsame$ transport for $M,K,L,S,Q,R,E,P,N$;
\item $C$ records displayed $\Cont$ replay through the metric, compact, continuum, compactum, cube-refinement, cover, provenance, and local naming rows;
\item $P$ is the $\Pkg$ provenance over $\MengerCurveUp$, $\MetricUp$, $\CompactMetricUp$, $\PeanoContinuumUp$, $\SierpinskiCarpetUp$, $\MengerCompactumUp$, $\MengerCoverCompactnessUp$, $\MengerNobelingEmbeddingUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$ rows;
\item $N$ is the local $\NameCert_{\MengerCurveUp}$ row.
\end{enumerate}
The carrier has no coordinate for an arbitrary open cover, a host continuum object, a quotient of compact subsets, a selected point sequence, or an ambient Euclidean equality oracle.
\end{definition}

\begin{theorem}[Menger curve NameCert obligations]
\label{thm:menger-curve-namecert-obligations}
For every accepted $\MengerCurveUp$ carrier $U=(M,K,L,S,Q,R,E,H,C,P,N)$, the local $\NameCert_{\MengerCurveUp}$ surface consists exactly of metric exposure through $M$; compact metric finite-net exposure through $K$; Peano-continuum exposure through $L$; planar comparison exposure through $S$; Menger compactum exposure through $Q$; finite cube-refinement exposure through $R$; finite cover-compactness exposure through $E$; componentwise transport through $H$; displayed replay through $C$; provenance through $P$; and local naming through $N$.
\end{theorem}

\begin{theorem}[Menger curve finite cube refinement]
\label{thm:menger-curve-finite-cube-refinement}
Every compact, metric, continuum, or embedding-facing consumer of an accepted $\MengerCurveUp$ packet must read the finite route
$$
\MetricUp,\ \CompactMetricUp,\ \PeanoContinuumUp,\ \SierpinskiCarpetUp,\ \MengerCompactumUp,\ R,\ \MengerCoverCompactnessUp,\ \MengerNobelingEmbeddingUp
$$
through the displayed rows $M,K,L,S,Q,R,E,H,C,P,N$ before treating the packet as the Menger curve. The cube-refinement ledger $R$ is consumed as finite subcube incidence and adjacency data; it cannot be replaced by open-cover compactness, a host universal-curve theorem, quotient compactum equality, or an ambient Euclidean subset.
\end{theorem}

\falsifiablePrediction{An accepted $\MengerCurveUp$ consumer that reaches compact, metric, continuum, or embedding-facing data while omitting the finite cube-refinement ledger $R$ refutes the carrier of \autoref{def:menger-curve-carrier}.}

\independenceWitness{menger-compactum, peano-continuum, sierpinski-carpet, compactmetric, metric: $\MengerCurveUp$ is not bijective to any listed sibling because this packet binds metric source rows, compact finite-net rows, continuum rows, planar comparison rows, Menger compactum rows, finite cube-refinement rows, finite cover-compactness rows, transport, replay, provenance, and local naming into one curve certificate.}

\closureat{MengerCurveUp}{seedStr}

\begin{closurestatus}{\MengerCurveUp}
  \constructivestory{A $\MengerCurveUp$ packet is generated from a MetricUp source row, a CompactMetricUp finite-net row, a PeanoContinuumUp row, a SierpinskiCarpetUp comparison row, a MengerCompactumUp row, a finite cube-refinement ledger, a MengerCoverCompactnessUp row, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:menger-curve-carrier}, \autoref{thm:menger-curve-namecert-obligations}, and \autoref{thm:menger-curve-finite-cube-refinement} seed the finite cube-refinement NameCert surface for the displayed Menger curve packet.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not claim Lean verification, open-cover compactness, host universal-curve uniqueness, quotient compactum equality, ambient Euclidean subset equality, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, finite cube-refinement exposure, compact metric exposure, continuum exposure, Menger compactum exposure, cover-compactness exposure, transport, replay, provenance, and local naming for BEDC.Derived.MengerCurveUp.}
\end{closurestatus}
