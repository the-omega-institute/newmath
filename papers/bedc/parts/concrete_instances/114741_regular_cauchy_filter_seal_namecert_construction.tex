\chapter{A Concrete Naming Certificate for $\RegularCauchyFilterSealUp$}
\label{ch:concrete-instances-regular-cauchy-filter-seal-namecert}
\origin{human}

Following Bishop and Bridges, the $\RegularCauchyFilterSealUp$ packet records the finite tail-filter seal used when regular Cauchy sequence evidence from \autoref{sec:real-completeness-regular-cauchy} is routed into the L10 Real and Completion corridor. It sits after the finite observation surface of \autoref{ch:concrete-instances-streamname-namecert}, the regular rational readback of \autoref{ch:concrete-instances-regseqrat-namecert}, the dyadic tolerance core of \autoref{ch:concrete-instances-dyadicratcore-namecert}, and the terminal real-name seal of \autoref{ch:concrete-instances-real-namecert}. The packet turns the regular Cauchy tail discipline into a displayed filter seal, not into a quotient stream, selected limit, countable-choice schedule, host equality proof, trichotomy decision, or ambient $\RealUp$ completeness theorem.

\begin{definition}[Regular Cauchy filter seal carrier]
\label{def:regular-cauchy-filter-seal-carrier}
A $\RegularCauchyFilterSealUp$ carrier is a finite $\BHist$ packet
$$
T=(S,R,D,W,F,E,L,H,C,P,N)
$$
with the following displayed rows:
\begin{enumerate}
\item $S$ is the $\StreamNameUp$ tail-window row, recording the finite windows opened by the regular Cauchy tail request.
\item $R$ is the $\RegSeqRatUp$ regular-rational readback row attached to those windows.
\item $D$ is the $\DyadicRatCoreUp$ tolerance ledger, recording the dyadic tail radius, comparison scale, and endpoint budget.
\item $W$ is the tail-seal witness row, stating that the displayed windows and dyadic budget give the required regular Cauchy tail comparison.
\item $F$ is the filter-facing seal row generated from $S,R,D,W$ before any completion consumer may inspect the tail-filter route.
\item $E$ is the terminal $\RealUp$ seal boundary reached only after the StreamName, RegSeqRat, DyadicRatCore, and filter-facing rows have been displayed.
\item $L$ is the ledger row connecting the carrier to the regular Cauchy diagonal and tail-seal markers of \autoref{sec:real-completeness-regular-cauchy}.
\item $H$ records componentwise $\hsame$ transport for tail windows, regular readback, dyadic tolerance, tail witness, filter seal, terminal seal, ledger, replay, provenance, and local naming rows.
\item $C$ records displayed $\Cont$ replay from a regular Cauchy tail request through $S,R,D,W,F,E,L$.
\item $P$ is the $\Pkg$ provenance over $\RegularCauchyFilterSealUp$, $\StreamNameUp$, $\RegSeqRatUp$, $\DyadicRatCoreUp$, $\RealUp$, $\BHist$, $\hsame$, $\Cont$, and $\NameCert$.
\item $N$ is the local $\NameCert_{\RegularCauchyFilterSealUp}$ row.
\end{enumerate}
The classifier compares accepted packets only through $S,R,D,W,F,E,L,H,C,P,N$. It has no coordinate for quotient streams, selected limits, countable choice, host equality, trichotomy, ambient Real completeness, or bridge credentials.
\end{definition}

\begin{theorem}[Regular Cauchy filter seal NameCert obligations]
\label{thm:regular-cauchy-filter-seal-namecert-obligations}
For every accepted $\RegularCauchyFilterSealUp$ carrier $T=(S,R,D,W,F,E,L,H,C,P,N)$, the local $\NameCert_{\RegularCauchyFilterSealUp}$ surface consists exactly of carrier admission for the displayed eleven-row packet; tail-window exposure through $S$; regular-rational readback through $R$; dyadic tolerance exposure through $D$; tail-seal witnessing through $W$; filter-facing sealing through $F$; terminal Real sealing through $E$; vision-ledger routing through $L$; componentwise $\hsame$ transport through $H$; displayed $\Cont$ replay through $C$; provenance through $P$; and local naming through $N$.
\end{theorem}
\begin{proof}
Project the carrier of \autoref{def:regular-cauchy-filter-seal-carrier}. The analytic tail route is exactly $S,R,D,W,F,E,L$, and $H,C,P,N$ are the structural rows.

The regular Cauchy evidence is read through finite windows and dyadic budgets. The row $S$ opens the inspected tail windows, $R$ supplies the regular rational readback, and $D$ fixes the tolerance scale.

The seal rows are downstream. The witness row $W$ records the tail comparison, the filter-facing row $F$ packages that comparison as a seal, and the terminal row $E$ is exposed only after the earlier rows are visible.

Finally, the ledger and structural rows bind the route to the regular Cauchy vision site and replay it for later consumers. Because no excluded object is a carrier coordinate, quotient streams, selected limits, countable choice, host equality, trichotomy, ambient completeness, and bridge credentials cannot become local obligations.
\end{proof}

\begin{theorem}[Regular Cauchy filter seal tail route]
\label{thm:regular-cauchy-filter-seal-tail-route}
Every Real-facing or Completion-facing consumer of an accepted $\RegularCauchyFilterSealUp$ packet reads the tail-filter seal only through
$$
\StreamNameUp \longmapsto \RegSeqRatUp \longmapsto \DyadicRatCoreUp \longmapsto \RegularCauchyFilterSealUp \longmapsto \RealUp .
$$
The route realizes the regular Cauchy diagonal and tail-seal rows of \autoref{sec:real-completeness-regular-cauchy} as finite StreamName, RegSeqRat, DyadicRatCore, and Real seal data. It exports no quotient stream, selected limit, countable-choice schedule, host equality proof, trichotomy decision, ambient $\RealUp$ completeness theorem, or bridge credential.
\end{theorem}
\begin{proof}
Use the obligation surface of \autoref{thm:regular-cauchy-filter-seal-namecert-obligations}. It forces each accepted read to expose $S,R,D$ before the tail witness or filter-facing seal can be consumed.

The row $W$ records the regular Cauchy tail comparison inside the displayed dyadic budget, and $F$ packages that comparison as the filter-facing seal. The terminal row $E$ is therefore downstream of the finite tail route, not a selected limit.

The ledger row $L$ ties this route to the regular Cauchy diagonal and tail-seal markers, while $H,C,P,N$ transport, replay, source, and name the same packet. Since the carrier has no excluded coordinate, the stated nonescape claims follow.
\end{proof}

\falsifiablePrediction{An accepted $\RegularCauchyFilterSealUp$ consumer that reaches a filter-facing seal or terminal $\RealUp$ row without first displaying the $\StreamNameUp$ tail window, $\RegSeqRatUp$ readback, and $\DyadicRatCoreUp$ tolerance ledger refutes the carrier surface.}

\independenceWitness{streamname, regseqrat, dyadicratcore, real, regular-cauchy-vision: no listed sibling supplies the whole tail-filter seal packet $T=(S,R,D,W,F,E,L,H,C,P,N)$, because each sibling contributes only a window row, regular readback, tolerance ledger, terminal seal, or vision marker while this chapter binds them into one finite seal certificate.}

\closureat{RegularCauchyFilterSealUp}{seedStr}

\begin{closurestatus}{\RegularCauchyFilterSealUp}
  \constructivestory{A $\RegularCauchyFilterSealUp$ packet is generated from StreamName tail windows, RegSeqRat readback rows, DyadicRatCore tolerance ledgers, tail-seal witness rows, filter-facing seal rows, terminal RealUp seal boundaries, vision ledger rows, componentwise hsame transport, displayed Cont replay, Pkg provenance, and a local NameCert row.}
  \theoryclosure{\seedClosure}
  \scopeclosed{\autoref{def:regular-cauchy-filter-seal-carrier}, \autoref{thm:regular-cauchy-filter-seal-namecert-obligations}, and \autoref{thm:regular-cauchy-filter-seal-tail-route} seed the finite tail-filter seal route over StreamName, RegSeqRat, DyadicRatCore, and Real rows.}
  \formalstatus{\unformalizedV}
  \bridgestatus{none}
  \notclaimed{This chapter does not close quotient streams, selected limits, countable choice, host equality, trichotomy, RealUp completeness, Lean theorem coverage, or bridge checking.}
  \upgradepath{Obligation closure requires checked carrier admission, tail-window exposure, RegSeqRat readback, dyadic tolerance exactness, tail-seal witnessing, filter-facing seal nonescape, Real-seal nonescape, transport, replay, provenance, and local naming for BEDC.Derived.RegularCauchyFilterSealUp.}
\end{closurestatus}
