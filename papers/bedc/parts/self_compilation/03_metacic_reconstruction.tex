\section{MetaCIC Reconstruction (V1-V3 Analysis)}\label{sec:self-comp-metacic}

The MetaCIC reconstruction surface asks how much of the host calculus can be
represented as BEDC data and predicates. It is a reconstruction analysis, not a
replacement of the host. The relevant namespace is `\verb|BEDC.MetaCIC|', whose
role is to name the current surface on which CIC syntax, typing, and reduction
can be discussed as first-class BEDC objects.

V1 is the achievable structural layer. At this layer BEDC constructs syntax,
contexts, terms, universe annotations as needed, beta-reduction, and typing
judgments as inductive objects or inductive predicates over BEDC histories.
\leandef{BEDC.MetaCIC.Term}
\leandef{BEDC.MetaCIC.BetaStep}
\leandef{BEDC.MetaCIC.HasType}
The result is not a claim that BEDC has reimplemented Lean. It is a claim that
the grammar and judgment forms of a CIC fragment can be represented directly
enough to support quantification and induction over them.

The difference between behavior and structure is central. A Turing-machine
simulation of Lean execution can describe observed runs:

$$
\begin{aligned}
\exists t,\ \mathrm{MachineTrace}(\mathrm{Lean},x,t)
  \wedge \mathrm{Accepts}(t).
\end{aligned}
$$

Such a statement is behavioral and trace-based. It predicts or records a
finite execution. A V1 inductive predicate has a different form:

$$
\begin{aligned}
\Gamma \vdash_{\mathrm{MetaCIC}} t : T.
\end{aligned}
$$

This is a first-class judgment. BEDC can quantify over contexts, terms, and
derivations, and can prove results by induction on the derivation. TM
simulation gives Sigma-one execution traces; direct V1 reconstruction gives
typed judgment objects. The base sort judgment is inhabited in the empty
context. \leanchecked{BEDC.MetaCIC.sort\_in\_empty\_ctx}

The same fragment carries concrete checked examples. A polymorphic identity
term over `Sort' is well typed, and its Pi type is itself a represented typing
judgment. \leanchecked{BEDC.MetaCIC.id\_sort\_well\_typed}
\leanchecked{BEDC.MetaCIC.pi\_sort\_sort\_well\_typed} Applying that identity
to `Sort' has the expected represented application typing, and the corresponding
beta step contracts to the argument. \leanchecked{BEDC.MetaCIC.id\_sort\_applied}
\leanchecked{BEDC.MetaCIC.id\_sort\_beta} A singleton sort context also exposes
the ordinary variable-lookup case for de Bruijn index zero.
\leanchecked{BEDC.MetaCIC.var\_zero\_in\_sort\_ctx}

V1 therefore provides the self-compilation construction surface for syntax and
judgments. It supports statements of the form ``for every represented derivation
with this constructor shape, the following structural property holds.'' It also
supports explicit boundary claims: which syntax is represented, which
conversion relation is present, which typing rules are included, and which
host-kernel features remain outside the fragment.

V2 is the metatheory layer. Subject reduction and confluence are the natural
targets here. Subject reduction says that typing survives reduction:

$$
\begin{aligned}
\Gamma \vdash t : T \wedge t \to_{\beta} u
  \longrightarrow \Gamma \vdash u : T.
\end{aligned}
$$

Confluence says that divergent reductions can be joined. These theorems are
structural metatheorems about the represented calculus, and substantial parts of
them are appropriate targets for BEDC once V1 is fixed with sufficient
precision.

Strong normalization has a different status. A global strong-normalization
theorem strong enough to recover consistency of the represented host calculus
meets the Godel-II boundary when the represented calculus is comparable in
strength to the host in which the proof is carried out. BEDC can study bounded
or fragment-specific termination claims, and it can state which hypotheses make
those claims available. It does not obtain an axiom-free global consistency
certificate for its own host by internal reconstruction.

V3 is the replacement reading: BEDC becomes the foundation instead of CIC. This
is not a claim of the paper. BEDC is formalized inside the host CIC. All Lean
theorems in the project are checked by that host. The MetaCIC surface may
represent syntax and prove fragment metatheory, but it does not move BEDC
outside the host or give BEDC a stronger foundational status than the kernel
that checks it.

This yields the governing inclusion:

$$
\begin{aligned}
\mathrm{BEDC\ formalization} \subseteq \mathrm{host\ CIC}.
\end{aligned}
$$

The inclusion is methodological, not merely notational. A theorem about a
represented calculus is a theorem inside the host. It can be useful, precise,
and internally quantified, but it cannot serve as a host-independent foundation
for the host without crossing the Godel-Tarski boundary.

The MetaCIC reconstruction surface thus complements the Manifest. The Manifest
registers finite paper-anchored theorem entries as checked Lean data. MetaCIC
reconstruction studies the syntax and judgment structure of the calculus in
which such checking is meaningful. Together they explain why ``BEDC compiles
itself'' has a legitimate L4-mid reading and no legitimate L4a or V3 reading.

\subsection{Substrate Gap Discovery}\label{subsec:substrate-gap-discovery}

The substitution preservation lemma for the represented type theory exposes
the substrate's hidden requirements in successive stages, each identified by a
Lean-checked counterexample.

\paragraph{First stage: shift in substitution.} A direct
$\mathrm{substitute}$ function that omits a binder-aware shift cannot satisfy
the preservation lemma. Substitution under a $\mathrm{lam}$ or $\mathrm{pi}$
binder must lift free variables of the substituted term to account for the
surrounding binder. The function is therefore equipped with a separate
$\mathrm{shift}$ operation:

$$
\begin{aligned}
\mathrm{substitute}\ d\ v\ (\mathrm{lam}\ \mathrm{dom}\ \mathrm{body})
  ={}& \mathrm{lam}\ (\mathrm{substitute}\ d\ v\ \mathrm{dom}) \\
     & (\mathrm{substitute}\ (d+1)\ (\mathrm{shift}\ 0\ 1\ v)\ \mathrm{body}).
\end{aligned}
$$

This is the first layer of substrate the represented type theory requires.

\paragraph{Second stage: shift in context lookup.} With binder-aware
substitution in place, the preservation lemma is still refutable. A
Lean-checked counterexample arises with $\Gamma = [\mathrm{var}\ 1]$,
$B = \mathrm{var}\ 1$, $s = \mathrm{var}\ 0$, $t = \mathrm{var}\ 0$,
$A = \mathrm{var}\ 1$: both premises hold via the variable rule under
unshifted lookup, but the substituted conclusion type does not match the
substituted lookup result. The substrate fix is a shift in
$\mathrm{Ctx.lookup}$ that lifts looked-up types by the cons depth.

\paragraph{Third stage: context well-formedness.} Even with shifted lookup, a
further counterexample remains. The context $[\mathrm{var}\ 1]$ is itself
ill-typed: the type $\mathrm{var}\ 1$ references a de Bruijn index that does
not exist in the context. The substitute preservation statement is therefore
false in general; it is true only when the context is well-formed.
The substrate fix is an inductive predicate

$$
\begin{aligned}
  \mathrm{WellFormedCtx} : \mathrm{Ctx} \to \mathrm{Prop}
\end{aligned}
$$

generated by

$$
\begin{aligned}
  & \frac{}{\mathrm{WellFormedCtx}\ []} \\[4pt]
  & \frac{
      \mathrm{WellFormedCtx}\ \Gamma
      \quad
      \mathrm{HasType}\ \Gamma\ T\ \mathrm{sort}
    }{
      \mathrm{WellFormedCtx}\ (T :: \Gamma)
    }.
\end{aligned}
$$

The preservation lemma carries an explicit
$\mathrm{WellFormedCtx}\ (B :: \Gamma)$ premise.

\paragraph{Sixth stage: Bool-level substitute.} With context well-formedness
in place, the generalized substitute preservation proof identifies a further
substrate constraint. The V3 $\mathrm{substitute}$ implementation uses
Prop-level conditional branching on de Bruijn index decidability:

$$
\begin{aligned}
  \mathrm{substitute}\ d\ v\ (\mathrm{var}\ i) =
  \mathrm{if}\ i = d\ \mathrm{then}\ v\
  \mathrm{else}\ \mathrm{if}\ i > d\ \mathrm{then}\ \mathrm{var}\ (i{-}1)
  \mathrm{else}\ \mathrm{var}\ i.
\end{aligned}
$$

Generalized substitution-shift lemmas require structural induction through
these conditional positions. The structural induction inherits Lean's stdlib
decidable-equality machinery, which has $\mathrm{propext}$ in its transitive
provenance. Under the tactic discipline of \autoref{sec:pure-cic-tactics},
this disqualifies the proof.

The substrate fix is to re-implement $\mathrm{substitute}$ using Boolean
branching:

$$
\begin{aligned}
  \mathrm{substitute}\ d\ v\ (\mathrm{var}\ i) =
  \mathrm{match}\ \mathrm{Nat.beq}\ i\ d,\ \mathrm{Nat.blt}\ d\ i\
  \mathrm{with}
  \\
  \mid \mathtt{true},\, \_ \Rightarrow v
  \\
  \mid \mathtt{false},\, \mathtt{true}
  \Rightarrow \mathrm{var}\ (i{-}1)
  \\
  \mid \mathtt{false},\, \mathtt{false}
  \Rightarrow \mathrm{var}\ i.
\end{aligned}
$$

The match dispatches structurally on $\mathrm{Bool}$ values without invoking
decidable-equality Prop machinery, so structural induction through these
positions stays in pure CIC.

This is a discipline-driven substrate decision rather than a mathematical
necessity. A Lean formalization that admits $\mathrm{propext}$ may use the
Prop-level form without difficulty. BEDC's pure-CIC invariant places the
constraint as a load-bearing condition, surfacing the decidable-if cost only
when the substitute preservation proof is attempted in earnest. The
substrate-gap discovery sequence therefore has methodological as well as
type-theoretic content.

\paragraph{Seventh stage: shift propagation through binders.} The V6
Bool-level substitute removes the $\mathrm{propext}$ exposure, and the
substitution preservation weakening step exposes a further substrate issue.
The syntactic shift function applies $\mathrm{shift}\ 0\ 1$ to terms
uniformly, but Pi codomain shifts must additionally reflect the post-shift
body context's variable re-binding. Concretely,

$$
\begin{aligned}
  \mathrm{shift}\ 0\ 1\ (\Pi\ (\mathrm{var}\ 0).\ \mathrm{var}\ 0) =
  \Pi\ (\mathrm{var}\ 1).\ \mathrm{var}\ 0.
\end{aligned}
$$

Under weakening from $\Gamma$ to $(B :: \Gamma)$, the resulting
$\mathrm{lam}$ in the shifted body context $[\mathrm{var}\ 1, \ldots]$
type-checks the body $\mathrm{var}\ 0$ at lookup-type $\mathrm{var}\ 1$,
requiring the Pi codomain to be $\mathrm{var}\ 1$, not $\mathrm{var}\ 0$.
The naive weakening lemma is therefore unprovable for terms whose binder
bodies reference previously bound variables.

The substrate fix is non-trivial. Three candidate approaches are available:
\begin{itemize}
  \item Re-engineer $\mathrm{shift}$ to propagate context-dependent variable
    re-bindings through binder codomains, requiring extra context tracking;
  \item Restrict the substitution preservation statement to closed terms,
    terms with no free de Bruijn variables in the affected range, sidestepping
    the binder propagation issue;
  \item Reformulate $\mathrm{HasType}$ to treat the codomain modulo a
    syntactic equivalence relation that absorbs the shift gap.
\end{itemize}

The closed-term restriction is the most tractable substrate-friendly option.
Closed-term substitution preservation is recorded as
\autoref{thm:closed-term-substitute-preserves-typing}; it sidesteps the
binder propagation gap at the cost of generality.

\paragraph{Ninth stage: depth-parameterized form also refuted.} The
textbook-standard depth-parameterized statement
$$
\begin{aligned}
  & \mathrm{substitute\_preserves\_typing\_general} : \\
  & \quad \forall \Gamma_1\ \Gamma_2,\
    \mathrm{WellFormedCtx}\ (\Gamma_1 \mathbin{+\!+} B :: \Gamma_2) \to
    \mathrm{HasType}\ (\Gamma_1 \mathbin{+\!+} B :: \Gamma_2)\ t\ A \to \\
  & \qquad \mathrm{HasType}\ \Gamma_2\ s\ B \to
    \mathrm{HasType}\ (\Gamma_1 \mathbin{+\!+} \Gamma_2)\
      (\mathrm{substitute}\ |\Gamma_1|\ s\ t)\
      (\mathrm{substitute}\ |\Gamma_1|\ s\ A)
\end{aligned}
$$
admits a Lean-verified counterexample when $\Gamma_1 = []$: the statement
degenerates to the existing minimal counterexample at depth 0.
Depth-parameterization alone does not resolve the underlying
$\mathrm{lookup}/\mathrm{shift}$ interaction. The substrate fix likely
requires re-engineering one of: \texttt{Ctx.lookup}, \texttt{shift}, or the
typing rule for \texttt{lam} (e.g., adding context-well-formedness premises to
the \texttt{lamRule}).

\paragraph{Tenth stage: substitute composition unconditional form refuted.}
The textbook substitute composition equation
$$
\begin{aligned}
  \mathrm{substitute}\ 0\ s\ (\mathrm{substitute}\ 0\ a\ \mathrm{cod}) =
  \mathrm{substitute}\ 0\ (\mathrm{substitute}\ 0\ s\ a)\
    (\mathrm{substitute}\ 1\ s\ \mathrm{cod})
\end{aligned}
$$
admits a Lean-verified counterexample without closure constraints on
$s$. The substrate fix is to require $\mathrm{ClosedAt}\ 0\ s$ as an
additional hypothesis; under that hypothesis, all five inductive cases
of $\mathrm{cod}$ are covered. The sort case is unconditional, the var
case assumes $\mathrm{ClosedAt}\ 0\ s$, the app case uses two recursive
premises, and the lam and pi cases are conditional on the
shift-substitute commutation equation
$$
\begin{aligned}
  \mathrm{shift}\ 0\ 1\ (\mathrm{substitute}\ 0\ s\ a) =
  \mathrm{substitute}\ 1\ (\mathrm{shift}\ 0\ 1\ s)\
    (\mathrm{shift}\ 0\ 1\ a).
\end{aligned}
$$
The commutation lemma is the remaining piece for full unconditional
composition closure.

The pattern repeats: each composition lemma's unconditional form
fails, and the closed-term restriction makes a sub-cases tractable
without resolving the underlying $\mathrm{shift}/\mathrm{substitute}$
binder-propagation issue identified at finding 7.

\paragraph{Eleventh stage: WF premise does not discharge R8.}
The proof program for
$\mathrm{closed\_term\_substitute\_preserves\_typing}$ has a substrate
constraint: the well-formedness premise
$\mathrm{WellFormedCtx}\ (B :: \Gamma)$ does not discharge the R8
counterexample by ruling out the ill-typed source context
$[\mathrm{var}\ 1]$.

A WF-conformant variant of the R8 counterexample exists:
$\Gamma = []$, $B = \mathrm{sort}$, $s = \mathrm{sort}$,
$t = \mathrm{lam}\ (\mathrm{var}\ 0)\ (\mathrm{var}\ 0)$. Here:
\begin{itemize}
  \item $\mathrm{WellFormedCtx}\ [\mathrm{sort}]$ is provable
    because sort itself is well-formed at depth 0;
  \item $\mathrm{ClosedAt}\ 0\ B$ and $\mathrm{ClosedAt}\ 0\ s$ both hold;
  \item the source $\mathrm{HasType}\ [\mathrm{sort}]\ t\ A$ types
    via the lambda rule;
  \item the substituted target is not derivable under any
    $\mathrm{HasType}$ rule in the current calculus.
\end{itemize}

The substrate fix for the main theorem therefore requires either
re-engineering the lambda typing rule so its codomain tracks shift
propagation explicitly, changing $\mathrm{Ctx.lookup}$ semantics, or
restating the main theorem with a different invariant, such as over fully
closed terms only, where substitute reduces to identity.

\paragraph{Twelfth stage: subject reduction also refuted.}
Subject reduction for the represented type theory admits a
Lean-verified counterexample using the substitute preservation
counterexample as the typed-beta-redex source: applying the lambda
$\mathrm{lam}\ (\mathrm{var}\ 0)\ (\mathrm{var}\ 0)$ to $\mathrm{sort}$
in context $[\mathrm{sort}]$ produces a beta step whose target
$\mathrm{substitute}\ 0\ \mathrm{sort}\ (\mathrm{var}\ 0) = \mathrm{sort}$
is not derivable at the source type. The substrate-design fix that
would discharge R11 would also discharge R12, since both depend on
the same lam-codomain propagation behavior.

\begin{table}[t]
\centering
\begin{tabular}{r|p{0.34\linewidth}|p{0.34\linewidth}|l}
\hline
No. & Finding & Consequence & Status \\
\hline
1 & Shift in substitution required & Binder-aware substitution required & Closed \\
\hline
2 & Shift in context lookup required & Lookup must account for cons depth & Closed \\
\hline
3 & Context well-formedness required & Ill-typed contexts excluded & Closed \\
\hline
4 & Unrestricted statement shape refuted & Premises must expose context validity & Closed \\
\hline
5 & Naive generalized statement shape refuted & Statement shape constrained by lookup and substitution & Closed \\
\hline
6 & Prop-level substitute exposes $\mathrm{propext}$ & Bool-level substitute required for pure CIC & Closed \\
\hline
7 & Shift propagation through binders exposed & Closed-term route or substrate redesign required & Open \\
\hline
8 & Depth-zero generalized form refuted & Minimal counterexample remains active & Open \\
\hline
9 & Depth-parameterized form refuted & Substrate redesign required & Open \\
\hline
10 & Unconditional composition form refuted & Closure restriction (ClosedAt 0 s) & 5/5 cases, two conditional \\
\hline
11 & WF premise does not discharge R8 counterexample & Substrate redesign of lam typing rule & Open \\
\hline
12 & Subject reduction also refuted & Same lam typing redesign as R11 & Open \\
\hline
\end{tabular}
\caption{Substrate-gap findings for MetaCIC substitution preservation.}
\label{tab:metacic-substrate-gap-findings}
\end{table}

\paragraph{Methodological note.} The seventh finding differs in character
from findings one through six. The first three are substrate omissions; the
fourth and fifth are statement-shape errors; the sixth is a tactic-discipline
obstacle, the $\mathrm{propext}$ leakage through Prop-level decidable-if,
addressed by Bool-level substitution. The seventh is a substrate-design issue
that requires a structural choice: re-engineer shift, restrict to closed
terms, or change typing equivalence. It is not a missing piece of the existing
V6 substrate; it is a constraint of V6's chosen shift semantics that surfaces
at the weakening lemma.

\paragraph{Position.} The progression illustrates a methodological point:
when the represented system's metatheorem is not provable, the substrate is
incomplete. Each Lean-checked rejection identifies the next gap rigorously, not
by suspicion. The substrate built up through the discovery sequence is what
the represented type theory actually requires to support its metatheorems,
distilled bottom-up rather than imported from an external reference.

\paragraph{Note on tactic discipline.} Each Lean check above is constrained to
the pure-CIC tactic subset codified in \autoref{sec:pure-cic-tactics},
ensuring that the counterexamples are genuine refutations rather than
artifacts of impure proof automation.

\subsubsection*{Summary Table}\label{subsubsec:gap-summary-table}

\begin{table}[h]
\centering
\begin{tabular}{|c|l|l|l|}
\hline
\textbf{\#} & \textbf{Finding} & \textbf{Substrate Fix} & \textbf{Status} \\
\hline
1 & V1 substitute lacks binder shift & V3 substitute with shift & Fixed \\
\hline
2 & V1 Ctx.lookup unshifted tail types & V4 lookup with cons-depth shift & Fixed \\
\hline
3 & Context not required well-formed & V5 WellFormedCtx inductive & Fixed \\
\hline
4 & Naive cons-form weakening unprovable & N/A (statement-shape) & Counterexample \\
\hline
5 & Generalized n=0 weakening with WF & N/A (statement-shape) & Counterexample \\
\hline
6 & Prop-level if leaks propext & V6 Bool-level branching & Fixed \\
\hline
7 & Shift binder propagation issue & Closed-term restriction & Workaround \\
\hline
8 & ClosedAt 0 not propagating in lam & Depth-parameterized statement & Open \\
\hline
9 & Depth-parameterized form refuted & Substrate redesign required & Open \\
\hline
10 & Unconditional composition form refuted & Closure restriction (ClosedAt 0 s) & 5/5 cases, two conditional \\
\hline
11 & WF premise does not discharge R8 counterexample & Substrate redesign of lam typing rule & Open \\
\hline
12 & Subject reduction also refuted & Same lam typing redesign as R11 & Open \\
\hline
\end{tabular}
\caption{Twelve substrate-gap findings, each Lean-verified.}
\end{table}

\paragraph{V6 substrate metatheoretic boundary.}
The twelve substrate-gap findings collectively map the V6 MetaCIC
substrate's metatheoretic boundary. Both substitute preservation
and subject reduction are refuted under the current
$\mathrm{Typing}$ rules; each is documented by a Lean-verified
counterexample. The findings divide into three classes:
\begin{itemize}
  \item \textbf{Substrate omissions} (R1-R3): substitute lacked binder
    shift, lookup lacked shift, context lacked well-formedness; each
    fixed by a corresponding substrate addition.
  \item \textbf{Statement-shape failures} (R4-R5, R7-R9, R11): naive
    forms of weakening, generalized weakening, and substitute
    preservation each refuted; restricted forms or closed-self
    variants admit proofs in specific sub-cases.
  \item \textbf{Tactic discipline} (R6) and \textbf{metatheorem
    refutations} (R10, R12): unconditional substitute composition
    refuted, subject reduction refuted; partial coverage via closure
    restrictions.
\end{itemize}
The substrate is consistent (no axioms, all proofs axiom-free); it
admits substantial fragments of the represented metatheory (case
lemmas, identities on closed terms, decidable type checking). It
does not admit unconditional substitute preservation or subject
reduction. Further progress requires substrate-design choices
(typing rule restructuring or context-aware shift propagation) that
are outside the present chapter's scope.

\subsection{V7 Metatheory Progress}\label{subsec:v7-metatheory-progress}

The V7 substrate is represented by \texttt{BEDC.MetaCIC.TypingV2}. Its
sanity layer covers the sort, Pi, identity, application, and dependent
identity examples, and its atomic substitution layer is no longer a schematic
placeholder: sort and variable atoms are checked directly.
\leanchecked{BEDC.MetaCIC.V2.substitute\_preserves\_typing\_V2\_sort\_var}
The captured-substitution demonstration also has a positive source typing
derivation and a rejected captured target.
\leanchecked{BEDC.MetaCIC.V2.r11\_substitute\_preserves\_typing\_V2}

The remaining Pi case is a structural obstacle rather than a missing
constructor case. Under V7, the binder premise for Pi is typed below
$(\mathrm{shift}\ 0\ 1\ \mathrm{dom}) :: \Gamma$. Substitution through that
premise reduces to a depth-1 typing-preservation induction: the recursive
argument must preserve typing under the shifted binder domain while the
substituting term is lifted across the same binder. The present substrate
does not directly expose that depth-1 induction principle, so Pi-case
substitute preservation is the missing bridge between the checked atomic
cases and the full substitution theorem.

The beta case of subject reduction is therefore isolated in
\texttt{BEDC.MetaCIC.SubjectReductionV2}. It is provable from a clean
substitution-preservation lemma:
\leandef{BEDC.MetaCIC.V2.BetaSubstitutionPreservationV2}
\leanchecked{BEDC.MetaCIC.V2.subject\_reduction\_V2\_beta}
This puts the beta subject-reduction burden exactly on
$\mathrm{BetaSubstitutionPreservationV2}$, without adding an extra
operational premise to beta reduction itself.

\texttt{BEDC.MetaCIC.V6V7Bridge} identifies the semantic footprint of the
V7 redesign. If every lambda and Pi binder domain in a derivation satisfies
$\mathrm{ClosedAt}\ 0$, then the V6 and V7 typing relations translate in both
directions:
\leanchecked{BEDC.MetaCIC.hasType\_V6\_to\_V2\_closedDomains}
\leanchecked{BEDC.MetaCIC.hasType\_V2\_to\_V6\_closedDomains}
Thus V7 is a genuinely different substrate only where binder domains remain
open. For closed binder domains, the shift inserted by V7 is judgmentally
absorbed by closedness, and the two derivation systems coincide.

\subsection{Substrate Stack}\label{subsec:substrate-stack}

The V1 MetaCIC reconstruction is a layered stack of Lean modules. Each module
contributes a specific fragment of the represented type theory's substrate.
The stack is finite, ordered by formal dependency, and audited at PR merge
time.

\paragraph{Syntax layer.} \texttt{BEDC.MetaCIC.Syntax} provides
$\mathrm{Term}$ (an inductive with five constructors: $\mathrm{var}$,
$\mathrm{app}$, $\mathrm{lam}$, $\mathrm{pi}$, $\mathrm{sort}$),
$\mathrm{shift}$ (de Bruijn lifting at cutoff with explicit amount,
dispatching on Boolean $\mathrm{Nat.ble}$), and $\mathrm{substitute}$
(capture-aware substitution at arbitrary depth, dispatching on Boolean
$\mathrm{Nat.beq}$ and $\mathrm{Nat.blt}$; the Bool-level branching avoids the
$\mathrm{propext}$ exposure that Prop-level decidable-if would introduce for
generalized substitute lemmas).

\paragraph{Typing layer.} \texttt{BEDC.MetaCIC.Typing} provides
$\mathrm{Ctx}$ as a list of represented types, $\mathrm{Ctx.lookup}$ with
depth-aware $\mathrm{shift}$ on tail-looked-up types, and the inductive
predicate
$$
\begin{aligned}
\mathrm{HasType} : \mathrm{Ctx} \to \mathrm{Term} \to \mathrm{Term}
  \to \mathrm{Prop}.
\end{aligned}
$$
Its formation rules are $\mathrm{sortRule}$, $\mathrm{varRule}$,
$\mathrm{piRule}$, $\mathrm{lamRule}$, and $\mathrm{appRule}$.

\paragraph{Reduction layer.} \texttt{BEDC.MetaCIC.Beta} provides
$\mathrm{BetaStep}$ with one base beta rule and three congruence rules:
app-left, app-right, and lam-body.

\paragraph{Well-formedness layer.} \texttt{BEDC.MetaCIC.ContextWF} provides
$\mathrm{WellFormedCtx}$ with empty and cons formation rules. The cons rule
requires the head type to be derivable at $\mathrm{Sort}$ in the predecessor
context. This premise is required for substitution preservation: without it,
ill-typed contexts admit refutations of preservation.

\paragraph{Substitution-preservation layer.}
\texttt{BEDC.MetaCIC.Substitution} hosts the substitution-preservation target
with the $\mathrm{WellFormedCtx}$ premise. Its proof body is the active
research target.

\paragraph{Subject-reduction layer.}
\texttt{BEDC.MetaCIC.SubjectReduction} depends on substitution preservation
and is the next downstream target.

\paragraph{Operational layer.} \texttt{BEDC.MetaCIC.Examples} carries
concrete $\mathrm{HasType}$ witnesses for representative terms: the base sort
judgment, the polymorphic identity, and variable lookup.
\texttt{BEDC.MetaCIC.LeanBridge} carries one worked encoding of a host CIC
term into MetaCIC with its $\mathrm{HasType}$ derivation.

\paragraph{Decidable layer.} \texttt{BEDC.MetaCIC.Checker} provides
$\mathrm{Term.beq}$, structural equality on terms, with soundness
$\mathrm{Term.beq\_eq}$; $\mathrm{infer}$, type inference returning
$\mathrm{Option}\ \mathrm{Term}$, with soundness $\mathrm{infer\_sound}$; and
$\mathrm{check}$, decidable typing check, with soundness
$\mathrm{check\_sound}$.

\paragraph{Display and elaboration layer.}
\texttt{BEDC.MetaCIC.PrettyPrint} provides a display function
$\mathrm{Term} \to \mathrm{String}$. \texttt{BEDC.MetaCIC.Elaborator}
provides $\mathrm{ElabFailure}$, $\mathrm{ElabResult}$,
$\mathrm{normalizeOneStep}$ for single-step beta reduction, and
$$
\begin{aligned}
\mathrm{elaborate} : \mathrm{Ctx} \to \mathrm{Term} \to \mathrm{ElabResult}.
\end{aligned}
$$

\paragraph{Reflection bridge implementation status.}
$\texttt{BEDC.MetaCIC.HostBridge}.\mathrm{reflectExpr}$ covers eight
host Lean $\mathrm{Expr}$ constructors: $\mathrm{sort}$,
$\mathrm{bvar}$, $\mathrm{app}$, $\mathrm{lam}$,
$\mathrm{forallE}$, $\mathrm{const}$, $\mathrm{fvar}$ returning
$\mathrm{none}$, and $\mathrm{mdata}$ as a passthrough. Two elaboration
commands surface the encoding to BEDC automation:
$\#\mathrm{reflect\_metacic}\ e$ prints the $\mathrm{Term}$ form, and
$\#\mathrm{metacic\_decide}\ e$ runs $\mathrm{infer}$ on the reflected
$\mathrm{Term}$ and prints the inferred MetaCIC type. The latter is the
end-to-end demonstration of the host-MetaCIC pipeline.

\paragraph{Audit gating.} Every module in the stack is gated by
\texttt{check-axioms.py}, which rejects raw \texttt{axiom} declarations;
\texttt{bedc\_ci.py axiom-purity --strict}, which rejects transitive
\texttt{propext}, \texttt{Classical.choice}, and \texttt{Quot.sound}
dependencies; \texttt{bedc\_ci.py audit}, which rejects paper-Lean marker
drift; and \texttt{bedc\_ci.py marker-existence-audit}, which rejects paper
references to non-existent Lean names. A module that fails any of these
audits is rejected at PR merge time.

\paragraph{Stack closure.} The substrate stack is open at the
substitution-preservation layer and closed elsewhere: the remaining layers
carry definitions and theorems that are axiom-free under
\texttt{\#print axioms}. Subject reduction depends on substitution
preservation; closing the substitution layer is the prerequisite for opening
the subject-reduction layer. No layer is closed by importing unverified
content from outside the stack.

\subsection{Current State of the Reconstruction}\label{subsec:current-state}

This subsection records the state of the MetaCIC reconstruction as encoded
in BEDC's Lean library.

\paragraph{Definitions in place.} The represented type theory exposes
$\mathrm{Term}$, $\mathrm{Ctx}$, $\mathrm{HasType}$,
$\mathrm{BetaStep}$, $\mathrm{shift}$, $\mathrm{substitute}$,
$\mathrm{WellFormedCtx}$, $\mathrm{infer}$, $\mathrm{check}$,
$\mathrm{Term.beq}$, $\mathrm{pp}$, $\mathrm{normalizeOneStep}$, and
$\mathrm{elaborate}$. Each definition is axiom-free under
\texttt{\#print axioms}.

\paragraph{Theorems with full proofs.} The library carries axiom-free proofs
for:
\begin{itemize}
  \item Sanity instances of $\mathrm{HasType}$: $\mathrm{sort}$ in the empty
    context, polymorphic identity at the $\mathrm{Sort}$ level,
    $\mathrm{Pi}$ types, and variable lookup in singleton contexts.
  \item A worked encoding of host single-sort polymorphic identity as a
    MetaCIC $\mathrm{Term}$ with its $\mathrm{HasType}$ derivation
    (\autoref{sec:self-comp-metacic}).
  \item Soundness of $\mathrm{Term.beq}$ as structural equality.
  \item Soundness of $\mathrm{infer}$ against $\mathrm{HasType}$.
  \item Soundness of $\mathrm{check}$ against $\mathrm{HasType}$.
\end{itemize}

\paragraph{Theorems with statements pending proof.} The library carries the
unrestricted target, $\mathrm{substitute\_preserves\_typing}$, guarded by a
$\mathrm{WellFormedCtx}$ premise. Its proof requires weakening of
$\mathrm{HasType}$, substitution-shift composition, and binder substitution
composition. The same substrate also supports
$\mathrm{closed\_term\_substitute\_preserves\_typing}$ as a more tractable
closed-term variant. The substrate gap analysis in
\autoref{subsec:substrate-gap-discovery} identifies the
$\mathrm{WellFormedCtx}$ requirement and the binder-shift propagation issue;
the remaining proof work is the induction-based gluing from the available
helper lemmas.

\paragraph{Closed-term substitute preservation.} The library carries
a closed-term restriction of substitute preservation:
$$
\begin{aligned}
  \mathrm{closed\_term\_substitute\_preserves\_typing} :
  \mathrm{WellFormedCtx}\ (B :: \Gamma) \to
  \mathrm{ClosedAt}\ 0\ B \to
  \mathrm{ClosedAt}\ 0\ s \to \\
  \mathrm{HasType}\ (B :: \Gamma)\ t\ A \to
  \mathrm{HasType}\ \Gamma\ s\ B \to \\
  \mathrm{HasType}\ \Gamma\ (\mathrm{substitute}\ 0\ s\ t)\
    (\mathrm{substitute}\ 0\ s\ A).
\end{aligned}
$$
The statement is registered as
$\mathrm{ClosedTermSubstitutePreservesTypingStatement}$. The
infrastructure---$\mathrm{ClosedAt}$ inductive, $\mathrm{shift\_closed}$,
$\mathrm{substitute\_closed}$, $\mathrm{substitute\_shift\_at\_eq}$---is in
place with axiom-free proofs.

\paragraph{Closed-term substitute preservation: case status.}
The closed-term restriction has axiom-free case lemmas covering all five
inductive cases of the typing derivation. The variable case is split by
depth, and the application helper carries a type-equality premise:

\begin{itemize}
  \item $\mathrm{substitute\_sort\_preserves\_typing\_closed\_anchor}$ (sort case);
  \item $\mathrm{substitute\_var\_zero\_preserves\_typing\_closed\_anchor}$ (variable, depth 0);
  \item $\mathrm{substitute\_var\_nonzero\_preserves\_typing\_closed\_anchor}$ (variable, depth > 0);
  \item $\mathrm{substitute\_pi\_preserves\_typing\_closed\_anchor}$ (Pi);
  \item $\mathrm{substitute\_lam\_preserves\_typing\_closed\_anchor}$ (lambda, proved via term induction; the $\mathrm{HasType}$-induction path at this case is substrate-gap 8 territory);
  \item $\mathrm{substitute\_app\_preserves\_typing\_closed\_anchor\_with\_type\_eq}$ (application, conditional on a substitute composition equation).
\end{itemize}

The remaining work for the main theorem is two pieces:
\begin{enumerate}
  \item Prove the substitute composition equation
    $\mathrm{substitute}\ 0\ s\ (\mathrm{substitute}\ 0\ a\ \mathrm{cod})
    = \mathrm{substitute}\ 0\ (\mathrm{substitute}\ 0\ s\ a)\
      (\mathrm{substitute}\ 1\ s\ \mathrm{cod})$, discharging the
    application conditional.
  \item Run the outer induction on the typing derivation, supplying each
    case lemma's recursive premises and assembling them into the main theorem.
\end{enumerate}

The closed-term restriction sidesteps the seventh substrate gap
(\autoref{subsec:substrate-gap-discovery}) by ensuring the substitution
targets and source terms have no free variables that would expose the
binder-shift propagation issue.

\paragraph{Substitute composition.} The closure-restricted form of
the substitute composition equation
$$
\begin{aligned}
  \mathrm{substitute}\ 0\ s\ (\mathrm{substitute}\ 0\ a\ \mathrm{cod}) =
  \mathrm{substitute}\ 0\ (\mathrm{substitute}\ 0\ s\ a)\
    (\mathrm{substitute}\ 1\ s\ \mathrm{cod})
\end{aligned}
$$
is established for five of five inductive cases of $\texttt{cod}$:
sort, var, app, lam, and pi. The sort case is unconditional, the var
case assumes $\mathrm{ClosedAt}\ 0\ s$, the app case uses two recursive
premises, and the lam and pi cases are conditional on a
shift-substitute commutation lemma
$$
\begin{aligned}
  \mathrm{shift}\ 0\ 1\ (\mathrm{substitute}\ 0\ s\ a) =
  \mathrm{substitute}\ 1\ (\mathrm{shift}\ 0\ 1\ s)\
    (\mathrm{shift}\ 0\ 1\ a).
\end{aligned}
$$
The commutation lemma itself is the remaining piece for full
unconditional composition closure.

\paragraph{Host reflection bridge.}
$\texttt{BEDC.MetaCIC.HostBridge}.\mathrm{reflectExpr}$ covers eight
host Lean $\mathrm{Expr}$ constructors: $\mathrm{sort}$,
$\mathrm{bvar}$, $\mathrm{app}$, $\mathrm{lam}$,
$\mathrm{forallE}$, $\mathrm{const}$, $\mathrm{fvar}$ returning
$\mathrm{none}$, and $\mathrm{mdata}$ as a passthrough. The elaboration
commands $\#\mathrm{reflect\_metacic}\ e$ and
$\#\mathrm{metacic\_decide}\ e$ expose the bridge: the former prints
the reflected $\mathrm{Term}$, and the latter runs $\mathrm{infer}$ on
that reflected term and prints the inferred MetaCIC type.

\paragraph{Out of scope.} The reconstruction does not currently implement
strong normalization, confluence, full universe hierarchy, inductive
declarations within MetaCIC, or a verified embedding into the host Lean kernel.
These remain research-stage as outlined in
\autoref{sec:self-comp-reflection-bridge}.

\paragraph{Audit guarantees.} The reconstruction's status is maintained by
four standing audits at PR merge time:
\begin{itemize}
  \item \texttt{check-axioms.py} rejects any \texttt{axiom} declaration.
  \item \texttt{bedc\_ci.py axiom-purity --strict} rejects theorems whose
    transitive axiom dependency includes \texttt{Classical.choice},
    \texttt{Quot.sound}, or \texttt{propext}.
  \item \texttt{bedc\_ci.py audit} rejects
    $\backslash\mathtt{leanchecked}\{X\}$ markers whose $X$ does not exist in
    \texttt{lean4/BEDC/}.
  \item \texttt{bedc\_ci.py marker-existence-audit} reports the same
    information as a standing first-class audit.
\end{itemize}
The current state is coherent. Statements pending proof are not statements
pending replacement, and the audit guarantees make this distinction
enforceable.
