\chapter{Asking Events and Policies}
\label{ch:core-asking-events-and-policies}

\section{Asking events}

\begin{definition}[Probe names and evidence tokens]
\(\PN\) is a syntactic class of probe names. \(\Ev\) is a syntactic class of evidence tokens. Neither is assumed to be a set of functions or a set of proofs.
\end{definition}
\leandef{BEDC.FKernel.Ask.ProbeName}
\leandef{BEDC.FKernel.Ask.Evidence}

\begin{definition}[Asking event]
\label{def:asking-event}
An asking event is a judgment
\[
\Ask(\pi,h,m,\delta),
\]
where \(\pi:\PN\), \(h:\Hist\), \(m:\Mark\), and \(\delta:\Ev\). It is read: asking probe name \(\pi\) of history \(h\) emits mark \(m\) with evidence token \(\delta\).
\end{definition}
\leandef{BEDC.FKernel.Ask.AskEvent}

\section{Asking policies}

\begin{definition}[Asking policy]
\label{def:asking-policy}
An asking policy \(\AskPol(\Pi,D)\) for a bundle \(\Pi\) over a source specification \(D\) consists of:
\begin{enumerate}
  \item totality: every admitted history in \(D\) has generated asking events for every probe in \(\Pi\);
  \item mark determinacy: if \(\Ask(\pi,h,m,\delta)\) and \(\Ask(\pi,h,n,\theta)\), then \(\msame(m,n)\);
  \item evidence composability: evidence tokens produced by sequential asking can be combined into a signature evidence token;
  \item history-respect: if \(\hsame(h,k)\), then asking results agree up to \(\msame\) whenever both are generated.
\end{enumerate}
The word ``consists'' is a metalanguage description of a certificate record; the object-language content is the availability of these certified properties.
\end{definition}
\leanchecked{BEDC.FKernel.Ask.ask\_total\_from\_policy}
\leanchecked{BEDC.FKernel.Ask.ask\_total}
\leanchecked{BEDC.FKernel.Ask.ask\_respects\_history}
\leanchecked{BEDC.FKernel.Sig.sig\_respects\_history}

\begin{lemma}[Asking determinacy]
\label{thm:asking-determinacy}
\label{lem:asking-determinacy}
Under an asking policy, a fixed probe name applied to a fixed history emits a mark unique up to \(\msame\).
\end{lemma}
\leanchecked{BEDC.FKernel.Ask.ask\_deterministic}
\leanchecked{BEDC.FKernel.Ask.asking\_determinacy}
\begin{proof}
This is the mark determinacy field of \(\AskPol\).
\end{proof}
