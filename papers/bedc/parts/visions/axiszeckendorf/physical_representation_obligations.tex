\section{Physical representation obligations}

\begin{definition}[Low-energy electromagnetic representation certificate]
\label{def:zeckendorf-low-energy-electromagnetic-representation-certificate}
A low-energy electromagnetic representation certificate for the closed
Window-six readout in a closure mode \(\mu\) is a separate physical map
$$
  \rho_{\mathrm{EM}}
  :
  R_{Z6,\mu}^\star
  \longmapsto
  \alpha_{\mathrm{EM}}^{-1}(0),
$$
where the target is the infrared electromagnetic coupling readout. The
certificate must preserve the source fold, the residual-fiber ledger, the
golden phase-character row, the boundary-parity row, and the
cannot-claim boundary. It is not supplied by the numerical expression for
\(R_{Z6,\mu}^\star\) alone.
\end{definition}

\begin{principle}[Impedance reading of the scalar]
\label{prin:zeckendorf-impedance-reading}
If the representation certificate of
\autoref{def:zeckendorf-low-energy-electromagnetic-representation-certificate}
is supplied, the same scalar may be read as the vacuum impedance ratio
$$
  \alpha_{\mathrm{EM}}^{-1}(0)
  =
  \frac{2R_K}{Z_0},
$$
where \(R_K\) is the von Klitzing resistance and \(Z_0\) is the
characteristic impedance of vacuum. This reading interprets the
Zeckendorf-golden phase output as an impedance-matching surface for a
low-energy electromagnetic phase medium, not as a primitive physical
constant inserted into the construction.
\end{principle}

\begin{principle}[Independent route convergence]
\label{prin:zeckendorf-independent-route-convergence}
An admitted physical representation of a selected \(R_{Z6,\mu}^\star\)
must be stable under independent low-energy measurement routes. Atomic recoil,
electron magnetic-moment comparison, spectroscopic fine-structure
readouts, and quantum-Hall impedance routes may differ in instrumentation
and systematic error model, but after route-specific corrections they
must converge to the same infrared scalar if they are to certify the same
Window-six projection.
\end{principle}

\begin{principle}[Window refinement for running coupling]
\label{prin:zeckendorf-running-coupling-window-refinement}
The Window-six readout is an infrared candidate. It does not assert that
the same scalar is the effective electromagnetic coupling at all energy
scales. A representation that reaches nonzero momentum transfer must
replace the fixed denominator by a scale-indexed ledger
$$
  D_{6,\mu}^\star\rightsquigarrow D(Q)
$$
or replace the fixed window by a scale-indexed family
$$
  X_6\rightsquigarrow X_{m(Q)}.
$$
Thus any comparison with high-energy running of \(\alpha\) is an
obligation to construct a spectral-response activation map $z=z(Q)$ and a
Zeckendorf window-refinement flow, not an allowed reuse of the infrared
scalar.
\end{principle}

\begin{principle}[Projection does not expose the hidden vacuum ledger]
\label{prin:zeckendorf-projection-not-hidden-ledger}
The scalar \(\alpha_{\mathrm{EM}}^{-1}(0)\), even when represented by
\(R_{Z6,\mu}^\star\), exposes only a projection ratio. It does not expose the
raw microstate space \(\Omega_6\), the full residual fibers
\(\mathcal{F}_6(x)\), or a unique continuous spacetime geometry. The
recoverable object is the finite auditable skeleton
$$
  (X_6,\mathsf{Fold}_6,\varphi,\chi_\varphi^Z,D_{6,\mu}^\star,
  \text{boundary parity},\text{residual ledger}),
$$
not the total physical substrate.
\end{principle}

\begin{principle}[Hidden boundary parity]
\label{prin:zeckendorf-hidden-boundary-parity}
The eightfold boundary-parity row is not expected to appear as a large
smooth correction in ordinary continuous low-energy probes. If it has a
physical representation, its admissible signatures are restricted to
integral, mod-\(2\), boundary-condition, topology-sensitive, or coherent
phase-residual observables. Ordinary smooth QED success therefore
constrains the parity row to remain hidden from continuous probes; it
does not by itself identify or eliminate the discrete row.
\end{principle}

\begin{principle}[Sixfold phase-circumference interface]
\label{prin:zeckendorf-sixfold-phase-circumference-interface}
The boundary-parity row enters electromagnetic readouts through a
sixfold phase interface, not as three independently exposed parity bits.
The admissible phase route has the form
$$
  C_\partial\times T
  \longrightarrow
  \mathbb{Z}/6\mathbb{Z}
  \longrightarrow
  U(1),
$$
where \(T\cong\mathbb{Z}/3\mathbb{Z}\) is the edge-flux torsion source
and the \(C_\partial\cong(\mathbb{Z}/2\mathbb{Z})^3\) contribution is
read through the invariant parity character. Equivalently, the fixed
Window-six projection formula
$$
  R_6(D)
  =
  2\pi\varphi^5
  \frac{21+34C(D)}{21\varphi^{-1}+34C(D)\varphi^{-2}}
$$
may be written as
$$
  R_6(D)
  =
  2\pi
  \frac{21+34C(D)}{21\varphi^{-6}+34C(D)\varphi^{-7}}.
$$
In this form \(\varphi^{-6}\) records the sixfold phase shell,
\(\varphi^{-7}\) records the first boundary seam beyond that shell, and
\(2\pi\) is the circumference interface for the \(Z_6\to U(1)\) readout.
This does not collapse the phase reservoir coefficient to the six-element
image coefficient; it separates the admissible phase gate from the
reservoir, image, and curvature-residual readout modes.
\end{principle}

\input{parts/visions/axiszeckendorf/flux_operator_geometry}

\input{parts/visions/axiszeckendorf/right_block_flux_reservoir_coefficient}

\input{parts/visions/axiszeckendorf/static_green_exact_boundary_flux}

\begin{theorem}[Independent denominator is required for predictivity]
\label{thm:zeckendorf-denominator-predictivity-requirement}
Let
$$
  R(D)
  :=
  2\pi\varphi^5
  \frac{
    F_8+F_9C(D)
  }{
    F_8\varphi^{-1}+F_9C(D)\varphi^{-2}
  },
$$
where
$$
  C(D)
  :=
  \frac12
  +
  \frac14\cos^2\left(\frac{\pi}{\varphi}\right)
  +
  \frac{1}{D\varphi^5}.
$$
For positive \(D\), the readout \(R(D)\) is strictly decreasing in
\(D\). Consequently, if \(D\) is obtained by inverting an observed
low-energy value of \(\alpha_{\mathrm{EM}}^{-1}\), then the expression
has no predictive force as a Window-six claim. A predictive use requires
a denominator rule
$$
  D_6=\Gamma(X_6,\mathsf{Fold}_6,\text{boundary parity},
  \text{tail ledger})
$$
that does not use the measured electromagnetic coupling.
\end{theorem}
\begin{proof}
The derivative with respect to \(C\) is
$$
  \frac{\partial R}{\partial C}
  =
  2\pi\varphi^5
  \frac{
    F_8F_9(\varphi^{-1}-\varphi^{-2})
  }{
    (F_8\varphi^{-1}+F_9C\varphi^{-2})^2
  }.
$$
Since \(\varphi^{-1}>\varphi^{-2}\), this derivative is positive. Also
$$
  \frac{dC}{dD}
  =
  -\frac{1}{D^2\varphi^5}
  <
  0.
$$
Thus \(dR/dD<0\). In any interval where the inverse problem is posed,
the measured scalar can therefore determine a denominator by inversion
rather than by Window-six structure. Such an inverted denominator is a
fit, not an independent prediction. Predictivity requires that the
denominator be supplied by the Window-six ledger before comparison with
the physical scalar.
\end{proof}

\begin{definition}[Low-energy alpha-projection gate]
\label{def:zeckendorf-low-energy-alpha-projection-gate}
A low-energy alpha-projection gate is protocol data
\(\Gamma^\alpha\) for the electromagnetic representation certificate that
selects an admissible cross-scale boundary-return branch before the
Window-six residual ledger is evaluated. At the first post-Window-six
boundary it has the form
$$
  \Gamma^\alpha_7:
  \{0,F_{10},F_{11}\}
  \longrightarrow
  \{0,F_{10}\}.
$$
Equivalently, it retains the \(F_{10}=55\) return branch from
\(\{0,55,89\}\) and excludes the \(F_{11}=89\) diffusion branch from the
low-energy residual ledger. This gate is a separate protocol field; it is
not inferred from the measured electromagnetic scalar.
\end{definition}

\begin{principle}[Square-residue gate candidate]
\label{prin:zeckendorf-square-residue-gate-candidate}
A natural candidate implementation of
\autoref{def:zeckendorf-low-energy-alpha-projection-gate} is the
square-residue projection at \(p_\ast=571\). The candidate is guided by
the order data
$$
  \operatorname{ord}_{571}(34)=285,\qquad
  \operatorname{ord}_{571}(55)=19,\qquad
  \operatorname{ord}_{571}(89)=570,\qquad
  \operatorname{ord}_{571}(144)=285.
$$
Under this reading the low-energy projection keeps the re-lockable
residue branch and excludes the primitive diffusion branch. This remains
protocol-gate data until a NameCert supplies the corresponding internal
certificate.
\end{principle}

\begin{principle}[Protocol-gated Window-six minimal residual closure]
\label{prin:zeckendorf-protocol-gated-window-six-minimal-residual-closure}
The protocol-gated Window-six minimal closure route treats the coarse
denominator
$$
  D_0=F_{10}-F_6=F_9+F_7=47
$$
as fixed by the outer uplift tail, the eightfold boundary parity, the
next boundary, and the deep tail. In the coarse ledger, \(F_9\) is the
boundary alias already visible in the projection weights, while
\(F_7=N_{\geq 3}(6)\) is the non-displayed deep tail. The first residual
seam is therefore placed at the golden shell of this deep-tail address
$$
  \varphi^{-(F_5+F_3)}=\varphi^{-7},
$$
not at a shallower term that would rewrite the visible \(F_8\) layer or
the explicit \(F_9\) boundary layer. The boundary holonomy quotient is
the two-bit residual quotient
$$
  C_{\partial}/P_6^{\mathrm{geo}}\cong(\mathbb{Z}/2\mathbb{Z})^2.
$$
The boundary sheet expectation is the two-sheet average
$$
  E_{\mathrm{sheet}}(a)=\frac12(a+P_w aP_w),
$$
with index \(2\), and the binary boundary uplift satisfies
$$
  (\mathsf{Fold}^{\mathrm{bin}}_6)^{-1}(w)
  =
  \{V_6(w),V_6(w)+34\},
$$
so the sheet difference is \(34=F_9\). The nonzero Window-six uplift tail
offsets obey
$$
  \Delta_{\mathrm{tail}}(6)\subset\{F_8,F_9,F_{10}\}
  =
  \{21,34,55\}.
$$
The Window-six boundary difference itself is only two-sheeted,
$$
  \Delta^{\mathrm{bdry}}_6=\{0,F_9\},
$$
whereas the next boundary differences have three branches,
$$
  \Delta^{\mathrm{bdry}}_7=\{0,F_{10},F_{11}\},
  \qquad
  \Delta^{\mathrm{bdry}}_8=\{0,F_{11},F_{12}\}.
$$
Therefore the Window-six sheet parity is not transported across scales
without protocol data. Given the low-energy gate \(\Gamma^\alpha_7\) of
\autoref{def:zeckendorf-low-energy-alpha-projection-gate}, the first
cross-scale return branch is \(F_{10}\). The closure mode then determines
whether this return is held fixed at the Window-six anchor or walked to
the next retained branch. The anchor-local step is
$$
  u_6:=\varphi^{-(F_6+F_3)}=\varphi^{-10}.
$$
The walking-gate mode uses the next retained branch \(F_{12}\), hence its
third displayed exponent is \(7+10+12=29\). The primitive
\(F_{11}\) branch is not part of the low-energy ledger selected by the
gate.
The Window-six closure displays three residual terms because the fiber
multiplicities are only \(2,3,4\), equivalently because the tail ledger
stops at \(N_{\geq 4}(6)\) inside Window six. Further terms require
cross-window protocol refinement. Its first two coefficient
normalizations are
$$
  q_0=1,\qquad
  q_1=-\frac{F_2}{F_3}.
$$
Here \(q_0=1\) reads the single occurrence of the \(F_7\) deep-tail seam,
\(q_1=-1/2\) reads the two-sheet overlap subtraction. The same
coefficient is also the alternating phase eigenvalue in the untwisted
fold-gauge mismatch spectrum
$$
  \operatorname{spec}(A_0)=\{1,\tfrac12,-\tfrac12\},
$$
where the \(-1/2\) mode is the Jordan alternating return seen by the
statistical projection. The third
coefficient is fixed only after a readout mode is chosen. The full
phase-reservoir readout sees
$$
  C_\partial\cong(\mathbb{Z}/2\mathbb{Z})^3,
  \qquad
  q_2^{\mathrm{res}}=\frac{|C_\partial|}{N_{\geq4}(6)}=\frac89,
$$
also matching the ratio between the fiber histogram layers
\(\#\{d_6=2\}=8\) and \(\#\{d_6=4\}=9\), and matching the right-block
escape flux in
\autoref{prin:zeckendorf-right-block-flux-reservoir-coefficient}. The
\(U(1)\)-image readout
uses the six-element phase image supplied by the \(Z_6\) interface
$$
  C_\partial\times T\to\mathbb{Z}/6\mathbb{Z}\to U(1),
  \qquad
  q_2^{\mathrm{img}}=\frac6{N_{\geq4}(6)}=\frac23.
$$
The irreducible
curvature-residual readout quotients the locally absorbable diagonal
subgroup
$$
  P_6^{\mathrm{geo}}\cong\mathbb{Z}/2\mathbb{Z},
  \qquad
  C_\partial/P_6^{\mathrm{geo}}\cong(\mathbb{Z}/2\mathbb{Z})^2,
$$
and hence has
$$
  q_2^{\mathrm{curv}}
  =
  \frac{|C_\partial/P_6^{\mathrm{geo}}|}{N_{\geq4}(6)}
  =
  \frac49.
$$
\end{principle}

\begin{theorem}[Protocol-gated Window-six closure determines a readout family]
\label{thm:zeckendorf-protocol-gated-window-six-closed-denominator-derivation}
Assume the protocol-gated Window-six minimal residual closure of
\autoref{prin:zeckendorf-protocol-gated-window-six-minimal-residual-closure}.
The three-term residual polynomial selected by the Window-six fiber-depth
cutoff, the low-energy gate, a closure mode, and a readout mode
$$
  Q_6(u)=q_0+q_1u+q_2u^2
$$
therefore has coefficients
$$
  q_0=1,\qquad
  q_1=-\frac{F_2}{F_3},\qquad
  q_2\in
  \left\{
    \frac89,\frac23,\frac49
  \right\},
$$
where \(q_2=8/9\) is the phase-reservoir readout, \(q_2=2/3\) is the
\(Z_6\to U(1)\) image readout, and \(q_2=4/9\) is the irreducible
curvature-residual readout. In anchor-local mode the
resulting three-term Window-six residual denominator is
$$
\begin{aligned}
  D_{6,\mathrm{loc}}^{(q_2)}
  =
  F_9+F_7
  +
  \varphi^{-(F_5+F_3)}
  \left[
    1
    -
    \frac{F_2}{F_3}\varphi^{-(F_6+F_3)}
    +
    q_2\varphi^{-2(F_6+F_3)}
  \right].
\end{aligned}
$$
Equivalently, the three anchor-local readouts are
$$
  D_{6,\mathrm{loc}}^{\mathrm{res}}
  =
  47+\varphi^{-7}-\frac12\varphi^{-17}+\frac89\varphi^{-27},
  \qquad
  D_{6,\mathrm{loc}}^{\mathrm{img}}
  =
  47+\varphi^{-7}-\frac12\varphi^{-17}+\frac23\varphi^{-27},
  \qquad
  D_{6,\mathrm{loc}}^{\mathrm{curv}}
  =
  47+\varphi^{-7}-\frac12\varphi^{-17}+\frac49\varphi^{-27}.
$$
In walking-gate mode the three readouts are
$$
  D_{6,\mathrm{walk}}^{\mathrm{res}}
  =
  47+\varphi^{-7}-\frac12\varphi^{-17}+\frac89\varphi^{-29},
  \qquad
  D_{6,\mathrm{walk}}^{\mathrm{img}}
  =
  47+\varphi^{-7}-\frac12\varphi^{-17}+\frac23\varphi^{-29},
  \qquad
  D_{6,\mathrm{walk}}^{\mathrm{curv}}
  =
  47+\varphi^{-7}-\frac12\varphi^{-17}+\frac49\varphi^{-29}.
$$
\end{theorem}
\begin{proof}
By the protocol-gated Window-six minimal closure, the anchor-local
denominator has the form
$$
  D_{6,\mathrm{loc}}^{(q_2)}
  =
  D_0
  +
  \varphi^{-(F_5+F_3)}
  Q_6(\varphi^{-(F_6+F_3)}).
$$
Taking the specified three-term polynomial and substituting the displayed
coefficients gives the first formula. The Fibonacci values
$$
  F_2=1,\quad
  F_3=2,\quad
  F_5=5,\quad
  F_6=8,\quad
  F_7=13,\quad
  F_9=34
$$
then give \(F_9+F_7=47\), \(F_5+F_3=7\),
\(F_6+F_3=10\), and \(F_2/F_3=1/2\). Substituting
\(q_2=8/9\), \(q_2=2/3\), and \(q_2=4/9\) gives the three
anchor-local readouts.
For walking-gate mode, the first two exponents remain \(7\) and \(17\),
while the third exponent walks from the \(F_{10}\) return to the next
retained \(F_{12}\) branch. Thus the third exponent is \(7+10+12=29\).
Substituting the three allowed readout coefficients gives the displayed
walking-gate readouts.
\end{proof}

\begin{theorem}[Fixed electromagnetic readout selects the local reservoir branch]
\label{thm:zeckendorf-fixed-electromagnetic-readout-selection}
Assume the readout is the fixed Window-six projection formula whose visible
state and boundary rows are \(F_8\) and \(F_9\). Assume also that the
physical target is the low-energy electromagnetic impedance readout whose
phase observations are gated by the \(Z_6\to U(1)\) image. Then the
admissible low-energy denominator selected from
\autoref{thm:zeckendorf-protocol-gated-window-six-closed-denominator-derivation}
is
$$
  D_{6,\alpha}^{\star}
  =
  D_{6,\mathrm{loc}}^{\mathrm{res}}
  =
  47+\varphi^{-7}-\frac12\varphi^{-17}+\frac89\varphi^{-27}.
$$
\end{theorem}
\begin{proof}
The fixed Window-six projection formula keeps the visible pair
\((F_8,F_9)\). A walking residual would move the residual ledger to the
next retained branch \(F_{12}\) while the visible readout remains fixed at
\((F_8,F_9)\). Thus the fixed low-energy formula selects the
anchor-local closure. The \(Z_6\to U(1)\) image supplies the admissible
phase observation gate, but \(\alpha_{\mathrm{EM}}^{-1}(0)\) is an
impedance readout of the phase reservoir behind that gate rather than a
bare image-cardinality readout. By
\autoref{prin:zeckendorf-right-block-flux-reservoir-coefficient}, the
same reservoir coefficient is also the normalized right-block escape
flux \(P(\mathrm{leave}\ U_R\mid U_R)=8/9\). Thus the selected
coefficient is the flux-reservoir coefficient \(q_2=8/9\), not the image
coefficient \(q_2=2/3\) or the curvature quotient \(q_2=4/9\).
Substitution into the
readout-family theorem gives the displayed denominator.
\end{proof}

\begin{principle}[Remaining formalization obligations]
\label{prin:zeckendorf-unclosed-denominator-obligations}
The protocol-gated Window-six closure becomes a fully internal
Window-six theorem only after six further obligations are discharged.
First, the ProtocolGateLegality obligation must certify
$$
  \Gamma^\alpha_7:
  \{0,F_{10},F_{11}\}\to\{0,F_{10}\}
$$
as an allowed low-energy projection gate. If the square-residue gate is
used, this obligation must certify the \(p_\ast=571\) residue-stratum
separation in \autoref{prin:zeckendorf-square-residue-gate-candidate}.
Second, the GoldenResidualLadder obligation must derive
$$
  D_6-D_0\in\varphi^{-7}\mathbb{Q}_{Z}[\varphi^{-10}],
$$
from the deep-tail seam and the gated \(F_{10}\) uplift-return rule.
Third, the TwoSheetMobius obligation must derive
$$
  q_1=\frac{1-2}{2}=-\frac12.
$$
It must also identify the same coefficient with the Jordan alternating
phase mode of the fold-gauge mismatch spectrum. Fourth, the ReadoutMode
obligation must distinguish the phase reservoir
\(C_\partial\), giving \(q_2=8/9\), the \(Z_6\to U(1)\) image, giving
\(q_2=2/3\), and the irreducible curvature-residual quotient
\(C_\partial/P_6^{\mathrm{geo}}\), giving \(q_2=4/9\); for
\(\alpha_{\mathrm{EM}}^{-1}(0)\) it must select the phase-reservoir
impedance readout gated by \(Z_6\), using the local conditional escape
flux \(8/9\) rather than the global cut-flux density \(1/12\). Fifth,
the ClosureMode obligation
must certify that fixed
\(R_6(D)\) readouts select anchor-local closure, while walking-gate
closure belongs to a scale-indexed \(R_m(D_m)\) refinement object. Sixth,
the GreenResolved obligation separates the static right-boundary cut-flux
source from centered Green-relaxation input. The static low-energy
right-boundary readout has $q_3=0$ by
\autoref{thm:zeckendorf-static-right-boundary-source-green-zero}; the
right-boundary cut vector is an instance of the general Dirichlet
cut-source identity $L_B\mathbf{1}_I=b_B$ and therefore has zero
$571$-Green class. By the same theorem, centered relaxation observables
for the coarse Markov kernel have susceptibility and Green--Kubo
denominators containing $571$. A nonzero Green term may therefore enter
only in a running or dynamic response certificate, such as
$$
  D_{6,\alpha}(z)
  =
  D_{6,\alpha}^{\star}
  +
  \Delta_R(z)\varphi^{-37}
  +
  O(\varphi^{-47}),
  \qquad
  \Delta_R(0)=0.
$$
The Window-six seed candidate is
\autoref{def:zeckendorf-window-six-green-running-seed}, with
$\Delta_R(1)=26401/(2^{13}\cdot571)$. The GreenClass branch must
therefore show that any physical running readout uses this centered
Green-excess source, or else replace it by a certified higher-window
Green mode. The FluxGreenCoupling branch must decide separately whether
the global cut-flux density $\Phi_R=1/12$ participates in the activation
of the Green-excess parameter. Until these obligations are supplied, the
closed denominator is a
protocol-gated Window-six ledger conclusion rather than a fully certified
internal theorem.
\end{principle}

\begin{theorem}[Physical refutation surface for the Window-six readout]
\label{thm:zeckendorf-window-six-physical-refutation-surface}
The physical interpretation of a selected \(R_{Z6,\mu}^\star\) is
refutable at the
derived-interface level by any of the following failures:
no admissible low-energy projection gate selects the \(F_{10}\) branch;
the physical representation cannot decide the anchor-local versus
walking-gate closure mode at the required precision;
the physical representation cannot decide whether the target reads the
phase reservoir, the \(Z_6\to U(1)\) image, or the irreducible
curvature-residual quotient; the
selected third coefficient remains a fitted rational rather than a
readout-mode normalization; independent low-energy routes converge to
incompatible values after systematic corrections; a high-energy
comparison reuses \(R_{Z6,\mu}^\star\) without a scale-indexed ledger or
spectral-response activation map and window-refinement flow; or later
residual corrections inside the static cut-flux layer require
terms outside the selected gated ladder
$$
  \{7,17,27\}\quad\text{in anchor-local mode,}
  \qquad
  \{7,17,29\}\quad\text{in walking-gate mode.}
$$
\end{theorem}
\begin{proof}
The first failure violates the protocol-gate requirement in
\autoref{def:zeckendorf-low-energy-alpha-projection-gate}. The second
leaves the denominator family mode-underdetermined at the physical
interface. The third leaves the coefficient family readout-underdetermined.
The fourth
violates the residual closure principle in
\autoref{prin:zeckendorf-window-six-residual-closure}, the
protocol-gated closure principle in
\autoref{prin:zeckendorf-protocol-gated-window-six-minimal-residual-closure},
and the readout-mode coefficient normalization in
\autoref{prin:zeckendorf-unclosed-denominator-obligations}. The fifth
violates the route-convergence requirement of
\autoref{prin:zeckendorf-independent-route-convergence}. The sixth
violates the infrared boundary of
\autoref{prin:zeckendorf-running-coupling-window-refinement}. The seventh
violates the displayed Window-six residual denominators, whose correction
terms occur at \(7,17,27\) in anchor-local mode and at \(7,17,29\) in
walking-gate mode. Each failure breaks an explicit interface condition
without needing to deny the finite Zeckendorf fold construction itself.
\end{proof}
