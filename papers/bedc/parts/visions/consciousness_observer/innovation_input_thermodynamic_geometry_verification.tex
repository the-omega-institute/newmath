\chapter{Innovation Input Thermodynamic Geometry Verification}
\label{ch:visions-innovation-input-thermodynamic-geometry-verification}
\origin{ai}

\autoref{ch:visions-innovation-input-thermodynamic-geometry-boundary}
places the meeting law, curvature read, local entropy identity,
entanglement area row, and modular-temperature row into one
thermodynamic dictionary. This chapter records the adjacent verification
certificate: the cone slot behind the two twelfth-coefficients, the
agreement of the curvature instruments away from critical points, the
scheme test for the massive area slope, the three-dimensional Ricci tensor
read, the homogeneous next-order trace channel, and the grid-scaling test
which separates continuum terms from lattice residue.

\section{Cone Slot and the Two Twelfths}
\label{sec:innovation-input-thermo-verification-cone-slot}

\begin{theorem}[Cone coefficient and curvature slot]
\label{thm:innovation-input-thermo-cone-slot}
Let
$$
s(\alpha)
=
\frac{1}{12}
\left(
\frac{2\pi}{\alpha}
-
\frac{\alpha}{2\pi}
\right)
$$
be the Sommerfeld cone coefficient. In the entanglement limit
$\alpha=2\pi-\delta$, its linear term is $\delta/(12\pi)$. The
distributional cone curvature satisfies $\int R=2\delta$, and the smooth
DeWitt slot $a_1=R/6$ contributes
$$
\frac{1}{4\pi}\frac{2\delta}{6}
=
\frac{\delta}{12\pi}.
$$
Thus the massive area-law coefficient $-1/12$ and the local
heat-geometry correction $+R/12$ occupy the same linear heat-kernel slot.
\end{theorem}

\begin{proof}
The displayed expansion gives
$$
s(2\pi-\delta)
=
\frac{\delta}{12\pi}+O(\delta^2).
$$
The DeWitt contribution of a localized cone curvature is the second
displayed expression. The two linear coefficients are identical.
\end{proof}

\begin{corollary}[Curvature-mass slot target]
\label{cor:innovation-input-thermo-curvature-mass-slot-target}
For minimal coupling, scalar curvature and mass-squared enter the same
heat-kernel slot with opposite signs. The entanglement row therefore has
the numerical target
$$
\frac{\partial S_{\rm EE}}{\partial R}
=
-
\frac16
\frac{\partial S_{\rm EE}}{\partial m^2}
$$
in a regulator comparison where both derivatives are measured in the same
units.
\end{corollary}

\begin{remark}[Linearity scope]
\label{rem:innovation-input-thermo-cone-linearity-scope}
The identification above is a linear-slot statement. Entanglement entropy
uses this linear cone term, while higher Renyi rows probe nonlinear cone
dependence and need not share the same coefficient identity.
\end{remark}

\section{Dual Curvature Instruments}
\label{sec:innovation-input-thermo-verification-dual-instruments}

\begin{definition}[Two curvature instruments]
\label{def:innovation-input-thermo-two-curvature-instruments}
For a two-dimensional conformal machine row, define the geometric and
thermodynamic scalar-curvature reads by
$$
R_h
=
6\left(\frac1T-\operatorname{tr}(hg)\right),
\qquad
R_S
=
12\left(\frac1T-\dot S\right).
$$
Both instruments use the same flat-control subtraction but otherwise read
different data: $R_h$ reads the meeting defect, while $R_S$ reads entropy
production.
\end{definition}

\begin{principle}[Generic-point witness]
\label{prin:innovation-input-thermo-generic-point-witness}
Away from critical points of the conformal factor, the two instruments
agree with the analytic curvature and with each other:
$$
\begin{array}{c|c|c|c|c}
\text{point} & R & T & R_h & R_S\\
\hline
(1.060,0.628) & -0.3704 & 0.04/0.07
& -0.3737/-0.3744 & -0.3724/-0.3741\\
(2.199,1.100) & +0.1919 & 0.04/0.07
& +0.1949/+0.1959 & +0.1939/+0.1952
\end{array}
$$
The agreement with the analytic value is at the percent scale, and the two
instruments agree with each other at approximately $0.3$--$0.5\%$. This is
the noncritical-point form of the internal consistency test in
\autoref{prin:innovation-input-thermo-internal-consistency-test}.
\end{principle}

\begin{principle}[Gradient robustness]
\label{prin:innovation-input-thermo-gradient-robustness}
The test points have nonzero conformal gradient. The log-Gram mixed
derivative is a discretization of an exact heat-kernel identity, while the
entropy read is scalar. The common curvature output is therefore not an
artifact of placing the measurement at extrema of the conformal factor.
\end{principle}

\section{Regulator Scheme Comparison}
\label{sec:innovation-input-thermo-verification-regulator-comparison}

\begin{definition}[Second lattice dispersion row]
\label{def:innovation-input-thermo-second-lattice-dispersion}
The comparison regulator uses the lattice dispersion
$$
f_B(k)
=
4\sin^2\frac{k}{2}
+
\frac12
\left(4\sin^2\frac{k}{2}\right)^2.
$$
It lies in the same long-wavelength regulator class as the nearest-neighbor
row but changes the ultraviolet area coefficient.
\end{definition}

\begin{principle}[Area coefficient scheme test]
\label{prin:innovation-input-thermo-area-coefficient-scheme-test}
For the two regulator schemes, the fitted area rows are
$$
\begin{array}{c|c|c|c}
\text{quantity} & \text{scheme A} & \text{scheme B} & \text{read}\\
\hline
\kappa_{\rm UV} & 0.07732 & 0.11062 & \text{scheme dependent}\\
\text{mass slope} & -0.08163 & -0.08170 & \text{universal}\\
\text{difference slope} & -0.0031 & -0.0031
& \text{common residue cancels}
\end{array}
$$
The ultraviolet intercept moves by about $43\%$, while the mass slope is
stable across schemes to $7\times10^{-5}$. The induced-gravity row is
therefore split into a regulator-dependent Newton coupling and a universal
matter-response slope.
\end{principle}

\begin{principle}[Cross-scheme invariance test]
\label{prin:innovation-input-thermo-cross-scheme-invariance-test}
The correct test for a universal quantity is invariance under a regulator
scheme change, not absolute closeness to a continuum value in a single
scheme. Cross-scheme differencing cancels the shared finite-mass lattice
residue and makes the universal slope visible at much higher precision.
\end{principle}

\section{Three-Dimensional Ricci Tensor Read}
\label{sec:innovation-input-thermo-verification-three-dimensional-ricci}

\begin{principle}[Non-tensor defect as operator audit]
\label{prin:innovation-input-thermo-non-tensor-audit}
If the measured meeting defect cannot be organized as Ricci curvature and
instead contains bare Hessian terms of the conformal factor, the operator
being measured is not the Laplace--Beltrami operator of the displayed
metric. The meeting-defect protocol therefore audits the operator as well
as the curvature: a non-tensor defect is a certificate of an incorrect
discretized generator, not a modification of
\autoref{thm:innovation-input-thermo-curvature-defect}.
\end{principle}

\begin{principle}[Three-dimensional tensor witness]
\label{prin:innovation-input-thermo-three-dimensional-tensor-witness}
For the conformally flat metric $G=e^{2\varphi}\delta_3$ with
$$
\varphi=0.3\cos x+0.15\cos y
$$
at the point $(\pi,\pi)$, the Ricci eigenvalues are distinct. After using
the reversible volume measure in the discrete operator, the directional
meeting-defect reads are
$$
\begin{array}{c|c|c|c}
\text{axis} & -R^i{}_i/3 & T=0.06\ \text{read}
& \text{relative residue at }T=0.06/0.10\\
\hline
x & +0.6149 & +0.6476 & +5.3\%/+7.4\%\\
y & +0.4919 & +0.5092 & +3.5\%/+4.3\%\\
z & +0.3689 & +0.3673 & -0.4\%/-2.4\%
\end{array}
$$
The measured ratio $1.763:1.386:1$ follows the predicted
$5/3:4/3:1$ tensor pattern. The residual is handled by the scaling test of
\autoref{sec:innovation-input-thermo-verification-residual-discrimination}.
\end{principle}

\section{Homogeneous Next-Order Trace Channel}
\label{sec:innovation-input-thermo-verification-next-order-trace}

\begin{theorem}[Homogeneous trace expansion]
\label{thm:innovation-input-thermo-homogeneous-trace-expansion}
For a homogeneous smooth geometry,
$$
\operatorname{tr}h
=
\frac{d}{2T}
-
\frac{R}{6}
-
T
\left[
\frac{|\operatorname{Riem}|^2-|\operatorname{Ric}|^2}{90}
+
\frac{\square R}{15}
\right]
+
O(T^2).
$$
The $R^2$ term cancels between the logarithm and the derivative of the
heat-kernel diagonal.
\end{theorem}

\begin{proof}
Use
$$
\operatorname{tr}h=-\partial_T\log K_T(z,z)
$$
and insert the standard $a_2$ coefficient of the heat-kernel diagonal. The
logarithm subtracts the square of the $a_1$ term, eliminating the pure
$R^2$ contribution and leaving the displayed combination.
\end{proof}

\begin{principle}[Weyl visibility order]
\label{prin:innovation-input-thermo-weyl-visibility-order}
The first-order meeting defect reads Ricci curvature. The homogeneous
next-order trace row is the first place where the Riemann-square term can
carry Weyl curvature. On conformally flat backgrounds the same term
reduces to a Ricci functional, which explains why the earlier numerical
rows do not isolate a vacuum-curvature degree of freedom.
\end{principle}

\begin{principle}[Sphere and entropy next-order witnesses]
\label{prin:innovation-input-thermo-next-order-numerical-witnesses}
On the unit sphere, the trace-channel fit gives intercept $-0.333333$ and
slope $-0.022219$, matching $-1/3$ and $-1/45$. The local entropy identity
on the same geometry gives intercept $0.166647$ against $1/6$ and a
measured next-order slope $+0.01207$. The exact rational form of that
entropy slope belongs to the full tensor coincidence-limit calculation.
\end{principle}

\begin{principle}[Full tensor coincidence-limit target]
\label{prin:innovation-input-thermo-full-tensor-target}
The remaining full-tensor coefficient is governed by
$$
[a_{1;ij'}]
=
\frac{1}{12}\nabla_i\nabla_jR
-
[a_{1;ij}].
$$
The missing input is the coincidence limit $[a_{1;ij}]$, available through
the Christensen and Decanini--Folacci coefficient systems. Its numerical
targets are the directional slopes in
\autoref{sec:innovation-input-thermo-verification-residual-discrimination}
and the entropy next-order slope above.
\end{principle}

\section{Residual Discrimination by Grid Scaling}
\label{sec:innovation-input-thermo-verification-residual-discrimination}

\begin{principle}[Scaling discriminator]
\label{prin:innovation-input-thermo-scaling-discriminator}
In the three-dimensional tensor witness, narrow-window fits by
$A+BT$ and by $a/T+bT$ are both plausible. The decisive discriminator is
grid scaling. At $n=96$ and $T=0.03$,
$$
\begin{array}{c|c|c|c}
\text{axis} & \text{measured residue}
& dx^2/T\ \text{prediction} & T^0\ \text{prediction}\\
\hline
x & +0.0196 & +0.0191 & +0.0242\\
z & +0.0015 & +0.0017 & +0.0058
\end{array}
$$
The $dx^2/T$ prediction matches both axes, while a continuum $T^0$ residue
does not. The residual is therefore a lattice term, not a missing
continuum correction to the Ricci theorem.
\end{principle}

\begin{principle}[Residual mechanism and tensor target]
\label{prin:innovation-input-thermo-residual-mechanism}
The residual amplitude is nearly isotropic,
$$
a\approx(4.6,4.1,3.7)\times10^{-4}
\approx
0.16\,Q\,dx^2.
$$
It is attributed to the local mismatch between face conductance and point
measure in a critical-point discretization. After removing the grid term,
the remaining directional slopes have signs
$$
b=(+0.412,+0.172,-0.127),
$$
which form the numerical target for the full tensor next-order
coincidence-limit formula.
\end{principle}

\begin{principle}[Fit-form discipline]
\label{prin:innovation-input-thermo-fit-form-discipline}
When two asymptotic fit forms are indistinguishable on a small time
window, the fit is not itself evidence for either form. A scaling
experiment in the regulator parameter is the required discriminator.
\end{principle}
