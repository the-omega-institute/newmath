\subsection{NameCert: Window Six Parry Measure Stationary Covector Modp Descent}
\label{sec:namecert-window6_parry_measure_stationary_covector_modp_descent_certificate}
\origin{ai}

\paragraph{Statement.} For the Window6 edge-flux matrix E in basis (U\_2,U\_1,U\_L,U\_R), exact Smith normal form gives diag(1,1,3,3450), hence coker(E)=Z/3 plus Z/3450. Since rho\_6=10, the edge-cokernel clock K\_6\textasciicircum{}edge=coker(E)/10 coker(E) is Z/10: the Z/3 factor is killed by multiplication by 10 and the Z/3450 factor leaves the Z/10 quotient. This clock has a canonical zero and canonical order-10 generator [e\_UR], so it is an internally rooted group, not a torsor. Exact mod-10 enumeration gives the left kernel of E as the cyclic group generated by chi=(2,8,0,1), and the condition chi(e\_UR)=1 pins chi uniquely. The induced cell residues are (U\_2,U\_1,U\_L,U\_R)->(2,8,0,1), with explicit cokernel witnesses for e\_U2-2e\_UR, e\_U1-8e\_UR, and e\_UL. The minimal forward boundary seam is j\_seam=e\_U1-e\_UR and carries grade chi(j\_seam)=7 mod 10. The independent Fibonacci seam is s\_6=7 with rho\_6=10, so the internal phase-lock is a\_int=7-7=0 mod 10. The certificate uses exact integer SNF and mod-10 cokernel arithmetic only, with no alpha/137 input and no fitted target. This packet was harvested from a bio-G oracle consultation and fully independently re-verified locally with sympy. It explicitly does not claim physical alpha or complete a=0 origin selection. Three open obligations remain: SeamIdentificationCert: q\_0 residual event is j\_seam=e\_U1-e\_UR; ReturnFunctorCert: q\_1,q\_2,... are successive returns of the same seam class with degree 10, so deg(q\_k)=7+10k; clock-identity-fork: decide whether the residue clock is intrinsically the P\_10 torsor or whether K\_6\textasciicircum{}edge needs an equivariant realization into P\_10. If theory forces the P\_10 torsor as the clock body, then the edge-cokernel group needs a K\_6\textasciicircum{}edge to P\_10 equivariant realization certificate and the torsor-origin problem reappears at that layer. The P\_10 single-one orbit remains an originless torsor; this constructs a different rooted object, and residue 7 is an exact cokernel identity rather than a fit.

\paragraph{Carrier.} The Window6 integer edge-flux matrix E on basis (U\_2,U\_1,U\_L,U\_R), its cokernel, and the quotient K\_6\textasciicircum{}edge=coker(E)/10 coker(E).

\paragraph{Relations.} edge-cokernel group K\_6\textasciicircum{}edge with canonical zero and canonical order-10 generator [e\_UR]; mod-10 left-kernel character chi pinned by chi(e\_UR)=1; cell residues (U\_2,U\_1,U\_L,U\_R)->(2,8,0,1); minimal boundary seam j\_seam=e\_U1-e\_UR with grade 7; Fibonacci seam s\_6=7 and horizon rho\_6=10 as an independent phase-lock comparator; separation from the originless P\_10 torsor and from any physical alpha readout

\paragraph{Counts and identities.} No exact count has been certified in the author-facing facts.

\paragraph{Witness extraction.} The fallback records only the finite construction data already present in the sanitized packet.

\paragraph{Not claimed.} physical fine-structure constant alpha, R\_6(D*)=alpha\textasciicircum{}-1, or any physical/external/metrological interpretation; complete a=0 origin selection proof; a basepoint selection inside the originless P\_10 torsor; the P\_10 torsor no-go still stands
