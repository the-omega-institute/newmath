\origin{ai}
\subsection{Missing finite data for the window-six stationary covector certificate}\label{sec:window6-parry-measure-stationary-covector-modp-descent-certificate-missing-data}
\paragraph{Status.} The supplied finite record contains the name of the intended certificate but no mathematical rows, matrices, relations, partitions, congruence data, or numerical identities. A self-contained construction cannot be written from first principles without inventing unprovided structure. In particular, the chapter would need the explicit finite carrier, the admissibility predicate on $\{0,1\}^6$ or another stated carrier, the transition relation, the partition used for descent, the integer or rational matrix entries, the stationary covector entries, the normalization convention, the modulus or moduli, and the exact identities that the certificate is meant to verify.
\paragraph{Required finite certificate fields.} To make the section complete, the finite data should specify the carrier as an explicit set or as a decidable predicate on $\{0,1\}^6$, the transition relation as ordered pairs or as a matrix, the exact count of accepted states, the partition blocks if a quotient or descent is used, the matrix or kernel before descent, the descended matrix or kernel after descent, the covector before and after descent, and the exact arithmetic checks. The arithmetic checks should include the displayed identity $vA=\lambda v$ or its normalized Markov form, together with any modular identity such as reduction of all entries modulo a stated prime $p$.
\paragraph{Boundary.} No theorem prose is supplied because there is no finite witness data to verify. The intended not-claimed boundary must remain local: no physical-constant identification, global flow law, external dependency, metrological comparison, or interpretation beyond the finite construction follows unless a separate certificate supplies it.