\origin{ai}

\subsection{Observable execution decomposition for the $B^*_{Q6}$ rate-slot bridge}
\label{sec:bioreality-observable-execution-decomposition-bstar-q6-rate-slot-bridge}

\paragraph{Finite row surface.}
The rate-slot bridge record is a Saccharomyces cerevisiae finite-row audit of whether a $B^*_{Q6}$ residual coordinate can be attached to an observed execution readout before any stronger protein-level interpretation is allowed.  The registered run is \texttt{365aa14cdedc}; it used the deterministic seed \texttt{sha256:b\_star\_q6\_rate\_slot\_bridge\_powered:deterministic}, joined $n=3289$ genes, and terminated with status \texttt{passed}.  Its declared finite verdict is \texttt{observable\_execution\_subblock}: at least one observed rate-proxy slot passed both effective null classes.  Thus the admissible reading is not that the residual coordinate realizes translation as a whole, but that the finite yeast table contains a nonempty observed execution subblock after the relevant null comparisons.

\paragraph{Coordinate and row discipline.}
The coordinate side is the nine-dimensional $B^*_{Q6}$ residual list
$\{\mathrm{K\_AAA},\mathrm{Arg\_AGR},\mathrm{Ile\_AUA},\mathrm{Leu\_CUN\_vs\_UUR},\mathrm{Leu\_UUA\_vs\_UUG},\mathrm{Ser\_UCR\_vs\_AGY},\mathrm{Ser\_UCA\_vs\_UCG},\mathrm{Thr\_ACR\_vs\_ACY},f_3\text{-}\mathrm{stress}\}$.
A rate slot is admissible only when it is read as a measured or explicitly bounded execution proxy, not as a direct residual-to-protein assertion.  The finite surface therefore has the schematic form
$$
\begin{aligned}
(q_i, r_j, z_g) &\mapsto \Delta_{ij},\\
q_i &\in B^*_{Q6},\\
r_j &\in \{\text{observed rate proxy slots}\},\\
z_g &\supset \{\text{amino-acid composition},\text{organism},\mathrm{GC3},\text{CDS length},\text{$M$-density baseline}\}.
\end{aligned}
$$
The content of the run is that some $\Delta_{ij}$ survived both null classes on the finite yeast join.  The chapter therefore records an execution-readout decomposition, not a direct molecular mechanism.

\paragraph{Numerical placement among neighboring finite audits.}
The rate-slot result is stronger than the three-organism modeled-tAI boundary and weaker than a universal biological law.  The modeled-tAI survival audit used only $3$ organisms and explicitly refused powered survival in that setting, even though it computed $81$ bounded coupling entries above threshold $0.01$ and observed maximum absolute $S_{ij}=3801961.031607896$.  The present run instead works inside a yeast observed-rate surface with $3289$ joined genes and asks for an effective-null survival of at least one execution slot.  Related yeast finite joins show why this separation matters.  In the yeast protein-abundance mediation audit, the matched CDS/protein/tAI table had $5815$ proteins, rank $\operatorname{rank}(X)=9$, residual degrees of freedom $5792$, total $R^2_{QP}=0.29837624994140827$, conditioned value $R^2_{QP\mid T}=0.0151709093608293$, and mediated fraction $M^{QTP}=0.9491551041216973$.  Those numbers concern modeled per-protein tAI and protein abundance, not the observed rate-slot verdict itself.  The rate-slot bridge is therefore a preceding execution-contact record: it says that at least one rate proxy is non-null under the registered null design before the manuscript is allowed to discuss downstream protein mediation.

\paragraph{Mass-balance and turnover checks.}
Two additional finite audits bound over-reading of the observed subblock.  The dynamic RPF/protein mass-balance run used $120$ genes and $718$ intervals; it reported $R^2_{L0}=0.9960628201518571$, $R^2_{L1}=0.996410628851051$, $R^2_{L2}=0.9712443377743347$, $R^2_{L3}=0.8953999568421264$, $\Delta R^2=-0.07584438093220836$, $\Delta\mathrm{DL}=-668.8046070381928$ bits, $p_{\mathrm{TEbin}}=0.6831683168316832$, and $p_{\mathrm{timeshuffle}}=0.9306930693069307$.  This is a negative constraint on treating the execution subblock as a closed mass-balance mechanism.  Likewise, the turnover audit used $3290$ genes and $200$ null draws, with $R^2_{L2}=0.055740851626933985$, $R^2_{L3}=0.050278872193227464$, $\Delta R^2=-0.005461979433706483$, $\Delta\mathrm{DL}=-13.68819707902732$ bits, $p_{\mathrm{matchedbin}}=0.8855721393034826$, and $p_{\mathrm{recoded}}=1.0$.  These finite rows say that the observed execution slot does not become a turnover or proteostasis law merely because one rate proxy survived its own null screen.

\paragraph{Structure and abundance controls.}
The yeast structure-mediation audit supplies a further boundary.  Its five-way join contained $4759$ genes, with reported CDS join hit rate $0.9156038419146592$ and overall join hit rate $0.8818572656921754$.  After residualization, it obtained $R^2_{QP}=0.2715520808117803$, $R^2_{QP\mid S}=0.2706601505000995$, $R^2_{QP\mid T,M}=0.13773967696311287$, and $R^2_{QP\mid T,M,S}=0.13759011208318542$.  The structure-only fraction was $0.003284564452662019$, the structure-incremental fraction was $0.0005507778820193332$, $S_Q$ structure $R^2$ was $0.003774248455422586$, and the right-tail permutation values were $0.027972027972027972$ for $S_Q$ structure and $0.1258741258741259$ for the incremental structure fraction.  These numbers permit a statistical decomposition of abundance residuals, but they do not allow the rate-slot bridge to claim folding, structural order, physical admissibility, function, or phenotype.

\paragraph{Axis identity and module context.}
The structural-order axis audit found exactly one fold-level significant coordinate: $f_3$-stress, with weighted held-out increment $0.0031744502294756994$, weighted absolute unit loading $0.5109586606724208$, and sign-flip value $p=0.03125$.  Other coordinates remained finite but did not pass that gate, for example $\mathrm{Arg\_AGR}$ had increment $0.0020139270616115154$ with $p=0.142578125$, and $\mathrm{Ile\_AUA}$ had increment $0.0013067592718559024$ with $p=0.0693359375$.  The independent code-table module audit supplies the lower-layer context: its registered targets included \texttt{h0.null.model.aa\_class\_preserving}, \texttt{h1.leave\_one\_code\_out.by\_family}, and \texttt{h1.leave\_one\_codon\_out.module\_classification}; both the boundary probe and the independent replication reported $40$ successes in $40$ trials with one-sided binomial upper-tail value $9.094947017729282\times 10^{-13}$, and both the negative control and the ablation control had $0$ unexpected successes in $20$ trials.  These module-classification rows justify using the residual coordinates as finite coordinates, while the execution audit is the separate readout step.

\paragraph{Local interpretation.}
The accepted statement is therefore the following bounded claim.  On the yeast table with $3289$ genes, the registered rate-slot analysis found at least one observed execution proxy for which the $B^*_{Q6}$ residual surface survived both effective null classes.  This licenses an observable-execution subblock in the cross-layer relation.  It does not license a direct $q_i\to P_k$ protein claim unless a same-coordinate $q_i\to T_j$ step has survived first, and it does not license translation realization, folding, physical admissibility, molecular function, mechanism, phenotype, selection, or universality without a separate layer-matched contact and its own finite statistic.