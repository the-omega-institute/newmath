\subsection{Rank-Powered Translation Readout Boundary for the GCN4 U34 Switch}
\label{subsec:bioreality-gcn4-u34-q6-translation-boundary}
\origin{ai}

\paragraph{Finite surface.}
The record concerns a finite organism-by-CDS readout surface in which the coordinates are synonymous-residual $B^*_{Q6}$ directions and the readouts are translation-layer weights after amino-acid quotient residualization. The finite row contact for the GCN4 U34 modification switch kept $5155$ genes, with recorded runtime $0.0$ seconds. This row count is not itself a translation-survival statistic; it fixes the scale of the available gene table for this packet and therefore gives the chapter a concrete finite surface rather than an unrestricted biological assertion.

The coordinate side is the nine-dimensional residual family
$$
\begin{aligned}
Q=\{&K_{AAA},\ Arg_{AGR},\ Ile_{AUA},\ Leu_{CUN/UUR},\ Leu_{UUA/UUG},\\
&Ser_{UCR/AGY},\ Ser_{UCA/UCG},\ Thr_{ACR/ACY},\ f_3\text{-stress}\}.
\end{aligned}
$$
The powered modeled-readout run used the same nine labels on the translation-weight side, so the controlled survival surface has shape $9\times 9$. The readout is explicitly modeled tAI, computed from GtRNAdb anticodon copy counts and dos Reis wobble weights, then projected onto the same synonymous-residual coordinate names. Thus the finite matrix is a bounded codon-adaptation readout, not a measured ribosome-footprint, proteomics, folding, function, phenotype, or mechanism assay.

\paragraph{Residualization and rank.}
The admissible statistic is computed only after removing amino-acid composition and simpler composition baselines. In the powered modeled-tAI run, the control design had $24$ control columns, rank $23$, and residual degrees of freedom $17$ after controls. The organism panel contained $40$ organisms, and the residualized coordinate block had rank $9$. The recorded global fit scalar was
$$
\begin{aligned}
R^2_{QT}=0.9003849000036579,
\end{aligned}
$$
with a degenerate-fit threshold of $0.999999$ and a positive partial-$R^2$ threshold of $10^{-12}$. These numbers certify that the reported powered modeled-readout matrix is not the three-organism rank-saturated all-$1.0$ artifact. They do not certify a measured translation event.

For a coordinate $q_i$ and readout $T_j$, the intended entry is the controlled survival score
$$
\begin{aligned}
S^{QT}_{ij}=S_{q_i}(T_j\mid Z_T),
\end{aligned}
$$
where $Z_T$ includes amino-acid composition, organism, GC3, CDS length, $M$-density, and simpler codon-usage baselines. The finite break condition is local and sharp: the translation-readout claim breaks when every entry satisfies $S^{QT}_{ij}\leq 0$ after these controls, or when rank saturation makes the fitted surface non-informative. Conversely, a positive modeled entry may be reported only as controlled modeled-readout survival for the specified surface.

\paragraph{Three-organism null boundary.}
A separate underpowered run used $3$ organisms: Homo sapiens, Saccharomyces cerevisiae, and Escherichia coli K-12. It computed modeled tAI weights for all $61$ sense codons in each organism, with zero raw-weight fallback count $0$ in all three cases. The local GtRNAdb payload contained all $20$ standard amino-acid families for each organism, with FASTA header counts $432$, $275$, and $89$, respectively. This underpowered matrix had $81$ entries above the bounded coupling threshold $0.01$, and its maximum absolute entry was $3801961.031607896$. Those finite numbers show nonzero bounded coupling in a small table, but they are not a powered survival result. In particular, the $n=3$ all-$1.0$ controlled surface is treated as a rank-degenerate null boundary, not as biological survival.

\paragraph{Contact interpretation.}
The contacts are separated by layer. Per-organism codon-usage tables report sequence composition. tRNA gene-copy resources report annotation-level anticodon-copy summaries. tAI or stAI resources supply organism-scoped codon-adaptation weights. These contacts can be used to build a bounded translation-readout matrix, but they do not observe elongation speed, ribosome occupancy, protein abundance, folding, structural order, localization, molecular function, system phenotype, or a universal rule. The GCN4 U34 switch name therefore anchors a finite cross-layer test candidate, not a license to infer translation mechanism from codon geometry.

\paragraph{Local verdict.}
The finite evidence supports the following restrained statement. A rank-adequate modeled tAI panel with $40$ organisms, $9$ residual coordinates, $9$ modeled readout labels, control rank $23$, residual degrees of freedom $17$, and $R^2_{QT}=0.9003849000036579$ gives a nondegenerate bounded surface on which $S^{QT}_{ij}$ can be discussed after controls. The same evidence does not establish measured translation, folding, physical admissibility, biological function, mechanism, protein output, phenotype, or universality. Any such stronger claim requires a separate layer-matched contact and a new controlled statistic on joined rows.