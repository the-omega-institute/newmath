\subsection{Human N-terminal acetylation occupancy and bounded translation-window scope}\label{sec:human-nterminal-acetylation-occupancy-bounded-scope}
\origin{ai}

\paragraph{Finite row.}
The finite record considered here is the human N-terminal acetylation occupancy row keyed by the claim identifier
$h3.cross\_layer\_relation.nterminal\_acetylation.nterminal\_acetylation\_occupancy\_human$.
Its verified run is $90ea6b5f7fc7$, with $n=1426$ rows and runtime $5.372$ seconds.  The row records that the preregistered human gate preserves the frozen yeast rule under the stated transfer test.  It also records $abundance\_controlled=false$, so the result is not a protein-abundance-adjusted statement.  In symbols, the usable finite summary is
$$
\begin{aligned}
N_{\mathrm{NatA,human}}&=1426,\\
\tau_{\mathrm{NatA,human}}&=5.372\,\mathrm{s},\\
A_{\mathrm{control}}&=\mathrm{false}.
\end{aligned}
$$
The row therefore supports only a bounded occupancy comparison on the attached finite panel: the yeast-derived rule survives the preregistered human occupancy gates, but the table does not by itself say that codon geometry, ribosome occupancy, protein abundance, folding, localization, stability, phenotype, mechanism, or universality has been measured.

\paragraph{Coordinate readback.}
The admissible carrier is a finite table of coding-sequence or protein identifiers with a matched N-terminal-acetylation occupancy readout.  The human row is read as a finite cross-layer contact because the table has a row count, an executable run identifier, and a declared gate outcome.  The value $n=1426$ is the counting measure of the panel used by this chapter; it is not an asymptotic population size, a species-wide census, or a mechanistic sample of every human coding transcript.  The Boolean value $abundance\_controlled=false$ is part of the mathematical content of the row.  It prevents any interpretation in which the occupancy gate is silently converted into a protein-output-adjusted law.

\paragraph{Relation to the translation-window packet.}
The same finite bundle also carries a boundary request for an $f_3$-stress synonymous residual coordinate tested against position-resolved translation readouts.  That request requires same-organism, same-CDS, same-window joins among codon counts, bounded wobble-weight rows, matched mRNA abundance, and position-resolved ribosome-footprint occupancy.  Those rows are not present in the verified N-terminal-acetylation fact.  Thus the formal role of the $f_3$-stress coordinate is conditional rather than realized: it can become a translation-window covariate only after a matrix with the required translation readout exists.  A suitable row would have the finite form
$$
\begin{aligned}
&\bigl(organism,\ CDS,\ position\ bin,\ S_{f_3},\ W_{asym},\ Z_{AA},\ GC3,\ L,\ M,\ mRNA,\ RPF,\ b\bigr),\\
&S_{f_3}\ \text{tested against a bounded occupancy residual only when}\ b=\mathrm{pass}.
\end{aligned}
$$
Here $S_{f_3}$ denotes the synonymous third-position residual coordinate, $W_{asym}$ denotes the wobble-asymmetry flag, $Z_{AA}$ denotes amino-acid-bin controls, $L$ denotes length, $M$ denotes the methionine-density baseline, $mRNA$ is the matched transcript-abundance control, $RPF$ is the local ribosome-footprint readout, and $b$ is the rank and break-condition flag.  This display is a finite shape condition, not a statement that such rows have already been joined for the present human N-terminal-acetylation run.

\paragraph{What the verified row can claim.}
The human occupancy row can claim a finite gated transfer result: within the $1426$-row human panel, the preregistered rule imported from the yeast setting is retained by the human N-terminal-acetylation occupancy gate.  The claim is about occupancy status under that finite protocol.  It does not acquire an abundance adjustment, because the verified scalar explicitly says that abundance control is absent.  It also does not acquire a translation-window interpretation, because no matched ribosome-footprint statistic is reported in the row.  The mathematical support is therefore a bounded finite relation
$$
\begin{aligned}
\mathrm{YeastRule}\times \mathrm{HumanPanel}_{1426}\times \mathrm{Gate}_{pre}\longrightarrow \mathrm{OccupancySurvival},
\end{aligned}
$$
with no additional arrow to translation efficiency, protein abundance, structural order, physical admissibility, biological function, mechanism, phenotype, or universal law.

\paragraph{Linked finite contrast.}
A separate linked BioReality fact, $h3.cross\_layer\_relation.protein\_solubility.protein\_hydrophobic\_clustering\_solubility\_esol$, has run identifier $8742a224b538$, $n=3198$, and runtime $60.0$ seconds.  This fact is useful here only as a contrast in layer discipline.  Its larger row count and longer runtime belong to a protein-solubility surface, not to the human N-terminal-acetylation occupancy surface.  The two facts may coexist in the same finite audit bundle, but the solubility row is not evidence for N-terminal acetylation, and the N-terminal-acetylation row is not evidence for solubility.  Their scalar summaries are therefore kept as two separate finite contacts:
$$
\begin{aligned}
(N_{\mathrm{NatA}},\tau_{\mathrm{NatA}})&=(1426,5.372\,\mathrm{s}),\\
(N_{\mathrm{solubility}},\tau_{\mathrm{solubility}})&=(3198,60.0\,\mathrm{s}).
\end{aligned}
$$
This separation is essential because the chapter is not allowed to use a successful finite check on one biological surface as proof of a different surface.

\paragraph{Local boundary.}
The cannot-claim boundary is strict.  From the present finite rows one may report the human N-terminal-acetylation occupancy transfer result, the absence of abundance control for that result, and the existence of a separate protein-solubility finite run.  One may not infer translation realization from codon-window topology, modeled tAI or stAI weights, wobble asymmetry, review eligibility, or notation for an unjoined statistic.  One may not infer ribosome occupancy without footprint rows, protein abundance without matched abundance measurements and controls, folding or physical admissibility without a structure or physical assay, function without a functional contact, mechanism without perturbation or causal evidence, or universality without an independently scoped population design.  The row is therefore ready as a finite BioReality chapter only because its positive assertion is narrow: a $1426$-row human N-terminal-acetylation occupancy gate preserves the stated yeast-derived rule, while all translation-window and downstream biological interpretations remain blocked until their own matched contacts supply finite rows.