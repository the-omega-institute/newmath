\origin{ai}
\subsection{Human N-terminal acetylation occupancy as a bounded cross-layer row}
\label{subsec:h3-cross-layer-relation-nterminal-acetylation-nterminal-acetylation-occupancy-human}

\paragraph{Finite row and observed status.}
The row named \texttt{h3.cross\_layer\_relation.nterminal\_acetylation.nterminal\_acetylation\_occupancy\_human} records a human N-terminal acetylation occupancy test with experiment run identifier \texttt{90ea6b5f7fc7}. Its finite audit payload reports $n=1426$ rows and a runtime of $5.372$ seconds. The stored classification is that the preregistered yeast rule transfers to human under the declared gates. The same row also records \texttt{abundance\_controlled=false}, so the supported reading is not a protein-abundance-adjusted statement. It is a finite occupancy-row statement: within the curated human panel, the N-terminal acetylation rule survives the declared gate, but the row itself does not certify an abundance mediation, a folding consequence, a localization consequence, or a mechanism.

\paragraph{Coordinate scope.}
The admissible carrier is a finite table keyed by protein or coding-sequence identity, N-terminal status, and the matched occupancy readout used by the run. The row is allowed to say that the human occupancy panel has $1426$ evaluated entries and that the transfer rule was accepted by the preregistered gate. It is not allowed to infer a general organism-level law from the row count alone. The notation should therefore be read as a finite relation between a named N-terminal acetylation occupancy surface and a declared rule class, not as a complete biological account of translation, protein maturation, or cellular phenotype.

\paragraph{Attached comparison row.}
The same finite input bundle also contains \texttt{h3.cross\_layer\_relation.protein\_solubility.protein\_hydrophobic\_clustering\_solubility\_esol}, with experiment run identifier \texttt{8742a224b538}, $n=3198$, and runtime $60.0$ seconds. This second row is not evidence for the N-terminal acetylation claim. Its role here is audit separation: the bundle includes more than one cross-layer biological record, and the solubility row must remain confined to its hydrophobic-clustering/eSol surface. The N-terminal acetylation paragraph may cite the existence of the second row only to make clear that the chapter is not silently borrowing solubility, folding, or structural-order evidence for the acetylation occupancy result.

\paragraph{Bounded mathematical statement.}
Let $H$ be the finite human occupancy panel with $|H|=1426$. Let $a_i$ denote the N-terminal acetylation-side coordinate used for row $i$, and let $o_i$ denote the matched occupancy readout admitted by the run. The recorded result can be expressed as a bounded finite-row assertion:
$$
\begin{aligned}
&\text{run}(H)=\texttt{90ea6b5f7fc7},\\
&|H|=1426,\\
&\text{runtime}(H)=5.372\ \text{seconds},\\
&\text{abundance\_controlled}(H)=\mathrm{false},\\
&\text{gate\_readback}(H)=\text{yeast rule transfers to human under the preregistered gates}.
\end{aligned}
$$
This is the strongest local statement licensed by the supplied finite row. The absence of an abundance control is part of the result, not a removable annotation. Any downstream claim that requires abundance separation must introduce a separate matched abundance contact and recompute the statistic under that enlarged design.

\paragraph{Relation to the f\_3-stress translation-window material.}
The surrounding finite data describe a distinct f\_3-stress residual program in which coding-sequence windows, wobble-weight asymmetry, amino-acid composition controls, GC3, length, M-density, matched mRNA abundance, and position-resolved ribosome occupancy must be joined before a translation-window statistic can be promoted. That material does not upgrade the human N-terminal acetylation row. In this chapter it functions only as a local guardrail: codon-window topology, modeled tAI or stAI weights, review eligibility, and named ribosome-occupancy contacts do not supply measured translation realization unless the same-organism, same-CDS, same-window rows and rank checks exist.

\paragraph{No downstream promotion.}
The N-terminal acetylation occupancy row supports a bounded cross-layer occupancy record and the stated conservation-style readback under its gate. It does not establish translation efficiency, ribosome pausing, folding, physical admissibility, biological function, mechanism, phenotype, or universality. It also does not establish protein abundance because the finite row explicitly records \texttt{abundance\_controlled=false}. A stronger claim would require a separate finite table whose rows join the acetylation coordinate to the downstream measurement of interest, together with controls for abundance, sequence composition, experimental condition, rank sufficiency, and an explicit failure condition.

\paragraph{Break condition.}
The chapter fails its own scope if the run identifier \texttt{90ea6b5f7fc7}, the $1426$ human rows, or the phrase that the yeast rule transfers to human under preregistered gates is treated as proof of translation mechanism, folding, protein stability, localization, function, phenotype, or a universal biological law. It also fails if the comparison row \texttt{8742a224b538} with $3198$ eSol entries is used as structural support for N-terminal acetylation. The only admitted conclusion is the finite human occupancy-gate readback, with the no-abundance-control limitation kept visible.