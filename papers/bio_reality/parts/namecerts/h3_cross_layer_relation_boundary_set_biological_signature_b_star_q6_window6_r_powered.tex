\subsection{Window-six boundary avoidance in the $B^*_Q6$ residual panel}\label{subsec:bioreality-window-six-boundary-avoidance}
\origin{ai}

\paragraph{Finite object under test.}
This record treats the Window6 boundary set $R$ as a fixed finite object before any abundance readout is inspected.  The row reports $|R|=10$ sense codons inside the ordinary $61$-sense-codon alphabet, and the abundance comparison is then evaluated organism by organism.  The construction is therefore not a search over all codon subsets after seeing protein abundance; it is a test of one predeclared boundary set against per-organism abundance preference signs.  The experiment used $15$ requested and $15$ computed organisms, with a minimum inclusion rule of $500$ proteins per organism.  The deterministic seed is recorded as \texttt{sha256:b\_star\_q6\_boundary\_set\_biological\_signature\_powered:deterministic}, and each individual organism diagnostic used $1000$ permutations at two-sided level $\alpha=0.05$.

\paragraph{Direction ledger.}
For each computed organism the readback sign is one of three finite outcomes: avoided, preferred, or zero.  The all-computed summary is
$$
\begin{aligned}
N_{\mathrm{computed}}&=15,\\
N_{\mathrm{avoided}}&=15,\\
N_{\mathrm{preferred}}&=0,\\
N_{\mathrm{zero}}&=0,\\
\mathrm{majority\ fraction}&=1.0.
\end{aligned}
$$
The avoided organisms are \texttt{escherichia\_coli\_k12\_mg1655}, \texttt{mycobacterium\_smegmatis\_str\_mc2\_155}, \texttt{saccharomyces\_cerevisiae}, \texttt{homo\_sapiens}, \texttt{danio\_rerio}, \texttt{gallus\_gallus}, \texttt{pseudomonas\_aeruginosa\_pao1}, \texttt{bacillus\_subtilis\_subsp\_subtilis\_str\_168}, \texttt{mus\_musculus}, \texttt{caenorhabditis\_elegans}, \texttt{drosophila\_melanogaster}, \texttt{halobacterium\_salinarum}, \texttt{sulfolobus\_solfataricus}, \texttt{dictyostelium\_discoideum}, and \texttt{rattus\_norvegicus}.  No organism in this finite row has a preferred or zero direction.  The cross-organism sign test gives
$$
\begin{aligned}
P_{\mathrm{one\ sided,avoided}}&=3.0517578125\cdot 10^{-5},\\
P_{\mathrm{two\ sided}}&=6.103515625\cdot 10^{-5}.
\end{aligned}
$$
Thus the finite claim supported here is a directional abundance-preference signature: this particular Window6 boundary set is consistently avoided across the computed organism panel.

\paragraph{Individually significant organisms.}
The row also separates the sign count from the per-organism permutation diagnostic.  Six organisms pass the individual two-sided threshold, and all six have the same avoided direction:
$$
\begin{aligned}
N_{\mathrm{significant}}&=6,\\
N_{\mathrm{significant,avoided}}&=6,\\
N_{\mathrm{significant,preferred}}&=0,\\
N_{\mathrm{significant,zero}}&=0.
\end{aligned}
$$
Those six are \texttt{homo\_sapiens}, \texttt{danio\_rerio}, \texttt{gallus\_gallus}, \texttt{pseudomonas\_aeruginosa\_pao1}, \texttt{mus\_musculus}, and \texttt{rattus\_norvegicus}.  The diagnostic table records an older majority threshold of $8$, but the realized significant subset has size $6$; the chapter therefore uses the sign-test ledger and the significant-organism ledger as distinct finite readbacks rather than collapsing them into a single pass/fail rule.

\paragraph{Relation to the residual-coordinate panel.}
The boundary-set row is tied to the same $B^*_Q6$ cross-layer coordinate panel used in the surrounding finite analyses.  In the direction-aware H-ranking row, every computed residual-support vector $a_\perp$ lives in a nine-coordinate basis:
$$
\begin{aligned}
&\texttt{K\_AAA},\ \texttt{Arg\_AGR},\ \texttt{Ile\_AUA},\ \texttt{Leu\_CUN\_vs\_UUR},\ \texttt{Leu\_UUA\_vs\_UUG},\\
&\texttt{Ser\_UCR\_vs\_AGY},\ \texttt{Ser\_UCA\_vs\_UCG},\ \texttt{Thr\_ACR\_vs\_ACY},\ \texttt{f3\_stress}.
\end{aligned}
$$
The H-ranking policy uses absorbed energy $A_H$, coordinate-support cosine $C_H$, and the stated regularized score $A_HC_H-\lambda DL(H)$ with $\lambda=0.01$ and $DL(H)=\log_2(1+n_{\mathrm{columns}})$.  That ranking does not explain the Window6 boundary sign pattern as a mechanism.  It only gives a neighboring finite ledger showing that cross-layer readouts are weak and organism-specific: for example, the largest raw $A_HC_H$ in danio is measured TE at $0.011566051846174291$, while human ranks complex membership at $0.006566030115340095$ before the same $0.01$ one-column penalty, and yeast ranks mRNA at $0.0011462622274400686$.  Under the penalized score, danio measured TE is only $0.001566051846174291$ and most readouts are negative; this supports keeping the Window6 boundary signature as an association result rather than a discovered common biological channel.

\paragraph{Cross-organism context.}
The wider finite panel contains two relevant scale checks.  First, the optimal-codon conservation run computed $15$ of $16$ requested organisms with $41$ synonymous design columns and mean pairwise cosine $0.2629282074265475$, classified as clustered rather than universal identity.  Second, the yeast $f3$ stress readout reach test reports a selection-aware null $95$th percentile of $0.02697141300639046$ and an observed best-subset coverage of $0.22618445168998694$, leaving $0.7738155483100131$ unexplained after the full independent-readout set.  These numbers are not proofs of the Window6 boundary effect; they bound the surrounding interpretation.  The boundary set can carry a reproducible avoided-abundance sign while still sitting inside an organism-specific residual geometry that is only partially reached by fetchable readouts.

\paragraph{Local claim.}
The supported statement is deliberately narrow.  A fixed Window6 boundary set of $10$ codons, tested against abundance preference signs in $15$ organisms, has an all-computed avoided count of $15/15$ and a two-sided binomial sign-test value of $6.103515625\cdot 10^{-5}$.  Among individually significant organisms, the direction is again $6/6$ avoided.  This is enough to name a finite cross-organism avoided-abundance signature for this boundary set.

\paragraph{Boundary of interpretation.}
No translation process, ribosome mechanism, folding state, physical admissibility fact, biological function, phenotype, shared pathway, or universal law follows from this row.  The set $R$ is fixed by the Window6/BEDC geometry and then compared with external abundance data; that comparison is an association test.  The H-ranking quantities $A_H$, $C_H$, raw $A_HC_H$, and $A_HC_H-0.01DL(H)$ remain descriptive projection values, not causes.  Missing readouts are not zeros, within-null outcomes, or negative evidence.  Any stronger biological statement would require a separate layer-matched contact and a statistic for that layer rather than promotion of the boundary-set sign ledger itself.