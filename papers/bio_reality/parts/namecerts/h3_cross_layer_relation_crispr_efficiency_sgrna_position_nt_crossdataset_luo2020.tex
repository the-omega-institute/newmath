\origin{ai}
\subsection{Cross-protocol sgRNA position readout for the Luo CRISPR efficiency panel}
\label{sec:bioreality-crispr-sgrna-position-luo2020}

\paragraph{Finite assay surface.}
The record considered here is the cross-protocol sgRNA efficiency claim
\texttt{h3.cross\_layer\_relation.crispr\_efficiency.sgrna\_position\_nt\_crossdataset\_luo2020}.  Its finite assay surface is not a general statement about CRISPR biology; it is a bounded table-level contact between position-indexed guide-sequence features and a held-out CRISPR efficiency readout.  The verified Luo-side run supplies a cross-protocol panel with $n_{\mathrm{Luo}}=10543$ rows, a positive cross-protocol flag, and the declared frozen source recorded as the Doench2016 repository.  The run identifier is \texttt{2e9cce255c01}, with runtime $33.705459$ seconds at the recorded verification time $2026$-$06$-$24$T$05{:}41{:}53$Z.  The companion Doench-side finite contact, \texttt{h3.cross\_layer\_relation.crispr\_efficiency.sgrna\_position\_nt\_efficiency\_doench2016}, supplies $n_{\mathrm{Doench}}=5310$ rows and records $r_{\mathrm{GC,main}}=-0.0582917$ under run identifier \texttt{b6c6363256e0}.  These two finite facts are the numerical basis used here: a same-domain training or source surface from Doench2016 and a larger Luo2020 cross-protocol readback.

\paragraph{Row variables and controls.}
Each row is treated as an indexed guide observation rather than as a molecular mechanism.  Let $g$ denote an sgRNA row, $Y(g)$ its measured efficiency readout in the Luo-side panel, and $X_k(g)$ the nucleotide or local position feature at guide coordinate $k$.  The finite cross-protocol statement is that a position-feature coordinate may be tested for survival only after the finite row set is joined to the declared source protocol and after simpler composition explanations are kept visible.  The numerical surface is therefore summarized by
$$
\begin{aligned}
 n_{\mathrm{Luo}} &= 10543,\\
 n_{\mathrm{Doench}} &= 5310,\\
 r_{\mathrm{GC,main}} &= -0.0582917,\\
 \mathrm{cross\_protocol} &= \mathrm{true}.
\end{aligned}
$$
The small negative GC main correlation is not itself a claim that GC determines efficiency.  It is a scalar guardrail showing that composition must remain in the control set when position-indexed guide features are evaluated.  The cross-protocol flag means that the Luo panel is being read as an out-of-source or protocol-separated surface, not merely as an internal reuse of the same assay table.

\paragraph{Admissible claim.}
The admissible mathematical claim is a finite survival statement for guide-position coordinates.  For a selected position feature $X_k$, write $Z$ for the control family containing at least coarse nucleotide composition, guide-level GC summary, protocol/source indicators, and any simpler baseline features used before position-specific terms are admitted.  A position coordinate is retained only if the Luo-side held-out readout improves after these controls.  In symbolic form, the local survival entry has the shape
$$
\begin{aligned}
 S_k(Y\mid Z) &= \Delta\mathcal{L}(Y; Z, X_k)-\Delta\mathcal{L}(Y; Z),\\
 S_k(Y\mid Z) &> 0 \quad \text{only for a retained position feature.}
\end{aligned}
$$
Here $\Delta\mathcal{L}$ may be read as the finite improvement supplied by the chosen scoring procedure, such as an out-of-sample loss, rank score, or explained-variance increment.  The chapter does not require a universal functional form for $\Delta\mathcal{L}$; it requires that the reported row counts, source separation, and control status remain attached to the finite result.

\paragraph{Cross-protocol interpretation.}
The Luo row count $10543$ gives the cross-protocol surface enough finite support to ask whether Doench-style position features retain predictive contact beyond a single source table.  The Doench row count $5310$ and the recorded source label identify the prior assay surface from which the cross-protocol comparison is routed.  The pair of facts therefore supports a bounded statement: sgRNA position-indexed sequence coordinates can be tested as transportable predictors of measured CRISPR efficiency across a Luo-side panel anchored to the Doench2016 source route.  What is retained is the finite relation between guide sequence positions and measured efficiency; what is not retained is any unmeasured molecular story about why the relation occurs.

\paragraph{Local refusal boundary.}
No translation-layer, folding, physical-admissibility, protein-function, mechanistic, phenotype, or universality conclusion follows from this sgRNA efficiency record.  The measured object is CRISPR guide efficiency in finite assay tables, not ribosome occupancy, protein abundance, protein structure, cellular fitness mechanism, or a general biological law.  A position feature that survives in the Luo panel would still be only a bounded predictor for that assay family.  Any downstream claim about nuclease binding kinetics, repair outcome mechanism, chromatin state, transcript abundance, protein function, or organism-level phenotype would require a separate row-matched contact and its own controlled statistic.  Conversely, failure of a position feature in this finite panel would not prove that guide position is irrelevant in all CRISPR settings; it would only reject the specified cross-protocol survival entry under the controls and source split described above.

\paragraph{Status of this record.}
This chapter is ready as a self-contained BioReality record because the finite row counts and source separation are present: $10543$ Luo rows, $5310$ Doench rows, the cross-protocol flag, the Doench2016 frozen-source label, and the observed GC scalar.  The result should be cited only at that scope.  It records a cross-dataset sgRNA position-efficiency contact, with composition and source controls kept explicit, and with all higher-layer biological interpretations withheld until separately measured.