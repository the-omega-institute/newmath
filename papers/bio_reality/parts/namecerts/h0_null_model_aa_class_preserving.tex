\subsection{Amino-acid-class-preserving null model for a local codon tile}
\label{subsec:bioreality-aa-class-preserving-null-model}
\origin{ai}

\paragraph{Finite carrier and reality contact.}
The BioReality record for \texttt{h0.null.model.aa\_class\_preserving} is a finite code-read statement on the six-bit codon cube $Q_6$.  The carrier is not a biochemical mechanism; it is a coordinate display whose rows are codons and whose labels are supplied by the curated standard genetic-code table.  The contact asserts only that a standard-code table assigns each codon to an amino-acid or stop label.  The BEDC distinctions used here are the six-bit codon display, local tile faces, singleton label corners, and three-point label classes.  The internal structure is coordinate, closure, spectrum, and relation data, with readback through the standard-code label table.

\paragraph{Observed boundary row.}
The finite boundary relation $R$ has cardinality $13$.  Its codons are
$$
\begin{aligned}
R=\{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}
$$
The punctured-cube check gives the same row and records the finite face vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the object under test is a small local tile in $Q_6$, not a whole-code theorem.  The observed module partition accounts for all $13$ boundary codons with no missing, extra, or unassigned codons.  The modules are \texttt{agr} $=\{\texttt{AGA},\texttt{AGG}\}$, \texttt{ar\_lys} $=\{\texttt{AAA}\}$, \texttt{cun} $=\{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\}$, \texttt{ile\_met} $=\{\texttt{AUA}\}$, \texttt{ssn} $=\{\texttt{UCA},\texttt{UUA}\}$, and \texttt{stop\_trp} $=\{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}$.  The classification summary is $R\_size=13$, $assigned\_count=13$, $extra\_module\_codons=\emptyset$, $missing\_from\_modules=\emptyset$, and $unassigned\_codons=\emptyset$.

\paragraph{Spectral row.}
The local spectral data distinguish the observed tile from a purely combinatorial label shuffle.  The recorded values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The same row reports a cubic, or radial, polynomial residual of $-6.224354365258478\times 10^{-12}$, so the displayed values are numerically consistent with the stated finite spectral equation to the tolerance used by the probe.  These numbers are code-layer invariants of the coordinate construction.  They do not assert translation, folding, physical admissibility, biological function, mechanism, or universality.

\paragraph{Shape significance before the amino-acid-class null.}
A shape significance probe with seed $42$ drew $N=2000$ samples.  It required $57377$ distinct-sample attempts and incurred $55377$ duplicate retries.  The observed exact-shape count was $1$, giving $p_{exact}=0.0005$.  The high-Jaccard count at threshold $0.9$ was $25$, giving $p_{J\ge 0.9}=0.0125$.  In symbols, the finite-row report is
$$
\begin{aligned}
N&=2000,\\
\#\{\text{exact shape matches}\}&=1,\\
p_{exact}&=0.0005,\\
\#\{J\ge 0.9\}&=25,\\
p_{J\ge 0.9}&=0.0125.
\end{aligned}
$$
This supports the claim that the observed local row is not a routine outcome of the sampled null process, while still leaving the biological interpretation bounded by the curated label contact.

\paragraph{Amino-acid-class-preserving null model.}
The claim-specific null model preserves amino-acid class information while randomizing the relevant finite structure.  With seed $42$ and $N=2000$ null samples, the observed $M$ row has size $20$.  The exact-shape match count is $0$, so the empirical exact-shape probability is reported as $p=0.0$ at this sample resolution.  The lower-tail count at or below the observed $M$ size is $7$, giving
$$
\begin{aligned}
observed\_M\_size&=20,\\
N&=2000,\\
exact\_match\_count&=0,\\
p_{aa\_class,exact}&=0.0,\\
lower\_tail\_count\_{\le observed\ M}&=7,\\
p_{aa\_class,lower\ tail}&=0.0035.
\end{aligned}
$$
The exact-shape statistic says that no sampled amino-acid-class-preserving null row reproduced the observed finite shape.  The lower-tail statistic says that only $7$ of $2000$ sampled rows were at or below the observed $M$ size, yielding $0.0035$.  The correct reading is finite and local: the amino-acid-class-preserving null does not explain the observed window-six tile at the recorded resolution.

\paragraph{Leave-one checks.}
The leave-one-code-out row evaluated $23$ tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$.  The observed hit rate above baseline is $0.9841269841269841$, the hit rate in $M\setminus T$ is $0.9365079365079365$, and the hit rate in $M\setminus T$ plus boundary is $0.9841269841269841$.  By family, the five recorded family hit rates above baseline are: alternative yeast $0.8333333333333334$, bacterial and plastid $1.0$, candidate other $1.0$, mitochondrial $1.0$, and nuclear ciliate $1.0$.  The leave-one-codon-out row reports $anchor\_count=0$, $boundary\_count=5$, $closure\_coverage\_high=8$, $predictable\_count=8$, and $recovery\_rate=1.0$.  Its threshold check also records $recovery\_rate\_above\_threshold=1.0$.  These rows support code-read recovery inside the local tile and its boundary, not a claim about the molecular realization of translation.

\paragraph{Boundary of the claim.}
The supported statement is that window-six codon coordinates expose a statistically nontrivial local boundary-tile structure when read through a curated standard-code label table.  The contact tests code-read and local codon-tile label splitting only.  It cannot test translation realization, structural order, physical admissibility, folded protein structure, biological function, universal mechanism, or biological necessity.  Amino-acid label-sequence readback is not folded protein structure.  A local codon tile is not a global biological law.  Window-six geometry alone is not translation, structure, physical admissibility, or function.  Any promotion beyond code-read requires a separate higher-layer contact, such as a translation experiment, structure experiment, physical admissibility assay, or function-level observation.