\subsection{Amino-Acid-Class-Preserving Null Row for the Window-Six Codon Tile}\label{sec:bioreality-aa-class-preserving-null-row}
\origin{ai}

\paragraph{Finite carrier and readback.}
This BioReality row treats the standard-code codon table as a finite code-read contact on the six-bit codon cube $Q_6$. The BEDC carrier is not a biochemical mechanism: it is a finite coordinate display whose distinctions are the six-bit codon display, a local tile face, singleton label corners, and three-point label classes. The reality bridge is the curated standard-code table, which assigns each codon to an amino-acid label or a stop label. The bridge supports code-layer readback only. It does not observe translation realization, folded structure, physical admissibility, biological function, a universal mechanism, or any claim that DNA must obey this geometry as a biological law.

\paragraph{Observed boundary set.}
The row records a distinguished boundary set $R$ of cardinality $13$. The exact finite list is
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same row is read as a punctured-cube boundary with $f$-vector $(13,16,4)$. Thus the code-read phenomenon being tested is not an arbitrary narrative about codons, but a finite object with $13$ listed vertices, $16$ finite one-dimensional incidences, and $4$ two-dimensional faces in the displayed local geometry. The observed local set $M$ has size $20$ in the amino-acid-class-preserving null row. The supplied finite record gives the size and the statistical comparisons for $M$, but not the full codon list for $M$.

\paragraph{Spectral row.}
The same finite carrier carries a derived spectral summary. The recorded values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial or cubic polynomial residual is $-6.224354365258478\times 10^{-12}$, so the stored finite computation is numerically at tolerance for the stated spectral relation. This supports the claim that the window-six display has a reproducible local numerical signature. It does not convert that signature into a claim about translation, folding, physical admissibility, function, mechanism, or universality.

\paragraph{Amino-acid-class-preserving null comparison.}
The active claim row is the amino-acid-class-preserving null model. In this comparison, $N=2000$ finite samples were drawn with seed $42$. The observed local size is $|M|=20$. The exact shape match count under the amino-acid-class-preserving null was $0$, giving
$$
\begin{aligned}
p_{\mathrm{aa\ class,exact}} &= 0.0.
\end{aligned}
$$
The lower-tail count at or below the observed $M$ size was $7$ out of $2000$, giving
$$
\begin{aligned}
p_{\mathrm{aa\ class,lower\ tail}} &= 0.0035.
\end{aligned}
$$
This is the central finite-row conclusion: after preserving amino-acid label classes, the observed window-six local shape remains rare in the sampled null ensemble. The conclusion is only a code-read conclusion. It says that the standard-code label table makes this local finite tile unusually structured relative to the specified null model; it does not say that the tile is a biological law or a complete molecular explanation.

\paragraph{Shape comparison against an unrestricted finite sample.}
A companion shape-significance row used $N=2000$ samples with seed $42$. It required $57377$ distinct sample attempts and encountered $55377$ duplicate retries. The exact shape match count was $1$, so
$$
\begin{aligned}
p_{\mathrm{exact\ shape}} &= 0.0005.
\end{aligned}
$$
The high-overlap threshold counted $25$ samples with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{Jaccard}\geq 0.9} &= 0.0125.
\end{aligned}
$$
This companion row is consistent with the amino-acid-class-preserving row: exact recovery of the observed finite shape is rare, and near recovery is still a low-frequency event in the sampled ensemble. The duplicate-retry count is also part of the finite audit surface; it shows that the sampled space has strong collision pressure and that the reported $N=2000$ rows are distinct accepted samples rather than raw attempts.

\paragraph{Leave-one-code and leave-one-codon probes.}
The leave-one-code-out probe evaluated $23$ code tables and $63$ total targets. Its baseline hit rate was $0.19821739130434785$. The hit rate above baseline was $0.9841269841269841$, the hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary was $0.9841269841269841$. The family-level row has $5$ families: alternative yeast has hit rate $0.8333333333333334$, while bacterial/plastid, candidate other, mitochondrial, and nuclear ciliate each have hit rate $1.0$ above baseline. The leave-one-codon-out row records anchor count $0$, boundary count $5$, closure coverage high $8$, predictable count $8$, and recovery rate $1.0$. These probes show that the local boundary and closure data have finite predictive content at the code-label layer. They do not supply a contact for folded structure, physical admissibility, biological function, or translation mechanism.

\paragraph{Module classification.}
The $13$ codons in $R$ are completely assigned to six named modules. The finite module classification is
$$
\begin{aligned}
\mathrm{agr} &= \{AGA, AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA, CUC, CUG, CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA, UUA\},\\
\mathrm{stop\_trp} &= \{UAA, UAG, UGA\}.
\end{aligned}
$$
The assigned count is $13$, the reported $R$ size is $13$, and the lists of extra module codons, missing codons, and unassigned codons are all empty. Hence the boundary set is not merely cardinality-matched; it is exhausted by the named finite module decomposition. Every module has predictable-or-boundary support in the supplied row.

\paragraph{Scope boundary.}
The finite-row verdict is that the standard-code table supports a local codon-tile split in $Q_6$ and that the amino-acid-class-preserving null model gives no exact matches in $2000$ samples, with lower-tail probability $0.0035$ for the observed $M$ size. This is a ready code-read claim. The cannot-claim boundary is part of the statement: no translation realization, no folded protein structure, no physical admissibility, no biological function, no molecular mechanism, and no universality follows from this row unless a separate reality contact supplies that higher-layer observation.