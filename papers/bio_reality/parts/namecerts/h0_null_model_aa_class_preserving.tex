\origin{ai}
\subsection{Amino-acid-class-preserving null model for the window-six codon tile}
\label{sec:h0-null-model-aa-class-preserving}

\paragraph{Finite object.}
This BioReality record concerns the standard-code codon table as a finite code-read object, not as a biochemical mechanism.  The carrier is the six-bit codon cube $Q_6$, with the usual $64$ codon addresses read back through a curated standard-code label table.  The BEDC-side distinctions used here are the six-bit codon display, a local tile face, a singleton label corner, and a three-point label class.  The external contact is only the curated assignment of codons to amino-acid or stop labels.  It supplies codon-label readback; it does not supply translation dynamics, folding, physical admissibility, biological function, mechanism, or universality.

\paragraph{Observed residual set.}
The finite residual set $R$ has cardinality $13$.  In the verified row it is exactly
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row identifies $R$ with a punctured-cube pattern whose face data have $f$-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the claim being tested is not that the genetic code is forced by cube geometry.  The finite statement is narrower: after the standard-code labels are attached as an external contact, this specific set of $13$ codons forms the observed local boundary object in the six-bit display.

\paragraph{Amino-acid-class-preserving null.}
The null model preserves amino-acid class information while randomizing the local shape test.  With seed $42$ and $N=2000$ sampled null rows, the observed size of the BEDC-marked set was $20$.  The exact-shape count was $0$, giving
$$
\begin{aligned}
N&=2000,\\
\#\{\text{exact null matches}\}&=0,\\
p_{\mathrm{exact}}&=0.0.
\end{aligned}
$$
The lower-tail count at or below the observed marked-set size was $7$, so the finite-row lower-tail value was
$$
\begin{aligned}
p_{\mathrm{lower}}=\frac{7}{2000}=0.0035.
\end{aligned}
$$
This is the chapter's main null-model datum.  Under an amino-acid-class-preserving comparison, the observed tile is not reproduced by any of the $2000$ sampled exact-shape null rows, and only $7$ sampled rows land in the lower tail at or below the observed marked-set size.

\paragraph{Unrestricted shape comparison.}
A companion shape-significance row uses the same seed $42$ and $N=2000$, but records a broader shape comparison.  It required $57377$ distinct-sample attempts and $55377$ duplicate retries.  It found $1$ exact shape match and $25$ matches at Jaccard threshold at least $0.9$, hence
$$
\begin{aligned}
p_{\mathrm{shape}}&=\frac{1}{2000}=0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9}&=\frac{25}{2000}=0.0125.
\end{aligned}
$$
This comparison is not a replacement for the amino-acid-class-preserving null; it is a finite consistency check.  Both rows point in the same direction: the observed local code-read tile is uncommon among the sampled finite alternatives.

\paragraph{Spectral readback.}
The finite spectrum associated with the same code-read object gives
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The reported cubic or radial polynomial residual is $-6.224354365258478\times 10^{-12}$, and the spectral-radius entry in tolerance is $0.6752479308830213$.  These numbers are used here only as finite diagnostics of the same encoded object.  They do not convert the local codon tile into a dynamical law.

\paragraph{Leave-one readback checks.}
Two leave-one rows test whether the local tile remains informative when parts of the code-read contact are withheld.  In the leave-one-code-out row, $23$ tables were evaluated across $63$ targets.  The baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary was $0.9841269841269841$.  In the leave-one-codon-out row, the anchor count was $0$, the boundary count was $5$, the predictable count was $8$, and the recovery rate was $1.0$.  The high-closure coverage count was $8$, with recovery rate above threshold also $1.0$.

\paragraph{Family and module decomposition.}
The leave-one-code-out family row separates $5$ families.  Their hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
The module classification row assigns all $13$ residual codons and leaves no extra module codons, no missing residual codons, and no unassigned codons.  The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
This module list is the finite-row explanation of where the residual codons sit.  It is a local codon-label decomposition, not a protein-structure claim.

\paragraph{Boundary verdict.}
The finite evidence supports a code-read statement: the curated standard-code table, when placed on the six-bit codon cube, exposes a local boundary-tile pattern whose residual set has $13$ codons, punctured-cube face data $(13,16,4)$, significant sampled-null behavior, stable spectral diagnostics, and complete module classification.  The same evidence also fixes the cannot-claim boundary.  No translation realization, folded structure, structural order, physical admissibility, biological function, universal mechanism, or necessity claim follows from this chapter.  Any such promotion would require a separate higher-layer reality contact, distinct from the curated codon-label table used here.