\origin{ai}
\subsection{Amino-Acid-Class-Preserving Null Model for the Window-Six Codon Tile}
\label{sec:bior-reality-aa-class-preserving-null-model}

\paragraph{Finite carrier and contact layer.}
The object under test is the standard-code codon table read as a finite row over the six-bit codon cube $Q_6$.  The BEDC-side display records coordinates, closure, spectrum, and relation data; the biological contact records only a curated code table assigning each codon to an amino-acid or stop label.  Thus the carrier is finite and the reality contact is deliberately narrow: it supports codon-label readback and local tile checks, but it is not a contact for translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.  The local boundary relation $R$ has cardinality $13$, with the concrete codon list
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row is certified as a punctured-cube pattern with $f$-vector $(13,16,4)$.  In this chapter, $R$ is treated as a code-layer tile boundary: it is a finite combinatorial object visible after the standard-code labels are read back, not a biological law about DNA or proteins.

\paragraph{Spectral finite-row summary.}
The observed row has spectral quantities
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The reported cubic, equivalently radial, residual is $-6.224354365258478\cdot 10^{-12}$, so the numerical row satisfies the fitted finite polynomial relation to the recorded tolerance.  These numbers are not used as a physical model of translation.  They only say that, after the six-bit coordinate display and the standard-code readback have been fixed, the resulting finite matrix row has a stable local spectral signature.

\paragraph{Shape rarity under unconstrained and class-preserving nulls.}
Two finite null checks are relevant.  In the unconstrained shape comparison with $N=2000$ sampled rows and seed $42$, there was $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$.  Hence
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{J\geq 0.9} &= 0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null is stricter: it preserves the label-class structure while asking whether the observed local shape still appears.  In that run, again with $N=2000$ and seed $42$, the observed module size was $20$, the exact match count was $0$, and only $7$ sampled rows fell in the lower tail at or below the observed $M$ size.  The recorded values are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
The finite-row conclusion is therefore not merely that the tile is uncommon among arbitrary relabelings; it remains uncommon when the null model preserves amino-acid-class information.  This supports a code-read statement: the standard-code labels, when placed on the six-bit codon cube, expose a boundary tile whose observed shape is rare under the tested finite nulls.

\paragraph{Leave-one readback checks.}
The leave-one-code-out row evaluated $23$ tables and $63$ targets.  Its baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate inside $M\setminus T$ was $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus the boundary was $0.9841269841269841$.  By family, the hit-rate-above-baseline values were $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial-plastid, $1.0$ for candidate-other, $1.0$ for mitochondrial, and $1.0$ for nuclear-ciliate.  The leave-one-codon-out row reported anchor count $0$, boundary count $5$, closure coverage high $8$, predictable count $8$, and recovery rate $1.0$.  These numbers keep the claim inside the code layer: the row recovers label-position regularities and boundary behavior, not protein realization.

\paragraph{Module partition of the boundary row.}
The module classifier assigns all $13$ codons in $R$, with no missing, extra, or unassigned codons.  The concrete modules are
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The row count is therefore internally closed at $R_{\mathrm{size}}=13$ and $\mathrm{assigned\_count}=13$.  Each listed module has either predictable or boundary status in the leave-one-codon-out classification.  This is the chapter-specific reason to call the pattern a local tile: it is not just a set of codons, but a finite boundary set with a complete module assignment and a reproducible leave-one behavior.

\paragraph{Boundary verdict.}
The claim supported here is the following finite-row statement.  The standard-code label table, used only as a curated code-read contact, yields a six-bit codon-cube boundary row $R$ of size $13$ with $f$-vector $(13,16,4)$, stable spectral values $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$, rare shape behavior under both unconstrained and amino-acid-class-preserving nulls, high leave-one recovery, and complete module classification.  The cannot-claim boundary is part of the result: no translation process, folded protein structure, physical admissibility, biological function, universal mechanism, or necessity law follows from this tile.  Any such promotion requires a separate higher-layer contact, because the present contact observes codon-to-label assignment only.