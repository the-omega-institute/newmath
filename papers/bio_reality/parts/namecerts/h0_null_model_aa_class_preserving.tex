\origin{ai}
\subsection{Amino-Acid-Class-Preserving Null for the Window-Six Codon Tile}
\label{sec:biorreality-aa-class-preserving-null}

\paragraph{Finite carrier and readback contact.}
The finite object is the six-bit codon cube $Q_6$ equipped with the standard-code label readback.  The coordinate distinction is not a biological mechanism: it is a finite display in which codons are placed in a window-six geometry and then compared against the curated codon-to-label table.  The only external contact used here is the curated standard genetic-code bridge, whose observed fact is that each codon is assigned an amino-acid or stop label.  Thus the contact tests code-layer readback and local codon-tile label splits only.  It does not test translation realization, folded structure, physical admissibility, biological function, a universal mechanism, or any necessity claim about DNA obeying this geometry.

\paragraph{Observed punctured-cube row.}
The distinguished boundary row $R$ has cardinality $13$.  In codon notation the row is
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row is recorded as a punctured-cube pattern with finite $f$-vector $(13,16,4)$.  This is the row-level geometric datum against which the label-table readback is tested.  The row is local: it is a codon-window boundary tile, not a global law for genetic codes.

\paragraph{Spectral finite-row invariant.}
The associated spectral computation gives
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The cubic or radial polynomial residual is $-6.224354365258478\times 10^{-12}$, so the recorded spectral radius value is within the stated numerical tolerance.  This supplies a coordinate-and-spectrum invariant for the finite tile.  It still remains a code-layer invariant: no folding, physical admissibility, function, translation mechanism, or universality follows from these eigenvalue data alone.

\paragraph{Amino-acid-class-preserving null model.}
The null model preserves amino-acid classes while randomizing the relevant finite-row comparison.  With seed $42$ and $N=2000$ samples, the observed $M$ size is $20$.  The exact match count is $0$, giving an empirical exact-shape value $p=0.0$ at the displayed resolution.  The lower-tail count at or below the observed $M$ size is $7$, hence
$$
\begin{aligned}
\Pr_{\mathrm{null}}(\mathrm{lower\ tail}\leq M_{\mathrm{obs}})&=7/2000\\
&=0.0035.
\end{aligned}
$$
The class-preserving null therefore does not reproduce the observed local shape in this finite run.  A related shape-significance run with the same seed and $N=2000$ reports $1$ exact shape match, $25$ matches with Jaccard score at least $0.9$, $57377$ distinct-sample attempts, and $55377$ duplicate retries, yielding $p_{\mathrm{exact}}=0.0005$ and $p_{\mathrm{Jaccard}\geq 0.9}=0.0125$.  These numbers support the statement that the codon-window tile is nontrivial relative to the tested finite nulls, not the statement that it is a biological law.

\paragraph{Leave-one-code and leave-one-codon checks.}
The leave-one-code-out row evaluates $23$ code tables and $63$ total targets.  The baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M\setminus T$ is $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus boundary is $0.9841269841269841$.  The leave-one-codon-out check has anchor count $0$, boundary count $5$, predictable count $8$, closure coverage high $8$, and recovery rate $1.0$.  These finite checks say that the local tile has strong code-readback recovery under the tested omissions.  They do not say that the resulting amino-acid label sequence folds, functions, translates, or is physically realized.

\paragraph{Family and module decomposition.}
The family-level leave-one-code-out split records $5$ families.  The hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial/plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate.  The row-level module classification assigns all $13$ codons in $R$ with no extras and no omissions: $R_{\mathrm{size}}=13$, assigned count $13$, extra module codons $\varnothing$, missing-from-modules $\varnothing$, and unassigned codons $\varnothing$.  The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every listed module has predictable-or-boundary status in the supplied finite row.  This completes the local module accounting for the punctured-cube boundary row.

\paragraph{Local verdict and cannot-claim boundary.}
The chapter records a ready finite-row result for the claim $\mathrm{h0.null.model.aa\_class\_preserving}$.  The amino-acid-class-preserving null does not generate exact local-shape matches in $2000$ samples, and its lower-tail probability is $0.0035$ for the observed $M$ size.  Together with the punctured-cube row, the spectral invariant, the leave-one-code and leave-one-codon recovery checks, and the complete module classification, this supports a code-read local-tile statement: window-six codon coordinates expose a finite boundary pattern in the standard-code label table.  The result is deliberately bounded.  Without a separate higher-layer contact, it cannot be promoted to translation realization, folded protein structure, structural order, physical admissibility, biological function, a mechanism, or a universality theorem.