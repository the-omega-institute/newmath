\subsection{Amino-acid-class preserving null model for the local codon tile}\label{sec:bior-reality-aa-class-preserving-null-model}\origin{ai}

\paragraph{Finite carrier and reality contact.}
The finite carrier is the six-bit codon cube $Q_6$, read through the curated standard-code table as an external contact assigning each codon to an amino-acid or stop label. The geometric data are therefore not a biological mechanism by themselves. They are a finite coordinate display whose readback is constrained by a separate code table. The distinguished boundary row is the thirteen-codon set
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The finite audit records $|R|=13$ and identifies this same row with the punctured-cube boundary whose face-vector is $(13,16,4)$. Thus the contact is not merely a label list: it supplies a code-layer readback in which a concrete punctured local tile can be tested against curated codon labels.

\paragraph{Spectral row data.}
The same finite row has a recorded spectral signature. The measured values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial polynomial check gives $-6.224354365258478\times 10^{-12}$, and the same residual is recorded for the cubic polynomial condition. At the finite precision of the run, this residual is numerically zero for the stated row test. The spectral datum therefore supports a stable code-layer shape comparison, but it does not upgrade the row into translation, folded structure, physical admissibility, biological function, a mechanism, or a universal law.

\paragraph{Amino-acid-class preserving null model.}
The null test preserves amino-acid class while randomizing the relevant finite table structure. In $N=2000$ sampled null tables with seed $42$, the observed tile had $|M|=20$. The exact-shape count was $0$, and only $7$ null samples had row size at most the observed $M$ size. The recorded probabilities are
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0,\\
p_{\mathrm{lower\ tail}}&=0.0035.
\end{aligned}
$$
Thus the amino-acid-class preserving null does not reproduce the exact observed local shape in the sampled run, and the lower-tail event is rare at the recorded resolution. This is a finite-row significance statement about the standard-code label table under the specified null. It is not evidence that geometry alone causes the genetic code, and it is not evidence for protein realization or function.

\paragraph{Shape comparison against unconstrained sampled tables.}
A companion finite-row shape audit sampled $N=2000$ distinct tables, requiring $57377$ sample attempts and recording $55377$ duplicate retries. It found $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$. The corresponding probabilities were
$$
\begin{aligned}
p_{\mathrm{exact\ shape}}&=0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9}&=0.0125.
\end{aligned}
$$
These numbers place the observed local tile in a sparse part of the sampled finite shape space. The amino-acid-class preserving null is stricter for the exact shape in this run, since it records $0$ exact matches among $2000$ samples, while the unconstrained shape audit records $1$ exact match among $2000$ samples.

\paragraph{Leave-one tests and row modules.}
The leave-one-code-out audit evaluated $23$ tables and $63$ total targets. The baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$. Within $M\setminus T$ the hit rate was $0.9365079365079365$, and within $M\setminus T$ plus boundary it was again $0.9841269841269841$. By family, the recorded hit rates above baseline were $1.0$ for bacterial-plastid, mitochondrial, nuclear-ciliate, and candidate-other families, and $0.8333333333333334$ for alternative-yeast tables. The leave-one-codon-out audit records no anchors, $5$ boundary codons, $8$ high-coverage closure cases, $8$ predictable cases, and recovery rate $1.0$.

\paragraph{Complete module classification.}
The thirteen row codons are exhausted by six named modules:
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The module audit reports $R\_\mathrm{size}=13$, assigned count $13$, no extra module codons, no missing row codons, and no unassigned codons. Every module has either predictable or boundary status. The row is therefore closed as a finite code-layer classification: its codons, modules, row size, and boundary status are all accounted for inside the observed table readback.

\paragraph{Boundary of the claim.}
The claim is confined to the code-read layer. The curated table supports codon-label readback and local split checks; it does not observe translation realization, folded structure, structural order, physical admissibility, biological function, mechanism, or universality. Amino-acid label-sequence readback is not folded protein structure. A local codon tile is not a global biological law. Any promotion beyond the finite code-layer statement requires a separate reality contact designed for that higher layer.