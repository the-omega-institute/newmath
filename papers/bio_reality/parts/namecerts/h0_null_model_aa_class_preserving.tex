\origin{ai}
\subsection{Amino-acid-class-preserving null model for the window-six codon tile}
\label{sec:h0-null-model-aa-class-preserving}

\paragraph{Claim.}
This record concerns the claim \texttt{h0.null.model.aa\_class\_preserving}.  The object under inspection is the standard-code codon table, read only as a codon-to-label table.  The coordinate carrier is the six-bit codon cube $Q_6$, with distinctions for a six-bit codon display, local tile faces, singleton label corners, and three-point label classes.  The admissible readback is the curated standard-code label assignment: codons may be compared with amino-acid or stop labels, but no claim about translation realization, folded structure, physical admissibility, biological function, mechanism, or universality is licensed by this contact alone.

\paragraph{Observed boundary set.}
The finite row for the boundary set $R$ gives cardinality $13$ and identifies exactly the following codons:
$$
\begin{aligned}
R=\{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}
$$
A second finite check reports that the same $R$ is a punctured-cube pattern with face-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the row is not merely a count: it also carries a local combinatorial shape in the window-six display.  This is the level at which the geometric observation is meaningful.  It is a code-layer pattern attached to labels, not an observed physical process.

\paragraph{Spectral row.}
The spectral data give a second numerical signature for the same local tile.  The recorded values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The associated radial cubic residual is $-6.224354365258478\times 10^{-12}$, and the spectral-radius value in tolerance is again $0.6752479308830213$.  The small residual records numerical compatibility with the finite spectral equation used by the probe; it does not convert the codon tile into a biological law.  The only external contact in this record remains the curated code table.

\paragraph{Shape significance.}
Against the generic finite comparison row with seed $42$ and $N=2000$, the observed shape has one exact match among the sampled alternatives.  The row records $57377$ distinct-sample attempts and $55377$ duplicate retries, so the sampling space was constrained enough that uniqueness pressure matters.  The exact-shape probability is
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005,
\end{aligned}
$$
and the high-Jaccard threshold count is $25$, giving
$$
\begin{aligned}
p_{J\geq 0.9}=0.0125.
\end{aligned}
$$
These figures support the statement that the observed tile is uncommon under the sampled comparison class.  They do not by themselves explain why a biological system uses the labels; they only show that the coordinate-and-label pattern is not typical in that finite null row.

\paragraph{Amino-acid-class-preserving null row.}
The claim-specific null model preserves amino-acid class structure.  With seed $42$ and $N=2000$, the observed set size is $20$.  The exact-shape count is $0$, so the finite row reports
$$
\begin{aligned}
p_{\mathrm{class\ exact}}=0.0.
\end{aligned}
$$
For the lower-tail comparison, $7$ sampled cases have size at most the observed $M$ size, giving
$$
\begin{aligned}
p_{\mathrm{class\ lower\ tail}}=0.0035.
\end{aligned}
$$
The same value is recorded as the lower-tail significance statistic.  This is the central finite result for the present subsection: even after preserving amino-acid-class information in the null construction, the observed local tile remains sharply separated from the sampled alternatives by exact-shape and lower-tail tests.

\paragraph{Prediction rows.}
The leave-one-code-out row evaluated $23$ tables and $63$ targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The row also reports $0.9365079365079365$ inside $M\setminus T$ and $0.9841269841269841$ inside $M\setminus T$ plus the boundary region.  By family, the finite hit rates above baseline are $1.0$ for bacterial-plastid, mitochondrial, nuclear-ciliate, and candidate-other families, and $0.8333333333333334$ for alternative yeast.  The leave-one-codon-out row has anchor count $0$, boundary count $5$, predictable count $8$, high closure coverage $8$, and recovery rate $1.0$.  These rows show that the tile is useful for code-label recovery tasks inside the stated finite protocol, especially near the boundary set.  The conclusion remains a code-read conclusion.

\paragraph{Module classification.}
The module classification is complete for the $13$ codons in $R$: it reports $R$ size $13$, assigned count $13$, no extra module codons, no missing codons, and no unassigned codons.  The six modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\texttt{AGA},\texttt{AGG}\},\\
\mathrm{ar\_lys}&=\{\texttt{AAA}\},\\
\mathrm{cun}&=\{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\},\\
\mathrm{ile\_met}&=\{\texttt{AUA}\},\\
\mathrm{ssn}&=\{\texttt{UCA},\texttt{UUA}\},\\
\mathrm{stop\_trp}&=\{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}.
\end{aligned}
$$
Every module has a predictable-or-boundary status in the row.  This gives a finite partition of the boundary set that is compatible with the local tile interpretation and with the code-read recovery tests.

\paragraph{Local interpretation.}
The admissible conclusion is narrow.  In the standard-code table, the six-bit codon display exposes a local boundary-tile pattern whose $13$-codon set, punctured-cube face-vector, spectral values, null-model probabilities, recovery rates, and module partition all agree with a non-generic code-label structure.  The strongest claim supported by the data is therefore that amino-acid-class-preserving finite null models do not reproduce the observed local tile in the sampled row, and that the same tile supports high code-label recovery in the finite leave-one-out protocols.

\paragraph{Boundary.}
The same record blocks stronger readings.  The curated table contains assignments from codons to labels; it does not observe ribosomal translation, protein folding, structural order, thermodynamic or kinetic admissibility, biological function, or a universal mechanism.  Amino-acid label readback is not folded protein structure.  Cross-organism wording, CUN/UUR notation, or tRNA-Leu language would require separate organism-matched contacts before it could be used outside this code-layer packet.  Thus the result is a finite, table-anchored codon-label statement: the local tile is significant under the stated null rows, but any promotion beyond code readback must wait for an independent higher-layer observation.