\subsection{Amino-acid-class preserving null model for the codon boundary tile}\label{sec:bior-h0-null-model-aa-class-preserving}
\origin{ai}

\paragraph{Claim and finite display.}
The packet with identifier \texttt{h0.null.model.aa\_class\_preserving} tests whether the local codon-window pattern is still exceptional after the randomization is required to preserve amino-acid class labels.  The underlying finite carrier is the six-coordinate codon cube $Q_6$, equipped here only with the distinctions needed for code-layer readback: a six-bit codon display, local tile faces, singleton label corners, and three-point label classes.  The observed contact is a curated standard-code table assigning each codon to an amino-acid or stop label.  It supplies label readback, not a model of translation dynamics.

The principal boundary set $R$ has cardinality $13$.  In codon notation it is
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same finite list was recovered by the punctured-cube check, with face data summarized by the vector $(13,16,4)$.  Thus the observed local tile is not merely a count: it also carries a finite incidence profile, with $13$ vertices, $16$ one-dimensional adjacencies, and $4$ two-dimensional faces in the tested display.

\paragraph{Amino-acid-class preserving null test.}
The null model in this row keeps the amino-acid class constraint in force and then asks whether the observed local shape is typical under that restricted randomization.  For $N=2000$ sampled trials with seed $42$, the observed size of the tested set $M$ was $20$.  The exact observed shape appeared $0$ times among the $2000$ amino-acid-class preserving samples, giving the empirical exact-shape probability
$$
\begin{aligned}
p_{\mathrm{exact}} = 0/2000 = 0.0.
\end{aligned}
$$
Only $7$ of the $2000$ samples landed in the lower tail at or below the observed $M$ size, so the empirical lower-tail value was
$$
\begin{aligned}
p_{\mathrm{lower}} = 7/2000 = 0.0035.
\end{aligned}
$$
Both numbers point in the same direction: once the amino-acid class information is held fixed, the observed window-six local tile remains sharply atypical for this finite null ensemble.  The conclusion is a code-layer statement about the standard label table and the tested coordinate display.  It is not a claim that the geometry generates the biological code.

\paragraph{Shape and spectrum checks.}
A second finite shape run tested the same local organization without the amino-acid-class preserving restriction.  In $2000$ samples, with seed $42$, there was $1$ exact shape match and $25$ matches with Jaccard similarity at least $0.9$.  The corresponding empirical probabilities were
$$
\begin{aligned}
p_{\mathrm{shape}} &= 1/2000 = 0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9} &= 25/2000 = 0.0125.
\end{aligned}
$$
The run required $57377$ distinct-sample attempts and encountered $55377$ duplicate retries, which records that the sampling space was finite and heavily revisited under the distinctness condition.

The spectral row gives a compatible numerical signature.  The recorded values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The associated cubic residual was $-6.224354365258478\times 10^{-12}$, the same value recorded as the radial polynomial residual.  This is numerically indistinguishable from zero at the stated tolerance scale and therefore serves as a finite consistency check on the displayed spectral summary.  The spectral-radius entry was $0.6752479308830213$, matching the listed value of $\lambda_M$.

\paragraph{Predictive rows attached to the same code layer.}
The leave-one-code-out row evaluated $23$ tables and $63$ targets.  Its baseline hit rate was $0.19821739130434785$.  The hit rate inside $M-T$ was $0.9365079365079365$, while the hit rate inside $M-T$ together with the boundary was $0.9841269841269841$.  The same value, $0.9841269841269841$, was recorded for hit rate above baseline.  By family, the run covered $5$ families: alternative yeast had hit rate $0.8333333333333334$, while bacterial plastid, candidate other, mitochondrial, and nuclear ciliate each had hit rate $1.0$.

The leave-one-codon-out row records no anchor cases, $0$, and $5$ boundary cases.  It also records $8$ high-coverage closure cases, $8$ predictable cases, and recovery rate $1.0$.  These numbers support a local readback claim: when a codon or table entry is withheld, the finite neighborhood frequently recovers the held-out code-layer label position.  The result remains a statement about codon labels in the supplied table.

\paragraph{Module classification.}
The $13$ codons of $R$ are partitioned by the recorded module classification without omissions or extras.  The assigned count was $13$, the recorded $R$ size was $13$, the list of missing module codons was empty, the list of extra module codons was empty, and the list of unassigned codons was empty.  The six modules were
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Each module has either a predictable member or a boundary member in the recorded classification.  This supplies a compact finite account of how the punctured-cube boundary is distributed across familiar codon-label groups.

\paragraph{Local interpretation.}
The accepted interpretation is narrow.  The curated standard-code table confirms only that these codons carry the listed amino-acid or stop labels in the standard code.  The six-coordinate window, the punctured-cube boundary, the null-model probabilities, the spectral residual, and the leave-one-out rows together show that this finite code-layer arrangement is structured and atypical under the tested finite nulls.  They do not show that DNA is forced to obey this geometry as a biological law.

No translation realization, folded protein structure, physical admissibility, biological function, mechanism, or universality follows from this chapter.  An amino-acid label sequence is not a folded structure, and a codon-label table is not an experiment on folding, dynamics, cellular translation, organismal fitness, or biochemical feasibility.  Any future assertion crossing one of those boundaries must bring its own contact at that higher layer.  Until then, the result remains a finite statement about a local codon-tile split in the standard-code label table.