\subsection{Amino-acid-class-preserving null model for the local codon tile}
\label{subsec:h0-null-model-aa-class-preserving}
\origin{ai}

\paragraph{Finite setup.}
This record concerns a standard-code codon table viewed through the six-bit codon cube $Q_6$.  The visible data are codon labels: each of the $64$ codons is read as an amino-acid or stop label under the curated standard code.  The coordinate analysis distinguishes the six-bit codon display, local tile faces, singleton label corners, and three-point label classes.  The internal operations are only coordinate, closure, spectrum, and relation operations on this finite display.  They do not assert a biochemical mechanism.  The reality contact is the curated standard-code table, and its resolution is codon-label readback.

\paragraph{Observed boundary set.}
The distinguished boundary set $R$ has cardinality $13$.  Its codons are
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same finite row verifies that this set has the punctured-cube face count
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the local tile is not merely a list of exceptional codons: it is a finite subcomplex of the codon cube with $13$ vertices, $16$ one-dimensional incidences, and $4$ two-dimensional faces.  The module classification exhausts these $13$ codons with no missing or extra codons.  The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The row reports $R_{\mathrm{size}}=13$, assigned count $13$, no unassigned codons, no extra module codons, and no missing module codons.

\paragraph{Spectral finite check.}
The coordinate spectrum gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The associated radial expression evaluates to $-6.224354365258478\times 10^{-12}$, reported equally as the cubic-polynomial residual and the radial-polynomial residual.  At this tolerance scale, the spectral row records the same value $0.6752479308830213$ as the spectral radius in tolerance.  This is a finite numerical check on the codon-coordinate shape; it is not a physical energy law, not a folding constraint, and not an evolutionary universality statement.

\paragraph{Shape significance under broad sampling.}
The shape-significance run used seed $42$ with $N=2000$ sampled null instances.  It required $57377$ distinct-sample attempts and incurred $55377$ duplicate retries.  Among the $2000$ retained samples, there was $1$ exact shape match and $25$ samples with Jaccard overlap at least $0.9$.  The reported probabilities are
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9}&=0.0125.
\end{aligned}
$$
The same values are recorded as significant exact-shape and high-Jaccard $p$-values.  This supports the claim that the observed local tile is uncommon among the sampled finite alternatives.  The statement remains at the code-read layer, because the sampled objects are label-table alternatives rather than translation systems or molecular structures.

\paragraph{Amino-acid-class-preserving null row.}
The claim-specific null model preserves amino-acid class information.  In this stricter null row, again with seed $42$ and $N=2000$, the observed $M$ size is $20$.  The run reports $0$ exact matches to the observed shape.  It also reports $7$ samples in the lower tail at or below the observed $M$ size.  The finite probabilities are
$$
\begin{aligned}
p_{\mathrm{aa\ class,exact}}&=0.0,\\
p_{\mathrm{aa\ class,lower\ tail}}&=0.0035.
\end{aligned}
$$
The recorded significant values are the same: $p_{\mathrm{aa\ class,exact}}=0.0$ and $p_{\mathrm{aa\ class,lower\ tail}}=0.0035$.  The zero exact-match count does not mean mathematical impossibility; it means no exact match occurred in the $2000$ finite sampled rows.  The lower-tail count gives a second finite comparison in which only $7$ retained samples fall at or below the observed $M$ size.

\paragraph{Leave-one probes.}
The leave-one-code-out row evaluated $23$ code tables and $63$ targets.  Its baseline hit rate is $0.19821739130434785$.  The hit rate above baseline is $0.9841269841269841$, the hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ together with the boundary is $0.9841269841269841$.  The family-level leave-one-code-out row has $5$ families.  The reported family hit rates above baseline are $0.8333333333333334$ for alternative yeast, and $1.0$ for bacterial plastid, candidate other, mitochondrial, and nuclear ciliate families.

The leave-one-codon-out row reports anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$.  The same row records recovery rate above threshold as $1.0$.  These probes show that the finite tile is useful for local code-label recovery and boundary placement.  They do not show that the codon table itself implements a complete biological process.

\paragraph{Local verdict and scope.}
The finite rows make the following code-layer statement ready: window-six codon coordinates expose a local boundary-tile structure in the curated standard-code label table, and an amino-acid-class-preserving null model does not reproduce the observed shape in $2000$ sampled rows.  The exact observed boundary has $13$ codons, the punctured-cube count is $(13,16,4)$, the claim-specific exact-match count is $0$, and the lower-tail probability is $0.0035$.

No higher biological conclusion is licensed by this record alone.  The contact supplies codon-to-label assignments only.  It does not observe translation realization, folded protein structure, physical admissibility, biological function, a mechanism, or a universal law.  Any assertion about those layers would require a separate contact with data at that layer.  Amino-acid label-sequence readback is therefore kept distinct from folded structure, and the local codon tile is kept distinct from global biological necessity.