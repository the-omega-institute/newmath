\subsection{Leave-one-codon-out module classification on the window-six codon cube}\label{subsec:biorality-h1-leave-one-codon-out-module-classification}
\origin{ai}

\paragraph{Finite codon carrier.}
This BioReality record works at the code-read layer of the standard genetic code.  The finite carrier is the six-bit codon display $Q_6$, read as the $64$ codons obtained from three nucleotide positions with four choices at each position.  The biological contact used here is only a curated standard-code label table: each codon is assigned an amino-acid label or a stop label.  The contact is therefore a label table, not a translation apparatus, not a protein-structure observation, and not a physical model of molecular admissibility.

The distinguished leave-one-codon set $R$ has cardinality $13$.  In the observed row it is exactly
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row records that this set has the punctured-cube face count
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4),
\end{aligned}
$$
so the finite geometry being tested is a local tile and boundary pattern inside the codon cube.  No organismal mechanism is inferred from this combinatorial shape.

\paragraph{Spectral and sampling constraints.}
The associated spectral row gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  Its radial cubic check evaluates to $-6.224354365258478\times 10^{-12}$, which is numerically at zero at the reported tolerance.  Thus the recorded spectral quantities are internally coherent with the finite tile calculation.

The shape row was tested against $N=2000$ sampled null instances with seed $42$.  There was $1$ exact shape match, giving $p_{\mathrm{exact}}=0.0005$, and $25$ samples with Jaccard similarity at least $0.9$, giving $p_{J\geq 0.9}=0.0125$.  The sampling log also records $57377$ distinct-sample attempts and $55377$ duplicate retries, so the row is finite but not frictionless: the null space has enough repeated draws that the sampling process itself should be kept visible when interpreting tail probabilities.  Under the amino-acid-class-preserving null, with $N=2000$ and the same seed, the observed $M$ size is $20$, the exact-match count is $0$, and the lower-tail count at or below the observed $M$ size is $7$.  This gives $p=0.0$ for exact shape in the sampled run and $p=0.0035$ for the lower tail.  These numbers support a code-table regularity claim at the finite row level; they do not upgrade the row to a biological law.

\paragraph{Leave-one-code and family readback.}
The leave-one-code-out row evaluated $23$ code tables and $63$ targets.  The baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate inside $M\setminus T$ was $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus the boundary was again $0.9841269841269841$.  Family stratification separates this high aggregate rate into five recorded families: alternative yeast has hit rate $0.8333333333333334$, while bacterial plastid, candidate other, mitochondrial, and nuclear ciliate each have hit rate $1.0$ above baseline.  These rows say that the local code-label split is stable across the listed finite comparisons; they do not say that codon geometry causes translation or protein function.

\paragraph{Leave-one-codon recovery.}
The leave-one-codon-out row records no anchors, $5$ boundary cases, $8$ high-closure-coverage cases, and $8$ predictable cases.  Its recovery rate is $1.0$, and the recovery rate above threshold is also $1.0$.  In the present chapter this is interpreted as a local reconstruction statement: once a codon is omitted from the code-label table, the finite cube coordinates and boundary tile distinguish a predictable-or-boundary role for the affected row.  The interpretation is deliberately narrow.  It concerns the recovery of code labels and local boundary classes, not the realization of a peptide, the fold of a protein, physical feasibility, biological function, or universality across life.

\paragraph{Module classification.}
The module-classification row assigns all $13$ codons of $R$ with no extras, no missing codons, and no unassigned codons.  The complete module list is
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The accounting identity for the row is
$$
\begin{aligned}
2+1+4+1+2+3=13=|R|,
\end{aligned}
$$
and the recorded completion fields are $R_{\mathrm{size}}=13$, assigned count $13$, extra module codons $\varnothing$, missing codons $\varnothing$, and unassigned codons $\varnothing$.  Hence every codon in the leave-one-codon set is covered by a module that has either a predictable role or a boundary role.  The classification is a finite, row-level statement about the standard-code label table.  It is not evidence for folded structure, structural order, physical admissibility, biological function, a translation mechanism, or a universal rule unless an additional empirical contact is supplied for that higher layer.

\paragraph{Local verdict.}
The chapter therefore records a ready code-read result.  The standard-code table provides the labels, the $Q_6$ coordinate system provides the finite codon cube, and the tested rows show that the $13$-codon leave-one set is simultaneously a punctured local tile, a spectrally coherent finite pattern, a low-probability shape under the sampled nulls, and a completely classified module family.  The strongest supported statement is that window-six codon coordinates expose a robust local boundary-and-prediction structure for the curated label table.  The strongest blocked statement is equally important: the same data do not observe translation, folding, physical admissibility, function, mechanism, or biological universality.