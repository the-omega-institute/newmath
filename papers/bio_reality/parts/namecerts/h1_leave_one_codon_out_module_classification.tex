\subsection{Leave-One-Codon-Out Module Classification}\label{sec:bior-reality-leave-one-codon-out-module-classification}
\origin{ai}

\paragraph{Finite codon display.}
The record studies the standard-code codon table through a six-coordinate binary presentation of codons.  The working carrier is the codon cube $Q_6$, with codons read as finite vertices and with the curated standard-code table used only as a codon-to-label contact.  The construction separates four observable features: the six-bit codon display, a local tile face, singleton label corners, and three-point label classes.  These features are used to classify which codons become visible when one codon is withheld from the code-layer readback.  The contact is intentionally narrow: it observes that a curated table assigns codons to amino-acid or stop labels, and it does not observe translation dynamics, folded structure, chemical admissibility, function, mechanism, or universal biological law.

\paragraph{Boundary set and local geometry.}
The finite boundary set $R$ has cardinality $13$.  Its observed codons are
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same row verifies that this is the expected punctured-cube boundary, with face data
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the local object is not a full biological mechanism; it is a finite codon subset with $13$ vertices, $16$ one-dimensional incidences, and $4$ two-dimensional faces under the chosen coordinate closure.  The spectral row gives the paired local radii $\lambda_M=0.6752479308830213$ and $\lambda_R=0.4758352843197002$, with $\mu=3.051487585298128$.  The radial cubic check is numerically closed at $-6.224354365258478\times 10^{-12}$, so the spectral computation is stable at the recorded tolerance rather than a qualitative description.

\paragraph{Null comparisons.}
The observed $M$ shape was tested against $2000$ sampled controls with seed $42$.  There was $1$ exact shape match, giving $p=0.0005$, and $25$ controls reached Jaccard similarity at least $0.9$, giving $p=0.0125$.  The sampling process attempted $57377$ distinct draws and incurred $55377$ duplicate retries, so the row records a finite randomization pressure rather than an unrestricted population theorem.  A second amino-acid-class-preserving comparison again used $2000$ controls with seed $42$.  It recorded observed $|M|=20$, $0$ exact shape matches, and $7$ controls in the lower tail at or below the observed $M$ size.  The corresponding values are $p=0.0$ for exact shape in the finite run and $p=0.0035$ for the lower tail.  These rows support a nontrivial local code-layer pattern, but they do not convert the pattern into a claim about translation, folding, physical feasibility, organismal function, or universality.

\paragraph{Leave-one-code and leave-one-codon behavior.}
The leave-one-code-out row evaluated $23$ tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate inside $M-T$ was $0.9365079365079365$, and the rate inside $M-T$ together with the boundary was $0.9841269841269841$.  The leave-one-codon-out row gives a more local diagnostic: there were $0$ anchors, $5$ boundary cases, $8$ high-closure-coverage cases, and $8$ predictable cases, with recovery rate $1.0$ and recovery rate above threshold $1.0$.  In this chapter, these numbers mean that the six-coordinate code display recovers the withheld codon labels within the recorded local classes; they do not say that the cell implements the coordinate construction.

\paragraph{Module classification.}
The module row partitions all $13$ codons in $R$ into named local modules.  The recorded modules are
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
The completion check reports $R\_size=13$, $assigned\_count=13$, no extra module codons, no missing codons, and no unassigned codons.  Therefore every observed boundary codon is accounted for exactly once in the module ledger.  The module condition also records that every module has a predictable or boundary member: $\mathrm{agr}$ has $AGA,AGG$; $\mathrm{ar\_lys}$ has $AAA$; $\mathrm{cun}$ has $CUA,CUC,CUG,CUU$; $\mathrm{ile\_met}$ has $AUA$; $\mathrm{ssn}$ has $UCA,UUA$; and $\mathrm{stop\_trp}$ has $UAA,UAG,UGA$.

\paragraph{Family-level readback.}
A family stratification row lists $5$ families.  The family hit-rate-above-baseline values are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial-plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate.  These values show that the local boundary test is not confined to a single family label in the finite table audit.  They still remain code-read facts: the curated table supplies codon labels, while all claims about organism-specific translation apparatus, tRNA realization, folding pathways, structural order, physical admissibility, and biological function require separate observations.

\paragraph{Local verdict.}
The finite rows support the following chapter-level statement.  In the six-bit codon cube, the recorded boundary set $R$ forms a $13$-codon punctured-cube tile with stable face counts, a reproducible spectral signature, significant finite null comparisons, and a complete module classification.  Leave-one-code and leave-one-codon probes recover the withheld labels at high recorded rates, including a local recovery rate of $1.0$ in the leave-one-codon row.  The scientific scope is exactly the code layer: a local codon-tile pattern is visible in the curated standard-code readback, but no translation process, folded protein structure, physical admissibility, biological function, universal mechanism, or global biological law follows from these rows unless another contact directly measures that higher layer.