\subsection{Leave-one-codon-out module classification on the window-six codon cube}
\label{sec:bioreality-h1-leave-one-codon-out-module-classification}
\origin{ai}

\paragraph{Finite carrier and reality contact.}
The finite carrier is the six-bit codon cube $Q_6$, read as a codon display over the standard-code alphabet.  The biological contact used here is deliberately narrow: a curated standard-code table assigns each codon to an amino-acid label or to a stop label.  This contact supplies code-layer readback only.  It does not supply translation dynamics, folded structure, physical admissibility, function, mechanism, or universality.  The local BEDC distinctions are therefore the six-bit codon display, a local tile face, singleton label corners, and three-point label classes, while the readback remains the externally curated standard-code label table.

\paragraph{The residual row $R$.}
The residual codon row is finite and completely enumerated.  The verified cardinality is $13$, and the row is
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The punctured-cube check gives the same row and the finite face-vector
$$
\begin{aligned}
f(R)=(13,16,4).
\end{aligned}
$$
Thus the object being classified is not an inferred biological law but a concrete finite pattern: thirteen named codons, sixteen local one-dimensional adjacencies, and four local two-dimensional faces in the window-six coordinate geometry.

\paragraph{Spectral and shape evidence.}
The associated spectral row has numerical values
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial cubic residual is numerically negligible, with value $-6.224354365258478\times 10^{-12}$, and the same tolerance check reports spectral radius $0.6752479308830213$.  In the finite null comparison with $N=2000$ samples, the exact shape was matched once, giving $p_{\mathrm{exact}}=0.0005$.  The high-Jaccard threshold was met $25$ times, giving $p_{J\geq 0.9}=0.0125$.  The sampling record also reports $57377$ distinct-sample attempts and $55377$ duplicate retries, so the evidence should be read as a finite-row rarity statement under the implemented null sampler rather than as an asymptotic theorem.

\paragraph{Amino-acid-class preserving null.}
The amino-acid-class preserving null supplies a second finite control.  It used $N=2000$ samples, observed $M$-size $20$, found no exact shape matches, and counted $7$ samples in the lower tail at or below the observed $M$-size.  The reported values are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
These numbers support a code-layer statement: the observed local codon-tile geometry is not a typical output of the tested label-class preserving randomization.  They do not identify a biochemical cause for the pattern.

\paragraph{Leave-one-code and family checks.}
The leave-one-code-out run evaluated $23$ tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary was $0.9841269841269841$.  By family, the five-family report gives hit rates above baseline
$$
\begin{aligned}
\mathrm{alternative\ yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\ plastid} &= 1.0,\\
\mathrm{candidate\ other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\ ciliate} &= 1.0.
\end{aligned}
$$
These are finite readback rates over curated code tables.  They justify treating the tile boundary as a strong local predictor for the tested code-label rows, but they still remain inside the code-read layer.

\paragraph{Leave-one-codon recovery.}
The leave-one-codon-out row reports no anchors, $5$ boundary cases, $8$ high-coverage closure cases, and $8$ predictable cases.  Its recovery rate is $1.0$, and the reported recovery rate above threshold is also $1.0$.  In this setting, prediction means that the missing codon label is recovered from the finite local closure and boundary geometry supplied to the probe.  It does not mean that a cell translates by this geometry, that a protein fold is determined by it, or that the codon cube supplies a universal biological mechanism.

\paragraph{Module classification.}
The module classification is complete on the residual row.  The assigned count is $13$, the residual size is $13$, and the lists of extra module codons, codons missing from modules, and unassigned codons are all empty.  The complete module partition is
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every module has at least one predictable-or-boundary representative, and together the modules account for exactly the thirteen codons of $R$.  This is the central finite-row result: the residual punctured-cube row decomposes into named code-label modules without leftover codons.

\paragraph{Boundary of the claim.}
The supported claim is a local code-read claim.  Window-six codon coordinates expose a boundary-tile organization in the curated standard-code label table, and the leave-one-codon-out module classification closes the finite residual row.  The unsupported promotions are explicitly blocked.  No translation realization, folded protein structure, structural order, physical admissibility, biological function, universal mechanism, or global biological law follows from this chapter unless a separate reality contact supplies that higher-layer evidence.