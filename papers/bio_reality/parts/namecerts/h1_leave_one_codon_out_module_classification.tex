\origin{ai}
\subsection{Leave-one-codon-out module classification in the window-six codon cube}
\label{sec:bioreality-h1-leave-one-codon-out-module-classification}

\paragraph{Finite codon display.}
The record uses the standard-code codon table only as a curated label contact: each codon is read as a triplet with an amino-acid or stop label, and the window-six coordinate display supplies the finite cube in which local tile faces can be inspected. The carrier is the six-bit codon cube $Q_6$, with local distinctions given by coordinate, closure, spectrum, and relation data. The tested residue set $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row records $R$ as a punctured-cube shape with $f$-vector $(13,16,4)$. Thus the finite object under discussion is not an unrestricted biological mechanism; it is a small labeled region of the codon cube whose boundary behavior can be compared with the curated standard-code labels.

\paragraph{Spectral and sampling support.}
The finite-row spectrum gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$. The radial cubic residual is $-6.224354365258478\times 10^{-12}$, which is numerically at tolerance for the recorded computation. Shape significance was tested over $N=2000$ samples with seed $42$: there was $1$ exact shape match, giving $p_{\mathrm{exact}}=0.0005$, and $25$ samples had Jaccard overlap at least $0.9$, giving $p_{J\ge 0.9}=0.0125$. The sampler reports $57377$ distinct-sample attempts and $55377$ duplicate retries, so the finite comparison is visibly constrained by the discrete state space rather than by a continuous approximation. Under the amino-acid-class-preserving null model, again with $N=2000$ and seed $42$, the observed $M$-size is $20$, the exact-match count is $0$, and the lower-tail count at or below the observed size is $7$, yielding lower-tail probability $0.0035$ and exact-shape probability $0.0$ in the sampled run.

\paragraph{Leave-one-code and leave-one-codon behavior.}
The leave-one-code-out row evaluated $23$ tables and $63$ total targets. Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$. The hit rate inside $M-T$ is $0.9365079365079365$, and the rate inside $M-T$ plus boundary is $0.9841269841269841$. The leave-one-codon-out row has anchor count $0$, boundary count $5$, closure coverage high count $8$, predictable count $8$, and recovery rate $1.0$. These numbers justify the local statement that omitted codons in this row are recovered by predictable or boundary behavior inside the displayed codon-tile geometry. They do not justify a claim that the geometry performs translation, produces protein structure, enforces physical admissibility, realizes biological function, or states a universal biological rule.

\paragraph{Module classification.}
The module assignment is complete on the recorded set $R$: $R\_\mathrm{size}=13$, assigned count $13$, and the lists of extra module codons, missing codons, and unassigned codons are all empty. The six recorded modules are
$$
\begin{aligned}
\mathrm{AGR}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{AR\_Lys}&=\{\mathrm{AAA}\},\\
\mathrm{CUN}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{Ile\_Met}&=\{\mathrm{AUA}\},\\
\mathrm{SSN}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{Stop\_Trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every module has at least one codon certified by the predictable-or-boundary condition in the row. The classification therefore closes the finite accounting problem for this claim: all $13$ codons in $R$ are placed, no codon outside the recorded module union is introduced, and no member of $R$ remains unclassified.

\paragraph{Family-level stability.}
The related family summary reports $5$ families. Hit rates above baseline are $1.0$ for bacterial/plastid, candidate-other, mitochondrial, and nuclear-ciliate families, and $0.8333333333333334$ for alternative yeast. This does not make the local tile a global law; it says that the same finite readback test remains strong across the recorded table families, with the alternative-yeast family still above the baseline but not at perfect rate. The biological contact remains the curated table of codon-to-label assignments, so the evidence is confined to the code-read layer.

\paragraph{Scope boundary.}
The chapter records a local codon-label phenomenon: a window-six coordinate display exposes a finite boundary-tile classification for the $13$ codons in $R$, and the curated standard-code table supplies the labels against which that classification is read. No conclusion about translation realization, folded structure, structural order, physical admissibility, biological function, mechanism, or universality follows from these rows alone. Any such stronger statement would require a separate contact that directly observes the relevant higher-layer object rather than only the codon-to-label table.