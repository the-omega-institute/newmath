\origin{ai}
\subsection{Leave-One-Codon-Out Module Classification}
\label{subsec:h1-leave-one-codon-out-module-classification}

\paragraph{Finite carrier and reality contact.}
This record concerns the standard-code codon table read as finite data over the six-bit codon cube $Q_6$.  The BEDC carrier is the codon-coordinate display, while the external reality contact is the curated standard genetic-code bridge assigning each codon to an amino-acid label or a stop label.  The claim is therefore a code-read claim: window-six coordinates may expose local boundary-tile structure, but the label table itself remains an external contact.  The finite row under study is the residual set $R$ of thirteen codons
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The verified cardinality is $|R|=13$.  The same row is certified as a punctured-cube tile with face-vector $(13,16,4)$: thirteen vertices, sixteen local edges, and four local two-faces in the finite tile.  These numbers are not interpreted as a biological law; they are the coordinate signature of the row seen inside $Q_6$.

\paragraph{Spectral and shape evidence.}
The associated finite spectrum supplies three scalar checks.  The measured values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\times 10^{-12}$, which is numerically at tolerance for the recorded cubic relation.  In the shape-randomization row with $N=2000$ samples and seed $42$, the exact shape matched once, giving $p_{\mathrm{exact}}=0.0005$.  High-overlap shape matches at Jaccard threshold $0.9$ occurred $25$ times, giving $p_{J\geq 0.9}=0.0125$.  The distinct-sample procedure attempted $57377$ draws and recorded $55377$ duplicate retries, so the quoted probabilities are attached to a constrained finite sampling problem rather than to an unconstrained continuous null.  Under the amino-acid-class-preserving null model, again with $N=2000$ and seed $42$, the observed module size is $20$, the exact-shape match count is $0$, and the lower-tail count at or below the observed $M$ size is $7$.  Thus the recorded class-preserving probabilities are $p_{\mathrm{class\ exact}}=0.0$ and $p_{\mathrm{lower\ tail}}=0.0035$.

\paragraph{Leave-one-code and leave-one-codon behavior.}
The leave-one-code-out table evaluates $23$ genetic-code tables and $63$ targets.  Its baseline hit rate is $0.19821739130434785$, while the recorded hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M\setminus T$ is $0.9365079365079365$, and the rate inside $M\setminus T$ plus the boundary is again $0.9841269841269841$.  At the finer leave-one-codon-out level, the anchor count is $0$, the boundary count is $5$, the high-coverage closure count is $8$, and the predictable count is $8$.  The recovery rate is $1.0$, with the threshold comparison also recorded as $1.0$.  These values support a local code-layer recovery statement: missing codons in the row are recovered by predictable or boundary behavior inside the finite window.

\paragraph{Family stratification.}
The family-level leave-one-code-out row separates the evidence into five families.  The family hit-rate-above-baseline values are
$$
\begin{aligned}
\mathrm{alternative\ yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\ plastid} &= 1.0,\\
\mathrm{candidate\ other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\ ciliate} &= 1.0.
\end{aligned}
$$
This stratification is important because the code-read signal is not reported only as a pooled scalar.  Four of the five families have perfect recorded hit rate above baseline, and the remaining family still lies well above the baseline row.  The finite evidence therefore points to a robust local module effect across the tested family partition, subject to the same code-layer limitation.

\paragraph{Complete module classification.}
The module classification is complete on the thirteen-codon residual row.  The assigned count is $13$, the residual size is $13$, and the lists of extra module codons, missing module codons, and unassigned codons are all empty.  The verified partition is
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Each module contains only codons from $R$, and every codon in $R$ appears in exactly one displayed module.  The row-level conclusion is therefore finite and exact: every residual codon belongs to a named module, and every named module has predictable-or-boundary support in the leave-one-codon-out classification.

\paragraph{Cannot-claim boundary.}
The contact used here is a curated codon-label table.  It supports codon-label readback and local tile-split checks, but it does not observe translation realization, folded structure, structural order, physical admissibility, biological function, mechanism, or universality.  In particular, amino-acid label-sequence readback is not folded protein structure.  The local tile in $Q_6$ is not a global biological law, and window-six geometry alone is not translation, structure, physical admissibility, or function.  Any promotion beyond code-read would require a separate higher-layer contact, such as an experimental translation contact, a structural assay, a physical-admissibility model with its own validation, or a functional measurement.  Without such a contact, the present chapter proves only the finite code-layer statement: the thirteen-codon residual row has a complete predictable-or-boundary module classification against the curated standard-code label table.