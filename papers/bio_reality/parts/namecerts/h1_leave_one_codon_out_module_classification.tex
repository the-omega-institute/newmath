\subsection{Leave-one-codon-out module classification in the window-six codon cube}\label{sec:h1-leave-one-codon-out-module-classification}
\origin{ai}

\paragraph{Finite carrier and code-layer contact.}
The finite carrier is the six-bit codon cube $Q_6$, read only through the standard-code codon-label table.  The claim concerns code-layer readback, not a mechanism of translation.  The distinguished residual row $R$ has cardinality $13$ and is exactly the following codon set:
$$
\begin{aligned}
R=\{&AAA,AGA,AGG,AUA,CUA,CUC,CUG,CUU,\\
&UAA,UAG,UCA,UGA,UUA\}.
\end{aligned}
$$
The same row is detected as a punctured-cube trace with finite face-vector $(13,16,4)$.  Thus the local object is not a vague cluster: it has thirteen vertices, sixteen local one-faces, and four local two-faces inside the codon-coordinate display.  The observed table contact supplies only the labels attached to these codons in the curated standard genetic code; it does not by itself supply translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.

\paragraph{Spectral and null-model profile.}
The associated finite spectrum separates the larger module row from the residual row.  The recorded values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\cdot 10^{-12}$, so the displayed spectral constraint is numerically closed at the recorded tolerance.  Against the shape null model with $N=2000$ samples and seed $42$, the exact observed shape occurred once, giving $p_{\mathrm{exact}}=0.0005$.  High-Jaccard matches with threshold $0.9$ occurred $25$ times, giving $p_{J\ge 0.9}=0.0125$.  The run required $57377$ distinct-sample attempts and recorded $55377$ duplicate retries, which is part of the finite-row audit rather than biological interpretation.  Under the amino-acid-class-preserving null model with $N=2000$, the observed module size was $20$, exact-shape matches were $0$, and the lower-tail count at or below the observed size was $7$, giving lower-tail value $0.0035$.

\paragraph{Leave-one-code and leave-one-codon readback.}
The leave-one-code-out test evaluated $23$ code tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate inside $M\setminus T$ was $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus the boundary was again $0.9841269841269841$.  The leave-one-codon-out row records $0$ anchors, $5$ boundary codons, $8$ high-coverage closure codons, and $8$ predictable codons, with recovery rate $1.0$.  These numbers say that the code-label table is highly recoverable from the local coordinate split when one codon or one code is withheld; they do not say that the codon cube causes the biological code.

\paragraph{Module classification.}
The module classification is complete on $R$: the assigned count is $13$, the row size is $13$, and the lists of extra module codons, missing row codons, and unassigned codons are all empty.  The six modules are
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
Every listed module has a predictable-or-boundary witness in the leave-one-codon-out audit.  The largest local module is $\mathrm{cun}$ with four codons, the stop-tryptophan boundary module has three codons, the arginine module has two codons, the serine/leucine split module has two codons, and the lysine and isoleucine/methionine modules are singleton boundary contacts.  This is the chapter-level finite-row statement: the residual punctured-cube row is exhausted by named codon-label modules, and no codon in the row is left outside the classification.

\paragraph{Family-level stability.}
The code-family audit records five families.  The hit rates above baseline are $1.0$ for bacterial/plastid, candidate-other, mitochondrial, and nuclear-ciliate families, and $0.8333333333333334$ for the alternative-yeast family.  The result therefore has family-level support but not family-level identity: one family is lower than the exact-hit ceiling, and the contact remains a table-level readback contact.  The statement is a finite classification of codon-label behavior under the recorded windows and null models, not a law of biology.

\paragraph{Cannot-claim boundary.}
The local tile may be used to state a code-read fact: within $Q_6$, the standard-code table exposes a residual row of thirteen codons, a punctured-cube face-vector $(13,16,4)$, a complete six-module classification, and strong leave-one-code and leave-one-codon recovery rates.  Nothing in this contact upgrades the result to translation realization, folded protein structure, physical admissibility, biological function, causal mechanism, or universality.  Any such promotion would require a separate reality contact at that higher layer.