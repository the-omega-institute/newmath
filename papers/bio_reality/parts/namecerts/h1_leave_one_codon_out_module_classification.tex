\subsection{Leave-one-codon-out module classification in the window-six codon cube}\label{sec:h1-leave-one-codon-out-module-classification}
\origin{ai}

\paragraph{Finite carrier and readback contact.}
This chapter records a finite-row BioReality claim at the code-read layer.  The carrier is the six-bit codon cube $Q_6$, equipped with the distinctions of a six-bit codon display, local tile faces, singleton label corners, and three-point label classes.  The external contact is a curated standard-code table assigning each codon to an amino-acid or stop label.  That contact is used only as a codon-label readback bridge.  It is not treated as a mechanism of translation, not as folded structure, not as physical admissibility, not as biological function, and not as a universal law.

\paragraph{The recurrent row set.}
The finite recurrent set $R$ has cardinality $13$.  The recorded codons are
$$\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}$$
The same row is also recorded as a punctured-cube shape with $f$-vector
$$\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}$$
Thus the row is not merely a list of exceptional labels; it is a finite cubical boundary pattern inside the window-six display.

\paragraph{Spectral scale of the row.}
The spectral row data attach the scalar values
$$\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}$$
The radial polynomial residual is recorded as $-6.224354365258478\cdot 10^{-12}$, the same value reported for the cubic-polynomial check.  At the numerical resolution of the finite run, this residual is effectively at the floating-point tolerance scale for the claimed spectral relation.  The spectral datum is therefore used here as a finite coordinate witness for the row, not as an independent biological principle.

\paragraph{Null-model pressure.}
The shape-significance run used $N=2000$ sampled rows with seed $42$.  It required $57377$ distinct-sample attempts and $55377$ duplicate retries.  The exact shape appeared once, giving
$$\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}$$
The high-overlap condition $J\geq 0.9$ appeared $25$ times, giving
$$\begin{aligned}
p_{J\geq 0.9}=0.0125.
\end{aligned}$$
A separate amino-acid-class-preserving null run, again with $N=2000$ and seed $42$, recorded an observed $M$-size of $20$, exact-match count $0$, and lower-tail count $7$.  Its reported finite-row probabilities were
$$\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}$$
These values support the statement that the observed tile is sparse under the sampled null constraints.  They do not identify a biochemical cause.

\paragraph{Leave-one-code-out recovery.}
The leave-one-code-out row evaluated $23$ code tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$.  The hit rate inside $M\setminus T$ was $0.9365079365079365$, while the hit rate inside $M\setminus T$ together with the boundary was $0.9841269841269841$.  The same value, $0.9841269841269841$, is recorded as the hit rate above baseline.  By family, the row count is $5$, with hit rates above baseline
$$\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}$$
These numbers say that the local coordinate-and-boundary rule tracks most of the finite code-read targets in this sampled comparison.  The weaker alternative-yeast rate remains visible rather than being averaged away.

\paragraph{Leave-one-codon-out recovery.}
The leave-one-codon-out row records anchor count $0$, boundary count $5$, closure-coverage-high count $8$, predictable count $8$, and recovery rate $1.0$.  The recovery rate above threshold is also $1.0$.  This is the local content of the claim: when one codon is removed from the row, the finite window-six structure classifies the held-out codon through predictable closure or boundary membership.  Since the anchor count is $0$, the recovery is not being certified by fixed singleton anchors; it is being certified by the tile boundary and closure pattern.

\paragraph{Module classification.}
The module classification partitions the $13$ codons of $R$ into six named finite modules:
$$\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}$$
The completeness row reports $R$-size $13$, assigned count $13$, no extra module codons, no codons missing from the modules, and no unassigned codons.  Hence every codon in the recurrent row is accounted for exactly at the module-classification level.

\paragraph{Claim boundary.}
The chapter claim is therefore finite and local: the window-six codon coordinate exposes a recurrent boundary-tile pattern whose $13$ codons admit a complete module classification, and whose leave-one-codon-out behavior is fully covered by predictable or boundary status in the recorded row.  The standard-code table supplies the label readback contact.  Nothing in this chapter claims that the local tile is translation itself, that the tile determines folded protein structure, that it gives physical admissibility, that it entails biological function, that it supplies a mechanism, or that it establishes universality.  Any such higher-layer assertion would require a separate reality contact beyond the curated codon-label table.