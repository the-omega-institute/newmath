\origin{ai}

\subsection{Leave-one-codon-out module classification in the window-six codon cube}
\label{sec:boreality-h1-leave-one-codon-out-module-classification}

\paragraph{Finite carrier and readback contact.}
The object in this row is the standard-code codon table read through a six-bit codon display.  The finite carrier is the codon cube $Q_6$, and the only external contact used here is a curated standard-code table assigning each codon to an amino-acid or stop label.  The BEDC-side distinctions are the six-bit codon display, a local tile face, singleton label corners, and three-point label classes.  The internal structure used by the row is coordinate, closure, spectrum, and relation data; the biological readback is only the standard-code label table.  This is therefore a code-layer record, not a translation, folding, physical, functional, mechanistic, or universal claim.

\paragraph{The exceptional row $R$.}
The finite row singled out by the computation has cardinality $13$.  In codon notation it is
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The punctured-cube audit returns the same list and the face data
$$
\begin{aligned}
f(R)=(13,16,4).
\end{aligned}
$$
Thus the row is not merely counted as a thirteen-element subset; it is also recognized as a finite punctured-cube-shaped tile with $13$ vertices, $16$ one-dimensional incidences, and $4$ two-dimensional faces in the displayed local geometry.

\paragraph{Spectral finite-row check.}
The spectral row assigns
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The same run reports a cubic, or radial, polynomial residual of $-6.224354365258478\cdot 10^{-12}$, so the finite spectral relation is satisfied to numerical tolerance.  In this chapter the spectral data are used only as a finite consistency check for the local codon-row geometry.  They do not by themselves identify an evolutionary mechanism or a biochemical necessity.

\paragraph{Null-model scale for the $M$ shape.}
The shape-significance audit used $N=2000$ sampled rows with seed $42$.  It required $57377$ distinct-sample attempts, including $55377$ duplicate retries.  There was $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9} &= 0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null model also used $N=2000$ and seed $42$.  In that audit the observed $M$ size was $20$, the exact-match count was $0$, and the lower-tail count at or below the observed $M$ size was $7$.  Consequently
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
These numbers support the finite claim that the observed code-layer tile is not typical under the sampled shape and amino-acid-class-preserving null rows.  They do not promote the row beyond code readback.

\paragraph{Leave-one-code-out recovery.}
Across $23$ evaluated code tables and $63$ total targets, the leave-one-code-out audit reports a baseline hit rate of $0.19821739130434785$.  The hit rate above baseline is $0.9841269841269841$, the hit rate inside $M\setminus T$ is $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus boundary is $0.9841269841269841$.  By family, the hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\ plastid} &= 1.0,\\
\mathrm{candidate\ other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\ ciliate} &= 1.0.
\end{aligned}
$$
This row says that, for the supplied finite table family, local code-label perturbations are usually recovered by the module-and-boundary geometry.  It does not say that biological organisms compute this geometry, nor that the geometry is a universal law of genetic-code variation.

\paragraph{Leave-one-codon-out recovery.}
The leave-one-codon-out audit reports no anchors, $5$ boundary cases, $8$ high-closure-coverage cases, and $8$ predictable cases:
$$
\begin{aligned}
\mathrm{anchor\ count} &= 0,\\
\mathrm{boundary\ count} &= 5,\\
\mathrm{closure\ coverage\ high} &= 8,\\
\mathrm{predictable\ count} &= 8,\\
\mathrm{recovery\ rate} &= 1.0.
\end{aligned}
$$
The threshold comparison also returns $1.0$.  The mathematically relevant point is that every codon in the tested exceptional row is accounted for by the local predictable-or-boundary classification, even though the contact remains only a codon-to-label table.

\paragraph{Module classification.}
The module classifier partitions the thirteen codons of $R$ into six named finite modules:
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
The completeness audit gives $R$ size $13$, assigned count $13$, no extra module codons, no missing codons from the modules, and no unassigned codons.  Equivalently,
$$
\begin{aligned}
13 &= 2+1+4+1+2+3,\\
\mathrm{extra\ module\ codons} &= \varnothing,\\
\mathrm{missing\ from\ modules} &= \varnothing,\\
\mathrm{unassigned\ codons} &= \varnothing.
\end{aligned}
$$
This is the finite-row content of the claim: the leave-one-codon-out module classification is complete for the punctured thirteen-codon row, and each module has a predictable-or-boundary witness in the supplied audit.

\paragraph{Boundary of interpretation.}
The support condition is deliberately narrow.  The curated table supports codon-label readback and local split checks.  It cannot test translation realization, folded structure, structural order, physical admissibility, biological function, a universal mechanism, or the claim that DNA must obey the BEDC geometry as biological necessity.  Amino-acid label-sequence readback is not folded protein structure.  A local codon tile is not a global biological law.  Any later promotion across those boundaries requires a separate reality contact, such as a translation experiment, a structural assay, a physical-admissibility model with empirical calibration, or a function-level observation.  Without such a contact, the chapter records only the finite code-layer fact that the standard-code label table exposes a statistically non-generic local boundary-tile structure whose thirteen exceptional codons are completely classified by the six modules above.