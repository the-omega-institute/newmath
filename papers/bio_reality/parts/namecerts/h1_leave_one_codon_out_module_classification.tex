\subsection{Leave-One-Codon-Out Module Classification in the Window-Six Codon Cube}\label{sec:h1-leave-one-codon-out-module-classification}
\origin{ai}

\paragraph{Finite codon-coordinate record.}
This record concerns the standard-code codon table viewed through a six-bit codon display.  The biological contact is deliberately narrow: it is the curated table assigning codons to amino-acid or stop labels.  The coordinate object is the codon cube $Q_6$, and the tested distinctions are local tile faces, singleton label corners, and three-point label classes.  The readback is therefore a code-label readback, not an observation of translation dynamics, folded structure, thermodynamic admissibility, biological function, global mechanism, or universality.

The finite exceptional set $R$ has cardinality $13$.  In the verified row it is exactly
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same row identifies this set as a punctured-cube pattern with face-vector data
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the leave-one-codon-out packet is not built from a verbal codon grouping alone: it is a finite cube subconfiguration with thirteen named codons, sixteen one-dimensional incidences, and four two-dimensional tile faces.

\paragraph{Spectral and shape evidence.}
The associated spectral row reports
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic check is numerically stable at $-6.224354365258478\cdot 10^{-12}$, the same value recorded for the cubic-polynomial residual.  This supplies a finite numerical consistency check for the coordinate spectrum, but it does not by itself create a biological law.  It says that the chosen codon-coordinate construction has a reproducible spectral signature at the code-read layer.

The shape-significance experiment used $N=2000$ sampled comparators with seed $42$.  It required $57377$ distinct-sample attempts and incurred $55377$ duplicate retries.  There was $1$ exact shape match, giving $p_{\mathrm{exact}}=0.0005$, and $25$ samples had Jaccard similarity at least $0.9$, giving $p_{J\geq 0.9}=0.0125$.  A second amino-acid-class-preserving null model, also with $N=2000$ and seed $42$, recorded observed module size $20$, exact-match count $0$, lower-tail count $7$, exact-shape value $0.0$, and lower-tail value $0.0035$.  These values make the local code-coordinate pattern statistically nontrivial relative to the finite nulls supplied in the row, while leaving mechanism and structure claims outside the tested contact.

\paragraph{Leave-one-code and leave-one-codon behavior.}
The leave-one-code-out summary evaluated $23$ code tables and $63$ total targets.  The baseline hit rate was $0.19821739130434785$.  The hit rate above baseline was $0.9841269841269841$, the hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ together with the boundary was $0.9841269841269841$.  By family, the row records $5$ families: alternative yeast has hit rate $0.8333333333333334$, while bacterial/plastid, candidate-other, mitochondrial, and nuclear-ciliate each have hit rate $1.0$ above baseline.

The leave-one-codon-out row separates predictable recovery from boundary placement.  It records anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$.  The recovery-rate threshold entry is also $1.0$.  In plain terms, all recovered targets are accounted for by the finite predictable-or-boundary classification in the supplied row, but the evidence remains a classification over codon labels and local cube geometry.

\paragraph{Module classification.}
The module classification is complete over the thirteen codons of $R$.  The verified assignment is
$$
\begin{aligned}
AGR&=\{AGA,AGG\},\\
AR\_Lys&=\{AAA\},\\
CUN&=\{CUA,CUC,CUG,CUU\},\\
Ile\_Met&=\{AUA\},\\
SSN&=\{UCA,UUA\},\\
Stop\_Trp&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
The completeness counters are $R\_size=13$ and $assigned\_count=13$.  The lists of extra module codons, missing codons, and unassigned codons are all empty.  Consequently every codon in the thirteen-point exceptional set is placed in one of the named modules, and every listed module has at least one codon that is predictable or boundary-classified in the leave-one-codon-out test.

\paragraph{Scientific reading.}
The finite result is a local code-layer statement.  The six-bit codon coordinate exposes a stable boundary-tile organization for the listed standard-code labels, and the leave-one-codon-out module row closes the bookkeeping: no codon is left unclassified, no extra codon is introduced, and the module families match the thirteen-point set exactly.  The curated standard-code table is the contact that permits label readback.  It is not an experiment on ribosomal translation, not a structure assay, not a folding calculation, not a physical-admissibility test, not a function assay, and not a universality theorem.  Any future promotion beyond code labels would require a separate observation at the relevant biological layer.