\origin{ai}

\subsection{Leave-One-Codon-Out Module Classification}
\label{sec:h1-leave-one-codon-out-module-classification}

\paragraph{Finite carrier and contact.}
The object in this record is the six-bit codon cube $Q_6$ equipped with the standard-code label table as its only reality contact.  The carrier is read as a finite codon display, not as a mechanism.  The contact supplies codon-to-label readback at the code layer: a codon is assigned to an amino-acid label or to stop.  It does not supply translation realization, folded structure, physical admissibility, biological function, a universal mechanism, or any law forcing DNA to obey the displayed geometry.  All claims below are therefore finite-row claims about a local tile and its label boundary inside $Q_6$.

\paragraph{Distinguished boundary row.}
The distinguished row $R$ has cardinality $13$ and consists exactly of the codons
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same finite row is seen as a punctured-cube boundary with $f$-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the row is not merely a list of exceptional labels: it carries a local two-dimensional tile profile with thirteen vertices, sixteen edges, and four square faces.  The row is the complete population later classified into modules; no codon outside this list is part of the module audit.

\paragraph{Spectral scale and shape rarity.}
The associated finite spectral audit records
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\times 10^{-12}$, so the displayed numbers satisfy the intended finite spectral relation to numerical tolerance.  Against $2000$ sampled null rows, the exact shape match count is $1$, giving $p_{\mathrm{exact}}=0.0005$.  The high-overlap threshold $J\geq 0.9$ has $25$ matches, giving $p_{J\geq 0.9}=0.0125$.  The sampler needed $57377$ distinct-sample attempts and $55377$ duplicate retries, a useful warning that the finite state space is highly constrained under the sampling rule.  Under the amino-acid-class-preserving null model, the observed row size is $20$, the exact-shape count is $0$ in $2000$ samples, and the lower-tail count at or below the observed size is $7$, giving a lower-tail value $0.0035$.  These numbers support a local finite-row anomaly, not a promoted biological mechanism.

\paragraph{Leave-one-code and leave-one-codon rows.}
The leave-one-code-out audit evaluated $23$ code tables and $63$ total targets.  The baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M\setminus T$ is $0.9365079365079365$, and the rate inside $M\setminus T$ plus the boundary is $0.9841269841269841$.  In the leave-one-codon-out audit there are $0$ anchors, $5$ boundary codons, $8$ predictable codons, and $8$ high-coverage closures; the recovery rate is $1.0$, and the recovery-rate-above-threshold value is also $1.0$.  By family, the hit-rate-above-baseline values are $1.0$ for bacterial-plastid, mitochondrial, nuclear-ciliate, and candidate-other families, and $0.8333333333333334$ for alternative yeast, over $5$ families.

\paragraph{Module classification.}
The module classifier assigns all thirteen codons in $R$ and assigns no extra codons.  Equivalently,
$$
\begin{aligned}
R_{\mathrm{size}}&=13,\\
N_{\mathrm{assigned}}&=13,\\
N_{\mathrm{missing}}&=0,\\
N_{\mathrm{unassigned}}&=0,\\
N_{\mathrm{extra}}&=0.
\end{aligned}
$$
The module partition is
$$
\begin{aligned}
\mathrm{AGR}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{AR\_Lys}&=\{\mathrm{AAA}\},\\
\mathrm{CUN}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{Ile\_Met}&=\{\mathrm{AUA}\},\\
\mathrm{SSN}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{Stop\_Trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every listed module has predictable-or-boundary support: the two AGR codons, the single AR-Lys codon, the four CUN codons, the single Ile-Met codon, the two SSN codons, and the three Stop-Trp codons are all accounted for.  This is the chapter-specific finite-row conclusion: inside the six-bit codon display, the leave-one-codon-out boundary row admits a complete module classification, and each module is covered by the predictable-or-boundary test.

\paragraph{Scope boundary.}
The result is a code-read result only.  The standard-code contact tells us how the thirteen codons are labelled, and the finite audits show that this labelled row has a punctured-cube profile, a stable numerical spectrum, rare null-model shape behavior, strong leave-one-code-out performance, and complete leave-one-codon-out module coverage.  None of those facts imply translation realization, folding, structural order, physical admissibility, biological function, mechanism, or universality.  Crossing any of those boundaries requires a separate contact with the corresponding layer.  Without such a contact, amino-acid label readback remains label readback, and a local codon tile remains a local finite-row pattern.