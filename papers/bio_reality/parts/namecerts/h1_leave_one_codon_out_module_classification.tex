\subsection{Leave-one-codon-out module classification in the window-six codon cube}\label{sec:h1-leave-one-codon-out-module-classification}
\origin{ai}

\paragraph{Finite carrier and contact.}
The finite carrier is the six-bit codon display $Q_6$, read only through the curated standard-code label table.  The reality contact used here is a codon-label bridge: it states that the standard genetic code assigns each codon an amino-acid or stop label.  It does not state a translation mechanism, a folded structure, a physical admissibility condition, a biological function, or a universality law.  The local object tested in this row is therefore a code-read object: a finite window-six coordinate pattern whose readback is checked against curated labels.

\paragraph{Residual codon row.}
The distinguished residual set $R$ has cardinality $13$.  In the supplied finite row it is exactly
$$\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}$$
The punctured-cube check reports the same thirteen codons and the face data
$$\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}$$
Thus the row is not merely a count: it records a concrete local tile with $13$ vertices, $16$ one-dimensional faces, and $4$ two-dimensional faces inside the six-bit codon cube.

\paragraph{Spectral row.}
The associated spectral scalars separate the local $M$ and $R$ sides numerically.  The supplied values are
$$\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}$$
The radial polynomial residual is $-6.224354365258478\cdot 10^{-12}$, the same value reported for the cubic-polynomial check, so the observed spectral row is numerically closed to the displayed tolerance.  These numbers are used only as finite-row coordinates of the code-read tile; they do not promote the tile to a physical or functional law.

\paragraph{Shape significance and class-preserving null.}
Against the reported shape null with $N=2000$, the row has $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$.  The resulting probabilities are
$$\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9}&=0.0125.
\end{aligned}$$
The sampling trace reports $57377$ distinct-sample attempts and $55377$ duplicate retries under seed $42$.  Under the amino-acid-class-preserving null, the observed $M$ size is $20$, the exact match count is $0$, the exact-shape probability is $0.0$, and the lower-tail count at or below the observed $M$ size is $7$ out of $2000$, giving $p=0.0035$.  This supports the claim that the displayed code-read shape is atypical under the tested finite nulls, while still leaving all higher biological layers outside the claim.

\paragraph{Leave-one-code and leave-one-codon rows.}
The leave-one-code-out row evaluates $23$ code tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$, while the observed hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$.  By family, the hit-rate-above-baseline values are $1.0$ for bacterial-plastid, $1.0$ for mitochondrial, $1.0$ for nuclear-ciliate, $1.0$ for candidate-other, and $0.8333333333333334$ for alternative-yeast, across $5$ families.

For leave-one-codon-out recovery, the finite row reports anchor count $0$, boundary count $5$, closure coverage high $8$, predictable count $8$, and recovery rate $1.0$.  The recovery rate above threshold is also $1.0$.  This is a local label-recovery statement: the row says that the omitted codon labels are recovered by the displayed finite closure-or-boundary classification in the tested code-read setting.

\paragraph{Module classification.}
The module classification is complete on $R$.  The assigned count is $13$, equal to $R$ size $13$, with no extra module codons, no missing codons from the modules, and no unassigned codons.  The six modules are
$$\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}$$
Each module has at least one predictable-or-boundary witness in the supplied row.  Since these six displayed modules partition the thirteen listed residual codons, the classification supplies a finite local account of the leave-one-codon-out residual tile.

\paragraph{Boundary of the claim.}
The supported statement is deliberately narrow: window-six codon coordinates expose a local boundary-tile structure whose curated readback agrees with the standard-code label table in the reported finite tests.  The result does not claim that DNA obeys this geometry as biological necessity.  It does not claim translation realization, folded protein structure, structural order, physical admissibility, biological function, mechanism, or universality.  Amino-acid label-sequence readback remains a code-layer observation unless a separate higher-layer contact supplies structure, physics, function, or mechanism.  Within that boundary, the row is ready: the residual set has a complete $13$-codon module classification, the finite nulls give small reported probabilities, and the leave-one-codon-out recovery row closes at recovery rate $1.0$ for the tested local code-read task.