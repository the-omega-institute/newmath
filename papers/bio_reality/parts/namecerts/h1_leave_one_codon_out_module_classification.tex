\subsection{Leave-One-Codon-Out Module Classification in the Window-Six Codon Cube}\label{sec:bioreality-h1-leave-one-codon-out-module-classification}
\origin{ai}

\paragraph{Finite carrier and contact layer.}
This record treats the standard-code codon table as an external code-read contact and the six-bit codon display as the finite carrier $Q_6$.  The carrier is not used as a biological mechanism.  It is used only to ask whether the codon labels supplied by the curated standard-code bridge expose a local boundary-tile pattern.  The reality contact is therefore the finite table assigning each codon to an amino-acid or stop label, with resolution at codon-label readback.  The contact can test a code-layer split and a local codon tile, but it cannot test translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.

\paragraph{Observed residue set.}
The distinguished residue set $R$ has cardinality $13$.  The verified codon rows are
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same finite row was verified as a punctured-cube shape with face-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the object under discussion is a thirteen-codon local tile inside the codon cube, with sixteen one-dimensional adjacencies and four two-dimensional faces in the verified finite complex.  The row is not a full genetic-code model; it is the local residue on which the leave-one-codon-out classification is evaluated.

\paragraph{Spectral and null-model context.}
The accompanying finite spectrum gives
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The reported cubic, or radial, polynomial residual was $-6.224354365258478\times 10^{-12}$, so the numerical spectrum is internally consistent at the supplied tolerance.  The surrounding $M$-shape evidence used $N=2000$ null samples with seed $42$.  It required $57377$ distinct-sample attempts and $55377$ duplicate retries, producing $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$.  Hence the reported finite-row probabilities were
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9}&=0.0125.
\end{aligned}
$$
A separate amino-acid-class-preserving null model also used $N=2000$ and seed $42$.  It reported observed $M$-size $20$, exact-match count $0$, and lower-tail count $7$, giving
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These values support the claim that the local code-read shape is not typical under the supplied finite null rows.  They do not convert the tile into a biological law.

\paragraph{Leave-one-code-out evidence.}
Across the leave-one-code-out comparison, $23$ tables were evaluated against $63$ total targets.  The baseline hit rate was $0.19821739130434785$.  The hit rate above baseline was $0.9841269841269841$, the hit rate inside $M\setminus T$ was $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus the boundary was $0.9841269841269841$.  By family, the verified row count was $5$ families, with hit rates above baseline
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
This is evidence for a robust code-layer recovery pattern across the supplied family partition.  It remains a finite-table readback result: the rows count code-label agreement and local boundary placement, not biochemical implementation.

\paragraph{Leave-one-codon-out recovery.}
The leave-one-codon-out row reported $0$ anchors, $5$ boundary codons, $8$ high-coverage closure codons, and $8$ predictable codons.  The recovery rate was
$$
\begin{aligned}
\mathrm{recovery\ rate}=1.0,
\end{aligned}
$$
with the threshold comparison also reported as $1.0$.  In words, every codon left out of the thirteen-codon residue row was recovered either by predictability or by boundary status in the supplied finite procedure.  The absence of anchors is important: the recovery is not being read as a direct mechanistic anchoring in biological translation, but as a local closure-or-boundary classification inside $Q_6$ with labels supplied by the external table.

\paragraph{Module classification.}
The complete module classification assigns all $13$ codons of $R$ and nothing else.  The verified completion row is
$$
\begin{aligned}
R_{\mathrm{size}}&=13,\\
\mathrm{assigned\ count}&=13,\\
\mathrm{extra\ module\ codons}&=\varnothing,\\
\mathrm{missing\ from\ modules}&=\varnothing,\\
\mathrm{unassigned\ codons}&=\varnothing.
\end{aligned}
$$
The six finite modules are
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
The classification is therefore exact at the finite-row level: every codon in $R$ is assigned to exactly one named module, no codon outside the supplied module union is introduced, and no member of $R$ is left unclassified.  The module names are read as code-layer labels for the observed local split, not as claims about protein structure or cellular realization.

\paragraph{Boundary verdict.}
The finite data support the following chapter-specific statement.  In the six-bit codon display $Q_6$, the standard-code label table exposes a thirteen-codon punctured-cube residue with face-vector $(13,16,4)$, nontrivial spectral row values, significant finite null comparisons, high leave-one-code-out recovery, and a complete six-module leave-one-codon-out classification.  This is a ready code-read claim about a local boundary tile.  It is not a claim that DNA obeys this geometry as biological necessity.  It is not a claim that window-six geometry alone supplies translation, folded protein structure, physical admissibility, biological function, mechanism, or universality.  Any such higher-layer assertion would require a separate reality contact whose observed facts are not present in this finite-row record.