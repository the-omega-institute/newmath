\origin{ai}
\subsection{Leave-One-Codon-Out Module Classification}
\label{sec:h1-leave-one-codon-out-module-classification}

\paragraph{Finite codon display.}
This record studies a six-bit codon display as a finite code-read object. The biological contact is the curated standard-code table: it assigns each codon to an amino-acid or stop label, and it is used here only as a codon-label readback bridge. The internal object is the codon cube $Q_6$ with coordinate, closure, spectrum, and relation data. The finite distinguished relation $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The punctured-cube check returns the same thirteen codons, with face-vector data $(13,16,4)$. Thus the row is not merely a label list: it is a finite local tile with thirteen vertices, sixteen one-dimensional faces, and four two-dimensional faces inside the selected codon geometry.

\paragraph{Spectral and null-model scale.}
The associated spectral row gives $\lambda_M = 0.6752479308830213$, $\lambda_R = 0.4758352843197002$, and $\mu = 3.051487585298128$. The reported cubic, or radial, polynomial residual is $-6.224354365258478 \times 10^{-12}$, so the numerical spectral certificate is at machine-tolerance scale for the finite row. The shape-significance run used $N=2000$ samples, seed $42$, and $57377$ distinct-sample attempts with $55377$ duplicate retries. It found $1$ exact shape match and $25$ matches with Jaccard similarity at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9} &= 0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null model also used $N=2000$ and seed $42$. It reports observed $|M|=20$, exact-match count $0$, lower-tail count $7$, $p_{\mathrm{aa\ class\ exact}}=0.0$, and lower-tail value $0.0035$. These figures support the claim that the observed finite tile is not a typical draw under the stated row-level null models, while still leaving the labels themselves as external contacts supplied by the curated code table.

\paragraph{Leave-one-code-out readback.}
The leave-one-code-out row evaluates $23$ code tables and $63$ total targets. Its baseline hit rate is $0.19821739130434785$. The observed hit rate above baseline is $0.9841269841269841$, the hit rate in $M-T$ is $0.9365079365079365$, and the hit rate in $M-T$ plus boundary is again $0.9841269841269841$. At the family level, five families are recorded. The family hit-rate-above-baseline values are $0.8333333333333334$ for alternative yeast, and $1.0$ for bacterial plastid, candidate other, mitochondrial, and nuclear ciliate. These rows are code-read evidence: they say that the local finite structure is strongly aligned with curated label changes in the tested code-table family rows.

\paragraph{Leave-one-codon-out boundary row.}
The leave-one-codon-out row records $8$ predictable codons, $5$ boundary codons, $0$ anchors, high closure coverage $8$, and recovery rate $1.0$. The recovery-rate-above-threshold value is also $1.0$. In this row, every successful readback is therefore accounted for either by a predictable position or by a boundary position. The absence of anchors is important: this is not a claim that a hidden mechanistic anchor determines translation. It is a finite-row classification of local code-label behavior under single-codon removal.

\paragraph{Module classification.}
The module classification is complete. It assigns all $13$ codons in $R$, with $R$-size $13$, assigned count $13$, no extra module codons, no missing-from-modules entries, and no unassigned codons. The six modules are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA, AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA, CUC, CUG, CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA, UUA\},\\
\mathrm{stop\_trp} &= \{UAA, UAG, UGA\}.
\end{aligned}
$$
Each module has a predictable-or-boundary witness in the leave-one-codon-out sense. The singleton modules $\mathrm{ar\_lys}$ and $\mathrm{ile\_met}$ show that singleton label corners are part of the finite tile rather than accidental omissions. The four-codon $\mathrm{cun}$ block supplies the largest local face-like module. The $\mathrm{agr}$, $\mathrm{ssn}$, and $\mathrm{stop\_trp}$ modules record the remaining two- and three-point label classes in the selected relation.

\paragraph{Verdict and boundary.}
The finite row supports the chapter-specific statement that window-six codon coordinates expose a local boundary-tile structure whose thirteen-codon relation is completely partitioned by the six named modules, and whose leave-one-codon-out behavior is covered by predictable or boundary status. The curated standard-code table is the only reality contact used for labels. Therefore the result is a code-read result, not a biological mechanism claim. No translation realization, folded structure, physical admissibility, biological function, universal mechanism, or necessity law follows from this chapter unless a separate higher-layer contact supplies it. Amino-acid label-sequence readback is not folded protein structure, and a local codon tile is not a global biological law.