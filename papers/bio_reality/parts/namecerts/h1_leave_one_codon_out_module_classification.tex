\origin{ai}

\subsection{Leave-One-Codon-Out Module Classification in the Window-Six Codon Cube}
\label{sec:bioreality-h1-leave-one-codon-out-module-classification}

\paragraph{Finite claim.}
The claim recorded here is that the standard-code codon table, when read only as a curated codon-to-label table, supports a finite window-six classification of the leave-one-codon-out boundary set into named local modules.  The carrier is the codon cube $Q_6$, whose vertices encode codons by a six-bit display.  The relevant finite subset is the thirteen-codon row
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The verified cardinality row reports $|R|=13$, with the same thirteen labels listed above.  The punctured-cube row records the face data
$$
\begin{aligned}
f(R)=(13,16,4),
\end{aligned}
$$
so the object under discussion is not an unconstrained list of codons but a finite local tile with thirteen vertices, sixteen one-dimensional incidences, and four two-dimensional faces inside the six-bit codon display.

\paragraph{Module partition.}
The leave-one-codon-out module classification is complete on this row.  The verified module table assigns every codon of $R$ and introduces no codon outside $R$:
$$
\begin{aligned}
R_{\mathrm{size}}&=13, & \mathrm{assigned}&=13,\\
\mathrm{missing}&=\varnothing, & \mathrm{extra}&=\varnothing, & \mathrm{unassigned}&=\varnothing.
\end{aligned}
$$
The six module blocks are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
The classification row further records that each of these modules has at least one member witnessed as predictable or boundary-supported in the leave-one-codon-out probe.  Thus the finite result is not merely that thirteen codons can be named, but that the entire thirteen-codon tile is exhausted by six biologically interpretable local label modules while preserving the boundary status required by the probe.

\paragraph{Leave-one-codon-out evidence.}
The direct leave-one-codon-out row reports no anchors, five boundary cases, eight high-coverage closure cases, and eight predictable cases:
$$
\begin{aligned}
\mathrm{anchor\_count}&=0, & \mathrm{boundary\_count}&=5,\\
\mathrm{closure\_coverage\_high}&=8, & \mathrm{predictable\_count}&=8.
\end{aligned}
$$
The recovery rate is recorded as $1.0$, and the threshold comparison is also $1.0$.  In finite terms, the observation is that the removed-codon test does not need a hidden anchor class in this row: the thirteen codons are recovered through the predictable and boundary channels, and the six modules listed above cover all recovered codons without residue.

\paragraph{Code-table readback.}
The only empirical contact used here is the curated standard-code table, treated as a codon-label table.  It supplies the amino-acid or stop labels needed to interpret $AAA$, $AGA$, $AGG$, $AUA$, the $CUN$ block, $UCA$, $UUA$, and the stop-related codons $UAA$, $UAG$, $UGA$ at the code-reading layer.  This contact is sufficient for checking local label splits and local module names, but it is not a measurement of translation in a cell, not an observation of folded protein geometry, not a physical-admissibility experiment, and not a functional assay.  All conclusions in this subsection therefore remain at the codon-label level.

\paragraph{Spectral row.}
The same finite construction carries a numerical spectral signature.  The recorded spectral values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial polynomial residual is $-6.224354365258478\times 10^{-12}$, the same value reported for the cubic-polynomial check.  This near-zero residual is used only as a finite numerical consistency check for the local codon-tile construction.  It does not upgrade the code-table contact into a biological mechanism; it records that the finite coordinate object and its spectral calculation agree to the stated numerical tolerance.

\paragraph{Shape significance.}
Against the recorded null sampling row with $N=2000$ and seed $42$, the exact shape appeared once, giving
$$
\begin{aligned}
\mathrm{exact\_shape\_matches}&=1, & p_{\mathrm{exact}}&=0.0005.
\end{aligned}
$$
The high-overlap row counted $25$ samples with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
\mathrm{jaccard\_ge\_0.9\_matches}&=25, & p_{\mathrm{jaccard}\ge 0.9}&=0.0125.
\end{aligned}
$$
The run required $57377$ distinct-sample attempts and $55377$ duplicate retries, so the finite sampling space is visibly constrained by repeated draws.  A second null model preserving amino-acid class labels records $N=2000$, exact-match count $0$, observed $M$ size $20$, lower-tail count $7$, exact-shape $p$-value $0.0$, and lower-tail $p$-value $0.0035$.  These rows support the narrower statement that the observed local shape is uncommon under the stated finite nulls.  They do not establish that the window-six geometry is a necessary biological principle.

\paragraph{Related leave-one-code-out rows.}
The leave-one-code-out comparison over $23$ evaluated code tables and $63$ targets gives a baseline hit rate of $0.19821739130434785$.  The hit rate above baseline is $0.9841269841269841$, the hit rate in $M-T$ is $0.9365079365079365$, and the hit rate in $M-T$ plus boundary is $0.9841269841269841$.  By family, the five reported family hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\_yeast}&=0.8333333333333334,\\
\mathrm{bacterial\_plastid}&=1.0,\\
\mathrm{candidate\_other}&=1.0,\\
\mathrm{mitochondrial}&=1.0,\\
\mathrm{nuclear\_ciliate}&=1.0.
\end{aligned}
$$
These comparisons are included because they are finite rows connected to the same codon-tile boundary question.  Their role is corroborative: they show that the local boundary channel also performs well in a leave-one-code-out setting, but they do not replace the module-completion row for the thirteen-codon classification.

\paragraph{Scope boundary.}
The conclusion is therefore precise.  The standard-code label table, together with the finite window-six codon coordinates, supports a complete six-module classification of the thirteen-codon leave-one-codon-out row $R$.  The row has $13$ assigned codons, no missing codons, no extra module codons, no unassigned codons, and every module has predictable or boundary support.  The surrounding finite evidence supplies a punctured-cube face vector, spectral consistency values, low null-model frequencies, and leave-one-code-out hit rates.  None of these facts implies translation realization, folded structure, physical admissibility, biological function, a molecular mechanism, or universality.  Any such stronger statement would require a separate contact measuring that higher layer directly.