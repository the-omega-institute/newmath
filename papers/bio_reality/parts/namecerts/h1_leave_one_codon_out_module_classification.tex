\origin{ai}
\subsection{Leave-One-Codon-Out Module Classification for the Window-Six Codon Tile}
\label{sec:bior-h1-leave-one-codon-out-module-classification}

\paragraph{Claim.}
The finite observation concerns the standard-code codon table at the code-read layer. The carrier is the six-bit codon cube $Q_6$, equipped with codon-coordinate distinctions, local tile faces, singleton label corners, and three-point label classes. In this row, the relevant residual set $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The punctured-cube computation reports the same set and gives the face-vector summary $(13,16,4)$. Thus the finite object is not an unrestricted codon table; it is a specific local boundary tile inside the codon cube, read against curated standard-code labels.

\paragraph{Module partition.}
The leave-one-codon-out module row classifies all $13$ members of $R$ into six named modules, with no missing or extra codons. The complete assignment is
$$
\begin{aligned}
AGR &= \{AGA, AGG\},\\
AR\_Lys &= \{AAA\},\\
CUN &= \{CUA, CUC, CUG, CUU\},\\
Ile\_Met &= \{AUA\},\\
SSN &= \{UCA, UUA\},\\
Stop\_Trp &= \{UAA, UAG, UGA\}.
\end{aligned}
$$
The audit row records $R\_size=13$, $assigned\_count=13$, $extra\_module\_codons=\varnothing$, $missing\_from\_modules=\varnothing$, and $unassigned\_codons=\varnothing$. This is the main finite certificate for the claim: every residual codon in the local tile has been accounted for by a module name, and the module cover is neither overfull nor deficient.

\paragraph{Predictability and boundary status.}
The associated leave-one-codon-out probe reports $8$ predictable cases, $5$ boundary cases, $0$ anchor cases, and $8$ high-closure-coverage cases. Its recovery rate is $1.0$, with the row also recording $recovery\_rate\_above\_threshold=1.0$. The module-level statement is therefore not merely that the codons can be listed; it is that each listed module lands in the predictable-or-boundary side of the local test. In particular, the six modules above exhaust the punctured cube and none of their codons are left outside the classification.

\paragraph{Code-table comparison.}
The leave-one-code-out row evaluates $23$ tables and $63$ total targets. It reports a baseline hit rate of $0.19821739130434785$, a hit rate in $M-T$ of $0.9365079365079365$, and a hit rate in $M-T$ plus boundary of $0.9841269841269841$. The above-baseline hit rate is also $0.9841269841269841$. At the family level, $5$ families are recorded: alternative yeast has hit rate $0.8333333333333334$, while bacterial plastid, candidate other, mitochondrial, and nuclear ciliate each have hit rate $1.0$. These numbers support a code-layer regularity statement: the local tile and its boundary extension recover a high fraction of the curated label changes under the finite leave-one-code-out tests.

\paragraph{Spectral and null comparisons.}
The spectral row gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$. The radial cubic check is numerically near zero, with both $cubic\_polynomial\_satisfied$ and $radial\_polynomial\_value$ equal to $-6.224354365258478\times 10^{-12}$, while the reported spectral-radius value in tolerance is $0.6752479308830213$. The shape-significance row uses $N=2000$ null samples with seed $42$; it records $1$ exact shape match and $25$ matches at Jaccard threshold $0.9$, giving $p\_{exact}=0.0005$ and $p\_{Jaccard\ge 0.9}=0.0125$. The amino-acid-class-preserving null row also uses $N=2000$ with seed $42$ and observed $M$ size $20$; it reports $0$ exact matches, $7$ lower-tail samples at or below the observed size, $p=0.0$ for exact-shape recovery, and lower-tail probability $0.0035$. These finite null rows make the local tile non-generic relative to the tested randomizations, but they do not upgrade the claim beyond the code-read layer.

\paragraph{Reality contact.}
The only external contact used here is the curated standard-code table, understood as a codon-to-label source. It supports statements of the form: this codon is assigned to an amino-acid label or stop label in the curated table, and the local codon-coordinate construction can be checked against those labels. It does not by itself observe ribosomal translation, molecular folding, energetic admissibility, organismal fitness, biochemical function, or a universal mechanism. The correct reading is therefore narrow: window-six coordinates expose a finite local boundary pattern in the standard-code label table, and the module classification accounts for the residual codons in that pattern.

\paragraph{Boundary.}
No claim of translation realization follows from this chapter. No folded protein structure, structural ordering principle, physical admissibility statement, biological function, mechanism, or universality claim follows from the codon-label readback. Any such assertion would require a separate contact at the appropriate layer, for example an experimental translation record, a structure contact, a physical assay, or a functional measurement. Without that additional contact, the present row remains a finite code-layer classification of the standard-code labels on a local codon tile.