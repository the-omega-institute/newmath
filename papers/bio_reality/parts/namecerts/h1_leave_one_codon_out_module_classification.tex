\subsection{Leave-One-Codon-Out Module Classification}\label{sec:bioreality-h1-leave-one-codon-out-module-classification}
\origin{ai}

\paragraph{Finite codon display.}
This record treats the standard genetic-code table as a finite code-layer contact and asks how a window-six codon coordinate separates a local boundary tile. The carrier is the six-bit codon cube $Q_6$, read only as a finite display for codons and labels. The observed residual set $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same finite row reports the punctured-cube face counts $(13,16,4)$ for this residual tile. Thus the object under discussion is not an unconstrained biological process but a specific finite subcomplex inside the codon cube, equipped with a curated standard-code label table as its code-read contact.

\paragraph{Spectral and shape evidence.}
The associated spectral summary gives
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128,
\end{aligned}
$$
with the reported cubic or radial polynomial residual equal to $-6.224354365258478\times 10^{-12}$. In the finite randomization check with $N=2000$, the exact shape count was $1$, giving $p=0.0005$, and the high-Jaccard count at threshold $0.9$ was $25$, giving $p=0.0125$. The run required $57377$ distinct-sample attempts and recorded $55377$ duplicate retries, so the finite sampling row should be read as a constrained comparison rather than as a claim of exhaustive biological necessity. Under the amino-acid-class-preserving null with $N=2000$, the observed size was $20$, the exact-match count was $0$, and the lower-tail count at or below the observed size was $7$, giving lower-tail probability $0.0035$ and exact-shape probability $0.0$ in the sampled run.

\paragraph{Leave-one evidence.}
For leave-one-code-out evaluation, $23$ tables and $63$ total targets were evaluated. The baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$. The hit rate inside $M-T$ was $0.9365079365079365$, and the rate inside $M-T$ plus boundary was again $0.9841269841269841$. The leave-one-codon-out row gives anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$. These finite numbers support a local classification claim: codons omitted from the standard-code readback can be sorted by predictable closure behavior or by boundary membership in the displayed tile.

\paragraph{Module classification.}
The module row is complete on $R$: it reports $R\_\mathrm{size}=13$, assigned count $13$, no extra module codons, no missing codons, and no unassigned codons. The six reported modules are
$$
\begin{aligned}
\mathrm{AGR}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{AR\_Lys}&=\{\mathrm{AAA}\},\\
\mathrm{CUN}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{Ile\_Met}&=\{\mathrm{AUA}\},\\
\mathrm{SSN}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{Stop\_Trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Each of these modules contains at least one codon classified as predictable or boundary-supported in the leave-one-codon-out analysis. The conclusion is therefore finite and row-specific: the residual codons in $R$ are fully assigned to named local modules, and every named module touches the predictable-or-boundary side of the window-six test.

\paragraph{Family-level context.}
The leave-one-code-out family summary contains $5$ families. The hit-rate-above-baseline values are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate. These family rows are useful as finite comparative support for the code-read layer, but they do not change the scope of the chapter. They show that the same local codon-tile signal is repeatedly visible in the supplied table comparisons; they do not by themselves identify a translation mechanism or a physical constraint.

\paragraph{Scope of the claim.}
The contact used here is a curated standard-code assignment of codons to amino-acid or stop labels. That contact can test codon-label readback and local label splitting. It cannot test translation realization, folded structure, structural order, physical admissibility, biological function, or a universal mechanism. Amino-acid label sequences are therefore not treated as protein structures. The local tile is a code-layer pattern in a finite coordinate display; any promotion to translation, folding, physical chemistry, function, mechanism, or universality requires an additional contact that directly observes that higher layer.

\paragraph{Verdict.}
The finite row for \texttt{h1.leave\_one\_codon\_out.module\_classification} is ready as a BioReality chapter seed. Its positive content is the complete assignment of the $13$ residual codons into six modules, with every module represented on the predictable-or-boundary side of the leave-one-codon-out test. Its negative content is equally important: the curated code table supplies label readback only, so the result remains a local code-layer classification and does not assert folding, function, physical admissibility, translation realization, biological mechanism, or global law.