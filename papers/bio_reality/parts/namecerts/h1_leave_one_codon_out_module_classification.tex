\subsection{Leave-One-Codon-Out Module Classification in the Window-Six Codon Cube}
\label{sec:h1-leave-one-codon-out-module-classification}
\origin{ai}

\paragraph{Finite carrier and observed row.}
The finite carrier is the six-bit codon cube $Q_6$, read only as a codon-coordinate display equipped with an external standard-code label table.  The distinguished finite row is the set $R$ of thirteen codons
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The cardinality check gives $|R|=13$.  The same row is certified as a punctured-cube pattern with face-vector $(13,16,4)$: thirteen vertices, sixteen local edges, and four two-dimensional faces inside the induced cubical tile.  This is a code-read object, not a biological mechanism.  Its only permitted readback is the comparison between window-six coordinates, local tile faces, singleton label corners, three-point label classes, and the curated standard-code codon labels.

\paragraph{Spectral coordinate.}
The finite-row spectrum separates the row $R$ from the ambient module $M$ by scalar invariants.  The recorded values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\cdot 10^{-12}$, the same value recorded for the cubic-polynomial check, so the numerical spectrum is within the stated tolerance for the finite computation.  These numbers are used only as coordinate evidence for the local row geometry.  They do not assert that biological translation, folded structure, physical admissibility, function, mechanism, or universality follows from the cube coordinate.

\paragraph{Shape significance against finite null rows.}
The shape-significance probe sampled $N=2000$ distinct null rows with seed $42$.  Because duplicates were rejected, the run made $57377$ distinct-sample attempts and incurred $55377$ duplicate retries.  Among the accepted null rows, the exact observed shape occurred once, giving
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005.
\end{aligned}
$$
The high-Jaccard threshold test found $25$ rows with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{J\geq 0.9}&=0.0125.
\end{aligned}
$$
A second null model preserving amino-acid class recorded observed module size $|M|=20$, exact-match count $0$ out of $N=2000$, and lower-tail count $7$.  Hence the amino-acid-class-preserving exact-shape value is $0.0$, while the lower-tail value is $0.0035$.  These finite-row statistics support the claim that the observed local tile is not a generic row under the tested nulls.  They still do not promote the tile to a global law of molecular biology.

\paragraph{Leave-one-code-out contact.}
The leave-one-code-out probe evaluated $23$ code tables and $63$ total targets.  The baseline hit rate was $0.19821739130434785$.  The hit rate inside $M\setminus T$ was $0.9365079365079365$, while the hit rate inside $M\setminus T$ plus the local boundary was $0.9841269841269841$.  The recorded hit rate above baseline was also $0.9841269841269841$.  By family, the hit-rate-above-baseline values were $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial-plastid, $1.0$ for candidate-other, $1.0$ for mitochondrial, and $1.0$ for nuclear-ciliate families, across $5$ families.  This contact is read as code-layer evidence that the window-six row has predictive local boundary content for curated label changes.

\paragraph{Leave-one-codon-out module partition.}
The leave-one-codon-out probe records $0$ anchor codons, $5$ boundary codons, $8$ high-closure-coverage codons, and $8$ predictable codons, with recovery rate $1.0$ and recovery-rate-above-threshold $1.0$.  The module classification is complete: $R$ has size $13$, all $13$ codons are assigned, and there are no extra module codons, no missing codons, and no unassigned codons.  The finite partition is
$$
\begin{aligned}
\mathrm{AGR} &= \{AGA, AGG\},\\
\mathrm{AR/Lys} &= \{AAA\},\\
\mathrm{CUN} &= \{CUA, CUC, CUG, CUU\},\\
\mathrm{Ile/Met} &= \{AUA\},\\
\mathrm{SSN} &= \{UCA, UUA\},\\
\mathrm{Stop/Trp} &= \{UAA, UAG, UGA\}.
\end{aligned}
$$
Every displayed module has at least one codon certified as predictable or boundary-supported by the finite probe.  The result is therefore not merely a list of thirteen codons; it is a complete local-module decomposition of the punctured row $R$ inside the six-bit display.

\paragraph{Reality contact and cannot-claim boundary.}
The only reality contact used here is the curated standard-code table, treated as an external bridge that assigns codons to amino-acid or stop labels.  That contact can test code-read agreement and local codon-tile label splits.  It cannot test translation realization, protein folding, structural order, physical admissibility, biological function, mechanism, or universality.  Consequently the local result is this: window-six codon coordinates expose a finite boundary-tile structure whose row $R$ has size $13$, face-vector $(13,16,4)$, significant null-model shape statistics, stable spectral coordinates, high leave-one-code-out recovery, and a complete leave-one-codon-out module classification.  The result stops at code-layer label readback.  Any claim about translation, folding, physical admissibility, function, mechanism, or universality requires a separate higher-layer contact not supplied by this finite row.