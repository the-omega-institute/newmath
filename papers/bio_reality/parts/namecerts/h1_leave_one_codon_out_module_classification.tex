\origin{ai}
\subsection{Leave-One-Codon-Out Module Classification}
\label{sec:h1-leave-one-codon-out-module-classification}

\paragraph{Finite carrier and code-layer contact.}
The finite carrier is the six-bit codon cube $Q_6$, read through the standard-code codon table as an external code-layer contact.  The contact is only a codon-to-label table: it assigns each codon to an amino-acid label or to stop, and it gives no independent observation of translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.  The row under study is therefore a code-read statement about local tile structure in $Q_6$, not a biological necessity theorem.

\paragraph{Distinguished residual set.}
The residual codon set $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
As a punctured-cube object, this same set has face-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the row is not merely a loose list of exceptional labels: it is a finite local subcomplex with $13$ vertices, $16$ one-faces, and $4$ two-faces inside the codon cube display.

\paragraph{Spectral scale.}
The associated finite operator data separate the module side from the residual side.  The reported values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic check returns residual value $-6.224354365258478\cdot 10^{-12}$, so the polynomial constraint is satisfied to numerical tolerance at the stated scale.  The numerical point is not used as a mechanistic biological law; it is used only as finite-row evidence that the local code-label geometry has a stable spectral readback.

\paragraph{Shape significance against finite nulls.}
Against the shape null with $N=2000$ samples, the exact shape was matched once, giving
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}
$$
The high-overlap threshold $J\geq 0.9$ was met $25$ times, giving
$$
\begin{aligned}
p_{J\geq 0.9}=0.0125.
\end{aligned}
$$
The sampler required $57377$ distinct-sample attempts and recorded $55377$ duplicate retries under seed $42$.  Under the amino-acid-class-preserving null with $N=2000$, the observed module size was $20$, the exact-shape count was $0$, and the lower-tail count at or below the observed module size was $7$.  Hence
$$
\begin{aligned}
p_{\mathrm{aa\ class,exact}}&=0.0,\\
p_{\mathrm{aa\ class,lower\ tail}}&=0.0035.
\end{aligned}
$$
These figures support a finite local anomaly at the code-read layer.  They do not license promotion to translation, folding, physical admissibility, function, mechanism, or universality.

\paragraph{Leave-one-code-out behavior.}
For the leave-one-code-out test, $23$ code tables and $63$ total targets were evaluated.  The baseline hit rate was $0.19821739130434785$, while the observed hit rate above baseline was $0.9841269841269841$.  Inside $M-T$ the hit rate was $0.9365079365079365$, and inside $M-T$ plus boundary it was $0.9841269841269841$.  The family-resolved table has $5$ families: alternative yeast has hit rate $0.8333333333333334$, while bacterial plastid, candidate other, mitochondrial, and nuclear ciliate each have hit rate $1.0$ above baseline.  This says that the local boundary readback is robust across the finite table family partition supplied in the row, still only at the label-table level.

\paragraph{Leave-one-codon-out classification.}
For the leave-one-codon-out test, the anchor count is $0$, the boundary count is $5$, the high-closure-coverage count is $8$, and the predictable count is $8$.  The recovery rate is
$$
\begin{aligned}
\rho=1.0.
\end{aligned}
$$
The module classification is complete: $R$ has size $13$, the assigned count is $13$, and the lists of extra module codons, missing module codons, and unassigned codons are all empty.  The six finite modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every module has a predictable-or-boundary witness in the finite row.  The classification therefore exhausts the residual set without surplus and without omission.

\paragraph{Local verdict and boundary.}
The row supports the following code-read statement: in the six-bit codon display, the standard-code label table exposes a finite local boundary-tile organization on the $13$-codon residual set, and the leave-one-codon-out modules classify that residual set completely.  The evidence combines coordinate structure, closure behavior, spectral scale, null-model rarity, leave-one-code-out recovery, leave-one-codon-out recovery, and complete module assignment.  The cannot-claim boundary is part of the statement: no translation process, folded protein structure, physical admissibility, biological function, universal mechanism, or global biological law follows from this row unless a separate higher-layer reality contact supplies that observation.