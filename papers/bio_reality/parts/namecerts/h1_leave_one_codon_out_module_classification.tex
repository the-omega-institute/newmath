\origin{ai}

\subsection{Leave-one-codon-out module classification for the window-six codon tile}
\label{sec:bior-reality-leave-one-codon-module-classification}

\paragraph{Finite carrier and readback contact.}
This record treats the standard-code codon table as a finite code-layer contact, not as a mechanism.  The BEDC-minimal carrier is the six-bit codon cube $Q_6$, with distinctions given by the six-bit codon display, local tile faces, singleton label corners, and three-point label classes.  Its internal structure is coordinate, closure, spectrum, and relation; its readback is the standard-code label table.  The external contact used here is only the curated standard-code table: it assigns codons to amino-acid or stop labels at codon-label resolution.  Therefore the observable layer is $\mathrm{code\_read}$.

\paragraph{The punctured relation row.}
The finite relation row $R$ has cardinality $13$.  The listed codons are
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row is recorded as a punctured-cube object with $f$-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the local tile is not an informal sketch: it is a finite row with $13$ vertices, $16$ one-dimensional incidences, and $4$ two-dimensional faces under the supplied cube readback.

\paragraph{Spectral finite-row check.}
The spectral row gives
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\times 10^{-12}$, and the same value is reported for the cubic-polynomial check.  At this finite precision the spectrum is internally coherent with the recorded row, since the residual is numerically negligible relative to the displayed coordinates.  This is a coordinate and closure diagnostic; it is not a biological law.

\paragraph{Shape significance and null controls.}
Against the sampled shape null with $N=2000$ and seed $42$, the run made $57377$ distinct-sample attempts and incurred $55377$ duplicate retries.  It found $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9}&=0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null also used $N=2000$ and seed $42$.  It records observed $M$-size $20$, exact-match count $0$, and lower-tail count $7$ at or below the observed $M$-size.  The corresponding values are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These figures support the claim that the observed window-six row is not a typical draw under the two supplied finite nulls.  They do not identify a causal translation process.

\paragraph{Leave-one-code and leave-one-codon readback.}
The leave-one-code-out row evaluated $23$ tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$.  By family, the above-baseline hit rates are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial or plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate, across $5$ families.

For the leave-one-codon-out row, the anchor count is $0$, the boundary count is $5$, the predictable count is $8$, and the high-coverage closure count is $8$.  The recovery rate is
$$
\begin{aligned}
\mathrm{recovery\ rate}=1.0,
\end{aligned}
$$
with the above-threshold recovery rate also $1.0$.  This says that the finite codon-label readback is captured by predictable or boundary positions in the supplied local tile, not that the codons are physically realized by the tile.

\paragraph{Module classification.}
The module classification is complete on the $13$ codons of $R$: the recorded $R$-size is $13$, the assigned count is $13$, and the lists of extra module codons, missing codons, and unassigned codons are all empty.  The six modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Each module has predictable or boundary support under the supplied row.  The singleton modules $\mathrm{ar\_lys}$ and $\mathrm{ile\_met}$ mark label-corner behavior, while $\mathrm{agr}$, $\mathrm{cun}$, $\mathrm{ssn}$, and $\mathrm{stop\_trp}$ express multi-codon label-class or boundary behavior inside the same finite relation.

\paragraph{Local verdict.}
The ready claim is narrow.  Window-six codon coordinates expose a local boundary-tile structure for the standard-code label table, and the leave-one-codon-out classification assigns every one of the $13$ relation codons to one of the six recorded modules with no extras, omissions, or unassigned rows.  The numerical evidence combines the punctured-cube row, the spectral residual, the sampled null comparisons, the leave-one-code-out hit rates, and the module-complete leave-one-codon-out row.

\paragraph{Cannot-claim boundary.}
Nothing in this contact supplies translation realization, folded structure, structural order, physical admissibility, biological function, a universal mechanism, or a proof that DNA obeys BEDC geometry as a biological necessity.  Amino-acid label-sequence readback is not folded protein structure.  A local codon tile is not a global biological law.  Any promotion from $\mathrm{code\_read}$ to translation, folding, physical admissibility, function, mechanism, or universality requires a separate reality contact that directly observes that higher layer.