\subsection{Leave-One-Codon-Out Module Classification}\label{sec:h1-leave-one-codon-out-module-classification}
\origin{ai}

\paragraph{Finite carrier and reality contact.}
The finite carrier for this record is the six-bit codon cube $Q_6$, read through the standard-code codon-label table. The table is used only as a curated code-layer contact: it assigns each codon to an amino-acid or stop label, and it does not supply a translation mechanism, a folding experiment, a physical-admissibility criterion, a biological-function assay, or a universality theorem. The distinguished leave-one-codon-out residue set has cardinality
$$
\begin{aligned}
|R| &= 13.
\end{aligned}
$$
The thirteen codons are
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
As a finite row check, the same set is reported as a punctured-cube object with face vector
$$
\begin{aligned}
f(R) &= (13,16,4).
\end{aligned}
$$
Thus the row object is not merely a list of exceptional codons: it carries a local tile boundary shape inside the window-six coordinate display.

\paragraph{Spectral and null-model row evidence.}
The spectral row associated to the local tile reports
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial polynomial residual is numerically small,
$$
\begin{aligned}
P_{\mathrm{radial}} &= -6.224354365258478\times 10^{-12},
\end{aligned}
$$
and the same value is recorded for the cubic polynomial residual. The spectral-radius row records $0.6752479308830213$ within tolerance. These numbers support a finite coordinate-and-spectrum description of the code-layer tile; they do not turn the tile into a global biological law.

The shape row was tested against $N=2000$ finite samples with seed $42$. Among $57377$ distinct-sample attempts, $55377$ duplicate retries occurred, one exact shape match was observed, and $25$ samples had Jaccard similarity at least $0.9$. The reported probabilities are
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9} &= 0.0125.
\end{aligned}
$$
An amino-acid-class-preserving null row with $N=2000$ and seed $42$ recorded observed $|M|=20$, zero exact matches, and seven samples in the lower tail at or below the observed $M$ size. Its reported values are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
These rows give finite statistical pressure toward a non-generic local shape in the code readback. They still remain code-layer rows because the only supplied external contact is the curated codon-label table.

\paragraph{Leave-one-code and leave-one-codon recovery rows.}
The leave-one-code-out row evaluated $23$ tables and $63$ total targets. Its baseline hit rate is
$$
\begin{aligned}
h_{\mathrm{baseline}} &= 0.19821739130434785,
\end{aligned}
$$
while the hit rate above baseline is
$$
\begin{aligned}
h_{\mathrm{above\ baseline}} &= 0.9841269841269841.
\end{aligned}
$$
The hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$. Family-wise leave-one-code-out rows report five families. The family hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate.

The leave-one-codon-out row records no anchors, five boundary cases, eight high-coverage closure cases, and eight predictable cases:
$$
\begin{aligned}
\#\mathrm{anchors} &= 0,\\
\#\mathrm{boundary} &= 5,\\
\#\mathrm{high\ closure\ coverage} &= 8,\\
\#\mathrm{predictable} &= 8.
\end{aligned}
$$
The recovery rate is $1.0$, and the recovery rate above threshold is also $1.0$. This is the local recovery fact used by the module classification: every classified residue module must be visible either as predictable or as boundary-supported inside the finite window-six row.

\paragraph{Module classification.}
The module classification is complete on the thirteen-codon residue set. The assigned count is $13$, the reported $R$ size is $13$, and the lists of extra module codons, missing module codons, and unassigned codons are all empty:
$$
\begin{aligned}
R_{\mathrm{size}} &= 13,\\
\#\mathrm{assigned} &= 13,\\
\#\mathrm{extra} &= 0,\\
\#\mathrm{missing} &= 0,\\
\#\mathrm{unassigned} &= 0.
\end{aligned}
$$
The six module rows are
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
These six rows partition the thirteen listed codons, with no leftover codon and no codon outside $R$ imported into the module table. In particular, the four-codon $\mathrm{cun}$ row is the largest local face-like block, the $\mathrm{stop\_trp}$ row supplies a three-point stop-to-tryptophan boundary class, and the singleton rows $\mathrm{ar\_lys}$ and $\mathrm{ile\_met}$ mark label-corner behavior. The two-point $\mathrm{agr}$ and $\mathrm{ssn}$ rows complete the local boundary inventory. The classification therefore supports the finite claim that every codon in the leave-one-codon-out residue set belongs to a module that is either predictable or boundary-supported.

\paragraph{Boundary of the claim.}
The claim proven by these rows is a code-read claim about a finite window-six codon display with curated standard-code labels. It says that the thirteen-codon residue set has a complete six-module classification, that the same set carries a punctured-cube face vector $(13,16,4)$, and that the observed recovery and null-model rows are strong for this finite code-layer pattern. It does not claim that DNA obeys this geometry as a biological necessity. It does not claim translation realization, folded structure, physical admissibility, biological function, universal mechanism, or cross-organism law. Amino-acid label-sequence readback remains label readback. A separate higher-layer contact would be required before any statement about translation, folding, physical realization, function, mechanism, or universality could be made.