\subsection{Leave-One-Codon-Out Module Classification}\label{subsec:h1-leave-one-codon-out-module-classification}
\origin{ai}

\paragraph{Finite carrier and reality contact.}
This finite-row record studies the standard-code codon table through the six-bit codon display $Q_6$. The biological contact is deliberately narrow: a curated standard genetic-code table assigns each codon to an amino-acid or stop label. This contact supports code-layer readback and local split tests, but it is not a mechanism for translation, not a protein-structure observation, not a physical-admissibility test, and not a function assay. The carrier therefore remains a codon-label table with BEDC coordinates, closure, spectrum, and relation data, rather than a claim about molecular realization.

\paragraph{The distinguished relation row.}
The verified relation row $R$ has cardinality $13$. The codons in the row are
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The punctured-cube check returns the same thirteen codons and the face-vector signature
$$
\begin{aligned}
f(R)=(13,16,4).
\end{aligned}
$$
This is the finite local tile whose boundary behavior is being read back against the curated code labels. The row should be read as a local codon-window pattern, not as a global biological law.

\paragraph{Spectral and null-model scale.}
The spectral computation gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$. The radial cubic residual is $-6.224354365258478\times 10^{-12}$, so the numerical polynomial check is at numerical zero at the reported tolerance. Against the shape null with $N=2000$ samples and seed $42$, there was $1$ exact shape match after $57377$ distinct-sample attempts with $55377$ duplicate retries, giving
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{J\geq 0.9}&=0.0125,
\end{aligned}
$$
with $25$ high-Jaccard matches. Under the amino-acid-class-preserving null, again with $N=2000$ and seed $42$, the observed $M$ size is $20$, the exact-match count is $0$, and the lower-tail count at or below the observed $M$ size is $7$. Thus
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These values support that the finite shape is not typical under the reported null procedures, while still leaving the contact at the code-read layer.

\paragraph{Leave-one-code and leave-one-codon behavior.}
The leave-one-code-out probe evaluated $23$ tables and $63$ total targets. Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$. The hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus the boundary is $0.9841269841269841$. By family, the hit-rate-above-baseline values are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial/plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate, across $5$ families. The leave-one-codon-out row has anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$, with the recovery-rate-above-threshold value also $1.0$.

\paragraph{Module classification.}
The module classification is complete: $R$ has size $13$, the assigned count is $13$, and there are no extra module codons, no codons missing from modules, and no unassigned codons. The six modules are
$$
\begin{aligned}
\mathrm{AGR}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{AR\_LYS}&=\{\mathrm{AAA}\},\\
\mathrm{CUN}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ILE\_MET}&=\{\mathrm{AUA}\},\\
\mathrm{SSN}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{STOP\_TRP}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every module has predictable-or-boundary support in the finite report. The classification therefore accounts for all thirteen codons of $R$ without residue. This is the core positive claim of the row: the local window-six tile admits a complete module decomposition aligned with the curated standard-code label table.

\paragraph{Cannot-claim boundary.}
The result does not say that DNA obeys BEDC geometry as a biological necessity. It does not establish translation realization, folded protein structure, structural order, physical admissibility, biological function, a universal mechanism, or code universality. It also does not infer protein function from the codon-tile split. The curated table supplies codon-to-label readback only. Any claim that crosses from code labels to translation, folding, physical admissibility, function, mechanism, or universality requires a separate reality contact with its own finite-row support.

\paragraph{Local verdict.}
For claim $\mathrm{h1.leave\_one\_codon\_out.module\_classification}$, the finite data are internally aligned: the thirteen-codon relation row is explicitly listed, the punctured-cube face vector is reported, the spectral residual is numerically negligible, the null comparisons are small in the reported tails, and the module partition assigns all thirteen codons with no missing or extra entries. The appropriate reading is a code-layer, local-boundary classification of a curated standard-code table, not a higher-layer biological mechanism.