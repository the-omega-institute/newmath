\subsection{Leave-One-Codon-Out Module Classification in the Window-Six Codon Cube}\label{subsec:bioreality-codon-module-classification}
\origin{ai}

\paragraph{Finite carrier and contact layer.}
This record concerns the standard-code codon table as a code-read contact on the six-bit codon cube $Q_6$.  The carrier is not a model of translation, folding, physical admissibility, function, mechanism, or biological universality.  It is a finite coordinate display in which codons are read as cube vertices and curated amino-acid or stop labels are supplied by the external table contact.  The local distinctions used here are the six-bit codon display, local tile faces, singleton label corners, and three-point label classes.  The internal BEDC structure is coordinate, closure, spectrum, and relation; the readback is the standard-code label table.

\paragraph{Observed residual set.}
The finite residual row has cardinality $13$.  The observed set is
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same row is certified as a punctured-cube shape with face-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the leave-one-codon-out problem is not being summarized by a vague enrichment statement: all thirteen residual codons are explicitly named, and the local cubical boundary data have finite counts.

\paragraph{Spectral and shape checks.}
The spectral row gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The reported cubic, equivalently radial, polynomial residual is $-6.224354365258478\times 10^{-12}$, so the numerical spectral equation is satisfied to the recorded tolerance.  The shape-significance row used $N=2000$ null samples with seed $42$.  It found $1$ exact shape match, giving
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005,
\end{aligned}
$$
and $25$ samples with Jaccard similarity at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{Jaccard}\geq 0.9}=0.0125.
\end{aligned}
$$
The sampling process required $57377$ distinct-sample attempts and encountered $55377$ duplicate retries.  These numbers should be read as finite-row evidence for a specific local shape, not as a mechanism for how biological coding arose.

\paragraph{Amino-acid-class preserving null.}
A second null row preserves amino-acid class and again uses $N=2000$ with seed $42$.  The observed $M$ size is $20$.  The row reports $0$ exact matches, hence
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact\ shape}}=0.0,
\end{aligned}
$$
and $7$ lower-tail samples at or below the observed $M$ size, hence
$$
\begin{aligned}
p_{\mathrm{aa\ class\ lower\ tail}}=0.0035.
\end{aligned}
$$
This gives a sharper finite-row separation from the amino-acid-class preserving null, while still remaining a code-layer statement about codon-label readback.

\paragraph{Leave-one-code and leave-one-codon readback.}
Across $23$ evaluated code tables, the leave-one-code-out row reports $63$ total targets.  The baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M\setminus T$ is $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus boundary is $0.9841269841269841$.  The leave-one-codon-out row records $0$ anchors, $5$ boundary cases, $8$ high-coverage closure cases, $8$ predictable cases, and recovery rate $1.0$.  In finite form,
$$
\begin{aligned}
\mathrm{anchors}&=0, & \mathrm{boundary}&=5, & \mathrm{predictable}&=8, & \mathrm{recovery}&=1.0.
\end{aligned}
$$
These data support the narrower statement that the window-six geometry detects local codon-label boundary structure in the curated code table.

\paragraph{Family-level row.}
The leave-one-code-out family row has $5$ families.  The family hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast}&=0.8333333333333334,\\
\mathrm{bacterial\ plastid}&=1.0,\\
\mathrm{candidate\ other}&=1.0,\\
\mathrm{mitochondrial}&=1.0,\\
\mathrm{nuclear\ ciliate}&=1.0.
\end{aligned}
$$
This family row is useful because it prevents the local tile claim from being read as a single-table anecdote.  At the same time, it does not turn the tile into a universal biological law; it remains a finite code-read regularity measured against curated code-table contacts.

\paragraph{Module classification.}
The module-classification row assigns all $13$ residual codons, with $R$ size $13$, assigned count $13$, no extra module codons, no missing codons, and no unassigned codons.  The complete module partition is
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
Every listed module has predictable or boundary support in the leave-one-codon-out analysis.  The finite equality
$$
\begin{aligned}
13=2+1+4+1+2+3
\end{aligned}
$$
checks the row-level completeness of the partition.

\paragraph{Local verdict and boundary.}
The ready claim is the following bounded code-layer statement: in the window-six codon cube, the thirteen-codon residual set admits a complete module classification, and every module has predictable or boundary support under the finite leave-one-codon-out probe.  The curated standard-code table supplies the reality contact for codon-to-label readback.  Nothing in this record supplies translation realization, folded protein structure, structural order, physical admissibility, biological function, a universal mechanism, or a proof that DNA obeys BEDC geometry as biological necessity.  Any such higher-layer claim requires a separate contact with its own finite rows.