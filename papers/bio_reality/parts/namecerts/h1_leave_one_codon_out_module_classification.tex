\subsection{NameCert: h1.leave\_one\_codon\_out.module\_classification}
\label{sec:namecert-h1_leave_one_codon_out_module_classification}
\noindent\textit{Proposed by bio-namer; review status: draft.}

\paragraph{NameCert chapter slug.}
Proposed slug: `14110\_bioreality\_q\_six\_module\_classification\_ledger\_namecert\_construction`. The concept is BioReality-namespaced, uses the recommended `BioRealityQSix` prefix, and avoids Loning-occupied names such as `Codon64`, `Q\_6`, `M\textasciicircum{}star`, `StopCodonZeckendorfSuffix`, support nerve, trigger lattice, reassignment spectrum, `dna\_window\_six\_boundary\_tiles`, and standard-code bridge-row terminology.

\paragraph{Carrier.}
Carrier name: `BioRealityQSixModuleClassificationLedger`. BEDC 5-tuple: `(carrier, distinctions, internal\_classification\_packet, external\_reality\_bridge, cannot\_claim\_boundary)`, where the carrier is a finite window-six codon-coordinate module-classification packet; distinctions separate module labels, codon-row slots, predictable rows, boundary rows, anchor rows, and completion witnesses; internal\_classification\_packet records only BEDC coordinate, median-closure, quotient, spectrum-compatible, relation, and local boundary bookkeeping; external\_reality\_bridge points to curated biological tables and reproducible experiment runs; and cannot\_claim\_boundary prevents module classification from being promoted to translation, folding, physical admissibility, function, mechanism, biological-code necessity, or global biological law.

\paragraph{Distinctions.}
- The linked conjecture's six-bit codon display, local tile face, singleton label corner, three-point label class, coordinate, closure, spectrum, and relation distinctions. - The experiment's reassigned-support distinction, referenced through the `R\_codons` field of run `bbe8a4d0455b`. - The experiment's module-label distinction, referenced through the `per\_module` keys `agr`, `ar\_lys`, `cun`, `ile\_met`, `ssn`, and `stop\_trp`. - The experiment's codon-row distinction, referenced through the `classification` and `rows` fields of run `bbe8a4d0455b`. - The experiment's category distinction between `predictable`, `boundary`, and `anchor` rows. - The experiment's module-completion witness, referenced through the `module\_classification\_complete` check. - The experiment's per-module support witness, referenced through the `every\_module\_has\_predictable\_or\_boundary` check. - The bridge distinction between curated genetic-code label readback and internal BEDC coordinate, median, quotient, spectrum, relation, and local boundary structure.

\paragraph{Readback.}
The carrier reads back to observable data by following the `curated.standard.code.table` reality contact, linked conjecture `codon.window6.local.tile.boundary`, claim `h1.leave\_one\_codon\_out.module\_classification`, and latest passed `leave\_one\_codon\_out\_module` experiment run `bbe8a4d0455b`. The readback surface is a provenance pointer to curated code-label rows and reproducible leave-one-codon module classifications; the carrier itself records separated field structure and readback discipline, not translation realization, mechanism, or a biological law.

\paragraph{Internal newmath/BEDC derivation.}
The internal content is a pure BEDC finite-coordinate packet: a BioReality-namespaced window-six carrier, a module-index slot, a codon-row slot, a median-closure slot, a local boundary slot, an anchor slot, a quotient-compatible local tile slot, a spectrum-compatible bookkeeping slot, and a TasteGate-style separation between internal coordinate structure and external readback. The BEDC side may speak about finite distinctions, local closure membership, boundary membership, anchor membership, quotient-compatible carriers, spectrum-compatible bookkeeping, and field separation. No curated genetic-code entry, NCBI table identifier, support size, assigned-count value, module codon list, amino-acid label, stop label, category count, translation table entry, experiment statistic, or biological conclusion is a kernel fact. Such values remain bridge and provenance fields supplied by section 6.

\paragraph{External reality sources.}
- Curated genetic-code source pointer: `tools/bio\_reality/data/ncbi\_genetic\_codes.json` and reality contact `curated.standard.code.table`. - Conjecture pointer: `tools/bio\_reality/inbox/conjectures.jsonl` record `codon.window6.local.tile.boundary`. - Claim pointer: `tools/bio\_reality/registries/claims.json` record `h1.leave\_one\_codon\_out.module\_classification`. - Experiment provenance pointer: `tools/bio\_reality/state/experiment\_runs.jsonl` latest passed `experiment\_run\_id` `bbe8a4d0455b` for `leave\_one\_codon\_out\_module`. - Statistical witness pointer: the event payload, linked conjecture verified-facts entry, claim history, and experiment run fields `module\_classification\_complete`, `every\_module\_has\_predictable\_or\_boundary`, `R\_codons`, `classification`, `per\_module`, and `rows`; these are referenced by field name and run ID, not imported into the internal BEDC carrier. - Dependency pointer: claim `h1.leave.one.codon.out` and its recorded experiment provenance supply the upstream leave-one-codon classification surface; the module ledger records only the stable downstream claim boundary. - Gate/probe boundary pointer: probe `codon.local.tile.no.global.law` and the linked conjecture's forbidden claims.

\paragraph{Cannot-claim boundary.}
- This chapter does not claim biological-code necessity. - This chapter does not claim that window-six geometry realizes translation. - This chapter does not claim that leave-one-codon module classification is a translation classifier. - This chapter does not claim that curated module labels are derived by BEDC. - This chapter does not claim that predictable, boundary, or anchor membership establishes folded protein structure, physical admissibility, biological function, biochemical mechanism, evolutionary necessity, or universal mechanism. - This chapter does not claim that a curated table row, module label, support size, assigned-count value, codon list, amino-acid label, stop label, NCBI table identifier, or statistical witness is a BEDC kernel fact. - This chapter does not claim that local codon-tile geometry is a global biological law. - This chapter does not claim causality, standard-code universality, reassignment necessity, biological inevitability, or a universal mechanism from the module-classification packet.

\paragraph{Bridge disclaimer.}
This proposal is a vision-level scientific-model bridge for a stable BioReality code-read leave-one-codon module-classification claim. It is not a CheckedCert claim about molecular biology, and it does not turn a BEDC window-six coordinate, a local codon tile, a median-closure packet, a module ledger, a predictable row, a boundary witness, an anchor slot, a curated-table comparison, or an empirical statistical witness into translation realization, folded structure, physical admissibility, function realization, biological-code necessity, or a universal biological law; those require separate external reality contacts.

\paragraph{Lean target sketch (statement-only).}
`BEDC.Derived.BioRealityQSixModuleClassificationLedgerUp.TasteGate` carrier alignment, with curated-table, module-label, codon-row, experiment-run, conjecture-boundary, and statistical-witness evidence represented only as bridge/provenance fields.

