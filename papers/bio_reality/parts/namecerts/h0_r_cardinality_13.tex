\subsection{Window-Six Codon Boundary Tile}\label{sec:bioreality-h0-r-cardinality-13}
\origin{ai}

\paragraph{Carrier and readback.}
This BioReality row records a finite window-six codon calculation on the codon cube $Q_6$.  The carrier is the six-bit codon display, while the only external contact used here is the curated standard-code label table: each codon is read back as an amino-acid label or a stop label.  The BEDC-side distinctions are the local tile face, the singleton label corner, and the three-point label class; the biological object is only the standard-code codon table at the code-read layer.  Thus the row is not a claim about translation as a cellular process.  It is a finite statement about which codon labels sit on a local boundary pattern after the standard table has supplied the labels.

\paragraph{The thirteen-row boundary set.}
The finite boundary set $R$ has cardinality $13$.  In the codon alphabet used by the table, the row is
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
Equivalently, the recorded scalar is $|R|=13$.  The punctured-cube check gives the same codon list and the face data
$$
\begin{aligned}
f(R)=(13,16,4),
\end{aligned}
$$
so the local object being discussed is a finite tile with $13$ codon vertices, $16$ one-dimensional adjacencies, and $4$ two-dimensional faces.  This is the precise scope of the geometric assertion: a local tile has been found inside the six-coordinate codon cube after label readback.  The row does not assert that this tile is a global law of biological coding.

\paragraph{Spectral finite check.}
The spectral row assigns the local matrix data the scalars
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The recorded cubic, or radial, polynomial residual is $-6.224354365258478\times 10^{-12}$, which is numerically zero at the stated precision.  The spectral radius reported in tolerance is again $0.6752479308830213$.  These numbers support the finite-row internal consistency of the coordinate, closure, spectrum, and relation structure.  They do not supply a biochemical mechanism, a translation dynamics, or a folding law.

\paragraph{Shape significance and null contacts.}
Against the sampled shape null with seed $42$, the run used $N=2000$ sampled shapes, with $57377$ distinct-sample attempts and $55377$ duplicate retries.  The exact-shape count was $1$, giving $p_{\mathrm{exact}}=0.0005$.  The high-overlap count at Jaccard threshold $0.9$ was $25$, giving $p_{J\geq 0.9}=0.0125$.  A second amino-acid-class-preserving null, again with $N=2000$ and seed $42$, recorded observed $M$-size $20$, exact-match count $0$, and $7$ samples in the lower tail at or below the observed $M$ size.  Its exact-shape value is $p=0.0$ at the run resolution, while the lower-tail value is $0.0035$.  These finite numbers say that the observed local shape is uncommon under the two supplied randomizations.  They do not turn the local tile into a necessity theorem about life, chemistry, translation, protein structure, or function.

\paragraph{Leave-one-code and leave-one-codon behavior.}
The leave-one-code-out row evaluated $23$ code tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate inside $M\setminus T$ was $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus boundary was again $0.9841269841269841$.  The family-level decomposition used $5$ families: alternative yeast had hit rate $0.8333333333333334$, and bacterial plastid, candidate other, mitochondrial, and nuclear ciliate each had hit rate $1.0$ above baseline.  The leave-one-codon-out row recorded anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$.  These rows are read as code-layer recovery checks: omitted labels tend to be recovered by the local code geometry and boundary information.  The checks still remain label-table checks; no omitted protein fold, molecular admissibility, or realized function is reconstructed by them.

\paragraph{Module classification.}
The thirteen codons in $R$ are partitioned by the recorded module classification as follows:
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
The classification is complete at the row level: $R$ has size $13$, the assigned count is $13$, and the extra-module, missing-from-module, and unassigned-codon lists are all empty.  The additional module predicate says that every module has either predictable or boundary status.  This is a finite certificate that the listed local boundary codons are exhausted by the displayed module classes.

\paragraph{Reality boundary.}
The contact named by this row is the curated standard-code table.  It tests codon-label readback and local codon-tile label split, and it cannot test translation realization, structural order, physical admissibility, or function realization.  Consequently the local conclusion is intentionally narrow: window-six codon coordinates expose a thirteen-row boundary tile whose labels are supplied by the standard table and whose module split is finite and complete.  No translation, folding, physical admissibility, biological function, mechanism, or universality follows from this row unless a separate higher-layer contact supplies the missing evidence.