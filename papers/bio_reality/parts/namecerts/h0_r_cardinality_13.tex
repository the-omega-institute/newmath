\subsection{Window-six boundary row for the standard-code table}\label{subsec:h0-r-cardinality-13}
\origin{ai}

\paragraph{Finite carrier and observed row.}
The record uses the six-coordinate codon cube $Q_6$ as its finite display: each codon is read as a six-bit window while the biological label is taken from a curated standard-code table.  The row under inspection is the boundary set $R$ singled out by the local tile computation.  The verified row has cardinality $13$ and consists exactly of
$$\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}$$
Thus the finite datum is not merely a size assertion: the thirteen named codons are part of the observation.  In the window-six presentation, this row is the place where a local face of the codon cube, singleton-corner behavior, and a three-point label class are all visible at the code-reading level.  The result is a compact boundary tile in codon-coordinate space, with the labels supplied by the standard table rather than inferred from geometry alone.

\paragraph{Combinatorial shape.}
A second verification identifies the same thirteen codons as a punctured-cube row and records the face data
$$\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}$$
Here $f_0=13$ is the number of row vertices, $f_1=16$ is the number of retained local edges, and $f_2=4$ is the number of retained square faces in the finite tile.  This is the internal geometric part of the record: it says that the row is not an arbitrary thirteen-element subset of the codon cube, but a small cubical boundary pattern with a specific incidence profile.  The shape remains local.  It supports a statement about a code-table row inside $Q_6$; it does not by itself describe translation, protein realization, folded structure, admissible chemistry, biological function, a mechanism, or a law across organisms.

\paragraph{Spectral coordinates.}
The same finite object carries a numerical spectral signature.  The verified values are
$$\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}$$
The reported radial cubic residual is $-6.224354365258478\times 10^{-12}$, and the same number is recorded as the cubic-polynomial residual.  At the scale of this finite audit, the residual is numerically zero.  The spectral radius field agrees with $\lambda_M=0.6752479308830213$.  These quantities give an internal coordinate fingerprint for the local tile, but the fingerprint is still only a code-layer invariant.  It is a measurement attached to the finite codon display, not an observation of translation machinery or of a folded molecular object.

\paragraph{Null comparison against shuffled code rows.}
The shape is unusual under the recorded null experiments.  In the general shape comparison with $N=2000$ samples and seed $42$, the audit reports $57377$ distinct-sample attempts, $55377$ duplicate retries, $1$ exact shape match, and $25$ matches with Jaccard similarity at least $0.9$.  The resulting empirical values are
$$\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9}&=0.0125.
\end{aligned}$$
An amino-acid-class-preserving null model, also with $N=2000$ and seed $42$, reports observed $|M|=20$, no exact shape matches, and $7$ samples in the lower tail at or below the observed $M$ size.  Its values are
$$\begin{aligned}
p_{\mathrm{class\ exact}}&=0.0,\\
p_{\mathrm{class\ lower\ tail}}&=0.0035.
\end{aligned}$$
The row therefore survives two finite comparisons as a non-generic local code-table pattern.  The available finite data give the size of $M$ but not a named list of all elements of $M$, so this chapter uses only the reported $M$ count and the verified $R$ list.

\paragraph{Prediction rows and module accounting.}
The leave-one-code-out check evaluates $23$ tables and $63$ targets.  Its baseline hit rate is $0.19821739130434785$, while the reported hit rate above baseline is $0.9841269841269841$.  The rate inside $M-T$ is $0.9365079365079365$, and the rate inside $M-T$ plus the boundary is again $0.9841269841269841$.  The leave-one-codon-out check records no anchors, $5$ boundary cases, $8$ high-coverage closure cases, $8$ predictable cases, and recovery rate $1.0$.  By family, there are $5$ families: alternative yeast has hit rate $0.8333333333333334$, while bacterial plastid, candidate other, mitochondrial, and nuclear ciliate each have hit rate $1.0$ above baseline.

The thirteen codons are also fully assigned to six named modules:
$$\begin{aligned}
AGR&=\{AGA,AGG\},\\
AR\_LYS&=\{AAA\},\\
CUN&=\{CUA,CUC,CUG,CUU\},\\
ILE\_MET&=\{AUA\},\\
SSN&=\{UCA,UUA\},\\
STOP\_TRP&=\{UAA,UAG,UGA\}.
\end{aligned}$$
The module audit reports $R$ size $13$, assigned count $13$, no missing codons, no extra module codons, and no unassigned codons.  This closes the finite row accounting: every codon in the boundary row is named, every name is placed in a module, and each module has either predictable or boundary status in the recorded leave-one-codon-out classification.

\paragraph{Scientific reading and boundary.}
The supported claim is narrow.  A six-coordinate codon display, together with the curated standard-code table, exposes a thirteen-codon local boundary row with a punctured-cube incidence profile, a stable numerical spectrum, significant finite null behavior, and complete module assignment.  The biological contact is the codon-to-label table: it lets the row be read as standard-code labels and lets the local split be checked against finite code data.  It does not observe translation, folding, structure, physical admissibility, function, mechanism, or universality.  Any step into those higher biological layers would require a separate contact of the appropriate kind.  In this chapter the codon tile remains a local code-read record, not a global biological principle.