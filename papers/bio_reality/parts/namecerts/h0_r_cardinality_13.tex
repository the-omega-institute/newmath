\subsection{Window-six codon boundary row}\label{sec:biorality-codon-window-six-boundary-row}
\origin{ai}

\paragraph{Finite carrier and observed row.}
The finite object in this record is the six-bit codon cube $Q_6$ read through the curated standard-code label table.  The coordinate carrier is not treated as a biological law; it is a finite display on which local tile faces, singleton label corners, and three-point label classes can be inspected.  The verified boundary row is the thirteen-codon set
$$
\begin{aligned}
R=\{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\\
&\texttt{CUU},\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}
$$
The finite-row audit gives $|R|=13$, with the same thirteen entries returned by the cardinality check and by the punctured-cube readback.  The punctured-cube computation reports the face-vector
$$
\begin{aligned}
f(R)=(13,16,4),
\end{aligned}
$$
so the row is recorded as a small local tile boundary rather than as a global code theorem.  Its readback contact is only the curated standard-code table: the contact assigns codons to amino-acid or stop labels, and it does not observe translation realization, folded structure, physical admissibility, function, mechanism, or universality.

\paragraph{Spectral and null-model shape checks.}
The associated spectral row has numerical values
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\times 10^{-12}$, the same value reported for the cubic-polynomial satisfaction check, and the spectral-radius tolerance record returns $0.6752479308830213$.  These numbers support the internal finite-row coherence of the coordinate probe, not a biological mechanism.

The shape-significance null model used $N=2000$ distinct samples, with $57377$ distinct-sample attempts and $55377$ duplicate retries under seed $42$.  It found $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9}&=0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null also used $N=2000$ and seed $42$.  It reported observed $M$-size $20$, exact-match count $0$, lower-tail count $7$, class-preserving exact-shape value $0.0$, and lower-tail value $0.0035$.  Thus the available finite evidence says that the observed local shape is uncommon under the sampled nulls.  It still remains a code-read layer result because the nulls are defined over codon-label tables and finite coordinate probes.

\paragraph{Leave-one recovery and family rows.}
The leave-one-code-out experiment evaluated $23$ tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$, while the reported hit rate above baseline was $0.9841269841269841$.  The hit rate inside $M\setminus T$ was $0.9365079365079365$, and the rate inside $M\setminus T$ plus boundary was $0.9841269841269841$.  These row counts support the statement that the local boundary row is informative for code-label recovery in the tested finite table family.

The leave-one-codon-out row reported anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$, with the recovery-rate-above-threshold field also equal to $1.0$.  The family-level leave-one-code-out record divided the alternatives into $5$ families.  The family hit rates above baseline were
$$
\begin{aligned}
\text{alternative yeast}&:0.8333333333333334,\\
\text{bacterial plastid}&:1.0,\\
\text{candidate other}&:1.0,\\
\text{mitochondrial}&:1.0,\\
\text{nuclear ciliate}&:1.0.
\end{aligned}
$$
The weaker alternative-yeast number is part of the finite record and should not be smoothed away.  It marks the probe as strong but not literally uniform across all observed families.

\paragraph{Module classification of the thirteen-row.}
The module classification accounts for every codon in $R$ and assigns no extra codons.  It reports $R$-size $13$, assigned count $13$, missing-from-module list empty, unassigned-codon list empty, and extra-module-codon list empty.  The six named modules are
$$
\begin{aligned}
\text{agr}&=\{\texttt{AGA},\texttt{AGG}\},\\
\text{ar\_lys}&=\{\texttt{AAA}\},\\
\text{cun}&=\{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\},\\
\text{ile\_met}&=\{\texttt{AUA}\},\\
\text{ssn}&=\{\texttt{UCA},\texttt{UUA}\},\\
\text{stop\_trp}&=\{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}.
\end{aligned}
$$
This completes the finite code-read partition of the observed row.  The partition is a local label-boundary certificate: it says that the thirteen codons are captured by named modules inside the curated label table and by the window-six coordinate probe.

\paragraph{Local cannot-claim boundary.}
The supported statement is deliberately narrow.  Window-six coordinates expose a local codon-tile boundary, and the curated standard-code table supplies the external contact needed to read the codons as amino-acid or stop labels.  No translation process is observed by this chapter.  No folded protein structure is observed.  No physical admissibility, biological function, global mechanism, or universality theorem follows from the row.  Amino-acid label-sequence readback remains a code-layer readback unless a separate higher-layer contact supplies structure, realization, or function.  The finite evidence therefore supports a chapter-specific verdict: $R$ is a thirteen-codon local boundary row with uncommon sampled shape, coherent spectral numerics, strong leave-one recovery behavior, and complete module accounting, but it is not promoted beyond code-read scope.