\origin{ai}

\subsection{Window-six codon boundary row}
\label{subsec:h0-r-cardinality-13}

\paragraph{Finite carrier and readback contact.}
The carrier in this record is the six-bit codon cube $Q_6$, read through the standard-code codon label table.  The biological contact used here is only the curated assignment of codons to amino-acid or stop labels.  It is therefore a code-read contact, not a mechanism contact.  The finite row isolates a boundary set $R$ of size $13$:
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The verified cardinality row reports $|R|=13$, with the same thirteen codons returned by the expected-codon check.  This is the local object under discussion.  It should be read as a finite boundary tile in codon-coordinate space whose labels are supplied by the external standard-code table.

\paragraph{Punctured-cube geometry.}
The same thirteen codons also pass the punctured-cube check.  The recorded face data are
$$
\begin{aligned}
f(R)=(13,16,4),
\end{aligned}
$$
so the row has $13$ vertices, $16$ one-dimensional adjacencies, and $4$ two-dimensional faces in the finite tile representation.  This is a coordinate statement about the window-six display.  It does not by itself assert that biological translation is generated by this geometry.  Rather, the point is that the curated code labels meet a small, structured local face in $Q_6$.

\paragraph{Spectral row.}
The spectral probe gives a numerical separation between the larger $M$ side and the boundary $R$ side.  The recorded values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The associated radial cubic residual is $-6.224354365258478\times 10^{-12}$, equal to the reported cubic-polynomial residual within numerical tolerance.  Thus the finite-row audit treats the spectral radius value $0.6752479308830213$ as numerically stable at the displayed precision.  The interpretation remains local: these numbers certify a spectrum for the finite coordinate object and do not certify folding, physical admissibility, or function.

\paragraph{Shape significance under unconstrained sampling.}
The shape-significance probe used $N=2000$ sampled alternatives with seed $42$.  It required $57377$ distinct-sample attempts and encountered $55377$ duplicate retries.  Among those sampled alternatives there was $1$ exact shape match, giving
$$
\begin{aligned}
p_{\mathrm{exact}} = 0.0005.
\end{aligned}
$$
There were $25$ alternatives with Jaccard similarity at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{Jaccard}\geq 0.9}=0.0125.
\end{aligned}
$$
These values support the claim that the observed local tile is not a common outcome of this finite null sampler.  They do not turn the tile into a universal biological law; they only quantify rarity relative to the stated coordinate null.

\paragraph{Amino-acid-class preserving null.}
A second null model preserves amino-acid class information.  In this row the observed $M$ size is $20$.  Over $N=2000$ trials with seed $42$, the exact-shape match count is $0$, so the reported exact-shape probability is
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}=0.0.
\end{aligned}
$$
The lower-tail count at or below the observed $M$ size is $7$, giving
$$
\begin{aligned}
p_{\mathrm{aa\ class\ lower\ tail}}=0.0035.
\end{aligned}
$$
This is a stronger constrained-row check: even after preserving amino-acid class, the observed finite shape remains rare in the sampled population.  The row still depends on the curated label table as an external readback bridge.

\paragraph{Leave-one-code-out behavior.}
The leave-one-code-out probe evaluated $23$ tables and $63$ total targets.  The baseline hit rate is $0.19821739130434785$.  The row reports hit rate above baseline $0.9841269841269841$, hit rate inside $M-T$ equal to $0.9365079365079365$, and hit rate inside $M-T$ plus boundary equal to $0.9841269841269841$.  By family, the five recorded groups have hit rates above baseline
$$
\begin{aligned}
\mathrm{alternative\ yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\ plastid} &= 1.0,\\
\mathrm{candidate\ other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\ ciliate} &= 1.0.
\end{aligned}
$$
These rows show that the local boundary construction has high recovery behavior against the finite code-table perturbation test.  The lower alternative-yeast value marks a family-level limitation rather than a failure of the whole code-read row.

\paragraph{Leave-one-codon-out and module partition.}
The leave-one-codon-out probe reports anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$.  The module classification covers all thirteen $R$ codons with assigned count $13$, no extra module codons, no missing codons, and no unassigned codons.  The six modules are
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every module has either predictable or boundary support in the finite audit.  This makes the row internally complete as a local code-read classification of $R$.

\paragraph{Local boundary verdict.}
The finite evidence supports the following bounded claim: in the six-bit codon display, the curated standard-code labels expose a thirteen-codon boundary tile with punctured-cube face vector $(13,16,4)$, stable spectral values, rare shape statistics under two finite null models, and complete module assignment across the thirteen boundary codons.  The claim is deliberately local.  No translation process, folded structure, physical admissibility, biological function, mechanism, or universality follows from this row unless a separate reality contact supplies that higher-layer fact.  Amino-acid label-sequence readback is not folded protein structure, and a local codon tile is not a global biological law.