\subsection{Window-six codon boundary record for a thirteen-row tile}\label{subsec:bioreality-h0-r-cardinality-13}
\origin{ai}

\paragraph{Finite carrier and contact.}
The finite carrier is the six-bit codon display $Q_6$, read only through a curated standard-code table that assigns each codon to an amino-acid or stop label.  The record is therefore a code-read record: it tests a local codon-tile label split, not translation as a cellular process.  The distinguished boundary row $R$ has cardinality $13$.  In the observed standard-code readback it is exactly
$$
\begin{aligned}
R=\{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}
$$
The verified row count is $|R|=13$, and the same row is recovered by the punctured-cube check.  Its finite face data have $f$-vector $(13,16,4)$, so the contact is not merely a list of labels: it also supplies a local tile shape with thirteen vertices, sixteen one-dimensional adjacencies, and four two-dimensional faces inside the codon-window representation.

\paragraph{Spectral coordinates.}
The associated finite coordinate computation reports three scalar spectral values.  The boundary component has $\lambda_R=0.4758352843197002$, the complementary or module-side component has $\lambda_M=0.6752479308830213$, and the radial parameter is $\mu=3.051487585298128$.  The cubic or radial polynomial residual is numerically negligible at $-6.224354365258478\cdot 10^{-12}$, so the displayed scalars satisfy the finite spectral equation to the recorded tolerance:
$$
\begin{aligned}
\lambda_R&=0.4758352843197002,\\
\lambda_M&=0.6752479308830213,\\
\mu&=3.051487585298128,\\
P_{\mathrm{rad}}(\lambda_R,\lambda_M,\mu)&=-6.224354365258478\cdot 10^{-12}.
\end{aligned}
$$
The larger value $\lambda_M$ should be read as a coordinate-side separation in this finite graph, not as a biochemical energy, fitness, or folding parameter.

\paragraph{Null checks and shape significance.}
Against the recorded finite-row null model, the shape is rare.  In $N=2000$ distinct null samples with seed $42$, the exact shape appeared once, giving $p_{\mathrm{exact}}=0.0005$.  High-overlap shape recovery at Jaccard threshold $0.9$ occurred $25$ times, giving $p_{J\geq 0.9}=0.0125$.  The sampling process required $57377$ distinct-sample attempts and $55377$ duplicate retries, which records the finite combinatorial pressure of the null ensemble rather than an independent biological mechanism.  In the amino-acid-class-preserving null, again with $N=2000$ and seed $42$, the observed module-side size was $20$, the exact-match count was $0$, and the lower-tail count at or below the observed $M$ size was $7$.  Thus the class-preserving exact-shape value is $0.0$, while the lower-tail value is $0.0035$:
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{J\geq 0.9}&=0.0125,\\
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These values support a finite local-tile anomaly relative to the stated nulls.  They do not promote the anomaly to a universal genetic-code law.

\paragraph{Leave-one readback checks.}
The leave-one-code-out probe evaluated $23$ code tables and $63$ total targets.  The baseline hit rate was $0.19821739130434785$.  The hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ together with the boundary was $0.9841269841269841$.  The recorded hit rate above baseline is also $0.9841269841269841$.  The leave-one-codon-out probe gives a complementary finite-row check: it has anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$.  These numbers say that the local coordinate and closure probes recover the held-out code labels at the code-read layer with high finite accuracy.  They do not say that a ribosome, tRNA charging system, folded protein, physical admissibility condition, or functional phenotype has been reconstructed.

\paragraph{Family and module decomposition.}
The family-level leave-one-code-out table has $5$ families.  The hit rates above baseline are $1.0$ for bacterial-plastid, mitochondrial, nuclear-ciliate, and candidate-other families, and $0.8333333333333334$ for the alternative-yeast family.  The thirteen-row boundary is completely assigned by the module classification: $\texttt{AGR}=\{\texttt{AGA},\texttt{AGG}\}$, $\texttt{ar\_lys}=\{\texttt{AAA}\}$, $\texttt{CUN}=\{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\}$, $\texttt{ile\_met}=\{\texttt{AUA}\}$, $\texttt{SSN}=\{\texttt{UCA},\texttt{UUA}\}$, and $\texttt{stop\_trp}=\{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}$.  The classification reports $R$-size $13$, assigned count $13$, no extra module codons, no missing module entries, and no unassigned codons.  Thus the boundary row is not an orphaned codon list: every codon in $R$ is accounted for by one of the finite modules, and every listed module element lies in the same thirteen-row boundary.

\paragraph{Scope boundary.}
The contact is a curated standard-code bridge, so the strongest local conclusion is that window-six coordinates expose a finite boundary-tile structure whose label readback aligns with the standard-code table and survives the stated null and leave-one probes.  The cannot-claim boundary is part of the record itself.  No translation realization, folded structure, structural order, physical admissibility, biological function, mechanism, biological necessity, or universality follows from this chapter unless a separate higher-layer reality contact supplies that information.  Amino-acid label-sequence readback remains code-layer readback; it is not a folded protein structure and it is not evidence of function.