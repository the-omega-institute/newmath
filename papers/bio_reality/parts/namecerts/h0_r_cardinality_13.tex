\origin{ai}
\subsection{Window-six codon boundary row}
\label{sec:bioreality-h0-r-cardinality-13}

\paragraph{Finite carrier and contact.}
This record treats the standard-code codon table as a code-read contact on the six-bit codon cube $Q_6$.  The contact is deliberately narrow: it supplies codon-to-label readback from a curated standard genetic-code table, and it does not supply translation realization, folded structure, physical admissibility, biological function, a universal mechanism, or biological necessity.  The finite row under inspection is the boundary set $R$ selected by the window-six local tile procedure.  In the verified row, $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}
$$
The same row is certified as a punctured-cube face complex with $f$-vector $(13,16,4)$.  Thus the object is not merely a list of exceptional codons: it is a finite local tile with thirteen vertices, sixteen one-dimensional incidences, and four two-dimensional faces inside the six-coordinate codon display.

\paragraph{Spectral and null-model measurements.}
The associated spectral probe gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  Its radial cubic residual is numerically negligible at $-6.224354365258478\cdot 10^{-12}$, matching the separately reported cubic-polynomial residual.  The finite randomization tests also point to a non-generic tile.  In the shape-significance experiment with $N=2000$ samples, there was $1$ exact shape match, so $p_{\mathrm{exact}}=0.0005$; there were $25$ matches with Jaccard score at least $0.9$, so $p_{\mathrm{Jaccard}\ge 0.9}=0.0125$.  The experiment required $57377$ distinct-sample attempts and incurred $55377$ duplicate retries, which records the sampling burden rather than a biological mechanism.  In the amino-acid-class-preserving null model with $N=2000$, the observed comparison set had size $|M|=20$, the exact-shape count was $0$, and the lower-tail count at or below the observed $M$ size was $7$.  Hence the reported exact-shape value is $0.0$, while the lower-tail value is $0.0035$.

\paragraph{Leave-one-out readback.}
The code-level readback tests are consistent with the same local boundary interpretation.  In the leave-one-code-out table test, $23$ tables and $63$ targets were evaluated.  The baseline hit rate was $0.19821739130434785$, while the hit rate in $M-T$ was $0.9365079365079365$ and the hit rate in $M-T$ plus boundary was $0.9841269841269841$.  The reported hit rate above baseline is therefore $0.9841269841269841$.  In the leave-one-codon-out test, there were $0$ anchors, $5$ boundary cases, $8$ high-coverage closure cases, and $8$ predictable cases, with recovery rate $1.0$.  These are code-read recoveries: they say that a codon label is recovered or bounded inside the finite window-six geometry, not that a molecule is translated, folded, stabilized, or assigned function by the geometry.

\paragraph{Module partition.}
The thirteen codons in $R$ are exhausted by six named modules.  The module classification records $R$ size $13$, assigned count $13$, no missing codons, no extra module codons, and no unassigned codons.  The concrete partition is
$$
\begin{aligned}
\mathrm{agr}&=\{\texttt{AGA},\texttt{AGG}\},\\
\mathrm{ar\_lys}&=\{\texttt{AAA}\},\\
\mathrm{cun}&=\{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\},\\
\mathrm{ile\_met}&=\{\texttt{AUA}\},\\
\mathrm{ssn}&=\{\texttt{UCA},\texttt{UUA}\},\\
\mathrm{stop\_trp}&=\{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}.
\end{aligned}
$$
Every module has a predictable-or-boundary witness in the reported classification.  At the family level, the leave-one-code-out hit rate above baseline is $0.8333333333333334$ for alternative yeast and $1.0$ for each of bacterial plastid, candidate other, mitochondrial, and nuclear ciliate families, across $5$ families.  This supports a finite-row claim about the geometry of code-label differences, not a general law about all genetic codes.

\paragraph{Boundary verdict.}
The finite evidence supports the following self-contained reading.  The window-six codon coordinate system exposes a thirteen-codon local boundary tile in the standard-code label table, with punctured-cube combinatorics, stable spectral scalars, significant rarity under the stated randomization probes, and strong code-level leave-one-out recovery.  The admissible conclusion is only at the code-read layer: curated labels meet a finite coordinate construction and produce a local split.  The same data explicitly block promotion to translation, folded protein structure, physical admissibility, biological function, mechanism, or universality.  Any such higher-layer claim would require a separate reality contact whose observations live at that higher layer.