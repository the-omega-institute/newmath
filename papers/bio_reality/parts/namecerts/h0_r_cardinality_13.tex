\origin{ai}
\subsection{Window-Six Codon Boundary Row}
\label{sec:bioreality-h0-r-cardinality-13}

\paragraph{Finite carrier and readback contact.}
The claim $h0.R.cardinality.13$ is a finite-row record about the standard-code codon table viewed through a six-bit codon display. The carrier is the codon cube $Q_6$, and the only reality contact used here is a curated standard-code table assigning each codon to an amino-acid label or to a stop label. This contact is a code-read contact: it supports label readback and local split checks, but it does not observe translation realization, folded structure, physical admissibility, biological function, a mechanism, or universality. The row therefore treats codon labels as external reality contacts attached to a finite BEDC coordinate display, not as consequences forced by cube geometry alone.

\paragraph{Boundary set.}
The verified boundary row is the finite set $R$ of thirteen codons
$$
\begin{aligned}
R=\{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}
$$
The finite audit reports $|R|=13$, with the same thirteen codons appearing both in the cardinality row and in the punctured-cube row. The punctured-cube certificate gives the face-count vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4),
\end{aligned}
$$
so the local object is not merely a list of exceptional labels: it carries a small cubical boundary shape with thirteen vertices, sixteen one-dimensional adjacencies, and four two-dimensional faces. The distinction tested by this row is precisely local. It says that these codons occupy a structured boundary tile in the window-six display; it does not say that the tile is a global biological law.

\paragraph{Spectral row.}
The same finite object has a recorded spectrum summary. The numerical row gives
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial polynomial check is numerically closed at $-6.224354365258478\cdot 10^{-12}$, and the cubic polynomial row reports the same residual. Within the stated tolerance, the spectral radius attached to $M$ is therefore $0.6752479308830213$. These values are used only as finite-row invariants of the displayed code-read geometry. They do not supply a physical dynamics, a translation process, or a protein-structure model.

\paragraph{Shape significance rows.}
The finite Monte Carlo shape row used $N=2000$ samples with seed $42$. It required $57377$ distinct-sample attempts and saw $55377$ duplicate retries. Among those samples, there was $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$. The corresponding reported probabilities are
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{J\geq 0.9}&=0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null row also used $N=2000$ and seed $42$. It records observed $|M|=20$, exact-match count $0$, lower-tail count $7$, exact-shape probability $0.0$, and lower-tail probability $0.0035$. Thus, under the tested finite nulls, the observed shape is rare both as an exact local shape and as a lower-tail event in the amino-acid-class-preserving control. This is evidence for a code-layer boundary pattern, not evidence for folding, physical admissibility, function, mechanism, or universal necessity.

\paragraph{Leave-one readback rows.}
The leave-one-code-out row evaluated $23$ tables and $63$ total targets. The baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$. The hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$. The leave-one-codon-out row records anchor count $0$, boundary count $5$, closure-coverage-high count $8$, predictable count $8$, and recovery rate $1.0$. These readback rows say that the local coordinate-plus-boundary package has strong finite recovery behavior at the code-label level. They do not convert codon-label recovery into translation realization or biological function.

\paragraph{Family and module rows.}
The family row partitions the leave-one-code-out behavior into $5$ families. The hit rates above baseline by family are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial or plastid tables, $1.0$ for candidate other tables, $1.0$ for mitochondrial tables, and $1.0$ for nuclear ciliate tables. The module-classification row is complete: it assigns all $13$ codons of $R$, with $R$-size $13$, assigned count $13$, no extra module codons, no missing codons from modules, and no unassigned codons. The six modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\texttt{AGA},\texttt{AGG}\},\\
\mathrm{ar\_lys}&=\{\texttt{AAA}\},\\
\mathrm{cun}&=\{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\},\\
\mathrm{ile\_met}&=\{\texttt{AUA}\},\\
\mathrm{ssn}&=\{\texttt{UCA},\texttt{UUA}\},\\
\mathrm{stop\_trp}&=\{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}.
\end{aligned}
$$
Every listed module has predictable or boundary status in the finite certificate. This gives a row-level decomposition of the thirteen-codon boundary into local code-read modules, while preserving the scope boundary: no module classification here observes a ribosome, a peptide, a folded protein, an energetic admissibility condition, a selectable phenotype, or a universal generative law.

\paragraph{Boundary verdict.}
The finite evidence supports the following local statement. In the window-six codon display, the standard-code label table exposes a thirteen-codon boundary set $R$ with punctured-cube shape vector $(13,16,4)$, nontrivial spectral row values, rare null-model shape statistics, high leave-one readback rates, and a complete six-module classification. The result is a code-read boundary tile certified by finite rows and by a curated label-table contact. It remains deliberately bounded: amino-acid label-sequence readback is not folded protein structure, local tile recovery is not translation realization, and no claim of physical admissibility, biological function, mechanism, or universality follows without a separate contact designed to test that higher layer.