\subsection{Window-six codon boundary tile}\label{sec:bior-codon-window-six-boundary}
\origin{ai}

\paragraph{Finite carrier and reality contact.}
This record treats the standard-code codon table as a finite code-read contact, not as a mechanism.  The carrier is the six-bit codon cube $Q_6$, read through a curated standard-code table assigning each codon to an amino-acid or stop label.  The observed boundary row is the finite set
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The verified cardinality row is therefore $|R|=13$, with the listed thirteen codons exactly matching the expected codon row.  This is the central finite claim: the local window-six coordinate system isolates a thirteen-codon boundary tile inside the code-read layer.  The claim is intentionally weaker than any biological-law statement.  The curated table supplies label readback; it does not supply translation realization, folded structure, physical admissibility, biological function, universal mechanism, or code necessity.

\paragraph{Punctured-cube shape.}
The same thirteen-codon row is verified as a punctured-cube boundary with face-count vector
$$
\begin{aligned}
f(R)=(13,16,4).
\end{aligned}
$$
Thus the row is not only a set of named codons but also a local tile with thirteen vertices, sixteen one-dimensional adjacencies, and four two-dimensional faces in the induced finite geometry.  The display is a coordinate fact about the codon table under the chosen six-bit encoding.  It is not evidence that DNA must realize this geometry as a physical constraint, and it is not evidence that amino-acid strings obtained by label readback have any particular folded structure.

\paragraph{Spectral row.}
The finite-row spectrum gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The radial cubic check returns
$$
\begin{aligned}
P(\mu)=-6.224354365258478\cdot 10^{-12},
\end{aligned}
$$
which is numerically at zero at the recorded tolerance.  The spectral radius row also records $0.6752479308830213$ as the value in tolerance.  These numbers support a reproducible finite spectral signature for the local tile and its surrounding coordinate object.  They do not promote the tile to an explanation of translation, folding, physical admissibility, function, mechanism, or universality.

\paragraph{Shape significance.}
Against the recorded shape null with $N=2000$ samples, the experiment used seed $42$, made $57377$ distinct-sample attempts, and encountered $55377$ duplicate retries.  It observed $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$.  The corresponding rows are
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9} &= 0.0125.
\end{aligned}
$$
Under the amino-acid-class-preserving null, again with $N=2000$ and seed $42$, the observed main tile size is $|M|=20$, the exact-shape count is $0$, and the lower-tail count at or below the observed $M$ size is $7$.  The recorded probabilities are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
These finite null rows make the local tile statistically sharp relative to the tested randomizations.  They do not identify a biochemical cause, and they do not test protein realization.

\paragraph{Leave-one-code-out readback.}
The code-table perturbation row evaluated $23$ tables and $63$ total targets.  The baseline hit rate is $0.19821739130434785$.  The hit rate above baseline is $0.9841269841269841$, the hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$.  By family, the hit-rate-above-baseline values are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial or plastid tables, $1.0$ for candidate other tables, $1.0$ for mitochondrial tables, and $1.0$ for nuclear ciliate tables, over $5$ recorded families.  This supports code-layer stability of the local readback pattern across the finite family table, while still leaving all higher biological layers outside the contact.

\paragraph{Leave-one-codon-out and module classification.}
The codon-removal row reports $0$ anchors, $5$ boundary cases, $8$ high-coverage closure cases, $8$ predictable cases, and recovery rate $1.0$.  The module classification assigns every codon in $R$ to one of six named modules:
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
The classification row records $R$ size $13$, assigned count $13$, no extra module codons, no missing codons from the modules, and no unassigned codons.  Hence the thirteen-codon boundary row is completely partitioned by the finite module table.  The correct interpretation is a local label split in the standard-code readback layer.  No translation process, folded protein structure, physical admissibility condition, biological function, causal mechanism, or universality theorem follows unless an additional reality contact specifically supplies that higher-layer evidence.