\subsection{Window-Six Boundary Row for the Standard-Code Table}
\label{sec:bioreality-h0-r-cardinality-13}
\origin{ai}

\paragraph{Carrier and reality contact.}
The finite carrier is the six-bit codon cube $Q_6$, read as a codon display with one local tile face, singleton label corners, and three-point label classes. The biological contact used here is only the curated standard-code label table: it assigns each codon to an amino-acid label or to a stop label. This contact is a code-read bridge. It is not a translation experiment, not a folding experiment, not a physical-admissibility test, not a function assay, not a mechanism, and not a universality proof. The row recorded in this chapter is therefore a finite local boundary statement about codon-label readback inside $Q_6$.

\paragraph{Boundary row.}
The verified row $R$ has cardinality $13$. Its elements are exactly
$$
\begin{aligned}
R={}&\{\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\quad \texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\},\\
|R|={}&13.
\end{aligned}
$$
This is the finite content of claim $h0.R.cardinality.13$. The same row is also verified as a punctured-cube pattern with face-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the row is not merely a list of labels: it carries a local tile shape in the six-bit codon coordinate system. The shape is still local. It says that these thirteen codons occupy a distinguished boundary configuration in the codon cube under the supplied coordinate and closure probes; it does not say that biology must instantiate the geometry as a law.

\paragraph{Spectral row.}
The same finite record supplies a spectral readout. The measured values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial polynomial value, equivalently the cubic-polynomial residual in the recorded run, is $-6.224354365258478\times 10^{-12}$. This residual is numerically negligible at the precision of the finite computation and supports the internal spectral consistency of the row. The statement remains a coordinate-and-spectrum statement about the codon-label table; no protein structure or realized biochemical dynamics is observed by this computation.

\paragraph{Shape significance.}
Against the recorded null sample with $N=2000$, seed $42$, $57377$ distinct-sample attempts, and $55377$ duplicate retries, there was $1$ exact shape match. The exact-shape probability estimate is therefore
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}
$$
For the high-overlap threshold, $25$ samples had Jaccard similarity at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{Jaccard}\ge 0.9}=0.0125.
\end{aligned}
$$
A second amino-acid-class-preserving null, also with $N=2000$ and seed $42$, recorded observed $M$-size $20$, exact-match count $0$, and lower-tail count $7$. Its reported probabilities are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These values support the claim that the observed local shape is not a common outcome under the supplied finite null models. They do not promote the local shape into a global biological necessity.

\paragraph{Leave-one readback checks.}
The leave-one-code-out row evaluated $23$ code tables and $63$ total targets. The baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$. The hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary was $0.9841269841269841$. These numbers indicate that the local boundary data is informative for code-layer readback under the tested leave-one-code-out protocol.

For the leave-one-codon-out row, the anchor count was $0$, the boundary count was $5$, the predictable count was $8$, and the high-closure coverage count was $8$. The recovery rate was
$$
\begin{aligned}
\rho_{\mathrm{recovery}}=1.0,
\end{aligned}
$$
with the recorded threshold comparison also equal to $1.0$. This check again belongs to the code-read layer: it measures whether the finite coordinate and boundary data recover codon-label information in the curated table setting.

\paragraph{Family and module structure.}
The leave-one-code-out family split contains $5$ families. The recorded family hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate. The module classification is complete: $R$-size is $13$, assigned count is $13$, extra module codons are empty, missing codons are empty, and unassigned codons are empty. The assigned modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\texttt{AGA},\texttt{AGG}\},\\
\mathrm{ar\_lys}&=\{\texttt{AAA}\},\\
\mathrm{cun}&=\{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\},\\
\mathrm{ile\_met}&=\{\texttt{AUA}\},\\
\mathrm{ssn}&=\{\texttt{UCA},\texttt{UUA}\},\\
\mathrm{stop\_trp}&=\{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}.
\end{aligned}
$$
Every module has predictable or boundary support in the supplied finite row. This gives a compact finite decomposition of the thirteen-codon boundary set into named code-read modules.

\paragraph{Cannot-claim boundary.}
The verified content is: in the six-bit codon cube $Q_6$, the curated standard-code table exposes a thirteen-codon boundary row with punctured-cube shape, nontrivial spectral readout, significant finite-null contrast, and complete module assignment. The verified content is not: translation realization, folded protein structure, physical admissibility, biological function, biochemical mechanism, or universal law. Amino-acid label-sequence readback is still only label readback. Any claim crossing into translation, folding, physical admissibility, function, mechanism, or universality requires a separate reality contact designed for that higher layer.