\origin{ai}
\subsection{Window-six codon boundary row}
\label{sec:bioreality-h0-r-cardinality-13}

\paragraph{Finite carrier and readback contact.}
This record treats the standard-code codon table as a finite code-read contact on the six-bit codon cube $Q_6$.  The carrier is not used as a mechanism for translation.  It is used only as a finite display in which codons occupy coordinate positions and receive curated amino-acid or stop labels from the external standard-code table.  The observed boundary row is the finite set
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The verified row count is
$$
\begin{aligned}
|R| = 13.
\end{aligned}
$$
A separate punctured-cube check returns the same thirteen codons and the face-count vector
$$
\begin{aligned}
(f_0,f_1,f_2) = (13,16,4).
\end{aligned}
$$
Thus the row is a concrete finite boundary tile in the code-read layer: it is a singleton-label and three-point-label split pattern over codon labels, not a claim about molecular dynamics.

\paragraph{Spectral row signature.}
The finite coordinate construction also gives a numerical spectrum for the same local display.  The recorded values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial polynomial residual is
$$
\begin{aligned}
-6.224354365258478\times 10^{-12},
\end{aligned}
$$
with the same value recorded for the cubic-polynomial check.  The small residual is evidence that the finite spectral calculation is internally coherent for this row.  It is not evidence that the genome, ribosome, folded protein, or cellular phenotype is governed by this geometry.

\paragraph{Shape significance under finite nulls.}
The shape check used $N=2000$ sampled controls with seed $42$.  It required $57377$ distinct-sample attempts and incurred $55377$ duplicate retries.  Among these controls there was $1$ exact shape match and $25$ matches at Jaccard threshold at least $0.9$.  The corresponding finite-row probabilities are
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{J\ge 0.9} &= 0.0125.
\end{aligned}
$$
An amino-acid-class-preserving null, also with $N=2000$ and seed $42$, produced $0$ exact matches for the observed shape and $7$ lower-tail samples at or below the observed $M$ size.  The recorded observed size is $|M|=20$, with
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
These numbers support a non-random finite local arrangement relative to the tested nulls.  They do not promote the arrangement to a universal biological law.

\paragraph{Leave-out readback checks.}
The leave-one-code-out test evaluated $23$ code tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$.  The boundary-aware recovery rates were
$$
\begin{aligned}
\mathrm{hit\ rate\ above\ baseline} &= 0.9841269841269841,\\
\mathrm{hit\ rate\ in}\ M\setminus T &= 0.9365079365079365,\\
\mathrm{hit\ rate\ in}\ M\setminus T\ \mathrm{plus\ boundary} &= 0.9841269841269841.
\end{aligned}
$$
The family-level summary covers $5$ families, with hit rates above baseline equal to $0.8333333333333334$ for alternative yeast and $1.0$ for bacterial plastid, candidate other, mitochondrial, and nuclear ciliate families.  The leave-one-codon-out test has recovery rate $1.0$, predictable count $8$, closure-coverage-high count $8$, boundary count $5$, and anchor count $0$.  This supports the claim that the row is useful for code-label readback and local split prediction inside the finite table.

\paragraph{Module partition.}
The thirteen row codons are completely assigned to six named modules.  The recorded module partition is
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The module audit reports $R$ size $13$, assigned count $13$, no extra module codons, no missing row codons, and no unassigned codons.  Each module has either predictable or boundary support.  The finite module account is therefore closed at the level of row membership and code-label classification.

\paragraph{Boundary of the claim.}
The only reality contact used here is the curated standard genetic-code table, whose observed fact is that codons are assigned amino-acid or stop labels.  This contact can test code readback and local codon-tile label split.  It cannot test translation realization, folded structure, structural order, physical admissibility, biological function, mechanism, or universality.  Consequently, the result is a code-layer statement: window-six coordinates expose a finite local boundary tile whose row is $R$, whose cardinality is $13$, whose punctured-cube vector is $(13,16,4)$, and whose readback behavior is strong under the listed leave-out probes.  No translation, folding, physical admissibility, function, mechanism, or universal biological law follows unless a separate higher-layer reality contact supplies it.