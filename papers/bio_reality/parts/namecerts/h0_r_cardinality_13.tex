\origin{ai}

\subsection{Window-six codon boundary in the standard-code table}
\label{sec:bioreality-h0-r-cardinality-13}

\paragraph{Finite carrier and observed boundary row.}
The record uses the six-bit codon cube $Q_6$ as its finite coordinate carrier. Each codon is read as a length-three word over $\{\mathrm{A},\mathrm{C},\mathrm{G},\mathrm{U}\}$, equivalently as a six-coordinate display once the four nucleotides are expanded into two-bit symbols. The finite row singled out by the standard-code contact is the thirteen-codon set
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The verified cardinality statement is therefore $|R|=13$, and the independent row named by the punctured-cube check returns the same thirteen codons. Its finite cell data are summarized by the $f$-vector $(13,16,4)$: thirteen vertices, sixteen one-dimensional adjacencies, and four square faces in the induced local tile. This is a code-layer observation about a boundary-shaped subset of the codon cube, not a statement about translation dynamics or molecular realization.

\paragraph{Label contact and row meaning.}
The only external contact used here is a curated standard genetic-code table assigning each codon to an amino-acid or stop label. That contact is sufficient to compare codon coordinates with label readback, including singleton label corners and three-point label classes. It is not a measurement of a ribosome, a folding trajectory, a biochemical admissibility constraint, or an organism-level function. The row therefore has the following local interpretation: the window-six geometry exposes a small boundary tile whose labels can be read from the curated table, while the labels themselves remain supplied by the biological table rather than by the coordinate geometry alone.

\paragraph{Spectral and shape checks.}
The verified spectral row records $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$. The cubic relation is numerically satisfied with residual $-6.224354365258478\times 10^{-12}$, the same value reported for the radial-polynomial evaluation, and the spectral-radius tolerance row reports $0.6752479308830213$. These numbers support a reproducible finite calculation on the observed tile and its comparison object $M$; they do not turn the tile into a universal biological principle.

\paragraph{Null rows and rarity of the observed shape.}
The shape-significance run used $N=2000$ sampled comparisons with seed $42$. It required $57377$ distinct-sample attempts and produced $55377$ duplicate retries, which records that the sampling space was heavily constrained by the finite shape condition. In that run there was $1$ exact shape match, so $p_{\mathrm{exact}}=0.0005$, and there were $25$ matches at Jaccard threshold at least $0.9$, so $p_{J\ge 0.9}=0.0125$. A separate amino-acid-class-preserving null with $N=2000$ and seed $42$ found observed $|M|=20$, exact match count $0$, lower-tail count $7$, $p_{\mathrm{class,exact}}=0.0$, and $p_{\mathrm{class,lower}}=0.0035$. These rows justify treating the observed window-six arrangement as a non-generic local code-table feature under the tested finite nulls. They do not identify a physical mechanism.

\paragraph{Prediction rows from held-out code and codon tests.}
The leave-one-code-out check evaluated $23$ tables and $63$ targets. Its baseline hit rate was $0.19821739130434785$, while the reported hit rate above baseline was $0.9841269841269841$. The hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus the boundary row was again $0.9841269841269841$. The leave-one-codon-out row reports anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$. These tests make the row useful as a finite predictor of code-layer label placement under the specified omissions. They still stay at the codon-label layer: no conclusion about folding, physical admissibility, biological function, translation realization, mechanism, or universality follows without a separate observation designed for that higher layer.

\paragraph{Family and module decomposition.}
The family-level leave-one-code-out row separates $5$ families. The hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate. The module classification assigns all thirteen $R$ codons and leaves no extra module codons, no missing codons, and no unassigned codons. The modules are
$$
\begin{aligned}
\mathrm{AGR}&=\{\mathrm{AGA},\mathrm{AGG}\},&
\mathrm{AR/Lys}&=\{\mathrm{AAA}\},\\
\mathrm{CUN}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},&
\mathrm{Ile/Met}&=\{\mathrm{AUA}\},\\
\mathrm{SSN}&=\{\mathrm{UCA},\mathrm{UUA}\},&
\mathrm{Stop/Trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
This decomposition records how the thirteen boundary codons are organized into named local modules for code-readback testing. Its completeness statement is purely finite: $R$ has size $13$, the assigned count is $13$, and the residual sets are empty.

\paragraph{Local boundary verdict.}
The finite evidence supports the following chapter-level statement. In the standard-code table, the six-bit codon display contains a thirteen-codon boundary row with punctured-cube cell data $(13,16,4)$, a reproducible spectral profile, rarity under the tested nulls, strong held-out code-layer recovery, and a complete six-module assignment. The row is therefore ready as a finite BioReality record for local codon-label structure. The same record explicitly stops at the code-read layer: it does not by itself assert translation, folded structure, structural order, physical admissibility, biological function, a mechanism, or universality.