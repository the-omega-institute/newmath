\origin{ai}
\subsection{Window-six local tile boundary for the standard code}
\label{sec:biorality-h0-r-cardinality-13}

\paragraph{Finite carrier and observed readback.}
The carrier is the six-bit codon cube $Q_6$, read through a standard-code label table. The contact used here is only the curated codon-to-label table: it assigns each codon to an amino-acid or stop label, and therefore supplies code-layer readback but no translation realization, no folded structure, no physical admissibility test, no function realization, no mechanism, and no universality claim. Within this contact, the finite row distinguished by the local tile computation is the thirteen-codon set
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The recorded cardinality is $|R|=13$, and the row list is exact at the code-read layer. The same row is reported as a punctured-cube support with $f$-vector $(13,16,4)$, so the row is not merely a list of labels: it has a finite face structure inside the six-bit coordinate display. The BEDC minimal form used here consists of a six-bit codon display, a local tile face, a singleton label corner, and a three-point label class, with internal coordinate, closure, spectrum, and relation data. The biological object remains the standard-code codon table, not a biological dynamics.

\paragraph{Spectral and shape evidence.}
The local row sits inside a larger observed shape of size $20$ for $M$. The spectral record gives
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The associated cubic or radial polynomial residual is $-6.224354365258478\times 10^{-12}$, which is numerically at the stated tolerance scale for the finite computation. Against $N=2000$ sampled null rows with seed $42$, the exact shape count is $1$, giving $p_{\mathrm{exact}}=0.0005$. The number of samples with Jaccard score at least $0.9$ is $25$, giving $p_{J\geq 0.9}=0.0125$. The sampling record also reports $57377$ distinct-sample attempts and $55377$ duplicate retries, so the finite null comparison is visibly constrained by the small combinatorial support being sampled. Under the amino-acid-class-preserving null, again with $N=2000$ and seed $42$, the exact match count is $0$, the lower-tail count at or below the observed $M$ size is $7$, the exact-shape probability is $0.0$, and the lower-tail probability is $0.0035$. These numbers support a local code-layer shape anomaly, not a claim about protein structure or physical necessity.

\paragraph{Leave-one readback behavior.}
The leave-one-code-out row uses $23$ evaluated tables and $63$ total targets. Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$. The hit rate inside $M\setminus T$ is $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus boundary is $0.9841269841269841$. This says that the local coordinate-and-boundary pattern has strong readback power for held-out code labels in the tested finite family. It does not say that the six-bit window translates molecules, selects viable proteins, predicts folds, or explains biological function.

\paragraph{Leave-one-codon boundary behavior.}
The leave-one-codon-out run records anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$. Thus the row behaves as a boundary-mediated code-read object rather than as an anchor-determined mechanism. The module classification accounts for all thirteen codons in $R$: the $agr$ module contains $AGA,AGG$; the $ar\_lys$ module contains $AAA$; the $cun$ module contains $CUA,CUC,CUG,CUU$; the $ile\_met$ module contains $AUA$; the $ssn$ module contains $UCA,UUA$; and the $stop\_trp$ module contains $UAA,UAG,UGA$. The classification record gives $R$ size $13$, assigned count $13$, no extra module codons, no missing codons from modules, and no unassigned codons. At the family level, five families are present: alternative yeast has hit rate above baseline $0.8333333333333334$, while bacterial plastid, candidate other, mitochondrial, and nuclear ciliate each have hit rate above baseline $1.0$.

\paragraph{Local verdict and boundary.}
The finite record supports the following code-read statement: window-six codon coordinates expose a local boundary-tile structure in the standard-code label table, with the distinguished row $R$ of size $13$, punctured-cube face data $(13,16,4)$, stable module accounting across all thirteen row codons, and strong leave-one readback rates. The reality contact is exactly the curated standard-code table. Therefore the chapter may claim a finite local tile at the codon-label layer. It may not claim translation realization, folded structure, structural order, physical admissibility, biological function, mechanism, or universality. Any such higher-layer statement requires a separate contact carrying that layer of observation.