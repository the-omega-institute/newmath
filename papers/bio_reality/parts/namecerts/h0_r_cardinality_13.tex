\origin{ai}
\subsection{Window-six local tile boundary in the standard-code codon table}
\label{sec:bioreality-h0-r-cardinality-13}

\paragraph{Carrier and contact.}
The finite carrier is the six-coordinate codon cube $Q_6$ equipped with a standard-code label readout. The biological contact used here is deliberately narrow: a curated standard genetic-code table assigns each codon to an amino-acid label or to stop. This contact tests only code-layer label readback and local codon-tile splits. It does not observe translation dynamics, folded protein geometry, physical admissibility, biological function, or a universal mechanism. Those stronger readings require separate observations, and no such observation is part of this finite row.

\paragraph{The boundary set $R$.}
The row isolates a boundary region $R$ of cardinality $13$. The complete listed set is
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The recorded value $R\_cardinality\_equals\_13 = 13$ agrees with the explicit enumeration and with $size = 13$. The same thirteen codons also appear in the punctured-cube row, where the face data have $f$-vector $(13,16,4)$. Thus the finite content is not merely a count: the row supplies the named codons and the local tile combinatorics in which the count is realized.

\paragraph{Local geometry and spectrum.}
The spectral row records $\lambda_M = 0.6752479308830213$, $\lambda_R = 0.4758352843197002$, and $\mu = 3.051487585298128$. The cubic and radial residuals are both $-6.224354365258478\times 10^{-12}$, so the numerical witness is internally consistent at the displayed tolerance. Interpreted at the code-read layer, these numbers say that the selected codon tile has a stable finite spectral profile. They do not say that the tile is a biochemical law. The spectrum is a finite coordinate measurement attached to the codon-label table, not an account of ribosomal realization or protein structure.

\paragraph{Null comparisons.}
The shape-significance experiment used $N=2000$ samples with seed $42$. Among those samples there was $1$ exact shape match, giving $p\_exact = 0.0005$, and $25$ samples had Jaccard overlap at least $0.9$, giving $p\_{J\ge 0.9} = 0.0125$. The row also records $57377$ distinct-sample attempts and $55377$ duplicate retries, so the comparison is tied to a concrete finite sampling process rather than to an informal visual resemblance. Under the amino-acid-class-preserving null with $N=2000$ and seed $42$, the exact match count is $0$, the lower-tail count at the observed $M$ size is $7$, the observed $M$ size is $20$, the exact-shape value is $0.0$, and the lower-tail value is $0.0035$. These null rows support the claim that the local tile is unusual relative to the recorded finite randomizations. They still remain code-layer evidence: they compare codon-label arrangements and tile shapes, not protein folding or organismal function.

\paragraph{Leave-one readbacks.}
The leave-one-code-out row evaluates $23$ tables and $63$ targets. It records a baseline hit rate of $0.19821739130434785$, a hit rate above baseline of $0.9841269841269841$, a hit rate in $M-T$ of $0.9365079365079365$, and a hit rate in $M-T$ plus boundary of $0.9841269841269841$. The family breakdown has $5$ families: alternative yeast has $0.8333333333333334$, while bacterial plastid, candidate other, mitochondrial, and nuclear ciliate each have $1.0$. The leave-one-codon-out row records $anchor\_count=0$, $boundary\_count=5$, $closure\_coverage\_high=8$, $predictable\_count=8$, and $recovery\_rate=1.0$. These data make the local boundary operational: withheld labels are often recovered by the finite code-layer geometry together with its boundary region. They do not promote the geometry into a global biological principle.

\paragraph{Module accounting.}
The module classification accounts for all $13$ codons in $R$, with $assigned\_count=13$, $extra\_module\_codons=\emptyset$, $missing\_from\_modules=\emptyset$, and $unassigned\_codons=\emptyset$. The modules are
$$
\begin{aligned}
agr &= \{AGA, AGG\},\\
ar\_lys &= \{AAA\},\\
cun &= \{CUA, CUC, CUG, CUU\},\\
ile\_met &= \{AUA\},\\
ssn &= \{UCA, UUA\},\\
stop\_trp &= \{UAA, UAG, UGA\}.
\end{aligned}
$$
Every module has either predictable or boundary status in the recorded classification. This is the strongest local conclusion justified by the finite row: the thirteen boundary codons form a completely assigned local code-read module system inside the six-coordinate codon display.

\paragraph{Scientific boundary.}
The claim is ready at the code-read layer. The curated table supplies codon-to-label contact, the $13$-codon boundary set is explicit, the punctured-cube data give $(13,16,4)$, the spectral values are numerically coherent, the null comparisons are finite and non-generic, and the leave-one rows show strong code-layer recovery. The same evidence also fixes the stopping point. No translation process, folded structure, physical admissibility condition, biological function, mechanism, or universality theorem follows from this row. Amino-acid label sequences remain label readbacks until a separate structure or function contact is supplied.