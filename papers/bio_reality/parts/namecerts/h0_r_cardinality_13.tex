\subsection{Window-six codon boundary row}\label{subsec:bioreality-h0-r-cardinality-13}
\origin{ai}

\paragraph{Finite coordinate record.}
The row fixes a six-bit codon display for the standard-code label table and reads only the code layer.  In this record the distinguished boundary set is
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The verified cardinality is $|R|=13$, with the same thirteen codons appearing in the punctured-cube check.  The associated cell-count vector is $(13,16,4)$: thirteen selected codon vertices, sixteen local adjacencies, and four local faces in the tile summary.  The biological contact used here is a curated standard-code table assigning codons to amino-acid or stop labels.  It supplies label readback only; it is not an observation of translation dynamics, protein folding, physical admissibility, biological function, a general mechanism, or a universal biological law.

\paragraph{Local shape and spectral summary.}
The local geometric row is not treated as a symbolic coincidence.  In the shape-significance run with seed $42$, the comparison used $N=2000$ sampled alternatives, requiring $57377$ distinct-sample attempts and $55377$ duplicate retries.  Exactly one sampled table matched the observed shape, giving $p_{\mathrm{exact}}=0.0005$.  Twenty-five samples reached Jaccard similarity at least $0.9$, giving $p_{J\geq 0.9}=0.0125$.  The amino-acid-class-preserving null model used the same $N=2000$ and seed $42$; it produced $0$ exact matches, a lower-tail count of $7$ at the observed $M$ size, observed $|M|=20$, $p_{\mathrm{class,exact}}=0.0$, and $p_{\mathrm{class,lower}}=0.0035$.  These numbers support the statement that the observed local shape is unusually placed relative to the tested finite alternatives, while still remaining a code-table statement.

\paragraph{Radial spectrum.}
The row also carries a finite spectral check.  The recorded values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\times 10^{-12}$, and the same value is recorded as the cubic-polynomial residual.  The spectral radius in tolerance is $0.6752479308830213$.  These scalars are used as finite consistency data for the window-six construction: they say that the selected codon boundary, its closure context, and the associated local spectrum agree within numerical tolerance.  They do not say that the spectrum is a physical energy, a folding score, or a mechanistic law.

\paragraph{Leave-one checks.}
The leave-one-code-out row evaluated $23$ code tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M\setminus T$ is $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus the boundary is $0.9841269841269841$.  By family, the five recorded classes have hit rates above baseline
$$
\begin{aligned}
\mathrm{alternative\ yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\ plastid} &= 1.0,\\
\mathrm{candidate\ other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\ ciliate} &= 1.0.
\end{aligned}
$$
The leave-one-codon-out row records $0$ anchor codons, $5$ boundary codons, $8$ high-coverage closure cases, $8$ predictable cases, and recovery rate $1.0$.  Thus the finite test data make the boundary row recoverable in the code-label regime, not in any higher biological regime.

\paragraph{Module partition.}
The thirteen $R$ codons are completely assigned to six named modules.  The module decomposition is
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The classification record has $R$ size $13$, assigned count $13$, no extra module codons, no missing codons from the modules, and no unassigned codons.  Every module contains either a predictable codon or a boundary codon under the recorded tests.  This gives a closed finite row for the local codon-tile split: singleton-corner behavior, three-point label-class behavior, and local-face behavior can be inspected inside one six-bit codon display.

\paragraph{Scope of the claim.}
The chapter claim is therefore narrow.  The curated table contact lets the row compare codon coordinates with standard-code labels, and the finite computations verify that the selected $R$ row has cardinality $13$, punctured-cube cell vector $(13,16,4)$, stable module coverage, nontrivial null-model separation, and reproducible leave-one recovery.  Nothing in this row observes ribosomal translation, folded protein structure, admissible molecular physics, organismal function, evolutionary necessity, or a universal rule for biology.  Any such promotion would require a separate contact that actually measures the higher layer.  Until that contact is supplied, the row is a local code-read boundary record and no more.