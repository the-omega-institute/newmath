\subsection{Window-Six Codon Boundary Row}
\label{subsec:biorality-h0-r-cardinality-13}
\origin{ai}

\paragraph{Finite carrier and row statement.}
The finite carrier is the six-bit codon cube $Q_6$ read through the standard-code label table.  In this row the distinguished boundary set $R$ has cardinality $13$.  The verified row is
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\},
\end{aligned}
$$
and the recorded scalar check is $|R|=13$.  The same finite set is also the punctured-cube row in the local tile computation, with face data summarized by the verified $f$-vector $(13,16,4)$.  Thus the object asserted here is not a vague codon regularity: it is a concrete finite subset of the codon cube, together with a local incidence profile measured on the row itself.

\paragraph{Readback contact.}
The only reality contact used by this row is the curated standard genetic-code table.  That contact supplies codon-to-label readback at the code layer: a codon is assigned an amino-acid or stop label.  It does not supply a ribosomal translation event, a folded three-dimensional structure, physical admissibility, biological function, a mechanism, or a universality theorem.  The claim is therefore confined to code readback: window-six coordinates expose a local boundary-tile split, while the biological labels remain external contacts supplied by the curated table.

\paragraph{Spectral row.}
The spectral computation attached to the same row gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The reported cubic-polynomial residual and radial-polynomial residual are both $-6.224354365258478\times 10^{-12}$, so the finite numerical row is internally stable at the displayed tolerance.  In compact form the checked scalar package is
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128,\\
P_{\mathrm{rad}}(\mu) &= -6.224354365258478\times 10^{-12}.
\end{aligned}
$$
These values are used only as finite-row diagnostics for the local codon tile.  They are not promoted to biochemical energy, folding order, or evolutionary law.

\paragraph{Shape significance and null rows.}
Under the shape-significance experiment with $N=2000$ sampled rows, seed $42$, and $57377$ distinct-sample attempts, the run recorded $55377$ duplicate retries, $1$ exact shape match, and $25$ matches with Jaccard score at least $0.9$.  The corresponding finite-row probabilities are
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9} &= 0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null experiment used the same sample count $N=2000$ and seed $42$.  It recorded observed $M$-size $20$, exact-match count $0$, and lower-tail count $7$, giving
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
No explicit member list for $M$ is part of this row package; only its verified size and null-model statistics are used here.  These finite null rows support the statement that the observed local boundary shape is atypical against the tested combinatorial controls, not the stronger statement that the geometry is a biological necessity.

\paragraph{Leave-one-code-out behavior.}
The leave-one-code-out probe evaluated $23$ code tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$.  The hit rate above baseline was $0.9841269841269841$, the hit rate in $M-T$ was $0.9365079365079365$, and the hit rate in $M-T$ plus boundary was $0.9841269841269841$.  By family, the finite hit-rate row has $5$ families: alternative yeast has rate $0.8333333333333334$, while bacterial-plastid, candidate-other, mitochondrial, and nuclear-ciliate each have rate $1.0$.  This is evidence that the boundary-tile readback is stable across the tested code-table family rows at the code layer.  It is still not evidence for translation realization, protein structure, physical admissibility, function, mechanism, or universality.

\paragraph{Leave-one-codon-out and module classification.}
The leave-one-codon-out run records anchor count $0$, boundary count $5$, closure coverage high $8$, predictable count $8$, and recovery rate $1.0$.  The module classification accounts for all $13$ members of $R$: assigned count is $13$, $R$-size is $13$, with no extra module codons, no missing members, and no unassigned codons.  The module rows are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA, AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA, CUC, CUG, CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA, UUA\},\\
\mathrm{stop\_trp} &= \{UAA, UAG, UGA\}.
\end{aligned}
$$
Every listed module has a predictable-or-boundary witness in the finite computation.  The classification is therefore complete for this row: it partitions the verified $13$ codons of $R$ into six named local modules and leaves no codon outside the module accounting.

\paragraph{Boundary verdict.}
The row is ready as a BioReality namecert record at the code-read layer.  It asserts a finite local pattern in the six-bit codon cube, anchored by a curated standard-code label table and supported by the displayed finite-row statistics.  The cannot-claim boundary is part of the claim itself: no translation, folding, structural order, physical admissibility, biological function, universal mechanism, or global biological law follows from this local codon tile unless a separate higher-layer reality contact is supplied.