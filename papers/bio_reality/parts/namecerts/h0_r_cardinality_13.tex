\origin{ai}

\subsection{Window-Six Boundary Row for the Standard Code}
\label{sec:bior-reality-h0-r-cardinality-13}

\paragraph{Finite carrier and readback contact.}
The record $h0.R.cardinality.13$ is a finite code-read observation on the six-bit codon cube $Q_6$.  The carrier is not a biochemical mechanism; it is the coordinate display obtained by writing the standard-code codons as finite six-coordinate rows and then asking which rows appear in the local boundary tile selected by the probe.  The only external contact used here is the curated standard genetic-code table, whose observed fact is that each codon has an amino-acid or stop label.  Thus the readback is a standard-code label table, not translation, folding, physical admissibility, function, mechanism, or universality.

The boundary row $R$ has exactly thirteen codons:
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The measured size is
$$
\begin{aligned}
|R|=13,
\end{aligned}
$$
and the finite-row certificate reports the same number as $R\_cardinality\_equals\_13=13$.  The associated punctured-cube check gives the face-vector
$$
\begin{aligned}
f(R)=(13,16,4),
\end{aligned}
$$
so the observation is not merely a list of codons: it is a local tile with thirteen vertices, sixteen one-dimensional incidences, and four two-dimensional faces in the induced window-six geometry.

\paragraph{Spectral fingerprint.}
The same finite row has a numerical spectrum attached to the local split.  The reported values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial polynomial residual is $-6.224354365258478\cdot 10^{-12}$, and the cubic-polynomial residual is the same reported value.  This places the spectral check inside numerical tolerance for the finite row.  The interpretation is deliberately narrow: the numbers certify a coordinate-and-relation fingerprint of the local codon tile.  They do not say that the biological process of translation is generated by the spectrum, nor that an amino-acid label sequence has acquired any folded structure.

\paragraph{Shape significance against finite nulls.}
Two finite null contacts are recorded for the shape.  In the direct shape experiment, $N=2000$ samples were evaluated with seed $42$.  The sampler required $57377$ distinct-sample attempts and encountered $55377$ duplicate retries.  There was $1$ exact shape match, giving
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}
$$
There were $25$ samples with Jaccard similarity at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{Jaccard}\ge 0.9}=0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null used $N=2000$ samples with the same seed.  It observed $0$ exact matches, $7$ lower-tail samples at or below the observed $M$ size, and an observed $M$ size of $20$.  Its reported probabilities are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
These finite-row numbers support the claim that the local tile is a nontrivial code-layer pattern under the tested nulls.  They still do not promote the tile into a universal biological law.  The contact remains the curated code table plus finite coordinate probes.

\paragraph{Leave-one recovery checks.}
The leave-one-code-out experiment evaluated $23$ code tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$.  The measured hit rate above baseline was $0.9841269841269841$, the hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus the boundary was $0.9841269841269841$.  These values say that the local coordinate structure is predictive at the code-read layer under the supplied finite protocol.

The leave-one-codon-out row reports $0$ anchors, $5$ boundary codons, $8$ high-coverage closure cases, $8$ predictable cases, and recovery rate $1.0$.  The recovery-rate threshold check is also $1.0$.  This is a finite recovery certificate for the coordinate display, not a claim that missing biological function can be recovered from geometry alone.

\paragraph{Family and module decomposition.}
The leave-one-code-out family split has $5$ families.  The family hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate.  The module classification is complete for the thirteen codons in $R$: it reports $R\_size=13$, $assigned\_count=13$, no extra module codons, no missing codons from modules, and no unassigned codons.  The modules are
$$
\begin{aligned}
agr &= \{AGA,AGG\},\\
ar\_lys &= \{AAA\},\\
cun &= \{CUA,CUC,CUG,CUU\},\\
ile\_met &= \{AUA\},\\
ssn &= \{UCA,UUA\},\\
stop\_trp &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
Every listed module has a predictable-or-boundary witness in the finite classification.  This is the strongest local reading supported by the data: the thirteen-row boundary tile is organized by codon modules visible through the curated standard-code labels and the derived coordinate probes.

\paragraph{Local cannot-claim boundary.}
The claim is therefore ready only at the code-read layer.  The row $R$ exposes a finite, locally significant, module-complete boundary tile in the six-bit codon display, with spectral and null-model checks attached.  The same evidence explicitly blocks higher-layer promotion.  No translation realization, folded protein structure, physical admissibility, biological function, universal mechanism, or claim that DNA must obey this BEDC geometry follows from the row.  Any such claim would require a separate external contact that observes that higher layer directly.