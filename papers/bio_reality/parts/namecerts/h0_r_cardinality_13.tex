\subsection{Window-Six Codon Boundary Row}\label{subsec:h0-r-cardinality-13}
\origin{ai}

\paragraph{Finite carrier.}
The finite carrier for this record is the six-bit codon cube $Q_6$, read only through a standard-code codon-label table.  The observed boundary row is a thirteen-codon subset $R$ of the codon cube.  In the verified finite row, the cardinality computation gives
$$
\begin{aligned}
|R| &= 13,
\end{aligned}
$$
and the row itself is
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
This is a code-read statement: the row is not being treated as a biochemical mechanism.  The contact used here is the curated standard-code table, whose role is to attach amino-acid or stop labels to codons.  That contact supplies a label readback, not translation realization, folding, physical admissibility, function, mechanism, or universality.

\paragraph{Punctured-cube shape.}
The same row is verified as the punctured-cube boundary row with face-vector
$$
\begin{aligned}
(f_0,f_1,f_2) &= (13,16,4).
\end{aligned}
$$
Thus the row has thirteen vertices, sixteen one-dimensional adjacencies, and four local two-dimensional faces in the selected window-six tile.  This supports the local geometric description of the codon display: coordinate, closure, spectrum, and relation data are available inside the finite carrier.  It does not support a claim that DNA is governed by this geometry as a biological necessity.  The only admissible readback is that the curated code labels interact with this local tile in a detectable way.

\paragraph{Spectral row.}
The spectral finite row reports
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial cubic check is numerically closed at
$$
\begin{aligned}
-6.224354365258478\cdot 10^{-12},
\end{aligned}
$$
which is also recorded as the cubic-polynomial residual.  The spectral radius value in tolerance is $0.6752479308830213$.  These numbers make the boundary row a finite spectral object, but the numbers remain code-layer measurements.  They do not identify a protein fold, a physical admissibility constraint, a functional phenotype, or a universal origin principle.

\paragraph{Shape significance.}
Against the reported null sample with seed $42$, the shape-significance row used $N=2000$ sampled alternatives.  It required $57377$ distinct-sample attempts and $55377$ duplicate retries.  There was $1$ exact shape match, giving
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,
\end{aligned}
$$
and there were $25$ high-Jaccard matches at threshold $0.9$, giving
$$
\begin{aligned}
p_{J\geq 0.9} &= 0.0125.
\end{aligned}
$$
A separate amino-acid-class-preserving null row, also with $N=2000$ and seed $42$, reports observed $|M|=20$, exact-match count $0$, and lower-tail count $7$.  Its exact-shape value is
$$
\begin{aligned}
p_{\mathrm{aa\ class,exact}} &= 0.0,
\end{aligned}
$$
while the lower-tail value is
$$
\begin{aligned}
p_{\mathrm{aa\ class,lower}} &= 0.0035.
\end{aligned}
$$
These finite-row values make the local tile non-generic under the recorded nulls.  They do not by themselves license any higher-layer biological conclusion.

\paragraph{Leave-one readbacks.}
The leave-one-code-out finite row evaluated $23$ code tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$, while the observed hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$.  The leave-one-codon-out row reports anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate
$$
\begin{aligned}
\rho_{\mathrm{recovery}} &= 1.0.
\end{aligned}
$$
The family-resolved code-out row has $5$ families.  The family hit-rate-above-baseline values are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate.  These rows show that the local tile has predictive code-read structure under the supplied finite tests.

\paragraph{Module closure.}
The module classification assigns all thirteen $R$ codons, with $R\_size=13$, assigned count $13$, no extra module codons, no missing module codons, and no unassigned codons.  The recorded modules are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
Every module has predictable-or-boundary support in the finite row.  This is the strongest local conclusion available here: the thirteen-codon row is a closed, classified, locally structured code-read object in the six-bit codon display.

\paragraph{Boundary of the claim.}
The cannot-claim boundary is part of the mathematical content of this record.  A local codon tile is not a global biological law.  Window-six geometry alone is not translation, not folded protein structure, not physical admissibility, not biological function, not a mechanism, and not a universality theorem.  Amino-acid label-sequence readback is only label readback unless a separate reality contact supplies the higher layer.  The curated standard-code table supports codon-to-label comparison and local split checks; it blocks promotion to translation, folding, physical admissibility, function, mechanism, or universality.