\origin{ai}
\subsection{Window-six codon boundary row}
\label{sec:bioreality-h0-r-cardinality-13}

\paragraph{Finite coordinate object.}
This BioReality record concerns the standard-code codon table only at the code-read layer.  The carrier is the six-bit codon cube $Q_6$, read through a local tile face, a singleton label corner, and a three-point label class.  The finite row singled out by the probe is the boundary set $R$.  In the verified run $5d4a3459997d$, this set has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
Thus the row claim is not a narrative assertion about biology in general; it is the finite statement that the local window-six construction returns these thirteen codons from the curated standard-code label table.  The independent punctured-cube check, run $e19917012c60$, returns the same $R$ list and the face-count vector $(13,16,4)$.  The record therefore has a concrete combinatorial footprint: thirteen vertices, sixteen local adjacencies, and four two-dimensional faces in the observed punctured tile.

\paragraph{Spectral and shape evidence.}
The associated spectrum is numerically stable in the finite-row audit.  Run $1026324eff41$ reports $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The same run evaluates the radial cubic residual as $-6.224354365258478\times 10^{-12}$, recorded both as the cubic-polynomial value and the radial-polynomial value.  These values support the local coordinate diagnosis: $R$ is not merely a list of labels, but a row whose position inside the six-bit codon cube has a reproducible spectral signature.

The shape null comparison also remains finite and explicit.  In run $0b267c88f8e4$, $N=2000$ sampled alternatives required $57377$ distinct-sample attempts, with $55377$ duplicate retries.  The exact shape match count was $1$, giving $p_{\mathrm{exact}}=0.0005$.  The high-overlap count at Jaccard threshold $0.9$ was $25$, giving $p_{J\geq 0.9}=0.0125$.  A stricter amino-acid-class-preserving null, run $e8d5de747a0c$, used $N=2000$, found $0$ exact matches, and reported $7$ lower-tail samples at or below the observed $M$ size $20$.  Its exact-shape value was $0.0$, while the lower-tail value was $0.0035$.  The supplied data do not identify the individual members of $M$, so the chapter uses only the finite scalar facts attached to $M$: observed size $20$, spectral radius $\lambda_M$, and the two null-model p-values.

\paragraph{Code-read recovery checks.}
The leave-one-code-out audit tests whether the same local boundary information recovers code-table differences without promoting the result beyond the code layer.  Run $429e6e2c4eb4$ evaluated $23$ tables and $63$ targets.  The baseline hit rate was $0.19821739130434785$, while the hit rate in $M-T$ was $0.9365079365079365$ and the hit rate in $M-T$ plus boundary was $0.9841269841269841$.  The same $0.9841269841269841$ value is recorded as the hit rate above baseline.  By family, run $7dda978c6c97$ reports $5$ families: alternative yeast has rate $0.8333333333333334$, while bacterial plastid, candidate other, mitochondrial, and nuclear ciliate each have rate $1.0$.

The leave-one-codon-out audit gives a second finite check at the codon level.  Run $e9e1a6e41633$ reports anchor count $0$, boundary count $5$, predictable count $8$, high closure coverage $8$, and recovery rate $1.0$.  The module classification run $bbe8a4d0455b$ accounts for all thirteen $R$ codons with no missing or extra assignments: $R$ size $13$, assigned count $13$, missing-from-modules list empty, unassigned-codons list empty, and extra-module-codons list empty.  The six modules are
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
Every module therefore contains a predictable or boundary codon under the recorded finite classification.

\paragraph{Reality contact and boundary.}
The only reality contact used here is the curated standard genetic-code bridge: a table assigning codons to amino-acid or stop labels.  This contact can test code readback and local codon-tile label splits.  It cannot test translation realization, folded structure, structural order, physical admissibility, biological function, universal mechanism, or any claim that DNA must obey this geometry as a biological necessity.  The observed alignment is consequently narrow: the standard-code table supports the finite $R$ row, its punctured-cube footprint, its spectral values, its null-model rarity, and its code-layer recovery behavior.  It does not supply a protein structure contact, a translation mechanism contact, or a function assay.

The operative cannot-claim boundary is local and binding.  From the data above one may say that window-six codon coordinates expose a reproducible local boundary-tile structure in the curated standard-code label table.  One may not infer translation, folding, physical admissibility, biological function, mechanism, or universality unless a separate reality contact supplies that higher-layer evidence.  Amino-acid label-sequence readback remains label readback; it is not folded protein structure.