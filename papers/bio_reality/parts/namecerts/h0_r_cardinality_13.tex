\subsection{Window-six codon boundary row}
\label{subsec:bior reality-h0-r-cardinality-13}
\origin{ai}

\paragraph{Finite carrier and readback contact.}
This record concerns the standard-code codon table as a finite code-read object on the six-bit codon cube $Q_6$.  The carrier is not a molecular mechanism; it is the coordinate display in which each codon is read as a finite word over the four nucleotide labels and then placed in a window-six geometry.  The external contact is the curated standard-code label table: it supplies codon-to-amino-acid or codon-to-stop labels, and it does not by itself supply translation realization, folded structure, physical admissibility, biological function, a universal mechanism, or any necessity theorem for DNA.  In this chapter the admissible claim is therefore local: the coordinate-and-label readback exposes a boundary row $R$ inside the code layer.

\paragraph{The thirteen-row boundary set.}
The finite computation returns a boundary set of cardinality $13$.  The row is exactly
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
Thus the checked scalar is
$$
\begin{aligned}
|R|=13.
\end{aligned}
$$
The same row is also identified as a punctured-cube boundary with face data
$$
\begin{aligned}
f(R)=(13,16,4),
\end{aligned}
$$
where the three entries record the finite counts reported by the local tile computation.  This is the chapter's central finite-row datum: the table contact supplies the labels, while the window-six display supplies the coordinate relation in which these thirteen codons become the boundary row.

\paragraph{Spectral scalars.}
The associated spectral check reports three numerical quantities.  The $M$-side scalar is $\lambda_M=0.6752479308830213$, the $R$-side scalar is $\lambda_R=0.4758352843197002$, and the coupling parameter is $\mu=3.051487585298128$.  The radial cubic residual is reported as
$$
\begin{aligned}
-6.224354365258478\cdot 10^{-12},
\end{aligned}
$$
so the polynomial check is numerically at zero at the displayed precision.  These scalars support a finite coordinate signature for the row; they do not upgrade the row into a biological law.

\paragraph{Null-model pressure.}
The shape is not treated as a visual coincidence.  Against the reported finite null sample with $N=2000$, the exact-shape count is $1$, giving $p_{\mathrm{exact}}=0.0005$.  The high-overlap threshold $J\geq 0.9$ has $25$ matches, giving $p_{J\geq 0.9}=0.0125$.  The run required $57377$ distinct-sample attempts and recorded $55377$ duplicate retries, so the finite sampling regime is explicitly visible rather than hidden behind an asymptotic statement.  A second amino-acid-class-preserving null reports observed $M$-size $20$, exact-match count $0$, and lower-tail count $7$ out of $2000$, giving
$$
\begin{aligned}
p_{\mathrm{aa\ class,exact}}&=0.0,\\
p_{\mathrm{aa\ class,lower\ tail}}&=0.0035.
\end{aligned}
$$
These numbers justify preserving the row as a concrete finite anomaly candidate at the code-read layer.  They do not assert translation dynamics, protein structure, or function.

\paragraph{Leave-one contacts.}
The leave-one-code-out probe evaluated $23$ tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate in $M-T$ is $0.9365079365079365$ and the hit rate in $M-T$ plus boundary is $0.9841269841269841$.  The same value, $0.9841269841269841$, is reported as the hit rate above baseline.  By family, the hit rates above baseline are $0.8333333333333334$ for alternative yeast and $1.0$ for bacterial or plastid, candidate other, mitochondrial, and nuclear ciliate families.  The leave-one-codon-out probe records anchor count $0$, boundary count $5$, predictable count $8$, high closure coverage $8$, and recovery rate $1.0$.  These figures say that the boundary row has finite predictive readback in the curated label table; they do not say that the coordinate pattern manufactures the biological code.

\paragraph{Module partition.}
The thirteen codons in $R$ are completely assigned to six named modules.  The module partition is
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The module audit reports $R$-size $13$, assigned count $13$, no extra module codons, no codons missing from the modules, and no unassigned codons.  Hence the row is not only a cardinality statement; it is a fully classified finite boundary row inside the standard-code readback.

\paragraph{Scope boundary.}
The result is a local tile statement.  The curated standard-code table is a reality bridge for codon labels, not a mechanism and not a universality proof.  The permitted conclusion is that the window-six coordinate display, when read through the curated standard-code labels, isolates a thirteen-codon boundary row with the displayed punctured-cube face vector, spectral scalars, null-model pressure, leave-one recovery behavior, and complete module classification.  The forbidden conclusion is equally part of the record: no translation realization, no folded protein structure, no physical admissibility, no biological function, no global mechanism, and no universality claim follows from this row unless a separate higher-layer contact supplies that evidence.