\subsection{Window-Six Codon Tile Boundary}\label{sec:bioreality-h0-r-cardinality-13}
\origin{ai}

\paragraph{Finite carrier and observed boundary row.}
The finite carrier is the six-bit codon cube $Q_6$, read only at the code layer through a curated standard-code table.  The observed boundary row $R$ has cardinality $13$.  In the supplied finite run, the row is exactly
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
    &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
Thus the primary finite assertion is not a biological law but a codon-display fact: the local tile cut selects thirteen named codons inside the window-six display.  The same row is also recorded as a punctured-cube pattern with $f$-vector
$$
\begin{aligned}
f(R)=(13,16,4),
\end{aligned}
$$
so the numerical record contains thirteen vertices, sixteen local adjacency edges, and four local two-dimensional faces in the induced tile complex.  This is the full content of the boundary observation at this layer: a finite subset of the codon cube, with its local face data, whose labels are read back from the external standard-code contact.

\paragraph{Spectral and null-model coordinates.}
The finite spectral probe attached to the same codon window records
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The reported radial or cubic residual is $-6.224354365258478\cdot 10^{-12}$, which is numerically at tolerance scale for the finite computation.  Against a shape null with $N=2000$ sampled tables, the run attempted $57377$ distinct samples and encountered $55377$ duplicate retries.  It found $1$ exact shape match, giving $p_{\mathrm{exact}}=0.0005$, and $25$ high-Jaccard matches with threshold at least $0.9$, giving $p_{J\geq 0.9}=0.0125$.  A separate amino-acid-class-preserving null used $N=2000$ samples, observed $|M|=20$, found $0$ exact shape matches, and counted $7$ samples in the lower tail at or below the observed $M$ size.  The corresponding reported values are
$$
\begin{aligned}
p_{\mathrm{aa\ class,exact}}&=0.0,\\
p_{\mathrm{aa\ class,lower\ tail}}&=0.0035.
\end{aligned}
$$
These numbers support a finite-row distinction between the observed code-label geometry and the tested null rows.  They do not by themselves identify a biochemical mechanism.

\paragraph{Readback and recovery checks.}
The leave-one-code-out finite row evaluated $23$ tables and $63$ targets.  Its baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary was again $0.9841269841269841$.  The leave-one-codon-out check reports $0$ anchors, $5$ boundary cases, $8$ high-coverage closure cases, $8$ predictable cases, and recovery rate $1.0$.  By family, the five reported families have above-baseline hit rates
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
These rows say that the local tile is stable under the supplied code-read perturbation tests: the boundary geometry continues to recover most held-out label positions relative to the finite baseline.

\paragraph{Module accounting.}
The thirteen codons in $R$ are completely assigned by the supplied module classification.  The six modules are
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
The accounting row gives $R\_size=13$ and assigned count $13$, with no extra module codons, no missing codons, and no unassigned codons.  Every listed module has either predictable or boundary status in the supplied row.  This is the strongest local combinatorial reading of the claim: the row $R$ is not merely a cardinality count but a fully partitioned set of thirteen codon labels inside the window-six boundary tile.

\paragraph{Reality-contact boundary.}
The only reality contact used here is the curated standard-code table, whose observed fact is that codons are assigned to amino-acid or stop labels.  That contact can test code readback and a local codon-tile label split.  It cannot test translation realization, folded structure, structural order, physical admissibility, biological function, mechanism, or universality.  Consequently, the finite result may be stated as follows: window-six codon coordinates expose a local boundary-tile structure with $|R|=13$, the explicit codon list above, punctured-cube face vector $(13,16,4)$, spectral coordinates, null-model separation, recovery rows, and complete module accounting.  No claim about translation, folding, physical admissibility, biological function, universal mechanism, or biological necessity follows unless a separate higher-layer contact supplies that information.