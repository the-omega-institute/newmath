\origin{ai}

\subsection{Window-six codon boundary row}
\label{sec:bior-h0-r-cardinality-13-window-six-boundary-row}

\paragraph{Finite carrier.}
The row uses the six-bit codon cube $Q_6$ as its finite display space.  A codon is read as a point in this cube, while the amino-acid or stop annotation is not generated by the cube itself.  The annotation is supplied by the curated standard-code table, which is treated here as an observed code-label contact.  The finite computation isolates a distinguished boundary set $R$ of thirteen codons:
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The verified cardinality row records $|R|=13$, with the same thirteen entries appearing in the expected codon list and in the size field.  Thus the primary assertion of this packet is not a biological mechanism claim; it is the finite statement that the window-six coordinate calculation picks out exactly these thirteen codon positions at the code-read layer.

\paragraph{Local tile shape.}
The companion punctured-cube row identifies the same $R$ as a local boundary tile with face data
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
This says that the selected codons form a thirteen-vertex local object with sixteen one-dimensional incidences and four two-dimensional faces in the induced cube geometry.  The row is therefore more structured than a bare list, but its meaning remains local: it records a finite shape inside $Q_6$ and a code-label readback against the curated table.  It does not by itself describe translation dynamics, molecular folding, chemical admissibility, biological function, a universal mechanism, or any global law over organisms.

\subsection{Spectral and sampling checks}
\label{sec:bior-h0-r-cardinality-13-spectral-sampling-checks}

\paragraph{Spectral row.}
The spectrum row attaches numerical invariants to the same finite object.  The recorded values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The associated radial cubic check evaluates to $-6.224354365258478\times 10^{-12}$, and the same value is recorded as the cubic-polynomial residual.  At this numerical tolerance the spectral computation is consistent with the finite tile data.  The spectral entry supports the internal coherence of the local codon geometry; it does not promote the codon table contact into an observation of protein structure or cellular realization.

\paragraph{Shape significance.}
The shape-significance run used $N=2000$ samples with seed $42$.  Among those samples there was $1$ exact shape match, giving $p_{\mathrm{exact}}=0.0005$.  The high-overlap count was $25$ at Jaccard threshold $0.9$, giving $p_{J\geq 0.9}=0.0125$.  The sampling log also records $57377$ distinct-sample attempts and $55377$ duplicate retries.  These numbers make the observed local shape uncommon under the sampled comparison procedure, especially at exact shape equality.  They do not identify a causal origin for the genetic code and do not test whether the local tile is realized as a translation apparatus.

\paragraph{Amino-acid-class preserving null.}
A stricter comparison preserving amino-acid class used $N=2000$ samples with the same seed.  The observed $M$ size was $20$.  The exact-shape count was $0$, so the reported exact-shape value is $p=0.0$ at the displayed sampling resolution.  The lower-tail count at or below the observed $M$ size was $7$, giving $p=0.0035$.  This null model is important because it keeps a label-class constraint while still failing to reproduce the observed shape at the recorded rate.  The conclusion remains statistical and finite: it supports the claim that the row is not a generic consequence of preserving amino-acid classes alone within this sample design.

\subsection{Readback stress tests}
\label{sec:bior-h0-r-cardinality-13-readback-stress-tests}

\paragraph{Leave-one-code-out row.}
The leave-one-code-out check evaluated $23$ tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$.  The hit rate above baseline was $0.9841269841269841$, the hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary was $0.9841269841269841$.  The family-resolved row reports $5$ families: alternative yeast has hit rate $0.8333333333333334$ above baseline, while bacterial-plastid, candidate-other, mitochondrial, and nuclear-ciliate each record $1.0$.  These checks show that the code-level tile has strong finite readback behavior across the tested table family grouping.  They still read codon labels; they do not observe ribosomal execution, folded conformations, physical viability, or phenotype.

\paragraph{Leave-one-codon-out row.}
The leave-one-codon-out check records anchor count $0$, boundary count $5$, predictable count $8$, high closure coverage $8$, and recovery rate $1.0$.  In this row every held-out codon is recovered through the finite code-layer procedure as either predictable or boundary-covered.  The module classification accounts for all thirteen codons of $R$ with no extras, no missing entries, and no unassigned codons.  The modules are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
The accounting row states $R_{\mathrm{size}}=13$ and assigned count $13$.  This gives a complete finite decomposition of the selected boundary codons into the six named modules.  The classification is a code-table fact pattern, not a structural biology result.

\subsection{Local scope of the claim}
\label{sec:bior-h0-r-cardinality-13-local-scope}

\paragraph{Observed contact.}
The only reality contact used here is the curated standard-code table, observed as a codon-to-amino-acid-or-stop label assignment.  This contact has the right resolution for code-label readback and for testing a local codon-tile label split.  It has the wrong resolution for translation realization, folded structure, physical admissibility, biological function, or universality.  Consequently the row is aligned with the code-read layer: it supports the finite claim that the window-six coordinate view selects a thirteen-codon boundary set with a structured local tile and strong readback checks against the curated label table.

\paragraph{Boundary statement.}
The chapter therefore asserts a finite code-layer result and withholds all higher-layer promotions.  The set $R$ has thirteen verified codons, its punctured-cube face vector is $(13,16,4)$, its spectral values are numerically consistent with the recorded radial check, its exact-shape frequency is $0.0005$ in the sampled shape comparison, its amino-acid-class preserving exact-shape frequency is $0.0$ at the displayed resolution, and its leave-one-code and leave-one-codon tests show high recovery behavior.  None of these rows, alone or together, establish that DNA must obey this geometry as a biological necessity.  None establishes protein folding, physical admissibility, function, organism-level universality, or a mechanism of translation.  Any such claim would require a separate contact at the corresponding biological layer.