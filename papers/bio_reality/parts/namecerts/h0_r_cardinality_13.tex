\subsection{Window-Six Codon Boundary Tile}\label{sec:bioreality-h0-r-cardinality-13}\origin{ai}

\paragraph{Finite codon display.} The claim concerns a six-bit codon display whose observable readback is the curated standard-code label table. The internal object is not a mechanism of translation; it is a finite coordinate presentation on the codon cube, together with a local tile cut and a label readback at the code layer. The verified boundary region $R$ has exactly thirteen codons:
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The run reports both $|R|=13$ and the same thirteen-member list as the expected codon set. Thus the finite row is not merely a cardinality assertion: the named codons are part of the certificate. A second check identifies the same $R$ as a punctured-cube pattern with face-vector data
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
This is the local tile datum used by the chapter: thirteen visible vertices, sixteen one-dimensional adjacencies, and four two-dimensional faces inside the codon-coordinate presentation.

\paragraph{Spectral and shape checks.} The finite spectrum attached to the row separates the local boundary tile from a generic label shuffle. The recorded scalar values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128,\\
P_{\mathrm{radial}}&=-6.224354365258478\cdot 10^{-12}.
\end{aligned}
$$
The last value is the numerical residual for the cubic/radial constraint, small enough in the run to be reported as satisfying the polynomial tolerance. The shape-significance probe used $N=2000$ sampled alternatives with seed $42$. It needed $57377$ distinct-sample attempts and encountered $55377$ duplicate retries. Among the sampled alternatives, there was $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{J\geq 0.9}&=0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null check also used $N=2000$ and seed $42$. It found $0$ exact matches, $7$ samples in the lower tail at or below the observed $M$ size, and an observed $M$ size of $20$, yielding
$$
\begin{aligned}
p_{\mathrm{class,exact}}&=0.0,\\
p_{\mathrm{class,lower}}&=0.0035.
\end{aligned}
$$
The data therefore support a finite coordinate anomaly at the code-label layer, not an unrestricted biological conclusion.

\paragraph{Module accounting.} The thirteen codons in $R$ are exhausted by six named local modules. The recorded classification is
$$
\begin{aligned}
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\}.
\end{aligned}
$$
The module audit reports $R$ size $13$, assigned count $13$, no missing codons, no extra module codons, and no unassigned codons. Hence every codon in the boundary region is accounted for exactly at the level of the supplied module partition. The leave-one-codon probe reports $8$ predictable cases, $5$ boundary cases, $0$ anchor cases, high closure coverage $8$, and recovery rate $1.0$. The leave-one-code-out probe evaluated $23$ tables and $63$ targets. Its baseline hit rate is $0.19821739130434785$, while the observed hit rates are $0.9365079365079365$ in $M\setminus T$ and $0.9841269841269841$ after adding the boundary. The same $0.9841269841269841$ value is reported as above-baseline performance.

\paragraph{Family-level readback.} The by-family leave-one-code-out audit groups the finite results into $5$ families. The above-baseline rates are $0.8333333333333334$ for alternative yeast and $1.0$ for bacterial plastid, candidate other, mitochondrial, and nuclear ciliate families. These numbers say that the finite coordinate split remains informative across the listed table families under the stated probe, but the inference is still a label-table inference. The only reality contact used here is the curated standard genetic-code table, whose observation is that codons are assigned amino-acid or stop labels. It resolves codon-label readback and local label splits; it does not observe a ribosome, a folded molecule, a thermodynamic admissibility condition, or a function.

\paragraph{Local boundary.} The chapter therefore records a finite code-layer result: the window-six codon coordinates isolate a thirteen-codon boundary tile, the punctured-cube face data match $(13,16,4)$, the spectral quantities satisfy the recorded numerical tolerance, and the null probes give small finite-sample tail values for exact or near-exact shape recovery. What may be claimed is local codon-label structure in the standard-code table. What may not be claimed from this contact is translation realization, folded structure, physical admissibility, biological function, a mechanism, or universality. Any such stronger statement would require a separate reality contact measuring that stronger layer rather than reusing codon-label readback as if it were structural or functional evidence.