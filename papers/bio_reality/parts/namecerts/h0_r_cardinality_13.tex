\subsection{Window-Six Codon Tile Boundary}\label{sec:bioreality-window-six-codon-tile-boundary}\origin{ai}

\paragraph{Finite carrier and readback contact.} This BioReality record treats the standard-code codon table as a finite code-read contact, not as a direct mechanism. The carrier is the six-bit codon cube $Q_6$, read through a curated standard-code table assigning each codon to an amino-acid or stop label. The BEDC-side distinctions are the six-bit codon display, the local tile face, the singleton label corner, and the three-point label class. The external contact supplies the label table only: it supports codon-label readback and local split checks, while it does not observe translation realization, folded structure, physical admissibility, biological function, or universality.

\paragraph{Observed boundary row.} The finite row singled out by the computation is a set $R$ of thirteen codons:
$$\begin{aligned}
R = \{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}$$
The verified cardinality statement is $|R|=13$, with the same thirteen codons returned by the expected-codon check. The punctured-cube check returns exactly the same row and gives the local cell count vector
$$\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}$$
Thus the finite object is not merely a count. It is a local tile with thirteen vertices, sixteen one-dimensional adjacencies, and four two-dimensional faces inside the window-six display. This is the finite geometry exposed by the probe; it is still a code-layer row, not a biological law.

\paragraph{Spectral coordinates.} The associated spectral row gives three scalar coordinates:
$$\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}$$
The radial cubic residual is reported as $-6.224354365258478\times 10^{-12}$, and the same value is reported for the cubic-polynomial check. The spectral-radius entry in tolerance is $0.6752479308830213$. These values are finite-row diagnostics of the local codon-tile coordinate system. They do not supply a translation apparatus, a folding energy, a physical admissibility criterion, or a functional assay.

\paragraph{Shape tests against finite nulls.} In the unrestricted shape comparison with $N=2000$ samples and seed $42$, the run made $57377$ distinct-sample attempts and $55377$ duplicate retries. It found $1$ exact shape match, giving $p_{\mathrm{exact}}=0.0005$, and $25$ matches with Jaccard score at least $0.9$, giving $p_{J\geq 0.9}=0.0125$. The amino-acid-class-preserving null, also with $N=2000$ and seed $42$, reports observed $M$-size $20$, exact-match count $0$, lower-tail count $7$, $p_{\mathrm{aa\ class\ exact}}=0.0$, and lower-tail probability $0.0035$. These tests make the local row non-generic relative to the stated finite nulls, but the conclusion remains a code-read conclusion: the nulls test the codon-label geometry supplied to the row, not ribosomal realization or protein structure.

\paragraph{Leave-one readback checks.} The leave-one-code-out check evaluated $23$ tables and $63$ total targets. The baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$. The hit rate inside $M\setminus T$ was $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus boundary was again $0.9841269841269841$. The family stratification reports $5$ families. Their hit rates above baseline are $1.0$ for bacterial-plastid, mitochondrial, nuclear-ciliate, and candidate-other families, and $0.8333333333333334$ for alternative-yeast. The leave-one-codon-out row has anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$. The recovery-rate-above-threshold field is also $1.0$. These numbers support the statement that the local boundary has predictive readback structure within the finite codon-label regime.

\paragraph{Module partition.} The module classification assigns all thirteen $R$ codons with no omissions: $R$-size $13$, assigned count $13$, no extra module codons, no missing codons, and no unassigned codons. The six reported modules are
$$\begin{aligned}
\mathrm{agr} &= \{\texttt{AGA},\texttt{AGG}\},\\
\mathrm{ar\_lys} &= \{\texttt{AAA}\},\\
\mathrm{cun} &= \{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\},\\
\mathrm{ile\_met} &= \{\texttt{AUA}\},\\
\mathrm{ssn} &= \{\texttt{UCA},\texttt{UUA}\},\\
\mathrm{stop\_trp} &= \{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}.
\end{aligned}$$
Every module has predictable or boundary status according to the supplied row. This is the most concrete local summary of the pattern: the thirteen-codon boundary decomposes into six label-relevant modules, each accounted for by the code-read probe.

\paragraph{Scope boundary.} The supported claim is therefore narrow. Window-six codon coordinates expose a local boundary-tile structure in the standard-code label table, with $|R|=13$, punctured-cube vector $(13,16,4)$, spectral coordinates $(\lambda_M,\lambda_R,\mu)=(0.6752479308830213,0.4758352843197002,3.051487585298128)$, non-generic finite-null behavior, and high leave-one readback rates. Nothing in this row promotes the tile to translation, folding, physical admissibility, function, mechanism, or universality. Amino-acid label-sequence readback is not folded protein structure. A separate higher-layer contact would be required before any such claim could be made.