\origin{ai}

\subsection{Window-six Codon Tile Boundary}
\label{sec:bior-h0-r-cardinality-thirteen}

\paragraph{Finite carrier and readback contact.}
The finite carrier is the six-bit codon display $Q_6$ read through the curated standard-code label table.  The construction used here is deliberately local: it records a window-six coordinate, a local tile face, a singleton label corner, and a three-point label class, then reads the resulting codon set against the standard-code table.  The reality contact is only the curated codon-to-amino-acid-or-stop label assignment.  It is not a contact with ribosomal translation dynamics, folded protein structure, thermodynamic admissibility, biological function, or a universal genetic-code mechanism.

\paragraph{The observed row.}
The distinguished row $R$ has cardinality $13$.  Its complete codon list is
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same finite row is also recorded as a punctured-cube object with $f$-vector $(13,16,4)$.  Thus the code-layer observation is not merely a size count: the row contains $13$ vertices, $16$ local one-dimensional incidences, and $4$ local two-dimensional faces in the punctured-cube presentation.  This is the finite combinatorial content of the claim.

\paragraph{Spectral finite-row signature.}
The spectral readback assigns the row-side and complement-side values $\lambda_R=0.4758352843197002$ and $\lambda_M=0.6752479308830213$, with radial parameter $\mu=3.051487585298128$.  The cubic-polynomial residual is $-6.224354365258478\times 10^{-12}$, the same value recorded for the radial-polynomial residual, so the finite computation is numerically at tolerance.  In compact form, the recorded spectral check is
$$
\begin{aligned}
\lambda_R&=0.4758352843197002,\\
\lambda_M&=0.6752479308830213,\\
\mu&=3.051487585298128,\\
p_{\mathrm{radial}}(\mu)&=-6.224354365258478\times 10^{-12}.
\end{aligned}
$$
These numbers support a local code-read signature for the row.  They do not turn the tile into a biological law.

\paragraph{Shape significance and null rows.}
Against the finite null sample with $N=2000$ and seed $42$, the exact-shape comparison found $1$ exact match, giving $p_{\mathrm{exact}}=0.0005$.  The high-Jaccard comparison found $25$ rows with Jaccard score at least $0.9$, giving $p_{J\geq 0.9}=0.0125$.  The sampling process attempted $57377$ distinct draws and recorded $55377$ duplicate retries, so the null comparison is being reported as a finite-row empirical contact rather than as an asymptotic theorem.  Under the amino-acid-class-preserving null with $N=2000$ and the same seed, the observed $M$-side size was $20$, the exact-shape count was $0$, and the lower-tail count at or below the observed $M$ size was $7$.  The corresponding reported values are $p=0.0$ for exact shape in the sampled table and $p=0.0035$ for the lower tail.  The $M$ codon list itself is not supplied in this row, so only the recorded $M$ size and null statistics are used here.

\paragraph{Leave-one-code-out readback.}
The leave-one-code-out test evaluated $23$ code tables and $63$ total targets.  The baseline hit rate was $0.19821739130434785$.  The hit rate inside $M\setminus T$ was $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus the boundary was $0.9841269841269841$.  The same $0.9841269841269841$ value is recorded as the rate above baseline.  By family, the recorded hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate, across $5$ families.  This supports a code-label boundary pattern that survives the stated finite leave-one-code-out probe.

\paragraph{Leave-one-codon-out modules.}
The leave-one-codon-out row records $0$ anchors, $5$ boundary codons, $8$ predictable codons, closure coverage $8$, and recovery rate $1.0$.  The module classification is complete: $R$ has size $13$, the assigned count is $13$, and there are no extra module codons, no missing row codons, and no unassigned codons.  The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every module has either predictable or boundary status in the recorded classification.  The complete assignment explains why the row can be discussed as a local codon-tile boundary rather than as an arbitrary thirteen-codon subset.

\paragraph{Boundary of the claim.}
The claim established by this finite-row record is a code-read claim: window-six codon coordinates expose a local boundary-tile structure when read against curated standard-code labels.  The table contact supplies codon labels and stop labels; it supplies no observation of translation realization, no folded structure, no physical admissibility criterion, no biological function, no mechanism, and no universality result.  Amino-acid label-sequence readback is therefore not protein-structure readback.  Any promotion beyond the code layer requires a separate reality contact at that higher layer.