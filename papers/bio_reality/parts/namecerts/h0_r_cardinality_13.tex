\origin{ai}

\subsection{Window-six codon tile with thirteen boundary labels}
\label{sec:bioreality-h0-r-cardinality-13}

\paragraph{Finite coordinate packet.}
The object considered here is the standard-code codon table viewed only at the label-read layer. The carrier is the six-coordinate codon cube $Q_6$, with each codon represented as a six-bit display of its three nucleotide positions. The tested local tile separates a distinguished boundary set $R$ from the remaining standard-code labels. In the finite row for this packet the boundary set has exactly thirteen codons:
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
Thus the measured cardinality is $|R|=13$. The same row records that this set is the punctured-cube boundary pattern for the local tile, with face data summarized by the $f$-vector $(13,16,4)$. This is a finite statement about codon coordinates and curated labels; it is not a statement about translation dynamics or molecular realization.

\paragraph{Spectral and closure numerics.}
The associated spectral row gives three scalar checks for the same local object. The measured values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\times 10^{-12}$, and the same value is recorded as the cubic-polynomial residual. This places the finite spectral calculation within numerical tolerance for the encoded local tile. The row also reports a spectral-radius value in tolerance equal to $0.6752479308830213$, matching the displayed value of $\lambda_M$.

\paragraph{Finite null comparisons.}
Two finite randomized comparisons are attached to the same packet. In the shape-significance row, $N=2000$ samples were evaluated from seed $42$. The run required $57377$ distinct-sample attempts and encountered $55377$ duplicate retries. Exact shape agreement occurred once, giving $p_{\mathrm{exact}}=0.0005$. High-overlap agreement at Jaccard threshold $0.9$ occurred $25$ times, giving $p_{J\geq 0.9}=0.0125$. In the amino-acid-class-preserving comparison, again with $N=2000$ and seed $42$, the observed module size was $20$. Exact agreement occurred $0$ times, so the recorded exact-shape value is $0.0$, while the lower-tail count at or below the observed module size was $7$, giving lower-tail value $0.0035$. These rows support the statement that the observed local split is not a typical draw under the recorded finite comparison procedures. They do not identify a biochemical cause.

\paragraph{Readback and leave-out rows.}
The code-read contact is the curated standard genetic-code table, whose role is to assign codons to amino-acid or stop labels. Within that limited contact, the leave-one-code-out row evaluates $23$ tables and $63$ targets. The baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$. The hit rate inside $M-T$ is $0.9365079365079365$, and the rate inside $M-T$ together with the boundary is $0.9841269841269841$. The leave-one-codon-out row records $0$ anchors, $5$ boundary cases, $8$ predictable cases, closure coverage high equal to $8$, and recovery rate $1.0$. These finite rows say that the local tile is informative for codon-label readback under the stated deletion tests.

\paragraph{Family and module accounting.}
The by-family leave-one-code-out row contains five families. The recorded family hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate. The module-classification row assigns all thirteen boundary codons with no missing or extra codons. Its modules are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA, AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA, CUC, CUG, CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA, UUA\},\\
\mathrm{stop\_trp} &= \{UAA, UAG, UGA\}.
\end{aligned}
$$
The accounting row gives $R$-size $13$, assigned count $13$, an empty list of extra module codons, an empty list missing from modules, and an empty list of unassigned codons. Hence the finite module ledger closes exactly at the code-label level.

\paragraph{Scope boundary.}
The local codon tile is a boundary observation inside a six-coordinate codon display, checked against a curated standard-code label table. It supports a code-read statement about the thirteen listed codons, their local punctured-cube geometry, the displayed spectral scalars, the finite random-comparison rows, and the deletion-test behavior. Nothing in this packet supplies a separate observation of translation realization, folded protein structure, physical admissibility, biological function, causal mechanism, or universality across life. Amino-acid label readback remains label readback. Any move from this local code-layer pattern to structure, function, or mechanism requires an additional contact that directly observes that higher layer.