\origin{ai}

\subsection{A thirteen-codon boundary set in the standard-code cube}
\label{sec:bioreality-h0-r-cardinality-13}

\paragraph{Record.}
The finite observation in this section concerns the standard-code codon table after it is read only as a codon-to-label table.  The carrier is the six-coordinate codon cube, with one coordinate for each displayed bit of a codon window.  The claim tested here is not that the cube generates biology, but that this coordinate display marks out a specific local boundary set when the curated standard-code labels are placed on it.  The observed boundary set is
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
      &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The finite count is therefore
$$
\begin{aligned}
|R| = 13.
\end{aligned}
$$
This is the whole cardinality assertion of the row: the finite table contact supplies labels, the coordinate calculation selects the listed codons, and the recorded size is thirteen.

\paragraph{Local shape data.}
The same row family records that this thirteen-codon set is the punctured-cube boundary object for the tested coordinate window.  Its face data are summarized by the vector
$$
\begin{aligned}
(f_0,f_1,f_2) = (13,16,4).
\end{aligned}
$$
Thus the selected object has thirteen vertices, sixteen one-dimensional adjacencies, and four two-dimensional faces in the local tile calculation.  The statement is deliberately local: it says that the standard-code labels, when viewed through this window, mark a finite boundary tile with this small face vector.  It does not say that the tile is a law of genetics, an explanation of translation, or a physical principle.

\paragraph{Spectral row.}
The associated spectrum row gives three scalar values for the local operator calculation:
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The recorded cubic residual is
$$
\begin{aligned}
-6.224354365258478\times 10^{-12},
\end{aligned}
$$
which is also listed as the radial polynomial value.  This residual is numerically close to zero at the recorded tolerance scale and is treated here only as a finite computational check on the local coordinate calculation.  It is not evidence for a biochemical energy model, a folding model, or an organism-level mechanism.

\paragraph{Null comparisons.}
Two null rows give the finite contrast class.  In the shape-significance run with seed $42$, the sample size was $N=2000$.  The exact shape appeared once, so
$$
\begin{aligned}
p_{\mathrm{exact}} = 0.0005.
\end{aligned}
$$
The high-overlap event with Jaccard score at least $0.9$ appeared twenty-five times, giving
$$
\begin{aligned}
p_{\mathrm{Jaccard}\geq 0.9} = 0.0125.
\end{aligned}
$$
That run required $57377$ distinct-sample attempts and recorded $55377$ duplicate retries.  A separate amino-acid-class-preserving null run, also with $N=2000$ and seed $42$, recorded observed $M$-size $20$, exact match count $0$, and lower-tail count $7$.  Its reported probabilities were
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
These numbers support the narrow reading that the selected finite shape is not typical under the recorded randomizations.  They do not convert the coordinate pattern into a claim about translation dynamics or biological function.

\paragraph{Readout tests.}
The leave-one-code-out row evaluated $23$ tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$.  The recorded hit rate above baseline was $0.9841269841269841$, the hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary was $0.9841269841269841$.  The leave-one-codon-out row reported no anchors, five boundary cases, eight high-coverage closure cases, eight predictable cases, and recovery rate $1.0$.  These are finite readout statistics for code-label recovery under deletion tests.  They remain code-layer measurements, not observations of a ribosome, a folded molecule, a viable organism, or a universal biological procedure.

\paragraph{Family and module accounting.}
The family-level leave-one-code-out row partitions the evaluated cases into five families.  The family hit rates above baseline were
$$
\begin{aligned}
\mathrm{alternative\_yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\_plastid} &= 1.0,\\
\mathrm{candidate\_other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\_ciliate} &= 1.0.
\end{aligned}
$$
The module classification row accounts for all thirteen codons of $R$ with no missing members, no extra module codons, and no unassigned codons.  The six recorded modules are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
The assigned count is $13$, matching the recorded $R$-size.  This makes the row internally closed as a finite accounting statement: every codon in the selected boundary set is assigned to exactly one of the listed modules in the stored classification.

\paragraph{Reality contact and scope.}
The only external contact used here is the curated standard-code table, treated as a codon-label bridge at codon-label resolution.  It says that codons are assigned to amino-acid or stop labels in the standard code.  It does not observe translation itself, folded structure, physical admissibility, biochemical function, or a universal mechanism.  Accordingly, the finite result of this section is a local code-read statement: the standard-code labels, under the six-coordinate window calculation, single out the thirteen codons listed above, with the recorded punctured-cube face vector, spectral scalars, null contrasts, deletion-test rates, and module accounting.  Any assertion about translation, folding, admissible physics, biological function, mechanistic causation, or universality would require a separate contact that directly measures that higher layer.