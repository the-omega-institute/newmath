\origin{ai}

\subsection{Window-six spectral packet for the standard code}
\label{sec:h0-spectrum-window-six-spectral-packet}

\paragraph{Finite carrier.}
The record studies the standard genetic-code table only at the code-read layer.  Its finite carrier is the six-bit codon cube $Q_6$, where a codon is read as a three-position display over the four RNA letters and is then treated as a finite coordinate point.  The distinctions used in the packet are the six-bit codon display, a local tile face, a singleton label corner, and a three-point label class.  The readback contact is the curated standard-code table assigning codons to amino-acid or stop labels.  This contact is enough to test codon-label readback and local label splits, but it is not a contact with translation dynamics, folded structure, physical admissibility, biological function, mechanism, or universality.

\paragraph{Boundary set.}
The verified boundary set $R$ has cardinality $13$.  The codons in $R$ are exactly
$$
\begin{aligned}
R=\{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}
$$
The same finite row verifies that $R$ is a punctured-cube object with face vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the codon set is not being asserted as a global biological principle; it is a finite local shape extracted from the codon-label table and then constrained by the geometry of the six-bit display.

\paragraph{Spectral values.}
The spectral row for claim $h0.spectrum$ gives three scalar quantities.  The observed values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial cubic check is numerically closed at tolerance scale: both the listed cubic residual and the radial residual are $-6.224354365258478\times 10^{-12}$.  The spectral-radius entry is $0.6752479308830213$, matching the supplied value of $\lambda_M$.  These numbers make the packet more specific than a qualitative observation: the local codon tile comes with a measured spectral profile tied to the finite standard-code readback.

\paragraph{Shape significance.}
The finite randomization row for the $M$-shape comparison used $N=2000$ samples with seed $42$.  It required $57377$ distinct-sample attempts and encountered $55377$ duplicate retries.  The exact shape appeared once, giving
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}
$$
The high-overlap threshold $J\geq 0.9$ was met $25$ times, giving
$$
\begin{aligned}
p_{J\geq 0.9}=0.0125.
\end{aligned}
$$
A second null model preserving amino-acid classes used $N=2000$ samples, again with seed $42$.  It found $0$ exact matches and $7$ lower-tail samples at or below the observed $M$ size of $20$, yielding
$$
\begin{aligned}
p_{\mathrm{class\ exact}}&=0.0,\\
p_{\mathrm{class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
The packet therefore records a finite statistical surprise at the code-label layer, while keeping the interpretation inside that layer.

\subsection{Readback recovery and module organization}
\label{sec:h0-spectrum-readback-recovery-module-organization}

\paragraph{Leave-one-code-out behavior.}
The code-table perturbation row evaluated $23$ tables and $63$ total targets.  The baseline hit rate was $0.19821739130434785$.  Against that baseline, the hit rate above baseline was $0.9841269841269841$; the hit rate inside $M-T$ was $0.9365079365079365$; and the hit rate inside $M-T$ plus the boundary was $0.9841269841269841$.  By family, the above-baseline rates were $1.0$ for bacterial-plastid, mitochondrial, nuclear-ciliate, and candidate-other families, and $0.8333333333333334$ for the alternative-yeast family.  These are code-read recovery numbers: they concern label-table variation, not biochemical execution.

\paragraph{Leave-one-codon-out behavior.}
The codon-level perturbation row reports no anchors, $5$ boundary codons, $8$ high-coverage closure cases, and $8$ predictable codons.  Its recovery rate is
$$
\begin{aligned}
1.0.
\end{aligned}
$$
This supports the narrow claim that the local coordinate-and-closure description can recover the relevant code-layer pattern under codon removal.  It does not show that a ribosome realizes the pattern, that a peptide folds, or that an organism-level function follows.

\paragraph{Module classification.}
The module classification is complete for the $13$ codons of $R$: the assigned count is $13$, with no missing codons, no unassigned codons, and no extra module codons.  The six listed modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\texttt{AGA},\texttt{AGG}\},\\
\mathrm{ar\_lys}&=\{\texttt{AAA}\},\\
\mathrm{cun}&=\{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\},\\
\mathrm{ile\_met}&=\{\texttt{AUA}\},\\
\mathrm{ssn}&=\{\texttt{UCA},\texttt{UUA}\},\\
\mathrm{stop\_trp}&=\{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}.
\end{aligned}
$$
Every module has a predictable-or-boundary witness, so the spectral packet, the punctured-cube boundary, and the module decomposition agree on the same finite codon set.

\paragraph{Local verdict.}
The evidence supports the following bounded statement.  Window-six codon coordinates expose a local boundary-tile structure in the standard-code label table.  The verified rows identify the thirteen-codon boundary, its punctured-cube face vector, its spectral scalars, its statistical comparison against two finite null models, and its recovery behavior under code-table and codon-level perturbations.  The same rows do not license a higher-layer biological conclusion.  No translation realization, folded protein structure, physical admissibility claim, biological function, mechanism, or universal biological law follows from this packet unless a separate contact measuring that layer is supplied.