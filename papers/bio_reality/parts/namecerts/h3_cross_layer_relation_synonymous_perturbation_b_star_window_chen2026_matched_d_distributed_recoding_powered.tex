\origin{ai}

\subsection{Matched distributed recoding over the Chen protein-output window}
\label{sec:bioreality-chen-distributed-recoding-q6-window}

\paragraph{Finite surface.}
This record concerns a finite perturbation surface in which a synonymous coding-window coordinate is allowed to enter protein-output interpretation only through a controlled translation-facing readout.  The coordinate side is the nine-dimensional $B^{\ast}_{Q6}$ residual window basis used for the Escherichia coli coding sequence assay, with the actual basis recorded as \texttt{B\_star\_window\_Q6\_9D\_Ecoli\_CAI\_spatial\_windows\_full\_nt60\_531\_and\_middle3\_drop\_first\_60\_codons}.  The finite rows are split into $47$ AmpR rows and $53$ GmR rows, with $19$ multi-row $D$ strata.  The run used organism-specific CAI weights from \texttt{tools/bio\_reality/data/ecoli\_genome\_cai\_weights.json}; the dataset was already present for the run, and the registered computation completed in $465.144$ seconds.

\paragraph{Controlled statistic.}
Let $q_i$ range over the $B^{\ast}_{Q6}$ window coordinates, let $D$ denote the matched distributed-recoding stratum, and let $Y$ denote the protein-output readout attached to the AmpR/GmR split.  The admissible finite question is not whether the synonymous coordinate is biologically active in general, but whether the coordinate supplies residual protein-output information after the matched $D$ design and the coding-sequence controls have already absorbed the simpler signal.  The registered AmpR contrast is summarized by
$$
\begin{aligned}
  n_{\mathrm{AmpR}} &= 47,\\
  n_{\mathrm{GmR}} &= 53,\\
  n_D &= 19,\\
  \widehat\beta_{\mathrm{AmpR}} &= 0.0,\\
  p_{\mathrm{AmpR}} &= 1.0.
\end{aligned}
$$
The finite readout is therefore a null result for the target AmpR direction: within this matched distributed-recoding surface, the AmpR slope is exactly zero in the registered summary and the independent $p$-value is $1.0$.

\paragraph{Relation to the broader translation gate.}
The negative AmpR statistic is interpreted against a stricter surrounding rule.  A direct $Q_i \to P_k$ association is not promoted to protein realization unless a same-coordinate $Q_i \to T_j$ translation step is present and controlled.  The larger modeled protein-abundance mediation run supplies the shape of this rule: across $12$ organisms, the joint nine-coordinate $B^{\ast}_{Q6}$ protein-abundance decomposition used per-protein modeled tAI as $T$ and log$_{10}$ PAXdb abundance as $Y$.  In that run, examples of mediated fractions included $0.9491551041216973$ in \textit{Saccharomyces cerevisiae}, $0.612922252793667$ in \textit{Escherichia coli} K12 MG1655, $0.27206289160119157$ in \textit{Homo sapiens}, and $0.01850668564589326$ in \textit{Sulfolobus solfataricus}.  Those values define a statistical conditioning surface, not a causal mechanism.  The present Chen window does not improve that status: its own AmpR slope is $0.0$ and hence does not supply a positive perturbational protein-output increment.

\paragraph{Readout boundary.}
The tAI contact used elsewhere is a bounded codon-adaptation readout, not a ribosome-occupancy observation.  The modeled-tAI survival audit computed nine residual coordinates, namely \texttt{Arg\_AGR}, \texttt{Ile\_AUA}, \texttt{K\_AAA}, \texttt{Leu\_CUN\_vs\_UUR}, \texttt{Leu\_UUA\_vs\_UUG}, \texttt{Ser\_UCA\_vs\_UCG}, \texttt{Ser\_UCR\_vs\_AGY}, \texttt{Thr\_ACR\_vs\_ACY}, and \texttt{f3\_stress}.  It found $81$ modeled entries above the bounded coupling threshold $0.01$ and a maximum absolute modeled entry of $3801961.031607896$, but only across $3$ organisms.  The same audit explicitly records that $3$ organisms are insufficient for powered survival and that the surface is modeled tAI coupling rather than ribosome profiling or proteomics validation.  Thus the Chen finite-row result is read as a perturbation-window null inside a translation-gated program, not as a direct statement about ribosome dynamics.

\paragraph{Comparison with adjacent perturbation records.}
Other synonymous perturbation windows show that the machinery can register positive protein-output surfaces when the finite rows contain them.  The Nieuwkoop E. coli mRFP protein-output run used $1459$ variants and reported $\Delta R^2 = 0.05339906007191191$, $\Delta\mathrm{DL}=19.089327473282296$ bits, $p_{\mathrm{matched}}=0.004975124378109453$, and $p_{\mathrm{within}}=0.004975124378109453$.  The Chen 2017 GFP escape run used $3556$ variants, split as $1124$ in region 1 and $2432$ in region 2, and recorded an out-of-frame mRNA-to-protein slope of $0.016523671594873352$.  Those neighboring records do not rescue the present surface: the Chen distributed-recoding AmpR statistic remains $\widehat\beta=0.0$ with $p=1.0$ on its own matched finite rows.

\paragraph{Local conclusion.}
For this chapter, the allowed conclusion is bounded and negative.  The Chen matched distributed-recoding window supplies a completed finite audit over $47$ AmpR rows, $53$ GmR rows, and $19$ $D$ strata, but it does not support a positive AmpR protein-output effect for the registered $B^{\ast}_{Q6}$ spatial-window basis.  It therefore remains at controlled perturbation-window status.  No translation realization, protein folding, physical admissibility, biological function, molecular mechanism, organismal phenotype, selection law, or universal biological rule follows from this chapter.  Any such stronger claim would require a separate layer-matched contact and a same-row statistic showing the required translation or post-translation step before promotion.