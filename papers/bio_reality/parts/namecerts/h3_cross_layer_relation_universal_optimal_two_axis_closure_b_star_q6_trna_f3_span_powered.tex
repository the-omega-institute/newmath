\subsection{Rank-powered two-axis closure for the $B^*_{Q6}$ $f_3$-stress coordinate}\label{sec:bioreality-q6-f3-stress-two-axis-closure}
\origin{ai}

\paragraph{Finite carrier.}
This record concerns a finite residual coordinate surface whose rows are coding sequences grouped by organism and whose distinguished coordinate is the $B^*_{Q6}$ $f_3$-stress axis after amino-acid projection. The finite codon boundary is explicit. The row named $M=\operatorname{Med}(R)\cup\{\mathrm{ACA}\}$ contains
$\{\mathrm{AAA},\mathrm{AAG},\mathrm{ACA},\mathrm{ACG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}$.
The $f_3$-saturated row removes the two $\mathrm{AC}$ entries and is
$\{\mathrm{AAA},\mathrm{AAG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}$.
The recorded $f_3$ boundary set is empty. Thus the finite carrier is not an unconstrained codon-space assertion: it is a fixed synonymy-residual surface with a specified saturated support and a zero boundary row for the $f_3$ operation in this packet.

\paragraph{Two-axis test.}
The powered two-axis closure experiment was run as $\texttt{b\_star\_q6\_universal\_optimal\_two\_axis\_closure\_powered}$ with $5$ folds, $41$ synonymous design columns, a minimum of $500$ proteins per organism, $11$ organisms computed out of $16$ requested, and deterministic seed $\texttt{sha256:b\_star\_q6\_universal\_optimal\_two\_axis\_closure\_powered:deterministic}$. Its finite conclusion is $\texttt{third\_axis\_present}$, with the recorded reason that $d_\perp$ has systematically positive held-out $R^2$ and that the $d_\perp$ directions are mutually same-signed across organisms. In the notation of this chapter, the two proposed explanatory axes do not close the observed residual surface:
$$
\begin{aligned}
\operatorname{span}(d_{\mathrm{tRNA}},d_{f_3}) &\subsetneq \operatorname{span}(d_{\mathrm{tRNA}},d_{f_3},d_\perp),\\
\Delta_{\mathrm{heldout}}(d_\perp) &> 0\quad\text{on the recorded cross-organism row.}
\end{aligned}
$$
This is a finite closure failure for the two-axis model, not a declaration that the extra direction is already a biological mechanism.

\paragraph{Relation to the residual-axis spectrum.}
The same finite panel records that the non-tRNA residual optimality direction aligns most with $f_3$ translation selection in the residual-axis experiment: $11$ organisms were computed out of $16$ requested, with $5$ folds, $41$ synonymous design columns, and a minimum of $500$ proteins per organism. A separate identity run reports $d_\perp$ as aligned to usage frequency within synonym family, with mean cosine $0.128$ and one-sided sign-test $p=0.000488$ on the same $11$-of-$16$ computed organism count. The mechanism run records the phrase $\texttt{selection-not-GC; not-wobble-clustered; CSC/positional identity needs\_external}$ and gives the finite reason that GC-orthogonalized $d_\perp$ keeps significant cross-species alignment and held-out $R^2$. These rows make the mathematical reading narrow: the third direction is a reproducible residual direction in the controlled finite design, while its mechanistic identity remains bounded by the available contacts.

\paragraph{tRNA and wobble break condition.}
The $R$-sense tRNA-poverty experiment supplies the local asymmetry needed for this packet to be biologically testable rather than purely topological. It computed $40$ organisms out of $46$ requested, skipped $6$, used $1000$ permutations at $\alpha=0.05$, and passed with the recorded mechanism call that $R$ sense codons are systematically tRNA-poor under GtRNAdb tGCN plus dos Reis wobble weights. This supports a tRNA-supply interpretation for $R$ avoidance, but only at the level of codon usage and tRNA or wobble-weight readback. In particular, the $f_3$ coordinate becomes a translation-facing candidate only when a context has an asymmetric weight condition of the form
$$
\begin{aligned}
w_c &\ne w_{c^{f_3}},\\
S_{f_3\text{-stress},T_j} &> 0
\end{aligned}
$$
after the declared controls. Without the second line, the first line is only a modeled or annotated asymmetry and not a measured survival row.

\paragraph{Readout rule.}
The bounded readout for this chapter is the controlled translation entry $S_{f_3\text{-stress},T_j}$, where $T_j$ may be a tAI surface, an stAI surface, or a measured ribosome-occupancy or translation-efficiency row. The control set includes amino-acid composition, organism, GC3, CDS length, $M$-density, matched mRNA abundance when TE is computed, rank sufficiency, and simpler codon baselines. A positive finite entry has the form
$$
\begin{aligned}
S_{f_3\text{-stress},T_j} &= R^2_{\mathrm{full}}-R^2_{\mathrm{baseline}},\\
S_{f_3\text{-stress},T_j} &>0,
\end{aligned}
$$
with the baseline containing the declared controls. The present verified row establishes the powered residual-geometry fact $\texttt{third\_axis\_present}$; it does not by itself establish a same-CDS measured ribosome or TE survival statistic for the exact $f_3$-stress packet.

\paragraph{Local non-promotion boundary.}
The chapter therefore has a precise admissible claim and a precise refusal. The admissible claim is that, on the finite powered panel, the two-axis span of tRNA adaptation and $f_3$-stress residual direction is insufficient: an additional same-signed residual direction survives held-out testing across $11$ computed organisms, and related rows identify this direction with usage-frequency-within-family behavior at mean cosine $0.128$ with sign-test $p=0.000488$. The refused claim is equally important. No translation realization, ribosome occupancy, protein abundance, folded structure, physical admissibility, biological function, molecular mechanism, phenotype, or universal biological law follows from this row alone. Any such assertion requires its own layer-matched contact and its own controlled statistic. The three-organism all-$1.0$ rank-degenerate surface is treated as a null boundary, not as survival evidence. The scientific content of this packet is therefore a finite, controlled residual-closure failure together with an explicit requirement for a nondegenerate translation-readout row before any downstream biological interpretation is allowed.