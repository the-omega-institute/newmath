\origin{ai}
\subsection{Leave-One-Codon Recovery in the Six-Bit Codon Cube}
\label{sec:bioreality-h1-leave-one-codon-out}

\paragraph{Finite object and reality contact.}
The finite object for this record is the codon cube $Q_6$, whose vertices are six-bit displays of the $64$ codons.  The biological contact is deliberately narrow: a curated standard-code table assigns each codon to an amino-acid label or to a stop label.  This contact is sufficient for code-layer label readback, local label-split tests, and finite recovery accounting.  It is not evidence for translation dynamics, folded structure, physical admissibility, organismal function, a biochemical mechanism, or a universal biological law.  Any such higher-layer assertion would require a separate observation channel.

\paragraph{Recovered exceptional row set.}
The finite row $R$ has cardinality $13$.  The recovered codons are
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The punctured-cube check returns the same set and the finite face count
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the observed row is not only a list of codon labels; it is also a small cubical subshape with $13$ vertices, $16$ one-dimensional adjacencies, and $4$ square faces inside the six-coordinate codon display.  The role of the shape is local.  It records a boundary-tile pattern in the code table and does not promote that pattern into a statement about realized molecular processes.

\paragraph{Spectral and null-model calibration.}
The spectrum attached to the row gives
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The associated radial cubic residual is $-6.224354365258478\times 10^{-12}$, which is numerically at tolerance for the stated finite computation.  Against the shape-randomization experiment with $N=2000$ samples and seed $42$, there was $1$ exact shape match, so the exact-shape probability estimate is $p=0.0005$.  The high-overlap test counted $25$ samples with Jaccard score at least $0.9$, giving $p=0.0125$.  The distinct-sample procedure attempted $57377$ draws and retried $55377$ duplicates, which records that the sampled finite universe was heavily revisited under the chosen constraints.  Under the amino-acid-class-preserving null, again with $N=2000$ and seed $42$, the exact shape count was $0$ and the lower-tail count at or below the observed $M$ size was $7$.  The observed $M$ size was $20$, giving exact-shape estimate $0.0$ and lower-tail estimate $0.0035$.  These numbers support a finite code-layer anomaly relative to the specified nulls, not a causal account of biology.

\paragraph{Leave-one-codon recovery.}
The leave-one-codon experiment reports no anchors, $5$ boundary codons, $8$ predictable codons, and $8$ high-coverage closure cases.  The recovery rate is
$$
\begin{aligned}
\mathrm{recovery\_rate}=1.0,
\end{aligned}
$$
with the threshold flag also equal to $1.0$.  In this chapter that value is read as a finite reconstruction fact: when a codon in the exceptional row is withheld under the stated coordinate and closure procedure, the row-level structure still accounts for the held-out codon through predictable or boundary behavior.  The result remains a property of the code table as represented in $Q_6$.

\paragraph{Comparison with leave-one-code tests.}
The related leave-one-code-out run evaluated $23$ code tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate inside $M\setminus T$ was $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus the boundary was $0.9841269841269841$.  Grouping by code-family gives $5$ families: alternative yeast has family hit rate above baseline $0.8333333333333334$, while bacterial plastid, candidate other, mitochondrial, and nuclear ciliate each have rate $1.0$.  This comparison is useful because it shows that the codon-level row is not isolated from the code-table perturbation tests, while still keeping the inference at the label table layer.

\paragraph{Module accounting.}
The module classification partitions all $13$ codons in $R$ with no missing or extra assignments.  The six modules are
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The completeness record is $R\_\mathrm{size}=13$ and assigned count $13$, with empty extra-module, missing-from-module, and unassigned-codon lists.  Every module has either predictable behavior or boundary behavior.  The singleton modules $\mathrm{ar\_lys}$ and $\mathrm{ile\_met}$ mark single-corner label events, while the $\mathrm{stop\_trp}$ block supplies the three-point label class.  The $\mathrm{cun}$ block is the four-codon local face component, and the two-point blocks record paired boundary behavior in the same finite display.

\paragraph{Local conclusion.}
The row supports the following bounded conclusion.  In the six-bit codon cube, the standard-code label table contains a $13$-codon exceptional row whose cubical shape, spectral values, leave-one-codon recovery, family-level leave-one-code behavior, and module partition cohere as a finite code-read pattern.  The result is strongest as a local boundary-tile statement: curated codon labels interact with the coordinate, closure, spectrum, and relation structure in a way that is difficult to reproduce under the recorded null models.  It is not a statement that DNA must instantiate this geometry as a biological law, and it is not evidence for protein folding, structural order, biochemical feasibility, organismal function, translation realization, mechanism, or universality without a further reality contact designed for that layer.