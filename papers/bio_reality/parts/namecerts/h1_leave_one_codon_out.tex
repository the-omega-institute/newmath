\origin{ai}

\subsection{Leave-One-Codon Boundary Readback}
\label{sec:h1-leave-one-codon-out}

\paragraph{Finite carrier and contact.}
The finite carrier is the six-bit codon cube $Q_6$, read through a curated standard-code table.  The table is used only as a code-layer reality contact: it assigns each codon to an amino-acid or stop label, but it does not by itself observe translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.  The local object under inspection is therefore a window-six boundary tile with a singleton label corner and a three-point label class, not a global biological law.

\paragraph{Exceptional row set.}
The finite exceptional row set $R$ has cardinality $13$.  The observed rows are
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same rows realize the punctured-cube readback, with finite face data
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the chapter records a concrete local tile on the codon cube: thirteen codon rows, sixteen one-dimensional incidences, and four two-dimensional faces.  The row statement is finite and literal; it is not promoted to a claim that all biological code variation is forced by this geometry.

\paragraph{Spectral coordinates.}
The associated finite spectrum gives three scalar coordinates.  The measured values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\times 10^{-12}$, the same value recorded for the cubic-polynomial check.  This numerical residual is treated as a finite-row consistency check for the coordinate calculation, not as a biological mechanism.  The coordinate calculation says that the local tile has a reproducible spectral signature in the chosen finite display.

\paragraph{Shape and null comparisons.}
The finite-shape comparison used $N=2000$ sampled controls with seed $42$.  The exact shape matched once, giving $p_{\mathrm{exact}}=0.0005$, while $25$ samples had Jaccard score at least $0.9$, giving $p_{J\geq 0.9}=0.0125$.  The distinct-sample accounting was explicit: $57377$ distinct sample attempts and $55377$ duplicate retries.  Under the amino-acid-class-preserving null, the observed $M$ size was $20$, with exact-match count $0$ over $N=2000$ controls.  The lower-tail count at or below the observed $M$ size was $7$, giving $p_{\mathrm{lower\ tail}}=0.0035$, while the amino-acid-class exact-shape rate was $0.0$.  These numbers support a finite local-shape anomaly against the tested nulls.  They do not identify a translation apparatus, a folded protein state, or a universal constraint on genetic codes.

\paragraph{Leave-one-code and family readback.}
At the code-table level, the leave-one-code-out probe evaluated $23$ tables and $63$ targets.  Its baseline hit rate was $0.19821739130434785$.  The hit rate in $M\setminus T$ was $0.9365079365079365$, and the hit rate in $M\setminus T$ plus boundary was $0.9841269841269841$.  The same $0.9841269841269841$ value is recorded as the hit rate above baseline.  By family, $5$ families were evaluated: alternative yeast had hit rate $0.8333333333333334$, while bacterial plastid, candidate other, mitochondrial, and nuclear ciliate each had hit rate $1.0$ above baseline.  These are label-readback statistics for curated code tables.  They are not observations of protein function or physical admissibility.

\paragraph{Leave-one-codon result.}
For the codon-level leave-one-out probe, the finite counts are sharp:
$$
\begin{aligned}
\mathrm{anchor\ count} &= 0,\\
\mathrm{boundary\ count} &= 5,\\
\mathrm{closure\ coverage\ high} &= 8,\\
\mathrm{predictable\ count} &= 8,\\
\mathrm{recovery\ rate} &= 1.0.
\end{aligned}
$$
The thresholded recovery-rate check is also $1.0$.  Thus every tested codon target in this finite probe is recovered either by the high-coverage closure side or by the boundary accounting, with no anchor rows needed.  The point is deliberately narrow: the local codon-tile split is visible at the code-read layer when the curated standard-code table supplies labels.

\paragraph{Module partition.}
The thirteen rows in $R$ are completely assigned to six named modules.  The module partition is
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The classification check reports $R$ size $13$, assigned count $13$, no missing rows from the modules, no extra module codons, and no unassigned codons.  Each module has a predictable-or-boundary witness.  This closes the finite bookkeeping for the local code-read probe: the same thirteen-row punctured cube is covered by the six module names without residue.

\paragraph{Boundary of the claim.}
The resulting claim is a bounded BioReality record.  Window-six codon coordinates expose a local boundary-tile structure in the curated standard-code label table.  The support condition is exactly code-layer readback plus local split checking.  The cannot-claim boundary is part of the result: no translation realization, no folded structure, no structural order, no physical admissibility, no biological function, no universal mechanism, and no claim that DNA obeys this finite geometry as biological necessity follows from these rows.  Any such promotion would require a separate reality contact that directly observes the higher layer.