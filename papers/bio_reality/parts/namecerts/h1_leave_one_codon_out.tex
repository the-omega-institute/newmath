\origin{ai}

\subsection{Leave-one-codon local tile boundary}
\label{sec:h1-leave-one-codon-out}

\paragraph{Finite carrier and code-layer contact.}
This record treats the standard-code codon table as a finite code-layer contact over a six-bit codon display.  The carrier is the codon cube $Q_6$, read through a curated standard-code table that assigns each codon to an amino-acid or stop label.  The contact is only a codon-label readback.  It does not observe ribosomal translation, folded protein structure, physical admissibility, biological function, a generative mechanism, or a universal biological law.  The finite question is therefore local: when one codon label is withheld, does the window-six coordinate system expose a boundary-tile pattern that recovers the held-out code-layer label or places it in an explicit boundary class?

\paragraph{The exceptional region.}
The row set $R$ has cardinality $13$.  Its observed codons are
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same finite row set is reported as a punctured-cube pattern with $f$-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the local object is not a single isolated codon, but a finite face-and-boundary configuration inside $Q_6$.  The intended readback is still the ordinary standard-code label table: coordinates, closure, spectrum, and relation structure organize the finite rows, while the labels themselves remain supplied by the external code table.

\paragraph{Spectral row signature.}
The observed spectrum separates the main coordinate block from the exceptional row set.  The measured values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The associated cubic or radial polynomial residual is $-6.224354365258478\cdot 10^{-12}$, with the same value recorded for the radial polynomial check.  This residual is numerically negligible at the reported precision, so the spectral claim used here is only that the finite window-six construction has a stable row signature at the code-read level.  It is not a claim that biological translation is spectrally generated by the cube.

\paragraph{Shape significance against row nulls.}
The observed $M$-shape test used $N=2000$ sampled tables with seed $42$.  It required $57377$ distinct-sample attempts and incurred $55377$ duplicate retries.  There was $1$ exact shape match, giving
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}
$$
There were $25$ samples with Jaccard similarity at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{Jaccard}\ge 0.9}=0.0125.
\end{aligned}
$$
A second amino-acid-class-preserving null used $N=2000$ with seed $42$.  It observed $0$ exact matches, an observed $M$ size of $20$, and $7$ lower-tail samples at or below that observed size.  The reported probabilities are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These numbers support the finite-row claim that the observed codon-tile geometry is unusually structured under the stated nulls.  They do not promote the tile to a mechanism of code origin, a law of molecular biology, or a statement about protein structure.

\paragraph{Leave-one-codon result.}
For the codon-level leave-one-out task, the observed finite row has $0$ anchors, $5$ boundary cases, $8$ high-closure-coverage cases, and $8$ predictable cases.  The recovery rate is
$$
\begin{aligned}
\mathrm{recovery\ rate}=1.0,
\end{aligned}
$$
with the same value reported for recovery above threshold.  In this chapter the number $1.0$ has a narrow meaning: every tested withheld codon in the recorded row is either recovered by the local coordinate closure or accounted for by the boundary classification.  Since $\mathrm{anchor\ count}=0$, the result is not an anchor-based universality statement; it is a boundary-and-closure statement for a finite local tile.

\paragraph{Code-table comparison rows.}
The broader leave-one-code-out table evaluated $23$ code tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$.  The observed hit rate above baseline is $0.9841269841269841$, the hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$.  By family, $5$ families are present.  The family hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
These comparison rows are used as finite support for the code-read boundary pattern.  They remain label-table facts; they are not direct observations of translation dynamics or organismal function.

\paragraph{Module classification.}
The codon modules exhaust the $13$-element row set: $R$ size is $13$, assigned count is $13$, and there are no extra module codons, no missing codons from modules, and no unassigned codons.  The module assignment is
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every listed module has a predictable-or-boundary witness.  This completes the finite module accounting for the local tile: the thirteen codons are neither left floating outside the module table nor absorbed into an unobserved higher-layer interpretation.

\paragraph{Boundary verdict.}
The finite-row verdict is that the window-six codon coordinates expose a local boundary-tile structure for the standard-code label table.  The strongest chapter-local statement is a code-read statement: the curated table supplies codon labels, and the BEDC-style coordinate, closure, spectrum, and relation data organize a thirteen-codon punctured-cube boundary inside $Q_6$.  The cannot-claim boundary is part of the result.  No translation realization, folded structure, structural order, physical admissibility, biological function, universal mechanism, or biological necessity follows from this contact.  Any such promotion requires a separate reality contact that observes the higher layer directly.