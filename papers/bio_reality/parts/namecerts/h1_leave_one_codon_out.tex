\origin{ai}
\subsection{Leave-One-Codon-Out Local Tile Boundary}
\label{sec:h1-leave-one-codon-out-local-tile-boundary}

\paragraph{Finite carrier and contact.}
This BioReality record concerns the standard-code codon table only at the code-read layer.  The finite carrier is the six-bit codon cube $Q_6$, read as a codon display with local tile faces, singleton label corners, and three-point label classes.  The only external contact used here is the curated standard-code table, which assigns each codon to an amino-acid label or stop label.  Thus the datum is a label table contact, not a translation apparatus, not a folded-structure contact, not a physical-admissibility contact, and not a biological-function contact.

\paragraph{Observed residual row.}
The distinguished residual row $R$ has cardinality $13$.  Its codons are
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row is recorded as a punctured-cube pattern with face vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
The finite-row audit therefore treats the observed residual set as a concrete local object inside $Q_6$, not as a global law of molecular biology.  Its role is to test whether leave-one-codon deletion can be recovered by local closure and boundary structure in the code-label table.

\paragraph{Spectral row data.}
The spectral summary supplies three scalar values:
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The recorded cubic, or radial, polynomial residual is $-6.224354365258478\times 10^{-12}$, so the finite computation reports the spectral identity at numerical tolerance.  These numbers are used only as finite-row invariants of the codon-label display.  They do not imply that the physical translation system is governed by this geometry.

\paragraph{Shape and null comparisons.}
The shape-significance run used $N=2000$ samples with seed $42$.  It required $57377$ distinct-sample attempts and $55377$ duplicate retries.  The exact shape count was $1$, giving
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}
$$
The count with Jaccard similarity at least $0.9$ was $25$, giving
$$
\begin{aligned}
p_{J\geq 0.9}=0.0125.
\end{aligned}
$$
An amino-acid-class-preserving null run, also with $N=2000$ and seed $42$, reported observed $M$-size $20$, exact-match count $0$, and lower-tail count $7$.  Its exact-shape value is $0.0$, while the lower-tail value is
$$
\begin{aligned}
p_{\mathrm{lower\ tail}}=0.0035.
\end{aligned}
$$
These finite-row comparisons support the claim that the observed local shape is not typical under the tested null models.  They do not identify a biochemical mechanism.

\paragraph{Leave-one-code and leave-one-codon recovery.}
The leave-one-code-out row evaluated $23$ tables and $63$ targets.  Its baseline hit rate was $0.19821739130434785$.  The reported hit rate above baseline was $0.9841269841269841$, the hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary was $0.9841269841269841$.  The leave-one-codon-out row then records the more local test: anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate
$$
\begin{aligned}
\rho_{\mathrm{recover}}=1.0.
\end{aligned}
$$
The thresholded recovery-rate field is also $1.0$.  The finite interpretation is that every tested leave-one-codon event is recovered by the available predictable-or-boundary alternatives in this local display.  The recovery is a code-label recovery statement: it says that the finite coordinate, closure, spectrum, and relation data recover a label-side deletion pattern in the curated table.

\paragraph{Family and module structure.}
The family-level leave-one-code-out audit has $5$ families.  The hit-rate-above-baseline values are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate.  The leave-one-codon module classification is complete: $R$ has size $13$, the assigned count is $13$, and the extra-module, missing-from-module, and unassigned-codon lists are empty.  The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Each module has predictable-or-boundary status in the finite audit.  This is the strongest local statement supported by the row: the residual codons are fully assigned to named modules, and the local leave-one-codon test has no unassigned codon left over.

\paragraph{Boundary verdict.}
The ready claim is therefore narrow.  Window-six codon coordinates expose a local boundary-tile structure in the standard-code label table, and the finite-row data support a perfect leave-one-codon recovery rate for the tested residual row.  The result remains inside code-read.  No translation realization, folded protein structure, structural order, physical admissibility, biological function, universal mechanism, or biological necessity follows from this row unless a separate contact supplies that higher-layer evidence.  Amino-acid label-sequence readback is not folded protein structure, and a local codon tile is not a global biological law.