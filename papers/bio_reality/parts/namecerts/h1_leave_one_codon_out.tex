\origin{ai}
\subsection{Leave-One-Codon-Out Codon Boundary Test}
\label{sec:h1-leave-one-codon-out}

\paragraph{Question.}
The finite row named \texttt{h1.leave.one.codon.out} asks whether a window-six coordinate view of the standard-code codon table has enough local structure to recover omitted codon labels at the code-read layer. The carrier is the six-bit codon cube $Q_6$, with codons displayed as vertices and local tile faces used as finite probes. The biological contact is deliberately narrow: a curated standard genetic-code table assigning codons to amino-acid or stop labels. Thus the row tests label readback and local boundary structure, not translation dynamics, folding, physical chemistry, biological function, mechanism, or universality.

\paragraph{Boundary set.}
The distinguished codon set $R$ has cardinality $13$. The verified list is
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row is certified as a punctured-cube pattern with $f$-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
This records a small finite tile system: thirteen codon vertices, sixteen local edges, and four square faces. The point is not that the cube geometry is a biological law. The point is that the observed reassignment-sensitive codons occupy a sharply constrained finite shape in the window-six display.

\paragraph{Spectral coordinates.}
The row also carries a finite spectral summary. The measured values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The recorded cubic residual is $-6.224354365258478\cdot 10^{-12}$, the same value reported for the radial relation residual. This places the computation at numerical zero for the present finite audit. The spectrum is therefore used as a consistency check on the coordinate construction, not as evidence for a molecular mechanism.

\paragraph{Null comparisons.}
Against $2000$ random samples with seed $42$, the exact shape comparison produced $1$ exact match after $57377$ distinct-sample attempts and $55377$ duplicate retries. The exact-shape probability was $p=0.0005$, and the high-Jaccard event $J\geq 0.9$ occurred $25$ times, giving $p=0.0125$. A second null preserving amino-acid classes used $2000$ samples with the same seed. It found $0$ exact shape matches, $7$ lower-tail samples at or below the observed $M$ size, observed $|M|=20$, exact-shape probability $0.0$, and lower-tail probability $0.0035$. These numbers say that the local shape is not a typical draw under the stated finite randomizations. They do not say that the codon table is forced by the coordinate system.

\paragraph{Leave-one-code-out comparison.}
The related leave-one-code-out finite row evaluated $23$ tables and $63$ total targets. Its baseline hit rate was $0.19821739130434785$. The hit rate above baseline was $0.9841269841269841$, the hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus the boundary was $0.9841269841269841$. By family, the hit-rate-above-baseline values were $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate. These companion measurements support the interpretation that the same local codon region is repeatedly selected by finite code-read perturbations.

\paragraph{Leave-one-codon-out row.}
For the codon-level omission test itself, the verified row reports anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, recovery rate $1.0$, and recovery rate above threshold $1.0$. The absence of anchors is important: the successful recovery is not being attributed to fixed singleton anchors. Instead, the row separates predictable positions from boundary-supported positions inside the local tile. In this finite packet, every omitted codon target is recovered by the permitted code-layer data, with eight targets marked predictable and five codons accounted for by boundary behavior.

\paragraph{Module accounting.}
The module classification is complete over the same $13$ codons. It assigns all $13$ codons, leaves no extra module codons, leaves no codon missing from modules, and leaves no unassigned codons. The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Each module has either predictable support or boundary support. This is the finite reason the row can be read as a local codon-window test rather than as a loose list of exceptional codons.

\paragraph{Verdict.}
The row is ready as a code-read claim. The finite evidence supports the following constrained statement: in the six-bit codon cube, the reassignment-sensitive codons form a $13$-vertex punctured-cube tile with stable spectral coordinates; leave-one-codon-out recovery succeeds at rate $1.0$ inside the local predictable-or-boundary classification; and related code-table perturbations show hit rates far above the baseline $0.19821739130434785$. The result remains tied to curated codon-to-label readback. It does not license any claim about translation realization, folded protein structure, physical admissibility, biological function, a universal mechanism, or a global biological law unless a separate observational contact supplies that higher-layer information.