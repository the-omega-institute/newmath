\origin{ai}

\subsection{Leave-one-codon-out boundary recovery in the window-six codon cube}
\label{sec:h1-leave-one-codon-out}

\paragraph{Claim.}
This record concerns the standard-code codon table after it is displayed as a six-coordinate codon cube.  The claim is not that the cube is a biological mechanism.  The claim is narrower: when one codon label is withheld, the finite coordinate data recover the withheld row either as a predictable local closure case or as an explicitly boundary-facing case.  The observed leave-one-codon-out run reports no anchor cases, five boundary cases, eight high-coverage closure cases, and eight predictable cases.  The recovery rate is $1.0$, and the recorded threshold comparison is also $1.0$.

\paragraph{Finite codon set.}
The distinguished row set $R$ has cardinality $13$.  The verified list is
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
A separate cube-shape check identifies this same set as the punctured-cube row set and gives the face-count vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the finite object being read is not an arbitrary list of codons: it has a verified local cubical profile with thirteen vertices, sixteen edges, and four two-dimensional faces in the inspected boundary complex.

\paragraph{Spectral coordinates.}
The spectrum run supplies three scalar coordinates.  The recorded values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The same run records the radial cubic residual as $-6.224354365258478\times 10^{-12}$, and the spectral-radius tolerance value is $0.6752479308830213$.  These numbers are used only as finite numerical invariants of the codon display.  They do not identify a translational process, a folded structure, a thermodynamic state, or a functional phenotype.

\paragraph{Shape significance checks.}
The first null comparison used $N=2000$ sampled alternatives with seed $42$.  It required $57377$ distinct-sample attempts and $55377$ duplicate retries.  There was one exact shape match and twenty-five matches with Jaccard similarity at least $0.9$.  The corresponding probabilities are
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9} &= 0.0125.
\end{aligned}
$$
A second amino-acid-class-preserving null run also used $N=2000$ and seed $42$.  It reports observed $M$-size $20$, zero exact matches, and seven lower-tail samples with size at most the observed $M$-size.  Its finite probabilities are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
The input row contains the size of $M$ and the null statistics, but not a codon-by-codon listing of $M$; no unlisted $M$ rows are inferred here.

\paragraph{Leave-one-code and family controls.}
The related leave-one-code-out table check evaluated $23$ code tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$.  The observed hit rate above baseline is $0.9841269841269841$, the hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary rows is $0.9841269841269841$.  A by-family audit records five families.  The hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate.  These controls support the local label-recovery reading while keeping the evidence at the codon-label layer.

\paragraph{Module classification.}
The thirteen rows of $R$ are completely assigned to six named modules.  The module partition is
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
The classification audit gives $R$-size $13$, assigned count $13$, no extra module codons, no missing rows from the modules, and no unassigned codons.  Each listed module has either a predictable member or a boundary member, so the leave-one-codon-out behavior is distributed across the whole finite partition rather than isolated in one exceptional codon family.

\paragraph{Reality contact.}
The reality contact used here is the curated standard genetic-code table, interpreted only as a codon-to-label table.  It supplies the amino-acid or stop label attached to each codon in the standard code.  The contact does not observe translation dynamics, ribosomal realization, structural order, physical admissibility, folded protein geometry, biological function, or a universal law of genetics.  Therefore the admissible readback is: the six-bit codon display exposes a finite local tile and boundary pattern whose labels agree with curated standard-code rows.  Any claim beyond codon-label readback requires a different contact.

\paragraph{Boundary verdict.}
The finite result is ready at the code-read layer.  The leave-one-codon-out row gives recovery rate $1.0$ with five boundary cases and eight predictable cases.  The row set $R$ is a verified thirteen-codon punctured-cube set with face vector $(13,16,4)$, its spectrum supplies stable scalar invariants, and the module classification covers all thirteen rows without missing or extra codons.  The same data also block over-reading: no translation, folding, physical admissibility, function, mechanism, or universality follows from this record unless a separate experiment or curated contact explicitly supplies that higher-layer observation.