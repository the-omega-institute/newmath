\chapter{Leave-One-Codon-Out Boundary Test for a Local Codon Tile}
\label{ch:bioreality-h1-leave-one-codon-out}
\origin{ai}

This chapter records a finite BioReality test for the standard-code codon table at the code-read layer. The carrier is the six-bit codon cube $Q_6$, read through a curated standard genetic-code table. The internal BEDC-side structure consists of coordinate, closure, spectrum, and relation data, while the biological contact is only the codon-to-label table. The result is therefore a local boundary-tile record, not a claim about translation, folding, physical admissibility, biological function, mechanism, or universality.

\subsection{Finite Carrier, Punctured Tile, and Label Contact}
\label{sec:h1-leave-one-codon-out-carrier}

\paragraph{Carrier and readback.}
The finite carrier is the codon cube $Q_6$, with each codon represented as a six-bit display over the four nucleotide letters. The readback contact is a curated standard-code table assigning each codon to an amino-acid or stop label. This contact has codon-label resolution only: it can test code-read claims and local label splits, but it cannot test translation realization, structural order, physical admissibility, protein function, or any universal biological mechanism.

\paragraph{Recovered relation row.}
The relation row $R$ has cardinality $13$ and is reported as the following finite set:
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}, \qquad |R|=13.
\end{aligned}
$$
The punctured-cube check gives the same codon list and the finite face-vector
$$
\begin{aligned}
f(R)=(13,16,4).
\end{aligned}
$$
Thus the row is not being used as an open-ended biological class. It is a thirteen-codon finite subobject inside $Q_6$, equipped with local face data. The table contact then supplies labels for these codons, but the geometry itself does not supply those labels.

\paragraph{Spectral row.}
The spectral probe reports
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial polynomial value is $-6.224354365258478\times 10^{-12}$, also reported as the cubic-polynomial residual. This is effectively zero at the stated numerical tolerance and supports treating the finite row as a stable local spectral object. The number is not a physical law; it is a finite-coordinate diagnostic attached to the codon-tile row.

\subsection{Leave-One-Codon-Out Recovery}
\label{sec:h1-leave-one-codon-out-recovery}

\paragraph{Primary recovery statistic.}
The leave-one-codon-out row reports $8$ predictable cases, $5$ boundary cases, and $0$ anchor cases. The closure-coverage-high count is $8$, and the reported recovery rate is
$$
\begin{aligned}
\operatorname{recovery\_rate}=1.0, \qquad
\operatorname{recovery\_rate\_above\_threshold}=1.0.
\end{aligned}
$$
This means that, for the finite test actually supplied, every evaluated codon omission is either recovered by the local predictable structure or lands on the boundary side of the tile. The statistic supports a code-layer local split: it does not say that the omitted codon is biologically generated by the geometry, nor that the corresponding amino-acid label realizes a folded structure.

\paragraph{Module classification.}
The module classification accounts for all thirteen relation codons. It reports $R$-size $13$, assigned count $13$, no missing module codons, no extra module codons, and no unassigned codons. The finite modules are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
Each listed module has a predictable-or-boundary status. The completion check is important because it prevents the chapter from presenting a selective success on only the easiest codons: the supplied module partition covers exactly the thirteen-codon relation row.

\subsection{Comparison Rows and Null Pressure}
\label{sec:h1-leave-one-codon-out-null-pressure}

\paragraph{Leave-one-code-out comparison.}
The broader leave-one-code-out comparison evaluated $23$ code tables and $63$ total targets. Its baseline hit rate is $0.19821739130434785$, while the reported hit rate above baseline is $0.9841269841269841$. The hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$. These rows are used here only as supporting context: they say that the local boundary mechanism is not isolated to a single removed-codon calculation, while still remaining a code-read statistic.

\paragraph{Family rows.}
The by-family row reports $5$ families. The family hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\_yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\_plastid} &= 1.0,\\
\mathrm{candidate\_other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\_ciliate} &= 1.0.
\end{aligned}
$$
This supports the finite-row interpretation that the codon-tile boundary is a robust code-table readback pattern across the supplied family grouping. It does not promote the result to a universal property of DNA, translation, protein structure, or organismal function.

\paragraph{Shape-significance rows.}
Against the reported shape null, $N=2000$ samples were used. There was $1$ exact shape match, giving $p_{\mathrm{exact}}=0.0005$, and $25$ samples with Jaccard similarity at least $0.9$, giving $p_{J\ge 0.9}=0.0125$. The sampling row records $57377$ distinct sample attempts and $55377$ duplicate retries under seed $42$. Against the amino-acid-class-preserving null, the observed $M$-size is $20$, the exact-shape match count is $0$, and the lower-tail count at or below the observed $M$-size is $7$ out of $2000$. The corresponding reported values are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
These null rows provide finite empirical pressure that the observed tile is not a routine artifact of the tested randomizations. They do not identify a biochemical cause.

\subsection{Boundary Verdict}
\label{sec:h1-leave-one-codon-out-boundary-verdict}

\paragraph{What the row supports.}
The finite evidence supports the following code-read statement: in the six-bit codon cube, the thirteen-codon relation row $R$ forms a punctured local tile with face-vector $(13,16,4)$, stable spectral diagnostics, complete module accounting, and leave-one-codon-out recovery by predictable-or-boundary status. The curated standard-code table supplies the reality contact needed to read the codons as amino-acid or stop labels. Within that scope, the chapter records a positive local boundary-tile result.

\paragraph{What the row does not support.}
No translation process follows from this row. No folded protein structure follows from amino-acid label readback. No physical admissibility, biological function, mechanism, or universality follows from the local tile. The curated standard-code contact contains codon-to-label assignments, not ribosomal dynamics, structural measurements, energetics, organismal fitness, or functional assays. Any claim beyond code-read must therefore be blocked unless a separate higher-layer contact is supplied.

\paragraph{Final local claim.}
The leave-one-codon-out row is ready as a BioReality finite-row chapter at the code-read layer. It records a strong local tile and boundary recovery pattern: $8$ predictable codons, $5$ boundary codons, $0$ anchor codons, complete thirteen-codon module coverage, and recovery rate $1.0$. Its proper interpretation is narrow and finite. The result is a BEDC-discovered codon-coordinate pattern tested against a curated label table, not a global biological law.