\subsection{Leave-one-codon-out boundary tile for the standard code}\label{sec:h1-leave-one-codon-out}
\origin{ai}

\paragraph{Carrier and contact.}
This finite-row record uses the six-bit codon cube $Q_6$ as its carrier.  The displayed distinctions are the six-bit codon display, a local tile face, a singleton label corner, and a three-point label class.  The internal structure used for the readback is coordinate, closure, spectrum, and relation data; the only external contact is a curated standard-code label table.  Thus the biological object under test is the standard-code codon table at the code-read layer.  The contact says that codons are assigned to amino-acid or stop labels, but it does not say that the local tile is translation, folding, physical admissibility, function, mechanism, or universality.

\paragraph{The finite exceptional row.}
The distinguished row $R$ has cardinality $13$.  Its observed codons are
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same row is reported as a punctured cube with $f$-vector $(13,16,4)$.  This is the local combinatorial object used by the leave-one-codon-out check.  The record should therefore be read as a finite boundary tile inside $Q_6$, not as a claim that the global genetic code is generated by cube geometry alone.

\paragraph{Spectral and shape controls.}
The row also carries a finite spectral signature.  The supplied scalar values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The reported cubic, or radial, polynomial residual is $-6.224354365258478\cdot 10^{-12}$, which is effectively zero at the stated numerical tolerance.  Against the shape null model with $N=2000$ samples and seed $42$, the exact shape count is $1$, giving $p_{\mathrm{exact}}=0.0005$, while the high-Jaccard count is $25$, giving $p_{J\geq 0.9}=0.0125$.  The sample required $57377$ distinct-sample attempts and $55377$ duplicate retries.  Under the amino-acid-class-preserving null, again with $N=2000$ and seed $42$, the observed $M$ size is $20$, the exact shape count is $0$, and the lower-tail count at or below the observed $M$ size is $7$.  The corresponding finite-row probabilities are $p_{\mathrm{aa,class,exact}}=0.0$ and $p_{\mathrm{aa,class,lower\ tail}}=0.0035$.

\paragraph{Leave-one-codon-out result.}
For the specific claim $h1.leave.one.codon.out$, the leave-one-codon-out row has anchor count $0$, boundary count $5$, closure-high coverage $8$, and predictable count $8$.  The reported recovery rate is
$$
\begin{aligned}
\mathrm{recovery\ rate}=1.0,
\end{aligned}
$$
and the recovery-rate-above-threshold scalar is also $1.0$.  In words, every tested omission in the finite row is recovered by the stated local rule as predictable or boundary-supported, with no anchor cases recorded.  This is a code-layer recovery statement about curated labels on a finite codon row.  It is not a statement about ribosomal realization, protein structure, admissible chemistry, cellular function, or a universal mechanism.

\paragraph{Module accounting.}
The module classification accounts for all $13$ codons in $R$: the reported $R$ size is $13$, the assigned count is $13$, and the extra, missing, and unassigned sets are all empty.  The six modules are
$$
\begin{aligned}
agr&=\{AGA,AGG\},\\
ar\_lys&=\{AAA\},\\
cun&=\{CUA,CUC,CUG,CUU\},\\
ile\_met&=\{AUA\},\\
ssn&=\{UCA,UUA\},\\
stop\_trp&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
Each module has a predictable or boundary entry in the supplied classification.  This is the strongest local reading of the row: the tile is complete as a finite partition of the observed exceptional codons, and the leave-one-codon-out rule closes over that partition at the stated recovery rate.

\paragraph{Neighboring code-level evidence.}
The related leave-one-code-out experiment evaluated $23$ code tables and $63$ targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$.  By family, the reported above-baseline hit rates are $1.0$ for bacterial-plastid, mitochondrial, nuclear-ciliate, and candidate-other families, and $0.8333333333333334$ for alternative yeast, across $5$ families.  These neighboring rows support the same code-read interpretation: finite codon-label geometry is informative for local label recovery, but the contact remains a curated label table.

\paragraph{Local boundary.}
The supported statement is that window-six codon coordinates expose a local boundary-tile pattern in the standard-code label table.  The forbidden promotion is explicit.  No translation realization follows from this row.  No folded structure follows from amino-acid label readback.  No physical admissibility, biological function, mechanism, or universality follows from the local tile.  Any claim crossing those boundaries requires a separate reality contact whose observed fact is about translation, structure, admissibility, function, mechanism, or universality rather than about codon-to-label assignment.