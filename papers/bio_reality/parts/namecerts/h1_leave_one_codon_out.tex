\subsection{Leave-one-codon-out boundary recovery for the standard-code table}
\label{subsec:h1-leave-one-codon-out}
\origin{ai}

\paragraph{Finite carrier and reality contact.}
This record concerns the finite six-bit codon display $Q_6$ together with a curated standard-code table as the only reality contact.  The carrier is not being treated as a biological mechanism.  It is a finite coordinate display on which codons are read back as code labels.  The contact supplies the observed fact that the standard genetic-code table assigns each codon to an amino-acid or stop label.  The contact does not observe translation realization, folded structure, physical admissibility, biological function, a universal mechanism, or any necessity claim saying that DNA must obey the displayed geometry.

\paragraph{The punctured-cube row.}
The finite distinguished row $R$ has cardinality $13$.  In the verified run its entries are
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same run verifies that this row is the punctured-cube row in the window-six display, with face data summarized by the $f$-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the local object used by the leave-one-codon-out test is not an informal visual motif: it is the concrete finite set above, equipped with the displayed face counts.  The set is still only a code-layer object.  A local tile face and a singleton label corner can organize label readback, but they do not promote the row into a statement about translation, folding, physical admissibility, function, mechanism, or universality.

\paragraph{Spectral scale.}
The same finite construction carries a spectral certificate.  The verified values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\cdot 10^{-12}$, which is also the reported cubic-polynomial residual.  The tolerance-level spectral radius recorded for $M$ is $0.6752479308830213$.  These numbers support the claim that the row is being evaluated inside a reproducible finite coordinate calculation.  They do not identify a molecular dynamic, a folding landscape, or a biochemical admissibility law.

\paragraph{Shape significance and the observed $M$ size.}
The shape-significance run used $N=2000$ null samples with seed $42$.  It required $57377$ distinct-sample attempts and incurred $55377$ duplicate retries.  Among those samples, the exact-shape match count was $1$, giving
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{J\geq 0.9}&=0.0125.
\end{aligned}
$$
The high-Jaccard count was $25$.  An amino-acid-class-preserving null model, again with $N=2000$ and seed $42$, reported observed $M$ size $20$, exact match count $0$, and lower-tail count $7$.  Its finite-row probabilities were
$$
\begin{aligned}
p_{\mathrm{aa\ class,exact}}&=0.0,\\
p_{\mathrm{aa\ class,lower\ tail}}&=0.0035.
\end{aligned}
$$
The supplied finite data do not include an explicit codon-by-codon list for $M$; consequently this chapter uses the verified $M$ row size and null-model statistics, while spelling out the supplied $R$ row exactly.

\paragraph{Leave-one-code-out background.}
The companion leave-one-code-out calculation evaluated $23$ code tables and $63$ targets.  Its baseline hit rate was $0.19821739130434785$.  Against that baseline, the reported hit rate above baseline was $0.9841269841269841$, the hit rate in $M-T$ was $0.9365079365079365$, and the hit rate in $M-T$ plus boundary was $0.9841269841269841$.  By family, the finite summary splits into $5$ families with hit rates above baseline
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
These values supply the code-read background for the codon-level test, but they still remain table-level readback values.  They do not certify translation, structure, physical admissibility, function, mechanism, or universality.

\paragraph{Leave-one-codon-out result.}
For the claim $h1.leave.one.codon.out$, the verified run reports anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, recovery rate $1.0$, and recovery rate above threshold $1.0$.  In finite-row terms, removing one codon at a time from the local code-read tile does not require a hidden anchor row in this run: the recovered information is carried by predictable closure behavior and boundary status.  The numerical summary is
$$
\begin{aligned}
\mathrm{anchor\ count}&=0,\\
\mathrm{boundary\ count}&=5,\\
\mathrm{closure\ coverage\ high}&=8,\\
\mathrm{predictable\ count}&=8,\\
\mathrm{recovery\ rate}&=1.0.
\end{aligned}
$$
Thus the local conclusion is narrow but concrete: in the supplied standard-code contact, the window-six row exposes a leave-one-codon-out recovery pattern at the code-read layer.

\paragraph{Module classification.}
The module classification is complete on the same $13$-codon row.  The reported $R$ size is $13$, the assigned count is $13$, and there are no extra module codons, no missing codons from modules, and no unassigned codons.  The finite modules are
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
Every listed module has predictable-or-boundary status in the supplied classification.  This is the most precise local form of the claim: the row is completely assigned to finite modules, and the leave-one-codon-out behavior is covered by predictable or boundary behavior module by module.

\paragraph{Boundary verdict.}
The chapter therefore records a ready code-layer result, not a biological universality theorem.  The standard-code table contact supports codon-label readback and local split checks.  It blocks promotion to protein realization, folded structure, physical admissibility, biological function, universal mechanism, or any claim that window-six geometry alone is translation.  Amino-acid label-sequence readback is not folded protein structure.  Any future crossing from code readback into translation, structure, admissibility, or function would require a separate reality contact measuring that higher layer directly.