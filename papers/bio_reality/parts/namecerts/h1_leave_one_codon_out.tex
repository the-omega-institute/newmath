\subsection{Leave-One-Codon-Out Boundary Readback}\label{sec:h1-leave-one-codon-out}\origin{ai}

\paragraph{Finite carrier and reality contact.}
This record concerns the standard-code codon table viewed through a finite six-bit codon display. The carrier is the codon cube $Q_6$, and the relevant distinctions are a local tile face, a singleton label corner, and a three-point label class. The only external contact used here is a curated standard-code table assigning each codon to an amino-acid or stop label. That contact supports code-layer label readback; it is not a mechanism for translation, a structural contact, a physical-admissibility test, or a function assay.

\paragraph{Observed exceptional row.}
The row $R$ has cardinality $13$. In codon notation the verified row is
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same row is certified as a punctured cube with $f$-vector $(13,16,4)$. Thus the row is not merely a list of labels selected after the fact: it carries a finite cubical boundary signature, with thirteen vertices, sixteen one-dimensional adjacencies, and four square faces after the missing-corner puncture is imposed.

\paragraph{Spectral finite-row check.}
The spectral probe reports $\lambda_M = 0.6752479308830213$, $\lambda_R = 0.4758352843197002$, and $\mu = 3.051487585298128$. The radial cubic residual is $-6.224354365258478 \cdot 10^{-12}$, recorded equivalently as the cubic-polynomial residual and the radial-polynomial value. The tolerance-level spectral-radius check therefore uses the value $0.6752479308830213$ for $\lambda_M$. These numbers are used only as finite-row diagnostics of the local codon tile; they do not by themselves identify a biological law.

\paragraph{Shape significance against null rows.}
Against the shape null with seed $42$, the finite-row audit used $N=2000$ distinct samples after $57377$ sample attempts and $55377$ duplicate retries. It found $1$ exact shape match and $25$ samples with Jaccard score at least $0.9$. Hence
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9} &= 0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null, also with $N=2000$ and seed $42$, recorded observed $M$-size $20$, exact-match count $0$, and lower-tail count $7$ for rows no larger than the observed $M$-size. Its reported values are $p=0.0$ for exact shape under that null and $p=0.0035$ for the lower tail. These are finite combinatorial contrasts with label-preserving randomizations; they do not promote the tile to translation, folding, physical admissibility, function, mechanism, or universality.

\paragraph{Leave-one-code-out and leave-one-codon-out readback.}
The leave-one-code-out audit evaluated $23$ code tables and $63$ total targets. Its baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$. The hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary was $0.9841269841269841$. At the codon level, the leave-one-codon-out run reported anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, recovery rate $1.0$, and recovery-rate-above-threshold $1.0$. Thus the row has no anchor-only explanation in this audit: its support is boundary and closure coverage inside the finite codon display.

\paragraph{Module classification.}
The module audit partitions all thirteen row codons without extras or omissions. The complete assignment is
$$
\begin{aligned}
\mathrm{agr} &= \{AGA, AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA, CUC, CUG, CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA, UUA\},\\
\mathrm{stop\_trp} &= \{UAA, UAG, UGA\}.
\end{aligned}
$$
The module-classification row count is $R\_size=13$ with assigned count $13$, no extra module codons, no codons missing from the modules, and no unassigned codons. Each module has either predictable or boundary status. This makes the row a finite local readback object rather than a global biological mechanism.

\paragraph{Family-level robustness.}
The by-family leave-one-code-out check reports $5$ families. The family hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial/plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate. These values support the claim that the local codon-tile boundary is visible across the audited code-table families at the code-read layer.

\paragraph{Boundary verdict.}
The finite-row data support a chapter-local statement: window-six codon coordinates expose a punctured-cube boundary tile whose labels can be read back through the curated standard-code table. The supported layer is code readback and local label-split checking. No translation realization, folded structure, structural order, physical admissibility, biological function, universal mechanism, or biological necessity follows from this contact. Any such stronger claim would require a separate external contact with its own finite audit.