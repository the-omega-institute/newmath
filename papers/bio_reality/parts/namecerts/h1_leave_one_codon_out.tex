\origin{ai}

\subsection{Leave-One-Codon Readback in the Window-Six Codon Cube}
\label{sec:h1-leave-one-codon-out}

\paragraph{Finite carrier and contact.}
This record concerns the standard-code codon table viewed through a finite six-bit codon display. The carrier is the codon cube $Q_6$, with coordinates read as a window-six encoding of codon positions and base choices. The biological contact used here is deliberately narrow: a curated standard-code table assigns each codon to an amino-acid label or to a stop label. Thus the observed object is a code-layer label table, not a translation apparatus, not a folding experiment, not a physical-admissibility test, and not a functional assay. The BEDC-side distinctions used in this row are the six-bit codon display, a local tile face, a singleton label corner, and a three-point label class. The internal structure recorded for the row consists of coordinate, closure, spectrum, and relation data, and the readback target is the standard-code label table.

\paragraph{Exceptional row.}
The finite exceptional row $R$ has cardinality $13$. The observed row is
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row is recorded as a punctured-cube object with face-vector
$$
\begin{aligned}
f(R)=(13,16,4).
\end{aligned}
$$
This is a finite combinatorial fact about the displayed codon coordinates and the curated label readback. It is not a statement that the codon table is generated by geometry as a biological necessity. The row only says that this particular set of thirteen codons is the boundary-sensitive region seen by the window-six probe.

\paragraph{Spectral and null-model checks.}
The spectral summary separates the displayed module $M$ from the exceptional row $R$. The supplied scalar values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The associated radial cubic check is numerically closed at
$$
\begin{aligned}
-6.224354365258478\cdot 10^{-12},
\end{aligned}
$$
which is treated here as tolerance-level zero for the finite computation. The shape test used $N=2000$ sampled null rows with seed $42$. It found one exact shape match, so the exact-shape frequency was
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}
$$
It also found $25$ rows with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{J\geq 0.9}=0.0125.
\end{aligned}
$$
The sample required $57377$ distinct-sample attempts and incurred $55377$ duplicate retries. Under the amino-acid-class-preserving null model, again with $N=2000$ and seed $42$, the observed $M$ size was $20$, the exact-match count was $0$, and the lower-tail count at or below the observed $M$ size was $7$. The corresponding reported values are
$$
\begin{aligned}
p_{\mathrm{class\ exact}}&=0.0,\\
p_{\mathrm{class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These numbers support a finite-row anomaly at the code-read layer. They do not by themselves identify a biochemical cause.

\paragraph{Leave-one-codon result.}
For the claim $\mathrm{h1.leave.one.codon.out}$, the leave-one-codon probe reports no anchor cases, five boundary cases, and eight predictable cases:
$$
\begin{aligned}
\mathrm{anchor\_count}&=0,\\
\mathrm{boundary\_count}&=5,\\
\mathrm{predictable\_count}&=8,\\
\mathrm{closure\_coverage\_high}&=8.
\end{aligned}
$$
The reported recovery rate is
$$
\begin{aligned}
\mathrm{recovery\_rate}=1.0,
\end{aligned}
$$
and the thresholded recovery-rate check is also $1.0$. Thus every tested leave-one-codon instance in this finite row is recovered either as predictable or as boundary-supported by the local tile construction. Since the anchor count is zero, the record should not be read as saying that individual codons carry independent geometric anchors. The better reading is local: the window-six coordinate system sees boundary and closure structure around the exceptional row.

\paragraph{Module partition.}
The module classification accounts for all thirteen codons in $R$ with no missing codons, no extra module codons, and no unassigned codons. The six modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The classification summary is
$$
\begin{aligned}
|R|&=13,\\
\mathrm{assigned\_count}&=13,\\
\mathrm{missing\_from\_modules}&=0,\\
\mathrm{extra\_module\_codons}&=0,\\
\mathrm{unassigned\_codons}&=0.
\end{aligned}
$$
Equivalently, every module has at least one predictable-or-boundary explanation in the local probe. This is the finite combinatorial content of the module row; it is not a claim that the modules are realized as evolutionary, biochemical, or functional units.

\paragraph{Code-table comparison.}
The neighboring leave-one-code-out table-level probe evaluated $23$ tables and $63$ total targets. Its baseline hit rate was
$$
\begin{aligned}
0.19821739130434785,
\end{aligned}
$$
while the hit rate above baseline was
$$
\begin{aligned}
0.9841269841269841.
\end{aligned}
$$
The reported hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary was $0.9841269841269841$. The family-level split contains five families. Their hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\_yeast}&=0.8333333333333334,\\
\mathrm{bacterial\_plastid}&=1.0,\\
\mathrm{candidate\_other}&=1.0,\\
\mathrm{mitochondrial}&=1.0,\\
\mathrm{nuclear\_ciliate}&=1.0.
\end{aligned}
$$
These table-level and family-level numbers are supporting context for the codon-level row. They show that the local code-read probe remains strong when compared with related code-table variation, while preserving the same layer restriction.

\paragraph{Boundary of the claim.}
The contact used in this row is the curated standard genetic-code bridge. It can test codon-label readback and local codon-tile label splits. It cannot test translation realization, folded structure, structural order, physical admissibility, biological function, universal mechanism, or any claim that DNA must obey this geometry as a biological law. The observed alignment is therefore: the code table supports local code-layer readback, and the local tile boundary blocks promotion to higher biological layers. Amino-acid label-sequence readback is not folded protein structure. A separate structure contact, physical experiment, translation model, or function assay would be required before any such higher-layer statement could be made.

\paragraph{Verdict.}
The finite row is ready as a code-read result. In $Q_6$, the thirteen-codon row $R$ is recovered as a punctured-cube boundary object with complete module assignment, strong leave-one-codon recovery, nontrivial spectral separation, and small null-model frequencies. The correct mathematical statement is local and finite: window-six codon coordinates expose boundary-tile structure in the standard-code label table. The correct biological restraint is equally local: curated codon labels are external-reality contacts for code readback only, and they do not supply translation, folding, physical admissibility, function, mechanism, or universality.