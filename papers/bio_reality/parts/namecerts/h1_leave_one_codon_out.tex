\subsection{Leave-One-Codon-Out Boundary Tile Record}\label{sec:h1-leave-one-codon-out}
\origin{ai}

\paragraph{Finite carrier and readback contact.}
This record treats the standard-code codon table as a finite code-read object, not as a mechanism for translation or protein formation.  The carrier is the six-bit codon cube $Q_6$, with rows read back through the curated standard genetic-code table.  The only external contact used here is the finite assignment of codons to amino-acid or stop labels.  Thus the evidence can test a codon-label split inside the code layer, but it cannot by itself test translation realization, folded structure, physical admissibility, biological function, universal mechanism, or any claim that DNA must obey this geometry as a biological law.

The distinguished residual set $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
As a punctured-cube object, this finite row has face-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
The point of the record is therefore narrow: it asks whether the leave-one-codon-out probe sees recoverable local boundary structure in this finite codon-label display.

\paragraph{Spectral and shape controls.}
The associated spectral row gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The radial cubic check is numerically closed at residual $-6.224354365258478\cdot 10^{-12}$, recorded both as the cubic-polynomial value and the radial-polynomial value.  These scalars do not assert biology beyond the table; they only certify that the finite coordinate object has a stable numerical signature under the stated computation.

The shape-significance control used $N=2000$ sampled alternatives with seed $42$.  Among $57377$ distinct-sample attempts, $55377$ retries were duplicates, and exactly $1$ exact shape match was found.  Thus
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005,\qquad p_{J\geq 0.9}=0.0125,
\end{aligned}
$$
with $25$ samples at Jaccard similarity at least $0.9$.  A second amino-acid-class-preserving null also used $N=2000$ and seed $42$; it observed $0$ exact matches, an observed $M$-size of $20$, and $7$ samples in the lower tail at or below that observed size.  Its finite-row values are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}=0.0,
\qquad
p_{\mathrm{aa\ class\ lower\ tail}}=0.0035.
\end{aligned}
$$
These controls support non-random-looking finite geometry in the code-read display, while leaving the biological interpretation bounded by the contact.

\paragraph{Leave-one-code-out background.}
The broader leave-one-code-out row evaluated $23$ tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate inside $M\setminus T$ was $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus the boundary was $0.9841269841269841$.  By family, $5$ families were represented: alternative yeast had hit rate above baseline $0.8333333333333334$, and the bacterial-plastid, candidate-other, mitochondrial, and nuclear-ciliate families each had hit rate above baseline $1.0$.  These numbers give the leave-one-codon-out row a finite comparative background, but they do not promote the local geometry to an evolutionary, biochemical, or universal law.

\paragraph{Leave-one-codon-out result.}
For the claim $\mathrm{h1.leave.one.codon.out}$ itself, the finite row reports anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, recovery rate $1.0$, and recovery rate above threshold $1.0$.  In symbols, the measured summary is
$$
\begin{aligned}
\mathrm{anchors}&=0,&
\mathrm{boundary}&=5,&
\mathrm{predictable}&=8,\\
\mathrm{high\ closure\ coverage}&=8,&
\mathrm{recovery\ rate}&=1.0,&
\mathrm{threshold\ recovery}&=1.0.
\end{aligned}
$$
The interpretation is that all codons counted as recoverable in this probe are recovered by local closure or boundary behavior in the six-bit display.  Since the anchor count is zero, the row should not be read as saying that a single global anchor determines the codon table.  It says only that this particular leave-one-codon-out test exposes a local boundary-tile pattern in the curated code-label table.

\paragraph{Module-level accounting.}
The module classification is complete for the same $13$ codons: the reported $R$-size is $13$, the assigned count is $13$, and there are no missing module rows, no extra module codons, and no unassigned codons.  The finite partition is
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},&
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},&
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},&
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every listed module has a predictable-or-boundary witness in the recorded classification.  This is the strongest chapter-local conclusion: within the finite $Q_6$ codon display and under the curated standard-code readback, the residual $13$-row set admits complete module accounting and perfect recovery in the leave-one-codon-out boundary probe.

\paragraph{Boundary verdict.}
The verdict is a code-layer result.  The support condition is satisfied because the curated code table supports codon-label readback and local split checks.  The break condition is also explicit: the result would be overread if it were promoted to translation realization, folded protein structure, physical admissibility, biological function, universal mechanism, or a claim that window-six geometry is a biological necessity.  Amino-acid label-sequence readback is not folded structure.  A local codon tile is not a global biological law.  Therefore this finite-row chapter records a ready code-read boundary result, with the exact scope limited to the standard-code label table and the local leave-one-codon-out geometry described above.