\origin{ai}
\subsection{Leave-one-codon-out recovery as a local code-layer boundary test}
\label{sec:biorality-h1-leave-one-codon-out}

\paragraph{Finite carrier and readback contact.}
This record treats the standard-code codon table as a finite code-layer readback over the six-bit codon cube $Q_6$.  The carrier is not a biochemical mechanism; it is the finite coordinate display in which each codon has three nucleotide positions and therefore six binary coordinates after the purine-pyrimidine and strong-weak style distinctions are fixed by the finite row.  The external contact is the curated standard genetic-code table, which supplies only codon-to-label assignments.  Thus the admissible observation is a code-read observation: a codon may be read back as an amino-acid label or a stop label.  No translation realization, folded structure, physical admissibility, biological function, universal law, or mechanism is inferred from this contact.

\paragraph{Recovered boundary row.}
The finite exceptional row $R$ has cardinality $13$.  In the verified row it is exactly
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row is certified as a punctured-cube pattern with face-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
This means that the observed row is not merely a list of labels; it carries a finite local tile shape inside $Q_6$.  The chapter-specific claim is exactly this finite claim: leaving out one codon from the code-read contact can still recover a local boundary or predictable class inside the same finite coordinate window.

\paragraph{Leave-one-codon-out result.}
For the codon-level leave-one-out probe, the verified row reports
$$
\begin{aligned}
\mathrm{anchor\_count}&=0,\\
\mathrm{boundary\_count}&=5,\\
\mathrm{predictable\_count}&=8,\\
\mathrm{closure\_coverage\_high}&=8,\\
\mathrm{recovery\_rate}&=1.0.
\end{aligned}
$$
Thus all recovered codons are accounted for by the finite split into predictable and boundary cases, with no anchor cases in this row.  The number $8$ records the high-coverage closure part, and the number $5$ records the boundary part.  The equality $8+5=13$ matches the cardinality of $R$, so the row closes as a finite code-layer partition of the exceptional codons.  The reported value $\mathrm{recovery\_rate\_above\_threshold}=1.0$ is read only as a finite-row recovery statistic, not as evidence for a biological necessity principle.

\paragraph{Module classification.}
The module classification assigns every codon in $R$ to one of six named local modules, with no missing or extra codons.  The verified module row is
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The completeness audit gives $R\_\mathrm{size}=13$, $\mathrm{assigned\_count}=13$, $\mathrm{extra\_module\_codons}=\varnothing$, $\mathrm{missing\_from\_modules}=\varnothing$, and $\mathrm{unassigned\_codons}=\varnothing$.  This is the finite reason that the leave-one-codon-out result can be stated modulewise: each exceptional codon belongs to exactly one listed local class in the recorded partition.

\paragraph{Spectral and null-model checks.}
The associated spectral row records
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial polynomial value and the cubic-polynomial residual are both reported as $-6.224354365258478\cdot 10^{-12}$, which is numerically consistent with zero at the stated tolerance.  The shape-significance row used $N=2000$ samples, with $57377$ distinct-sample attempts and $55377$ duplicate retries.  It found $1$ exact shape match and $25$ matches with Jaccard similarity at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{J\geq 0.9}&=0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null also used $N=2000$.  It reported $\mathrm{observed\_M\_size}=20$, $\mathrm{exact\_match\_count}=0$, $\mathrm{lower\_tail\_count\_le\_observed\_M\_size}=7$, $p_{\mathrm{aa\ class\ exact}}=0.0$, and $p_{\mathrm{aa\ class\ lower\ tail}}=0.0035$.  These null rows support the statement that the finite tile is statistically non-generic under the recorded sampling schemes.  They still do not promote the tile into a translation, folding, admissibility, function, mechanism, or universality claim.

\paragraph{Code-table comparison rows.}
The code-level leave-one-out row evaluated $23$ tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary was $0.9841269841269841$.  By family, the recorded rates above baseline were $1.0$ for bacterial-plastid, candidate-other, mitochondrial, and nuclear-ciliate families, and $0.8333333333333334$ for the alternative-yeast family, across $5$ families.  These rows provide context for the codon-level recovery claim: the local codon tile is aligned with a broader finite code-read phenomenon, but the contact remains a curated table contact.

\paragraph{Boundary verdict.}
The finite-row verdict is that the six-bit codon display exposes a local boundary-tile structure for the standard-code readback.  The row $R$ has $13$ codons, decomposes into complete local modules, satisfies the recorded punctured-cube face-vector $(13,16,4)$, and has a leave-one-codon-out recovery rate of $1.0$ with $8$ predictable and $5$ boundary cases.  The correct interpretation is therefore local and code-layered: the curated standard-code table supports codon-label readback and local split checks.  It does not by itself test translation realization, folded protein structure, structural order, physical admissibility, biological function, universal mechanism, or biological necessity.  Any such higher-layer statement requires a separate reality contact whose observable actually lives at that layer.