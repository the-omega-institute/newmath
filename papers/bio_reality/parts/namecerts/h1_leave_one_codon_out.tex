\subsection{Leave-One-Codon-Out Local Tile Boundary}\label{sec:h1-leave-one-codon-out}
\origin{ai}

\paragraph{Claim.} The claim \texttt{h1.leave.one.codon.out} records a finite code-read phenomenon inside the six-bit codon cube $Q_6$.  The carrier is the codon display with coordinates induced by the usual four-letter codon alphabet, and the observed structure is a local tile boundary rather than a biological law.  The row is therefore read as follows: window-six codon coordinates expose a finite boundary-tile pattern, while the amino-acid and stop labels remain supplied by the curated standard-code contact.  The chapter uses only code-layer readback.  It does not claim translation realization, folding, physical admissibility, biological function, mechanism, or universality.

\paragraph{Finite row.} The finite exceptional row $R$ has cardinality $13$.  The observed codons are
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row is also recorded as a punctured-cube object with face vector $(13,16,4)$.  Thus the data are not merely a list of labels: the row has a finite cubical boundary signature, with thirteen vertices, sixteen one-dimensional adjacencies, and four two-dimensional faces in the induced local tile.  The label readback is still external: the standard genetic-code table supplies the codon-to-amino-acid-or-stop labels, and the cubical row supplies only a coordinate pattern on those labels.

\paragraph{Spectral coordinates.} The associated spectral record gives
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The cubic radial check is numerically closed at $-6.224354365258478\cdot 10^{-12}$, the same value reported for the radial polynomial residual.  Within this finite row, the spectral radius assigned to $M$ is therefore $0.6752479308830213$, and the residual is at numerical roundoff scale for the stated polynomial test.  This supports the claim that the local tile is a reproducible finite-coordinate object, not that the tile is a mechanism of translation or a constraint on protein structure.

\paragraph{Leave-one-codon-out recovery.} In the leave-one-codon-out test for this row, the finite counts are anchor count $0$, boundary count $5$, high closure coverage $8$, and predictable count $8$.  The reported recovery rate is $1.0$, with recovery rate above threshold also $1.0$.  The absence of anchors is important: recovery is not being certified by fixed singleton anchors inside the row.  Instead, the predictable cases are recovered through closure and boundary information in the local tile.  The boundary count $5$ marks the part of the row where the coordinate probe still sees the codon as boundary-supported rather than internally anchored.

\paragraph{Module partition.} The module classification is complete: $R$ has size $13$, the assigned count is $13$, and there are no extra module codons, no missing codons, and no unassigned codons.  The finite modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every listed module has either predictable or boundary support.  This is the chapter-specific content of the leave-one-codon-out row: the thirteen codons form six named local modules, and the module accounting exactly exhausts the punctured-cube row.

\paragraph{Comparison rows.} The code-level leave-one-code-out comparison evaluated $23$ tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary was $0.9841269841269841$.  Family-level readback separated the same comparison into five families: alternative yeast had hit rate above baseline $0.8333333333333334$, while bacterial-plastid, candidate-other, mitochondrial, and nuclear-ciliate families each had hit rate above baseline $1.0$.  These rows are supportive context for the code-read layer only.  They show that the finite boundary-and-closure pattern has strong code-table recovery behavior across the reported family partition, but they do not supply a contact for translation dynamics or function.

\paragraph{Null checks.} The shape-significance null row used $N=2000$ samples with seed $42$.  It recorded $57377$ distinct sample attempts, $55377$ duplicate retries, one exact shape match, and $25$ matches at Jaccard threshold at least $0.9$.  The corresponding values were $p=0.0005$ for exact shape and $p=0.0125$ for high Jaccard.  The amino-acid-class-preserving null row also used $N=2000$ and seed $42$; it had exact match count $0$, lower-tail count $7$, observed $M$ size $20$, exact-shape value $0.0$, and lower-tail value $0.0035$.  These null rows make the local tile nontrivial as a finite code-read object.  They do not erase the dependence on the curated standard-code contact, and they do not convert the pattern into an intrinsic biological necessity.

\paragraph{Reality contact and boundary.} The only required reality contact here is the curated standard-code table, whose observed fact is that codons are assigned to amino-acid or stop labels.  This contact can test code readback and local codon-tile label splits.  It cannot test translation realization, structural order, physical admissibility, function realization, or universal mechanism.  Consequently, the supported statement is narrow: a local codon-tile pattern may be read at the code layer, and the leave-one-codon-out row recovers all reported held-out codons under the stated finite closure and boundary accounting.  Any claim about protein realization, folded structure, biological function, physical admissibility, translation mechanism, or universality is blocked unless a separate higher-layer reality contact is supplied.