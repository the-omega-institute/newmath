\subsection{Leave-one-codon-out recovery inside a local codon tile}
\label{sec:bioreality-h1-leave-one-codon-out}
\origin{ai}

\paragraph{Finite packet.}
This record studies the standard-code codon table at the code-read layer, using a six-bit codon display as the finite coordinate carrier. The observed labels are not inferred from geometry; they are supplied by the curated standard genetic-code table, which assigns each codon to an amino-acid or stop label. The coordinate side is therefore allowed to expose local tile structure, while the biological label side remains an external contact. The leave-one-codon-out row has experiment run id \texttt{e9e1a6e41633}, verified at \texttt{2026-05-30T16:46:16+00:00}. Its finite counts are
$$
\begin{aligned}
\operatorname{anchor\_count} &= 0,\\
\operatorname{boundary\_count} &= 5,\\
\operatorname{closure\_coverage\_high} &= 8,\\
\operatorname{predictable\_count} &= 8,\\
\operatorname{recovery\_rate} &= 1.0,\\
\operatorname{recovery\_rate\_above\_threshold} &= 1.0.
\end{aligned}
$$
Thus every codon tested in the high-coverage leave-one-codon-out portion is recovered by the finite local procedure, but the recovery is entirely code-layer recovery: it recovers label-table placement, not translation chemistry or protein-level realization.

\paragraph{Punctured-cube row.}
The distinguished remainder set $R$ has cardinality $13$. The verified codons are
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The punctured-cube check reports the same set and has face-vector
$$
\begin{aligned}
f(R)=(13,16,4).
\end{aligned}
$$
This is the local tile boundary on which the row is based: thirteen codon vertices, sixteen one-dimensional adjacencies, and four square faces in the finite codon-cube display. The assertion is not that biological evolution must realize this geometry. The assertion is narrower: after the curated code labels are attached, this finite set is the observed local boundary object used by the leave-one-codon-out test.

\paragraph{Module partition.}
The module classification row assigns all thirteen codons in $R$ and reports no missing, extra, or unassigned codons. Its modules are
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The row records $R\_size=13$ and $assigned\_count=13$, with empty lists for extra module codons, missing codons, and unassigned codons. The classification therefore covers exactly the local remainder set. Each module has either predictable or boundary status in the finite test, which is why the recovery result can be stated without adding a hidden residual class.

\paragraph{Spectral and null checks.}
The associated spectral row records
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128,\\
\operatorname{radial\_polynomial\_value} &= -6.224354365258478\times 10^{-12}.
\end{aligned}
$$
The same residual is reported for the cubic-polynomial check, so the numerical discrepancy is at rounding scale for this finite computation. The shape-significance row used $N=2000$ sampled controls, with $57377$ distinct-sample attempts and $55377$ duplicate retries. It found $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9} &= 0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null row also used $N=2000$. It reports observed $M$-size $20$, exact-match count $0$, and $7$ lower-tail samples at or below the observed $M$-size, giving
$$
\begin{aligned}
p_{\mathrm{aa\ class,exact}} &= 0.0,\\
p_{\mathrm{aa\ class,lower\ tail}} &= 0.0035.
\end{aligned}
$$
These controls make the local row harder to dismiss as an arbitrary relabeling artifact, but they still remain finite statistical checks against specified control procedures rather than proofs of mechanism.

\paragraph{Relation to code-level leave-one-out tests.}
The broader leave-one-code-out row evaluated $23$ tables and $63$ total targets. Its baseline hit rate is $0.19821739130434785$, while the reported hit rate above baseline is $0.9841269841269841$. The hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus the boundary is $0.9841269841269841$. By family, the above-baseline hit rates are $1.0$ for bacterial-plastid, candidate-other, mitochondrial, and nuclear-ciliate families, and $0.8333333333333334$ for the alternative-yeast family. These rows support the same reading as the codon-level packet: local finite structure is repeatedly visible at the code-label layer, but the contact remains a label table.

\paragraph{Scope boundary.}
The support condition is deliberately narrow. The curated standard-code table can test codon-label readback and local label-split behavior. It cannot by itself test translation realization, folded structure, physical admissibility, biological function, a universal mechanism, or the claim that DNA must obey this coordinate geometry as a biological necessity. Amino-acid label sequences are not folded proteins. CUN, UUR, stop, tryptophan, arginine, lysine, isoleucine, and methionine labels are code-table labels in this packet, not structural observations. Any promotion beyond the code-read layer requires a separate contact that directly observes the higher layer being claimed.

\paragraph{Conclusion.}
The finite row is ready as a BioReality record. It gives a concrete thirteen-codon punctured-cube boundary, a complete module assignment of those thirteen codons, perfect recovery in the reported leave-one-codon-out high-coverage row, nontrivial spectral scalars, and finite null comparisons with small empirical tail probabilities. The correct scientific interpretation is local and bounded: window-six codon coordinates expose a reproducible boundary-tile pattern after curated labels are attached, while all claims about translation, folding, physical admissibility, function, mechanism, or universality remain outside the evidence supplied here.