\origin{ai}

\subsection{Leave-one-codon-out recovery inside the window-six codon cube}
\label{sec:h1-leave-one-codon-out}

\paragraph{Finite coordinate setting.}
The record studies the standard-code codon table through a six-bit codon display, where each codon is represented as a point of the codon cube $Q_6$ and then read only at the code-label layer.  The relevant finite subset is the thirteen-codon region
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The cardinality check gives $|R|=13$.  The same row identifies this set as a punctured-cube pattern with face data
$$
\begin{aligned}
f(R) = (13,16,4),
\end{aligned}
$$
so the evidence is not merely a label inventory: it also records a finite local tile shape in the codon-coordinate cube.  The accompanying spectral row gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$, with radial cubic residual $-6.224354365258478\cdot 10^{-12}$.  These numbers bind the displayed tile to an internal coordinate-and-spectrum calculation, while the amino-acid and stop labels remain supplied by the curated standard-code table.

\paragraph{Leave-one-codon-out row.}
The primary finite test removes one codon at a time from the local code-layer pattern and asks whether the missing codon remains recoverable from the remaining local closure and boundary information.  In the verified row for $h1.leave.one.codon.out$, the anchor count is $0$, the boundary count is $5$, the number of high-coverage closure cases is $8$, and the predictable count is $8$.  The resulting recovery rate is
$$
\begin{aligned}
\mathrm{recovery\ rate}=1.0,
\qquad
\mathrm{recovery\ rate\ above\ threshold}=1.0.
\end{aligned}
$$
Thus every codon counted as predictable in this row is recovered by the local window-six procedure, and the remaining cases are accounted for by the finite boundary count rather than by a hidden appeal to translation or protein behavior.  The row has no anchor successes; all positive recovery is carried by closure and boundary geometry at the code-read level.

\paragraph{Comparison with code-table perturbations.}
The broader leave-one-code-out row supplies a second finite check across $23$ evaluated tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M-T$ is $0.9365079365079365$, and the rate inside $M-T$ plus the boundary is $0.9841269841269841$.  This comparison is important because it separates a local codon-coordinate phenomenon from a generic ability to guess labels in arbitrary table perturbations.  The same finite packet reports $|M|=20$ in the amino-acid-class preserving null row, with $0$ exact shape matches among $2000$ samples and $7$ lower-tail samples at or below the observed $M$ size.  The corresponding null probabilities are
$$
\begin{aligned}
p_{\mathrm{aa\ class, exact\ shape}} &= 0.0,\\
p_{\mathrm{aa\ class, lower\ tail}} &= 0.0035.
\end{aligned}
$$
A separate shape-significance row with seed $42$ reports $2000$ samples, $57377$ distinct-sample attempts, $55377$ duplicate retries, $1$ exact shape match, and $25$ matches at Jaccard score at least $0.9$.  The resulting values are $p_{\mathrm{exact}}=0.0005$ and $p_{\mathrm{Jaccard}\ge 0.9}=0.0125$.  These finite null rows support the claim that the observed local tile is a nontrivial finite pattern, not a restatement of the curated label table.

\paragraph{Module partition.}
The thirteen codons of $R$ are completely assigned to six named modules.  The module classification is
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
The row reports $R\_size=13$, assigned count $13$, no missing module codons, no extra module codons, and no unassigned codons.  Hence the local recovery statement is not scattered over an unmanaged residue set: every codon in the punctured-cube region is placed in a finite module, and every module has predictable or boundary support.  The family row also records $5$ code families, with hit-rate-above-baseline values $1.0$ for bacterial-plastid, candidate-other, mitochondrial, and nuclear-ciliate families, and $0.8333333333333334$ for alternative-yeast.  This family split gives a finite stress test on the code-read pattern while still staying inside label-table observations.

\paragraph{Reality contact and local interpretation.}
The only biological contact used here is the curated standard genetic-code table, understood as a codon-to-amino-acid-or-stop label assignment.  That contact can test whether the six-bit coordinates and local tile faces line up with standard-code label readback.  It does not observe ribosomal translation, molecular structure, thermodynamic admissibility, biochemical function, or a universal mechanism.  Accordingly, the claim supported by this packet is local and finite: in the window-six codon cube, the thirteen-codon punctured-cube region has a reproducible leave-one-codon-out recovery behavior, a complete module assignment, significant shape statistics under the supplied null rows, and a spectral signature with the listed $\lambda_M$, $\lambda_R$, and $\mu$ values.

\paragraph{Scope boundary.}
No conclusion about translation realization, folded protein structure, physical admissibility, biological function, universal code dynamics, or organism-independent mechanism follows from these rows alone.  Amino-acid label readback is a code-layer observation, not a structural contact.  The local codon tile may guide future experiments, but crossing from code labels to translation, folding, physical feasibility, function, mechanism, or universality requires a separate finite contact designed for that higher layer.