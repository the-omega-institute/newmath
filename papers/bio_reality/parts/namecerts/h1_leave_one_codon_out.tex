\subsection{Leave-One-Codon-Out Boundary Recovery}\label{sec:h1-leave-one-codon-out}
\origin{ai}

\paragraph{Finite object.}
The record studies a window-six coordinate display of the standard-code codon table. The carrier is the six-bit codon cube $Q_6$, read back through a curated codon-to-label table. The BEDC-side distinctions are the six-bit codon display, a local tile face, singleton label corners, and three-point label classes. The biological contact is deliberately narrower: it is only the standard-code label table, hence only a code-read contact. It supplies codon labels; it does not supply translation dynamics, folded structure, physical admissibility, function, mechanism, or universality.

\paragraph{Recovered finite row.}
The row selected by the coordinate closure is the set $R$ of thirteen codons
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The finite audit reports $|R|=13$. The same row is also certified as a punctured-cube object with $f$-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the local object is not merely a list of exceptional labels: it is a finite cubical boundary pattern inside the codon cube. The row is still read through the external label table, so the geometry and the biological labels remain two different sources of information.

\paragraph{Spectral coordinates.}
The spectral summary attached to the finite row gives
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic check evaluates to $-6.224354365258478\cdot 10^{-12}$, with the same residual reported for the cubic polynomial condition. This is treated as numerical closure evidence for the finite coordinate construction, not as a biological necessity claim. The spectral radius in tolerance is $0.6752479308830213$, matching the reported value of $\lambda_M$.

\paragraph{Shape significance.}
Against the coordinate null, the $M$-shape experiment used $N=2000$ samples with seed $42$. It required $57377$ distinct sample attempts and recorded $55377$ duplicate retries. The exact-shape count was $1$, so the exact-shape probability was
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}
$$
The high-overlap count at Jaccard threshold $0.9$ was $25$, giving
$$
\begin{aligned}
p_{J\ge 0.9}=0.0125.
\end{aligned}
$$
A second amino-acid-class-preserving null used $N=2000$ samples with the same seed. It observed $0$ exact matches, an observed $M$-size of $20$, and $7$ samples in the lower tail at or below the observed $M$-size. Its reported probabilities were
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These values support the claim that the local row is a non-generic coordinate-and-label pattern under the tested finite nulls. They do not identify a biochemical mechanism.

\paragraph{Leave-one-codon-out recovery.}
The specific leave-one-codon-out audit for this row reports $8$ predictable codons, $5$ boundary codons, and $0$ anchor codons. The high closure coverage count is $8$. The recovery rate is
$$
\begin{aligned}
\mathrm{recovery\ rate}=1.0,
\end{aligned}
$$
and the reported recovery rate above threshold is also $1.0$. In words, every tested omitted codon in the row is recovered either as predictable from the local closure data or as a boundary case of the same finite tile. This is the central positive finite-row result: the row behaves as a complete leave-one-codon-out local boundary object at the code-read layer.

\paragraph{Module decomposition.}
The thirteen codons are completely assigned to six named modules. The module audit reports $R$-size $13$, assigned count $13$, no extra module codons, no missing codons, and no unassigned codons. The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
Every module has a predictable-or-boundary witness. This matters because the row is not only recovered codon by codon; it is also exhausted by a finite module classification with no leftover codons.

\paragraph{Code-table comparison.}
The broader leave-one-code-out comparison evaluated $23$ tables and $63$ total targets. Its baseline hit rate was $0.19821739130434785$. The observed hit rate above baseline was $0.9841269841269841$, the hit rate in $M-T$ was $0.9365079365079365$, and the hit rate in $M-T$ plus boundary was $0.9841269841269841$. By family, the audit used $5$ families and reported hit-rate-above-baseline values
$$
\begin{aligned}
\mathrm{alternative\ yeast}&=0.8333333333333334,\\
\mathrm{bacterial\ plastid}&=1.0,\\
\mathrm{candidate\ other}&=1.0,\\
\mathrm{mitochondrial}&=1.0,\\
\mathrm{nuclear\ ciliate}&=1.0.
\end{aligned}
$$
These numbers place the leave-one-codon-out row inside a stronger code-read pattern, while still keeping the biological interpretation attached to curated label readback only.

\paragraph{Boundary of the claim.}
The verified claim is local and finite: window-six codon coordinates expose a boundary-tile structure whose thirteen-codon row is recovered under leave-one-codon-out testing and whose module classification is complete. The curated table supports only codon-label readback and local split checks. No translation realization follows. No folded protein structure follows. No physical admissibility, biological function, universal mechanism, or global biological law follows. Any such higher-layer claim would require a separate contact supplying that layer directly.