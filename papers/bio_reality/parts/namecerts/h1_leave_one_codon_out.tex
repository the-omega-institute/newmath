\subsection{Leave-One-Codon Recovery in the Window-Six Codon Tile}
\label{sec:h1-leave-one-codon-out}
\origin{ai}

\paragraph{Finite coordinate object.}
The claim \texttt{h1.leave.one.codon.out} concerns a window-six codon display in which each codon is represented as a six-bit point of the codon cube $Q_6$.  The data record is not a claim that biology is generated by the cube.  It is a finite test of whether the standard-code label table, once read at the codon-label level, exposes a stable local boundary pattern under leave-one-codon deletion.  The distinguished recovered set $R$ has exactly thirteen codons:
$$
\begin{aligned}
R=\{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}
$$
The independent cardinality row reports $|R|=13$, and the punctured-cube row reports the same thirteen-codon list with face-vector data $(13,16,4)$.  Thus the row being tested is not an arbitrary amino-acid narrative; it is a finite subset of the codon cube with a recorded local combinatorial profile.

\paragraph{Leave-one-codon recovery row.}
The leave-one-codon experiment has run id \texttt{e9e1a6e41633}.  It reports $0$ anchor codons, $5$ boundary codons, $8$ predictable codons, and $8$ cases of high closure coverage.  The recovery rate is $1.0$, and the threshold comparison is also $1.0$.  In the local language of the chapter, this means that every tested deletion in the finite packet is recovered either through predictable closure behavior or through an explicitly marked boundary case; no anchor-only explanation is used.  The essential numerical profile is
$$
\begin{aligned}
\mathrm{anchors}&=0, & \mathrm{boundary}&=5, & \mathrm{predictable}&=8,\\
\mathrm{high\ closure\ coverage}&=8, & \mathrm{recovery\ rate}&=1.0, & \mathrm{above\ threshold}&=1.0.
\end{aligned}
$$
This supports the narrow reading that the window-six coordinate system sees a local codon-tile boundary in the standard-code label table.  It does not by itself identify a molecular mechanism.

\paragraph{Module-level accounting.}
The module-classification row closes the accounting over all thirteen codons of $R$.  It reports $R\_size=13$, $assigned\_count=13$, with no extra module codons, no missing codons, and no unassigned codons.  The six modules are recorded as
$$
\begin{aligned}
\mathrm{agr}&=\{\texttt{AGA},\texttt{AGG}\}, &
\mathrm{ar\_lys}&=\{\texttt{AAA}\},\\
\mathrm{cun}&=\{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\}, &
\mathrm{ile\_met}&=\{\texttt{AUA}\},\\
\mathrm{ssn}&=\{\texttt{UCA},\texttt{UUA}\}, &
\mathrm{stop\_trp}&=\{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}.
\end{aligned}
$$
The conclusion from this table is finite and local: every module represented in $R$ contains at least one codon classified as predictable or boundary-covered by the leave-one-codon procedure.  The row does not assert that these modules are evolutionary units, structural modules, or biochemical mechanisms.  It says that, inside this particular codon-coordinate readback, the thirteen codons partition without leftovers into six named finite classes.

\paragraph{Spectral and null-model context.}
The spectral row supplies three scalar checks: $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The recorded cubic residual is $-6.224354365258478\times 10^{-12}$, the same value reported for the radial polynomial residual, so the numerical spectrum is internally consistent at the tolerance scale of the finite run.  The shape-significance row, with $N=2000$ and seed $42$, found $1$ exact shape match among the sampled alternatives, giving $p_{\mathrm{exact}}=0.0005$.  It also found $25$ samples with Jaccard score at least $0.9$, giving $p_{\mathrm{Jaccard}\ge 0.9}=0.0125$.  A stricter amino-acid-class-preserving null model, also with $N=2000$ and seed $42$, reported $0$ exact matches, $7$ lower-tail samples at or below the observed $M$ size, observed $|M|=20$, $p_{\mathrm{class\ exact}}=0.0$, and $p_{\mathrm{class\ lower\ tail}}=0.0035$.  These numbers make the local shape unusual under the recorded finite null models, while still leaving the biological interpretation at the code-label layer.

\paragraph{Code-table comparison rows.}
The broader leave-one-code-out row evaluated $23$ tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate in $M\setminus T$ was $0.9365079365079365$, and the hit rate in $M\setminus T$ plus boundary cases returned to $0.9841269841269841$.  The family summary covers $5$ families.  It reports above-baseline family hit rates of $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial-plastid tables, $1.0$ for candidate-other tables, $1.0$ for mitochondrial tables, and $1.0$ for nuclear-ciliate tables.  These rows are relevant because the codon-level deletion result is not isolated from the code-table comparison layer: the same finite geometry that gives the thirteen-codon local set also aligns with high recovery behavior across recorded table families.  The appropriate interpretation remains comparative readback of curated code labels, not a statement about translation dynamics.

\paragraph{Reality contact and local verdict.}
The only contact used here is the curated standard-code table, which assigns codons to amino-acid or stop labels.  That contact can test code-level label readback and the local split of a codon tile.  It does not observe ribosomal realization, folded structure, thermodynamic admissibility, cellular function, or a universal generative law.  Therefore the ready verdict is deliberately narrow: the finite rows support a window-six local boundary pattern for the standard-code codon-label table, with complete recovery in the leave-one-codon row and complete assignment of the thirteen-codon set into six modules.  No conclusion about translation, protein folding, physical admissibility, biological function, mechanism, or universality follows unless an independent contact measuring that higher layer is added.