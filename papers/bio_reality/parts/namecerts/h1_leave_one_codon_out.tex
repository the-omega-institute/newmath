\origin{ai}
\subsection{Leave-One-Codon-Out Boundary Readback}
\label{sec:h1-leave-one-codon-out}

\paragraph{Finite carrier and contact.}
The record \texttt{h1.leave.one.codon.out} is a code-read claim over the six-bit codon cube $Q_6$.  Its carrier is not a biological mechanism; it is a finite coordinate display whose rows are codons and whose observed labels are supplied by a curated standard-code table.  The permitted reality contact is therefore the codon-to-label table alone: it can test local codon tile label split and code-layer readback, but it cannot test translation realization, folded structure, physical admissibility, biological function, universal mechanism, or any law saying that DNA must obey this geometry.  The local boundary pattern is read as a finite tile phenomenon in $Q_6$, with curated amino-acid or stop labels attached externally.

\paragraph{Distinguished residual set.}
The finite residual set $R$ has cardinality $13$.  The certified row list is
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same rows are certified as a punctured-cube pattern with $f$-vector $(13,16,4)$.  Thus the finite object seen by the probe is not merely a list of exceptional codons: it has a concrete local face count, edge count, and square count inside the window-six display.  This is the level at which the claim is allowed to speak.  It is a statement about a finite row subset and its label boundary, not about ribosomes, folding pathways, energetic admissibility, or phenotype.

\paragraph{Spectral coordinates.}
The associated spectral summary supplies three numerical coordinates:
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial or cubic polynomial residual is $-6.224354365258478\cdot 10^{-12}$, so the finite spectral readback is numerically consistent at the displayed tolerance.  The higher value of $\lambda_M$ relative to $\lambda_R$ records that the larger ambient row set carries stronger spectral weight than the residual tile.  This comparison is descriptive only.  It does not license a claim that spectral radius is a biochemical force, a translation rule, or a sufficient condition for function.

\paragraph{Shape significance.}
The shape probe for $M$ used $N=2000$ samples with seed $42$.  It observed $1$ exact shape match, giving
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{J\geq 0.9}&=0.0125.
\end{aligned}
$$
The sample required $57377$ distinct-sample attempts and had $55377$ duplicate retries; $25$ samples reached Jaccard similarity at least $0.9$.  In the amino-acid-class-preserving null, the observed size was $|M|=20$, with $0$ exact matches among $N=2000$ trials, lower-tail count $7$, exact-shape $p$-value $0.0$, and lower-tail $p$-value $0.0035$.  These numbers support the claim that the displayed finite shape is not a generic artifact of the sampled null models.  They still remain code-layer evidence: the null models compare finite row-label geometry, not protein realization or organismal function.

\paragraph{Leave-one-code-out readback.}
The companion leave-one-code-out run evaluated $23$ code tables and $63$ total targets.  Its ordinary hit rate was $0.19821739130434785$, while the hit rate above that rate was $0.9841269841269841$.  The hit rate inside $M\setminus T$ was $0.9365079365079365$, and the rate in $M\setminus T$ plus the boundary was again $0.9841269841269841$.  By family, the reported rates above the ordinary rate were $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, $1.0$ for nuclear ciliate, and $0.8333333333333334$ for alternative yeast, across $5$ families.  These finite values say that the boundary rule has strong code-table readback performance in the tested rows; they do not say that the rule is a universal generator of codes.

\paragraph{Leave-one-codon-out verdict.}
For the direct leave-one-codon-out run, the anchor count is $0$, the boundary count is $5$, the predictable count is $8$, and high closure coverage is $8$.  The recovery rate is
$$
\begin{aligned}
\mathrm{recovery\_rate}=1.0,
\end{aligned}
$$
with recovery above threshold also equal to $1.0$.  The absence of anchors matters: the recovery is being read from closure and boundary behavior rather than from isolated singleton anchors.  The finite verdict is therefore that every tested omitted codon in the displayed residual geometry is recovered either as predictable by closure or as a boundary case within the local tile.

\paragraph{Module classification.}
The module classification is complete: $R$ has size $13$, the assigned count is $13$, and there are no extra module codons, no rows missing from the modules, and no unassigned codons.  The six modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The supporting fact is stronger than a raw count: every listed module has predictable-or-boundary support.  The modules therefore give a complete partition of the residual codon rows relevant to this claim.  The partition is a code-read partition only; it is not a structural fold class, a physical admissibility class, a functional family, or a mechanism of reassignment.

\paragraph{Cannot-claim boundary.}
The correct local statement is narrow.  Window-six codon coordinates expose a finite local tile with $13$ residual rows, $16$ one-dimensional faces, $4$ two-dimensional faces, complete module assignment, and perfect leave-one-codon-out recovery under the stated probe.  Curated standard-code labels supply the only reality bridge.  From these facts alone, no translation realization, protein folding, structural order, physical admissibility, biological function, universal mechanism, or global biological law follows.  Any such statement would require a separate contact whose measured object is actually translation, structure, admissibility, function, mechanism, or cross-system universality.