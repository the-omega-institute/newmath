\subsection{Leave-one-codon-out recovery on the window-six codon tile}\label{sec:h1-leave-one-codon-out}
\origin{ai}

\paragraph{Finite carrier and readback contact.}
The object in this record is the standard-code codon table read through a finite window-six display.  The carrier is the codon cube $Q_6$, whose points are six-bit codon displays.  The BEDC distinctions used here are only the finite distinctions visible at the code-read layer: a six-bit codon coordinate, a local tile face, a singleton label corner, and a three-point label class.  The readback contact is the curated standard-code table, which assigns each codon to an amino-acid or stop label.  This contact is a label table.  It is not a translation experiment, not a folding experiment, not a physical-admissibility oracle, not a function assay, not a mechanism, and not a universality proof.

\paragraph{The recovered residual set.}
The finite residual set $R$ has cardinality $13$.  The supplied row identifies it as
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same finite row reports that this set is a punctured-cube object with $f$-vector $(13,16,4)$.  Thus the local tile is not just a list of exceptional codons: it carries a finite face pattern with thirteen vertices, sixteen one-dimensional incidences, and four two-dimensional faces in the tested coordinate geometry.  The claim made in this subsection is restricted to that finite code-read pattern.

\paragraph{Spectral coordinates.}
The spectral row gives the numerical invariants
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The recorded cubic, or radial, polynomial residual is $-6.224354365258478\times 10^{-12}$, and the reported spectral-radius value in tolerance is $0.6752479308830213$.  These numbers are used here as finite-row coordinates of the observed tile.  They do not assert that molecular translation is governed by this spectrum.  They say only that, in the supplied finite codon-cube representation, the observed set has the stated spectral readback.

\paragraph{Shape significance under finite null rows.}
The shape-significance row used $N=2000$ null samples with seed $42$.  It reports one exact shape match, giving $p_{\mathrm{exact}}=0.0005$, and twenty-five samples with Jaccard similarity at least $0.9$, giving $p_{J\geq 0.9}=0.0125$.  The same row records $57377$ distinct-sample attempts and $55377$ duplicate retries.  Under the amino-acid-class-preserving null, again with $N=2000$ and seed $42$, the observed $M$-size is $20$, the exact match count is $0$, the lower-tail count at or below the observed $M$-size is $7$, the exact-shape $p$-value is $0.0$, and the lower-tail $p$-value is $0.0035$.  These statistics support the narrower statement that the supplied code-read geometry is not a typical row under the tested finite null procedures.  They do not support any higher-layer biological claim without another contact.

\paragraph{Leave-one-codon-out result.}
For the specific leave-one-codon-out test, the row reports $0$ anchor codons, $5$ boundary codons, $8$ predictable codons, high closure coverage of $8$, and recovery rate $1.0$.  The threshold comparison is also $1.0$.  In the language of this chapter, every tested omitted codon in the finite row is recovered by the local closure-or-boundary criterion, but the recovery is explicitly a code-read recovery.  It recovers the codon label position inside the window-six tile.  It does not recover a translation event, an RNA or ribosomal process, a protein structure, a physical fold, a biological function, or a universal mechanism.

\paragraph{Module classification.}
The module classification is complete on the thirteen-element set $R$: it reports $R$-size $13$, assigned count $13$, no extra module codons, no missing codons, and no unassigned codons.  The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The supplied row states that every module has either a predictable or boundary representative.  This is the finite-row reason that the local leave-one-codon-out claim is accepted as a completed code-read record: the entire residual set is classified, and each classified module is touched by the recovery-or-boundary condition.

\paragraph{Code-table comparison.}
The related leave-one-code-out row evaluated $23$ tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The hit rate in $M\setminus T$ is $0.9365079365079365$, and the hit rate in $M\setminus T$ plus boundary is again $0.9841269841269841$.  By family, the above-baseline hit rates are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate, across $5$ families.  These rows give external finite support that the same local geometry is visible across code-table readbacks, while leaving the biological interpretation at the label-table layer.

\paragraph{Cannot-claim boundary.}
The contact used here can test code-read and local codon-tile label split.  It cannot test translation realization, folded structure, structural order, physical admissibility, biological function, universal mechanism, or biological necessity.  Therefore the accepted statement is only this: in the supplied window-six codon-cube row, the thirteen-codon residual set forms a punctured local tile whose leave-one-codon-out recovery rate is $1.0$, whose modules are completely classified, and whose code-table readback is supported by the curated standard-code label contact.  No stronger biological conclusion follows unless a separate higher-layer contact is supplied.