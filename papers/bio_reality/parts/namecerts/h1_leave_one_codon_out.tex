\origin{ai}

\subsection{Leave-One-Codon Recovery Inside the Window-Six Codon Tile}
\label{subsec:h1-leave-one-codon-out}

\paragraph{Claim record.}
The packet \texttt{h1.leave.one.codon.out} records a code-layer recovery test on the standard-code codon table.  The carrier is the six-coordinate codon cube $Q_6$, read only as a finite codon display with curated amino-acid or stop labels.  The tested question is local: after withholding one codon from the distinguished boundary set, the remaining window-six neighborhood and label information are asked whether the omitted codon is still recovered as predictable or as boundary-adjacent.  This is not a claim about translation in a cell, folding of the encoded amino-acid sequence, physical realizability, function, evolutionary mechanism, or universality.  Those layers would require separate observations with their own measurement routes.

\paragraph{Finite codon rows.}
The distinguished recovered set has cardinality $13$.  The verified row list is
$$
\begin{aligned}
R=\{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}
$$
The same run identifies this set as a punctured-cube pattern with face-vector data
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the record is not merely a label table lookup: it also asserts a finite incidence shape in the six-bit codon display.  The explicit list matters because the recovery statement is row-wise; there are no hidden asymptotic parameters or population-level averages in the claim.

\paragraph{Leave-one-codon result.}
For the leave-one-codon experiment, the verified run \texttt{e9e1a6e41633} reports $13$ codon candidates partitioned by the local recovery test into $8$ predictable rows and $5$ boundary rows, with no anchor rows:
$$
\begin{aligned}
\operatorname{predictable}&=8,\\
\operatorname{boundary}&=5,\\
\operatorname{anchor}&=0,\\
\operatorname{recovery\ rate}&=1.0.
\end{aligned}
$$
The high-coverage closure count is also $8$, matching the predictable-count side of the split.  In this finite table, every withheld member of $R$ is recovered either by direct local predictability or by boundary membership, so the thresholded recovery statistic is $1.0$ as well.  The result is therefore a complete finite-row recovery statement for this packet, not a probabilistic extrapolation over unobserved codon systems.

\paragraph{Module-level classification.}
The $13$ rows are also exhausted by six named modules.  The verified classification is
$$
\begin{aligned}
\text{ar\_lys}&=\{\texttt{AAA}\},\\
\text{agr}&=\{\texttt{AGA},\texttt{AGG}\},\\
\text{ile\_met}&=\{\texttt{AUA}\},\\
\text{cun}&=\{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\},\\
\text{ssn}&=\{\texttt{UCA},\texttt{UUA}\},\\
\text{stop\_trp}&=\{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}.
\end{aligned}
$$
The module audit reports $R$-size $13$, assigned count $13$, no extra module codons, no missing members, and no unassigned codons.  Hence the local recovery claim is aligned with a complete module decomposition of the finite boundary set.  Each module contains at least one row classified as predictable or boundary-supported, so no module is left outside the recovery certificate.

\paragraph{Spectral and shape checks.}
The associated shape record gives a spectral radius $\lambda_M=0.6752479308830213$ for the observed $M$-shape and $\lambda_R=0.4758352843197002$ for the recovered $R$-shape, with $\mu=3.051487585298128$.  The radial polynomial residual is $-6.224354365258478\times 10^{-12}$, the same value reported for the cubic-polynomial check, so the finite numerical witness is within computational tolerance:
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128,\\
P(\lambda_M,\lambda_R,\mu)&=-6.224354365258478\times 10^{-12}.
\end{aligned}
$$
The observed $M$-shape has size $20$ in the amino-acid-class-preserving null experiment.  In the broader randomized shape test with $N=2000$ samples and seed $42$, only $1$ exact shape match was observed, giving $p=0.0005$, while $25$ samples reached Jaccard similarity at least $0.9$, giving $p=0.0125$.  The sampler required $57377$ distinct-sample attempts and $55377$ duplicate retries.  Under the amino-acid-class-preserving null model, $0$ exact shape matches were observed in $2000$ trials, and only $7$ trials lay in the lower tail at or below the observed $M$-size, giving lower-tail probability $0.0035$.

\paragraph{Code-table comparison.}
The related leave-one-code-out table supplies the surrounding code-read context.  Across $23$ evaluated tables and $63$ total targets, the baseline hit rate is $0.19821739130434785$.  The corresponding hit rate above baseline is $0.9841269841269841$, the hit rate in $M\setminus T$ is $0.9365079365079365$, and the hit rate in $M\setminus T$ plus boundary is $0.9841269841269841$.  By family, the above-baseline rates are $1.0$ for bacterial-plastid, mitochondrial, nuclear-ciliate, and candidate-other families, and $0.8333333333333334$ for alternative yeast.  These family rows support the interpretation that the window-six boundary pattern is a stable code-layer signal across the finite comparison set, while still remaining a statement about codon-label readback.

\paragraph{Reality contact and boundary of interpretation.}
The only reality contact used here is the curated standard genetic-code table, which assigns codons to amino-acid or stop labels.  That contact can test code-read assignments and local codon tile label splits.  It does not observe ribosomal realization, kinetic admissibility, folded structure, biochemical function, organism-level mechanism, or a universal biological law.  Therefore the packet is ready as a finite code-layer record: the $13$ listed codons form the verified boundary set, all rows are covered by predictable-or-boundary recovery, the module classification is complete, and the supporting null rows indicate that the observed finite shape is not typical under the stated randomization checks.  No stronger biological layer is asserted by this chapter.