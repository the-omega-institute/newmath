\origin{ai}

\subsection{Leave-One-Codon-Out Recovery for a Window-Six Local Tile}
\label{sec:h1-leave-one-codon-out-local-tile}

\paragraph{Finite object.}
This record treats the standard-code codon table only as a code-layer readback contact.  The carrier is the six-bit codon cube $Q_6$, with each codon displayed as a finite coordinate in a window-six presentation.  The finite row under test is the subset $R$ of codons selected by the local tile boundary calculation.  The observed row has cardinality
$$
\begin{aligned}
|R| &= 13,
\end{aligned}
$$
and the recorded codons are
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same finite row is recorded as a punctured-cube object with $f$-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the row is not being introduced as a biological mechanism.  It is a finite local object: thirteen label-bearing codon corners, sixteen local edges, and four two-faces inside the chosen coordinate display.

\paragraph{Spectral and shape checks.}
The local spectrum attached to the row reports
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The cubic radial relation is numerically satisfied to the recorded residual
$$
\begin{aligned}
-6.224354365258478\times 10^{-12},
\end{aligned}
$$
which is also the stored radial-polynomial value.  The Monte Carlo shape check used $N=2000$ accepted samples after $57377$ distinct-sample attempts and $55377$ duplicate retries.  It found $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9} &= 0.0125.
\end{aligned}
$$
A second null preserving amino-acid class used $N=2000$ trials.  It recorded observed $|M|=20$, exact-match count $0$, and lower-tail count $7$, hence
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
These are finite-row significance facts about the displayed shape and its class-preserving null comparison.  They do not say that codons physically choose the geometry; they say that the curated label table, when read through this coordinate probe, yields a row whose shape is rare under the recorded nulls.

\paragraph{Leave-one-codon-out test.}
The chapter claim is the codon-level leave-one-out recovery statement.  In this test the row is probed by removing one codon at a time and asking whether the held-out label position is recovered by the local closure or by the boundary rule.  The recorded scalars are
$$
\begin{aligned}
\mathrm{anchor\_count} &= 0,\\
\mathrm{boundary\_count} &= 5,\\
\mathrm{closure\_coverage\_high} &= 8,\\
\mathrm{predictable\_count} &= 8,\\
\mathrm{recovery\_rate} &= 1.0,\\
\mathrm{recovery\_rate\_above\_threshold} &= 1.0.
\end{aligned}
$$
Thus every recorded held-out codon is accounted for by either the predictable closure side or the boundary side of the local tile.  The decomposition is not an amino-acid chemistry claim.  It is a finite reconstruction claim at the code-read layer: after a singleton codon label corner is withheld, the window-six tile retains enough local information to place the missing corner in the recorded recovery class.

\paragraph{Module accounting.}
The module classification covers all thirteen codons in $R$ with no extras and no omissions.  The recorded module ledger is
$$
\begin{aligned}
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\}.
\end{aligned}
$$
The stored completeness row is
$$
\begin{aligned}
R\_\mathrm{size} &= 13,\\
\mathrm{assigned\_count} &= 13,\\
\mathrm{missing\_from\_modules} &= 0,\\
\mathrm{unassigned\_codons} &= 0,\\
\mathrm{extra\_module\_codons} &= 0.
\end{aligned}
$$
Every module has a predictable-or-boundary explanation in the finite probe.  This is the strongest local interpretation licensed by the data: the row is internally accounted for as a collection of six modules, and the module ledger exactly exhausts $R$.

\paragraph{Code-family comparison.}
The codon-level result is consistent with the table-level leave-one-code-out row.  Across $23$ evaluated code tables and $63$ total targets, the baseline hit rate is $0.19821739130434785$.  The recorded hit rate above baseline is $0.9841269841269841$, the hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$.  The family-level row reports $5$ families, with hit rates above baseline
$$
\begin{aligned}
\mathrm{alternative\_yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\_plastid} &= 1.0,\\
\mathrm{candidate\_other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\_ciliate} &= 1.0.
\end{aligned}
$$
These values support the same code-read conclusion from a broader table comparison: the local tile and its boundary rule capture a large fraction of the finite label movement seen in the curated code rows.

\paragraph{Reality contact and boundary.}
The sole external contact used here is the curated standard genetic-code table, whose observed fact is that codons are assigned amino-acid or stop labels.  The contact has codon-label resolution only.  It can test code-read behavior and local codon-tile label splits.  It cannot test translation realization, folded structure, structural order, physical admissibility, biological function, universal mechanism, or biological necessity.  Therefore the verdict of this chapter is deliberately local: window-six codon coordinates expose a finite boundary-tile pattern in the standard-code label table, and the leave-one-codon-out row recovers the held-out codon positions at rate $1.0$ inside the recorded predictable-or-boundary classes.  No claim about translation, folding, physical admissibility, function, mechanism, or universality follows unless a separate higher-layer reality contact supplies that information.