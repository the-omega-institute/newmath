\origin{ai}

\subsection{Leave-One-Codon Boundary Tile}
\label{subsec:h1-leave-one-codon-out}

\paragraph{Finite carrier and readback contact.}
The claim \texttt{h1.leave.one.codon.out} is a finite-row code-read record on the six-bit codon cube $Q_6$. Its carrier is not a biological mechanism, but a coordinate display for the standard-code label table. The external contact is the curated standard genetic-code bridge: it supplies codon-to-label readback at codon resolution and does not supply translation dynamics, folding, physical admissibility, biological function, mechanism, or universality. The BEDC-minimal distinctions used here are the six-bit codon display, a local tile face, a singleton label corner, and a three-point label class. The internal structure is coordinate, closure, spectrum, and relation; the readback is the standard-code label table.

\paragraph{Punctured-cube row.}
The distinguished finite relation row $R$ has cardinality $13$. The recorded codons are
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same row is recorded as a punctured-cube object with $f$-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the local object is neither a full biological law nor an unconstrained list: it is a thirteen-vertex finite tile inside the codon cube, with sixteen finite adjacency edges and four finite two-faces in the recorded row. The row is the only object used for the local leave-one-codon analysis; any stronger interpretation would require a separate reality contact.

\paragraph{Spectral row.}
The spectral summary is finite and numeric. The recorded values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The cubic and radial checks have residual
$$
\begin{aligned}
-6.224354365258478\times 10^{-12},
\end{aligned}
$$
which is numerically at tolerance scale for this row. These numbers support a finite spectral description of the local tile. They do not say that DNA is governed by this geometry as biological necessity; they only say that this coordinate display has the recorded finite spectral profile.

\paragraph{Shape null rows.}
Against the shape null with $N=2000$ sampled rows, the finite audit reports $57{,}377$ distinct-sample attempts and $55{,}377$ duplicate retries. There was one exact shape match, so
$$
\begin{aligned}
p_{\mathrm{exact}} = 0.0005.
\end{aligned}
$$
There were $25$ matches with Jaccard score at least $0.9$, so
$$
\begin{aligned}
p_{J\geq 0.9}=0.0125.
\end{aligned}
$$
Under the amino-acid-class-preserving null, again with $N=2000$, the observed $M$ size is $20$, the exact match count is $0$, and the lower-tail count at or below the observed $M$ size is $7$. The recorded finite probabilities are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
The supplied row does not include an explicit list of $M$ codons, so $M$ is used here only through the recorded scalar $|M|=20$ and the associated null statistics.

\paragraph{Leave-one-code and leave-one-codon rows.}
The leave-one-code-out row evaluated $23$ code tables and $63$ total targets. Its baseline hit rate is
$$
\begin{aligned}
0.19821739130434785,
\end{aligned}
$$
while the hit rate above baseline is
$$
\begin{aligned}
0.9841269841269841.
\end{aligned}
$$
The hit rate in $M\setminus T$ is $0.9365079365079365$, and the hit rate in $M\setminus T$ plus boundary is $0.9841269841269841$. The codon-level leave-one row is sharper: it records anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, recovery rate $1.0$, and recovery rate above threshold $1.0$. In this chapter the word ``predictable'' therefore means predictable by this finite local code-read procedure, not predictable as translation, folding, admissibility, function, or mechanism.

\paragraph{Family and module resolution.}
The family-level row has five families. The hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\ plastid} &= 1.0,\\
\mathrm{candidate\ other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\ ciliate} &= 1.0.
\end{aligned}
$$
The module classification is complete: $R$ has size $13$, the assigned count is $13$, and there are no extra module codons, no missing codons, and no unassigned codons. The modules are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
Every listed module has predictable-or-boundary status in the supplied row. This is the finite reason the local-tile claim is ready at the code-read layer.

\paragraph{Boundary verdict.}
The local codon-tile pattern supports a code-layer readback claim: within the supplied finite rows, the punctured-cube relation, spectral scalars, null comparisons, leave-one recovery, family hit rates, and complete module classification agree with a local boundary-tile structure for the standard-code table. The same evidence blocks promotion beyond the code layer. No translation realization, folded structure, structural order, physical admissibility, biological function, universal mechanism, or biological necessity follows from this chapter. Such claims require separate contacts that observe those layers directly.