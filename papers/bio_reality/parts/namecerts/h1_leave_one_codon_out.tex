\subsection{Leave-one-codon-out recovery as a local code-read boundary}
\label{sec:bior-reality-h1-leave-one-codon-out}
\origin{ai}

\paragraph{Finite carrier and contact.}
The experiment is a finite code-read record on the six-bit codon cube $Q_6$.  The carrier is not a biochemical mechanism; it is the coordinate display in which codons are read as cube vertices and then compared with an external standard-code label table.  The reality contact is the curated standard genetic-code bridge: it assigns each codon to an amino-acid or stop label.  This contact tests codon-label readback and local label splits only.  It does not test translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.

\paragraph{Exceptional row set.}
The finite row set $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same computation identifies $R$ as a punctured-cube object with finite face vector
$$
\begin{aligned}
f(R)=(13,16,4).
\end{aligned}
$$
This row set is the local object under discussion.  Its meaning is bounded by the coordinate construction and by the curated code-label readback; it is not promoted to a global law of biology.

\paragraph{Spectral finite-row signature.}
The associated finite spectrum gives the numerical signature
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial cubic check is numerically closed at the recorded tolerance, with polynomial residual $-6.224354365258478\times 10^{-12}$.  Thus the coordinate object has a reproducible finite-row spectral profile.  This is a statement about the cube-coordinate and label-table record, not about molecular dynamics.

\paragraph{Shape significance.}
Against the recorded null sample with $N=2000$ and seed $42$, there was $1$ exact shape match, giving
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005.
\end{aligned}
$$
There were $25$ samples with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{J\geq 0.9} &= 0.0125.
\end{aligned}
$$
The distinct-sampling log records $57377$ distinct-sample attempts and $55377$ duplicate retries.  Under the amino-acid-class-preserving null with $N=2000$ and seed $42$, the exact-shape count was $0$, the lower-tail count at the observed $M$ size was $7$, the observed $M$ size was $20$, and the corresponding lower-tail probability was $0.0035$.  The exact-shape probability in that class-preserving null was recorded as $0.0$ at this sample resolution.  These figures support a finite-row local signal, while leaving all higher biological layers outside the claim.

\paragraph{Leave-one-code-out context.}
The broader leave-one-code-out calculation evaluated $23$ code tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$.  The hit rate above baseline was $0.9841269841269841$, the hit rate inside $M\setminus T$ was $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus boundary was $0.9841269841269841$.  By family, the above-baseline hit rates were $1.0$ for bacterial-plastid, mitochondrial, nuclear-ciliate, and candidate-other families, and $0.8333333333333334$ for alternative-yeast, across $5$ families.  These numbers give the finite comparative background for the codon-level test.

\paragraph{Leave-one-codon-out recovery.}
The codon-level result for claim $\mathrm{h1.leave.one.codon.out}$ records anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate
$$
\begin{aligned}
\rho_{\mathrm{recover}}=1.0.
\end{aligned}
$$
The recorded above-threshold recovery rate is also $1.0$.  Therefore every recovered codon in this finite test is accounted for by the local predictable-or-boundary classification, with no anchor rows needed.  This is the precise code-read content of the claim: leave-one-codon-out recovery is complete inside the finite local boundary system supplied by the codon cube and the curated code-label table.

\paragraph{Module classification.}
The module classification assigns all $13$ codons in $R$ and leaves no extras or unassigned codons.  The six modules are
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The completeness record is $R$ size $13$, assigned count $13$, missing-from-modules list empty, extra-module-codons list empty, and unassigned-codons list empty.  In particular, the local codon-tile boundary is not an informal visual impression: it is a finite partition of the exceptional codon rows, checked against the label-readback contact.

\paragraph{Boundary verdict.}
The ready verdict is local.  The finite data support the claim that window-six codon coordinates expose a leave-one-codon-out local boundary-tile structure at the code-read layer.  The curated standard-code table supplies the external labels needed for that readback.  No translation process, no folded protein structure, no physical admissibility condition, no biological function, no mechanism, and no universal biological law follows from this chapter.  Any such claim would require a separate contact whose observations live at that higher layer.