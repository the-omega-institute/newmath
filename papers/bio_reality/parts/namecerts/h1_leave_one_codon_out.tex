\subsection{Leave-One-Codon-Out Codon Boundary Test}\label{sec:bioreality-h1-leave-one-codon-out}\origin{ai}

\paragraph{Finite carrier and readback contact.} The carrier for this record is the six-bit codon cube $Q_6$, read through a curated standard-code table.  The cube coordinate is internal finite data: a codon is represented by a six-bit display, local tile faces are computed inside that coordinate system, and label corners or three-point label classes are then read back only through the external standard-code contact.  The contact used here says only that a curated standard-code table assigns each codon to an amino-acid or stop label.  It is therefore a code-layer contact, not a translation experiment, not a folded-structure measurement, not a physical-admissibility certificate, not a function assay, not a mechanism, and not a universality theorem.

\paragraph{Residual codon set.} The residual set $R$ has cardinality $13$.  The verified row is
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same finite row verifies that this is a punctured-cube shape with $f$-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the object tested by the leave-one-codon-out row is not an informal pattern in the codon table.  It is a fixed thirteen-codon finite subset of $Q_6$ with a recorded face count, edge count, and vertex count inside the coordinate display.

\paragraph{Spectral finite row.} The associated spectral row records
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial polynomial check gives $-6.224354365258478\cdot 10^{-12}$, and the cubic polynomial check gives the same value.  Within the numerical tolerance of the run, the spectral radius row is therefore the displayed value $0.6752479308830213$.  This spectral statement is a finite-coordinate statement about the row data; it does not by itself assert any biological mechanism.

\paragraph{Shape significance rows.} Against the reported null sample with $N=2000$ and seed $42$, the exact-shape row has one exact match, hence $p_{\mathrm{exact}}=0.0005$.  The high-Jaccard row has $25$ matches at threshold $0.9$, hence $p_{J\geq 0.9}=0.0125$.  The run also records $57377$ distinct sample attempts and $55377$ duplicate retries.  A second amino-acid-class-preserving null row again uses $N=2000$ and seed $42$; it records observed $M$-size $20$, exact match count $0$, lower-tail count $7$, amino-acid-class exact-shape value $0.0$, and lower-tail value $0.0035$.  These numbers support the claim that the displayed local shape is not a frequent accident under the tested finite nulls.  They do not convert the tile into a law of translation or a law of protein structure.

\paragraph{Leave-one-code-out comparison.} The related leave-one-code-out row evaluates $23$ code tables and $63$ targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M\setminus T$ is $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus boundary is $0.9841269841269841$.  By family, five families are recorded: alternative yeast has hit rate above baseline $0.8333333333333334$, while bacterial plastid, candidate other, mitochondrial, and nuclear ciliate each have hit rate above baseline $1.0$.  This comparison supplies finite support for a code-read boundary pattern across curated table rows, but it remains a label-readback comparison.

\paragraph{Leave-one-codon-out verdict.} The claim-specific row records anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, recovery rate $1.0$, and recovery-rate-above-threshold value $1.0$.  In local terms, leaving one codon out produces no anchor recoveries, five boundary cases, and eight predictable or high-coverage cases.  The finite row therefore supports the code-layer statement that the six-bit window exposes local boundary-tile structure in the curated standard-code readback.

\paragraph{Module classification.} The module classifier assigns all $13$ codons of $R$ with no missing codons, no extra module codons, and no unassigned codons.  The complete module partition recorded by the row is
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Each module has a predictable or boundary representative in the recorded classification.  The assignment count is $13$, matching the verified $R$-size $13$, so the module row is closed on the finite carrier actually tested.

\paragraph{Cannot-claim boundary.} The supported statement is narrow: window-six codon coordinates expose a local boundary-tile split whose labels can be read against the curated standard-code table.  Nothing in this row supplies translation realization, folded protein structure, structural order, physical admissibility, biological function, a mechanism of code origin, or universality across biology.  Amino-acid label-sequence readback remains at the code layer.  Any passage from this code-read layer to translation, folding, admissibility, function, mechanism, or universality requires a separate reality contact with its own finite rows.