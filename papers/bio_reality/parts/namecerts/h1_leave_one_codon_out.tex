\subsection{Leave-one-codon-out recovery on the window-six codon tile}\label{sec:bioreality-h1-leave-one-codon-out}
\origin{ai}

\paragraph{Finite carrier and readback contact.}
This record treats the standard-code codon table as a finite code-read contact, not as a mechanism.  The carrier is the six-bit codon cube $Q_6$, read through the usual codon display and then compared with a curated standard-code label table.  The finite exceptional set recovered by the coordinate pipeline has cardinality $13$:
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The recorded size check is $|R|=13$.  Its local punctured-cube summary has $f$-vector $(13,16,4)$, so the row is not merely a list of codons but a finite tile with vertices, one-dimensional faces, and two-dimensional faces in the window-six coordinate presentation.  The curated table supplies only codon-to-amino-acid or codon-to-stop labels.  It is therefore a reality contact for label readback, and no claim about translation realization, folding, physical admissibility, function, mechanism, or universality is licensed by this contact alone.

\paragraph{Spectral and null-model coordinates.}
The same finite row reports a spectral witness with
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\cdot 10^{-12}$, also reported as the cubic-polynomial residual, so the finite numerical spectrum is internally consistent at the displayed tolerance.  The shape significance run used $N=2000$ samples, seed $42$, and $57377$ distinct-sample attempts with $55377$ duplicate retries.  It found $1$ exact shape match and $25$ matches with Jaccard overlap at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{J\geq 0.9}&=0.0125.
\end{aligned}
$$
A class-preserving amino-acid null model also used $N=2000$ and seed $42$.  In that run the observed $M$-size was $20$, the exact-match count was $0$, and the lower-tail count at or below the observed $M$-size was $7$, giving exact-shape probability $0.0$ in the sample and lower-tail probability $0.0035$.  These numbers support a finite code-layer anomaly relative to the sampled nulls; they do not make the anomaly a biological law.

\paragraph{Leave-one-code-out comparison.}
The leave-one-code-out row gives the broader code-family comparison against $23$ evaluated tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$.  The hit rate inside $M\setminus T$ is $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus the boundary is $0.9841269841269841$.  The same value, $0.9841269841269841$, is reported as the hit rate above baseline.  Family-level readback is also finite and explicit across $5$ families:
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
This comparison says that the coordinate split has strong recovery behavior for code-table labels under the tested leave-one-code-out regime.  The row still stays at the code-read layer: a family hit is a label-table hit, not a demonstrated translation process or realized phenotype.

\paragraph{Leave-one-codon-out recovery.}
The chapter-specific leave-one-codon-out experiment has run identifier $\mathrm{e9e1a6e41633}$.  Its finite counts are
$$
\begin{aligned}
\mathrm{anchor\ count}&=0,\\
\mathrm{boundary\ count}&=5,\\
\mathrm{closure\ coverage\ high}&=8,\\
\mathrm{predictable\ count}&=8,\\
\mathrm{recovery\ rate}&=1.0,\\
\mathrm{recovery\ rate\ above\ threshold}&=1.0.
\end{aligned}
$$
Thus, within this finite table calculation, every held-out codon that is counted by the recovery criterion is recovered either through the predictable part of the closure computation or through the boundary behavior of the local tile.  The zero anchor count is important: this is not a row whose success is explained by direct anchoring of held-out codons.  Its support comes from the local closure-and-boundary pattern in the six-bit coordinate cube, checked against the external label table.

\paragraph{Module classification.}
The leave-one-codon-out module classification assigns all $13$ codons of $R$ and leaves no unassigned or extra module codons.  The recorded completeness check is $R\_\mathrm{size}=13$, assigned count $13$, missing-from-modules list empty, extra-module-codons list empty, and unassigned-codons list empty.  The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every listed module has predictable-or-boundary support in the row.  This is the finite content of the local tile claim: the codons selected by the punctured-cube row are not scattered without a recovered module structure, and the leave-one-codon-out probe sees the module structure through predictable or boundary positions.

\paragraph{Local boundary verdict.}
The supported claim is therefore narrow and finite.  In the window-six coordinate presentation of the standard-code table, the set $R$ forms a $13$-codon local tile with punctured-cube statistics $(13,16,4)$, a consistent spectral row, significant sampled shape behavior, complete module assignment, and perfect leave-one-codon-out recovery under the stated criterion.  The reality contact is the curated standard genetic-code table, so the observation is a code-read observation.  It does not assert that DNA obeys this geometry as a biological necessity.  It does not assert translation realization, folded structure, structural order, physical admissibility, biological function, universal mechanism, or protein-level consequence.  Any such higher-layer assertion requires a separate contact that directly observes that layer.