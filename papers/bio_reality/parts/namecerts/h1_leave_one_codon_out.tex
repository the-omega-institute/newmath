\origin{ai}

\subsection{Leave-one-codon-out recovery inside the window-six codon cube}
\label{sec:h1-leave-one-codon-out}

\paragraph{Finite object.}
This record concerns the standard-code codon table viewed through a six-bit codon display.  The working carrier is the codon cube $Q_6$, with the observed labels supplied by the curated standard genetic-code table.  The row is deliberately local: it tests whether a deleted codon label can be recovered from the window-six neighborhood, local tile face, singleton label corner, and three-point label class.  The relevant readout is a code-layer label assignment, not translation chemistry or protein realization.

The distinguished punctured-cube set $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
Its recorded face data are the finite vector
$$
\begin{aligned}
f(R)=(13,16,4),
\end{aligned}
$$
so the local object is not merely a list of codons: it carries a small cubical boundary profile.  The finite row also records an $M$-side observed size of $20$ in the amino-acid-class-preserving null experiment; the membership list of $M$ is not included in this packet, so this chapter uses the supplied cardinal and comparison statistics rather than asserting an unavailable codon list.

\paragraph{Leave-one-codon-out result.}
For the claim identifier $\mathrm{h1.leave.one.codon.out}$, the verified row reports no anchor cases, five boundary cases, and eight predictable cases:
$$
\begin{aligned}
\mathrm{anchor\_count}&=0,\\
\mathrm{boundary\_count}&=5,\\
\mathrm{predictable\_count}&=8,\\
\mathrm{closure\_coverage\_high}&=8.
\end{aligned}
$$
The measured recovery rate is
$$
\begin{aligned}
\mathrm{recovery\_rate}=1.0,
\qquad
\mathrm{recovery\_rate\_above\_threshold}=1.0.
\end{aligned}
$$
Thus, within this finite packet, every evaluated leave-one-codon-out target is accounted for by predictable or boundary behavior.  The interpretation is intentionally narrow: the result says that the six-bit coordinate system and the curated label table expose a stable local recovery pattern for this row.  It does not say that the codon cube generates biological translation, molecular structure, viable chemistry, organismal function, or a universal biological principle.

\paragraph{Module accounting.}
The module classification is complete on $R$: the row reports $R\_\mathrm{size}=13$, $\mathrm{assigned\_count}=13$, with no missing, extra, or unassigned codons.  The six recorded modules are
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The finite statement attached to the row is that every module contains a predictable or boundary case.  This matters because the recovery result is not isolated to a single codon family: it is distributed over arginine-related codons, the lysine singleton, the CUN block, the isoleucine--methionine singleton, the SSN pair recorded here, and the stop--tryptophan block.  The conclusion remains a code-label conclusion.  The appearance of stop, tryptophan, leucine, methionine, arginine, or lysine labels is read from the curated table and does not by itself observe ribosomal implementation or folded protein geometry.

\paragraph{Spectral and null-model context.}
The accompanying spectrum row gives
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The same row records a cubic residual of approximately $-6.224354365258478\times 10^{-12}$, so the numerical check is at roundoff scale for the stored polynomial relation.  This supplies a coordinate-level sanity check for the observed local shape, not a mechanistic explanation.

Two null rows give the empirical contrast.  In the shape-significance row with $N=2000$ samples and seed $42$, there was $1$ exact shape match, $25$ matches with Jaccard score at least $0.9$, $57377$ distinct-sample attempts, and $55377$ duplicate retries.  The recorded probabilities are
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9} &= 0.0125.
\end{aligned}
$$
In the amino-acid-class-preserving null row, again with $N=2000$ and seed $42$, the observed $M$ size is $20$, the exact match count is $0$, the lower-tail count at or below the observed $M$ size is $7$, and the reported probabilities are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact\ shape}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
These finite comparisons support the claim that the observed local codon-tile profile is not common under the supplied randomization rows.  They still do not identify a causal biological mechanism.

\paragraph{Relation to code-table leave-one-out rows.}
The companion leave-one-code-out row evaluated $23$ tables and $63$ targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus the boundary is $0.9841269841269841$.  By family, the finite row reports five families: alternative yeast has hit rate above baseline $0.8333333333333334$, while bacterial/plastid, candidate-other, mitochondrial, and nuclear-ciliate each have rate $1.0$.

These rows make the leave-one-codon-out packet more specific rather than broader.  The standard-code table is the contact that supplies codon-to-label readback.  The geometry supplies a compact local partition and recovery test.  The two together justify a code-layer statement: missing local labels in this finite row fall into predictable or boundary classes with the recorded recovery rate.  They do not justify claims about translation realization, folding, physical admissibility, biological function, molecular mechanism, or universality unless another observation, built for that layer, is added.

\paragraph{Scope.}
The admissible conclusion is therefore bounded as follows.  A six-bit codon coordinate can expose a statistically unusual local boundary pattern in the standard-code label table.  The row $R$ has $13$ codons, a punctured-cube face vector $(13,16,4)$, a complete six-module assignment, and leave-one-codon-out recovery rate $1.0$ with five boundary and eight predictable cases.  The curated table is sufficient for codon-label readback and local split checks.  It is insufficient for asserting translation dynamics, folded structure, physical viability, organismal role, mechanism, or a global law of biology.