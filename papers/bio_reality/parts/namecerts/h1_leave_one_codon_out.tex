\origin{ai}
\subsection{Leave-One-Codon-Out Boundary Recovery in the Window-Six Codon Cube}
\label{sec:h1-leave-one-codon-out}

\paragraph{Finite carrier and readback contact.}
The finite carrier for this record is the six-bit codon display $Q_6$, read only through the standard-code label table. The carrier is not treated as a biochemical mechanism. It is a finite coordinate object whose visible distinctions are coordinate position, closure, spectrum, and relation, with readback supplied by a curated standard genetic-code bridge. The external contact says only that the standard code assigns each codon to an amino-acid or stop label. Thus the observation lives at the code-read layer: it can test a local codon tile and a label split, but it cannot by itself test translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.

\paragraph{Recovered residual set.}
The residual codon set $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The punctured-cube computation reports the same thirteen codons and the face vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
This is the finite tile that the leave-one-codon-out probe is allowed to discuss. The probe is not licensed to infer that DNA, translation, or protein structure obeys this geometry as a biological necessity.

\paragraph{Spectral and shape checks.}
The associated spectral row reports $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$. The radial cubic residual is numerically negligible, with polynomial value $-6.224354365258478\cdot 10^{-12}$, and the spectral-radius entry is $0.6752479308830213$. In the finite random-shape comparison with $N=2000$, the exact shape count is $1$, giving $p=0.0005$, while the high-Jaccard count at threshold $0.9$ is $25$, giving $p=0.0125$. The run required $57377$ distinct-sample attempts with $55377$ duplicate retries under seed $42$. In the amino-acid-class-preserving null row, the observed $M$ size is $20$, exact-match count is $0$, the lower-tail count at or below the observed $M$ size is $7$, the exact-shape value is $0.0$, and the lower-tail value is $0.0035$ over $N=2000$ draws. These numbers support a finite-row code-layer signal; they do not supply a physical or functional model.

\paragraph{Leave-one-codon-out result.}
For the claim $h1.leave.one.codon.out$, the leave-one-codon-out row reports anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, recovery rate $1.0$, and recovery-rate-above-threshold value $1.0$. The meaning is local: when one codon is omitted from the finite readback setting, the window-six coordinate and closure data recover the row as predictable or boundary-supported in the observed code-layer tile. Since the anchor count is zero, the result is not an anchor theorem about a globally forced biological code. Its support comes from closure and boundary behavior inside the finite carrier together with the curated label contact.

\paragraph{Code-table comparison.}
The related leave-one-code-out table comparison evaluated $23$ tables and $63$ total targets. The baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$. The hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$. By family, the five reported groups have above-baseline hit rates $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate. These rows are consistent with the local boundary interpretation: the signal is strong as a finite code-read recovery statistic, but the statistic remains tied to label readback.

\paragraph{Module classification.}
The module classification is complete on $R$: the residual size is $13$, the assigned count is $13$, and there are no extra module codons, no missing module codons, and no unassigned codons. The six displayed modules are
$$
\begin{aligned}
agr&=\{AGA,AGG\},\\
ar\_lys&=\{AAA\},\\
cun&=\{CUA,CUC,CUG,CUU\},\\
ile\_met&=\{AUA\},\\
ssn&=\{UCA,UUA\},\\
stop\_trp&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
Every module has predictable or boundary status. This is the chapter-specific finite witness: the thirteen-codon residual tile decomposes into named local modules, and the leave-one-codon-out recovery row covers them without promoting the tile to a law of translation or protein realization.

\paragraph{Boundary verdict.}
The contact and mismatch rows are aligned on scope. The curated standard-code table supports codon-to-label readback and local split checks. It blocks promotion to protein realization, folded structure, structural order, physical admissibility, biological function, universal mechanism, or any claim that window-six geometry alone is translation. Amino-acid label-sequence readback is not folded protein structure. Therefore the chapter records a ready finite-row result at the code-read layer: the local window-six tile has strong recovery and module coverage against the supplied finite rows, while every higher biological layer remains unavailable unless a separate reality contact supplies it.