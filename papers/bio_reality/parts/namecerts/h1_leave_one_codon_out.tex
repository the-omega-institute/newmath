\origin{ai}
\subsection{Leave-one-codon-out boundary record}
\label{sec:h1-leave-one-codon-out}

\paragraph{Finite carrier and readback contact.}
This record concerns the finite six-bit codon cube $Q_6$ equipped with the standard-code label table as an external readback contact. The carrier is not treated as a biological mechanism. It is a finite coordinate display on which local tile faces, singleton label corners, and three-point label classes can be inspected against curated codon labels. The observed contact is only that the curated standard genetic-code bridge assigns each codon to an amino-acid or stop label. Consequently the layer under test is code readback: the row may test a codon-label split, but it cannot by itself test translation realization, folded structure, physical admissibility, biological function, a universal mechanism, or any claim that DNA obeys this geometry as a biological necessity.

\paragraph{Distinguished row.}
The distinguished finite row $R$ has cardinality $13$. Its observed codons are
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The punctured-cube check returns the same $13$ codons, with face-vector data $(13,16,4)$. This is the local finite object behind the leave-one-codon-out test. The row is therefore not a vague pattern in the full code table: it is a concrete finite subset of the codon cube whose boundary and module behavior can be counted directly.

\paragraph{Spectral and shape evidence.}
The associated spectral record reports $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$. The radial cubic residual is $-6.224354365258478\times 10^{-12}$, matching the same tolerance-level value reported for the cubic polynomial check. The finite-shape significance run used $N=2000$ samples with seed $42$. It found $1$ exact shape match, giving $p_{\mathrm{exact}}=0.0005$, and $25$ matches with Jaccard score at least $0.9$, giving $p_{J\geq 0.9}=0.0125$. The run required $57377$ distinct-sample attempts and recorded $55377$ duplicate retries, so the null sampling pressure is itself visible in the finite-row audit. Under the amino-acid-class-preserving null, the observed $M$ size is $20$, the exact shape count is $0$ out of $2000$, and the lower-tail count at or below the observed $M$ size is $7$, giving lower-tail probability $0.0035$.

\paragraph{Leave-one-code-out comparison.}
The related leave-one-code-out run evaluated $23$ code tables and $63$ total targets. Its baseline hit rate is $0.19821739130434785$. Against that baseline, the reported hit rate above baseline is $0.9841269841269841$, the hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$. By code-family aggregation, $5$ families are represented: alternative yeast has hit rate $0.8333333333333334$, while bacterial plastid, candidate other, mitochondrial, and nuclear ciliate each have hit rate $1.0$ above baseline. These numbers support a robust code-read layer effect, but they do not promote the local tile to a translation, structure, function, mechanism, or universality claim.

\paragraph{Leave-one-codon-out result.}
The claim-specific run has experiment id $e9e1a6e41633$ and was verified at $2026$-$05$-$27T22:09:13+00:00$. It reports anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, recovery rate $1.0$, and recovery-rate-above-threshold $1.0$. Thus every recovered event in this finite row is accounted for by predictable or boundary behavior, with no anchor contribution. In local finite-row language, the leave-one-codon-out result says that the window-six coordinate can expose a boundary-tile split inside $Q_6$ when the external label table is used as the readback contact.

\paragraph{Module classification.}
The module classification is complete: $R$ has size $13$, the assigned count is $13$, and there are no extra module codons, no missing codons from modules, and no unassigned codons. The six reported modules are
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\}, & \mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\}, & \mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\}, & \mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
Each module has predictable-or-boundary support. This is the strongest local reading of the row: the entire $13$-codon distinguished set is partitioned into named finite modules, and every module is covered by the same predictable-or-boundary criterion.

\paragraph{Boundary verdict.}
The finite evidence supports the code-read statement that window-six codon coordinates expose a local boundary-tile structure whose labels are supplied by the curated standard-code table. The contact does not observe ribosomal translation, protein folding, structural order, physical admissibility, biological function, or a universal generative mechanism. Amino-acid label-sequence readback remains a code-layer readback. Any crossing into translation, structure, physical admissibility, function, mechanism, or universality would require a separate higher-layer contact, not merely the local codon-tile split recorded here.