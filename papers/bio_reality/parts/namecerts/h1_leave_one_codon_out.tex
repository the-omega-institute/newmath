\origin{ai}

\subsection{Leave-One-Codon-Out Codon Boundary Tile}
\label{sec:bior-leave-one-codon-out-boundary}

\paragraph{Claim.}
The finite claim recorded here is the code-layer statement named \texttt{h1.leave.one.codon.out}.  Its carrier is the six-bit codon cube $Q_6$, read through the curated standard-code label table.  The claim is not that the cube geometry is a biological law.  It is the narrower finite-row assertion that the window-six coordinates expose a local boundary-tile pattern for the codons in the distinguished residual set $R$, while the amino-acid and stop labels remain external reality contacts supplied by the curated code table.

\paragraph{Finite carrier and residual row.}
The residual set has cardinality thirteen.  In the observed row it is exactly
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row is reported as a punctured-cube pattern with face-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the finite object under discussion is not a full codon table and not a translation mechanism; it is a thirteen-corner local residue inside $Q_6$ with sixteen one-dimensional incidences and four two-dimensional tile faces in the recorded combinatorial readback.

\paragraph{Leave-one-codon-out row.}
For the leave-one-codon-out probe, the verified scalar row gives
$$
\begin{aligned}
\mathrm{anchor\_count} &= 0,\\
\mathrm{boundary\_count} &= 5,\\
\mathrm{closure\_coverage\_high} &= 8,\\
\mathrm{predictable\_count} &= 8,\\
\mathrm{recovery\_rate} &= 1.0,\\
\mathrm{recovery\_rate\_above\_threshold} &= 1.0.
\end{aligned}
$$
The row therefore says that none of the thirteen residual codons is recovered by an anchor count in this test, five sit on the boundary side of the local tile, and eight are covered by the high-closure predictable side.  The recovery rate is complete for this finite probe: every held-out codon in the tested row lands in the predictable-or-boundary account.  This is a local code-read fact about the standard-code labels, not an independent biological realization claim.

\paragraph{Module accounting.}
The thirteen codons are fully assigned by the module classification.  The six modules are
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The classification row records $R\_\mathrm{size}=13$, $\mathrm{assigned\_count}=13$, no extra module codons, no codons missing from the modules, and no unassigned codons.  This complete module cover is the row-level reason the leave-one-codon-out result can be read as a finite boundary-tile statement rather than as a loose collection of unrelated exceptions.

\paragraph{Spectral and null-model context.}
The associated spectrum row reports
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The same computation gives a radial polynomial value $-6.224354365258478\cdot 10^{-12}$ and the same value for the cubic-polynomial residual, so the displayed spectral numbers are within the recorded numerical tolerance.  Against the shape null model with $N=2000$ samples and seed $42$, the exact shape match count is $1$, giving $p_{\mathrm{exact}}=0.0005$, while the high-Jaccard count is $25$, giving $p_{J\geq 0.9}=0.0125$.  The sampler required $57377$ distinct-sample attempts and encountered $55377$ duplicate retries.  Under the amino-acid-class-preserving null model, again with $N=2000$ and seed $42$, the observed $M$ size is $20$, the exact match count is $0$, and the lower-tail count at or below the observed size is $7$, giving $p_{\mathrm{aa\ class,exact}}=0.0$ and $p_{\mathrm{aa\ class,lower\ tail}}=0.0035$.  The input row does not list the members of $M$, so this chapter uses only the provided scalar $|M|=20$ and the recorded comparisons involving $M$.

\paragraph{Code-table readback.}
The wider leave-one-code-out row evaluates $23$ tables and $63$ total targets.  It reports a baseline hit rate of $0.19821739130434785$, a hit rate above baseline of $0.9841269841269841$, a hit rate in $M-T$ of $0.9365079365079365$, and a hit rate in $M-T$ plus boundary of $0.9841269841269841$.  By family, the recorded hit rates above baseline are $0.8333333333333334$ for alternative yeast and $1.0$ for bacterial-plastid, candidate-other, mitochondrial, and nuclear-ciliate families.  These rows support the same reading as the codon-level probe: the finite geometry is informative at the label-readback layer, especially when the boundary component is retained.

\paragraph{Reality contact.}
The only reality contact used here is the curated standard-code table, which assigns each codon to an amino-acid or stop label.  That contact can test code-read and local codon-tile label split claims.  It cannot test translation realization, structural order, physical admissibility, or functional realization.  Consequently, the observed agreement between the $Q_6$ boundary tile and the curated labels is a contact with code-layer readback only.

\paragraph{Cannot-claim boundary.}
No translation, folding, physical admissibility, function, mechanism, or universality follows from this chapter.  A local codon tile is not a global biological law.  Window-six geometry alone is not translation, not protein structure, not a physical admissibility condition, and not biological function.  Amino-acid label-sequence readback is not folded protein structure.  Any claim crossing from code labels to translation, fold, physical admissibility, function, mechanism, or universality requires a separate reality contact that supplies that higher-layer observation.