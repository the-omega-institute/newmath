\subsection{Leave-One-Codon-Out Local Tile Boundary}\label{sec:h1-leave-one-codon-out}\origin{ai}

\paragraph{Finite carrier and contact.}
The record concerns the standard-code codon table as a finite code-read contact on the six-bit codon cube $Q_6$.  The BEDC carrier is not a biological mechanism: it is a coordinate display in which codons are read as finite vertices, local tile faces are inspected, singleton label corners are separated from three-point label classes, and the readback is restricted to the curated standard-code label table.  The external contact says only that a curated standard genetic-code bridge assigns each codon to an amino-acid or stop label.  It does not observe translation realization, folded structure, physical admissibility, biological function, a universal mechanism, or any necessity claim about DNA.

\paragraph{Recovered row.}
The distinguished finite row $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row is certified as a punctured-cube shape with face-vector $(13,16,4)$.  Thus the row is not merely a list of recoded labels; it has a concrete local cubical footprint inside $Q_6$.  The leave-one-codon-out probe then evaluates whether removing a single codon from this row leaves enough finite local information to recover the omitted codon as predictable or boundary-supported.  In the observed run, the anchor count is $0$, the boundary count is $5$, the high-coverage closure count is $8$, and the predictable count is $8$.  The recovery rate is therefore
$$
\begin{aligned}
\mathrm{recovery\_rate}=1.0,
\end{aligned}
$$
with recovery above threshold also equal to $1.0$.

\paragraph{Module split.}
The module classification exhausts the same $13$ codons without extras or omissions.  The assigned count is $13$, the row size is $13$, the missing-from-modules list is empty, the extra-module-codons list is empty, and the unassigned-codons list is empty.  The six finite modules are
$$
\begin{aligned}
\mathrm{AGR}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{AR\_Lys}&=\{\mathrm{AAA}\},\\
\mathrm{CUN}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{Ile\_Met}&=\{\mathrm{AUA}\},\\
\mathrm{SSN}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{Stop\_Trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every module has a predictable-or-boundary witness, so the local tile statement is module-complete at the code-read layer.  The singleton modules $\mathrm{AR\_Lys}$ and $\mathrm{Ile\_Met}$ are not interpreted as mechanistic anchors here; the run reports zero anchors.  Their role is finite classification inside the leave-one-codon-out row.

\paragraph{Spectral and null checks.}
The same finite geometry is accompanied by a spectral check with
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\times 10^{-12}$, matching the reported tolerance scale for the finite computation.  Against the shape null with $N=2000$ samples and seed $42$, there was $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$.  The corresponding values are
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9}&=0.0125.
\end{aligned}
$$
The sampling log records $57377$ distinct-sample attempts and $55377$ duplicate retries.  Against the amino-acid-class-preserving null with $N=2000$ and seed $42$, the observed $M$ size is $20$, the exact-match count is $0$, the lower-tail count at or below the observed $M$ size is $7$, and
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These null checks support the claim that the row is a non-generic finite code-read pattern under the tested randomizations.  They do not promote the pattern beyond the code layer.

\paragraph{Relation to code-table leave-one-out.}
The broader leave-one-code-out run evaluated $23$ code tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$.  By family, the five reported families have hit rates above baseline
$$
\begin{aligned}
\mathrm{alternative\ yeast}&=0.8333333333333334,\\
\mathrm{bacterial\ plastid}&=1.0,\\
\mathrm{candidate\ other}&=1.0,\\
\mathrm{mitochondrial}&=1.0,\\
\mathrm{nuclear\ ciliate}&=1.0.
\end{aligned}
$$
The codon-level row should be read as the local finite-row version of this code-read phenomenon: it explains a boundary-sensitive recovery task on the curated label table, not a physical implementation of translation.

\paragraph{Boundary of the claim.}
The supported statement is deliberately narrow.  Window-six codon coordinates expose a local boundary-tile structure in which the $13$-codon row is recovered under leave-one-codon-out probing with recovery rate $1.0$, module coverage is complete, and the tested null models give small tail values.  The unsupported statements are equally explicit: no translation event is observed, no protein fold is inferred, no structural order is measured, no physical admissibility is certified, no biological function is established, no mechanism is derived, and no universality law follows.  Amino-acid label-sequence readback remains label readback.  Any claim crossing from this local code-read tile to translation, folded structure, physical admissibility, function, mechanism, or universality requires a separate reality contact supplying that higher-layer observation.