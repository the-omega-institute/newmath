\subsection{Leave-one-codon local tile boundary}\label{sec:codon-window-local-tile-boundary}
\origin{ai}

\paragraph{Finite codon display.}
This record concerns the standard-code codon table only at the code-read layer.  The carrier is the six-coordinate codon cube $Q_6$, read through a fixed codon-label table whose entries assign each codon an amino-acid or stop label.  The construction uses four finite distinctions: the six-bit display of a codon, a local tile face, a singleton label corner, and a three-point label class.  These distinctions are internal coordinates for a local boundary test; the amino-acid and stop labels themselves enter through the curated code table.  The observed exceptional codon set has size $13$:
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same finite row verifies that $R$ is a punctured-cube pattern with face vector $(13,16,4)$.  Thus the object being tested is not an unrestricted biological system.  It is a finite code-table configuration with a local cubical shape and a fixed label readout.

\paragraph{Spectral and shape evidence.}
The spectral row records two local radii and one radial parameter:
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The associated cubic relation is satisfied up to numerical residue $-6.224354365258478\cdot 10^{-12}$, which is within the reported tolerance for the radial computation.  In the independent shape comparison against $2000$ sampled alternatives, the exact-shape count is $1$, giving $p=0.0005$, while the high-Jaccard count at threshold $0.9$ is $25$, giving $p=0.0125$.  The sampling process required $57377$ distinct-sample attempts and recorded $55377$ duplicate retries.  These numbers support a narrow claim: the standard-code pattern has a measurable finite shape that is uncommon under the sampled nulls.  They do not identify a biochemical mechanism.

\paragraph{Leave-one-codon result.}
For the leave-one-codon test attached to this row, the verified values are:
$$
\begin{aligned}
\mathrm{anchor\ count} &= 0,\\
\mathrm{boundary\ count} &= 5,\\
\mathrm{high\ closure\ coverage} &= 8,\\
\mathrm{predictable\ count} &= 8,\\
\mathrm{recovery\ rate} &= 1.0.
\end{aligned}
$$
The recovery rate above threshold is also $1.0$.  Since the anchor count is zero, the result should be read as a boundary-and-closure recovery statement rather than as a direct singleton-anchor explanation.  The code-table row says that the local window can recover the withheld codon cases through the finite boundary structure in the tested set; it does not say that the cell executes this geometry, nor that translation is explained by it.

\paragraph{Code-table comparison.}
A related leave-one-code-out row evaluates $23$ tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$.  The hit rate above that baseline is $0.9841269841269841$, the hit rate inside $M\setminus T$ is $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus boundary is $0.9841269841269841$.  The amino-acid-class-preserving null uses $2000$ samples: exact-match count $0$, lower-tail count $7$, observed $M$ size $20$, exact-shape probability $0.0$, and lower-tail probability $0.0035$.  These rows strengthen the finite code-layer interpretation by showing that the boundary behavior is not merely a high-frequency accident in the reported null ensembles.

\paragraph{Module accounting.}
The thirteen codons in $R$ are fully assigned to six modules, with no missing codons, no unassigned codons, and no extra module codons.  The module partition is:
$$
\begin{aligned}
\mathrm{agr} &= \{AGA, AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA, CUC, CUG, CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA, UUA\},\\
\mathrm{stop\_trp} &= \{UAA, UAG, UGA\}.
\end{aligned}
$$
The completeness row gives $R$ size $13$ and assigned count $13$.  The predicate recorded for every module is that it has a predictable member or a boundary member.  This is the strongest local reading supported by the finite data: the leave-one-codon behavior is organized across the whole exceptional set, not only across a hand-picked codon pair.

\paragraph{Family-level stability.}
The by-family code-out row reports five families.  The hit rate above baseline is $1.0$ for bacterial-plastid, candidate-other, mitochondrial, and nuclear-ciliate families, and $0.8333333333333334$ for alternative-yeast.  This supports stability of the code-read boundary test across the recorded family grouping, while still leaving the standard-code table as the contact that supplies the labels.  The family row is therefore comparative evidence about code-table behavior, not a statement about organism-level realization.

\paragraph{Local boundary.}
The contact used here is a curated standard genetic-code table.  It observes codon-to-label assignment at codon-label resolution.  It can test code-read claims and local codon-tile label splits.  It cannot test translation realization, structural order, physical admissibility, function realization, or a universal mechanism.  Consequently, this chapter records a finite local code-table result: window-six codon coordinates expose a recoverable boundary pattern for the leave-one-codon task, with $R$ a thirteen-codon punctured-cube set, spectral values $\lambda_M=0.6752479308830213$ and $\lambda_R=0.4758352843197002$, and recovery rate $1.0$.  No claim about protein folding, folded structure, biochemical admissibility, biological function, translation machinery, or universal law follows from this contact.  Any such stronger claim would require a separate reality contact designed for that layer.