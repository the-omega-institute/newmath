\subsection{Leave-One-Codon-Out Boundary Recovery in the Window-Six Codon Cube}
\label{sec:h1-leave-one-codon-out}
\origin{ai}

\paragraph{Finite carrier and contact layer.}
The finite carrier is the six-bit codon cube $Q_6$, read as the $64$ codons obtained from three two-bit nucleotide coordinates.  The reality contact used here is only the curated standard-code label table: each codon is assigned an amino-acid label or a stop label.  This contact supports code-layer readback and local split tests.  It does not by itself supply translation realization, folded structure, physical admissibility, biological function, universal mechanism, or any claim that the organism must obey this geometry as a biological necessity.

\paragraph{The recovered exceptional row.}
The row singled out by the finite coordinate-and-label construction has cardinality $13$.  The verified row is
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The independent punctured-cube check gives the same list and the finite face data
$$
\begin{aligned}
|R|&=13, & f(R)&=(13,16,4).
\end{aligned}
$$
Thus the row is not merely a label collection; it also has a verified cubical incidence profile inside the six-coordinate display.  The corresponding $M$-shape scalar supplied by the finite-row audit is $|M|=20$.  The explicit $M$ codon list is not part of this row packet, so the present section uses the verified size and statistical checks rather than inventing a missing list.

\paragraph{Spectral finite-row check.}
The same finite object carries a numerical spectral certificate.  The reported values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213, & \lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128, & p_{\mathrm{rad}}(\mu)&=-6.224354365258478\cdot 10^{-12}.
\end{aligned}
$$
The residual is at numerical tolerance for the stated cubic radial polynomial, so the spectral row is consistent with the recovered local cubical shape.  This is a coordinate-and-spectrum fact about the finite readback object, not a physical law of molecular biology.

\paragraph{Null-model pressure.}
Against $2000$ sampled alternatives with seed $42$, the exact $M$-shape match count is $1$, giving $p_{\mathrm{exact}}=0.0005$.  The high-overlap count at Jaccard threshold $0.9$ is $25$, giving $p_{J\geq 0.9}=0.0125$.  The sampling process required $57377$ distinct-sample attempts and encountered $55377$ duplicate retries, so the empirical row is being compared against a constrained finite space rather than an unconstrained continuum.  Under the amino-acid-class-preserving null model, again with $N=2000$ and seed $42$, the exact shape count is $0$, the lower-tail count at the observed $M$ size is $7$, and the two reported tail values are
$$
\begin{aligned}
p_{\mathrm{aa\ class,exact}}&=0.0, & p_{\mathrm{aa\ class,lower\ tail}}&=0.0035.
\end{aligned}
$$
These numbers support the claim that the observed code-layer row is finite-row atypical under the stated nulls.  They do not promote the row to translation, folding, physical admissibility, function, mechanism, or universality.

\paragraph{Leave-one-codon-out result.}
The leave-one-codon-out probe removes one codon from the code-layer row and asks whether the local boundary and closure data recover the missing label position.  The verified result has
$$
\begin{aligned}
\mathrm{predictable\ count}&=8, & \mathrm{boundary\ count}&=5,\\
\mathrm{anchor\ count}&=0, & \mathrm{closure\ coverage\ high}&=8,\\
\mathrm{recovery\ rate}&=1.0, & \mathrm{recovery\ rate\ above\ threshold}&=1.0.
\end{aligned}
$$
Thus every tested missing-codon case is accounted for by either predictable closure behavior or boundary behavior in the local tile; no case requires an anchor count.  The result is a local code-read recovery statement: it says that the finite six-bit coordinate object has enough boundary-and-closure information to recover the held-out codon label placement in this probe.

\paragraph{Relation to leave-one-code-out evidence.}
The companion leave-one-code-out audit evaluated $23$ tables and $63$ targets.  Its baseline hit rate is $0.19821739130434785$, while the reported hit rates are $0.9365079365079365$ inside $M-T$ and $0.9841269841269841$ inside $M-T$ plus boundary, with the same $0.9841269841269841$ rate above baseline.  By family, the above-baseline rates are
$$
\begin{aligned}
\mathrm{alternative\ yeast}&=0.8333333333333334, & \mathrm{bacterial\ plastid}&=1.0,\\
\mathrm{candidate\ other}&=1.0, & \mathrm{mitochondrial}&=1.0,\\
\mathrm{nuclear\ ciliate}&=1.0. & &
\end{aligned}
$$
These code-table comparisons are consistent with the codon-level recovery result, but they remain label-table tests.  They are not evidence for protein realization or structural order unless an additional contact supplies those layers.

\paragraph{Module decomposition.}
The $13$ codons of $R$ are completely assigned to six finite modules:
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\}, & \mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\}, & \mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\}, & \mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The module audit reports $R$-size $13$, assigned count $13$, no extra module codons, no missing row codons, and no unassigned codons.  Each module has predictable or boundary support.  This makes the leave-one-codon-out statement internally finite: the row, its modules, and its recovery mechanism close on the same $13$ displayed codons.

\paragraph{Local verdict and cannot-claim boundary.}
The chapter-level conclusion is that window-six codon coordinates expose a local boundary-tile structure in the standard-code label table.  The evidence is finite and concrete: a $13$-codon row, punctured-cube face vector $(13,16,4)$, spectral values $\lambda_M=0.6752479308830213$ and $\lambda_R=0.4758352843197002$, statistically atypical $M$-shape behavior under two null models, perfect leave-one-codon-out recovery in the tested row packet, and complete assignment of the row to six modules.  The supported claim is only at the code-read layer.  No translation event, folded protein structure, physical admissibility condition, biological function, universal mechanism, or global biological law follows from this chapter unless a separate higher-layer reality contact is supplied.