\origin{ai}

\subsection{Leave-one-codon-out boundary recovery in the window-six codon cube}
\label{sec:bior-actual-leave-one-codon-out}

\paragraph{Finite carrier and readback contact.}
This BioReality record uses the six-bit codon display as a finite carrier $Q_6$ and reads labels only through the curated standard-code table.  The biological contact is therefore a code-layer contact: each codon is assigned an amino-acid label or a stop label, and the test may compare local codon-tile structure against that finite label table.  The coordinate, closure, spectrum, and relation data do not by themselves constitute translation, folding, physical admissibility, biological function, mechanism, or universality.  Those higher-layer claims remain unavailable unless a separate contact supplies them.

\paragraph{Observed punctured-cube row.}
The distinguished relation row $R$ has cardinality $13$.  The observed list is
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same row is reported as a punctured-cube object with $f$-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the finite object being tested is not an informal visual impression: it is a thirteen-codon row with sixteen one-dimensional incidences and four two-dimensional faces in the reported tile complex.

\paragraph{Spectral row geometry.}
The spectral audit gives the values
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The cubic or radial polynomial residual is $-6.224354365258478\times 10^{-12}$, which is numerically at tolerance scale for the reported computation.  This supports using the local row as a finite coordinate object, while still leaving the code labels as external reality contacts rather than internally generated biological facts.

\paragraph{Shape significance under finite nulls.}
Against the reported shape null with $N=2000$ samples and seed $42$, the exact-shape match count is $1$, giving
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}
$$
The high-overlap count at Jaccard threshold $0.9$ is $25$, giving
$$
\begin{aligned}
p_{J\geq 0.9}=0.0125.
\end{aligned}
$$
The sampling log records $57377$ distinct-sample attempts and $55377$ duplicate retries.  Under the amino-acid-class-preserving null, again with $N=2000$ and seed $42$, the observed $M$ size is $20$, the exact-shape match count is $0$, and the lower-tail count at or below the observed $M$ size is $7$.  The corresponding reported values are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
These numbers support a finite-row anomaly statement at the code-read layer.  They do not make the local tile into a law of biology.

\paragraph{Leave-one-code-out comparison.}
The companion leave-one-code-out row evaluates $23$ code tables and $63$ targets.  Its baseline hit rate is $0.19821739130434785$.  The hit rate above baseline is $0.9841269841269841$, the hit rate inside $M\setminus T$ is $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus boundary is $0.9841269841269841$.  By family, the five reported family hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\_yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\_plastid} &= 1.0,\\
\mathrm{candidate\_other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\_ciliate} &= 1.0.
\end{aligned}
$$
These companion counts locate the leave-one-codon-out test inside a broader finite family of code-read perturbations, without changing the scope of the claim.

\paragraph{Leave-one-codon-out result.}
For the focal claim $h1.leave.one.codon.out$, the finite-row result reports anchor count $0$, boundary count $5$, high-coverage closure count $8$, predictable count $8$, and recovery rate $1.0$.  The thresholded recovery statistic is also $1.0$:
$$
\begin{aligned}
\mathrm{anchor\_count} &= 0,\\
\mathrm{boundary\_count} &= 5,\\
\mathrm{closure\_coverage\_high} &= 8,\\
\mathrm{predictable\_count} &= 8,\\
\mathrm{recovery\_rate} &= 1.0.
\end{aligned}
$$
Thus every reported predictable leave-one-codon-out case is recovered in this finite audit, but the recovery is a code-layer recovery against the curated table, not a direct observation of translation or structure.

\paragraph{Module classification.}
The module classification is complete on the thirteen-codon row: $R$ size is $13$, assigned count is $13$, and the lists of extra module codons, missing row codons, and unassigned codons are all empty.  The six reported modules are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
Each module has a predictable-or-boundary status in the reported classification.  This is the strongest local formulation of the claim: the thirteen-codon punctured-cube row decomposes into named finite modules, and the leave-one-codon-out row recovers the predicted or boundary cases at rate $1.0$.

\paragraph{Local cannot-claim boundary.}
The boundary is part of the result.  The curated standard-code table supports codon-label readback and local split checks only.  It does not observe ribosomal translation, protein folding, structural order, physical admissibility, biological function, a universal mechanism, or a biological necessity that DNA must obey the window-six geometry.  Amino-acid label-sequence readback is therefore not folded protein structure.  A future structural or functional claim would require a separate contact with its own finite rows, noise model, and failure condition.