\subsection{Human prime-editor synonymous-window fitness record}
\label{sec:bioreality-human-prime-editor-synonymous-window-fitness}
\origin{ai}

\paragraph{Finite surface.}
This record concerns a finite perturbation surface whose rows are synonymous prime-editor windows in human coding sequence.  The row object is not a codon-usage table by itself and it is not a protein-realization assay by itself.  Its admissible coordinates are the projected $B^{\ast}_{Q6}$ synonymous residual coordinates evaluated on edited windows, together with finite controls for amino-acid identity, gene grouping, coding-sequence context, GC3, length-scale effects, and the local density baseline induced by the selected residual coordinates.  The verified run used $9000$ synonymous rows in the main analysis surface, a compact synonymous surface of $24000$ rows, and $1822$ genes.  The run also imposed a maximum analysis budget of $3000$ rows per cell line and used $B=200$ resampling or null-evaluation units.  Its recorded runtime was $295.329$ seconds under experiment run id \texttt{b1a1a8c7baae}.

\paragraph{Gene-block design.}
The finite-row claim is specifically a held-out predictive statement over gene blocks, not a within-gene memorization statement.  The recorded design note is that held-out folds are gene-block folds, and direct gene dummy fixed effects are not included because test genes are unseen in the training folds.  Thus the fitted object has the form
$$
\begin{aligned}
  \mathcal{D}_{\mathrm{PE}}=
  \{(q_{r},z_{r},g_{r},y_{r}) : 1\le r\le 9000,\ g_{r}\in G,\ |G|=1822\},
\end{aligned}
$$
where $q_{r}$ is the finite $B^{\ast}_{Q6}$ window residual vector, $z_{r}$ is the local control vector, $g_{r}$ is the gene block, and $y_{r}$ is the measured prime-editor fitness readout.  The compact surface is the larger bookkeeping surface
$$
\begin{aligned}
  |\mathcal{D}_{\mathrm{compact}}|=24000,
  \qquad
  |\mathcal{D}_{\mathrm{analysis}}|=9000,
  \qquad
  B=200.
\end{aligned}
$$
The purpose of the gene-block split is to ask whether the synonymous-window residual geometry has finite predictive contact with a human editing-fitness readout after gene identity has been made unavailable as a training shortcut.

\paragraph{Coordinate discipline.}
The admissible residual coordinates are the nine $B^{\ast}_{Q6}$ axes used by the surrounding finite mediation program: $K\_AAA$, $Arg\_AGR$, $Ile\_AUA$, $Leu\_CUN\_vs\_UUR$, $Leu\_UUA\_vs\_UUG$, $Ser\_UCR\_vs\_AGY$, $Ser\_UCA\_vs\_UCG$, $Thr\_ACR\_vs\_ACY$, and $f3\_stress$.  In a separate modeled translation-readout audit, these same nine axes were computed on three organisms, with $61$ sense codons in the codon-count space, a coupling threshold of $0.01$, and $81$ modeled-tAI entries above that threshold.  That audit recorded organism count $3$, not a powered multi-organism survival panel; it therefore supports only bounded modeled-readout contact, not measured translation realization.  The present human prime-editor chapter inherits the same coordinate names only as finite window coordinates.  It does not turn modeled tAI, codon geometry, or residual notation into a ribosome measurement.

\paragraph{Readout statement.}
The observed layer here is synonymous perturbation fitness under prime editing.  The finite row is allowed to say that, in this dataset, a $B^{\ast}_{Q6}$ window coordinate can be evaluated against a human edited-fitness response under the declared gene-block design.  The row is not allowed to say that the response is mediated by translation, protein abundance, folding, molecular function, physical admissibility, organismal phenotype, mechanism, or a universal biological rule.  A positive predictive increment, if reported by the downstream model ledger, would remain a finite association with the prime-editor fitness surface.  A nonpositive increment would bound the coordinate at perturbation-window status for this contact.

\paragraph{Relation to the mediation matrix.}
The broader cross-layer mediation program uses triples $(q_i,t_j,p_k)$, where $q_i$ is a $B^{\ast}_{Q6}$ residual coordinate, $t_j$ is a translation readout such as modeled tAI, stAI, ribosome occupancy, or translation efficiency, and $p_k$ is matched protein abundance.  In the multi-organism protein-abundance mediation run, the human row contained $19053$ joined proteins, modeled per-protein tAI from $432$ human tRNA entries, and no zero raw-weight fallback.  The human mediation decomposition reported
$$
\begin{aligned}
  R^2_{QP} &= 0.04454565118019448,\\
  R^2_{QP\mid T} &= 0.03242643251185274,\\
  R^2_{QTP,\mathrm{indirect}} &= 0.012119218668341744,\\
  M^{QTP} &= 0.27206289160119157.
\end{aligned}
$$
Those numbers are relevant only as a separate bounded protein-abundance mediation surface.  They do not certify that the human prime-editor fitness rows pass through translation or protein abundance.  They instead clarify what kind of additional joined matrix would be required before this prime-editor fitness record could be promoted to a translation-conditioned protein or mechanism claim.

\paragraph{Finite verdict.}
The chapter is therefore a ready finite-row record for a human synonymous prime-editor fitness surface: $9000$ analyzed synonymous rows, $24000$ compact rows, $1822$ gene blocks, $3000$ maximum rows per cell line, and $B=200$ null or resampling units.  The admissible conclusion is that the $B^{\ast}_{Q6}$ synonymous-window coordinates have been placed against a held-out human prime-editor fitness readout under gene-block splitting.  The forbidden conclusion is stronger: no translation step, folded structure, physical admissibility, biochemical function, causal mechanism, selection law, or universality follows from this record unless a separate layer-matched contact supplies the missing rows and the corresponding controlled statistic.