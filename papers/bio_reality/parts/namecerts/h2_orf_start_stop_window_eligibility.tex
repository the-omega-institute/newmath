\subsection{Start--Stop Window Eligibility as a Boundary Record}\label{sec:h2-orf-start-stop-window-eligibility}
\origin{ai}

\paragraph{Finite row.}
This record concerns the claim identifier \texttt{h2.orf.start\_stop.window\_eligibility} and the biological object described as open-reading-frame eligibility from start/stop codon enumeration.  The verified run attached to the row is \texttt{c26a0d6656f7}, verified at \texttt{2026-06-01T16:19:03+00:00}.  Its finite input is deliberately small: the external nucleotide-window manifest was loaded, the manifest contained exactly $1$ window, and the start/stop enumeration classifier returned exactly $1$ classified window.  Thus the local numerical assertion is only
$$
\begin{aligned}
\text{input windows} &= 1,\\
\text{classified windows} &= 1,\\
\text{loaded external manifest} &= 1.
\end{aligned}
$$
The snapshot date for this row is \texttt{2026-05-25}.  The row records the scope as \texttt{external\_window\_start\_stop\_enumeration\_only}; the finite manifest is an annotation input to the experiment and is not generated by the codon-coordinate construction itself.

\paragraph{Codon-coordinate background held fixed.}
The row is tied to previously verified finite codon sets, but those sets supply only a coordinate vocabulary.  The run \texttt{338799579a87} verified that the motif union $R$ is the thirteen-element set
$$
\begin{aligned}
R = \{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\\
&\texttt{CUU},\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}
$$
No missing and no extra motifs were reported for that equality.  The run \texttt{a53ef0f36703} verified that the ambient set $M$ has cardinality $20$ and is exactly
$$
\begin{aligned}
M = \{&\texttt{AAA},\texttt{AAG},\texttt{ACA},\texttt{ACG},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{AUG},\\
&\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UCG},\\
&\texttt{UGA},\texttt{UGG},\texttt{UUA},\texttt{UUG}\}.
\end{aligned}
$$
The finite saturation check \texttt{f4eee68dd2c3} further reports $\operatorname{Sat}_{f3}(R)$ as the eighteen-element set obtained from $M$ by omitting \texttt{ACA} and \texttt{ACG}, with an empty $f3$ boundary list.  The quotient-geometry run \texttt{fd0a506a0881} gives $|\pi_M|=10$, an $M$ vector $(20,38,26,8,1)$, and a quotient vector $(10,14,6,1)$.  These values show that the enumeration has a finite combinatorial setting, not that any cell of that setting is a translated biological product.

\paragraph{Eligibility statement.}
The row therefore licenses only the following narrow reading: within the single external window supplied to the experiment, the frozen start/stop-window rule could classify the supplied window as belonging to the eligibility layer.  The stored field \texttt{no\_geometry\_to\_translation\_promotion} has value \texttt{orf\_eligibility\_only}.  In ordinary prose, this means that the computation may say that one supplied nucleotide interval passes the start/stop boundary screen, but it may not say that the interval is translated, folded, physically admissible, functional, mechanistic, or universal.  The classification is a finite boundary screen over a supplied record; it is not a discovery of an expressed protein.

\paragraph{Why a separate contact is required.}
The row itself has no attached contact objects and no mismatch entries.  Its two probes state the same scientific guardrail from complementary directions.  First, a start/stop-window seed must remain at boundary status unless an external sequence, transcript, or curated annotation supplies the biological interval being tested.  Second, the packet is not allowed to record an ORF-eligibility claim when there is no sequence database, transcript evidence, or curated annotation able to test the interval.  These probes are not statistical evidence by themselves; they are local admissibility tests that prevent a coordinate screen from being read as biological realization.

\paragraph{Comparison with the stronger contact run.}
A distinct finite contact run in the same ledger, \texttt{7c9b30b02d46}, shows what a stronger external-contact situation looks like.  It loaded \texttt{tools/bio\_reality/data/curated\_orf\_interval\_contact\_dataset.json} with $168$ rows from NCBI RefSeq GenBank XML CDS intervals for \emph{Escherichia coli} K-12 MG1655 accession \texttt{NC\_000913.3}, using snapshot date \texttt{2026-05-30}.  Its positive curated ORF class had $72$ accepted out of $72$, its matched non-ORF decoys had $72$ rejected out of $72$, and its boundary cases had $24$ promoted out of $24$ equal to $0$, so the promoted fraction for those boundary cases was $0.0$.  The positive and decoy exact one-sided binomial upper-tail values were both $2.117582368135751\times 10^{-22}$, and the predeclared pairwise advantage also had $72$ wins out of $72$ pairs with the same upper-tail value.  The three held-out splits each contained $24$ positives and $24$ decoys, with positive accept rate $1.0$ and decoy reject rate $1.0$ in every split.  That stronger run remains an external curated-interval contact; it does not retroactively turn the present one-window boundary row into translation evidence.

\paragraph{Local cannot-claim boundary.}
The self-check run \texttt{0f47d2f0cf41} records a boundary statement: no codon geometry is promoted across biological layers, and $3$ gates were checked.  Accordingly, this chapter claims no translation realization, no folded structure, no physical admissibility, no biological function, no mechanism, and no universal law from start/stop-window enumeration alone.  Any later assertion at one of those layers would need its own contact with the relevant biological reality and its own finite audit trail.  The present row is ready only as a finite, externally supplied window classification at the ORF-eligibility boundary.