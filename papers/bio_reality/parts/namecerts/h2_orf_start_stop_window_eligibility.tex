\subsection{Start--stop window eligibility}\label{sec:bioreality-orf-start-stop-window-eligibility}
\origin{ai}

\paragraph{Finite object being tested.}
This record concerns the narrow layer in which a nucleotide interval is eligible for open-reading-frame status only through start and stop codon enumeration.  The verified object is not a translated product and not a cellular mechanism.  It is a finite classification task over externally supplied nucleotide-window records.  In the finite run with identifier \texttt{c26a0d6656f7}, the external window manifest was loaded with value $1$, the number of input windows was $1$, and the classifier reported that all $1$ of the $1$ input windows were classified by start--stop enumeration.  Thus the local numerical assertion is
$$
\begin{aligned}
  \mathrm{classified} &= 1,\\
  \mathrm{input\_windows} &= 1,\\
  \mathrm{window\_count} &= 1.
\end{aligned}
$$
The run scope was explicitly recorded as \texttt{external\_window\_start\_stop\_enumeration\_only}, with snapshot date \texttt{2026-05-25}.  The source field identifies the input as a curated finite nucleotide-window manifest for BioReality ORF start/stop enumeration, and states that those records are annotation inputs to the experiment rather than facts generated by the formal coordinate system alone.

\paragraph{Boundary status before interval contact.}
The seed packet for this layer has no independent biological carrier before a separate sequence, transcript, or curated interval annotation is attached.  Its minimal carrier and distinction fields were still marked as awaiting contact with observed biological data, and its internal structure was recorded as none.  Consequently, before such contact, the only admissible interpretation is a testable boundary condition: the start--stop rule says what kind of interval would count at this layer, but it does not itself supply an observed interval.  In this state, asserting ORF eligibility without a corresponding curated interval would overstate the data.

\paragraph{Curated interval contact.}
A separate finite contact later supplied a stronger interval-level check.  In run \texttt{7c9b30b02d46}, the curated interval dataset \texttt{tools/bio\_reality/data/curated\_orf\_interval\_contact\_dataset.json} was loaded with $168$ rows.  The snapshot date was \texttt{2026-05-30}, and the source was NCBI RefSeq GenBank XML CDS intervals from \textit{Escherichia coli} K-12 MG1655 complete genome accession \texttt{NC\_000913.3}.  The required input classes were present: boundary cases, matched non-ORF decoys, and positive curated ORFs.  The classifier identifier was \texttt{orf\_start\_stop\_window\_eligibility}; its rules were recorded as unmodified.

\paragraph{Finite acceptance and rejection counts.}
The interval contact separated positive curated intervals from matched decoys in the recorded finite table.  The positive interval statistic had $72$ accepted positives out of $72$, hence accept rate $1.0$, with one-sided binomial upper-tail value $2.117582368135751\times 10^{-22}$.  The matched decoy statistic had $72$ rejected decoys out of $72$, hence reject rate $1.0$, with the same one-sided binomial upper-tail value $2.117582368135751\times 10^{-22}$.  The predeclared pairwise comparison had $72$ wins across $72$ matched pairs, win rate $1.0$, and one-sided binomial upper-tail value $2.117582368135751\times 10^{-22}$.  These finite results may be summarized as
$$
\begin{aligned}
  \mathrm{positive\ accepted} &= 72/72,\\
  \mathrm{decoy\ rejected} &= 72/72,\\
  \mathrm{pairwise\ wins} &= 72/72,\\
  p &= 2.117582368135751\times 10^{-22}.
\end{aligned}
$$
Boundary cases were also kept out of the promoted class: $24$ boundary cases were tested, $0$ were promoted, and the promoted fraction was $0.0$.

\paragraph{Held-out stability.}
The interval contact was not confined to a single aggregate count.  It was recorded across $3$ held-out splits.  In the split \texttt{refseq\_nc\_000913\_left\_locus\_fold}, $24$ positives had positive accept rate $1.0$ and $24$ decoys had decoy reject rate $1.0$.  In \texttt{refseq\_nc\_000913\_middle\_locus\_fold}, the same counts and rates were recorded: $24$ positives at accept rate $1.0$ and $24$ decoys at reject rate $1.0$.  In \texttt{refseq\_nc\_000913\_right\_locus\_fold}, again $24$ positives were accepted at rate $1.0$ and $24$ decoys were rejected at rate $1.0$.  Each of the $3$ split records was marked as passed.  The protocol fields state that classifier rules, held-out splits, input classes, and baselines were fixed before the external evidence was observed: the protocol timestamp was \texttt{2026-05-29T19:11:13+00:00}, while the external evidence timestamp was \texttt{2026-05-30T10:29:31+00:00}.

\paragraph{Codon-coordinate background.}
The ORF-eligibility layer is numerically connected to the finite codon-coordinate computations only as background geometry, not as a source of biological realization.  The motif-compression computation recorded the set
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\},
\end{aligned}
$$
with no missing and no extra entries.  The larger finite set $M$ had cardinality $20$ and was recorded as
$$
\begin{aligned}
M=\{&\mathrm{AAA},\mathrm{AAG},\mathrm{ACA},\mathrm{ACG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\\
&\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\\
&\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}.
\end{aligned}
$$
The $f_3$ saturation run recorded $\mathrm{Sat}_{f_3}(R)=M\setminus\{\mathrm{ACA},\mathrm{ACG}\}$ and an empty $f_3$ boundary.  The quotient-geometry run recorded the $M$ face vector $(20,38,26,8,1)$, quotient size $10$, quotient face vector $(10,14,6,1)$, and product-decomposition flag as empty.  These numbers describe the finite codon-coordinate environment in which the start and stop symbols live; they do not turn a coordinate pattern into a realized transcript or protein.

\paragraph{Local limitation.}
The claim established here is only that the supplied finite windows and curated intervals were handled by the declared start--stop enumeration protocol at the ORF-eligibility layer.  No translation event follows from this record.  No folded molecular structure follows from this record.  No physical admissibility, cellular function, mechanistic explanation, or universal biological law follows from this record.  Those stronger statements would require further contact with the appropriate biological measurements or annotations.  The self-check run reinforces this restriction: it recorded $3$ checked gates and stated that no codon geometry is promoted across biological layers.  Therefore the chapter's conclusion is deliberately narrow: after curated interval contact, this finite table supports ORF start--stop eligibility for the recorded rows; outside that contact, the seed remains only a boundary test for what would need to be observed.