\subsection{Start--Stop Window Eligibility as a Contact-Bounded ORF Record}\label{sec:biorality-h2-orf-start-stop-window-eligibility}
\origin{ai}

\paragraph{Claim scope.}
This record concerns only open-reading-frame eligibility as determined by finite start- and stop-codon window enumeration.  The claim is not that a nucleotide interval is translated, not that a peptide folds, not that a molecule is physically admissible, not that a gene product has function, not that a mechanism has been identified, and not that a universal biological law has been obtained.  The row is a bounded eligibility record: it says that a finite external window manifest can be classified by the start--stop enumeration rule, while all stronger biological readings remain outside the packet unless a further observation supplies them.

\paragraph{Finite codon background.}
The row carries the finite motif set
$R=\{\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}$,
with no missing and no extra elements in the recorded motif union.  The ambient codon collection is
$M=\{\mathrm{AAA},\mathrm{AAG},\mathrm{ACA},\mathrm{ACG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}$,
so $|M|=20$.  The saturation row reports
$\mathrm{Sat}_{f3}(R)=M\setminus\{\mathrm{ACA},\mathrm{ACG}\}$, namely
$\{\mathrm{AAA},\mathrm{AAG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}$,
and the same row records an empty $f3$ boundary.  The associated finite geometry has $f$-vector $(20,38,26,8,1)$, while the quotient geometry has $10$ quotient points and $f$-vector $(10,14,6,1)$.  These numbers describe the finite enumeration substrate only; they do not themselves certify any biological interval.

\paragraph{Window enumeration contact.}
The direct start--stop enumeration row loads an external finite nucleotide-window manifest dated 2026-05-25.  Its source is a curated finite nucleotide-window manifest for BioReality ORF start/stop enumeration, and the row explicitly records that the records are external annotation inputs rather than consequences of the codon geometry.  The manifest contains $1$ input window, the classifier assigns a class to $1$ window, and the classification count is therefore
$$
\begin{aligned}
\mathrm{classified}&=1,\\
\mathrm{input\_windows}&=1,\\
\mathrm{window\_count}&=1.
\end{aligned}
$$
The recorded scope is external window start--stop enumeration only.  Its promotion field is $\mathrm{orf\_eligibility\_only}$, which fixes the scientific interpretation: the finite rule has touched a curated window input, but no translation event, protein structure, physical viability, function, mechanism, or broad biological regularity is inferred from that contact.

\paragraph{Curated interval evidence.}
A separate interval-contact row supplies a larger external check using NCBI RefSeq GenBank XML CDS intervals from \emph{Escherichia coli} K-12 MG1655 complete genome accession NC\_000913.3, with snapshot date 2026-05-30.  The curated interval dataset is loaded from \texttt{tools/bio\_reality/data/curated\_orf\_interval\_contact\_dataset.json} and has $168$ rows.  The required input classes are boundary cases, matched non-ORF decoys, and positive curated ORFs.  The frozen classifier is identified as \texttt{orf\_start\_stop\_window\_eligibility}, with classifier rules unmodified after the protocol was fixed.

\paragraph{Finite statistical controls.}
The positive interval test accepts $72$ of $72$ curated positives, giving accept rate $1.0$ and exact one-sided binomial upper-tail value $2.117582368135751\times 10^{-22}$.  The matched decoy test rejects $72$ of $72$ decoys, giving reject rate $1.0$ with the same exact one-sided binomial value $2.117582368135751\times 10^{-22}$.  The predeclared pairwise comparison records $72$ wins in $72$ matched pairs, win rate $1.0$, again with one-sided binomial upper-tail value $2.117582368135751\times 10^{-22}$.  Boundary controls contain $24$ cases and promote $0$ of them, so the promoted fraction is $0.0$.

\paragraph{Held-out split stability.}
The interval contact records three held-out splits.  In \texttt{refseq\_nc\_000913\_left\_locus\_fold}, $24$ positives are accepted at rate $1.0$ and $24$ decoys are rejected at rate $1.0$.  The middle-locus split has the same counts and rates: $24$ positives, $24$ decoys, positive accept rate $1.0$, and decoy reject rate $1.0$.  The right-locus split also has $24$ positives and $24$ decoys with the same two rates.  Thus the finite-row stability statement is not based on a single aggregate alone: it is repeated across left, middle, and right locus folds with identical pass status.

\paragraph{Protocol boundary.}
The protocol fields report that baseline comparisons, classifier rules, input classes, and held-out splits were fixed before the external observation dated 2026-05-30T10:29:31+00:00; the protocol timestamp is 2026-05-29T19:11:13+00:00.  The analysis field also records that no translation or structure input was used.  This matters because the row is about eligibility under start--stop interval classification, not about downstream cellular realization.  The self-check row records three checked gates and states that no codon geometry is promoted across biological layers.

\paragraph{Local conclusion.}
The claim is ready at the ORF-eligibility layer for the finite contacts stated above.  The single-window manifest establishes that the start--stop enumeration can classify the supplied external window.  The curated interval contact gives a larger finite check with $168$ rows, $72$ accepted positives, $72$ rejected matched decoys, $24$ non-promoted boundary cases, and three stable held-out splits.  These facts support only the bounded conclusion that the specified finite external interval records are eligible under the stated start--stop rule.  They do not establish translation, folding, physical admissibility, biological function, mechanism, or universality; each of those stronger assertions would require an additional contact designed for that layer.