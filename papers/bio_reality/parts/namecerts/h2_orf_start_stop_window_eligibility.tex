\origin{ai}

\subsection{Start--Stop Window Eligibility as a Finite Boundary Test}
\label{subsec:h2-orf-start-stop-window-eligibility}

\paragraph{Object of the record.}
This chapter records a finite BioReality test for open-reading-frame eligibility at the level of start and stop codon enumeration. The object under inspection is not translation, protein production, folding, cellular activity, or organismal function. It is the narrower question whether a nucleotide window supplied by an external finite manifest is classifiable by the elementary start--stop boundary rule used in the experiment. The relevant run reports experiment id \texttt{c26a0d6656f7}, verification time \texttt{2026-06-04T13:10:03+00:00}, snapshot date \texttt{2026-05-25}, and scope \texttt{external\_window\_start\_stop\_enumeration\_only}. The manifest loaded successfully, with $1$ external input window and $1$ classified window:
$$
\begin{aligned}
  \mathrm{input\_windows} &= 1,\\
  \mathrm{classified\_windows} &= 1,\\
  \mathrm{window\_count} &= 1.
\end{aligned}
$$
The resulting finite statement is therefore an eligibility-boundary statement: every row in the loaded manifest was assigned a start--stop enumeration class by the frozen rule, and no claim is made outside that loaded manifest.

\paragraph{Finite codon background used by the boundary rule.}
The enumeration is situated against the finite codon data already checked in the BioReality ledger. The motif-compression run \texttt{338799579a87} verifies that the finite set $R$ is exactly
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\\
&\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\},
\end{aligned}
$$
with no missing and no extra elements. The independent run \texttt{a53ef0f36703} checks that the associated finite set $M$ has cardinality $20$ and is precisely
$$
\begin{aligned}
M=\{&\mathrm{AAA},\mathrm{AAG},\mathrm{ACA},\mathrm{ACG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\\
&\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\\
&\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}.
\end{aligned}
$$
The start/stop-window test uses these as finite enumerated data, not as a biological generator of expression. In particular, the verified status $M=WNR\cup CUN$ and $|M|=20$ is a combinatorial input to the window classification, while the biological interval itself remains supplied by the external manifest.

\paragraph{Saturation and quotient checks.}
The same finite environment carries two additional checks that constrain the enumeration without enlarging its biological meaning. The saturation run \texttt{f4eee68dd2c3} reports
$$
\begin{aligned}
\mathrm{Sat}_{f3}(R) = \{&\mathrm{AAA},\mathrm{AAG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\mathrm{CUA},\mathrm{CUC},\\
&\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\mathrm{UGA},\mathrm{UGG},\\
&\mathrm{UUA},\mathrm{UUG}\},
\end{aligned}
$$
so the saturation omits exactly the two listed $M$ elements $\mathrm{ACA}$ and $\mathrm{ACG}$, while $M=\mathrm{Med}(R)\cup\{\mathrm{ACA}\}$ is checked as the same $20$-element list displayed above. The recorded $f3$ boundary list is empty, written in the finite ledger as $0$ boundary cases for that component. Separately, the quotient-geometry run \texttt{fd0a506a0881} reports the $M$ face vector
$$
\begin{aligned}
f(M)=(20,38,26,8,1),
\end{aligned}
$$
and the quotient face vector
$$
\begin{aligned}
f(\pi_M)=(10,14,6,1),
\end{aligned}
$$
with $|\pi_M|=10$. These quotient numbers support the finite bookkeeping of the codon environment. They do not convert the codon geometry into an observed transcript, translated peptide, folded structure, admissible molecule, function, mechanism, or universal biological law.

\paragraph{External interval contact and stronger controls.}
A linked curated-interval contact, experiment id \texttt{7c9b30b02d46}, supplies a larger finite reality check for the same eligibility layer. It loads \texttt{tools/bio\_reality/data/curated\_orf\_interval\_contact\_dataset.json} with $168$ rows from NCBI RefSeq GenBank XML CDS intervals for \textit{Escherichia coli} K-12 MG1655 accession \texttt{NC\_000913.3}, snapshot date \texttt{2026-05-30}. The protocol was fixed before external observation: classifier rules, input classes, baselines, and held-out splits were fixed at \texttt{2026-05-29T19:11:13+00:00}, and the external evidence was observed at \texttt{2026-05-30T10:29:31+00:00}. The required finite input classes were present: boundary cases, matched non-ORF decoys, and positive curated ORFs.

The curated contact reports $72$ accepted positives out of $72$ and $72$ rejected matched decoys out of $72$. The one-sided exact binomial tail recorded for the positive acceptance test is
$$
\begin{aligned}
  n_{+} &= 72, & \mathrm{accepted}_{+} &= 72, & \widehat p_{+} &= 1.0, & p_{\mathrm{tail}} &= 2.117582368135751\times 10^{-22},
\end{aligned}
$$
and the matched-decoy rejection test has the same count and tail probability:
$$
\begin{aligned}
  n_{-} &= 72, & \mathrm{rejected}_{-} &= 72, & \widehat p_{-} &= 1.0, & p_{\mathrm{tail}} &= 2.117582368135751\times 10^{-22}.
\end{aligned}
$$
The predeclared paired baseline comparison likewise records $72$ wins in $72$ pairs, win rate $1.0$, and one-sided binomial tail $2.117582368135751\times 10^{-22}$. Boundary cases remain unpromoted: among $24$ boundary rows, $0$ were promoted, so the promotion fraction is $0.0$.

\paragraph{Held-out split behavior.}
The curated contact also records three held-out stability checks. In the left-locus fold, $24$ positives were accepted at rate $1.0$ and $24$ decoys were rejected at rate $1.0$. In the middle-locus fold, the same counts and rates hold. In the right-locus fold, the same counts and rates hold again. Thus the finite split summary is
$$
\begin{aligned}
  \mathrm{split\_count} &= 3,\\
  \mathrm{positive\_n\ per\ split} &= 24,\\
  \mathrm{decoy\_n\ per\ split} &= 24,\\
  \mathrm{positive\_accept\_rate\ per\ split} &= 1.0,\\
  \mathrm{decoy\_reject\_rate\ per\ split} &= 1.0.
\end{aligned}
$$
These checks justify saying that the frozen start--stop-window classifier behaved consistently on the finite curated interval table and its matched controls. They do not justify removing the finite-table qualifier.

\paragraph{Local boundary of the claim.}
The classifier id is \texttt{orf\_start\_stop\_window\_eligibility}, and the ledger records that its rules were not modified during the contact. The self-check run \texttt{0f47d2f0cf41} records $3$ checked gates and explicitly confines the result to BioReality self-checking without cross-layer promotion. Therefore the chapter's admissible conclusion is exactly this: within the loaded finite start/stop manifest, all $1$ submitted windows were classified by the frozen enumeration rule; within the linked curated interval table, $72$ positives, $72$ matched decoys, and $24$ boundary cases behaved according to the predeclared eligibility test and controls. Nothing in these rows establishes translation realization, protein folding, physical admissibility, biological function, molecular mechanism, genome-wide necessity, or universal law. Any such stronger statement would require a separate contact carrying the relevant biological measurement.