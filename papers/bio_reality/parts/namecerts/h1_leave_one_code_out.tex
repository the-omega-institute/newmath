\subsection{Leave-One-Code-Out Boundary Test for the Standard Code}\label{sec:h1-leave-one-code-out}
\origin{ai}

\paragraph{Finite carrier and reality contact.}
This record treats the standard-code codon table as a finite code-layer contact on the six-bit codon cube $Q_6$.  The carrier is not a biochemical mechanism.  It is a finite display of $64$ codon vertices, each read through the curated standard-code label table as an amino-acid or stop label.  The contact therefore tests code readback and local codon-tile label splits only.  It does not test translation realization, folded structure, physical admissibility, biological function, universal mechanism, or any claim that the codon cube is a biological law.  Those higher layers require separate contacts.

\paragraph{Boundary row.}
The distinguished boundary row $R$ has size $13$.  In codon notation the row is
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same finite row is also recorded as a punctured-cube object with face-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the row is not merely a list of exceptional labels.  It has a local cubical incidence profile: thirteen vertices, sixteen one-dimensional incidences, and four two-dimensional tile faces inside the six-bit display.  The paper-level reading is deliberately narrow: the row is a code-coordinate boundary row, not a global biological constraint.

\paragraph{Spectral row data.}
The row also carries a finite spectral signature.  The recorded values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial cubic check is numerically closed at the displayed precision: the polynomial residual is $-6.224354365258478\times 10^{-12}$, and the same value is recorded for the cubic-polynomial satisfaction check.  The spectral-radius tolerance row reports $0.6752479308830213$, matching $\lambda_M$.  These numbers support a finite coordinate-and-spectrum description of the local tile, but they do not convert the tile into a translation, folding, physical, or functional theory.

\paragraph{Shape significance.}
Against the sampled null model with $N=2000$ and seed $42$, the exact-shape row reports one exact shape match, so
$$
\begin{aligned}
p_{\mathrm{exact}} = 0.0005.
\end{aligned}
$$
The high-overlap test reports $25$ samples with Jaccard score at least $0.9$, hence
$$
\begin{aligned}
p_{\mathrm{Jaccard}\ge 0.9} = 0.0125.
\end{aligned}
$$
The sampling trace records $57377$ distinct-sample attempts and $55377$ duplicate retries.  A second amino-acid-class-preserving null, again with $N=2000$ and seed $42$, reports no exact shape match, an observed $M$ size of $20$, and $7$ samples in the lower tail at or below that observed size.  Its exact-shape probability is recorded as $0.0$, and its lower-tail probability is
$$
\begin{aligned}
p_{\mathrm{aa\ class\ lower\ tail}} = 0.0035.
\end{aligned}
$$
These finite rows say that the displayed code-layer shape is uncommon under the sampled controls.  They do not say why biology realizes the table, nor do they supply a dynamical or structural cause.

\paragraph{Leave-one-code-out row.}
The leave-one-code-out test is the central row for this chapter.  It evaluated $23$ tables and $63$ total targets.  The baseline hit rate was
$$
\begin{aligned}
0.19821739130434785,
\end{aligned}
$$
while the reported hit rate above baseline was
$$
\begin{aligned}
0.9841269841269841.
\end{aligned}
$$
Inside $M\setminus T$ the hit rate was $0.9365079365079365$, and inside $M\setminus T$ plus the boundary row it rose to $0.9841269841269841$.  The finite reading is that the local coordinate, closure, spectrum, and relation data recover almost all code-read targets in this leave-one-code-out protocol, relative to the displayed baseline.  The statement remains a finite readback statement: it is about codon labels in the curated code table, not about amino-acid chemistry beyond those labels.

\paragraph{Family row.}
The family split contains $5$ families.  The family hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\ plastid} &= 1.0,\\
\mathrm{candidate\ other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\ ciliate} &= 1.0.
\end{aligned}
$$
The one non-unit displayed family rate is still far above the baseline row $0.19821739130434785$.  This supports the same local conclusion across the recorded table families: the finite tile carries strong code-read information.  It does not support universality in the biological sense, because the only contact supplied here is the curated code-label bridge.

\paragraph{Leave-one-codon-out and module classification.}
The leave-one-codon-out row reports no anchors, $5$ boundary codons, $8$ predictable codons, high closure coverage of $8$, and recovery rate $1.0$.  The module classification assigns all $13$ codons in $R$ and records no extra module codons, no missing codons, and no unassigned codons.  The modules are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
The module row is complete at the finite level because its assigned count is $13$, equal to $|R|$.  Each displayed module has either a predictable member or a boundary member according to the recorded classification.  This gives a local explanation of why the boundary row is useful for code-read recovery: the row is partitioned into finite label modules that align with the leave-one-code and leave-one-codon tests.

\paragraph{Boundary verdict.}
The chapter verdict is a code-read verdict.  The finite evidence supports the claim that window-six codon coordinates expose a local boundary-tile structure in the curated standard-code table.  The support comes from the $13$-codon boundary row, the punctured-cube face vector $(13,16,4)$, the spectral row with $\lambda_M=0.6752479308830213$ and $\lambda_R=0.4758352843197002$, the sampled shape-significance rows with $p_{\mathrm{exact}}=0.0005$ and amino-acid-class lower-tail probability $0.0035$, and the leave-one-code-out hit rate $0.9841269841269841$ over $63$ targets from $23$ tables.  The cannot-claim boundary is part of the result: no translation realization, no folded protein structure, no physical admissibility, no biological function, no mechanism, and no universality follows from this chapter unless a separate higher-layer reality contact supplies that information.