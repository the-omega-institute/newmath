\subsection{Leave-One-Code-Out Boundary Readback for the Codon Tile}
\label{sec:bioreality-h1-leave-one-code-out}
\origin{ai}

\paragraph{Finite carrier and reality contact.}
This BioReality record treats the standard-code codon table as a finite code-read contact, not as a mechanism.  The carrier is the six-bit codon cube $Q_6$, read through the four visible distinctions used by the packet: six-bit codon display, local tile face, singleton label corner, and three-point label class.  The only external contact used here is the curated standard-code table assigning each codon to an amino-acid or stop label.  Thus the readback target is a finite label table.  It is not translation realization, folded structure, physical admissibility, biological function, or universality.

\paragraph{Observed residual tile.}
The verified residual set has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&AAA,AGA,AGG,AUA,CUA,CUC,CUG,CUU,\\
&UAA,UAG,UCA,UGA,UUA\}.
\end{aligned}
$$
The same finite row reports the punctured-cube face vector
$$
\begin{aligned}
f(R)=(13,16,4).
\end{aligned}
$$
This is the local tile that the chapter is allowed to discuss.  Its role is boundary-level evidence: the codon labels in $R$ are visible to the curated code table, while the higher biological layers remain outside this contact.

\paragraph{Spectral and null-model checks.}
The spectrum row gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The radial cubic check is numerically closed at
$$
\begin{aligned}
P(\mu,\lambda_M,\lambda_R)=-6.224354365258478\cdot 10^{-12},
\end{aligned}
$$
which is recorded as the same tolerance-scale value for the cubic polynomial and radial polynomial.  The shape-significance row sampled $N=2000$ null instances with seed $42$, required $57377$ distinct-sample attempts, and recorded $55377$ duplicate retries.  It found $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$, hence
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9}&=0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null row also used $N=2000$ and seed $42$.  It reports observed $|M|=20$, exact match count $0$, and lower-tail count $7$, giving
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These finite numbers support a non-generic code-layer pattern.  They do not by themselves promote the pattern to a biological law.

\paragraph{Leave-one-code-out readback.}
The leave-one-code-out row evaluated $23$ tables and $63$ total targets.  The baseline hit rate is $0.19821739130434785$.  Against that baseline, the finite readback reports
$$
\begin{aligned}
\operatorname{hit}(M\setminus T)&=0.9365079365079365,\\
\operatorname{hit}(M\setminus T\cup\partial)&=0.9841269841269841,\\
\operatorname{hit}_{\mathrm{above\ baseline}}&=0.9841269841269841.
\end{aligned}
$$
The family split is likewise finite-row specific: $5$ families are evaluated, with above-baseline hit rates $0.8333333333333334$ for alternative yeast, and $1.0$ for bacterial/plastid, candidate-other, mitochondrial, and nuclear-ciliate families.  The result is therefore a code-read recovery statement over curated label tables: most leave-one-code-out targets fall inside the residual geometry plus its boundary, far above the stated baseline.

\paragraph{Codon-level module accounting.}
The leave-one-codon-out row supplies a separate local accounting: anchor count $0$, boundary count $5$, predictable count $8$, high closure coverage $8$, and recovery rate $1.0$.  The module classification is complete over the $13$ residual codons: assigned count $13$, $R$ size $13$, no extra module codons, no missing codons from modules, and no unassigned codons.  The module rows are
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
Every listed module has either predictable or boundary status.  This completes the finite code-layer accounting for the residual tile.

\paragraph{Boundary verdict.}
The chapter verdict is a scoped code-read claim.  Window-six codon coordinates expose a local boundary-tile structure in the curated standard-code table, and the leave-one-code-out row gives a high recovery rate relative to its finite baseline.  The contact does not observe ribosomal translation, molecular folding, structural order, thermodynamic admissibility, organismal function, evolutionary mechanism, or universality.  Any claim at those layers requires a separate reality contact.  In the present packet, amino-acid label-sequence readback remains only label-sequence readback.