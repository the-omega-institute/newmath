\origin{ai}

\subsection{Leave-one-code-out code-read boundary for the standard codon table}
\label{sec:bioreality-h1-leave-one-code-out}

\paragraph{Finite carrier.}
This record treats the standard genetic-code table as a finite code-read contact on the six-bit codon cube $Q_6$.  The carrier is not a molecular mechanism: it is the finite display obtained by assigning each codon a standard-code amino-acid or stop label and then asking which local boundary rows are exposed by the window-six coordinate system.  The finite distinctions used here are the six-bit codon display, local tile faces, singleton label corners, and three-point label classes.  The internal objects are coordinates, closures, spectra, and relations; the readback is only the standard-code label table.

\paragraph{Observed boundary row.}
The distinguished relation row has cardinality $13$.  The verified row is
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\\
&\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same finite row is certified as the punctured-cube row, with face-vector data
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the code-read contact is not a vague enrichment of the codon table: it selects a specific $13$-codon row with $16$ one-dimensional incidences and $4$ two-dimensional faces in the local cube geometry.

\paragraph{Spectral signature.}
The verified spectral row gives
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The reported radial polynomial residual is $-6.224354365258478\times 10^{-12}$, so the recorded cubic relation is numerically satisfied at the displayed precision.  This spectral contact is still a finite code-read invariant: it is a property of the coordinate-and-label construction, not an observation of translation, folding, physical admissibility, function, mechanism, or biological universality.

\paragraph{Shape significance against unrestricted rows.}
The finite Monte Carlo row for the $M$-shape test used $N=2000$ samples with seed $42$.  It found $1$ exact shape match and $25$ samples with Jaccard score at least $0.9$.  The resulting finite-row estimates are
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9}&=0.0125.
\end{aligned}
$$
The sampler made $57377$ distinct-sample attempts and recorded $55377$ duplicate retries.  These numbers support the claim that the local tile has a non-generic finite shape relative to the sampled null, while also showing why the conclusion must remain local: a small code-read $p$-value is not a mechanism and is not a physical realization theorem.

\paragraph{Amino-acid-class-preserving null row.}
A stricter null preserved amino-acid classes across $N=2000$ samples, again with seed $42$.  The observed $M$ size was $20$.  The exact-shape count was $0$, giving
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
The lower-tail count at or below the observed $M$ size was $7$.  This preserves the finite-row interpretation: the coordinate pattern remains unusual even under a label-class-preserving perturbation, but the contact still reads only codon labels.

\paragraph{Leave-one-code-out prediction row.}
The named leave-one-code-out test evaluated $23$ tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$.  The verified recovery rates were
$$
\begin{aligned}
\mathrm{hit\_rate\_above\_baseline}&=0.9841269841269841,\\
\mathrm{hit\_rate\_in\_}M\!\setminus\!T&=0.9365079365079365,\\
\mathrm{hit\_rate\_in\_}M\!\setminus\!T\mathrm{\ plus\ boundary}&=0.9841269841269841.
\end{aligned}
$$
Consequently, the finite claim is a code-read claim: when alternative code labels are withheld at the table level, the local tile and its boundary recover nearly all targets above the baseline row.  It does not assert that the code is generated by the geometry, nor that DNA obeys the six-bit display as a biological necessity.

\paragraph{Family split.}
The leave-one-code-out row also records $5$ family classes.  The family hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
This split is useful because it locates the one visibly weaker family row without changing the finite verdict: all reported families remain above the baseline contact, and four of the five family classes are recorded at perfect hit rate in this finite table audit.

\paragraph{Module classification.}
The $13$ codons in $R$ are exhausted by six named modules:
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The classification row reports $R$ size $13$, assigned count $13$, no extra module codons, no missing codons, and no unassigned codons.  In the leave-one-codon-out companion row, the anchor count is $0$, the boundary count is $5$, the predictable count is $8$, the high-closure-coverage count is $8$, and the recovery rate is $1.0$.  Thus every module has either a predictable or boundary status in the finite coordinate contact.

\paragraph{Reality-contact boundary.}
The only reality contact used here is the curated standard genetic-code bridge: a table assigning codons to amino-acid or stop labels.  That contact can test code-read behavior and local codon-tile label splits.  It cannot test translation realization, folded structure, structural order, physical admissibility, biological function, mechanism, or universality.  Amino-acid label-sequence readback therefore remains a code-layer observation.  No folded protein structure, no physical admissibility condition, and no function realization follows from this row unless a separate higher-layer contact supplies those observations.

\paragraph{Finite verdict.}
The claim $\mathrm{h1.leave.one.code.out}$ is ready as a finite BioReality code-read record.  The evidence is the conjunction of the explicit $13$-codon punctured-cube row, the spectral values $\lambda_M=0.6752479308830213$ and $\lambda_R=0.4758352843197002$, the unrestricted and amino-acid-class-preserving null rows, the $23$-table and $63$-target leave-one-code-out recovery row, and the complete six-module classification of $R$.  The conclusion is intentionally narrow: window-six codon coordinates expose a local boundary-tile structure around standard-code labels, while the curated code table remains the external reality contact and blocks promotion beyond code readback.