\subsection{Leave-One-Code-Out Readback for the Window-Six Codon Tile}\label{sec:h1-leave-one-code-out}\origin{ai}

\paragraph{Finite carrier and readback contact.} The carrier in this record is the six-bit codon cube $Q_6$, read only as a finite coordinate display. A codon is treated as a point of this cube, and the curated standard-code table is used as an external contact assigning each codon to an amino-acid or stop label. The contact tests code-layer readback and local label split only. It does not supply translation realization, folded structure, physical admissibility, biological function, mechanism, or universality. Those claims remain outside the present row unless a separate contact supplies them.

\paragraph{Boundary set.} The distinguished boundary set $R$ has cardinality $13$ and is exactly the following finite row:
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\},\qquad |R|=13.
\end{aligned}
$$
The same row is recorded as a punctured-cube boundary with face-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the geometry used here is not a biological law; it is a finite local tile on which standard-code labels can be read back and compared.

\paragraph{Spectral finite row.} The associated spectral record separates the $M$-side and $R$-side scales. The recorded values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\cdot 10^{-12}$, also recorded as the value of the cubic polynomial at the computed point. This is used only as a numerical consistency check on the finite coordinate row; it is not promoted to a dynamical or mechanistic biological statement.

\paragraph{Shape significance.} Against the unconstrained sampled null, the observed $M$-shape has one exact match among $2000$ samples, with $57377$ distinct-sample attempts and $55377$ duplicate retries. The exact-shape probability is
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}
$$
The high-overlap threshold gives $25$ matches with Jaccard similarity at least $0.9$, hence
$$
\begin{aligned}
p_{J\geq 0.9}=0.0125.
\end{aligned}
$$
Under the amino-acid-class-preserving null, the observed $M$-size is $20$, the exact-match count is $0$ among $2000$ samples, and the lower-tail count at or below the observed size is $7$. Therefore the recorded finite-row probabilities are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These rows justify treating the tile as a non-generic finite coordinate event relative to the sampled nulls, while still leaving the biological interpretation at the code-read layer.

\paragraph{Leave-one-code-out test.} The claim-specific leave-one-code-out row evaluates $23$ genetic-code tables and $63$ total targets. The baseline hit rate is $0.19821739130434785$. The observed hit rate above baseline is
$$
\begin{aligned}
0.9841269841269841,
\end{aligned}
$$
with hit rate inside $M\setminus T$ equal to $0.9365079365079365$ and hit rate inside $M\setminus T$ plus boundary equal to $0.9841269841269841$. Thus, when one code table is withheld, the local window-six tile plus boundary information recovers nearly all recorded targets above the baseline rate in this finite row. The conclusion is a code-label readback conclusion: the table labels are still supplied by the curated reality contact, not by the geometry alone.

\paragraph{Family-level split.} The same leave-one-code-out computation is recorded over $5$ families. The family hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\_yeast}&:0.8333333333333334,\\
\mathrm{bacterial\_plastid}&:1.0,\\
\mathrm{candidate\_other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\_ciliate}&:1.0.
\end{aligned}
$$
The weaker alternative-yeast row is still far above the baseline rate, while the other four listed families are recorded at full hit rate. This supports a local code-read stability claim across the evaluated family partition, not a universal statement about all genetic-code evolution.

\paragraph{Module accounting.} The $13$ codons of $R$ are completely assigned to six named modules, with no missing codons, no extra module codons, and no unassigned codons. The finite module decomposition is
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The module classification row records $R\_\mathrm{size}=13$ and assigned count $13$. Every module has a predictable-or-boundary witness in the row, so the local boundary split is not concentrated in one isolated codon class.

\paragraph{Leave-one-codon-out cross-check.} The companion leave-one-codon-out finite row records anchor count $0$, boundary count $5$, predictable count $8$, high closure coverage $8$, and recovery rate $1.0$. This supplies an internal robustness check: the row can recover the withheld codon signal through predictable or boundary behavior, but it still has no anchor codons and therefore does not license a stronger mechanistic reading.

\paragraph{Local verdict.} The finite evidence supports the following bounded statement. Window-six codon coordinates expose a local boundary-tile structure whose standard-code labels can be read back through the curated code table, and the leave-one-code-out rows show high recovery above the baseline rate. The evidence does not show that DNA obeys this geometry as biological necessity. It does not show translation realization, folded protein structure, structural order, physical admissibility, biological function, mechanism, or universality. Amino-acid label-sequence readback remains a code-layer observation, not a protein-structure or function observation.