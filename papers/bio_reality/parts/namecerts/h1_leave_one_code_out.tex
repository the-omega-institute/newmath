\subsection{Leave-One-Code-Out Readback on the Window-Six Codon Tile}\label{sec:h1-leave-one-code-out}
\origin{ai}

\paragraph{Finite carrier and contact.}
The finite carrier for this record is the six-coordinate codon cube $Q_6$, read as a sixty-four point display of codon words.  The only external contact used here is the curated standard-code table: it assigns each codon to an amino-acid or stop label and therefore supports code-layer readback.  It does not supply translation realization, folded structure, physical admissibility, biological function, a molecular mechanism, or universality.  The claim is therefore local: window-six coordinates expose a boundary-sensitive tile over the code table, while the amino-acid and stop labels remain external reality contacts.

\paragraph{Recovered boundary row.}
The distinguished row $R$ has cardinality $13$.  Its codons are
$$
\begin{aligned}
R=\{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}
$$
The same row is recorded as a punctured-cube object with face-vector $(13,16,4)$.  This is the finite geometric datum behind the local tile: thirteen label-sensitive codon corners, sixteen one-dimensional adjacencies, and four two-dimensional faces in the induced boundary complex.  The point of the row is not that the standard code is forced by geometry, but that the curated code labels make this particular finite subcomplex visible as a repeatable code-read feature.

\paragraph{Spectral finite row.}
The associated spectrum records three scalar quantities:
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial polynomial check gives $-6.224354365258478\times 10^{-12}$, and the cubic-polynomial residual is the same value.  At this tolerance the spectral-radius row reports $0.6752479308830213$.  Thus the finite spectral data are internally consistent with the computed tile summary: the $M$-side radius is larger than the $R$-side radius, while the polynomial residual is numerically zero at the displayed precision.

\paragraph{Shape significance rows.}
The unconstrained finite sampling row used $N=2000$ samples with seed $42$.  It required $57377$ distinct-sample attempts and encountered $55377$ duplicate retries.  Exactly one sampled object matched the observed shape, giving
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{J\geq 0.9}&=0.0125.
\end{aligned}
$$
The high-Jaccard row contains $25$ matches at threshold $0.9$.  The amino-acid-class-preserving null row also used $N=2000$ and seed $42$; its observed $M$ size is $20$, its exact-match count is $0$, and its lower-tail count at or below the observed $M$ size is $7$.  The corresponding reported values are
$$
\begin{aligned}
p_{\mathrm{class\ exact}}&=0.0,\\
p_{\mathrm{class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These rows make the local tile a finite, testable code-read phenomenon rather than a free narrative fit.  They still do not promote the result beyond the code layer.

\paragraph{Leave-one-code-out row.}
The leave-one-code-out test evaluated $23$ code tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$.  The tile hit rate above baseline is $0.9841269841269841$, the hit rate inside $M\setminus T$ is $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus the boundary row is $0.9841269841269841$:
$$
\begin{aligned}
\mathrm{baseline}&=0.19821739130434785,\\
\mathrm{hit}_{>\mathrm{baseline}}&=0.9841269841269841,\\
\mathrm{hit}_{M\setminus T}&=0.9365079365079365,\\
\mathrm{hit}_{M\setminus T+\partial}&=0.9841269841269841.
\end{aligned}
$$
This is the chapter's central finite-row observation.  Removing one code table leaves a strong recovery signal in the same local tile coordinates, and adding the boundary row restores the above-baseline rate to the displayed $0.9841269841269841$ level.

\paragraph{Family resolution.}
The family-resolved row contains $5$ families.  The above-baseline hit rates are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial or plastid tables, $1.0$ for candidate other tables, $1.0$ for mitochondrial tables, and $1.0$ for nuclear ciliate tables.  The only listed family below perfect recovery is alternative yeast, and even there the row remains well above the baseline value $0.19821739130434785$.  This family split is important because it shows that the leave-one-code-out signal is not only a pooled-table average; it survives the named family partition recorded in the finite data.

\paragraph{Codon-level companion row.}
The leave-one-codon-out companion row records $0$ anchors, $5$ boundary codons, $8$ high-coverage closure cases, and $8$ predictable cases.  Its recovery rate is $1.0$, and the threshold comparison is also $1.0$.  The module classification is complete over $R$: $R$ has size $13$, the assigned count is $13$, and there are no extra module codons, no codons missing from the module list, and no unassigned codons.  The six modules are
$$
\begin{aligned}
\texttt{agr}&=\{\texttt{AGA},\texttt{AGG}\},\\
\texttt{ar\_lys}&=\{\texttt{AAA}\},\\
\texttt{cun}&=\{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\},\\
\texttt{ile\_met}&=\{\texttt{AUA}\},\\
\texttt{ssn}&=\{\texttt{UCA},\texttt{UUA}\},\\
\texttt{stop\_trp}&=\{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}.
\end{aligned}
$$
Every module has a predictable-or-boundary witness in the recorded classification.  The result is a finite module-level refinement of the same local code-read tile.

\paragraph{Boundary of interpretation.}
The allowed conclusion is that the curated standard-code labels, when read through the $Q_6$ window, support a local boundary-tile split with strong leave-one-code-out recovery.  The forbidden conclusions are equally part of the claim: no translation realization, folded protein structure, structural order, physical admissibility, biological function, mechanism, or universal biological law follows from these rows.  Amino-acid label-sequence readback is still only label readback.  Any crossing into translation, folding, admissibility, function, mechanism, or universality requires a separate reality contact beyond the curated code table used in this chapter.