\origin{ai}

\subsection{Leave-one-code-out recovery at the codon-label boundary}
\label{sec:bioreality-h1-leave-one-code-out}

\paragraph{Finite carrier and reality contact.}
This record uses the six-bit codon cube $Q_6$ as the finite carrier.  Each codon is read as a six-coordinate display, and the only external contact used here is the curated standard-code label table: a codon is assigned to an amino-acid label or to stop.  The contact is therefore a code-read contact, not a translation apparatus.  It permits finite checks of label splits, local tile faces, singleton label corners, and three-point label classes inside $Q_6$, but it does not by itself observe a ribosome, a folded protein, a physical admissibility condition, a biochemical function, a mechanism, or a universal law.

\paragraph{Observed exceptional row.}
The distinguished row $R$ has cardinality $13$.  In this finite run the row is exactly
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same row is detected as a punctured-cube object with face-vector
$$
\begin{aligned}
f(R) = (13,16,4).
\end{aligned}
$$
Thus the code-read anomaly is not only a list of labels: it is a finite subobject of the codon cube with $13$ vertices, $16$ one-dimensional adjacencies, and $4$ two-dimensional faces in the induced local tile geometry.

\paragraph{Spectral and shape checks.}
The associated spectral summary separates the row scale from the larger tile scale.  The recorded values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\cdot 10^{-12}$, which is numerically zero at the precision of the run.  Against $N=2000$ distinct null samples, obtained after $57377$ sample attempts and $55377$ duplicate retries, the exact-shape count was $1$, giving
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{J\ge 0.9} &= 0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null gives an even stricter exact-shape observation: among $N=2000$ class-preserving samples, the exact-match count was $0$, while the lower-tail count at the observed $M$-size was $7$.  The observed $M$-size was $20$, with
$$
\begin{aligned}
p_{\mathrm{class,exact}} &= 0.0,\\
p_{\mathrm{class,lower\ tail}} &= 0.0035.
\end{aligned}
$$
These numbers support the finite claim that the row and its surrounding $M$-tile are not typical under the tested combinatorial nulls.  They do not identify a biological mechanism.

\paragraph{Leave-one-code-out recovery.}
The leave-one-code-out probe evaluated $23$ code tables and $63$ target events.  The baseline hit rate was
$$
\begin{aligned}
0.19821739130434785.
\end{aligned}
$$
The finite tile recovers targets far above that baseline: the hit rate above baseline was $0.9841269841269841$, the hit rate inside $M\setminus T$ was $0.9365079365079365$, and the hit rate inside $M\setminus T$ after adding the boundary was $0.9841269841269841$.  Thus the local boundary enlargement is not cosmetic in this probe: it raises the finite recovery region to the same recorded rate as the above-baseline recovery statistic.

\paragraph{Family stratification.}
The same leave-one-code-out behavior is visible after grouping the evaluated tables into $5$ families.  The family hit rates above baseline were
$$
\begin{aligned}
\mathrm{alternative\ yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\ plastid} &= 1.0,\\
\mathrm{candidate\ other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\ ciliate} &= 1.0.
\end{aligned}
$$
The alternative-yeast family is the only listed family below $1.0$, but it remains well above the baseline rate $0.19821739130434785$.  The finite conclusion is therefore family-stable at the code-read layer: the local tile is repeatedly informative about code-table variation, but the observation remains a table-level readback.

\paragraph{Leave-one-codon-out boundary behavior.}
The companion leave-one-codon-out probe recorded $8$ predictable codons, $5$ boundary codons, $0$ anchors, and closure coverage $8$.  Its recovery rate was $1.0$, and the recorded recovery-rate-above-threshold value was also $1.0$.  The module classification accounts for all $13$ codons of $R$: assigned count $13$, no extra module codons, no missing row codons, and no unassigned codons.  The modules are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA, AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA, CUC, CUG, CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA, UUA\},\\
\mathrm{stop\_trp} &= \{UAA, UAG, UGA\}.
\end{aligned}
$$
Every listed module has predictable-or-boundary support.  This is the local combinatorial sense in which the row behaves as a boundary tile: the modules cover the row without residue, and the leave-one probes keep the missing code or codon inside a finite predictable-or-boundary envelope.

\paragraph{Boundary of the claim.}
The claim proved by these finite rows is deliberately narrow.  Window-six codon coordinates expose a local boundary-tile structure for curated code labels.  The reality contact is the standard-code table, so the conclusion is a code-read conclusion.  No translation realization follows.  No folded structure follows.  No physical admissibility, biological function, universal mechanism, or global biological law follows.  Amino-acid label-sequence readback is not protein structure, and a local codon tile is not a law of DNA.  Any future promotion across those boundaries would require a separate reality contact at the relevant layer.