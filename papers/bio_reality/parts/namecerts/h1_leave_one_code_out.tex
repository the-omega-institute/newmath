\subsection{Leave-one-code-out readback on the window-six codon tile}\label{subsec:h1-leave-one-code-out}\origin{ai}

\paragraph{Finite object.} The carrier is the six-bit codon cube $Q_6$, read only as a finite coordinate display for codon labels. The contact used here is the curated standard-code table: each codon is assigned an amino-acid or stop label, and that table supplies the external labels against which the window-six construction is read. The claim is therefore a code-read claim. It is not a claim about translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.

\paragraph{Distinguished boundary row.} The finite distinguished row $R$ has cardinality $13$. In the supplied run it is exactly
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row is recorded as a punctured-cube object with $f$-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the row is not being used as an unconstrained list of exceptions. It is a finite face-and-boundary object inside the codon cube, with thirteen visible vertices, sixteen one-dimensional incidences, and four two-dimensional faces in the recorded tile.

\paragraph{Spectral readback.} The coordinate row is accompanied by a finite spectral check. The recorded values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial polynomial value is $-6.224354365258478\times 10^{-12}$, and the same value is recorded for the cubic-polynomial residual. This is within the numerical tolerance of the run and records that the displayed row is being read through a stable finite spectrum, not through a free narrative choice of codons.

\paragraph{Null comparison for the tile shape.} Against the recorded shape null with $N=2000$ samples and seed $42$, there was $1$ exact shape match. The exact-shape probability is therefore
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}
$$
There were $25$ matches at Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{J\ge 0.9}=0.0125.
\end{aligned}
$$
The run also records $57377$ distinct-sample attempts and $55377$ duplicate retries. Under the amino-acid-class-preserving null, again with $N=2000$ and seed $42$, the observed module size is $20$, the exact-match count is $0$, and the lower-tail count at or below the observed size is $7$. Hence the recorded probabilities are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These numbers support the local claim that the tile is a non-generic finite codon-label pattern under the tested nulls. They do not promote the tile into a biological law.

\paragraph{Leave-one-code-out recovery.} The principal row for this subsection is the leave-one-code-out test. The run evaluated $23$ code tables and $63$ total targets. The baseline hit rate is
$$
\begin{aligned}
0.19821739130434785.
\end{aligned}
$$
The hit rate above baseline is
$$
\begin{aligned}
0.9841269841269841,
\end{aligned}
$$
while the hit rate inside $M\setminus T$ is
$$
\begin{aligned}
0.9365079365079365.
\end{aligned}
$$
Including the boundary gives
$$
\begin{aligned}
\mathrm{hit\ rate}(M\setminus T+\partial)=0.9841269841269841.
\end{aligned}
$$
Thus, when one code table is left out, the local window-six tile recovers almost all targets above the baseline rate recorded for the same finite protocol.

\paragraph{Family split.} The leave-one-code-out behavior is not carried by a single family alone. The family count is $5$, with family-level hit rates above baseline
$$
\begin{aligned}
\mathrm{alternative\ yeast}&=0.8333333333333334,\\
\mathrm{bacterial\ plastid}&=1.0,\\
\mathrm{candidate\ other}&=1.0,\\
\mathrm{mitochondrial}&=1.0,\\
\mathrm{nuclear\ ciliate}&=1.0.
\end{aligned}
$$
This supports a finite code-read regularity across the recorded families. The weakest recorded family is still above four fifths, and the remaining four families are complete under the supplied row criterion.

\paragraph{Leave-one-codon-out companion check.} The companion leave-one-codon-out row records $0$ anchors, $5$ boundary cases, $8$ high-coverage closure cases, and $8$ predictable cases. Its recovery rate is
$$
\begin{aligned}
1.0,
\end{aligned}
$$
with the threshold comparison also recorded as $1.0$. This companion row is used here only as a consistency check on the local tile boundary: it says that the finite tile can recover codon-level omissions in the supplied test, not that codon geometry realizes translation or protein structure.

\paragraph{Module completeness.} The module classification assigns all $13$ codons of $R$ and records no missing, extra, or unassigned codons. The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The completeness row is
$$
\begin{aligned}
R_{\mathrm{size}}=13,\qquad \mathrm{assigned}=13,\qquad \mathrm{missing}=0,\qquad \mathrm{extra}=0,\qquad \mathrm{unassigned}=0.
\end{aligned}
$$
So every displayed boundary codon belongs to exactly the recorded local module accounting for this finite packet.

\paragraph{Local verdict.} The finite evidence supports the following bounded statement: window-six codon coordinates expose a local boundary-tile structure whose label readback is unusually stable under the supplied leave-one-code-out, leave-one-codon-out, spectral, and null-model checks. The standard-code label table remains an external reality contact; it is not derived from the cube alone. The result is therefore confined to code-layer label readback and local split behavior. No translation process, folded-protein structure, physical admissibility, biological function, universal mechanism, or global biological law follows from this chapter unless a separate higher-layer contact supplies that information.