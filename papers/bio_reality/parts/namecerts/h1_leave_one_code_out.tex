\subsection{Leave-One-Code-Out Code-Read Boundary}\label{sec:bioreality-h1-leave-one-code-out}
\origin{ai}

\paragraph{Finite carrier and reality contact.}
The record uses the six-bit codon cube $Q_6$ as its finite carrier and reads it only through a curated standard-code label table.  The biological object is therefore not translation as a cellular process, nor protein structure, nor function.  It is the finite table-level assignment of codons to amino-acid or stop labels.  The coordinate distinctions used in this row are the six-bit codon display, the local tile face, the singleton label corner, and the three-point label class.  The internal BEDC structure is recorded as coordinate, closure, spectrum, and relation data, with readback restricted to the standard-code label table.

\paragraph{Distinguished relation row.}
The finite relation row $R$ has cardinality $13$.  The verified codon list is
$$
\begin{aligned}
R=\{&\mathtt{AAA},\mathtt{AGA},\mathtt{AGG},\mathtt{AUA},\mathtt{CUA},\mathtt{CUC},\mathtt{CUG},\mathtt{CUU},\\
&\mathtt{UAA},\mathtt{UAG},\mathtt{UCA},\mathtt{UGA},\mathtt{UUA}\}.
\end{aligned}
$$
The same run identifies this row with a punctured-cube shape and reports the face vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the finite object is not merely a set of exceptional codons; it is a coded local tile with thirteen vertices, sixteen one-dimensional incidences, and four two-dimensional faces inside the six-bit codon display.

\paragraph{Spectral coordinate check.}
The spectral row supplies three scalar coordinates:
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic check evaluates to $-6.224354365258478\times 10^{-12}$, and the reported spectral radius in tolerance is $0.6752479308830213$.  This is used only as a finite coordinate consistency check.  It does not assert that genetic code evolution, translation chemistry, or protein folding is governed by this spectrum.

\paragraph{Shape significance against finite null rows.}
The shape-significance audit sampled $N=2000$ null rows with seed $42$.  It required $57377$ distinct-sample attempts and encountered $55377$ duplicate retries.  The exact-shape count was $1$, giving
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9}&=0.0125,
\end{aligned}
$$
with $25$ high-Jaccard matches.  A second amino-acid-class-preserving null model, again with $N=2000$ and seed $42$, reports observed $M$ size $20$, exact-match count $0$, and lower-tail count $7$.  Its exact-shape value is $0.0$ in the finite run, while the lower-tail value is
$$
\begin{aligned}
p_{\mathrm{class\ lower\ tail}}=0.0035.
\end{aligned}
$$
These numbers support the claim that the local tile is finite-row non-random relative to the stated null models.  They do not promote the tile to a global biological law.

\paragraph{Leave-one-code-out readback.}
The leave-one-code-out test evaluates $23$ code tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$.  The observed hit rate above baseline is $0.9841269841269841$, the hit rate inside $M\setminus T$ is $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus boundary is $0.9841269841269841$.  In finite-row terms, the code-read classifier recovers almost all held-out code-table targets once the local module and boundary geometry are supplied:
$$
\begin{aligned}
\mathrm{baseline}&=0.19821739130434785,\\
\mathrm{hit}(M\setminus T)&=0.9365079365079365,\\
\mathrm{hit}(M\setminus T\ \mathrm{plus\ boundary})&=0.9841269841269841.
\end{aligned}
$$
The family breakdown has five families.  The reported family hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate.

\paragraph{Leave-one-codon-out and module closure.}
The leave-one-codon-out row reports anchor count $0$, boundary count $5$, closure coverage high $8$, predictable count $8$, and recovery rate $1.0$.  The module classification accounts for all thirteen codons of $R$: assigned count $13$, no extra module codons, no missing codons, and no unassigned codons.  The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathtt{AGA},\mathtt{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathtt{AAA}\},\\
\mathrm{cun}&=\{\mathtt{CUA},\mathtt{CUC},\mathtt{CUG},\mathtt{CUU}\},\\
\mathrm{ile\_met}&=\{\mathtt{AUA}\},\\
\mathrm{ssn}&=\{\mathtt{UCA},\mathtt{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathtt{UAA},\mathtt{UAG},\mathtt{UGA}\}.
\end{aligned}
$$
Every listed module has a predictable-or-boundary explanation in the finite record.  This gives a closed code-read description of the local tile: the codons in $R$ are exhausted by the six named modules, and the leave-one tests remain inside predictable or boundary behavior.

\paragraph{Boundary verdict.}
The supported claim is narrow.  Window-six codon coordinates expose a local boundary-tile structure in the curated standard-code table, and the finite tests show strong code-read recovery relative to the stated baselines.  The contact is still only a codon-label contact.  No translation realization, folded protein structure, structural order, physical admissibility, biological function, universal mechanism, or biological necessity follows from these rows.  Any such higher-layer assertion requires a separate reality contact with its own finite rows and its own null model.