\subsection{Leave-One-Code-Out Readback for the Window-Six Codon Tile}\label{sec:h1-leave-one-code-out}\origin{ai}

\paragraph{Finite carrier and contact.}
The carrier is the six-bit codon cube $Q_6$, read only as a finite codon display. The biological contact used here is the curated standard-code table: it assigns each codon to an amino-acid or stop label, and it is used only as a code-layer label bridge. The local distinctions retained by the record are the six-bit codon display, the local tile face, the singleton label corner, and the three-point label class. The internal BEDC-side structure is coordinate, closure, spectrum, and relation; the readback is the standard-code label table. This chapter therefore tests a local code-read phenomenon, not translation as a process.

\paragraph{Observed relation row.}
The finite relation row has cardinality $13$. The row is the following codon set:
$$\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}$$
The punctured-cube check gives the same row and reports the face-vector signature
$$\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}$$
Thus the observed row is not being treated as an informal visual analogy: it is a finite subset of $Q_6$ with a recorded combinatorial boundary profile.

\paragraph{Spectral finite-row signature.}
The spectral audit reports
$$\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}$$
The cubic or radial polynomial residual is $-6.224354365258478\cdot 10^{-12}$, with the same value recorded for the radial polynomial check. The spectral radius tolerance row records $0.6752479308830213$. These numbers are used only as a finite signature of the coordinate-and-closure construction; they are not promoted to a biochemical law.

\paragraph{Shape significance checks.}
Against the sampled null model with $N=2000$ and seed $42$, the exact shape match count is $1$, giving
$$\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}$$
The high-overlap row records $25$ samples with Jaccard score at least $0.9$, giving
$$\begin{aligned}
p_{J\geq 0.9}=0.0125.
\end{aligned}$$
The sampling log records $57377$ distinct-sample attempts and $55377$ duplicate retries. Under the amino-acid-class-preserving null model with $N=2000$ and seed $42$, the observed size is $|M|=20$, the exact-shape match count is $0$, and the lower-tail count at or below the observed $M$ size is $7$. The recorded probabilities are
$$\begin{aligned}
p_{\mathrm{aa\ class,exact}}&=0.0,\\
p_{\mathrm{aa\ class,lower\ tail}}&=0.0035.
\end{aligned}$$
These rows support the claim that the local tile is statistically nontrivial relative to the supplied finite nulls, while still leaving the contact at code-read resolution.

\paragraph{Leave-one-code-out result.}
For the claim $h1.leave.one.code.out$, the leave-one-code-out audit evaluates $23$ code tables and $63$ total targets. The baseline hit rate is
$$\begin{aligned}
0.19821739130434785,
\end{aligned}$$
whereas the finite BEDC-side rows report
$$\begin{aligned}
\mathrm{hit\ rate\ above\ baseline}&=0.9841269841269841,\\
\mathrm{hit\ rate\ in\ }M\setminus T&=0.9365079365079365,\\
\mathrm{hit\ rate\ in\ }M\setminus T\mathrm{\ plus\ boundary}&=0.9841269841269841.
\end{aligned}$$
The chapter-specific reading is therefore narrow: when a code label is withheld from the comparison row, the local tile-plus-boundary structure recovers almost all of the finite targets above the baseline rate, over the stated $23$-table, $63$-target audit.

\paragraph{Family-resolved row.}
The family analysis records $5$ families. The family hit rates above baseline are
$$\begin{aligned}
\mathrm{alternative\ yeast}&=0.8333333333333334,\\
\mathrm{bacterial\ plastid}&=1.0,\\
\mathrm{candidate\ other}&=1.0,\\
\mathrm{mitochondrial}&=1.0,\\
\mathrm{nuclear\ ciliate}&=1.0.
\end{aligned}$$
This is a finite family-stratified readback statement. It says that the local row remains strong across the supplied family bins; it does not say that the bins are mechanistic causes.

\paragraph{Leave-one-codon and module classification.}
The companion leave-one-codon row records anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$. The module classification assigns all $13$ relation codons, with $R$ size $13$, assigned count $13$, no missing module codons, no extra module codons, and no unassigned codons. The modules are
$$\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}$$
Every listed module has a predictable or boundary status, and the finite module row exhausts the relation row.

\paragraph{Local boundary verdict.}
The result is a code-read verdict for the standard-code label table. The support condition is that the curated table supplies codon-to-label readback and the finite rows expose a local boundary-tile split. The break condition is any promotion of this tile to translation realization, folded structure, physical admissibility, biological function, universal mechanism, or necessity of DNA obeying BEDC geometry. No such promotion is licensed here. Amino-acid label-sequence readback is not folded protein structure. A local codon tile is not a global biological law. Window-six geometry alone is not translation, structure, physical admissibility, function, mechanism, or universality unless a separate reality contact supplies that layer.