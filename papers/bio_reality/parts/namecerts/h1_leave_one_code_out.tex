\subsection{Leave-one-code-out recovery in the window-six codon tile}
\label{sec:bior-reality-h1-leave-one-code-out}
\origin{ai}

\paragraph{Finite carrier and contact.}
The record uses the six-bit codon cube $Q_6$ as a finite coordinate carrier for standard-code label readback.  A codon is read only as a displayed coordinate with an externally curated amino-acid or stop label.  The contact is therefore the curated standard-code table: it supplies codon-to-label assignments, but it does not supply translation dynamics, folded structure, physical admissibility, biological function, a mechanism, or universality.  The local object is a tile boundary inside $Q_6$, not a global law for molecular biology.

The distinguished residual row $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row is the punctured-cube residue with face-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the local carrier has a concrete finite shape: thirteen codon vertices, sixteen codon-adjacency edges, and four local square faces.  This shape is the finite row that the leave-one-code-out probe is allowed to use.

\paragraph{Spectral and null-row checks.}
The finite spectrum separates the residual row from the ambient measured tile.  The recorded values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\times 10^{-12}$, so the observed spectral radius is numerically on the polynomial row to the displayed tolerance.  The random-shape comparison used $N=2000$ sampled rows with seed $42$.  It saw one exact shape match, giving $p_{\mathrm{exact}}=0.0005$, and twenty-five rows with Jaccard score at least $0.9$, giving $p_{\mathrm{Jaccard}\ge 0.9}=0.0125$.  The sampling process required $57377$ distinct-sample attempts and $55377$ duplicate retries, which is itself a finite warning that the sampled universe has substantial collision pressure.

The amino-acid-class-preserving null row gives a stricter check.  In $N=2000$ class-preserving samples, the exact-shape count was $0$, the observed $M$-size was $20$, and only $7$ samples fell in the lower tail at or below that observed size.  The resulting rows are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
No list of the twenty $M$ codons is carried by this row, so the chapter uses only the reported scalar $|M|=20$ and does not reconstruct an unprovided $M$ membership list.

\paragraph{Leave-one-code-out result.}
The leave-one-code-out probe evaluated $23$ code tables and $63$ target events.  The baseline hit rate was
$$
\begin{aligned}
0.19821739130434785,
\end{aligned}
$$
while the hit rate above baseline was
$$
\begin{aligned}
0.9841269841269841.
\end{aligned}
$$
Inside $M\setminus T$ the hit rate was $0.9365079365079365$, and inside $M\setminus T$ together with the boundary allowance it was again $0.9841269841269841$.  The finite conclusion is therefore narrow but strong: under the curated label contact, the local tile almost always recovers the held-out code-row target above the baseline rate.  This is a code-read statement about label rows in $Q_6$; it is not a statement that the cell implements this tile as a necessary mechanism.

The codon-level leave-one-out row is compatible with that interpretation.  It has anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$.  Hence the local row has no independent anchor witnesses, but its predictable and boundary classes cover the tested codon recovery events.  The row supports a boundary-tile reading rather than a causal reading.

\paragraph{Family and module rows.}
The family split contains $5$ families.  Their hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
The weakest listed family is alternative yeast, but it remains far above the global baseline $0.19821739130434785$.  The complete module classification assigns all $13$ residual codons and leaves no missing, extra, or unassigned codons.  The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The audit row reports $R$-size $13$, assigned count $13$, empty extra-module codons, empty missing-from-modules list, and empty unassigned-codons list.  Each module therefore has a predictable-or-boundary explanation inside the local tile vocabulary.

\paragraph{Boundary verdict.}
The chapter records a ready code-layer claim: window-six codon coordinates expose a local boundary-tile structure for the standard-code label table, and the leave-one-code-out row reaches $0.9841269841269841$ above-baseline hit rate over $63$ targets from $23$ evaluated tables.  The shape, spectrum, null rows, family split, and module partition all support the same finite reading.  The cannot-claim boundary is part of the claim itself: no translation realization, folded protein structure, structural order, physical admissibility, biological function, universal mechanism, or global biological law follows from this row unless a separate higher-layer reality contact supplies that missing evidence.