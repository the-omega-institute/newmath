\subsection{Leave-One-Code-Out Code Readback Boundary}\label{sec:h1-leave-one-code-out}\origin{ai}

\paragraph{Finite-row object.}
This BioReality record concerns the finite standard-code codon table at the code-read layer.  The carrier is the six-bit codon cube $Q_6$, read through the concrete codon labels supplied by the curated standard genetic-code bridge.  The BEDC-side distinctions used here are the six-bit codon display, a local tile face, a singleton label corner, and a three-point label class.  The internal structure used by the row is coordinate, closure, spectrum, and relation data; the biological readback is only the standard-code label table.  The claim identifier is $\texttt{h1.leave.one.code.out}$, and its role is deliberately local: window-six coordinates may expose a boundary-tile pattern among code labels, but the amino-acid and stop labels themselves are external reality contacts, not consequences of the coordinate system alone.

\paragraph{Relation row.}
The relation row $R$ has cardinality $13$.  The verified member list is
$$
\begin{aligned}
R=\{&AAA,AGA,AGG,AUA,CUA,CUC,CUG,CUU,\\
&UAA,UAG,UCA,UGA,UUA\}.
\end{aligned}
$$
The same finite row is certified as a punctured-cube pattern with $f$-vector $(13,16,4)$.  Thus the row is not merely a count: it has thirteen vertices, sixteen local one-dimensional incidences, and four two-dimensional tile faces in the punctured-cube presentation.  These numbers are the finite support on which the leave-one-code-out probe is evaluated.

\paragraph{Spectral row.}
The spectral audit gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The radial cubic check is numerically closed at the recorded tolerance:
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128,\\
P_{radial}&=-6.224354365258478\times 10^{-12}.
\end{aligned}
$$
This says that the finite coordinate-and-relation row has a stable spectral readout for this packet.  It does not say that the spectrum is a biochemical force, a translation mechanism, or a physical admissibility criterion.

\paragraph{Shape significance.}
Against the reported finite null sample with $N=2000$, the exact shape count is $1$, giving $p_{exact}=0.0005$.  The high-Jaccard count at threshold $0.9$ is $25$, giving $p_{J\ge 0.9}=0.0125$.  The sample required $57377$ distinct-sample attempts with $55377$ duplicate retries at seed $42$.  In the amino-acid-class-preserving null row, the observed $M$ size is $20$, the exact match count is $0$, and the lower-tail count at or below the observed $M$ size is $7$, so
$$
\begin{aligned}
N&=2000,\\
p_{class\ exact}&=0.0,\\
p_{class\ lower\ tail}&=0.0035,\\
|M|_{observed}&=20.
\end{aligned}
$$
These finite-row comparisons support the statement that the observed local tile is uncommon under the recorded nulls.  They do not promote the tile to a universal biological law.

\paragraph{Leave-one-code-out probe.}
The leave-one-code-out row evaluated $23$ tables and $63$ total targets.  The baseline hit rate is $0.19821739130434785$.  The recorded hit rate above baseline is $0.9841269841269841$; the hit rate inside $M\setminus T$ is $0.9365079365079365$; and the hit rate inside $M\setminus T$ plus the boundary row is $0.9841269841269841$:
$$
\begin{aligned}
\text{tables evaluated}&=23,\\
\text{targets}&=63,\\
\text{baseline hit rate}&=0.19821739130434785,\\
\text{above-baseline hit rate}&=0.9841269841269841,\\
\text{hit rate in }M\setminus T&=0.9365079365079365,\\
\text{hit rate in }M\setminus T\text{ plus boundary}&=0.9841269841269841.
\end{aligned}
$$
The finite-row interpretation is that the local tile and its boundary account for almost all recorded leave-one-code-out targets relative to the baseline.  This is a code-read prediction statement about curated label tables, not a claim about translation realization or protein production.

\paragraph{Family readback.}
The by-family row reports $5$ families.  The family hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial or plastid codes, $1.0$ for the candidate-other group, $1.0$ for mitochondrial codes, and $1.0$ for nuclear ciliate codes.  This family split is consistent with the same boundary-tile reading: the row is not concentrated in one named biological family, although alternative yeast is the least saturated family in this finite table.  The statement remains finite and extensional: it is a comparison across the recorded table families, not a derivation of code evolution.

\paragraph{Leave-one-codon-out and modules.}
The leave-one-codon-out row has anchor count $0$, boundary count $5$, predictable count $8$, high closure coverage $8$, and recovery rate $1.0$.  The module classification is complete: $R$ has size $13$, the assigned count is $13$, and there are no extra module codons, no missing codons from the modules, and no unassigned codons.  The module partition is
$$
\begin{aligned}
agr&=\{AGA,AGG\},\\
ar\_lys&=\{AAA\},\\
cun&=\{CUA,CUC,CUG,CUU\},\\
ile\_met&=\{AUA\},\\
ssn&=\{UCA,UUA\},\\
stop\_trp&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
Each module has a predictable or boundary status in the finite classification.  The absence of anchors and the presence of five boundary cases are important: the row is better read as a local boundary geometry than as a mechanism that fixes biological meaning.

\paragraph{Reality contact and cannot-claim boundary.}
The only reality contact used here is the curated standard-code table, which assigns codons to amino-acid or stop labels at codon-label readback resolution.  That contact can test code readback and a local codon-tile label split.  It cannot test translation realization, folded structure, structural order, physical admissibility, function realization, mechanism, or universality.  Therefore no translation, folding, physical admissibility, biological function, universal mechanism, or global biological law follows from this chapter unless a separate contact supplies that higher-layer evidence.  Amino-acid label-sequence readback is not folded protein structure, and a local codon tile is not a global biological law.