\subsection{Leave-One-Code-Out Boundary in the Window-Six Codon Cube}
\label{sec:biorality-h1-leave-one-code-out}
\origin{ai}

\paragraph{Claim and carrier.}
This BioReality record concerns the claim \texttt{h1.leave.one.code.out}.  Its finite carrier is the six-bit codon cube $Q_6$, read as a codon display rather than as a biochemical mechanism.  The distinctions used here are a local tile face, singleton label corners, and three-point label classes.  The internal data are coordinate, closure, spectrum, and relation data, with readback through the curated standard-code label table.  The biological object touched by the record is only the standard-code codon table: a table assigning codons to amino-acid or stop labels.  The claimed layer is therefore $\mathrm{code\_read}$, not translation, folding, physical admissibility, biological function, mechanism, or universality.

\paragraph{Relation row.}
The finite relation row $R$ has cardinality $13$.  The verified codons in the row are
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row is verified as a punctured-cube row with $f$-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the observed local tile is not a full cube and not a global code law; it is a finite boundary pattern inside the six-bit codon display.  This point matters because the row can support local split checks against the curated code labels, while still leaving higher biological layers untouched.

\paragraph{Spectral row.}
The associated spectral quantities give a second finite check on the same carrier.  The recorded values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial or cubic polynomial residual is $-6.224354365258478\times 10^{-12}$, so the finite calculation is numerically at tolerance for this row.  The spectral row is evidence for a stable coordinate-and-closure signature in the codon cube; it is not evidence that the codon cube itself causes translation or constrains protein structure.

\paragraph{Shape statistics.}
The $M$-shape comparison was run with $N=2000$ samples and seed $42$.  The run required $57377$ distinct-sample attempts and recorded $55377$ duplicate retries.  It found $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9}&=0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null model also used $N=2000$ and seed $42$.  In that constrained null, the observed $M$ size was $20$, the exact-match count was $0$, and the lower-tail count at or below the observed $M$ size was $7$.  The reported values were
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact\ shape}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These numbers support the claim that the observed code-label split is not a routine outcome of the tested finite null rows.  They do not identify a physical cause, a translation apparatus, or a functional advantage.

\paragraph{Leave-one-code-out row.}
The leave-one-code-out probe evaluated $23$ code tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$.  The hit rate above baseline was $0.9841269841269841$, the hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary was $0.9841269841269841$.  In compact form,
$$
\begin{aligned}
\mathrm{tables}&=23,\\
\mathrm{targets}&=63,\\
\mathrm{baseline}&=0.19821739130434785,\\
\mathrm{hit}_{>\mathrm{baseline}}&=0.9841269841269841,\\
\mathrm{hit}_{M-T}&=0.9365079365079365,\\
\mathrm{hit}_{M-T+\partial}&=0.9841269841269841.
\end{aligned}
$$
The finite-row reading is that almost all tested leave-one-code-out targets land above the baseline once the local $M-T$ and boundary structure are used.  This is a code-read result about label recovery across curated tables, not a claim about the existence, universality, or mechanism of the genetic code.

\paragraph{Family row.}
The family-level leave-one-code-out record contains $5$ families.  The hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast}&=0.8333333333333334,\\
\mathrm{bacterial\ plastid}&=1.0,\\
\mathrm{candidate\ other}&=1.0,\\
\mathrm{mitochondrial}&=1.0,\\
\mathrm{nuclear\ ciliate}&=1.0.
\end{aligned}
$$
This distribution is chapter-specific evidence that the local tile is informative across the sampled code-family rows.  The weakest listed family is still above the baseline row, while four of the five families are recorded at $1.0$.

\paragraph{Codon-level module row.}
The leave-one-codon-out record reports no anchors, $5$ boundary cases, $8$ high-coverage closure cases, and $8$ predictable cases, with recovery rate $1.0$.  The module classification is complete: $R$ has size $13$, the assigned count is $13$, and there are no extra module codons, no codons missing from modules, and no unassigned codons.  The six finite modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every module has predictable-or-boundary status in the supplied row.  This gives a finite account of why the local tile can recover code labels under leave-one-codon-out testing, while still remaining a local code-layer statement.

\paragraph{Reality contact and boundary.}
The only reality contact used here is the curated standard-code table.  It observes that codons have amino-acid or stop labels; it does not observe ribosomal realization, folding, structural order, physical admissibility, function, mechanism, or universality.  Consequently, the support condition is narrow: the curated code table supports codon-label readback and local split checks.  The break condition is any promotion of this local tile to translation, structure, physical admissibility, function, universal mechanism, or biological necessity without a separate higher-layer contact.

\paragraph{Verdict.}
The claim is ready at the code-read layer.  The finite rows jointly show a $13$-codon punctured-cube relation, a stable spectral signature, low null-model frequencies for the observed shape, a $23$-table leave-one-code-out hit rate of $0.9841269841269841$ above baseline, and a complete $13$-codon module classification.  The result is therefore a bounded BioReality record: window-six codon coordinates expose local boundary-tile structure in curated code labels, while all stronger biological interpretations remain blocked until separate contacts are supplied.