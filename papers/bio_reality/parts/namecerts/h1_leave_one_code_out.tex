\origin{ai}
\subsection{Leave-one-code-out recovery in the window-six codon tile}
\label{sec:h1-leave-one-code-out}

\paragraph{Finite object.}
The record studies the standard-code codon table only at the code-read layer.  The carrier is the six-bit codon cube $Q_6$, read through a curated codon-to-label table whose rows assign each codon to an amino-acid or stop label.  The BEDC distinctions used here are a six-bit codon display, a local tile face, a singleton label corner, and a three-point label class.  These distinctions support a local boundary-tile computation, not a biological mechanism.  The finite exceptional row $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same row is recorded as a punctured-cube object with $f$-vector $(13,16,4)$.  Thus the code-read object is not an unstructured list of labels: it carries a finite local shape with thirteen vertices, sixteen one-dimensional incidences, and four tile faces.

\paragraph{Spectral and shape evidence.}
The spectral probe separates the $M$ and $R$ rows by the reported radii
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The associated radial cubic residual is $-6.224354365258478\times 10^{-12}$, which is numerically at tolerance for the stated finite computation.  The shape-significance probe used $N=2000$ samples with seed $42$, requiring $57377$ distinct-sample attempts and $55377$ duplicate retries.  It found $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9}&=0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null used the same sample count and seed.  In that null, the observed $M$ size is $20$, the exact-match count is $0$, the lower-tail count at or below the observed $M$ size is $7$, and the lower-tail probability is $0.0035$.  These numbers support a finite code-table boundary pattern, while leaving the biological interpretation confined to label readback.

\paragraph{Leave-one-code-out row.}
For the claim $h1.leave.one.code.out$, the finite evaluation covers $23$ code tables and $63$ held-out targets.  The baseline hit rate is $0.19821739130434785$.  The tile-derived hit rate above baseline is $0.9841269841269841$, the hit rate inside $M\setminus T$ is $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus boundary is again $0.9841269841269841$.  In compact form, the observed row is
$$
\begin{aligned}
\mathrm{tables}&=23,\\
\mathrm{targets}&=63,\\
\mathrm{baseline}&=0.19821739130434785,\\
\mathrm{tile\ hit\ rate}&=0.9841269841269841.
\end{aligned}
$$
The gap between the baseline and the tile hit rate is the chapter-specific finite signal: under leave-one-code-out removal, the window-six local tile almost always places the target in the predicted or boundary-supported region.  This is a recovery statement about finite code rows, not a claim that the codon cube causes the genetic code.

\paragraph{Family and module checks.}
The family-level split has $5$ families.  The hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate.  The module classification is complete: $R$ has size $13$, the assigned count is $13$, and there are no extra module codons, no missing module codons, and no unassigned codons.  The six modules are
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
Every module has predictable-or-boundary support.  The companion leave-one-codon-out record has anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$.  These module data make the boundary interpretation sharper: the signal is distributed across named codon modules rather than being only a scalar aggregate.

\paragraph{Reality contact and cannot-claim boundary.}
The only reality contact used here is the curated standard genetic-code bridge: a table that assigns codons to amino-acid or stop labels.  It can test code readback and local codon-tile label splits.  It cannot test translation realization, folded protein structure, structural order, physical admissibility, biological function, a universal mechanism, or the necessity of BEDC geometry in DNA.  Therefore the finite verdict is local and conditional: window-six coordinates expose a robust boundary-tile pattern in the curated code table, with $23$ tables, $63$ targets, a $0.9841269841269841$ above-baseline hit rate, a thirteen-codon punctured-cube row, and complete six-module accounting.  No translation, folding, physical admissibility, function, mechanism, or universality follows unless a separate contact supplies that higher-layer evidence.