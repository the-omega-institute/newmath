\subsection{Leave-One-Code-Out Boundary Readback in the Codon Cube}
\label{sec:bioreality-h1-leave-one-code-out}
\origin{ai}

\paragraph{Finite carrier and reality contact.}
The finite carrier is the six-bit codon cube $Q_6$, read as a codon display whose vertices are the $64$ codons over $\{\mathrm{A},\mathrm{C},\mathrm{G},\mathrm{U}\}$.  The BioReality contact used here is only the curated standard-code label table: each codon is assigned an amino-acid label or a stop label.  This contact tests code-layer readback and local label splits.  It does not test translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.  Thus the object of study is not protein biology as such, but a finite labeled cube with an externally supplied standard-code readback.

\paragraph{Observed punctured boundary row.}
The distinguished row $R$ has cardinality $13$ and is exactly the following codon set:
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The recorded finite geometry identifies this same row as a punctured-cube pattern with $f$-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
The data therefore separate a concrete local tile from the full $64$-vertex codon cube: the tile has $13$ vertices, $16$ one-dimensional incidences, and $4$ square faces in the extracted local complex.  This is a coordinate and closure statement about the labeled codon display, not a biological law about DNA.

\paragraph{Spectral row signature.}
The spectral record gives the finite-row scalars
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The same run reports a radial polynomial residual of $-6.224354365258478\times 10^{-12}$, equivalently the cubic-polynomial check is satisfied to the same numerical residual.  In this chapter the spectral numbers are used only as finite signatures of the codon-tile extraction.  They do not identify a physical energy, a folding constraint, or an evolutionary mechanism.

\paragraph{Shape significance against finite null rows.}
Against the recorded shape null with $N=2000$ samples and seed $42$, the exact-shape count is $1$, so the exact-shape probability is
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}
$$
The high-overlap statistic records $25$ matches with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{J\geq 0.9}=0.0125.
\end{aligned}
$$
The sampler required $57377$ distinct-sample attempts and encountered $55377$ duplicate retries, so the finite null space is visibly constrained by collisions.  A second amino-acid-class-preserving null with $N=2000$ reports observed $|M|=20$, exact-match count $0$, lower-tail count $7$, and
$$
\begin{aligned}
p_{\mathrm{aa\ class,exact}}&=0.0,\\
p_{\mathrm{aa\ class,lower\ tail}}&=0.0035.
\end{aligned}
$$
These numbers support the claim that the extracted tile is not a typical row under the tested finite nulls.  They do not upgrade the tile into a global rule for translation or protein structure.

\paragraph{Leave-one-code-out readback.}
The claim-specific leave-one-code-out row evaluates $23$ code tables and $63$ total targets.  Its baseline hit rate is
$$
\begin{aligned}
0.19821739130434785.
\end{aligned}
$$
The observed hit rate above baseline is
$$
\begin{aligned}
0.9841269841269841,
\end{aligned}
$$
while the hit rate inside $M\setminus T$ is
$$
\begin{aligned}
0.9365079365079365
\end{aligned}
$$
And the hit rate inside $M\setminus T$ plus the boundary is
$$
\begin{aligned}
0.9841269841269841.
\end{aligned}
$$
Thus the finite row says that, when one code table is held out, the local window-six tile plus its boundary almost always recovers the relevant code-layer target relative to the baseline.  The statement is deliberately local: it is about codon-label readback under a curated table bridge.

\paragraph{Family-level resolution.}
The family stratification has $5$ families.  The hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
The only listed family below perfect recovery is the alternative-yeast family, still above the baseline by a wide margin.  The other four families have hit rate $1.0$ in the recorded finite row.  This makes the leave-one-code-out behavior a family-stable code-read phenomenon rather than a single aggregate artifact.

\paragraph{Codon-level boundary modules.}
The leave-one-codon-out row reports anchor count $0$, boundary count $5$, predictable count $8$, closure coverage high $8$, and recovery rate $1.0$.  The module classification assigns all $13$ row codons and no others: $R$ has size $13$, assigned count is $13$, and the extra, missing, and unassigned lists are all empty.  The complete module table is
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every listed module has predictable or boundary status.  The boundary count $5$ and predictable count $8$ add to the full row size $13$, matching the module-complete accounting.

\paragraph{Boundary verdict.}
The ready claim is that window-six codon coordinates expose a local boundary-tile structure in the standard-code label table.  The strongest supported readback is code-layer: a $13$-codon punctured row with a four-face local complex, stable spectral scalars, significant finite-null behavior, high leave-one-code-out recovery across $23$ tables and $63$ targets, and a complete six-module classification of the row.  The cannot-claim boundary is part of the mathematical statement.  No translation realization, folded protein structure, physical admissibility, biological function, universal mechanism, or global biological law follows from these rows unless a separate higher-layer reality contact supplies that information.