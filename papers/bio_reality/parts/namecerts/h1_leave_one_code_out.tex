\origin{ai}
\subsection{Leave-One-Code-Out Readback for the Window-Six Codon Tile}
\label{sec:h1-leave-one-code-out}

\paragraph{Finite object under test.}
The record concerns the standard-code codon table at the code-read layer.  The finite carrier is the six-coordinate codon cube $Q_6$, read as a codon display rather than as a biochemical mechanism.  The distinguished finite features are a local tile face, a singleton label corner, and a three-point label class, with coordinate, closure, spectral, and relational data used only to organize codon-label readback.  The curated standard-code table is the contact that supplies labels: it assigns each codon an amino-acid or stop label, but it does not by itself observe translation dynamics, folded structure, physical admissibility, function, or a universal biological law.

\paragraph{Observed residual set.}
The finite residual set $R$ has cardinality $13$.  In the recorded row it is exactly
$\{\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}$.
The same row identifies this set with a punctured cube whose face-vector is $(13,16,4)$.  Thus the test is not an unstructured list of exceptional codons: the residual codons occupy a finite local shape with thirteen vertices, sixteen one-dimensional adjacencies, and four two-dimensional faces.  This shape is the concrete object against which the leave-one-code-out recovery row is evaluated.

\paragraph{Spectral coordinate check.}
The spectral row supplies three scalar coordinates for the same finite construction.  The recorded values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\times 10^{-12}$, and the same value is reported as the cubic-polynomial residual.  The spectral-radius value in tolerance is $0.6752479308830213$.  These numbers are used here as finite diagnostics of the codon tile; they are not promoted into any claim about molecular realization.

\paragraph{Leave-one-code-out row.}
For claim $\mathrm{h1.leave.one.code.out}$, the experiment evaluated $23$ code tables and $63$ total targets.  The baseline hit rate was $0.19821739130434785$.  The hit rate above that baseline was $0.9841269841269841$, the hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ together with the boundary allowance was $0.9841269841269841$.  Equivalently, the finite row says that the local tile plus boundary account recovers almost all held-out code-table targets in this dataset, while the stricter $M-T$ region alone misses a small remainder.

\paragraph{Shape significance and amino-acid-class null.}
The finite-row significance test used $N=2000$ random trials with seed $42$.  It required $57377$ distinct-sample attempts and incurred $55377$ duplicate retries.  Among those trials, there was $1$ exact shape match and $25$ matches at Jaccard similarity at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9} &= 0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null row also used $N=2000$ and seed $42$.  It reports observed $M$ size $20$, exact-match count $0$, and lower-tail count $7$ at or below the observed $M$ size.  The corresponding values are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
These values support the narrow statement that the finite shape is unusual under the recorded null procedures.  They do not identify a biochemical cause.

\paragraph{Codon-level recovery and module coverage.}
The leave-one-codon-out row reports anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$.  The module classification is complete for the $13$ residual codons: assigned count is $13$, with no missing codons, no unassigned codons, and no extra module codons.  The modules are
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every listed module contains predictable or boundary-supported codons, so the residual set is not merely counted correctly; it is partitioned into named finite modules without leftover entries.

\paragraph{Code-family readback.}
The by-family leave-one-code-out row contains $5$ families.  Its hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate.  The weaker alternative-yeast rate marks the only family-level departure from perfect above-baseline recovery in this row.  The result is therefore a family-indexed readback regularity over finite code tables, not an assertion that all organisms instantiate the same mechanism.

\paragraph{Local verdict.}
The row supports a code-layer claim: window-six coordinates expose a local boundary-tile regularity in curated codon-label data, and the leave-one-code-out recovery is far above the recorded baseline.  The supporting numerical witnesses are the $13$-codon residual set, the punctured-cube face-vector $(13,16,4)$, the spectral values $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, $\mu=3.051487585298128$, the shape-test probabilities $0.0005$ and $0.0125$, the amino-acid-class null values $0.0$ and $0.0035$, and the $23$-table, $63$-target leave-one-code-out recovery row.  The contact boundary is strict: no translation process, folded protein structure, physical admissibility, biological function, mechanistic explanation, or universality claim follows from this chapter unless an additional contact directly supplies that higher-layer observation.