\origin{ai}

\subsection{Leave-One-Code-Out Boundary Recovery}
\label{subsec:h1-leave-one-code-out-boundary-recovery}

\paragraph{Finite carrier and readback.}
This BioReality record concerns the standard-code codon table read through a six-bit codon cube $Q_6$.  The carrier is finite: each codon occupies one six-bit display point, and the only external reality contact used here is the curated standard-code label table, which assigns each codon to an amino-acid or stop label.  The BEDC-side distinctions are the coordinate display, the local tile face, singleton label corners, and three-point label classes.  The readback layer is therefore only $\mathrm{code\_read}$: it can compare codon labels and local boundary tiles, but it does not observe translation realization, protein folding, physical admissibility, biological function, mechanism, or universality.

\paragraph{The punctured boundary row.}
The finite boundary row $R$ has cardinality $13$.  The observed row is
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row is verified as a punctured-cube pattern with $f$-vector
$$
\begin{aligned}
f(R)=(13,16,4).
\end{aligned}
$$
Thus the datum is not merely a list of exceptional labels: it is a finite local shape in the six-bit cube.  The row still remains a code-layer object, because the contact supplying the labels is a curated table rather than a biochemical mechanism.

\paragraph{Spectral row signature.}
The row also carries a finite spectral signature.  The recorded values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial polynomial residual is $-6.224354365258478\times 10^{-12}$, so the cubic relation is satisfied to the recorded numerical tolerance.  This spectral evidence supports the claim that the observed row is organized in the coordinate closure, spectrum, and relation structure of the window-six display.  It does not promote the local tile to a law of biology.

\paragraph{Shape significance against finite null rows.}
Against the recorded finite-row null sample with $N=2000$, the exact shape match count is $1$, giving
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{J\ge 0.9} &= 0.0125.
\end{aligned}
$$
The run required $57377$ distinct sample attempts and $55377$ duplicate retries under seed $42$.  The amino-acid-class-preserving null is sharper: with $N=2000$, exact shape matches are $0$, the lower-tail count at the observed size is $7$, the observed $M$ size is $20$, and the recorded probabilities are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
These numbers support a finite-row anomaly claim for the code-read layer.  They do not support a claim about folding, function, physical feasibility, or a universal generating mechanism.

\paragraph{Leave-one-code-out recovery.}
The target claim $\mathrm{h1.leave.one.code.out}$ evaluates $23$ code tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$.  The observed hit rates are
$$
\begin{aligned}
\mathrm{hit\_rate\_above\_baseline} &= 0.9841269841269841,\\
\mathrm{hit\_rate\_in\_M\setminus T} &= 0.9365079365079365,\\
\mathrm{hit\_rate\_in\_M\setminus T\ plus\ boundary} &= 0.9841269841269841.
\end{aligned}
$$
Thus the leave-one-code-out probe says that when the code table is varied within the tested family, the local window-six tile almost always recovers the held-out code-read target above the baseline.  This is the main finite-row support for the conjecture: the six-bit coordinate geometry is informative for label-table boundary recovery.

\paragraph{Family-level readback.}
The family analysis separates the $23$ tables into $5$ families.  The hit-rate-above-baseline values are
$$
\begin{aligned}
\mathrm{alternative\_yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\_plastid} &= 1.0,\\
\mathrm{candidate\_other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\_ciliate} &= 1.0.
\end{aligned}
$$
The weakest listed family is still above the baseline rate, while four of the five listed families attain $1.0$.  This supports a family-stable code-read phenomenon, not a claim that every biological translation context is generated by the same local geometry.

\paragraph{Module classification.}
The module classification exhausts the $13$ codons of $R$: it records $R\_\mathrm{size}=13$, assigned count $13$, no extra module codons, no missing row codons, and no unassigned codons.  The modules are
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every module has a predictable-or-boundary classification.  The leave-one-codon-out companion record reports anchor count $0$, boundary count $5$, closure coverage high $8$, predictable count $8$, and recovery rate $1.0$.  These figures show that the row is internally organized into finite modules compatible with the local boundary probe.

\paragraph{Boundary verdict.}
The conjecture is ready at the code-read layer.  The finite evidence supports the statement that window-six codon coordinates expose local boundary-tile structure in the curated standard-code label table.  The same evidence also fixes the cannot-claim boundary: no translation realization, folded protein structure, structural order, physical admissibility, biological function, universal mechanism, or biological necessity follows from these rows.  Amino-acid label-sequence readback is not folded structure.  Any promotion beyond $\mathrm{code\_read}$ requires a separate reality contact that directly observes the higher layer.