\origin{ai}
\subsection{Three-axis insufficiency for the powered $B^{\ast}_{Q6}$ residual surface}
\label{sec:bior-reality-q6-three-axis-insufficiency}

\paragraph{Finite coordinate surface.}
This record concerns the finite CDS-by-organism residual surface obtained after amino-acid projection, restricted to the $B^{\ast}_{Q6}$ coordinate family and then read against organism-scoped codon usage, tRNA or wobble-weight annotations, and bounded translation-layer rows.  The relevant $f_3$ saturation row is not a symbolic guess: in run $76d3dc408a57$, the set $M={\rm Med}\,R\cup ACA$ is
$\{AAA,AAG,ACA,ACG,AGA,AGG,AUA,AUG,CUA,CUC,CUG,CUU,UAA,UAG,UCA,UCG,UGA,UGG,UUA,UUG\}$,
while $\operatorname{Sat}_{f_3}(R)$ removes exactly $ACA$ and $ACG$, giving
$\{AAA,AAG,AGA,AGG,AUA,AUG,CUA,CUC,CUG,CUU,UAA,UAG,UCA,UCG,UGA,UGG,UUA,UUG\}$.
The recorded $f_3$ boundary is the empty list, so the finite row does not obtain its force from an unobserved boundary residue.  Its stress coordinate is instead the synonymous imbalance that remains after amino-acid quotienting, especially where a context codon $c$ and its third-position partner $c^{f_3}$ need not receive the same wobble or tRNA weight.

\paragraph{Power calculation and decision row.}
The powered three-axis test used $5$ folds, required at least $500$ proteins per organism, computed $11$ organisms out of $16$ requested organisms, and used $41$ synonymous-design columns.  Its deterministic seed was $\texttt{sha256:b\_star\_q6\_optimality\_axis\_sufficiency\_powered:deterministic}$, and the recorded status was passed.  The finite conclusion is
$$
\begin{aligned}
  \operatorname{ThreeAxisSufficiency}(B^{\ast}_{Q6}) &= \text{false at the tested surface},\\
  \operatorname{RecordedCall} &= \texttt{fourth\_mechanism\_present}.
\end{aligned}
$$
The phrase in the row is treated here as a statistical-axis call: after the core tRNA direction, the $f_3$-translation-selection residual direction, and the previously isolated perpendicular direction are included, a further residual component remains detectable in the finite design.  This chapter therefore records insufficiency of the three-axis span, not a biological mechanism by itself.

\paragraph{Relation to the adjacent powered rows.}
The insufficiency call is consistent with the neighboring finite rows.  The two-axis closure test, also on $11$ computed organisms out of $16$ requested organisms with $5$ folds and $41$ synonymous-design columns, called \texttt{third\_axis\_present}: the perpendicular direction had systematically positive held-out $R^2$ and same-signed organism directions.  The third-axis identity row then identified $d_{\perp}$ provisionally with usage frequency within family, with mean cosine $0.128$ and one-sided sign-test $p=0.000488$.  Orthogonalizing against GC did not erase the signal: the third-axis mechanism row called selection rather than GC, not wobble-clustered, and left CSC or positional identity as needing a separate contact.  In the broader per-organism spectrum, the captured spectra were led by $d_{\rm resid4}$ on average, followed by $d_{\rm tRNA}$; the dominant-count table was $\{d_{\rm resid4}:11\}$ over the $11$ computed organisms, while the largest GC3 rank effect reported for $d_{\rm usage}$ was only $\rho=0.136$.  These rows make the three-axis insufficiency call a finite pattern across the powered panel rather than an isolated wording choice.

\paragraph{What the row does and does not identify.}
The auxiliary identity tests narrow what the remaining component is unlikely to be, without turning it into a closed mechanistic claim.  The mRNA-stability identity test passed with the conclusion \texttt{fourth\_axis\_not\_mrna\_stability}; in yeast the elongation-dwell test passed but only called \texttt{partial}; the codon-pair identity test passed with \texttt{not\_codon\_pair} using $50$ shuffle-null replicates; and the translation-accuracy test passed with \texttt{not\_accuracy} using $200$ permutations over $11$ computed organisms out of $16$ requested organisms.  The tRNA-poverty row separately used $1000$ permutations over $40$ computed organisms out of $46$ requested organisms and recorded that $R$ sense codons are systematically tRNA-poor under GtRNAdb tGCN plus dos Reis wobble weights.  Thus the finite picture is: tRNA supply is a real boundary pressure, the first residual optimal direction aligns with $f_3$ translation selection, the third axis is usage-frequency-like and not merely GC, and the three-axis model still leaves a fourth component.  None of those statements names a final biochemical pathway.

\paragraph{Bounded translation readback.}
The only permitted readback for the $f_3$-stress coordinate is a controlled survival entry against a translation-layer row such as tAI, stAI, ribosome occupancy, or translation efficiency.  In symbols, the local quantity has the form
$$
\begin{aligned}
  S_{f_3\text{-stress},T_j}\;\big|\;Z,
  \qquad
  Z &= \{\text{amino-acid composition, organism, GC3, length, M-density, mRNA controls}\}.
\end{aligned}
$$
A positive finite entry must remain non-vacuous after these controls and after simpler codon-baseline ablation.  A rank-degenerate all-$1.0$ surface, especially from a three-organism panel, is a null boundary rather than evidence for survival.  Modeled tAI or stAI weights may serve as bounded codon-adaptation readouts, but they are not direct observations of ribosome occupancy, protein abundance, folding, localization, stability, molecular function, phenotype, or a universal biological rule.

\paragraph{Local no-promotion rule.}
This chapter supports the following finite claim only: in the powered $B^{\ast}_{Q6}$ residual surface, the tested three-axis span is insufficient and a fourth residual component is present in the finite statistical design.  No translation realization, protein abundance effect, folded structure, physical admissibility, function, mechanism, phenotype, localization, stability, or universality follows from this row unless a separate layer-matched contact supplies the corresponding measurement and the controlled statistic is recomputed on joined rows.  The current result is therefore a disciplined axis-insufficiency record: strong enough to require a fourth component in the tested surface, but deliberately too narrow to license downstream biological interpretation.