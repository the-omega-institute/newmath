\origin{ai}

\subsection{Three-axis insufficiency for the $B^*_{Q6}$ optimality surface}
\label{sec:bioreality-q6-three-axis-sufficiency}

\paragraph{Finite coordinate being tested.}
The record concerns a finite synonymous-codon residual surface after amino-acid projection.  The distinguished coordinate is the $f_3$-stress component of the $B^*_{Q6}$ construction, read only through matched translation-layer rows when those rows exist.  In this chapter the word ``survival'' is restricted to a controlled entry $S_{f3\text{-}stress,T_j}$ for a bounded translation readout such as tAI, stAI, ribosome occupancy, or translation efficiency.  The coordinate is not treated as a measurement of protein abundance, folded structure, localization, stability, biological function, phenotype, physical admissibility, mechanism, or universality.

The finite saturation check fixes the relevant third-position boundary.  The set $M=\operatorname{Med}(R)\cup ACA$ is the following 20-codon row:
$$
\begin{aligned}
M=\{&AAA,AAG,ACA,ACG,AGA,AGG,AUA,AUG,CUA,CUC,\\
& CUG,CUU,UAA,UAG,UCA,UCG,UGA,UGG,UUA,UUG\}.
\end{aligned}
$$
After $f_3$ saturation the residual row removes only $ACA$ and $ACG$:
$$
\begin{aligned}
\operatorname{Sat}_{f_3}(R)=\{&AAA,AAG,AGA,AGG,AUA,AUG,CUA,CUC,CUG,CUU,\\
& UAA,UAG,UCA,UCG,UGA,UGG,UUA,UUG\}.
\end{aligned}
$$
The recorded $f_3$ boundary is empty.  Thus the finite codon geometry by itself supplies a closed residual coordinate, but it does not supply a translation experiment.

\paragraph{Powered three-axis test.}
The powered optimality-axis sufficiency assay was run with experiment id $b\_star\_q6\_optimality\_axis\_sufficiency\_powered$ and run id $2436cae9d455$.  It used $5$ folds, required at least $500$ proteins per organism, computed $11$ organisms out of $16$ requested organisms, and used $41$ synonymous design columns.  Its deterministic seed was $\mathrm{sha256}:b\_star\_q6\_optimality\_axis\_sufficiency\_powered:deterministic$.  The recorded status is passed, and the finite conclusion is not that three axes close the system; it is
$$
\begin{aligned}
\text{three-axis sufficiency call} = \text{fourth\_mechanism\_present}.
\end{aligned}
$$
This is the central BioReality content of the row: the first three optimality axes are not sufficient on the powered multi-organism panel.  The result forces a residual mechanism slot at the empirical interface, but it does not identify that slot as any downstream biological process.

\paragraph{Relation to the surrounding finite spectrum.}
The same data block contains several constraints that locate the insufficiency more sharply.  The per-organism axis-strength spectrum used the same $5$-fold, $500$-protein, $11/16$ organism, $41$-column design and reported that captured spectra are led by $d_{resid4}$ on average, followed by $d_{tRNA}$.  The dominant-count summary is $\{d_{resid4}:11\}$, so every computed organism in that assay places the fourth residual direction first.  Domain means are explicitly descriptive because the sample is imbalanced, with $n=1$ archaeal, $n=2$ bacterial, and $n=8$ eukaryotic organisms.  The largest GC3 rank effect in that row is small, $\rho=0.136$ for $d_{usage}$, so the recorded spectrum is not reduced to a GC3 ranking statement.

The two-axis closure assay also used $5$ folds, $11/16$ organisms, and $41$ synonymous design columns, and its finite conclusion is $\text{third\_axis\_present}$.  Its reason is that $d_{\perp}$ has systematically positive held-out $R^2$ and that the $d_{\perp}$ directions are mutually same-signed across organisms.  The third-axis identity row then identifies $d_{\perp}$ provisionally with usage frequency within family, with mean cosine $0.128$ and one-sided sign-test $p=0.000488$.  The mechanism row for this third axis records the finite interpretation $\text{selection-not-GC; not-wobble-clustered}$, with CSC or positional identity still requiring an additional contact.  Together these rows show a layered pattern: a tRNA-related core, an $f_3$ translation-selection residual, a third usage-frequency axis, and then a remaining fourth component.

\paragraph{Mechanism exclusions for the fourth component.}
The fourth component is not left completely unconstrained.  The mRNA-stability row has status passed and concludes $\text{fourth\_axis\_not\_mrna\_stability}$ because yeast $d_{opt\_halflife}$ has low alignment with abundance $d_{resid4}$ under the fixed gates.  The codon-pair row has status passed and concludes $\text{not\_codon\_pair}$ with $5$ folds, $500$ proteins per organism, $41$ synonymous design columns, and $50$ shuffle-null replicates.  The translation-accuracy row has status passed and concludes $\text{not\_accuracy}$ using $11/16$ organisms, $200$ permutations, and the same $41$-column synonymous design.  The elongation-dwell row is narrower: for \emph{Saccharomyces cerevisiae} it concludes $\text{partial}$ rather than full identification.  These rows make the fourth mechanism present but not yet named as mRNA stability, codon-pair structure, translation accuracy, or a fully resolved dwell-time mechanism.

\paragraph{Translation-readout gate.}
The $f_3$-stress coordinate becomes a translation-layer claim only if the context weights break the $f_3$ pairing.  In finite terms the relevant asymmetry is $w_c\ne w_{c^{f_3}}$.  A modelled tAI or stAI surface may provide a bounded codon-adaptation row, and a ribosome-profiling or translation-efficiency surface may provide a measured row, but neither contact is interchangeable with the other.  The admissible support condition is a nondegenerate controlled entry
$$
\begin{aligned}
S_{f3\text{-}stress,T_j} > 0
\end{aligned}
$$
after controls for amino-acid composition, organism, GC3, CDS length, $M$-density, matched mRNA abundance where used, rank sufficiency, and simpler codon baselines.  A three-organism all-$1.0$ matrix is therefore a saturated boundary, not a survival result.  Likewise, codon topology and modelled adaptation weights alone do not establish the sign, magnitude, or general direction of a translation effect.

\paragraph{Local no-promotion rule.}
The chapter supports a finite statement about optimality-axis insufficiency: on the powered $B^*_{Q6}$ synonymous residual design, the three-axis model leaves a fourth component, and the surrounding assays constrain several candidate identities.  It does not support a claim that $f_3$-stress changes protein abundance, folded structure, localization, stability, molecular function, organismal phenotype, physical admissibility, or a universal law.  Those interpretations require their own layer-matched measurements and their own controlled statistics.  Until same-organism and same-CDS codon, wobble-weight, translation-readout, and mRNA-control rows are joined and the declared $S_{f3\text{-}stress,T_j}$ statistic is computed, the present record remains a bounded coordinate-and-readout boundary rather than a downstream biological mechanism.