\origin{ai}
\subsection{Positional dwell localization of the $B^*_{Q6}$ $f_3$ abundance component}
\label{subsec:bioreality-f3-stress-dwell-positional-ramp-body}

\paragraph{Finite row surface.}
The record is a finite cross-layer test on the \emph{Saccharomyces cerevisiae} matched panel.  The tested coordinate is the one-dimensional $f_3\_stress$ direction inside the nine-coordinate $B^*_{Q6}$ residual basis, after residualizing codon-derived coordinates and protein abundance against the shared covariate basis.  The active target is the projection $P_{f3}$ of $Z$-residualized $\log_{10}$ protein abundance onto the $Z$-residualized $f_3\_stress$ direction on the joined base plus positional-dwell rows.  The run identifier is \texttt{6db78e1bf746}; the finite test status is \texttt{passed}.  The joined positional panel has $n=1257$ rows, while the base design was capped at $1800$ rows.  The covariate basis has rank $24$, the tested $f_3$ source has rank $1$, and the cross-validation design used $5$ folds.  The finite target component has
$$
\begin{aligned}
\|P_{f3}\|^2 &= 44.20095750389323,\\
\frac{\|P_{f3}\|^2}{\|P_{\mathrm{target}}\|^2} &= 0.1999063916718794.
\end{aligned}
$$
Thus the positional experiment is not testing total protein abundance directly; it tests whether the already isolated $f_3\_stress$ abundance component is better predicted by positional ribosome-occupancy dwell summaries than by the base controls alone.

\paragraph{Readout definition.}
The dwell readout consists of per-gene relative ribosome-occupancy summaries computed separately from file-provided $5'$ head and $3'$ tail A-site occupancy windows.  The null model is deterministic row-label permutation of $P_{f3}$ with the base and full designs, folds, and segment choice held fixed; it is run separately for each segment and for the joint segment design.  The permutation count is $120$, with deterministic seed \texttt{sha256:b\_star\_q6\_f3\_stress\_dwell\_positional\_powered:deterministic}.  The observed classification is positional rather than symmetric: the leading segment by held-out increment is \texttt{5prime}, the dominant segment is \texttt{5prime}, $5'$ ramp passage is true, and $3'$ body passage is false.  In the finite readback, the segment label therefore records a localized dwell association at the head-window side of the coding sequence, not a general body-wide ribosome-occupancy effect.

\paragraph{Relation to the non-positional dwell test.}
The companion dwell mediation calculation on the same yeast context gives the scale of the non-positional signal.  In that run, $n=1257$ joined rows were again used from a base cap of $1800$, with $5$ folds, $120$ deterministic permutations, $\operatorname{rank}(f_3)=1$, and $\operatorname{rank}(Z)=24$.  The base model had held-out $R^2=0.1855578757644517$, the dwell-only readout had $R^2=0.034336580953121976$, and the full model had $R^2=0.20412261520502883$.  The held-out dwell increment was
$$
\begin{aligned}
\Delta R^2_{\mathrm{dwell}} &= 0.01856473944057712,\\
\operatorname{Null}_{95} &= 0.0013115362189692937,\\
\rho(f_3,\mathrm{dwell}) &= 0.1615441450901653,\\
p_{\mathrm{approx}} &= 7.873916057196935\times 10^{-9}.
\end{aligned}
$$
These numbers show that the positional record is refining a real dwell increment rather than trying to discover dwell relevance from scratch.  The positional run asks where that increment is concentrated, and the finite answer is the $5'$ ramp-side window.

\paragraph{Ramp and body comparison.}
A related structural-order ramp/body assay gives an independent finite check on the same positional vocabulary.  For the primary $50$-codon ramp window, the weighted $f_3$ ramp increment was $0.0011165640103326878$, while the weighted $f_3$ body increment was $-0.00026157313923957926$.  The paired ramp-minus-body increment was $0.001378137149572267$.  The sign-flip probability for positive ramp signal was $0.0478515625$, the body probability was $0.54296875$, and the paired ramp-minus-body probability was $0.05859375$.  At window sizes $30$, $50$, and $70$, the weighted ramp increments were $0.0007864264335388174$, $0.0011165640103326878$, and $0.0007157308766185055$, respectively, while the corresponding body increments were $0.00023737813475316197$, $-0.00026157313923957926$, and $-0.0008384704154638841$.  That related assay concluded \texttt{ramp\_positive\_but\_not\_localized}: the ramp signal is positive, but the stricter paired localization gate does not pass there.  The present dwell-position record is narrower: it does pass the $5'$ positional dwell gate and does not pass the $3'$ body gate.

\paragraph{Dictionary and reduction context.}
The readout-dictionary certificate on the same yeast abundance context gives the broader reduction scale for the $B^*_{Q6}$ protein-abundance residual.  It used $n=1800$ joined rows from $4758$ available rows, $5$ folds, $120$ permutations, $\lambda_{\mathrm{DL}}=0.01$, and $\rho_{\mathrm{join}}=0.05$.  Its best selected measured-readout dictionary covered $C^*=0.24538211439730545$ of the residual signal, with selection-aware null $95$th percentile $0.029149007242956115$, leaving unexplained fraction $0.7546178856026946$.  The positional dwell result should be read inside that scale: the $5'$ dwell window carries a detectable component of the $f_3\_stress$ abundance projection, while most of the larger residual dictionary remains outside this single positional readout.

\paragraph{Finite verdict.}
The finite conclusion is that the $f_3\_stress$ component of the controlled yeast protein-abundance residual has a positionally localized dwell increment, with the leading and dominant segment at the $5'$ head/ramp side and with no corresponding $3'$ body passage.  The evidence is a rank-sufficient, fold-held, permutation-bounded finite matrix result: $n=1257$, $5$ folds, $120$ permutations, $\operatorname{rank}(f_3)=1$, and $\operatorname{rank}(Z)=24$.  The claim is therefore a positional readout statement about a measured ribosome-occupancy dwell summary absorbing part of a controlled protein-abundance projection.

\paragraph{Local no-promotion boundary.}
This chapter does not infer translation realization in general, folded structural order, physical admissibility, molecular function, pathway mechanism, organismal phenotype, evolutionary direction, or universality from the positional dwell association.  The result is observational and finite-row: it reports that a $5'$ positional dwell readout improves held-out prediction of the isolated $P_{f3}$ component beyond the base design and beyond its fixed-row permutation null.  Any stronger claim about translation mechanism, folding, localization, protein stability, biological function, phenotype, or causal law would require a separate layer-matched measurement and its own controlled statistic.