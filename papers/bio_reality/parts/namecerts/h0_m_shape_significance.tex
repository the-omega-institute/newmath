\origin{ai}
\subsection{Window-six codon boundary tile}
\label{sec:codon-window-six-boundary-tile}

\paragraph{Finite carrier and readback contact.}
The record is a finite code-read experiment on the six-bit codon cube $Q_6$.  The carrier is not a biological mechanism; it is a coordinate display in which each standard-code codon is read as a vertex and the curated standard genetic-code table supplies the external label contact.  The contact is therefore exactly a codon-label readback contact: it assigns each codon to an amino-acid or stop label, and it does not observe translation realization, folded structure, physical admissibility, function, mechanism, or universality.  The local object under study is a boundary tile in this six-bit display, with distinctions among coordinate, closure, spectrum, and relation data.  The finite exceptional relation $R$ has cardinality
$$
\begin{aligned}
|R| &= 13,
\end{aligned}
$$
and the thirteen codons are
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
As a finite cubical subobject, this same row satisfies the punctured-cube face count
$$
\begin{aligned}
f(R)=(13,16,4),
\end{aligned}
$$
so the coordinate claim is not merely that thirteen labels were selected.  It is that the selected codons occupy a specific local tile shape in $Q_6$.

\paragraph{Shape significance.}
The primary finite-row test sampled $N=2000$ distinct comparison shapes with seed $42$.  Obtaining those distinct samples required $57377$ sample attempts and $55377$ duplicate retries, which records the finite enumeration pressure of the null draw rather than hiding it.  Among the $2000$ distinct samples, exactly one sample matched the observed shape exactly, and $25$ samples had Jaccard overlap at least $0.9$ with the observed shape.  Thus the empirical shape probabilities were
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{J\geq 0.9} &= 0.0125.
\end{aligned}
$$
The exact-shape row therefore supports a sharp finite anomaly at the code-read layer.  The high-Jaccard row supports a weaker but still small-tail statement: near matches occur more often than exact matches, but still only in $25$ of $2000$ distinct samples.  No stronger biological conclusion is licensed by this row.  The significant object is a local codon-tile shape exposed by window-six coordinates against the curated standard-code labels.

\paragraph{Class-preserving null comparison.}
A second null model preserved amino-acid class structure.  In that model, again with $N=2000$ and seed $42$, the observed $M$-size was $20$.  The exact class-preserving shape match count was $0$, giving
$$
\begin{aligned}
p_{\mathrm{class,exact}} &= 0.0.
\end{aligned}
$$
The lower-tail count at or below the observed $M$-size was $7$, giving
$$
\begin{aligned}
p_{\mathrm{class,lower}} &= 0.0035.
\end{aligned}
$$
This comparison is important because it keeps label-class information in the null rather than replacing the biology contact by an unconstrained random table.  Even under that more constrained finite comparison, the observed local tile remains a small-tail event.  The result is still a code-layer statement: it concerns codon-label geometry under a class-preserving finite null, not the production of proteins or the physical viability of any peptide.

\paragraph{Spectral row.}
The same finite relation has a spectral signature.  The recorded values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial polynomial residual was
$$
\begin{aligned}
-6.224354365258478\cdot 10^{-12},
\end{aligned}
$$
with the same value reported for the cubic-polynomial check.  This is a numerical finite-row certificate that the displayed coordinate object has a stable spectral readback within tolerance.  It does not turn the coordinate object into a law of molecular biology.  It says that the codon tile, when encoded in this relation and spectrum pipeline, has a reproducible numerical signature.

\paragraph{Leave-one readback tests.}
The leave-one-code-out experiment evaluated $23$ tables and $63$ total targets.  Its baseline hit rate was
$$
\begin{aligned}
0.19821739130434785,
\end{aligned}
$$
whereas the observed hit rate above baseline was
$$
\begin{aligned}
0.9841269841269841.
\end{aligned}
$$
The hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary was $0.9841269841269841$.  Family-level leave-one-code-out readback split the result into five families.  The alternative-yeast family had hit rate $0.8333333333333334$ above baseline, while the bacterial-plastid, candidate-other, mitochondrial, and nuclear-ciliate families each had hit rate $1.0$ above baseline.  These rows say that the local tile is useful for label recovery under finite code-table perturbations.  They do not say that the tile realizes translation or explains why any organism uses a code.

\paragraph{Leave-one-codon module structure.}
The leave-one-codon-out row recorded no anchor codons, $5$ boundary codons, $8$ high-coverage closure cases, $8$ predictable cases, and recovery rate $1.0$.  The module classification accounted for every codon in $R$ with no missing codons, no extra module codons, and no unassigned codons.  The complete module split was
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The assigned count was $13$, matching $|R|=13$.  This is the finite module evidence behind the local boundary interpretation: every relation codon is classified into a named module, and each module has predictable or boundary support.  The row is local and finite.  It does not infer folded structure from amino-acid labels, and it does not infer function from membership in a module.

\paragraph{Local boundary verdict.}
The finite data support the following chapter-level statement.  In the six-bit codon display $Q_6$, the curated standard-code label table exposes a local boundary tile whose relation has $13$ codons, punctured-cube face vector $(13,16,4)$, small exact-shape probability $0.0005$, small high-Jaccard probability $0.0125$, class-preserving lower-tail probability $0.0035$, stable spectral values $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$, and strong leave-one readback behavior across code tables and codons.  The cannot-claim boundary is part of the result: no translation, folding, physical admissibility, biological function, universal mechanism, or global biological law follows from these rows unless a separate reality contact supplies that higher-layer evidence.  The chapter therefore records a code-read anomaly with a precise local boundary, not a promotion of window-six geometry into molecular mechanism.