\origin{ai}

\subsection{Window-Six Local Tile Significance for the Standard Code}
\label{sec:biorereality-window-six-local-tile-significance}

\paragraph{Finite carrier and reality contact.}
The finite carrier is the six-bit codon cube $Q_6$, read as the $64$ codon displays over the alphabet $\{A,C,G,U\}$ after the usual two-bit encoding of each nucleotide position. The external contact used here is only the curated standard-code label table: it assigns each codon an amino-acid or stop label. Thus the tested layer is code readback, not translation as a biochemical process. The BEDC-side distinctions are the coordinate display, the closure operation on a local tile face, the associated spectrum, and the relation between singleton label corners and three-point label classes. The readback object is the standard-code label table itself.

\paragraph{Observed boundary row.}
For the boundary row $R$, the verified finite set has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same row is verified as a punctured-cube shape with face vector
$$
\begin{aligned}
f(R)=(13,16,4).
\end{aligned}
$$
This is a finite combinatorial statement about the row selected by the window-six coordinate construction. It does not by itself assert that the codon table is forced by geometry, nor that the displayed tile is a universal biological law.

\paragraph{Shape significance.}
The primary finite-row test for the local tile used $N=2000$ sampled comparison rows with seed $42$. Because distinct comparison rows were required, the sampler made $57377$ distinct-sample attempts and encountered $55377$ duplicate retries. Among these comparisons there was $1$ exact shape match and $25$ rows with Jaccard similarity at least $0.9$. Hence the recorded empirical probabilities are
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{J\geq 0.9} &= 0.0125.
\end{aligned}
$$
The exact-shape significance value and the high-Jaccard significance value are the same two finite-row values, $0.0005$ and $0.0125$. Under this sampled null, the observed local tile is therefore rare as an exact combinatorial shape and still uncommon under the high-overlap relaxation.

\paragraph{Class-preserving null.}
A second null keeps amino-acid class information fixed while resampling the shape. In that null, again with $N=2000$ and seed $42$, the observed $M$-row size is $20$. The exact match count is $0$, giving
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}=0.
\end{aligned}
$$
The lower-tail count at or below the observed $M$ size is $7$, hence
$$
\begin{aligned}
p_{\mathrm{aa\ class\ lower\ tail}}=0.0035.
\end{aligned}
$$
This class-preserving test separates label-class preservation from geometric coincidence: even after preserving the amino-acid-class constraint, the observed row size and shape remain finite-row exceptional in the sampled comparison family.

\paragraph{Spectral row data.}
The associated spectral values are recorded as
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial polynomial check gives
$$
\begin{aligned}
P_{\mathrm{radial}}(\lambda_M)=-6.224354365258478\times 10^{-12},
\end{aligned}
$$
which is also the recorded cubic-polynomial residual. This residual is numerically at tolerance scale for the finite spectral calculation, and the spectral-radius entry is $0.6752479308830213$. The spectrum is therefore part of the finite coordinate evidence, not an additional biological mechanism.

\paragraph{Leave-one-code-out readback.}
The code-level stability probe evaluated $23$ tables and $63$ total targets. Its baseline hit rate is
$$
\begin{aligned}
0.19821739130434785.
\end{aligned}
$$
The hit rate inside $M\setminus T$ is
$$
\begin{aligned}
0.9365079365079365,
\end{aligned}
$$
while the hit rate inside $M\setminus T$ plus the boundary is
$$
\begin{aligned}
0.9841269841269841.
\end{aligned}
$$
The recorded hit rate above baseline is also $0.9841269841269841$. By family, the five recorded family hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
These numbers support the code-read layer: when a code-table entry is held out, the local tile and its boundary retain strong label-recovery behavior relative to the baseline.

\paragraph{Leave-one-codon-out and module partition.}
The codon-level probe records anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$. The recovery-rate threshold check is also $1.0$. The module classification assigns all $13$ row codons, with $R$ size $13$, assigned count $13$, no extra module codons, no missing row codons, and no unassigned codons. The finite module partition is
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
Every listed module has either a predictable or boundary status under the probe. This completes the finite row accounting for the displayed boundary set.

\paragraph{Local conclusion and boundary of claim.}
The finite data support the following bounded claim: window-six codon coordinates expose a statistically rare local boundary-tile structure whose row $R$ has $13$ named codons, punctured-cube face vector $(13,16,4)$, significant sampled exact-shape probability $0.0005$, significant sampled high-Jaccard probability $0.0125$, and strong code-label recovery behavior in leave-one-code-out and leave-one-codon-out probes. The curated standard-code table supplies the label contact needed for code readback. No stronger biological conclusion is licensed here. In particular, no translation realization, folded structure, structural order, physical admissibility, biological function, universal mechanism, or biological necessity follows from this tile unless a separate higher-layer reality contact supplies that information.