\subsection{Window-six codon tile significance}\label{sec:h0-m-shape-significance}
\origin{ai}

\paragraph{Finite carrier and reality contact.}
The record is a vision-level NameCert for a finite code-read claim on the six-bit codon cube $Q_6$.  The carrier is the codon display with sixty-four codon positions, and the only external contact used here is the curated standard-code table assigning each codon to an amino-acid or stop label.  This contact has codon-label resolution only.  It supports readback of the standard-code label table and local split checks inside the coordinate cube, but it does not observe translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.

\paragraph{Observed boundary row.}
The boundary row $R$ has cardinality $13$.  The observed codons are
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The punctured-cube check gives the same thirteen codons and records the finite face data
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the local object is not merely a count: it has a concrete boundary-tile shape inside the window-six coordinate presentation.  The row is still a code-layer object.  Its elements are codons with curated labels, not realized peptide events or structural states.

\paragraph{Shape significance.}
The primary randomization run used seed $42$ and $N=2000$ accepted samples.  Because the finite space contains many duplicate draws under the sampling procedure, the run also records $57377$ distinct-sample attempts and $55377$ duplicate retries.  Among the accepted samples there was $1$ exact shape match and $25$ samples with Jaccard score at least $0.9$.  The resulting empirical tail values are
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9}&=0.0125.
\end{aligned}
$$
These numbers make the local window-six tile statistically non-generic under the stated finite-row null experiment.  The result is a finite coordinate significance statement: the observed standard-code boundary shape is rare among the sampled alternatives.  It is not a claim that biological DNA obeys this geometry as a necessity, nor that the local tile is a global biological law.

\paragraph{Amino-acid-class preserving null.}
A second null model preserves amino-acid class information and again uses seed $42$ with $N=2000$.  In that run the observed label-side object has size $20$, the exact shape count is $0$, and the lower-tail count at or below the observed $M$ size is $7$.  The reported values are
$$
\begin{aligned}
p_{\mathrm{class\ exact}}&=0.0,\\
p_{\mathrm{class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
The class-preserving null therefore does not erase the finite-row signal.  It sharpens the reading: the claim is not that arbitrary relabeling produces the observed shape, but that the curated standard-code readback occupies a rare local configuration even when the null retains amino-acid-class structure.

\paragraph{Spectral readback.}
The spectrum check supplies a second internal diagnostic for the same finite shape.  The recorded values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial polynomial residual is $-6.224354365258478\times 10^{-12}$, and the same value is recorded for the cubic-polynomial check.  This residual is numerically negligible at the stated precision and supports the finite spectral consistency of the coordinate object.  The spectral row is an internal coordinate witness; it does not add a biochemical mechanism.

\paragraph{Leave-one-code and leave-one-codon probes.}
The leave-one-code-out probe evaluated $23$ tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$.  The observed hit rate above baseline is $0.9841269841269841$, the hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$.  These figures show that the local boundary information is predictive for code-layer held-out targets in this finite experiment.

The leave-one-codon-out probe reports $0$ anchor codons, $5$ boundary codons, $8$ high-closure-coverage cases, $8$ predictable cases, and recovery rate $1.0$.  The recovery-rate threshold check is also $1.0$.  This is again a code-read recovery statement.  The recovered item is a codon-label relation inside the finite standard-code table, not a translation event in a cellular system.

\paragraph{Family-level and module-level structure.}
The family-level code-out analysis groups the evidence into $5$ families.  The hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\_yeast}&:0.8333333333333334,\\
\mathrm{bacterial\_plastid}&:1.0,\\
\mathrm{candidate\_other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\_ciliate}&:1.0.
\end{aligned}
$$
The module classification is complete on the thirteen-element row $R$: assigned count is $13$, there are no extra module codons, no missing row codons, and no unassigned codons.  The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every listed module has a predictable-or-boundary witness in the finite-row classification.  This module statement is useful because it localizes the signal to interpretable codon families rather than leaving the result as a single aggregate score.

\paragraph{Local verdict.}
The finite data support the following bounded conclusion.  In the window-six codon cube $Q_6$, the curated standard-code label table exposes a rare local boundary-tile configuration with row $R$ of size $13$, punctured-cube face vector $(13,16,4)$, empirical exact-shape value $0.0005$, high-Jaccard value $0.0125$, class-preserving lower-tail value $0.0035$, and stable leave-one recovery behavior.  The conclusion is restricted to code-read geometry and finite label readback.  No translation, folding, physical admissibility, biological function, mechanism, or universality follows unless a separate reality contact supplies that higher-layer observation.