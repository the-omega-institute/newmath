\subsection{Window-six codon tile significance}\label{subsec:bioreality-h0-m-shape-significance}
\origin{ai}

\paragraph{Coordinate object.}
This record concerns the standard-code codon table as a code-layer object on the six-bit codon cube $Q_6$.  The finite display has four named distinctions: the six-bit codon coordinate, a local tile face, singleton label corners, and three-point label classes.  The intended readback is only the curated codon-to-label table: each codon is read as an amino-acid or stop label under the standard genetic code.  The internal data used here are coordinates, closure, spectrum, and relations on the finite codon display.  The biological contact is therefore deliberately narrow: it tests whether a local codon-label pattern is present in the code table, not whether translation, folded structure, physical admissibility, biological function, mechanism, or universality has been established.

\paragraph{Boundary set.}
The verified boundary set $R$ has cardinality $13$.  Its codons are \texttt{AAA}, \texttt{AGA}, \texttt{AGG}, \texttt{AUA}, \texttt{CUA}, \texttt{CUC}, \texttt{CUG}, \texttt{CUU}, \texttt{UAA}, \texttt{UAG}, \texttt{UCA}, \texttt{UGA}, and \texttt{UUA}.  The same row also verifies that this set is the punctured-cube boundary shape with face-vector $(13,16,4)$.  Thus the finite object is not merely a count: it carries a local cubical incidence signature.

$$\begin{aligned}
|R| &= 13,\\
f(R) &= (13,16,4).
\end{aligned}$$

The module classification accounts for all thirteen codons without surplus or omission.  The six modules are \texttt{agr} $=\{\texttt{AGA},\texttt{AGG}\}$, \texttt{ar\_lys} $=\{\texttt{AAA}\}$, \texttt{cun} $=\{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\}$, \texttt{ile\_met} $=\{\texttt{AUA}\}$, \texttt{ssn} $=\{\texttt{UCA},\texttt{UUA}\}$, and \texttt{stop\_trp} $=\{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}$.  The assigned count is $13$, the reported $R$ size is $13$, and both the extra-module and missing-module lists are empty.

\paragraph{Spectral signature.}
The row-level spectral calculation separates the local tile from a purely combinatorial name.  The verified spectral scalars are $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The radial cubic residual is numerically $-6.224354365258478\times 10^{-12}$, the same value reported for the cubic polynomial check, so the finite floating-point verification is at numerical zero at the recorded precision.

$$\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128,\\
P_{\mathrm{radial}}(\lambda_M,\lambda_R,\mu) &\approx -6.224354365258478\times 10^{-12}.
\end{aligned}$$

This does not convert the code table into a physical law.  It says only that the finite codon tile has a reproducible spectral profile inside the tested coordinate presentation.

\paragraph{Shape significance.}
The primary finite-row significance test used $N=2000$ sampled null instances with seed $42$.  The run needed $57377$ distinct-sample attempts and recorded $55377$ duplicate retries.  There was $1$ exact shape match, giving $p_{\mathrm{exact}}=0.0005$.  There were $25$ matches with Jaccard similarity at least $0.9$, giving $p_{J\geq 0.9}=0.0125$.  The row records both values as significant in the tested finite model.

$$\begin{aligned}
N &= 2000,\\
\#\{\mathrm{exact\ matches}\} &= 1,\\
p_{\mathrm{exact}} &= 0.0005,\\
\#\{J\geq 0.9\} &= 25,\\
p_{J\geq 0.9} &= 0.0125.
\end{aligned}$$

The amino-acid-class-preserving null model gives a second control.  In that run, again with $N=2000$ and seed $42$, the observed $M$ size is $20$, the exact-shape count is $0$, and the lower-tail count at or below the observed $M$ size is $7$.  The reported exact-shape probability is $0.0$, while the lower-tail probability is $0.0035$.  This supports the finite claim that the observed code-layer tile is atypical under the tested label-class-preserving reshufflings.

\paragraph{Leave-one tests.}
The leave-one-code-out row evaluated $23$ tables and $63$ targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M\setminus T$ is $0.9365079365079365$, and the rate after adjoining the boundary is $0.9841269841269841$.  By family, the above-baseline rates are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial/plastid, $1.0$ for candidate-other, $1.0$ for mitochondrial, and $1.0$ for nuclear-ciliate, across $5$ families.

The leave-one-codon-out row reports no anchors, $5$ boundary codons, $8$ predictable codons, $8$ high-coverage closure cases, and recovery rate $1.0$.  The thresholded recovery row is also $1.0$.  These numbers support a local robustness reading: the coordinate-and-boundary packet recovers held-out code labels at high rate in these finite tests.

\paragraph{Local verdict.}
The finite record is therefore ready at the code-read layer.  The standard-code label table supplies a curated contact for codon labels, and the tested finite coordinate object exhibits a thirteen-codon punctured-cube boundary, a stable module accounting, a near-zero spectral residual, significant shape rarity under the sampled null, and strong leave-one recovery.  The result remains local.  Nothing in these rows observes ribosomal translation dynamics, protein folding, structural order, chemical admissibility, phenotype, biological function, cross-organism necessity, or a universal mechanism.  Any such stronger statement would require a separate contact whose data directly measure that higher layer.