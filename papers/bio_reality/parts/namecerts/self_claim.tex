\origin{ai}
\subsection{Self-Check Boundary for ORF Eligibility}
\label{sec:bioreality-self-claim-orf-eligibility-boundary}

\paragraph{Finite record.}
This BioReality record concerns open-reading-frame eligibility as a start--stop enumeration problem.  The object under inspection is not a translated gene product and not a molecular mechanism.  It is the weaker question of whether a finite nucleotide interval has the codon-boundary form that would make it eligible for an ORF annotation after an independent biological contact is supplied.  The self-check row records that the present packet is a boundary audit: its checked gate count is $3$, and its recorded boundary sentence is that no codon geometry is promoted across biological layers.  Thus the row is allowed to discuss finite codon sets, window classifications, and curated interval contacts, but it is not allowed to infer translation, folding, physical admissibility, function, mechanism, or universality from those data alone.

\paragraph{Codon geometry available to the row.}
The row carries a finite codon layer inherited from the verified motif and marker computations.  The motif-compression fact identifies the set $R$ with exactly the following thirteen codons:
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The marker set $M$ is verified to have cardinality $20$ and to equal the union of the reported $WNR$ and $CUN$ families:
$$
\begin{aligned}
M=\{&AAA, AAG, ACA, ACG, AGA, AGG, AUA, AUG, CUA, CUC,\
&CUG, CUU, UAA, UAG, UCA, UCG, UGA, UGG, UUA, UUG\}.
\end{aligned}
$$
A separate saturation computation reports that $Sat_{f3}(R)$ is $M$ without $ACA$ and $ACG$, hence has eighteen listed members, and the recorded $f3$ boundary residue is empty.  The same finite geometry has quotient data: the $M$ face vector is $(20,38,26,8,1)$, the quotient has size $10$, and the quotient face vector is $(10,14,6,1)$.  These numbers describe the finite combinatorial coordinate system used by the row; they do not themselves observe a biological interval.

\paragraph{Start--stop enumeration as a lower-layer test.}
The start--stop window fact reports one loaded external nucleotide-window manifest with $1$ input window and $1$ classified window.  Its scope is explicitly limited to external window start--stop enumeration, with snapshot date $2026$-$05$-$25$.  The classification label is $orf\_eligibility\_only$, so the row treats this as a narrow eligibility predicate over a finite window record.  The enumeration can say that a supplied window has the required start and stop boundary pattern; it cannot say that the interval is expressed, translated, stable as a folded product, physically realized, functional, or generally lawful across organisms.

\paragraph{Curated interval contact.}
The stronger contact row uses a curated interval dataset with $168$ rows from NCBI RefSeq GenBank XML CDS intervals for the Escherichia coli K-12 MG1655 complete genome accession $NC\_000913.3$, with snapshot date $2026$-$05$-$30$.  The input classes present are $boundary\_case$, $matched\_non\_orf\_decoy$, and $positive\_curated\_orf$.  The protocol was frozen before the external observation: the protocol time is $2026$-$05$-$29T19:11:13+00:00$, while the external evidence was observed at $2026$-$05$-$30T10:29:31+00:00$.  The classifier identifier is $orf\_start\_stop\_window\_eligibility$, and the row records that the classifier rules were not modified.

\paragraph{Finite statistical checks.}
Within the curated interval contact, the positive class has $72$ accepted intervals out of $72$, giving accept rate $1.0$ and one-sided binomial upper-tail value $2.117582368135751\times 10^{-22}$.  The matched decoy class has $72$ rejected intervals out of $72$, giving reject rate $1.0$ with the same one-sided binomial value $2.117582368135751\times 10^{-22}$.  The predeclared paired comparison records $72$ wins out of $72$ pairs, hence win rate $1.0$ and the same binomial tail value.  Boundary cases are not promoted: among $24$ boundary cases, $0$ are promoted, so the promoted fraction is $0.0$.

\paragraph{Held-out stability.}
The row also records three held-out split checks.  In $refseq\_nc\_000913\_left\_locus\_fold$, $24$ positive examples are accepted at rate $1.0$ and $24$ decoys are rejected at rate $1.0$.  The same $24$--$24$ pattern and the same two rates occur for $refseq\_nc\_000913\_middle\_locus\_fold$ and $refseq\_nc\_000913\_right\_locus\_fold$.  All three splits are marked as passed.  These split records support stability of the frozen eligibility classifier on this curated finite contact, not a claim about translation or biological function outside the contact.

\paragraph{Local boundary verdict.}
The self-check claim is therefore admissible as a gatekeeping statement.  A start--stop geometry packet may remain as an ORF-eligibility boundary record, and a separately supplied curated interval contact may be tested by the frozen eligibility classifier.  What follows locally is only scoped finite eligibility against the stated contact rows.  No translation realization, folded structure, physical admissibility, biological function, mechanism, or universal biological law follows from the codon geometry, the quotient numbers, the single-window enumeration, or the curated interval statistics unless another independent contact is added that directly measures that higher layer.