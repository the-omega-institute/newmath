\origin{ai}
\subsection{The Punctured-Cube Boundary Row in Window-Six Codon Coordinates}
\label{sec:bioreality-h0-r-punctured-cube}

\paragraph{Finite carrier and observed row.}
The finite carrier is the six-bit codon cube $Q_6$, read only as a coordinate display for the $64$ standard codon labels.  The observed row $R$ is the finite set
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The cardinality audit gives $|R|=13$, with exactly the same thirteen codons in the cardinality row and in the punctured-cube row.  In the local face complex induced by this row, the recorded face vector is
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the row is not merely a named list: it is a finite local tile with thirteen vertices, sixteen one-dimensional adjacencies, and four filled local faces inside the window-six coordinate presentation.  The useful readback is deliberately narrow.  The curated standard-code table supplies codon-to-label assignments, so the row can be checked against amino-acid or stop labels, but that contact is a code-label bridge rather than a mechanism.

\paragraph{Punctured-cube content.}
The claim identified by $h0.R.punctured.cube$ is that the thirteen-codon row is the punctured-cube boundary object selected by the window-six coordinate, closure, spectrum, and relation probes.  The word ``punctured'' is used here at the finite-row level: the observed support has the above thirteen labeled vertices and the $(13,16,4)$ face-vector signature, not a continuum geometry and not an ambient biological necessity.  The same row is also the complete support of the finite modules later used for recovery checks.  The module assignment is exhaustive:
$$
\begin{aligned}
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\}.
\end{aligned}
$$
The module-classification audit records $R\_size=13$, $assigned\_count=13$, no extra module codons, no missing codons from the modules, and no unassigned codons.  This matters because the boundary row is not being interpreted by a hidden remainder class; every listed codon is accounted for by one of the six named finite modules.

\paragraph{Spectral and null-model checks.}
The spectrum row reports three scalar quantities attached to the finite construction:
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The cubic or radial polynomial residual is $-6.224354365258478\times 10^{-12}$, so the recorded spectral identity is satisfied to numerical tolerance, with the spectral radius entry matching $0.6752479308830213$.  These numbers do not make the codon table physical; they certify that the finite coordinate object has a reproducible spectral signature.

Against the broad shape null model with $N=2000$ sampled rows and seed $42$, the exact-shape count is $1$, giving $p\_exact=0.0005$.  The high-Jaccard count is $25$, giving $p\_jaccard\_ge\_0.9=0.0125$.  The sampling log records $57377$ distinct-sample attempts and $55377$ duplicate retries, so the reported probabilities should be read with the stated finite sampling protocol rather than as closed-form asymptotics.  Against the amino-acid-class-preserving null with $N=2000$ and seed $42$, the observed $M$ size is $20$, the exact-match count is $0$, the lower-tail count at or below the observed $M$ size is $7$, and the reported lower-tail probability is $0.0035$.  The exact-shape probability in that class-preserving run is reported as $0.0$ at the given sample size.  These two null checks support the claim that the observed finite row is not a routine output of the tested randomizations, while still remaining a finite-row statistical statement.

\paragraph{Recovery checks.}
The leave-one-code-out row evaluates $23$ code tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate in $M-T$ is $0.9365079365079365$ and the hit rate in $M-T$ plus boundary is $0.9841269841269841$.  The same $0.9841269841269841$ value is reported as the hit rate above baseline.  Family-level leave-one-code-out recovery is also recorded for five families: alternative yeast has hit rate $0.8333333333333334$, and bacterial plastid, candidate other, mitochondrial, and nuclear ciliate each have hit rate $1.0$.

The leave-one-codon-out row reports $anchor\_count=0$, $boundary\_count=5$, $closure\_coverage\_high=8$, $predictable\_count=8$, and $recovery\_rate=1.0$, with the recovery-rate-above-threshold flag also $1.0$.  Interpreted together with the module audit, this says that the thirteen-codon boundary row is recoverable at the code-read layer by the finite construction and its boundary classification.  It does not say that a biological cell must realize the construction, nor that a peptide, structure, or function has been recovered.

\paragraph{Reality contact and boundary.}
The only external contact used here is the curated standard-code table, whose observed fact is that codons are assigned to amino-acid or stop labels.  This contact has codon-label resolution.  It can test the code-read layer and the local codon-tile label split.  It cannot test translation realization, structural order, physical admissibility, function realization, a universal mechanism, or folded protein structure.  Therefore the finite-row verdict is local: window-six codon coordinates expose a punctured-cube boundary row with $13$ codons, face vector $(13,16,4)$, stable module coverage, spectral scalars, and nontrivial null-model behavior.  No translation, folding, physical admissibility, function, mechanism, or universality follows unless a separate higher-layer reality contact supplies it.

\paragraph{Finite-row verdict.}
The row $R$ is ready as a BioReality namecert record at the code-read layer.  The affirmative part is finite and concrete: the thirteen listed codons form the recorded punctured-cube row; the row has the audited face vector $(13,16,4)$; its spectral data are numerically consistent with the recorded polynomial residual; its shape is rare under the supplied null experiments; and its codons are exactly partitioned by the six recovery modules.  The negative part is equally part of the claim: a local codon tile is not a global biological law, window-six geometry alone is not translation or function, and amino-acid label-sequence readback is not folded protein structure.