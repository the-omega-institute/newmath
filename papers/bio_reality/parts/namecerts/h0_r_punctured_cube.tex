\origin{ai}

\subsection{Punctured-cube code-read record}
\label{sec:h0-r-punctured-cube}

\paragraph{Finite carrier and readback contact.}
This BioReality record works at the code-read layer of the standard-code codon table.  The carrier is the six-bit codon cube $Q_6$, read through a curated codon-to-label table.  The finite distinction under test is not a global biological law: it is a local tile boundary inside the codon display, with one singleton label corner and one three-point label class visible after the standard-code labels are read back.  The external contact used here is only the curated standard genetic-code bridge, whose observed fact is that codons are assigned amino-acid or stop labels.  Thus the row tests a code-layer label split, not translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.

\paragraph{Punctured cube row.}
The verified residual set $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same row is recorded as a punctured-cube object with face-vector
$$
\begin{aligned}
f(R)=(13,16,4).
\end{aligned}
$$
The list is not merely a size certificate: the row names all thirteen codons in the residual tile.  The module classification accounts for the same thirteen entries without surplus or omission.  The six modules are
$$
\begin{aligned}
\mathrm{AGR}&=\{AGA,AGG\},\\
\mathrm{AR\_Lys}&=\{AAA\},\\
\mathrm{CUN}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{Ile\_Met}&=\{AUA\},\\
\mathrm{SSN}&=\{UCA,UUA\},\\
\mathrm{Stop\_Trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
The classification reports $R\_size=13$, $assigned\_count=13$, no extra module codons, no missing codons, and no unassigned codons.  This is the finite-row reason that the punctured-cube claim is stated as a concrete residual object rather than as a loose similarity assertion.

\paragraph{Spectral and null-model numerics.}
The finite spectrum attached to this row records
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The cubic and radial polynomial checks both return the residual value $-6.224354365258478\times 10^{-12}$, so the displayed spectrum is numerically consistent to the recorded tolerance.  The shape-significance run used $N=2000$ samples with seed $42$, made $57377$ distinct-sample attempts, and incurred $55377$ duplicate retries.  It found $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{J\geq 0.9}&=0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null also used $N=2000$ and seed $42$.  In that run the observed $M$ size is $20$; no explicit finite codon list for $M$ is supplied in this row.  The run reports $0$ exact matches and $7$ samples in the lower tail at or below the observed $M$ size, with
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These numbers support the claim that the observed tile is a finite code-read pattern with low null frequency under the recorded sampling procedures.  They do not convert the tile into a biological mechanism.

\paragraph{Leave-one readback checks.}
The leave-one-code-out probe evaluated $23$ tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$.  By family, the above-baseline rates are $1.0$ for bacterial-plastid, candidate-other, mitochondrial, and nuclear-ciliate families, and $0.8333333333333334$ for alternative-yeast, across $5$ families.  The leave-one-codon-out probe reports $anchor\_count=0$, $boundary\_count=5$, $closure\_coverage\_high=8$, $predictable\_count=8$, and recovery rate $1.0$.  The same probe records $recovery\_rate\_above\_threshold=1.0$.  These checks say that the local boundary and closure data are useful for code-label recovery tasks in the finite rows supplied here.

\paragraph{Boundary of the claim.}
The claim certified by this row is narrow.  The standard-code table supplies codon-label readback, and the six-bit coordinate display exposes a local punctured-cube boundary pattern on the listed residual set $R$.  No inference to translation, folding, structural order, physical admissibility, biological function, mechanism, or universality follows from this row.  Amino-acid label-sequence readback is not folded protein structure.  A local codon tile is not a global law of biology.  Any statement across those boundaries requires a separate reality contact with its own finite-row evidence.