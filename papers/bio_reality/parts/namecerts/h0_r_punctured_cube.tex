\subsection{Punctured-cube row in the codon display}\label{sec:h0-r-punctured-cube}\origin{ai}

\paragraph{Finite carrier and row statement.}
The carrier is the six-bit codon cube $Q_6$, read only as a finite coordinate display for the $64$ codons.  The row selected by the local tile test is the following $13$-codon set:
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The independent cardinality row reports $|R|=13$ and returns the same ordered list.  The punctured-cube row reports that this same set has face-vector data
$$
\begin{aligned}
f(R)=(13,16,4),
\end{aligned}
$$
so the observation is not merely that thirteen codons have been named.  It is that these thirteen labels occupy a local cubical boundary pattern with $13$ vertices, $16$ one-dimensional adjacencies, and $4$ two-dimensional faces in the chosen six-bit display.

\paragraph{Code-layer readback.}
The contact for this row is the curated standard-code table.  That contact supplies codon-to-label assignments for amino-acid and stop labels, so it can test code-layer readback and local label splitting.  It does not supply translation dynamics, folded structure, physical admissibility, biological function, mechanism, or universality.  Consequently the mathematical object in this subsection is a finite coordinate-and-label record: a local tile in $Q_6$ together with its standard-code labels.  It is not a claim that biological translation is generated by this geometry, and it is not a claim that a label sequence determines a protein structure.

\paragraph{Spectral row.}
The spectral probe separates the displayed row from a purely verbal boundary description.  The recorded values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The same run reports a radial or cubic polynomial residual of $-6.224354365258478\times 10^{-12}$, which is within numerical tolerance for the stated finite-row computation.  These numbers are used only as finite invariants of the coordinate display.  They do not promote the row to a physical law.

\paragraph{Shape nulls.}
Against the shape null, $N=2000$ distinct samples were evaluated after $57377$ sample attempts and $55377$ duplicate retries.  The exact punctured-cube shape appeared once, giving
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}
$$
A high-overlap threshold using Jaccard score at least $0.9$ produced $25$ matches, giving
$$
\begin{aligned}
p_{J\geq 0.9}=0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null gives a second check.  In $2000$ samples it reports observed $|M|=20$, exact-match count $0$, lower-tail count $7$, exact-shape probability $0.0$, and lower-tail probability $0.0035$.  Thus the finite record is unusually specific under both the direct shape null and the amino-acid-class-preserving null, while still remaining a code-layer observation.

\paragraph{Leave-one-out behavior.}
The code-level recovery probes keep the same boundary discipline.  In the leave-one-code-out table, $23$ tables and $63$ targets were evaluated.  The baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$.  In the leave-one-codon-out row, the anchor count is $0$, the boundary count is $5$, the high-coverage closure count is $8$, the predictable count is $8$, and the recovery rate is $1.0$.  These values support the claim that the local tile has predictive code-read structure; they do not support any structural or functional claim beyond the codon-label table.

\paragraph{Family and module decomposition.}
The family-level row contains $5$ families.  Their hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial-plastid, $1.0$ for candidate-other, $1.0$ for mitochondrial, and $1.0$ for nuclear-ciliate.  The module classification assigns all $13$ row codons with no missing or extra module codons:
$$
\begin{aligned}
\mathrm{AGR}&=\{AGA,AGG\},\\
\mathrm{AR\_LYS}&=\{AAA\},\\
\mathrm{CUN}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ILE\_MET}&=\{AUA\},\\
\mathrm{SSN}&=\{UCA,UUA\},\\
\mathrm{STOP\_TRP}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
The row-level audit reports $R$-size $13$, assigned count $13$, no extra module codons, no codons missing from the modules, and no unassigned codons.  This makes the local tile a complete finite code-read packet: every codon in the punctured-cube row is accounted for by the displayed module partition.

\paragraph{Local verdict and boundary.}
The ready claim is that the standard-code codon table contains a finite $13$-codon row whose six-bit coordinate support is a punctured-cube tile with face-vector $(13,16,4)$, nontrivial spectral values, low shape-null frequencies, strong leave-one-out recovery, and a complete module classification.  The cannot-claim boundary is part of the record itself: no translation realization, folded structure, physical admissibility, biological function, mechanism, or universality follows from this row unless a separate reality contact supplies that higher-layer information.