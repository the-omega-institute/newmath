\subsection{Punctured Codon Cube Boundary Record}\label{sec:bior-punctured-codon-cube-boundary}\origin{ai}

\paragraph{Finite-row object.}
This BioReality record treats the standard-code codon table only at the code-read layer. The carrier is the six-bit codon cube $Q_6$, read through a curated codon-to-label table. The finite row singled out by the probe is the set
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The audited cardinality row gives $|R|=13$, and the same thirteen codons occur in the punctured-cube row. The local face data are summarized by the finite vector
$$
\begin{aligned}
f(R)=(13,16,4),
\end{aligned}
$$
so the object is not being used as a loose visual metaphor: the row asserts thirteen displayed codons, sixteen local one-dimensional incidences, and four local two-dimensional faces in the chosen window-six geometry.

\paragraph{Spectral readback.}
The spectrum row records three scalar quantities attached to this same code-read construction:
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The associated radial or cubic residual is $-6.224354365258478\cdot 10^{-12}$, so the finite computation is numerically consistent with the polynomial constraint at the reported tolerance. This chapter uses those numbers only as coordinates of the local codon-label pattern. They do not assert translation dynamics, folded structure, physical admissibility, biological function, a mechanism, or universality.

\paragraph{Shape significance.}
Against the reported finite null sample with $N=2000$ and seed $42$, the exact-shape test observed $1$ exact match. The corresponding exact-shape probability is
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}
$$
The high-overlap test observed $25$ samples with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{J\geq 0.9}=0.0125.
\end{aligned}
$$
The sampler made $57377$ distinct-sample attempts and recorded $55377$ duplicate retries, so the result is a finite-row empirical fact about this sampled comparison class rather than a closed-form biological law. A second amino-acid-class-preserving null row used $N=2000$ and seed $42$. It reported observed $M$ size $20$, exact match count $0$, lower-tail count $7$, exact-shape probability $0.0$, and lower-tail probability $0.0035$. Thus the local shape is uncommon under both the reported unconstrained comparison and the amino-acid-class-preserving comparison, but the contact remains a codon-label contact.

\paragraph{Leave-one-code-out behavior.}
The leave-one-code-out row evaluated $23$ tables and $63$ total targets. The baseline hit rate was $0.19821739130434785$. The hit rate above baseline was $0.9841269841269841$, the hit rate in $M\setminus T$ was $0.9365079365079365$, and the hit rate in $M\setminus T$ plus boundary was $0.9841269841269841$. Broken down by family, the family count was $5$, with above-baseline rates
$$
\begin{aligned}
\mathrm{alternative\ yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\ plastid} &= 1.0,\\
\mathrm{candidate\ other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\ ciliate} &= 1.0.
\end{aligned}
$$
These rows support a local recovery pattern for label-table variants. They do not identify a biochemical necessity behind those variants.

\paragraph{Leave-one-codon-out behavior.}
The leave-one-codon-out row reports anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$. The threshold comparison also reports recovery rate above threshold $1.0$. This is a finite boundary statement: when one codon is hidden from the row, the local window-six data recover the tested target through predictable or boundary behavior in the reported cases. The statement is deliberately limited to the code-read task.

\paragraph{Module classification.}
The thirteen codons of $R$ are fully assigned to six reported modules:
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
The classification row gives $R$ size $13$, assigned count $13$, no missing module codons, no extra module codons, and no unassigned codons. Every listed module has predictable-or-boundary status. This closes the finite accounting of the punctured-cube record: all thirteen codons in the row are accounted for by the local module split.

\paragraph{Reality contact and boundary.}
The only reality contact used here is the curated standard genetic-code bridge, whose observed fact is that a standard-code table assigns codons to amino-acid or stop labels. That contact can test code-read behavior and local codon-tile label splits. It cannot test translation realization, structural order, physical admissibility, folded protein structure, biological function, mechanism, or universality. Therefore the ready claim is narrow: window-six codon coordinates expose a statistically uncommon local boundary-tile pattern for the thirteen-codon row $R$, and the curated table supplies the label readback. No higher-layer biological claim follows unless a separate higher-layer contact supplies it.