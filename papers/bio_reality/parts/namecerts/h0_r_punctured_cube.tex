\subsection{A punctured local tile in the six-bit codon cube}
\label{sec:h0-r-punctured-cube}
\origin{ai}

\paragraph{Finite carrier and observed row.}
The finite carrier is the six-bit codon cube $Q_6$, read through the standard-code codon-label table.  In this row the distinguished set $R$ is not an inferred biological mechanism; it is the finite codon set selected by the window-six coordinate and then read back against the curated label table.  The verified row is
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same computation records $|R|=13$, and the independently recorded cardinality row gives the same thirteen codons.  The face data of the row is summarized by
$$
\begin{aligned}
f(R)=(13,16,4),
\end{aligned}
$$
so the local object has thirteen vertices, sixteen one-dimensional adjacencies, and four two-dimensional faces in the chosen cubical coordinate.  This is the sense in which the row is called a punctured local cube: it is a finite cubical tile with a missing global-cube completion, not a statement that the biological code is generated by cubical geometry.

\paragraph{Label contact.}
The only external contact used here is the curated standard-code table, whose role is to assign codons to amino-acid or stop labels.  This contact supports code-layer readback and local label-split checks.  It does not supply translation dynamics, molecular structure, physical admissibility, biological function, a global evolutionary law, or a universal mechanism.  Therefore the row may say that the thirteen displayed codons occupy a special local tile under the selected coordinate, but it may not say that DNA or translation must obey this tile as a biological necessity.

\paragraph{Spectral row.}
The finite spectral calculation attached to the same coordinate records
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial polynomial residual is $-6.224354365258478\times 10^{-12}$, which is also recorded as the cubic-polynomial residual.  Thus the numerical row is internally stable at the displayed precision.  The relevant comparison is not between a spectral value and a biological mechanism; it is between the finite coordinate object and its null rows.  The spectrum is evidence that the selected coordinate has a reproducible finite signature, while the curated label table remains the only reality bridge for biological labels.

\paragraph{Shape significance rows.}
Against the shape null model with seed $42$, the computation used $N=2000$ samples, required $57377$ distinct-sample attempts, and encountered $55377$ duplicate retries.  The exact shape appeared once, giving
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{J\geq 0.9} &= 0.0125,
\end{aligned}
$$
with $25$ samples reaching Jaccard similarity at least $0.9$.  A separate amino-acid-class-preserving null row, again with $N=2000$ and seed $42$, recorded observed $|M|=20$, exact match count $0$, lower-tail count $7$, exact-shape $p$-value $0.0$, and lower-tail $p$-value $0.0035$.  These rows support a finite local-coordinate anomaly relative to the sampled nulls.  They do not identify a biochemical cause, because neither null row contains a translation experiment, a folding assay, a physical-admissibility constraint, or a function measurement.

\paragraph{Leave-one evidence.}
The leave-one-code-out row evaluated $23$ code tables and $63$ targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M\setminus T$ is $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus boundary is $0.9841269841269841$.  The leave-one-codon-out row records anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$.  These figures say that the local coordinate has strong finite recovery behavior under the stated leave-one probes.  They still remain code-read facts: a recovered codon label is not a recovered protein structure, not a demonstrated translation event, and not a function prediction.

\paragraph{Family and module decomposition.}
The by-family row separates the leave-one-code-out evidence into $5$ families.  The family hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate.  The module-classification row assigns all thirteen $R$ codons and reports no extras, no missing module codons, and no unassigned codons:
$$
\begin{aligned}
\mathrm{AGR} &= \{AGA, AGG\},\\
\mathrm{AR\_Lys} &= \{AAA\},\\
\mathrm{CUN} &= \{CUA, CUC, CUG, CUU\},\\
\mathrm{Ile\_Met} &= \{AUA\},\\
\mathrm{SSN} &= \{UCA, UUA\},\\
\mathrm{Stop\_Trp} &= \{UAA, UAG, UGA\}.
\end{aligned}
$$
The assigned count is $13$, matching $|R|=13$.  The decomposition gives a finite audit trail for the local label split: every module has predictable-or-boundary support, and the module list covers exactly the punctured row.  This is the strongest legitimate reading of the data in this chapter.

\paragraph{Boundary verdict.}
The row is ready as a BioReality code-read record.  It supports the claim that window-six codon coordinates expose a distinguished thirteen-codon punctured tile with reproducible face data, spectral values, null-model rarity, leave-one recovery, and complete module classification.  It also blocks promotion beyond the code layer.  No translation realization, folded structure, structural order, physical admissibility, biological function, mechanism, or universality follows from these rows unless a separate reality contact supplies that higher-layer observation.