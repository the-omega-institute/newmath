\subsection{A finite punctured-cube row in the standard-code table}
\label{sec:bioreality-h0-r-punctured-cube}
\origin{ai}

\paragraph{Object and coordinate scope.}
The object in this record is the standard-code codon table read only as a finite codon-to-label table.  The coordinate carrier is the six-bit codon display $Q_6$, where each codon is represented by a finite word over the observed nucleotide alphabet and then inspected through local tile faces, singleton label corners, and three-point label classes.  The claim is not that the biological translation system is generated by this coordinate geometry.  The claim is narrower: the curated standard-code labels expose a finite local boundary row $R$ whose internal row data match a punctured-cube tile in the window-six coordinate system.

\paragraph{The row $R$.}
The verified row is the thirteen-codon set
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The independent cardinality check gives $|R|=13$ and returns exactly the same codon list.  Thus the finite-row assertion is not only a shape label: it is a concrete row identity on the standard-code readback table.  The punctured-cube audit reports the face-vector match
$$
\begin{aligned}
f(R)=(13,16,4),
\end{aligned}
$$
so the row has thirteen vertices, sixteen local edges, and four local square faces in the inspected tile complex.  This is the finite sense in which $R$ is called a punctured cube: the statement is about the observed local cell counts and the named codon row, not about a global law on all biological codon use.

\paragraph{Spectral signature.}
The same row sits inside a measured coordinate spectrum.  The recorded values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\times 10^{-12}$, and the spectral-radius value reported within tolerance is again $0.6752479308830213$.  These numbers give a finite numerical witness for the row geometry: $R$ is not merely selected by a verbal grouping of codons, but is also tied to a reproducible coordinate spectrum.  The residual is close to zero at the stated precision, so the spectral calculation is internally consistent for the finite row under the implemented probe.

\paragraph{Null probes and shape significance.}
Against the general shape null model, the audit used $N=2000$ distinct sampled tables, with $57377$ distinct-sample attempts and $55377$ duplicate retries under seed $42$.  It found $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$.  The corresponding finite-row tail values are
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9}&=0.0125.
\end{aligned}
$$
Against the amino-acid-class-preserving null, again with $N=2000$ and seed $42$, the observed $M$-size was $20$, the exact-match count was $0$, and the lower-tail count at or below the observed $M$-size was $7$.  The recorded values are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These are finite Monte Carlo row facts.  They support the statement that the punctured-cube row is unusual under the tested coordinate-preserving and amino-acid-class-preserving randomizations.  They do not identify a biochemical cause.

\paragraph{Leave-one readback checks.}
The leave-one-code-out probe evaluated $23$ tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$, while the observed hit rate above baseline was $0.9841269841269841$.  The hit rate in $M\setminus T$ was $0.9365079365079365$, and the hit rate in $M\setminus T$ plus boundary was $0.9841269841269841$.  The family-level leave-one-code-out audit used $5$ families and reported hit rates above baseline of $0.8333333333333334$ for alternative yeast and $1.0$ for bacterial plastid, candidate other, mitochondrial, and nuclear ciliate families.  The leave-one-codon-out probe reported anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$.  These checks say that, once the finite code-layer row and its boundary are fixed, omitted labels are frequently recovered by the same local coordinate rule.  They still remain label readback checks.

\paragraph{Module accounting.}
The module classification covers all thirteen codons of $R$ with no missing or extra module codons: $R$-size $13$, assigned count $13$, no missing-from-modules entries, no extra-module-codons entries, and no unassigned codons.  The verified modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every listed module has a predictable-or-boundary status in the finite audit.  This module partition is the local explanation of the row's boundary behavior: the row is not a loose collection of thirteen codons, but a completely assigned set of finite label modules inside the standard-code readback.

\paragraph{Reality contact and boundary.}
The only reality contact used here is the curated standard-code table assigning codons to amino-acid or stop labels.  That contact tests code-layer readback and local codon tile label splits.  It does not test translation realization, folded structure, structural order, physical admissibility, biological function, mechanism, or universality.  Therefore the cannot-claim boundary is part of the result: no translation, no folding, no physical admissibility, no function, no mechanism, and no universal biological law follows from this punctured-cube row unless a separate higher-layer contact supplies that information.  The finite conclusion is exactly that the standard-code label table contains a thirteen-codon row $R$ with punctured-cube face data $(13,16,4)$, a reproducible spectral signature, significant finite null-model behavior, and complete module accounting at the code-read layer.