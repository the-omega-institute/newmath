\subsection{Punctured-cube row for the standard-code boundary tile}
\label{sec:biorality-h0-r-punctured-cube}
\origin{ai}

\paragraph{Claim row.}
The row named $h0.R.punctured.cube$ is a finite code-read claim about the standard-code codon table.  Its carrier is the six-bit codon cube $Q_6$, read through a curated standard-code label table.  The distinctions used in this row are the six-bit codon display, a local tile face, a singleton label corner, and a three-point label class.  The internal structure recorded by the row is coordinate, closure, spectrum, and relation data.  The biological object touched by the row is only the standard-code codon table as a codon-to-label readback source.

\paragraph{Observed finite set.}
The boundary set $R$ contains exactly thirteen codons.  In the row order supplied by the finite computation, it is
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The independent cardinality row gives $|R|=13$ and returns the same thirteen codons.  The punctured-cube row identifies this same set as the punctured local cube row and gives the face-vector
$$
\begin{aligned}
f(R)=(13,16,4).
\end{aligned}
$$
Thus the finite object is not merely a named codon list: it has thirteen vertices, sixteen one-dimensional incidences, and four two-dimensional faces in the local tile representation used by the computation.

\paragraph{Spectral coordinate.}
The spectral row gives a numerical coordinate for the same local split.  The supplied values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The associated radial or cubic polynomial residual is $-6.224354365258478\times 10^{-12}$, so the recorded spectral radius value $0.6752479308830213$ is within the stated numerical tolerance of the polynomial constraint.  This is a coordinate fact about the finite display and its derived spectrum, not a biological mechanism.

\paragraph{Shape significance against finite null rows.}
The unconstrained shape-significance row used $N=2000$ samples with seed $42$.  It required $57377$ distinct-sample attempts and recorded $55377$ duplicate retries.  Among those samples there was $1$ exact shape match, giving $p_{\mathrm{exact}}=0.0005$.  There were $25$ samples with Jaccard score at least $0.9$, giving $p_{\mathrm{Jaccard}\ge 0.9}=0.0125$.  A second null row preserving amino-acid classes also used $N=2000$ and seed $42$.  In that constrained null there were $0$ exact matches, so the supplied exact-shape value is $p=0.0$ at the finite sampling resolution, and the lower-tail count for size at most the observed $M$ size was $7$, giving lower-tail value $0.0035$.  The same row records the observed value $|M|=20$.  No explicit codon list for $M$ is part of the supplied row data, so the chapter uses $|M|=20$ as the available finite datum rather than reconstructing an unprovided set.

\paragraph{Predictive readback rows.}
The leave-one-code-out row evaluated $23$ tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$.  The hit rate in $M\setminus T$ is $0.9365079365079365$, while the hit rate in $M\setminus T$ plus the boundary is $0.9841269841269841$.  The row also records a hit rate above baseline of $0.9841269841269841$.  The leave-one-codon-out row records $0$ anchors, $5$ boundary cases, $8$ high-coverage closure cases, $8$ predictable cases, and recovery rate $1.0$.  These figures support the local code-read interpretation: when codon labels are withheld inside the finite table task, the local tile and boundary data recover or classify the withheld label positions at the code layer.

\paragraph{Family and module decomposition.}
The family-level leave-one-code-out row groups the results into $5$ families.  The family hit-rate-above-baseline values are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial or plastid codes, $1.0$ for candidate other codes, $1.0$ for mitochondrial codes, and $1.0$ for nuclear ciliate codes.  The module-classification row assigns all thirteen codons of $R$ with no extras and no omissions: $R$ has size $13$, the assigned count is $13$, the extra-module list is empty, the missing-from-modules list is empty, and the unassigned-codon list is empty.  The six finite modules are
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
This decomposition is complete for the supplied $R$ row: every listed codon is placed into exactly one named module in the finite classification record.

\paragraph{Reality contact and boundary.}
The only external contact used here is the curated standard-code table, treated as a codon-label readback bridge.  It supports the observation that codons are assigned amino-acid or stop labels in the standard code.  It does not, by itself, test translation realization, folded structure, structural order, physical admissibility, biological function, a universal mechanism, or a global law of DNA.  Therefore the claim remains: window-six codon coordinates expose a local boundary-tile structure in the standard-code label table, with $R$ realized as the thirteen-codon punctured-cube row above.  No translation, folding, physical admissibility, function, mechanism, or universality follows unless a separate higher-layer reality contact supplies that information.

\paragraph{Local verdict.}
The finite rows align at the code-read layer.  The same thirteen-codon set appears in the punctured-cube and cardinality rows; its finite face-vector is $(13,16,4)$; its spectral coordinate satisfies the recorded polynomial to residual about $6.23\times 10^{-12}$ in magnitude; the shape is rare under the supplied sampled null rows; the leave-one-code and leave-one-codon probes give high code-layer recovery; and the module classification accounts for all thirteen codons without extras or omissions.  The row is therefore ready as a BioReality vision-level record of a local standard-code boundary tile, provided the cannot-claim boundary is kept explicit.