\subsection{Punctured codon cube at the code-read boundary}\label{sec:bioreality-punctured-codon-cube}\origin{ai}

\paragraph{Finite carrier and reality contact.} The carrier in this row is the six-bit codon cube $Q_6$, read through a fixed standard-code label table. The reality contact is deliberately narrow: it supplies codon-to-label assignments for the standard genetic code, and it does not supply a translation apparatus, a fold, a physical admissibility test, a function assay, a mechanism, or a universality theorem. Within that scope, the distinguished row is the finite set
$$\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}$$
The cardinality audit gives $|R|=13$, with the same thirteen codons appearing in the cardinality row and in the punctured-cube row. Thus the row is not being used as a symbolic placeholder for an unspecified region: it is the explicit thirteen-codon boundary set displayed above.

\paragraph{Local tile geometry.} The row realizes a punctured local tile in the codon cube. Its finite face count is recorded by the vector
$$\begin{aligned}
f(R)=(13,16,4),
\end{aligned}$$
so the observed object has thirteen vertices, sixteen one-dimensional incidences, and four two-dimensional faces in the selected local cubical presentation. This is the chapter's mathematical content: a local boundary-tile shape is visible after the six-bit codon display is combined with the curated label readback. The observation is not a global biological law. It says that this finite row has a punctured-cube signature inside the chosen coordinate display; it does not say that DNA or translation is governed by that geometry.

\paragraph{Spectral readout.} The same finite row has a numerical spectral signature. The reported spectral values are
$$\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}$$
The radial cubic check has residual $-6.224354365258478\cdot 10^{-12}$, and the spectral-radius tolerance record repeats $0.6752479308830213$ for $\lambda_M$. These numbers support a stable finite-row readout: the local row is not only a set of thirteen codons, but also a row with a reproducible adjacency-spectrum profile in the chosen finite coordinate system.

\paragraph{Shape significance under finite nulls.} Against the sampled shape null with $N=2000$ and seed $42$, the exact punctured shape occurred once, giving
$$\begin{aligned}
p_{\mathrm{exact}} = 0.0005.
\end{aligned}$$
The high-overlap threshold produced $25$ matches with Jaccard score at least $0.9$, giving
$$\begin{aligned}
p_{J\geq 0.9}=0.0125.
\end{aligned}$$
The same run reports $57377$ distinct-sample attempts and $55377$ duplicate retries, so the finite sampling space is visibly constrained by repeated draws. A separate amino-acid-class-preserving null, again with $N=2000$ and seed $42$, reports observed $M$-size $20$, exact-match count $0$, lower-tail count $7$, exact-shape value $0.0$, and lower-tail value $0.0035$. These data make the row suitable for a local code-read claim, not for an unrestricted biological claim.

\paragraph{Prediction rows at the same boundary.} Leave-one-code-out evaluation gives $23$ tables and $63$ total targets. The baseline hit rate is $0.19821739130434785$, while the observed above-baseline hit rate is $0.9841269841269841$. The restricted $M-T$ hit rate is $0.9365079365079365$, and the $M-T$ plus boundary rate is again $0.9841269841269841$. Leave-one-codon-out evaluation reports anchor count $0$, boundary count $5$, predictable count $8$, high closure coverage $8$, and recovery rate $1.0$. By family, the above-baseline rates are $0.8333333333333334$ for alternative yeast and $1.0$ for bacterial-plastid, candidate-other, mitochondrial, and nuclear-ciliate families. These rows support the same interpretation: the local tile is a useful code-read boundary object under finite perturbation tests.

\paragraph{Module classification.} The thirteen codons are exhausted by six named modules:
$$\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}$$
The module audit records $R$-size $13$, assigned count $13$, no extra module codons, no missing codons from the modules, and no unassigned codons. Every listed module has either predictable or boundary status. This gives a finite internal bookkeeping statement for the row: the punctured-cube set is not merely cardinality-$13$, but is also completely distributed across the six displayed code-read modules.

\paragraph{Cannot-claim boundary.} The chapter therefore claims only a local codon-tile boundary at the code-read layer. The curated standard-code table allows codon labels to be read back and compared with the finite coordinate pattern. It does not observe translation realization, folded structure, structural order, physical admissibility, biological function, universal mechanism, or biological necessity. Any claim crossing from this row to translation, folding, physical admissibility, function, mechanism, or universality requires a separate reality contact. Without such a contact, amino-acid label readback remains label readback, and the punctured cube remains a local finite-row signature rather than a global law of biology.