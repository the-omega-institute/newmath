\origin{ai}

\subsection{Window-six punctured-cube readback for the standard code}
\label{sec:bioreality-h0-r-punctured-cube}

\paragraph{Finite codon carrier.}
This record treats the standard-code table as a finite code-layer contact.  The carrier is the six-bit codon cube $Q_6$, with codons read as coordinate points and with labels supplied by the curated standard-code table.  The distinguished boundary set $R$ is exactly the following thirteen codons:
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The row audit records $|R|=13$, and the same thirteen entries are returned by the punctured-cube computation.  The local cell count is summarized by the face vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4),
\end{aligned}
$$
so the object observed here is not an arbitrary list of labels but a finite cubical boundary pattern inside the window-six coordinate display.

\paragraph{Local label geometry.}
The finite distinctions used by the packet are: the six-bit codon display, local tile faces, singleton label corners, and three-point label classes.  The computation therefore has two sources of information that must not be conflated.  The coordinate and closure data come from the finite cube construction; the amino-acid and stop labels come from the curated code table.  This is enough to state a local code-read claim: the standard-code labels meet a punctured-cube boundary pattern on the listed thirteen codons.  It is not enough to infer that the cube geometry is a biological necessity or that the displayed pattern explains translation.

\paragraph{Spectrum and finite significance checks.}
The spectral record supplies three scalar quantities for the same finite object: $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The associated cubic constraint is numerically closed at the recorded tolerance, with residual
$$
\begin{aligned}
-6.224354365258478\times 10^{-12}.
\end{aligned}
$$
The shape-significance run used $N=2000$ sampled controls with seed $42$.  It found one exact shape match, giving $p_{\mathrm{exact}}=0.0005$, and twenty-five controls with Jaccard score at least $0.9$, giving $p_{J\geq 0.9}=0.0125$.  The distinct-sample log also matters: $57377$ attempts were required, with $55377$ duplicate retries, so the control space is finite and visibly constrained rather than a source of unlimited independent draws.

\paragraph{Amino-acid-class preserving null.}
A second null model preserves amino-acid classes.  In that audit the observed module size is $20$, again with $N=2000$ and seed $42$.  The exact-shape count is $0$, so the recorded exact-shape value is $p=0.0$ at the displayed sample resolution.  The lower-tail count at or below the observed size is $7$, giving $p=0.0035$.  This strengthens the code-layer reading: under controls that retain amino-acid-class structure, the observed local organization remains uncommon.  The result is still a finite table comparison, not a mechanistic statement about ribosomes, cellular context, or protein behavior.

\paragraph{Leave-one recovery.}
The leave-one-code-out audit evaluated $23$ code tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M-T$ is $0.9365079365079365$, and the rate inside $M-T$ plus boundary is again $0.9841269841269841$.  The family-resolved audit has $5$ families: alternative yeast has rate $0.8333333333333334$, while bacterial plastid, candidate other, mitochondrial, and nuclear ciliate each have rate $1.0$.  The leave-one-codon-out audit records $8$ predictable codons, $5$ boundary codons, $0$ anchors, high closure coverage $8$, and recovery rate $1.0$.  These numbers support a local recovery phenomenon for codon-label readback, with boundary membership carrying predictive information in the finite table.

\paragraph{Module accounting.}
The thirteen boundary codons are partitioned by the module classifier without leftover entries.  The assigned count is $13$, the recorded $R$ size is $13$, the unassigned list is empty, the missing-from-modules list is empty, and the extra-module-codons list is empty.  The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Each listed module has either predictable or boundary status in the local audit.  This gives the record a concrete finite shape: a thirteen-point punctured-cube boundary, a four-face local tile count, and a complete module assignment aligned with standard-code labels.

\paragraph{Scope boundary.}
The contact used here is the curated standard genetic-code table.  It observes codon-to-label assignments at code-read resolution.  It does not observe translation realization, folded structure, physical admissibility, biological function, universal mechanism, or any law forcing organisms to instantiate this geometry.  In particular, reading an amino-acid label sequence from codons is not evidence for a folded protein structure.  Any claim crossing into translation, structure, admissibility, function, mechanism, or universality requires a separate contact that measures that higher layer directly.  The present packet therefore records a finite local boundary phenomenon in the standard-code table and keeps all higher biological interpretations outside the claim.