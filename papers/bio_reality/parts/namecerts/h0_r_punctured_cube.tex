\origin{ai}
\subsection{Punctured-cube row for the standard-code label table}
\label{sec:bioreality-h0-r-punctured-cube}

\paragraph{Finite contact.}
The finite row records a code-layer contact with the curated standard genetic-code table.  The carrier is the six-bit codon cube $Q_6$, read as a codon display with local tile faces, singleton label corners, and three-point label classes.  The biological object is the standard-code codon table, but the contact is only a code-read contact: it supplies codon-to-label assignments for amino-acid or stop labels, not translation realization, not folded structure, not physical admissibility, not biological function, not a mechanism, and not universality.  The observed finite row is the subset
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The row count is $|R|=13$.  The same row is reported as the punctured-cube row, with face data
$$
\begin{aligned}
f(R)=(13,16,4),
\end{aligned}
$$
so the local shape has thirteen vertices, sixteen edges, and four square faces in the finite coordinate readback.  This is a finite combinatorial statement about the codon display and its curated labels.  It is not a statement that biological DNA must realize this geometry as a law.

\paragraph{Spectral scale.}
The accompanying spectrum separates the measured local row from a merely enumerated codon list.  The finite spectral values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial polynomial check gives $-6.224354365258478\times 10^{-12}$, and the cubic-polynomial residual is the same finite tolerance value.  Thus the record treats the punctured-cube row as a coordinated finite object with a reproducible spectral readback, not just as a hand-named set of codons.  The larger row $M$ is present here only through supplied scalars: the observed $M$ size is $20$, and no explicit member list for $M$ is asserted in this chapter.

\paragraph{Null-model pressure.}
The finite-row tests put the shape under two null comparisons.  In the shape-significance run with seed $42$, the sample size was $N=2000$, with $57377$ distinct-sample attempts and $55377$ duplicate retries.  The exact-shape count was $1$, giving
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{J\geq 0.9} &= 0.0125,
\end{aligned}
$$
with $25$ high-Jaccard matches at threshold $0.9$.  In the amino-acid-class-preserving null, again with $N=2000$ and seed $42$, the exact-match count was $0$, the lower-tail count at or below the observed $M$ size was $7$, and the reported values were
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
These finite numbers support the claim that the row has nontrivial local shape relative to the tested nulls.  They do not promote the row beyond the code layer.  A low finite-row probability is still only evidence about the displayed code-label geometry under the stated sampling rules.

\paragraph{Recovery probes.}
The leave-one-code-out probe evaluated $23$ tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate in $M-T$ was $0.9365079365079365$, and the hit rate in $M-T$ plus boundary was $0.9841269841269841$.  The leave-one-codon-out probe had anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$.  The family-level readout has five families, with hit rates above baseline
$$
\begin{aligned}
\mathrm{alternative\ yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\ plastid} &= 1.0,\\
\mathrm{candidate\ other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\ ciliate} &= 1.0.
\end{aligned}
$$
These probes make the row usable as a local boundary-tile record: missing code labels are often recovered by the finite closure-and-boundary geometry.  The support condition remains local.  The probe breaks if the row is used to claim translation, folded structure, physical admissibility, biological function, a universal mechanism, or any global law of DNA.

\paragraph{Module partition.}
The module classification assigns all thirteen codons in $R$ with no missing row elements, no extra module codons, and no unassigned codons.  The six finite modules are
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The reported module summary has $R$ size $13$ and assigned count $13$.  Every listed module has a predictable-or-boundary status in the finite row.  This gives the chapter its precise readback: the punctured-cube subset is a complete local module record for the supplied standard-code labels.

\paragraph{Boundary verdict.}
The ready claim is therefore narrow.  The six-bit codon coordinate exposes a punctured-cube row $R$ of size $13$, with finite face vector $(13,16,4)$, nontrivial spectral values, low null-model probabilities, strong leave-one-out recovery, and complete module classification.  The curated standard-code table supplies the required external label contact.  No further biological layer follows from this chapter.  Amino-acid label-sequence readback is not folded protein structure; a codon tile is not a translation apparatus; a local boundary pattern is not physical admissibility, function, mechanism, or universality.  Any claim crossing those boundaries requires a separate contact with its own finite rows.