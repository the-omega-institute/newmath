\subsection{The punctured-cube codon window}\label{sec:h0-r-punctured-cube}
\origin{ai}

\paragraph{Finite carrier and reality contact.}
The finite carrier is the six-bit codon cube $Q_6$, read through the standard-code label table.  The table is used only as a code-layer contact: it assigns each codon to an amino-acid or stop label, and it does not by itself observe translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.  Within this contact, the distinguished row set $R$ is exactly the thirteen-codon set
$$\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}$$
The separate cardinality check reports $|R|=13$ and returns the same thirteen codons.  Thus the finite-row claim is not merely that a local pattern exists somewhere in $Q_6$, but that the named row set is exactly this codon list and has exactly this size.

\paragraph{Punctured-cube shape.}
The verified local shape attached to $R$ has face-vector
$$\begin{aligned}
f(R)=(13,16,4).
\end{aligned}$$
In the BioReality reading, this is a local boundary-tile statement inside the codon-coordinate display.  It says that the selected codon rows form a thirteen-vertex punctured-cube pattern with sixteen one-dimensional incidences and four two-dimensional faces under the chosen window-six adjacency.  The claim is therefore finite and inspectable: the object is the displayed set $R$, the coordinate carrier is $Q_6$, and the measured combinatorial certificate is $(13,16,4)$.

\paragraph{Spectral readback.}
The same packet records a spectral contrast between the local row pattern and the complementary or comparison module data.  The reported values are
$$\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}$$
The associated cubic or radial polynomial residual is $-6.224354365258478\times 10^{-12}$, so the recorded spectral computation is numerically at tolerance scale rather than a qualitative label attached after the fact.  This supports a code-read distinction between the local $R$ tile and the larger measured structure $M$, while still remaining inside finite codon-label readback.

\paragraph{Null-model separation.}
The shape signal is tested against sampled alternatives.  In the general shape experiment, $N=2000$ samples were attempted under seed $42$; because the distinct-sample filter was strict, $57377$ distinct-sample attempts produced $55377$ duplicate retries.  Exactly one sample matched the exact shape, giving
$$\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}$$
There were $25$ samples with Jaccard similarity at least $0.9$, giving
$$\begin{aligned}
p_{\mathrm{Jaccard}\geq 0.9}=0.0125.
\end{aligned}$$
A second amino-acid-class-preserving null model also used $N=2000$ and seed $42$.  It reported observed $M$ size $20$, exact-match count $0$, lower-tail count $7$, exact-shape value $0.0$, and lower-tail value $0.0035$.  These finite-row numbers justify treating the punctured-cube pattern as a nontrivial local code-layer regularity, not as an untested visual impression.

\paragraph{Leave-one readback tests.}
The leave-one-code-out probe evaluated $23$ code tables and $63$ targets.  Its baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate inside $M\setminus T$ was $0.9365079365079365$, and the rate inside $M\setminus T$ plus the boundary was again $0.9841269841269841$.  The leave-one-codon-out probe reported anchor count $0$, boundary count $5$, closure coverage high $8$, predictable count $8$, and recovery rate $1.0$.  These values say that the local tile has predictive readback behavior under deletion probes, but only for codon-label placement in the finite table.

\paragraph{Family and module accounting.}
The family-level leave-one-code-out calculation separates five families.  Their hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate.  The module classification covers all thirteen $R$ codons with no missing or extra module rows: $R$ size $13$, assigned count $13$, missing-from-modules $\varnothing$, extra-module-codons $\varnothing$, and unassigned-codons $\varnothing$.  The modules are
$$\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}$$
Every listed module has a predictable or boundary status in the probe output.  This gives a finite decomposition of the punctured-cube row set into biologically named code modules without claiming that the decomposition is a law of translation or a mechanism of protein realization.

\paragraph{Boundary of the claim.}
The supported statement is deliberately narrow.  Window-six codon coordinates expose a local boundary-tile structure in the standard-code label table: $R$ is the thirteen-codon punctured-cube row set, its face-vector is $(13,16,4)$, its spectrum has the recorded values, and the deletion probes recover high code-layer hit rates.  Nothing here promotes the tile to translation, folded protein structure, physical admissibility, biological function, a mechanism, or universality.  Any such stronger claim would require a separate reality contact that observes the higher layer directly.  Amino-acid label readback remains label readback; it is not folded structure, and it is not evidence of functional realization.