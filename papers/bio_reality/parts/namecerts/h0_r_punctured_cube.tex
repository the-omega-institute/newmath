\origin{ai}
\subsection{Window-six punctured cube around the standard-code boundary}
\label{sec:bioreality-h0-r-punctured-cube}

\paragraph{Finite object.}
The packet records a window-six codon display on the codon cube $Q_6$, with coordinates supplied by the six-bit presentation of codons and labels supplied by the curated standard-code table.  The selected boundary row is the thirteen-codon set
$R=\{\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}$.
The verified cardinality row reports $|R|=13$, and the punctured-cube row reports the same ordered codon list.  Its cell-count signature is
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4),
\end{aligned}
$$
so the local tile is not being asserted from visual analogy alone: the vertex, edge, and face counts are part of the finite certificate.  The row is called punctured because the selected codon set occupies a cube-like local tile with one missing corner behavior rather than a full unconstrained block.  The singleton-corner and three-point-label-class distinctions are therefore treated as finite combinatorial observations tied to the standard-code labels, not as biological forces.

\paragraph{Spectral and sampling checks.}
The same packet carries a numerical spectrum row.  The reported values are $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The cubic residual and radial residual are both $-6.224354365258478\cdot 10^{-12}$, which places the finite computation inside the stated tolerance rather than merely near the intended qualitative shape.  A shape-significance run with $N=2000$ samples and seed $42$ observed one exact shape match after $57377$ distinct-sample attempts and $55377$ duplicate retries, giving $p_{\mathrm{exact}}=0.0005$.  The same run found $25$ samples with Jaccard score at least $0.9$, giving $p_{\mathrm{Jaccard}\geq 0.9}=0.0125$.  These numbers support the narrower claim that the observed tile is uncommon under the recorded sampling protocol.  They do not identify the historical cause of the standard code and do not convert a code-label pattern into a law of molecular biology.

\paragraph{Class-preserving null comparison.}
A second null row preserves amino-acid class structure and again uses $N=2000$ with seed $42$.  It reports observed $|M|=20$, zero exact matches, and $7$ lower-tail samples at or below the observed $M$ size.  Thus the exact-shape count is $0$, the exact-shape empirical value is $0.0$, and the lower-tail value is $0.0035$.  The point of this row is narrower than a mechanism claim: even after preserving amino-acid classes, the finite shape remains difficult to recover by the recorded null procedure.  The packet therefore treats the curated standard-code table as the contact that supplies codon-to-label assignments, while the coordinate and closure computations organize those labels into a local finite pattern.

\paragraph{Predictive leave-out rows.}
The leave-one-code-out row evaluates $23$ code tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$.  The leave-one-codon-out row records $0$ anchors, $5$ boundary cases, $8$ high-coverage closure cases, $8$ predictable cases, and recovery rate $1.0$.  These rows make the finite object useful as a code-read probe: when one label or codon is withheld, the local boundary organization often recovers the withheld item within the recorded evaluation.  They still remain code-layer tests.  They do not observe translation in a ribosome, a folded protein, a physical admissibility condition, a function, a universal mechanism, or an organism-independent necessity.

\paragraph{Family and module structure.}
The family-stratified leave-one-code-out row lists $5$ families.  The hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial or plastid codes, $1.0$ for candidate-other rows, $1.0$ for mitochondrial rows, and $1.0$ for nuclear-ciliate rows.  The module-classification row assigns all $13$ codons of $R$ with no extra module codons, no missing codons, and no unassigned codons.  The modules are $\mathrm{AGR}=\{\mathrm{AGA},\mathrm{AGG}\}$, $\mathrm{ar\_lys}=\{\mathrm{AAA}\}$, $\mathrm{CUN}=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\}$, $\mathrm{ile\_met}=\{\mathrm{AUA}\}$, $\mathrm{SSN}=\{\mathrm{UCA},\mathrm{UUA}\}$, and $\mathrm{stop\_trp}=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}$.  Each module has predictable or boundary status in the recorded classification.  This is the strongest local reading of the row: the thirteen codons form a fully assigned boundary-tile packet, and its module decomposition is complete in the finite data.

\paragraph{Local boundary.}
The reality contact is a curated standard-code table assigning codons to amino-acid or stop labels.  That contact has exactly the right resolution for code-label readback and local split checks, but it has no structure measurement, no translation trace, no folding observation, no physical admissibility assay, and no function experiment.  Consequently the admissible conclusion is that window-six codon coordinates expose a statistically sharp local boundary tile in the standard-code label table.  The inadmissible conclusion would be to promote the same finite pattern to translation, folded structure, physical possibility, biological function, mechanism, or universality without an additional contact designed for that higher layer.  The packet is therefore ready as a self-contained code-read chapter: it names the thirteen-codon boundary, records the punctured-cube count $(13,16,4)$, reports the spectrum and null rows, and keeps all higher biological claims outside the scope of this evidence.