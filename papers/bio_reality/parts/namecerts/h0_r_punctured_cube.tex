\origin{ai}

\subsection{Punctured-cube codon boundary in window-six coordinates}
\label{sec:bior-reality-punctured-cube-codon-boundary}

\paragraph{Finite carrier and readback contact.}
This record treats the standard-code codon table as a finite reality contact at the code-read layer.  The carrier is the six-bit codon display $Q_6$, equipped only with coordinate, closure, spectrum, and relation data.  The external contact is a curated standard-code label table: it assigns codons to amino-acid or stop labels, and it supplies a readback bridge for label comparison.  It does not supply translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.  The local object isolated by the finite-row computation is the set
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
    &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The cardinality check gives $|R|=13$, and the punctured-cube certificate reports the face-vector
$$
\begin{aligned}
f(R)=(13,16,4).
\end{aligned}
$$
Thus the row-level claim is not merely that thirteen labels are interesting, but that the thirteen listed codons realize the same finite punctured-cube boundary shape under the chosen window-six coordinate display.

\paragraph{Shape and spectral witnesses.}
The spectral row attached to the same carrier gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The radial cubic check is numerically closed to tolerance, with polynomial value $-6.224354365258478\cdot 10^{-12}$.  The accompanying shape-significance run used $N=2000$ distinct null samples after $57377$ distinct-sample attempts and $55377$ duplicate retries.  It found one exact shape match and twenty-five samples with Jaccard score at least $0.9$, hence
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{J\geq 0.9}&=0.0125.
\end{aligned}
$$
These values support the finite-row reading that the local tile is non-generic relative to the sampled coordinate-preserving comparison class.  A second null preserving amino-acid class constraints reported observed $M$-size $20$, zero exact matches among $N=2000$ samples, and seven lower-tail samples with size at most the observed value.  The corresponding values were
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
The finite conclusion is therefore a local code-read boundary statement: the punctured-cube row and the surrounding $M$-shape are statistically constrained in the tested finite comparison regimes.

\paragraph{Module-level decomposition.}
The thirteen codons in $R$ are completely assigned by the module-classification row.  The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
The classification row reports $R$-size $13$, assigned count $13$, no extra module codons, no codons missing from the modules, and no unassigned codons.  Hence every element of the punctured-cube set is accounted for by the listed local modules.  The same row states that every module has a predictable or boundary interpretation, so the decomposition is not an after-the-fact naming of isolated rows; it is a complete finite partition of the observed boundary tile.

\paragraph{Leave-one tests.}
The leave-one-code-out row evaluated $23$ tables and $63$ total targets.  The baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary was $0.9841269841269841$.  By family, five families were evaluated: alternative yeast had hit rate above baseline $0.8333333333333334$, while bacterial plastid, candidate other, mitochondrial, and nuclear ciliate each had value $1.0$.  The leave-one-codon-out row reported anchor count $0$, boundary count $5$, closure-coverage-high count $8$, predictable count $8$, and recovery rate $1.0$.  These finite rows say that the local coordinate tile has strong code-read recovery behavior under the tested deletions, while still remaining a local tile rather than a biological mechanism.

\paragraph{Boundary of the claim.}
The claim supported here is exactly the following: in the six-bit codon display, the listed thirteen standard-code codons form a finite punctured-cube boundary tile with face-vector $(13,16,4)$, statistically non-generic shape behavior in the supplied null comparisons, complete module classification, and strong leave-one recovery at the label-read layer.  The curated table is a contact for codon-to-label readback only.  No translation process is observed by this contact.  No folded protein structure, structural order, physical admissibility, biological function, universal genetic-code mechanism, or necessity of BEDC geometry for DNA follows from this row.  Any such higher-layer statement would require a separate contact with its own finite rows and its own mismatch audit.