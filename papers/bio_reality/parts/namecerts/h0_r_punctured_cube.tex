\origin{ai}
\subsection{Punctured Codon Cube at the Code-Read Boundary}
\label{sec:bioreality-h0-r-punctured-cube}

\paragraph{Finite carrier and observed row.}
The carrier is the six-bit codon cube $Q_6$, read only as a finite coordinate display for standard-code labels.  The row selected by the punctured-cube probe is
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The finite audit records $|R|=13$, with the same thirteen codons listed independently under the cardinality check.  The face-count summary for the induced local tile is
$$
\begin{aligned}
f(R)=(13,16,4),
\end{aligned}
$$
so the row is not being used merely as an unordered exception list: it carries a concrete local face signature inside $Q_6$.  The phrase punctured cube means precisely this finite code-layer object: a local tile with thirteen label corners and a four-face residue pattern, not a biological necessity theorem.

\paragraph{Code-label readback.}
The only reality contact used here is the curated standard-code table, which assigns each codon to an amino-acid or stop label.  Therefore the admissible readback is restricted to the code layer: the row may be compared with the standard-code label table, and local singleton or three-point label classes may be checked against that table.  This contact does not observe translation realization, folded structure, physical admissibility, biological function, universal mechanism, or any claim that DNA must obey the window-six geometry.  In particular, an amino-acid label sequence is still a label sequence; it is not a folded protein structure.

\paragraph{Spectral coordinate check.}
The same finite coordinate package gives a spectral audit.  The recorded values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\cdot 10^{-12}$, recorded both as the cubic-polynomial value and as the radial-polynomial value.  Thus the coordinate computation is numerically coherent at the displayed tolerance: the spectral radius reported for the ambient $M$ object is $0.6752479308830213$, while the punctured row $R$ has its own smaller coordinate value $0.4758352843197002$.  These numbers certify only a finite-row coordinate relation; they do not supply a mechanism for biological realization.

\paragraph{Shape significance against finite nulls.}
Against the shape null with seed $42$, the audit used $N=2000$ sampled alternatives.  It required $57377$ distinct-sample attempts and observed $55377$ duplicate retries.  The exact shape appeared once, giving
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,
\end{aligned}
$$
and the high-Jaccard threshold $J\geq 0.9$ appeared $25$ times, giving
$$
\begin{aligned}
p_{J\geq 0.9} &= 0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null used the same $N=2000$ and seed $42$.  Its exact-match count was $0$, so the recorded exact-shape value was $0.0$.  The observed $M$ size was $20$, and the lower-tail count at or below that size was $7$, giving
$$
\begin{aligned}
p_{\mathrm{lower\ tail}} &= 0.0035.
\end{aligned}
$$
These finite nulls support the statement that the displayed local tile is atypical under the tested row-generating controls.  They do not by themselves identify a causal process or establish universality.

\paragraph{Predictive stress tests at the code layer.}
The leave-one-code-out audit evaluated $23$ tables and $63$ targets.  The baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate inside $M-T$ was $0.9365079365079365$, and the rate inside $M-T$ plus boundary was $0.9841269841269841$.  By family, the hit-rate-above-baseline values were $0.8333333333333334$ for alternative yeast and $1.0$ for bacterial-plastid, candidate-other, mitochondrial, and nuclear-ciliate families, across $5$ families.  The leave-one-codon-out audit found $0$ anchors, $5$ boundary codons, $8$ high-coverage closures, $8$ predictable codons, and recovery rate $1.0$.  These rows say that the finite tile has predictive stability for code-label recovery under the tested omissions.  They do not say that the codon table is generated by the tile in biology.

\paragraph{Module accounting.}
The module classification is complete for the thirteen codons in $R$: it reports $R$ size $13$, assigned count $13$, no extra module codons, no missing codons from the modules, and no unassigned codons.  The modules are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
Every module has either predictable or boundary status in the recorded classification.  This gives a local finite-row partition of the punctured-cube boundary, with singleton, two-point, three-point, and four-point components visible in the standard-code readback.

\paragraph{Local verdict and boundary.}
The finite evidence supports the code-read claim that the thirteen-codon row $R$ is a punctured-cube local tile in $Q_6$ with $f(R)=(13,16,4)$, coherent spectral coordinates, nontrivial null-model rarity, complete module accounting, and strong leave-one-out recovery at the label-table layer.  The claim stops there.  No translation event, folded structure, physical admissibility condition, biological function, universal mechanism, or global biological law follows from this chapter unless a separate reality contact supplies that higher-layer information.