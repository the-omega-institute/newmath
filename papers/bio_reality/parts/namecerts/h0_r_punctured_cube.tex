\origin{ai}

\subsection{Punctured Codon Cube Boundary Tile}
\label{sec:h0-r-punctured-cube}

\paragraph{Finite carrier.}
The record uses the standard-code codon table only as a curated label contact.  The BEDC carrier is the six-bit codon cube $Q_6$, read as a finite display of codon positions with local tile faces, singleton label corners, and three-point label classes.  In this coordinate display, the tested row $R$ is the following thirteen-codon set:
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The finite-row audit reports $|R|=13$ and the same thirteen expected codons in the independent cardinality check.  The punctured-cube check returns the face-vector signature
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4),
\end{aligned}
$$
so the local object being discussed is not an unspecified visual impression: it is a finite subcomplex-like boundary tile with thirteen displayed vertices, sixteen displayed edges, and four displayed two-faces under the chosen codon-window coordinate.

\paragraph{Coordinate readback.}
The biological readback is deliberately narrow.  The contact named here is a curated standard-code table assigning codons to amino-acid or stop labels.  That table tests code-layer label readback and local codon tile label splits.  It does not test translation realization, structural order, physical admissibility, folded protein structure, biological function, a universal mechanism, or any necessity claim saying that DNA must obey this BEDC geometry.  Thus the result is a code-read boundary record: the six-bit coordinate exposes a local pattern, while the amino-acid and stop labels remain external reality contacts supplied by the curated table.

\paragraph{Spectral row.}
The associated spectral summary gives a second finite invariant of the same coordinate construction.  The measured values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial polynomial residual is reported as $-6.224354365258478\times 10^{-12}$, the same value recorded for the cubic-polynomial satisfaction check.  At the precision of the finite computation, this places the spectral calculation inside numerical tolerance rather than leaving it as a qualitative shape label.

\paragraph{Shape significance.}
Against the reported finite null sample for the $M$-shape comparison, the run used $N=2000$ samples with seed $42$.  It required $57377$ distinct-sample attempts and $55377$ duplicate retries, which records a substantial collision burden in the sampled finite space.  The exact shape match count was $1$, giving
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,
\end{aligned}
$$
and the count with Jaccard similarity at least $0.9$ was $25$, giving
$$
\begin{aligned}
p_{\mathrm{Jaccard}\geq 0.9} &= 0.0125.
\end{aligned}
$$
These numbers support the local claim that the observed coordinate shape is uncommon under that sampled comparison.  They do not promote the shape into a law of translation or biological mechanism.

\paragraph{Amino-acid-class preserving null.}
The amino-acid-class preserving null model gives a stricter code-label control.  In $N=2000$ samples with seed $42$, the observed $M$ size is $20$, the exact-match count is $0$, and the lower-tail count at or below the observed $M$ size is $7$.  The reported values are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact\ shape}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
This is a finite-row comparison inside the label-preserving code layer.  It strengthens the local readback record but still does not supply a separate physical or functional contact.

\paragraph{Leave-one-code-out row.}
The code-level perturbation audit evaluated $23$ tables and $63$ total targets.  The baseline hit rate was $0.19821739130434785$.  The hit rate above baseline was $0.9841269841269841$, the hit rate in $M-T$ was $0.9365079365079365$, and the hit rate in $M-T$ plus boundary was $0.9841269841269841$.  By family, the five reported family rates above baseline were $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial-plastid, $1.0$ for candidate-other, $1.0$ for mitochondrial, and $1.0$ for nuclear-ciliate.  These row counts say that the local coordinate module has predictive stability for codon-label table perturbations.  They do not say that a translated molecule is produced, folded, admissible, or functional.

\paragraph{Leave-one-codon-out row.}
The codon-level perturbation audit reports $0$ anchors, $5$ boundary codons, $8$ predictable codons, closure coverage high at $8$, and recovery rate $1.0$.  The module classification is complete: the assigned count is $13$, $R$ has size $13$, and there are no missing module codons, no extra module codons, and no unassigned codons.  The six modules are
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every listed module has predictable or boundary status in the audit.  This is the most local form of the punctured-cube claim: each member of the thirteen-codon row is accounted for by the finite module partition under the tested coordinate rule.

\paragraph{Boundary verdict.}
The claim is therefore accepted only at the code-read layer.  The ready content is that the six-bit codon coordinate exposes a thirteen-codon punctured-cube boundary tile with face vector $(13,16,4)$, nontrivial sampled shape significance, a recorded spectral signature, strong leave-one-code-out recovery, and complete leave-one-codon-out module coverage.  The cannot-claim boundary is part of the mathematical record: no translation, no folding, no physical admissibility, no biological function, no mechanism, and no universality follows unless a separate reality contact supplies that higher layer.