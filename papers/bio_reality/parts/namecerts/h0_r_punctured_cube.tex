\subsection{The punctured-cube row of the standard-code readback}
\label{sec:biorality-h0-r-punctured-cube}
\origin{ai}

\paragraph{Finite carrier and contact.}
This row records a finite code-read phenomenon on the six-bit codon cube $Q_6$.  The carrier is the codon display with coordinates supplied by the window-six representation, and the only external contact used here is the curated standard genetic-code table.  That contact assigns each codon to an amino-acid or stop label, so it supports codon-label readback and local split checks.  It is not a mechanism, a translation realization, a folding observation, a physical-admissibility certificate, a function assay, or a universality theorem.  The finite subset detected in the row is
$$\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}$$
The independently recorded cardinality row agrees with the list:
$$\begin{aligned}
|R|&=13,\\
R_{\mathrm{cardinality}}&=13.
\end{aligned}$$
Thus the object under discussion is a thirteen-codon local row inside the codon cube, read back through standard-code labels and not promoted beyond that layer.

\paragraph{Punctured-cube incidence.}
The row is not merely a named set of codons.  Its finite incidence data match a punctured-cube tile: the verified $f$-vector is
$$\begin{aligned}
f(R)&=(13,16,4).
\end{aligned}$$
Here the first coordinate is the number of vertices in the detected row, the second is the number of local cube edges retained by the tile, and the third is the number of local square faces.  The word ``punctured'' is used only for this finite incidence pattern: the row behaves like a local cube-shaped tile with one corner or local completion absent in the codon-coordinate display.  No biological necessity is inferred from this shape.  The shape is a code-layer combinatorial observation attached to a label table, not a claim that DNA, translation, proteins, or cellular mechanisms obey this geometry as a global law.

\paragraph{Spectral check.}
The same finite object has a recorded spectral signature.  With $M$ denoting the complementary or comparison module used by the finite probe, the verified values are
$$\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}$$
The radial cubic check is numerically closed at the recorded tolerance:
$$\begin{aligned}
p_{\mathrm{radial}}(\mu)&=-6.224354365258478\cdot 10^{-12},\\
\rho_{\mathrm{recorded}}&=0.6752479308830213.
\end{aligned}$$
This gives an internal consistency check on the finite-row readback.  It does not convert the row into a physical spectrum of a molecule; it is the spectrum of the finite codon-coordinate construction used in the probe.

\paragraph{Null and shape controls.}
The punctured-cube shape was compared against finite null samples.  In the shape null, $N=2000$ samples were drawn with seed $42$.  The row had one exact shape match and twenty-five high-Jaccard matches:
$$\begin{aligned}
N&=2000,\\
\#\{\mathrm{exact\ shape\ matches}\}&=1,\\
\#\{J\geq 0.9\}&=25,\\
p_{\mathrm{exact}}&=0.0005,\\
p_{J\geq 0.9}&=0.0125.
\end{aligned}$$
The sampler recorded $57377$ distinct-sample attempts and $55377$ duplicate retries, so the row is being tested against a visibly constrained finite sampling space rather than against an informal background.  Under the amino-acid-class-preserving null, again with $N=2000$ and seed $42$, the observed module size was $20$, the exact shape count was $0$, and the lower-tail count at or below the observed module size was $7$:
$$\begin{aligned}
M_{\mathrm{obs}}&=20,\\
\#\{\mathrm{aa\ class\ exact\ shape}\}&=0,\\
\#\{\mathrm{lower\ tail}\leq M_{\mathrm{obs}}\}&=7,\\
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}$$
These numbers support a non-generic local code-read pattern in the stated finite tests.  They still do not supply a translation mechanism, a folding contact, physical admissibility, functional selection, or universality.

\paragraph{Leave-one readback tests.}
The row also has predictive finite-row behavior under leave-one probes.  Across $23$ evaluated code tables and $63$ total targets, the leave-one-code-out probe recorded
$$\begin{aligned}
h_{\mathrm{baseline}}&=0.19821739130434785,\\
h_{M\setminus T}&=0.9365079365079365,\\
h_{M\setminus T+\partial}&=0.9841269841269841,\\
h_{\mathrm{above\ baseline}}&=0.9841269841269841.
\end{aligned}$$
In the leave-one-codon-out probe, the recovery rate was $1.0$, with $8$ predictable codons at high closure coverage, $5$ boundary codons, and $0$ anchors:
$$\begin{aligned}
\#\{\mathrm{anchors}\}&=0,\\
\#\{\mathrm{boundary}\}&=5,\\
\#\{\mathrm{predictable}\}&=8,\\
\mathrm{recovery\ rate}&=1.0.
\end{aligned}$$
The family-level leave-one-code-out table splits the hit-rate-above-baseline row into five families: alternative yeast has $0.8333333333333334$, while bacterial plastid, candidate other, mitochondrial, and nuclear ciliate each have $1.0$.  This is evidence for a robust finite code-read tile under the stated row tests, not evidence for a general biological mechanism.

\paragraph{Module classification.}
The thirteen codons in $R$ are completely assigned by the recorded module classification.  The six modules are
$$\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}$$
The accounting row is exact:
$$\begin{aligned}
|R|&=13,\\
\#\{\mathrm{assigned}\}&=13,\\
\#\{\mathrm{missing\ from\ modules}\}&=0,\\
\#\{\mathrm{unassigned}\}&=0,\\
\#\{\mathrm{extra\ module\ codons}\}&=0.
\end{aligned}$$
Every module has predictable or boundary status in the recorded classification.  The module data are therefore a finite certificate that the punctured-cube row is not an accidental subset with dangling codons; it is fully accounted for by the local row decomposition.

\paragraph{Local verdict and boundary.}
The ready claim is the following finite statement: in the window-six codon-coordinate display, the standard-code readback contains a thirteen-codon row $R$ whose incidence vector is $(13,16,4)$, whose module classification is complete, whose spectral check is numerically closed at the displayed tolerance, and whose leave-one probes recover the row at the recorded rates.  The claim stops exactly there.  The curated standard-code table supplies label readback only.  No translation realization, folded protein structure, structural order, physical admissibility, biological function, molecular mechanism, or universal biological law follows from this chapter unless a separate reality contact supplies that higher-layer information.