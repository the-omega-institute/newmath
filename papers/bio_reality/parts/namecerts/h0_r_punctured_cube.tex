\subsection{The punctured codon cube as a local code-read tile}\label{subsec:h0-r-punctured-cube}
\origin{ai}

\paragraph{Finite carrier and reality contact.}
The finite carrier is the six-bit codon cube $Q_6$, read through the curated standard-code table as a codon-to-label contact. This contact is deliberately narrow: it supplies the standard-code label assigned to each codon, and it does not supply translation dynamics, folded structure, physical admissibility, biological function, a universal mechanism, or a law of biological necessity. The observed finite row for the local boundary set is
$$\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}$$
The independent cardinality row gives $|R|=13$, with the same thirteen codons. Thus the finite claim is not an inferred biological mechanism; it is the equality between the extracted local boundary row and a thirteen-point punctured-cube row inside the six-bit display of the standard-code table.

\paragraph{Punctured-cube certificate.}
The punctured-cube certificate records the face-count vector
$$\begin{aligned}
f(R)=(13,16,4).
\end{aligned}$$
Here the first coordinate counts the thirteen codon vertices in the row, while the remaining coordinates record the sixteen one-dimensional and four two-dimensional local tile incidences detected by the coordinate closure. The statement is therefore a finite combinatorial statement about a displayed subset of $Q_6$: the row has the codon membership listed above and has the face vector $(13,16,4)$. Nothing in this certificate asserts that DNA must obey this geometry as a biological law. The only supported readback is that, under the curated standard-code bridge, these codons occupy a locally coherent boundary tile at the code-read layer.

\paragraph{Spectral row.}
The same finite carrier has a recorded spectral summary. The $M$-side scalar is $\lambda_M=0.6752479308830213$, the $R$-side scalar is $\lambda_R=0.4758352843197002$, and the radial scalar is $\mu=3.051487585298128$. The polynomial residual row is
$$\begin{aligned}
P(\mu)=-6.224354365258478\cdot 10^{-12},
\end{aligned}$$
with the same value recorded as the cubic-polynomial and radial-polynomial residual. The spectral-radius row records $0.6752479308830213$, matching $\lambda_M$ within the supplied tolerance. These numbers are used only as finite diagnostics of the code-read tile. They do not identify a protein structure, a folding route, a physical energy model, or a selection mechanism.

\paragraph{Shape significance and null rows.}
Against the shape null row with $N=2000$ sampled alternatives, the run made $57377$ distinct-sample attempts and encountered $55377$ duplicate retries. It found $1$ exact shape match, giving $p_{\mathrm{exact}}=0.0005$, and $25$ high-Jaccard matches at threshold $0.9$, giving $p_{J\geq 0.9}=0.0125$. A second amino-acid-class-preserving null row used $N=2000$, found $0$ exact matches, and observed $7$ lower-tail samples at or below the observed $M$ size. That row records
$$\begin{aligned}
|M|&=20,\\
p_{\mathrm{aa\ class,exact}}&=0.0,\\
p_{\mathrm{aa\ class,lower\ tail}}&=0.0035.
\end{aligned}$$
These finite rows support the claim that the displayed shape is not a typical draw under the tested null procedures. They do not promote the tile beyond the code-read layer: significance of a local codon display is not evidence of translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.

\paragraph{Leave-one recovery rows.}
The leave-one-code-out row evaluated $23$ tables and $63$ targets. Its baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$. The hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus the boundary row was $0.9841269841269841$. The leave-one-codon-out row recorded $0$ anchors, $5$ boundary cases, $8$ high-coverage closure cases, $8$ predictable cases, and recovery rate $1.0$. These are finite prediction-and-recovery diagnostics for codon-label rows. They certify that the local coordinate closure is predictive at the code-read interface supplied by the curated table, not that the table is generated by the coordinate system as a biological necessity.

\paragraph{Family and module decomposition.}
The family-stratified leave-one-code-out row contains $5$ families. The hit rates above baseline are $0.8333333333333334$ for alternative yeast, $1.0$ for bacterial plastid, $1.0$ for candidate other, $1.0$ for mitochondrial, and $1.0$ for nuclear ciliate. The module classification assigns all thirteen $R$ codons and records no missing, extra, or unassigned codons:
$$\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}$$
The completeness row is $R_{\mathrm{size}}=13$, assigned count $13$, extra-module codons $\varnothing$, missing-from-modules $\varnothing$, and unassigned codons $\varnothing$. Thus every module has either predictable or boundary status in the finite code-read analysis.

\paragraph{Local verdict and cannot-claim boundary.}
The ready finite-row verdict is that $R$ is the thirteen-codon punctured-cube boundary tile listed above, with face vector $(13,16,4)$, nontrivial spectral diagnostics, significant shape rows under the supplied nulls, and complete module classification. The result is a local BioReality record at the code-read layer: the curated standard-code table is the reality contact, and the six-bit coordinate system is the display in which the local tile is measured. No translation realization, folded protein structure, structural order, physical admissibility, biological function, universal mechanism, or global biological law follows from this chapter unless a separate reality contact supplies that higher-layer information.