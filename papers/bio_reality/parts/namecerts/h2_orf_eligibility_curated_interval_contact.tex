\subsection{Curated interval contact for open-reading-frame eligibility}\label{subsec:h2-orf-eligibility-curated-interval-contact}
\origin{ai}

\paragraph{Question fixed by the row.}
This record concerns the layer $\mathrm{orf\_eligibility}$ for open-reading-frame eligibility as tested by start and stop codon enumeration against a curated interval annotation.  The row does not claim that the geometry of codon windows is itself a biological observation.  It asks a narrower question: after the start--stop classifier has been fixed without translation, structure, physical-admissibility, or function inputs, does a curated nucleotide-interval contact separate the positive CDS intervals from matched non-ORF decoys and boundary cases inside the supplied finite manifest?

\paragraph{Finite codon background.}
The codon substrate used by the packet is finite.  The motif row records
$$\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\\
&\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\},
\end{aligned}$$
with no missing and no extra entries relative to the motif union check.  The ambient set $M$ has cardinality $20$ and is exactly
$$\begin{aligned}
M=\{&\mathrm{AAA},\mathrm{AAG},\mathrm{ACA},\mathrm{ACG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\mathrm{CUA},\mathrm{CUC},\\
&\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}.
\end{aligned}$$
The saturation row further reports that $\mathrm{Sat}_{f3}(R)$ is $M$ with $\mathrm{ACA}$ and $\mathrm{ACG}$ absent, while the $f3$ boundary is empty.  Thus the codon-side packet is a closed finite enumeration, not an inferred biological mechanism.  The quotient-geometry check contributes the scalar shape data $M$-vector $(20,38,26,8,1)$, quotient vector $(10,14,6,1)$, quotient size $10$, and an empty product-decomposition residue.  These numbers provide the finite combinatorial coordinate used by the classifier, but they do not by themselves observe an ORF.

\paragraph{Prior window gate.}
Before the curated interval contact, the start--stop window row loaded one outside nucleotide-window manifest from the snapshot dated $2026$-$05$-$25$.  It classified $1$ input window as $1$ classified window, with scope restricted to external window start--stop enumeration only.  The row explicitly retained the output as $\mathrm{orf\_eligibility}$ only.  In particular, the classifier was not allowed to convert a start--stop window into realized translation, folded protein structure, physical admissibility, or biological function.

\paragraph{Curated interval observation.}
The interval-contact row loads $168$ rows from \texttt{tools/bio\_reality/data/curated\_orf\_interval\_contact\_dataset.json}.  The source is the NCBI RefSeq GenBank XML CDS interval annotation for \emph{Escherichia coli} K-12 MG1655 complete genome accession $\mathrm{NC\_000913.3}$, with snapshot date $2026$-$05$-$30$.  The required input classes are all present: boundary cases, matched non-ORF decoys, and positive curated ORFs.  The protocol was fixed before the biological annotation was inspected: the protocol time was $2026$-$05$-$29\,19{:}11{:}13+00{:}00$, the external observation time was $2026$-$05$-$30\,10{:}29{:}31+00{:}00$, classifier rules were frozen, held-out splits were frozen, input classes were frozen, baselines were predeclared, and the analysis used no translation or structure input.

\paragraph{Classifier and controls.}
The classifier is identified as \texttt{orf\_start\_stop\_window\_eligibility}; its rules were not modified during the contact.  The positive interval test accepted $72$ of $72$ curated ORF intervals, so the accept rate was $1.0$, with exact one-sided binomial upper-tail value $2.117582368135751\cdot 10^{-22}$.  The matched decoy test rejected $72$ of $72$ matched non-ORF decoys, so the reject rate was $1.0$, with the same exact one-sided binomial upper-tail value $2.117582368135751\cdot 10^{-22}$.  The predeclared pairwise comparison won $72$ of $72$ positive--decoy pairs, giving win rate $1.0$ and exact one-sided binomial upper-tail value $2.117582368135751\cdot 10^{-22}$.  Boundary cases were capped rather than promoted: among $24$ boundary cases, $0$ were promoted and the promoted fraction was $0.0$.

\paragraph{Held-out stability.}
The split stability check used $3$ held-out folds.  The left-locus fold \texttt{refseq\_nc\_000913\_left\_locus\_fold} passed with $24$ positives accepted at rate $1.0$ and $24$ decoys rejected at rate $1.0$.  The middle-locus fold \texttt{refseq\_nc\_000913\_middle\_locus\_fold} passed with the same $24/24$ positive acceptance and $24/24$ decoy rejection.  The right-locus fold \texttt{refseq\_nc\_000913\_right\_locus\_fold} also passed with $24$ positives, $24$ decoys, positive accept rate $1.0$, and decoy reject rate $1.0$.  The split data therefore agree with the aggregate positive, decoy, and pairwise tests inside the curated manifest.

\paragraph{Local conclusion.}
Within this finite row, the contact supports only this statement: a frozen start--stop-window eligibility classifier, when supplied with the specified RefSeq curated CDS interval annotation, separates $72$ positive curated ORF intervals from $72$ matched non-ORF decoys, leaves $24$ boundary cases unpromoted, and remains stable across three held-out locus folds.  The self-check row records $3$ checked gates and states that no codon geometry is promoted across biological layers.  Accordingly, the result is an interval-level eligibility contact, not a claim about translation, folding, thermodynamic or kinetic admissibility, molecular mechanism, cellular role, evolutionary conservation, organism-wide law, or universality beyond the rows of this dataset.  Any stronger biological assertion would require a further contact of the appropriate kind, with its own finite evidence and its own non-promotion checks.