\subsection{Curated Interval Contact for ORF Eligibility}\label{sec:h2-orf-eligibility-curated-interval-contact}
\origin{ai}

\paragraph{Layer and object.}
This record concerns open-reading-frame eligibility as a finite interval-contact question. The biological object is not protein production, not folding, and not function; it is the narrower assertion that a curated nucleotide interval can pass a start--stop enumeration test when the interval itself is supplied by an outside biological annotation. The operative layer is therefore $\mathsf{orf\_eligibility}$, and the local scope is $\mathsf{external\_orf\_interval\_contact\_only}$. The contact used here is a curated CDS-interval manifest from NCBI RefSeq GenBank XML for \emph{Escherichia coli} K-12 MG1655, accession $\mathrm{NC\_000913.3}$, with snapshot date $2026$-$05$-$30$. The loaded file is $\mathsf{tools/bio\_reality/data/curated\_orf\_interval\_contact\_dataset.json}$ and contains $168$ rows.

\paragraph{Finite geometric background retained by the packet.}
The lower finite codon geometry remains visible but does not by itself create an ORF claim. The motif-compressed set is
$$\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}$$
There are no missing or extra elements in that finite comparison. The larger finite set $M$ has cardinality $20$ and is exactly
$$\begin{aligned}
M=\{&\mathrm{AAA},\mathrm{AAG},\mathrm{ACA},\mathrm{ACG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\mathrm{CUA},\mathrm{CUC},\\
&\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}.
\end{aligned}$$
The saturation computation records
$$\begin{aligned}
\operatorname{Sat}_{f_3}(R)=M\setminus\{\mathrm{ACA},\mathrm{ACG}\},
\end{aligned}$$
with boundary $f_3$-residue empty. The quotient geometry of $M$ has $f$-vector $(20,38,26,8,1)$, and the quotient has $f$-vector $(10,14,6,1)$ with $10$ quotient points. These data explain why the packet has a finite combinatorial substrate, but the substrate is held below the biological layer until a curated interval is attached.

\paragraph{Protocol separation before observation.}
The finite classifier was fixed before the curated interval observation. The protocol was frozen at $2026$-$05$-$29\,19{:}11{:}13+00{:}00$, while the outside curated evidence was observed at $2026$-$05$-$30\,10{:}29{:}31+00{:}00$. The classifier identifier is $\mathsf{orf\_start\_stop\_window\_eligibility}$, and its rules were not modified after freezing. The analysis declares that it uses no translation or structure input; the baselines, held-out splits, and input classes were all fixed in advance. The required row classes are exactly $\mathsf{boundary\_case}$, $\mathsf{matched\_non\_orf\_decoy}$, and $\mathsf{positive\_curated\_orf}$. This is the point at which the record moves from a mere boundary question to a tested interval-contact record: the geometry classifies windows only after the curated biological interval is presented as input.

\paragraph{Start--stop enumeration contact.}
A preceding start--stop-window check loaded one finite external nucleotide-window manifest with $1$ input window, and all $1$ of those windows were classified by start--stop enumeration. That check was explicitly scoped as $\mathsf{external\_window\_start\_stop\_enumeration\_only}$ and carried the local output $\mathsf{orf\_eligibility\_only}$. The present contact is stronger because it uses the $168$-row curated interval dataset rather than a single window, but it preserves the same layer discipline: start and stop codon enumeration can test interval eligibility, not translation realization or downstream biological activity.

\paragraph{Positive, decoy, and boundary rows.}
The curated interval run separates three finite populations. The positive curated-ORF class has $72$ accepted rows out of $72$, so the observed accept rate is $1.0$. Under the exact one-sided binomial statistic recorded by the run, the upper-tail probability is
$$\begin{aligned}
p=2.117582368135751\times 10^{-22}.
\end{aligned}$$
The matched non-ORF decoy class has $72$ rejected rows out of $72$, hence reject rate $1.0$, with the same recorded one-sided binomial upper-tail probability
$$\begin{aligned}
p=2.117582368135751\times 10^{-22}.
\end{aligned}$$
The predeclared paired baseline comparison has $72$ wins in $72$ pairs, win rate $1.0$, again with
$$\begin{aligned}
p=2.117582368135751\times 10^{-22}.
\end{aligned}$$
The boundary control contains $24$ cases, none of which were promoted: $0$ promoted out of $24$, promoted fraction $0.0$. Thus the contact does not merely accept positives; it also rejects matched decoys and prevents ambiguous boundary rows from becoming biological claims.

\paragraph{Held-out split stability.}
The held-out analysis uses $3$ splits. In the left-locus split $\mathsf{refseq\_nc\_000913\_left\_locus\_fold}$, $24$ positives have accept rate $1.0$ and $24$ decoys have reject rate $1.0$. In the middle-locus split $\mathsf{refseq\_nc\_000913\_middle\_locus\_fold}$, the same counts and rates occur: $24$ positives at accept rate $1.0$ and $24$ decoys at reject rate $1.0$. In the right-locus split $\mathsf{refseq\_nc\_000913\_right\_locus\_fold}$, again $24$ positives are accepted at rate $1.0$ and $24$ decoys are rejected at rate $1.0$. All three splits are marked as passing. This supplies a finite stability check over the curated interval rows rather than a single aggregate count.

\paragraph{Local verdict.}
The record is ready at the ORF-eligibility layer. Its finite evidence consists of the $168$ curated rows, the $72/72$ positive acceptances, the $72/72$ matched decoy rejections, the $72/72$ paired baseline wins, the $0/24$ boundary promotions, and the three held-out splits with perfect positive and decoy rates. The self-check records $3$ checked gates and states that no codon geometry is promoted across biological layers. Consequently the claim established here is only this: under the frozen start--stop-window classifier, curated RefSeq CDS intervals in this finite manifest pass the ORF-eligibility contact while matched decoys and boundary rows are controlled.

\paragraph{Scope boundary.}
Nothing in this chapter establishes translation, peptide realization, folded molecular structure, physical admissibility, biochemical mechanism, biological function, organism-level fitness, or a universal law over genomes. Those stronger assertions would require a separate contact whose data are capable of testing the relevant layer. The RefSeq annotation remains curated biological input to this experiment; it is not kernel content and it is not replaced by the finite codon geometry. The chapter therefore records a successful finite interval-contact test and keeps all higher biological readings outside the local conclusion.