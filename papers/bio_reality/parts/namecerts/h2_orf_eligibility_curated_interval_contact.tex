\subsection{Curated Interval Contact for ORF Eligibility}\label{subsec:h2-orf-eligibility-curated-interval-contact}
\origin{ai}

\paragraph{Claim and evidential role.}
The claim indexed by \texttt{h2.orf\_eligibility.curated\_interval\_contact} records a finite contact test for open-reading-frame eligibility from start and stop codon enumeration.  Its biological object is not translation, protein existence, folding, molecular admissibility, or function.  It is the narrower statement that a frozen start--stop interval classifier, when supplied with a curated interval annotation contact, separates curated ORF intervals from matched non-ORF decoys and does not promote boundary cases.  The layer is therefore \texttt{orf\_eligibility}, with scope \texttt{external\_orf\_interval\_contact\_only}.  The contact source is the NCBI RefSeq GenBank XML CDS interval annotation for \emph{Escherichia coli} K-12 MG1655 complete genome accession NC\_000913.3, with snapshot date 2026-05-30.

\paragraph{Frozen protocol.}
The protocol was fixed before the external observation was read: the protocol timestamp is 2026-05-29T19:11:13+00:00, and the curated biological evidence was observed at 2026-05-30T10:29:31+00:00.  The input classes, classifier rules, held-out splits, and baselines were all declared as fixed.  The analysis also records that no translation or structural input was used.  The classifier identifier is \texttt{orf\_start\_stop\_window\_eligibility}, and the classifier rule-change flag is false.  Thus the contact test is a finite-row interval audit against curated annotation, not a procedure tuned after observing the interval labels.

\paragraph{Finite data.}
The curated interval dataset was loaded from \texttt{tools/bio\_reality/data/curated\_orf\_interval\_contact\_dataset.json}.  It contains $168$ rows, with required input classes
$\texttt{boundary\_case}$, $\texttt{matched\_non\_orf\_decoy}$, and $\texttt{positive\_curated\_orf}$.  The positive interval test has $n=72$, accepted $72$, accept rate $1.0$, and one-sided exact binomial upper-tail value $2.117582368135751\times 10^{-22}$.  The matched decoy rejection test has $n=72$, rejected $72$, reject rate $1.0$, and the same one-sided exact binomial value $2.117582368135751\times 10^{-22}$.  The predeclared paired baseline comparison has $72$ wins out of $72$ pairs, win rate $1.0$, and one-sided exact binomial value $2.117582368135751\times 10^{-22}$.  Boundary controls have $n=24$, promoted $0$, and promoted fraction $0.0$.

\paragraph{Held-out stability.}
The held-out split audit has three folds.  On \texttt{refseq\_nc\_000913\_left\_locus\_fold}, the positive count is $24$ with positive accept rate $1.0$, and the decoy count is $24$ with decoy reject rate $1.0$.  The same finite profile occurs on \texttt{refseq\_nc\_000913\_middle\_locus\_fold} and \texttt{refseq\_nc\_000913\_right\_locus\_fold}.  Each split is marked passed.  Equivalently, the recorded split table is summarized by
$$
\begin{aligned}
\text{split count} &= 3,\\
\text{positive rows per split} &= 24,\\
\text{decoy rows per split} &= 24,\\
\text{positive accept rate per split} &= 1.0,\\
\text{decoy reject rate per split} &= 1.0.
\end{aligned}
$$
This is a stability statement over the supplied curated-row partition only.  It is not a claim that all genomic windows, all organisms, or all annotation systems obey the same pattern.

\paragraph{Boundary seed before contact.}
A separate finite row records the preceding start--stop enumeration boundary at \texttt{h2.orf.start\_stop.window\_eligibility}.  That row loaded one external nucleotide-window manifest dated 2026-05-25, classified $1$ input window, and explicitly scoped the result to \texttt{external\_window\_start\_stop\_enumeration\_only}.  Its own guard says \texttt{orf\_eligibility\_only}; it does not convert geometry into translation.  The present curated-interval row is the additional contact that tests ORF eligibility against curated biological intervals.  It does not erase the earlier boundary: without such a curated interval, transcript, or sequence-contact input, a start--stop window remains only an eligibility question.

\paragraph{Codon-set context.}
The lower-level finite codon rows provide the combinatorial substrate used elsewhere in the packet.  The motif-compression row records
$$
\begin{aligned}
R=\{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\texttt{UAA},\texttt{UAG},\\
&\texttt{UCA},\texttt{UGA},\texttt{UUA}\},
\end{aligned}
$$
with no missing and no extra elements.  The medium set is recorded as
$$
\begin{aligned}
M=\{&\texttt{AAA},\texttt{AAG},\texttt{ACA},\texttt{ACG},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{AUG},\texttt{CUA},\texttt{CUC},\\
&\texttt{CUG},\texttt{CUU},\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UCG},\texttt{UGA},\texttt{UGG},\texttt{UUA},\texttt{UUG}\},
\end{aligned}
$$
with cardinality $20$.  The saturation row records $\mathrm{Sat}_{f3}(R)=M\setminus\{\texttt{ACA},\texttt{ACG}\}$ and an empty $f3$ boundary.  The quotient-geometry row records $|\pi_M|=10$, $M$-vector $(20,38,26,8,1)$, and quotient vector $(10,14,6,1)$.  These finite codon facts are admissible as enumeration and quotient facts.  They are not biological interval observations by themselves.

\paragraph{Local mathematical reading.}
The contact row may be read as a finite predicate test on a labeled row set $D$ with $|D|=168$.  The row classes partition the relevant audit into positive curated intervals, matched non-ORF decoys, and boundary cases.  The recorded outcomes are
$$
\begin{aligned}
\#\{\text{accepted positive intervals}\} &= 72,\\
\#\{\text{positive intervals}\} &= 72,\\
\#\{\text{rejected matched decoys}\} &= 72,\\
\#\{\text{matched decoys}\} &= 72,\\
\#\{\text{promoted boundary cases}\} &= 0,\\
\#\{\text{boundary cases}\} &= 24.
\end{aligned}
$$
The three exact one-sided binomial values attached to the positive, decoy, and paired-baseline tests are each $2.117582368135751\times 10^{-22}$.  The statistic reported by the row is therefore not a vague qualitative fit: it is a matched finite-row separation with explicit counts, predeclared pairing, and stable held-out folds.

\paragraph{What the row does not authorize.}
The interval contact does not establish translation, folded protein structure, physical admissibility, biological function, mechanism, or universality.  It also does not classify windows outside the supplied curated dataset, and the RefSeq annotation contact remains a curated biological input rather than kernel content of the mathematical coordinate system.  The self-check row reinforces this boundary: it records $3$ checked gates and states that no codon geometry is promoted across biological layers.  Consequently, the local conclusion is only that the frozen ORF-eligibility classifier has a finite curated-interval contact with $72/72$ positive acceptance, $72/72$ matched decoy rejection, $0/24$ boundary promotion, and three passed held-out splits.  Any stronger biological claim would require a separate contact designed for that stronger layer.