\origin{ai}

\subsection{Curated interval contact for ORF-boundary eligibility}
\label{subsec:h2-orf-eligibility-curated-interval-contact}

\paragraph{Question fixed by the row.}
This packet concerns only open-reading-frame eligibility as a finite start--stop interval test.  The biological object is not a protein, not a transcript abundance measurement, and not a folding or function assertion.  It is the narrower question whether a frozen start/stop-window classifier, using only the declared boundary geometry, agrees with an external curated interval annotation on a finite contact table.  The external contact is the NCBI RefSeq GenBank XML CDS interval record for \textit{Escherichia coli} K--12 MG1655 complete genome accession NC\_000913.3, with snapshot date 2026-05-30.  The dataset path recorded for the run is \texttt{tools/bio\_reality/data/curated\_orf\_interval\_contact\_dataset.json}, and the table has 168 rows.

\paragraph{Codon-level finite background.}
The finite codon substrate used by the boundary classifier was already closed as an enumerated object before the interval contact was evaluated.  The motif set $R$ is exactly
$\{\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}$,
with no missing and no extra elements in the motif-compression check.  The ambient set $M$ has cardinality $20$ and is the displayed union of the WNR and CUN finite families:
$\{\mathrm{AAA},\mathrm{AAG},\mathrm{ACA},\mathrm{ACG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}$.
The $f_3$ saturation check records
$$
\begin{aligned}
\operatorname{Sat}_{f_3}(R)={}&\{\mathrm{AAA},\mathrm{AAG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\},
\end{aligned}
$$
so this saturation is $M$ with $\mathrm{ACA}$ and $\mathrm{ACG}$ absent, and the recorded $f_3$ boundary remainder is empty.  The same finite object has $f$-vector $(20,38,26,8,1)$, while the quotient geometry has size $10$ at the vertex level and $f$-vector $(10,14,6,1)$.  These numbers are used here only to pin down the finite classifier substrate; they do not by themselves supply an observed biological interval.

\paragraph{Frozen protocol and contact table.}
The protocol was fixed before the external observation: the row records protocol freezing at 2026-05-29T19:11:13+00:00 and external evidence observation at 2026-05-30T10:29:31+00:00.  The input classes, held-out splits, baselines, and classifier rules were all frozen, and the analysis was declared to use no translation or structure input.  The classifier identifier is \texttt{orf\_start\_stop\_window\_eligibility}; the run records \texttt{classifier\_rules\_modified=false}.  The required classes present in the table are \texttt{boundary\_case}, \texttt{matched\_non\_orf\_decoy}, and \texttt{positive\_curated\_orf}.  A separate one-window start/stop enumeration check, with snapshot date 2026-05-25, records one external nucleotide window loaded and one input window classified, again only within the scope of external-window start/stop enumeration.

\paragraph{Finite acceptance and rejection statistics.}
On the curated interval contact, the positive class has $72$ accepted examples out of $72$, for acceptance rate $1.0$.  The corresponding exact one-sided binomial upper-tail value is $2.117582368135751\times 10^{-22}$.  The matched non-ORF decoy class has $72$ rejected examples out of $72$, for rejection rate $1.0$, with the same recorded binomial value $2.117582368135751\times 10^{-22}$.  The predeclared paired comparison has $72$ wins in $72$ pairs, win rate $1.0$, and the same exact one-sided binomial upper-tail value.  The boundary control is also finite and explicit: $24$ boundary cases were tested, $0$ were promoted, and the promoted fraction is $0.0$.

\paragraph{Held-out split stability.}
The contact table is also stable under the three declared held-out folds.  The left-locus fold \texttt{refseq\_nc\_000913\_left\_locus\_fold} passes with $24$ positives accepted at rate $1.0$ and $24$ decoys rejected at rate $1.0$.  The middle-locus fold \texttt{refseq\_nc\_000913\_middle\_locus\_fold} has the same $24$ positive and $24$ decoy counts, again with rates $1.0$ and $1.0$.  The right-locus fold \texttt{refseq\_nc\_000913\_right\_locus\_fold} repeats the same finite outcome.  Thus the row is not merely a pooled success statement; it records matched positive/decoy tests, a paired baseline sign-test advantage, a non-promotion boundary cap, and three frozen held-out partitions.

\paragraph{Local conclusion.}
The contact upgrades the seed from a contact-gap boundary record to a finite external-annotation agreement statement for ORF interval eligibility on this dataset.  The verified scope is \texttt{external\_orf\_interval\_contact\_only}.  Within that scope, the frozen geometry-only classifier separates the $72$ curated positives from the $72$ matched decoys, leaves $24$ boundary rows unpromoted, and remains stable across the three declared folds.  This is an interval-eligibility result and no more: it does not establish translation, folding, physical admissibility, biological function, molecular mechanism, a global rule for genomes, or classification outside the 168-row contact table.  The RefSeq annotation remains an external curated biological input rather than content generated by the finite codon geometry itself.