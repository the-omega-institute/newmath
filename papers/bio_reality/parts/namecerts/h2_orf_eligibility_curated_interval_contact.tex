\subsection{Curated Interval Contact for ORF Eligibility}\label{subsec:h2-orf-eligibility-curated-interval-contact}
\origin{ai}

\paragraph{Finite packet.}
This record concerns open-reading-frame eligibility as a finite biological-annotation contact, not as an intrinsic consequence of codon geometry. The starting boundary packet only says that a start/stop-window classifier has something to test once an observed sequence interval is supplied. Its minimal carrier is therefore a waiting state: before the curated interval is attached, the admissible assertion is only that a contact is required. The contact used here is the NCBI RefSeq GenBank XML CDS interval annotation for \emph{Escherichia coli} K-12 MG1655 complete genome accession NC\_000913.3, with snapshot date 2026-05-30. The finite table loaded from \texttt{tools/bio\_reality/data/curated\_orf\_interval\_contact\_dataset.json} has $168$ rows and contains the three required input classes $\{\text{boundary\_case},\text{matched\_non\_orf\_decoy},\text{positive\_curated\_orf}\}$.

\paragraph{Codon-level background held fixed.}
The lower finite codon data used by the packet are already closed as explicit row lists. The motif-compression residue set is
$R=\{\text{AAA},\text{AGA},\text{AGG},\text{AUA},\text{CUA},\text{CUC},\text{CUG},\text{CUU},\text{UAA},\text{UAG},\text{UCA},\text{UGA},\text{UUA}\}$,
with no missing and no extra elements in the checked motif union. The larger twenty-element set is
$M=\{\text{AAA},\text{AAG},\text{ACA},\text{ACG},\text{AGA},\text{AGG},\text{AUA},\text{AUG},\text{CUA},\text{CUC},\text{CUG},\text{CUU},\text{UAA},\text{UAG},\text{UCA},\text{UCG},\text{UGA},\text{UGG},\text{UUA},\text{UUG}\}$,
and the recorded identity is $M=\mathrm{WNR}\cup\mathrm{CUN}$ with cardinality $20$. The $f_3$ saturation check gives
$\mathrm{Sat}_{f_3}(R)=M\setminus\{\text{ACA},\text{ACG}\}$,
namely $18$ elements, and the $f_3$ boundary is empty. The quotient-geometry check records the $M$ face vector $(20,38,26,8,1)$, quotient face vector $(10,14,6,1)$, and quotient size $10$. These finite codon facts constrain the coordinate environment of the classifier, but they are not themselves biological interval observations.

\paragraph{Protocol separation.}
The curated contact was evaluated only after a frozen protocol. The protocol was fixed at 2026-05-29T19:11:13+00:00, while the external evidence was observed at 2026-05-30T10:29:31+00:00. The row states record that the classifier rules, input classes, held-out splits, and baselines were fixed before the observation, and that the analysis used no translation or protein-structure input. The classifier identifier is \texttt{orf\_start\_stop\_window\_eligibility}; its rules were not modified during the contact evaluation. A separate finite start/stop-window enumeration row records one loaded external window from snapshot date 2026-05-25, with all $1$ input window classified in the scope \texttt{external\_window\_start\_stop\_enumeration\_only}. That earlier enumeration is weaker than the interval contact in this subsection: it classifies boundary windows, whereas the present row checks curated positive intervals against matched non-ORF decoys and boundary cases.

\paragraph{Observed finite performance.}
The curated interval table contains $72$ positive curated ORF intervals, $72$ matched non-ORF decoys, and $24$ boundary cases. All $72$ positive intervals were accepted, giving accept rate $1.0$. All $72$ matched decoys were rejected, giving reject rate $1.0$. The exact one-sided binomial upper-tail value recorded for the positive interval test is $2.117582368135751\times 10^{-22}$, and the same value is recorded for the matched decoy rejection test. The predeclared paired baseline comparison has $72$ wins in $72$ pairs, hence win rate $1.0$, with the same one-sided binomial value.
$$
\begin{aligned}
 n_{+}&=72, & a_{+}&=72, & \widehat p_{+}&=1.0,\\
 n_{-}&=72, & r_{-}&=72, & \widehat p_{-}&=1.0,\\
 n_{\mathrm{pair}}&=72, & w_{\mathrm{pair}}&=72, & \widehat p_{\mathrm{pair}}&=1.0,\\
 p_{\mathrm{binom}}&=2.117582368135751\times 10^{-22}.&&&
\end{aligned}
$$
The boundary-control clause records $24$ boundary cases with $0$ promotions, so the promoted fraction is $0.0$. Thus the table supplies a finite contact for this dataset: positive curated intervals pass the frozen start/stop-window eligibility classifier, matched decoys fail it, and boundary cases remain unpromoted.

\paragraph{Held-out stability.}
The same finite behavior persists over the three held-out splits. In \texttt{refseq\_nc\_000913\_left\_locus\_fold}, $24$ positives have accept rate $1.0$ and $24$ decoys have reject rate $1.0$. In \texttt{refseq\_nc\_000913\_middle\_locus\_fold}, $24$ positives again have accept rate $1.0$ and $24$ decoys have reject rate $1.0$. In \texttt{refseq\_nc\_000913\_right\_locus\_fold}, the same $24$-and-$24$ split passes with positive accept rate $1.0$ and decoy reject rate $1.0$. The split count is therefore $3$, and every listed split passes under the frozen protocol.

\paragraph{Local interpretation.}
The appropriate conclusion is narrow. The RefSeq interval contact converts the earlier waiting-state ORF-eligibility seed into a finite dataset-level eligibility result for the loaded \emph{E. coli} NC\_000913.3 CDS interval manifest. It does not say that the codon coordinate set $M$, the motif set $R$, the quotient geometry, or start/stop-window enumeration alone observes an ORF. It says that, after the curated interval annotation is supplied as a biological contact, the frozen geometry-only classifier separates the listed positives from the listed matched decoys and refuses to promote the listed boundary cases.

\paragraph{Local non-promotion rule.}
No translation event follows from this row. No folded protein structure follows from this row. No physical admissibility, biochemical function, mechanism, organism-wide rule, or universal biological law follows from this row. The matched decoy and boundary controls do not classify windows outside the loaded dataset. The NCBI RefSeq annotation remains an external curated biological contact rather than kernel content of the codon-coordinate construction. Any stronger biological statement must be supplied by a separate contact that directly observes the stronger layer.