\origin{ai}

\subsection{Curated interval contact for ORF eligibility}
\label{subsec:h2-orf-eligibility-curated-interval-contact}

\paragraph{Question fixed by the row.}
This record concerns only the layer called \emph{open-reading-frame eligibility}: whether a finite nucleotide interval, when checked against a fixed start--stop enumeration rule, is accepted as an eligible ORF interval in the supplied curated annotation table. The biological object is not a translated product, not a folded molecule, and not a physical or functional assertion. The row therefore has a narrow form: a previously frozen start--stop-window classifier is brought into contact with curated CDS intervals from the \emph{Escherichia coli} K-12 MG1655 RefSeq genome accession NC\_000913.3, snapshot date 2026-05-30. The contact table has 168 rows at path \texttt{tools/bio\_reality/data/curated\_orf\_interval\_contact\_dataset.json}. Its input classes are exactly the three classes needed for this test: \texttt{positive\_curated\_orf}, \texttt{matched\_non\_orf\_decoy}, and \texttt{boundary\_case}.

\paragraph{Frozen protocol and permitted input.}
The protocol was fixed before the curated interval observations were inspected: the protocol time was 2026-05-29T19:11:13+00:00 and the external observation time was 2026-05-30T10:29:31+00:00. The classifier identifier was \texttt{orf\_start\_stop\_window\_eligibility}, and the audit records \texttt{classifier\_rules\_modified=false}. The analysis used no translation or protein-structure input, the baselines were predeclared, the input classes were fixed, and the held-out splits were fixed. Thus the row tests a geometry-only interval classifier against a curated biological interval table; it does not permit the classifier to learn from later biological annotations or from protein-level evidence.

\paragraph{Codon-level coordinate data retained in the packet.}
The finite codon table used by the surrounding coordinate record is explicit. The motif-compressed set is
$R=\{\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}$,
with no missing or extra elements in the compression check. The 20-element set $M$ is
$\{\texttt{AAA},\texttt{AAG},\texttt{ACA},\texttt{ACG},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{AUG},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UCG},\texttt{UGA},\texttt{UGG},\texttt{UUA},\texttt{UUG}\}$,
and the audit identifies this same set as $\mathrm{WNR}\cup\mathrm{CUN}$ with cardinality 20. The saturation check gives $\mathrm{Sat}_{f3}(R)=M\setminus\{\texttt{ACA},\texttt{ACG}\}$ and an empty $f3$ boundary. The associated finite geometry controls report the $M$ vector $(20,38,26,8,1)$, quotient vector $(10,14,6,1)$, and quotient size $10$.

\paragraph{Finite outcome table.}
The curated-interval contact has three measured components. First, the positive curated intervals were all accepted: $72$ accepted out of $72$, with one-sided binomial upper-tail value $2.117582368135751\times 10^{-22}$. Second, the matched non-ORF decoys were all rejected: $72$ rejected out of $72$, with the same one-sided binomial value $2.117582368135751\times 10^{-22}$. Third, the paired baseline comparison records $72$ wins in $72$ pairs, win rate $1.0$, again with one-sided binomial value $2.117582368135751\times 10^{-22}$. In compact form, the principal finite counts are
$$
\begin{aligned}
N_{\mathrm{rows}}&=168,\\
N_{\mathrm{positive}}&=72,\qquad A_{\mathrm{positive}}=72,\\
N_{\mathrm{decoy}}&=72,\qquad R_{\mathrm{decoy}}=72,\\
N_{\mathrm{boundary}}&=24,\qquad P_{\mathrm{boundary}}=0.
\end{aligned}
$$
The boundary rows are important: all $24$ boundary cases remained unpromoted, so the promoted fraction was $0.0$. This prevents the contact from being read as a rule that classifies arbitrary nearby windows outside the supplied table.

\paragraph{Held-out split stability.}
The same pattern is recorded across three held-out folds. In \texttt{refseq\_nc\_000913\_left\_locus\_fold}, there were $24$ positives accepted at rate $1.0$ and $24$ decoys rejected at rate $1.0$. The middle fold has the same counts and rates, and the right fold also has the same counts and rates. Each split is marked \texttt{passed=true}. Thus the row is not merely a pooled count: the finite contact remains stable under the three predeclared locus folds.

\paragraph{Local interpretation.}
The conclusion licensed by this chapter is exactly an interval-level eligibility conclusion for this curated contact: under the frozen start--stop enumeration classifier, the positive curated CDS intervals in the table are accepted, the matched decoys are rejected, the boundary cases are not promoted, and the three held-out folds preserve the same behavior. This is a contact with curated biological annotation, not an expansion of the codon-coordinate table into biological mechanism. The self-check records three checked gates and states that no codon geometry is promoted across biological layers.

\paragraph{Boundary of the claim.}
No translation event follows from this row. No folded structure, physical admissibility, molecular function, regulatory mechanism, organism-level role, or universal biological law follows from this row. The RefSeq GenBank XML CDS intervals are curated biological annotation inputs to this experiment, not kernel content and not a proof that any window outside the 168-row table is classified. A separate contact would be required for translation, protein structure, physical chemistry, function, mechanism, or broader biological scope. The present result is therefore a sharply delimited ORF-eligibility contact: strong within the frozen finite table, silent beyond it.