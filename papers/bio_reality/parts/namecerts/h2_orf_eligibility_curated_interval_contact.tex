\origin{ai}

\subsection{Curated ORF Interval Contact for Start--Stop Eligibility}
\label{subsec:h2-orf-eligibility-curated-interval-contact}

\paragraph{Question.}
This record tests whether a start--stop enumeration rule can make a finite, externally checked claim about open-reading-frame eligibility once it is supplied with curated nucleotide intervals. The object under examination is not translation, protein formation, or function. It is the narrower predicate that a window has the start--stop pattern expected of an ORF candidate under a fixed enumeration rule, and that this predicate is evaluated against curated interval annotations rather than inferred from codon geometry alone.

\paragraph{Finite codon substrate.}
The preceding finite codon calculations fix the combinatorial substrate on which the ORF-window rule is evaluated. The motif-compressed set is
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The verified ambient list has cardinality $20$ and is
$$
\begin{aligned}
M=\{&\mathrm{AAA},\mathrm{AAG},\mathrm{ACA},\mathrm{ACG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\mathrm{CUA},\mathrm{CUC},\\
&\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}.
\end{aligned}
$$
The same finite run verifies that $M$ is the union of the WNR and CUN classes, and the third saturation step satisfies
$$
\begin{aligned}
\mathrm{Sat}_{f_3}(R)=M\setminus\{\mathrm{ACA},\mathrm{ACG}\}.
\end{aligned}
$$
Its recorded boundary is empty, so the finite codon calculation has no remaining $f_3$ boundary elements to add. The quotient calculation gives an $M$-level vector $(20,38,26,8,1)$, a quotient vector $(10,14,6,1)$, and quotient size $10$. These numbers specify the enumerative geometry used by the classifier; they do not by themselves assert that any biological molecule is translated or functional.

\paragraph{Protocol.}
The interval-contact protocol was fixed before the curated observations were inspected. The protocol time is recorded as $2026$-$05$-$29\,19{:}11{:}13+00{:}00$, while the external annotation evidence was observed at $2026$-$05$-$30\,10{:}29{:}31+00{:}00$. The classifier identifier is \texttt{orf\_start\_stop\_window\_eligibility}, and the row states that its rules were not modified after the protocol was fixed. The analysis used no translation or structural input, the baselines were predeclared, the held-out splits were fixed, and the admissible input classes were fixed as boundary cases, matched non-ORF decoys, and positive curated ORFs.

\paragraph{Curated interval contact.}
The attached dataset is \texttt{tools/bio\_reality/data/curated\_orf\_interval\_contact\_dataset.json}. It contains $168$ rows from NCBI RefSeq GenBank XML CDS intervals for \emph{Escherichia coli} K-12 MG1655 complete genome accession NC\_000913.3, with snapshot date $2026$-$05$-$30$. The finite row separates three roles. There are $72$ positive curated ORF intervals, $72$ matched non-ORF decoys, and $24$ boundary cases. The one-window start--stop enumeration run was previously classified at scope \texttt{external\_window\_start\_stop\_enumeration\_only}, with $1$ loaded external window and $1$ classified input window. The present interval-contact row expands that setting to curated intervals while keeping the scope at \texttt{external\_orf\_interval\_contact\_only}.

\paragraph{Observed statistics.}
The positive curated intervals are accepted in all $72$ cases, so the accept rate is $1.0$. The matched decoys are rejected in all $72$ cases, so the reject rate is $1.0$. The reported exact one-sided binomial upper-tail value is
$$
\begin{aligned}
p = 2.117582368135751\times 10^{-22}
\end{aligned}
$$
for the positive interval statistic and the same value for the matched-decoy rejection statistic. The predeclared paired baseline comparison records $72$ wins out of $72$ pairs, with win rate $1.0$ and the same upper-tail value $2.117582368135751\times 10^{-22}$. The boundary-case control records $24$ cases, $0$ promotions, and promotion fraction $0.0$.

\paragraph{Held-out stability.}
The held-out split check contains three folds. In \texttt{refseq\_nc\_000913\_left\_locus\_fold}, there are $24$ positives and $24$ decoys, with positive accept rate $1.0$ and decoy reject rate $1.0$. The middle-locus fold has the same counts and rates, and the right-locus fold also has $24$ positives, $24$ decoys, positive accept rate $1.0$, and decoy reject rate $1.0$. All three held-out splits are marked as passed. Thus the finite evidence is not a single aggregate count only; it is also stable across the three recorded locus folds.

\paragraph{Local conclusion.}
Within this finite row, the start--stop eligibility classifier has made contact with a curated interval table and separates the recorded positive intervals from matched decoys with perfect finite rates under the stated protocol. The claim established here is therefore only this: for the supplied NC\_000913.3 curated interval dataset and the frozen start--stop-window classifier, the finite ORF-eligibility predicate agrees with the positive and decoy labels in the recorded counts, while boundary cases are not promoted. The self-check row states the same limitation in a compact way: three gates are checked, and no codon geometry is promoted across biological layers.

\paragraph{Local non-claims.}
No translation event follows from this row. No folded protein structure, physical admissibility, molecular mechanism, biological function, or universal genomic law follows from this row. The curated interval table is an observed annotation input to the experiment, not content generated by the codon geometry. The matched decoys and boundary controls do not classify windows outside the supplied dataset. Any later assertion about translation, folding, physical realization, function, mechanism, or universality would require an additional contact designed for that layer.