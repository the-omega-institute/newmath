\subsection{Curated interval contact for ORF eligibility}
\label{sec:h2-orf-eligibility-curated-interval-contact}
\origin{ai}

\paragraph{Finite packet.}
The record concerns open-reading-frame eligibility as a finite interval-classification question.  The biological object is not protein production, folded molecular structure, biochemical feasibility, or function; it is the narrower question whether externally curated nucleotide intervals satisfy the predeclared start/stop-window eligibility rule.  The finite contact table has $168$ rows, loaded from \texttt{tools/bio\_reality/data/curated\_orf\_interval\_contact\_dataset.json}.  Its source is the NCBI RefSeq GenBank XML CDS interval annotation for \emph{Escherichia coli} K-12 MG1655, accession NC\_000913.3, with snapshot date $2026$-$05$-$30$.  The scope field is \texttt{external\_orf\_interval\_contact\_only}; hence the table is an observed biological annotation input to the experiment, not an item generated by the finite codon geometry alone.

\paragraph{Predeclared classifier.}
The classifier is \texttt{orf\_start\_stop\_window\_eligibility}.  Its rules were fixed before the external observation was inspected: the protocol was fixed at $2026$-$05$-$29$ $19{:}11{:}13+00{:}00$, while the external evidence was observed at $2026$-$05$-$30$ $10{:}29{:}31+00{:}00$.  The finite flags record that input classes, held-out splits, classifier rules, and baselines were fixed in advance, and that the analysis used no translation or structure input.  The required row classes were exactly \texttt{boundary\_case}, \texttt{matched\_non\_orf\_decoy}, and \texttt{positive\_curated\_orf}.  A separate one-window start/stop enumeration check also records $1$ loaded external window and $1$ classified input window, with scope \texttt{external\_window\_start\_stop\_enumeration\_only}; that earlier window check remains an eligibility-only computation.

\paragraph{Codon-level finite background.}
The codon geometry used by the packet is finite and explicitly enumerated.  The motif set $R$ is
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\\
&\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The mediator set $M$ has cardinality $20$ and is
$$
\begin{aligned}
M=\{&\mathrm{AAA},\mathrm{AAG},\mathrm{ACA},\mathrm{ACG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\mathrm{CUA},\mathrm{CUC},\\
&\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}.
\end{aligned}
$$
The same finite row records $M=\mathrm{WNR}\cup\mathrm{CUN}$.  The $f_3$ saturation of $R$ is $M$ without $\mathrm{ACA}$ and $\mathrm{ACG}$, and the reported $f_3$ boundary is empty.  The quotient geometry is also finite: $M$ has $f$-vector $(20,38,26,8,1)$, the quotient has $f$-vector $(10,14,6,1)$, and the quotient image has size $10$.  These values describe the geometry available to the classifier, but they do not by themselves assert that any biological interval exists.

\paragraph{Observed interval performance.}
The curated interval contact gives three paired finite tests.  First, among the $72$ positive curated ORF intervals, all $72$ were accepted, so the accept rate was $1.0$; the one-sided exact binomial upper-tail value was $2.117582368135751\times 10^{-22}$.  Second, among the $72$ matched non-ORF decoys, all $72$ were rejected, so the rejection rate was $1.0$; the same reported one-sided value was $2.117582368135751\times 10^{-22}$.  Third, the predeclared positive-versus-decoy paired baseline comparison had $72$ wins in $72$ pairs, giving win rate $1.0$ and the same sign-test value $2.117582368135751\times 10^{-22}$.  In display form, the three finite rates are
$$
\begin{aligned}
\widehat p_{\mathrm{pos}}&=72/72=1.0,\\
\widehat p_{\mathrm{decoy}}&=72/72=1.0,\\
\widehat p_{\mathrm{pair}}&=72/72=1.0.
\end{aligned}
$$
The boundary controls add $24$ boundary cases and promote none of them, giving promoted fraction $0.0$.  Thus the interval contact supports the narrow classification: within this finite curated table, positive intervals and matched decoys are separated by the fixed start/stop-window rule, while boundary cases are not raised to positive interval status.

\paragraph{Held-out stability.}
The held-out check has $3$ splits.  In \texttt{refseq\_nc\_000913\_left\_locus\_fold}, $24$ positives were accepted at rate $1.0$ and $24$ decoys were rejected at rate $1.0$.  The same $24$/$24$ positive and $24$/$24$ decoy result holds for \texttt{refseq\_nc\_000913\_middle\_locus\_fold} and for \texttt{refseq\_nc\_000913\_right\_locus\_fold}.  All three split records are marked as passed.  This split stability is evidence about the finite curated interval table and its matched decoys; it is not evidence that the classifier governs unobserved genomes, transcripts, proteins, or organisms.

\paragraph{Local boundary.}
The self-check reports $3$ checked gates and states that no codon geometry is promoted across biological layers.  The present contact therefore licenses only the following chapter-local claim: the fixed ORF start/stop-window classifier separates the supplied RefSeq-derived curated intervals from their supplied matched decoys, with the row counts and exact binomial values displayed above.  Nothing here establishes actual translation, ribosome engagement, folded protein structure, physical admissibility, biological function, causal mechanism, or universal genomic law.  Those would require further contacts with the corresponding biological layer.  The NCBI RefSeq annotation remains an external curated biological input, and the finite codon geometry remains a classifier coordinate rather than a source of biological realization.