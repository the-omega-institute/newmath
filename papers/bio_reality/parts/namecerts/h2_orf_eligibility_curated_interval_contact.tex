\subsection{Curated interval contact for open-reading-frame eligibility}
\label{sec:h2-orf-eligibility-curated-interval-contact}
\origin{ai}

\paragraph{Layer and contact type.}
This record concerns the layer named $\mathsf{orf\_eligibility}$ and only that layer.  The biological object is eligibility of an open reading frame under finite start--stop codon enumeration, tested against curated interval annotations rather than inferred from codon geometry alone.  The operative classifier is $\mathsf{orf\_start\_stop\_window\_eligibility}$, and the row states that its rules were not modified after the protocol freeze.  The contact scope is $\mathsf{external\_orf\_interval\_contact\_only}$, with snapshot date 2026-05-30.  The source is an NCBI RefSeq GenBank XML curated CDS-interval contact from the complete genome accession NC\_000913.3 for Escherichia coli K-12 MG1655.  Thus the packet is not a claim that the BEDC coordinate system itself observes an open reading frame; it is a finite comparison between a predeclared start--stop window classifier and an external curated interval table.

\paragraph{Upstream finite codon geometry.}
The codon-side finite basis used by the packet is explicitly bounded.  The motif-compression row records
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\\
&\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\},
\end{aligned}
$$
with no missing and no extra elements in that comparison.  The ambient finite set $M$ has cardinality $20$ and is recorded as
$$
\begin{aligned}
M = \{&\mathrm{AAA},\mathrm{AAG},\mathrm{ACA},\mathrm{ACG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\mathrm{CUA},\mathrm{CUC},\\
&\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}.
\end{aligned}
$$
The saturation row gives $\mathsf{Sat}_{f3}(R)=M\setminus\{\mathrm{ACA},\mathrm{ACG}\}$, hence $18$ codons in the displayed saturation image, while $M=\mathsf{Med}(R)\cup\{\mathrm{ACA}\}$ and the $f3$ boundary is empty.  The quotient-geometry row reports the $M$ face vector $(20,38,26,8,1)$, quotient size $10$, and quotient face vector $(10,14,6,1)$.  These numbers define the finite geometric context in which the ORF-window rule is evaluated; they do not themselves supply a biological interval.

\paragraph{External interval table and frozen protocol.}
The curated interval dataset loaded from tools/bio\_reality/data/curated\_orf\_interval\_contact\_dataset.json contains $168$ rows.  Its required input classes are $\mathsf{boundary\_case}$, $\mathsf{matched\_non\_orf\_decoy}$, and $\mathsf{positive\_curated\_orf}$.  The protocol was frozen at 2026-05-29T19:11:13+00:00, before the external evidence time 2026-05-30T10:29:31+00:00.  The recorded freeze flags state that the classifier rules, input classes, held-out splits, and baselines were fixed in advance, and that the analysis used no translation or protein-structure input.  This ordering matters: the contact is a finite biological table tested by a frozen eligibility rule, not a rule retuned after seeing the curated positives.

\paragraph{Positive, decoy, and boundary rows.}
The positive interval statistic has $n=72$ curated positive intervals, with $72$ accepted and acceptance rate $1.0$.  The matched decoy statistic has $n=72$ decoys, with $72$ rejected and rejection rate $1.0$.  Both one-sided exact binomial upper-tail values are recorded as $2.117582368135751\times 10^{-22}$.  The predeclared paired baseline comparison has $72$ pairs, $72$ wins, win rate $1.0$, and the same upper-tail value $2.117582368135751\times 10^{-22}$.  Boundary controls contribute $24$ cases, of which $0$ are promoted, so the promoted fraction is $0.0$.  The finite statistical summary is therefore
$$
\begin{aligned}
&\text{positive accepted}/\text{positive total}=72/72,\\
&\text{decoy rejected}/\text{decoy total}=72/72,\\
&\text{paired wins}/\text{paired total}=72/72,\\
&\text{boundary promotions}/\text{boundary total}=0/24.
\end{aligned}
$$
These are the row-level facts that support contact at the ORF-eligibility layer.

\paragraph{Held-out split stability.}
The held-out stability check contains $3$ splits.  The left-locus fold has $24$ positives accepted at rate $1.0$ and $24$ decoys rejected at rate $1.0$.  The middle-locus fold has the same counts and rates, and the right-locus fold also has the same counts and rates.  All three folds are marked as passed.  Consequently, the contact is not concentrated in a single listed locus fold: within the supplied finite split design, the frozen rule separates the curated positive class from matched non-ORF decoys in each of the three folds.

\paragraph{Earlier window enumeration record.}
A preceding finite-window row loaded one external window dataset on snapshot date 2026-05-25.  It recorded $1$ input window and $1$ window classified by start--stop enumeration, with scope $\mathsf{external\_window\_start\_stop\_enumeration\_only}$.  That row explicitly marked the output as $\mathsf{orf\_eligibility\_only}$.  The present interval contact strengthens the empirical contact from one enumerated external window to a curated interval table of $168$ rows, but it keeps the same layer discipline: the rule tests eligibility against curated intervals and does not become an assertion of translation.

\paragraph{Local cannot-claim boundary.}
The finite contact permits the following narrow reading: under a protocol frozen before external observation, the geometry-only start--stop classifier separates $72$ curated positive CDS-interval rows from $72$ matched non-ORF decoys, does so across $3$ held-out locus folds, and leaves $24$ boundary cases unpromoted.  No stronger biological conclusion follows inside this record.  In particular, it does not show that any interval is translated, that a peptide folds, that a structure is physically admissible, that a product has biological function, that a mechanism has been identified, or that a universal law over genomes has been established.  It also does not classify windows outside the supplied curated dataset.  Any such statement would require a separate contact carrying the relevant biological evidence.  The self-check row is consistent with this restriction: it records $3$ checked gates and states that no codon geometry is promoted across biological layers.