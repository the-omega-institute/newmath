\subsection{Curated interval contact for open-reading-frame eligibility}\label{subsec:h2-orf-eligibility-curated-interval-contact}
\origin{ai}

\paragraph{Finite claim being tested.}
This record tests the layer called $\mathrm{orf\_eligibility}$ for open-reading-frame eligibility from start--stop codon enumeration.  The mathematical object is deliberately narrow: it asks whether a frozen start--stop-window classifier can separate curated coding intervals from matched non-ORF decoys after an external curated interval table has been supplied.  Before that contact is attached, the seed is only a boundary object: its carrier, distinctions, and readback all say that the layer is still waiting for contact with biological data.  The classifier used at contact time is $\mathrm{orf\_start\_stop\_window\_eligibility}$, and the audit records that its rules were not modified after the protocol was frozen.

\paragraph{Coordinate data available before the interval contact.}
The codon-level coordinate side supplies a finite background but not a biological interval by itself.  The motif set $R$ is exactly
$$\begin{aligned}
R=\{&AAA,AGA,AGG,AUA,CUA,CUC,CUG,CUU,\\
&UAA,UAG,UCA,UGA,UUA\}.
\end{aligned}$$
The larger set $M$ has cardinality $20$ and is exactly
$$\begin{aligned}
M=\{&AAA,AAG,ACA,ACG,AGA,AGG,AUA,AUG,CUA,CUC,\\
&CUG,CUU,UAA,UAG,UCA,UCG,UGA,UGG,UUA,UUG\}.
\end{aligned}$$
The $f_3$ saturation computation reports
$$\begin{aligned}
\operatorname{Sat}_{f_3}(R)=M\setminus\{ACA,ACG\},
\end{aligned}$$
with zero $f_3$ boundary cases.  The finite quotient geometry has $|\pi_M|=10$, an $M$-level face vector $(20,38,26,8,1)$, and a quotient face vector $(10,14,6,1)$.  These numbers constrain the coordinate inventory, but none of them is counted as an observed ORF, a transcript, a protein product, or a functional biological event.

\paragraph{External interval contact.}
The contact dataset is the curated ORF interval table at \texttt{tools/bio\_reality/data/curated\_orf\_interval\_contact\_dataset.json}.  It contains $168$ rows and is sourced from NCBI RefSeq GenBank XML CDS intervals for \textit{Escherichia coli} K-12 MG1655 complete genome accession NC\_000913.3, with snapshot date 2026-05-30.  The required input classes are all present: boundary cases, matched non-ORF decoys, and positive curated ORFs.  A prior one-window start--stop enumeration contact, dated 2026-05-25, only showed that its single supplied window was classified by the enumeration rule; it did not by itself provide the curated interval evidence used here.

\paragraph{Frozen protocol and separation result.}
The protocol was frozen at 2026-05-29T19:11:13+00:00, before the external evidence observation at 2026-05-30T10:29:31+00:00.  The recorded freeze conditions say that the input classes, held-out splits, baselines, and classifier rules were fixed in advance, and that the analysis used no translation or structure input.  On the curated contact, all $72$ positive intervals were accepted, so the positive accept rate was $1.0$ with one-sided exact binomial upper-tail value $2.117582368135751\times 10^{-22}$.  All $72$ matched non-ORF decoys were rejected, so the decoy rejection rate was $1.0$ with the same reported upper-tail value $2.117582368135751\times 10^{-22}$.  The paired baseline comparison also recorded $72$ wins in $72$ positive--decoy pairs, giving win rate $1.0$ and one-sided binomial upper-tail value $2.117582368135751\times 10^{-22}$.

\paragraph{Held-out stability.}
The interval contact was also checked across three predeclared held-out splits.  In the left-locus fold, $24$ positives were accepted at rate $1.0$ and $24$ decoys were rejected at rate $1.0$.  The middle-locus fold had the same counts and rates, and the right-locus fold also had $24$ accepted positives and $24$ rejected decoys.  Thus the split audit records $3$ passed splits, each with $24$ positive intervals and $24$ decoys.  Boundary controls remained outside promotion: $24$ boundary cases were inspected and $0$ were promoted, giving promoted fraction $0.0$.

\paragraph{Local non-entailment boundary.}
The result establishes only contact-level ORF interval eligibility for this finite curated table under the frozen start--stop-window classifier.  It does not establish translation, folded protein structure, physical admissibility, biological function, biochemical mechanism, or a universal law about all possible nucleotide windows.  The matched decoys and boundary controls do not classify windows outside this dataset, and the RefSeq table remains an external curated biological input rather than kernel content of the coordinate system.  The self-check attached to the broader packet records three checked gates and explicitly keeps codon geometry from moving across biological layers without an independent contact appropriate to the higher layer.