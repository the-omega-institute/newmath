\subsection{Curated Interval Contact for ORF Eligibility}\label{subsec:h2-orf-eligibility-curated-interval-contact}
\origin{ai}

\paragraph{Object and local scope.}
This record concerns open-reading-frame eligibility as tested against a finite curated interval contact.  The biological object is not a realized protein product, not a folded molecule, not a kinetic mechanism, and not a functional annotation.  It is the narrower question whether a start--stop window classifier, fixed before the external annotation was inspected, separates curated coding intervals from matched non-ORF decoys on a finite dataset.  The verified scope is therefore
$\mathrm{external\_orf\_interval\_contact\_only}$,
with snapshot date $2026$-$05$-$30$ and source described as NCBI RefSeq GenBank XML CDS intervals from \emph{Escherichia coli} K-12 MG1655 complete genome accession NC\_000913.3.

\paragraph{Finite codon geometry carried into the contact.}
The codon-side finite data used by the surrounding classifier are explicitly bounded.  The motif-compressed set $R$ is
$$\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU, UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}$$
The twenty-element set $M=WNR\cup CUN$ is
$$\begin{aligned}
M=\{&AAA, AAG, ACA, ACG, AGA, AGG, AUA, AUG, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UCG, UGA, UGG, UUA, UUG\},
\end{aligned}$$
so $|M|=20$.  The finite saturation check records
$$\begin{aligned}
\operatorname{Sat}_{f_3}(R)=\{&AAA, AAG, AGA, AGG, AUA, AUG, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UCG, UGA, UGG, UUA, UUG\},
\end{aligned}$$
with boundary set $\varnothing$, and the corresponding statement $M=\operatorname{Med}(R)\cup\{ACA,ACG\}$ is verified on the same finite list.  The quotient geometry is also finite: the $M$-side vector is $(20,38,26,8,1)$, the quotient vector is $(10,14,6,1)$, and $|\pi_M|=10$.  These numbers are used only as the classifier's finite codon-coordinate input; they do not by themselves assert an observed ORF.

\paragraph{Prior contact gap.}
Before the curated interval contact, the start--stop enumeration layer had only one externally loaded window record, with $1$ input window and $1$ classified window under the scope $\mathrm{external\_window\_start\_stop\_enumeration\_only}$ at snapshot date $2026$-$05$-$25$.  That earlier check was explicitly limited to ORF eligibility.  It carried no translation, structure, physical admissibility, or functional information.  The self-check likewise counted $3$ gates and stated that no codon geometry was promoted across biological layers.

\paragraph{Protocol freezing and input classes.}
For the curated interval contact, the protocol was frozen at $2026$-$05$-$29\,19{:}11{:}13+00{:}00$, while the external evidence was observed at $2026$-$05$-$30\,10{:}29{:}31+00{:}00$.  The recorded flags say that classifier rules, input classes, held-out splits, and baselines were fixed in advance, and that the analysis used no translation or protein-structure input.  The classifier identifier is $\mathrm{orf\_start\_stop\_window\_eligibility}$, and the rule-modification flag is false.  The required finite input classes are exactly $\mathrm{boundary\_case}$, $\mathrm{matched\_non\_orf\_decoy}$, and $\mathrm{positive\_curated\_orf}$.

\paragraph{Curated rows and exact finite outcomes.}
The curated dataset path is $\mathrm{tools/bio\_reality/data/curated\_orf\_interval\_contact\_dataset.json}$, and it contains $168$ rows.  Among the positive curated intervals, $72$ of $72$ are accepted, giving accept rate $1.0$.  Among matched non-ORF decoys, $72$ of $72$ are rejected, giving reject rate $1.0$.  The paired predeclared baseline comparison has $72$ wins in $72$ pairs, again with win rate $1.0$.  The exact one-sided binomial upper-tail probability recorded for the positive interval statistic, the matched decoy statistic, and the paired baseline statistic is
$$\begin{aligned}
p=2.117582368135751\times 10^{-22}.
\end{aligned}$$
Boundary cases are kept out of the promoted class: $24$ boundary cases have $0$ promotions, so the promoted fraction is $0.0$.

\paragraph{Held-out split stability.}
The finite split check contains $3$ held-out splits.  In $\mathrm{refseq\_nc\_000913\_left\_locus\_fold}$, $24$ positives have accept rate $1.0$ and $24$ decoys have reject rate $1.0$.  The same counts and rates hold in $\mathrm{refseq\_nc\_000913\_middle\_locus\_fold}$ and $\mathrm{refseq\_nc\_000913\_right\_locus\_fold}$.  Each split is marked as passed.  Thus the observed separation is not reported merely as a single pooled table; it is also stable across the three predeclared locus folds.

\paragraph{Local conclusion.}
Within this finite contact, the frozen geometry-only classifier separates the curated positives from matched decoys with perfect finite rates on the reported rows, rejects the boundary cases from promotion, and preserves the predeclared no-promotion discipline.  The result is an ORF-eligibility contact for the named dataset and snapshot, not a general biological theorem.  No translation event, folded protein structure, physical admissibility, biological function, molecular mechanism, organism-wide law, or universal classifier follows from these rows unless another independently specified contact supplies that stronger biological information.  The NCBI RefSeq annotation remains curated biological input to the experiment rather than content generated by the codon-geometry kernel.