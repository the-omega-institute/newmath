\subsection{Boundary status of the start--stop ORF-eligibility seed}\label{subsec:h0-m-equals-wnr-cun-orf-eligibility-boundary}
\origin{ai}

\paragraph{Finite packet.}
The row named \texttt{h0.M.equals.WNR.CUN} is a BioReality seed record for open-reading-frame eligibility from start/stop codon enumeration. Its biological object is not an observed open reading frame. It is the finite boundary packet obtained before any sequence, transcript, or curated interval annotation has been attached. The packet therefore has a minimal BEDC readback of awaiting reality contact: the carrier is awaiting reality contact, the only distinction is awaiting reality contact, and no internal biological structure is yet licensed. The supplied run \texttt{a53ef0f36703} verifies that the enumerated set $M$ has cardinality $20$ and is exactly
$$
\begin{aligned}
M=\{&\mathrm{AAA},\mathrm{AAG},\mathrm{ACA},\mathrm{ACG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\mathrm{CUA},\mathrm{CUC},\\
&\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}.
\end{aligned}
$$
This is a finite codon-window fact only. The number $20$ is a row-level count for the symbolic enumeration, not a count of expressed intervals, translated products, admissible folds, viable molecules, or biological functions.

\paragraph{Motif and saturation checks.}
The linked motif-compression row \texttt{h0.R.motif.compression}, run \texttt{338799579a87}, verifies that the motif union $R$ contains the thirteen codons
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row reports no missing entries and no extra entries for that motif union. The saturation row \texttt{h0.f3.saturation}, run \texttt{f4eee68dd2c3}, verifies that $\operatorname{Sat}_{f3}(R)$ reaches $M$ except for $\mathrm{ACA}$ and $\mathrm{ACG}$, and that adjoining the recorded mediation data restores the full twenty-element $M$. In local terms, the finite check says that the boundary of the $f3$ saturation step is empty after the stated completion:
$$
\begin{aligned}
\partial_{f3}(R)&=\varnothing,\\
|M|&=20,\\
|R|&=13.
\end{aligned}
$$
These equalities are useful as internal audit data for the seed. They do not create a biological observation. In particular, the empty $f3$ boundary is a closure statement inside the finite codon-window computation, not evidence that a proposed interval occurs in a genome, appears in a transcript, is translated, folds, remains physically admissible, participates in a mechanism, or has a function.

\paragraph{Quotient geometry.}
The linked quotient-geometry row \texttt{h0.quotient.geometry}, run \texttt{fd0a506a0881}, records an additional finite shape constraint on the same symbolic packet. The $M$-complex has $f$-vector
$$
\begin{aligned}
f(M)=(20,38,26,8,1),
\end{aligned}
$$
and the quotient has $10$ rows with quotient $f$-vector
$$
\begin{aligned}
f(M/{\sim})=(10,14,6,1).
\end{aligned}
$$
The reported projection table has ten binary rows, so the quotient size and the row count agree at $10$. The product-decomposition check is also recorded as empty, so no extra obstruction is present in the supplied quotient row. These data give a compact finite geometry for the codon-window enumeration: twenty codon entries are compressed to ten quotient representatives with the displayed cell counts. The geometry is still only geometry of the seed. It is not a module classification of a biological system, not a statistical hit-rate, and not a measurement of enrichment, because no module-classification scalar, hit-rate, $p$-value, or $\lambda$ value is present in the supplied row.

\paragraph{Reality-contact gate.}
The BioReality self-check row \texttt{self.claim}, run \texttt{0f47d2f0cf41}, records three checked gates and states the local boundary explicitly: no codon geometry is promoted across biological layers. The two probe records impose the same rule from opposite sides. The probe \texttt{orf.eligibility.boundary.requires.contact} says that a start/stop-window ORF-eligibility seed must remain a boundary probe unless an external sequence or transcript contact supplies an observed biological interval. The probe \texttt{orf.eligibility.contact.gap.check} says that an ORF-eligibility claim breaks the packet if no sequence database, transcript evidence, or curated annotation contact can test the proposed interval. Both probes require the same kind of missing contact, namely \texttt{curated.orf.interval.annotation}. Since the contact list is empty, the current packet has no such witness.

\paragraph{Local cannot-claim boundary.}
The admissible claim is therefore narrow. The chapter may say that the supplied finite rows verify a twenty-codon set $M$, a thirteen-codon motif union $R$, an $f3$ saturation completion whose boundary is empty in the finite computation, and a quotient geometry with ten quotient rows and quotient $f$-vector $(10,14,6,1)$. The chapter may not say that these rows establish ORF eligibility for any biological interval. It may not infer translation realization, folded structure, physical admissibility, biological function, mechanism, or universality from the enumeration. It may not promote the layer \texttt{orf\_eligibility} without a separate reality contact. Until a curated ORF interval annotation, sequence contact, or transcript contact is attached, the only stable BioReality status of \texttt{h0.M.equals.WNR.CUN} is boundary status: the finite packet identifies what would have to be tested, while withholding the biological claim itself.