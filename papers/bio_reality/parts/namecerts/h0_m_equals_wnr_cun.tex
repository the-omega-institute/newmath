\origin{ai}

\subsection{Start--Stop Enumeration and the ORF-Eligibility Contact Boundary}
\label{sec:bioreality-h0-m-equals-wnr-cun-orf-eligibility-boundary}

\paragraph{Finite codon packet.}
The claim $h0.M.equals.WNR.CUN$ is recorded here as a finite codon-coordinate packet, not as a direct biological assertion. The verified finite set $M$ has cardinality $20$ and is exactly the union named by the row as $WNR \cup CUN$:
$$
\begin{aligned}
M=\{&AAA,AAG,ACA,ACG,AGA,AGG,AUA,AUG,CUA,CUC,\\
& CUG,CUU,UAA,UAG,UCA,UCG,UGA,UGG,UUA,UUG\}.
\end{aligned}
$$
The associated experiment run $a53ef0f36703$ reports both $M\_cardinality\_equals\_20=20$ and $size=20$, so the packet is closed at the level of finite enumeration. The row also records the motif compression set
$$
\begin{aligned}
R=\{&AAA,AGA,AGG,AUA,CUA,CUC,CUG,CUU,UAA,UAG,\\
& UCA,UGA,UUA\},
\end{aligned}
$$
with no missing and no extra entries in run $338799579a87$. This matters biologically only as a disciplined finite substrate: it supplies a bounded list of codon strings on which an open-reading-frame eligibility test can be asked, but it does not itself supply an observed genomic interval.

\paragraph{Saturation and quotient geometry.}
The finite-row audit gives a second check on the coordinate side. In run $f4eee68dd2c3$, the saturation statement records
$$
\begin{aligned}
\operatorname{Sat}_{f3}(R)=\{&AAA,AAG,AGA,AGG,AUA,AUG,CUA,CUC,CUG,CUU,\\
& UAA,UAG,UCA,UCG,UGA,UGG,UUA,UUG\},
\end{aligned}
$$
namely $M$ without $ACA$ and $ACG$, while the reported $f3$ boundary is empty. The same row records $M=Med(R)\cup\{ACA,ACG\}$ as the full twenty-element list above. Independently, run $fd0a506a0881$ reports the $M$ face vector $(20,38,26,8,1)$, a quotient size $|\pi_M|=10$, and quotient face vector $(10,14,6,1)$. Its quotient representative matrix consists of ten binary rows, all ending in the last coordinate except the row $(0,1,0,0,0)$, and the product-decomposition discrepancy list is empty. These numbers justify treating the packet as a small, completely enumerated combinatorial object. They do not justify treating it as a transcript, coding sequence, translated peptide, folded domain, physically admissible molecule, cellular mechanism, biological function, or universal biological law.

\paragraph{ORF-eligibility interpretation.}
The BioReality layer attached to this row is $orf\_eligibility$. At this layer, the biological question is whether a finite nucleotide interval is eligible under start/stop codon enumeration. The finite codon packet alone is weaker than that question: it names codon strings and geometric relations among them, but it has no sequence database row, transcript observation, or curated interval annotation attached at the seed stage. For that reason the seed record is admissible only as a boundary question: if an observed interval is later supplied, the start/stop enumeration can be evaluated on that interval; until then, the record remains a finite proposal about what would have to be checked.

\paragraph{Reality contact and measured interval evidence.}
A separate curated contact does appear elsewhere in the finite-row bundle and fixes the scale of what counts as an actual ORF-eligibility contact. Run $c26a0d6656f7$ loaded one external nucleotide-window dataset, with $window\_count=1$, $input\_windows=1$, and $classified=1$, under the stated scope $external\_window\_start\_stop\_enumeration\_only$ and snapshot date $2026$-$05$-$25$. A stronger curated interval contact, run $7c9b30b02d46$, loaded $168$ rows from a RefSeq-derived interval dataset for \textit{Escherichia coli} K-$12$ MG1655 accession $NC\_000913.3$, with snapshot date $2026$-$05$-$30$. The required classes were present: $boundary\_case$, $matched\_non\_orf\_decoy$, and $positive\_curated\_orf$. The protocol was frozen before observation: the protocol time was $2026$-$05$-$29T19:11:13+00:00$, while external evidence was observed at $2026$-$05$-$30T10:29:31+00:00$.

\paragraph{Finite statistical checks.}
The curated interval contact reports $72$ positive intervals accepted out of $72$ and $72$ matched decoys rejected out of $72$. The positive acceptance statistic has accept rate $1.0$ with one-sided binomial upper-tail value $2.117582368135751\times 10^{-22}$; the matched-decoy rejection statistic has reject rate $1.0$ with the same one-sided value. The predeclared paired comparison also gives $72$ wins in $72$ pairs, win rate $1.0$, and one-sided binomial upper-tail value $2.117582368135751\times 10^{-22}$. Held-out stability is reported over three splits: $refseq\_nc\_000913\_left\_locus\_fold$, $refseq\_nc\_000913\_middle\_locus\_fold$, and $refseq\_nc\_000913\_right\_locus\_fold$. Each split has $24$ positives accepted at rate $1.0$ and $24$ decoys rejected at rate $1.0$. Boundary cases are explicitly not promoted: $24$ boundary cases have $0$ promotions, giving promoted fraction $0.0$. These statistics support the existence of a separately contacted ORF-eligibility test for the curated interval dataset; they do not retroactively turn the original codon-coordinate packet into biological observation.

\paragraph{Local exclusion rule.}
The row's self-check reports $checked\_gate\_count=3$ and states that no codon geometry is promoted across biological layers. The local rule for this chapter is therefore strict. Start/stop-window eligibility may be asserted only when a separate sequence, transcript, or curated annotation contact supplies the interval being tested. Even then, the conclusion is confined to ORF eligibility for that contacted interval and that classifier. No translation realization, folded structure, physical admissibility, cellular function, molecular mechanism, organism-level role, or universal biological regularity follows from the finite codon list, the quotient geometry, the start/stop enumeration, or the curated interval statistics without another contact designed for that higher layer.