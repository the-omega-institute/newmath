\origin{ai}

\subsection{Motif compression and ORF-eligibility boundary}
\label{subsec:h0-r-motif-compression-orfb}

\paragraph{Finite motif row.}
The record considered here is the finite codon motif compression claim with identifier \texttt{h0.R.motif.compression}.  Its verified row is not an assertion about translation or phenotype.  It is a finite equality check on a thirteen-element motif set $R$, namely
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The experiment run \texttt{338799579a87}, verified at \texttt{2026-05-31T18:55:32+00:00}, reports that this list is exactly the motif union under test: the missing-list is empty and the extra-list is empty.  Thus the row supports only the finite compression statement that these thirteen codons are precisely the motif-coded subset being enumerated.

\paragraph{Ambient codon universe used by the row.}
The same finite audit attaches the motif row to a twenty-element ambient set $M$.  The verified row \texttt{h0.M.equals.WNR.CUN}, run \texttt{a53ef0f36703}, gives $|M|=20$ and lists
$$
\begin{aligned}
M=\{&\mathrm{AAA},\mathrm{AAG},\mathrm{ACA},\mathrm{ACG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\\
&\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\\
&\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}.
\end{aligned}
$$
This is useful because the ORF-eligibility discussion can be kept on a sharply bounded footing: a start/stop-window packet may use finite codon enumeration as a syntactic object, but the biological interval itself is not produced by the codon geometry.  The verified $M$ row gives a closed enumeration context; it does not say that any listed codon is expressed, translated, folded, selected, or functional in a cell.

\paragraph{Saturation and quotient checks.}
The saturation row \texttt{h0.f3.saturation}, run \texttt{f4eee68dd2c3}, records that $\mathrm{Sat}_{f3}(R)$ is the eighteen-element set
$$
\begin{aligned}
\{&\mathrm{AAA},\mathrm{AAG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\mathrm{CUA},\mathrm{CUC},\\
&\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\mathrm{UGA},\mathrm{UGG},\\
&\mathrm{UUA},\mathrm{UUG}\}.
\end{aligned}
$$
The same row records that adjoining $\mathrm{ACA}$ and $\mathrm{ACG}$ recovers the twenty-element $M$, while the $f3$ boundary list is empty.  The quotient-geometry row \texttt{h0.quotient.geometry}, run \texttt{fd0a506a0881}, reports the ambient $f$-vector $(20,38,26,8,1)$ and quotient $f$-vector $(10,14,6,1)$, with quotient size $10$ and ten displayed quotient coordinates.  These numbers are internal finite combinatorial measurements of the enumerated object.  They are admissible as checks on the row's finite shape, but they do not create a biological observation.

\paragraph{ORF-eligibility contact discipline.}
For the biological object named in this packet, open-reading-frame eligibility from start/stop codon enumeration, the seed status is boundary-level until a separate biological interval contact is attached.  A single external-window start/stop enumeration row is present under \texttt{h2.orf.start\_stop.window\_eligibility}: it reports one input window, one classified window, one external window dataset loaded, snapshot date \texttt{2026-05-25}, and scope \texttt{external\_window\_start\_stop\_enumeration\_only}.  Its own recorded non-promotion field is \texttt{orf\_eligibility\_only}.  Consequently the start/stop-window result is not a claim that translation occurred; it is only an eligibility classification relative to the supplied window.

\paragraph{Curated interval contact.}
A stronger finite contact is recorded separately under \texttt{h2.orf\_eligibility.curated\_interval\_contact}.  It loaded a curated interval dataset with $168$ rows from \texttt{tools/bio\_reality/data/curated\_orf\_interval\_contact\_dataset.json}, snapshot date \texttt{2026-05-30}, sourced from NCBI RefSeq GenBank XML CDS intervals for \textit{Escherichia coli} K-12 MG1655 complete genome accession \texttt{NC\_000913.3}.  The protocol fields record that classifier rules, input classes, baselines, and held-out splits were fixed before the external evidence time \texttt{2026-05-30T10:29:31+00:00}.  The row contains the three required input classes: \texttt{boundary\_case}, \texttt{matched\_non\_orf\_decoy}, and \texttt{positive\_curated\_orf}.  Across the finite contact, $72$ positives were accepted, $72$ matched decoys were rejected, and $24$ boundary cases had zero promotion.  The positive exact one-sided binomial upper-tail value is $2.117582368135751\cdot 10^{-22}$, the matched-decoy rejection exact one-sided binomial upper-tail value is also $2.117582368135751\cdot 10^{-22}$, and the predeclared paired baseline sign-test advantage has $72$ wins out of $72$ pairs with the same upper-tail value.  The three held-out splits each contain $24$ positives and $24$ decoys, with positive accept rate $1.0$, decoy reject rate $1.0$, and pass status true.

\paragraph{Local conclusion and prohibited promotions.}
The chapter-level conclusion is therefore narrow.  The thirteen-codon motif row is verified exactly, its twenty-codon ambient enumeration and associated saturation and quotient measurements are finite and explicit, and separate curated interval rows can support ORF-eligibility classification within their own external-contact scope.  None of this licenses a stronger biological statement.  No translation realization, folded protein structure, physical admissibility, molecular function, causal mechanism, or universal biological law follows from the motif compression, the start/stop-window geometry, the quotient coordinates, or the curated interval row unless another independently specified contact supplies the relevant observation.  The self-check row \texttt{self.claim}, run \texttt{0f47d2f0cf41}, states the same guardrail in finite form: three gates were checked, and no codon geometry is promoted across biological layers.