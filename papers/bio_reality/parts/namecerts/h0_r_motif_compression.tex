\subsection{Motif-compression boundary for open-reading-frame eligibility}\label{sec:bioreality-h0-r-motif-compression}\origin{ai}

\paragraph{Record scope.} This record concerns the claim identifier $\mathrm{h0.R.motif.compression}$ and its use as a disciplined seed for open-reading-frame eligibility. The biological object is not a translated product, a folded molecule, a physical realization, or a cellular role. It is only the finite question of whether start and stop codon enumeration can define a window-level eligibility test once a separate biological interval contact is supplied. Before such contact is present, the record remains a boundary datum: it states what would be testable, and it prevents the finite motif coordinate from being read as an observed biological fact.

\paragraph{Motif row.} The verified motif-compression row gives the finite set
$$
\begin{aligned}
R &= \{\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\qquad \mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\},\\
|R| &= 13.
\end{aligned}
$$
The accompanying ledger has $0$ missing codons and $0$ extra codons for this motif union. Thus the finite row is exact as a codon-list statement: the row verifies that the named compression selects precisely these thirteen codons and no others. The verification run for this row is $338799579a87$, recorded at $2026$-$06$-$01\mathrm{T}07{:}28{:}18+00{:}00$.

\paragraph{Ambient codon carrier.} The same packet links this row to the larger verified carrier $M$, whose cardinality is $20$. The recorded list is
$$
\begin{aligned}
M &= \{\mathrm{AAA},\mathrm{AAG},\mathrm{ACA},\mathrm{ACG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\\
&\qquad \mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\\
&\qquad \mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\},\\
|M| &= 20.
\end{aligned}
$$
The row $\mathrm{h0.M.equals.WNR.CUN}$ states that this is the verified $\mathrm{WNR}\cup\mathrm{CUN}$ carrier, again with size $20$. The motif row is therefore a proper finite sub-selection inside a twenty-codon enumerated carrier, not an independently observed transcript or coding annotation.

\paragraph{Saturation and quotient checks.} The associated saturation row reports that $\operatorname{Sat}_{f3}(R)$ contains $18$ codons, namely every codon in $M$ except $\mathrm{ACA}$ and $\mathrm{ACG}$. It also records an empty $f3$ boundary list, and it verifies
$$
\begin{aligned}
M &= \operatorname{Med}(R)\cup\{\mathrm{ACA}\}.
\end{aligned}
$$
The quotient-geometry row gives the finite vector $M_f=(20,38,26,8,1)$ for $M$, quotient size $10$, and quotient vector $(10,14,6,1)$. Its recorded quotient matrix contains $10$ rows. These numbers are useful as internal finite controls: they show that the motif row participates in a stable enumerative geometry. They do not supply biological contact. In particular, a clean quotient vector and an empty local boundary are not evidence that a nucleotide interval exists in a genome, that it is transcribed, or that a ribosome realizes it.

\paragraph{Eligibility contact distinction.} A separate start-stop enumeration check records exactly $1$ input nucleotide window and $1$ classified window, with scope explicitly limited to external-window start and stop enumeration only. Its snapshot date is $2026$-$05$-$25$. That row also records the classification as $\mathrm{orf\_eligibility\_only}$, which is the crucial local boundary: the classifier may speak about start and stop window eligibility for the supplied window, but it does not promote the result to translation, protein structure, physical admissibility, function, mechanism, or universality.

\paragraph{Curated interval contact.} A later curated interval contact supplies the kind of biological observation that the seed row itself lacks. That contact loads $168$ rows from a curated interval dataset with snapshot date $2026$-$05$-$30$, drawn from NCBI RefSeq GenBank XML CDS intervals for \emph{Escherichia coli} K-12 MG1655 complete genome accession $\mathrm{NC\_000913.3}$. It separates the rows into required input classes: boundary cases, matched non-ORF decoys, and positive curated ORFs. The positive interval statistic accepts $72$ of $72$ positives, with accept rate $1.0$ and one-sided binomial upper-tail value $2.117582368135751\times 10^{-22}$. The matched decoy statistic rejects $72$ of $72$ decoys, also with rate $1.0$ and the same upper-tail value $2.117582368135751\times 10^{-22}$. The predeclared paired baseline comparison has $72$ wins in $72$ pairs and the same one-sided value. Three held-out splits are recorded; each has $24$ positives accepted at rate $1.0$ and $24$ decoys rejected at rate $1.0$.

\paragraph{Boundary cases.} The curated contact also records $24$ boundary cases, with $0$ promoted and promoted fraction $0.0$. This is not an incidental caution; it is the chapter-level rule. Boundary material remains boundary material unless a separate contact supplies the relevant interval and the stated layer is exactly the one tested. The protocol records that classifier rules, input classes, baselines, and held-out splits were fixed before the outside observation, with protocol time $2026$-$05$-$29\mathrm{T}19{:}11{:}13+00{:}00$ and evidence observation time $2026$-$05$-$30\mathrm{T}10{:}29{:}31+00{:}00$. The classifier is identified as $\mathrm{orf\_start\_stop\_window\_eligibility}$ and is recorded as geometry-only, with no rule modification.

\paragraph{Local verdict.} The finite motif-compression row is ready as a BioReality seed because it has exact finite codon content, zero list mismatch, a twenty-codon ambient carrier, an eighteen-codon saturation control, and a quotient geometry check with explicit scalar values. Its biological interpretation remains sharply limited. Without a separate sequence, transcript, or curated annotation contact, the row cannot assert ORF eligibility. Even with the curated interval contact, the established layer is only interval-level start-stop eligibility for that dataset. No translation event, folded structure, physical admissibility, biological function, mechanistic explanation, or universal biological law follows from the finite motif row or from start-stop enumeration alone.

\paragraph{Self-check.} The self-check row records $3$ checked gates and states that no codon geometry is promoted across biological layers. This confirms the intended reading of the packet: finite enumerative agreement may define a disciplined test site, but biological promotion requires the proper observational contact and remains confined to the layer that the contact can actually test.