\subsection{Motif Compression Boundary for ORF Eligibility}\label{sec:bioreality-h0-r-motif-compression}
\origin{ai}

\paragraph{Finite codon packet.}
The row named \texttt{h0.R.motif.compression} is a finite codon-compression record whose verified content is the equality between the marked set $R$ and its motif union.  The certified list is
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\\
&\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The audit found no missing codons and no extra codons in this comparison: both discrepancy lists have cardinality $0$.  Thus the compression claim is not an approximate biological assertion; it is the finite identity that these $13$ codons are exactly the codons delivered by the motif enumeration used in this packet.

\paragraph{Ambient enumeration.}
The linked ambient table gives the larger codon set $M$ as a $20$-element union of the WNR and CUN components.  Its verified entries are
$$
\begin{aligned}
M=\{&\mathrm{AAA},\mathrm{AAG},\mathrm{ACA},\mathrm{ACG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\mathrm{CUA},\mathrm{CUC},\\
&\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}.
\end{aligned}
$$
Within this row, the biological reading is deliberately narrow: the finite codon geometry supplies a start--stop-window eligibility boundary, not an observed gene, transcript, coding sequence, protein product, or functional annotation.  The $13$-element set $R$ is therefore a compressed coordinate inside a $20$-element codon universe, and the only immediate claim is exact finite membership in that coordinate system.

\paragraph{Saturation and quotient checks.}
The companion saturation row reports that $\operatorname{Sat}_{f_3}(R)$ contains $18$ codons, namely all entries of $M$ except $\mathrm{ACA}$ and $\mathrm{ACG}$.  It also reports an empty $f_3$ boundary list, so the boundary check contributes no additional codon beyond the enumerated finite result.  A separate quotient-geometry check gives the $M$ face vector as $(20,38,26,8,1)$ and the quotient face vector as $(10,14,6,1)$, with quotient size $10$.  These numbers describe finite combinatorial shape only.  They do not convert the codon table into translation, folding, molecular stability, cellular role, mechanism, or a universal biological rule.

\paragraph{ORF eligibility contact status.}
The start--stop-window eligibility row loaded one external nucleotide-window record and classified all one of its input windows by start/stop enumeration.  Its declared scope is \texttt{external\_window\_start\_stop\_enumeration\_only}, and its own finite output says \texttt{orf\_eligibility\_only}.  This is why the seed packet remains boundary-level unless an independent biological interval observation is attached.  Enumerating a start and a stop codon can specify a candidate window condition, but it is not by itself evidence that translation occurs, that a folded product exists, that the product is physically admissible, or that it has biological function.

\paragraph{Curated interval evidence and its limit.}
A later curated-interval contact supplies a finite external annotation test rather than a deduction from the codon geometry alone.  That contact loaded $168$ rows from a curated ORF interval dataset.  The required input classes were present: boundary cases, matched non-ORF decoys, and positive curated ORFs.  Among $72$ positive intervals, $72$ were accepted, giving accept rate $1.0$ and one-sided binomial upper-tail probability $2.117582368135751\times 10^{-22}$.  Among $72$ matched decoys, all $72$ were rejected, also with one-sided binomial upper-tail probability $2.117582368135751\times 10^{-22}$.  The predeclared pairwise comparison likewise recorded $72$ wins in $72$ pairs, for win rate $1.0$ and the same probability value.  Boundary controls counted $24$ cases and promoted $0$ of them, so the promoted fraction was $0.0$.

\paragraph{Held-out split stability.}
The interval contact also reports three held-out splits.  Each split had $24$ positives and $24$ decoys; each accepted positives at rate $1.0$, rejected decoys at rate $1.0$, and passed.  The three split names were \texttt{refseq\_nc\_000913\_left\_locus\_fold}, \texttt{refseq\_nc\_000913\_middle\_locus\_fold}, and \texttt{refseq\_nc\_000913\_right\_locus\_fold}.  The protocol was frozen before external observation: the protocol time was \texttt{2026-05-29T19:11:13+00:00}, while external evidence was observed at \texttt{2026-05-30T10:29:31+00:00}.  The classifier identifier was \texttt{orf\_start\_stop\_window\_eligibility}, and the row records that the classifier rules were not modified.  These finite controls support an interval-contact result for the curated dataset; they still do not license claims about translation realization, protein structure, physical admissibility, function, global law, or mechanism outside the measured contact.

\paragraph{Local interpretation.}
The self-check row records $3$ checked gates and states that no codon geometry is promoted across biological layers.  This chapter therefore reads the motif-compression equality as a precise finite boundary instrument.  The equality $R=\mathrm{motif\ union}$ is real as a table identity; the $20$-element ambient enumeration, $18$-element saturation, quotient size $10$, and curated $168$-row contact are real as finite reports.  The biological conclusion is intentionally weaker than a gene-discovery statement: without a separate sequence, transcript, or curated interval contact, the packet only marks what would have to be tested for ORF eligibility.  With such a contact, the statement remains confined to ORF interval eligibility in that dataset, and nothing in the row establishes translation, folding, physical admissibility, function, mechanism, or universality.