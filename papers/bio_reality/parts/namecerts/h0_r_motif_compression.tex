\origin{ai}
\subsection{Motif compression and ORF-eligibility boundary}
\label{subsec:bior-orf-eligibility-motif-compression}

\paragraph{Finite motif register.}
The record named \texttt{h0.R.motif.compression} isolates a finite RNA-codon motif register and records exactly which codons belong to the compressed set $R$.  The verified row reports
$$
\begin{aligned}
R = \{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}
$$
The same row records $0$ missing elements and $0$ extra elements relative to the motif union used in the computation.  Thus this chapter treats the motif compression claim as a finite equality of enumerated codon names, not as a claim about translation, expression, or biological activity.  The verified timestamp for this equality is \texttt{2026-06-01T07:28:18+00:00}, under experiment run \texttt{338799579a87}.

\paragraph{Ambient codon set and saturation context.}
The surrounding finite register $M$ has cardinality $20$ and is certified as the union reported in \texttt{h0.M.equals.WNR.CUN}:
$$
\begin{aligned}
M = \{&\texttt{AAA},\texttt{AAG},\texttt{ACA},\texttt{ACG},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{AUG},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UCG},\texttt{UGA},\texttt{UGG},\texttt{UUA},\texttt{UUG}\}.
\end{aligned}
$$
The finite saturation row states that applying the $f_3$ saturation to $R$ gives the $18$-element set $M \setminus \{\texttt{ACA},\texttt{ACG}\}$, namely
$$
\begin{aligned}
\operatorname{Sat}_{f_3}(R) = \{&\texttt{AAA},\texttt{AAG},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{AUG},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UCG},\texttt{UGA},\texttt{UGG},\texttt{UUA},\texttt{UUG}\}.
\end{aligned}
$$
The same computation records the $f_3$ boundary as empty.  Separately, the quotient-geometry row reports the $M$ face vector $(20,38,26,8,1)$, the quotient face vector $(10,14,6,1)$, quotient size $10$, and a ten-row projection table.  These numbers describe only the combinatorial organization of the finite codon register.  They do not identify an observed gene, an expressed transcript, a translated peptide, a folded molecule, or a functional biological role.

\paragraph{ORF-eligibility interpretation.}
The biological object attached to this packet is open-reading-frame eligibility from start- and stop-codon enumeration.  The verified one-window start/stop row records $1$ loaded external window, $1$ input window, and $1$ classified window, with scope restricted to \texttt{external\_window\_start\_stop\_enumeration\_only} and snapshot date \texttt{2026-05-25}.  This is a boundary-level statement: enumeration can say that a proposed nucleotide window has the syntactic start/stop shape required for an ORF candidate, but the enumeration alone does not turn that candidate into an observed ORF.  A separate sequence, transcript, or curated-annotation contact must supply the biological interval being tested.

\paragraph{Curated interval contact as a separate row.}
A later curated-interval contact row is available for the ORF-eligibility layer, but it is not part of the motif-compression equality itself.  That contact loaded $168$ rows from \texttt{tools/bio\_reality/data/curated\_orf\_interval\_contact\_dataset.json}, sourced from NCBI RefSeq GenBank XML CDS intervals for \textit{Escherichia coli} K-12 MG1655 accession \texttt{NC\_000913.3}, with snapshot date \texttt{2026-05-30}.  The row contains $72$ positive curated intervals, $72$ matched non-ORF decoys, and $24$ boundary cases.  Positives were accepted at rate $1.0$ with $72$ accepted out of $72$ and one-sided exact binomial upper-tail value $2.117582368135751\times 10^{-22}$.  Matched decoys were rejected at rate $1.0$ with $72$ rejected out of $72$ and the same recorded upper-tail value $2.117582368135751\times 10^{-22}$.  The predeclared paired comparison records $72$ wins out of $72$ pairs, win rate $1.0$, and the same one-sided binomial value.  Boundary cases were not promoted: $0$ of $24$ were promoted, giving promoted fraction $0.0$.

\paragraph{Held-out stability and frozen protocol.}
The curated contact row also records three held-out splits.  The left-locus, middle-locus, and right-locus folds each contain $24$ positives and $24$ decoys; in each fold the positive accept rate is $1.0$, the decoy reject rate is $1.0$, and the split is marked as passed.  The protocol was frozen at \texttt{2026-05-29T19:11:13+00:00}; external evidence was observed at \texttt{2026-05-30T10:29:31+00:00}.  The classifier identifier is \texttt{orf\_start\_stop\_window\_eligibility}, and the row records that classifier rules were not modified.  It also records that baselines, held-out splits, and input classes were fixed before the observation, while the analysis used no translation or protein-structure input.

\paragraph{Local claim boundary.}
The motif-compression equality, the $20$-element ambient register, the $18$-element saturation row, the quotient face vectors, the one-window start/stop enumeration, and the $168$-row curated interval contact all remain within ORF-eligibility bookkeeping.  They do not establish translation realization, protein folding, physical admissibility, biological function, molecular mechanism, or a universal biological law.  Such conclusions would require a different contact type and a separately declared test.  The self-check row reinforces this boundary by recording that no codon geometry is promoted across biological layers and that $3$ gates were checked.  Accordingly, this chapter uses the finite motif register to state what was exactly enumerated and what was externally tested, while keeping every stronger biological reading outside the present claim.