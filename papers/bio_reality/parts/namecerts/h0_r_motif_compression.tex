\subsection{Motif Compression as an ORF-Eligibility Boundary Record}\label{subsec:h0-r-motif-compression-orf-boundary}
\origin{ai}

\paragraph{Finite codon packet.}
The record named $h0.R.motif.compression$ begins with a strictly finite codon calculation. The verified motif set $R$ is exactly
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The audit reports no missing motif members and no extra motif members, so this row supplies a closed thirteen-element enumeration rather than a statistical fit. Its direct biological reading is deliberately narrow: it gives a finite syntactic region in codon space that can be used to formulate start/stop-window questions. It does not, by itself, say that an open reading frame has been observed in an organism or transcript.

\paragraph{Ambient codon universe.}
The same finite run records the larger twenty-element set $M$, namely
$$
\begin{aligned}
M=\{&\mathrm{AAA},\mathrm{AAG},\mathrm{ACA},\mathrm{ACG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\\
&\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\\
&\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}.
\end{aligned}
$$
Thus the motif packet sits inside a twenty-element enumerated universe, with $|M|=20$ and $|R|=13$. The row also records $M$ as the union used by the $h0.M.equals.WNR.CUN$ check. This matters because the chapter is not using prose similarity between codons as evidence; the only admitted geometry is the listed finite membership calculation.

\paragraph{Saturation boundary.}
The $f3$ saturation row gives a second containment test. It verifies that
$$
\begin{aligned}
\operatorname{Sat}_{f3}(R)=\{&\mathrm{AAA},\mathrm{AAG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\mathrm{CUA},\mathrm{CUC},\\
&\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\mathrm{UGA},\mathrm{UGG},\\
&\mathrm{UUA},\mathrm{UUG}\}.
\end{aligned}
$$
This saturated set has eighteen codons. The same audit says that $M$ is recovered by adjoining $\mathrm{ACA}$ and $\mathrm{ACG}$ to this eighteen-element list, and it records the $f3$ boundary as empty, with boundary count $0$. In biological terms, that is still only a codon-space closure fact. It permits a disciplined question about which codon windows are eligible for an ORF boundary test, but it does not make a claim about expression, translation, folding, physical stability, molecular mechanism, organism-level effect, or a universal biological rule.

\paragraph{Quotient geometry.}
The quotient row gives additional finite shape data. The $M$ face vector is recorded as $(20,38,26,8,1)$, and the quotient face vector is recorded as $(10,14,6,1)$. The quotient projection has ten listed rows, so the compression reduces the twenty-element codon universe to ten quotient coordinates in the verified projection. This quotient information is useful only as an indexing discipline for the finite motif calculation. It must not be read as a biological process model. In particular, a quotient coordinate is not a ribosome, not a transcript, not a protein structure, and not a functional assay.

\paragraph{ORF eligibility contact rule.}
The linked ORF-window check records one external nucleotide-window dataset and classifies one input window by start/stop enumeration. Its scope is explicitly limited to external-window start/stop enumeration, and the recorded layer is $\mathrm{orf\_eligibility}$ only. This is the local rule of interpretation for the motif packet: codon geometry can prepare a boundary question, while an actual eligibility claim requires a separate biological interval source. Without that added source, the record remains a pre-contact boundary test rather than an observed ORF statement.

\paragraph{Curated interval evidence.}
A separate curated interval contact supplies the stronger finite reality check. That row loads $168$ curated records from NCBI RefSeq GenBank XML CDS intervals for \emph{Escherichia coli} K-12 MG1655 accession NC\_000913.3, with snapshot date 2026-05-30. The required classes are present: positive curated ORFs, matched non-ORF decoys, and boundary cases. The positive side accepts $72$ of $72$ intervals, giving accept rate $1.0$; the matched decoy side rejects $72$ of $72$ decoys, giving reject rate $1.0$. The paired baseline comparison records $72$ wins out of $72$ pairs, again with win rate $1.0$. The exact one-sided binomial upper-tail value is $2.117582368135751\times 10^{-22}$ for the positive interval statistic, the matched decoy rejection statistic, and the paired baseline advantage. Held-out stability is reported across three splits, each with $24$ positives and $24$ decoys: the left-locus, middle-locus, and right-locus folds all pass with positive accept rate $1.0$ and decoy reject rate $1.0$. Boundary cases remain unpromoted, with $24$ boundary cases and $0$ promotions.

\paragraph{Frozen protocol and interpretation.}
The curated contact row records that classifier rules, input classes, baselines, and held-out splits were fixed before the external observation. The protocol time is 2026-05-29T19:11:13+00:00, and the external evidence time is 2026-05-30T10:29:31+00:00. The classifier identifier is $\mathrm{orf\_start\_stop\_window\_eligibility}$, and its rules were not modified during the contact. This supports an ORF-eligibility statement only at the curated-interval layer: the finite geometry supplies a predeclared codon-window classifier, and the external annotation supplies the biological interval against which that classifier is checked.

\paragraph{Local cannot-claim boundary.}
The chapter therefore has two different kinds of content. The motif and quotient rows are finite codon-space facts: $|R|=13$, $|M|=20$, the saturated list has $18$ elements, the $f3$ boundary has size $0$, and the quotient projection has size $10$. The curated interval row is a separate biological contact with $168$ rows and exact finite test statistics. Neither kind of row licenses promotion beyond ORF eligibility. No translation event, folded conformation, physical admissibility, biochemical mechanism, cellular function, organismal role, or universal law follows from these data unless another contact is supplied that measures that specific layer. The self-check is consistent with this reading: it records three checked gates and states that no codon geometry is promoted across biological layers.
