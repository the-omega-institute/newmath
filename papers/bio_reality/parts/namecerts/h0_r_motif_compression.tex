\origin{ai}

\subsection{Motif compression at the ORF-eligibility boundary}
\label{sec:biorality-h0-r-motif-compression}

\paragraph{Finite codon record.}
The record named $h0.R.motif.compression$ is a finite codon-compression claim attached to the biological question of open-reading-frame eligibility from start/stop codon enumeration. Its verified finite set $R$ is exactly the following $13$ codons:
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The comparison against the declared motif union has no residual error: the verified $\mathrm{no\_extra}$ list is empty and the verified $\mathrm{no\_missing}$ list is empty. Thus this chapter records an exact finite equality, not a statistical approximation and not an inferred biological mechanism. The experimental run identifier for this equality is $338799579a87$, verified at $2026$-$06$-$01$ $07{:}28{:}18+00{:}00$.

\paragraph{Ambient finite universe.}
The same row links this compressed $R$ record to the ambient set $M$, whose verified cardinality is $20$. The complete listed universe is
$$
\begin{aligned}
M = \{&\mathrm{AAA},\mathrm{AAG},\mathrm{ACA},\mathrm{ACG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\\
&\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\\
&\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}.
\end{aligned}
$$
The fact $|M|=20$ is a finite enumeration fact. It gives the scale of the codon window in which the $13$-element motif record sits, but it does not by itself identify an observed gene, transcript, translated product, folded molecule, or cellular role.

\paragraph{Saturation and quotient checks.}
A linked finite closure calculation reports that $\mathrm{Sat}_{f3}(R)$ contains $18$ codons: all members of $M$ except $\mathrm{ACA}$ and $\mathrm{ACG}$. The same calculation records the $f3$ boundary as the empty list. In symbols, the verified saturation information is summarized by
$$
\begin{aligned}
\mathrm{Sat}_{f3}(R) = M \setminus \{\mathrm{ACA},\mathrm{ACG}\},\qquad
|\mathrm{Sat}_{f3}(R)| = 18.
\end{aligned}
$$
A separate finite quotient computation records $\pi(M)$ with size $10$, an $M$-level vector $(20,38,26,8,1)$, and a quotient vector $(10,14,6,1)$. These numbers are useful internal checks on the finite combinatorial organization of the codon record. They are not evidence that the compressed motif has crossed into translation, structure, physical admissibility, function, biochemical mechanism, or a universal biological law.

\paragraph{ORF-eligibility contact status.}
The ORF-eligibility side of the row is deliberately stricter than the codon compression side. The start/stop-window enumeration check loaded one external window record, classified one input window, and reported $\mathrm{all\_windows\_classified\_by\_start\_stop\_enumeration}=1$. Its scope is explicitly $\mathrm{external\_window\_start\_stop\_enumeration\_only}$, with snapshot date $2026$-$05$-$25$. This supports only the local claim that a finite start/stop-window enumeration can classify the supplied window under the stated rules. It does not license treating a codon-window pattern as an observed open reading frame without a separate sequence, transcript, or curated-annotation contact.

\paragraph{Curated interval contact.}
A later curated interval contact gives a separate biological test surface for ORF-eligibility, but only within its own dataset. That contact loaded $168$ rows from a curated interval dataset. The required input classes present were $\mathrm{boundary\_case}$, $\mathrm{matched\_non\_orf\_decoy}$, and $\mathrm{positive\_curated\_orf}$. The positive interval statistic accepted $72$ of $72$ positives, with accept rate $1.0$ and one-sided binomial upper-tail value $2.117582368135751\times 10^{-22}$. The matched decoy statistic rejected $72$ of $72$ decoys, with reject rate $1.0$ and the same one-sided binomial value $2.117582368135751\times 10^{-22}$. A predeclared paired comparison records $72$ wins in $72$ pairs, again with one-sided value $2.117582368135751\times 10^{-22}$.

\paragraph{Held-out and boundary controls.}
The curated interval contact also records three held-out splits. In the left-locus, middle-locus, and right-locus folds, each split used $24$ positives and $24$ decoys, with positive accept rate $1.0$, decoy reject rate $1.0$, and passed status true. The boundary-control block reports $24$ boundary cases and $0$ promotions, giving promoted fraction $0.0$. The classifier identifier is $\mathrm{orf\_start\_stop\_window\_eligibility}$, and the classifier-rules-modified flag is false. These checks matter because they separate a tested interval-contact result from a purely geometric codon-window record.

\paragraph{Local non-promotion rule.}
The scientific content of this chapter is therefore narrow. The $13$-codon equality for $R$, the $20$-codon ambient list $M$, the $18$-codon saturation, the quotient sizes and vectors, and the curated interval statistics are finite records with explicit scopes. From the motif compression alone one may not conclude translation realization, protein folding, physical admissibility, biological function, biochemical mechanism, or universality. Even the curated interval contact remains an external annotation contact for ORF-eligibility only; it does not convert codon geometry into a global biological claim. The self-check row records this boundary as a BioReality self-check with $3$ checked gates and states that no codon geometry is promoted across biological layers.