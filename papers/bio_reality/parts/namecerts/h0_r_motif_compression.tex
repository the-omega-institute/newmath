\subsection{Motif Compression and the ORF-Eligibility Boundary}
\label{sec:bioreality-h0-r-motif-compression}
\origin{ai}

\paragraph{Finite motif packet.}
This record concerns the finite row identified by \texttt{h0.R.motif.compression}.  Its biological reading is deliberately narrow: the packet records a motif-level codon compression and asks how far such a finite codon geometry can be carried before an independently supplied biological interval is required.  The verified motif set is
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The audit row reports no missing elements and no extra elements in this motif reconstruction.  Thus the claim is not an open-ended biological assertion; it is a finite equality check saying that the declared motif union has exactly the thirteen displayed codons and nothing else.  The equality was recorded in experiment \texttt{338799579a87} and verified at \texttt{2026-06-01T07:28:18+00:00}.

\paragraph{Ambient codon neighborhood.}
The same packet is linked to a larger finite neighborhood $M$ with cardinality $20$.  The audited list is
$$
\begin{aligned}
M=\{&\mathrm{AAA},\mathrm{AAG},\mathrm{ACA},\mathrm{ACG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\\
&\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\\
&\mathrm{UGA},\mathrm{UGG},\mathrm{UUA},\mathrm{UUG}\}.
\end{aligned}
$$
This gives a concrete containment context for the motif equality: $R$ is a thirteen-codon subconfiguration inside a twenty-codon finite universe.  The record \texttt{h0.M.equals.WNR.CUN} also stores $|M|=20$, so the ambient count and the displayed enumeration agree.  The compression claim therefore has two independent finite checks attached to it: the exact thirteen-element motif row and the exact twenty-element ambient row.

\paragraph{Saturation and quotient geometry.}
A further finite audit supplies the saturation boundary.  The row \texttt{h0.f3.saturation} reports
$$
\begin{aligned}
\operatorname{Sat}_{f_3}(R)=M\setminus\{\mathrm{ACA},\mathrm{ACG}\},
\end{aligned}
$$
with the eighteen codons
$$
\begin{aligned}
\{&\mathrm{AAA},\mathrm{AAG},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{AUG},\mathrm{CUA},\mathrm{CUC},\\
&\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UCG},\mathrm{UGA},\mathrm{UGG},\\
&\mathrm{UUA},\mathrm{UUG}\}.
\end{aligned}
$$
The same row records an empty $f_3$ boundary list, so the finite saturation calculation closes at the level of the enumerated codons.  The quotient-geometry audit gives the $M$-level face vector $(20,38,26,8,1)$ and the quotient face vector $(10,14,6,1)$, with quotient size $10$.  These numbers are useful as geometry, but they remain geometry: they do not by themselves state that any biological transcript was present, that any protein was made, or that any cellular role exists.

\paragraph{ORF-eligibility interpretation.}
The biological object attached to the row is open-reading-frame eligibility from start and stop codon enumeration.  In this chapter that phrase means only a finite start/stop-window test.  A separate row, \texttt{h2.orf.start\_stop.window\_eligibility}, reports that one external nucleotide window was loaded and that all loaded windows were classified by start/stop enumeration: the input-window count was $1$ and the classified count was $1$.  The stored scope is \texttt{external\_window\_start\_stop\_enumeration\_only}, and the row explicitly keeps the outcome at \texttt{orf\_eligibility\_only}.  Consequently, the motif compression can be used to organize the finite codon boundary, and the window row can say whether the supplied window passes the start/stop enumeration rule, but neither row turns a codon pattern into observed translation.

\paragraph{Curated interval contact.}
A stronger contact row is also linked to the packet: \texttt{h2.orf\_eligibility.curated\_interval\_contact}.  It records a curated interval dataset with $168$ rows from NCBI RefSeq GenBank XML CDS intervals for \textit{Escherichia coli} K-12 MG1655 accession \texttt{NC\_000913.3}.  Within that finite dataset, $72$ positive intervals were accepted, giving accept rate $1.0$, and $72$ matched non-ORF decoys were rejected, giving reject rate $1.0$.  The positive-interval exact one-sided binomial tail probability and the matched-decoy exact one-sided binomial tail probability are both
$$
\begin{aligned}
p=2.117582368135751\cdot 10^{-22}.
\end{aligned}
$$
The paired baseline comparison also records $72$ wins out of $72$ pairs, again with one-sided binomial tail probability $2.117582368135751\cdot 10^{-22}$.  The held-out split audit has three splits; each split contains $24$ positives and $24$ decoys, with positive accept rate $1.0$ and decoy reject rate $1.0$.  Boundary cases are not promoted: $24$ boundary cases were checked and $0$ were promoted, so the promoted fraction is $0.0$.

\paragraph{Local cannot-claim boundary.}
The finite rows above support only the stated finite conclusions.  From $R$ one may claim exact motif enumeration.  From $M$ one may claim the twenty-codon ambient neighborhood.  From the saturation row one may claim the eighteen-codon saturated list and the exclusion of $\mathrm{ACA}$ and $\mathrm{ACG}$ from that saturated list.  From the quotient row one may claim the recorded face-vector and quotient-size computations.  From the curated interval row one may claim the finite performance of the frozen start/stop-window classifier on the stated curated dataset and its matched controls.  None of these statements, either separately or together, establishes translation realization, folded molecular structure, physical admissibility, biological function, molecular mechanism, or a universal law over genomes.  Such stronger conclusions would require another contact whose data type is capable of testing the stronger biological layer.

\paragraph{Name-certificate reading.}
The name-certificate role of this row is therefore precise.  It certifies that \texttt{h0.R.motif.compression} is a finite codon-motif equality with an audited thirteen-element set, and it records how that equality sits beside a twenty-element codon neighborhood, an eighteen-element saturation list, a ten-point quotient image, a one-window start/stop enumeration check, and a $168$-row curated interval contact.  The self-check row records that three gates were checked and that no codon geometry is promoted across biological layers.  This is the correct boundary for the chapter: the finite motif and interval data make an ORF-eligibility test meaningful when the curated interval contact is present, while the biological layers above ORF eligibility remain outside the claim.
