\origin{ai}
\subsection{Family-stratified leave-one-code-out boundary for the codon label table}
\label{sec:bioreality-h1-leave-one-code-out-by-family}

\paragraph{Finite codon display.}
This record treats the standard-code codon table as a finite code-layer contact rather than as a claim about the whole translation apparatus.  The carrier is the six-coordinate codon cube $Q_6$, read as a binary display of the three nucleotide positions.  The distinctions used here are the local tile face, the singleton label corner, and the three-point label class.  The biological contact is a curated codon-to-label table: it records amino-acid and stop labels for codons, and it does not by itself observe ribosomal realization, folded structure, physical admissibility, biological function, or universal mechanism.

The distinguished boundary set $R$ has thirteen codons:
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The cardinality check gives $|R|=13$.  The same finite row identifies $R$ with a punctured-cube pattern whose face-vector is
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
This is the local geometric object being tested.  It is not promoted into a biological law; it is a code-table pattern whose readout is constrained to codon labels.

\paragraph{Spectral and shape checks.}
The finite spectral row reports
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The associated cubic or radial polynomial residual is $-6.224354365258478\times 10^{-12}$, so the displayed spectral relation is numerically satisfied to the recorded tolerance.  The shape-null row used $N=2000$ samples with seed $42$.  It observed one exact shape match, giving $p_{\mathrm{exact}}=0.0005$, and twenty-five samples with Jaccard score at least $0.9$, giving $p_{J\geq 0.9}=0.0125$.  The sampling log also records $57377$ distinct sample attempts and $55377$ duplicate retries.  These values support that the observed tile is a rare finite pattern under the sampled null model, but they still speak only about the codon-coordinate and label-table layer.

A second null model preserves amino-acid class.  In that row, $N=2000$, seed $42$, the observed size of $M$ is $20$, the exact-shape match count is $0$, and the lower-tail count at or below the observed $M$ size is $7$.  Thus
$$
\begin{aligned}
p_{\mathrm{class\ exact}} &= 0.0,\\
p_{\mathrm{class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
The row does not include a membership list for $M$, so this chapter uses only the recorded size and null statistics for $M$ and does not infer unreported codon membership.

\paragraph{Leave-one-code-out family row.}
The target claim is the family-stratified leave-one-code-out result.  The finite row evaluates five families and records the hit rate above baseline by family:
$$
\begin{aligned}
\mathrm{alternative\_yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\_plastid} &= 1.0,\\
\mathrm{candidate\_other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\_ciliate} &= 1.0.
\end{aligned}
$$
Thus four of the five named families have perfect above-baseline recovery in the recorded row, while the alternative-yeast family remains high but not perfect.  The aggregate leave-one-code-out row evaluated $23$ tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate in $M\setminus T$ is $0.9365079365079365$ and the hit rate in $M\setminus T$ together with boundary codons is $0.9841269841269841$.  The recorded above-baseline rate is also $0.9841269841269841$.  The family row therefore refines the aggregate result: the local tile-and-boundary signal remains strong after stratifying the code-table comparison by biological code family.

\paragraph{Single-codon recovery and module partition.}
The leave-one-codon-out row records no anchors, five boundary codons, eight codons with high closure coverage, and eight predictable codons.  Its recovery rate is $1.0$, and the threshold comparison is also $1.0$.  This makes the codon-level row compatible with the family-level row: the local construction recovers the recorded boundary-sensitive targets, but the recovery is still a finite label-table recovery.

The module classification is complete on the same thirteen-codon boundary set.  The six recorded modules are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA, AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA, CUC, CUG, CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA, UUA\},\\
\mathrm{stop\_trp} &= \{UAA, UAG, UGA\}.
\end{aligned}
$$
The completion row gives $R_{\mathrm{size}}=13$ and assigned count $13$, with no extra module codons, no missing codons from the modules, and no unassigned codons.  Hence the family-stratified leave-one-code-out result is not floating over an unspecified residue: every codon in $R$ is accounted for by the displayed module partition, and each module has either a predictable or boundary status in the recorded classification.

\paragraph{Interpretation and boundary.}
The finite evidence supports the following code-layer statement.  In the six-coordinate codon display, the standard-code labels expose a local boundary-tile pattern whose thirteen-codon residue, punctured-cube face-vector, spectral row, null-model rarity, module partition, and leave-one-code-out family recovery agree with the recorded finite rows.  The strongest row for this chapter is the family-stratified result: across five families, the above-baseline recovery rates are $0.8333333333333334,1.0,1.0,1.0,1.0$ in the named family order above.

The same evidence also fixes the limit of the claim.  The curated table supplies codon-to-label readback only.  No conclusion about translation realization, protein folding, structural order, physical admissibility, biological function, mechanistic necessity, or universality follows from this local tile.  Any such conclusion would require a separate contact that directly observes the higher layer.  In the present record, amino-acid labels and stop labels are finite code labels; they are not folded structures, not functions, and not a universal biological mechanism.