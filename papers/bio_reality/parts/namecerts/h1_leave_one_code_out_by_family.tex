\subsection{Leave-one-code-out family stratification for the codon boundary tile}
\label{sec:h1-leave-one-code-out-by-family}
\origin{ai}

\paragraph{Finite carrier and external label contact.}
This BioReality record is a finite-row statement about the standard-code codon table read through a six-bit codon display.  The carrier is the codon cube $Q_6$, and the only external contact used here is the curated standard-code label table.  That contact assigns each codon to an amino-acid or stop label; it does not provide a mechanism of translation, a folded protein structure, a physical admissibility test, a biological function, or a universality theorem.  The local object under test is therefore a code-layer boundary tile: a finite split inside the codon-label readback, not a global biological law.

The distinguished residual set $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
As a punctured-cube object, the same row reports the face vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the boundary tile is not introduced as a vague geometric metaphor: it is a concrete thirteen-codon subset with sixteen one-dimensional incidences and four two-dimensional faces in the finite codon display.

\paragraph{Spectral and null-model calibration.}
The spectral row fixes three numerical invariants for the same finite object.  The reported values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic check returns residual $-6.224354365258478\times 10^{-12}$, so the spectral relation is numerically saturated at the displayed precision.  The Monte Carlo shape calibration used $N=2000$ sampled tables, with $57377$ distinct-sample attempts and $55377$ duplicate retries.  It found $1$ exact shape match, giving
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005,
\end{aligned}
$$
and $25$ samples with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{Jaccard}\geq 0.9}=0.0125.
\end{aligned}
$$
A separate amino-acid-class-preserving null used $N=2000$, observed $M$-size $20$, found $0$ exact-shape matches, and had $7$ lower-tail samples at or below the observed size.  Its reported values are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These rows support a finite code-read anomaly statement: the observed boundary tile is unusually structured under the tested finite nulls.  They do not identify an evolutionary cause or a biochemical mechanism.

\paragraph{Leave-one-code-out family row.}
The chapter-specific row stratifies leave-one-code-out recovery by genetic-code family.  It evaluates $5$ families and reports the family hit rate above baseline as
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
The pooled leave-one-code-out row evaluated $23$ tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$.  The hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary was again $0.9841269841269841$.  The family stratification therefore says that the recovery signal is not confined to a single named code family: four of the five listed families reach rate $1.0$, and the remaining alternative-yeast family still reaches $0.8333333333333334$.

\paragraph{Codon-level module accounting.}
The leave-one-codon-out row supplies the local accounting behind the family statement.  It reports $0$ anchors, $5$ boundary codons, $8$ high-coverage closure codons, $8$ predictable codons, and recovery rate $1.0$.  The module classification is complete: $R$ has size $13$, the assigned count is $13$, and the extra, missing, and unassigned lists are all empty.  The six displayed modules are
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
Every codon in $R$ is accounted for by one of these modules, and every module has predictable or boundary status in the finite row.  This is the local tile boundary in its operational form: the row does not merely state that thirteen codons are special; it partitions those thirteen codons into biologically named code-change modules and checks that the leave-one-codon-out classifier covers them without residue.

\paragraph{Readback verdict and boundary of claim.}
The ready claim is that window-six codon coordinates expose a finite local boundary-tile structure for standard-code label readback, and that the leave-one-code-out signal persists when grouped by the five reported code families.  The concrete support consists of the thirteen-codon residual set, the punctured-cube face vector $(13,16,4)$, the spectral values $\lambda_M$, $\lambda_R$, and $\mu$, the small finite-null $p$-values, the pooled $23$-table and $63$-target leave-one-code-out rates, and the complete six-module codon accounting.

The cannot-claim boundary is part of the mathematical content of the record.  No translation realization follows from this row.  No folded structure follows from amino-acid label readback.  No physical admissibility, biological function, mechanism, or universality follows from the six-bit geometry or from the curated label table.  Any such higher-layer claim would require a separate contact whose observations live at that higher layer.  In the absence of that separate contact, this chapter remains a code-read theorem of finite rows: local codon-tile structure is present in the displayed data, and promotion beyond code labels is blocked.