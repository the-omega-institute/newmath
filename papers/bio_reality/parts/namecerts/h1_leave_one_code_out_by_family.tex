\subsection{Leave-One-Code-Out Stability by Code Family}\label{sec:h1-leave-one-code-out-by-family}
\origin{ai}

\paragraph{Finite carrier and readback.}
This record treats the standard-code codon table as a finite reality contact read through the six-bit codon cube $Q_6$.  The coordinate carrier is not a biological mechanism; it is a finite display in which codons are inspected by coordinate, closure, spectrum, and relation data.  The reality bridge is the curated standard genetic-code table, whose observed content is only that each codon has an amino-acid or stop label.  The chapter therefore works at the $\mathrm{code\_read}$ layer: it tests whether the window-six coordinate exposes a stable local boundary-tile signal, while keeping the label table as the external source of biological labels.

\paragraph{Residual row.}
The distinguished residual row $R$ has cardinality $13$.  The verified row is
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The punctured-cube check gives the same row and the finite face vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the local object is not merely a list of exceptional labels; it has the finite incidence profile of a punctured cubical tile inside the codon display.  The available finite-row data do not provide the individual codon list for $M$, but they do give $|M|=20$ through the amino-acid-class-preserving null-model run, so the row-level discussion here reports $M$ by its verified size rather than inventing an omitted codon list.

\paragraph{Spectral and shape checks.}
The associated spectral row is numerically stable at
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\cdot 10^{-12}$, and the same value is reported as the cubic-polynomial residual, so the numerical spectrum is at tolerance for this finite run.  Against $N=2000$ sampled comparison tables, the exact shape appeared once, giving
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005,
\end{aligned}
$$
and high-Jaccard matches with threshold $0.9$ appeared $25$ times, giving
$$
\begin{aligned}
p_{J\geq 0.9}=0.0125.
\end{aligned}
$$
The sampling log records $57377$ distinct-sample attempts and $55377$ duplicate retries under seed $42$.  Under the amino-acid-class-preserving null model with $N=2000$, the observed $M$ size was $20$, the exact-shape count was $0$, and the lower-tail count at or below the observed size was $7$, yielding
$$
\begin{aligned}
p_{\mathrm{aa\ class,exact}} &= 0.0,\\
p_{\mathrm{aa\ class,lower\ tail}} &= 0.0035.
\end{aligned}
$$
These numbers support a finite coordinate anomaly at the code-label layer, not a mechanism claim.

\paragraph{Leave-one-code-out family test.}
The leave-one-code-out test evaluated $23$ code tables and $63$ targets.  Its baseline hit rate was
$$
\begin{aligned}
0.19821739130434785,
\end{aligned}
$$
while the hit rate above baseline was
$$
\begin{aligned}
0.9841269841269841.
\end{aligned}
$$
The same value $0.9841269841269841$ is obtained when the recovery set is $M-T$ plus the boundary, and the more restrictive $M-T$ condition gives $0.9365079365079365$.  The family-level breakdown contains $5$ families.  Their hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\ plastid} &= 1.0,\\
\mathrm{candidate\ other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\ ciliate} &= 1.0.
\end{aligned}
$$
The signal is therefore not confined to a single named code family.  Four families are complete at the reported precision, while the alternative-yeast family remains high but not perfect.  This is the chapter-specific content of the claim: family grouping preserves the leave-one-code-out local-tile recovery pattern above the finite baseline.

\paragraph{Leave-one-codon-out and module closure.}
The codon-level leave-one-out run reports $0$ anchors, $5$ boundary cases, $8$ predictable cases, and $8$ high-closure-coverage cases, with recovery rate
$$
\begin{aligned}
1.0.
\end{aligned}
$$
The module classification covers all $13$ residual-row codons with no extra module codons, no missing row codons, and no unassigned codons.  The verified modules are
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
This is the finite module form behind the family result.  Each module has predictable-or-boundary status in the verified run, so the family-level stability is read as a row-and-module phenomenon in $Q_6$, not as a statement about downstream biochemical realization.

\paragraph{Reality-contact boundary.}
The only declared contact is the curated standard-code table.  It can test codon-label readback and local codon-tile label split.  It cannot test translation realization, folded structure, structural order, physical admissibility, biological function, mechanism, or universality.  Consequently, no translation, folding, physical admissibility, function, mechanism, or universality follows from the finite-row data unless a separate reality contact supplies that higher layer.  Amino-acid label-sequence readback remains a code-layer observation; it is not folded protein structure and it is not evidence of biological function.  The local codon tile is therefore a bounded code-read certificate: it records a stable family-wise boundary pattern in the curated label table while explicitly blocking promotion to a global biological law.