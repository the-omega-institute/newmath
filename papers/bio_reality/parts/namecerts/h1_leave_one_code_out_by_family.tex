\origin{ai}

\subsection{Family-wise leave-one-code-out boundary for the standard-code window}
\label{sec:h1-leave-one-code-out-by-family}

\paragraph{Finite carrier and readback contact.}
This record works at the code-read layer of the standard-code codon table.  The carrier is the six-bit codon cube $Q_6$, read through a curated table assigning each codon to an amino-acid or stop label.  The finite BEDC distinctions used here are the six-bit codon display, a local tile face, singleton label corners, and three-point label classes.  The external contact is deliberately narrow: it supplies codon-to-label readback, and it does not supply translation realization, folded structure, physical admissibility, biological function, a universal mechanism, or any necessity theorem saying that biological DNA must obey this geometry.

\paragraph{Boundary row.}
The finite boundary set $R$ has cardinality $13$.  In codon notation the row is
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
   &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same row is certified as the punctured-cube boundary row, with $f$-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the local object being tested is not the whole code table.  It is a thirteen-codon boundary packet inside the six-coordinate display, with four local two-faces and sixteen one-dimensional incidences.  The row is therefore suitable for code-layer split tests, but it is too small and too local to carry a global biological conclusion by itself.

\paragraph{Spectral finite signature.}
The associated finite spectral row has three reported scalar values:
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\times 10^{-12}$, so the displayed finite calculation is internally stable at the reported numerical tolerance.  This is a coordinate-spectrum fact about the code-read row.  It does not convert the codon labels into a folding, energetics, or function claim.

\paragraph{Shape rarity under finite null rows.}
The shape-significance row used $N=2000$ sampled tables with seed $42$.  It required $57377$ distinct-sample attempts and recorded $55377$ duplicate retries.  There was $1$ exact shape match, giving
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,
\end{aligned}
$$
and there were $25$ rows with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{J\geq 0.9}&=0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null row has observed $M$-size $20$.  In that null, $N=2000$, the exact-shape count is $0$, while $7$ samples land in the lower tail at or below the observed $M$-size.  The corresponding finite probabilities are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These rows support a finite rarity statement for the local shape relative to the sampled nulls.  They do not identify a biochemical cause.

\paragraph{Leave-one-code-out performance.}
The aggregate leave-one-code-out row evaluated $23$ tables and $63$ total targets.  The baseline hit rate was $0.19821739130434785$.  The observed hit rate above baseline was $0.9841269841269841$, the hit rate inside $M\setminus T$ was $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus boundary was $0.9841269841269841$.  The family-wise row partitions these tests into $5$ families.  The family hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
This is the chapter-specific claim: within the finite family split supplied by the row data, the local window-six boundary model remains above its baseline in all five families, with four families at perfect reported rate and the alternative-yeast family at $5/6$.

\paragraph{Codon-level recovery and module partition.}
The leave-one-codon-out row reports anchor count $0$, boundary count $5$, closure-coverage-high count $8$, predictable count $8$, and recovery rate $1.0$.  The module classification covers all $13$ boundary codons, with assigned count $13$, no missing codons, no extra module codons, and no unassigned codons.  The six modules are
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
Every module has a predictable-or-boundary witness in the finite row.  This makes the boundary packet inspectable at the module level rather than only as a thirteen-element list.

\paragraph{Local verdict and cannot-claim boundary.}
The finite data support a code-read statement: the six-bit codon display exposes a local boundary-tile structure whose family-wise leave-one-code-out behavior is far above the stated baseline, and whose thirteen-codon row is completely classified by the listed modules.  The curated standard-code contact licenses only codon-label readback and local split checks.  No translation process, folded protein structure, structural order, physical admissibility, biological function, universal mechanism, or global biological law follows from this row unless a separate higher-layer contact supplies that evidence.  Amino-acid label-sequence readback remains a code-layer observation; it is not a protein-structure observation.