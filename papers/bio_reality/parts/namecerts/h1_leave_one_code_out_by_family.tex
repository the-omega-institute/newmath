\origin{ai}

\subsection{Leave-one-code-out family audit for the codon boundary tile}
\label{sec:h1-leave-one-code-out-by-family}

\paragraph{Finite object.}
The record studies a window-six representation of the standard genetic code, with each codon treated as a six-bit coordinate in the codon cube $Q_6$.  The observed object is not translation itself, but the finite label table that assigns each codon to an amino-acid or stop label.  The relevant reality contact is therefore a curated standard-code table at code-read resolution.  It permits comparison between codon coordinates and labels, and it does not by itself observe ribosomal realization, molecular structure, thermodynamic admissibility, phenotype, function, mechanism, or universality.

\paragraph{Boundary set.}
The finite boundary set $R$ has cardinality $13$.  In the verified row it is exactly
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row identifies $R$ with a punctured-cube pattern whose face-vector is $(13,16,4)$.  This is the local finite geometry being tested: a codon-coordinate tile with a removed or exposed boundary profile, read against the standard-code labels.  The row does not claim that this geometry is a global law of biology; it claims that this particular finite label table has a nontrivial local boundary organization under the chosen six-bit codon display.

\paragraph{Spectral and sampling checks.}
The spectral summary records $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The radial cubic check has residual $-6.224354365258478\times 10^{-12}$, the same value reported for the cubic-polynomial residual, so the numerical spectral certificate is internally stable at the recorded tolerance.  The shape-significance sample used $N=2000$ random trials with seed $42$.  It found one exact shape match, giving $p_{\mathrm{exact}}=0.0005$, and $25$ matches at Jaccard threshold at least $0.9$, giving $p_{J\geq 0.9}=0.0125$.  The row also reports $57377$ distinct-sample attempts and $55377$ duplicate retries, so the finite experiment was operating in a constrained combinatorial space rather than an unlimited independent draw model.

\paragraph{Amino-acid-class-preserving null.}
A second null model preserves amino-acid class information.  In $N=2000$ trials with seed $42$, it found no exact shape match and $7$ lower-tail events at or below the observed $M$ size.  The observed size was $|M|=20$, with $p_{\mathrm{aa\ class,exact}}=0.0$ and lower-tail value $0.0035$.  Thus the recorded boundary behavior is not only unusual against the broader shape sampler; it also remains rare when the randomization keeps the amino-acid class partition fixed.  The row gives the size of $M$ but does not provide an explicit codon list for $M$, so the present section uses the size and null statistics rather than reconstructing an unlisted set.

\paragraph{Leave-one-code-out result.}
Across the leave-one-code-out audit, $23$ code tables were evaluated and $63$ targets were counted.  The baseline hit rate was $0.19821739130434785$.  The hit rate in $M\setminus T$ was $0.9365079365079365$, while the hit rate after adding the boundary component was $0.9841269841269841$.  The same value, $0.9841269841269841$, is reported as the hit rate above baseline.  The finite interpretation is that the local coordinate-and-boundary construction recovers most of the code-variation targets in this row, and the boundary addition accounts for the last improvement from the high $M\setminus T$ rate to the reported near-complete rate.

\paragraph{Family split.}
The family-level row has $5$ families.  The hit-rate-above-baseline values are
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
Four families are saturated at $1.0$, and the alternative-yeast family remains high at $5/6$.  This makes the claim more specific than a single pooled success statistic: the recovery behavior is distributed across named code-table families, with only one listed family below complete recovery in the recorded finite run.

\paragraph{Module classification.}
The leave-one-codon-out module row assigns all $13$ codons of $R$ and reports no missing, extra, or unassigned codons.  The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The classification row states $R\_\mathrm{size}=13$ and assigned count $13$, with every module containing predictable or boundary codons.  The associated leave-one-codon-out figures are anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$.  These data support a local module reading: the codons that form the punctured-cube boundary are partitioned into biologically named recoding-relevant groups, and the finite audit records complete coverage at the module level.

\paragraph{Scope of the claim.}
The supported statement is deliberately narrow.  The six-bit codon window exposes a local boundary tile for the standard-code label table, and the leave-one-code-out family audit shows strong finite recovery against the recorded baselines.  The contact remains a code-label contact.  It does not observe translation machinery, folded protein structure, physical admissibility, biological function, evolutionary necessity, or a universal mechanism.  Any move from this code-read layer to those stronger layers requires an additional empirical contact of the appropriate kind.  Without such a contact, an amino-acid label sequence is only a label readback, not a structural or functional biological realization.