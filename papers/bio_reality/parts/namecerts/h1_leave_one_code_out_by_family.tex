\origin{ai}
\subsection{Leave-one-code-out behavior by code family}
\label{sec:bioreality-h1-leave-one-code-out-by-family}

\paragraph{Finite codon display.}
The experiment uses the standard-code codon table as a curated codon-to-label contact and places it on a six-coordinate codon display.  The object under test is not a biochemical machine: it is the finite question of whether the window-six codon coordinates expose a stable local boundary pattern when one compares code tables by family.  The observed exceptional region $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
As a finite cubical set this same set has the recorded face vector $(13,16,4)$, so the row is not merely a list of labels: it is a small punctured-cube pattern in the codon-coordinate presentation.  The table contact only supplies label readback, and all geometric language in this subsection refers to that finite coordinate representation.

\paragraph{Spectral and shape checks.}
The spectral row gives $\lambda_M = 0.6752479308830213$, $\lambda_R = 0.4758352843197002$, and $\mu = 3.051487585298128$.  The radial cubic check is numerically closed at $-6.224354365258478\times 10^{-12}$, which is within the recorded tolerance for the finite computation.  Against $2000$ sampled null trials for the shape statistic, the exact-shape count is $1$, giving $p=0.0005$, and the high-overlap threshold $J\geq 0.9$ has $25$ matches, giving $p=0.0125$.  The sampling log also records $57377$ distinct-sample attempts and $55377$ duplicate retries, so the finite test is best read as a stringent comparison against a constrained discrete search space, not as a continuous asymptotic estimate.

\paragraph{Family-level leave-one-code-out row.}
The family-level leave-one-code-out row evaluates $5$ code families.  The hit rate above the baseline is $1.0$ for bacterial plastid, $1.0$ for mitochondrial, $1.0$ for nuclear ciliate, $1.0$ for the candidate-other family, and $0.8333333333333334$ for alternative yeast.  Thus every recorded family is above the baseline test, with the only non-perfect family still recovering five sixths of the tested family-level targets above baseline.  This family split should be compared with the coarser leave-one-code-out aggregate: $23$ tables were evaluated, $63$ targets were tested, the baseline hit rate was $0.19821739130434785$, the hit rate in $M\setminus T$ was $0.9365079365079365$, and the rate in $M\setminus T$ plus the boundary was $0.9841269841269841$.  The finite evidence therefore says that the local codon-coordinate neighborhood is highly predictive for the code-label rows under this leave-one-family organization.

\paragraph{Codon-module accounting.}
The codon-level module classification accounts for all $13$ codons in $R$ without residue: the assigned count is $13$, the recorded $R$ size is $13$, the missing-from-modules list is empty, the extra-module-codons list is empty, and the unassigned-codons list is empty.  The six modules are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
Each module has either a predictable member or a boundary member in the recorded codon-level test.  In the leave-one-codon-out row the predictable count is $8$, the boundary count is $5$, the anchor count is $0$, the high-closure coverage is $8$, and the recovery rate is $1.0$.  This is the finite reason to treat the $13$-codon region as a coherent local tile boundary rather than as unrelated codon exceptions.

\paragraph{Amino-acid-class preserving null.}
A second null model preserves amino-acid class while randomizing the relevant finite structure.  In $2000$ trials it gives exact-match count $0$ and lower-tail count $7$ at the observed $M$ size $20$.  The exact-shape value is therefore recorded as $p=0.0$ at this sample resolution, and the lower-tail value is $p=0.0035$.  These numbers support a code-layer distinction: the observed local tile is not reproduced by the sampled amino-acid-class preserving null at ordinary frequency.  They do not identify a physical cause of the genetic code and do not by themselves distinguish historical contingency from biochemical selection.

\paragraph{Local interpretation.}
The finite conclusion is that a six-coordinate codon display, together with the curated standard-code label table, exposes a stable local boundary pattern concentrated on the $13$ codons listed above.  The strongest family-level row is the above-baseline recovery across $5$ families, with four families at $1.0$ and alternative yeast at $0.8333333333333334$.  The supporting rows agree: the punctured-cube face vector is $(13,16,4)$, the spectral values are numerically stable, the module decomposition is complete, and the leave-one-codon-out recovery is $1.0$ when predictable and boundary codons are counted together.

\paragraph{Scope limit.}
This chapter stays at the code-read layer.  The curated table observes codon labels, not ribosomal translation dynamics, protein folding, structural order, physical admissibility, biological function, a universal mechanism, or a law forcing biology to obey this coordinate geometry.  An amino-acid label sequence is still only a label sequence here.  Any statement about translation realization, folded protein structure, physical viability, biological function, or universality would require a separate contact that directly observes that higher layer.