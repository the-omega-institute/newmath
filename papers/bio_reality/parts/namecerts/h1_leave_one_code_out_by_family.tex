\origin{ai}
\subsection{Family-stratified leave-one-code-out boundary for the codon tile}
\label{sec:h1-leave-one-code-out-by-family}

\paragraph{Finite carrier and readback contact.}
The record is a finite code-read experiment on the six-bit codon cube $Q_6$.  The carrier is not a protein object and not a translation mechanism; it is a coordinate display for codons together with a curated standard-code label table.  The only external contact used here is the table-level fact that each codon has an amino-acid or stop label in the standard code.  Thus the readback layer is the code-label layer: singleton label corners and three-point label classes can be inspected as finite rows, but no folded structure, physical admissibility, biological function, universal mechanism, or necessity claim follows from the row geometry alone.

\paragraph{Boundary set.}
The distinguished boundary row $R$ has cardinality $13$.  Its codons are
$$\begin{aligned}
R=\{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}$$
As a punctured local cube, the finite row has face-vector
$$\begin{aligned}
f(R)=(13,16,4).
\end{aligned}$$
This is the local tile under discussion.  The statement is deliberately weaker than a law of biology: it says that the standard-code labels expose a finite boundary tile in $Q_6$, not that biological translation is generated by this tile.

\paragraph{Spectral and shape checks.}
The finite spectrum attached to the row gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The radial cubic residual is numerically negligible at $-6.224354365258478\times 10^{-12}$, recorded equivalently as the cubic-polynomial residual and the radial-polynomial value.  In the unrestricted finite null with $N=2000$, the exact shape appeared once, giving
$$\begin{aligned}
p_{\mathrm{exact}}=0.0005,
\qquad
p_{J\geq 0.9}=0.0125,
\end{aligned}$$
with $25$ samples at Jaccard similarity at least $0.9$.  The sampling log also records $57377$ distinct-sample attempts and $55377$ duplicate retries under seed $42$.  Under the amino-acid-class-preserving null with $N=2000$, the observed module size is $20$, the exact-shape count is $0$, and the lower-tail count at or below the observed size is $7$, giving
$$\begin{aligned}
p_{\mathrm{aa\ class, exact}}=0,
\qquad
p_{\mathrm{aa\ class, lower\ tail}}=0.0035.
\end{aligned}$$
These numbers support the claim that the displayed tile is a non-generic finite-row feature of the code-read table.  They do not supply a molecular mechanism.

\paragraph{Leave-one-code-out aggregate.}
The aggregate leave-one-code-out test evaluated $23$ tables and $63$ targets.  The baseline hit rate is $0.19821739130434785$.  The row hit rate inside $M-T$ is $0.9365079365079365$, and the row hit rate inside $M-T$ plus boundary is $0.9841269841269841$.  The hit rate above baseline is also $0.9841269841269841$.  Thus the finite boundary row is strongly reusable for code-label recovery across the tested table rows, while remaining a code-read statement: the successful target is a codon-label placement, not a protein fold or function.

\paragraph{Family-stratified result.}
The family-stratified leave-one-code-out table has $5$ families.  The hit rates above baseline are
$$\begin{aligned}
\text{alternative yeast}&:0.8333333333333334,\\
\text{bacterial plastid}&:1.0,\\
\text{candidate other}&:1.0,\\
\text{mitochondrial}&:1.0,\\
\text{nuclear ciliate}&:1.0.
\end{aligned}$$
This is the specific finite-row content of the claim.  Four families are saturated at hit rate $1.0$, while the alternative-yeast family remains high but not saturated at $0.8333333333333334$.  The family split therefore refines the aggregate result without changing its layer: it is evidence for robust local code-label recovery by family, not evidence that window-six geometry realizes translation or function.

\paragraph{Codon-level module classification.}
The leave-one-codon-out record has anchor count $0$, boundary count $5$, predictable count $8$, high closure coverage $8$, and recovery rate $1.0$.  The module classification assigns all $13$ codons of $R$ with no missing, extra, or unassigned codons.  The modules are
$$\begin{aligned}
\text{AGR}&=\{\texttt{AGA},\texttt{AGG}\},\\
\text{AR-Lys}&=\{\texttt{AAA}\},\\
\text{CUN}&=\{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\},\\
\text{Ile-Met}&=\{\texttt{AUA}\},\\
\text{SSN}&=\{\texttt{UCA},\texttt{UUA}\},\\
\text{Stop-Trp}&=\{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}.
\end{aligned}$$
Every module has either predictable or boundary support.  This completes the finite code-read partition of the boundary row: $R$ has size $13$, the assigned count is $13$, and the lists of extra module codons, missing codons, and unassigned codons are all empty.

\paragraph{Local verdict and cannot-claim boundary.}
The family-stratified record is ready as a BioReality code-read claim.  The claim is that window-six codon coordinates expose a local boundary-tile structure whose leave-one-code-out recovery remains high by family, with family rates $0.8333333333333334,1.0,1.0,1.0,1.0$ and complete module coverage of the $13$ boundary codons.  The claim stops there.  A local codon tile is not a global biological law; window-six geometry alone is not translation, folded structure, physical admissibility, function, mechanism, or universality; and amino-acid label-sequence readback is not protein structure.  Any promotion beyond the code-read layer requires a separate reality contact that actually observes the higher-layer object.