\subsection{Leave-One-Code-Out Recovery by Code Family}\label{sec:h1-leave-one-code-out-by-family}
\origin{ai}

\paragraph{Finite carrier and contact.}
This BioReality record treats the standard-code codon table as a finite code-read contact, not as a mechanism. The carrier is the six-bit codon cube $Q_6$, read through a curated standard-code label table. The relevant finite distinction is a local tile face with singleton label corners and three-point label classes. The contact says only that codons have curated amino-acid or stop labels. It does not say how translation is realized, how a protein folds, which physical states are admissible, which functions occur, or whether the observed pattern is universal.

\paragraph{Boundary set.}
The distinguished boundary row is the thirteen-codon set
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The finite audit records $|R|=13$. The same row is a punctured-cube object with $f$-vector $(13,16,4)$, so the chapter is not using an unconstrained list of exceptions. It is using a concrete thirteen-vertex boundary object inside the codon cube. The row-level module classification is complete: $R_{\mathrm{size}}=13$, the assigned count is $13$, the missing-from-modules list is empty, the extra-module-codons list is empty, and the unassigned-codons list is empty.

\paragraph{Modules.}
The thirteen boundary codons decompose into six named modules. The recorded modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every module has either a predictable codon or a boundary codon in the leave-one-codon-out record. The leave-one-codon-out row has anchor count $0$, boundary count $5$, predictable count $8$, high closure coverage $8$, and recovery rate $1.0$. Thus the finite recovery signal is not a single isolated codon event; it is distributed across the named boundary modules.

\paragraph{Leave-one-code-out family readback.}
The family-stratified leave-one-code-out row evaluates five code families. The family count is $5$, and the hit-rate-above-baseline values are
$$
\begin{aligned}
\mathrm{alternative\_yeast}&=0.8333333333333334,\\
\mathrm{bacterial\_plastid}&=1.0,\\
\mathrm{candidate\_other}&=1.0,\\
\mathrm{mitochondrial}&=1.0,\\
\mathrm{nuclear\_ciliate}&=1.0.
\end{aligned}
$$
The all-family summary is therefore four perfect family rows and one non-perfect but still high row. The broader leave-one-code-out experiment evaluated $23$ tables and $63$ targets. Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$. The hit rate inside $M-T$ is $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$. These numbers support a code-read claim: the six-bit local tile boundary is highly aligned with curated label changes across the evaluated finite rows.

\paragraph{Spectral and null checks.}
The spectral row reports
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The associated cubic, or radial, polynomial residual is $-6.224354365258478\times 10^{-12}$, so the displayed spectral relation is numerically closed to the stated tolerance. The shape-significance null used $N=2000$ samples with seed $42$, $57377$ distinct-sample attempts, and $55377$ duplicate retries. It found $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$, giving $p_{\mathrm{exact}}=0.0005$ and $p_{\mathrm{Jaccard}\geq 0.9}=0.0125$. The amino-acid-class-preserving null, also with $N=2000$ and seed $42$, recorded observed $M$ size $20$, exact match count $0$, lower-tail count $7$, $p_{\mathrm{exact}}=0.0$, and $p_{\mathrm{lower\ tail}}=0.0035$. These null rows make the tile signal hard to explain as an arbitrary relabeling artifact inside the tested finite models.

\paragraph{Claim boundary.}
The ready claim is narrow: window-six codon coordinates expose a local boundary-tile structure whose leave-one-code-out recovery remains high when stratified by code family. The curated standard-code table supplies the reality contact for codon-label readback only. No translation process, folded protein structure, physical admissibility, biological function, universal mechanism, or global biological law follows from this chapter. Crossing any of those layers requires a separate contact whose observations live at that layer.