\origin{ai}

\subsection{Leave-One-Code-Out Family Readback for the Window-Six Codon Tile}
\label{sec:h1-leave-one-code-out-by-family}

\paragraph{Finite carrier and code-layer scope.}
The record concerns a six-coordinate codon display $Q_6$ whose observable readout is the curated standard genetic-code label table.  The carrier is used only as a finite coordinate arena for codon labels.  It is not treated as a biological mechanism, a translation apparatus, a folding model, a physical feasibility criterion, or a universal law.  The claim tested here is narrower: after one code table is withheld, the local window-six tile and its boundary information retain enough code-layer structure to recover many held-out codon-label targets above a naive baseline, and this behavior remains visible after grouping alternate code tables by family.

\paragraph{The punctured cube component.}
The finite boundary set $R$ has cardinality $13$.  Its codons are
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row records $R$ as a punctured-cube object with face-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the relevant finite object is not merely a list of exceptional codons.  It has a small two-dimensional tile profile inside the six-bit codon cube, with thirteen vertices, sixteen one-dimensional adjacencies, and four local faces.  This is the local geometric signal used by the leave-one-code-out probe.

\paragraph{Spectral and shape checks.}
The spectral row gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The recorded cubic residual is
$$
\begin{aligned}
-6.224354365258478\cdot 10^{-12},
\end{aligned}
$$
which is also the radial-polynomial residual.  The checked spectral-radius value is $0.6752479308830213$.  These numbers support the local tile as a reproducible finite coordinate object rather than an arbitrary hand-picked phrase.

The shape-significance row used $N=2000$ sampled controls with seed $42$.  The observed exact-shape match count was $1$, so the exact-shape probability estimate was
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005.
\end{aligned}
$$
The high-overlap criterion $J\geq 0.9$ occurred $25$ times, giving
$$
\begin{aligned}
p_{J\geq 0.9}=0.0125.
\end{aligned}
$$
The sampling log also records $57377$ distinct-sample attempts and $55377$ duplicate retries, so the low exact-match rate is not being reported without its finite sampling burden.  Under the amino-acid-class-preserving null model, again with $N=2000$ and seed $42$, the observed $M$ size was $20$, the exact match count was $0$, and the lower-tail count at or below the observed $M$ size was $7$.  The corresponding values are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}=0.0,\qquad p_{\mathrm{aa\ class\ lower\ tail}}=0.0035.
\end{aligned}
$$
These controls do not make the codon tile causal.  They say only that the finite shape is unusually structured relative to the tested randomizations.

\paragraph{Leave-one-code-out performance.}
The aggregate leave-one-code-out probe evaluated $23$ code tables and $63$ held-out targets.  The baseline hit rate was $0.19821739130434785$.  The tile-based recovery row reports
$$
\begin{aligned}
\mathrm{hit\ rate\ above\ baseline}&=0.9841269841269841,\\
\mathrm{hit\ rate\ in}\ M-T&=0.9365079365079365,\\
\mathrm{hit\ rate\ in}\ M-T\ \mathrm{plus\ boundary}&=0.9841269841269841.
\end{aligned}
$$
The family-level row refines this aggregate result into five families.  The family count is $5$, with hit rates above baseline
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
The weakest listed family is alternative yeast, still above the baseline rate by a large margin.  The other four listed families achieve full above-baseline recovery in the finite row.  This is the specific content of the family claim: the local codon tile is not only an aggregate summary over all alternate code tables, but remains visible after the alternate tables are grouped into biologically named code families.

\paragraph{Module accounting.}
The module-classification row assigns all thirteen $R$ codons to six modules and reports no missing, extra, or unassigned codons.  The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The completion check records $R$ size $13$, assigned count $13$, no extra module codons, no codons missing from the modules, and no unassigned codons.  The same finite row states that every module has either a predictable member or a boundary member.  This closes the internal bookkeeping for the family-level readback: the codons used by the local tile are all accounted for by named modules, and the modules all meet the predictable-or-boundary criterion.

\paragraph{Single-codon withholding check.}
The leave-one-codon-out row gives anchor count $0$, boundary count $5$, closure coverage high $8$, predictable count $8$, and recovery rate $1.0$.  The threshold comparison is also $1.0$.  This check is weaker in biological scope than the leave-one-code-out family result, but it is useful as a local consistency test: individual withheld codons in the row are recovered through predictable or boundary behavior rather than through an independent mechanistic model.

\paragraph{Reality contact and non-promotion rule.}
The only reality contact used here is the curated standard-code table assigning codons to amino-acid or stop labels.  That contact can test code-layer readback and local codon-tile label splits.  It cannot test translation realization, folded structure, structural order, physical admissibility, biological function, or universal mechanism.  Therefore the result may be stated as a finite code-read pattern: the window-six codon coordinates expose a local boundary tile whose leave-one-code-out family behavior is strongly above the listed baseline.  It may not be promoted to a claim about protein folding, function, organism-level necessity, or a universal biological law unless an additional and separately specified contact supplies that higher-layer observation.

\paragraph{Verdict.}
For claim $\mathrm{h1.leave\_one\_code\_out.by\_family}$, the finite row is ready as a code-layer chapter record.  The key numbers are the five-family summary, the $23$ table and $63$ target aggregate, the $13$-codon punctured cube, the six complete modules, the spectral values $\lambda_M=0.6752479308830213$ and $\lambda_R=0.4758352843197002$, and the shape-control probabilities $0.0005$, $0.0125$, and $0.0035$.  The scientific reading is deliberately local: strong codon-label recovery is observed inside the curated code table setting, while every claim beyond code-label readback remains outside the evidence supplied by this record.