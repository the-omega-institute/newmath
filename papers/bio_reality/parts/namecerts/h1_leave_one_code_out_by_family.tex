\origin{ai}
\subsection{Leave-One-Code-Out Stability by Family}
\label{sec:biorality-h1-leave-one-code-out-by-family}

\paragraph{Finite object and contact layer.}
This record concerns the claim \texttt{h1.leave\_one\_code\_out.by\_family}.  The carrier is the six-bit codon cube $Q_6$, read only through the standard-code label table.  The biological contact is therefore a curated codon-to-label table: each codon is assigned an amino-acid label or a stop label, and no translation process, molecular structure, physical admissibility condition, biological function, mechanism, or universality principle is observed by this contact.  The local object is a window-six boundary tile in the code-read layer.  It may support finite label readback and local split tests, but it is not by itself a global biological law.

\paragraph{Punctured-cube row.}
The distinguished row $R$ has cardinality $13$.  Its verified codons are
$$\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}$$
The same row is verified as a punctured-cube pattern with $f$-vector
$$\begin{aligned}
f(R)=(13,16,4).
\end{aligned}$$
This is the finite boundary set on which the family-level leave-one-code-out calculation is evaluated.  The row is not interpreted as a biochemical generator; it is a coordinate-and-closure object whose readback is mediated by the external standard-code table.

\paragraph{Spectral and shape evidence.}
The finite spectrum separates the marked row from its ambient window.  The verified numerical values are
$$\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}$$
The cubic or radial polynomial residual is $-6.224354365258478\times 10^{-12}$, so the reported spectral-radius calculation is numerically closed at the stated tolerance.  In the shape null model with $N=2000$ sampled rows and seed $42$, exactly one sampled row matched the observed shape and $25$ had Jaccard score at least $0.9$.  Thus
$$\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9}&=0.0125.
\end{aligned}$$
The run attempted $57377$ distinct samples and encountered $55377$ duplicate retries, so the finite-row report records both the small tail counts and the sampling pressure.  Under the amino-acid-class-preserving null model, again with $N=2000$ and seed $42$, the observed marked size is $|M|=20$, the exact-shape match count is $0$, and the lower-tail count at or below the observed size is $7$.  The corresponding reported values are
$$\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}$$
These figures support finite shape specificity at the code-read level, not a claim about translation dynamics or protein structure.

\paragraph{Leave-one-code-out aggregate.}
Across the leave-one-code-out experiment, $23$ code tables were evaluated against $63$ total targets.  The baseline hit rate is $0.19821739130434785$.  The verified hit rate inside $M\setminus T$ is $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus the boundary is $0.9841269841269841$.  The same value is reported as the hit rate above baseline:
$$\begin{aligned}
h_{\mathrm{baseline}}&=0.19821739130434785,\\
h_{M\setminus T}&=0.9365079365079365,\\
h_{M\setminus T+\partial}&=0.9841269841269841,\\
h_{\mathrm{above\ baseline}}&=0.9841269841269841.
\end{aligned}$$
The family-level refinement partitions the tested code tables into $5$ families.  The verified family hit rates above baseline are
$$\begin{aligned}
h_{\mathrm{alternative\ yeast}}&=0.8333333333333334,\\
h_{\mathrm{bacterial\ plastid}}&=1.0,\\
h_{\mathrm{candidate\ other}}&=1.0,\\
h_{\mathrm{mitochondrial}}&=1.0,\\
h_{\mathrm{nuclear\ ciliate}}&=1.0.
\end{aligned}$$
Thus all five families remain above the baseline row, with four families at full hit rate and the alternative-yeast family still at $5/6$.  The conclusion is deliberately narrow: the local tile has stable family-level code-label readback under the reported leave-one-code-out protocol.

\paragraph{Leave-one-codon and module rows.}
The related leave-one-codon-out row reports $8$ predictable codons, $5$ boundary codons, $0$ anchors, closure coverage high at $8$, and recovery rate $1.0$.  The module classification accounts for all $13$ row codons: $R_{\mathrm{size}}=13$, assigned count $13$, no extra module codons, no missing row codons, and no unassigned codons.  The verified modules are
$$\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}$$
Each listed module has a predictable-or-boundary explanation in the finite row.  This supplies a local accounting of the thirteen codons that drive the family-level result.

\paragraph{Boundary verdict.}
The verdict is ready at the code-read layer.  The finite data show that the six-bit coordinate row, its punctured-cube boundary, the spectral calculation, the null-model tail counts, the leave-one-code-out aggregate, the family hit rates, and the module classification agree as a finite local tile around the curated standard-code table.  The result does not claim that DNA obeys this geometry as biological necessity.  It does not claim translation realization, folded structure, structural order, physical admissibility, biological function, universal mechanism, or a law of all genetic codes.  Any such promotion would require a separate contact whose observable content is translation, structure, admissibility, function, mechanism, or universality rather than codon-label readback.