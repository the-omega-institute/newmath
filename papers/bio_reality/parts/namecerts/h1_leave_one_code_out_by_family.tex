\subsection{Family-resolved leave-one-code-out stability for the window-six codon tile}\label{sec:h1-leave-one-code-out-by-family}
\origin{ai}

\paragraph{Claim.}
The claim tested here is that the window-six codon coordinate system detects a stable local boundary pattern across families of non-standard code tables, while the amino-acid and stop labels themselves remain supplied by a curated code table. The finite carrier is the six-coordinate codon cube $Q_6$, read as a codon display with local faces, singleton label corners, and three-point label classes. The row under audit is $\texttt{h1.leave\_one\_code\_out.by\_family}$, with experiment run $\texttt{7dda978c6c97}$ verified at $\texttt{2026-06-03T05:29:06+00:00}$.

\paragraph{Observed punctured-cube set.}
The distinguished codon set $R$ has cardinality $13$. The verified row gives
$$
\begin{aligned}
R=\{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}
$$
The same row records the punctured-cube face data
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4),
\end{aligned}
$$
so the finite object is not merely a list of exceptional codons: it carries a local cubical incidence shape. In the downstream module classification, these same $13$ codons are partitioned without residue into six modules:
$$
\begin{aligned}
\text{AGR}&=\{\texttt{AGA},\texttt{AGG}\},\\
\text{AR/Lys}&=\{\texttt{AAA}\},\\
\text{CUN}&=\{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\},\\
\text{Ile/Met}&=\{\texttt{AUA}\},\\
\text{SSN}&=\{\texttt{UCA},\texttt{UUA}\},\\
\text{Stop/Trp}&=\{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}.
\end{aligned}
$$
The module row reports $R\_\mathrm{size}=13$, assigned count $13$, no extra module codons, no missing codons, and no unassigned codons. Thus the local partition is total on $R$.

\paragraph{Spectral and sampling evidence.}
The recorded spectral quantities are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic check is numerically closed to tolerance, with polynomial value $-6.224354365258478\times 10^{-12}$. The finite sampling rows also record that, among $N=2000$ sampled shape comparators with seed $42$, there was $1$ exact shape match and $25$ matches at Jaccard score at least $0.9$. The corresponding probabilities are
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9}&=0.0125.
\end{aligned}
$$
An amino-acid-class-preserving null run, also with $N=2000$ and seed $42$, found exact match count $0$, lower-tail count $7$, observed $M$ size $20$, exact-shape probability $0.0$, and lower-tail probability $0.0035$. These numbers support a finite local signal: the observed punctured-cube organization is rare under the recorded comparator regimes, while still being a code-layer pattern rather than a biochemical mechanism.

\paragraph{Leave-one-code-out family row.}
The family-resolved leave-one-code-out audit evaluates $5$ families. The hit rates above baseline are
$$
\begin{aligned}
\text{alternative yeast}&:0.8333333333333334,\\
\text{bacterial/plastid}&:1.0,\\
\text{candidate other}&:1.0,\\
\text{mitochondrial}&:1.0,\\
\text{nuclear ciliate}&:1.0.
\end{aligned}
$$
Thus four of the five reported families achieve perfect above-baseline recovery in this test, and the remaining family stays high at $5/6$. The coarser leave-one-code-out row supplies the corresponding aggregate setting: $23$ code tables evaluated, $63$ total targets, baseline hit rate $0.19821739130434785$, hit rate in $M-T$ equal to $0.9365079365079365$, and hit rate in $M-T$ plus boundary equal to $0.9841269841269841$. The family row is therefore not an isolated display statistic; it refines an already high aggregate leave-one-code-out recovery into biologically named table families.

\paragraph{Leave-one-codon-out compatibility.}
The codon-level deletion row gives anchor count $0$, boundary count $5$, closure coverage high $8$, predictable count $8$, and recovery rate $1.0$. The companion module row states that every module has either predictable or boundary status, namely AGR, AR/Lys, CUN, Ile/Met, SSN, and Stop/Trp. This is the local consistency check needed for the family row: the family-level success is not obtained by losing the codon modules, because the modules still cover exactly the $13$ codons of $R$ and every module is accounted for by the predictable-or-boundary classification.

\paragraph{Reality contact and scope.}
The label contact used here is a curated standard-code table assigning codons to amino-acid or stop labels. It can test code readback and local codon-tile label splits. It does not test translation realization, folded structure, physical admissibility, biological function, or a universal generative mechanism. Consequently, the finite evidence supports the following restricted conclusion: in $Q_6$, the standard-code label table and the observed non-standard code families expose a stable local boundary pattern on the $13$-codon punctured-cube set $R$, with strong leave-one-code-out family recovery and complete module coverage. No conclusion about actual translation dynamics, protein folding, molecular viability, organismal function, mechanistic causation, or universality follows from these rows unless an additional contact of the appropriate kind is supplied.

\paragraph{Verdict.}
The row $\texttt{h1.leave\_one\_code\_out.by\_family}$ is ready as a finite code-read claim. Its numerical content is specific: $5$ families, four family rates equal to $1.0$, one equal to $0.8333333333333334$, aggregate leave-one-code-out rate $0.9841269841269841$ with boundary, punctured-cube set size $13$, face vector $(13,16,4)$, complete six-module classification, spectral values $\lambda_M=0.6752479308830213$ and $\lambda_R=0.4758352843197002$, and rare-shape sampling probabilities $0.0005$, $0.0125$, and $0.0035$ in the recorded null regimes. The scientific reading is deliberately narrow: a finite codon-coordinate pattern is observed at the label-readback layer, and higher biological layers remain outside the tested claim.