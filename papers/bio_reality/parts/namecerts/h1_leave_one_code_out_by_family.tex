\origin{ai}
\subsection{Family-resolved leave-one-code-out readback for the local codon tile}
\label{sec:bioreality-h1-leave-one-code-out-by-family}

\paragraph{Claim scope.}
The claim studied here is a code-layer statement about the standard-code codon table in a six-coordinate codon display.  The carrier is the finite codon cube $Q_6$, with codons represented as six-bit positions and then read against curated amino-acid or stop labels.  The finite observation does not assert that the coordinate geometry is itself a biological law.  It says that, once the standard-code labels are supplied as a curated contact, the local tile detected in the six-bit display has a family-resolved recovery pattern under leave-one-code-out tests.  The tested layer is therefore code readback only: codon labels are compared with local tile membership, boundary membership, and amino-acid-family preserving null samples.

\paragraph{Finite row.}
The family-resolved run is identified by experiment row $7dda978c6c97$ and was verified at $2026$-$06$-$02T18{:}21{:}30+00{:}00$.  It evaluates five code families.  The observed above-baseline family hit rates are
$$
\begin{aligned}
\operatorname{rate}(\mathrm{alternative\_yeast}) &= 0.8333333333333334,\\
\operatorname{rate}(\mathrm{bacterial\_plastid}) &= 1.0,\\
\operatorname{rate}(\mathrm{candidate\_other}) &= 1.0,\\
\operatorname{rate}(\mathrm{mitochondrial}) &= 1.0,\\
\operatorname{rate}(\mathrm{nuclear\_ciliate}) &= 1.0.
\end{aligned}
$$
Thus four of the five families are saturated at rate $1.0$, while the alternative-yeast family is still high at $5/6$.  The parent leave-one-code-out aggregate row gives $23$ evaluated tables and $63$ total targets, with baseline hit rate $0.19821739130434785$, hit rate in $M-T$ equal to $0.9365079365079365$, and hit rate in $M-T$ plus boundary equal to $0.9841269841269841$.  The same number, $0.9841269841269841$, is recorded as the above-baseline leave-one-code-out hit rate.  The family row is therefore not an isolated statistic: it refines a global leave-one-code-out recovery profile whose finite table count and target count are explicitly recorded.

\paragraph{Codon tile and boundary rows.}
The recurrent set $R$ has cardinality $13$.  The finite row lists
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The punctured-cube row reports the same thirteen codons and the face-vector data
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
This is the local geometric object used by the family-level readback: it is a finite tile with thirteen codon vertices, sixteen one-dimensional incidences, and four two-dimensional faces in the recorded cell structure.  The leave-one-codon-out row records no anchor codons, five boundary codons, eight high-coverage closure codons, eight predictable codons, and recovery rate $1.0$.  These values explain why the family-level test is naturally reported with a boundary allowance: the finite tile has a nonempty boundary component, and the boundary component is not a mechanism claim.

\paragraph{Module partition.}
The codon modules recorded for the thirteen recurrent codons are complete.  The row assigns all $13$ recurrent codons and records no missing, extra, or unassigned codons.  The modules are
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The module-completeness statement is important because the family-resolved leave-one-code-out row can otherwise be inflated by unassigned residual codons.  Here the finite row closes that loophole: every recurrent codon is accounted for by one of the named modules, and every named module has predictable or boundary support.

\paragraph{Spectral and null rows.}
The spectral row gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The radial polynomial residual is $-6.224354365258478\times 10^{-12}$, recorded both as the cubic-polynomial value and as the radial-polynomial value.  The observed spectral radius is therefore within the stated numerical tolerance at $0.6752479308830213$.  Separately, the shape-significance row uses $N=2000$ null samples with seed $42$.  It records $57377$ distinct-sample attempts and $55377$ duplicate retries, with one exact shape match and twenty-five samples at Jaccard similarity at least $0.9$.  The corresponding probabilities are
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{J\geq 0.9} &= 0.0125.
\end{aligned}
$$
The amino-acid-class preserving null row also uses $N=2000$ and seed $42$.  It records observed $M$ size $20$, exact-match count $0$, lower-tail count $7$, exact-shape probability $0.0$, and lower-tail probability $0.0035$.  These rows support a finite empirical separation between the observed code-label pattern and the sampled label-preserving null ensembles, without treating the separation as an explanation of translation or function.

\paragraph{Reality contact.}
The only external contact used here is the curated standard-code table.  It supplies codon-to-label assignments: each codon is read as an amino-acid label or a stop label.  This contact tests code readback and the local split of labels across the codon tile.  It does not observe ribosomal realization, folded protein geometry, energetic admissibility, organismal fitness, or a universal generative principle.  Consequently the family rates are legitimate finite code-layer measurements, but they are not evidence that proteins fold in the tile shape, that the tile determines biological activity, or that the coordinate system is a necessary law of genetic coding.

\paragraph{Boundary.}
The locally supported statement is that the six-coordinate codon display, together with the curated standard-code labels, exposes a stable family-resolved leave-one-code-out pattern around the thirteen-codon recurrent set and its boundary.  The cannot-promote clauses are part of the statement.  No translation process, folded structure, physical admissibility condition, biological function, mechanism, or universality claim follows from this row unless a separate empirical contact is added for that higher layer.  In particular, amino-acid label readback remains label readback; it is not folded structure.  Cross-organism wording and tRNA-family notation likewise remain outside the present row unless matched to their own organism-specific evidence.  The finite result is therefore ready as a BioReality code-read chapter: high family hit rates, a complete thirteen-codon module partition, spectral and null-model support, and a locally enforced boundary against higher-layer biological conclusions.