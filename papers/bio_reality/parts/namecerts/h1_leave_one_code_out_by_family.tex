\origin{ai}

\subsection{Family-Stratified Leave-One-Code-Out Readback}
\label{sec:h1-leave-one-code-out-by-family}

\paragraph{Object and coordinate frame.}
This record concerns the standard-code codon table as a code-layer object on the six-coordinate codon cube $Q_6$. The finite display uses codon strings as the observable alphabet and treats the curated standard-code table as the contact that supplies amino-acid or stop labels. The carrier is therefore not a biochemical mechanism. It is a finite coordinate presentation in which local faces, singleton label corners, and three-point label classes can be tested against the observed codon-label table. The admissible readback is code-level label assignment only: a codon can be read as an amino-acid or stop label, and local tile splits can be compared with the curated table.

\paragraph{Boundary set.}
The distinguished boundary set $R$ has cardinality $13$. In the verified row it is exactly
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same set is reported as a punctured-cube pattern with face vector $(13,16,4)$. Thus the row is not merely recording thirteen exceptional labels; it records a finite local shape inside the six-bit codon cube. The module partition is complete on $R$: the $\mathrm{AGR}$ module is $\{\mathrm{AGA},\mathrm{AGG}\}$, the $\mathrm{AR\_Lys}$ module is $\{\mathrm{AAA}\}$, the $\mathrm{CUN}$ module is $\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\}$, the $\mathrm{Ile/Met}$ module is $\{\mathrm{AUA}\}$, the $\mathrm{SSN}$ module is $\{\mathrm{UCA},\mathrm{UUA}\}$, and the stop/tryptophan module is $\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}$. The classification reports $R$-size $13$, assigned count $13$, no extra module codons, no missing codons, and no unassigned codons.

\paragraph{Spectral and shape measurements.}
The finite-row spectrum gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$. The recorded radial cubic residual is $-6.224354365258478\times 10^{-12}$, so the numerical polynomial check is at roundoff scale for the stated row. The corresponding shape experiment used $N=2000$ samples with seed $42$. It found one exact shape match, giving $p_{\mathrm{exact}}=0.0005$, and $25$ samples with Jaccard score at least $0.9$, giving $p_{\mathrm{Jaccard}\ge 0.9}=0.0125$. The sampling also records $57377$ distinct-sample attempts and $55377$ duplicate retries. Under the amino-acid-class-preserving null model, again with $N=2000$ and seed $42$, the observed $M$-size is $20$, the exact match count is $0$, and the lower-tail count at or below the observed $M$-size is $7$. This gives $p=0.0$ for exact class-preserving shape recovery in the sampled run and $p=0.0035$ for the lower-tail event.

\paragraph{Leave-one-code-out row.}
The family-stratified leave-one-code-out measurement evaluates five families. The hit rate above baseline is $1.0$ for bacterial/plastid, candidate-other, mitochondrial, and nuclear-ciliate families, while the alternative-yeast family has hit rate $0.8333333333333334$. Thus four of the five reported families reach complete above-baseline recovery in this finite row, and the remaining family still exceeds five-sixths. The broader leave-one-code-out run evaluated $23$ tables and $63$ targets. Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$. The hit rate inside $M-T$ is $0.9365079365079365$, and the rate inside $M-T$ together with boundary support is again $0.9841269841269841$. These numbers support the limited conclusion that the window-six coordinate geometry is strongly aligned with local code-label recovery across the evaluated table families.

\paragraph{Leave-one-codon-out row.}
The codon-level deletion row reports anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$. The recovery-rate threshold flag is also $1.0$. In this chapter's scope, those figures say that every tested deletion case is recovered through predictable or boundary-supported behavior, not that the codon system has been explained mechanistically. The module row reinforces the same local interpretation: every module has either predictable or boundary codons, and the module assignment covers all thirteen $R$ codons without residue.

\paragraph{Scientific verdict.}
The finite evidence supports a code-read claim: window-six codon coordinates expose a stable local boundary-tile pattern in the standard-code table and in related leave-one-code-out families. The row is especially specific because it gives the exact $13$-codon boundary set, its punctured-cube face vector, a complete module assignment, spectral scalars, null-model $p$-values, and family-stratified recovery rates. The result should therefore be read as a finite codon-label regularity, not as a universal statement about biology.

\paragraph{Local scope limit.}
No translation process, folded protein structure, physical admissibility, biological function, global mechanism, or universality follows from these rows. The curated standard-code table supplies codon-to-label assignments; it does not observe ribosomal realization, molecular folding, thermodynamic admissibility, organismal function, or a universal biological law. Any claim crossing from codon-label readback into those layers requires a separate contact of the appropriate kind. In particular, amino-acid label sequences produced by the code table are not protein structures, and cross-organism codon-family wording does not itself become evidence about folding, function, or physical realization.