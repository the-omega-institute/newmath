\origin{ai}
\subsection{Leave-One-Code-Out Recovery by Code Family}
\label{sec:h-one-leave-one-code-out-by-family}

\paragraph{Finite codon display.}
This BioReality record concerns the finite window-six codon display $Q_6$, read only as a six-bit coordinate carrier for codon labels.  The biological contact is the curated standard-code label table: it assigns each codon an amino-acid or stop label, and it supplies a code-layer readback.  The contact does not supply translation dynamics, folded structure, physical admissibility, biological function, a mechanism, or universality.  The BEDC object under inspection is therefore a local boundary tile in the code-read layer, with coordinates, closure, spectrum, and relation data attached to a finite codon table.

\paragraph{The exceptional relation row.}
The relation row $R$ has cardinality $13$.  In codon notation it is
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same row is certified as a punctured-cube pattern with face vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the finite row is not merely a list of rare codons: it carries a four-face local tile with sixteen one-dimensional incidences and thirteen surviving vertices.  The row remains a code-coordinate fact; it is not promoted to a biological law.

\paragraph{Shape and null rows.}
Against the sampled null model for the $M$-shape, the run used $N=2000$ distinct sampled rows after $57377$ attempts and $55377$ duplicate retries, with seed $42$.  Exactly one sampled row matched the observed shape, giving $p_{\mathrm{exact}}=0.0005$.  Twenty-five sampled rows had Jaccard score at least $0.9$, giving $p_{J\ge 0.9}=0.0125$.  In the amino-acid-class-preserving null model, the observed $M$ size was $20$; among $N=2000$ samples, the exact-shape count was $0$ and the lower-tail count at or below the observed $M$ size was $7$.  Hence
$$
\begin{aligned}
p_{\mathrm{aa\ class, exact}} &= 0.0,\\
p_{\mathrm{aa\ class, lower\ tail}} &= 0.0035.
\end{aligned}
$$
These finite-row numbers support the claim that the code-read tile is structured relative to the supplied null families.  They do not imply that the tile is a physical constraint on molecular realization.

\paragraph{Spectral row.}
The finite spectrum also has a concrete readback.  The reported values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial polynomial residual and the cubic-polynomial residual are both $-6.224354365258478\times 10^{-12}$, so the numerical row is internally coherent at the reported tolerance.  The spectral radius is again a coordinate diagnostic of the finite code tile, not a claim about folding energy, viability, or selection.

\paragraph{Leave-one-code-out row.}
The leave-one-code-out probe evaluated $23$ code tables and $63$ total targets.  The baseline hit rate was $0.19821739130434785$.  The row hit rate above baseline was $0.9841269841269841$, the hit rate inside $M-T$ was $0.9365079365079365$, and the hit rate inside $M-T$ plus boundary was $0.9841269841269841$.  The family-level split contains five families:
$$
\begin{aligned}
\mathrm{alternative\ yeast} &\mapsto 0.8333333333333334,\\
\mathrm{bacterial\ plastid} &\mapsto 1.0,\\
\mathrm{candidate\ other} &\mapsto 1.0,\\
\mathrm{mitochondrial} &\mapsto 1.0,\\
\mathrm{nuclear\ ciliate} &\mapsto 1.0.
\end{aligned}
$$
This is the chapter-specific finite-row claim: when a code table is left out by family, the local tile recovers above the baseline rate in every reported family, with the alternative-yeast family still at $5/6$ and the other four families at full reported recovery.

\paragraph{Leave-one-codon-out module row.}
The codon-level companion row reports $0$ anchors, $5$ boundary codons, $8$ predictable codons, closure coverage high at $8$, and recovery rate $1.0$.  The complete module partition assigns all $13$ relation codons, with $R$ size $13$, assigned count $13$, no extra module codons, no missing codons, and no unassigned codons.  The modules are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
Every module has a predictable-or-boundary explanation in the finite row.  This module classification is a local accounting of the code table; it does not identify a translation apparatus or a protein-level consequence.

\paragraph{Reality-contact boundary.}
The only reality contact used here is the curated standard genetic-code bridge.  It can test code-read assignments and local codon-tile label splits.  It cannot test translation realization, structural order, physical admissibility, biological function, or universal mechanism.  Consequently, amino-acid label-sequence readback must remain at the code layer.  No folded protein structure, physical admissibility, biological function, global biological necessity, or universal mechanism follows from the window-six geometry unless a separate higher-layer contact supplies that information.

\paragraph{Claim.}
For the finite data recorded above, the leave-one-code-out by-family probe supports a local code-read statement: the window-six codon coordinates expose a stable boundary-tile pattern across five reported code-family rows, with $R$ realized as a $13$-codon punctured cube and with family hit rates from $0.8333333333333334$ to $1.0$ above the baseline row $0.19821739130434785$.  The result is a finite, chapter-local BioReality contact between BEDC coordinates and the curated codon-label table.  Its scope is exactly the code-read boundary tile.