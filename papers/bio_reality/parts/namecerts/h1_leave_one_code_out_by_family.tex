\origin{ai}
\subsection{Leave-One-Code-Out Recovery by Code Family}
\label{sec:h1-leave-one-code-out-by-family}

\paragraph{Finite carrier and readback contact.}
This record treats the standard-code codon table as a finite code-read contact on a six-bit codon display.  The carrier is the codon cube $Q_6$, with codons read as finite rows and labels supplied by the curated standard genetic-code table.  The table is used only as a codon-to-label reality bridge: it assigns codons to amino-acid or stop labels, but it does not by itself observe translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.  The local object extracted from the row data is the thirteen-codon boundary set
$$
\begin{aligned}
R=\{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}
$$
The finite audit reports $|R|=13$, and the punctured-cube face count for this row set is the vector $(13,16,4)$.  Thus the object being tested is not the full biological code as a law, but a local tile with thirteen vertices, sixteen edges, and four two-faces inside the six-coordinate display.

\paragraph{Spectral and null-model calibration.}
The same row object has spectral radius $\lambda_M=0.6752479308830213$ on the measured tile and $\lambda_R=0.4758352843197002$ on the boundary component, with auxiliary value $\mu=3.051487585298128$.  The reported cubic residual is $-6.224354365258478\times 10^{-12}$, the same value recorded as the radial polynomial residual, so the finite spectral identity is numerically stable at the stated precision.  Shape significance was tested against $N=2000$ samples with seed $42$.  The experiment required $57377$ distinct-sample attempts and encountered $55377$ duplicate retries.  It found $1$ exact shape match and $25$ samples with Jaccard score at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9}&=0.0125.
\end{aligned}
$$
An amino-acid-class-preserving null model with $N=2000$ and seed $42$ found no exact shape match, an observed measured-tile size of $20$, and $7$ samples in the lower tail at or below that observed size.  Its reported values are $p=0.0$ for exact-shape recovery and $p=0.0035$ for the lower-tail event.  These numbers support a nontrivial local code-read shape; they do not promote the shape to a mechanism.

\paragraph{Leave-one-code-out result.}
The leave-one-code-out experiment evaluated $23$ code tables and $63$ targets.  The baseline hit rate was $0.19821739130434785$.  The measured hit rate above baseline was $0.9841269841269841$, the hit rate in $M-T$ was $0.9365079365079365$, and the hit rate in $M-T$ plus boundary was again $0.9841269841269841$.  Grouped by code family, the run reports $5$ families.  Four families reached perfect above-baseline recovery, while the alternative-yeast family remained high but not perfect:
$$
\begin{aligned}
\mathrm{bacterial\_plastid}&:1.0,\\
\mathrm{candidate\_other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\_ciliate}&:1.0,\\
\mathrm{alternative\_yeast}&:0.8333333333333334.
\end{aligned}
$$
This is the chapter-specific finite-row claim: within the tested table family partition, the local window-six tile recovers nearly all leave-one-code-out targets above the baseline, with one named family showing a lower but still high rate.

\paragraph{Codon-module classification.}
The leave-one-codon-out module audit classifies all thirteen boundary codons without extras or omissions.  The assigned count is $13$, the boundary size is $13$, the missing-from-modules list is empty, the extra-module-codons list is empty, and the unassigned-codons list is empty.  The complete module partition is
$$
\begin{aligned}
\mathrm{agr}&=\{\texttt{AGA},\texttt{AGG}\},\\
\mathrm{ar\_lys}&=\{\texttt{AAA}\},\\
\mathrm{cun}&=\{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\},\\
\mathrm{ile\_met}&=\{\texttt{AUA}\},\\
\mathrm{ssn}&=\{\texttt{UCA},\texttt{UUA}\},\\
\mathrm{stop\_trp}&=\{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}.
\end{aligned}
$$
The companion leave-one-codon-out row reports boundary count $5$, anchor count $0$, predictable count $8$, high closure coverage $8$, and recovery rate $1.0$.  Every listed module has either predictable or boundary status in the finite audit.  This makes the row-level structure internally closed as a code-read partition, while preserving the distinction between a label-space recovery and any downstream biological realization.

\paragraph{Boundary of the claim.}
The supported statement is local: window-six codon coordinates expose a boundary-tile pattern whose recovery behavior is strong under the recorded leave-one-code-out and leave-one-codon-out probes.  The curated standard-code table supplies the amino-acid and stop labels needed for readback, and the finite experiments quantify the observed split.  Nothing in this contact tests whether DNA obeys the geometry as a biological necessity.  Nothing here proves translation realization, folded protein structure, structural order, physical admissibility, biological function, a universal mechanism, or a global law of genetic coding.  Crossing any of those boundaries requires a separate higher-layer reality contact.