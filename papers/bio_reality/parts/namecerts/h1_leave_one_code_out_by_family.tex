\subsection{Leave-One-Code-Out Stability by Genetic-Code Family}\label{subsec:bioreality-leave-one-code-out-by-family}
\origin{ai}

\paragraph{Finite claim.}
This BioReality record concerns the finite-row claim that the window-six codon display exposes a local boundary-tile signal in the standard-code codon table, while the amino-acid and stop labels remain external reality contacts supplied by a curated genetic-code table.  The carrier is the six-bit codon cube $Q_6$.  The distinctions used in the row are the six-bit codon display, local tile faces, singleton label corners, and three-point label classes.  The internal structure is coordinate, closure, spectrum, and relation data, and the readback is the standard-code label table.  The layer is therefore code readback only: the row tests codon-label splitting behavior, not translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.

\paragraph{Boundary set and punctured-cube row.}
The verified boundary set $R$ has cardinality $13$.  Its listed codons are
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same finite row verifies that this $R$ is the punctured-cube boundary row with $f$-vector $(13,16,4)$.  Thus the object is not an unrestricted collection of codons: it is a thirteen-vertex local boundary object inside the six-bit display, with sixteen one-dimensional incidences and four two-dimensional faces in the recorded finite complex.  This is the combinatorial support on which the code-read experiments are evaluated.

\paragraph{Spectral and shape controls.}
The spectral row reports $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The recorded cubic, equivalently the radial polynomial check, evaluates to $-6.224354365258478\cdot 10^{-12}$, so the numerical residual is at tolerance scale rather than a new biological assertion.  A separate finite-row shape test used $N=2000$ null samples with seed $42$.  It found one exact shape match, giving $p_{\mathrm{exact}}=0.0005$, and $25$ samples with Jaccard score at least $0.9$, giving $p_{J\geq 0.9}=0.0125$.  The same run records $57377$ distinct-sample attempts and $55377$ duplicate retries.  These numbers support a finite combinatorial distinction between the observed local tile and the sampled null population, but they do not identify a biochemical mechanism.

\paragraph{Leave-one-code-out family row.}
The primary row for this claim evaluates five code families.  The family count is $5$, and the hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\_yeast}&:0.8333333333333334,\\
\mathrm{bacterial\_plastid}&:1.0,\\
\mathrm{candidate\_other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\_ciliate}&:1.0.
\end{aligned}
$$
The non-family aggregate leave-one-code-out row evaluates $23$ tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$, while the observed hit rate above baseline is $0.9841269841269841$.  The hit rate inside $M\setminus T$ is $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus boundary is again $0.9841269841269841$.  Thus, at the code-read layer, the boundary-tile coordinate structure recovers most altered targets across the evaluated table rows and does so consistently across the five recorded families, with alternative yeast being the only listed family below perfect above-baseline hit rate.

\paragraph{Codon-level recovery and modules.}
The leave-one-codon-out row records recovery rate $1.0$ and recovery rate above threshold $1.0$.  It also records $8$ predictable codons, $5$ boundary codons, $0$ anchors, and high closure coverage $8$.  The module classification is complete: $R$ has size $13$, the assigned count is $13$, and the lists of extra module codons, missing module codons, and unassigned codons are all empty.  The six recorded modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every module has predictable or boundary support in the recorded row.  This turns the thirteen codons of $R$ into a fully assigned local code-read partition rather than a partially named cluster.

\paragraph{Reality contact and null boundary.}
The external contact is the curated standard-code table, whose observed fact is that codons are assigned to amino-acid or stop labels.  That contact has resolution only at codon label readback.  It can test code-read behavior and local codon-tile label splits; it cannot test translation realization, structural order, physical admissibility, or function realization.  The amino-acid class preserving null row gives observed $|M|=20$, exact match count $0$ in $N=2000$ samples, lower-tail count $7$, $p_{\mathrm{aa\ class\ exact}}=0.0$, and lower-tail $p=0.0035$.  These finite numbers sharpen the local code-read signal, but the contact does not contain a structure experiment, a folding assay, a translation mechanism, or a function measurement.

\paragraph{Verdict.}
The finite rows support the following bounded conclusion: in the six-bit codon cube, the standard-code label table exhibits a local boundary-tile pattern whose leave-one-code-out behavior remains above baseline across five genetic-code families, with aggregate hit rate $0.9841269841269841$ above a baseline of $0.19821739130434785$.  The same record gives a thirteen-codon punctured-cube boundary, complete module assignment, codon-level recovery rate $1.0$, significant shape controls, and a small spectral residual.  The result remains a code-read BioReality claim.  No translation, folding, physical admissibility, biological function, mechanism, or universality follows unless a separate higher-layer reality contact supplies it.