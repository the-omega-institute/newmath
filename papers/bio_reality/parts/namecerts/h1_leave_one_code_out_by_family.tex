\subsection{Leave-One-Code-Out Recovery by Code Family}\label{sec:bioreality-h1-leave-one-code-out-by-family}
\origin{ai}

\paragraph{Finite record.}
This record concerns the claim \texttt{h1.leave\_one\_code\_out.by\_family}. The object under test is the standard-code codon table, read only at the code-label layer. The carrier is the six-coordinate codon cube $Q_6$, together with a local tile face, singleton label corners, and three-point label classes. The experiment asks whether the window-six coordinate presentation retains a stable local boundary pattern when one genetic-code table is left out and the remaining code family is used to score recovery. The finite row reports five code families. Their above-baseline hit rates are
$$
\begin{aligned}
\text{alternative yeast} &= 0.8333333333333334,\\
\text{bacterial plastid} &= 1.0,\\
\text{candidate other} &= 1.0,\\
\text{mitochondrial} &= 1.0,\\
\text{nuclear ciliate} &= 1.0.
\end{aligned}
$$
Thus four of the five family strata have full above-baseline recovery in this finite run, while the alternative-yeast stratum still clears the baseline comparison with rate $5/6$. The experiment identifier for this row is \texttt{7dda978c6c97}, with verification time \texttt{2026-06-04T02:17:42+00:00}.

\paragraph{Codon boundary set.}
The shared boundary set $R$ has cardinality $13$. The row identifies it explicitly as
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same set is reported as a punctured-cube pattern with face-vector $(13,16,4)$. This is the finite combinatorial substrate used by the family-level leave-one-code-out row: it is not an inferred organismal mechanism, but a codon-coordinate boundary set whose labels are supplied by the curated code table.

\paragraph{Spectral and shape checks.}
The finite spectral row gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$. The reported cubic residual is $-6.224354365258478\times 10^{-12}$, matching the radial-polynomial residual to numerical tolerance. The shape-significance run used $N=2000$ random samples with seed $42$. It observed one exact shape match, $25$ samples with Jaccard similarity at least $0.9$, $55377$ duplicate retries across $57377$ distinct-sample attempts, and the finite p-values
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9} &= 0.0125.
\end{aligned}
$$
A second amino-acid-class-preserving null run, also with $N=2000$ and seed $42$, recorded observed $M$-size $20$, exact-match count $0$, lower-tail count $7$, exact-shape p-value $0.0$, and lower-tail p-value $0.0035$. These numbers support the finite claim that the observed codon-tile geometry is not typical under the stated randomization schemes. They do not identify a biochemical cause.

\paragraph{Leave-one-code and leave-one-codon behavior.}
The broader leave-one-code-out table evaluated $23$ tables and $63$ targets. Its baseline hit rate was $0.19821739130434785$, while the hit rate above baseline was $0.9841269841269841$. The recovery rate inside $M\setminus T$ was $0.9365079365079365$, and the recovery rate inside $M\setminus T$ plus boundary was $0.9841269841269841$. The leave-one-codon-out row gives recovery rate $1.0$, predictable count $8$, boundary count $5$, anchor count $0$, and high closure coverage $8$. Together with the by-family result, these finite rows say that the local coordinate boundary remains highly predictive across the recorded leave-out regimes. They do not say that codon geometry performs translation, folds protein, selects a physical realization, or determines biological function.

\paragraph{Module classification.}
The module classification accounts for all $13$ codons in $R$: assigned count $13$, $R$-size $13$, no missing codons, no extra module codons, and no unassigned codons. The finite modules are
$$
\begin{aligned}
\mathrm{AGR} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{AR\_LYS} &= \{\mathrm{AAA}\},\\
\mathrm{CUN} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ILE\_MET} &= \{\mathrm{AUA}\},\\
\mathrm{SSN} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{STOP\_TRP} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Each module has either a predictable or boundary role in the recorded classification. This supplies a finite internal partition of the codon boundary set, while the amino-acid and stop labels remain imported from the curated standard-code table.

\paragraph{Reality contact and boundary of interpretation.}
The sole contact used here is the curated standard genetic-code table, understood as a codon-to-label source. It can test code readback and local codon-tile label splits. It cannot test translation realization, folded structure, structural order, physical admissibility, biological function, a universal biological law, or a general mechanism. Therefore the ready verdict is limited: window-six codon coordinates expose a strong local boundary-tile regularity in the supplied finite rows, and the family-wise leave-one-code-out result is aligned with that regularity. Any move from this code-layer statement to translation, folding, function, physical feasibility, or universality would require a separate contact whose observations live at that higher layer.