\subsection{Leave-One-Code-Out Behavior by Code Family}\label{sec:h1-leave-one-code-out-by-family}
\origin{ai}

\paragraph{Finite object under test.}
The finite carrier for this record is the six-bit codon cube $Q_6$, read through the standard-code label table as an external reality contact.  The BEDC-side distinctions are a six-bit codon display, a local tile face, singleton label corners, and three-point label classes.  The biological readback is only the codon-to-label table: each codon receives an amino-acid or stop label, and no higher biological realization is included in the contact.  In this carrier the distinguished residual row $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row is seen as a punctured-cube tile with face-vector $(13,16,4)$.  Thus the local object is not an arbitrary codon list: it has a finite boundary shape, a codon-coordinate interpretation, and a label-table readback, all confined to the code layer.

\paragraph{Spectral and shape checks.}
The finite spectral readout separates the row from a null coordinate background.  The recorded values are
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128,
\end{aligned}
$$
and the cubic radial check gives residual $-6.224354365258478\times 10^{-12}$, within the stated tolerance.  The sampled shape test used $N=2000$ null draws with seed $42$.  It found one exact shape match, giving $p_{\mathrm{exact}}=0.0005$, and $25$ matches at Jaccard threshold $0.9$, giving $p_{J\geq 0.9}=0.0125$.  The sampling process required $57377$ distinct-sample attempts with $55377$ duplicate retries.  These numbers support a finite local-shape distinction; they do not by themselves identify a biological mechanism.

\paragraph{Leave-one-code-out family result.}
The family-level leave-one-code-out row evaluates five code families.  The above-baseline hit rates by family are
$$
\begin{aligned}
\mathrm{alternative\_yeast}&=0.8333333333333334,\\
\mathrm{bacterial\_plastid}&=1.0,\\
\mathrm{candidate\_other}&=1.0,\\
\mathrm{mitochondrial}&=1.0,\\
\mathrm{nuclear\_ciliate}&=1.0.
\end{aligned}
$$
Thus four of the five reported families are fully above the baseline criterion in this finite-row test, while the alternative-yeast family remains above baseline but not perfect.  The companion leave-one-code-out aggregate evaluated $23$ tables and $63$ total targets.  Its baseline hit rate was $0.19821739130434785$, while the above-baseline hit rate was $0.9841269841269841$.  The hit rate inside $M\setminus T$ was $0.9365079365079365$, and the rate inside $M\setminus T$ plus the boundary was $0.9841269841269841$.  These values say that, once the finite tile and boundary are fixed, most left-out code-label targets fall back into the coordinate-supported region rather than behaving like baseline draws.

\paragraph{Codon-module coverage.}
The codon-level leave-one-out check records no anchors, five boundary codons, eight high-coverage closure codons, eight predictable codons, and recovery rate $1.0$.  The module classification accounts for all $13$ codons of $R$, with assigned count $13$, no missing module codons, no extra module codons, and no unassigned codons.  The six finite modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every module has a predictable-or-boundary status.  This is the row-level reason that the family result is meaningful: the family hits are not merely scattered codon coincidences, but are supported by a complete partition of the distinguished residual row into named finite modules.

\paragraph{Amino-acid-class preserving null.}
The amino-acid-class preserving null model also used $N=2000$ and seed $42$.  It found zero exact shape matches, so the reported exact-shape count is $0$ and the empirical exact-shape value is $0.0$.  The lower-tail count at the observed $M$ size was $7$, with observed $M$ size $20$, giving lower-tail value $0.0035$.  This null is important because it preserves an external label-class constraint while still failing to reproduce the observed local tile at ordinary frequency.  The row therefore records a code-layer coordinate signal against a label-preserving comparison, not merely against an unconstrained shuffle.

\paragraph{Reality contact and boundary.}
The only reality contact used here is the curated standard genetic-code bridge.  It supports code readback and local codon-tile label-split checks.  It does not observe translation realization, folded structure, structural order, physical admissibility, biological function, a universal mechanism, or any necessity claim saying that DNA must obey this BEDC geometry.  The proper conclusion is therefore local and finite: window-six codon coordinates expose a boundary-tile pattern whose family-level leave-one-code-out behavior is strong against the recorded baselines.  The contact blocks promotion beyond the code layer.  Amino-acid label-sequence readback is not folded protein structure, and a local codon tile is not a global biological law unless a separate higher-layer contact supplies the missing evidence.