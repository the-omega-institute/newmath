\origin{ai}
\subsection{Leave-One-Code-Out Family Stability}
\label{subsec:h1-leave-one-code-out-by-family}

\paragraph{Finite carrier and readback contact.}
This BioReality record works over the six-bit codon cube $Q_6$, read only through the curated standard-code label table.  The carrier is the finite codon display, the local geometric object is a tile face with boundary behavior, and the reality contact is the assignment of codons to amino-acid or stop labels.  The contact tests code-layer readback and local label splitting only.  It does not test translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.

\paragraph{Boundary row.}
The distinguished boundary row $R$ has cardinality $13$.  Its codons are
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same row is verified as a punctured-cube pattern with face data
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the finite object used by the family test is not an unrestricted list of codon exceptions.  It is a thirteen-codon boundary set with a four-face local tile profile inside the six-bit cube.  The row supplies a coordinate-level target for code readback, while the curated label table supplies the external labels.

\paragraph{Spectral finite signature.}
The associated finite operator signature has numerical values
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial cubic residual is $-6.224354365258478\times 10^{-12}$, so the recorded spectral value satisfies the cubic check to numerical tolerance.  This spectrum is used only as a finite-row signature of the codon-coordinate construction.  It is not a physical energy, a biochemical rate, or a claim about molecular dynamics.

\paragraph{Shape significance controls.}
Against the finite null run with $N=2000$ and seed $42$, the exact shape appeared once, giving
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{J\geq 0.9} &= 0.0125.
\end{aligned}
$$
The run recorded $57377$ distinct-sample attempts and $55377$ duplicate retries.  Under the amino-acid-class-preserving null, the observed $M$ size was $20$, the exact-shape count was $0$, and the lower-tail count at or below the observed size was $7$ out of $2000$, giving
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
These controls support a finite geometric enrichment statement: the observed code-layer tile is rare under the recorded randomizations.  They do not turn the tile into a universal biological law.

\paragraph{Leave-one-code-out row.}
The leave-one-code-out experiment evaluated $23$ code tables and $63$ total targets.  The baseline hit rate was $0.19821739130434785$.  The observed hit rate inside $M-T$ was $0.9365079365079365$, and the observed hit rate inside $M-T$ plus boundary was $0.9841269841269841$.  The recorded hit rate above baseline was also $0.9841269841269841$.  In finite terms, almost all held-out code targets land in the predicted local tile or its boundary extension, while the baseline rate is about one fifth.  The correct reading is therefore code-layer stability of the local boundary construction across held-out code tables.

\paragraph{Family stratification.}
The family-level row groups the leave-one-code-out result into $5$ code families.  The verified family hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\_yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\_plastid} &= 1.0,\\
\mathrm{candidate\_other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\_ciliate} &= 1.0.
\end{aligned}
$$
Four families have complete above-baseline recovery in this finite row, and the alternative-yeast family retains five-sixths recovery.  This is the chapter-specific claim: the leave-one-code-out signal is not confined to a single family label, but persists across the recorded family partition of the codon-label tables.

\paragraph{Codon-module closure.}
The leave-one-codon-out module classification partitions all $13$ boundary codons with no missing, extra, or unassigned codons.  The verified modules are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA,AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA,UUA\},\\
\mathrm{stop\_trp} &= \{UAA,UAG,UGA\}.
\end{aligned}
$$
The classification has $R$ size $13$, assigned count $13$, no extra module codons, no codons missing from modules, and no unassigned codons.  The associated leave-one-codon-out row records $8$ predictable codons, $5$ boundary codons, $0$ anchors, closure coverage $8$, and recovery rate $1.0$.  Hence every module is accounted for by predictable or boundary behavior inside the finite code-read layer.

\paragraph{Local verdict and cannot-claim boundary.}
The finite data support the statement that window-six codon coordinates expose a local boundary-tile structure whose code-label readback is stable under the recorded leave-one-code-out family stratification.  The evidence is a conjunction of the thirteen-codon punctured-cube boundary, the spectral finite signature, the rare-shape null controls, the $23$-table leave-one-code-out recovery, and the $5$-family hit-rate row.  The result remains strictly local.  No translation process, folded protein structure, physical admissibility, biological function, mechanism, or universality follows from this chapter unless a separate reality contact supplies that higher-layer evidence.