\chapter{Leave-One-Code-Out Recovery by Family}\label{ch:h1-leave-one-code-out-by-family}
\origin{ai}

\subsection{Finite-row claim and contact boundary}\label{sec:h1-leave-one-code-out-by-family-claim}

\paragraph{Claim.}
This record concerns the finite-row claim $\mathrm{h1.leave\_one\_code\_out.by\_family}$ for the standard-code codon table viewed through a six-bit codon display.  The carrier is the codon cube $Q_6$, the visible distinctions are a local tile face, a singleton label corner, and a three-point label class, and the readback contact is the curated standard-code label table.  The biological object is therefore not translation itself, but the code-layer assignment of codons to amino-acid or stop labels.  The operative question is whether the window-six coordinate and closure data recover held-out code-label changes above a baseline after the rows are grouped into biological code families.

\paragraph{Reality contact.}
The only external contact used here is the curated standard genetic-code bridge.  It says that a standard-code table assigns each codon to an amino-acid or stop label.  This contact can test code readback and local codon-tile label splits, but it cannot test translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.  Accordingly, every numerical conclusion below is a code-layer conclusion.  No claim about protein realization, folded protein structure, admissible chemistry, function, universal genetic-law status, or biological necessity follows from these rows unless a separate higher-layer contact supplies that information.

\subsection{Boundary set and cube shape}\label{sec:h1-leave-one-code-out-by-family-boundary-set}

\paragraph{The recovered boundary row.}
The finite boundary set $R$ has cardinality $13$.  The listed codons are
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The punctured-cube check gives the same set and records the face-vector summary $(13,16,4)$.  Thus the row being used by the family experiment is not an unconstrained list of exceptional codons; it is a specific thirteen-codon punctured-cube boundary in the six-bit display.

\paragraph{Spectral scale.}
The associated spectral row records
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The cubic, or radial, polynomial residual is $-6.224354365258478\cdot 10^{-12}$, so the numerical row is internally stable at the reported tolerance.  The value $\lambda_M=0.6752479308830213$ is the reported spectral radius in tolerance.  These numbers are used only as finite-row invariants of the coordinate construction; they are not interpreted as physical rates or molecular energies.

\subsection{Null rows and significance checks}\label{sec:h1-leave-one-code-out-by-family-null-rows}

\paragraph{Shape-significance row.}
The reported $M$ shape row has observed size $20$.  Under the sampled null with seed $42$, the experiment used $N=2000$ accepted samples, with $57377$ distinct-sample attempts and $55377$ duplicate retries.  It found $1$ exact shape match and $25$ matches with Jaccard similarity at least $0.9$.  Hence
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9} &= 0.0125.
\end{aligned}
$$
The row therefore treats the observed finite shape as rare under this null model, while still keeping the interpretation at the code-read layer.

\paragraph{Amino-acid-class-preserving null.}
A second null preserves amino-acid class.  It again used $N=2000$ and seed $42$.  The exact match count was $0$, the observed $M$ size was $20$, and the lower-tail count at or below the observed $M$ size was $7$.  The recorded values are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact\ shape}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
This is a stronger guard against the trivial objection that the pattern is only an amino-acid-class artifact.  It still does not promote the tile to a biological mechanism.

\subsection{Leave-one-code-out family recovery}\label{sec:h1-leave-one-code-out-by-family-recovery}

\paragraph{Global held-out-code row.}
Before grouping by family, the leave-one-code-out experiment evaluated $23$ tables and $63$ total targets.  The baseline hit rate was $0.19821739130434785$.  The hit rate above baseline was $0.9841269841269841$, the hit rate inside $M\setminus T$ was $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus boundary was $0.9841269841269841$.  Thus the finite row says that held-out code-label targets are recovered very often by the local window-six region plus its boundary, compared with the stated baseline.

\paragraph{Family-stratified row.}
The family experiment partitions the held-out-code targets into $5$ families.  The reported family hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast} &:\ 0.8333333333333334,\\
\mathrm{bacterial\ plastid} &:\ 1.0,\\
\mathrm{candidate\ other} &:\ 1.0,\\
\mathrm{mitochondrial} &:\ 1.0,\\
\mathrm{nuclear\ ciliate} &:\ 1.0.
\end{aligned}
$$
The weakest family row is therefore alternative yeast at $0.8333333333333334$, while the other four reported families are perfect at the finite-row resolution.  The family split is important because it checks that the high aggregate hit rate is not concentrated in a single family bucket.

\subsection{Leave-one-codon and module accounting}\label{sec:h1-leave-one-code-out-by-family-modules}

\paragraph{Codon-level recovery row.}
The leave-one-codon-out row records anchor count $0$, boundary count $5$, predictable count $8$, high closure coverage $8$, and recovery rate $1.0$.  The row also records recovery rate above threshold as $1.0$.  This says that the thirteen-codon boundary is fully accounted for by the finite predictable-or-boundary classification, rather than by hidden anchor codons.

\paragraph{Module classification.}
The module classification is complete: $R$ has size $13$, the assigned count is $13$, and there are no missing, extra, or unassigned codons.  The six modules are
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every listed module has predictable-or-boundary support.  This module accounting is the local finite-row reason that the family result is meaningful: the family-level held-out hits rest on a completely assigned thirteen-codon boundary, not on a partially classified residue set.

\subsection{Interpretation and cannot-claim boundary}\label{sec:h1-leave-one-code-out-by-family-boundary}

\paragraph{Supported interpretation.}
The supported statement is narrow.  In the window-six codon coordinate system, the standard-code label table exposes a local boundary-tile pattern.  The recovered set $R$ has $13$ codons, punctured-cube shape $(13,16,4)$, stable spectral summaries, significant shape rows under two nulls, strong leave-one-code-out performance over $23$ tables and $63$ targets, and family-stratified above-baseline hit rates between $0.8333333333333334$ and $1.0$.  The local codon modules are completely assigned.

\paragraph{Blocked promotions.}
The same data explicitly block stronger promotions.  A local codon tile is not a global biological law.  Window-six geometry alone is not translation, structure, physical admissibility, function, mechanism, or universality.  Amino-acid label-sequence readback is not folded protein structure.  Any future claim crossing from code readback into translation realization, structural order, physical admissibility, function, or mechanism must introduce a separate contact that observes that higher layer directly.  Without such a contact, this chapter remains a finite code-layer boundary record.