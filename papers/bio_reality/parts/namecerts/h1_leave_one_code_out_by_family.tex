\origin{ai}
\subsection{Leave-one-code-out stability by code family}
\label{sec:h1-leave-one-code-out-by-family}

\paragraph{Finite row and intended layer.}
This record studies a window-six codon coordinate on the codon cube $Q_6$, with codons read as six-bit displays and with local tile faces, singleton label corners, and three-point label classes treated as the finite distinctions under inspection.  The biological contact is only the curated standard-code codon table: it assigns each codon to an amino-acid label or to a stop label.  The claim therefore lives at the code-read layer.  It asks whether the finite window-six geometry gives stable local boundary information when one code table, or one codon position, is withheld from the comparison.  The row is not a claim about translation in cells, folded protein structure, thermodynamic admissibility, biological function, a universal mechanism, or a law governing all organisms.  Those higher layers require separate contacts with their own observations.

\paragraph{The punctured boundary set.}
The finite boundary set $R$ has cardinality $13$.  In codon notation it is
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row verifies that this set is the punctured-cube boundary pattern used by the test, with face-vector data
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the object under discussion is a concrete finite substructure of the codon cube, not an open-ended biological regularity.  The codons in $R$ are exactly the codons on which the leave-out and module checks concentrate.

\paragraph{Spectral and shape controls.}
The spectral calculation reports three finite scalar values:
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic check is numerically closed at $-6.224354365258478\times 10^{-12}$, which is within the stated tolerance for the computation.  The shape comparison against $2000$ sampled alternatives found one exact shape match, giving $p=0.0005$, and found $25$ samples with Jaccard similarity at least $0.9$, giving $p=0.0125$.  The sampling record also reports $57377$ distinct-sample attempts and $55377$ duplicate retries at seed $42$.  Under the amino-acid-class-preserving null, the observed $M$-size is $20$, with $0$ exact matches among $2000$ samples and $7$ samples in the lower tail at or below the observed $M$-size.  This gives an exact-shape value $0.0$ in the sampled row and a lower-tail value $0.0035$.  These numbers support a finite code-layer separation; they do not identify a cellular mechanism.

\paragraph{Leave-one-code-out result.}
The global leave-one-code-out check evaluated $23$ tables and $63$ targets.  Its baseline hit rate is
$$
\begin{aligned}
0.19821739130434785.
\end{aligned}
$$
Against this baseline, the observed hit rate above baseline is
$$
\begin{aligned}
0.9841269841269841.
\end{aligned}
$$
The hit rate inside $M\setminus T$ is $0.9365079365079365$, and the rate inside $M\setminus T$ together with the boundary component is again $0.9841269841269841$.  The by-family refinement keeps the same code-read interpretation and separates the result into five family rows:
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
The family count is $5$.  Four families are saturated at $1.0$ in this finite test, while the alternative-yeast family remains high but not saturated.  The conclusion is therefore family-stable at the table-label level: withholding a code family still leaves the local window-six boundary signal largely recoverable relative to the baseline.

\paragraph{Leave-one-codon-out and module accounting.}
The codon-level leave-out row gives $0$ anchors, $5$ boundary codons, $8$ codons with high closure coverage, $8$ predictable codons, and recovery rate $1.0$.  The module classification assigns every codon in $R$ to one of six named modules and leaves no unassigned codons:
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The completeness row records $R$-size $13$, assigned count $13$, no extra module codons, no missing codons from the module inventory, and no unassigned codons.  This matters because the family result is not being read from a vague cluster: the tested boundary set is exhausted by named finite modules, and every module has either predictable or boundary status in the supplied row.

\paragraph{Interpretation and boundary.}
The finite evidence says that a window-six codon coordinate exposes a robust local tile boundary for the standard-code label table and for the leave-one-code-out by-family split.  The reality contact supplies codon-to-label assignments; it does not observe ribosomal translation, tRNA dynamics, folded structure, stability, activity, organism-level adaptation, or universality.  Consequently, the strongest local conclusion is this: within the supplied finite code-label table, the $13$-codon punctured boundary and its six-module classification retain high recovery under leave-one-code-out tests, with family hit rates ranging from $0.8333333333333334$ to $1.0$ above the baseline comparison.  Any statement beyond code-label readback must be held back until a distinct higher-layer observation is attached.