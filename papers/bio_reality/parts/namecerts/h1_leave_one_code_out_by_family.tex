\subsection{Leave-One-Code-Out Stability by Code Family}\label{sec:h1-leave-one-code-out-by-family}
\origin{ai}

\paragraph{Finite carrier and code-layer scope.}
This record treats the standard-code codon table as a finite code-read contact on the six-bit codon cube $Q_6$.  The carrier is not a biological mechanism by itself.  It is a coordinate display in which each codon is read as a vertex, local tile faces are tested as finite boundary data, singleton label corners and three-point label classes are separated, and the readback is the curated standard-code label table.  The contact used here is only the curated assignment of codons to amino-acid or stop labels.  It can test code-layer readback and local codon-tile label split; it cannot test translation realization, folded structure, physical admissibility, biological function, a universal mechanism, or any necessity claim about how DNA must behave.

\paragraph{Observed finite boundary row.}
The finite exceptional row $R$ has cardinality $13$ and is exactly the displayed codon set
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The punctured-cube test reports the same row and the face-vector
$$
\begin{aligned}
f(R) &= (13,16,4).
\end{aligned}
$$
Thus the finite evidence is not a vague geometric metaphor: the row contains $13$ named codons, $16$ one-dimensional incidences, and $4$ two-dimensional faces in the local tile complex tested by the pipeline.  The module assignment is complete on this row: the $13$ codons are all assigned, with no missing codons, no extra module codons, and no unassigned codons.  The six modules are
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every listed module has a predictable or boundary explanation inside the code-read experiment, but this is still a statement about finite codon-label rows, not about protein realization.

\paragraph{Spectral and shape controls.}
The associated spectral row separates $M$ and $R$ numerically.  The recorded values are
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The radial cubic residual is small at the recorded precision:
$$
\begin{aligned}
\mathrm{residual} &= -6.224354365258478\cdot 10^{-12}.
\end{aligned}
$$
The null shape test used $N=2000$ distinct sampled controls after $57377$ sample attempts and $55377$ duplicate retries.  It found $1$ exact shape match and $25$ matches at Jaccard threshold at least $0.9$, giving
$$
\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{J\geq 0.9} &= 0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null model used $N=2000$, observed $|M|=20$, found $0$ exact matches, and found $7$ controls in the lower tail at or below the observed $M$ size.  Its recorded probabilities are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
These controls support that the observed finite tile is not a typical sampled code-layer row under the tested nulls.  They do not convert the row into a law of molecular biology.

\paragraph{Leave-one-code-out aggregate behavior.}
The leave-one-code-out test evaluated $23$ tables and $63$ total targets.  The baseline hit rate was
$$
\begin{aligned}
0.19821739130434785,
\end{aligned}
$$
while the observed hit rates were
$$
\begin{aligned}
\mathrm{hit\ rate\ above\ baseline} &= 0.9841269841269841,\\
\mathrm{hit\ rate\ in}\ M\setminus T &= 0.9365079365079365,\\
\mathrm{hit\ rate\ in}\ (M\setminus T)\cup \partial &= 0.9841269841269841.
\end{aligned}
$$
The leave-one-codon-out row is compatible with the same local-boundary reading: it reports $0$ anchors, $5$ boundary codons, $8$ high-closure-coverage codons, $8$ predictable codons, and recovery rate $1.0$.  The recovery rate above threshold is also $1.0$.  In code-layer terms, removed codon-label information is usually recovered by the finite local geometry and its boundary extension; in biological terms, this remains a label-table recovery statement only.

\paragraph{Family-resolved leave-one-code-out row.}
The family-resolved experiment is the specific claim recorded here.  It has experiment id $\mathrm{7dda978c6c97}$, family count $5$, and the following hit rates above baseline:
$$
\begin{aligned}
\mathrm{alternative\_yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\_plastid} &= 1.0,\\
\mathrm{candidate\_other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\_ciliate} &= 1.0.
\end{aligned}
$$
Thus four of the five named families are perfect at this resolution, and the remaining alternative-yeast family still exceeds the baseline substantially.  The result is a family-level robustness statement about finite code-read targets: under the tested leave-one-code-out perturbations, the local six-bit codon tile keeps recovering the held-out label behavior far above the baseline row.

\paragraph{Boundary verdict.}
The finite-row verdict is ready at the code-read layer.  The evidence supports a local codon-tile boundary structure for the curated standard-code label table, with $|R|=13$, complete module assignment on $R$, nontrivial spectral separation, significant shape controls, strong leave-one-code-out recovery across $23$ tables and $63$ targets, and family-level rates $0.8333333333333334$ or $1.0$ across the five named families.  The same evidence explicitly blocks promotion beyond its contact.  No translation realization, folded structure, structural order, physical admissibility, biological function, global mechanism, or universality follows from this chapter unless a separate higher-layer reality contact supplies that information.