\subsection{Leave-One-Code-Out Recovery by Family}\label{sec:bioreality-h1-leave-one-code-out-by-family}
\origin{ai}

\paragraph{Record.}
This BioReality record concerns the finite-row claim \texttt{h1.leave\_one\_code\_out.by\_family}.  The object under readback is the curated standard-code codon table, treated here as an external code-label contact.  The BEDC-minimal form is the six-bit codon cube $Q_6$ equipped with four displayed distinctions: the six-bit codon display, a local tile face, singleton label corners, and three-point label classes.  Its internal structure is coordinate, closure, spectrum, and relation data.  The biological readback is only the standard-code label table: codons are assigned amino-acid or stop labels, and no translation process or protein realization is supplied by this contact.

\paragraph{Finite carrier row.}
The distinguished relation row $R$ has cardinality $13$.  The row is
$$\begin{aligned}
R=\{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}$$
The punctured-cube check gives the same $13$ codons and the face-vector
$$\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}$$
Thus the finite object is not introduced as a smooth global principle; it is a concrete codon subset with thirteen vertices, sixteen row edges, and four local two-faces in the chosen codon-cube display.

\paragraph{Spectral and shape rows.}
The associated spectral row reports
$$\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}$$
The cubic radial residual is $-6.224354365258478\times 10^{-12}$, recorded both as the cubic-polynomial value and the radial-polynomial value, so the numerical row is effectively at tolerance for the displayed polynomial relation.  The shape significance run used $N=2000$ finite draws with seed $42$.  It made $57377$ distinct-sample attempts and $55377$ duplicate retries, found $1$ exact shape match, and found $25$ matches with Jaccard score at least $0.9$.  The corresponding empirical values are
$$\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\ge 0.9}&=0.0125.
\end{aligned}$$
Under the amino-acid-class-preserving null row, again with $N=2000$ and seed $42$, the observed $M$ size is $20$, exact shape matches are $0$, and the lower-tail count at or below the observed $M$ size is $7$.  The displayed values are $p_{\mathrm{aa\ class\ exact}}=0.0$ and $p_{\mathrm{aa\ class\ lower\ tail}}=0.0035$.  These rows support a finite local shape signal in the code-label contact, not a claim about biochemical necessity.

\paragraph{Leave-one-code-out row.}
The leave-one-code-out experiment evaluated $23$ code tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$.  The hit rate in $M-T$ is $0.9365079365079365$, and the hit rate in $M-T$ plus boundary is $0.9841269841269841$.  The reported hit rate above baseline is also $0.9841269841269841$.  By family, the finite row contains $5$ families:
$$\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}$$
Thus the family-level row says that four of the five displayed families reach perfect above-baseline recovery in this finite probe, while the alternative-yeast family remains positive but not perfect at $5/6=0.8333333333333334$.

\paragraph{Leave-one-codon-out and module row.}
The single-codon deletion row has $0$ anchors, $5$ boundary cases, $8$ predictable cases, $8$ high-closure-coverage cases, and recovery rate $1.0$.  The module classification is complete over the same $13$ codons: $R$ has size $13$, the assigned count is $13$, and there are no extra module codons, no codons missing from modules, and no unassigned codons.  The six displayed modules are
$$\begin{aligned}
\mathrm{agr}&=\{\texttt{AGA},\texttt{AGG}\},\\
\mathrm{ar\_lys}&=\{\texttt{AAA}\},\\
\mathrm{cun}&=\{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\},\\
\mathrm{ile\_met}&=\{\texttt{AUA}\},\\
\mathrm{ssn}&=\{\texttt{UCA},\texttt{UUA}\},\\
\mathrm{stop\_trp}&=\{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}.
\end{aligned}$$
Every module has either predictable support or boundary support in the finite classification row.  This is the local sense in which the codon-window geometry exposes a boundary-tile pattern: the row partitions the thirteen distinguished codons into named finite modules, and those modules are compatible with the leave-one-codon and leave-one-code finite probes.

\paragraph{Reality contact and cannot-claim boundary.}
The only reality contact used here is the curated standard-code table.  It tests code readback and local codon-tile label splits.  It does not test translation realization, folded protein structure, structural order, physical admissibility, biological function, a universal mechanism, or biological necessity.  A local codon tile is therefore not promoted to a global biological law.  Window-six geometry alone is not translation, structure, physical admissibility, or function, and amino-acid label-sequence readback is not folded protein structure.  Any later claim crossing from code labels into translation, folding, physical admissibility, function, mechanism, or universality must introduce a separate higher-layer contact rather than reusing this code-table row.