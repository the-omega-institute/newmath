\origin{ai}

\subsection{Family-stratified leave-one-code-out boundary record}
\label{sec:bioreality-h1-leave-one-code-out-by-family}

\paragraph{Finite object.}
This record concerns the standard-code codon table as a finite code-read object on the six-bit codon cube $Q_6$.  The carrier is the set of codon displays, and the only biological contact used here is a curated codon-to-label table assigning each codon to an amino-acid label or to a stop label.  The BEDC-side distinctions are the six-bit codon display, the local tile face, a singleton label corner, and a three-point label class.  The internal structure read in this chapter is coordinate, closure, spectrum, and relation; the readback is only the standard-code label table.

\paragraph{Observed residual row.}
The finite residual set $R$ has cardinality $13$.  Its codons are
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row is recorded as a punctured-cube pattern with face-vector $(13,16,4)$.  Thus the finite observation is not merely a count: the row carries a local combinatorial shape inside $Q_6$, with thirteen vertices, sixteen local one-dimensional incidences, and four local two-dimensional faces.  The observed module partition is complete: the $13$ residual codons are assigned with no extras, no missing codons, and no unassigned codons.  The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},&
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},&
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},&
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},&
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every listed module has either predictable or boundary status under the leave-one-codon-out classification.

\paragraph{Spectral finite check.}
The spectral row gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The reported spectral radius in tolerance is again $0.6752479308830213$.  The radial cubic check evaluates to $-6.224354365258478\times 10^{-12}$, and the same value is recorded for the cubic-polynomial residual.  In finite-row terms, the polynomial error is at numerical zero for the stated computation, so the spectral data are consistent with a stable local shape rather than a free verbal reading of the codon labels.

\paragraph{Shape significance rows.}
Against the shape null sample with $N=2000$, seed $42$, and $57377$ distinct-sample attempts, the observed shape has $1$ exact match and $25$ matches with Jaccard score at least $0.9$.  The corresponding values are $p_{\mathrm{exact}}=0.0005$ and $p_{\mathrm{Jaccard}\ge 0.9}=0.0125$.  The same run records $55377$ duplicate retries, which is relevant because the finite comparison is being made against distinct sampled alternatives.  In the amino-acid-class-preserving null model with $N=2000$ and seed $42$, the observed $M$ size is $20$, the exact-shape match count is $0$, and the lower-tail count at or below the observed $M$ size is $7$.  The reported values are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These rows support a finite local-tile anomaly at the code-read layer.  They do not by themselves identify a biochemical mechanism.

\paragraph{Leave-one-code-out family row.}
The family-stratified leave-one-code-out claim evaluates $5$ families.  The hit rate above baseline is $1.0$ for bacterial-plastid, candidate-other, mitochondrial, and nuclear-ciliate families.  The alternative-yeast family has hit rate $0.8333333333333334$ above baseline.  The aggregate leave-one-code-out row evaluates $23$ tables and $63$ targets, with baseline hit rate $0.19821739130434785$, hit rate in $M\setminus T$ equal to $0.9365079365079365$, and hit rate in $M\setminus T$ plus boundary equal to $0.9841269841269841$.  The same $0.9841269841269841$ value is reported as the hit rate above baseline.  Thus the family row is not an isolated statistic: it sits over a finite set of tables and targets where most held-out code labels are recovered by the local $M\setminus T$ or boundary criterion.

\paragraph{Leave-one-codon-out row.}
The companion leave-one-codon-out probe reports anchor count $0$, boundary count $5$, predictable count $8$, high closure coverage $8$, and recovery rate $1.0$.  Since the residual row has $13$ codons, the finite accounting is compatible with the module table above: every module is accounted for, while the row distinguishes codons recovered by predictability from codons retained only as boundary cases.  This is the local sense in which the window-six coordinates expose a boundary-tile pattern.

\paragraph{Reality contact.}
The reality contact is the curated standard genetic-code bridge.  It supports codon label readback and local split checks: a codon can be read as an amino-acid or stop label, and the finite table can be compared with the $Q_6$ tile geometry.  The contact does not observe translation realization, folded protein structure, physical admissibility, function realization, or a universal mechanism.  The table is therefore a code-layer contact only.

\paragraph{Boundary verdict.}
The ready claim is that window-six codon coordinates expose a family-stable local boundary-tile signal for the curated standard-code label table.  The evidence is the $13$-codon residual row, the punctured-cube face-vector $(13,16,4)$, the spectral values $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, $\mu=3.051487585298128$, the exact-shape and high-Jaccard null values $0.0005$ and $0.0125$, the amino-acid-class-preserving lower-tail value $0.0035$, the complete six-module classification, the aggregate $23$-table and $63$-target leave-one-code-out rates, and the five-family stratification.  The cannot-claim boundary is part of the result: no translation, folding, physical admissibility, biological function, mechanism, or universality follows from this chapter unless a separate higher-layer reality contact supplies it.  Amino-acid label-sequence readback is not folded protein structure, and a local codon tile is not a global biological law.