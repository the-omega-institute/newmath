\origin{ai}

\subsection{Leave-One-Code-Out Family Stability for the Window-Six Codon Tile}
\label{subsec:h1-leave-one-code-out-by-family}

\paragraph{Finite-row object.}
This record concerns the standard-code codon table as a code-read contact, not as a mechanism of translation.  The carrier is the six-bit codon cube $Q_6$, read through the finite display of codons over the alphabet $\{\mathrm{A},\mathrm{C},\mathrm{G},\mathrm{U}\}$.  The observed local tile is the finite residual set
$$
\begin{aligned}
R = \{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The row audit gives $|R|=13$, and the same thirteen codons are returned by the punctured-cube test.  The punctured-cube face count is
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4),
\end{aligned}
$$
so the finite object being tested is not merely a list of codon names: it is a local boundary-tile subcomplex inside the codon cube.  The explicit contact used for readback is only the curated standard genetic-code table, which assigns each codon to an amino-acid or stop label.  That contact has resolution at the codon-label layer and does not observe ribosomal realization, folding, physical admissibility, biological function, or any universal mechanism.

\paragraph{Spectral and shape coordinates.}
The coordinate record includes a finite closure statistic and a spectral coordinate for the observed tile.  The sampled shape test used $N=2000$ null rows with seed $42$.  Among $57377$ distinct-sample attempts and $55377$ duplicate retries, there was $1$ exact shape match and $25$ rows with Jaccard score at least $0.9$.  Thus the reported exact-shape probability is $p_{\mathrm{exact}}=0.0005$, while the high-Jaccard probability is $p_{\geq 0.9}=0.0125$.  Under the amino-acid-class-preserving null model with $N=2000$, the observed closure size is $|M|=20$, the exact-shape count is $0$, and the lower-tail count at or below the observed $M$ size is $7$.  The corresponding finite-row probabilities are
$$
\begin{aligned}
p_{\mathrm{class\ exact}} &= 0.0,\\
p_{\mathrm{class\ lower\ tail}} &= 0.0035.
\end{aligned}
$$
The supplied spectrum has
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128,
\end{aligned}
$$
and the cubic radial polynomial residual is $-6.224354365258478\cdot 10^{-12}$.  These numbers support the finite-row claim that the window-six display contains a sharply localized tile and closure signature, but they do not identify a biochemical cause.

\paragraph{Leave-one-code-out family audit.}
The family-level leave-one-code-out audit evaluates whether the local tile continues to recover withheld code-table variation above a baseline.  The aggregate leave-one-code-out row evaluated $23$ tables and $63$ targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate inside $M\setminus T$ is $0.9365079365079365$ and the hit rate inside $M\setminus T$ plus boundary is $0.9841269841269841$.  The above-baseline aggregate hit rate is therefore
$$
\begin{aligned}
h_{\mathrm{above}}=0.9841269841269841.
\end{aligned}
$$
The family-stratified row contains $5$ families.  The hit-rate-above-baseline values are
$$
\begin{aligned}
h_{\mathrm{alternative\ yeast}} &= 0.8333333333333334,\\
h_{\mathrm{bacterial\ plastid}} &= 1.0,\\
h_{\mathrm{candidate\ other}} &= 1.0,\\
h_{\mathrm{mitochondrial}} &= 1.0,\\
h_{\mathrm{nuclear\ ciliate}} &= 1.0.
\end{aligned}
$$
Thus every reported family remains above the baseline, with four families at $1.0$ and the alternative-yeast family at $5/6$.  The chapter-specific conclusion is a stability claim about code-label readback: the same local tile that is visible in the standard-code table also retains predictive pressure when whole code families are withheld.

\paragraph{Codon-module boundary.}
The leave-one-codon-out audit reports $0$ anchors, $5$ boundary rows, $8$ high-closure-coverage rows, $8$ predictable rows, and recovery rate $1.0$.  The module classification is complete over the thirteen residual codons: $R$ has size $13$, the assigned count is $13$, and there are no extra module codons, no missing residual codons, and no unassigned codons.  The finite modules are
$$
\begin{aligned}
\mathrm{agr} &= \{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys} &= \{\mathrm{AAA}\},\\
\mathrm{cun} &= \{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met} &= \{\mathrm{AUA}\},\\
\mathrm{ssn} &= \{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp} &= \{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Each module has a predictable or boundary status in the finite audit.  This is the concrete reason the family-level result is not being stated as a vague enrichment: the relevant codons are exhausted by named finite modules, and the module accounting has no residual unmatched codon.

\paragraph{Local verdict and boundary.}
The finite-row verdict is that the window-six codon coordinates expose a local boundary-tile structure whose code-label readback is stable under the reported leave-one-code-out family audit.  The evidence basis is external reality through the curated standard-code label table, together with BEDC-style coordinate, closure, spectrum, and relation probes.  The supported claim is only at the code-read layer: curated labels can be compared with the local tile, and the reported family rows show above-baseline recovery.  No translation realization follows from this chapter.  No folded protein structure follows.  No physical admissibility, biological function, universal mechanism, or global biological law follows.  Any such higher-layer claim would require a separate contact with its own observed rows, noise model, and mismatch audit.