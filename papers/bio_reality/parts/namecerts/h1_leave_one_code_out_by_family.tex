\subsection{Leave-One-Code-Out Stability by Code Family}\label{sec:h1-leave-one-code-out-by-family}
\origin{ai}

\paragraph{Finite carrier.}
The carrier for this record is the six-bit codon cube $Q_6$, read as a finite coordinate display for the standard-code codon table.  The data do not treat the codon cube as a biological law.  They treat it as a bounded row object with four internal handles: coordinate, closure, spectrum, and relation.  The visible exceptional relation row $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The punctured-cube check returns the same $13$ codons and has face-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the row is not merely a count: it is a finite local tile with vertices, edges, and two-dimensional faces inside the codon display.

\paragraph{Spectral and shape controls.}
The row is accompanied by a spectral control.  The recorded values are $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The cubic or radial polynomial residual is $-6.224354365258478\times 10^{-12}$, so the numerical row is internally coherent to the stated tolerance.  Shape significance was tested against $N=2000$ sampled rows with seed $42$.  The experiment required $57377$ distinct-sample attempts and $55377$ duplicate retries.  It found $1$ exact shape match, giving
$$
\begin{aligned}
p_{\mathrm{exact}}=0.0005,
\qquad
p_{J\geq 0.9}=0.0125,
\end{aligned}
$$
with $25$ samples at Jaccard similarity at least $0.9$.  An amino-acid-class-preserving null model with the same seed and $N=2000$ found $0$ exact shape matches, $7$ samples in the lower tail at or below the observed $M$ size, and observed $|M|=20$.  Its reported probabilities are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}=0.0,
\qquad
p_{\mathrm{aa\ class\ lower\ tail}}=0.0035.
\end{aligned}
$$
These numbers support a finite-row shape signal at the code-label layer.  They do not identify a translation mechanism, a structural fold, a physical admissibility criterion, or a functional phenotype.

\paragraph{Leave-one-code-out family row.}
The family-level leave-one-code-out test evaluates whether the local tile remains predictive when codon tables are grouped by code family.  The row count is $5$ families.  The family hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
The related leave-one-code-out aggregate uses $23$ evaluated tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$, while the hit rate above baseline is $0.9841269841269841$.  The stricter hit rate inside $M-T$ is $0.9365079365079365$, and the rate inside $M-T$ plus boundary returns to $0.9841269841269841$.  The family row therefore says that the local codon-tile boundary is stable across the listed family partition, with the weakest listed family still above the baseline row and four of five families at $1.0$.

\paragraph{Codon-level module readback.}
The leave-one-codon-out companion row separates the $13$ codons in $R$ into named modules.  The module classification is complete: $R$ has size $13$, the assigned count is $13$, and there are no extra module codons, no codons missing from modules, and no unassigned codons.  The module rows are
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
Every module has a predictable-or-boundary witness.  The codon-level leave-one-out row records $0$ anchors, $5$ boundary codons, $8$ predictable codons, high closure coverage $8$, and recovery rate $1.0$.  This is the finite-row reason that the family-level test can be read as a local boundary phenomenon rather than as an unconstrained table coincidence.

\paragraph{Reality contact.}
The only reality contact used here is the curated standard-code table: it assigns codons to amino-acid or stop labels.  That contact supports code-layer readback and local label-split checks.  It does not contain a translation realization, a protein structure, a physical admissibility measurement, a function assay, a universal mechanism, or a proof that DNA is governed by the codon-cube geometry.  The contact therefore licenses statements about codon-to-label rows, not statements about folded proteins or biological function.

\paragraph{Boundary verdict.}
The row $h1.leave\_one\_code\_out.by\_family$ is ready as a finite code-read claim.  Its positive content is precise: in the six-bit codon display, the $13$-codon local tile has stable leave-one-code-out behavior across $5$ code-family rows, with family hit rates $0.8333333333333334,1.0,1.0,1.0,1.0$ and with supporting aggregate hit rates far above the baseline $0.19821739130434785$.  Its negative content is equally precise: no translation, folding, physical admissibility, biological function, mechanism, or universality follows unless a separate higher-layer contact supplies it.  Amino-acid label-sequence readback is still only label readback.