\origin{ai}
\subsection{Leave-One-Code-Out Stability by Code Family}
\label{sec:h1-leave-one-code-out-by-family}

\paragraph{Finite carrier and readback contact.}
This BioReality row records a finite code-layer test on the six-bit codon cube $Q_6$.  The carrier is not a mechanism for translation; it is a coordinate display in which codons are read as finite vertices and then compared with a curated standard-code label table.  The reality contact used here is exactly the codon-label readback supplied by that table: it assigns each codon to an amino-acid or stop label.  The contact therefore supports local label-split checks, but it does not observe protein realization, folded structure, physical admissibility, biological function, or a universal biological law.

\paragraph{Boundary set.}
The finite exceptional row $R$ has cardinality $13$.  Its observed members are
$$
\begin{aligned}
R = \{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
     &UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same row is certified as a punctured-cube pattern with $f$-vector $(13,16,4)$.  Thus the local tile is not merely a list of named codons: it has a finite face profile inside the coordinate cube.  The companion finite row supplies the observed size $|M|=20$; the present data do not include an explicit membership list for $M$, so this chapter uses $M$ only through its reported size, hit rates, and shape statistics.

\paragraph{Spectral row.}
The spectral finite row reports
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}
$$
The associated radial or cubic polynomial residual is $-6.224354365258478\cdot 10^{-12}$, which is numerically at tolerance scale for this finite computation.  These numbers are used only as coordinate evidence for the shape of the local code-layer tile.  They are not promoted into a claim about biochemical necessity.

\paragraph{Shape significance checks.}
Against the finite null sample with $N=2000$, seed $42$, $57377$ distinct-sample attempts, and $55377$ duplicate retries, the exact shape match count is $1$.  The corresponding exact-shape probability is
$$
\begin{aligned}
p_{\mathrm{exact}} = 0.0005.
\end{aligned}
$$
The high-overlap statistic gives $25$ matches with Jaccard score at least $0.9$, hence
$$
\begin{aligned}
p_{\mathrm{Jaccard}\ge 0.9} = 0.0125.
\end{aligned}
$$
A second amino-acid-class-preserving null model, again with $N=2000$ and seed $42$, reports observed $|M|=20$, exact match count $0$, lower-tail count $7$, exact-shape probability $0.0$, and lower-tail probability $0.0035$.  These rows support the statement that the observed coordinate pattern is finite-row atypical under the tested nulls.  They do not identify a causal translation process.

\paragraph{Leave-one-code-out aggregate.}
The aggregate leave-one-code-out row evaluates $23$ code tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$.  The observed hit rate above baseline is $0.9841269841269841$; the hit rate inside $M-T$ is $0.9365079365079365$; and the hit rate inside $M-T$ plus boundary is $0.9841269841269841$.  In finite-row terms, the local codon tile retains nearly all recoveries when the withheld unit is a code table rather than a single codon.  This is a code-read stability statement: it says that the coordinate-and-boundary representation remains predictive for label readback across the tested finite collection.

\paragraph{Family-resolved row.}
The chapter-specific row partitions the leave-one-code-out result into $5$ code families.  The family hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\_yeast} &= 0.8333333333333334,\\
\mathrm{bacterial\_plastid} &= 1.0,\\
\mathrm{candidate\_other} &= 1.0,\\
\mathrm{mitochondrial} &= 1.0,\\
\mathrm{nuclear\_ciliate} &= 1.0.
\end{aligned}
$$
Thus four of the five reported families attain full above-baseline hit rate in this finite row, while the alternative-yeast family remains high but not perfect.  The correct interpretation is family-local stability of code-label readback under this leave-one-code-out protocol.  It is not evidence that every biological code is generated by the six-bit geometry, nor that the geometry supplies a universal mechanism.

\paragraph{Single-codon and module checks.}
The leave-one-codon-out companion row reports anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$.  The module classification covers all $13$ members of $R$: assigned count $13$, no extra module codons, no missing members, and no unassigned codons.  The finite modules are
$$
\begin{aligned}
\mathrm{agr} &= \{AGA, AGG\},\\
\mathrm{ar\_lys} &= \{AAA\},\\
\mathrm{cun} &= \{CUA, CUC, CUG, CUU\},\\
\mathrm{ile\_met} &= \{AUA\},\\
\mathrm{ssn} &= \{UCA, UUA\},\\
\mathrm{stop\_trp} &= \{UAA, UAG, UGA\}.
\end{aligned}
$$
Every listed module has predictable-or-boundary status in the supplied row.  This is the finite certificate that the by-family leave-one-code-out behavior is not floating above an unclassified boundary set: the $13$ codons in $R$ are exhausted by explicit local modules.

\paragraph{Cannot-claim boundary.}
The supported claim is narrow.  Window-six codon coordinates expose a local boundary-tile structure whose code-label readback is stable in the reported leave-one-code-out family rows.  The curated standard-code table is the only external contact used here, and it is a bridge for labels, not a bridge for translation dynamics.  Therefore no translation realization, folded protein structure, physical admissibility, biological function, biochemical mechanism, or universality claim follows from this chapter unless a separate higher-layer reality contact is supplied.  Amino-acid label-sequence readback remains at the code layer; it is not folded structure and it is not function realization.