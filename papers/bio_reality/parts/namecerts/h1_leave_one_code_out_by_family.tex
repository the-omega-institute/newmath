\subsection{Leave-One-Code-Out Stability by Code Family}\label{subsec:h1-leave-one-code-out-by-family}
\origin{ai}

\paragraph{Finite carrier and readback contact.}
The row works over the codon cube $Q_6$, read as the finite six-bit codon display whose visible labels are the standard codons over $\{A,C,G,U\}$.  The biological contact is deliberately narrow: a curated standard-code table supplies codon-to-label readback at the code layer.  It does not supply translation realization, folded structure, physical admissibility, biological function, a mechanism of code emergence, or a universality theorem.  Thus every assertion below is a finite-row claim about coordinate, closure, spectrum, and relation data over the displayed codon table, not a promotion from a local tile to a global biological law.

\paragraph{Boundary set and punctured-cube row.}
The distinguished row $R$ has cardinality $13$ and is exactly
$$
\begin{aligned}
R=\{&AAA,AGA,AGG,AUA,CUA,CUC,CUG,CUU,\\
&UAA,UAG,UCA,UGA,UUA\}.
\end{aligned}
$$
The same row is reported as a punctured-cube configuration with $f$-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
This is the finite geometric object being tested: a local codon tile with a singleton-corner and three-point label-class distinction, not a claim that the genome or the translation apparatus is forced by this geometry.

\paragraph{Spectral and shape diagnostics.}
The spectral row records
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The cubic, or radial, polynomial residual is $-6.224354365258478\times 10^{-12}$, and the recorded spectral radius in tolerance is $0.6752479308830213$.  A finite null comparison with $N=2000$ trials, seed $42$, and $57377$ distinct-sample attempts reports one exact shape match and $25$ matches at Jaccard threshold at least $0.9$.  The corresponding probabilities are
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{J\geq 0.9}&=0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null row, also at $N=2000$ and seed $42$, records observed $|M|=20$, zero exact matches, and $7$ lower-tail samples at or below the observed $M$ size.  Its reported values are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These numbers support a finite non-randomness reading of the displayed row under the sampled nulls.  They do not identify a molecular mechanism.

\paragraph{Leave-one-code-out row.}
The global leave-one-code-out experiment evaluated $23$ code tables and $63$ targets.  The baseline hit rate is $0.19821739130434785$.  The hit rate above baseline is $0.9841269841269841$, the hit rate inside $M\setminus T$ is $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus boundary is $0.9841269841269841$.  The family-resolved row has $5$ families.  Its hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
The chapter-specific claim is therefore not merely that one pooled leave-one-code-out statistic is high.  The stronger finite-row observation is that the high recovery is stable across the recorded code-family partition, with four of five families at $1.0$ above baseline and the remaining alternative-yeast family still at $0.8333333333333334$.

\paragraph{Leave-one-codon-out and module partition.}
The leave-one-codon-out row records anchor count $0$, boundary count $5$, closure-coverage-high count $8$, predictable count $8$, and recovery rate $1.0$.  The module classification assigns all $13$ codons of $R$, with $R$ size $13$, assigned count $13$, no missing module codons, no extra module codons, and no unassigned codons.  The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
Every listed module has a predictable-or-boundary explanation in the finite row.  This makes the family leave-one-code-out result compatible with the codon-level module partition: the same $13$-codon boundary row is not only recovered at high rate in pooled code-table perturbations, but is also exhausted by named local modules without residue.

\paragraph{Boundary of the claim.}
The verified content is a code-read statement.  Window-six codon coordinates expose a local boundary-tile structure whose row $R$, punctured-cube $f$-vector, spectral values, null-model probabilities, leave-one-code-out family rates, and leave-one-codon-out module partition are all finite data.  The curated standard-code table is the external contact that permits codon-label readback.  No translation process, no folded protein structure, no structural order, no physical admissibility, no biological function, no universal mechanism, and no necessity claim follows from this chapter unless a separate higher-layer contact is supplied.  Amino-acid label-sequence readback remains a code-layer observation; it is not folded structure and not functional realization.