\subsection{Leave-One-Code-Out Family Stability for the Codon Tile}\label{sec:h1-leave-one-code-out-by-family}
\origin{ai}

\paragraph{Record.}
This finite-row record concerns the standard-code codon table as a code-read object, not as a mechanism of translation.  The carrier is the six-bit codon cube $Q_6$, read through a curated standard-code label table assigning each codon to an amino-acid label or to stop.  The observed local tile is the subset
$$
\begin{aligned}
R=\{&AAA,AGA,AGG,AUA,CUA,CUC,CUG,CUU,\\
&UAA,UAG,UCA,UGA,UUA\}.
\end{aligned}
$$
The row count is $|R|=13$.  The same thirteen-row set is also reported as a punctured-cube object with face vector $(13,16,4)$, so the finite evidence is not merely a list of exceptional labels: it has a local cubical incidence profile inside the window-six coordinate display.

\paragraph{Spectral coordinates.}
The associated finite-row spectral readback gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$.  The reported radial cubic check has residual $-6.224354365258478\times 10^{-12}$, the same value listed as the cubic-polynomial residual.  Thus the numerical row is internally coherent at the stated tolerance: the local tile, its complementing ambient marker, and the radial parameter are not separate narrative decorations but entries of one finite coordinate computation.

\paragraph{Shape significance.}
Against the sampled null row for the $M$-shape, the experiment used $N=2000$ distinct samples after $57377$ sample attempts and $55377$ duplicate retries.  It found $1$ exact shape match and $25$ samples with Jaccard score at least $0.9$.  The corresponding empirical probabilities are
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9}&=0.0125.
\end{aligned}
$$
A second amino-acid-class-preserving null row used $N=2000$, with observed $M$-size $20$, exact-match count $0$, and lower-tail count $7$.  Its reported probabilities are $p_{\mathrm{aa\ class\ exact}}=0.0$ and $p_{\mathrm{aa\ class\ lower\ tail}}=0.0035$.  These rows support the narrower claim that the observed code-label geometry is unusual under the stated finite null models; they do not identify a biological cause.

\paragraph{Leave-one-code-out row.}
The leave-one-code-out aggregate evaluated $23$ tables and $63$ total targets.  The baseline hit rate was $0.19821739130434785$.  The hit rate in $M\setminus T$ was $0.9365079365079365$, and the hit rate in $M\setminus T$ plus boundary was $0.9841269841269841$.  The row also reports $0.9841269841269841$ as the hit rate above baseline.  At the family level there are $5$ families, with hit rates above baseline
$$
\begin{aligned}
\mathrm{alternative\_yeast}&=0.8333333333333334,\\
\mathrm{bacterial\_plastid}&=1.0,\\
\mathrm{candidate\_other}&=1.0,\\
\mathrm{mitochondrial}&=1.0,\\
\mathrm{nuclear\_ciliate}&=1.0.
\end{aligned}
$$
The only family below perfect recovery in this row is alternative yeast, and it remains above the aggregate baseline by a large margin.  This is the chapter-specific content of the claim: the local tile is not only visible in the standard-code coordinate display, but remains highly predictive across the finite family split used in the leave-one-code-out test.

\paragraph{Module classification.}
The thirteen codons of $R$ are completely assigned to six modules.  The AGR module is $\{AGA,AGG\}$, the AR-Lys module is $\{AAA\}$, the CUN module is $\{CUA,CUC,CUG,CUU\}$, the Ile-Met module is $\{AUA\}$, the SSN module is $\{UCA,UUA\}$, and the Stop-Trp module is $\{UAA,UAG,UGA\}$.  The classification row reports $R\_size=13$, assigned count $13$, no extra module codons, no missing codons from the modules, and no unassigned codons.  In the leave-one-codon-out row, the anchor count is $0$, the boundary count is $5$, closure coverage high is $8$, predictable count is $8$, and the recovery rate is $1.0$.  Hence every displayed module has either predictable or boundary support in the finite row, while none of the thirteen codons is left outside the module accounting.

\paragraph{Boundary.}
The reality contact is the curated standard-code table.  It can test code-layer readback and local codon-tile label splits.  It cannot test translation realization, folded structure, structural order, physical admissibility, biological function, universal mechanism, or necessity of the BEDC geometry in DNA.  Amino-acid label-sequence readback is still only label readback.  Therefore the supported conclusion is local and finite: window-six codon coordinates expose a stable boundary-tile pattern for the curated code labels, with strong leave-one-code-out family performance.  No translation, folding, physical admissibility, function, mechanism, or universality follows unless a separate higher-layer reality contact supplies that claim.