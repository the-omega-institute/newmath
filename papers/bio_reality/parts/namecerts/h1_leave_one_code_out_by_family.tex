\origin{ai}

\subsection{Leave-One-Code-Out Stability by Code Family}
\label{sec:h1-leave-one-code-out-by-family}

\paragraph{Claim.}
The finite row for \texttt{h1.leave\_one\_code\_out.by\_family} records a code-read phenomenon on the six-bit codon cube $Q_6$.  The object under test is the standard-code codon table as a curated codon-to-label assignment, not a translation apparatus and not a protein-structure observation.  The claim is that the local window-six coordinate presentation separates a small boundary region whose label behavior remains stable when related genetic-code tables are withheld by family.  The tested readout is therefore a family-stratified recovery check for codon labels, with the following scope: the row may support local code-label readback, but it does not by itself establish translation, folding, physical admissibility, biological function, mechanism, or universality.

\paragraph{Finite carrier and boundary set.}
The finite coordinate carrier is the codon cube $Q_6$, read as a six-bit display of the $64$ codons.  In the verified standard-code row, the distinguished boundary set $R$ has cardinality $13$:
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\mathrm{UAA},\mathrm{UAG},\\
&\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row verifies that this set has the punctured-cube face profile
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the code-read packet is not merely counting exceptional codons.  It records a finite subcomplex with $13$ vertices, $16$ one-dimensional incidences, and $4$ local faces inside the window-six coordinate cube.  The singleton-corner and three-point label-class distinctions are used only as local label geometry; they are not promoted to global biological law.

\paragraph{Spectral and sampling checks.}
The supplied spectral row gives
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The radial cubic check is numerically closed at the recorded tolerance, with value $-6.224354365258478\times 10^{-12}$.  The sampling row for the $M$-shape comparison used $N=2000$ samples, seed $42$, and $57377$ distinct-sample attempts, of which $55377$ were duplicate retries.  It observed exactly one exact shape match, giving
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9}&=0.0125,
\end{aligned}
$$
with $25$ high-Jaccard matches.  Under the amino-acid-class-preserving null, the observed $M$ size is $20$, the exact-match count is $0$, and the lower-tail count at or below the observed size is $7$ out of $2000$, giving
$$
\begin{aligned}
p_{\mathrm{class\ exact}}&=0.0,\\
p_{\mathrm{class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These figures support a finite geometric enrichment claim for the code-label layer.  They do not supply a biochemical realization claim.

\paragraph{Leave-one-code-out family row.}
The family-stratified leave-one-code-out run evaluated $5$ code families.  The recorded hit rates above the baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast}&:0.8333333333333334,\\
\mathrm{bacterial\ plastid}&:1.0,\\
\mathrm{candidate\ other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\ ciliate}&:1.0.
\end{aligned}
$$
The non-stratified companion row evaluated $23$ tables and $63$ total targets.  Its baseline hit rate is $0.19821739130434785$, while the observed above-baseline hit rate is $0.9841269841269841$.  The hit rate inside $M\setminus T$ is $0.9365079365079365$, and the rate inside $M\setminus T$ plus the boundary is $0.9841269841269841$.  The family row is therefore not a one-family artifact in the supplied data: four of the five family bins reach $1.0$, and the remaining alternative-yeast bin remains at $5/6$.

\paragraph{Codon-level module accounting.}
The leave-one-codon-out companion row reports $8$ predictable codons, $5$ boundary codons, $0$ anchors, and recovery rate $1.0$.  Its module classification assigns all $13$ boundary codons and leaves no missing, extra, or unassigned codons.  The verified modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
The module row reports $R$ size $13$, assigned count $13$, empty extra-module codons, empty missing-from-modules list, and empty unassigned-codons list.  This makes the local boundary description closed at the finite row level: every codon in $R$ is accounted for by one named module, and every module contains at least a predictable or boundary member.

\paragraph{Reality contact and blocked promotions.}
The only reality contact used here is the curated standard genetic-code table, understood as a codon-label table.  It can test code-read behavior and local codon-tile label splits.  It cannot test translation realization, structural order, physical admissibility, or function.  Consequently, the chapter records a positive code-layer observation and a negative boundary condition at the same time.  A label sequence recovered from codons is still only a label sequence.  No folded protein structure, no physical admissibility statement, no biological function, no universal mechanism, and no cross-organism law follows from these rows unless a separate contact supplies observations at that higher layer.

\paragraph{Conclusion.}
The row \texttt{h1.leave\_one\_code\_out.by\_family} is ready as a finite code-read claim.  The strongest supported statement is that the window-six codon coordinate system, together with the curated standard-code labels, identifies a $13$-codon boundary set with punctured-cube face data, nontrivial spectral values, significant sampling enrichment, complete module accounting, and strong leave-one-code-out recovery across five code families.  The supported layer stops at codon-to-label readback.  Higher biological interpretations require separate observations and are not carried by this packet.