\subsection{Leave-one-code-out stability by code family}\label{sec:h1-leave-one-code-out-by-family}
\origin{ai}

\paragraph{Finite object and contact.}
This BioReality record concerns the standard-code codon table as a finite code-read contact.  The BEDC minimal form used here is the six-bit codon cube $Q_6$, equipped with coordinate, closure, spectrum, and relation structure, and read back through the curated standard-code label table.  The biological object is not translation itself, but the finite assignment of codons to amino-acid or stop labels supplied by the curated table.  The relevant local distinction is a window-six codon display with local tile faces, singleton label corners, and three-point label classes.  The contact therefore tests code-layer label readback and local codon-tile label splits only.

\paragraph{Boundary row.}
The conjectural row is $\texttt{codon.window6.local.tile.boundary}$: window-six codon coordinates may expose local boundary-tile structure, while the amino-acid and stop labels remain external reality contacts.  The associated boundary probe is $\texttt{codon.local.tile.no.global.law}$.  Its support condition is deliberately narrow: the curated code table supports codon-label readback and local split checks.  It does not supply translation realization, folded structure, physical admissibility, biological function, mechanism, or universality.  Thus a local codon tile is not promoted to a global biological law.

\paragraph{The distinguished relation $R$.}
The finite relation selected by the coordinate pipeline has cardinality $13$.  The verified codon set is
$$\begin{aligned}
R=\{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}$$
The punctured-cube audit returns the same thirteen codons and the face-vector data
$$\begin{aligned}
f(R)=(13,16,4).
\end{aligned}$$
This is the finite tile whose predictive behavior is tested below.  The statement is not that all codons, all translation systems, or all biological realizations are generated by this tile; the statement is only that this finite relation has a stable local code-read signature against the supplied contacts.

\paragraph{Spectral row.}
The spectral audit separates the selected relation from a merely nominal list of codons.  The recorded values are
$$\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128.
\end{aligned}$$
The radial cubic check gives
$$\begin{aligned}
P(\mu)=-6.224354365258478\cdot 10^{-12},
\end{aligned}$$
and the same numerical residual is recorded as the cubic-polynomial value.  The spectral radius entry is $0.6752479308830213$, matching $\lambda_M$ within the stated tolerance row.  These numbers are used as finite-row diagnostics of the coordinate object, not as physical constants or mechanistic parameters.

\paragraph{Shape significance and null rows.}
The shape-significance finite run used $N=2000$ sampled tables, seed $42$, and $57377$ distinct-sample attempts with $55377$ duplicate retries.  It found $1$ exact shape match and $25$ samples with Jaccard similarity at least $0.9$.  Consequently
$$\begin{aligned}
p_{\mathrm{exact}} &= 0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9} &= 0.0125.
\end{aligned}$$
The amino-acid-class-preserving null also used $N=2000$ and seed $42$.  It records observed $M$-size $20$, exact-match count $0$, and lower-tail count $7$ for samples at or below the observed $M$-size.  The corresponding rows are
$$\begin{aligned}
p_{\mathrm{aa\ class\ exact}} &= 0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}} &= 0.0035.
\end{aligned}$$
These null rows support the claim that the observed local shape is not a routine outcome of the tested finite randomizations.  They do not identify a biochemical cause.

\paragraph{Leave-one-code-out row.}
Across the leave-one-code-out experiment, $23$ code tables were evaluated and $63$ targets were tested.  The baseline hit rate was $0.19821739130434785$.  The observed hit rate above baseline was
$$\begin{aligned}
0.9841269841269841,
\end{aligned}$$
with hit rate $0.9365079365079365$ inside $M-T$ and $0.9841269841269841$ inside $M-T$ plus boundary.  This row is the global finite summary for the code-table deletion test.  It says that the local coordinate relation often recovers the left-out code-read target beyond the baseline row; it does not say that the coordinate relation performs translation or predicts organismal viability.

\paragraph{Family-level leave-one-code-out row.}
The present claim refines the leave-one-code-out result by family.  Five families were evaluated.  The family hit rates above baseline are
$$\begin{aligned}
\text{alternative yeast} &= 0.8333333333333334,\\
\text{bacterial plastid} &= 1.0,\\
\text{candidate other} &= 1.0,\\
\text{mitochondrial} &= 1.0,\\
\text{nuclear ciliate} &= 1.0.
\end{aligned}$$
Thus four of the five named families are perfect on this finite statistic, and the remaining family is still above the baseline level.  The important point is family stability: the same local tile signature is not confined to one isolated code-table row.  The interpretation remains code-read only, because every label used by the test enters through the curated code-table contact.

\paragraph{Leave-one-codon-out and module classification.}
The codon-level deletion row records $0$ anchors, $5$ boundary codons, $8$ high-coverage closure codons, $8$ predictable codons, and recovery rate $1.0$.  The module-classification row accounts for all thirteen codons in $R$: $R$-size $13$, assigned count $13$, no extra module codons, no missing codons, and no unassigned codons.  The modules are
$$\begin{aligned}
\text{agr} &= \{\texttt{AGA},\texttt{AGG}\},\\
\text{ar\_lys} &= \{\texttt{AAA}\},\\
\text{cun} &= \{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\},\\
\text{ile\_met} &= \{\texttt{AUA}\},\\
\text{ssn} &= \{\texttt{UCA},\texttt{UUA}\},\\
\text{stop\_trp} &= \{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}.
\end{aligned}$$
Each module has a predictable or boundary interpretation in the finite row.  This supplies an internal audit of the thirteen-codon relation: no codon in $R$ is left outside the module ledger.

\paragraph{Verdict.}
The finite evidence supports the chapter claim at the code-read layer: window-six coordinates expose a local codon-tile boundary pattern whose leave-one-code-out recovery remains high by family, with family count $5$ and family-level hit rates $0.8333333333333334,1.0,1.0,1.0,1.0$.  The supporting rows include the thirteen-codon relation, the punctured-cube vector $(13,16,4)$, the spectral diagnostics, the shape-significance nulls, the $23$-table and $63$-target leave-one-code-out summary, the $1.0$ codon-level recovery rate, and the complete six-module classification.  The local boundary is therefore ready as a finite BioReality record.  It remains explicitly bounded: no translation, folding, physical admissibility, biological function, mechanism, or universality follows unless a separate contact supplies that higher-layer fact.