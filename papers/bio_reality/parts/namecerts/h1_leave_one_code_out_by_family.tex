\subsection{Family-resolved leave-one-code-out behavior in the codon cube}
\label{subsec:h1-leave-one-code-out-by-family}
\origin{ai}

\paragraph{Finite codon display.}
This record concerns the standard-code codon table viewed through a six-coordinate codon display.  The biological contact used here is the curated table assigning each codon to an amino-acid label or to stop.  The contact is intentionally narrow: it supplies code-layer labels, not translation dynamics, fold formation, physical admissibility, organismal function, or a universal mechanism.  The local question is whether the window-six coordinates and their derived tile boundary predict where leave-one-code-out targets fall after the curated labels are read into the finite codon cube.

\paragraph{Distinguished residue region.}
The punctured region $R$ has cardinality $13$.  Its recorded members are
$$
\begin{aligned}
R = \{&\texttt{AAA},\texttt{AGA},\texttt{AGG},\texttt{AUA},\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU},\\
&\texttt{UAA},\texttt{UAG},\texttt{UCA},\texttt{UGA},\texttt{UUA}\}.
\end{aligned}
$$
The same row records the cubical face count vector $(13,16,4)$ for this punctured-cube region.  Thus the family-level experiment is not being read against an unstructured list of codons: it is read against a finite codon subset with a fixed vertex count, edge count, and two-dimensional face count.  The larger tile $M$ is recorded here by size, with $|M|=20$, and by its statistical behavior; the codon-by-codon membership list of $M$ is not supplied in this row, so this subsection does not infer any additional $M$ membership beyond the supplied finite statistics.

\paragraph{Spectral calibration.}
The same finite display has a recorded spectral profile
$$
\begin{aligned}
\lambda_M &= 0.6752479308830213,\\
\lambda_R &= 0.4758352843197002,\\
\mu &= 3.051487585298128,\\
P(\mu) &= -6.224354365258478\cdot 10^{-12}.
\end{aligned}
$$
The near-zero value of the radial polynomial evaluation is used only as a finite numerical consistency check for the observed codon geometry.  It is not a claim that biology is forced by this geometry.  The contact remains the curated code table, and the spectral numbers only help describe the coordinate pattern in which the table labels are tested.

\paragraph{Null comparisons for the tile shape.}
Against $N=2000$ sampled alternatives, the shape test required $57377$ distinct-sample attempts and incurred $55377$ duplicate retries.  There was $1$ exact shape match, giving $p_{\mathrm{exact}}=0.0005$, and $25$ samples with Jaccard similarity at least $0.9$, giving $p_{\mathrm{Jaccard}\ge 0.9}=0.0125$.  Under the amino-acid-class-preserving null model with the same $N=2000$ and seed $42$, the observed $M$ size was $20$, the exact-match count was $0$, and the lower-tail count at or below the observed size was $7$.  The corresponding values are
$$
\begin{aligned}
p_{\mathrm{aa\ class,exact}} &= 0.0,\\
p_{\mathrm{aa\ class,lower\ tail}} &= 0.0035.
\end{aligned}
$$
These values support the finite claim that the observed code-layer tile is unusually placed relative to the listed null procedures.  They do not identify a biochemical cause.

\paragraph{Leave-one-code-out rates.}
The aggregate leave-one-code-out row evaluates $23$ tables and $63$ targets.  The baseline hit rate is $0.19821739130434785$.  The observed hit rate above baseline is $0.9841269841269841$; the hit rate inside $M-T$ is $0.9365079365079365$; and the hit rate inside $M-T$ together with the boundary is $0.9841269841269841$.  The family-resolved row contains $5$ families.  Their recorded hit rates above baseline are
$$
\begin{aligned}
\text{alternative yeast} &= 0.8333333333333334,\\
\text{bacterial plastid} &= 1.0,\\
\text{candidate other} &= 1.0,\\
\text{mitochondrial} &= 1.0,\\
\text{nuclear ciliate} &= 1.0.
\end{aligned}
$$
Thus four of the five families are recorded at full above-baseline recovery, while the alternative-yeast family remains high but not perfect.  The finite interpretation is family-level stability of the local code-read signal, with one explicitly measured departure from the full-rate pattern.

\paragraph{Leave-one-codon-out boundary check.}
The codon-level leave-one-out row records $0$ anchors, $5$ boundary codons, $8$ high-coverage closure codons, $8$ predictable codons, and recovery rate $1.0$.  This is the local boundary statement: the recovery is complete for the tested codon-level task, but the recovery is a statement about code labels in the finite table.  It does not assert that a codon tile realizes translation, produces a folded structure, satisfies physical constraints, determines biological function, or holds as a universal law across biological contexts.

\paragraph{Module partition.}
The module classification is complete for the $13$ codons in $R$: $R\_\mathrm{size}=13$, assigned count $13$, no extra module codons, no missing codons, and no unassigned codons.  The modules are recorded as
$$
\begin{aligned}
\text{agr} &= \{\texttt{AGA},\texttt{AGG}\},\\
\text{ar\_lys} &= \{\texttt{AAA}\},\\
\text{cun} &= \{\texttt{CUA},\texttt{CUC},\texttt{CUG},\texttt{CUU}\},\\
\text{ile\_met} &= \{\texttt{AUA}\},\\
\text{ssn} &= \{\texttt{UCA},\texttt{UUA}\},\\
\text{stop\_trp} &= \{\texttt{UAA},\texttt{UAG},\texttt{UGA}\}.
\end{aligned}
$$
Every listed module has a predictable or boundary status in the recorded classification.  This supplies the local finite explanation for why the family-level leave-one-code-out rates can be read as a boundary-tile phenomenon rather than as a free-form label coincidence.

\paragraph{Scope of the claim.}
The resulting claim is ready at the code-read layer: in the supplied finite rows, the six-coordinate codon display, the punctured region $R$, the size and null behavior of $M$, the spectral calibration, the leave-one-code-out rates, and the complete module partition jointly support a family-resolved local tile signal for the curated standard-code table.  The statement stops there.  A separate contact would be required before making any claim about translation realization, folded protein structure, physical admissibility, biological function, mechanism, or universality.