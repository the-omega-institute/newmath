\subsection{Leave-One-Code-Out Families in the Window-Six Codon Tile}
\label{subsec:bioreality-leave-one-code-out-by-family}
\origin{ai}

\paragraph{Finite carrier and readback contact.}
This BioReality record works over the six-bit codon cube $Q_6$, with each codon displayed as a finite coordinate row and read back through the curated standard genetic-code table. The contact is deliberately narrow: it supplies codon-to-label assignments for amino-acid and stop labels, and it does not supply translation realization, folded structure, physical admissibility, biological function, a mechanism, or universality. The local object tested here is a boundary tile inside the code-read layer. The carrier distinctions are the six-bit codon display, a local tile face, a singleton label corner, and a three-point label class; the internal bookkeeping uses coordinate, closure, spectrum, and relation data.

\paragraph{Boundary row $R$.}
The distinguished row $R$ has cardinality $13$. The enumerated codons are
$$
\begin{aligned}
R=\{&\mathrm{AAA},\mathrm{AGA},\mathrm{AGG},\mathrm{AUA},\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU},\\
&\mathrm{UAA},\mathrm{UAG},\mathrm{UCA},\mathrm{UGA},\mathrm{UUA}\}.
\end{aligned}
$$
The same row is reported as a punctured-cube shape with $f$-vector $(13,16,4)$. Thus the finite evidence is not merely a count: the row occupies $13$ vertices, carries $16$ one-dimensional incidences, and has $4$ two-dimensional faces in the observed local tile. This is the finite geometry against which later code-read recoveries are compared.

\paragraph{Spectral coordinates.}
The spectral probe gives $\lambda_M=0.6752479308830213$, $\lambda_R=0.4758352843197002$, and $\mu=3.051487585298128$. The cubic or radial polynomial residual is $-6.224354365258478\times 10^{-12}$, so the reported spectral radius is numerically stable at $0.6752479308830213$ within the row tolerance used by the experiment. In compact form, the finite spectral row is
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128,\\
P_{\mathrm{rad}}&=-6.224354365258478\times 10^{-12}.
\end{aligned}
$$
These numbers are used only as code-layer coordinates. They do not promote the tile into a biological mechanism.

\paragraph{Shape significance and null comparison.}
The finite shape test used $N=2000$ sampled alternatives, with $57377$ distinct sample attempts and $55377$ duplicate retries. It found $1$ exact shape match and $25$ matches with Jaccard score at least $0.9$. The corresponding probabilities are
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9}&=0.0125.
\end{aligned}
$$
The amino-acid-class-preserving null also used $N=2000$. In that null, the observed $M$ size was $20$, the exact-shape count was $0$, and the lower-tail count at or below the observed $M$ size was $7$. The reported probabilities are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
These rows support the statement that the observed local tile is not a common outcome under the sampled finite nulls. They do not identify a causal molecular path.

\paragraph{Leave-one-code-out recovery.}
The leave-one-code-out experiment evaluated $23$ code tables and $63$ held-out targets. The baseline hit rate was $0.19821739130434785$. The hit rate above baseline was $0.9841269841269841$, the hit rate inside $M\setminus T$ was $0.9365079365079365$, and the hit rate inside $M\setminus T$ plus the boundary was $0.9841269841269841$. Thus the finite code-read row is
$$
\begin{aligned}
\mathrm{tables}&=23,\\
\mathrm{targets}&=63,\\
\mathrm{baseline}&=0.19821739130434785,\\
\mathrm{above\ baseline}&=0.9841269841269841,\\
\mathrm{inside}\ M\setminus T&=0.9365079365079365,\\
\mathrm{inside}\ (M\setminus T)\cup \partial&=0.9841269841269841.
\end{aligned}
$$
This is the direct finite-row basis for the family-level claim: leave-one-code-out targets are usually recovered by the local coordinate-and-boundary construction, relative to the baseline row.

\paragraph{Family stratification.}
The family-stratified row contains $5$ families. Their hit rates above baseline are
$$
\begin{aligned}
\mathrm{alternative\ yeast}&=0.8333333333333334,\\
\mathrm{bacterial\ plastid}&=1.0,\\
\mathrm{candidate\ other}&=1.0,\\
\mathrm{mitochondrial}&=1.0,\\
\mathrm{nuclear\ ciliate}&=1.0.
\end{aligned}
$$
The weakest listed family is alternative yeast, at $5/6=0.8333333333333334$ if read as the displayed decimal fraction. The other four listed families have full above-baseline hit rate in the finite row. The claim is therefore family-local and code-read-local: the observed boundary tile is predictive across these named families for the reported held-out code labels, but only inside the curated label table contact.

\paragraph{Leave-one-codon-out boundary behavior.}
The leave-one-codon-out row reports $0$ anchors, $5$ boundary codons, $8$ predictable codons at high closure coverage, and $8$ predictable codons in total, with recovery rate $1.0$ and recovery rate above threshold $1.0$. This says that the codon-level finite row is carried by predictable and boundary behavior rather than by anchor recovery. The relevant scalar summary is
$$
\begin{aligned}
\mathrm{anchors}&=0,\\
\mathrm{boundary\ codons}&=5,\\
\mathrm{high\ closure\ coverage}&=8,\\
\mathrm{predictable\ codons}&=8,\\
\mathrm{recovery\ rate}&=1.0.
\end{aligned}
$$
Again, this is a code-label statement, not a statement about translation dynamics or protein realization.

\paragraph{Module classification.}
The module classification is complete over the $13$ codons of $R$: $R$ has size $13$, the assigned count is $13$, and the lists of extra module codons, missing codons, and unassigned codons are all empty. The modules are
$$
\begin{aligned}
\mathrm{agr}&=\{\mathrm{AGA},\mathrm{AGG}\},\\
\mathrm{ar\_lys}&=\{\mathrm{AAA}\},\\
\mathrm{cun}&=\{\mathrm{CUA},\mathrm{CUC},\mathrm{CUG},\mathrm{CUU}\},\\
\mathrm{ile\_met}&=\{\mathrm{AUA}\},\\
\mathrm{ssn}&=\{\mathrm{UCA},\mathrm{UUA}\},\\
\mathrm{stop\_trp}&=\{\mathrm{UAA},\mathrm{UAG},\mathrm{UGA}\}.
\end{aligned}
$$
Every listed module has predictable-or-boundary support, and no codon in $R$ is left outside the classification. This gives the family result a finite internal partition: the code-read boundary is not only a global $13$-row count, but also a complete module assignment over the observed row.

\paragraph{Local verdict and boundary.}
The finite evidence supports the following local BioReality verdict: window-six codon coordinates expose a nontrivial local boundary-tile structure for standard-code label readback, and the leave-one-code-out family row is strongly above baseline in the reported finite experiments. The support is strongest where the family hit rate is $1.0$ and weakest, among the listed families, at alternative yeast with $0.8333333333333334$. The result remains inside the code-read layer. It does not claim that DNA obeys this geometry as biological necessity. It does not claim translation realization, folded protein structure, structural order, physical admissibility, biological function, a universal mechanism, or any higher-layer biological law. Any such promotion would require a separate reality contact beyond the curated standard-code table.