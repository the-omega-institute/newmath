\subsection{Leave-one-code-out stability by code family}\label{sec:h1-leave-one-code-out-by-family}
\origin{ai}

\paragraph{Finite object and readout.}
This record studies a six-coordinate codon cube $Q_6$ whose observed labels are supplied by a curated standard genetic-code table.  The object under test is not the biochemical act of translation, nor a claim about protein shape.  It is the code-layer question of whether the window-six codon coordinate system isolates a stable local boundary tile when one compares the standard-code labels with alternate code tables grouped by family.  The finite row used here is the family-level leave-one-code-out run with experiment identifier $7dda978c6c97$, verified at $2026$-$06$-$03$ $07{:}10{:}32+00{:}00$.

\paragraph{Boundary set in the codon cube.}
The finite boundary set $R$ has cardinality $13$.  Its codons are
$$
\begin{aligned}
R=\{&AAA, AGA, AGG, AUA, CUA, CUC, CUG, CUU,\\
&UAA, UAG, UCA, UGA, UUA\}.
\end{aligned}
$$
The same finite set is recorded as a punctured-cube shape with $f$-vector
$$
\begin{aligned}
(f_0,f_1,f_2)=(13,16,4).
\end{aligned}
$$
Thus the row is not merely a scalar summary: it singles out a concrete thirteen-codon subset of $Q_6$, together with a finite face-incidence profile.  The accompanying module partition accounts for every codon in $R$ and introduces no outside codons.  The six modules are
$$
\begin{aligned}
\mathrm{agr}&=\{AGA,AGG\},\\
\mathrm{ar\_lys}&=\{AAA\},\\
\mathrm{cun}&=\{CUA,CUC,CUG,CUU\},\\
\mathrm{ile\_met}&=\{AUA\},\\
\mathrm{ssn}&=\{UCA,UUA\},\\
\mathrm{stop\_trp}&=\{UAA,UAG,UGA\}.
\end{aligned}
$$
The classification row gives $R\_\mathrm{size}=13$, $assigned\_count=13$, and empty lists for missing, extra, and unassigned codons.  Consequently every boundary codon in this record is assigned to exactly one displayed module, and the module vocabulary is exhausted by the listed thirteen codons.

\paragraph{Spectral and shape controls.}
The associated shape calculation gives an observed finite $M$-size of $20$ under the amino-acid-class-preserving null model.  In the Monte Carlo row with $N=2000$ and seed $42$, the unconstrained shape comparison reports one exact shape match and $25$ matches with Jaccard score at least $0.9$.  The corresponding probabilities are
$$
\begin{aligned}
p_{\mathrm{exact}}&=0.0005,\\
p_{\mathrm{Jaccard}\geq 0.9}&=0.0125.
\end{aligned}
$$
The same run records $57377$ distinct-sample attempts and $55377$ duplicate retries, so the finite comparison is being made against distinct sampled shapes rather than against a stream of unreconciled repeated draws.  Under the amino-acid-class-preserving null with $N=2000$ and seed $42$, the exact-match count is $0$, the lower-tail count at or below the observed $M$-size is $7$, and the reported probabilities are
$$
\begin{aligned}
p_{\mathrm{aa\ class\ exact}}&=0.0,\\
p_{\mathrm{aa\ class\ lower\ tail}}&=0.0035.
\end{aligned}
$$
The spectral row records
$$
\begin{aligned}
\lambda_M&=0.6752479308830213,\\
\lambda_R&=0.4758352843197002,\\
\mu&=3.051487585298128.
\end{aligned}
$$
The cubic radial check evaluates to $-6.224354365258478\times 10^{-12}$, and the same value is reported for the radial polynomial residual.  At the finite precision of the run, this residual is a near-zero numerical certificate that the recorded spectral quantities satisfy the intended cubic relation.

\paragraph{Leave-one-code-out family behavior.}
The primary family-level row evaluates five code families.  Their hit rates above the baseline are
$$
\begin{aligned}
\mathrm{alternative\_yeast}&:0.8333333333333334,\\
\mathrm{bacterial\_plastid}&:1.0,\\
\mathrm{candidate\_other}&:1.0,\\
\mathrm{mitochondrial}&:1.0,\\
\mathrm{nuclear\_ciliate}&:1.0.
\end{aligned}
$$
Thus four of the five displayed families achieve full above-baseline recovery in this finite row, while the alternative-yeast family remains above baseline but not perfect.  The related table-level leave-one-code-out experiment evaluates $23$ tables and $63$ targets.  Its baseline hit rate is $0.19821739130434785$, whereas the hit rate above baseline is $0.9841269841269841$.  The same row gives
$$
\begin{aligned}
\mathrm{hit\ rate\ in}\ M\setminus T&=0.9365079365079365,\\
\mathrm{hit\ rate\ in}\ (M\setminus T)\cup \partial&=0.9841269841269841.
\end{aligned}
$$
The boundary term therefore accounts for the gap between the interior-only recovery and the full above-baseline recovery in the table-level comparison.  The codon-level leave-one-out row is consistent with this interpretation: it reports anchor count $0$, boundary count $5$, high closure coverage $8$, predictable count $8$, and recovery rate $1.0$.

\paragraph{Interpretation at the code layer.}
The finite evidence supports the following code-layer statement.  When the standard genetic-code labels are read against the six-bit codon display, the thirteen-codon set $R$ forms a local boundary tile whose module partition is complete, whose spectral coordinates are numerically coherent, and whose leave-one-code-out behavior is stable across five recorded code families.  The row does not say that the cube geometry causes genetic coding.  It says that, after the curated label table is admitted as the observational contact, the coordinate system organizes the observed differences into a small boundary-supported pattern with strong finite recovery statistics.

\paragraph{Local prohibition on stronger biological claims.}
No claim about translation realization follows from this row.  No claim about folded structure follows from amino-acid label readback.  No claim about physical admissibility, biological function, evolutionary mechanism, or universal biological law follows from a local codon-tile split.  The only admitted contact here is a curated codon-to-label table, and that contact observes code labels rather than molecular execution or structural outcome.  Any promotion beyond the code layer would require a separate observation route for the higher layer being asserted.