| BEDC source | mathlib target | bridge status | constructive content | axioms | boundary |
| --- | --- | --- | --- | --- | --- |
| BEDC.Derived.IntUp.IntPairClassifier + BEDC.Derived.PrimeUp.NatMul + BEDC.Derived.NatUp.NatUnaryPrefix | mathlib Int | adequacy(0-axiom) | toInt/relIff(cross-sum); pairAdd/pairNeg/zero preservation; CInt normalization with CInt≃+Int and CInt≃+*Int; multiplication derived from BEDC NatMul via natMul_bwordLength and natMulFn_rel; pairLe derived from BEDC NatUnaryPrefix with pairLe↔Int.le, CInt.le_iff_toInt_le, total/antisymm/trans; Nontrivial; LT/DecidableEq/DecidableLE/DecidableLT/PartialOrder/LinearOrder; Dvd and dvd_iff_toInt_dvd | axioms=[] | 全部声明 0-axiom, 留在 `BedcMathlibBridge.All` 受 Gate A 守门; 构造性桥停在序与整除面; mathlib ℤ 的 ring / ordered-ring / Euclid / OrderIso 结构需 classical 公理, 记为下方 boundary-fact 行, 不作构造性 claim, 不写桥定理 |
| CInt (= BEDC.Derived.IntUp 差分对规范化) | mathlib Int 的 CommRing / IsOrderedRing / IsStrictOrderedRing / EuclideanDomain / OrderIso(≃o) | boundary fact (not bridged) | 无 — BEDC 不对这些作构造性 claim, 桥侧无对应 Lean 声明 | n/a (无桥声明) | 边界事实, 非桥定理: mathlib 侧 ℤ 的这些结构经 classical 公理成立 — CommRing / ZeroLEOneClass=[propext]; IsOrderedAddMonoid=[propext, Quot.sound]; OrderIso=[Quot.sound]; IsOrderedRing / IsStrictOrderedRing / EuclideanDomain=[propext, Classical.choice, Quot.sound]。这是 constructive→classical 公理边界(本桥最高价值 gap)。桥**不**用这三条公理 transport 它们(否则等同用 CIC 重抄 mathlib, 与 BEDC 脱钩, 见红线 axiom laundering) |
